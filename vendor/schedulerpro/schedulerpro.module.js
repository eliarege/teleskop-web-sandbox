const __defProp = Object.defineProperty;
const __defNormalProp = (a, b, c) => b in a ? __defProp(a, b, {
  'enumerable': true,
  'configurable': true,
  'writable': true,
  'value': c
}) : a[b] = c;
const __publicField = (a, b, c) => {
  __defNormalProp(a, typeof b !== 'symbol' ? b + '' : b, c);
  return c;
};
const __accessCheck = (a, b, c) => {
  if (!b.has(a))
    throw TypeError('Cannot ' + c);
};
const __privateGet = (a, b, c) => {
  __accessCheck(a, b, 'read from private field');
  return c ? c.call(a) : b.get(a);
};
const __privateAdd = (a, b, c) => {
  if (b.has(a))
    throw TypeError('Cannot add the same private member more than once');
  b instanceof WeakSet ? b.add(a) : b.set(a, c);
};
const __privateSet = (a, b, c, e) => {
  __accessCheck(a, b, 'write to private field');
  e ? e.call(a, c) : b.set(a, c);
  return c;
};
const registry = Object.create(null);
const helpers = [
  'Array',
  'Browser',
  [
    'Css',
    'CSSHelper'
  ],
  'Date',
  'Dom',
  'Event',
  'Function',
  'Object',
  'String',
  'TimeZone',
  'Version'
].map(a => Array.isArray(a) ? a : [
  a,
  a + 'Helper'
]);
const mapify = a => Object.assign(Object.create(null), Object.fromEntries(a));
const byName = mapify(helpers);
const byType = mapify(helpers.map(([a, b]) => [
  b,
  a
]));
const denied = a => (b, c) => {
  throw new Error('Cannot ' + a + ' "Helpers.' + c + '": object is readonly');
};
const Helpers = new Proxy(registry, {
  'defineProperty': denied('define property'),
  'deleteProperty': denied('delete'),
  'set': denied('assign'),
  'get'(a, b) {
    const c = registry[b];
    if (!c) {
      throw new Error('Property not defined "Helpers.' + b + '": ' + (b in byName ? 'import ' + byName[b] + ' before using it' : 'invalid helper name'));
    }
    return c;
  }
});
Object.defineProperty(registry, 'register', {
  'value'(a) {
    const b = a.$name, c = byType[b];
    registry[c] = a;
  }
});
helpers.forEach(a => registry[a[0]] = null);
const Helpers_default = Helpers;
let _a;
const BrowserHelper = class {
  static isPhone = (_a = globalThis.matchMedia) == null ? void 0 : _a.call(globalThis, '(max-height:414px) or (max-width:414px)').matches;
  static PointerEventConstructor = globalThis.PointerEvent || globalThis.CustomEvent;
  static supportsPointerEventConstructor = typeof PointerEvent !== 'undefined';
  static supportsPointerEvents = Boolean(globalThis.PointerEvent || globalThis.MSPointerEvent);
  static $name = 'BrowserHelper';
  static cacheFlags(a = navigator.platform, b = navigator.userAgent) {
    const c = this;
    c._isLinux = Boolean(a.match(/Linux/));
    c._isMac = Boolean(a.match(/Mac/));
    c._isWindows = Boolean(a.match(/Win32/));
    c._isMobile = Boolean(b.match(/Mobile|Opera Mini|Opera Mobi|Puffin/) || typeof globalThis.orientation === 'number');
    c._isWebkit = Boolean(b.match(/WebKit/) && !b.match(/Edg/));
    c._firefoxVersion = c.getVersion(b, /Firefox\/(\d+)\./);
    c._isFirefox = c._firefoxVersion > 0;
    c._chromeVersion = c.getVersion(b, /Chrom(?:e|ium)\/(\d+)\./);
    c._isChrome = c._chromeVersion > 0;
    c._isSafari = Boolean(b.match(/Safari/)) && !c._isChrome;
    c._isMobileSafari = Boolean(b.match(/Mobile.*Safari/));
    c._safariVersion = c.getVersion(b, /Version\/(.*).Safari/);
    c._isAndroid = Boolean(b.match(/Android/g));
  }
  static get supportsOverflowClip() {
    if (this._supportsOverflowClip == null) {
      const a = document.createElement('div');
      a.style.overflow = 'clip';
      a.style.display = 'none';
      document.documentElement.appendChild(a);
      this._supportsOverflowClip = a.ownerDocument.defaultView.getComputedStyle(a).getPropertyValue('overflow') === 'clip';
      a.remove();
    }
    return this._supportsOverflowClip;
  }
  static get supportsSticky() {
    return true;
  }
  static getVersion(a, b) {
    const c = a.match(b);
    return c ? parseFloat(c[1]) : 0;
  }
  static get isTouchDevice() {
    if (this._isTouchDevice === void 0) {
      this._isTouchDevice = globalThis.matchMedia('(any-pointer:coarse)').matches;
    }
    return this._isTouchDevice;
  }
  static get isHoverableDevice() {
    if (this._isHoverableDevice === void 0) {
      this._isHoverableDevice = globalThis.matchMedia('(any-hover: hover)').matches;
    }
    return this._isHoverableDevice;
  }
  static get isBrowserEnv() {
    return typeof window !== 'undefined';
  }
  static get isMac() {
    return this._isMac;
  }
  static get isWindows() {
    return this._isWindows;
  }
  static get isLinux() {
    return this._isLinux;
  }
  static get isAndroid() {
    return this._isAndroid;
  }
  static get isWebkit() {
    return this._isWebkit;
  }
  static get isChrome() {
    return this._isChrome;
  }
  static get chromeVersion() {
    return this._chromeVersion;
  }
  static get isFirefox() {
    return this._isFirefox;
  }
  static get firefoxVersion() {
    return this._firefoxVersion;
  }
  static get isSafari() {
    return this._isSafari;
  }
  static get safariVersion() {
    return this._safariVersion;
  }
  static get isMobileSafari() {
    return this._isMobileSafari;
  }
  static get isMobile() {
    return this._isMobile;
  }
  static get platform() {
    const a = this;
    return a._isLinux ? 'linux' : a._isMac ? 'mac' : a._isWindows ? 'windows' : a._isAndroid ? 'android' : a._isMobileSafari ? 'ios' : null;
  }
  static get supportsRandomUUID() {
    let a, b;
    if (this._supportsRandomUUID === void 0) {
      try {
        this._supportsRandomUUID = Boolean(((b = (a = globalThis.crypto).randomUUID) == null ? void 0 : b.call(a).length) > 0);
      } catch (c) {
        this._supportsRandomUUID = false;
      }
    }
    return this._supportsRandomUUID;
  }
  static get storageAvailable() {
    let a, b;
    try {
      a = localStorage;
      b = '__storage_test__';
      a.setItem(b, b);
      a.removeItem(b);
      return true;
    } catch (c) {
      return c instanceof DOMException && (c.code === 22 || c.code === 1014 || c.name === 'QuotaExceededError' || c.name === 'NS_ERROR_DOM_QUOTA_REACHED') && a.length !== 0;
    }
  }
  static setLocalStorageItem(a, b) {
    this.storageAvailable && localStorage.setItem(a, b);
  }
  static getLocalStorageItem(a) {
    return this.storageAvailable && localStorage.getItem(a);
  }
  static removeLocalStorageItem(a) {
    this.storageAvailable && localStorage.removeItem(a);
  }
  static get ctrlKey() {
    return this.isMac ? 'metaKey' : 'ctrlKey';
  }
  static get metaKey() {
    return this.isMac ? 'ctrlKey' : 'metaKey';
  }
  static searchParam(a, b = null, c = document.location.search) {
    const e = new RegExp('[?&]' + a + '=?([^&]*)'), f = c.match(e);
    return f && f[1] || b;
  }
  static getCookie(a) {
    const b = encodeURIComponent(a) + '=', e = document.cookie.split(';');
    for (let f = 0; f < e.length; f++) {
      let g = e[f];
      while (g.charAt(0) === ' ') {
        g = g.substring(1, g.length);
      }
      if (g.indexOf(b) === 0) {
        return decodeURIComponent(g.substring(b.length, g.length));
      }
    }
    return '';
  }
  static download(b, c) {
    const e = document.createElement('a');
    e.download = b;
    e.href = c || b;
    e.style.cssText = 'display:none';
    document.body.appendChild(e);
    e.click();
    document.body.removeChild(e);
  }
  static downloadBlob(a, b) {
    const c = globalThis.URL.createObjectURL(a);
    this.download(b, c);
    globalThis.URL.revokeObjectURL(c);
  }
  static get queryString() {
    let a;
    const b = new URL(globalThis.location.href).searchParams;
    return (a = Object.fromEntries) == null ? void 0 : a.call(Object, b.entries());
  }
  static copyToClipboard(a) {
    let b = true;
    const c = document.createElement('textarea');
    c.value = a;
    c.style.height = c.style.width = 0;
    document.body.appendChild(c);
    c.select();
    try {
      document.execCommand('copy');
    } catch (f) {
      b = false;
    }
    c.remove();
    return b;
  }
  static isBryntumOnline(a) {
    a = Array.isArray(a) ? a : [a];
    return Boolean(/^(www\.)?bryntum\.com/.test(globalThis.location.host) || (a == null ? void 0 : a.some(b => this.queryString[b] != null)));
  }
  static get isCSP() {
    const {
      bryntum: a,
      document: b
    } = globalThis;
    if (a.CSP == null) {
      a.CSP = Boolean(b.querySelector('meta[http-equiv="Content-Security-Policy"]'));
    }
    return a.CSP;
  }
};
Helpers_default.register(BrowserHelper);
if (BrowserHelper.isBrowserEnv) {
  BrowserHelper.cacheFlags();
}
BrowserHelper._$name = 'BrowserHelper';
const {
  hasOwnProperty: hasOwnProperty2,
  toString
} = Object.prototype;
const {isFrozen} = Object;
const afterRe = /\s*<\s*/;
const beforeRe = /\s*>\s*/;
const blendOptions = {};
const typeCache = {};
const emptyObject = Object.freeze({});
const Objects = class _Objects {
  static assign(a, ...b) {
    for (let c, e, f = 0; f < b.length; f++) {
      c = b[f];
      if (c) {
        for (e in c) {
          a[e] = c[e];
        }
      }
    }
    return a;
  }
  static assignIf(a, ...b) {
    for (let c, e, f = 0; f < b.length; f++) {
      c = b[f];
      if (c) {
        for (e in c) {
          if (!(e in a) || a[e] === void 0) {
            a[e] = c[e];
          }
        }
      }
    }
    return a;
  }
  static blend(a, b, c) {
    c = c || blendOptions;
    a = a || {};
    const {
      clone: clone = _Objects.clone,
      merge: merge = _Objects.blend
    } = c;
    if (Array.isArray(b)) {
      if (b.length > 1) {
        b.forEach(e => {
          a = _Objects.blend(a, e, c);
        });
        return a;
      }
      b = b[0];
    }
    if (b) {
      let e, f, g;
      for (f in b) {
        g = b[f];
        if (g && _Objects.isObject(g)) {
          e = a[f];
          c.key = f;
          if (e && _Objects.isObject(e)) {
            if (isFrozen(e)) {
              a[f] = e = clone(e, c);
            }
            g = merge(e, g, c);
          } else {
            g = isFrozen(g) ? g : clone(g, c);
          }
        }
        a[f] = g;
      }
    }
    return a;
  }
  static clone(a, b) {
    let c = a, e;
    if (a && typeof a === 'object') {
      const f = b && typeof b === 'object' && b;
      if (f) {
        b = null;
      }
      if (_Objects.isObject(a)) {
        if (a.skipClone) {
          c = a;
        } else {
          c = {};
          for (e in a) {
            c[e] = _Objects.clone(a[e]);
          }
        }
      } else if (Array.isArray(a)) {
        c = [];
        for (e = a.length; e-- > 0;) {
          c[e] = _Objects.clone(a[e]);
        }
      } else if (_Objects.isDate(a)) {
        c = new Date(a.getTime());
      } else if (b) {
        c = b(a);
      }
    }
    return c;
  }
  static mergeUniformObjects(a, ...b) {
    for (const c of b) {
      for (const e in c) {
        const f = c[e];
        if (f) {
          if (this.isObject(f)) {
            this.mergeUniformObjects(a[e] || (a[e] = {}), f);
          } else if (Array.isArray(f)) {
            if (!(e in a)) {
              a[e] = [];
            }
            this.mergeArrayOfIdentityObjects(a[e], f);
          } else {
            a[e] = this.clone(f);
          }
        }
      }
    }
    return a;
  }
  static mergeArrayOfIdentityObjects(a, b) {
    for (const c of b) {
      if (this.isObject(c)) {
        const e = a.find(f => f.id === c.id);
        if (e) {
          this.mergeUniformObjects(e, c);
        } else {
          a.push(this.clone(c));
        }
      } else {
        a.push(c);
      }
    }
  }
  static createTruthyKeys(a) {
    const b = StringHelper.split(a), c = b && {};
    if (b) {
      for (const e of b) {
        if (e) {
          c[e] = true;
        }
      }
    }
    return c;
  }
  static getPath(a, b) {
    return b.split('.').reduce((c, e) => {
      return (c || emptyObject)[e];
    }, a);
  }
  static getPathDefault(a, b, c) {
    const e = Array.isArray(b) ? b : typeof b === 'string' ? b.split('.') : [b], f = e.length - 1;
    return e.reduce((g, i, j) => {
      if (c && !(i in g)) {
        g[i] = j === f ? c : {};
      }
      return (g || emptyObject)[i];
    }, a);
  }
  static getPropertyDescriptor(a, b) {
    let c = null;
    for (let e = a; e && !c && !_Objects.hasOwn(e, 'isBase'); e = Object.getPrototypeOf(e)) {
      c = Object.getOwnPropertyDescriptor(e, b);
    }
    return c;
  }
  static hasPath(a, b) {
    return b.split('.').every(c => {
      if (a && c in a) {
        a = a[c];
        return true;
      }
      return false;
    });
  }
  static getTruthyKeys(a) {
    let b;
    if (typeof a === 'string') {
      b = [a];
    } else if (Array.isArray(a)) {
      b = a.slice();
    } else {
      b = [];
      if (a) {
        for (const c in a) {
          if (a[c]) {
            b.push(c);
          }
        }
      }
    }
    return b;
  }
  static getTruthyValues(a) {
    const b = [];
    if (a) {
      for (const c in a) {
        if (a[c]) {
          b.push(a[c]);
        }
      }
    }
    return b;
  }
  static isClass(a) {
    let b;
    if (typeof a === 'function' && ((b = a.prototype) == null ? void 0 : b.constructor) === a) {
      return true;
    }
    return false;
  }
  static isDate(a) {
    return Boolean(a == null ? void 0 : a.getUTCDate) && _Objects.typeOf(a) === 'date';
  }
  static isPromise(a) {
    if (Promise && Promise.resolve) {
      return Promise.resolve(a) === a || typeof (a == null ? void 0 : a.then) === 'function';
    }
    throw new Error('Promise not supported in your environment');
  }
  static isEmpty(a) {
    if (a && typeof a === 'object') {
      for (const b in a) {
        return false;
      }
    }
    return true;
  }
  static isObject(a) {
    const b = a == null ? void 0 : a.constructor;
    return Boolean(b ? b === Object || b.getPrototypeOf && b.prototype && !Object.getPrototypeOf(b.prototype) : a && typeof a === 'object');
  }
  static isInstantiated(a) {
    return a ? typeof a === 'object' && !_Objects.isObject(a) : false;
  }
  static merge(a, ...b) {
    return _Objects.blend(a, b);
  }
  static mergeItems(a, b, c) {
    c = c || blendOptions;
    let e, f, g, i, j, l, n;
    const {
      merge: merge = _Objects.blend
    } = c;
    a = a || {};
    if (Array.isArray(b)) {
      b.forEach(o => {
        a = _Objects.mergeItems(a, o, c);
      });
    } else if (b) {
      for (j in b) {
        n = b[j];
        e = null;
        if (j.includes('>')) {
          [j, e] = j.split(beforeRe);
          f = 0;
        } else if (j.includes('<')) {
          [e, j] = j.split(afterRe);
          f = 1;
        }
        if (j in a) {
          if (n && a[j] && merge) {
            c.key = j;
            n = merge(a[j], n, c);
          }
          a[j] = n;
        } else if (!e) {
          a[j] = n;
          i == null ? void 0 : i.set(j, i.size);
        } else {
          if (!i) {
            i = new Map();
            g = 0;
            for (const o in a) {
              i.set(o, g++);
            }
          }
          g = i.get(e);
          a[j] = n;
          if (g == null && f) {
            g = i.size;
          } else {
            l = l || [];
            g = (g || 0) + f;
            for (const p of i) {
              const [q, r] = p;
              if (g <= r) {
                l && (l[i.size - r - 1] = q);
                i.set(q, r + 1);
              }
            }
            if (l) {
              while (l.length) {
                const u = l.pop(), w = a[u];
                delete a[u];
                a[u] = w;
              }
            }
          }
          i.set(j, g);
        }
      }
    }
    return a;
  }
  static setPath(a, b, c) {
    b.split('.').reduce((e, f, g, i) => {
      const j = g === i.length - 1;
      if (j) {
        return e[f] = c;
      } else if (!(e[f] instanceof Object)) {
        e[f] = {};
      }
      return e[f];
    }, a);
    return a;
  }
  static typeOf(a) {
    let b, c;
    if (a === null) {
      c = 'null';
    } else if (a !== a) {
      c = 'nan';
    } else {
      c = typeof a;
      if (c === 'object') {
        if (a.isBase) {
          c = 'instance';
        } else if (Array.isArray(a)) {
          c = 'array';
        } else if (!(c = typeCache[b = toString.call(a)])) {
          typeCache[b] = c = b.slice(8, -1).toLowerCase();
        }
      } else if (c === 'function' && a.isBase) {
        c = 'class';
      }
    }
    return c;
  }
};
Object.defineProperty(Objects, 'hasOwn', { 'value': Object.hasOwn || ((a, b) => hasOwnProperty2.call(a, b)) });
Objects._$name = 'Objects';
let charsToEncode;
let entitiesToDecode;
let htmlEncodeRe;
let htmlDecodeRe;
const camelLettersRe = /([a-z])([A-Z])/g;
const crlfRe = /[\n\r]/g;
const escapeRegExpRe = /[.*+?^${}()|[\]\\]/g;
const htmlRe = /[&<]/;
const idRe = /(^[^a-z]+[^\w]+)/gi;
const whiteSpaceRe = /\s+/;
const domIdRe = /^[^a-z]+|[^\w:.-]+/gi;
const htmlDecoder = (a, b) => entitiesToDecode[b.toLowerCase()] || String.fromCharCode(parseInt(b.substr(2), 10));
const htmlEncoder = (a, b) => charsToEncode[b];
const hyphenateCamelLetters = (a, b, c) => {
  return b + '-' + c.toLowerCase();
};
const separateCamelLetters = (a, b, c) => {
  return b + ' ' + c.toLowerCase();
};
const replaceNonIdChar = a => {
  if (a) {
    return '_x' + [...a].map(b => b.charCodeAt(0).toString(16)).join('');
  }
  return '__blank__';
};
const hyphenateCache = {};
const separatedCache = {};
const ancestors = [];
const circularReplacer = function (a, b) {
  if (b && typeof b === 'object') {
    while (ancestors.length && ancestors[ancestors.length - 1] !== this) {
      ancestors.pop();
    }
    if (ancestors.includes(b)) {
      return '[Circular]';
    }
    ancestors.push(b);
  }
  return b;
};
const _StringHelper = class _StringHelper {
  static fakeNodeUUIDIndex = 0;
  static $name = 'StringHelper';
  static capitalize(a) {
    return a && a[0].toUpperCase() + a.substr(1);
  }
  static uncapitalize(a) {
    return a && a[0].toLowerCase() + a.substr(1);
  }
  static hyphenate(a) {
    const b = hyphenateCache[a];
    if (b) {
      return b;
    }
    return hyphenateCache[a] = a.replace(camelLettersRe, hyphenateCamelLetters);
  }
  static separate(a) {
    const b = separatedCache[a];
    if (b) {
      return b;
    }
    return separatedCache[a] = this.capitalize(a.replace(camelLettersRe, separateCamelLetters));
  }
  static createId(a) {
    return String(a).replace(idRe, replaceNonIdChar);
  }
  static makeValidDomId(a, b = '') {
    if (a == null) {
      return null;
    }
    return String(a).replace(domIdRe, b);
  }
  static escapeRegExp(a, b) {
    let c = a.replace(escapeRegExpRe, '\\$&');
    if (b !== void 0) {
      c = new RegExp(c, b);
    }
    return c;
  }
  static decodeHtml(a) {
    return a && String(a).replace(htmlDecodeRe, htmlDecoder);
  }
  static encodeHtml(a = '') {
    return a && String(a).replace(htmlEncodeRe, htmlEncoder);
  }
  static encodeHtmlBR(a) {
    let b;
    return (b = _StringHelper.encodeHtml(a)) == null ? void 0 : b.replace(crlfRe, '<br>');
  }
  static isHtml(a) {
    return typeof a === 'string' && htmlRe.test(a || '');
  }
  static initHtmlEntities(a) {
    a = a || {
      '&': '&amp;',
      '>': '&gt;',
      '<': '&lt;',
      '"': '&quot;',
      '\'': '&#39;'
    };
    const b = Object.keys(a);
    charsToEncode = a;
    entitiesToDecode = b.reduce((c, e) => {
      c[a[e]] = e;
      return c;
    }, {});
    htmlEncodeRe = new RegExp('([' + b.map(e => '[-]'.includes(e) ? '\\' + e : e).join('') + '])', 'g');
    htmlDecodeRe = new RegExp('(' + Object.values(a).join('|') + '|&#[0-9]+;)', 'ig');
  }
  static safeJsonParse(a) {
    let b = null;
    try {
      b = JSON.parse(a);
    } catch (c) {
    }
    return b;
  }
  static safeJsonStringify(a, b = null, c = null) {
    let f = null;
    try {
      if (b === 'circular') {
        ancestors.length = 0;
        b = circularReplacer;
      }
      f = JSON.stringify(a, b, c);
    } catch (g) {
    }
    return f;
  }
  static joinPaths(a, b = '/') {
    return a.join(b).replace(new RegExp('\\' + b + '+', 'g'), b);
  }
  static split(a, b = whiteSpaceRe) {
    let c = a;
    if (typeof c === 'string') {
      c = a.trim();
      c = c ? c.split(b) : [];
    }
    return c;
  }
  static xss(a, ...b) {
    const c = [];
    let e = b.length;
    c[e] = a[e];
    while (e-- > 0) {
      c[e] = a[e] + _StringHelper.encodeHtml(b[e]);
    }
    return c.join('');
  }
  static xssBR(a, ...b) {
    const c = [];
    let e = b.length;
    c[e] = a[e];
    while (e-- > 0) {
      c[e] = a[e] + _StringHelper.encodeHtmlBR(b[e]);
    }
    return c.join('');
  }
  static toJavaScriptValue(a, b) {
    const c = Objects.typeOf(a);
    if (c === 'boolean' || c === 'string' || c === 'number' || a === null) {
      return _StringHelper.safeJsonStringify(a);
    }
    if (a === globalThis) {
      return 'window';
    }
    if (c === 'date') {
      return 'new Date(' + a.getFullYear() + ', ' + a.getMonth() + ', ' + a.getDate() + ', ' + a.getHours() + ', ' + a.getMinutes() + ', ' + a.getSeconds() + ', ' + a.getMilliseconds() + ')';
    }
    if (c === 'array') {
      return '[' + a.map(e => _StringHelper.toJavaScriptValue(e, b)) + ']';
    }
    if (c === 'object' || c === 'instance') {
      return this.toJavaScriptString(a, b);
    }
    if (c === 'function') {
      let e = a.toString();
      if (e.match(/^async (\w+?)\(/)) {
        e = e.replace(/^async (\w+?)\(/, 'async function(');
      } else if (!e.startsWith('async(') && e.match(/^(\w+?)\(/)) {
        e = e.replace(/^(\w+?)\(/, 'function(');
      }
      return e;
    }
    if (c === 'class') {
      if (a.toJavaScriptValue) {
        return a.toJavaScriptValue(b);
      }
      return Object.prototype.hasOwnProperty.call(a, '$name') ? a.$name : a.name;
    }
  }
  static toJavaScriptString(a, b = {}) {
    let c;
    const e = (c = b.level) != null ? c : 0, f = 2;
    return '{\n' + Object.keys(a).map(g => ' '.repeat((e + 1) * f) + (g.match(/[- *]/) ? '"' + g + '"' : g) + (': ' + _StringHelper.toJavaScriptValue(a[g], {
      ...b,
      'level': e + 1
    }))).join(',\n') + '\n' + ' '.repeat(e * f) + '}';
  }
  static encodeAttributeSelector(a, b) {
    return '[' + a + '="' + String(b).replace(/["\\]/g, '\\$&') + '"]';
  }
  static generateUUID() {
    let a;
    if (BrowserHelper.supportsRandomUUID) {
      return globalThis.crypto.randomUUID();
    }
    if ((a = globalThis.crypto) == null ? void 0 : a.getRandomValues) {
      return ('10000000-1000-4000-8000' + -100000000000).replace(/[018]/g, b => (b ^ globalThis.crypto.getRandomValues(new Uint8Array(1))[0] & 15 >> b / 4).toString(16));
    }
    return Date.now() + '-' + ++_StringHelper.fakeNodeUUIDIndex;
  }
};
var StringHelper = _StringHelper;
StringHelper.initHtmlEntities();
Helpers_default.register(StringHelper);
StringHelper._$name = 'StringHelper';
const EMPTY_ARRAY = Object.freeze([]);
const {hasOwn} = Objects;
const firstOfAsync = async (a, b) => {
  let c = b;
  for await (c of a) {
    break;
  }
  return c;
};
const toFixedFix = 1.005.toFixed(2) === '1.01' ? null : (a, b) => {
  const c = a.toString().split('.'), e = +(!c[1] ? c[0] : c.join('.') + '1');
  return a.toFixed.call(e, b);
};
const _ObjectHelper = class _ObjectHelper extends Objects {
  static $name = 'ObjectHelper';
  static forEach(a, b) {
    let c;
    if (a) {
      if (Array.isArray(a)) {
        a.forEach(b);
      } else {
        for (c in a) {
          b(a[c], c);
        }
      }
    }
  }
  static transformArrayToNamedObject(a, b = 'ref') {
    const c = {};
    a.forEach((e, f) => {
      const g = e[b] != null && e[b].toString().length ? e[b] : f;
      c[g] = e;
    });
    return c;
  }
  static transformNamedObjectToArray(a, b = 'ref') {
    return Object.keys(a).filter(c => a[c]).map(c => {
      const e = a[c];
      e[b] = c;
      return e;
    });
  }
  static isEqual(c, e, f = false, g = false) {
    if (!g && (c === null && e !== null || c === void 0 && e !== void 0 || e === null && c !== null || e === void 0 && c !== void 0)) {
      return false;
    }
    if (c == null || e == null) {
      return c == null && e == null;
    }
    if (c === e) {
      return true;
    }
    const i = typeof c, j = typeof e;
    if (i === j) {
      switch (i) {
      case 'number':
      case 'string':
      case 'boolean':
        return c === e;
      }
      switch (true) {
      case c instanceof Date && e instanceof Date:
        return c.getTime() === e.getTime();
      case Array.isArray(c) && Array.isArray(e):
        return c.length === e.length ? c.every((k, l) => OH.isEqual(k, e[l], f)) : false;
      case i === 'object' && c.constructor.prototype === e.constructor.prototype:
        return f ? OH.isDeeplyEqual(c, e, f) : Helpers_default.String.safeJsonStringify(c, 'circular') === Helpers_default.String.safeJsonStringify(e, 'circular');
      }
    }
    return String(c) === String(e);
  }
  static isDeeplyEqual(c, e, f = {}) {
    if (c === e) {
      return true;
    }
    if (!c || !e) {
      return false;
    }
    const g = OH.keys(c, f.ignore), j = OH.keys(e, f.ignore);
    if (g.length !== j.length) {
      return false;
    }
    for (let k = 0; k < g.length; k++) {
      const l = g[k], n = j[k];
      if (l !== n) {
        return false;
      }
      const o = c[l], p = e[n];
      if (f.shouldEvaluate) {
        if (f.shouldEvaluate(l, {
            'value': o,
            'object': c
          }, {
            'value': p,
            'object': e
          }) === false) {
          continue;
        }
      }
      if (f.evaluate) {
        const q = f.evaluate(l, {
          'value': o,
          'object': c
        }, {
          'value': p,
          'object': e
        });
        if (q === false) {
          return false;
        }
        if (q === true) {
          continue;
        }
      }
      if (!OH.isEqual(o, p, f)) {
        return false;
      }
    }
    return true;
  }
  static isPartial(c, e) {
    c = String(c).toLowerCase();
    e = String(e).toLowerCase();
    return c.indexOf(e) !== -1;
  }
  static isLessThan(c, e) {
    if (c instanceof Date && e instanceof Date) {
      return Helpers_default.Date.isBefore(c, e);
    }
    return c < e;
  }
  static isMoreThan(c, e) {
    if (c instanceof Date && e instanceof Date) {
      return Helpers_default.Date.isAfter(c, e);
    }
    return c > e;
  }
  static first(a, b) {
    let c = b, e;
    if (a) {
      if (a[Symbol.iterator]) {
        for (c of a) {
          break;
        }
      } else if (a[Symbol.asyncIterator]) {
        c = firstOfAsync(a, b);
      } else if (_ObjectHelper.isObject(a)) {
        if (c === void 0) {
          c = EMPTY_ARRAY;
        }
        for (e in a) {
          c = [
            e,
            a[e]
          ];
          break;
        }
      }
    }
    return c;
  }
  static fork(a) {
    let b, c, e;
    if (a && a.constructor === Object) {
      b = Object.setPrototypeOf({}, a);
      for (c in a) {
        e = a[c];
        if (e) {
          if (e.constructor === Object) {
            b[c] = OH.fork(e);
          } else if (e instanceof Array) {
            b[c] = e.slice();
          }
        }
      }
    } else {
      b = a;
    }
    return b;
  }
  static copyProperties(a, b, c) {
    let e, f;
    for (f = 0; f < c.length; f++) {
      e = c[f];
      if (e in b) {
        a[e] = b[e];
      }
    }
    return a;
  }
  static copyPropertiesIf(a, b, c) {
    if (b) {
      for (const e of c) {
        if (!(e in a) && e in b) {
          a[e] = b[e];
        }
      }
    }
    return a;
  }
  static entries(a, b) {
    const c = [], e = typeof b === 'function';
    if (a) {
      for (const f in a) {
        if (e ? !b(f, a[f]) : !(b == null ? void 0 : b[f])) {
          c.push([
            f,
            a[f]
          ]);
        }
      }
    }
    return c;
  }
  static fromEntries(a, b) {
    b = b || {};
    if (a) {
      for (let c = 0; c < a.length; ++c) {
        b[a[c][0]] = a[c][1];
      }
    }
    return b;
  }
  static keys(a, b, c) {
    const e = [], f = typeof b === 'function';
    if (a) {
      let g = 0;
      for (const i in a) {
        if (f ? !b(i, a[i]) : !(b == null ? void 0 : b[i])) {
          e.push(c ? c(i, g) : i);
          ++g;
        }
      }
    }
    return e;
  }
  static values(a, b, c) {
    const e = [], f = typeof b === 'function';
    if (a) {
      let g = 0;
      for (const i in a) {
        if (f ? !b(i, a[i]) : !(b == null ? void 0 : b[i])) {
          e.push(c ? c(a[i], g) : a[i]);
          ++g;
        }
      }
    }
    return e;
  }
  static pathExists(a, b) {
    const c = b.split('.');
    return c.every(e => {
      if (!a || !(e in a)) {
        return false;
      }
      a = a[e];
      return true;
    });
  }
  static pathifyKeys(a, b) {
    const c = {};
    for (const e in a) {
      if (hasOwn(a, e)) {
        const f = b == null ? void 0 : b[e];
        const g = (f == null ? void 0 : f.type) === 'object' || (f == null ? void 0 : f.complexMapping) || !Boolean(b);
        if (g && Array.isArray(a[e])) {
          c[e] = a[e].slice();
        } else if (g && a[e] instanceof Object) {
          const i = this.pathifyKeys(a[e]);
          for (const j in i) {
            c[e + '.' + j] = i[j];
          }
        } else {
          c[e] = a[e];
        }
      }
    }
    return c;
  }
  static deletePath(a, b) {
    b.split('.').reduce((c, e, f, g) => {
      if (c == null) {
        return null;
      }
      if (hasOwn(c, e)) {
        if (f === g.length - 1) {
          delete c[e];
        } else {
          return c[e];
        }
      }
    }, a);
  }
  static coerce(a, b) {
    const c = Objects.typeOf(a), e = Objects.typeOf(b), f = typeof a === 'string';
    if (c !== e) {
      switch (e) {
      case 'string':
        return String(a);
      case 'number':
        return Number(a);
      case 'boolean':
        return f && (!a || a === 'false' || a === '0') ? false : Boolean(a);
      case 'null':
        return f && (!a || a === 'null') ? null : false;
      case 'undefined':
        return f && (!a || a === 'undefined') ? void 0 : false;
      case 'date':
        return f && isNaN(a) ? Helpers_default.Date.parse(a) : Date(Number(a));
      }
    }
    return a;
  }
  static wrapProperty(a, b, c, e, f = true) {
    const g = {};
    let i = Object.getPrototypeOf(a), j = Object.getOwnPropertyDescriptor(i, b);
    while (!j && i && f) {
      i = Object.getPrototypeOf(i);
      if (i) {
        j = Object.getOwnPropertyDescriptor(i, b);
      }
    }
    if (j) {
      if (j.set) {
        g.set = k => {
          j.set.call(a, k);
          e && e.call(a, j.get.call(a));
        };
      } else {
        g.set = e;
      }
      if (j.get) {
        g.get = () => {
          let k = j.get.call(a);
          if (c) {
            k = c.call(a, k);
          }
          return k;
        };
      } else {
        g.get = c;
      }
    } else {
      g.set = k => {
        a['_' + b] = k;
        e && e.call(a, k);
      };
      g.get = () => {
        let k = a['_' + b];
        if (c) {
          k = c.call(a, k);
        }
        return k;
      };
    }
    Object.defineProperty(a, b, g);
  }
  static hookProperty(a, b, c) {
    const e = _ObjectHelper.getPropertyDescriptor(c.prototype, 'value'), f = _ObjectHelper.getPropertyDescriptor(a, b), g = '_' + b, i = class {
        get value() {
          return f ? f.get.call(this) : this[g];
        }
        set value(k) {
          if (f) {
            f.set.call(this, k);
          } else {
            this[g] = k;
          }
        }
      }, j = _ObjectHelper.getPropertyDescriptor(i.prototype, 'value');
    Object.setPrototypeOf(c.prototype, i.prototype);
    Object.defineProperty(a, b, {
      'configurable': true,
      'get': e.get || j.get,
      'set': e.set || j.set
    });
    return () => delete a[b];
  }
  static cleanupProperties(a, b = false) {
    Object.entries(a).forEach(([c, e]) => {
      if (b) {
        e === void 0 && delete a[c];
      } else {
        e == null && delete a[c];
      }
    });
    return a;
  }
  static removeAllProperties(a) {
    Object.keys(a).forEach(b => delete a[b]);
    return a;
  }
  static assertType(a, b, c) {
    const e = Objects.typeOf(a);
    if (a != null && e !== b) {
      throw new Error('Incorrect type "' + e + '" for ' + c + ', expected "' + b + '"');
    }
  }
  static assertObject(a, b) {
    OH.assertType(a, 'object', b);
  }
  static assertInstance(a, b) {
    OH.assertType(a, 'instance', b);
  }
  static assertClass(a, b) {
    OH.assertType(a, 'class', b);
  }
  static assertFunction(a, b) {
    if (typeof a !== 'function' || a.isBase || a.$$name) {
      throw new Error('Incorrect type for ' + b + ', got "' + a + '" (expected a function)');
    }
  }
  static assertNumber(a, b) {
    const c = Number(a);
    if (typeof a !== 'number' || isNaN(c)) {
      throw new Error('Incorrect type for ' + b + ', got "' + a + '" (expected a Number)');
    }
  }
  static assertBoolean(a, b) {
    OH.assertType(a, 'boolean', b);
  }
  static assertString(a, b) {
    OH.assertType(a, 'string', b);
  }
  static assertArray(a, b) {
    OH.assertType(a, 'array', b);
  }
  static toFixed(a, b) {
    if (toFixedFix) {
      return toFixedFix(a, b);
    }
    return a.toFixed(b);
  }
  static roundTo(a, b = 1) {
    return Math.round(a / b) * b;
  }
  static round(a, b) {
    if (b == null) {
      return a;
    }
    const c = 10 ** b;
    return Math.round(a * c) / c;
  }
  static getMapPath(a, b, c) {
    const e = Array.isArray(b) ? b : typeof b === 'string' ? b.split('.') : [b], f = e.length === 1, g = e[0], i = a.has(g) ? a.get(g) : a.set(g, f ? c : {}).get(g);
    if (f) {
      return i;
    }
    return OH.getPathDefault(i, e.slice(1), c);
  }
};
const ObjectHelper = _ObjectHelper;
Helpers_default.register(ObjectHelper);
var OH = ObjectHelper;
ObjectHelper._$name = 'ObjectHelper';
const _VersionHelper = class _VersionHelper {
  static $name = 'VersionHelper';
  static setVersion(a, b) {
    a = a.toLowerCase();
    VH[a] = {
      'version': b,
      'isNewerThan'(f) {
        return _VersionHelper.semanticCompareVersion(f, b, '<');
      },
      'isOlderThan'(f) {
        return _VersionHelper.semanticCompareVersion(f, b, '>');
      }
    };
    let c = '';
    if (typeof productName !== 'undefined') {
      c = productName;
    }
    const e = c + '.' + a + b.replace(/\./g, '-');
    if (BrowserHelper.isBrowserEnv && !globalThis.bryntum.silenceBundleException) {
      if (globalThis.bryntum[e] === true) {
        if (this.isTestEnv) {
          globalThis.BUNDLE_EXCEPTION = true;
        } else {
          let f = c || a;
          if (f === 'core') {
            f = 'grid';
          }
          let g = StringHelper.capitalize(f);
          if (f === 'schedulerpro') {
            g = 'SchedulerPro';
          }
          throw new Error('The Bryntum ' + g + ' bundle was loaded multiple times by the application.\n\nCommon reasons you are getting this error includes:\n\n* Imports point to different types of the bundle (e.g. *.module.js and *.umd.js)\n* Imports point to both sources and bundle\n* Imports do not use the shortest relative path, JS treats them as different files\n* Cache busters differ between imports, JS treats ' + f + '.module.js?1 and ' + f + '.module.js?2 as different files\n* Imports missing file type, verify they all end in .js\n\nSee https://bryntum.com/products/' + f + '/docs/guide/' + g + '/gettingstarted/es6bundle#troubleshooting for more information\n\n');
        }
      } else {
        globalThis.bryntum[e] = true;
      }
    }
  }
  static getVersion(a) {
    a = a.toLowerCase();
    if (!VH[a]) {
      throw new Error('No version specified! Please check that you import VersionHelper correctly into the class from where you call `deprecate` function.');
    }
    return VH[a].version;
  }
  static semanticCompareVersion(a, b, c = '=') {
    a = a || '';
    b = b || '';
    const e = a.split(/[-.]/), f = b.split(/[-.]/), g = c.includes('<'), i = (k, l) => {
        const n = [
            'rc',
            'beta',
            'alpha'
          ], o = k.map(p => {
            if (n.includes(p)) {
              return -n.indexOf(p) - 2;
            }
            const q = Number.parseInt(p);
            return Number.isNaN(q) ? -n.length : q;
          });
        while (o.length < l) {
          o.push(-1);
        }
        return o;
      }, j = () => {
        const k = Math.max(e.length, f.length), l = i(e, k), n = i(f, k);
        for (let o = 0; o < k; o++) {
          if (l[o] !== n[o]) {
            return g ? l[o] < n[o] : l[o] > n[o];
          }
        }
        return true;
      };
    switch (c) {
    case '=':
      return a === b;
    case '<=':
    case '>=':
      return a === b || j();
    case '<':
    case '>':
      return a !== b && j();
    }
    return false;
  }
  static checkVersion(a, b, c) {
    return _VersionHelper.semanticCompareVersion(VH.getVersion(a), b, c);
  }
  static deprecate(a, b, c) {
    const e = VH.checkVersion(a, b, '<');
    if (e) {
      console.warn('Deprecation warning: You are using a deprecated API which will change in v' + b + '. ' + c);
    } else {
      throw new Error('Deprecated API use. ' + c);
    }
  }
  static get isTestEnv() {
    let a, b, c;
    const f = Boolean((a = globalThis.bryntum) == null ? void 0 : a.isTestEnv);
    try {
      return f || Boolean((c = (b = globalThis.parent) == null ? void 0 : b.bryntum) == null ? void 0 : c.isTestEnv);
    } catch (g) {
      return f;
    }
  }
  static get isDebug() {
    const a = false;
    return a;
  }
};
const VersionHelper = _VersionHelper;
var VH = VersionHelper;
if (BrowserHelper.isBrowserEnv) {
  if (VH.isTestEnv) {
    BrowserHelper._isHoverableDevice = true;
  }
  globalThis.bryntum = Object.assign(globalThis.bryntum || {}, {
    'getVersion': VH.getVersion.bind(VH),
    'checkVersion': VH.checkVersion.bind(VH),
    'deprecate': VH.deprecate.bind(VH),
    'license': '66250'
  });
}
Helpers_default.register(VersionHelper);
VersionHelper._$name = 'VersionHelper';
const {defineProperty, getOwnPropertyDescriptor} = Reflect;
const {
  hasOwnProperty: hasOwnProperty3,
  toString: toString2
} = Object.prototype;
const instancePropertiesSymbol = Symbol('instanceProperties');
const configuringSymbol = Symbol('configuring');
const lazyConfigValues = Symbol('lazyConfigValues');
const DATE_TYPE = toString2.call(new Date());
const whitespace = /\s+/;
const hasOwn2 = Object.hasOwn || ((a, b) => hasOwnProperty3.call(a, b));
const dateEquals = (a, b) => {
  if (a === b) {
    return true;
  }
  if (a && b && toString2.call(a) === DATE_TYPE && toString2.call(b) === DATE_TYPE) {
    return a.getTime() === b.getTime();
  }
  return false;
};
const getDay = (a, b) => {
  a.setHours(0, 0, 0, 0);
  b && a.setDate(a.getDate() + b);
  return a;
};
const specialDates = {
  'now': () => new Date(),
  'today': () => getDay(new Date()),
  'tomorrow': () => getDay(new Date(), 1),
  'yesterday': () => getDay(new Date(), -1)
};
const createClsProps = (a, b) => {
  a[b] = 1;
  return a;
};
const observableGet = (a, b) => function () {
  let c;
  (c = this.configObserver) == null ? void 0 : c.get(a, this);
  return b.call(this);
};
const Config = class _Config {
  static get(a, b) {
    const {cache: c} = this, e = c[a] || (c[a] = new _Config(a));
    let f = e, g;
    if (b) {
      g = _Config.makeCacheKey(a, b);
      if (!(f = g && c[g])) {
        f = e.extend(b);
        if (g) {
          c[g] = f;
        }
      }
    }
    return f;
  }
  constructor(a) {
    const b = this, c = a[0].toUpperCase() + a.substr(1);
    b.base = b;
    b.name = a;
    b.field = '_' + a;
    b.capName = c;
    b.changer = 'change' + c;
    b.initializing = 'initializing' + c;
    b.updater = 'update' + c;
  }
  get descriptor() {
    let a = this._descriptor;
    if (!a || !hasOwn2(this, '_descriptor')) {
      this._descriptor = a = this.makeDescriptor();
    }
    return a;
  }
  get initDescriptor() {
    let a = this._initDescriptor;
    if (!a || !hasOwn2(this, '_initDescriptor')) {
      this._initDescriptor = a = this.makeInitter();
    }
    return a;
  }
  convert(a, b) {
    return a;
  }
  equal(a, b) {
    return a === b;
  }
  extend(a) {
    const b = Object.assign(Object.create(this), a), {
        convert: c,
        equal: e,
        merge: f
      } = a, {
        convertMethods: g,
        equalityMethods: i
      } = _Config;
    if (typeof c === 'string') {
      if (!(b.convert = g[c]) && c.endsWith('[]')) {
        b.convert = g[c] = _Config.makeArrayConvert(g[c.slice(0, -2)]);
      }
    }
    if (typeof e === 'string') {
      if (!(b.equal = i[e]) && e.endsWith('[]')) {
        b.equal = i[e] = _Config.makeArrayEquals(i[e.slice(0, -2)]);
      }
    }
    if (typeof f === 'string') {
      b.merge = _Config.mergeMethods[f];
    }
    return b;
  }
  define(a) {
    const {name: b} = this, c = getOwnPropertyDescriptor(a, b);
    let e = this.descriptor;
    if (c == null ? void 0 : c.get) {
      e = Object.assign({}, e);
      e.get = observableGet(b, c.get);
    }
    defineProperty(a, b, e);
  }
  defineInitter(a, b) {
    const c = this, {name: e} = c, f = a[instancePropertiesSymbol];
    let g, i;
    if (!f[e] && (i = getOwnPropertyDescriptor(a, e)) && !('value' in i)) {
      f[e] = i;
    }
    defineProperty(a, e, c.initDescriptor);
    if (c.lazy) {
      g = a[lazyConfigValues] || (a[lazyConfigValues] = new Map());
      g.set(e, b);
    }
  }
  static makeArrayConvert(a) {
    return function (b, c) {
      if (b != null) {
        if (Array.isArray(b)) {
          b = b.map((e, f) => a.call(this, e, c));
        } else {
          b = [a.call(this, b, c)];
        }
      }
      return b;
    };
  }
  static makeArrayEquals(a) {
    return (b, c) => {
      let e, f = b && c && b.length === (e = c.length);
      if (f && Array.isArray(b) && Array.isArray(c)) {
        if (a) {
          while (f && e-- > 0) {
            f = a(b[e], c[e]);
          }
        } else {
          while (f && e-- > 0) {
            f = b[e] === c[e];
          }
        }
      } else {
        f = a ? a(b, c) : b === c;
      }
      return f;
    };
  }
  static makeCacheKey(a, b) {
    const c = Object.keys(b).sort();
    for (let e, f, g, j = c.length; j-- > 0;) {
      g = b[e = c[j]];
      if (g == null && g === false) {
        c.splice(j, 1);
      } else {
        f = typeof g;
        if (f === 'function') {
          return null;
        }
        if (f === 'string') {
          c[j] = e + ':"' + g + '"';
        } else if (f === 'number') {
          c[j] = e + ':' + g;
        }
      }
    }
    return c.length ? a + '>' + c.join('|') : a;
  }
  makeDescriptor() {
    const {
      base: a,
      field: b,
      changer: c,
      updater: e,
      name: f
    } = this;
    if (a !== this) {
      return a.descriptor;
    }
    return {
      'get'() {
        let g;
        (g = this.configObserver) == null ? void 0 : g.get(f, this);
        return this[b];
      },
      'set'(g) {
        let i, j;
        const k = this, l = k.$meta.configs[f];
        let n = k[b], o, p;
        if (typeof g === 'string') {
          let q = g;
          if (g.startsWith('up.')) {
            q = (i = k.owner) == null ? void 0 : i.resolveProperty(g.slice(3));
          } else if (g.startsWith('this.')) {
            q = k.resolveProperty(g.slice(5));
          }
          if (q !== void 0 && typeof q !== 'function') {
            g = q;
          }
        }
        if (l.convert !== defaultConvert) {
          g = l.convert(g, k);
        }
        if (k[c]) {
          o = (p = k[c](g, n)) === void 0 && 1;
          if (!o) {
            g = p;
            n = k[b];
          }
        }
        if (!o && !(l.equal === strictEquals ? n === g : l.equal(n, g))) {
          k[b] = g;
          o = 2;
          (j = k[e]) == null ? void 0 : j.call(k, g, n);
        }
        if (o && !k.isDestroyed && !k.onConfigChange.$nullFn) {
          k.onConfigChange({
            'name': f,
            'value': g,
            'was': n,
            'config': l,
            'applied': o
          });
        }
      }
    };
  }
  makeInitter() {
    const a = this;
    if (a !== a.base) {
      if (a.lazy) {
        return a.makeLazyInitter();
      }
      return a.base.initDescriptor;
    }
    return a.makeBasicInitter();
  }
  makeBasicInitter() {
    const a = this, {
        initializing: b,
        name: c
      } = a;
    return {
      'configurable': true,
      'get'() {
        const e = this;
        a.removeInitter(e);
        e[b] = true;
        e[c] = e[configuringSymbol][c];
        e[b] = false;
        e.configDone[c] = true;
        return e[c];
      },
      'set'(e) {
        a.removeInitter(this);
        this.configDone[c] = true;
        this[c] = e;
      }
    };
  }
  makeLazyInitter() {
    const a = this, {
        initializing: b,
        name: c
      } = a;
    return {
      'configurable': true,
      'get'() {
        const e = this, f = e[lazyConfigValues].get(c);
        a.removeInitter(e);
        if (!e.isDestroying) {
          e[b] = true;
          e[c] = f;
          e[b] = false;
        }
        return e[c];
      },
      'set'(e) {
        a.removeInitter(this);
        this[c] = e;
      }
    };
  }
  removeInitter(a) {
    const {name: b} = this, c = a[instancePropertiesSymbol][b], e = a[lazyConfigValues];
    if (c) {
      defineProperty(a, b, c);
    } else {
      delete a[b];
    }
    if ((e == null ? void 0 : e.delete(b)) && !e.size) {
      delete a[lazyConfigValues];
    }
  }
  setDefault(a, b) {
    defineProperty(a.prototype, this.field, {
      'configurable': true,
      'writable': true,
      'value': b
    });
  }
  merge(a, b) {
    if (b && a && Objects.isObject(a)) {
      if (b.isBase) {
        return b.setConfig(a);
      }
      if (Objects.isObject(b)) {
        a = Objects.merge(Objects.clone(b), a);
      }
    }
    return a;
  }
};
const {prototype} = Config;
var {
  convert: defaultConvert,
  equal: strictEquals
} = prototype;
Config.symbols = {
  'configuring': configuringSymbol,
  'instanceProperties': instancePropertiesSymbol,
  'lazyConfigs': lazyConfigValues
};
Config.cache = Object.create(null);
Config.convertMethods = {
  'array': Config.makeArrayConvert(defaultConvert),
  'date'(a, b) {
    let c = a, e, f, g;
    if (specialDates[a]) {
      c = specialDates[a]();
    } else if (a != null) {
      if (typeof a !== 'string') {
        e = new Date(a);
      } else {
        g = this.formats;
        if (g) {
          g = g.flatMap(i => i in b ? b[i] : i);
          for (f of g) {
            e = f && Helpers_default.Date.parse(a, f);
            if (e) {
              break;
            }
          }
        }
        if (!e) {
          e = Helpers_default.Date.parse(a);
        }
      }
      if (isNaN(e)) {
        if (this.strict) {
          throw new Error('Invalid date: ' + a);
        }
      } else {
        c = e;
      }
    }
    return c;
  },
  'day'(a, b) {
    a = Config.convertMethods.date.call(this, a, b);
    a == null ? void 0 : a.setMinutes(0, 0, 0);
    return a;
  }
};
Config.equalityMethods = {
  'array': Config.makeArrayEquals(),
  'date': dateEquals,
  'day': dateEquals,
  'strict': Config.equal = strictEquals
};
Config.mergeMethods = {
  'distinct'(a, b) {
    let c = b ? b.slice() : [];
    if (a != null) {
      if (Objects.isObject(a)) {
        if (b === void 0) {
          c = a;
        } else {
          let e, f;
          for (e in a) {
            f = c.indexOf(e);
            if (a[e]) {
              if (f < 0) {
                c.push(e);
              }
            } else if (f > -1) {
              c.splice(f, 1);
            }
          }
        }
      } else if (Array.isArray(a)) {
        a.forEach(g => !c.includes(g) && c.push(g));
      } else if (!c.includes(a)) {
        c.push(a);
      }
    }
    return c;
  },
  'merge': Config.merge = prototype.merge,
  'classList'(a, b) {
    if (typeof a === 'string') {
      if (!a.length) {
        return b;
      }
      a = a.split(whitespace);
    }
    if (Array.isArray(a)) {
      a = a.reduce(createClsProps, {});
    }
    return Config.merge(a, b);
  },
  'objects'(a, b) {
    return a === true ? b || {} : Config.merge(a, b);
  },
  'replace'(a) {
    return a;
  },
  'items'(a, b, c, e) {
    if (e) {
      return Objects.mergeItems(b, a, { 'merge': (f, g) => prototype.merge(g, f) });
    }
    return prototype.merge(a, b);
  }
};
Object.assign(prototype, {
  '_descriptor': null,
  '_initDescriptor': null,
  'date': false,
  'lazy': false,
  'nullify': false,
  'render': false
});
Config._$name = 'Config';
const ConfigBinding = class {
  constructor(a, b, c, e) {
    const f = this;
    let g, i;
    f.destroyed = false;
    f.mapFwd = {};
    f.mapRev = {};
    f.source = a;
    f.syncing = false;
    f.target = b;
    f.sourceHook = Helpers_default.Function.after(a, 'onConfigChange', j => f.onConfigChange(j, true), f, noReturn);
    f.targetHook = (e == null ? void 0 : e.oneway) ? null : Helpers_default.Function.after(b, 'onConfigChange', j => f.onConfigChange(j, false), f, noReturn);
    for (g in c) {
      i = c[g];
      if (i) {
        if (typeof i !== 'string') {
          i = g;
        }
        f.mapFwd[g] = i;
        f.mapRev[i] = g;
      }
    }
    f.unbind = f.destroy.bind(f);
    f.unbind.binding = f;
  }
  destroy() {
    let a, b;
    if (!this.destroyed) {
      this.destroyed = true;
      (a = this.sourceHook) == null ? void 0 : a.call(this);
      (b = this.targetHook) == null ? void 0 : b.call(this);
    }
  }
  onConfigChange(a, b) {
    const c = this, e = b ? c.mapFwd : c.mapRev;
    if (e[a.name]) {
      c.sync(b);
    }
  }
  sync(a) {
    a = a !== false;
    const b = this, [c, e, f] = a ? [
        b.source,
        b.target,
        b.mapFwd
      ] : [
        b.target,
        b.source,
        b.mapRev
      ];
    if (!b.syncing && !c.isDestroying && !e.isDestroying) {
      let g = 0, i, j, k, l, o, p;
      b.syncing = true;
      for (j in f) {
        l = f[j];
        k = c[j];
        if (k !== e[l]) {
          ++g;
          (i || (i = {}))[o = l] = p = k;
        }
      }
      if (g > 1) {
        e.setConfig(i);
      } else if (g) {
        e[o] = p;
      }
      b.syncing = false;
    }
  }
};
const MetaClass = class {
  constructor(a) {
    a && Object.assign(this, a);
  }
  getInherited(a, b = true) {
    let c;
    let e = this[a];
    if (!(a in this)) {
      e = (c = this.super) == null ? void 0 : c.getInherited(a, b);
      if (e || b) {
        this[a] = e = Object.create(e || null);
        defineProperty2(e, '$meta', { 'value': this });
      }
    }
    return e;
  }
};
const {getPrototypeOf} = Object;
var {defineProperty: defineProperty2} = Reflect;
const hasOwn3 = (a => (b, c) => a.call(b, c))(Object.prototype.hasOwnProperty);
const metaSymbol = Symbol('classMetaData');
const mixinTagSymbol = Symbol('mixinTag');
const originalConfigSymbol = Symbol('originalConfig');
const createdAtSymbol = Symbol('createdAt');
const configuringSymbol2 = Config.symbols.configuring;
const instancePropertiesSymbol2 = Config.symbols.instanceProperties;
const lazyConfigsSymbol = Config.symbols.lazyConfigs;
const defaultConfigOptions = {
  'merge': 'replace',
  'simple': true
};
const cloneObject = a => Helpers_default.Object.clone(a);
const emptyFn = () => {
};
const newMeta = a => new MetaClass(a);
const isObject = a => Helpers_default.Object.isObject(a);
var noReturn = { 'return': false };
const setupNames = {};
const emptyObject2 = Object.freeze({});
const emptyArray = Object.freeze([]);
const configTypeRe = /^(date|day)(\[])?$/;
const Base = class _Base {
  static get isBase() {
    return true;
  }
  get isBase() {
    return true;
  }
  static get declarable() {
    return [
      'declarable',
      'configurable',
      'defaultConfig',
      'properties',
      'prototypeProperties'
    ];
  }
  constructor(...a) {
    const b = this, c = b.constructor;
    if (b.$meta.class !== c) {
      c.$meta.initialized = true;
    }
    b.construct(...a);
    b.afterConstruct();
    b.isConstructing = false;
  }
  static new(...a) {
    a = a.filter(b => b);
    a = a.length > 1 ? this.mergeConfigs(...a) : a[0];
    return new this(a);
  }
  construct(...a) {
    if (a[0] !== null) {
      this.configure(...a);
    }
    this.afterConfigure();
  }
  static destroy(...a) {
    const b = c => {
      if (c == null ? void 0 : c.destroy) {
        c.destroy();
      } else if (Array.isArray(c)) {
        c.forEach(b);
      }
    };
    b(a);
  }
  destroy() {
    const a = this, {id: b} = a;
    a.isDestroying = true;
    a.destroy = emptyFn;
    a.doDestroy();
    Object.setPrototypeOf(a, null);
    for (const c in a) {
      if (c !== 'destroy' && c !== 'isDestroying') {
        delete a[c];
      }
    }
    delete a[originalConfigSymbol];
    a.isDestroyed = true;
    a.id = b;
  }
  _thisIsAUsedExpression(a) {
  }
  static get $$name() {
    return hasOwn3(this, '$name') && this.$name || hasOwn3(this, '_$name') && this._$name || this.name;
  }
  get $$name() {
    return this.constructor.$$name;
  }
  startConfigure(a) {
  }
  finishConfigure(a) {
  }
  afterConfigure() {
  }
  afterConstruct() {
  }
  callback(a, b, c = emptyArray) {
    const {
      handler: e,
      thisObj: f
    } = this.resolveCallback(a, b === 'this' ? this : b) || emptyObject2;
    return e == null ? void 0 : e.apply(f, c);
  }
  resolveProperty(a) {
    let b = this;
    while (b) {
      if (_Base.hasConfigPath(b, a, true)) {
        return Helpers_default.Object.getPath(b, a);
      }
      b = b.owner;
    }
    return void 0;
  }
  resolveCallback(a, b = this, c = true) {
    if (a == null ? void 0 : a.substring) {
      if (a.endsWith('?')) {
        c = false;
        a = a.substring(0, a.length - 1);
      }
      if (a.startsWith('up.')) {
        a = a.substring(3);
        for (b = this.owner; b && !b[a]; b = b.owner);
        if (!b) {
          return;
        }
      } else if (a.startsWith('this.')) {
        a = a.substring(5);
        b = this;
      }
      if (!b || !(b instanceof Object)) {
        return;
      }
      a = b[a];
    }
    if (typeof a === 'function') {
      return {
        'handler': a,
        'thisObj': b
      };
    }
    if (c) {
      throw new Error('No method named ' + a + ' on ' + (b.$$name || 'thisObj object'));
    }
  }
  bindCallback(a, b = this) {
    if (a) {
      const {
        handler: c,
        thisObj: e
      } = this.resolveCallback(a, b);
      if (c) {
        return c.bind(e);
      }
    }
  }
  delay(a, b, c = a.name || a) {
    a = this.setTimeout ? a : a.bind(this);
    const e = this.setTimeout ? this : globalThis;
    return e[typeof b === 'number' ? 'setTimeout' : 'requestAnimationFrame'](a, b, c);
  }
  doDestroy() {
    const a = this, {nullify: b} = a.$meta;
    if (b) {
      for (let c = 0; c < b.length; ++c) {
        if (a[b[c].field] != null) {
          a[b[c].name] = null;
        }
      }
    }
  }
  destroyProperties(...a) {
    let b, c;
    const e = this;
    let f;
    for (f of a) {
      if (f in e && (!e[configuringSymbol2] || !e[configuringSymbol2][f])) {
        (c = (b = e[f]) == null ? void 0 : b.destroy) == null ? void 0 : c.call(b);
        delete e[f];
      }
    }
  }
  configure(a = {}) {
    const b = this, c = b.$meta, {beforeConfigure: e} = b.initialConfig = a, {configs: f} = c, g = b.getDefaultConfiguration();
    let i, j, k;
    b.isConfiguring = true;
    Object.assign(b, b.getProperties());
    for (j in a) {
      k = a[j];
      i = f[j];
      g[j] = i ? i.merge(k, g[j], null, c) : k;
    }
    if (e) {
      delete g.beforeConfigure;
      e(b, g);
    }
    b.setConfig(b[originalConfigSymbol] = g, true);
    b.isConfiguring = false;
  }
  bindConfigs(a, b, c) {
    const e = new ConfigBinding(this, a, b, c);
    (c == null ? void 0 : c.sync) !== false && e.sync();
    return e.unbind;
  }
  getConfig(a, ...b) {
    const c = this[a];
    return b.length ? [
      c,
      ...b.map(e => this[e])
    ] : c;
  }
  setConfig(a, b = false) {
    let c;
    const e = this, f = e[configuringSymbol2], g = f ? e.configDone : e.configDone = {}, i = e.$meta.configs, j = e[lazyConfigsSymbol];
    let k, l;
    e[instancePropertiesSymbol2] = {};
    e[configuringSymbol2] = f ? Object.setPrototypeOf(Object.assign({}, a), f) : a;
    for (l in a) {
      if (a[l] !== null || hasOwn3(a, l)) {
        k = i[l] || Config.get(l);
        if (!(j == null ? void 0 : j.has(l))) {
          k.defineInitter(e, a[l]);
        }
        if (!b) {
          g[l] = false;
        }
      } else {
        g[l] = true;
      }
    }
    if (b) {
      e.startConfigure(a);
    }
    for (l in a) {
      if (!g[l] && !(b && ((c = i[l]) == null ? void 0 : c.lazy))) {
        e[l] = a[l];
      }
    }
    if (f) {
      e[configuringSymbol2] = f;
    } else {
      delete e[configuringSymbol2];
    }
    if (b) {
      e.finishConfigure(a);
    }
    return e;
  }
  hasConfig(a) {
    let b;
    const c = this, e = c[configuringSymbol2];
    return Boolean(c['_' + a] != null || ((b = c[lazyConfigsSymbol]) == null ? void 0 : b.get(a)) != null || !c.configDone[a] && e && (e[a] != null || hasOwn3(e, a)));
  }
  static hasConfigPath(a, b) {
    return b.split('.').every(c => {
      let e, f, g;
      const i = (f = (e = a == null ? void 0 : a.$meta) == null ? void 0 : e.configs) == null ? void 0 : f[c], j = i && i.descriptor.get === ((g = Helpers_default.Object.getPropertyDescriptor(a, c)) == null ? void 0 : g.get);
      if (j ? a.hasConfig(c) : c in a) {
        a = a[c];
        return Object !== void 0;
      }
      return false;
    });
  }
  peekConfig(a) {
    const b = this, c = b[lazyConfigsSymbol], e = b[configuringSymbol2];
    if (c == null ? void 0 : c.has(a)) {
      return c.get(a);
    }
    if (e && a in e) {
      if (b.configDone[a]) {
        return b[a];
      }
      if (e[a] != null || hasOwn3(e, a)) {
        return e[a];
      }
    }
  }
  triggerConfig(a) {
    const b = this, {configDone: c} = b, e = b[lazyConfigsSymbol], f = b[configuringSymbol2], g = (e == null ? void 0 : e.has(a)) || f && (f[a] != null || hasOwn3(f, a)) ? !c[a] : null;
    if (g) {
      b.getConfig(a);
    }
    return g;
  }
  triggerConfigs(a) {
    const b = this, c = b.$meta.configs, e = b[lazyConfigsSymbol], f = e ? [...e.keys()].filter(g => c[g].lazy === a) : emptyArray;
    for (const g of f) {
      b.triggerConfig(g);
    }
    return f.length ? f : null;
  }
  onConfigChange() {
  }
  get config() {
    const a = {}, b = this[originalConfigSymbol];
    for (const c in b) {
      a[c] = b[c];
    }
    return a;
  }
  static processConfigValue(a, b) {
    if (a === globalThis) {
      return globalThis;
    } else if (Array.isArray(a)) {
      return a.map(c => _Base.processConfigValue(c, b));
    } else if (a instanceof _Base) {
      if (b.visited.has(a)) {
        return;
      }
      return a.getCurrentConfig(b);
    } else if (a instanceof HTMLElement || a instanceof DocumentFragment) {
      return null;
    } else if (isObject(a)) {
      const c = {};
      for (const e in a) {
        if (e !== 'owner') {
          c[e] = _Base.processConfigValue(a[e], b);
        }
      }
      return c;
    }
    return a;
  }
  getConfigValue(a, b) {
    let c;
    const e = this, f = e[lazyConfigsSymbol];
    if (!((c = e.$meta.configs[a]) == null ? void 0 : c.lazy)) {
      return _Base.processConfigValue(e[a], b);
    }
    if (f == null ? void 0 : f.has(a)) {
      return _Base.processConfigValue(f.get(a), b);
    }
  }
  preProcessCurrentConfigs() {
  }
  getCurrentConfig(a = {}) {
    const b = this, c = a.configs === 'all' ? b.config : cloneObject(b.initialConfig), e = a.skip || {}, f = a.visited || (a.visited = new Set()), g = a.depth || (a.depth = 0), i = {};
    if (f.has(b)) {
      return void 0;
    }
    f.add(b);
    this.preProcessCurrentConfigs(c);
    for (const j in c) {
      if (e[j]) {
        continue;
      }
      const k = b.getConfigValue(j, {
        ...a,
        'depth': g + 1
      });
      if (k !== void 0) {
        i[j] = k;
      }
    }
    return i;
  }
  getConfigString(a = {}) {
    return Helpers_default.String.toJavaScriptString(this.getCurrentConfig(a));
  }
  getTestCase(a = {}) {
    const b = this.isGantt ? 'Gantt' : this.isSchedulerPro ? 'SchedulerPro' : this.isCalendar ? 'Calendar' : this.isScheduler ? 'Scheduler' : this.isGrid ? 'Grid' : this.isTaskBoard ? 'TaskBoard' : null;
    if (b) {
      const c = b.toLowerCase(), e = '../../build/' + c + '.module.js';
      let f, g;
      if (a.import === 'static') {
        f = 'import * as module from "' + e + '";Object.assign(window, module);';
        g = '';
      } else {
        f = 'import("' + e + '").then(module => { Object.assign(window, module);\n';
        g = '});';
      }
      const i = VersionHelper.getVersion(c);
      if (i) {
        f += '\nconsole.log(\'' + b + ' ' + i + '\');\n';
      }
      return f + '      \nconst ' + c + ' = new ' + b + '(' + this.getConfigString(a) + ');\n' + g;
    }
  }
  downloadTestCase(a = {}) {
    a.output = 'return';
    const b = this.getTestCase(a);
    Helpers_default.Browser.download('app.js', 'data:application/javascript;charset=utf-8,' + escape(b));
  }
  static initClass() {
    return this.$meta.class;
  }
  static get $meta() {
    if (!hasOwn3(this, metaSymbol)) {
      const a = this[metaSymbol] = new MetaClass();
      a.class = this;
      this.setupClass(a);
      return a;
    }
    return this[metaSymbol];
  }
  static onClassMixedIn() {
  }
  static mergeConfigs(a, ...b) {
    const c = this.$meta.configs, e = cloneObject(a) || {};
    let f, g, j, k;
    for (g = 0; g < b.length; ++g) {
      f = b[g];
      if (f) {
        for (j in f) {
          k = f[j];
          if (c[j]) {
            k = c[j].merge(k, e[j]);
          } else if (e[j] && k) {
            k = Config.merge(k, e[j]);
          }
          e[j] = k;
        }
      }
    }
    return e;
  }
  static mixin(...a) {
    let b = this, c;
    for (c = 0; c < a.length; ++c) {
      const e = a[c], f = e[mixinTagSymbol] || (e[mixinTagSymbol] = Symbol('mixinTag'));
      if (b[f]) {
        continue;
      }
      b = e(b);
      b[f] = true;
      if (hasOwn3(b, 'onClassMixedIn')) {
        b.onClassMixedIn();
      }
    }
    return b;
  }
  static setupClass(a) {
    let b;
    const c = a.class, e = getPrototypeOf(c).$meta, f = c.$$name, g = e.names, j = c.prototype;
    defineProperty2(j, '$meta', { 'value': a });
    Object.assign(a, {
      'super': e,
      'config': Object.create(e.config),
      'configs': Object.create(e.configs),
      'declarables': e.declarables,
      'forkConfigs': e.forkConfigs,
      'hierarchy': Object.freeze([
        ...e.hierarchy,
        c
      ]),
      'names': g.includes(f) ? g : Object.freeze([
        ...g,
        f
      ]),
      'properties': e.properties,
      'nullify': (b = e.nullify) == null ? void 0 : b.slice()
    });
    if (g !== a.names) {
      const k = 'is' + f, l = n => {
          if (!hasOwn3(n, k)) {
            defineProperty2(n, k, {
              'get'() {
                return true;
              }
            });
          }
        };
      l(j);
      l(c);
    }
    for (let n, o, p = 0; p < a.declarables.length; ++p) {
      n = a.declarables[p];
      if (hasOwn3(c, n)) {
        o = setupNames[n] || (setupNames[n] = 'setup' + Helpers_default.String.capitalize(n));
        c[o](c, a);
      }
    }
  }
  static setupConfigs(a, b, c) {
    const e = a.config, f = a.configs, g = a.class, i = a.super;
    let {nullify: j} = a, k, l, n, o, p, q;
    for (const r in b) {
      p = b[r];
      if (c) {
        if (!(k = f[r])) {
          k = Config.get(r, defaultConfigOptions);
        } else {
          p = k.merge(p, e[r], a, i);
        }
      } else {
        l = n = o = void 0;
        if (p && typeof p === 'object' && '$config' in p) {
          n = p.$config;
          if (configTypeRe.test(String(n))) {
            n = { 'type': n };
          }
          if (n) {
            if (!isObject(n)) {
              n = Helpers_default.Object.createTruthyKeys(n);
            } else if (n.type) {
              n = {
                'convert': n.type,
                'equal': n.type,
                ...n
              };
              delete n.type;
            }
          }
          o = 'default' in p;
          l = o ? p.default : l;
          p = 'value' in p ? p.value : null;
        }
        if (!(k = f[r])) {
          k = Config.get(r, n);
          k.define(g.prototype);
          o = !(k.field in g.prototype);
          q = false;
        } else {
          q = k.nullify;
          if (n) {
            k = k.extend(n);
          }
          p = k.merge(p, e[r], a, i);
        }
        if (o) {
          k.setDefault(g, l);
        }
        if (k.nullify && !q) {
          (j || (j = a.nullify || (a.nullify = []))).push(k);
        }
      }
      if (p && (isObject(p) || Array.isArray(p)) && !Object.isFrozen(p)) {
        a.forkConfigs = true;
      }
      f[r] = k;
      e[r] = p;
    }
  }
  static setupConfigurable(a, b) {
    a.setupConfigs(b, a.configurable, false);
  }
  static setupDefaultConfig(a, b) {
    a.setupConfigs(b, a.defaultConfig, true);
  }
  static setupDeclarable(a, b) {
    const c = a.declarable;
    let e = b.declarables, f, g;
    for (g = 0; g < c.length; ++g) {
      if (!e.includes(c[g])) {
        if (!f) {
          b.declarables = f = e = e.slice();
        }
        e.push(c[g]);
      }
    }
  }
  static setupProperties(a, b) {
    b.properties = b.super.properties.slice();
    b.properties.push(a);
    Object.freeze(b.properties);
  }
  static setupPrototypeProperties(a) {
    Object.assign(a.prototype, a.prototypeProperties);
  }
  static fork(a) {
    let b = a, c, e;
    if (a && isObject(a) && !Object.isFrozen(a)) {
      b = Object.create(a);
      for (c in a) {
        e = a[c];
        if (e) {
          if (isObject(e)) {
            b[c] = _Base.fork(e);
          } else if (Array.isArray(e)) {
            b[c] = e.slice();
          }
        }
      }
    }
    return b;
  }
  static getDefaultConfiguration() {
    return this.$meta.class.prototype.getDefaultConfiguration();
  }
  getDefaultConfiguration() {
    const a = this.$meta, b = a.forkConfigs ? _Base.fork(a.config) : Object.create(a.config);
    if (VersionHelper.isTestEnv && Helpers_default.Browser.isBrowserEnv && b.testConfig && globalThis.__applyTestConfigs) {
      for (const c in b.testConfig) {
        b[c] = b.testConfig[c];
      }
    }
    return b;
  }
  getProperties() {
    const a = this.$meta.properties, b = {};
    for (let c = 0; c < a.length; c++) {
      const e = Object.getOwnPropertyDescriptor(a[c], 'properties');
      Object.assign(b, e.get !== void 0 ? a[c].properties : cloneObject(e.value));
    }
    return b;
  }
  static get superclass() {
    return getPrototypeOf(this);
  }
  classHierarchy(a) {
    const b = this.$meta.hierarchy, c = a ? b.indexOf(a) : 0;
    return c > 0 ? b.slice(c) : b;
  }
  static isOfTypeName(a) {
    return this.$meta.names.includes(a);
  }
  detachListeners(a) {
    let b = this.$detachers;
    b = b == null ? void 0 : b[a];
    if (b) {
      while (b.length) {
        b.pop()();
      }
    }
  }
  trackDetacher(a, b) {
    const c = this.$detachers || (this.$detachers = {}), e = c[a] || (c[a] = []);
    e.push(b);
  }
  untrackDetachers(a) {
    const b = this.$detachers;
    if (b) {
      for (const c in b) {
        const e = b[c];
        for (let f = e.length; f-- > 0;) {
          if (e[f].eventer === a) {
            e.splice(f, 1);
          }
        }
      }
    }
  }
};
const proto = Base.prototype;
proto.onConfigChange.$nullFn = emptyFn.$nullFn = true;
Base[metaSymbol] = proto.$meta = newMeta({
  'class': Base,
  'config': Object.freeze({}),
  'configs': Object.create(null),
  'declarables': Base.declarable,
  'forkConfigs': false,
  'hierarchy': Object.freeze([Base]),
  'names': Object.freeze(['Base']),
  'nullify': null,
  'properties': Object.freeze([]),
  'super': null
});
Object.assign(proto, {
  '$detachers': null,
  'configObserver': null,
  'isConstructing': true,
  'isDestroyed': false,
  'isDestroying': false
});
Base.emptyFn = emptyFn;
VersionHelper.setVersion('core', '6.0.4');
Base._$name = 'Base';
const _ArrayHelper = class _ArrayHelper {
  static $name = 'ArrayHelper';
  static clean(a) {
    return a.reduce((b, c) => {
      if (c !== null && c !== void 0 && !(Array.isArray(c) && c.length === 0) && c !== '')
        b.push(c);
      return b;
    }, []);
  }
  static from(a, b, c) {
    const e = [];
    if (a) {
      for (const f of a) {
        if (!b || b(f)) {
          e.push(c ? c(f) : f);
        }
      }
    }
    return e;
  }
  static remove(a, ...b) {
    let c, e, f = false;
    b = b[0] instanceof Set ? [...b[0]] : b;
    for (let g = 0; g < b.length; g++) {
      e = b[g];
      if ((c = a.indexOf(e)) !== -1) {
        a.splice(c, 1);
        f = true;
      }
    }
    return f;
  }
  static findInsertionIndex(a, b, c = this.lexicalCompare, e) {
    const f = b.length;
    let g, i;
    if (e < f) {
      g = e > 0 ? c(b[e - 1], a) : 0;
      i = e < f - 1 ? c(a, b[e]) : 0;
      if (g < 1 && i < 1) {
        return e;
      }
    }
    return this.binarySearch(b, a, c);
  }
  static findLast(a, b, c) {
    for (let {length: e} = a, f = e - 1; f >= 0; f--) {
      if (b.call(c, a[f], f, a)) {
        return a[f];
      }
    }
  }
  static binarySearch(a, b, c = 0, e = a.length, f = this.lexicalCompare) {
    const g = a.length;
    let i, j;
    if (c instanceof Function) {
      f = c;
      c = 0;
    } else if (e instanceof Function) {
      f = e;
      e = g;
    }
    --e;
    while (c <= e) {
      i = c + e >> 1;
      j = f(b, a[i]);
      if (j >= 0) {
        c = i + 1;
      } else if (j < 0) {
        e = i - 1;
      }
    }
    return c;
  }
  magnitudeCompare(a, b) {
    return a < b ? -1 : a > b ? 1 : 0;
  }
  lexicalCompare(a, b) {
    a = String(a);
    b = String(b);
    return a < b ? -1 : a > b ? 1 : 0;
  }
  static fill(a, b = {}, c = null) {
    const e = [], f = Array.isArray(b) ? b : [b];
    for (let g = 0; g < a; g++) {
      for (const j of f) {
        const k = Object.assign({}, j);
        if (c) {
          c(k, g);
        }
        e.push(k);
      }
    }
    return e;
  }
  static populate(a, b, c = false) {
    const e = [];
    for (let f = 0; f < a; f++) {
      e.push(b(f + (c ? 1 : 0)));
    }
    return e;
  }
  static include(a, ...b) {
    for (const c of b) {
      if (!a.includes(c)) {
        a.push(c);
      }
    }
  }
  static unique(a) {
    return [...new Set(a)];
  }
  static allowNegative(a) {
    return new Proxy(a, {
      'get'(b, c, e) {
        if (typeof c !== 'string') {
          return Reflect.get(b, c, e);
        }
        const f = Number(c);
        if (Number.isNaN(f)) {
          return Reflect.get(b, c, e);
        }
        return b[f < 0 ? b.length + f : f];
      },
      'set'(b, c, e, f) {
        if (typeof c !== 'string') {
          return Reflect.set(b, c, e, f);
        }
        const g = Number(c);
        if (Number.isNaN(g)) {
          return Reflect.set(b, c, e, f);
        }
        b[g < 0 ? b.length + g : g] = e;
        return true;
      }
    });
  }
  static delta(c, e, f = false) {
    if (!e) {
      return f ? {
        'toAdd': c,
        'toRemove': [],
        'toKeep': []
      } : {
        'onlyInA': c,
        'onlyInB': [],
        'inBoth': []
      };
    }
    const g = [], j = [], k = new Set(), l = new Set(e);
    for (let n = 0; n < c.length; n++) {
      const o = c[n];
      if (l.has(o)) {
        k.add(o);
      } else {
        g.push(o);
      }
    }
    for (let p = 0; p < e.length; p++) {
      const q = e[p];
      if (!k.has(q)) {
        j.push(q);
      }
    }
    if (f) {
      return {
        'toAdd': g,
        'toRemove': j,
        'toKeep': k
      };
    }
    return {
      'onlyInA': g,
      'onlyInB': j,
      'inBoth': [...k]
    };
  }
  static asArray(a) {
    if (a == null) {
      return a;
    }
    if (a instanceof Set) {
      return Array.from(a);
    }
    return Array.isArray(a) ? a : [a];
  }
  static identity(a) {
    return a;
  }
  static keyBy(a, b, c = _ArrayHelper.identity) {
    const e = typeof b === 'string' ? g => g == null ? void 0 : g[b] : b, f = typeof c === 'string' ? g => g == null ? void 0 : g[c] : c;
    return a.reduce((g, i) => {
      g[e(i)] = f(i);
      return g;
    }, {});
  }
  static aggregate(a, b, c, e, f = {}) {
    const g = a.length, i = a[0].length, j = [];
    f.targetArray = j;
    f.arrays = a;
    for (let k = 0; k < i; k++) {
      f.entryIndex = k;
      j.push(e(k, f));
    }
    for (let l = 0; l < g; l++) {
      const n = a[l];
      f.rowIndex = l;
      for (let o = 0; o < i; o++) {
        f.entryIndex = o;
        const p = b(n[o], f);
        j[o] = c(j[o], p, l, o, f);
      }
    }
    return j;
  }
  static groupBy(a, b) {
    const c = typeof b === 'string' ? e => e == null ? void 0 : e[b] : b;
    return a.reduce((e, f) => {
      const g = c(f);
      (e[g] = e[g] || []).push(f);
      return e;
    }, {});
  }
  static groupByIndexed(a, b) {
    return a.reduce((c, e) => {
      const f = b(e);
      let g = c.get(f);
      if (g === void 0) {
        g = new Set();
        c.set(f, g);
      }
      g.add(e);
      return c;
    }, new Map());
  }
};
const ArrayHelper = _ArrayHelper;
Helpers_default.register(ArrayHelper);
ArrayHelper._$name = 'ArrayHelper';
const commaSepRe = /,\s*/;
const decompiledSym = Symbol('decompiled');
const fnRe1 = /^\s*(async\s+)?([a-z_]\w*)\s*=>([\s\S]+)$/i;
const fnRe2 = /^\s*(async\s*)?\s*\(((?:[a-z_]\w*(?:, [a-z_]\w*)*)?)\)\s+=>([\s\S]+)$/i;
const fnRe3 = /^(\s*async)?(?:\s*function)?(?:\s*([a-z_]\w*))?\s*\(((?:[a-z_]\w*(?:, [a-z_]\w*)*)?)\)([\s\S]+)$/i;
const {hasOwnProperty: hasOwnProperty4} = Object.prototype;
const _FunctionHelper = class _FunctionHelper {
  static $name = 'FunctionHelper';
  static after(a, b, c, e, f) {
    const g = typeof c === 'string', i = (f == null ? void 0 : f.return) !== false, j = (...k) => {
        const l = j.$nextHook.call(a, ...k), n = (e == null ? void 0 : e.isDestroyed) ? void 0 : i ? g ? e[c](l, ...k) : c.call(e, l, ...k) : g ? e[c](...k) : c.call(e, ...k);
        return n === void 0 ? l : n;
      };
    return _FunctionHelper.hookMethod(a, b, j);
  }
  static before(a, b, c, e) {
    const f = typeof c === 'string', g = (...i) => {
        const j = (e == null ? void 0 : e.isDestroyed) ? 0 : f ? e[c](...i) : c.call(e, ...i);
        return j === false ? j : g.$nextHook.call(a, ...i);
      };
    return _FunctionHelper.hookMethod(a, b, g);
  }
  static curry(a) {
    return function b(...c) {
      if (c.length >= a.length) {
        return a.apply(this, c);
      } else {
        return function (...e) {
          return b.apply(this, c.concat(e));
        };
      }
    };
  }
  static bindAll(a) {
    for (const b in a) {
      if (typeof a[b] === 'function') {
        a[b] = a[b].bind(a);
      }
    }
  }
  static createInterceptor(a, b, c) {
    return function (...e) {
      const f = c || this;
      if (b.call(f, ...e) !== false) {
        return a.call(f, ...e);
      }
      return false;
    };
  }
  static createSequence(a, b, c) {
    return (...e) => {
      const f = a.call(c, ...e), g = b.call(c, ...e);
      return g === void 0 ? f : g;
    };
  }
  static createThrottled(a, b, c, e, f) {
    let g = -Number.MAX_VALUE, i, j;
    const k = () => {
        j = 0;
        g = performance.now();
        i.push.apply(i, e);
        a.apply(c, i);
      }, l = function (...n) {
        const o = performance.now() - g;
        i = n;
        if (o >= b) {
          clearTimeout(j);
          k();
        } else {
          if (!j) {
            j = setTimeout(k, b - o);
          }
          if (f) {
            i.push.apply(i, e);
            f.apply(c, i);
          }
        }
      };
    l.cancel = () => clearTimeout(j);
    return l;
  }
  static createBuffered(a, b, c, e) {
    let f, g;
    const i = () => {
        g = 0;
        j.isPending = false;
        f.push.apply(f, e);
        a.apply(c, f);
      }, j = function (...k) {
        f = k;
        if (g) {
          clearTimeout(g);
        }
        j.isPending = true;
        g = setTimeout(i, b);
      };
    j.cancel = () => {
      j.isPending = false;
      clearTimeout(g);
    };
    return j;
  }
  static decompile(a) {
    if (!(decompiledSym in a)) {
      const b = a.toString();
      let c = fnRe1.exec(b), e, f, g, i, j;
      if (c) {
        e = [c[2]];
        f = c[3];
      } else if (c = fnRe2.exec(b)) {
        j = c[2].trim();
        e = j ? j.split(commaSepRe) : [];
        f = c[3];
      } else if (c = fnRe3.exec(b)) {
        g = c[2];
        j = c[3].trim();
        e = j ? j.split(commaSepRe) : [];
        f = c[4];
      }
      f = f == null ? void 0 : f.trim();
      a[decompiledSym] = i = c && {
        'args': e,
        'async': Boolean(c[1]),
        'body': (f == null ? void 0 : f.startsWith('{')) ? f.substring(1, f.length - 1).trim() : f
      };
      if (g) {
        i.name = g;
      }
    }
    return a[decompiledSym];
  }
  static hookMethod(a, b, c) {
    c.$nextHook = a[b];
    a[b] = c;
    return () => {
      let e;
      if (hasOwnProperty4.call(a, b)) {
        let g = a[b], i;
        if (g === c) {
          if (((e = Object.getPrototypeOf(a)) == null ? void 0 : e[b]) === c.$nextHook) {
            delete a[b];
          } else {
            a[b] = c.$nextHook;
          }
        } else {
          for (; i = g == null ? void 0 : g.$nextHook; g = i) {
            if (i === c) {
              g.$nextHook = c.$nextHook;
              break;
            }
          }
        }
      }
    };
  }
  static noThrow(a, b, c) {
    const e = a[b];
    a[b] = (...f) => {
      try {
        return e.apply(a, f);
      } catch (g) {
        return c == null ? void 0 : c(g);
      }
    };
  }
  static returnTrue() {
    return true;
  }
  static animate(a, b, c, e = 'linear') {
    let f = false;
    const g = new Promise(i => {
      const j = performance.now(), k = () => {
          const l = Math.min((performance.now() - j) / a, 1), n = c && c.setTimeout ? c : globalThis;
          if (!f) {
            if (b.call(c, this.easingFunctions[e](l)) === false) {
              i();
            }
          }
          if (f || l === 1) {
            n.requestAnimationFrame(() => i());
          } else {
            n.requestAnimationFrame(k);
          }
        };
      k();
    });
    g.cancel = () => {
      f = true;
      g.cancelled = true;
      return false;
    };
    return g;
  }
};
const FunctionHelper = _FunctionHelper;
Helpers_default.register(FunctionHelper);
const half = 0.5;
const e1 = 1.70158;
const e2 = 7.5625;
const e3 = 1.525;
const e4 = 2 / 2.75;
const e5 = 2.25 / 2.75;
const e6 = 1 / 2.75;
const e7 = 1.5 / 2.75;
const e8 = 2.5 / 2.75;
const e9 = 2.625 / 2.75;
const e10 = 0.75;
const e11 = 0.9375;
const e12 = 0.984375;
const s1 = 1.70158;
const s2 = 1.70158;
FunctionHelper.easingFunctions = {
  'linear': a => a,
  'easeInQuad': a => Math.pow(a, 2),
  'easeOutQuad': a => -(Math.pow(a - 1, 2) - 1),
  'easeInOutQuad': a => (a /= half) < 1 ? half * Math.pow(a, 2) : -half * ((a -= 2) * a - 2),
  'easeInCubic': a => Math.pow(a, 3),
  'easeOutCubic': a => Math.pow(a - 1, 3) + 1,
  'easeInOutCubic': a => (a /= half) < 1 ? half * Math.pow(a, 3) : half * (Math.pow(a - 2, 3) + 2),
  'easeInQuart': a => Math.pow(a, 4),
  'easeOutQuart': a => -(Math.pow(a - 1, 4) - 1),
  'easeInOutQuart': a => (a /= half) < 1 ? half * Math.pow(a, 4) : -half * ((a -= 2) * Math.pow(a, 3) - 2),
  'easeInQuint': a => Math.pow(a, 5),
  'easeOutQuint': a => Math.pow(a - 1, 5) + 1,
  'easeInOutQuint': a => (a /= half) < 1 ? half * Math.pow(a, 5) : half * (Math.pow(a - 2, 5) + 2),
  'easeInSine': a => -Math.cos(a * (Math.PI / 2)) + 1,
  'easeOutSine': a => Math.sin(a * (Math.PI / 2)),
  'easeInOutSine': a => -half * (Math.cos(Math.PI * a) - 1),
  'easeInExpo': a => a === 0 ? 0 : Math.pow(2, 10 * (a - 1)),
  'easeOutExpo': a => a === 1 ? 1 : -Math.pow(2, -10 * a) + 1,
  'easeInOutExpo': a => a === 0 ? 0 : a === 1 ? 1 : (a /= half) < 1 ? half * Math.pow(2, 10 * (a - 1)) : half * (-Math.pow(2, -10 * --a) + 2),
  'easeInCirc': a => -(Math.sqrt(1 - a * a) - 1),
  'easeOutCirc': a => Math.sqrt(1 - Math.pow(a - 1, 2)),
  'easeInOutCirc': a => (a /= half) < 1 ? -half * (Math.sqrt(1 - a * a) - 1) : half * (Math.sqrt(1 - (a -= 2) * a) + 1),
  'easeOutBounce': a => a < e6 ? e2 * a * a : a < e4 ? e2 * (a -= e7) * a + e10 : a < e8 ? e2 * (a -= e5) * a + e11 : e2 * (a -= e9) * a + e12,
  'easeInBack': a => a * a * ((e1 + 1) * a - e1),
  'easeOutBack': a => (a = a - 1) * a * ((e1 + 1) * a + e1) + 1,
  'easeInOutBack': a => {
    let b = s1;
    return (a /= half) < 1 ? half * (a * a * (((b *= e3) + 1) * a - b)) : half * ((a -= 2) * a * (((b *= e3) + 1) * a + b) + 2);
  },
  'elastic': a => -1 * Math.pow(4, -8 * a) * Math.sin((a * 6 - 1) * (2 * Math.PI) / 2) + 1,
  'swingFromTo': a => {
    let b = s2;
    return (a /= half) < 1 ? half * (a * a * (((b *= e3) + 1) * a - b)) : half * ((a -= 2) * a * (((b *= e3) + 1) * a + b) + 2);
  },
  'swingFrom': a => a * a * ((e1 + 1) * a - e1),
  'swingTo': a => (a -= 1) * a * ((e1 + 1) * a + e1) + 1,
  'bounce': a => a < e6 ? e2 * a * a : a < e4 ? e2 * (a -= e7) * a + e10 : a < e8 ? e2 * (a -= e5) * a + e11 : e2 * (a -= e9) * a + e12,
  'bouncePast': a => a < e6 ? e2 * a * a : a < e4 ? 2 - (e2 * (a -= e7) * a + e10) : a < e8 ? 2 - (e2 * (a -= e5) * a + e11) : 2 - (e2 * (a -= e9) * a + e12),
  'easeFromTo': a => (a /= half) < 1 ? half * Math.pow(a, 4) : -half * ((a -= 2) * Math.pow(a, 3) - 2),
  'easeFrom': a => Math.pow(a, 4),
  'easeTo': a => Math.pow(a, 0.25)
};
FunctionHelper._$name = 'FunctionHelper';
const {isArray} = Array;
const {hasOwnProperty: hasOwnProperty5} = Object.prototype;
const specialProperties = {
  'thisObj': 1,
  'detachable': 1,
  'once': 1,
  'detacher': 1,
  'prio': 1,
  'args': 1,
  'expires': 1,
  'buffer': 1,
  'throttle': 1,
  'name': 1,
  '$internal': 1
};
const priorityComparator = (c, e) => e.prio - c.prio;
const Events_default = a => class b extends (a || Base) {
  eventsSuspended = null;
  constructor() {
    super(...arguments);
  }
  static get $name() {
    return 'Events';
  }
  static get declarable() {
    return ['deprecatedEvents'];
  }
  static setupDeprecatedEvents(c, e) {
    const f = e.getInherited('deprecatedEvents'), g = c.deprecatedEvents;
    for (const i in g) {
      f[i.toLowerCase()] = f[i] = g[i];
    }
  }
  static get configurable() {
    return {
      'listeners': {
        'value': null,
        '$config': {
          'merge'(c, e) {
            if (c !== null) {
              if (!c) {
                return e;
              }
              if (e) {
                c = c ? [c] : [];
                c.push[isArray(e) ? 'apply' : 'call'](c, e);
              }
            }
            return c;
          }
        }
      },
      'internalListeners': null,
      'bubbleEvents': null
    };
  }
  destroy() {
    this.trigger('beforeDestroy');
    super.destroy();
  }
  construct(c, ...e) {
    if (this.configuredListeners = c == null ? void 0 : c.listeners) {
      c = Objects.assign({}, c);
      delete c.listeners;
    }
    super.construct(c, ...e);
    this.processConfiguredListeners();
  }
  processConfiguredListeners() {
    if (this.configuredListeners) {
      const c = this, {isConfiguring: e} = c;
      c.isConfiguring = false;
      c.listeners = c.configuredListeners;
      c.configuredListeners = null;
      c.isConfiguring = e;
    }
  }
  doDestroy() {
    this.trigger('destroy');
    this.removeAllListeners(false);
    super.doDestroy();
  }
  static setupClass(c) {
    super.setupClass(c);
    b.prototype.onListen.$nullFn = true;
    b.prototype.onUnlisten.$nullFn = true;
  }
  addListener(c, e, f) {
    let g;
    if (isArray(c)) {
      for (let l = 0, {length: n} = c; l < n; l++) {
        this.addListener(c[l], e);
      }
      return;
    }
    const j = this, k = j.$meta.getInherited('deprecatedEvents');
    if (typeof c === 'string') {
      return j.addListener({
        [c]: e,
        'detachable': e.detachable !== false,
        'thisObj': f
      });
    } else {
      e = c.thisObj = c.thisObj !== void 0 ? c.thisObj : e;
      for (const r in c) {
        if (!specialProperties[r] && c[r] != null) {
          const u = r.toLowerCase(), v = k == null ? void 0 : k[u], w = j.eventListeners || (j.eventListeners = {}), z = ArrayHelper.asArray(c[r]);
          if (v) {
            const {
              product: A,
              invalidAsOfVersion: B,
              message: C
            } = v;
            VersionHelper.deprecate(A, B, C);
          }
          for (const E of z) {
            const F = E.expires || c.expires, G = {
                'fn': typeof E === 'object' ? E.fn : E,
                'thisObj': E.thisObj !== void 0 ? E.thisObj : e,
                'args': E.args || c.args,
                'prio': E.prio !== void 0 ? E.prio : c.prio !== void 0 ? c.prio : 0,
                'once': E.once !== void 0 ? E.once : c.once !== void 0 ? c.once : false,
                'buffer': E.buffer || c.buffer,
                'throttle': E.throttle || c.throttle,
                '$internal': c.$internal,
                'catchAll': r === 'catchAll'
              };
            if (F) {
              const {alt: I} = F, J = I ? F.delay : F, K = c.name || r, L = () => {
                  j.un(u, G);
                  if (I && !G.called) {
                    j.callback(I, e);
                  }
                };
              if (j.isDelayable) {
                j.setTimeout({
                  'fn': L,
                  'name': K,
                  'cancelOutstanding': true,
                  'delay': J
                });
              } else {
                globalThis.setTimeout(L, J);
              }
            }
            let H = w[u] || (w[u] = []);
            if (H.$firing) {
              w[u] = H = H.slice();
            }
            H.splice(ArrayHelper.findInsertionIndex(G, H, priorityComparator, H.length), 0, G);
            if (!j.onListen.$nullFn && H.length < 2) {
              j.onListen(u);
            }
            (g = j.afterAddListener) == null ? void 0 : g.call(j, u, G);
          }
        }
      }
      if (c.relayAll) {
        j.relayAll(c.relayAll);
      }
      if (e && e !== j) {
        j.attachAutoDetacher(c, e);
      }
      const o = c.detachable !== false, p = c.name, q = c.expires || o || p ? () => {
          if (!j.isDestroyed) {
            j.removeListener(c, e);
          }
        } : null;
      if (q) {
        q.eventer = j;
        q.listenerName = p;
        if (p && (e == null ? void 0 : e.trackDetacher)) {
          e.trackDetacher(p, q);
        }
        if (c.expires) {
          j.delay(q, isNaN(c.expires) ? c.expires.delay : c.expires, p);
        }
        if (o) {
          return q;
        }
      }
    }
  }
  on(c, e, f) {
    return this.addListener(c, e, f);
  }
  ion(c) {
    c.$internal = true;
    return this.on(c);
  }
  un(...c) {
    this.removeListener(...c);
  }
  updateInternalListeners(c, e) {
    e == null ? void 0 : e.detach();
    if (c) {
      c.detach = this.ion(c);
    }
  }
  get listeners() {
    return this.eventListeners;
  }
  changeListeners(c) {
    if (this.isConfiguring) {
      this.getConfig('internalListeners');
      if (c) {
        this.on(c, this);
      }
    } else {
      if (Array.isArray(c)) {
        for (let e = 0, f = c[0], {length: g} = c; e < g; f = c[++e]) {
          if (!('thisObj' in f)) {
            c[e] = Objects.assign({ 'thisObj': this }, f);
          }
        }
      } else if (c && !('thisObj' in c)) {
        c = Objects.assign({ 'thisObj': this }, c);
      }
      return c;
    }
  }
  updateListeners(c, e) {
    e && this.un(e);
    c && this.on(c);
  }
  removeListener(c, e = c.thisObj, f) {
    const g = this;
    if (typeof c === 'string') {
      return g.removeListener({ [c]: e }, f);
    }
    Object.entries(c).forEach(([i, j]) => {
      let k;
      if (!specialProperties[i] && j != null) {
        i = i.toLowerCase();
        const {eventListeners: l} = g, n = g.findListener(i, j, e);
        if (n >= 0) {
          let o = l[i];
          (k = g.afterRemoveListener) == null ? void 0 : k.call(g, i, o[n]);
          if (o.length > 1) {
            if (o.$firing) {
              l[i] = o = o.slice();
            }
            o.splice(n, 1);
          } else {
            delete l[i];
            if (!g.onUnlisten.$nullFn) {
              g.onUnlisten(i);
            }
          }
        }
      }
    });
    if (c.thisObj && !c.thisObj.isDestroyed) {
      g.detachAutoDetacher(c);
    }
  }
  findListener(c, e, f) {
    let g;
    const j = (g = this.eventListeners) == null ? void 0 : g[c], k = e.fn || e, l = e.thisObj || f;
    if (j) {
      for (let n, o = 0, {length: p} = j; o < p; o++) {
        n = j[o];
        if (n.fn === k && n.thisObj === l) {
          return o;
        }
      }
    }
    return -1;
  }
  hasListener(c) {
    let e;
    return Boolean((e = this.eventListeners) == null ? void 0 : e[c == null ? void 0 : c.toLowerCase()]);
  }
  relayAll(c, e, f = true) {
    if (!this.relayAllTargets) {
      this.relayAllTargets = [];
    }
    const {relayAllTargets: g} = this;
    c.ion({
      'beforeDestroy': ({source: i}) => {
        if (i === c) {
          const j = g.filter(k => k.through === c);
          j.forEach(k => ArrayHelper.remove(g, k));
        }
      }
    });
    g.push({
      'through': c,
      'prefix': e,
      'transformCase': f
    });
  }
  removeAllListeners(c = true) {
    let e;
    const f = this.eventListeners;
    let g, j;
    for (const k in f) {
      const l = f[k];
      for (g = l.length; g-- > 0;) {
        const n = l[g];
        if (!n.$internal || !c) {
          this.removeListener(k, n);
          j = n.thisObj;
          (e = j == null ? void 0 : j.untrackDetachers) == null ? void 0 : e.call(j, this);
        }
      }
    }
  }
  relayEvents(c, e, f = '', g) {
    const i = {
      'detachable': true,
      'thisObj': this
    };
    e.forEach(j => {
      if (f) {
        j = StringHelper.capitalize(j);
      }
      i[j] = (k, ...l) => {
        if (g) {
          k = Object.assign({}, k, { 'source': this });
        }
        return this.trigger(f + j, k, ...l);
      };
    });
    return c.on(i);
  }
  onListen() {
  }
  onUnlisten() {
  }
  destructorInterceptor() {
    const {
      autoDetachers: c,
      target: e,
      oldDestructor: f
    } = this;
    for (let g = 0; g < c.length; g++) {
      const {
        dispatcher: j,
        config: k
      } = c[g];
      if (!j.isDestroyed) {
        j.removeListener(k, e);
      }
    }
    f.call(e);
  }
  attachAutoDetacher(c, e) {
    const f = c.thisObj || e, g = 'doDestroy' in f ? 'doDestroy' : 'destroy';
    if (g in f) {
      let {$autoDetachers: i} = f;
      if (!i) {
        f.$autoDetachers = i = [];
      }
      if (!f.$oldDestructor) {
        f.$oldDestructor = f[g];
        f[g] = this.destructorInterceptor.bind({
          'autoDetachers': i,
          'oldDestructor': f.$oldDestructor,
          'target': f
        });
      }
      i.push({
        'config': c,
        'dispatcher': this
      });
    } else {
      f[g] = () => {
        this.removeListener(c);
      };
    }
  }
  detachAutoDetacher(c) {
    const e = c.thisObj;
    if (e.$oldDestructor && !e.isDestroying) {
      ArrayHelper.remove(e.$autoDetachers, e.$autoDetachers.find(f => f.config === c && f.dispatcher === this));
      if (!e.$autoDetachers.length) {
        e['doDestroy' in e ? 'doDestroy' : 'destroy'] = e.$oldDestructor;
        e.$oldDestructor = null;
      }
    }
  }
  once(c, e) {
    return this.ion({
      [c]: () => this.callback(e),
      'once': true
    });
  }
  await(c, e = {
    'checkLog': true,
    'resetLog': true,
    'args': null
  }) {
    const f = this;
    if (e === false) {
      e = { 'checkLog': false };
    }
    const {args: g} = e;
    return new Promise(i => {
      let j;
      if (e.checkLog && ((j = f._triggered) == null ? void 0 : j[c])) {
        i();
        if (e.resetLog) {
          f.clearLog(c);
        }
      }
      if (g) {
        const k = f.on({
          [c]: (...l) => {
            const n = typeof g === 'function' ? g(...l) : Object.keys(g).every(o => {
              return o in l[0] && l[0][o] === g[o];
            });
            if (n) {
              i(...l);
              if (e.resetLog) {
                f.clearLog(c);
              }
              k();
            }
          },
          'prio': -10000
        });
      } else {
        f.on({
          [c]: (...l) => {
            i(...l);
            if (e.resetLog) {
              f.clearLog(c);
            }
          },
          'prio': -10000,
          'once': true
        });
      }
    });
  }
  clearLog(c) {
    if (this._triggered) {
      if (c) {
        delete this._triggered[c];
      } else {
        this._triggered = {};
      }
    }
  }
  trigger(c, e) {
    let f, g, j, k;
    const l = this, n = c.toLowerCase(), {
        eventsSuspended: o,
        relayAllTargets: p,
        callOnFunctions: q
      } = l;
    let r = (f = l.eventListeners) == null ? void 0 : f[n], u;
    if (!l._triggered) {
      l._triggered = {};
    }
    l._triggered[c] = true;
    if (o) {
      if (o.shouldQueue) {
        o.queue.push(arguments);
      }
      return true;
    }
    if ((g = l.eventListeners) == null ? void 0 : g.catchall) {
      (r = r ? r.slice() : []).push(...l.eventListeners.catchall);
      r.sort(priorityComparator);
    }
    if (!r && !p && !q) {
      return true;
    }
    if (e) {
      if (!('source' in e)) {
        if (Object.isExtensible(e)) {
          e.source = l;
        } else {
          e = Object.setPrototypeOf({ 'source': l }, e);
        }
      }
    } else {
      e = { 'source': l };
    }
    if (e.type !== n) {
      if (e.constructor !== Object) {
        Reflect.defineProperty(e, 'type', { 'get': () => n });
      } else {
        e.type = n;
      }
    }
    e.eventName = c;
    if (!('bubbles' in e) && ((j = l.bubbleEvents) == null ? void 0 : j[c])) {
      e.bubbles = l.bubbleEvents[c];
    }
    if (q) {
      const w = 'on' + StringHelper.capitalize(c);
      if (w in l) {
        const z = l[w] ? l.callback(l[w], l, [e]) : true;
        let A;
        if (Objects.isPromise(z)) {
          (u || (u = [])).push(z);
        } else {
          A = z === false || A;
        }
        if (!l.isDestroyed && hasOwnProperty5.call(l, w) && !((k = l.pluginFunctionChain) == null ? void 0 : k[w])) {
          const B = Object.getPrototypeOf(l);
          if (w in B) {
            const C = B[w].call(l, e);
            if (Objects.isPromise(C)) {
              (u || (u = [])).push(C);
            } else {
              A = C === false || A;
            }
            if (l.isDestroyed) {
              return;
            }
          }
        }
        if (A) {
          return false;
        }
      }
    }
    let v;
    if (r) {
      let E = 0, F = false;
      r.$firing = true;
      for (E; E < r.length && !l.isDestroyed && !F; E++) {
        const G = r[E];
        if (v === false && !G.$internal) {
          continue;
        }
        let H, I = G.thisObj;
        if (!I || !I.isDestroyed) {
          G.called = true;
          if (G.once) {
            l.removeListener(n, G);
          }
          if (typeof G.fn === 'string') {
            if (I) {
              H = I[G.fn];
            }
            if (!H) {
              const K = l.resolveCallback(G.fn);
              H = K.handler;
              I = K.thisObj;
            }
          } else {
            H = G.fn;
          }
          if (G.buffer) {
            if (!G.bufferFn) {
              const L = Number(G.buffer);
              if (typeof L !== 'number' || isNaN(L)) {
                throw new Error('Incorrect type for buffer, got "' + L + '" (expected a Number)');
              }
              G.bufferFn = FunctionHelper.createBuffered(H, L, I, G.args);
            }
            H = G.bufferFn;
          }
          if (G.throttle) {
            const N = Number(G.throttle);
            if (typeof N !== 'number' || isNaN(N)) {
              throw new Error('Incorrect type for throttle, got "' + N + '" (expected a Number)');
            }
            if (!G.throttledFn) {
              G.throttledFn = FunctionHelper.createThrottled(H, N, I, G.args);
            }
            H = G.throttledFn;
          }
          const J = H.call(I || l, ...G.args || [], e);
          if (v !== false) {
            v = J;
          }
          if (G.$internal && J === false) {
            F = true;
          }
          if (Objects.isPromise(J)) {
            J.$internal = G.$internal;
            (u || (u = [])).push(J);
          }
        }
      }
      r.$firing = false;
      if (F) {
        return false;
      }
    }
    p == null ? void 0 : p.forEach(O => {
      let P = c;
      if (O.transformCase) {
        P = StringHelper.capitalize(P);
      }
      if (O.prefix) {
        P = O.prefix + P;
      }
      if (O.through.trigger(P, e) === false) {
        return false;
      }
    });
    if (e.bubbles && l.owner && !l.owner.isDestroyed) {
      return l.owner.trigger(c, e);
    }
    u = u == null ? void 0 : u.filter(O => v !== false || O.$internal);
    if (u == null ? void 0 : u.length) {
      return new Promise(O => {
        Promise.all(u).then(P => {
          const Q = !P.some(R => R === false);
          O(Q);
        });
      });
    }
    return v !== false;
  }
  suspendEvents(c = false) {
    const e = this.eventsSuspended || (this.eventsSuspended = {
      'shouldQueue': c,
      'queue': [],
      'count': 0
    });
    e.count++;
  }
  resumeEvents() {
    const c = this.eventsSuspended;
    if (c) {
      if (--c.count === 0) {
        this.eventsSuspended = null;
        if (c.shouldQueue) {
          for (const e of c.queue) {
            this.trigger(...e);
          }
        }
      }
    }
    return !Boolean(this.eventsSuspended);
  }
};
const AsyncHelper = class {
  static animationFrame() {
    return new Promise(a => {
      requestAnimationFrame(a);
    });
  }
  static sleep(a) {
    return new Promise(b => {
      setTimeout(() => {
        b();
      }, a);
    });
  }
  static yield() {
    return Promise.resolve();
  }
};
AsyncHelper._$name = 'AsyncHelper';
let zeroBased;
const allBorders = [
  'border-top-width',
  'border-right-width',
  'border-bottom-width',
  'border-left-width'
];
const allMargins = [
  'margin-top',
  'margin-right',
  'margin-bottom',
  'margin-left'
];
const allPaddings = [
  'padding-top',
  'padding-right',
  'padding-bottom',
  'padding-left'
];
const borderNames = {
  't': 'border-top-width',
  'r': 'border-right-width',
  'b': 'border-bottom-width',
  'l': 'border-left-width'
};
const paddingNames = {
  't': 'padding-top',
  'r': 'padding-right',
  'b': 'padding-bottom',
  'l': 'padding-left'
};
const alignSpecRe = /^([trblc])(\d*)-([trblc])(\d*)$/i;
const alignPointRe = /^([trblc])(\d*)$/i;
const edgeNames = [
  'top',
  'right',
  'bottom',
  'left'
];
const edgeIndices = {
  't': 0,
  'r': 1,
  'b': 2,
  'l': 3
};
const defaultAlignments = [
  'b-t',
  'l-r',
  't-b',
  'r-l'
];
const edgeAligments = {
  'bt': 1,
  'tb': 1,
  'lr': 2,
  'rl': 2
};
const emptyArray2 = Object.freeze([]);
const zeroOffsets = Object.freeze([
  0,
  0
]);
const matchDimensions = [
  'width',
  'height'
];
const parseEdges = (a, b = a, c = a, e = b) => Array.isArray(a) ? parseEdges.apply(null, a) : [
  a,
  b,
  c,
  e
];
const parseTRBL = (a, b, c, e) => Array.isArray(a) ? parseEdges.apply(null, a) : typeof a.top === 'number' ? [
  a.top,
  a.right,
  a.bottom,
  a.left
] : [
  a,
  b,
  c,
  e
];
const parseAlign = (a, b) => {
  const c = alignSpecRe.exec(a), e = c[1], f = c[3], g = parseInt(c[2] || 50), i = parseInt(c[4] || 50), j = b && !(edgeIndices[e] & 1) ? 100 - g : g, k = b && !(edgeIndices[f] & 1) ? 100 - i : i, l = edgeAligments[e + f];
  return {
    'myAlignmentPoint': e + j,
    'myEdge': e,
    'myOffset': j,
    'targetAlignmentPoint': f + k,
    'targetEdge': f,
    'targetOffset': k,
    'startZone': edgeIndices[f],
    'edgeAligned': l
  };
};
const flipAlign = a => '' + edgeNames[(edgeIndices[a.myEdge] + 2) % 4][0] + a.myOffset + '-' + edgeNames[(edgeIndices[a.targetEdge] + 2) % 4][0] + a.targetOffset;
const createOffsets = a => {
  if (a == null) {
    return zeroOffsets;
  } else if (typeof a === 'number') {
    return [
      a,
      a
    ];
  }
  return a;
};
const Rectangle = class _Rectangle {
  isRectangle = true;
  constructor(a, b, c, e) {
    ObjectHelper.assertNumber(a, 'Rectangle.x');
    ObjectHelper.assertNumber(b, 'Rectangle.y');
    ObjectHelper.assertNumber(c, 'Rectangle.width');
    ObjectHelper.assertNumber(e, 'Rectangle.height');
    const f = this;
    if (c < 0) {
      a += c;
      c = -c;
    }
    if (e < 0) {
      b += e;
      e = -e;
    }
    f._x = a;
    f._y = b;
    f._width = c;
    f._height = e;
  }
  static from(a, b, c) {
    if (typeof a === 'string') {
      a = document.querySelector(a);
    } else if ((a == null ? void 0 : a.nodeType) === Node.DOCUMENT_FRAGMENT_NODE) {
      a = a.host || a.ownerDocument;
    }
    if (typeof b === 'string') {
      b = document.querySelector(b);
    }
    if (a == null || a.isRectangle) {
      return a;
    }
    a = a.element || a;
    if (c === void 0 && typeof b === 'boolean') {
      c = b;
      b = null;
    }
    if (!(b == null ? void 0 : b.isRectangle)) {
      if (b) {
        let {
          scrollLeft: k,
          scrollTop: l
        } = b;
        if (BrowserHelper.isSafari && b === document.body) {
          k = l = 0;
        }
        b = _Rectangle.from(b).translate(-k, -l);
      } else {
        if (!zeroBased) {
          zeroBased = new _Rectangle(0, 0, 0, 0);
        }
        b = zeroBased;
      }
    }
    const e = a === document || a === globalThis, f = a === document.body && document.body.offsetHeight === 0, g = f && a.getBoundingClientRect(), i = f ? new _Rectangle(g.left, g.top, g.width, document.body.parentElement.scrollHeight) : e ? new _Rectangle(0, 0, globalThis.innerWidth, globalThis.innerHeight) : a.getBoundingClientRect(), j = c || e ? [
        0,
        0
      ] : [
        globalThis.pageXOffset,
        globalThis.pageYOffset
      ];
    return new _Rectangle(i.left + j[0] - b.x, i.top + j[1] - b.y, i.width, i.height);
  }
  static fromScreen(a, b) {
    return _Rectangle.from(a, b, true);
  }
  static inner(a, b, c = false) {
    const e = this.from(a, b, c);
    if (document.body.contains(a)) {
      const f = DomHelper.getStyleValue(a, allBorders);
      e.x += parseFloat(f[borderNames.l]);
      e.y += parseFloat(f[borderNames.t]);
      e.right -= parseFloat(f[borderNames.r]);
      e.bottom -= parseFloat(f[borderNames.b]);
    }
    return e;
  }
  static content(a, b, c = false) {
    const e = this.from(a, b, c);
    if (document.body.contains(a)) {
      const f = DomHelper.getStyleValue(a, allBorders), g = DomHelper.getStyleValue(a, allPaddings);
      e.x += parseFloat(f[borderNames.l]) + parseFloat(g[paddingNames.l]);
      e.y += parseFloat(f[borderNames.t]) + parseFloat(g[paddingNames.t]);
      e.right -= parseFloat(f[borderNames.r]) + parseFloat(g[paddingNames.r]);
      e.bottom -= parseFloat(f[borderNames.b]) + parseFloat(g[paddingNames.b]);
    }
    return e;
  }
  static client(a, b, c = false, f, g) {
    const j = this.content(a, b, c), k = DomHelper.scrollBarWidth;
    let l, n = f ? [...a.children].filter(o => DomHelper.getStyleValue(o, 'position') === 'sticky') : emptyArray2;
    if (g) {
      n = n.filter(o => !o.contains(g));
    }
    if (k && !a.classList.contains('b-hide-scroll')) {
      if (a.scrollHeight > a.clientHeight && DomHelper.getStyleValue(a, 'overflow-y') !== 'hidden') {
        l = parseFloat(DomHelper.getStyleValue(a, 'padding-right'));
        j.right += l - Math.max(l, k);
      }
      if (a.scrollWidth > a.clientWidth && DomHelper.getStyleValue(a, 'overflow-x') !== 'hidden') {
        l = parseFloat(DomHelper.getStyleValue(a, 'padding-bottom'));
        j.bottom += l - Math.max(l, k);
      }
    }
    for (let o = 0, {length: p} = n; o < p; o++) {
      const q = n[o], u = this.fromScreen(q);
      if (parseFloat(DomHelper.getStyleValue(q, 'left')) === 0) {
        j.x += u.width;
      } else if (parseFloat(DomHelper.getStyleValue(q, 'right')) === 0) {
        j.right -= u.width;
      } else if (parseFloat(DomHelper.getStyleValue(q, 'top')) === 0) {
        j.y += u.height;
      } else if (parseFloat(DomHelper.getStyleValue(q, 'bottom')) === 0) {
        j.bottom -= u.height;
      }
    }
    return j;
  }
  static outer(a, b, c = false) {
    const e = this.from(a, b, c);
    if (document.body.contains(a)) {
      const f = DomHelper.getStyleValue(a, allMargins);
      e.x -= parseFloat(f['margin-left']);
      e.y -= parseFloat(f['margin-top']);
      e.right += parseFloat(f['margin-right']);
      e.bottom += parseFloat(f['margin-bottom']);
    }
    return e;
  }
  static union(...a) {
    let {
        x: b,
        y: c,
        right: e,
        bottom: f
      } = a[0], g;
    if (a.length > 1) {
      for (let j = 1; j < a.length; j++) {
        g = a[j];
        if (g.x < b) {
          b = g.x;
        }
        if (g.y < c) {
          c = g.y;
        }
        if (g.right > e) {
          e = g.right;
        }
        if (g.bottom > f) {
          f = g.bottom;
        }
      }
    }
    return new _Rectangle(b, c, e - b, f - c);
  }
  roundPx(a = globalThis.devicePixelRatio || 1) {
    const b = this;
    b._x = DomHelper.roundPx(b._x, a);
    b._y = DomHelper.roundPx(b._y, a);
    b._width = DomHelper.roundPx(b._width, a);
    b._height = DomHelper.roundPx(b._height, a);
    return b;
  }
  static get $$name() {
    return hasOwnProperty.call(this, '$name') && this.$name || hasOwnProperty.call(this, '_$name') && this._$name || this.name;
  }
  get $$name() {
    return this.constructor.$$name;
  }
  clone() {
    const a = this, b = new _Rectangle(a.x, a.y, a.width, a.height);
    b.isAlignRectangle = a.isAlignRectangle;
    b.minHeight = a.minHeight;
    b.minWidth = a.minWidth;
    return b;
  }
  contains(a) {
    const b = this;
    if (a.isRectangle) {
      return a._x >= b._x && a._y >= b._y && a.right <= b.right && a.bottom <= b.bottom;
    } else {
      return false;
    }
  }
  intersect(a, c = false, e = false) {
    const f = this, g = Math.max(f.y, a.y), i = Math.min(f.right, a.right), j = Math.min(f.bottom, a.bottom), k = Math.max(f.x, a.x), l = e ? j >= g && i >= k : j > g && i > k;
    if (l) {
      return c ? true : new _Rectangle(k, g, i - k, j - g);
    } else {
      return false;
    }
  }
  equals(a, b = false) {
    const c = b ? e => Math.round(e) : e => e;
    return a.isRectangle && c(a.x) === c(this.x) && c(a.y) === c(this.y) && c(a.width) === c(this.width) && c(a.height) === c(this.height);
  }
  translate(a, b) {
    this._x += a || 0;
    this._y += b || 0;
    return this;
  }
  moveTo(a, b) {
    if (a != null) {
      this._x = a;
    }
    if (b != null) {
      this._y = b;
    }
    return this;
  }
  getDelta(a) {
    return [
      a.x - this.x,
      a.y - this.y
    ];
  }
  get center() {
    const a = new Point(this.x + this.width / 2, this.y + this.height / 2, 0, 0);
    a.target = this.target;
    return a;
  }
  set x(a) {
    const b = a - this._x;
    this._x = a;
    this._width -= b;
  }
  get x() {
    return this._x;
  }
  get start() {
    return this.left;
  }
  set left(a) {
    this.x = a;
  }
  get left() {
    return this.x;
  }
  set top(a) {
    this.y = a;
  }
  get top() {
    return this.y;
  }
  set y(a) {
    const b = a - this._y;
    this._y = a;
    this._height -= b;
  }
  get y() {
    return this._y;
  }
  set width(a) {
    this._width = a;
  }
  get width() {
    return this._width;
  }
  set height(a) {
    this._height = a;
  }
  get height() {
    return this._height;
  }
  set right(a) {
    this._width = a - this._x;
  }
  get right() {
    return this._x + this._width;
  }
  get end() {
    return this.right;
  }
  set bottom(a) {
    this._height = a - this._y;
  }
  get bottom() {
    return this._y + this._height;
  }
  getStart(a, b = true) {
    if (b) {
      return a ? this.right : this.left;
    }
    return this.top;
  }
  getEnd(a, b = true) {
    if (b) {
      return a ? this.left : this.right;
    }
    return this.bottom;
  }
  get area() {
    return this.width * this.height;
  }
  set minWidth(a) {
    const b = this;
    if (isNaN(a)) {
      b._minWidth = null;
    } else {
      b._minWidth = Number(a);
      if (!b.isAlignRectangle) {
        b.width = Math.max(b.width, b._minWidth);
      }
    }
  }
  get minWidth() {
    return this._minWidth;
  }
  set minHeight(a) {
    const b = this;
    if (isNaN(a)) {
      b._minHeight = null;
    } else {
      b._minHeight = Number(a);
      if (!b.isAlignRectangle) {
        b.height = Math.max(b.height, b._minHeight);
      }
    }
  }
  get minHeight() {
    return this._minHeight;
  }
  adjust(a, b, c, e) {
    const f = this;
    f.x += a;
    f.y += b;
    f.width += c;
    f.height += e;
    return f;
  }
  inflate(a, b = a, c = a, e = b) {
    [a, b, c, e] = parseTRBL(a, b, c, e);
    return this.adjust(-e, -a, b, c);
  }
  deflate(a, b = a, c = a, e = b) {
    [a, b, c, e] = parseTRBL(a, b, c, e);
    return this.adjust(e, a, -b, -c);
  }
  constrainTo(a, b) {
    const c = this, e = c.height, f = c.y, g = c.minWidth || c.width, i = c.minHeight || c.height;
    if (c.height >= a.height) {
      if (b && i > a.height) {
        return false;
      }
      c._y = a.y;
      c.height = a.height;
    }
    if (c.width >= a.width) {
      if (b && g > a.width) {
        c.y = f;
        c.height = e;
        return false;
      }
      c._x = a.x;
      c.width = a.width;
    }
    c.translate.apply(c, c.constrainVector = [
      Math.min(a.right - c.right, 0),
      Math.min(a.bottom - c.bottom, 0)
    ]);
    c.translate(Math.max(a.x - c.x, 0), Math.max(a.y - c.y, 0));
    return c;
  }
  alignTo(a) {
    let b = this.clone(), {
        target: c,
        constrainTo: e,
        constrainPadding: f
      } = a, g, j, k, l;
    if (c && !c.isRectangle) {
      c = _Rectangle.from(c.element ? c.element : c);
    }
    if (e) {
      if (!e.isRectangle) {
        l = e === globalThis || e === document;
        const J = 'ignorePageScroll' in a ? a.ignorePageScroll : !l;
        e = _Rectangle.from(e.element ? e.element : e, null, J);
      }
      if (f) {
        f = parseEdges(f);
        f[0] = Math.min(f[0], c.top);
        f[1] = Math.min(f[1], e.right - c.right);
        f[2] = Math.min(f[2], e.bottom - c.bottom);
        f[3] = Math.min(f[3], c.left);
        e = e.deflate.apply(e.clone(), f);
      }
    }
    const n = this, o = createOffsets(a.offset), {
        align: p,
        axisLock: q,
        anchorSize: u,
        anchorPosition: v,
        matchSize: w,
        position: z,
        rtl: A
      } = a, B = c && (c.width < 2 && c.height < 2), C = parseAlign(p, A), E = e && e.clone(), F = [], G = [{
          'zone': j = C.startZone,
          'align': p
        }], H = w && matchDimensions[C.startZone & 1], I = n[H];
    if (H && q) {
      b[H] = w === 'min' ? Math.max(b.width, c[H]) : c[H];
    } else if (!C.edgeAligned && w) {
      b.width = c.width;
      b.height = c.height;
    }
    if (e) {
      b.constrainTo(e);
    }
    if (e && C.startZone != null) {
      if (q) {
        G.push({
          'zone': j = (j + 2) % 4,
          'align': flipAlign(C)
        });
        if (q === 'flexible') {
          G.push({
            'zone': j = (C.startZone + 1) % 4,
            'align': defaultAlignments[j]
          });
          G.push({
            'zone': j = (j + 2) % 4,
            'align': defaultAlignments[j]
          });
        }
      } else {
        for (let K = 1; K < 4; K++) {
          G.push({
            'zone': j = (j + 1) % 4,
            'align': defaultAlignments[j]
          });
        }
      }
    }
    if (v) {
      const L = C.startZone & 1 ? 'y' : 'x';
      g = {
        [L]: v[L],
        'edge': edgeNames[(C.startZone + 2) % 4]
      };
    }
    if (E && c) {
      E.adjust(-c.width, -c.height, c.width, c.height);
      c.constrainTo(E);
    }
    b.minWidth = n.minWidth;
    b.minHeight = n.minHeight;
    if (z) {
      b.moveTo(z.x, z.y);
      b.translate.apply(b, o);
      if (e) {
        b.constrainTo(e);
      }
    } else {
      const N = C.myEdge === 'c' || C.targetEdge === 'c', O = u && !N ? [
          u[1] + o[0],
          u[1] + o[1]
        ] : o;
      const P = c.getAlignmentPoint(C.targetAlignmentPoint), Q = c.getAlignmentPoint(C.targetAlignmentPoint, O);
      let R = b.getAlignmentPoint(C.myAlignmentPoint);
      let S = !B && b.clone().translate(P[0] - R[0], P[1] - R[1]).intersect(c, true);
      b.translate(Q[0] - R[0], Q[1] - R[1]);
      if (S) {
        b.translate(-(Q[0] - R[0]), -(Q[1] - R[1]));
        R = b.getAlignmentPoint(C.myAlignmentPoint);
        b.translate(P[0] - R[0], P[1] - R[1]);
        b.translate(...O);
        if (e) {
          b.constrainTo(e);
        }
        k = C.startZone;
      } else if (e && !e.contains(b)) {
        const T = b.clone(), U = [];
        let V, W;
        g = null;
        F[0] = V = e.clone();
        V.bottom = c.y - O[1];
        F[1] = V = e.clone();
        V.x = c.right + O[0];
        F[2] = V = e.clone();
        V.y = c.bottom + O[1];
        F[3] = V = e.clone();
        V.right = c.x - O[0];
        for (let X = 0; X < G.length; X++) {
          if (H && X === 2) {
            b[H] = I;
          }
          V = F[k = G[X].zone];
          b = b.alignTo({
            'target': c,
            'offsets': O,
            'align': G[X].align
          });
          if (b.constrainTo(V, true)) {
            U.push({
              'result': b,
              'zone': k
            });
            if (!W || b.width < n.width || b.height < n.height) {
              b.align = G[X].align;
              break;
            }
          }
          if (!W || V.area > W.area) {
            const Y = b.clone();
            switch (k) {
            case 0:
              Y.moveTo(null, V.bottom - Y.height);
              break;
            case 1:
              Y.moveTo(V.left);
              break;
            case 2:
              Y.moveTo(null, V.top);
              break;
            case 3:
              Y.moveTo(V.right - Y.width);
              break;
            }
            W = {
              'area': V.area,
              'result': Y,
              'zone': k
            };
          }
        }
        if (U.length) {
          if (U.length > 1 && !q) {
            U.sort((Z, a0) => {
              const a1 = Math.sqrt((T.x - Z.result.x) ** 2 + (T.y - Z.result.y) ** 2), a2 = Math.sqrt((T.x - a0.result.x) ** 2 + (T.y - a0.result.y) ** 2);
              return a1 - a2;
            });
          }
          b = U[0].result;
          k = U[0].zone;
        } else {
          b = W.result;
          k = W.zone;
          if (l) {
            b.constrainTo(e);
          }
        }
      } else {
        k = C.startZone;
      }
      if (B) {
        b.overlap = S = false;
      } else {
        b.overlap = S = b.intersect(c, true);
      }
      b.zone = k;
      if (u && !S) {
        if (!g) {
          const Z = k & 1, a0 = Z ? 'y' : 'x', a1 = Z ? 'bottom' : 'right', a2 = Math.max(c[a0], b[a0]), a3 = Math.min(c[a1], b[a1]);
          let a4 = a2 + (a3 - a2) / 2 - u[0] / 2;
          const a5 = a4 + u[0];
          if (a5 > b[a1]) {
            a4 -= a5 - b[a1];
          }
          if (a4 < b[a0]) {
            a4 += b[a0] - a4;
          }
          g = {
            [a0]: a4 - b[a0],
            'edge': edgeNames[(k + 2) % 4]
          };
        }
        b.anchor = g;
      }
    }
    return b;
  }
  getAlignmentPoint(a, b = zeroOffsets) {
    a = String(a);
    const c = this, e = alignPointRe.exec(a) || alignSpecRe.exec(a), f = e && e[1].toLowerCase(), g = e && Math.min(Math.max(parseInt(e[2] || 50), 0), 100) / 100;
    switch (f) {
    case 't':
      return [
        c.x + c.width * g,
        c.y - b[1]
      ];
    case 'r':
      return [
        c.right + b[0],
        c.y + c.height * g
      ];
    case 'b':
      return [
        c.x + c.width * g,
        c.bottom + b[1]
      ];
    case 'l':
      return [
        c.x - b[0],
        c.y + c.height * g
      ];
    case 'c': {
        return [
          c.x + c.width / 2,
          c.y + c.height / 2
        ];
      }
    }
  }
  highlight() {
    const a = this, b = DomHelper.createElement({
        'parent': document.body,
        'style': 'position:absolute;z-index:9999999;pointer-events:none;\n                            left:' + a.x + 'px;top:' + a.y + 'px;width:' + a.width + 'px;height:' + a.height + 'px'
      });
    return DomHelper.highlight(b).then(() => b.remove());
  }
  visualize(a, b) {
    const c = this, e = ObjectHelper.merge({
        'style': {
          'inset-inline-start': c.x + 'px',
          'top': c.y + 'px',
          'width': c.width + 'px',
          'height': c.height + 'px',
          'pointerEvents': 'none',
          ...a.class ? {} : {
            'position': 'absolute',
            'z-index': 9999999
          }
        }
      }, a);
    return b ? e : DomHelper.createElement(e);
  }
  toString(a = ',') {
    return [
      this.top + 'px',
      this.right + 'px',
      this.bottom + 'px',
      this.left + 'px'
    ].join(a);
  }
};
var Point = class extends Rectangle {
  static from(a, b = true) {
    const c = b ? 'client' : 'screen';
    if (a.changedTouches) {
      a = a.changedTouches[0];
    }
    const e = a[c + 'X'], f = a[c + 'Y'];
    return new this(e, f);
  }
  constructor(a, b) {
    super(a, b, 0, 0);
  }
  constrain(a) {
    this.x = Math.min(Math.max(this.x, a.x), a.right - 1);
    this.y = Math.min(Math.max(this.y, a.y), a.bottom - 1);
    return this;
  }
  toArray() {
    return [
      this.x,
      this.y
    ];
  }
};
Rectangle._$name = 'Rectangle';
const valueSymbol = Symbol('value');
const lengthSymbol = Symbol('length');
const DomClassList = class _DomClassList {
  static change(a, b, c, e = 'string') {
    c = _DomClassList.normalize(c, 'object');
    const f = _DomClassList.normalize(a, 'array').filter(g => !c[g]);
    if (b) {
      b = _DomClassList.normalize(b, 'array');
      for (let g = 0; g < b.length; ++g) {
        if (!f.includes(b[g])) {
          f.push(b[g]);
        }
      }
    }
    return _DomClassList.normalize(f, e);
  }
  static from(a, b) {
    if (a) {
      if (a.isDomClassList) {
        b = b != null ? b : true;
      } else {
        b = b != null ? b : Objects.isObject(a) && !Objects.isEmpty(a);
        a = new _DomClassList(a);
      }
      if (!a.value && !b) {
        a = null;
      }
    }
    return a || (b ? new _DomClassList() : null);
  }
  static normalize(a, b = 'string') {
    a = a || '';
    const e = typeof a, f = b === 'array', g = b === 'object', j = !f && !g;
    let k = e === 'string', l, n, o;
    if (e === 'object') {
      if (a.nodeType === Element.ELEMENT_NODE && typeof a.getAttribute === 'function') {
        a = a.getAttribute('class') || '';
        k = true;
      } else if (a == null ? void 0 : a.isDomClassList) {
        a = a.values;
      } else if (a instanceof DOMTokenList) {
        a = Array.from(a);
      } else if (a instanceof Map) {
        a = Array.from(a.keys()).filter(p => a.get(p));
      } else if (a instanceof Set) {
        a = Array.from(a);
      } else if (!Array.isArray(a)) {
        a = Objects.getTruthyKeys(a);
      }
    }
    if (k) {
      a = [...new Set(StringHelper.split(a))];
    }
    for (n = a.length; n-- > 0;) {
      l = a[n];
      if (!l.length) {
        a.splice(n, 1);
      } else if (l.includes(' ')) {
        a.splice(n, 1, ...StringHelper.split(l));
      }
    }
    if (f) {
      o = a;
    } else if (j) {
      o = a.join(' ');
    } else {
      o = Object.create(null);
      for (n = 0; n < a.length; ++n) {
        o[a[n]] = true;
      }
    }
    return o;
  }
  constructor(...a) {
    this.process(1, a);
  }
  clear() {
    for (const a in this) {
      this[a] = false;
    }
    return this;
  }
  set(...a) {
    return this.clear().process(1, a);
  }
  get isDomClassList() {
    return true;
  }
  clone() {
    return new _DomClassList(this);
  }
  contains(a) {
    if (typeof a === 'string' && a) {
      return Boolean(this[a]);
    }
    return false;
  }
  toString() {
    return this.length ? this.value + ' ' : '';
  }
  toJSON() {
    return this.toString();
  }
  trim() {
    return this.value;
  }
  isEqual(a) {
    const b = _DomClassList.normalize(a, 'array'), c = b.length;
    if (this.length === c) {
      for (let e = 0; e < c; e++) {
        if (!this[b[e]]) {
          return false;
        }
      }
      return true;
    }
    return false;
  }
  get value() {
    let a = this[valueSymbol], b;
    if (a == null) {
      b = Objects.getTruthyKeys(this);
      this[lengthSymbol] = b.length;
      this[valueSymbol] = a = b.join(' ');
    }
    return a;
  }
  set value(a) {
    const b = this, c = Object.keys(b), e = c.length;
    for (let f = 0; f < e; f++) {
      delete b[c[f]];
    }
    if (a) {
      b.process(1, [a]);
    } else {
      delete b[valueSymbol];
    }
  }
  get values() {
    return Objects.getTruthyKeys(this);
  }
  get length() {
    return this.value ? this[lengthSymbol] : 0;
  }
  process(a, b) {
    for (let c, e, f = 0; f < b.length; f++) {
      if (b[f]) {
        c = b[f];
        if (c.isDomClassList || Objects.isObject(c)) {
          for (e in c) {
            this[e] = a ? c[e] : !c[e];
          }
        } else {
          c = _DomClassList.normalize(b[f], 'array');
          for (e = 0; e < c.length; ++e) {
            this[c[e]] = a;
          }
        }
      }
    }
    delete this[valueSymbol];
    return this;
  }
  assign(a) {
    for (const b in a) {
      if (!this[b] !== !a[b]) {
        this[b] = a[b];
        delete this[valueSymbol];
      }
    }
    return this;
  }
  assignTo(a) {
    const b = a.nodeType === Element.ELEMENT_NODE ? a.classList : a, c = _DomClassList.from(b);
    c.add(this);
    b.value = c.value;
  }
  add(...a) {
    return this.process(1, a);
  }
  remove(...a) {
    return this.process(0, a);
  }
  toggle(a, b = Boolean(!this[a])) {
    b = Boolean(b);
    if (Boolean(this[a]) !== b) {
      this[a] = b;
      delete this[valueSymbol];
      return true;
    }
  }
  split() {
    return Objects.getTruthyKeys(this);
  }
  forEach(a) {
    return Objects.getTruthyKeys(this).forEach(a);
  }
};
DomClassList.prototype[valueSymbol] = null;
DomClassList._$name = 'DomClassList';
const touchProperties = [
  'clientX',
  'clientY',
  'pageX',
  'pageY',
  'screenX',
  'screenY'
];
const isOption = {
  'element': 1,
  'thisObj': 1,
  'once': 1,
  'delegate': 1,
  'delay': 1,
  'capture': 1,
  'passive': 1,
  'throttled': 1,
  'autoDetach': 1,
  'expires': 1,
  'block': 1,
  'raw': 1,
  'fromGlobal': 1
};
const normalizedKeyNames = {
  'Spacebar': 'Space',
  'Del': 'Delete',
  'Esc': 'Escape',
  'Left': 'ArrowLeft',
  'Up': 'ArrowUp',
  'Right': 'ArrowRight',
  'Down': 'ArrowDown'
};
const specialKeys = {
  'Control': 'ctrl',
  'Alt': 'alt',
  'Shift': 'shift',
  'Meta': 'meta'
};
const specialKeyRe = /^(ctrl|shift|alt|meta)$/;
const eventProps = [
  'altKey',
  'bubbles',
  'button',
  'buttons',
  'cancelable',
  'clientX',
  'clientY',
  'ctrlKey',
  'layerX',
  'layerY',
  'metaKey',
  'pageX',
  'pageY',
  'returnValue',
  'screenX',
  'screenY',
  'shiftKey'
];
const powerSet = (...a) => a.reduce((b, c) => {
  for (let e = 0, f = b.length; e < f; ++e) {
    b.push(e ? b[e] + '+' + c : c);
  }
  return b;
}, ['']);
const letterKeyNames = Object.fromEntries('abcdefghijklmnopqrstuvwxyz'.split('').map(a => [
  a,
  a.toUpperCase()
]));
const modifierKeys = powerSet('Ctrl', 'Alt', 'Meta', 'Shift');
const getModKeys = a => modifierKeys[(a.ctrlKey && 1) | (a.altKey && 2) | (a.metaKey && 4) | (a.shiftKey && 8)];
const getKeyName = a => {
  const b = [getModKeys(a)], {
      code: c,
      key: e
    } = a;
  b[b[0] ? 1 : 0] = (c == null ? void 0 : c.includes('Numpad')) ? c : letterKeyNames[e] || e;
  return b.join('+');
};
const {isSafari} = BrowserHelper;
const wrapperMap = new WeakMap();
const fixTarget = a => (a == null ? void 0 : a.nodeType) === Element.TEXT_NODE ? a.parentElement : a;
const _EventHelper = class _EventHelper {
  static eventNameMap = {
    'mousedown': 'MouseDown',
    'mouseup': 'MouseUp',
    'click': 'Click',
    'dblclick': 'DblClick',
    'contextmenu': 'ContextMenu',
    'mouseenter': 'MouseEnter',
    'mouseleave': 'MouseLeave',
    'mousemove': 'MouseMove',
    'mouseover': 'MouseOver',
    'mouseout': 'MouseOut',
    'keyup': 'KeyUp',
    'keydown': 'KeyDown',
    'keypress': 'KeyPress'
  };
  static $name = 'EventHelper';
  static normalizeEvent(a) {
    return ObjectHelper.copyPropertiesIf(a, a.changedTouches[0] || a.touches[0], touchProperties);
  }
  static copyEvent(a, b) {
    return ObjectHelper.copyProperties(a, b, eventProps);
  }
  static getXY(a) {
    if (a.touches) {
      a = a.touches[0];
    }
    return [
      a.clientX,
      a.clientY
    ];
  }
  static getDistanceBetween(a, b, c) {
    const e = EH.getXY(a), f = EH.getXY(b), g = e[0] - f[0], i = e[1] - f[1];
    if (c == null ? void 0 : c.x) {
      return g;
    }
    if (c == null ? void 0 : c.y) {
      return i;
    }
    return Math.sqrt(Math.pow(g, 2) + Math.pow(i, 2));
  }
  static getPagePoint(a) {
    return new Point(a.pageX, a.pageY);
  }
  static getClientPoint(a) {
    return new Point(a.clientX, a.clientY);
  }
  static addListener(a, b, c, e) {
    if (a.nodeType) {
      if (typeof b === 'string') {
        e = Object.assign({
          'element': a,
          [b]: c
        }, e);
      } else {
        e = Object.assign({ 'element': a }, b);
      }
    } else {
      e = a;
    }
    return EH.on(e);
  }
  static on(a) {
    const b = a.element, c = a.thisObj, e = [];
    for (const g in a) {
      if (!isOption[g]) {
        let i = a[g];
        if (typeof i !== 'object') {
          i = { 'handler': i };
        }
        const j = i.element || b;
        e.push(EH.addElementListener(j, g, i, a));
      }
    }
    const f = () => {
      for (let k, l = 0; l < e.length; l++) {
        k = e[l];
        EH.removeEventListener(k[0], k[1], k[2]);
      }
      e.length = 0;
    };
    if (c && a.autoDetach !== false) {
      c.doDestroy = FunctionHelper.createInterceptor(c.doDestroy, f, c);
    }
    return f;
  }
  static addElementListener(a, b, c, e) {
    const f = EH.createHandler(a, b, c, e), {spec: g} = f, i = c.expires || e.expires, j = g.capture != null || g.passive != null ? {
        'capture': g.capture,
        'passive': g.passive
      } : void 0;
    a.addEventListener(b, f, j);
    if (i) {
      const k = c.thisObj || e.thisObj, l = (k == null ? void 0 : k.isDelayable) ? k : globalThis, {alt: n} = i, o = n ? i.delay : i, {spec: p} = f;
      p.expires = i;
      p.timerId = l[typeof o === 'number' ? 'setTimeout' : 'requestAnimationFrame'](() => {
        p.timerId = null;
        EH.removeEventListener(a, b, f);
        if (n && !f.called) {
          (typeof n === 'string' ? k[n] : n).call(k);
        }
      }, o, 'listener-timer-' + performance.now());
    }
    return [
      a,
      b,
      f,
      j
    ];
  }
  static getComposedPathTarget(a) {
    let b;
    return a.composedPath()[0] || ((b = a.path) == null ? void 0 : b[0]);
  }
  static fixEvent(a, b) {
    let c, e;
    if (a.isBryntumEvent) {
      return a;
    }
    if (wrapperMap.has(a)) {
      return wrapperMap.get(a);
    }
    const {
        ctrlKey: f,
        metaKey: g,
        type: i,
        target: j
      } = a, k = this.getComposedPathTarget(a), l = ((e = (c = k == null ? void 0 : k.getRootNode) == null ? void 0 : c.call(k)) == null ? void 0 : e.mode) === 'open', n = {
        'set'(p, q, r) {
          if (q in p) {
            return Reflect.set(p, q, r);
          } else {
            n[q] = r;
            return true;
          }
        },
        'get'(p, q) {
          switch (q) {
          case 'fixed':
          case 'isBryntumEvent':
            return true;
          case 'key':
            return normalizedKeyNames[p.key] || p.key;
          case 'code':
            return p.key === ' ' && !p.code ? 'Space' : p.code;
          case 'ctrlKey':
            return BrowserHelper.isMac && g !== f ? g : f;
          case 'metaKey':
            return BrowserHelper.isMac && g !== f ? f : g;
          case 'virtualKey':
            return getKeyName(o);
          case 'virtualModKeys':
            return getModKeys(o);
          case 'borderOffsetX':
            return !isNaN(p.offsetX) ? p.offsetX + (isSafari ? 0 : parseInt(j.ownerDocument.defaultView.getComputedStyle(j).getPropertyValue('border-left-width'))) : void 0;
          case 'borderOffsetY':
            return !isNaN(p.offsetY) ? p.offsetY + (isSafari ? 0 : parseInt(j.ownerDocument.defaultView.getComputedStyle(j).getPropertyValue('border-top-width'))) : void 0;
          case 'target':
            return l ? k : fixTarget(p.target);
          case 'relatedTarget':
            return fixTarget(p.relatedTarget);
          case 'originalTarget':
            return j;
          case 'deepTarget':
            return k;
          case 'currentTarget':
            return n.delegatedTarget || p.currentTarget;
          case 'browserEvent':
            return p;
          default:
            if (q in p) {
              const r = Reflect.get(p, q);
              return typeof r === 'function' ? r.bind(p) : r;
            } else {
              return n[q];
            }
          }
        },
        'has'(p, q) {
          return q in p || q in n;
        }
      };
    const o = new Proxy(a, n);
    wrapperMap.set(a, o);
    if (i.startsWith('touch')) {
      this.normalizeEvent(o);
    }
    if (!b) {
      switch (i) {
      case 'keydown':
        GlobalEvents_default.currentKeyDown = o;
        break;
      case 'mousedown':
        GlobalEvents_default.currentMouseDown = o;
        break;
      case 'pointerdown':
        GlobalEvents_default.currentPointerDown = o;
        break;
      case 'touchstart':
        GlobalEvents_default.currentTouch = o;
      }
    }
    return o;
  }
  static createHandler(a, b, c, e) {
    const f = c.delay || e.delay, g = c.throttled || e.throttled, i = c.block || e.block, j = 'once' in c ? c.once : e.once, k = 'capture' in c ? c.capture : e.capture, l = 'passive' in c ? c.passive : e.passive, n = 'delegate' in c ? c.delegate : e.delegate, o = 'fromGlobal' in c ? c.fromGlobal : e.fromGlobal, p = c.handler, q = c.expires, r = c.thisObj || e.thisObj;
    let u = (v, ...w) => {
      if (EH.playingDemo && v.isTrusted) {
        return;
      }
      if (r == null ? void 0 : r.isDestroyed) {
        return;
      }
      u.called = true;
      (typeof p === 'string' ? r[p] : p).call(r, EH.fixEvent(v, o), ...w);
    };
    if (i) {
      const v = u;
      let w, z;
      u = (A, ...B) => {
        const C = performance.now();
        if (!w || A.target !== z || C - w > i) {
          z = A.target;
          w = C;
          v(A, ...B);
        }
      };
    }
    if (f != null) {
      const A = u, B = (r == null ? void 0 : r.setTimeout) ? r : globalThis;
      u = (...C) => {
        B.setTimeout(() => {
          A(...C);
        }, f);
      };
    }
    if (g != null) {
      let C, E = g;
      if (g.buffer) {
        C = F => {
          return g.alt.call(EH, EH.fixEvent(F, o));
        };
        E = g.buffer;
      }
      if (r == null ? void 0 : r.isDelayable) {
        u = r.throttle(u, {
          'delay': E,
          'throttled': C
        });
      } else {
        u = FunctionHelper.createThrottled(u, E, r, null, C);
      }
    }
    if (j) {
      const F = u;
      u = (...G) => {
        EH.removeEventListener(a, b, u);
        F(...G);
      };
    }
    if (n) {
      const G = u;
      u = (H, ...I) => {
        let J, K;
        H = EH.fixEvent(H, o);
        const L = (K = (J = H.target).closest) == null ? void 0 : K.call(J, n);
        if (!L) {
          return;
        }
        H.delegatedTarget = L;
        G(H, ...I);
      };
    }
    if (c.thisObj && c.autoDetach !== false) {
      r.doDestroy = FunctionHelper.createInterceptor(r.doDestroy, () => EH.removeEventListener(a, b, u), r);
    }
    u.spec = {
      'delay': f,
      'throttled': g,
      'block': i,
      'once': j,
      'thisObj': r,
      'capture': k,
      'expires': q,
      'passive': l,
      'delegate': n
    };
    return u;
  }
  static removeEventListener(a, b, c) {
    const {
      expires: e,
      timerId: f,
      thisObj: g,
      capture: i
    } = c.spec;
    if ((e == null ? void 0 : e.alt) && f) {
      const j = (g == null ? void 0 : g.isDelayable) ? g : globalThis;
      j[typeof e.delay === 'number' ? 'clearTimeout' : 'cancelAnimationFrame'](f);
    }
    a.removeEventListener(b, c, i);
  }
  static onTransitionEnd({
    element: a,
    animationName: b,
    property: c,
    handler: e,
    mode: mode = b ? 'animation' : 'transition',
    duration: duration = DomHelper['get' + (mode === 'transition' ? 'Property' : '') + StringHelper.capitalize(mode) + 'Duration'](a, c),
    timeout: f,
    thisObj: thisObj = globalThis,
    args: args = [],
    timerSource: g,
    runOnDestroy: i
  }) {
    let j, k;
    g = g || (thisObj.isDelayable ? thisObj : globalThis);
    const {isDelayable: l} = g, n = performance.now(), o = 'transitionTimer' + n, p = 'timeoutTimer' + n, q = [
        a,
        c,
        ...args
      ], r = () => {
        u();
        if (!thisObj.isDestroyed) {
          if (thisObj.callback) {
            thisObj.callback(e, thisObj, q);
          } else {
            e.apply(thisObj, q);
          }
        }
      }, u = EH.on({
        'element': a,
        [mode + 'end']({
          animationName: v,
          propertyName: w,
          target: z
        }) {
          let A, B;
          if (z === a) {
            if (w === c || (v == null ? void 0 : v.match(b))) {
              if (j) {
                if (k) {
                  (A = g.clearTimeout) == null ? void 0 : A.call(g, l ? p : k);
                }
                (B = g.clearTimeout) == null ? void 0 : B.call(g, l ? o : j);
                j = null;
              }
              r();
            }
          }
        }
      });
    if (f) {
      k = g.setTimeout(r, Math.max(f, (duration || 0) + 100), p);
    }
    if (duration != null) {
      j = g.setTimeout(r, duration + 50, o, i);
    }
    return u;
  }
  static async waitForTransitionEnd(a) {
    return new Promise(b => {
      a.handler = b;
      _EventHelper.onTransitionEnd(a);
    });
  }
  static createDblClickWrapper(a, b, c) {
    let e, f, g;
    return () => {
      if (!f) {
        f = EH.on({
          'element': a,
          'touchstart': i => {
            e = i.changedTouches[0].identifier;
            i.preventDefault();
          },
          'touchend': i => {
            if (i.changedTouches[0].identifier === e) {
              i.preventDefault();
              clearTimeout(g);
              e = f = null;
              const j = Rectangle.from(i.changedTouches[0].target, null, true), k = i.changedTouches[0].pageX - j.x, l = i.changedTouches[0].pageY - j.y, n = Object.assign({ 'browserEvent': i }, i), o = new MouseEvent('dblclick', n);
              Object.defineProperties(o, {
                'target': { 'value': i.target },
                'offsetX': { 'value': k },
                'offsetY': { 'value': l }
              });
              if (typeof b === 'string') {
                b = c[b];
              }
              b.call(c, o);
            }
          },
          'once': true
        });
        g = setTimeout(() => {
          f();
          e = f = null;
        }, EH.dblClickTime);
      }
    };
  }
  static toSpecialKey(a, b = 'ctrlKey') {
    let c = false;
    if (a === true) {
      c = b;
    } else if (typeof a === 'string') {
      a = a.toLowerCase();
      if (a.match(specialKeyRe)) {
        c = a + 'Key';
      }
    }
    return c;
  }
  static specialKeyFromEventKey(a) {
    return specialKeys[a] || 'no-special-key';
  }
};
const EventHelper = _EventHelper;
Helpers_default.register(EventHelper);
var EH = EventHelper;
EH.longPressTime = 700;
EH.dblClickTime = 300;
if (BrowserHelper.isTouchDevice) {
  EH.on({
    'element': document,
    'touchmove': a => {
      if (a.target.closest('.b-dragging')) {
        a.preventDefault();
      }
    },
    'passive': false,
    'capture': true
  });
}
EventHelper._$name = 'EventHelper';
const DEFAULT_FONT_SIZE = 14;
const t0t0 = { 'align': 't0-t0' };
const ELEMENT_NODE = Node.ELEMENT_NODE;
const TEXT_NODE = Node.TEXT_NODE;
const {isObject: isObject2} = ObjectHelper;
const numberRe = /[+-]?\d*\.?\d+[eE]?-?\d*/g;
const numberReSrc = numberRe.source;
const translateMatrix2dRe = new RegExp('matrix\\((?:' + numberReSrc + '),\\s?(?:' + numberReSrc + '),\\s?(?:' + numberReSrc + '),\\s?(?:' + numberReSrc + '),\\s?(' + numberReSrc + '),\\s?(' + numberReSrc + ')');
const translateMatrix3dRe = new RegExp('matrix3d\\((?:-?\\d*),\\s?(?:-?\\d*),\\s?(?:-?\\d*),\\s?(?:-?\\d*),\\s?(?:-?\\d*),\\s?(?:-?\\d*),\\s?(?:-?\\d*),\\s?(?:-?\\d*),\\s?(?:-?\\d*),\\s?(?:-?\\d*),\\s?(?:-?\\d*),\\s?(?:-?\\d*),\\s?(-?\\d*),\\s?(-?\\d*)');
const translateMatrixRe = new RegExp('(?:' + translateMatrix2dRe.source + ')|(?:' + translateMatrix3dRe.source + ')');
const pxTtranslateXRe = new RegExp('translate(3d|X)?\\((' + numberReSrc + ')px(?:,\\s?(' + numberReSrc + ')px)?');
const pxTtranslateYRe = new RegExp('translate(3d|Y)?\\((' + numberReSrc + ')px(?:,\\s?(' + numberReSrc + ')px)?');
const whiteSpaceRe2 = /\s+/;
const semicolonRe = /\s*;\s*/;
const colonRe = /\s*:\s*/;
const digitsRe = /^-?((\d+(\.\d*)?)|(\.?\d+))$/;
const elementPropKey = '$bryntum';
const focusabilityCache = {};
const elementCreateExpandos = {
  'elementData': '',
  'for': 'htmlFor',
  'retainElement': ''
};
const elementCreateProperties = {
  'tag': 1,
  'html': 1,
  'text': 1,
  'children': 1,
  'tooltip': 1,
  'style': 1,
  'dataset': 1,
  'parent': 1,
  'nextSibling': 1,
  'ns': 1,
  'reference': 1,
  'class': 1,
  'className': 1,
  'unmatched': 1,
  'onlyChildren': 1,
  'listeners': 1,
  'compareHtml': 1,
  'syncOptions': 1,
  'keepChildren': 1
};
const focusabilities = [
  [
    'f',
    'focusable'
  ],
  [
    'n',
    'natural'
  ],
  [
    'p',
    'programmatic'
  ],
  [
    't',
    'tabbable'
  ]
];
const focusabilityDecode = a => Object.freeze(Object.fromEntries(focusabilities.map(([b, e]) => [
  e,
  a.includes(b)
])));
const styleIgnoreProperties = {
  'length': 1,
  'parentRule': 1,
  'style': 1
};
const nativeEditableTags = {
  'INPUT': 1,
  'TEXTAREA': 1
};
const nativeFocusableTags = {
  'BUTTON': 1,
  'IFRAME': 1,
  'EMBED': 1,
  'INPUT': 1,
  'OBJECT': 1,
  'SELECT': 1,
  'TEXTAREA': 1,
  'BODY': 1
};
const win = globalThis;
const doc = document;
const emptyObject3 = Object.freeze({});
const arraySlice = Array.prototype.slice;
const immediatePromise = Promise.resolve();
const fontProps = [
  'font-size',
  'font-size-adjust',
  'font-style',
  'font-weight',
  'font-family',
  'font-kerning',
  'font-stretch',
  'line-height',
  'text-transform',
  'text-decoration',
  'letter-spacing',
  'word-break'
];
const isHiddenWidget = a => a._hidden;
const mergeChildren = (a, b, c) => {
  if (c.key === 'children') {
    return ObjectHelper.mergeItems(a, b, c);
  }
  return ObjectHelper.blend(a, b, c);
};
const isVisible = (a, b) => {
  if (a.isConnected) {
    const c = a.ownerDocument.defaultView.getComputedStyle(a);
    return c.getPropertyValue('display') !== 'none' && (b || c.getPropertyValue('visibility') !== 'hidden');
  }
};
const hasLayout = a => a && a.isConnected && (a === doc.body || Boolean(a.offsetParent) || (a.ownerDocument.defaultView && ('offsetParent' in a && DomHelper.getStyleValue(a, 'position') !== 'fixed') ? a.offsetParent : hasLayout(a.parentNode)));
const elementOrConfigToElement = a => {
  if (a instanceof Node) {
    return a;
  }
  if (typeof a === 'string') {
    return DH.createElementFromTemplate(a);
  }
  return DH.createElement(a);
};
const canonicalStyles = Object.create(null);
const canonicalizeStyle = (a, b) => {
  const c = canonicalStyles[a] || [
    StringHelper.hyphenate(a),
    b
  ];
  if (!canonicalStyles[a]) {
    canonicalStyles[c[0]] = canonicalStyles[a] = c;
  }
  return c;
};
const slideInAnimationName = /b-slide-in-from-\w+/;
const scrollStyles = {
  'auto': 1,
  'scroll': 1
};
const getParent = a => {
  const b = a.ownerSVGElement ? a.ownerSVGElement.parentNode : a.parentNode || a.target;
  return (b == null ? void 0 : b.nodeType) === Node.DOCUMENT_FRAGMENT_NODE ? b.host : b;
};
const isElement = a => (a == null ? void 0 : a.nodeType) === Node.ELEMENT_NODE;
[
  'top',
  'right',
  'bottom',
  'left',
  'width',
  'height',
  'maxWidth',
  'maxHeight',
  'minWidth',
  'minHeight',
  'borderSpacing',
  'borderWidth',
  'borderTopWidth',
  'borderRightWidth',
  'borderBottomWidth',
  'borderLeftWidth',
  'insetBlockEnd',
  'insetBlockStart',
  'insetInlineEnd',
  'insetInlineStart',
  'marginTop',
  'marginRight',
  'marginBottom',
  'marginLeft',
  'marginInlineStart',
  'marginInlineEnd',
  'paddingTop',
  'paddingRight',
  'paddingBottom',
  'paddingLeft',
  'fontSize',
  'letterSpacing',
  'lineHeight',
  'outlineWidth',
  'textIndent',
  'wordSpacing',
  'insetInlineStart',
  'insetBlockStart',
  'insetInlineEnd',
  'insetBlockEnd'
].forEach(a => canonicalizeStyle(a, true));
let scrollBarWidth = null;
let idCounter = 0;
let themeInfo = null;
let templateElement;
let htmlParser;
let scrollBarMeasureElement;
const _DomHelper = class _DomHelper {
  static $name = 'DomHelper';
  static async slideIn(a, b = 1) {
    const c = 'b-slide-in-' + (b > 0 ? 'next' : 'previous'), {
        classList: e,
        parentNode: {classList: f}
      } = a;
    f.add('.b-sliding-child-element');
    e.add(c);
    await EventHelper.waitForTransitionEnd({
      'element': a,
      'animationName': slideInAnimationName
    });
    e.remove(c);
    f.remove('.b-sliding-child-element');
  }
  static getFocusability(a) {
    let b = '';
    if (a) {
      const {nodeName: c} = a, e = a.getAttribute('tabIndex');
      b = nativeFocusableTags[c] || (c === 'A' || c === 'LINK') && a.href || a.contentEditable === 'true' ? 'n' : '';
      if (e === '-1') {
        b += 'fp';
      } else if (b || e != null) {
        b += 'ft';
      }
    }
    return focusabilityCache[b] || (focusabilityCache[b] = focusabilityDecode(b));
  }
  static isFocusable(a, b) {
    if (a && !b) {
      if (!DH.isVisible(a) || DH.Widget.fromElement(a, isHiddenWidget)) {
        return false;
      }
    }
    return DH.getFocusability(a).focusable;
  }
  static isEditable(a) {
    return Boolean((a.isContentEditable || nativeEditableTags[a.nodeName]) && !(a.readOnly || a.disabled || a.closest('[inert]')));
  }
  static isInView(a, b = false, c) {
    if (!hasLayout(isElement(a.target) ? a.target : a)) {
      return false;
    }
    const e = (c == null ? void 0 : c.positioned) && _DomHelper.getStyleValue(c.element, 'position') !== 'fixed', f = Rectangle.from(globalThis), g = b ? 'contains' : 'intersect', i = e && c.element.offsetParent, j = e && Rectangle.from(i);
    f.height = doc.scrollingElement.scrollHeight;
    if (a === doc.body) {
      return f;
    }
    const k = this.getViewportIntersection(a, f, g);
    if (k && e) {
      k.translate(doc.scrollingElement.scrollLeft, doc.scrollingElement.scrollTop);
    }
    return e && k ? k.translate(-j.x + i.scrollLeft, -j.y + i.scrollTop) : k;
  }
  static getViewportIntersection(a, b, c) {
    let e;
    const f = isElement(a.target) ? a.target : null, g = _DomHelper.getVisibleViewport(getParent(a), b, f || a), i = Rectangle.from(f || a, null, true);
    if (f) {
      i.translate(a.offsetX + 1, a.offsetY + 1);
      i.height = i.width = 0;
    }
    return (e = g == null ? void 0 : g[c]) == null ? void 0 : e.call(g, i, false, true);
  }
  static getVisibleViewport(a, b, c) {
    const e = [];
    let f = b;
    for (let g = a; g && g !== doc.body; g = getParent(g)) {
      const j = g.ownerDocument.defaultView.getComputedStyle(g);
      if ((scrollStyles[j.overflowX] || scrollStyles[j.overflowY]) && j.display !== 'contents') {
        e.unshift(Rectangle.client(g, null, true, true, c));
      }
    }
    for (let k = 0, {length: l} = e; f && k < l; k++) {
      f = f.intersect(e[k]);
    }
    return f;
  }
  static isVisible(a) {
    let b;
    const c = a.ownerDocument;
    let e = false;
    for (; a; a = getParent(a)) {
      const f = (b = a.ownerDocument) == null ? void 0 : b.defaultView.getComputedStyle(a);
      e = e || f.getPropertyValue('visibility') === 'visible';
      if (a === c) {
        return true;
      }
      if (isElement(a) && !isVisible(a, e)) {
        return false;
      }
    }
    return false;
  }
  static isDOMEvent(a) {
    return a instanceof Event;
  }
  static merge(a, ...b) {
    return ObjectHelper.blend(a, b, { 'merge': mergeChildren });
  }
  static normalizeChildren(a, b, c) {
    let e;
    let f = a == null ? void 0 : a.children, g, j, k, l, n;
    if ((e = a == null ? void 0 : a.syncOptions) == null ? void 0 : e.ignoreRefs) {
      c = true;
    }
    if (f && !(a instanceof Node)) {
      if (Array.isArray(f)) {
        for (j = 0; j < f.length; ++j) {
          DH.normalizeChildren(f[j], b, c);
        }
      } else {
        l = f;
        a.children = f = [];
        for (k in l) {
          g = l[k];
          if (g == null ? void 0 : g.isWidget) {
            g = g.element;
          }
          n = !k.startsWith('$') && !DH.isElement(g);
          n && (b == null ? void 0 : b(k, !c));
          if (g) {
            if (!(g instanceof Node)) {
              if (g.reference === false) {
                delete g.reference;
              } else if (n && typeof g !== 'string') {
                g.reference = k;
              }
              DH.normalizeChildren(g, b, c);
            }
            f.push(g);
          }
        }
      }
    }
    return a;
  }
  static roundPx(a, b = globalThis.devicePixelRatio || 1) {
    const c = 1 / b;
    return Math.round(a / c) * c;
  }
  static floorPx(a, b = globalThis.devicePixelRatio || 1) {
    const c = 1 / b;
    return Math.floor(a * c) / c;
  }
  static isCustomElement(a) {
    return Boolean(a == null ? void 0 : a.shadowRoot);
  }
  static elementFromPoint(a, b) {
    let c = document.elementFromPoint(a, b);
    if (DH.isCustomElement(c)) {
      c = c.shadowRoot.elementFromPoint(a, b) || c;
    }
    return c;
  }
  static childFromPoint(a, b, c, e = a) {
    const f = c == null ? b : new Rectangle(b, c, 0, 0);
    let g = null;
    Array.from(a.children).reverse().some(i => {
      if (Rectangle.from(i, e).contains(f)) {
        g = i.children.length && DH.childFromPoint(i, f, null, e) || i;
        return true;
      }
    });
    return g;
  }
  static unitize(a, b, c = 'px') {
    const [e, f] = canonicalizeStyle(a);
    if (b != null) {
      b = String(b);
      b = f && digitsRe.test(b) ? b + c : b;
    }
    return [
      e,
      b
    ];
  }
  static get activeElement() {
    let a = document.activeElement;
    while (a.shadowRoot) {
      a = a.shadowRoot.activeElement;
    }
    return a;
  }
  static getActiveElement(a) {
    if (a == null ? void 0 : a.isWidget) {
      a = a.element;
    }
    let b = ((a == null ? void 0 : a.getRootNode()) || document).activeElement;
    while (b == null ? void 0 : b.shadowRoot) {
      b = b.shadowRoot.activeElement;
    }
    return b;
  }
  static getRootElement(a) {
    let b;
    const c = (b = a.getRootNode) == null ? void 0 : b.call(a), {nodeType: e} = c;
    return e === Node.DOCUMENT_NODE ? c.body : e === Node.DOCUMENT_FRAGMENT_NODE ? c : a.ownerDocument.contains(a) ? a.ownerDocument.body : null;
  }
  static getOutermostElement(a) {
    let b;
    const c = (b = a.getRootNode) == null ? void 0 : b.call(a);
    if (c == null ? void 0 : c.body) {
      return c == null ? void 0 : c.body;
    }
    while (a.parentNode !== c && a.parentNode) {
      a = a.parentNode;
    }
    return a;
  }
  static isValidFloatRootParent(a) {
    let b;
    return ((b = a.tagName) == null ? void 0 : b.toUpperCase()) === 'BODY' || a.getRootNode().nodeType === Node.DOCUMENT_FRAGMENT_NODE;
  }
  static getId(a) {
    return a.id || (a.id = 'b-element-' + ++idCounter);
  }
  static getCommonAncestor(a, b) {
    let c, e;
    if (a === b) {
      return a;
    }
    while (a && !(((c = a[a.isWidget ? 'owns' : 'contains']) == null ? void 0 : c.call(a, b)) || a === b)) {
      a = ((e = a.owner) == null ? void 0 : e.isWidget) && a.owner !== a ? a.owner : a.parentNode;
    }
    return a;
  }
  static getElement(a) {
    if (typeof a === 'string') {
      return doc.querySelector(a);
    }
    return a;
  }
  static setAttributes(a, b) {
    const c = DH.getElement(a);
    if (c && b) {
      for (const e in b) {
        if (b[e] == null) {
          c.removeAttribute(e);
        } else {
          c.setAttribute(e, b[e]);
        }
      }
    }
  }
  static addAttributeValue(a, b, c) {
    let e;
    const f = DH.getElement(a);
    if (f && c) {
      f.setAttribute(b, [...new Set((e = f.getAttribute(b)) == null ? void 0 : e.split(' ')).add(c)].join(' '));
    }
  }
  static removeAttributeValue(a, b, c) {
    let e;
    const f = DH.getElement(a);
    if (f && c) {
      const g = new Set((e = f.getAttribute(b)) == null ? void 0 : e.split(' '));
      g.delete(c);
      g.size ? f.setAttribute(b, [...g].join(' ')) : f.removeAttribute(b);
    }
  }
  static setLength(a, b, c) {
    if (arguments.length === 1) {
      c = typeof a === 'number' ? a + 'px' : a != null ? a : '';
    } else {
      a = DH.getElement(a);
      c = a.style[b] = typeof c === 'number' ? c + 'px' : c != null ? c : '';
    }
    return c;
  }
  static percentify(a, b = 2) {
    const c = Math.pow(10, b);
    return a == null || a === '' || isNaN(a) ? '' : Math.round(a * c) / c + '%';
  }
  static getChild(a, b) {
    return a.querySelector(':scope>' + b);
  }
  static hasChild(a, b) {
    return DH.getChild(a, b) != null;
  }
  static children(a, b) {
    if (b[0] === '>' || b.startsWith('# ')) {
      if (b[0] === '#') {
        b = b.substr(2);
      }
      b = ':scope ' + b;
    }
    return Array.from(a.querySelectorAll(b));
  }
  static getChildElementCount(a) {
    return a.childElementCount;
  }
  static down(a, b) {
    let c;
    if (!a) {
      return null;
    }
    if ((c = a.matches) == null ? void 0 : c.call(a, b)) {
      return a;
    }
    b = ':scope ' + b;
    return a.querySelector(b);
  }
  static isDescendant(a, b) {
    const c = DH.getRootElement(a), e = DH.getRootElement(b);
    if (e && c !== e && e.host) {
      return DH.isDescendant(c, e.host);
    }
    return a.contains(b);
  }
  static getEventElement(a, b = 'target') {
    return !a || DH.isElement(a) ? a : a[b];
  }
  static isElement(a) {
    return (a == null ? void 0 : a.nodeType) === document.ELEMENT_NODE && DH.isNode(a);
  }
  static isReactElement(a) {
    return (a == null ? void 0 : a.$$typeof) === Symbol.for('react.element');
  }
  static isNode(a) {
    return Boolean(a) && typeof a.nodeType === 'number' && !isObject2(a);
  }
  static forEachSelector(a, b, c) {
    if (typeof a === 'string') {
      throw new Error('DomHelper.forEachSelector must provide a root element context (for shadow root scenario)');
    }
    DH.children(a, b).forEach(c);
  }
  static forEachChild(a, b) {
    Array.from(a.children).forEach(b);
  }
  static removeEachSelector(a, b) {
    DH.forEachSelector(a, b, c => c.remove());
  }
  static removeClsGlobally(a, ...b) {
    b.forEach(c => DH.forEachSelector(a, '.' + c, e => e.classList.remove(c)));
  }
  static getAncestor(a, b, c = null) {
    let e = false, f, g = a;
    b = ArrayHelper.asArray(b);
    while (g = g.parentElement) {
      if (b.includes(g)) {
        e = true;
        break;
      }
      if (c && g === c)
        break;
      f = g;
    }
    if (!e)
      return null;
    return f || a;
  }
  static getParents(a) {
    const b = [];
    while (a.parentElement) {
      b.push(a.parentElement);
      a = a.parentElement;
    }
    return b;
  }
  static makeValidId(a, b = '') {
    return StringHelper.makeValidDomId(a, b);
  }
  static createElement(a = {}, b) {
    let c, e;
    let f = b, g, j, k, l, n, o, p, q, r, u, v;
    if (typeof f === 'boolean') {
      throw new Error('Clean up');
    } else if (b) {
      n = b.ignoreRefs;
      r = b.refOwner;
      u = b.refs;
      f = b.returnAll;
      v = b.syncIdField;
      if (n) {
        k = true;
        n = n !== 'children';
      }
    }
    if (typeof a.parent === 'string') {
      a.parent = document.getElementById(a.parent);
    }
    const w = a.parent || a.nextSibling && a.nextSibling.parentNode, {
        dataset: z,
        html: A,
        reference: B,
        syncOptions: C,
        text: E
      } = a;
    if (C) {
      v = C.syncIdField || v;
      l = C.ignoreRefs;
      if (l) {
        k = true;
        n = l !== 'children';
        b = {
          ...b,
          'ignoreRefs': true
        };
      }
    }
    if (n) {
      r = null;
    }
    if (a.ns) {
      g = doc.createElementNS(a.ns, a.tag || 'svg');
    } else {
      g = doc.createElement(a.tag || 'div');
    }
    if (E != null) {
      DH.setInnerText(g, E);
    } else if (A != null) {
      if (A instanceof DocumentFragment) {
        g.appendChild(A);
      } else {
        g.innerHTML = A;
      }
    }
    if (a.tooltip) {
      DH.Widget.attachTooltip(g, a.tooltip);
    }
    if (a.style) {
      DH.applyStyle(g, a.style);
    }
    if (z) {
      for (p in z) {
        q = z[p];
        if (q != null) {
          g.dataset[p] = q;
        }
      }
    }
    if (w) {
      this.addChild(w, g, a.nextSibling);
    }
    if (r) {
      g.$refOwnerId = r.id;
    }
    if (B && !n) {
      if (r) {
        g.$reference = B;
        r.attachRef(B, g, a);
      } else {
        if (!u) {
          b = Object.assign({}, b);
          b.refs = u = {};
        }
        u[B] = g;
        g.setAttribute('data-reference', B);
      }
    }
    const F = a.className || a.class, G = Object.keys(a);
    if (F) {
      g.setAttribute('class', DomClassList.normalize(F));
    }
    for (j = 0; j < G.length; ++j) {
      p = G[j];
      q = a[p];
      if ((o = elementCreateExpandos[p]) != null) {
        g[o || p] = q;
      } else if (!elementCreateProperties[p] && p && q != null) {
        g.setAttribute(p, q);
      }
    }
    if (!a['aria-hidden'] && !a.role && !a.tabIndex && !_DomHelper.isFocusable(g, true) && !g.htmlFor) {
      g.setAttribute('role', 'presentation');
    }
    (e = b == null ? void 0 : b.callback) == null ? void 0 : e.call(b, {
      'action': 'newElement',
      'domConfig': a,
      'targetElement': g,
      'syncId': r ? B : b.syncIdField && ((c = a.dataset) == null ? void 0 : c[b.syncIdField])
    });
    if (f === true) {
      b.returnAll = f = [g];
    } else if (Array.isArray(f)) {
      f.push(g);
    }
    if (a.children) {
      if (v) {
        g.syncIdMap = {};
      }
      ArrayHelper.asArray(a.children).forEach(I => {
        let J, K, L, N;
        if (I) {
          if (typeof I === 'string') {
            const O = document.createTextNode(I);
            if (r) {
              O.$refOwnerId = r.id;
            }
            g.appendChild(O);
          } else if (this.isReactElement(I)) {
            (J = b == null ? void 0 : b.reactCallback) == null ? void 0 : J.call(b, {
              'jsx': I,
              'domConfig': a,
              'targetElement': g
            });
          } else if (isNaN(I.nodeType)) {
            I.parent = g;
            if (!I.ns && a.ns) {
              I.ns = a.ns;
            }
            const P = DH.createElement(I, {
                ...b,
                'ignoreRefs': (L = (K = a.syncOptions) == null ? void 0 : K.ignoreRef) != null ? L : k
              }), Q = (N = I.dataset) == null ? void 0 : N[v];
            if (Q != null) {
              g.syncIdMap[Q] = P;
            }
            delete I.parent;
          } else {
            g.appendChild(I);
          }
        }
      });
    }
    g.lastDomConfig = a;
    const H = u || f || g;
    if (a.replaceElement) {
      a.replaceElement.replaceWith(H);
    }
    return H;
  }
  static createElementFromTemplate(a, b = emptyObject3) {
    const {
      array: c,
      raw: e,
      fragment: f
    } = b;
    let g;
    if (DH.supportsTemplate) {
      (templateElement || (templateElement = doc.createElement('template'))).innerHTML = a;
      g = templateElement.content;
      if (f) {
        return g.cloneNode(true);
      }
    } else {
      g = (htmlParser || (htmlParser = new DOMParser())).parseFromString(a, 'text/html').body;
      if (f) {
        const i = g.childNodes;
        g = document.createDocumentFragment();
        while (i.length) {
          g.appendChild(i[0]);
        }
        return g;
      }
    }
    if (e) {
      g = g.childNodes;
    } else {
      g = g.children;
    }
    return g.length === 1 && !c ? g[0] : arraySlice.call(g);
  }
  static triggerMouseEvent(a, b = 'contextmenu') {
    const c = this.isInView(a), e = c || Rectangle.from(a), f = e.center, g = new MouseEvent(b, {
        'clientX': f.x,
        'clientY': f.y,
        'offsetX': c.width / 2,
        'offsetY': c.height / 2,
        'bubbles': true
      });
    a.dispatchEvent(g);
  }
  static insertFirst(a, b) {
    if (b && b.nodeType !== ELEMENT_NODE && b.tag) {
      b = DH.createElement(b);
    }
    return a.insertBefore(b, a.firstElementChild);
  }
  static insertBefore(a, b, c) {
    if (b && b.nodeType !== ELEMENT_NODE && b.tag) {
      b = DH.createElement(b);
    }
    return c ? a.insertBefore(b, c) : DH.insertFirst(a, b);
  }
  static insertAt(a, b, c) {
    const e = Array.from(a.children);
    if (c >= e.length) {
      return DH.append(a, b);
    }
    const f = e[c];
    return DH.insertBefore(a, b, f);
  }
  static append(a, b) {
    if (b.forEach) {
      if (Array.isArray(b)) {
        b = b.map(c => elementOrConfigToElement(c));
      }
      if (a.append) {
        a.append(...b);
      } else {
        const c = document.createDocumentFragment();
        b.forEach(function (e) {
          c.appendChild(e);
        });
        a.appendChild(c);
      }
      return b;
    } else {
      return a.appendChild(elementOrConfigToElement(b));
    }
  }
  static getTranslateX(a) {
    const b = a.style.transform;
    let c = pxTtranslateXRe.exec(b);
    if (c) {
      return parseFloat(c[2]);
    } else {
      c = translateMatrixRe.exec(b) || translateMatrixRe.exec(DH.getStyleValue(a, 'transform'));
      return c ? parseFloat(c[1] || c[3]) : 0;
    }
  }
  static getTranslateY(a) {
    const b = a.style.transform;
    let c = pxTtranslateYRe.exec(b);
    if (c) {
      const e = parseFloat(c[c[1] === 'Y' ? 2 : 3]);
      return isNaN(e) ? 0 : e;
    } else {
      c = translateMatrixRe.exec(b) || translateMatrixRe.exec(DH.getStyleValue(a, 'transform'));
      return c ? parseFloat(c[2] || c[4]) : 0;
    }
  }
  static getTranslateXY(a) {
    return [
      DH.getTranslateX(a),
      DH.getTranslateY(a)
    ];
  }
  static getOffsetX(a, b = null) {
    return b ? a.getBoundingClientRect().left - b.getBoundingClientRect().left : a.offsetLeft;
  }
  static getOffsetY(a, b = null) {
    return b ? a.getBoundingClientRect().top - b.getBoundingClientRect().top : a.offsetTop;
  }
  static getOffsetXY(a, b = null) {
    return [
      DH.getOffsetX(a, b),
      DH.getOffsetY(a, b)
    ];
  }
  static getPageX(a) {
    return a.getBoundingClientRect().left + win.pageXOffset;
  }
  static getPageY(a) {
    return a.getBoundingClientRect().top + win.pageYOffset;
  }
  static getExtremalSizePX(a, b) {
    const c = StringHelper.hyphenate(b), e = c.split('-')[1];
    let f = DH.getStyleValue(a, c);
    if (/%/.test(f)) {
      if (a.parentElement) {
        f = parseInt(DH.getStyleValue(a.parentElement, e), 10);
      } else {
        f = NaN;
      }
    } else {
      f = parseInt(f, 10);
    }
    return f;
  }
  static setScale(a, b, c = b) {
    const e = DH.getStyleValue(a, 'transform').split(/,\s*/);
    if (e.length > 1) {
      if (e[0].startsWith('matrix3d')) {
        e[0] = 'matrix3d(' + b;
        e[5] = c;
      } else {
        e[0] = 'matrix(' + b;
        e[3] = c;
      }
      a.style.transform = e.join(',');
    } else {
      a.style.transform = 'scale(' + b + ', ' + c + ')';
    }
  }
  static setTranslateX(a, b) {
    const c = DH.getStyleValue(a, 'transform').split(/,\s*/);
    b = DH.roundPx(b);
    if (c.length > 1) {
      c[c[0].startsWith('matrix3d') ? 12 : 4] = b;
      a.style.transform = c.join(',');
    } else {
      a.style.transform = 'translateX(' + b + 'px)';
    }
  }
  static setTranslateY(a, b) {
    const c = DH.getStyleValue(a, 'transform').split(/,\s*/);
    b = DH.roundPx(b);
    if (c.length > 1) {
      c[c[0].startsWith('matrix3d') ? 13 : 5] = b;
      a.style.transform = c.join(',') + ')';
    } else {
      a.style.transform = 'translateY(' + b + 'px)';
    }
  }
  static setTop(a, b) {
    DH.setLength(a, 'top', b);
  }
  static setLeft(a, b) {
    DH.setLength(a, 'left', b);
  }
  static setTopLeft(a, b, c) {
    DH.setLength(a, 'top', b);
    DH.setLength(a, 'left', c);
  }
  static setTopInsetInlineStart(a, b, c) {
    DH.setLength(a, 'top', b);
    DH.setLength(a, 'inset-inline-start', c);
  }
  static setRect(a, {
    x: b,
    y: c,
    width: e,
    height: f
  }) {
    DH.setTopInsetInlineStart(a, c, b);
    DH.setLength(a, 'width', e);
    DH.setLength(a, 'height', f);
  }
  static setTranslateXY(a, b, c) {
    if (b == null) {
      return DH.setTranslateY(a, c);
    }
    if (c == null) {
      return DH.setTranslateX(a, b);
    }
    b = DH.roundPx(b);
    c = DH.roundPx(c);
    const e = DH.getStyleValue(a, 'transform').split(/,\s*/), f = e[0].startsWith('matrix3d');
    if (e.length > 1) {
      e[f ? 12 : 4] = b;
      e[f ? 13 : 5] = c;
      a.style.transform = e.join(',') + ')';
    } else {
      a.style.transform = 'translate(' + b + 'px, ' + c + 'px)';
    }
  }
  static addTranslateX(a, b) {
    DH.setTranslateX(a, DH.getTranslateX(a) + b);
  }
  static addTranslateY(a, b) {
    DH.setTranslateY(a, DH.getTranslateY(a) + b);
  }
  static addLeft(a, b) {
    DH.setLeft(a, DH.getOffsetX(a) + b);
  }
  static addTop(a, b) {
    DH.setTop(a, DH.getOffsetY(a) + b);
  }
  static alignTo(a, b, c = t0t0, e) {
    b = b instanceof Rectangle ? b : Rectangle.from(b, true);
    const f = a.style.top, g = f ? [
        parseInt(a.style.insetInlineStart || 0, 10),
        parseInt(a.style.top || 0, 10)
      ] : DH.getTranslateXY(a), i = Rectangle.from(a, true);
    if (e) {
      i.roundPx();
      b.roundPx();
    }
    const j = i.alignTo(Object.assign(c, { 'target': b }));
    if (f) {
      DH.setTopInsetInlineStart(a, g[1] + j.y - i.y, g[0] + j.x - i.x);
    } else {
      DH.setTranslateXY(a, g[0] + j.x - i.x, g[1] + j.y - i.y);
    }
  }
  static getStyleValue(a, b, c, e) {
    const f = c ? a.style : a.ownerDocument.defaultView.getComputedStyle(a, e);
    if (Array.isArray(b)) {
      const g = {};
      for (const i of b) {
        g[i] = f.getPropertyValue(StringHelper.hyphenate(i));
      }
      return g;
    }
    return f.getPropertyValue(StringHelper.hyphenate(b));
  }
  static getEdgeSize(a, b, c) {
    const e = b === 'border' ? '-width' : '', f = { 'raw': {} };
    for (const g of [
        'top',
        'right',
        'bottom',
        'left'
      ]) {
      if (!c || c.includes(g[0])) {
        f[g] = parseFloat(f.raw[g] = DH.getStyleValue(a, b + '-' + g + e));
      }
    }
    f.width = (f.left || 0) + (f.right || 0);
    f.height = (f.top || 0) + (f.bottom || 0);
    return f;
  }
  static parseStyle(a) {
    if (typeof a === 'string') {
      const b = a.split(semicolonRe);
      a = {};
      for (let c = 0, {length: e} = b; c < e; c++) {
        const f = b[c].split(colonRe);
        a[f[0]] = f[1];
      }
    }
    return a || {};
  }
  static applyStyle(a, b, c = false) {
    if (typeof b === 'string') {
      if (c) {
        if (b.length || a.style.cssText.length) {
          a.style.cssText = b;
        }
      } else {
        a.style.cssText += b;
      }
    } else if (b) {
      if (c) {
        a.style.cssText = '';
      }
      if (b.style && typeof b.style !== 'string') {
        b = ObjectHelper.assign({}, b, b.style);
      }
      let e, f;
      for (e in b) {
        if (!styleIgnoreProperties[e]) {
          [e, f] = DH.unitize(e, b[e]);
          if (f == null) {
            a.style.removeProperty(e);
          } else {
            a.style.setProperty(e, f);
          }
        }
      }
      if (typeof b.style === 'string') {
        a.style.cssText += b.style;
      }
    }
  }
  static getCSSText(a) {
    if (typeof a === 'string') {
      return a;
    }
    let b = '';
    for (const c in a) {
      if (!styleIgnoreProperties[c]) {
        b += StringHelper.hyphenate(c) + ':' + a[c] + ';';
      }
    }
    return b;
  }
  static assignClasses(a, b) {
    ObjectHelper.forEach(b, (c, e) => a.classList.toggle(e, Boolean(c)));
  }
  static toggleClasses(a, b, c = null) {
    b = ArrayHelper.asArray(b);
    if (c === true) {
      a.classList.add(...b);
    } else if (c === false) {
      a.classList.remove(...b);
    } else {
      b.forEach(e => a.classList.toggle(e));
    }
  }
  static addTemporaryClass(a, b, c, e = globalThis, f = b) {
    if (c > 0) {
      a.classList.add(b);
      e.setTimeout({
        'fn': g => a.classList.remove(g),
        'delay': c,
        'name': f,
        'args': [b],
        'cancelOutstanding': true
      });
    }
  }
  static getPropertyTransitionDuration(a, b) {
    const c = globalThis.getComputedStyle(a), e = c.transitionProperty.split(', '), f = c.transitionDuration.split(', '), g = e.indexOf(StringHelper.hyphenate(b));
    let i;
    if (g !== -1) {
      i = parseFloat(f[g]) * 1000;
    }
    return i;
  }
  static getAnimationDuration(a) {
    return parseFloat(DH.getStyleValue(a, 'animation-duration')) * 1000;
  }
  static highlight(a, b = globalThis) {
    if (a instanceof Rectangle) {
      return a.highlight();
    }
    return new Promise(c => {
      b.setTimeout(() => {
        a.classList.add('b-fx-highlight');
        b.setTimeout(() => {
          a.classList.remove('b-fx-highlight');
          c();
        }, 1000);
      }, 0);
    });
  }
  static get scrollBarWidth() {
    if (scrollBarWidth === null) {
      const a = scrollBarMeasureElement || (scrollBarMeasureElement = DH.createElement({
        'parent': doc.documentElement,
        'className': 'b-scrollbar-measure-element',
        'style': 'position:absolute;top:-9999em;height:100px;overflow-y:scroll'
      }));
      if (a.parentNode !== doc.documentElement) {
        doc.documentElement.appendChild(a);
      }
      scrollBarWidth = a.offsetWidth;
    }
    return scrollBarWidth;
  }
  static get scrollBarPadElement() {
    return {
      'className': 'b-yscroll-pad',
      'children': [{ 'className': 'b-yscroll-pad-sizer' }]
    };
  }
  static get scrollLimit() {
    const a = _DomHelper.createElement({
        'parent': document.body,
        'style': 'position:absolute; left:-999px; top:0; width:5px; height:5px',
        'children': [{ 'style': 'position:absolute; width:1px; height:1px' }]
      }), b = a.firstElementChild;
    let c = 0, e = 20000000;
    while (e > 1000000) {
      b.style.transform = 'translateY(' + e + 'px)';
      a.style.lineHeight = '' + (c = 1 - c);
      if (a.scrollHeight >= e) {
        break;
      }
      e -= 1000000;
    }
    Object.defineProperty(_DomHelper, 'scrollLimit', { 'value': e });
    a.remove();
    return e;
  }
  static resetScrollBarWidth() {
    scrollBarWidth = null;
  }
  static measureText(a, b, c = false, e = void 0) {
    const f = DH.getMeasureElement(b, e);
    f[c ? 'innerHTML' : 'innerText'] = a;
    const g = f.clientWidth;
    f.className = '';
    return g;
  }
  static measureSize(a, b, c = true) {
    let e;
    if (!a) {
      return 0;
    }
    if (typeof a === 'number') {
      return a;
    }
    if (!a.length) {
      return 0;
    }
    if (/^\d+(px)?$/.test(a)) {
      return parseInt(a);
    }
    if (b) {
      const f = (e = a.includes) == null ? void 0 : e.call(a, '%'), g = DH.getMeasureElement(b, f ? b.parentNode : void 0), {parentElement: i} = g;
      g.innerHTML = '';
      f && (i.style.display = 'contents');
      g.style.minWidth = g.style.maxWidth = DH.setLength(a);
      const j = c ? g.offsetWidth : g.getBoundingClientRect().width;
      i.style.display = '';
      g.style.minWidth = g.style.maxWidth = g.className = '';
      return j;
    }
    if (/^\d+em$/.test(a)) {
      return parseInt(a) * DEFAULT_FONT_SIZE;
    }
    return isNaN(a) ? 0 : parseInt(a);
  }
  static getMeasureElement(a, b) {
    b = b || doc.body;
    const c = win.getComputedStyle(a), e = b.offScreenDiv = b.offScreenDiv || DH.createElement({
        'parent': b,
        'style': 'position:fixed;top:-10000px;left:-10000px;visibility:hidden;contain:strict',
        'className': 'b-measure-element',
        'children': [{ 'style': 'white-space:nowrap;display:inline-block;will-change:contents;width:auto;contain:none' }]
      }, { 'returnAll': true })[1];
    fontProps.forEach(f => {
      if (e.style[f] !== c[f]) {
        e.style[f] = c[f];
      }
    });
    e.className = a.className;
    if (e.parentElement.parentElement !== b) {
      b.appendChild(e.parentElement);
    }
    return e;
  }
  static stripTags(a) {
    const b = DH.$domParser || (DH.$domParser = new DOMParser()), c = b.parseFromString(a, 'text/html');
    return c.body.textContent;
  }
  static sync(a, b) {
    if (typeof a === 'string') {
      if (a === '') {
        b.innerHTML = '';
        return;
      } else {
        a = DH.createElementFromTemplate(a);
      }
    }
    DH.performSync(a, b);
    return b;
  }
  static performSync(a, b) {
    if (a.outerHTML !== b.outerHTML) {
      DH.syncAttributes(a, b);
      DH.syncContent(a, b);
      DH.syncChildren(a, b);
      return true;
    }
    return false;
  }
  static getSyncAttributes(a) {
    const b = {}, c = [];
    for (let e = 0; e < a.attributes.length; e++) {
      const f = a.attributes[e];
      if (f.specified) {
        const g = f.name.toLowerCase();
        b[g] = f.value;
        c.push(g);
      }
    }
    return {
      'attributes': b,
      'names': c
    };
  }
  static syncAttributes(a, b) {
    const {
        attributes: c,
        names: e
      } = DH.getSyncAttributes(a), {
        attributes: f,
        names: g
      } = DH.getSyncAttributes(b), j = e.includes('dataset'), k = e.filter(o => !g.includes(o)), l = g.filter(o => !e.includes(o) && (!j || !o.startsWith('data-'))), n = e.filter(o => g.includes(o));
    if (k.length > 0) {
      for (let o = 0; o < k.length; o++) {
        const p = k[o];
        if (p === 'style') {
          DH.applyStyle(b, c.style, true);
        } else if (p === 'dataset') {
          Object.assign(b.dataset, c.dataset);
        } else {
          b.setAttribute(p, c[p]);
        }
      }
    }
    if (l.length > 0) {
      for (let q = 0; q < l.length; q++) {
        b.removeAttribute(l[q]);
      }
    }
    if (n.length > 0) {
      for (let r = 0; r < n.length; r++) {
        const u = n[r];
        if (u === 'style') {
          DH.applyStyle(b, c.style, true);
        } else if (u === 'dataset') {
          Object.assign(b.dataset, c.dataset);
        } else if (u === 'class' && (c.class.isDomClassList || typeof c.class === 'object')) {
          let v;
          if (c.class.isDomClassList) {
            v = c.class;
          } else {
            v = new DomClassList(c.class);
          }
          if (!v.isEqual(f.class)) {
            b.setAttribute('class', v);
          }
        } else if (f[u] !== c[u]) {
          b.setAttribute(u, c[u]);
        }
      }
    }
  }
  static syncContent(a, b) {
    if (DH.getChildElementCount(a) === 0) {
      b.innerText = a.innerText;
    }
  }
  static setInnerText(a, b) {
    const {firstChild: c} = a;
    if ((c == null ? void 0 : c.nodeType) === Element.TEXT_NODE) {
      c.data = b;
    } else {
      a.textContent = b;
    }
  }
  static syncChildren(a, b) {
    const c = this, e = arraySlice.call(a.childNodes), f = arraySlice.call(b.childNodes);
    while (e.length) {
      const g = e.shift(), i = f.shift();
      if (g && g.nodeType !== TEXT_NODE && g.nodeType !== ELEMENT_NODE) {
        throw new Error('Source node type ' + g.nodeType + ' not supported by DomHelper.sync()');
      }
      if (i && i.nodeType !== TEXT_NODE && i.nodeType !== ELEMENT_NODE) {
        throw new Error('Target node type ' + i.nodeType + ' not supported by DomHelper.sync()');
      }
      if (!i) {
        b.appendChild(g);
      } else {
        if (g.nodeType === i.nodeType) {
          if (g.nodeType === TEXT_NODE) {
            i.data = g.data;
          } else {
            if (g.tagName === i.tagName) {
              c.performSync(g, i);
            } else {
              b.insertBefore(g, i);
              i.remove();
            }
          }
        } else if (g.nodeType === TEXT_NODE && i.nodeType === ELEMENT_NODE) {
          b.innerText = g.data.trim();
        } else {
          const j = g.parentElement || g;
          throw new Error('Currently no support for transforming nodeType.\n' + j.outerHTML);
        }
      }
    }
    f.forEach(k => {
      k.remove();
    });
  }
  static syncClassList(a, b) {
    const {classList: c} = a, e = typeof b === 'string', f = e ? b.split(whiteSpaceRe2) : DomClassList.normalize(b, 'array'), g = f.length;
    let j = c.length !== g, k;
    for (k = 0; !j && k < g; k++) {
      j = !c.contains(f[k]);
    }
    if (j) {
      a.className = e ? b : f.join(' ');
    }
    return j;
  }
  static updateClassList(a, b) {
    const {classList: c} = a;
    let e, f, g = false;
    for (e in b) {
      f = Boolean(b[e]);
      if (c.contains(e) !== f) {
        c[f ? 'add' : 'remove'](e);
        g = true;
      }
    }
    return g;
  }
  static setTheme(a, b) {
    a = a.toLowerCase();
    const {head: c} = document, e = DH.getThemeInfo(b).name.toLowerCase();
    let f = c.querySelectorAll('[data-bryntum-theme]:not([data-loading])'), g = 0;
    if (e === a) {
      return immediatePromise;
    }
    DH.removeEachSelector(c, '#bryntum-theme[data-loading],link[data-bryntum-theme][data-loading]');
    const i = {
      'theme': a,
      'prev': e
    };
    function j(k, l, n) {
      const o = _DomHelper.createElement({
        'tag': 'link',
        'rel': 'stylesheet',
        'dataset': {
          'loading': true,
          'bryntumTheme': true
        },
        'href': k.href.replace(e, a),
        'nextSibling': k
      });
      o.addEventListener('load', () => {
        delete o.dataset.loading;
        themeInfo = null;
        if (++g === f.length) {
          f.forEach(p => p.remove());
          GlobalEvents_default.trigger('theme', i);
          l(i);
        }
      });
      o.addEventListener('error', p => {
        delete o.dataset.loading;
        n(p);
      });
    }
    if (f.length) {
      return new Promise((k, l) => {
        f.forEach((n, o) => {
          j(n, k, l, o === f.length - 1);
        });
      });
    } else {
      const k = c.querySelector('#bryntum-theme:not([data-loading])') || c.querySelector('[href*="' + e + '.css"]:not([data-loading])');
      if (!(k == null ? void 0 : k.href.includes(e + '.css'))) {
        throw new Error('Theme link for ' + e + ' not found');
      }
      f = [k];
      return new Promise((l, n) => j(k, l, n));
    }
  }
  static get themeInfo() {
    return _DomHelper.getThemeInfo();
  }
  static getThemeInfo(a) {
    if (!themeInfo) {
      const b = DH.createElement({
          'parent': document.body,
          'className': 'b-theme-info'
        }), c = DH.getStyleValue(b, 'content', false, ':before');
      if (c) {
        try {
          themeInfo = JSON.parse(c.replace(/^["']|["']$|\\/g, ''));
        } catch (f) {
          themeInfo = null;
        }
      }
      themeInfo = themeInfo || (a ? { 'name': a } : null);
      b.remove();
    }
    return themeInfo;
  }
  static async transition({
    element: a,
    selector: selector = '[data-dom-transition]',
    duration: b,
    action: c,
    thisObj: thisObj = this,
    addTransition: addTransition = {},
    removeTransition: removeTransition = {}
  }) {
    const e = new Set(), f = Array.from(a.querySelectorAll(selector)), g = new Map(f.map(l => {
        let n = 0, o = l.parentElement;
        while (o && o !== a) {
          n++;
          o = o.parentElement;
        }
        l.$depth = n;
        if (l.scrollHeight > l.offsetHeight && getComputedStyle(l).overflow === 'auto') {
          l.$scrollTop = l.scrollTop;
          e.add(l);
        }
        const {parentElement: p} = l, q = Rectangle.from(l, a), r = Rectangle.from(l, p), u = getComputedStyle(p), v = parseFloat(u.borderLeftWidth);
        if (v) {
          q.left -= v;
          r.left -= v;
        }
        return [
          l.id,
          {
            'element': l,
            'globalBounds': q,
            'localBounds': r,
            'depth': n,
            'parentElement': p
          }
        ];
      }));
    c.call(thisObj);
    const i = Array.from(a.querySelectorAll(selector)), j = new Map(i.map(l => {
        const n = Rectangle.from(l, a), o = Rectangle.from(l, l.parentElement), p = globalThis.getComputedStyle(l.parentElement), q = parseFloat(p.borderLeftWidth);
        if (q) {
          n.left -= q;
          o.left -= q;
        }
        return [
          l.id,
          {
            'element': l,
            'globalBounds': n,
            'localBounds': o
          }
        ];
      })), k = [
        'position',
        'top',
        'left',
        'width',
        'height',
        'padding',
        'margin',
        'zIndex',
        'minWidth',
        'minHeight',
        'opacity',
        'overflow'
      ];
    for (const [l, n] of g) {
      const o = j.get(l);
      if (o) {
        const {element: p} = o, {
            style: q,
            parentElement: r
          } = p, u = parseInt(DH.getStyleValue(p, 'zIndex')), {
            globalBounds: v,
            localBounds: w,
            depth: z,
            parentElement: A
          } = n, B = A !== r;
        ObjectHelper.copyProperties(p.$initial = { 'parentElement': r }, q, k);
        let C;
        if (B) {
          o.bounds = o.globalBounds;
          C = v;
          a.appendChild(p);
        } else {
          o.bounds = o.localBounds;
          C = w;
          A.appendChild(p);
        }
        let E = 'hidden';
        if (e.has(p)) {
          p.$scrollPlaceholder = DH.createElement({
            'parent': p,
            'style': { 'height': p.scrollHeight }
          });
          E = 'auto';
        }
        const F = {
          'position': 'absolute',
          'top': C.top + 'px',
          'left': C.left + 'px',
          'width': C.width + 'px',
          'height': C.height + 'px',
          'minWidth': 0,
          'minHeight': 0,
          'margin': 0,
          'zIndex': z + (u || 0),
          'overflow': E
        };
        if (p.dataset.domTransition !== 'preserve-padding') {
          F.padding = 0;
        }
        Object.assign(q, F);
        o.processed = true;
      } else {
        const {
          element: G,
          localBounds: H,
          depth: I,
          parentElement: J
        } = n;
        G.$initial = { 'removed': true };
        Object.assign(G.style, {
          'position': 'absolute',
          'top': H.top + 'px',
          'left': H.left + 'px',
          'width': H.width + 'px',
          'height': H.height + 'px',
          'minWidth': 0,
          'minHeight': 0,
          'padding': 0,
          'margin': 0,
          'zIndex': I,
          'overflow': 'hidden'
        });
        J.appendChild(G);
        j.set(l, {
          'element': G,
          'bounds': H,
          'removed': true,
          'processed': true
        });
        i.push(G);
      }
    }
    for (const [, K] of j) {
      if (!K.processed) {
        const {element: L} = K, {
            style: N,
            parentElement: O
          } = L, P = K.bounds = K.localBounds;
        L.classList.add('b-dom-transition-adding');
        ObjectHelper.copyProperties(L.$initial = { 'parentElement': O }, N, k);
        Object.assign(N, {
          'position': 'absolute',
          'top': addTransition.top ? 0 : P.top + 'px',
          'left': addTransition.left ? 0 : P.left + 'px',
          'width': addTransition.width ? 0 : P.width + 'px',
          'height': addTransition.height ? 0 : P.height + 'px',
          'opacity': addTransition.opacity ? 0 : null,
          'zIndex': O.$depth + 1,
          'overflow': 'hidden'
        });
      }
    }
    for (const Q of e) {
      Q.scrollTop = Q.$scrollTop;
    }
    a.classList.add('b-dom-transition');
    a.firstElementChild.offsetWidth;
    for (const [, {
            element: R,
            bounds: S,
            removed: T
          }] of j) {
      if (T) {
        Object.assign(R.style, {
          'top': removeTransition.top ? 0 : S.top + 'px',
          'left': removeTransition.left ? 0 : S.left + 'px',
          'width': removeTransition.width ? 0 : S.width + 'px',
          'height': removeTransition.height ? 0 : S.height + 'px',
          'opacity': removeTransition.opacity ? 0 : R.$initial.opacity
        });
      } else {
        Object.assign(R.style, {
          'top': S.top + 'px',
          'left': S.left + 'px',
          'width': S.width + 'px',
          'height': S.height + 'px',
          'opacity': R.$initial.opacity
        });
      }
    }
    await AsyncHelper.sleep(b);
    a.classList.remove('b-dom-transition');
    for (const U of i) {
      if (U.$initial) {
        if (U.$initial.removed) {
          U.remove();
        } else {
          ObjectHelper.copyProperties(U.style, U.$initial, k);
          if (U.$scrollPlaceholder) {
            U.$scrollPlaceholder.remove();
            delete U.$scrollPlaceholder;
          }
          U.classList.remove('b-dom-transition-adding');
          U.$initial.parentElement.appendChild(U);
        }
      }
    }
    for (const V of e) {
      V.scrollTop = V.$scrollTop;
      delete V.$scrollTop;
    }
  }
  static async loadScript(a) {
    return new Promise((b, c) => {
      const e = document.createElement('script');
      e.src = a;
      e.onload = b;
      e.onerror = c;
      document.head.appendChild(e);
    });
  }
  static isNamedColor(a) {
    return a && !/^(#|hsl|rgb|hwb|lab|lch|oklab|oklch)/.test(a);
  }
  static createColorStyle(a) {
    return (a == null ? void 0 : a.length) ? this.isNamedColor(a) ? 'var(--cal-color-' + a + ')' : a : '';
  }
  static get NodeFilter() {
    return NodeFilter;
  }
  static addChild(a, b, c) {
    a.insertBefore(b, c);
  }
  static cloneStylesIntoShadowRoot(a, b) {
    return new Promise((c, e) => {
      if (b) {
        a.querySelectorAll('style, link[rel="stylesheet"]').forEach(i => i.remove());
      }
      const f = document.querySelectorAll('link[rel="stylesheet"]');
      let g = 0;
      f.forEach(i => {
        const j = i.cloneNode();
        j.addEventListener('load', () => {
          g += 1;
          if (g === f.length) {
            c();
          }
        });
        j.addEventListener('error', k => {
          e(j.href);
        });
        a.appendChild(j);
      });
      document.querySelectorAll('style').forEach(i => {
        const j = i.cloneNode();
        j.innerText = i.innerText;
        a.appendChild(j);
      });
      if (!f.length) {
        c();
      }
    });
  }
};
var DomHelper = _DomHelper;
Helpers_default.register(DomHelper);
var DH = DomHelper;
let clearTouchTimer;
const clearTouchEvent = () => DH.isTouchEvent = false;
const setTouchEvent = () => {
  DH.isTouchEvent = true;
  clearTimeout(clearTouchTimer);
  clearTouchTimer = setTimeout(clearTouchEvent, 400);
};
doc.addEventListener('touchstart', setTouchEvent, true);
doc.addEventListener('touchend', setTouchEvent, true);
DH.canonicalStyles = canonicalStyles;
DH.supportsTemplate = 'content' in doc.createElement('template');
DH.elementPropKey = elementPropKey;
DH.numberRe = numberRe;
if (!('children' in Node.prototype)) {
  const elementFilter = isElement;
  Object.defineProperty(Node.prototype, 'children', {
    'get': function () {
      return Array.prototype.filter.call(this.childNodes, elementFilter);
    }
  });
}
if (!Element.prototype.matches) {
  Element.prototype.matches = Element.prototype.matchesSelector || Element.prototype.mozMatchesSelector || Element.prototype.msMatchesSelector || Element.prototype.oMatchesSelector || Element.prototype.webkitMatchesSelector || function (a) {
    const b = (this.document || this.ownerDocument).querySelectorAll(a);
    let c = b.length;
    while (--c >= 0 && b.item(c) !== this) {
    }
    return c > -1;
  };
}
if (win.Element && !Element.prototype.closest) {
  Node.prototype.closest = Element.prototype.closest = function (a) {
    let b = this;
    if (!doc.documentElement.contains(b))
      return null;
    do {
      if (b.matches(a))
        return b;
      b = b.parentElement || b.parentNode;
    } while (b !== null && b.nodeType === b.ELEMENT_NODE);
    return null;
  };
} else {
  Node.prototype.closest = function (a) {
    let b;
    return (b = this.parentNode) == null ? void 0 : b.closest(a);
  };
}
(function (a) {
  a.forEach(function (b) {
    if (Object.prototype.hasOwnProperty.call(b, 'remove')) {
      return;
    }
    Object.defineProperty(b, 'remove', {
      'configurable': true,
      'enumerable': true,
      'writable': true,
      'value': function c() {
        this.parentNode && this.parentNode.removeChild(this);
      }
    });
  });
}([
  Element.prototype,
  CharacterData.prototype,
  DocumentType.prototype
]));
globalThis.addEventListener('resize', () => scrollBarWidth = null);
DomHelper._$name = 'DomHelper';
const activeTouches = new Map();
const longpressMoveThreshold = 5;
const isFloatingWidget = a => a.floating;
const longPressCancelEvents = {
  'touchend': 1,
  'pointerup': 1
};
const ignoreModifierKeys = {
  'Meta': 1,
  'Control': 1,
  'Alt': 1
};
const onPointerUp = a => {
  if ((currentPointerDown == null ? void 0 : currentPointerDown.pointerId) === a.pointerId) {
    currentPointerDown = null;
  }
};
const trackTouches = a => {
  const {touches: b} = a;
  activeTouches.clear();
  if (b) {
    for (let c = 0; c < b.length; c++) {
      const e = b.item(c);
      activeTouches.set(e.identifier, [
        e,
        a
      ]);
    }
  }
};
var GlobalEvents = new class GlobalEventsHandler extends Base.mixin(Events_default) {
  suspendFocusEvents() {
    focusEventsSuspended = true;
  }
  resumeFocusEvents() {
    focusEventsSuspended = false;
  }
  setupFocusListenersOnce(a, b) {
    if (a && !GlobalEvents.observedElements.has(a)) {
      GlobalEvents.setupFocusListeners(a, b);
      GlobalEvents.observedElements.add(a);
    }
  }
  detachFocusListeners(a) {
    if (a.$globalEventsDetacher) {
      a.$globalEventsDetacher();
      a.$globalEventsDetacher = null;
      GlobalEvents.observedElements.delete(a);
    }
  }
  setupFocusListeners(a = document, b, c = false) {
    let e, f, g;
    const i = {
      'element': a,
      'fromGlobal': true,
      'touchstart'(j) {
        const {touches: k} = j;
        trackTouches(j);
        if (k.length === 2 && k[0].target.closest('.b-widget') && k[1].target.closest('.b-widget')) {
          e = [
            k[0],
            k[1]
          ];
          f = [
            (e[0].clientX + e[1].clientX) / 2,
            (e[0].clientY + e[1].clientY) / 2
          ];
          g = DomHelper.elementFromPoint(f[0], f[1]);
          return;
        }
        if (!globalTouchStart && j.changedTouches.length === 1) {
          globalTouchStart = j.changedTouches[0];
          if (!BrowserHelper.isAndroid) {
            const l = ({
                clientX: p,
                clientY: q,
                type: r
              }) => {
                if (longPressCancelEvents[r] || !globalTouchStart || Math.max(Math.abs(p - globalTouchStart.clientX), Math.abs(q - globalTouchStart.clientY)) > longpressMoveThreshold) {
                  contextMenuTouchId = null;
                  n();
                  clearTimeout(o);
                }
              }, n = b.on({
                'element': document,
                'touchmove': l,
                'touchend': l,
                'pointermove': l,
                'pointerup': l,
                'capture': true
              }), o = setTimeout(() => {
                contextMenuTouchId = globalTouchStart.identifier;
                n();
                j.target.dispatchEvent(new MouseEvent('contextmenu', b.copyEvent({}, j)));
              }, b.longPressTime);
          }
        } else {
          globalTouchStart = null;
        }
      },
      'touchmove': {
        'passive': false,
        'handler': j => {
          const {touches: k} = j;
          if (k.length === 2 && e) {
            const l = [
                k[0],
                k[1]
              ], n = b.getDistanceBetween(e[0], e[1]) - b.getDistanceBetween(l[0], l[1]), o = Math.abs(e[0].clientX - e[1].clientX) - Math.abs(l[0].clientX - l[1].clientX), p = Math.abs(e[0].clientY - e[1].clientY) - Math.abs(l[0].clientY - l[1].clientY);
            if (Math.abs(n) > 2) {
              const q = {
                'clientX': f[0],
                'clientY': f[1],
                'bubbles': true
              };
              if (GlobalEvents.Widget.convertPinchToMousewheel) {
                j.preventDefault();
                j.stopImmediatePropagation();
                const u = new MouseEvent('wheel', {
                  ...q,
                  'ctrlKey': true
                });
                u.deltaY = p;
                u.deltaX = o;
                g.dispatchEvent(u);
              }
              const r = new MouseEvent('bryntum-pinch', q);
              r.deltaY = p;
              r.deltaX = o;
              r.touchMove = j;
              g.dispatchEvent(r);
            }
            e = l;
          }
        }
      },
      'touchend': {
        'passive': false,
        'handler': j => {
          trackTouches(j);
          if (j.touches.length !== 2) {
            e = null;
          }
          if (globalTouchStart) {
            if (j.changedTouches[0].identifier === contextMenuTouchId) {
              j.stopImmediatePropagation();
              if (j.cancelable !== false) {
                j.preventDefault();
              }
            } else if (j.changedTouches.length === 1 && j.changedTouches[0].identifier === globalTouchStart.identifier) {
              GlobalEvents.trigger('globaltap', { 'event': j });
            }
            globalTouchStart = null;
          }
        }
      },
      'touchcancel': {
        'passive': false,
        'handler': trackTouches
      },
      'mousedown': {
        'handler': j => {
          lastInteractionType = 'mouse';
          if (!globalTouchStart) {
            GlobalEvents.trigger('globaltap', { 'event': j });
          }
          currentMouseDown = j;
          const k = j.ctrlKey || j.altKey || j.shiftKey || j.metaKey;
          if (!currentKeyDown && k) {
            currentKeyDown = new KeyboardEvent('keydown', {
              'key': j.ctrlKey ? 'Control' : j.shiftKey ? 'Shift' : j.altKey ? 'Alt' : 'Meta',
              'ctrlKey': j.ctrlKey,
              'altKey': j.altKey,
              'shiftKey': j.shiftKey,
              'metaKey': j.metaKey
            });
          } else if (currentKeyDown && !k) {
            currentKeyDown = null;
          }
        },
        'passive': false
      },
      'mouseup'() {
        currentMouseDown = null;
      },
      'pointerdown': {
        'passive': false,
        'handler': j => {
          let k;
          currentPointerDown = j;
          DomHelper.usingKeyboard = false;
          (k = a.classList) == null ? void 0 : k.remove('b-using-keyboard');
          if (a.nodeType === Node.DOCUMENT_FRAGMENT_NODE) {
            DomHelper.removeClsGlobally(a, 'b-using-keyboard');
          }
        }
      },
      'pointerup': {
        'passive': false,
        'handler': onPointerUp
      },
      'pointercancel': {
        'passive': false,
        'handler': onPointerUp
      },
      'keydown'(j) {
        const {
          key: k,
          target: l
        } = j;
        lastInteractionType = 'key';
        currentKeyDown = j;
        currentOverElement = l;
        if (!ignoreModifierKeys[k]) {
          DomHelper.usingKeyboard = true;
          if (k === 'Escape') {
            GlobalEvents.Widget.queryAll(n => n.isTooltip && n.isVisible).forEach(n => n.hide());
          }
          if (a.nodeType === Node.DOCUMENT_FRAGMENT_NODE) {
            for (const n of a.children) {
              if (n.matches('.b-outer')) {
                n.classList.add('b-using-keyboard');
              }
            }
          } else {
            a.classList.add('b-using-keyboard');
          }
        }
      },
      'mouseenter'(j) {
        currentOverElement = j.target;
      },
      'keypress'() {
        lastInteractionType = 'key';
      },
      'keyup'() {
        currentKeyDown = null;
      },
      'focusin'(j) {
        let k;
        const {Widget: l} = GlobalEvents, {target: n} = j;
        if ((n == null ? void 0 : n.shadowRoot) || (n == null ? void 0 : n._shadowRoot)) {
          return;
        }
        if (n && l.accessibility) {
          const C = n.firstElementChild || n, E = Rectangle.from(n), F = E.center;
          (DomHelper.childFromPoint(C, E.width / 2, E.height / 2) || C).dispatchEvent(new PointerEvent('pointerover', {
            'bubbles': true,
            'clientX': F.x,
            'clientY': F.y,
            'pointerType': 'focus'
          }));
        }
        if (focusEventsSuspended || n && !n.isConnected) {
          return;
        }
        const o = !j.relatedTarget ? null : j.relatedTarget instanceof HTMLElement ? j.relatedTarget : document.body, p = n || document.body, q = l.fromElement(o), r = l.fromElement(p), u = DomHelper.getCommonAncestor(q, r), v = !!(o && p.compareDocumentPosition(o) & 4), w = l.query(isTopVisibleModal);
        let z = null;
        if (p && p !== document.body) {
          z = DomHelper.getActiveElement(p);
        } else {
          z = DomHelper.getActiveElement(document);
        }
        if (w && !w._isRevertingFocus) {
          if (!r || !w.owns(r) && !(w.element.compareDocumentPosition(r.element) & 4 && r.up(isFloatingWidget))) {
            return w.focus();
          }
        }
        let A = createWidgetEvent('focusout', o, n, q, r, v);
        for (let G = q, H; G && G !== u; G = H) {
          H = G.owner;
          if (!G.isDestroying && G.onFocusOut) {
            G.onFocusOut(A);
            if (n && z !== DomHelper.getActiveElement(n)) {
              return;
            }
          }
        }
        if (u && n === u.element) {
          if (!u.isDestroying && DomHelper.getActiveElement(u) === p && u.focusElement && u.focusElement !== u.element) {
            if (!u.element.contains(z) || u.focusDescendant) {
              u.setTimeout(() => {
                let I;
                return (I = u.focus) == null ? void 0 : I.call(u);
              }, 0);
            }
          }
        } else {
          A = createWidgetEvent('focusin', p, o, q, r, v);
          for (let I = r; I && I !== u; I = I.owner) {
            if (!I.isDestroying) {
              (k = I.onFocusIn) == null ? void 0 : k.call(I, A);
            }
          }
        }
        const B = DomHelper.getCommonAncestor((o == null ? void 0 : o.nodeType) === Element.ELEMENT_NODE ? o : null, p) || p.parentNode;
        if (B) {
          A = createWidgetEvent('focusmove', p, o, q, r, v, { 'bubbles': true });
          B.dispatchEvent(A);
        }
      },
      'focusout'(j) {
        if (focusEventsSuspended) {
          return;
        }
        if (!j.relatedTarget || !GlobalEvents.Widget.fromElement(j.relatedTarget)) {
          const k = j.relatedTarget && j.relatedTarget instanceof HTMLElement ? j.relatedTarget : null;
          i.focusin({
            'target': k,
            'relatedTarget': j.target
          });
          currentKeyDown = currentMouseDown = null;
        }
      },
      'blur': {
        'element': globalThis,
        'handler'(j) {
          if (j.target === globalThis) {
            currentKeyDown = null;
            currentMouseDown = null;
          }
        }
      },
      'capture': true,
      'passive': true
    };
    c && (detacher == null ? void 0 : detacher());
    detacher = this.detachEvents = a.$globalEventsDetacher = b.on(i);
  }
  get activeTouches() {
    return Array.from(activeTouches.values());
  }
  get hasActiveTouches() {
    return activeTouches.size > 0;
  }
  get lastInteractionType() {
    return lastInteractionType;
  }
  get shiftKeyDown() {
    return Boolean(currentKeyDown == null ? void 0 : currentKeyDown.shiftKey);
  }
  get ctrlKeyDown() {
    return Boolean((currentKeyDown == null ? void 0 : currentKeyDown.ctrlKey) || (currentKeyDown == null ? void 0 : currentKeyDown.metaKey));
  }
  get altKeyDown() {
    return Boolean(currentKeyDown == null ? void 0 : currentKeyDown.altKey);
  }
  get currentOverElement() {
    return currentOverElement;
  }
  isKeyDown(a) {
    return !a ? Boolean(currentKeyDown) : (currentKeyDown == null ? void 0 : currentKeyDown.key) === a || currentKeyDown[(a == null ? void 0 : a.toLowerCase()) + 'Key'] === true;
  }
  isMouseDown(a = 0) {
    return (currentMouseDown == null ? void 0 : currentMouseDown.button) === a;
  }
  get currentMouseDown() {
    return currentMouseDown;
  }
  set currentMouseDown(a) {
    currentMouseDown = a;
  }
  get currentPointerDown() {
    return currentPointerDown;
  }
  set currentPointerDown(a) {
    currentPointerDown = a;
  }
  get currentTouch() {
    return globalTouchStart;
  }
  set currentTouch(a) {
    globalTouchStart = a;
  }
  get currentKeyDown() {
    return currentKeyDown;
  }
  set currentKeyDown(a) {
    currentKeyDown = a;
  }
}();
var isTopVisibleModal = a => a.isVisible && a.isTopModal;
GlobalEvents.observedElements = new Set();
let globalTouchStart;
let contextMenuTouchId;
var focusEventsSuspended = false;
let lastInteractionType;
let currentKeyDown;
let currentMouseDown;
let currentPointerDown;
let currentOverElement;
let detacher;
function createWidgetEvent(a, b, c, e, f, g, i) {
  const j = new CustomEvent(a, i);
  Object.defineProperty(j, '_target', {
    'get'() {
      return b;
    }
  });
  Object.defineProperty(j, 'relatedTarget', {
    'get'() {
      return c;
    }
  });
  j.fromWidget = e;
  j.toWidget = f;
  j.backwards = g;
  return j;
}
var GlobalEvents_default = GlobalEvents;
const paramValueRegExp = /^(\w+)=(.*)$/;
const parseParams = function (a) {
  const b = {}, c = a.split('&');
  for (const e of c) {
    const [f, g, i] = paramValueRegExp.exec(e), j = decodeURIComponent(g), k = decodeURIComponent(i);
    if (f) {
      let l = b[j];
      if (l) {
        if (!Array.isArray(l)) {
          l = b[j] = [l];
        }
        l.push(k);
      } else {
        b[j] = k;
      }
    }
  }
  return b;
};
const _AjaxHelper = class _AjaxHelper {
  static DEFAULT_FETCH_OPTIONS = {};
  static get(a, b) {
    return this.fetch(a, b);
  }
  static post(a, b, c = {}) {
    if (!(b instanceof FormData) && !(typeof b === 'string')) {
      b = JSON.stringify(b);
      c.headers = c.headers || {};
      c.headers['Content-Type'] = c.headers['Content-Type'] || 'application/json';
    }
    return this.fetch(a, Object.assign({
      'method': 'POST',
      'body': b
    }, c));
  }
  static fetch(a, b) {
    let c;
    b = Objects.merge({}, _AjaxHelper.DEFAULT_FETCH_OPTIONS, b);
    if (typeof AbortController !== 'undefined') {
      c = b.abortController = new AbortController();
      b.signal = c.signal;
    }
    if (!('credentials' in b)) {
      b.credentials = 'include';
    }
    if (b.queryParams) {
      const f = Object.entries(b.queryParams);
      if (f.length) {
        let g = false;
        if (b.headers && b.addQueryParamsToBody === true) {
          const i = new Headers(b.headers).get('Content-Type');
          let j;
          switch (i) {
          case 'application/x-www-form-urlencoded':
            j = URLSearchParams;
            break;
          case 'multipart/form-data':
            j = FormData;
            break;
          }
          if (j) {
            const k = b.body || (b.body = new j());
            if (k instanceof j) {
              f.forEach(([l, n]) => k.set(l, n));
              g = true;
            }
          }
        }
        if (!g) {
          a += (a.includes('?') ? '&' : '?') + f.map(([l, n]) => l + '=' + encodeURIComponent(n)).join('&');
        }
      }
    }
    const e = new Promise((l, n) => {
      fetch(a, b).then(o => {
        if (b.parseJson) {
          o.json().then(p => {
            o.parsedJson = p;
            l(o);
          }).catch(p => {
            o.parsedJson = null;
            o.error = p;
            n(o);
          });
        } else {
          l(o);
        }
      }).catch(o => {
        o.stack = e.stack;
        n(o);
      });
    });
    e.stack = new Error().stack;
    e.abort = function () {
      c == null ? void 0 : c.abort();
    };
    return e;
  }
  static mockUrl(a, b) {
    const c = this;
    (c.mockAjaxMap || (c.mockAjaxMap = {}))[a] = b;
    if (!_AjaxHelper.originalFetch) {
      _AjaxHelper.originalFetch = _AjaxHelper.fetch;
      _AjaxHelper.fetch = c.mockAjaxFetch.bind(c);
    }
  }
  static async mockAjaxFetch(a, b) {
    const c = a.split('?');
    let e = this.mockAjaxMap[c[0]], f = null;
    if (e) {
      if (typeof e === 'function') {
        e = await e(c[0], c[1] && parseParams(c[1]), b);
      }
      try {
        f = (b == null ? void 0 : b.parseJson) && JSON.parse(e.responseText);
      } catch (g) {
        f = null;
        e.error = g;
      }
      e = Object.assign({
        'status': 200,
        'ok': true,
        'headers': new Headers(),
        'statusText': 'OK',
        'url': a,
        'parsedJson': f,
        'text': () => new Promise(i => {
          i(e.responseText);
        }),
        'json': () => new Promise(i => {
          i(f);
        })
      }, e);
      return new Promise(function (i, j) {
        if (e.synchronous) {
          i(e);
        } else {
          setTimeout(function () {
            i(e);
          }, 'delay' in e ? e.delay : 100);
        }
      });
    } else {
      return _AjaxHelper.originalFetch(a, b);
    }
  }
};
const AjaxHelper = _AjaxHelper;
AjaxHelper._$name = 'AjaxHelper';
const global = typeof self !== 'undefined' ? self : typeof globalThis !== 'undefined' ? globalThis : null;
const _LocaleHelper = class _LocaleHelper {
  static skipLocaleIntegrityCheck = false;
  static mergeLocales(...a) {
    const b = {};
    a.forEach(c => {
      Object.keys(c).forEach(e => {
        if (typeof c[e] === 'object') {
          b[e] = {
            ...b[e],
            ...c[e]
          };
        } else {
          b[e] = c[e];
        }
      });
    });
    return b;
  }
  static trimLocale(a, b) {
    const c = (e, f) => {
      if (a[e]) {
        if (f) {
          if (a[e][f]) {
            delete a[e][f];
          }
        } else {
          delete a[e];
        }
      }
    };
    Object.keys(b).forEach(e => {
      if (Object.keys(b[e]).length > 0) {
        Object.keys(b[e]).forEach(f => c(e, f));
      } else {
        c(e);
      }
    });
  }
  static normalizeLocale(a, b) {
    if (!a) {
      throw new Error('"nameOrConfig" parameter can not be empty');
    }
    if (typeof a === 'string') {
      if (!b) {
        throw new Error('"config" parameter can not be empty');
      }
      if (b.locale) {
        b.name = a || b.name;
      } else {
        b.localeName = a;
      }
    } else {
      b = a;
    }
    let c = {};
    if (b.name || b.locale) {
      c = Object.assign({ 'localeName': b.name }, b.locale);
      b.desc && (c.localeDesc = b.desc);
      b.code && (c.localeCode = b.code);
      b.path && (c.localePath = b.path);
    } else {
      if (!b.localeName) {
        throw new Error('"config" parameter doesn\'t have "localeName" property');
      }
      c = Object.assign({}, b);
    }
    for (const e of [
        'name',
        'desc',
        'code',
        'path'
      ]) {
      if (c[e]) {
        delete c[e];
      }
    }
    if (!c.localeName) {
      throw new Error('Locale name can not be empty');
    }
    return c;
  }
  static get locales() {
    return global.bryntum.locales || {};
  }
  static set locales(a) {
    global.bryntum.locales = a;
  }
  static get localeName() {
    return global.bryntum.locale || 'En';
  }
  static set localeName(a) {
    global.bryntum.locale = a || _LocaleHelper.localeName;
  }
  static get locale() {
    return _LocaleHelper.localeName && this.locales[_LocaleHelper.localeName] || this.locales.En || Object.values(this.locales)[0] || {
      'localeName': '',
      'localeDesc': '',
      'localeCoode': ''
    };
  }
  static publishLocale(a, b) {
    const {locales: c} = global.bryntum, e = _LocaleHelper.normalizeLocale(a, b), {localeName: f} = e;
    if (!c[f] || b === true) {
      c[f] = e;
    } else {
      c[f] = this.mergeLocales(c[f] || {}, e || {});
    }
    return c[f];
  }
};
const LocaleHelper = _LocaleHelper;
global.bryntum = global.bryntum || {};
global.bryntum.locales = global.bryntum.locales || {};
LocaleHelper._$name = 'LocaleHelper';
const LocaleManager = class extends Events_default(Base) {
  static get defaultConfig() {
    return { 'throwOnMissingLocale': VersionHelper.isTestEnv };
  }
  construct(...a) {
    let b;
    const c = this;
    super.construct(...a);
    if (BrowserHelper.isBrowserEnv) {
      const e = document.querySelector('script[data-default-locale]');
      if (e) {
        c.applyLocale(e.dataset.defaultLocale);
      } else if ((b = c.locale) == null ? void 0 : b.localeName) {
        c.applyLocale(c.locale.localeName);
      }
    }
  }
  get locales() {
    return LocaleHelper.locales;
  }
  set locales(a) {
    LocaleHelper.locales = a;
  }
  set locale(a) {
    this.applyLocale(a);
  }
  get locale() {
    return LocaleHelper.locale;
  }
  get localeCode() {
    let a, b;
    return (b = (a = this.locale) == null ? void 0 : a.localeCode) != null ? b : 'en-US';
  }
  applyLocale(a, b, c = false) {
    const e = this;
    let f;
    if (typeof a === 'string') {
      if (typeof b !== 'object') {
        f = e.locales[a];
        if (!f) {
          if (c) {
            return true;
          }
          throw new Error('Locale "' + a + '" is not published. Publish with LocaleHelper.publishLocale() before applying.');
        }
      } else {
        f = LocaleHelper.publishLocale(a, b);
      }
    } else {
      f = LocaleHelper.publishLocale(a);
    }
    if (e.locale.localeName && e.locale.localeName === f.localeName && b !== true) {
      return e.locale;
    }
    LocaleHelper.localeName = f.localeName;
    const g = () => {
      e.trigger('locale', f);
    };
    if (f.localePath) {
      return new Promise((i, j) => {
        e.loadLocale(f.localePath).then(k => {
          k.text().then(l => {
            const n = new Function(l);
            n();
            if (BrowserHelper.isBrowserEnv) {
              f = e.locales[f.localeName];
              if (f) {
                delete f.localePath;
              }
            }
            g();
            i(f);
          });
        }).catch(k => j(k));
      });
    }
    g();
    return f;
  }
  loadLocale(a) {
    return AjaxHelper.get(a);
  }
  set throwOnMissingLocale(a) {
    this._throwOnMissingLocale = a;
  }
  get throwOnMissingLocale() {
    return this._throwOnMissingLocale;
  }
};
const LocaleManagerSingleton = new LocaleManager();
const LocaleManager_default = LocaleManagerSingleton;
const ObjectProto = Object.getPrototypeOf(Object);
const localeRe = /L{.*?}/g;
const capturelocaleRe = /L{(.*?)}/g;
const classMatchRe = /((.*?)\.)?(.+)/g;
const escape2 = a => a.replace(/{(\d+)}/gm, '[[$1]]');
const unescape = a => a.replace(/\[\[(\d+)]]/gm, '{$1}');
const emptyObject4 = Object.freeze(Object.create(null));
const Localizable_default = a => class b extends (a || Base) {
  static get $name() {
    return 'Localizable';
  }
  static get configurable() {
    return {
      'localeClass': null,
      'localizable': null,
      'localizableProperties': {
        'value': [],
        '$config': { 'merge': 'distinct' }
      }
    };
  }
  static clsName(c) {
    let e, f;
    return typeof c === 'string' ? c : c === ObjectProto ? 'Object' : c.$$name || c.name || ((e = c.prototype) == null ? void 0 : e.$$name) || ((f = c.prototype) == null ? void 0 : f.name);
  }
  static parseLocaleString(c) {
    const e = [];
    let f;
    if (c == null ? void 0 : c.includes('L{')) {
      c = escape2(c);
      capturelocaleRe.lastIndex = 0;
      while ((f = capturelocaleRe.exec(c)) != null) {
        classMatchRe.lastIndex = 0;
        const g = classMatchRe.exec(f[1]);
        e.push({
          'match': unescape(f[0]),
          'localeKey': unescape(g[3]),
          'localeClass': g[2]
        });
      }
    }
    return e.length > 0 ? e : [{
        'match': c,
        'localeKey': c,
        'localeClass': void 0
      }];
  }
  construct(c = {}, ...e) {
    super.construct(c, ...e);
    LocaleManager_default.ion({
      'locale': 'updateLocalization',
      'thisObj': this
    });
    this.updateLocalization();
  }
  get localeClass() {
    return this._localeClass || null;
  }
  localizeProperty(c) {
    let e, f, g;
    const i = this, j = Objects.getPath(i, c), k = ((e = i.$meta.configs[c]) == null ? void 0 : e.localeKey) || ((g = (f = i.fieldMap) == null ? void 0 : f[c]) == null ? void 0 : g.defaultValue);
    let l;
    if (k) {
      l = b.localize(k, i, i.localeClass || i);
      if (l && !(c in (i.initialConfig || emptyObject4)) && !(c in (i.originalData || emptyObject4))) {
        Objects.setPath(i.isColumn ? i.data : i, c, l);
      } else {
        return j;
      }
    } else if (typeof j === 'string') {
      i.originalLocales = i.originalLocales || {};
      l = Objects.getPath(i.originalLocales, c);
      if (l === void 0) {
        Objects.setPath(i.originalLocales, c, j);
        l = j;
      }
      if (l) {
        Objects.setPath(i, c, l = i.optionalL(l, i));
      }
    }
    return l || j;
  }
  updateLocalization() {
    let c, e;
    if (this.localizable !== false) {
      (c = this.localizableProperties) == null ? void 0 : c.forEach(this.localizeProperty, this);
      (e = this.trigger) == null ? void 0 : e.call(this, 'localized');
    }
  }
  static getTranslation(c, e, f) {
    const g = LocaleManager_default.locale;
    let i = null, j, k;
    if (g) {
      for (const {
            match: l,
            localeKey: n,
            localeClass: o
          } of this.parseLocaleString(c)) {
        const p = r => {
          let u;
          const v = (u = g[r]) == null ? void 0 : u[n];
          if (v) {
            if (typeof v === 'function') {
              i = e != null ? v(e) : v;
            } else if (typeof v === 'object' || c === l) {
              i = v;
            } else {
              i = (i || c).replace(l, v);
            }
            if (typeof v === 'string' && v.includes('L{')) {
              i = this.getTranslation(v, e, f);
            }
          }
          return v;
        };
        let q = false;
        for (k = f; k && (j = b.clsName(k)); k = Object.getPrototypeOf(k)) {
          if (q = p(j)) {
            break;
          } else if (typeof k === 'string') {
            break;
          }
        }
        if (!q && o) {
          p(o);
        }
      }
    }
    return i;
  }
  static localize(c, e = void 0, ...f) {
    if ((f == null ? void 0 : f.length) === 0) {
      f = [this];
    }
    let g = null;
    f.some(i => {
      g = b.getTranslation(c, e, i);
      return g != null;
    });
    return g;
  }
  static L(c, e = void 0, ...f) {
    if ((f == null ? void 0 : f.length) === 0) {
      f = [this];
    }
    const g = this.localize(c, e, ...f);
    if (g == null && LocaleManager_default.throwOnMissingLocale && c.includes('L{')) {
      throw new Error('Localization is not found for \'' + c + '\' in \'' + f.map(i => b.clsName(i)).join(', ') + '\'. ' + (LocaleManager_default.locale.localeName ? 'Locale : ' + LocaleManager_default.locale.localeName : ''));
    }
    return g != null ? g : c;
  }
  L(c, e, f = this.localeClass) {
    const {constructor: g} = this;
    if (f && b.clsName(f) !== b.clsName(g)) {
      return b.L(c, e, f, g);
    } else {
      return b.L(c, e, g);
    }
  }
  static optionalL(c, e = void 0, ...f) {
    const g = LocaleManager_default.throwOnMissingLocale;
    LocaleManager_default.throwOnMissingLocale = g && localeRe.test(c);
    if ((f == null ? void 0 : f.length) === 0) {
      f = [this];
    }
    const i = b.L(c, e, ...f);
    LocaleManager_default.throwOnMissingLocale = g;
    return i;
  }
  optionalL(c, e = this, f = false) {
    const g = LocaleManager_default.throwOnMissingLocale;
    LocaleManager_default.throwOnMissingLocale = g && localeRe.test(c) && !f;
    const i = this.L(c, e);
    LocaleManager_default.throwOnMissingLocale = g;
    return i;
  }
  get localeManager() {
    return LocaleManager_default;
  }
  get localeHelper() {
    return LocaleHelper;
  }
};
function getDescriptor(a, b) {
  const c = ObjectHelper.getPropertyDescriptor(a, b);
  return c && (c.get || c.set) ? c : null;
}
const InstancePlugin = class extends Base.mixin(Events_default, Localizable_default) {
  static $name = 'InstancePlugin';
  static get configurable() {
    return {
      'clientListeners': null,
      'disabled': false,
      'client': null,
      'keyMap': null
    };
  }
  updateClient(a) {
    if (!this.owner) {
      this.owner = a;
    }
  }
  updateKeyMap(a) {
    const {client: b} = this;
    b.keyMap = b.mergeKeyMaps(b.keyMap, a, StringHelper.uncapitalize(this.constructor.$name));
  }
  static initPlugins(a, ...b) {
    for (const c of b) {
      this.initPlugin(a, c);
    }
  }
  static initPlugin(a, b, c) {
    const e = a.plugins || (a.plugins = {}), f = [a];
    if (c) {
      f.push(c);
    }
    return e[b.$$name] = new b(...f);
  }
  get enabled() {
    return !this.disabled;
  }
  getFocusRevertTarget() {
    let a;
    return (a = this.client) == null ? void 0 : a.getFocusRevertTarget();
  }
  construct(...a) {
    const b = this;
    let [c, e] = a, f;
    if (a.length === 1) {
      if (ObjectHelper.isObject(c)) {
        e = c;
        c = e.client;
      }
    } else {
      e = ObjectHelper.assign({}, e);
      delete e.client;
    }
    b.client = c;
    super.construct(e);
    b.applyPluginConfig(c);
    f = b.clientListeners;
    if (f) {
      f = ObjectHelper.assign({}, f);
      f.thisObj = b;
      c.ion(f);
    }
  }
  applyPluginConfig(a) {
    const b = this, c = b.pluginConfig || b.constructor.pluginConfig;
    if (c) {
      const {
        assign: e,
        chain: f,
        after: g,
        before: i,
        override: j
      } = c;
      e && b.applyAssign(a, e);
      (f || g) && b.applyChain(a, f || g);
      i && b.applyChain(a, i, false);
      j && b.applyOverride(a, j);
    }
  }
  applyAssign(a, b) {
    b.forEach(c => this.assign(a, c));
  }
  applyChain(a, b, c = true) {
    if (Array.isArray(b)) {
      for (const e of b) {
        this.chain(a, e, e, c);
      }
    } else {
      for (const f in b) {
        this.chain(a, f, b[f], c);
      }
    }
  }
  applyOverride(a, b) {
    const c = this;
    if (!c.overridden) {
      c.overridden = {};
    }
    b.forEach(e => {
      if (!c[e]) {
        throw new Error('Trying to chain fn ' + a.$$name + '#' + e + ', but plugin fn ' + c.$$name + '#' + e + ' does not exist');
      }
      if (typeof a[e] === 'function') {
        c.overridden[e] = a[e].bind(a);
      }
      a[e] = c[e].bind(c);
    });
  }
  assign(a, b) {
    const c = this, e = getDescriptor(c, b);
    if (e) {
      Object.defineProperty(a, b, {
        'configurable': true,
        'enumerable': true,
        'get': e.get && e.get.bind(c),
        'set': e.set && e.set.bind(c)
      });
    } else {
      a[b] = c[b].bind(c);
    }
  }
  chain(a, b, c, e = true) {
    let f = 0;
    if (typeof b === 'object') {
      b = b.fn;
    }
    if (typeof c === 'object') {
      f = c.prio || 0;
      c = c.fn;
    }
    const g = this, i = a.pluginFunctionChain || (a.pluginFunctionChain = {}), j = g[c] && g[c].bind(g), k = g.functionChainRunner;
    if (!j) {
      throw new Error('Trying to chain fn ' + a.$$name + '#' + c + ', but plugin fn ' + g.$$name + '#' + c + ' does not exist');
    }
    if (!i[b]) {
      let l = a[b];
      if (l) {
        l = l.bind(a);
        l.$this = a;
        l.$prio = 0;
      }
      i[b] = l ? [l] : [];
      a[b] = (...n) => k(i[b], n);
    }
    j.$this = g;
    j.$prio = f;
    i[b][e ? 'push' : 'unshift'](j);
    i[b].$sorted = false;
  }
  functionChainRunner(a, b) {
    let c, e, f;
    if (!a.$sorted) {
      a.sort((g, j) => j.$prio - g.$prio);
      a.$sorted = true;
    }
    for (e = 0; e < a.length; e++) {
      c = a[e];
      if (!c.$this.isDestroyed) {
        f = c(...b);
        if (f === false) {
          break;
        }
      }
    }
    return f;
  }
  doDisable(a) {
    let b, c, e, f;
    const g = this, {constructor: i} = g, j = 'featureClass' in i ? i.featureClass : 'b-' + i.$$name.toLowerCase(), k = StringHelper.uncapitalize(i.$$name);
    if (j) {
      (c = (b = g.client) == null ? void 0 : b._element) == null ? void 0 : c.classList[a ? 'remove' : 'add'](j);
    }
    if (!g.isConfiguring) {
      if (a) {
        g.trigger('disable');
      } else {
        g.trigger('enable');
      }
      (f = (e = g.client).syncSplits) == null ? void 0 : f.call(e, l => {
        const n = l.features[k];
        if (n) {
          n.disabled = a;
        }
      });
    }
  }
  updateDisabled(a) {
    this.doDisable(a);
  }
  throwOverrideIsMissing(a) {
    throw new Error('Trying to override fn ' + a.plugIntoName + '#' + a.fnName + ', but plugin fn ' + a.pluginName + '#' + a.fnName + ' does not exist');
  }
  get rootElement() {
    return this.client.rootElement;
  }
  get state() {
    let a;
    return (a = this.constructor.stateProperties) == null ? void 0 : a.reduce((b, c) => {
      b[c] = this[c];
      return b;
    }, {});
  }
};
InstancePlugin._$name = 'InstancePlugin';
const Pluggable_default = a => class b extends (a || Base) {
  static get $name() {
    return 'Pluggable';
  }
  get plugins() {
    if (!this._plugins) {
      this._plugins = {};
    }
    return this._plugins;
  }
  set plugins(c) {
    if (c) {
      if (!Array.isArray(c))
        c = [c];
      InstancePlugin.initPlugins(this, ...c);
    }
    this.initPlugins();
  }
  initPlugins() {
  }
  addPlugins(...c) {
    InstancePlugin.initPlugins(this, ...c);
  }
  addPlugin(c, e) {
    return InstancePlugin.initPlugin(this, c, e);
  }
  hasPlugin(c) {
    return this.getPlugin(c) != null;
  }
  getPlugin(c) {
    let e;
    if (typeof c === 'function') {
      c = c.$$name;
    }
    return (e = this.plugins) == null ? void 0 : e[c];
  }
  get widgetClass() {
  }
};
const locale = {
  'localeName': 'En',
  'localeDesc': 'English (US)',
  'localeCode': 'en-US',
  'Object': {
    'Yes': 'Yes',
    'No': 'No',
    'Cancel': 'Cancel',
    'Ok': 'OK',
    'Week': 'Week',
    'None': 'None'
  },
  'CodeEditor': {
    'apply': 'Apply',
    'autoApply': 'Auto apply',
    'downloadCode': 'Download code',
    'editor': 'Code editor',
    'viewer': 'Code viewer'
  },
  'ColorPicker': { 'noColor': 'No color' },
  'Combo': {
    'noResults': 'No results',
    'recordNotCommitted': 'Record could not be added',
    'addNewValue': a => 'Add ' + a
  },
  'FilePicker': { 'file': 'File' },
  'Field': {
    'badInput': 'Invalid field value',
    'patternMismatch': 'Value should match a specific pattern',
    'rangeOverflow': a => 'Value must be less than or equal to ' + a.max,
    'rangeUnderflow': a => 'Value must be greater than or equal to ' + a.min,
    'stepMismatch': 'Value should fit the step',
    'tooLong': 'Value should be shorter',
    'tooShort': 'Value should be longer',
    'typeMismatch': 'Value is required to be in a special format',
    'valueMissing': 'This field is required',
    'invalidValue': 'Invalid field value',
    'minimumValueViolation': 'Minimum value violation',
    'maximumValueViolation': 'Maximum value violation',
    'fieldRequired': 'This field is required',
    'validateFilter': 'Value must be selected from the list'
  },
  'DateField': { 'invalidDate': 'Invalid date input' },
  'DatePicker': {
    'gotoPrevYear': 'Go to previous year',
    'gotoPrevMonth': 'Go to previous month',
    'gotoNextMonth': 'Go to next month',
    'gotoNextYear': 'Go to next year'
  },
  'NumberFormat': {
    'locale': 'en-US',
    'currency': 'USD'
  },
  'DurationField': { 'invalidUnit': 'Invalid unit' },
  'TimeField': { 'invalidTime': 'Invalid time input' },
  'TimePicker': {
    'hour': 'Hour',
    'minute': 'Minute',
    'second': 'Second'
  },
  'List': {
    'loading': 'Loading...',
    'selectAll': 'Select All'
  },
  'GridBase': {
    'loadMask': 'Loading...',
    'syncMask': 'Saving changes, please wait...'
  },
  'PagingToolbar': {
    'firstPage': 'Go to first page',
    'prevPage': 'Go to previous page',
    'page': 'Page',
    'nextPage': 'Go to next page',
    'lastPage': 'Go to last page',
    'reload': 'Reload current page',
    'noRecords': 'No records to display',
    'pageCountTemplate': a => 'of ' + a.lastPage,
    'summaryTemplate': a => 'Displaying records ' + a.start + ' - ' + a.end + ' of ' + a.allCount
  },
  'PanelCollapser': {
    'Collapse': 'Collapse',
    'Expand': 'Expand'
  },
  'Popup': { 'close': 'Close' },
  'UndoRedo': {
    'Undo': 'Undo',
    'Redo': 'Redo',
    'UndoLastAction': 'Undo last action',
    'RedoLastAction': 'Redo last undone action',
    'NoActions': 'No items in the undo queue'
  },
  'FieldFilterPicker': {
    'equals': 'equals',
    'doesNotEqual': 'does not equal',
    'isEmpty': 'empty',
    'isNotEmpty': 'not empty',
    'contains': 'contains',
    'doesNotContain': 'does not contain',
    'startsWith': 'starts with',
    'endsWith': 'ends with',
    'isOneOf': 'one of',
    'isNotOneOf': 'not one of',
    'isGreaterThan': 'greater than',
    'isLessThan': 'less than',
    'isGreaterThanOrEqualTo': 'greater or equals',
    'isLessThanOrEqualTo': 'less or equals',
    'isBetween': 'between',
    'isNotBetween': 'not between',
    'isBefore': 'before',
    'isAfter': 'after',
    'isToday': 'today',
    'isTomorrow': 'tomorrow',
    'isYesterday': 'yesterday',
    'isThisWeek': 'this week',
    'isNextWeek': 'next week',
    'isLastWeek': 'last week',
    'isThisMonth': 'this month',
    'isNextMonth': 'next month',
    'isLastMonth': 'last month',
    'isThisYear': 'this year',
    'isNextYear': 'next year',
    'isLastYear': 'last year',
    'isYearToDate': 'year to date',
    'isTrue': 'true',
    'isFalse': 'false',
    'selectAProperty': 'Select property',
    'selectAnOperator': 'Select operator',
    'caseSensitive': 'Case-sensitive',
    'and': 'and',
    'dateFormat': 'D/M/YY',
    'selectValue': 'Select value',
    'selectOneOrMoreValues': 'Select value(s)',
    'enterAValue': 'Enter value',
    'enterANumber': 'Enter number',
    'selectADate': 'Select date',
    'selectATime': 'Select time'
  },
  'FieldFilterPickerGroup': { 'addFilter': 'Add filter' },
  'DateHelper': {
    'locale': 'en-US',
    'weekStartDay': 0,
    'nonWorkingDays': {
      0: true,
      6: true
    },
    'weekends': {
      0: true,
      6: true
    },
    'unitNames': [
      {
        'single': 'millisecond',
        'plural': 'ms',
        'abbrev': 'ms'
      },
      {
        'single': 'second',
        'plural': 'seconds',
        'abbrev': 's'
      },
      {
        'single': 'minute',
        'plural': 'minutes',
        'abbrev': 'min'
      },
      {
        'single': 'hour',
        'plural': 'hours',
        'abbrev': 'h'
      },
      {
        'single': 'day',
        'plural': 'days',
        'abbrev': 'd'
      },
      {
        'single': 'week',
        'plural': 'weeks',
        'abbrev': 'w'
      },
      {
        'single': 'month',
        'plural': 'months',
        'abbrev': 'mon'
      },
      {
        'single': 'quarter',
        'plural': 'quarters',
        'abbrev': 'q'
      },
      {
        'single': 'year',
        'plural': 'years',
        'abbrev': 'yr'
      },
      {
        'single': 'decade',
        'plural': 'decades',
        'abbrev': 'dec'
      }
    ],
    'unitAbbreviations': [
      ['mil'],
      [
        's',
        'sec'
      ],
      [
        'm',
        'min'
      ],
      [
        'h',
        'hr'
      ],
      ['d'],
      [
        'w',
        'wk'
      ],
      [
        'mo',
        'mon',
        'mnt'
      ],
      [
        'q',
        'quar',
        'qrt'
      ],
      [
        'y',
        'yr'
      ],
      ['dec']
    ],
    'parsers': {
      'L': 'MM/DD/YYYY',
      'LT': 'HH:mm A',
      'LTS': 'HH:mm:ss A'
    },
    'ordinalSuffix': a => {
      const b = [
        '11',
        '12',
        '13'
      ].find(e => a.endsWith(e));
      let c = 'th';
      if (!b) {
        const e = a[a.length - 1];
        c = {
          1: 'st',
          2: 'nd',
          3: 'rd'
        }[e] || 'th';
      }
      return a + c;
    }
  }
};
const En_default = LocaleHelper.publishLocale(locale);
const {toString: toString3} = Object.prototype;
const DATE_TYPE2 = toString3.call(new Date());
const tempDate = new Date();
const MS_PER_HOUR = 1000 * 60 * 60;
const defaultValue = (a, b) => isNaN(a) || a == null ? b : a;
const rangeFormatPartRe = /([ES]){([^}]+)}/g;
const enOrdinalSuffix = a => {
  const b = [
    '11',
    '12',
    '13'
  ].find(e => a.endsWith(e));
  let c = 'th';
  if (!b) {
    const e = a[a.length - 1];
    c = {
      1: 'st',
      2: 'nd',
      3: 'rd'
    }[e] || 'th';
  }
  return a + c;
};
const useIntlFormat = (a, b, c) => {
  const e = intlFormatterCache[a] || (intlFormatterCache[a] = new Intl.DateTimeFormat(locale2, b));
  return e.format(c);
};
const formatTime = (a, b, c, e = false) => {
  let f = useIntlFormat(a, b, c);
  if (/am|pm/i.test(f)) {
    f = f.replace(/^0/, '');
    if (e) {
      f = f.replace(/:00/, '');
    }
  }
  return f;
};
const getDayDiff = (a, b) => Math.floor((a.getTime() - b.getTime() - (a.getTimezoneOffset() - b.getTimezoneOffset()) * validConversions.minute.millisecond) / validConversions.day.millisecond) + 1;
const normalizeDay = a => a >= 0 ? a : a + 7;
const msRegExp = /([^\w])(S+)/gm;
const msReplacer = (a, b) => b + 'SSS';
const splitRegExp = /[:.\-/\s]/;
var locale2 = 'en-US';
let ordinalSuffix = enOrdinalSuffix;
let formatCache = {};
let formatRedirects = {};
var intlFormatterCache = {};
let parserCache = {};
const redirectFormat = a => {
  const b = intlFormatConfigs[a];
  if (!b) {
    throw new Error('Only international formats should be used here');
  }
  if (formatRedirects[a] !== void 0) {
    return formatRedirects[a];
  }
  const c = new Intl.DateTimeFormat(locale2, b), e = c.formatToParts(new Date(2001, 1, 2, 3, 4, 5, 6)).map(f => {
      const g = f.type, i = b[g];
      if (g === 'literal') {
        return f.value.replace(/,/g, '');
      } else if (g === 'day') {
        return i === 'numeric' ? 'D' : 'DD';
      } else if (g === 'month') {
        return i === 'short' ? 'MMM' : i === 'long' ? 'MMMM' : i === 'numeric' ? 'M' : 'MM';
      } else if (g === 'year') {
        return i === 'numeric' ? 'YYYY' : 'YY';
      }
    }).join('');
  return formatRedirects[a] = e;
};
const DEFAULT_YEAR = 2020;
const DEFAULT_MONTH = 0;
const DEFAULT_DAY = 1;
var intlFormatConfigs = {
  'l': {
    'year': 'numeric',
    'month': 'numeric',
    'day': 'numeric'
  },
  'll': {
    'year': 'numeric',
    'month': 'short',
    'day': 'numeric'
  }
};
var formats = {
  'M': a => a.getMonth() + 1,
  'Mo': a => ordinalSuffix(formats.M(a).toString()),
  'MM': a => (a.getMonth() + 1).toString().padStart(2, '0'),
  'MMM': a => useIntlFormat('MMM', { 'month': 'short' }, a),
  'MMMM': a => useIntlFormat('MMMM', { 'month': 'long' }, a),
  'Q': a => Math.ceil((a.getMonth() + 1) / 3),
  'Qo': a => ordinalSuffix(formats.Q(a).toString()),
  'D': a => a.getDate(),
  'Do': a => ordinalSuffix(formats.D(a).toString()),
  'DD': a => a.getDate().toString().padStart(2, '0'),
  'DDD': a => Math.ceil((new Date(a.getFullYear(), a.getMonth(), a.getDate(), 12, 0, 0) - new Date(a.getFullYear(), 0, 0, 12, 0, 0)) / validConversions.day.millisecond),
  'DDDo': a => ordinalSuffix(formats.DDD(a).toString()),
  'DDDD': a => formats.DDD(a).toString().padStart(3, '0'),
  'd': a => a.getDay(),
  'do': a => ordinalSuffix(a.getDay().toString()),
  'd1': a => useIntlFormat('d1', { 'weekday': 'narrow' }, a).substr(0, 1),
  'dd': a => formats.ddd(a).substring(0, 2),
  'ddd': a => useIntlFormat('ddd', { 'weekday': 'short' }, a),
  'dddd': a => useIntlFormat('dddd', { 'weekday': 'long' }, a),
  'u': a => {
    const b = intlFormatterCache.u || (intlFormatterCache.u = new Intl.DateTimeFormat('en-GB', {
        'timeZone': 'UTC',
        'year': 'numeric',
        'month': '2-digit',
        'day': '2-digit'
      })), c = b.formatToParts(a);
    return '' + c[4].value + c[2].value + c[0].value + 'Z';
  },
  'uu': a => {
    const b = intlFormatterCache.uu || (intlFormatterCache.uu = new Intl.DateTimeFormat('en-GB', {
        'timeZone': 'UTC',
        'hour12': false,
        'year': 'numeric',
        'month': '2-digit',
        'day': '2-digit',
        'hour': '2-digit',
        'minute': '2-digit',
        'second': '2-digit'
      })), c = b.formatToParts(a);
    return '' + c[4].value + c[2].value + c[0].value + 'T' + c[6].value + c[8].value + c[10].value + 'Z';
  },
  'e': a => a.getDay(),
  'E': a => a.getDay() + 1,
  'W': a => DateHelper.getWeekNumber(a)[1],
  'Wo': a => ordinalSuffix(formats.W(a).toString()),
  'WW': a => formats.W(a).toString().padStart(2, '0'),
  'Wp': a => DateHelper.localize('L{Week}') + ' ' + formats.W(a),
  'WWp': a => DateHelper.localize('L{Week}') + ' ' + formats.WW(a),
  'Wp0': a => '' + DateHelper.localize('L{Week}')[0] + formats.W(a),
  'WWp0': a => '' + DateHelper.localize('L{Week}')[0] + formats.WW(a),
  'Y': a => a.getFullYear(),
  'YY': a => (a.getFullYear() % 100).toString().padStart(2, '0'),
  'YYYY': a => a.getFullYear(),
  'A': a => a.getHours() < 12 ? 'AM' : 'PM',
  'a': a => a.getHours() < 12 ? 'am' : 'pm',
  'H': a => a.getHours(),
  'HH': a => a.getHours().toString().padStart(2, '0'),
  'h': a => a.getHours() % 12 || 12,
  'hh': a => formats.h(a).toString().padStart(2, '0'),
  'k': a => a.getHours() || 24,
  'kk': a => formats.k(a).toString().padStart(2, '0'),
  'K': a => formatTime('K', { 'hour': 'numeric' }, a),
  'KK': a => formatTime('KK', { 'hour': '2-digit' }, a),
  'm': a => a.getMinutes(),
  'mm': a => formats.m(a).toString().padStart(2, '0'),
  's': a => a.getSeconds(),
  'ss': a => formats.s(a).toString().padStart(2, '0'),
  'S': a => Math.floor(a.getMilliseconds() / 100).toString(),
  'SS': a => Math.floor(a.getMilliseconds() / 10).toString().padStart(2, '0'),
  'SSS': a => a.getMilliseconds().toString().padStart(3, '0'),
  'z': a => useIntlFormat('z', { 'timeZoneName': 'short' }, a),
  'zz': a => useIntlFormat('zz', { 'timeZoneName': 'long' }, a),
  'Z': a => DH2.getGMTOffset(a),
  'LT': a => formatTime('LT', {
    'hour': '2-digit',
    'minute': '2-digit'
  }, a),
  'LST': a => formatTime('LST', {
    'hour': 'numeric',
    'minute': '2-digit'
  }, a, true),
  'LTS': a => formatTime('LTS', {
    'hour': '2-digit',
    'minute': '2-digit',
    'second': '2-digit'
  }, a),
  'L': a => useIntlFormat('L', {
    'year': 'numeric',
    'month': '2-digit',
    'day': '2-digit'
  }, a),
  'l': a => useIntlFormat('l', intlFormatConfigs.l, a),
  'LL': a => useIntlFormat('LL', {
    'year': 'numeric',
    'month': 'long',
    'day': 'numeric'
  }, a),
  'll': a => useIntlFormat('ll', intlFormatConfigs.ll, a),
  'LLL': a => useIntlFormat('LLL', {
    'year': 'numeric',
    'month': 'long',
    'day': 'numeric',
    'hour': 'numeric',
    'minute': '2-digit'
  }, a),
  'lll': a => useIntlFormat('lll', {
    'year': 'numeric',
    'month': 'short',
    'day': 'numeric',
    'hour': 'numeric',
    'minute': '2-digit'
  }, a),
  'LLLL': a => useIntlFormat('LLLL', {
    'year': 'numeric',
    'month': 'long',
    'day': 'numeric',
    'hour': 'numeric',
    'minute': '2-digit',
    'weekday': 'long'
  }, a),
  'llll': a => useIntlFormat('llll', {
    'year': 'numeric',
    'month': 'short',
    'day': 'numeric',
    'hour': 'numeric',
    'minute': '2-digit',
    'weekday': 'short'
  }, a)
};
const formatKeys = Object.keys(formats).sort((c, e) => e.length - c.length);
const formatRegexp = '^(?:' + formatKeys.join('|') + ')';
const emptyFn2 = () => ({});
const isNumber = a => numberRegex.test(a);
const parseMilliseconds = a => isNumber(a) && { 'milliseconds': parseInt(a.padEnd(3, '0').substring(0, 3)) };
const parsers = {
  'YYYY': a => {
    const b = parseInt(a);
    return { 'year': b >= 1000 && b <= 9999 ? b : NaN };
  },
  'Y': a => ({ 'year': parseInt(a) }),
  'YY': a => {
    const b = parseInt(a);
    return { 'year': b + (b > 1968 ? 1900 : 2000) };
  },
  'M': a => ({ 'month': parseInt(a) - 1 }),
  'MM': a => ({ 'month': parseInt(a) - 1 }),
  'Mo': a => ({ 'month': parseInt(a) - 1 }),
  'MMM': a => {
    const b = (a || '').toLowerCase();
    for (const [c, e] of Object.entries(DateHelper._monthShortNamesIndex)) {
      if (b.startsWith(c)) {
        return { 'month': e.value };
      }
    }
  },
  'MMMM': a => {
    const b = (a || '').toLowerCase();
    for (const [c, e] of Object.entries(DateHelper._monthNamesIndex)) {
      if (b.startsWith(c)) {
        return { 'month': e.value };
      }
    }
  },
  'DD': a => ({ 'date': parseInt(a) }),
  'D': a => ({ 'date': parseInt(a) }),
  'Do': a => ({ 'date': parseInt(a) }),
  'DDD': emptyFn2,
  'DDDo': emptyFn2,
  'DDDD': emptyFn2,
  'd': emptyFn2,
  'do': emptyFn2,
  'd1': emptyFn2,
  'dd': emptyFn2,
  'ddd': emptyFn2,
  'dddd': emptyFn2,
  'Q': emptyFn2,
  'Qo': emptyFn2,
  'W': emptyFn2,
  'Wo': emptyFn2,
  'WW': emptyFn2,
  'e': emptyFn2,
  'E': emptyFn2,
  'HH': a => ({ 'hours': parseInt(a) }),
  'hh': a => ({ 'hours': parseInt(a) }),
  'mm': a => ({ 'minutes': parseInt(a) }),
  'H': a => ({ 'hours': parseInt(a) }),
  'm': a => ({ 'minutes': parseInt(a) }),
  'ss': a => ({ 'seconds': parseInt(a) }),
  's': a => ({ 'seconds': parseInt(a) }),
  'S': parseMilliseconds,
  'SS': parseMilliseconds,
  'SSS': parseMilliseconds,
  'A': a => ({ 'amPm': a.toLowerCase() }),
  'a': a => ({ 'amPm': a.toLowerCase() }),
  'L': 'MM/DD/YYYY',
  'LT': 'HH:mm A',
  'LTS': 'HH:mm:ss A',
  'l': {
    'type': 'dynamic',
    'parser': () => redirectFormat('l')
  },
  'll': {
    'type': 'dynamic',
    'parser': () => redirectFormat('ll')
  },
  'Z': a => {
    if (!a || !timeZoneRegEx.test(a) && a !== 'Z') {
      return null;
    }
    let b = 0;
    if (a !== 'Z') {
      const c = timeZoneRegEx.exec(a);
      if (c) {
        const e = c[1] === '+' ? 1 : -1, f = parseInt(c[2]) || 0, g = parseInt(c[3]) || 0;
        b = e * (f * 60 + g);
      } else {
        b = -1 * new Date().getTimezoneOffset();
      }
    }
    return { 'timeZone': b };
  }
};
const parserKeys = Object.keys(parsers).sort((c, e) => e.length - c.length);
const parserRegexp = new RegExp('(' + parserKeys.join('|') + ')');
const localeStrRegExp = new RegExp('^(LL|LLL|lll|LLLL|llll)$');
var validConversions = {
  'decade': {
    'decade': 1,
    'year': 10,
    'quarter': 40,
    'month': 120,
    'week': 520,
    'day': 3652,
    'hour': 24 * 3652,
    'minute': 1440 * 3652,
    'second': 86400 * 3652,
    'millisecond': 86400000 * 3652
  },
  'year': {
    'decade': 0.1,
    'year': 1,
    'quarter': 4,
    'month': 12,
    'week': 52,
    'day': 365,
    'hour': 24 * 365,
    'minute': 1440 * 365,
    'second': 86400 * 365,
    'millisecond': 86400000 * 365
  },
  'quarter': {
    'decade': 1 / 40,
    'year': 1 / 4,
    'quarter': 1,
    'month': 3,
    'week': 4,
    'day': 91,
    'hour': 24 * 91,
    'minute': 1440 * 91,
    'second': 86400 * 91,
    'millisecond': 86400000 * 91
  },
  'month': {
    'decade': 1 / 120,
    'year': 1 / 12,
    'quarter': 1 / 3,
    'month': 1,
    'week': 4,
    'day': -30,
    'hour': -24 * 30,
    'minute': -1440 * 30,
    'second': -86400 * 30,
    'millisecond': -86400000 * 30
  },
  'week': {
    'decade': -1 / 520,
    'year': -1 / 52,
    'quarter': -1 / 13,
    'month': -1 / 4,
    'day': 7,
    'hour': 168,
    'minute': 10080,
    'second': 604800,
    'millisecond': 604800000
  },
  'day': {
    'decade': -1 / 3652,
    'year': -1 / 365,
    'quarter': -1 / 91,
    'month': -1 / 30,
    'week': 1 / 7,
    'hour': 24,
    'minute': 1440,
    'second': 86400,
    'millisecond': 86400000
  },
  'hour': {
    'decade': -1 / (3652 * 24),
    'year': -1 / (365 * 24),
    'quarter': -1 / (91 * 24),
    'month': -1 / (30 * 24),
    'week': 1 / 168,
    'day': 1 / 24,
    'minute': 60,
    'second': 3600,
    'millisecond': 3600000
  },
  'minute': {
    'decade': -1 / (3652 * 1440),
    'year': -1 / (365 * 1440),
    'quarter': -1 / (91 * 1440),
    'month': -1 / (30 * 1440),
    'week': 1 / 10080,
    'day': 1 / 1440,
    'hour': 1 / 60,
    'second': 60,
    'millisecond': 60000
  },
  'second': {
    'decade': -1 / (3652 * 86400),
    'year': -1 / (365 * 86400),
    'quarter': -1 / (91 * 86400),
    'month': -1 / (30 * 86400),
    'week': 1 / 604800,
    'day': 1 / 86400,
    'hour': 1 / 3600,
    'minute': 1 / 60,
    'millisecond': 1000
  },
  'millisecond': {
    'decade': -1 / (3652 * 86400000),
    'year': -1 / (365 * 86400000),
    'quarter': -1 / (91 * 86400000),
    'month': -1 / (30 * 86400000),
    'week': 1 / 604800000,
    'day': 1 / 86400000,
    'hour': 1 / 3600000,
    'minute': 1 / 60000,
    'second': 1 / 1000
  }
};
const normalizedUnits = {
  'ms': 'millisecond',
  'milliseconds': 'millisecond',
  's': 'second',
  'seconds': 'second',
  'm': 'minute',
  'mi': 'minute',
  'min': 'minute',
  'minutes': 'minute',
  'h': 'hour',
  'hours': 'hour',
  'd': 'day',
  'days': 'day',
  'w': 'week',
  'weeks': 'week',
  'M': 'month',
  'mo': 'month',
  'mon': 'month',
  'months': 'month',
  'q': 'quarter',
  'quarters': 'quarter',
  'y': 'year',
  'years': 'year',
  'dec': 'decade',
  'decades': 'decade'
};
const withDecimalsDurationRegex = /^\s*([-+]?\d+(?:[.,]\d*)?|[-+]?(?:[.,]\d+))\s*([^\s]+)?/i;
const noDecimalsDurationRegex = /^\s*([-+]?\d+)(?![.,])\s*([^\s]+)?/i;
const canonicalUnitNames = [
  'millisecond',
  'second',
  'minute',
  'hour',
  'day',
  'week',
  'month',
  'quarter',
  'year',
  'decade'
];
const canonicalUnitAbbreviations = [
  ['mil'],
  [
    's',
    'sec'
  ],
  [
    'm',
    'min'
  ],
  [
    'h',
    'hr'
  ],
  ['d'],
  [
    'w',
    'wk'
  ],
  [
    'mo',
    'mon',
    'mnt'
  ],
  [
    'q',
    'quar',
    'qrt'
  ],
  [
    'y',
    'yr'
  ],
  ['dec']
];
const deltaUnits = [
  'decade',
  'year',
  'month',
  'week',
  'day',
  'hour',
  'minute',
  'second',
  'millisecond'
];
const dateProperties = [
  'milliseconds',
  'seconds',
  'minutes',
  'hours',
  'date',
  'month',
  'year'
];
const parseNumber = a => {
  const b = parseFloat(a);
  return isNaN(b) ? null : b;
};
var numberRegex = /^[0-9]+$/;
var timeZoneRegEx = /([+-])(\d\d):*(\d\d)*$/;
const unitMagnitudes = {
  'millisecond': 0,
  'second': 1,
  'minute': 2,
  'hour': 3,
  'day': 4,
  'week': 5,
  'month': 6,
  'quarter': 7,
  'year': 8,
  'decade': 9
};
const snapFns = {
  'round'(a, b = 1) {
    return Math.round(a / b) * b;
  },
  'floor'(a, b = 1) {
    return Math.floor(a / b) * b;
  },
  'ceil'(a, b = 1) {
    return Math.ceil(a / b) * b;
  }
};
const keyCache = {};
const _DateHelper = class _DateHelper extends Localizable_default() {
  static $name = 'DateHelper';
  static MS_PER_DAY = MS_PER_HOUR * 24;
  static set defaultFormat(a) {
    DH2._defaultFormat = a;
  }
  static get defaultFormat() {
    return DH2._defaultFormat || 'YYYY-MM-DDTHH:mm:ssZ';
  }
  static set defaultParseFormat(a) {
    this._defaultParseFormat = a;
  }
  static get defaultParseFormat() {
    return this._defaultParseFormat || this._defaultFormat || 'YYYY-MM-DDTHH:mm:ss.SSSZ';
  }
  static get today() {
    return _DateHelper.clearTime(new Date(), false);
  }
  static get tomorrow() {
    return _DateHelper.add(_DateHelper.today, 1, 'day');
  }
  static get yesterday() {
    return _DateHelper.add(_DateHelper.today, -1, 'day');
  }
  static buildParser(a) {
    const b = a.split(parserRegexp), c = [];
    if (b.length === 1 || localeStrRegExp.test(a)) {
      return [];
    } else {
      b.reduce((e, f, g, i) => {
        if (g !== 0 || f !== '') {
          if (parserRegexp.test(f)) {
            const j = this.localize('L{parsers}') || {}, k = j[f] || parsers[f];
            if (f === 'Z' && g < i.length - 2) {
              throw new Error('Invalid format ' + a + ' TimeZone (Z) must be last token');
            }
            const l = typeof k === 'function' || typeof k === 'string' ? k : k.parser();
            if (typeof l === 'string') {
              const n = DH2.buildParser(l), o = n.pop();
              delete o.last;
              c.push(...n);
              e = o;
            } else {
              e.pattern = f;
              e.fn = l;
            }
          } else {
            e.splitter = f;
            c.push(e);
            e = {};
          }
        } else if (Object.prototype.hasOwnProperty.call(e, 'pattern')) {
          c.push(e);
        }
        return e;
      }, {});
    }
    c[c.length - 1].last = true;
    return c;
  }
  static makeKey(a) {
    if (a.length === 10) {
      return a;
    }
    if (a.getTime) {
      a = a.getTime();
    }
    const b = keyCache[Math.trunc(a / MS_PER_HOUR)];
    if (b) {
      return b;
    }
    tempDate.setTime(a);
    const c = tempDate.getMonth() + 1, e = tempDate.getDate();
    return keyCache[Math.trunc(a / MS_PER_HOUR)] = tempDate.getFullYear() + '-' + (c < 10 ? '0' + c : c) + '-' + (e < 10 ? '0' + e : e);
  }
  static parseKey(a) {
    return DH2.parse(a, 'YYYY-MM-DD');
  }
  static parse(a, b = DH2.defaultParseFormat, c = false) {
    if (a instanceof Date) {
      return a;
    }
    if (typeof a !== 'string' || !a) {
      return null;
    }
    const e = {
      'year': null,
      'month': null,
      'date': null,
      'hours': null,
      'minutes': null,
      'seconds': null,
      'milliseconds': null
    };
    b = b.replace(msRegExp, msReplacer);
    let f = parserCache[b], g;
    if (!f) {
      f = parserCache[b] = DH2.buildParser(b);
    }
    if (a.includes('\u202F')) {
      a = a.replace(/\s/g, ' ');
    }
    f.reduce((j, k) => {
      let l;
      if (k.last) {
        Object.assign(e, k.fn(j));
      } else {
        let n;
        if (k.splitter === 'T' && j.indexOf('T') === -1) {
          n = j.indexOf(' ');
        } else {
          const q = j.indexOf('+');
          let {splitter: r} = k;
          if (!c && splitRegExp.test(r)) {
            r = splitRegExp;
          }
          n = k.splitter !== '' ? j.search(typeof r === 'string' ? StringHelper.escapeRegExp(r) : r) : ((l = k.pattern) == null ? void 0 : l.length) || -1;
          if (q > -1 && n > q) {
            n = -1;
          }
        }
        let o, p;
        if (n === -1 || k.pattern === 'SSS' && j.match(/^\d+Z$/)) {
          const u = j.split(/([Z\-+])/);
          if (u.length === 1) {
            o = j;
            p = '';
          } else {
            o = u[0];
            p = '' + u[1] + u[2];
          }
        } else {
          o = j.substring(0, n) || j;
          p = j.substring(n + k.splitter.length);
        }
        if (k.fn) {
          const v = k.fn(o);
          if (v) {
            Object.assign(e, v);
          } else {
            p = o + p;
          }
        }
        return p;
      }
    }, a);
    if (e.year && !e.date) {
      e.date = 1;
    }
    if (e.date > 31 || e.month > 12) {
      return null;
    }
    const i = DH2.create(e, c);
    if (i) {
      g = i;
    } else if (!c) {
      g = new Date(a);
    }
    return g;
  }
  static create(a, b = false) {
    const c = { ...a };
    let e = isNaN(c.year) || b && (isNaN(c.month) || isNaN(c.date)), f = false;
    if (!e) {
      let i = true;
      dateProperties.forEach(j => {
        if (!(j in c) || isNaN(c[j])) {
          c[j] = 0;
        }
        i = i && c[j] === null;
      });
      e = i;
    }
    if (e) {
      return null;
    }
    if (c.amPm === 'am') {
      c.hours = c.hours % 12;
    } else if (c.amPm === 'pm') {
      c.hours = c.hours % 12 + 12;
    }
    if ('timeZone' in c) {
      f = true;
      c.minutes -= c.timeZone;
    }
    if (b && (c.year == null || c.month == null || c.date == null)) {
      return null;
    }
    const g = [
      defaultValue(c.year, DEFAULT_YEAR),
      defaultValue(c.month, DEFAULT_MONTH),
      defaultValue(c.date, DEFAULT_DAY),
      c.hours,
      c.minutes,
      c.seconds,
      c.milliseconds
    ];
    return f ? new Date(Date.UTC(...g)) : new Date(...g);
  }
  static toUTC(a) {
    return new Date(Date.UTC(a.getUTCFullYear(), a.getUTCMonth(), a.getUTCDate(), a.getUTCHours(), a.getUTCMinutes(), a.getUTCSeconds(), a.getUTCMilliseconds()));
  }
  static format(a, b = DH2.defaultFormat) {
    if (!a || isNaN(a)) {
      return null;
    }
    let c = formatCache[b], e = '';
    if (!c) {
      c = formatCache[b] = [];
      for (let f = 0; f < b.length; f++) {
        const g = b.slice(f).match(formatRegexp), j = g == null ? void 0 : g[0];
        if (j) {
          const k = this.localize('L{formats}') || {}, l = k[j] || formats[j];
          c.push(l);
          f += j.length - 1;
        } else if (b[f] === '{') {
          const n = b.indexOf('}', f + 1);
          if (n === -1) {
            c.push(b.substr(f + 1));
            f = b.length;
          } else {
            c.push(b.substring(f + 1, n));
            f = n;
          }
        } else {
          c.push(b[f]);
        }
      }
    }
    c.forEach(o => {
      if (typeof o === 'string') {
        e += o;
      } else {
        e += o(a);
      }
    });
    return e;
  }
  static formatRange(a, b) {
    return b.replace(rangeFormatPartRe, (c, e, f) => _DateHelper.format(a[e === 'S' ? 0 : 1], f));
  }
  static asMilliseconds(a, b = null) {
    if (typeof a === 'string') {
      b = a;
      a = 1;
    }
    return DH2.as('millisecond', a, b);
  }
  static asMonths(a) {
    const b = DH2.as('ms', DH2.daysInMonth(a), 'day'), c = (a.valueOf() - DH2.startOf(a, 'month').valueOf()) / b;
    return a.getYear() * 12 + a.getMonth() + c;
  }
  static monthsToDate(a) {
    const b = Math.floor(a), c = a - b, e = new Date(0, b), f = DH2.as('ms', DH2.daysInMonth(e), 'days');
    e.setTime(e.getTime() + c * f);
    return e;
  }
  static formatDelta(a, b) {
    let c, e, f = ', ';
    if (typeof b === 'boolean') {
      c = b;
    } else if (b) {
      c = b.abbrev;
      f = b.unitSeparator || f;
    }
    const g = this.getDelta(a, b), i = [], j = (b == null ? void 0 : b.separator) || (c ? '' : ' ');
    for (e in g) {
      i.push('' + g[e] + j + e);
    }
    return (b == null ? void 0 : b.asString) === false ? i : i.join(f);
  }
  static getDelta(a, b) {
    let c, e, f, g, i, j, k;
    if (typeof b === 'boolean') {
      c = b;
    } else if (b) {
      c = b.abbrev;
      g = DH2.normalizeUnit(b.precision);
      j = b.maxUnit;
      k = !c && b.ignoreLocale;
    }
    const l = {}, n = c ? DH2.getShortNameOfUnit : DH2.getLocalizedNameOfUnit;
    const o = j ? deltaUnits.slice(deltaUnits.indexOf(j)) : deltaUnits;
    for (i of o) {
      e = DH2.as(i, a);
      f = g === i;
      e = Math[f ? 'round' : 'floor'](e);
      if (e || f && !l.length) {
        l[k ? i : n.call(DH2, i, e !== 1)] = e;
        a -= DH2.as('ms', e, i);
      }
      if (f || !a) {
        break;
      }
    }
    return l;
  }
  static as(a, b, c = 'ms') {
    if (typeof b === 'string') {
      b = DH2.parseDuration(b);
    }
    if (typeof b === 'object') {
      c = b.unit;
      b = b.magnitude;
    }
    if (a === c) {
      return b;
    }
    a = DH2.normalizeUnit(a);
    c = DH2.normalizeUnit(c);
    if (a === c) {
      return b;
    } else if (unitMagnitudes[c] > unitMagnitudes[a]) {
      return b * Math.abs(validConversions[c][a]);
    } else {
      return b / Math.abs(validConversions[a][c]);
    }
  }
  static formatContainsHourInfo(a) {
    const b = /(\\.)/g, c = /([HhKkmSsAa]|LT|L{3,}|l{3,})/;
    return c.test(a.replace(b, ''));
  }
  static is24HourFormat(a) {
    return DH2.format(DH2.getTime(13, 0, 0), a).includes('13');
  }
  static add(a, b, c = 'ms', e = true) {
    let f;
    if (typeof a === 'string') {
      f = DH2.parse(a);
    } else if (e) {
      f = new Date(a.getTime());
    } else {
      f = a;
    }
    if (typeof b === 'string') {
      const g = _DateHelper.parseDuration(b);
      b = g.magnitude;
      c = g.unit;
    } else if (b && typeof b === 'object') {
      c = b.unit;
      b = b.magnitude;
    }
    if (!c || b === 0) {
      return f;
    }
    c = DH2.normalizeUnit(c);
    switch (c) {
    case 'millisecond':
      f.setTime(f.getTime() + b);
      break;
    case 'second':
      f.setTime(f.getTime() + b * 1000);
      break;
    case 'minute':
      f.setTime(f.getTime() + b * 60000);
      break;
    case 'hour':
      f.setTime(f.getTime() + b * 3600000);
      break;
    case 'day':
      if (b % 1 === 0) {
        f.setDate(f.getDate() + b);
        if (f.getHours() === 23 && a.getHours() === 0) {
          f.setHours(f.getHours() + 1);
        }
      } else {
        f.setTime(f.getTime() + b * 86400000);
      }
      break;
    case 'week':
      f.setDate(f.getDate() + b * 7);
      break;
    case 'month': {
        let i = f.getDate();
        if (i > 28) {
          i = Math.min(i, DH2.getLastDateOfMonth(DH2.add(DH2.getFirstDateOfMonth(f), b, 'month')).getDate());
        }
        f.setDate(i);
        f.setMonth(f.getMonth() + b);
        break;
      }
    case 'quarter':
      DH2.add(f, b * 3, 'month', false);
      break;
    case 'year':
      f.setFullYear(f.getFullYear() + b);
      break;
    case 'decade':
      f.setFullYear(f.getFullYear() + b * 10);
      break;
    }
    return f;
  }
  static diff(a, b, c = 'ms', e = true) {
    c = DH2.normalizeUnit(c);
    if (!a || !b)
      return 0;
    let f;
    switch (c) {
    case 'year':
      f = DH2.diff(a, b, 'month') / 12;
      break;
    case 'quarter':
      f = DH2.diff(a, b, 'month') / 3;
      break;
    case 'month':
      f = (b.getFullYear() - a.getFullYear()) * 12 + (b.getMonth() - a.getMonth());
      if (f === 0 && e) {
        f = DH2.diff(a, b, 'day', e) / DH2.daysInMonth(a);
      }
      break;
    case 'week':
      f = DH2.diff(a, b, 'day') / 7;
      break;
    case 'day': {
        const g = a.getTimezoneOffset() - b.getTimezoneOffset();
        f = (b - a + g * 60 * 1000) / 86400000;
        break;
      }
    case 'hour':
      f = (b - a) / 3600000;
      break;
    case 'minute':
      f = (b - a) / 60000;
      break;
    case 'second':
      f = (b - a) / 1000;
      break;
    case 'millisecond':
      f = b - a;
      break;
    }
    return e ? f : Math.round(f);
  }
  static startOf(a, b = 'day', c = true, e = DH2.weekStartDay) {
    if (!a) {
      return null;
    }
    b = DH2.normalizeUnit(b);
    if (c) {
      a = DH2.clone(a);
    }
    switch (b) {
    case 'year':
      a.setMonth(0, 1);
      a.setHours(0, 0, 0, 0);
      return a;
    case 'quarter':
      a.setMonth((DH2.get(a, 'quarter') - 1) * 3, 1);
      a.setHours(0, 0, 0, 0);
      return a;
    case 'month':
      a.setDate(1);
      a.setHours(0, 0, 0, 0);
      return a;
    case 'week': {
        const f = a.getDay();
        let g = f - e;
        if (g < 0) {
          g += 7;
        }
        a.setDate(a.getDate() - g);
        a.setHours(0, 0, 0, 0);
        return a;
      }
    case 'day':
      a.setHours(0, 0, 0, 0);
      return a;
    case 'hour':
      a.getMinutes() > 0 && a.setMinutes(0);
    case 'minute':
      a.getSeconds() > 0 && a.setSeconds(0);
    case 'second':
      a.getMilliseconds() > 0 && a.setMilliseconds(0);
    case 'millisecond':
      return a;
    }
  }
  static endOf(a) {
    return new Date(a.getFullYear(), a.getMonth(), a.getDate() + 1);
  }
  static clone(a) {
    return new Date(a.getTime());
  }
  static clearTime(a, b = true) {
    if (!a) {
      return null;
    }
    if (b) {
      a = new Date(a.getTime());
    }
    a.setHours(0, 0, 0, 0);
    return a;
  }
  static midnight(a, b) {
    let c = DH2.clearTime(a);
    if (b && c < a) {
      c = DH2.add(c, 1, 'd');
    }
    return c;
  }
  static isMidnight(a) {
    return DH2.midnight(a).getTime() === a.getTime();
  }
  static getTimeOfDay(a, b = 'ms') {
    const c = a.getHours() * validConversions.hour.millisecond + a.getMinutes() * validConversions.minute.millisecond + a.getSeconds() * validConversions.second.millisecond + a.getMilliseconds();
    return b === 'ms' ? c : DH2.as(b, c, 'ms');
  }
  static set(a, b, c) {
    if (!b) {
      return a;
    }
    if (typeof b === 'string') {
      switch (DH2.normalizeUnit(b)) {
      case 'millisecond':
        if (c !== 0 || a.getMilliseconds() > 0) {
          a.setMilliseconds(c);
        }
        break;
      case 'second':
        if (c !== 0 || a.getSeconds() > 0) {
          a.setSeconds(c);
        }
        break;
      case 'minute':
        if (c !== 0 || a.getMinutes() > 0) {
          a.setMinutes(c);
        }
        break;
      case 'hour':
        a.setHours(c);
        break;
      case 'day':
      case 'date':
        a.setDate(c);
        break;
      case 'week':
        throw new Error('week not implemented');
      case 'month':
        a.setMonth(c);
        break;
      case 'quarter':
        a.setDate(1);
        a.setMonth((c - 1) * 3);
        break;
      case 'year':
        a.setFullYear(c);
        break;
      }
    } else {
      Object.entries(b).sort((e, f) => unitMagnitudes[e[0]] - unitMagnitudes[f[0]]).forEach(([e, f]) => {
        DH2.set(a, e, f);
      });
    }
    return a;
  }
  static setDateToMidday(a, b = true) {
    return DH2.set(DH2.clearTime(a, b), 'hour', 12);
  }
  static constrain(a, b, c) {
    if (b != null) {
      a = DH2.max(a, b);
    }
    return c == null ? a : DH2.min(a, c);
  }
  static getTime(a, b = 0, c = 0, e = 0) {
    if (a instanceof Date) {
      e = a.getMilliseconds();
      c = a.getSeconds();
      b = a.getMinutes();
      a = a.getHours();
    }
    return new Date(DEFAULT_YEAR, DEFAULT_MONTH, DEFAULT_DAY, a, b, c, e);
  }
  static copyTimeValues(a, b) {
    a.setHours(b.getHours());
    a.setMinutes(b.getMinutes());
    a.setSeconds(b.getSeconds());
    a.setMilliseconds(b.getMilliseconds());
    return a;
  }
  static combineDateAndTime(a, b) {
    return _DateHelper.copyTimeValues(new Date(a), b);
  }
  static get isDSTEnabled() {
    const a = new Date().getFullYear(), b = new Date(a, 0, 1), c = new Date(a, 6, 1);
    return b.getTimezoneOffset() !== c.getTimezoneOffset();
  }
  static isDST(a) {
    const b = a.getFullYear(), c = new Date(b, 0, 1), e = new Date(b, 6, 1);
    return a.getTimezoneOffset() < Math.max(c.getTimezoneOffset(), e.getTimezoneOffset());
  }
  static isBefore(a, b) {
    return a < b;
  }
  static isAfter(a, b) {
    return a > b;
  }
  static isEqual(a, b, c = null) {
    if (c === null) {
      return a && b && a.getTime() === b.getTime();
    }
    return DH2.startOf(a, c) - DH2.startOf(b, c) === 0;
  }
  static compare(a, b, c = null) {
    if (c) {
      a = DH2.startOf(a, c);
      b = DH2.startOf(b, c);
    }
    if (a < b)
      return -1;
    if (a > b)
      return 1;
    return 0;
  }
  static clamp(a, b, c) {
    if (!isNaN(a)) {
      if (b != null) {
        a = Math.max(a, b);
      }
      if (c != null) {
        a = Math.min(a, c);
      }
      return new Date(a);
    }
  }
  static isSameDate(a, b) {
    return DH2.compare(a, b, 'd') === 0;
  }
  static isSameTime(a, b) {
    return a.getHours() === b.getHours() && a.getMinutes() === b.getMinutes() && a.getSeconds() === b.getSeconds() && a.getMilliseconds() === b.getMilliseconds();
  }
  static isStartOf(a, b) {
    return DH2.isEqual(a, DH2.startOf(a, b));
  }
  static betweenLesser(a, b, c) {
    return b.getTime() <= a.getTime() && a.getTime() < c.getTime();
  }
  static betweenLesserEqual(a, b, c) {
    return b.getTime() <= a.getTime() && a.getTime() <= c.getTime();
  }
  static intersectSpans(a, b, c, e) {
    return DH2.betweenLesser(a, c, e) || DH2.betweenLesser(c, a, b);
  }
  static compareUnits(a, b) {
    return Math.sign(unitMagnitudes[DH2.normalizeUnit(a)] - unitMagnitudes[DH2.normalizeUnit(b)]);
  }
  static timeSpanContains(a, b, c, e) {
    return c - a >= 0 && b - e >= 0;
  }
  static get weekStartDay() {
    if (DH2._weekStartDay == null) {
      DH2._weekStartDay = this.localize('L{weekStartDay}') || 0;
    }
    return DH2._weekStartDay;
  }
  static get nonWorkingDays() {
    return { ...this.localize('L{nonWorkingDays}') };
  }
  static get nonWorkingDaysAsArray() {
    return Object.keys(this.nonWorkingDays).map(Number);
  }
  static get weekends() {
    return { ...this.localize('L{weekends}') };
  }
  static get(a, b) {
    switch (DH2.normalizeUnit(b)) {
    case 'millisecond':
      return a.getMilliseconds();
    case 'second':
      return a.getSeconds();
    case 'minute':
      return a.getMinutes();
    case 'hour':
      return a.getHours();
    case 'date':
    case 'day':
      return a.getDate();
    case 'week':
      return formats.W(a);
    case 'month':
      return a.getMonth();
    case 'quarter':
      return Math.floor(a.getMonth() / 3) + 1;
    case 'year':
      return a.getFullYear();
    }
    return null;
  }
  static daysInYear(a) {
    const b = a.getFullYear(), c = new Date(b + 1, 0, 1) - new Date(b, 0, 1);
    return this.as('day', c);
  }
  static daysInMonth(a) {
    return 32 - new Date(a.getFullYear(), a.getMonth(), 32).getDate();
  }
  static hoursInDay(a) {
    const b = a.getFullYear(), c = a.getMonth(), e = a.getDate(), f = new Date(b, c, e + 1) - new Date(b, c, e);
    return this.as('hour', f);
  }
  static getNormalizedUnitDuration(a, b) {
    let c;
    switch (b) {
    case 'month':
      c = DH2.asMilliseconds(DH2.daysInMonth(a), 'day');
      break;
    case 'year':
      c = DH2.asMilliseconds(DH2.daysInYear(a), 'day');
      break;
    case 'day':
      c = DH2.asMilliseconds(DH2.hoursInDay(a), 'hour');
      break;
    default:
      c = DH2.asMilliseconds(b);
    }
    return c;
  }
  static getFirstDateOfMonth(a) {
    return new Date(a.getFullYear(), a.getMonth(), 1);
  }
  static getLastDateOfMonth(a) {
    return new Date(a.getFullYear(), a.getMonth() + 1, 0);
  }
  static min(a, b) {
    return a.getTime() < b.getTime() ? a : b;
  }
  static max(a, b) {
    return a.getTime() > b.getTime() ? a : b;
  }
  static getNext(a, b, c = 1, e = DH2.weekStartDay) {
    if (b === 'week') {
      const f = DH2.clone(a), g = f.getDay();
      DH2.startOf(f, 'day', false);
      DH2.add(f, e - g + 7 * (c - (e <= g ? 0 : 1)), 'day', false);
      if (f.getDay() !== e) {
        DH2.add(f, 1, 'hour');
      }
      return f;
    }
    return DH2.startOf(DH2.add(a, c, b), b, false);
  }
  static isValidDate(a) {
    return DH2.isDate(a) && !isNaN(a);
  }
  static isDate(a) {
    return a && toString3.call(a) === DATE_TYPE2;
  }
  static getStartOfNextDay(a, b = false, c = false) {
    let e = DH2.add(c ? a : DH2.clearTime(a, b), 1, 'day');
    if (e.getDate() === a.getDate()) {
      const f = DH2.add(DH2.clearTime(a, b), 2, 'day').getTimezoneOffset(), g = a.getTimezoneOffset();
      e = DH2.add(e, g - f, 'minute');
    }
    return e;
  }
  static getEndOfPreviousDay(a, b = false) {
    const c = b ? a : DH2.clearTime(a, true);
    if (c - a) {
      return c;
    } else {
      return DH2.add(c, -1, 'day');
    }
  }
  static getWeekDescription(a, b = a) {
    const c = a.getMonth() === b.getMonth() ? _DateHelper.format(a, 'MMMM') : _DateHelper.format(a, 'MMM') + ' - ' + _DateHelper.format(b, 'MMM'), e = _DateHelper.getWeekNumber(a);
    return e[1] + ', ' + c + ' ' + e[0];
  }
  static getWeekNumber(a, b = _DateHelper.weekStartDay) {
    const c = new Date(a.getFullYear(), 0, 1), e = new Date(a.getFullYear(), 11, 31), f = normalizeDay(c.getDay() - b), g = normalizeDay(e.getDay() - b), i = getDayDiff(a, c);
    let j;
    if (f < 4) {
      j = Math.floor((i + f - 1) / 7) + 1;
    } else {
      j = Math.floor((i + f - 1) / 7);
    }
    if (j) {
      let l = a.getFullYear();
      if (j === 53 && g < 3) {
        l++;
        j = 1;
      }
      return [
        l,
        j
      ];
    }
    const k = _DateHelper.getWeekNumber(new Date(a.getFullYear() - 1, 11, 31))[1];
    return [
      a.getFullYear() - 1,
      k
    ];
  }
  static formatCount(a, b) {
    b = DH2.normalizeUnit(b);
    if (a !== 1)
      b += 's';
    return a + ' ' + b;
  }
  static getUnitToBaseUnitRatio(a, b, c = false) {
    a = DH2.normalizeUnit(a);
    b = DH2.normalizeUnit(b);
    if (a === b)
      return 1;
    if (validConversions[a] && validConversions[a][b] && (c || validConversions[a][b] > 0)) {
      return 1 / DH2.as(b, 1, a);
    }
    if (validConversions[b] && validConversions[b][a] && (c || validConversions[b][a] > 0)) {
      return DH2.as(a, 1, b);
    }
    return -1;
  }
  static getShortNameOfUnit(a) {
    a = DH2.parseTimeUnit(a);
    return DH2.unitLookup[a].abbrev;
  }
  static getLocalizedNameOfUnit(a, b = false) {
    const c = a.charAt(0) === a.charAt(0).toUpperCase();
    a = DH2.normalizeUnit(a);
    a = DH2.parseTimeUnit(a);
    a = DH2.unitLookup[a][b ? 'plural' : 'single'];
    if (c) {
      a = StringHelper.capitalize(a);
    }
    return a;
  }
  static normalizeUnit(a) {
    if (!a) {
      return null;
    }
    const b = a.toLowerCase();
    if (b === 'date') {
      return b;
    }
    return canonicalUnitNames.includes(b) ? b : normalizedUnits[a] || normalizedUnits[b];
  }
  static getUnitByName(a) {
    return DH2.normalizeUnit(a) || DH2.normalizeUnit(DH2.parseTimeUnit(a));
  }
  static getDurationInUnit(a, b, c, e) {
    return DH2.diff(a, b, c, e);
  }
  static doesUnitsAlign(a, b) {
    return !(a !== b && b === 'week');
  }
  static getSmallerUnit(a) {
    return canonicalUnitNames[unitMagnitudes[DH2.normalizeUnit(a)] - 1] || null;
  }
  static getLargerUnit(a) {
    return canonicalUnitNames[unitMagnitudes[DH2.normalizeUnit(a)] + 1] || null;
  }
  static round(a, b, c, e) {
    return DH2.snap('round', a, b, c, e);
  }
  static floor(a, b, c, e) {
    return DH2.snap('floor', a, b, c, e);
  }
  static ceil(a, b, c, e) {
    return DH2.snap('ceil', a, b, c, e);
  }
  static snap(a, b, c, e, f = DH2.weekStartDay) {
    const g = snapFns[a];
    if (typeof c === 'string') {
      c = DH2.parseDuration(c);
    }
    if (Objects.isObject(c)) {
      const j = c.magnitude || c.increment;
      switch (c.unit) {
      case 'week': {
          if (!e) {
            const k = b.getDay();
            e = DH2.add(DH2.clearTime(b), k >= f ? f - k : -(k - f + 7), 'day');
          }
          return DH2[a](b, j * 7 + ' days', e);
        }
      case 'month': {
          b = DH2.asMonths(b);
          let l;
          if (e) {
            e = DH2.asMonths(e);
            l = e + g(b - e, j);
          } else {
            l = g(b, j);
          }
          return DH2.monthsToDate(l);
        }
      case 'quarter':
        return DH2[a](b, j * 3 + ' months', e);
      case 'year':
        return DH2[a](b, j * 12 + ' months', e);
      case 'decade':
        return DH2[a](b, j * 10 + ' years', e);
      }
      c = DH2.as('ms', j, c.unit);
    }
    const i = b.getTimezoneOffset() * 1000 * 60;
    if (e) {
      const n = e.getTimezoneOffset() * 1000 * 60, o = b - e + n - i, p = new Date(e.getTime() + g(o, c)), q = n - p.getTimezoneOffset() * 1000 * 60;
      if (q) {
        DH2.add(p, -q, 'ms', false);
      }
      return new Date(p);
    } else {
      return new Date(g(b.valueOf() - i, c) + i);
    }
  }
  static parseDuration(a, b = true, c) {
    let e;
    const f = b ? withDecimalsDurationRegex : noDecimalsDurationRegex, g = f.exec(a);
    if (a == null || !g) {
      return null;
    }
    const i = parseNumber((e = g[1]) == null ? void 0 : e.replace(',', '.')), j = DH2.parseTimeUnit(g[2]) || c;
    if (!j) {
      return null;
    }
    return {
      'magnitude': i,
      'unit': j
    };
  }
  static parseTimeUnit(a) {
    const b = a == null ? null : DH2.durationRegEx.exec(a.toLowerCase());
    if (!b) {
      return null;
    }
    for (let c = 0; c < canonicalUnitNames.length; c++) {
      if (b[c + 1]) {
        return canonicalUnitNames[c];
      }
    }
  }
  static getGMTOffset(a = new Date()) {
    if (!a) {
      return;
    }
    const b = a.getTimezoneOffset();
    if (!b)
      return 'Z';
    return (b > 0 ? '-' : '+') + Math.abs(Math.trunc(b / 60)).toString().padStart(2, '0') + ':' + Math.abs(b % 60).toString().padStart(2, '0');
  }
  static fillDayNames() {
    const a = new Date('2000-01-01T12:00:00'), b = DH2._dayNames || [], c = DH2._dayShortNames || [];
    b.length = 0;
    c.length = 0;
    for (let e = 2; e < 9; e++) {
      a.setDate(e);
      b.push(DH2.format(a, 'dddd'));
      c.push(DH2.format(a, 'ddd'));
    }
    DH2._dayNames = b;
    DH2._dayShortNames = c;
  }
  static getDayNames() {
    return DH2._dayNames;
  }
  static getDayName(a) {
    return DH2._dayNames[a];
  }
  static getDayShortNames() {
    return DH2._dayShortNames;
  }
  static getDayShortName(a) {
    return DH2._dayShortNames[a];
  }
  static fillMonthNames() {
    const a = new Date('2000-01-15T12:00:00'), b = DH2._monthNames || [], c = DH2._monthShortNames || [], e = {}, f = {};
    b.length = 0;
    c.length = 0;
    for (let g = 0; g < 12; g++) {
      a.setMonth(g);
      const i = DH2.format(a, 'MMMM');
      b.push(i);
      const j = DH2.format(a, 'MMM');
      c.push(j);
      e[i.toLowerCase()] = {
        'name': i,
        'value': g
      };
      f[j.toLowerCase()] = {
        'name': j,
        'value': g
      };
    }
    DH2._monthNames = b;
    DH2._monthShortNames = c;
    DH2._monthNamesIndex = e;
    DH2._monthShortNamesIndex = f;
  }
  static getMonthShortNames() {
    return DH2._monthShortNames;
  }
  static getMonthShortName(a) {
    return DH2._monthShortNames[a];
  }
  static getMonthNames() {
    return DH2._monthNames;
  }
  static getMonthName(a) {
    return DH2._monthNames[a];
  }
  static set locale(a) {
    locale2 = a;
    intlFormatterCache = {};
    formatCache = {};
    formatRedirects = {};
  }
  static get locale() {
    return locale2;
  }
  static setupDurationRegEx(a = [], b = []) {
    const c = this, e = {};
    let f = '';
    for (let g = 0; g < b.length; g++) {
      const k = b[g], l = a[g];
      l.canonicalUnitName = canonicalUnitNames[g];
      e[l.single] = e[l.single.toUpperCase()] = e[l.canonicalUnitName] = e[l.canonicalUnitName.toUpperCase()] = l;
      f += (g ? '|' : '') + '(';
      for (let n = 0; n < k.length; n++) {
        f += k[n].toLowerCase() + '|';
      }
      locale2 = c.localize('L{locale}') || 'en-US';
      if (locale2 !== 'en-US') {
        const o = canonicalUnitAbbreviations[g];
        for (let p = 0; p < o.length; p++) {
          f += o[p] + '|';
        }
      }
      f += l.single.toLowerCase() + '|' + l.plural.toLowerCase() + '|' + l.canonicalUnitName + '|' + l.canonicalUnitName + 's)';
    }
    c.unitLookup = e;
    c.durationRegEx = new RegExp('^(?:' + f + ')$');
  }
  static applyLocale() {
    const a = this, b = a.localize('L{unitAbbreviations}') || [], c = a.unitNames = a.localize('L{unitNames}');
    if (c === 'unitNames') {
      return;
    }
    locale2 = a.localize('L{locale}') || 'en-US';
    if (locale2 === 'en-US') {
      ordinalSuffix = enOrdinalSuffix;
    } else {
      ordinalSuffix = a.localize('L{ordinalSuffix}') || ordinalSuffix;
    }
    formatCache = {};
    formatRedirects = {};
    parserCache = {};
    intlFormatterCache = {};
    DH2._weekStartDay = null;
    DH2.setupDurationRegEx(c, b);
    DH2.fillDayNames();
    DH2.fillMonthNames();
  }
};
var DateHelper = _DateHelper;
Helpers_default.register(DateHelper);
var DH2 = DateHelper;
DH2.useIntlFormat = useIntlFormat;
LocaleManager_default.ion({
  'locale': 'applyLocale',
  'prio': 1000,
  'thisObj': DH2
});
if (LocaleManager_default.locale) {
  DH2.applyLocale();
}
const DateSet = class _DateSet extends Set {
  get isDateSet() {
    return true;
  }
  add(a) {
    a = DateHelper.makeKey(a);
    if (!this.has(a)) {
      this.generation = (this.generation || 0) + 1;
    }
    return super.add(a);
  }
  delete(a) {
    a = DateHelper.makeKey(a);
    if (this.has(a)) {
      this.generation++;
    }
    return super.delete(a);
  }
  has(a) {
    return super.has(DateHelper.makeKey(a));
  }
  clear() {
    if (this.size) {
      this.generation++;
    }
    return super.clear();
  }
  equals(a) {
    Array.isArray(a) && (a = new _DateSet(a));
    return a.size === this.size && [...this].every(b => a.has(b));
  }
  get dates() {
    return [...this].sort().map(a => DateHelper.parseKey(a));
  }
};
DateHelper.DateSet = DateSet;
DateHelper._$name = 'DateHelper';
const {defineProperty: defineProperty3} = Reflect;
let performance2;
if (BrowserHelper.isBrowserEnv) {
  performance2 = globalThis.performance;
} else {
  performance2 = {
    'now'() {
      return new Date().getTime();
    }
  };
}
let globalDelays = null;
if (VersionHelper.isTestEnv) {
  const bryntum2 = globalThis.bryntum || (globalThis.bryntum = {});
  globalDelays = bryntum2.globalDelays = {
    'timeouts': new Map(),
    'intervals': new Map(),
    'animationFrames': new Map(),
    'idleCallbacks': new Map(),
    'isEmpty'(a = false) {
      return globalDelays.timeouts.size + globalDelays.animationFrames.size + globalDelays.idleCallbacks.size + (a ? globalDelays.intervals.size : 0) === 0;
    },
    'getFiltered'({
      ignoreTimeouts: ignoreTimeouts = [],
      maxDelay: maxDelay = 5000,
      includeIntervals: includeIntervals = false
    }) {
      const a = [], b = [
          'timeouts',
          'animationFrames',
          'idleCallbacks'
        ];
      if (includeIntervals) {
        b.push('intervals');
      }
      for (const c of b) {
        const e = globalDelays[c];
        for (const [, f] of e.entries()) {
          if (!ignoreTimeouts.includes(f.name) && (!Number.isInteger(f.delay) || f.delay < maxDelay)) {
            a.push(f);
          }
        }
      }
      return a;
    }
  };
}
const asapPromise = Promise.resolve();
const emptyArray3 = Object.freeze([]);
const emptyFn3 = () => {
};
const emptyObject5 = Object.freeze({});
const queueMicrotask = globalThis.queueMicrotask;
const makeInvoker = (a, b, c, e) => {
  const f = typeof b === 'string', g = (e == null ? void 0 : e.appendArgs) || emptyArray3, i = () => {
      c.timerId = null;
      c.lastCallTime = performance2.now();
      const j = c.args;
      c.args = null;
      if (f) {
        a[b](...j, ...g);
      } else {
        b.call(a, ...j, ...g);
      }
      c.called = true;
      ++c.calls;
    };
  a = (e == null ? void 0 : e.thisObj) || a;
  c.lastCallTime = -9000000000;
  c.calls = 0;
  c.suspended = e == null ? void 0 : e.suspended;
  c.invoker = i;
  i.wrapFn = c;
  return i;
};
const decorateWrapFn = (a, b, c = 'clearTimeout') => {
  b.cancel = () => {
    if (b.isPending) {
      a[c](b.timerId);
      b.args = b.timerId = null;
    }
  };
  b.flush = () => {
    if (b.isPending) {
      a[c](b.timerId);
      b.timerId = null;
      b.invoker();
    }
  };
  b.now = (...e) => {
    b.cancel();
    b.args = e;
    b.invoker();
  };
  b.resume = e => {
    const f = b.suspended;
    b.suspended = e || f < 1 ? 0 : f - 1;
  };
  b.suspend = () => {
    ++b.suspended;
  };
  b.immediate = false;
  b.suspended = b.suspended ? 1 : 0;
  b.timerId = null;
  defineProperty3(b, 'isPending', {
    'get'() {
      return b.timerId !== null;
    }
  });
  return b;
};
const Delayable_default = a => class b extends (a || Base) {
  static get $name() {
    return 'Delayable';
  }
  static get declarable() {
    return ['delayable'];
  }
  doDestroy() {
    super.doDestroy();
    this.clearDelayables();
  }
  clearDelayables() {
    const c = this, [e, f, g, i, j, k] = [
        c.idleCallbackIds,
        c.animationFrameIds,
        c.intervalIds,
        c.timeoutMap,
        c.timeoutIds,
        c.microtasksPending
      ];
    c.idleCallbackIds = c.animationFrameIds = c.intervalIds = c.timeoutMap = c.timeoutIds = c.microtasksPending = null;
    j == null ? void 0 : j.forEach((l, n) => {
      if (typeof l === 'function') {
        l();
      }
      clearTimeout(n);
      globalDelays == null ? void 0 : globalDelays.timeouts.delete(n);
    });
    i == null ? void 0 : i.forEach((l, n) => clearTimeout(n));
    g == null ? void 0 : g.forEach(l => {
      clearInterval(l);
      globalDelays == null ? void 0 : globalDelays.intervals.delete(l);
    });
    f == null ? void 0 : f.forEach(l => {
      cancelAnimationFrame(l);
      globalDelays == null ? void 0 : globalDelays.animationFrames.delete(l);
    });
    e == null ? void 0 : e.forEach(l => {
      cancelIdleCallback(l);
      globalDelays == null ? void 0 : globalDelays.idleCallbacks.delete(l);
    });
    k == null ? void 0 : k.forEach(l => l.cancel());
  }
  hasTimeout(c) {
    let e;
    return Boolean((e = this.timeoutMap) == null ? void 0 : e.has(c));
  }
  asap(c, e) {
    const f = this, g = (...j) => {
        if (!g.suspended) {
          g.called = false;
          g.args = j;
          if (g.immediate) {
            i();
          } else if (!g.isPending) {
            g.timerId = f.queueMicrotask(i);
          }
        }
      }, i = makeInvoker(f, c, g, e);
    return decorateWrapFn(f, g, 'cancelMicrotask');
  }
  queueMicrotask(c, e, f) {
    let g, i;
    if (!f && e && !Array.isArray(e)) {
      f = e;
      e = null;
    }
    f = f || emptyObject5;
    e = e || f.args || emptyArray3;
    const j = this, k = typeof c === 'string', l = k ? c : c.name, n = j.microtasksPending || (j.microtasksPending = new Map()), o = () => n.delete(g), p = () => {
        if (i === n.get(g)) {
          n.delete(g);
          if (k) {
            !j.isDestroyed && j[c](...e);
          } else {
            c.apply(j, e);
          }
        }
      };
    if (j.isDestroying && !f.runOnDestroy) {
      return emptyFn3;
    }
    g = f.cancelOutstanding ? l || c : o;
    i = {
      'key': g,
      'name': l,
      'fn': c,
      'args': e,
      'options': f,
      'cancel': o,
      'wrapFn': p
    };
    o.taskDef = i;
    queueMicrotask ? queueMicrotask(p) : asapPromise.then(p);
    n.set(g, i);
    return o;
  }
  cancelMicrotask(c) {
    c == null ? void 0 : c();
  }
  setTimeout({
    fn: c,
    delay: e,
    name: f,
    runOnDestroy: g,
    cancelOutstanding: i,
    args: j
  }) {
    if (arguments.length > 1 || typeof arguments[0] === 'function') {
      [c, e, f, g, i] = arguments;
    }
    if (typeof c === 'string') {
      f = c;
    } else if (!f) {
      f = c.name || c;
    }
    if (i) {
      this.clearTimeout(f);
    }
    const k = this, l = k.timeoutIds || (k.timeoutIds = new Map()), n = k.timeoutMap || (k.timeoutMap = new Map()), o = setTimeout(() => {
        if (typeof c === 'string') {
          c = k[f];
        }
        l == null ? void 0 : l.delete(o);
        n == null ? void 0 : n.delete(f);
        globalDelays == null ? void 0 : globalDelays.timeouts.delete(o);
        c.apply(k, j);
      }, e);
    l.set(o, g ? c : true);
    globalDelays == null ? void 0 : globalDelays.timeouts.set(o, {
      'fn': c,
      'delay': e,
      'name': f
    });
    if (f) {
      n.set(f, o);
    }
    return o;
  }
  clearTimeout(c) {
    let e;
    let f = c;
    if (typeof f === 'string') {
      if (this.timeoutMap) {
        f = this.timeoutMap.get(c);
        this.timeoutMap.delete(c);
      } else {
        return;
      }
    }
    clearTimeout(f);
    (e = this.timeoutIds) == null ? void 0 : e.delete(f);
    globalDelays == null ? void 0 : globalDelays.timeouts.delete(f);
  }
  clearInterval(c) {
    let e;
    clearInterval(c);
    (e = this.intervalIds) == null ? void 0 : e.delete(c);
    globalDelays == null ? void 0 : globalDelays.intervals.delete(c);
  }
  setInterval(c, e, f) {
    const g = setInterval(c, e);
    (this.intervalIds || (this.intervalIds = new Set())).add(g);
    globalDelays == null ? void 0 : globalDelays.intervals.set(g, {
      'fn': c,
      'delay': e,
      'name': f
    });
    return g;
  }
  requestAnimationFrame(c, e = [], f = this) {
    const g = this.animationFrameIds || (this.animationFrameIds = new Set()), i = requestAnimationFrame(() => {
        globalDelays == null ? void 0 : globalDelays.animationFrames.delete(i);
        g.delete(i) && c.apply(f, e);
      });
    g.add(i);
    globalDelays == null ? void 0 : globalDelays.animationFrames.set(i, {
      'fn': c,
      'extraArgs': e,
      'thisObj': f
    });
    return i;
  }
  requestIdleCallback(c, e = [], f = this) {
    const g = this.idleCallbackIds || (this.idleCallbackIds = new Set()), i = requestIdleCallback(() => {
        globalDelays == null ? void 0 : globalDelays.idleCallbacks.delete(i);
        g.delete(i) && c.apply(f, e);
      });
    g.add(i);
    globalDelays == null ? void 0 : globalDelays.idleCallbacks.set(i, {
      'fn': c,
      'extraArgs': e,
      'thisObj': f
    });
    return i;
  }
  createOnFrame(c, e = [], f = this, g) {
    let i;
    const j = (...k) => {
      if (i != null && g) {
        this.cancelAnimationFrame(i);
        i = null;
      }
      if (i == null) {
        i = this.requestAnimationFrame(() => {
          if (typeof c === 'string') {
            c = f[c];
          }
          i = null;
          k.push(...e);
          c.apply(f, k);
        });
      }
    };
    j.cancel = () => this.cancelAnimationFrame(i);
    return j;
  }
  cancelAnimationFrame(c) {
    let e;
    cancelAnimationFrame(c);
    (e = this.animationFrameIds) == null ? void 0 : e.delete(c);
    globalDelays == null ? void 0 : globalDelays.animationFrames.delete(c);
  }
  cancelIdleCallback(c) {
    let e;
    cancelIdleCallback(c);
    (e = this.idleCallbackIds) == null ? void 0 : e.delete(c);
    globalDelays == null ? void 0 : globalDelays.idleCallbacks.delete(c);
  }
  async nextAnimationFrame() {
    return new Promise(c => this.requestAnimationFrame(c));
  }
  buffer(c, e) {
    let f = e;
    if (e && typeof e !== 'number') {
      f = e.delay;
    } else {
      e = null;
    }
    const g = (...j) => {
        if (g.suspended) {
          return;
        }
        const {delay: k} = g;
        g.cancel();
        g.called = false;
        g.args = j;
        if (g.immediate || !k) {
          i();
        } else {
          g.timerId = this.setTimeout(i, k);
        }
      }, i = makeInvoker(this, c, g, e);
    g.delay = f;
    return decorateWrapFn(this, g);
  }
  raf(c, e) {
    let f = e;
    if (e && typeof e !== 'boolean') {
      f = e.cancelOutstanding;
    } else {
      e = null;
    }
    const g = (...j) => {
        if (g.suspended) {
          return;
        }
        if (g.cancelOutstanding || g.immediate) {
          g.cancel();
        }
        g.called = false;
        g.args = j;
        if (g.immediate) {
          i();
        } else if (!g.isPending) {
          g.timerId = this.requestAnimationFrame(i);
        }
      }, i = makeInvoker(this, c, g, e);
    g.cancelOutstanding = f;
    g.raf = true;
    return decorateWrapFn(this, g, 'cancelAnimationFrame');
  }
  idle(c, e) {
    let f = e;
    if (e && typeof e !== 'boolean') {
      f = e.cancelOutstanding;
    } else {
      e = null;
    }
    const g = (...j) => {
        if (g.suspended) {
          return;
        }
        if (g.cancelOutstanding || g.immediate) {
          g.cancel();
        }
        g.called = false;
        g.args = j;
        if (g.immediate) {
          i();
        } else if (!g.isPending) {
          g.timerId = this.requestIdleCallback(i);
        }
      }, i = makeInvoker(this, c, g, e);
    g.cancelOutstanding = f;
    this.setTimeout(() => this.idleCallbackIds.delete(g.timerId) && g.now(), 100);
    return decorateWrapFn(this, g, 'cancelIdleCallback');
  }
  throttle(c, e) {
    let f = e, g;
    if (e && typeof e !== 'number') {
      f = e.delay;
      g = e.throttled;
    } else {
      e = null;
    }
    const i = this, j = (...l) => {
        if (j.suspended) {
          return;
        }
        const {delay: n} = j, o = performance2.now() - j.lastCallTime;
        j.args = l;
        if (j.immediate || o >= n) {
          i.clearTimeout(j.timerId);
          k();
        } else {
          if (!j.isPending) {
            j.timerId = i.setTimeout(k, n - o);
            j.called = false;
          }
          if (g) {
            g.wrapFn.args = l;
            g();
          }
        }
      }, k = makeInvoker(i, c, j, e);
    j.delay = f;
    if (g) {
      g = makeInvoker(i, g, () => {
      }, e);
    }
    return decorateWrapFn(i, j);
  }
  static setupDelayable(c) {
    c.setupDelayableMethods(c.delayable);
  }
  static setupDelayableMethods(c, e = null) {
    const f = this, g = c.static, i = e || f.prototype;
    if (g) {
      delete c.static;
    }
    for (const j in c) {
      let k = c[j];
      const l = j + 'Now', n = typeof k;
      if (!i[l]) {
        i[l] = i[j];
      }
      if (n === 'number') {
        k = {
          'type': 'buffer',
          'delay': k
        };
      } else if (n === 'string') {
        k = { 'type': k };
      }
      defineProperty3(i, j, {
        'get'() {
          const o = this, p = o[k.type]((...q) => o[l](...q), k);
          defineProperty3(o, j, { 'value': p });
          return p;
        }
      });
    }
  }
  get widgetClass() {
  }
};
const StateStorage = class {
  get data() {
    return Object.create(null);
  }
  get keys() {
    return [];
  }
  clear() {
  }
  getItem(a) {
    return null;
  }
  removeItem(a) {
  }
  setItem(a, b) {
  }
};
StateStorage._$name = 'StateStorage';
const Local = class extends StateStorage {
  constructor(a) {
    super();
    this.prefix = a.prefix || '';
  }
  get isLocal() {
    return true;
  }
  get data() {
    const a = empty(), b = this.keys;
    for (const c of b) {
      a[c] = this.getItem(c);
    }
    return a;
  }
  get keys() {
    return getKeys(this.prefix, this.prefix.length);
  }
  clear() {
    const a = getKeys(this.prefix);
    for (const b of a) {
      localStorage.removeItem(b);
    }
  }
  getItem(a) {
    const b = localStorage.getItem(this.prefix + a);
    return b === null ? b : JSON.parse(b);
  }
  removeItem(a) {
    return localStorage.removeItem(this.prefix + a);
  }
  setItem(a, b) {
    return localStorage.setItem(this.prefix + a, JSON.stringify(b));
  }
};
const Memory = class extends StateStorage {
  constructor() {
    super();
    this.clear();
  }
  get isMemory() {
    return true;
  }
  get data() {
    return ObjectHelper.clone(this._data);
  }
  get keys() {
    return Object.keys(this._data);
  }
  clear() {
    this._data = empty();
  }
  getItem(a) {
    return a in this._data ? this._data[a] : null;
  }
  removeItem(a) {
    delete this._data[a];
  }
  setItem(a, b) {
    this._data[a] = b;
  }
};
var empty = () => Object.create(null);
var getKeys = (a, b = 0) => {
  const c = [], e = localStorage.length;
  for (let f, g = 0; g < e; ++g) {
    f = localStorage.key(g);
    f.startsWith(a) && c.push(f.slice(b));
  }
  return c;
};
const nullStorage = new StateStorage();
const storageTypes = {
  'local': Local,
  'memory': Memory
};
const StateProvider = class _StateProvider extends Base.mixin(Delayable_default, Events_default) {
  static get $name() {
    return 'StateProvider';
  }
  static get configurable() {
    return {
      'prefix': 'bryntum-state:',
      'storage': 'local'
    };
  }
  static get delayable() {
    return { 'writeStatefuls': 50 };
  }
  static get instance() {
    return this._instance;
  }
  static set instance(a) {
    if (a == null) {
      a = nullProvider;
    } else {
      if (typeof a === 'string' || ObjectHelper.isClass(a) || a instanceof StateStorage) {
        a = { 'storage': a };
      }
      if (ObjectHelper.isObject(a)) {
        a = new _StateProvider(a);
      }
    }
    this._instance = a;
  }
  static setup(a) {
    this.instance = a;
    return this.instance;
  }
  doDestroy() {
    self.writeStatefuls.flush();
    super.doDestroy();
  }
  get data() {
    return this.storage.data;
  }
  set data(a) {
    if (!a) {
      this.clear();
    } else {
      for (const b in a) {
        this.setValue(b, a[b]);
      }
    }
  }
  clear() {
    this.storage.clear();
    return this;
  }
  changeStorage(a) {
    if (a == null) {
      a = nullStorage;
    } else {
      if (typeof a === 'string') {
        if (!storageTypes[a]) {
          throw new Error('Invalid storage type "' + a + '" (expected one of: "' + Object.keys(storageTypes).join('", "') + '")');
        }
        a = storageTypes[a];
      }
      if (ObjectHelper.isClass(a)) {
        a = new a(this);
      }
    }
    return a;
  }
  saveStateful(a, b) {
    (this.pendingSaves || (this.pendingSaves = [])).push([
      a,
      b
    ]);
    this.writeStatefuls();
  }
  writeStatefuls() {
    const a = this, {pendingSaves: b} = a, c = b == null ? void 0 : b.length, e = [], f = [];
    a.pendingSaves = null;
    if (c) {
      for (let g, j, k, l = 0; l < c; ++l) {
        [j, g] = b[l];
        if (!j.isDestroying && j.isSaveStatePending) {
          k = j.saveState({
            ...g,
            'immediate': true
          });
          if (k) {
            e.push(k);
            f.push(j);
          }
        }
      }
      if (e.length) {
        a.trigger('save', {
          'stateIds': e,
          'saved': f
        });
      }
    }
  }
  getValue(a) {
    this.writeStatefuls.flush();
    return this.storage.getItem(a);
  }
  setValue(a, b) {
    const c = this, {storage: e} = c, f = c.getValue(a);
    if (b != null) {
      e.setItem(a, b);
      c.trigger('set', {
        'key': a,
        'value': b,
        'was': f
      });
    } else if (f !== null) {
      e.removeItem(a);
      c.trigger('remove', {
        'key': a,
        'was': f
      });
    }
    return c;
  }
};
var nullProvider = new StateProvider({ 'storage': nullStorage });
StateProvider._instance = nullProvider;
StateProvider._$name = 'StateProvider';
const primitiveRe = /boolean|number|string/;
const State_default = a => {
  let b;
  return b = class extends (a || Base) {
    afterConstruct() {
      super.afterConstruct();
      this.stateProvider && this.loadState();
    }
    finalizeInit() {
      this.stateProvider && this.loadState();
      super.finalizeInit();
    }
    get isStateful() {
      return true;
    }
    get isStatefulActive() {
      return !this.statefulSuspended && !this.isResponsivePending && !this.isResponsiveUpdating;
    }
    get state() {
      return this._state = this.getState();
    }
    set state(c) {
      this._state = c;
      if (c) {
        this.applyState(c);
      }
    }
    updateStatefulEvents(c) {
      let e;
      const f = this, g = {
          'name': 'statefulEvents',
          'thisObj': f
        };
      f.detachListeners(g.name);
      if (c) {
        if (typeof c === 'string') {
          c = StringHelper.split(c);
        } else if (!Array.isArray(c)) {
          c = ObjectHelper.getTruthyKeys(c);
        }
        if (c.length) {
          for (const i of c) {
            g[i] = 'onStatefulEvent';
          }
          (e = f.ion) == null ? void 0 : e.call(f, g);
        }
      }
    }
    get statefulId() {
      const c = this, {responsiveState: e} = c;
      let f = c.stateId;
      if (f == null && c.hasGeneratedId === false && c.stateful !== false) {
        f = c.id;
      }
      if (f && e) {
        f = f + '[' + e + ']';
      }
      return f;
    }
    get statefulness() {
      const {stateful: c} = this;
      return Array.isArray(c) ? ObjectHelper.createTruthyKeys(c) : c;
    }
    get stateProvider() {
      let c;
      return (c = this._stateProvider) != null ? c : this._stateId && StateProvider.instance;
    }
    applyState(c) {
      c = this.pruneState(c);
      if (c) {
        this.setConfig(c);
      }
    }
    getState() {
      const c = this, {
          initialConfig: e,
          statefulness: f,
          isConstructing: g
        } = c, {configs: i} = c.$meta, j = g ? Object.setPrototypeOf(e, c.$meta.config) : c;
      let k = null, l, n;
      if (f) {
        k = {};
        for (l in f) {
          if (f[l]) {
            n = j[l];
            if (n == null ? void 0 : n.isStateful) {
              n = n.state;
            } else if (!g) {
              if (ObjectHelper.isDate(n)) {
                n = DateHelper.format(n, 'YYYY-MM-DDTHH:mm:ssZ');
              }
              if (i[l].equal(n, e == null ? void 0 : e[l]) || !primitiveRe.test(typeof n)) {
                continue;
              }
            }
            k[l] = n;
          }
        }
      }
      return k;
    }
    loadState(c, e) {
      if (typeof c === 'boolean') {
        e = c;
        c = null;
      }
      const f = this, {statefulLoaded: g} = f;
      if (f.isStatefulActive && (e || !g)) {
        const i = f.loadStatefulData(c || (c = f.statefulId));
        if (!g && c) {
          f.defaultState = f.state;
          f.statefulLoaded = true;
        }
        if (i) {
          f.state = i;
        }
      }
    }
    loadStatefulData(c) {
      let e;
      c = this.isStatefulActive ? c || this.statefulId : null;
      return c && ((e = this.stateProvider) == null ? void 0 : e.getValue(c));
    }
    resetDefaultState() {
      if (this.defaultState) {
        this.state = this.defaultState;
      }
    }
    resumeStateful(c = false) {
      this.statefulSuspended = c ? 0 : Math.max(this.statefulSuspended - 1, 0);
    }
    saveState(c) {
      if (typeof c === 'string') {
        c = { 'id': c };
      } else {
        c = c || {};
      }
      const e = this, {stateProvider: f} = e, g = c.id || e.isStatefulActive && e.statefulId;
      if (g && f) {
        if (c.immediate) {
          e.isSaveStatePending = false;
          f.setValue(g, e.state);
        } else if (!e.isSaveStatePending) {
          e.isSaveStatePending = true;
          f.saveStateful(e, c);
        }
        return g;
      }
    }
    suspendStateful() {
      ++this.statefulSuspended;
    }
    onConfigChange({
      name: c,
      value: e,
      was: f,
      config: g
    }) {
      super.onConfigChange({
        'name': c,
        'value': e,
        'was': f,
        'config': g
      });
      if (!this.isConstructing && this.isStatefulActive && this.statefulId) {
        const {stateful: i} = this;
        if (Array.isArray(i) ? i.includes(c) : i == null ? void 0 : i[c]) {
          this.saveState();
        }
      }
    }
    onStatefulEvent() {
      if (!this.isConstructing) {
        this.saveState();
      }
    }
    pruneState(c) {
      const {statefulness: e} = this;
      if (e) {
        const f = {};
        for (const g in c) {
          if (e[g]) {
            f[g] = c[g];
          }
        }
        c = f;
      }
      return c;
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'State'), __publicField(b, 'configurable', {
    'stateful': {
      'value': null,
      '$config': { 'merge': 'classList' }
    },
    'statefulEvents': {
      '$config': { 'merge': 'classList' },
      'value': ['stateChange']
    },
    'stateId': null,
    'stateProvider': null
  }), __publicField(b, 'prototypeProperties', {
    'statefulLoaded': false,
    'statefulSuspended': 0
  }), b;
};
const idCounts = ObjectHelper.getPathDefault(globalThis, 'bryntum.idCounts', Object.create(null));
const idTypes = {
  'string': 1,
  'number': 1
};
const Identifiable_default = a => class b extends (a || Base) {
  static get $name() {
    return 'Identifiable';
  }
  static get declarable() {
    return ['identifiable'];
  }
  static get configurable() {
    return { 'id': '' };
  }
  static setupIdentifiable(c, e) {
    const {identifiable: f} = c;
    f.idMap = Object.create(null);
    Reflect.defineProperty(c, 'identifiable', {
      'get'() {
        return f;
      }
    });
  }
  doDestroy() {
    this.constructor.unregisterInstance(this);
    super.doDestroy();
  }
  changeId(c) {
    return (this.hasGeneratedId = !c) ? this.generateAutoId() : c;
  }
  updateId(c, e) {
    const f = this, g = f.constructor;
    e && g.unregisterInstance(f, e);
    if (!f.hasGeneratedId || g.identifiable.registerGeneratedId !== false) {
      g.registerInstance(f, c);
    }
  }
  generateAutoId() {
    return this.constructor.generateId('b-' + this.$$name.toLowerCase() + '-');
  }
  static get all() {
    return Object.values(this.identifiable.idMap);
  }
  static generateId(c = 'generatedId') {
    return c + (idCounts[c] = (idCounts[c] || 0) + 1);
  }
  static registerInstance(c, e = c.id) {
    const {idMap: f} = this.identifiable;
    if (e in f && !this.disableThrow) {
      throw new Error('Id ' + e + ' already in use');
    }
    f[e] = c;
  }
  static unregisterInstance(c, e = c.id) {
    const {idMap: f} = this.identifiable;
    if (idTypes[typeof c]) {
      delete f[c];
    } else if (f[e] === c) {
      delete f[e];
    }
  }
  static getById(c) {
    const e = this.identifiable.idMap;
    if (e) {
      return e[c];
    }
  }
  static get registeredInstances() {
    const c = this.identifiable.idMap;
    return c ? Object.values(c) : [];
  }
};
const STM_PROP = Symbol('STM_PROP');
const unrecordedFields = {
  'parentIndex': 1,
  'orderedParentIndex': 1
};
const ModelStm_default = a => class b extends (a || Base) {
  static get $name() {
    return 'ModelStm';
  }
  static get defaultConfig() {
    return { 'stm': null };
  }
  joinStore(c) {
    if (!this.stm) {
      this.stm = c.stm;
    }
  }
  unjoinStore(c, e = false) {
    let f;
    if (this.stm === c.stm) {
      this.stm = null;
    }
    (f = super.unjoinStore) == null ? void 0 : f.call(this, c, e);
  }
  get stm() {
    return this[STM_PROP];
  }
  set stm(c) {
    this[STM_PROP] = c;
  }
  beforeChronoFieldSet(c, e) {
    let f;
    const g = this;
    if (!g.inSetting && ((f = g.stm) == null ? void 0 : f.enabled) && !unrecordedFields[c] && !g.constructor.nonPersistableFields[c]) {
      if (g.getFieldDefinition(c)) {
        return {
          [c]: {
            'value': e,
            'oldValue': g.graph.activeTransaction.readCurrentOrProposedOrPrevious(g.$[c])
          }
        };
      }
    }
    return null;
  }
  afterChronoFieldSet(c, e, f) {
    f && this.afterSet(c, e, false, false, f, true);
  }
  shouldRecordFieldChange(c, e, f) {
    const g = this.firstStore;
    return !(this.constructor.nonPersistableFields[c] || e == null && f == null || e === f || e instanceof Date && f instanceof Date && e.getTime() === f.getTime() || c === 'id' || c === '$PhantomId' || c === 'parentId' && g && g.oldIdMap[e] === g.getById(f));
  }
  afterSet(c, e, f, g, i, j) {
    const {stm: k} = this, l = this.constructor.nonPersistableFields;
    if ((k == null ? void 0 : k.isBase) && k.shouldRecordAction && !unrecordedFields[c] && !l[c]) {
      if (i) {
        let n;
        const [o, p] = Object.keys(i).reduce((q, r) => {
          const {
            value: u,
            oldValue: v
          } = i[r];
          if (this.shouldRecordFieldChange(r, v, u)) {
            n = true;
            q[0][r] = u;
            q[1][r] = v;
          }
          return q;
        }, [
          {},
          {}
        ]);
        if (n) {
          k.onModelUpdate(this, o, p, j);
        }
      }
    }
  }
  beforeInsertChild(c) {
    let e;
    const f = ((e = super.beforeInsertChild) == null ? void 0 : e.call(this, c)) || [], {stm: g} = this;
    if (g == null ? void 0 : g.enabled) {
      f.push(c.reduce((i, j) => {
        if (j.root === this.root) {
          i.set(j, {
            'parent': j.parent,
            'index': j.parent ? j.parentIndex : void 0
          });
        }
        return i;
      }, new Map()));
    }
    return f;
  }
  afterInsertChild(c, e, f, g, i) {
    let j;
    const {stm: k} = this;
    if (k == null ? void 0 : k.shouldRecordAction) {
      const l = f.pop();
      if (g) {
        k.onModelInsertChild(this, c, g, l, i);
      }
    }
    (j = super.afterInsertChild) == null ? void 0 : j.call(this, c, e, f, g);
  }
  beforeRemoveChild(c, e) {
    const f = super.beforeRemoveChild ? super.beforeRemoveChild(c, e) : [], {stm: g} = this;
    if ((g == null ? void 0 : g.shouldRecordAction) && !e) {
      f.push(c.reduce((i, j) => {
        i.set(j, {
          'parentIndex': j.parentIndex,
          'orderedParentIndex': j.orderedParentIndex
        });
        return i;
      }, new Map()));
    }
    return f;
  }
  afterRemoveChild(c, e, f) {
    let g;
    const {stm: i} = this;
    if ((i == null ? void 0 : i.shouldRecordAction) && !f) {
      const j = e.pop();
      if (c && c.length) {
        i.onModelRemoveChild(this, c, j);
      }
    }
    (g = super.afterRemoveChild) == null ? void 0 : g.call(this, c, e, f);
  }
};
const defaultTraverseOptions = { 'includeFilteredOutRecords': false };
const fixTraverseOptions = a => {
  a = a || false;
  if (typeof a === 'boolean') {
    a = { 'includeFilteredOutRecords': a };
  }
  return a || defaultTraverseOptions;
};
const TreeNode_default = a => class b extends (a || Base) {
  static get $name() {
    return 'TreeNode';
  }
  static set convertEmptyParentToLeaf(c) {
    if (c === true) {
      c = {
        'onLoad': true,
        'onRemove': true
      };
    } else if (c === false) {
      c = {
        'onLoad': false,
        'onRemove': false
      };
    }
    this._convertEmptyParentToLeaf = c;
  }
  constructor(...c) {
    super(...c);
    if (this.children) {
      this.orderedChildren = this.orderedChildren || [];
    }
  }
  static get convertEmptyParentToLeaf() {
    return this._convertEmptyParentToLeaf || {
      'onLoad': false,
      'onRemove': false
    };
  }
  ingestChildren(c, e = this.stores) {
    let f, g, j;
    const {
        inProcessChildren: k,
        constructor: l
      } = this, n = e == null ? void 0 : e[0];
    if (c === true) {
      if (k) {
        return true;
      }
      return [];
    }
    if (c) {
      let o = ArrayHelper.asArray(c);
      if (!o.length) {
        return [];
      }
      const p = o.length, q = [];
      o = (f = n == null ? void 0 : n.cloneImmutable(o)) != null ? f : o;
      for (let r = 0, u; r < p; r++) {
        u = o[r];
        u = u.isModel ? u : (g = n == null ? void 0 : n.createRecord(u, false, true)) != null ? g : new l(u, null, null, true);
        u = (j = n == null ? void 0 : n.processRecord(u)) != null ? j : u;
        q.push(u);
      }
      if (this.children === true && n) {
        const v = n.createSorterFn(n.sorters);
        q.sort(v);
      }
      return q;
    }
  }
  processChildren(c = this.stores) {
    const e = this, {meta: f} = e;
    e.inProcessChildren = true;
    const g = e.ingestChildren(e.data[e.constructor.childrenField], c);
    if (g) {
      const {convertEmptyParentToLeaf: i} = e.constructor, j = i === true || i.onLoad;
      if (j ? g.length : Array.isArray(g)) {
        f.isLeaf = false;
        if (e.children === true) {
          e.children = [];
        } else if (g.length === 0) {
          e.children = g;
          return;
        }
        e.insertChild(g);
      } else if (g === true) {
        f.isLeaf = false;
        e.children = true;
      } else if (e.remoteChildCount > 0) {
        f.isLeaf = false;
      } else if (!e.isRoot) {
        f.isLeaf = e.constructor.convertEmptyParentToLeaf.onLoad;
      }
    }
    e.inProcessChildren = false;
  }
  ancestorsExpanded(c = this.firstStore) {
    const {parent: e} = this;
    return !e || e.isExpanded(c) && e.ancestorsExpanded(c);
  }
  isExpanded(c = this.firstStore) {
    const e = this.instanceMeta(c.id);
    if (!Object.prototype.hasOwnProperty.call(e, 'collapsed')) {
      e.collapsed = !this.expanded;
    }
    return !e.collapsed;
  }
  get expanded() {
    return this.data.expanded;
  }
  get childLevel() {
    let c = this, e = -1;
    while (c && !c.isRoot) {
      ++e;
      c = c.parent;
    }
    return e;
  }
  get isLeaf() {
    return this.meta.isLeaf !== false && !this.isRoot;
  }
  get isParent() {
    return !this.isLeaf;
  }
  get isLoaded() {
    return this.isParent && Array.isArray(this.children);
  }
  get hierarchyModificationDataToWrite() {
    const {parentIdField: c} = this.constructor, e = [
        c,
        'parentIndex',
        'orderedParentIndex'
      ].reduce((f, g) => {
        let i;
        if (g in this.meta.modified) {
          const j = this.constructor.getFieldDefinition(g);
          if (g === c) {
            f[(i = j == null ? void 0 : j.dataSource) != null ? i : c] = this.getFieldPersistentValue(g);
          } else if (j.persist) {
            f[j.dataSource] = this.getFieldPersistentValue(g);
          }
        }
        return f;
      }, {});
    return Object.keys(e).length > 0 ? e : null;
  }
  get descendantCount() {
    return this.getDescendantCount();
  }
  get visibleDescendantCount() {
    return this.getDescendantCount(true);
  }
  getDescendantCount(c = false, e = this.firstStore, f = false) {
    let {children: g} = this;
    if (f && this.unfilteredChildren) {
      g = this.unfilteredChildren;
    }
    if (!g || !Array.isArray(g) || c && !this.isExpanded(e)) {
      return 0;
    }
    return g.reduce((i, j) => i + j.getDescendantCount(c, e, f), g.length);
  }
  get allChildren() {
    return this.getAllChildren(false);
  }
  get allUnfilteredChildren() {
    return this.getAllChildren(true);
  }
  getAllChildren(c = false) {
    const {[c ? 'unfilteredChildren' : 'children']: e} = this;
    if (!e || e === true) {
      return [];
    }
    return e.reduce((f, g) => {
      f.push(g);
      f.push.apply(f, c ? g.allUnfilteredChildren : g.allChildren);
      return f;
    }, []);
  }
  get firstChild() {
    const {children: c} = this;
    return (c == null ? void 0 : c.length) && c[0] || null;
  }
  get lastChild() {
    const {children: c} = this;
    return (c == null ? void 0 : c.length) && c[c.length - 1] || null;
  }
  get previousSiblingsTotalCount() {
    let c = this.previousSibling, e = this.parentIndex;
    while (c) {
      e += c.descendantCount;
      c = c.previousSibling;
    }
    return e;
  }
  get previousOrderedSibling() {
    let c, e;
    return (e = (c = this.parent) == null ? void 0 : c.orderedChildren[this.orderedParentIndex - 1]) != null ? e : null;
  }
  get nextOrderedSibling() {
    let c, e;
    return (e = (c = this.parent) == null ? void 0 : c.orderedChildren[this.orderedParentIndex + 1]) != null ? e : null;
  }
  get root() {
    let c;
    return ((c = this.parent) == null ? void 0 : c.root) || this;
  }
  get parentId() {
    return this.parent && !this.parent.isAutoRoot ? this.parent.id : null;
  }
  set parentId(c) {
    const e = this, {parent: f} = e, g = c === null ? e.firstStore.rootNode : e.firstStore.getById(c);
    if (!(g === f || !f && !g)) {
      if (e.isBatchUpdating) {
        e.meta.batchChanges.parentId = c;
      } else {
        if (g) {
          g.appendChild(e);
        } else {
          e.parent.removeChild(e);
        }
      }
    }
  }
  static set parentIdField(c) {
    this._parentIdField = c;
    Object.defineProperty(this.prototype, c, {
      'set': function (e) {
        this.parentId = e;
      },
      'get': function () {
        return this.parentId;
      }
    });
  }
  static get parentIdField() {
    return this._parentIdField || 'parentId';
  }
  getChildren(c) {
    let e;
    if (c.includeFilteredOutRecords) {
      e = this.unfilteredChildren || this.children;
    } else if (c.useOrderedTree) {
      e = this.orderedChildren;
    } else {
      e = this.children;
    }
    return e;
  }
  traverse(c, e, f) {
    f = fixTraverseOptions(f);
    const g = this, j = g.getChildren(f);
    if (!e) {
      c.call(g, g);
    }
    for (let k = 0, n = j == null ? void 0 : j.length; k < n; k++) {
      j[k].traverse(c, false, f);
    }
  }
  traverseBefore(c, e, f) {
    f = fixTraverseOptions(f);
    const g = this, j = g.getChildren(f);
    for (let k = 0, n = j == null ? void 0 : j.length; k < n; k++) {
      j[k].traverse(c, false, f);
    }
    if (!e) {
      c.call(g, g);
    }
  }
  traverseWhile(c, e, f) {
    f = fixTraverseOptions(f);
    const g = this;
    let i = e || c.call(g, g) !== false;
    if (i) {
      const j = g.getChildren(f);
      if (j == null ? void 0 : j.length) {
        i = j.every(k => k.traverseWhile(c, false, f));
      }
    }
    return i;
  }
  bubble(c, e = false) {
    let f = this;
    if (!e) {
      c.call(f, f);
    }
    while (f.parent) {
      f = f.parent;
      c.call(f, f);
    }
  }
  bubbleWhile(c, e = false) {
    let f = this, g = true;
    if (!e) {
      g = c.call(f, f);
    }
    while (g && f.parent) {
      f = f.parent;
      g = c.call(f, f);
    }
    return g;
  }
  contains(c, e = false, f = void 0) {
    if (c && typeof c === 'object') {
      c = c.id;
    }
    return !this.traverseWhile(g => g.id != c, e, f);
  }
  getTopParent(c) {
    let e;
    if (c) {
      e = [];
      this.bubbleWhile(f => {
        e.push(f);
        return f.parent && !f.parent.isRoot;
      });
    } else {
      e = null;
      this.bubbleWhile(f => {
        e = f;
        return f.parent && !f.parent.isRoot;
      });
    }
    return e;
  }
  appendChild(c, e = false, f = {}) {
    return this.insertChild(c, null, e, f);
  }
  insertChild(c, e = null, f = false, g = {}) {
    let i, j, k, l, n, o, p, q;
    const r = this, u = Array.isArray(c);
    c = ArrayHelper.asArray(c);
    if (typeof e === 'number') {
      e = (j = (i = r.children) == null ? void 0 : i[e]) != null ? j : null;
    }
    if (!f && ((k = r.stores) == null ? void 0 : k.every(A => A.trigger('beforeAdd', {
        'records': c,
        'parent': r
      }) !== false)) === false) {
      return null;
    }
    c = r.ingestChildren(c);
    const v = (o = (n = e == null ? void 0 : e.parentIndex) != null ? n : (l = r.children) == null ? void 0 : l.length) != null ? o : 0, w = (p = r.beforeInsertChild) == null ? void 0 : p.call(r, c), z = r.internalAppendInsert(c, e, f, g);
    if (z.length) {
      r.convertToParent(f);
    }
    (q = r.afterInsertChild) == null ? void 0 : q.call(r, v, c, w, z, g.orderedBeforeNode);
    return u || !z ? z : z[0];
  }
  convertToParent(c = false) {
    const e = this, f = e.isLeaf;
    e.meta.isLeaf = false;
    if (!e.children) {
      e.children = [];
    }
    if (f && !e.root.isLoading && !c) {
      e.signalNodeChanged({
        'isLeaf': {
          'value': false,
          'oldValue': true
        }
      });
    }
  }
  signalNodeChanged(c, e = this.stores) {
    e == null ? void 0 : e.forEach(f => {
      f.trigger('update', {
        'record': this,
        'records': [this],
        'changes': c
      });
      f.trigger('change', {
        'action': 'update',
        'record': this,
        'records': [this],
        'changes': c
      });
    });
  }
  tryInsertChild() {
    return this.insertChild(...arguments);
  }
  internalAppendInsert(c, e, f, g) {
    const j = this, {
        stores: k,
        root: l,
        children: n
      } = j, {firstStore: o} = l, {parentIdField: p} = j.constructor, q = j.id;
    let r, u, v, w, z, A = {};
    if (!l.isLoading && o) {
      z = [];
      for (v = 0; v < c.length; v++) {
        const B = c[v];
        A[B.id] = B.root === l;
        z[v] = B.parentIndex;
      }
    }
    if (e && e.parent !== j) {
      e = null;
    }
    if (n) {
      const C = e ? e.parentIndex : n.length;
      if (n[u = C] === c[0] || n[u = C - 1] === c[0]) {
        for (r = true, v = 0; r && v < c.length; v++) {
          if (c[v] !== n[u + v]) {
            r = false;
          }
        }
      }
    }
    if (r) {
      return c;
    }
    for (v = 0; v < c.length; v++) {
      const E = c[v], F = E.parent;
      if (o && !l.isLoading) {
        E.traverse(I => {
          if (I.root === l) {
            A[I.id] = true;
          }
        });
      }
      const G = Boolean(A[E.id]), H = F === j && G;
      if ((F == null ? void 0 : F.removeChild(E, G, f, {
          'isInserting': true,
          'skipIndexUpdate': H,
          ...g
        })) === false) {
        if (!w) {
          c = c.slice();
          w = true;
        }
        c.splice(v--, 1);
      } else {
        E.parent = j;
        E.data[p] = q;
        const {meta: I} = E;
        if (I.modified[p] === q && !F) {
          I.oldParentId = q;
        }
        if (F) {
          I.oldParentId = F.id;
        }
      }
    }
    if (c.length) {
      if (!Array.isArray(n)) {
        j.children = [];
      }
      if (!Array.isArray(j.orderedChildren)) {
        j.orderedChildren = [];
      }
      const J = j.addToChildren(e, c, g);
      k == null ? void 0 : k.forEach(K => {
        if (!K.isChained) {
          c.forEach(L => L.joinStore(K));
          K.onNodeAddChild(j, c, J, A, f);
          c.forEach((L, N) => {
            if (L.meta.oldParentId != null && (!(j.inProcessChildren || j.isLoading) || A[L.id])) {
              const O = {
                  [p]: q,
                  [j.getDataSource('parentIndex')]: L.parentIndex
                }, P = {}, {
                  modified: Q,
                  oldParentId: R
                } = L.meta, S = z[N];
              delete L.meta.oldParentId;
              if (j.id !== R) {
                P[p] = {
                  'value': q,
                  'oldValue': R
                };
              }
              if (L.parentIndex !== S) {
                P.parentIndex = {
                  'value': L.parentIndex,
                  'oldValue': S
                };
              }
              if (Q[p] === j.id) {
                Reflect.deleteProperty(Q, p);
              } else if (!(p in Q)) {
                Q[p] = R;
              }
              if (A[L.id]) {
                const T = K.getById(R);
                if (T.isLeaf && !f) {
                  T.signalNodeChanged({
                    'isLeaf': {
                      'value': true,
                      'oldValue': false
                    }
                  }, [K]);
                }
              }
              L.afterChange(O, P);
            }
            L.traverse(U => {
              if (!U.ignoreBag && !U.isLinked) {
                K.updateModifiedBagForRecord(U);
              }
            });
          });
        }
      });
    }
    return c;
  }
  removeChild(c, e = false, f = false, g = {}) {
    let i, j;
    const k = this, l = [], n = k.isLeaf, {
        children: o,
        stores: p
      } = k;
    c = ArrayHelper.asArray(c);
    c = c.filter(u => u.parent === k);
    if (!f && p) {
      for (const r of p) {
        if (!r.isChained && r.trigger('beforeRemove', {
            'parent': k,
            'records': c,
            'isMove': e
          }) === false) {
          return false;
        }
      }
    }
    const q = (i = k.beforeRemoveChild) == null ? void 0 : i.call(k, c, e);
    for (const u of c) {
      const {parentIdField: v} = u.constructor, {modified: w} = u.meta, z = u.parent ? u.parent.id : null;
      if (!(v in w) && !u.isLinked) {
        w[v] = z;
      }
      const A = k.removeFromChildren(u, g);
      p == null ? void 0 : p.forEach(B => {
        if (!B.isChained) {
          const {isRemoving: C} = B;
          B.isRemoving = true;
          l.push(...B.onNodeRemoveChild(k, [u], A, {
            'isMove': e,
            'silent': f
          }));
          B.isRemoving = C;
        }
      });
      if (!e) {
        u.parent = u.parentIndex = u.unfilteredIndex = u.nextSibling = u.previousSibling = null;
        u.data[v] = null;
      }
    }
    if ((k.unfilteredChildren || o).length === 0 && k.constructor.convertEmptyParentToLeaf.onRemove && !k.isRoot) {
      k.meta.isLeaf = true;
    }
    if (k.isLeaf !== n && !f && !e) {
      k.signalNodeChanged({
        'isLeaf': {
          'value': true,
          'oldValue': false
        }
      });
    }
    (j = k.afterRemoveChild) == null ? void 0 : j.call(k, c, q, e);
    return l;
  }
  clearParentId() {
    const c = this;
    Reflect.deleteProperty(c.data, c.parentIdField);
    Reflect.deleteProperty(c.originalData, c.parentIdField);
    if (c.meta.modified) {
      Reflect.deleteProperty(c.meta.modified, c.parentIdField);
    }
  }
  replaceChildren(c) {
    this.clearChildren();
    this.data[this.constructor.childrenField] = c;
    this.processChildren();
    return this.children;
  }
  clearChildren(c = false) {
    const e = this, {stores: f} = e, g = e.unfilteredChildren || e.children;
    e.children = [];
    e.orderedChildren = [];
    if (g && g !== true) {
      f.forEach(j => {
        if (!j.isChained) {
          j.onNodeRemoveChild(e, g, 0, {
            'unfiltered': true,
            'silent': c
          });
        }
      });
      if (e.unfilteredChildren) {
        e.unfilteredChildren = [];
      }
    }
    const {convertEmptyParentToLeaf: i} = e.constructor;
    if (i === true || i.onRemove) {
      e.meta.isLeaf = true;
    }
  }
  clear() {
    let c;
    const e = this, {stores: f} = e, g = (c = e.unfilteredChildren || e.children) == null ? void 0 : c.slice();
    if (!e.isRoot || !g) {
      return;
    }
    for (const i of f) {
      if (!i.isChained) {
        if (i.trigger('beforeRemove', {
            'parent': e,
            'records': g,
            'isMove': false,
            'removingAll': true
          }) === false) {
          return false;
        }
      }
    }
    e.children.length = e.orderedChildren.length = 0;
    if (e.unfilteredChildren) {
      e.unfilteredChildren.length = 0;
    }
    f.forEach(j => {
      g.forEach(k => {
        if (k.stores.includes(j)) {
          k.unjoinStore(j);
        }
        k.parent = k.parentIndex = k.nextSibling = k.previousSibling = null;
      });
      j.storage.suspendEvents();
      j.storage.clear();
      j.storage.resumeEvents();
      j.added.clear();
      j.modified.clear();
      j.trigger('removeAll');
      j.trigger('change', { 'action': 'removeall' });
    });
  }
  updateChildrenIndices(c, e, f = false) {
    let g = null;
    for (let j = 0; j < c.length; j++) {
      const k = c[j], l = k[e];
      if (e === 'parentIndex' || e === 'orderedParentIndex') {
        if (l === void 0 || f) {
          k.setData(e, j);
        } else if (l !== j) {
          k.set(e, j, true);
        }
      } else {
        k[e] = j;
      }
      if (e === 'parentIndex') {
        k.previousSibling = g;
        if (g) {
          g.nextSibling = k;
        }
        if (j === c.length - 1) {
          k.nextSibling = null;
        }
        g = k;
      }
    }
  }
  addToChildren(c, e, f = {}) {
    let g, i;
    const j = this, k = [
        [
          j.children,
          'parentIndex',
          c
        ],
        [
          j.unfilteredChildren,
          'unfilteredIndex',
          c
        ],
        [
          j.orderedChildren,
          'orderedParentIndex',
          (f == null ? void 0 : f.orderedBeforeNode) === void 0 ? (f == null ? void 0 : f.orderedParentIndex) !== void 0 ? j.orderedChildren[(g = f.orderedParentIndex.index) != null ? g : f.orderedParentIndex] : c : f == null ? void 0 : f.orderedBeforeNode
        ]
      ];
    for (const l of k) {
      const [n, o, p] = l;
      if (n) {
        let q;
        if (p) {
          q = n.indexOf(p);
          if (q === -1) {
            q = n.length;
          }
        } else {
          q = n.length;
        }
        l.push(q);
        n.splice(q, 0, ...e);
        if (!((f == null ? void 0 : f.skipIndexUpdate) || ((i = f == null ? void 0 : f[o]) == null ? void 0 : i.skip))) {
          j.updateChildrenIndices(n, o);
        }
      }
    }
    return k[0][3];
  }
  removeFromChildren(c, e) {
    let f;
    const g = [
      [
        this.children,
        'parentIndex'
      ],
      [
        this.unfilteredChildren,
        'unfilteredIndex'
      ],
      [
        this.orderedChildren,
        'orderedParentIndex'
      ]
    ];
    for (const i of g) {
      const [j, k] = i;
      if (j) {
        const l = j.indexOf(c);
        i.push(l);
        if (l > -1) {
          j.splice(l, 1);
          if (!((e == null ? void 0 : e.skipIndexUpdate) || ((f = e == null ? void 0 : e[k]) == null ? void 0 : f.skip))) {
            this.updateChildrenIndices(j, k);
          }
        }
      }
    }
    return g[0][2];
  }
  sortOrderedChildren(c = true, e = false) {
    const f = [];
    if (!this.isLeaf) {
      this.orderedChildren.sort((g, i) => {
        let j, k;
        if (e) {
          const l = (j = g.meta.modified.orderedParentIndex) != null ? j : g.orderedParentIndex, n = (k = i.meta.modified.orderedParentIndex) != null ? k : i.orderedParentIndex, o = l - n;
          if (o !== 0) {
            f.push(g);
            f.push(i);
          }
          return o;
        } else {
          return g.orderedParentIndex - i.orderedParentIndex;
        }
      });
      if (c) {
        this.orderedChildren.forEach(g => {
          f.push(...g.sortOrderedChildren(c, e));
        });
      }
      this.updateChildrenIndices(this.orderedChildren, 'orderedParentIndex', true);
    }
    return new Set(f);
  }
  sortVisibleChildren(c = true, e) {
    const f = this;
    if (!f.isLeaf) {
      f.children.sort(e);
      if (c) {
        f.orderedChildren.forEach(g => g.sortVisibleChildren(c, e));
      }
      f.updateChildrenIndices(f.children, 'parentIndex', true);
    }
  }
  sortUnfilteredChildren(c = true, e) {
    const f = this;
    if (!f.isLeaf && f.unfilteredChildren) {
      f.unfilteredChildren.sort(e);
      if (c) {
        f.orderedChildren.forEach(g => g.sortUnfilteredChildren(c, e));
      }
      f.updateChildrenIndices(f.unfilteredChildren, 'unfilteredIndex', true);
    }
  }
  unjoinStore(c, e = false) {
    let f;
    const g = this;
    if (g.unfilteredChildren) {
      g.children = g.unfilteredChildren.slice();
      g.unfilteredChildren = null;
    }
    (f = super.unjoinStore) == null ? void 0 : f.call(this, c, e);
  }
};
const propertyOverrides = {
  'id': 1,
  'stores': 1,
  'parentIndex': 1,
  'parent': 1,
  'previousSibling': 1,
  'nextSibling': 1,
  'unfilteredIndex': 1
};
const proxyConfig = {
  'get'(a, b) {
    if (b === 'proxyMeta') {
      return this.proxyMeta;
    }
    if (b === 'constructor') {
      return a.constructor;
    }
    if (b === 'setData') {
      return this.setDataOverride;
    }
    if (b === 'set') {
      return this.setOverride;
    }
    if (b === 'instanceMeta') {
      return this.instanceMetaOverride;
    }
    if (propertyOverrides[b]) {
      return this.proxyMeta.data[b];
    }
    return Reflect.get(a, b, this.proxyRecord);
  },
  'set'(a, b, c) {
    if (propertyOverrides[b]) {
      this.proxyMeta.data[b] = c;
    } else {
      a[b] = c;
    }
    return true;
  },
  'instanceMetaOverride'(a) {
    const {proxyMeta: b} = this, c = a.id || a, e = b.map || (b.map = ObjectHelper.clone(b.originalRecord.instanceMeta(c)));
    return e[c] || (e[c] = {});
  },
  'setDataOverride'(a, b) {
    if (a === 'parentIndex') {
      this.proxyMeta.data.parentIndex = b;
    } else {
      this.proxyMeta.originalRecord.setData(a, b);
    }
  },
  'setOverride'(a, b, ...c) {
    if (a === 'parentIndex') {
      this.proxyMeta.data.parentIndex = b;
    } else {
      this.proxyMeta.originalRecord.set(a, b, ...c);
    }
  }
};
const ModelLink_default = a => {
  let b;
  return b = class extends (a || Base) {
    link() {
      if (this.isLinked) {
        return this.$original.link();
      }
      const c = this, e = {
          ...proxyConfig,
          'proxyMeta': {
            'originalRecord': c,
            'data': {
              'id': c.id + '_link_' + StringHelper.generateUUID(),
              'stores': []
            }
          }
        }, f = new Proxy(c, e);
      e.proxyRecord = f;
      (c.meta.linkedRecords || (c.meta.linkedRecords = [])).push(f);
      return f;
    }
    get isLinked() {
      let c;
      return Boolean((c = this.proxyMeta) == null ? void 0 : c.originalRecord);
    }
    get hasLinks() {
      return Boolean(!this.proxyMeta && this.$links.length);
    }
    removeLink(c, e = null, f = false) {
      if (c.hasLinks) {
        for (const g of c.$links.slice()) {
          if (e) {
            ArrayHelper.include(e, g);
          } else {
            g.remove(f);
          }
        }
      } else if (c.isLinked) {
        ArrayHelper.remove(c.$original.$links, c);
      }
    }
    beforeRemove(c) {
      this.removeLink(this, c);
    }
    removeChild(c, e, f, g) {
      if (!(g == null ? void 0 : g.isInserting)) {
        c = ArrayHelper.asArray(c);
        for (const i of c) {
          this.removeLink(i, null, f);
        }
      }
      return super.removeChild(c, e, f, g);
    }
    get $originalId() {
      return this.$original.id;
    }
    get $links() {
      let c;
      return (c = this.meta.linkedRecords) != null ? c : [];
    }
  }, __publicField(b, '$name', 'ModelLink'), __publicField(b, 'isModel', true), b;
};
const {defineProperty: defineProperty4} = Reflect;
const ownerSymbol = Symbol('owner');
const typeSplitRe = /[\s,]+/;
const Factoryable_default = a => class b extends (a || Base) {
  static get $name() {
    return 'Factoryable';
  }
  static get declarable() {
    return [
      'factoryable',
      'alias',
      'type'
    ];
  }
  static register(c, e, f = globalThis.__BRYNTUM_EXAMPLE) {
    const {factoryable: g} = this.initClass(), {
        caseless: j,
        registry: k
      } = g, l = StringHelper.split(c, typeSplitRe);
    for (let n, o, p = 0; p < l.length; ++p) {
      o = l[p];
      n = j ? o.toLowerCase() : o;
      if (!f && n in k) {
        throw new Error('Type "' + o + '" already registered with ' + g.class.name + ' factory');
      }
      k[o] = k[n] = e.initClass();
    }
  }
  static isA(c, e) {
    return this.isType(c, e, true);
  }
  static isType(c, e, f) {
    const {factoryable: g} = this, {
        caseless: i,
        registry: j
      } = g, k = j[i ? e.toLowerCase() : e];
    return k ? f ? c instanceof k : c.constructor === k : false;
  }
  static setupAlias(c) {
    c.register(c.alias, c);
  }
  static setupFactoryable(c, e) {
    const f = e.super.class;
    let {factoryable: g} = c;
    g = {
      'caseless': true,
      'defaultType': null,
      'extends': f.factoryable ? [f] : null,
      'typeKey': 'type',
      ...g
    };
    g.class = c;
    g.registry = Object.create(null);
    if (g.extends && !Array.isArray(g.extends)) {
      g.extends = [g.extends];
    }
    defineProperty4(c, 'factoryable', {
      'get'() {
        return g;
      }
    });
  }
  static setupType(c, e) {
    const {type: f} = c;
    c.register(f, c, e.replaceType);
    defineProperty4(c.prototype, 'type', {
      'get': () => f,
      'set': g => {
        if (g !== f) {
          throw new Error('Can not mutate `type` config to different value');
        }
      }
    });
  }
  static create(c, e) {
    return this.reconfigure(null, c, e);
  }
  static reconfigure(c, e, f) {
    const g = this, {factoryable: i} = g, {typeKey: j} = i;
    let k = f, l, n, o, p, q, r, u, v, w;
    if (f && !ObjectHelper.isClass(f)) {
      k = null;
      u = typeof f;
      if (u === 'function') {
        r = f;
      } else if (u === 'string') {
        k = f;
      } else if (ObjectHelper.isObject(f)) {
        l = f.cleanup;
        n = f.defaults;
        p = f.owner;
        r = f.setup;
        k = f.type;
        v = f.transform;
      } else {
        p = f;
      }
    }
    if (v) {
      e = typeof v === 'string' ? p[v](e) : v(e);
    }
    w = e;
    if (typeof w === 'string') {
      e = {};
    } else if (e) {
      if (e === true) {
        e = {};
      }
      if (!ObjectHelper.isObject(e)) {
        if (p && e !== c && (c == null ? void 0 : c[ownerSymbol]) === p) {
          typeof l === 'string' ? p[l](c) : l == null ? void 0 : l(c);
          c.destroy();
        }
        return e;
      }
      w = e[j];
    }
    w = w && g.resolveType(w);
    if (c) {
      if (e && (!w || c.constructor === w)) {
        if (j in e) {
          e = ObjectHelper.assign({}, e);
          delete e[j];
        }
        c.setConfig(e);
        return c;
      }
      if (p && c[ownerSymbol] === p) {
        typeof l === 'string' ? p[l](c) : l == null ? void 0 : l(c);
        c.destroy();
      }
    }
    if (e) {
      if (n) {
        if (!(o = w)) {
          if (!(o = n[j] || k || i.defaultType)) {
            throw new Error('No default mergeType defined for ' + i.class.name + ' factory');
          }
          o = g.resolveType(o);
        }
        if (o) {
          e = o.mergeConfigs(n, e);
        }
      }
      if (r) {
        q = typeof r === 'string' ? p[r](e, w, n) : r(e, w, n);
        if (q === null) {
          return q;
        }
        e = q || e;
      }
      if (!w) {
        if (!(w = e[j] || k || i.defaultType)) {
          throw new Error('No default type defined for ' + i.class.name + ' factory');
        }
        w = g.resolveType(w);
      }
      if (n && !o) {
        e = w.mergeConfigs(n, e);
      }
      if (j in e) {
        e = ObjectHelper.assign({}, e);
        delete e[j];
      }
      e = new w(e);
      if (p) {
        e[ownerSymbol] = p;
      }
    }
    return e || null;
  }
  static resolveType(c, e) {
    if (typeof c !== 'string') {
      return c;
    }
    const {factoryable: f} = this, g = f.extends;
    let j = f.registry[f.caseless ? c.toLowerCase() : c], k;
    for (k = 0; !j && g && k < g.length; ++k) {
      j = g[k].resolveType(c, true);
    }
    if (!j && !e) {
      throw new Error('Invalid type name "' + c + '" passed to ' + f.class.name + ' factory');
    }
    return j;
  }
};
const {getOwnPropertyDescriptor: getOwnPropertyDescriptor2} = Reflect;
const DataField = class extends Base.mixin(Factoryable_default) {
  static get $name() {
    return 'DataField';
  }
  static get type() {
    return 'auto';
  }
  static get factoryable() {
    return { 'defaultType': 'auto' };
  }
  static get prototypeProperties() {
    return {
      'compare': null,
      'compareItems': null,
      'dataSource': null,
      'alwaysWrite': false,
      'nullable': true,
      'nullText': null,
      'nullValue': void 0,
      'persist': true,
      'readOnly': false,
      'internal': false,
      'calculated': false,
      'useProp': null,
      'bypassEqualityOnSyncDataset': false
    };
  }
  construct(a) {
    const b = this;
    if (a) {
      b.name = a.name;
      Object.assign(b, a);
    }
    if (b.compare) {
      b.compareItems = (c, e) => b.compare(c == null ? void 0 : c[b.name], e == null ? void 0 : e[b.name]);
    }
  }
  defineAccessor(a, b) {
    const {
      name: c,
      dataSource: e
    } = this;
    if (!b && c in a && a.$meta.hierarchy.some(f => {
        let g;
        return ((g = getOwnPropertyDescriptor2(f.prototype, c)) == null ? void 0 : g.enumerable) === false;
      })) {
      return;
    }
    Reflect.defineProperty(a, c, {
      'configurable': true,
      'enumerable': true,
      'get': this.complexMapping ? function () {
        return this.complexGet(c, e);
      } : function () {
        if (this.batching && c in this.meta.batchChanges) {
          return this.meta.batchChanges[c];
        }
        return this.data[e];
      },
      'set'(f) {
        const g = this.$meta.fields.map[c];
        if (!(g && g.readOnly)) {
          this.set(c, f);
        }
      }
    });
  }
  isEqual(a, b) {
    return ObjectHelper.isEqual(a, b);
  }
  print(a) {
    return a == null ? this.nullText : this.printValue(a);
  }
  printValue(a) {
    return String(a);
  }
  getCurrentConfig() {
    const a = {};
    for (const b in this.constructor.prototypeProperties) {
      a[b] = this[b];
    }
    delete a.isConstructing;
    return a;
  }
};
DataField._$name = 'DataField';
const ArrayDataField = class extends DataField {
  static type = 'array';
  static $name = 'ArrayDataField';
  isEqual(c, e) {
    return c === e;
  }
  getAt(a, b) {
    return a.get(this.name)[b];
  }
};
ArrayDataField.initClass();
ArrayDataField._$name = 'ArrayDataField';
const BooleanDataField = class extends DataField {
  static alias = 'bool';
  static type = 'boolean';
  static $name = 'BooleanDataField';
  static get prototypeProperties() {
    return { 'nullValue': false };
  }
  isEqual(a, b) {
    if (a == null && b == null) {
      return true;
    }
    return super.isEqual(a, b);
  }
  convert(a) {
    let b;
    if (a == null) {
      return this.nullable ? a : this.nullValue;
    }
    if (((b = a.toLowerCase) == null ? void 0 : b.call(a)) === 'false') {
      return false;
    }
    return Boolean(a);
  }
};
BooleanDataField.initClass();
BooleanDataField._$name = 'BooleanDataField';
const DateDataField = class extends DataField {
  static get $name() {
    return 'DateDataField';
  }
  static get type() {
    return 'date';
  }
  static get prototypeProperties() {
    return { 'format': null };
  }
  convert(a) {
    if (a == null) {
      if (!this.nullable) {
        a = this.nullValue;
      }
    } else if (a === 'now') {
      a = new Date();
    } else if (!(a instanceof Date)) {
      const b = a;
      a = DateHelper.parse(a, this.format || DateHelper.defaultParseFormat);
      if (!a || isNaN(a)) {
        a = void 0;
        if (b !== '') {
          console.warn('Date parsing failed for "' + this.name + '" field, input: ' + JSON.stringify(b) + '. ' + (this.format ? 'Expected format: ' + this.format + '.' : 'You should configure your Model field with a "format" to specify how dates should be parsed. See docs for DateDataField here: https://bryntum.com/products/scheduler/docs/api/Core/data/field/DateDataField, and see https://bryntum.com/products/scheduler/docs/api/Core/helper/DateHelper for the different formats.') + '"');
        }
      }
    }
    return a;
  }
  serialize(a) {
    if (a instanceof Date) {
      a = DateHelper.format(a, this.format || DateHelper.defaultFormat);
    }
    return a;
  }
  printValue(a) {
    return DateHelper.format(a, this.format || DateHelper.defaultFormat);
  }
};
DateDataField.initClass();
DateDataField._$name = 'DateDataField';
const StringDataField = class extends DataField {
  static get $name() {
    return 'StringDataField';
  }
  static get type() {
    return 'string';
  }
  static get prototypeProperties() {
    return { 'nullValue': '' };
  }
  convert(a) {
    return a == null ? this.nullable ? a : this.nullValue : String(a);
  }
};
StringDataField.initClass();
StringDataField._$name = 'StringDataField';
const DurationUnitDataField = class extends StringDataField {
  static get $name() {
    return 'DurationUnitDataField';
  }
  static get type() {
    return 'durationunit';
  }
  isEqual(a, b) {
    return DateHelper.compareUnits(a, b) === 0;
  }
};
DurationUnitDataField.initClass();
DurationUnitDataField._$name = 'DurationUnitDataField';
const IntegerDataField = class extends DataField {
  static alias = 'int';
  static type = 'integer';
  static $name = 'IntegerDataField';
  static get prototypeProperties() {
    return {
      'nullValue': 0,
      'rounding': 'round'
    };
  }
  convert(a) {
    return a == null ? this.nullable ? a : this.nullValue : Math[this.rounding](Number(a));
  }
};
IntegerDataField.initClass();
IntegerDataField._$name = 'IntegerDataField';
const ModelDataField = class extends DataField {
  static configurable = { 'modelClass': null };
  static get $name() {
    return 'ModelDataField';
  }
  static get type() {
    return 'model';
  }
  static get prototypeProperties() {
    return { 'complexMapping': true };
  }
  isEqual(a, b) {
    return a && b && b instanceof a.constructor && b.id == a.id;
  }
};
ModelDataField.initClass();
ModelDataField._$name = 'ModelDataField';
const NumberDataField = class extends DataField {
  static alias = 'float';
  static type = 'number';
  static $name = 'NumberDataField';
  static get prototypeProperties() {
    return {
      'nullValue': 0,
      'precision': null
    };
  }
  isEqual(a, b) {
    return isNaN(Number(a)) && isNaN(Number(b)) || super.isEqual(a, b);
  }
  convert(a) {
    if (a == null) {
      return this.nullable ? a : this.nullValue;
    }
    a = Number(a);
    if (isNaN(a)) {
      return;
    }
    let b = this.precision;
    if (b) {
      b = 10 ** b;
      a = Math.round(a * b) / b;
    } else if (b === 0) {
      a = Math.round(a);
    }
    return a;
  }
};
NumberDataField.initClass();
NumberDataField._$name = 'NumberDataField';
const ObjectDataField = class extends DataField {
  static get $name() {
    return 'ObjectDataField';
  }
  static get type() {
    return 'object';
  }
  static get prototypeProperties() {
    return { 'complexMapping': true };
  }
};
ObjectDataField.initClass();
ObjectDataField._$name = 'ObjectDataField';
const isIdAutoGenerated = a => typeof a === 'string' && a.startsWith('_generated');
const StoreDataField = class extends DataField {
  static type = 'store';
  static $name = 'StoreDataField';
  init(a, b) {
    let c;
    const e = this, f = e.name + 'Store', g = {
        'skipStack': true,
        'syncDataOnLoad': true,
        'useRawData': false
      };
    if (e.store) {
      ObjectHelper.assign(g, e.store);
    }
    if (e.modelClass) {
      g.modelClass = e.modelClass;
    }
    (c = b['init' + StringHelper.capitalize(f)]) == null ? void 0 : c.call(b, g);
    if (!g.storeClass && !e.storeClass) {
      throw new Error('Field \'' + e.name + '\' with type \'store\' must have a storeClass configured');
    }
    const i = b.meta[f] = new (g.storeClass || e.storeClass)(g);
    if (e.complexMapping) {
      ObjectHelper.setPath(a, e.dataSource, i);
    } else {
      a[e.dataSource] = i;
    }
    i.verifyNoGeneratedIds = false;
    i.usesId = e.usesId || !i.count || !i.every(j => j.hasGeneratedId);
    i.$currentValue = e.getValue(i, b);
    i.ion({
      'change': ({action: j}) => {
        const k = e.getValue(i, b);
        if (!i.$isSettingStoreFieldData) {
          const l = i.$preserveCurrentDataset;
          i.$preserveCurrentDataset = e.subStore && (j === 'update' || j === 'remove' || j === 'add');
          e.$isUpdatingRecord = true;
          b.set(e.name, k);
          e.$isUpdatingRecord = false;
          i.$preserveCurrentDataset = l;
        }
        i.$currentValue = k;
      }
    });
  }
  set(a, b, c) {
    let e, f;
    const g = this, i = g.name + 'Store', {[i]: j} = c.meta;
    if (a == null ? void 0 : a.isStore) {
      a = a.$currentValue;
    }
    if (!j) {
      c.meta.initableValues.set(g, a);
      return false;
    }
    if (j.$isSettingStoreFieldData) {
      return;
    }
    j.$isSettingStoreFieldData = true;
    a = (f = (e = c['process' + StringHelper.capitalize(i) + 'Data']) == null ? void 0 : e.call(c, a, c)) != null ? f : a;
    if (!j.$preserveCurrentDataset) {
      j.data = a;
    }
    j.$isSettingStoreFieldData = false;
    j.usesId = g.usesId || !j.count || !j.every(k => k.hasGeneratedId);
  }
  serialize(a, b) {
    const c = b.meta[this.name + 'Store'];
    return this.$isUpdatingRecord ? this.getValue(c, b) : c.$currentValue;
  }
  getValue(a, b) {
    const c = 'get' + StringHelper.capitalize(this.name) + 'StoreValue';
    if (b[c]) {
      return b[c](a);
    }
    return a.allRecords.map(e => {
      const f = e.persistableData;
      if (!a.usesId) {
        delete f.id;
      }
      return f;
    });
  }
  isEqual(c, e) {
    if (c == null && e == null) {
      return true;
    } else if (c == null && e != null || c != null && e == null) {
      return false;
    } else {
      if (c.isStore && e.isStore) {
        return ObjectHelper.isDeeplyEqual(c.$currentValue, e.$currentValue);
      } else if (c.isStore && e.isStore || !c.isStore && !e.isStore) {
        return ObjectHelper.isEqual(c, e, true);
      } else {
        const f = c.isStore ? c : e, g = f.modelClass, j = g.$meta.fields.defs, l = c.isStore ? c.$currentValue : e.$currentValue, n = c.isStore ? e : c;
        if (l.length !== n.length) {
          return false;
        } else {
          for (let o = 0; o < j.length; o++) {
            const p = j[o];
            if (!p.persist) {
              continue;
            }
            for (let q = 0; q < l.length; q++) {
              const r = l[q][p.dataSource];
              const u = n[q][p.dataSource];
              if (r !== void 0 && u === void 0) {
                if (p.dataSource === g.idField && isIdAutoGenerated(r)) {
                  continue;
                }
                if (p.defaultValue !== void 0 && ObjectHelper.isEqual(p.defaultValue, r, true)) {
                  continue;
                }
              }
              if (!ObjectHelper.isEqual(r, u, true)) {
                return false;
              }
            }
          }
          return true;
        }
      }
    }
  }
  getOldValue(a) {
    const b = a.meta[this.name + 'Store'];
    return b ? ObjectHelper.clone(b.$currentValue) : null;
  }
  getAt(a, b) {
    const c = a.meta[this.name + 'Store'];
    return c == null ? void 0 : c.getAt(b);
  }
};
StoreDataField.initClass();
StoreDataField._$name = 'StoreDataField';
const nestedRe = new RegExp(/^(.*?)\.(.*)/);
const arrayRe = /(.*)\[(.*)]\.?(.*)/;
const {defineProperty: defineProperty5} = Reflect;
const {hasOwn: hasOwn4} = ObjectHelper;
const _undefined = void 0;
const internalProps = {
  'children': 1,
  'data': 1,
  'meta': 1
};
const abbreviationFields = [
  'name',
  'title',
  'text',
  'label',
  'description'
];
const fieldDataTypes = {
  'boolean': 1,
  'number': 1,
  'date': 1,
  'object': 1
};
const fieldsOrder = {
  'parentId': 1,
  '$PhantomId': 2,
  'id': 3
};
const _Model = class _Model extends Base.mixin(ModelStm_default, TreeNode_default, ModelLink_default) {
  static relations = null;
  static get $name() {
    return 'Model';
  }
  static get declarable() {
    return ['fields'];
  }
  static get fields() {
    return [
      {
        'name': 'parentIndex',
        'type': 'number',
        'persist': false,
        'internal': true
      },
      {
        'name': 'orderedParentIndex',
        'type': 'number',
        'persist': false,
        'internal': true
      },
      {
        'name': 'readOnly',
        'type': 'boolean'
      },
      {
        'name': 'expanded',
        'internal': true
      }
    ];
  }
  static set idField(a) {
    this._assignedIdField = true;
    this._idField = a;
  }
  static get idField() {
    return this._idField;
  }
  get id() {
    let a;
    return (a = this._id) != null ? a : this.getData('id');
  }
  set id(a) {
    this.set('id', a);
  }
  static set childrenField(a) {
    this._childrenField = a;
  }
  static get childrenField() {
    if (!this._childrenField) {
      const a = this.fieldMap.children;
      this._childrenField = (a == null ? void 0 : a.dataSource) || 'children';
    }
    return this._childrenField;
  }
  get indexPath() {
    const a = [];
    let b = this, c = b.childLevel;
    for (b = this; b && !b.isRoot; b = b.parent) {
      a[c--] = b.parentIndex + 1;
    }
    return a;
  }
  construct(a = {}, b = null, c = null, e = false, f = false, g = false) {
    let i, j;
    const k = this, {
        constructor: l,
        fieldMap: n
      } = k;
    k.meta = {
      'modified': {},
      ...l.metaConfig,
      ...c
    };
    if (l.applyConfigs) {
      const o = {};
      if (!k.useRawData || !k.useRawData.enabled) {
        a = { ...a };
      }
      const p = k.getDefaultConfiguration();
      if (l.autoExposeFields) {
        for (const q in p) {
          if (q in a) {
            if (a[q] !== void 0) {
              o[q] = a[q];
            }
            delete a[q];
          }
        }
      } else {
        for (const r in a) {
          if (!(r in l.fieldMap)) {
            if (a[r] === void 0) {
              o[r] = p[r];
            } else {
              o[r] = a[r];
            }
            delete a[r];
          }
        }
      }
      super.construct(o);
    } else {
      super.construct(null);
    }
    if (!e) {
      l.exposeProperties(a, g);
    }
    if (!hasOwn4(l, 'idFieldProcessed')) {
      let u = k.meta.idField;
      if (!u) {
        if (l._assignedIdField) {
          u = l.idField;
        }
      }
      if (u && u !== n.id.dataSource) {
        l.addField({
          'name': 'id',
          'dataSource': u,
          'internal': true
        });
      }
      l._idField = n.id.dataSource;
      l.idFieldProcessed = true;
    }
    k._internalId = _Model._internalIdCounter++;
    if (!k.originalData) {
      k.originalData = a;
    }
    k.data = l.processData(a, false, b, k, f);
    k._id = n.id.complexMapping ? ObjectHelper.getPath(k.data, l._idField) : k.data[l._idField];
    ((i = k.meta.initableValues) == null ? void 0 : i.size) && k.assignInitables();
    if (k._id == null && !(l.allowNullId && k._id === null)) {
      k.setData('id', k.generateId(b));
    }
    if (k.data[l.childrenField]) {
      k.processChildren((j = ArrayHelper.asArray(b)) != null ? j : []);
    }
    k.generation = 0;
  }
  set isCreating(a) {
    let b;
    const c = this;
    if (Boolean(c.meta.isCreating) !== a) {
      c.meta.isCreating = a;
      (b = c.stores) == null ? void 0 : b.forEach(e => e.onIsCreatingToggle(c, a));
    }
  }
  get isCreating() {
    return Boolean(this.meta.isCreating);
  }
  equals(a) {
    if (a instanceof this.constructor) {
      for (let b = this.$meta.fields.defs, c = 0, {length: e} = b; c < e; c++) {
        const f = b[c], {name: g} = f;
        if (g !== 'id' && !f.isEqual(this.getValue(g), a.getValue(g))) {
          return false;
        }
      }
      return true;
    }
    return false;
  }
  get subclass() {
    return new this.constructor(Object.setPrototypeOf({ 'id': _undefined }, this.data), this.stores[0], null, true);
  }
  static processData(a, b = false, c, e, f) {
    const {
        fieldMap: g,
        fieldDataSourceMap: i,
        defaultValues: j
      } = this, {
        useRawData: useRawData = { 'enabled': false }
      } = c || {}, k = f || useRawData.enabled ? a : ObjectHelper.clone(a);
    let l;
    b = b || useRawData.disableDefaultValue || f;
    if (!b) {
      for (l in j) {
        const n = i[l];
        let o = j[l];
        if (Array.isArray(o)) {
          o = o.slice();
        } else if (o instanceof Date) {
          o = new Date(o);
        } else if (o && typeof o === 'object') {
          o = ObjectHelper.clone(o);
        }
        if (n == null ? void 0 : n.complexMapping) {
          if (ObjectHelper.getPath(k, l) === _undefined) {
            ObjectHelper.setPath(k, l, o);
          }
        } else if (k[l] === _undefined) {
          k[l] = o;
        }
      }
    }
    if (!useRawData.disableTypeConversion && !f) {
      for (l in g) {
        const p = g[l], {
            name: q,
            dataSource: r
          } = p, u = r !== q, v = p.complexMapping, w = u && (v ? ObjectHelper.pathExists(a, r) : r in a), z = q in a && (!u || !w), A = !useRawData.disableTypeConversion && p.convert;
        if (z || A) {
          if (!b || z || w) {
            const B = z ? k[q] : v ? ObjectHelper.getPath(k, r) : k[r], C = A ? p.convert(B, a, e) : B;
            if (C === void 0 && B === _undefined && !w) {
              continue;
            }
            if (v) {
              ObjectHelper.setPath(k, r, C);
            } else {
              k[r] = C;
            }
            if (u) {
              delete k[q];
            }
          }
        }
      }
    }
    this.$meta.fields.initable.length && this.initInitables(e, k);
    return k;
  }
  static setupClass(a) {
    super.setupClass(a);
    if (!a.fields) {
      this.setupFields(this, a);
    }
  }
  static setupFields(a, b) {
    let c, e, f, g, i;
    const j = hasOwn4(a, 'fields') && a.fields, k = b.super.fields, l = b.fields = {
        'defs': (c = k == null ? void 0 : k.defs.slice()) != null ? c : [],
        'exposedData': false,
        'defaults': k ? { ...k.defaults } : {},
        'exposed': Object.create((e = k == null ? void 0 : k.exposed) != null ? e : null),
        'ordinals': Object.create((f = k == null ? void 0 : k.ordinals) != null ? f : null),
        'map': Object.create((g = k == null ? void 0 : k.map) != null ? g : null),
        'sources': Object.create((i = k == null ? void 0 : k.sources) != null ? i : null)
      };
    if (hasOwn4(a, 'defaults')) {
      Object.assign(l.defaults, a.defaults);
    }
    if (hasOwn4(a, 'idField')) {
      a.addField({
        'name': 'id',
        'dataSource': a.idField,
        'internal': true
      });
      l.exposed[a.idField] = true;
    }
    if (j == null ? void 0 : j.length) {
      j.map(a.addField, a);
    }
    l.initable = l.defs.filter(n => n.init);
    a.exposeRelations();
  }
  static get defaultValues() {
    return this.$meta.fields.defaults;
  }
  static get allFields() {
    return this.$meta.fields.defs;
  }
  get allFields() {
    return this.$meta.fields.defs;
  }
  static get fieldMap() {
    return this.$meta.fields.map;
  }
  get fieldMap() {
    return this.$meta.fields.map;
  }
  static get fieldDataSourceMap() {
    return this.$meta.fields.sources;
  }
  static exposeProperties(a, b = true) {
    const c = this, e = c.$meta.fields, f = b ? 'exposed' : 'map';
    if (a && c.autoExposeFields && !e.exposedData) {
      let g, i, j;
      for (g in a) {
        if (!e[f][g] && g !== c.childrenField) {
          j = ObjectHelper.typeOf(a[g]);
          i = {
            'name': g,
            'dataSource': g,
            'fromData': true
          };
          if (fieldDataTypes[j]) {
            i.type = j;
          }
          c.addField(i);
        }
      }
      e.exposedData = true;
    }
    c.exposeRelations();
  }
  static addField(a) {
    if (a == null) {
      return;
    }
    if (typeof a === 'string') {
      a = { 'name': a };
    }
    const b = this.initClass(), c = b.$meta.fields, {ordinals: e} = c, f = c.exposed, {name: g} = a, i = c.map[g], j = a.dataSource || (a.dataSource = g);
    let k, l;
    if (!i || a.type && a.type !== i.type) {
      k = DataField.create(a);
      k.definedBy = i ? i.definedBy : b;
      k.ordinal = i ? i.ordinal : e[g] = c.defs.length;
    } else {
      k = Object.create(i);
      for (l in a) {
        if (l !== 'type') {
          k[l] = a[l];
        }
      }
    }
    k.owner = b;
    c.defs[k.ordinal] = k;
    c.map[g] = k;
    c.sources[j] = k;
    if (j.includes('.')) {
      k.complexMapping = true;
    }
    if (k.complexMapping) {
      f[j.split('.')[0]] = true;
    } else {
      f[j] = true;
    }
    if ('defaultValue' in k) {
      c.defaults[j] = k.defaultValue;
    }
    if (!internalProps[g]) {
      k.defineAccessor(b.prototype);
    }
    b._nonPersistableFields = null;
    b._alwaysWriteFields = null;
    return k;
  }
  static removeField(a) {
    const b = this.initClass(), c = b.$meta.fields, e = c.map[a], {ordinals: f} = c, g = f[a];
    if (e) {
      c.defs.splice(g, 1);
      delete f[a];
      delete c.defaults[a];
      delete c.exposed[a];
      delete c.map[a];
      delete c.sources[e.dataSource];
      for (const i in f) {
        if (f[i] > g) {
          --f[i];
        }
      }
      delete b.prototype[a];
    }
  }
  static exposeRelations() {
    const a = this;
    if (hasOwn4(a, 'exposedRelations')) {
      return;
    }
    if (a.relations) {
      a.exposedRelations = [];
      for (const b in a.relations) {
        const c = a.relations[b];
        c.relationName = b;
        a.exposedRelations.push(c);
        if (!Reflect.ownKeys(a.prototype).includes(b)) {
          defineProperty5(a.prototype, b, {
            'enumerable': true,
            'get': function () {
              return this.getForeign(b);
            },
            'set': function (e) {
              this.setForeign(b, e, c);
            }
          });
        }
      }
    }
  }
  static initInitables(a, b) {
    const c = a.meta.initableValues = new Map();
    for (const e of this.$meta.fields.initable) {
      const f = ObjectHelper.getPath(b, e.dataSource);
      f !== void 0 && c.set(e, f);
      !e.lazy && e.init(b, a);
    }
  }
  assignInitables() {
    const {initableValues: a} = this.meta;
    for (const [b, c] of a) {
      if (b.set(c, this.data, this) !== false) {
        a.delete(b);
      }
    }
  }
  static get autoExposeFields() {
    return true;
  }
  static fieldSorter(c, e) {
    return (fieldsOrder[c] || 100) - (fieldsOrder[e] || 100);
  }
  get fields() {
    return this.$meta.fields.defs;
  }
  getFieldDefinition(a) {
    return this.constructor.getFieldDefinition(a);
  }
  getFieldDefinitionFromDataSource(a) {
    return this.$meta.fields.sources[a];
  }
  get fieldNames() {
    return Object.keys(this.data);
  }
  static getFieldDefinition(a) {
    let b;
    const c = this.fieldMap[a];
    if (!c) {
      if (a.includes('[')) {
        const [, e, , f] = a.match(arrayRe), g = e.includes('.') ? this.getFieldDefinition(e) : this.fieldMap[e];
        if (g == null ? void 0 : g.isStoreDataField) {
          return (b = g.modelClass) == null ? void 0 : b.getFieldDefinition(f);
        }
      } else if (a.includes('.')) {
        const [i, j] = a.split('.', 2), k = this.fieldMap[i];
        if ((k == null ? void 0 : k.isModelDataField) && k.modelClass) {
          return k.modelClass.getFieldDefinition(j);
        }
      }
    }
    return c;
  }
  static getFieldDataSource(a) {
    let b;
    return ((b = this.getFieldDefinition(a)) == null ? void 0 : b.dataSource) || a;
  }
  getDataSource(a) {
    const b = this.constructor.getFieldDefinition(a);
    return (b == null ? void 0 : b.dataSource) || (b == null ? void 0 : b.name);
  }
  static processField(a, b, c) {
    const e = this.fieldMap[a];
    return (e == null ? void 0 : e.convert) ? e.convert(b, this.data, c) : b;
  }
  initRelations() {
    const a = this, b = a.constructor.exposedRelations;
    if (!b) {
      return;
    }
    a.stores.forEach(c => {
      let e;
      if (!c.modelRelations) {
        c.initRelations();
      }
      const f = [];
      (e = c.modelRelations) == null ? void 0 : e.forEach(g => {
        f.push({
          'related': a.initRelation(g),
          'config': g
        });
      });
      c.updateRecordRelationCache(a, f);
    });
  }
  initRelation(a) {
    const b = this, c = b.get(a.foreignKey), e = c !== _undefined && typeof a.foreignStore !== 'string' && a.foreignStore.getById(c), f = b.meta.relationCache || (b.meta.relationCache = {});
    f[a.relationName] = e || (c != null ? {
      'id': c,
      'placeHolder': true
    } : null);
    return e;
  }
  removeRelation(a) {
    const {
      relationName: b,
      foreignKey: c,
      nullFieldOnRemove: e
    } = a;
    if (this.meta.relationCache[b]) {
      delete this.meta.relationCache[b];
      if (e) {
        this.setData(c, null);
      }
    }
  }
  getForeign(a) {
    let b;
    return (b = this.meta.relationCache) == null ? void 0 : b[a];
  }
  setForeign(a, b, c) {
    const e = _Model.asId(b);
    return this.set(c.foreignKey, e);
  }
  getRelationConfig(a) {
    let b, c;
    return (c = (b = this.firstStore) == null ? void 0 : b.modelRelations) == null ? void 0 : c.find(e => e.foreignKey === a);
  }
  flatGet(a, b) {
    if (this.batching && a in this.meta.batchChanges) {
      return this.meta.batchChanges[a];
    }
    return this.data[b];
  }
  complexGet(a, b) {
    if (this.batching && a in this.meta.batchChanges) {
      return this.meta.batchChanges[a];
    }
    return ObjectHelper.getPath(this.data, b);
  }
  get(a) {
    if (!a) {
      return;
    }
    const b = this, {fieldMap: c} = b, e = c[a];
    if (!e) {
      if (a.includes('[')) {
        const [, f, g, i] = a.match(arrayRe);
        const j = c[f];
        if (j == null ? void 0 : j.getAt) {
          b._thisIsAUsedExpression(b[f]);
          const k = j.getAt(b, g);
          if (k && i) {
            if (k.isModel) {
              return k.getValue(i);
            }
            return k[i];
          }
          return k;
        }
        return null;
      }
      if (a.includes('.')) {
        const [l, n] = a.split('.', 2), o = c[l];
        if (o == null ? void 0 : o.isStoreDataField) {
          const p = b[l], q = p.modelClass;
          if (n in q.fieldMap || !(n in p)) {
            return p == null ? void 0 : p.getDistinctValues(n);
          }
          return p[n];
        } else if (!ObjectHelper.hasPath(b.data, a)) {
          return ObjectHelper.getPath(b, a);
        }
        return b.complexGet(a, a);
      }
    }
    if (e == null ? void 0 : e.complexMapping) {
      return b.complexGet(a, e.dataSource);
    }
    return b.flatGet(a, (e == null ? void 0 : e.dataSource) || a);
  }
  getValue(a) {
    if (!a) {
      return;
    }
    if (a in this && !a.includes('.')) {
      return this[a];
    }
    return this.get(a);
  }
  setValue(a, b) {
    if (a in this) {
      this[a] = b;
    } else {
      this.set(a, b);
    }
  }
  setData(a, b) {
    let c, e;
    const {
      data: f,
      fieldMap: g
    } = this;
    if (typeof a === 'string') {
      const j = g[a], k = (c = j == null ? void 0 : j.dataSource) != null ? c : a;
      if (j == null ? void 0 : j.set) {
        j.set(b, this.data, this);
      } else if (j == null ? void 0 : j.complexMapping) {
        ObjectHelper.setPath(f, k, b);
      } else {
        f[k] = b;
      }
      if (j === 'id') {
        this._id = b;
      }
    } else {
      const l = Object.keys(a);
      for (let n = 0; n < l.length; n++) {
        const o = l[n], p = g[o], q = (e = p == null ? void 0 : p.dataSource) != null ? e : o;
        if (p == null ? void 0 : p.set) {
          p.set(b, this.data, this);
        } else if (q) {
          if (p == null ? void 0 : p.complexMapping) {
            ObjectHelper.setPath(f, q, a[o]);
          } else {
            f[q] = a[o];
          }
        }
      }
    }
  }
  getData(a) {
    let b;
    const c = this.fieldMap[a], e = (b = c == null ? void 0 : c.dataSource) != null ? b : a;
    if (e) {
      if (c == null ? void 0 : c.complexMapping) {
        return ObjectHelper.getPath(this.data, e);
      }
      return this.data[e];
    }
  }
  syncId(a) {
    const b = this.id;
    if (b !== a) {
      this._id = a;
      this.setData('id', a);
      const c = {
        'id': {
          'value': a,
          'oldValue': b
        }
      };
      this.afterChange(c, c);
    }
  }
  set(a, b, c = false, e = false, f = false, g = false, i = true) {
    let j;
    const k = this;
    if (k.isBatchUpdating) {
      k.inBatchSet(a, b, c || k.$silenceBatch);
      return null;
    } else {
      const l = k.inSet(a, b, c, e, f, g, i);
      (j = k.afterSet) == null ? void 0 : j.call(k, a, b, c, e, l);
      return l;
    }
  }
  fieldToKeys(a, b) {
    if (typeof a !== 'string') {
      return ObjectHelper.assign({}, a);
    }
    return { [a]: b };
  }
  inBatchSet(a, b, c) {
    let e;
    const f = this, {
        meta: g,
        constructor: i,
        fieldMap: j
      } = f, k = {};
    let l, n = false;
    if (typeof a !== 'string') {
      Object.keys(f.fieldToKeys(a, b)).forEach(o => {
        l = j[o] || ObjectHelper;
        b = i.processField(o, a[o], f);
        if (!l.isEqual(g.batchChanges[o], b)) {
          k[o] = {
            'value': b,
            'oldValue': f.get(o)
          };
          g.batchChanges[o] = b;
          n = true;
        }
      });
    } else {
      l = j[a] || ObjectHelper;
      if (!l.isEqual(g.batchChanges[a], b)) {
        k[a] = {
          'value': b,
          'oldValue': f.get(a)
        };
        g.batchChanges[a] = b;
        n = true;
      }
    }
    if (n) {
      f.generation++;
      if (!c) {
        const o = {
          'action': 'update',
          'record': f,
          'records': [f],
          'changes': k
        };
        (e = f.stores) == null ? void 0 : e.forEach(p => p.trigger('batchedUpdate', { ...o }));
        f.forEachLinked((p, q) => p.trigger('batchedUpdate', {
          ...o,
          'record': q,
          'records': [q]
        }));
      }
    }
  }
  inSet(a, b, c, e, f = false, g = false, j = true) {
    let k, l, n, o, p, q;
    const r = this, {
        data: u,
        meta: v,
        fieldMap: w,
        constructor: z
      } = r, {
        prototype: A,
        childrenField: B,
        relations: C
      } = z, E = {}, F = r.fieldToKeys(a, b), G = Object.keys(F).sort(z.fieldSorter), H = (k = r.stores) == null ? void 0 : k.some(J => J.isSyncingDataOnLoad);
    let I = false;
    if (!c && j && !r.triggerBeforeUpdate(F)) {
      return null;
    }
    r.inSetting = true;
    for (let J = 0; J < G.length; J++) {
      const K = G[J];
      if (K === B) {
        continue;
      }
      if (K.includes('[')) {
        const [, W, X, Y] = K.match(arrayRe), Z = r.fieldMap[W];
        if (Z == null ? void 0 : Z.getAt) {
          const a0 = (n = Z.getAt(r, X)) != null ? n : (l = Z.onSubRecordMissingInSet) == null ? void 0 : l.call(Z, r, X);
          if (a0 == null) {
            throw new Error('The store field \'' + W + '\' does not contain a record at index ' + X + ' - can not set the data');
          }
          if (a0.isModel) {
            a0.set(Y, F[K]);
          } else {
            ObjectHelper.setPath(a0, Y, F[K]);
          }
          continue;
        }
      }
      const L = K.includes('.');
      if (C && L) {
        const [, a1, a2] = K.match(nestedRe);
        if (C[a1]) {
          if (a2.includes('.')) {
            r[a1].set(a2, F[K]);
          } else {
            r[a1][a2] = F[K];
          }
          continue;
        }
      }
      const N = w[K], O = N || ObjectHelper, P = N == null ? void 0 : N.readOnly, Q = (o = N == null ? void 0 : N.dataSource) != null ? o : K, R = !f && !N && K in A || (N == null ? void 0 : N.useProp), S = R ? r[Q] : (N == null ? void 0 : N.complexMapping) || L ? ObjectHelper.getPath(u, Q) : u[Q], T = z.processField(K, F[K], r), U = F[K] = { 'value': T }, V = r.getRelationConfig(K);
      if (!P && (H && (N == null ? void 0 : N.bypassEqualityOnSyncDataset) || !O.isEqual(S, T)) && (!g || T !== void 0)) {
        r.generation++;
        U.oldValue = (q = (p = N == null ? void 0 : N.getOldValue) == null ? void 0 : p.call(N, r)) != null ? q : S;
        I = true;
        if (K in v.modified && O.isEqual(v.modified[K], T, false, true)) {
          Reflect.deleteProperty(v.modified, K);
          if (r.isReverting) {
            r.data[Q] = T;
          }
        } else if (!r.ignoreBag) {
          if (!(K in v.modified) && !(S == null && T == null)) {
            r.storeFieldChange(K, S);
          }
          if (U.oldValue === _undefined) {
            Reflect.deleteProperty(U, 'oldValue');
          }
        }
        E[K] = U;
        r.applyValue(R, Q, T, f, N);
        if (V && !e) {
          r.initRelation(V);
          r.stores.forEach(a3 => a3.cacheRelatedRecord(r, T, V.relationName, U.oldValue));
        }
      }
    }
    if (I) {
      r.afterChange(F, E, c, e, f);
    }
    r.inSetting = false;
    return I ? E : null;
  }
  applyValue(a, b, c, e, f) {
    let g, i;
    const j = this;
    if (((f == null ? void 0 : f.dataSource) || b) === j.constructor.idField) {
      j.$isPhantom = false;
      j._id = c;
    }
    if (((f == null ? void 0 : f.dataSource) || b) === j.constructor.parentIdField) {
      if (((g = j.firstStore) == null ? void 0 : g.tree) && !(((i = j.parent) == null ? void 0 : i.isRoot) && (c == null || !j.firstStore.getById(c)))) {
        (j.firstStore.getById(c) || j.firstStore.rootNode).insertChild(j, null, false, { 'orderedParentIndex': { 'skip': true } });
      }
    } else if (a) {
      j[(f == null ? void 0 : f.name) || b] = c;
    } else if (f == null ? void 0 : f.set) {
      f.set(c, j.data, j);
    } else {
      let k = f == null ? void 0 : f.complexMapping;
      if (!f && b.includes('.')) {
        const l = b.split('.')[0];
        f = j.constructor.fieldMap[l];
        k = (f == null ? void 0 : f.complexMapping) || (f == null ? void 0 : f.type) === 'object';
      }
      if (k) {
        ObjectHelper.setPath(j.data, b, c);
      } else {
        j.data[b] = c;
      }
    }
  }
  afterChange(a, b, c, e, f) {
    if (this.stores) {
      for (const g of this.stores) {
        g.onModelChange(this, a, b, c, e, f);
      }
    }
    this.forEachLinked((i, j) => i.onModelChange(j, a, b, c, e, f));
  }
  forEachLinked(a) {
    for (const b of this.$links) {
      for (const c of b.stores) {
        a(c, b);
      }
    }
  }
  get isPersistable() {
    return !this.isBatchUpdating && !this.isCreating;
  }
  get isModified() {
    return Boolean(this.meta.modified && Object.keys(this.meta.modified).length > 0);
  }
  get hasPersistableChanges() {
    return this.isPersistable && !ObjectHelper.isEmpty(this.rawModificationData);
  }
  isFieldModified(a) {
    return this.isModified && a in this.meta.modified;
  }
  getUnmodified(a) {
    return this.isModified ? this.meta.modified[a] : this.get(a);
  }
  getFieldPersistentValue(a) {
    const b = typeof a === 'string' ? this.getFieldDefinition(a) : a, c = (b == null ? void 0 : b.name) || a;
    let e;
    if (!b || b.persist) {
      e = this.getValue(c);
      if (b == null ? void 0 : b.serialize) {
        e = b.serialize(e, this);
      }
    }
    return e;
  }
  get modifications() {
    const a = this.rawModifications;
    if (a && Object.keys(a).length) {
      a[this.constructor.idField] = this.id;
    }
    return a;
  }
  get rawModifications() {
    const a = this, b = {};
    if (!a.isModified) {
      return null;
    }
    let c = false;
    Object.keys(a.meta.modified).forEach(e => {
      const f = a.getFieldPersistentValue(e);
      if (f !== _undefined) {
        b[e] = f;
        c = true;
      }
    });
    return c ? b : null;
  }
  get modificationData() {
    const a = this.rawModificationData;
    if (a && Object.keys(a).length) {
      ObjectHelper.setPath(a, this.constructor.idField, this.id);
    }
    return a;
  }
  get rawModificationData() {
    const a = this, {fieldMap: b} = a.constructor, c = {};
    if (!a.isModified) {
      return null;
    }
    let e = false;
    Object.keys(a.meta.modified).forEach(f => {
      let g = b[f], i = g == null ? void 0 : g.dataSource;
      if (f.includes('.')) {
        const j = f.match(nestedRe)[1];
        g = b[j];
        i = f;
      }
      if (g == null ? void 0 : g.persist) {
        const k = a.getFieldPersistentValue(f);
        if (k !== _undefined) {
          ObjectHelper.setPath(c, i, k);
          e = true;
        }
      }
    });
    return e ? c : null;
  }
  get modificationDataToWrite() {
    const a = this.constructor.alwaysWriteFields, b = this.modificationData;
    a.forEach(c => {
      b[this.getFieldDefinition(c).dataSource] = this.getFieldPersistentValue(c);
    });
    return b;
  }
  get persistableData() {
    const a = this, b = {};
    a.fields.forEach(c => {
      const e = a.getFieldPersistentValue(c);
      if (e !== _undefined) {
        if (c == null ? void 0 : c.complexMapping) {
          ObjectHelper.setPath(b, c.dataSource, e);
        } else {
          b[c.dataSource] = e;
        }
      }
    });
    return b;
  }
  get dataByFieldName() {
    const {data: a} = this;
    return this.fields.reduce((b, c) => {
      if (ObjectHelper.hasPath(a, c.dataSource)) {
        b[c.name] = a[c.dataSource];
      }
      return b;
    }, {});
  }
  get isCommitting() {
    return Boolean(this.meta.committing);
  }
  internalClearChanges(a = true, b = true, c = null, e = false) {
    let f, g;
    const i = this, {meta: j} = i;
    if (c) {
      for (const l in c) {
        delete j.modified[l];
      }
      if (e) {
        for (const n in j.modified) {
          if (!((f = i.getFieldDefinition(n)) == null ? void 0 : f.persist)) {
            delete j.modified[n];
          }
        }
      }
    } else {
      j.modified = {};
    }
    const k = ObjectHelper.isEmpty(j.modified);
    j.committing = false;
    if (b) {
      (g = i.stores) == null ? void 0 : g.forEach(o => {
        k && o.modified.remove(i);
        o.added.remove(i);
        if (a) {
          const p = o.collectDescendants(i).all;
          o.added.remove(p);
          k && o.modified.remove(p);
        }
      });
    }
  }
  clearChanges(a) {
    let b;
    this.internalClearChanges(a);
    (b = this.stores) == null ? void 0 : b.forEach(c => c.trigger('clearChanges', { 'record': this }));
  }
  storeFieldChange(a, b) {
    this.meta.modified[a] = b;
  }
  revertChanges(a = false) {
    this.isReverting = true;
    this.set(this.meta.modified, _undefined, a);
    this.isReverting = false;
  }
  setByDataSource(a) {
    let b;
    const c = this, {fieldDataSourceMap: e} = c.constructor, f = ObjectHelper.pathifyKeys(a, e), g = {};
    for (const i in f) {
      const j = e[i], k = (b = j == null ? void 0 : j.name) != null ? b : i;
      g[k] = f[i];
    }
    return c.set(g);
  }
  applyChangeset(a, b = '$PhantomId', c = true) {
    let e;
    const f = this, g = f.constructor, {
        idField: i,
        fieldDataSourceMap: j
      } = g, k = ObjectHelper.pathifyKeys(a, j), l = ObjectHelper.pathifyKeys(f.modificationData), n = {}, o = b in a && !(i in a);
    if (!o && b in k) {
      delete k[b];
    }
    for (const q in k) {
      const r = j[q], u = (e = r == null ? void 0 : r.name) != null ? e : q;
      if (c || ((r == null ? void 0 : r.name) === i ? o : k[q] === l[q])) {
        n[u] = k[q];
      }
    }
    const p = f.set(n);
    f.internalClearChanges(false, true, c ? null : n);
    return p ? Object.entries(p).reduce((v, [w, z]) => {
      v[w] = z.value;
      return v;
    }, {}) : {};
  }
  get internalId() {
    return this._internalId;
  }
  get isPhantom() {
    return this.id === '' || this.id == null || this.hasGeneratedId || Boolean(this.$isPhantom);
  }
  get isModel() {
    return true;
  }
  get hasGeneratedId() {
    return typeof this.id === 'string' && this.id.startsWith('_generated');
  }
  static generateId(a = this.$$name) {
    return '_generated' + a + '_' + StringHelper.generateUUID();
  }
  generateId() {
    return this.constructor.generateId();
  }
  static asId(a) {
    return (a == null ? void 0 : a.isModel) ? a.id : ObjectHelper.isObject(a) ? a[this.fieldMap.id.dataSource] : a;
  }
  get json() {
    return StringHelper.safeJsonStringify(this);
  }
  toJSON() {
    const {
        children: a,
        unfilteredChildren: b
      } = this, c = this.persistableData;
    if (b || a) {
      c[this.constructor.childrenField] = (b || a).map(e => e.toJSON());
    }
    return c;
  }
  toString() {
    const a = this, b = abbreviationFields.find(e => e in a.constructor.fieldMap), c = b ? {
        [a.constructor.idField]: a.id,
        [b]: a[b]
      } : a.data;
    return StringHelper.safeJsonStringify(c);
  }
  get isBatchUpdating() {
    return Boolean(this.batching);
  }
  hasBatchedChange(a) {
    let b, c;
    return (c = (b = this.meta) == null ? void 0 : b.batchChanges) == null ? void 0 : c[a];
  }
  beginBatch(a = false) {
    const b = this;
    if (!b.batching) {
      b.batching = 0;
      b.meta.batchChanges = {};
    }
    if (a) {
      b.$silenceBatch = (b.$silenceBatch || 0) + 1;
    }
    b.batching++;
  }
  endBatch(a = false, b = false, c = true) {
    const e = this, {parentIdField: f} = e.constructor;
    if (!e.batching) {
      return;
    }
    e.batching--;
    e.$silenceBatch && e.$silenceBatch--;
    if (e.batching > 0) {
      return;
    }
    if (!ObjectHelper.isEmpty(e.meta.batchChanges)) {
      let {batchChanges: g} = e.meta;
      e.meta.batchChanges = null;
      if (g[f]) {
        g = { ...g };
        e.parentId = g[f];
        delete g[f];
      }
      e.set(g, _undefined, a, false, b, void 0, c);
    }
  }
  cancelBatch() {
    let a;
    if (this.batching) {
      const b = this, {batchChanges: c} = b.meta, e = {};
      b.batching = null;
      b.meta.batchChanges = null;
      b.generation++;
      if (!b.$silenceBatch) {
        Object.entries(c).forEach(([g, i]) => {
          e[g] = {
            'oldValue': i,
            'value': b[g]
          };
        });
        const f = {
          'action': 'update',
          'record': b,
          'records': [b],
          'changes': e
        };
        (a = b.stores) == null ? void 0 : a.forEach(g => g.trigger('batchedUpdate', { ...f }));
        b.forEachLinked((g, i) => g.trigger('batchedUpdate', {
          ...f,
          'record': i,
          'records': [i]
        }));
      }
      b.$silenceBatch && b.$silenceBatch--;
    }
  }
  triggerBeforeUpdate(a) {
    let b;
    return !((b = this.stores) == null ? void 0 : b.some(c => c.trigger('beforeUpdate', {
      'record': this,
      'changes': a
    }) === false));
  }
  copy(a = null, b) {
    const c = this, e = ObjectHelper.clone(c.data), f = c.constructor.idField, g = ObjectHelper.isObject(b) ? b.deep : b;
    let i;
    if (a && typeof a === 'object') {
      i = a[f];
      Object.assign(e, a);
    } else {
      i = a;
    }
    if (g && c.children) {
      e.children = c.children.map(k => k.copy(a === false ? false : void 0, b));
    } else {
      delete e.children;
      delete e.expanded;
    }
    if (c.$meta.fields.initable.length > 0) {
      const k = c.toJSON();
      for (const l of c.$meta.fields.initable) {
        e[l.name] = k[l.name];
      }
    }
    if (a !== false) {
      e[f] = i || c.generateId(c.firstStore);
    }
    const j = new c.constructor(e, null, null, false, true);
    j.originalInternalId = c.internalId;
    return j;
  }
  copyData(a, b, c) {
    const e = {}, f = this.skipCopyFields || {};
    a.allFields.forEach(({name: g}) => {
      if (g !== a.constructor.idField && !(g in f)) {
        e[g] = b ? a.get(g) : a.getValue(g);
      }
    });
    this.set(e, null, c);
  }
  remove(a = false) {
    const b = this, {
        parent: c,
        stores: e
      } = b;
    if (c) {
      c.removeChild(b);
    } else if ((e == null ? void 0 : e.length) && !b.isSpecialRow) {
      const f = e.slice();
      f.forEach(g => g.remove(b, a, false, true));
    }
  }
  beforeRemove(a, b) {
    return super.beforeRemove(a, b);
  }
  get isValid() {
    return true;
  }
  get firstStore() {
    let a;
    return (a = this.stores) == null ? void 0 : a[0];
  }
  joinStore(a) {
    let b, c;
    const e = this;
    if (!e.stores || !e.stores.includes(a)) {
      super.joinStore(a);
      a.register(e);
      if (!e.stores) {
        e.stores = [a];
      } else {
        e.stores.push(a);
      }
      if (e.unjoinedStores && e.unjoinedStores.includes(a)) {
        e.unjoinedStores.splice(e.unjoinedStores.indexOf(a), 1);
      }
      ((b = e.children) == null ? void 0 : b.length) && e.children.forEach(f => f.joinStore(a));
      e.constructor.exposedRelations && e.initRelations();
      if (a.tree && !e.isRoot && !((c = a.stm) == null ? void 0 : c.isRestoring)) {
        e.instanceMeta(a.id).collapsed = !e.expanded;
      }
    }
  }
  unjoinStore(a, b = false) {
    let c, e, f;
    const g = this, {stores: i} = g;
    if (i == null ? void 0 : i.includes(a)) {
      if (!a.isDestroying) {
        a.unregister(g);
        if (!g.unjoinedStores) {
          g.unjoinedStores = [a];
        } else {
          g.unjoinedStores.push(a);
        }
      }
      (e = (c = g.unfilteredChildren || g.children) == null ? void 0 : c.forEach) == null ? void 0 : e.call(c, j => j.unjoinStore(a, b));
      i.splice(i.indexOf(a), 1);
      (f = super.unjoinStore) == null ? void 0 : f.call(this, a, b);
      a.uncacheRelatedRecord(g);
      if (g.meta.map && !a.stm) {
        delete g.meta.map[a.id];
      }
    }
  }
  isPartOfStore(a) {
    let b;
    if (a) {
      return a.includes(this);
    }
    return ((b = this.stores) == null ? void 0 : b.length) > 0;
  }
  get isRemoved() {
    return !this.isPartOfStore();
  }
  instanceMeta(a) {
    let b;
    const {meta: c} = this, e = a.id || a;
    return ((b = c.map) == null ? void 0 : b[e]) || this.setInstanceMeta(e, {});
  }
  setInstanceMeta(a, b) {
    const {meta: c} = this, e = a.id || a;
    if (!c.map) {
      c.map = {};
    }
    return c.map[e] = b;
  }
  get isGroupHeader() {
    return 'groupRowFor' in this.meta;
  }
  get isGroupFooter() {
    return 'groupFooterFor' in this.meta;
  }
  get isSpecialRow() {
    let a;
    return Boolean((a = this.meta) == null ? void 0 : a.specialRow);
  }
  get $original() {
    return this.isLinked ? this.proxyMeta.originalRecord : this;
  }
  static get nonPersistableFields() {
    const a = this;
    if (!a._nonPersistableFields) {
      a._nonPersistableFields = {};
      a.allFields.forEach(b => {
        if (!b.persist || b.calculated) {
          a._nonPersistableFields[b.name] = 1;
        }
      });
    }
    return a._nonPersistableFields;
  }
  static get alwaysWriteFields() {
    const a = this;
    if (!a._alwaysWriteFields) {
      a._alwaysWriteFields = [];
      a.allFields.forEach(b => {
        if (b.alwaysWrite) {
          a._alwaysWriteFields.push(b.name);
        }
      });
    }
    return a._alwaysWriteFields;
  }
  get domId() {
    return typeof this.id === 'string' ? this.id.replace(/[ .]/g, '-') : this.id;
  }
  static toJavaScriptValue(a) {
    const {names: b} = this.$meta, c = b[b.length - 2], e = b[b.length - 3];
    return 'class ' + c + ' extends ' + e + ' { static fields = ' + StringHelper.toJavaScriptValue(this.fields, a) + '; }';
  }
  getCurrentConfig(a) {
    const {
        data: b,
        children: c
      } = this, {
        defaultValues: e,
        applyConfigs: f
      } = this.constructor, g = f ? super.getCurrentConfig(a) : {};
    if (g) {
      for (const i of this.fields) {
        if (i.persist) {
          const j = ObjectHelper.getPath(b, i.dataSource);
          if (j != null && !i.isEqual(j, e[i.name])) {
            ObjectHelper.setPath(g, i.dataSource, Base.processConfigValue(j, a));
          }
        }
      }
      if (c) {
        if (Array.isArray(c)) {
          g.children = [];
          for (const k of c) {
            g.children.push(k.getCurrentConfig(a));
          }
        } else {
          g.children = c;
        }
      }
      if (this.hasGeneratedId) {
        delete g.id;
      }
      delete g.parentId;
      delete g.parentIndex;
    }
    return g;
  }
};
const Model = _Model;
Model._idField = 'id';
Model._internalIdCounter = 1;
Model._assignedIdField = false;
Model.exposeProperties();
Model._$name = 'Model';
const hasOwn5 = Object.hasOwn || ((a, b) => Object.prototype.hasOwnProperty.call(a, b));
let cacheKey = null;
function setParser(a, b) {
  Object.defineProperty(a, 'parser', { 'value': b });
  return b;
}
const Default = class {
  constructor(a) {
    this.formatter = a;
  }
  format(a) {
    return this.formatter.defaultFormat(a);
  }
  parse(a, b) {
    return this.formatter.defaultParse(a, b);
  }
  resolvedOptions() {
    return null;
  }
};
const Formatter = class {
  static standardOptions = Object.freeze([]);
  static get(a) {
    if (a == null) {
      return this.NULL;
    }
    if (a instanceof this) {
      return a;
    }
    const b = typeof a === 'string' ? a : JSON.stringify(a), c = this.cache;
    let e = c.get(b);
    if (!e) {
      cacheKey = b;
      e = new this(a);
      c.set(b, e);
    }
    return e;
  }
  static get cache() {
    return hasOwn5(this, '_cache') && this._cache || (this._cache = new Map());
  }
  static get NULL() {
    return hasOwn5(this, '_null') ? this._null : this._null = new this(null);
  }
  constructor(a) {
    const b = this, {standardOptions: c} = b.constructor;
    b.cacheKey = cacheKey;
    cacheKey = null;
    b.initialize();
    if (a === null) {
      b.formatter = new Default(b);
    } else {
      b.configure(a);
      for (const [e, f] of Object.entries(b.resolvedOptions())) {
        if (f != null && c.includes(e)) {
          b[e] = f;
        }
      }
    }
  }
  get parser() {
    return setParser(this, new this.constructor.Parser(this));
  }
  defaultFormat(a) {
    return a == null ? a : String(a);
  }
  defaultParse(a) {
    return a;
  }
  format(a) {
    return a == null ? a : this.formatter.format(a);
  }
  parse(a, b) {
    return a == null ? a : this.parser.parse(a, b);
  }
  parseStrict(a) {
    return this.parse(a, true);
  }
  resolvedOptions() {
    return this.formatter.resolvedOptions();
  }
};
Formatter._$name = 'Formatter';
const escapeRegExp = StringHelper.escapeRegExp;
const digitsRe2 = /[\d+-]/g;
const newFormatter = (a, b) => new Intl.NumberFormat(a || void 0, b);
const numFormatRe = /^(?:([$])\s*)?(?:(\d+)>)?\d+(,\d+)?(?:\.((\d*)(?:#*)|[*]))?(?:\s*([%])?)?$/;
const unicodeMinus = '\u2212';
const NumberParser = class {
  constructor(a) {
    const b = this, c = a.locale, e = newFormatter(c, { 'maximumFractionDigits': 3 }), f = a.is.currency ? b._decodeStyle(c, {
        'style': 'currency',
        'currency': a.currency,
        'currencyDisplay': a.currencyDisplay
      }) : null, g = a.is.percent ? b._decodeStyle(c, { 'style': 'percent' }) : null, i = e.format(1.2).replace(digitsRe2, '')[0], j = e.format(1000000000).replace(digitsRe2, '')[0] || '';
    Object.assign(b, {
      'currency': f,
      'decimal': i,
      'formatter': a,
      'grouper': j,
      'percent': g
    });
    b.decimal = i;
    b.decimalRe = escapeRegExp(i, 'g');
    b.grouper = j;
    b.stripRe = new RegExp('(?:\\s+|' + escapeRegExp(j) + ')' + (f ? '|(?:' + escapeRegExp(f.text) + ')' : '') + (g ? '|(?:' + escapeRegExp(g.text) + ')' : ''), 'g');
  }
  decimalPlaces(a) {
    a = a.replace(this.stripRe, '');
    const b = a.indexOf(this.decimal) + 1;
    return b && a.length - b;
  }
  parse(a, b) {
    if (typeof a === 'string') {
      a = a.replace(this.stripRe, '').replace(this.decimalRe, '.').replace(unicodeMinus, '-');
      a = b ? Number(a) : parseFloat(a);
      if (this.formatter.is.percent) {
        a /= 100;
      }
    }
    return a;
  }
  _decodeStyle(a, b) {
    const c = newFormatter(a, b), e = newFormatter(a, Object.assign(c.resolvedOptions(), { 'style': 'decimal' })), f = c.format(0), g = e.format(0);
    return {
      'suffix': f.startsWith(g),
      'text': f.replace(g, '').trim()
    };
  }
};
const _NumberFormat = class _NumberFormat extends Formatter {
  static standardOptions = [
    'currency',
    'currencyDisplay',
    'locale',
    'maximumFractionDigits',
    'minimumFractionDigits',
    'minimumIntegerDigits',
    'maximumSignificantDigits',
    'minimumSignificantDigits',
    'style',
    'useGrouping'
  ];
  static get $name() {
    return 'NumberFormat';
  }
  initialize() {
    this._as = {};
    this.is = {
      'decimal': false,
      'currency': false,
      'percent': false,
      'null': true,
      'from': null
    };
  }
  get truncator() {
    const a = this.maximumFractionDigits, b = Math.min(20, a + 1);
    return a == null ? null : this.as({
      'style': 'decimal',
      'maximumFractionDigits': b,
      'minimumFractionDigits': b
    }, 'truncator');
  }
  configure(a) {
    if (typeof a !== 'string') {
      Object.assign(this, a);
    } else {
      this.template = a;
    }
    const b = this, c = {}, e = b.locale ? LocaleManager_default.locales[b.locale] : LocaleManager_default.locale, f = e == null ? void 0 : e.NumberFormat, {template: g} = b, {standardOptions: i} = b.constructor;
    if (f) {
      for (const j in f) {
        if (b[j] == null && typeof f[j] !== 'function') {
          b[j] = f[j];
        }
      }
    }
    if (g) {
      const k = numFormatRe.exec(g), l = k[2], n = k[4];
      b.useGrouping = !!k[3];
      b.style = k[1] ? 'currency' : k[6] ? 'percent' : 'decimal';
      if (l) {
        b.integer = +l;
      }
      if (n === '*') {
        b.fraction = [
          0,
          20
        ];
      } else if (n != null) {
        b.fraction = [
          k[5].length,
          n.length
        ];
      }
    }
    b._minMax('fraction', true, true);
    b._minMax('integer', true, false);
    b._minMax('significant', false, true);
    for (const o of i) {
      if (b[o] != null) {
        c[o] = b[o];
      }
    }
    b.is.from = b.from && b.from.is;
    b.is[b.style] = !(b.is.null = false);
    b.formatter = newFormatter(b.locale, c);
  }
  as(a, b = null) {
    const c = this.resolvedOptions() || { 'template': '9.*' }, e = this._as;
    let f = b && e[b];
    if (!f) {
      if (typeof a === 'string') {
        c.style = a;
      } else {
        Object.assign(c, a);
      }
      c.from = this;
      f = new _NumberFormat(c);
    }
    if (b) {
      e[b] = f;
    }
    return f;
  }
  defaultParse(a, b) {
    return a == null ? a : b ? Number(a) : parseFloat(a);
  }
  format(a) {
    if (typeof a === 'string') {
      const b = Number(a);
      a = isNaN(b) ? this.parse(a) : b;
    }
    return super.format(a);
  }
  round(a) {
    return this.parse(this.format(a));
  }
  truncate(a) {
    const b = this, c = b.maximumFractionDigits, {truncator: e} = b;
    let f = b.parse(a), g;
    if (e) {
      f = e.format(f);
      g = f.indexOf(e.parser.decimal);
      if (g > -1 && f.length - g - 1 > c) {
        f = f.slice(0, g + c + 1);
      }
      f = e.parse(f);
    }
    return f;
  }
  resolvedOptions() {
    const a = super.resolvedOptions();
    for (const b in a) {
      if (a[b] === void 0) {
        a[b] = this[b];
      }
    }
    return a;
  }
  _minMax(a, b, c) {
    const e = this, f = e[a];
    if (f != null) {
      const g = StringHelper.capitalize(a), i = 'maximum' + g + 'Digits', j = 'minimum' + g + 'Digits';
      if (typeof f === 'number') {
        if (b) {
          e[j] = f;
        }
        if (c) {
          e[i] = f;
        }
      } else {
        e[j] = f[0];
        e[i] = f[1];
      }
    }
  }
};
const NumberFormat = _NumberFormat;
NumberFormat.Parser = NumberParser;
Object.assign(NumberFormat.prototype, {
  'currency': null,
  'currencyDisplay': 'symbol',
  'fraction': null,
  'from': null,
  'integer': null,
  'locale': null,
  'maximumFractionDigits': null,
  'minimumFractionDigits': null,
  'minimumIntegerDigits': null,
  'maximumSignificantDigits': null,
  'minimumSignificantDigits': null,
  'significant': null,
  'style': 'decimal',
  'template': null,
  'useGrouping': true
});
Formatter.number = (a, b) => NumberFormat.get(a).format(b);
LocaleManager_default.ion({ 'locale': () => NumberFormat.cache.clear() });
NumberFormat._$name = 'NumberFormat';
const Duration = class _Duration {
  constructor(a, b) {
    if (typeof a === 'number' || a === null) {
      this._magnitude = a;
      this._unit = b;
    } else {
      if (typeof a === 'string') {
        Object.assign(this, DateHelper.parseDuration(a));
      }
      if (typeof a === 'object') {
        Object.assign(this, a);
      }
    }
  }
  get magnitude() {
    return this._magnitude;
  }
  set magnitude(a) {
    this._magnitude = typeof a === 'number' && a;
  }
  get unit() {
    return this._unit;
  }
  set unit(a) {
    this._unit = DateHelper.parseTimeUnit(a);
  }
  get isValid() {
    return this._magnitude != null && Boolean(DateHelper.normalizeUnit(this._unit));
  }
  get milliseconds() {
    return this.isValid ? Math.round(DateHelper.asMilliseconds(this._magnitude, this._unit)) : 0;
  }
  isEqual(a) {
    return Boolean(a) && this._magnitude != null && a._magnitude != null && this.milliseconds === a.milliseconds;
  }
  toString(a) {
    const b = this, c = a ? 'getShortNameOfUnit' : 'getLocalizedNameOfUnit', e = a ? '' : ' ';
    return b.isValid ? '' + NumberFormat.get('9.#').format(b._magnitude) + e + DateHelper[c](b._unit, Math.abs(b._magnitude) !== 1) : '';
  }
  toJSON() {
    return this.toString();
  }
  valueOf() {
    return this.milliseconds;
  }
  add(a) {
    return new _Duration({
      'unit': this.unit,
      'magnitude': DateHelper.as(this.unit, this.milliseconds + new _Duration({
        'magnitude': a.magnitude,
        'unit': a.unit || this.unit
      }).milliseconds)
    });
  }
  diff(a) {
    return new _Duration({
      'unit': this.unit,
      'magnitude': DateHelper.as(this.unit, this.milliseconds - a.milliseconds)
    });
  }
};
Duration._$name = 'Duration';
const nestedValueReducer = (a, b) => a == null ? void 0 : a[b];
const relativeDateUnitRegExp = /^is(this|next|last)(week|month|year)$/i;
const relativeDateOperators = [
  'isToday',
  'isTomorrow',
  'isYesterday',
  'isThisWeek',
  'isNextWeek',
  'isLastWeek',
  'isThisMonth',
  'isNextMonth',
  'isLastMonth',
  'isThisYear',
  'isNextYear',
  'isLastYear',
  'isYearToDate'
];
const _CollectionFilter = class _CollectionFilter extends Base.mixin(Identifiable_default) {
  static $name = 'CollectionFilter';
  static get defaultConfig() {
    return {
      'value': null,
      'operator': null,
      'filterBy': null,
      'convert': null,
      'caseSensitive': true,
      'id': null,
      'type': null,
      'internal': null,
      'disabled': false
    };
  }
  static get configurable() {
    return { 'property': null };
  }
  construct(a) {
    if (typeof a === 'function') {
      a = { 'filterBy': a };
    }
    if (!a.type) {
      if (DateHelper.isDate(a.value) || Array.isArray(a.value) && a.value.every(DateHelper.isDate)) {
        a.type = 'date';
      } else if (a.value instanceof Duration) {
        a.type = 'duration';
      }
    } else {
      if (a.type === 'date' && a.value != null && !Array.isArray(a.value)) {
        a.value = new Date(a.value);
      } else if (a.type === 'duration' && a.value != null && !Array.isArray(a.value)) {
        a.value = new Duration(a.value);
      }
    }
    super.construct(a);
  }
  get id() {
    if (!this._id) {
      if (this.internal) {
        this._id = _CollectionFilter.generateId('b-internal-' + this.property + '-filter-');
      } else {
        this._id = this.property && this.property + '-' + this.operator || _CollectionFilter.generateId('b-filter-');
      }
    }
    return this._id;
  }
  set id(a) {
    this._id = a;
  }
  onChange(a) {
    let b;
    const c = this;
    if (!c.isConfiguring && ((b = c.owner) == null ? void 0 : b.onFilterChanged) && !c.owner.isConfiguring) {
      c.owner.onFilterChanged(c, a);
    }
  }
  get filterBy() {
    return this._filterBy || this.defaultFilterBy;
  }
  set filterBy(a) {
    this._filterBy = a;
  }
  defaultFilterBy(a) {
    const b = this;
    let c;
    if (a.isModel) {
      c = a.getValue(b.property);
    } else if (b._propertyItems.length > 1) {
      c = b._propertyItems.reduce(nestedValueReducer, a);
    } else {
      c = a[b.property];
    }
    return b[b.operator](b.convert(c));
  }
  updateProperty(a) {
    this._propertyItems = a.split('.');
    this.onChange('property');
  }
  set value(a) {
    const b = this;
    b._value = a;
    if (Array.isArray(a) && ({
        'date': 1,
        'duration': 1
      }[b.type] || a.length > 0 && typeof a[0] === 'string')) {
      b._filterValue = a.map(c => b.convert(c));
    } else if (!b.caseSensitive && Array.isArray(a) && a.length > 0 && typeof a[0] === 'string') {
      b._filterValue = a.map(c => c == null ? void 0 : c.toLowerCase());
    } else if (!b.caseSensitive && typeof a === 'string') {
      b._filterValue = a.toLowerCase();
    } else {
      b._filterValue = b.convert(a);
    }
    b.onChange('value');
  }
  get value() {
    return this._value;
  }
  get filterValue() {
    return this._filterValue;
  }
  set operator(a) {
    this._operator = a;
    this.onChange('operator');
  }
  get operator() {
    const a = this;
    if (a._operator) {
      return a._operator;
    }
    if (Array.isArray(a.filterValue)) {
      return 'isIncludedIn';
    }
    return typeof a.filterValue === 'string' ? '*' : '=';
  }
  convert(a) {
    let b;
    if (this.operator !== 'sameTime' && !(typeof this.filterValue === 'string' && a instanceof Date)) {
      if (this.operator === 'sameDay') {
        a = DateHelper.clearTime(a);
      }
      a = (b = a == null ? void 0 : a.valueOf()) != null ? b : a;
    }
    a = !this.caseSensitive && typeof a === 'string' ? a.toLowerCase() : a;
    return a;
  }
  filter(a) {
    return this.filterBy(a);
  }
  startsWith(a) {
    return String(a).startsWith(this.filterValue);
  }
  endsWith(a) {
    return String(a).endsWith(this.filterValue);
  }
  isIncludedIn(a) {
    return this.filterValue.length === 0 || this.filterValue.includes(a);
  }
  isNotIncludedIn(a) {
    return !this.isIncludedIn(a);
  }
  includes(a) {
    return this.filterValue.length === 0 || String(a).includes(this.filterValue);
  }
  doesNotInclude(a) {
    return !this.includes(a);
  }
  sameTime(a) {
    return DateHelper.isSameTime(a, this.filterValue);
  }
  sameDay(a) {
    return a === this.filterValue;
  }
  ['='](a) {
    return ObjectHelper.isEqual(a, this.filterValue);
  }
  ['!='](a) {
    return !ObjectHelper.isEqual(a, this.filterValue);
  }
  ['>'](a) {
    return ObjectHelper.isMoreThan(a, this.filterValue);
  }
  ['>='](a) {
    return ObjectHelper.isMoreThan(a, this.filterValue) || ObjectHelper.isEqual(a, this.filterValue);
  }
  ['<'](a) {
    return ObjectHelper.isLessThan(a, this.filterValue);
  }
  ['<='](a) {
    return ObjectHelper.isLessThan(a, this.filterValue) || ObjectHelper.isEqual(a, this.filterValue);
  }
  ['*'](a) {
    return ObjectHelper.isPartial(a, this.filterValue);
  }
  between(a) {
    const [b, c] = this._filterValue;
    return (ObjectHelper.isMoreThan(a, b) || ObjectHelper.isEqual(a, b)) && (ObjectHelper.isLessThan(a, c) || ObjectHelper.isEqual(a, c));
  }
  notBetween(a) {
    return !this.between(a);
  }
  empty(a) {
    return a === void 0 || a === null || String(a).length === 0;
  }
  notEmpty(a) {
    return !this.empty(a);
  }
  isToday(a) {
    return this.between(a);
  }
  isTomorrow(a) {
    return this.between(a);
  }
  isYesterday(a) {
    return this.between(a);
  }
  isThisWeek(a) {
    return this.between(a);
  }
  isNextWeek(a) {
    return this.between(a);
  }
  isLastWeek(a) {
    return this.between(a);
  }
  isThisMonth(a) {
    return this.between(a);
  }
  isNextMonth(a) {
    return this.between(a);
  }
  isLastMonth(a) {
    return this.between(a);
  }
  isThisYear(a) {
    return this.between(a);
  }
  isNextYear(a) {
    return this.between(a);
  }
  isLastYear(a) {
    return this.between(a);
  }
  isYearToDate(a) {
    return this.between(a);
  }
  isTrue(a) {
    return a === true;
  }
  isFalse(a) {
    return a === false;
  }
  setRelativeDateValues() {
    this._filterValue = _CollectionFilter.getRelativeDateRange(this._operator).map(a => a.valueOf());
  }
  get isNoOp() {
    let a;
    return (this.operator === 'includes' || this.operator === 'isIncludedIn') && ((a = this.filterValue) == null ? void 0 : a.length) === 0;
  }
  static getRelativeDateRange(a, b = new Date()) {
    let c, e, f, g, i, j, k;
    switch (a) {
    case 'isYearToDate':
      return [
        DateHelper.floor(b, '1 year'),
        b
      ];
    case 'isToday':
      c = DateHelper.floor(b, '1 day');
      return [
        c,
        DateHelper.add(c, 1, 'day')
      ];
    case 'isYesterday':
      c = DateHelper.floor(b, '1 day');
      return [
        DateHelper.add(c, -1, 'day'),
        c
      ];
    case 'isTomorrow':
      e = DateHelper.getStartOfNextDay(b);
      return [
        e,
        DateHelper.add(e, 1, 'day')
      ];
    case 'isThisWeek':
    case 'isNextWeek':
    case 'isLastWeek':
    case 'isThisMonth':
    case 'isNextMonth':
    case 'isLastMonth':
    case 'isThisYear':
    case 'isNextYear':
    case 'isLastYear':
      f = a.toLowerCase().match(relativeDateUnitRegExp);
      if (!f) {
        throw new Error('Unrecognized relative date expression: ' + a);
      }
      [, j, k] = f;
      g = '1 ' + k;
      i = DateHelper.floor(b, g);
      if (j === 'next') {
        i = DateHelper.add(i, 1, k);
      } else if (j === 'last') {
        i = DateHelper.add(i, -1, k);
      }
      return [
        i,
        DateHelper.add(i, 1, k)
      ];
    }
  }
  static generateFiltersFunction(a) {
    if (!a || !a.length && !a.count) {
      return FunctionHelper.returnTrue;
    }
    for (const b of a) {
      if (b.type === 'date' && relativeDateOperators.includes(b._operator)) {
        b.setRelativeDateValues();
      }
    }
    return function (c) {
      let e = true;
      for (const f of a) {
        if (!f.disabled) {
          e = f.filter(c);
        }
        if (!e) {
          break;
        }
      }
      return e;
    };
  }
};
const CollectionFilter = _CollectionFilter;
CollectionFilter._$name = 'CollectionFilter';
const idCounts2 = ObjectHelper.getPathDefault(globalThis, 'bryntum.idCounts', Object.create(null));
const IdHelper = class {
  static generateId(a = 'generatedId') {
    return a + (idCounts2[a] = (idCounts2[a] || 0) + 1);
  }
  static reset(a) {
    idCounts2[a] = 0;
  }
};
IdHelper._$name = 'IdHelper';
const CollectionSorter = class extends Base {
  static get defaultConfig() {
    return {
      'property': null,
      'direction': 'ASC',
      'sortFn': null,
      'convert': null,
      'id': null,
      'useLocaleSort': null
    };
  }
  construct(a) {
    if (typeof a === 'function') {
      a = { 'sortFn': a };
    }
    super.construct(a);
  }
  get id() {
    return this._id || (this._id = this.property || IdHelper.generateId('b-sorter'));
  }
  set id(a) {
    this._id = a;
  }
  set sortFn(a) {
    this._sortFn = a;
  }
  get sortFn() {
    if (this._sortFn) {
      return this._sortFn;
    }
    return this.defaultSortFn;
  }
  defaultSortFn(a, b) {
    const c = this, {
        convert: e,
        property: f,
        useLocaleSort: g
      } = c, i = c.direction.toLowerCase() === 'desc' ? -1 : 1;
    a = a[f];
    b = b[f];
    if (e) {
      a = e(a);
      b = e(b);
    }
    if (g && a != null && b != null && typeof a === 'string') {
      if (g === true) {
        return String(a).localeCompare(b);
      }
      if (typeof g === 'string') {
        return String(a).localeCompare(b, g);
      }
      if (typeof g === 'object') {
        return String(a).localeCompare(b, g.locale, g);
      }
    }
    return (a > b ? 1 : a < b ? -1 : 0) * i;
  }
  static generateSortFunction(a, b) {
    const c = a.isCollection ? a.values : a, e = c.length;
    return (f, g) => {
      let j, k;
      for (k = 0; k < e; ++k) {
        j = c[k].sortFn(f, g);
        if (j) {
          return j;
        }
      }
      return b ? b(f, g) : 0;
    };
  }
};
CollectionSorter._$name = 'CollectionSorter';
const return0 = () => 0;
const reverseNumericSortFn = (c, e) => e - c;
const filteredIndicesProperty = Symbol('filteredIndicesProperty');
const emptyArray4 = Object.freeze([]);
const sortEvent = Object.freeze({
  'action': 'sort',
  'added': emptyArray4,
  'removed': emptyArray4,
  'replaced': emptyArray4
});
const filterEvent = Object.freeze({
  'action': 'filter',
  'added': emptyArray4,
  'removed': emptyArray4,
  'replaced': emptyArray4
});
const keyTypes = {
  'string': 1,
  'number': 1
};
function addItemToIndex(a, b, c, e) {
  if (b.unique !== false) {
    b.set(c, a);
    if (e) {
      b.count++;
    }
  } else {
    let f = b.get(c);
    if (!f) {
      f = new Set();
      b.set(c, f);
    }
    f.add(a);
  }
}
function removeItemFromIndex(a, b, c) {
  if (b.unique !== false) {
    b.delete(c);
  } else if (b.has(c)) {
    b.get(c).delete(a);
    if (!b.get(c).size) {
      b.delete(c);
    }
  }
}
function doRebuildIndices(a, b, c, e, f = false) {
  for (let g = 0; g < a.length; g++) {
    const k = a[g];
    if (k) {
      for (let l = 0; l < e; l++) {
        const n = c[l], o = k[n], p = b[n];
        o !== void 0 && addItemToIndex(k, p, o, !f && p.onDuplicate);
      }
    }
  }
}
const Collection = class _Collection extends Base.mixin(Events_default) {
  _addedValues = null;
  _sortFunction = null;
  constructor() {
    super(...arguments);
  }
  static get configurable() {
    return {
      'idProperty': 'id',
      'extraKeys': null,
      'autoFilter': true,
      'autoSort': true,
      'sorters': {
        '$config': ['lazy'],
        'value': []
      }
    };
  }
  get isCollection() {
    return true;
  }
  construct(a) {
    this.generation = 0;
    this._values = [];
    super.construct(a);
  }
  doDestroy() {
    let a;
    super.doDestroy();
    const b = this;
    b._values.length = 0;
    if (b.isFiltered) {
      b._filteredValues.length = 0;
      b.filters.destroy();
    }
    (a = b._sorters) == null ? void 0 : a.destroy();
  }
  clear() {
    const a = this, b = a._values.slice();
    if (a.totalCount) {
      a._values.length = 0;
      if (a._filteredValues) {
        a._filteredValues.length = 0;
      }
      a._indicesInvalid = true;
      a.generation++;
      a.trigger('change', {
        'action': 'clear',
        'removed': b
      });
    }
  }
  equals(a, b) {
    if (a.isCollection) {
      a = a.values;
    }
    if (a.length === this.count) {
      let {values: c} = this;
      if (b) {
        a = a.map(b);
        c = c.map(b);
      }
      return ArrayHelper.delta(a, c).inBoth.length === this.count;
    }
    return false;
  }
  replaceValues({
    values: a,
    filteredValues: b,
    silent: silent = false,
    isNewDataset: isNewDataset = false
  }) {
    const c = this;
    let e, f;
    if (c.isFiltered && !isNewDataset) {
      const g = Boolean(b);
      if (!g) {
        b = a.slice();
        a = null;
      } else if (a) {
        e = c._values;
        c._values = a.slice();
      }
      f = c._filteredValues;
      c._filteredValues = b.slice();
    } else {
      e = c._values;
      c._values = a.slice();
      b = null;
      if (c.isFiltered && isNewDataset && c.autoFilter) {
        c._filterFunction = null;
        c._filteredValues = c._values.filter(c.filterFunction);
      } else if (c._filteredValues) {
        c._filteredValues.length = 0;
      }
    }
    c._indicesInvalid = true;
    c._addedValues = void 0;
    c.generation++;
    if (!silent) {
      c.trigger('change', {
        'action': 'replaceValues',
        'replacedValues': e,
        'replacedFilteredValues': f,
        'values': a,
        'filteredValues': b
      });
    }
  }
  set values(a) {
    this.invalidateIndices();
    this.splice(0, this._values.length, a);
  }
  get values() {
    return this.isFiltered ? this._filteredValues : this._values;
  }
  get filteredValues() {
    return this._filteredValues;
  }
  [Symbol.iterator]() {
    return this.values[Symbol.iterator]();
  }
  forEach(a, b = false) {
    (this.isFiltered && !b ? this._filteredValues : this._values).forEach(a);
  }
  map(a, b = false) {
    return (this.isFiltered && !b ? this._filteredValues : this._values).map(a);
  }
  reduce(a, b = false, c = []) {
    return (this.isFiltered && !b ? this._filteredValues : this._values).reduce(a, c);
  }
  find(a, b = false) {
    return (this.isFiltered && !b ? this._filteredValues : this._values).find(a);
  }
  get first() {
    return this.values[0];
  }
  get last() {
    return this.values[this.count - 1];
  }
  get allValues() {
    return this._values;
  }
  get addedValues() {
    return this._addedValues;
  }
  match(a, b = true) {
    const c = this, {_values: e} = c, f = [];
    c.forEach(g => {
      const i = a.get(g.id, true);
      if (i) {
        const j = c.indexOf(g, true), k = e[j];
        e[j] = i;
        c.removeFromIndices(k);
        c.addToIndices(i);
      } else if (b) {
        f.push(g);
      }
    });
    if (f.length) {
      c.remove(f);
    }
    if (c.isFiltered) {
      c._filteredValues = c._values.filter(c.filterFunction);
    }
    return f;
  }
  add(...a) {
    if (a.length === 1) {
      this.splice(this._values.length, null, ...a);
    } else {
      this.splice(this._values.length, null, a);
    }
  }
  remove(...a) {
    if (a.length === 1) {
      this.splice(0, ...a);
    } else {
      this.splice(0, a);
    }
  }
  move(a, b) {
    a = ArrayHelper.asArray(a);
    while (a.length && a[0] === b) {
      a.shift();
    }
    if (!a.length) {
      return;
    }
    const c = this, {_values: e} = c, f = c.indexOf(a[0], true);
    if (a.length === 1 && e[f + 1] === b) {
      return;
    }
    c.suspendEvents();
    c.remove(a);
    c.resumeEvents();
    const g = b ? c.indexOf(b, true) : e.length;
    if (g === -1) {
      throw new Error('Collection move beforeItem parameter must be present in Collection');
    }
    e.splice(g, 0, ...a);
    c._indicesInvalid = 1;
    c.trigger('change', {
      'action': 'move',
      'items': a,
      'from': f,
      'to': g
    });
    return g;
  }
  splice(a = 0, b, ...c) {
    const e = this, f = e.idProperty, g = e._values, j = {}, k = [], l = [], n = e.totalCount;
    let o, p;
    if ((c == null ? void 0 : c.length) === 1 && Array.isArray(c[0])) {
      c = c[0];
    }
    if (e.trigger('beforeSplice', {
        'index': a,
        'toRemove': b,
        'toAdd': c
      }) === false) {
      return;
    }
    if (c) {
      if (n && c.length) {
        const q = e.indices[f];
        o = [];
        for (let r = 0; r < c.length; r++) {
          const u = c[r], v = u[f], w = q.get(v), z = w ? g.indexOf(w) : -1;
          j[v] = true;
          if (z !== -1) {
            if (g[z] !== u) {
              l.push([
                g[z],
                u
              ]);
              g[z] = u;
            }
          } else {
            o.push(u);
          }
        }
      } else {
        o = c;
      }
    }
    if (b) {
      if (typeof b === 'number') {
        b = Math.min(b, g.length - a);
        for (let A = a; b; --b) {
          const B = g[A][f];
          if (j[B]) {
            a++;
            A++;
          } else {
            k.push(g[A]);
            g.splice(A, 1);
            p = true;
          }
        }
      } else {
        let C = o.length === 0, E;
        b = ArrayHelper.asArray(b);
        const F = b.reduce((G, H) => {
          const I = typeof H === 'number', J = I ? H : e.indexOf(H, true);
          if (C && (E != null && J !== E + 1 || I)) {
            C = false;
          }
          if (J >= 0 && J < n) {
            G.push(J);
          }
          E = J;
          return G;
        }, []).sort(reverseNumericSortFn);
        if (C) {
          if (F.length) {
            k.push.apply(k, b);
            g.splice(F[F.length - 1], F.length);
            p = true;
          }
        } else {
          for (let G = 0; G < F.length; G++) {
            const H = F[G];
            if (H !== -1) {
              const I = g[H][f];
              if (!j[I]) {
                k.unshift(g[H]);
                g.splice(H, 1);
                p = true;
              }
            }
          }
        }
      }
      if (k.length && !e._indicesInvalid) {
        k.forEach(e.removeFromIndices, e);
      }
    }
    if (o.length) {
      g.splice(Math.min(a, g.length), 0, ...o);
      p = true;
      if (!e._indicesInvalid) {
        o.forEach(e.addToIndices, e);
      }
      if (!e._addedValues) {
        e._addedValues = new Set();
      }
      for (const J of o) {
        e._addedValues.add(J);
      }
    }
    if (k.length && e._addedValues) {
      for (const K of k) {
        e._addedValues.delete(K);
      }
    }
    if (l.length && !e._indicesInvalid) {
      l.forEach(L => {
        e.removeFromIndices(L[0]);
        e.addToIndices(L[1]);
      });
    }
    if (p || l.length) {
      if (e.isSorted) {
        e.onSortersChanged();
      } else if (e.isFiltered) {
        if (e.autoFilter) {
          e.onFiltersChanged({
            'action': 'splice',
            'oldCount': 1
          });
        } else {
          e._filteredValues.splice(Math.min(a, e._filteredValues.length), 0, ...o);
        }
      }
      e.generation++;
      e.trigger('change', {
        'action': 'splice',
        'removed': k,
        'added': o,
        'replaced': l,
        'oldCount': n
      });
    } else {
      e.trigger('noChange', {
        'index': a,
        'toRemove': b,
        'toAdd': c
      });
    }
  }
  changeId(a, b) {
    const c = this, {idProperty: e} = c, f = keyTypes[typeof a] ? a : a[e], g = c.get(f);
    if (g) {
      const i = c.get(b);
      if (i && g !== i) {
        throw new Error('Attempt to set item ' + f + ' to already existing member\'s id ' + b);
      }
      c.removeIndexEntry(a, e, f);
      c.addIndexEntry(a, e, b);
      g[e] = b;
    }
  }
  get(a, b = false) {
    return this.getBy(this.idProperty, a, b);
  }
  getAt(a, b = false) {
    if (this.isFiltered && !b) {
      return this._filteredValues[a];
    } else {
      return this._values[a];
    }
  }
  getBy(a, b, c = false) {
    return this.findItem(a, b, this.isFiltered && c);
  }
  get count() {
    return this.values.length;
  }
  get totalCount() {
    return this._values.length;
  }
  updateIdProperty(a) {
    this.addIndex({
      'property': a,
      'unique': true
    });
  }
  changeSorters(a) {
    return new _Collection({
      'values': ArrayHelper.asArray(a),
      'internalListeners': {
        'change': 'onSortersChanged',
        'thisObj': this
      }
    });
  }
  addSorter(a) {
    const b = a instanceof CollectionSorter ? a : new CollectionSorter(a);
    this.sorters.add(b);
    return b;
  }
  get isSorted() {
    let a;
    return Boolean((a = this._sorters) == null ? void 0 : a.count);
  }
  onSortersChanged() {
    const a = this;
    a._sortFunction = null;
    a._addedValues = null;
    a._values.sort(a.sortFunction);
    a.trigger('change', sortEvent);
  }
  get sortFunction() {
    if (!this._sortFunction) {
      if (this.isSorted) {
        this._sortFunction = CollectionSorter.generateSortFunction(this.sorters.values);
      } else {
        this._sortFunction = return0;
      }
    }
    return this._sortFunction;
  }
  get filters() {
    if (!this._filters) {
      this._filters = new _Collection({
        'internalListeners': {
          'change': 'onFiltersChanged',
          'thisObj': this
        }
      });
    }
    return this._filters;
  }
  addFilter(a) {
    const b = a instanceof CollectionFilter ? a : new CollectionFilter(a);
    this.filters.add(b);
    return b;
  }
  removeFilter(a) {
    const {filters: b} = this;
    if (!a.isCollectionFilter) {
      a = b.get(a);
    }
    b.remove(a);
  }
  clearFilters() {
    this.filters.clear();
  }
  get isFiltered() {
    return Boolean(this._filters && this._filters.count);
  }
  onFiltersChanged({
    action: a,
    removed: b,
    oldCount: c
  }) {
    const e = this, f = c || a === 'clear' && b.length ? e._filteredValues : e._values;
    e._filterFunction = null;
    e._filteredValues = e._values.filter(e.filterFunction);
    e._indicesInvalid = true;
    const {
      toAdd: g,
      toRemove: i
    } = ArrayHelper.delta(e._filteredValues, f, true);
    e.trigger('change', {
      ...filterEvent,
      'added': g,
      'removed': i
    });
  }
  get filterFunction() {
    if (!this._filterFunction) {
      if (this.isFiltered) {
        this._filterFunction = CollectionFilter.generateFiltersFunction(this.filters.values);
      } else {
        this._filterFunction = FunctionHelper.returnTrue;
      }
    }
    return this._filterFunction;
  }
  changeExtraKeys(a) {
    const b = ArrayHelper.asArray(a);
    return b.map(c => {
      if (typeof c === 'string') {
        return {
          'property': c,
          'unique': true
        };
      }
      return c;
    });
  }
  updateExtraKeys(a) {
    for (let b = 0; b < a.length; b++) {
      this.addIndex(a[b]);
    }
  }
  addIndex(a) {
    const b = this;
    if (a) {
      (b._indices || (b._indices = {}))[a.property] = new Map();
      Object.assign(b._indices[a.property], a);
      b.invalidateIndices();
      if (a.dependentOn) {
        b.hasCompositeIndex = true;
      }
    }
  }
  findIndex(a, b, c = false) {
    const e = this.findItem(a, b, c);
    if (!e) {
      return -1;
    }
    const f = this.isFiltered && !c ? this._filteredValues : this._values;
    return f.indexOf(e);
  }
  findItem(a, b, c = false) {
    let e;
    const f = this, {isFiltered: g} = f, j = g && !c ? f.indices[filteredIndicesProperty][a] : f.indices[a];
    if (j) {
      const k = (e = j.get(b)) != null ? e : typeof b === 'string' && b.length && !isNaN(b) && j.get(Number(b)) || null;
      if (k != null) {
        return k;
      }
    } else {
      const l = g && !c ? f._filteredValues : f._values, n = l.length;
      for (let o = 0; o < n; o++) {
        const p = l[o];
        if (p[a] == b) {
          return p;
        }
      }
    }
    return null;
  }
  removeIndex(a) {
    delete this._indices[a];
    this.hasCompositeIndex = Object.values(this.indices).some(b => b.dependentOn);
  }
  indexOf(a, b = false) {
    return this.findIndex(this.idProperty, keyTypes[typeof a] ? a : a[this.idProperty], b);
  }
  includes(a, b = false) {
    if (Array.isArray(a)) {
      return a.some(c => this.includes(c));
    }
    return Boolean(this.findItem(this.idProperty, keyTypes[typeof a] ? a : a[this.idProperty], b));
  }
  get indices() {
    if (this._indicesInvalid) {
      this.rebuildIndices();
    }
    return this._indices;
  }
  invalidateIndices() {
    this._indicesInvalid = true;
  }
  rebuildIndices() {
    const a = this, b = a.isFiltered, c = a._indices || (a._indices = {}), e = Object.keys(c), f = e.length, g = a._values;
    let k = false, l;
    if (b) {
      l = c[filteredIndicesProperty] = {};
    }
    for (let n = 0; n < f; n++) {
      const o = c[e[n]];
      o.clear();
      if (o.onDuplicate) {
        o.count = 0;
        k = true;
      }
      if (b) {
        let p = l[e[n]];
        if (p) {
          p.clear();
        } else {
          p = l[e[n]] = new Map();
          p.unique = o.unique;
        }
      }
    }
    doRebuildIndices(g, c, e, f);
    if (k) {
      for (let q = 0; q < f; q++) {
        const r = c[e[q]];
        if (r.onDuplicate && r.count > r.size) {
          const u = new Set();
          for (let v = 0; v < g.length; v++) {
            const w = g[v], z = w[e[q]];
            if (u.has(z)) {
              r.onDuplicate(w, z, r);
            }
            u.add(z);
          }
        }
      }
    }
    if (b) {
      doRebuildIndices(a._filteredValues, l, e, f, true);
    }
    a._indicesInvalid = false;
  }
  getIndices(a) {
    const b = [this.indices[a]];
    if (this.isFiltered) {
      b.push(this.indices[filteredIndicesProperty][a]);
    }
    return b;
  }
  addToIndices(a) {
    Object.keys(this.indices).forEach(b => {
      this.addIndexEntry(a, b, a[b]);
    });
  }
  removeFromIndices(a) {
    Object.keys(this.indices).forEach(b => {
      this.removeIndexEntry(a, b, a[b]);
    });
  }
  removeIndexEntry(a, b, c) {
    this.getIndices(b).forEach(e => removeItemFromIndex(a, e, c));
  }
  addIndexEntry(a, b, c) {
    this.getIndices(b).forEach(e => addItemToIndex(a, e, c));
  }
  onItemMutation(a, b) {
    const c = this;
    if (!c._indicesInvalid && Object.keys(c.indices).length > 1) {
      Object.keys(b).forEach(e => {
        let f;
        const g = c.indices[e];
        if (g) {
          const {
            value: i,
            oldValue: j
          } = b[e];
          c.removeIndexEntry(a, e, j);
          c.addIndexEntry(a, e, i);
        }
        if (c.hasCompositeIndex) {
          const k = Object.values(c.indices).find(l => {
            let n;
            return (n = l.dependentOn) == null ? void 0 : n[e];
          });
          if (k) {
            const l = {};
            for (const p in k.dependentOn) {
              l[p] = ((f = b[p]) == null ? void 0 : f.oldValue) || a[p];
            }
            const n = a.buildIndexKey(l);
            c.removeIndexEntry(a, k.property, n);
            c.addIndexEntry(a, k.property, a[k.property]);
          }
        }
      });
    }
  }
};
Collection._$name = 'Collection';
const nonPrimitives = new WeakMap();
const safeIndexKey = a => {
  if (a && typeof a === 'object') {
    let b = nonPrimitives.get(a);
    if (b === void 0) {
      b = Symbol('bscik');
      nonPrimitives.set(a, b);
    }
    a = b;
  }
  return a;
};
const Bag = class {
  constructor(a) {
    const b = this;
    b.generation = 0;
    b.items = new Set();
    b.idMap = {};
    b.idProperty = 'id';
    if (a) {
      if (a.idProperty) {
        b.idProperty = a.idProperty;
      }
      if (a.values) {
        b.values = a.values;
      }
    }
  }
  get(a) {
    return this.idMap[safeIndexKey(a)];
  }
  get count() {
    return this.items.size;
  }
  add(...a) {
    if (a.length === 1 && Array.isArray(a[0])) {
      a = a[0];
    }
    const b = this, {
        items: c,
        idMap: e,
        idProperty: f
      } = b, g = a.length;
    for (let j = 0; j < g; j++) {
      const k = a[j], l = keyTypes[typeof k] ? k : safeIndexKey(k[f]), n = e[l];
      if (n == null) {
        e[l] = k;
        c.add(k);
        b.generation++;
      } else if (n !== k) {
        e[l] = k;
        c.delete(n);
        c.add(k);
      }
    }
  }
  at(a) {
    let b, c;
    if (a < 0) {
      a += this.count;
    }
    for (b of this.items) {
      if (!a--) {
        c = b;
        break;
      }
    }
    return c;
  }
  remove(a) {
    a = ArrayHelper.asArray(a);
    const {
        items: b,
        idMap: c,
        idProperty: e
      } = this, f = a.length;
    for (let g = 0; g < f; g++) {
      const j = a[g], k = keyTypes[typeof j] ? j : safeIndexKey(j[e]), l = c[k];
      if (l != null) {
        b.delete(l);
        delete c[k];
        this.generation++;
      }
    }
  }
  clear() {
    this.items.clear();
    this.idMap = {};
    this.generation++;
  }
  countOf(a) {
    let b = 0, c;
    for (c of this.items) {
      if (a(c)) {
        ++b;
      }
    }
    return b;
  }
  changeId(a, b) {
    const c = this, {
        idMap: e,
        idProperty: f
      } = c, g = keyTypes[typeof a] ? a : safeIndexKey(a[f]), i = c.get(g);
    if (i) {
      const j = c.get(b);
      if (j && i !== j) {
        throw new Error('Attempt to set item ' + g + ' to already existing member\'s id ' + b);
      }
      i[f] = b;
      delete e[g];
      e[b] = i;
    }
  }
  filter(a, b) {
    const {items: c} = this, e = [];
    let f = 0;
    c.forEach(g => {
      if (a.call(b, g, f++, c)) {
        e.push(g);
      }
    });
    return e;
  }
  includes(a) {
    const b = keyTypes[typeof a] ? a : safeIndexKey(a[this.idProperty]);
    return Boolean(this.idMap[b]);
  }
  map(a, b) {
    const {items: c} = this, e = new Array(c.size);
    let f = 0;
    c.forEach(g => {
      e[f] = a.call(b, g, f++, c);
    });
    return e;
  }
  forEach(a, b) {
    return this.items.forEach(a, b);
  }
  find(a, b = 0) {
    let c, e;
    if (b < 0) {
      b += this.countOf(a);
    }
    for (c of this.items) {
      if (a(c) && !b--) {
        e = c;
        break;
      }
    }
    return e;
  }
  [Symbol.iterator]() {
    return this.items[Symbol.iterator]();
  }
  indexOf(a, b) {
    let c = -1, e;
    for (e of this.items) {
      if (!b || b(e)) {
        ++c;
        if (e === a) {
          return c;
        }
      }
    }
    return -1;
  }
  get values() {
    return [...this.items];
  }
  set values(a) {
    a = ArrayHelper.asArray(a);
    this.clear();
    this.add.apply(this, a);
    this.generation++;
  }
  get first() {
    for (const a of this.items) {
      return a;
    }
    return null;
  }
  get last() {
    let a = null;
    for (const b of this.items) {
      a = b;
    }
    return a;
  }
  sort(a) {
    this.values = this.values.sort(a);
  }
  some(a, b) {
    return this.values.some(a, b);
  }
};
Bag._$name = 'Bag';
const StoreBag = class extends Bag {
  add(...a) {
    if (a.length === 1 && Array.isArray(a[0])) {
      a = a[0];
    }
    return super.add(...a.filter(b => b.isPersistable));
  }
};
StoreBag._$name = 'StoreBag';
const StoreCRUD_default = a => class b extends (a || Base) {
  static get $name() {
    return 'StoreCRUD';
  }
  static get defaultConfig() {
    return { 'autoCommit': false };
  }
  static get properties() {
    return {
      'isRemoving': false,
      'suspendCount': 0
    };
  }
  remove(c, e = false) {
    const f = this, {storage: g} = f;
    c = ArrayHelper.asArray(c).reduce((i, j) => {
      j = f.getById(j);
      if (j) {
        i.push(j);
      }
      return i;
    }, []);
    if (c.length === 0) {
      return c;
    }
    if (!f.tree && !e && f.trigger('beforeRemove', { 'records': c }) === false) {
      return [];
    }
    f.isRemoving = true;
    if (f.isGrouped) {
      const i = g.count, j = [], k = new Set();
      for (const l of c) {
        const {groupParent: n} = l.instanceMeta(f);
        if (n.meta.collapsed) {
          j.push(l);
        }
        ArrayHelper.remove(n.groupChildren, l);
        ArrayHelper.remove(n.unfilteredGroupChildren, l);
        n.meta.childCount--;
        k.add(n);
      }
      for (const o of k) {
        if (o.groupChildren.length > 0) {
          f.onModelChange(o, {}, {});
        }
      }
      if (j.length) {
        g.trigger('change', {
          'action': 'splice',
          'removed': j,
          'added': [],
          'replaced': [],
          'oldCount': i
        });
      }
    } else if (f.tree) {
      const p = [], q = c.length > 1, r = c[0], u = q ? void 0 : g.indexOf(r), v = c.reduce((z, A) => {
          const {parent: B} = A;
          if (B) {
            if (!z[B.id]) {
              z[B.id] = [
                B,
                []
              ];
            }
            z[B.id][1].push(A);
          }
          return z;
        }, {}), w = Array.from(Object.values(v));
      if (!e && f.trigger('beforeRemove', {
          'records': c,
          'isMove': false,
          'parent': q ? void 0 : r.parent
        }) === false) {
        f.isRemoving = false;
        return [];
      }
      f.suspendAutoCommit();
      w.sort((z, A) => A[0].childLevel - z[0].childLevel);
      for (const [z, A] of w) {
        p.push(...z.removeChild(A, false, true));
      }
      if (!e) {
        f.trigger('remove', {
          'parent': q ? void 0 : r.parent,
          'index': u,
          'isChild': true,
          'allRecords': p,
          'isMove': false,
          'records': c
        });
        f.trigger('change', {
          'action': 'remove',
          'records': c
        });
      }
      f.resumeAutoCommit();
      f.isRemoving = false;
      return c;
    }
    if (c.length === 0) {
      f.isRemoving = false;
      return c;
    }
    for (const B of c) {
      B.beforeRemove(c);
    }
    if (e) {
      f.suspendEvents();
    }
    g.remove(c);
    if (e) {
      f.resumeEvents();
    }
    if (f.autoCommit) {
      f.doAutoCommit();
    }
    f.isRemoving = false;
    return c;
  }
  clear(c = false) {
    let e, f;
    const g = this, {storage: j} = g;
    if (g.storage.totalCount || ((f = (e = g.rootNode) == null ? void 0 : e.unfilteredChildren) == null ? void 0 : f.length)) {
      if (!c && g.trigger('beforeRemove', {
          'records': j.allValues,
          'removingAll': true
        }) === false) {
        return null;
      }
      if (g.rootNode) {
        if (g.isChained) {
          const k = g.getAllDataRecords();
          for (let l = k.length - 1, n; l >= 0; l--) {
            n = k[l];
            if (n && !n.isDestroyed) {
              n.unjoinStore(g);
            }
          }
        } else {
          g.rootNode.clearChildren(c);
        }
      } else if (c) {
        const o = g.registeredRecords;
        for (let p = o.length - 1, q; p >= 0; p--) {
          q = o[p];
          if (q && !q.isDestroyed) {
            q.unjoinStore(g);
          }
        }
      }
      if (c) {
        g.removed.clear();
        j.suspendEvents();
      }
      j.clear();
      if (c) {
        j.resumeEvents();
      }
      g.added.clear();
      g.modified.clear();
    }
  }
  removeAll(c = false) {
    const e = this, f = e.storage;
    let g;
    e.isRemoving = true;
    if (c) {
      f.suspendEvents();
      const j = e.registeredRecords;
      for (let k = j.length - 1, l; k >= 0; k--) {
        l = j[k];
        if (l && !l.isDestroyed && !l.isRoot) {
          l.unjoinStore(e);
        }
      }
    }
    if (e.tree) {
      g = e.rootNode.clear() !== false;
    } else {
      g = e.clear() !== null;
    }
    if (c) {
      f.resumeEvents();
    }
    e.isRemoving = false;
    return g;
  }
  add(c, e = false, f = {}) {
    const g = this, {storage: i} = g;
    c = ArrayHelper.asArray(c);
    if (!(c == null ? void 0 : c.length)) {
      return;
    }
    g.tree = g.tree || Boolean(g.autoTree && c[0].children);
    if (g.tree) {
      const k = new Map(), l = g.modelClass.getFieldDataSource('parentIndex'), {parentIdField: n} = g.modelClass, o = [];
      c.forEach(p => {
        const q = p[n];
        if (!k.has(q)) {
          k.set(q, {
            'append': [],
            'insert': []
          });
        }
        const r = k.get(q);
        if (!p.isModel && l in p) {
          r.insert.push(p);
        } else {
          r.append.push(p);
        }
      });
      k.forEach(({
        append: p,
        insert: q
      }, r) => {
        const u = r == null ? g.rootNode : g.getById(r);
        if (!u) {
          throw new Error('Parent node with id ' + r + ' not found, cannot add children.');
        }
        if (p.length) {
          o.push(...u.appendChild(p, e, f));
        }
        if (q.length) {
          for (const v of q) {
            if ('orderedParentIndex' in v) {
              if ('orderedParentIndex' in f && ObjectHelper.isObject(f.orderedParentIndex)) {
                f.orderedParentIndex.index = v.orderedParentIndex;
              } else {
                f.orderedParentIndex = v.orderedParentIndex;
              }
            }
            o.push(u.insertChild(v, v[l], e, f));
          }
        }
      });
      if (g.isFiltered && g.reapplyFilterOnAdd) {
        g.filter({ 'silent': true });
      }
      if (g.reapplySortersOnAdd && !g.isSyncingDataOnLoad) {
        g.sort();
      }
      return o;
    }
    if (!e && g.trigger('beforeAdd', { 'records': c }) === false) {
      return null;
    }
    g.tree = g.tree || Boolean(g.autoTree && c[0].children);
    if (g.tree) {
      return g.rootNode.appendChild(c);
    }
    const j = g.processRecords(c);
    if (e) {
      g.suspendEvents();
    }
    g.addingClean = f.clean;
    i.add(j);
    g.addingClean = false;
    if (e) {
      g.resumeEvents();
    }
    if (g.autoCommit) {
      g.doAutoCommit();
    }
    return j;
  }
  cloneImmutable(c) {
    if (this.useRawData.enabled && !c[0].isModel && !Object.isExtensible(c[0])) {
      c = ObjectHelper.clone(c);
    }
    return c;
  }
  processRecords(c, e) {
    return this.cloneImmutable(c).map(f => {
      const g = this.processRecord(f.isModel ? f : this.createRecord(f));
      e == null ? void 0 : e.call(this, g);
      return g;
    });
  }
  insert(c, e, f = false) {
    let g;
    const j = this, k = j.storage, l = k.getAt(c), n = k.values, o = [];
    e = ArrayHelper.asArray(e);
    if (j.tree) {
      const v = j.rootNode;
      return v.insertChild(e, (g = v.children) == null ? void 0 : g[c], f);
    }
    if (!f && j.trigger('beforeAdd', { 'records': e }) === false) {
      return null;
    }
    let p, q, r;
    if (n[q = c] === e[0] || n[q = c - 1] === e[0]) {
      for (p = true, r = 0; p && r < e.length; r++) {
        if (e[r] !== n[q + r]) {
          p = false;
        }
      }
    }
    if (p) {
      return null;
    }
    const u = j.processRecords(e, w => {
      const z = k.indexOf(w);
      if (w.children && w.children.length && j.autoTree) {
        j.tree = true;
      }
      if (z > -1) {
        if (z < c && l)
          c--;
        o.push(z);
      }
      w.meta.previousIndex = z;
    });
    j.suspendEvents();
    j.storage.remove(o);
    j.resumeEvents();
    if (f) {
      j.suspendEvents();
    }
    k.splice(c, 0, ...u);
    if (f) {
      j.resumeEvents();
    }
    if (j.autoCommit) {
      j.doAutoCommit();
    }
    return u;
  }
  move(c, e) {
    if (this.isTree) {
      e.parent.insertChild(c, e);
    } else {
      this.storage.move(c, e);
    }
  }
  acceptChanges() {
    const c = this;
    c.added.forEach(e => e.internalClearChanges(true, false));
    c.modified.forEach(e => e.internalClearChanges(true, false));
    c.added.clear();
    c.modified.clear();
    c.removed.clear();
  }
  commit(c = false) {
    const {changes: e} = this;
    if (!c && this.trigger('beforeCommit', { 'changes': e }) === false) {
      return false;
    }
    this.acceptChanges();
    if (!c) {
      this.trigger('commit', { 'changes': e });
    }
    return e;
  }
  revertChanges() {
    const c = this, {changes: e} = c;
    if (e) {
      const f = {
        'action': 'clearchanges',
        'changes': e
      };
      c.add(c.removed.values, true);
      c.remove(c.added.values, true);
      c.modified.forEach(g => g.revertChanges(true));
      c.added.clear();
      c.modified.clear();
      c.removed.clear();
      c.trigger('change', f);
      c.trigger('refresh', f);
    }
  }
  get changes() {
    const c = this, e = c.modified.values.filter(f => f.rawModifications);
    return c.added.count || e.length || c.removed.count ? {
      'added': c.added.values.slice(),
      'modified': e,
      'removed': c.removed.values.slice()
    } : null;
  }
  get hasChanges() {
    return Boolean(this.added.count || this.removed.count || this.modified.values.some(c => c.rawModifications));
  }
  get autoCommit() {
    return this._autoCommit;
  }
  set autoCommit(c) {
    this._autoCommit = c;
    if (c && this.hasChanges) {
      this.commit();
    }
  }
  suspendAutoCommit() {
    this.suspendCount++;
  }
  resumeAutoCommit(c = true) {
    this.suspendCount--;
    if (this.autoCommit && c) {
      this.doAutoCommit();
    }
  }
  doAutoCommit() {
    if (this.suspendCount <= 0) {
      this.commit();
    }
  }
};
const StoreChanges_default = a => class b extends (a || Base) {
  static get $name() {
    return 'StoreChanges';
  }
  static get configurable() {
    return { 'applyChangesetFilterSortTarget': 'changes' };
  }
  applyChangesFromStore(c) {
    const e = this, {changes: f} = c;
    if (!f) {
      return;
    }
    if (f.added) {
      e.add(f.added);
    }
    if (f.removed) {
      e.remove(f.removed.map(g => g.id));
    }
    if (f.modified) {
      f.modified.forEach(g => {
        const i = e.getById(g.id);
        i == null ? void 0 : i.set(g.modifications);
      });
    }
  }
  applyChangeset(c, e = null, f = '$PhantomId', g = true, i = false) {
    let j, k, l, n, o;
    const p = this, q = (j = e == null ? void 0 : e(c, p)) != null ? j : c, r = q.$input || q, {
        added: u,
        updated: v,
        modified: w,
        removed: z
      } = r, A = (k = v != null ? v : w) != null ? k : [], B = p.modelClass.getFieldDataSource('id'), C = p.modelClass.getFieldDataSource('parentId'), E = i ? new Map() : null, F = p.tree, G = [], H = [], I = new Set();
    let J = false, K = [];
    p.trigger('startApplyChangeset');
    p._groupVisibleRecordIds = [];
    p.isGrouped && p.forEach(L => {
      p._groupVisibleRecordIds.push(L.id);
    });
    if ((u == null ? void 0 : u.length) > 0) {
      const L = [], N = [];
      for (const Q of u) {
        let R;
        if (R = p.getById(Q[f]) || p.getById(ObjectHelper.getPath(Q, B))) {
          L.push(Q);
          F && I.add(R.parent);
        } else {
          N.push(Q);
        }
      }
      A.unshift.apply(A, L);
      const O = p.reapplySortersOnAdd;
      p.reapplySortersOnAdd = false;
      const P = (l = p.add(N, false, {
        'orderedParentIndex': { 'skip': true },
        'clean': g
      })) != null ? l : [];
      G.push(...P);
      p.reapplySortersOnAdd = O;
      if (F) {
        for (const S of P) {
          const {parent: T} = S;
          if (T.isRoot) {
            J = true;
            K = [T];
            break;
          }
          if (!T.isRoot && K.every(U => !U.contains(T))) {
            K.push(T);
          }
          if ('orderedParentIndex' in S.originalData) {
            I.add(T);
          }
        }
      }
      for (const U of P) {
        E == null ? void 0 : E.set(U.id, U.data);
        U.internalClearChanges();
      }
      F && I.forEach(V => {
        V.updateChildrenIndices(V.orderedChildren, 'orderedParentIndex', true);
      });
    }
    if ((A == null ? void 0 : A.length) > 0) {
      for (const V of A) {
        const W = V[f], X = ObjectHelper.getPath(V, B), Y = ObjectHelper.getPath(V, C), Z = (n = p.getById(W)) != null ? n : p.getById(X);
        if (Z) {
          const a0 = Y != null ? (o = p.getById(Y)) == null ? void 0 : o.isLeaf : false;
          this.filterTreeHierarchyChanges(I, Z, V);
          const a1 = Z.applyChangeset(V, f, g);
          if (a0) {
            p.toggleCollapse(Z.parent, false);
          }
          if (F && !J && K.every(a2 => !a2.contains(Z))) {
            if (Z.parent.isRoot) {
              J = true;
              K = [Z.parent];
            } else {
              K.push(Z.parent);
            }
          }
          E == null ? void 0 : E.set(X, a1);
          H.push(Z);
        }
      }
    }
    if ((z == null ? void 0 : z.length) > 0) {
      p.applyRemovals(z);
    }
    if (p.applyChangesetFilterSortTarget === 'changes') {
      const a2 = p.filterChangeset(G, H);
      K.push(...a2);
    }
    p.afterChangesetApplied(K);
    if (F && p.isSorted && p.reapplySortersOnAdd && ((u == null ? void 0 : u.length) || (A == null ? void 0 : A.length))) {
      const a3 = new Set();
      K.forEach(a4 => {
        if (a3.has(a4)) {
          return;
        }
        a3.add(a4);
        a4.sortVisibleChildren(false, p.sorterFn);
        a4.sortUnfilteredChildren(false, p.sorterFn);
      });
      p.storage.replaceValues({
        'values': p.collectDescendants(p.rootNode).visible,
        'silent': true
      });
      p.afterPerformSort(false);
    }
    p._groupVisibleRecordIds = null;
    p.trigger('endApplyChangeset');
    return E;
  }
  afterChangesetApplied(c) {
    const e = new Set();
    c.forEach(f => {
      if (e.has(f)) {
        return;
      }
      f.traverse(g => {
        if (e.has(g)) {
          return;
        }
        e.add(g);
        g.sortOrderedChildren(false, false);
        if (g.children) {
          g.updateChildrenIndices(g.children, 'parentIndex', true);
        }
        if (g.unfilteredChildren) {
          g.updateChildrenIndices(g.unfilteredChildren, 'unfilteredIndex', true);
        }
      });
    });
  }
  applyRemovals(c) {
    const e = this, {removed: f} = e, g = e.modelClass.idField, i = [];
    for (const j of c) {
      const k = ObjectHelper.getPath(j, g);
      if (f.includes(k)) {
        const l = f.get(k);
        l.internalClearChanges(false, true, null);
        l.$isPhantom = true;
        f.remove(k);
      } else {
        i.push(k);
      }
    }
    if (e.tree) {
      const n = i.reduce((o, p) => {
        let q;
        if (e.getById(p)) {
          o.add((q = e.getById(p).parentNode) != null ? q : e.rootNode);
        }
        return o;
      }, new Set());
      n.forEach(o => o.sortOrderedChildren(false, false));
    }
    e.remove(i);
    for (const o of i) {
      f.remove(o);
    }
  }
  filterChangeset(c, e) {
    const f = this, {
        isFiltered: g,
        tree: i,
        isGrouped: j,
        filtersFunction: k
      } = f, l = f.isSorted ? f.createSorterFn(f.sorters) : void 0, {
        allValues: n,
        addedValues: o,
        isSorted: p
      } = f.storage, q = l != null || p ? l != null ? l : f.storage.sortFunction : null, r = new Set();
    if (!g) {
      return [];
    }
    let u = false, v;
    if (j) {
      v = f.groupers;
      f.clearGroupers(true);
    }
    if (i) {
      const z = new Set(e.filter(k));
      for (const B of c.filter(k)) {
        z.add(B);
      }
      z.forEach(C => C.bubble(E => z.add(E)));
      z.delete(f.rootNode);
      const A = ArrayHelper.groupByIndexed(Array.from(z).filter(C => {
        let E, F;
        return !((F = (E = C.parent) == null ? void 0 : E.children) == null ? void 0 : F.includes(C));
      }), C => C.parent);
      for (const [C, E] of A) {
        if (C.unfilteredChildren) {
          C.children.push(...E);
          r.add(C);
          f.traverseFilter(C, false, E);
        }
      }
    } else if (q && !j) {
      const {filteredValues: F} = f.storage, G = o ? F.findLastIndex(J => !o.has(J)) + 1 : F.length, H = F.slice(0, G), I = new Set(e.filter(k));
      for (const J of F) {
        if (I.has(J)) {
          I.delete(J);
        }
      }
      for (const K of I) {
        H.push(K);
      }
      H.sort(q);
      F.splice(0, G, ...H);
      u = true;
    } else {
      const L = e.filter(N => k(N) && !f.storage.includes(N));
      if (L.length > 0) {
        f.includeInSubset(n, f.storage.filteredValues, L);
        u = true;
      }
    }
    const w = new Set(c.filter(N => !k(N)));
    if (w.size > 0) {
      if (i) {
        for (const N of w) {
          ArrayHelper.remove(N.parent.children, N);
          r.add(N.parent);
        }
      } else {
        ArrayHelper.remove(f.storage.filteredValues, w);
      }
      u = true;
    }
    if (v) {
      f.group(v[0], null, false, true, true);
      u = true;
    }
    if (i && r.size > 0) {
      f.storage.replaceValues({
        'values': f.collectDescendants(f.rootNode).visible,
        'silent': true
      });
    } else if (u) {
      f._idMap = null;
      f.trigger('refresh');
    }
    return [...r];
  }
  filterTreeHierarchyChanges(c, e, f) {
    if (this.tree && c.has(e.parent)) {
      delete f.orderedParentIndex;
      delete f.parentIndex;
    }
  }
  includeInSubset(c, e, f) {
    const g = new Set(f);
    let i = 0, j = 0, k = g.size === 0;
    while (j < c.length && !k) {
      const l = e[i];
      let n = c[j];
      while (l !== n) {
        if (g.has(n)) {
          e.splice(i, 0, n);
          i++;
          g.delete(n);
          k = g.size === 0;
        }
        n = c[++j];
      }
      if (g.has(l)) {
        g.delete(l);
      }
      if (i < e.length) {
        i++;
      }
    }
    return e;
  }
};
const StoreFilter_default = a => {
  let b;
  return b = class extends (a || Base) {
    static get $name() {
      return 'StoreFilter';
    }
    static get defaultConfig() {
      return {
        'filters': null,
        'reapplyFilterOnAdd': false,
        'reapplyFilterOnUpdate': false
      };
    }
    set reapplyFilterOnAdd(c) {
      this.storage.autoFilter = c;
    }
    get reapplyFilterOnAdd() {
      return this.storage.autoFilter;
    }
    set filters(c) {
      const e = this, g = e.filters;
      g.clear();
      e._filtersFunction = null;
      if (c) {
        if (c.constructor.name === 'Object') {
          for (const i of Object.entries(c)) {
            if (i[0] === 'filterBy' && typeof i[1] === 'function') {
              g.add(new CollectionFilter({ 'filterBy': i[1] }));
            } else {
              g.add(new CollectionFilter(i[1].constructor.name === 'Object' ? Object.assign({ 'property': i[0] }, i[1]) : {
                'property': i[0],
                'value': i[1]
              }));
            }
          }
        } else if (Array.isArray(c)) {
          g.add(...c.map(j => {
            if (j instanceof CollectionFilter) {
              return j;
            }
            return new CollectionFilter(j);
          }));
        } else if (c.isCollection) {
          g.add(...c.values);
        } else {
          g.add(new CollectionFilter({ 'filterBy': c }));
        }
        g.forEach(j => j.owner = e);
      }
    }
    get filters() {
      return this._filters || (this._filters = new Collection({ 'extraKeys': ['property'] }));
    }
    set filtersFunction(c) {
      this._filtersFunction = c;
    }
    get filtersFunction() {
      const c = this, {
          filters: e,
          isGrouped: f
        } = c;
      if (!c._filtersFunction) {
        if (e.count) {
          const g = CollectionFilter.generateFiltersFunction(e);
          c._filtersFunction = i => {
            if (f && i.isSpecialRow) {
              return i.groupChildren.some(g);
            }
            return g(i);
          };
        } else {
          c._filtersFunction = FunctionHelper.returnTrue;
        }
      }
      return c._filtersFunction;
    }
    get isFiltered() {
      return this.filters.values.some(c => !c.disabled);
    }
    isFilteredOut(c) {
      return this.isFiltered && !this.filtersFunction(c);
    }
    traverseFilter(c, e = true, f = void 0) {
      const g = this, {filtersFunction: i} = g, j = !c.isRoot && i(c), k = c.unfilteredChildren || c.children;
      if (!k || !k.length) {
        return j;
      }
      if (!c.unfilteredChildren) {
        c.unfilteredChildren = c.children.slice();
      }
      c.children = c.unfilteredChildren.filter(l => e ? g.traverseFilter(l, e) : i(l) || (f == null ? void 0 : f.has(l)));
      if (g.isSorted) {
        c.children.sort(g.sorterFn);
      }
      c.updateChildrenIndices(c.unfilteredChildren, 'unfilteredIndex', true);
      c.updateChildrenIndices(c.children, 'parentIndex', true);
      return j || Boolean(c.children.length);
    }
    traverseClearFilter(c) {
      const e = this;
      if (c.unfilteredChildren) {
        c.children = c.orderedChildren.slice();
        c.unfilteredChildren = null;
      }
      if (c.children) {
        if (e.isSorted) {
          c.children.sort(e.sorterFn);
        }
        c.children.forEach(f => e.traverseClearFilter(f));
        c.updateChildrenIndices(c.children, 'parentIndex', true);
      }
    }
    get latestFilterField() {
      return this.filters.last ? this.filters.last.property : null;
    }
    addFilter(c, e = false) {
      const f = this;
      c = c instanceof CollectionFilter ? c : new CollectionFilter(c);
      c.owner = f;
      f.filters.add(c);
      if (!e) {
        if (f.remoteFilter) {
          return f.filter().then(() => c);
        } else {
          f.filter();
        }
      }
      return c;
    }
    filter(c) {
      const e = this;
      let f = false, g;
      if (c) {
        let j = typeof c;
        if (j === 'object') {
          if ('silent' in c || 'replace' in c || c.filters) {
            f = c.silent;
            if (c.replace) {
              e.clearFilters(c.silent || c.filters.length !== 0);
            }
            g = c.internal;
            c = c.filters;
            j = typeof c;
          }
        }
        if (c) {
          const k = e.isFiltered;
          e.isConfiguring = true;
          if (Array.isArray(c)) {
            c.forEach(l => e.addFilter(l, true), e);
          } else if (j === 'function') {
            const l = new CollectionFilter(c);
            l.internal = g;
            e.addFilter(l, true);
          } else if (j === 'string') {
            e.addFilter({
              'property': c,
              'value': arguments[1]
            }, true);
          } else {
            e.addFilter(c, true);
          }
          e.isConfiguring = false;
          if (!e.isFiltered && !k) {
            return null;
          }
        }
      }
      e.filtersFunction = null;
      const i = e.performFilter(f);
      e._idMap = null;
      return i;
    }
    suspendFilterOnUpdate() {
      this.filterBatching++;
    }
    resumeFilterOnUpdate(c) {
      this.filterBatching--;
      if (this.filterBatching < 0) {
        throw new Error('No matching `startFilterBatching` call');
      }
      if (this.filterBatching === 0 && this.needsFiltering) {
        this.needsFiltering = false;
        this.performFilter(c);
      }
    }
    performFilter(c) {
      const e = this;
      if (e.filterBatching > 0) {
        e.needsFiltering = true;
        return;
      }
      const {
          storage: f,
          filters: g,
          rootNode: i
        } = e, j = e.count;
      if (!c) {
        e.trigger('beforeFilter', { 'filters': g });
      }
      let k, l;
      if (e.tree) {
        const n = f.values;
        if (e.isFiltered) {
          e.traverseFilter(i);
        } else {
          e.traverseClearFilter(i);
        }
        const o = e.collectDescendants(i).visible;
        f.replaceValues({
          'values': o,
          'silent': true
        });
        const p = ArrayHelper.delta(o, n, true);
        k = p.toAdd;
        l = p.toRemove;
      } else {
        f.ion({
          'change'({
            removed: q,
            added: u
          }) {
            l = q;
            k = u;
          },
          'once': true
        });
        if (e.isFiltered) {
          e.isGrouped && e.includeCollapsed();
          f.addFilter({
            'id': 'primary-filter',
            'filterBy': e.filtersFunction
          });
          e.isGrouped && e.excludeCollapsed();
        } else {
          f.filters.clear();
        }
      }
      e.afterPerformFilter(c || e.isRemoteDataLoading ? null : {
        'action': 'filter',
        'filters': g,
        'oldCount': j,
        'added': k,
        'removed': l,
        'records': e.storage.values
      });
    }
    afterPerformFilter(c) {
      this.resetRelationCache();
      if (c) {
        this.triggerFilterEvent(c);
      }
    }
    triggerFilterEvent(c) {
      this.trigger('filter', c);
      if (!this.remoteFilter) {
        this.trigger('refresh', c);
        this.trigger('change', c);
      }
    }
    filterBy(c) {
      return this.filter(c);
    }
    removeFilter(c, e = false) {
      let f, g;
      const i = this, j = c instanceof CollectionFilter ? c : (f = i.filters.get(c)) != null ? f : i.filters.getBy('property', c);
      if (j) {
        (g = i._filters) == null ? void 0 : g.remove(j);
        i._filtersFunction = null;
        if (!e) {
          if (i.remoteFilter) {
            return i.filter().then(() => j);
          } else {
            i.filter();
          }
        }
        return j;
      }
    }
    clearFilters(c = false) {
      this.filters.remove(this.filters.values.filter(e => !e.internal));
      if (!c) {
        return this.filter();
      }
    }
    convertFilterToString(c) {
      const e = this.filters.getBy('property', c);
      return e && !e.filterBy ? String(e) : '';
    }
    doDestroy() {
      let c;
      (c = this._filters) == null ? void 0 : c.destroy();
      super.doDestroy();
    }
  }, __publicField(b, 'properties', {
    'filterBatching': 0,
    'needsFiltering': false
  }), b;
};
const resortActions = {
  'add': 1,
  'replace': 1
};
const defaultGroupSortFn = ({_groupValue: a}, {_groupValue: b}) => a < b ? -1 : a > b ? 1 : 0;
const arrayAt = (a, b) => a[b < 0 ? a.length + b : b];
const StoreGroup_default = a => {
  let b;
  return b = class extends (a || Base) {
    static get properties() {
      return { 'collapsedGroups': new Set() };
    }
    construct(c) {
      super.construct(c);
      this.ion({
        'change': 'onDataChanged',
        'thisObj': this
      });
    }
    get GroupRow() {
      let c;
      return this._GroupRowClass || (this._GroupRowClass = (c = class extends this.modelClass {
        get isCollapsed() {
          return this.meta.collapsed;
        }
      }, __publicField(c, 'isSpecialRow', true), __publicField(c, 'fields', [{ 'name': '_groupValue' }]), c));
    }
    updateGroupers(c) {
      this.setGroupers(c);
    }
    setGroupers(c, e = null) {
      const f = this, {storage: g} = f;
      let i;
      f._idMap = null;
      if (c == null ? void 0 : c.length) {
        f._groupers = c.map(j => f.createGrouper(j));
      } else if (f.groupers) {
        delete f._groupers;
        f.includeCollapsed();
        g.replaceValues({
          'values': f.removeHeadersAndFooters(g._values),
          'filteredValues': g.isFiltered ? f.removeHeadersAndFooters(g._filteredValues) : null,
          'silent': true
        });
        i = f.group(null, null, null, false, e == null ? void 0 : e.silent);
      }
      return i;
    }
    createGrouper(c) {
      if (!c || c.isGrouper) {
        return c;
      }
      if (typeof c === 'string') {
        c = { 'field': c };
      }
      const {field: e} = c, f = e == null ? void 0 : e.indexOf('.'), g = f > 0, i = e == null ? void 0 : e.split('.'), j = g ? i[0] : e, k = g ? arrayAt(i, -1) : e, l = g ? e.slice(f + 1) : e;
      return {
        'isGrouper': true,
        ...c,
        'complexMapping': g,
        'rootFieldName': j,
        'leafFieldName': k,
        'fieldPath': l
      };
    }
    get unfilteredGroupRecords() {
      let c;
      const e = this;
      if (e.isGrouped) {
        const {generation: f} = e.storage;
        if (((c = e._unfilteredGroupRecords) == null ? void 0 : c.generation) !== f) {
          e._unfilteredGroupRecords = e.storage.allValues.filter(g => g.isSpecialRow);
          e._unfilteredGroupRecords.generation = f;
        }
      }
      return e._unfilteredGroupRecords || [];
    }
    getGroupHeaderForRecord(c, e = false) {
      if (this.isGrouped) {
        return c ? c.meta.groupRowFor ? c : c.instanceMeta(this).groupParent : this.groupRecords.last;
      }
    }
    includeCollapsed() {
      for (const c of this.collapsedGroups) {
        this.expand(this.getById(c), false);
      }
    }
    excludeCollapsed() {
      for (const c of this.collapsedGroups) {
        this.collapse(this.getById(c));
      }
    }
    onDataChange({
      source: c,
      action: e,
      removed: f
    }) {
      let g;
      const i = this, {groupers: j} = i;
      if (j) {
        if (j.length) {
          if (e === 'splice' && (f == null ? void 0 : f.length) || e === 'move') {
            c.replaceValues({
              ...i.prepareGroupRecords(),
              'silent': true
            });
          }
        } else {
          c.replaceValues({
            'values': i.removeHeadersAndFooters(c._values),
            'filteredValues': c.isFiltered ? i.removeHeadersAndFooters(c._filteredValues) : null,
            'silent': true
          });
        }
      }
      (g = super.onDataChange) == null ? void 0 : g.call(this, ...arguments);
    }
    move(c, e) {
      const f = this;
      if (f.isGrouped && !f.tree) {
        let g = e;
        if (e == null ? void 0 : e.isSpecialRow) {
          g = f.getPrev(e, false, false);
          if (!g) {
            return;
          }
        }
        const i = f.getGroupHeaderForRecord(g), j = f.groupers[0].field, k = i.meta.groupRowFor, {reapplyFilterOnUpdate: l} = f;
        f.reapplyFilterOnUpdate = false;
        f.beginBatch();
        c.forEach(n => n.setValue(j, k));
        f.endBatch();
        f.reapplyFilterOnUpdate = l;
        if (f.isFiltered && (e == null ? void 0 : e.isSpecialRow)) {
          const {unfilteredGroupRecords: n} = f, o = n.indexOf(i);
          e = n[o + 1];
        }
      }
      super.move(c, e);
    }
    collapse(c) {
      if (c && !c.meta.collapsed) {
        this.excludeGroupRecords(c);
        c.meta.collapsed = true;
        this.collapsedGroups.add(c.id);
        this.trigger('toggleGroup', {
          'groupRecord': c,
          'collapse': true
        });
        return true;
      }
      return false;
    }
    expand(c, e = true) {
      if (c == null ? void 0 : c.meta.collapsed) {
        this.includeGroupRecords(c);
        c.meta.collapsed = false;
        e && this.collapsedGroups.delete(c.id);
        e && this.trigger('toggleGroup', {
          'groupRecord': c,
          'collapse': false
        });
        return true;
      }
      return false;
    }
    removeHeadersAndFooters(c) {
      return c.filter(e => {
        if (e == null ? void 0 : e.unfilteredGroupChildren) {
          e.groupChildren.length = e.unfilteredGroupChildren.length = e.meta.childCount = 0;
        }
        if ((e == null ? void 0 : e.isSpecialRow) || (e == null ? void 0 : e.isLinked) && e.instanceMeta(this).groupParent) {
          this.unregister(e);
          return false;
        }
        return true;
      });
    }
    prepareGroupRecords() {
      let c;
      const e = this, {
          GroupRow: f,
          isFiltered: g,
          reapplyFilterOnUpdate: i,
          groupRecords: j
        } = e, k = e.groupers[0], l = k.ascending !== false ? 1 : -1, {
          field: n,
          fn: o = defaultGroupSortFn,
          leafFieldName: p
        } = k, q = typeof n === 'function', r = n === e.modelClass.idField, u = new Collection(), v = e._groupVisibleRecordIds || [], w = C => {
          const E = !g || e.filtersFunction(C);
          return i ? E : E || v.includes(C.id);
        }, z = Boolean(e.startGroupsCollapsed && !(j == null ? void 0 : j.count));
      j == null ? void 0 : j.forEach(C => {
        if (C.isCollapsed) {
          e.includeGroupRecords(C);
        }
      });
      const A = e.removeHeadersAndFooters(e.storage._values);
      if (e.sorters.length) {
        A.sort(e.sorterFn);
      }
      if (g) {
        e.filtersFunction = null;
      }
      A.forEach(C => {
        const E = C, F = q ? n(C) : C.getValue(n), G = ArrayHelper.asArray(F == void 0 ? '!!novalue!!' : F), H = Array.isArray(G), I = H && !G.length, J = I ? 1 : G.length;
        for (let K = 0; K < J; K++) {
          const L = I ? [] : G[K], N = 'group-header-' + (typeof L === 'number' ? L : StringHelper.createId(L));
          let O = u.get(N);
          if (!O) {
            O = (j == null ? void 0 : j.get(N)) || new f({
              [e.modelClass.idField]: N,
              [r ? '' : p]: L,
              '_groupValue': L
            }, e, {
              'collapsed': z,
              'specialRow': true,
              'groupRowFor': L,
              'groupField': p,
              'emptyArray': I
            });
            u.add(O);
            O.meta.childCount = 0;
            O.groupChildren = [];
            O.unfilteredGroupChildren = [];
            O.stores = [e];
          }
          e.register(O);
          if (K > 0) {
            const P = E.groupLinks || (E.groupLinks = []);
            C = P[L] || (P[L] = E.link());
            C.stores = [e];
            e.register(C);
          }
          C.instanceMeta(e).groupParent = O;
          if (w(C)) {
            O.groupChildren.push(C);
            O.meta.childCount++;
          }
          O.unfilteredGroupChildren.push(C);
        }
      });
      u._values.sort((C, E) => o(C, E) * l);
      if (e.useGroupFooters) {
        for (let C = 0, E = u.count; C < E; C++) {
          const F = u.values[C], G = F.meta.groupRowFor, H = 'group-footer-' + (typeof G === 'number' ? G : StringHelper.createId(G)), I = e.getById(H) || new f({
              [e.modelClass.idField]: H,
              [r ? '' : p]: G,
              '_groupValue': G
            }, e, {
              'specialRow': true,
              'groupFooterFor': G,
              'groupRecord': F
            });
          I.stores = [e];
          e.register(I);
          I.groupChildren = F.groupChildren;
          u.splice(++C, 0, I);
          E++;
          F.groupChildren.push(I);
          F.unfilteredGroupChildren.push(I);
          F.meta.childCount++;
        }
      }
      e._idMap = null;
      const B = {
        'values': u.reduce((J, K) => {
          if (!K.isGroupFooter) {
            J.push(K);
            if (!K.meta.collapsed) {
              J.push(...K.unfilteredGroupChildren);
            }
          }
          return J;
        }, false, [])
      };
      if (g) {
        B.filteredValues = B.values.filter(w);
      }
      (c = e.groupRecords) == null ? void 0 : c.forEach(J => {
        if (!u.includes(J)) {
          J.destroy();
        }
      });
      e.groupRecords = u;
      return B;
    }
    get isGrouped() {
      let c;
      return Boolean((c = this.groupers) == null ? void 0 : c.length);
    }
    group(c, e, f = false, g = true, i = false) {
      let j, k;
      const l = this, n = l.createGrouper(c);
      let o;
      if (f) {
        l.groupers.push(n);
      } else if (c) {
        if (e == null && !('ascending' in n)) {
          n.ascending = n.fn && ((j = l.groupInfo) == null ? void 0 : j.field) === n.field && ((k = l.groupInfo) == null ? void 0 : k.fn) === n.fn ? !l.groupInfo.ascending : true;
        }
        l.groupInfo = o = n;
        l.groupers = [l.groupInfo];
      }
      if (o) {
        const {prototype: p} = l.modelClass;
        if (o.complexMapping && !Object.prototype.hasOwnProperty.call(p, c)) {
          Object.defineProperty(p, c, {
            'get'() {
              return ObjectHelper.getPath(this, c);
            }
          });
        }
      }
      if (g !== false) {
        if (l.remoteSort && !l.isRemoteDataLoading) {
          return l.sort(null, null, false, true).then(() => l.onAfterGrouping(i));
        } else {
          l.sort(null, null, false, true);
        }
      }
      l.onAfterGrouping(i);
    }
    onAfterGrouping(c) {
      if (c) {
        return;
      }
      const e = this, f = e.groupers || [];
      e.trigger('group', {
        'isGrouped': e.isGrouped,
        'groupers': f,
        'records': e.storage.values
      });
      e.trigger('refresh', {
        'action': 'group',
        'isGrouped': e.isGrouped,
        'groupers': f,
        'records': e.storage.values
      });
    }
    addGrouper(c, e = true) {
      return this.group(c, e, true);
    }
    removeGrouper(c) {
      const e = this, {groupers: f} = e;
      if (!f) {
        return;
      }
      const g = f.findIndex(i => i.field === c);
      if (g > -1) {
        f.splice(g, 1);
        if (!f.length) {
          return e.clearGroupers();
        } else {
          return e.group();
        }
      }
    }
    clearGroupers(c = false) {
      return this.setGroupers(null, { 'silent': c });
    }
    isRecordInGroup(c, e) {
      let f;
      if (!this.isGrouped) {
        return null;
      }
      const g = (f = this.groupers[0]) == null ? void 0 : f.field;
      return c.getValue(g) === e && !c.isSpecialRow;
    }
    isInCollapsedGroup(c) {
      const {groupParent: e} = c.instanceMeta(this);
      return e == null ? void 0 : e.meta.collapsed;
    }
    getGroupRecords(c) {
      if (!this.isGrouped) {
        return null;
      }
      return this.storage.values.filter(e => this.isRecordInGroup(e, c));
    }
    getGroupTitles() {
      if (!this.isGrouped) {
        return null;
      }
      return this.getDistinctValues(this.groupers[0].field);
    }
    onDataChanged({
      changes: c,
      action: e
    }) {
      if (this.isGrouped && (!c && resortActions[e] || c && this.groupers.some(f => f.rootFieldName in c))) {
        this.sort();
      }
    }
    internalIncludeExcludeGroupRecords(c, e) {
      const f = this, g = f.indexOf(c), i = f.allIndexOf(c), {
          id: j,
          storage: k
        } = f, {
          _filteredValues: l,
          _values: n
        } = k, {
          meta: o,
          groupChildren: p,
          unfilteredGroupChildren: q
        } = c;
      if (i === -1 || o.collapsed && !e || !o.collapsed && e) {
        return;
      }
      q.forEach(r => r.instanceMeta(j).hiddenByCollapse = !e);
      if (e) {
        if (l) {
          l.splice(g + 1, 0, ...p.filter(u => !f.isAvailable(u.id)));
        }
        k._values.splice(i + 1, 0, ...q.filter(u => !f.isAvailable(u.id)));
      } else {
        if (l) {
          l.splice(g + 1, p.length);
        }
        n.splice(i + 1, q.length);
      }
      k._indicesInvalid = true;
      f._idMap = null;
    }
    excludeGroupRecords(c) {
      this.internalIncludeExcludeGroupRecords(c, false);
    }
    includeGroupRecords(c) {
      this.internalIncludeExcludeGroupRecords(c, true);
    }
    collectGroupRecords(c, e = true) {
      const f = c ? this.storage.allValues : this.storage.values;
      return f.reduce((g, i) => {
        if (i.isSpecialRow) {
          if (e && !i.isGroupFooter) {
            g.push(i);
          }
          if (i.isGroupHeader) {
            g.push.apply(g, c ? i.unfilteredGroupChildren : i.groupChildren);
          }
        }
        return g;
      }, []);
    }
  }, __publicField(b, '$name', 'StoreGroup'), __publicField(b, 'configurable', {
    'groupers': null,
    'useGroupFooters': false,
    'startGroupsCollapsed': null
  }), b;
};
const StoreProxy_default = a => class b extends (a || Base) {
  static get configurable() {
    return { 'objectify': null };
  }
  initProxy() {
    if (!globalThis.Proxy) {
      throw new Error('Proxy not supported');
    }
    const c = new Proxy(this, {
      'get'(e, f) {
        if (f in e) {
          return e[f];
        }
        if (f === '$store') {
          return e;
        }
        let g = e.getById(f);
        if (!g && !isNaN(parseInt(f))) {
          g = e.getAt(parseInt(f));
        }
        return g;
      },
      'set'(e, f, g) {
        if (f in e || e.isDestroying || f === '_triggered') {
          e[f] = g;
        } else {
          e.add({
            [e.modelClass.idField]: f,
            ...g
          });
        }
        return true;
      },
      'deleteProperty'(e, f) {
        if (e.isDestroying) {
          delete e[f];
          return true;
        }
        return Boolean(e.remove(f).length);
      },
      'has'(e, f) {
        if (f in e) {
          return true;
        }
        if (f.startsWith('{') && f.endsWith('}')) {
          const g = StringHelper.safeJsonParse(f);
          f = g == null ? void 0 : g.id;
        }
        return e.includes(f);
      }
    });
    return c;
  }
};
const StoreRelation_default = a => {
  let b;
  return b = class extends (a || Base) {
    initRelations(c) {
      const e = this, f = e.modelClass.exposedRelations;
      if (c && e.modelRelations) {
        e.modelRelations.forEach(g => {
          let i;
          return (i = g.storeDetacher) == null ? void 0 : i.call(g);
        });
      }
      if ((!e.modelRelations || e.modelRelations.length === 0 || c) && !e.isChained) {
        e.modelRelations = [];
        f == null ? void 0 : f.forEach(g => {
          const i = { ...g }, {
              foreignStore: j,
              relationName: k,
              relatedCollectionName: l
            } = i, n = typeof j === 'string' ? e[j] : j;
          i.dependentStore = e;
          e.modelRelations.push(i);
          if (n) {
            i.foreignStoreProperty = i.foreignStore;
            i.foreignStore = n;
            const o = n.dependentStoreConfigs;
            if (o.has(e)) {
              const p = o.get(e);
              if (c) {
                const q = p.find(r => r.relationName === k);
                if (q) {
                  ArrayHelper.remove(p, q);
                }
              }
              p.push(i);
            } else {
              o.set(e, [i]);
            }
            if (l) {
              n.initRelationCollection(i, e);
            }
            if (n.count > 0) {
              n.updateDependentStores('dataset', n.records);
            }
          }
        });
      }
    }
    initRelationCollection(c, e) {
      const f = this, g = c.relatedCollectionName, i = f.collectionStores || (f.collectionStores = {});
      i[g] = {
        'store': e,
        'config': c
      };
      if (!f[g + 'Store']) {
        f[g + 'Store'] = e;
      }
      if (f.allRecords.length) {
        f.initModelRelationCollection(g, f.allRecords);
      }
    }
    initModelRelationCollection(c, e) {
      const f = this;
      e.forEach(g => {
        g.traverse(i => {
          const j = c in i ? '$related' + StringHelper.capitalize(c) : c;
          Object.defineProperty(i, j, {
            'enumerable': true,
            'configurable': true,
            'get': function () {
              return f.getCollection(this, c);
            },
            'set': function (k) {
              return f.setCollection(this, c, k);
            }
          });
        });
      });
    }
    resetRelationCache() {
      this.relationCache = {};
      this.forEach(c => c.initRelations());
    }
    updateRecordRelationCache(c, e) {
      e == null ? void 0 : e.forEach(f => {
        const {config: g} = f, i = f.related ? f.related.id : c.getValue(g.foreignKey);
        i !== void 0 && this.cacheRelatedRecord(c, i, g.relationName, i);
      });
    }
    getRelationCollection(c, e) {
      let f;
      const g = Model.asId(e);
      return ((f = this.relationCache[c]) == null ? void 0 : f[g]) || [];
    }
    getCollection(c, e) {
      let f;
      const {
        config: g,
        store: i
      } = this.collectionStores[e];
      return ((f = i.relationCache[g.relationName]) == null ? void 0 : f[c.id]) || [];
    }
    setCollection(c, e, f) {
      const {
          config: g,
          store: i
        } = this.collectionStores[e], j = i.relationCache[g.relationName] || (i.relationCache[g.relationName] = {}), k = (j[c.id] || []).slice(), l = [], n = [];
      i.suspendEvents();
      k.forEach(o => {
        if (!f.includes(o)) {
          o[g.foreignKey] = null;
          i.remove(o);
          n.push(o);
        }
      });
      f.forEach(o => {
        if (o.isModel instanceof Model) {
          if (!o.stores.includes(i)) {
            i.add(o);
            l.push(o);
          }
        } else {
          [o] = i.add(o);
          l.push(o);
        }
        o[g.foreignKey] = c.id;
      });
      i.resumeEvents();
      if (n.length) {
        i.trigger('remove', { 'records': n });
        i.trigger('change', {
          'action': 'remove',
          'records': n
        });
      }
      if (l.length) {
        i.trigger('add', { 'records': l });
        i.trigger('change', {
          'action': 'add',
          'records': l
        });
      }
    }
    cacheRelatedRecord(c, e, f, g = null) {
      const i = this, j = i.relationCache[f] || (i.relationCache[f] = {});
      if (g !== null) {
        i.uncacheRelatedRecord(c, f, g);
      }
      if (e != null) {
        ArrayHelper.include(j[e] || (j[e] = []), c);
      }
    }
    uncacheRelatedRecord(c, e = null, f = null) {
      const g = this;
      function i(j, k) {
        const l = g.relationCache[j], n = l == null ? void 0 : l[k];
        if (n) {
          const o = n.indexOf(c);
          o >= 0 && n.splice(o, 1);
          if (n.length === 0) {
            delete l[k];
          }
        }
      }
      if (f != null) {
        i(e, f);
      } else {
        if (c.meta.relationCache) {
          Object.entries(c.meta.relationCache).forEach(([j, k]) => i(j, k == null ? void 0 : k.id));
        }
      }
    }
    updateDependentStores(c, e, f) {
      let g;
      const i = this;
      i.dependentStoreConfigs.forEach(j => {
        j.forEach(k => {
          const {
              dependentStore: l,
              relatedCollectionName: n,
              relationName: o,
              foreignKey: p
            } = k, q = l.relationCache[o];
          if (c === 'dataset') {
            n && this.initModelRelationCollection(n, e);
            l.forEach(r => {
              const u = r.initRelation(k);
              u && l.cacheRelatedRecord(r, u.id, o, u.id);
            });
          } else if (c === 'removeall') {
            l.forEach(r => r.removeRelation(k));
            delete l.relationCache[o];
            return;
          } else if (c === 'replace') {
            e = e.map(u => u[1]);
          }
          if (c === 'add' || c === 'replace') {
            n && this.initModelRelationCollection(n, e);
          }
          if (c !== 'dataset') {
            e.forEach(r => {
              const u = q == null ? void 0 : q[r.id];
              switch (c) {
              case 'remove':
                if (u) {
                  u.forEach(v => v.removeRelation(k));
                }
                break;
              case 'add':
              case 'replace':
                l.forEach(v => {
                  if (v.getValue(p) == r.id) {
                    v.initRelation(k);
                    l.cacheRelatedRecord(v, r.id, o);
                  }
                });
                break;
              }
              if (!f && (u == null ? void 0 : u.length) && k.propagateRecordChanges) {
                u.forEach(v => v.stores.forEach(w => i.notifyRelatedStore(w, v, k.relationName, r)));
              }
            });
          }
        });
      });
      if (c !== 'dataset' && (e == null ? void 0 : e.length)) {
        (g = i.modelRelations) == null ? void 0 : g.forEach(j => {
          if (j.propagateRecordChanges) {
            e.forEach(k => {
              let l;
              const n = k[j.relationName];
              (l = n == null ? void 0 : n.stores) == null ? void 0 : l.forEach(o => {
                i.notifyRelatedStore(o, n, j.relatedCollectionName, k);
              });
            });
          }
        });
      }
    }
    notifyRelatedStore(c, e, f, {meta: g}) {
      if (this.isChained || c.isChained) {
        return;
      }
      let {_ignoreRelationUpdate: i} = g, j = false;
      if (!(i == null ? void 0 : i.size)) {
        g._ignoreRelationUpdate = i = new Set();
        j = true;
      }
      if (!i.has(c)) {
        i.add(this).add(c);
        e.meta._ignoreRelationUpdate = i;
        c.onModelChange(e, { [f]: e[f] }, { [f]: e[f] }, false, true);
        if (j) {
          i.clear();
        }
      }
    }
    updateDependentRecordIds(c, e) {
      let f;
      (f = this.dependentStoreConfigs) == null ? void 0 : f.forEach(g => {
        g.forEach(i => {
          let j;
          const {
              dependentStore: k,
              relationName: l,
              foreignKey: n
            } = i, o = k.relationCache[l], p = (j = o == null ? void 0 : o[c]) == null ? void 0 : j.slice();
          p == null ? void 0 : p.forEach(q => {
            k.cacheRelatedRecord(q, e, l, c);
            q.meta._ignoreRelatedIdUpdate = true;
            q.set(n, e, false, true);
            delete q.meta._ignoreRelatedIdUpdate;
          });
        });
      });
    }
  }, __publicField(b, '$name', 'StoreRelation'), b;
};
const StoreSum_default = a => class b extends (a || Base) {
  static get $name() {
    return 'StoreSum';
  }
  sum(c, e = this.storage.values) {
    if (!e) {
      return 0;
    }
    return e.reduce((f, g) => {
      if (g.isSpecialRow) {
        return f;
      }
      const i = Number(g.getValue(c));
      return isNaN(i) ? f : f + i;
    }, 0);
  }
  min(c, e = this.storage.values) {
    if (!(e == null ? void 0 : e.length)) {
      return 0;
    }
    return e.reduce((f, g) => {
      const i = g.getValue(c), j = typeof (i == null ? void 0 : i.valueOf());
      if (j === 'number' && (i < f || f == null)) {
        f = i;
      }
      return f;
    }, e[0].getValue(c));
  }
  max(c, e = this.storage.values) {
    if (!(e == null ? void 0 : e.length)) {
      return 0;
    }
    return e.reduce((f, g) => {
      const i = g.getValue(c), j = typeof (i == null ? void 0 : i.valueOf());
      if (j === 'number' && (i > f || f == null)) {
        f = i;
      }
      return f;
    }, e[0].getValue(c));
  }
  average(c, e = this.storage.values) {
    if (!(e == null ? void 0 : e.length)) {
      return 0;
    }
    let f = 0;
    const g = e.reduce((i, j) => {
      if (j.isSpecialRow) {
        return i;
      }
      const k = parseFloat(j.getValue(c));
      if (!isNaN(k)) {
        f++;
        return i + k;
      } else {
        return i;
      }
    }, 0);
    return f > 0 ? g / f : 0;
  }
  groupSum(c, e) {
    return this.sum(e, this.getGroupRecords(c));
  }
};
const findInString = (a, b) => String(a).toLowerCase().includes(b);
const matchFns = {
  'string': findInString,
  'number': (a, b) => a === Number(b),
  'boolean': findInString,
  'date': (a, b) => {
    if (a instanceof Date && b instanceof Date) {
      return a - b === 0;
    }
    return String(a.getMonth() + 1).includes(b) || String(a.getDate()).includes(b) || String(a.getFullYear()).includes(b);
  },
  'object': (a, b) => a === b,
  'undefined': (a, b) => a === b
};
const StoreSearch_default = a => class b extends (a || Base) {
  static get $name() {
    return 'StoreSearch';
  }
  search(c, e = null, f, g) {
    const k = this.isTree && !g ? this.rootNode.allChildren : this.getAllDataRecords(g), l = k.length, n = [];
    if (c == null) {
      return [];
    }
    if (typeof c === 'string') {
      c = c.toLowerCase();
    }
    let o, p, q, r, u, v;
    for (o = 0; o < l; o++) {
      q = k[o];
      p = 0;
      for (const w of e || q.fieldNames) {
        r = q.getValue(w);
        u = r instanceof Date ? 'date' : typeof r;
        const z = f == null ? void 0 : f[p];
        if (z) {
          r = z(r);
          u = 'string';
        }
        v = matchFns[u];
        if (r && (v == null ? void 0 : v(r, c))) {
          n.push({
            'index': o,
            'data': q,
            'field': w,
            'id': q.id
          });
        }
        p++;
      }
    }
    return n;
  }
  findByField(c, e, f = false, g = false) {
    const j = this.getAllDataRecords(g), k = j.length, l = new Set(), n = [];
    let o, p, q;
    if (e != null) {
      e = String(e).toLowerCase();
    }
    for (o = 0; o < k; o++) {
      p = j[o];
      q = p.getValue(c);
      if (!f || !l.has(q)) {
        const r = q instanceof Date ? 'date' : typeof q, u = matchFns[r];
        if (e == null && q === e || e === '' && q === '' || e && u(q, e)) {
          n.push({
            'id': p.id,
            'index': o,
            'data': p
          });
          if (f) {
            l.add(q);
          }
        }
      }
    }
    return n;
  }
  find(c, e = false) {
    return this.getAllDataRecords(e).find(c);
  }
  findRecord(c, e, f = false) {
    const g = i => ObjectHelper.isEqual(i[c], e);
    if (this.isTree) {
      return this.query(g, f)[0];
    }
    return this.getAllDataRecords(f).find(g);
  }
  query(c, e = false) {
    return this.getAllDataRecords(e).filter(c);
  }
  some(c, e = false) {
    return this.getAllDataRecords(e).some(c);
  }
};
const StoreSort_default = a => class b extends (a || Base) {
  static get $name() {
    return 'StoreSort';
  }
  static get defaultConfig() {
    return { 'useLocaleSort': null };
  }
  static get configurable() {
    return {
      'sorters': [],
      'reapplySortersOnAdd': false
    };
  }
  get isSorted() {
    return Boolean(this.sorters.length) || this.isGrouped;
  }
  changeSorters(c) {
    return c.map(e => this.normalizeSorterConfig(e, true));
  }
  updateReapplySortersOnAdd(c) {
    this.storage.autoSort = c;
  }
  sort(c, e, f = false, g = false) {
    const i = this, j = i.allRecords, k = i.sorters ? i.sorters.slice() : [];
    let l = null, n;
    if (c) {
      if (Array.isArray(c)) {
        i.sorters = c.map(o => i.normalizeSorterConfig(o, typeof o === 'string' ? true : e));
      } else {
        const o = i.normalizeSorterConfig(c, e);
        if (f) {
          n = i.getCurrentSorterByField(o.field);
          if (n) {
            l = n.ascending;
            n.ascending = o.ascending;
          } else {
            i.sorters.push(o);
          }
        } else {
          i.sorters = [o];
        }
      }
    }
    if (!g && i.trigger('beforeSort', {
        'sorters': i.sorters,
        'records': j,
        'currentSorters': k
      }) === false) {
      i.sorters = k;
      if (l !== null) {
        n.ascending = l;
      }
      return null;
    }
    return i.performSort(g);
  }
  normalizeSorterConfig(c, e) {
    let f, g, i;
    const j = this, k = { 'ascending': e };
    if (typeof c === 'object') {
      ObjectHelper.assign(k, c);
      if (c.fn) {
        delete k.fn;
        k.sortFn = c.fn;
      }
      k.ascending = (f = c.ascending) != null ? f : e;
    } else if (typeof c === 'function') {
      k.sortFn = c;
    } else {
      k.field = c;
    }
    if (k.ascending == null) {
      const l = j.getCurrentSorterByField(k.field);
      k.ascending = l ? !l.ascending : true;
    }
    if (k.sortFn == null) {
      const n = (i = (g = j.modelClass) == null ? void 0 : g.$meta.fields.map[k.field]) == null ? void 0 : i.compareItems;
      if (n) {
        k.sortFn = n;
      }
    }
    return k;
  }
  getCurrentSorterByField(c) {
    return typeof c === 'string' && this.sorters.find(e => e.field === c) || null;
  }
  addSorter(c, e = true) {
    return this.sort(c, e, true);
  }
  removeSorter(c) {
    const e = this.sorters.findIndex(f => f.field === c || f.sortFn === c);
    if (e > -1) {
      this.sorters.splice(e, 1);
      return this.sort();
    }
  }
  clearSorters(c = false) {
    if (this.sorters.length) {
      this.sorters.length = 0;
      return this.sort(void 0, void 0, void 0, c);
    }
  }
  createSorterFn(c) {
    const e = this.useLocaleSort;
    return (f, g) => {
      for (let j = 0; j < c.length; j++) {
        const k = c[j], {
            field: l,
            ascending: n = true,
            useLocaleSort: useLocaleSort = e
          } = k, o = k.fn || k.sortFn, p = n ? 1 : -1;
        if (o) {
          const u = o.call(k, f, g);
          if (u === 0) {
            continue;
          }
          if (u !== null) {
            return u * p;
          }
        }
        const q = f.isModel ? f.getValue(l) : f[l], r = g.isModel ? g.getValue(l) : g[l];
        if (q === r) {
          continue;
        }
        if (q == null) {
          return -p;
        }
        if (r == null) {
          return p;
        }
        if (useLocaleSort && typeof q === 'string') {
          if (useLocaleSort === true) {
            return String(q).localeCompare(r) * p;
          }
          if (typeof useLocaleSort === 'string') {
            return String(q).localeCompare(r, useLocaleSort) * p;
          }
          if (typeof useLocaleSort === 'object') {
            return String(q).localeCompare(r, useLocaleSort.locale, useLocaleSort) * p;
          }
        }
        if (q > r) {
          return p;
        }
        if (q < r) {
          return -p;
        }
      }
      return 0;
    };
  }
  get sorterFn() {
    return this.createSorterFn(this.remoteSort ? [{ 'field': '_remoteSortIndex' }] : this.sorters);
  }
  performSort(c) {
    const e = this, {
        rootNode: f,
        storage: g,
        sorterFn: i
      } = e;
    if (e.tree) {
      !e.isChained && f.traverse(j => {
        if (j.isLoaded && j.isParent) {
          j.children.sort(i);
          j.updateChildrenIndices(j.children, 'parentIndex', true);
        }
      });
      g.replaceValues({
        'values': e.collectDescendants(f).visible,
        'silent': true
      });
    } else if (e.isGrouped) {
      g.replaceValues({
        ...e.prepareGroupRecords(),
        'silent': true
      });
    } else {
      g.replaceValues({
        'values': g.values.sort(i),
        'silent': true
      });
    }
    e.afterPerformSort(c || e.isRemoteDataLoading);
  }
  afterPerformSort(c) {
    if (c) {
      return;
    }
    const e = this;
    e._idMap = null;
    const f = {
      'action': 'sort',
      'sorters': e.sorters,
      'records': e.allRecords
    };
    e.trigger('sort', f);
    e.trigger('refresh', f);
  }
};
const returnTrue = () => true;
const StoreChained_default = a => class b extends (a || Base) {
  static get $name() {
    return 'StoreChained';
  }
  static get defaultConfig() {
    return {
      'chainedFilterFn': null,
      'chainedFields': null,
      'masterStore': null,
      'doRelayToMaster': [
        'add',
        'remove',
        'insert'
      ],
      'dontRelayToMaster': [],
      'excludeCollapsedRecords': true,
      'syncSort': true,
      'chainFilters': null,
      'chainSuspended': 0
    };
  }
  static get properties() {
    return { 'chainedStores': null };
  }
  construct(c) {
    super.construct(c);
    const e = this, {masterStore: f} = e;
    if (f) {
      e.methodNamesToRelay.forEach(g => e[g] = (...i) => e.relayToMaster(g, i));
      e.removeAll = (...g) => {
        f.remove(e.getRange(), ...g);
      };
      f.ion({
        'changePreCommit': e.onMasterDataChangedPreCommit,
        'change': e.onMasterDataChanged,
        'prio': 1,
        'thisObj': e
      });
      if (e.syncSort) {
        f.ion({
          'sort': () => e.sort(f.sorters),
          'thisObj': e
        });
      }
      if (!f.chainedStores) {
        f.chainedStores = [];
      }
      f.chainedStores.push(e);
      e.fillFromMaster();
    }
  }
  get $master() {
    return this.masterStore || this;
  }
  get isChained() {
    return Boolean(this.masterStore);
  }
  set chainedFilterFn(c) {
    this._chainedFilterFn = this.thisObj ? c.bind(this.thisObj) : c;
  }
  get chainedFilterFn() {
    return this._chainedFilterFn || returnTrue;
  }
  get methodNamesToRelay() {
    const c = Array.isArray(this.doRelayToMaster), e = Array.isArray(this.dontRelayToMaster);
    return c && this.doRelayToMaster.filter(f => !e || !this.dontRelayToMaster.includes(f)) || [];
  }
  updateChainedStores() {
    let c;
    (c = this.chainedStores) == null ? void 0 : c.forEach(e => e.fillFromMaster());
  }
  fillFromMaster() {
    const c = this, {
        masterStore: e,
        isTree: f
      } = c, {isFiltered: g} = e;
    let i = [];
    if (!c.isChained) {
      throw new Error('fillFromMaster only allowed on chained store');
    }
    if (c.isChainSuspended) {
      return;
    }
    if (e.isGrouped && g) {
      e.forEach(j => i.push(j), e, {
        'includeFilteredOutRecords': true,
        'includeCollapsedGroupRecords': true
      });
    } else {
      let j = null;
      if (c.chainFilters && g) {
        const k = e.filters.values.filter(l => !l.ignoreForChain);
        if (k.length) {
          j = CollectionFilter.generateFiltersFunction(k);
        }
      }
      i = e.allRecords.filter(l => !l.isSpecialRow && c.chainedFilterFn(l) && (!j || j(l)));
      if (c.masterStore.sorterFn && g && e.isSorted && !e.remoteSort) {
        i.sort(e.sorterFn);
      }
    }
    if (f) {
      c.idRegister = {};
      c.internalIdRegister = {};
      i.forEach(l => {
        if (l.stores.includes(c)) {
          c.register(l);
        } else {
          l.joinStore(c);
        }
      });
      if (c.excludeCollapsedRecords) {
        const l = c.getChildren(c.rootNode);
        i = c.doIncludeExclude(l, true);
      } else {
        i.forEach(n => n.instanceMeta(c).collapsed = false);
      }
    }
    c.isFillingFromMaster = true;
    c.data = i;
    c.isFillingFromMaster = false;
  }
  commitToMaster() {
    const c = this, e = c.masterStore;
    if (!c.isChained) {
      throw new Error('commitToMaster only allowed on chained store');
    }
    e.beginBatch();
    e.remove(c.removed.values);
    e.add(c.added.values);
    e.endBatch();
    return c.commit();
  }
  relayToMaster(c, e) {
    return this.masterStore[c](...e);
  }
  onMasterDataChangedPreCommit(c) {
    this.onMasterDataChanged(c);
    this.$masterEventhandled = true;
  }
  onMasterDataChanged({
    action: c,
    changes: e,
    isMove: f
  }) {
    let g;
    const i = this;
    if (i.$masterEventhandled) {
      i.$masterEventhandled = false;
      return;
    }
    if (f && c === 'remove') {
      return;
    }
    if (c !== 'update' || i.chainedFields === '*' || ((g = i.chainedFields) == null ? void 0 : g.some(j => j in e))) {
      i.fillFromMaster();
    }
  }
  makeChained(c = returnTrue, e, f) {
    if (this.isChained) {
      const g = c;
      c = i => g(i) && this.chainedFilterFn(i);
    }
    return new ((f == null ? void 0 : f.storeClass) || this.constructor)({
      'id': this.id + '-chained-' + StringHelper.generateUUID(),
      'tree': false,
      'autoTree': false,
      ...f || {},
      'masterStore': this.$master,
      'modelClass': this.modelClass,
      'syncDataOnLoad': false,
      'chainedFilterFn': c,
      'chainedFields': e
    });
  }
  chain() {
    return this.makeChained(...arguments);
  }
  doDestroy() {
    let c;
    (c = this.chainedStores) == null ? void 0 : c.forEach(e => e.destroy());
    super.doDestroy();
  }
  suspendChain() {
    this.chainSuspended++;
  }
  resumeChain(c = false) {
    if (this.chainSuspended && !--this.chainSuspended && c) {
      this.fillFromMaster();
    }
  }
  get isChainSuspended() {
    return this.chainSuspended > 0;
  }
};
const StoreState_default = a => class b extends (a || Base) {
  static get $name() {
    return 'StoreState';
  }
  getState() {
    const {
        sorters: c,
        groupers: e,
        filters: f
      } = this, g = {};
    if (c == null ? void 0 : c.length) {
      g.sorters = c.map(i => {
        const j = ObjectHelper.cleanupProperties(ObjectHelper.clone(i));
        delete j.fn;
        delete j.sortFn;
        return j;
      });
    }
    if (e == null ? void 0 : e.length) {
      g.groupers = e.map(i => {
        const j = ObjectHelper.cleanupProperties(ObjectHelper.clone(i));
        delete j.fn;
        return j;
      });
      g.collapsedGroups = [...this.collapsedGroups];
    }
    if (f == null ? void 0 : f.values.length) {
      g.filters = f.values.map(i => {
        const j = ObjectHelper.cleanupProperties(ObjectHelper.clone(i.config));
        j.value = i.value;
        if (j.caseSensitive) {
          delete j.caseSensitive;
        }
        return j;
      });
    }
    return g;
  }
  applyState(c) {
    let e;
    const f = this, {
        sorters: sorters = [],
        groupers: groupers = [],
        filters: filters = []
      } = c, {
        sortParamName: g,
        filterParamName: i
      } = f;
    if (f.isSorted || sorters.length !== 0 || groupers.length !== 0) {
      f.sorters = sorters.filter(j => j.field || j.sortFn && !g);
      f.groupers = groupers.slice();
      f.sort();
    }
    (e = c.collapsedGroups) == null ? void 0 : e.forEach(j => {
      const k = f.getById(j);
      if (k) {
        f.collapse(k, true);
      }
    });
    if (f.isFiltered || filters.length !== 0) {
      f.filters = filters.filter(j => j.property || j.filterBy && !i);
      f.filter();
    }
  }
};
const zeroPad = a => String(a).padStart(6, '0');
const Wbs = class _Wbs {
  constructor(a) {
    this.value = a;
    this._padded = null;
  }
  set value(a) {
    this._value = String(a != null ? a : '');
  }
  get value() {
    return this._value;
  }
  static from(a) {
    return a == null ? a : a instanceof _Wbs ? a : new _Wbs(a);
  }
  static pad(a) {
    return a instanceof _Wbs ? a.valueOf() : _Wbs.split(a).map(zeroPad).join('.');
  }
  static split(a) {
    let b, c;
    if (a || a === 0) {
      switch (typeof a) {
      case 'object':
        a = String(a);
      case 'string':
        c = a.split('.');
        for (b = c.length; b-- > 0;) {
          c[b] = parseInt(c[b], 10);
        }
        break;
      case 'number':
        c = [a];
        break;
      }
    }
    return c || [];
  }
  static compare(a, b) {
    if (a === b) {
      return 0;
    }
    if (!a || !b) {
      return a ? 1 : b ? -1 : 0;
    }
    a = _Wbs.pad(a);
    b = _Wbs.pad(b);
    return a < b ? -1 : b < a ? 1 : 0;
  }
  append(a) {
    const b = this.value;
    return _Wbs.from(b ? b + '.' + a : a);
  }
  isEqual(a) {
    return !_Wbs.compare(this, a);
  }
  match(a) {
    let b = false;
    if (a) {
      const c = this.value, e = a[0] === '*', f = a.endsWith('*'), g = a.length;
      if (e === f) {
        b = c.indexOf(e ? a.substr(1, g - 2) : a) > -1;
      } else if (e) {
        b = c.endsWith(a.substr(1));
      } else {
        b = c.startsWith(a.substr(0, g - 1));
      }
    }
    return b;
  }
  toString() {
    return this.value;
  }
  toJSON() {
    return this.toString();
  }
  valueOf() {
    let a;
    return (a = this._padded) != null ? a : this._padded = _Wbs.pad(this.value);
  }
};
Wbs._$name = 'Wbs';
const emptyArray5 = Object.freeze([]);
const StopBranch = Symbol('StopBranch');
const nullKeyUUID = StringHelper.generateUUID();
const StoreTree_default = a => {
  let b;
  return b = class extends (a || Base) {
    get StopBranch() {
      return StopBranch;
    }
    get isTree() {
      return this.tree || this.masterStore && this.masterStore.tree;
    }
    get leaves() {
      const c = this, e = [];
      if (c.isTree) {
        c.traverse(f => {
          if (f.isLeaf) {
            e.push(f);
          }
        });
        return e;
      } else {
        c.allRecords.forEach(f => {
          if (f.isLeaf) {
            e.push(f);
          }
          f.traverse(g => {
            if (g.isLeaf) {
              e.push(g);
            }
          }, true);
        });
      }
      return e;
    }
    async loadChildren(c) {
    }
    onNodeAddChild(c, e, f, g, i = false) {
      let j;
      const k = this, l = c === k.rootNode && c.isLoading, {storage: n} = k, {previousSibling: o} = e[0];
      let p;
      const {
        visible: q,
        all: r
      } = k.collectDescendants(e, void 0, void 0, {
        'inCollapsedBranch': !(c.isExpanded(k) && c.ancestorsExpanded(k)),
        'applyFilter': k.isFiltered && k.reapplyFilterOnAdd
      });
      if (!l && r.length) {
        for (const u of r) {
          if (!k.modified.includes(u) && !g[u.id]) {
            if (k.removed.includes(u)) {
              k.removed.remove(u);
            } else if (!u.isLinked) {
              k.added.add(u);
            }
          }
        }
      }
      if (l && k.rootVisible) {
        q.unshift(c);
        r.unshift(c);
      }
      if (q.length) {
        if (f === 0 || !o) {
          p = n.indexOf(c);
        } else {
          p = n.indexOf(o) + o.getDescendantCount(true, k);
        }
        n.suspendEvents();
        k.storage.splice(++p, 0, q);
        n.resumeEvents();
        k._idMap = null;
      } else {
        k._allRecords = null;
      }
      k.updateDependentStores('add', e);
      if (l && q.length) {
        if (k.sorters.length) {
          k.sort(null, null, false, true);
        }
        (j = k.afterLoadData) == null ? void 0 : j.call(k);
        if (!i) {
          const v = {
            'action': 'dataset',
            'data': k._data,
            'records': q
          };
          k.trigger('refresh', v);
          k.trigger('change', v);
        }
      } else if (!i) {
        const w = {
          'action': 'add',
          'parent': c,
          'isChild': true,
          'isMove': g,
          'records': e,
          'allRecords': r,
          'index': p
        };
        k.trigger('add', w);
        k.trigger('change', w);
        if (g && Object.values(g).some(z => z)) {
          const z = {
            'newParent': c,
            'records': e.filter(A => g[A.id]),
            'oldParents': e.map(A => {
              return k.getById(A.meta.oldParentId);
            })
          };
          k.trigger('move', z);
        }
      }
    }
    onNodeRemoveChild(c, e, f, g = {
      'isMove': false,
      'silent': false,
      'unfiltered': false
    }) {
      const i = this, {storage: j} = i, k = [], l = [], {
          isMove: n,
          silent: o,
          unfiltered: p
        } = g, q = p && i.isFiltered, r = q && c.unfilteredChildren ? c.unfilteredChildren : e;
      i.collectDescendants(r, k, l, {
        'inCollapsedBranch': !(c.isExpanded(i) && c.ancestorsExpanded(i)),
        'unfiltered': q
      });
      if (!n) {
        for (const u of e) {
          u.unjoinStore(i);
        }
        for (const v of l) {
          if (v.stores.includes(i)) {
            v.unjoinStore(i);
          }
          if (i.added.includes(v)) {
            i.added.remove(v);
          } else if (!v.isLinked) {
            i.removed.add(v);
          }
        }
        i.modified.remove(l);
      }
      if (k.length) {
        f = j.indexOf(k[0]);
        if (f > -1) {
          j.suspendEvents();
          j.splice(f, k.length);
          j.resumeEvents();
          i._idMap = null;
        }
      } else {
        f = -1;
        i._allRecords = null;
      }
      if (!o && (i.fireRemoveEventForMoveAction || !n)) {
        const w = {
          'action': 'remove',
          'parent': c,
          'isChild': true,
          'isMove': n,
          'records': e,
          'allRecords': l,
          'index': f
        };
        i.trigger('remove', w);
        i.trigger('change', w);
      }
      return l;
    }
    collectDescendants(c, e = [], f = [], g = {}) {
      let j;
      const k = this, {
          inCollapsedBranch: inCollapsedBranch = false,
          unfiltered: unfiltered = false,
          applyFilter: applyFilter = false
        } = g, l = Array.isArray(c) ? c : (j = k.getChildren(c, unfiltered)) != null ? j : [];
      if (applyFilter) {
        return {
          'visible': !inCollapsedBranch && l.flatMap(n => k.collectVisibleNodeDescendantsFiltered(n)),
          'all': l.flatMap(n => n.allChildren)
        };
      } else {
        for (let n = 0, o = l.length, p; n < o; n++) {
          p = l[n];
          if (!inCollapsedBranch) {
            e.push(p);
          }
          f.push(p);
          k.collectDescendants(p, e, f, {
            'inCollapsedBranch': inCollapsedBranch || !p.isExpanded(k),
            'unfiltered': unfiltered
          });
        }
        return {
          'visible': e,
          'all': f
        };
      }
    }
    collectVisibleNodeDescendantsFiltered(c) {
      const e = c.unfilteredChildren || c.children;
      if (!e || e.length === 0 || !c.isLeaf && !c.isExpanded(this)) {
        return this.filtersFunction(c) ? [c] : [];
      }
      const f = e.flatMap(g => this.collectVisibleNodeDescendantsFiltered(g));
      return f.length || this.filtersFunction(c) ? [
        c,
        ...f
      ] : [];
    }
    getChildren(c, e = false) {
      const f = this, g = (e || f.isChained) && c.unfilteredChildren || c.children;
      return !(g == null ? void 0 : g.length) ? emptyArray5 : f.isChained ? g.filter(f.chainedFilterFn).sort(f.sorterFn) : g;
    }
    internalToggleTreeSubRecords(c, e) {
      const f = this, {storage: g} = f, i = g.indexOf(c), j = f.doIncludeExclude(f.getChildren(c), e);
      if (f.isFiltered && e && c.unfilteredChildren) {
        f.updateChildrenHiddenState(c);
      }
      if (j.length && i !== false) {
        g.suspendEvents();
        if (e) {
          g.splice(i + 1, 0, ...j);
          const k = {
            'action': 'add',
            'isExpand': true,
            'parent': c,
            'records': j,
            'index': i + 1
          };
          f.trigger('add', k);
          f.trigger('change', k);
        } else {
          g.splice(i + 1, j.length);
          const l = {
            'action': 'remove',
            'isCollapse': true,
            'parent': c,
            'records': j,
            'index': i + 1
          };
          f.trigger('remove', l);
          f.trigger('change', l);
        }
        g.resumeEvents();
        f._idMap = null;
      }
    }
    updateChildrenHiddenState(c) {
      let e;
      (e = c.unfilteredChildren) == null ? void 0 : e.forEach(f => {
        f.instanceMeta(this.id).hidden = false;
        if (!f.isLeaf) {
          this.updateChildrenHiddenState(f);
        }
      });
    }
    doIncludeExclude(c, e, f = []) {
      const g = this, j = (c == null ? void 0 : c.length) || 0;
      for (let k = 0; k < j; k++) {
        const l = c[k];
        if (!g.isChained || g.chainedFilterFn(l)) {
          const n = l.instanceMeta(g.id);
          if (e || !n.hidden) {
            f.push(l);
          }
          n.hidden = !e;
          if (l.isExpanded(g)) {
            g.doIncludeExclude(g.getChildren(l), e, f);
          }
        }
      }
      return f;
    }
    async toggleCollapse(c, e) {
      const f = this, g = f.getById(c), i = g.instanceMeta(f);
      if (e === void 0) {
        e = !i.collapsed;
      }
      if (!i.isLoadingChildren && !g.isLeaf && g.isExpanded(f) === e) {
        f.trigger('beforeToggleNode', {
          'record': g,
          'collapse': e
        });
        i.collapsed = e;
        if (i.collapsed) {
          f.onNodeCollapse(g);
          return true;
        } else {
          f.onNodeExpand(g);
          let j = true;
          if (!g.isLoaded) {
            i.isLoadingChildren = true;
            try {
              await f.loadChildren(g);
            } catch (k) {
              i.collapsed = true;
              j = false;
              f.trigger('loadChildrenException', {
                'record': g,
                'exception': k
              });
            } finally {
              i.isLoadingChildren = false;
            }
          }
          return j;
        }
      }
    }
    onNodeCollapse(c) {
      if (c.ancestorsExpanded(this)) {
        return this.internalToggleTreeSubRecords(c, false);
      }
    }
    onNodeExpand(c) {
      if (c.ancestorsExpanded(this)) {
        return this.internalToggleTreeSubRecords(c, true);
      }
    }
    transformToTree(c) {
      const {
          parentIdField: e,
          idField: f,
          childrenField: g
        } = this.modelClass, i = new Map(), j = new Set(), k = [];
      for (const n of c) {
        const o = n == null ? void 0 : n[f];
        if (o != null) {
          i.set(o, n);
        }
      }
      const l = p => {
        const q = Object.assign({}, p);
        q[g] = [];
        i.set(q[f], q);
        j.add(p[f]);
        return q;
      };
      for (let p of c) {
        if (p) {
          const q = p[f];
          if (j.has(q)) {
            p = i.get(q);
          } else {
            p = l(p);
          }
          const r = p[e];
          if (r != null) {
            let u = i.get(r);
            if (u) {
              if (!u[g]) {
                u = l(u);
              }
              u[g].push(p);
            }
          } else {
            if (p[g]) {
              k.push(p);
            } else if (p[f] != null) {
              k.push(l(p));
            } else {
              k.push(p);
            }
          }
        }
      }
      i.forEach(v => {
        let w, z;
        (z = (w = v[g]).sort) == null ? void 0 : z.call(w, (A, B) => A.parentIndex - B.parentIndex);
      });
      return k;
    }
    treeify(c, e, f = false, g) {
      const {length: j} = c, k = [], l = [], n = {};
      let o, p;
      for (o = 0; o < j; o++) {
        let r = c[o];
        r = r.field || r;
        if (!c[o].call) {
          c[o] = u => u[r];
          c[o].fieldName = r;
        }
        k[o] = new Map();
      }
      this.rootNode.traverse(u => {
        p = [];
        if (u.isLeaf) {
          for (o = 0; o < j; o++) {
            const v = c[o], w = g ? g(v(u), v.fieldName) : v(u), z = k[o], A = w === '' ? nullKeyUUID : w, B = ArrayHelper.asArray(A) || [nullKeyUUID], C = [];
            if (B[0] === StopBranch) {
              break;
            }
            const E = p.length > 0 ? p : [null];
            E.forEach(F => {
              let G;
              for (let H = 0, {length: I} = B; H < I; H++) {
                const J = B[H], K = ((G = F == null ? void 0 : F.path) != null ? G : '') + ((J == null ? void 0 : J.isModel) ? J.id : J) + '/', L = StringHelper.makeValidDomId('generated_' + K, '_'), N = z.get(L) || z.set(L, {
                    'id': L,
                    'key': B[H] === nullKeyUUID ? '' : J,
                    'path': K,
                    'expanded': true,
                    'readOnly': true,
                    'children': [],
                    'generatedParent': true,
                    'field': v.fieldName,
                    'firstGroupChild': u
                  }).get(L);
                C.push(N);
                if (F && !F.children.includes(N)) {
                  F.children.push(N);
                }
              }
            });
            p = C;
          }
          if (p.length) {
            p.forEach((F, G) => F.children.push(G ? u.link() : u));
          } else {
            l.push(u);
          }
        }
      }, true);
      if (e || f) {
        k.forEach(u => u.forEach((v, w, z) => {
          e == null ? void 0 : e(v);
          if (f) {
            v = this.createRecord(v);
            z.set(w, v);
          }
        }));
      }
      n.children = [
        ...k[0].values(),
        ...l
      ];
      const q = u => {
        if (u.children) {
          u.children.sort((v, w) => {
            if (v.isLeaf === w.isLeaf) {
              if (v.isLeaf) {
                let z;
                for (let A = 0; !z && A < j; A++) {
                  const B = c[A](v), C = c[A](w);
                  if (B === StopBranch || C === StopBranch) {
                    return v.isLeaf < w.isLeaf ? -1 : v.isLeaf > w.isLeaf ? 1 : 0;
                  }
                  z = B < C ? -1 : C < B ? 1 : 0;
                }
                return z;
              } else if (typeof v.key === 'string' && typeof w.key === 'string') {
                return v.key.localeCompare(w.key, void 0, { 'numeric': true });
              } else {
                return v.key < w.key ? -1 : v.key > w.key ? 1 : 0;
              }
            } else {
              return v.isLeaf < w.isLeaf ? -1 : v.isLeaf > w.isLeaf ? 1 : 0;
            }
          });
          u.children.forEach(q);
        }
      };
      q(n);
      return n;
    }
    treeifyFlatData(c) {
      const {
        childrenField: e,
        parentIdField: f
      } = this.modelClass;
      let g = false, i = true;
      for (const j of c) {
        if ((j == null ? void 0 : j.isModel) || Array.isArray(j == null ? void 0 : j[e])) {
          i = false;
          break;
        }
        if ((j == null ? void 0 : j[f]) != null) {
          g = true;
        }
      }
      if (i && g) {
        c = this.transformToTree(c);
      }
      return c;
    }
    async indent(c) {
      const e = this;
      c = Array.isArray(c) ? c : [c];
      c = c.filter(f => {
        let g = Boolean(f.previousSibling);
        while (g && !f.isRoot) {
          g = !c.includes(f.parent);
          f = f.parent;
        }
        return g;
      });
      if (c.length && e.trigger('beforeIndent', { 'records': c }) !== false) {
        c.sort((f, g) => Wbs.compare(f.wbsCode, g.wbsCode));
        e.beginBatch();
        for (const f of c) {
          const g = f.previousSibling;
          g.appendChild(f);
          e.toggleCollapse(g, false);
        }
        e.endBatch();
        e.trigger('indent', { 'records': c });
        e.trigger('change', {
          'action': 'indent',
          'records': c
        });
      }
    }
    async outdent(c) {
      const e = this;
      c = Array.isArray(c) ? c : [c];
      c = c.filter(f => {
        const {parent: g} = f;
        let i = g && !g.isRoot;
        while (i && !f.isRoot) {
          i = !c.includes(g);
          f = f.parent;
        }
        return i;
      });
      if (c.length && e.trigger('beforeOutdent', { 'records': c }) !== false) {
        c.sort((f, g) => Wbs.compare(f.wbsCode, g.wbsCode));
        e.beginBatch();
        for (const f of c) {
          const {parent: g} = f, i = g.children.slice(g.children.indexOf(f) + 1);
          g.parent.insertChild(f, g.nextSibling);
          f.appendChild(i);
          e.toggleCollapse(f, false);
        }
        e.endBatch();
        e.trigger('outdent', { 'records': c });
        e.trigger('change', {
          'action': 'outdent',
          'records': c
        });
      }
    }
  }, __publicField(b, '$name', 'StoreTree'), __publicField(b, 'StopBranch', StopBranch), __publicField(b, 'configurable', {
    'transformFlatData': null,
    'fireRemoveEventForMoveAction': false
  }), b;
};
const WalkHelper = class {
  static preWalk(a, b, c) {
    const e = [a];
    let f, g;
    while (e.length) {
      f = e.pop();
      c(f);
      g = b(f);
      if (g) {
        e.push.apply(e, g.slice().reverse());
      }
    }
  }
  static preWalkWithParent(a, b, c) {
    const e = [{
        'node': a,
        'parent': null
      }];
    while (e.length) {
      const {
        parent: f,
        node: g
      } = e.pop();
      c(f, g);
      const i = b(g);
      if (Array.isArray(i)) {
        e.push(...i.slice().reverse().map(j => ({
          'node': j,
          'parent': g
        })));
      }
    }
  }
  static preWalkUnordered(a, b, c) {
    const e = [a];
    let f, g;
    while (e.length) {
      f = e.pop();
      c(f);
      g = b(f);
      if (g) {
        e.push.apply(e, g);
      }
    }
  }
  static postWalk(a, b, c) {
    const e = new Map(), f = [a];
    let g, i;
    while (f.length) {
      g = f[f.length - 1];
      if (e.has(g)) {
        c(g);
        f.pop();
      } else {
        i = b(g);
        if (i) {
          f.push(...i.slice().reverse());
        }
        e.set(g, g);
      }
    }
  }
  static prePostWalk(a, b, c, e) {
    const f = new Map(), g = [a];
    let i, j;
    while (g.length) {
      i = g[g.length - 1];
      if (f.has(i)) {
        e(i);
        g.pop();
      } else {
        c(i);
        j = b(i);
        if (j) {
          g.push(...j.slice().reverse());
        }
        f.set(i, i);
      }
    }
  }
};
WalkHelper._$name = 'WalkHelper';
const StoreSync_default = a => class b extends (a || Base) {
  static get $name() {
    return 'StoreSync';
  }
  static get configurable() {
    return {
      'syncDataOnLoad': null,
      'shouldSyncDataset': null,
      'shouldSyncRecord': null,
      'reorderOnSync': true
    };
  }
  syncDataset(c) {
    let e, f, g, j, k;
    const l = this, {storage: n} = l, o = (j = (e = l.shouldSyncDataset) == null ? void 0 : e.call(l, {
        'data': c,
        'store': l,
        'records': l.records
      })) != null ? j : (g = (f = l.project) == null ? void 0 : f.shouldSyncDataset) == null ? void 0 : g.call(f, {
        'data': c,
        'store': l,
        'records': l.records
      });
    if (o === false) {
      return;
    }
    l.trigger('startApplyChangeset');
    l.isSyncingDataOnLoad = true;
    const {
      toAdd: p,
      toRemove: q,
      toMove: r,
      updated: u,
      ids: v
    } = l.tree ? l.syncTreeDataset(c, o) : l.syncFlatDataset(c, o);
    let w = (k = l.syncDataOnLoad) == null ? void 0 : k.threshold, z = false;
    if (w) {
      if (typeof w === 'string') {
        w = parseInt(w, 10) / 100 * l.count;
      }
      z = p.length + q.length + r.length + u.length > w;
    }
    if (l.tree) {
      if (p.length) {
        const B = l.add(p, z);
        B.forEach(C => C.clearParentId());
      }
      if (r.length) {
        for (const {
              parent: C,
              node: E,
              index: F
            } of r) {
          const G = l.getById(C.id);
          G.insertChild(E, F);
        }
      }
      l.remove(q, z);
    } else {
      if (z) {
        l.suspendEvents();
      }
      l.remove(q);
      l.add(p);
      if (z) {
        l.resumeEvents();
      }
    }
    if (w && !z) {
      u.forEach(({
        record: H,
        toSet: I,
        wasSet: J
      }) => l.onModelChange(H, I, J));
    }
    l.acceptChanges();
    const A = {
      'added': p,
      'removed': q,
      'updated': u,
      'thresholdSurpassed': z
    };
    if (l.isFiltered && !l.remoteFilter) {
      l.filter({ 'silent': l.isRemoteDataLoading });
    }
    if (l.isGrouped) {
      l.group(null, null, false, true, l.isRemoteDataLoading);
    } else if (l.isSorted) {
      if (l.remoteSort) {
        n.replaceValues({
          'values': n.values.sort(l.createSorterFn(l.sorters)),
          'silent': true
        });
      } else {
        l.sort();
      }
    } else if (l.reorderOnSync) {
      if (!l.tree) {
        if (n.values.some((H, I) => v[H.id] !== I)) {
          n.replaceValues({
            'values': n.values.sort((H, I) => v[H.id] - v[I.id]),
            'silent': true
          });
          !z && l.afterPerformSort();
        }
      } else {
        let H = false, I = 0;
        WalkHelper.preWalk(l.rootNode, J => Array.isArray(J.children) && !H ? J.children : null, J => {
          if (J.id !== v[I++]) {
            H = true;
          }
        });
        if (H) {
          l.sort((J, K) => v[J.id] - v[K.id], void 0, void 0, true);
          l.clearSorters(true);
          !z && l.afterPerformSort();
        }
      }
    }
    if (z) {
      l.trigger('refresh', {
        'action': 'batch',
        'data': c,
        'records': n.values,
        'syncInfo': A
      });
    }
    l.isSyncingDataOnLoad = false;
    l.trigger('endApplyChangeset');
    l.trigger('loadSync', A);
  }
  syncFlatDataset(c, e) {
    if (!c) {
      return { 'toRemove': this.records };
    }
    const f = this, {
        idField: g,
        allFields: i
      } = f.modelClass, j = [], k = [], l = [], n = {}, o = {}, p = e instanceof Set;
    const {
      threshold: q,
      keepMissingValues: r
    } = f.syncDataOnLoad;
    let u = 0;
    c.forEach((v, w) => {
      let z, A;
      v = v.isModel ? v.data : v;
      const B = v[g], C = f.getById(B);
      if (!p || e.has(B)) {
        if (C) {
          if (f.shouldSyncRecord ? f.shouldSyncRecord({
              'record': C,
              'data': v,
              'store': f
            }) !== false : v !== C.data) {
            (z = f.beforeSyncRecord) == null ? void 0 : z.call(f, {
              'record': C,
              'data': v
            });
            if (r === false) {
              for (const F of i) {
                if (!(F.dataSource in v) && F.dataSource in C.data) {
                  v[F.dataSource] = F.defaultValue;
                }
              }
            }
            const E = C.set(v, null, Boolean(q));
            if (E) {
              l.push({
                'record': C,
                'wasSet': E,
                'toSet': v
              });
            }
            (A = f.afterSyncRecord) == null ? void 0 : A.call(f, {
              'record': C,
              'data': v
            });
          }
        } else {
          k.push(f.processRecord(f.createRecord(v)));
        }
      }
      if (C) {
        u++;
      }
      n[B] = 1;
      o[B] = w;
    });
    if (u < f.allRecords.filter(v => !v.isSpecialRow).length) {
      if (e) {
        for (const v of e) {
          if (!n[v]) {
            j.push(f.getById(v));
          }
        }
      } else {
        f.forEach(w => {
          if (!n[w.id]) {
            j.push(w);
          }
        }, f, {
          'includeFilteredOutRecords': true,
          'includeCollapsedGroupRecords': true
        });
      }
    }
    return {
      'toAdd': k,
      'toRemove': j,
      'toMove': [],
      'updated': l,
      'ids': o
    };
  }
  syncTreeDataset(c) {
    let e;
    if (!c) {
      return { 'toRemove': this.records };
    }
    const f = this, {
        idField: g,
        parentIdField: i,
        childrenField: j,
        allFields: k
      } = f.modelClass, {
        keepMissingValues: l,
        threshold: n
      } = (e = f.syncDataOnLoad) != null ? e : {}, o = [], p = [], q = [], r = [], u = new Set(), v = {};
    let w = 0;
    if (f.transformFlatData) {
      c = f.treeifyFlatData(c);
    }
    WalkHelper.preWalkWithParent({
      'isRoot': true,
      'id': f.rootNode.id,
      'children': c
    }, z => z.children, (z, A) => {
      if (z) {
        const {
          id: B,
          node: C
        } = f.resolveSyncNode(A);
        if (C) {
          if (f.shouldSyncRecord ? f.shouldSyncRecord({
              'record': C,
              'data': A,
              'store': f
            }) !== false : A !== C.data) {
            let E;
            const F = C.children;
            if (F !== true && A[j] === true) {
              C.clearChildren();
              C.data[j] = C.children = true;
              delete A[j];
              f.toggleCollapse(C, true);
              E = true;
            }
            if (C.parent.id !== z[g]) {
              q.push({
                'node': C,
                'parent': z,
                'index': z[j].indexOf(A)
              });
            }
            if (l === false) {
              for (const H of k) {
                if (H.name !== 'parentId' && !(H.dataSource in A) && H.dataSource in C.data) {
                  A[H.dataSource] = H.defaultValue;
                }
              }
            }
            if (typeof A.expanded === 'boolean') {
              this.toggleCollapse(C, !A.expanded);
            }
            const G = C.set(A, null, Boolean(n));
            if (G) {
              r.push({
                'record': C,
                'wasSet': G,
                'toSet': A
              });
            } else if (E) {
              C.signalNodeChanged({
                [j]: {
                  'value': true,
                  'oldValue': F
                }
              });
            }
          }
        } else {
          A[i] = z[g];
          p.push({
            ...A,
            ...Array.isArray(A[j]) ? { 'children': [] } : void 0
          });
        }
        u.add(C);
        v[B] = w++;
      }
    });
    if (u.count !== c.length) {
      f.traverse(z => {
        if (!u.has(z)) {
          o.push(z);
        }
      });
    }
    return {
      'toAdd': p,
      'toRemove': o,
      'toMove': q,
      'updated': r,
      'ids': v
    };
  }
  resolveSyncNode(c) {
    const e = c[this.modelClass.idField], f = this.getById(e);
    return {
      'id': e,
      'node': f
    };
  }
};
const RevisionException = class extends Error {
  constructor(...a) {
    super(...a);
    this.name = 'RevisionException';
  }
};
RevisionException._$name = 'RevisionException';
const throwAbstractMethodCall = () => {
  throw new Error('Abstract method call!');
};
const throwInvalidMethodCall = () => {
  throw new Error('Method cannot be called at this state!');
};
const StateBase = class extends Base {
  canUndo(a) {
    throwAbstractMethodCall();
  }
  canRedo(a) {
    throwAbstractMethodCall();
  }
  canCheckoutTo(a) {
    throwAbstractMethodCall();
  }
  onUndo(a) {
    throwAbstractMethodCall();
  }
  onRedo(a) {
    throwAbstractMethodCall();
  }
  onCheckoutTo(a) {
    throwAbstractMethodCall();
  }
  onCheckoutToHead(a) {
    throwAbstractMethodCall();
  }
  onStartRevision(a, b) {
    throwAbstractMethodCall();
  }
  onStopRevision(a) {
    throwAbstractMethodCall();
  }
  onStartTransaction(a) {
    throwAbstractMethodCall();
  }
  onStopTransaction(a) {
    throwAbstractMethodCall();
  }
  onStopTransactionDelayed(a) {
    throwAbstractMethodCall();
  }
  onRejectTransaction(a) {
    throwAbstractMethodCall();
  }
  onEnable(a) {
    throwAbstractMethodCall();
  }
  onDisable(a) {
    throwAbstractMethodCall();
  }
  onAutoRecordOn(a) {
    throwAbstractMethodCall();
  }
  onAutoRecordOff(a) {
    throwAbstractMethodCall();
  }
  onResetQueue(a) {
    throwAbstractMethodCall();
  }
  onModelUpdate(a) {
    throwAbstractMethodCall();
  }
  onStoreModelAdd(a) {
    throwAbstractMethodCall();
  }
  onStoreModelInsert(a) {
    throwAbstractMethodCall();
  }
  onStoreModelRemove(a) {
    throwAbstractMethodCall();
  }
  onStoreModelRemoveAll(a) {
    throwAbstractMethodCall();
  }
  onModelInsertChild(a) {
    throwAbstractMethodCall();
  }
  onModelRemoveChild(a) {
    throwAbstractMethodCall();
  }
};
StateBase._$name = 'StateBase';
const STATE_PROP = Symbol('STATE_PROP');
const STORES_PROP = Symbol('STORES_PROP');
const QUEUE_PROP = Symbol('QUEUE_PROP');
const POS_PROP = Symbol('POS_PROP');
const ACTION_QUEUE_PROP = Symbol('ACTION_QUEUE_PROP');
const REVISION_NAMES_PROP = Symbol('REVISION_NAMES_PROP');
const REVISION_QUEUE_PROP = Symbol('REVISION_QUEUE_PROP');
const REVISION_INDEX_PROP = Symbol('REVISION_INDEX_PROP');
const CURRENT_REV_ID_PROP = Symbol('CURRENT_REV_ID_PROP');
const TRANSACTION_PROP = Symbol('TRANSACTION_PROP');
const TRANSACTION_TIMER_PROP = Symbol('TRANSACTION_TIMER_PROP');
const AUTO_RECORD_PROP = Symbol('AUTO_RECORD_PROP');
const IS_APPLYING_STASH = Symbol('IS_APPLYING_STASH');
const PROPS = Object.freeze([
  STATE_PROP,
  STORES_PROP,
  QUEUE_PROP,
  POS_PROP,
  ACTION_QUEUE_PROP,
  REVISION_NAMES_PROP,
  REVISION_QUEUE_PROP,
  REVISION_INDEX_PROP,
  CURRENT_REV_ID_PROP,
  TRANSACTION_PROP,
  TRANSACTION_TIMER_PROP,
  AUTO_RECORD_PROP,
  IS_APPLYING_STASH
]);
const registry2 = new Map();
const registerStmState = (a, b) => {
  registry2.set(a, b);
};
const resolveStmState = a => {
  if (typeof a === 'string') {
    a = registry2.get(a);
  }
  return a;
};
const Registry_default = {
  'registerStmState': registerStmState,
  'resolveStmState': resolveStmState
};
const resetQueue = (a, b) => {
  const {
    undo: c,
    redo: e,
    revision: f
  } = b;
  let g;
  if (c && !e) {
    g = {
      [QUEUE_PROP]: a[QUEUE_PROP].slice(a.position),
      [POS_PROP]: 0
    };
  } else if (e && !c) {
    g = { [QUEUE_PROP]: a[QUEUE_PROP].slice(0, a.position) };
  } else {
    g = {
      [QUEUE_PROP]: [],
      [POS_PROP]: 0
    };
  }
  if (f) {
    g = Object.assign(g != null ? g : {}, {
      [REVISION_NAMES_PROP]: [],
      [REVISION_INDEX_PROP]: -1,
      [REVISION_QUEUE_PROP]: []
    });
  }
  ;
  return [
    g,
    () => {
      a.notifyStoresAboutQueueReset(b);
    }
  ];
};
const DisabledStateClass = class extends StateBase {
  canUndo() {
    return false;
  }
  canRedo() {
    return false;
  }
  canCheckoutTo() {
    return false;
  }
  onUndo() {
    throwInvalidMethodCall();
  }
  onRedo() {
    throwInvalidMethodCall();
  }
  onEnable(a) {
    return a.autoRecord ? 'autoreadystate' : 'readystate';
  }
  onDisable() {
    throwInvalidMethodCall();
  }
  onAutoRecordOn() {
    return { [AUTO_RECORD_PROP]: true };
  }
  onAutoRecordOff() {
    return { [AUTO_RECORD_PROP]: false };
  }
  onStartTransaction() {
    throwInvalidMethodCall();
  }
  onStopTransaction() {
    throwInvalidMethodCall();
  }
  onStopTransactionDelayed() {
    throwInvalidMethodCall();
  }
  onRejectTransaction() {
    throwInvalidMethodCall();
  }
  onResetQueue(a, b) {
    return resetQueue(a, b);
  }
  onModelUpdate() {
  }
  onModelInsertChild() {
  }
  onModelRemoveChild() {
  }
  onStoreModelAdd() {
  }
  onStoreModelInsert() {
  }
  onStoreModelRemove() {
  }
  onStoreRemoveAll() {
  }
};
const DisabledState = new DisabledStateClass();
const DisabledState_default = DisabledState;
Registry_default.registerStmState('disabledstate', DisabledState);
const ActionBase = class extends Base {
  static get defaultConfig() {
    return { 'inversed': false };
  }
  get type() {
    return this.constructor.name;
  }
  undo() {
    throwAbstractMethodCall();
  }
  redo() {
    throwAbstractMethodCall();
  }
  invert() {
    const a = new this.constructor({
        ...this.initialConfig,
        'isUserInput': this.isUserInput,
        'inversed': true
      }), {
        undo: b,
        redo: c
      } = a;
    a.undo = c;
    a.redo = b;
    return a;
  }
};
const ACTION_TYPES = {
  'ADD': 'AddAction',
  'INSERT': 'InsertAction',
  'INSERT_CHILD': 'InsertChildAction',
  'REMOVE': 'RemoveAction',
  'REMOVE_ALL': 'RemoveAllAction',
  'REMOVE_CHILD': 'RemoveChildAction',
  'UPDATE': 'UpdateAction',
  'EVENT_UPDATE': 'EventUpdateAction'
};
ActionBase._$name = 'ActionBase';
const MODEL_PROP = Symbol('MODEL_PROP');
const NEW_DATA_PROP = Symbol('NEW_DATA_PROP');
const OLD_DATA_PROP = Symbol('OLD_DATA_PROP');
const UpdateAction = class extends ActionBase {
  static $name = 'UpdateAction';
  static get defaultConfig() {
    return {
      'model': void 0,
      'newData': void 0,
      'oldData': void 0,
      'isInitialUserAction': false
    };
  }
  get type() {
    return ACTION_TYPES.UPDATE;
  }
  get model() {
    return this[MODEL_PROP];
  }
  set model(a) {
    this[MODEL_PROP] = a;
  }
  get newData() {
    return this[NEW_DATA_PROP];
  }
  set newData(a) {
    this[NEW_DATA_PROP] = { ...a };
  }
  get oldData() {
    return this[OLD_DATA_PROP];
  }
  set oldData(a) {
    this[OLD_DATA_PROP] = { ...a };
  }
  undo() {
    const {
      model: a,
      oldData: b
    } = this;
    if (a.$) {
      Object.assign(a, b);
    }
    a.set(b, null, null, null, Boolean(a.$));
  }
  redo() {
    const {
      model: a,
      newData: b
    } = this;
    if (a.$) {
      Object.assign(a, b);
    }
    a.set(b, null, null, null, Boolean(a.$));
  }
};
UpdateAction._$name = 'UpdateAction';
const PARENT_MODEL_PROP = Symbol('PARENT_MODEL_PROP');
const CHILD_MODELS_PROP = Symbol('CHILD_MODELS_PROP');
const INSERT_INDEX_PROP = Symbol('INSERT_INDEX_PROP');
const ORDERED_PRP = Symbol('ORDERED_PROP');
const CONTEXT_PROP = Symbol('CONTEXT_PROP');
const InsertChildAction = class extends ActionBase {
  static $name = 'InsertChildAction';
  static get defaultConfig() {
    return {
      'parentModel': void 0,
      'childModels': void 0,
      'insertIndex': void 0,
      'orderedBeforeNode': void 0,
      'context': void 0,
      'stores': void 0
    };
  }
  get type() {
    return ACTION_TYPES.INSERT_CHILD;
  }
  get parentModel() {
    return this[PARENT_MODEL_PROP];
  }
  set parentModel(a) {
    this[PARENT_MODEL_PROP] = a;
  }
  get childModels() {
    return this[CHILD_MODELS_PROP];
  }
  set childModels(a) {
    this[CHILD_MODELS_PROP] = a.slice(0);
  }
  get insertIndex() {
    return this[INSERT_INDEX_PROP];
  }
  set insertIndex(a) {
    this[INSERT_INDEX_PROP] = a;
  }
  get orderedBeforeNode() {
    return this[ORDERED_PRP];
  }
  set orderedBeforeNode(a) {
    this[ORDERED_PRP] = a;
  }
  get context() {
    return this[CONTEXT_PROP];
  }
  set context(a) {
    this[CONTEXT_PROP] = a;
  }
  get stores() {
    return this[STORES_PROP];
  }
  set stores(a) {
    this[STORES_PROP] = a;
  }
  undo() {
    const {
        parentModel: a,
        context: b,
        childModels: c
      } = this, e = new Map(), f = new Set();
    for (const g of c) {
      const i = b.get(g);
      if (!i) {
        f.add(g);
      } else {
        let j = e.get(i.parent);
        if (!j) {
          j = {
            'moveRight': [],
            'moveLeft': [],
            'moveFromAnotherParent': []
          };
          e.set(i.parent, j);
        }
        if (i.parent === a) {
          if (i.index > g.parentIndex) {
            j.moveRight.push({
              'parent': i.parent,
              'model': g,
              'index': i.index + 1
            });
          } else {
            j.moveLeft.push({
              'parent': i.parent,
              'model': g,
              'index': i.index
            });
          }
        } else {
          j.moveFromAnotherParent.push({
            'parent': i.parent,
            'model': g,
            'index': i.index
          });
        }
      }
    }
    f.forEach(k => k.parent.removeChild(k));
    for (const k of e.values()) {
      const {
        moveRight: l,
        moveLeft: n,
        moveFromAnotherParent: o
      } = k;
      o.forEach(p => {
        p.parent.insertChild(p.model, p.index);
      });
      n.sort((p, q) => p.index - q.index);
      l.sort((p, q) => q.index - p.index);
    }
    for (const p of e.values()) {
      const {
        moveRight: q,
        moveLeft: r
      } = p;
      r.forEach(u => {
        u.parent.insertChild(u.model, u.index);
      });
      q.forEach(u => {
        u.parent.insertChild(u.model, u.index);
      });
    }
  }
  redo() {
    let a, b;
    const {
        parentModel: c,
        insertIndex: e,
        childModels: f,
        orderedBeforeNode: g
      } = this, i = (a = c.children) == null ? void 0 : a[e];
    c.insertChild(f, i, false, { 'orderedBeforeNode': g != null ? g : (b = i == null ? void 0 : i.previousSibling) == null ? void 0 : b.nextOrderedSibling });
  }
};
InsertChildAction._$name = 'InsertChildAction';
const PARENT_MODEL_PROP2 = Symbol('PARENT_MODEL_PROP');
const CHILD_MODELS_PROP2 = Symbol('CHILD_MODELS_PROP');
const CONTEXT_PROP2 = Symbol('CONTEXT_PROP');
const RemoveChildAction = class extends ActionBase {
  static $name = 'RemoveChildAction';
  static get defaultConfig() {
    return {
      'parentModel': void 0,
      'childModels': void 0,
      'context': void 0,
      'stores': void 0
    };
  }
  get type() {
    return ACTION_TYPES.REMOVE_CHILD;
  }
  get parentModel() {
    return this[PARENT_MODEL_PROP2];
  }
  set parentModel(a) {
    this[PARENT_MODEL_PROP2] = a;
  }
  get childModels() {
    return this[CHILD_MODELS_PROP2];
  }
  set childModels(a) {
    this[CHILD_MODELS_PROP2] = a.slice(0);
  }
  get context() {
    return this[CONTEXT_PROP2];
  }
  set context(a) {
    this[CONTEXT_PROP2] = a;
  }
  get stores() {
    return this[STORES_PROP];
  }
  set stores(a) {
    this[STORES_PROP] = a;
  }
  undo() {
    const {
      parentModel: a,
      context: b,
      childModels: c
    } = this;
    c.sort((e, f) => {
      const g = b.get(e).parentIndex, i = b.get(f).parentIndex;
      return g - i;
    });
    c.forEach(e => {
      const f = b.get(e);
      a.insertChild(e, f.parentIndex, void 0, { 'orderedParentIndex': f.orderedParentIndex });
    });
  }
  redo() {
    this.parentModel.removeChild(this.childModels);
  }
};
RemoveChildAction._$name = 'RemoveChildAction';
const STORE_PROP = Symbol('STORE_PROP');
const MODEL_LIST_PROP = Symbol('MODEL_LIST_PROP');
const AddAction = class extends ActionBase {
  static $name = 'AddAction';
  static get defaultConfig() {
    return {
      'store': void 0,
      'modelList': void 0,
      'silent': false
    };
  }
  get type() {
    return ACTION_TYPES.ADD;
  }
  get store() {
    return this[STORE_PROP];
  }
  set store(a) {
    this[STORE_PROP] = a;
  }
  get modelList() {
    return this[MODEL_LIST_PROP];
  }
  set modelList(a) {
    this[MODEL_LIST_PROP] = a.slice(0);
  }
  undo() {
    this.store.remove(this.modelList, this.silent);
  }
  redo() {
    this.store.add(this.modelList, this.silent);
  }
};
AddAction._$name = 'AddAction';
const STORE_PROP2 = Symbol('STORE_PROP');
const MODEL_LIST_PROP2 = Symbol('MODEL_LIST_PROP');
const INSERT_INDEX_PROP2 = Symbol('INSERT_INDEX_PROP');
const CONTEXT_PROP3 = Symbol('CONTEXT_PROP');
const InsertAction = class extends ActionBase {
  static $name = 'InsertAction';
  static get defaultConfig() {
    return {
      'store': void 0,
      'modelList': void 0,
      'insertIndex': void 0,
      'context': void 0,
      'silent': false,
      'reportUndoneRemoval': true
    };
  }
  get type() {
    return ACTION_TYPES.INSERT;
  }
  get store() {
    return this[STORE_PROP2];
  }
  set store(a) {
    this[STORE_PROP2] = a;
  }
  get modelList() {
    return this[MODEL_LIST_PROP2];
  }
  set modelList(a) {
    this[MODEL_LIST_PROP2] = a.slice(0);
  }
  get insertIndex() {
    return this[INSERT_INDEX_PROP2];
  }
  set insertIndex(a) {
    this[INSERT_INDEX_PROP2] = a;
  }
  get context() {
    return this[CONTEXT_PROP3];
  }
  set context(a) {
    this[CONTEXT_PROP3] = a;
  }
  undo() {
    const {
      store: a,
      modelList: b,
      context: c,
      silent: e,
      reportUndoneRemoval: f
    } = this;
    b.sort((g, i) => {
      const j = c.get(g), k = c.get(i);
      return j !== void 0 && k !== void 0 ? j - k : 0;
    });
    b.forEach(g => {
      const i = c.get(g);
      g._undoingInsertion = f;
      if (i !== void 0) {
        a.insert(i, g, e);
      } else {
        a.remove(g, e);
      }
      g._undoingInsertion = false;
    });
  }
  redo() {
    const a = this;
    a.store.insert(a.insertIndex, a.modelList, a.silent);
  }
};
InsertAction._$name = 'InsertAction';
const STORE_PROP3 = Symbol('STORE_PROP');
const MODEL_LIST_PROP3 = Symbol('MODEL_LIST_PROP');
const CONTEXT_PROP4 = Symbol('CONTEXT_PROP');
const RemoveAction = class extends ActionBase {
  static $name = 'RemoveAction';
  static get defaultConfig() {
    return {
      'store': void 0,
      'modelList': void 0,
      'context': void 0,
      'silent': false
    };
  }
  get type() {
    return ACTION_TYPES.REMOVE;
  }
  get store() {
    return this[STORE_PROP3];
  }
  set store(a) {
    this[STORE_PROP3] = a;
  }
  get modelList() {
    return this[MODEL_LIST_PROP3];
  }
  set modelList(a) {
    this[MODEL_LIST_PROP3] = a.slice(0);
  }
  get context() {
    return this[CONTEXT_PROP4];
  }
  set context(a) {
    this[CONTEXT_PROP4] = a;
  }
  undo() {
    const {
      store: a,
      context: b,
      modelList: c,
      silent: e
    } = this;
    c.sort((f, g) => {
      const i = b.get(f), j = b.get(g);
      return i - j;
    });
    c.forEach(f => {
      const g = b.get(f);
      a.insert(g, f, e);
    });
  }
  redo() {
    this.store.remove(this.modelList, this.silent);
  }
};
RemoveAction._$name = 'RemoveAction';
const STORE_PROP4 = Symbol('STORE_PROP');
const ALL_RECORDS_PROP = Symbol('ALL_RECORDS_PROP');
const RemoveAllAction = class extends ActionBase {
  static $name = 'RemoveAllAction';
  static get defaultConfig() {
    return {
      'store': void 0,
      'allRecords': void 0,
      'silent': false
    };
  }
  get type() {
    return ACTION_TYPES.REMOVE_ALL;
  }
  get store() {
    return this[STORE_PROP4];
  }
  set store(a) {
    this[STORE_PROP4] = a;
  }
  get allRecords() {
    return this[ALL_RECORDS_PROP];
  }
  set allRecords(a) {
    this[ALL_RECORDS_PROP] = a.slice(0);
  }
  undo() {
    const {
      store: a,
      allRecords: b,
      silent: c
    } = this;
    a.add(b, c);
  }
  redo() {
    this.store.removeAll(this.silent);
  }
};
RemoveAllAction._$name = 'RemoveAllAction';
const makeModelUpdateAction = (a, b, c, e) => {
  return new UpdateAction({
    'model': a,
    'newData': b,
    'oldData': c,
    'isInitialUserAction': e
  });
};
const makeModelInsertChildAction = (a, b, c, e, f, g) => {
  return new InsertChildAction({
    'parentModel': a,
    'childModels': c,
    'insertIndex': b,
    'orderedBeforeNode': f,
    'context': e,
    'stores': g
  });
};
const makeModelRemoveChildAction = (a, b, c, e) => {
  return new RemoveChildAction({
    'parentModel': a,
    'childModels': b,
    'context': c,
    'stores': e
  });
};
const makeStoreModelAddAction = (a, b, c) => {
  return new AddAction({
    'store': a,
    'modelList': b,
    'silent': c
  });
};
const makeStoreModelInsertAction = (a, b, c, e, f) => {
  return new InsertAction({
    'store': a,
    'insertIndex': b,
    'modelList': c,
    'context': e,
    'silent': f
  });
};
const makeStoreModelRemoveAction = (a, b, c, e) => {
  return new RemoveAction({
    'store': a,
    'modelList': b,
    'context': c,
    'silent': e
  });
};
const makeStoreRemoveAllAction = (a, b, c) => {
  return new RemoveAllAction({
    'store': a,
    'allRecords': b,
    'silent': c
  });
};
const invertAction = a => {
  return a.invert();
};
const TRANSACTION_TYPES = {
  'DATA_CORRECTION': 'DATA_CORRECTION',
  'TEMPORARY': 'TEMPORARY',
  'CONFLICT_RESOLUTION': 'CONFLICT_RESOLUTION'
};
const Transaction = class extends Base {
  static get configurable() {
    return {
      'title': null,
      'committed': false,
      'inputGeneration': 0
    };
  }
  static from(a, b = {}) {
    const c = new this({
      ...this.initialConfig,
      ...b,
      'inputGeneration': a.reduce((e, f) => {
        let g;
        return Math.max(e, (g = f.inputGeneration) != null ? g : 0);
      }, 0),
      'committed': false
    });
    c[ACTION_QUEUE_PROP] = a.flatMap(e => e[ACTION_QUEUE_PROP].filter(f => {
      if (e.inputGeneration > 0) {
        if (b.conflictResolutionFor) {
          return f.isUserInput === e.inputGeneration;
        } else {
          return f.isUserInput;
        }
      } else {
        return f;
      }
    }));
    return c;
  }
  static createTransactionWithOriginalInput(a) {
    const b = new this({
      ...this.initialConfig,
      'title': a.title,
      'committed': false
    });
    b[ACTION_QUEUE_PROP] = a[ACTION_QUEUE_PROP].filter(c => c.isUserInput ? c.isUserInput === 1 : !c.isUserInput);
    return b;
  }
  construct(...a) {
    this[ACTION_QUEUE_PROP] = [];
    super.construct(...a);
  }
  get queue() {
    return this[ACTION_QUEUE_PROP].slice(0);
  }
  get length() {
    return this[ACTION_QUEUE_PROP].length;
  }
  get filterUserInput() {
    return this.inputGeneration > 0;
  }
  addAction(a) {
    this[ACTION_QUEUE_PROP].push(a);
  }
  undo() {
    const a = this[ACTION_QUEUE_PROP];
    for (let b = a.length - 1; b >= 0; --b) {
      a[b].undo();
    }
  }
  redo() {
    const a = this[ACTION_QUEUE_PROP];
    for (let b = 0, c = a.length; b < c; ++b) {
      a[b].redo();
    }
  }
  mergeUpdateModelActions() {
    let a, b;
    const c = this[ACTION_QUEUE_PROP], e = new Map(), f = [];
    function g(i, j = false) {
      return i.id + '-' + j;
    }
    for (const i of c) {
      if (i.isUpdateAction && ((a = i.model) == null ? void 0 : a.isModel)) {
        const j = e.get(g(i.model, i.isUserInput));
        if (!j) {
          e.set(g(i.model, i.isUserInput), i);
          f.push(i);
        } else {
          for (const k in i.oldData) {
            if (i.newData.hasOwnProperty(k)) {
              if (!j.oldData.hasOwnProperty(k)) {
                j.oldData[k] = i.oldData[k];
              }
              j.newData[k] = i.newData[k];
            }
          }
        }
      } else {
        f.push(i);
        if (i.isRemoveAction && ((b = i.modelList) == null ? void 0 : b.length)) {
          for (const l of i.modelList) {
            e.delete(g(l));
            e.delete(g(l, true));
          }
        }
      }
    }
    this[ACTION_QUEUE_PROP] = f;
    this.inputGeneration = Math.max(...f.map(n => {
      let o;
      return (o = n.isUserInput) != null ? o : 0;
    }), 0);
  }
  mergeAddUpdateModelActions() {
    const a = this[ACTION_QUEUE_PROP], b = new Set(), c = [];
    for (const e of a) {
      if (e.isAddAction || e.isInsertAction) {
        e.modelList.forEach(f => b.add(f));
      } else if (e.isInsertChildAction) {
        e.childModels.forEach(f => b.add(f));
      }
      if (e.isUpdateAction || e.isEventUpdateAction) {
        if (!b.has(e.model)) {
          c.push(e);
        }
      } else {
        c.push(e);
      }
    }
    this[ACTION_QUEUE_PROP] = c;
    this.inputGeneration = Math.max(...c.map(f => {
      let g;
      return (g = f.isUserInput) != null ? g : 0;
    }), 0);
  }
  invert() {
    const a = new this.constructor({
      ...this.initialConfig,
      'title': this.title
    });
    a[ACTION_QUEUE_PROP] = this[ACTION_QUEUE_PROP].map(b => invertAction(b));
    return a;
  }
  groupUserInput() {
    return this[ACTION_QUEUE_PROP].reduce((a, b) => {
      let c;
      const e = (c = b.isUserInput) != null ? c : 'default';
      if (!a.has(e)) {
        a.set(e, []);
      }
      a.get(e).push(b);
      return a;
    }, new Map());
  }
  getUserInput(a) {
    const b = new Map(), c = {}, e = this.groupUserInput().get(a != null ? a : this.inputGeneration || 'default'), f = (g, i) => {
        if (!(i in c)) {
          c[i] = { [g]: [] };
        } else if (!(g in c[i])) {
          c[i][g] = [];
        }
        return c[i][g];
      };
    for (const g of e) {
      switch (g.type) {
      case ACTION_TYPES.ADD:
      case ACTION_TYPES.INSERT: {
          if (g.inversed) {
            const i = f('removed', g.store.id);
            i.push(...g.modelList);
          } else {
            const j = f('added', g.store.id);
            j.push(...g.modelList);
          }
          break;
        }
      case ACTION_TYPES.REMOVE:
      case ACTION_TYPES.REMOVE_ALL: {
          if (g.inversed) {
            const k = f('added', g.store.id);
            k.push(...g.modelList || g.allRecords);
          } else {
            const l = f('removed', g.store.id);
            l.push(...g.modelList || g.allRecords);
          }
          break;
        }
      case ACTION_TYPES.INSERT_CHILD: {
          for (const n of g.stores) {
            g.childModels.forEach(o => {
              if (n.added.includes(o)) {
                f('added', n.id).push(o);
              } else if (n.removed.includes(o)) {
                f('removed', n.id).push(o);
              } else {
                const p = o.hierarchyModificationDataToWrite;
                if (p) {
                  if (b.has(o)) {
                    b.set(o, {
                      ...b.get(o),
                      ...p
                    });
                  } else {
                    b.set(o, p);
                  }
                }
              }
            });
          }
          break;
        }
      case ACTION_TYPES.REMOVE_CHILD: {
          for (const o of g.stores) {
            if (g.inversed) {
              const p = f('added', o.id);
              p.push(...g.childModels);
            } else {
              const q = f('removed', o.id);
              q.push(...g.childModels);
            }
          }
          break;
        }
      case ACTION_TYPES.UPDATE:
      case ACTION_TYPES.EVENT_UPDATE: {
          if (b.has(g.model)) {
            b.set(g.model, {
              ...b.get(g.model),
              ...g.newData
            });
          } else {
            b.set(g.model, { ...g.newData });
          }
          break;
        }
      default:
        break;
      }
    }
    if (b.size > 0) {
      c.updated = b;
    }
    if (Object.keys(c).length > 0) {
      return c;
    }
  }
  markCurrentTransactionContentUserInput() {
    const a = this.inputGeneration + 1, b = this[ACTION_QUEUE_PROP];
    let c = false;
    b.forEach(e => {
      if (e.isUserInput === void 0 && !e.isCalculated) {
        e.isUserInput = a;
        c = true;
      }
    });
    if (c) {
      this.inputGeneration = a;
    }
  }
  markCurrentTransactionContentCalculated() {
    const a = this.inputGeneration, b = this[ACTION_QUEUE_PROP];
    b.forEach(c => {
      if (c.isCalculated === void 0 && !c.isUserInput) {
        c.isCalculated = a;
      }
    });
  }
  normalizeUserInputGeneration() {
    this[ACTION_QUEUE_PROP].forEach(a => a.isUserInput = this.inputGeneration);
  }
};
Transaction._$name = 'Transaction';
const ReadyStateClass = class extends StateBase {
  canUndo(a) {
    return 0 < a.position && a.position <= a.length;
  }
  canRedo(a) {
    return 0 <= a.position && a.position < a.length;
  }
  canCheckoutTo(a, b) {
    if (!a.revisionsEnabled) {
      console.warn('Revisions are disabled');
      return false;
    }
    if (!a[REVISION_NAMES_PROP].includes(b)) {
      console.warn('Revision is not found');
      return false;
    }
    return true;
  }
  onUndo(a, b) {
    let c = a.position;
    const e = a[QUEUE_PROP], f = Math.max(0, c - b), g = () => {
        a.notifyStoresAboutStateRestoringStart();
        const i = [];
        while (c !== f) {
          const j = e[--c];
          j.undo();
          i.push(j);
        }
        return [
          a.autoRecord ? 'autoreadystate' : 'readystate',
          () => {
            a.notifyStoresAboutStateRestoringStop({
              'cause': 'undo',
              'transactions': i
            });
          }
        ];
      };
    return [
      {
        [STATE_PROP]: 'restoringstate',
        [POS_PROP]: f
      },
      g
    ];
  }
  onRedo(a, b) {
    let c = a.position;
    const e = a[QUEUE_PROP], f = Math.min(e.length, c + b);
    const g = () => {
      a.notifyStoresAboutStateRestoringStart();
      const i = [];
      do {
        const j = e[c++];
        j.redo();
        i.push(j);
      } while (c !== f);
      return [
        a.autoRecord ? 'autoreadystate' : 'readystate',
        () => {
          a.notifyStoresAboutStateRestoringStop({
            'cause': 'redo',
            'transactions': i
          });
        }
      ];
    };
    return [
      {
        [STATE_PROP]: 'restoringstate',
        [POS_PROP]: f
      },
      g
    ];
  }
  onCheckoutTo(a, b) {
    const c = a[REVISION_NAMES_PROP].indexOf(b), e = Math.min(c, a[REVISION_INDEX_PROP]), f = Math.max(c, a[REVISION_INDEX_PROP]), g = c < a[REVISION_INDEX_PROP];
    let i = a[REVISION_QUEUE_PROP].slice(e, f);
    if (g) {
      i = i.filter(k => !k.conflictResolutionFor || k.type === TRANSACTION_TYPES.TEMPORARY).reverse();
      const j = a[REVISION_QUEUE_PROP].filter((k, l) => l >= e && k.dummy);
      j.forEach(k => {
        a[REVISION_QUEUE_PROP].splice(a[REVISION_QUEUE_PROP].indexOf(k), 1);
        a[REVISION_NAMES_PROP].splice(a[REVISION_NAMES_PROP].indexOf(k.title), 1);
      });
    }
    return [
      {
        [STATE_PROP]: 'checkoutstate',
        [REVISION_INDEX_PROP]: c
      },
      () => {
        a.notifyStoresAboutCheckoutStart();
        for (const k of i) {
          k[g ? 'undo' : 'redo']();
        }
      }
    ];
  }
  onCheckoutToHead(a) {
    throwInvalidMethodCall();
  }
  onEnable() {
    throwInvalidMethodCall();
  }
  onDisable() {
    return 'disabledstate';
  }
  onAutoRecordOn() {
    return {
      [STATE_PROP]: 'autoreadystate',
      [AUTO_RECORD_PROP]: true
    };
  }
  onAutoRecordOff() {
    throwInvalidMethodCall();
  }
  onStartTransaction(a, b) {
    const c = new Transaction({ 'title': b });
    return [
      {
        [STATE_PROP]: 'recordingstate',
        [TRANSACTION_PROP]: c
      },
      () => {
        a.notifyStoresAboutStateRecordingStart(c);
      }
    ];
  }
  onStopTransaction() {
    throwInvalidMethodCall();
  }
  onStopTransactionDelayed() {
    throwInvalidMethodCall();
  }
  onRejectTransaction() {
    throwInvalidMethodCall();
  }
  onResetQueue(a, b) {
    return resetQueue(a, b);
  }
  onModelUpdate() {
  }
  onModelInsertChild() {
  }
  onModelRemoveChild() {
  }
  onStoreModelAdd() {
  }
  onStoreModelInsert() {
  }
  onStoreModelRemove() {
  }
  onStoreRemoveAll() {
  }
};
const ReadyState = new ReadyStateClass();
const ReadyState_default = ReadyState;
Registry_default.registerStmState('readystate', ReadyState);
const RecordingStateClass = class extends StateBase {
  canUndo() {
    return false;
  }
  canRedo() {
    return false;
  }
  canCheckoutTo() {
    return false;
  }
  onEnable() {
  }
  onDisable(a) {
    const b = a[TRANSACTION_PROP];
    a.notifyStoresAboutStateRecordingStop(b, { 'disabled': true });
    return {
      [STATE_PROP]: 'disabledstate',
      [TRANSACTION_PROP]: null
    };
  }
  onAutoRecordOn(a) {
    return [
      {
        [STATE_PROP]: 'autorecordingstate',
        [AUTO_RECORD_PROP]: true
      },
      () => {
        a.stopTransactionDelayed();
      }
    ];
  }
  onAutoRecordOff() {
    throwInvalidMethodCall();
  }
  onStartTransaction() {
    throwInvalidMethodCall();
  }
  onStopTransaction(a, b) {
    const c = a[TRANSACTION_PROP], e = a[QUEUE_PROP];
    let f = a[POS_PROP];
    if (c.length) {
      if (!c.title && !b && a.getTransactionTitle) {
        c.title = a.getTransactionTitle(c);
      } else if (b) {
        c.title = b;
      }
      e[f] = c;
      e.length = ++f;
    }
    return [
      {
        [STATE_PROP]: 'readystate',
        [POS_PROP]: f,
        [TRANSACTION_PROP]: null
      },
      () => {
        a.notifyStoresAboutStateRecordingStop(c, { 'stop': true });
      }
    ];
  }
  onRejectTransaction(a) {
    const b = a[TRANSACTION_PROP];
    return [
      {
        [STATE_PROP]: 'restoringstate',
        [TRANSACTION_PROP]: null
      },
      () => {
        if (b.length) {
          b.undo();
        }
        return [
          'readystate',
          () => {
            a.notifyStoresAboutStateRecordingStop(b, { 'rejected': true });
          }
        ];
      }
    ];
  }
  onStopTransactionDelayed() {
    throwInvalidMethodCall();
  }
  onResetQueue(a, b) {
    return resetQueue(a, b);
  }
  onModelUpdate(a, b, c, e, f) {
    const g = a[TRANSACTION_PROP];
    g.addAction(a.makeModelUpdateAction(b, c, e, f));
  }
  onModelInsertChild(a, b, c, e, f, g) {
    const i = a[TRANSACTION_PROP], j = b.stores.filter(k => a.stores.includes(k));
    i.addAction(a.makeModelInsertChildAction(b, c, e, f, g, j));
  }
  onModelRemoveChild(a, b, c, e) {
    const f = a[TRANSACTION_PROP], g = b.stores.filter(i => a.stores.includes(i));
    f.addAction(a.makeModelRemoveChildAction(b, c, e, g));
  }
  onStoreModelAdd(a, b, c, e) {
    const f = a[TRANSACTION_PROP];
    f.addAction(a.makeStoreModelAddAction(b, c, e));
  }
  onStoreModelInsert(a, b, c, e, f, g) {
    const i = a[TRANSACTION_PROP], j = a.makeStoreModelInsertAction(b, c, e, f, g);
    j.reportUndoneRemoval = !a.revisionsEnabled;
    i.addAction(j);
  }
  onStoreModelRemove(a, b, c, e, f) {
    const g = a[TRANSACTION_PROP];
    g.addAction(a.makeStoreModelRemoveAction(b, c, e, f));
  }
  onStoreRemoveAll(a, b, c, e) {
    const f = a[TRANSACTION_PROP];
    f.addAction(a.makeStoreRemoveAllAction(b, c, e));
  }
};
const RecordingState = new RecordingStateClass();
const RecordingState_default = RecordingState;
Registry_default.registerStmState('recordingstate', RecordingState);
const RestoringStateClass = class extends StateBase {
  static get $name() {
    return 'RestoringStateClass';
  }
  canUndo() {
    return false;
  }
  canRedo() {
    return false;
  }
  canCheckoutTo() {
    return false;
  }
  onUndo() {
    throwInvalidMethodCall();
  }
  onRedo() {
    throwInvalidMethodCall();
  }
  onEnable() {
    throwInvalidMethodCall();
  }
  onDisable() {
    throwInvalidMethodCall();
  }
  onAutoRecordOn() {
    return { [AUTO_RECORD_PROP]: true };
  }
  onAutoRecordOff() {
    return { [AUTO_RECORD_PROP]: false };
  }
  onStartTransaction() {
    throwInvalidMethodCall();
  }
  onStopTransaction() {
    throwInvalidMethodCall();
  }
  onStopTransactionDelayed() {
    throwInvalidMethodCall();
  }
  onRejectTransaction() {
    throwInvalidMethodCall();
  }
  onQueueReset() {
    throwInvalidMethodCall();
  }
  onModelUpdate() {
  }
  onModelInsertChild() {
  }
  onModelRemoveChild() {
  }
  onStoreModelAdd() {
  }
  onStoreModelInsert() {
  }
  onStoreModelRemove() {
  }
  onStoreRemoveAll() {
  }
};
const RestoringState = new RestoringStateClass();
const RestoringState_default = RestoringState;
Registry_default.registerStmState('restoringstate', RestoringState);
const AutoReadyStateClass = class extends ReadyStateClass {
  onAutoRecordOn() {
    throwInvalidMethodCall();
  }
  onAutoRecordOff() {
    return {
      [STATE_PROP]: 'readystate',
      [AUTO_RECORD_PROP]: false
    };
  }
  onStartTransaction(a, b) {
    const c = new Transaction({ 'title': b });
    return [
      {
        [STATE_PROP]: 'autorecordingstate',
        [TRANSACTION_PROP]: c
      },
      () => {
        a.notifyStoresAboutStateRecordingStart(c);
        a.stopTransactionDelayed();
      }
    ];
  }
  onModelUpdate(a, b, c, e) {
    a.startTransaction();
    a.onModelUpdate(b, c, e);
  }
  onModelInsertChild(a, b, c, e, f) {
    a.startTransaction();
    a.onModelInsertChild(b, c, e, f);
  }
  onModelRemoveChild(a, b, c, e) {
    a.startTransaction();
    a.onModelRemoveChild(b, c, e);
  }
  onStoreModelAdd(a, b, c, e) {
    a.startTransaction();
    a.onStoreModelAdd(b, c, e);
  }
  onStoreModelInsert(a, b, c, e, f, g) {
    a.startTransaction();
    a.onStoreModelInsert(b, c, e, f, g);
  }
  onStoreModelRemove(a, b, c, e, f) {
    a.startTransaction();
    a.onStoreModelRemove(b, c, e, f);
  }
  onStoreRemoveAll(a, b, c, e) {
    a.startTransaction();
    a.onStoreRemoveAll(b, c, e);
  }
};
const AutoReadyState = new AutoReadyStateClass();
const AutoReadyState_default = AutoReadyState;
Registry_default.registerStmState('autoreadystate', AutoReadyState);
const AutoRecordingStateClass = class extends RecordingStateClass.mixin(Delayable_default) {
  onDisable(a) {
    const b = a[TRANSACTION_PROP], c = a[TRANSACTION_TIMER_PROP];
    if (c) {
      this.clearTimeout(c);
    }
    a.notifyStoresAboutStateRecordingStop(b, { 'disabled': true });
    return {
      [STATE_PROP]: 'disabledstate',
      [TRANSACTION_PROP]: null,
      [TRANSACTION_TIMER_PROP]: null
    };
  }
  onAutoRecordOn(a) {
    throwInvalidMethodCall();
  }
  onAutoRecordOff(a) {
    const b = a[TRANSACTION_TIMER_PROP];
    if (b) {
      this.clearTimeout(b);
    }
    return {
      [STATE_PROP]: 'recordingstate',
      [AUTO_RECORD_PROP]: false,
      [TRANSACTION_TIMER_PROP]: null
    };
  }
  onStopTransaction(a, b) {
    const c = a[TRANSACTION_PROP], e = a[TRANSACTION_TIMER_PROP], f = a[QUEUE_PROP];
    let g = a[POS_PROP];
    if (e) {
      this.clearTimeout(e);
    }
    if (c.length) {
      if (!c.title && !b && a.getTransactionTitle) {
        c.title = a.getTransactionTitle(c);
      } else if (b) {
        c.title = b;
      }
      f[g] = c;
      f.length = ++g;
    }
    return [
      {
        [STATE_PROP]: 'autoreadystate',
        [POS_PROP]: g,
        [TRANSACTION_PROP]: null,
        [TRANSACTION_TIMER_PROP]: null
      },
      () => {
        a.notifyStoresAboutStateRecordingStop(c, { 'stop': true });
      }
    ];
  }
  onStopTransactionDelayed(a) {
    let b = a[TRANSACTION_TIMER_PROP];
    if (b) {
      this.clearTimeout(b);
    }
    b = this.setTimeout(() => {
      a.stopTransaction();
    }, a.autoRecordTransactionStopTimeout);
    return {
      [STATE_PROP]: AutoRecordingState,
      [TRANSACTION_TIMER_PROP]: b
    };
  }
  onResetQueue(a, b) {
    return resetQueue(a, b);
  }
  onRejectTransaction(a) {
    const b = a[TRANSACTION_PROP], c = a[TRANSACTION_TIMER_PROP];
    if (c) {
      this.clearTimeout(c);
    }
    return [
      {
        [STATE_PROP]: 'restoringstate',
        [TRANSACTION_PROP]: null,
        [TRANSACTION_TIMER_PROP]: null
      },
      () => {
        if (b.length) {
          b.undo();
        }
        return [
          'autoreadystate',
          () => {
            a.notifyStoresAboutStateRecordingStop(b, { 'rejected': true });
          }
        ];
      }
    ];
  }
  onModelUpdate(a, ...b) {
    super.onModelUpdate(a, ...b);
    a.stopTransactionDelayed();
  }
  onModelInsertChild(a, ...b) {
    super.onModelInsertChild(a, ...b);
    a.stopTransactionDelayed();
  }
  onModelRemoveChild(a, ...b) {
    super.onModelRemoveChild(a, ...b);
    a.stopTransactionDelayed();
  }
  onStoreModelAdd(a, ...b) {
    super.onStoreModelAdd(a, ...b);
    a.stopTransactionDelayed();
  }
  onStoreModelInsert(a, ...b) {
    super.onStoreModelInsert(a, ...b);
    a.stopTransactionDelayed();
  }
  onStoreModelRemove(a, ...b) {
    super.onStoreModelRemove(a, ...b);
    a.stopTransactionDelayed();
  }
  onStoreRemoveAll(a, ...b) {
    super.onStoreRemoveAll(a, ...b);
    a.stopTransactionDelayed();
  }
};
var AutoRecordingState = new AutoRecordingStateClass();
const AutoRecordingState_default = AutoRecordingState;
Registry_default.registerStmState('autorecordingstate', AutoRecordingState);
const CheckoutStateClass = class extends ReadyStateClass {
  canUndo(a) {
    return false;
  }
  canRedo(a) {
    return false;
  }
  onUndo(a) {
    throwInvalidMethodCall();
  }
  onRedo(a) {
    throwInvalidMethodCall();
  }
  onStartRevision(a, b) {
    const c = new Transaction({ 'title': b });
    return [
      {
        [STATE_PROP]: 'revisionrecordingstate',
        [TRANSACTION_PROP]: c
      },
      () => {
        a.notifyStoresAboutRevRecordingStart();
      }
    ];
  }
  onStartTemporaryRevision(a, b, c) {
    const e = new Transaction({ 'title': b });
    if (c) {
      e[ACTION_QUEUE_PROP] = c[ACTION_QUEUE_PROP].slice();
    }
    return [
      {
        [STATE_PROP]: 'temporaryrevisionrecordingstate',
        [TRANSACTION_PROP]: e
      },
      () => {
        a.notifyStoresAboutTempRevRecordingStart();
      }
    ];
  }
  onCheckoutToHead(a) {
    a.notifyStoresAboutCheckoutToHead({ 'revision': a[REVISION_NAMES_PROP][a[REVISION_NAMES_PROP].length - 1] });
    return a.autoRecord ? 'autoreadystate' : 'readystate';
  }
  onStartTransaction(a) {
    throwInvalidMethodCall();
  }
  onStopTransaction(a) {
    throwInvalidMethodCall();
  }
  onStopTransactionDelayed(a) {
    throwInvalidMethodCall();
  }
  onRejectTransaction(a) {
    throwInvalidMethodCall();
  }
  onEnable(a) {
    throwInvalidMethodCall();
  }
  onDisable(a) {
    throwInvalidMethodCall();
  }
  onAutoRecordOn(a) {
    throwInvalidMethodCall();
  }
  onAutoRecordOff(a) {
    throwInvalidMethodCall();
  }
  onResetQueue(a) {
    throwInvalidMethodCall();
  }
  onModelUpdate() {
  }
  onModelInsertChild() {
  }
  onModelRemoveChild() {
  }
  onStoreModelAdd() {
  }
  onStoreModelInsert() {
  }
  onStoreModelRemove() {
  }
  onStoreRemoveAll() {
  }
};
const CheckoutState = new CheckoutStateClass();
const CheckoutState_default = CheckoutState;
Registry_default.registerStmState('checkoutstate', CheckoutState);
const RevisionRecordingStateClass = class extends RecordingStateClass {
  canUndo(a) {
    return false;
  }
  canRedo(a) {
    return false;
  }
  canCheckoutTo() {
    return false;
  }
  onUndo(a) {
    throwInvalidMethodCall();
  }
  onRedo(a) {
    throwInvalidMethodCall();
  }
  onCheckoutToHead(a) {
    throwInvalidMethodCall();
  }
  onStopRevision(a, b) {
    const c = a[TRANSACTION_PROP], e = a[REVISION_QUEUE_PROP], f = a[REVISION_NAMES_PROP], g = a[REVISION_INDEX_PROP] + 1;
    const i = Transaction.createTransactionWithOriginalInput(c);
    f.splice(g, 0, i.title);
    e.splice(g - 1, 0, i);
    a.commitRevision(i.title);
    return [
      {
        [STATE_PROP]: 'checkoutstate',
        [REVISION_INDEX_PROP]: g,
        [TRANSACTION_PROP]: null
      },
      () => {
        a.notifyStoresAboutRevRecordingStop(i);
      }
    ];
  }
  onStartTransaction(a) {
    throwInvalidMethodCall();
  }
  onStopTransaction(a) {
    throwInvalidMethodCall();
  }
  onStopTransactionDelayed(a) {
    throwInvalidMethodCall();
  }
  onRejectTransaction(a) {
    throwInvalidMethodCall();
  }
  onEnable(a) {
    throwInvalidMethodCall();
  }
  onDisable(a) {
    throwInvalidMethodCall();
  }
  onAutoRecordOn(a) {
    throwInvalidMethodCall();
  }
  onAutoRecordOff(a) {
    throwInvalidMethodCall();
  }
  onResetQueue(a) {
    throwInvalidMethodCall();
  }
};
const RevisionRecordingState = new RevisionRecordingStateClass();
const RevisionRecordingState_default = RevisionRecordingState;
Registry_default.registerStmState('revisionrecordingstate', RevisionRecordingState);
const TemporaryRevisionRecordingStateClass = class extends RecordingStateClass {
  canUndo(a) {
    return false;
  }
  canRedo(a) {
    return false;
  }
  canCheckoutTo() {
    return false;
  }
  onUndo(a) {
    throwInvalidMethodCall();
  }
  onRedo(a) {
    throwInvalidMethodCall();
  }
  onCheckoutToHead(a) {
    throwInvalidMethodCall();
  }
  onStopRevision(a, b) {
    const c = a[TRANSACTION_PROP];
    return [
      {
        [STATE_PROP]: 'checkoutstate',
        [TRANSACTION_PROP]: null
      },
      () => {
        a.notifyStoresAboutTempRevRecordingStop(c);
      }
    ];
  }
  onStartTransaction(a) {
    throwInvalidMethodCall();
  }
  onStopTransaction(a) {
    throwInvalidMethodCall();
  }
  onStopTransactionDelayed(a) {
    throwInvalidMethodCall();
  }
  onRejectTransaction(a) {
    throwInvalidMethodCall();
  }
  onEnable(a) {
    throwInvalidMethodCall();
  }
  onDisable(a) {
    throwInvalidMethodCall();
  }
  onAutoRecordOn(a) {
    throwInvalidMethodCall();
  }
  onAutoRecordOff(a) {
    throwInvalidMethodCall();
  }
  onResetQueue(a) {
    throwInvalidMethodCall();
  }
};
const TemporaryRevisionRecordingState = new TemporaryRevisionRecordingStateClass();
const TemporaryRevisionRecordingState_default = TemporaryRevisionRecordingState;
Registry_default.registerStmState('temporaryrevisionrecordingstate', TemporaryRevisionRecordingState);
const stateTransition = (a, b, ...c) => {
  const e = a.state, f = b.call(a[STATE_PROP], a, ...c);
  if (typeof f === 'string') {
    a[STATE_PROP] = Registry_default.resolveStmState(f);
  } else if (f instanceof StateBase) {
    a[STATE_PROP] = f;
  } else if (Array.isArray(f)) {
    const [g, i] = f;
    if (typeof g === 'string') {
      a[STATE_PROP] = Registry_default.resolveStmState(g);
    } else if (g instanceof StateBase) {
      a[STATE_PROP] = g;
    } else if (g && typeof g === 'object') {
      a = Object.assign(a, g);
      a[STATE_PROP] = Registry_default.resolveStmState(a[STATE_PROP]);
    }
    if (typeof i === 'function') {
      stateTransition(a, i, ...c);
    }
  } else if (f && typeof f === 'object') {
    a = Object.assign(a, f);
    a[STATE_PROP] = Registry_default.resolveStmState(a[STATE_PROP]);
  }
  if (e !== ReadyState_default && e !== AutoReadyState_default && (f !== ReadyState_default && f !== AutoReadyState_default)) {
    a.trigger('ready');
  }
};
const StateTrackingManager = class extends Events_default(Base) {
  static $name = 'StateTrackingManager';
  static get defaultConfig() {
    return {
      'disabled': true,
      'autoRecord': false,
      'autoRecordTransactionStopTimeout': 100,
      'autoRecordMergeUpdateActions': true,
      'mergeAddUpdateActions': false,
      'makeModelUpdateAction': makeModelUpdateAction,
      'makeModelInsertChildAction': makeModelInsertChildAction,
      'makeModelRemoveChildAction': makeModelRemoveChildAction,
      'makeStoreModelAddAction': makeStoreModelAddAction,
      'makeStoreModelInsertAction': makeStoreModelInsertAction,
      'makeStoreModelRemoveAction': makeStoreModelRemoveAction,
      'makeStoreRemoveAllAction': makeStoreRemoveAllAction,
      'getTransactionTitle': null,
      'revisionsEnabled': false,
      'asyncUndoRedo': false,
      'revisionLocalPrefix': 'local-',
      'revisionQueueMaxLength': 20,
      'revisionQueueCommittedMinLength': 1
    };
  }
  construct(...a) {
    Object.assign(this, {
      [STATE_PROP]: ReadyState_default,
      [STORES_PROP]: [],
      [QUEUE_PROP]: [],
      [POS_PROP]: 0,
      [TRANSACTION_PROP]: null,
      [TRANSACTION_TIMER_PROP]: null,
      [AUTO_RECORD_PROP]: false,
      [IS_APPLYING_STASH]: false,
      'stashedTransactions': {}
    });
    super.construct(...a);
  }
  get state() {
    return this[STATE_PROP];
  }
  get position() {
    return this[POS_PROP];
  }
  get length() {
    return this[QUEUE_PROP].length;
  }
  get stores() {
    return Array.from(this[STORES_PROP]);
  }
  hasStore(a) {
    return this[STORES_PROP].includes(a);
  }
  addStore(a) {
    if (!this.hasStore(a)) {
      this[STORES_PROP].push(a);
      a.stm = this;
      a.forEach(b => b.stm = this);
      if (a.isTree) {
        a.rootNode.stm = this;
      }
    }
  }
  removeStore(a) {
    if (this.hasStore(a)) {
      this[STORES_PROP] = this[STORES_PROP].filter(b => b !== a);
      a.stm = null;
      a.forEach(b => b.stm = null);
    }
  }
  forEachStore(a) {
    this[STORES_PROP].forEach(b => a(b, b.id));
  }
  get disabled() {
    return this.state === DisabledState_default;
  }
  set disabled(a) {
    const b = this;
    if (b.disabled !== a) {
      if (a) {
        stateTransition(b, b.state.onDisable, b);
      } else {
        stateTransition(b, b.state.onEnable, b);
      }
      b.trigger('stmDisabled', { 'disabled': a });
      b.trigger('disabled', { 'disabled': a });
    }
  }
  get enabled() {
    return !this.disabled;
  }
  enable() {
    this.disabled = false;
  }
  disable() {
    this.disabled = true;
  }
  get isReady() {
    return this.state === ReadyState_default || this.state === AutoReadyState_default;
  }
  waitForReadiness() {
    return this.await('ready', false);
  }
  get isRecording() {
    return this.state === RecordingState_default || this.state === AutoRecordingState_default;
  }
  get isApplyingStash() {
    return this[IS_APPLYING_STASH];
  }
  get shouldRecordAction() {
    return this.enabled && (!this.isRestoringState || this.isApplyingStash) && !this.isCheckingOut;
  }
  get autoRecord() {
    return this[AUTO_RECORD_PROP];
  }
  set autoRecord(a) {
    const b = this;
    if (b.autoRecord != a) {
      if (a) {
        stateTransition(b, b.state.onAutoRecordOn, b);
      } else {
        stateTransition(b, b.state.onAutoRecordOff, b);
      }
    }
  }
  startTransaction(a = null) {
    stateTransition(this, this.state.onStartTransaction, a);
  }
  stopTransaction(a = null) {
    const b = this;
    if (b.autoRecord && b.autoRecordMergeUpdateActions) {
      b.mergeTransactionUpdateActions();
    }
    if (b.mergeAddUpdateActions) {
      b.mergeTransactionAddUpdateActions();
    }
    stateTransition(b, b.state.onStopTransaction, a);
  }
  stopTransactionDelayed() {
    stateTransition(this, this.state.onStopTransactionDelayed);
  }
  rejectTransaction() {
    stateTransition(this, this.state.onRejectTransaction);
  }
  get transaction() {
    return this[TRANSACTION_PROP];
  }
  get queue() {
    return this[QUEUE_PROP].map(a => a.title);
  }
  get rawQueue() {
    return this[QUEUE_PROP];
  }
  get isRestoringState() {
    return this.state === RestoringState_default;
  }
  get isRestoring() {
    return this.state === RestoringState_default || this.isApplyingStash || this.isNavigatingRevisions;
  }
  get canUndo() {
    return this.state.canUndo(this);
  }
  get canRedo() {
    return this.state.canRedo(this);
  }
  async undo(a = 1) {
    if (!this.isReady) {
      await this.waitForReadiness();
    }
    stateTransition(this, this.state.onUndo, a);
  }
  async undoAll() {
    if (!this.isReady) {
      await this.waitForReadiness();
    }
    this.undo(this.length);
  }
  async redo(a = 1) {
    if (!this.isReady) {
      await this.waitForReadiness();
    }
    stateTransition(this, this.state.onRedo, a);
  }
  async redoAll() {
    if (!this.isReady) {
      await this.waitForReadiness();
    }
    this.redo(this.length);
  }
  resetQueue(a = {
    'undo': true,
    'redo': true,
    'revision': true
  }) {
    stateTransition(this, this.state.onResetQueue, a);
  }
  resetUndoQueue() {
    this.resetQueue({ 'undo': true });
  }
  resetRedoQueue() {
    this.resetQueue({ 'redo': true });
  }
  resetRevisionQueue() {
    this.resetQueue({ 'revision': true });
  }
  notifyStoresAboutStateRecordingStart(a) {
    this.forEachStore(b => {
      let c;
      return (c = b.onStmRecordingStart) == null ? void 0 : c.call(b, this, a);
    });
    this.trigger('recordingStart', {
      'stm': this,
      'transaction': a
    });
  }
  notifyStoresAboutStateRecordingStop(a, b) {
    const c = this;
    c.forEachStore(e => {
      let f;
      return (f = e.onStmRecordingStop) == null ? void 0 : f.call(e, c, a, b);
    });
    c.trigger('recordingStop', {
      'stm': c,
      'transaction': a,
      'reason': b
    });
    if (c.revisionsEnabled && b.stop) {
      c.increaseRevision(Transaction.from([a]));
    }
  }
  notifyStoresAboutStateRestoringStart() {
    this.forEachStore(a => {
      let b;
      return (b = a.onStmRestoringStart) == null ? void 0 : b.call(a, this);
    });
    this.trigger('restoringStart', { 'stm': this });
  }
  notifyStoresAboutStateRestoringStop({
    cause: a,
    transactions: b
  }) {
    const c = this;
    c.forEachStore(e => {
      let f;
      return (f = e.onStmRestoringStop) == null ? void 0 : f.call(e, c);
    });
    c.trigger('restoringStop', {
      'stm': c,
      'cause': a,
      'transactions': b
    });
    if (c.revisionsEnabled) {
      if (c.asyncUndoRedo) {
        const e = () => {
          const f = Transaction.from(b);
          c.increaseRevision(a === 'undo' ? f.invert() : f);
        };
        c.trigger('increaseRevisionAsync', { 'callback': e });
      } else {
        const f = Transaction.from(b);
        c.increaseRevision(a === 'undo' ? f.invert() : f);
      }
    }
  }
  notifyStoresAboutQueueReset(a) {
    this.forEachStore(b => {
      let c;
      return (c = b.onStmQueueReset) == null ? void 0 : c.call(b, this, a);
    });
    this.trigger('queueReset', {
      'stm': this,
      'options': a
    });
  }
  notifyStoresAboutCheckoutStart() {
    this.trigger('checkoutStart', { 'stm': this });
  }
  notifyStoresAboutRevRecordingStart() {
    this.trigger('revisionRecordingStart', { 'stm': this });
  }
  notifyStoresAboutTempRevRecordingStart() {
    this.trigger('temporaryRevisionRecordingStart', { 'stm': this });
  }
  notifyStoresAboutRevRecordingStop(a) {
    this.trigger('revisionRecordingStop', {
      'stm': this,
      'revision': a.title
    });
  }
  notifyStoresAboutTempRevRecordingStop(a) {
    this.trigger('temporaryRevisionRecordingStop', {
      'stm': this,
      'revision': a.title
    });
  }
  notifyStoresAboutCheckoutToHead({revision: a}) {
    this.trigger('checkoutToHead', {
      'stm': this,
      'revision': a
    });
  }
  onModelUpdate(a, b, c, e) {
    stateTransition(this, this.state.onModelUpdate, a, b, c, e);
  }
  onModelInsertChild(a, b, c, e, f) {
    stateTransition(this, this.state.onModelInsertChild, a, b, c, e, f);
  }
  onModelRemoveChild(a, b, c) {
    stateTransition(this, this.state.onModelRemoveChild, a, b, c);
  }
  onStoreModelAdd(a, b, c) {
    stateTransition(this, this.state.onStoreModelAdd, a, b, c);
  }
  onStoreModelInsert(a, b, c, e, f) {
    stateTransition(this, this.state.onStoreModelInsert, a, b, c, e, f);
  }
  onStoreModelRemove(a, b, c, e) {
    stateTransition(this, this.state.onStoreModelRemove, a, b, c, e);
  }
  onStoreRemoveAll(a, b, c) {
    stateTransition(this, this.state.onStoreRemoveAll, a, b, c);
  }
  onUndoKeyPress(a) {
    const b = this;
    if (b.enabled) {
      if (a.shiftKey) {
        if (b.canRedo) {
          a.preventDefault();
          b.redo();
        }
      } else if (b.canUndo) {
        a.preventDefault();
        b.undo();
      }
    }
  }
  stash() {
    const a = this;
    if (a.transaction) {
      const b = IdHelper.generateId('_stashedTransactionGeneratedId_');
      a.stashedTransactions[b] = a.transaction;
      a.rejectTransaction();
      return b;
    }
  }
  applyStash(a) {
    const b = this, c = b.stashedTransactions[a];
    b[IS_APPLYING_STASH] = true;
    if (c) {
      b.startTransaction(c.title);
      c.redo();
      delete b.stashedTransactions[a];
    }
    b[IS_APPLYING_STASH] = false;
  }
  mergeTransactionUpdateActions(a = this.transaction) {
    a.mergeUpdateModelActions();
  }
  mergeTransactionAddUpdateActions(a = this.transaction) {
    a.mergeAddUpdateModelActions();
  }
  canCheckoutTo(a) {
    return (this.isReady || this.state === CheckoutState_default) && this.state.canCheckoutTo(this, a);
  }
  get currentRevision() {
    if (this.revisionsEnabled) {
      return this[REVISION_NAMES_PROP][this[REVISION_INDEX_PROP]];
    }
  }
  get lastRevision() {
    if (this.revisionsEnabled) {
      return this[REVISION_NAMES_PROP][this[REVISION_NAMES_PROP].length - 1];
    }
  }
  get currentRevisionTransaction() {
    if (this.revisionsEnabled) {
      return this[REVISION_QUEUE_PROP][this[REVISION_INDEX_PROP] - 1];
    }
  }
  get lastCommittedRevision() {
    const a = this[REVISION_QUEUE_PROP];
    let b;
    for (let c = a.length - 1; c >= 0; c--) {
      if (a[c].committed) {
        b = a[c];
        break;
      }
    }
    return b;
  }
  get lastCommittedRevisionId() {
    let a, b;
    return (b = (a = this.lastCommittedRevision) == null ? void 0 : a.title) != null ? b : this[REVISION_NAMES_PROP][0];
  }
  get localRevisions() {
    return this[REVISION_QUEUE_PROP].slice(this[REVISION_QUEUE_PROP].findIndex(a => !a.committed));
  }
  get isCheckingOut() {
    return this.state === CheckoutState_default;
  }
  get isRecordingRevision() {
    return this.state === RevisionRecordingState_default;
  }
  get isRecordingTemporaryRevision() {
    return this.state === TemporaryRevisionRecordingState_default;
  }
  get isNavigatingRevisions() {
    return this.state === CheckoutState_default || this.state === RevisionRecordingState_default || this.state === TemporaryRevisionRecordingState_default;
  }
  initRevision(a, b = true) {
    const c = this;
    if (c.revisionsEnabled) {
      c.autoRecord = b;
      c[REVISION_NAMES_PROP] = [a];
      c[REVISION_QUEUE_PROP] = [];
      c[REVISION_INDEX_PROP] = 0;
    }
  }
  increaseRevision(a, b) {
    const c = this;
    let e;
    if (!c.revisionsEnabled) {
      return;
    }
    if (c.isNavigatingRevisions && !a.type) {
      throw new RevisionException('Cannot add new revision when in the checkout state');
    }
    if (!a[ACTION_QUEUE_PROP].length && !a.type) {
      return;
    }
    const f = IdHelper.generateId(c.revisionLocalPrefix);
    a.title = f;
    if (c.trigger('beforeRevisionAdd', { 'localRevisionId': f }) === false) {
      return;
    }
    const g = a.conflictResolutionFor && c[REVISION_QUEUE_PROP].find(j => j.type === TRANSACTION_TYPES.TEMPORARY && j.conflictResolutionFor === a.conflictResolutionFor);
    if (g) {
      c[REVISION_NAMES_PROP].splice(c[REVISION_NAMES_PROP].indexOf(g.title), 1, f);
      c[REVISION_QUEUE_PROP].splice(c[REVISION_QUEUE_PROP].indexOf(g), 1, a);
    } else {
      c[REVISION_NAMES_PROP].push(f);
      c[REVISION_QUEUE_PROP].push(a);
    }
    if (!a.conflictResolutionFor) {
      c[REVISION_INDEX_PROP] = c[REVISION_NAMES_PROP].length - 1;
    }
    if (a.type === TRANSACTION_TYPES.TEMPORARY) {
      return;
    }
    if (a.filterUserInput) {
      if (!a.conflictResolutionFor) {
        a.normalizeUserInputGeneration();
      }
      e = a.getUserInput();
    }
    if (!b && c[REVISION_QUEUE_PROP].length > c.revisionQueueMaxLength && !a.type) {
      c.cleanUpRevisions();
    }
    const i = {
      'localRevisionId': f,
      'userInput': e
    };
    if (a.conflictResolutionFor) {
      i.conflictResolutionFor = a.conflictResolutionFor;
      i.revisionChanges = b;
    }
    c.trigger('revisionAdd', i);
  }
  createDataCorrectionTransaction() {
    const a = new Transaction({ 'type': TRANSACTION_TYPES.DATA_CORRECTION });
    this.increaseRevision(a);
  }
  createConflictResolutionRevision(a, b, c) {
    const {transaction: e} = this;
    c = ObjectHelper.clone(c);
    if (b) {
      const f = this[REVISION_QUEUE_PROP].find(g => g.conflictResolutionFor === b);
      if (f) {
        f.conflictResolutionFor = a;
      }
    }
    this.increaseRevision(Transaction.from([e], {
      'conflictResolutionFor': a != null ? a : e.title,
      'type': TRANSACTION_TYPES.CONFLICT_RESOLUTION
    }), c);
  }
  createTemporaryConflictResolutionRevision(a, b) {
    const {transaction: c} = this;
    this.increaseRevision(Transaction.from([c], {
      'conflictResolutionFor': a != null ? a : c.title,
      'type': TRANSACTION_TYPES.TEMPORARY
    }), b);
  }
  checkoutTo(a) {
    if (!this.canCheckoutTo(a)) {
      return false;
    }
    stateTransition(this, this.state.onCheckoutTo, a);
  }
  checkoutToLastCommittedRevision() {
    this.checkoutTo(this.lastCommittedRevisionId);
  }
  checkoutToHead() {
    this.checkoutTo(this[REVISION_NAMES_PROP][this[REVISION_NAMES_PROP].length - 1]);
    stateTransition(this, this.state.onCheckoutToHead);
  }
  checkoutToNext() {
    if (this[REVISION_INDEX_PROP] + 1 < this[REVISION_NAMES_PROP].length) {
      return this.checkoutTo(this[REVISION_NAMES_PROP][this[REVISION_INDEX_PROP] + 1]);
    } else {
      return false;
    }
  }
  startRevision(a) {
    stateTransition(this, this.state.onStartRevision, a);
  }
  stopRevision() {
    stateTransition(this, this.state.onStopRevision);
  }
  startTemporaryRevision(a, b) {
    stateTransition(this, this.state.onStartTemporaryRevision, a, b);
  }
  stopTemporaryRevision() {
    stateTransition(this, this.state.onStopRevision);
  }
  commitRevision(a, b) {
    const c = this[REVISION_NAMES_PROP], e = this[REVISION_QUEUE_PROP], f = e.find(g => g.title === a);
    for (let g = e.indexOf(f) - 1; g >= 0; g--) {
      if (e[g].committed === false && e[g].type !== TRANSACTION_TYPES.TEMPORARY && !e[g].conflictResolutionFor) {
        throw new RevisionException('Cannot commit revision because it is preceded by an uncommitted rev id ' + e[g].title, e);
      }
    }
    if (b) {
      if (c.includes(b)) {
        throw new RevisionException('Proposed revision id (' + b + ') already exists');
      }
      f.title = c[c.indexOf(f.title)] = b;
      e.forEach(j => {
        if (j.type === TRANSACTION_TYPES.TEMPORARY && j.conflictResolutionFor === a) {
          j.conflictResolutionFor = b;
        }
      });
    }
    f.committed = true;
  }
  cleanUpRevisions(a = this.revisionQueueCommittedMinLength) {
    const b = this[REVISION_NAMES_PROP], c = this[REVISION_QUEUE_PROP], e = [];
    for (let f = 0; f < c.length; f++) {
      const g = c[f];
      if (g.committed) {
        e.push(g);
      } else {
        break;
      }
    }
    e.splice(Math.max(0, e.length - 1 - a), Math.min(e.length, a));
    c.splice(0, e.length);
    b.splice(1, e.length);
  }
  markCurrentTransactionContentUserInput() {
    if (this.isRecording || this.isRecordingRevision || this.isRecordingTemporaryRevision) {
      this.transaction.markCurrentTransactionContentUserInput();
    }
  }
  markCurrentTransactionContentCalculated() {
    if (this.isRecording || this.isRecordingRevision || this.isRecordingTemporaryRevision) {
      this.transaction.markCurrentTransactionContentCalculated();
    }
  }
};
StateTrackingManager._$name = 'StateTrackingManager';
const StoreStm_default = a => {
  let b;
  return b = class extends (a || Base) {
    static get $name() {
      return 'StoreStm';
    }
    changeStm(c, e) {
      let f;
      if ((f = e == null ? void 0 : e.hasStore) == null ? void 0 : f.call(e, this)) {
        e.removeStore(this);
      }
      if (c) {
        if (!c.isStateTrackingManager) {
          c = new StateTrackingManager(c);
        }
        if (!c.hasStore(this)) {
          c.addStore(this);
        }
      }
      return c;
    }
    add(c, e = false, f = {}) {
      let g;
      const {stm: i} = this;
      if (!this.tree && (i == null ? void 0 : i.shouldRecordAction)) {
        g = super.add(c, e, f);
        if (g == null ? void 0 : g.length) {
          i.onStoreModelAdd(this, g, e);
        }
      } else {
        g = super.add(c, e, f);
      }
      return g;
    }
    insert(c, e, f = false) {
      let g;
      const {stm: i} = this;
      if (!this.tree && (i == null ? void 0 : i.shouldRecordAction)) {
        const j = (Array.isArray(e) ? e : [e]).reduce((k, l) => {
          const n = l instanceof Model ? this.indexOf(l) : void 0;
          if (n !== void 0 && n !== -1) {
            k.set(l, n);
          }
          return k;
        }, new Map());
        g = super.insert(c, e);
        if (g == null ? void 0 : g.length) {
          c = this.indexOf(g[0]);
          i.onStoreModelInsert(this, c, g, j, f);
        }
      } else {
        g = super.insert(c, e, f);
      }
      return g;
    }
    remove(c, e = false, f) {
      let g;
      const {stm: i} = this;
      if (!this.tree && (i == null ? void 0 : i.shouldRecordAction)) {
        const j = (Array.isArray(c) ? c : [c]).map(l => this.getById(l)).filter(l => !!l);
        const k = j.reduce((l, n) => {
          const o = this.indexOf(n);
          if (o !== void 0 && o !== -1) {
            l.set(n, o);
          }
          return l;
        }, new Map());
        g = super.remove(c, e, f);
        if (g == null ? void 0 : g.length) {
          i.onStoreModelRemove(this, g, k, e);
        }
      } else {
        g = super.remove(c, e, f);
      }
      return g;
    }
    removeAll(c) {
      const {stm: e} = this;
      let f;
      if (e == null ? void 0 : e.shouldRecordAction) {
        const {
            tree: g,
            rootNode: i,
            allRecords: j
          } = this, k = j.length, l = g ? i.children.slice() : j.slice();
        f = super.removeAll(c);
        if (k && this.count === 0) {
          e.onStoreRemoveAll(this, l, c);
        }
      } else {
        f = super.removeAll(c);
      }
      return f;
    }
    beforeApplyChangeset() {
      const {
        stm: c,
        crudManager: e
      } = this;
      let f = false, g = null;
      if (!(e == null ? void 0 : e.applyingChangeset) && (c == null ? void 0 : c.enabled) && !c.isNavigatingRevisions) {
        f = true;
        if (c.isRecording) {
          g = c.stash();
        }
        if (this.ignoreRemoteChangesInSTM) {
          c.disable();
        } else {
          c.startTransaction();
        }
      }
      return {
        'shouldResume': f,
        'transactionId': g
      };
    }
    applyChangeset(c, e, f, g, i) {
      const {
          shouldResume: j,
          transactionId: k
        } = this.beforeApplyChangeset(), l = super.applyChangeset(c, e, f, g, i);
      this.afterApplyChangeset(j, k);
      return l;
    }
    afterApplyChangeset(c, e) {
      if (c) {
        const {stm: f} = this;
        if (this.ignoreRemoteChangesInSTM) {
          f.enable();
        } else {
          f.stopTransaction();
        }
        f.applyStash(e);
      }
    }
  }, __publicField(b, 'configurable', {
    'stm': null,
    'ignoreRemoteChangesInSTM': false
  }), b;
};
const Promissory = class {
  static new(a) {
    return new this(a);
  }
  constructor(a) {
    this.promise = new Promise((b, c) => {
      this.resolve = b;
      this.reject = c;
    });
    if (a) {
      a(this);
    }
  }
  catch(a) {
    this.promise = this.promise.catch(a);
    return this;
  }
  finally(a) {
    this.promise = this.promise.finally(a);
    return this;
  }
  then(...a) {
    this.promise = this.promise.then(...a);
    return this;
  }
};
Promissory._$name = 'Promissory';
const StoreLazyLoadPlugin = class extends InstancePlugin {
  static configurable = {
    'totalCountProperty': 'total',
    'dataProperty': 'data',
    'chunkSize': 100
  };
  static $name = 'StoreLazyLoadPlugin';
  static get pluginConfig() {
    return {
      'override': ['updateAutoLoad'],
      'assign': [
        'load',
        'isLoading',
        'lazyGetAt'
      ],
      'after': ['afterLoadData'],
      'before': ['setStoreData']
    };
  }
  static get properties() {
    return { 'loadQueue': {} };
  }
  get isLoading() {
    return this.client._isLoading || Object.values(this.loadQueue).some(a => ObjectHelper.isPromise(a));
  }
  get storage() {
    return this.client.storage;
  }
  set totalCount(a) {
    this._totalCount = a;
  }
  get totalCount() {
    return this._totalCount;
  }
  setEstimatedTotalCount(a) {
    this.totalCount = a;
  }
  afterConstruct() {
    const a = this, {client: b} = a;
    if (b.syncDataOnLoad) {
      b.syncDataOnLoad = false;
    }
    a.storeListenersDetacher = b.ion({
      'beforeAdd': 'internalOnBeforeAdd',
      'beforeRemove': 'internalOnBeforeAdd',
      'change': 'internalOnChange',
      'commit': 'internalOnCommit',
      'endApplyChangeset': 'internalOnCommit',
      'thisObj': a
    });
    if (!b.requestData) {
      b.requestData = a.requestData;
    }
    if (b.autoLoad) {
      a.load();
    }
  }
  updateAutoLoad(a) {
    if (a && !this.client.isAjaxStore) {
      this.load();
    }
  }
  internalOnBeforeAdd() {
    if (this.isLoading) {
      throw new Error('Removing or adding is not allowed when a lazy loaded Store is loading');
    }
  }
  internalOnChange() {
    if (!this.$loadLock && (this.client.added.count || this.client.removed.count)) {
      this.$loadLock = new Promissory();
    }
  }
  internalOnCommit() {
    if (!this.client.changes && this.$loadLock) {
      this.$loadLock.resolve();
      this.$loadLock = null;
    }
  }
  afterLoadData() {
    if (this.totalCount) {
      this.storage.values.length = this.totalCount;
    }
  }
  setStoreData(a) {
    if (this.client.records.filter(b => b).length && !(a == null ? void 0 : a.length)) {
      this.totalCount = null;
      this.loadQueue = {};
    }
  }
  clearLoaded() {
    const {client: a} = this;
    a.suspendEvents();
    this.totalCount = null;
    a.clear(true);
    this.loadQueue = {};
    a.resumeEvents();
  }
  async requestData({
    startIndex: a,
    count: b
  }) {
  }
  lazyGetAt(a) {
    return this.getAt(a);
  }
  async getAt(a) {
    let b;
    const c = this, {
        loadQueue: e,
        totalCount: f
      } = c;
    if (a < 0 || f != null && a > f) {
      return null;
    }
    const g = c.getFromStorage(a);
    if (g) {
      return g;
    }
    await ((b = c.$loadLock) == null ? void 0 : b.promise);
    if (e[a]) {
      await e[a];
    } else {
      await c.doLazyLoad(c.calculateRange(a));
    }
    return c.getFromStorage(a);
  }
  getFromStorage(a) {
    return this.storage.getAt(a);
  }
  calculateRange(a) {
    const {
        totalCount: b,
        loadQueue: c,
        chunkSize: e
      } = this, f = Math.max(0, a - e), g = (b ? Math.min(a + e, b) : a + e) - 1;
    let i = a, j = a;
    while (i > f && !c[i - 1] && !this.getFromStorage(i - 1)) {
      i -= 1;
    }
    while (j < g && !c[j + 1] && !this.getFromStorage(j + 1)) {
      j += 1;
    }
    return {
      'startIndex': i,
      'count': j - i + 1
    };
  }
  async doLazyLoad({
    startIndex: a,
    count: b
  }) {
    this.triggerLazyLoadStart(arguments[0]);
    const c = this, {loadQueue: e} = c, f = a + b - 1, g = c.client.requestData(arguments[0]);
    for (let n = a; n <= f; n++) {
      e[n] = g;
    }
    const j = await g;
    if (!j) {
      return;
    }
    const {
      [c.totalCountProperty]: k,
      [c.dataProperty]: l
    } = j;
    for (let o = a; o <= f; o++) {
      e[o] = null;
    }
    if (k != null) {
      c.totalCount = k;
    } else if (c.totalCount == null) {
      if (b > l.length) {
        c.setEstimatedTotalCount(a + l.length);
      }
    }
    c.addData(l, a);
    if (c.totalCount != null) {
      c.storage.values.length = c.totalCount;
    }
    c.triggerLazyLoadEnd(arguments[0], l);
  }
  addData(a, b) {
    const {
      client: c,
      storage: e
    } = this;
    if (!(a == null ? void 0 : a.length)) {
      return;
    }
    if (!c.count && !b) {
      c.data = a;
      return;
    }
    for (let j = a.length - 1; j >= 0; j--) {
      const k = a[j], l = c.idRegister[k.id];
      if (l) {
        l.setByDataSource(k, true);
        l.internalClearChanges(false, true, k);
        a.splice(j, 1);
      }
    }
    if (!a.length) {
      return;
    }
    const {values: f} = e, g = c.processRecords(a, void 0, true);
    if (f.length < b) {
      f.length = b;
    }
    if (b == null) {
      b = f.length;
    }
    f.splice(b, g.length, ...g);
    c.joinRecordsToStore(g);
    c.updateDependentStores('add', g);
    for (const n of g) {
      c.idMap[n.id] = {
        'index': b,
        'visibleIndex': b,
        'record': n
      };
      b += 1;
      e.addToIndices(n);
    }
    c.trigger('change', {
      'action': 'lazyload',
      'records': g
    });
  }
  checkDuplicates(a) {
    const {storage: b} = this;
    for (const c of a) {
      const e = b.indexOf(c.id);
      if (e >= 0) {
        b.values.splice(e, 1);
      }
    }
  }
  triggerLazyLoadStart(a) {
    const b = this;
    if (!b.isLoading) {
      if (b.isConfiguring) {
        b.delay(() => b.trigger('lazyLoadStarted'), 0);
      } else {
        b.client.trigger('lazyLoadStarted');
      }
    }
    b.client.trigger('beforeLazyLoad', { ...a });
  }
  triggerLazyLoadEnd(a, b) {
    if (!this.isLoading) {
      this.client.trigger('lazyLoadEnded');
    }
    this.client.trigger('afterLazyLoad', {
      ...a,
      'data': b
    });
  }
  load(a) {
    if (this.client.count > 0) {
      this.clearLoaded();
    }
    return this.getAt(0);
  }
  doDestroy() {
    let a;
    (a = this.storeListenersDetacher) == null ? void 0 : a.call(this);
    this.loadQueue = {};
    super.doDestroy();
  }
};
StoreLazyLoadPlugin._$name = 'StoreLazyLoadPlugin';
const countChildren = a => {
  let b, c;
  return (c = (b = a.children) == null ? void 0 : b.reduce((e, f) => {
    let g;
    return e + ((g = f.remoteChildCount) != null ? g : 0) + 1;
  }, 0)) != null ? c : 0;
};
const isFullyLoaded = a => countChildren(a) >= a.remoteChildCount;
const TreeStoreLazyLoadPlugin_default = a => {
  let b;
  return b = class extends a {
    static get pluginConfig() {
      let c, e;
      const f = {};
      for (const i in super.pluginConfig) {
        f[i] = [...super.pluginConfig[i]];
      }
      f.override = (c = f.override) != null ? c : [];
      f.override.push('loadChildren', 'getAt');
      const g = (e = f.assign) == null ? void 0 : e.indexOf('lazyGetAt');
      if (g >= 0) {
        f.assign.splice(g, 1);
      }
      return f;
    }
    constructor() {
      super(...arguments);
      this.totalCount = 0;
    }
    load() {
      const c = this, {rootNode: e} = c.client, f = c.loadQueue[e.id];
      if (!f) {
        return c.doLazyLoad({
          'parent': e,
          'startIndex': 0
        });
      } else if (f === true) {
        c.clearLoaded();
        return c.load();
      } else {
        return f;
      }
    }
    changeRemoteChildCount(c, e, f) {
      let g;
      if (c == null ? void 0 : c.length) {
        const i = c.map(j => {
          let k, l, n;
          return (n = j.parent) != null ? n : this.client.getById((l = j.parentId) != null ? l : (k = j.originalData) == null ? void 0 : k.parentId);
        });
        while (i.length) {
          const j = i.shift();
          if (j) {
            if (!j.isRoot) {
              i.push(j.parent);
            }
            j.remoteChildCount = (g = j.remoteChildCount ? j.remoteChildCount + e : j.children.length) != null ? g : 0;
            f.add(j);
          }
        }
      }
    }
    internalOnCommit({changes: c}) {
      super.internalOnCommit();
      const e = new Set();
      this.changeRemoteChildCount(c.added, 1, e);
      this.changeRemoteChildCount(c.removed, -1, e);
      e.forEach(f => {
        this.loadQueue[f.id] = isFullyLoaded(f);
      });
    }
    loadChildren(c) {
      return this.doLazyLoad({
        'parent': c,
        'startIndex': 0
      });
    }
    getAt(c) {
      let e;
      const f = this, {
          loadQueue: g,
          client: i
        } = f, j = i.records[c];
      if (j) {
        const {parent: k} = j;
        if (j.expanded && j.remoteChildCount > 0 && !((e = j.children) == null ? void 0 : e.length) && !g[j.id]) {
          f.doLazyLoad({
            'parent': j,
            'startIndex': 0
          });
        }
        if (j === k.lastChild && !g[k.id]) {
          f.doLazyLoad({
            'startIndex': j.parentIndex + 1,
            'parent': k
          });
        }
      }
      return j;
    }
    doLazyLoad({
      parent: c,
      ...e
    }) {
      const f = this, {
          loadQueue: g,
          client: i
        } = f, {id: j} = c;
      c.instanceMeta(i).isLoadingChildren = true;
      f.triggerLazyLoadStart(arguments[0]);
      return g[j] = i.requestData({
        ...e,
        'parentId': c.isRoot ? 'root' : j,
        'count': f.chunkSize
      }).then(k => {
        let l;
        const {
          [f.totalCountProperty]: n,
          [f.dataProperty]: o
        } = k;
        if (c.isRoot && n >= 0) {
          c.remoteChildCount = n;
        }
        f.addData(o, c);
        f.totalCount = i.count;
        g[j] = isFullyLoaded(c);
        if (!f.isLoading) {
          (l = i.trigger) == null ? void 0 : l.call(i, 'refresh', { 'action': 'lazyload' });
        }
        f.triggerLazyLoadEnd(arguments[0], o);
        c.instanceMeta(i).isLoadingChildren = false;
      });
    }
    addData(c, e) {
      const {
        client: f,
        loadQueue: g
      } = this;
      WalkHelper.preWalk({ 'children': c }, ({children: i}) => (i == null ? void 0 : i.length) ? i : null, i => {
        const {
          id: j,
          children: k
        } = i;
        if (j && i.remoteChildCount > 0) {
          if (i.expanded === false && !(k == null ? void 0 : k.length)) {
            i.children = true;
          } else if ((k == null ? void 0 : k.length) && !g[j] && isFullyLoaded(i)) {
            g[j] = true;
          } else if (!k) {
            i.children = [];
          }
        }
      });
      if (!f.modelClass.fieldMap.remoteChildCount) {
        f.modelClass.addField('remoteChildCount');
      }
      e.appendChild(c, true);
      f.added.clear();
    }
  }, __publicField(b, '$name', 'TreeStoreLazyLoadPlugin'), __publicField(b, 'configurable', { 'chunkSize': 50 }), b;
};
const dataAddRemoveActions = {
  'splice': 1,
  'clear': 1
};
const defaultTraverseOptions2 = {
  'includeFilteredOutRecords': false,
  'includeCollapsedGroupRecords': false
};
const fixTraverseOptions2 = (a, b) => {
  b = b || false;
  if (typeof b === 'boolean') {
    b = {
      'includeFilteredOutRecords': b,
      'includeCollapsedGroupRecords': false
    };
  }
  return b || defaultTraverseOptions2;
};
const useRawDataUnset = Symbol('useRawDataUnset');
const useRawDataDefaults = {
  'enabled': true,
  'disableDuplicateIdCheck': true,
  'disableDefaultValue': false,
  'disableTypeConversion': false
};
const useRawDataRemoteDefaults = {
  'enabled': true,
  'disableDuplicateIdCheck': false,
  'disableDefaultValue': false,
  'disableTypeConversion': false
};
const _Store = class _Store extends Base.mixin(Delayable_default, Identifiable_default, Events_default, Pluggable_default, State_default, StoreFilter_default, StoreChanges_default, StoreCRUD_default, StoreRelation_default, StoreSum_default, StoreSearch_default, StoreSort_default, StoreGroup_default, StoreChained_default, StoreState_default, StoreTree_default, StoreStm_default, StoreSync_default, StoreProxy_default) {
  static $lazyLoadPluginClass = StoreLazyLoadPlugin;
  static configurable = {
    'id': true,
    'modelClass': Model,
    'verifyNoGeneratedIds': true,
    'lazyLoad': null,
    'autoLoad': null,
    'useRawData': useRawDataUnset
  };
  static get $name() {
    return 'Store';
  }
  static get properties() {
    return {
      'relationCache': {},
      'dependentStoreConfigs': new Map(),
      'addingClean': false
    };
  }
  static get defaultConfig() {
    return {
      'fields': null,
      'autoTree': true,
      'data': null,
      'tree': false,
      'callOnFunctions': true,
      'storage': null,
      'allowNoId': true,
      'preventSubClassingModel': null,
      'storeClass': null
    };
  }
  static get identifiable() {
    return { 'registerGeneratedId': false };
  }
  constructor(...a) {
    super(...a);
    if (this.objectify) {
      return this.initProxy();
    }
  }
  construct(a = {}) {
    const b = this;
    Object.assign(b, {
      'added': new StoreBag(),
      'removed': new StoreBag(),
      'modified': new StoreBag(),
      'idRegister': {},
      'internalIdRegister': {},
      'oldIdMap': {}
    });
    super.construct(a);
    b.initRelations();
  }
  static from(a, b = {}, c = null) {
    if (a && !a.isStore) {
      if (typeof a === 'string') {
        a = _Store.getStore(a);
      } else {
        if (Array.isArray(a)) {
          if (c) {
            a = a.map(e => e.isModel ? e : c(e));
          }
          a = ObjectHelper.assign({}, b, { 'data': a });
        } else {
          a = ObjectHelper.assign({}, b, a);
        }
        a = new _Store(a);
      }
    }
    return a;
  }
  equals(a) {
    return a.isStore && a.$master === this.$master;
  }
  doDestroy() {
    let a, b, c, e;
    const f = this, g = f.registeredRecords;
    (b = (a = f.stm) == null ? void 0 : a.removeStore) == null ? void 0 : b.call(a, f);
    for (let j = g.length - 1, k; j >= 0; j--) {
      k = g[j];
      if (!(k == null ? void 0 : k.isDestroyed)) {
        k.unjoinStore(f);
      }
    }
    (c = f._storage) == null ? void 0 : c.destroy();
    if (f.isChained) {
      if (!f.masterStore.isDestroying) {
        ArrayHelper.remove(f.masterStore.chainedStores, f);
      }
    } else {
      (e = f.rootNode) == null ? void 0 : e.destroy();
    }
    super.doDestroy();
  }
  beginBatch() {
    this.suspendEvents();
  }
  endBatch() {
    if (this.resumeEvents()) {
      const {values: a} = this.storage;
      this.trigger('refresh', {
        'action': 'batch',
        'data': a,
        'records': a
      });
    }
  }
  set storage(a) {
    const b = this;
    if (a == null ? void 0 : a.isCollection) {
      b._storage = a;
    } else {
      b._storage = new Collection(a);
    }
    b._storage.autoFilter = b.reapplyFilterOnAdd;
    b._storage.autoSort = b.reapplySortersOnAdd;
    for (const c of b._storage) {
      c.joinStore(b);
    }
    b._storage.ion({
      'change': 'onDataChange',
      'thisObj': b
    });
  }
  get storage() {
    if (!this._storage) {
      this.storage = {};
    }
    return this._storage;
  }
  get allRecords() {
    let a;
    const b = this;
    if (((a = b._allRecords) == null ? void 0 : a.generation) !== b.storage.generation) {
      if (b.isTree) {
        const c = b.collectDescendants(b.rootNode, void 0, void 0, { 'unfiltered': true }).all;
        if (b.rootVisible) {
          c.unshift(b.rootNode);
        }
        b._allRecords = c;
      } else {
        b._allRecords = b.isGrouped ? b.collectGroupRecords(true, true) : b.storage.allValues;
      }
      b._allRecords.generation = b.storage.generation;
    }
    return b._allRecords;
  }
  getAllDataRecords(a) {
    const b = this;
    if (b.tree) {
      return a ? b.allRecords : b.rootNode.allChildren;
    }
    return b.isGrouped ? b.collectGroupRecords(a, false) : (a ? b.storage.allValues : b.storage.values).filter(c => c);
  }
  onIsCreatingToggle(a, b) {
    const c = this, e = a.isPersistable && !b;
    c.added[e ? 'add' : 'remove'](a);
    if (e) {
      c.trigger('addConfirmed', { 'record': a });
      if (c.autoCommit) {
        c.doAutoCommit();
      }
    }
  }
  joinRecordsToStore(a) {
    const {totalCount: b} = this;
    for (let c = 0; c < a.length; c++) {
      const e = a[c];
      e.setData('parentIndex', b + c - a.length);
      e.joinStore(this);
    }
  }
  onDataChange({
    source: a,
    action: b,
    added: c,
    removed: e,
    replaced: f,
    oldCount: g,
    items: j,
    from: k,
    to: l
  }) {
    const n = this, o = dataAddRemoveActions[b], p = o && (c == null ? void 0 : c.length), q = o && (e == null ? void 0 : e.length);
    let r, u;
    n._idMap = null;
    if (p) {
      n.joinRecordsToStore(c);
    }
    f == null ? void 0 : f.forEach(([v, w]) => {
      v.unjoinStore(n, true);
      w.joinStore(n);
    });
    super.onDataChange(...arguments);
    if (!n.isTree) {
      if (p) {
        for (const v of c) {
          if (n.removed.includes(v)) {
            n.removed.remove(v);
          } else if (!n.addingClean && !v.isLinked) {
            n.added.add(v);
          }
        }
        r = !n.remoteFilter && n.isFiltered && n.reapplyFilterOnAdd;
        if (r) {
          n.filter({ 'silent': true });
        }
        u = !n.remoteSort && n.isSorted && n.reapplySortersOnAdd;
        if (u) {
          n.sort(null, null, false, true);
        }
      }
      if (q) {
        for (const w of e) {
          w.cancelBatch();
          w.unjoinStore(n);
          if (n.added.includes(w)) {
            n.added.remove(w);
          } else if (!w._undoingInsertion && !w.isCreating && !w.isLinked) {
            n.removed.add(w);
          }
        }
        n.modified.remove(e);
        r = !n.remoteFilter && n.isFiltered;
        if (r) {
          n.filter({ 'silent': true });
        }
      }
    }
    switch (b) {
    case 'clear':
      n.relationCache = {};
      n.updateDependentStores('removeall');
      n.trigger('removeAll');
      n.trigger('change', { 'action': 'removeall' });
      break;
    case 'splice':
      if (p) {
        n.updateDependentStores('add', c);
        const z = c.reduce((C, E) => {
            const {previousIndex: F} = E.meta;
            if (F > -1 && F < C)
              C = F;
            return C;
          }, c[0].meta.previousIndex), A = a.indexOf(c[0], !a.autoFilter), B = {
            'records': c,
            'index': A
          };
        if (z > -1) {
          B.oldIndex = z;
        }
        n.trigger('add', B);
        n.trigger('change', Object.assign({ 'action': 'add' }, B));
        if (r) {
          n.triggerFilterEvent({
            'action': 'filter',
            'filters': n.filters,
            'oldCount': g,
            'records': n.storage.allValues
          });
        }
        if (u) {
          n.trigger('sort', {
            'action': 'sort',
            'sorters': n.sorters,
            'records': n.storage.allValues
          });
        }
      }
      if (e.length) {
        n.updateDependentStores('remove', e);
        n.trigger('remove', { 'records': e });
        n.trigger('change', {
          'action': 'remove',
          'records': e
        });
      }
      if (f.length) {
        n.updateDependentStores('replace', f);
        n.trigger('replace', {
          'records': f,
          'all': n.records.length === f.length
        });
        n.trigger('change', {
          'action': 'replace',
          'replaced': f,
          'all': n.records.length === f.length
        });
      }
      break;
    case 'filter':
      if (n.isGrouped || n.isSorted) {
        n.performSort(true);
      }
      break;
    case 'move': {
        const C = Math.min(k, l), E = Math.min(n.storage.allValues.length - 1, Math.max(k, l));
        for (let F = n.storage.allValues, G = C; G <= E; G++) {
          F[G].setData('parentIndex', G);
        }
        n.trigger('move', {
          'records': j,
          'from': k,
          'to': l
        });
        if (n.isFiltered) {
          n.performFilter();
        }
        n.trigger('change', {
          'action': b,
          'record': j[0],
          'records': j,
          'from': k,
          'to': l
        });
        break;
      }
    }
  }
  onDataReplaced(a, b) {
    let c, e;
    const f = this, {storage: g} = f, j = g.allValues, k = Boolean(f.sorters.length > 0);
    for (let n = 0; n < j.length; n++) {
      (c = j[n]) == null ? void 0 : c.joinStore(f);
    }
    if (!f.remoteFilter && f.isFiltered) {
      f.filter({ 'silent': true });
    }
    if (f.remoteSort) {
      if (f.isGrouped) {
        g.replaceValues({
          ...f.prepareGroupRecords(),
          'silent': true
        });
      }
    } else {
      if (f.isGrouped) {
        f.group(null, null, false, !k, true);
      }
      if (k) {
        f.sort(null, null, false, true);
      }
    }
    if (!f.useRawData.disableDuplicateIdCheck) {
      const {idMap: o} = f;
      if (Object.keys(o).length < g.values.length) {
        const p = [];
        g.values.forEach(q => o[q.id] ? delete o[q.id] : p.push(q));
        throw new Error('Id collision on ' + p.map(q => q.id));
      }
    }
    const l = {
      'action': a,
      'data': b,
      'records': g.values
    };
    f.updateDependentStores(a, l.records);
    (e = f.afterLoadData) == null ? void 0 : e.call(f);
    if (!f.isRemoteDataLoading) {
      f.trigger('refresh', l);
    }
    f.trigger('change', l);
  }
  onModelChange(a, b, c, e, f) {
    const g = this, i = {
        'record': a,
        'records': [a],
        'changes': c,
        'batch': a.batching != null,
        'fromRelationUpdate': f
      };
    g.storage.onItemMutation(a, c);
    if ('id' in c) {
      const {
        oldValue: k,
        value: l
      } = b.id;
      g.updateDependentRecordIds(k, l);
      g.onRecordIdChange({
        'record': a,
        'oldValue': k,
        'value': l
      });
    } else if (!a.meta._ignoreRelatedIdUpdate) {
      g.updateDependentStores('update', [a], e);
    }
    const j = a.ignoreBag || a.isLinked ? false : g.updateModifiedBagForRecord(a);
    g.onUpdateRecord(a, c);
    if (!e) {
      if ('id' in c) {
        const {
          oldValue: n,
          value: o
        } = b.id;
        g.trigger('idChange', {
          'store': g,
          'record': a,
          'oldValue': n,
          'value': o
        });
      }
      g.trigger('update', i);
      g.trigger('change', Object.assign({ 'action': 'update' }, i));
    }
    if (g.autoCommit && j) {
      g.doAutoCommit();
    }
  }
  updateModifiedBagForRecord(a) {
    const {
      modified: b,
      added: c
    } = this;
    if (a.isModified && !a.isRoot && a.isPartOfStore(this)) {
      if (!b.includes(a) && !c.includes(a)) {
        if (a.isPhantom) {
          c.add(a);
        } else {
          b.add(a);
        }
      }
      return true;
    }
    b.remove(a);
    return false;
  }
  get idMap() {
    const a = this, b = !a._idMap, c = a._idMap || (a._idMap = {});
    if (b) {
      const e = a.storage.values;
      for (let f, g = 0, i = 0; g < e.length; g++) {
        f = e[g];
        if (f) {
          c[f.id] = {
            'index': g,
            'visibleIndex': i,
            'record': f
          };
        }
        if (!(f == null ? void 0 : f.isSpecialRow)) {
          i++;
        }
      }
      if (a.isFiltered) {
        for (let j = 0, k = a.storage._values.length; j < k; j++) {
          const n = a.storage._values[j];
          if (n.id in c) {
            c[n.id].unfilteredIndex = j;
          } else {
            c[n.id] = {
              'index': -1,
              'unfilteredIndex': j,
              'record': n
            };
          }
        }
      }
    }
    return c;
  }
  changeModelClass(a) {
    const {fields: b} = this;
    this.originalModelClass = a;
    let c = a;
    if (b == null ? void 0 : b.length) {
      class e extends a {
        static get fields() {
          return b;
        }
      }
      c = e;
    } else if (!this.preventSubClassingModel) {
      class f extends a {
      }
      c = f;
    }
    c.initClass();
    return c;
  }
  changeLazyLoad(a) {
    const b = this;
    let c = b.constructor.$lazyLoadPluginClass;
    if (b.isTree) {
      if (!b.treeLazyLoadClass) {
        b.treeLazyLoadClass = class f extends TreeStoreLazyLoadPlugin_default(c) {
        };
      }
      c = b.treeLazyLoadClass;
    }
    let e = b.getPlugin(c);
    if (a) {
      const g = ObjectHelper.isObject(a);
      if (!e) {
        e = b.addPlugin(c, g ? a : null);
      } else if (g) {
        ObjectHelper.assign(e, a);
      }
      return e;
    } else {
      e == null ? void 0 : e.destroy();
      return false;
    }
  }
  changeId(a, b) {
    return super.changeId(a !== true && a, b);
  }
  updateId(a, b) {
    const c = _Store.getById(a);
    c && _Store.unregisterInstance(c);
    super.updateId(a, b);
  }
  generateAutoId() {
    return _Store.generateId('store-');
  }
  get tree() {
    return this._tree;
  }
  set tree(a) {
    this._tree = a;
    if (a && !this.rootNode) {
      this.rootNode = this.buildRootNode();
      this.rootNode.isAutoRoot = true;
    }
  }
  buildRootNode() {
    return {};
  }
  static getStore(a, b) {
    if (a instanceof _Store) {
      return a;
    }
    if (this.getById(a)) {
      return this.getById(a);
    }
    if (Array.isArray(a)) {
      let c;
      const e = a.map(f => {
        if (f instanceof Model) {
          c = f.constructor;
        } else if (typeof f === 'string') {
          f = { 'text': f };
        } else {
        }
        return f;
      });
      if (!c) {
        class f extends Model {
        }
        c = f;
      }
      a = {
        'autoCreated': true,
        'data': e,
        'modelClass': c,
        'allowNoId': true
      };
      if (!b) {
        b = _Store;
      }
    }
    if (b) {
      return new b(a);
    }
  }
  static get stores() {
    return _Store.registeredInstances;
  }
  get rootNode() {
    return this.masterStore ? this.masterStore.rootNode : this._rootNode;
  }
  set rootNode(a) {
    let b;
    const c = this, e = c._rootNode;
    if (a === e) {
      return;
    }
    if (e) {
      c.clear(true);
      c.removed.clear();
    }
    if (a instanceof Model) {
      a.instanceMeta(c).collapsed = false;
      c._rootNode = a;
    } else {
      c._rootNode = a = new c.modelClass({
        'expanded': true,
        [c.modelClass.idField]: c.id + '-rootNode',
        ...a
      }, c, { 'isRoot': true }, true);
    }
    c._tree = true;
    a.isRoot = true;
    a.joinStore(c);
    if (((b = a.children) == null ? void 0 : b.length) || c.rootVisible) {
      a.isLoading = true;
      c.onNodeAddChild(a, a.children || [], 0);
      a.isLoading = false;
    }
    c.trigger('rootChange', {
      'oldRoot': e,
      'rootNode': a
    });
  }
  set data(a) {
    this.setStoreData(a);
  }
  get data() {
    return this.getAllDataRecords(true).map(a => a.data);
  }
  setStoreData(a) {
    const b = this, {
        idField: c,
        childrenField: e
      } = b.modelClass;
    if ((a == null ? void 0 : a.length) && !Object.isExtensible(a[0]) && (b.transformFlatData || b.useRawData.enabled)) {
      if (b.transformFlatData) {
        b.useRawData = b.useRawData || {
          'disableDuplicateIdCheck': false,
          'disableDefaultValue': false,
          'disableTypeConversion': false
        };
      }
      a = ObjectHelper.clone(a);
    }
    b.getConfig('plugins');
    b.processConfiguredListeners();
    if (a && !Array.isArray(a)) {
      a = ObjectHelper.transformNamedObjectToArray(a, c);
    }
    b.tree = !b.isChained && (b.tree || Boolean(b.autoTree && (a == null ? void 0 : a.some(f => f[e]))));
    if (a && (b.remoteSort || b.remoteFilter)) {
      for (let f = 0; f < a.length; f++) {
        a[f]._remoteSortIndex = f;
      }
    }
    if (!b.syncDataOnLoad || !b._data) {
      b._data = a;
      if (b.tree) {
        b.loadTreeData(a);
      } else {
        b.loadData(a);
      }
      b.added.clear();
      b.removed.clear();
      b.modified.clear();
    } else {
      b.syncDataset(a);
    }
  }
  loadTreeData(a) {
    const b = this, c = b.rootNode;
    if (b.transformFlatData) {
      a = b.treeifyFlatData(a);
    }
    c.isLoading = true;
    b.clear(true);
    c.appendChild(a);
    b.updateDependentStores('dataset', [c]);
    c.isLoading = false;
    if (a.length === 0) {
      const e = {
        'action': 'dataset',
        'data': [],
        'records': []
      };
      b.trigger('refresh', e);
      b.trigger('change', e);
    } else if (b.isFiltered) {
      b.filter();
    }
  }
  loadData(a, b = 'dataset') {
    let c;
    const e = this, {
        storage: f,
        allowNoId: g
      } = e, j = e.modelClass.fieldMap.id.dataSource, k = e.find(n => n.isCreating);
    if (k) {
      f.values.splice(e.records.indexOf(k), 1);
    }
    let l = e.verifyNoGeneratedIds;
    e.removeHeadersAndFooters(e.storage.values);
    e._idMap = null;
    e.oldIdMap = {};
    if (a) {
      const n = !(a[0] instanceof Model), o = a.length, p = new Array(o);
      if (n) {
        e.modelClass.exposeProperties(a[0]);
      }
      for (let q = 0; q < o; q++) {
        let r = a[q];
        if (n) {
          const u = r[j];
          if (!g && u == null) {
            throw new Error('Id required but not found on row ' + q);
          }
          if (l && ((c = u == null ? void 0 : u.startsWith) == null ? void 0 : c.call(u, '_generated'))) {
            console.warn('Generated id found in data: ' + u + '. Generated ids are temporary and should be replaced with real ids by the backend');
            l = false;
          }
          r = e.createRecord(r, true);
        }
        if (!e.isFillingFromMaster) {
          r = e.processRecord(r, true);
          r.setData('parentIndex', q);
        }
        p[q] = r;
      }
      e.clear(true);
      f.replaceValues({
        'values': p,
        'isNewDataset': true,
        'silent': true
      });
      if (k && !f.values.includes(k)) {
        f.values.push(k);
      }
      e._data = a;
      e.onDataReplaced(b, a);
    } else {
      e.clear(true);
      e._data = null;
    }
    e.isSyncingDataOnLoad = false;
  }
  getRange(a, b, c = true) {
    return (c ? this.storage.allValues : this.storage.values).slice(a, b);
  }
  createRecord(a, b = false, c = false) {
    return new this._modelClass(a, this, null, b, false, c);
  }
  processRecord(a, b = false) {
    return a;
  }
  refreshData() {
    this.filter();
    this.sort();
  }
  onRecordIdChange({
    record: a,
    oldValue: b,
    value: c
  }) {
    const e = this, f = e._idMap, {
        idRegister: g,
        oldIdMap: i
      } = e;
    e.storage._indicesInvalid = true;
    i[b] = a;
    if (f && !f[c]) {
      const j = f[b];
      delete f[b];
      f[c] = j;
    }
    e.added.changeId(b, c);
    e.removed.changeId(b, c);
    e.modified.changeId(b, c);
    delete g[b];
    g[c] = a;
    a.index = e.storage.indexOf(a);
  }
  onUpdateRecord(a, b) {
    let c;
    const e = this, {internalId: f} = b, {
        internalIdRegister: g,
        reapplyFilterOnUpdate: i
      } = e;
    if (f) {
      this.storage._indicesInvalid = true;
      delete g[f.oldValue];
      g[f.value] = a;
    }
    if ((i === true || ((c = i.fields) == null ? void 0 : c.some(j => j in b))) && e.isFiltered) {
      e.filter();
    }
  }
  get useRawData() {
    let a;
    if (this._useRawData === useRawDataUnset) {
      if (this.readUrl || ((a = this.crudManager) == null ? void 0 : a.loadUrl)) {
        return useRawDataRemoteDefaults;
      }
      return { 'enabled': false };
    }
    return this._useRawData;
  }
  changeUseRawData(a) {
    if (a === useRawDataUnset) {
      return useRawDataUnset;
    }
    if (a === true) {
      return useRawDataDefaults;
    }
    return a ? Object.assign(a, { 'enabled': true }) : { 'enabled': false };
  }
  getCount(a = true) {
    let b, c, e;
    const f = this, {
        storage: g,
        groupRecords: i,
        isFiltered: j,
        isGrouped: k,
        isTree: l,
        rootNode: n
      } = f;
    if (typeof a === 'boolean') {
      VersionHelper.deprecate('core', '7.0.0', 'getCount(true/false) deprecated in favor of getCount(options)');
      return a ? f.count : g.totalCount - ((i == null ? void 0 : i.count) || 0);
    }
    if (a.all) {
      return this.allRecords.length;
    }
    if (!('visibleData' in a)) {
      a.visibleData = true;
    }
    let o = 0;
    const {
        visibleData: p,
        headersFooters: q,
        filteredOut: r,
        collapsed: u
      } = a, v = j ? (b = i == null ? void 0 : i.values.reduce((z, A) => z + (f.includes(A) ? 1 : 0), 0)) != null ? b : 0 : (c = i == null ? void 0 : i.count) != null ? c : 0, w = g.count - v;
    if (p) {
      o += w;
    }
    if (q && k) {
      if (j && r) {
        o += (e = i.count) != null ? e : 0;
      } else {
        o += v;
      }
    }
    if (r && j) {
      if (l) {
        o += n.getDescendantCount(false, f, true) - n.getDescendantCount(false, f, false);
      } else {
        o += g.allValues.length - g.values.length;
      }
    }
    if (u) {
      if (k) {
        let z = 0;
        for (const A of f.collapsedGroups) {
          z += i.get(A).groupChildren.length;
        }
        o += z;
      } else if (l) {
        o += n.descendantCount - w;
      }
    }
    return o;
  }
  get count() {
    return this.storage.count;
  }
  get originalCount() {
    let a;
    VersionHelper.deprecate('core', '7.0.0', 'originalCount was deprecated in favor of getCount()');
    return this.storage.totalCount - (((a = this.groupRecords) == null ? void 0 : a.count) || 0);
  }
  get allCount() {
    VersionHelper.deprecate('core', '7.0.0', 'allCount was deprecated in favor of totalCount');
    return this.isTree ? this.rootNode.descendantCount : this.storage.totalCount;
  }
  get totalCount() {
    return this.getCount({ 'all': true });
  }
  get records() {
    return this.storage.values;
  }
  get first() {
    return this.storage.values[0];
  }
  get last() {
    return this.storage.values[this.storage.values.length - 1];
  }
  getAt(a, b = false) {
    return this.storage.getAt(a, b);
  }
  register(a) {
    const b = this;
    if (!b.useRawData.disableDuplicateIdCheck) {
      const c = b.isTree && b.idRegister[a.id];
      if (c && c !== a) {
        throw new Error('Id collision on ' + a.id);
      }
    }
    b.idRegister[a.id] = a;
    b.internalIdRegister[a.internalId] = a;
  }
  unregister(a) {
    delete this.idRegister[a.id];
    delete this.internalIdRegister[a.internalId];
  }
  get registeredRecords() {
    return Object.values(this.idRegister);
  }
  getById(a) {
    if (a == null ? void 0 : a.isModel) {
      const b = a;
      if (b.hasLinks && !this.storage.allValues.includes(b)) {
        return b.$links.find(c => this.storage.allValues.includes(c));
      }
      return b;
    }
    return this.idRegister[a];
  }
  isAvailable(a) {
    const b = this.getById(a);
    return b && this.storage.includes(b) || false;
  }
  getByInternalId(a) {
    return this.internalIdRegister[a];
  }
  includes(a) {
    if (this.isTree) {
      return this.idRegister[Model.asId(a)] != null;
    }
    return this.indexOf(a) > -1;
  }
  indexOf(a, b = false, c = false) {
    let e;
    if ((a == null ? void 0 : a.isModel) && !((e = a.stores) == null ? void 0 : e.includes(this.$store || this))) {
      const i = a.$links.find(j => this.storage.allValues.includes(j));
      if (i) {
        return this.indexOf(i, b);
      }
      return -1;
    }
    if (this.isTree) {
      return (c ? this.rootNode.allChildren : this.storage.values).indexOf(this.getById(a));
    }
    const f = Model.asId(a);
    if (f == null) {
      return -1;
    }
    const g = this.idMap[f];
    return g ? g[b ? 'visibleIndex' : 'index'] : -1;
  }
  allIndexOf(a) {
    if (this.isTree) {
      return this.allRecords.indexOf(this.getById(a));
    } else {
      return this.storage.indexOf(a, true);
    }
  }
  getDistinctValues(a, b = false) {
    const c = this, e = [], f = {};
    let g;
    c.forEach(i => {
      if (!i.isSpecialRow && !i.isRoot) {
        g = i.getValue(a);
        const j = g instanceof Date ? g.getTime() : g;
        if (!f[j]) {
          e.push(g);
          f[j] = 1;
        }
      }
    }, c, {
      'includeCollapsedGroupRecords': true,
      'includeFilteredOutRecords': b
    });
    return e;
  }
  getValueCount(a, b) {
    let c = 0;
    this.forEach(e => {
      if (ObjectHelper.isEqual(e.getValue(a), b))
        c++;
    });
    return c;
  }
  set json(a) {
    if (typeof a === 'string') {
      a = StringHelper.safeJsonParse(a);
    }
    this.data = a;
  }
  get json() {
    return StringHelper.safeJsonStringify(this);
  }
  get formattedJSON() {
    return StringHelper.safeJsonStringify(this, null, 4);
  }
  toJSON() {
    const a = this, {
        persistable: b,
        dataSource: c
      } = a.modelClass.getFieldDefinition('expanded');
    return (a.isTree ? a.rootNode.unfilteredChildren || a.rootNode.children || [] : a.allRecords).map(e => {
      const f = e.toJSON();
      if (a.tree && e.isParent && b) {
        f[c] = e.isExpanded(a);
      }
      return f;
    });
  }
  preProcessCurrentConfigs(a) {
    super.preProcessCurrentConfigs(a);
    delete a.project;
  }
  getInlineData(a) {
    let b;
    const c = [];
    if (this.tree) {
      (b = this.rootNode.children) == null ? void 0 : b.forEach(e => c.push(e.getCurrentConfig(a)));
    } else {
      this.forEach(e => c.push(e.getCurrentConfig(a)));
    }
    return c;
  }
  getCurrentConfig(a) {
    let b;
    const c = super.getCurrentConfig(a), {state: e} = this;
    if (c) {
      if (c.data) {
        c.data = this.getInlineData(a);
      }
      delete c.project;
      delete c.stm;
      delete c.asyncEvents;
      if (((b = c.modelClass) == null ? void 0 : b.$meta.hierarchy[c.modelClass.$meta.hierarchy.length - 2]) === this.constructor.defaultConfig.modelClass) {
        delete c.modelClass;
      }
      if (!this.tree) {
        delete c.tree;
      }
      if (e) {
        Object.assign(c, e);
      }
    }
    return c;
  }
  forEach(a, b = this, c) {
    const e = this, f = (g, k) => {
        if (g && !g.isRoot && !g.isSpecialRow) {
          return a.call(b, g, k);
        }
      };
    c = fixTraverseOptions2(e, c);
    if (e.isTree) {
      if (e.isChained) {
        c = {
          ...c,
          'sorterFn': e.sorterFn
        };
      }
      e.rootNode.traverseWhile(f, false, c);
    } else {
      const g = c.includeFilteredOutRecords ? e.storage.allValues : e.storage.values;
      if (e.isGrouped && c.includeCollapsedGroupRecords) {
        for (let k = 0; k < g.length; k++) {
          const l = g[k], n = c.includeFilteredOutRecords ? l.unfilteredGroupChildren : l.groupChildren;
          if (n && l.meta.collapsed === true) {
            for (let o = 0; o < n.length; o++) {
              const p = n[o];
              if (f(p, o) === false) {
                return;
              }
            }
          } else if (f(l, k) === false) {
            return;
          }
        }
      } else {
        for (let q = 0; q < g.length; q++) {
          if (f(g[q], q) === false) {
            return;
          }
        }
      }
    }
  }
  map(a, b = this) {
    return this.storage.values.map(a, b);
  }
  flatMap(a, b = this) {
    return this.storage.values.flatMap(a, b);
  }
  every(a, b = this, c) {
    return this.storage[c ? 'allValues' : 'values'].every(a, b);
  }
  reduce(a, b = [], c = this) {
    if (c !== this) {
      a = a.bind(c);
    }
    return this.storage.values.reduce(a, b, c);
  }
  [Symbol.iterator]() {
    return this.storage.values[Symbol.iterator]();
  }
  traverse(a, b = this.rootNode, c = b === this.rootNode, e) {
    const f = this;
    e = fixTraverseOptions2(f, e);
    if (f.isTree) {
      if (typeof b === 'boolean') {
        c = b;
        b = f.rootNode;
      }
      if (f.isChained) {
        const g = a;
        a = i => {
          if (f.chainedFilterFn(i)) {
            g(i);
          }
        };
      }
      b.traverse(a, c, e);
    } else {
      f.forEach(i => i.traverse(a, false, e), f, e);
    }
  }
  traverseWhile(a, b = this.rootNode, c = b === this.rootNode, e) {
    const f = this;
    e = fixTraverseOptions2(f, e);
    if (f.isTree) {
      if (typeof b === 'boolean') {
        c = b;
        b = f.rootNode;
      }
      if (f.isChained) {
        const g = a;
        a = i => {
          if (f.chainedFilterFn(i)) {
            g(i);
          }
        };
      }
      b.traverseWhile(a, c, e);
    } else {
      for (const i of f.storage) {
        if (i.traverse(a, false, e) === false) {
          break;
        }
      }
    }
  }
  getNext(a, b = false, c = false) {
    const e = this, f = e.storage.values;
    let g = e.indexOf(a);
    if (g >= f.length - 1) {
      if (b) {
        g = -1;
      } else {
        return null;
      }
    }
    const i = f[g + 1];
    if (c && i && (i.isSpecialRow || typeof c === 'function' && c(i))) {
      return e.getNext(f[g + 1], b, c);
    }
    return i;
  }
  getPrev(a, b = false, c = false) {
    const e = this, f = e.storage.values;
    let g = e.indexOf(a);
    if (g === 0) {
      if (b) {
        g = f.length;
      } else {
        return null;
      }
    }
    const i = f[g - 1];
    if (g > 0 && c && i && (i.isSpecialRow || typeof c === 'function' && c(i))) {
      return e.getPrev(f[g - 1], b, c);
    }
    return i;
  }
  getAdjacent(a, b = true, c = false, e = false) {
    return b ? this.getNext(a, c, e) : this.getPrev(a, c, e);
  }
  getNextLeaf(a, b = false) {
    const c = this, e = c.leaves, f = c.getById(a);
    let g = e.indexOf(f);
    if (g >= e.length - 1) {
      if (b) {
        g = -1;
      } else {
        return null;
      }
    }
    return e[g + 1];
  }
  getPrevLeaf(a, b = false) {
    const c = this, e = c.leaves, f = c.getById(a);
    let g = e.indexOf(f);
    if (g === 0) {
      if (b) {
        g = e.length;
      } else {
        return null;
      }
    }
    return e[g - 1];
  }
  getAdjacentLeaf(a, b = true, c = false) {
    return b ? this.getNextLeaf(a, c) : this.getPrevLeaf(a, c);
  }
};
const Store = _Store;
Store.initClass();
Store._$name = 'Store';
const AjaxStoreLazyLoad = class extends StoreLazyLoadPlugin {
  static $name = 'AjaxStoreLazyLoadPlugin';
  static get pluginConfig() {
    const a = {};
    for (const b in super.pluginConfig) {
      a[b] = [...super.pluginConfig[b]];
    }
    a.assign.push('updateResponseTotalProperty', 'updateResponseDataProperty', 'beforePerformSort', 'beforePerformFilter');
    return a;
  }
  updateResponseTotalProperty(a) {
    this.totalCountProperty = a;
  }
  updateResponseDataProperty(a) {
    this.dataProperty = a;
  }
  async beforePerformSort() {
    this.clearLoaded();
  }
  async beforePerformFilter() {
    this.clearLoaded();
  }
  async requestData(a) {
    const {
        totalCountProperty: b,
        dataProperty: c
      } = this.lazyLoad, e = await this.internalLoad(a, 'Chunk', void 0, false);
    return {
      [b]: e.json[b],
      [c]: e.data
    };
  }
  async load(a) {
    const {client: b} = this, c = b.params;
    if (a) {
      b.params = {
        ...a,
        ...c
      };
    }
    await super.load();
    if (a) {
      b.params = c;
    }
  }
};
AjaxStoreLazyLoad._$name = 'AjaxStoreLazyLoad';
const processParamEntry = (a, b) => {
  if (Array.isArray(b[1])) {
    b[1].forEach(c => a.push(encodeURIComponent(b[0]) + '=' + encodeURIComponent(c)));
  } else {
    a.push(encodeURIComponent(b[0]) + '=' + encodeURIComponent(b[1]));
  }
  return a;
};
const immediatePromise2 = Promise.resolve();
const prependZeros = a => {
  return String(a).padStart(2, '0');
};
const dateToString = a => {
  return a.getFullYear() + '-' + prependZeros(a.getMonth() + 1) + '-' + prependZeros(a.getDate()) + 'T' + prependZeros(a.getHours()) + ':' + prependZeros(a.getMinutes()) + ':' + prependZeros(a.getSeconds()) + '.' + a.getMilliseconds();
};
const AjaxStore = class extends Store {
  static $lazyLoadPluginClass = AjaxStoreLazyLoad;
  static configurable = {
    'responseSuccessProperty': 'success',
    'responseDataProperty': 'data',
    'responseTotalProperty': 'total',
    'params': null,
    'pageSize': 50,
    'autoCommitTimeout': 10,
    'includeChildrenInRemoveRequest': true
  };
  static $name = 'AjaxStore';
  static get defaultConfig() {
    return {
      'headers': null,
      'fetchOptions': null,
      'sendAsFormData': null,
      'writeAllFields': null,
      'parentIdParamName': 'id',
      'filterParamName': null,
      'restfulFilter': false,
      'sortParamName': null,
      'pageParamName': null,
      'pageStartParamName': null,
      'pageSizeParamName': 'pageSize',
      'useRestfulMethods': null,
      'httpMethods': {
        'create': 'POST',
        'read': 'GET',
        'update': 'PUT',
        'delete': 'DELETE'
      },
      'paramsInBody': VersionHelper.checkVersion('core', '6.0', '>=')
    };
  }
  afterConstruct(a) {
    super.afterConstruct(a);
    if (this.autoLoad) {
      this.load().catch(() => {
      });
    }
  }
  get isLoading() {
    return super.isLoading || this._isLoading ? 2 : this.loadTriggerPromise ? 1 : false;
  }
  get isCommitting() {
    let a;
    return Boolean((a = this.commitPromise) != null ? a : this.autoCommitTimeoutPromise);
  }
  updatePageSize(a) {
    const b = this;
    if (!this.isConfiguring) {
      const c = b.currentPage = b.currentPage ? Math.min(b.currentPage, Math.floor((b.totalCount + a - 1) / a)) : 1;
      if (a) {
        b.loadPage(c);
      }
    }
  }
  set pageParamName(a) {
    if (this.tree) {
      throw new Error('Paging cannot be supported for tree stores');
    }
    if (this.pageStartParamName) {
      throw new Error('Configs pageStartParamName and pageParamName are mutually exclusive');
    }
    this._pageParamName = a;
  }
  get pageParamName() {
    return this._pageParamName;
  }
  set pageStartParamName(a) {
    if (this.tree) {
      throw new Error('Paging cannot be supported for tree stores');
    }
    if (this.pageParamName) {
      throw new Error('Configs pageParamName and pageStartParamName are mutually exclusive');
    }
    this._pageStartParamName = a;
  }
  get pageStartParamName() {
    return this._pageStartParamName;
  }
  get isPaged() {
    return Boolean(this.pageParamName || this.pageStartParamName);
  }
  get allCount() {
    VersionHelper.deprecate('core', '7.0.0', 'Deprecated in favor of totalCount');
    return 'remoteTotal' in this ? this.remoteTotal : super.allCount;
  }
  get totalCount() {
    return 'remoteTotal' in this ? this.remoteTotal : super.totalCount;
  }
  get lastPage() {
    if (this.isPaged) {
      return Math.floor((this.totalCount + this.pageSize - 1) / this.pageSize);
    }
  }
  buildQueryString(a, ...b) {
    const c = a.includes('?'), e = Object.entries(Object.assign({}, ...b)).reduce(processParamEntry, []).join('&');
    return e ? (c ? '&' : '?') + e : '';
  }
  async performSort(a) {
    let b;
    const c = this;
    if (c.remoteSort && !c.isRemoteDataLoading) {
      c.isRemoteDataLoading = true;
      if (await ((b = c.beforePerformSort) == null ? void 0 : b.call(c)) === false) {
        return;
      }
      const e = await c.load();
      c.afterPerformSort(a);
      c.isRemoteDataLoading = false;
      return e;
    } else {
      super.performSort(a);
    }
  }
  async performFilter(a) {
    let b, c;
    const e = this;
    if (e.filterBatching > 0) {
      e.needsFiltering = true;
      return;
    }
    if (e.remoteFilter) {
      (b = e.loadingPromise) == null ? void 0 : b.abort();
      e.isRemoteDataLoading = true;
      const f = e.count, {filters: g} = e;
      if (await ((c = e.beforePerformFilter) == null ? void 0 : c.call(e, {
          'filters': g,
          'oldCount': f
        })) === false) {
        return;
      }
      e.currentPage = 1;
      const i = await e.load(), j = a ? null : {
          'action': 'filter',
          'filters': g,
          'oldCount': f,
          'records': e.storage.values
        };
      e.afterPerformFilter(j);
      e.trigger('refresh', j);
      e.isRemoteDataLoading = false;
      return i;
    } else {
      super.performFilter(a);
    }
  }
  encodeFilterParams(a) {
    const b = [];
    for (const {
          property: c,
          operator: e,
          value: f,
          caseSensitive: g
        } of a) {
      b.push({
        'field': c,
        'operator': e,
        'value': f,
        'caseSensitive': g
      });
    }
    return JSON.stringify(b, function (i, j) {
      return i === '' ? j : this[i] instanceof Date ? dateToString(this[i]) : j;
    });
  }
  encodeSorterParams(a) {
    return JSON.stringify(a.filter(b => !b.sortFn).map(b => b));
  }
  buildReadUrl() {
    const {readUrl: a} = this;
    if (this.restfulFilter && this.filters.count) {
      const b = a.endsWith('/') ? a : a + '/';
      return b + this.filters.first.value;
    }
    return a;
  }
  internalLoad(a, b, c, e = 0) {
    a = ObjectHelper.assign({}, this.params, a);
    const f = this, g = f.buildReadUrl(), i = {
        'action': 'read' + b,
        'params': a,
        'url': g
      };
    if (!g) {
      throw new Error('No load url specified');
    }
    if (f.trigger('beforeLoad' + b, i) === false) {
      throw false;
    }
    f.loadArgs = [
      g,
      i,
      a,
      b,
      c
    ];
    if (e === false) {
      return new Promise((j, k) => f.sendLoadRequest(j, k));
    }
    return f.loadTriggerPromise || (f.loadTriggerPromise = new Promise((j, k) => {
      f.setTimeout({
        'delay': e,
        'fn': 'sendLoadRequest',
        'args': [
          j,
          k
        ],
        'cancelOutstanding': true
      });
    }));
  }
  async sendLoadRequest(a, b) {
    let c, e;
    const f = this, [g, i, j, k, l] = f.loadArgs;
    f.loadTriggerPromise = null;
    if (g) {
      f._isLoading = true;
      f.trigger('load' + k + 'Start', i);
      f.trigger('beforeRequest', i);
      if (f.filterParamName && f.isFiltered) {
        j[f.filterParamName] = f.encodeFilterParams(f.filters.values);
      }
      if (f.remoteSort && f.isSorted) {
        j[f.sortParamName] = f.encodeSorterParams(f.groupers ? f.groupers.concat(f.sorters) : f.sorters);
      }
      if (f.isPaged) {
        if (!(f.pageParamName in j || f.pageStartParamName in j)) {
          const o = Math.min(f.currentPage || 1, f.totalCount ? f.lastPage : Infinity);
          if (f.pageParamName) {
            j[f.pageParamName] = o;
          } else {
            j[f.pageStartParamName] = (o - 1) * f.pageSize;
          }
        }
        j[f.pageSizeParamName] = f.pageSize;
      }
      const n = {
        'headers': f.headers,
        'parseJson': true
      };
      if (f.useRestfulMethods) {
        n.method = f.httpMethods.read;
        if (i.body) {
          n.body = JSON.stringify(i.body);
        }
      }
      try {
        const p = f.paramsInBody && (((c = n.method) == null ? void 0 : c.toUpperCase()) === 'POST' || ((e = n.method) == null ? void 0 : e.toUpperCase()) === 'PUT') && n.body == null, q = i.url + (p ? '' : f.buildQueryString(i.url, j)), r = f.loadingPromise = AjaxHelper.get(q, ObjectHelper.assign(n, f.fetchOptions, p ? { 'body': JSON.stringify(j) } : null)), u = await r;
        if (f.isDestroyed) {
          return;
        }
        const v = u.parsedJson, w = Array.isArray(v), z = w || v && ObjectHelper.getPath(v, f.responseSuccessProperty) !== false, A = w ? null : ObjectHelper.getPath(v, f.responseTotalProperty);
        f.loadingPromise = null;
        f._isLoading = false;
        i.response = u;
        i.json = v;
        if (z) {
          if (A != null) {
            f.remoteTotal = parseInt(A, 10);
          }
          if (f.isPaged) {
            if (f.remoteTotal >= 0) {
              const B = f.pageParamName ? j[f.pageParamName] : j[f.pageStartParamName] / f.pageSize + 1;
              f.currentPage = Math.min(B, f.lastPage);
            } else {
              throw new Error('A paged store must receive its responseTotalProperty in each data packet');
            }
          }
          i.data = w ? v : ObjectHelper.getPath(v, f.responseDataProperty);
          await (l == null ? void 0 : l(i));
          !f.isDestroyed && f.trigger('load' + k, i);
          a(i);
        } else {
          Object.assign(i, {
            'exception': true,
            'exceptionType': 'server',
            'error': v == null ? void 0 : v.error
          });
          !f.isDestroyed && f.trigger('exception', i);
          b(i);
        }
        !f.isDestroyed && f.trigger('afterRequest', i);
      } catch (C) {
        f._isLoading = false;
        i.exception = true;
        if (C instanceof Response) {
          i.exceptionType = C.ok ? 'server' : 'network';
          i.response = C;
          i.error = C.error;
        } else {
          i.exceptionType = 'server';
          i.error = C;
        }
        !f.isDestroyed && f.trigger('exception', i);
        b(i);
        !f.isDestroyed && f.trigger('afterRequest', i);
      }
    }
  }
  async load(a) {
    const b = this;
    if (b.isPaged) {
      return b.loadPage(b.currentPage || 1, a);
    } else {
      return b.internalLoad(a, '', c => {
        b.data = c.data;
      });
    }
  }
  async loadChildren(a) {
    return this.readUrl ? this.internalLoad({ [this.parentIdParamName]: a.id }, 'Children', b => {
      b.parentRecord = a;
      if (a.children.length) {
        a.clearChildren(true);
      }
      a.data[a.constructor.childrenField] = b.data;
      a.processChildren(a.stores);
    }, false) : this.immediatePromise;
  }
  async loadPage(a, b) {
    if (this.totalCount) {
      a = Math.min(a, this.lastPage);
    }
    const c = this, e = c.pageParamName ? { [c.pageParamName]: a } : { [c.pageStartParamName]: (a - 1) * c.pageSize };
    e[c.pageSizeParamName] = c.pageSize;
    return c.internalLoad(ObjectHelper.assign(e, b), 'Page', f => {
      c.loadData(f.data, 'pageLoad');
    });
  }
  async nextPage(a) {
    const b = this;
    return b.isPaged && b.currentPage !== b.lastPage ? b.loadPage(b.currentPage + 1, a) : immediatePromise2;
  }
  async previousPage(a) {
    return this.isPaged && this.currentPage !== 1 ? this.loadPage(this.currentPage - 1, a) : immediatePromise2;
  }
  async commit() {
    this.clearTimeout('autoCommit');
    this.autoCommitTimeoutPromise = null;
    return this.queueCommit();
  }
  queueCommit() {
    const a = this;
    let b;
    if (a.commitPromise) {
      b = a.commitPromise.then(() => a.performCommit());
    } else {
      a._commitPromiseQueueLength = 0;
      b = a.performCommit();
    }
    a._commitPromiseQueueLength += 1;
    return a.commitPromise = b == null ? void 0 : b.finally(() => {
      a._commitPromiseQueueLength -= 1;
      if (a._commitPromiseQueueLength === 0) {
        a.commitPromise = null;
      }
    });
  }
  performCommit() {
    const a = this, {changes: b} = a, c = [];
    if (!b) {
      if (a.modified.count) {
        a.modified.forEach(e => e.internalClearChanges(true, false));
        a.modified.clear();
      }
      return immediatePromise2;
    }
    if (a.trigger('beforeCommit', { 'changes': b }) !== false) {
      [
        ...b.added,
        ...b.modified,
        ...b.removed
      ].forEach(g => g.meta.committing = true);
      a.trigger('commitStart', { 'changes': b });
      const e = {
        'action': 'commit',
        'exception': false,
        'changes': b
      };
      let f = a.commitRemoved(e);
      if (f) {
        c.push(f);
      }
      f = a.commitAdded(e);
      if (f) {
        c.push(f);
      }
      f = a.commitModified(e);
      if (f) {
        c.push(f);
      }
      if (!c.length) {
        a.modified.forEach(g => g.internalClearChanges(true, false));
        a.modified.clear();
        a.added.forEach(g => g.internalClearChanges(true, false));
        a.added.clear();
        a.removed.clear();
        a.trigger('commit', { 'changes': b });
        return immediatePromise2;
      }
      f = c.length === 1 ? c[0] : Promise.all(c);
      return new Promise((g, i) => {
        f.then(() => {
          if (e.exception) {
            a.trigger('exception', e);
            i(e);
          } else {
            a.trigger('commit', { 'changes': b });
            g(e);
          }
        }).catch(() => {
          i(e);
        });
      });
    } else {
      return immediatePromise2;
    }
  }
  doAutoCommit() {
    const a = this;
    if (a.suspendCount <= 0 && !a.hasTimeout('autoCommit')) {
      a.autoCommitTimeoutPromise = new Promise(b => {
        a.setTimeout({
          'name': 'autoCommit',
          'fn': () => {
            a.autoCommitTimeoutPromise = null;
            a.commit().then().catch(c => {
              const {response: e} = c;
              if (!(e && (e.ok && e.parsedJson || [
                  500,
                  404
                ].includes(e.status)))) {
                throw c;
              }
            });
            b();
          },
          'delay': a.autoCommitTimeout
        });
      });
    }
  }
  commitAdded(a) {
    const b = this, c = [...b.added], e = {
        'action': 'create',
        'params': b.params
      };
    return c.length && b.createUrl ? new Promise(f => {
      const g = c.map(l => l.persistableData);
      a.create = e;
      e.body = { 'data': g };
      b.trigger('beforeRequest', e);
      let i = e.body;
      if (b.sendAsFormData) {
        const l = new FormData();
        l.append('data', JSON.stringify(g));
        i = l;
      }
      const j = {
          'headers': b.headers,
          'parseJson': true
        }, k = b.createUrl + b.buildQueryString(b.createUrl, b.params);
      if (b.useRestfulMethods) {
        j.method = b.httpMethods.create;
      }
      AjaxHelper.post(k, i, ObjectHelper.assign(j, b.fetchOptions)).then(n => {
        const o = n.parsedJson, p = Array.isArray(o) || o && o.success !== false;
        a.response = n;
        e.json = o;
        e.response = n;
        if (p) {
          const q = b.processReturnedData(o, c, g);
          b.added.remove(q);
          b.modified.add(q.filter(r => r.meta.modified && !ObjectHelper.isEmpty(r.meta.modified)));
          b.trigger('commitAdded');
          b.trigger('refresh', e);
          f(a);
        } else {
          c.forEach(u => u.meta.committing = false);
          a.exception = e.exception = true;
          a.exceptionType = e.exceptionType = 'server';
          b.trigger('exception', e);
          f(a);
        }
        b.trigger('afterRequest', e);
      }).catch(n => {
        c.forEach(o => o.meta.committing = false);
        a.exception = e.exception = true;
        if (n instanceof Response) {
          a.response = n;
          e.exceptionType = n.ok ? 'server' : 'network';
          e.response = n;
          e.error = n.error;
        } else {
          e.exceptionType = 'server';
          e.error = n;
        }
        !b.isDestroyed && b.trigger('exception', e);
        f(a);
        !b.isDestroyed && b.trigger('afterRequest', e);
      });
    }) : null;
  }
  commitModified(a) {
    const b = this, c = [...b.changes.modified], e = {
        'action': 'update',
        'params': b.params
      }, f = c.length && b.updateUrl ? new Promise(g => {
        const i = c.map(l => {
          if (b.writeAllFields) {
            return l.persistableData;
          } else {
            return l.modificationDataToWrite;
          }
        }).filter(l => !ObjectHelper.isEmpty(l));
        if (i.length === 0) {
          b.modified.clear();
          c.forEach(l => l.meta.committing = false);
          g();
          return;
        }
        a.update = e;
        e.body = { 'data': i };
        b.trigger('beforeRequest', e);
        let j = e.body;
        if (b.sendAsFormData) {
          const l = new FormData();
          l.append('data', JSON.stringify(i));
          j = l;
        }
        const k = {
          'headers': b.headers,
          'parseJson': true
        };
        if (b.useRestfulMethods) {
          k.method = b.httpMethods.update;
        }
        AjaxHelper.post(b.updateUrl + b.buildQueryString(b.updateUrl, b.params), j, ObjectHelper.assign(k, b.fetchOptions)).then(n => {
          const o = n.parsedJson, p = Array.isArray(o) || o && o.success !== false;
          a.response = n;
          e.json = o;
          e.response = n;
          if (p) {
            b.processReturnedData(o, c, i, true, true);
            b.trigger('commitModified');
            b.trigger('refresh', e);
            g(a);
          } else {
            c.forEach(q => q.meta.committing = false);
            a.exception = e.exception = true;
            e.exceptionType = 'server';
            b.trigger('exception', e);
            g(a);
          }
          b.trigger('afterRequest', e);
        }).catch(n => {
          c.forEach(o => o.meta.committing = false);
          a.exception = e.exception = true;
          if (n instanceof Response) {
            a.response = n;
            e.exceptionType = n.ok ? 'server' : 'network';
            e.response = n;
            e.error = n.error;
          } else {
            e.exceptionType = 'server';
            e.error = n;
          }
          !b.isDestroyed && b.trigger('exception', e);
          g(a);
          !b.isDestroyed && b.trigger('afterRequest', e);
        });
      }) : null;
    if (!c.length && b.modified.count) {
      b.modified.clear();
    }
    return f;
  }
  processReturnedData(a, b, c, e = false) {
    if (!Array.isArray(a)) {
      a = ObjectHelper.getPath(a, this.responseDataProperty);
    }
    const {idField: f} = b[0].constructor, g = !a || a.length === b.length, i = [];
    b.forEach((j, k) => {
      const l = e ? a == null ? void 0 : a.find(n => n[f] === j.id) : a == null ? void 0 : a[k];
      if (g || l) {
        const n = j.modificationDataToWrite, o = e ? c.find(q => q.id === j.id) : c[k], p = {};
        j.meta.committing = false;
        if (l) {
          j.syncId(l[f]);
          Object.assign(j.data, this.modelClass.processData(l, e, this, j));
        }
        if (n) {
          for (const q in o) {
            if (n[q] === o[q]) {
              const r = j.getFieldDefinitionFromDataSource(q);
              if (r == null ? void 0 : r.persist) {
                p[r.name] = 1;
              }
            }
          }
        }
        j.internalClearChanges(true, true, p, true);
        i.push(j);
      }
    });
    return i;
  }
  commitRemoved(a) {
    const b = this, {
        removed: c,
        includeChildrenInRemoveRequest: e
      } = b, f = {
        'action': 'delete',
        'params': b.params
      };
    return c.count && b.deleteUrl ? new Promise(g => {
      let i, j;
      a.delete = f;
      let k;
      if (!e) {
        k = [];
        for (const o of c.values) {
          const p = (j = o.parentId) != null ? j : (i = o.meta.modified) == null ? void 0 : i.parentId;
          if (p != null && (p === b.rootNode.id || !c.includes(p))) {
            k.push(o);
          }
        }
      } else {
        k = c.values;
      }
      f.body = { 'ids': k.map(q => q.id) };
      b.trigger('beforeRequest', f);
      let l = f.body;
      if (b.sendAsFormData) {
        const q = new FormData();
        q.append('id', JSON.stringify(l.ids));
        l = q;
      }
      const n = {
        'headers': b.headers,
        'parseJson': true
      };
      if (b.useRestfulMethods) {
        n.method = b.httpMethods.delete;
      }
      AjaxHelper.post(b.deleteUrl + b.buildQueryString(b.deleteUrl, b.params), l, ObjectHelper.assign(n, b.fetchOptions)).then(r => {
        const u = r.parsedJson, v = Array.isArray(u), w = v || u && u.success !== false;
        a.response = r;
        f.json = u;
        f.response = r;
        if (w) {
          c.forEach(z => z.meta.committing = false);
          c.clear();
          b.trigger('commitRemoved');
          b.trigger('refresh', f);
          g(a);
        } else {
          c.forEach(z => z.meta.committing = false);
          a.exception = f.exception = true;
          f.exceptionType = 'server';
          b.trigger('exception', f);
          g(a);
        }
        b.trigger('afterRequest', f);
      }).catch(r => {
        c.forEach(u => u.meta.committing = false);
        a.exception = f.exception = true;
        if (r instanceof Response) {
          a.response = r;
          f.exceptionType = r.ok ? 'server' : 'network';
          f.response = r;
          f.error = r.error;
        } else {
          f.exceptionType = 'server';
          f.error = r;
        }
        !b.isDestroyed && b.trigger('exception', f);
        g(a);
        !b.isDestroyed && b.trigger('afterRequest', f);
      });
    }) : null;
  }
  get remoteFilter() {
    return Boolean(this.filterParamName || this.restfulFilter);
  }
  get remoteSort() {
    return Boolean(this.sortParamName);
  }
};
AjaxStore._$name = 'AjaxStore';
const NOT_PASSED = {};
const DomDataStore = class {
  static get(a, b = NOT_PASSED) {
    const c = a._domData || (a._domData = {});
    return b === NOT_PASSED ? c : c[b];
  }
  static remove(a, b) {
    const {_domData: c} = a, e = c == null ? void 0 : c[b];
    c == null ? true : delete c[b];
    return e;
  }
  static set(a, b, c = NOT_PASSED) {
    if (c === NOT_PASSED) {
      a._domData = c = b;
    } else {
      (a._domData || (a._domData = {}))[b] = c;
    }
    return c;
  }
  static assign(a, b) {
    Object.assign(a._domData || (a._domData = {}), b);
  }
};
DomDataStore._$name = 'DomDataStore';
const CSSHelper = class {
  static $name = 'CSSHelper';
  static insertRule(a, b = document.head) {
    const c = this.getStyleSheet(b), {cssRules: e} = c, f = e.length, g = typeof a === 'string';
    (g ? [a] : a).forEach((i, j) => c.insertRule(i, j));
    if (e.length > f) {
      return g ? e[0] : Array.from(e).slice(0, a.length);
    }
  }
  static findRule(a) {
    let b;
    const c = typeof a === 'function';
    Array.prototype.find.call(document.head.querySelectorAll('link[rel=stylesheet],style[type*=css]'), e => {
      b = Array.prototype.find.call(e.sheet.rules || e.sheet.cssRules, f => {
        return c ? a(f) : f.selectorText === a;
      });
      if (b) {
        return true;
      }
    });
    return b;
  }
  static getStyleSheet(a = document.head) {
    if (!a.$bryntumStylesheet) {
      a.$bryntumStylesheet = DomHelper.createElement({
        'tag': 'style',
        'id': 'bryntum-private-styles',
        'type': 'text/css',
        'parent': a
      }).sheet;
    }
    return a.$bryntumStylesheet;
  }
  static getCSSVersion() {
    return getComputedStyle(document.documentElement).getPropertyValue('--bryntum-version').replace(/[" ]/gm, '');
  }
};
Helpers_default.register(CSSHelper);
CSSHelper._$name = 'CSSHelper';
const arraySlice2 = Array.prototype.slice;
const emptyArray6 = Object.freeze([]);
const emptyObject6 = Object.freeze({});
const htmlRe2 = /[&<]/;
const {getPrototypeOf: getPrototypeOf2} = Object;
const {toString: toString4} = Object.prototype;
const {
  isEqual,
  isObject: isObject3
} = ObjectHelper;
const checkEqualityIgnore = {
  'parent': 1,
  'elementData': 1,
  'ns': 1,
  'syncOptions': 1
};
const makeCheckEqualityOptions = () => ({
  'ignore': checkEqualityIgnore,
  'refsFound': new Set()
});
const isClass = {
  'class': 1,
  'className': 1,
  'classname': 1
};
const simpleTypes = {
  'bigint': 1,
  'boolean': 1,
  'function': 1,
  'number': 1,
  'string': 1,
  'symbol': 1
};
const syncIgnoreAttributes = {
  'tag': 1,
  'html': 1,
  'text': 1,
  'children': 1,
  'tooltip': 1,
  'parent': 1,
  'nextSibling': 1,
  'ns': 1,
  'reference': 1,
  'elementData': 1,
  'retainElement': 1,
  'compareHtml': 1,
  'syncOptions': 1,
  'listeners': 1,
  'isReleased': 1,
  'null': 1,
  '': 1,
  'keepChildren': 1,
  'retainChildren': 1
};
const addAndCacheCls = (a, b) => {
  const c = 'className' in b ? 'className' : 'class', e = b[c];
  if (e) {
    if (typeof e === 'string') {
      const f = e.split(' ');
      if (!f.includes(a)) {
        f.push(a);
        b[c] = f.join(' ');
      }
    } else if (Array.isArray(e)) {
      if (!e.includes(a)) {
        e.push(a);
      }
    } else if (e.isDomClassList) {
      e.add(a);
    } else if (ObjectHelper.isObject(e)) {
      e[a] = 1;
    }
  }
};
const removeAndUncacheCls = (a, b) => {
  const c = 'className' in b ? 'className' : 'class', e = b[c];
  if (e) {
    if (typeof e === 'string') {
      const f = e.split(' ');
      if (f.includes(a)) {
        f.splice(f.indexOf(a), 1);
        b[c] = f.join(' ');
      }
    } else if (Array.isArray(e)) {
      if (e.includes(a)) {
        e.splice(e.indexOf(a), 1);
      }
    } else if (e.isDomClassList) {
      e.remove(a);
    } else if (ObjectHelper.isObject(e)) {
      delete e[a];
    }
  }
};
const DomSync = class _DomSync {
  static checkEquality(a, b, c, e) {
    let f;
    if (a === b) {
      return true;
    }
    if (a == null) {
      return b == null;
    }
    if (!a || !b) {
      return false;
    }
    const g = typeof a, j = typeof b;
    if (g !== j || simpleTypes[g]) {
      return false;
    }
    let k = c.equalityCache || (c.equalityCache = new Map()), l = Boolean(e), n, o, p, q, r, u, v;
    k = k.get(a) || k.set(a, new Map()).get(a);
    n = k.get(b);
    if (n === void 0) {
      n = true;
      if (getPrototypeOf2(a) !== getPrototypeOf2(b) || a instanceof Node) {
        n = false;
      } else if (Array.isArray(a)) {
        o = a.length;
        if (o !== b.length) {
          n = false;
        } else {
          while (o-- > 0) {
            if (!_DomSync.checkEquality(a[o], b[o], c, l)) {
              n = false;
              break;
            }
          }
        }
      } else {
        u = a.syncOptions;
        q = u == null ? void 0 : u.ignoreRefs;
        if (q) {
          l = true;
          e = q !== 'children';
        }
        p = c.ignore || emptyObject6;
        for (r in b) {
          if (!p[r] && !(r in a) && !(e && r === 'reference')) {
            n = false;
            break;
          }
        }
        if (n) {
          if (toString4.call(b) === '[object Date]') {
            n = a.getTime() === b.getTime();
          } else if (DomHelper.isReactElement(a) || DomHelper.isReactElement(b)) {
            n = false;
          } else {
            for (r in a) {
              if (!p[r] && !(e && r === 'reference')) {
                if (!(r in b)) {
                  n = false;
                  break;
                }
                v = a[r];
                if (!_DomSync.checkEquality(v, b[r], c, l)) {
                  n = false;
                  break;
                }
              }
            }
          }
        }
      }
      if (!e && isObject3(a) && a.reference) {
        (f = c.refsFound) == null ? void 0 : f.add(a.reference);
      }
      k.set(b, n);
    }
    return n;
  }
  static sync(a) {
    let b;
    const c = a, {refOwner: e} = a, f = e == null ? void 0 : e.byRef, g = makeCheckEqualityOptions();
    let j = a.affected, k, l, n, o;
    if (typeof j === 'string') {
      j = [j];
    }
    a = {
      ...a,
      'checkEqualityOptions': g
    };
    if (e) {
      e.byRef = {};
      if (j) {
        for (l in f) {
          if (!j.includes(l)) {
            e.byRef[l] = f[l];
          }
        }
      }
      a.refsWas = f;
    }
    c.changed = _DomSync.performSync(a, a.targetElement);
    if (e) {
      if (!j) {
        j = Object.keys(f);
      }
      for (k = 0; k < j.length; ++k) {
        l = j[k];
        n = f[l];
        if (g.refsFound.has(l) || n.retainElement) {
          e.byRef[l] = n;
        } else {
          o = n.lastDomConfig;
          (b = n.blur) == null ? void 0 : b.call(n);
          n.remove();
          e.detachRef(l, n, o);
        }
      }
    }
    return a.targetElement;
  }
  static performSync(a, b) {
    const {
        domConfig: c,
        callback: e
      } = a, {lastDomConfig: f} = b, g = a.configEquality || _DomSync.checkEquality;
    if (!g(c, f, a.checkEqualityOptions, a.ignoreRefs)) {
      if (c) {
        if (!c.onlyChildren) {
          _DomSync.syncAttributes(c, b, a);
          _DomSync.syncContent(c, b);
        }
        if (!c.keepChildren) {
          _DomSync.syncChildren(a, b);
        }
      } else {
        b.innerHTML = null;
        b.syncIdMap = null;
      }
      b.lastDomConfig = !((c == null ? void 0 : c.onlyChildren) && f) ? c : {
        ...f,
        'children': c.children
      };
      return true;
    } else {
      e == null ? void 0 : e({
        'action': 'none',
        'domConfig': c,
        'targetElement': b
      });
    }
    return false;
  }
  static syncDataset(a, b) {
    const {lastDomConfig: c} = b, e = a === c, f = Object.keys(a.dataset), g = c && c.dataset && Object.keys(c.dataset), j = ArrayHelper.delta(f, g);
    let k, l, n, o;
    for (l = 0; l < j.onlyInA.length; l++) {
      k = j.onlyInA[l];
      o = a.dataset[k];
      if (o != null) {
        b.setAttribute('data-' + StringHelper.hyphenate(k), o);
      }
    }
    for (l = 0; l < j.inBoth.length; l++) {
      k = j.inBoth[l];
      o = a.dataset[k];
      if (e || o != c.dataset[k]) {
        n = 'data-' + StringHelper.hyphenate(k);
        if (o == null) {
          b.removeAttribute(n);
        } else {
          b.setAttribute(n, o);
        }
      }
    }
    for (l = 0; l < j.onlyInB.length; l++) {
      b.removeAttribute('data-' + StringHelper.hyphenate(j.onlyInB[l]));
    }
  }
  static addCls(a, b) {
    const {lastDomConfig: c} = b;
    a = DomClassList.normalize(a, 'array');
    a.forEach(e => {
      b.classList.add(e);
      addAndCacheCls(e, c);
    });
  }
  static removeCls(a, b) {
    const {lastDomConfig: c} = b;
    a = DomClassList.normalize(a, 'array');
    a.forEach(e => {
      b.classList.remove(e);
      removeAndUncacheCls(e, c);
    });
  }
  static syncClassList(a, b, e) {
    let f = a.className || a.class, g = false, j, l, n, o, p, q;
    if (e) {
      l = DomClassList.normalize(b, 'array');
      f = DomClassList.normalize(f, 'object');
      q = DomClassList.normalize(e.className || e.class, 'object');
      p = [];
      for (n = 0, o = l.length; n < o; ++n) {
        j = l[n];
        if (f[j] || !(j in q)) {
          q[j] = 1;
          p.push(j);
        } else {
          g = true;
        }
      }
      for (j in f) {
        if (!q[j]) {
          p.push(j);
          g = true;
        }
      }
      if (!g) {
        return;
      }
      f = p.join(' ');
    } else {
      f = DomClassList.normalize(f);
    }
    b.setAttribute('class', f);
  }
  static getSyncAttributes(a) {
    const b = {}, c = [];
    if (a) {
      Object.keys(a).forEach(e => {
        if (!syncIgnoreAttributes[e]) {
          const f = e.toLowerCase();
          b[f] = a[e];
          c.push(f);
        }
      });
    }
    return {
      'attributes': b,
      'names': c
    };
  }
  static syncAttributes(a, b, c) {
    const {lastDomConfig: e} = b, f = a === e, g = _DomSync.getSyncAttributes(a), {
        attributes: j,
        names: k
      } = g, {
        attributes: l,
        names: n
      } = f ? g : _DomSync.getSyncAttributes(e), {
        onlyInA: o,
        onlyInB: p,
        inBoth: q
      } = f ? {
        'onlyInA': emptyArray6,
        'onlyInB': emptyArray6,
        'inBoth': k
      } : ArrayHelper.delta(k, n);
    let r, u, v = false;
    for (u = 0; u < o.length; u++) {
      r = o[u];
      const w = j[r];
      if (r === 'style' && w != null) {
        DomHelper.applyStyle(b, w, true);
      } else if (r === 'dataset') {
        _DomSync.syncDataset(a, b);
      } else if (isClass[r]) {
        _DomSync.syncClassList(a, b);
        v = true;
      } else if (w != null) {
        b.setAttribute(r, w);
      }
    }
    for (u = 0; u < q.length; u++) {
      r = q[u];
      const z = j[r], A = l[r];
      if (z == null) {
        b.removeAttribute(r);
      } else if (r === 'style') {
        if (c.strict) {
          if (f) {
            _DomSync.syncStyles(b, z);
          } else if (!isEqual(z, A, true)) {
            _DomSync.syncStyles(b, z, A);
          }
        } else if (f || !isEqual(z, A, true)) {
          DomHelper.applyStyle(b, z, true);
        }
      } else if (r === 'dataset') {
        _DomSync.syncDataset(a, b);
      } else if (isClass[r]) {
        _DomSync.syncClassList(a, b, c.strict && b.lastDomConfig);
        v = true;
      } else if (f || z !== A) {
        b.setAttribute(r, z);
      }
    }
    for (u = 0; u < p.length; u++) {
      r = p[u];
      if (!isClass[r] || !v) {
        b.removeAttribute(r);
      }
    }
  }
  static syncStyles(a, b, c) {
    let e, f, g;
    if (!c) {
      e = b;
    } else {
      e = {};
      b = DomHelper.parseStyle(b);
      c = DomHelper.parseStyle(c);
      if (b) {
        for (f in b) {
          g = b[f];
          if (c[f] !== g) {
            e[f] = g;
          }
        }
      }
      for (f in c) {
        if (!(f in b)) {
          e[f] = '';
        }
      }
    }
    DomHelper.applyStyle(a, e);
  }
  static syncContent(a, b) {
    let c;
    const {
        html: e,
        text: f
      } = a, g = f != null ? f : e;
    b.didSetTextContent = false;
    if (a.elementData) {
      b.elementData = a.elementData;
    }
    if (g instanceof DocumentFragment) {
      if (b.childNodes.length === 1 && DomHelper.getChildElementCount(b) === 0 && g.childNodes.length === 1 && DomHelper.getChildElementCount(g) === 0) {
        DomHelper.setInnerText(b, g.firstChild.data);
      } else {
        b.innerHTML = '';
        b.appendChild(g);
      }
    } else if (e != null && htmlRe2.test(e)) {
      b.innerHTML = String(e);
    } else if (g != null) {
      const {lastDomConfig: i} = b;
      if (((c = i == null ? void 0 : i.children) == null ? void 0 : c.length) || (i == null ? void 0 : i.html) && htmlRe2.test(b.lastDomConfig.html)) {
        b.innerHTML = '';
      }
      DomHelper.setInnerText(b, String(g));
      b.didSetTextContent = true;
    }
  }
  static insertTextNode(a, b, c, e, f = null) {
    const g = document.createTextNode(a);
    b.insertBefore(g, f);
    if (e) {
      g.$refOwnerId = e.id;
    }
    c == null ? void 0 : c({
      'action': 'newNode',
      'domConfig': a,
      'targetElement': g
    });
  }
  static insertElement(a, b, c, e, f, g, i) {
    let j;
    const k = i.ns ? document.createElementNS(i.ns, a.tag || 'svg') : document.createElement(a.tag || 'div');
    b.insertBefore(k, c);
    _DomSync.performSync(i, k);
    if (g != null) {
      f[g] = k;
    }
    if (!a.role && !a.tabIndex && !a['aria-hidden'] && !DomHelper.isFocusable(k, true) && !k.htmlFor) {
      k.setAttribute('role', 'presentation');
    }
    if (e) {
      k.$refOwnerId = e.id;
      if (g) {
        k.$reference = g;
        e.attachRef(g, k, a);
      }
    }
    (j = i.callback) == null ? void 0 : j.call(i, {
      'action': 'newElement',
      'domConfig': a,
      'targetElement': k,
      'syncId': g
    });
  }
  static syncChildren(a, b) {
    let c, e, f, g;
    let {
        domConfig: i,
        syncIdField: j,
        callback: k,
        releaseThreshold: l,
        configEquality: n,
        ns: o,
        refOwner: p,
        refsWas: q,
        strict: r,
        checkEqualityOptions: u,
        ignoreRefs: v
      } = a, w = null, z, A, B;
    const C = i.syncOptions || {};
    if ((c = i.html) != null ? c : i.text) {
      return;
    }
    v = Boolean((e = C.ignoreRefs) != null ? e : v);
    if (v) {
      p = q = null;
    }
    if ('strict' in C) {
      r = C.strict;
    }
    const E = p ? p.byRef : {}, F = arraySlice2.call(i.children || []), G = arraySlice2.call(b.childNodes), H = q || b.syncIdMap || {}, I = b.releasedIdMap || {}, J = L => {
        while (G.length && G[0].parentNode !== b) {
          G.shift();
        }
        return (L ? G.shift() : G[0]) || null;
      };
    j = C.syncIdField || j;
    r = C.strict || r;
    k = C.callback || k;
    n = C.configEquality || n;
    l = 'releaseThreshold' in C ? C.releaseThreshold : l;
    if (j) {
      b.syncIdMap = E;
    }
    const K = {
      'checkEqualityOptions': u || makeCheckEqualityOptions(),
      'ignoreRefs': v,
      'refOwner': p,
      'refsWas': q,
      'strict': r,
      'syncIdField': j,
      'releaseThreshold': l,
      'callback': k,
      'configEquality': n
    };
    while (F.length) {
      const L = F.shift();
      B = null;
      if (!L) {
        continue;
      }
      if (DomHelper.isReactElement(L)) {
        k({
          'jsx': L,
          'targetElement': b,
          'domConfig': i
        });
        continue;
      }
      if (Array.isArray(L)) {
        this.syncChildren({
          ...a,
          'domConfig': {
            ...a.domConfig,
            'children': L
          }
        }, b);
        continue;
      }
      if (L instanceof Node) {
        A = J();
        if (L !== A) {
          b.insertBefore(L, A);
        }
        z = G.indexOf(L);
        if (z > -1) {
          G.splice(z, 1);
        }
        continue;
      }
      const N = typeof L === 'string';
      K.domConfig = L;
      K.ns = L.ns || o;
      if (!N) {
        if (p) {
          B = L.reference;
        } else if (j && L.dataset) {
          B = L.dataset[j];
        }
        if (B != null && !L.unmatched) {
          const R = H[B] || I[B];
          if (R) {
            const {lastDomConfig: S} = R;
            if (R.retainElement) {
            } else if (_DomSync.performSync(K, R)) {
              k == null ? void 0 : k({
                'action': 'reuseOwnElement',
                'domConfig': L,
                'targetElement': R,
                'lastDomConfig': S,
                'syncId': B
              });
            }
            if (L.retainElement) {
              R.retainElement = true;
            }
            E[B] = R;
            ArrayHelper.remove(G, R);
            delete I[B];
            R.isReleased = false;
            A = J();
            if (R.parentNode !== b || r && R.nextSibling !== A) {
              b.insertBefore(R, A);
            }
          } else if (r) {
            _DomSync.insertElement(L, b, J(), p, E, B, K);
          } else {
            F.push(L);
            L.unmatched = true;
          }
          continue;
        }
        if (L.unmatched) {
          delete L.unmatched;
        }
      }
      let O = null, P = null, Q;
      while (!P && (Q = J(true))) {
        if (p) {
          if (Q.$refOwnerId !== p.id) {
            continue;
          }
          if (Q.$reference) {
            if (!r) {
              continue;
            }
            O = Q;
            break;
          }
          P = Q;
        } else if (!Q.retainElement) {
          P = Q;
        }
        if (!P) {
          (w || (w = [])).push(Q);
        }
      }
      if (O || !P) {
        if (N) {
          _DomSync.insertTextNode(L, b, k, p, O);
        } else {
          _DomSync.insertElement(L, b, O, p, E, B, K);
        }
      } else {
        if (!N && P.nodeType === Node.ELEMENT_NODE && (L.tag || 'div').toLowerCase() === P.tagName.toLowerCase()) {
          const {lastDomConfig: T} = P, U = _DomSync.performSync(K, P);
          if (j && ((f = T == null ? void 0 : T.dataset) == null ? void 0 : f[j]) != null) {
            const V = T.dataset[j];
            delete I[V];
            delete H[V];
          }
          if (B != null) {
            E[B] = P;
          }
          P.isReleased = false;
          U && (k == null ? void 0 : k({
            'action': 'reuseElement',
            'domConfig': L,
            'targetElement': P,
            'lastDomConfig': T,
            'syncId': B
          }));
        } else if (N && P.nodeType === Node.TEXT_NODE) {
          P.data = L;
        } else {
          if (N) {
            _DomSync.insertTextNode(L, b, k, p, P);
          } else {
            _DomSync.insertElement(L, b, P, p, E, B, K);
          }
          (g = P.blur) == null ? void 0 : g.call(P);
          P.remove();
        }
      }
    }
    if (b.didSetTextContent && G.length === 1 && G[0].nodeType === Node.TEXT_NODE) {
      return;
    }
    if (i.retainChildren) {
      return;
    }
    while (A = J(true)) {
      if (!p || A.$refOwnerId === p.id && !A.$reference) {
        (w || (w = [])).push(A);
      }
    }
    if (w) {
      _DomSync.syncChildrenCleanup(b, w, E, k, p, l, j);
    }
  }
  static syncChildrenCleanup(a, b, c, e, f, g, i) {
    let j;
    let k = 0, l;
    for (const n of b) {
      const {lastDomConfig: o} = n;
      if (n.nodeType === Element.TEXT_NODE) {
        n.remove();
        continue;
      }
      if (!n.retainElement) {
        if (!f && i && (g == null || k < g)) {
          if (!n.isReleased) {
            n.className = 'b-released';
            n.isReleased = true;
            if (o == null ? void 0 : o.dataset) {
              if (!a.releasedIdMap) {
                a.releasedIdMap = {};
              }
              a.releasedIdMap[o.dataset[i]] = n;
            }
            e == null ? void 0 : e({
              'action': 'releaseElement',
              'domConfig': o,
              'lastDomConfig': o,
              'targetElement': n
            });
            if (o) {
              o.isReleased = true;
              if (o.className) {
                o.className = 'b-released';
              }
              if (o.class) {
                o.class = 'b-released';
              }
              if (o.style) {
                o.style = null;
              }
            }
          }
          k++;
        } else {
          (j = n.blur) == null ? void 0 : j.call(n);
          n.remove();
          if (f) {
            l = n.$reference;
            if (l) {
              f.detachRef(l, n, o);
            }
          }
          if (a.releasedIdMap && i && (o == null ? void 0 : o.dataset)) {
            delete a.releasedIdMap[o.dataset[i]];
          }
          e == null ? void 0 : e({
            'action': 'removeElement',
            'domConfig': n.lastDomConfig,
            'lastDomConfig': n.lastDomConfig,
            'targetElement': n
          });
        }
      } else if (i) {
        if (o) {
          c[n.dataset[i]] = n;
        }
      }
    }
  }
  static removeChild(a, b) {
    if (a.contains(b)) {
      const c = a.syncIdMap;
      if (c) {
        const e = Object.values(c).indexOf(b);
        if (e > -1) {
          delete c[Object.keys(c)[e]];
        }
      }
      a.removeChild(b);
    }
  }
  static addChild(a, b, c) {
    a.appendChild(b);
    if (!a.syncIdMap) {
      a.syncIdMap = {};
    }
    a.syncIdMap[c] = b;
  }
  static getChild(a, b) {
    let c;
    const e = String(b).split('.');
    for (const f of e) {
      a = (c = a == null ? void 0 : a.syncIdMap) == null ? void 0 : c[f];
      if (!a) {
        return null;
      }
    }
    return a;
  }
};
DomSync._$name = 'DomSync';
const Fullscreen = class _Fullscreen {
  static init() {
    const a = [
        'fullscreenEnabled',
        'requestFullscreen',
        'exitFullscreen',
        'fullscreenElement'
      ], b = g => a.map(i => {
        let j = g + StringHelper.capitalize(i);
        if (g === 'moz') {
          j = j.replace('screen', 'Screen');
          if ('mozCancelFullScreen' in document && i === 'exitFullscreen') {
            j = 'mozCancelFullScreen';
          }
        }
        return j;
      });
    this.functions = 'fullscreenEnabled' in document && a || 'webkitFullscreenEnabled' in document && b('webkit') || 'mozFullScreenEnabled' in document && b('moz') || 'msFullscreenEnabled' in document && b('ms') || [];
    const c = [
        'fullscreenchange',
        'fullscreenerror'
      ], e = [
        'MSFullscreenChange',
        'MSFullscreenError'
      ], f = g => c.map(i => g + StringHelper.capitalize(i));
    this.events = 'fullscreenEnabled' in document && c || 'webkitFullscreenEnabled' in document && f('webkit') || 'mozFullscreenEnabled' in document && f('moz') || 'msFullscreenEnabled' in document && e || [];
    this.onFullscreenChange(this.onInternalFullscreenChange.bind(this));
  }
  static get enabled() {
    return Boolean(this.functions[0] && document[this.functions[0]]);
  }
  static async request(a) {
    return this.functions[1] && (a == null ? void 0 : a[this.functions[1]]());
  }
  static async exit() {
    return this.functions[2] && document[this.functions[2]]();
  }
  static get isFullscreen() {
    return !!this.element;
  }
  static get element() {
    return this.functions[3] && document[this.functions[3]];
  }
  static onFullscreenChange(a) {
    if (this.events[0]) {
      document.addEventListener(this.events[0], a);
    }
  }
  static unFullscreenChange(a) {
    if (this.events[0]) {
      document.removeEventListener(this.events[0], a);
    }
  }
  static onInternalFullscreenChange() {
    const a = globalThis.bryntum, {element: b} = _Fullscreen;
    if (_Fullscreen.isFullscreen && a._floatRoot || !_Fullscreen.isFullscreen && !a._floatRoot) {
      return;
    }
    if (b) {
      if (!b.closest('.b-floatroot')) {
        const c = DomHelper.getRootElement(b), {floatRoot: e} = c;
        if (e) {
          a._floatRoot = e;
          a._oldParent = e.parentElement;
          b.appendChild(e);
        }
      }
    } else {
      if (a._floatRoot) {
        a._oldParent.appendChild(a._floatRoot);
      }
      a._oldParent = null;
      a._floatRoot = null;
    }
  }
};
Fullscreen.init();
Fullscreen._$name = 'Fullscreen';
let resizeFireTimer = null;
const resizedQueue = [];
const isAbsolutelyPositioned = a => {
  let b;
  return a.nodeType === a.ELEMENT_NODE && ((b = a.ownerDocument.defaultView) == null ? void 0 : b.getComputedStyle(a).getPropertyValue('position')) === 'absolute';
};
const ResizeMonitor = class _ResizeMonitor {
  static addResizeListener(a, b) {
    const c = this;
    if (a === document || a === globalThis) {
      a = document.body;
    }
    if (a.nodeType === a.DOCUMENT_FRAGMENT_NODE) {
      a = a.host;
    }
    if (!a.classList.contains('b-resize-monitored')) {
      a.classList.add('b-resize-monitored');
      a._bResizemonitor = { 'handlers': [] };
    }
    if (a === document.body) {
      if (!c.hasWindowResizeListener) {
        globalThis.addEventListener('resize', c.onWindowResize);
        c.hasWindowResizeListener = true;
      }
    } else if (globalThis.ResizeObserver) {
      if (!c.resizeObserver) {
        c.resizeObserver = new ResizeObserver(c.onElementResize);
      }
      c.resizeObserver.observe(a);
    } else {
      a.classList.add('b-no-resizeobserver');
      const [e, f, g] = DomHelper.createElement({
        'parent': a,
        'className': 'b-resize-monitors',
        'children': [
          { 'className': 'b-resize-monitor-expand' },
          { 'className': 'b-resize-monitor-shrink' }
        ]
      }, { 'returnAll': true });
      f.scrollLeft = f.scrollTop = g.scrollLeft = g.scrollTop = 1000000;
      f.addEventListener('scroll', c.onSizeMonitorScroll, true);
      g.addEventListener('scroll', c.onSizeMonitorScroll, true);
      (b.targetMutationMonitor = new MutationObserver(i => {
        const j = [], k = [];
        for (const n of i) {
          if (n.type === 'childList') {
            j.push.apply(j, n.addedNodes);
            k.push.apply(k, n.removedNodes);
          }
        }
        const l = [
          ...j.filter(o => !k.includes(o)),
          ...k.filter(o => !j.includes(o))
        ];
        if (l.length === 0) {
          return;
        }
        if (l.length > 0 && l.every(isAbsolutelyPositioned)) {
          return;
        }
        f.dispatchEvent(new CustomEvent('scroll', { 'bubbles': false }));
      })).observe(a, {
        'childList': true,
        'subtree': true
      });
      b.monitorElement = e;
    }
    a._bResizemonitor.handlers.push(b);
  }
  static removeResizeListener(a, b) {
    if (a) {
      if (a === document || a === globalThis) {
        a = document.body;
      }
      const c = a._bResizemonitor;
      let e = 0;
      if (c && c.handlers) {
        ArrayHelper.remove(c.handlers, b);
        e = c.handlers.length;
      }
      if (!e) {
        a.classList.remove('b-resize-monitored');
        if (this.resizeObserver) {
          this.resizeObserver.unobserve(a);
        } else {
          if (b.monitorElement) {
            b.monitorElement.remove();
            b.monitorElement = null;
          }
          if (b.targetMutationMonitor) {
            b.targetMutationMonitor.disconnect();
          }
        }
      }
    }
  }
  static onElementResize(a) {
    for (const b of a) {
      const c = b.target, e = c._bResizemonitor, f = b.contentRect || c.getBoundingClientRect();
      if (hasLayout(c)) {
        if (!e.rectangle || f.width !== e.rectangle.width || f.height !== e.rectangle.height) {
          const g = e.rectangle;
          e.rectangle = f;
          for (const i of e.handlers) {
            i(c, g, f);
          }
        }
      }
    }
  }
  static onSizeMonitorScroll(a) {
    let b;
    if (a.target.isConnected || ((b = document.body) == null ? void 0 : b.contains(a.target))) {
      a.stopImmediatePropagation();
      const c = a.target.parentNode, f = c.parentNode, g = f._bResizemonitor, i = f.getBoundingClientRect();
      if (!g.rectangle || i.width !== g.rectangle.width || i.height !== g.rectangle.height) {
        resizedQueue.push([
          f,
          g.rectangle,
          i
        ]);
        g.rectangle = i;
        if (!resizeFireTimer) {
          resizeFireTimer = requestAnimationFrame(_ResizeMonitor.fireResizeEvents);
        }
      }
      c.firstChild.scrollLeft = c.firstChild.scrollTop = c.childNodes[1].scrollTop = c.childNodes[1].scrollLeft = 1000000;
    }
  }
  static onWindowResize(a) {
    const b = document.body, c = b._bResizemonitor, f = c.rectangle;
    c.rectangle = document.documentElement.getBoundingClientRect();
    for (const g of c.handlers) {
      g(b, f, c.rectangle);
    }
  }
  static fireResizeEvents() {
    for (const a of resizedQueue) {
      for (const b of a[0]._bResizemonitor.handlers) {
        if (a[0].offsetParent) {
          b.apply(this, a);
        }
      }
    }
    resizeFireTimer = null;
    resizedQueue.length = 0;
  }
  static removeGlobalListeners() {
    globalThis.removeEventListener('resize', this.onWindowResize);
  }
};
ResizeMonitor._$name = 'ResizeMonitor';
const scrollLiterals = {
  'auto': 'auto',
  'true': 'auto',
  'false': 'hidden',
  'hidden-scroll': 'auto',
  'clip': BrowserHelper.supportsOverflowClip ? 'clip' : 'hidden'
};
const scrollerCls = 'b-widget-scroller';
const defaultScrollOptions = { 'block': 'nearest' };
const immediatePromise3 = Promise.resolve();
const scrollPromise = a => new Promise(b => EventHelper.on({
  'element': a === document.documentElement ? globalThis : a,
  'scroll': b,
  'once': true
}));
const xAxis = { 'x': 1 };
const isScrollable = {
  'auto': 1,
  'scroll': 1
};
const isScrollableConfig = {
  'true': 1,
  'auto': 1
};
const allScroll = {
  'overflowX': 'auto',
  'overflowY': 'auto'
};
const normalizeEdgeOffset = a => {
  let b, c, e, f;
  let g, i, j, k;
  if (!a) {
    g = i = j = k = 0;
  } else if (typeof a === 'number') {
    g = i = j = k = a;
  } else {
    g = (b = a.top) != null ? b : 0;
    i = (c = a.bottom) != null ? c : 0;
    j = (e = a.start) != null ? e : 0;
    k = (f = a.end) != null ? f : 0;
  }
  return {
    'top': g,
    'bottom': i,
    'start': j,
    'end': k
  };
};
const Scroller = class _Scroller extends Delayable_default(Events_default(Base)) {
  static get configurable() {
    return {
      'widget': null,
      'element': {
        '$config': { 'nullify': true },
        'value': null
      },
      'contentElement': {
        '$config': { 'nullify': true },
        'value': null
      },
      'overflowX': null,
      'overflowY': null,
      'translate': null,
      'behavior': null,
      'x': 0,
      'y': 0,
      'rtlSource': null,
      'propagateSync': null
    };
  }
  static get delayable() {
    return {
      'onScrollEnd': {
        'type': 'buffer',
        'delay': 100
      }
    };
  }
  get isRTL() {
    let a;
    return Boolean((a = this.rtlSource) == null ? void 0 : a.rtl);
  }
  syncOverflowState() {
    const a = this, {
        hasOverflowX: b,
        hasOverflowY: c,
        element: e
      } = a, f = a.hasOverflowX = e.scrollWidth > e.clientWidth, g = a.hasOverflowY = e.scrollHeight > e.clientHeight;
    if (b !== f || c !== g) {
      const i = new DomClassList(e.classList, {
        'b-horizontal-overflow': f,
        'b-vertical-overflow': g
      });
      DomHelper.syncClassList(e, i);
      if (!a.isConfiguring) {
        a.trigger('overflowChange', {
          'x': f,
          'y': g
        });
      }
    }
  }
  hasOverflow(a = 'y') {
    const b = this, c = b['overflow' + a.toUpperCase()], e = b['overflow' + (a === 'y' ? 'x' : 'y')];
    if (!DomHelper.scrollBarWidth || !isScrollableConfig[c] || e === 'hidden-scroll') {
      const f = a === 'y' ? 'Height' : 'Width';
      return b['scroll' + f] > b['client' + f];
    }
    return b['hasOverflow' + a.toUpperCase()];
  }
  hasScrollbar(a = 'y') {
    const {element: b} = this;
    if (b && DomHelper.scrollBarWidth) {
      const c = a === 'y', e = c ? 'Width' : 'Height', f = b['client' + e], g = parseInt(DomHelper.getStyleValue(b, 'border' + (c ? 'Left' : 'Top') + 'Width')) + parseInt(DomHelper.getStyleValue(b, 'border' + (c ? 'Right' : 'Bottom') + 'Width')), i = b['offset' + e] - g - f;
      return Math.abs(i - DomHelper.scrollBarWidth) < 2;
    }
  }
  addPartner(a, b = xAxis) {
    const c = this;
    if (typeof b === 'string') {
      b = { [b]: 1 };
    }
    if (!c.partners) {
      c.partners = {};
    }
    c.partners[a.id] = {
      'scroller': a,
      'axes': b
    };
    if (b.x) {
      a.x = c.x;
    }
    if (b.y) {
      a.y = c.y;
    }
    if (!a.isPartneredWith(c)) {
      a.addPartner(c, b);
    }
  }
  eachPartner(a) {
    const {partners: b} = this;
    if (b) {
      Object.values(b).forEach(a);
    }
  }
  removePartner(a) {
    if (a && this.isPartneredWith(a)) {
      delete this.partners[a.id];
      a.removePartner(this);
    }
  }
  isPartneredWith(a) {
    let b;
    return Boolean((b = this.partners) == null ? void 0 : b[a.id]);
  }
  clearPartners() {
    if (this.partners) {
      Object.values(this.partners).forEach(a => a.scroller.removePartner(this));
    }
  }
  async scrollIntoView(a, b = defaultScrollOptions) {
    const c = this, {isRectangle: e} = a, f = e ? a : Rectangle.from(a), {
        xDelta: g,
        yDelta: i
      } = c.getDeltaTo(a, b), j = c.scrollBy(g, i, b);
    if (b.highlight || b.focus) {
      j.then(() => {
        let k, l;
        a = e ? f.translate(-g, -i) : (l = (k = b.elementAfterScroll) == null ? void 0 : k.call(b)) != null ? l : a;
        if (b.highlight) {
          if (isNaN(b.highlight)) {
            (c.widget || c).callback(b.highlight, null, [a]);
          } else {
            DomHelper.highlight(a, c);
          }
        }
        if (b.focus) {
          a.focus({ 'preventScroll': true });
        }
      });
    }
    return j;
  }
  static async scrollIntoView(a, b = defaultScrollOptions, c = false) {
    const e = Rectangle.from(a), f = typeof b === 'object' ? b.animate : b, g = _Scroller._globalScroller || (_Scroller._globalScroller = new _Scroller()), i = [];
    g.rtlSource = { 'rtl': c };
    let j = 0, k = 0, l;
    if (b.maxWidth) {
      e.width = Math.min(e.width, b.maxWidth);
      b.maxWidth = null;
    }
    if (b.maxHeight) {
      e.height = Math.min(e.height, b.maxHeight);
      b.maxHeight = null;
    }
    for (let n = a.parentNode; n.nodeType === Node.ELEMENT_NODE; n = n.parentNode) {
      if (n === document.body && n !== document.scrollingElement) {
        continue;
      }
      const o = n === document.scrollingElement ? allScroll : n.ownerDocument.defaultView.getComputedStyle(n), p = DomDataStore.get(n, 'scroller');
      if (b.y !== false && ((p == null ? void 0 : p.hasOverflowY) || isScrollable[o.overflowY] && n.scrollHeight > n.clientHeight) || b.x !== false && ((p == null ? void 0 : p.hasOverflowX) || isScrollable[o.overflowX] && n.scrollWidth > n.clientWidth)) {
        let q = g;
        if (p) {
          q = p;
        } else {
          g.element = n;
          g.positionDirty = true;
        }
        const {
          xDelta: r,
          yDelta: u
        } = q.getDeltaTo(e, b);
        if (r || u) {
          i.push({
            'element': n,
            'x': n.scrollLeft,
            'y': n.scrollTop,
            'xDelta': r,
            'yDelta': u
          });
          e.translate(-r, -u);
          j += r;
          k += u;
        }
      }
    }
    if (i.length) {
      const v = Math.abs(j), w = Math.abs(k);
      let z = f && (typeof f === 'number' ? f : typeof f.duration === 'number' ? f.duration : 300);
      if (z && (v > 10 || w > 10)) {
        if (Math.max(v, w) < 50) {
          z = Math.min(z, 500);
        }
        l = g.scrollAnimation = FunctionHelper.animate(z, A => {
          const B = A === 1;
          for (const {
                element: C,
                x: E,
                y: F,
                xDelta: G,
                yDelta: H
              } of i) {
            g.element = C;
            if (G) {
              g.x = Math[c ? 'min' : 'max'](E + (B ? G : Math.round(G * A)), 0);
            }
            if (H) {
              g.y = Math.max(F + (B ? H : Math.round(H * A)), 0);
            }
          }
        }, null, f.easing);
        l.then(() => {
          g.scrollAnimation = null;
        });
      } else {
        for (const {
              element: A,
              xDelta: B,
              yDelta: C
            } of i) {
          A.scrollTop += C;
          A.scrollLeft += B;
        }
        l = scrollPromise(i[i.length - 1].element);
      }
    } else {
      l = immediatePromise3;
    }
    if (b.highlight || b.focus) {
      l.then(() => {
        if (b.highlight) {
          if (isNaN(b.highlight)) {
            g.callback(b.highlight, null, [a]);
          } else {
            DomHelper.highlight(a, g);
          }
        }
        if (b.focus) {
          a.focus({ 'preventScroll': true });
        }
      });
    }
    return l;
  }
  scrollBy(a = 0, b = 0, c = defaultScrollOptions) {
    const e = this, f = typeof c === 'object' ? c.animate : c, g = Math.abs(a), i = Math.abs(b);
    if (e.scrollAnimation) {
      e.scrollAnimation.cancel();
      e.scrollAnimation = null;
    }
    if (a || b) {
      e.silent = c.silent;
    }
    let j = f && (typeof f === 'number' ? f : typeof f.duration === 'number' ? f.duration : 300);
    if (j && (g > 10 || i > 10)) {
      const {
        x: k,
        y: l
      } = e;
      let n = k, o = l;
      if (Math.max(g, i) < 50) {
        j = Math.min(j, 500);
      }
      e.scrollAnimation = FunctionHelper.animate(j, p => {
        let q, r;
        const u = p === 1;
        if (a) {
          if (Math.abs(e.x - n) > 1 && !c.force) {
            return (q = e.scrollAnimation) == null ? void 0 : q.cancel();
          }
          e.x = Math.max(k + (u ? a : Math.round(a * p)), 0);
        }
        if (b) {
          if (Math.abs(e.y - o) > 1 && !c.force) {
            return (r = e.scrollAnimation) == null ? void 0 : r.cancel();
          }
          e.y = Math.max(l + (u ? b : Math.round(b * p)), 0);
        }
        n = e.x;
        o = e.y;
      }, e, f.easing);
      e.element.classList.add('b-scrolling');
      e.scrollAnimation.then(() => {
        if (!e.isDestroyed) {
          e.element.classList.remove('b-scrolling');
          e.scrollAnimation = null;
        }
      });
      return e.scrollAnimation;
    } else {
      if (a || b) {
        const p = e.x, q = e.y;
        if (Math.round(g) >= 1) {
          e.x += a;
        }
        if (Math.round(i) >= 1) {
          e.y += b;
        }
        if (e.x !== p || e.y !== q) {
          return scrollPromise(e.element);
        }
      }
      return immediatePromise3;
    }
  }
  async scrollTo(a, b, c) {
    const {
        x: e,
        y: f
      } = this, g = a == null ? 0 : a - e, i = b == null ? 0 : b - f;
    this.scrollingToCenter = c == null ? void 0 : c.scrollingToCenter;
    return this.scrollBy(g, i, c);
  }
  doDestroy() {
    let a, b;
    const c = this;
    if (c._element) {
      c._element.removeEventListener('scroll', c.scrollHandler);
      (a = c.wheelListenerRemover) == null ? void 0 : a.call(c);
    }
    (b = c.scrollAnimation) == null ? void 0 : b.cancel();
    Object.values(c.partners || {}).forEach(({scroller: e}) => e.removePartner(c));
    super.doDestroy();
  }
  onElMutation(a) {
    const b = this, [c, e] = DomHelper.getTranslateXY(b.element);
    if (b._x !== -c || b.y !== -e) {
      const f = new CustomEvent('scroll', { 'bubbles': true });
      Object.defineProperty(f, 'target', { 'get': () => b.element });
      b.onScroll(f);
    }
  }
  onElResize() {
    const a = this, {widget: b} = a;
    if (!(b == null ? void 0 : b.isAnimating)) {
      a.syncOverflowState();
    } else if (b.findListener('animationend', 'onElResize', a) === -1) {
      b.ion({
        'animationEnd': 'onElResize',
        'thisObj': a,
        'once': true
      });
    }
  }
  onScroll(a) {
    const b = this, {
        _x: c,
        _y: f,
        element: g
      } = b;
    let i = 0;
    if (b.overflowX === 'clip' && g.scrollLeft !== c) {
      g.scrollLeft = c;
      ++i;
    }
    if (b.overflowY === 'clip' && g.scrollTop !== f) {
      g.scrollTop = f;
      ++i;
    }
    if (i === 2) {
      return;
    }
    if (!b.widget || !b.widget.isDestroyed) {
      b.positionDirty = true;
      if (!g.classList.contains('b-scrolling')) {
        g.classList.add('b-scrolling');
      }
      a.widget = b.widget;
      a.scrollingToCenter = b.scrollingToCenter;
      if (!b.silent) {
        b.trigger('scroll', a);
      }
      b.syncPartners();
      b.controllingPartner = null;
      b.onScrollEnd(a);
    }
  }
  syncPartners(a, b = this.propagateSync) {
    const c = this;
    if (c.partners) {
      Object.values(c.partners).forEach(({
        axes: e,
        scroller: f
      }) => {
        if (f !== c.controllingPartner || a) {
          if (f.sync(c, e) && b) {
            f.syncPartners(a, b);
          }
        }
      });
    }
  }
  onScrollEnd(a) {
    const b = this;
    if (b.silent) {
      b.silent = false;
    }
    b.trigger('scrollEnd', a);
    b.scrollingToCenter = false;
    b.controllingPartner = null;
    b.element.classList.remove('b-scrolling');
  }
  getDeltaTo(a, b) {
    const c = this;
    if (!c.viewport) {
      return {
        'xDelta': 0,
        'yDelta': 0
      };
    }
    const {
        x: e,
        y: f,
        scrollWidth: g,
        scrollHeight: i,
        isRTL: j
      } = c, k = a instanceof Rectangle ? a : Rectangle.from(a), l = b.block || 'nearest', n = c.viewport, o = normalizeEdgeOffset(b.edgeOffset);
    if (b.maxWidth) {
      if (k.x < n.x) {
        k.x = k.right - b.maxWidth;
        o.start = 0;
      } else {
        k.width = Math.min(k.width, b.maxWidth);
        o.end = 0;
      }
    }
    if (b.maxHeight) {
      if (k.y < n.y) {
        k.y = k.bottom - b.maxHeight;
        o.top = 0;
      } else {
        k.height = Math.min(k.height, b.maxHeight);
        o.bottom = 0;
      }
    }
    const p = b.x !== false && n.width >= k.width + (o.start + o.end) ? o : {
        'start': 0,
        'end': 0
      }, q = b.y !== false && n.height >= k.height + (o.top + o.bottom) ? o : {
        'top': 0,
        'bottom': 0
      }, r = new Rectangle(j ? n.right + e - g : n.x - e, n.y - f, g, i), u = k.clone().adjust(-p.start, -q.top, p.end, q.bottom).constrainTo(r), v = u.clone(), w = c.isRTL ? -1 : 1;
    let z = 0, A = 0;
    if (l === 'start') {
      v.moveTo(n.x + (c.isRTL ? n.width - v.width : 0), n.y);
      z = u.x - v.x;
      A = u.y - v.y;
    } else if (l === 'end') {
      v.moveTo(n.x + (!c.isRTL ? n.width - v.width : 0), n.bottom - v.height);
      z = u.x - v.x;
      A = u.y - v.y;
    } else if (l === 'center') {
      const B = n.center;
      v.moveTo(B.x - v.width / 2, B.y - v.height / 2);
      z = z || u.x - v.x;
      A = A || u.y - v.y;
    } else {
      if (v.width > n.width) {
        z = v.x - n.x;
      } else {
        if (v.right > n.right) {
          z = v.right - n.right;
        } else if (v.x < n.x) {
          z = v.x - n.x;
        }
      }
      if (v.height > n.height) {
        A = v.y - n.y;
      } else {
        if (v.bottom > n.bottom) {
          A = v.bottom - n.bottom;
        } else if (v.y < n.y) {
          A = v.y - n.y;
        }
      }
    }
    z = w * Math.round(z);
    A = Math.round(A);
    if (b.constrainToScrollable !== false) {
      z = Math.max(Math.min(z, c.maxX - e), -e);
      A = Math.max(Math.min(A, c.maxY - f), -f);
    }
    return {
      'xDelta': b.x === false ? 0 : z,
      'yDelta': b.y === false ? 0 : A
    };
  }
  get viewport() {
    return Rectangle.client(this.element);
  }
  updateWidget(a) {
    this.rtlSource = this.owner = a;
  }
  updateElement(a, b) {
    let c;
    const e = this;
    if (e === _Scroller._globalScroller) {
      e._element = a;
      e.positionDirty = true;
      return;
    }
    const f = e.scrollHandler || (e.scrollHandler = e.onScroll.bind(e)), g = e.resizeHandler || (e.resizeHandler = e.onElResize.bind(e));
    if (b) {
      DomDataStore.remove(b, 'scroller');
      if (e.translate) {
        (c = e.mutationObserver) == null ? void 0 : c.disconnect();
      } else {
        b.removeEventListener('scroll', f);
        b.classList.remove(scrollerCls);
        b.style.overflowX = b.style.overflowY = '';
      }
      ResizeMonitor.removeResizeListener(b, g);
    }
    if (a) {
      DomDataStore.set(a, 'scroller', e);
      if (e.translate) {
        if (!e.mutationObserver) {
          e.mutationObserver = new MutationObserver(e.mutationHandler || (e.mutationHandler = e.onElMutation.bind(e)));
        }
        e._x = e._y = 0;
        if (document.contains(a)) {
          const [i, j] = DomHelper.getTranslateXY(a);
          e._x = -i;
          e._y = -j;
        }
        e.mutationObserver.observe(a, { 'attributes': true });
      } else {
        a.addEventListener('scroll', f);
        a.classList.add(scrollerCls);
      }
      ResizeMonitor.addResizeListener(a, g);
      if (!e.widget) {
        e.rtlSource = {
          get 'rtl'() {
            return DomHelper.getStyleValue(a, 'direction') === 'rtl';
          }
        };
      }
      if (e.isRTL) {
        a.classList.add('b-rtl');
      }
      if (e.positionDirty) {
        e.updateOverflowX(e.overflowX);
        e.updateOverflowY(e.overflowY);
      }
      e.syncOverflowState();
      if (e.isConfiguring) {
        e._x && e.updateX(e._x);
        e._y && e.updateY(e._y);
      }
    }
    e.positionDirty = true;
  }
  get x() {
    const a = this, {element: b} = a;
    if (b && a.positionDirty) {
      if (a.translate) {
        const [c, e] = DomHelper.getTranslateXY(b);
        a._x = -c;
        a._y = -e;
      } else {
        a._x = Math.abs(b.scrollLeft);
        a._y = b.scrollTop;
      }
      a.positionDirty = false;
    }
    return a._x;
  }
  get scrollLeft() {
    return this.x * (this.isRTL ? -1 : 1);
  }
  changeX(a) {
    a = Math.max(a, 0);
    if (!this.isConfiguring || a) {
      return a;
    }
    this._x = a;
  }
  updateContentElement(a) {
    if (a) {
      a = typeof a === 'string' ? this.element.querySelector(a) : a;
      ResizeMonitor.addResizeListener(a, this.resizeHandler);
    }
  }
  updateX(a) {
    let b;
    const c = this, {
        behavior: e,
        element: f
      } = c;
    if (f && !((b = c.widget) == null ? void 0 : b.isConfiguring)) {
      a = Math.round(a);
      c.trigger('scrollStart', { 'x': a });
      if (c.translate) {
        DomHelper.setTranslateX(f, -a);
      } else if (e) {
        f.scrollTo({
          'behavior': e,
          'left': c.isRTL ? -a : a
        });
      } else {
        f.scrollLeft = c.isRTL ? -a : a;
      }
    }
    c.positionDirty = true;
  }
  sync(a, b) {
    const c = this, {
        x: e,
        y: f
      } = b;
    let g = false;
    if (e) {
      if (c.x !== a.x) {
        c.controllingPartner = a;
        c.x = a.x;
        g = true;
      }
    }
    if (f) {
      if (c.y !== a.y) {
        c.controllingPartner = a;
        c.y = a.y;
        g = true;
      }
    }
    return g;
  }
  get y() {
    const a = this, {element: b} = a;
    if (b && a.positionDirty) {
      if (a.translate) {
        const [c, e] = DomHelper.getTranslateXY(b);
        a._x = -c;
        a._y = -e;
      } else {
        a._x = b.scrollLeft;
        a._y = b.scrollTop;
      }
      a.positionDirty = false;
    }
    return a._y;
  }
  changeY(a) {
    if (!this.isConfiguring || a) {
      return a;
    }
    this._y = a;
  }
  updateY(a) {
    const b = this, {
        behavior: c,
        element: e,
        widget: f
      } = b;
    if (e && !(f == null ? void 0 : f.isConfiguring)) {
      b.trigger('scrollStart', { 'y': a });
      if (b.translate) {
        DomHelper.setTranslateY(e, -a);
      } else if (c) {
        e.scrollTo({
          'behavior': c,
          'top': a
        });
      } else {
        e.scrollTop = a;
      }
    }
    b.positionDirty = true;
  }
  get maxX() {
    return this.scrollWidth - this.clientWidth;
  }
  get maxY() {
    return this.scrollHeight - this.clientHeight;
  }
  get lastScrollLeft() {
    return (this.scrollWidth - this.clientWidth) * (this.isRTL ? -1 : 1);
  }
  updateOverflowX(a, b) {
    const c = this, {
        element: e,
        translate: f
      } = c, {
        style: g,
        classList: i
      } = e;
    if (b === 'hidden-scroll') {
      i.remove('b-hide-scroll');
    }
    if (a === 'hidden-scroll' && !f) {
      const j = isScrollable[c.overflowY];
      if (j) {
        a = 'hidden';
        c.enableWheel();
      } else {
        i.add('b-hide-scroll');
      }
    }
    if (!f) {
      g.overflowX = scrollLiterals[a] || a;
    }
    if (!c.isConfiguring) {
      c.positionDirty = true;
      c.syncOverflowState();
    }
  }
  updateOverflowY(a, b) {
    const c = this, {
        element: e,
        translate: f
      } = c, {
        style: g,
        classList: i
      } = e;
    if (b === 'hidden-scroll') {
      i.remove('b-hide-scroll');
    }
    if (a === 'hidden-scroll' && !f) {
      const j = isScrollable[c.overflowX];
      if (j) {
        a = 'hidden';
        c.enableWheel();
      } else {
        i.add('b-hide-scroll');
      }
    }
    if (!f) {
      g.overflowY = scrollLiterals[a] || a;
    }
    if (!c.isConfiguring) {
      c.positionDirty = true;
      c.syncOverflowState();
    }
  }
  enableWheel() {
    if (!this.wheelListenerRemover) {
      this.wheelListenerRemover = EventHelper.on({
        'element': this.element,
        'wheel': 'onWheel',
        'thisObj': this
      });
    }
  }
  onWheel(a) {
    const b = this, {
        overflowX: c,
        overflowY: f
      } = b, g = a.deltaX, i = a.deltaY, j = Math.abs(g) - Math.abs(i);
    if (c === 'hidden-scroll' && isScrollable[f] && j > 0) {
      b.x += g;
    } else if (f === 'hidden-scroll' && isScrollable[c] && j < 0) {
      b.y += i;
    }
  }
  get scrollWidth() {
    let a, b;
    return (b = (a = this.element) == null ? void 0 : a.scrollWidth) != null ? b : 0;
  }
  set scrollWidth(a) {
    const b = this, {
        element: c,
        isRTL: e
      } = b;
    let f = b.widthStretcher;
    if (f && a == null) {
      f.remove();
      b.widthStretcher = null;
    } else if (a) {
      a = Math.abs(a);
      if (!f) {
        f = b.widthStretcher = DomHelper.createElement({
          'className': 'b-scroller-stretcher b-horizontal-stretcher',
          'retainElement': true
        });
      }
      f.style.transform = 'translateX(' + (a - 1) * (e ? -1 : 1) + 'px)';
      if (c && !c.contains(f)) {
        c.insertBefore(f, c.firstElementChild);
      }
    }
    if (b.propagate !== false) {
      b.eachPartner(({scroller: g}) => {
        g.propagate = false;
        g.scrollWidth = a;
        delete g.propagate;
      });
    }
    b.positionDirty = true;
    b.syncOverflowState();
  }
  get scrollHeight() {
    let a, b;
    return (b = (a = this.element) == null ? void 0 : a.scrollHeight) != null ? b : 0;
  }
  set scrollHeight(a) {
    const b = this, c = b.stretcher || (b.stretcher = DomHelper.createElement({ 'className': 'b-scroller-stretcher' }));
    c.style.transform = 'translateY(' + (a - 1) + 'px)';
    if (b.element && b.element.lastChild !== c) {
      b.element.appendChild(c);
    }
    b.positionDirty = true;
    b.syncOverflowState();
  }
  get clientWidth() {
    let a;
    return ((a = this.element) == null ? void 0 : a.clientWidth) || 0;
  }
  get clientHeight() {
    let a;
    return ((a = this.element) == null ? void 0 : a.clientHeight) || 0;
  }
  get id() {
    if (!this._id) {
      if (this.widget) {
        this._id = this.widget.id + '-scroller';
      } else {
        this._id = IdHelper.generateId('scroller-');
      }
    }
    return this._id;
  }
  preProcessCurrentConfigs(a) {
    super.preProcessCurrentConfigs();
    delete a.widget;
    delete a.element;
  }
};
Scroller._$name = 'Scroller';
const KeyMap_default = a => {
  let b;
  return b = class extends (a || Base) {
    get widgetClass() {
    }
    get keyMapElement() {
      return this.element;
    }
    get keyMapSubComponents() {
      return this.features;
    }
    matchKeyMapEntry(c, e = this.keyMap) {
      if (e && !c.handled && c.key !== void 0) {
        return ObjectHelper.keys(e).find(f => {
          const {
              altKey: g,
              ctrlKey: i,
              metaKey: j,
              shiftKey: k
            } = c, l = f.toLowerCase().split('+'), n = f.endsWith('+') ? '+' : l.pop(), o = n === 'space' ? ' ' : n, p = l.includes('alt'), q = l.includes('ctrl'), r = l.includes('meta'), u = l.includes('shift');
          return o === c.key.toLowerCase() && (p ? g : !g) && (q ? i : !i) && (r ? j : !j) && (u ? k : !k);
        });
      }
    }
    performKeyMapAction(c) {
      let e, f;
      const g = this, {keyMap: i} = g;
      let j = false, k, l, n, o;
      if (i && !c.handled && c.key !== void 0) {
        const p = g.matchKeyMapEntry(c);
        if (i[p]) {
          const q = ArrayHelper.asArray(i[p]);
          c.fromKeyMap = true;
          c.widget = this;
          for (k of q) {
            o = true;
            if (ObjectHelper.isObject(k)) {
              if (!k.handler) {
                continue;
              }
              if (k.preventDefault === false) {
                o = false;
              }
              k = k.handler;
            }
            if (typeof k === 'string') {
              const {
                thisObj: r,
                handler: u
              } = g.resolveKeyMapAction(k);
              if (((e = r.isActionAvailable) == null ? void 0 : e.call(r, {
                  'key': p,
                  'action': k,
                  'event': c,
                  'actionName': k.split('.').pop()
                })) !== false) {
                if (u.call(r, c) !== false) {
                  j = true;
                  break;
                }
              }
            } else if (k.call(g, c) !== false) {
              j = true;
              break;
            }
          }
          if (j) {
            if (o) {
              c.preventDefault();
            }
            c.handled = true;
          }
        }
        if (!j && (n = ObjectHelper.getTruthyKeys(i.delegate))) {
          for (l of n) {
            l = ObjectHelper.getPath(g, l);
            j = (f = l == null ? void 0 : l.performKeyMapAction) == null ? void 0 : f.call(l, c);
            if (j) {
              break;
            }
          }
        }
      }
      return j;
    }
    resolveKeyMapAction(c) {
      const e = this, {keyMapSubComponents: f} = e;
      if (c.startsWith('up.') || c.startsWith('this.')) {
        return e.resolveCallback(c);
      }
      if (f && c.includes('.')) {
        const [g, i] = c.split('.');
        if (g && i) {
          return {
            'thisObj': f[g],
            'handler': f[g][i]
          };
        }
      }
      return {
        'thisObj': e,
        'handler': e[c]
      };
    }
    updateKeyMap(c) {
      let e;
      const f = this, g = ObjectHelper.keys(c);
      (e = f.keyMapDetacher) == null ? void 0 : e.call(f);
      f.keyMapDetacher = (g.length || null) && EventHelper.on({
        'element': f.keyMapElement,
        'keydown': 'keyMapOnKeyDown',
        'thisObj': f
      });
    }
    keyMapOnKeyDown(c) {
      this.performKeyMapAction(c);
    }
    mergeKeyMaps(c, e, f = null) {
      const g = {};
      if (c) {
        ObjectHelper.assign(g, c);
      }
      for (const i in e) {
        if (!e[i]) {
          continue;
        }
        const j = ArrayHelper.asArray(c == null ? void 0 : c[i]), k = [];
        if (j == null ? void 0 : j.length) {
          k.push(...j);
        }
        if (!(j == null ? void 0 : j.some(l => {
            const n = l.handler ? l.handler : l;
            return typeof n === 'string' && n.startsWith(f + '.');
          }))) {
          for (const l of ArrayHelper.asArray(e[i])) {
            if (ObjectHelper.isObject(l) && l.handler) {
              k.push(ObjectHelper.assignIf({ 'handler': (f ? f + '.' : '') + l.handler }, l));
            } else if (typeof l === 'function') {
              k.push(l);
            } else {
              k.push((f ? f + '.' : '') + l);
            }
          }
          k.sort((n, o) => {
            const p = (n.weight || 0) - (o.weight || 0);
            if (p === 0 && (j == null ? void 0 : j.length)) {
              return j.indexOf(n) - j.indexOf(o);
            }
            return p;
          });
        }
        g[i] = k;
      }
      return g;
    }
  }, __publicField(b, '$name', 'KeyMap'), __publicField(b, 'configurable', {
    'keyMap': {
      'value': null,
      '$config': {
        'merge': 'objects',
        'nullify': true
      }
    }
  }), b;
};
const RTL_default = a => {
  let b;
  return b = class extends (a || Base) {
    get widgetClass() {
    }
    get isRTL() {
      return this.rtl;
    }
    updateRtl(c) {
      const {element: e} = this;
      if (e) {
        e.classList.toggle('b-rtl', c === true);
        e.classList.toggle('b-ltr', c === false);
      }
    }
    startConfigure(c) {
      let e, f;
      (e = super.startConfigure) == null ? void 0 : e.call(this, arguments);
      if (!c.floating && c.rtl == null) {
        const g = this, i = c.rootElement || c.forElement || ((f = g.parent) == null ? void 0 : f.contentElement) || (g.floating ? g.floatRoot : g.changeElementRef(g.getRenderContext(c)[0] || c.adopt));
        if (i) {
          c.rtl = i.nodeType === 1 && getComputedStyle(i).getPropertyValue('direction') === 'rtl';
        } else {
          const j = c.owner || c.parent;
          if (j) {
            c.rtl = j[j.isConfiguring ? 'peekConfig' : 'getConfig']('rtl');
          } else {
            c.rtl = getComputedStyle(document.body).getPropertyValue('direction') === 'rtl';
          }
        }
        if (c.rtl) {
          g.configDone.rtl = false;
        }
      }
    }
    render(...c) {
      let e, f, g;
      (e = super.render) == null ? void 0 : e.call(this, ...c);
      if (BrowserHelper.isChrome && BrowserHelper.chromeVersion < 87 || BrowserHelper.isFirefox && BrowserHelper.firefoxVersion < 66 || BrowserHelper.isSafari && BrowserHelper.safariVersion < 14.1) {
        this.element.classList.add('b-legacy-inset');
      }
      if (getComputedStyle(this.element).direction === 'rtl' || ((f = this.owner) == null ? void 0 : f.rtl)) {
        this.rtl = true;
        (g = this.childItems) == null ? void 0 : g.forEach(j => j.rtl = true);
      }
    }
  }, __publicField(b, '$name', 'RTL'), __publicField(b, 'configurable', { 'rtl': null }), b;
};
const assignValueDefaults = Object.freeze({
  'highlight': false,
  'onlyName': false
});
const EMPTY = Object.freeze([]);
const NO_SYNC = Object.freeze({ 'sync': false });
const floatRoots = [];
const hideModes = [
  'clip',
  'display',
  'opacity'
];
const highlightExternalChange = 'highlightExternalChange';
const isTransparent = /transparent|rgba\(0,\s*0,\s*0,\s*0\)/;
const renderConfigObserver = Symbol('renderConfigObserver');
const htmlRe3 = /<div|<button|<input|<p|<br|<li/;
const textInputTypes = {
  'INPUT': 1,
  'TEXTAREA': 1
};
const addElementListeners = (a, b, c, e) => {
  let f = (c == null ? void 0 : c.listeners) || (c == null ? void 0 : c.internalListeners);
  f = (f == null ? void 0 : f.on) || f;
  if (f) {
    const g = EventHelper.on(ObjectHelper.assign({
      'element': b,
      'thisObj': a
    }, f));
    if (e) {
      (a._refListeners || (a._refListeners = Object.create(null)))[e] = g;
    } else {
      c.listeners = {
        'on': f,
        'un': g
      };
    }
  }
};
const mergeAnim = (a, b) => {
  return a && b && b[ObjectHelper.keys(a)[0]] ? Config.merge(a, b) : a;
};
const widgetTriggerPaint = a => {
  a.isVisible && a.triggerPaint();
};
const negationPseudo = /^:not\((.+)\)$/;
const nonFlowedPositions = /absolute|fixed/i;
const isScaled = a => a.scale != null;
const {hasOwn: hasOwn6} = ObjectHelper;
const {defineProperty: defineProperty6} = Reflect;
const parseDuration = a => parseFloat(a) * (a.endsWith('ms') ? 1 : 1000);
const alignedClass = [
  'b-aligned-above',
  'b-aligned-right',
  'b-aligned-below',
  'b-aligned-left'
];
const returnFalseProp = {
  'configurable': true,
  'value': false
};
const localizeRE = /(?:L\{([^}.]+)\})/;
const alignSpecRe2 = /^([trblc])(\d*)-([trblc])(\d*)$/i;
const mergeAlign = (a, b) => {
  if (alignSpecRe2.test(a)) {
    a = { 'align': a };
  }
  if (alignSpecRe2.test(b)) {
    b = { 'align': b };
  }
  return Config.merge(a, b);
};
const callbackRe = /^[\w.]+$/;
const alignChangeDims = {
  1: 'maxHeight',
  2: 'height',
  4: 'maxWidth',
  8: 'width'
};
const isSingletonWidget = a => !a.isRipple && !a.closest(b => b.isMessageDialog || b.isSharedTooltip);
const getLayoutTarget = a => DomHelper.getStyleValue(a, 'display') === 'contents' ? getLayoutTarget(a.parentNode) : a;
const getEventTarget = a => {
  if (a == null ? void 0 : a.target) {
    const {ownerSVGElement: b} = a.target, c = b == null ? void 0 : b.getBoundingClientRect();
    if (b) {
      return {
        'target': a.target.ownerSVGElement,
        'offsetX': a.clientX - c.x,
        'offsetY': a.clientY - c.y
      };
    }
    let {
        offsetX: e,
        offsetY: f
      } = a, g = getLayoutTarget(a.target);
    if (DomHelper.getStyleValue(a.target, 'display') === 'contents') {
      const i = g.getBoundingClientRect(g);
      e -= i.x;
      f -= i.y;
    }
    return {
      'target': getLayoutTarget(a.target),
      'offsetX': e,
      'offsetY': f
    };
  }
};
const _Widget = class _Widget extends Base.mixin(Localizable_default, Events_default, Delayable_default, Identifiable_default, Factoryable_default, KeyMap_default, RTL_default) {
  static convertPinchToMousewheel = true;
  static mergeCls(a, b) {
    if (b && a) {
      a = new DomClassList(b).assign(typeof a === 'string' ? new DomClassList(a) : a);
    } else if (a && !a.isDomClassList) {
      a = new DomClassList(a);
    }
    return a;
  }
  static get $name() {
    return 'Widget';
  }
  static get type() {
    return 'widget';
  }
  static get configurable() {
    return {
      'element': true,
      'callOnFunctions': true,
      'hideMode': {
        '$config': null,
        'default': 'display'
      },
      'id': null,
      'html': {
        '$config': 'lazy',
        'value': null
      },
      'content': {
        '$config': 'lazy',
        'value': null
      },
      'cls': {
        '$config': { 'merge': 'classList' },
        'value': null
      },
      'ui': {
        '$config': { 'merge': 'classList' },
        'value': null
      },
      'collapsify': null,
      'contentElementCls': {
        '$config': { 'merge': 'classList' },
        'value': null
      },
      'defaultCls': {
        '$config': { 'merge': 'classList' },
        'value': null
      },
      'dock': null,
      'parent': null,
      'tab': null,
      'elementAttributes': null,
      'htmlCls': {
        '$config': { 'merge': 'classList' },
        'value': { 'b-html': 1 }
      },
      'style': null,
      'disabled': null,
      'readOnly': {
        'value': null,
        'default': false,
        '$config': null
      },
      'ignoreParentReadOnly': null,
      'adopt': null,
      'appendTo': null,
      'insertBefore': null,
      'insertFirst': null,
      'dataset': null,
      'tooltip': {
        '$config': [
          'lazy',
          'nullify'
        ],
        'value': null
      },
      'showTooltipWhenDisabled': true,
      'preventTooltipOnTouch': null,
      'monitorResize': {
        '$config': [
          'lazy',
          'nullify'
        ],
        'value': null
      },
      'masked': {
        '$config': 'nullify',
        'value': null
      },
      'maskDefaults': { 'target': 'element' },
      'floating': null,
      'positionable': null,
      'positioned': null,
      'draggable': null,
      'align': {
        '$config': { 'merge': mergeAlign },
        'value': 't-b'
      },
      'centered': null,
      'constrainTo': void 0,
      'anchor': null,
      'owner': {
        '$config': [
          'lazy',
          'nullify'
        ],
        'value': null
      },
      'scrollAction': null,
      'hideAnimation': {
        '$config': { 'merge': mergeAnim },
        'value': null
      },
      'showAnimation': {
        '$config': { 'merge': mergeAnim },
        'value': null
      },
      'top': null,
      'right': null,
      'bottom': null,
      'left': null,
      'x': null,
      'y': null,
      'scrollable': {
        '$config': [
          'lazy',
          'nullify'
        ],
        'value': null
      },
      'scrollerClass': Scroller,
      'defaultBindProperty': 'html',
      'defaultAction': null,
      'overflowable': {
        'value': null,
        'default': true,
        '$config': null
      },
      'width': null,
      'height': null,
      'maxHeight': null,
      'maxWidth': null,
      'minWidth': null,
      'minHeight': null,
      'scaleToFitWidth': null,
      'allowGrowWidth': true,
      'margin': null,
      'flex': null,
      'weight': null,
      'alignSelf': null,
      'ripple': null,
      'title': null,
      'localizableProperties': [
        'title',
        'ariaLabel',
        'ariaDescription'
      ],
      'requireSize': false,
      'ref': null,
      'hidden': null,
      'textAlign': null,
      'ignoreScrollDuration': 500,
      'tag': 'div',
      'recomposeAsync': null,
      'rootElement': null,
      'htmlMutationObserver': {
        '$config': [
          'lazy',
          'nullify'
        ],
        'value': {
          'childList': true,
          'subtree': true
        }
      },
      'role': {
        '$config': 'lazy',
        'value': 'presentation'
      },
      'ariaLabel': {
        '$config': 'lazy',
        'value': null
      },
      'ariaDescription': {
        '$config': 'lazy',
        'value': null
      },
      'ariaLive': {
        '$config': 'lazy',
        'value': null
      },
      'ariaElement': 'element',
      'ariaHasPopup': null,
      'realignTimeout': 300,
      'testConfig': {
        'ignoreScrollDuration': 100,
        'realignTimeout': 50
      },
      'maximizeOnMobile': null,
      'twinForwardEvents': {
        '$config': { 'merge': 'classList' },
        'value': null
      },
      'twinSyncConfigs': {
        '$config': { 'merge': 'classList' },
        'value': ['disabled']
      }
    };
  }
  static get prototypeProperties() {
    return {
      'hasGeneratedId': false,
      'hasPainted': false,
      'innerItem': true
    };
  }
  static get declarable() {
    return ['renderConfigs'];
  }
  static get delayable() {
    return {
      'recompose': 'raf',
      'doHideOrRealign': 'raf',
      'onAlignConstraintChange': 'raf'
    };
  }
  static get factoryable() {
    return { 'defaultType': 'widget' };
  }
  static get identifiable() {
    return {};
  }
  static get all() {
    return super.all;
  }
  static get recomposeAsync() {
    return _Widget._recomposeAsync;
  }
  static set recomposeAsync(a) {
    _Widget._recomposeAsync = a;
    if (a != null) {
      const {all: b} = _Widget;
      for (let c = 0; c < b.length; ++c) {
        if (b[c].isComposable) {
          b[c].recomposeAsync = a;
        }
      }
    }
  }
  updateWeight() {
    let a, b;
    if (this.isPainted) {
      (b = (a = this.parent) == null ? void 0 : a.onChildWeightChange) == null ? void 0 : b.call(a, this);
    }
  }
  isA(a) {
    return this.isType(a, true);
  }
  isType(a, b) {
    return _Widget.isType(this, a, b);
  }
  static setupRenderConfigs(a, b) {
    const c = b.getInherited('renderConfigs'), {renderConfigs: e} = a;
    if (e) {
      if (Array.isArray(e)) {
        for (const f of e) {
          c[f] = true;
        }
      } else {
        ObjectHelper.assign(c, e);
      }
      c[renderConfigObserver] = null;
    }
  }
  static weightSortFn({weight: c}, {weight: e}) {
    if (typeof c === 'string' || typeof e === 'string') {
      return String(c).localeCompare(String(e));
    }
    return (c != null ? c : Number.MAX_SAFE_INTEGER) - (e != null ? e : Number.MAX_SAFE_INTEGER);
  }
  construct(a = {}, ...b) {
    const c = this, {domSyncCallback: e} = c, {recomposeAsync: f} = _Widget;
    if (f != null && c.recomposeAsync == null) {
      c.recomposeAsync = f;
    }
    if (!globalThis.bryntum.cssVersion) {
      const g = globalThis.bryntum.cssVersion = CSSHelper.getCSSVersion(), i = VersionHelper.getVersion('core');
      if (g && g !== i) {
        console.warn('CSS version ' + g + ' doesn\'t match bundle version ' + i + '!\nMake sure you have imported css from the appropriate product version.');
      }
    }
    c.configureAriaDescription = a.ariaDescription;
    c._isAnimatingCounter = 0;
    c.alignmentChanges = 0;
    c.byRef = Object.create(null);
    c.onTargetResize = c.onTargetResize.bind(c);
    c.onFullscreenChange = c.onFullscreenChange.bind(c);
    c.domSyncCallback = e.$nullFn ? null : e.bind(c);
    c._isUserAction = false;
    super.construct(a, ...b);
    c.finalizeInit();
  }
  startConfigure(a) {
    super.startConfigure(a);
    this.getConfig('element');
  }
  finalizeInit() {
    let a;
    const b = this, {adopt: c} = b, e = b.insertBefore || b.appendTo || b.insertFirst || c;
    b.getConfig('html');
    b.getConfig('content');
    if (e) {
      if (c) {
        b.element.classList.remove('b-hidden');
        b._hidden = false;
      }
      if (b.owner || ((a = e.nodeType ? e : document.getElementById(e)) == null ? void 0 : a.isConnected)) {
        b.render();
      } else {
        b.onFirstResizeAfterConnect = b.onFirstResizeAfterConnect.bind(b);
        ResizeMonitor.addResizeListener(e, b.onFirstResizeAfterConnect);
      }
    }
  }
  onFirstResizeAfterConnect(a) {
    ResizeMonitor.removeResizeListener(a, this.onFirstResizeAfterConnect);
    if (!this.isDestroyed && !this.rendered) {
      this.render();
    }
  }
  doDestroy() {
    let a, b, c, e;
    const f = this, {
        preExistingElements: g,
        element: j,
        adopt: k,
        _refListeners: l,
        _rootElement: n,
        eventRoot: o,
        ownedWidgets: p
      } = f;
    if (Fullscreen.element === j) {
      Fullscreen.exit();
    }
    if (l) {
      Object.values(l, q => q());
      f._refListeners = null;
    }
    if (j) {
      const q = !f._tooltip && n && ((a = _Widget.Tooltip) == null ? void 0 : a.getSharedTooltip(n, o, true));
      if ((q == null ? void 0 : q.owner) === f) {
        q.owner = null;
        q.hide();
      }
      f.onExitFullscreen();
      f.clearTimeout('scrollListenerTimeout');
      f.clearTimeout('resizeListenerTimeout');
      f.removeTransientListeners();
      if (f.floating || f.positioned || f.positionable) {
        f.isPainted && f.hide(false);
      } else {
        f.revertFocus();
      }
      ResizeMonitor.removeResizeListener(j.parentElement, f.onParentElementResize);
      ResizeMonitor.removeResizeListener(j, f.onElementResize);
      if (k) {
        for (let r = Array.from(j.childNodes), u = 0, {length: v} = r; u < v; u++) {
          const w = r[u];
          if (!g.includes(w) && w !== f.floatRoot) {
            w.remove();
          }
        }
        j.className = f.adoptedElementClassName;
        j.style.cssText = f.adoptedElementCssText;
      }
      (b = f.dragEventDetacher) == null ? void 0 : b.call(f);
      (c = f.dragOverEventDetacher) == null ? void 0 : c.call(f);
      f.dragGhost.remove();
    }
    (e = f.connectedObserver) == null ? void 0 : e.disconnect();
    super.doDestroy();
    p && Object.values(p).forEach(z => {
      let A;
      return (A = z.destroy) == null ? void 0 : A.call(z);
    });
    o && Promise.resolve().then(() => {
      if (!Object.values(_Widget.identifiable.idMap).some(isSingletonWidget)) {
        GlobalEvents_default.detachFocusListeners(o);
      }
    });
    if (!k) {
      j.remove();
    }
  }
  get assignValueDefaults() {
    return assignValueDefaults;
  }
  get valueName() {
    return this.name || this.ref || this.id;
  }
  getValueName(a) {
    a = a && typeof a === 'object' ? a.onlyName : a;
    return a ? this.name : this.valueName;
  }
  assignFieldValue(a, b, c) {
    const e = this, f = e.defaultBindProperty;
    if (f in e) {
      e[f] = c;
    }
  }
  assignValue(a, b = assignValueDefaults) {
    const c = this, e = c[highlightExternalChange], f = c.getValueName(b);
    if (f && (!a || f in a)) {
      if (b.highlight === false) {
        c[highlightExternalChange] = false;
      }
      c.assignFieldValue(a, f, a ? a[f] : null);
      c[highlightExternalChange] = e;
    }
  }
  gatherValue(a) {
    const b = this, c = b.defaultBindProperty;
    if (b.constructor !== _Widget && c in b) {
      a[b.name || b.ref || b.id] = b[c];
    }
  }
  gatherValues(a) {
    this.eachWidget(b => b.gatherValue(a), false);
  }
  addToOverflowMenu(a, b, c) {
    const e = this.ensureOverflowTwin(b, c);
    a.items.push(e);
  }
  get twinForwardEventPairs() {
    const a = this._twinForwardEvents;
    return a ? ObjectHelper.entries(a) : EMPTY;
  }
  configureOverflowTwin(a) {
    const b = this, {twinSyncConfigs: c} = b;
    let e = ObjectHelper.clone(b.initialConfig), f, g;
    delete e.id;
    delete e.listeners;
    delete e.internalListeners;
    delete e.parent;
    e.hidden = false;
    e.type = b.type;
    e._overflowTwinOrigin = b;
    e.owner = b.owner;
    b.twinForwardEventPairs.forEach(([i]) => {
      delete e['on' + StringHelper.capitalize(i)];
    });
    if (c) {
      for (f in c) {
        g = c[f];
        if (g) {
          delete e[f];
          e[typeof g === 'string' ? g : f] = b[f];
        }
      }
    }
    if (a) {
      e = typeof a === 'function' ? a(e) || e : ObjectHelper.assign(e, a);
    }
    return e;
  }
  createOverflowTwin(a) {
    const b = this, c = b.configureOverflowTwin(a), e = _Widget.create(c), {twinSyncConfigs: f} = b;
    f && b.bindConfigs(e, f, NO_SYNC);
    b.twinForwardEventPairs.forEach(([g, i]) => {
      e.ion({
        [g]: j => {
          if (b.overflowTwin === j.source) {
            j = ObjectHelper.assign({}, j);
            delete j.source;
            b.trigger(typeof i === 'string' ? i : j.eventName, j);
          }
        }
      });
    });
    return e;
  }
  ensureOverflowTwin(a, b) {
    let {overflowTwin: c} = this;
    if (!c) {
      this.overflowTwin = c = this.createOverflowTwin(a);
      b == null ? void 0 : b(c);
    }
    return c;
  }
  get isComposable() {
    return !this.compose.$nullFn;
  }
  adoptElement(a) {
    const b = this, c = typeof a === 'string' ? document.getElementById(a) : a, e = _Widget.fromElement(c);
    if (e && e.adopt && e !== b) {
      const f = typeof e.adopt === 'string' ? document.getElementById(e.adopt) : e.adopt;
      if (f === c) {
        e.destroy();
      }
    }
    b.preExistingElements = Array.from(c.childNodes);
    b.adoptedElementClassName = c.className;
    b.adoptedElementCssText = c.style.cssText;
    c.lastDomConfig = null;
    c.$refOwnerId = b.id;
    c.className = c.style.cssText = '';
    return c;
  }
  addRefAccessor(a, b) {
    const {prototype: c} = this.constructor;
    defineProperty6(c, b, {
      'writable': true,
      'value': null
    });
    defineProperty6(c, a, {
      'get'() {
        this.recompose.flush();
        return this[b];
      },
      'set'(e) {
        this[b] = e;
      }
    });
  }
  attachRef(a, b, c) {
    const e = this, f = '_' + a;
    b.dataset && (b.dataset.ownerCmp = e.id);
    if (e.isComposable) {
      if (!(f in e)) {
        e.addRefAccessor(a, f);
      }
      addElementListeners(e, b, c, a);
    }
    e.byRef[a] = b;
    e[a] = b;
  }
  detachRef(a, b, c) {
    const e = this, f = e._refListeners;
    if (f == null ? void 0 : f[a]) {
      f[a]();
      delete f[a];
    }
    e[a] = null;
    delete e.byRef[a];
  }
  afterRecompose() {
  }
  compose() {
    const {widgetClassList: a} = this;
    return { 'class': { ...DomClassList.normalize(a, 'object') } };
  }
  doCompose() {
    const a = this, {$meta: b} = a, e = b.hierarchy, f = b.renderConfigs || b.getInherited('renderConfigs');
    let {composers: g} = b, j = null, k, l, n, o;
    a.recompose.suspend();
    if (!g) {
      b.composers = g = [];
      for (n = e.indexOf(_Widget); n < e.length; ++n) {
        o = e[n].prototype;
        if (hasOwn6(o, 'compose')) {
          g.push(o);
        }
      }
      if (!hasOwn6(f, renderConfigObserver)) {
        f[renderConfigObserver] = {
          'get'(p) {
            if (p !== 'element') {
              f[p] = true;
            }
          }
        };
      }
    }
    a.configObserver = f[renderConfigObserver];
    for (n = 0; n < g.length; ++n) {
      k = g[n].compose.call(a, j);
      j = j ? DomHelper.merge(j, k) : k;
    }
    if (hasOwn6(a, 'compose') && (k = a.compose)) {
      k = k.call(a, j);
      DomHelper.merge(j, k);
    }
    a.configObserver = null;
    return DomHelper.normalizeChildren(j, (p, q) => {
      if (q) {
        l = '_' + p;
        if (!(l in a)) {
          a.addRefAccessor(p, l);
        }
      }
    });
  }
  get element() {
    if (this.isComposable && !this.isDestroying) {
      this.recompose.flush();
    }
    return this._element;
  }
  domSyncCallback(a) {
  }
  changeElement(a) {
    const b = this, c = b.isComposable, {adopt: e} = b;
    b.id = b.id || (e == null ? void 0 : e.id) || null;
    if (e) {
      a = b.adoptElement(e);
    } else {
      if (c) {
        a = b.doCompose();
      }
    }
    if (typeof a === 'string') {
      a = DomHelper.createElementFromTemplate(a);
    } else if (ObjectHelper.isObject(a)) {
      a = DomHelper.createElement(a, {
        'refOwner': b,
        'callback': b.domSyncCallback
      });
      b.recompose.resume();
    } else if (DomHelper.isReactElement(b.peekConfig('html'))) {
      a = document.createElement('div');
    } else if (a.nodeType !== 1) {
      a = DomHelper.createElementFromTemplate(b.template(b));
    }
    !a.id && (a.id = b.id);
    if (b.elementAttributes) {
      DomHelper.setAttributes(a, b.elementAttributes);
    }
    return a;
  }
  updateElement(a) {
    const b = this, c = b.isComposable;
    if (b.adopt) {
      b._hidden = true;
      c && b.recomposeNow();
    }
    c && addElementListeners(b, a, a.lastDomConfig);
    const {className: e} = a, {
        contentElement: f,
        contentElementCls: g,
        isComposable: j
      } = b, k = f !== a, l = !j && a.querySelectorAll('[data-reference]'), n = j ? [] : b.widgetClassList;
    let o = (k || !j) && b.uiClasses;
    e && n.unshift(e);
    b._hidden && n.push('b-hidden', 'b-hide-mode-' + b.hideMode);
    b._readOnly && n.push('b-readonly');
    FunctionHelper.noThrow(a, 'remove');
    if (o) {
      if (g == null ? void 0 : g.value) {
        o = o.slice();
        o.push(g.value);
      }
      o = o.join(' ');
    } else {
      o = g == null ? void 0 : g.value;
    }
    if (o) {
      if (k) {
        f.className += ' ' + o;
      } else {
        n.push(o);
      }
    }
    if (!b.parent) {
      const {defaultCls: p} = b, {outerCls: q} = _Widget;
      n.push(...p ? q.filter(r => !(r in p) || p[r]) : q);
    }
    a.className = n.join(' ');
    if (l) {
      for (let r, u = 0; u < l.length; ++u) {
        r = l[u];
        b.attachRef(r.getAttribute('data-reference'), r);
        r.removeAttribute('data-reference');
      }
    }
    if (!b.scaleToFitWidth) {
      b.getConfig('monitorResize');
    }
    b.getConfig('role');
    b.getConfig('ariaLabel');
    b.getConfig('ariaDescription');
    if (b._html) {
      b.getConfig('htmlMutationObserver');
    }
    b.trigger('elementCreated', { 'element': a });
  }
  updateAriaDescription(a) {
    const {ariaElement: b} = this, c = this.id + '-aria-desc-el';
    if (a) {
      const e = this._ariaDescEl || (this._ariaDescEl = DomHelper.createElement({
        'className': 'b-aria-desc-element',
        'id': c,
        'parent': b
      }));
      e.innerText = a.match(localizeRE) ? this.L(a, this) : a;
      DomHelper.addAttributeValue(b, 'aria-describedBy', c);
    } else {
      DomHelper.removeAttributeValue(b, 'aria-describedBy', c);
    }
  }
  updateAriaLabel(a) {
    DomHelper.setAttributes(this.ariaElement, { 'aria-label': (a == null ? void 0 : a.match(localizeRE)) ? this.L(a, this) : a });
  }
  updateAriaHasPopup(a) {
    DomHelper.setAttributes(this.ariaElement, { 'aria-haspopup': a });
  }
  updateAriaLive(a) {
    DomHelper.setAttributes(this.ariaElement, { 'aria-live': a });
  }
  updateRole(a) {
    let b, c;
    if (a) {
      (b = this.ariaElement) == null ? void 0 : b.setAttribute('role', a);
    } else {
      (c = this.ariaElement) == null ? void 0 : c.removeAttribute('role');
    }
  }
  get ariaElement() {
    this.getConfig('element');
    const {_ariaElement: a} = this;
    return a.nodeType === Node.ELEMENT_NODE ? a : ObjectHelper.getPath(this, a);
  }
  isCollapsified({
    collapsed: a,
    alt: b
  }) {
    const {collapsify: c} = this, e = c === 'hide', f = c === false, g = c == null, i = c === 'overlay';
    return b ? i || g && a : f || e || g && !a;
  }
  async recompose() {
    return this.recomposeInternal();
  }
  async recomposeInternal() {
    const a = this, b = {
        'targetElement': a.element,
        'domConfig': a.doCompose(),
        'refOwner': a,
        'callback': a.domSyncCallback,
        'strict': true
      };
    if (a.transitionRecompose) {
      a.isTransitioningDom = true;
      await DomHelper.transition(ObjectHelper.assign({
        'element': a.element,
        'action'() {
          DomSync.sync(b);
        }
      }, a.transitionRecompose));
      if (a.isDestroyed) {
        return;
      }
      a.isTransitioningDom = false;
      a.trigger('transitionedRecompose');
    } else {
      DomSync.sync(b);
    }
    if (b.changed) {
      a.afterRecompose();
      a.trigger('recompose');
    }
    a.resumeRecompose();
  }
  resumeRecompose() {
    this.recompose.resume();
  }
  changeElementRef(a) {
    if (typeof a === 'string') {
      const b = a;
      if (!(a = document.getElementById(b))) {
        throw new Error('No element found with id \'' + b + '\'');
      }
    }
    return a;
  }
  changeAppendTo(a) {
    return this.changeElementRef(a);
  }
  updateAppendTo(a) {
    if (!this.isConfiguring && a) {
      this.render();
    }
  }
  changeInsertBefore(a) {
    return this.changeElementRef(a);
  }
  updateInsertBefore(a) {
    if (!this.isConfiguring && a) {
      this.render();
    }
  }
  changeInsertFirst(a) {
    return this.changeElementRef(a);
  }
  updateInsertFirst(a) {
    if (!this.isConfiguring && a) {
      this.render();
    }
  }
  getRenderContext(a = this, b) {
    let c = b || a.appendTo, {
        insertFirst: e,
        insertBefore: f
      } = a;
    if (e) {
      c = e;
      f = c.firstChild;
    }
    if (f) {
      c = f.parentElement;
    }
    return [
      c,
      f || void 0
    ];
  }
  render(a, b = true) {
    let c;
    const e = this, {element: f} = e, [g, i] = e.getRenderContext(e, a);
    e.emptyCache();
    if (e.syncElement && e.currentElement) {
      DomHelper.sync(f, e.currentElement);
    } else {
      g == null ? void 0 : g.insertBefore(f, i);
      e.currentElement = f;
    }
    if ((c = f.parentElement) == null ? void 0 : c.closest('.b-widget')) {
      f.classList.remove(..._Widget.outerCls);
    }
    super.render(g, b);
    e.rendered = true;
    e.getConfig('role');
    if (b) {
      e.getConfig('contentRange');
      e.triggerPaint();
    }
    e.setupFocusListeners();
  }
  template({
    tag: a,
    html: b,
    htmlCls: c,
    name: e
  }) {
    const f = this;
    if (typeof b === 'string' && callbackRe.test(b) && f.resolveCallback(b, f, false)) {
      b = f.callback(b, f, [f]);
    }
    const g = (b == null ? void 0 : b.call) ? b.call(f, f) : b;
    return '<' + a + ' class="' + (g ? c : '') + '" ' + (e ? 'data-name="' + e + '"' : '') + '>' + (g || '') + '</' + a + '>';
  }
  updateRecomposeAsync(a) {
    this.recompose.immediate = !a;
  }
  onConfigChange({name: a}) {
    let b;
    if (this._element && !this.isDestroying && ((b = this.$meta.renderConfigs) == null ? void 0 : b[a])) {
      this.recompose();
    }
  }
  preProcessCurrentConfigs(a) {
    super.preProcessCurrentConfigs(a);
    delete a.parent;
  }
  getConfigValue(a, b) {
    if (a === 'style') {
      return this._style;
    }
    return super.getConfigValue(a, b);
  }
  getCurrentConfig(a) {
    const b = super.getCurrentConfig(a);
    if ((a == null ? void 0 : a.depth) > 0) {
      b.type = this.type;
    }
    return b;
  }
  get dataset() {
    return this.element.dataset;
  }
  changeDataset(a) {
    ObjectHelper.assign(this.dataset, a);
  }
  get dragGhost() {
    return this.constructor._dragGhost || (this.constructor._dragGhost = DomHelper.createElement({
      'html': '\xA0',
      'style': 'position:absolute;top:-10000em;left:-10000em'
    }));
  }
  updateParent(a) {
    const {_element: b} = this;
    if (b) {
      b.classList[a ? 'remove' : 'add'](..._Widget.outerCls);
    }
  }
  get constrainTo() {
    let a = this._constrainTo;
    a = a === void 0 ? globalThis : (a == null ? void 0 : a.nodeType) === Node.DOCUMENT_FRAGMENT_NODE ? a.host || a.ownerDocument : a;
    if (this.positioned) {
      const {offsetParent: b} = this.element;
      if (b && DomHelper.getStyleValue(b, 'overflow') === 'hidden') {
        a = Rectangle.from(b).moveTo(0, 0);
      } else if (a && !a.isRectangle) {
        const c = a === document || a === globalThis;
        a = Rectangle.from(a, b);
        if (c) {
          a.translate(globalThis.pageXOffset, globalThis.pageYOffset);
        }
      }
    }
    return a;
  }
  updateCentered(a) {
    const b = this, {
        element: c,
        _anchorElement: e
      } = b;
    if (a && !b.floating && !b.positioned) {
      throw new Error('`centered` is only relevant when a Widget is `floating` or `positioned`');
    }
    if (a) {
      c.classList.add('b-centered');
      c.style.transform = c.style.left = c.style.insetInlineStart = c.style.right = c.style.top = '';
      e == null ? void 0 : e.classList.add('b-hide-display');
      c.classList.remove('b-anchored');
    } else {
      c.classList.remove('b-centered');
    }
  }
  get contentElement() {
    return this.element;
  }
  get contentRange() {
    const a = this, b = a._contentRange || (a._contentRange = new Range());
    if (b.collapsed) {
      if (a.initializingHtml && a.adopt) {
        b.setStart(a.contentElement, 0);
      } else {
        b.selectNodeContents(a.contentElement);
      }
    }
    return b;
  }
  fixRefOwnerId(a, b, c) {
    if (a.$refOwnerId === c) {
      a.$refOwnerId = b;
      for (let {childNodes: e} = a, f = e.length; f-- > 0;) {
        this.fixRefOwnerId(e[f], b, c);
      }
    }
  }
  get placement() {
    let a;
    const b = this, {element: c} = b;
    let e, f, g;
    if ((c == null ? void 0 : c.offsetParent) && !nonFlowedPositions.test(DomHelper.getStyleValue(c, 'position'))) {
      const i = c.nextElementSibling, j = c.previousElementSibling, k = !i && j;
      f = DomHelper.getStyleValue(c.parentElement, 'flex-direction');
      if (f) {
        f = f.startsWith('row') ? 'h' : 'v';
      } else {
        e = (a = i || j) == null ? void 0 : a.getBoundingClientRect();
        g = e && c.getBoundingClientRect();
        f = e && Math.abs(e.top - g.top) < Math.abs(e.left - g.left) ? 'h' : 'v';
      }
      f += f === 'h' ? k ? 'r' : 'l' : k ? 'b' : 't';
    }
    return f;
  }
  updateId(a, b) {
    super.updateId(a, b);
    if (b) {
      const {
        byRef: c,
        element: e
      } = this;
      for (const f in c) {
        c[f].dataset && (c[f].dataset.ownerCmp = a);
      }
      e.id = a;
      this.fixRefOwnerId(e, a, b);
    }
  }
  get style() {
    const {element: a} = this;
    return (a == null ? void 0 : a.ownerDocument.defaultView.getComputedStyle(a)) || this._style;
  }
  updateStyle(a) {
    this.element && DomHelper.applyStyle(this.element, a);
  }
  updateTitle(a) {
    if (this.titleElement) {
      this.titleElement.innerHTML = a;
    }
  }
  beforeAlignTo(a) {
  }
  alignTo(a) {
    let b, c;
    const e = this, {
        lastAlignSpec: f,
        element: g
      } = e, {
        offsetParent: i,
        style: j,
        classList: k
      } = g;
    if (f) {
      f.monitorIntersection && e.intersectionObserver.unobserve(f.target);
      if (isFinite(f.zone)) {
        g.classList.remove(alignedClass[f.zone]);
      }
    }
    if (!a) {
      e.removeTransientListeners();
      e.anchor = e.lastAlignSpec = null;
      return;
    }
    e.beforeAlignTo(a);
    if (a.nodeType === Element.ELEMENT_NODE || a.isWidget || a.$$name === 'Point') {
      a = { 'target': a };
    }
    e.releaseSizeConstraints();
    const {
        scrollable: l,
        constrainTo: n
      } = e, o = DomHelper.measureSize(DomHelper.getStyleValue(g, 'minHeight'), g), p = DomHelper.measureSize(DomHelper.getStyleValue(g, 'minWidth'), g), q = e.positioned && DomHelper.getStyleValue(g, 'position') !== 'fixed', r = e.scale || 1, u = a.target, v = a.realignEvent || getEventTarget(a.domEvent || a.event || (((b = u == null ? void 0 : u.target) == null ? void 0 : b.nodeType) === Node.ELEMENT_NODE ? u : null)), w = ((c = v == null ? void 0 : v.target) == null ? void 0 : c.nodeType) === Element.ELEMENT_NODE, z = w ? v.target : (u == null ? void 0 : u.isRectangle) ? u : (u == null ? void 0 : u.element) || u, A = (z == null ? void 0 : z.nodeType) === Element.ELEMENT_NODE;
    a = a.realignTarget ? ObjectHelper.clone(a) : ObjectHelper.merge({
      'aligningToEvent': w,
      'aligningToElement': A,
      'constrainTo': n,
      'align': 'b-t',
      'axisLock': e.axisLock,
      'anchor': e.anchor
    }, e.align, a);
    if (w) {
      a.domEvent = v;
    }
    const B = a.minWidth || p, C = a.minHeight || o;
    e.lastAlignSpec = a;
    if (A) {
      a.target = a.anchoredTo = z;
      if (hasLayout(z instanceof SVGElement ? z.closest('svg') : z)) {
        a = Object.setPrototypeOf({}, a);
        a.target = e.lastAlignSpec.targetRect = a.realignTarget || (a.allowTargetOut ? Rectangle.from(z, q ? i : null, !q) : DomHelper.isInView(w ? v : z, false, e));
        if (!a.target) {
          const Q = e.hide();
          if (!e.isDestroyed) {
            e.lastAlignSpec.targetOutOfView = true;
          }
          return Q;
        }
        a.target.height = Math.max(a.target.height, 1);
        a.target.width = Math.max(a.target.width, 1);
      }
    }
    if (a.anchor) {
      a.anchorSize = e.anchorSize;
      if (!g.contains(e.anchorPathElement)) {
        g.appendChild(e.anchorElement);
      }
    }
    if (typeof a.offset === 'string') {
      const R = g.querySelector(a.offset), S = Rectangle.from(R), T = S.getAlignmentPoint(a.align), U = Rectangle.from(g), V = U.getAlignmentPoint(a.align);
      a.offset = [
        V[0] - T[0],
        V[1] - T[1]
      ];
    }
    e.isAligning = true;
    e.trigger('beforeAlign', a);
    const E = Rectangle.from(g, q ? i : null, true), {
        width: F,
        height: G
      } = E;
    E.isAlignRectangle = true;
    B && (E.minWidth = B * r);
    C && (E.minHeight = C * r);
    e.isAligning = false;
    a.rtl = e.rtl;
    if (a.align.includes('s') || a.align.includes('e')) {
      if (e.rtl) {
        a.align = a.align.replace(/s/g, 'r').replace(/e/g, 'l');
      } else {
        a.align = a.align.replace(/s/g, 'l').replace(/e/g, 'r');
      }
    }
    const H = e.lastAlignSpec.result = E.alignTo(a);
    let {
      align: I,
      anchor: J,
      x: K,
      y: L,
      width: N,
      height: O,
      overlap: P
    } = H;
    e.lastAlignSpec.zone = H.zone;
    if (O != G) {
      const W = O < G;
      e.alignmentChanges = e.alignmentChanges | (W ? 1 : 2);
      j[e.alignedHeightStyle = alignChangeDims[e.alignmentChanges & 3]] = O / r + 'px';
      if (W && !e._minHeight && o) {
        j.minHeight = Math.min(O, o) / r + 'px';
      }
      if (l) {
        l.overflowY = W;
      }
    }
    if (N != F) {
      const X = N < F;
      e.alignmentChanges = e.alignmentChanges | (X ? 4 : 8);
      j[e.alignedWidthStyle = alignChangeDims[e.alignmentChanges & 12]] = N / r + 'px';
      if (X && !e._minWidth && p) {
        j.minWidth = Math.min(N, p) / r + 'px';
      }
      if (l) {
        l.overflowX = X;
      }
    }
    if (I && e.alignmentChanges) {
      const Y = Rectangle.from(g, q ? i : null, true);
      a.align = I;
      const Z = e.lastAlignSpec.result = Y.alignTo(a);
      J = Z.anchor;
      K = Z.x;
      L = Z.y;
      N = Z.width;
      O = Z.height;
    }
    if (!e.centered) {
      e.setXY(K, L);
    }
    if (!H.overlap && isFinite(H.zone)) {
      k.add(alignedClass[H.zone]);
    }
    if (J == null ? void 0 : J.edge) {
      let {edge: a0} = J;
      const {anchorElement: a1} = e, {style: a2} = a1, {style: a3} = a1.firstChild, a4 = Rectangle.from(g), a5 = [];
      if (a0 === 'top' || a0 === 'bottom') {
        a5[0] = J.x;
        a5[1] = a0 === 'top' ? 1 : a4.height - 1;
      } else {
        a5[0] = a0 === 'left' ? 1 : a4.width - 1;
        a5[1] = J.y;
      }
      a2.display = 'none';
      let a6 = DomHelper.childFromPoint(g, ...a5);
      if (((a6 == null ? void 0 : a6.matches(':focus')) || a6 === GlobalEvents_default.currentOverElement) && g.compareDocumentPosition(a6) & Node.DOCUMENT_POSITION_CONTAINED_BY) {
        a6 = a6.parentNode;
      }
      if (a6 && a6 !== document) {
        let a7 = DomHelper.getStyleValue(a6, 'background-color');
        while (a7.match(isTransparent) && DomHelper.getStyleValue(a6, 'position') !== 'absolute') {
          a6 = a6.parentNode;
          if (a6 === document) {
            break;
          }
          a7 = DomHelper.getStyleValue(a6, 'background-color');
        }
        if (a7.match(isTransparent)) {
          e.anchorPathElement.setAttribute('fill', e.defaultAnchorBackgroundColor);
        } else {
          e.anchorPathElement.setAttribute('fill', a7);
        }
      }
      a2.transform = a2.display = '';
      if (a0 === 'left' || a0 === 'right') {
        a0 = a0 === 'left' ? e.rtl ? 'end' : 'start' : e.rtl ? 'start' : 'end';
      }
      a1.className = 'b-anchor b-anchor-' + a0;
      J.x && (J.x /= r);
      J.y && (J.y /= r);
      a3.left = (J.x || 0) + 'px';
      a3.translate = '0 ' + (J.y || 0) + 'px';
      k.add('b-anchored');
    } else if (e._anchorElement) {
      e.anchorElement.classList.add('b-hide-display');
      k.remove('b-anchored');
    }
    if (!A) {
      j.pointerEvents = 'none';
      const a8 = DomHelper.elementFromPoint(K, L);
      if (!e.owns(a8)) {
        e.lastAlignSpec.anchoredTo = a8;
      }
      j.pointerEvents = '';
    }
    if ((e.scrollAction === 'realign' && A || e.scrollAction === 'hide') && !e.documentScrollListener) {
      e.clearTimeout('scrollListenerTimeout');
      if (a.monitorTargetMutation && !e.targetObserver) {
        const a9 = e.targetObserver || (e.targetObserver = new MutationObserver(e.onTargetParentMutation.bind(e)));
        a9.observe(DomHelper.getRootElement(z), {
          'childList': true,
          'attributes': true,
          'subtree': true
        });
      }
      e.setTimeout(() => {
        const aa = A ? z : e.lastAlignSpec.anchoredTo, ab = aa ? DomHelper.getRootElement(aa) : null;
        e.documentScrollListener = EventHelper.addListener({
          'element': document,
          'scroll': 'doHideOrRealign',
          'transitionend': 'realignOnTransitionEnd',
          'capture': true,
          'thisObj': e
        });
        if (ab == null ? void 0 : ab.mode) {
          e.targetRootScrollListener = EventHelper.addListener({
            'element': ab,
            'scroll': 'doHideOrRealign',
            'transitionend': 'realignOnTransitionEnd',
            'capture': true,
            'thisObj': e
          });
        }
      }, e.scrollAction === 'hide' ? e.ignoreScrollDuration : 0, 'scrollListenerTimeout');
    }
    if (A) {
      if (a.monitorResize && !e.observedTargetEl) {
        ResizeMonitor.addResizeListener(z, e.onTargetResize);
        e.observedTargetEl = z;
      }
      if (a.monitorIntersection && !(P || z.contains(g) || z.ownerSVGElement)) {
        e.intersectionObserver.observe(z);
      }
    }
    if (!BrowserHelper.isAndroid) {
      if (!e.observedConstrainEl) {
        const aa = (n == null ? void 0 : n.isRectangle) ? globalThis : n;
        e.clearTimeout('resizeListenerTimeout');
        e.setTimeout(() => {
          e.observedConstrainEl = aa || globalThis;
          ResizeMonitor.addResizeListener(e.observedConstrainEl, e.onAlignConstraintChange);
        }, e.ignoreScrollDuration, 'resizeListenerTimeout');
      }
    }
  }
  onTargetParentMutation(a) {
    let b;
    const {
      element: c,
      lastAlignSpec: e
    } = this;
    if (e == null ? void 0 : e.aligningToElement) {
      if (!((b = e.target) == null ? void 0 : b.isConnected)) {
        this.hide();
      } else if (a.some(({target: f}) => f.contains(e.target) && !c.contains(f))) {
        this.realign();
      }
    }
  }
  get intersectionObserver() {
    return this._intersectionObserver || (this._intersectionObserver = new IntersectionObserver(this.onTargetIntersectionchange.bind(this), { 'root': BrowserHelper.isSafari ? this.rootElement : this.rootElement.ownerDocument }));
  }
  onTargetIntersectionchange(a) {
    if (!this.isDestroyed) {
      const b = a[a.length - 1];
      if (!b.isIntersecting) {
        this.onAlignTargetOutOfView(b.target);
      }
    }
  }
  onTargetResize() {
    const {lastAlignSpec: a} = this;
    if (a) {
      const {
          width: b,
          height: c
        } = a.targetRect, {
          width: e,
          height: f
        } = a.target.getBoundingClientRect();
      if (e !== b || f !== c) {
        this.onAlignConstraintChange(...arguments);
      }
    }
  }
  onAlignTargetOutOfView(a) {
    this.hide();
    this.lastAlignSpec && (this.lastAlignSpec.targetOutOfView = true);
  }
  onAlignConstraintChange(a, b, {height: c}) {
    const {style: e} = this.contentElement, {overflowY: f} = e;
    this.setTimeout(this.realign, 50);
    if (b && c > b.height) {
      e.overflowY = 'hidden';
      this.requestAnimationFrame(() => e.overflowY = f);
    }
  }
  realign() {
    const a = this, {lastAlignSpec: b} = a;
    if ((a.floating || a.positioned) && b && a.isVisible) {
      if (b.aligningToElement) {
        const c = b.target.contains(this.element), e = DomHelper.isInView(b.target, false, a);
        if (!b.allowTargetOut && (!hasLayout(b.target) || !e)) {
          a.hide();
          if (!a.isDestroyed) {
            a.lastAlignSpec.targetOutOfView = true;
          }
          return;
        }
        b.realignEvent = b.domEvent;
        b.realignTarget = c || b.aligningToEvent ? null : e;
      }
      DomHelper.addTemporaryClass(a.element, 'b-realigning', a.realignTimeout, a);
      a.alignTo(b);
    }
  }
  rectangle(a, b, c) {
    return this.rectangleOf('element', a, b, c);
  }
  rectangleOf(a, b, c, e) {
    if (typeof b !== 'string') {
      e = c;
      c = b;
      b = '';
    } else if (b === 'border') {
      b = '';
    }
    if (typeof c === 'boolean') {
      e = c;
      c = void 0;
    }
    if (a !== 'element' && c === void 0) {
      c = this.element;
    }
    c = (c == null ? void 0 : c.isWidget) ? c.element : c;
    if (typeof a === 'string') {
      a = this[a];
    }
    return Rectangle[b || 'from'](a, c, e);
  }
  releaseSizeConstraints() {
    const a = this, {
        scrollable: b,
        element: c,
        alignmentChanges: e
      } = a, {style: f} = c;
    if (e & 12) {
      DomHelper.setLength(c, a.alignedWidthStyle, a['_last' + StringHelper.capitalize(a.alignedWidthStyle)] || '');
      f.minWidth = a._minWidth || '';
      if (b) {
        b.overflowY = b.config.overflowY;
      }
    }
    if (e & 3) {
      DomHelper.setLength(c, a.alignedHeightStyle, a['_last' + StringHelper.capitalize(a.alignedHeightStyle)] || '');
      f.minHeight = a._minHeight || '';
      if (b) {
        b.overflowX = b.config.overflowX;
      }
    }
    a.alignmentChanges = 0;
  }
  toFront() {
    const {element: a} = this, b = this.floating ? this.floatRoot : this.positioned ? a == null ? void 0 : a.parentNode : null, c = document.createDocumentFragment();
    if (this.containsFocus) {
      for (let e = a.nextSibling, f; e; e = f) {
        f = e.nextSibling;
        if (b.contains(e) && !this.owns(e)) {
          c.appendChild(e);
        }
      }
      b.insertBefore(c, a);
    } else {
      b.appendChild(a);
    }
  }
  updateRef(a) {
    this.element.dataset.ref = a;
  }
  get overflowElement() {
    return this.contentElement;
  }
  get maxHeightElement() {
    return this.element;
  }
  changeAlign(a) {
    return typeof a === 'string' ? { 'align': a } : a;
  }
  changeScrollable(a, b) {
    if (typeof a === 'boolean') {
      a = {
        'overflowX': a,
        'overflowY': a
      };
    }
    if (a) {
      a.element = this.overflowElement;
      a.widget = this;
      if (!a.isScroller) {
        a = b ? b.setConfig(a) : new this.scrollerClass(a);
      }
      a.syncOverflowState();
    } else {
      b == null ? void 0 : b.destroy();
    }
    return a;
  }
  handleReactElement(a) {
    let b;
    const c = this.closest(f => f.reactComponent), e = (c == null ? void 0 : c.reactComponent) || ((b = bryntum == null ? void 0 : bryntum.query(f => {
        let g;
        return !!((g = f.reactComponent) == null ? void 0 : g.state);
      })) == null ? void 0 : b.reactComponent);
    e == null ? void 0 : e.processWidgetContent({
      'reactElement': a,
      'widget': this,
      'reactComponent': e
    });
  }
  get html() {
    if (this.isConfiguring) {
      return this.content || this._html;
    }
    return this.contentElement.innerHTML;
  }
  changeHtml(a) {
    if (a && typeof a === 'object' && !DomHelper.isReactElement(a) && !Array.isArray(a)) {
      a = [a];
    }
    return a;
  }
  updateHtml(a) {
    const b = this;
    b.noChildElements = typeof a === 'string' && !htmlRe3.test(a);
    if (!b.initializingElement) {
      b.insertContent(a, !(b.initializingHtml && b.adopt));
    }
  }
  changeHtmlMutationObserver(a, b) {
    const c = this, {contentElement: e} = c;
    b == null ? void 0 : b.disconnect();
    if (a) {
      const f = new MutationObserver(() => {
        if (c.updatingHtml) {
          c.updatingHtml = false;
        } else {
          c._html = e.innerHTML;
        }
      });
      f.observe(e, a);
      return f;
    }
  }
  updateContent(a) {
    if (!this.initializingElement) {
      this.insertContent(a);
    }
  }
  insertContent(a, b = false) {
    const c = this, {
        element: e,
        contentElement: f,
        htmlCls: g,
        contentRange: i,
        autoClose: j
      } = c, k = b && f === e && c._anchorElement;
    if (DomHelper.isReactElement(a)) {
      return c.handleReactElement(a);
    }
    c.updatingHtml = true;
    c.getConfig('htmlMutationObserver');
    if (k) {
      e.removeChild(k);
    }
    if (g) {
      g.values.forEach(l => e.classList[a ? 'add' : 'remove'](l));
    }
    c.autoClose = false;
    if (a) {
      let l, n;
      if (typeof a === 'object') {
        n = document.createDocumentFragment();
        if (Array.isArray(a)) {
          DomSync.sync({
            'domConfig': {
              'children': a,
              'onlyChildren': true
            },
            'targetElement': n
          });
        } else {
          DomSync.sync({
            'domConfig': {
              ...a,
              'onlyChildren': true
            },
            'targetElement': n
          });
        }
      } else if (b) {
        f.innerHTML = a;
        l = true;
      } else {
        if (!b) {
          i.deleteContents();
        }
        n = DomHelper.createElementFromTemplate(a, { 'fragment': true });
      }
      if (!l) {
        if (b) {
          f.innerHTML = '';
          f.appendChild(n);
        } else {
          if (e.isConnected) {
            i.insertNode(n);
          } else {
            f.insertBefore(n, f.firstChild);
          }
        }
      }
    } else {
      if (b || !e.isConnected) {
        f.innerHTML = '';
      } else {
        i.deleteContents();
      }
    }
    c.autoClose = j;
    if (k) {
      e.appendChild(k);
    }
    c.contentRangeStartOffset = i.startOffset;
    c.contentRangeEndOffset = i.endOffset;
    f.classList.toggle('b-no-child-elements', c.noChildElements);
    if (c.isComposable) {
      c.recompose();
    }
    if ((c.floating || c.positioned) && !c.isAligning) {
      c.realign();
    }
  }
  onThemeChange() {
    let a;
    this._anchorSize = null;
    if ((a = this.anchorElement) == null ? void 0 : a.offsetParent) {
      this.realign();
    }
  }
  get anchorSize() {
    const a = this;
    let b = this._anchorSize;
    if (!b) {
      const c = parseFloat(DomHelper.getStyleValue(a.element, 'border-top-width')), e = DomHelper.getStyleValue(a.element, 'border-top-color'), f = a.anchorElement, {className: g} = f, i = f.firstElementChild, j = a.anchorPathElement = i.lastElementChild, k = a._hidden;
      f.className = 'b-anchor b-anchor-top';
      let l = DomHelper.getStyleValue(a.contentElement, 'background-color');
      if (l.match(isTransparent)) {
        l = DomHelper.getStyleValue(a.element, 'background-color');
      }
      a.defaultAnchorBackgroundColor = l;
      b = i.getBoundingClientRect();
      const [n, o] = b = a._anchorSize = [
        b.width,
        b.height
      ];
      f.className = g;
      i.setAttribute('height', o + c);
      i.setAttribute('width', n);
      j.setAttribute('d', 'M0,' + o + 'L' + n / 2 + ',0.5L' + n + ',' + o);
      if (c) {
        j.setAttribute('stroke-width', c);
        j.setAttribute('stroke', e);
      }
      b[1] -= c;
      k && a.syncHiddenCls();
      if (!a.themeChangeListener) {
        a.themeChangeListener = GlobalEvents_default.ion({
          'theme': 'onThemeChange',
          'thisObj': a
        });
      }
      a.anchorPathElement.setAttribute('fill', a.defaultAnchorBackgroundColor);
    }
    return b;
  }
  get anchorElement() {
    const a = this;
    if (!a._anchorElement) {
      const b = a.floating || a.positioned, c = a.id + '-shadow-filter';
      a._anchorElement = DomHelper.createElement({
        'parent': a.element,
        'className': 'b-anchor b-anchor-top',
        'children': [{
            'tag': 'svg',
            'ns': 'http://www.w3.org/2000/svg',
            'version': '1.1',
            'class': 'b-pointer-el',
            'children': [
              b ? {
                'tag': 'defs',
                'children': [{
                    'tag': 'filter',
                    'id': c,
                    'children': [{
                        'tag': 'feDropShadow',
                        'dx': 0,
                        'dy': -1,
                        'stdDeviation': 1,
                        'flood-opacity': 0.2
                      }]
                  }]
              } : null,
              {
                'tag': 'path',
                [b ? 'filter' : '']: 'url(#' + c + ')'
              }
            ]
          }]
      });
    }
    return a._anchorElement;
  }
  updateAnchor(a) {
    if (this._anchorElement) {
      this._anchorElement.classList[a ? 'remove' : 'add']('b-hide-display');
    }
  }
  updateDraggable(a) {
    let b, c, e;
    const f = this, {element: g} = f;
    if (a) {
      f.dragEventDetacher = EventHelper.addListener({
        'element': g,
        'dragstart': 'onWidgetDragStart',
        'dragend': 'onWidgetDragEnd',
        'thisObj': f
      });
      f.dragDetacher = EventHelper.on({
        'element': g,
        'mousedown'(i) {
          const {target: j} = i, k = _Widget.fromElement(j);
          if (!i.target.closest('.b-field-inner') && k === this || this.strips && Object.values(this.strips).includes(k)) {
            g.setAttribute('draggable', 'true');
          }
        },
        'thisObj': f
      });
    } else {
      (b = f.dragEventDetacher) == null ? void 0 : b.call(f);
      (c = f.dragOverEventDetacher) == null ? void 0 : c.call(f);
      (e = f.dragDetacher) == null ? void 0 : e.call(f);
    }
  }
  onWidgetDragStart(a) {
    let b;
    const c = this;
    if (!c.validateDragStartEvent(a)) {
      return;
    }
    const {
        element: f,
        align: g,
        constrainTo: i
      } = c, j = c.positioned && DomHelper.getStyleValue(f, 'position') !== 'fixed', k = j ? f.parentElement : c.rootElement, l = Rectangle.from(f, j ? k : null), n = a.clientX, o = a.clientY, p = document.scrollingElement || document.body, [q, r] = j ? DomHelper.getOffsetXY(f, k) : c.getXY(), u = (b = j ? Rectangle.content(k).moveTo(0, 0) : i && ((i == null ? void 0 : i.isRectangle) ? i : Rectangle.from(i))) == null ? void 0 : b.deflate(g.constrainPadding || 0), v = {
        'element': k,
        'dragover': w => {
          f.classList.remove('b-centered');
          l.moveTo(q + w.clientX - n - (j ? 0 : p.scrollLeft), r + w.clientY - o - (j ? 0 : p.scrollTop));
          if (u) {
            l.constrainTo(u);
          }
          DomHelper.setTopInsetInlineStart(f, l.y, l.x);
        }
      };
    if (BrowserHelper.isTouchDevice) {
      v.touchmove = w => w.preventDefault();
    }
    c.floatRoot.appendChild(c.dragGhost);
    c.setDragImage(a);
    a.dataTransfer.effectAllowed = 'none';
    c.dragOverEventDetacher = EventHelper.addListener(v);
    c.alignTo();
  }
  validateDragStartEvent(a) {
    let b;
    const c = this, {element: f} = c, g = DomHelper.elementFromPoint(a.clientX, a.clientY), {handleSelector: i} = c.draggable;
    if (i) {
      const j = (b = negationPseudo.exec(i)) == null ? void 0 : b[1];
      if (j) {
        if (g.closest('#' + f.id + ' ' + j)) {
          a.preventDefault();
          return false;
        }
      } else if (!g.closest('#' + f.id + ' ' + i)) {
        a.preventDefault();
        return false;
      }
    }
    return true;
  }
  setDragImage(a) {
    if (a.dataTransfer.setDragImage) {
      a.dataTransfer.setData('application/node type', '');
      a.dataTransfer.setDragImage(this.dragGhost, -9999, -9999);
    }
  }
  setStyle(a, b) {
    DomHelper.applyStyle(this.element, ObjectHelper.isObject(a) ? a : { [a]: b });
    return this;
  }
  onWidgetDragEnd() {
    this.dragGhost.remove();
    this.dragOverEventDetacher();
    this.element.removeAttribute('draggable');
  }
  changeFloating(a) {
    return Boolean(a);
  }
  changePositioned(a) {
    return Boolean(a);
  }
  updatePositionable(a) {
    this.element.classList.toggle('b-positionable', Boolean(a));
    if (a) {
      this.innerItem = false;
    }
  }
  updatePositioned(a) {
    this.element.classList.toggle('b-positioned', Boolean(a));
    if (a) {
      this.innerItem = false;
    }
  }
  getXY() {
    const {element: a} = this;
    return [
      DomHelper.getPageX(a),
      DomHelper.getPageY(a)
    ];
  }
  setXY(a, b) {
    const c = this, {element: e} = c;
    if (c.floating || c.positioned) {
      if (a != null) {
        c._x = a;
      } else {
        a = c._x;
      }
      if (b != null) {
        c._y = b;
      } else {
        b = c._y;
      }
      if (DomHelper.getStyleValue(e, 'position') === 'fixed') {
        const f = e.getBoundingClientRect(), [g, i] = DomHelper.getTranslateXY(e), j = a - f.x, k = b - f.y;
        DomHelper.setTranslateXY(e, g + j, i + k);
      } else if (c.rtl) {
        DomHelper.setTopInsetInlineStart(e, b, e.offsetParent.offsetWidth - e.offsetWidth - a);
      } else {
        DomHelper.setTopInsetInlineStart(e, b, a);
      }
      if (c.isConstructing) {
        c.centered = false;
      } else {
        e.classList.remove('b-centered');
      }
    }
  }
  setPos(a, b) {
    const c = this, {positionable: e} = c;
    [a, b] = DomHelper.unitize(a, b != null ? b : '');
    c.element.style[a] = b;
    if (c.top == null && c.right == null && c.bottom == null && c.left == null) {
      if (e === 'after' || e === 'before') {
        c.positionable = null;
      }
    } else if (!e) {
      c.positionable = 'before';
    }
  }
  updateBottom(a) {
    this.setPos('bottom', a);
  }
  updateLeft(a) {
    this.setPos('inset-inline-start', a);
  }
  updateRight(a) {
    this.setPos('inset-inline-end', a);
  }
  updateTop(a) {
    this.setPos('top', a);
  }
  get x() {
    return this.getXY()[0];
  }
  changeX(a) {
    this.setXY(a);
  }
  get y() {
    return this.getXY()[1];
  }
  changeY(a) {
    this.setXY(null, a);
  }
  get width() {
    const a = this, b = a.element;
    if (a.monitorResize) {
      if (a._width == null) {
        a._width = b.offsetWidth;
      }
      return a._width;
    }
    return b.offsetWidth;
  }
  changeWidth(a) {
    const b = this;
    DomHelper.setLength(b.element, 'width', a);
    b._lastWidth = a;
    b._flex = null;
    b.element.style.flex = '';
    if (b.monitorResize) {
      b._oldWidth = b._width;
      b._width = null;
    } else {
      return a;
    }
  }
  clearWidthCache() {
    this._width = null;
  }
  get maxWidth() {
    return DomHelper.measureSize(this.element.style.maxWidth, this.element);
  }
  updateMaxWidth(a) {
    this._lastMaxWidth = a;
    DomHelper.setLength(this.element, 'maxWidth', a);
  }
  get minWidth() {
    return DomHelper.measureSize(this.element.style.minWidth, this.element);
  }
  updateMinWidth(a) {
    DomHelper.setLength(this.element, 'minWidth', a);
  }
  updateFlex(a) {
    this.getConfig('width');
    if (typeof a === 'number' || !isNaN(a)) {
      a = a + ' ' + a;
    }
    this.element.style.flex = a;
    this.element.style.width = '';
  }
  updateAlignSelf(a) {
    this.element.style.alignSelf = a;
  }
  updateMargin(a) {
    this.element.style.margin = this.parseTRBL(a).join(' ');
  }
  updateTextAlign(a, b) {
    b && this.element.classList.remove('b-text-align-' + b);
    this.element.classList.add('b-text-align-' + a);
  }
  updatePlaceholder(a) {
    if (this.input) {
      if (a == null) {
        this.input.removeAttribute('placeholder');
      } else {
        this.input.placeholder = a;
      }
    }
  }
  get height() {
    const a = this, b = a.element;
    if (a.monitorResize) {
      if (a._height == null) {
        a._height = b.offsetHeight;
      }
      return a._height;
    }
    return b.offsetHeight;
  }
  changeHeight(a) {
    DomHelper.setLength(this.element, 'height', a);
    this._lastHeight = a;
    this._height = null;
  }
  get maxHeight() {
    return DomHelper.measureSize(this.maxHeightElement.style.maxHeight, this.element);
  }
  updateMaxHeight(a) {
    this._lastMaxHeight = a;
    DomHelper.setLength(this.maxHeightElement, 'maxHeight', a);
  }
  get minHeight() {
    return DomHelper.measureSize(this.element.style.minHeight, this.element);
  }
  updateMinHeight(a) {
    DomHelper.setLength(this.element, 'minHeight', a);
  }
  updateDisabled(a = false, b) {
    const c = this, {
        element: e,
        focusElement: f,
        ariaElement: g
      } = c;
    c.trigger('beforeUpdateDisabled', { 'disabled': a });
    if (a) {
      c.revertFocus();
      if (!c._disabled !== !a) {
        return;
      }
    }
    if (e) {
      e.classList[a ? 'add' : 'remove']('b-disabled');
      if (a === 'inert') {
        e.inert = true;
      } else if (b === 'inert') {
        e.inert = false;
      }
      if (f) {
        f.disabled = a;
      }
      if (g) {
        g.setAttribute('aria-disabled', Boolean(a));
      }
    }
    c.onDisabled(a);
  }
  onDisabled(a) {
  }
  disable() {
    this.disabled = true;
  }
  enable() {
    this.disabled = false;
  }
  requestFullscreen() {
    const a = this, b = Fullscreen.request(a.floating ? a.floatRoot : a.element);
    Fullscreen.onFullscreenChange(a.onFullscreenChange);
    a.element.classList.add('b-fullscreen');
    return b;
  }
  exitFullscreen() {
    return Fullscreen.exit();
  }
  onFullscreenChange() {
    if (!Fullscreen.isFullscreen) {
      this.onExitFullscreen();
    }
  }
  onExitFullscreen() {
    Fullscreen.unFullscreenChange(this.onFullscreenChange);
    this.element.classList.remove('b-fullscreen');
  }
  get tooltip() {
    let a;
    const b = this;
    if (b._tooltip) {
      return b._tooltip;
    } else {
      const c = (a = _Widget.Tooltip) == null ? void 0 : a.getSharedTooltip(b.rootElement, b.eventRoot);
      if (c && c.activeTarget === b._element && c.isVisible) {
        return c;
      }
    }
  }
  changeTooltip(a, b) {
    const c = this, {element: e} = c;
    if (a) {
      if (!(c.preventTooltipOnTouch && BrowserHelper.isTouchDevice)) {
        if (!a.isTooltip && a.constructor.name !== 'Object') {
          a = {
            'html': typeof a === 'string' ? a : c.placeholder,
            'textContent': true
          };
        }
        if ((b == null ? void 0 : b.isTooltip) || a.newInstance) {
          a.type = 'tooltip';
          if (!a.forElement)
            a.forElement = e;
          if (!('showOnHover' in a) && !a.forSelector)
            a.showOnHover = true;
          if (!('autoClose' in a))
            a.autoClose = true;
          a = _Widget.reconfigure(b, a, c);
          c.detachListeners('tooltipValueListener');
          if (!c.configureAriaDescription) {
            a.ion({
              'name': 'tooltipValueListener',
              'innerHtmlUpdate': 'onTooltipValueChange',
              'thisObj': c
            });
          }
        } else {
          e.dataset.btip = true;
          c.tipConfig = a;
          return;
        }
      }
    } else {
      if (b) {
        if (a == null && b.isTooltip) {
          b.destroy();
        } else {
          b.html = null;
          return;
        }
      } else {
        delete e.dataset.btip;
      }
    }
    return a;
  }
  onTooltipValueChange({
    value: a,
    source: b
  }) {
    this.ariaDescription = typeof a == 'string' ? a : b.contentElement.innerText;
  }
  get tooltipText() {
    const a = this._tooltip;
    if (a) {
      return a.isTooltip ? a.contentElement.innerText : typeof a === 'string' ? a : a.html;
    } else if (this.tipConfig) {
      return this.tipConfig.html;
    }
  }
  get isVisible() {
    const a = this, {element: b} = a;
    let c;
    return Boolean(b && !a._hidden && !a.isDestroying && b.isConnected && (!a.requireSize || hasLayout(b)) && (!(c = a.parent) || c.isVisible));
  }
  get isVisibleForValidation() {
    const a = this, {element: b} = a;
    let c;
    return Boolean(b && (!a._hidden || a.tab && !a.tab.hidden) && !a.isDestroying && b.isConnected && (!(c = a.containingWidget) || c.isVisibleForValidation));
  }
  whenVisible(a, b = this, c, e = a.toString()) {
    let f;
    const g = this;
    if (g.isVisible && g.isPainted) {
      (f = g.toCallWhenVisible) == null ? void 0 : f.delete(e);
      g.callback(a, b, c);
    } else {
      (g.toCallWhenVisible || (g.toCallWhenVisible = new Map())).set(e, {
        'callback': a,
        'thisObj': b,
        'args': c
      });
    }
  }
  focus() {
    let a;
    (a = this.focusableElement) == null ? void 0 : a.focus({ 'preventScroll': true });
  }
  get focusability() {
    return DomHelper.getFocusability(this.focusElement);
  }
  get focusableElement() {
    const a = this, b = !a.isDestroying && a.isVisible && !a.disabled && a.focusElement;
    return (b === document.body || (b == null ? void 0 : b.offsetParent)) && b;
  }
  get focusElement() {
  }
  get isFocusable() {
    return Boolean(this.focusableElement);
  }
  get isTabbable() {
    return this.focusability.tabbable;
  }
  set isTabbable(a) {
    const {
        focusability: b,
        focusElement: c
      } = this, [e, f] = b.natural ? [
        !a && '-1',
        a
      ] : [
        a && '0',
        !a
      ];
    if (c && !a !== !b.tabbable) {
      f && c.removeAttribute('tabIndex');
      e && c.setAttribute('tabIndex', e);
    }
  }
  async show({
    align: a,
    animate: animate = true
  } = {}) {
    let b;
    const c = this, {
        element: e,
        floating: f
      } = c, {style: g} = e, i = animate && c.showAnimation;
    let j, k, l = !c.isVisible;
    if (l) {
      l = c.trigger('beforeShow');
      if (ObjectHelper.isPromise(l)) {
        l = await l;
      }
    }
    if (l !== false && ((b = c.internalBeforeShow) == null ? void 0 : b.call(c)) !== false) {
      return new Promise(n => {
        c.cancelHideShowAnimation();
        c.updateCentered(c._centered);
        if (f) {
          const o = c.floatRoot;
          if (!o.contains(e)) {
            if (c.rendered) {
              o.appendChild(c.element);
            } else {
              c.render(o, false);
            }
          }
          if (g.transform.includes('scale')) {
            c.scale = null;
            g.transform = g.transformOrigin = '';
          }
          const p = c.closest(isScaled);
          if (p) {
            const {scale: q} = p;
            c.scale = q;
            g.transform = 'scale(' + q + ')';
            g.transformOrigin = '0 0';
          }
        }
        c._hidden = false;
        c.syncHiddenCls();
        if (f && !isVisible(e)) {
          c.hide(false);
          return n();
        }
        c.onConfigChange({
          'name': 'hidden',
          'value': false,
          'was': true,
          'config': c.$meta.configs.hidden
        });
        if (i) {
          j = Object.keys(i)[0];
          k = i[j];
          const r = c.currentAnimation = {
            'showing': true,
            'styleProp': j,
            'resolve': n
          };
          c.isAnimating = true;
          g.transition = 'none';
          g[j] = k.from;
          DomHelper.getStyleValue(e, j);
          c.currentAnimation.detacher = EventHelper.onTransitionEnd({
            'element': e,
            'property': j,
            'duration': parseDuration(k.duration) + 20,
            'handler': () => c.afterHideShowAnimate(r),
            'thisObj': c
          });
          g.transition = j + ' ' + k.duration + ' ease ' + k.delay;
          g[j] = k.to;
        }
        c.afterShow(a, !i ? n : null);
      });
    } else {
      return Promise.resolve();
    }
  }
  async showBy(a, b, c) {
    const e = this, f = Array.isArray(a);
    if (f || typeof a === 'number') {
      const g = f ? a : [
        a,
        b
      ];
      a = Object.assign({
        'target': new Point(g[0] + 1, g[1] + 1),
        'matchSize': false,
        'align': 't0-b0'
      }, f ? b : c);
    }
    e.requireSize = true;
    if (e.isVisible) {
      DomHelper.addTemporaryClass(e.element, 'b-realigning', 300, e);
      e.alignTo(a);
    } else {
      return e.show({ 'align': a });
    }
  }
  afterShow(a, b) {
    let c, e;
    const f = this;
    f.trigger('show');
    f._configuredAnchorState = f.anchor;
    (e = (c = f.owner) == null ? void 0 : c.onChildShow) == null ? void 0 : e.call(c, f);
    f.triggerPaint();
    if (f.floating || f.positioned) {
      if (a) {
        f.alignTo(a);
      } else if (f.centered) {
        f.alignTo({
          'target': f.constrainTo,
          'align': 'c-c'
        });
      } else if (f.forElement) {
        f.alignTo(f.forElement);
      }
    }
    b == null ? void 0 : b();
  }
  onChildHide(a) {
    let b;
    if (a.floating) {
      (b = this.ariaElement) == null ? void 0 : b.removeAttribute('aria-owns');
    }
  }
  onChildShow(a) {
    let b;
    if (a.floating && ((b = this.childItems) == null ? void 0 : b.includes[a])) {
      this.ariaHasPopup = a.role;
      this.ariaElement.setAttribute('aria-owns', a.id);
    }
  }
  triggerPaint() {
    let a;
    const b = this, {
        element: c,
        toCallWhenVisible: e
      } = b, f = !b.isPainted;
    if (b.isVisible) {
      if (f) {
        b.getConfig('scrollable');
        if (b.scaleToFitWidth && !b.monitorResize) {
          b.onParentElementResize = b.onParentElementResize.bind(b);
          ResizeMonitor.addResizeListener(c.parentElement, b.onParentElementResize);
          b.updateScale();
        }
        if (!b.hideBryntumDomMessage && (b.isTaskBoardBase || b.isGridBase || b.isCalendar)) {
          c.insertBefore(new Comment('POWERED BY BRYNTUM (https://bryntum.com)'), c.firstChild);
        }
        if (b.onConnectedCallback && globalThis.ResizeObserver) {
          b.connectedObserver = new ResizeObserver(() => {
            if (b.isElementConnected && !c.isConnected) {
              b.onConnectedCallback(false);
              b.isElementConnected = false;
            } else if (!b.isElementConnected && c.isConnected) {
              b.onConnectedCallback(true, b.isElementConnected == null);
              b.isElementConnected = true;
            }
          });
          b.connectedObserver.observe(c);
        }
      }
      b.eachWidget(widgetTriggerPaint, false);
      if (f) {
        b.getConfig('tooltip');
        if (!_Widget.Ripple && _Widget.RippleClass) {
          _Widget.Ripple = new _Widget.RippleClass({ 'rootElement': b.rootElement });
        }
        if (globalThis.DocsBrowser && b.floating && b.closest(g => {
            let i;
            return (i = g.element) == null ? void 0 : i.closest('.fiddlePanelResult,.b-owned-by-fiddle');
          })) {
          c.classList.add('b-owned-by-fiddle');
        }
      }
      b.isPainted = true;
      f && b.triggerConfigs('paint');
      b.onInternalPaint({ 'firstPaint': f });
      b.trigger('paint', { 'firstPaint': f });
      (a = b.afterInternalPaint) == null ? void 0 : a.call(b, { 'firstPaint': f });
      if (e == null ? void 0 : e.size) {
        for (const {
              callback: g,
              thisObj: i,
              args: j
            } of e.values()) {
          b.callback(g, i, j);
        }
        e.clear();
      }
      if (f) {
        if (b.monitorResize && !b.scaleToFitWidth) {
          ResizeMonitor.onElementResize([{ 'target': c }]);
        }
      }
      b.hasPainted = true;
    }
  }
  onInternalPaint() {
  }
  cancelHideShowAnimation() {
    const a = this, {
        currentAnimation: b,
        element: c
      } = a;
    if (b) {
      a.isAnimating = false;
      if (c.classList.contains('b-hiding')) {
        c.classList.remove('b-hiding');
        a._hidden = false;
      }
      b.detacher();
      b.resolve();
      c.style.transition = c.style[b.styleProp] = '';
      a.currentAnimation = null;
      a.trigger((b.showing ? 'show' : 'hide') + 'AnimationEnd');
    }
  }
  afterHideShowAnimate(a) {
    const b = this;
    if (a === b.currentAnimation) {
      b.element.classList.remove('b-hiding');
      b.cancelHideShowAnimation();
      if (b._hidden) {
        b.afterHideAnimation();
      }
    }
  }
  suspendVisibility() {
    this._visibilitySuspended = (this._visibilitySuspended || 0) + 1;
    Object.defineProperty(this, 'isVisible', returnFalseProp);
  }
  resumeVisibility(a = true) {
    if (!--this._visibilitySuspended) {
      delete this.isVisible;
      if (a) {
        this.triggerPaint();
      }
    }
  }
  hide(a = true) {
    return new Promise(b => {
      const c = this;
      if (!c._hidden && c.trigger('beforeHide', { 'animate': a }) !== false) {
        const {
            element: e,
            lastAlignSpec: f
          } = c, {style: g} = e, i = a && c.hideAnimation;
        c.clearTimeout('scrollListenerTimeout');
        c.clearTimeout('resizeListenerTimeout');
        c._hidden = true;
        if (f) {
          f.targetOutOfView = null;
          if (f.monitorIntersection) {
            c.intersectionObserver.takeRecords();
            c.intersectionObserver.unobserve(f.target);
          }
        }
        c.onConfigChange({
          'name': 'hidden',
          'value': true,
          'was': false,
          'config': c.$meta.configs.hidden
        });
        if (!e) {
          b();
          return;
        }
        if (e.contains(DomHelper.getActiveElement(e))) {
          c.revertFocus(true);
        }
        if (c.isDestroyed) {
          b();
          return;
        }
        c.cancelHideShowAnimation();
        if (i) {
          const j = Object.keys(i)[0], k = i[j];
          if (Number(getComputedStyle(c.element)[j]) !== k.to) {
            const l = c.currentAnimation = {
              'hiding': true,
              'styleProp': j,
              'resolve': b
            };
            e.classList.add('b-hiding');
            c.isAnimating = true;
            c.currentAnimation.detacher = EventHelper.onTransitionEnd({
              'element': e,
              'property': j,
              'duration': parseDuration(k.duration) + 20,
              'handler': () => c.afterHideShowAnimate(l),
              'thisObj': c
            });
            g[j] = k.from;
            DomHelper.getStyleValue(e, j);
            g.transition = j + ' ' + k.duration + ' ease ' + k.delay;
            g[j] = k.to;
          } else {
            c.syncHiddenCls();
          }
        } else {
          c.syncHiddenCls();
        }
        c.ownedWidgets && Object.values(c.ownedWidgets).forEach(n => {
          let o;
          return n.floating && ((o = n.hide) == null ? void 0 : o.call(n, a));
        });
        c.afterHide(!i && b, i);
      } else {
        b(false);
      }
    });
  }
  realignOnTransitionEnd(a) {
    this.doHideOrRealign(a);
  }
  doHideOrRealign({
    target: a,
    isTrusted: b
  }) {
    const c = this, {
        lastAlignSpec: e,
        element: f
      } = c, g = e == null ? void 0 : e.anchoredTo, i = e == null ? void 0 : e.target, j = e == null ? void 0 : e.position, k = DomHelper.getActiveElement(c);
    if (!b || !hasLayout(f) || a && (f.contains(a) || a.nodeType === Element.ELEMENT_NODE && c.owns(a) || a.nodeType === Element.DOCUMENT_NODE && f.contains(k) && textInputTypes[k] && globalThis.innerHeight < document.body.offsetHeight)) {
      return;
    }
    if (e.aligningToElement) {
      const n = DomHelper.isInView(i, false, c);
      if (n && n.equals(e.targetRect)) {
        return;
      }
    }
    const l = c.scrollAction === 'hide' && c.getXY();
    c.realign();
    if (!c.isDestroyed && isVisible(f) && c.scrollAction === 'hide') {
      const [o, p] = c.getXY(), q = o !== l[0] || p !== l[1];
      if ((e == null ? void 0 : e.aligningToEvent) || (q || (i == null ? void 0 : i.$$name) === 'Point' || j) && (!g || !hasLayout(g) || a && DomHelper.isDescendant(a, g))) {
        c.hide();
      }
    }
  }
  afterHide(a = null, b = this.hideAnimation) {
    let c, e;
    const f = this;
    f._anchor = f._configuredAnchorState;
    f.removeTransientListeners();
    if (!b) {
      f.afterHideAnimation();
    }
    f.trigger('hide');
    (e = (c = f.owner) == null ? void 0 : c.onChildHide) == null ? void 0 : e.call(c, f);
    a && a();
  }
  removeTransientListeners() {
    let a, b;
    const c = this, {targetObserver: e} = c;
    c.clearTimeout('resizeListenerTimeout');
    c.clearTimeout('scrollListenerTimeout');
    if (e) {
      e.disconnect();
      delete c.targetObserver;
    }
    c.documentScrollListener = (a = c.documentScrollListener) == null ? void 0 : a.call(c);
    c.targetRootScrollListener = (b = c.targetRootScrollListener) == null ? void 0 : b.call(c);
    if (c.observedTargetEl) {
      ResizeMonitor.removeResizeListener(c.observedTargetEl, c.onTargetResize);
      c.observedTargetEl = false;
    }
    if (c.observedConstrainEl) {
      ResizeMonitor.removeResizeListener(c.observedConstrainEl, c.onAlignConstraintChange);
      c.observedConstrainEl = false;
    }
  }
  afterHideAnimation() {
    const a = this, {element: b} = a;
    if (!a.hasOwnProperty('_hideMode') && a.floating && a.floatRoot.contains(b)) {
      b.remove();
    } else {
      a.syncHiddenCls();
    }
    if (a.defaultAnchorBackgroundColor) {
      a.anchorPathElement.setAttribute('fill', a.defaultAnchorBackgroundColor);
    }
  }
  changeHidden(a) {
    const b = this;
    let c;
    if (b.isConfiguring) {
      c = Boolean(a);
      b.syncHiddenCls(c);
    } else {
      b.trigger('beforeChangeHidden', { 'hidden': a });
      if (a) {
        b.hide();
      } else {
        b.show();
      }
    }
    return c;
  }
  syncHiddenCls(a = this._hidden) {
    const {
        element: b,
        hideMode: e
      } = this, f = { 'b-hidden': a };
    for (const g of hideModes) {
      f['b-hide-mode-' + g] = g === e && (a || g !== 'display');
    }
    DomHelper.assignClasses(b, f);
  }
  get assignedId() {
    return this.hasGeneratedId ? null : this.id;
  }
  get owner() {
    return this.parent || this._owner || this.containingWidget;
  }
  changeOwner(a) {
    if (!this.parent) {
      return a;
    }
  }
  updateOwner(a, b) {
    const {id: c} = this;
    if (b) {
      b.ownedWidgets && delete b.ownedWidgets[c];
      DomHelper.removeAttributeValue(b.ariaElement, 'aria-owns', c);
    }
    if (a) {
      (a.ownedWidgets || (a.ownedWidgets = {}))[c] = this;
      DomHelper.addAttributeValue(a.ariaElement, 'aria-owns', c);
    }
  }
  get containingWidget() {
    let a, b;
    let c = this.parent;
    if (!c) {
      const e = ((a = this.forElement) == null ? void 0 : a.nodeType) === Element.ELEMENT_NODE ? this.forElement : (b = this.element) == null ? void 0 : b.parentNode;
      c = (e == null ? void 0 : e.closest('.b-widget')) && _Widget.fromElement(e);
    }
    return c;
  }
  get previousSibling() {
    return this.getSibling(-1);
  }
  get nextSibling() {
    return this.getSibling(1);
  }
  getSibling(a) {
    const b = this, {parent: c} = b, e = c ? c.childItems : Array.from(b.element.parentElement.querySelectorAll('.b-widget'));
    return c ? e[e.indexOf(b) + a] : _Widget.fromElement(e[e.indexOf(b.element) + a]);
  }
  up(a, b, c) {
    let e;
    const {owner: f} = this;
    return a ? (e = f == null ? void 0 : f.closest) == null ? void 0 : e.call(f, a, b, c) : f;
  }
  closest(a, b, c) {
    const e = typeof c, f = e === 'number', g = e === 'string';
    for (let i = this, j = 1; i; i = i.owner, j++) {
      if (_Widget.widgetMatches(i, a, b)) {
        return i;
      }
      if (f && j >= c) {
        return;
      } else if (g && _Widget.widgetMatches(i, c, b)) {
        return;
      } else if (i === c) {
        return;
      }
    }
  }
  owns(a, b) {
    if (a) {
      if ('eventPhase' in a) {
        a = a.target;
      }
      if (a.nodeType === Element.ELEMENT_NODE) {
        if (this.element.contains(a)) {
          return true;
        }
        a = a.getAttribute('owned-by') ? _Widget.getById(a.getAttribute('owned-by')) : _Widget.fromElement(a);
      }
      while (a) {
        if (a === this || b && this.ownedWidgets && Object.values(this.ownedWidgets).some(c => c.owns(a, true))) {
          return true;
        }
        a = a.owner;
      }
    }
    return false;
  }
  eachAncestor(a) {
    let b = this.owner;
    while (b) {
      if (a(b) === false) {
        return false;
      }
      b = b.owner;
    }
    return true;
  }
  changeMaximizeOnMobile(a) {
    const b = this;
    if (b.floating && BrowserHelper.isMobile) {
      const {initialConfig: c} = b;
      if (a) {
        b.centered = b.modal = false;
        b.maximized = true;
      } else {
        b.centered = c.centered;
        b.modal = c.modal;
        b.maximized = c.maximized;
      }
    }
  }
  changeMonitorResize(a, b) {
    const c = this.scaleToFitWidth ? false : Boolean(a);
    if (c !== Boolean(b)) {
      return c;
    }
  }
  updateMonitorResize(a) {
    const b = this;
    if (!hasOwn6(b, 'onElementResize')) {
      b.onElementResize = b.onElementResize.bind(b);
    }
    ResizeMonitor[a ? 'addResizeListener' : 'removeResizeListener'](b.element, b.onElementResize);
  }
  changeReadOnly(a) {
    a = Boolean(a);
    if (Boolean(this._readOnly) !== a) {
      return a;
    }
  }
  updateReadOnly(a) {
    let b;
    (b = this.element) == null ? void 0 : b.classList[a ? 'add' : 'remove']('b-readonly');
    if (!this.isConfiguring) {
      this.eachWidget(c => {
        if (c.ignoreParentReadOnly) {
          return;
        }
        if (!('_originalReadOnly' in c)) {
          c._originalReadOnly = c.config.readOnly || false;
        }
        c.readOnly = a || c._originalReadOnly;
      }, false);
      this.trigger('readOnly', { 'readOnly': a });
    }
  }
  eachWidget(a, b = true, c) {
    const e = c ? this.childItemsOrderedByRelevance : this.childItems, f = (e == null ? void 0 : e.length) || 0, g = {};
    for (let j = 0; j < f; j++) {
      const k = e[j];
      g.down = b;
      if (a(k, g) === false) {
        return false;
      }
      if (g.down && k.eachWidget) {
        if (k.eachWidget(a, b, c) === false) {
          return false;
        }
      }
    }
    return true;
  }
  queryAll(a) {
    const b = [];
    this.eachWidget(c => {
      if (a(c)) {
        b.push(c);
      }
    });
    return b;
  }
  query(a, b) {
    let c = null;
    this.eachWidget(e => {
      if (a(e)) {
        c = e;
        return false;
      }
    }, true, b);
    return c;
  }
  getWidgetByRef(a) {
    let b, c;
    if (a instanceof _Widget) {
      return a;
    }
    return ((b = this == null ? void 0 : this.widgetMap) == null ? void 0 : b[a]) || ((c = this == null ? void 0 : this.owner) == null ? void 0 : c.getWidgetByRef(a));
  }
  onFocusIn(a) {
    const b = this, {element: c} = b;
    b.containsFocus = true;
    b.focusInEvent = a;
    if (b.floating || b.positioned) {
      b.toFront();
    }
    c.classList.add('b-contains-focus');
    b.updateAriaLabel(b.localizeProperty('ariaLabel'));
    b.updateAriaDescription(b.localizeProperty('ariaDescription'));
    if (c.contains(a._target) && b.onInternalKeyDown && !b.keyDownListenerRemover) {
      b.keyDownListenerRemover = EventHelper.on({
        'element': c,
        'keydown': 'onInternalKeyDown',
        'thisObj': b
      });
    }
    b.trigger('focusin', a);
  }
  onFocusOut(a) {
    const b = this;
    if (b.keyDownListenerRemover) {
      b.keyDownListenerRemover();
      b.keyDownListenerRemover = null;
    }
    if (!b.isDestroyed) {
      if (!a.relatedTarget) {
        b.revertFocus(!b.isVisible);
      }
      b.containsFocus = false;
      b.element.classList.remove('b-contains-focus');
      b.updateAriaLabel(b.localizeProperty('ariaLabel'));
      b.updateAriaDescription(b.localizeProperty('ariaDescription'));
      b.trigger('focusout', a);
    }
  }
  captureFocus() {
    const a = this, b = DomHelper.getActiveElement(a), c = a.contains(b) && a.captureFocusItem(b);
    return (e, f) => {
      if (c && !a.isDestroying) {
        const g = DomHelper.getActiveElement(a);
        if (g !== b || f) {
          c(e);
        }
      }
    };
  }
  captureFocusItem(a) {
    return (b = true) => {
      if (this.contains(a)) {
        b ? a.focus() : a.focus({ 'preventScroll': true });
      }
    };
  }
  contains(a, b) {
    const {element: c} = this;
    if (a && c) {
      if (a.isWidget) {
        a = a.element;
      }
      return c.contains(a) && (!b || c !== a);
    }
  }
  revertFocus(a) {
    let b;
    const c = this, e = DomHelper.getActiveElement(c);
    let f = (b = c.focusInEvent) == null ? void 0 : b.relatedTarget;
    if (a || c.containsFocus && (f == null ? void 0 : f.nodeType) === Element.ELEMENT_NODE && c.element.contains(e)) {
      if (!f || !DomHelper.isFocusable(f)) {
        f = c.getFocusRevertTarget();
      }
      c._isRevertingFocus = true;
      if (f && DomHelper.isFocusable(f)) {
        f._isRevertingFocus = true;
        f.focus({ 'preventScroll': true });
        f._isRevertingFocus = false;
      } else {
        e == null ? void 0 : e.blur();
      }
      c._isRevertingFocus = false;
    }
  }
  getFocusRevertTarget() {
    let a;
    const b = this, {
        owner: c,
        focusInEvent: e
      } = b, f = e ? e.backwards ? 1 : -1 : -1;
    let g = e && e.relatedTarget;
    const i = g && _Widget.fromElement(g);
    if (c && !c.isDestroyed && (!g || !DomHelper.isFocusable(g) || i && !i.isFocusable)) {
      g = null;
      if (c.eachWidget) {
        const j = [];
        c.eachWidget(k => {
          if (k === b || k.isFocusable) {
            j.push(k);
          }
        }, false);
        if (j.length > 1) {
          const k = j.indexOf(b);
          g = j[k + f] || j[k - f];
        }
      }
      if (!g && c.isFocusable) {
        g = c;
      }
      g = g ? g.focusElement : (a = c.getFocusRevertTarget) == null ? void 0 : a.call(c);
    }
    return g;
  }
  getStaticWidgetClasses(a, b) {
    const c = new DomClassList(), e = this.$meta.hierarchy;
    let f, g, j, k, l;
    for (g = e.indexOf(a); g < e.length; ++g) {
      f = e[g];
      l = Reflect.getOwnPropertyDescriptor(f.prototype, 'widgetClass');
      if (l == null ? void 0 : l.get) {
        k = l.get.call(this);
      } else {
        j = hasOwn6(f, '$$name') || hasOwn6(f, '$name') || hasOwn6(f, '_$name') ? f.$$name : f.name;
        if (j.length < 3 || j.includes('$')) {
          console.warn('Class "' + j + '" extending "' + f.$name + '" should have "$name" static getter with no less than 3 chars.');
        }
        k = 'b-' + j.toLowerCase();
      }
      if (k) {
        c.add(b ? k + b : k);
      }
    }
    return c;
  }
  get rootUiClass() {
    return _Widget;
  }
  get staticClassList() {
    const {$meta: a} = this;
    let b = a.staticClassList;
    if (!b) {
      a.staticClassList = b = this.getStaticWidgetClasses(_Widget);
      BrowserHelper.isTouchDevice && b.add('b-touch');
    }
    return b;
  }
  get uiClasses() {
    this.getConfig('ui');
    return this._uiClasses;
  }
  get uiClassList() {
    this.getConfig('ui');
    return this._uiClassList;
  }
  get widgetClassList() {
    const a = this, {
        cls: b,
        defaultCls: c,
        uiClasses: e
      } = a;
    let {staticClassList: f} = a;
    if (c || b) {
      f = f.clone();
      c && f.assign(c);
      b && f.assign(b);
    }
    const g = f.values;
    e && g.push(...e);
    a.floating && g.push('b-floating');
    if (a.collapsify === 'hide') {
      g.push('b-collapsify-hide');
    }
    return g;
  }
  changeCls(a) {
    return DomClassList.from(a);
  }
  updateCls(a, b) {
    if (!this.isConfiguring && !this.isComposable) {
      const {element: c} = this;
      if (b) {
        ObjectHelper.getTruthyKeys(b).forEach(e => c.classList.remove(e));
      }
      a.assignTo(c);
    }
  }
  changeContentElementCls(a) {
    return DomClassList.from(a);
  }
  changeHtmlCls(a) {
    return DomClassList.from(a);
  }
  changeDefaultCls(a) {
    return DomClassList.from(a, true);
  }
  updateHideMode() {
    this.hasPainted && this.syncHiddenCls();
  }
  changeUi(a) {
    return DomClassList.from(a);
  }
  updateUi(a) {
    let b = null, c, e;
    if (a) {
      const f = this.getStaticWidgetClasses(this.rootUiClass);
      for (e in a) {
        if (a[e]) {
          for (c in f) {
            if (f[c]) {
              (b || (b = new DomClassList()))[c + '-ui-' + e] = 1;
            }
          }
        }
      }
    }
    this._uiClasses = b == null ? void 0 : b.values;
    this._uiClassList = b;
  }
  fromCache(a, b = false, c = this.element) {
    let e;
    if (!c) {
      return null;
    }
    const f = this;
    if (!((e = f.cache) == null ? void 0 : e[a])) {
      if (!f.cache) {
        f.cache = {};
      }
      return f.cache[a] = b ? DomHelper.children(c, a) : c.querySelector(a);
    }
    return f.cache[a];
  }
  emptyCache() {
    if (this.cache) {
      this.cache = {};
    }
  }
  changeMasked(a, b) {
    let c;
    if (((c = this.masked) == null ? void 0 : c.type) === 'trial') {
      return;
    }
    if (a === true || a === '') {
      a = '\xA0';
    }
    if (b && !b.isDestroyed) {
      if (typeof a === 'string') {
        b.text = a;
        a = b;
      } else if (a) {
        b.setConfig(a);
        a = b;
      } else {
        b.destroy();
      }
    } else if (a) {
      const e = _Widget.resolveType('mask');
      a = e.mergeConfigs(this.maskDefaults, a);
      a.owner = this;
      a = e.mask(a);
    }
    return a || null;
  }
  onMaskAutoClose(a) {
    if (a.isDestroyed && a === this.masked) {
      this.masked = null;
    }
  }
  mask(a) {
    this.masked = a;
    return this.masked;
  }
  unmask() {
    this.masked = null;
  }
  onInternalResize(a, b, c, e, f) {
    this._width = a.offsetWidth;
    this._height = a.offsetHeight;
  }
  onElementResize(a, b) {
    let c;
    const e = this, {element: f} = e, g = (c = e._width) != null ? c : e._oldWidth, i = e._height, j = f.offsetWidth, k = f.offsetHeight;
    if (e.floating && b) {
      e.onFloatingWidgetResize(...arguments);
    }
    if (!e.suspendResizeMonitor && (g !== j || i !== k)) {
      e.onInternalResize(f, j, k, g, i);
      e.trigger('resize', {
        'width': j,
        'height': k,
        'oldWidth': g,
        'oldHeight': i
      });
    }
  }
  onFloatingWidgetResize(a, b, c) {
    const e = this, {
        lastAlignSpec: f,
        constrainTo: g
      } = e;
    if (e.isVisible && f && f.target) {
      const i = !b || c.height !== b.height, j = !b || c.width !== b.width, k = g && !Rectangle.from(g).contains(Rectangle.from(e.element, null, true));
      if (i && f.zone !== 2 || j && f.zone !== 1 || k) {
        e.requestAnimationFrame(() => e.realign());
      }
    }
  }
  updateScale() {
    const a = this, b = a.element, c = b.parentElement;
    if (!a.configuredWidth) {
      a.configuredWidth = a.width;
    }
    b.style.display = 'none';
    const e = Rectangle.client(c), f = e.width / a.configuredWidth, g = a.scale = a.allowGrowWidth ? Math.min(f, 1) : f;
    b.style.transform = 'scale(' + g + ')';
    b.style.transformOrigin = 'top left';
    b.style.display = '';
    if (a.allowGrowWidth && f > 1) {
      a.width = a.configuredWidth * f;
    }
  }
  onParentElementResize(a) {
    this.updateScale();
  }
  parseTRBL(a, b = 'px') {
    a = a || 0;
    if (typeof a === 'number') {
      return [
        '' + a + b,
        '' + a + b,
        '' + a + b,
        '' + a + b
      ];
    }
    const c = a.split(' '), e = c.length;
    if (e === 1) {
      c[1] = c[2] = c[3] = c[0];
    } else if (e === 2) {
      c[2] = c[0];
      c[3] = c[1];
    } else if (e === 3) {
      c[3] = c[1];
    }
    return [
      isFinite(c[0]) ? '' + c[0] + b : c[0],
      isFinite(c[1]) ? '' + c[1] + b : c[2],
      isFinite(c[2]) ? '' + c[2] + b : c[3],
      isFinite(c[3]) ? '' + c[3] + b : c[4]
    ];
  }
  get documentRoot() {
    let a;
    return ((a = this.owner) == null ? void 0 : a.documentRoot) || this.element.getRootNode();
  }
  get eventRoot() {
    let a, b;
    return ((a = this.element) == null ? void 0 : a.isConnected) ? DomHelper.getRootElement(this.element) : ((b = this.owner) == null ? void 0 : b.eventRoot) || this._rootElement;
  }
  get rootElement() {
    let a, b;
    const c = this;
    if (!c._rootElement) {
      let e = ((a = c.owner) == null ? void 0 : a.rootElement) || DomHelper.getRootElement(c.forElement || (((b = c.element) == null ? void 0 : b.isConnected) ? c.element : c.getRenderContext()[0] || c.element));
      if (!e) {
        e = document.body;
      }
      c._rootElement = e;
    }
    return c._rootElement;
  }
  get floatRoot() {
    let a;
    const b = this, {rootElement: c} = b;
    let {floatRoot: e} = c;
    if (!e) {
      const {outerCls: f} = _Widget, g = (a = DomHelper.getThemeInfo(null, c)) == null ? void 0 : a.name;
      if (!DomHelper.isValidFloatRootParent(c)) {
        throw new Error('Attaching float root to wrong root');
      }
      if (g) {
        f.push('b-theme-' + g.toLowerCase());
      }
      e = c.floatRoot = DomHelper.createElement({
        'className': 'b-float-root ' + f.join(' '),
        'parent': c
      });
      floatRoots.push(e);
      if (BrowserHelper.isAndroid) {
        e.style.height = screen.height + 'px';
        EventHelper.on({
          'element': globalThis,
          'orientationchange': () => e.style.height = screen.height + 'px',
          'thisObj': this
        });
      }
      GlobalEvents_default.ion({
        'theme': ({
          theme: i,
          prev: j
        }) => {
          e.classList.add('b-theme-' + i.toLowerCase());
          e.classList.remove('b-theme-' + j.toLowerCase());
        }
      });
    } else if (!c.contains(e)) {
      c.appendChild(e);
    }
    e.classList.toggle('b-rtl', Boolean(b.rtl));
    return e;
  }
  get floatRootMaxZIndex() {
    let a = 1;
    Array.from(this.floatRoot.children).forEach(b => {
      const c = parseInt(getComputedStyle(b).zIndex || 0, 10);
      if (c > a) {
        a = c;
      }
    });
    return a;
  }
  get accessibility() {
    return this.constructor.accessibility;
  }
  static get accessibility() {
    return document.body.classList.contains('b-accessibility');
  }
  set accessibility(a) {
    this.constructor.accessibility = a;
  }
  static set accessibility(a) {
    document.body.classList.toggle('b-accessibility', Boolean(a));
  }
  static get floatRoots() {
    return floatRoots;
  }
  static removeFloatRoot(a) {
    floatRoots.splice(floatRoots.indexOf(a), 1);
  }
  static get outerCls() {
    const a = ['b-outer'], {platform: b} = BrowserHelper;
    if (b) {
      a.push('b-' + b);
    }
    if (BrowserHelper.isTouchDevice) {
      a.push('b-touch-events');
    }
    if (BrowserHelper.isMobile) {
      a.push('b-mobile');
    }
    if (DomHelper.scrollBarWidth) {
      a.push('b-visible-scrollbar');
    } else {
      a.push('b-overlay-scrollbar');
    }
    if (BrowserHelper.isChrome) {
      a.push('b-chrome');
    } else if (BrowserHelper.isSafari) {
      a.push('b-safari');
    } else if (BrowserHelper.isFirefox) {
      a.push('b-firefox');
    }
    if (BrowserHelper.isPhone) {
      a.push('b-phone');
    }
    if (!globalThis.ResizeObserver) {
      a.push('b-no-resizeobserver');
    }
    return a;
  }
  get isAnimating() {
    return this._isAnimatingCounter > 0;
  }
  set isAnimating(a) {
    const b = this, {_isAnimatingCounter: c} = b;
    b._isAnimatingCounter = Math.max(0, c + (a ? 1 : -1));
    if (c === 0 && a) {
      b.element.classList.add('b-animating');
      b.trigger('animationStart');
    } else if (c === 1 && !a) {
      b.element.classList.remove('b-animating');
      b.trigger('animationEnd');
    }
  }
  async waitForAnimations() {
    if (this.isAnimating) {
      await this.await('animationEnd', { 'checkLog': false });
    }
  }
  async executeAndAwaitAnimations(a, b) {
    const c = a.getAnimations({ 'subtree': true });
    await b();
    const e = a.getAnimations({ 'subtree': true }), f = ArrayHelper.delta(c, e).onlyInB, g = f.filter(i => i.effect.getTiming().iterations !== Infinity);
    if (g.length) {
      await Promise.allSettled(g.map(i => i.finished));
    }
  }
  static query(a, b = false) {
    const {idMap: c} = _Widget.identifiable;
    for (const e in c) {
      if (_Widget.widgetMatches(c[e], a, b)) {
        return c[e];
      }
    }
    return null;
  }
  static queryAll(a, b = false) {
    const {idMap: c} = _Widget.identifiable, e = [];
    for (const f in c) {
      if (_Widget.widgetMatches(c[f], a, b)) {
        e.push(c[f]);
      }
    }
    return e;
  }
  static fromElement(a, b, c) {
    const e = typeof b;
    if (a && !a.nodeType) {
      a = a.target;
    }
    if (e === 'number' || b && b.nodeType === Element.ELEMENT_NODE) {
      c = b;
      b = null;
    }
    let f = a, g = 0, i, j, k;
    if (typeof c !== 'number') {
      i = c;
      c = Number.MAX_VALUE;
    }
    if (e === 'string') {
      b = b.toLowerCase();
    }
    while (f && f.nodeType === Element.ELEMENT_NODE && g < c && f !== i) {
      j = f.dataset && f.dataset.ownerCmp || f.id;
      if (j) {
        k = _Widget.getById(j);
        if (k) {
          if (b) {
            if (e === 'function') {
              if (b(k)) {
                return k;
              }
            } else if (_Widget.widgetMatches(k, b, true)) {
              return k;
            }
          } else {
            return k;
          }
        }
        g++;
      }
      f = getParent(f);
    }
    return null;
  }
  static fromSelector(a) {
    const b = document.querySelector(a);
    return b ? _Widget.fromElement(b) : null;
  }
  triggerFieldChange(a, b = true) {
    let c;
    if (b) {
      this.trigger('change', a);
    } else {
      a.source = this;
    }
    (c = this.eachAncestor) == null ? void 0 : c.call(this, e => {
      let f, g;
      (f = e.onFieldChange) == null ? void 0 : f.call(e, a);
      if ((g = e.isolateFieldChange) == null ? void 0 : g.call(e, this)) {
        return false;
      }
    });
  }
  isolateFieldChange(a) {
    return this.isolateFields;
  }
  setupFocusListeners() {
    GlobalEvents_default.setupFocusListenersOnce(this.eventRoot, EventHelper);
  }
  static widgetMatches(a, b, c) {
    if (b === '*') {
      return true;
    }
    if (typeof b === 'function') {
      return b(a);
    }
    return _Widget.isType(a, b, c);
  }
  static attachTooltip(a, b) {
    if (typeof b === 'string')
      b = { 'html': b };
    _Widget.create(Object.assign({ 'forElement': a }, b), 'tooltip');
    return a;
  }
  static append(a, b = {}) {
    if (b instanceof HTMLElement || typeof b === 'string') {
      b = { 'appendTo': b };
    }
    return ArrayHelper.asArray(a).map(c => _Widget.create(ObjectHelper.assign({}, b, c)));
  }
  changeDock(a) {
    if (a === 'start') {
      return 'left';
    }
    if (a === 'end') {
      return 'right';
    }
    return a;
  }
  updateRtl(a) {
    super.updateRtl(a);
    this.eachWidget(b => {
      if (!('rtl' in b.initialConfig)) {
        b.rtl = a;
      }
    });
  }
};
const Widget = _Widget;
const proto2 = Widget.prototype;
[
  'compose',
  'domSyncCallback'
].forEach(a => proto2[a].$nullFn = true);
Widget.initClass();
DomHelper.Widget = Widget;
GlobalEvents_default.Widget = Widget;
Object.assign(globalThis.bryntum || (globalThis.bryntum = {}), {
  'Widget': Widget,
  'get': (a = globalThis.$0) => a.nodeType ? Widget.fromElement(a) : Widget.getById(a),
  'query': Widget.query,
  'queryAll': Widget.queryAll,
  'fromElement': Widget.fromElement,
  'fromSelector': Widget.fromSelector
});
Widget._$name = 'Widget';
const Layout = class extends Base.mixin(Events_default, Factoryable_default) {
  static factoryable = { 'defaultType': 'default' };
  static configurable = {
    'owner': null,
    'containerCls': 'b-auto-container',
    'itemCls': null
  };
  static type = 'default';
  get contentElement() {
    let a;
    return (a = this.owner) == null ? void 0 : a.contentElement;
  }
  onChildAdd(a) {
  }
  onChildRemove(a) {
  }
  renderChildren() {
    const a = this, {
        owner: b,
        itemCls: c
      } = a, {
        contentElement: e,
        items: f
      } = b, g = b.itemCls, j = f == null ? void 0 : f.length;
    if (j) {
      b.textContent = false;
      for (let k, l, n = 0; n < j; n++) {
        const o = f[n], {
            element: p,
            positionable: q
          } = o, {classList: r} = p;
        p.dataset.itemIndex = n;
        if (r.contains('b-outer')) {
          r.remove(...Widget.outerCls);
        }
        if (c) {
          r.add(c);
        }
        if (g) {
          r.add(g);
        }
        l = e;
        if (q) {
          k = b.getPositionableLocation(q);
          if (!(l = k[0])) {
            l = k[1].parentElement;
            o.insertBefore = k[1];
          }
        }
        o.render(l, Boolean(b.isPainted));
      }
    }
    a.syncPendingConfigs();
    a.syncChildCount();
  }
  removeChild(a) {
    const b = this, {element: c} = a, {
        owner: e,
        itemCls: f
      } = b, {contentElement: g} = e, i = e.itemCls;
    if (g.contains(c)) {
      c.remove();
    }
    delete c.dataset.itemIndex;
    if (f) {
      c.classList.remove(f);
    }
    if (i) {
      c.classList.remove(i);
    }
    b.fixChildIndices();
    b.syncChildCount();
  }
  appendChild(a) {
    const {element: b} = a, {
        owner: c,
        itemCls: e
      } = this, {contentElement: f} = c, g = c.itemCls;
    b.dataset.itemIndex = c.indexOfChild(a);
    c.textContent = false;
    if (e) {
      b.classList.add(e);
    }
    if (g) {
      b.classList.add(g);
    }
    a.render(f, Boolean(c.isPainted));
    this.syncChildCount();
  }
  insertChild(a, b) {
    const c = this, {element: e} = a, {
        owner: f,
        itemCls: g
      } = c, {contentElement: i} = f, j = DomHelper.getChild(i, '[data-item-index="' + b + '"]'), k = f.itemCls;
    f.textContent = false;
    if (g) {
      e.classList.add(g);
    }
    if (k) {
      e.classList.add(k);
    }
    i.insertBefore(e, j);
    a.render(null, Boolean(f.isPainted));
    c.fixChildIndices();
    c.syncChildCount();
  }
  fixChildIndices() {
    this.owner.items.forEach((a, b) => {
      a.element.dataset.itemIndex = b;
    });
  }
  syncChildCount() {
    let a;
    const {owner: b} = this, {length: c} = b.items;
    (a = b.contentElement) == null ? void 0 : a.classList[c === 1 ? 'add' : 'remove']('b-single-child');
  }
  syncConfigLater(a) {
    const b = this.pendingConfigs || (this.pendingConfigs = []);
    if (!b.includes(a)) {
      b.push(a);
    }
  }
  syncConfigStyle({
    name: a,
    style: b,
    classes: c,
    map: e
  }) {
    const f = this, g = 'b-box-' + a + '-', {contentElement: i} = f, j = f[a];
    let k = (e == null ? void 0 : e[j]) || j;
    if (i) {
      if (c) {
        const {classList: l} = i;
        c.forEach(n => {
          if (n !== k) {
            l.remove(g + n);
          }
        });
        if (c.includes(k)) {
          l.add(g + k);
          k = '';
        }
      }
      i.style[b] = k;
    } else {
      f.syncConfigLater(a);
    }
  }
  syncPendingConfigs() {
    const a = this, {pendingConfigs: b} = a;
    let c;
    if (b) {
      a.pendingConfigs = null;
      while (c = b.pop()) {
        a[a.$meta.configs[c].updater](a[c]);
      }
    }
  }
};
Layout.initClass();
Layout._$name = 'Layout';
let lastTouchTime = 0;
const hasRipple = a => a.ripple;
const Ripple = class extends Widget {
  static type = 'ripple';
  static $name = 'Ripple';
  static get defaultConfig() {
    return {
      'old_element': {
        'children': [{
            'className': 'b-ripple-inner',
            'reference': 'rippleElement'
          }]
      },
      'element': {
        'children': [{
            'tag': 'svg',
            'class': 'b-ripple-inner',
            'reference': 'rippleElement',
            'ns': 'http://www.w3.org/2000/svg',
            'version': '1.1',
            'viewBox': '0 0 100 100',
            'children': [{
                'reference': 'circleElement',
                'tag': 'circle',
                'cx': '0',
                'cy': '0',
                'r': 10
              }]
          }]
      },
      'floating': true,
      'hideAnimation': false,
      'showAnimation': false,
      'scrollAction': 'realign',
      'color': 'rgba(0,0,0,.3)',
      'startRadius': 10,
      'radius': 100
    };
  }
  afterConstruct() {
    super.afterConstruct();
    EventHelper.on({
      'element': this.rootElement,
      'mousedown': 'onRippleControllingEvent',
      'thisObj': this,
      'capture': true,
      'once': true
    });
  }
  onRippleControllingEvent(a) {
    let b;
    const c = this;
    c.show();
    const e = DomHelper.getStyleValue(c.circleElement, 'animationName');
    c.hide();
    (b = c.listenerDetacher) == null ? void 0 : b.call(c);
    if (e && e !== 'none') {
      c.listenerDetacher = EventHelper.on({
        'mousedown': {
          'element': c.rootElement,
          'capture': true,
          'handler': 'onMousedown'
        },
        'touchstart': {
          'element': c.rootElement,
          'capture': true,
          'handler': 'onTouchStart'
        },
        'animationend': {
          'element': c.circleElement,
          'handler': 'onAnimationEnd'
        },
        'thisObj': c
      });
      if (a.type === 'mousedown') {
        c.onMousedown(a);
        GlobalEvents_default.ion({
          'theme': 'onRippleControllingEvent',
          'thisObj': this
        });
      }
    }
  }
  onTouchStart(a) {
    lastTouchTime = performance.now();
    this.handleTriggerEvent(a);
  }
  onMousedown(a) {
    if (performance.now() - lastTouchTime > 200) {
      this.handleTriggerEvent(a);
    }
  }
  handleTriggerEvent(a) {
    const b = Widget.fromElement(a.target, hasRipple);
    if (b) {
      const c = b.ripple, e = c.delegate ? a.target.closest(c.delegate) : b.focusElement || b.element;
      if (e) {
        const f = ObjectHelper.assign({
          'event': a,
          'target': e,
          'radius': this.radius
        }, c);
        if (typeof f.clip === 'string') {
          f.clip = b[f.clip] || a.target.closest(f.clip);
          if (!f.clip) {
            return;
          }
        }
        this.ripple(f);
      }
    }
  }
  ripple({
    event: a,
    point: point = EventHelper.getClientPoint(a),
    target: target = a.target,
    clip: clip = target,
    radius: radius = this.radius,
    color: color = this.color
  }) {
    this.clip = clip;
    clip = Rectangle.from(clip, null, true);
    const b = this, c = clip.getDelta(point), e = b.rippleElement.style, f = b.circleElement, g = DomHelper.getStyleValue(target, 'border-radius');
    b.hide();
    e.transform = 'translateX(' + c[0] + 'px) translateY(' + c[1] + 'px)';
    e.height = e.width = radius + 'px';
    b.element.style.borderRadius = g;
    f.setAttribute('r', radius);
    f.setAttribute('fill', color);
    b.showBy({
      'target': clip,
      'align': 'c-c',
      'matchSize': true
    });
  }
  onAnimationEnd(a) {
    if (a.animationName === 'b-ripple-expand') {
      this.hide();
    }
  }
};
Widget.RippleClass = Ripple;
Ripple._$name = 'Ripple';
const isActive = a => a.element.classList.contains('b-visible');
const isPending = a => !isActive(a) && a.isVisible;
const _Mask = class _Mask extends Widget {
  static counter = 0;
  static delayable = {
    'deferredClose': 0,
    'delayedShow': 0,
    'syncCover': {
      'type': 'throttle',
      'delay': 100
    }
  };
  static configurable = {
    'autoClose': null,
    'cover': null,
    'icon': 'b-icon b-icon-spinner',
    'errorDefaults': {
      'icon': 'b-icon b-icon-warning',
      'autoClose': 3000,
      'showDelay': 0
    },
    'maxProgress': null,
    'mode': 'dark',
    'progress': null,
    'target': {
      '$config': 'nullify',
      'value': void 0
    },
    'text': null,
    'type': null,
    'showDelay': null,
    'useTransition': false
  };
  static type = 'mask';
  static $name = 'Mask';
  static get(a) {
    return DomDataStore.get(a, 'masks') || DomDataStore.set(a, 'masks', []);
  }
  static getActive(a) {
    return _Mask.get(a).find(isActive) || null;
  }
  static getPending(a) {
    return _Mask.get(a).filter(isPending).sort(Widget.weightSortFn);
  }
  static mask(a, b = document.body) {
    return _Mask.new({ 'target': b }, a);
  }
  static mergeConfigs(...a) {
    a = a.map(b => typeof b === 'string' ? { 'text': b } : b);
    return super.mergeConfigs(...a);
  }
  static sync(a) {
    const b = _Mask.getActive(a), c = !b && _Mask.getPending(a)[0] || null, e = b || c, f = (e == null ? void 0 : e.mode) || '', g = f.endsWith('blur') ? 'b-masked-' + f : '';
    let i, j, k;
    if (c) {
      j = c.element.classList;
      if (c.showDelay && !a.classList.contains('b-masked')) {
        j.add('b-delayed-show');
        c.delayedShow();
      }
      j.add('b-visible');
    }
    a.classList.toggle('b-masked', Boolean(e));
    for (i of a.children) {
      j = i.classList;
      if (!j.contains('b-mask')) {
        k = Array.from(j).filter(l => l.startsWith('b-masked-') && g !== l);
        k.length && j.remove(...k);
        g && j.add(g);
      }
    }
  }
  static unmask(a = document.body) {
    let b;
    const c = _Mask.get(a), e = c.length;
    return e ? (b = c[e - 1]) == null ? void 0 : b.close() : null;
  }
  static unmaskAll(a = document.body) {
    _Mask.get(a).filter(b => !isActive(b)).forEach(b => b.destroy());
    return _Mask.unmask(a);
  }
  destroy() {
    if (this.type !== 'trial') {
      super.destroy();
    }
  }
  compose() {
    const {
      icon: a,
      maxProgress: b,
      mode: c,
      progress: e,
      showDelay: f,
      text: g,
      useTransition: i
    } = this;
    return {
      'class': {
        'b-mask': 1,
        'b-delayed-show': f,
        'b-progress': b,
        'b-prevent-transitions': !i,
        ['b-mask-' + c]: 1
      },
      'children': {
        'maskContent': {
          'class': 'b-mask-content',
          'children': {
            'progressElement': b ? {
              'class': 'b-mask-progress-bar',
              'style': { 'width': Math.max(0, Math.min(100, Math.round(e / b * 100))) + '%' }
            } : null,
            'maskText': {
              'class': 'b-mask-text',
              'html': (a ? '<i class="b-mask-icon ' + a + '"></i>' : '') + g
            }
          }
        }
      }
    };
  }
  generateAutoId() {
    const {type: a} = this;
    return 'mask' + (typeof a === 'string' ? a.trim() : '') + '-' + _Mask.counter++;
  }
  updateAutoClose(a) {
    this.deferredClose.cancel();
    if (a) {
      this.deferredClose.delay = a;
      this.deferredClose();
    }
  }
  updateCover() {
    this.syncCover();
  }
  syncCover() {
    let a, b;
    (b = (a = this.owner) == null ? void 0 : a.syncMaskCover) == null ? void 0 : b.call(a, this);
  }
  set error(a) {
    this.setConfig({
      ...this.errorDefaults,
      'text': a
    });
  }
  onOwnerResize() {
    this.syncCover();
  }
  updateOwner(a) {
    this.detachListeners('cover');
    a == null ? void 0 : a.ion({
      'name': 'cover',
      'recompose': 'onOwnerResize',
      'resize': 'onOwnerResize',
      'thisObj': this
    });
  }
  updateShowDelay(a) {
    const {delayedShow: b} = this;
    b.delay = a;
    if (!a) {
      b.flush();
    }
  }
  changeTarget(a) {
    if (a === void 0) {
      a = document.body;
    } else if (typeof a === 'string') {
      a = this.owner[a];
    }
    return a;
  }
  updateTarget(a, b) {
    const c = this, {
        id: e,
        element: f
      } = c, g = b && _Mask.get(b);
    if (b) {
      if (b[e] === c) {
        delete b[e];
      }
      ArrayHelper.remove(g, c);
      _Mask.sync(b);
    }
    if (a) {
      if (!a[e]) {
        a[e] = c;
      }
      if (f.parentNode !== a) {
        a.appendChild(f);
      }
      ArrayHelper.include(_Mask.get(a), c);
      _Mask.sync(a);
    } else {
      f.remove();
    }
  }
  deferredClose() {
    let a;
    const b = this, {owner: c} = b;
    b.close().then(() => {
      let e;
      return (e = c == null ? void 0 : c.onMaskAutoClose) == null ? void 0 : e.call(c, b);
    });
    (a = c == null ? void 0 : c.onMaskAutoClosing) == null ? void 0 : a.call(c, b);
  }
  delayedShow() {
    this.element.classList.remove('b-delayed-show');
  }
  updateHidden(a, b) {
    let c;
    (c = super.updateHidden) == null ? void 0 : c.call(this, a, b);
    a && this.element.classList.remove('b-visible');
    _Mask.sync(this.target);
  }
  afterShow(...a) {
    let b;
    (b = super.afterShow) == null ? void 0 : b.call(this, ...a);
    _Mask.sync(this.target);
  }
  afterHide(...a) {
    let b;
    (b = super.afterHide) == null ? void 0 : b.call(this, ...a);
    this.element.classList.remove('b-visible');
    _Mask.sync(this.target);
  }
  async close() {
    await this.hide();
    this.destroy();
  }
};
const Mask = _Mask;
Mask.initClass();
Mask._$name = 'Mask';
const emptyObject7 = Object.freeze({});
const {isArray: isArray2} = Array;
const returnWeight = a => a.weight;
const isNotHidden = a => a && !a.hidden;
const stylesToCheck = [
  'display',
  'flex-direction'
];
const boxLayouts = {
  'default': 1,
  'box': 1
};
const Container = class extends Widget {
  static type = 'container';
  static $name = 'Container';
  static get configurable() {
    return {
      'items': null,
      'lazyItems': {
        '$config': ['lazy'],
        'value': null
      },
      'defaults': null,
      'defaultType': 'widget',
      'layoutStyle': null,
      'itemCls': null,
      'layout': { 'type': 'default' },
      'namedItems': {
        '$config': 'lazy',
        'value': null
      },
      'overflowable': {
        'value': null,
        'default': false,
        '$config': null
      },
      'textContent': true,
      'record': null,
      'strictRecordMapping': null,
      'autoUpdateRecord': null,
      'autoUpdateFields': null,
      'hideWhenEmpty': null,
      'contentElMutationObserver': {
        '$config': [
          'lazy',
          'nullify'
        ],
        'value': true
      },
      'isolateFields': false,
      'focusDescendant': false,
      'defaultBindProperty': null,
      'defaultFocus': null
    };
  }
  static get prototypeProperties() {
    return {
      'flexRowCls': 'b-flex-row',
      'flexColCls': 'b-flex-column',
      'initialItems': true
    };
  }
  startConfigure(a) {
    const b = a.items || a.lazyItems;
    if (!(this.hasItems = Boolean(b && (isArray2(b) ? b : Object.keys(b)).length))) {
      this.initialItems = false;
    }
    super.startConfigure(a);
  }
  get firstItem() {
    return this.getAt(0);
  }
  get lastItem() {
    return this.getAt(-1);
  }
  getPositionableItems(a, b) {
    let c;
    const e = ((c = this._items) == null ? void 0 : c.filter(f => a ? f.positionable === a : f.positionable)) || [];
    return b ? Object.fromEntries(e.map((f, g) => [
      '$' + f.positionable + g,
      f.element
    ])) : e;
  }
  getPositionableLocation(a) {
    return [this.contentElement];
  }
  getAt(a) {
    return this.ensureItems().at(a);
  }
  remove(...a) {
    let b = true;
    if (a.length === 1) {
      if (isArray2(a[0])) {
        a = a[0];
      } else {
        b = false;
      }
    }
    const c = this, {_items: e} = c, f = [];
    for (let g = 0; g < a.length; g++) {
      const j = a[g];
      if (e.includes(j)) {
        e.remove(j);
        c.layout.removeChild(j);
        f.push(j);
        c.onChildRemove(j);
      }
    }
    return b ? f : f[0];
  }
  removeAll() {
    return this.remove(this.items);
  }
  add(...a) {
    const b = this, c = b.ensureItems(), e = [];
    let f = true, g, j, k;
    if (a.length === 1) {
      if (isArray2(a[0])) {
        a = a[0];
      } else {
        f = false;
      }
    }
    for (j = 0; j < a.length; j++) {
      g = a[j];
      if (g.isWidget) {
        g.parent = b;
      } else {
        g = b.createWidget(g);
      }
      if ((g == null ? void 0 : g.weight) != null) {
        k = ArrayHelper.findInsertionIndex(g, c.values, Widget.weightSortFn);
        e.push(b.insert(g, k));
      } else if (g) {
        if (!c.includes(g)) {
          c.add(g);
          b.onChildAdd(g);
          b.layout.appendChild(g);
          e.push(g);
        }
      }
    }
    return f ? e : e[0];
  }
  ensureItems() {
    const a = this;
    a.getConfig('items');
    a.getConfig('lazyItems');
    if (!a._items) {
      a.items = [];
    }
    return a._items;
  }
  insert(a, b) {
    const c = this, e = c.ensureItems();
    if (a instanceof Widget) {
      a.parent = c;
    } else {
      a = c.createWidget(a);
    }
    if (e.includes(b)) {
      b = c.indexOfChild(b);
    }
    b = Math.min(b, e.count);
    const f = e.values;
    f.splice(b, 0, a);
    e.values = f;
    c.onChildAdd(a);
    c.layout.insertChild(a, b);
    return a;
  }
  indexOfChild(a) {
    return this.items.indexOf(a);
  }
  changeLazyItems(a) {
    this.items = a;
    this.layout.renderChildren();
  }
  onChildWeightChange(a) {
    let b;
    if (this.items.length > 1) {
      const {lastItem: c} = this;
      this._items.sort(Widget.weightSortFn);
      const {items: e} = this;
      this.contentElement.insertBefore(a.element, ((b = e[e.indexOf(a) + 1]) == null ? void 0 : b.element) || c.element.nextSibling);
    }
  }
  changeItems(a, b) {
    const c = this, e = [], f = new Bag();
    if (isArray2(a)) {
      c.processItemsArray(a, e);
    } else if (a) {
      c.processItemsObject(a, c.namedItems, e);
    }
    if (e.some(returnWeight)) {
      e.sort(Widget.weightSortFn);
    }
    f.add(e);
    if (b) {
      b.forEach(g => {
        c.remove(g);
        if (!f.includes(g) && g._createdBy === c) {
          g.destroy();
        }
      });
    }
    return f;
  }
  afterConstruct() {
    const {rtl: a} = this, {classList: b} = this.contentElement;
    b.toggle('b-rtl', a === true);
    b.toggle('b-ltr', a === false);
  }
  updateRtl(a) {
    super.updateRtl(a);
    const {contentElement: b} = this;
    if (b) {
      b.classList.toggle('b-rtl', a === true);
      b.classList.toggle('b-ltr', a === false);
    }
  }
  updateItems(a, b) {
    let c = 0;
    a.forEach(e => {
      this.onChildAdd(e);
      if (b && !b.temporary) {
        this.layout.insertChild(e, c);
      }
      ++c;
    });
    this.initialItems = false;
  }
  updateHideWhenEmpty() {
    this.syncChildCount(this.rendered);
  }
  get items() {
    const a = this;
    a.getConfig('lazyItems');
    if (!a._items) {
      if (a.initializingItems) {
        return [];
      }
      a._items = new Bag();
      a._items.temporary = true;
    }
    return a._items.values;
  }
  processItemsArray(a, b) {
    const c = a.length;
    let e, f;
    for (e = 0; e < c; e++) {
      f = a[e];
      if (f instanceof Widget) {
        f.parent = this;
        f.element.classList.remove(...Widget.outerCls);
      } else {
        f = this.createWidget(f);
      }
      if (f) {
        if (f.ref || f.id) {
          this.addDescendant(f);
        }
        b.push(f);
      }
    }
  }
  processItemsObject(a, b, c = []) {
    let e, f;
    for (f in a) {
      e = a[f];
      if (e) {
        if (b && f in b) {
          e = typeof e === 'object' ? ObjectHelper.merge(ObjectHelper.clone(b[f]), e) : b[f];
        }
        if (e) {
          if (e instanceof Widget) {
            e.parent = this;
          } else {
            if (e instanceof Object) {
              e.ref = f;
            }
            e = this.createWidget(e);
          }
          if (e) {
            e.ref = f;
            this.addDescendant(e);
            c.push(e);
          }
        }
      }
    }
    return c;
  }
  onChildAdd(a) {
    const b = this;
    if (a.innerItem && b.readOnly && !a.ignoreParentReadOnly) {
      a.readOnly = true;
    }
    b.onChildAddLayout(a);
    if (a.ref || a.id) {
      for (let c = b; c; c = c.parent) {
        c.addDescendant(a);
      }
    }
    b.syncChildCount(true);
    !b.onConfigChange.$nullFn && b.onConfigChange({
      'name': 'items',
      'config': b.$meta.configs.items
    });
  }
  onChildAddLayout(a) {
    if (a.innerItem) {
      this.layout.onChildAdd(a);
    }
  }
  onChildHide(a) {
    let b;
    super.onChildHide(a);
    if ((b = this._items) == null ? void 0 : b.includes(a)) {
      this.syncChildCount(true);
    }
  }
  onChildShow(a) {
    let b;
    super.onChildShow(a);
    if ((b = this._items) == null ? void 0 : b.includes(a)) {
      this.syncChildCount(true);
    }
  }
  syncChildCount(a) {
    if (!this.isConfiguring && !this.isDestroying) {
      const b = this, {
          items: c,
          hasItems: e
        } = b, f = c.filter(isNotHidden), {length: g} = f;
      b.visibleChildCount = g;
      if (b.hideWhenEmpty && a) {
        const i = Boolean(!g);
        if (Boolean(b._hidden) !== i) {
          b.hidden = i;
        }
      }
      c.forEach(j => j.element.classList.remove('b-last-visible-child', 'b-first-visible-child'));
      if (g) {
        f[0].element.classList.add('b-first-visible-child');
        f[g - 1].element.classList.add('b-last-visible-child');
      }
      b.hasItems = Boolean(b.childItems.length);
      b.contentElement.classList[g ? 'remove' : 'add']('b-no-visible-children');
      if (!b.isComposable) {
        b.updateTextContent(b._textContent);
      } else if (e !== b.hasItems) {
        b.recompose();
      }
    }
  }
  syncFlexDirection() {
    const a = this, {
        contentElement: b,
        flexColCls: c,
        flexRowCls: e
      } = a, f = new DomClassList(b.className), g = DomHelper.getStyleValue(b, stylesToCheck);
    f[e] = f[c] = 0;
    if (g.display === 'flex') {
      f[g['flex-direction'] === 'row' ? e : c] = 1;
    }
    DomHelper.syncClassList(b, f);
  }
  addDescendant(a) {
    const b = a.ref || a.id, c = this._widgetMap || (this._widgetMap = {});
    if (!c[b]) {
      c[b] = a;
    }
  }
  cleanupWidgetMap(a) {
    const b = this, c = new Map(), e = a.ref || a.id;
    if (e) {
      c.set(e, a);
    }
    a.eachWidget(f => {
      const g = f.ref || f.id;
      if (g) {
        c.set(g, f);
      }
    });
    for (let f = b; f; f = f.parent) {
      for (const [g, i] of c) {
        if (f.widgetMap[g] === i) {
          delete f.widgetMap[g];
        }
      }
    }
  }
  onChildRemove(a) {
    const b = this;
    b.cleanupWidgetMap(a);
    b.layout.onChildRemove(a);
    b.syncChildCount(true);
    !b.onConfigChange.$nullFn && b.onConfigChange({
      'name': 'items',
      'config': b.$meta.configs.items
    });
  }
  get widgetMap() {
    if (!this._widgetMap) {
      this._widgetMap = {};
    }
    if (!this.initializingItems) {
      this.getConfig('items');
    }
    return this._widgetMap;
  }
  changeRecord(a) {
    this._record = a == null ? emptyObject7 : null;
    return a;
  }
  updateRecord(a) {
    let b;
    const c = this;
    (b = c.recordUpdateDetacher) == null ? void 0 : b.call(c);
    c.trigger('beforeSetRecord', { 'record': a });
    c.setValues(a, {
      'onlyName': c.strictRecordMapping,
      'highlight': Boolean(c.$highlight)
    });
    if (c.autoUpdateFields && (a == null ? void 0 : a.firstStore)) {
      c.recordUpdateDetacher = a.firstStore.ion({
        'update': c.onRecordUpdated,
        'thisObj': c
      });
    }
  }
  setRecord(a, b) {
    this.$highlight = b;
    this.record = a;
    this.$highlight = false;
  }
  onRecordUpdated({record: a}) {
    if (a === this.record) {
      this.setValues(this.record, true, true);
    }
  }
  onFieldChange({
    source: a,
    userAction: b
  }) {
    if (this.autoUpdateRecord && b) {
      const {
          record: c,
          strictRecordMapping: e
        } = this, {
          name: f,
          ref: g,
          isValid: isValid = true,
          defaultBindProperty: i
        } = a, j = a[i || 'value'], k = e ? f : f || g;
      if (c && k && isValid) {
        if (c.isModel) {
          c.setValue(k, j);
        } else {
          c[k] = j;
        }
      }
    }
  }
  getValues(a) {
    const b = {};
    this.eachWidget((c, e) => {
      if (c.isolateFields) {
        e.down = false;
      } else if ('name' in c && (!a || a(c))) {
        b[c.name] = c.value;
      }
    }, true);
    return b;
  }
  get values() {
    const a = {};
    this.gatherValue(a);
    return a;
  }
  set values(a) {
    this.assignValue(a);
  }
  get isSettingValues() {
    return Boolean(this.assigningValues);
  }
  get assigningValues() {
    let a;
    return this._assigningValues || ((a = this.parent) == null ? void 0 : a.assigningValues);
  }
  set assigningValues(a) {
    this._assigningValues = a;
  }
  assignValue(a, b) {
    super.assignValue(a, b);
    if (!this.isolateFields) {
      this.setValues(a, b);
    }
  }
  gatherValue(a) {
    super.gatherValue(a);
    if (!this.isolateFields) {
      this.gatherValues(a);
    }
  }
  setValues(a, b = this.assignValueDefaults) {
    this.assigningValues = b;
    this.eachWidget(c => c.assignValue(a, b), false);
    this.assigningValues = false;
  }
  get hasNoChildren() {
    const a = this, {
        items: b,
        lazyItems: c
      } = a.initialConfig, e = b && (isArray2(b) ? b : ObjectHelper.values(b)), f = c && (isArray2(c) ? c : ObjectHelper.values(c)), g = a.isConfiguring ? f || e : a.items;
    return !(g == null ? void 0 : g.filter(isNotHidden).length);
  }
  afterRecompose() {
    super.afterRecompose();
    this.realign();
  }
  updateTextContent(a) {
    const b = this;
    if (!b.isComposable) {
      const c = Boolean(a && b.hasNoChildren), {classList: e} = b.contentElement, f = c !== e.contains('b-text-content');
      if (f) {
        e[c ? 'add' : 'remove']('b-text-content');
        if (b.rendered) {
          b.realign();
        }
      }
    }
  }
  updateLayoutStyle(a) {
    DomHelper.applyStyle(this.contentElement, a);
  }
  updateElement(a) {
    super.updateElement(...arguments);
    if (a) {
      const {classList: b} = this.contentElement, {containerCls: c} = this.layout;
      b.add('b-content-element');
      if (c) {
        b.add(c);
      }
    }
  }
  onInternalPaint() {
    let a;
    (a = super.onInternalPaint) == null ? void 0 : a.call(this, ...arguments);
    this.getConfig('contentElMutationObserver');
  }
  changeContentElMutationObserver(a, b) {
    if (b) {
      b.takeRecords();
      b.disconnect();
    }
    if (a) {
      const c = this, {
          element: e,
          contentElement: f
        } = c;
      a = new MutationObserver(g => c.onContentElMutation(g));
      a.observe(f, { 'attributes': true });
      if (f !== e) {
        a.observe(e, { 'attributes': true });
      }
      c.syncFlexDirection();
    }
    return a;
  }
  onContentElMutation(a) {
    if (boxLayouts[this.layout.type] && this.isVisible && a.some(b => this.contentElement.contains(b.target))) {
      this.syncFlexDirection();
    }
  }
  changeLayout(a, b) {
    return Layout.reconfigure(b, a, {
      'owner': this,
      'defaults': { 'owner': this }
    });
  }
  get childItems() {
    return this.items;
  }
  get childItemsOrderedByRelevance() {
    return this.items;
  }
  getWidgetById(a) {
    return this.widgetMap[a];
  }
  processWidgetConfig(a) {
  }
  setupWidgetConfig(a, b) {
    const c = this;
    if (typeof a === 'string') {
      a = { 'html': a };
    } else if (a.nodeType === Element.ELEMENT_NODE) {
      a = {
        'element': a,
        'id': a.id
      };
    }
    if (typeof b === 'string' || !b && (b = a.type)) {
      if (b === 'checkbox' && BrowserHelper.isMobile && Widget.resolveType('slidetoggle', true)) {
        b = a.type = 'slidetoggle';
      }
      b = Widget.resolveType(b, true);
    }
    a = (b || Widget).mergeConfigs(c.defaults, a, { 'parent': c });
    for (let e = a.parent; e; e = e.parent) {
      if (e.processWidgetConfig(a) === false) {
        return null;
      }
    }
    if (c.trigger('beforeWidgetCreate', { 'widgetConfig': a }) === false) {
      return null;
    }
    return a;
  }
  createWidget(a) {
    const b = Widget.create(this.setupWidgetConfig(a), this.defaultType);
    b && (b._createdBy = this);
    return b;
  }
  updateDefaults(a, b) {
    if (!this.isConfiguring && a) {
      const c = Object.entries(a);
      this.eachWidget(e => {
        c.forEach(([f, g]) => {
          if (!b || e[f] === b[f]) {
            e[f] = g;
          }
        });
      }, false);
    }
  }
  render() {
    this.getConfig('lazyItems');
    this.layout.renderChildren();
    this.syncChildCount();
    super.render(...arguments);
  }
  get focusElement() {
    let a = this.defaultFocus && this.query(this.defaultFocus);
    if (!this.defaultFocus || !a) {
      a = this.query(b => b.isFocusable, true);
    }
    if (a) {
      return a.focusElement;
    }
    return super.focusElement;
  }
  doDestroy() {
    let a;
    (a = this._items) == null ? void 0 : a.forEach(b => {
      let c;
      return (c = b.destroy) == null ? void 0 : c.call(b);
    });
    this.layout.destroy();
    super.doDestroy();
  }
  get isValid() {
    let a = true;
    this.eachWidget(b => {
      if ('isValid' in b && b.isVisibleForValidation && !b.isValid) {
        a = false;
      }
    }, true);
    return a;
  }
};
Container.initClass();
Container._$name = 'Container';
const {assign} = ObjectHelper;
const now = () => performance.now();
const nostart = { 'start': false };
const unitRe = /^[.\d]+([^\d].*)?$/;
const getTransitions = a => {
  const b = a.$bryntum || (a.$bryntum = {});
  return b.transitions || (b.transitions = Object.create(null));
};
const milliseconds = (a, b) => {
  let c;
  if (typeof a === 'string') {
    b = (c = unitRe.exec(a)) == null ? void 0 : c[1];
    a = parseFloat(a);
  }
  return a && a * (b === 's' || !b && a < 10 ? 1000 : 1);
};
const syncTransitions = a => {
  const b = ObjectHelper.values(getTransitions(a)).filter(c => c.completed == null).map(c => c.toString());
  a.style.transition = b.join(', ');
};
const Anim = a => class b extends a {
  static get prototypeProperties() {
    return {
      '_delay': null,
      '_duration': null,
      '_retain': null,
      '_timing': null,
      '_unit': null,
      'owner': null,
      'reverting': null
    };
  }
  constructor(...c) {
    super(...c);
    this.id = ++idSeed;
  }
  start() {
    this.startTime = now();
  }
  get delay() {
    let c, e, f;
    return milliseconds((f = (e = this._delay) != null ? e : (c = this.owner) == null ? void 0 : c.delay) != null ? f : 0, this.unit);
  }
  set delay(c) {
    this._delay = c;
  }
  get duration() {
    let c;
    const {
      owner: e,
      unit: f
    } = this;
    return milliseconds((c = this._duration) != null ? c : e ? e.duration : Animator.defaultDuration, f);
  }
  set duration(c) {
    this._duration = c;
  }
  get elapsed() {
    return now() - this.startTime;
  }
  get remaining() {
    return this.duration - this.elapsed;
  }
  get retain() {
    let c, e;
    return (e = this._retain) != null ? e : (c = this.owner) == null ? void 0 : c.retain;
  }
  set retain(c) {
    this._retain = c;
  }
  get timing() {
    let c, e;
    return (e = this._timing) != null ? e : (c = this.owner) == null ? void 0 : c.timing;
  }
  set timing(c) {
    this._timing = c;
  }
  get unit() {
    let c, e;
    return (e = this._unit) != null ? e : (c = this.owner) == null ? void 0 : c.unit;
  }
  set unit(c) {
    this._unit = c;
  }
};
var idSeed = 0;
const AnimatorTransition = class _AnimatorTransition extends Base.mixin(Anim) {
  static get $name() {
    return 'AnimatorTransition';
  }
  static get prototypeProperties() {
    return {
      'element': null,
      'property': null,
      'from': null,
      'to': null,
      'completed': null,
      'promissory': null,
      'reverting': null
    };
  }
  afterConstruct() {
    super.afterConstruct();
    const a = this, {
        element: b,
        transitions: c
      } = a;
    let {property: e} = a;
    [e, a.to] = DomHelper.unitize(e, a.to);
    a.from = DomHelper.unitize(e, a.from)[1];
    a.promissory = new Promissory();
    a.property = e;
    const f = c[e];
    c[e] = a;
    let {from: g} = a;
    if (f) {
      a.from = f.to;
      g = null;
      f.destroy();
    }
    if (g === null) {
      g = a.getCurrentStyleValue();
      if (!f) {
        a.from = g;
      }
    }
    b.style[e] = g;
    a.getCurrentStyleValue();
  }
  doDestroy() {
    const a = this;
    a.finish(false);
    if (a.completed && !a.retain) {
      a.clearStyle();
    }
    super.doDestroy();
  }
  get promise() {
    let a;
    return (a = this.promissory) == null ? void 0 : a.promise;
  }
  get transitions() {
    return getTransitions(this.element);
  }
  clearStyle() {
    this.setStyle('');
  }
  finish(a) {
    const b = this, {
        transitions: c,
        promissory: e,
        property: f
      } = b;
    if (e) {
      b.completed = a;
      b.promissory = null;
      e.resolve(a);
      if (c[f] === b) {
        delete c[f];
        if (!a) {
          syncTransitions(b.element);
          b.clearStyle();
        }
      } else {
        b.completed = false;
      }
    }
  }
  getCurrentStyleValue() {
    return DomHelper.getStyleValue(this.element, this.property);
  }
  revert() {
    const {
      duration: a,
      elapsed: b,
      element: c,
      from: e,
      property: f,
      _retain: g,
      _timing: i
    } = this;
    return new _AnimatorTransition({
      'element': c,
      'property': f,
      'retain': g,
      'timing': i,
      'duration': Math.round(Math.min(a, b)),
      'reverting': this,
      'to': e,
      'unit': 'ms'
    });
  }
  setStyle(a) {
    this.element.style[this.property] = a;
  }
  start() {
    const a = this, {
        delay: b,
        duration: c,
        element: e,
        property: f
      } = a;
    EventHelper.onTransitionEnd({
      'element': e,
      'property': f,
      'duration': b + c + 20,
      'thisObj': a.owner,
      'handler': () => {
        let g;
        return (g = a.finish) == null ? void 0 : g.call(a, true);
      }
    });
    super.start();
    a.setStyle(a.to);
  }
  toString() {
    const {
      delay: a,
      duration: b,
      property: c,
      timing: e
    } = this;
    return c + ' ' + b + 'ms ' + (e || 'ease-in-out') + (a ? ' ' + a + 'ms' : '');
  }
};
AnimatorTransition.initClass();
var Animator = class _Animator extends Base.mixin(Anim) {
  static get $name() {
    return 'Animator';
  }
  static get prototypeProperties() {
    return {
      'element': null,
      'finalize': null,
      'prefinalize': null,
      'completed': null,
      'items': null
    };
  }
  static register(a, b) {
    if (ObjectHelper.isObject(a)) {
      ObjectHelper.entries(a).forEach(c => _Animator.register(...c));
      return;
    }
    _Animator.fx[a] = b;
    _Animator[a] = c => {
      if (DomHelper.isElement(c)) {
        c = {
          'element': c,
          [a]: {}
        };
      } else {
        c = {
          'element': c.element,
          [a]: c
        };
        delete c[a].element;
      }
      return _Animator.run(c);
    };
  }
  static run(a) {
    return new _Animator(a).start();
  }
  constructor(a) {
    let b;
    super(null);
    const c = this, e = [], f = {};
    let g, i, j, k;
    if (Array.isArray(a)) {
      c.items = a;
    } else {
      for (j in a) {
        (_Animator.specialPropsRe.test(j) ? c : f)[j] = a[j];
      }
    }
    ObjectHelper.keys(f).forEach(l => {
      k = f[l];
      if (k != null && k !== false && k === k) {
        if (!(i = _Animator.fx[l])) {
          k = assign(c.defaults, typeof k === 'object' ? k : { 'to': k });
          k.owner = c;
          k.property = l;
          g = new AnimatorTransition(k);
        } else {
          k = assign(c.defaults, i(k, c, l));
          k.owner = c;
          g = new _Animator(k);
        }
        e.push(g);
      }
    });
    (b = c.items) == null ? void 0 : b.forEach(l => {
      if (ObjectHelper.isInstantiated(l)) {
        l.owner = c;
      } else {
        l = assign(c.defaults, l);
        l.owner = c;
        l = new _Animator(l);
      }
      e.push(l);
    });
    c.items = e;
    c.promise = (e.length === 1 ? e[0].promise : Promise.all(e.map(l => l.promise))).then(l => {
      let n;
      (n = c.finish) == null ? void 0 : n.call(c, l);
      return c.completed || false;
    });
  }
  doDestroy() {
    this.items.forEach(b => b.destroy());
    super.doDestroy();
  }
  get defaults() {
    return { 'element': this.element };
  }
  get retain() {
    const {
      _retain: a,
      finalize: b,
      owner: c
    } = this;
    return a != null ? a : b ? false : c ? c.retain : true;
  }
  set retain(a) {
    super.retain = a;
  }
  done() {
    return this.promise;
  }
  finish(a) {
    let b, c;
    const e = this, {items: f} = e;
    syncTransitions(e.element);
    e.completed = typeof a === 'boolean' ? a : !a.some(g => !g);
    (b = e.prefinalize) == null ? void 0 : b.call(e, e.completed, e);
    while (f.length) {
      f.pop().destroy();
    }
    (c = e.finalize) == null ? void 0 : c.call(e, e.completed, e);
  }
  revert(a) {
    let b;
    const c = this, {reverting: e} = c, f = !a || ((b = a.start) != null ? b : true);
    let g = c.defaults;
    if (e) {
      if (e.finalize) {
        g.finalize = e.finalize;
      }
      if (e.prefinalize) {
        g.prefinalize = e.prefinalize;
      }
      g.retain = e._retain;
    }
    g = assign(g, a, {
      'items': c.items.map(i => i.revert(nostart)),
      'reverting': c
    });
    g = new _Animator(g);
    f && g.start();
    return g;
  }
  start() {
    const {
      element: a,
      items: b
    } = this;
    super.start();
    if (b.length) {
      syncTransitions(a);
      b.forEach(c => c.start());
    }
    return this;
  }
};
Animator.initClass().Transition = AnimatorTransition;
Animator.specialPropsRe = new RegExp('^(?:' + Object.keys(Animator.prototypeProperties).concat(Object.keys(Animator.superclass.prototypeProperties)).map(a => a[0] === '_' ? a.substr(1) : a).join('|') + ')$');
Animator.defaultDuration = 200;
Animator.fx = {};
Animator.register({
  'puff'(a) {
    if (a === true) {
      a = {};
    } else if (typeof a !== 'object') {
      a = { 'transform': 'scale(' + a + ')' };
    } else if (a.scale) {
      a = ObjectHelper.clone(a);
      a.transform = 'scale(' + a.scale + ')';
      delete a.scale;
    }
    return assign({
      'opacity': 0,
      'transform': 'scale(8)'
    }, a);
  }
});
Animator._$name = 'Animator';
const ClickRepeater = class extends Base.mixin(Delayable_default) {
  static get configurable() {
    return {
      'element': null,
      'delegate': null,
      'delay': 500,
      'startRate': 2,
      'endRate': 20,
      'accelerateDuration': 4000,
      'pressedCls': 'b-pressed'
    };
  }
  doDestroy() {
    let a;
    (a = this.mousedownRemover) == null ? void 0 : a.call(this);
    this.cancel();
    super.doDestroy();
  }
  cancel() {
    let a, b, c;
    const e = this;
    (a = e.activeElement) == null ? void 0 : a.classList.remove(e.pressedCls);
    e.activeElement = null;
    (b = e.acceleration) == null ? void 0 : b.cancel();
    e.activeListenerRemover = (c = e.activeListenerRemover) == null ? void 0 : c.call(e);
    e.clearTimeout(e.autoRepeatTimer);
    e.clearTimeout(e.repeatTimer);
  }
  updateElement(a) {
    let b;
    (b = this.mousedownRemover) == null ? void 0 : b.call(this);
    this.mousedownRemover = EventHelper.on({
      'element': a,
      'mousedown': 'onMouseDown',
      'thisObj': this
    });
  }
  onMouseDown(a) {
    const b = this, c = b.delegate ? a.target.closest(b.delegate) : b.element;
    b.cancel();
    if (c) {
      b.activeElement = c;
      b.triggerEvent = a;
      c.classList.add(b.pressedCls);
      b.activeListenerRemover = EventHelper.on({
        'mouseup': {
          'element': document,
          'capture': true,
          'handler': 'onMouseUp'
        },
        'mousemove': {
          'element': c,
          'handler': 'onTargetMouseMove'
        },
        'mouseleave': {
          'element': c,
          'handler': 'onTargetMouseLeave'
        },
        'mouseenter': {
          'element': c,
          'handler': 'onTargetMouseEnter'
        },
        'thisObj': b
      });
      b.autoRepeatTimer = b.setTimeout('startAutoRepeat', b.delay);
    }
  }
  onMouseUp() {
    this.cancel();
  }
  onTargetMouseLeave() {
    this.activeElement.classList.remove(this.pressedCls);
    this.clearTimeout(this.repeatTimer);
  }
  onTargetMouseEnter(a) {
    const b = this;
    if (!a.buttons) {
      b.cancel();
    } else {
      b.activeElement.classList.add(b.pressedCls);
      b.triggerEvent = a;
      b.fireClick();
    }
  }
  onTargetMouseMove(a) {
    this.triggerEvent = a;
  }
  startAutoRepeat() {
    const a = this;
    a.interval = a.startInterval = 1000 / a.startRate;
    a.accelerationDelta = a.startInterval - 1000 / a.endRate;
    a.fireClick();
    a.acceleration = FunctionHelper.animate(a.accelerateDuration, a.nextTick, a, 'easeOutQuad');
  }
  nextTick(a) {
    this.interval = this.startInterval - this.accelerationDelta * a;
  }
  fireClick() {
    const a = this, b = new MouseEvent('click', a.triggerEvent);
    a.triggerEvent.target.dispatchEvent(b);
    a.repeatTimer = a.setTimeout(a.fireClick, a.interval);
  }
};
ClickRepeater._$name = 'ClickRepeater';
const autoRotateRe = /^(?:undefined|null|LEFT|RIGHT)$/;
const inverted = {
  'TOP': 'BOTTOM',
  'RIGHT': 'LEFT',
  'BOTTOM': 'TOP',
  'LEFT': 'RIGHT'
};
const canonicalDock = a => {
  const b = a == null ? void 0 : a.toUpperCase();
  return [
    b,
    b === 'LEFT' || b === 'RIGHT'
  ];
};
const Rotatable_default = a => {
  let b;
  return b = class extends (a || Base) {
    static get configurable() {
      return {
        'rotate': null,
        'invertRotate': null
      };
    }
    compose() {
      const {rotate: c} = this;
      return {
        'class': {
          ['b-rotate-' + (c || '').toLowerCase()]: c,
          'b-rotate-vertical': c
        }
      };
    }
    syncRotationToDock(c) {
      if (autoRotateRe.test(String(this.rotate))) {
        const [e, f] = canonicalDock(c);
        this.rotate = f ? this.invertRotate ? inverted[e] : e : null;
      }
    }
    get widgetClass() {
      return null;
    }
  }, __publicField(b, '$name', 'Rotatable'), b;
};
const Tool = class extends Widget.mixin(Rotatable_default) {
  static configurable = {
    'align': {
      'value': null,
      '$config': { 'merge': 'replace' }
    },
    'href': null,
    'repeat': null,
    'defaultBindProperty': null
  };
  static type = 'tool';
  static $name = 'Tool';
  compose() {
    const {
      align: a,
      href: b
    } = this;
    return {
      'tag': b != null ? 'a' : 'button',
      'class': {
        ['b-align-' + (a || 'end')]: 1,
        'b-icon': 1
      },
      'listeners': { 'click': 'onInternalClick' }
    };
  }
  get focusElement() {
    return this.element;
  }
  get panel() {
    let a, b;
    if (((a = this.parent) == null ? void 0 : a.isPanel) && ((b = this.element) == null ? void 0 : b.parentNode.matches('.b-panel-header'))) {
      return this.parent;
    }
  }
  changeAlign(a) {
    return a;
  }
  onInternalClick(a) {
    const b = this, {
        handler: c,
        panel: e
      } = b, f = {
        'domEvent': a,
        'tool': b
      };
    if (b.disabled) {
      return;
    }
    b.trigger('click', f);
    if (!b.isDestroyed) {
      b.trigger('action', f);
      if (!b.isDestroyed && (e == null ? void 0 : e.trigger('toolClick', f)) !== false) {
        c && b.callback(c, e, [
          a,
          e,
          b
        ]);
      }
    }
  }
  onInternalKeyDown(a) {
    const b = a.key.trim() || a.code;
    if (b === 'Enter') {
      a.stopPropagation();
    }
  }
  updateDisabled(a, b) {
    let c;
    super.updateDisabled(a, b);
    a && ((c = this.repeat) == null ? void 0 : c.cancel());
  }
  changeRepeat(a, b) {
    b == null ? void 0 : b.destroy();
    return a && ClickRepeater.new({ 'element': this.element }, a);
  }
};
Tool.initClass();
Tool._$name = 'Tool';
const rightRe = /right/i;
const autoAlign = a => (a.collapsed || a.collapsing) && a.owner.expandedHeaderDock === 'top' && a.owner.collapsible.direction.match(rightRe) ? 'start' : 'end';
const CollapseTool = class extends Tool {
  static type = 'collapsetool';
  static $name = 'CollapseTool';
  static get configurable() {
    return {
      'autoAlign': true,
      'collapsed': null,
      'collapsing': null,
      'collapsify': false,
      'direction': 'up',
      'defaultBindProperty': null
    };
  }
  compose() {
    const {
      collapsed: a,
      direction: b
    } = this;
    return {
      'class': {
        ['b-icon-collapse-' + b]: 1,
        'b-collapsed': a
      }
    };
  }
  changeAutoAlign(a) {
    return a === true ? autoAlign : a;
  }
  syncAutoAlign() {
    const {autoAlign: a} = this;
    if (a) {
      this.align = a(this);
    }
  }
  updateAutoAlign() {
    this.syncAutoAlign();
  }
  updateCollapsed() {
    this.syncAutoAlign();
  }
  updateCollapsing() {
    this.syncAutoAlign();
  }
  updateDirection() {
    this.syncAutoAlign();
  }
};
CollapseTool.initClass();
CollapseTool._$name = 'CollapseTool';
const defaultedDirectionRe = /^(?:UP|DOWN|LEFT|RIGHT)$/;
const dockBeforeRe = /^(left|top)$/i;
const emptyObject8 = {};
const headerDockRe = /^b-dock-(top|left|right|bottom)$/;
const revealerCls = 'b-panel-collapse-revealer';
const sideDockRe = /^(?:left|right)$/i;
const unflexCls = 'b-collapse-unflex';
const {round} = Math;
const canonicalDirection = [
  'up',
  'down',
  'left',
  'right'
].reduce((a, b) => {
  a[b.toUpperCase()] = a[b] = b;
  return a;
}, {});
const clipByDock = {
  'top': [
    'top',
    'bottom'
  ],
  'right': [
    'right',
    'left'
  ],
  'bottom': [
    'bottom',
    'top'
  ],
  'left': [
    'left',
    'right'
  ]
};
const collapseDirectionByPlacement = {
  'hl': 'LEFT',
  'hr': 'RIGHT',
  'vb': 'DOWN',
  'vt': 'UP'
};
const crossAxis = {
  'h': 'w',
  'w': 'h'
};
const dockByDirection = {
  'up': 'top',
  'right': 'right',
  'down': 'bottom',
  'left': 'left'
};
const dockIsHorz = {
  'top': false,
  'right': true,
  'bottom': false,
  'left': true
};
const directionByDock = {
  'top': 'UP',
  'right': 'RIGHT',
  'bottom': 'DOWN',
  'left': 'LEFT'
};
const transverseTransform = {
  'top': a => 'translate(0, -' + round(a.height || 0) + 'px)',
  'bottom': a => 'translate(0, ' + round(a.height || 0) + 'px)',
  'right': a => 'translate(' + round(a.width || 0) + 'px, 0)',
  'left': a => 'translate(-' + round(a.width || 0) + 'px, 0)'
};
let idSeed2 = 0;
const PanelCollapser = class extends Base.mixin(Factoryable_default) {
  static type = 'inline';
  static $name = 'PanelCollapser';
  static get configurable() {
    return {
      'animation': { 'duration': 200 },
      'collapsed': {
        'value': null,
        '$config': null,
        'default': false
      },
      'direction': null,
      'collapseTooltip': 'L{Collapse}',
      'expandTooltip': 'L{Expand}',
      'panel': {
        'value': null,
        '$config': 'nullify'
      },
      'supportAxis': null,
      'tool': {
        'type': 'collapsetool',
        'handler'(a) {
          let b;
          (b = this.collapsible) == null ? void 0 : b.onCollapseClick(a);
        }
      }
    };
  }
  static get factoryable() {
    return { 'defaultType': 'inline' };
  }
  get collapsing() {
    return this.collapsingExpanding === 'collapsing';
  }
  get collapsingExpanding() {
    let a;
    const b = (a = this.currentOperation) == null ? void 0 : a.collapsing;
    return b == null ? null : b ? 'collapsing' : 'expanding';
  }
  get currentDock() {
    let a, b, c, e;
    return (e = (c = (b = (a = this.panel) == null ? void 0 : a.header) == null ? void 0 : b.dock) == null ? void 0 : c.toLowerCase()) != null ? e : 'top';
  }
  get expanding() {
    return this.collapsingExpanding === 'expanding';
  }
  get collapseTool() {
    let a, b;
    return (b = (a = this.panel) == null ? void 0 : a.tools) == null ? void 0 : b.collapse;
  }
  get collapseDim() {
    return sideDockRe.test(this.collapseDir) ? 'width' : 'height';
  }
  getCollapseDir(a) {
    let b;
    let {
      direction: c,
      panel: e
    } = this;
    if (!c || defaultedDirectionRe.test(c)) {
      const f = e == null ? void 0 : e.placement;
      if (f) {
        c = collapseDirectionByPlacement[f];
      } else {
        c = directionByDock[((b = e == null ? void 0 : e.header) == null ? void 0 : b.dock) || 'top'];
      }
    }
    return a ? canonicalDirection[c] : c;
  }
  get collapseDir() {
    return this.getCollapseDir(true);
  }
  get collapseDock() {
    return this.collapseInfo[0];
  }
  get collapseInfo() {
    const {panel: a} = this, b = a.hasHeader && a.expandedHeaderDock;
    let c = dockByDirection[this.collapseDir], e = false;
    if (b) {
      if (!(e = dockIsHorz[c] !== dockIsHorz[b])) {
        c = b;
      }
    }
    return [
      c,
      e
    ];
  }
  get toolsConfig() {
    const {
      direction: a,
      tool: b
    } = this;
    return b && { 'collapse': ObjectHelper.assign({ 'direction': a.toLowerCase() }, b) };
  }
  beforeCollapse(a) {
    const {panel: b} = this, {element: c} = b;
    if (c.contains(DomHelper.getActiveElement(c))) {
      b.revertFocus(true);
    }
  }
  changeTool(a) {
    const b = this, {panel: c} = b;
    if (b.isConfiguring || b.isDestroying || !c || c.isDestroying) {
      return a;
    }
    c.tools = { 'collapse': a };
  }
  collapse(a) {
    let b, c;
    const e = this, {panel: f} = e, g = {
        'id': ++idSeed2,
        'completed': false,
        'panel': f
      };
    let {currentOperation: i} = e;
    a = a != null ? a : true;
    if (ObjectHelper.isObject(a)) {
      g.collapsed = true;
      ObjectHelper.assign(g, a);
      a = g.collapsed;
      delete g.collapsed;
    }
    g.collapsing = a;
    g.previous = i != null ? i : null;
    if (a !== e.collapsed) {
      if (i) {
        if (i.collapsing !== a) {
          g.animation = i.animation.revert({
            'finalize'() {
              let j;
              (j = e.collapseFinalize) == null ? void 0 : j.call(e, g, true);
            }
          });
          g.collapsing = a;
          i = g;
        }
      } else {
        g.animation = ObjectHelper.clone(('animation' in g ? g : e).animation);
        if (e.beforeCollapse(g) !== false) {
          if (g.animation) {
            g.animation.finalize = j => {
              let k;
              return (k = e.collapseFinalize) == null ? void 0 : k.call(e, g, j);
            };
          }
          f.changingCollapse = true;
          e[a ? 'collapseBegin' : 'expandBegin'](g);
          if (g.animation) {
            i = g;
          } else {
            g.completed = true;
            e[a ? 'collapseEnd' : 'expandEnd'](g);
          }
          f.changingCollapse = false;
          if (!g.animation) {
            e.onComplete(a ? 'collapse' : 'expand');
          }
        }
      }
    } else if (i && i.collapsing !== a) {
      e[a ? 'expandRevert' : 'collapseRevert'](g);
      g.animation = i.animation.revert({
        'finalize'() {
          let j;
          (j = e.collapseFinalize) == null ? void 0 : j.call(e, g, false);
        }
      });
      i = g;
    }
    e.currentOperation = i;
    return (c = (b = i == null ? void 0 : i.animation) == null ? void 0 : b.done()) != null ? c : Promise.resolve(a === e.collapsed);
  }
  collapseFinalize(a, b) {
    const c = this, {
        currentOperation: e,
        panel: f
      } = c, g = f.collapsed ? 'expand' : 'collapse';
    if (e === a) {
      c.currentOperation = null;
      a.completed = b;
      f.changingCollapse = true;
      c[g + 'End'](a);
      f.changingCollapse = false;
      b && c.onComplete(g);
    }
  }
  applyHeaderDock(a, b = true) {
    const c = this, {
        currentDock: e,
        panel: f
      } = c, {header: g} = f, i = a ? c.collapseDock : f.expandedHeaderDock;
    if (i !== e && f.hasHeader) {
      f.header = g ? typeof g === 'string' ? { 'title': g } : ObjectHelper.clone(f.header) : {};
      f.header.dock = i;
      b && f.recompose.flush();
    }
  }
  composeHeader(a) {
    const {panel: b} = this, {class: c} = a, e = b.expandedHeaderDock || Object.keys(c).filter(f => c[f] && headerDockRe.test(f)).map(f => headerDockRe.exec(f)[1][0]);
    c[revealerCls] = 1;
    c['b-collapsible-' + e[0] + this.collapseDir[0]] = 1;
    return a;
  }
  composeTitle(a) {
    a.class[revealerCls] = 1;
    return a;
  }
  collapseBegin(a) {
    let b;
    const c = this, {animation: e} = a, {
        collapseDim: f,
        collapseTool: g,
        panel: i
      } = c, {
        element: j,
        placement: k
      } = i, [l, n] = c.collapseInfo, o = g == null ? void 0 : g.element.classList, p = !k || k[0] === 'h' && f === 'width' || k[0] === 'v' && f === 'height';
    c.configuredWidth = i._lastWidth;
    c.configuredHeight = i._lastHeight;
    c.transverseCollapse = n;
    c.applyHeaderDock(true);
    const q = i.rectangle(), r = c.lockInnerSize().moveTo(0, 0), u = r.clone(), v = clipByDock[l], w = (b = i.headerElement) == null ? void 0 : b.getBoundingClientRect(), z = round((w == null ? void 0 : w[f]) || 0);
    j.classList.toggle(unflexCls, p);
    if (e) {
      u[v[0]] = u[v[1]];
      j.classList.add('b-collapsing');
      o == null ? void 0 : o.add('b-collapsed', 'b-collapsing');
      if (g) {
        g.collapsing = true;
      }
      if (o) {
        o.remove('b-collapsed');
        i.rectangle();
        o.remove('b-collapsing');
        o.add('b-collapsed');
      }
      e.element = j;
      e.retain = true;
      e[f] = {
        'from': round(q[f]),
        'to': z
      };
      e.items = [{
          'element': c.innerElement,
          'retain': false,
          'clip': {
            'from': 'rect(' + r + ')',
            'to': 'rect(' + u + ')'
          }
        }];
      if (n) {
        e.items.push({
          'element': i.headerElement,
          'duration': e.duration,
          'retain': false,
          'transform': {
            'from': transverseTransform[l](w),
            'to': 'translate(0, 0)'
          }
        });
      }
      a.animation = Animator.run(e);
    } else {
      j.style[f] = z + 'px';
      if (g) {
        o.add('b-collapsing', 'b-collapsed');
        g.rectangle();
        o.remove('b-collapsing');
      }
    }
  }
  collapseEnd(a) {
    const b = this, {collapseTool: c} = b;
    b.panel.element.classList.remove('b-collapsing');
    if (c) {
      c.collapsing = false;
    }
    if (a.completed) {
      b.collapsed = true;
    } else {
      b.applyHeaderDock(false);
      b.restoreConfiguredSize();
      b.lockInnerSize(false);
    }
  }
  collapseRevert(a) {
    let b;
    (b = this.collapseTool) == null ? void 0 : b.element.classList.remove('b-collapsed');
  }
  expandBegin(a) {
    const b = this, {animation: c} = a, {
        collapseDim: e,
        collapseTool: f,
        panel: g
      } = b, [i, j] = b.collapseInfo, {element: k} = g, l = k.classList, n = l.contains(unflexCls), o = g.rectangle();
    l.remove('b-collapsed', 'b-collapsing');
    g.element.style[e] = '';
    b.restoreConfiguredSize();
    b.lockInnerSize(false);
    const p = g.rectangle(), q = b.lockInnerSize().moveTo(0, 0), r = q.clone(), u = clipByDock[b.collapseDock];
    f == null ? void 0 : f.element.classList.remove('b-collapsed');
    if (c) {
      r[u[0]] = r[u[1]];
      l.add('b-collapsed', 'b-expanding');
      n && l.add(unflexCls);
      c.element = k;
      c[e] = {
        'from': round(o[e]),
        'to': round(p[e])
      };
      c.items = [{
          'element': b.innerElement,
          'retain': false,
          'clip': {
            'from': 'rect(' + r + ')',
            'to': 'rect(' + q + ')'
          }
        }];
      if (j) {
        c.items.push({
          'element': g.headerElement,
          'duration': c.duration,
          'retain': false,
          'transform': {
            'from': 'translate(0, 0)',
            'to': transverseTransform[i](o)
          }
        });
      }
      a.animation = Animator.run(c);
    }
  }
  expandEnd(a) {
    const b = this;
    b.panel.element.classList.remove('b-expanding');
    if (a.completed) {
      b.collapsed = false;
      b.applyHeaderDock(false);
      b.restoreConfiguredSize();
      b.lockInnerSize(false);
    }
  }
  expandRevert(a) {
    let b;
    (b = this.collapseTool) == null ? void 0 : b.element.classList.add('b-collapsed');
  }
  get innerElement() {
    return this.panel.collapseWrapElement || this.panel.bodyWrapElement;
  }
  get innerSizeElement() {
    return this.transverseCollapse ? this.panel.element : this.innerElement;
  }
  get supportAxis() {
    let {_supportAxis: a} = this;
    const b = a === true;
    if (b || a == null) {
      a = this.collapseDim[0];
      if (b || DomHelper.getStyleValue(this.panel.element, 'position') === 'absolute') {
        a += crossAxis[a];
      }
    }
    return a || '';
  }
  lockInnerSize(a = true) {
    const b = this, {
        innerElement: c,
        panel: e
      } = b, f = a ? b.supportAxis : '', g = e.element, i = e.headerElement, j = a && i && Rectangle.from(i, g), k = a && Rectangle.from(b.innerSizeElement, g), l = c.style;
    if (i) {
      i.style.minWidth = f.includes('w') ? j.width + 'px' : '';
      i.style.minHeight = f.includes('h') ? j.height + 'px' : '';
    }
    l.width = a ? k.width + 'px' : '';
    l.height = a ? k.height + 'px' : '';
    c.classList[a ? 'add' : 'remove']('b-panel-collapse-size-locker');
    return k;
  }
  onCollapseClick(a) {
    let b = this.collapsing ? false : this.expanding ? true : !this.collapsed;
    if (a.altKey) {
      b = {
        'animation': null,
        'collapsed': b
      };
    }
    this.collapse(b);
  }
  onComplete(a) {
    let b;
    (b = this.panel) == null ? void 0 : b.trigger(a);
  }
  onPanelHeaderClick({event: a}) {
    if (a.button === 0 && this.panel.collapsed && a.target.classList.contains(revealerCls)) {
      this.onRevealerClick();
    }
  }
  onPanelConfigChange({
    name: a,
    value: b
  }) {
    const c = this, {panel: e} = c;
    if (a === 'collapsed') {
      if (e.isPainted) {
        c.collapsed = b;
      }
    } else if (a === 'header' && !e.changingCollapse) {
      c.syncDirection();
    }
  }
  onPanelPaint() {
    this.syncDirection();
    if (this.panel.collapsed && !this.collapsed) {
      this.collapse({
        'animation': null,
        'collapsed': true
      });
    }
  }
  onRevealerClick() {
    this.panel._collapse({ 'collapsed': false });
  }
  restoreConfiguredSize(a) {
    const {
      configuredHeight: b,
      configuredWidth: c,
      panel: e
    } = this;
    a = a != null ? a : 'wh';
    e.element.classList.remove(unflexCls);
    if (c != null && a.includes('w')) {
      e.width = c;
    }
    if (b != null && a.includes('h')) {
      e.height = b;
    }
  }
  splitHeaderItems({
    as: a,
    dock: b
  } = emptyObject8) {
    let c;
    return (c = this.panel) == null ? void 0 : c.splitHeaderItems({
      'as': a,
      'dock': b,
      'alt': true
    });
  }
  syncDirection() {
    const a = this, {direction: b} = a;
    if (!b || defaultedDirectionRe.test(b)) {
      a.direction = a.getCollapseDir();
    }
  }
  changeCollapsed(a) {
    return Boolean(a);
  }
  updateCollapsed(a) {
    const {
      collapseTool: b,
      panel: c
    } = this;
    if (c) {
      c.collapsed = a;
      c.element.classList[a ? 'add' : 'remove']('b-collapsed');
    }
    if (b) {
      b.collapsed = a;
    }
  }
  updateDirection(a) {
    const {
      collapseTool: b,
      panel: c
    } = this;
    if (b) {
      b.direction = canonicalDirection[a];
    }
    if (c == null ? void 0 : c.rendered) {
      c.recompose();
    }
  }
  updatePanel(a) {
    let b;
    const c = this;
    c.syncDirection();
    (b = c.panelChangeDetacher) == null ? void 0 : b.call(c);
    c.panelChangeDetacher = a && FunctionHelper.after(a, 'onConfigChange', 'onPanelConfigChange', c, { 'return': false });
  }
  wrapCollapser(a, b) {
    let c;
    const e = this, [f, g] = e.collapseInfo;
    if (!g) {
      return [
        a,
        b
      ];
    }
    const {
        collapseDir: i,
        panel: j
      } = e, {
        expandedHeaderDock: k,
        header: l,
        uiClassList: n
      } = j, o = (c = j.tools) == null ? void 0 : c.recollapse, [p, q] = e.splitHeaderItems({
        'as': 'element',
        'dock': f
      }), r = j.hasHeader ? j.title || (l == null ? void 0 : l.title) || '\xA0' : null, u = r && {
        'tag': 'header',
        'class': new DomClassList({
          ...n,
          ['b-dock-' + k]: 1,
          'b-panel-header': 1,
          'b-panel-collapser-header': 1
        }, l == null ? void 0 : l.cls),
        'children': [
          ...p,
          {
            'reference': 'collapserTitleElement',
            'html': r,
            'class': {
              ...n,
              ['b-align-' + ((l == null ? void 0 : l.titleAlign) || 'start')]: 1,
              'b-header-title': 1
            }
          },
          ...q
        ]
      };
    if (o) {
      o.direction = i;
    }
    return [
      'collapseWrapElement',
      {
        'class': {
          ...n,
          ['b-panel-collapser-header-' + k]: 1,
          ['b-panel-collapser-' + i]: 1,
          ['b-' + (dockIsHorz[k] ? 'h' : 'v') + 'box']: 1,
          'b-panel-collapser': 1,
          'b-box-center': 1
        },
        'children': dockBeforeRe.test(k) ? {
          'collapserHeaderElement': u,
          [a]: b
        } : {
          [a]: b,
          'collapserHeaderElement': u
        }
      }
    ];
  }
};
PanelCollapser.maps = {
  'clipByDock': clipByDock,
  'dockByDirection': dockByDirection,
  'dockIsHorz': dockIsHorz
};
PanelCollapser.initClass();
PanelCollapser._$name = 'PanelCollapser';
const PENDING = Symbol('pendingCreate');
const DynamicObject = class extends Base {
  static get prototypeProperties() {
    return {
      'cleanup': null,
      'configName': null,
      'created': null,
      'factory': null,
      'inferType': 'name',
      'owner': null,
      'ownerName': null,
      'proxyable': typeof Proxy !== 'undefined',
      'setup': null,
      'transform': null
    };
  }
  static get properties() {
    return {
      'defaults': {},
      'instances': {},
      'object': Object.create({})
    };
  }
  get proxy() {
    const a = this;
    let b = null;
    if (a.proxyable) {
      b = new Proxy(a.instances, {
        'get'(c, e) {
          return a.get(e);
        },
        'set'(c, e, f) {
          a.set(e, f);
          return true;
        },
        'deleteProperty'(c, e) {
          a.set(e, null);
          return true;
        }
      });
    }
    Reflect.defineProperty(a, 'proxy', {
      'configurable': true,
      'value': b
    });
    return b;
  }
  get target() {
    return this.proxy || this.object;
  }
  define(a, b) {
    const c = this, {transform: e} = c, f = e ? e(b, a) : b, g = ObjectHelper.isInstantiated(f);
    c.instances[a] = PENDING;
    if (!g) {
      c.setDefaults(a, f);
    }
    c.defineProp(a, true);
    if (g) {
      c.set(a, f);
    }
  }
  defineProp(a, b) {
    const c = this, {object: e} = c;
    if (!c.proxy) {
      Reflect.defineProperty(b ? Object.getPrototypeOf(e) : e, a, {
        'configurable': !b,
        'enumerable': true,
        'get'() {
          return c.get(a);
        },
        'set'(f) {
          return c.set(a, f);
        }
      });
    }
  }
  flush() {
    let a;
    const b = this;
    try {
      b.updating = true;
      for (const c in b.defaults) {
        b.get(c);
      }
      b.afterConfigureOwner = (a = b.afterConfigureOwner) == null ? void 0 : a.call(b);
    } finally {
      b.updating = false;
    }
  }
  get(a) {
    const {
      defaults: b,
      instances: c
    } = this;
    if (c[a] === PENDING) {
      this.set(a, PENDING);
    }
    return c[a] || b[a] && null;
  }
  set(a, b) {
    let c;
    const e = this, {
        cleanup: f,
        configName: g,
        defaults: i,
        factory: j,
        instances: k,
        owner: l,
        setup: n,
        transform: o,
        updating: p
      } = e, q = l && g && !p, r = k[a] === PENDING ? null : k[a], u = j.reconfigure(r, b === PENDING ? {} : b || null, {
        'cleanup': f && (v => f(v, a)),
        'defaults': i[a] || e.setDefaults(a, {}),
        'owner': e.owner,
        'setup': n && ((v, w, z) => n(v, a, w, z)),
        'transform': o && (v => o(v, a))
      });
    if (u !== r) {
      const v = q && { ...k };
      k[a] = u;
      if (u) {
        e.defineProp(a);
        (c = e.created) == null ? void 0 : c.call(e, u, a);
      } else {
        delete e.object[a];
        delete k[a];
      }
      if (q) {
        l.onConfigChange({
          'name': g,
          'config': l.$meta.configs[g],
          'value': e.target,
          'was': v
        });
      }
    }
  }
  setDefaults(a, b) {
    const {
        defaults: c,
        factory: e,
        inferType: f,
        instances: g,
        owner: i,
        ownerName: j
      } = this, {typeKey: k} = e.factoryable;
    b = b === true ? {} : ObjectHelper.assign({}, b);
    if (f === 'name' || f === true && !b[k]) {
      b[k] = a;
    }
    if (j) {
      b[j] = i;
    }
    b.beforeConfigure = l => {
      g[a] = l;
    };
    return c[a] = b;
  }
  update(a) {
    const b = this, {owner: c} = b;
    let e, f;
    try {
      b.updating = true;
      if (a) {
        for (e in a) {
          f = a[e];
          if (b.defaults[e]) {
            b.set(e, f);
          } else if (f) {
            b.define(e, f);
          }
        }
      } else {
        for (e in b.instances) {
          b.set(e, null);
        }
      }
    } finally {
      b.updating = false;
    }
    if (c) {
      if (!c.isConfiguring) {
        b.flush();
      } else if (!b.afterConfigureOwner) {
        b.afterConfigureOwner = FunctionHelper.before(c, 'afterConfigure', 'flush', b);
      }
    }
  }
};
DynamicObject.initClass();
DynamicObject._$name = 'DynamicObject';
const emptyArray7 = [];
const emptyObject9 = {};
const toolConfigs = {
  'align': 1,
  'weight': 1
};
const Toolable_default = a => {
  let b;
  return b = class extends (a || Widget) {
    static get configurable() {
      return {
        'tools': {
          'value': null,
          '$config': { 'nullify': true }
        },
        'toolDefaults': {
          '*': {
            'type': 'tool',
            'align': 'end'
          }
        }
      };
    }
    byWeightSortFn(c, e) {
      return (c.weight || 0) - (e.weight || 0);
    }
    byWeightReverseSortFn(c, e) {
      return (e.weight || 0) - (c.weight || 0);
    }
    gatherTools({
      align: c,
      alt: e,
      refs: f
    } = emptyObject9) {
      let g, j, k;
      const {
          collapsed: l,
          tools: n
        } = this, o = {
          'collapsed': l,
          'alt': e
        };
      let p = [], q, r, u, v, w;
      for (r in n) {
        w = n[r];
        q = (k = (j = (g = w == null ? void 0 : w.align) == null ? void 0 : g.align) != null ? j : w == null ? void 0 : w.align) != null ? k : 'end';
        if (q === c && w.isCollapsified(o)) {
          p.push(w);
        }
      }
      p.sort(this[c === 'end' ? 'byWeightReverseSortFn' : 'byWeightSortFn']);
      if (f) {
        const z = f === 'widget', A = {};
        for (u = 0; u < p.length; ++u) {
          v = p[u];
          A[v.ref] = z ? v : v.element;
        }
        p = A;
      }
      return p;
    }
    getEndTools({
      alt: c,
      refs: e
    } = emptyObject9) {
      return this.gatherTools({
        'align': 'end',
        'alt': c,
        'refs': e
      });
    }
    getStartTools({
      alt: c,
      refs: e
    } = emptyObject9) {
      return this.gatherTools({
        'align': 'start',
        'alt': c,
        'refs': e
      });
    }
    get childItems() {
      return [
        ...this.getStartTools(),
        ...this._items || emptyArray7,
        ...this.getEndTools()
      ];
    }
    changeTools(c, e) {
      const f = this, g = f.$tools || (f.$tools = new DynamicObject({
          'configName': 'tools',
          'factory': Widget,
          'inferType': false,
          'owner': f,
          'created'(i) {
            let j, k;
            i.innerItem = false;
            (k = i.syncRotationToDock) == null ? void 0 : k.call(i, (j = f.header) == null ? void 0 : j.dock);
            FunctionHelper.after(i, 'onConfigChange', (l, {name: n}) => {
              if (toolConfigs[n]) {
                f.onConfigChange({
                  'name': 'tools',
                  'value': g.target
                });
              }
            });
            f.onChildAdd(i);
          },
          'setup'(i, j) {
            i = ObjectHelper.merge({}, f.toolDefaults['*'], f.toolDefaults[j], i);
            i.parent = f;
            i.ref = j;
            return i;
          }
        }));
      g.update(c);
      if (!e) {
        return g.target;
      }
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'Toolable'), b;
};
const directionCls = [
  'b-hbox',
  'b-vbox'
];
const syncAlign = {
  'name': 'align',
  'style': 'alignItems'
};
const syncContentAlign = {
  'name': 'contentAlign',
  'style': 'alignContent'
};
const syncDirection = {
  'name': 'direction',
  'style': 'flexDirection'
};
const syncJustify = {
  'name': 'justify',
  'style': 'justifyContent',
  'classes': ['stretch']
};
const syncWrap = {
  'name': 'wrap',
  'style': 'flexWrap',
  'map': {
    'false': 'nowrap',
    'true': 'wrap',
    'reverse': 'wrap-reverse'
  }
};
const Box = class extends Layout {
  static configurable = {
    'containerCls': 'b-box-container',
    'itemCls': 'b-box-item',
    'align': null,
    'contentAlign': null,
    'direction': null,
    'horizontal': true,
    'justify': null,
    'reverse': null,
    'wrap': null
  };
  static alias = 'hbox';
  static type = 'box';
  static $name = 'Box';
  get vertical() {
    return this.horizontal === false;
  }
  set vertical(a) {
    return this.horizontal = !a;
  }
  updateAlign() {
    this.syncConfigStyle(syncAlign);
  }
  updateContentAlign() {
    this.syncConfigStyle(syncContentAlign);
  }
  updateDirection() {
    this.syncConfigStyle(syncDirection);
  }
  updateHorizontal() {
    let a;
    const b = this, c = (a = b.contentElement) == null ? void 0 : a.classList, e = Number(b.vertical);
    if (c) {
      c.remove(directionCls[1 - e]);
      c.add(directionCls[e]);
    } else {
      b.syncConfigLater('horizontal');
    }
    b.syncDirection();
  }
  updateJustify() {
    this.syncConfigStyle(syncJustify);
  }
  updateReverse() {
    this.syncDirection();
  }
  updateWrap() {
    this.syncConfigStyle(syncWrap);
  }
  syncDirection() {
    const a = this, {reverse: b} = a;
    a.direction = b ? '' + (a.vertical ? 'column' : 'row') + (b ? '-reverse' : '') : null;
  }
};
Box.initClass();
Box._$name = 'Box';
const VBox = class extends Box {
  static configurable = { 'horizontal': false };
  static type = 'vbox';
  static $name = 'VBox';
};
VBox.initClass();
VBox._$name = 'VBox';
const Badge_default = a => {
  let b;
  return b = class extends (a || Base) {
    static get configurable() {
      return { 'badge': null };
    }
    compose() {
      const {badge: c} = this;
      return {
        'dataset': { 'badge': c },
        'class': { 'b-badge': c != null && c !== '' }
      };
    }
  }, __publicField(b, '$name', 'Badge'), b;
};
const bIcon = /(?:^|\s)b-icon-/;
const bFa = /(?:^|\s)b-fa-/;
const fullConfigKeys = [
  'items',
  'type',
  'widgets',
  'html',
  'listeners'
];
const menuListenersName = Symbol('defaultListener');
const Button = class extends Widget.mixin(Badge_default, Rotatable_default) {
  static type = 'button';
  static $name = 'Button';
  static get configurable() {
    return {
      'icon': null,
      'menuIcon': 'b-icon-picker',
      'pressedIcon': null,
      'pressedCls': null,
      'iconAlign': 'start',
      'behaviorType': 'button',
      'text': {
        'value': null,
        '$config': null,
        'default': ''
      },
      'color': null,
      'toggleable': void 0,
      'pressed': false,
      'toggleGroup': null,
      'supportsPressedClick': false,
      'supportsUnpressing': false,
      'ripple': { 'radius': 75 },
      'twinForwardEvents': { 'item': 'action' },
      'twinSyncConfigs': { 'pressed': 'checked' },
      'localizableProperties': ['text'],
      'menu': {
        '$config': [
          'lazy',
          'nullify'
        ],
        'value': null
      },
      'menuDefaults': {
        'type': 'menu',
        'autoShow': false,
        'autoClose': true,
        'floating': true,
        'scrollAction': 'realign',
        'align': 't0-b0'
      },
      'href': null,
      'target': null,
      'tabIndex': null,
      'defaultBindProperty': null
    };
  }
  construct(...a) {
    super.construct(...a);
    this.ion({ 'hide': 'onInternalHide' });
  }
  updateElement(a, b) {
    let c, e, f;
    const g = this, {constructor: i} = g, j = super.updateElement(a, b), k = g.peekConfig('menu'), l = k ? k.isWidget ? k.role : ((e = (c = i.resolveType(k.type)) == null ? void 0 : c.configurable) == null ? void 0 : e.role) || ((f = i.configurable.menuDefaults) == null ? void 0 : f.type) || 'menu' : false;
    g.ariaHasPopup = l;
    return j;
  }
  compose() {
    const {
        color: a,
        href: b,
        icon: c,
        iconAlign: e,
        pressed: f,
        pressedIcon: g,
        target: i,
        text: j,
        toggleable: k,
        toggleGroup: l,
        menuIcon: n,
        behaviorType: o,
        tabIndex: p,
        pressedCls: q
      } = this, r = this.hasConfig('menu'), u = f && g ? g : c;
    return {
      'tag': b ? 'a' : 'button',
      'href': b,
      'target': i,
      'tabIndex': p,
      'type': o,
      'class': {
        ['b-icon-align-' + e]: c,
        [a]: Boolean(a),
        'b-pressed': f && k,
        'b-text': Boolean(j),
        'b-has-menu': r,
        ...f && q ? Object.fromEntries(q.split(' ').map(v => [
          v,
          1
        ])) : {}
      },
      [k && !r ? 'aria-pressed' : '']: f,
      'dataset': { 'group': l },
      'listeners': {
        'click': 'onInternalClick',
        'mousedown': 'onInternalMousedown'
      },
      'children': {
        'iconElement': (c || g) && {
          'aria-hidden': true,
          'tag': 'i',
          'class': {
            ...DomClassList.normalize(u, 'object'),
            'b-button-icon': 1,
            'b-icon': bIcon.test(u),
            'b-fa': bFa.test(u)
          }
        },
        'label': j && {
          'tag': 'label',
          'for': this.id,
          'text': j
        },
        'menuIconElement': r && n && {
          'tag': 'i',
          'class': {
            'b-icon': bIcon.test(n),
            'b-fa': bFa.test(n),
            'b-button-menu-icon': 1,
            [n]: 1
          }
        }
      }
    };
  }
  onInternalHide() {
    let a;
    (a = this._menu) == null ? void 0 : a.hide();
  }
  get childItems() {
    return this._menu && [this.menu];
  }
  onFocusOut(a) {
    let b;
    super.onFocusOut(a);
    (b = this.menu) == null ? void 0 : b.hide();
  }
  get focusElement() {
    return this.element;
  }
  changeText(a) {
    return a == null ? '' : a;
  }
  changeToggleable(a) {
    if (a === void 0) {
      a = Boolean(this.toggleGroup || this.config.menu);
    }
    return a;
  }
  changeMenu(a, b) {
    const c = this, {element: e} = c;
    if (a) {
      if (a.isWidget) {
        a.forElement = e;
        a.owner = c;
        a.constrainTo = c.rootElement;
      } else {
        if (typeof a === 'object' && !fullConfigKeys.some(f => f in a)) {
          a = { 'lazyItems': a };
        }
        a = Widget.reconfigure(b, a ? ObjectHelper.merge({
          'owner': c,
          'constrainTo': c.rootElement,
          'forElement': e
        }, c.menuDefaults, a) : null, c);
      }
      a.align.constrainPadding = 10;
      c.detachListeners(menuListenersName);
      a.ion({
        'name': menuListenersName,
        'beforeShow': 'onMenuBeforeShow',
        'hide': 'onMenuHide',
        'show': 'onMenuShow',
        'thisObj': this
      });
    } else {
      b == null ? void 0 : b.destroy();
    }
    return a;
  }
  onMenuBeforeShow({source: a}) {
    return this.trigger('beforeShowMenu', { 'menu': a });
  }
  onMenuShow() {
    this.ariaElement.setAttribute('aria-expanded', true);
  }
  onMenuHide() {
    this.ariaElement.setAttribute('aria-expanded', false);
    this.toggle(false);
  }
  updateMenu(a) {
    this.toggleable = Boolean(a);
  }
  updatePressed(a) {
    let b, c;
    const e = this;
    if (!e.toggleable || e.isConfiguring) {
      return;
    }
    const {menu: f} = e;
    if (a) {
      DomHelper.forEachSelector(e.rootElement, 'button[data-group=' + e.toggleGroup + ']', g => {
        if (g !== e.element) {
          Widget.getById(g.id).pressed = false;
        }
      });
    }
    if (f) {
      f.owner = e;
      f.forElement = e.element;
      if (!f.initialConfig.minWidth) {
        f.minWidth = e.width;
      }
      f.align.minHeight = (b = f._minHeight) != null ? b : 100;
      f.align.minWidth = (c = f._minWidth) != null ? c : 100;
      f[a ? 'show' : 'hide']();
    }
    e.trigger('toggle', {
      'pressed': a,
      'userAction': e._isUserAction
    });
  }
  configureOverflowTwin(a) {
    const b = this, c = super.configureOverflowTwin(a), {menu: e} = b;
    c.type = Widget.resolveType('menuitem');
    if (e) {
      c.menu = b.constructor.mergeConfigs(c.menu, {
        'type': 'menu',
        'items': e.items.map(f => f.ensureOverflowTwin())
      });
    } else if (b.toggleable) {
      c.checked = b.pressed;
    }
    if (!c.text) {
      c.text = b.text || b.tooltipText;
    }
    return c;
  }
  onInternalMousedown(a) {
    let b;
    if (((b = this._menu) == null ? void 0 : b.containsFocus) && this.pressed) {
      a.preventDefault();
    }
  }
  changeTooltip(a, b) {
    const c = this, e = (a == null ? void 0 : a.html) || a;
    if (!c.ariaLabel && !c.text && typeof e === 'string') {
      c.ariaLabel = c.optionalL(e, c);
    }
    return super.changeTooltip(a, b);
  }
  onInternalClick(a) {
    const b = this, c = { 'event': a };
    if (b.disabled) {
      return;
    }
    b._isUserAction = true;
    if (b.toggleable) {
      const e = b.toggleGroup && b.pressed && !b.supportsUnpressing;
      if (e && !b.supportsPressedClick) {
        return;
      }
      if (!e) {
        b.toggle(!b.pressed);
      }
      if (b.isDestroyed) {
        return;
      }
    }
    b.trigger('click', c);
    if (!b.isDestroyed) {
      b.trigger('action', c);
    }
    if (!b.href) {
      a.preventDefault();
      a.stopPropagation();
    }
    b._isUserAction = false;
  }
  toggle(a = !this.pressed) {
    if (this.trigger('beforeToggle', {
        'pressed': a,
        'userAction': this._isUserAction
      }) !== false) {
      this.pressed = a;
    }
  }
};
Button.initClass();
Button._$name = 'Button';
const asElementRefs = { 'refs': 'element' };
const onCreateTwin = a => a.element.style.margin = '';
const isToolbar = a => a.isToolbar;
const itemScoreFn = a => a[0] + (a[1].minifiable ? 0 : 9000000000);
const twinOverrides = {
  'hidden': false,
  'width': ''
};
const twinOverridesHorz = {
  ...twinOverrides,
  'flex': ''
};
const widthAffecting = {
  'flex': 1,
  'hidden': 1,
  'maxWidth': 1,
  'minWidth': 1,
  'width': 1,
  'disabled': 1
};
const _axisProps = [
  {
    'box': 'hbox',
    'clientSizeProp': 'clientWidth',
    'edgeProp': 'right',
    'flexDir': 'row',
    'horizontal': true,
    'max': 'maxX',
    'overflow': 'overflowX',
    'pos': 'x',
    'scrollSize': 'scrollWidth',
    'sizeProp': 'width'
  },
  {
    'box': 'vbox',
    'clientSizeProp': 'clientHeight',
    'edgeProp': 'bottom',
    'flexDir': 'column',
    'horizontal': false,
    'max': 'maxY',
    'overflow': 'overflowY',
    'pos': 'y',
    'scrollSize': 'scrollHeight',
    'sizeProp': 'height'
  }
];
const defaultRepeat = {
  'delay': 0,
  'startRate': 40,
  'endRate': 200,
  'accelerateDuration': 500
};
const nonSyncedConfigs = {
  'menu': 1,
  'pressed': 1
};
const _Toolbar = class _Toolbar extends Container.mixin(Toolable_default) {
  static prototypeProperties = {
    'flexRowCls': 'b-hbox',
    'flexColCls': 'b-vbox'
  };
  static configurable = {
    'defaultType': 'button',
    'dock': 'top',
    'layout': { 'type': 'box' },
    'overflow': {
      '$config': [
        'lazy',
        'nullify'
      ],
      'value': { 'type': 'menu' }
    },
    'toolDefaults': {
      'overflowMenuButton': {
        'type': 'button',
        'hidden': true,
        'icon': 'b-icon-menu',
        'menuIcon': null,
        'ariaLabel': 'Show overflowing items',
        'defaultCls': { 'b-overflow-button': 1 }
      },
      'overflowScrollEnd': {
        'handler': 'up.onEndScrollClick',
        'hidden': true,
        'defaultCls': {
          'b-icon-angle-right': 1,
          'b-overflow-button': 1,
          'b-icon': 1
        }
      },
      'overflowScrollStart': {
        'align': 'start',
        'handler': 'up.onStartScrollClick',
        'hidden': true,
        'defaultCls': {
          'b-icon-angle-left': 1,
          'b-overflow-button': 1,
          'b-icon': 1
        }
      }
    },
    'widgetCls': null,
    'ignoreParentReadOnly': true
  };
  static delayable = {
    'syncOverflowVisibility': {
      'type': 'raf',
      'cancelOutstanding': true
    }
  };
  static type = 'toolbar';
  static $name = 'Toolbar';
  static getEvictionList(a) {
    const b = a.filter(c => c.overflowable !== 'none');
    b.forEach((c, e) => b[e] = [
      e,
      c
    ]);
    b.sort((c, e) => itemScoreFn(e) - itemScoreFn(c));
    return b;
  }
  compose() {
    const a = this, {
        axisProps: b,
        dock: c
      } = a, e = a.getEndTools(asElementRefs), f = a.getStartTools(asElementRefs);
    return {
      'class': {
        ['b-dock-' + c]: 1,
        ['b-' + c + '-toolbar']: 1,
        ['b-' + b.box]: 1
      },
      'children': {
        ...f,
        'toolbarContent': {
          'class': {
            'b-box-center': 1,
            'b-toolbar-content': 1
          }
        },
        ...e
      }
    };
  }
  get axisProps() {
    return _axisProps[this.layout.horizontal ? 0 : 1];
  }
  get contentElement() {
    return this.toolbarContent;
  }
  get overflowMenuButton() {
    let a;
    return (a = this.tools) == null ? void 0 : a.overflowMenuButton;
  }
  get overflowType() {
    const {overflow: a} = this;
    return typeof a === 'string' ? a : a == null ? void 0 : a.type;
  }
  onChildAdd(a) {
    let b;
    super.onChildAdd(a);
    this.processAddedLeafItem(a);
    (b = a.syncRotationToDock) == null ? void 0 : b.call(a, this.dock);
  }
  onChildRemove(a) {
    super.onChildRemove(a);
    this.syncOverflowVisibility();
  }
  processAddedLeafItem(a) {
    FunctionHelper.after(a, 'onConfigChange', this.onLeafItemConfigChange, a);
    if (a.isContainer) {
      a.eachWidget(b => this.processAddedLeafItem(b));
    }
  }
  onInternalPaint({firstPaint: a}) {
    let b;
    (b = super.onInternalPaint) == null ? void 0 : b.call(this, ...arguments);
    if (a) {
      this.getConfig('overflow');
    }
  }
  updateDock(a) {
    let b;
    const c = this, {layout: e} = c, {vertical: f} = e;
    e.vertical = canonicalDock(a)[1];
    if (!c.initialItems) {
      if (f !== e.vertical) {
        c.updateOverflow(c.overflow);
      }
      for (const g of c.childItems) {
        (b = g.syncRotationToDock) == null ? void 0 : b.call(g, a);
      }
    }
  }
  updateOverflow(a, b) {
    let c, e;
    const f = this, {
        axisProps: g,
        contentElement: i,
        overflowType: j
      } = f, {flexDir: k} = g, l = (c = f.overflowMenuButton) == null ? void 0 : c._menu, n = {};
    if (l) {
      if (a) {
        l == null ? void 0 : l.removeAll();
      } else {
        l.eachWidget(o => {
          o._overflowTwinOrigin.overflowTwin = null;
        });
      }
    }
    if (b === 'menu') {
      n.overflowMenuButton = null;
    } else if (b === 'scroll') {
      n.overflowScrollStart = n.overflowScrollEnd = null;
    }
    if (j === 'menu') {
      (e = f.scrollable) == null ? void 0 : e.destroy();
      i.style.overflow = i.style.overflowX = i.style.overflowY = '';
      n.overflowMenuButton = { 'cls': { ['b-' + k + '-menu']: 1 } };
    } else if (j === 'scroll') {
      const o = typeof a === 'object' && (a == null ? void 0 : a.repeat) || defaultRepeat;
      f.scrollable = {
        [g.overflow]: 'hidden-scroll',
        'element': i,
        'internalListeners': {
          'scroll': 'onContentScroll',
          'thisObj': f
        }
      };
      n.overflowScrollStart = {
        'repeat': o,
        'invertRotate': true,
        'cls': { ['b-' + k + '-start-scroller']: 1 }
      };
      n.overflowScrollEnd = {
        'repeat': o,
        'invertRotate': true,
        'cls': { ['b-' + k + '-end-scroller']: 1 }
      };
    }
    f.tools = n;
    if (j) {
      i.classList.add('b-overflow');
      f.monitorResize = true;
      f.syncOverflowVisibility();
    } else {
      i.classList.remove('b-overflow');
      f.monitorResize = false;
    }
  }
  onContentScroll() {
    this.syncScrollerState();
  }
  onStartScrollClick() {
    this.scrollable[this.axisProps.pos] -= 2;
  }
  onEndScrollClick() {
    this.scrollable[this.axisProps.pos] += 2;
  }
  onInternalResize() {
    super.onInternalResize(...arguments);
    if (this.isPainted) {
      this.syncOverflowVisibility();
    }
  }
  syncOverflowVisibility() {
    const a = this, {
        overflowType: b,
        contentElement: c,
        isVisible: e
      } = a, {
        clientSizeProp: f,
        edgeProp: g,
        sizeProp: i
      } = a.axisProps, {
        overflowMenuButton: j,
        overflowScrollStart: k,
        overflowScrollEnd: l
      } = a.tools, n = a.rtl && a.layout.horizontal, o = b === 'menu', p = () => Math.ceil(c[f] + (!k || k.hidden ? 0 : k.rectangle('outer')[i]) + (!l || l.hidden ? 0 : l.rectangle('outer')[i])), q = () => {
        if (H.length === 0) {
          return 0;
        }
        const I = H[H.length - 1].rectangle(c);
        return Math.floor(n ? c[f] - I.left : I[g]);
      };
    let r, u, v, w, z, A, B, C, E, F, G, H;
    if (!e || !b || a.items.length === 0) {
      return;
    }
    a.inSyncOverflowVisibility = true;
    j == null ? void 0 : j.hide();
    a.eachWidget((I, J) => {
      B = I.minifiable;
      F = I.overflowable;
      F = B ? F || B : F;
      if (I.floating) {
        J.down = false;
      } else {
        J.down = !F;
        if (I.innerItem) {
          if (I._toolbarOverflow) {
            I.hidden = I._toolbarOverflow = false;
          }
          if (I._toolbarMinified) {
            I.minified = I._toolbarMinified = false;
          }
          if (I.isVisible) {
            B && (C || (C = [])).push(I);
            F && (H || (H = [])).push(I);
          }
        }
      }
    });
    if (H) {
      r = p();
      u = q();
    }
    if (H && u > r) {
      if (o) {
        while (u > r && (z = C == null ? void 0 : C.pop())) {
          A = u;
          z._toolbarMinified = true;
          z.minified = true;
          u = q();
          A -= u;
          (E || (E = [])).push([
            z,
            A
          ]);
        }
        if (u > r) {
          j.show();
          r = p();
          w = _Toolbar.getEvictionList(H);
          for (v of w) {
            if (u > r) {
              z = v[1];
              z._toolbarOverflowWidth = z.width;
              z._toolbarOverflow = true;
              z.hidden = true;
              H.splice(H.indexOf(z), 1);
              (G || (G = [])).push(v);
              u = q();
            }
          }
          if (G) {
            G.sort((I, J) => I[0] - J[0]).forEach((I, J) => G[J] = I[1]);
            while (E == null ? void 0 : E.length) {
              [z, A] = E.pop();
              if (u + A <= r && !z._toolbarOverflow) {
                u += A;
                z.minified = z._toolbarMinified = false;
              }
            }
            a.syncOverflowMenuButton(G);
          } else {
            j.hide();
          }
        }
      } else {
        l.show();
        k.show();
        a.syncScrollerState();
      }
    } else if (!o) {
      l == null ? void 0 : l.hide();
      k == null ? void 0 : k.hide();
    }
    a.inSyncOverflowVisibility = false;
  }
  syncOverflowMenuButton(a) {
    let b;
    const c = this, {
        axisProps: e,
        overflowMenuButton: f
      } = c, g = {
        'cls': 'b-toolbar-overflow-menu',
        'minWidth': 280,
        'items': [],
        'align': {
          'align': e.horizontal ? 't100-b100' : 'r100-l100',
          'axisLock': 'flexible'
        }
      };
    for (const i of a) {
      c.overflowItemFilter(i) && i.addToOverflowMenu(g, c.horizontal ? twinOverridesHorz : twinOverrides, onCreateTwin);
    }
    g.items.forEach(j => {
      let k = j._overflowTwinOrigin, l = k.parent.indexOfChild(k);
      if (k.parent !== c) {
        l = c.indexOfChild(k.parent) + l / 100000;
      }
      j.weight = l;
    });
    if ((b = f._menu) == null ? void 0 : b.isMenu) {
      const j = f.menu, {
          toAdd: k,
          toRemove: l
        } = ArrayHelper.delta(g.items, j.items, true);
      j.remove(l);
      j.add(k);
    } else {
      f.menu = g;
    }
  }
  syncScrollerState() {
    const a = this, {
        axisProps: b,
        scrollable: c
      } = a, {
        overflowScrollStart: e,
        overflowScrollEnd: f
      } = a.tools, g = c[b.pos], i = c[b.max];
    e.disabled = !g;
    f.disabled = Math.abs(Math.ceil(g)) >= Math.abs(i);
  }
  overflowItemFilter(a) {
    const {filter: b} = this.overflow;
    return Boolean(b ? b.call(this, a) : DomHelper.getChildElementCount(a.element));
  }
  onLeafItemConfigChange(a, {
    name: b,
    value: c
  }) {
    const e = this, f = e.up(isToolbar), g = f.hasConfig('overflow'), {overflowTwin: i} = e;
    if (!g || (f == null ? void 0 : f.inSyncOverflowVisibility) || b === 'hidden' && c === e._toolbarOverflow) {
      return;
    }
    if (!widthAffecting[b]) {
      if (e._width != null && !e.monitorResize || e._flex && !String(e._flex).endsWith('auto')) {
        return;
      }
    }
    if (i && !nonSyncedConfigs[b]) {
      i[b] = c;
    }
    if ((f == null ? void 0 : f.isPainted) && e.ref !== 'overflowMenuButton') {
      if (!((e.isTextField || e.isSlider || e.isCheckbox) && b === 'value' && (e.containsFocus || (i == null ? void 0 : i.containsFocus)))) {
        f.syncOverflowVisibility();
      }
    }
  }
  createWidget(a) {
    if (a === '->') {
      a = {
        'type': 'widget',
        'cls': 'b-toolbar-fill'
      };
    } else if (a === '|') {
      a = {
        'type': 'widget',
        'cls': 'b-toolbar-separator'
      };
    } else if (typeof a === 'string') {
      a = {
        'type': 'widget',
        'cls': 'b-toolbar-text',
        'html': a
      };
    }
    const b = super.createWidget(a);
    if (this.widgetCls) {
      b.element.classList.add(this.widgetCls);
    }
    return b;
  }
};
const Toolbar = _Toolbar;
Toolbar.initClass();
Toolbar._$name = 'Toolbar';
const isTabbable = a => !a.classList.contains('b-focus-trap') && !a.classList.contains('b-hidden') && DomHelper.getFocusability(a).focusable ? DomHelper.NodeFilter.FILTER_ACCEPT : DomHelper.NodeFilter.FILTER_SKIP;
const emptyArray8 = [];
const emptyObject10 = {};
const emptySplit = [
  emptyArray8,
  emptyArray8
];
const finishBodyWrap = (a, b, c) => {
  const {vertical: e} = a;
  delete a.vertical;
  return {
    ...a,
    'class': {
      ...b,
      ['b-' + (e ? 'v' : 'h') + 'box']: 1,
      'b-box-center': 1,
      'b-panel-bar-wrap': !c
    }
  };
};
const wrapBody = (a, b, c = false) => {
  const e = {
    'vertical': c,
    'children': a ? [a] : []
  };
  if (b) {
    e.tag = b;
  }
  return e;
};
const setCls = (a, b) => {
  if (a == null ? void 0 : a.classList) {
    a == null ? void 0 : a.classList.add(b);
  } else if (a == null ? void 0 : a.class) {
    if (typeof a.class === 'string') {
      a.class = { [a.class]: 1 };
    }
    a.class[b] = 1;
  }
};
const barConfigs = {
  'dock': 1,
  'hidden': 1,
  'weight': 1
};
const dockDirection = {
  'top': [
    true,
    true
  ],
  'bottom': [
    true,
    false
  ],
  'left': [
    false,
    true
  ],
  'right': [
    false,
    false
  ]
};
const headerDock = {
  'header': 1,
  'pre-header': 1
};
const _Panel = class _Panel extends Container.mixin(State_default, Toolable_default) {
  static type = 'panel';
  static $name = 'Panel';
  static get configurable() {
    return {
      'localizableProperties': ['title'],
      'collapsed': {
        'value': null,
        '$config': null,
        'default': false
      },
      'collapsible': {
        'value': null,
        '$config': 'nullify'
      },
      'bodyCls': {
        '$config': { 'merge': 'classList' },
        'value': null
      },
      'bodyTag': null,
      'bodyWrapTag': null,
      'trapFocus': null,
      'title': null,
      'header': null,
      'stateful': ['collapsed'],
      'stripDefaults': {
        '*': {
          'type': 'toolbar',
          'dock': 'top'
        },
        'bbar': {
          'dock': 'bottom',
          'weight': -1000
        },
        'tbar': { 'weight': -1000 }
      },
      'strips': {
        'value': null,
        '$config': 'nullify'
      },
      'toolDefaults': {
        'close': { 'weight': -1000 },
        'collapse': { 'weight': -990 }
      },
      'footer': null,
      'revealed': null,
      'tbar': null,
      'bbar': null,
      'role': 'region'
    };
  }
  updateElement(a, b) {
    const c = super.updateElement(a, b);
    if (this.titleElement) {
      DomHelper.setAttributes(this.ariaElement, { 'aria-describedby': this.titleElement.id });
    }
    return c;
  }
  get bodyWrapRootElement() {
    return this[this._bodyWrapRootName];
  }
  changeFooter(a) {
    if (a) {
      if (a.dock === 'start') {
        a.dock = 'left';
      } else if (a.dock === 'end') {
        a.dock = 'right';
      }
    }
    return a;
  }
  compose() {
    const a = this, {
        collapsible: b,
        focusable: c,
        hasItems: e,
        revealed: f,
        tools: g
      } = a, i = a.composeHeader(), j = a.getPositionableItems('after', true), k = a.getPositionableItems('before', true), l = (i == null ? void 0 : i.class['b-dock-left']) || (i == null ? void 0 : i.class['b-dock-right']);
    let n = a.composeBody(), o = 'bodyWrapElement';
    if (b) {
      [o, n] = b.wrapCollapser(o, n);
    }
    a._bodyWrapRootName = o;
    return {
      'tabIndex': e && c !== false || c ? 0 : null,
      'class': {
        ['b-panel-collapsible-' + (b == null ? void 0 : b.type)]: b,
        ['b-panel-collapse-' + (b == null ? void 0 : b.collapseDir)]: b,
        ['b-' + (l ? 'h' : 'v') + 'box']: 1,
        'b-panel-collapsible': b,
        'b-panel-has-header': i,
        'b-panel-has-tools': g ? 1 : 0,
        'b-panel-overlay-revealed': f
      },
      'children': {
        'topFocusTrap': {
          'aria-hidden': true,
          'tabIndex': 0,
          'class': {
            'b-focus-trap': 1,
            'b-top-focus-trap': 1
          }
        },
        ...k,
        'headerElement': i,
        [o]: n,
        ...j,
        'bottomFocusTrap': {
          'aria-hidden': true,
          'tabIndex': 0,
          'class': {
            'b-focus-trap': 1,
            'b-end-focus-trap': 1
          }
        }
      }
    };
  }
  composeBody() {
    let a, b;
    const c = this, {
        bodyCls: e,
        bodyConfig: f,
        bodyWrapTag: g,
        footer: j,
        uiClassList: k
      } = c, l = ObjectHelper.values(c.strips, (A, B) => !dockDirection[B == null ? void 0 : B.dock]).sort(c.byWeightSortFn), n = {
        'top': null,
        'right': null,
        'bottom': null,
        'left': null
      };
    let o, p, q, r, u, v, w;
    if (j) {
      q = j.dock || 'bottom';
      l.unshift({
        'dock': q,
        'element': {
          'tag': 'footer',
          'reference': 'footerElement',
          'html': typeof j === 'string' ? j : j.html,
          'class': {
            ...k,
            ['b-dock-' + q]: 1,
            ['' + (j.cls || '')]: 1
          }
        }
      });
    }
    if (e) {
      if (!f[u = 'className']) {
        u = 'class';
      }
      f[u] = new DomClassList(f[u]).assign(e);
    }
    for (r = l.length; r-- > 0;) {
      o = l[r];
      [v, p] = dockDirection[o.dock];
      if (!w) {
        w = wrapBody(f, g, v);
      } else if (w.vertical !== v) {
        w = wrapBody(finishBodyWrap(w, k), '', v);
      }
      w.children[p ? 'unshift' : 'push'](o.element);
      n[o.dock] = o;
    }
    setCls(n.top, 'b-innermost');
    setCls(n.right, 'b-innermost');
    setCls(n.bottom, 'b-innermost');
    setCls(n.left, 'b-innermost');
    const z = finishBodyWrap(w || wrapBody(f, g), k, true);
    z.class[((a = c.layout) == null ? void 0 : a.containerCls) + '-panel'] = Boolean((b = c.layout) == null ? void 0 : b.containerCls);
    z.class['b-panel-body-wrap'] = 1;
    z.class['b-' + c.$$name.toLowerCase() + '-body-wrap'] = 1;
    return z;
  }
  get hasHeader() {
    if (this.header === false) {
      return false;
    }
    const {
        header: a,
        title: b,
        tools: c,
        parent: e
      } = this, f = this.maximizable || Object.values(c || {}).some(g => !g.hidden);
    return a || !(e == null ? void 0 : e.suppressChildHeaders) && (b || f);
  }
  get rootUiClass() {
    return _Panel;
  }
  composeHeader(a) {
    let b;
    const c = this;
    if (!c.hasHeader && !a) {
      return;
    }
    const e = c.header || {}, f = e.dock || 'top', [g, j] = c.splitHeaderItems({
        'as': 'element',
        'dock': f
      }), k = c.$meta.hierarchy, l = c.composeTitle(e), n = new DomClassList({
        ['b-dock-' + f]: 1,
        ...c.uiClassList
      }, e.cls);
    let o, p;
    for (o = k.indexOf(_Panel); o < k.length; ++o) {
      p = k[o].$$name;
      if (p !== 'Grid') {
        n['b-' + p.toLowerCase() + '-header'] = 1;
      }
    }
    const q = {
      'tag': 'header',
      'class': n,
      'children': [
        ...g,
        l,
        ...j
      ]
    };
    return ((b = c.collapsible) == null ? void 0 : b.composeHeader(q)) || q;
  }
  composeTitle(a) {
    let b;
    const c = typeof a === 'string' ? a : this.title || a.title, e = {
        'reference': 'titleElement',
        'id': this.id + '-panel-title',
        'html': c != null ? c : '\xA0',
        'class': {
          ['b-align-' + (a.titleAlign || 'start')]: 1,
          'b-header-title': 1,
          ...this.uiClassList
        }
      };
    if (ObjectHelper.isObject(c)) {
      delete e.html;
      ObjectHelper.merge(e, c);
    }
    return ((b = this.collapsible) == null ? void 0 : b.composeTitle(e)) || e;
  }
  updateTitle() {
  }
  afterRecompose() {
    let a;
    super.afterRecompose();
    const b = this, {headerElement: c} = b;
    (a = b._headerClickDetacher) == null ? void 0 : a.call(b);
    b._headerClickDetacher = c && EventHelper.on({
      'element': c,
      'click': e => {
        let f;
        return (f = b.trigger) == null ? void 0 : f.call(b, 'panelHeaderClick', { 'event': e });
      }
    });
  }
  onPanelHeaderClick(a) {
    let b;
    (b = this.collapsible) == null ? void 0 : b.onPanelHeaderClick(a);
  }
  onInternalPaint() {
    let a;
    super.onInternalPaint(...arguments);
    (a = this.collapsible) == null ? void 0 : a.onPanelPaint(this);
  }
  splitHeaderItems({
    as: a,
    dock: b,
    alt: c
  } = emptyObject10) {
    let e, f, g, j;
    const k = this, l = a === 'element', {collapsed: n} = k, o = k.getEndTools({ 'alt': c }), p = k.getStartTools({ 'alt': c }), q = ObjectHelper.values(k.strips, (z, A) => !headerDock[A == null ? void 0 : A.dock] && A.isCollapsified({
        'collapsed': n,
        'alt': c
      }));
    let r = emptySplit, u, v, w;
    if (q.length + o.length + p.length) {
      r = [
        v = [
          ...p,
          ...q.filter(z => z.dock === 'pre-header').sort(k.byWeightSortFn)
        ],
        u = [
          ...q.filter(z => z.dock === 'header').sort(k.byWeightReverseSortFn),
          ...o
        ]
      ];
      for (w = 0; w < v.length; ++w) {
        b && ((f = (e = v[w]).syncRotationToDock) == null ? void 0 : f.call(e, b));
        if (l) {
          v[w] = v[w].element;
        }
      }
      for (w = 0; w < u.length; ++w) {
        b && ((j = (g = u[w]).syncRotationToDock) == null ? void 0 : j.call(g, b));
        if (l) {
          u[w] = u[w].element;
        }
      }
    }
    return r;
  }
  set bodyConfig(a) {
    this._bodyConfig = a;
  }
  get bodyConfig() {
    const a = this, {bodyTag: b} = a, c = ObjectHelper.merge({
        'reference': 'bodyElement',
        'className': {
          ...a.getStaticWidgetClasses(_Panel, '-content'),
          'b-box-center': 1,
          'b-text-content': a.textContent && a.hasNoChildren
        }
      }, a._bodyConfig);
    if (b) {
      c.tag = b;
    }
    return c;
  }
  changeBodyCls(a) {
    return DomClassList.from(a);
  }
  changeTbar(a) {
    this.getConfig('strips');
    this.strips = { 'tbar': a };
    return this.strips.tbar;
  }
  changeBbar(a) {
    this.getConfig('strips');
    this.strips = { 'bbar': a };
    return this.strips.bbar;
  }
  get childItems() {
    let a;
    const b = this, c = ObjectHelper.values(b.strips, (j, l) => !dockDirection[l == null ? void 0 : l.dock]).sort(b.byWeightSortFn), [e, f] = b.splitHeaderItems(), [g, i] = ((a = b.collapsible) == null ? void 0 : a.splitHeaderItems()) || emptySplit;
    return [
      ...e,
      ...g,
      ...f,
      ...i,
      ...c.filter(j => dockDirection[j.dock][1]),
      ...b.items,
      ...c.filter(j => !dockDirection[j.dock][1]).reverse()
    ];
  }
  get childItemsOrderedByRelevance() {
    let a;
    const b = this, c = ObjectHelper.values(b.strips, (j, l) => !dockDirection[l == null ? void 0 : l.dock]).sort(b.byWeightSortFn), [e, f] = b.splitHeaderItems(), [g, i] = ((a = b.collapsible) == null ? void 0 : a.splitHeaderItems()) || emptySplit;
    return [
      ...b.items,
      ...c.filter(j => !dockDirection[j.dock][1]).reverse(),
      ...e,
      ...g,
      ...f,
      ...i,
      ...c.filter(j => dockDirection[j.dock][1])
    ];
  }
  changeStrips(a, b) {
    const c = this, e = c.$strips || (c.$strips = new DynamicObject({
        'configName': 'strips',
        'factory': Widget,
        'inferType': false,
        'owner': c,
        'created'(f) {
          let g;
          const {dock: i} = f;
          if (!headerDock[i] && !dockDirection[i]) {
            throw new Error('Invalid dock value "' + i + '"; must be: top, left, right, bottom, header, or pre-header');
          }
          FunctionHelper.after(f, 'onConfigChange', (j, {name: k}) => {
            if (barConfigs[k]) {
              c.onConfigChange({
                'name': 'strips',
                'value': e.target
              });
            }
          });
          f.innerItem = false;
          c.onChildAdd(f);
          f.parent = c;
          (g = f.layout) == null ? void 0 : g.renderChildren();
          if (f.hasItems) {
            c.hasItems = true;
          }
        },
        'setup'(f, g) {
          f = ObjectHelper.merge(ObjectHelper.clone(c.stripDefaults['*']), c.stripDefaults[g], f);
          f.parent = c;
          f.ref = g;
          return f;
        },
        'transform'(f) {
          if (Array.isArray(f)) {
            f = { 'items': f };
          }
          return f || null;
        }
      }));
    e.update(a);
    if (!b) {
      return e.target;
    }
  }
  get collapsing() {
    let a;
    return (a = this.collapsible) == null ? void 0 : a.collapsing;
  }
  get collapsingExpanding() {
    let a;
    return (a = this.collapsible) == null ? void 0 : a.collapsingExpanding;
  }
  get expanding() {
    let a;
    return (a = this.collapsible) == null ? void 0 : a.expanding;
  }
  changeCollapsed(a) {
    const b = this, {collapsible: c} = b;
    b.recompose.flush();
    a = Boolean(a);
    if (!c || b.changingCollapse || !b.isPainted) {
      return a;
    }
    c == null ? void 0 : c.collapse({
      'animation': null,
      'collapsed': a
    });
  }
  changeCollapsible(a, b) {
    const c = this;
    c.getConfig('tools');
    if (a) {
      if (a === true) {
        a = {};
      } else if (typeof a === 'string') {
        a = { [dockDirection[a] ? 'direction' : 'type']: a };
      }
    }
    return PanelCollapser.reconfigure(b, a, {
      'owner': c,
      'defaults': { 'panel': c },
      'cleanup'() {
        if (c.collapsed) {
          b.collapse({
            'animation': null,
            'collapsed': false
          });
          c._collapsed = 1;
        }
      }
    });
  }
  updateCollapsible(a) {
    const b = this, c = a == null ? void 0 : a.toolsConfig;
    b.tools = {
      'collapse': (c == null ? void 0 : c.collapse) || null,
      'recollapse': (c == null ? void 0 : c.recollapse) || null
    };
    if (a && b.isPainted && b.collapsed) {
      b._collapsed = 1;
    }
    if (b.collapsed === 1) {
      b.collapsed = true;
    }
  }
  _collapse(a, b) {
    let c;
    if (b !== true && b !== void 0) {
      if (b === false) {
        a.collapsed = !a.collapsed;
      } else if (typeof b === 'number') {
        a.animation = { 'duration': b };
      } else if (b === null) {
        a.animation = b;
      } else if (b === true) {
      } else if ('animation' in b) {
        ObjectHelper.merge(a, b);
      } else {
        a.animation = b;
      }
    }
    return (c = this.collapsible) == null ? void 0 : c.collapse(a);
  }
  collapse(a) {
    return this.collapsePanel(a);
  }
  expand(a) {
    return this.expandPanel(a);
  }
  collapsePanel(a) {
    return this._collapse({ 'collapsed': true }, a);
  }
  expandPanel(a) {
    return this._collapse({ 'collapsed': false }, a);
  }
  toggleCollapsed(a) {
    return this.collapsed ? this.expand(a) : this.collapse(a);
  }
  get expandedHeaderDock() {
    let a, b, c;
    return (c = (b = this._expandedHeaderDock) != null ? b : (a = this.initialConfig.header) == null ? void 0 : a.dock) != null ? c : 'top';
  }
  set expandedHeaderDock(a) {
    this._expandedHeaderDock = a == null ? void 0 : a.toLowerCase();
  }
  updateHeader(a) {
    if (!this.changingCollapse) {
      this.expandedHeaderDock = a == null ? void 0 : a.dock;
    }
  }
  updateTrapFocus(a) {
    let b;
    const c = this, {element: e} = c, {classList: f} = e, g = a === 'before', i = a === 'after';
    f.toggle('b-focus-trapped', a && !g && !i);
    f.toggle('b-focus-trapped-before', g);
    f.toggle('b-focus-trapped-after', i);
    c.focusTrapListener = (b = c.focusTrapListener) == null ? void 0 : b.call(c);
    if (a) {
      c.focusTrapListener = EventHelper.on({
        'element': e,
        'focusin': 'onFocusTrapped',
        'delegate': '.b-focus-trap',
        'thisObj': c
      });
      if (!c.treeWalker) {
        c.treeWalker = c.setupTreeWalker(e, DomHelper.NodeFilter.SHOW_ELEMENT, isTabbable);
      }
    }
  }
  setupTreeWalker(a, b, c) {
    return document.createTreeWalker(a, b, c);
  }
  onFocusTrapped(a) {
    const b = this, c = b.treeWalker, f = a.target === b.bottomFocusTrap, g = a.target === b.topFocusTrap;
    if ((g || f) && b.trigger('focusTrap', {
        'bottom': f,
        'top': g
      }) !== false) {
      if (f) {
        c.currentNode = b.topFocusTrap;
        c.nextNode();
      } else {
        c.currentNode = b.bottomFocusTrap;
        c.previousNode();
      }
      b.requestAnimationFrame(() => c.currentNode.focus());
    }
  }
  get focusElement() {
    return this.hasItems && (super.focusElement || this.element);
  }
  get contentElement() {
    return this.element && this.bodyElement;
  }
  getPositionableLocation(a) {
    let b, c;
    if (a === 'before') {
      c = this.headerElement || this.bodyWrapRootElement;
    } else if (a === 'after') {
      c = this.bottomFocusTrap;
    } else {
      b = this.contentElement;
    }
    return [
      b,
      c
    ];
  }
  get widgetClassList() {
    let a;
    const b = this, c = super.widgetClassList;
    if (b.hasHeader) {
      c.push('b-panel-has-header', 'b-header-dock-' + (((a = b.header) == null ? void 0 : a.dock) || 'top'));
    }
    if (b.tbar) {
      c.push('b-panel-has-top-toolbar');
    }
    if (b.bbar) {
      c.push('b-panel-has-bottom-toolbar');
    }
    return c;
  }
};
const Panel = _Panel;
Panel.initClass();
Panel._$name = 'Panel';
const _Popup = class _Popup extends Panel {
  static type = 'popup';
  static $name = 'Popup';
  static get configurable() {
    return {
      'autoShow': true,
      'autoClose': true,
      'showOnClick': false,
      'forElement': null,
      'monitorResize': true,
      'floating': true,
      'hidden': true,
      'axisLock': true,
      'hideAnimation': {
        'opacity': {
          'from': 1,
          'to': 0,
          'duration': '.3s',
          'delay': '0s'
        }
      },
      'showAnimation': {
        'opacity': {
          'from': 0,
          'to': 1,
          'duration': '.4s',
          'delay': '0s'
        }
      },
      'stripDefaults': { 'bbar': { 'layout': { 'justify': 'flex-end' } } },
      'testConfig': {
        'hideAnimation': null,
        'showAnimation': null
      },
      'closeAction': 'hide',
      'trapFocus': true,
      'focusOnToFront': true,
      'closable': null,
      'maximizable': null,
      'modal': null,
      'maximized': null,
      'tools': {
        'close': {
          'cls': 'b-popup-close',
          'handler': 'close',
          'weight': -1000,
          'ariaLabel': 'L{Popup.close}',
          'hidden': true
        },
        'maximize': {
          'cls': 'b-popup-expand',
          'handler': 'toggleMaximized',
          'weight': -999,
          'hidden': true
        }
      },
      'highlightReturnedFocus': true,
      'role': 'dialog'
    };
  }
  finalizeInit() {
    const a = this, {forElement: b} = a;
    a.anchoredTo = b;
    a.initialAnchor = a.anchor;
    if (b && a.showOnClick !== false) {
      if (!a.initialConfig.autoShow) {
        a.autoShow = false;
      }
      EventHelper.on({
        'element': b,
        [a.showOnClick === 2 ? 'contextmenu' : 'click']: 'onElementUserAction',
        'thisObj': a,
        'capture': true
      });
    }
    super.finalizeInit();
    if (a.autoShow && (!b || DomHelper.isVisible(b))) {
      if (a.autoShow === true) {
        a.show();
      } else {
        a.setTimeout(() => a.show(), a.autoShow);
      }
    }
  }
  onInternalPaint({firstPaint: a}) {
    let b;
    (b = super.onInternalPaint) == null ? void 0 : b.call(this, ...arguments);
    const c = this;
    if (a && c.headerElement) {
      EventHelper.on({
        'element': c.headerElement,
        'dblclick': c.onHeaderDblClick,
        'thisObj': c
      });
    }
  }
  doDestroy() {
    this.syncModalMask();
    super.doDestroy();
  }
  compose() {
    const {
      hasNoChildren: a,
      textContent: b
    } = this;
    return { 'class': { 'b-text-popup': Boolean(b && a) } };
  }
  close() {
    let a, b;
    const c = this;
    if (!c._hidden && c.trigger('beforeClose') !== false || c._hidden && c.closeAction === 'destroy') {
      c.modal && c.closeAction === 'destroy' && c.revertFocus();
      c.unmask();
      if (c.containsFocus && c.highlightReturnedFocus) {
        c.setTimeout(() => c.element.classList.add('b-using-keyboard'), 0);
      }
      const e = c.items.length && ((a = c.items.find(f => f.errorTip)) == null ? void 0 : a.errorTip);
      if (e) {
        (b = e.pointerOverOutDetacher) == null ? void 0 : b.call(e);
        e.hide();
      }
      return c[c.closeAction]();
    }
  }
  toggleMaximized() {
    this.maximized = !this.maximized;
  }
  updateMaximized(a) {
    DomHelper.toggleClasses(this.element, ['b-maximized'], a);
  }
  onInternalKeyDown(a) {
    const b = this;
    if (a.key === 'Escape') {
      a.stopImmediatePropagation();
      if (b.floating || b.positioned) {
        b.close(true);
      } else if (b.collapsible) {
        if (b.revealed) {
          b.collapsible.toggleReveal();
        } else {
          b.collapse();
        }
      }
    }
  }
  onDocumentMouseDown({event: a}) {
    const b = this, {owner: c} = b, {target: e} = a;
    if (!b.isVisible) {
      return;
    }
    if (a.type !== 'touchend' && (c == null ? void 0 : c.isButton) && c._menu === b && c.element.contains(e)) {
      a.preventDefault();
      return false;
    }
    if (b.modal && e === _Popup.modalMask) {
      a.preventDefault();
      if (b.modal.closeOnMaskTap) {
        b.close();
      } else if (!b.containsFocus) {
        b.focus();
      }
    } else if (!b.owns(e, true) && b.autoClose) {
      if (b.containsFocus) {
        const f = DomHelper.getActiveElement(b.element);
        EventHelper.on({
          'element': document,
          'once': true,
          'expires': 100,
          [a.type]: g => {
            if (g.defaultPrevented && DomHelper.getActiveElement(b.element) === f) {
              b.close();
            }
          }
        });
      } else {
        b.close();
      }
    }
  }
  get isTopModal() {
    return DomHelper.isVisible(_Popup.modalMask) && this.element.previousElementSibling === _Popup.modalMask;
  }
  onFocusIn(a) {
    const b = DomHelper.getActiveElement(this);
    super.onFocusIn(a);
    if (DomHelper.getActiveElement(this) === b && a.target === this.element) {
      this.focus();
    }
  }
  onFocusOut(a) {
    const b = globalThis.visualViewport && globalThis.visualViewport.height < document.documentElement.clientHeight - 1;
    if (!b && !this.modal && this.autoClose) {
      this.close();
    }
    super.onFocusOut(a);
  }
  afterShow() {
    const a = this;
    if (a.autoClose || a.modal) {
      a.addDocumentMouseDownListener();
    }
    super.afterShow(...arguments);
    a.toFront();
  }
  toFront() {
    if (this.floating) {
      super.toFront();
      this.syncModalMask();
    }
    if (this.focusOnToFront) {
      this.focus();
    }
  }
  addDocumentMouseDownListener() {
    if (!this.mouseDownRemover) {
      this.mouseDownRemover = GlobalEvents_default.ion({
        'globaltap': 'onDocumentMouseDown',
        'thisObj': this
      });
    }
  }
  updateModal(a) {
    if (a) {
      const b = this;
      b.floating = true;
      if (typeof b._x !== 'number' && typeof b._y !== 'number') {
        b.centered = true;
        b.anchor = false;
      }
    }
  }
  syncModalMask() {
    const a = this, {
        modal: b,
        element: c
      } = a;
    c.setAttribute('aria-modal', Boolean(b));
    if (b && a.isVisible) {
      a.modalMask.classList.remove('b-hide-display');
      a.modalMask.classList.toggle('b-modal-transparent', Boolean(b.transparent));
      c.classList.add('b-modal');
      a.modalMask.setAttribute('owned-by', a.id);
    } else if (a.isPainted) {
      c.classList.remove('b-modal');
      const e = a.floatRoot.querySelectorAll('.b-modal'), f = e.length ? _Popup.fromElement(e[e.length - 1], 'popup') : null;
      if (f) {
        f.syncModalMask();
      } else {
        _Popup.modalMask.classList.add('b-hide-display');
      }
    }
  }
  afterHide() {
    let a;
    const b = this;
    super.afterHide(...arguments);
    (a = b.mouseDownRemover) == null ? void 0 : a.call(b);
    b.mouseDownRemover = null;
    b.syncModalMask();
  }
  onElementUserAction(a) {
    const {showOnClick: b} = this;
    if (typeof b !== 'number' || b === a.button) {
      if (a.button === 2) {
        a.preventDefault();
      }
      this.show();
    }
  }
  onHeaderDblClick() {
    if (this.maximizable) {
      this.toggleMaximized();
    }
  }
  updateClosable(a) {
    this.tools.close.hidden = !a;
  }
  updateMaximizable(a) {
    this.tools.maximize.hidden = !a;
  }
  get modalMask() {
    const {modalMask: a} = _Popup;
    if (a.nextElementSibling !== this.element) {
      this.floatRoot.insertBefore(a, this.element);
    }
    return a;
  }
  static get modalMask() {
    if (!this._modalMask) {
      this._modalMask = DomHelper.createElement({ 'className': 'b-modal-mask b-hide-display' });
      EventHelper.on({
        'element': this._modalMask,
        'wheel': a => a.preventDefault()
      });
    }
    return this._modalMask;
  }
};
const Popup = _Popup;
Popup.initClass();
Popup._$name = 'Popup';
const bIcon2 = /^b-icon-/;
const bFa2 = /^b-fa-/;
const MenuItem = class extends Widget {
  static type = 'menuitem';
  static $name = 'MenuItem';
  static get configurable() {
    return {
      'checked': null,
      'separator': null,
      'toggleGroup': null,
      'menu': {
        'value': null,
        '$config': [
          'lazy',
          'nullify'
        ]
      },
      'icon': null,
      'text': null,
      'closeParent': null,
      'href': null,
      'radio': null,
      'target': null,
      'localizableProperties': ['text'],
      'role': 'menuitem',
      'openMenuDelay': 120
    };
  }
  updateElement(a, b) {
    const c = super.updateElement(a, b);
    if (typeof this.checked === 'boolean') {
      this.role = 'menuitemcheckbox';
    }
    this.ariaHasPopup = this.hasMenu ? 'menu' : false;
    return c;
  }
  compose() {
    let a;
    const b = this, {
        checked: c,
        href: e,
        hasMenu: f,
        radio: g,
        separator: i,
        target: j,
        text: k
      } = b, l = typeof c === 'boolean', n = b.icon || (l ? 'b-fw-icon' : ''), o = 'b-icon-' + (g ? 'radio-' : ''), p = typeof k === 'object';
    return {
      'tag': e ? 'a' : 'div',
      'tabIndex': -1,
      'href': e,
      'target': j,
      'class': {
        'b-has-submenu': f,
        'b-checked': c,
        'b-separator': i || ((a = b.cls) == null ? void 0 : a['b-separator'])
      },
      'dataset': { 'group': b.toggleGroup },
      [l ? 'aria-checked' : '']: c,
      [f ? 'aria-expanded' : '']: false,
      'children': {
        'iconElement': n && {
          'aria-hidden': true,
          'tag': 'i',
          'class': {
            'b-fa': bFa2.test(n),
            'b-icon': bIcon2.test(n),
            'b-menuitem-icon': 1,
            [o + 'checked']: c === true,
            [o + 'unchecked']: c === false,
            ...DomClassList.normalize(n, 'object')
          }
        },
        'textElement': {
          'tag': 'span',
          [p ? void 0 : 'text']: p ? void 0 : k,
          'class': {
            'b-menu-text': 1,
            'b-menu-custom-content': p
          },
          'children': p ? [k] : null
        },
        'subMenuIcon': f && {
          'aria-hidden': true,
          'tag': 'i',
          'class': {
            'b-fw-icon': 1,
            'b-icon-sub-menu': 1
          }
        }
      }
    };
  }
  doAction(a) {
    const b = this, c = this.parent, e = {
        'menu': c,
        'item': b,
        'element': b.element,
        'bubbles': true,
        'domEvent': a
      };
    if (typeof b.checked === 'boolean') {
      const f = !b.checked;
      if (!b.toggleGroup || f) {
        b.checked = !b.checked;
      }
    } else if (b.href && a.key) {
      b.element.click();
    }
    b.trigger('beforeItem', e);
    b.trigger('item', e);
    if (b.closeParent && c) {
      c.rootMenu.close();
      if (a && !b.href) {
        a.preventDefault();
      }
    }
  }
  get focusElement() {
    return this.element;
  }
  get contentElement() {
    return this.textElement;
  }
  get isFocusable() {
    const {focusElement: a} = this;
    return a && this.isVisible && (a === document.body || a.offsetParent);
  }
  get hasMenu() {
    return this.hasConfig('menu');
  }
  get childItems() {
    const {_menu: a} = this;
    return a ? [a] : [];
  }
  get radio() {
    let a;
    return Boolean((a = this._radio) != null ? a : this.toggleGroup);
  }
  onFocusIn(a) {
    const {menu: b} = this;
    super.onFocusIn(a);
    if (!this.disabled && b) {
      b.clearTimeout(b.closeTimer);
      this.delay('openMenu', b.closeTimer ? 0 : this.openMenuDelay);
      b.closeTimer = null;
    }
  }
  onFocusOut(a) {
    let b;
    const c = this;
    c.clearTimeout('openMenu');
    super.onFocusOut(a);
    const f = c._menu;
    if (f && !((b = c.owner) == null ? void 0 : b.ignoreFocusOut)) {
      if (DomHelper.usingKeyboard) {
        c.closeMenu();
      } else {
        f.clearTimeout(f.closeTimer);
        f.closeTimer = f.setTimeout(() => c.closeMenu(), f.closeMenuDelay);
      }
    }
  }
  openMenu(a) {
    const {
      menu: b,
      parent: c
    } = this;
    if (!this.disabled && b) {
      b.focusOnToFront = a;
      c.isMenu ? c.openSubMenu(this.element, this) : b.show();
    }
  }
  onChildShow(a) {
    super.onChildShow(a);
    this.ariaElement.setAttribute('aria-expanded', true);
  }
  closeMenu() {
    if (this._menu.isWidget) {
      this.menu.close();
    }
    this.menu.closeTimer = null;
  }
  onChildHide(a) {
    super.onChildHide(a);
    this.ariaElement.setAttribute('aria-expanded', false);
  }
  changeToggleGroup(a) {
    if (a && typeof this.checked !== 'boolean') {
      this.checked = false;
    }
    return a;
  }
  changeChecked(a, b) {
    if (this.isConfiguring || typeof b === 'boolean') {
      return Boolean(a);
    }
  }
  updateChecked(a) {
    const b = this;
    if (!b.isConfiguring) {
      if (b.toggleGroup) {
        b.uncheckToggleGroupMembers();
      }
      b.element.setAttribute('aria-checked', a);
      b.trigger('toggle', {
        'menu': b.owner,
        'item': b,
        'element': b.element,
        'bubbles': true,
        'checked': a
      });
    }
  }
  getToggleGroupMembers() {
    const a = this, {
        checked: b,
        toggleGroup: c,
        element: e
      } = a, f = [];
    if (b && c) {
      DomHelper.forEachSelector(a.rootElement, '[data-group=' + c + ']', g => {
        if (g !== e) {
          const i = Widget.fromElement(g);
          i && f.push(i);
        }
      });
    }
    return f;
  }
  uncheckToggleGroupMembers() {
    if (this.checked && this.toggleGroup) {
      this.getToggleGroupMembers().forEach(a => a.checked = false);
    }
  }
  get closeParent() {
    const a = typeof this.checked === 'boolean' ? this._closeParent : this._closeParent !== false;
    return a && !this.hasMenu;
  }
  changeMenu(a, b) {
    const c = this, {
        constrainTo: e,
        scrollAction: f
      } = c.owner;
    if (a && typeof a === 'object' && !('items' in a) && !('widgets' in a) && !('html' in a)) {
      a = { 'lazyItems': a };
    }
    return Menu.reconfigure(b, a, {
      'owner': c,
      'defaults': {
        'type': 'menu',
        'align': 's0-e0',
        'anchor': true,
        'autoClose': true,
        'autoShow': false,
        'cls': { 'b-sub-menu': 1 },
        'forElement': c.element,
        'owner': c,
        'ariaLabel': c.text,
        'constrainTo': e,
        'scrollAction': f
      }
    });
  }
};
MenuItem.initClass();
MenuItem._$name = 'MenuItem';
const validKeys = {
  'ArrowUp': 1,
  'ArrowDown': 1,
  'ArrowRight': 1,
  'ArrowLeft': 1,
  'Enter': 1,
  'Escape': 1
};
var Menu = class extends Popup {
  static configurable = {
    'layout': 'vbox',
    'focusable': true,
    'showAnimation': null,
    'hideAnimation': {
      'opacity': {
        'from': 1,
        'to': 0,
        'duration': '.15s',
        'delay': '0s'
      }
    },
    'align': { 'align': 't-b' },
    'scrollAction': 'hide',
    'focusOnHover': null,
    'scrollable': false,
    'defaultType': 'menuitem',
    'tools': { 'close': false },
    'role': 'menu',
    'ariaElement': 'bodyElement',
    'closeMenuDelay': 200
  };
  static type = 'menu';
  static $name = 'Menu';
  construct(a) {
    if (Array.isArray(a)) {
      a = { 'lazyItems': a };
    }
    super.construct(a);
    EventHelper.on({
      'element': this.element,
      'click': 'onMouseClick',
      'mouseover': 'onMouseOver',
      'mouseenter': 'onMouseEnter',
      'mouseleave': 'onMouseLeave',
      'thisObj': this
    });
  }
  afterShow() {
    const {
        items: a,
        element: b
      } = this, c = a.some(f => f.hasConfig('menu'));
    if (c) {
      b.classList.add('b-menu-with-submenu');
    }
    const e = a.some(f => f.icon);
    if (e) {
      b.classList.add('b-menu-with-icon');
    }
    super.afterShow(...arguments);
  }
  createWidget(a) {
    if (typeof a === 'string') {
      a = { 'text': a };
    }
    return super.createWidget(a);
  }
  get focusElement() {
    let a;
    const b = this, c = (a = b.parentMenu) == null ? void 0 : a.element.contains(DomHelper.getActiveElement(b.parentMenu)), e = b.items[0];
    if (c || DomHelper.usingKeyboard || !(e instanceof MenuItem)) {
      return super.focusElement;
    }
    return b.element;
  }
  onDocumentMouseDown({event: a}) {
    if (!this.parentMenu || !this.parentMenu.owns(a.target)) {
      return super.onDocumentMouseDown(...arguments);
    }
  }
  hide(a) {
    let b, c;
    const e = this;
    super.hide(a);
    if (!e.isVisible) {
      (c = (b = e.currentSubMenu) == null ? void 0 : b.hide) == null ? void 0 : c.call(b, a);
      if (e.parentMenu) {
        e.parentMenu.currentSubMenu = null;
      }
    }
  }
  show() {
    const a = super.show(...arguments);
    const {parentMenu: b} = this;
    if (this.isVisible && b) {
      b.currentSubMenu = this;
    }
    return a;
  }
  onMouseClick(a) {
    const b = a.target.closest('.b-menuitem');
    if (b) {
      this.triggerElement(b, a);
    }
  }
  onMouseOver(a) {
    let b;
    if (this.focusOnHover !== false) {
      const c = (b = a.relatedTarget) == null ? void 0 : b.closest('.b-widget'), e = a.target.closest('.b-widget'), f = Widget.fromElement(e);
      if (!DomHelper.isTouchEvent && e && e !== c && f.parent === this) {
        this.setTimeout({
          'fn': 'handleMouseOver',
          'delay': 30,
          'args': [f],
          'cancelOutstanding': true
        });
      }
    }
  }
  handleMouseOver(a) {
    a.focus();
  }
  onMouseEnter() {
    this.clearTimeout(this.closeTimer);
    const {parentMenu: a} = this;
    if (a) {
      a.ignoreFocusOut = true;
      a.setTimeout(() => a.ignoreFocusOut = false, 500);
      a.clearTimeout(a.hideSubMenuTimer);
    }
  }
  onMouseLeave(a) {
    const b = this, {relatedTarget: c} = a, e = c && b.owns(c);
    let f = c instanceof HTMLElement && Widget.fromElement(c), g = !e;
    if (f) {
      while (f.ownerCmp) {
        f = f.ownerCmp;
      }
      g &= !DomHelper.getAncestor(f.element, [a.target]);
    }
    if (!e && g) {
      if (b.currentSubMenu) {
        b.hideSubMenuTimer = b.setTimeout(() => {
          let i;
          return (i = b.currentSubMenu) == null ? void 0 : i.hide();
        }, b.closeMenuDelay);
      }
      if (b.element.contains(DomHelper.getActiveElement(b)) && DomHelper.getActiveElement(b).matches('.b-menuitem')) {
        b.focusElement.focus();
      }
    }
  }
  onInternalKeyDown(a) {
    const b = Widget.fromElement(a), c = b && b !== this && !(b instanceof MenuItem);
    if (a.key === 'Escape') {
      (c ? this : this.rootMenu).close();
      return;
    }
    super.onInternalKeyDown(a);
    if (c) {
      return;
    }
    if (validKeys[a.key]) {
      a.preventDefault();
    }
    const e = this.element, f = DomHelper.getActiveElement(e);
    this.navigateFrom(f !== e && e.contains(f) ? f : null, a.key, a);
  }
  navigateFrom(a, b, c) {
    let e, f;
    const g = this, {treeWalker: i} = g, j = a && g.getItem(a), k = g.rtl ? 'ArrowLeft' : 'ArrowRight', l = g.rtl ? 'ArrowRight' : 'ArrowLeft';
    let n;
    switch (b) {
    case 'ArrowUp':
      i.currentNode = a || (a = g.bottomFocusTrap);
      i.previousNode();
      n = i.currentNode;
      (e = j == null ? void 0 : j.menu) == null ? void 0 : e.hide(false);
      break;
    case 'ArrowDown':
      i.currentNode = a || (a = g.topFocusTrap);
      i.nextNode();
      n = i.currentNode;
      (f = j == null ? void 0 : j.menu) == null ? void 0 : f.hide(false);
      break;
    case ' ':
      if (a && !a.classList.contains('b-disabled')) {
        if (j == null ? void 0 : j.menu) {
          g.openSubMenu(a, j);
        } else {
          g.triggerElement(a, c);
        }
      }
      break;
    case k:
      if (a && (j == null ? void 0 : j.menu) && !a.classList.contains('b-disabled')) {
        const o = g.openSubMenu(a, j);
        o == null ? void 0 : o.focus();
      } else {
        i.currentNode = a || (a = g.topFocusTrap);
        i.nextNode();
        n = i.currentNode;
      }
      break;
    case l:
      if (g.isSubMenu) {
        g.hide();
      } else if (!a) {
        i.currentNode = a || (a = g.topFocusTrap);
        i.nextNode();
        n = i.currentNode;
      }
      break;
    case 'Enter':
      if (a && !a.classList.contains('b-disabled')) {
        g.triggerElement(a, c);
      }
      break;
    }
    if (n) {
      if (n === g.element) {
        g.navigateFrom(g.bottomFocusTrap, 'ArrowUp', c);
      } else if (n === a) {
        g.navigateFrom(g.topFocusTrap, 'ArrowDown', c);
      } else {
        n.focus();
      }
    }
  }
  getItem(a) {
    if (a != null) {
      if (typeof a === 'number') {
        return this.items[a];
      }
      if (a.nodeType === Element.ELEMENT_NODE) {
        return Widget.fromElement(a, 'menuitem', this.contentElement);
      }
      return this.items.find(b => b.id == a);
    }
  }
  triggerElement(a, b) {
    const c = this.getItem(a);
    if (c && !c.disabled) {
      c.doAction(b);
    }
  }
  get isSubMenu() {
    let a;
    return this === ((a = this.owner) == null ? void 0 : a.menu);
  }
  openSubMenu(a, b) {
    let c;
    const e = this, f = b.menu;
    if (f) {
      if (!f.isVisible) {
        const g = {
          'item': b,
          'element': a
        };
        if (e.trigger('beforeSubMenu', g) === false) {
          return;
        }
        if (((c = b.onBeforeSubMenu) == null ? void 0 : c.call(b, g)) === false) {
          return;
        }
        f.show();
        if (f.isVisible && !f.isMenu) {
          e.currentSubMenu = f;
        }
      }
      return f;
    }
  }
  set selectedElement(a) {
    const b = this, c = b._selectedElement;
    if (c) {
      const e = b.getItem(c), f = e == null ? void 0 : e.menu;
      f == null ? void 0 : f.hide();
      c.classList.remove('b-active');
    }
    b._selectedElement = a;
    if (a) {
      const g = DomHelper.isFocusable(a);
      a.classList.add('b-active');
      b.scrollable.scrollIntoView(a, {
        'animate': !g,
        'focus': g
      });
    }
  }
  get selectedElement() {
    return this._selectedElement;
  }
  selectFirst() {
    const a = this.treeWalker;
    a.currentNode = this.topFocusTrap;
    a.nextNode();
    this.requestAnimationFrame(() => a.currentNode.focus());
  }
  get parentMenu() {
    let a;
    const b = this.owner;
    return b && (b.isMenu ? b : (a = b.up) == null ? void 0 : a.call(b, 'menu'));
  }
  get rootMenu() {
    let a = this;
    while (a.parentMenu instanceof this.constructor) {
      a = a.parentMenu;
    }
    return a;
  }
};
Menu.initClass();
Menu._$name = 'Menu';
let contextMenuKeyEvent;
const ContextMenuBase = class extends InstancePlugin {
  static get $name() {
    return 'ContextMenuBase';
  }
  static get configurable() {
    return {
      'type': null,
      'menu': {
        '$config': [
          'lazy',
          'nullify'
        ],
        'value': {
          'type': 'menu',
          'autoShow': false,
          'closeAction': 'hide',
          'constrainTo': globalThis,
          'scrollAction': null
        }
      },
      'items': {},
      'triggerEvent': null,
      'clickTriggerSelector': null,
      'preventNativeMenu': null,
      'keyMap': {
        ' ': {
          'handler': 'showContextMenuByKey',
          'weight': 100
        },
        'Ctrl+Space': 'showContextMenuByKey'
      }
    };
  }
  static get pluginConfig() {
    return {
      'assign': ['showContextMenu'],
      'chain': [
        'onElementContextMenu',
        'onElementClick',
        'onElementDblClick'
      ]
    };
  }
  construct(...a) {
    let b;
    super.construct(...a);
    if (!((b = this.type) == null ? void 0 : b.length)) {
      throw new Error('Config \'type\' is required to be specified for context menu');
    }
  }
  onElementContextMenu(a) {
    this.onElementEvent(a);
  }
  onElementClick(a) {
    this.onElementEvent(a);
  }
  onElementDblClick(a) {
    this.onElementEvent(a);
  }
  onElementEvent(a) {
    if (!a.handled && this.triggerEvent === a.type) {
      if (a.ctrlKey === true && !BrowserHelper.isMac) {
        a.preventDefault();
        return;
      }
      const b = a;
      if (contextMenuKeyEvent) {
        const {target: c} = contextMenuKeyEvent;
        a = new MouseEvent(a.type, a);
        Object.defineProperties(a, {
          'target': { 'value': c },
          'offsetX': { 'value': b.offsetX },
          'offsetY': { 'value': b.offsetY }
        });
        a.preventDefault = function () {
          b.preventDefault();
          b.handled = true;
        };
        a = EventHelper.fixEvent(a);
      }
      this.internalShowContextMenu(a);
    } else if (a.type === 'click' && this.clickTriggerSelector && a.target.matches(this.clickTriggerSelector)) {
      this.internalShowContextMenu(a, {
        'align': 'l-r',
        'target': a.target
      });
    }
  }
  showContextMenuByKey(a) {
    if (!DomHelper.isEditable(a.target)) {
      const {target: b} = a;
      if (b) {
        contextMenuKeyEvent = a;
        DomHelper.triggerMouseEvent(b, this.triggerEvent);
        a.handled = true;
        a.preventDefault();
        a.stopImmediatePropagation();
        contextMenuKeyEvent = null;
        return true;
      }
    }
    return false;
  }
  internalShowContextMenu(a, b) {
    const c = this;
    let e;
    if (c.disabled) {
      return;
    }
    if (!a.handled && !a.defaultPrevented && (e = c.getDataFromEvent(a)) && c.shouldShowMenu(e)) {
      if (a.type === 'contextmenu' && BrowserHelper.isSafari && !a.ctrlKey && a.metaKey) {
        EventHelper.on({
          'element': a.target,
          'capture': true,
          'once': true,
          'thisObj': this,
          'click'(f) {
            f.preventDefault();
            f.stopImmediatePropagation();
          }
        });
      }
      e.domEvent = a;
      c.showContextMenu(e, b);
    }
  }
  getDataFromEvent(a) {
    return {
      'feature': this,
      'event': a,
      'targetElement': this.getTargetElementFromEvent(a)
    };
  }
  getTargetElementFromEvent(a) {
    return a.target;
  }
  async showContextMenu(a, b) {
    let c, e;
    const f = this, g = DomHelper.isDOMEvent(a), i = g ? f.getDataFromEvent(a) : a, j = g ? a : i.domEvent;
    (c = f._menu) == null ? void 0 : c.hide();
    if (f.disabled) {
      return;
    }
    f.menuContext = i;
    const {
        type: k,
        client: l,
        processItems: n
      } = f, o = DomHelper.isInView(i.targetElement).center;
    Objects.assign(i, {
      'point': (j == null ? void 0 : j.clientX) ? [
        j.clientX + 1,
        j.clientY + 1
      ] : [
        o.x,
        o.y
      ],
      'menu': f,
      'items': {},
      'selection': l.selectedRecords
    });
    f.callChainablePopulateMenuMethod(i);
    Objects.merge(i.items, f.baseItems);
    if (f.preventNativeMenu) {
      f.preventDefaultEvent(i);
    }
    if ((!n || await f.callback(n, l, [i]) !== false) && f.hasActiveMenuItems(i)) {
      f.populateItemsWithData(i);
      if (f.beforeContextMenuShow(i) !== false) {
        const {menu: p} = f, {featureItems: q} = p, {items: r} = i;
        f.preventDefaultEvent(i);
        let u = l.trigger(k + 'MenuBeforeShow', i);
        if (ObjectHelper.isPromise(u)) {
          j.handled = true;
          u = await u;
        }
        if (!Object.values(r).some(v => v)) {
          return;
        }
        if (u !== false) {
          if (q) {
            p.remove(q);
            ArrayHelper.asArray(q).forEach(v => v.destroy());
          }
          p.featureItems = p.add(...Array.isArray(r) ? r : p.processItemsObject(r));
          p.showBy(b || {
            'target': i.domEvent,
            'align': 's0-e100'
          });
          (e = f.touchMoveDetacher) == null ? void 0 : e.call(f);
          f.touchMoveDetacher = EventHelper.on({
            'element': l.element,
            'touchmove': () => p.hide(),
            'thisObj': p,
            'once': true
          });
          if (j) {
            j.preventDefault();
            j.handled = true;
          }
        }
      }
    }
  }
  get baseItems() {
    if (!this._baseItems) {
      const a = this, {namedItems: b} = a, c = a._baseItems = Objects.assign({}, a.items);
      for (const e in c) {
        const f = c[e];
        if (f) {
          if (b && e in b) {
            c[e] = typeof f === 'object' ? Objects.merge(Objects.clone(b[e]), f) : b[e];
          } else if (f === true) {
            delete c[e];
          }
        }
      }
    }
    return this._baseItems;
  }
  hideContextMenu(a) {
    let b;
    (b = this.menu) == null ? void 0 : b.hide(a);
  }
  callChainablePopulateMenuMethod(a) {
    let b, c;
    (c = (b = this.client)['populate' + StringHelper.capitalize(this.type) + 'Menu']) == null ? void 0 : c.call(b, a);
  }
  hasActiveMenuItems(a) {
    return Object.values(a.items).some(b => b && !b.hidden);
  }
  shouldShowMenu() {
    return true;
  }
  beforeContextMenuShow(a) {
  }
  populateItemsWithData(a) {
  }
  preventDefaultEvent(a) {
    let b;
    (b = a.event) == null ? void 0 : b.preventDefault();
  }
  get triggerEvent() {
    return this._triggerEvent || this.client.contextMenuTriggerEvent;
  }
  changeMenu(a, b) {
    const c = this, {
        client: e,
        type: f
      } = c;
    if (a) {
      return Menu.reconfigure(b, a ? Menu.mergeConfigs({
        'owner': e,
        'rootElement': e.rootElement,
        'onItem'(g) {
          e.trigger(f + 'MenuItem', g);
        },
        'onToggle'(g) {
          e.trigger(f + 'MenuToggleItem', g);
        },
        'onDestroy'() {
          c.menu = null;
        },
        'onBeforeItem'(g) {
          Object.assign(g, c.menuContext);
        },
        'onShow'({source: g}) {
          c.menuContext.menu = g;
          e.trigger(f + 'MenuShow', c.menuContext);
        }
      }, a) : null, c);
    } else if (b == null ? void 0 : b.isWidget) {
      b.destroy();
    }
  }
};
ContextMenuBase._$name = 'ContextMenuBase';
const DragHelperContainer_default = a => {
  let b;
  return b = class extends (a || Base) {
    initContainerDrag() {
      const c = this;
      if (!c.mode) {
        c.mode = 'container';
      }
      if (c.mode === 'container' && !c.containers) {
        throw new Error('Container drag mode must specify containers');
      }
    }
    grabContainerDrag(c) {
      const e = this;
      if (!e.ignoreSelector || !c.target.closest(e.ignoreSelector)) {
        const f = DomHelper.getAncestor(c.target, e.containers, e.outerElement);
        if (f) {
          const g = f.getBoundingClientRect();
          e.context = {
            'element': f,
            'valid': true,
            'action': 'container',
            'offsetX': c.pageX - g.left,
            'offsetY': c.pageY - g.top,
            'originalPosition': {
              'parent': f.parentElement,
              'prev': f.previousElementSibling,
              'next': f.nextElementSibling
            }
          };
        }
        return true;
      }
      return false;
    }
    startContainerDrag(c) {
      let e;
      const f = this, {
          context: g,
          floatRootOwner: i
        } = f, {element: j} = g, k = j.cloneNode(true), l = i == null ? void 0 : i.element.closest('.b-outer');
      k.classList.add(f.dragProxyCls, f.draggingCls);
      ((i == null ? void 0 : i.floatRoot) || DomHelper.getRootElement(j)).appendChild(k);
      g.dragProxy = k;
      g.dragging = j;
      j.classList.add(f.dropPlaceholderCls);
      if ((e = l == null ? void 0 : l.parentElement) == null ? void 0 : e.matches('.b-float-root')) {
        k.style.zIndex = i.floatRootMaxZIndex + 1;
      }
    }
    onContainerDragStarted(c) {
      const e = this, {context: f} = e, {
          element: g,
          dragProxy: i
        } = f, j = g.getBoundingClientRect();
      if (e.autoSizeClonedTarget) {
        i.style.width = j.width + 'px';
        i.style.height = j.height + 'px';
        DomHelper.setTranslateXY(f.dragProxy, j.left, j.top);
      } else if (e.useCenteredCursor) {
        const k = i.getBoundingClientRect();
        Object.assign(f, {
          'offsetX': k.width / 2,
          'offsetY': k.height / 2
        });
        DomHelper.setTranslateXY(i, c.clientX, c.clientY);
      }
    }
    updateContainerDrag(c) {
      let e;
      const f = this, {context: g} = f;
      if (!g.started || !g.targetElement) {
        return;
      }
      const i = DomHelper.getAncestor(g.targetElement, f.containers, 'b-gridbase'), j = (e = g.dragging) == null ? void 0 : e.contains(DomHelper.getActiveElement(g.dragging));
      if (i && DomHelper.isDescendant(g.element, i)) {
        return;
      }
      if (j) {
        GlobalEvents_default.suspendFocusEvents();
      }
      if (i && g.valid) {
        f.moveNextTo(i, c);
      } else {
        f.revertPosition();
      }
      if (j) {
        GlobalEvents_default.resumeFocusEvents();
      }
      c.preventDefault();
    }
    finishContainerDrag(c) {
      const e = this, {context: f} = e, {
          dragging: g,
          dragProxy: i,
          valid: j,
          draggedTo: k,
          insertBefore: l,
          originalPosition: n
        } = f;
      if (g) {
        f.valid = Boolean(j && (k || e.externalDropTargetSelector && c.target.closest(e.externalDropTargetSelector)) && (g !== l || n.parent !== k));
        f.finalize = (o = f.valid) => {
          if (!o && e.context) {
            e.revertPosition();
          }
          g.classList.remove(e.dropPlaceholderCls);
          i.remove();
          e.reset();
        };
        f.async = false;
        e.trigger('drop', {
          'context': f,
          'event': c
        });
        if (!f.async) {
          f.finalize();
        }
      }
    }
    abortContainerDrag(c = false, e = null, f = false) {
      const g = this, {context: i} = g;
      if (i.dragging) {
        i.dragging.classList.remove(g.dropPlaceholderCls);
        i.dragProxy.remove();
        g.revertPosition();
      }
      if (!f) {
        g.trigger(c ? 'drop' : 'abort', {
          'context': i,
          'event': e
        });
      }
      g.reset();
    }
    updateContainerProxy(c) {
      let e;
      const f = this, {context: g} = f, i = g.dragProxy;
      let j = c.pageX - g.offsetX, k = c.pageY - g.offsetY;
      if (typeof f.minX === 'number') {
        j = Math.max(f.minX, j);
      }
      if (typeof f.maxX === 'number') {
        j = Math.min(f.maxX - i.offsetWidth, j);
      }
      if (typeof f.minY === 'number') {
        k = Math.max(f.minY, k);
      }
      if (typeof f.maxY === 'number') {
        k = Math.min(f.maxY - i.offsetHeight, k);
      }
      if (f.lockX) {
        DomHelper.setTranslateY(i, k);
      } else if (f.lockY) {
        DomHelper.setTranslateX(i, j);
      } else {
        DomHelper.setTranslateXY(i, j, k);
      }
      let l;
      if (c.type === 'touchmove') {
        const n = c.changedTouches[0];
        l = DomHelper.elementFromPoint(n.clientX, n.clientY);
      } else {
        l = c.target;
      }
      g.targetElement = ((e = this.resolveTargetElement) == null ? void 0 : e.call(this, l)) || l;
    }
    moveNextTo(c, e) {
      const {context: f} = this, g = f.dragging, i = c.parentElement;
      if (c !== g) {
        const j = Rectangle.from(c).center.x;
        if (this.isRTL && e.pageX > j || !this.isRTL && e.pageX < j) {
          i.insertBefore(g, c);
          f.insertBefore = c;
        } else {
          if (c.nextElementSibling) {
            if (c.nextElementSibling !== g) {
              f.insertBefore = c.nextElementSibling;
              i.insertBefore(g, c.nextElementSibling);
            } else if (!f.insertBefore && g.parentElement.lastElementChild !== g) {
              f.insertBefore = c.nextElementSibling;
            }
          } else {
            i.appendChild(g);
            f.insertBefore = null;
          }
        }
        f.draggedTo = i;
      }
    }
    revertPosition() {
      const {context: c} = this, {dragging: e} = c, {
          parent: f,
          next: g
        } = c.originalPosition;
      if (g) {
        const i = g.previousSibling === e || !g && e === f.lastChild;
        if (!i) {
          f.insertBefore(e, g);
        }
      } else {
        f.appendChild(e);
      }
      c.draggedTo = null;
    }
  }, __publicField(b, '$name', 'DragHelperContainer'), b;
};
const noScroll = {
  'pageXOffset': 0,
  'pageYOffset': 0
};
const DragHelperTranslate_default = a => class b extends Delayable_default(a || Base) {
  static get $name() {
    return 'DragHelperTranslate';
  }
  static get configurable() {
    return {
      'positioning': null,
      'skipUpdatingElement': null
    };
  }
  initTranslateDrag() {
    const c = this;
    if (!c.isElementDraggable && c.targetSelector) {
      c.isElementDraggable = e => e.closest(c.targetSelector);
    }
  }
  grabTranslateDrag(c) {
    const e = this.getTarget(c);
    if (e) {
      this.context = {
        'valid': true,
        'element': e,
        'startPageX': c.pageX,
        'startPageY': c.pageY,
        'startClientX': c.clientX,
        'startClientY': c.clientY
      };
      return true;
    }
    return false;
  }
  getTarget(c) {
    return c.target.closest(this.targetSelector);
  }
  getX(c) {
    if (this.positioning === 'absolute') {
      return parseFloat(c.style.left, 10);
    }
    if (this.positioning === 'inset') {
      return parseFloat(getComputedStyle(c).insetInlineStart, 10);
    }
    return DomHelper.getTranslateX(c);
  }
  getY(c) {
    if (this.positioning === 'absolute') {
      return parseFloat(c.style.top, 10);
    }
    if (this.positioning === 'inset') {
      return parseFloat(getComputedStyle(c).insetBlockStart, 10);
    }
    return DomHelper.getTranslateY(c);
  }
  getXY(c) {
    if (this.positioning === 'absolute') {
      return [
        c.offsetLeft,
        c.offsetTop
      ];
    }
    if (this.positioning === 'inset') {
      const e = getComputedStyle(c);
      return [
        parseFloat(e.insetInlineStart, 10),
        parseFloat(e.insetBlockStart, 10)
      ];
    }
    return DomHelper.getTranslateXY(c);
  }
  setXY(c, e, f) {
    if (this.skipUpdatingElement) {
      return;
    }
    if (this.positioning === 'absolute') {
      c.style.left = e + 'px';
      c.style.top = f + 'px';
    } else if (this.positioning === 'inset') {
      c.style.insetInlineStart = e + 'px';
      c.style.insetBlockStart = f + 'px';
    } else {
      DomHelper.setTranslateXY(c, e, f);
    }
  }
  startTranslateDrag(c) {
    const e = this, {
        context: f,
        outerElement: g,
        proxySelector: i
      } = e, j = e.dragWithin = e.dragWithin || e.cloneTarget && document.body;
    let k = f.dragProxy || f.element;
    const l = k, n = k.parentElement;
    if (e.cloneTarget) {
      const q = i ? k.querySelector(i) : k;
      let {
        width: r,
        height: u,
        x: v,
        y: w
      } = Rectangle.from(q, Rectangle.outer(j));
      if (e.isRTL && k.style.insetInlineStart) {
        v = Rectangle.outer(j).right - Rectangle.from(k).right;
      }
      k = e.createProxy(k);
      let z = v, A = w;
      if (e.autoSizeClonedTarget) {
        k.style.width = r + 'px';
        k.style.height = u + 'px';
      }
      k.classList.add(e.dragProxyCls, e.draggingCls);
      k.classList.remove('b-hover', 'b-selected', 'b-focused');
      j.appendChild(k);
      if (!e.autoSizeClonedTarget || i) {
        const B = k.getBoundingClientRect(), {
            x: C,
            y: E
          } = j.getBoundingClientRect(), F = c.clientX - C, G = c.clientY - E + (j !== document.body ? document.body.getBoundingClientRect().y : 0);
        z = F - B.width / 2;
        A = G - B.height / 2;
        f.startPageX = c.pageX;
        f.startPageY = c.pageY;
      }
      e.setXY(k, z, A);
      l.classList.add('b-drag-original');
      if (e.hideOriginalElement) {
        l.classList.add('b-hidden');
      }
    }
    k.classList.add(e.draggingCls);
    const o = e.getX(k), p = e.getY(k);
    Object.assign(f, {
      'element': k,
      'grabbed': l,
      'grabbedParent': n,
      'grabbedNextSibling': k.nextElementSibling,
      'elementStartX': o,
      'elementStartY': p,
      'elementX': e.positioning === 'inset' ? o : DomHelper.getOffsetX(k, j || g),
      'elementY': e.positioning === 'inset' ? p : DomHelper.getOffsetY(k, j || g),
      'scrollX': 0,
      'scrollY': 0,
      'scrollManagerElementContainsDragProxy': !e.cloneTarget || j === g
    });
    if (j) {
      f.parentElement = k.parentElement;
      if (j !== k.parentElement) {
        j.appendChild(k);
      }
      e.updateTranslateProxy(c);
    }
  }
  onTranslateDragStarted() {
    const c = this, {context: e} = c;
    let {relatedElements: f} = e;
    if (c.unifiedProxy) {
      e.element.classList.add('b-drag-main', 'b-drag-unified-proxy');
    }
    if ((f == null ? void 0 : f.length) > 0) {
      e.relatedElStartPos = [];
      e.relatedElDragFromPos = [];
      const {proxySelector: g} = c;
      let [i, j] = [
        e.elementStartX,
        e.elementStartY
      ];
      e.originalRelatedElements = f;
      f = e.relatedElements = f.map((k, l) => {
        const n = g ? k.querySelector(g) : k, {
            x: o,
            y: p,
            width: q,
            height: r
          } = Rectangle.from(n, c.dragWithin), u = c.cloneTarget ? c.createProxy(k) : k;
        u.classList.add(c.draggingCls);
        u.classList.remove('b-hover', 'b-selected', 'b-focused');
        if (c.cloneTarget) {
          c.setXY(u, o, p);
          c.dragWithin.appendChild(u);
          u.classList.add(c.dragProxyCls);
          if (c.autoSizeClonedTarget) {
            u.style.width = q + 'px';
            u.style.height = r + 'px';
          }
          if (c.hideOriginalElement) {
            k.classList.add('b-hidden');
          }
          k.classList.add('b-drag-original');
        }
        e.relatedElStartPos[l] = e.relatedElDragFromPos[l] = c.getXY(u);
        if (c.unifiedProxy) {
          u.classList.add('b-drag-unified-animation', 'b-drag-unified-proxy');
          i += c.unifiedOffset;
          j += c.unifiedOffset;
          c.setXY(u, i, j);
          e.relatedElDragFromPos[l] = [
            i,
            j
          ];
          u.style.zIndex = 100 - l;
        }
        return u;
      });
      if (c.unifiedProxy && f && f.length > 0) {
        EventHelper.onTransitionEnd({
          'element': f[0],
          'property': 'transform',
          'handler'() {
            f.forEach(k => k.classList.remove('b-drag-unified-animation'));
          },
          'thisObj': c,
          'once': true
        });
      }
    }
  }
  applyConstraints(c, e, f) {
    const g = this, {
        constrain: i,
        dragWithin: j
      } = g, {
        pageXOffset: k,
        pageYOffset: l
      } = j === document.body ? globalThis : noScroll;
    if (j && i) {
      if (e < 0) {
        e = 0;
      }
      if (e + c.offsetWidth > j.scrollWidth) {
        e = j.scrollWidth - c.offsetWidth;
      }
      if (f < 0) {
        f = 0;
      }
      if (f + c.offsetHeight > j.scrollHeight) {
        f = j.scrollHeight - c.offsetHeight;
      }
    }
    if (typeof g.minX === 'number') {
      e = Math.max(g.minX + k, e);
    }
    if (typeof g.maxX === 'number') {
      e = Math.min(g.maxX + k, e);
    }
    if (typeof g.minY === 'number') {
      f = Math.max(g.minY + l, f);
    }
    if (typeof g.maxY === 'number') {
      f = Math.min(g.maxY + l, f);
    }
    return {
      'constrainedX': e,
      'constrainedY': f
    };
  }
  updateTranslateProxy(c, e) {
    const f = this, {
        lockX: g,
        lockY: i,
        context: j
      } = f, k = j.dragProxy || j.element, {
        relatedElements: l,
        relatedElDragFromPos: n
      } = j, o = f.isRTL && f.positioning === 'inset' ? -1 : 1;
    if (j.scrollManagerElementContainsDragProxy && e) {
      j.scrollX = e.getRelativeLeftScroll(k);
      j.scrollY = e.getRelativeTopScroll(k);
    }
    j.pageX = c.pageX;
    j.pageY = c.pageY;
    j.clientX = c.clientX;
    j.clientY = c.clientY;
    let p = j.elementStartX + (c.pageX - j.startPageX + j.scrollX) * o, q = j.elementStartY + c.pageY - j.startPageY + j.scrollY;
    if (f.snapCoordinates) {
      const v = f.snapCoordinates({
        'element': k,
        'newX': p,
        'newY': q
      });
      p = v.x;
      q = v.y;
    }
    const {
      constrainedX: r,
      constrainedY: u
    } = f.applyConstraints(k, p, q);
    if (j.started || r !== p || u !== q) {
      f.setXY(k, g ? void 0 : r, i ? void 0 : u);
    }
    if (l) {
      const w = g ? 0 : r - j.elementStartX, z = i ? 0 : u - j.elementStartY;
      l.forEach((A, B) => {
        const [C, E] = n[B];
        f.setXY(A, C + w, E + z);
      });
    }
    j.newX = r;
    j.newY = u;
  }
  async finishTranslateDrag(c) {
    const e = this, f = e.context, {target: g} = c, i = !e.lockX && Math.round(f.newX) !== Math.round(f.elementStartX), j = !e.lockY && Math.round(f.newY) !== Math.round(f.elementStartY), k = f.dragProxy || f.element, {relatedElements: l} = f;
    if (!e.ignoreSamePositionDrop || i || j) {
      if (f.valid === false) {
        await e.abortTranslateDrag(true, c);
      } else {
        const n = !e.allowDropOutside && Rectangle.from(e.dragWithin || e.outerElement);
        if (n && (typeof e.minX !== 'number' && e.minX !== true && c.pageX < n.left || typeof e.maxX !== 'number' && e.maxX !== true && c.pageX > n.right || typeof e.minY !== 'number' && e.minY !== true && c.pageY < n.top || typeof e.maxY !== 'number' && e.maxY !== true && c.pageY > n.bottom)) {
          await e.abortTranslateDrag(true, c);
        } else {
          f.finalize = async (o = f.valid) => {
            if (f.finalized) {
              console.warn('DragHelper: Finalizing already finalized drag');
              return;
            }
            f.finalized = true;
            if (!o && e.context) {
              await e.abortTranslateDrag(true, null, true);
            }
            if (!e.isDestroyed) {
              e.trigger('dropFinalized', {
                'context': f,
                'event': c,
                'target': g
              });
              e.reset();
            }
            if (!e.cloneTarget && k.parentElement !== f.grabbedParent) {
              [
                k,
                ...l || []
              ].forEach(p => p.style.transform = '');
            }
          };
          f.async = false;
          await e.trigger('drop', {
            'context': f,
            'event': c,
            'target': g
          });
          if (!f.async) {
            await f.finalize();
          }
        }
      }
    } else {
      e.abortTranslateDrag(false, c);
    }
  }
  async abortTranslateDrag(c = false, e = null, f = false) {
    let g, i;
    const j = this, {
        cloneTarget: k,
        context: l,
        proxySelector: n,
        dragWithin: o,
        draggingCls: p
      } = j, {
        relatedElements: q,
        relatedElStartPos: r,
        grabbed: u
      } = l, v = l.dragProxy || l.element;
    l.valid = false;
    (g = j.scrollManager) == null ? void 0 : g.stopMonitoring();
    if (l.aborted) {
      console.warn('DragHelper: Aborting already aborted drag');
      return;
    }
    let {
      elementStartX: w,
      elementStartY: z
    } = l;
    const A = w !== j.getX(v) || z !== j.getY(v);
    if (v && l.started) {
      if (!k && o && o !== l.grabbedParent) {
        l.grabbedParent.insertBefore(v, l.grabbedNextSibling);
      }
      if (k) {
        if (n) {
          const B = u.querySelector(n) || u, {
              x: C,
              y: E
            } = Rectangle.from(B);
          w = C;
          z = E;
        }
      }
      v.classList.add('b-aborting');
      j.setXY(v, w, z);
      q == null ? void 0 : q.forEach((F, G) => {
        F.classList.remove(p);
        F.classList.add('b-aborting');
        j.setXY(F, r[G][0], r[G][1]);
      });
      if (!f) {
        j.trigger(c ? 'drop' : 'abort', {
          'context': l,
          'event': e
        });
      }
      if (v.isConnected && !j.isDestroying && A) {
        const F = j.positioning === 'inset' ? 'inset' : 'transform';
        await EventHelper.waitForTransitionEnd({
          'element': v,
          'property': DomHelper.getPropertyTransitionDuration(v, F) ? F : 'all',
          'thisObj': j,
          'once': true,
          'timeout': 300,
          'runOnDestroy': true
        });
      }
      if (!j.isDestroyed) {
        j.trigger('abortFinalized', {
          'context': l,
          'event': e
        });
      }
    }
    if ((i = j.context) == null ? void 0 : i.started) {
      j.reset();
    }
  }
  cleanUp() {
    const c = this, {
        context: e,
        cloneTarget: f,
        draggingCls: g,
        dragProxyCls: i
      } = c, j = e.dragProxy || e.element, {
        relatedElements: k,
        originalRelatedElements: l,
        grabbed: n
      } = e, o = f && (c.removeProxyAfterDrop || !e.valid), p = [
        g,
        'b-aborting',
        i,
        'b-drag-main',
        'b-drag-unified-proxy'
      ];
    j.classList.remove(...p);
    if (o) {
      j.remove();
    }
    k == null ? void 0 : k.forEach(q => {
      if (o) {
        q.remove();
      } else {
        q.classList.remove(...p);
      }
    });
    n.classList.remove('b-drag-original', 'b-hidden');
    l == null ? void 0 : l.forEach(q => q.classList.remove('b-hidden', 'b-drag-original'));
  }
};
const rootElementListeners = {
  'move': 'onMouseMove',
  'up': 'onMouseUp',
  'docclick': 'onDocumentClick',
  'touchstart': 'onTouchStart',
  'touchmove': 'onTouchMove',
  'touchend': 'onTouchEnd',
  'keydown': 'onKeyDown',
  'keyup': 'onKeyDownUp'
};
const DragHelper = class extends Base.mixin(Events_default, DragHelperContainer_default, DragHelperTranslate_default) {
  static get defaultConfig() {
    return {
      'dragProxyCls': 'b-drag-proxy',
      'invalidCls': 'b-drag-invalid',
      'draggingCls': 'b-dragging',
      'dropPlaceholderCls': 'b-drop-placeholder',
      'dragThreshold': 5,
      'outerElement': document.body,
      'dragWithin': null,
      'unifiedProxy': null,
      'monitoringConfig': null,
      'constrain': true,
      'minX': null,
      'maxX': null,
      'minY': null,
      'maxY': null,
      'mode': 'translateXY',
      'isElementDraggable': null,
      'targetSelector': null,
      'dropTargetSelector': null,
      'dropTargetCls': null,
      'proxySelector': null,
      'cloneTarget': false,
      'autoSizeClonedTarget': true,
      'hideOriginalElement': false,
      'containers': null,
      'ignoreSelector': null,
      'startEvent': null,
      'lockX': false,
      'lockY': false,
      'touchStartDelay': 200,
      'scrollManager': null,
      'snapCoordinates': null,
      'unifiedOffset': 5,
      'removeProxyAfterDrop': true,
      'clickSwallowDuration': 50,
      'ignoreSamePositionDrop': true,
      'allowDropOutside': null,
      'floatRootOwner': null,
      'mouseMoveListenerElement': document,
      'externalDropTargetSelector': null,
      'testConfig': {
        'clickSwallowDuration': 50,
        'touchStartDelay': 100
      },
      'rtlSource': null,
      'createProxy': null,
      'resolveTargetElement': null,
      'disabled': false
    };
  }
  construct(a) {
    const b = this;
    super.construct(a);
    b.initListeners();
    if (b.isContainerDrag) {
      b.initContainerDrag();
    } else {
      b.initTranslateDrag();
    }
    b.onScrollManagerScrollCallback = b.onScrollManagerScrollCallback.bind(b);
  }
  doDestroy() {
    this.reset(true);
    super.doDestroy();
  }
  initListeners() {
    const a = this, {outerElement: b} = a, c = {
        'element': b,
        'pointerdown': 'onPointerDown',
        'thisObj': a
      };
    a.mouseMoveListenerElement = a.getMouseMoveListenerTarget(b);
    EventHelper.on(c);
  }
  getMouseMoveListenerTarget(a) {
    const b = a.getRootNode();
    let c = this.mouseMoveListenerElement;
    if (b.nodeType === Node.DOCUMENT_FRAGMENT_NODE && b.mode === 'closed') {
      c = a.closest('.b-outer') || c;
    }
    return c;
  }
  get isRTL() {
    let a;
    return Boolean((a = this.rtlSource) == null ? void 0 : a.rtl);
  }
  onPointerDown(a) {
    const b = this;
    if (a.button !== 0 || b.context || b.disabled || b.isElementDraggable && !b.isElementDraggable(a.target, a)) {
      return;
    }
    b.startEvent = a;
    const c = b.isContainerDrag ? b.grabContainerDrag(a) : b.grabTranslateDrag(a);
    if (c) {
      const e = globalThis;
      b.blurDetacher = EventHelper.on({
        'element': e,
        'blur': b.onWindowBlur,
        'thisObj': b
      });
      const f = {
        'element': b.mouseMoveListenerElement,
        'thisObj': b,
        'capture': true,
        'keydown': rootElementListeners.keydown,
        'keyup': rootElementListeners.keyup
      };
      if (a.pointerType === 'touch') {
        b.touchStartTimer = b.setTimeout(() => b.touchStartTimer = null, b.touchStartDelay, 'touchStartDelay');
        f.touchmove = {
          'handler': rootElementListeners.touchmove,
          'passive': false
        };
        f.touchend = f.pointerup = rootElementListeners.touchend;
      } else {
        f.pointermove = rootElementListeners.move;
        f.pointerup = rootElementListeners.up;
      }
      b.dragListenersDetacher = EventHelper.on(f);
      if (b.dragWithin && b.dragWithin !== b.outerElement && b.outerElement.contains(b.dragWithin)) {
        const g = Rectangle.from(b.dragWithin, b.outerElement);
        b.minY = g.top;
        b.maxY = g.bottom;
        b.minX = g.left;
        b.maxX = g.right;
      }
    }
  }
  async internalMove(a) {
    let b, c;
    if (a.scrollInitiated) {
      return;
    }
    const e = this, {context: f} = e, g = EventHelper.getDistanceBetween(e.startEvent, a), i = e.touchStartTimer && g > e.dragThreshold;
    if (i) {
      e.abort(true);
      return;
    }
    if (!e.touchStartTimer && (f == null ? void 0 : f.element) && (f.started || g >= e.dragThreshold) && ((b = a.target) == null ? void 0 : b.nodeType) === Node.ELEMENT_NODE) {
      if (!f.started) {
        const j = EventHelper.getXY(e.startEvent), k = EventHelper.getXY(a), l = Math.abs(j[0] - k[0]) >= Math.abs(j[1] - k[1]) ? 'horizontal' : 'vertical';
        if (!f.beforeDragStartFired && e.trigger('beforeDragStart', {
            'context': f,
            'event': a,
            'initialDirection': l
          }) === false) {
          return e.abort();
        }
        f.beforeDragStartFired = true;
        if (Math.abs(EventHelper.getDistanceBetween(e.startEvent, a, {
            'x': e.lockY,
            'y': e.lockX
          })) < e.dragThreshold) {
          return;
        }
        if (e.isContainerDrag) {
          e.startContainerDrag(a);
        } else {
          e.startTranslateDrag(a);
        }
        f.started = true;
        (c = e.scrollManager) == null ? void 0 : c.startMonitoring(ObjectHelper.merge({
          'scrollables': [{ 'element': e.dragWithin || e.outerElement }],
          'callback': e.onScrollManagerScrollCallback
        }, e.monitoringConfig));
        f.outermostEl = DomHelper.getOutermostElement(a.target);
        f.outermostEl.classList.add('b-draghelper-active');
        if (e.dropTargetSelector && e.dropTargetCls) {
          DomHelper.getRootElement(e.outerElement).querySelectorAll(e.dropTargetSelector).forEach(o => o.classList.add(e.dropTargetCls));
        }
        const n = e.trigger('dragStart', {
          'context': f,
          'event': a,
          'initialDirection': l
        });
        if (ObjectHelper.isPromise(n)) {
          await n;
        }
        e.onKeyDownUp(a);
        f.moveUnblocked = true;
        if (e.isContainerDrag) {
          e.onContainerDragStarted(a);
        } else {
          e.onTranslateDragStarted(a);
        }
        e.trigger('afterDragStart', {
          'context': f,
          'event': a,
          'initialDirection': l
        });
      }
      if (f.moveUnblocked) {
        if (e._cachedMouseEvent) {
          e.update(a);
          e.update(e._cachedMouseEvent);
          delete e._cachedMouseEvent;
        } else {
          e.update(a);
        }
      } else {
        e._cachedMouseEvent = a;
      }
    }
  }
  onScrollManagerScrollCallback(a) {
    let b;
    const {lastMouseMoveEvent: c} = this;
    if (((b = this.context) == null ? void 0 : b.element) && c) {
      c.isScroll = true;
      this.update(c, a);
    }
  }
  onTouchMove(a) {
    let b;
    if (((b = a.changedTouches) == null ? void 0 : b.length) === 1) {
      if (this.context && !this.touchStartTimer) {
        a.preventDefault();
      }
      this.internalMove(a);
    }
  }
  onMouseMove(a) {
    this.internalMove(a);
  }
  update(a, b) {
    const c = this, {context: e} = c;
    let f = c.getMouseMoveEventTarget(a);
    if (a.type === 'touchmove') {
      f = EventHelper.getComposedPathTarget(a);
    }
    e.target = f;
    let g = c.allowDropOutside || !c.dragWithin || c.dragWithin.contains(a.target);
    if (g && c.dropTargetSelector) {
      g = g && Boolean(f == null ? void 0 : f.closest(c.dropTargetSelector));
    }
    if (c.isContainerDrag) {
      c.updateContainerProxy(a, b);
    } else {
      c.updateTranslateProxy(a, b);
    }
    e.valid = g;
    c.trigger('drag', {
      'context': e,
      'event': a
    });
    if (c.isContainerDrag) {
      c.updateContainerDrag(a, b);
    }
    e.valid = e.valid && g;
    for (const i of c.draggedElements) {
      i.classList.toggle(c.invalidCls, !e.valid);
    }
    if (a) {
      c.lastMouseMoveEvent = a;
    }
  }
  get draggedElements() {
    let a;
    const {context: b} = this;
    return [
      b.dragProxy || b.element,
      ...(a = b.relatedElements) != null ? a : []
    ];
  }
  async abort(a = false) {
    let b, c;
    const e = this, {context: f} = e;
    (c = (b = e.scrollManager) == null ? void 0 : b.stopMonitoring) == null ? void 0 : c.call(b);
    e.removeListeners();
    if ((f == null ? void 0 : f.started) && !f.aborted) {
      f.element.getBoundingClientRect();
      f.valid = false;
      if (e.isContainerDrag) {
        e.abortContainerDrag(void 0, void 0, a);
      } else {
        e.abortTranslateDrag(void 0, void 0, a);
      }
      f.aborted = true;
    } else {
      e.reset(true);
    }
  }
  removeListeners() {
    let a, b;
    (a = this.dragListenersDetacher) == null ? void 0 : a.call(this);
    (b = this.blurDetacher) == null ? void 0 : b.call(this);
  }
  reset(a) {
    const b = this, {context: c} = b;
    if (c == null ? void 0 : c.started) {
      for (const e of b.draggedElements) {
        e.classList.remove(b.invalidCls);
      }
      c.outermostEl.classList.remove('b-draghelper-active');
      if (b.isContainerDrag) {
        c.dragProxy.remove();
      } else {
        b.cleanUp();
      }
      if (b.dropTargetSelector && b.dropTargetCls) {
        DomHelper.getRootElement(b.outerElement).querySelectorAll(b.dropTargetSelector).forEach(f => f.classList.remove(b.dropTargetCls));
      }
    }
    b.removeListeners();
    if (!a) {
      b.trigger('reset');
    }
    b.context = b.lastMouseMoveEvent = null;
  }
  onTouchEnd(a) {
    DomHelper.getOutermostElement(a.target).classList.remove('b-no-touch-scroll');
    this.onMouseUp(a);
  }
  onDocumentClick(a) {
    a.stopPropagation();
  }
  onMouseUp(a) {
    let b;
    const c = this, {context: e} = c;
    c.removeListeners();
    if (e) {
      (b = c.scrollManager) == null ? void 0 : b.stopMonitoring();
      if (e.started) {
        if (e.moveUnblocked) {
          a.stopPropagation();
          e.finalizing = true;
          if (c.isContainerDrag) {
            c.finishContainerDrag(a);
          } else {
            c.finishTranslateDrag(a);
          }
          EventHelper.on({
            'element': document,
            'thisObj': c,
            'click': rootElementListeners.docclick,
            'capture': true,
            'expires': c.clickSwallowDuration,
            'once': true
          });
        } else {
          c.ion({
            'drag'() {
              c.onMouseUp(a);
            },
            'once': true
          });
        }
      } else {
        c.reset(true);
      }
    }
  }
  onKeyDown(a) {
    let b;
    if (((b = this.context) == null ? void 0 : b.started) && a.key === 'Escape') {
      a.stopImmediatePropagation();
      this.abort();
    }
    this.onKeyDownUp(a);
  }
  onKeyDownUp(a) {
    let b;
    (b = this.context) == null ? void 0 : b.element.classList.toggle('b-drag-copy', a[BrowserHelper.ctrlKey] || a[BrowserHelper.metaKey]);
  }
  onWindowBlur() {
    if (this.context && !this.context.finalizing) {
      this.abort();
    }
  }
  createProxy(a) {
    if (this.proxySelector) {
      a = a.querySelector(this.proxySelector) || a;
    }
    const b = a.cloneNode(true);
    b.removeAttribute('id');
    return b;
  }
  get isContainerDrag() {
    return this.mode === 'container';
  }
  async animateProxyTo(a, b = { 'align': 'c-c' }) {
    const {
        context: c,
        draggedElements: e
      } = this, {element: f} = c, g = a.isRectangle ? a : Rectangle.from(a);
    e.forEach(i => {
      i.classList.add('b-drag-final-transition');
      DomHelper.alignTo(i, g, b);
    });
    await EventHelper.waitForTransitionEnd({
      'element': f,
      'property': 'all',
      'thisObj': this,
      'once': true
    });
    e.forEach(i => i.classList.remove('b-drag-final-transition'));
  }
  get isDragging() {
    let a;
    return Boolean((a = this.context) == null ? void 0 : a.started);
  }
  getMouseMoveEventTarget(a) {
    return !a.isScroll ? a.target : DomHelper.elementFromPoint(a.clientX, a.clientY);
  }
};
DragHelper._$name = 'DragHelper';
const documentListeners = {
  'down': 'onMouseDown',
  'move': 'onMouseMove',
  'up': 'onMouseUp',
  'docclick': 'onDocumentClick',
  'touchstart': {
    'handler': 'onTouchStart',
    'passive': false
  },
  'touchmove': 'onTouchMove',
  'touchend': 'onTouchEnd',
  'keydown': 'onKeyDown'
};
const ResizeHelper = class extends Events_default(Base) {
  static configurable = { 'skipUpdatingElement': null };
  static get defaultConfig() {
    return {
      'resizingCls': 'b-resizing',
      'dragThreshold': 5,
      'handleSize': 10,
      'dynamicHandleSize': null,
      'reservedSpace': 10,
      'touchHandleSize': 30,
      'minWidth': 1,
      'maxWidth': 0,
      'minHeight': 1,
      'maxHeight': 0,
      'outerElement': document.body,
      'scroller': null,
      'allowResize': null,
      'dragWithin': null,
      'isElementResizable': null,
      'targetSelector': null,
      'leftHandle': true,
      'rightHandle': true,
      'topHandle': true,
      'bottomHandle': true,
      'handleSelector': null,
      'handleContainerSelector': null,
      'startEvent': null,
      'grab': null,
      'invalidCls': 'b-resize-invalid',
      'handleVisibilityThreshold': null,
      'skipTranslate': false,
      'direction': 'horizontal',
      'clickSwallowDuration': 50,
      'constrainToParent': true,
      'rtlSource': null
    };
  }
  construct(a) {
    const b = this;
    super.construct(a);
    if (!b.handleSelector && !BrowserHelper.isHoverableDevice) {
      b.handleSize = b.touchHandleSize;
    }
    b.handleVisibilityThreshold = b.handleVisibilityThreshold || 2 * b.handleSize;
    b.initListeners();
    b.initResize();
  }
  doDestroy() {
    this.abort(true);
    super.doDestroy();
  }
  updateSkipUpdatingElement(a) {
    if (a) {
      this.skipTranslate = true;
    }
  }
  getX(a) {
    if (this.positioning === 'inset') {
      return parseFloat(getComputedStyle(a).insetInlineStart, 10);
    }
    return DomHelper.getTranslateX(a);
  }
  getY(a) {
    if (this.positioning === 'inset') {
      return parseFloat(getComputedStyle(a).insetBlockStart, 10);
    }
    return DomHelper.getTranslateY(a);
  }
  setX(a, b) {
    if (this.positioning === 'inset') {
      a.style.insetInlineStart = b + 'px';
    } else {
      DomHelper.setTranslateX(a, b);
    }
  }
  setY(a, b) {
    if (this.positioning === 'inset') {
      a.style.insetBlockStart = b + 'px';
    } else {
      DomHelper.setTranslateY(a, b);
    }
  }
  initResize() {
    const a = this;
    if (!a.isElementResizable && a.targetSelector) {
      a.isElementResizable = b => b.closest(a.targetSelector);
    }
    if (a.grab) {
      const {
        edge: b,
        element: c,
        event: e
      } = a.grab;
      a.startEvent = e;
      const f = a.getX(c) || c.offsetLeft, g = a.getY(c) || c.offsetTop;
      a.context = {
        'element': c,
        'edge': b,
        'valid': true,
        'async': false,
        'elementStartX': f,
        'elementStartY': g,
        'newX': f,
        'newY': g,
        'elementWidth': c.offsetWidth,
        'elementHeight': c.offsetHeight,
        'startX': e.clientX + a.scrollLeft,
        'startY': e.clientY + a.scrollTop,
        'finalize': () => {
          let i;
          return (i = a.reset) == null ? void 0 : i.call(a);
        }
      };
      c.classList.add(a.resizingCls);
      a.internalStartResize(a.isTouch);
    }
  }
  initListeners() {
    const a = this, b = {
        'element': a.outerElement,
        'mousedown': documentListeners.down,
        'touchstart': documentListeners.touchstart,
        'thisObj': a
      };
    if (!a.handleSelector && BrowserHelper.isHoverableDevice) {
      b.mousemove = {
        'handler': documentListeners.move,
        'delegate': a.targetSelector
      };
      b.mouseleave = {
        'handler': 'onMouseLeaveTarget',
        'delegate': a.targetSelector,
        'capture': true
      };
    }
    EventHelper.on(b);
  }
  get isRTL() {
    let a;
    return Boolean((a = this.rtlSource) == null ? void 0 : a.rtl);
  }
  get scrollLeft() {
    if (this.scroller) {
      return this.scroller.x;
    }
    return this.outerElement.scrollLeft;
  }
  get scrollTop() {
    if (this.scroller) {
      return this.scroller.y;
    }
    return this.outerElement.scrollTop;
  }
  internalStartResize(a) {
    let b;
    const c = {
      'element': document,
      'keydown': documentListeners.keydown,
      'thisObj': this
    };
    if (a) {
      c.touchmove = documentListeners.touchmove;
      c.touchend = c.pointerup = documentListeners.touchend;
    } else {
      c.mousemove = documentListeners.move;
      c.mouseup = documentListeners.up;
    }
    this.removeDragListeners = EventHelper.on(c);
    (b = this.onResizeHandlePointerDown) == null ? void 0 : b.call(this, this.startEvent);
  }
  removeDragListeners() {
  }
  reset() {
    let a;
    (a = this.removeDragListeners) == null ? void 0 : a.call(this);
    this.context = null;
    this.trigger('reset');
  }
  canResize(a, b) {
    return !this.isElementResizable || this.isElementResizable(a, b);
  }
  onPointerDown(a, b) {
    const c = this;
    c.startEvent = b;
    if (c.canResize(b.target, b) && c.grabResizeHandle(a, b)) {
      b.stopImmediatePropagation();
      if (b.type === 'touchstart') {
        b.preventDefault();
      }
      c.internalStartResize(a);
    }
  }
  onTouchStart(a) {
    if (a.touches.length > 1) {
      return;
    }
    this.onPointerDown(true, a);
  }
  onMouseDown(a) {
    if (a.button !== 0) {
      return;
    }
    this.onPointerDown(false, a);
  }
  internalMove(a, b) {
    let c;
    const e = this, {
        context: f,
        direction: g
      } = e;
    if ((f == null ? void 0 : f.element) && (f.started || EventHelper.getDistanceBetween(e.startEvent, b) >= e.dragThreshold)) {
      if (!f.started) {
        (c = e.scrollManager) == null ? void 0 : c.startMonitoring(ObjectHelper.merge({
          'scrollables': [{
              'element': e.dragWithin || e.outerElement,
              'direction': g
            }],
          'callback': i => {
            let j;
            return ((j = e.context) == null ? void 0 : j.element) && e.lastMouseMoveEvent && e.update(e.lastMouseMoveEvent, i);
          }
        }, e.monitoringConfig));
        e.trigger('resizeStart', {
          'context': f,
          'event': b
        });
        f.started = true;
      }
      e.update(b);
    } else if (!a && !e.handleSelector) {
      e.checkResizeHandles(b);
    }
  }
  onTouchMove(a) {
    this.internalMove(true, a);
  }
  onMouseMove(a) {
    this.internalMove(false, a);
  }
  onPointerUp(a, b) {
    let c, e, f;
    const g = this, i = g.context;
    (c = g.removeDragListeners) == null ? void 0 : c.call(g);
    if (i) {
      (e = g.scrollManager) == null ? void 0 : e.stopMonitoring();
      if (i.started) {
        EventHelper.on({
          'element': DomHelper.getRootElement(b.target),
          'thisObj': g,
          'click': documentListeners.docclick,
          'expires': g.clickSwallowDuration,
          'capture': true,
          'once': true
        });
      }
      g.finishResize(b);
    } else {
      (f = g.reset) == null ? void 0 : f.call(g);
    }
  }
  onTouchEnd(a) {
    this.onPointerUp(true, a);
  }
  onMouseUp(a) {
    this.onPointerUp(false, a);
  }
  onDocumentClick(a) {
    a.stopPropagation();
  }
  onKeyDown(a) {
    if (a.key === 'Escape') {
      this.abort();
    }
  }
  update(a) {
    const b = this, {
        context: c,
        constrainToParent: e
      } = b, {element: f} = c;
    if (e) {
      const g = Rectangle.from(b.outerElement);
      c.currentX = Math.max(Math.min(a.clientX, g.right), g.x) + b.scrollLeft;
      c.currentY = Math.max(Math.min(a.clientY, g.bottom), g.y) + b.scrollTop;
    } else {
      c.currentX = a.clientX + b.scrollLeft;
      c.currentY = a.clientY + b.scrollTop;
    }
    f.classList.add(b.resizingCls);
    if (b.updateResize(a)) {
      b.trigger('resizing', {
        'context': c,
        'event': a
      });
      f.classList.toggle(b.invalidCls, c.valid === false);
    }
    b.lastMouseMoveEvent = a;
  }
  abort(a = false) {
    let b, c;
    const e = this;
    (c = (b = e.scrollManager) == null ? void 0 : b.stopMonitoring) == null ? void 0 : c.call(b);
    if (e.context) {
      e.abortResize(null, a);
    } else if (!e.isDestroyed) {
      e.reset();
    }
  }
  grabResizeHandle(a, b) {
    let c;
    const e = this;
    if (e.allowResize && !e.allowResize(b.target, b)) {
      return false;
    }
    const f = e.handleSelector, g = b.type === 'touchstart' ? b.changedTouches[0] : b, i = g.clientX, j = g.clientY;
    let k = e.targetSelector ? b.target.closest(e.targetSelector) : b.target;
    if (k) {
      let l;
      if (f) {
        if (b.target.matches(f)) {
          if (e.direction === 'horizontal') {
            if (b.pageX < DomHelper.getPageX(k) + k.offsetWidth / 2) {
              l = e.isRTL ? 'right' : 'left';
            } else {
              l = e.isRTL ? 'left' : 'right';
            }
          } else {
            if (b.pageY < DomHelper.getPageY(k) + k.offsetHeight / 2) {
              l = 'top';
            } else {
              l = 'bottom';
            }
          }
        } else {
          return false;
        }
      } else {
        if (e.direction === 'horizontal') {
          if (e.overLeftHandle(b, k)) {
            l = e.isRTL ? 'right' : 'left';
          } else if (e.overRightHandle(b, k)) {
            l = e.isRTL ? 'left' : 'right';
          }
        } else {
          if (e.overTopHandle(b, k)) {
            l = 'top';
          } else if (e.overBottomHandle(b, k)) {
            l = 'bottom';
          }
        }
        if (!l) {
          e.context = null;
          return false;
        }
      }
      if (b.type === 'touchstart') {
        b.preventDefault();
      }
      const n = (c = e.internalBeforeStart) == null ? void 0 : c.call(e, {
        'element': k,
        'edge': l
      });
      if (n) {
        k = n.element;
        l = n.edge;
      }
      if (e.trigger('beforeResizeStart', {
          'element': k,
          'event': b
        }) !== false) {
        const o = e.getX(k) || k.offsetLeft, p = e.getY(k) || k.offsetTop;
        e.context = {
          'element': k,
          'edge': l,
          'isTouch': a,
          'valid': true,
          'async': false,
          'direction': e.direction,
          'elementStartX': o,
          'elementStartY': p,
          'newX': o,
          'newY': p,
          'elementWidth': k.offsetWidth,
          'elementHeight': k.offsetHeight,
          'startX': i + e.scrollLeft,
          'startY': j + e.scrollTop,
          'finalize': () => {
            let q;
            return (q = e.reset) == null ? void 0 : q.call(e);
          }
        };
        k.classList.add(e.resizingCls);
        return true;
      }
    }
    return false;
  }
  checkResizeHandles(a) {
    const b = this, c = b.targetSelector ? a.target.closest(b.targetSelector) : a.target;
    let e = false;
    if (c && (!b.allowResize || b.allowResize(a.target, a))) {
      b.currentElement = b.handleContainerSelector ? a.target.closest(b.handleContainerSelector) : a.target;
      if (b.currentElement) {
        if (b.direction === 'horizontal') {
          e = b.overLeftHandle(a, c) || b.overRightHandle(a, c);
        } else {
          e = b.overTopHandle(a, c) || b.overBottomHandle(a, c);
        }
        if (e) {
          b.highlightHandle();
        } else {
          b.unHighlightHandle();
        }
      }
    } else if (b.currentElement) {
      b.unHighlightHandle();
    }
    return e;
  }
  onMouseLeaveTarget(a) {
    const b = this;
    b.currentElement = b.handleContainerSelector ? a.target.closest(b.handleContainerSelector) : a.target;
    if (b.currentElement) {
      b.unHighlightHandle();
    }
  }
  updateResize(a) {
    const b = this, {
        context: c,
        allowEdgeSwitch: e,
        skipTranslate: f,
        skipUpdatingElement: g
      } = b;
    let i;
    if (e) {
      if (b.direction === 'horizontal') {
        c.edge = c.currentX > c.startX ? 'right' : 'left';
      } else {
        c.edge = c.currentY > c.startY ? 'bottom' : 'top';
      }
    }
    const {
        element: j,
        elementStartX: k,
        elementStartY: l,
        elementWidth: n,
        elementHeight: o,
        edge: p
      } = c, {style: q} = j, r = (c.currentX - c.startX) * (b.positioning === 'inset' && b.isRTL ? -1 : 1), u = c.currentY - c.startY, v = DomHelper.getExtremalSizePX(j, 'minWidth') || b.minWidth, w = DomHelper.getExtremalSizePX(j, 'maxWidth') || b.maxWidth, z = DomHelper.getExtremalSizePX(j, 'minHeight') || b.minHeight, A = DomHelper.getExtremalSizePX(j, 'maxHeight') || b.maxHeight, B = p === 'right' && (!b.isRTL || b.positioning === 'inset') || p === 'bottom' ? 1 : -1, C = n + r * B, E = o + u * B;
    let F = Math.max(v, C), G = Math.max(z, E);
    if (w > 0) {
      F = Math.min(F, w);
    }
    if (A > 0) {
      G = Math.min(G, A);
    }
    if (b.direction === 'horizontal' && n !== F) {
      if (!g) {
        q.flex = '';
        q.width = Math.abs(F) + 'px';
      }
      c.newWidth = F;
      if (p === 'left' || F < 0) {
        const H = Math.max(Math.min(k + n - b.minWidth, k + r), 0);
        if (!f) {
          b.setX(j, Math.round(H));
        }
        c.newX = H;
      } else if (p === 'right' && e && !f) {
        b.setX(j, k);
      }
      i = true;
    } else if (b.direction === 'vertical' && o !== E) {
      if (!g) {
        q.flex = '';
        q.height = Math.abs(G) + 'px';
      }
      c.newHeight = G;
      if (p === 'top' || G < 0) {
        c.newY = Math.max(Math.min(l + o - b.minHeight, l + u), 0);
        if (!f) {
          b.setY(j, c.newY);
        }
      } else if (p === 'bottom' && e && !f) {
        b.setY(j, l);
      }
      i = true;
    }
    return i;
  }
  finishResize(a) {
    let b;
    const c = this, e = c.context, f = {
        'context': e,
        'event': a
      };
    e.element.classList.remove(c.resizingCls);
    if (e.started) {
      let g = false;
      if (c.direction === 'horizontal') {
        g = e.newWidth && e.newWidth !== e.elementWidth;
      } else {
        g = e.newHeight && e.newHeight !== e.elementHeight;
      }
      c.trigger(g ? 'resize' : 'cancel', f);
      if (!e.async) {
        e.finalize();
      }
    } else {
      (b = c.reset) == null ? void 0 : b.call(c);
    }
  }
  abortResize(a = null, b = false) {
    const c = this, e = c.context;
    e.element.classList.remove(c.resizingCls);
    if (c.direction === 'horizontal') {
      if (e.edge === 'left' || e.allowEdgeSwitch && !e.skipTranslate) {
        c.setX(e.element, e.elementStartX);
      }
      e.element.style.width = e.elementWidth + 'px';
    } else {
      c.setY(e.element, e.elementStartY);
      e.element.style.height = e.elementHeight + 'px';
    }
    !b && c.trigger('cancel', {
      'context': e,
      'event': a
    });
    if (!c.isDestroyed) {
      c.reset();
    }
  }
  highlightHandle() {
    const a = this, b = a.targetSelector ? a.currentElement.closest(a.targetSelector) : a.currentElement;
    a.currentElement.classList.add('b-resize-handle');
    b.classList.add('b-over-resize-handle');
  }
  unHighlightHandle() {
    const a = this, b = a.targetSelector ? a.currentElement.closest(a.targetSelector) : a.currentElement;
    b && b.classList.remove('b-over-resize-handle');
    a.currentElement.classList.remove('b-resize-handle');
    a.currentElement = null;
  }
  overAnyHandle(a, b) {
    return this.overStartHandle(a, b) || this.overEndHandle(a, b);
  }
  overStartHandle(a, b) {
    return this.direction === 'horizontal' ? this.overLeftHandle(a, b) : this.overTopHandle(a, b);
  }
  overEndHandle(a, b) {
    return this.direction === 'horizontal' ? this.overRightHandle(a, b) : this.overBottomHandle(a, b);
  }
  getDynamicHandleSize(a, b) {
    const c = a ? 2 : 1, {handleSize: e} = this;
    if (this.dynamicHandleSize && e * c > b - this.reservedSpace) {
      return Math.max(Math.floor((b - this.reservedSpace) / c), 0);
    }
    return e;
  }
  overLeftHandle(a, b) {
    const c = this, {offsetWidth: e} = b;
    if (c.leftHandle && c.canResize(b, a) && (e >= c.handleVisibilityThreshold || c.dynamicHandleSize)) {
      const f = Rectangle.from(b);
      f.width = c.getDynamicHandleSize(c.rightHandle, e);
      return f.width > 0 && f.contains(EventHelper.getPagePoint(a));
    }
    return false;
  }
  overRightHandle(a, b) {
    const c = this, {offsetWidth: e} = b;
    if (c.rightHandle && c.canResize(b, a) && (e >= c.handleVisibilityThreshold || c.dynamicHandleSize)) {
      const f = Rectangle.from(b);
      f.x = f.right - c.getDynamicHandleSize(c.leftHandle, e);
      return f.width > 0 && f.contains(EventHelper.getPagePoint(a));
    }
    return false;
  }
  overTopHandle(a, b) {
    const c = this, {offsetHeight: e} = b;
    if (c.topHandle && c.canResize(b, a) && (e >= c.handleVisibilityThreshold || c.dynamicHandleSize)) {
      const f = Rectangle.from(b);
      f.height = c.getDynamicHandleSize(c.bottomHandle, e);
      return f.height > 0 && f.contains(EventHelper.getPagePoint(a));
    }
    return false;
  }
  overBottomHandle(a, b) {
    const c = this, {offsetHeight: e} = b;
    if (c.bottomHandle && c.canResize(b, a) && (e >= c.handleVisibilityThreshold || c.dynamicHandleSize)) {
      const f = Rectangle.from(b);
      f.y = f.bottom - c.getDynamicHandleSize(c.bottomHandle, e);
      return f.height > 0 && f.contains(EventHelper.getPagePoint(a));
    }
    return false;
  }
};
ResizeHelper._$name = 'ResizeHelper';
const tagSpaceRe = />[ \t\r\n]+</g;
const multiSpaceRe = /\s\s+/g;
const TemplateHelper = class _TemplateHelper {
  static tpl(a, ...b) {
    let c = 0, e = b.map((f, g) => {
        let j = a[g];
        if (Array.isArray(f)) {
          j += f.reduce((k, l) => k += (typeof l === 'string' ? l.trim() : l) + '\n', '');
        } else {
          if (f === void 0) {
            f = '';
          }
          j += typeof f === 'string' ? f.trim() : f;
        }
        c++;
        return j;
      }).join('');
    if (c <= a.length) {
      e += a[a.length - 1];
    }
    return e.replace(tagSpaceRe, '><').replace(multiSpaceRe, ' ').trim();
  }
  static docsTpl(a, ...b) {
    let c = 0, e = b.map((f, g) => {
        let j = a[g];
        if (Array.isArray(f)) {
          j += f.reduce((k, l) => k += (typeof l === 'string' ? l.trim() : l) + '\n', '');
        } else {
          if (f === void 0) {
            f = '';
          }
          j += typeof f === 'string' ? f.trim() : f;
        }
        c++;
        return j;
      }).join('');
    if (c <= a.length) {
      e += a[a.length - 1];
    }
    return e.replace(tagSpaceRe, '><').trim();
  }
  static repeat(a) {
    return function (...b) {
      return _TemplateHelper.tpl(...b).repeat(a);
    };
  }
  static loop(a) {
  }
};
TemplateHelper._$name = 'TemplateHelper';
const TimeZoneOffsetInfo = class extends Array {
  constructor(a, b) {
    super();
    this.timeZone = a;
    this.year = b;
  }
};
const TimeZoneDate = class {
  constructor(a, b) {
    this.asString = a;
    this.timeZone = b;
    this.asArray = parseStringDate(a);
  }
  get asLocalDate() {
    if (!this._asLocalDate) {
      this._asLocalDate = new Date(...this.asArray);
    }
    return this._asLocalDate;
  }
  get asTicksUtc() {
    if (!this._asTicksUtc) {
      this._asTicksUtc = new Date(Date.UTC(...this.asArray)).getTime();
    }
    return this._asTicksUtc;
  }
};
const toTimeZoneInternal = (a, b) => {
  const c = a.toLocaleString('sv-SE', { 'timeZone': b });
  return new TimeZoneDate(c, b);
};
const getOffsetUtc = (a, b) => {
  const {
    asTicksUtc: c,
    asString: e
  } = toTimeZoneInternal(new Date(a), b);
  return {
    'offset': (a - c) / 60000,
    'tzTicksUtc': c,
    'tzString': e
  };
};
var parseStringDate = a => {
  const b = a.split(/[\s-:]/).map(c => c * 1);
  b[1] -= 1;
  return b;
};
const min = -60000;
const hour = 3600000;
const day = -86400000;
const month = 2592000000;
const findOffsetDates = (a, b) => {
  const c = offsetDateCache.get(a, b);
  if (c) {
    return c;
  }
  const e = new TimeZoneOffsetInfo(a, b), f = new Date(Date.UTC(b, 0, 1)).getTime();
  let g = f, i = month, j = 0, k = [], l = null, n, o, p, q;
  while (!q) {
    ({
      offset: p,
      tzTicksUtc: n,
      tzString: o
    } = getOffsetUtc(g, a));
    if (l == null) {
      l = p;
      e.push({ 'offset': p });
    } else if (i > 0 && p !== l) {
      i = i === month ? day : min;
    } else if (i < 0 && p === l) {
      if (i === day) {
        i = hour;
      } else {
        if (e.length === 1) {
          e.push(k);
          l = k.offset;
        } else {
          e[0].startTicks = k.startTicks;
          e[0].startDateString = k.startDateString;
          q = true;
        }
        i = month;
      }
    }
    k = {
      'offset': p,
      'startDateString': o,
      'startTicks': n
    };
    if (i === month) {
      g = f;
      g += j * month;
      j += 1;
    }
    g += i;
    if (j > 11) {
      q = true;
    }
  }
  offsetDateCache.set(e);
  return e;
};
var offsetDateCache = {
  '_cache': {},
  'get'(a, b) {
    let c;
    return (c = this._cache[a]) == null ? void 0 : c[b];
  },
  'set'(a) {
    const {timeZone: b} = a, {_cache: c} = this;
    if (!c[b]) {
      c[b] = {};
    }
    c[b][a.year] = a;
  }
};
const TimeZoneHelper = class {
  static $name = 'TimeZoneHelper';
  static toTimeZone(a, b) {
    if (typeof b === 'number') {
      return this.toUtcOffset(a, b);
    }
    const c = toTimeZoneInternal(a, b);
    if (c.asArray[3] !== c.asLocalDate.getHours()) {
      console.warn('Incorrect time zone conversion due to local DST-switch detected');
    }
    return c.asLocalDate;
  }
  static fromTimeZone(a, b) {
    if (typeof b === 'number') {
      return this.fromUtcOffset(a, b);
    }
    const c = this.dateAsArray(a), e = findOffsetDates(b, a.getUTCFullYear());
    let f = e[0].offset;
    if (e.length === 2) {
      const g = Date.UTC(...c);
      if (g >= e[1].startTicks && g < e[0].startTicks) {
        f = e[1].offset;
      }
    }
    c[4] += f;
    return new Date(Date.UTC(...c));
  }
  static toUtcOffset(a, b) {
    const c = a.getTimezoneOffset() + b;
    return new Date(a.getTime() + c * 60 * 1000);
  }
  static fromUtcOffset(a, b) {
    const c = -a.getTimezoneOffset() - b;
    return new Date(a.getTime() + c * 60 * 1000);
  }
  static dateAsArray(a) {
    return [
      a.getFullYear(),
      a.getMonth(),
      a.getDate(),
      a.getHours(),
      a.getMinutes(),
      a.getSeconds(),
      a.getMilliseconds()
    ];
  }
};
TimeZoneHelper.findOffsetDates = findOffsetDates;
Helpers_default.register(TimeZoneHelper);
TimeZoneHelper._$name = 'TimeZoneHelper';
const _Toast = class _Toast extends Widget {
  static toasts = {
    'top-start': [],
    'top-end': [],
    'bottom-start': [],
    'bottom-end': []
  };
  static type = 'toast';
  static $name = 'Toast';
  static get configurable() {
    return {
      'testConfig': {
        'destroyTimeout': 1,
        'timeout': 1000
      },
      'floating': true,
      'timeout': 2500,
      'autoDestroy': null,
      'destroyTimeout': 200,
      'showProgress': true,
      'color': null,
      'sideMargin': 20,
      'side': 'bottom',
      'role': 'alert'
    };
  }
  changeSide(a) {
    if (a == 'top' || a == 'bottom') {
      a += '-end';
    } else if (a === 'start' || a == 'end') {
      a = 'bottom-' + a;
    }
    return a;
  }
  compose() {
    const {
      appendTo: a,
      color: b,
      html: c,
      showProgress: e,
      style: f,
      timeout: g,
      side: i
    } = this;
    return {
      'parent': a || this.floatRoot,
      'class': {
        ...DomClassList.normalize(b, 'object'),
        ['b-side-' + i]: 1,
        'b-toast-hide': 1
      },
      'html': c,
      'style': {
        ...DomHelper.parseStyle(f),
        '--side': i.split('-')[0]
      },
      'children': {
        'progressElement': e && {
          'style': 'animation-duration:' + g / 1000 + 's;',
          'class': { 'b-toast-progress': 1 }
        }
      },
      'listeners': { 'click': 'hide' }
    };
  }
  doDestroy() {
    this.untoast();
    super.doDestroy();
  }
  getNextInset(a) {
    const {
      sideMargin: b,
      element: c
    } = this;
    return parseInt(c.style[a], 10) + c.offsetHeight + b;
  }
  async show() {
    let a, b;
    await super.show(...arguments);
    const c = this, {
        element: e,
        side: f
      } = c, g = _Toast.toasts[f], i = f.split('-')[0];
    if (!g.includes(c)) {
      e.style[i] = ((b = (a = g[0]) == null ? void 0 : a.getNextInset(i)) != null ? b : c.sideMargin) + 'px';
      g.unshift(c);
      e.getBoundingClientRect();
      e.classList.remove('b-toast-hide');
      if (c.timeout > 0) {
        c.hideTimeout = c.setTimeout('hide', c.timeout);
      }
      DomHelper.addAttributeValue(document.body, 'aria-describedby', c.id);
    }
  }
  async hide() {
    const a = this;
    a.untoast();
    a.element.classList.add('b-toast-hide');
    if (a.autoDestroy && !a.destroyTimer) {
      a.destroyTimer = a.setTimeout('destroy', a.destroyTimeout);
    }
  }
  untoast() {
    const a = _Toast.toasts[this.side];
    if (a.includes(this)) {
      a.splice(a.indexOf(this), 1);
    }
    DomHelper.removeAttributeValue(document.body, 'aria-describedby', this.id);
  }
  static hideAll() {
    Object.keys(_Toast.toasts).forEach(a => _Toast.toasts[a].slice().reverse().forEach(b => b.hide()));
  }
  static show(a) {
    const b = _Toast.new({
      'autoDestroy': true,
      'rootElement': document.body
    }, typeof a === 'string' ? { 'html': a } : a);
    b.show();
    return b;
  }
};
const Toast = _Toast;
Toast.initClass();
Toast._$name = 'Toast';
const WidgetHelper = class {
  static getById(a) {
    VersionHelper.deprecate('core', '7.0.0', 'Use Widget.getById instead');
    return Widget.getById(a);
  }
  static fromElement(a, b, c) {
    VersionHelper.deprecate('core', '7.0.0', 'Use Widget.fromElement instead');
    return Widget.fromElement(a, b, c);
  }
  static createWidget(a = {}) {
    VersionHelper.deprecate('core', '7.0.0', 'Use Widget.create instead');
    return a.isWidget ? a : Widget.create(a);
  }
  static append(a, b) {
    VersionHelper.deprecate('core', '7.0.0', 'Use a Container instead');
    a = Array.isArray(a) && a || [a];
    if (b instanceof HTMLElement || typeof b === 'string') {
      b = { 'appendTo': b };
    }
    if (b.insertFirst) {
      const c = typeof b.insertFirst === 'string' ? document.getElementById(b.insertFirst) : b.insertFirst;
      if (c.firstChild) {
        b.insertBefore = c.firstChild;
      } else {
        b.appendTo = c;
      }
    }
    return a.map(e => Widget.create(ObjectHelper.assign({}, b || {}, e)));
  }
  static openPopup(a, b) {
    VersionHelper.deprecate('core', '7.0.0', 'Use Popup directly instead');
    return Widget.create(ObjectHelper.assign({ 'forElement': a }, typeof b === 'string' ? { 'html': b } : b), 'popup');
  }
  static showContextMenu(a, b) {
    VersionHelper.deprecate('core', '7.0.0', 'Use Menu directly instead');
    const c = this;
    if (c.currentContextMenu) {
      c.currentContextMenu.destroy();
    }
    if (a instanceof HTMLElement) {
      b.forElement = a;
    } else {
      b.forElement = document.body;
      if (Array.isArray(a)) {
        a = new Point(...a);
      }
      if (a instanceof Point) {
        b.align = { 'position': a };
      }
    }
    b.internalListeners = { 'destroy': c.currentContextMenu = null };
    return c.currentContextMenu = Widget.create(b, 'menu');
  }
  static attachTooltip(a, b) {
    VersionHelper.deprecate('core', '7.0.0', 'Use Tooltip directly instead');
    return Widget.attachTooltip(a, b);
  }
  static hasTooltipAttached(a) {
    VersionHelper.deprecate('core', '7.0.0', 'Will be removed');
    return Widget.resolveType('tooltip').hasTooltipAttached(a);
  }
  static destroyTooltipAttached(a) {
    VersionHelper.deprecate('core', '7.0.0', 'Will be removed');
    return Widget.resolveType('tooltip').destroyTooltipAttached(a);
  }
  static mask(a, b = 'Loading') {
    VersionHelper.deprecate('core', '7.0.0', 'Use Mask directly instead');
    if (a) {
      if (a instanceof HTMLElement) {
        a = {
          'target': a,
          'text': b
        };
      }
      return Mask.mask(a, a.target);
    }
  }
  static unmask(a, b = true) {
    VersionHelper.deprecate('core', '7.0.0', 'Use Mask directly instead');
    if (a.mask) {
      if (b) {
        a.mask.close();
      } else {
        a.mask.hide();
      }
    }
  }
  static toast(a) {
    VersionHelper.deprecate('core', '7.0.0', 'Use Toast directly instead');
    return Toast.show(a);
  }
};
WidgetHelper._$name = 'WidgetHelper';
const XMLHelper = class {
  static convertFromObject(a, b = {}) {
    Objects.assignIf(b, {
      'rootName': 'root',
      'elementName': 'element',
      'includeHeader': true,
      'rootElementForArray': true
    });
    const {
      rootName: c,
      elementName: e,
      includeHeader: f,
      rootElementForArray: g
    } = b;
    let {xmlns: i} = b;
    i = i ? ' xmlns="' + i + '"' : '';
    const j = f ? '<?xml version="1.0" encoding="UTF-8" standalone="yes"?>' : '';
    const k = l => {
      const n = [];
      for (const p in l) {
        const q = l[p];
        if (Array.isArray(q)) {
          if (g) {
            n.push('<' + p + '>');
          }
          for (const r of q) {
            if (e.length) {
              n.push('<' + e + '>');
            } else {
              n.push('<' + p + '>');
            }
            n.push(k(r));
            if (e.length) {
              n.push('</' + e + '>');
            } else {
              n.push('</' + p + '>');
            }
          }
          if (g) {
            n.push('</' + p + '>');
          }
        } else if (Objects.isObject(q)) {
          n.push('<' + p + '>' + k(q) + '</' + p + '>');
        } else {
          if (q == null) {
            n.push('<' + p + '/>');
          } else {
            n.push('<' + p + '>' + StringHelper.encodeHtml(q) + '</' + p + '>');
          }
        }
      }
      return n.join('');
    };
    return j + '<' + c + i + '>' + k(a) + '</' + c + '>';
  }
};
XMLHelper._$name = 'XMLHelper';
const RandomGenerator = class {
  constructor() {
    this.random100 = [
      46,
      2,
      36,
      46,
      54,
      59,
      18,
      20,
      71,
      55,
      88,
      98,
      13,
      61,
      61,
      40,
      2,
      15,
      3,
      32,
      51,
      45,
      64,
      25,
      81,
      85,
      54,
      13,
      57,
      49,
      64,
      22,
      81,
      94,
      0,
      62,
      17,
      7,
      11,
      2,
      33,
      99,
      85,
      26,
      83,
      83,
      96,
      26,
      20,
      89,
      91,
      38,
      26,
      13,
      11,
      79,
      32,
      30,
      5,
      51,
      70,
      7,
      5,
      56,
      58,
      77,
      37,
      89,
      40,
      80,
      78,
      59,
      26,
      36,
      8,
      51,
      60,
      23,
      86,
      5,
      11,
      96,
      64,
      94,
      87,
      64,
      4,
      78,
      17,
      85,
      35,
      0,
      90,
      86,
      23,
      55,
      53,
      9,
      35,
      59,
      29,
      2,
      64,
      42,
      8,
      49,
      43,
      73,
      6,
      53,
      38,
      9,
      39,
      31,
      32,
      40,
      49,
      13,
      78,
      68,
      20,
      99,
      24,
      78,
      35,
      91,
      73,
      46,
      67,
      76,
      89,
      69,
      30,
      69,
      25,
      3,
      4,
      55,
      1,
      65,
      66,
      76,
      83,
      19,
      67,
      1,
      95,
      24,
      54,
      45,
      56,
      40,
      67,
      92,
      72,
      4,
      69,
      8,
      47,
      50,
      27,
      2,
      38,
      9,
      14,
      83,
      12,
      14,
      62,
      95,
      22,
      47,
      35,
      18,
      38,
      14,
      86,
      64,
      68,
      61,
      52,
      69,
      39,
      93,
      20,
      73,
      32,
      52,
      74,
      6,
      56,
      68,
      99,
      29,
      24,
      92,
      40,
      67,
      6,
      72,
      31,
      41,
      91,
      53,
      80,
      55,
      33,
      97,
      97,
      99,
      18,
      20,
      5,
      27,
      82,
      84,
      61,
      78,
      27,
      67,
      7,
      42,
      75,
      95,
      91,
      25,
      63,
      21,
      70,
      36,
      46,
      0,
      1,
      45,
      84,
      6,
      86,
      15,
      10,
      62,
      96,
      94,
      10,
      23,
      93,
      83,
      94,
      47,
      5,
      29,
      29,
      52,
      51,
      37,
      77,
      96,
      43,
      72,
      43,
      14,
      54,
      14,
      72,
      52,
      4,
      39,
      15,
      26,
      68,
      28,
      25,
      76,
      60,
      50,
      22,
      40,
      72,
      74,
      68,
      58,
      8,
      48,
      40,
      62,
      52,
      24,
      9,
      26,
      47,
      44,
      49,
      96,
      7,
      77,
      90,
      45,
      76,
      47,
      5,
      86,
      1,
      36,
      18,
      42,
      19,
      90,
      34,
      23,
      70,
      32,
      69,
      79,
      0,
      99,
      57,
      80,
      72,
      21,
      19,
      72,
      85,
      68,
      4,
      40,
      86,
      62,
      0,
      63,
      4,
      11,
      69,
      31,
      78,
      31,
      21,
      78,
      29,
      84,
      13,
      53,
      57,
      10,
      26,
      50,
      24,
      30,
      90,
      42,
      51,
      96,
      93,
      21,
      99,
      23,
      81,
      0,
      89,
      43,
      86,
      63,
      93,
      19,
      54,
      71,
      92,
      36,
      4,
      95,
      37,
      99,
      60,
      29,
      23,
      50,
      68,
      95,
      57,
      95,
      77,
      53,
      99,
      78,
      75,
      12,
      92,
      47,
      23,
      14,
      0,
      41,
      98,
      11,
      34,
      64,
      26,
      90,
      50,
      23,
      38,
      31,
      74,
      76,
      16,
      76,
      66,
      23,
      22,
      72,
      48,
      50,
      20,
      36,
      37,
      58,
      5,
      43,
      49,
      64,
      81,
      30,
      8,
      21,
      98,
      75,
      60,
      17,
      50,
      42,
      27,
      38,
      90,
      74,
      45,
      68,
      67,
      27,
      31,
      15,
      58,
      76,
      41,
      99,
      23,
      98,
      53,
      98,
      56,
      19,
      79,
      2,
      4,
      38,
      96,
      24,
      65,
      51,
      43,
      42,
      41,
      60,
      46,
      7,
      90,
      65,
      3,
      27,
      63,
      99,
      51,
      44,
      86,
      1,
      54,
      40,
      15,
      74,
      3,
      81,
      51,
      63,
      87,
      79,
      84,
      72,
      22,
      38,
      96,
      95,
      33,
      41,
      21,
      99,
      21,
      69,
      7,
      49,
      40,
      52,
      41,
      6,
      91,
      19,
      76,
      40,
      54,
      17,
      33,
      11,
      11,
      0,
      1,
      32,
      94,
      33,
      13,
      18,
      45,
      7,
      85,
      61,
      42,
      54,
      45,
      72,
      78,
      96,
      17,
      9,
      80,
      87,
      41,
      96,
      66,
      0,
      8,
      59,
      18,
      21,
      2,
      28,
      64,
      75,
      97,
      32,
      80,
      86,
      97,
      97,
      55,
      2,
      73,
      75,
      11,
      89,
      67,
      58,
      70,
      76,
      12,
      46,
      64,
      17,
      22,
      97,
      25,
      35,
      93,
      57,
      82,
      46,
      57,
      61,
      31,
      74,
      27,
      4,
      32,
      85,
      53,
      86,
      53,
      53,
      42,
      5,
      28,
      50,
      65,
      63,
      70,
      61,
      73,
      37,
      13,
      80,
      7,
      34,
      22,
      3,
      26,
      6,
      62,
      78,
      12,
      56,
      87,
      41,
      58,
      64,
      31,
      27,
      45,
      35,
      18,
      66,
      62,
      43,
      89,
      69,
      94,
      93,
      33,
      74,
      2,
      43,
      85,
      37,
      82,
      41,
      74,
      9,
      15,
      44,
      33,
      42,
      65,
      19,
      1,
      49,
      78,
      12,
      29,
      9,
      78,
      7,
      55,
      12,
      45,
      40,
      33,
      16,
      86,
      14,
      52,
      16,
      73,
      76,
      0,
      98,
      75,
      91,
      78,
      46,
      99,
      95,
      90,
      69,
      78,
      45,
      62,
      55,
      37,
      88,
      49,
      77,
      27,
      83,
      38,
      73,
      39,
      1,
      75,
      40,
      65,
      83,
      54,
      95,
      7,
      73,
      4,
      30,
      26,
      36,
      89,
      21,
      5,
      95,
      11,
      14,
      87,
      45,
      36,
      21,
      77,
      55,
      5,
      66,
      51,
      98,
      48,
      62,
      74,
      58,
      23,
      82,
      30,
      28,
      19,
      53,
      89,
      76,
      98,
      8,
      34,
      70,
      28,
      54,
      16,
      52,
      35,
      93,
      54,
      54,
      72,
      49,
      18,
      93,
      72,
      90,
      71,
      73,
      15,
      60,
      38,
      80,
      76,
      53,
      70,
      39,
      69,
      25,
      5,
      31,
      61,
      46,
      6,
      54,
      34,
      31,
      52,
      33,
      36,
      79,
      76,
      44,
      29,
      28,
      38,
      1,
      66,
      2,
      90,
      91,
      1,
      76,
      78,
      31,
      55,
      37,
      71,
      2,
      3,
      38,
      85,
      0,
      95,
      42,
      2,
      39,
      57,
      87,
      61,
      77,
      98,
      2,
      24,
      80,
      48,
      27,
      47,
      71,
      15,
      7,
      49,
      60,
      86,
      3,
      2,
      29,
      38,
      54,
      36,
      59,
      83,
      27,
      47,
      9,
      36,
      42,
      8,
      73,
      85,
      9,
      16,
      73,
      60,
      39,
      12,
      43,
      25,
      23,
      29,
      28,
      47,
      40,
      77,
      20,
      89,
      22,
      30,
      41,
      59,
      96,
      19,
      56,
      20,
      76,
      73,
      39,
      46,
      72,
      40,
      47,
      37,
      52,
      29,
      79,
      37,
      39,
      50,
      41,
      87,
      66,
      17,
      75,
      31,
      45,
      26,
      88,
      70,
      11,
      90,
      40,
      74,
      9,
      32,
      65,
      72,
      61,
      6,
      93,
      54,
      15,
      84,
      22,
      99,
      47,
      10,
      96,
      4,
      84,
      19,
      85,
      73,
      45,
      25,
      16,
      8,
      94,
      99,
      39,
      28,
      26,
      68,
      87,
      48,
      1,
      65,
      86,
      46,
      86,
      7,
      60,
      82,
      45,
      75,
      38,
      56,
      41,
      35,
      30,
      86,
      91,
      97,
      85,
      45,
      5,
      14,
      69,
      85,
      96,
      37,
      18,
      26,
      16,
      38,
      16,
      1,
      44,
      94,
      85,
      58,
      60,
      20,
      5,
      47,
      52,
      41,
      50,
      71,
      43,
      42,
      67,
      64,
      38,
      65,
      83,
      99,
      78,
      96,
      33,
      20,
      98,
      24,
      6,
      2,
      25,
      16,
      16,
      44,
      63,
      24,
      68,
      56,
      49,
      91,
      15,
      59,
      99,
      27,
      43,
      34,
      28,
      36,
      45,
      1,
      10,
      19,
      54,
      26,
      75,
      17,
      88,
      96,
      63,
      24,
      71,
      93,
      72,
      97,
      66,
      87,
      18,
      86
    ];
    this.randomCache = {};
    this.rndIndex = 0;
  }
  nextRandom(a) {
    const {randomCache: b} = this;
    let c;
    if (b[a]) {
      c = b[a];
    } else {
      c = this.random100.filter(e => e < a);
      b[a] = c;
    }
    return c[this.rndIndex++ % c.length];
  }
  reset() {
    this.rndIndex = 0;
  }
  fromArray(a) {
    return a[this.nextRandom(a.length)];
  }
  randomArray(a, b) {
    const c = [], e = this.nextRandom(b + 1), f = {};
    for (let g = 0, j = this.nextRandom(a.length); g < e; g++) {
      while (f[j]) {
        j = this.nextRandom(a.length);
      }
      f[j] = true;
      c.push(a[j]);
    }
    return c;
  }
};
RandomGenerator._$name = 'RandomGenerator';
const lorem = [
  'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.',
  'Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.',
  'Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur.',
  'Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.',
  'Sed ut perspiciatis unde omnis iste natus error sit voluptatem accusantium doloremque laudantium, totam rem aperiam, eaque ipsa quae ab illo inventore veritatis et quasi architecto beatae vitae dicta sunt explicabo.',
  'Nemo enim ipsam voluptatem quia voluptas sit aspernatur aut odit aut fugit, sed quia consequuntur magni dolores eos qui atione voluptatem sequi nesciunt.',
  'Neque porro quisquam est, qui dolorem ipsum quia dolor sit amet, consectetur, adipisci velit, sed quia non numquam eius modi tempora incidunt ut labore et dolore magnam aliquam quaerat voluptatem.',
  'Ut enim ad minima veniam, quis nostrum exercitationem ullam corporis suscipit laboriosam, nisi ut aliquid ex ea commodi consequatur?',
  'Quis autem vel eum iure reprehenderit qui in ea voluptate velit esse quam nihil molestiae consequatur, vel illum qui dolorem eum fugiat quo voluptas nulla pariatur?'
];
const _DataGenerator = class _DataGenerator {
  static rndText = new RandomGenerator();
  static rndRating = new RandomGenerator();
  static rndTime = new RandomGenerator();
  static rnd = new RandomGenerator();
  static reset() {
    this.rnd.reset();
    this.rndTime.reset();
    this.rndRating.reset();
  }
  static get cities() {
    return [
      'Stockholm',
      'Barcelona',
      'Paris',
      'Dubai',
      'New York',
      'San Francisco',
      'Washington',
      'Montreal'
    ];
  }
  static get namesWithAvatars() {
    return [
      'Adam',
      'Amit',
      'Angelo',
      'Arcady',
      'Arnold',
      'Barbara',
      'Celia',
      'Chang',
      'Dan',
      'Daniel',
      'Dave',
      'david',
      'Don',
      'Doug',
      'Emilia',
      'George',
      'Gloria',
      'Henrik',
      'Hitomi',
      'James',
      'Jane',
      'Jenny',
      'John',
      'Jong',
      'Karen',
      'Kate',
      'Lee',
      'Linda',
      'Lisa',
      'Lola',
      'Macy',
      'Madison',
      'Malik',
      'Mark',
      'Mary',
      'Maxim',
      'Melissa',
      'Mike',
      'Rob',
      'Steve',
      'Team',
      'Theo'
    ];
  }
  static get firstNames() {
    return [
      'Mike',
      'Linda',
      'Don',
      'Karen',
      'Doug',
      'Jenny',
      'Daniel',
      'Melissa',
      'John',
      'Jane',
      'Theo',
      'Lisa',
      'Adam',
      'Mary',
      'Barbara',
      'James',
      'David'
    ];
  }
  static get surNames() {
    return [
      'McGregor',
      'Ewans',
      'Scott',
      'Smith',
      'Johnson',
      'Adams',
      'Williams',
      'Brown',
      'Jones',
      'Miller',
      'Davis',
      'More',
      'Wilson',
      'Taylor',
      'Anderson',
      'Thomas',
      'Jackson'
    ];
  }
  static get teams() {
    return [
      'Lions',
      'Eagles',
      'Tigers',
      'Horses',
      'Dogs',
      'Cats',
      'Panthers',
      'Rats',
      'Ducks',
      'Cougars',
      'Hens',
      'Roosters'
    ];
  }
  static get foods() {
    return [
      'Pancake',
      'Burger',
      'Fish n chips',
      'Carbonara',
      'Taco',
      'Salad',
      'Bolognese',
      'Mac n cheese',
      'Waffles'
    ];
  }
  static get colors() {
    return [
      'Blue',
      'Green',
      'Red',
      'Yellow',
      'Pink',
      'Purple',
      'Orange',
      'Teal',
      'Black'
    ];
  }
  static get skills() {
    return [
      'JavaScript',
      'CSS',
      'TypeScript',
      'React',
      'Vue',
      'Angular',
      'Java',
      'PHP',
      'Python',
      'C#',
      'C++',
      'BASIC',
      'COBOL',
      'FORTRAN',
      'PASCAL',
      'SQL'
    ];
  }
  static get tasks() {
    return [
      'Meetings',
      'Documentation',
      'Email communication',
      'Project management',
      'Budgeting',
      'Marketing and advertising',
      'Customer service',
      'Research and analysis',
      'Data entry',
      'IT support',
      'Employee management',
      'Sales and business development',
      'Event planning',
      'Graphic design',
      'Writing and editing',
      'Presentation',
      'Travel arrangements and expense management',
      'Training and development',
      'Quality assurance',
      'Customer support',
      'Technical writing',
      'Social media management',
      'Translation',
      'Legal research',
      'Data analysis and visualization',
      'Video editing and production',
      'Network admin',
      'Content creation',
      'Market research',
      'Public relations',
      'Teaching and training',
      'Recruiting',
      'Product development'
    ];
  }
  static generateName() {
    const {
        rnd: a,
        namesWithAvatars: b,
        surNames: c
      } = this, e = a.fromArray(b), f = a.fromArray(c);
    return e + ' ' + f;
  }
  static *generate(a, b = false, c = 1) {
    let e;
    let f, g;
    if (typeof a === 'object') {
      b = a.randomHeight;
      c = (e = a.initialId) != null ? e : 1;
      f = a.addSkills;
      g = a.rowCallback;
      a = a.count;
    }
    const j = this, {
        rnd: k,
        rndTime: l,
        rndRating: n,
        rndText: o,
        firstNames: p,
        surNames: q,
        teams: r,
        foods: u,
        colors: v,
        cities: w,
        skills: z
      } = j;
    for (let A = 0; A < a; A++) {
      const B = k.fromArray(p), C = k.fromArray(q), E = B + ' ' + String.fromCharCode(65 + A % 25) + ' ' + C, F = k.nextRandom(60) + 1, G = new Date(2019, 0, F), H = new Date(2019, 0, F + k.nextRandom(30) + 2), I = {
          'id': c > -1 ? A + c : void 0,
          'title': 'Row ' + A,
          'name': E,
          'firstName': B,
          'surName': C,
          'city': k.fromArray(w),
          'team': k.fromArray(w) + ' ' + k.fromArray(r),
          'age': 10 + k.nextRandom(80),
          'food': k.fromArray(u),
          'color': k.fromArray(v),
          'score': k.nextRandom(100) * 10,
          'rank': k.nextRandom(100) + 1,
          'start': G,
          'finish': H,
          'time': DateHelper.getTime(l.nextRandom(24), l.nextRandom(12) * 5),
          'percent': k.nextRandom(100),
          'done': k.nextRandom(100) < 50,
          'rating': n.nextRandom(5),
          'active': F > 30,
          'relatedTo': Math.min(a - 1, A + c + k.nextRandom(10)),
          'notes': lorem[o.nextRandom(7) + 1]
        };
      if (f) {
        I.skills = k.randomArray(z, typeof f === 'number' ? f : 7);
      }
      const J = g == null ? void 0 : g(I);
      J && Object.assign(I, J);
      if (b) {
        I.rowHeight = k.nextRandom(b === true ? 20 : b) * 5 + 20;
      }
      yield I;
    }
  }
  static generateData(a, b = false, c = 1, e = true) {
    let f = a;
    if (typeof a !== 'object') {
      f = {
        'count': a,
        'randomHeight': b,
        'initialId': c,
        'reset': e
      };
    }
    f.reset !== false && this.reset();
    if (_DataGenerator.overrideRowCount) {
      f.count = _DataGenerator.overrideRowCount;
    }
    const g = [], j = this.generate(f);
    for (let k = 0; k < f.count; k++) {
      g.push(j.next().value);
    }
    return g;
  }
  static generateEvents({
    viewStartDate: a,
    viewEndDate: b,
    nbrResources: nbrResources = 50,
    nbrEvents: nbrEvents = 5,
    dependencies: c,
    tickUnit: tickUnit = 'days',
    minDuration: minDuration = 2,
    maxDuration: maxDuration = 10,
    gap: gap = 1,
    eventCallback: e
  }) {
    const f = this.generateData(nbrResources), g = [];
    let k, l;
    c = [];
    for (let n = 0; n <= nbrResources; n++) {
      for (let o = 0; o <= nbrEvents; o++) {
        const p = Math.round(Math.random() * (maxDuration - 1 - minDuration)) + minDuration, q = g.length + 1;
        k = o === 0 ? DateHelper.add(a, Math.round(Math.random() * 3) * gap, tickUnit) : DateHelper.add(l, gap, tickUnit);
        l = DateHelper.add(k, p, tickUnit);
        g.push({
          'id': q,
          'name': this.tasks[(n + o) % (this.tasks.length - 1)],
          'startDate': k,
          'duration': p,
          'endDate': l,
          'resourceId': n
        });
        e == null ? void 0 : e(g[g.length - 1]);
        if (c && n > 0) {
          c.push({
            'id': c.length + 1,
            'from': q - 1,
            'to': q
          });
        }
      }
    }
    return {
      'resources': f,
      'events': g,
      'dependencies': c
    };
  }
  static generateOneEventPerTickAndResource({
    startDate: a,
    endDate: b,
    nbrResources: nbrResources = 50,
    tickUnit: tickUnit = 'days'
  }) {
    const c = this.generateData(nbrResources), e = [], f = DateHelper.getDurationInUnit(a, b, tickUnit);
    for (let g = 0; g <= nbrResources; g++) {
      for (let k = 0; k < f; k++) {
        e.push({
          'id': e.length + 1,
          'resourceId': g,
          'name': this.tasks[(g + k) % (this.tasks.length - 1)],
          'startDate': DateHelper.add(a, k, tickUnit),
          'duration': 1,
          'durationUnit': tickUnit
        });
      }
    }
    return {
      'resources': c,
      'events': e
    };
  }
  static generateRow() {
    return _DataGenerator.generateData(1, false, -1, false)[0];
  }
};
const DataGenerator = _DataGenerator;
DataGenerator._$name = 'DataGenerator';
const knownProps = [
  'action',
  'target',
  'to',
  'deltaX',
  'deltaY',
  'x',
  'y',
  'text'
];
const DemoBot = class extends Events_default(Delayable_default()) {
  static get defaultConfig() {
    return {
      'repeat': true,
      'outerElement': document.body,
      'callOnFunctions': true
    };
  }
  construct(a) {
    super.construct(a);
    const b = this;
    if (b.widget) {
      b.outerElement = b.widget.element;
      b.widget.playingDemo = true;
    }
    EventHelper.playingDemo = true;
    Object.assign(b, {
      'prevTarget': null,
      'currentStep': 0,
      'mouse': DomHelper.createElement({
        'parent': b.outerElement,
        'tag': 'div',
        'className': 'simulated-mouse'
      }),
      'timeoutId': null,
      'innerIntervalId': null,
      'mouseOutElements': []
    });
    b.intervalId = b.setInterval(b.nextStep.bind(b), 1000);
    b.outerElement.classList.add('b-playing-demo');
    b.outerElement.addEventListener('click', c => {
      if (c.isTrusted) {
        b.abort();
      }
    });
  }
  doDestroy() {
    this.abort();
  }
  abort(a = false) {
    const b = this;
    b.mouse.style.top = '-100px';
    b.clearInterval(b.intervalId);
    b.timeoutId && b.clearTimeout(b.timeoutId);
    b.innerIntervalId && b.clearInterval(b.innerIntervalId);
    b.outerElement.classList.remove('b-playing-demo');
    if (b.widget) {
      b.widget.playingDemo = false;
    }
    EventHelper.playingDemo = false;
    b.trigger(a ? 'done' : 'abort');
  }
  triggerEvent(a, b, c) {
    if (!a)
      return null;
    let e;
    if (b.startsWith('mouse')) {
      const f = this.mouse.getBoundingClientRect();
      e = new MouseEvent(b, Object.assign({
        'view': globalThis,
        'bubbles': true,
        'cancelable': true,
        'clientX': f.left,
        'clientY': f.top
      }, c || {}));
    } else {
      e = document.createEvent('Event');
      e.initEvent(b, true, false);
    }
    a.dispatchEvent(e);
    return e;
  }
  handleMouseMove(a, b) {
    const c = this, e = c.mouse;
    e.classList.add('quick');
    if (c.mouseDown)
      e.classList.add('drag');
    const f = Rectangle.from(e, c.outerElement), g = f.x, j = f.y;
    let k = 0, l = 0;
    if (a.to) {
      if (typeof a.to === 'string') {
        const o = c.outerElement.querySelector(a.to);
        if (o) {
          const p = Rectangle.from(o, c.outerElement), q = p.x + p.width / 2, r = p.y + p.height / 2;
          k = (q - g) / 10;
          l = (r - j) / 10;
        }
      } else if (a.to.x) {
        k = (a.to.x - g) / 10;
      } else {
        k = a.to[0] / 10;
        l = a.to[1] / 10;
      }
    } else if (a.deltaX) {
      k = a.deltaX / 10;
    } else if (a.x) {
      k = (a.x - g) / 10;
    }
    if (a.deltaY) {
      l = a.deltaY / 10;
    }
    let n = 0;
    c.innerIntervalId = c.setInterval(() => {
      if (c.shouldPause) {
        return;
      }
      if (n++ === 9) {
        clearInterval(c.innerIntervalId);
        if (a.then) {
          a.then();
        }
      }
      const u = g + k * n, v = j + l * n;
      e.style.left = u + 'px';
      e.style.top = v + 'px';
      const w = e.getBoundingClientRect(), z = w.left, A = w.top, B = DomHelper.elementFromPoint(z, A);
      if (B !== c.prevTarget) {
        if (c.prevTarget) {
          c.mouseOutElements.push(c.prevTarget);
          if (!DomHelper.isDescendant(c.mouseOutElements[0], B)) {
            c.mouseOutElements.forEach(C => c.triggerEvent(C, 'mouseout'));
            c.mouseOutElements.length = 0;
          }
        }
        c.prevTarget = B;
        c.triggerEvent(B, 'mouseover');
      }
      c.triggerEvent(B, a.action, {
        'clientX': z,
        'clientY': A
      });
    }, 50);
  }
  getTarget(a) {
    const b = this, c = a.target;
    if (!c) {
      return b.prevTarget || b.outerElement;
    }
    if (typeof c === 'function') {
      return c(a);
    }
    return document.querySelector(c);
  }
  normalizeStep(a) {
    if (a.action) {
      if (typeof a.action === 'function') {
        return a.action(a);
      }
      return a;
    }
    if (typeof a === 'function') {
      a();
      return a;
    }
    for (const b in a) {
      if (Object.hasOwnProperty.call(a, b) && !knownProps.includes(b)) {
        a.action = b.toLowerCase();
        a.to = a[b];
      }
    }
    if (!a.target && (typeof a.to === 'string' || typeof a.to === 'function'))
      a.target = a.to;
    return a;
  }
  get isScrolling() {
    const a = this, b = a.outerElement.getBoundingClientRect(), c = a.lastTop && b.top !== a.lastTop;
    a.lastTop = b.top;
    return c;
  }
  get isInView() {
    const a = this.outerElement.getBoundingClientRect();
    return a.top < globalThis.innerHeight && a.bottom > 0;
  }
  get shouldPause() {
    return !this.isInView || this.isScrolling || document.hidden || !document.hasFocus();
  }
  nextStep() {
    const a = this;
    if (a.shouldPause) {
      return;
    }
    if (a.currentStep === a.steps.length) {
      if (a.repeat) {
        a.currentStep = 0;
      } else {
        return a.abort(true);
      }
    }
    if (a.currentStep === 0) {
      a.trigger('initialize');
    }
    const b = a.mouse, c = a.normalizeStep(a.steps[a.currentStep++]), e = a.getTarget(c), f = c.action;
    if (e && f) {
      b.className = 'simulated-mouse';
      if (f === 'mousemove') {
        a.handleMouseMove(c, e);
      } else {
        if (e !== a.prevTarget) {
          const g = Rectangle.from(e, a.outerElement);
          b.style.left = g.x + g.width / 2 + 'px';
          b.style.top = g.y + g.height / 2 + 'px';
        }
        if (f === 'mousedown') {
          a.mouseDown = true;
        }
        if (f === 'mouseup') {
          a.mouseDown = false;
        }
        a.timeoutId = a.setTimeout(() => {
          a.prevTarget = e;
          b.classList.add(f);
          if (f === 'type') {
            const i = Widget.fromElement(e), j = c.text.split('|');
            i.value = j[j.length === 1 || i.value != j[0] ? 0 : 1];
          } else {
            a.triggerEvent(e, f);
          }
        }, f === 'type' ? 100 : 550);
      }
    }
  }
};
DemoBot._$name = 'DemoBot';
const {defineProperty: defineProperty7} = Object;
const fencibleSymbol = Symbol('fencible');
const NONE = [];
const distinct = a => Array.from(new Set(a));
const parseNames = a => a ? distinct(StringHelper.split(a)) : NONE;
const Fencible_default = a => {
  let b;
  return b = class extends (a || Base) {
    static setupFenced(c, e) {
      const {fenced: f} = c, g = e.getInherited('fenced');
      let i, j, k, l, n;
      for (l in f) {
        n = f[l];
        if (n === true) {
          n = l;
        }
        if (!ObjectHelper.isObject(n)) {
          n = { 'all': n };
        }
        i = parseNames(n.all);
        j = parseNames(n.any);
        k = n.lock ? parseNames(n.lock) : distinct(i.concat(j));
        j = j.length ? j : null;
        g[l] = {
          'any': j,
          'all': i,
          'lock': k
        };
      }
    }
    construct(...c) {
      const e = this, f = Object.getPrototypeOf(e), g = e.$meta.getInherited('fenced'), i = e[fencibleSymbol] = {}, j = k => !i[k];
      ObjectHelper.forEach(g, ({
        any: k,
        all: l,
        lock: n
      }, o) => {
        defineProperty7(e, o, {
          'configurable': true,
          'value'(...p) {
            if (l.every(j) && (!k || k.some(j))) {
              let q;
              try {
                for (q of n) {
                  i[q] = (i[q] || 0) + 1;
                }
                return f[o].apply(e, p);
              } finally {
                for (q of n) {
                  --i[q];
                }
              }
            }
          }
        });
      });
      return super.construct(...c);
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'Fencible'), __publicField(b, 'declarable', ['fenced']), b;
};
const {
  floor,
  max,
  min: min2
} = Math;
const Axis = class extends Base.mixin(Fencible_default) {
  static fenced = { 'sync': true };
  static configurable = {
    'owner': null,
    'drift': null,
    'overflow': null,
    'pos': {
      'value': null,
      'default': 0,
      '$config': 'lazy'
    },
    'range': {
      'value': null,
      '$config': 'lazy'
    },
    'scrollSize': null
  };
  get direction() {
    const {name: a} = this, {
        events: b,
        scrollStarted: c
      } = this.owner;
    let e = 0, f, g, j;
    for (f = b.length; f-- > 0;) {
      j = b[f][a];
      if (g != null) {
        e = g - j;
        if (e) {
          break;
        }
      }
      g = j;
    }
    if (!e && g != null) {
      e = g - c[a];
    }
    return e;
  }
  get drift() {
    return this._drift || 0;
  }
  get driftRange() {
    const {range: a} = this;
    return [
      a[0],
      max(a[0], a[1] - this.scrollSize)
    ];
  }
  get items() {
    let a;
    return (a = this.owner) == null ? void 0 : a.items;
  }
  get safeZone() {
    const {
        drift: a,
        scrollSize: b
      } = this, c = floor(this.owner.safetyMargin * b);
    return [
      a + c,
      a + b - c
    ];
  }
  get scroller() {
    let a, b;
    return (b = (a = this.owner) == null ? void 0 : a.client) == null ? void 0 : b.scrollable;
  }
  get scrollLimit() {
    let a;
    return (a = this.owner) == null ? void 0 : a.scrollLimit;
  }
  get vast() {
    const {range: a} = this;
    return a ? a[1] - a[0] > this.scrollSize : false;
  }
  changePos(a) {
    const {range: b} = this;
    return min2(max(a || 0, b[0]), b[1]);
  }
  updatePos(a) {
    this.sync({ 'pos': a });
  }
  updateDrift(a) {
    const b = this, {
        pos: c,
        range: e
      } = b, f = c - a;
    if (b.scrollPos === f) {
      const g = new CustomEvent('scroll', { 'detail': 'driftChange' });
      b.owner.scroller.element.dispatchEvent(g);
    } else if (e[0] <= c && c < e[1]) {
      b.scrollPos = f;
    }
  }
  changeRange(a, b) {
    const [c, e] = Array.isArray(a) ? a : [
        0,
        a
      ], f = [
        c || 0,
        e == null ? Infinity : e
      ];
    if (!b || f[0] !== b[0] || f[1] !== b[1]) {
      return f;
    }
  }
  updateRange(a) {
    this.scrollSize = a[1] - a[0];
    this.sync({ 'range': a });
  }
  changeScrollSize(a) {
    return min2(a, this.scrollLimit);
  }
  snap() {
    const a = this, {
        direction: b,
        items: c,
        owner: e,
        pos: f,
        range: g
      } = a;
    let i, j, k, l;
    for (i of c) {
      [j, k] = a.getItemSpan(i);
      if (j <= f && f < k) {
        if (j < f) {
          l = b < 0 && j >= g[0] ? j : k;
          a.pos = l;
          e.watchForEnd();
        } else {
          e.scrolling = false;
        }
        break;
      }
    }
  }
  sync(a) {
    const b = Object.assign(this, a), {
        _drift: c,
        range: e,
        vast: f
      } = b;
    if (e && e[0] !== e[1]) {
      let {pos: g} = b, i;
      const j = g < e[0] || g >= e[1];
      if (g == null || j) {
        g = e[1] < 0 ? e[0] : min2(max(0, e[0]), e[1]);
      }
      if (!f) {
        i = e[0];
      } else {
        const {
          driftRange: k,
          safeZone: l,
          scrollSize: n
        } = b;
        if (c == null || g < l[0] || g > l[1]) {
          i = min2(max(k[0], floor(g - n / 2)), k[1]);
        } else {
          i = c;
        }
      }
      b.scroller.positionDirty = true;
      b.overflow = 'hidden-scroll';
      b.drift = i;
      if (i !== c) {
        if (j) {
          b.positionDirty = false;
          b._pos = null;
          b.pos = g;
        } else {
          b.positionDirty = true;
        }
      } else if ((a == null ? void 0 : a.pos) != null) {
        b.scrollPos = g - i;
        b.positionDirty = true;
      } else {
        if (b.positionDirty) {
          b.positionDirty = false;
          b._pos = null;
        }
        b.pos = b.scrollPos + i;
      }
      if ((a == null ? void 0 : a.range) || i !== c) {
        b.owner.syncItems();
      }
    } else {
      b.overflow = 'hidden';
    }
  }
  toLogical(a) {
    return a + this.drift;
  }
  toPhysical(a) {
    const b = a - this.drift;
    return b >= 0 && b < this.scrollLimit ? b : NaN;
  }
};
const XAxis = class extends Axis {
  get name() {
    return 'x';
  }
  get other() {
    let a;
    return (a = this.owner) == null ? void 0 : a.y;
  }
  get scrollPos() {
    return this.scroller.x;
  }
  set scrollPos(a) {
    this.scroller.x = a;
  }
  get scrollSize() {
    return this.scroller.scrollWidth;
  }
  set scrollSize(a) {
    super.scrollSize = a;
  }
  getItemSpan(a) {
    const b = a.$vxy, {x: c} = b;
    return [
      c,
      c + b.width
    ];
  }
  updateScrollSize(a) {
    this.scroller.scrollWidth = a;
  }
  updateOverflow(a) {
    this.scroller.overflowX = a;
  }
};
const YAxis = class extends Axis {
  get name() {
    return 'y';
  }
  get other() {
    let a;
    return (a = this.owner) == null ? void 0 : a.x;
  }
  get scrollPos() {
    return this.scroller.y;
  }
  set scrollPos(a) {
    this.scroller.y = a;
  }
  get scrollSize() {
    return this.scroller.scrollHeight;
  }
  set scrollSize(a) {
    super.scrollSize = a;
  }
  getItemSpan(a) {
    const b = a.$vxy, {y: c} = b;
    return [
      c,
      c + b.height
    ];
  }
  updateScrollSize(a) {
    this.scroller.scrollHeight = a;
  }
  updateOverflow(a) {
    this.scroller.overflowY = a;
  }
};
Axis.X = XAxis;
Axis.Y = YAxis;
Axis._$name = 'Axis';
const infinityCls = 'b-infinity-scroller';
const NO_SIZE = Object.freeze({
  'width': 0,
  'height': 0
});
const addVirtualPos = (a, b) => {
  const c = 'v' + b;
  if (!(c in a)) {
    Object.defineProperty(a, c, {
      'configurable': true,
      'get'() {
        let e;
        return (e = this.$vxy) == null ? void 0 : e[b];
      },
      'set'(e) {
        if (this.$vxy) {
          this.$vxy[b] = e;
        }
      }
    });
  }
};
const Virtualizer = class extends Base {
  static prototypeProperties = {
    '_height': 0,
    '_width': 0,
    '_x': 0,
    '_y': 0
  };
  static configurable = {
    'hidden': null,
    'owner': null,
    'widget': null
  };
  get height() {
    return this.getDim('height');
  }
  get width() {
    return this.getDim('width');
  }
  get x() {
    return this._x;
  }
  set x(a) {
    this.moveTo(a);
  }
  get xy() {
    return [
      this._x,
      this._y
    ];
  }
  set xy(a) {
    this.moveTo(...a);
  }
  get y() {
    return this._y;
  }
  set y(a) {
    this.moveTo(null, a);
  }
  getDim(a) {
    const b = this, {widget: c} = b, e = '_' + a;
    return c ? c.monitorResize ? c[a] : b[e] || (b[e] = c[a]) : 0;
  }
  moveTo(a, b) {
    if (a != null) {
      this._x = a;
    }
    if (b != null) {
      this._y = b;
    }
    this.sync();
  }
  sync() {
    const a = this, {
        owner: b,
        widget: c
      } = a, {element: e} = c, f = b.x.toPhysical(a.x), g = b.y.toPhysical(a.y), i = isNaN(f + g);
    a.hidden = i;
    if (i) {
      a.size = NO_SIZE;
    } else {
      e.style.transform = 'translate(' + f + 'px, ' + g + 'px)';
    }
  }
  updateHidden(a) {
    let b, c;
    (c = (b = this.widget) == null ? void 0 : b.element) == null ? void 0 : c.classList.toggle('b-scroll-hidden', a);
  }
  updateWidget(a, b) {
    b == null ? void 0 : b.element.classList.remove('b-infinity-scroller-item');
    if (a) {
      a.element.classList.add('b-infinity-scroller-item');
      addVirtualPos(a, 'x');
      addVirtualPos(a, 'y');
      addVirtualPos(a, 'xy');
    }
  }
};
const InfinityScroller = class extends Base.mixin(Events_default) {
  static configurable = {
    'animate': 'smooth',
    'client': null,
    'safetyMargin': 0.01,
    'scroller': null,
    'scrollIdle': 8,
    'scrolling': null,
    'scrollLimit': {
      'value': 0,
      '$config': 'lazy'
    },
    'snap': null,
    'x': {},
    'y': {}
  };
  get element() {
    let a;
    return (a = this.scroller) == null ? void 0 : a.element;
  }
  get events() {
    return this._events || (this._events = []);
  }
  get items() {
    let a;
    return (((a = this.client) == null ? void 0 : a.items) || []).filter(b => b.$vxy);
  }
  get lastEvent() {
    let a;
    return (a = this._events) == null ? void 0 : a[this._events.length - 1];
  }
  get pos() {
    return [
      this.x.pos,
      this.y.pos
    ];
  }
  changeScrollLimit(a) {
    return a || DomHelper.scrollLimit;
  }
  updateAnimate(a, b) {
    let c;
    const e = (c = this.element) == null ? void 0 : c.classList;
    b && (e == null ? void 0 : e.remove('b-infinity-scroller-' + b));
    a && (e == null ? void 0 : e.add('b-infinity-scroller-' + a));
  }
  updateClient(a) {
    this.scroller = (a == null ? void 0 : a.scrollable) || null;
  }
  updateScroller(a, b) {
    let c;
    this.detachListeners('scroller');
    (c = b == null ? void 0 : b.element) == null ? void 0 : c.classList.remove(infinityCls);
    if (a) {
      a.element.classList.add(infinityCls);
      a.ion({
        'thisObj': this,
        'name': 'scroller',
        'scroll': 'onInternalScroll'
      });
    }
  }
  changeX(a) {
    return Axis.X.new(a, { 'owner': this });
  }
  changeY(a) {
    return Axis.Y.new(a, { 'owner': this });
  }
  onInternalScroll(a) {
    const b = this, {
        lastPos: c,
        scrolling: e
      } = b;
    if (!e) {
      b.scrolling = 'maybe';
    }
    b.sync();
    const f = [
      b.x.pos,
      b.y.pos
    ];
    if (c && c[0] === f[0] && c[1] === f[1]) {
      b.scrolling = e;
    } else {
      b.lastPos = f;
      b.pushEvent(a);
      if (!e) {
        b.scrolling = true;
      }
      b.triggerScroll('scroll', a);
    }
  }
  onInternalScrollEnd() {
    const a = this, {scrolling: b} = a;
    if (b) {
      if (b === 'snap') {
        a.updateScrolling(b);
      } else {
        a.scrolling = a.snap ? 'snap' : false;
      }
    }
  }
  pushEvent(a) {
    const b = this, {events: c} = b, e = {
        'event': a,
        'x': b.x.pos,
        'y': b.y.pos
      }, f = c[c.length - 1];
    if (a.type === (f == null ? void 0 : f.event.type) && f.x === e.x && f.y === e.y) {
      f.event = a;
    } else {
      if (!f) {
        b.scrollStarted = e;
      }
      c.push(e);
      while (c.length > 3) {
        c.shift();
      }
    }
  }
  scrollTo(a) {
    let b, c;
    const e = this, {scrollingTo: f} = e, g = e.x.pos, i = e.y.pos, j = (b = a == null ? void 0 : a.x) != null ? b : g + (a == null ? void 0 : a.dx) || 0, k = (c = a == null ? void 0 : a.y) != null ? c : i + (a == null ? void 0 : a.dy) || 0, l = j - g, n = k - i, o = l || n, p = (a == null ? void 0 : a.animate) !== false && (a == null ? void 0 : a.animate) !== null;
    if (f) {
      if (f.scroll.x === j && f.scroll.y === k && f.scroll.animate === p) {
        return f.promise;
      }
      f.finish();
    }
    const q = e.scrollingTo = Promissory.new();
    q.scroll = {
      'x': j,
      'y': k,
      'animate': p
    };
    o && e.unanimated(!p, () => {
      if (l) {
        e.x.pos = j;
      }
      if (n) {
        e.y.pos = k;
      }
    });
    if (o && p) {
      q.cancel = e.ion({
        'once': true,
        'scrollEnd'() {
          q.resolve(e.x.pos === j && e.y.pos === k);
        }
      });
      q.finish = () => {
        if (q.cancel) {
          q.cancel();
          q.cancel = null;
          q.resolve(false);
          e.scrollStarted = null;
        }
      };
    } else {
      q.resolve(true);
    }
    q.finally(() => {
      if (e.scrollingTo === q) {
        e.scrollingTo = null;
      }
    });
    return q.promise;
  }
  suppressAnim(a) {
    const b = this, c = b._suppressAnim || 0, e = b._suppressAnim = Math.max(0, c + (a ? 1 : -1));
    b.scroller.behavior = e ? 'instant' : null;
  }
  sync() {
    this.unanimated(() => {
      this.x.sync();
      this.y.sync();
    });
  }
  syncItems() {
    for (const a of this.items) {
      a.$vxy.sync();
    }
  }
  triggerScroll(a, b, c = this.x.pos, e = this.y.pos) {
    this.trigger(a, {
      'widget': this.client,
      'relatedEvent': b,
      'x': c,
      'y': e
    });
  }
  updateScrolling(a, b) {
    const c = this, e = b === 'maybe', {lastEvent: f} = c;
    if (!a) {
      if (b && ((f == null ? void 0 : f.event.detail) === 'driftChange' || !e)) {
        c.scrollEnded = f;
        c.events.length = 0;
        c.triggerScroll('scrollEnd', f.event, f.x, f.y);
        c.element.classList.remove('b-scrolling');
      }
    } else if (e) {
      c.watchForEnd();
      c.scrollEnded = null;
      c.triggerScroll('scrollStart', f.event, f.x, f.y);
      c.element.classList.add('b-scrolling');
    } else if (a === 'snap') {
      c[c.snap].snap();
    }
  }
  unanimated(a, b) {
    if (typeof a === 'function') {
      b = a;
      a = true;
    }
    if (!a) {
      b();
    } else {
      this.suppressAnim(true);
      try {
        b();
      } finally {
        this.suppressAnim(false);
      }
    }
  }
  virtualize(a) {
    if (typeof a === 'string') {
      a = this.client.widgetMap[a];
    }
    let b = a.$vxy;
    if (!b || b.owner !== this) {
      b == null ? void 0 : b.destroy();
      a.$vxy = b = new Virtualizer({
        'owner': this,
        'widget': a
      });
    }
    return b;
  }
  watchForEnd() {
    const a = this, {
        client: b,
        scrollIdle: c,
        scrollStarted: e
      } = a;
    let f = 0, g, i;
    (function j() {
      if (!a.isDestroyed && !b.isDestroyed && e === a.scrollStarted) {
        i = a.pos;
        if (!g || g[0] !== i[0] || g[1] !== i[1]) {
          g = i;
          f = 0;
        } else if (++f >= c && !GlobalEvents_default.hasActiveTouches) {
          a.onInternalScrollEnd();
          return;
        }
        b.requestAnimationFrame(j);
      }
    }());
  }
};
InfinityScroller._$name = 'InfinityScroller';
const InfinityScrollable_default = a => {
  let b;
  return b = class extends a {
    get infinityScrollerDefaults() {
      return { 'animate': this.animate };
    }
    compose() {
      return {};
    }
    changeInfinityScroller(c, e) {
      if (e) {
        e.setConfig(c);
      } else {
        e = InfinityScroller.new({ 'client': this }, this.infinityScrollerDefaults, c);
      }
      return e;
    }
    onChildAdd(c) {
      let e;
      c.innerItem && ((e = this._infinityScroller) == null ? void 0 : e.virtualize(c));
      return super.onChildAdd(c);
    }
    onInfiniteScroll() {
      this.syncSoon();
    }
    onInfiniteScrollEnd() {
      this.syncSoon.now();
    }
    onInfiniteScrollStart() {
    }
    onInternalResize(c, e, f, g, i) {
      super.onInternalResize(c, e, f, g, i);
      this.hasPainted && this.syncSoon();
    }
    onInternalPaint(c) {
      let e;
      (e = super.onInternalPaint) == null ? void 0 : e.call(this, c);
      if (c.firstPaint) {
        this.syncSoon.now();
        this.infinityScroller.ion({
          'thisObj': this,
          'scroll': 'onInfiniteScroll',
          'scrollStart': 'onInfiniteScrollStart',
          'scrollEnd': 'onInfiniteScrollEnd'
        });
      }
    }
    get scrollingTo() {
      return this.infinityScroller.scrollingTo;
    }
    scrollTo(c) {
      return this.infinityScroller.scrollTo(c);
    }
    sync() {
      this.infinityScroller.sync();
    }
    syncSoon() {
      const c = this;
      if (c.isPainted) {
        c.recompose.flush();
        c.infinityScroller.animate = c.animate;
        c.sync();
      }
    }
    unanimated(c, e) {
      return this.infinityScroller.unanimated(c, e);
    }
    updateAnimation() {
      this._infinityScroller && this.syncSoon();
    }
    get widgetClass() {
    }
  }, __publicField(b, 'configurable', {
    'animate': 'smooth',
    'infinityScroller': {
      'value': {},
      '$config': 'lazy'
    },
    'monitorResize': true,
    'scrollable': {}
  }), __publicField(b, 'delayable', { 'syncSoon': 'raf' }), b;
};
const Navigator = class extends Base.mixin(Events_default, Factoryable_default) {
  static get $name() {
    return 'Navigator';
  }
  static get type() {
    return 'navigator';
  }
  static get configurable() {
    return {
      'ownerCmp': null,
      'itemsTabbable': null,
      'target': null,
      'keyEventTarget': null,
      'processEvent': null,
      'itemSelector': null,
      'activeItem': null,
      'focusCls': 'b-active',
      'keys': null,
      'allowCtrlKey': null,
      'allowShiftKey': null,
      'scrollDuration': 50,
      'disabled': null,
      'datasetIdProperty': 'id',
      'testConfig': { 'scrollDuration': 1 }
    };
  }
  static get factoryable() {
    return { 'defaultType': 'navigator' };
  }
  get activeItem() {
    const {_activeItem: a} = this;
    if (this.target.contains(a)) {
      return a;
    }
    this._activeItem = null;
  }
  updateOwnerCmp(a) {
    if (!this.itemSelector) {
      this.itemSelector = '.' + this.ownerCmp.itemCls;
    }
    DomHelper.setAttributes(this.keyEventTarget, { 'aria-activedescendant': this.owner.id + '-active-descendant' });
  }
  set navigationEvent(a) {
    const {owner: b} = this;
    this._navigationEvent = b.navigationEvent = a;
    if (a) {
      Promise.resolve(1).then(() => this.navigationEvent = null);
    }
  }
  get navigationEvent() {
    return this._navigationEvent;
  }
  static getComposedKeyName(a) {
    const b = (a.key || '').trim() || a.code;
    return '' + (a.ctrlKey ? 'CTRL+' : '') + (a.shiftKey ? 'SHIFT+' : '') + (a.altKey ? 'ALT+' : '') + b;
  }
  onKeyDown(a) {
    let b;
    const c = this, {
        ownerCmp: e,
        itemSelector: f,
        activeItem: g,
        itemsTabbable: i
      } = c, {target: j} = a, k = c.target.querySelector(f), l = (a.key || '').trim() || a.code, n = c.constructor.getComposedKeyName(a), o = j.matches(f) || j === c.keyEventTarget;
    if (!c.disabled && c.processEvent) {
      a = c.processEvent.call(e, a);
    }
    if (a && !c.disabled && k && c.target.offsetParent && o) {
      if (!i) {
        if (c.inlineFlow == null) {
          const p = k.parentNode, q = DomHelper.getStyleValue(k, 'position'), r = DomHelper.getStyleValue(k, 'display'), u = DomHelper.getStyleValue(k, 'float');
          c.inlineFlow = q === 'absolute' || r === 'inline' || r === 'inline-block' || u === 'left' || u === 'right' || DomHelper.getStyleValue(p, 'display') === 'flex' && DomHelper.getStyleValue(p, 'flex-direction') === 'row';
        }
        if (!c.prevKey) {
          if (c.inlineFlow) {
            c.prevKey = 'ArrowLeft';
            c.nextKey = 'ArrowRight';
          } else {
            c.prevKey = 'ArrowUp';
            c.nextKey = 'ArrowDown';
          }
        }
      }
      c.navigationEvent = a;
      if (g) {
        switch (l) {
        case c.prevKey:
          if (c.disabled || a.ctrlKey && !c.allowCtrlKey) {
            return;
          }
          if (a.shiftKey && !c.allowShiftKey) {
            return;
          }
          a.preventDefault();
          a.handled = true;
          c.navigatePrevious(a);
          break;
        case c.nextKey:
          if (c.disabled || a.ctrlKey && !c.allowCtrlKey) {
            return;
          }
          if (a.shiftKey && !c.allowShiftKey) {
            return;
          }
          a.preventDefault();
          a.handled = true;
          c.navigateNext(a);
          break;
        default: {
            const v = (b = c.keys) == null ? void 0 : b[n];
            if (v && !c.disabled) {
              if (c.callback(v, c.thisObj || e, [a]) === false) {
                return;
              }
            } else if (e.onInternalKeyDown) {
              e.onInternalKeyDown(a);
            }
            if (DomHelper.getActiveElement(j) !== j) {
              a.preventDefault();
            }
            if (i) {
              return;
            }
          }
        }
      } else {
        if (i) {
          return;
        }
        if (l === c.nextKey || l === c.prevKey) {
          c.activeItem = c.getDefaultNavigationItem(a);
        }
      }
      if (c.activeItem !== g) {
        c.triggerNavigate(a);
      }
    }
  }
  getDefaultNavigationItem(a) {
    const {
      target: b,
      itemSelector: c,
      prevKey: e,
      nextKey: f
    } = this;
    if (b.compareDocumentPosition(a.target) & 4 && a.key === e) {
      return b.querySelector(c + ':last-of-type');
    }
    if (b.compareDocumentPosition(a.target) & 2 && a.key === f) {
      return b.querySelector('' + c);
    }
  }
  navigatePrevious(a) {
    const b = this, c = b.previous;
    a.preventDefault();
    if (c) {
      b.ownerCmp.scrollable.scrollIntoView(c, { 'animate': b.scrollDuration }).then(() => {
        b.activeItem = c;
        b.triggerNavigate(a);
      });
    }
  }
  navigateNext(a) {
    const b = this, c = b.next;
    a.preventDefault();
    if (c) {
      b.ownerCmp.scrollable.scrollIntoView(c, { 'animate': b.scrollDuration }).then(() => {
        b.activeItem = c;
        b.triggerNavigate(a);
      });
    }
  }
  get owner() {
    return this.ownerCmp;
  }
  get previous() {
    return this.getAdjacent(-1);
  }
  get next() {
    return this.getAdjacent(1);
  }
  getAdjacent(a = 1, b = this.activeItem) {
    const c = this.treeWalker;
    c.currentNode = b;
    c[a < 0 ? 'previousNode' : 'nextNode']();
    if (c.currentNode !== this.activeItem) {
      return c.currentNode;
    }
  }
  onTargetFocusIn(a) {
    let b;
    const c = this, {
        target: e,
        relatedTarget: f
      } = a, {
        owner: g,
        itemsTabbable: i,
        skipScrollIntoView: j,
        previousActiveItem: k
      } = c;
    if (!e.matches('.b-focus-trap')) {
      if (e.matches(c.itemSelector)) {
        c.skipScrollIntoView = true;
        c.activeItem = e;
        c.skipScrollIntoView = j;
        if (c.activeItem && i) {
          c.triggerNavigate(a);
        }
      } else if (e === c.target) {
        const l = f ? e.compareDocumentPosition(f) : 0;
        if (l & Node.DOCUMENT_POSITION_CONTAINED_BY) {
          (b = g.previousSibling) == null ? void 0 : b.focus();
        } else {
          c.activeItem = k && c.target.contains(k) ? k : 0;
        }
      }
    }
  }
  onTargetFocusOut(a) {
    const b = this, {relatedTarget: c} = a;
    if (!(c == null ? void 0 : c.matches('.b-focus-trap'))) {
      if (!c || !b.target.contains(c) || !c.matches(b.itemSelector)) {
        if (b.activeItem) {
          b.activeItem = null;
          if (b.itemsTabbable) {
            b.triggerNavigate(a);
          }
        }
      }
    }
  }
  onTargetMouseDown(a) {
    const b = this, c = a.target.closest(b.itemSelector);
    b.navigationEvent = a;
    if (b.itemsTabbable) {
      if (c === b.activeItem) {
        b.triggerNavigate(a);
      }
    } else if (b.ownerCmp.itemsFocusable === false) {
      b.onFocusGesture(a);
    } else if (c) {
      a.preventDefault();
      c.focus({ 'preventScroll': true });
    }
  }
  onTargetClick(a) {
    let b;
    const c = this, {skipScrollIntoView: e} = c;
    if (c.skipNextClick || ((b = c.navigationEvent) == null ? void 0 : b.ignoreNavigation)) {
      c.skipNextClick = false;
      return;
    }
    if (c.disabled) {
      return;
    }
    c.skipScrollIntoView = true;
    if (c.processEvent) {
      a = c.processEvent.call(c.ownerCmp, a);
    }
    if (a) {
      c.activeItem = a.target.closest(c.itemSelector);
      c.triggerNavigate(a);
    }
    c.skipScrollIntoView = e;
  }
  onFocusGesture(a) {
    if (a.target === this.ownerCmp.contentElement || a.target.closest(this.itemSelector) && this.ownerCmp.itemsFocusable === false) {
      a.preventDefault();
    }
  }
  acceptNode(a) {
    return a.offsetParent && a.matches && a.matches(this.itemSelector) ? DomHelper.NodeFilter.FILTER_ACCEPT : DomHelper.NodeFilter.FILTER_SKIP;
  }
  changeActiveItem(a) {
    if (a != null) {
      if (this.ownerCmp.getItem) {
        a = this.ownerCmp.getItem(a);
      }
    }
    return a;
  }
  updateActiveItem(a, b) {
    let c;
    const e = this, {ownerCmp: f} = e, g = b && e.target.contains(b), i = a && DomHelper.isFocusable(a) && a !== DomHelper.getActiveElement(a);
    if (g) {
      e.previousActiveItem = b;
    }
    if (b) {
      b.classList.remove(e.focusCls);
      b.removeAttribute('id');
    }
    if (a != null) {
      if (e.skipScrollIntoView) {
        if (i) {
          a.focus({ 'preventScroll': true });
        }
      } else {
        (c = f.scrollable) == null ? void 0 : c.scrollIntoView(a, {
          'block': 'nearest',
          'focus': i,
          'silent': e.scrollSilently
        });
      }
      if (a === b && g) {
        return;
      }
      a.classList.add(e.focusCls);
      a.id = e.owner.id + '-active-descendant';
      e._activeItem = a;
    } else {
      e._activeItem = null;
      if (b === DomHelper.getActiveElement(b)) {
        f.revertFocus();
      }
    }
  }
  updateTarget(a, b) {
    const c = this, e = {
        'element': a,
        'thisObj': c,
        'focusin': 'onTargetFocusIn',
        'focusout': 'onTargetFocusOut',
        'mousedown': {
          'handler': 'onTargetMouseDown',
          'delegate': c.itemSelector
        }
      };
    if (!c.itemsTabbable && !c.itemsFocusable) {
      e.click = 'onTargetClick';
    }
    if (!Object.prototype.hasOwnProperty.call(c, 'acceptNode')) {
      c.acceptNode = c.acceptNode.bind(c);
      c.acceptNode.acceptNode = c.acceptNode;
    }
    EventHelper.on(e);
    c.treeWalker = c.setupTreeWalker(a, DomHelper.NodeFilter.SHOW_ELEMENT, c.acceptNode);
    if (!c.keyEventTarget) {
      c.keyEventTarget = a;
    }
    (c.targetMutationMonitor = new MutationObserver(c.onTargetChildListChange.bind(c))).observe(a, {
      'childList': true,
      'subtree': true
    });
  }
  setupTreeWalker(a, b, c) {
    return document.createTreeWalker(a, b, c);
  }
  onTargetChildListChange() {
    let a;
    const b = this, {
        activeItem: c,
        datasetIdProperty: e
      } = b;
    if (c) {
      if (b.target.contains(c)) {
        if (!c.classList.contains(b.focusCls)) {
          c.classList.add(b.focusCls);
        }
      } else {
        b.activeItem = b.target.querySelector(b.itemSelector + '.' + b.focusCls + '[data-' + StringHelper.hyphenate(e) + '="' + c.dataset[e] + '"]') || ((a = b.target.querySelector(b.itemSelector + '.' + b.focusCls)) == null ? void 0 : a[c.dataset.index]) || null;
      }
    }
  }
  changeKeyEventTarget(a) {
    this._keyEventTarget = a;
    EventHelper.on({
      'element': a,
      'keydown': 'onKeyDown',
      'thisObj': this
    });
  }
  triggerNavigate(a, b = this.activeItem) {
    let c;
    const e = {
      'event': a,
      'item': b,
      'oldItem': this.previousActiveItem
    };
    this.trigger('navigate', e);
    (c = this.ownerCmp) == null ? void 0 : c.trigger('navigate', e);
  }
};
Navigator._$name = 'Navigator';
const Clipboard = class extends Base.mixin(Events_default) {
  _content = null;
  hasNativeAccess = true;
  constructor() {
    super(...arguments);
  }
  async writeText(a, b) {
    const c = this, {_content: f} = c;
    if (b && c.hasNativeAccess) {
      try {
        await navigator.clipboard.writeText(a);
      } catch (g) {
        c.hasNativeAccess = false;
      }
    }
    if (f !== a) {
      c._content = a;
      c.triggerContentChange(f, false, true);
    }
  }
  writeData(a, b) {
    this._dataContent = a;
    this._contentSource = b;
  }
  async readText(a) {
    const b = this, {_content: c} = b;
    if (a && b.hasNativeAccess) {
      try {
        const f = await navigator.clipboard.readText();
        if (c !== f) {
          b._content = f;
          b._dataContent = b._contentSource = null;
          b.triggerContentChange(c, true);
        }
        return f;
      } catch (g) {
        b.hasNativeAccess = false;
      }
    }
    return c;
  }
  readData(a) {
    if (a && !ArrayHelper.asArray(a).includes(this._contentSource)) {
      return;
    }
    return this._dataContent;
  }
  triggerPaste(a) {
    this.trigger('paste', {
      'source': a,
      'text': this._content
    });
  }
  triggerContentChange(a, b = false, c = false) {
    this.trigger('contentChange', {
      'fromRead': b,
      'fromWrite': c,
      'oldText': a,
      'newText': this._content
    });
  }
  async clear(a) {
    await this.writeText('', a);
    this.writeData(null);
  }
};
const Clipboardable_default = a => {
  let b;
  return b = class extends (a || Base) {
    construct(...c) {
      super.construct(...c);
      if (!globalThis.bryntum.clipboard) {
        globalThis.bryntum.clipboard = new Clipboard();
      }
      globalThis.bryntum.clipboard.ion({
        'paste': 'onClipboardPaste',
        'contentChange': 'onClipboardContentChange',
        'thisObj': this
      });
    }
    get clipboard() {
      return globalThis.bryntum.clipboard;
    }
    onClipboardPaste({
      text: c,
      source: e
    }) {
      let f;
      const g = this, {
          clipboardText: i,
          isCut: j
        } = g, k = g.compareClipboardText(i, c);
      if (k && j) {
        (f = g.handleCutData) == null ? void 0 : f.call(g, {
          'text': c,
          'source': e
        });
        g.isCut = false;
        g.cutData = null;
      } else if (!k) {
        g.clearClipboard(false);
      }
    }
    onClipboardContentChange({newText: c}) {
      if (!this.compareClipboardText(this.clipboardText, c)) {
        this.clearClipboard(false);
      }
    }
    set cutData(c) {
      let e, f;
      const g = this;
      (e = g._cutData) == null ? void 0 : e.forEach(i => g.setIsCut(i, false));
      g._cutData = ArrayHelper.asArray(c);
      (f = g._cutData) == null ? void 0 : f.forEach(i => g.setIsCut(i, true));
    }
    get cutData() {
      return this._cutData;
    }
    async writeToClipboard(c, e, f = {}) {
      if (await this.beforeCopy({
          'data': c,
          'isCut': e,
          ...f
        }) === false) {
        return;
      }
      const g = this, i = typeof c === 'string', j = i ? c : g.stringConverter ? g.stringConverter(c) : StringHelper.safeJsonStringify(c), k = i ? j : g.cloneData ? g.cloneData({
          'data': c,
          'stringData': j,
          'isPaste': false
        }) : ObjectHelper.clone(c);
      g.clipboardText = j;
      g.clipboard.writeData(k, g.$$name);
      await g.clipboard.writeText(j, g.useNativeClipboard);
      ;
      g.clipboardData = k;
      g.originalData = c;
      g.isCut = e;
      g.cutData = e && !i ? c : null;
      return {
        'data': e ? c : k,
        'clonedData': k,
        'originalData': c,
        'stringData': j
      };
    }
    async readFromClipboard(c = {}, e = false) {
      let f;
      const g = this, {clipboard: i} = g, j = await i.readText(g.useNativeClipboard), k = g.compareClipboardText(g.clipboardText, j), l = k ? g.originalData : [];
      let n = i.readData(g.allowedDataSources);
      if (!k) {
        g.clearClipboard(false);
      }
      const {isCut: o} = g;
      if (o || !n || n === j) {
        n = g.transformClipboardText(j);
      }
      if (n && !o && typeof n !== 'string' && g.cloneData) {
        n = g.cloneData({
          'data': n,
          'isPaste': true
        });
      }
      if (n == null || Array.isArray(n) && n.length === 0 || await ((f = g.beforePaste) == null ? void 0 : f.call(g, {
          'data': n,
          'text': j,
          'originalData': l,
          ...c,
          'isCut': o,
          'isOwn': k
        })) === false) {
        return;
      }
      e || i.triggerPaste(g);
      return {
        'data': n,
        'text': j,
        'isOwn': k,
        'isCut': o,
        'originalData': l
      };
    }
    async clearClipboard(c = true) {
      const e = this;
      e.clipboardData = e.originalData = e.clipboardText = e.cutData = null;
      e.isCut = false;
      if (c) {
        await e.clipboard.clear(e.useNativeClipboard);
      }
    }
    compareClipboardText(c, e) {
      const f = /\r\n|(?!\r\n)[\n-\r\x85\u2028\u2029]/g;
      return (c == null ? void 0 : c.replace(f, '\n')) === (e == null ? void 0 : e.replace(f, '\n'));
    }
    transformClipboardText(c) {
      const e = this;
      return e.compareClipboardText(e.clipboardText, c) ? e.isCut ? e.originalData : e.clipboardData : e.stringParser && c ? e.stringParser(c) : c;
    }
    hasClipboardData() {
      const {clipboard: c} = this, {_content: e} = c;
      if (this.useNativeClipboard && c.hasNativeAccess) {
        return;
      }
      return Boolean(e && this.transformClipboardText(e));
    }
  }, __publicField(b, '$name', 'Clipboardable'), __publicField(b, 'configurable', {
    'useNativeClipboard': false,
    'allowedDataSources': null
  }), b;
};
const Finalizable_default = a => class b extends (a || Base) {
  static get $name() {
    return 'Finalizable';
  }
  construct(...c) {
    super.construct(...c);
    this.finalizer = null;
    this.finalizing = null;
    this.isFinalized = false;
    this.isFinalizing = false;
  }
  doFinalize() {
    this.destroy();
  }
  finalize() {
    const c = this;
    let e = c.finalizing;
    if (!e && !c.isFinalized) {
      c.isFinalizing = true;
      c.finalizing = e = c._awaitFinalizer();
    }
    return e;
  }
  async _awaitFinalizer() {
    const c = this;
    try {
      await c.finalizer;
    } finally {
      c.finalizing = null;
      c.isFinalized = true;
      c.doFinalize();
    }
  }
};
const ABORTED = Symbol('dragAbort');
const INIT = Symbol('dragInit');
const DRAGGING = Symbol('dragDrag');
const DROPPED = Symbol('dragDrop');
const lockDirections = {
  'x': 'horizontal',
  'y': 'vertical'
};
const DragContext = class extends Base.mixin(Finalizable_default, Delayable_default, Identifiable_default) {
  static $name = 'DragContext';
  static get configurable() {
    return {
      'itemElement': null,
      'scrollManager': null,
      'monitoringConfig': null,
      'source': null,
      'target': null,
      'targetElement': null,
      'threshold': 5,
      'touchStartDelay': 300,
      'event': null
    };
  }
  static get identifiable() {
    return {};
  }
  construct(...a) {
    super.construct(...a);
    const b = this, {event: c} = b;
    Object.assign(b, {
      'altKey': null,
      'cleaners': [],
      'ctrlKey': null,
      'data': new Map(),
      'element': c.target,
      'endEvent': null,
      'lastMoveEvent': null,
      'metaKey': null,
      'previousTarget': null,
      'scrollerAction': null,
      'shiftKey': null,
      'state': INIT,
      'startEvent': c,
      'touchStartTimer': null,
      '_valid': true
    });
    if ('touches' in c && b.touchStartDelay) {
      b.touchStartTimer = b.setTimeout(() => b.touchStartTimer = null, b.touchStartDelay, 'touchStartDelay');
    }
    const e = globalThis;
    EventHelper.on({
      'element': e,
      'blur': 'onWindowBlur',
      'thisObj': b,
      'autoDetach': true
    });
  }
  doDestroy() {
    const a = this, {
        source: b,
        target: c
      } = a;
    a.cleanup();
    if ((c == null ? void 0 : c.dropping) === a) {
      c.dropping = null;
    }
    if ((b == null ? void 0 : b.dragging) === a) {
      b.dragging = null;
    }
    super.doDestroy();
  }
  onWindowBlur() {
    if (this.started && !this.completed) {
      this.abort();
    }
  }
  get aborted() {
    return this.state === ABORTED;
  }
  get completed() {
    return this.isDestroying || this.aborted || this.endEvent !== null;
  }
  get pending() {
    return this.state === INIT;
  }
  get started() {
    return this.state !== INIT && !this.aborted;
  }
  get valid() {
    return this.started && this.targetElement != null && this._valid;
  }
  set valid(a) {
    this._valid = a;
  }
  async get(a) {
    if (this.aborted) {
      throw new Error('Data is not available on aborted drag');
    }
    if (!this.completed) {
      throw new Error('Data is not available until drag completion');
    }
    if (Array.isArray(a)) {
      return Promise.all(a.map(c => this.get(c)));
    }
    let b = this.data.get(a);
    if (typeof b === 'function') {
      b = await b();
      this.data.set(a, b);
    }
    return b;
  }
  has(a) {
    return this.data.has(a);
  }
  peek(a) {
    if (this.aborted) {
      throw new Error('Data is not available on aborted drag');
    }
    if (Array.isArray(a)) {
      return a.map(c => this.peek(c));
    }
    let b = this.data.get(a);
    if (typeof b === 'function') {
      b = true;
    }
    return b;
  }
  set(a, b) {
    this.data.set(a, b);
  }
  changeTarget(a, b) {
    if (a !== b) {
      const c = this;
      c._target = a;
      c.previousTarget = b;
      if (b) {
        b.dropping = null;
      }
      if (a) {
        a.dropping = c;
        if (a.dropping !== c) {
          a = null;
          c.valid = false;
        }
      }
      c._target = b;
    }
    return a;
  }
  updateTarget(a, b) {
    const c = this;
    if (b) {
      c.source.dragLeaveTarget(c, b);
    }
    if (a) {
      c.valid = true;
      a.dragMove(c);
      c.source.dragEnterTarget(c);
    }
  }
  updateTargetElement(a) {
    let b, c, e, f, g;
    for (g = a; g; g = g.parentElement) {
      c = DomDataStore.get(g, 'droppables');
      if (c) {
        for (f = 0; f < c.length; ++f) {
          b = c[f];
          if (b.dropRootElement.contains(a)) {
            e = b.droppableSelector;
            if (!e || a.closest('#' + DomHelper.getId(b.dropRootElement) + ' ' + e)) {
              this.target = b;
              if (this.target === b) {
                return;
              }
            }
          }
        }
      }
    }
  }
  abort() {
    const a = this, {
        element: b,
        source: c
      } = a;
    b == null ? void 0 : b.getBoundingClientRect();
    if (a.state !== DROPPED) {
      a.state = ABORTED;
      a.cleanup();
    }
    c == null ? void 0 : c.endDrag(a);
  }
  begin() {
    const a = this, {source: b} = a, c = b.beforeDrag(a);
    if (c !== false) {
      b.dragging = a;
    }
    return c;
  }
  cleanup() {
    let a;
    while (a = this.cleaners.pop()) {
      a();
    }
  }
  end(a) {
    const b = this, {
        lastMoveEvent: c,
        target: e
      } = b, {dragSwallowClickTime: f} = b.source;
    b.event = b.domEvent = b.endEvent = a;
    b.syncFlags();
    if (b.started) {
      if ((c == null ? void 0 : c.clientX) !== a.clientX || (c == null ? void 0 : c.clientY) !== a.clientY || (c == null ? void 0 : c.target) !== a.target) {
        b.track();
      }
      if (f) {
        const g = globalThis;
        EventHelper.on({
          'element': g,
          'capture': true,
          'expires': f,
          'once': true,
          'click'(i) {
            i.stopPropagation();
          }
        });
      }
      b.state = DROPPED;
      if (e !== b.source) {
        e == null ? void 0 : e.dragDrop(b);
      }
    }
  }
  fakeKey(a, b) {
    const c = this, {lastMoveEvent: e} = c;
    if (e && c.element) {
      let f;
      e.isKey = true;
      if (a.key === 'Alt') {
        if (c.altKey !== b) {
          c.altKey = b;
          f = true;
        }
      } else if (a.key === 'Control') {
        if (c.ctrlKey !== b) {
          c.ctrlKey = b;
          f = true;
        }
      }
      if (f) {
        c.event = c.domEvent = e;
        c.track();
      }
    }
  }
  keyDown(a) {
    if (!this.completed) {
      if (a.key === 'Escape') {
        this.abort();
      } else if (this.isDragToggleKey(a.key)) {
        this.fakeKey(a, true);
      }
    }
  }
  keyUp(a) {
    if (!this.completed && this.isDragToggleKey(a.key)) {
      this.fakeKey(a, false);
    }
  }
  getDistance(a) {
    return EventHelper.getDistanceBetween(this.startEvent, a);
  }
  isDragToggleKey(a) {
    return a === 'Control' || a === 'Alt';
  }
  move(a) {
    const b = this, {target: c} = a, e = b.getDistance(a), f = e >= b.threshold;
    b.syncFlags();
    if (b.touchStartTimer) {
      if (f) {
        b.abort();
      }
      return;
    }
    if (c && c.nodeType === Node.ELEMENT_NODE && c.isConnected) {
      if (f && !b.started) {
        b.event = b.domEvent = a;
        if (b.start() === false) {
          b.abort();
          return;
        }
      }
      if (a.touches) {
        b.startEvent.preventDefault();
        a.preventDefault();
        a.stopPropagation();
      }
      if (b.started && !b.completed) {
        b.lastMoveEvent = b.event = b.domEvent = a;
        if (a.type === 'touchmove') {
          a.preventDefault();
          a.stopImmediatePropagation();
        }
        b.track();
      }
    }
  }
  start() {
    const a = this, {
        scrollManager: b,
        monitoringConfig: c,
        source: e
      } = a, {
        draggingBodyCls: f,
        dragLock: g
      } = e, i = e.dragRootElement;
    a.state = DRAGGING;
    if (b) {
      const k = b.startMonitoring(Objects.merge({
        'scrollables': [{ 'element': i }],
        'direction': lockDirections[g] || g || 'both',
        'callback'(l) {
          const {lastMoveEvent: n} = a;
          if (n && a.element) {
            n.isScroll = true;
            a.event = a.domEvent = n;
            a.scrollerAction = l;
            a.track();
            a.scrollerAction = null;
          }
        }
      }, c));
      a.cleaners.push(k);
    }
    const j = e.dragRootElement.closest('.b-outer') || document.body;
    j.classList.add(f);
    a.cleaners.push(() => j.classList.remove(f));
    if (e.startDrag(a) === false) {
      a.cleanup();
      return false;
    }
  }
  syncFlags() {
    const a = this, {event: b} = a;
    a.altKey = b.altKey;
    a.ctrlKey = b.ctrlKey || b.metaKey;
    a.metaKey = b.metaKey;
    a.shiftKey = b.shiftKey;
  }
  track() {
    const a = this, {
        event: b,
        source: c,
        target: e
      } = a;
    let f = b.target, g;
    if (b.type === 'touchmove') {
      g = b.changedTouches[0];
      f = DomHelper.elementFromPoint(g.clientX, g.clientY);
    }
    a.targetElement = f;
    if (e === a.target) {
      e == null ? void 0 : e.dragMove(a);
    }
    c.trackDrag(a);
  }
};
DragContext.prototype.STATE = DragContext.STATE = Object.freeze({
  'ABORTED': ABORTED,
  'INIT': INIT,
  'DRAGGING': DRAGGING,
  'DROPPED': DROPPED
});
DragContext._$name = 'DragContext';
const DragProxy = class _DragProxy extends Base.mixin(Factoryable_default) {
  static get type() {
    return 'default';
  }
  static get configurable() {
    return { 'dragging': null };
  }
  static get factoryable() {
    return { 'defaultType': _DragProxy };
  }
  updateDragging(a, b) {
    if (b) {
      this.close(b);
    }
    if (a) {
      this.open(a);
    }
  }
  close(a) {
  }
  open(a) {
  }
  dragStart(a) {
    this.dragging = a;
  }
  dragMove(a) {
  }
  dragEnd(a) {
    this.dragging = null;
  }
};
DragProxy.initClass();
DragProxy._$name = 'DragProxy';
const Draggable_default = a => class b extends (a || Base) {
  static get $name() {
    return 'Draggable';
  }
  static get configurable() {
    return {
      'dragging': {
        '$config': 'nullify',
        'value': null
      },
      'draggingClsSelector': null,
      'dragDocumentListeners': {
        'element': document,
        'keydown': 'onDragKeyDown',
        'keyup': 'onDragKeyUp',
        'contextmenu': 'onDragContextMenu',
        'mousemove': 'onDragPointerMove',
        'mouseup': 'onDragPointerUp',
        'pointerup': 'onDragPointerUp',
        'touchend': 'onDragPointerUp',
        'touchmove': {
          'handler': 'onDragPointerMove',
          'passive': false
        }
      },
      'dragItemSelector': null,
      'dragItemOverCls': null,
      'dragLock': null,
      'dragMinDistance': 1,
      'dragProxy': {
        '$config': [
          'lazy',
          'nullify'
        ],
        'value': null
      },
      'dragRootElement': {
        '$config': 'nullify',
        'value': null
      },
      'dragSameTargetDrop': false,
      'dragSelector': null,
      'ignoreSelector': null,
      'dragSwallowClickTime': 50,
      'dragThreshold': 5,
      'dragTouchStartDelay': 300,
      'dropTargetSelector': null,
      'overItem': null,
      'testConfig': { 'dragSwallowClickTime': 50 }
    };
  }
  static get properties() {
    return {
      'draggingCls': 'b-draggable-active',
      'draggingBodyCls': 'b-draghelper-active',
      'draggingItemCls': 'b-dragging-item',
      'draggingStartedCls': 'b-draggable-started',
      'draggableCls': 'b-draggable'
    };
  }
  beforeDrag(c) {
    const {
        dragRootElement: e,
        dragSelector: f,
        ignoreSelector: g
      } = this, i = f && c.element.closest(f);
    return !f || Boolean(i && i === e || e.contains(i) && (!g || !c.element.matches(g)));
  }
  dragStart(c) {
  }
  dragOver(c) {
  }
  dragEnterTarget(c) {
  }
  dragLeaveTarget(c, e) {
  }
  dragDrop(c) {
  }
  dragEnd(c) {
  }
  get activeDrag() {
    const {dragging: c} = this;
    return (c == null ? void 0 : c.started) && !c.completed ? c : null;
  }
  get dragEventer() {
    return this.trigger ? this : null;
  }
  get draggingClassElement() {
    const {
      draggingClsSelector: c,
      dragRootElement: e
    } = this;
    return c ? e == null ? void 0 : e.closest(c) : e;
  }
  beginDrag(c) {
    const {
      draggingCls: e,
      draggingClassElement: f
    } = this;
    if (e && f) {
      f.classList.add(e);
      c.cleaners.push(() => f.classList.remove(e));
    }
  }
  async endDrag(c) {
    const e = this, {
        dragEventer: f,
        dragProxy: g
      } = e;
    if (c.valid) {
      await e.dragDrop(c);
    }
    if (e.isDestroyed) {
      return;
    }
    if (c.pending) {
      c.destroy();
    } else {
      e.dragEnd(c);
      g == null ? void 0 : g.dragEnd(c);
      f == null ? void 0 : f.trigger(c.valid ? 'drop' : 'dragCancel', {
        'drag': c,
        'event': c.event
      });
      e.finalizeDrag(c);
    }
  }
  async finalizeDrag(c) {
    let e;
    await ((e = c.finalize) == null ? void 0 : e.call(c));
  }
  moveDrag(c) {
    if (this.dragOver(c) !== false) {
      const {
        dragEventer: e,
        dragProxy: f
      } = this;
      f == null ? void 0 : f.dragMove(c);
      e == null ? void 0 : e.trigger('drag', {
        'drag': c,
        'event': c.event
      });
    }
  }
  setupDragContext(c) {
    const e = this, {
        dragItemSelector: f,
        id: g
      } = e, {target: i} = c;
    return {
      'event': c,
      'id': g ? g + '-drag-' + (e._nextDragId = (e._nextDragId || 0) + 1) : null,
      'itemElement': f ? i.closest(f) : i,
      'touchStartDelay': e.dragTouchStartDelay,
      'source': e,
      'threshold': e.dragThreshold
    };
  }
  startDrag(c) {
    const {
        draggingStartedCls: e,
        draggingClassElement: f,
        draggingItemCls: g,
        dragEventer: i,
        dragProxy: j
      } = this, {itemElement: k} = c;
    if ((i == null ? void 0 : i.trigger('beforeDragStart', {
        'drag': c,
        'event': c.event
      })) === false) {
      return false;
    }
    if (e && f) {
      f.classList.add(e);
      c.cleaners.push(() => f.classList.remove(e));
    }
    if (g && k) {
      k.classList.add(g);
      c.cleaners.push(() => k.classList.remove(g));
    }
    j == null ? void 0 : j.dragStart(c);
    const l = this.dragStart(c);
    if (l !== false) {
      i == null ? void 0 : i.trigger('dragStart', {
        'drag': c,
        'event': c.event
      });
    }
    return l;
  }
  trackDrag(c) {
    let e;
    const {dropTargetSelector: f} = this;
    c.valid = !(f && !((e = c.targetElement) == null ? void 0 : e.closest(f)));
    this.moveDrag(c);
  }
  configureListeners(c) {
    const e = this, f = ObjectHelper.assign({ 'thisObj': e }, e.dragDocumentListeners);
    if ('touches' in c.startEvent) {
      delete f.mousemove;
      delete f.mouseup;
    } else {
      delete f.contextmenu;
      delete f.touchmove;
      delete f.touchend;
      delete f.pointerup;
    }
    f.element = DomHelper.getRootElement(e.dragRootElement);
    return f;
  }
  updateDragging(c, e) {
    const f = this;
    if (c) {
      const g = f.configureListeners(c);
      c.cleaners.push(EventHelper.on(g));
      f.beginDrag(c);
    } else if (e) {
      e.destroy();
    }
  }
  changeDragProxy(c, e) {
    return DragProxy.reconfigure(e, c, {
      'owner': this,
      'defaults': { 'owner': this }
    });
  }
  updateDragRootElement(c, e) {
    let f;
    const g = this, {
        draggableCls: i,
        dragItemSelector: j,
        onDragItemMouseMove: k
      } = g;
    e == null ? void 0 : e.classList.remove(i);
    (f = g._dragRootDetacher) == null ? void 0 : f.call(g);
    if (c) {
      const l = {
        'thisObj': g,
        'element': c,
        'mousedown': 'onDragMouseDown',
        'touchstart': 'onDragTouchStart',
        'pointerdown': n => {
          let o, p;
          return n.pointerId && ((p = (o = n.target).releasePointerCapture) == null ? void 0 : p.call(o, n.pointerId));
        }
      };
      if (k) {
        l.mousemove = {
          'delegate': j,
          'handler': 'onDragItemMouseMove'
        };
      }
      if (g.dragItemOverCls || k || g.onDragItemMouseEnter || g.onDragItemMouseLeave) {
        Object.assign(l, {
          'mouseover': {
            'delegate': j,
            'handler': 'onDragItemMouseOver'
          },
          'mouseout': {
            'delegate': j,
            'handler': 'onDragItemMouseOut'
          }
        });
      }
      c.classList.add(i);
      g._dragRootDetacher = EventHelper.on(l);
    }
  }
  onDragItemMouseOver(c) {
    this.overItem = c;
  }
  onDragItemMouseOut(c) {
    if (!this.dragging) {
      this.overItem = c;
    }
  }
  changeOverItem(c) {
    let e;
    this.enterLeaveEvent = c;
    if (c.type === 'mouseout') {
      return ((e = c.relatedTarget) == null ? void 0 : e.closest(this.dragItemSelector)) || null;
    } else {
      return c.target.closest(this.dragItemSelector);
    }
  }
  updateOverItem(c, e) {
    let f, g;
    const i = this, {dragItemOverCls: j} = i;
    if (e) {
      j && e.classList.remove(j);
      (f = i.onDragItemMouseLeave) == null ? void 0 : f.call(i, i.enterLeaveEvent, e);
    }
    if (c) {
      j && c.classList.add(j);
      (g = i.onDragItemMouseEnter) == null ? void 0 : g.call(i, i.enterLeaveEvent, c);
    }
  }
  onDragContextMenu(c) {
    c.preventDefault();
  }
  onDragKeyDown(c) {
    this.dragging.keyDown(c);
  }
  onDragKeyUp(c) {
    this.dragging.keyUp(c);
  }
  onDragMouseDown(c) {
    if (c.button === 0) {
      this.onDragPointerDown(c);
    }
  }
  onDragPointerDown(c) {
    let {dragging: e} = this;
    if (!e) {
      e = this.setupDragContext(c);
      if (e) {
        e = new DragContext(e);
        if (e.begin() === false) {
          e.destroy();
        }
      }
    } else if (!e.isFinalizing) {
      e.abort();
    }
  }
  changeDragging(c, e) {
    e == null ? void 0 : e.destroy();
    return c;
  }
  onDragPointerMove(c) {
    const {changedTouches: e} = c, {dragging: f} = this;
    if (f && !f.completed && (!e || e.length === 1)) {
      f == null ? void 0 : f.move(c);
    }
  }
  onDragPointerUp(c) {
    const {dragging: e} = this;
    if (e && !e.completed) {
      e.end(c);
      this.endDrag(e);
    }
  }
  onDragTouchStart(c) {
    if (c.touches.length === 1) {
      this.onDragPointerDown(c);
    }
  }
};
const Droppable_default = a => class b extends (a || Base) {
  static get $name() {
    return 'Droppable';
  }
  static get configurable() {
    return {
      'droppableSelector': null,
      'dropping': null,
      'dropRootElement': {
        '$config': 'nullify',
        'value': null
      }
    };
  }
  get dropEventer() {
    return this.trigger ? this : null;
  }
  get droppableCls() {
    return 'b-droppable';
  }
  dragEnter(c) {
    let e;
    return (e = this.dropEventer) == null ? void 0 : e.trigger('dragEnter', {
      'drag': c,
      'event': c.event
    });
  }
  dragMove(c) {
    let e;
    return (e = this.dropEventer) == null ? void 0 : e.trigger('dragMove', {
      'drag': c,
      'event': c.event
    });
  }
  dragDrop(c) {
    let e;
    return (e = this.dropEventer) == null ? void 0 : e.trigger('drop', {
      'drag': c,
      'event': c.event
    });
  }
  dragLeave(c) {
    let e;
    return (e = this.dropEventer) == null ? void 0 : e.trigger('dragLeave', {
      'drag': c,
      'event': c.event
    });
  }
  changeDropping(c, e) {
    if (c !== e) {
      const f = this;
      if (e) {
        if (e.aborted || !e.completed) {
          f.dragLeave(e);
        }
      }
      if (c) {
        f._dropping = c;
        if (f.dragEnter(c) === false) {
          c = null;
        }
        f._dropping = e;
      }
    }
    return c;
  }
  updateDropRootElement(c, e) {
    const f = this, {droppableCls: g} = f;
    let j, k, l;
    if (e) {
      j = DomDataStore.get(e, 'droppables');
      l = true;
      if (Array.isArray(j) && (k = j.indexOf(f)) > -1) {
        if (j.length < 2) {
          DomDataStore.remove(e, 'droppables');
        } else {
          j.splice(k, 1);
          j.forEach(n => {
            if (g === n.droppableCls) {
              l = false;
            }
          });
        }
      }
      l && e.classList.remove(g);
    }
    if (c) {
      j = DomDataStore.get(c, 'droppables');
      if (j) {
        j.push(f);
      } else {
        DomDataStore.set(c, 'droppables', [f]);
      }
      c.classList.add(g);
    }
  }
};
const Featureable_default = a => class b extends (a || Base) {
  static get $name() {
    return 'Featureable';
  }
  static get configurable() {
    return { 'features': null };
  }
  static get declarable() {
    return ['featureable'];
  }
  static setupFeatureable(c) {
    const e = {
      'ownerName': 'client',
      ...c.featureable
    };
    e.factory.initClass();
    Reflect.defineProperty(c, 'featureable', {
      'get'() {
        return e;
      }
    });
  }
  doDestroy() {
    let c;
    const e = this.features;
    super.doDestroy();
    for (const f in e) {
      const g = e[f];
      (c = g.destroy) == null ? void 0 : c.call(g);
    }
  }
  hasFeature(c) {
    let e;
    return Boolean((e = this.features) == null ? void 0 : e[c]);
  }
  changeFeatures(c, e) {
    if (this.isDestroying) {
      return;
    }
    const f = this, {featureable: g} = f.constructor, i = f.$features || (f.$features = new DynamicObject({
        'configName': 'features',
        'factory': g.factory,
        'owner': f,
        'ownerName': g.ownerName
      }));
    i.update(c);
    if (!e) {
      return i.target;
    }
  }
  get widgetClass() {
  }
  getCurrentConfig(c) {
    const e = super.getCurrentConfig(c), {features: f} = e;
    if (f) {
      for (const g in f) {
        if (Object.keys(f[g]).length === 0) {
          f[g] = true;
        }
      }
    }
    return e;
  }
};
const EDGES = {
  'e': 'b-hover-edge',
  't': 'b-hover-top',
  'r': 'b-hover-right',
  'b': 'b-hover-bottom',
  'l': 'b-hover-left'
};
const EDGE_CLASSES = {
  [EDGES.e]: 1,
  [EDGES.t]: 1,
  [EDGES.r]: 1,
  [EDGES.b]: 1,
  [EDGES.l]: 1
};
const ZONES = {
  't': [
    EDGES.e,
    EDGES.t
  ],
  'r': [
    EDGES.e,
    EDGES.r
  ],
  'b': [
    EDGES.e,
    EDGES.b
  ],
  'l': [
    EDGES.e,
    EDGES.l
  ],
  'tr': [
    EDGES.e,
    EDGES.t,
    EDGES.r
  ],
  'bl': [
    EDGES.e,
    EDGES.b,
    EDGES.l
  ],
  'tl': [
    EDGES.e,
    EDGES.t,
    EDGES.l
  ],
  'br': [
    EDGES.e,
    EDGES.b,
    EDGES.r
  ]
};
const Hoverable_default = a => class b extends a.mixin(Delayable_default) {
  static get $name() {
    return 'Hoverable';
  }
  static get configurable() {
    return {
      'hoverCls': null,
      'hoverAnimationCls': null,
      'hoverRootCls': null,
      'hoverRootActiveCls': null,
      'hoverDelay': null,
      'hoverElement': null,
      'hoverIgnoreElement': null,
      'hoverEdges': null,
      'hoverEdgeSize': 10,
      'hoverRootElement': {
        '$config': 'nullify',
        'value': null
      },
      'hoverSelector': null,
      'hoverTarget': {
        '$config': 'nullify',
        'value': null
      },
      'hoverTrack': null,
      'hoverZone': null
    };
  }
  static get delayable() {
    return { 'setHoverTarget': 0 };
  }
  hoverEnter(c) {
  }
  hoverIgnore(c) {
    let e;
    return (e = this.hoverIgnoreElement) == null ? void 0 : e.contains(c);
  }
  hoverLeave(c) {
  }
  hoverMove(c) {
  }
  onHoverMouseMove(c) {
    const e = this, {
        hoverEdges: f,
        hoverEdgeSize: g,
        hoverTarget: i
      } = e;
    if (i) {
      if (f) {
        const {
            top: j,
            left: k,
            width: n,
            height: o,
            right: p,
            bottom: q
          } = i.getBoundingClientRect(), {
            clientX: u,
            clientY: v
          } = c, w = k + n / 2, z = j + o / 2, A = v < (g ? j + g : z), B = u >= (g ? p - g : w), C = v >= (g ? q - g : z), E = u < (g ? k + g : w), F = A || C ? A ? 't' : 'b' : '', G = B || E ? B ? 'r' : 'l' : '';
        e.hoverZone = (f.includes(F) ? F : '') + (f.includes(G) ? G : '');
      }
      e.hoverEvent = c;
      e.hoverTrack && e.hoverMove(c);
    }
  }
  onHoverMouseOver(c) {
    this.hoverEvent = c;
    this.hoverElement = c.target;
  }
  onHoverMouseOut(c) {
    this.hoverEvent = c;
    this.hoverElement = c.relatedTarget;
  }
  updateHoverDelay(c) {
    this.setHoverTarget.delay = c;
  }
  changeHoverEdges(c) {
    return c === true ? 'trbl' : (c || '').replace('v', 'tb').replace('h', 'lr');
  }
  updateHoverEdges() {
    this.syncHoverListeners();
  }
  changeHoverElement(c) {
    if (!this.hoverIgnore(c)) {
      return c;
    }
  }
  updateHoverElement(c) {
    const {hoverSelector: e} = this;
    if (e) {
      c = c == null ? void 0 : c.closest(e);
    }
    this.setHoverTarget(c);
  }
  updateHoverRootElement(c, e) {
    const {hoverRootCls: f} = this;
    if (f) {
      e == null ? void 0 : e.classList.remove(f);
      c == null ? void 0 : c.classList.add(f);
    }
    this.syncHoverListeners();
  }
  changeHoverTarget(c, e) {
    if (e) {
      this.hoverZone = null;
    }
    return c;
  }
  updateHoverTarget(c, e) {
    const f = this, {
        hoverCls: g,
        hoverAnimationCls: i,
        hoverRootActiveCls: j,
        hoverRootElement: k
      } = f;
    if (j) {
      k == null ? void 0 : k.classList[c ? 'add' : 'remove'](j);
    }
    if (e) {
      g && e.classList.remove(g);
      i && e.classList.remove(i);
      f.hoverLeave(e);
    }
    if (c) {
      g && c.classList.add(g);
      f.hoverEnter(e);
      if (f.hoverTrack) {
        f.hoverMove(f.hoverEvent);
      }
      if (i) {
        c.getBoundingClientRect();
        c.classList.add(i);
      }
    }
  }
  updateHoverTrack() {
    this.syncHoverListeners();
  }
  updateHoverZone(c) {
    const {
      hoverAnimationCls: e,
      hoverTarget: f
    } = this;
    if (f) {
      const {className: g} = f, i = DomClassList.change(g, c ? ZONES[c] : null, EDGE_CLASSES);
      if (g !== i) {
        f.className = i;
        if (c && e) {
          f.classList.remove(e);
          f.getBoundingClientRect();
          f.classList.add(e);
        }
      }
    }
  }
  setHoverTarget(c) {
    this.hoverTarget = c;
  }
  syncHoverListeners() {
    let c;
    const e = this, f = e.hoverRootElement, g = {
        'element': f,
        'thisObj': e,
        'mouseover': 'onHoverMouseOver',
        'mouseout': 'onHoverMouseOut'
      };
    if (e.hoverTrack || e.hoverEdges) {
      g.mousemove = 'onHoverMouseMove';
    }
    (c = e._hoverRootDetacher) == null ? void 0 : c.call(e);
    e._hoverRootDetacher = f && EventHelper.on(g);
  }
};
const LoadMaskable_default = a => {
  let b;
  return b = class extends a {
    static get configurable() {
      return {
        'loadMaskDefaults': {
          'useTransition': true,
          'showDelay': 1000
        },
        'loadMaskError': {
          'icon': 'b-icon b-icon-warning',
          'autoClose': 3000,
          'showDelay': 0
        },
        'loadMask': { 'text': 'L{GridBase.loadMask}' },
        'syncMask': { 'text': 'L{GridBase.syncMask}' },
        'localizableProperties': [
          'loadMask.text',
          'syncMask.text'
        ],
        'testConfig': {
          'loadMaskError': {
            'icon': 'b-icon b-icon-warning',
            'autoClose': 500,
            'showDelay': 0
          }
        }
      };
    }
    applyLoadMask() {
      const c = this, {loadMask: e} = c;
      if (e) {
        c.masked = Mask.mergeConfigs(c.loadMaskDefaults, e);
      }
      return c.masked;
    }
    applyMaskError(c) {
      const {
        loadMaskError: e,
        masked: f
      } = this;
      if (e === false) {
        f.hide();
      } else if (f) {
        ObjectHelper.assign(f.errorDefaults, e);
        f.error = c;
      }
      return f;
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'LoadMaskable'), b;
};
const excludedPropNames = {
  'constructor': 1,
  'prototype': 1,
  'name': 1,
  'length': 1,
  'arguments': 1,
  'caller': 1,
  'callee': 1,
  '__proto__': 1
};
const Override = class {
  static apply(a) {
    if (!a.target)
      throw new Error('Override must specify what it overrides, using static getter target');
    if (!a.target.class)
      throw new Error('Override must specify which class it overrides, using target.class');
    if (!this.shouldApplyOverride(a))
      return false;
    const b = Object.getOwnPropertyNames(a), c = Object.getOwnPropertyNames(a.prototype);
    b.splice(b.indexOf('target'), 1);
    this.internalOverrideAll(a.target.class, b, a);
    this.internalOverrideAll(a.target.class.prototype, c, a.prototype);
    return true;
  }
  static internalOverrideAll(a, b, c) {
    Reflect.ownKeys(c).forEach(e => {
      if (b.includes(e) && !excludedPropNames[e]) {
        const f = Object.getOwnPropertyDescriptor(c, e);
        let g = a;
        let i = null;
        while (!i && g) {
          i = Object.getOwnPropertyDescriptor(g, e);
          if (!i) {
            g = Object.getPrototypeOf(g);
          }
        }
        if (i) {
          this.internalOverride(g, e, f, i);
        }
      }
    });
  }
  static internalOverride(a, b, c, e) {
    const f = a._overridden = a._overridden || {};
    f[b] = a[b];
    if (e.get) {
      Object.defineProperty(a, b, {
        'enumerable': false,
        'configurable': true,
        'get': c.get
      });
    } else {
      a[b] = c.value;
    }
  }
  static shouldApplyOverride(a) {
    const b = a.target;
    if (!b.maxVersion && !b.minVersion)
      return true;
    if (!b.product)
      throw new Error('Override must specify product when using versioning');
    if (b.maxVersion && VersionHelper[b.product].isNewerThan(b.maxVersion)) {
      return false;
    }
    if (b.minVersion && VersionHelper[b.product].isOlderThan(b.minVersion)) {
      return false;
    }
    return true;
  }
};
Override._$name = 'Override';
const MILLIS_PER_MINUTE = 60 * 1000;
const MILLIS_PER_HOUR = 60 * MILLIS_PER_MINUTE;
const MILLIS_PER_DAY = 24 * MILLIS_PER_HOUR;
const timeRe = /(\d+)?:?(\d*)/;
const DayTime = class _DayTime {
  static format(a) {
    const b = Math.floor(a / MILLIS_PER_HOUR), c = Math.floor(a / MILLIS_PER_MINUTE) % 60;
    return b + ':' + (c < 10 ? '0' : '') + c;
  }
  static parse(a) {
    const b = typeof a;
    if (b === 'string') {
      const c = timeRe.exec(a);
      a = Number(c[1] || 0) * MILLIS_PER_HOUR + Number(c[2] || 0) * MILLIS_PER_MINUTE;
    } else if (b !== 'number') {
      a = DateHelper.getTimeOfDay(a);
    } else if (a <= 24) {
      a *= MILLIS_PER_HOUR;
    }
    return Math.min(Math.max(Math.floor(a), 0), MILLIS_PER_DAY);
  }
  constructor(a) {
    let b = 0, c, e;
    if (a == null ? void 0 : a.isDayView) {
      b = a.dayStartShift;
      c = a.dayStartTime;
      e = a.dayEndTime;
    } else if (typeof a === 'number') {
      b = c = e = a;
    } else if (a) {
      b = a.startShift;
      c = a.timeStart;
      e = a.timeEnd;
    }
    this.startShift = b = _DayTime.parse(b || 0);
    this.timeEnd = (e == null ? (b + MILLIS_PER_DAY) % MILLIS_PER_DAY : _DayTime.parse(e)) || MILLIS_PER_DAY;
    this.timeStart = c == null ? b : _DayTime.parse(c);
  }
  get startHour() {
    return Math.floor(this.timeStart / MILLIS_PER_HOUR);
  }
  get endHour() {
    return Math.floor(this.timeEnd / MILLIS_PER_HOUR);
  }
  get startTimeOffsetMs() {
    const {
      startShift: a,
      timeStart: b
    } = this;
    return b < a ? MILLIS_PER_DAY - a + b : b - a;
  }
  get today() {
    return this.startOfDay(new Date());
  }
  ceil(a) {
    const b = this.startOfDay(a);
    if (b < a) {
      b.setDate(b.getDate() + 1);
    }
    return b;
  }
  contains(a) {
    return !this.outside(a);
  }
  dateKey(a) {
    a = this.shiftDate(a, -1);
    return DateHelper.makeKey(a);
  }
  dayOfDate(a) {
    return this.shiftDate(DateHelper.clearTime(a));
  }
  dayOfWeek(a) {
    a = this.shiftDate(a, -1);
    return a.getDay();
  }
  delta(a, b = 'ms') {
    const {timeStart: c} = this, e = _DayTime.parse(a), f = (this.startShift && e < c ? e + MILLIS_PER_DAY : e) - c;
    return b === 'ms' ? f : DateHelper.as(b, f, 'ms');
  }
  duration(a = 'ms') {
    const {
        timeStart: b,
        timeEnd: c
      } = this, e = b < c ? c - b : MILLIS_PER_DAY - b + c;
    return a === 'ms' ? e : DateHelper.as(a, e, 'ms');
  }
  equals(a) {
    return this.startShift === (a == null ? void 0 : a.startShift) && this.timeStart === a.timeStart && this.timeEnd === a.timeEnd;
  }
  intersects(a, b) {
    const c = this, {
        timeStart: e,
        timeEnd: f
      } = c, [g, i] = c._dateRangeArgs(a, b), [j, k] = c.timeRange(g, i);
    if (e < f) {
      if (j < k) {
        return j < f && e <= k;
      }
      return j < f || e <= k;
    }
    return !(j < k) || j < f || e <= k;
  }
  isIntraDay(a, b) {
    const c = this, [e, f] = c._dateRangeArgs(a, b), g = c.startOfDay(e), i = MILLIS_PER_DAY - DateHelper.diff(g, f, 'ms');
    if (i < 0) {
      return false;
    }
    return i > 0 || g < e;
  }
  isInterDay(a) {
    return a.allDay || !this.isIntraDay(...arguments);
  }
  outside(a) {
    const {
        startShift: b,
        timeStart: c,
        timeEnd: e
      } = this, f = _DayTime.parse(a);
    if (c < e) {
      if (f < c) {
        return f < b ? 1 : -1;
      }
      if (f < e) {
        return 0;
      }
      return f < b ? -1 : 1;
    }
    if (f < e || f >= c) {
      return 0;
    }
    return f < b ? 1 : -1;
  }
  parseKey(a) {
    return this.dayOfDate(DateHelper.parseKey(a));
  }
  shiftDate(a, b = 1) {
    const {startShift: c} = this, e = typeof a;
    a = e === 'number' ? new Date(a) : e === 'string' ? DateHelper.parse(a) : new Date(a.getTime());
    if (b && c) {
      a.setMilliseconds(b > 0 ? c : -c);
    }
    return a;
  }
  sortEvents(a, b) {
    const c = this.startOfDay(a);
    return b == null ? void 0 : b.sort((e, f) => {
      e = e.eventRecord || e;
      f = f.eventRecord || f;
      let {startDate: g} = e, {startDate: i} = f;
      if (!g) {
        return -1;
      }
      if (!i) {
        return 1;
      }
      g = g < c ? c : g;
      i = i < c ? c : i;
      return g - i || f.endDate - i - (e.endDate - g);
    });
  }
  startOfDay(a) {
    a = this.shiftDate(a, -1);
    a = DateHelper.clearTime(a);
    a = this.shiftDate(a);
    return a;
  }
  timeRange(a, b) {
    const [c, e] = this._dateRangeArgs(a, b);
    return [
      DateHelper.getTimeOfDay(c),
      DateHelper.getTimeOfDay(e)
    ];
  }
  toString() {
    const {
        startShift: a,
        timeEnd: b,
        timeStart: c
      } = this, e = a ? '@' + _DayTime.format(a) : '', f = _DayTime.format(c);
    if (c === b) {
      return a ? e : f;
    }
    return f + '-' + _DayTime.format(b) + e;
  }
  _dateRangeArgs(a, b) {
    return a.isModel ? [
      a.startDate,
      a.endingDate
    ] : [
      a,
      b
    ];
  }
};
DayTime.MIDNIGHT = new DayTime({
  'startShift': 0,
  'timeStart': 0,
  'timeEnd': 24
});
DayTime.MILLIS_PER_MINUTE = MILLIS_PER_MINUTE;
DayTime.MILLIS_PER_HOUR = MILLIS_PER_HOUR;
DayTime.MILLIS_PER_DAY = MILLIS_PER_DAY;
DayTime._$name = 'DayTime';
const _Month = class _Month extends Events_default(Base) {
  static $name = 'Month';
  static get configurable() {
    return {
      'date': {
        '$config': { 'equal': 'date' },
        'value': DateHelper.clearTime(new Date())
      },
      'month': null,
      'year': null,
      'weekStartDay': null,
      'hideNonWorkingDays': null,
      'nonWorkingDays': null,
      'sixWeeks': null
    };
  }
  configure(a) {
    super.configure(a);
    this.updateDayNumbers();
    if (a.date) {
      this.date = a.date;
    }
    this.generation = 0;
  }
  changeDate(a) {
    if (this.isConfiguring) {
      return;
    }
    a = typeof a === 'string' ? DateHelper.parse(a, 'YYYY-MM-DD') : new Date(a);
    if (isNaN(a)) {
      throw new Error('Month date ingestion must be passed a Date, or a valid YYYY-MM-DD date string');
    }
    return a;
  }
  updateDate(a, b) {
    const c = this, {
        dayColumnIndex: e,
        weekCount: f
      } = c, g = DateHelper.getFirstDateOfMonth(a), i = DateHelper.getLastDateOfMonth(g), j = e[g.getDay()], k = e[i.getDay()], l = !b || a.getFullYear() !== b.getFullYear(), n = !b || a.getMonth() !== b.getMonth(), o = c.eventListeners && (b ? a.getDate() !== b.getDate() | (c.getWeekId(a) !== c.getWeekId(b)) << 1 | n << 2 | l << 3 : 15);
    c._year = a.getFullYear();
    c._month = a.getMonth();
    c.startDayOfMonth = 1 - j;
    c.endDayOfMonth = i.getDate() + (6 - k);
    if (c.sixWeeks) {
      c.endDayOfMonth += (6 - c.weekCount) * 7;
    }
    if (!c.weekBase || l) {
      c.calculateWeekBase();
    }
    if (n || l) {
      c.generation++;
    }
    if (o) {
      const p = {
        'newDate': a,
        'oldDate': b,
        'changes': {
          'd': true,
          'w': Boolean(o & 2),
          'm': Boolean(o & 12),
          'y': Boolean(o & 8),
          'r': c.weekCount !== f
        }
      };
      c.trigger('dateChange', p);
      if (o & 2) {
        c.trigger('weekChange', p);
      }
      if (o & 12) {
        c.trigger('monthChange', p);
      }
      if (o & 8) {
        c.trigger('yearChange', p);
      }
    }
  }
  calculateWeekBase() {
    const a = this, {dayColumnIndex: b} = a, c = new Date(a.year, 0, 1), e = new Date(a.year, 11, 31), f = a.month ? a.getOtherMonth(c) : a;
    if (a.dayColumnIndex[c.getDay()] > 3) {
      a.weekBase = f.startDate;
    } else {
      a.weekBase = new Date(a.year, 0, f.startDayOfMonth - 7);
    }
    const g = Math.floor(DateHelper.diff(a.weekBase, e, 'day') / 7);
    a.has53weeks = g === 53 && b[e.getDay()] > 2;
  }
  getWeekStart(a) {
    if (typeof a === 'number') {
      return DateHelper.add(this.weekBase, Math.max(a, 1) * 7, 'day');
    }
    const b = this, [c, e] = a;
    if (c === b.year) {
      return b.getWeekStart(e);
    }
    return b.getOtherMonth(new Date(c, 0, 1)).getWeekStart(e);
  }
  getOtherMonth(a) {
    const b = this, c = b === otherMonth ? new _Month(null) : otherMonth;
    c.configure({
      'weekBase': null,
      'weekStartDay': b.weekStartDay,
      'nonWorkingDays': b.nonWorkingDays,
      'hideNonWorkingDays': b.hideNonWorkingDays,
      'sixWeeks': b.sixWeeks,
      'date': new Date(a.getFullYear(), 0, 1)
    });
    c.date = a;
    c.updateDate(c.date, c.date);
    return c;
  }
  changeYear(a) {
    const b = new Date(this.date);
    b.setFullYear(a);
    this.date = b;
  }
  changeMonth(a) {
    const b = new Date(this.date);
    b.setMonth(a);
    this.date = b;
  }
  get weekStartDay() {
    return typeof this._weekStartDay === 'number' ? this._weekStartDay : DateHelper.weekStartDay;
  }
  updateWeekStartDay() {
    const a = this;
    a.updateDayNumbers();
    if (!a.isConfiguring && a.date) {
      a.weekBase = null;
      a.updateDate(a.date, a.date);
    }
  }
  get nonWorkingDays() {
    return this._nonWorkingDays || DateHelper.nonWorkingDays;
  }
  changeNonWorkingDays(a) {
    return ObjectHelper.assign({}, a);
  }
  updateNonWorkingDays() {
    this.updateDayNumbers();
  }
  updateHideNonWorkingDays() {
    this.updateDayNumbers();
  }
  updateSixWeeks() {
    if (!this.isConfiguring) {
      this.updateDate(this.date, this.date);
    }
  }
  get dayCount() {
    return this.endDayOfMonth + 1 - this.startDayOfMonth;
  }
  get weekCount() {
    return this.dayCount / 7;
  }
  get startDate() {
    const a = this;
    if (a.year != null && a.month != null && a.startDayOfMonth != null) {
      return new Date(a.year, a.month, a.startDayOfMonth);
    }
  }
  get endDate() {
    const a = this;
    if (a.year != null && a.month != null && a.startDayOfMonth != null) {
      return new Date(a.year, a.month, a.endDayOfMonth);
    }
  }
  eachDay(a) {
    for (let b = this.startDayOfMonth; b <= this.endDayOfMonth; b++) {
      a(new Date(this.year, this.month, b));
    }
  }
  eachWeek(a) {
    const b = this, {weekCount: c} = b;
    for (let e = b.startDayOfMonth, f = 0; f < c; f++) {
      const g = [], i = b.getWeekNumber(new Date(b.year, b.month, e));
      for (let j = 0; j < 7; j++, e++) {
        g.push(new Date(b.year, b.month, e));
      }
      a(i, g);
    }
  }
  getWeekNumber(a) {
    const b = this;
    a = DateHelper.clearTime(a);
    if (a.getFullYear() !== b.year) {
      return b.getOtherMonth(new Date(a.getFullYear(), 0, 1)).getWeekNumber(a);
    }
    let c = Math.floor(DateHelper.diff(b.weekBase, a, 'day') / 7), e = a.getFullYear();
    if (!c) {
      return b.getOtherMonth(new Date(b.year - 1, 0, 1)).getWeekNumber(new Date(b.year, 0, 0));
    } else if (c === 53 && !b.has53weeks) {
      c = 1;
      e++;
    } else if (c > 53) {
      c = c % 52;
    }
    return [
      e,
      c
    ];
  }
  getWeekId(a) {
    const b = this.getWeekNumber(a);
    return b[0] * 100 + b[1];
  }
  getCellData(a, b, c = DayTime.MIDNIGHT) {
    const e = this, f = a.getDay(), g = e.visibleDayColumnIndex[f], i = e.nonWorkingDays[f], j = e.hideNonWorkingDays && i;
    if (a < e.startDate || a > e.endDate) {
      e.month = a.getMonth();
    }
    return {
      'day': f,
      'dayTime': c,
      'visibleColumnIndex': g,
      'isNonWorking': i,
      'week': e.getOtherMonth(a).getWeekNumber(a),
      'key': DateHelper.format(a, 'YYYY-MM-DD'),
      'columnIndex': e.dayColumnIndex[f],
      'date': new Date(a),
      'dayEnd': c.duration('s'),
      'tomorrow': c.dayOfDate(DateHelper.add(a, 1, 'day')),
      'isOtherMonth': Math.sign(a.getMonth() + a.getFullYear() * 12 - (b.month + b.year * 12)),
      'visible': !j && (a >= b.startDate && a < DateHelper.add(b.endDate, 1, 'day')),
      'isRowStart': g === 0,
      'isRowEnd': g === e.visibleColumnCount - 1
    };
  }
  updateDayNumbers() {
    const a = this, {
        weekStartDay: b,
        nonWorkingDays: c,
        hideNonWorkingDays: e
      } = a, f = a.dayColumnIndex = [], g = a.canonicalDayNumbers = [], i = a.visibleDayColumnIndex = [];
    let j = 0;
    for (let k = 0; k < 7; k++) {
      const l = (b + k) % 7;
      g[k] = l;
      f[l] = k;
      if (!e || !c[l]) {
        i[l] = j++;
      }
    }
    a.visibleColumnCount = j;
    a.weekLength = e ? 7 - ObjectHelper.keys(c).length : 7;
  }
};
const Month = _Month;
var otherMonth = new Month(null);
Month._$name = 'Month';
let nextObjectIdentity = 0;
const objectIdentityMap = new WeakMap();
const argsToCacheKey = (...a) => a.map(b => {
  let c;
  if (b && typeof b == 'object' || typeof b == 'function') {
    c = objectIdentityMap.get(b);
    if (c === void 0) {
      c = ++nextObjectIdentity;
      objectIdentityMap.set(b, c);
    }
  } else {
    c = String(b);
  }
  return c;
}).join('-');
const memo = a => {
  const b = new Map();
  return (...c) => {
    const e = argsToCacheKey(c);
    let f = b.get(e);
    if (f === void 0) {
      f = a(...c);
      b.set(e, f);
    }
    return f;
  };
};
const memoCps = a => {
  const b = new Map(), c = n => n[0], e = n => n[1], f = (n, o) => c(n).push(o), g = (n, o) => e(n).push(o), i = (n, o) => e(n).some(p => ObjectHelper.isEqual(p, o)), j = () => [
      [],
      []
    ], k = n => !e(n).length && !c(n).length, l = n => {
      let o = b.get(n);
      if (o === void 0) {
        o = j();
        b.set(n, o);
      }
      return o;
    };
  return (n, o) => {
    const p = l(n);
    if (k(p)) {
      f(p, o);
      a(n, q => {
        if (!i(p, q)) {
          g(p, q);
          c(p).forEach(r => r(q));
        }
      });
    } else {
      f(p, o);
      e(p).forEach(q => o(q));
    }
  };
};
const SUCCESS = Symbol('success');
const FAILURE = Symbol('failure');
const success = (a, b) => [
  SUCCESS,
  a,
  b
];
const failure = a => [
  FAILURE,
  a
];
const isSuccess = a => a.length && a[0] === SUCCESS;
const resolveParser = a => typeof a === 'function' && !a.length ? a() : a;
const succeed = memo(a => memoCps((b, c) => c(success(a, b))));
const string = memo(a => memoCps((b, c) => {
  const e = Math.min(a.length, b.length), f = b.substr(0, e), g = b.substr(e);
  c(f === a ? success(f, g) : failure(g));
}));
const bind = (a, b) => (c, e) => resolveParser(a)(c, f => {
  if (isSuccess(f)) {
    const [, g, i] = f;
    b(g)(i, e);
  } else {
    e(f);
  }
});
const seq = memo((...a) => {
  const b = memo((c, e) => memoCps(bind(c, f => bind(e, g => succeed([].concat(f, g))))));
  return a.reduce(b, succeed([]));
});
const alt = memo((...a) => memoCps((b, c) => a.forEach(e => resolveParser(e)(b, c))));
const regexp = memo(a => (b, c) => {
  const e = new RegExp('^' + a), f = e.exec(b);
  if (f) {
    const g = f[0], i = b.substr(g.length);
    c(success(g, i));
  } else {
    c(failure(b));
  }
});
const red = memo((a, b) => bind(a, (...c) => succeed(b(...[].concat.apply([], c)))));
const runParser = (a, b) => {
  const c = [];
  a(b, e => {
    if (isSuccess(e)) {
      const [, , f] = e;
      if (f === '') {
        c.push(e);
      }
    }
  });
  return c;
};
const defineParser = a => (b, c) => c ? resolveParser(a)(b, c) : runParser(resolveParser(a), b);
const Parser_default = {
  'memo': memo,
  'memoCps': memoCps,
  'success': success,
  'failure': failure,
  'isSuccess': isSuccess,
  'resolveParser': resolveParser,
  'succeed': succeed,
  'string': string,
  'bind': bind,
  'seq': seq,
  'alt': alt,
  'regexp': regexp,
  'red': red,
  'runParser': runParser,
  'defineParser': defineParser
};
const ScrollManager = class extends Base.mixin(Delayable_default, Events_default) {
  static get configurable() {
    return {
      'element': null,
      'zoneWidth': 50,
      'scrollSpeed': 5,
      'direction': 'both',
      'startScrollDelay': 500,
      'stopScrollWhenPointerOut': false,
      'testConfig': {
        'scrollSpeed': 2,
        'startScrollDelay': 100
      },
      'activeScroll': {
        '$config': ['lazy'],
        'value': {}
      },
      'monitoring': {
        '$config': [
          'lazy',
          'nullify'
        ],
        'value': true
      },
      'owner': null
    };
  }
  changeMonitoring(a, b) {
    b == null ? void 0 : b.clear();
    return new Map();
  }
  doDestroy() {
    this.stopMonitoring();
    super.doDestroy();
  }
  get isScrolling() {
    return Object.keys(this.activeScroll).length !== 0;
  }
  get rtl() {
    let a;
    return (a = this.owner) == null ? void 0 : a.rtl;
  }
  startMonitoring(a = {}) {
    const b = this, {
        element: c,
        direction: e
      } = b, {
        scrollables: scrollables = [],
        direction: direction = e,
        callback: f
      } = a, g = [];
    if (!scrollables.length) {
      scrollables.push({ 'element': c });
    }
    scrollables.forEach(i => {
      const j = i.element;
      if (typeof j === 'string') {
        DomHelper.forEachSelector(c, j, k => {
          b.createMonitor(k, i.direction || direction, i.callback || f);
          g.push(k);
        });
      } else {
        b.createMonitor(j, i.direction || direction, i.callback || f);
        g.push(j);
      }
    });
    return function i() {
      let j;
      (j = b.stopMonitoring) == null ? void 0 : j.call(b, g);
    };
  }
  createMonitor(a, b, c) {
    const {monitoring: e} = this;
    if (!e.has(a)) {
      e.set(a, new ScrollManagerMonitor({
        'scrollManager': this,
        'element': a,
        'direction': b,
        'callback': c
      }));
    }
  }
  stopMonitoring(a) {
    const b = this, {monitoring: c} = b;
    a = ArrayHelper.asArray(a);
    if (c) {
      if (!a) {
        c.forEach(e => b.stopMonitoring(e.element));
        return;
      }
      a.forEach(e => {
        const f = c.get(e);
        e.classList.remove('b-scrolling');
        if (f) {
          f.destroy();
          c.delete(e);
        }
      });
    }
  }
  requestScroll(a, b) {
    const {activeScroll: c} = this;
    if (a in c && c[a] !== b) {
      return false;
    } else {
      c[a] = b;
      return true;
    }
  }
  releaseScroll(a) {
    const {
      activeScroll: activeScroll = {}
    } = this;
    Object.keys(activeScroll).forEach(b => {
      if (activeScroll[b] === a) {
        delete activeScroll[b];
      }
    });
  }
  getRelativeScroll(a, b = 'left') {
    let c = 0;
    this.monitoring.forEach((e, f) => {
      if (DomHelper.isDescendant(f, a)) {
        c += b === 'left' ? e.scrollRelativeLeft : e.scrollRelativeTop;
      }
    });
    return c;
  }
};
var ScrollManagerMonitor = class extends Base {
  construct(a) {
    const b = this, {element: c} = a, e = c.scrollLeft, f = c.scrollTop;
    Object.assign(a, {
      'startScrollLeft': e,
      'startScrollTop': f
    });
    super.construct(a);
    EventHelper.on({
      'element': c,
      'scroll': 'onElementScroll',
      'pointermove': 'onMouseMove',
      'capture': true,
      'thisObj': b
    });
    EventHelper.on({
      'element': c,
      'pointerleave': 'onPointerLeave',
      'thisObj': b
    });
  }
  doDestroy() {
    this.stopScroll(true);
    super.doDestroy();
  }
  startScroll() {
    const a = this;
    if (a.pendingScrollFinalize) {
      a.scrollManager.releaseScroll(a);
      a.pendingScrollFinalize = false;
    }
    a.scrolling = true;
    a.performScroll();
  }
  stopScroll(a = null) {
    const b = this, c = () => {
        b.pendingScrollFinalize = false;
        if (!b.isDestroyed) {
          b.scrollManager.releaseScroll(b);
          b.scrolling = false;
        }
      };
    if (b.scrollRequested) {
      b.scrollManager.cancelAnimationFrame(b.frameId);
      b.scrollRequested = false;
    }
    b.scrollManager.clearTimeout(b.scrollTimeout);
    b.scrollTimeout = null;
    if (!a && (b.ongoingScrollTop || b.ongoingScrollLeft)) {
      b.pendingScrollFinalize = true;
      Promise.all([
        b.ongoingScrollTop,
        b.ongoingScrollLeft
      ].filter(Boolean)).then(() => b.pendingScrollFinalize && c());
    } else {
      b.ongoingScrollTop = b.ongoingScrollLeft = null;
      c();
    }
  }
  onPointerLeave() {
    this.scrollManager.stopScrollWhenPointerOut && this.stopScroll();
  }
  onMouseMove(a) {
    const b = this, {scrollManager: c} = b, e = b.element.getBoundingClientRect(), f = c.zoneWidth, g = c.scrollSpeed;
    b.scrollDeltaX = b.scrollDeltaY = 0;
    if (b.direction !== 'vertical') {
      const {
        scrollLeft: i,
        scrollWidth: j,
        clientWidth: k
      } = b.element;
      if (c.rtl) {
        if (a.clientX < e.left + f && j + i - k >= 1) {
          b.scrollDeltaX = -Math.round((f + (e.left - a.clientX)) / g) - 1;
        } else if (a.clientX > e.right - f && i < 0) {
          b.scrollDeltaX = Math.round((f - (e.right - a.clientX)) / g) + 1;
        }
      } else {
        if (a.clientX > e.right - f && j - i - k >= 1) {
          b.scrollDeltaX = Math.round((f - (e.right - a.clientX)) / g) + 1;
        } else if (a.clientX < e.left + f && i > 0) {
          b.scrollDeltaX = -Math.round((f + (e.left - a.clientX)) / g) - 1;
        }
      }
    }
    if (b.direction !== 'horizontal') {
      const {
        scrollTop: l,
        scrollHeight: n,
        clientHeight: o
      } = b.element;
      if (a.clientY > e.bottom - f && n - l - o >= 1) {
        b.scrollDeltaY = Math.round((f - (e.bottom - a.clientY)) / g) + 1;
      } else if (a.clientY < e.top + f && l > 0) {
        b.scrollDeltaY = -Math.round((f + (e.top - a.clientY)) / g) - 1;
      }
    }
    if (b.scrollDeltaX !== 0 && !c.requestScroll('horizontal', b)) {
      b.scrollDeltaX = 0;
    }
    if (b.scrollDeltaY !== 0 && !c.requestScroll('vertical', b)) {
      b.scrollDeltaY = 0;
    }
    if (b.scrollDeltaX === 0 && b.scrollDeltaY === 0) {
      b.scrolling && b.stopScroll();
    } else if (!b.scrollTimeout) {
      b.scrollTimeout = c.setTimeout(() => b.startScroll(), c.startScrollDelay);
    }
  }
  performScroll() {
    const a = this, {
        element: b,
        scrollManager: c
      } = a;
    if (a.scrolling && !a.scrollRequested) {
      if (a.scrollDeltaX !== 0) {
        const e = b.scrollLeft, f = Math.min(e + a.scrollDeltaX, b.scrollWidth - b.clientWidth);
        b.scrollLeft = f;
        if (b.scrollLeft !== e) {
          a.ongoingScrollLeft = new Promise(g => b.addEventListener('scroll', i => {
            let j;
            (j = c.trigger) == null ? void 0 : j.call(c, 'scroll', {
              'event': i,
              'deltaY': a.scrollDeltaY,
              'deltaX': a.scrollDeltaX
            });
            g();
          }, { 'once': true }));
        } else {
          a.ongoingScrollLeft = null;
          a.scrollDeltaX = 0;
        }
      }
      if (a.scrollDeltaY !== 0) {
        const g = b.scrollTop, i = Math.min(g + a.scrollDeltaY, b.scrollHeight - b.clientHeight);
        b.scrollTop = i;
        if (b.scrollTop !== g) {
          a.ongoingScrollTop = new Promise(j => b.addEventListener('scroll', k => {
            let l;
            (l = c.trigger) == null ? void 0 : l.call(c, 'scroll', {
              'event': k,
              'deltaY': a.scrollDeltaY,
              'deltaX': a.scrollDeltaX
            });
            j();
          }, { 'once': true }));
        } else {
          a.ongoingScrollTop = null;
          a.scrollDeltaY = 0;
        }
      }
      if (a.scrollDeltaX !== 0 || a.scrollDeltaY !== 0) {
        a.scrollRequested = true;
        a.frameId = a.scrollManager.requestAnimationFrame(() => {
          a.scrollRequested = false;
          a.performScroll(a);
        });
      } else {
        a.stopScroll();
      }
    }
  }
  onElementScroll() {
    let a, b;
    (b = (a = this.config) == null ? void 0 : a.callback) == null ? void 0 : b.call(this.thisObj || this.scrollManager, this);
  }
  get scrollLeft() {
    return this.element.scrollLeft;
  }
  get scrollTop() {
    return this.verticalElement ? this.verticalElement.scrollTop : this.element.scrollTop;
  }
  get scrollRelativeLeft() {
    return this.scrollLeft - this.startScrollLeft;
  }
  get scrollRelativeTop() {
    return this.scrollTop - this.startScrollTop;
  }
  getRelativeLeftScroll(a) {
    return this.scrollManager.getRelativeScroll(a, 'left');
  }
  getRelativeTopScroll(a) {
    return this.scrollManager.getRelativeScroll(a, 'top');
  }
};
ScrollManager._$name = 'ScrollManager';
const realignTransitions = {
  'left': true,
  'right': true,
  'top': true,
  'bottom': true,
  'transform': true
};
const isBoolean = {
  'true': 1,
  'false': 1
};
const immediatePromise4 = Promise.resolve();
const _Tooltip = class _Tooltip extends Popup {
  static type = 'tooltip';
  static $name = 'Tooltip';
  static get configurable() {
    return {
      'autoHide': true,
      'mouseOffsetX': 15,
      'mouseOffsetY': 15,
      'html': { '$config': { 'equals': () => false } },
      'getHtml': null,
      'forElement': null,
      'trackMouse': null,
      'updateContentOnMouseMove': false,
      'forSelector': null,
      'hideOnDelegateChange': null,
      'anchorToTarget': true,
      'showOnHover': null,
      'hoverDelay': 500,
      'autoShow': false,
      'dismissDelay': 0,
      'hideDelay': 500,
      'loadingMsg': 'Loading...',
      'allowOver': null,
      'anchor': true,
      'align': {
        'align': 'b-t',
        'minHeight': 300
      },
      'axisLock': true,
      'activeTarget': {
        '$config': { 'equal': (a, b) => a == null && b == null || a === b },
        'value': null
      },
      'testConfig': {
        'hideDelay': 100,
        'hoverDelay': 100,
        'showAnimation': null,
        'hideAnimation': null
      },
      'role': 'tooltip',
      'describedElement': null
    };
  }
  afterConfigure() {
    const a = this, {forSelector: b} = a;
    if (b) {
      a.showOnHover = a.showOnHover !== false;
      if (!a.forElement) {
        if (!a.anchorToTarget) {
          a.trackMouse = true;
        }
        a.forElement = a.rootElement.host || a.rootElement;
      }
    }
    super.afterConfigure();
    if (a.forElement && a.showOnHover) {
      a.pointerOverOutDetacher = EventHelper.on({
        'element': a.forElement,
        'pointerover': 'internalOnPointerOver',
        'pointerout': 'internalOnPointerOut',
        'thisObj': a
      });
    }
  }
  doDestroy() {
    let a;
    (a = this.pointerOverOutDetacher) == null ? void 0 : a.call(this);
    super.doDestroy();
  }
  set focusOnToFront(a) {
    super.focusOnToFront = a;
  }
  get focusOnToFront() {
    let a;
    return ((a = this.triggeredByEvent) == null ? void 0 : a.pointerType) === 'focus' ? false : super.focusOnToFront && DomHelper.usingKeyboard;
  }
  get focusElement() {
    const a = super.focusElement;
    if (a !== this.element) {
      return a;
    }
  }
  get anchorToTarget() {
    return this._anchorToTarget && !this.trackMouse;
  }
  get anchor() {
    return super.anchor && !this.trackMouse;
  }
  set anchor(a) {
    super.anchor = a;
  }
  onDocumentMouseDown({event: a}) {
    let b;
    const c = this, {triggeredByEvent: e} = c;
    if (e && DomHelper.isTouchEvent) {
      if (a.pageX === e.pageX && a.pageY === e.pageY && ((b = c.activeTarget) == null ? void 0 : b.contains(a.target)) && performance.now() - e.timeStamp < 500) {
        return;
      }
    }
    c.abortDelayedShow();
    super.onDocumentMouseDown({ 'event': a });
  }
  internalOnPointerOver(a) {
    const b = this, {
        target: c,
        relatedTarget: e
      } = a, {
        forElement: f,
        forSelector: g,
        activeTarget: i
      } = b;
    let j;
    if (b.disabled || b.owner && !b.owner.showTooltipWhenDisabled && b.owner.disabled) {
      return;
    }
    if (b.allowOver && b.element.contains(c)) {
      return;
    }
    if (g) {
      const k = a.target.closest('[data-btip]');
      if (k && !k.matches(g)) {
        if (i) {
          b.handleForElementOut();
        }
        return;
      }
      ;
      j = b.filterTarget(c);
      if ((i == null ? void 0 : i.contains(c)) && i.contains(e) && j === i) {
        return;
      }
      if (j && (e == null ? void 0 : e.closest(g)) === j && !e.dataset.btip) {
        return;
      }
    } else if (!f.contains(e)) {
      j = f;
    } else {
      return;
    }
    if (j) {
      b.handleForElementOver(a, j);
    } else if (i) {
      b.handleForElementOut();
    }
  }
  filterTarget(a) {
    return a.closest(this.forSelector);
  }
  realignOnTransitionEnd(a) {
    let b;
    const c = this, {currentOverElement: e} = _Tooltip;
    if (realignTransitions[a.propertyName]) {
      if (c.allowOver && c.element.contains(e)) {
        return;
      }
      if (((b = c.activeTarget) == null ? void 0 : b.contains(e)) && !c.trackMouse) {
        c.realign();
      }
    }
  }
  async handleForElementOver(a, b) {
    const c = this, {
        activeTarget: e,
        hideOnDelegateChange: f,
        anchorToTarget: g
      } = c, i = b !== e, j = i && f;
    if (c.trigger('pointerOver', {
        'event': a,
        'target': b
      }) === false) {
      c.internalOnPointerOut(a);
    } else {
      c.triggeredByEvent = a;
      if (c.hasTimeout('hide')) {
        c.abortDelayedHide();
        if (!i) {
          return;
        }
      }
      if (!f && c.element.classList.contains('b-hiding')) {
        c.cancelHideShowAnimation();
      }
      if (!c._hidden && j) {
        c.hide(false);
      }
      if (c.activeTarget && c.hasTimeout('show')) {
        c.clearTimeout('show');
      }
      c.activeTarget = b;
      if (c.isVisible) {
        c.lastAlignSpec = null;
        const k = c.updateContent();
        if (Objects.isPromise(k) && !c.loadingMsg) {
          await k;
        }
        if (c.trigger('beforeShow') === false) {
          return c.hide();
        }
        c.alignTo({
          [g ? 'target' : 'position']: g ? b : 'mouse',
          'overlap': !(g && c.anchor)
        });
        c.trigger('show');
        c.afterShowByTarget();
      } else {
        c.activeTarget = b;
        c.delayShow(b);
      }
    }
  }
  async delayShow(a) {
    let b;
    const c = this;
    if ((b = c.currentAnimation) == null ? void 0 : b.showing) {
      c.cancelHideShowAnimation();
    }
    if (!c.isVisible && !c.hasTimeout('show')) {
      if (!c.hoverDelay || c.forSelector && Date.now() - c.lastHidden < c.quickShowInterval) {
        const e = c.updateContent();
        if (Objects.isPromise(e) && !c.loadingMsg) {
          await e;
        }
        c.showByTarget(a);
      } else {
        c.addDocumentMouseDownListener();
        if (!c.listeningForMouseMove && !c.anchorToTarget) {
          c.mouseMoveRemover = EventHelper.on({
            'element': c.rootElement,
            'mousemove': 'onMouseMove',
            'thisObj': c
          });
        }
        c.setTimeout(async () => {
          if (c.activeTarget === a && a.isConnected) {
            const f = c.updateContent();
            if (Objects.isPromise(f) && !c.loadingMsg) {
              await f;
            }
            c.showByTarget(a);
          }
        }, !c.triggeredByEvent || c.triggeredByEvent.type === 'pointerover' ? c.hoverDelay : 0, 'show', true);
      }
    } else if (c.isVisible) {
      c.showByTarget(a);
    }
  }
  changeAllowOver(a) {
    if (!this.inAfterShow) {
      this.configuredAllowOver = a;
    }
    return a;
  }
  updateAllowOver(a) {
    let b;
    const c = this, {element: e} = c;
    e.classList.toggle('b-allow-over', Boolean(a));
    if (a) {
      c.allowOverlisteners = EventHelper.on({
        'element': e,
        'mouseenter': 'onOwnElementMouseEnter',
        'mouseleave': 'internalOnPointerOut',
        'thisObj': c
      });
    } else {
      (b = c.allowOverlisteners) == null ? void 0 : b.call(c);
    }
  }
  updateContent() {
    const a = this;
    if (a.getHtml) {
      const b = a.callback(a.getHtml, a, [{
          'tip': a,
          'element': a.element,
          'activeTarget': a.activeTarget,
          'event': a.triggeredByEvent
        }]);
      a.html = b;
      return b;
    }
  }
  get hasContent() {
    return Boolean(DomHelper.isReactElement(this._html) || (this._html !== '' && (typeof this.html === 'string' && this.html.length) || this.items.length));
  }
  internalBeforeShow() {
    return (this.updateContentOnMouseMove || this.hasContent) && !this.disabled;
  }
  showAsyncMessage(a = this.optionalL(this.loadingMsg)) {
    if (a) {
      this.html = '\n                <div class="b-tooltip-loading">\n                    <i class="b-icon b-icon-spinner"></i>\n                    ' + StringHelper.encodeHtml(a) + '\n                </div>\n            ';
    }
  }
  showByTarget(a) {
    let b;
    const c = this, {anchorToTarget: e} = c;
    c.mouseMoveRemover = (b = c.mouseMoveRemover) == null ? void 0 : b.call(c);
    c.showBy({
      [e ? 'target' : 'position']: e ? a : 'mouse',
      'overlap': !(e && c.anchor)
    });
  }
  afterShowByTarget() {
    const a = this, {dismissDelay: b} = a;
    a.abortDelayedShow();
    if (b) {
      a.setTimeout('hide', b);
    }
    if (a.element.parentNode) {
      a.toFront();
    }
    if (!a.mouseMoveRemover && (a.trackMouse || a.updateContentOnMouseMove)) {
      a.mouseMoveRemover = EventHelper.on({
        'element': a.rootElement,
        'pointermove': 'onMouseMove',
        'thisObj': a
      });
    }
    a.inAfterShow = true;
    a.allowOver = a.allowOver || a.configuredAllowOver != false && a.childItems.some(c => c.isVisible && !c.disabled && c.focusElement);
    a.inAfterShow = false;
  }
  updateActiveTarget(a, b) {
    const c = this, e = DomHelper.getActiveElement(a);
    if (a) {
      if (!c.isConfiguring) {
        c.trigger('overTarget', {
          'newTarget': a,
          'lastTarget': b
        });
      }
      c.describedElement = a.contains(e) ? e : null;
    } else {
      c.trigger('targetOut', { 'lastTarget': b });
      c.describedElement = null;
    }
  }
  updateDescribedElement(a, b) {
    if (a) {
      DomHelper.addAttributeValue(a, 'aria-describedby', this.id);
    }
    if (b) {
      DomHelper.removeAttributeValue(b, 'aria-describedby', this.id);
    }
  }
  internalOnPointerOut(a) {
    let b;
    const c = this, e = a.type === 'pointerover' ? a.target : a.relatedTarget;
    if (c.allowOver && (c.element.contains(e) || c.owns(e, true))) {
      return;
    }
    if (c.element.contains(e) && ((b = c.activeTarget) == null ? void 0 : b.contains(e))) {
      return;
    }
    if (c.activeTarget && !c.activeTarget.contains(e)) {
      c.handleForElementOut();
    }
  }
  handleForElementOut() {
    const a = this, {hideDelay: b} = a;
    if (a.trigger('pointerOut') === false) {
      a.activeTarget = null;
      return true;
    }
    a.abortDelayedShow();
    if (a.isVisible && b !== false) {
      a.abortDelayedHide();
      if (b > 0) {
        a.setTimeout('hide', b);
      } else {
        a.hide();
      }
    }
  }
  show(a) {
    const b = this;
    if (!a && b.forElement && b.anchorToTarget && !b.forSelector) {
      b.showByTarget(b.forElement);
    } else {
      super.show(...arguments);
    }
    if (b.isVisible) {
      b.afterShowByTarget();
    } else {
      b.activeTarget = null;
    }
  }
  get hoverDelay() {
    let a;
    return ((a = this.triggeredByEvent) == null ? void 0 : a.pointerType) === 'focus' ? 0 : this._hoverDelay;
  }
  get hideDelay() {
    let a;
    return ((a = this.triggeredByEvent) == null ? void 0 : a.pointerType) === 'focus' ? 0 : this.autoHide ? this._hideDelay : false;
  }
  hide() {
    let a, b;
    const c = this;
    c.abortDelayedShow();
    if (!c._hidden) {
      c.abortDelayedHide();
      const e = super.hide(...arguments);
      c.lastHidden = Date.now();
      c.activeTarget = null;
      (a = c.mouseMoveRemover) == null ? void 0 : a.call(c);
      c.mouseMoveRemover = null;
      (b = c.transitionEndDetacher) == null ? void 0 : b.call(c);
      c.transitionEndDetacher = null;
      return e;
    } else {
      return immediatePromise4;
    }
  }
  abortDelayedShow() {
    let a, b, c;
    const e = this;
    if (e.hasTimeout('show')) {
      (a = e.mouseDownRemover) == null ? void 0 : a.call(e);
      e.mouseDownRemover = null;
      e.clearTimeout('show');
      (b = e.mouseMoveRemover) == null ? void 0 : b.call(e);
      e.mouseMoveRemover = null;
      (c = e.transitionEndDetacher) == null ? void 0 : c.call(e);
      e.transitionEndDetacher = null;
    }
  }
  abortDelayedHide() {
    this.clearTimeout('hide');
  }
  realign() {
    const a = this, b = a.lastAlignSpec;
    if (!a.isConfiguring && !a.isVisible && (b == null ? void 0 : b.targetOutOfView)) {
      if (b.allowTargetOut || DomHelper.isInView(b.target, false, a)) {
        a.show();
        b.targetOutOfView = false;
      }
    }
    super.realign();
  }
  alignTo(a) {
    const b = this, c = (b.mouseOffsetX || 1) * (b.rtl ? -1 : 1), e = b.mouseOffsetY || 1, f = Math.max(Math.min(c, 5), 0), g = Math.max(Math.min(e, 5), 0);
    if (!b.isVisible) {
      return;
    }
    let i;
    if (!b.anchorToTarget && a.position === 'mouse') {
      i = new Point(b.triggeredByEvent.pageX - globalThis.scrollX, b.triggeredByEvent.pageY - globalThis.scrollY);
      a.position = new Point(i.x + (c || 1) - (c < 0 ? b.width : 0), i.y + (e || 1) - (e < 0 ? b.height : 0));
    }
    if (a && !(b.triggeredByEvent && b.element.contains(b.triggeredByEvent.target))) {
      if (a.nodeType === Node.ELEMENT_NODE) {
        a = { 'target': a };
      }
    }
    super.alignTo(a);
    if (i && b.lastAlignSpec.result.inflate(g, f).contains(i)) {
      b.lastAlignSpec.position = 'mouse';
      b.mouseOffsetY = -e;
      b.mouseOffsetX = -c;
      b.realign();
      b.mouseOffsetY = e;
      b.mouseOffsetX = c;
    }
  }
  set html(a) {
    super.html = a;
    if (this.isVisible) {
      this.realign();
    }
  }
  get html() {
    return super.html;
  }
  changeHtml(a) {
    const b = this;
    if (Objects.isPromise(a)) {
      b.showAsyncMessage();
      a.target = b.activeTarget;
      a.then(c => {
        if (a.target === b.activeTarget) {
          b.html = c;
        }
      });
      return;
    }
    if (typeof a !== 'object') {
      a = a != null ? b.optionalL(String(a)) : '';
    }
    return a;
  }
  updateHtml(a, b) {
    const c = this;
    let e = a === '';
    if (!e) {
      super.updateHtml(a, b);
      if (c.hasContent) {
        if (!Objects.isPromise(a)) {
          c.trigger('innerHtmlUpdate', { 'value': a });
        }
      } else {
        e = true;
      }
    }
    if (e) {
      c.hide();
    }
  }
  onMouseMove(a) {
    const b = this, {
        forElement: c,
        activeTarget: e
      } = b, f = b.hasTimeout('hide'), g = a.target;
    b.triggeredByEvent = a;
    if (!b._hidden) {
      let i;
      const j = e == null ? void 0 : e.contains(g), k = !j && b.forSelector && e && !f && g.nodeType === Node.ELEMENT_NODE && !g.matches(b.forSelector) && !(b.allowOver && b.element.contains(g)), l = (c == null ? void 0 : c.document) ? c.document.documentElement : c, n = l && (l.contains ? l.contains(g) : l.compareDocumentPosition(g) & 16);
      if (k) {
        i = b.handleForElementOut();
      }
      if (i || !f || n) {
        if (b.dismissDelay && !f) {
          b.setTimeout('hide', b.dismissDelay);
        }
        if (b.updateContentOnMouseMove && b.getHtml) {
          b.html = b.callback(b.getHtml, b, [{
              'tip': b,
              'element': b.element,
              'forElement': e,
              'event': a
            }]);
          if (!b.html) {
            b.hide();
            return;
          }
        }
        if (b.trackMouse) {
          b.alignTo({
            'position': 'mouse',
            'ignorePageScroll': true
          });
        }
      }
    }
  }
  onElementUserAction(a) {
    const {showOnClick: b} = this;
    if (typeof b !== 'number' || b === a.button) {
      if (a.button === 2) {
        a.preventDefault();
      }
      this.internalOnPointerOver(a);
    }
  }
  onOwnElementMouseEnter(a) {
    this.abortDelayedHide();
  }
  static getSharedTooltip(a, b, c) {
    let e, f, g;
    let i = (f = (e = b.bryntum) == null ? void 0 : e.tooltip) == null ? void 0 : f.get(_Tooltip);
    if (!i && !c) {
      if (!((g = b.bryntum) == null ? void 0 : g.tooltip)) {
        ObjectHelper.setPath(b, 'bryntum.tooltip', new Map());
      }
      const j = b.bryntum.tooltip;
      j.set(_Tooltip, true);
      i = new _Tooltip({
        'forElement': b,
        'rootElement': a,
        'forSelector': '[data-btip]',
        'resetCfg': {},
        'isSharedTooltip': true,
        'cls': 'b-tooltip-shared',
        'internalListeners': {
          'pointerOver'({
            source: k,
            target: l
          }) {
            for (const p in k.resetCfg) {
              if (p === 'listeners') {
                k.un(k.resetCfg[p].set);
              } else if (p !== 'html') {
                k[p] = k.resetCfg[p].was;
              }
            }
            k.resetCfg = {};
            const n = Widget.getById(l.id), o = (n == null ? void 0 : n.tipConfig) || k.gatherDataConfigs(l.dataset);
            if (n && n.element !== l || !n && l.matches('.b-widget') || (n == null ? void 0 : n.disabled) && !n.showTooltipWhenDisabled) {
              return false;
            }
            k._owner = n;
            k.element.classList.toggle('b-target-disabled', Boolean(n == null ? void 0 : n.disabled));
            for (const q in o) {
              k.resetCfg[q] = {
                'set': o[q],
                'was': k[q]
              };
              if (q === 'listeners') {
                k.ion(o[q]);
              } else {
                k[q] = o[q];
              }
            }
          },
          'hide'({source: k}) {
            k.owner = null;
          }
        },
        'gatherDataConfigs'(k) {
          const l = this, n = {};
          for (const o in k) {
            if (o.startsWith('btip')) {
              if (o.length > 4) {
                const p = StringHelper.uncapitalize(o.substr(4));
                if (p in l.getDefaultConfiguration()) {
                  const q = k[o];
                  n[p] = isBoolean[q] ? q === 'true' : isNaN(q) ? q : parseInt(q, 10);
                }
              } else {
                n.html = k[o];
              }
            }
          }
          return n;
        },
        'filterTarget'(k) {
          const l = k.closest(this.forSelector);
          if (l) {
            return l;
          }
          if (_Tooltip.showOverflow && k.closest('.b-widget')) {
            while ((k == null ? void 0 : k.nodeType) === Element.ELEMENT_NODE) {
              if (DomHelper.getStyleValue(k, 'text-overflow') === 'ellipsis' && k.clientWidth < k.scrollWidth) {
                this.html = StringHelper.encodeHtml(k.textContent);
                return k;
              }
              k = k.parentNode;
            }
          }
        }
      });
      EventHelper.on({
        'element': b,
        'mouseenter': k => _Tooltip.currentOverElement = k.target,
        'keydown': k => _Tooltip.currentOverElement = k.target,
        'capture': true,
        'thisObj': i
      });
      j.set(_Tooltip, i);
    }
    return i;
  }
  static encodeConfig(a) {
    const b = {};
    if (typeof a === 'string') {
      b.btip = a;
    } else {
      for (const c in a) {
        b['btip' + (c === 'html' ? '' : StringHelper.capitalize(c))] = a[c];
      }
    }
    return b;
  }
};
const Tooltip = _Tooltip;
Tooltip.initClass();
Object.defineProperty(Widget, 'tooltip', {
  'get'() {
    return Tooltip.getSharedTooltip(document.body, document.body);
  }
});
Widget.Tooltip = Tooltip;
Tooltip._$name = 'Tooltip';
const DragTipProxy = class extends DragProxy {
  static get type() {
    return 'tip';
  }
  static get configurable() {
    return {
      'align': 't10-b50',
      'offset': 20,
      'tooltip': {
        '$config': [
          'lazy',
          'nullify'
        ],
        'value': { 'type': 'tooltip' }
      }
    };
  }
  open() {
    this.getConfig('tooltip');
  }
  close() {
    let a;
    (a = this.tooltip) == null ? void 0 : a.hide();
  }
  dragMove(a) {
    const {
        offset: b,
        tooltip: c
      } = this, {event: e} = a;
    if (c) {
      if (!c.isVisible) {
        c.show();
      }
      c.alignTo({
        'align': this.align,
        'target': new Rectangle(e.clientX - b, e.clientY - b, b * 2, b * 2)
      });
    }
  }
  changeTooltip(a, b) {
    return Widget.reconfigure(b, a, this);
  }
};
DragTipProxy.initClass();
DragTipProxy._$name = 'DragTipProxy';
const Labelable_default = a => {
  let b;
  return b = class extends (a || Base) {
    get hasLabel() {
      return Boolean(this.label);
    }
    compose() {
      const {
        hasLabel: c,
        labelPosition: e
      } = this;
      return {
        'class': {
          ['b-label-' + e]: c,
          'b-has-label': c
        }
      };
    }
    changeLabel(c) {
      return c || '';
    }
    setupLabel(c) {
      return ObjectHelper.assign({
        'tag': 'label',
        'for': this.id + '-input',
        'class': 'b-label b-align-' + (c.align || 'start')
      }, c);
    }
    updateLabelWidth(c) {
      if (this.labelElement) {
        this.labelElement.style.flex = '0 0 ' + DomHelper.setLength(c);
        this.inputWrap.style.flexBasis = c == null ? '' : 'auto';
      }
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'Labelable'), __publicField(b, 'configurable', {
    'label': null,
    'labelPosition': 'before',
    'labelCls': null,
    'labelWidth': {
      'value': null,
      '$config': { 'localeKey': 'L{labelWidth}' }
    }
  }), b;
};
const Validatable_default = (a = Base) => {
  let b;
  return b = class extends a.mixin(Localizable_default) {
    get widgetClass() {
    }
    doDestroy() {
      const c = this, e = c._rootElement && c.constructor.getSharedErrorTooltip(c._rootElement, {
          'doNotCreate': true,
          'cachePath': c.errorTooltipCachePath
        });
      super.doDestroy();
      if ((e == null ? void 0 : e.field) === c) {
        e.hide();
      }
    }
    get errorTip() {
      return this.constructor.getSharedErrorTooltip(this.rootElement, {
        'cachePath': this.errorTooltipCachePath,
        'tooltipConfig': this.errorTooltipConfig
      });
    }
    static get errorTip() {
      return this.getSharedErrorTooltip(document.body);
    }
    static getSharedErrorTooltip(c, e = {}) {
      let f;
      const {
        doNotCreate: g,
        cachePath: cachePath = 'errorTooltip',
        tooltipConfig: i
      } = e;
      let j = (f = c.bryntum) == null ? void 0 : f[cachePath];
      if (!j && !g) {
        c.bryntum = c.bryntum || {};
        j = Tooltip.new({
          'cls': 'b-field-error-tip',
          'forSelector': '.b-field.b-invalid .b-field-inner',
          'align': {
            'align': 'l-r',
            'monitorTargetMutation': true
          },
          'axisLock': 'flexible',
          'scrollAction': 'realign',
          'trapFocus': false,
          'rootElement': c,
          'onBeforeShow'() {
            const k = this, l = Widget.fromElement(k.activeTarget);
            if (l) {
              const n = l.getErrors();
              if (n) {
                k.html = n.join('<br>');
                k.field = l;
                return true;
              }
            }
            return false;
          }
        }, i);
        c.bryntum[cachePath] = j;
      }
      return j;
    }
    setError(c, e, f) {
      const g = this;
      (g.errors || (g.errors = {}))[c] = g.optionalL(c);
      if (!e) {
        g.syncInvalid();
      }
      if (f) {
        const i = () => {
            g.clearError(c);
            j();
          }, j = g.ion({
            'keydown': i,
            'focusOut': i
          });
      }
    }
    clearError(c, e) {
      const f = this;
      if (f.errors) {
        if (c) {
          delete f.errors[c];
        } else {
          f.errors = {};
        }
      }
      if (!e) {
        f.syncInvalid();
      }
    }
    getErrors() {
      const c = this;
      if (!c.isValid) {
        const e = c.validity, f = ObjectHelper.keys(e).find(j => j !== 'valid' && j !== 'customError' && e[j]), g = c.errors && Object.values(c.errors);
        let i;
        if (g == null ? void 0 : g.length) {
          i = g;
        } else if (e.customError) {
          i = [c.input.validationMessage];
        } else if (f) {
          i = [c.L(f, {
              'min': c.min,
              'max': c.max
            })];
        } else {
          i = [c.L(c.invalidValueError)];
        }
        if ((i == null ? void 0 : i.length) > 0) {
          return i;
        }
      }
    }
    syncInvalid() {
      const c = this, {isPainted: e} = c;
      c.updatingInvalid = true;
      if (e) {
        const {
          isValid: f,
          element: g,
          inputWrap: i
        } = c;
        g.classList[f ? 'remove' : 'add']('b-invalid');
        if (f) {
          const j = c.constructor.getSharedErrorTooltip(c.rootElement, {
            'doNotCreate': true,
            'cachePath': this.errorTooltipCachePath
          });
          if ((j == null ? void 0 : j.isVisible) && j.field === c) {
            j.hide();
          }
        } else if (i) {
          const k = c.errorTip;
          if (k && (c.containsFocus || c.isVisible && i.contains(Tooltip.currentOverElement))) {
            if (k.activeTarget === i && k.isVisible) {
              k.onBeforeShow();
            } else {
              k.activeTarget = i;
              k.showBy(i);
            }
          }
        }
      }
      c.updatingInvalid = false;
    }
  }, __publicField(b, '$name', 'Validatable'), b;
};
const FieldContainer = class extends Container {
  static type = 'fieldcontainer';
  static $name = 'FieldContainer';
  static get configurable() {
    return {
      'animation': { 'duration': 300 },
      'collapsed': null,
      'collapser': {
        'value': null,
        '$config': 'nullify'
      },
      'syncableConfigs': null,
      'syncConfigTriggers': {
        '$config': { 'merge': 'classList' },
        'value': null
      },
      'testConfig': { 'animation': { 'duration': 10 } }
    };
  }
  static get delayable() {
    return { 'syncChildConfigs': 'raf' };
  }
  get inline() {
    let a;
    return (a = this.owner.inline) != null ? a : this.ensureItems().count === 1;
  }
  changeCollapsed(a) {
    if (this.togglingCollapse) {
      this.togglingCollapse = false;
      return a;
    }
    this.toggleCollapse(Boolean(a));
  }
  updateCollapsed(a) {
    let b;
    this.collapser = (b = this.collapser) == null ? void 0 : b.destroy();
    this.setCollapsedCls(a);
  }
  updateCollapser(a, b) {
    if (b && b.completed == null) {
      if (!b.reverting || !a) {
        b.destroy();
      }
    }
    this.setOwnerCls('b-collapsing', a);
  }
  get collapsing() {
    const {collapser: a} = this;
    return a != null && a.collapsed;
  }
  get collapsingExpanding() {
    return this.collapser != null;
  }
  get expanding() {
    const {collapser: a} = this;
    return a != null && !a.collapsed;
  }
  collapse(a) {
    this.toggleCollapse(true, a);
  }
  expand(a) {
    this.toggleCollapse(false, a);
  }
  setCollapsedCls(a) {
    this.setOwnerCls('b-collapsed', a);
  }
  setOwnerCls(a, b) {
    let c, e;
    (e = (c = this.owner) == null ? void 0 : c.element) == null ? void 0 : e.classList[b ? 'add' : 'remove'](a);
  }
  syncChildConfigs() {
    const a = this, {
        owner: b,
        syncableConfigs: c
      } = a;
    if (c) {
      let e, f;
      for (e in c) {
        f = c[e](b);
        a.eachWidget(g => {
          g[e] = f;
        }, false);
      }
    }
    b.afterSyncChildConfigs(a);
  }
  syncContainer() {
    const {
      inline: a,
      layout: b
    } = this;
    if (this.autoLayout) {
      b.horizontal = a;
      b.justify = a ? 'center' : 'stretch';
    }
  }
  toggleCollapse(a, b) {
    const c = this, {
        animation: e,
        collapser: f,
        inline: g
      } = c, {containerWrapElement: i} = c.owner, j = k => {
        if (k) {
          c.element.style.height = '';
          c.togglingCollapse = true;
          c.collapsed = a;
          c.collapser = null;
        }
      };
    if (a == null) {
      a = !c.collapsed;
    }
    if (b !== false && b !== null) {
      if (!i || !c.isVisible) {
        b = null;
      } else {
        if (b === true) {
          b = {};
        } else if (typeof b === 'number') {
          b = { 'duration': b };
        }
        b = e || b ? ObjectHelper.merge({}, e, b) : null;
      }
    }
    if (!b) {
      c.togglingCollapse = true;
      c.collapsed = a;
    } else if (f && a !== f.collapsed) {
      c.collapser = f.revert({ 'finalize': j });
      c.collapser.collapsed = a;
    } else if (!f && a !== c.collapsed) {
      !a && c.setCollapsedCls(false);
      const {element: k} = c, {height: l} = k.getBoundingClientRect(), n = g ? 1 : l;
      !a && c.setCollapsedCls(true);
      k.style.height = l + 'px';
      c.collapser = Animator.run(ObjectHelper.merge({
        'finalize': j,
        'element': i,
        [g ? 'opacity' : 'height']: {
          'from': a ? n : 0,
          'to': a ? 0 : n
        }
      }, b));
      c.collapser.collapsed = a;
    }
  }
};
FieldContainer.initClass();
FieldContainer._$name = 'FieldContainer';
const byWeight = (a, b) => (a.weight || 0) - (b.weight || 0);
const byWeightReverse = (a, b) => (b.weight || 0) - (a.weight || 0);
const emptyArray9 = [];
const triggerConfigs = {
  'align': true,
  'weight': true
};
const _Field = class _Field extends Widget.mixin(Badge_default, Labelable_default, Validatable_default) {
  static type = 'field';
  static $name = 'Field';
  static get configurable() {
    return {
      'defaultBindProperty': 'value',
      'skipValidation': null,
      'placeholder': null,
      'value': '',
      'name': null,
      'labels': null,
      'required': null,
      'clearable': null,
      'revertOnEscape': null,
      'hint': null,
      'hintHtml': null,
      'inputWidth': null,
      'keyStrokeChangeDelay': 0,
      'readOnly': null,
      'editable': true,
      'defaultAction': 'change',
      'stepTriggers': null,
      'triggers': null,
      'highlightExternalChange': false,
      'localizableProperties': [
        'label',
        'title',
        'placeholder',
        'labelWidth'
      ],
      'autoSelect': false,
      'autoComplete': 'off',
      'spellCheck': null,
      'validateOnInput': true,
      'inputAttributes': null,
      'inputType': 'text',
      'inputAlign': null,
      'attributes': {
        'value': [
          'placeholder',
          'autoComplete',
          'spellCheck',
          'minLength',
          'maxLength',
          'pattern',
          'tabIndex',
          'min',
          'max'
        ],
        '$config': { 'merge': 'distinct' }
      },
      'nullValue': {
        '$config': null,
        'value': null,
        'default': null
      },
      'updatedClsDuration': 500,
      'inputReadOnly': false,
      'ariaElement': 'input',
      'container': {
        '$config': 'nullify',
        'value': null
      },
      'containerDefaults': {
        'type': 'fieldcontainer',
        'syncableConfigs': { 'disabled': a => a.disabled },
        'syncConfigTriggers': { 'disabled': 1 }
      },
      'containValues': null,
      'inline': null,
      'testConfig': { 'updatedClsDuration': 10 },
      'keyMap': {},
      'twinSyncConfigs': ['value']
    };
  }
  static get delayable() {
    return { 'highlightChanged': 'raf' };
  }
  doDestroy() {
    let a, b, c;
    const e = this, {triggers: f} = e;
    (a = e.inputListenerRemover) == null ? void 0 : a.call(e);
    (b = e.keyListenerRemover) == null ? void 0 : b.call(e);
    (c = e.container) == null ? void 0 : c.destroy();
    super.doDestroy();
    if (f) {
      for (const g of Object.values(f)) {
        g.destroy();
      }
    }
  }
  get childItems() {
    const {
        container: a,
        triggers: b
      } = this, c = b ? Object.values(b) : [];
    return a ? [
      ...c,
      ...a.childItems
    ] : c;
  }
  get invalidValueError() {
    return 'L{invalidValue}';
  }
  construct(a) {
    super.construct(a);
    const b = this;
    if (b.keyStrokeChangeDelay) {
      b.changeOnKeyStroke = b.buffer(b.internalOnChange, b.keyStrokeChangeDelay);
    }
  }
  changeKeyMap(a) {
    let b;
    const c = this;
    let e = {};
    if (a) {
      if (!c._fieldKeyMap) {
        c._fieldKeyMap = a;
        ObjectHelper.assign(e, a);
      } else {
        ObjectHelper.assign(e, c._fieldKeyMap);
      }
    }
    for (const f in c.triggers) {
      if ((b = c.triggers[f]) == null ? void 0 : b.keyMap) {
        e = c.mergeKeyMaps(e, c.triggers[f].keyMap, f);
      }
    }
    return e;
  }
  get keyMapSubComponents() {
    return this.triggers;
  }
  onFocusIn(a) {
    const b = this;
    b.valueOnFocus = ObjectHelper.clone(b.value);
    b.validOnFocus = !(b.errors && Object.keys(b.errors).length);
    super.onFocusIn(a);
    b.focusing = true;
    b.syncInvalid();
    b.focusing = false;
  }
  onFocusOut(a) {
    super.onFocusOut(a);
    this.syncRequired();
    this.onEditComplete();
  }
  onEditComplete() {
  }
  get rootUiClass() {
    return _Field;
  }
  compose() {
    this.getConfig('clearable');
    const a = this, {
        container: b,
        innerElements: c,
        label: e,
        labels: f,
        labelCls: g,
        readOnly: i,
        stepTriggers: j,
        triggers: k
      } = a, l = ObjectHelper.values(k, (r, u) => !u), n = l.filter(r => r.align === 'start').sort(byWeight), o = l.filter(r => r.align !== 'start').sort(byWeightReverse), p = a.setupLabel.bind(a), q = [
        ...n.map(r => r.element),
        ...c,
        ...o.map(r => r.element)
      ];
    return {
      'class': {
        'b-has-step-triggers': j,
        'b-has-start-trigger': n.length,
        'b-no-input': !c.length,
        'b-readonly': i,
        'b-field-container': Boolean(b),
        'b-field-container-inline': b == null ? void 0 : b.inline
      },
      'children': [
        ...(f || emptyArray9).filter(r => r && r.align !== 'end').map(p),
        (e || null) && p({
          'reference': 'labelElement',
          'class': 'b-label b-align-start ' + (g || ''),
          'html': e
        }),
        q.length ? {
          'class': 'b-field-inner',
          'reference': 'inputWrap',
          'children': q
        } : null,
        ...(f || emptyArray9).filter(r => (r == null ? void 0 : r.align) === 'end').map(p),
        ...b ? [{
            'reference': 'containerWrapElement',
            'children': [b.element],
            'class': { 'b-field-container-wrap': 1 }
          }] : emptyArray9
      ]
    };
  }
  updateElement(a, b) {
    let c;
    const e = this, f = e.initialConfig.value, {input: g} = e;
    super.updateElement(a, b);
    if (f != null) {
      e.value = f;
    }
    e.syncEmpty();
    e.updateInputReadOnly(e.inputReadOnly);
    e.syncInvalid();
    const i = g || e.focusElement;
    (c = e.keyListenerRemover) == null ? void 0 : c.call(e);
    e.keyListenerRemover = i && EventHelper.on({
      'element': i,
      'thisObj': e,
      'keydown': 'internalOnKeyEvent',
      'keypress': 'internalOnKeyEvent',
      'keyup': 'internalOnKeyEvent'
    });
  }
  get innerElements() {
    return [this.inputElement];
  }
  get inputElement() {
    const {
        attributes: a,
        inputAttributes: b,
        id: c,
        inputCls: e,
        inputType: f,
        name: g
      } = this, j = ObjectHelper.assign({
        'reference': 'input',
        'tag': 'input',
        'type': f,
        'name': g || c,
        'id': c + '-input',
        'retainElement': true
      }, b);
    if (e) {
      j.class = e;
    }
    for (let k, l, n = a.length; n-- > 0;) {
      k = a[n];
      l = this[k];
      if (l != null) {
        j[k] = l;
      }
    }
    return j;
  }
  get caretPos() {
    return this.textSelection[0];
  }
  set caretPos(a) {
    this.textSelection = a;
  }
  get focusElement() {
    return this.input;
  }
  get textSelection() {
    let a;
    const b = this.input;
    let c;
    try {
      c = [
        b.selectionStart,
        b.selectionEnd,
        b.selectionDirection
      ];
    } catch (f) {
      c = ((a = b.value) == null ? void 0 : a.length) || 0;
      c = [
        c,
        c
      ];
    }
    return c;
  }
  set textSelection(a) {
    if (typeof a === 'number') {
      this.select(a, a);
    } else {
      this.select(...a);
    }
  }
  get hasTextSelection() {
    const [a, b] = this.textSelection;
    return a && b - a > 0;
  }
  getAfterValue(a) {
    const [b, c] = this.textSelection, e = this.input.value;
    return '' + e.substr(0, b) + a + e.substr(c || b);
  }
  select(a, b) {
    const c = this.focusElement;
    if (c.value.length) {
      if (arguments.length === 0) {
        this.selectAll();
        return;
      }
      if (!this.supportsTextSelection) {
        return;
      }
      c.setSelectionRange(a, b);
    }
  }
  moveCaretToEnd() {
    const a = this.input;
    if (a.createTextRange) {
      const b = a.createTextRange();
      b.collapse(false);
      b.select();
    } else if (this.supportsTextSelection) {
      this.select(a.value.length, a.value.length);
    }
  }
  selectAll() {
    this.focusElement.select();
  }
  get caretAtEnd() {
    return this.textSelection[1] === this.input.value.length;
  }
  get caretAtStart() {
    return !this.textSelection[1];
  }
  get selectedAll() {
    const {textSelection: a} = this;
    return !a[0] && a[1] === this.input.value.length;
  }
  syncEmpty() {
    let a;
    const {
        isEmptyInput: b,
        isEmpty: c,
        element: e
      } = this, f = (a = this.triggers) == null ? void 0 : a.clear, g = b && c;
    if (e) {
      if (f) {
        f[g ? 'hide' : 'show']();
      }
      e.classList.toggle('b-empty', g);
    }
  }
  updateHint() {
    this.syncHint();
  }
  updateHintHtml() {
    this.syncHint();
  }
  syncHint() {
    const a = this, {
        input: b,
        hint: c,
        hintHtml: e
      } = a, f = b == null ? void 0 : b.parentElement;
    if (b) {
      let g = e || c;
      const i = a.hintElement || g && (a.hintElement = DomHelper.createElement({
        'parent': f,
        'className': 'b-field-hint',
        'nextSibling': b.nextSibling,
        'children': [{ 'className': 'b-field-hint-content' }]
      }));
      if (i) {
        if (typeof g === 'function') {
          g = g({
            'source': a,
            'value': a.value
          });
        }
        i.firstChild[e ? 'innerHTML' : 'textContent'] = g || '';
      }
      a.element.classList[g ? 'remove' : 'add']('b-field-no-hint');
    }
  }
  updateEditable() {
    this.syncInputReadOnly();
  }
  syncInputReadOnly() {
    this.getConfig('readOnly');
    this.inputReadOnly = this._readOnly || this.editable === false;
  }
  updateInputReadOnly(a) {
    const b = this, {
        input: c,
        inputListenerRemover: e
      } = b;
    if (c) {
      c.readOnly = a ? 'readOnly' : null;
      if (a) {
        b.inputListenerRemover = e == null ? void 0 : e();
      } else if (!e) {
        b.inputListenerRemover = EventHelper.on({
          'element': c,
          'thisObj': b,
          'focus': 'internalOnInputFocus',
          'change': 'internalOnChange',
          'input': 'internalOnInput'
        });
      }
    }
  }
  changeReadOnly(a) {
    return Boolean(a);
  }
  updateReadOnly(a) {
    this.syncInputReadOnly();
  }
  updateClearable(a) {
    const b = this;
    b.getConfig('triggers');
    b.triggers = {
      'clear': a && ObjectHelper.assign({
        'cls': 'b-icon-remove',
        'weight': 1000,
        'key': 'Shift+backspace',
        'handler'() {
          b.clear(...arguments);
        }
      }, a) || null
    };
  }
  changeTriggers(a, b) {
    const c = this, e = c.$triggers || (c.$triggers = new DynamicObject({
        'configName': 'triggers',
        'factory': _Field.Trigger,
        'inferType': false,
        'owner': c,
        'created'(f) {
          FunctionHelper.after(f, 'onConfigChange', (g, {name: i}) => {
            if (triggerConfigs[i]) {
              c.onConfigChange({
                'name': 'triggers',
                'value': e.target
              });
            }
          });
        },
        'setup'(f, g) {
          f.reference = f.ref = g;
          f.parent = c;
        }
      }));
    if (a && !c.stepTriggers && (a.back || a.forward)) {
      a = ObjectHelper.assign({}, a);
      delete a.back;
      delete a.forward;
    }
    e.update(a);
    if (c.element) {
      c.keyMap = c.keyMap;
    }
    if (!b) {
      return e.target;
    }
  }
  updateInputWidth(a) {
    this.input.style.width = DomHelper.setLength(a);
    this.element.classList.add('b-has-width');
  }
  updateInputAlign(a) {
    this.input.style.textAlign = a;
  }
  get isValid() {
    const a = this;
    if (!a.disabled && !a.skipValidation) {
      a.syncRequired();
      if (a.checkValidity) {
        a.callback(a.checkValidity, a, [a]);
      }
      if (a.errors && Object.keys(a.errors).length) {
        return false;
      }
      const b = a.validity;
      if (b) {
        return b.valid;
      }
    }
    return true;
  }
  get isEmpty() {
    return this.value == null || this.value === '';
  }
  get isEmptyInput() {
    return !this.input || this.input.value == null || this.input.value === '';
  }
  get validity() {
    const a = this.input;
    return a == null ? void 0 : a.validity;
  }
  async setValue(a) {
    this.value = a;
  }
  changeValue(a, b) {
    if (a == null) {
      a = this.nullValue;
    }
    if (this.hasChanged(b, a)) {
      return a;
    }
    if (a === '') {
      this.syncRequired();
    }
    return b;
  }
  updateValue(a, b) {
    const c = this;
    if (!c.isConfiguring) {
      c.syncRequired();
      if (!c.inputting) {
        c.triggerChange();
      }
    }
    if (!c.inputting) {
      c._lastValue = a;
    }
    c.syncInputFieldValue();
  }
  hasChanged(a, b) {
    return b !== a;
  }
  syncInputFieldValue(a = false) {
    const b = this, {
        input: c,
        parent: e,
        inputValueAttr: f,
        inputValue: g
      } = b;
    if (c && !b.inputting && c[f] !== g) {
      c[f] = g;
      if (!b.isConfiguring && !b.containsFocus && b.highlightExternalChange) {
        c.classList.remove('b-field-updated');
        b.clearTimeout('removeUpdatedCls');
        if (e) {
          const {assigningValues: i} = e;
          if (i === true || (i == null ? void 0 : i.highlight) === false) {
            a = true;
          }
        }
        if (!a) {
          b.highlightChanged();
        }
      }
    }
    b.syncEmpty();
    b.syncInvalid();
  }
  highlightChanged() {
    this.input.classList.add('b-field-updated');
    this.setTimeout('removeUpdatedCls', this.updatedClsDuration);
  }
  removeUpdatedCls() {
    this.input.classList.remove('b-field-updated');
  }
  get inputValue() {
    return this.value == null ? '' : this.value;
  }
  get inputValueAttr() {
    return 'value';
  }
  get supportsTextSelection() {
    const a = this.focusElement;
    return a && (a.tagName.toLowerCase() === 'textarea' || a.type && /text|search|password|tel|url/.test(a.type));
  }
  internalOnInputFocus() {
    if (this.autoSelect) {
      this.selectAll();
    }
  }
  internalOnChange(a) {
    const b = this;
    if (b.hasChanged(b._lastValue, b.value) && b.isValid) {
      b.triggerChange(a, true);
      b._lastValue = b.value;
    }
  }
  triggerChange(a, b = Boolean(this._isUserAction)) {
    const c = this, {
        value: e,
        _lastValue: f,
        isValid: g
      } = c;
    c.syncHint();
    c.triggerFieldChange({
      'value': e,
      'oldValue': f,
      'event': a,
      'userAction': b,
      'valid': g
    });
    if (c.defaultAction === 'change') {
      c.trigger('action', {
        'value': e,
        'oldValue': f,
        'event': a,
        'userAction': b,
        'valid': g
      });
    }
  }
  fetchInputValue() {
    this.value = this.input[this.inputValueAttr];
  }
  internalOnInput(a) {
    let b;
    const c = this;
    if (!c.validateOnInput) {
      c.errorTip.hide();
    }
    c.inputting = true;
    c.fetchInputValue();
    c.inputting = false;
    c.trigger('input', {
      'value': c.value,
      'event': a
    });
    (b = c.changeOnKeyStroke) == null ? void 0 : b.call(c, a);
  }
  get needsInputSync() {
    let a;
    return this.input[this.inputValueAttr] !== String((a = this.inputValue) != null ? a : '');
  }
  internalOnKeyEvent(a) {
    const b = this, {value: c} = b;
    let e = false;
    if (a.type === 'keydown') {
      if (a.key === 'Escape' && !(b.readOnly || b.disabled)) {
        const f = b.isValid, g = 'initialValue' in b ? b.initialValue : b.valueOnFocus, i = b.hasChanged(g, c), {needsInputSync: j} = b;
        if (b.revertOnEscape && (!f || i || j)) {
          if (i) {
            b.value = g;
          }
          if (j) {
            b.syncInputFieldValue(true);
          }
          b.clearError();
          e = (i || j) && b.isValid || f !== b.validOnFocus;
        } else if (!b.isEmpty && b.clearable && b.revertOnEscape) {
          b.clear();
          a.stopPropagation();
        }
      }
    }
    if (!b.isDestroyed) {
      if (e) {
        a.stopImmediatePropagation();
      }
      b.trigger(a.type, { 'event': a });
    }
  }
  clear(a) {
    const b = this, {value: c} = b;
    if (c !== b.nullValue || b.input.value.length > 0) {
      b.value = b.nullValue;
      b.clearError();
      b.syncInvalid();
      b.trigger('clear', { 'event': a });
    }
  }
  onDisabled() {
    this.syncInvalid();
  }
  syncRequired() {
    let a;
    const b = this;
    if (!b.isConfiguring && (!b.focusing || b.accessibility) && b.required && b.isEmpty && !((a = b.parent) == null ? void 0 : a.isSettingValues)) {
      b.setError('L{fieldRequired}', b.updatingInvalid);
    } else {
      b.clearError('L{fieldRequired}', b.updatingInvalid);
    }
  }
  get containerCollapsed() {
    let a;
    return (a = this.container) == null ? void 0 : a.collapsed;
  }
  set containerCollapsed(a) {
    const {container: b} = this;
    if (b) {
      b.collapsed = a;
    }
  }
  afterSyncChildConfigs(a) {
  }
  changeContainer(a, b) {
    if (a && (Array.isArray(a) || !('type' in a || 'items' in a))) {
      a = { 'items': a };
    }
    const c = this, e = a && ObjectHelper.merge({
        'owner': c,
        'layout': 'box'
      }, c.containerDefaults), f = Widget.reconfigure(b, a, {
        'owner': c,
        'defaults': e
      });
    if (f) {
      f.autoLayout = (e == null ? void 0 : e.layout) === 'box';
      if (f === b) {
        f.syncContainer();
        return;
      }
    }
    return f;
  }
  onConfigChange(a) {
    let b, c;
    super.onConfigChange(a);
    if ((c = (b = this.container) == null ? void 0 : b.syncConfigTriggers) == null ? void 0 : c[a.name]) {
      this.container.syncChildConfigs();
    }
  }
  assignValue(a, b) {
    const c = this, {
        container: e,
        containValues: f
      } = c, g = c.getValueName(b), i = a && g in a;
    if (e && f !== false) {
      if (a && f === 'nested') {
        if (i) {
          a = ObjectHelper.clone(a[g]);
          super.assignValue({ [g]: a && !('value' in a) || a.value }, b);
          if (a) {
            delete a.value;
          }
          e.assignValue(a, b);
        }
      } else {
        super.assignValue(a, b);
        e.assignValue(a, b);
      }
    } else {
      super.assignValue(a, b);
    }
  }
  gatherValue(a) {
    super.gatherValue(a);
    this.gatherValues(a);
  }
  gatherValues(a, b) {
    const c = this, {
        container: e,
        containValues: f,
        valueName: g
      } = c, i = typeof f;
    b = b != null ? b : g in a;
    if (e && f !== false) {
      if (f === 'nested') {
        if (b) {
          a[g] = {
            'value': a[g],
            ...e.values
          };
        }
      } else if (f === true || f == null && b || i === 'string' && c[f] || i === 'function' && f(c)) {
        e.gatherValues(a);
      }
    }
  }
  updateContainer(a, b) {
    b && this.detachListeners(b.id);
    if (a) {
      if (!a.rendered) {
        a.render();
      }
      a.syncContainer();
      a.syncChildConfigs();
    }
  }
};
const Field = _Field;
let _a2;
Field.Trigger = (_a2 = class extends Widget {
  static get factoryable() {
    return {
      'defaultType': Field.Trigger,
      'extends': null
    };
  }
  get field() {
    return this.parent;
  }
  construct() {
    super.construct(...arguments);
    if (this.key) {
      this.keyMap = { [this.key]: 'onClick' };
    }
  }
  compose() {
    return {
      'aria-hidden': true,
      'class': {
        ['b-align-' + (this.align || 'end')]: 1,
        'b-icon': 1
      },
      'listeners': {
        'click': 'onClick',
        'mousedown': 'onMousedown'
      }
    };
  }
  changeAlign(a) {
    return a;
  }
  onClick(a) {
    const b = this, {field: c} = b;
    if (!(c.disabled || c.readOnly)) {
      if (a.key) {
        a.preventDefault();
      }
      if (c.trigger('trigger', { 'trigger': b }) !== false && b.handler) {
        c._isUserAction = true;
        c.callback(b.handler, c, [
          a,
          b
        ]);
        c._isUserAction = false;
      }
    }
  }
  onMousedown(a) {
    const b = this.field, c = 'key' in a;
    if (!c && DomHelper.isTouchEvent) {
      if (b.editable) {
        b.editable = false;
        b.setTimeout(() => b.editable = true, 500);
      }
    }
    a.preventDefault();
    if (DomHelper.getActiveElement(b.input) !== b.input) {
      b.focus();
    }
  }
}, __publicField(_a2, '$name', 'FieldTrigger'), __publicField(_a2, 'type', 'trigger'), __publicField(_a2, 'configurable', {
  'align': null,
  'weight': null,
  'role': null
}), _a2);
let _a3;
Field.SpinTrigger = (_a3 = class extends Field.Trigger {
  compose() {
    return {
      'children': {
        'upButton': {
          'class': {
            'b-icon': 1,
            'b-spin-up': 1
          }
        },
        'downButton': {
          'class': {
            'b-icon': 1,
            'b-spin-down': 1
          }
        }
      }
    };
  }
  updateRepeat(a) {
    let b;
    (b = this.clickRepeater) == null ? void 0 : b.destroy();
    this.clickRepeater = a ? ClickRepeater.new({ 'element': this.element }, a) : null;
  }
  onClick(a) {
    const b = this, {field: c} = b;
    c._isUserAction = true;
    if (!(c.disabled || c.readOnly)) {
      if (a.target === b.upButton) {
        c.doSpinUp(a.shiftKey);
      } else if (a.target === b.downButton) {
        c.doSpinDown(a.shiftKey);
      }
    }
    c._isUserAction = false;
  }
  get isFocusable() {
    return !this.disabled && super.isFocusable;
  }
}, __publicField(_a3, '$name', 'SpinTrigger'), __publicField(_a3, 'type', 'spintrigger'), __publicField(_a3, 'configurable', {
  'repeat': {
    '$config': ['nullify'],
    'value': true
  }
}), _a3);
Field.Trigger.initClass();
Field.SpinTrigger.initClass();
Widget.register(Field.Trigger, 'trigger');
Widget.register(Field.SpinTrigger, 'spintrigger');
Field._$name = 'Field';
const TextField = class extends Field {
  static configurable = { 'nullValue': '' };
  static alias = 'text';
  static type = 'textfield';
  static $name = 'TextField';
  construct(a) {
    if ((a == null ? void 0 : a.inputType) === 'hidden') {
      a.hidden = true;
    }
    super.construct(...arguments);
  }
};
TextField.initClass();
TextField._$name = 'TextField';
const PickerField = class extends TextField {
  static type = 'pickerfield';
  static $name = 'PickerField';
  static get configurable() {
    return {
      'editable': true,
      'pickerAlignElement': 'inputWrap',
      'value': null,
      'triggers': { 'expand': { 'cls': 'bars' } },
      'autoExpand': null,
      'picker': {
        'value': { 'floating': true },
        '$config': [
          'lazy',
          'nullify'
        ]
      },
      'inputType': 'text',
      'monitorResize': true,
      'nullValue': null
    };
  }
  doDestroy() {
    let a;
    (a = this.globalTapListener) == null ? void 0 : a.call(this);
    super.doDestroy();
  }
  changeEditable(a) {
    if (BrowserHelper.isMobile && this.initialConfig.editable !== true) {
      a = false;
    }
    return a;
  }
  updateEditable(a) {
    let b;
    const c = this, {
        element: e,
        ariaElement: f,
        nonEditableClickTarget: g
      } = c, {expand: i} = c.triggers, j = globalThis.matchMedia('(max-width: 410px)').matches;
    super.updateEditable(...arguments);
    e.classList.toggle('b-not-editable', !a);
    if (a === false) {
      if (BrowserHelper.isMobile && j) {
        i == null ? void 0 : i.hide();
      }
      f == null ? void 0 : f.removeAttribute('aria-autocomplete');
      (b = c.globalTapListener) == null ? void 0 : b.call(c);
      g && EventHelper.on({
        'element': g,
        'click': k => {
          const l = g.getRootNode().elementFromPoint(k.clientX, k.clientY);
          if (k.target === g && l === g) {
            c.onTriggerClick(k);
          }
        },
        'thisObj': c
      });
    } else {
      i == null ? void 0 : i.show();
      f == null ? void 0 : f.setAttribute('aria-autocomplete', 'list');
      c.globalTapListener = GlobalEvents_default.ion({
        'globaltap': 'showKeyboard',
        'thisObj': c
      });
    }
  }
  get nonEditableClickTarget() {
    return this.input;
  }
  updateElement(a, b) {
    let c;
    const e = super.updateElement(a, b), f = this.peekConfig('picker'), g = f ? f.isWidget ? f.role : (c = this.constructor.resolveType(f.type)) == null ? void 0 : c.$meta.config.role : false;
    DomHelper.setAttributes(this.ariaElement, { 'aria-expanded': false });
    this.ariaHasPopup = g;
    return e;
  }
  get pickerAlignElement() {
    const {_pickerAlignElement: a} = this;
    return a.nodeType === Node.ELEMENT_NODE ? a : ObjectHelper.getPath(this, a);
  }
  changePicker(a, b) {
    throw new Error('changePicker() must be implemented in PickerField subclass implementations');
  }
  updatePicker(a) {
    this.detachListeners('pickerField.picker');
    a == null ? void 0 : a.ion({
      'name': 'pickerField.picker',
      'thisObj': this,
      'show': 'onPickerShow',
      'hide': 'onPickerHide'
    });
  }
  get childItems() {
    const a = super.childItems;
    if (this._picker) {
      a.push(this.picker);
    }
    return a;
  }
  onEditComplete() {
    super.onEditComplete();
    this.hidePicker();
  }
  onElementResize(a) {
    const b = this;
    if (b.pickerVisible) {
      b.picker.requestAnimationFrame(b.picker.realign, null, b.picker);
    }
    super.onElementResize(a);
  }
  internalOnKeyEvent(a) {
    const b = this;
    let c = true;
    if (a.type === 'keydown' && !b.disabled && !a.shiftKey && !a.ctrlKey) {
      if (b.pickerVisible) {
        const {picker: e} = b;
        if (a.key === 'Escape') {
          a.preventDefault();
          a.stopImmediatePropagation();
          b.hidePicker();
          c = false;
        } else if (e.onInternalKeyDown) {
          e.onInternalKeyDown(a);
        } else if (a.key === 'ArrowDown') {
          if (e.focusable) {
            e.focus();
          }
        }
      } else if (a.key === 'ArrowDown' && b.picker) {
        b.onTriggerKeyDown(a);
      }
    }
    if (c) {
      super.internalOnKeyEvent(a);
    }
  }
  onFocusIn(a) {
    let b;
    const c = this, f = (b = GlobalEvents_default.currentPointerDown) == null ? void 0 : b.target;
    super.onFocusIn(a);
    if (c.autoExpand && !(f == null ? void 0 : f.matches('.b-fieldtrigger'))) {
      c.minChars = 0;
      c.onTriggerClick(a);
    }
  }
  onTriggerClick(a) {
    if (!this.disabled) {
      this.togglePicker('key' in a);
    }
  }
  onTriggerKeyDown(a) {
    if (this.trigger('triggerKey', { 'event': a }) !== false) {
      a.stopImmediatePropagation();
      a.preventDefault();
      this.onTriggerClick(a);
    }
  }
  showKeyboard({event: a}) {
    const b = this.input;
    if (DomHelper.isTouchEvent && DomHelper.getActiveElement(b) === b && a.target === b) {
      GlobalEvents_default.suspendFocusEvents();
      b.blur();
      b.focus();
      GlobalEvents_default.resumeFocusEvents();
    }
  }
  togglePicker(a) {
    if (this.pickerVisible) {
      this.hidePicker();
    } else {
      this.showPicker(a);
    }
  }
  showPicker(a) {
    const b = this, {picker: c} = b;
    if (c) {
      DomHelper.setAttributes(b.ariaElement, {
        'aria-controls': c.id,
        'aria-expanded': true
      });
      c.show();
      if (c.isVisible) {
        if (a) {
          b.focusPicker();
        }
      }
    }
  }
  onPickerShow() {
    const a = this;
    a.pickerVisible = true;
    a.element.classList.add('b-open');
    a.trigger('togglePicker', { 'show': true });
    a.pickerTapOutRemover = GlobalEvents_default.ion({
      'globaltap': 'onPickerTapOut',
      'thisObj': a
    });
    a.pickerKeyDownRemover = EventHelper.on({
      'element': a.picker.element,
      'keydown': 'onPickerKeyDown',
      'thisObj': a
    });
  }
  onPickerHide() {
    let a, b, c;
    const e = this;
    (a = e.ariaElement) == null ? void 0 : a.setAttribute('aria-expanded', false);
    e.pickerVisible = false;
    e.element.classList.remove('b-open');
    e.trigger('togglePicker', { 'show': false });
    (b = e.pickerTapOutRemover) == null ? void 0 : b.call(e);
    (c = e.pickerKeyDownRemover) == null ? void 0 : c.call(e);
  }
  onPickerTapOut({event: a}) {
    if (!this.containsFocus && !this.owns(a.target)) {
      this.hidePicker();
    }
  }
  onPickerKeyDown(a) {
    if (a.key === 'Tab' && !this.picker.trapFocus) {
      const b = DomHelper.getActiveElement(this.input), c = new KeyboardEvent('keydown', a);
      this.input.dispatchEvent(c);
      if (c.defaultPrevented) {
        a.preventDefault();
      }
      if (DomHelper.getActiveElement(this.input) === b) {
        this.input.focus();
      } else {
        a.preventDefault();
      }
      if (!this.isDestroyed) {
        this.hidePicker();
      }
    }
  }
  hidePicker() {
    if (this.pickerVisible) {
      this.picker.hide();
    }
  }
  focusPicker() {
  }
  focus() {
    const a = this.input;
    if (DomHelper.isTouchEvent && this.editable) {
      a.readOnly = true;
      this.setTimeout(() => a.readOnly = false, 500);
    }
    super.focus();
  }
};
PickerField._$name = 'PickerField';
const itemRange = document.createRange();
const List = class extends Widget {
  static configurable = {
    'itemCls': 'b-list-item',
    'selectedCls': 'b-selected',
    'selectIconCls': 'b-icon-check',
    'tag': 'ul',
    'items': null,
    'displayField': 'text',
    'store': {
      'value': {},
      '$config': 'lazy'
    },
    'navigator': true,
    'scrollable': {
      'overflowX': false,
      'overflowY': true
    },
    'itemsFocusable': true,
    'multiSelect': false,
    'toggleAllIfCtrlPressed': false,
    'collapsibleGroups': false,
    'selectAllItem': false,
    'itemTpl'(a) {
      let b = StringHelper.encodeHtml(a.getValue(this.displayField));
      if (b == null || b === '') {
        b = '&nbsp;';
      }
      return b;
    },
    'tooltipTemplate': null,
    'getItemStyle': null,
    'getItemCls': null,
    'groupHeaderTpl'(a, b) {
      let c = StringHelper.encodeHtml(b);
      if (c == null || c === '') {
        c = '&nbsp;';
      }
      return '' + ((this.allowGroupSelect || a.isParent) && this.isSelectable(a) ? this.itemIconTpl(a) : '') + c;
    },
    'allowGroupSelect': true,
    'clearSelectionOnEmptySpaceClick': false,
    'selected': {
      '$config': [
        'nullify',
        'lazy'
      ],
      'value': []
    },
    'activateOnMouseover': null,
    'role': 'listbox',
    'keyMap': { 'Ctrl+a': 'selectAll' },
    'title': null,
    'virtualize': null,
    'listItemHeight': 10,
    'isSelectable': () => true
  };
  static type = 'list';
  static $name = 'List';
  construct(a, ...b) {
    let c;
    const e = this;
    if (Array.isArray(a)) {
      a = { 'items': a };
    }
    super.construct(a, ...b);
    const {element: f} = e, {classList: g} = f;
    if (e.multiSelect) {
      g.add('b-multiselect');
    }
    if ((c = e.store) == null ? void 0 : c.count) {
      e.refresh();
    } else {
      g.add('b-empty');
    }
    EventHelper.on({
      'element': f,
      'delegate': e.itemSelector,
      'mouseover': 'onMouseOver',
      'thisObj': e
    });
    EventHelper.on({
      'element': f,
      'click': 'onClick',
      'thisObj': e
    });
  }
  doDestroy() {
    let a;
    this.detachListeners('store');
    (a = this.navigator) == null ? void 0 : a.destroy();
    super.doDestroy();
  }
  onFocusIn(a) {
    let b;
    this.restoreActiveItem((b = a._target) == null ? void 0 : b.closest(this.itemSelector));
    super.onFocusIn(a);
  }
  get isVirtualized() {
    return typeof this.virtualize === 'number' ? this.store.count > this.virtualize : Boolean(this.virtualize);
  }
  outOfViewTpl(a, b) {
    return '<li class="b-list-item b-out-of-view" data-index="' + b + '" data-id="' + StringHelper.encodeHtml(a.id) + '"></li>';
  }
  onItemIntersection(a, b) {
    if (b) {
      const c = parseInt(a.dataset.index), e = this.store.getAt(c);
      DomHelper.sync(this.itemWrapperTpl(e, c), a);
      this.itemIntersectionObserver.unobserve(a);
    }
  }
  contentTpl() {
    const a = this, b = Math.ceil(a.height / a.listItemHeight), c = a.store.records.map((e, f) => !a.isVirtualized || f < b ? a.itemWrapperTpl(e, f) : a.outOfViewTpl(e, f));
    if (a.multiSelect && a.selectAllItem !== false) {
      c.unshift(a.selectAllItemTpl());
    }
    if (a.title) {
      c.unshift('<li class="b-list-item b-list-title">' + a.title + '</li>');
    }
    return TemplateHelper.tpl`${ c }`;
  }
  itemWrapperTpl(a, b) {
    const c = this, {
        selected: e,
        getItemStyle: f,
        store: g,
        tooltipTemplate: j
      } = c, k = (j == null ? void 0 : j(a)) || '', l = 'style="' + (f ? c.callback(f, c, [a]) : '') + ';--tree-level:' + (a.childLevel || 0) + '"', n = c.callback(c.isSelectable, c, [a]);
    if (g.isGrouped && !a.isGroupHeader && a.instanceMeta(g).groupParent.meta.collapsed) {
      return '';
    }
    if (!a.ancestorsExpanded(g)) {
      return '';
    }
    return TemplateHelper.tpl`<li ${ l } class="${ c.getItemClasses(a, b) }" role="option" ${ k ? 'data-btip="' + StringHelper.encodeHtml(k) + '"' : '' } aria-selected="${ e.includes(a) }" data-index="${ b }" ${ n ? '' : 'data-noselect' } data-id="${ StringHelper.encodeHtml(a.id) }" ${ c.itemsFocusable ? 'tabindex="-1"' : '' }>${ c.itemContentTpl(a, b) }</li>`;
  }
  itemContentTpl(a, b) {
    if (a.isGroupHeader || a.isParent && this.store.excludeCollapsedRecords !== false) {
      let c = this.groupHeaderTpl(a, a.isParent ? this.itemTpl(a, b) : StringHelper.capitalize(a.meta.groupRowFor));
      if (this.collapsibleGroups || a.isParent) {
        const e = a.isParent ? a.instanceMeta(this.store).collapsed : a.meta.collapsed, f = e ? 'right' : 'down';
        c += '<button class="b-button b-tool b-icon b-icon-collapse-' + f + ' b-list-expander-icon" data-noselect></button>';
      }
      return c;
    }
    return '' + this.itemIconTpl(a, b) + this.itemTpl(a, b);
  }
  itemIconTpl(a, b) {
    return this.multiSelect && this.callback(this.isSelectable, this, [a]) ? '<i class="b-selected-icon b-icon ' + this.selectIconCls + '"></i>' : '';
  }
  selectAllItemTpl() {
    const a = this.allSelected ? 'b-selected' : '';
    return '<li class="' + this.itemCls + ' b-select-all-item ' + a + '" data-noselect data-select-all data-id="select-all" ' + (this.itemsFocusable ? 'tabindex="-1"' : '') + '>' + this.itemIconTpl({}) + '<div>' + this.L('selectAll') + '</div></li>';
  }
  getItemClasses(a) {
    let b, c;
    const e = this, {getItemCls: f} = e, g = (b = e._navigator) == null ? void 0 : b.activeItem, i = (g == null ? void 0 : g.dataset.id) == a.id, j = 'groupRowFor' in a.meta ? 'b-list-item-group-header' : a.isParent ? 'b-list-item-tree-parent' : '', k = e.selected.includes(a) || ((c = a.groupChildren) == null ? void 0 : c.every(n => e.selected.includes(n))), l = f ? this.callback(f, this, [a]) : '';
    return e.itemCls + ' ' + (a.cls || '') + ' ' + l + ' ' + (k ? e.selectedCls : '') + ' ' + (i ? e.navigator.focusCls : '') + ' ' + j;
  }
  onBeforeStoreLoad() {
    this.mask(this.L('L{loading}'));
  }
  onAfterStoreRequest() {
    this.unmask();
  }
  onStoreChange({
    source: a,
    action: b,
    records: c,
    record: e,
    changes: f,
    isCollapse: g
  }) {
    const i = this, {selected: j} = i;
    switch (b) {
    case 'dataset': {
        j.match(a.storage, !i._externallyOwnedSelection);
        break;
      }
    case 'remove':
      if (!g) {
        j.remove(c);
      }
      break;
    case 'clear':
      i.deselectAll();
      break;
    case 'update': {
        const {id: k} = f;
        if (k) {
          DomHelper.setAttributes(i.contentElement.querySelector(StringHelper.encodeAttributeSelector('data-id', k.oldValue)), { 'data-id': k.value });
        }
        i.refreshItem(e);
        return;
      }
    }
    i.refresh();
  }
  onStoreRefresh() {
    if (this._selected) {
      this.refresh();
    } else {
      this.getConfig('selected');
    }
  }
  refresh() {
    let a;
    const b = this;
    if (b.isVisible && b.store) {
      const c = (a = b.navigator) == null ? void 0 : a.activeItem;
      if (b.itemIntersectionObserver) {
        b.itemIntersectionObserver.disconnect();
        b.itemIntersectionObserver = null;
      }
      b.paintListener = null;
      const e = DomHelper.createElementFromTemplate(b.contentTpl(), { 'fragment': true });
      b.clearItems().insertNode(e);
      b.element.classList.toggle('b-virtualized', b.isVirtualized);
      if (b.isVirtualized) {
        b.itemIntersectionObserver = new IntersectionObserver(f => {
          for (const g of f) {
            b.onItemIntersection(g.target, g.isIntersecting, g);
          }
        });
        for (const f of b.element.querySelectorAll(b.itemSelector)) {
          b.itemIntersectionObserver.observe(f);
        }
        b.listItemHeight = b.element.children[0].clientHeight;
      }
      if (c) {
        b.restoreActiveItem(c);
      }
      b.refreshCount = (b.refreshCount || 0) + 1;
      b.element.classList.toggle('b-empty', b.store.count === 0);
      b.updateSelectAllState();
    } else if (!b.paintListener) {
      b.paintListener = b.once('paint', 'refresh');
    }
  }
  clearItems() {
    const a = this, {contentElement: b} = a, c = b.querySelector(a.itemSelector), e = b.lastChild;
    if (c) {
      const f = b.querySelectorAll(a.itemSelector + ':not(.b-mask)');
      itemRange.setStartBefore(c);
      itemRange.setEndAfter(f[f.length - 1]);
      itemRange.deleteContents();
    } else {
      if (e) {
        itemRange.setStartAfter(e);
        itemRange.setEndAfter(e);
      } else {
        itemRange.setStart(b, 0);
        itemRange.setEnd(b, 0);
      }
    }
    return itemRange;
  }
  refreshItem(...a) {
    for (const b of a) {
      const c = this.getItem(b);
      if (c) {
        const e = this.store.indexOf(b), f = DomHelper.createElementFromTemplate(this.itemWrapperTpl(b, e));
        DomHelper.sync(f, c);
      }
    }
  }
  getItem(a) {
    if (a != null) {
      if (typeof a === 'number') {
        return this.contentElement.querySelector('[data-index="' + (a < 0 ? this.store.count + a : a) + '"]');
      } else if (a.nodeType === Element.ELEMENT_NODE) {
        a = a.dataset.id;
      }
      if ((a == null ? void 0 : a.id) != null) {
        a = a.id;
      }
      return this.contentElement.querySelector(StringHelper.encodeAttributeSelector('data-id', a));
    }
  }
  getRecordFromElement(a) {
    if (a.target) {
      a = a.target;
    }
    a = a.closest(this.itemSelector);
    return this.store.getAt(parseInt(a.dataset.index));
  }
  updateItems(a) {
    this.store.data = ArrayHelper.asArray(a != null ? a : []).map(b => {
      if (typeof b === 'string') {
        return { [this.displayField]: b };
      }
      return b;
    });
  }
  updateDisabled(a, b) {
    super.updateDisabled(a, b);
    this.navigator && (this.navigator.disabled = a);
  }
  updateLocalization() {
    super.updateLocalization();
    this.refresh();
  }
  updateTitle() {
    this.refresh();
  }
  changeSelected(a, b) {
    const c = this, e = {
        'change': 'onInternalSelectionChange',
        'thisObj': c
      };
    if (a) {
      if (a.isCollection) {
        c._externallyOwnedSelection = true;
        a.ion(e);
      } else {
        c._externallyOwnedSelection = false;
        a = ArrayHelper.asArray(a).reduce((f, g) => {
          g = g.isModel ? g : c.store.getById(g);
          if (g && c.callback(c.isSelectable, c, [g])) {
            f.push(g);
          }
          return f;
        }, []);
        if (b) {
          b.splice(0, b.count, a);
          a = b;
          c.refresh();
        } else {
          const f = a;
          a = c._selected = new Collection({ 'internalListeners': e });
          a.add(f);
        }
      }
      if (a !== b) {
        b == null ? void 0 : b.un(e);
      }
    } else {
      b == null ? void 0 : b.destroy();
    }
    return a;
  }
  updateSelected(a) {
    if (a) {
      this.refresh();
    }
  }
  get itemSelector() {
    return '.' + this.itemCls;
  }
  get selected() {
    return this._selected;
  }
  updateMultiSelect(a, b) {
    if (!a && b && this.selected.count > 1) {
      this.deselect(this.selected.allValues.slice(1));
    }
  }
  get store() {
    this.getConfig('items');
    return this._store;
  }
  changeStore(a) {
    const b = this;
    if (!(a instanceof Store)) {
      if (Array.isArray(a)) {
        a = { 'data': a };
      }
      a = new Store(a);
    }
    b.detachListeners('store');
    if (a) {
      const c = {
        'name': 'store',
        'change': 'onStoreChange',
        'refresh': 'onStoreRefresh',
        'thisObj': b
      };
      if (a.readUrl) {
        c.beforeLoad = 'onBeforeStoreLoad';
        c.afterRequest = 'onAfterStoreRequest';
      }
      if (a.isGrouped) {
        b.element.classList.add('b-grouped');
        c.groupchange = 'onStoreRefresh';
      }
      a.ion(c);
      if (a.tree) {
        a.traverse(e => {
          if (e.isParent && !e.expanded) {
            a.onNodeExpand(e);
          }
        });
      }
    }
    return a;
  }
  updateStore(a) {
    if (a == null ? void 0 : a.records.length) {
      this.getConfig('selected');
    }
    this.refresh();
  }
  changeNavigator(a, b) {
    const c = this, {element: e} = c;
    if (e) {
      a = Navigator.reconfigure(b, a ? Navigator.mergeConfigs({
        'ownerCmp': c,
        'disabled': c.disabled,
        'target': e,
        'keyEventTarget': e
      }, a) : null, c);
      if ((a == null ? void 0 : a.keyEventTarget) === e && !c.itemsTabbable) {
        e.tabIndex = 0;
      }
    }
    return a;
  }
  get minAlignHeight() {
    const a = this.element.lastElementChild, b = this.minHeight;
    if (b != null) {
      return a ? Math.min(a.offsetTop + a.offsetHeight, b) : 0;
    }
  }
  alignTo(...a) {
    super.minHeight = this.minAlignHeight;
    super.alignTo(...a);
  }
  hide(...a) {
    this.navigator && (this.navigator.activeItem = null);
    return super.hide(...a);
  }
  show() {
    const {refreshCount: a} = this, {previousActiveItem: b} = this.navigator;
    super.minHeight = this._minHeight;
    const c = super.show(...arguments);
    if (this.refreshCount === a) {
      this.restoreActiveItem(b, true);
    }
    return c;
  }
  restoreActiveItem(a = this.navigator.previousActiveItem, b = 'nearest') {
    const c = this, {
        navigator: e,
        lastAlignSpec: f
      } = c;
    if (a != null) {
      if (typeof b !== 'string') {
        const g = f ? f.zone : 1;
        b = g & 1 ? 'nearest' : g ? 'start' : 'end';
      }
      a = c.getItem(a);
      if (a && !c.disabled) {
        c.scrollable.scrollIntoView(a, { 'block': b });
        e.activeItem = a;
      }
    }
  }
  onMouseOver(a) {
    const b = this, c = a.currentTarget;
    if (!DomHelper.isTouchEvent && c && b.navigator && b.activateOnMouseover) {
      b.setTimeout({
        'fn': 'handleMouseOver',
        'delay': 30,
        'args': [c],
        'cancelOutstanding': true
      });
    }
  }
  handleMouseOver(a) {
    this.navigator.activeItem = a;
  }
  onClick(a) {
    const b = a.target.closest(this.itemSelector);
    if (b) {
      this.onItemClick(b, a);
    } else if (this.clearSelectionOnEmptySpaceClick) {
      this.deselectAll();
    }
  }
  onSpaceKey(a) {
    if (a.target.nodeName.toUpperCase() !== 'INPUT' || a.target.readOnly) {
      this.onEnterKey(a);
    }
  }
  onEnterKey(a) {
    const b = this.navigator.activeItem;
    if (b && !this.disabled) {
      this.onItemClick(b, a);
      a.stopImmediatePropagation();
      a.preventDefault();
    }
  }
  onInternalKeyDown(a) {
    const b = this;
    switch (a.key) {
    case 'ArrowLeft':
      b.collapseItem();
      break;
    case 'ArrowRight':
      b.expandItem();
      break;
    case ' ':
      b.onSpaceKey(a);
      break;
    case 'Enter':
      b.onEnterKey(a);
      break;
    }
  }
  collapseItem() {
    const {activeItem: a} = this.navigator;
    if (a) {
      this.toggleCollapse(this.getRecordFromElement(a), true);
    }
  }
  expandItem() {
    const {activeItem: a} = this.navigator;
    if (a) {
      this.toggleCollapse(this.getRecordFromElement(a), false);
    }
  }
  onItemClick(a, b) {
    const c = this, {allowGroupSelect: e} = c, f = parseInt(a.dataset.index), g = c.store.getAt(f), i = g == null ? void 0 : g.isSpecialRow, j = a.contains(b.target.closest('[data-noselect]'));
    if (c.disabled || c.trigger('beforeItem', {
        'item': a,
        'record': g,
        'index': f,
        'event': b,
        'userAction': true
      }) === false) {
      return;
    }
    c._isUserAction = true;
    if (a.contains(b.target.closest('[data-select-all]'))) {
      c.onSelectAllClick(a, b);
    } else if (b.target.matches('.b-list-expander-icon')) {
      c.toggleCollapse(g);
      return;
    } else if (g && !j && (!i || e)) {
      const {selected: k} = c, {groupParent: l} = g.instanceMeta(c.store);
      if (c.multiSelect) {
        let n = k.includes(g), o = g;
        if (c.toggleAllIfCtrlPressed && b.ctrlKey) {
          o = c.store.records;
        } else if (e && i) {
          n = !g.groupChildren.some(p => !k.includes(p));
          a.classList.toggle('b-selected', !n);
          o = g.groupChildren;
        }
        k[n ? 'remove' : 'add'](o);
        if (!i && l && e) {
          const p = c.getItem(l);
          p.classList.toggle('b-selected', l.groupChildren.every(q => k.includes(q)));
        }
      } else {
        k.splice(0, k.count, g);
      }
    }
    if (c.isDestroyed) {
      return;
    }
    c.lastClicked = g;
    c.trigger('item', {
      'item': a,
      'record': g,
      'index': f,
      'event': b,
      'userAction': true
    });
    c._isUserAction = false;
  }
  onInternalSelectionChange({
    action: a,
    removed: b,
    added: c,
    replaced: e
  }) {
    const f = this, {
        selectedCls: g,
        unselectedCls: i
      } = f;
    let j, k;
    if (a === 'clear') {
      for (k of f.element.querySelectorAll('.' + g)) {
        k.classList.remove(g);
        k.setAttribute('aria-selected', false);
        i && k.classList.remove(i);
      }
    } else if (a !== 'replaceValues') {
      for (j of b) {
        k = f.getItem(j);
        if (k) {
          k.classList.remove(g);
          k.setAttribute('aria-selected', false);
        }
      }
      for (j of c) {
        k = f.getItem(j);
        if (k) {
          k.classList.add(g);
          k.setAttribute('aria-selected', true);
        }
      }
    }
    f.selectAllItem && f.updateSelectAllState();
    f.trigger('selectionChange', { 'selected': f.selected.values });
  }
  onSelectAllClick(a) {
    const b = a.classList.contains(this.selectedCls);
    if (b) {
      this.deselectAll();
    } else {
      this.selectAll();
    }
  }
  updateSelectAllState() {
    let a;
    (a = this.element.querySelector('.b-select-all-item')) == null ? void 0 : a.classList.toggle(this.selectedCls, this.allSelected);
  }
  updateAllowGroupSelect() {
    this.refresh();
  }
  get allSelected() {
    const {
      selected: a,
      store: b
    } = this;
    return a.count && (b.isFiltered ? b.records.every(c => a.includes(c)) : b.count === a.count);
  }
  selectAll(a) {
    const b = this;
    if (b.multiSelect || !a) {
      b.selected.add(b.store.records);
      if (a && !b.navigator.activeItem) {
        b.navigator.activeItem = b.getItem(-1);
      }
    }
  }
  deselectAll() {
    this.selected.clear();
  }
  select(a) {
    const {store: b} = this, {modelClass: c} = b;
    a = ArrayHelper.asArray(a).reduce((e, f) => {
      f = b.getById(c.asId(f));
      if (f) {
        e.push(f);
      }
      return e;
    }, []);
    this.selected.add(a);
  }
  deselect(a) {
    const {modelClass: b} = this.store;
    a = ArrayHelper.asArray(a).reduce((e, f) => {
      f = this.selected.get(b.asId(f));
      if (f) {
        e.push(f);
      }
      return e;
    }, []);
    this.selected.remove(a);
  }
  toggleCollapse(a, b) {
    const {store: c} = this;
    if (a.isGroupHeader) {
      b = b === void 0 ? !a.meta.collapsed : b;
      c[b ? 'collapse' : 'expand'](a);
      this.refresh();
      this.trigger('toggleGroup', {
        'groupRecord': a,
        'collapse': b
      });
    } else if (a.isParent) {
      b = b === void 0 ? !a.instanceMeta(c).collapsed : b;
      a.instanceMeta(c).collapsed = b;
      this.refresh();
      this.trigger('toggleNode', {
        'record': a,
        'collapse': b
      });
    }
  }
  updateCollapsibleGroups(a, b) {
    if (b && !a) {
      const {store: c} = this;
      c.groupRecords.forEach(e => c.expand(e));
    }
    this.refresh();
  }
};
List.initClass();
List.prototype.navigatorClass = Navigator;
List._$name = 'List';
const ChipView = class extends List {
  static configurable = {
    'itemCls': 'b-chip',
    'multiSelect': false,
    'closable': true,
    'iconTpl': null,
    'closeHandler': null
  };
  static type = 'chipview';
  static $name = 'ChipView';
  itemContentTpl(a, b) {
    const c = this;
    return TemplateHelper.tpl`${ c.iconTpl ? this.iconTpl(a) : '' }
            ${ c.itemTpl(a, b) }
            ${ c.closable ? '<div class="b-icon b-close-icon b-icon-clear" data-noselect></div>' : '' }`;
  }
  onInternalKeyDown(a) {
    const b = this, {selected: c} = b, {generation: e} = c;
    if (b.closable && c.count && (a.key === 'Delete' || a.key === 'Backspace')) {
      if (b.closeHandler) {
        b.callback(b.closeHandler, b.owner, [
          c.values,
          { 'isKeyEvent': true }
        ]);
      } else {
        b.store.remove(c.values);
      }
      if (c.generation !== e) {
        a.stopImmediatePropagation();
      }
    } else {
      super.onInternalKeyDown(a);
    }
  }
  updateClosable(a) {
    this.element.classList[a ? 'add' : 'remove']('b-chips-closable');
    this.navigator && (this.navigator.disabled = !a);
    if (!this.isConfiguring) {
      this.refresh();
    }
  }
  onClick(a) {
    const b = this, c = a.target.closest('.' + b.itemCls);
    if (b.closable && a.target.classList.contains('b-close-icon')) {
      const e = b.store.getAt(parseInt(c.dataset.index));
      if (b.closeHandler) {
        b.callback(b.closeHandler, b.owner, [[e]]);
      } else {
        b.store.remove(e);
      }
    } else {
      super.onClick(a);
    }
  }
};
ChipView.initClass();
ChipView._$name = 'ChipView';
const errorFieldRequired = 'L{fieldRequired}';
const errorValidateFilter = 'L{validateFilter}';
const errorTooShort = 'L{Field.tooShort}';
const errorRecordNotCommitted = 'L{recordNotCommitted}';
const filterKeyTypes = /[\w\s]/;
const Combo = class extends PickerField {
  static delayable = { 'filterOnInput': 0 };
  static alias = 'combobox,dropdown';
  static type = 'combo';
  static $name = 'Combo';
  static get configurable() {
    return {
      'primaryFilter': {},
      'picker': {
        'type': 'list',
        'floating': true,
        'scrollAction': 'realign',
        'itemsFocusable': false,
        'activateOnMouseover': true,
        'align': {
          'align': 't0-b0',
          'axisLock': true
        },
        'maxHeight': 324,
        'scrollable': { 'overflowY': true },
        'autoShow': false,
        'focusOnHover': false
      },
      'multiSelect': null,
      'items': null,
      'store': null,
      'valueField': void 0,
      'displayField': 'text',
      'clearWhenInputEmpty': null,
      'pickerWidth': null,
      'minChars': null,
      'selected': null,
      'listItemTpl': null,
      'displayValueRenderer': null,
      'listCls': null,
      'triggers': {
        'expand': {
          'cls': 'b-icon-picker',
          'handler': 'onTriggerClick'
        }
      },
      'filterParamName': null,
      'encodeFilterParams': a => a.map(b => b.value),
      'filterOnEnter': false,
      'hideTrigger': null,
      'overlayAnchor': null,
      'keyStrokeFilterDelay': null,
      'defaultAction': 'select',
      'triggerAction': 'all',
      'filterOperator': 'startsWith',
      'caseSensitive': false,
      'hidePickerOnSelect': null,
      'chipView': {
        '$config': [
          'lazy',
          'nullify'
        ],
        'value': { 'type': 'combochipview' }
      },
      'chipStore': {
        '$config': [
          'lazy',
          'nullify'
        ],
        'value': {}
      },
      'filterSelected': null,
      'emptyText': null,
      'value': null,
      'valueCollection': {
        '$config': [
          'nullify',
          'lazy'
        ],
        'value': {}
      },
      'validateFilter': true,
      'clearTextOnPickerHide': true,
      'clearTextOnSelection': true,
      'destroyStore': false,
      'multiValueSeparator': ',',
      'createOnUnmatched': null,
      'role': 'combobox',
      'inlinePicker': null,
      'testConfig': { 'keyStrokeFilterDelay': 0 },
      'localizeDisplayFields': false,
      'buildItems': null,
      'sortItemsOnLocaleChange': false
    };
  }
  static get queryLast() {
    return 'last';
  }
  construct(a) {
    super.construct(...arguments);
    if (!this.store) {
      this.items = [];
    }
    if (this.filterOnEnter) {
      this.hideTrigger = true;
    }
  }
  startConfigure(a) {
    this.usingRecordAsValue = a.valueField === null;
    super.startConfigure(...arguments);
  }
  afterConfigure() {
    super.afterConfigure();
    const a = this;
    if (!ObjectHelper.hasOwn(a, '_value')) {
      a._value = a.valueField === a.displayField ? '' : null;
      a._lastValue = a._lastValue || a._value;
    }
  }
  get childItems() {
    const {_chipView: a} = this, b = super.childItems;
    if (a) {
      b.push(a);
    }
    return b;
  }
  get innerElements() {
    let a;
    const b = (a = this._chipView) == null ? void 0 : a.element, {
        input: c,
        inputElement: e
      } = this;
    if (b) {
      DomSync.sync({
        'targetElement': c,
        'domConfig': e
      });
    }
    return [b || e];
  }
  updateInlinePicker(a) {
    if (a) {
      this.element.classList.add('b-inline-picker');
      this.getConfig('picker');
      this.triggers.expand = null;
      this.pickerVisible = true;
    }
  }
  hidePicker() {
    if (!this.inlinePicker) {
      return super.hidePicker(...arguments);
    }
  }
  changeChipStore(a) {
    if (a && !(a instanceof Store)) {
      a = new Store(Store.mergeConfigs({ 'storage': this.valueCollection }, a));
    }
    return a;
  }
  updateChipStore(a, b) {
    b == null ? void 0 : b.destroy();
  }
  changeChipView(a, b) {
    const c = this;
    c.element.classList[a ? 'add' : 'remove']('b-uses-chipview');
    if (a) {
      const {input: e} = c;
      if (!c.chipStore) {
        c.chipStore = {};
      }
      return ComboChipView.reconfigure(b, a, {
        'defaults': {
          'parent': c,
          'insertBefore': e,
          'store': c.chipStore,
          'closable': !c.readOnly,
          'navigator': {
            'type': 'combochipnavigator',
            'keyEventTarget': e
          }
        }
      });
    }
    b == null ? void 0 : b.destroy();
  }
  updateChipView(a) {
    let b;
    const c = this;
    c._chipViewEventDetacher = (b = c._chipViewEventDetacher) == null ? void 0 : b.call(c);
    c.chipStore = a == null ? void 0 : a.store;
    if (a) {
      a.element.appendChild(c.input);
      c._chipViewEventDetacher = EventHelper.on({
        'element': a.element,
        'mousedown': 'onChipViewMousedown',
        'thisObj': c
      });
    }
  }
  updateMultiSelect(a, b) {
    const c = this, {
        input: e,
        element: f
      } = c, g = !c.isConfiguring;
    let {value: i} = c;
    f.classList[a ? 'add' : 'remove']('b-multiselect');
    if (a) {
      const {chipView: j} = c, {parentNode: k} = e, l = j == null ? void 0 : j.element;
      if (l && l !== k) {
        k.insertBefore(l, e);
        l.appendChild(e);
        c.chipView.refresh();
      }
      e.value = '';
      if (g) {
        i = ArrayHelper.asArray(i);
      }
    } else {
      const n = c._chipView, {parentNode: o} = e, p = n == null ? void 0 : n.element;
      if (p === o) {
        p.parentNode.insertBefore(e, p);
        p.remove();
        f.classList.remove('b-uses-chipview');
      }
      if (g && typeof i !== 'string') {
        i = (i == null ? void 0 : i.length) ? i[0] : null;
      }
    }
    if (g) {
      c.value = i;
    }
  }
  onChipViewMousedown(a) {
    a.preventDefault();
    if (!this.containsFocus) {
      this.focus();
    }
  }
  onChipClose(a, b = {}) {
    if (b.isKeyEvent && this.input.value === '' || !b.isKeyEvent) {
      this._isUserAction = true;
      this.valueCollection.remove(a);
      this._isUserAction = false;
    }
  }
  updateFilterParamName(a) {
    if (this.hideTrigger !== false) {
      this.hideTrigger = Boolean(a);
    }
  }
  updateHideTrigger(a) {
    this.element.classList[a ? 'add' : 'remove']('b-hide-trigger');
  }
  updateKeyStrokeFilterDelay(a) {
    this.filterOnInput.delay = a;
  }
  updateReadOnly(a) {
    super.updateReadOnly(...arguments);
    this._chipView && (this._chipView.closable = !a);
  }
  updateDisabled(a, b) {
    super.updateDisabled(a, b);
    this._chipView && (this._chipView.closable = !a);
  }
  updateFilterOperator(a) {
    if (this.primaryFilter) {
      this.primaryFilter.operator = a;
    }
  }
  get minChars() {
    let a;
    return (a = this._minChars) != null ? a : this.remoteFilter ? 4 : 1;
  }
  get validateFilter() {
    return this._validateFilter && !this.createOnUnmatched;
  }
  get items() {
    return this.store.allRecords;
  }
  updateBuildItems(a) {
    if (a) {
      this.items = a.call(this);
    }
  }
  changeItems(a) {
    const b = this;
    if (a == null) {
      if (b.store && !b.store.isItemStore) {
        return;
      }
      a = [];
    }
    if (b.buildItems && !(a == null ? void 0 : a.length)) {
      a = b.buildItems();
    }
    if (a instanceof Store) {
      b.store = a;
      return;
    }
    const c = b.displayField;
    let e, f = b.valueField, g;
    if (Array.isArray(a)) {
      g = a.map(i => {
        let j = i;
        if (i instanceof Model) {
          e = i.constructor;
          if (!f) {
            b.valueField = f = e.idField;
          }
        } else {
          if (typeof i === 'string' || typeof i === 'number') {
            if (!f) {
              b.valueField = f = b.displayField;
            }
            j = {
              [f]: i,
              [c]: i
            };
          } else {
            if (!f) {
              b.valueField = f = 'value';
            }
            if (Array.isArray(i)) {
              j = {
                [f]: i[0],
                [c]: i[1]
              };
            }
          }
        }
        if (i.selected) {
          b.value = j;
        }
        return j;
      });
    } else {
      if (!f) {
        b.valueField = f = 'value';
      }
      g = [];
      Object.entries(a).forEach(([i, j]) => {
        g.push({
          [f]: i,
          [c]: j
        });
      });
    }
    if (b.store) {
      b.store.data = g;
    } else {
      const i = f === c ? {
        'name': 'value',
        'dataSource': c
      } : f;
      if (!e) {
        class j extends Model {
          static get idField() {
            return b.valueField;
          }
          static set idField(k) {
            super.idField = k;
          }
          static get fields() {
            const k = [
              i,
              c
            ];
            if (b.localizeDisplayFields) {
              k.push('localeClass');
            }
            return k;
          }
        }
        e = j;
      }
      b.store = new Store({
        'isItemStore': true,
        'data': g,
        'idField': f,
        'verifyNoGeneratedIds': false,
        'modelClass': e,
        'useRawData': false
      });
    }
  }
  get value() {
    const a = this, {
        valueCollection: b,
        valueField: c
      } = a;
    if (c == null) {
      return a.multiSelect ? b.values.slice() : b.first;
    }
    let e;
    if (a.multiSelect) {
      e = b.count ? b.map(f => f[c]) : a._lastValue || [];
    } else {
      e = b.count ? b.first[c] : a._lastValue;
    }
    return e;
  }
  set value(a) {
    super.value = a;
  }
  get needsInputSync() {
    return this.usesChipView ? false : !this.editable;
  }
  changeValue(a, b) {
    let c, e;
    const f = this;
    if (a === b) {
      f.syncInputFieldValue(true);
      return;
    }
    if (!f.multiSelect && Array.isArray(a) && a.length > 1) {
      throw new Error('Multiple values cannot be set to a non-multiSelect Combo');
    }
    f.triggerConfig('items');
    if (!f.inputting) {
      (c = f._chipView) == null ? void 0 : c.selected.clear();
    }
    if (!f.store) {
      f.items = [];
    }
    const {
        valueField: g,
        displayField: j,
        store: k,
        valueCollection: l,
        _picker: n
      } = f, {storage: o} = k, p = (e = f.hidePickerOnSelect) != null ? e : !f.multiSelect, q = f._isUserAction || (n == null ? void 0 : n._isUserAction) || p && f.pickerVisible || false;
    if (!f.remoteFilter && k.isAjaxStore && !k.count) {
      k.ion({
        'load': () => f.value = a,
        'once': true,
        'thisObj': f
      });
      f._lastValue = a;
      return;
    }
    let r;
    if (a != null) {
      const u = Array.isArray(a), v = u ? a.slice() : [a];
      if (f.remoteFilter) {
        if (a != null) {
          if (ObjectHelper.isObject(a) || a.isModel) {
            if (!k.getById(a.id)) {
              k.data = [a];
              f.valueCollection.splice(0, f.valueCollection.count, k.first);
            } else {
              f.valueCollection.splice(0, f.valueCollection.count, k.getById(a.id));
            }
          } else {
            const A = f.isConfiguring;
            f.primaryFilter.setConfig({
              'value': a,
              'disabled': false
            });
            k.performFilter(true).then(() => {
              if (f.isDestroyed) {
                return;
              }
              const {isConfiguring: B} = f;
              f.isConfiguring = A;
              l.splice(0, l.count, k.allRecords);
              f.isConfiguring = B;
            });
          }
          return;
        }
      } else if (k.isFiltered) {
        if (k.isChained) {
          k.fillFromMaster();
        }
        f.primaryFilter.disabled = true;
        k.filter();
      } else if (k.isChained) {
        k.fillFromMaster();
      }
      for (let B = 0, C = v.length; B < C; B++) {
        let E = v[B];
        if (E instanceof Model) {
          if (!o.includes(E, true)) {
            k.add(E);
          }
        } else {
          const F = ObjectHelper.isObject(E);
          if (F && g) {
            E = E[k.modelClass.fieldMap[g].dataSource];
          }
          r = o.getBy(j, E) || o.getBy(g, E) || (l.getBy(j, E) || l.getBy(g, E));
          if (r instanceof Set) {
            r = [...r][0];
          }
          if (r) {
            if (F) {
              r.set(v[B]);
            }
            v[B] = r;
          } else {
            v.splice(B, 1);
            C--;
            B--;
          }
        }
      }
      const w = !v.length;
      const z = l.generation;
      l.splice(0, l.count, v);
      if (w) {
        f._value = u && a.length === 0 ? null : a;
        f._lastValue = f._value;
      }
      if (w || l.generation === z) {
        f.syncInputFieldValue();
      }
      f.syncEmpty();
      if (w && !f.isConfiguring) {
        f.triggerFieldChange({
          'value': a,
          'oldValue': b,
          'userAction': q,
          'valid': f.isValid
        });
      }
    } else {
      if (l.count) {
        l.clear();
      } else {
        const G = f._value;
        f._value = null;
        f.syncInputFieldValue();
        f.syncEmpty();
        if (!f.isConfiguring) {
          f.triggerFieldChange({
            'value': a,
            'oldValue': G,
            'userAction': q,
            'valid': f.isValid
          });
        }
      }
    }
    f._lastValue = f._value;
  }
  hasChanged(a, b) {
    if (this.multiSelect) {
      return !ObjectHelper.isEqual(a, b);
    }
    return super.hasChanged(...arguments);
  }
  onComboStoreChange({action: a}) {
    if (a !== 'filter') {
      this.syncInputFieldValue(true);
    }
  }
  syncInputFieldValue(a) {
    let b;
    if (this.usesChipView) {
      (b = this.chipView) == null ? void 0 : b.refresh();
    } else {
      super.syncInputFieldValue(a);
    }
  }
  get usesChipView() {
    return Boolean(this.multiSelect && this._chipView);
  }
  get isEmpty() {
    return this.valueCollection.count === 0;
  }
  get inputValue() {
    const a = this;
    let b = a.selected ? a.selected[a.displayField] : a.value;
    if (a.displayValueRenderer) {
      b = a.callback(a.displayValueRenderer, a, [
        a.selected,
        a
      ]);
    }
    return b == null ? '' : b;
  }
  get nonEditableClickTarget() {
    let a;
    return this.multiSelect && ((a = this.chipView) == null ? void 0 : a.element) || super.nonEditableClickTarget;
  }
  changeValueCollection(a, b) {
    b == null ? void 0 : b.destroy();
    if (a) {
      if (!a.isCollection) {
        a = new Collection({
          'internalListeners': {
            'noChange': 'onValueCollectionNoChange',
            'change': 'onValueCollectionChange',
            'prio': -1000,
            'thisObj': this
          }
        });
      }
      return a;
    }
  }
  changePrimaryFilter(a) {
    if (a.isCollectionFilter) {
      a.setConfig({
        'disabled': true,
        'property': this.displayField,
        'operator': this.filterOperator,
        'caseSensitive': this.caseSensitive
      });
    } else {
      if (typeof a === 'function') {
        a = { 'filterBy': a };
      }
      a = new CollectionFilter({
        'id': 'primary',
        'disabled': true,
        'property': this.displayField,
        'operator': this.filterOperator,
        'caseSensitive': this.caseSensitive,
        ...a
      });
    }
    return a;
  }
  changeStore(a) {
    const b = this, c = [], {
        valueCollection: e,
        keyStrokeFilterDelay: f,
        filterParamName: g
      } = b;
    if (Array.isArray(a)) {
      b.items = a;
      return;
    }
    let i;
    if (a) {
      if (a.isStore) {
        const j = a.filters.get('primary');
        if (b.remoteFilter) {
          a.filterParamName = g;
        }
        i = a.remoteFilter;
        if (j) {
          b.primaryFilter = j;
        } else {
          c.push(b.primaryFilter);
        }
      } else {
        if (typeof a === 'string') {
          a = Store.getStore(a);
        } else {
          a = new (a.readUrl ? AjaxStore : Store)(a);
          b.destroyStore = true;
        }
        i = b.remoteFilter || a.restfulFilter;
        if (i && g) {
          a.filterParamName = g;
          if (b.encodeFilterParams) {
            a.encodeFilterParams = b.encodeFilterParams;
          }
        }
        c.push(b.primaryFilter);
      }
      if (!b.valueField && !b.usingRecordAsValue) {
        b.valueField = a.modelClass.idField;
      }
      if (b.filterSelected) {
        const k = l => !b.containsFocus || !e.includes(l);
        if (i) {
          a.storage.autoFilter = true;
          a.storage.addFilter({
            'id': b.id + '-selected-filter',
            'filterBy': k
          });
        } else {
          c.push(k);
          a.reapplyFilterOnAdd = true;
        }
      }
      e.addIndex({
        'property': b.displayField,
        'unique': false
      });
      e.addIndex({
        'property': b.valueField,
        'unique': true
      });
      c.forEach(l => a.addFilter(l, true));
      if (i) {
        b.keyStrokeFilterDelay = Math.max(300, f || 0);
      } else {
        b.keyStrokeFilterDelay = f != null ? f : 10;
      }
    }
    return a;
  }
  updateStore(a, b) {
    const c = this, {_picker: e} = c;
    let f;
    if (c.destroyStore && b) {
      b.destroy();
    }
    if (e) {
      e.store = a;
    }
    a.storage.addIndex({
      'property': c.displayField,
      'unique': false
    });
    a.storage.addIndex({
      'property': c.valueField,
      'unique': true
    });
    f = { 'filter': 'onStoreFilter' };
    if (c.displayValueRenderer) {
      (f || (f = {})).change = 'onComboStoreChange';
    }
    c.detachListeners('store');
    (a == null ? void 0 : a.storage) && c.valueCollection.match(a.storage);
    c.syncInputFieldValue();
    if (f) {
      f.name = 'store';
      f.thisObj = c;
      a.ion(f);
    }
  }
  get remoteFilter() {
    let a;
    return Boolean(this.filterParamName || ((a = this._store) == null ? void 0 : a.restfulFilter));
  }
  get record() {
    return this.selected;
  }
  get records() {
    return this.valueCollection.values.slice();
  }
  get selected() {
    const {store: a} = this;
    return this.multiSelect ? this.valueCollection.values.slice().sort((b, c) => a.indexOf(b) - a.indexOf(c))[0] : this.valueCollection.first;
  }
  onEditComplete() {
    const a = this, b = a.valueCollection.count;
    super.onEditComplete();
    if (b) {
      a.clearError(errorValidateFilter);
      a.syncInputFieldValue();
    }
    if (a.required && !b) {
      a.setError(errorFieldRequired);
    }
  }
  onTriggerClick(a) {
    let b;
    const c = this, e = 'key' in a;
    if (c.remoteFilter && c.filterOnEnter) {
      return;
    }
    if (c.pickerVisible) {
      c.hidePicker();
    } else if (!c.readOnly && !c.disabled) {
      switch ((b = c.triggerAction) == null ? void 0 : b.toLowerCase()) {
      case 'all':
        c.doFilter(null, e);
        break;
      case 'last':
        c.doFilter(c.lastQuery, e);
        break;
      default:
        c.doFilter(c.input.value, e);
      }
    }
  }
  internalOnInput(a) {
    const b = this;
    if (b.clearWhenInputEmpty && b.input.value === '') {
      b.clear();
    }
    b.syncEmpty();
    b.syncInputWidth();
    b.filterOnInput(a);
    b.trigger('input', {
      'value': b.input.value,
      'event': a
    });
  }
  filterOnInput(a) {
    const b = this, {value: c} = a.type === 'input' ? a.target : b.input, e = c.length;
    b.inputting = true;
    if (b.inlinePicker || e >= b.minChars && (!b.filterOnEnter || a.key === 'Enter')) {
      b.doFilter(c);
    } else {
      if (b.validateFilter && !b.remoteFilter) {
        b[e ? 'setError' : 'clearError'](errorValidateFilter);
      }
      b.hidePicker();
    }
    b.inputting = false;
  }
  syncInputWidth() {
    const a = this;
    if (a.usesChipView) {
      const b = a.input, c = a._inputPadding || (a._inputPadding = DomHelper.getEdgeSize(b, 'padding', 'lr')), e = b.value || '', f = DomHelper.measureText(e + 'W', b, false, a.element) + c.width;
      b.style.flex = '1 1 ' + Math.ceil(f) + 'px';
    }
  }
  doFilter(a, b) {
    let c;
    const e = this, {
        store: f,
        picker: g
      } = e, i = a == null || a === '';
    e.lastQuery = a;
    e.primaryFilter.setConfig({
      'value': a,
      'disabled': i
    });
    if (e.remoteFilter) {
      f.clear(true);
    }
    const j = () => {
      const {
        navigator: k,
        isVisible: l
      } = g;
      if (f.count) {
        if (!i && k) {
          k.activeItem = 0;
        }
      } else if (!e.remoteFilter && !i) {
        if (k) {
          k.activeItem = null;
        }
        if (e.validateFilter) {
          e.setError(errorValidateFilter);
        }
      }
      l && g.realign();
    };
    (c = e.filterPromise = f.filter()) == null ? void 0 : c.then(() => {
      e.filterPromise = null;
      j();
    });
    if (!e.inlinePicker) {
      if (g == null ? void 0 : g.isVisible) {
        if (g.lastAlignSpec.zone === 0) {
          g.realign();
        }
      } else {
        e.showPicker(b);
      }
    }
    if (!e.filterPromise) {
      j();
    }
  }
  onStoreFilter({source: a}) {
    const b = this, c = b._picker, e = c == null ? void 0 : c.element.dataset, {count: f} = a;
    if (b.remoteFilter) {
      if (f) {
        if (c) {
          c.navigator.activeItem = 0;
        }
      } else {
        if (b.validateFilter) {
          b.setError(errorValidateFilter);
        }
      }
      if (b.filterSelected && b.valueCollection.count) {
        a.storage.onFiltersChanged({
          'action': 'splice',
          'oldCount': 1
        });
        if (c) {
          c.refresh();
        }
      }
    }
    if (e) {
      if (b.createOnUnmatched && !f && b.primaryFilter.value != null) {
        e.addNewValue = b.L('L{addNewValue}')(b.primaryFilter.value);
      } else {
        e == null ? true : delete e.addNewValue;
      }
    }
  }
  onValueCollectionChange({source: a}) {
    let b, c, e, f;
    const g = this, {
        multiSelect: i,
        _picker: j
      } = g, k = (b = g.hidePickerOnSelect) != null ? b : !i, l = i ? a.values.slice() : a.first, n = a.values.slice(), o = g._isUserAction || (j == null ? void 0 : j._isUserAction) || k && g.pickerVisible || false, p = g._value;
    if (k) {
      g.hidePicker();
    }
    if (i && g.clearTextOnSelection) {
      g.input.value = '';
    }
    if (!a.count && g.required) {
      g.setError(errorFieldRequired);
    } else {
      g.clearError(errorFieldRequired);
      g.clearError(errorValidateFilter);
    }
    if (g.validateFilter && l) {
      g.clearError(errorValidateFilter);
    }
    if (g.filterSelected) {
      const r = j == null ? void 0 : j.navigator, u = Number((e = (c = r == null ? void 0 : r.activeItem) == null ? void 0 : c.dataset) == null ? void 0 : e.index);
      if (g.remoteFilter) {
        g.store.storage.onFiltersChanged({
          'action': 'splice',
          'oldCount': 1
        });
        if (g._picker) {
          g._picker.refresh();
        }
      } else {
        g.store.filter();
      }
      if (!isNaN(u)) {
        r.activeItem = Math.min(j.store.count, u);
      }
    }
    g._value = null;
    g._lastValue = null;
    const q = g.cacheCurrentValue(g.value);
    g.syncInputFieldValue();
    g.syncEmpty();
    if (!g.isConfiguring) {
      if (q !== p && !g.onConfigChange.$nullFn) {
        g.onConfigChange({
          'name': 'value',
          'value': q,
          'was': p,
          'config': g.$meta.configs.value
        });
      }
      g.triggerFieldChange({
        'value': q,
        'oldValue': p,
        'userAction': o,
        'valid': g.isValid
      });
      (f = g.trigger) == null ? void 0 : f.call(g, 'select', {
        'record': l,
        'records': n,
        'userAction': o
      });
      if (g.defaultAction === 'select') {
        g.trigger('action', {
          'value': q,
          'record': l,
          'records': n,
          'userAction': o
        });
      }
    }
  }
  cacheCurrentValue(a) {
    return this._value = a;
  }
  onValueCollectionNoChange({toAdd: a}) {
    if (!this.inlinePicker && !this.multiSelect && a.length && this.pickerVisible) {
      this.picker.hide();
      this.syncInputFieldValue();
    }
  }
  showPicker() {
    let a;
    const b = this, {picker: c} = b;
    if (b.readOnly || b.inlinePicker) {
      return;
    }
    c.multiSelect = b.multiSelect;
    super.showPicker(...arguments);
    if (b.overlayAnchor && !c.align.offset) {
      c.align.offset = -c.anchorSize[1];
      c.realign();
    }
    if (c.restoreActiveItem) {
      if (b.multiSelect) {
        c.restoreActiveItem(((a = c.navigator) == null ? void 0 : a.previousActiveItem) || b.selected || 0, true);
      } else {
        c.restoreActiveItem(b.selected || 0, true);
      }
    }
    b.input.focus();
  }
  changePicker(a, b) {
    if (a) {
      const c = this, e = c.pickerWidth || (a == null ? void 0 : a.width), f = List.mergeConfigs({
          'owner': c,
          'store': c.store,
          'selected': c.valueCollection,
          'multiSelect': c.multiSelect,
          'cls': c.listCls,
          'displayField': c.displayField,
          'forElement': c.pickerAlignElement,
          'align': {
            'matchSize': e == null ? 'min' : false,
            'anchor': c.overlayAnchor,
            'target': c.pickerAlignElement,
            'minHeight': c.inlinePicker ? null : Math.min(3, c.store.count) * 40
          },
          [c.listItemTpl ? 'itemTpl' : void 0]: c.listItemTpl,
          'width': e,
          'navigator': { 'keyEventTarget': c.input }
        }, a);
      if (c.inlinePicker) {
        Object.assign(f, {
          'floating': false,
          'align': null,
          'activateOnMouseover': false,
          'maxHeight': null,
          'appendTo': c.element
        });
      }
      a = List.reconfigure(b, a ? f : null, c);
      a.element.classList.add('b-combo-picker');
      a.element.dataset.emptyText = c.emptyText ? c.L(c.emptyText) : c.L('L{noResults}');
      a.ion({
        'navigate': 'onPickerNavigate',
        'thisObj': c
      });
      EventHelper.on({
        'element': a.element,
        'pointerdown': g => c.onPickerNavigate({ 'event': g }),
        'thisObj': c
      });
    } else {
      b == null ? void 0 : b.destroy();
    }
    return a;
  }
  onPickerNavigate({event: a}) {
    if (a.target.matches('[data-add-new-value]')) {
      this.addNewRecord(this.primaryFilter.value);
    }
  }
  onPickerHide() {
    const a = this;
    super.onPickerHide();
    if (a.multiSelect && a.clearTextOnPickerHide && !a.errors) {
      a.input.value = '';
    }
  }
  async internalOnKeyEvent(a) {
    let b;
    const c = this, e = c.input[c.inputValueAttr], f = e.length, {key: g} = a, {
        primaryFilter: i,
        store: j,
        multiSelect: k,
        minChars: l,
        _picker: n,
        _chipView: o
      } = c;
    let p;
    if (a.type === 'keydown' && (n == null ? void 0 : n.isVisible) && n.navigator) {
      const {activeItem: q} = n.navigator;
      if (q && k && g === c.multiValueSeparator) {
        c.input.value = '';
        n.onItemClick(q, a);
        i.setConfig({
          'value': '',
          'disabled': true
        });
        j.filter();
        n.hide();
        a.preventDefault();
        return;
      }
      if (!q && c.createOnUnmatched && (k && g === c.multiValueSeparator || g === 'Enter')) {
        a.preventDefault();
        await c.addNewRecord(e);
        return;
      }
    }
    if (c.editable) {
      if (o && !o.selected.count && a.type === 'keydown' && g === 'Backspace' && !f) {
        o.navigator.activeItem = c.valueCollection.count - 1;
        a.stopImmediatePropagation();
      }
    } else if (c.pickerVisible) {
      if (g === 'Backspace' && ((b = i.value) == null ? void 0 : b.length)) {
        i.value = i.value.substring(0, i.value.length - 1);
        c.store.filter();
        n.restoreActiveItem(0);
      } else if (a.type === 'keypress' && g.match(filterKeyTypes)) {
        if (!c.lastFilterKeypressTime || a.timeStamp - c.lastFilterKeypressTime > 2000) {
          i.value = '';
        }
        i.setConfig({
          'value': (i.value || '') + g,
          'disabled': false
        });
        c.store.filter();
        n.restoreActiveItem(0);
      }
      c.lastFilterKeypressTime = a.timeStamp;
    }
    if (c.pickerVisible && g === 'Enter' && c.primaryFilter.value === c.input.value) {
      p = true;
    }
    super.internalOnKeyEvent(...arguments);
    if (a.type === 'keydown' && g === 'Enter' && !p && c.remoteFilter) {
      if (f >= l) {
        a.stopPropagation();
        c.filterOnInput.now(a);
      } else if (f > 0) {
        a.stopPropagation();
        c.setError(errorTooShort + (' (' + l + ')'), false, true);
      }
    }
  }
  async addNewRecord(a) {
    let b, c;
    const e = this, {
        store: f,
        _picker: g,
        valueCollection: i,
        multiSelect: j,
        primaryFilter: k
      } = e, l = f.remoteFilter && f.autoCommit;
    if (e.filterPromise) {
      await e.filterPromise;
    }
    if (!l) {
      k.setConfig({
        'value': '',
        'disabled': true
      });
      f.filter();
    }
    e._isUserAction = true;
    const [n] = f.add(e.callback(e.createOnUnmatched, e, [
      a,
      e
    ]));
    if (f.isCommitting) {
      let o;
      try {
        await f.autoCommitTimeoutPromise;
        await f.commitPromise;
      } catch (p) {
        o = (c = (b = p.response) == null ? void 0 : b.parsedJson) == null ? void 0 : c.error;
      }
      if (n.isPhantom) {
        e.clearError();
        e.setError(o || errorRecordNotCommitted, false, true);
        f.remove(n);
        f.removed.remove(n);
      }
    }
    if (l) {
      k.setConfig({
        'value': '',
        'disabled': true
      });
      f.filter();
    }
    e.input.value = '';
    if (f.includes(n)) {
      i.splice(j ? i.count : 0, j ? 0 : i.count, n);
    }
    g == null ? void 0 : g.hide();
    e._isUserAction = false;
  }
  changeCreateOnUnmatched(a) {
    if (a === true) {
      a = this.defaultRecordCreator;
    }
    return a;
  }
  defaultRecordCreator(a) {
    return this.store.createRecord({ [this.displayField]: a });
  }
  updateLocalizeDisplayFields(a) {
    if (a) {
      this.localizableProperties.push('store');
    } else {
      ArrayHelper.remove(this.localizableProperties, 'store');
    }
  }
  localizeProperty(a) {
    if (a === 'store') {
      return this.localizeStore();
    }
    return super.localizeProperty(a);
  }
  localizeStore() {
    let a;
    const b = this, {displayField: c} = b;
    let {localizedDisplayFieldsMap: e} = b;
    if (!e) {
      b.localizedDisplayFieldsMap = e = new Map();
    }
    if (!b.store && b.buildItems) {
      b.items = b.buildItems();
    }
    for (const f of b.items) {
      if (f.id) {
        let g = e.get(f.id);
        if (!g && ((a = f[c]) == null ? void 0 : a.startsWith('L{'))) {
          g = f[c];
          e.set(f.id, g);
        }
        if (g) {
          f[c] = (f.localeClass || b).L(g);
        }
      }
    }
    if (b.sortItemsOnLocaleChange) {
      b.store.sort();
    }
    b.syncInputFieldValue();
  }
  createOverflowTwin(a) {
    return super.createOverflowTwin({
      'store': this.store,
      ...a
    });
  }
};
var ComboChipView = class extends ChipView {
  static get $name() {
    return 'ComboChipView';
  }
  static get type() {
    return 'combochipview';
  }
  static get defaultConfig() {
    return {
      'closeHandler': 'up.onChipClose',
      'itemsFocusable': false,
      'multiSelect': true,
      'itemTpl'(a) {
        return StringHelper.encodeHtml(a.getValue(this.owner.displayField));
      },
      'scrollable': { 'overflowY': 'auto' }
    };
  }
};
const ComboChipNavigator = class extends Navigator {
  static get $name() {
    return 'ComboChipNavigator';
  }
  static get type() {
    return 'combochipnavigator';
  }
  static get configurable() {
    return { 'allowShiftKey': true };
  }
  onTargetClick(a) {
    const b = this, c = a.target.closest(b.itemSelector);
    if (c && !a.target.classList.contains('b-close-icon')) {
      if (!a.shiftKey && !c.contains(a.target.closest('[data-noselect]'))) {
        b.ownerCmp.selected.clear();
      }
      b.inClickHandler = true;
      b.activeItem = c;
      b.inClickHandler = false;
    }
  }
  onKeyDown(a) {
    if (a.key !== 'Enter' && !a.target.selectionStart && !a.target.selectionEnd) {
      super.onKeyDown(a);
    }
  }
  updateActiveItem(a, b) {
    const c = this.ownerCmp;
    super.updateActiveItem(a, b);
    if (a && !this.inClickHandler) {
      c.selected.add(c.getRecordFromElement(a));
    }
  }
  navigatePrevious(a) {
    const b = this.ownerCmp;
    if (b.navigator.activeItem && !a.shiftKey) {
      b.selected.clear();
    }
    if (this.previous) {
      super.navigatePrevious(a);
    } else {
      this.activeItem = null;
    }
  }
  navigateNext(a) {
    const b = this.ownerCmp;
    if (b.navigator.activeItem && !a.shiftKey) {
      b.selected.clear();
    }
    if (this.next) {
      super.navigateNext(a);
    } else {
      this.activeItem = null;
    }
  }
};
Combo.initClass();
ComboChipView.initClass();
ComboChipNavigator.initClass();
Combo._$name = 'Combo';
const BooleanCombo = class extends Combo {
  static type = 'booleancombo';
  static $name = 'BooleanCombo';
  static get configurable() {
    return {
      'positiveValue': true,
      'positiveText': null,
      'negativeValue': false,
      'negativeText': null,
      'store': {
        'value': [],
        '$config': 'lazy'
      },
      'value': false
    };
  }
  changeStore(a, b) {
    const c = this;
    return super.changeStore(new Store({
      'data': [
        {
          'id': c.positiveValue,
          'text': c.positiveText || c.L('L{Object.Yes}')
        },
        {
          'id': c.negativeValue,
          'text': c.negativeText || c.L('L{Object.No}')
        }
      ]
    }), b);
  }
};
BooleanCombo.initClass();
BooleanCombo._$name = 'BooleanCombo';
const toggleOrderFn = ({$toggleOrder: c}, {$toggleOrder: e}) => (e || 0) - (c || 0);
const buttonListeners = {
  'click': 'resetValueCache',
  'beforeToggle': 'onItemBeforeToggle',
  'toggle': 'onItemToggle'
};
const _ButtonGroup = class _ButtonGroup extends Container.mixin(Rotatable_default, Validatable_default) {
  static configurable = {
    'defaultType': 'button',
    'cls': null,
    'items': null,
    'color': null,
    'useGap': false,
    'toggleGroup': null,
    'valueSeparator': ',',
    'columns': null,
    'hideWhenEmpty': true,
    'defaultBindProperty': 'value',
    'errorTooltipCachePath': 'buttonGroupErrorTooltip',
    'errorTooltipConfig': { 'forSelector': '.b-buttongroup.b-invalid' },
    'overflowable': true,
    'toggleable': null
  };
  static type = 'buttongroup';
  static $name = 'ButtonGroup';
  afterConstruct() {
    super.afterConstruct();
    this.ion({ 'change': 'internalOnChange' });
  }
  get inputWrap() {
    return this.element;
  }
  get isValid() {
    let a = super.isValid;
    const b = this, {toggleable: c} = b;
    b.clearError('L{Field.fieldRequired}', true);
    if (a && !b.disabled) {
      if (b.required && !b.value || c && b.pressed.length < c[0]) {
        a = false;
        b.setError('L{Field.fieldRequired}', true);
      }
    }
    if (b.checkValidity && b.callback(b.checkValidity, b, [b]) === false) {
      a = false;
    }
    if (a && b.errors && Object.keys(b.errors).length) {
      a = false;
    }
    return a;
  }
  get pressed() {
    return this.items.filter(a => a.pressed).sort(toggleOrderFn);
  }
  internalOnChange() {
    this.syncInvalid();
  }
  onChildAdd(a) {
    super.onChildAdd(a);
    a.ion({
      'thisObj': this,
      ...buttonListeners,
      'prio': 10000
    });
  }
  onChildRemove(a) {
    a.un({
      'thisObj': this,
      ...buttonListeners
    });
    super.onChildRemove(a);
  }
  onItemBeforeToggle({pressed: a}) {
    const {toggleable: b} = this;
    return a || !b || this.pressed.length > b[0];
  }
  onItemToggle(a) {
    const b = this, c = (b.toggleCount || 0) + 1, {toggleable: e} = b;
    b.resetValueCache();
    if (!b.isSettingValue && (!b.toggleGroup || a.pressed)) {
      if (e) {
        a.source.$toggleOrder = c;
        b.toggleCount = c;
        b.isSettingValue = true;
        b.pressed.slice(e[1]).forEach(f => f.pressed = false);
        b.isSettingValue = false;
      }
      b.triggerFieldChange({
        'value': b.value,
        'userAction': true,
        'event': a
      });
    }
  }
  resetValueCache() {
    this._value = null;
  }
  createWidget(a) {
    const b = this, c = b.constructor.resolveType(a.type || 'button');
    if (c.isButton) {
      if (b.color && !a.color) {
        a.color = b.color;
      }
      if (!a.toggleGroup && b.toggleGroup) {
        a.toggleGroup = b.toggleGroup;
      }
      if (b.toggleable) {
        a.toggleable = true;
      }
    }
    if (b.columns) {
      a.width = 100 / b.columns + '%';
    }
    a = super.createWidget(a);
    b.relayEvents(a, [
      'click',
      'action',
      'toggle'
    ]);
    return a;
  }
  changeToggleable(a) {
    if (a === true) {
      a = [
        0,
        Infinity
      ];
    } else if (typeof a === 'number') {
      a = [
        1,
        a
      ];
    }
    return a || null;
  }
  updateToggleable(a) {
    if (a && !this.isConstructing) {
      this.pressed.slice(a[1]).forEach(c => c.pressed = false);
    }
  }
  changeToggleGroup(a, b) {
    return a === true ? b || _ButtonGroup.generateId('toggleGroup') : a;
  }
  updateRotate(a) {
    this.eachWidget(b => {
      if (b.rotate !== false) {
        b.rotate = a;
      }
    });
  }
  get valueAsArray() {
    const a = [];
    this.items.forEach(b => {
      if (b.pressed && b.value !== void 0) {
        a.push(b.value);
      }
    });
    return a;
  }
  get value() {
    const a = this;
    if (!a._value) {
      a._value = a.valueAsArray.join(a.valueSeparator);
    }
    return a._value;
  }
  set value(a) {
    const b = this, c = b.value;
    if (a == null) {
      a = [];
    } else if (typeof a == 'string') {
      a = a.split(b.valueSeparator);
    } else if (!Array.isArray(a)) {
      a = [a];
    }
    b._value = a.join(b.valueSeparator);
    b.isSettingValue = true;
    b.items.forEach(e => {
      if (e.value !== void 0) {
        e.pressed = a.includes(e.value);
      }
    });
    b.isSettingValue = false;
    if (!b.isConfiguring && c !== b.value) {
      b.triggerFieldChange({
        'value': b.value,
        'userAction': Boolean(b._isUserAction)
      });
    }
  }
  get validity() {
    return {};
  }
  onDisabled(a) {
    this.syncInvalid();
    this.items.forEach(b => b.disabled = a || !b.ignoreParentReadOnly && this.readOnly);
  }
  updateReadOnly(a) {
    super.updateReadOnly(a);
    this.updateDisabled(this.disabled);
  }
  addToOverflowMenu(a, b, c) {
    const {
      toggleable: e,
      toggleGroup: f
    } = this;
    if (f || (e == null ? void 0 : e[0]) === 1 && e[1] === 1) {
      b = {
        ...b,
        'radio': true
      };
    }
    this.items.forEach(g => g.addToOverflowMenu(a, b, c));
  }
  get widgetClassList() {
    const a = super.widgetClassList;
    this.columns && a.push('b-columned');
    return a;
  }
  updateUseGap(a) {
    if (!a) {
      this.element.classList.add('b-no-gap');
    }
  }
};
const ButtonGroup = _ButtonGroup;
ButtonGroup.initClass();
ButtonGroup._$name = 'ButtonGroup';
const weekendDays = {
  0: 1,
  6: 1
};
const CalendarPanel = class extends Panel {
  static type = 'calendarpanel';
  static $name = 'CalendarPanel';
  static get configurable() {
    return {
      'layout': 'vbox',
      'textContent': false,
      'date': { '$config': 'date' },
      'month': {},
      'year': null,
      'weekStartDay': null,
      'sixWeeks': true,
      'showWeekColumn': null,
      'disabledDates': null,
      'headerRenderer': null,
      'weekRenderer': null,
      'cellRenderer': null,
      'disableWeekends': null,
      'disableNonWorkingDays': null,
      'hideNonWorkingDays': null,
      'hideNonWorkingDaysCls': 'b-hide-nonworking-days',
      'nonWorkingDays': null,
      'tip': null,
      'dayCellCls': 'b-calendar-cell',
      'dayHeaderCls': 'b-calendar-day-header',
      'disabledCls': 'b-disabled-date',
      'otherMonthCls': 'b-other-month',
      'weekendCls': 'b-weekend',
      'todayCls': 'b-today',
      'nonWorkingDayCls': 'b-nonworking-day',
      'dayNameFormat': 'ddd',
      'minRowHeight': {
        '$config': ['lazy'],
        'value': null
      },
      'minColumnWidth': {
        '$config': ['lazy'],
        'value': null
      },
      'disableOtherMonthCells': null,
      'disableOtherMonthCellsCls': 'b-disable-othermonth-cells',
      'hideOtherMonthCells': null,
      'hideOtherMonthCellsCls': 'b-hide-othermonth-cells',
      'animateTimeShift': true
    };
  }
  construct(a) {
    super.construct(a);
    if (!this.refreshCount) {
      this.refresh();
    }
  }
  onInternalPaint({firstPaint: a}) {
    let b;
    (b = super.onInternalPaint) == null ? void 0 : b.call(this, ...arguments);
    if (a) {
      if (!this.refreshCount) {
        this.refresh();
      }
      this.getConfig('minColumnWidth');
      this.getConfig('minRowHeight');
    }
  }
  get overflowElement() {
    return this.weeksElement;
  }
  doDestroy() {
    let a;
    (a = this.tip) == null ? void 0 : a.destroy();
    super.doDestroy();
  }
  updateMinRowHeight(a) {
    this.element.style.setProperty('--min-row-height', DomHelper.setLength(a));
    this.scrollable = { 'overflowY': a ? 'auto' : false };
  }
  updateMinColumnWidth(a) {
    const b = this;
    b.element.style.setProperty('--min-column-width', DomHelper.setLength(a));
    b.scrollable = { 'overflowX': a ? 'auto' : false };
    b.overflowElement.classList[a ? 'add' : 'remove']('b-min-columnwidth');
  }
  getDateFromDomEvent(a) {
    const b = (a.nodeType === Element.ELEMENT_NODE ? a : a.target).closest('#' + this.id + ' [data-date]');
    if (b) {
      return DateHelper.parseKey(b.dataset.date);
    }
  }
  changeTip(a, b) {
    const c = this;
    return Tooltip.reconfigure(b, a, {
      'owner': c,
      'defaults': {
        'type': 'tooltip',
        'owner': c,
        'id': c.id + '-cell-tip',
        'forElement': c.bodyElement,
        'forSelector': '.' + c.dayCellCls
      }
    });
  }
  updateTip(a) {
    this.detachListeners('tip');
    a == null ? void 0 : a.ion({
      'pointerOver': 'onTipOverCell',
      'name': 'tip',
      'thisObj': this
    });
  }
  updateElement(a, b) {
    const c = this;
    super.updateElement(a, b);
    c.updateHideNonWorkingDays(c.hideNonWorkingDays);
    c.weekdayCells = Array.from(a.querySelectorAll('.b-calendar-day-header'));
    c.weekElements = Array.from(a.querySelectorAll('.b-calendar-week'));
    c.weekDayElements = Array.from(a.querySelectorAll('.b-calendar-days'));
    c.cellElements = [];
    for (let e = 0, {length: f} = c.weekDayElements; e < f; e++) {
      c.cellElements.push(c.weekDayElements[e].previousSibling, ...c.weekDayElements[e].children);
    }
  }
  changeDate(a) {
    a = typeof a === 'string' ? DateHelper.parse(a) : new Date(a);
    if (isNaN(a)) {
      throw new Error('CalendarPanel date ingestion must be passed a Date, or a YYYY-MM-DD date string');
    }
    return DateHelper.clearTime(a);
  }
  updateDate(a) {
    this.month.date = a;
    this.element.dataset.month = DateHelper.format(a, 'YYYY-MM');
  }
  updateDayNameFormat() {
    const a = new Date('2000-06-04T12:00:00');
    this.shortDayNames = [];
    for (let b = 4; b < 11; b++) {
      a.setDate(b);
      this.shortDayNames.push(DateHelper.format(a, this.dayNameFormat));
    }
  }
  get weekStartDay() {
    return typeof this._weekStartDay === 'number' ? this._weekStartDay : DateHelper.weekStartDay;
  }
  updateWeekStartDay(a) {
    const b = this;
    if (b._month) {
      b.month.weekStartDay = a;
      b.dayNames = [];
      for (let c = 0; c < 7; c++) {
        b.dayNames[c] = b.shortDayNames[b.canonicalDayNumbers[c]];
      }
      if (b.refreshCount) {
        b.refresh();
      }
    }
  }
  updateHideNonWorkingDays(a) {
    let b;
    this.contentElement.classList.toggle(this.hideNonWorkingDaysCls, Boolean(a));
    (b = this.scrollable) == null ? void 0 : b.syncOverflowState();
    if (this._month) {
      this.month.hideNonWorkingDays = a;
    }
    if (!this.isConfiguring) {
      this.refresh();
    }
  }
  updateHideOtherMonthCells(a) {
    let b;
    this.element.classList.toggle(this.hideOtherMonthCellsCls, Boolean(a));
    (b = this.scrollable) == null ? void 0 : b.syncOverflowState();
  }
  updateDisableOtherMonthCells(a) {
    let b;
    this.element.classList.toggle(this.disableOtherMonthCellsCls, Boolean(a));
    (b = this.scrollable) == null ? void 0 : b.syncOverflowState();
  }
  get nonWorkingDays() {
    return this._nonWorkingDays || (this._localeNonWorkingDays || (this._localeNonWorkingDays = DateHelper.nonWorkingDays));
  }
  get weekends() {
    return this._localeWeekends || (this._localeWeekends = DateHelper.weekends);
  }
  changeNonWorkingDays(a) {
    return ObjectHelper.assign({}, a);
  }
  updateNonWorkingDays(a) {
    let b;
    if (this._month) {
      this.month.nonWorkingDays = a;
      this.refresh();
      (b = this.scrollable) == null ? void 0 : b.syncOverflowState();
    }
  }
  get visibleDayColumnIndex() {
    return this.month.visibleDayColumnIndex;
  }
  get dayColumnIndex() {
    return this.month.dayColumnIndex;
  }
  get canonicalDayNumbers() {
    return this.month.canonicalDayNumbers;
  }
  get visibleColumnCount() {
    return this.month.visibleColumnCount;
  }
  get weekLength() {
    return this.month.weekLength;
  }
  get startDate() {
    return this.month.startDate;
  }
  get duration() {
    return DateHelper.diff(this.month.startDate, this.month.endDate, 'day') + 1;
  }
  get endDate() {
    const {endDate: a} = this.month;
    if (a) {
      return DateHelper.add(a, 1, 'day');
    }
  }
  changeMonth(a, b) {
    const c = this;
    if (!(a instanceof Month)) {
      if (typeof a === 'number') {
        if (b) {
          b.month = a;
          return;
        }
        const e = c.date || DateHelper.clearTime(new Date());
        e.setMonth(a);
        a = { 'date': e };
      }
      a = Month.new({
        'weekStartDay': c.weekStartDay,
        'nonWorkingDays': c.nonWorkingDays,
        'hideNonWorkingDays': c.hideNonWorkingDays,
        'sixWeeks': c.sixWeeks
      }, a);
    }
    a.ion({
      'dateChange': 'onMonthDateChange',
      'thisObj': c
    });
    return a;
  }
  onMonthDateChange({
    source: a,
    newDate: b,
    oldDate: c,
    changes: e
  }) {
    const f = this;
    f.year = a.year;
    if (!f.isConfiguring) {
      if (!f.getCell(b) || (e.m || e.y)) {
        const {isVisible: g} = f;
        f.refresh();
        if (f.animateTimeShift && g) {
          DomHelper.slideIn(f.contentElement, b > c ? 1 : -1);
        }
      }
      f.trigger('dateChange', {
        'changes': e,
        'value': b,
        'oldValue': c
      });
    }
  }
  updateYear(a) {
    this.month.year = a;
  }
  updateShowWeekNumber(a) {
    this.updateShowWeekColumn(a);
  }
  updateShowWeekColumn(a) {
    const b = this;
    b.element.classList[a ? 'add' : 'remove']('b-show-week-column');
    if (b.floating) {
      if (!b.isAligning) {
        b.realign();
      }
    }
  }
  updateSixWeeks(a) {
    if (this.month) {
      this.month.sixWeeks = a;
      this.refresh();
    }
  }
  refresh() {
    this.doRefresh();
  }
  doRefresh() {
    let a;
    this.getConfig('element');
    const b = this, c = b.timeZone != null ? b.timeZone : (a = b.project) == null ? void 0 : a.timeZone, e = c != null ? TimeZoneHelper.toTimeZone(new Date(), c) : new Date(), {
        weekElements: f,
        weekDayElements: g,
        date: i,
        month: j,
        dayCellCls: k,
        dayHeaderCls: l,
        disabledCls: n,
        otherMonthCls: o,
        weekendCls: p,
        todayCls: q,
        nonWorkingDayCls: r,
        nonWorkingDays: u,
        canonicalDayNumbers: v,
        sixWeeks: w
      } = b;
    e.setHours(0, 0, 0, 0);
    if (!i) {
      b.date = e;
      return;
    }
    b.element.style.setProperty('--visible-week-count', j.weekCount);
    b.trigger('beforeRefresh');
    b.getConfig('dayNameFormat');
    for (let C = 0; C < 7; C++) {
      const E = b.weekdayCells[C], F = b.canonicalDayNumbers[C], G = {
          [l]: 1,
          [p]: weekendDays[F],
          [r]: u[F]
        };
      if (b.headerRenderer) {
        E.innerHTML = '';
        b.callback(b.headerRenderer, b, [
          E,
          C,
          F
        ]);
      } else {
        DomHelper.setInnerText(E, b.shortDayNames[F]);
      }
      DomHelper.syncClassList(E, G);
      E.dataset.columnIndex = C;
      E.dataset.cellDay = F;
    }
    let z = 0, A = 0, B = 6;
    for (let H = 6; H >= 0; H--) {
      if (!u[v[H]]) {
        B = H;
        break;
      }
    }
    f[4].classList.toggle('b-hide-display', j.weekCount < 5 && !w);
    f[5].classList.toggle('b-hide-display', j.weekCount < 6 && !w);
    j.eachWeek((I, J) => {
      const K = g[z], L = [
          K.previousSibling,
          ...K.children
        ];
      f[z].dataset.week = I[0] + ',' + I[1];
      if (b.weekRenderer) {
        b.callback(b.weekRenderer, b, [
          L[0],
          I
        ]);
      } else {
        L[0].innerText = I[1];
      }
      for (let N = 0; N < 7; N++) {
        const O = J[N], P = O.getDay(), Q = DateHelper.makeKey(O), R = u[P], S = L[N + 1], T = {
            [k]: 1,
            [n]: b.isDisabledDate(O),
            [o]: O.getMonth() !== j.month,
            [p]: weekendDays[P],
            [q]: O.getTime() === e.getTime(),
            [r]: R,
            'b-last-working-day': N === B,
            'b-first-visible-cell': !(O - (b.firstVisibleDate || -1)),
            'b-last-visible-cell': !(O - (b.lastVisibleDate || -1)),
            ['b-day-of-week-' + P]: 1
          };
        DomHelper.syncClassList(S, T);
        S.dataset.date = Q;
        S.dataset.cellIndex = A;
        S.dataset.columnIndex = N;
        if (S.lastDomConfig) {
          delete S.lastDomConfig.class;
          delete S.lastDomConfig.className;
        }
        if (b.cellRenderer) {
          b.callback(b.cellRenderer, b, [{
              'cell': S,
              'date': O,
              'day': P,
              'rowIndex': z,
              'cellIndex': A,
              'columnIndex': N,
              'key': Q,
              'week': I,
              'row': f[z],
              'source': b,
              'visibleColumnIndex': b.visibleDayColumnIndex[P]
            }]);
        } else {
          S.innerHTML = O.getDate();
        }
        A++;
      }
      z++;
    });
    b.visibleWeekCount = z;
    if (b.floating) {
      if (!b.isAligning) {
        b.realign();
      }
    }
    b.refreshCount = (b.refreshCount || 0) + 1;
    b.trigger('refresh');
  }
  isDisabledDate(a) {
    const b = this, c = a.getDay(), {disabledDates: e} = b;
    if (b.disableWeekends && weekendDays[c]) {
      return true;
    }
    if (b.disableNonWorkingDays && b.nonWorkingDays[c]) {
      return true;
    }
    if (e) {
      if (Array.isArray(e)) {
        a = DateHelper.clearTime(a, true);
        return e.some(f => !(DateHelper.clearTime(f, true) - a));
      } else {
        return b.callback(e, b, [a]);
      }
    }
  }
  get bodyConfig() {
    const a = super.bodyConfig, b = [];
    a.children = [
      {
        'tag': 'div',
        'className': 'b-calendar-row b-calendar-weekdays',
        'reference': 'weekdaysHeader',
        'children': [
          { 'class': 'b-week-number-cell' },
          ...ArrayHelper.fill(7, { 'class': this.dayHeaderCls }),
          DomHelper.scrollBarPadElement
        ]
      },
      {
        'className': 'b-weeks-container notranslate',
        'reference': 'weeksElement',
        'tabIndex': -1,
        'children': b
      }
    ];
    for (let c = 0; c < 6; c++) {
      const e = {
        'className': 'b-calendar-row b-calendar-week',
        'dataset': { 'rowIndex': c },
        'children': [
          { 'className': 'b-week-number-cell' },
          {
            'className': 'b-calendar-days',
            'children': [
              {},
              {},
              {},
              {},
              {},
              {},
              {}
            ],
            'syncOptions': {
              'ignoreRefs': true,
              'strict': false
            }
          }
        ]
      };
      b.push(e);
    }
    return a;
  }
  get firstVisibleDate() {
    if (this.hideOtherMonthCells) {
      const {
        year: a,
        month: b
      } = this.month;
      return new Date(a, b, 1);
    }
    for (const c = this, e = c.month.startDate;; e.setDate(e.getDate() + 1)) {
      if (!c.hideNonWorkingDays || !c.nonWorkingDays[e.getDay()]) {
        return e;
      }
    }
  }
  get lastVisibleDate() {
    const a = DateHelper.add(this.endDate, -1, 'd');
    if (this.hideOtherMonthCells) {
      return a;
    }
    for (const b = this, c = a;; c.setDate(c.getDate() - 1)) {
      if (!b.hideNonWorkingDays || !b.nonWorkingDays[c.getDay()]) {
        return c;
      }
    }
  }
  getCell(a, b) {
    if (!(typeof a === 'string')) {
      a = DateHelper.makeKey(a);
    }
    const c = this.weeksElement.querySelector('[data-date="' + a + '"]');
    if (c && (!b || !c.classList.contains(this.otherMonthCls))) {
      return c;
    }
  }
  onTipOverCell({
    source: a,
    target: b
  }) {
    a.date = DateHelper.parseKey(b.dataset.date);
  }
  updateLocalization() {
    this._localeNonWorkingDays = this._localeWeekends = null;
    this.updateDayNameFormat();
    this.updateWeekStartDay(this.weekStartDay);
    super.updateLocalization();
  }
};
CalendarPanel.initClass();
CalendarPanel._$name = 'CalendarPanel';
const {
  abs,
  ceil,
  floor: floor2,
  max: max2,
  min: min3
} = Math;
const itemCls = 'b-carousel-item';
const EMPTY_RANGE = [
  0,
  0
];
const INDEX_PROP = Symbol('carouselIndex');
const SYNC_GEN_PROP = Symbol('carouselSyncGen');
const reserveCls = 'b-carousel-reserve';
const reserveBeforeCls = reserveCls + '-before';
const reserveAfterCls = reserveCls + '-after';
const sortByIndex = (c, e) => c[INDEX_PROP] - e[INDEX_PROP];
const visibleFirstCls = 'b-carousel-first';
const visibleLastCls = 'b-carousel-last';
const visibleSlotCls = 'b-carousel-visible';
const xy = [
  'x',
  'y'
];
const yx = [
  'y',
  'x'
];
const namesForAxis = {
  'h': {
    'minSize': 'minWidth',
    'offsetPos': 'offsetLeft',
    'offsetSize': 'offsetWidth',
    'size': 'width',
    'vxy': [
      'vx',
      'vy'
    ],
    'xy': xy
  },
  'v': {
    'minSize': 'minHeight',
    'offsetSize': 'offsetHeight',
    'size': 'height',
    'vxy': [
      'vy',
      'vx'
    ],
    'xy': yx
  }
};
namesForAxis.h.other = namesForAxis.v;
namesForAxis.v.other = namesForAxis.h;
const Carousel = class extends Panel.mixin(Fencible_default, InfinityScrollable_default) {
  static fenced = { 'resync': true };
  static delayable = { 'triggerSlotVisibility': 'raf' };
  static configurable = {
    'atMax': null,
    'atMin': null,
    'configureSlot': (a, b, c) => ({}),
    'disableReserveSlots': null,
    'empty': null,
    'emptyHtml': null,
    'emptyText': 'No items to display',
    'firstVisibleSlot': null,
    'lastVisibleSlot': null,
    'range': [
      null,
      null
    ],
    'reserveSlots': 1,
    'scrollOnTab': null,
    'shrinkWrap': 'auto',
    'slots': null,
    'snap': true,
    'vertical': null
  };
  static type = 'carousel';
  static $name = 'Carousel';
  compose() {
    const {carouselClasses: a} = this;
    return { 'class': { ...a } };
  }
  get allSlots() {
    return this.ensureItems().values.filter(a => a.innerItem).sort(sortByIndex);
  }
  get bodyConfig() {
    const {
        carouselClasses: a,
        emptyHtml: b,
        emptyText: c
      } = this, e = super.bodyConfig;
    return ObjectHelper.merge(e, {
      'className': {
        'b-vbox': 1,
        ...a
      },
      'children': {
        'innerCt': {
          'class': {
            'b-carousel-inner-ct': 1,
            'b-box-center': 1
          }
        },
        'emptyElement': {
          'class': {
            'b-carousel-empty-text': 1,
            'b-box-center': 1
          },
          'html': b || StringHelper.xss`${ c }`
        }
      }
    });
  }
  get carouselClasses() {
    const {
      empty: a,
      vertical: b
    } = this;
    return {
      'b-carousel-empty': a,
      'b-carousel-vertical': b
    };
  }
  get contentElement() {
    return this.innerCt;
  }
  get currentIndex() {
    return this.indexFromPos(this.scrollPos);
  }
  set currentIndex(a) {
    this.goto(a);
  }
  get focusedSlot() {
    const {innerCt: a} = this;
    let b = null, c, e;
    for (c = DomHelper.getActiveElement(); c; c = e) {
      e = c.parentElement;
      if (e === a) {
        b = Widget.fromElement(c);
        break;
      }
    }
    return b;
  }
  get infinityScrollerDefaults() {
    const a = this, [b, c] = a.vertical ? yx : xy;
    return ObjectHelper.merge(super.infinityScrollerDefaults, {
      'snap': a.snap && b,
      [b]: {
        'overflow': 'hidden-scroll',
        'range': a.scrollRange,
        'pos': 0
      },
      [c]: { 'overflow': 'hidden' }
    });
  }
  get propNames() {
    return namesForAxis[this.vertical ? 'v' : 'h'];
  }
  get scrollAxis() {
    return this.infinityScroller[this.vertical ? 'y' : 'x'];
  }
  get scrolling() {
    return this.infinityScroller.scrolling;
  }
  get scrollPos() {
    return this.scrollAxis.pos;
  }
  set scrollPos(a) {
    this.scrollAxis.pos = a;
  }
  get scrollRange() {
    const {
      range: a,
      slotSize: b
    } = this;
    return b ? [
      a[0] * b,
      a[1] * b
    ] : [
      0,
      Infinity
    ];
  }
  get shrinkWrap() {
    const a = this._shrinkWrap;
    return a === 'auto' ? DomHelper.getStyleValue(this.element, 'position') === 'absolute' : a;
  }
  get lastVisibleIndex() {
    let a;
    return (a = this.lastVisibleSlot) == null ? void 0 : a[INDEX_PROP];
  }
  get visibleSlots() {
    return this.allSlots.filter(a => a.element.classList.contains(visibleSlotCls));
  }
  updateAtMax(a) {
    this.element.classList.toggle('b-carousel-at-max', a);
  }
  updateAtMin(a) {
    this.element.classList.toggle('b-carousel-at-min', a);
  }
  changeConfigureSlot(a) {
    if (typeof a === 'string') {
      const b = '_' + a + 'Fn';
      return this[b] || (this[b] = (...c) => this[a](...c));
    }
    return a;
  }
  triggerSlotVisibility() {
    const {
      firstVisibleSlot: a,
      lastVisibleSlot: b
    } = this;
    this.trigger('slotVisibility', {
      'firstVisibleSlot': a,
      'lastVisibleSlot': b
    });
  }
  updateFirstVisibleSlot(a) {
    this.triggerSlotVisibility();
  }
  updateLastVisibleSlot() {
    this.triggerSlotVisibility();
  }
  changeRange(a) {
    let b, c;
    a = a ? [
      (b = a[0]) != null ? b : -Infinity,
      (c = a[1]) != null ? c : Infinity
    ] : [
      0,
      0
    ];
    if (a[0] !== a[1]) {
      let e;
      if (a[1] < a[0]) {
        e = 'denormalized';
      }
      if (e) {
        throw new Error('Invalid carousel range [' + a[0] + ', ' + a[1] + '): ' + e);
      }
    }
    return a;
  }
  updateRange(a) {
    this.empty = !(a[0] < a[1]);
    this.syncSoon();
  }
  updateShrinkWrap() {
    this.isPainted && this.syncSoon();
  }
  updateVertical() {
    this.isPainted && this.syncSoon();
  }
  convertIndexPos(a, b) {
    const c = this, {
        allSlots: e,
        propNames: f
      } = c, [g, i] = c.range, j = Math.floor((g + i) / 2), k = e.length, l = e[k - 1], n = f.vxy[0], o = g <= 0 && 0 < i;
    if (!b && !(g <= a && a < i)) {
      return null;
    }
    let p = c.slotSize || 100, q, r, u, v, w, z, A, B;
    if (!k) {
      if (b && 0 <= a && a < p) {
        return o ? 0 : j;
      }
      A = 0;
      z = p;
      v = o ? 0 : j;
      w = v + 1;
    } else {
      p = 0;
      for (B of e) {
        u = B[INDEX_PROP];
        q = B[n];
        p = p || B.$vxy[f.size];
        if (B === e[0]) {
          v = u;
          A = q;
          if (a < (b ? q : u)) {
            break;
          }
        }
        if (b) {
          if (a === q) {
            return u;
          }
          if (a < q) {
            return u - 1;
          }
        } else if (a === u) {
          return q;
        }
        if (B === l) {
          r = q + B.$vxy[f.size];
          if (b && a < r) {
            return u;
          }
          w = u + 1;
          z = r;
        }
      }
      p = p || c.slotSize || 100;
    }
    if (b) {
      return a < A ? max2(v - ceil((A - a) / p), g) : min3(w + floor2((a - z) / p), i);
    }
    return a < v ? A - (v - a) * p : z + (a - w) * p;
  }
  ensurePlan(a) {
    return a;
  }
  ensureVisible(a) {
    const b = this;
    if (b.lastScrollPos == null) {
      b._afterSync = () => b.ensureVisible({
        ...a,
        'animate': false
      });
    } else if (b.scrolling && b.hasPainted) {
      b._afterScroll = () => b.ensureVisible(a);
    } else {
      const {
          index: c,
          animate: e
        } = b.ensurePlan(a), {
          currentIndex: f,
          lastVisibleIndex: g
        } = b;
      let i;
      if (c < f) {
        i = c - f;
      } else if (g < c) {
        i = c - g;
      }
      return i && b.go(i, e);
    }
  }
  getSlotIndex(a) {
    return a == null ? void 0 : a[INDEX_PROP];
  }
  indexFromPos(a) {
    return this.convertIndexPos(a, true);
  }
  posFromIndex(a) {
    return this.convertIndexPos(a, false);
  }
  slotFromIndex(a) {
    for (const b of this.allSlots) {
      if (b[INDEX_PROP] === a) {
        return b;
      }
    }
    return null;
  }
  backward(a) {
    return this.go(-1, a);
  }
  forward(a) {
    return this.go(1, a);
  }
  go(a, b) {
    return this.goto(this.currentIndex + a, b);
  }
  goto(a, b) {
    const c = this;
    if (c.lastScrollPos == null) {
      c._afterSync = () => {
        const {scrollPos: e} = c;
        c.goto(a, false);
        if (!c.hasPainted && e !== c.scrollPos) {
          c.sync();
        }
      };
    } else if (c.scrolling && c.hasPainted) {
      c._afterScroll = () => c.goto(a, b);
    } else {
      const {
          range: e,
          slots: f
        } = c, g = e[1] - (typeof f === 'number' ? f - 1 : 0), i = min3(g - 1, max2(e[0], a)), j = abs(c.currentIndex - i), k = c.posFromIndex(i);
      if (j >= c.allSlots.length) {
        b = false;
      }
      return c.scrollTo({
        'animate': b,
        [c.scrollAxis.name]: k
      });
    }
  }
  onChildAdd(a) {
    a.innerItem && a.element.classList.add(itemCls);
    return super.onChildAdd(a);
  }
  onChildRemove(a) {
    a.innerItem && a.element.classList.remove(itemCls);
    return super.onChildRemove(a);
  }
  onInfiniteScrollEnd(a) {
    super.onInfiniteScrollEnd(a);
    const {_afterScroll: b} = this;
    this._afterScroll = null;
    b == null ? void 0 : b();
  }
  onSlotFocusTrap({
    source: a,
    top: b
  }) {
    const c = this, e = b ? -1 : 1, f = a[INDEX_PROP], g = c.slotFromIndex(f + e);
    if (!g.element.classList.contains(reserveCls)) {
      g.element.focus();
    } else if (c.scrollOnTab) {
      c.go(e).then(i => {
        let j;
        if (i) {
          (j = g.element) == null ? void 0 : j.focus();
        }
      });
    }
    return false;
  }
  resync() {
    this.sync();
  }
  sync() {
    let a;
    this.syncCount = (this.syncCount || 0) + 1;
    if (!this.empty) {
      const b = this, {
          allSlots: c,
          disableReserveSlots: e,
          innerCt: f,
          scrollAxis: g,
          propNames: i,
          reserveSlots: j,
          slots: k
        } = b, [l, n] = b.range, o = g.pos, p = Widget.resolveType(((a = b.defaults) == null ? void 0 : a.type) || b.defaultType), q = b.indexFromPos(o), r = b.posFromIndex(q), u = o - r, v = [
          j,
          j
        ], w = b.syncGen = (b.syncGen || 0) + 1;
      if (b.scrolling && o > (b.lastScrollPos || 0) && u) {
        --v[0];
        ++v[1];
      }
      b.lastScrollPos = o;
      f.style.setProperty('--carousel-item-offset', Math.abs(u) + 'px');
      if (!b.shrinkWrapped) {
        f.style[i.minSize] = '';
      }
      let z = f[i.offsetSize], A = false, B = false, C = null, E = null, F, G, H, I;
      b.unanimated(() => {
        const {_afterSync: J} = b, K = k && k !== 'auto' ? b.syncFixed(q, i, c, p, v, k, l, n) : b.syncDynamic(q, i, c, p, v, k, z, l, n), L = [], N = c.filter(O => O[SYNC_GEN_PROP] !== w);
        if (N.length) {
          N.forEach(O => b.detachListeners(O.id + '-listeners'));
          b.remove(N);
        }
        f.style[i.other.minSize] = K[i.other.size] + 'px';
        z = f[i.offsetSize];
        for (I of c) {
          if (!N.includes(I)) {
            const O = g.getItemSpan(I), P = o + z, Q = O[1] <= o, R = P <= O[0], S = !Q && !R, T = S && O[0] <= o, U = S && O[1] >= P;
            if (S) {
              if (I[INDEX_PROP] === l) {
                B = true;
              }
              if (I[INDEX_PROP] === n - 1) {
                A = true;
              }
            }
            if (T) {
              C = I;
            }
            if (U) {
              E = I;
            }
            L.push([
              I,
              I.element.classList,
              {
                [reserveCls]: Q || R,
                [reserveBeforeCls]: Q,
                [visibleFirstCls]: T,
                [visibleSlotCls]: S,
                [visibleLastCls]: U,
                [reserveAfterCls]: R
              }
            ]);
          }
        }
        for ([I, F, H] of L) {
          if (e) {
            I.disabled = H[visibleSlotCls] ? false : e;
          }
          for (G in H) {
            F.toggle(G, H[G]);
          }
        }
        b.atMax = A;
        b.atMin = B;
        b.firstVisibleSlot = C;
        b.lastVisibleSlot = E;
        g.other.range = EMPTY_RANGE;
        b.syncDomOrder(N.length ? b.allSlots : c);
        g.sync({ 'range': b.scrollRange });
        if (g.pos !== o) {
          b.resync();
        } else if (J) {
          b._afterSync = null;
          J();
        }
      });
    }
  }
  syncDomOrder(a) {
    const b = DomHelper.getActiveElement();
    let c, e, f;
    for (f = a.length; f-- > 1;) {
      e = a[f].element;
      if (!c) {
        e.classList.add('b-last-visible-child');
      }
      c = a[f - 1].element;
      c.classList.remove('b-last-visible-child');
      e.classList.remove('b-first-visible-child');
      c.nextSibling !== e && c.parentElement.insertBefore(c, e);
    }
    c == null ? void 0 : c.classList.add('b-first-visible-child');
    if (this.element.contains(b) && b !== DomHelper.getActiveElement()) {
      b.focus();
    }
  }
  syncDynamic(a, b, c, e, f, g, i, j, k) {
    const l = this, {offsetSize: n} = b, o = g === 'auto', p = o ? '' : null;
    let [q, r] = f, u = 1, v = 0, w, z, A;
    for (A = a; j <= A && A < k; A += u) {
      z = l.syncSlot(c, e, A, p);
      w = w || z;
      if (v < i) {
        v += z.element[n];
        if (o) {
          return l.syncFixed(a, b, c, e, f, ceil(i / v));
        }
        if (u > 0) {
          if (A + 1 < k) {
            if (v < i || r) {
              continue;
            }
          }
          q += r;
          r = 0;
        }
      }
      if (--r < 1) {
        if (u < 0 || !q) {
          break;
        }
        u = -1;
        A = a;
        r = q;
      }
    }
    return w;
  }
  syncFixed(a, b, c, e, f, g, i, j) {
    const k = this, {
        shrinkWrap: l,
        shrinkWrapped: n
      } = k, o = 100 / g + '%', p = a + g;
    let q = a - f[0], r = p + f[1], u = 0, v, w, z, A, B;
    while (r > j) {
      --q;
      --r;
    }
    while (q < i) {
      ++q;
      ++r;
    }
    for (B = q; B < r; ++B) {
      w = k.syncSlot(c, e, B, l && !n ? '' : o);
      if (B === a) {
        v = w;
      }
      if (l) {
        (z || (z = [])).push(w);
      } else {
        w[b.size] = o;
      }
    }
    if (l) {
      A = min3(p, r - f[1]);
      for (B = A - g; B < A; ++B) {
        u += z[B - q][b.size];
      }
      for (w of z) {
        w[b.size] = o;
      }
      if (u) {
        k.innerCt.style[b.minSize] = u + 'px';
        k.shrinkWrapped = true;
      }
    }
    return v;
  }
  syncSlot(a, b, c, e) {
    const f = this, {
        propNames: g,
        syncGen: i
      } = f, j = e !== null;
    let k = null, l, n, o, p, q;
    for (p of a) {
      if (p[SYNC_GEN_PROP] !== i) {
        n = p[INDEX_PROP];
        if (n === c) {
          o = p;
          if (j) {
            o[g.size] = e;
          }
          break;
        }
        if (!k || n > k[INDEX_PROP]) {
          k = p;
        }
      }
    }
    if (!o) {
      l = f.configureSlot(c, f, k);
      if (k) {
        o = k;
        if (j) {
          o[g.size] = e;
        }
        delete l.type;
        o.setConfig(l);
      } else {
        q = l.type ? Widget.resolveType(l.type) : b;
        l = q.mergeConfigs(f.defaults, l);
        if (j) {
          l[g.size] = e;
        }
        o = f.add(l);
        a.push(o);
        o.ion({
          'thisObj': f,
          'name': o.id + '-listeners',
          'focusTrap': 'onSlotFocusTrap'
        });
      }
      o[INDEX_PROP] = c;
      if (!f.slotSize) {
        f.slotSize = o[g.size];
      }
      o[g.vxy[0]] = c * f.slotSize;
    }
    o[SYNC_GEN_PROP] = i;
    return o;
  }
};
Carousel.initClass();
Carousel._$name = 'Carousel';
const whenNotChecked = a => !a.value;
const Checkbox = class extends Field {
  static alias = 'check';
  static type = 'checkbox';
  static $name = 'Checkbox';
  static get configurable() {
    return {
      'inputType': 'checkbox',
      'autoCollapse': null,
      'toggleGroupRootElement': null,
      'containerDefaults': {
        'syncableConfigs': { 'disabled': a => a.disabled || !a.value },
        'syncConfigTriggers': {
          'autoCollapse': 1,
          'value': 1
        }
      },
      'text': '',
      'checkedValue': true,
      'uncheckedValue': false,
      'color': null,
      'value': '',
      'toggleGroup': null,
      'localizableProperties': [
        'label',
        'text'
      ],
      'twinSyncConfigs': { 'value': 'checked' }
    };
  }
  construct(a) {
    if ('checked' in a) {
      a = ObjectHelper.assign({}, a);
      a.value = a.checked;
      delete a.checked;
    }
    super.construct(a);
    this.syncHasText();
  }
  get textLabelCls() {
    return 'b-checkbox-label';
  }
  get innerElements() {
    return [
      this.inputElement,
      {
        'tag': 'label',
        'class': this.textLabelCls,
        'for': this.id + '-input',
        'reference': 'textLabel',
        'html': this.text || ''
      }
    ];
  }
  get inputElement() {
    const a = super.inputElement;
    if (this.toggleGroup) {
      a.dataset = { 'group': this.toggleGroup };
    }
    a.listeners = {
      'click': 'internalOnClick',
      'change': 'internalOnChange',
      'input': 'internalOnInput'
    };
    return a;
  }
  get checked() {
    return this.value;
  }
  set checked(a) {
    this.value = a;
  }
  syncHasText() {
    this.element.classList[this.text ? 'add' : 'remove']('b-text');
  }
  updateText(a) {
    if (this.textLabel) {
      this.syncHasText();
      this.textLabel.innerHTML = a;
    }
  }
  afterSyncChildConfigs(a) {
    super.afterSyncChildConfigs(a);
    let {autoCollapse: b} = this;
    if (b) {
      b = b === true ? whenNotChecked : b;
      a.collapsed = b(this);
    }
  }
  assignFieldValue(a, b, c) {
    this.value = c === this.checkedValue || (c === this.uncheckedValue ? false : null);
  }
  fetchInputValue() {
    if (!this.readOnly) {
      this.value = this.input.checked;
    }
  }
  gatherValue(a) {
    let b;
    const c = this, e = c.value ? c.checkedValue : c.uncheckedValue, f = e !== void 0, {valueName: g} = c;
    if (f) {
      a[g] = e;
    }
    c.gatherValues(a, f);
    if (e === true && ((b = a[g]) == null ? void 0 : b.value) === e) {
      delete a[g].value;
    }
  }
  changeValue(a) {
    return a === 'false' ? false : Boolean(a);
  }
  updateValue(a) {
    let b;
    const c = this, e = c.input.checked !== a;
    c.input.checked = a;
    (b = c.container) == null ? void 0 : b.syncChildConfigs();
    if (e && !c.inputting && !c.isConfiguring) {
      c.uncheckToggleGroupMembers();
      c.triggerChange(false, null);
    }
  }
  get inputValueAttr() {
    return 'checked';
  }
  updateColor(a, b) {
    const c = this.element.classList;
    if (b) {
      c.remove(b);
    }
    if (a) {
      c.add(a);
    }
  }
  getToggleGroupRootElement() {
    const a = this.toggleGroupRootElement;
    let b;
    if (a && typeof a === 'string' && this.element) {
      b = this.element.closest(a);
    }
    return b || this.rootElement;
  }
  getToggleGroupMembers() {
    const a = this, {
        toggleGroup: b,
        input: c,
        type: e
      } = a, f = [];
    if (b) {
      DomHelper.forEachSelector(a.getToggleGroupRootElement(), 'input[type=' + e + '][data-group=' + b + ']', g => {
        if (g !== c) {
          const i = Widget.fromElement(g);
          i && f.push(i);
        }
      });
    }
    return f;
  }
  uncheckToggleGroupMembers(a) {
    if (this.checked && this.toggleGroup) {
      this.getToggleGroupMembers().forEach(b => {
        b._propagatingUserActionFrom = this;
        b.checked = false;
        b._propagatingUserActionFrom = null;
      });
    }
  }
  check() {
    this.checked = true;
  }
  uncheck() {
    this.checked = false;
  }
  toggle() {
    this.checked = !this.checked;
  }
  internalOnClick(a) {
    if (this.readOnly) {
      a.preventDefault();
      return false;
    }
    return this.trigger('click', { 'event': a });
  }
  internalOnChange(a) {
    const b = this;
    if (b.readOnly && b.value !== b.input.checked) {
      b.input.checked = b.value;
      return;
    }
    b.value = b.input.checked;
    if (!b.inputting) {
      b.inputting = true;
      b.triggerChange(true, a);
      b.inputting = false;
    }
  }
  updateInputReadOnly(a) {
  }
  triggerChange(a, b) {
    const c = this, {checked: e} = c.input;
    const f = {
      'checked': e,
      'value': e,
      'oldValue': !e,
      'userAction': a,
      'valid': true,
      'event': b
    };
    if (c._propagatingUserActionFrom) {
      f.propagatingUserActionFrom = c._propagatingUserActionFrom;
    }
    const g = !e && a && c.toggleGroup && c.getToggleGroupMembers().filter(i => i.isVisible && !i.disabled).length || c.trigger('beforeChange', f) === false;
    if (g) {
      c.input.checked = c._value = !e;
    } else {
      c.triggerFieldChange(f, false);
      if (a) {
        c.uncheckToggleGroupMembers(a);
      }
      c.trigger('action', f);
      c.trigger('change', f);
      return true;
    }
  }
  configureOverflowTwin(a) {
    const b = this, c = super.configureOverflowTwin(a);
    c.type = Widget.resolveType('menuitem');
    if (!c.text) {
      c.text = b.label || b.text || b.tooltipText;
    }
    return c;
  }
};
Checkbox.initClass();
Checkbox._$name = 'Checkbox';
let monacoLoadPromise;
const CodeEditor = class extends Panel {
  static configurable = {
    'textContent': false,
    'scrollable': null,
    'codePath': null,
    'editor': {
      '$config': [
        'lazy',
        'nullify'
      ],
      'value': {
        'automaticLayout': true,
        'scrollBeyondLastLine': false
      }
    },
    'header': { 'cls': 'demo-header' },
    'bbar': {
      'overflow': null,
      'items': {
        'readOnly': {
          'hidden': true,
          'type': 'widget',
          'cls': 'b-icon b-icon-locked'
        },
        'status': {
          'type': 'widget',
          'html': '\xA0'
        },
        'cursorPos': {
          'type': 'widget',
          'html': '\xA0',
          'setPosition'({
            lineNumber: a,
            column: b
          }) {
            this.element.innerHTML = 'Ln ' + a + ', Col ' + b;
          }
        }
      }
    },
    'readOnly': null,
    'text': {
      '$config': [
        'lazy',
        'nullify'
      ],
      'value': null
    },
    'language': null,
    'theme': null,
    'status': null
  };
  static type = 'codeeditor';
  static $name = 'CodeEditor';
  construct(a = {}) {
    const b = this;
    b.editorReady = new Promise((c, e) => {
      b.resolveEditorReady = c;
      b.rejectEditorReady = e;
    });
    super.construct(...arguments);
    b.editorReady.then(() => {
      GlobalEvents_default.ion({
        'theme': ({theme: c}) => b.editor.updateOptions({ 'theme': c.toLowerCase().includes('dark') ? 'vs-dark' : 'vs-light' }),
        'thisObj': b
      });
      b.element.style.setProperty('--vscode-menu-background', getComputedStyle(b.element.querySelector('.monaco-editor')).getPropertyValue('--vscode-menu-background'));
    });
  }
  get monacoInstance() {
    return globalThis.monaco;
  }
  focus() {
    this.editor.focus();
  }
  static set monacoLoadPromise(a) {
    monacoLoadPromise = a;
  }
  static get monacoLoadPromise() {
    return monacoLoadPromise;
  }
  async updateCodePath(a, b) {
    let c;
    const e = this;
    if (b) {
      (c = e.loaderScript) == null ? void 0 : c.remove();
    }
    if (!monacoLoadPromise) {
      monacoLoadPromise = new Promise(f => {
        const g = () => {
            const k = globalThis.require;
            k.config({ 'paths': { 'vs': a + '/min/vs' } });
            k(['vs/editor/editor.main'], function () {
              if (!e.isDestroyed) {
                f(globalThis.monaco);
              }
            });
            j.removeEventListener('error', i);
            j.removeEventListener('load', g);
          }, i = () => {
            const k = 'Code editor path to Monaco editor ' + a + ' is incorrect';
            j.removeEventListener('error', i);
            j.removeEventListener('load', g);
            Toast.show(k);
            e.rejectEditorReady(k);
          }, j = e.loaderScript = DomHelper.createElement({
            'tag': 'script',
            'parent': document.head,
            'src': a + '/min/vs/loader.js'
          });
        j.addEventListener('error', i);
        j.addEventListener('load', g);
      });
    }
    globalThis.monaco = await monacoLoadPromise;
    e.getConfig('editor');
    e.getConfig('text');
  }
  updateReadOnly(a) {
    const {readOnly: b} = this.widgetMap;
    b && (b.hidden = !a);
    this.editor.updateOptions({ 'readOnly': a });
  }
  updateStatus(a) {
    const {status: b} = this.widgetMap;
    b && (b.html = a);
  }
  changeEditor(a, b) {
    let c, e;
    const f = this;
    (c = b == null ? void 0 : b.dispose) == null ? void 0 : c.call(b);
    if (a) {
      a = f.monacoInstance.editor.create(f.contentElement, Config.merge({
        'theme': f.theme || ((e = DomHelper.themeInfo) == null ? void 0 : e.name.toLowerCase().includes('dark')) ? 'vs-dark' : 'vs-light',
        'padding': { 'top': 14 }
      }, a));
      f.monacoInstance.languages.typescript.javascriptDefaults.setDiagnosticsOptions({ 'noSuggestionDiagnostics': true });
      a.onDidChangeCursorPosition(f.onCursorMove.bind(f));
      a.onDidChangeConfiguration(f.onEditorConfigChange.bind(f));
      a.onDidDispose(f.onEditorDestroy.bind(f));
      f.resolveEditorReady(a);
    }
    return a;
  }
  get text() {
    let a;
    return ((a = this.codeModel) == null ? void 0 : a.getValue()) || this._text;
  }
  changeText(a) {
    let b;
    this._text = (b = this.codeModel) == null ? void 0 : b.getValue();
    return a;
  }
  updateText(a) {
    let b, c;
    const {codeModel: e} = this;
    if (a != null) {
      e ? e.setValue(a) : this.loadText(a);
    } else {
      (c = (b = this.codeModel) == null ? void 0 : b.dispose) == null ? void 0 : c.call(b);
    }
  }
  async loadText(a = this.text, b = this.language) {
    let c;
    const e = this;
    e._text = a;
    e._language = b;
    (c = e.codeModel) == null ? void 0 : c.dispose();
    await e.editorReady;
    const f = e.codeModel = e.monacoInstance.editor.createModel(a, b);
    e.editor.setModel(f);
    e.trigger('load', {
      'model': f,
      'text': a
    });
    return f;
  }
  onEditorDestroy() {
    let a, b;
    (b = (a = this.codeModel) == null ? void 0 : a.dispose) == null ? void 0 : b.call(a);
    this.codeModel = null;
  }
  onEditorConfigChange() {
    const a = this;
    if (a.editor) {
      a.readOnly = a.editor.getOptions().get(a.monacoInstance.editor.EditorOptions.readOnly.id);
    }
  }
  onCursorMove({position: a}) {
    let b;
    (b = this.widgetMap.cursorPos) == null ? void 0 : b.setPosition(a);
  }
};
CodeEditor.initClass();
CodeEditor._$name = 'CodeEditor';
const ColorPicker = class extends List {
  static configurable = {
    'itemsFocusable': false,
    'navigator': { 'focusCls': 'b-color-active' },
    'itemWrapperTpl'(a, b) {
      const {selected: c} = this, {
          color: e,
          text: f
        } = a, g = this.getColorClassName(e);
      return TemplateHelper.tpl`
                    <li
                        class="${ this.getItemClasses(a, b) } ${ g }"
                        role="option"
                        aria-selected="${ c.includes(a) }"
                        data-index="${ b }"
                        data-id="${ StringHelper.encodeHtml(e) }"
                        data-btip="${ f }"
                        ${ this.itemsFocusable ? 'tabindex="-1"' : '' }
                        style="${ g ? '' : 'background-color: ' + e }"
                    ></li>`;
    },
    'colorClasses': null,
    'colorClassPrefix': null,
    'colors': [
      '#45171D',
      '#F03861',
      '#FF847C',
      '#FECEA8',
      '#A5F2E7',
      '#AA83F3',
      '#8983F3',
      '#A10054',
      '#073059',
      '#2866AB',
      '#5FBDC5',
      '#D8D95C',
      '#FFDEDE',
      '#F7F3CE',
      '#C5ECBE',
      '#3E3E3E',
      '#405559',
      '#68868C',
      '#EDEDED',
      '#D3D6DB',
      '#3A4750',
      '#303841',
      '#BE3144'
    ],
    'addNoColorItem': true,
    'columns': 6,
    'colorSelected': null,
    'store': { 'useRawData': false }
  };
  static type = 'colorpicker';
  static $name = 'ColorPicker';
  configure(a) {
    super.configure(a);
    this.setItems();
    this.ion({ 'item': 'onColorSelect' });
  }
  setItems() {
    const a = this, {
        colors: b,
        colorClasses: c
      } = a;
    let e = c || b;
    if (a.addNoColorItem) {
      a.store.modelClass.allowNullId = true;
      e = [
        ...e,
        null
      ];
    }
    a.items = e.map(f => !f || typeof f == 'string' ? {
      'color': f,
      'text': (c ? StringHelper.capitalize(f) : f) || a.L('L{noColor}')
    } : f);
  }
  afterConfigure() {
    let a;
    super.afterConfigure(...arguments);
    if (this.addNoColorItem || this.value) {
      this.select((a = this.value) != null ? a : null);
    }
  }
  updateColors() {
    if (!this.isConfiguring) {
      this.setItems();
    }
  }
  updateAddNoColorItem() {
    if (!this.isConfiguring) {
      this.setItems();
    }
  }
  onColorSelect({record: a}) {
    let b;
    this.refresh();
    const c = {
      'color': a.color,
      'record': a,
      'bubbles': true
    };
    (b = this.colorSelected) == null ? void 0 : b.call(this, c);
    this.trigger('colorSelected', c);
  }
  select(a) {
    if (!a || typeof a === 'string') {
      a = this.store.findRecord('color', a != null ? a : null);
    }
    a && super.select(a);
  }
  updateColumns(a) {
    this.style = 'grid-template-columns:repeat(' + a + ', 1fr);';
  }
  getColorClassName(a) {
    let b, c;
    if (((b = this.colorClasses) == null ? void 0 : b.includes(a)) || ((c = this.colorClasses) == null ? void 0 : c.find(e => e && e.color === a))) {
      return this.colorClassPrefix + a;
    }
    return a ? '' : 'b-no-color';
  }
};
ColorPicker.initClass();
ColorPicker._$name = 'ColorPicker';
const ColorField = class extends Combo {
  static configurable = {
    'displayField': 'text',
    'valueField': 'color',
    'editable': false,
    'picker': {
      'type': 'colorpicker',
      'align': {
        'align': 't100-b100',
        'matchSize': false
      }
    },
    'showBoxForNoColor': true,
    'colors': null,
    'addNoColorItem': true
  };
  static type = 'colorfield';
  static $name = 'ColorField';
  configure(a) {
    let b;
    const c = (b = a.picker) != null ? b : {};
    if (a.colors) {
      c.colors = a.colors;
    }
    if ('addNoColorItem' in a) {
      c.addNoColorItem = a.addNoColorItem;
    }
    a.picker = c;
    super.configure(a);
  }
  updatePicker(a, b) {
    super.updatePicker(a, b);
    if (a) {
      this.items = a.store.records;
    }
  }
  updateColors(a) {
    if (!this.isConfiguring) {
      this.picker.colors = a;
    }
  }
  updateAddNoColorItem(a) {
    if (!this.isConfiguring) {
      this.picker.addNoColorItem = a;
    }
  }
  set value(a) {
    if (!this.store) {
      this.items = [];
      this.store = this.picker.store;
    }
    if (!a) {
      a = this.store.findRecord('color', null);
    }
    super.value = a;
  }
  showPicker() {
    this.picker.refresh();
    super.showPicker(...arguments);
  }
  get value() {
    return super.value;
  }
  syncInputFieldValue(...a) {
    let b;
    const c = this, {value: e} = c;
    let f = (b = c.picker) == null ? void 0 : b.getColorClassName(e);
    if (!f) {
      c.colorBox.style.color = e;
    }
    f = 'b-colorbox ' + f;
    c.colorBox.className = f;
    if (!c.showBoxForNoColor) {
      c.element.classList.toggle('b-colorless', !e);
    }
    super.syncInputFieldValue(...a);
  }
  get innerElements() {
    return [
      {
        'reference': 'colorBox',
        'className': 'b-colorbox'
      },
      ...super.innerElements
    ];
  }
};
ColorField.initClass();
ColorField._$name = 'ColorField';
const choices = [
  'ok',
  'cancel',
  'yes',
  'no'
].reduce((b, c, e, f) => {
  b[c] = Object.freeze({
    ...Object.fromEntries(f.map(g => [
      g,
      false
    ])),
    'name': c,
    'approve': 'ok,yes'.includes(c),
    [c]: true
  });
  return b;
}, Object.create(null));
const defaultClasses = [
  'b-raised',
  'b-blue'
];
const nonDefaultClasses = ['b-gray'];
const okFirst = BrowserHelper.isWindows;
const splitRe = /(?:\s+,?|,)\s*/;
const widthyConfigs = /^(icon|menu|text)$/;
const ConfirmationBar = class extends Toolbar {
  static delayable = {
    'syncButtons': {
      'type': 'raf',
      'suspended': true
    }
  };
  static configurable = {
    'defaultButton': 'ok',
    'defaultType': 'button',
    'items': 'ok,cancel',
    'keyMap': {
      'Enter': 'doDefault',
      'NumpadEnter': 'doDefault',
      'Escape': 'doCancel'
    },
    'namedItems': {
      'yes': {
        'cls': 'b-yes-button',
        'text': 'L{Object.Yes}',
        'onClick': 'up.onButtonClick',
        'weight': okFirst ? 100 : 130
      },
      'no': {
        'cls': 'b-no-button',
        'text': 'L{Object.No}',
        'onClick': 'up.onButtonClick',
        'weight': okFirst ? 110 : 120
      },
      'ok': {
        'cls': 'b-ok-button',
        'text': 'L{Object.Ok}',
        'onClick': 'up.onButtonClick',
        'weight': okFirst ? 120 : 110
      },
      'cancel': {
        'cls': 'b-cancel-button',
        'text': 'L{Object.Cancel}',
        'onClick': 'up.onButtonClick',
        'weight': okFirst ? 130 : 100
      }
    },
    'syncWidthButtons': null
  };
  static type = 'confirmationbar';
  static $name = 'ConfirmationBar';
  get buttons() {
    return this.items.filter(a => a.isButton);
  }
  get cancelButton() {
    return this.widgetMap.cancel || null;
  }
  get noButton() {
    return this.widgetMap.no || null;
  }
  get okButton() {
    return this.widgetMap.ok || null;
  }
  get yesButton() {
    return this.widgetMap.yes || null;
  }
  changeItems(a, b) {
    if (typeof a === 'string') {
      a = Object.fromEntries(a.split(splitRe).map(c => [
        c,
        true
      ]));
    }
    return super.changeItems(a, b);
  }
  clickButton(a, b) {
    const c = this, {
        widgetMap: e,
        defaultButton: f
      } = c;
    let g = b ? e.cancel || e.no : e[f], i, j;
    if (!g) {
      for ([j, i] of Object.entries(e)) {
        if (j !== f) {
          g = i;
          break;
        }
      }
    }
    g == null ? void 0 : g.onInternalClick(a);
  }
  doCancel(a) {
    this.clickButton(a, true);
  }
  doDefault(a) {
    this.clickButton(a);
  }
  onButtonClick(a) {
    this.trigger('choice', {
      ...a,
      'choice': choices[a.source.ref],
      'source': this,
      'userAction': true
    });
  }
  onChildAdd(a) {
    super.onChildAdd(a);
    FunctionHelper.before(a, 'onConfigChange', 'onChildConfigChange', this);
    this.syncButtons();
  }
  onChildConfigChange({name: a}) {
    widthyConfigs.test(a) && this.syncButtons();
  }
  onChildRemove(a) {
    super.onChildRemove(a);
    this.syncButtons();
  }
  onInternalPaint(a) {
    super.onInternalPaint(a);
    a.firstPaint && this.syncButtons.resume();
    this.syncButtons.now();
  }
  syncButtons() {
    const a = this, b = a.isDestroying ? [] : a.buttons, {defaultButton: c} = a;
    let e, f;
    a.syncWidthButtons !== false && b.forEach(g => {
      if (f = g.text) {
        e = Math.max(e || 0, f.length + Boolean(g.icon) + Boolean(g.menu));
      }
    });
    b.forEach(g => {
      const {classList: i} = g.element, j = g.ref === c;
      i.add(...j ? defaultClasses : nonDefaultClasses);
      i.remove(...j ? nonDefaultClasses : defaultClasses);
      i.toggle('b-default-button', j);
      g.minWidth = e && g.text ? e + 'em' : null;
    });
  }
  updateAutoWidthButtons() {
    this.syncButtons();
  }
  updateDefaultButton() {
    this.syncButtons();
  }
};
ConfirmationBar.initClass();
ConfirmationBar._$name = 'ConfirmationBar';
const YearPicker = class extends Panel {
  static configurable = {
    'textContent': false,
    'tbar': {
      'overflow': null,
      'items': {
        'previous': {
          'type': 'tool',
          'cls': 'b-icon b-icon-previous',
          'onAction': 'up.previous',
          'weight': 100
        },
        'title': {
          'type': 'button',
          'cls': 'b-yearpicker-title',
          'weight': 200,
          'onAction': 'up.handleTitleClick'
        },
        'next': {
          'type': 'tool',
          'cls': 'b-icon b-icon-next',
          'onAction': 'up.next',
          'weight': 300
        }
      }
    },
    'itemCls': 'b-year-container',
    'yearButtonCount': 12,
    'year': null,
    'minYear': null,
    'maxYear': null,
    'startYear': null
  };
  static type = 'yearpicker';
  static $name = 'YearPicker';
  construct(a) {
    super.construct({
      'year': new Date().getFullYear(),
      ...a
    });
    EventHelper.on({
      'element': this.contentElement,
      'click': 'onYearClick',
      'delegate': '.b-yearpicker-year',
      'thisObj': this
    });
  }
  get focusElement() {
    return this.getYearButton(this.year) || this.getYearButton(this.startYear);
  }
  getYearButton(a) {
    return this.contentElement.querySelector('.b-yearpicker-year[data-year="' + a + '"]');
  }
  get value() {
    return this.year;
  }
  set value(a) {
    this.year = a;
  }
  onYearClick({target: a}) {
    const b = Math.min(Math.max(parseInt(a.innerText), this.minYear || 1), this.maxYear || 9999);
    if (this.year === b) {
      this.trigger('select', {
        'oldValue': b,
        'value': b
      });
    } else {
      this.year = b;
    }
  }
  handleTitleClick(a) {
    this.trigger('titleClick', a);
  }
  previous() {
    this.startYear = this.startYear - this.yearButtonCount;
  }
  next() {
    this.startYear = this.endYear + 1;
  }
  ingestYear(a) {
    if (!isNaN(a)) {
      return ObjectHelper.isDate(a) ? a.getFullYear() : a;
    }
  }
  changeYear(a) {
    if (a = this.ingestYear(a)) {
      return Math.min(Math.max(a, this.minYear || 1), this.maxYear || 9999);
    }
  }
  updateYear(a, b) {
    const c = this;
    if (!c.startYear || a > c.endYear) {
      c.startYear = a;
    } else if (a < c.startYear) {
      c.startYear = a - (c.yearButtonCount - 1);
    }
    if (!c.isConfiguring) {
      c.trigger('select', {
        'oldValue': b,
        'value': a
      });
    }
  }
  get endYear() {
    return this.startYear + this.yearButtonCount - 1;
  }
  changeStartYear(a) {
    if (a = this.ingestYear(a)) {
      a = this.minYear ? Math.max(a, this.minYear) : a;
      return this.maxYear ? Math.min(a, this.maxYear - (this.yearButtonCount - 1)) : a;
    }
  }
  async updateStartYear(a, b) {
    if (this.isVisible) {
      DomHelper.slideIn(this.contentElement, Math.sign(a - b));
    }
  }
  composeBody() {
    this.getConfig('year');
    const {startYear: a} = this, b = super.composeBody(), c = b.children[this.tbar ? 1 : 0].children = [];
    this.widgetMap.title.text = ('000' + a).slice(-4) + ' - ' + ('000' + this.endYear).slice(-4);
    for (let e = 0, f = a; e < this.yearButtonCount; e++, f++) {
      c.push({
        'tag': 'button',
        'dataset': { 'year': f },
        'class': {
          'b-yearpicker-year': 1,
          'b-selected': f === this.year
        },
        'text': ('000' + f).slice(-4)
      });
    }
    return b;
  }
};
YearPicker.initClass();
YearPicker._$name = 'YearPicker';
const DisplayField = class extends TextField {
  static alias = 'display';
  static type = 'displayfield';
  static $name = 'DisplayField';
  static get configurable() {
    return {
      'readOnly': true,
      'editable': false,
      'cls': 'b-display-field',
      'template': null,
      'ariaElement': 'displayElement'
    };
  }
  get focusElement() {
  }
  changeReadOnly() {
    return true;
  }
  changeEditable() {
    return false;
  }
  get inputElement() {
    return {
      'tag': 'span',
      'id': this.id + '-input',
      'reference': 'displayElement',
      'html': this.template ? this.template(this.value) : StringHelper.encodeHtml(this.value)
    };
  }
};
DisplayField.initClass();
DisplayField._$name = 'DisplayField';
const generateMonthNames = () => DateHelper.getMonthNames().map((a, b) => [
  b,
  a
]);
const dateSort = (a, b) => a.valueOf() - b.valueOf();
const {DateSet: DateSet2} = DateHelper;
const emptyArray10 = Object.freeze([]);
const ReadOnlyCombo = class extends Combo {
  static get $name() {
    return 'ReadOnlyCombo';
  }
  static get type() {
    return 'readonlycombo';
  }
  static get configurable() {
    return {
      'editable': false,
      'inputAttributes': {
        'tag': 'div',
        'tabIndex': -1
      },
      'inputValueAttr': 'innerHTML',
      'highlightExternalChange': false,
      'monitorResize': false,
      'triggers': { 'expand': false },
      'picker': {
        'align': {
          'align': 't-b',
          'axisLock': true,
          'matchSize': false
        },
        'cls': 'b-readonly-combo-list',
        'scrollable': { 'overflowX': false }
      }
    };
  }
};
ReadOnlyCombo.initClass();
const DatePicker = class extends CalendarPanel {
  static type = 'datepicker';
  static $name = 'DatePicker';
  static get delayable() {
    return { 'refresh': 'raf' };
  }
  static get configurable() {
    return {
      'activeDate': {
        '$config': 'date',
        'value': 'today'
      },
      'focusable': true,
      'textContent': false,
      'tbar': {
        'overflow': null,
        'items': {
          'prevYear': {
            'cls': 'b-icon b-icon-first b-datepicker-nav-button',
            'onAction': 'up.gotoPrevYear',
            'tooltip': 'L{DatePicker.gotoPrevYear}'
          },
          'prevMonth': {
            'cls': 'b-icon b-icon-previous b-datepicker-nav-button',
            'onAction': 'up.gotoPrevMonth',
            'tooltip': 'L{DatePicker.gotoPrevMonth}'
          },
          'fields': {
            'type': 'container',
            'cls': 'b-datepicker-title',
            'items': {
              'monthField': {
                'type': 'readonlycombo',
                'cls': 'b-datepicker-monthfield',
                'items': generateMonthNames(),
                'internalListeners': { 'select': 'up.onMonthPicked' }
              },
              'yearButton': {
                'type': 'button',
                'cls': 'b-datepicker-yearbutton',
                'internalListeners': { 'click': 'up.onYearPickerRequested' }
              }
            }
          },
          'nextMonth': {
            'cls': 'b-icon b-icon-next b-datepicker-nav-button',
            'onAction': 'up.gotoNextMonth',
            'tooltip': 'L{DatePicker.gotoNextMonth}'
          },
          'nextYear': {
            'cls': 'b-icon b-icon-last b-datepicker-nav-button',
            'onAction': 'up.gotoNextYear',
            'tooltip': 'L{DatePicker.gotoNextYear}'
          }
        }
      },
      'yearPicker': {
        'value': {
          'type': 'YearPicker',
          'yearButtonCount': 16,
          'trapFocus': true,
          'positioned': true,
          'hidden': true,
          'internalListeners': {
            'titleClick': 'up.onYearPickerTitleClick',
            'select': 'up.onYearPicked'
          }
        },
        '$config': 'lazy'
      },
      'date': null,
      'minDate': {
        '$config': 'date',
        'value': null
      },
      'maxDate': {
        '$config': 'date',
        'value': null
      },
      'focusDisabledDates': null,
      'multiSelect': null,
      'selection': {
        '$config': { 'equal': (a, b) => a && a.equals(b) },
        'value': null
      },
      'editMonth': {
        '$config': 'lazy',
        'value': null
      },
      'includeYear': {
        '$config': 'lazy',
        'value': null
      },
      'dayNameFormat': 'dd',
      'trapFocus': true,
      'role': 'grid',
      'focusDescendant': true,
      'alwaysRefreshOnMonthChange': null,
      'highlightSelectedWeek': null
    };
  }
  static get prototypeProperties() {
    return {
      'outOfRangeCls': 'b-out-of-range',
      'activeCls': 'b-active-date',
      'selectedCls': 'b-selected-date'
    };
  }
  construct(a) {
    const b = this;
    super.construct(a);
    b.externalCellRenderer = b.cellRenderer;
    b.cellRenderer = b.internalCellRenderer;
    b.element.setAttribute('aria-activedescendant', b.id + '-active-day');
    b.weeksElement.setAttribute('role', 'grid');
    b.weekElements.forEach(c => c.setAttribute('role', 'row'));
    b.element.setAttribute('ariaLabelledBy', b.widgetMap.fields.id);
    EventHelper.on({
      'element': b.weeksElement,
      'click': {
        'handler': 'onCellClick',
        'delegate': '.' + b.dayCellCls + ':not(.' + b.disabledCls + '):not(.' + b.outOfRangeCls + ')'
      },
      'mousedown': {
        'handler': 'onCellMousedown',
        'delegate': '.' + b.dayCellCls
      },
      'thisObj': b
    });
    b.getConfig('editMonth');
    b.getConfig('includeYear');
    b.refresh.flush();
  }
  afterHide() {
    let a;
    (a = this._yearPicker) == null ? void 0 : a.hide();
    super.afterHide(...arguments);
  }
  doDestroy() {
    let a, b;
    (a = this.yearButton) == null ? void 0 : a.destroy();
    (b = this.monthField) == null ? void 0 : b.destroy();
    super.doDestroy();
  }
  get activeDayCell() {
    return this.weeksElement.querySelector('#' + this.id + '-active-day');
  }
  get focusElement() {
    return !this.disabled && this.activeDayCell || this.element;
  }
  onMonthDateChange(a) {
    const b = super.onMonthDateChange(a);
    this.refresh.flush();
    return b;
  }
  updateEditMonth(a) {
    const {
        monthField: b,
        yearButton: c
      } = this.widgetMap, e = a === false;
    b.readOnly = e;
    c.disabled = e;
  }
  updateIncludeYear(a) {
    const {yearButton: b} = this.widgetMap;
    b.hidden = a === false;
  }
  doRefresh(...a) {
    const b = this, {date: c} = b, e = b.activeDayCell, f = DateHelper.betweenLesser(b.activeDate, b.month.startDate, b.month.endDate) ? b.activeDate : b._activeDate = c;
    b.getConfig('tbar');
    super.doRefresh(...a);
    b.widgetMap.monthField.value = c.getMonth();
    b.widgetMap.yearButton.text = c.getFullYear();
    const g = DateHelper.parseKey(e == null ? void 0 : e.dataset.date);
    if (f - g) {
      b.syncActiveDate(f, g);
    }
  }
  internalCellRenderer({
    cell: a,
    date: b
  }) {
    const c = this, {
        activeCls: e,
        selectedCls: f,
        externalCellRenderer: g
      } = c, i = c.isSelectedDate(b), j = {
        [e]: e && c.isActiveDate(b),
        [f]: i,
        [c.outOfRangeCls]: c.minDate && b < c.minDate || c.maxDate && b > c.maxDate
      };
    if (i) {
      if (c.multiSelect) {
        const k = !c.isSelectedDate(DateHelper.add(b, -1, 'd')), l = !c.isSelectedDate(DateHelper.add(b, 1, 'd'));
        j['b-range-start'] = k;
        j['b-range-end'] = l;
        j['b-in-range'] = !k && !l;
      }
    }
    DomHelper.updateClassList(a, j);
    a.innerHTML = '<div class="b-datepicker-cell-inner">' + b.getDate() + '</div>';
    a.setAttribute('role', 'gridcell');
    a.setAttribute('aria-label', DateHelper.format(b, 'MMMM D, YYYY'));
    if (c.isActiveDate(b)) {
      a.id = c.id + '-active-day';
    } else {
      a.removeAttribute('id');
    }
    if (g) {
      arguments[0].cell = a.firstChild;
      c.callback(g, this, arguments);
    }
  }
  onCellMousedown(a) {
    const b = a.target.closest('[data-date]');
    if (this.focusable) {
      b.focus();
      if (DomHelper.getActiveElement(b) === b) {
        a.preventDefault();
      }
    } else {
      a.preventDefault();
    }
    this.activeDate = DateHelper.parseKey(b.dataset.date);
  }
  onCellClick(a) {
    const b = a.target.closest('[data-date]');
    this.onUIDateSelect(DateHelper.parseKey(b.dataset.date), a);
  }
  onUIDateSelect(a, b) {
    const c = this, {
        lastClickedDate: e,
        multiSelect: f
      } = c;
    if (!c.isDisabledDate(a)) {
      if (c.trigger('beforeDateSelect', {
          'date': a,
          'event': b
        }) !== false) {
        c.lastClickedDate = a;
        c.activatingEvent = b;
        if (f) {
          c.handleMultiSelect(e, a, b);
        } else {
          c.date = c.selection = a;
          if (c.floating) {
            c.hide();
          }
        }
        c.activatingEvent = null;
      }
    }
  }
  handleMultiSelect(a, b, c) {
    const e = this, {multiSelect: f} = e, g = e._selection || (e._selection = new DateSet2()), i = g.dates, j = f === 'range', {
        size: k,
        generation: l
      } = g, n = k && {
        [DateHelper.makeKey(DateHelper.add(i[0], -1, 'd'))]: 1,
        [DateHelper.makeKey(i[0])]: 1,
        [DateHelper.makeKey(i[i.length - 1])]: 1,
        [DateHelper.makeKey(DateHelper.add(i[i.length - 1], 1, 'd'))]: 1
      }, o = g.has(b), p = o ? 'delete' : 'add';
    const q = j && (n == null ? void 0 : n[DateHelper.makeKey(b)]);
    if (c.ctrlKey) {
      if (f === true || !k || q) {
        g[p](b);
        if (j && !g.has(e.rangeStartDate)) {
          e.rangeStartDate.setDate(e.rangeStartDate.getDate() + (b < i[1] ? 1 : -1));
        }
      }
    } else if (c.shiftKey && k) {
      const [u, v] = [
        new Date(j ? e.rangeStartDate || (e.rangeStartDate = i[0]) : a),
        b
      ].sort(dateSort);
      if (j) {
        g.clear();
      }
      for (const w = u; w <= v; w.setDate(w.getDate() + 1)) {
        g.add(w);
      }
    } else if (!(g.has(b) && g.size === 1)) {
      g.clear();
      g.add(b);
    }
    const r = g.size;
    if (r === 1) {
      e.rangeStartDate = b;
    } else if (!r) {
      e.rangeStartDate = null;
    }
    if (g.generation !== l) {
      e.updateSelection(g);
    }
  }
  updateHighlightSelectedWeek(a) {
    this.element.classList.toggle('b-highlight-selected-week', Boolean(a));
  }
  updateMultiSelect(a) {
    this.element.classList.toggle('b-multiselect', Boolean(a));
    if (!a) {
      this.selection = [...this.selection][0];
    }
  }
  get selection() {
    const {_selection: a} = this, b = a ? a.dates : emptyArray10;
    return this.multiSelect === 'range' && b.length ? [
      b[0],
      b[b.length - 1]
    ] : b;
  }
  changeSelection(a, b) {
    const c = this;
    let e, f;
    if (!a) {
      e = new DateSet2();
    } else if (a.isDateSet) {
      if (b === a || (b == null ? void 0 : b.equals(a))) {
        return b;
      }
      f = ObjectHelper.first(a);
      e = a;
    } else {
      if (!a.forEach) {
        a = [a];
      }
      a.forEach((g, j) => a[j] = c.changeDate(g));
      f = a[0];
      a.sort(dateSort);
      if (c.multiSelect === 'range' && a.length === 2) {
        e = new DateSet2();
        for (const g = new Date(a[0]); g <= a[1]; g.setDate(g.getDate() + 1)) {
          e.add(g);
        }
      } else {
        f = a[0];
        e = new DateSet2(a);
      }
    }
    if (f) {
      c.rangeStartDate = DateHelper.clearTime(f);
    }
    return e;
  }
  updateSelection(a) {
    const b = this, {dates: c} = a, e = b.multiSelect === 'range' ? [
        c[0],
        c[c.length - 1]
      ] : c;
    if (!b.isConfiguring) {
      b.refresh.now();
      b.trigger('selectionChange', {
        'selection': e,
        'userAction': Boolean(b.activatingEvent)
      });
    }
  }
  onInternalKeyDown(a) {
    const b = this, c = a.key.trim() || a.code, e = b.activeDate;
    let f = new Date(e);
    if (c === 'Escape' && b.floating) {
      return b.hide();
    }
    if (e && b.weeksElement.contains(a.target)) {
      do {
        switch (c) {
        case 'ArrowLeft':
          a.preventDefault();
          if (a.ctrlKey) {
            f = b.gotoPrevMonth();
          } else {
            f.setDate(f.getDate() - 1);
          }
          break;
        case 'ArrowUp':
          a.preventDefault();
          f.setDate(f.getDate() - 7);
          break;
        case 'ArrowRight':
          a.preventDefault();
          if (a.ctrlKey) {
            f = b.gotoNextMonth();
          } else {
            f.setDate(f.getDate() + 1);
          }
          break;
        case 'ArrowDown':
          a.preventDefault();
          f.setDate(f.getDate() + 7);
          break;
        case 'Enter':
          return b.onUIDateSelect(e, a);
        }
      } while (b.isDisabledDate(f) && !b.focusDisabledDates);
      if (b.minDate && f < b.minDate) {
        return;
      }
      if (b.maxDate && f > b.maxDate) {
        return;
      }
      b.activeDate = f;
    }
  }
  changeMinDate(a) {
    return a && CalendarPanel.prototype.changeDate.apply(this, arguments);
  }
  updateMinDate(a) {
    this._yearpicker && (this._yearpicker.minYear = a == null ? void 0 : a.getFullYear());
    this.refresh();
  }
  changeMaxDate(a) {
    return a && CalendarPanel.prototype.changeDate.apply(this, arguments);
  }
  updateMaxDate(a) {
    this._yearpicker && (this._yearpicker.maxYear = a == null ? void 0 : a.getFullYear());
    this.refresh();
  }
  changeDate(a) {
    return DateHelper.clamp(super.changeDate(a), this.minDate, this.maxDate);
  }
  updateDate(a, b) {
    const c = this, {month: e} = c;
    if (c.isConfiguring && !c.initializingActiveDate) {
      if (!('selection' in c.initialConfig)) {
        c.selection = a;
      }
      if (!('activeDate' in c.initialConfig)) {
        c.activeDate = a;
      }
    }
    if (!e.date || a.getMonth() === e.month || !c.getCell(a) || c.alwaysRefreshOnMonthChange || c.isNavigating) {
      super.updateDate(a);
    } else {
      const f = e.getOtherMonth(a), g = c.eventListeners && (b ? a.getDate() !== b.getDate() | (f.getWeekId(a) !== e.getWeekId(b)) << 1 | (a.getMonth() !== (b == null ? void 0 : b.getMonth())) << 2 | (a.getFullYear() !== (b == null ? void 0 : b.getFullYear())) << 3 : 15);
      c.trigger('dateChange', {
        'changes': {
          'd': true,
          'w': Boolean(g & 2),
          'm': Boolean(g & 12),
          'y': Boolean(g & 8),
          'r': f.weekCount !== e.weekCount
        },
        'value': a,
        'oldValue': b
      });
    }
  }
  changeActiveDate(a, b) {
    const c = this;
    if (a === 'today') {
      a = new Date();
    }
    if (c.trigger('beforeActiveDateChange', {
        'activeDate': a,
        'oldActiveDate': b
      }) === false) {
      return;
    }
    a = a ? c.changeDate(a) : c.date || (c.date = DateHelper.clearTime(new Date()));
    if (isNaN(a)) {
      throw new Error('DatePicker activeDate must be passed a Date, or a YYYY-MM-DD date string');
    }
    return DateHelper.clamp(a, c.minDate, c.maxDate);
  }
  updateActiveDate(a, b) {
    const c = this, {isConfiguring: e} = c;
    if (e || !c.getCell(a)) {
      c.initializingActiveDate = e;
      c.date = a;
      c.initializingActiveDate = false;
    }
    if (!e && !c.refresh.isPending) {
      c.syncActiveDate(a, b);
    }
  }
  syncActiveDate(a, b) {
    const c = this, {activeCls: e} = c, f = c.getCell(a), g = b && c.getCell(b), i = DomHelper.getActiveElement(c.element);
    if (f) {
      c.focusable && f.setAttribute('tabIndex', '0');
      e && f.classList.add(e);
      f.id = c.id + '-active-day';
      if (c.weeksElement.contains(i)) {
        f.focus();
      }
    }
    if (g && g !== f) {
      c.focusable && g.removeAttribute('tabIndex');
      e && g.classList.remove(e);
      g.removeAttribute('id');
    }
  }
  set value(a) {
    const b = this, {
        selection: c,
        duration: e
      } = b;
    b.isNavigating = true;
    if (a) {
      a = b.changeDate(a, b.value);
      if (b.multiSelect === 'range' && (c == null ? void 0 : c.length) === 2) {
        if (!DateHelper.betweenLesserEqual(a, ...c)) {
          if (a < c[0]) {
            b.selection = [
              a,
              DateHelper.add(a, e - 1, 'd')
            ];
          } else {
            b.selection = [
              DateHelper.add(a, -(e - 1), 'd'),
              a
            ];
          }
        }
        b.date = b.activeDate = a;
        return;
      }
      if (a !== void 0) {
        b.selection = a;
        b.date = b.activeDate = a;
      }
    } else {
      b.date = b.activeDate = new Date();
      b.selection = null;
    }
    b.isNavigating = false;
  }
  get value() {
    const {selection: a} = this, b = (a == null ? void 0 : a.length) || null;
    return b && a[b - 1];
  }
  get duration() {
    return this.multiSelect === 'range' ? DateHelper.diff(...this.selection, 'd') + 1 : 1;
  }
  gotoPrevYear() {
    return this.goto(-1, 'year');
  }
  gotoPrevMonth() {
    return this.goto(-1, 'month');
  }
  gotoNextMonth() {
    return this.goto(1, 'month');
  }
  gotoNextYear() {
    return this.goto(1, 'year');
  }
  goto(a, b) {
    const c = this, {activeDate: e} = c, f = e && c.getCell(e);
    let g;
    if (b === 'month' && f && (e == null ? void 0 : e.getMonth()) === c.month.month + a) {
      g = e;
    } else {
      g = DateHelper.add(f ? e : c.date, a, b);
    }
    const i = new Date(g);
    i.setDate(1);
    const j = DateHelper.add(DateHelper.add(i, 1, 'month'), -1, 'day');
    if (c.minDate && a < 0 && j < c.minDate || c.maxDate && a > 0 && i > c.maxDate) {
      return;
    }
    c.isNavigating = true;
    const k = c.date = g;
    if (f) {
      c.activeDate = g;
    }
    c.isNavigating = false;
    return k;
  }
  isActiveDate(a) {
    return !(a - this.activeDate);
  }
  isSelectedDate(a) {
    let b;
    return (b = this._selection) == null ? void 0 : b.has(a);
  }
  onFocusIn(a) {
    let b;
    super.onFocusIn(a);
    if (a.toWidget === this && a._target === this.element && !this.disabled) {
      (b = this.focusElement) == null ? void 0 : b.focus();
    }
  }
  onMonthPicked({
    record: a,
    userAction: b
  }) {
    let c;
    if (b) {
      this.activeDate = DateHelper.add(this.activeDate, a.value - this.activeDate.getMonth(), 'month');
      (c = this.focusElement) == null ? void 0 : c.focus();
    }
  }
  onYearPickerRequested() {
    const {yearPicker: a} = this;
    if (a.isVisible) {
      a.hide();
    } else {
      a.year = a.startYear = this.activeDate.getFullYear();
      a.show();
      a.focus();
    }
  }
  onYearPickerTitleClick() {
    this.yearPicker.hide();
  }
  onYearPicked({
    value: a,
    source: b
  }) {
    let c;
    const e = new Date(this.activeDate);
    e.setFullYear(a);
    this.activeDate = e;
    (c = this.focusElement) == null ? void 0 : c.focus({ 'preventScroll': true });
    b.hide();
  }
  changeYearPicker(a, b) {
    let c, e;
    return YearPicker.reconfigure(b, a ? YearPicker.mergeConfigs({
      'owner': this,
      'appendTo': this.element,
      'minYear': (c = this.minDate) == null ? void 0 : c.getFullYear(),
      'maxYear': (e = this.maxDate) == null ? void 0 : e.getFullYear()
    }, a) : null, this);
  }
  get childItems() {
    const {_yearPicker: a} = this, b = super.childItems;
    if (a) {
      b.push(a);
    }
    return b;
  }
  updateLocalization() {
    let a;
    const {monthField: b} = this.widgetMap, c = generateMonthNames();
    if (!this.isConfiguring && !c.every((e, f) => e[1] === b.store.getAt(f).text)) {
      c[(a = b.value) != null ? a : this.date.getMonth()].selected = true;
      b.items = c;
    }
    super.updateLocalization();
  }
};
DatePicker.initClass();
DatePicker._$name = 'DatePicker';
const DateField = class extends PickerField {
  static alias = 'date';
  static type = 'datefield';
  static $name = 'DateField';
  static get configurable() {
    return {
      'format': 'L',
      'strictParsing': false,
      'fallbackFormat': 'YYYY-MM-DD',
      'timeFormat': 'HH:mm:ss:SSS',
      'keepTime': false,
      'pickerFormat': null,
      'validateDateOnly': null,
      'triggers': {
        'expand': {
          'cls': 'b-icon-calendar',
          'handler': 'onTriggerClick',
          'weight': 200
        },
        'back': {
          'cls': 'b-icon b-icon-angle-left b-step-trigger b-step-trigger-back',
          'key': 'Shift+ArrowDown',
          'handler': 'onBackClick',
          'align': 'start',
          'weight': 100
        },
        'forward': {
          'cls': 'b-icon b-icon-angle-right b-step-trigger b-step-trigger-forward',
          'key': 'Shift+ArrowUp',
          'handler': 'onForwardClick',
          'align': 'end',
          'weight': 100
        }
      },
      'calendarContainerCls': '',
      'min': null,
      'max': null,
      'step': false,
      'stepTriggers': {
        '$config': null,
        'default': true
      },
      'weekStartDay': null,
      'picker': {
        'type': 'datepicker',
        'role': 'dialog',
        'floating': true,
        'scrollAction': 'realign',
        'align': {
          'align': 't0-b0',
          'axisLock': true
        }
      },
      'value': null
    };
  }
  changePicker(a, b) {
    let c;
    if (a == null ? void 0 : a.isDatePicker) {
      return a;
    }
    const e = this, f = DatePicker.mergeConfigs({
        'owner': e,
        'forElement': e.pickerAlignElement,
        'minDate': e.min,
        'maxDate': e.max,
        'weekStartDay': e._weekStartDay,
        'align': {
          'anchor': e.overlayAnchor,
          'target': e.pickerAlignElement
        },
        'onSelectionChange': ({
          selection: i,
          source: j
        }) => {
          if (j.isVisible) {
            e._isUserAction = e._isPickerInput = true;
            e.value = e.value ? DateHelper.copyTimeValues(new Date(i[0]), e.value) : new Date(i[0]);
            e._isPickerInput = e._isUserAction = false;
            j.hide();
          }
        }
      }, a);
    if (e.calendarContainerCls) {
      f.cls = e.calendarContainerCls;
    }
    if (e.value) {
      f.value = e.value;
    } else if (!f.activeDate) {
      f.activeDate = new Date();
    }
    const g = DatePicker.reconfigure(b, a ? f : null, e);
    (c = g == null ? void 0 : g.refresh) == null ? void 0 : c.flush();
    return g;
  }
  getStepDate(a) {
    const {step: b} = this;
    return DateHelper.add(this.value, (a || 1) * b.magnitude, b.unit);
  }
  stepDate(a, b = true) {
    const c = this;
    if (!c.readOnly && c.value) {
      const e = c.getStepDate(a);
      if (!c.checkMinMax(e)) {
        if (b) {
          c._isUserAction = true;
          c.value = e;
          c.autoSelect && c.select();
          c._isUserAction = false;
        } else {
          c.value = e;
        }
      }
    }
  }
  onBackClick() {
    this.stepDate(-1);
  }
  onForwardClick() {
    this.stepDate(1);
  }
  showPicker(a) {
    if (this.readOnly) {
      return;
    }
    const b = this, {_picker: c} = b;
    if (c) {
      const e = {
        'minDate': b.min,
        'maxDate': b.max
      };
      if (b.value) {
        e.value = b.value;
      } else if (!c.activeDate) {
        e.activeDate = new Date();
      }
      c.setConfig(e);
    }
    super.showPicker(a);
  }
  focusPicker() {
    let a;
    (a = this.picker) == null ? void 0 : a.focus();
  }
  checkMinMax(a) {
    const {
      min: b,
      max: c
    } = this;
    return b && a < b ? -1 : c && a > c ? 1 : 0;
  }
  get isValid() {
    const a = this;
    a.clearError('L{Field.minimumValueViolation}', true);
    a.clearError('L{Field.maximumValueViolation}', true);
    let {value: b} = a;
    if (b) {
      let {
        min: c,
        max: e,
        validateDateOnly: f
      } = a;
      if (f) {
        b = DateHelper.clearTime(b, false);
        if (c) {
          c = DateHelper.clearTime(c, false);
        }
        if (e) {
          e = DateHelper.clearTime(e, false);
        }
      }
      if (c && b < c) {
        a.setError('L{Field.minimumValueViolation}', true);
        return false;
      }
      if (e && b > e) {
        a.setError('L{Field.maximumValueViolation}', true);
        return false;
      }
    }
    return super.isValid;
  }
  transformDateValue(a, b = true) {
    const c = this;
    if (a != null) {
      if (!DateHelper.isDate(a)) {
        if (typeof a === 'string') {
          a = DateHelper.parse(a, c.format, c.strictParsing) || DateHelper.parse(a, c.fallbackFormat, c.strictParsing);
        } else {
          a = new Date(a);
        }
      }
      if (DateHelper.isValidDate(a)) {
        if (b && (!c.min || a - c.min > -DateHelper.MS_PER_DAY) && (!c.max || a <= c.max)) {
          return c.transformTimeValue(a);
        }
        return a;
      }
    }
    return null;
  }
  transformTimeValue(a) {
    const b = this, {keepTime: c} = b;
    a = DateHelper.clone(a);
    if (!c) {
      DateHelper.clearTime(a, false);
    } else if (c !== 'entered' && c !== true) {
      const e = DateHelper.parse(c, b.timeFormat);
      if (DateHelper.isValidDate(e)) {
        DateHelper.copyTimeValues(a, e);
      } else if (DateHelper.isValidDate(b.value)) {
        DateHelper.copyTimeValues(a, b.value);
      }
    } else if (c === true && (b._isPickerInput || b.inputting) && DateHelper.isValidDate(b.value)) {
      DateHelper.copyTimeValues(a, b.value);
    }
    return a;
  }
  changeMin(a) {
    return this.transformDateValue(a, false);
  }
  updateMin(a) {
    const {
      input: b,
      _picker: c
    } = this;
    if (b) {
      if (a == null) {
        b.removeAttribute('min');
      } else {
        b.min = a;
      }
    }
    if (c) {
      c.minDate = a;
    }
    this.syncInvalid();
  }
  changeMax(a) {
    return this.transformDateValue(a, false);
  }
  updateMax(a) {
    const {
      input: b,
      _picker: c
    } = this;
    if (b) {
      if (a == null) {
        b.removeAttribute('max');
      } else {
        b.max = a;
      }
    }
    if (c) {
      c.maxDate = a;
    }
    this.syncInvalid();
  }
  get weekStartDay() {
    return typeof this._weekStartDay === 'number' ? this._weekStartDay : DateHelper.weekStartDay;
  }
  updateWeekStartDay(a) {
    if (this._picker) {
      this._picker.weekStartDay = a;
    }
  }
  changeValue(a, b) {
    const c = this, e = c.transformDateValue(a);
    if (a && !e) {
      if (!c.inputting || c.validateOnInput) {
        c.setError('L{invalidDate}');
      }
      return;
    }
    c.clearError('L{invalidDate}');
    if (c.hasChanged(b, e)) {
      return super.changeValue(e, b);
    }
    if (!c.inputting) {
      c.syncInputFieldValue();
    }
  }
  updateValue(a, b) {
    const c = this._picker;
    if (c && !this.inputting) {
      c.value = c.activeDate = a;
    }
    super.updateValue(a, b);
  }
  changeStep(a, b) {
    const c = typeof a;
    if (!a) {
      return null;
    }
    if (c === 'number') {
      a = {
        'magnitude': Math.abs(a),
        'unit': b ? b.unit : 'day'
      };
    } else if (c === 'string') {
      a = DateHelper.parseDuration(a);
    }
    if (a && a.unit && a.magnitude) {
      if (a.magnitude < 0) {
        a = {
          'magnitude': -a.magnitude,
          'unit': a.unit
        };
      }
      return a;
    }
  }
  updateStep(a) {
    this.element.classList[a ? 'remove' : 'add']('b-no-steppers');
    this.syncInvalid();
  }
  hasChanged(a, b) {
    if ((a == null ? void 0 : a.getTime) && (b == null ? void 0 : b.getTime) && this.keepTime !== 'entered' && this.keepTime !== true) {
      return !DateHelper.isEqual(DateHelper.clearTime(a), DateHelper.clearTime(b));
    }
    return super.hasChanged(a && a.getTime(), b && b.getTime());
  }
  get inputValue() {
    const a = this.value;
    return a ? DateHelper.format(a, this.format) : '';
  }
  updateFormat() {
    if (!this.isConfiguring) {
      this.syncInputFieldValue(true);
    }
  }
  updateLocalization() {
    super.updateLocalization();
    this.syncInputFieldValue(true);
  }
  internalOnKeyEvent(a) {
    let b;
    super.internalOnKeyEvent(a);
    if (a.type === 'keydown') {
      const c = this;
      let e;
      switch (a.virtualKey) {
      case 'Enter':
        c.isValid && ((b = c.picker) == null ? void 0 : b.hide());
        break;
      case 'ArrowLeft':
        if (c.caretAtStart || c.selectedAll) {
          e = -1;
        }
        break;
      case 'ArrowRight':
        if (c.caretAtEnd || c.selectedAll) {
          e = 1;
        }
        break;
      case 'NumpadAdd':
        e = 1;
        break;
      case 'NumpadSubtract':
        e = -1;
        break;
      }
      if (e && c.step) {
        c.stepDate(e);
        c.selectAll();
        a.preventDefault();
      }
    }
  }
};
DateField.initClass();
DateField._$name = 'DateField';
const NumberHelper = class {
  static get $name() {
    return 'NumberHelper';
  }
  static getLocalizedFormatter(a) {
    return typeof a === 'string' ? NumberFormat.get({
      'locale': LocaleManager_default.localeCode,
      'template': a
    }) : (a == null ? void 0 : a.locale) != null ? NumberFormat.get(a) : NumberFormat.get(Object.assign({}, a, { 'locale': LocaleManager_default.localeCode }));
  }
};
NumberHelper._$name = 'NumberHelper';
const preventDefault = a => a.ctrlKey && a.preventDefault();
const NumberField = class extends Field {
  static alias = 'number';
  static type = 'numberfield';
  static $name = 'NumberField';
  static get configurable() {
    return {
      'wrapAround': null,
      'min': null,
      'max': null,
      'step': 1,
      'largeStep': 0,
      'value': null,
      'format': '',
      'triggers': { 'spin': { 'type': 'spintrigger' } },
      'changeOnSpin': true,
      'inputType': null
    };
  }
  construct(a) {
    super.construct(a);
    const b = this;
    b.input.addEventListener('dblclick', () => {
      b.select();
    });
    if (typeof b.changeOnSpin === 'number') {
      b.bufferedSpinChange = b.buffer(b.triggerChange, b.changeOnSpin);
    }
  }
  acceptValue(a, b) {
    let c = !isNaN(a);
    if (c && !this.hasTextSelection) {
      c = false;
      const e = this.input.value, f = parseFloat(e);
      if (e !== b) {
        c = !this.acceptValue(f, e);
      }
    }
    return c;
  }
  okMax(a) {
    return isNaN(this.max) || a <= this.max;
  }
  okMin(a) {
    return isNaN(this.min) || a >= this.min;
  }
  internalOnKeyEvent(a) {
    if (a.type === 'keydown') {
      const b = this, c = a.key;
      let f;
      if (c === 'ArrowUp') {
        b.doSpinUp(a.shiftKey);
        f = true;
      } else if (c === 'ArrowDown') {
        b.doSpinDown(a.shiftKey);
        f = true;
      } else if (!a.altKey && !a.ctrlKey && c && c.length === 1) {
        const g = b.getAfterValue(c), i = b.formatter.parseStrict(g), j = i === b.value || g === '-' && (isNaN(b.min) || b.min < 0);
        f = !j && !b.acceptValue(i, g);
      }
      if (c === 'Enter' && b._changedBySilentSpin) {
        b.triggerChange(a, true);
        b._changedBySilentSpin = false;
      }
      if (f) {
        a.preventDefault();
      }
    }
    super.internalOnKeyEvent(a);
  }
  doSpinUp(a = false) {
    const b = this;
    if (b.readOnly) {
      return;
    }
    let c = (b.value || 0) + (a ? b.largeStep : b.step);
    if (!b.okMin(c)) {
      c = b.min;
    }
    if (b.okMax(c)) {
      b.applySpinChange(c);
    } else if (b.wrapAround) {
      c = b.min;
      b.applySpinChange(c);
    }
  }
  doSpinDown(a = false) {
    const b = this, c = a ? b.largeStep : b.step;
    if (b.readOnly) {
      return;
    }
    let e = (b.value || 0) - c;
    if (!b.okMax(e)) {
      e = b.max;
    }
    if (b.okMin(e)) {
      b.applySpinChange(e);
    } else if (b.wrapAround) {
      e = b.max - b.max % c;
      b.applySpinChange(e);
    }
  }
  applySpinChange(a) {
    const b = this;
    b._isUserAction = true;
    if (b.changeOnSpin !== true) {
      b._changedBySilentSpin = true;
      b.silenceChange = true;
      b.bufferedSpinChange && b.bufferedSpinChange(null, true);
    }
    b.value = a;
    b._isUserAction = false;
    b.silenceChange = false;
  }
  triggerChange() {
    if (!this.silenceChange) {
      super.triggerChange(...arguments);
    }
  }
  onFocusOut(a) {
    let b, c, f;
    super.onFocusOut(...arguments);
    const g = this, {input: i} = g, j = i.value, k = g.formatter.truncate(j), l = isNaN(k) ? j : g.formatValue(k);
    (f = (c = (b = g.triggers) == null ? void 0 : b.spin) == null ? void 0 : c.clickRepeater) == null ? void 0 : f.cancel();
    g.lastTouchmove = null;
    if (j !== l) {
      i.value = l;
    }
    if (g._changedBySilentSpin) {
      g.triggerChange(a, true);
      g._changedBySilentSpin = false;
    }
  }
  internalOnInput(a) {
    const b = this, {
        formatter: c,
        input: e
      } = b, {
        parser: f,
        maximumFractionDigits: g
      } = c, i = e.value, j = f.decimalPlaces(i);
    if (c.truncator && j > g) {
      let k = i, l;
      const n = c.truncate(i);
      if (!isNaN(n)) {
        k = b.formatValue(n);
        l = f.decimalPlaces(k);
        if (l < g) {
          k += '0'.repeat(g - l);
          l = g;
        }
        if (l < j) {
          const o = i.length - b.caretPos;
          e.value = k;
          b.caretPos = k.length - o + 1;
        }
      }
    }
    super.internalOnInput(a);
  }
  formatValue(a) {
    return this.formatter.format(a);
  }
  get formatter() {
    const a = this;
    if (a._formatter !== void 0) {
      return a._formatter;
    }
    return a._formatter = NumberHelper.getLocalizedFormatter(a.format);
  }
  updateStep(a) {
    let b;
    const c = this;
    c.element.classList.toggle('b-hide-spinner', !a);
    c._step = a;
    if (a && BrowserHelper.isMobile) {
      if (!c.touchMoveListener) {
        c.touchMoveListener = EventHelper.on({
          'element': c.input,
          'touchmove': 'onInputSwipe',
          'thisObj': c,
          'throttled': {
            'buffer': 150,
            'alt': preventDefault
          }
        });
      }
    } else {
      (b = c.touchMoveListener) == null ? void 0 : b.call(c);
    }
  }
  onInputSwipe(a) {
    const {lastTouchmove: b} = this;
    if (b) {
      const c = a.screenX - b.screenX, f = b.screenY - a.screenY, g = Math.abs(c) > Math.abs(f) ? c : f;
      this['doSpin' + (g > 0 ? 'Up' : 'Down')]();
    }
    a.preventDefault();
    this.lastTouchmove = a;
  }
  changeLargeStep(a) {
    return a || this.step * 10;
  }
  get validity() {
    const a = this.value, b = {};
    if (a != null) {
      b.rangeUnderflow = !this.okMin(a);
      b.rangeOverflow = !this.okMax(a);
    }
    b.valid = !b.rangeUnderflow && !b.rangeOverflow;
    return b;
  }
  changeValue(a, b) {
    const c = this;
    if (a || a === 0) {
      let e;
      if (typeof a !== 'number') {
        a = typeof a === 'string' ? c.formatter.parse(a) : Number(a);
        e = isNaN(a);
        if (e) {
          a = '';
        }
      }
      if (!e && c.format) {
        a = c.formatter.round(a);
      }
    } else {
      a = void 0;
    }
    return super.changeValue(a, b);
  }
  get inputValue() {
    return this.value != null ? this.formatValue(this.value) : '';
  }
  updateLocalization() {
    super.updateLocalization();
    this._formatter = void 0;
    this.syncInputFieldValue();
  }
};
NumberField.initClass();
NumberField._$name = 'NumberField';
const TimePicker = class extends Panel {
  static configurable = {
    'floating': true,
    'layout': 'hbox',
    'items': {
      'hour': {
        'label': 'L{TimePicker.hour}',
        'type': 'number',
        'min': 0,
        'max': 23,
        'highlightExternalChange': false,
        'format': '2>9',
        'wrapAround': true
      },
      'minute': {
        'label': 'L{TimePicker.minute}',
        'type': 'number',
        'min': 0,
        'max': 59,
        'highlightExternalChange': false,
        'format': '2>9',
        'wrapAround': true
      },
      'second': {
        'hidden': true,
        'label': 'L{TimePicker.second}',
        'type': 'number',
        'min': 0,
        'max': 59,
        'highlightExternalChange': false,
        'format': '2>9',
        'wrapAround': true
      },
      'amPm': {
        'type': 'buttongroup',
        'items': {
          'amButton': {
            'type': 'button',
            'text': 'AM',
            'toggleGroup': 'am-pm',
            'cls': 'b-blue',
            'onClick': 'up.onAmPmButtonClick'
          },
          'pmButton': {
            'type': 'button',
            'text': 'PM',
            'toggleGroup': 'am-pm',
            'cls': 'b-blue',
            'onClick': 'up.onAmPmButtonClick'
          }
        }
      }
    },
    'autoShow': false,
    'trapFocus': true,
    'seconds': null,
    'value': {
      '$config': { 'equal': 'date' },
      'value': null
    },
    'format': null,
    'max': null,
    'min': null,
    'initialValue': null
  };
  static type = 'timepicker';
  static $name = 'TimePicker';
  construct(a) {
    super.construct(a);
    this.refresh();
  }
  updateSeconds(a) {
    this.widgetMap.second[a ? 'show' : 'hide']();
  }
  onFieldChange() {
    if (!this.isConfiguring && !this.isRefreshing) {
      this.value = this.pickerToTime();
    }
  }
  onAmPmButtonClick({source: a}) {
    this._pm = a.ref === 'pmButton';
    if (this._value) {
      this.value = this.pickerToTime();
    }
  }
  onInternalKeyDown(a) {
    let b;
    const c = this;
    switch (a.key) {
    case 'Escape':
      c.triggerTimeChange(c._initialValue);
      c.hide();
      a.preventDefault();
      return;
    case 'Enter':
      c.value = c.pickerToTime();
      c.hide();
      a.preventDefault();
      return;
    }
    (b = super.onInternalKeyDown) == null ? void 0 : b.call(this, a);
  }
  pickerToTime() {
    const a = this, b = a._pm, {
        hour: c,
        minute: e,
        second: f
      } = a.widgetMap;
    c.format = a._is24Hour ? '2>9' : null;
    let g = c.value, i = new Date(a.value);
    if (!a._is24Hour) {
      if (b && g < 12)
        g = g + 12;
      if (!b && g === 12)
        g = 0;
    }
    i.setHours(g);
    i.setMinutes(e.value);
    if (a.seconds) {
      i.setSeconds(f.value);
    }
    if (a._min) {
      i = DateHelper.max(a._min, i);
    }
    if (a._max) {
      i = DateHelper.min(a._max, i);
    }
    return i;
  }
  triggerTimeChange(a) {
    this.trigger('timeChange', { 'time': a });
  }
  updateInitialValue(a) {
    this.value = a;
  }
  changeValue(a) {
    if (a) {
      a = typeof a === 'string' ? DateHelper.parse(a, this.format) : a;
    }
    if (!this.isVisible) {
      this._initialValue = a;
    }
    return a != null ? a : DateHelper.getTime(0);
  }
  updateValue(a) {
    if (this.isVisible) {
      this.triggerTimeChange(a);
    }
    this.refresh();
  }
  updateFormat(a) {
    this._is24Hour = DateHelper.is24HourFormat(a);
    this.refresh();
  }
  changeMin(a) {
    return typeof a === 'string' ? DateHelper.parse(a, this.format) : a;
  }
  changeMax(a) {
    return typeof a === 'string' ? DateHelper.parse(a, this.format) : a;
  }
  refresh() {
    const a = this;
    if (!a.isConfiguring && a.value) {
      a.isRefreshing = true;
      const {
          hour: b,
          minute: c,
          second: e,
          amButton: f,
          pmButton: g
        } = a.widgetMap, i = a.value, j = a._is24Hour, k = i.getHours(), l = a._pm = k >= 12;
      a.element.classList[j ? 'add' : 'remove']('b-24h');
      b.min = j ? 0 : 1;
      b.max = j ? 23 : 12;
      b.value = j ? k : k % 12 || 12;
      c.value = i.getMinutes();
      e.value = i.getSeconds();
      f.pressed = !l;
      g.pressed = l;
      f.hidden = g.hidden = j;
      a.isRefreshing = false;
    }
  }
};
TimePicker.initClass();
TimePicker._$name = 'TimePicker';
const TimeField = class extends PickerField {
  static alias = 'time';
  static type = 'timefield';
  static $name = 'TimeField';
  static get configurable() {
    return {
      'picker': {
        'type': 'timepicker',
        'align': {
          'align': 't0-b0',
          'axisLock': true
        }
      },
      'format': 'LT',
      'triggers': {
        'expand': {
          'align': 'end',
          'handler': 'onTriggerClick',
          'compose': () => ({ 'children': [{ 'class': { 'b-icon-clock-live': 1 } }] })
        },
        'back': {
          'align': 'start',
          'cls': 'b-icon b-icon-angle-left b-step-trigger',
          'key': 'Shift+ArrowDown',
          'handler': 'onBackClick'
        },
        'forward': {
          'align': 'end',
          'cls': 'b-icon b-icon-angle-right b-step-trigger',
          'key': 'Shift+ArrowUp',
          'handler': 'onForwardClick'
        }
      },
      'min': null,
      'max': null,
      'step': '5m',
      'stepTriggers': {
        '$config': null,
        'default': true
      },
      'value': null,
      'keepDate': true
    };
  }
  changePicker(a, b) {
    const c = this;
    return TimePicker.reconfigure(b, a, {
      'owner': c,
      'defaults': {
        'value': c.value,
        'forElement': c.pickerAlignElement,
        'owner': c,
        'align': {
          'anchor': c.overlayAnchor,
          'target': c.pickerAlignElement
        },
        'onTimeChange'({time: e}) {
          c._isUserAction = true;
          c.value = e;
          c._isUserAction = false;
        }
      }
    });
  }
  onBackClick() {
    const a = this, {min: b} = a;
    if (!a.readOnly && a.value) {
      const c = DateHelper.add(a.value, -1 * a.step.magnitude, a.step.unit);
      if (!b || b.getTime() <= c) {
        a.value = c;
      }
    }
  }
  onForwardClick() {
    const a = this, {max: b} = a;
    if (!a.readOnly && a.value) {
      const c = DateHelper.add(a.value, a.step.magnitude, a.step.unit);
      if (!b || b.getTime() >= c) {
        a.value = c;
      }
    }
  }
  get isValid() {
    const a = this;
    a.clearError('L{minimumValueViolation}', true);
    a.clearError('L{maximumValueViolation}', true);
    let b = a.value;
    if (b) {
      b = b.getTime();
      if (a._min && a._min.getTime() > b) {
        a.setError('L{minimumValueViolation}', true);
        return false;
      }
      if (a._max && a._max.getTime() < b) {
        a.setError('L{maximumValueViolation}', true);
        return false;
      }
    }
    return super.isValid;
  }
  hasChanged(a, b) {
    if ((a == null ? void 0 : a.getTime) && (b == null ? void 0 : b.getTime)) {
      return this.keepDate ? a - b !== 0 : !DateHelper.isSameTime(a, b);
    }
    return super.hasChanged(a, b);
  }
  showPicker() {
    const a = this, {
        picker: b,
        value: c
      } = a;
    if (a.readOnly) {
      return;
    }
    b.value = c;
    b.format = a.format;
    b.maxTime = a.max;
    b.minTime = a.min;
    if (!c) {
      a.value = b.value;
    }
    super.showPicker(true);
  }
  onPickerShow() {
    let a;
    super.onPickerShow();
    this.pickerKeyDownRemover = (a = this.pickerKeyDownRemover) == null ? void 0 : a.call(this);
  }
  focusPicker() {
    this.picker.focus();
  }
  transformTimeValue(a) {
    if (a != null) {
      if (typeof a === 'string') {
        a = DateHelper.parse(a, this.format);
        if (this.keepDate && a && this.value) {
          a = DateHelper.copyTimeValues(new Date(this.value), a);
        }
      } else {
        a = new Date(a);
      }
      if (DateHelper.isValidDate(a)) {
        if (!this.keepDate) {
          a = DateHelper.getTime(a);
        }
      } else {
        a = null;
      }
    }
    const b = { 'value': a };
    this.trigger('transformTimeValue', b);
    a = b.value;
    return a;
  }
  changeMin(a) {
    return this.transformTimeValue(a);
  }
  updateMin(a) {
    const {input: b} = this;
    if (b) {
      if (a == null) {
        b.removeAttribute('min');
      } else {
        b.min = a;
      }
    }
    this.syncInvalid();
  }
  changeMax(a) {
    return this.transformTimeValue(a);
  }
  updateMax(a) {
    const {input: b} = this;
    if (b) {
      if (a == null) {
        b.removeAttribute('max');
      } else {
        b.max = a;
      }
    }
    this.syncInvalid();
  }
  changeValue(a, b) {
    const c = this, e = c.transformTimeValue(a);
    if (a && !e || c.isRequired && a === '') {
      c.setError('L{invalidTime}');
      return;
    }
    c.clearError('L{invalidTime}');
    if (c.hasChanged(b, e)) {
      return super.changeValue(e, b);
    }
    if (!c.inputting) {
      c.syncInputFieldValue(true);
    }
  }
  updateValue(a, b) {
    const {expand: c} = this.triggers;
    if (c && a) {
      c.element.firstElementChild.style.animationDelay = -((a.getHours() * 60 + a.getMinutes()) / 10) + 's';
    }
    super.updateValue(a, b);
  }
  changeStep(a, b) {
    const c = typeof a;
    if (!a) {
      return null;
    }
    if (c === 'number') {
      a = {
        'magnitude': Math.abs(a),
        'unit': b ? b.unit : 'hour'
      };
    } else if (c === 'string') {
      a = DateHelper.parseDuration(a);
    }
    if ((a == null ? void 0 : a.unit) && (a == null ? void 0 : a.magnitude)) {
      if (a.magnitude < 0) {
        a = {
          'magnitude': -a.magnitude,
          'unit': a.unit
        };
      }
      return a;
    }
  }
  updateStep(a) {
    this.element.classList[a ? 'remove' : 'add']('b-no-steppers');
    this.syncInvalid();
  }
  updateFormat() {
    this.syncInputFieldValue(true);
  }
  get inputValue() {
    return DateHelper.format(this.value, this.format);
  }
  updateLocalization() {
    super.updateLocalization();
    this.syncInputFieldValue(true);
  }
};
TimeField.initClass();
TimeField._$name = 'TimeField';
const AbstractDateRange_default = a => {
  let b;
  return b = class extends a.mixin(Fencible_default) {
    get dateFieldDefaults() {
      return ObjectHelper.merge({ 'format': this.format }, this._dateFieldDefaults);
    }
    configureDateField(c, e, f) {
      const g = this, {dateStepTriggers: i} = g;
      return DateField.reconfigure(e, c, {
        'owner': g,
        'defaults': ObjectHelper.assign({ 'stepTriggers': i === true || i === 'both' || i === f }, g.dateFieldDefaults)
      });
    }
    changeFieldEndDate(c, e) {
      return this.configureDateField(c, e, 'end');
    }
    updateFieldEndDate(c) {
      let e;
      c && ((e = this.dateFieldContainer) == null ? void 0 : e.add(c));
    }
    changeFieldStartDate(c, e) {
      return this.configureDateField(c, e, 'start');
    }
    updateFieldStartDate(c) {
      let e;
      c && ((e = this.dateFieldContainer) == null ? void 0 : e.add(c));
    }
    updateFormat(c) {
      if (!this.isConstructing) {
        this.fieldEndDate.format = c;
        this.fieldStartDate.format = c;
      }
    }
    updatePickingStartDate(c) {
      this.element.classList.toggle('b-picking-start-date', Boolean(c));
    }
    updateValue(c, e) {
      let f;
      (f = super.updateValue) == null ? void 0 : f.call(this, c, e);
      this.syncValue('value', c);
    }
    stepDown(c) {
      this.adjustByKey(c, this.stepSmall);
    }
    stepUp(c) {
      this.adjustByKey(c, '-' + this.stepSmall);
    }
    stepDownLarge(c) {
      this.adjustByKey(c, this.stepLarge);
    }
    stepUpLarge(c) {
      this.adjustByKey(c, '-' + this.stepLarge);
    }
    adjustByKey(c, e) {
      let f, g;
      const i = this, {_isUserAction: j} = i, {target: k} = c, l = [
          i.fieldStartDate,
          i.fieldEndDate
        ], n = (k === l[0].input && 1 || k === l[1].input && 2 || 0) - 1, o = l[n], p = [
          l[0].value,
          l[1].value
        ];
      p[n] = DateHelper.add((f = p[n]) != null ? f : DateHelper.clearTime(new Date()), e);
      p[1 - n] = (g = p[1 - n]) != null ? g : p[n];
      if (p[1] < p[0]) {
        p[1 - n] = p[n];
      }
      if (i.checkValid(p)) {
        i._isUserAction = true;
        l[0].value = p[0];
        l[1].value = p[1];
        i._isUserAction = j;
      }
      o.selectAll();
      c.preventDefault();
    }
    checkMinMax(c = this.value) {
      const {
        maxDate: e,
        minDate: f,
        validateDateOnly: g
      } = this;
      let i = 0, j, k;
      if (c && (f || e)) {
        k = Array.isArray(c) ? c : [c];
        for (j of k) {
          j = g ? DateHelper.clearTime(j) : j;
          i = f && j < f ? -1 : e && j > e ? 1 : 0;
          if (i) {
            break;
          }
        }
      }
      return i;
    }
    checkValid(c = this.value) {
      return !this.checkMinMax(c);
    }
    hasChanged(c, e) {
      return !ObjectHelper.isEqual(c, e);
    }
    onDateFieldChange({
      source: c,
      userAction: e
    }) {
      const f = this, {
          fieldEndDate: g,
          fieldStartDate: i,
          _isUserAction: j
        } = f;
      f._isUserAction = e || j;
      f.syncValue(c.ref, [
        i.value,
        g.value
      ]);
      f._isUserAction = j;
    }
    onStartDateFocusIn() {
      this.pickingStartDate = true;
    }
    onStartDateFocusOut(c) {
      this.pickingStartDate = false;
    }
    syncValue(c, e = this.value) {
      const f = this;
      if (c !== 'fieldEndDate') {
        f.fieldEndDate.value = e && e[1];
      }
      if (c !== 'fieldStartDate') {
        f.fieldStartDate.value = e && e[0];
      }
      if (c !== 'value') {
        f.value = e;
      }
      if (!f.isConstructing && f.hasChanged(f._lastValue, e) && f.checkValid(e)) {
        f.triggerChange();
      }
      f._lastValue = e;
    }
    get widgetClass() {
    }
  }, __publicField(b, 'configurable', {
    'altFormats': ['YYYY-MM-DD'],
    'dateFieldDefaults': {
      'type': 'datefield',
      'autoSelect': true,
      'picker': null,
      'step': '1d',
      'internalListeners': { 'change': 'up.onDateFieldChange' },
      'triggers': {
        'expand': null,
        'back': {
          'align': 'end',
          'weight': 101
        }
      }
    },
    'fieldEndDate': {
      'ref': 'fieldEndDate',
      'cls': 'b-end-date',
      'weight': 40
    },
    'fieldStartDate': {
      'ref': 'fieldStartDate',
      'cls': 'b-start-date',
      'weight': 30,
      'internalListeners': {
        'focusIn': 'up.onStartDateFocusIn',
        'focusOut': 'up.onStartDateFocusOut'
      }
    },
    'dateStepTriggers': null,
    'format': 'ddd, MMM D',
    'keyMap': {
      'ArrowDown': 'stepDown',
      'ArrowUp': 'stepUp',
      'Ctrl+ArrowDown': 'stepDownLarge',
      'Ctrl+ArrowUp': 'stepUpLarge'
    },
    'pickingStartDate': null,
    'stepLarge': '1 month',
    'stepSmall': '7 days',
    'validateDateOnly': true,
    'value': {
      '$config': {
        'type': 'date[]',
        'formats': [
          'format',
          'altFormats'
        ]
      }
    }
  }), __publicField(b, 'fenced', { 'syncValue': true }), __publicField(b, 'prototypeProperties', { '_lastValue': null }), b;
};
const {isArray: isArray3} = Array;
const {DateSet: DateSet3} = DateHelper;
const getMonthNumber = a => a.getFullYear() * 12 + a.getMonth();
const materialRe = /material/i;
const MultiDatePicker = class extends Carousel.mixin(Fencible_default) {
  static fenced = { 'syncSelection': true };
  static configurable = {
    'configureSlot': 'configureDatePicker',
    'disableReserveSlots': 'inert',
    'slots': 2,
    'baseDate': {
      '$config': 'day',
      'value': 'today'
    },
    'cellRenderer': null,
    'headerRenderer': null,
    'weekRenderer': null,
    'weekStartDay': null,
    'date': {
      '$config': 'day',
      'value': 'today'
    },
    'minDate': { '$config': 'day' },
    'maxDate': { '$config': 'day' },
    'datePickerDefaults': {
      'type': 'datepicker',
      'activeDate': null,
      'animateTimeShift': false,
      'dayNameFormat': 'd1',
      'multiSelect': 'range',
      'trapFocus': false,
      'internalListeners': { 'selectionChange': 'up.onDatePickerSelectionChange' },
      'tbar': {
        'items': {
          'prevMonth': { 'onAction': 'up.onPrevMonthClick' },
          'nextMonth': { 'onAction': 'up.onNextMonthClick' },
          'fields': {
            'items': {
              'monthField': { 'disabled': true },
              'yearButton': { 'disabled': true }
            }
          }
        }
      },
      'yearPicker': { 'internalListeners': { 'select': 'up.onYearPickedMulti' } }
    },
    'includeYear': a => !a.getMonth(),
    'items': {
      'prevButton': {
        'type': 'button',
        'positionable': 'before',
        'cls': 'b-multidatepicker-nav-button b-multidatepicker-prev-button b-icon b-icon-previous',
        'onAction': 'up.onPrevMonthClick',
        'hideMode': 'opacity'
      },
      'nextButton': {
        'type': 'button',
        'positionable': 'after',
        'cls': 'b-multidatepicker-nav-button b-multidatepicker-next-button b-icon b-icon-next',
        'onAction': 'up.onNextMonthClick',
        'hideMode': 'opacity'
      }
    },
    'multiSelect': null,
    'navButtons': true,
    'selection': { '$config': 'day[]' }
  };
  static type = 'multidatepicker';
  static $name = 'MultiDatePicker';
  get datePickers() {
    return this.items.filter(a => a.isDatePicker);
  }
  compose() {
    let a;
    const {navButtons: b} = this, c = b === true ? materialRe.test(((a = DomHelper.getThemeInfo()) == null ? void 0 : a.name) || '') ? 'floating' : 'inline' : b;
    return {
      'class': {
        'b-multidatepicker-nav-buttons': b,
        ['b-multidatepicker-nav-' + c]: b
      }
    };
  }
  onInternalPaint(a) {
    if (a.firstPaint) {
      const b = this, {date: c} = b;
      c && b.unanimated(() => {
        b.scrollPos = b.posFromIndex(b.indexFromDate(c));
      });
    }
    super.onInternalPaint(a);
  }
  applyDate(a, b, c) {
    const e = this, f = b == null ? void 0 : b.containsFocus, {_isUserAction: g} = e;
    let {selection: i} = e, j = f ? c : b;
    if (!i || f && a >= i[1]) {
      j = c;
      i = [
        a,
        a
      ];
    } else if (f) {
      i = [
        a,
        i[1] || a
      ];
    } else if (a < i[0]) {
      j = c;
      i = [
        a,
        i[1] || a
      ];
    } else {
      i = [
        i[0] || a,
        a
      ];
    }
    j == null ? void 0 : j.focus();
    e._isUserAction = true;
    e.selection = i;
    e._isUserAction = g;
  }
  changeIncludeYear(a) {
    if (typeof a === 'number') {
      a = [a];
    }
    return isArray3(a) ? b => a.includes(b.getMonth() + 1) : a;
  }
  configureDatePicker(a, b, c) {
    const {includeYear: e} = b, f = DateHelper.add(b.baseDate, a, 'month');
    let g = {
      'date': f,
      'activeDate': null,
      'includeYear': e(f),
      'multiSelect': b.multiSelect,
      'selection': b.selection,
      'weekStartDay': b.weekStartDay
    };
    if (!c) {
      g = DatePicker.mergeConfigs(b.datePickerDefaults, g, {
        'cellRenderer': b.cellRenderer,
        'headerRenderer': b.headerRenderer,
        'weekRenderer': b.weekRenderer
      });
    }
    return g;
  }
  ensurePlan(a) {
    if (DateHelper.isDate(a)) {
      a = { 'date': a };
    }
    if (a.date) {
      a = {
        ...a,
        'index': this.indexFromDate(a.date)
      };
      delete a.date;
    }
    return a;
  }
  getCell(a) {
    if (!(typeof a === 'string')) {
      a = DateHelper.makeKey(a);
    }
    return this.element.querySelector('[data-date="' + a + '"]:not(.b-other-month)');
  }
  indexFromDate(a) {
    return a ? getMonthNumber(a) - getMonthNumber(this.baseDate) : a;
  }
  onDatePickerSelectionChange({
    selection: a,
    userAction: b
  }) {
    if (b) {
      const c = this, {_isUserAction: e} = c;
      c._isUserAction = true;
      c.syncSelection('picker', a);
      c._isUserAction = e;
    }
  }
  onNextMonthClick() {
    this.forward();
  }
  onPrevMonthClick() {
    this.backward();
  }
  onYearPickedMulti({
    source: a,
    value: b
  }) {
    const c = this, e = a.up('datepicker'), {focusableElement: f} = e;
    f == null ? void 0 : f.focus({ 'preventScroll': true });
    a.hide();
    b -= e.activeDate.getFullYear();
    c.go(b * 12);
  }
  updateAtMax(a, b) {
    const {
      nextButton: c,
      prevButton: e
    } = this.widgetMap;
    super.updateAtMax(a, b);
    if (c) {
      c.containsFocus && (e == null ? void 0 : e.focus());
      c.isTabbable = !a;
      c.hidden = a;
    }
  }
  updateAtMin(a, b) {
    const {
      nextButton: c,
      prevButton: e
    } = this.widgetMap;
    super.updateAtMin(a, b);
    if (e) {
      e.containsFocus && (c == null ? void 0 : c.focus());
      e.isTabbable = !a;
      e.hidden = a;
    }
  }
  changeDate(a) {
    return a && DateHelper.clamp(a, this.minDate, this.maxDate);
  }
  updateDate(a) {
    if (a != null) {
      this.currentIndex = this.indexFromDate(a);
    }
  }
  updateMaxDate() {
    this.syncDateRange();
  }
  updateMinDate() {
    this.syncDateRange();
  }
  syncDateRange() {
    this.range = [
      this.minDate,
      this.maxDate
    ];
  }
  updateNavButtons(a) {
    this.detachListeners('navButtons');
    a === true && GlobalEvents_default.ion({
      'theme': () => this.recompose(),
      'name': 'navButtons',
      'thisObj': this
    });
  }
  changeRange(a, b) {
    if (isArray3(a)) {
      let [c, e] = a, f;
      if (DateHelper.isDate(c)) {
        c = this.indexFromDate(c);
        f = true;
      }
      if (DateHelper.isDate(e)) {
        e = this.indexFromDate(e);
        f = true;
      }
      if (f) {
        a = [
          c,
          e
        ];
      }
    }
    return super.changeRange(a, b);
  }
  changeSelection(a, b) {
    if (!a) {
      a = new DateSet3();
    } else if (a.isDateSet) {
      if (b === a || (b == null ? void 0 : b.equals(a))) {
        return b;
      }
    }
    return a;
  }
  updateSelection(a) {
    const b = this;
    b.syncSelection('selection', a);
    if (!b.isConfiguring) {
      b.trigger('selectionChange', {
        'selection': a,
        'userAction': b._isUserAction
      });
    } else if (a) {
      b.date = a[0];
    }
  }
  syncSelection(a, b = this.selection) {
    const {datePickers: c} = this;
    if (a !== 'selection') {
      this.selection = b;
    }
    if (a !== 'picker') {
      c.forEach(e => e.selection = b);
    }
  }
};
MultiDatePicker.initClass();
MultiDatePicker._$name = 'MultiDatePicker';
const DateRangePicker = class extends MultiDatePicker.mixin(AbstractDateRange_default) {
  static configurable = {
    'confirmable': null,
    'datePickerDefaults': {
      'focusable': false,
      'internalListeners': { 'beforeDateSelect': 'up.onDatePickerBeforeDateSelect' }
    },
    'keyMap': { 'delegate': 'confirmable' },
    'multiSelect': 'range',
    'selection': [
      'today',
      'today'
    ],
    'stripDefaults': { 'bbar': { 'type': 'confirmationbar' } },
    'tbar': {
      'items': {
        'spacer': {
          'type': 'widget',
          'flex': 1,
          'weight': 0
        }
      }
    }
  };
  static type = 'daterangepicker';
  static $name = 'DateRangePicker';
  get dateFieldContainer() {
    return this.tbar;
  }
  changeConfirmable(a) {
    this.bbar = a;
    return this.bbar;
  }
  updateConfirmable(a) {
    this.detachListeners('confirmable');
    a == null ? void 0 : a.ion({
      'name': 'confirmable',
      'thisObj': this,
      'choice': 'doConfirm'
    });
  }
  get isValid() {
    return this.checkValid();
  }
  configureDatePicker(a, b, c) {
    const e = super.configureDatePicker(a, b, c);
    e.selection = this.selection;
    return e;
  }
  onDatePickerBeforeDateSelect({date: a}) {
    this.applyDate(a, this.fieldStartDate, this.fieldEndDate);
    return false;
  }
  doConfirm({
    choice: a,
    event: b,
    userAction: c
  }) {
    this.trigger('confirm', {
      'choice': a,
      'event': b,
      'userAction': c
    });
  }
  internalOnKeyDown(a) {
    const {confirmable: b} = this;
    let c = b == null ? void 0 : b.hotKeys[a.virtualKey];
    if (c) {
      if (c === 'default') {
        c = b.defaultButton;
      }
      c && this.doConfirm({
        'event': a,
        'choice': {
          [c]: true,
          'name': c
        }
      });
    } else {
      super.internalOnKeyDown(a);
    }
  }
  syncSelection(a, b = this.selection) {
    super.syncSelection(a, b);
    if (a !== 'value') {
      this.syncValue('selection', b);
    }
  }
  syncValue(a, b = this.value) {
    const c = this, e = (b == null ? void 0 : b[1]) < (b == null ? void 0 : b[0]);
    let f;
    if (a === 'fieldEndDate') {
      f = 2;
      if (e) {
        b = [
          b[1],
          b[1]
        ];
      }
    } else if (a === 'fieldStartDate') {
      f = 1;
      if (e) {
        b = [
          b[0],
          b[0]
        ];
      }
    }
    if (f && b && !c.isConstructing) {
      c.ensureVisible(b[f - 1]);
    }
    if (a !== 'selection') {
      c.syncSelection('value', b);
    }
    super.syncValue(a, b);
  }
  triggerChange(a, b = Boolean(this._isUserAction)) {
    const {
      value: c,
      _lastValue: e,
      isValid: f
    } = this;
    this.triggerFieldChange({
      'value': c,
      'oldValue': e,
      'event': a,
      'userAction': b,
      'valid': f
    });
  }
};
DateRangePicker.initClass();
DateRangePicker._$name = 'DateRangePicker';
const DateRangeField = class extends PickerField.mixin(AbstractDateRange_default) {
  static configurable = {
    'inline': true,
    'confirmable': null,
    'container': {},
    'confirmableDefaults': {
      'type': 'daterangepicker',
      'navButtons': 'floating',
      'trapFocus': true,
      'align': {
        'align': 't100-t100',
        'offset': '[data-ref="fieldEndDate"]',
        'overlap': true
      },
      'fieldStartDate': {
        'triggers': {
          'expand': {
            'align': 'start',
            'disabled': true
          }
        }
      }
    },
    'fieldEndDate': {
      'internalListeners': { 'triggerKey': 'up.onTriggerKey' },
      'triggers': { 'expand': null }
    },
    'fieldEndTime': null,
    'fieldEndTimeDefaults': {
      'ref': 'fieldEndTime',
      'cls': 'b-end-time',
      'weight': 20
    },
    'fieldStartDate': {
      'internalListeners': { 'triggerKey': 'up.onTriggerKey' },
      'triggers': {
        'expand': {
          'handler': 'up.onExpandPicker',
          'align': 'start'
        }
      }
    },
    'fieldStartTime': null,
    'fieldStartTimeDefaults': {
      'ref': 'fieldStartTime',
      'cls': 'b-start-time',
      'weight': 10
    },
    'keepTime': null,
    'keyMap': {
      'ArrowDown': 'onTriggerKeyDown',
      'ArrowUp': 'onKeyArrowUp',
      'Enter': 'onKeyEnter',
      'Escape': 'onKeyEscape'
    },
    'max': {
      '$config': {
        'type': 'date',
        'formats': [
          'format',
          'altFormats'
        ],
        'strict': true
      }
    },
    'min': {
      '$config': {
        'type': 'date',
        'formats': [
          'format',
          'altFormats'
        ],
        'strict': true
      }
    },
    'nonConfirmableDefaults': {
      'type': 'multidatepicker',
      'align': { 'align': 't100-r100' },
      'datePickerDefaults': {
        'focusable': false,
        'internalListeners': { 'beforeDateSelect': 'up.onDatePickerBeforeDateSelect' }
      }
    },
    'picker': {
      'cls': 'b-daterangefield-picker',
      'floating': true,
      'hideMode': 'clip',
      'multiSelect': 'range',
      'scrollAction': 'realign',
      'align': { 'axisLock': true }
    },
    'pickerAlignElement': 'container.element',
    'pickerCls': null,
    'pickTime': null,
    'stepSmall': '7 days',
    'timeFormat': 'HH:mm:ss:SSS',
    'triggers': { 'expand': null },
    'weekStartDay': null,
    'timeFieldDefaults': {
      'type': 'timefield',
      'autoSelect': true,
      'picker': { 'align': { 'align': 'b0-t0' } },
      'triggers': { 'expand': null }
    }
  };
  static alias = 'daterange';
  static type = 'daterangefield';
  static $name = 'DateRangeField';
  get dateFieldContainer() {
    return this.container;
  }
  get innerElements() {
    return [];
  }
  get maxDate() {
    return this.max;
  }
  get minDate() {
    return this.min;
  }
  compose() {
    const {pickTime: a} = this;
    return { 'class': { 'b-pick-time': Boolean(a) } };
  }
  updateConfirmable() {
    if (!this.isConstructing) {
      this.picker = this._pickerConfig;
    }
  }
  changeEndTimeField(a, b) {
    return TimeField.reconfigure(b, a, {
      'owner': this,
      'defaults': TimeField.mergeConfigs(this.timeFieldDefaults, this.fieldEndTimeDefaults)
    });
  }
  updateEndTimeField(a) {
    a && this.container.add(a);
  }
  changeStartTimeField(a, b) {
    return TimeField.reconfigure(b, a, {
      'owner': this,
      'defaults': TimeField.mergeConfigs(this.timeFieldDefaults, this.fieldStartTimeDefaults)
    });
  }
  updateStartTimeField(a) {
    a && this.container.add(a);
  }
  updateKeepTime(a) {
    a = !this.pickTime && a;
    this.fieldEndDate.keepTime = a;
    this.fieldStartDate.keepTime = a;
  }
  updateMax(a) {
    const {
      fieldEndDate: b,
      fieldStartDate: c,
      _picker: e
    } = this;
    b.max = a;
    c.max = a;
    if (e) {
      e.maxDate = a;
    }
    this.syncInvalid();
  }
  updateMin(a) {
    const {
      fieldEndDate: b,
      fieldStartDate: c,
      _picker: e
    } = this;
    b.min = a;
    c.min = a;
    if (e) {
      e.minDate = a;
    }
    this.syncInvalid();
  }
  changePicker(a, b) {
    const c = this, {
        confirmable: e,
        pickerAlignElement: f
      } = c, g = e ? c.confirmableDefaults : c.nonConfirmableDefaults, i = Widget.resolveType(g.type), j = i.mergeConfigs({
        'owner': c,
        'forElement': f,
        'minDate': c.min,
        'maxDate': c.max,
        'selection': c.value,
        'weekStartDay': c._weekStartDay,
        'align': {
          'anchor': c.overlayAnchor,
          'target': f
        }
      }, g, e && { 'confirmable': e });
    c._pickerConfig = a;
    if (ObjectHelper.isObject(a)) {
      a = ObjectHelper.merge({ 'type': j.type }, a);
    }
    return i.reconfigure(b, a, {
      'owner': c,
      'defaults': j
    });
  }
  updatePicker(a, b) {
    super.updatePicker(a, b);
    this.detachListeners('dateRangeField.picker');
    a == null ? void 0 : a.ion({
      'name': 'dateRangeField.picker',
      'thisObj': this,
      'selectionChange': 'onPickerSelectionChange',
      'confirm': 'onPickerConfirm',
      'focusIn': 'onPickerFocusIn'
    });
  }
  updatePickerCls(a, b) {
    let c, e;
    const f = (e = (c = this.picker) == null ? void 0 : c.element) == null ? void 0 : e.classList;
    b && (f == null ? void 0 : f.remove(b));
    a && (f == null ? void 0 : f.add(a));
  }
  updatePickTime(a) {
    let b = null, c = null;
    if (a === true) {
      b = c = {};
    } else if (a) {
      if (a.fieldStartTime || a.fieldEndTime) {
        c = a.fieldStartTime;
        b = a.fieldEndTime;
      } else {
        c = b = a;
      }
    }
    this.fieldEndTime = b;
    this.fieldStartTime = c;
  }
  changeValue(a) {
    const b = this;
    if (a && !(DateHelper.isDate(a[0]) && DateHelper.isDate(a[1]))) {
      b.setError('L{DateField.invalidDate}');
      return;
    }
    b.clearError('L{DateField.invalidDate}');
    return a;
  }
  get weekStartDay() {
    return typeof this._weekStartDay === 'number' ? this._weekStartDay : DateHelper.weekStartDay;
  }
  updateWeekStartDay(a) {
    if (this._picker) {
      this._picker.weekStartDay = a;
    }
  }
  maybeHidePicker() {
    if (!this.picker.isVisible || this.confirmable) {
      return false;
    }
    this.hidePicker();
  }
  onDatePickerBeforeDateSelect({date: a}) {
    this.picker.applyDate(a, this.fieldStartDate, this.fieldEndDate);
    return false;
  }
  onDateFieldChange({source: a}) {
    const b = this, {ref: c} = a, e = c === 'fieldStartDate' ? 0 : 1, {pickTime: f} = b;
    let g, i, j, k, l;
    if (!b.isConstructing) {
      g = a.value;
      i = 1 - e;
      k = b.value;
      if (g) {
        if (f) {
          j = e ? b.fieldStartTime : b.fieldEndTime;
          g = DateHelper.combineDateAndTime(g, j.value);
        } else {
          g = DateHelper.clearTime(g);
        }
        l = [
          g,
          g
        ];
        if (k) {
          l[i] = k[i];
          if (l[1] < l[0]) {
            l[i] = DateHelper.combineDateAndTime(g, l[i]);
            if (l[1] < l[0]) {
              l[i] = g;
            }
          }
        }
        b.syncValue(c, l);
      }
    }
  }
  onKeyArrowUp(a) {
    const b = this, {picker: c} = b;
    if (c.isVisible) {
      if (b.confirmable) {
        return false;
      }
      return b.stepUp(a);
    }
    b.showPicker();
  }
  onKeyEnter() {
    this.maybeHidePicker();
  }
  onKeyEscape() {
    this.maybeHidePicker();
  }
  onExpandPicker() {
    this.confirmable ? this.showPicker() : this.togglePicker();
  }
  onPickerSelectionChange({
    selection: a,
    userAction: b
  }) {
    if (!this.confirmable) {
      const c = this, {_isUserAction: e} = c;
      c._isUserAction = b || e;
      c.syncValue('picker', a);
      c._isUserAction = e;
    }
  }
  onPickerConfirm({
    choice: a,
    source: b,
    userAction: c
  }) {
    const e = this, {_isUserAction: f} = e, g = b.containsFocus && (b.pickingStartDate ? e.fieldStartDate : e.fieldEndDate);
    if (a.ok) {
      e._isUserAction = c || f;
      e.syncValue('picker', b.value);
      e._isUserAction = f;
    }
    e.hidePicker();
    g == null ? void 0 : g.focus();
  }
  onPickerFocusIn({relatedTarget: a}) {
    const {
        fieldEndDate: b,
        fieldStartDate: c
      } = this.container.widgetMap, e = a === c ? c : b;
    !this.confirmable && e.focus();
  }
  onTriggerKeyDown(a) {
    if (this.picker.isVisible && !this.confirmable) {
      return this.stepDown(a);
    }
    return super.onTriggerKeyDown(a);
  }
  focusPicker() {
    const {picker: a} = this, {
        fieldEndDate: b,
        fieldStartDate: c
      } = this.container.widgetMap, {tbar: e} = a, {
        fieldEndDate: f,
        fieldStartDate: g
      } = (e == null ? void 0 : e.widgetMap) || {}, i = e ? b.containsFocus && f || g : b.containsFocus && b || c;
    i.focus();
    if (i !== a) {
      a.ensureVisible({
        'animate': false,
        'date': i.value
      });
      i.selectAll();
    }
  }
  showPicker(a) {
    const b = this, {_picker: c} = b;
    if (!b.readOnly) {
      if (c) {
        c.range = [
          b.min,
          b.max
        ];
        c.selection = b.value;
      }
      a = a || Boolean(b.confirmable);
      super.showPicker(a);
    }
  }
  get isValid() {
    const a = this, b = a.checkMinMax();
    a.clearError('L{Field.minimumValueViolation}', true);
    a.clearError('L{Field.maximumValueViolation}', true);
    if (b) {
      a.setError(b < 0 ? 'L{Field.minimumValueViolation}' : 'L{Field.maximumValueViolation}', true);
      return false;
    }
    return super.isValid;
  }
  setupLabel(a) {
    return super.setupLabel(ObjectHelper.assign({ 'for': this.fieldStartDate.input.id }, a));
  }
  syncInputFieldValue() {
  }
  syncValue(a, b = this.value) {
    const c = this, {_picker: e} = c, f = (b == null ? void 0 : b[1]) < (b == null ? void 0 : b[0]);
    if ((e == null ? void 0 : e.isVisible) && !c.confirmable) {
      let g;
      if (a === 'fieldEndDate') {
        g = 2;
        if (f) {
          b = [
            b[1],
            b[1]
          ];
        }
      } else if (a === 'fieldStartDate') {
        g = 1;
        if (f) {
          b = [
            b[0],
            b[0]
          ];
        }
      }
      if (g && b && !c.isConstructing) {
        e.ensureVisible(b[g - 1]);
      }
    }
    if (a !== 'picker' && e) {
      e.selection = b;
    }
    super.syncValue(a, b);
  }
};
DateRangeField.initClass();
DateRangeField._$name = 'DateRangeField';
const midnightDate = new Date(2000, 0, 1);
const DateTimeField = class extends Field {
  static configurable = {
    'timeField': { 'type': 'timefield' },
    'dateField': {
      'type': 'datefield',
      'keepTime': true,
      'step': '1 d'
    },
    'weekStartDay': null,
    'inputTemplate': () => '',
    'ariaElement': 'element'
  };
  static alias = 'datetime';
  static type = 'datetimefield';
  static $name = 'DateTimeField';
  doDestroy() {
    this.dateField.destroy();
    this.timeField.destroy();
    super.doDestroy();
  }
  get childItems() {
    return [
      this.dateField,
      this.timeField
    ];
  }
  get fieldDefaults() {
    const {
      revertOnEscape: a,
      tabIndex: b
    } = this;
    return b == null ? { 'revertOnEscape': a } : {
      'revertOnEscape': a,
      'tabIndex': b
    };
  }
  get focusElement() {
    return this.dateField.input;
  }
  get innerElements() {
    return [
      this.dateField.element,
      this.timeField.element
    ];
  }
  internalOnKeyEvent() {
  }
  updateRevertOnEscape(a) {
    this.timeField.revertOnEscape = a;
    this.dateField.revertOnEscape = a;
  }
  changeTimeField(a, b) {
    return TimeField.reconfigure(b, a, {
      'owner': this,
      'defaults': this.fieldDefaults
    });
  }
  updateTimeField(a) {
    const b = this;
    EventHelper.on({
      'element': a.element,
      'keydown': 'onTimeFieldKeyDown',
      'thisObj': b
    });
    FunctionHelper.after(a, 'syncInvalid', () => {
      b.timeField && !b.updatingInvalid && b.syncInvalid();
    });
    a.ion({
      'thisObj': b,
      'change'({
        userAction: c,
        value: e
      }) {
        if (c && !b.$settingValue) {
          const f = b.dateField.value;
          b._isUserAction = true;
          b.value = f ? DateHelper.copyTimeValues(new Date(f), e || midnightDate) : null;
          b._isUserAction = false;
        }
      }
    });
    if (b.readOnly) {
      a.readOnly = true;
    }
  }
  changeDateField(a, b) {
    return DateField.reconfigure(b, a, {
      'owner': this,
      'defaults': this.fieldDefaults
    });
  }
  updateDateField(a) {
    const b = this;
    EventHelper.on({
      'element': a.element,
      'keydown': 'onDateFieldKeyDown',
      'thisObj': b
    });
    FunctionHelper.after(a, 'syncInvalid', () => {
      b.dateField && !b.updatingInvalid && b.syncInvalid();
    });
    a.ion({
      'thisObj': b,
      'change'({
        userAction: c,
        value: e
      }) {
        if (c && !b.$isInternalChange) {
          b._isUserAction = true;
          b.timeField.value = e;
          b.value = e;
          b._isUserAction = false;
        }
      },
      'keydown'({event: c}) {
        let e;
        if (c.key === 'Tab' && !c.shiftKey && ((e = b.timeField) == null ? void 0 : e.isVisible)) {
          c.stopPropagation();
        }
      }
    });
    if (b.readOnly) {
      a.readOnly = true;
    }
  }
  updateWeekStartDay(a) {
    if (this.dateField) {
      this.dateField.weekStartDay = a;
    }
  }
  changeWeekStartDay(a) {
    let b, c;
    return typeof a === 'number' ? a : (c = (b = this.dateField) == null ? void 0 : b.weekStartDay) != null ? c : DateHelper.weekStartDay;
  }
  syncInputFieldValue(a = this.isConfiguring) {
    super.syncInputFieldValue(true);
    const b = this, {
        dateField: c,
        timeField: e
      } = b, f = c.highlightExternalChange, g = e.highlightExternalChange;
    if (!a && !b.highlightExternalChange) {
      a = true;
    }
    b.$isInternalChange = true;
    c.highlightExternalChange = false;
    c.highlightExternalChange = f;
    if (a) {
      e.highlightExternalChange = c.highlightExternalChange = false;
    }
    e.value = c.value = b.inputValue;
    c.highlightExternalChange = f;
    e.highlightExternalChange = g;
    b.$isInternalChange = false;
    b.syncInvalid();
  }
  onTimeFieldKeyDown(a) {
    const b = this;
    if (a.key === 'Enter' || a.key === 'Tab') {
      const c = b.dateField.value;
      b._isUserAction = true;
      b.value = c ? DateHelper.copyTimeValues(new Date(c), b.timeField.value || midnightDate) : null;
      b._isUserAction = false;
    }
  }
  onDateFieldKeyDown(a) {
    const b = this;
    if (a.key === 'Tab' && !a.shiftKey) {
      a.stopPropagation();
      a.preventDefault();
      b.timeField.focus();
    } else if (a.key === 'Enter') {
      b.value = b.dateField.value;
    }
  }
  updateRequired(a) {
    this.timeField.required = this.dateField.required = a;
  }
  updateReadOnly(a, b) {
    super.updateReadOnly(a, b);
    if (!this.isConfiguring) {
      this.timeField.readOnly = this.dateField.readOnly = a;
    }
  }
  onDisabled(a) {
    this.timeField.disabled = this.dateField.disabled = a;
  }
  focus() {
    this.dateField.focus();
  }
  hasChanged(a, b) {
    let c, e;
    if ((a == null ? void 0 : a.getTime) && (b == null ? void 0 : b.getTime)) {
      return !DateHelper.isEqual(a, b);
    }
    return super.hasChanged((c = a == null ? void 0 : a.getTime) == null ? void 0 : c.call(a), (e = b == null ? void 0 : b.getTime) == null ? void 0 : e.call(b));
  }
  get isValid() {
    return this.timeField.isValid && this.dateField.isValid;
  }
  setError(a, b) {
    [
      this.dateField,
      this.timeField
    ].forEach(c => c.setError(a, b));
  }
  getErrors() {
    const a = [
      ...this.dateField.getErrors() || [],
      ...this.timeField.getErrors() || []
    ];
    return a.length ? a : null;
  }
  clearError(a, b) {
    [
      this.dateField,
      this.timeField
    ].forEach(c => c.clearError(a, b));
  }
  updateInvalid() {
    this.updatingInvalid = true;
    this.dateField.updateInvalid();
    this.timeField.updateInvalid();
    this.updatingInvalid = false;
  }
};
DateTimeField.initClass();
DateTimeField._$name = 'DateTimeField';
const fileStart = {
  'lineNumber': 0,
  'column': 0
};
const fileEnd = {
  'lineNumber': Number.MAX_SAFE_INTEGER,
  'column': Number.MAX_SAFE_INTEGER
};
const isBryntumCom = BrowserHelper.isBryntumOnline(['online']);
const {pathname} = globalThis.location;
const isUmd = pathname.endsWith('umd.html');
const moduleTag = document.querySelector('script[type=module]');
const isModule = pathname.endsWith('module.html') || (moduleTag == null ? void 0 : moduleTag.src.includes('app.module.js')) || pathname.endsWith('index.html') && isBryntumCom;
const _DemoCodeEditor = class _DemoCodeEditor extends CodeEditor {
  static loadMonacoEditor = async a => {
    if (!CodeEditor.monacoLoadPromise) {
      const b = DomHelper.createElement({
        'tag': 'script',
        'parent': document.head,
        'src': a + 'monaco-editor/min/vs/loader.js'
      });
      await new Promise(c => b.addEventListener('load', c));
      await (CodeEditor.monacoLoadPromise = new Promise(c => {
        const e = globalThis.require;
        e.config({ 'paths': { 'vs': a + 'monaco-editor/min/vs' } });
        e(['vs/editor/editor.main'], function () {
          c(globalThis.monaco);
        });
      }));
    }
  };
  static configurable = {
    'mode': 'vanilla',
    'appFolder': '',
    'collapsible': {
      'type': 'overlay',
      'direction': 'right',
      'autoClose': false
    },
    'collapsed': true,
    'preferredSources': null,
    'monitorResize': false,
    'editor': {
      'lineNumbers': 'off',
      'foldingImportsByDefault': true,
      'minimap': { 'enabled': false },
      'ariaLabel': 'Live code editor'
    },
    'tbar': {
      'overflow': null,
      'items': {
        'filesCombo': {
          'type': 'combo',
          'flex': '1 1 100%',
          'monitorResize': false,
          'editable': false,
          'fields': [{ 'name': 'text' }],
          'listItemTpl': ({text: a}) => {
            const b = a.split('.').pop();
            let c = 'b-fa-file';
            switch (b) {
            case 'js':
            case 'jsx':
            case 'ts':
            case 'tsx':
            case 'vue':
              c = 'b-fa-file-lines';
              break;
            case 'css':
            case 'scss':
              c = 'b-fa-palette';
              break;
            case 'htm':
            case 'html':
            case 'json':
              c = 'b-fa-file-code';
              break;
            }
            const e = a.lastIndexOf('/') + 1, f = a.substring(0, e), g = a.substring(e);
            return '<span class="b-editor-file-type b-fw-icon ' + c + '" ></span> <span class="b-editor-folder">' + f + '</span>' + g;
          },
          'picker': { 'maxHeight': 'calc(100vh * 3 / 4)' }
        }
      }
    },
    'codeCache': {}
  };
  static type = 'democodeeditor';
  static $name = 'DemoCodeEditor';
  construct(a = {}) {
    super.construct(...arguments);
    const b = this, {rtl: c} = b;
    if (c) {
      b.collapsible.direction = 'left';
    }
    new ResizeHelper({
      'targetSelector': '.b-codeeditor',
      'rightHandle': Boolean(c),
      'leftHandle': !c,
      'skipTranslate': true,
      'minWidth': 190
    });
  }
  get isVanilla() {
    return this.mode === 'vanilla';
  }
  get isFramework() {
    return this.mode === 'framework';
  }
  updateMode() {
    this.title = '<span class="title-container"><span class="title">' + (this.isVanilla ? 'L{CodeEditor.editor}' : 'L{CodeEditor.viewer}') + '</span></span>';
  }
  onCloseClick() {
    this.collapse();
  }
  async onFilesComboChange({value: a}) {
    await this.loadCode(a);
  }
  async collapseBoilerplate(a) {
    const {
        monacoInstance: b,
        editor: c
      } = this, {Selection: e} = b, f = [], g = a.findMatches('^import .*$', true, true);
    let i, j = true;
    [
      'baselines',
      'children',
      'segments',
      'intervals',
      'events',
      'tasks',
      'resources',
      'assignments',
      'dependencies',
      'timeRanges',
      'resourceTimeRanges',
      'rows'
    ].forEach(n => {
      const o = n + '\\s*(?:=|:)\\s*\\[';
      for (i = a.findForwards(o, fileStart, true); i && j; i = a.findForwards(o, j.range.getEndPosition(), true)) {
        j = a.findForwards('\\](?:;|,)?\\s*$', i.range.getEndPosition(), true);
        if (j) {
          while (j && f.some(p => p.containsRange(j.range))) {
            j = a.findForwards('\\](?:;|,)?\\s*$', j.range.getEndPosition(), true);
          }
          if (j) {
            f.push(new e(i.range.startLineNumber, 1, j.range.endLineNumber + 1, 1));
          }
        }
      }
    });
    let k, l = fileStart;
    while (k = a.findForwards('^\\s*//\\s*hide', l, true)) {
      const n = a.findForwards('^\\s*//\\s*end(?:-|(?:\\s*))?hide', {
        'lineNumber': k.range.endLineNumber,
        'column': 0
      }, true);
      if (n) {
        f.push(new e(k.range.startLineNumber, 1, n.range.endLineNumber + 1, 1));
      }
      l = (n == null ? void 0 : n.range.getEndPosition()) || fileEnd;
    }
    l = fileStart;
    while (k = a.findForwards('^\\s*//\\s*region', l, true)) {
      const o = a.findForwards('^\\s*//\\s*end(?:-|(?:\\s*))?region', {
        'lineNumber': k.range.endLineNumber,
        'column': 0
      }, true);
      if (o) {
        f.push(new e(k.range.startLineNumber, 1, o.range.endLineNumber + 1, 1));
      }
      l = (o == null ? void 0 : o.range.getEndPosition()) || fileEnd;
    }
    if (g.length) {
      f.push(new e(1, 1, g[g.length - 1].range.startLineNumber + 1, 1));
    }
    for (const p of f) {
      c.setSelections([
        ...c.getSelections(),
        p
      ]);
      await c.getAction('editor.createFoldingRangeFromSelection').run();
    }
  }
  set status(a) {
    this.widgetMap.status.html = a;
  }
  get isReadOnly() {
    return this.isFramework || this.fileExt === 'html' || this.fileExt === 'js' && (this.hasImports || isUmd);
  }
  toggleReadOnly() {
    const a = this, {
        contentElement: b,
        isReadOnly: c
      } = a;
    b.classList.toggle('readonly', c);
    if (c) {
      a.status = 'Read only' + (BrowserHelper.isCSP ? ' (Restricted by Content Security Policy)' : !BrowserHelper.isChrome && !BrowserHelper.isFirefox ? ' (try it on Chrome or Firefox)' : '');
    } else {
      a.status = 'Idle';
    }
    a.editor.updateOptions({ 'readOnly': c });
  }
  async loadCode(a) {
    a = '' + this.appFolder + a;
    const b = this, {isVanilla: c} = b;
    let f = b.codeCache[a], g = null;
    b.filename = a;
    if (!f) {
      try {
        const k = await AjaxHelper.get(location.href.replace(/[^/]*$/, '') + a);
        f = b.codeCache[a] = await k.text();
      } catch (l) {
        f = '';
        g = l;
      }
    }
    b.loadedCode = f;
    b.fileExt = a.split('.').pop();
    let i = 'plaintext';
    switch (b.fileExt) {
    case 'js':
    case 'jsx':
      i = 'javascript';
      break;
    case 'ts':
    case 'tsx':
    case 'vue':
      i = 'typescript';
      break;
    case 'css':
      i = 'css';
      break;
    case 'scss':
      i = 'scss';
      break;
    case 'html':
      i = 'html';
      break;
    case 'json':
      i = 'json';
      break;
    }
    b.language = i;
    const j = b.model = await b.loadText(f, i);
    j.findForwards = function (n, o) {
      const p = j.findNextMatch(...arguments);
      if (p && !p.range.getStartPosition().isBefore(o)) {
        return p;
      }
    };
    if (c && b.fileExt === 'js') {
      await b.collapseBoilerplate(j);
    }
    b.status = '' + (g ? g.message : 'Idle');
    b.toggleReadOnly();
  }
  sortFileNamesWithHierarchy(a) {
    const b = f => {
      const g = {};
      f.forEach(i => {
        const j = i.split('/');
        let k = g;
        j.forEach((l, n) => {
          if (!k[l]) {
            k[l] = n === j.length - 1 ? null : {};
          }
          k = k[l];
        });
      });
      return g;
    };
    const c = f => {
      const g = {};
      const i = Object.keys(f).sort((j, k) => {
        const l = f[j] !== null, n = f[k] !== null;
        if (l && !n)
          return -1;
        if (!l && n)
          return 1;
        return j.localeCompare(k);
      });
      i.forEach(j => {
        g[j] = f[j] !== null ? c(f[j]) : f[j];
      });
      return g;
    };
    const e = (f, g = '') => {
      let i = [];
      Object.keys(f).forEach(j => {
        const k = '' + g + (g !== '' ? '/' : '') + j;
        if (f[j] === null) {
          i.push(k);
        } else {
          i = i.concat(e(f[j], k));
        }
      });
      return i;
    };
    return e(c(b(a)));
  }
  async initialLoadCode() {
    let a;
    const b = this, {
        widgetMap: c,
        preferredSources: e
      } = b, {filesCombo: f} = c, g = f.store, i = b.appFolder + 'app.config.json', j = (await AjaxHelper.get(i, { 'parseJson': true })).parsedJson, k = [];
    if (b.isVanilla) {
      k.push(isModule ? 'index.module.html' : isUmd ? 'index.umd.html' : 'index.html', isModule ? 'app.module.js' : isUmd ? 'app.umd.js' : 'app.js');
      if (!isModule && !isUmd && ((a = j.merge) == null ? void 0 : a.length)) {
        b.hasImports = true;
        k.push(...j.merge);
      }
      if (document.head.querySelector('[href*="app.css"]')) {
        k.push('resources/app.css');
      }
    } else {
      if (j.source.length) {
        k.push(...j.source);
      } else {
        console.warn('No source files loaded from ' + i);
      }
    }
    f.items = b.sortFileNamesWithHierarchy(k).map(n => ({
      'text': n,
      'value': n
    }));
    const l = (e || []).map(n => g.find(o => n.test(o.value))).find(Boolean) || g.first;
    f.value = l.value;
    await b.loadCode(l.value);
    f.onChange = 'up.onFilesComboChange';
    b.toggleReadOnly();
    f.readOnly = g.count === 1;
  }
  static get monacoCodePath() {
    const a = /(.*?\/)examples/.exec(document.location.href);
    return a ? a[1] + 'examples/_shared/browser/lib/monaco-editor' : void 0;
  }
  static async toggleCodeEditor(a, b, c) {
    const e = !a;
    if (e) {
      a = new _DemoCodeEditor({
        'mode': 'framework',
        'appendTo': document.body,
        'codePath': _DemoCodeEditor.monacoCodePath,
        'appFolder': '../',
        ...c
      });
      Widget.disableThrow = true;
      b.icon = 'b-icon-spinner';
      await a.initialLoadCode();
      b.icon = 'b-icon-code';
    }
    if (a.collapsed) {
      await a.expandPanel();
      a.focus();
    } else {
      await a.collapsePanel();
    }
    return a;
  }
};
const DemoCodeEditor = _DemoCodeEditor;
DemoCodeEditor.initClass();
DemoCodeEditor._$name = 'DemoCodeEditor';
const DurationField = class extends TextField {
  static alias = 'duration';
  static type = 'durationfield';
  static $name = 'DurationField';
  static get defaultConfig() {
    return {
      'value': null,
      'unit': null,
      'defaultUnit': 'day',
      'magnitude': null,
      'useAbbreviation': false,
      'allowNegative': false,
      'decimalPrecision': null,
      'triggers': { 'spin': { 'type': 'spintrigger' } },
      'nullValue': null
    };
  }
  static get configurable() {
    return {
      'min': null,
      'max': null,
      'allowedUnits': null,
      'step': 1
    };
  }
  changeMin(a) {
    return typeof a === 'string' ? new Duration(a) : a;
  }
  changeMax(a) {
    return typeof a === 'string' ? new Duration(a) : a;
  }
  changeAllowedUnits(a) {
    if (typeof a === 'string') {
      a = a.split(',');
    }
    if (a.length > 0 && !a.includes(this.defaultUnit)) {
      this.defaultUnit = a[0];
    }
    return a;
  }
  updateAllowedUnits(a) {
    this.allowedUnitsRe = new RegExp('(' + a.join('|') + ')', 'i');
  }
  get inputValue() {
    return this.value == null ? '' : this.calcValue(true).toString(this.useAbbreviation);
  }
  set unit(a) {
    this._unit = a;
    this.value = this.calcValue();
  }
  get unit() {
    return this._unit;
  }
  get unitWithDefault() {
    return this._unit || this.defaultUnit;
  }
  set magnitude(a) {
    this.clearError('L{invalidUnit}');
    this._magnitude = a;
    super.value = this.calcValue();
  }
  get magnitude() {
    return this._magnitude;
  }
  roundMagnitude(a) {
    return a && this.decimalPrecision != null ? ObjectHelper.round(a, this.decimalPrecision) : a;
  }
  get allowDecimals() {
    return this.decimalPrecision !== 0;
  }
  get isValid() {
    const a = this, b = a.input.value, c = DateHelper.parseDuration(b, a.allowDecimals, b.match(/[\p{Letter}\p{Mark}]+/gui) ? void 0 : a.unitWithDefault), e = !b || c && c.magnitude == null;
    return a.skipValidation || super.isValid && (e && !a.required || !e && (a.allowNegative || (c == null ? void 0 : c.magnitude) >= 0));
  }
  internalOnChange(a) {
    const b = this, c = b.value, e = b._lastValue;
    if (b.hasChanged(e, c)) {
      b._lastValue = c;
      b.triggerFieldChange({
        'value': c,
        'event': a,
        'userAction': true,
        'valid': b.isValid
      });
    }
  }
  onFocusOut(a) {
    let b, c, f;
    this.syncInputFieldValue(true);
    (f = (c = (b = this.triggers) == null ? void 0 : b.spin) == null ? void 0 : c.clickRepeater) == null ? void 0 : f.cancel();
    return super.onFocusOut(a);
  }
  set value(a) {
    const b = this;
    let c, e;
    b.getConfig('allowedUnits');
    b.clearError('L{invalidUnit}');
    if (typeof a === 'number' || typeof a === 'string' && a.length > 0 && !isNaN(a)) {
      c = Number(a);
      e = b.unitWithDefault;
    } else if (typeof a === 'string') {
      if (/^\s*$/.test(a)) {
        c = null;
      } else {
        const f = DateHelper.parseDuration(a, b.allowDecimals, b.unitWithDefault);
        if (f) {
          if (!b.allowedUnitsRe || b.allowedUnitsRe.test(f.unit)) {
            e = f.unit;
            c = f.magnitude;
          } else {
            b.setError('L{invalidUnit}');
          }
        }
      }
    } else {
      if (a && 'unit' in a && 'magnitude' in a) {
        e = a.unit;
        c = a.magnitude;
      } else {
        e = null;
        c = null;
      }
    }
    if (b._magnitude !== c || b._unit != e) {
      b._magnitude = c;
      if (e) {
        b._unit = e;
      }
      super.value = b.calcValue();
    }
  }
  okMax(a) {
    if (typeof a === 'number') {
      a = new Duration({
        'unit': this.unitWithDefault,
        'magnitude': a
      });
    }
    return this.max == null || a <= this.max;
  }
  okMin(a) {
    if (typeof a === 'number') {
      a = new Duration({
        'unit': this.unitWithDefault,
        'magnitude': a
      });
    }
    return this.min == null || a >= this.min;
  }
  get validity() {
    const a = this.value, b = {};
    if (a != null) {
      b.rangeUnderflow = !this.okMin(a);
      b.rangeOverflow = !this.okMax(a);
    }
    b.valid = !b.rangeUnderflow && !b.rangeOverflow;
    return b;
  }
  get value() {
    return super.value;
  }
  calcValue(a = false) {
    const b = this;
    if ((!b._unit || b._magnitude == null) && b.clearable) {
      return null;
    } else {
      return new Duration(a ? this.roundMagnitude(b._magnitude) : this._magnitude, b.unitWithDefault);
    }
  }
  hasChanged(a, b) {
    return b && !a || !b && a || b && a && !a.isEqual(b);
  }
  get milliseconds() {
    return this.value ? this.value.milliseconds : 0;
  }
  onInternalKeyDown(a) {
    if (a.key === 'ArrowUp') {
      this.doSpinUp();
    } else if (a.key === 'ArrowDown') {
      this.doSpinDown();
    }
  }
  doSpinUp() {
    const a = this;
    let {value: b} = a;
    if (a.readOnly) {
      return;
    }
    if (!b) {
      b = new Duration({
        'magnitude': 0,
        'unit': this.defaultUnit
      });
    }
    let c = b.add(this._step);
    if (!a.okMin(c)) {
      c = a.min;
    }
    if (a.okMax(c)) {
      a.value = c;
    }
  }
  doSpinDown() {
    const a = this;
    let {value: b} = a;
    if (a.readOnly) {
      return;
    }
    if (!b) {
      b = new Duration({
        'magnitude': 0,
        'unit': this.defaultUnit
      });
    }
    let c = b.add({
      'magnitude': -this._step.magnitude,
      'unit': this._step.unit
    });
    if (!a.okMax(c)) {
      c = a.max;
    }
    if (a.okMin(c) && (a.allowNegative || (a.magnitude || 0) > 0)) {
      a.value = c;
    }
  }
  changeStep(a, b) {
    const c = typeof a;
    if (!a) {
      return null;
    }
    if (c === 'number') {
      a = {
        'magnitude': Math.abs(a),
        'unit': void 0
      };
    } else if (c === 'string') {
      a = DateHelper.parseDuration(a);
    }
    if (a && a.unit && a.magnitude) {
      if (a.magnitude < 0) {
        a = {
          'magnitude': -a.magnitude,
          'unit': a.unit
        };
      }
    }
    return a;
  }
  updateStep(a) {
    this.element.classList.toggle('b-no-steppers', Boolean(a));
    this.syncInvalid();
  }
};
DurationField.initClass();
DurationField._$name = 'DurationField';
const Fit = class extends Layout {
  static configurable = {
    'containerCls': 'b-fit-container',
    'itemCls': 'b-fit-item'
  };
  static type = 'fit';
  static $name = 'Fit';
};
Fit.initClass();
Fit._$name = 'Fit';
const Editor = class extends Container {
  static configurable = {
    'positioned': true,
    'hidden': true,
    'layout': 'fit',
    'align': {
      'align': 't0-t0',
      'offset': [
        0,
        0
      ]
    },
    'hideTarget': false,
    'appendToTargetParent': false,
    'matchSize': true,
    'matchFont': true,
    'fitTargetContent': false,
    'inputField': 'textfield',
    'blurAction': 'complete',
    'completeKey': 'Enter',
    'cancelKey': 'Escape',
    'invalidAction': 'block',
    'completeOnChange': null,
    'isolateFields': true,
    'instantUpdate': false
  };
  static type = 'editor';
  static $name = 'Editor';
  afterConfigure() {
    const a = this;
    super.afterConfigure();
    a.onTargetSizeChange = a.onTargetSizeChange.bind(a);
    EventHelper.on({
      'element': a.element,
      'keydown': 'onEditorKeyDown',
      'contextmenu': 'stopMouseEvents',
      'mousedown': 'stopMouseEvents',
      'mouseover': 'stopMouseEvents',
      'mouseout': 'stopMouseEvents',
      'mouseup': 'stopMouseEvents',
      'click': 'stopMouseEvents',
      'dblclick': 'stopMouseEvents',
      'thisObj': a
    });
    a.ion({
      'beforeHide': 'beforeEditorHide',
      'hide': 'afterEditorHide'
    });
    LocaleManager_default.ion({
      'locale': 'onLocaleChange',
      'thisObj': a
    });
  }
  onLocaleChange() {
    const {inputField: a} = this;
    if (a && !a.isDestroyed) {
      if (a.syncInputFieldValue) {
        a.syncInputFieldValue(true);
      } else if (!(a instanceof Field)) {
      }
    }
  }
  render(a) {
    const b = this.element.parentNode, [c] = this.getRenderContext(this, a);
    c.classList.add('b-editing');
    super.render(...arguments);
    if ((b == null ? void 0 : b.classList) && c !== b) {
      b.classList.remove('b-editing');
    }
  }
  async startEdit(a) {
    let b, c, e;
    const f = this;
    a = ObjectHelper.assignIf(a, {
      'align': f.align,
      'hideTarget': f.hideTarget,
      'matchSize': f.matchSize,
      'matchFont': f.matchFont,
      'fitTargetContent': f.fitTargetContent,
      'focus': true
    });
    if (f.isVisible) {
      await f.completeEdit();
    }
    if (f.trigger('beforeStart', a) !== false) {
      const {
          target: g,
          hideTarget: i,
          matchSize: j,
          matchFont: k,
          fitTargetContent: l,
          record: n,
          field: o = f.inputField.name,
          focus: p
        } = a, {
          inputField: q,
          appendToTargetParent: r
        } = f, {input: u} = q, v = DomHelper.getStyleValue(g, 'font-size'), w = DomHelper.getStyleValue(g, 'font-family');
      let {
        value: z,
        align: A
      } = a;
      if (n && o) {
        f.record = n;
        f.dataField = o;
        if (q.assignFieldValue !== Widget.prototype.assignFieldValue) {
          z = q.value;
        } else if (z === void 0) {
          z = n.getValue(o);
        }
      }
      if (r && g.nodeType === Node.ELEMENT_NODE) {
        g.parentElement.appendChild(f.element);
      }
      if (j) {
        if (g instanceof HTMLElement) {
          f.updateSize(g, j);
        }
        if (g instanceof HTMLElement) {
          ResizeMonitor.addResizeListener(g, f.onTargetSizeChange);
        }
      }
      if (u) {
        if (k) {
          u.style.fontSize = v;
          u.style.fontFamily = w;
        } else {
          u.style.fontSize = u.style.fontFamily = '';
        }
      }
      f.assigningValues = true;
      (b = q.clearError) == null ? void 0 : b.call(q);
      if ('setValue' in q) {
        await q.setValue(z);
      } else {
        q.value = z;
      }
      f.assigningValues = false;
      if (typeof A === 'string') {
        A = { 'align': A };
      }
      await f.showBy({
        'target': g,
        'allowTargetOut': true,
        ...A
      });
      if (l) {
        const C = u.scrollWidth - u.clientWidth;
        if (C > 0) {
          f.width += C + DomHelper.scrollBarWidth;
        }
      }
      p && ((c = q.focus) == null ? void 0 : c.call(q));
      if (g.nodeType === Element.ELEMENT_NODE) {
        if (i) {
          g.classList.add('b-hide-visibility');
        }
      }
      f.editing = true;
      const B = q.value;
      f.trigger('start', { 'value': B });
      if (Array.isArray(B) && q.editingRecords && ((e = B[0]) == null ? void 0 : e.isModel)) {
        f.oldValue = B.map(E => E.copy(E.id));
      } else {
        f.oldValue = B;
        if (Array.isArray(f.oldValue)) {
          f.oldValue = f.oldValue.slice();
        }
      }
      q.initialValue = f.oldValue;
      return true;
    }
    return false;
  }
  async refreshEdit() {
    if (this.isVisible) {
      const {
        record: a,
        dataField: b,
        inputField: c
      } = this;
      if (a && b) {
        const e = a.getValue(b);
        if (!ObjectHelper.isEqual(c.value, e)) {
          await c.setValue(e);
        }
      }
    }
  }
  finishEdit() {
    const {
      target: a,
      aligningToElement: b
    } = this.lastAlignSpec;
    if (b) {
      a.classList.remove('b-editing');
      a.classList.remove('b-hide-visibility');
    }
    this.editing = false;
    this.trigger('finishEdit');
  }
  onEditorKeyDown(a) {
    let b;
    const c = this;
    switch (a.key) {
    case c.completeKey:
      c.completeEdit(null, a);
      a.stopImmediatePropagation();
      break;
    case c.cancelKey:
      c.cancelEdit(a);
      a.stopImmediatePropagation();
      break;
    }
    (b = c.trigger) == null ? void 0 : b.call(c, 'keyDown', { 'event': a });
  }
  stopMouseEvents(a) {
    if (!this.allowMouseEvents) {
      a.stopPropagation();
    }
  }
  onFocusOut(a) {
    super.onFocusOut(a);
    const b = this;
    if (!b.isFinishing && b.editing) {
      switch (b.blurAction) {
      case 'cancel':
        b.cancelEdit(a);
        break;
      case 'complete':
        b.completeEdit(null, a);
      }
    }
  }
  async completeEdit(a, b) {
    let c, e, f, g;
    const i = this, {
        inputField: j,
        oldValue: k,
        record: l
      } = i, n = j.invalidAction || (j.allowInvalid === false ? 'block' : i.invalidAction), {value: o} = j;
    if (!i.isVisible) {
      return;
    }
    if (!j.isValid && n !== 'allow') {
      if (n === 'block') {
        (c = j.focus) == null ? void 0 : c.call(j);
        return false;
      } else if (n === 'revert') {
        return i.cancelEdit(b) !== false;
      }
    } else if (ObjectHelper.isEqual(o, k)) {
      return i.cancelEdit(b) !== false;
    } else {
      const p = {
        'inputField': j,
        'record': l,
        'value': o,
        'oldValue': k
      };
      if (i.trigger('beforeComplete', p) === false) {
        (e = j.focus) == null ? void 0 : e.call(j);
      } else {
        if (!a) {
          a = p.finalize || ((f = p.editorContext) == null ? void 0 : f.finalize);
        }
        if (a) {
          let q = await a(p);
          if (q === true) {
            i.onEditComplete();
          } else {
            if (j.setError) {
              const r = q || j.invalidValueError, u = () => {
                  v();
                  j.clearError(r);
                }, v = j.ion({
                  'change': u,
                  'input': u
                });
              j.setError(r);
            }
            if (n === 'block') {
              (g = j.focus) == null ? void 0 : g.call(j);
            } else if (n === 'revert') {
              await j.setValue(k);
              q = true;
            }
            q = false;
          }
          return q;
        } else {
          i.onEditComplete();
          return true;
        }
      }
    }
    return false;
  }
  cancelEdit(a) {
    let b;
    const c = this, {
        inputField: e,
        oldValue: f
      } = c, {value: g} = e;
    if (c.editing && !c.isFinishing && c.trigger('beforeCancel', {
        'value': g,
        'oldValue': f,
        'event': a
      }) !== false) {
      (b = e.clearError) == null ? void 0 : b.call(e);
      c.isFinishing = true;
      c.hide();
      c.trigger('cancel', {
        'value': g,
        'oldValue': f,
        'event': a
      });
      c.finishEdit();
      c.isFinishing = false;
    } else {
      return false;
    }
  }
  onEditComplete() {
    const a = this, {
        record: b,
        dataField: c,
        inputField: e,
        oldValue: f
      } = a, {value: g} = e;
    if (!a.isFinishing) {
      a.isFinishing = true;
      a.hide();
      if (b && (!a.instantUpdate || e._outstandingUpdate)) {
        const i = 'set' + StringHelper.capitalize(c);
        if (b[i]) {
          b[i](g);
        } else {
          b.setValue(c, g);
        }
      }
      a.trigger('complete', {
        'value': g,
        'oldValue': f
      });
      a.finishEdit();
      a.isFinishing = false;
    }
  }
  doDestroy() {
    if (this.createdInputField) {
      this.inputField.destroy();
    }
    super.doDestroy();
  }
  set owner(a) {
    this._owner = a;
  }
  get owner() {
    return this._owner || Widget.fromElement(this.element.parentNode);
  }
  changeInputField(a, b) {
    const c = this;
    if (b) {
      b.destroy();
    }
    if (typeof a === 'string') {
      a = { 'type': a };
    }
    if (!(a instanceof Widget)) {
      if (!a.type) {
        a.type = 'textfield';
      }
      a = Widget.create(a);
      c.createdInputField = true;
    }
    if (c.completeOnChange) {
      a.ion({
        'change': 'onInputFieldChange',
        'thisObj': c
      });
    }
    a.parent = c;
    return a;
  }
  updateInputField(a) {
    this.removeAll();
    this.add(a);
  }
  get inputField() {
    return this.items[0];
  }
  onInputFieldChange() {
    if (this.containsFocus) {
      this.completeEdit();
    }
  }
  onTargetSizeChange(a, b, c) {
    if (b && c) {
      this.updateSize(a);
    }
  }
  updateSize(a, b = this.lastMatchSize) {
    const c = this, e = Rectangle.inner(a);
    c.width = b.width === false ? 'auto' : e.width - c.align.offset[0];
    c.height = b.height === false ? 'auto' : e.height;
    c.lastMatchSize = b;
  }
  beforeEditorHide() {
    if (this.editing && !this.isFinishing) {
      this.cancelEdit();
      return false;
    }
  }
  afterEditorHide() {
    if (this.positioned) {
      this.element.remove();
    }
    ResizeMonitor.removeResizeListener(this.lastAlignSpec.target, this.onTargetSizeChange);
  }
};
Editor.initClass();
Editor._$name = 'Editor';
const filterableFieldDataTypes = {
  'number': true,
  'boolean': true,
  'string': true,
  'date': true,
  'duration': true,
  'time': true,
  'auto': true
};
const isInferrableType = {
  'number': true,
  'boolean': true,
  'string': true
};
const isSupportedDurationField = a => {
  let b;
  return ((b = a == null ? void 0 : a.column) == null ? void 0 : b.type) === 'duration';
};
const isFilterableField = a => filterableFieldDataTypes[a == null ? void 0 : a.type] || isSupportedDurationField(a);
const emptyString = '';
const clsBase = 'b-fieldfilterpicker';
const multiValueOperators = {
  'between': true,
  'notBetween': true,
  'isIncludedIn': true,
  'isNotIncludedIn': true
};
const valueInputTypes = {
  'textfield': true,
  'datefield': true,
  'numberfield': true,
  'durationfield': true,
  'combo': true,
  'timefield': true
};
const commonOperators = {
  'empty': {
    'value': 'empty',
    'text': 'L{isEmpty}',
    'argCount': 0
  },
  'notEmpty': {
    'value': 'notEmpty',
    'text': 'L{isNotEmpty}',
    'argCount': 0
  },
  '=': {
    'value': '=',
    'text': 'L{equals}'
  },
  '!=': {
    'value': '!=',
    'text': 'L{doesNotEqual}'
  },
  '>': {
    'value': '>',
    'text': 'L{isGreaterThan}'
  },
  '<': {
    'value': '<',
    'text': 'L{isLessThan}'
  },
  '>=': {
    'value': '>=',
    'text': 'L{isGreaterThanOrEqualTo}'
  },
  '<=': {
    'value': '<=',
    'text': 'L{isLessThanOrEqualTo}'
  },
  'between': {
    'value': 'between',
    'text': 'L{isBetween}',
    'argCount': 2
  },
  'notBetween': {
    'value': 'notBetween',
    'text': 'L{isNotBetween}',
    'argCount': 2
  },
  'isIncludedIn': {
    'value': 'isIncludedIn',
    'text': 'L{isOneOf}',
    'isArrayValued': true
  },
  'isNotIncludedIn': {
    'value': 'isNotIncludedIn',
    'text': 'L{isNotOneOf}',
    'isArrayValued': true
  }
};
const _FieldFilterPicker = class _FieldFilterPicker extends Container {
  static buildOperatorArgCountLookup = a => ArrayHelper.keyBy(Object.entries(a), ([b]) => b, ([, b]) => ArrayHelper.keyBy(b, ({value: c}) => c, ({argCount: c}) => c === void 0 ? 1 : c));
  static configurable = {
    'fields': null,
    'disabled': false,
    'readOnly': false,
    'layout': 'vbox',
    'propertyLocked': false,
    'operatorLocked': false,
    'filter': null,
    'propertyFieldConfig': null,
    'operatorFieldConfig': null,
    'valueFieldCls': null,
    'triggerChangeOnInput': true,
    'items': {
      'inputs': {
        'type': 'container',
        'layout': 'hbox',
        'cls': clsBase + '-inputs',
        'items': {
          'propertyPicker': {
            'type': 'combo',
            'items': {},
            'cls': clsBase + '-property',
            'placeholder': 'L{FieldFilterPicker.selectAProperty}',
            'highlightExternalChange': false
          },
          'operatorPicker': {
            'type': 'combo',
            'items': {},
            'cls': clsBase + '-operator',
            'placeholder': 'L{FieldFilterPicker.selectAnOperator}',
            'highlightExternalChange': false
          },
          'valueFields': {
            'type': 'container',
            'cls': clsBase + '-values',
            'items': {}
          }
        }
      },
      'caseSensitive': {
        'type': 'checkbox',
        'text': 'L{FieldFilterPicker.caseSensitive}',
        'cls': clsBase + '-case-sensitive'
      }
    },
    'operators': _FieldFilterPicker.defaultOperators,
    'dateFormat': 'L{FieldFilterPicker.dateFormat}',
    'store': null,
    'valueFieldPlaceholders': null,
    'getValueFieldConfig': null
  };
  static defaultOperators = {
    'string': [
      commonOperators.empty,
      commonOperators.notEmpty,
      commonOperators['='],
      commonOperators['!='],
      {
        'value': 'includes',
        'text': 'L{contains}'
      },
      {
        'value': 'doesNotInclude',
        'text': 'L{doesNotContain}'
      },
      {
        'value': 'startsWith',
        'text': 'L{startsWith}'
      },
      {
        'value': 'endsWith',
        'text': 'L{endsWith}'
      },
      commonOperators.isIncludedIn,
      commonOperators.isNotIncludedIn
    ],
    'number': [
      commonOperators.empty,
      commonOperators.notEmpty,
      commonOperators['='],
      commonOperators['!='],
      commonOperators['>'],
      commonOperators['<'],
      commonOperators['>='],
      commonOperators['<='],
      commonOperators.between,
      commonOperators.notBetween,
      commonOperators.isIncludedIn,
      commonOperators.isNotIncludedIn
    ],
    'date': [
      commonOperators.empty,
      commonOperators.notEmpty,
      commonOperators['='],
      commonOperators['!='],
      {
        'value': '<',
        'text': 'L{isBefore}'
      },
      {
        'value': '>',
        'text': 'L{isAfter}'
      },
      commonOperators.between,
      {
        'value': 'isToday',
        'text': 'L{isToday}',
        'argCount': 0
      },
      {
        'value': 'isTomorrow',
        'text': 'L{isTomorrow}',
        'argCount': 0
      },
      {
        'value': 'isYesterday',
        'text': 'L{isYesterday}',
        'argCount': 0
      },
      {
        'value': 'isThisWeek',
        'text': 'L{isThisWeek}',
        'argCount': 0
      },
      {
        'value': 'isNextWeek',
        'text': 'L{isNextWeek}',
        'argCount': 0
      },
      {
        'value': 'isLastWeek',
        'text': 'L{isLastWeek}',
        'argCount': 0
      },
      {
        'value': 'isThisMonth',
        'text': 'L{isThisMonth}',
        'argCount': 0
      },
      {
        'value': 'isNextMonth',
        'text': 'L{isNextMonth}',
        'argCount': 0
      },
      {
        'value': 'isLastMonth',
        'text': 'L{isLastMonth}',
        'argCount': 0
      },
      {
        'value': 'isThisYear',
        'text': 'L{isThisYear}',
        'argCount': 0
      },
      {
        'value': 'isNextYear',
        'text': 'L{isNextYear}',
        'argCount': 0
      },
      {
        'value': 'isLastYear',
        'text': 'L{isLastYear}',
        'argCount': 0
      },
      {
        'value': 'isYearToDate',
        'text': 'L{isYearToDate}',
        'argCount': 0
      },
      commonOperators.isIncludedIn,
      commonOperators.isNotIncludedIn
    ],
    'boolean': [
      {
        'value': 'isTrue',
        'text': 'L{isTrue}',
        'argCount': 0
      },
      {
        'value': 'isFalse',
        'text': 'L{isFalse}',
        'argCount': 0
      }
    ],
    'duration': [
      commonOperators.empty,
      commonOperators.notEmpty,
      commonOperators['='],
      commonOperators['!='],
      commonOperators['>'],
      commonOperators['<'],
      commonOperators['>='],
      commonOperators['<='],
      commonOperators.between,
      commonOperators.notBetween,
      commonOperators.isIncludedIn,
      commonOperators.isNotIncludedIn
    ],
    'time': [
      commonOperators.empty,
      commonOperators.notEmpty,
      commonOperators['='],
      commonOperators['!='],
      commonOperators['>'],
      commonOperators['<'],
      commonOperators['>='],
      commonOperators['<='],
      commonOperators.between,
      commonOperators.notBetween,
      commonOperators.isIncludedIn,
      commonOperators.isNotIncludedIn
    ],
    'relation': [
      commonOperators.empty,
      commonOperators.notEmpty,
      commonOperators['='],
      commonOperators['!='],
      commonOperators.isIncludedIn,
      commonOperators.isNotIncludedIn
    ],
    'auto': [{
        'value': '*',
        'text': 'L{contains}'
      }]
  };
  static type = 'fieldfilterpicker';
  static $name = 'FieldFilterPicker';
  static get defaultValueFieldPlaceholders() {
    return {
      'string': 'L{enterAValue}',
      'number': 'L{enterANumber}',
      'date': 'L{selectADate}',
      'relation': 'L{selectValue}',
      'list': 'L{selectOneOrMoreValues}',
      'duration': 'L{enterAValue}',
      'time': 'L{selectATime}',
      'auto': 'L{enterAValue}'
    };
  }
  afterConstruct() {
    const a = this;
    if (!a._fields) {
      throw new Error(_FieldFilterPicker.name + ' requires \'fields\' to be configured.');
    }
    if (!a._filter) {
      throw new Error(_FieldFilterPicker.name + ' requires \'filter\' to be configured.');
    }
    super.afterConstruct();
    const {
      widgetMap: {
        propertyPicker: b,
        operatorPicker: c,
        caseSensitive: e
      }
    } = a;
    b.ion({
      'select': 'onPropertySelect',
      'thisObj': a
    });
    c.ion({
      'select': 'onOperatorSelect',
      'thisObj': a
    });
    e.ion({
      'change': 'onCaseSensitiveChange',
      'thisObj': a
    });
    a.propertyFieldConfig && b.setConfig(a.propertyFieldConfig);
    a.operatorFieldConfig && c.setConfig(a.operatorFieldConfig);
    b.cls = a.allPropertyPickerClasses;
    c.cls = a.allOperatorPickerClasses;
    a.populateUIFromFilter();
  }
  changeDateFormat(a) {
    return this.L(a);
  }
  get allChildInputs() {
    const {
      propertyPicker: a,
      operatorPicker: b,
      caseSensitive: c
    } = this.widgetMap;
    return [
      a,
      b,
      ...this.valueFields,
      c
    ];
  }
  updateDisabled(a, b) {
    super.updateDisabled(a, b);
    this.allChildInputs.forEach(c => c.disabled = a);
  }
  updateReadOnly(a) {
    const {
      propertyPicker: b,
      operatorPicker: c
    } = this.widgetMap;
    this.allChildInputs.forEach(e => e.readOnly = a);
    b.readOnly = b.readOnly || a;
    c.readOnly = c.readOnly || a;
  }
  updatePropertyLocked(a) {
    this.widgetMap.propertyPicker.readOnly = a || this.readOnly;
    this.widgetMap.propertyPicker.cls = this.allPropertyPickerClasses;
  }
  updateOperatorLocked(a) {
    this.widgetMap.operatorPicker.readOnly = a || this.readOnly;
    this.widgetMap.operatorPicker.cls = this.allOperatorPickerClasses;
  }
  changeOperators(a) {
    const b = a != null ? a : _FieldFilterPicker.defaultOperators;
    return Object.keys(b).reduce((c, e) => ({
      ...c,
      [e]: b[e].map(f => ({
        ...f,
        'text': this.L(f.text)
      }))
    }), {});
  }
  get isMultiSelectValueField() {
    let a;
    return [
      'isIncludedIn',
      'isNotIncludedIn'
    ].includes((a = this._filter) == null ? void 0 : a.operator);
  }
  get allPropertyPickerClasses() {
    let a;
    return new DomClassList(clsBase + '-property', (a = this.propertyFieldConfig) == null ? void 0 : a.cls, { [clsBase + '-combo-locked']: this.propertyLocked });
  }
  get allOperatorPickerClasses() {
    let a;
    return new DomClassList(clsBase + '-operator', (a = this.operatorFieldConfig) == null ? void 0 : a.cls, { [clsBase + '-combo-locked']: this.operatorLocked });
  }
  getValueFieldConfigs() {
    const a = this, {
        valueFieldCls: b,
        fieldType: c,
        _filter: {operator: e},
        onValueChange: f,
        filterValues: g,
        isMultiSelectValueField: i,
        operatorArgCount: j,
        getValueFieldConfig: k,
        fieldIsRelation: l,
        triggerChangeOnInput: n
      } = a, o = ObjectHelper.merge({}, _FieldFilterPicker.defaultValueFieldPlaceholders, a.valueFieldPlaceholders);
    if (!c || !e || j === 0) {
      return [];
    }
    let p = {
      'type': 'textfield',
      'internalListeners': {
        'change': f,
        'input': n ? f : null,
        'thisObj': a
      },
      'bubbleEvents': { 'keydown': true },
      'cls': b,
      'dataset': { 'type': c },
      'placeholder': a.L(o[i ? 'list' : l ? 'relation' : c]),
      'highlightExternalChange': false
    };
    if (i || l) {
      p = {
        ...p,
        'type': 'combo',
        'multiSelect': i,
        'createOnUnmatched': true,
        'items': a.getUniqueDataValues(g),
        'value': g != null ? g : []
      };
    } else if ([
        'number',
        'date',
        'boolean',
        'duration',
        'time'
      ].includes(c)) {
      p.type = c + 'field';
    }
    if (k) {
      p = a.callback(k, a, [
        a.filter,
        p
      ]);
    }
    if (i) {
      return [p];
    }
    return ArrayHelper.populate(j, q => [
      {
        'type': 'widget',
        'tag': 'div',
        'cls': clsBase + '-value-separator',
        'content': a.L('L{FieldFilterPicker.and}')
      },
      {
        ...p,
        'value': g[q]
      }
    ]).flat().slice(1);
  }
  getUniqueDataValues(a = []) {
    let b;
    const c = this, {fieldType: e} = c;
    if (!c.store || !((b = c._filter) == null ? void 0 : b.property)) {
      return [];
    }
    const {relatedDisplayField: f} = c.selectedField;
    let g, i;
    if (c.fieldIsRelation) {
      const {foreignStore: j} = c.currentPropertyRelationConfig;
      if (f) {
        g = j.allRecords.reduce((k, l) => {
          if (l.id != null) {
            k.push({
              'text': l.getValue(f),
              'value': l.id
            });
          }
          return k;
        }, []);
        i = g.sort((k, l) => c.sortStrings(k.text, l.text));
      } else {
        g = j.allRecords.map(k => k.id);
      }
    } else {
      g = c.store.allRecords.map(k => k.getValue(c._filter.property));
    }
    if (!i) {
      g.push(...a);
      const k = ArrayHelper.unique(g.reduce((l, n) => {
        if (n != null && String(n).trim() !== '') {
          if (e === 'date') {
            l.push(n.valueOf());
          } else if (e === 'duration') {
            l.push(n.toString());
          } else {
            l.push(n);
          }
        }
        return l;
      }, []));
      if (e === 'string') {
        i = k.sort(c.sortStrings);
      } else if (e === 'duration') {
        i = k.map(l => new Duration(l)).filter(l => l.isValid).sort(c.sortDurations);
      } else {
        i = k.sort(c.sortNumerics);
      }
      if (e === 'date') {
        i = i.map(l => {
          const n = new Date(l);
          return {
            'text': DateHelper.format(n, c.dateFormat),
            'value': l
          };
        });
      } else if (e === 'duration') {
        i = i.map(l => l.toString());
      }
    }
    return i;
  }
  sortStrings(c, e) {
    return (c != null ? c : emptyString).localeCompare(e != null ? e : emptyString);
  }
  sortNumerics(c, e) {
    return c - e;
  }
  sortDurations(c, e) {
    return c.valueOf() - e.valueOf();
  }
  get fieldType() {
    let a;
    return (a = this.selectedField) == null ? void 0 : a.type;
  }
  get selectedField() {
    let a, b;
    return (b = this.fields) == null ? void 0 : b[(a = this._filter) == null ? void 0 : a.property];
  }
  get propertyOptions() {
    let a;
    return Object.entries((a = this.fields) != null ? a : {}).filter(([, b]) => filterableFieldDataTypes[b.type] || isSupportedDurationField(b)).map(([b, {title: c}]) => ({
      'value': b,
      'text': c != null ? c : b
    })).sort((c, e) => c.text.localeCompare(e.text));
  }
  get operatorOptions() {
    return this.operators[this.fieldIsRelation ? 'relation' : this.fieldType];
  }
  get fieldIsRelation() {
    return Boolean(this.currentPropertyRelationConfig);
  }
  get currentPropertyRelationConfig() {
    let a, b;
    return (b = (a = this.store) == null ? void 0 : a.modelRelations) == null ? void 0 : b.find(({foreignKey: c}) => {
      let e;
      return c === ((e = this._filter) == null ? void 0 : e.property);
    });
  }
  updateOperators() {
    delete this._operatorArgCountLookup;
  }
  get operatorArgCountLookup() {
    return this._operatorArgCountLookup || (this._operatorArgCountLookup = _FieldFilterPicker.buildOperatorArgCountLookup(this.operators));
  }
  updateFilter() {
    if (this._filter) {
      this.onFilterChange();
    }
  }
  updateStore(a) {
    let b;
    (b = this._store) == null ? void 0 : b.un(this);
    a == null ? void 0 : a.ion({
      'refresh': 'onStoreRefresh',
      'thisObj': this
    });
    this.inferFieldTypes();
  }
  onStoreRefresh({action: a}) {
    if (this.isMultiSelectValueField && [
        'dataset',
        'create',
        'update',
        'delete'
      ].includes(a)) {
      this.valueFields[0].items = this.getUniqueDataValues(this.filterValues);
    }
  }
  refreshValueFields() {
    const a = this, {valueFields: b} = a.widgetMap, {
        valueFields: c,
        fieldType: e,
        operatorArgCount: f,
        _filter: {
          property: g,
          operator: i
        }
      } = a, j = multiValueOperators[i];
    b.element.className = new DomClassList({
      [clsBase + '-values']: true,
      [clsBase + '-values-multiple']: j,
      [clsBase + '-values-' + e]: e !== void 0,
      'b-hidden': g == void 0 || i == void 0 || f === 0
    });
    const k = a.getValueFieldConfigs();
    if (k.length !== c.length || k.some(({type: l}, n) => c[n].type !== l)) {
      b.removeAll();
      b.add(k);
    } else {
      let l = 0;
      for (const n of k) {
        delete n.type;
        ObjectHelper.assign(c[l++], n);
      }
    }
    delete a._valueFields;
    a.refreshCaseSensitive();
  }
  refreshCaseSensitive() {
    let a, b;
    const c = this, {
        fieldType: e,
        operatorArgCount: f,
        isMultiSelectValueField: g
      } = c, i = (a = c._filter) == null ? void 0 : a.operator, {caseSensitive: j} = c.widgetMap;
    j.hidden = e !== 'string' || !i || g || f === 0;
    j.checked = ((b = c._filter) == null ? void 0 : b.caseSensitive) !== false;
  }
  onPropertySelect(a) {
    let b;
    const c = this, {_filter: e} = c;
    e.property = ((b = a.record) == null ? void 0 : b.data.value) || null;
    if (c.fieldType !== c._fieldType) {
      e.operator = null;
      e.value = null;
    }
    c._fieldType = e.type = c.fieldType;
    c.refreshOperatorPicker();
    c.refreshValueFields();
    c.triggerChange();
  }
  onCaseSensitiveChange({checked: a}) {
    this._filter.caseSensitive = a;
    this.triggerChange();
  }
  onOperatorSelect(a) {
    let b;
    const c = this, e = c.isMultiSelectValueField;
    const f = this.operatorArgCount;
    c._filter.operator = ((b = a.record) == null ? void 0 : b.data.value) || null;
    if (c.operatorArgCount !== f) {
      c._filter.value = null;
    }
    if (c.isMultiSelectValueField && !e) {
      c._filter.value = [];
    }
    c.refreshValueFields();
    c.triggerChange();
  }
  triggerChange() {
    const {
      filter: a,
      isValid: b
    } = this;
    this.trigger('change', {
      'filter': a,
      'isValid': b
    });
  }
  onValueChange() {
    const a = this, {
        isMultiSelectValueField: b,
        fieldType: c,
        _filter: e
      } = a, f = this.valueFields.map(g => g.value);
    if (b && c === 'date') {
      e.value = f[0].map(g => new Date(g));
    } else if (b && c === 'duration') {
      e.value = f[0].map(g => new Duration(g));
    } else {
      if (c === 'date' && e.operator === 'between' && DateHelper.isValidDate(f[1])) {
        f[1].setHours(23, 59, 59, 999);
      }
      e.value = f.length === 1 ? f[0] : f;
    }
    a.triggerChange();
  }
  refreshOperatorPicker() {
    const {operatorPicker: a} = this.widgetMap, {
        _filter: {
          operator: b,
          property: c
        },
        operatorOptions: e
      } = this;
    a.items = e;
    a.value = b;
    a.hidden = c === null;
  }
  populateUIFromFilter(a = false) {
    const b = this, {
        filterValues: c,
        widgetMap: {
          propertyPicker: e,
          operatorPicker: f
        },
        _filter: {
          property: g,
          operator: i,
          disabled: j
        },
        propertyOptions: k,
        operatorOptions: l,
        isMultiSelectValueField: n
      } = b;
    e.items = k;
    f.items = l;
    f.hidden = g === null;
    let o = a;
    if (e.value !== g) {
      e.value = g;
      b.refreshOperatorPicker();
      o = true;
    }
    if (f.value !== i) {
      if (i === null || !f.items.find(({value: p}) => p === i)) {
        f.clear();
      } else {
        f.value = i;
      }
      o = true;
    }
    if (!i && f.items.length === 1) {
      f.value = b._filter.operator = f.items[0].value;
    }
    if (o) {
      b.refreshValueFields();
    }
    b.refreshCaseSensitive();
    b.valueFields.forEach((p, q) => {
      if (n && (p.value.length > 0 || c.length > 0)) {
        if (b.fieldType === 'date') {
          p.value = c.map(r => r == null ? void 0 : r.valueOf());
        } else if (b.fieldType === 'duration') {
          p.value = c.map(r => r == null ? void 0 : r.toString());
        } else {
          p.value = c;
        }
      } else if (q >= c.length) {
        p.clear();
      } else {
        p.value = c[q];
      }
    });
    b.allChildInputs.forEach(p => p.disabled = b.disabled || j);
  }
  get valueFields() {
    return this._valueFields || (this._valueFields = this.widgetMap.valueFields.queryAll(a => a.owner === this.widgetMap.valueFields && valueInputTypes[a.type]));
  }
  get filterValues() {
    let a;
    if (((a = this._filter) == null ? void 0 : a.value) == null) {
      return [];
    }
    return ArrayHelper.asArray(this._filter.value);
  }
  onFilterChange() {
    const a = this, b = a.fieldType, c = b !== a._fieldType;
    a._fieldType = a._filter.type = b;
    a.populateUIFromFilter(c);
  }
  get operatorArgCount() {
    const {
      fieldType: a,
      filter: {operator: b},
      operatorArgCountLookup: c
    } = this;
    return a && b ? c[a][b] : 1;
  }
  get isValid() {
    const a = this, {
        filter: b,
        fieldType: c,
        filterValues: e,
        isMultiSelectValueField: f,
        operatorArgCount: g
      } = a, {operator: i} = b, j = g > 0 && (b == null ? void 0 : b.value) == null;
    return Boolean(c && i && !j && (f && e.length > 0 || e.length === g) && e.every(k => k != null && (c !== 'duration' || k.isValid)));
  }
  get focusElement() {
    let a, b, c, e;
    return (e = (c = (b = (a = this.valueFields.find(g => g.isEmptyInput)) != null ? a : this.operatorPicker) != null ? b : this.propertyPicker) == null ? void 0 : c.input) != null ? e : super.focusElement;
  }
  inferFieldTypes() {
    let a;
    if (((a = this.store) == null ? void 0 : a.totalCount) > 0 && this.fields) {
      for (const [b, c] of Object.entries(this.fields)) {
        if (c.type === 'auto') {
          c.type = _FieldFilterPicker.inferFieldType(this.store, b);
        }
      }
    }
  }
  static inferFieldType(a, b) {
    let c;
    const e = (c = a.find(f => f.getValue(b) != null, true)) == null ? void 0 : c.getValue(b);
    if (e !== void 0) {
      const f = typeof e;
      if (f === 'object' && e instanceof Date) {
        return 'date';
      } else if (isInferrableType[f]) {
        return f;
      }
    }
    return 'auto';
  }
  static localizeOperator(a, b) {
    const {defaultOperators: c} = _FieldFilterPicker, e = Object.keys(c).sort((f, g) => f === b ? -1 : g === b ? 1 : 0);
    for (const f of e) {
      for (const g of c[f]) {
        if (g.value === a) {
          return _FieldFilterPicker.L(g.text);
        }
      }
    }
    return a;
  }
};
const FieldFilterPicker = _FieldFilterPicker;
FieldFilterPicker.initClass();
FieldFilterPicker._$name = 'FieldFilterPicker';
const Label = class extends Widget {
  static configurable = {
    'text': null,
    'localizableProperties': ['text']
  };
  static type = 'label';
  static $name = 'Label';
  compose() {
    const {
      text: a,
      html: b
    } = this;
    return {
      'tag': 'label',
      'text': a,
      'html': b
    };
  }
};
Label.initClass();
Label._$name = 'Label';
const _FieldFilterPickerGroup = class _FieldFilterPickerGroup extends Container {
  static childPickerType = 'fieldfilterpicker';
  static configurable = {
    'filters': [],
    'fields': null,
    'disabled': false,
    'readOnly': false,
    'layout': 'vbox',
    'store': null,
    'limitToProperty': null,
    'valueFieldCls': null,
    'showAddFilterButton': true,
    'canDeleteFilter': null,
    'getFieldFilterPickerConfig': null,
    'canManageFilter': null,
    'addFilterButtonText': null,
    'triggerChangeOnInput': true,
    'items': {
      'pickers': {
        'type': 'container',
        'layout': 'vbox',
        'scrollable': true,
        'items': {}
      },
      'addFilterButton': {
        'type': 'button',
        'text': _FieldFilterPickerGroup.addFilterButtonDefaultText,
        'cls': 'b-' + _FieldFilterPickerGroup.type + '-add-button',
        'hidden': true
      }
    },
    'operators': null,
    'dateFormat': 'L{FieldFilterPicker.dateFormat}'
  };
  static addFilterButtonDefaultText = 'L{FieldFilterPickerGroup.addFilter}';
  static type = 'fieldfilterpickergroup';
  static $name = 'FieldFilterPickerGroup';
  afterConstruct() {
    const a = this;
    a.validateConfig();
    const {addFilterButton: b} = a.widgetMap;
    b.ion({
      'click': 'addFilter',
      'thisObj': a
    });
    b.text = a.L(b.text);
    a.store && a.updateStore(a.store);
    super.afterConstruct();
  }
  changeDateFormat(a) {
    return this.L(a);
  }
  validateConfig() {
    if (!this.fields && !this.store) {
      throw new Error('FieldFilterPickerGroup requires either a \'fields\' or \'store\' config property.');
    }
  }
  updateFields(a) {
    this.widgetMap.pickers.childItems.forEach(b => b.fields = a);
  }
  updateFilters(a, b) {
    const c = this;
    if (b) {
      b.filter(e => !a.find(f => f.id === e.id)).forEach(e => {
        let f;
        return (f = c.store) == null ? void 0 : f.removeFilter(e.id);
      });
    }
    a.forEach(e => e.id = e.id || c.nextFilterId);
    c.widgetMap.pickers.items = (a == null ? void 0 : a.map(e => c.getPickerRowConfig(e))) || [];
  }
  changeFilters(a) {
    const {
      canManageFilter: b,
      limitToProperty: c
    } = this;
    let e = a;
    if (b) {
      e = e.filter(f => this.callback(b, this, [f]));
    }
    if (c) {
      e = e.filter(f => f.property == null || f.property === c);
    }
    return e;
  }
  updateStore(a) {
    const b = this;
    b.detachListeners('store');
    if (a) {
      let c = false;
      b.widgetMap.pickers.childItems.forEach(({
        widgetMap: {
          filterPicker: {
            filter: e,
            isValid: f
          }
        }
      }) => {
        if (f && !a.filters.includes(e)) {
          a.removeFilter(e.id, true);
          a.addFilter(e, true);
          c = true;
        }
      });
      if (c) {
        a.filter();
      }
      b.appendFiltersFromStore();
      a.ion({
        'name': 'store',
        'filter': 'onStoreFilter',
        'thisObj': b
      });
    }
    b.widgetMap.pickers.childItems.forEach(e => e.store = a);
  }
  updateShowAddFilterButton(a) {
    this.widgetMap.addFilterButton.hidden = !a;
  }
  updateAddFilterButtonText(a) {
    this.widgetMap.addFilterButton.text = a != null ? a : _FieldFilterPickerGroup.addFilterButtonDefaultText;
  }
  appendFiltersFromStore() {
    const a = this;
    a.store.filters.forEach(b => {
      let c;
      const e = a.canManage(b), {
          property: f,
          operator: g,
          id: i
        } = b;
      if (e && f && g && !((c = a.filters) == null ? void 0 : c.find(j => j.id === i))) {
        a.appendFilter(b);
      }
    });
  }
  canManage(a) {
    const b = this;
    return !b.canManageFilter || b.callback(b.canManageFilter, b, [a]) === true;
  }
  getFilterPickerConfig(a) {
    const b = this, {
        fields: c,
        store: e,
        disabled: f,
        readOnly: g,
        valueFieldCls: i,
        operators: j,
        limitToProperty: k,
        dateFormat: l,
        getFieldFilterPickerConfig: n,
        triggerChangeOnInput: o
      } = b;
    return {
      'type': b.constructor.childPickerType,
      'fields': c != null ? c : b.getFieldsFromStore(e),
      'filter': a,
      'store': e,
      'disabled': f,
      'readOnly': g,
      'propertyLocked': Boolean(k),
      'valueFieldCls': i,
      'operators': j,
      'dateFormat': l,
      'internalListeners': {
        'change': 'onFilterPickerChange',
        'thisObj': b
      },
      'flex': 1,
      'triggerChangeOnInput': o,
      ...n ? b.callback(n, b, [a]) : void 0
    };
  }
  getFieldsFromStore(a) {
    let b, c;
    return Object.fromEntries((c = (b = a.modelClass) == null ? void 0 : b.fields.map(({
      name: e,
      type: f
    }) => [
      e,
      { 'type': f }
    ])) != null ? c : []);
  }
  getPickerRowConfig(a) {
    const b = this, {
        disabled: c,
        readOnly: e,
        canDeleteFilter: f
      } = b, g = !(f && b.callback(f, b, [a]) === false);
    return {
      'type': 'container',
      'layout': 'box',
      'cls': {
        ['b-' + _FieldFilterPickerGroup.type + '-row']: true,
        ['b-' + _FieldFilterPickerGroup.type + '-row-removable']: g
      },
      'dataset': { 'separatorText': b.L('L{FieldFilterPicker.and}') },
      'items': {
        'activeCheckbox': {
          'type': 'checkbox',
          'disabled': c,
          'readOnly': e,
          'checked': !Boolean(a.disabled),
          'internalListeners': {
            'change': 'onFilterActiveChange',
            'thisObj': b
          },
          'cls': 'b-' + _FieldFilterPickerGroup.type + '-filter-active'
        },
        'filterPicker': b.getFilterPickerConfig(a),
        'removeButton': {
          'type': 'button',
          'ref': 'removeButton',
          'disabled': c,
          'readOnly': e,
          'hidden': !g,
          'cls': 'b-transparent b-' + _FieldFilterPickerGroup.type + '-remove',
          'icon': 'b-fa-trash',
          'internalListeners': {
            'click': 'removeFilter',
            'thisObj': b
          }
        }
      }
    };
  }
  get allInputs() {
    const a = [
      this.constructor.childPickerType,
      'button',
      'checkbox'
    ];
    return this.queryAll(b => a.includes(b.type));
  }
  updateDisabled(a, b) {
    super.updateDisabled(a, b);
    this.allInputs.forEach(c => c.disabled = a);
  }
  updateReadOnly(a) {
    this.allInputs.forEach(b => b.readOnly = a);
  }
  onFilterActiveChange({
    source: a,
    checked: b
  }) {
    const c = this, e = c.getFilterIndex(a), f = c.filters[e], g = c.getFilterPicker(e);
    f.disabled = !b;
    g.onFilterChange();
    if (c.store && g.isValid) {
      c.store.addFilter(f, true);
    }
    c.updateStoreFilter();
    c.triggerChange();
  }
  onFilterPickerChange({
    source: a,
    filter: b,
    isValid: c
  }) {
    const e = this, {store: f} = e, g = e.getFilterIndex(a);
    if (f) {
      f.removeFilter(b.id, true);
      if (c) {
        f.addFilter(b, true);
      }
      e.updateStoreFilter();
    }
    Object.assign(e.filters[g], b);
    e.triggerChange();
  }
  getFilterIndex(a) {
    return parseInt(a.parent.element.dataset.itemIndex);
  }
  getPickerRow(a) {
    return this.widgetMap.pickers.childItems[a];
  }
  getFilterPicker(a) {
    return this.getPickerRow(a).widgetMap.filterPicker;
  }
  get nextFilterId() {
    this._nextId = (this._nextId || 0) + 1;
    return this.id + '-filter-' + this._nextId;
  }
  removeFilter({source: a}) {
    const b = this, c = b.getFilterIndex(a), e = b.filters[c], f = b.getPickerRow(c), g = b.query(i => i.isFocusable && i.type !== 'container' && !f.contains(i));
    if (g) {
      g.focus();
    }
    b.removeFilterAt(c);
    if (b.store) {
      b.store.removeFilter(e.id, true);
      b.updateStoreFilter();
    }
    b.trigger('remove', { 'filter': e });
    b.triggerChange();
  }
  addFilter({
    property: property = null,
    operator: operator = null,
    value: value = null
  } = {}) {
    let a;
    const b = this, {filters: c} = b, e = {
        'property': b.limitToProperty || property,
        'operator': operator,
        'value': value,
        'disabled': false,
        'id': b.nextFilterId,
        'caseSensitive': false
      };
    if (!b.trigger('beforeAddFilter', { 'filter': e })) {
      return;
    }
    b.appendFilter(e);
    if (b.getFilterPicker(c.length - 1).isValid) {
      (a = b.store) == null ? void 0 : a.addFilter(e, true);
      b.store && b.updateStoreFilter();
    }
    b.trigger('add', { 'filter': e });
    b.triggerChange();
  }
  appendFilter(a) {
    const b = this;
    if (!b.limitToProperty || a.property === b.limitToProperty) {
      b.filters.push(a);
      b.widgetMap.pickers.add(b.getPickerRowConfig(a, b.filters.length - 1));
    }
  }
  onStoreFilter(a) {
    const b = this;
    if (b._isUpdatingStore) {
      return;
    }
    const {filters: c} = a, e = c.values.reduce((f, g) => ({
        ...f,
        [g.id]: g
      }), {});
    for (let f = b.filters.length - 1; f >= 0; f--) {
      const g = b.filters[f], i = e[g.id], j = b.getPickerRow(f);
      if (j) {
        const {
          filterPicker: k,
          activeCheckbox: l
        } = j.widgetMap;
        if (!i && k.isValid) {
          b.removeFilterAt(f);
        } else if (i !== void 0) {
          const {
            operator: n,
            value: o,
            property: p,
            disabled: q,
            caseSensitive: r
          } = i;
          if (g !== i) {
            Object.assign(g, {
              'operator': n,
              'value': o,
              'property': p,
              'disabled': q,
              'caseSensitive': r
            });
          }
          k.filter = g;
          k.onFilterChange();
          l.checked = !q;
        }
      }
    }
    b.appendFiltersFromStore();
    b.triggerChange();
  }
  removeFilterAt(a) {
    const {
      widgetMap: {pickers: b},
      filters: c
    } = this;
    b.remove(b.childItems[a]);
    c.splice(a, 1);
    this.triggerChange();
  }
  updateStoreFilter() {
    let a;
    this._isUpdatingStore = true;
    (a = this.store) == null ? void 0 : a.filter();
    this._isUpdatingStore = false;
  }
  get value() {
    return this.filters;
  }
  triggerChange() {
    const {filters: a} = this, b = a.filter((c, e) => this.getPickerRow(e).widgetMap.filterPicker.isValid);
    this.trigger('change', {
      'filters': a,
      'validFilters': b
    });
  }
  activateAll() {
    this.setAllActiveStatus(true);
  }
  deactivateAll() {
    this.setAllActiveStatus(false);
  }
  setAllActiveStatus(a) {
    const b = this, {
        _filters: c,
        store: e
      } = b;
    c.forEach((f, g) => {
      if (a === f.disabled) {
        const {
          filterPicker: i,
          activeCheckbox: j
        } = b.getPickerRow(g).widgetMap;
        f.disabled = !a;
        i.onFilterChange();
        j.checked = a;
        if (a && e && i.isValid) {
          e.addFilter(f, true);
        }
      }
    });
    b.updateStoreFilter();
  }
  get focusElement() {
    let a, b;
    return (b = (a = this.getPickerRow(this._filters.length - 1)) == null ? void 0 : a.widgetMap.filterPicker.focusElement) != null ? b : super.focusElement;
  }
};
const FieldFilterPickerGroup = _FieldFilterPickerGroup;
FieldFilterPickerGroup.initClass();
FieldFilterPickerGroup._$name = 'FieldFilterPickerGroup';
const FieldSet = class extends Panel.mixin(Labelable_default) {
  static type = 'fieldset';
  static $name = 'FieldSet';
  static get configurable() {
    return {
      'bodyTag': 'fieldset',
      'focusable': false,
      'localizableProperties': ['label'],
      'inline': null,
      'inlineInternal': null,
      'layout': {
        'type': 'box',
        'horizontal': false
      }
    };
  }
  static get prototypeProperties() {
    return {
      'flexRowCls': 'b-hbox',
      'flexColCls': 'b-vbox'
    };
  }
  get bodyConfig() {
    const a = super.bodyConfig, {className: b} = a, {
        inlineInternal: c,
        hasLabel: e,
        title: f
      } = this;
    delete a.html;
    b['b-inline'] = c;
    b['b-fieldset-has-label'] = e;
    if (f) {
      a.children = {
        'legendElement': {
          'tag': 'legend',
          'text': f,
          'class': { 'b-fieldset-legend': 1 }
        }
      };
    }
    return a;
  }
  compose() {
    const {
      inlineInternal: a,
      label: b,
      labelCls: c,
      labelWidth: e
    } = this;
    return {
      'class': {
        'b-field': b,
        'b-vbox': !a
      },
      'children': {
        'labelElement > headerElement': (b || null) && {
          'tag': 'label',
          'html': b,
          'class': {
            'b-label': 1,
            'b-align-start': 1,
            [c]: c
          },
          'style': { 'width': DomHelper.unitize('width', e)[1] }
        }
      }
    };
  }
  syncInlineInternal() {
    let a;
    this.inlineInternal = (a = this.inline) != null ? a : this.label != null && this.labelPosition === 'before';
  }
  updateDisabled(a, b) {
    super.updateDisabled(a, b);
    this.eachWidget(c => {
      c.disabled = a;
    }, false);
  }
  updateInline() {
    this.syncInlineInternal();
  }
  updateInlineInternal(a) {
    this.layout.horizontal = a;
  }
  updateLabel() {
    this.syncInlineInternal();
  }
  updateLabelPosition() {
    this.syncInlineInternal();
  }
};
FieldSet.initClass();
FieldSet._$name = 'FieldSet';
const FileField = class extends Field {
  static type = 'filefield';
  static $name = 'FileField';
  static get configurable() {
    return {
      'multiple': null,
      'accept': null,
      'inputType': 'file',
      'attributes': [
        'multiple',
        'accept'
      ]
    };
  }
  get files() {
    return this.input.files;
  }
  pickFile() {
    this.input.click();
  }
  get multiple() {
    return this._multiple ? '' : null;
  }
  clear() {
    this.input.value = null;
  }
  triggerChange(a) {
    this.triggerFieldChange({
      'event': a,
      'value': this.input.value,
      'oldValue': this._lastValue,
      'userAction': true,
      'valid': true
    });
  }
};
FileField.initClass();
FileField._$name = 'FileField';
const FilePicker = class extends Container {
  static configurable = {
    'defaultBindProperty': 'value',
    'buttonConfig': null,
    'fileFieldConfig': null,
    'showBadge': true
  };
  static type = 'filepicker';
  static $name = 'FilePicker';
  construct(a = {}) {
    const b = this;
    a.items = [
      Object.assign({
        'type': 'button',
        'ref': 'fileButton',
        'text': 'L{FilePicker.file}',
        'localeClass': this
      }, a.buttonConfig),
      Object.assign({
        'type': 'filefield',
        'ref': 'fileField',
        'style': 'display: none'
      }, a.fileFieldConfig)
    ].concat(a.items || []);
    super.construct(a);
    b.button.ion({
      'click': b.onButtonClick,
      'thisObj': b
    });
    b.fileField.ion({
      'change': b.onFileFieldChange,
      'thisObj': b
    });
    b._thisIsAUsedExpression(b.fileTip);
  }
  get button() {
    return this.widgetMap.fileButton;
  }
  get fileField() {
    return this.widgetMap.fileField;
  }
  get files() {
    return this.fileField.files;
  }
  get fileTip() {
    const a = this;
    return a._fileTip || (a._fileTip = new Tooltip({
      'cls': 'b-file-tip',
      'forElement': a.button.element,
      'showOnHover': true,
      'align': 'b-t',
      'scrollAction': 'realign',
      'internalListeners': {
        'beforeshow'() {
          const b = this, c = a.files;
          if (c && c.length) {
            b.html = '' + Array.from(c).map(e => StringHelper.encodeHtml(e.name)).join('<br>');
            return true;
          }
          return false;
        }
      }
    }));
  }
  clear() {
    const a = this;
    a.fileField.clear();
    a.updateBadge();
    a.trigger('clear');
  }
  onButtonClick({event: a}) {
    const b = this;
    b.fileField.pickFile();
    a.preventDefault();
  }
  onFileFieldChange({valid: a}) {
    const b = this;
    b.updateBadge();
    b.triggerFieldChange({
      'files': b.files,
      'valid': a
    });
  }
  updateShowBadge() {
    if (!this.isConfiguring) {
      this.updateBadge();
    }
  }
  updateBadge() {
    this.button.badge = this.showBadge && this.files.length || '';
  }
};
FilePicker.initClass();
FilePicker._$name = 'FilePicker';
const FilterField = class extends TextField {
  static type = 'filterfield';
  static $name = 'FilterField';
  static get configurable() {
    return {
      'field': null,
      'store': null,
      'filterFunction': null,
      'clearable': true,
      'revertOnEscape': true,
      'ignoreParentReadOnly': true,
      'keyStrokeChangeDelay': 100,
      'internalListeners': { 'change': 'onInternalChange' }
    };
  }
  updateValue(a, b) {
    super.updateValue(a, b);
    if (a && this.isConfiguring) {
      this.onInternalChange({ 'value': a });
    }
  }
  onInternalChange({value: a}) {
    const {
      store: b,
      field: c,
      filterFunction: e
    } = this;
    if (b) {
      const f = (c || this.id) + '-Filter';
      if (a.length === 0) {
        b.removeFilter(f);
      } else {
        let g;
        if (e) {
          g = i => e(i, a);
        } else {
          a = a.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
          g = i => {
            let j;
            return (j = i.getValue(c)) == null ? void 0 : j.match(new RegExp(a, 'i'));
          };
        }
        b.filter({
          'id': f,
          'filterBy': g
        });
      }
    }
  }
};
FilterField.initClass();
FilterField._$name = 'FilterField';
const items = [
  {
    'ref': 'cancelButton',
    'cls': 'b-messagedialog-cancelbutton b-gray',
    'text': 'L{Object.Cancel}',
    'onClick': 'up.onCancelClick'
  },
  {
    'ref': 'okButton',
    'cls': 'b-messagedialog-okbutton b-raised b-blue',
    'text': 'L{Object.Ok}',
    'onClick': 'up.onOkClick'
  }
];
if (BrowserHelper.isWindows) {
  items.reverse();
}
const _MessageDialogConstructor = class _MessageDialogConstructor extends Popup {
  static type = 'messagedialog';
  static $name = 'MessageDialog';
  static get configurable() {
    return {
      'ui': 'plain',
      'centered': true,
      'modal': true,
      'hidden': true,
      'autoShow': false,
      'closeAction': 'hide',
      'title': '\xA0',
      'lazyItems': {
        '$config': ['lazy'],
        'value': [
          {
            'cls': 'b-messagedialog-message',
            'ref': 'message'
          },
          {
            'type': 'textfield',
            'cls': 'b-messagedialog-input',
            'ref': 'input'
          }
        ]
      },
      'showClass': null,
      'bbar': {
        'overflow': null,
        'items': items
      }
    };
  }
  construct() {
    this.okButton = this.yesButton = 1;
    this.cancelButton = 3;
    super.construct(...arguments);
  }
  destroy() {
  }
  async confirm() {
    return this.showDialog('confirm', ...arguments);
  }
  async alert() {
    return this.showDialog('alert', ...arguments);
  }
  async prompt({textField: a}) {
    const b = this.widgetMap.input;
    Widget.reconfigure(b, a);
    b.value = a == null ? void 0 : a.value;
    return this.showDialog('prompt', ...arguments);
  }
  showDialog(a, {
    message: message = '',
    title: title = '',
    cancelButton: b,
    okButton: c,
    align: e,
    modal: f,
    rootElement: rootElement = document.body
  }) {
    const g = this;
    g.rootElement = rootElement;
    g.getConfig('lazyItems');
    g.title = g.optionalL(title);
    g.widgetMap.message.html = g.optionalL(message);
    g.showClass = 'b-messagedialog-' + a;
    g.modal = typeof f === 'boolean' ? f : true;
    g.centered = !e;
    if (c) {
      c = typeof c === 'string' ? { 'text': c } : c;
    }
    if (b) {
      b = typeof b === 'string' ? { 'text': b } : b;
    }
    c = Object.assign({}, g.widgetMap.okButton.initialConfig, c);
    b = Object.assign({}, g.widgetMap.cancelButton.initialConfig, b);
    c.text = g.optionalL(c.text);
    b.text = g.optionalL(b.text);
    Widget.reconfigure(g.widgetMap.okButton, c);
    Widget.reconfigure(g.widgetMap.cancelButton, b);
    g.show({ 'align': e });
    return g.promise = new Promise(i => {
      g.resolve = i;
    });
  }
  show() {
    const a = DomHelper.getActiveElement(this.element);
    this.owner = this.element.contains(a) ? null : _MessageDialogConstructor.fromElement(document.activeElement);
    return super.show(...arguments);
  }
  updateShowClass(a, b) {
    const {classList: c} = this.element;
    if (b) {
      c.remove(b);
    }
    if (a) {
      c.add(a);
    }
  }
  doResolve(a) {
    const b = this, {resolve: c} = b;
    if (c) {
      const e = b.showClass === 'b-messagedialog-prompt';
      if (e && a === b.okButton && !b.widgetMap.input.isValid) {
        return;
      }
      b.resolve = b.reject = b.promise = null;
      c(e ? {
        'button': a,
        'text': b.widgetMap.input.value
      } : a);
      b.hide();
    }
  }
  onInternalKeyDown(a) {
    if (a.key === 'Escape') {
      a.stopImmediatePropagation();
      this.onCancelClick();
    } else if (a.key === 'Enter' && !a.target.closest('.b-button')) {
      a.stopImmediatePropagation();
      a.preventDefault();
      this.onOkClick();
    }
    super.onInternalKeyDown(a);
  }
  onOkClick() {
    this.doResolve(MessageDialog.okButton);
  }
  onCancelClick() {
    this.doResolve(MessageDialog.cancelButton);
  }
};
const MessageDialogConstructor = _MessageDialogConstructor;
MessageDialogConstructor.initClass();
var MessageDialog = new Proxy({}, {
  'get'(a, b) {
    const c = a.instance || (a.instance = new MessageDialogConstructor({ 'rootElement': document.body })), e = c[b];
    return typeof e === 'function' ? e.bind(c) : e;
  }
});
const MessageDialog_default = MessageDialog;
const PagingToolbar = class extends Toolbar {
  static type = 'pagingtoolbar';
  static $name = 'PagingToolbar';
  static get defaultConfig() {
    return {
      'store': null,
      'defaults': { 'localeClass': this },
      'items': {
        'firstPageButton': {
          'onClick': 'up.onFirstPageClick',
          'icon': 'b-icon-first',
          'weight': 100,
          'tooltip': 'L{PagingToolbar.firstPage}'
        },
        'previousPageButton': {
          'onClick': 'up.onPreviousPageClick',
          'icon': 'b-icon-previous',
          'weight': 110,
          'tooltip': 'L{PagingToolbar.prevPage}'
        },
        'pageNumber': {
          'type': 'numberfield',
          'label': 'L{page}',
          'min': 1,
          'max': 1,
          'triggers': null,
          'onChange': 'up.onPageNumberChange',
          'highlightExternalChange': false,
          'weight': 120
        },
        'pageCount': {
          'type': 'widget',
          'cls': 'b-pagecount b-toolbar-text',
          'weight': 130
        },
        'nextPageButton': {
          'onClick': 'up.onNextPageClick',
          'icon': 'b-icon-next',
          'weight': 140,
          'tooltip': 'L{PagingToolbar.nextPage}'
        },
        'lastPageButton': {
          'onClick': 'up.onLastPageClick',
          'icon': 'b-icon-last',
          'weight': 150,
          'tooltip': 'L{PagingToolbar.lastPage}'
        },
        'separator': {
          'type': 'widget',
          'cls': 'b-toolbar-separator',
          'weight': 151
        },
        'reloadButton': {
          'onClick': 'up.onReloadClick',
          'icon': 'b-icon-reload',
          'weight': 160,
          'tooltip': 'L{PagingToolbar.reload}'
        },
        'spacer': {
          'type': 'widget',
          'cls': 'b-toolbar-fill',
          'weight': 161
        },
        'dataSummary': {
          'type': 'widget',
          'cls': 'b-toolbar-text',
          'weight': 170
        }
      }
    };
  }
  set parent(a) {
    super.parent = a;
    if (!this.store) {
      this.store = a.store;
    }
  }
  get parent() {
    return super.parent;
  }
  set store(a) {
    const b = this;
    b.detachListeners('store');
    b._store = a;
    if (a) {
      a.ion({
        'name': 'store',
        'beforerequest': 'onStoreBeforeRequest',
        'afterrequest': 'onStoreChange',
        'change': 'onStoreChange',
        'thisObj': b
      });
      if (a.isLoading) {
        b.onStoreBeforeRequest();
      }
    }
  }
  get store() {
    return this._store;
  }
  onStoreBeforeRequest() {
    this.eachWidget(a => a.disable());
  }
  updateLocalization() {
    this.updateSummary();
    super.updateLocalization();
  }
  updateSummary() {
    const a = this, {
        pageCount: b,
        dataSummary: c
      } = a.widgetMap;
    let e = 0, f = 0, g = 0, i = 0, j = 0;
    if (a.store) {
      const {store: k} = a, {
          pageSize: l,
          currentPage: n
        } = k;
      e = k.count;
      f = k.lastPage;
      j = k.totalCount;
      g = Math.max(0, (n - 1) * l + 1);
      i = Math.min(j, g + l - 1);
    }
    b.html = a.L('L{pageCountTemplate}')({ 'lastPage': f });
    c.html = e ? a.L('L{summaryTemplate}')({
      'start': g,
      'end': i,
      'allCount': j
    }) : a.L('L{noRecords}');
  }
  onStoreChange() {
    const a = this, {
        widgetMap: b,
        store: c
      } = a, {
        count: e,
        lastPage: f,
        currentPage: g
      } = c, {
        pageNumber: i,
        pageCount: j,
        firstPageButton: k,
        previousPageButton: l,
        nextPageButton: n,
        lastPageButton: o,
        dataSummary: p
      } = b;
    a.eachWidget(q => q.enable());
    i.value = g;
    i.max = f;
    p.disabled = i.disabled = j.disabled = !e;
    k.disabled = l.disabled = g <= 1 || !e;
    n.disabled = o.disabled = g >= f || !e;
    a.updateSummary();
  }
  onPageNumberChange({value: a}) {
    if (this.store.currentPage !== a) {
      this.store.loadPage(a);
    }
  }
  onFirstPageClick() {
    this.store.loadPage(1);
  }
  onPreviousPageClick() {
    this.store.previousPage();
  }
  onNextPageClick() {
    this.store.nextPage();
  }
  onLastPageClick() {
    this.store.loadPage(this.store.lastPage);
  }
  onReloadClick() {
    this.store.loadPage(this.store.currentPage);
  }
};
PagingToolbar.initClass();
PagingToolbar._$name = 'PagingToolbar';
const {dockIsHorz: dockIsHorz2} = PanelCollapser.maps;
const collapseExposeEdge = {
  'top': 0,
  'down': 0,
  'left': 1,
  'bottom': 2,
  'up': 2,
  'right': 3
};
const translateByDir = {
  'up': {
    'from': 'translate(0,0)',
    'to': 'translate(0,-100%)'
  },
  'down': {
    'from': 'translate(0,0)',
    'to': 'translate(0,100%)'
  },
  'left': {
    'from': 'translate(0,0)',
    'to': 'translate(-100%,0)'
  },
  'right': {
    'from': 'translate(0,0)',
    'to': 'translate(100%,0)'
  }
};
const PanelCollapserOverlay = class extends PanelCollapser.mixin(Delayable_default) {
  static type = 'overlay';
  static $name = 'PanelCollapserOverlay';
  static get configurable() {
    return {
      'autoCloseDelay': 1000,
      'autoClose': true,
      'revealing': {
        'value': null,
        '$config': null,
        'default': false
      },
      'recollapseTool': {
        'type': 'collapsetool',
        'cls': 'b-recollapse',
        'collapsify': 'overlay',
        'handler'() {
          let a;
          (a = this.collapsible) == null ? void 0 : a.toggleReveal();
        }
      }
    };
  }
  static get delayable() {
    return { 'doAutoClose': 0 };
  }
  doAutoClose() {
    this.toggleReveal(false);
  }
  updateAutoCloseDelay(a) {
    const {doAutoClose: b} = this;
    if (!(b.suspended = a == null || a < 0)) {
      b.delay = a;
      b.immediate = !a;
    }
  }
  changeRecollapseTool(a) {
    const b = this, {panel: c} = b;
    if (b.isConfiguring || b.isDestroying || !c || c.isDestroying) {
      return a;
    }
    c.tools = { 'recollapse': a };
  }
  beforeCollapse(a) {
    if (super.beforeCollapse(a) === false) {
      return false;
    }
    if (this.panel.revealed) {
      a.animation = null;
    }
  }
  applyHeaderDock(a, b = true) {
    let c;
    (c = this.panel) == null ? void 0 : c.recompose();
    super.applyHeaderDock(a, b);
  }
  collapseBegin(a) {
    const b = this, {
        collapseDir: c,
        innerElement: e
      } = b, {animation: f} = a, {
        collapseTool: g,
        panel: i
      } = b;
    b.configuredWidth = i._lastWidth;
    b.configuredHeight = i._lastHeight;
    b.applyHeaderDock(true);
    b.lockInnerSize();
    g == null ? void 0 : g.element.classList.add('b-collapsed');
    if (f) {
      i.element.classList.add('b-collapsing');
      f.element = e;
      f.transform = translateByDir[c];
      a.animation = Animator.run(f);
    }
  }
  onComplete(a) {
    let b, c;
    super.onComplete(a);
    const e = this, {panel: f} = e, {element: g} = f;
    e.autoCloseLeaveDetacher = (b = e.autoCloseLeaveDetacher) == null ? void 0 : b.call(e);
    e.autoCloseClickDetacher = (c = e.autoCloseClickDetacher) == null ? void 0 : c.call(e);
    e.doAutoClose.cancel();
    if (a === 'reveal' && e.autoClose) {
      if (f.containsFocus) {
        e.autoCloseLeaveDetacher = EventHelper.on({
          'element': g,
          'mouseenter': i => {
            e.doAutoClose.cancel();
          },
          'mouseleave': i => {
            e.doAutoClose();
          }
        });
      }
      e.autoCloseClickDetacher = EventHelper.on({
        'element': document.body,
        'thisObj': f,
        'mousedown': i => {
          let j;
          if (!f.owns(i) && !((j = e.revealer) == null ? void 0 : j.contains(i.target)) && e.autoCloseDelay != null) {
            e.doAutoClose.now();
          }
        }
      });
    }
  }
  expandBegin(a) {
    const b = this, {animation: c} = a, {
        collapseDir: e,
        collapseTool: f,
        innerElement: g,
        panel: i
      } = b, {element: j} = i;
    j.classList.remove('b-collapsed', 'b-collapsing');
    b.restoreConfiguredSize();
    b.lockInnerSize(false);
    b.lockInnerSize();
    f == null ? void 0 : f.element.classList.remove('b-collapsed');
    if (c) {
      j.classList.add('b-collapsed', 'b-expanding');
      c.element = g;
      c.transform = {
        'from': translateByDir[e].to,
        'to': translateByDir[e].from
      };
      a.animation = Animator.run(c);
    }
  }
  expandEnd(a) {
    super.expandEnd(a);
    const {panel: b} = this;
    if (a.completed) {
      b.revealed = false;
    }
    b.element.classList.remove('b-expanding');
  }
  expandRevert(a) {
    super.expandRevert(a);
    this.panel.element.classList.add('b-expanding');
  }
  get innerElement() {
    return this.panel.overlayElement;
  }
  get innerSizeElement() {
    return this.panel.element;
  }
  get toolsConfig() {
    const a = this, {direction: b} = a, c = super.toolsConfig, e = a.recollapseTool;
    if (e) {
      return {
        ...c,
        'recollapse': e && ObjectHelper.assign({ 'direction': b.toLowerCase() }, e)
      };
    }
    return c;
  }
  lockInnerSize(a = true) {
    const b = this, {panel: c} = b, e = a && c.rectangle(), f = a ? [
        0,
        0,
        0,
        0
      ] : '', g = super.lockInnerSize(a);
    if (a) {
      f[collapseExposeEdge[b.collapseDir]] = '-' + (e[b.collapseDim] + 10) + 'px';
    }
    c.element.style.clipPath = a ? 'inset(' + f.join(' ') + ')' : '';
    return g;
  }
  onOverlayTransitionDone(a) {
    const b = this, {panel: c} = b;
    if (a.srcElement === c.overlayElement && b.revealing) {
      b.revealing = false;
      b.onComplete(c.revealed ? 'reveal' : 'unreveal');
    }
  }
  onRevealerClick() {
    this.toggleReveal();
  }
  toggleReveal(a) {
    const {panel: b} = this, {element: c} = b;
    if (b.collapsed) {
      this.revealer = DomHelper.getActiveElement(c);
      if (a == null) {
        a = !b.revealed;
      }
      if (b.revealed !== a && b.trigger('beforeToggleReveal', { 'reveal': a }) !== false) {
        if (!a && c.contains(this.revealer)) {
          b.revertFocus(true);
        }
        this.revealing = true;
        b.revealed = a;
      }
    }
  }
  updateRevealing(a) {
    const b = this, c = /left|right/i, {panel: e} = b, f = c.test(b.collapseDir) ? 'height' : 'width';
    if (e) {
      b.innerElement.style[f] = '0px';
      b.innerElement.style['min-' + f] = '100%';
      e.element.classList[a ? 'add' : 'remove']('b-panel-overlay-revealing');
    }
  }
  wrapCollapser(a, b) {
    let c;
    const e = this, {
        collapseDir: f,
        panel: g
      } = e, {
        expandedHeaderDock: i,
        header: j,
        uiClassList: k
      } = g, l = (c = g.tools) == null ? void 0 : c.recollapse, [n, o] = e.splitHeaderItems({
        'as': 'element',
        'dock': e.collapseDock
      }), p = dockIsHorz2[i], q = g.hasHeader ? g.title || (j == null ? void 0 : j.title) || '\xA0' : null;
    if (l) {
      l.direction = f;
    }
    return [
      'overlayElement',
      {
        'class': {
          ...k,
          ['b-panel-overlay-header-' + i]: 1,
          ['b-panel-overlay-' + f]: 1,
          ['b-' + (p ? 'h' : 'v') + 'box']: 1,
          'b-panel-overlay': 1,
          'b-box-center': 1
        },
        'listeners': {
          'transitionend': r => e.onOverlayTransitionDone(r),
          'transitioncancel': r => e.onOverlayTransitionDone(r)
        },
        'children': {
          'overlayHeaderElement': q && {
            'tag': 'header',
            'class': new DomClassList({
              ...k,
              ['b-dock-' + i]: 1,
              'b-panel-header': 1,
              'b-panel-overlay-header': 1
            }, j == null ? void 0 : j.cls),
            'children': [
              ...n,
              {
                'reference': 'overlayTitleElement',
                'html': q,
                'class': {
                  ...k,
                  ['b-align-' + ((j == null ? void 0 : j.titleAlign) || 'start')]: 1,
                  'b-header-title': 1
                }
              },
              ...o
            ]
          },
          [a]: b
        }
      }
    ];
  }
};
PanelCollapserOverlay.initClass();
PanelCollapserOverlay._$name = 'PanelCollapserOverlay';
const PasswordField = class extends Field {
  static alias = 'password';
  static type = 'passwordfield';
  static $name = 'PasswordField';
  construct(a = {}) {
    a.inputType = 'password';
    super.construct(...arguments);
    this.element.classList.add('b-textfield');
  }
};
PasswordField.initClass();
PasswordField._$name = 'PasswordField';
const Radio = class extends Checkbox {
  static alias = 'radiobutton';
  static type = 'radio';
  static $name = 'Radio';
  static get configurable() {
    return {
      'inputType': 'radio',
      'clearable': null,
      'uncheckedValue': void 0
    };
  }
  get textLabelCls() {
    return super.textLabelCls + ' b-radio-label';
  }
  internalOnClick(a) {
    if (super.internalOnClick(a) !== false) {
      if (this.checked && this.clearable) {
        this.checked = false;
      }
    }
  }
  updateName(a) {
    this.toggleGroup = a;
  }
  updateClearable() {
  }
};
Radio.initClass();
Radio._$name = 'Radio';
const RadioGroup = class extends FieldSet {
  static type = 'radiogroup';
  static $name = 'RadioGroup';
  static get configurable() {
    return {
      'defaultType': 'radio',
      'clearable': null,
      'name': null,
      'options': {
        'value': null,
        '$config': { 'merge': 'items' }
      },
      'defaultBindProperty': 'value'
    };
  }
  get existingOptions() {
    const {name: a} = this;
    return this.ensureItems().filter(b => b.name === a);
  }
  get refPrefix() {
    return (this.name || this.ref || this.id) + '_';
  }
  get selected() {
    return this.existingOptions.filter(a => a.input.checked)[0] || null;
  }
  get value() {
    const {selected: a} = this;
    return a ? a.checkedValue : null;
  }
  set value(a) {
    this.existingOptions.forEach(b => {
      b.isConfiguring = this.isConfiguring;
      b.checked = b.checkedValue === a;
      b.isConfiguring = false;
    });
  }
  ensureItems() {
    this.getConfig('options');
    return super.ensureItems();
  }
  changeOptions(a, b) {
    if (!(a && b && ObjectHelper.isDeeplyEqual(b, a))) {
      return a;
    }
  }
  convertOption(a, b, c) {
    const e = this, {name: f} = e, g = {
        'name': f,
        'type': 'radio',
        'value': a === e.value,
        'ref': '' + e.refPrefix + a,
        'checkedValue': a
      };
    if (typeof b === 'string') {
      g.text = b;
    } else {
      ObjectHelper.assign(g, b);
    }
    return c ? Widget.reconfigure(c, g) : g;
  }
  isOurRadio(a) {
    return a.isRadio && a.name === this.name;
  }
  isolateFieldChange(a) {
    return this.isOurRadio(a);
  }
  onChildAdd(a) {
    super.onChildAdd(a);
    if (this.isOurRadio(a)) {
      a.ion({
        'name': a.id,
        'beforeChange': 'onRadioItemBeforeChange',
        'change': 'onRadioItemChange',
        'click': 'onRadioClick',
        'thisObj': this
      });
    }
  }
  onChildRemove(a) {
    if (this.isOurRadio(a)) {
      this.detachListeners(a.id);
    }
    super.onChildRemove(a);
  }
  onRadioClick(a) {
    const {source: b} = a;
    if (b.checked && this.clearable && b.clearable == null) {
      b.checked = false;
    }
  }
  onRadioItemBeforeChange(a) {
    if (a.checked) {
      const b = this, {lastValue: c} = b;
      if (!b.reverting && b.trigger('beforeChange', b.wrapRadioEvent(a)) === false) {
        if (c != null && c !== b.value) {
          b.reverting = true;
          a.source.uncheckToggleGroupMembers();
          b.value = c;
          b.lastValue = c;
          b.reverting = false;
          return false;
        }
      }
    }
  }
  onRadioItemChange(a) {
    const b = this;
    if (a.checked && !b.reverting) {
      b.triggerFieldChange(b.wrapRadioEvent(a));
      b.lastValue = b.value;
    }
  }
  wrapRadioEvent(a) {
    return {
      'from': a,
      'item': a.source,
      'userAction': a.userAction,
      'lastValue': this.lastValue,
      'value': this.value
    };
  }
  updateOptions() {
    const a = this, {
        options: b,
        refPrefix: c
      } = a, e = a.existingOptions.reduce((k, l) => {
        k[l.ref.substring(c.length)] = l;
        return k;
      }, {});
    let f = 0, g, i;
    if (b) {
      for (g in b) {
        i = a.convertOption(g, b[g], e[g]);
        delete e[g];
        a.insert(i, f++);
      }
    }
    const j = Object.values(e);
    if (j == null ? void 0 : j.length) {
      a.remove(j);
      j.forEach(k => k.destroy());
    }
  }
};
RadioGroup.initClass();
RadioGroup._$name = 'RadioGroup';
const arrowKeys = {
  'ArrowUp': 1,
  'ArrowDown': 1,
  'ArrowLeft': 1,
  'ArrowRight': 1
};
const Slider = class extends Widget {
  static type = 'slider';
  static $name = 'Slider';
  static get configurable() {
    return {
      'text': null,
      'showValue': true,
      'showTooltip': false,
      'min': 0,
      'max': 100,
      'step': 1,
      'value': 50,
      'unit': null,
      'thumbSize': 20,
      'tooltip': {
        '$config': [
          'lazy',
          'nullify'
        ],
        'value': {
          'type': 'tooltip',
          'align': 'b-t',
          'anchor': false,
          'axisLock': true
        }
      },
      'localizableProperties': ['text'],
      'triggerChangeOnInput': null,
      'defaultBindProperty': 'value',
      'twinSyncConfigs': [
        'max',
        'min',
        'step',
        'value'
      ],
      'label': null
    };
  }
  compose() {
    const {
        id: a,
        min: b,
        max: c,
        showValue: e,
        step: f,
        text: g,
        value: i,
        unit: unit = '',
        disabled: j,
        readOnly: k,
        label: l
      } = this, n = a + '-input', o = Boolean(g || e);
    return {
      'class': {
        'b-has-field-label': l,
        'b-has-label': o,
        'b-text': o,
        'b-disabled': j
      },
      'children': {
        'inputLabel': l ? {
          'tag': 'label',
          'for': n,
          'text': l
        } : null,
        'input': {
          'tag': 'input',
          'type': 'range',
          'id': n,
          'reference': 'input',
          [j ? 'disabled' : '']: j,
          [k ? 'readOnly' : '']: k,
          'min': b,
          'max': c,
          'step': f,
          'value': i,
          'listeners': {
            'input': 'onInternalInput',
            'change': 'onInternalChange',
            'mouseover': 'onInternalMouseOver',
            'mouseout': 'onInternalMouseOut'
          }
        },
        'valueLabel': o ? {
          'tag': 'label',
          'class': 'b-slider-value',
          'for': n,
          'text': e ? g ? g + ' (' + i + unit + ')' : i + unit : g
        } : null
      }
    };
  }
  get focusElement() {
    return this.input;
  }
  get percentProgress() {
    return (this.value - this.min) / (this.max - this.min) * 100;
  }
  onInternalKeyDown(a) {
    if (!this.readOnly && arrowKeys[a.key]) {
      a.stopImmediatePropagation();
    }
  }
  onInternalChange() {
    this.updateUI();
    this.triggerChange(true);
    this.trigger('action', { 'value': this.value });
  }
  onInternalInput() {
    const a = this;
    if (a.readOnly) {
      a.input.value = a.value;
      return;
    }
    a.value = parseInt(a.input.value, 10);
    a.trigger('input', { 'value': a.value });
    if (a.triggerChangeOnInput) {
      a.triggerChange(a);
    }
  }
  onInternalMouseOver() {
    let a;
    const b = this, c = b.rtl ? 100 - b.percentProgress : b.percentProgress;
    (a = b.tooltip) == null ? void 0 : a.showBy({
      'target': Rectangle.from(b.input).inflate(b.thumbSize / 2, -b.thumbSize / 2),
      'align': 'b-t' + Math.round(c)
    });
  }
  onInternalMouseOut() {
    let a;
    (a = this.tooltip) == null ? void 0 : a.hide();
  }
  triggerChange(a) {
    this.triggerFieldChange({
      'value': this.value,
      'valid': true,
      'userAction': a
    });
  }
  updateMax(a) {
    const b = this;
    if (b.input && b._value > a) {
      b.value = a;
      b.trigger('input', { 'value': b.value });
    }
  }
  updateMin(a) {
    const b = this;
    if (b.input && b._value < a) {
      b.value = a;
      b.trigger('input', { 'value': b.value });
    }
  }
  changeTooltip(a, b) {
    let c;
    if (a) {
      a.owner = this;
    }
    return this.showTooltip ? Tooltip.reconfigure(b, a, {
      'owner': this,
      'defaults': {
        'forElement': this.input,
        'html': String(this.value) + ((c = this.unit) != null ? c : '')
      }
    }) : null;
  }
  changeValue(a) {
    const b = this, {
        min: c,
        step: e
      } = b;
    a = Math.min(Math.max(a, c), b.max);
    if (a > c) {
      return c + ObjectHelper.roundTo(a - c, e);
    }
    return ObjectHelper.roundTo(a, e);
  }
  updateValue(a) {
    let b;
    const c = this, {
        input: e,
        _tooltip: f
      } = c;
    if (f) {
      f.html = c.value + ((b = c.unit) != null ? b : '');
    }
    if (e && e.value !== String(a)) {
      e.value = a;
      c.triggerChange(false);
    }
    c.updateUI();
  }
  updateUI() {
    let a, b;
    const c = this;
    ((a = c._tooltip) == null ? void 0 : a.isVisible) && ((b = c._tooltip) == null ? void 0 : b.alignTo({
      'target': Rectangle.from(c.input).inflate(c.thumbSize / 2, -c.thumbSize / 2),
      'align': 'b-t' + Math.round(c.percentProgress)
    }));
  }
};
Slider.initClass();
Slider._$name = 'Slider';
const SlideToggle = class extends Checkbox {
  static type = 'slidetoggle';
  static $name = 'SlideToggle';
  static get properties() {
    return { 'toggledCls': 'b-slidetoggle-checked' };
  }
  construct(a) {
    if (a.checked) {
      a.cls = DomClassList.from(a.cls) || {};
      a.cls[this.constructor.properties.toggledCls] = 1;
    }
    super.construct(a);
  }
  get innerElements() {
    const a = super.innerElements;
    a.splice(1, 0, this.toggleElement);
    if (this.text) {
      a[a.length - 1].class = 'b-slidetoggle-label';
    } else {
      a.pop();
    }
    return a;
  }
  get toggleElement() {
    return {
      'class': 'b-slidetoggle-toggle',
      'reference': 'slideToggle',
      'children': [{
          'class': 'b-slidetoggle-thumb',
          'reference': 'slideThumb'
        }]
    };
  }
  internalOnChange() {
    super.internalOnChange();
    this.element.classList[this.value ? 'add' : 'remove'](this.toggledCls);
  }
};
SlideToggle.initClass();
SlideToggle._$name = 'SlideToggle';
const classesHV = [
  'b-horizontal',
  'b-vertical'
];
const hasFlex = a => DomHelper.getStyleValue(a.parentElement, 'display') === 'flex' && (parseInt(DomHelper.getStyleValue(a, 'flex-basis'), 10) || parseInt(DomHelper.getStyleValue(a, 'flex-grow'), 10));
const verticality = {
  'horizontal': false,
  'vertical': true
};
const Splitter = class extends Widget {
  static type = 'splitter';
  static $name = 'Splitter';
  static get configurable() {
    return {
      'orientation': 'auto',
      'vertical': null,
      'containerElement': {
        '$config': 'nullify',
        'value': null
      },
      'nextNeighbor': {
        '$config': 'nullify',
        'value': null
      },
      'previousNeighbor': {
        '$config': 'nullify',
        'value': null
      }
    };
  }
  static get delayable() {
    return { 'syncState': 'raf' };
  }
  doDestroy() {
    let a;
    (a = this.mouseDetacher) == null ? void 0 : a.call(this);
    super.doDestroy();
  }
  compose() {
    return {
      'class': { 'b-splitter': 1 },
      'listeners': {
        'pointerdown': 'onMouseDown',
        'mouseenter': 'onMouseEnter',
        'mouseleave': 'onMouseLeave',
        ...!BrowserHelper.supportsPointerEvents && {
          'mousedown': 'onMouseDown',
          'touchstart': 'onMouseDown'
        }
      }
    };
  }
  get currentOrientation() {
    return this.vertical ? 'vertical' : 'horizontal';
  }
  getSibling(a = true) {
    let {element: b} = this, c;
    while (!c && (b = b[(a ? 'next' : 'previous') + 'ElementSibling'])) {
      if (!b.isConnected || DomHelper.isVisible(b)) {
        c = b;
      }
    }
    return c;
  }
  get nextWidget() {
    let {element: a} = this, b;
    while (!b && (a = a.nextElementSibling)) {
      b = Widget.fromElement(a, this.element.parentElement);
    }
    return b;
  }
  get previousWidget() {
    let {element: a} = this, b;
    while (!b && (a = a.previousElementSibling)) {
      b = Widget.fromElement(a, this.element.parentElement);
    }
    return b;
  }
  updateContainerElement(a) {
    let b;
    const c = this;
    c.stateDetector = (b = c.stateDetector) == null ? void 0 : b.disconnect();
    if (a) {
      c.stateDetector = new MutationObserver(() => c.syncState());
      c.stateDetector.observe(a, {
        'attributes': true,
        'childList': true
      });
    }
  }
  updateNextNeighbor(a) {
    this.watchNeighbor(a, 'next');
  }
  updatePreviousNeighbor(a) {
    this.watchNeighbor(a, 'previous');
  }
  watchNeighbor(a, b) {
    this.detachListeners(b);
    a == null ? void 0 : a.ion({
      'name': b,
      'thisObj': this,
      'collapse': 'syncState',
      'expand': 'syncState',
      'hide': 'syncState',
      'show': 'syncState'
    });
  }
  updateOrientation() {
    this.syncState.now();
  }
  updateVertical(a) {
    let b;
    const c = (b = this.element) == null ? void 0 : b.classList;
    c == null ? void 0 : c.add(classesHV[a ? 1 : 0]);
    c == null ? void 0 : c.remove(classesHV[a ? 0 : 1]);
  }
  syncState() {
    let a;
    const b = this, {
        element: c,
        nextWidget: e,
        previousWidget: f
      } = b;
    let g = (a = verticality[b.orientation]) != null ? a : null;
    b.nextNeighbor = e;
    b.previousNeighbor = f;
    b.disabled = e && (e.collapsible && e.collapsed || e.hidden) || f && (f.collapsible && f.collapsed || f.hidden);
    if (g !== null && e && f) {
      b.containerElement = null;
    } else {
      b.containerElement = c.parentElement;
      if (b.rendered && c.offsetParent) {
        const i = DomHelper.getStyleValue(c.parentElement, 'flex-direction');
        if (i) {
          g = !i.startsWith('column');
        } else {
          const j = c.previousElementSibling, k = c.nextElementSibling;
          if (!j || !k) {
            return;
          }
          const l = j.getBoundingClientRect(), n = k.getBoundingClientRect(), o = l.top < n.top ? l : n, p = o === n ? l : n;
          g = o.top === p.top;
        }
      }
    }
    b.vertical = g;
  }
  onMouseDown(a) {
    let b;
    a.preventDefault();
    if (a.touches) {
      a = a.touches[0];
    }
    const c = this, {
        element: e,
        nextNeighbor: f,
        previousNeighbor: g
      } = c, i = g ? g.element : c.getSibling(false), j = f ? f.element : c.getSibling(), k = hasFlex(i), l = hasFlex(j), n = [];
    (b = c.mouseDetacher) == null ? void 0 : b.call(c);
    for (const p of e.parentElement.children) {
      if (hasFlex(p) && p !== e) {
        n.push({
          'element': p,
          'width': p.offsetWidth,
          'height': p.offsetHeight
        });
      }
    }
    c.context = {
      'startX': a.pageX,
      'startY': a.pageY,
      'prevWidth': i.offsetWidth,
      'prevHeight': i.offsetHeight,
      'nextWidth': j.offsetWidth,
      'nextHeight': j.offsetHeight,
      'prevHasFlex': k,
      'nextHasFlex': l,
      'flexed': n,
      'prev': i,
      'next': j
    };
    const o = {
      'element': document,
      'pointermove': 'onMouseMove',
      'pointerup': 'onMouseUp',
      'thisObj': c
    };
    if (!BrowserHelper.supportsPointerEvents) {
      o.mousemove = o.touchmove = 'onMouseMove';
      o.mouseup = o.touchend = 'onMouseUp';
    }
    e.classList.add('b-moving');
    c.mouseDetacher = EventHelper.on(o);
    c.trigger('splitterMouseDown', { 'event': a });
  }
  onMouseEnter(a) {
    this.syncState();
    this.trigger('splitterMouseEnter', { 'event': a });
  }
  onMouseLeave(a) {
    this.trigger('splitterMouseLeave', { 'event': a });
  }
  onMouseMove(a) {
    const b = this, {
        context: c,
        nextWidget: e,
        previousWidget: f
      } = b, g = c.prev.style, i = c.next.style, j = (a.pageX - c.startX) * (b.rtl ? -1 : 1), k = a.pageY - c.startY;
    a.preventDefault();
    Object.assign(c, {
      'deltaX': j,
      'deltaY': k
    });
    if (!c.started) {
      c.started = true;
      b.trigger('dragStart', {
        'context': c,
        'event': a
      });
      c.flexed.forEach(l => {
        l.element.style.flexGrow = b.vertical ? l.width : l.height;
        l.element.style.flexBasis = '0';
      });
    }
    if (b.vertical) {
      const l = c.prevWidth + j, n = c.nextWidth - j;
      if (c.prevHasFlex) {
        g.flexGrow = l;
      } else if (f) {
        f.width = l;
      } else {
        g.width = l + 'px';
      }
      if (c.nextHasFlex) {
        i.flexGrow = n;
      } else if (e) {
        e.width = n;
      } else {
        i.width = n + 'px';
      }
    } else {
      const o = c.prevHeight + k, p = c.nextHeight - k;
      if (c.prevHasFlex) {
        g.flexGrow = o;
      } else if (f) {
        f.height = o;
      } else {
        g.height = o + 'px';
      }
      if (c.nextHasFlex) {
        i.flexGrow = p;
      } else if (e) {
        e.height = p;
      } else {
        i.height = p + 'px';
      }
    }
    b.trigger('drag', {
      'context': c,
      'event': a
    });
  }
  onMouseUp(a) {
    let b;
    const c = this;
    (b = c.mouseDetacher) == null ? void 0 : b.call(c);
    c.mouseDetacher = null;
    c.element.classList.remove('b-moving');
    if (c.context.started) {
      c.trigger('drop', {
        'context': c.context,
        'event': a
      });
    }
    c.context = null;
  }
  render() {
    super.render(...arguments);
    this.syncState.now();
    if (this.vertical === null) {
      this.syncState();
    }
  }
};
Splitter.initClass();
Splitter._$name = 'Splitter';
const Tab = class extends Button {
  static type = 'tab';
  static $name = 'Tab';
  static get configurable() {
    return {
      'active': null,
      'index': null,
      'isFirst': null,
      'isLast': null,
      'item': {
        'value': null,
        '$config': 'nullify'
      },
      'itemCls': null,
      'tabPanel': null,
      'titleProperty': 'text',
      'titleSource': 'title',
      'role': 'tab'
    };
  }
  compose() {
    const {
        active: a,
        cls: b,
        index: c,
        isFirst: e,
        isLast: f
      } = this, g = this.owner.visibleChildCount;
    return {
      'tabindex': 0,
      'aria-selected': a,
      'aria-setsize': g,
      'aria-posinset': c + 1,
      'class': {
        'b-tabpanel-tab': 1,
        'b-active': a,
        'b-tab-first': e,
        'b-tab-last': f,
        ...b
      },
      'dataset': { 'index': c }
    };
  }
  updateIndex(a) {
    this.isFirst = !a;
  }
  updateItem(a, b) {
    let c, e;
    const f = this;
    if ((b == null ? void 0 : b.tab) === f) {
      b.tab = null;
    }
    if (a) {
      a.tab = f;
      f[f.titleProperty] = a[f.titleSource];
      f.itemCls = a.cls;
      f.ariaElement.setAttribute('aria-controls', a.id);
      a.role = 'tabpanel';
    }
    (c = f.itemChangeDetacher) == null ? void 0 : c.call(f);
    f.itemChangeDetacher = a && FunctionHelper.after(a, 'onConfigChange', 'onItemConfigChange', f, { 'return': false });
    (e = f.itemHideDetacher) == null ? void 0 : e.call(f);
    f.itemHideDetacher = a == null ? void 0 : a.ion({
      'beforeChangeHidden': 'onItemBeforeChangeHidden',
      'beforeHide': 'onItemBeforeHide',
      'beforeUpdateDisabled': 'onItemBeforeUpdateDisabled',
      'thisObj': f,
      'prio': 1000
    });
    f.syncMinMax();
  }
  updateItemCls(a, b) {
    const {element: c} = this, e = c && DomClassList.from(c == null ? void 0 : c.classList, true);
    if (c) {
      e.remove(b).add(a);
      c.className = e.value;
    }
  }
  updateRotate(a, b) {
    if (!a !== !b) {
      this.syncMinMax();
    }
  }
  syncMinMax() {
    const a = this, {
        rotate: b,
        tabPanel: c
      } = a;
    let {
      _minWidth: e,
      _minHeight: f,
      _maxWidth: g,
      _maxHeight: i
    } = a;
    if (c) {
      const {
        tabMinWidth: j,
        tabMaxWidth: k
      } = c;
      if (j != null) {
        if (b) {
          if (e === j) {
            e = null;
          }
          f = j;
        } else {
          if (f === j) {
            f = null;
          }
          e = j;
        }
      }
      if (k != null) {
        if (b) {
          if (g === k) {
            g = null;
          }
          i = k;
        } else {
          if (i === k) {
            i = null;
          }
          g = k;
        }
      }
      a.minWidth = e;
      a.minHeight = f;
      a.maxWidth = g;
      a.maxHeight = i;
    }
  }
  onItemBeforeChangeHidden({
    source: a,
    hidden: b
  }) {
    if (!a.$isDeactivating && !a.$isActivating) {
      const {tabPanel: c} = this;
      this.hidden = b;
      if (b && a === c.activeItem) {
        c.activateAvailableTab(a);
      }
    }
  }
  onItemBeforeHide() {
    if (!this.item.$isDeactivating) {
      this.hide();
    }
  }
  onItemBeforeUpdateDisabled({
    source: a,
    disabled: b
  }) {
    const {tabPanel: c} = this;
    this.disabled = b;
    if (a === c.activeItem) {
      c.activateAvailableTab(a);
    }
  }
  onItemConfigChange({
    name: a,
    value: b
  }) {
    if (a === this.titleSource) {
      this[this.titleProperty] = b;
    }
  }
};
Tab.initClass();
Tab._$name = 'Tab';
const isTab = a => a.isTab;
const TabBar = class extends Toolbar {
  static type = 'tabbar';
  static $name = 'TabBar';
  static get configurable() {
    return {
      'defaultType': 'tab',
      'overflow': 'scroll',
      'role': 'tablist',
      'ignoreParentReadOnly': true
    };
  }
  get firstTab() {
    return this.tabAt(0);
  }
  get lastTab() {
    return this.tabAt(-1);
  }
  get tabCount() {
    return this._items.countOf(isTab);
  }
  get tabs() {
    return ArrayHelper.from(this._items, isTab);
  }
  compose() {
    return { 'children': { 'toolbarContent': { 'class': { 'b-tabpanel-tabs': 1 } } } };
  }
  indexOfTab(a) {
    return this._items.indexOf(a, isTab);
  }
  onChildAdd(a) {
    super.onChildAdd(a);
    if (a.index == null) {
      this.syncTabs();
    }
  }
  onChildRemove(a) {
    super.onChildRemove(a);
    this.syncTabs();
  }
  onFocusIn() {
    const {activeIndex: a} = this.owner;
    if (!isNaN(a)) {
      this.tabs[a].focus();
    }
  }
  syncTabs() {
    const {tabs: a} = this;
    for (let b = 0, c = a.length; b < c; ++b) {
      a[b].index = b;
      a[b].isFirst = !b;
      a[b].isLast = b === c - 1;
    }
  }
  tabAt(a) {
    return this._items.find(isTab, a) || null;
  }
};
TabBar.initClass();
TabBar._$name = 'TabBar';
const animationClasses = [
  'b-slide-out-left',
  'b-slide-out-right',
  'b-slide-in-left',
  'b-slide-in-right'
];
const Card = class extends Layout {
  static configurable = {
    'containerCls': 'b-card-container',
    'itemCls': 'b-card-item',
    'hideChildHeaderCls': 'b-hide-child-headers',
    'animateCardChange': true,
    'activeItem': null,
    'activeIndex': null
  };
  static type = 'card';
  static $name = 'Card';
  onChildAdd(a) {
    super.onChildAdd(a);
    const b = this, {
        activeItem: c,
        owner: e
      } = b, f = e.activeIndex != null ? e.activeIndex : b.activeIndex || 0, g = e.items.indexOf(a), i = c != null ? a === c : g === f;
    a.ion({
      'beforeHide': 'onBeforeChildHide',
      'beforeShow': 'onBeforeChildShow',
      'thisObj': b
    });
    if (i) {
      b._activeIndex = g;
      b._activeItem = a;
      a.show();
    } else {
      a.$isDeactivating = true;
      a.hide();
      a.$isDeactivating = false;
    }
  }
  onChildRemove(a) {
    super.onChildRemove(a);
    const b = this;
    if (b._activeItem === a) {
      b.activateSiblingOf(a);
    }
    b._activeIndex = b.owner.items.indexOf(b._activeItem);
    a.un({
      'beforeHide': 'onBeforeChildHide',
      'beforeShow': 'onBeforeChildShow',
      'thisObj': b
    });
  }
  onBeforeChildShow({source: a}) {
    if (!this.owner.isConfiguring && !a.$isActivating) {
      this.activeItem = a;
      return false;
    }
  }
  onBeforeChildHide({source: a}) {
    if (!this.owner.isConfiguring && !a.$isDeactivating) {
      this.activateSiblingOf(a);
      return false;
    }
  }
  activateSiblingOf(a) {
    const {owner: b} = this, c = b.items.slice(), e = c.indexOf(a);
    c.splice(e, 1);
    this.activeIndex = Math.min(e, c.length - 1);
  }
  setActiveItem(a, b = this.activeIndex, c) {
    let e;
    const f = this, {owner: g} = f, {items: i} = g, j = a instanceof Widget, k = i[b], l = g.items[a = j ? i.indexOf(a) : parseInt(a, 10)], n = (c == null ? void 0 : c.animation) !== false, o = !(c == null ? void 0 : c.silent), p = {
        'prevActiveIndex': b,
        'prevActiveItem': k
      };
    if (l && !l.$isActivating && l !== k) {
      const q = k && k.element, r = l && l.element;
      if (f.animateDetacher) {
        const v = f.animateDetacher.event;
        if (v.activeItem === l) {
          return v.promise;
        }
        f.animateDetacher();
        v.prevActiveItem.element.classList.remove(...animationClasses);
        v.activeItem.element.classList.remove(...animationClasses);
        f.animateDetacher = null;
      }
      p.activeIndex = a;
      p.activeItem = l;
      if (o && g.trigger('beforeActiveItemChange', p) === false) {
        return null;
      }
      const u = f._activeIndex !== p.activeIndex;
      if (u) {
        f._activeIndex = p.activeIndex;
      }
      o && ((e = g.onBeginActiveItemChange) == null ? void 0 : e.call(g, p));
      if (u) {
        f._activeIndex = p.prevActiveIndex;
      }
      if (n && q && g.isVisible && f.animateCardChange) {
        p.promise = f.cardChangeAnimation = new Promise((w, z) => {
          const A = k.monitorResize;
          k.monitorResize = false;
          f.contentElement.style.overflowX = 'hidden';
          k._hidden = true;
          l.$isActivating = true;
          l.show();
          l.$isActivating = false;
          q.classList.add(a > b ? 'b-slide-out-left' : 'b-slide-out-right');
          r.classList.add(a < b ? 'b-slide-in-left' : 'b-slide-in-right');
          g.isAnimating = true;
          f.animateDetacher = EventHelper.onTransitionEnd({
            'mode': 'animation',
            'element': r,
            'thisObj': k,
            'handler'() {
              g.isAnimating = f.cardChangeAnimation = false;
              if (!f.animateDetacher) {
                f.setActiveItem(a, b, c);
                return;
              }
              f.animateDetacher = null;
              r.classList.remove(...animationClasses);
              if (q) {
                q.classList.remove(...animationClasses);
                k.$isDeactivating = true;
                k._hidden = false;
                k.hide();
                k.monitorResize = A;
                k.$isDeactivating = false;
              }
              f.contentElement.style.overflowX = '';
              f.onActiveItemChange(p, w, !o);
            }
          });
          f.animateDetacher.reject = z;
          f.animateDetacher.event = p;
        });
      } else {
        l.$isActivating = true;
        l.show();
        if (g.containsFocus) {
          l.focus();
        }
        l.$isActivating = false;
        if (k) {
          k.$isDeactivating = true;
          k.hide();
          k.$isDeactivating = false;
        }
        f.onActiveItemChange(p, null, !o);
      }
    }
    return p;
  }
  onActiveItemChange(a, b, c) {
    const e = this;
    e._activeItem = a.activeItem;
    e._activeIndex = a.activeIndex;
    !c && e.owner.trigger('activeItemChange', a);
    e.owner.containsFocus && a.activeItem.focus();
    b == null ? void 0 : b(a);
  }
  renderChildren() {
    const {owner: a} = this;
    a.contentElement.classList.toggle(this.hideChildHeaderCls, a.suppressChildHeaders);
    super.renderChildren();
  }
  changeActiveIndex(a) {
    const {owner: b} = this;
    return b.isConfiguring && !b._items ? a : Math.min(a, b.items.length - 1);
  }
  updateActiveIndex(a, b) {
    if (!this.owner.isConfiguring) {
      this.setActiveItem(a, b);
    }
  }
  updateActiveItem(a) {
    if (!this.owner.isConfiguring) {
      this.setActiveItem(a, this.activeIndex);
    }
  }
  get isChangingCard() {
    return Boolean(this.animateDetacher);
  }
};
Card.initClass();
Card._$name = 'Card';
const isMaximized = a => a.maximized;
const TabPanel = class extends Panel {
  static alias = 'tabs';
  static type = 'tabpanel';
  static $name = 'TabPanel';
  static get configurable() {
    return {
      'activeTab': 0,
      'animateTabChange': true,
      'autoHeight': false,
      'defaultType': 'container',
      'focusable': false,
      'itemCls': 'b-tabpanel-item',
      'layout': { 'type': 'card' },
      'suppressChildHeaders': true,
      'tabBar': {
        'type': 'tabbar',
        'weight': -2000
      },
      'tabMinWidth': null,
      'tabMaxWidth': null
    };
  }
  get activeIndex() {
    return this.layout.activeIndex;
  }
  get activeItem() {
    return this.layout.activeItem;
  }
  get activeTabItemIndex() {
    let a;
    const {
      activeTab: b,
      items: c,
      tabBar: e
    } = this;
    return c.indexOf((a = e.tabs[b]) == null ? void 0 : a.item);
  }
  get bodyConfig() {
    return ObjectHelper.merge({ 'className': { 'b-tabpanel-body': 1 } }, super.bodyConfig);
  }
  get focusElement() {
    let a;
    const b = this.items[this.activeTab || 0];
    return (b == null ? void 0 : b.focusElement) || ((a = b == null ? void 0 : b.tab) == null ? void 0 : a.focusElement);
  }
  get tabPanelBody() {
    return this.bodyElement;
  }
  finalizeInit() {
    super.finalizeInit();
    const a = this, {
        activeTab: b,
        layout: c
      } = a, {activeIndex: e} = c, {tabs: f} = a.tabBar, g = b >= 0 && b < f.length && a.items.indexOf(f[b].item);
    if (f.length > 0 && (g === false || g < 0)) {
      throw new Error('Invalid activeTab ' + b + ' (' + f.length + ' tabs)');
    }
    if (g !== e) {
      c.setActiveItem(g, e, {
        'animation': false,
        'silent': true
      });
    }
    c.animateCardChange = a.animateTabChange;
  }
  onChildAdd(a) {
    super.onChildAdd(a);
    if (!this.initialItems) {
      const b = this, {tabBar: c} = b, e = b.makeTabConfig(a), f = e && (c == null ? void 0 : c.firstTab), g = f && c._items, i = f && ArrayHelper.from(b._items, k => k.tab || k === a), j = f ? i.indexOf(a) + g.indexOf(f) : 0;
      if (e && c) {
        if (f && a.weight == null && j < g.count - 1) {
          c.insert(e, j);
        } else {
          c.add(e);
        }
      }
    }
  }
  onChildRemove(a) {
    const {tab: b} = a, {items: c} = this;
    if (b) {
      this.tabBar.remove(b);
      b.destroy();
    }
    if (a === this.activeItem) {
      this._activeTab = null;
      if (c.length) {
        this.activeTab = c[Math.min(this.activeIndex, c.length - 1)];
      }
    }
    super.onChildRemove(a);
  }
  isDisabledOrHiddenTab(a) {
    const {tabs: b} = this.tabBar, c = b == null ? void 0 : b[a];
    return c && (c.disabled || c.hidden);
  }
  findAvailableTab(a, b = 1) {
    const {tabs: c} = this.tabBar, e = c.length, f = Math.max(0, c.indexOf(a.tab));
    if (f) {
      b = -b;
    }
    let g;
    for (let i = 1; i <= e; ++i) {
      g = (f + (b < 0 ? e : 0) + i * b) % e;
      if (!this.isDisabledOrHiddenTab(g)) {
        break;
      }
    }
    return g;
  }
  activateAvailableTab(a, b = 1) {
    this.activeTab = this.findAvailableTab(a, b);
  }
  changeActiveTab(a, b) {
    const c = this, {
        tabBar: e,
        layout: f
      } = c, {tabCount: g} = e;
    if (a.isWidget || ObjectHelper.isObject(a)) {
      if (c.items.indexOf(a) === -1) {
        a = c.add(a);
      }
      a = e.indexOfTab(a.tab);
    } else {
      a = parseInt(a, 10);
    }
    if (!c.initialItems && g > 0 && (a < -1 || a >= g)) {
      throw new Error('Invalid activeTab ' + a + ' (' + g + ' tabs)');
    }
    if (c.isDisabledOrHiddenTab(a)) {
      a = c.findAvailableTab(a);
    }
    if (f.animateCardChange && f.cardChangeAnimation) {
      f.cardChangeAnimation.then(i => {
        if ((i == null ? void 0 : i.activeIndex) !== a) {
          c._activeTab = a;
          c.updateActiveTab(a, b);
        }
      });
    } else {
      return a;
    }
  }
  async updateActiveTab(a, b) {
    let c;
    if (!this.initialItems) {
      const {
        activeTabItemIndex: e,
        layout: f
      } = this;
      if (e > -1) {
        const g = this.items[b], i = this.items[e];
        if (f.activeItem !== i) {
          if (f.animateCardChange) {
            await this.tabSelectionPromise;
          }
          if (g == null ? void 0 : g.containsFocus) {
            g.tab.focus();
          }
          this.tabSelectionPromise = (c = f.setActiveItem(i)) == null ? void 0 : c.promise;
        }
      }
    }
  }
  changeTabBar(a) {
    this.getConfig('strips');
    this.strips = { 'tabBar': a };
    return this.strips.tabBar;
  }
  makeTabConfig(a) {
    const {tab: b} = a, c = {
        'item': a,
        'type': 'tab',
        'active': !a.hidden,
        'tabPanel': this,
        'disabled': Boolean(a.disabled),
        'hidden': a.initialConfig.hidden,
        'weight': a.weight || 0,
        'internalListeners': {
          'click': 'onTabClick',
          'thisObj': this
        },
        'localizableProperties': { 'text': false }
      };
    if (b === false) {
      return null;
    }
    return ObjectHelper.isObject(b) ? Tab.mergeConfigs(c, b) : c;
  }
  updateAnimateTabChange(a) {
    if (!this.isConfiguring) {
      this.layout.animateCardChange = a;
    }
  }
  updateItems(a, b) {
    const c = this, {
        activeTab: e,
        initialItems: f
      } = c;
    let g = 0, i;
    super.updateItems(a, b);
    if (f) {
      i = Array.from(a, j => c.makeTabConfig(j)).filter(j => {
        if (j) {
          j.index = g++;
          return true;
        }
      });
      if (g) {
        i[0].isFirst = true;
        i[g - 1].isLast = true;
        i[e].active = true;
        c.tabBar.add(i);
        c.activeTab = e;
      }
    }
  }
  updateTabMinWidth(a) {
    let b;
    (b = this.tabBar) == null ? void 0 : b.items.forEach(c => {
      if (c.isTab) {
        c.minWidth = a;
      }
    });
  }
  updateTabMaxWidth(a) {
    let b;
    (b = this.tabBar) == null ? void 0 : b.items.forEach(c => {
      if (c.isTab) {
        c.maxWidth = a;
      }
    });
  }
  updateAutoHeight(a) {
    this.detachListeners('themeAutoHeight');
    a && GlobalEvents_default.ion({
      'name': 'themeAutoHeight',
      'theme': 'internalOnThemeChange',
      'thisObj': this
    });
  }
  applyAutoHeight() {
    const a = this, {
        layout: b,
        activeTab: c,
        element: e
      } = a, {animateCardChange: f} = b;
    b.animateCardChange = false;
    a.flex = '0 0 0%';
    if (!a.up(isMaximized)) {
      const g = a.height = Math.max(...a.items.map(i => {
        a.activeTab = i;
        return e.clientHeight;
      })) + 1;
      a.flex = '1 1 ' + g + 'px';
    }
    a.activeTab = c;
    b.animateCardChange = f;
  }
  internalOnThemeChange() {
    if (this.isVisible) {
      this.applyAutoHeight();
    }
  }
  onBeginActiveItemChange(a) {
    const b = this.tabBar.tabs, {
        activeItem: c,
        prevActiveItem: e
      } = a;
    this.activeTab = b.indexOf(c == null ? void 0 : c.tab);
    if (e == null ? void 0 : e.tab) {
      e.tab.active = false;
    }
    if (c == null ? void 0 : c.tab) {
      c.tab.active = true;
      c.tab.show();
    }
  }
  onBeforeActiveItemChange(a) {
    return this.trigger('beforeTabChange', a);
  }
  onActiveItemChange(a) {
    this.trigger('tabChange', a);
  }
  onTabClick(a) {
    this.activeTab = a.source.item;
  }
  onInternalPaint() {
    super.onInternalPaint(...arguments);
    if (this.autoHeight) {
      this.applyAutoHeight();
    }
  }
};
TabPanel.initClass();
TabPanel._$name = 'TabPanel';
const TextAreaField = class extends Field {
  static alias = 'textarea';
  static type = 'textareafield';
  static $name = 'TextAreaField';
  static get configurable() {
    return {
      'resize': 'none',
      'inputAttributes': { 'tag': 'textarea' }
    };
  }
  updateResize(a) {
    this.input.style.resize = a;
  }
};
TextAreaField.initClass();
TextAreaField._$name = 'TextAreaField';
const _TextAreaPickerField = class _TextAreaPickerField extends PickerField {
  static configurable = {
    'picker': {
      'type': 'widget',
      'tag': 'textarea',
      'cls': 'b-textareapickerfield-picker',
      'scrollAction': 'realign',
      'align': {
        'align': 't-b',
        'axisLock': true
      },
      'autoShow': false
    },
    'triggers': {
      'expand': {
        'cls': 'b-icon-picker',
        'handler': 'onTriggerClick'
      }
    },
    'resize': 'none',
    'inputType': null
  };
  static type = 'textareapickerfield';
  static $name = 'TextAreaPickerField';
  get inputElement() {
    const a = super.inputElement;
    a.readOnly = 'readonly';
    a.reference = 'displayElement';
    this.ariaElement = 'displayElement';
    return a;
  }
  get focusElement() {
    let a;
    return ((a = this._picker) == null ? void 0 : a.isVisible) ? this.input : this.displayElement;
  }
  get needsInputSync() {
    let a;
    return this.displayElement[this.inputValueAttr] !== String((a = this.inputValue) != null ? a : '');
  }
  showPicker() {
    this.picker.width = this.pickerWidth || this.pickerAlignElement.offsetWidth;
    super.showPicker(true);
  }
  focusPicker() {
    this.input.focus();
  }
  onPickerKeyDown(a) {
    const b = this, c = b.input;
    switch (a.key.trim() || a.code) {
    case 'Escape':
      b.picker.hide();
      return;
    case 'Enter':
      if (a.ctrlKey) {
        b.syncInputFieldValue();
        b.picker.hide();
      }
      break;
    }
    b.input = b.displayElement;
    const e = super.onPickerKeyDown(a);
    b.input = c;
    return e;
  }
  syncInputFieldValue(a) {
    if (this.displayElement) {
      this.displayElement.value = this.inputValue;
    }
    super.syncInputFieldValue(a);
  }
  changeValue(a) {
    return a == null ? '' : a;
  }
  changePicker(a, b) {
    let c;
    const e = this, f = e.pickerWidth || (a == null ? void 0 : a.width);
    a = _TextAreaPickerField.reconfigure(b, a ? Objects.merge({
      'owner': e,
      'forElement': e.pickerAlignElement,
      'align': {
        'matchSize': f == null,
        'anchor': e.overlayAnchor,
        'target': e.pickerAlignElement
      },
      'id': e.id + '-input',
      'style': { 'resize': e.resize },
      'html': (c = e.value) != null ? c : ''
    }, a) : null, e);
    if (a) {
      const g = e.input = a.element;
      e.inputListenerRemover = EventHelper.on({
        'element': g,
        'thisObj': e,
        'focus': 'internalOnInputFocus',
        'change': 'internalOnChange',
        'input': 'internalOnInput',
        'keydown': 'internalOnKeyEvent',
        'keypress': 'internalOnKeyEvent',
        'keyup': 'internalOnKeyEvent'
      });
    }
    return a;
  }
};
const TextAreaPickerField = _TextAreaPickerField;
TextAreaPickerField.initClass();
TextAreaPickerField._$name = 'TextAreaPickerField';
const UndoRedoBase = class extends Container {
  static type = 'undoredobase';
  static $name = 'UndoRedoBase';
  static get configurable() {
    return {
      'project': null,
      'stm': null,
      'text': null,
      'color': null,
      'showZeroActionBadge': null,
      'cls': 'b-undo-controls b-toolbar',
      'layoutStyle': {
        'alignItems': 'stretch',
        'flexFlow': 'row nowrap',
        'overflow': 'visible'
      },
      'items': {
        'undoBtn': {
          'type': 'button',
          'icon': 'b-icon-undo',
          'tooltip': 'L{UndoRedo.UndoLastAction}',
          'onAction': 'up.onUndo'
        },
        'transactionsCombo': {
          'type': 'combo',
          'valueField': 'idx',
          'editable': false,
          'store': {},
          'emptyText': 'L{UndoRedo.NoActions}',
          'onAction': 'up.onTransactionSelected',
          'displayValueRenderer': 'up.transactionsDisplayValueRenderer'
        },
        'redoBtn': {
          'type': 'button',
          'icon': 'b-icon-redo',
          'tooltip': 'L{UndoRedo.RedoLastAction}',
          'onAction': 'up.onRedo'
        }
      },
      'overflowable': true
    };
  }
  afterConstruct() {
    this.updateUndoRedoControls();
  }
  updateStm(a) {
    this.detachListeners('undoredo');
    a == null ? void 0 : a.ion({
      'name': 'undoredo',
      'recordingstop': 'updateUndoRedoControls',
      'restoringstop': 'updateUndoRedoControls',
      'queueReset': 'updateUndoRedoControls',
      'disabled': 'updateUndoRedoControls',
      'thisObj': this
    });
  }
  changeItems(a) {
    const {
      undoBtn: b,
      redoBtn: c
    } = a;
    if (this.color) {
      b && (b.color = this.color);
      c && (c.color = this.color);
    }
    if (this.text) {
      b && (b.text = 'L{UndoRedo.Undo}');
      c && (c.text = 'L{UndoRedo.Redo}');
    }
    return super.changeItems(a);
  }
  updateProject(a) {
    this.stm = a.stm;
  }
  fillUndoRedoCombo() {
    const {transactionsCombo: a} = this.widgetMap;
    a && (a.items = [
      [
        0,
        'Original data'
      ],
      ...this.stm.queue.map((b, c) => [
        c + 1,
        b || 'Transaction ' + (c + 1)
      ])
    ]);
  }
  updateUndoRedoControls() {
    const {
        stm: a,
        showZeroActionBadge: b
      } = this, {
        undoBtn: c,
        redoBtn: e,
        transactionsCombo: f
      } = this.widgetMap;
    c.badge = a.position || (b ? '0' : '');
    e.badge = a.length - a.position || (b ? '0' : '');
    c.disabled = !a.canUndo;
    e.disabled = !a.canRedo;
    this.fillUndoRedoCombo();
    if (f) {
      f.disabled = f.store.count <= 1;
    }
  }
  transactionsDisplayValueRenderer(a, b) {
    let c;
    const e = ((c = this.stm) == null ? void 0 : c.position) || 0;
    return e + ' undo actions / ' + (b.store.count - e - 1) + ' redo actions';
  }
  onUndo() {
    const {
      stm: a,
      project: b
    } = this;
    if (a.canUndo) {
      if (a.revisionsEnabled) {
        b.queue(() => {
          a.undo();
          return b.commitAsync();
        });
      } else {
        a.undo();
      }
    }
  }
  onRedo() {
    const {
      stm: a,
      project: b
    } = this;
    if (a.canRedo) {
      if (a.revisionsEnabled) {
        b.queue(() => {
          a.redo();
          return b.commitAsync();
        });
      } else {
        a.redo();
      }
    }
  }
  onTransactionSelected(a) {
    const {stm: b} = this, c = b.position - a.value;
    if (c > 0) {
      b.canUndo && b.undo(c);
    } else if (c < 0) {
      b.canRedo && b.redo(-c);
    }
  }
};
UndoRedoBase.initClass();
UndoRedoBase._$name = 'UndoRedoBase';
const ns = 'http://www.w3.org/2000/svg';
const typePrio = {
  'bar': 1,
  'outline': 2,
  'text': 3
};
const byOrder = (a, b) => parseInt(a.order, 10) - parseInt(b.order, 10);
const byDatasetOrder = (a, b) => parseInt(a.dataset.order, 10) - parseInt(b.dataset.order, 10);
const getField = a => a.field;
const returnFalse = () => false;
const classesDelimiter = /\s+/;
const Histogram = class extends Widget {
  static properties = { 'refreshSuspended': 0 };
  static $name = 'Histogram';
  static type = 'histogram';
  static get configurable() {
    return {
      'data': null,
      'values': null,
      'series': null,
      'topValue': null,
      'element': {
        'children': [{
            'ns': ns,
            'tag': 'svg',
            'reference': 'svgElement',
            'width': '100%',
            'height': '100%',
            'preserveAspectRatio': 'none',
            'children': [
              {
                'ns': ns,
                'tag': 'g',
                'reference': 'scaledSvgGroup'
              },
              {
                'ns': ns,
                'tag': 'g',
                'reference': 'unscaledSvgGroup'
              }
            ]
          }]
      },
      'omitZeroHeightBars': false,
      'singleTextForAllBars': true,
      'monitorResize': true,
      'getRectClass': void 0,
      'getBarClass'(a, b, c, e) {
        return '';
      },
      'getOutlineClass'(a, b) {
        return '';
      },
      'getOutlineDOMConfig'(a, b, c) {
        return b;
      },
      'getBarTip'(a, b, c, e) {
      },
      'getBarText'(a, b, c) {
        return '';
      },
      'getBarDOMConfig': null,
      'getBarTextDOMConfig'(a, b, c, e) {
        return a;
      },
      'getBarTextTip'(a, b, c, e) {
      }
    };
  }
  construct(a) {
    const b = this;
    super.construct(a);
    b.scheduleRefresh = b.createOnFrame(b.refresh, [], b, true);
    b.refresh();
  }
  set tip(a) {
    let b;
    const c = this;
    (b = c.tip) == null ? void 0 : b.destroy();
    if (a) {
      c._tip = Tooltip.new({
        'owner': c,
        'forElement': c.svgElement,
        'forSelector': 'rect',
        'internalListeners': { 'beforeShow': 'up.onBeforeTipShow' }
      }, a);
    } else {
      c._tip = null;
    }
  }
  onElementResize() {
    super.onElementResize(...arguments);
    const a = this.svgElement.getBoundingClientRect();
    this.scaledSvgGroup.setAttribute('transform', 'scale(' + a.width + ' ' + a.height + ')');
  }
  onBeforeTipShow({source: a}) {
    const b = parseInt(a.activeTarget.dataset.index);
    a.html = a.contentTemplate({
      'histogram': this,
      'index': b
    });
  }
  updateSeries(a) {
    const b = this, c = b._series = {};
    let e = 0, f = 0;
    for (const g in a) {
      if (a[g] !== false) {
        const i = c[g] = Objects.merge({}, a[g]);
        if (!i.field) {
          i.field = g;
        }
        if (!i.type) {
          i.type = 'bar';
        }
        if (!('order' in i)) {
          i.order = typePrio[i.type] * 10 + e;
        }
        if (!('index' in i)) {
          i.index = e;
        }
        if (i.type === 'bar') {
          i.index = f++;
        }
        i.id = g;
        e++;
      }
    }
    if (!b.topValue && b._data) {
      b.topValue = b.getDataTopValue(b._data);
    }
    if (!b.refreshSuspended) {
      b.scheduleRefresh();
    }
  }
  getDataTopValue(a, b) {
    const c = Object.values(b || this.series).map(getField);
    let e = 0, f;
    for (let g = 0, {length: k} = a; g < k; g++) {
      f = a[g];
      for (let l = 0, {length: n} = c; l < n; l++) {
        e = Math.max(e, f[c[l]]);
      }
    }
    return e;
  }
  updateGetRectClass(a) {
    VersionHelper.deprecate('core', '7.0.0', 'getRectClass is deprecated in favor of getBarClass');
    this.getBarClass = a;
  }
  updateData(a) {
    const b = this;
    b._data = a;
    if (!b.topValue && b._data && b._series) {
      b.topValue = b.getDataTopValue(a);
    }
    if (!b.refreshSuspended) {
      b.scheduleRefresh();
    }
  }
  updateTopValue(a) {
    const b = this;
    b._topValue = a;
    if (!a && b._data) {
      b._topValue = b.getDataTopValue(b._data);
    }
    if (!b.refreshSuspended) {
      b.scheduleRefresh();
    }
  }
  scheduleRefresh() {
  }
  suspendRefresh() {
    this.refreshSuspended++;
  }
  resumeRefresh() {
    if (this.refreshSuspended) {
      this.refreshSuspended--;
    }
  }
  refresh(a) {
    const b = this, {
        series: c,
        _tip: e,
        topValue: f,
        singleTextForAllBars: g
      } = b, i = (a == null ? void 0 : a.args) || [], j = [], k = [];
    if (!c) {
      return;
    }
    const l = Object.values(c).sort(byOrder);
    if (g) {
      for (const n of l) {
        const o = b['draw' + StringHelper.capitalize(n.type)](n, ...i);
        if (o) {
          if (Array.isArray(o)) {
            j.push.apply(j, o);
          } else {
            j.push(o);
          }
        }
      }
      k.push(...b.drawText(null, ...i));
    } else {
      for (const p of l) {
        const q = b['draw' + StringHelper.capitalize(p.type)](p, ...i);
        if (q) {
          if (Array.isArray(q)) {
            j.push.apply(j, q);
          } else {
            j.push(q);
          }
        }
        if (p.type === 'bar') {
          k.push(...b.drawText(p, ...i));
        }
      }
      k.sort(byDatasetOrder);
    }
    j.sort(byDatasetOrder);
    DomSync.sync({
      'domConfig': {
        'width': '100%',
        'height': '100%',
        'preserveAspectRatio': 'none',
        'dataset': { 'topValue': f },
        'children': [
          {
            'ns': ns,
            'tag': 'g',
            'reference': 'scaledSvgGroup',
            'children': j
          },
          {
            'ns': ns,
            'tag': 'g',
            'reference': 'unscaledSvgGroup',
            'children': k
          }
        ]
      },
      'configEquality': returnFalse,
      'targetElement': b.svgElement
    });
    if (e && e.isVisible) {
      b.onBeforeTipShow({ 'source': e });
    }
  }
  drawBar(a, ...b) {
    const c = this, {
        topValue: e,
        data: f,
        omitZeroHeightBars: g,
        barStyle: i
      } = c, {
        field: j,
        order: k,
        stretch: l
      } = a, {length: n} = f, o = 1 / n, p = [], q = StringHelper.createId(a.id), r = a.index, u = l ? 1 : void 0;
    let v;
    for (let w = 0, z = 0, {length: A} = f; w < A; w++, z += v) {
      const B = f[w];
      let C = B.rectConfig = {
        'ns': ns,
        'tag': 'rect',
        'dataset': {}
      };
      const E = B[j], F = E ? u || B.height || (E > e ? e : E) / e : 0, G = 1 - F, H = c.callback('getBarTip', c, [
          a,
          C,
          B,
          w,
          ...b
        ]);
      v = B.width || o;
      if (i) {
        C.style = i;
      } else {
        delete C.style;
      }
      Object.assign(C.dataset, {
        'index': w,
        'order': k,
        'series': q
      });
      const I = {
          ['b-series-' + q]: 1,
          ['b-series-index-' + r]: 1
        }, J = c.callback('getBarClass', c, [
          a,
          C,
          B,
          w,
          ...b
        ]);
      if (J) {
        J.split(classesDelimiter).forEach(K => I[K] = 1);
      }
      Object.assign(C, {
        'x': z,
        'y': G,
        'width': v,
        'height': F,
        'class': I
      });
      if (H) {
        C.dataset.btip = H;
      } else {
        delete C.dataset.btip;
      }
      if (c.getBarDOMConfig) {
        C = c.getBarDOMConfig(a, C, B, w, ...b);
      }
      if (C && (C.height || !g)) {
        p.push(C);
      }
    }
    return p;
  }
  changeGetBarDOMConfig(a) {
    return a ? this.bindCallback(a) : null;
  }
  drawOutline(a, ...b) {
    const c = this, {
        topValue: e,
        data: f
      } = c, {
        field: g,
        order: j,
        id: k
      } = a, l = 1 / f.length, n = ['M 0,1'], o = {
        'ns': ns,
        'tag': 'path',
        'dataset': {
          'order': j,
          'id': k
        }
      };
    let p, q = 'M', r = 'L';
    for (let u = 0, v = 0, {length: w} = f; u < w; u++) {
      const z = 1 - f[u][g] / e;
      p = f[u].width || l;
      n.push(q + ' ' + v + ',' + z + ' ' + r + ' ' + (v += p) + ',' + z);
      q = r = '';
    }
    o.class = 'b-series-' + a.id + ' b-series-index-' + a.index + ' ' + c.callback('getOutlineClass', c, [
      a,
      f,
      ...b
    ]);
    o.d = n.join(' ');
    return c.callback('getOutlineDOMConfig', c, [
      a,
      o,
      f,
      ...b
    ]) || [];
  }
  drawText(a, ...b) {
    const c = this, {data: e} = c, f = 1 / e.length, g = '100%', i = [];
    for (let j = 0, k, l = 0, {length: n} = e; j < n; j++, l += k) {
      k = e[j].width || f;
      const o = c.callback('getBarText', c, [
        e[j],
        j,
        a,
        ...b
      ]);
      if (o) {
        const p = c.callback('getBarTextDOMConfig', c, [
          {
            'ns': ns,
            'tag': 'text',
            'className': 'b-bar-legend',
            'html': o,
            'left': l,
            'width': k,
            'x': (l + k / 2) * 100 + '%',
            'y': e[j].y !== void 0 ? e[j].y : g,
            'dataset': {
              'index': j,
              'series': a
            }
          },
          e[j],
          j,
          a,
          ...b
        ]);
        if (p) {
          const q = c.callback('getBarTextTip', c, [
            p,
            e[j],
            j,
            a,
            ...b
          ]);
          if (q) {
            p.dataset.btip = q;
          } else {
            delete p.dataset.btip;
          }
          i.push(p);
        }
      }
    }
    return i;
  }
};
Histogram.initClass();
Histogram._$name = 'Histogram';
const ns2 = 'http://www.w3.org/2000/svg';
const Scale = class extends Widget {
  static type = 'scale';
  static $name = 'Scale';
  static get configurable() {
    return {
      'scalePoints': null,
      'scaleMaxPadding': 0.1,
      'horizontal': false,
      'align': {
        'value': false,
        '$config': { 'merge': 'replace' }
      },
      'element': {
        'children': [{
            'ns': ns2,
            'tag': 'svg',
            'reference': 'svgElement',
            'width': '100%',
            'height': '100%',
            'preserveAspectRatio': 'none',
            'children': [
              {
                'ns': ns2,
                'tag': 'g',
                'reference': 'scaledSvgGroup',
                'children': [{
                    'ns': ns2,
                    'tag': 'path',
                    'reference': 'pathElement'
                  }]
              },
              {
                'ns': ns2,
                'tag': 'g',
                'reference': 'unscaledSvgGroup'
              }
            ]
          }]
      },
      'monitorResize': true
    };
  }
  construct(a) {
    super.construct(a);
    this.scheduleRefresh = this.createOnFrame(this.refresh, [], this, true);
    this.refresh();
  }
  changeAlign(a) {
    if (!a) {
      a = this.horizontal ? 'bottom' : 'right';
    }
    return a;
  }
  updateAlign(a, b) {
    this.element.classList.remove('b-align-' + b);
    this.element.classList.add('b-align-' + a);
  }
  updateHorizontal(a, b) {
    this.element.classList.remove('b-scale-' + (b ? 'horizontal' : 'vertical'));
    this.element.classList.add('b-scale-' + (a ? 'horizontal' : 'vertical'));
  }
  onElementResize() {
    super.onElementResize(...arguments);
    this.scheduleRefresh();
  }
  scheduleRefresh() {
  }
  refresh() {
    let a;
    if (!this.scalePoints) {
      return;
    }
    const b = this, {
        horizontal: c,
        width: e,
        height: f,
        align: g,
        scalePoints: i,
        scaleMaxPadding: j
      } = b, k = (a = i[i.length - 1]) == null ? void 0 : a.value, l = [], n = [];
    const o = 1 / (k + j * k);
    b.scaledSvgGroup.setAttribute('transform', 'scale(' + (c ? e : 1) + ' ' + (c ? 1 : f) + ')');
    for (const p of i) {
      const q = Boolean(p.text), r = o * p.value;
      if (q) {
        const u = {
          'ns': ns2,
          'tag': 'text',
          'className': 'b-scale-tick-label',
          'html': p.text,
          'dataset': { 'tick': p.value }
        };
        if (c) {
          u.x = r * 100 + '%';
          u.y = g === 'top' ? '1.6em' : f - 12;
        } else {
          u.x = g === 'left' ? '12' : '' + (e - 12);
          u.y = (1 - r) * 100 + '%';
        }
        n.push(u);
      }
      if (c) {
        if (g === 'top') {
          l.push('M' + r + ',0 L' + r + ',' + (q ? 10 : 5));
        } else {
          l.push('M' + r + ',' + f + ' L' + r + ',' + (f - (q ? 10 : 5)));
        }
      } else {
        if (g === 'left') {
          l.push('M0,' + (1 - r) + ' L' + (q ? 10 : 5) + ',' + (1 - r));
        } else {
          l.push('M' + e + ',' + (1 - r) + ' L' + (e - (q ? 10 : 5)) + ',' + (1 - r));
        }
      }
    }
    b.pathElement.setAttribute('d', l.join(''));
    DomSync.syncChildren({ 'domConfig': { 'children': n } }, b.unscaledSvgGroup);
  }
};
Scale.initClass();
Scale._$name = 'Scale';
const Minifiable_default = a => {
  let b;
  return b = class extends (a || Base) {
    compose() {
      const {minified: c} = this;
      return { 'class': { 'b-minified': c } };
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'Minifiable'), __publicField(b, 'configurable', {
    'minifiable': true,
    'minified': null
  }), b;
};
const EMPTY2 = [];
const isStateName = a => a[0] !== '*';
const pop = (a, b) => {
  const c = a[b] || null;
  delete a[b];
  return c;
};
const responsiveRootFn = a => a.responsiveRoot;
const scoring = { 'number': a => ({width: b}) => b <= a && a };
const splitConfigs = a => {
  delete a.once;
  return {
    'configs': a,
    'callback': pop(a, 'callback'),
    'when': pop(a, 'when')
  };
};
const splitMergedConfigs = (a, b, ...c) => {
  const e = c.flatMap(i => (i == null ? void 0 : i.once) || EMPTY2), f = b.mergeConfigs(...c), g = splitConfigs(f);
  g.once = e.length ? splitConfigs(b.mergeConfigs(...e)) : null;
  for (const i in f) {
    if (i in a) {
      delete f[i];
    }
  }
  return g;
};
const wrapWidget = (a, b) => {
  let c, e = Proxy.revocable(a, {
      'get'(i, j) {
        if (c) {
          c[j] = true;
        }
        return a[j];
      }
    }), f = FunctionHelper.after(a, 'onConfigChange', (i, {name: j}) => {
      if (c == null ? void 0 : c[j]) {
        b();
      }
    }), g = a.ion({
      'resize': () => {
        b();
      }
    });
  a.monitorResize = true;
  return {
    'widget': a,
    get 'object'() {
      return e == null ? void 0 : e.proxy;
    },
    'destroy'() {
      if (e) {
        e.revoke();
        f();
        g();
        e = f = g = null;
      }
    },
    'reset'() {
      c = Object.create(null);
    }
  };
};
const Responsive_default = a => {
  let b;
  return b = class extends (a || Base).mixin(Delayable_default, Fencible_default) {
    get isResponsivePending() {
      return this.responsiveUpdateCount === 0 && this.hasConfig('responsive');
    }
    get isResponsiveUpdating() {
      let c;
      return this._responsiveUpdating || ((c = this.responsiveWidget) == null ? void 0 : c._responsiveUpdating);
    }
    updateResponsive(c) {
      let e;
      const f = this, g = f.constructor, {
          initialConfig: i,
          responsiveDefaults: j
        } = f, k = Array.from(new Set(ObjectHelper.keys(c).concat(ObjectHelper.keys(j)))).filter(isStateName);
      let l = null, n, o, p, q;
      if (c) {
        l = { '*': splitMergedConfigs(i, g, j['*'], c['*']) };
        for (o of k) {
          p = c[o];
          if (p !== null && p !== false) {
            n = n || p && 'when' in p;
            l[o] = splitMergedConfigs(i, g, j['*'], j[o], c['*'], p);
            q = l[o].when;
            l[o].when = ((e = scoring[typeof q]) == null ? void 0 : e.call(scoring, q)) || q;
          }
        }
      }
      f.$responsiveStates = l;
      f.$responsiveWhen = n;
      f.syncResponsiveWidget();
    }
    updateResponsiveState(c, e) {
      let f, g, i, j;
      const k = this, {$responsiveStates: l} = k, n = ++k.responsiveStateChanges === 1, o = (f = k.element) == null ? void 0 : f.classList, p = l['*'], q = l[c] || p, r = n && (q.once || p.once), u = n && k.isStateful, v = k.responsiveWidget;
      let w = q.configs, z = r == null ? void 0 : r.configs, A;
      if (z) {
        w = w ? k.constructor.mergeConfigs(w, z) : z;
      }
      e && (o == null ? void 0 : o.remove('b-responsive-' + e.toLowerCase()));
      c && (o == null ? void 0 : o.add('b-responsive-' + c.toLowerCase()));
      if (u) {
        z = k.loadStatefulData();
        if (z) {
          w = w ? k.constructor.mergeConfigs(w, z) : z;
        }
        k.suspendStateful();
      }
      k._responsiveUpdating = true;
      try {
        (g = k.trigger) == null ? void 0 : g.call(k, 'beforeResponsiveStateChange', {
          'state': c,
          'oldState': e,
          'target': v
        });
        w && k.setConfig(w);
        for (A of [
            q.callback,
            r == null ? void 0 : r.callback
          ]) {
          (i = typeof A === 'string' ? k[A] : A) == null ? void 0 : i.call(k, {
            'source': k,
            'state': c,
            'oldState': e,
            'target': v,
            'initial': n
          });
        }
        (j = k.trigger) == null ? void 0 : j.call(k, 'responsiveStateChange', {
          'state': c,
          'oldState': e,
          'target': v
        });
      } finally {
        k._responsiveUpdating = false;
        u && k.resumeStateful();
      }
    }
    get responsiveTarget() {
      return this.responsiveWidget || this._responsiveTarget;
    }
    updateResponsiveTarget() {
      this.syncResponsiveWidget();
    }
    updateResponsiveWidget(c) {
      let e;
      const f = this, g = c && f.responsiveUpdate;
      (e = f.$responsiveWrapper) == null ? void 0 : e.destroy();
      f.$responsiveWrapper = c && wrapWidget(c, g);
      g == null ? void 0 : g.now();
    }
    responsiveUpdate() {
      const c = this, {
          $responsiveStates: e,
          $responsiveWrapper: f
        } = c, g = f == null ? void 0 : f.widget;
      if (e && f) {
        let i = null, j = 0, k = e, l, n;
        if (g && g !== c && !c.$responsiveWhen) {
          g.getConfig('responsive');
          k = g.$responsiveStates || k;
        }
        f.reset();
        for (n in e) {
          if (n !== '*') {
            l = k[n].when(f.object, BrowserHelper);
            if (l != null && l !== false && (!i || l < j)) {
              i = n;
              j = l;
            }
          }
        }
        ++c.responsiveUpdateCount;
        c.responsiveState = i;
      }
    }
    syncResponsiveWidget() {
      let c;
      const e = this;
      let f = null, g;
      if (!e.isDestroying && e.responsive) {
        g = e.responsiveTarget;
        if (!(f = g)) {
          f = !e.responsiveRoot && ((c = e.up) == null ? void 0 : c.call(e, responsiveRootFn)) || e;
        } else if (typeof g === 'string') {
          f = g === '@' ? e : g[0] === '@' ? e[g.substring(1)] : e.up(g);
          if (!f) {
            throw new Error('No match for responsiveTarget="' + g + '"');
          }
        }
        if (!f.isWidget) {
          throw new Error(f.constructor.$$name + ' is not a widget and cannot be a responsiveTarget');
        }
      }
      e.responsiveWidget = f;
      return f;
    }
  }, __publicField(b, '$name', 'Responsive'), __publicField(b, 'configurable', {
    'responsive': {
      '$config': { 'lazy': 'paint' },
      'value': null
    },
    'responsiveDefaults': {
      'small': { 'when': 400 },
      'medium': { 'when': 800 },
      'large': { 'when': () => Infinity },
      '*': {}
    },
    'responsiveRoot': null,
    'responsiveState': null,
    'responsiveTarget': {
      'value': null,
      '$config': { 'lazy': 'paint' }
    },
    'responsiveWidget': {
      'value': null,
      '$config': { 'nullify': true }
    }
  }), __publicField(b, 'delayable', { 'responsiveUpdate': 'raf' }), __publicField(b, 'fenced', { 'syncResponsiveWidget': true }), __publicField(b, 'prototypeProperties', {
    'responsiveStateChanges': 0,
    'responsiveUpdateCount': 0
  }), b;
};
const Styleable_default = a => {
  let b;
  return b = class extends (a || Base) {
    changeCssVarPrefix(c) {
      ObjectHelper.assertString(c, 'prefix');
      if (c && !c.endsWith('-')) {
        c = c + '-';
      }
      return c || '';
    }
    changeCss(c) {
      ObjectHelper.assertObject(c, 'css');
      const e = this;
      if (!globalThis.Proxy) {
        throw new Error('Proxy not supported');
      }
      const f = new Proxy({}, {
        'get'(g, i) {
          let j;
          const k = getComputedStyle(e.element || document.documentElement);
          return (j = k.getPropertyValue('--' + e.cssVarPrefix + StringHelper.hyphenate(i))) == null ? void 0 : j.trim();
        },
        'set'(g, i, j) {
          const k = e.element || document.documentElement;
          k.style.setProperty('--' + e.cssVarPrefix + StringHelper.hyphenate(i), j);
          return true;
        }
      });
      if (c) {
        if (e._element) {
          ObjectHelper.assign(f, c);
        } else {
          e.$initialCSS = c;
        }
      }
      return f;
    }
    updateElement(c, ...e) {
      super.updateElement(c, ...e);
      if (this.$initialCSS) {
        ObjectHelper.assign(this.css, this.$initialCSS);
      }
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'Styleable'), __publicField(b, 'configurable', {
    'cssVarPrefix': '',
    'css': {}
  }), b;
};
const _AvatarRendering = class _AvatarRendering extends Base {
  static $name = 'AvatarRendering';
  static get configurable() {
    return {
      'element': null,
      'colorPrefix': 'b-sch-',
      'tooltip': null,
      'size': null
    };
  }
  doDestroy() {
    let a;
    (a = this.tooltip) == null ? void 0 : a.destroy();
    super.doDestroy();
  }
  updateElement(a) {
    EventHelper.on({
      'element': a,
      'delegate': '.b-resource-image',
      'error': 'onImageErrorEvent',
      'thisObj': this,
      'capture': true
    });
  }
  changeTooltip(a) {
    return Tooltip.new({
      'forElement': this.element,
      'forSelector': '.b-resource-avatar',
      'cls': 'b-resource-avatar-tooltip'
    }, a);
  }
  static get failedUrls() {
    return this._failedUrls || (this._failedUrls = new Set());
  }
  getResourceAvatar(a) {
    if (Array.isArray(a)) {
      return a.map(n => this.getResourceAvatar(n));
    }
    const {
        initials: b,
        color: c,
        iconCls: e,
        imageUrl: f,
        defaultImageUrl: g,
        dataset: dataset = {},
        resourceRecord: i,
        alt: j = StringHelper.encodeHtml(i == null ? void 0 : i.name)
      } = a, k = this.getImageConfig(b, c, f, g, dataset, j) || this.getIconConfig(e, dataset, j) || this.getResourceInitialsConfig(b, c, dataset, j), {size: l} = this;
    Object.assign(k.style, {
      ...l ? {
        'height': l,
        'width': l
      } : void 0
    });
    return k;
  }
  getImageConfig(a, b, c, e, f, g) {
    c = _AvatarRendering.failedUrls.has(c) ? e : c || e;
    if (c) {
      return {
        'tag': 'img',
        'draggable': 'false',
        'loading': 'lazy',
        'class': {
          'b-resource-avatar': 1,
          'b-resource-image': 1
        },
        'style': {},
        'alt': g,
        'aria-label': g,
        'elementData': {
          'defaultImageUrl': e,
          'imageUrl': c,
          'initials': a,
          'color': b,
          'dataset': f
        },
        'src': c,
        'dataset': f
      };
    }
  }
  getIconConfig(a, b, c) {
    if (a) {
      return a && {
        'tag': 'i',
        'style': {},
        'aria-label': c,
        'class': {
          'b-resource-avatar': 1,
          'b-resource-icon': 1,
          [a]: 1
        },
        'dataset': b
      };
    }
  }
  getResourceInitialsConfig(a, b, c, e) {
    const f = DomHelper.isNamedColor(b) && b, g = !f && b, {size: i} = this;
    return {
      'tag': 'div',
      'aria-label': e,
      'class': {
        'b-resource-avatar': 1,
        'b-resource-initials': 1,
        ['' + this.colorPrefix + f]: f
      },
      'style': {
        'backgroundColor': g || null,
        ...i ? {
          'height': i,
          'width': i
        } : void 0
      },
      'text': a,
      'dataset': c
    };
  }
  onImageErrorEvent({target: a}) {
    if (!a.matches('.b-resource-avatar') || !a.isConnected) {
      return;
    }
    const {
      defaultImageUrl: b,
      initials: c,
      color: e,
      imageUrl: f,
      dataset: g
    } = a.elementData;
    if (b && !a.src.endsWith(b.replace(/^[./]*/gm, ''))) {
      a.src = b;
    } else {
      const {parentElement: i} = a, j = Array.from(i.childNodes).indexOf(a), k = this.getResourceInitialsConfig(c, e, g, a.alt), l = DomHelper.createElement(k);
      l.elementData = a.elementData;
      l.lastDomConfig = k;
      i.lastDomConfig && (i.lastDomConfig.children[j] = k);
      i.replaceChild(l, a);
    }
    _AvatarRendering.failedUrls.add(f);
  }
};
const AvatarRendering = _AvatarRendering;
AvatarRendering._$name = 'AvatarRendering';
if (window && !window.bryntum) {
  window.bryntum = {};
}
const _GridLocation = class _GridLocation {
  static NEXT_PAGE = 10;
  static PREV_PAGE = 9;
  static LAST_CELL = 8;
  static FIRST_CELL = 7;
  static LAST_COLUMN = 6;
  static FIRST_COLUMN = 5;
  static PREV_CELL = 4;
  static DOWN = 3;
  static NEXT_CELL = 2;
  static UP = 1;
  constructor(a) {
    if (a) {
      if (a.isLocation) {
        return a;
      }
      if (a.nodeType === Node.ELEMENT_NODE) {
        const b = Widget.fromElement(a, 'gridbase'), c = b && a.closest(b.focusableSelector);
        if (c) {
          const {dataset: e} = c.parentNode;
          this.init({
            'grid': b,
            'rowIndex': b.store.includes(e.id) ? b.store.indexOf(e.id) : e.index || -1,
            'columnId': c.dataset.columnId
          });
          this.initialTarget = a;
        }
      } else {
        this.init(a);
      }
    }
  }
  init(a) {
    let b, c;
    const e = this;
    const f = e.grid = a.grid, {
        store: g,
        columns: i
      } = f, {visibleColumns: j} = i;
    if (a.target) {
      e.actionTargets = [e._target = a.target];
    }
    if (a.record) {
      e._id = a.record.id;
    } else if ('id' in a) {
      e._id = a.id;
      if (a.id == null) {
        e._rowIndex = -1;
      }
    } else {
      const k = !isNaN(a.row) ? a.row : !isNaN(a.rowIndex) ? a.rowIndex : NaN;
      e._rowIndex = Math.max(Math.min(Number(k), g.count - 1), f.hideHeaders ? 0 : -1);
      e._id = (b = g.records[e._rowIndex]) == null ? void 0 : b.id;
    }
    if (!('_rowIndex' in e)) {
      e._rowIndex = g.indexOf(e.id);
    }
    e.isSpecialRow = (c = e.record) == null ? void 0 : c.isSpecialRow;
    if ('columnId' in a) {
      e._column = i.getById(a.columnId);
    } else if ('field' in a) {
      e._column = i.get(a.field);
    } else {
      const l = !isNaN(a.column) ? a.column : !isNaN(a.columnIndex) ? a.columnIndex : NaN;
      if (!isNaN(l)) {
        e._columnIndex = Math.min(Number(l), j.length - 1);
        e._column = j[e._columnIndex];
      } else {
        e._column = 'column' in a ? isNaN(a.column) ? a.column : j[a.column] : j[0];
      }
    }
    if (!('_columnIndex' in e)) {
      e._columnIndex = j.indexOf(e._column);
    }
  }
  get isLocation() {
    return true;
  }
  equals(a, b = false) {
    const c = this;
    return (a == null ? void 0 : a.isLocation) && a.grid === c.grid && (b ? c.id === a.id && c._column === a._column : a.record === c.record && a.column === c.column && a.target === c.target);
  }
  get rowIndex() {
    const {_id: a} = this, {store: b} = this.grid;
    return b.includes(a) ? b.indexOf(a) : Math.min(this._rowIndex, b.count - 1);
  }
  get visibleRowIndex() {
    const {rowManager: a} = this.grid, {rowIndex: b} = this;
    return b === -1 ? b : Math.max(Math.min(b, a.lastFullyVisibleTow.dataIndex), a.firstFullyVisibleTow.dataIndex);
  }
  get isSelectable() {
    return this.grid.store.includes(this._id) && !this.isSpecialRow;
  }
  get record() {
    let a;
    if ((a = this._record) == null ? void 0 : a.isModel) {
      return this._record;
    }
    if (this._rowIndex > -1) {
      const {store: b} = this.grid;
      if (!b.includes(this._id)) {
        return b.records[this._rowIndex];
      }
      return b.getById(this._id);
    }
  }
  get id() {
    return this._id;
  }
  get column() {
    const {visibleColumns: a} = this.grid.columns;
    if (!(a == null ? void 0 : a.includes(this._column))) {
      return a == null ? void 0 : a[this.columnIndex];
    }
    return this._column;
  }
  get columnId() {
    let a;
    return (a = this.column) == null ? void 0 : a.id;
  }
  get columnIndex() {
    let a;
    return Math.min(this._columnIndex, ((a = this.grid.columns.visibleColumns) == null ? void 0 : a.length) - 1);
  }
  move(a) {
    const b = this, {
        record: c,
        column: e,
        grid: f
      } = b, {store: g} = f, i = f.columns.visibleColumns, j = new _GridLocation();
    let k = g.includes(c) ? g.indexOf(c) : b.rowIndex, l = i.includes(e) ? i.indexOf(e) : b.columnIndex;
    const n = f.hideHeaders ? 0 : -1, o = g.count - 1, p = i.length - 1, q = k === n, r = k === o, u = l === 0, v = l === p;
    switch (a) {
    case _GridLocation.PREV_CELL:
      if (u) {
        if (!q) {
          l = p;
          k--;
        }
      } else {
        l--;
      }
      break;
    case _GridLocation.NEXT_CELL:
      if (v) {
        if (!r) {
          l = 0;
          k++;
        }
      } else {
        l++;
      }
      break;
    case _GridLocation.UP:
      if (!q) {
        k--;
      }
      break;
    case _GridLocation.DOWN:
      if (!r) {
        if (k === -1) {
          k = f.rowManager.firstFullyVisibleRow.dataIndex;
        } else {
          k++;
        }
      }
      break;
    case _GridLocation.FIRST_COLUMN:
      l = 0;
      break;
    case _GridLocation.LAST_COLUMN:
      l = p;
      break;
    case _GridLocation.FIRST_CELL:
      k = n;
      l = 0;
      break;
    case _GridLocation.LAST_CELL:
      k = o;
      l = p;
      break;
    case _GridLocation.PREV_PAGE:
      k = Math.max(n, k - Math.floor(f.scrollable.clientHeight / f.rowHeight));
      break;
    case _GridLocation.NEXT_PAGE:
      k = Math.min(o, k + Math.floor(f.scrollable.clientHeight / f.rowHeight));
      break;
    }
    j.init({
      'grid': f,
      'rowIndex': k,
      'columnIndex': l
    });
    return j;
  }
  get cell() {
    let a, b;
    const c = this, {
        grid: e,
        id: f,
        _cell: g
      } = c;
    if (g) {
      return g;
    }
    if (f == null) {
      return (a = e.columns.getById(c.columnId)) == null ? void 0 : a.element;
    } else {
      const {row: i} = c;
      if (i) {
        return i.getCell(c.columnId) || i.getCell((b = e.columns.getAt(c.columnIndex)) == null ? void 0 : b.id);
      }
    }
  }
  get row() {
    return this.grid.getRowById(this.id) || this.grid.getRow(this.rowIndex);
  }
  get target() {
    const {
        cell: a,
        _target: b
      } = this, {focusableFinder: c} = this.grid;
    if (a) {
      if (b) {
        return b;
      }
      c.currentNode = this.grid.focusableFinderCell = a;
      return c.nextNode() || a;
    }
  }
  get isActionable() {
    const {
        cell: a,
        _target: b
      } = this, c = a && DomHelper.getActiveElement(a), e = c && a.compareDocumentPosition(c) & Node.DOCUMENT_POSITION_CONTAINED_BY;
    return Boolean(e || b && b !== this.cell);
  }
  get isColumnHeader() {
    return this.cell && this.rowIndex === -1;
  }
  get isCell() {
    return this.cell && this.record;
  }
};
const GridLocation = _GridLocation;
GridLocation._$name = 'GridLocation';
const validWidth = a => typeof a === 'number' || (a == null ? void 0 : a.endsWith('px'));
const arrowFnRegex = /=>\s*['"]/;
const _Column = class _Column extends Model.mixin(Events_default, Localizable_default) {
  static autoExposeFields = false;
  static fields = [
    'type',
    'text',
    'field',
    'renderer',
    'afterRenderCell',
    'width',
    'flex',
    'autoWidth',
    'autoHeight',
    {
      'name': 'fitMode',
      'defaultValue': 'exact'
    },
    'readOnly',
    {
      'name': 'editor',
      'defaultValue': {}
    },
    'cellEditor',
    'finalizeCellEdit',
    {
      'name': 'managedCellEditing',
      'defaultValue': true
    },
    {
      'name': 'revertOnEscape',
      'defaultValue': true
    },
    {
      'name': 'invalidAction',
      'defaultValue': 'block'
    },
    {
      'name': 'sortable',
      'defaultValue': true,
      'convert'(a, b, c) {
        if (!a) {
          return false;
        }
        if (a === true) {
          return true;
        }
        const e = {};
        if (typeof a === 'function') {
          e.originalSortFn = a;
          e.sortFn = a.bind(c);
        } else if (typeof a === 'object') {
          Object.assign(e, a);
          if (e.fn) {
            e.sortFn = e.fn;
            delete e.fn;
          }
        }
        return e;
      }
    },
    {
      'name': 'searchable',
      'defaultValue': true
    },
    {
      'name': 'collapsible',
      'defaultValue': false
    },
    {
      'name': 'collapsed',
      'defaultValue': false
    },
    { 'name': 'collapseMode' },
    {
      'name': 'filterable',
      'defaultValue': true,
      'convert'(a) {
        if (!a) {
          return false;
        }
        if (a === true) {
          return true;
        }
        const b = { 'columnOwned': true };
        if (typeof a === 'function') {
          b.filterFn = a;
        } else if (typeof a === 'object') {
          Object.assign(b, a);
        }
        return b;
      }
    },
    { 'name': 'sealed' },
    {
      'name': 'hideable',
      'defaultValue': true
    },
    {
      'name': 'draggable',
      'defaultValue': true
    },
    {
      'name': 'groupable',
      'defaultValue': true
    },
    {
      'name': 'resizable',
      'defaultValue': true
    },
    'groupRenderer',
    'headerRenderer',
    'tooltip',
    'tooltipRenderer',
    'cellCls',
    'cls',
    'icon',
    'align',
    {
      'name': 'minWidth',
      'defaultValue': 60
    },
    'maxWidth',
    {
      'name': 'hidden',
      'defaultValue': false
    },
    { 'name': 'locked' },
    { 'name': 'region' },
    {
      'name': 'mergeCells',
      'type': 'boolean'
    },
    {
      'name': 'mergeable',
      'type': 'boolean',
      'defaultValue': true
    },
    'mergedRenderer',
    {
      'name': 'showColumnPicker',
      'defaultValue': true
    },
    {
      'name': 'enableHeaderContextMenu',
      'defaultValue': true
    },
    {
      'name': 'enableCellContextMenu',
      'defaultValue': true
    },
    'headerMenuItems',
    'cellMenuItems',
    'sum',
    'summaries',
    'summaryRenderer',
    'responsiveLevels',
    'tags',
    'touchConfig',
    'tree',
    'filterType',
    {
      'name': 'htmlEncode',
      'defaultValue': true
    },
    {
      'name': 'htmlEncodeHeaderText',
      'defaultValue': true
    },
    {
      'name': 'autoSyncHtml',
      'defaultValue': false
    },
    {
      'name': 'alwaysClearCell',
      'defaultValue': true
    },
    { 'name': 'headerWidgets' },
    {
      'name': 'instantUpdate',
      'defaultValue': false
    },
    {
      'name': 'repaintOnResize',
      'defaultValue': false
    },
    'editTargetSelector',
    {
      'name': 'exportable',
      'defaultValue': true
    },
    { 'name': 'exportedType' },
    {
      'name': 'ariaLabel',
      'defaultValue': 'L{Column.columnLabel}'
    },
    {
      'name': 'cellAriaLabel',
      'defaultValue': 'L{cellLabel}'
    }
  ];
  static type = 'column';
  static $name = 'Column';
  construct(a, b, c, ...e) {
    const f = this, g = c == null ? void 0 : c.isRoot;
    if (!g) {
      f.masterStore = b;
      if (b) {
        f._grid = Array.isArray(b) ? b[0].grid : b.grid;
      }
      f.localizableProperties = Config.mergeMethods.distinct(a.localizableProperties, [
        'text',
        'ariaLabel',
        'cellAriaLabel'
      ]);
      if (a.localeClass) {
        f.localeClass = a.localeClass;
      }
    }
    super.construct(a, b, c, ...e);
    if (!g) {
      if (f.isLeaf && !('field' in f.data)) {
        f.setData('field', '_' + (f.type || '') + ++_Column.emptyCount);
        f.noFieldSpecified = true;
      }
      if (!f.width && !f.flex && !f.children) {
        f.setData({
          'width': _Column.defaultWidth,
          'flex': null
        }, null, true);
      }
      f.headerWidgets && f.initHeaderWidgets(f.headerWidgets);
      if (f.isParent) {
        f.meta.visibleChildren = new Set();
        if (f.collapsible) {
          f.grid.once('paint', () => f.collapsible = true);
        }
      }
      if (f.renderer) {
        const i = f.renderer.toString();
        f.rendererReturningContent = i.includes('return ') || Boolean(i.match(arrowFnRegex));
      }
    }
  }
  shouldSync(a) {
    return a.isColumn && a.text === this.text && (a.field === this.field || String(a.renderer) === String(this.renderer)) && (!a.previousSibling && !this.previousSibling || a.previousSibling.shouldSync(this.previousSibling));
  }
  get isCollapsible() {
    let a;
    return ((a = this.children) == null ? void 0 : a.length) > 1 && this.collapsible;
  }
  get collapsed() {
    return this.get('collapsed');
  }
  set collapsed(a) {
    this.set('collapsed', a, true);
    this.onCollapseChange(!a);
    this.grid.trigger('columnCollapseToggle', {
      'column': this,
      'collapsed': a
    });
  }
  onCellFocus(a) {
    this.location = a;
    if (!this._headerArialLabelSet) {
      this.updateHeaderAriaLabel(this.localizeProperty('ariaLabel'));
      this._headerArialLabelSet = true;
    }
    if (a.rowIndex !== -1) {
      this.updateCellAriaLabel(this.localizeProperty('cellAriaLabel'));
    }
  }
  updateHeaderAriaLabel(a) {
    DomHelper.setAttributes(this.element, { 'aria-label': a });
  }
  updateCellAriaLabel(a) {
    let b, c;
    if (!((b = this.location) == null ? void 0 : b.isSpecialRow) && ((c = this.location) == null ? void 0 : c.cell)) {
      if (!(a == null ? void 0 : a.length)) {
        a = this.location.column.text;
      }
      DomHelper.setAttributes(this.location.cell, { 'aria-label': a });
    }
  }
  doDestroy() {
    let a, b, c;
    (c = (b = (a = this.data) == null ? void 0 : a.editor) == null ? void 0 : b.destroy) == null ? void 0 : c.call(b);
    this.destroyHeaderWidgets();
    super.doDestroy();
  }
  set headerWidgets(a) {
    this.initHeaderWidgets(a);
    this.set('headerWidgets', a);
  }
  get headerWidgets() {
    return this.get('headerWidgets');
  }
  initHeaderWidgets(a) {
    this.destroyHeaderWidgets();
    const b = this.headerWidgetMap = {};
    for (const c of a) {
      const e = Widget.create({
        'owner': this,
        ...c
      });
      e.element.classList.add('b-grid-headerwidget');
      b[e.ref || e.id] = e;
    }
  }
  destroyHeaderWidgets() {
    let a;
    for (const b of Object.values(this.headerWidgetMap || {})) {
      (a = b.destroy) == null ? void 0 : a.call(b);
    }
  }
  static generateAutoCls() {
    const a = [];
    for (let e = this; e !== _Column; e = e.superclass) {
      e.type && e.type !== e.superclass.type && a.push('b-' + e.type.toLowerCase() + '-cell');
    }
    const b = a.join(' ');
    (_Column.autoClsMap || (_Column.autoClsMap = new Map())).set(this, b);
    return b;
  }
  getCellClass(a) {
    let b;
    const {
        record: c,
        column: e
      } = a, {
        cellCls: f,
        internalCellCls: g,
        grid: i,
        constructor: j,
        align: k,
        field: l
      } = e, n = c.fieldMap[l], o = ((b = _Column.autoClsMap) == null ? void 0 : b.get(j)) || j.generateAutoCls(), p = a.cell.classList.contains('b-editing'), q = {
        [i.cellCls]: i.cellCls,
        [o]: o,
        [f]: f,
        [g]: g,
        'b-cell-dirty': l && !(n == null ? void 0 : n.calculated) && c.isFieldModified(l) && (e.compositeField || (n == null ? void 0 : n.persist) !== false),
        ['b-grid-cell-align-' + k]: k,
        'b-selected': i.selectionMode.cell && i.isCellSelected(a),
        'b-focused': i.isFocused(a),
        'b-auto-height': e.autoHeight,
        'b-editing': p
      };
    if (c.isSpecialRow && q['b-checkbox-selection']) {
      q['b-checkbox-selection'] = false;
    }
    return q;
  }
  get locked() {
    return this.data.region === 'locked';
  }
  set locked(a) {
    this.region = a ? 'locked' : 'normal';
  }
  get region() {
    if (!this.parent.isRoot) {
      return this.parent.region;
    }
    return this.get('region');
  }
  set region(a) {
    this.set('region', a);
  }
  get sortable() {
    const {sortable: a} = this.data;
    if (!a || (this.noFieldSpecified || !this.field) && !a.sortFn) {
      return false;
    }
    return a;
  }
  set sortable(a) {
    this.set('sortable', a);
  }
  get groupable() {
    return Boolean(!(this.noFieldSpecified || !this.field) && this.data.groupable);
  }
  set groupable(a) {
    this.set('groupable', a);
  }
  get filterable() {
    return this.isLeaf && this.data.filterable;
  }
  get editor() {
    const a = this;
    let {editor: b} = a.data;
    if (b && !b.isWidget) {
      const c = a.grid.processCellEditor({
        'editor': b,
        'field': a.field
      });
      if (c) {
        b = a.data.editor = c.editor;
      } else {
        if (typeof b === 'string') {
          b = { 'type': b };
        }
        b = a.data.editor = Widget.create(ObjectHelper.merge(a.defaultEditor, {
          'owner': a.grid,
          'label': StringHelper.encodeHtml(a.text)
        }, b));
      }
    }
    return b;
  }
  set editor(a) {
    this.data.editor = a;
  }
  get defaultEditor() {
    return {
      'type': 'textfield',
      'name': this.field
    };
  }
  getFilterableValue(a) {
    return this.getRawValue(a);
  }
  get owner() {
    return this._owner || this.subGrid;
  }
  set owner(a) {
    this._owner = a;
  }
  get grid() {
    let a;
    return this._grid || ((a = this.parent) == null ? void 0 : a.grid);
  }
  set subGrid(a) {
    this._subGrid = a;
  }
  get subGrid() {
    let a;
    return this._subGrid || ((a = this.grid) == null ? void 0 : a.getSubGridFromColumn(this));
  }
  get subGridElement() {
    return this.subGrid.element;
  }
  get element() {
    return this.grid.getHeaderElement(this);
  }
  get previousVisibleSibling() {
    const a = this.region;
    let b = this.previousSibling;
    while (b && (b.hidden || b.region !== a)) {
      b = b.previousSibling;
    }
    return b;
  }
  get nextVisibleSibling() {
    const a = this.region;
    let b = this.nextSibling;
    while (b && (b.hidden || b.region !== a)) {
      b = b.nextSibling;
    }
    return b;
  }
  get isLastInSubGrid() {
    return !this.nextVisibleSibling && (this.parent.isRoot || this.parent.isLastInSubGrid);
  }
  get allowDrag() {
    return !this.parent.isRoot || Boolean(this.nextVisibleSibling || this.previousVisibleSibling);
  }
  get textWrapper() {
    return DomHelper.getChild(this.element, '.b-grid-header-text');
  }
  get textElement() {
    return DomHelper.down(this.element, '.b-grid-header-text-content');
  }
  get contentElement() {
    return DomHelper.down(this.element, '.b-grid-header-children');
  }
  get isSorted() {
    return this.grid.store.sorters.some(a => a.field === this.field);
  }
  get isFocusable() {
    let a;
    return this.isLeaf && !((a = this.grid) == null ? void 0 : a.hideHeaders);
  }
  static get text() {
    return this.$meta.fields.defaults.text;
  }
  get headerText() {
    return this.htmlEncodeHeaderText ? StringHelper.encodeHtml(this.text) : this.text;
  }
  get isVisible() {
    return !this.hidden && (!this.parent || this.parent.isVisible);
  }
  hide(a = false, b = false) {
    const c = this, {parent: e} = c;
    if (!c.hidden) {
      c.hidden = true;
      if (e && !e.isRoot && !e.isTogglingAll) {
        const f = e.children.some(g => g.hidden !== true);
        if (!f && !e.hidden) {
          a = true;
          e.hide();
        }
      }
      if (c.isParent) {
        c.children.forEach(g => g.hide(true, true));
      } else if (!e.isRoot) {
        e.meta.visibleChildren[b ? 'add' : 'delete'](c);
      }
      if (!a) {
        c.stores.forEach(g => g.trigger('columnHide', { 'column': c }));
      }
    }
  }
  show(a = false) {
    let b;
    const c = this, {parent: e} = c;
    if (c.hidden) {
      c.hidden = false;
      if (e == null ? void 0 : e.hidden) {
        e.show();
      }
      if (c.isParent) {
        (b = c.meta.visibleChildren) == null ? void 0 : b.forEach(f => f.show(true));
      }
      if (!a) {
        c.stores.forEach(f => f.trigger('columnShow', { 'column': c }));
      }
    }
  }
  toggle(a) {
    if (this.hidden && a === void 0 || a === true) {
      return this.show();
    }
    if (!this.hidden && a === void 0 || a === false) {
      return this.hide();
    }
  }
  toggleChildren(a = this.children, b = void 0) {
    let c, e;
    const f = this;
    (c = f.grid.columns) == null ? void 0 : c.beginBatch();
    f.isTogglingAll = true;
    a.forEach(g => g.toggle(b));
    f.isTogglingAll = false;
    (e = f.grid.columns) == null ? void 0 : e.endBatch();
  }
  onCollapseChange(a = void 0) {
    let b, c;
    const e = this;
    if (e.collapseMode === 'toggleAll') {
      e.toggleChildren();
    } else {
      const {firstChild: f} = e;
      if (f.flex != null && e.collapsed) {
        f.oldFlex = f.flex;
        f.width = f.element.offsetWidth;
      } else if (!e.collapsed && f.oldFlex) {
        f.flex = f.oldFlex;
        f.oldFlex = null;
      }
      (b = e.grid.columns) == null ? void 0 : b.beginBatch();
      e.isTogglingAll = true;
      e.children.slice(1).forEach(g => g.toggle(a));
      e.isTogglingAll = false;
      (c = e.grid.columns) == null ? void 0 : c.endBatch();
    }
  }
  set collapsible(a) {
    const b = this;
    b.set('collapsible', a);
    if (b.isParent) {
      const {
        headerWidgets: headerWidgets = []
      } = b;
      if (a) {
        headerWidgets.push({
          'type': 'button',
          'ref': 'collapseExpand',
          'toggleable': true,
          'pressed': b.collapsed,
          'icon': 'b-icon-collapse-' + (b.grid.rtl ? 'right' : 'left'),
          'pressedIcon': 'b-icon-collapse-' + (b.grid.rtl ? 'left' : 'right'),
          'cls': 'b-grid-header-collapse-button b-transparent',
          'onToggle': ({pressed: c}) => b.collapsed = c
        });
      } else {
        const c = headerWidgets.findIndex(e => e.ref === 'collapseExpand');
        c > -1 && headerWidgets.splice(c, 1);
      }
      b.headerWidgets = headerWidgets;
      if (b.collapsed) {
        b.onCollapseChange(false);
      }
    }
  }
  get collapsible() {
    return this.get('collapsible');
  }
  generateId() {
    let a, b, c;
    const {grid: e} = this, f = [(b = (a = this.field) == null ? void 0 : a.replace(/\./g, '-')) != null ? b : 'col'];
    if (e) {
      f.unshift((c = e.stateId) != null ? c : e.id);
      if (!e.generatedIdIndex) {
        e.generatedIdIndex = 0;
      }
      f.push(e.generatedIdIndex += 1);
    } else {
      if (!_Column.generatedIdIndex) {
        _Column.generatedIdIndex = 0;
      }
      f.push(_Column.generatedIdIndex += 1);
    }
    return this.meta.generatedId = f.join('-');
  }
  get allIndex() {
    return this.masterStore.indexOf(this);
  }
  measureSize(a) {
    let b;
    return DomHelper.measureSize(a, (b = this.subGrid) == null ? void 0 : b.element);
  }
  get calcMinWidth() {
    const {
      width: a,
      minWidth: b
    } = this.data;
    if (validWidth(a) && validWidth(b)) {
      return Math.max(parseInt(a) || 0, parseInt(b) || 0);
    } else {
      return a;
    }
  }
  get width() {
    return this.data.width;
  }
  set width(a) {
    const b = { 'width': a };
    if (a && 'flex' in this.data) {
      b.flex = null;
    }
    this.set(b);
  }
  set flex(a) {
    const b = { 'flex': a };
    if (a && 'width' in this.data) {
      b.width = null;
    }
    this.set(b);
  }
  get flex() {
    return this.data.flex;
  }
  calculateMinWidth() {
    const a = this, b = a.measureSize(a.width), c = a.measureSize(a.minWidth);
    let e = 0;
    if (a.children) {
      e = a.children.reduce((f, g) => {
        return f + g.calculateMinWidth();
      }, 0);
    }
    return Math.max(b, c, e);
  }
  resizeToFitContent(a, b, c = false) {
    let e;
    const f = this, {
        grid: g,
        element: j,
        fitMode: k
      } = f, {rowManager: l} = g, {records: n} = g.$store, o = n.length;
    if (o <= 0 || f.fitMode === 'none' || !f.fitMode || ((e = f.data) == null ? void 0 : e.vue)) {
      return;
    }
    const [p] = l.rows, {
        rowElement: q,
        cellElement: r
      } = g.beginGridMeasuring(), u = new GridLocation({
        'grid': g,
        'column': f,
        'id': null
      });
    let v = 0, w, z, A, B, C, E, F = {
        'length': 0,
        'record': null
      };
    r._domData = {
      'columnId': f.id,
      'row': p,
      'rowElement': q
    };
    u._cell = r;
    u.updatingSingleRow = true;
    u.isMeasuring = true;
    r.innerHTML = '';
    if (g.resizeToFitIncludesHeader && !g.hideHeaders) {
      if (!g.$headerPadding) {
        const H = globalThis.getComputedStyle(j);
        g.$headerPadding = parseInt(H.paddingLeft);
      }
      const G = j.querySelector('.b-grid-header-text-content');
      G.style.cssText = 'flex: none; width: auto';
      v = G.offsetWidth + g.$headerPadding * 2 + 2;
      G.style.cssText = '';
    }
    if (o > 1000) {
      w = Math.max(Math.min(l.topIndex + Math.round(l.rowCount / 2) - 500, o - 1000), 0);
      z = w + 1000;
    } else {
      w = 0;
      z = o;
    }
    for (A = w; A < z; A++) {
      B = n[A];
      C = f.getRawValue(B);
      if (k === 'value') {
        E = String(C).length;
      } else {
        u._record = B;
        u._id = B.id;
        u._rowIndex = A;
        p.renderCell(u);
        if (k === 'textContent') {
          E = r.textContent.length;
        } else {
          const I = r.offsetWidth;
          if (I > v) {
            v = I;
          }
        }
      }
      if (E > F.length) {
        F = {
          'record': B,
          'length': E,
          'rowIndex': A
        };
      }
    }
    if (F.length > 0 && (k === 'value' || k === 'textContent')) {
      u._record = F.record;
      u._id = F.record.id;
      u._rowIndex = F.rowIndex;
      p.renderCell(u);
      v = Math.max(v, r.offsetWidth);
    }
    if (Array.isArray(a)) {
      [a, b] = a;
    }
    v = Math.max(v, a || 0);
    v = Math.min(v, b || 1000000);
    if (!c) {
      g.endGridMeasuring();
    }
    f.width = f.maxWidth ? v = Math.min(v, f.maxWidth) : v;
    return v;
  }
  getState() {
    const a = this, {parentId: b} = a, c = {
        'id': a.id,
        'hidden': a.hidden,
        'region': a.region,
        'locked': a.locked,
        'text': a.text
      };
    if (!a.noFieldSpecified) {
      c.field = a.field;
    }
    if (b != null) {
      c.parentId = b;
    }
    if (!a.children) {
      c[a.flex ? 'flex' : 'width'] = a.flex || a.width;
    }
    if (a.isCollapsible) {
      c.collapsed = a.collapsed;
    }
    return c;
  }
  applyState(a) {
    const b = this;
    b.beginBatch();
    if ('locked' in a) {
      b.locked = a.locked;
    }
    if ('width' in a) {
      b.width = a.width;
    }
    if ('flex' in a) {
      b.flex = a.flex;
    }
    if ('width' in a && b.flex) {
      b.flex = void 0;
    } else if ('flex' in a && b.width) {
      b.width = void 0;
    }
    if ('region' in a) {
      b.region = a.region;
    }
    if ('text' in a) {
      b.text = a.text;
    }
    b.endBatch();
    if ('hidden' in a) {
      b.toggle(a.hidden !== true);
    }
    if ('collapsed' in a) {
      b.collapsed = a.collapsed;
    }
  }
  remove() {
    const {
        subGrid: a,
        grid: b
      } = this, c = a && (b == null ? void 0 : b.focusedCell);
    if ((c == null ? void 0 : c.columnId) === this.id) {
      if (b.owns(DomHelper.getActiveElement(b))) {
        b.navigateRight();
      } else {
        b._focusedCell = new GridLocation({
          'grid': b,
          'rowIndex': c.rowIndex,
          'column': a.columns.getAdjacentVisibleLeafColumn(this.id, true, true)
        });
      }
    }
    super.remove();
  }
  getRawValue(a) {
    return a.getValue(this.field);
  }
  refreshCell(a) {
    this.grid.rowManager.refreshCell(a, this.id);
  }
  refreshCells() {
    this.grid.refreshColumn(this);
  }
  refreshHeader() {
    this.grid.refreshHeader(this);
  }
  clearCell(a) {
    a.innerHTML = '';
    delete a._content;
  }
  canEdit(a) {
    if (a.isEditable) {
      const b = a.isEditable(this.field);
      if (b !== void 0) {
        return b;
      }
    }
    return true;
  }
  insertChild(a, b = null, c = false) {
    a = Array.isArray(a) ? a : [a];
    a.forEach(e => {
      const {parent: f} = e;
      if ((f == null ? void 0 : f.collapsed) && e === f.firstChild && f.children.length > 1 && f.children.filter(g => !g.hidden).length === 1) {
        e.nextSibling.hidden = false;
      }
    });
    return this.sealed && !this.inProcessChildren ? null : super.insertChild(...arguments);
  }
  canFillValue() {
    return true;
  }
  getCurrentConfig(a) {
    let b;
    const c = super.getCurrentConfig(a);
    if ((b = this.sortable) == null ? void 0 : b.originalSortFn) {
      c.sortable = this.sortable.originalSortFn;
    }
    if (c.headerRenderer === this.internalHeaderRenderer) {
      delete c.headerRenderer;
    }
    delete c.ariaLabel;
    delete c.cellAriaLabel;
    delete c.disableRendererWarning;
    return c;
  }
  get visible() {
    return this.isVisible;
  }
  set visible(a) {
    this.hidden = !a;
  }
  get readOnly() {
    let a;
    const {
        field: b,
        grid: c
      } = this, e = Boolean(this.get('readOnly'));
    if (b && c) {
      return ((a = c.store.modelClass.getFieldDefinition(b)) == null ? void 0 : a.calculated) || e;
    }
    return e;
  }
  set readOnly(a) {
    this.set('readOnly', a);
  }
  get renderer() {
    let a;
    return ((a = this.data) == null ? void 0 : a.renderer) || this.defaultRenderer;
  }
  set renderer(a) {
    this.set('renderer', a);
  }
};
const Column = _Column;
Column.emptyCount = 0;
Column.defaultWidth = 100;
Column.exposeProperties();
Column._$name = 'Column';
const columnDefinitions = {
  'boolean': { 'type': 'check' },
  'date': { 'type': 'date' },
  'integer': {
    'type': 'number',
    'format': { 'maximumFractionDigits': 0 }
  },
  'number': { 'type': 'number' }
};
const lockedColumnSorters = [{ 'field': 'region' }];
const ColumnStore = class _ColumnStore extends Localizable_default(Store) {
  static get defaultConfig() {
    return {
      'modelClass': Column,
      'tree': true,
      'autoAddField': false,
      'syncDataOnLoad': { 'threshold': 1 },
      'sorters': lockedColumnSorters,
      'reapplySortersOnAdd': true,
      'useRawData': false
    };
  }
  construct(a) {
    const b = this, {grid: c} = a;
    if (c) {
      c._columnStore = b;
      b.id = c.id + '-columns';
      c.ion({
        'subGridCollapse': 'clearSubGridCaches',
        'subGridExpand': 'clearSubGridCaches',
        'thisObj': b
      });
    }
    super.construct(a);
    b.ion({
      'change': b.onStoreChange,
      'sort': () => b.updateChainedStores(),
      'thisObj': b,
      'prio': 1
    });
  }
  doDestroy() {
    const a = [];
    if (!this.isChained) {
      this.traverse(b => a.push(b));
    }
    super.doDestroy();
    if (!this.isChained) {
      a.forEach(b => b.destroy());
    }
  }
  getById(a) {
    return super.getById(a) || this.idRegister[a];
  }
  forEach(a, b = this) {
    this.traverseWhile((c, e) => a.call(b, c, e), true);
  }
  get totalFixedWidth() {
    let a = 0;
    for (const b of this) {
      if (!b.hidden) {
        if (b.children) {
          b.children.forEach(c => a += this.calculateFixedWidth(c));
        } else {
          a += this.calculateFixedWidth(b);
        }
      }
    }
    return a;
  }
  get hasFlex() {
    return this.visibleColumns.some(a => a.flex);
  }
  calculateFixedWidth(a) {
    if (a.flex) {
      return a.measureSize(Column.defaultWidth);
    } else {
      return Math.max(a.measureSize(a.width), a.measureSize(a.minWidth));
    }
  }
  get topColumns() {
    return this.isChained ? this.masterStore.rootNode.children.filter(this.chainedFilterFn) : this.rootNode.children;
  }
  get visibleColumns() {
    const a = this;
    if (!a._visibleColumns) {
      a._visibleColumns = a.leaves.filter(b => b.isVisible && (!b.subGrid || !b.subGrid.collapsed)).sort(a.sorterFn);
    }
    return a._visibleColumns;
  }
  onStoreChange({
    action: a,
    changes: b
  }) {
    if (a === 'update' && !('hidden' in b)) {
      return;
    }
    this.clearCaches();
  }
  clearSubGridCaches({subGrid: a}) {
    a.columns.clearCaches();
    this.clearCaches();
  }
  clearCaches() {
    let a;
    this._visibleColumns = null;
    (a = this.masterStore) == null ? void 0 : a.clearCaches();
  }
  onMasterDataChanged(a) {
    super.onMasterDataChanged(a);
    if (a.action !== 'update' || ('hidden' in a.changes || 'region' in a.changes)) {
      this.clearCaches();
    }
  }
  getAdjacentVisibleLeafColumn(a, b = true, c = false) {
    const e = this.visibleColumns, f = a instanceof Column ? a : this.getById(a);
    let g = e.indexOf(f) + (b ? 1 : -1);
    if (!e[g]) {
      if (c) {
        g = b ? 0 : e.length - 1;
      } else {
        return null;
      }
    }
    return e[g];
  }
  get bottomColumns() {
    return this.leaves;
  }
  get(a) {
    return this.findRecord('field', a, true);
  }
  lookupStateColumn(a) {
    let b = this.getById(a.id);
    if (!b && a.field) {
      const c = this.query(e => e.field === a.field);
      if (c.length === 1) {
        b = c[0];
      }
    }
    return b;
  }
  createRecord(a) {
    let b, c;
    const {
        grid: grid = {}
      } = this, {store: e} = grid, f = (c = (b = e == null ? void 0 : e.modelClass) == null ? void 0 : b.fieldMap) == null ? void 0 : c[a.field];
    let g = this.modelClass;
    if (f == null ? void 0 : f.column) {
      a = Objects.merge({}, f.column, a);
    }
    if (a.type) {
      g = _ColumnStore.getColumnClass(a.type);
      if (!g) {
        throw new Error('Column type \'' + a.type + '\' not registered. Please make sure you have imported the column class.');
      }
    }
    if (a.locked) {
      a.region = 'locked';
      delete a.locked;
    }
    const i = new g(a, this);
    if (!i.data.region) {
      i.data.region = grid.defaultRegion || 'normal';
    }
    if (this.autoAddField && !i.noFieldSpecified && e && !f) {
      let j = i.field;
      if (i.constructor.fieldType) {
        j = {
          'name': i.field,
          'type': i.constructor.fieldType
        };
      }
      e.modelClass.addField(j);
    }
    return i;
  }
  indexOf(a) {
    if (a == null) {
      return -1;
    }
    const b = super.indexOf(a);
    if (b > -1)
      return b;
    return this.records.findIndex(c => c.field === a);
  }
  removeAll(a = false) {
    const b = this, c = b.some(e => e.isTimeAxisColumn);
    if (c) {
      return b.remove(b.query(e => !e.isTimeAxisColumn), a);
    }
    return super.removeAll(a);
  }
  get usesAutoHeight() {
    return this.some(a => a.autoHeight);
  }
  get usesFlexAutoHeight() {
    return this.some(a => a.autoHeight && a.flex != null);
  }
  resolveSyncNode(a) {
    if (a.id) {
      return super.resolveSyncNode(a);
    }
    if (a.field) {
      return {
        'id': a.field,
        'node': this.allRecords.find(b => b.field === a.field)
      };
    }
    if (a.type) {
      return {
        'id': a.type,
        'node': this.allRecords.find(b => b.type === a.type)
      };
    }
    return {
      'id': null,
      'node': null
    };
  }
  static registerColumnType(a, b = false) {
    a.simpleRenderer = b;
    (_ColumnStore.columnTypes || (_ColumnStore.columnTypes = {}))[a.type] = a;
  }
  static getColumnClass(a) {
    return _ColumnStore.columnTypes && _ColumnStore.columnTypes[a];
  }
  generateColumnForField(a, b) {
    let c;
    if (typeof a === 'string' && this.grid) {
      a = (c = this.grid.store) == null ? void 0 : c.modelClass.fieldMap[a];
    }
    let e = a.column || columnDefinitions[a.type] || {};
    if (typeof e === 'string') {
      e = { 'type': e };
    }
    e = Object.assign({
      'text': a.text || StringHelper.separate(a.name),
      'field': a.name
    }, b, e);
    if (a.precision != null) {
      e.format.maximumFractionDigits = a.precision;
    }
    if (a.columnType) {
      e.type = a.columnType;
    }
    return this.createRecord(e);
  }
};
const columnResizeEvent = (a, b) => ({
  'update': ({
    store: c,
    record: e,
    changes: f
  }) => {
    let g = true;
    if ('width' in f || 'minWidth' in f || 'maxWidth' in f || 'flex' in f) {
      g = a.call(b, {
        'store': c,
        'record': e,
        'changes': f
      });
    }
    return g;
  }
});
ColumnStore.registerColumnType(Column, true);
ColumnStore._$name = 'ColumnStore';
const ActionColumn = class extends Column {
  static defaults = {
    'minWidth': 30,
    'filterable': false,
    'groupable': false,
    'sortable': false,
    'editor': false,
    'searchable': false,
    'htmlEncode': false,
    'resizable': false,
    'readOnly': true
  };
  static fields = [
    {
      'name': 'actions',
      'type': 'array'
    },
    {
      'name': 'disableIfGridReadOnly',
      'defaultValue': false
    }
  ];
  static $name = 'ActionColumn';
  static type = 'action';
  get groupHeaderReserved() {
    return true;
  }
  construct(a, b) {
    const c = this;
    if (a.renderer) {
      VersionHelper.deprecate('grid', '7.0.0', 'Using deprecated `renderer` with ActionColumn, use `afterRenderCell` callback instead.');
      a.afterRenderCell = a.renderer;
      delete a.renderer;
    }
    super.construct(a, b);
    if (!a.width && !a.flex) {
      c.grid.ion({
        'paint': 'updateAutoWidth',
        'thisObj': c
      });
    }
    if (c.disableIfGridReadOnly) {
      c.grid.element.classList.add('b-actioncolumn-readonly');
    }
  }
  defaultRenderer({
    grid: a,
    column: b,
    record: c,
    isExport: e,
    callExternalRenderer: callExternalRenderer = true
  }) {
    let f;
    const g = c && 'groupRowFor' in c.meta, {subGrid: i} = b, j = {
        'className': { 'b-action-ct': 1 },
        'children': (f = b.actions) == null ? void 0 : f.map((k, l) => {
          let n, o;
          if ('visible' in k) {
            if (typeof k.visible === 'function' && k.visible({ 'record': c }) === false || k.visible === false) {
              return '';
            }
          }
          if (g && !k.showForGroup || !g && k.showForGroup) {
            return '';
          }
          const {
              tooltip: p,
              renderer: q
            } = k, r = typeof p === 'function' || ((n = p == null ? void 0 : p.startsWith) == null ? void 0 : n.call(p, 'up.')) ? i.callback(p, i, [{ 'record': c }]) : p || '';
          let u;
          if (q) {
            const w = (o = i.callback(q, i, [{
                'index': l,
                'record': c,
                'column': b,
                'isExport': e,
                'tooltip': r,
                'action': k
              }])) != null ? o : '';
            if (typeof w === 'string') {
              u = {
                'tag': 'span',
                'class': 'b-action-item b-tool',
                'dataset': {
                  ...Tooltip.encodeConfig(r),
                  'index': l
                },
                'html': w
              };
            } else {
              w.dataset = w.dataset || {};
              w.dataset.index = l;
              u = w;
            }
          } else {
            u = {
              'tag': 'button',
              'dataset': {
                ...Tooltip.encodeConfig(r),
                'index': l
              },
              'className': {
                'b-tool': 1,
                'b-action-item': 1,
                [k.cls]: k.cls
              }
            };
          }
          const v = k.ariaLabel || (typeof k.tooltip === 'string' ? k.tooltip : void 0);
          if (v) {
            u['aria-label'] = StringHelper.encodeHtml(v);
          }
          return u;
        })
      };
    if (e) {
      return j.children.flatMap(k => k.html || []).join(',');
    }
    return j;
  }
  onCellClick({
    grid: a,
    column: b,
    record: c,
    target: e
  }) {
    let f;
    let g;
    if (b !== this || !(g = e.closest('.b-action-item[data-index]'))) {
      return;
    }
    const i = g.dataset.index, j = (f = b.actions) == null ? void 0 : f[i], k = j == null ? void 0 : j.onClick;
    if (k) {
      this.callback(k, b, [{
          'record': c,
          'action': j,
          'target': e,
          'grid': a,
          'column': b
        }]);
    }
  }
  updateAutoWidth() {
    const a = this, b = [], {actions: c} = a;
    if (!a.element) {
      return;
    }
    const e = a.actions = [];
    c == null ? void 0 : c.forEach(g => {
      const i = { ...g };
      delete i.visible;
      if (i.showForGroup) {
        delete i.showForGroup;
        b.push(i);
      } else {
        e.push(i);
      }
    });
    if (b.length > e.length) {
      a._actions = b;
    }
    const f = DomHelper.createElement(a.defaultRenderer({
      'column': a,
      'record': new a.grid.store.modelClass(),
      'callExternalRenderer': false
    })).outerHTML;
    a.width = DomHelper.measureText(f, a.element, true, a.element.parentElement);
    a.actions = c;
  }
};
ColumnStore.registerColumnType(ActionColumn);
ActionColumn.exposeProperties();
ActionColumn._$name = 'ActionColumn';
const NumberColumn = class extends Column {
  static defaults = {
    'filterType': 'number',
    'align': 'end'
  };
  static fields = [
    {
      'name': 'format',
      'defaultValue': ''
    },
    'min',
    'max',
    'step',
    'largeStep',
    'unit',
    'renderer'
  ];
  static fieldType = 'number';
  static type = 'number';
  get defaultEditor() {
    const {
      format: a,
      name: b,
      max: c,
      min: e,
      step: f,
      largeStep: g,
      align: i
    } = this;
    return ObjectHelper.cleanupProperties({
      'type': 'numberfield',
      'format': a,
      'name': b,
      'max': c,
      'min': e,
      'step': f,
      'largeStep': g,
      'textAlign': i
    });
  }
  get formatter() {
    const a = this, {format: b} = a;
    let c = a._formatter;
    if (!c || a._lastFormat !== b) {
      a._formatter = c = NumberHelper.getLocalizedFormatter(a._lastFormat = b);
    }
    return c;
  }
  formatValue(a) {
    if (a != null) {
      a = this.formatter.format(a);
      if (this.unit) {
        a = '' + a + this.unit;
      }
    }
    return a != null ? a : '';
  }
  defaultRenderer({value: a}) {
    return this.formatValue(a);
  }
  updateLocalization() {
    super.updateLocalization();
    this._formatter = null;
  }
};
ColumnStore.registerColumnType(NumberColumn, true);
NumberColumn.exposeProperties();
NumberColumn._$name = 'NumberColumn';
const AggregateColumn = class extends NumberColumn {
  static fields = [
    'function',
    'includeParentInChangeSet'
  ];
  static type = 'aggregate';
  static get defaults() {
    return {
      'function': 'sum',
      'includeParentInChangeSet': false
    };
  }
  construct(a, b) {
    const c = this;
    c.configuredAlign = 'align' in a;
    c.configuredEditor = 'editor' in a;
    super.construct(...arguments);
    const {grid: e} = b;
    if (c.function === 'sum') {
      c.function = 'sumChildren';
    }
    if (e) {
      c.store = e.store;
    }
  }
  set store(a) {
    const b = this, c = {
        'update': 'onRecordUpdate',
        'thisObj': b,
        'prio': 1000
      }, e = b._store;
    if (a !== e) {
      if (e) {
        e.un(c);
      }
      b._store = a;
      const {modelClass: f} = a, g = f.fieldMap[b.field];
      if (g && g.type === 'number') {
        if (!b.configuredAlign) {
          b.align = 'end';
        }
        if (!b.configuredEditor) {
          b.editor = 'number';
        }
      }
      a.ion(c);
    }
  }
  canEdit(a) {
    return a.isLeaf;
  }
  get store() {
    return this._store;
  }
  sumChildren(...a) {
    let b = 0;
    for (let c = 0, {length: e} = a; c < e; c++) {
      b += parseFloat(a[c] || 0, 10);
    }
    return b;
  }
  avg(...a) {
    let b = 0;
    const {length: c} = a;
    for (let e = 0; e < c; e++) {
      b += parseFloat(a[e] || 0, 10);
    }
    return b / c;
  }
  onRecordUpdate({
    record: a,
    changes: b
  }) {
    const c = this, {rowManager: e} = c.grid;
    if (c.field in b) {
      if (a.isLeaf) {
        a.bubble(f => {
          const g = e.getRowFor(f);
          if (g) {
            const i = g.getCell(c.field);
            if (i) {
              g.renderCell(i);
            }
          }
        }, true);
      }
    }
  }
  getRawValue(a) {
    let b;
    let c;
    const e = this, {field: f} = e;
    if ((b = a.children) == null ? void 0 : b.length) {
      const g = e.function, i = typeof g === 'string' && typeof Math[g] === 'function', {
          handler: j,
          thisObj: k
        } = i ? {
          'handler': Math[g],
          'thisObj': Math
        } : e.resolveCallback(g);
      c = j.apply(k, a.children.map(l => e.getRawValue(l)));
      if (e.includeParentInChangeSet) {
        a.set(f, c, true);
      } else {
        a.setData(f, c);
      }
    } else {
      c = a.getValue(f);
    }
    return c;
  }
  canFillValue({record: a}) {
    return a.isLeaf;
  }
};
ColumnStore.registerColumnType(AggregateColumn, true);
AggregateColumn.exposeProperties();
AggregateColumn._$name = 'AggregateColumn';
const onWidgetChange = ({
  source: a,
  value: b,
  userAction: c
}) => {
  if (c) {
    a.cellInfo.record.setValue(a.name, b);
  }
};
const WidgetColumn = class extends Column {
  static fields = [
    'widgets',
    'disableRendererWarning'
  ];
  static type = 'widget';
  static $name = 'WidgetColumn';
  static get defaults() {
    return {
      'filterable': false,
      'sortable': false,
      'editor': false,
      'searchable': false,
      'fitMode': false,
      'alwaysClearCell': false
    };
  }
  construct(a) {
    if (!a.disableRendererWarning) {
      if (a.renderer) {
        a.afterRenderCell = a.renderer;
        delete a.renderer;
        VersionHelper.deprecate('grid', '7.0.0', 'Using deprecated `renderer` with WidgetColumn, use `afterRenderCell` callback instead.');
      }
      if (this.renderer && this.renderer !== this.defaultRenderer) {
        a.afterRenderCell = this.renderer;
        this.renderer = this.defaultRenderer;
        VersionHelper.deprecate('grid', '7.0.0', 'Using deprecated `renderer` with WidgetColumn, use `afterRenderCell` callback instead.');
      }
    }
    super.construct(...arguments);
    const b = this, {grid: c} = b;
    if (c) {
      if (c.widgetColumnCache) {
        b.widgetColumnCache = c.widgetColumnCache;
      } else {
        b.widgetColumnCache = c.widgetColumnCache = new Map();
        c.ion({
          'removeRows': 'onRemoveGridRows',
          'thisObj': this
        });
      }
    }
  }
  onRemoveGridRows({rows: a}) {
    a.forEach(({columnWidgets: b}) => {
      if (b) {
        this.freeWidgets([...b.values()].flat());
        b.clear();
      }
    });
  }
  freeWidgets(a) {
    a == null ? void 0 : a.forEach(b => {
      let c;
      const {widgetColumnCache: e} = this, {column: f} = b.cellInfo, g = f.id + '-' + b.widgetColumnIndex;
      (c = f.onAfterWidgetDerendered) == null ? void 0 : c.call(f, b);
      b.cellInfo = b.__boundRecordId = b.owner = null;
      b.element.remove();
      (e.get(g) || e.set(g, []).get(g)).push(b);
    });
    a.length = 0;
  }
  freeColumnWidgets() {
    const {id: a} = this;
    this.grid.rowManager.forEach(b => {
      let c;
      const e = (c = b.columnWidgets) == null ? void 0 : c.get(a);
      if (e == null ? void 0 : e.length) {
        this.freeWidgets(e);
      }
    });
  }
  hide() {
    this.freeColumnWidgets();
    super.hide(...arguments);
  }
  getWidget(a, b) {
    let c;
    const e = this, {
        grid: f,
        widgetColumnCache: g
      } = e, i = b.column.id + '-' + a.widgetColumnIndex, j = g.get(i) || g.set(i, []).get(i);
    let k, {length: l} = j;
    if (l) {
      k = j[--l];
      delete k.onBeforeDestroy;
      j.length = l;
      a.owner = f;
      Widget.reconfigure(k, a);
    } else {
      e.onBeforeWidgetCreate(a, b);
      a.recomposeAsync = false;
      a.owner = f;
      k = Widget.create(a);
      e.onAfterWidgetCreate(k, b);
      if (k.name) {
        k.ion({ 'change': onWidgetChange });
      }
    }
    (c = e.onBeforeWidgetRender) == null ? void 0 : c.call(e, k);
    return k;
  }
  doDestroy() {
    if (this.grid.isDestroying) {
      [...this.widgetColumnCache.values()].flat().forEach(a => a.destroy());
      this.widgetColumnCache.clear();
    } else {
      this.freeColumnWidgets();
    }
    super.doDestroy();
  }
  updateReadOnly(a) {
    this.grid.rowManager.forEach(b => {
      let c, e;
      (e = (c = b.columnWidgets) == null ? void 0 : c.get(this.id)) == null ? void 0 : e.forEach(f => {
        if (!f.cellInfo.record.readOnly) {
          f.readOnly = a;
        }
      });
    });
  }
  defaultRenderer(a) {
    const b = this, {grid: c} = b, {
        cellElement: e,
        column: f,
        record: g,
        row: i,
        isExport: j
      } = a, {widgets: k} = f;
    if (!j && k) {
      const l = i.columnWidgets || (i.columnWidgets = new Map()), n = l.get(b.id);
      [...e.childNodes].forEach(o => {
        let p;
        if (!((p = o.classList) == null ? void 0 : p.contains('b-widget'))) {
          o.remove();
        }
      });
      a.widgets = f.widgets.map((o, p) => {
        let q, r;
        o.widgetColumnIndex = p;
        const u = (n == null ? void 0 : n[p]) || b.getWidget(o, a);
        u.cellInfo = a;
        if (!u.element.isConnected) {
          if (u.rendered) {
            e.appendChild(u.element);
          } else {
            u.render(e);
          }
        }
        if (!b.meta.isSelectionColumn) {
          u.readOnly = c.readOnly || g.readOnly || b.readOnly;
        }
        if (((q = b.onBeforeWidgetSetValue) == null ? void 0 : q.call(b, u, a)) !== false) {
          const v = o.valueProperty || 'value' in u && 'value' || u.defaultBindProperty;
          if (v && (!f.noFieldSpecified && f.widgets.length === 1 || u.name)) {
            const w = u.name ? g.getValue(u.name) : a.value;
            if (typeof w == 'object' && u.__boundRecordId !== g.id) {
              u['_' + v] = NaN;
            }
            u.suspendEvents();
            u[v] = w;
            u.resumeEvents();
            u.__boundRecordId = g.id;
          }
        }
        (r = b.onAfterWidgetSetValue) == null ? void 0 : r.call(b, u, a);
        return u;
      });
      l.set(b.id, a.widgets);
    }
    if (!k) {
      return '';
    }
  }
  onBeforeWidgetCreate(a, b) {
  }
  onAfterWidgetCreate(a, b) {
  }
  resizeToFitContent() {
  }
};
ColumnStore.registerColumnType(WidgetColumn);
WidgetColumn.exposeProperties();
WidgetColumn._$name = 'WidgetColumn';
const CheckColumn = class extends WidgetColumn {
  static defaults = {
    'align': 'center',
    'checkCls': null,
    'showCheckAll': false,
    'sortable': true,
    'filterable': true,
    'minWidth': 30,
    'widgets': [{
        'type': 'checkbox',
        'valueProperty': 'checked'
      }]
  };
  static fields = [
    'checkCls',
    'showCheckAll',
    'onAfterWidgetSetValue',
    'onBeforeWidgetSetValue',
    'callOnFunctions',
    'onBeforeToggle',
    'onToggle',
    'onToggleAll'
  ];
  static type = 'check';
  static $name = 'CheckColumn';
  construct(a, b) {
    let c;
    super.construct(...arguments);
    const e = this;
    e.widgets[0].cls = e.checkCls;
    Object.assign(e, {
      'externalHeaderRenderer': e.headerRenderer,
      'externalOnBeforeWidgetSetValue': e.onBeforeWidgetSetValue,
      'externalOnAfterWidgetSetValue': e.onAfterWidgetSetValue
    });
    e.setData({
      'onBeforeWidgetSetValue': e.internalOnBeforeWidgetSetValue,
      'onAfterWidgetSetValue': e.internalOnAfterWidgetSetValue,
      'headerRenderer': e.internalHeaderRenderer
    });
    if (!e.meta.isSelectionColumn) {
      const f = (c = e.grid) == null ? void 0 : c.store.modelClass;
      if (!e.field) {
        console.warn('CheckColumn MUST be configured with a field, otherwise the checked state will not be persistent. Widgets are recycled and reused');
      } else if (f && typeof e[e.field] !== 'boolean' && !f.fieldMap[e.field] && !e.constructor.suppressNoModelFieldWarning) {
        console.warn(e.$$name + ' is configured with a field, but this is not part of your Model `fields` collection.');
        f.addField({
          'name': e.field,
          'type': 'boolean'
        });
      }
    }
  }
  doDestroy() {
    let a;
    (a = this.headerCheckbox) == null ? void 0 : a.destroy();
    super.doDestroy();
  }
  internalHeaderRenderer({
    headerElement: a,
    column: b
  }) {
    let c;
    a.classList.add('b-check-header');
    if (b.showCheckAll) {
      a.classList.add('b-check-header-with-checkbox');
      if (b.headerCheckbox) {
        a.appendChild(b.headerCheckbox.element);
      } else {
        b.headerCheckbox = new Checkbox({
          'appendTo': a,
          'owner': this.grid,
          'ariaLabel': 'L{Checkbox.toggleSelection}',
          'internalListeners': {
            'change': 'onCheckAllChange',
            'thisObj': b
          }
        });
      }
    } else {
      c = b.headerText;
    }
    c = b.externalHeaderRenderer ? b.externalHeaderRenderer.call(this, ...arguments) : c;
    return b.showCheckAll ? void 0 : c;
  }
  updateCheckAllState(a) {
    if (this.headerCheckbox) {
      this.headerCheckbox.suspendEvents();
      this.headerCheckbox.checked = a;
      this.headerCheckbox.resumeEvents();
    }
  }
  onCheckAllChange({checked: a}) {
    const b = this;
    if (b.field) {
      const {store: c} = b.grid;
      c.beginBatch();
      c.forEach(e => b.updateRecord(e, b.field, a));
      c.endBatch();
    }
    b.trigger('toggleAll', { 'checked': a });
  }
  defaultRenderer(a) {
    let b;
    const {
        value: c,
        isExport: e,
        record: f
      } = a, g = super.defaultRenderer(a), i = (b = a.widgets) == null ? void 0 : b[0];
    if (e) {
      return g != null ? g : c == null ? '' : c;
    }
    i.readOnly = this.readOnly || f.readOnly && !this.meta.isSelectionColumn;
    if (c) {
      i.input.setAttribute('checked', true);
    } else {
      i.input.removeAttribute('checked');
    }
    return g;
  }
  onBeforeWidgetRender(a, b) {
    a.ion({
      'beforeChange': 'onBeforeCheckboxChange',
      'change': 'onCheckboxChange',
      'thisObj': this
    });
  }
  onAfterWidgetDerendered(a, b) {
    a.un({
      'beforeChange': 'onBeforeCheckboxChange',
      'change': 'onCheckboxChange',
      'thisObj': this
    });
  }
  internalOnBeforeWidgetSetValue(a, {cellElement: b}) {
    let c;
    b.widget = a;
    a.record = a.cellInfo.record;
    (c = this.externalOnBeforeWidgetSetValue) == null ? void 0 : c.call(this, ...arguments);
  }
  internalOnAfterWidgetSetValue(a) {
    let b;
    (b = this.externalOnAfterWidgetSetValue) == null ? void 0 : b.call(this, ...arguments);
  }
  onBeforeCheckboxChange({
    source: a,
    checked: b,
    userAction: c
  }) {
    const e = this, {grid: f} = e, {record: g} = a.cellInfo;
    if (c && e.field && (!f.features.cellEdit || f.features.cellEdit.disabled) || e.meta.isSelectionColumn && !f.isSelectable(g) && b) {
      return false;
    }
    return e.trigger('beforeToggle', {
      'record': g,
      'checked': b
    });
  }
  onCheckboxChange({
    source: a,
    checked: b
  }) {
    const c = this, {record: e} = a.cellInfo, {field: f} = c;
    if (f) {
      c.updateRecord(e, f, b);
      if (b) {
        c.updateCheckAllState(c.grid.store.every(g => g[f], null, true));
      } else {
        c.updateCheckAllState(false);
      }
    }
    c.trigger('toggle', {
      'record': e,
      'checked': b,
      'checkbox': a
    });
  }
  updateRecord(a, b, c) {
    a.setValue(b, c);
  }
  onCellKeyDown({
    event: a,
    cellElement: b,
    record: c
  }) {
    if (a.key === ' ') {
      const {target: e} = a, f = b.widget;
      if (e.matches('.b-grid-cell') && !((c == null ? void 0 : c.readOnly) || f.disabled || f.readOnly)) {
        f == null ? void 0 : f.toggle();
        a.preventDefault();
        a.handled = true;
      }
    }
  }
  getCurrentConfig(a) {
    const b = super.getCurrentConfig(a);
    delete b.onBeforeWidgetSetValue;
    delete b.onAfterWidgetSetValue;
    if (this.externalOnBeforeWidgetSetValue) {
      b.onBeforeWidgetSetValue = this.externalOnBeforeWidgetSetValue;
    }
    if (this.externalOnAfterWidgetSetValue) {
      b.onAfterWidgetSetValue = this.externalOnAfterWidgetSetValue;
    }
    return b;
  }
};
ColumnStore.registerColumnType(CheckColumn, true);
CheckColumn._$name = 'CheckColumn';
const ColorColumn = class extends Column {
  static defaults = {
    'align': 'center',
    'editor': null
  };
  static fields = [
    {
      'name': 'colorEditorType',
      'defaultValue': 'colorpicker'
    },
    'colors',
    {
      'name': 'addNoColorItem',
      'defaultValue': true
    }
  ];
  static type = 'color';
  static $name = 'ColorColumn';
  construct() {
    let a;
    super.construct(...arguments);
    const b = this, {grid: c} = b;
    b.menu = new Menu({
      'owner': c,
      'rootElement': c.rootElement,
      'autoShow': false,
      'align': 't50-b50',
      'anchor': true,
      'internalListeners': {
        'hide'() {
          b.picker.navigator.activeItem = null;
          delete b._editingRecord;
        }
      },
      'items': [Object.assign({
          'type': b.colorEditorType,
          'ref': 'list',
          'addNoColorItem': b.addNoColorItem,
          'colorSelected'({color: e}) {
            let f;
            (f = b._editingRecord) == null ? void 0 : f.set(b.field, e);
            b.menu.hide();
          }
        }, ((a = b.colors) == null ? void 0 : a.length) ? { 'colors': b.colors } : {})]
    });
  }
  applyValue(a, b, c) {
    if (!this.isConstructing) {
      const {picker: e} = this;
      if (b === 'colors') {
        e.colors = c;
      } else if (b === 'addNoColorItem') {
        e.addNoColorItem = c;
      }
    }
    super.applyValue(...arguments);
  }
  get picker() {
    return this.menu.widgetMap.list;
  }
  defaultRenderer({value: a}) {
    let b = 'b-empty', c = a;
    if (a) {
      const e = this.picker.getColorClassName(a);
      if (e) {
        b = e;
        c = null;
      } else {
        b = '';
      }
    }
    return {
      'className': 'b-color-cell-inner ' + b,
      'style': { 'backgroundColor': c },
      'data-btip': a
    };
  }
  onCellClick({
    grid: a,
    record: b,
    target: c
  }) {
    if (c.classList.contains('b-color-cell-inner') && !this.readOnly && !a.readOnly && !b.isSpecialRow && !b.readOnly) {
      const {
          picker: e,
          menu: f
        } = this, g = b.get(this.field);
      this._editingRecord = b;
      e.deselectAll();
      e.select(g);
      e.refresh();
      f.showBy(c);
    }
  }
};
ColumnStore.registerColumnType(ColorColumn);
ColorColumn._$name = 'ColorColumn';
const DateColumn = class extends Column {
  static fields = [
    'format',
    'pickerFormat',
    'step',
    'min',
    'max',
    'renderer'
  ];
  static fieldType = 'date';
  static type = 'date';
  static $name = 'DateColumn';
  static get defaults() {
    return {
      'format': 'L',
      'step': 1,
      'minWidth': 85,
      'filterType': 'date'
    };
  }
  construct(a = {}, b) {
    super.construct(a, b);
    if (this.grid) {
      const {field: c} = this, {store: e} = this.grid, {modelClass: f} = e, g = c && f.getFieldDefinition(c);
      if (g && !g.isDateDataField) {
        console.warn('DateColumn expects the `type` of the model field `' + c + '` to be of type `date`');
        const i = f.addField({
          ...g.getCurrentConfig(),
          'type': 'date',
          ['format' in a ? 'format' : void 0]: this.format
        });
        e.forEach(j => j.data[g.dataSource] = i.convert(j[c]));
      }
    }
  }
  defaultRenderer({value: a}) {
    return a ? this.formatValue(a) : '';
  }
  groupRenderer({
    cellElement: a,
    groupRowFor: b
  }) {
    a.innerHTML = this.formatValue(b);
  }
  formatValue(a) {
    if (typeof a === 'string') {
      a = DateHelper.parse(a, this.format || void 0);
    }
    return DateHelper.format(a, this.format || void 0);
  }
  set format(a) {
    const {editor: b} = this.data;
    this.set('format', a);
    if (b) {
      b.format = a;
    }
  }
  get format() {
    return this.get('format');
  }
  get defaultEditor() {
    const a = this, {
        min: b,
        max: c,
        step: e,
        format: f
      } = a;
    return {
      'name': a.field,
      'type': 'date',
      'calendarContainerCls': 'b-grid-cell-editor-related',
      'weekStartDay': a.grid.weekStartDay,
      'format': f,
      'max': c,
      'min': b,
      'step': e
    };
  }
};
ColumnStore.registerColumnType(DateColumn, true);
DateColumn.exposeProperties();
DateColumn._$name = 'DateColumn';
const PercentColumn = class extends NumberColumn {
  static defaults = {
    'min': 0,
    'max': 100,
    'step': 1,
    'align': null,
    'showCircle': false,
    'mode': 'bar',
    'showValue': false,
    'lowThreshold': 0,
    'htmlEncode': false,
    'searchable': false,
    'summaryRenderer': ({sum: a}) => a + '%',
    'fitMode': false
  };
  static fields = [
    'showValue',
    'lowThreshold',
    'showCircle',
    'mode'
  ];
  static type = 'percent';
  circleHeightPercentage = 0.75;
  constructor() {
    super(...arguments);
  }
  construct() {
    super.construct(...arguments);
    if (this.showCircle) {
      VersionHelper.deprecate('core', '7.0.0', 'PercentColumn `showCircle` is deprecated, in favor of `mode`');
      this.mode = 'circle';
    }
  }
  defaultRenderer({
    value: a,
    record: b,
    isExport: c
  }) {
    let e, f;
    a = a || 0;
    const g = this, i = g.formatValue(a, b) || a;
    if (c) {
      return i;
    } else if (g.mode === 'number') {
      return i + '%';
    }
    const {
      min: j,
      max: k
    } = g;
    if (g.mode === 'circle') {
      const l = Math.round(g.circleHeightPercentage * g.grid.rowHeight) + 'px';
      return {
        'tabIndex': 0,
        'role': 'progressbar',
        'aria-valuemin': j,
        'aria-valuemax': k,
        'aria-valuenow': a,
        'aria-label': (e = g.formatAriaLabel) == null ? void 0 : e.call(g, a, b),
        'className': {
          'b-percentdone-circle': 1,
          'b-empty': a === 0,
          'b-full': a === 100,
          'b-over': a > 100
        },
        'style': {
          'height': l,
          'width': l,
          '--grid-percent-circle-angle': a / 100 + 'turn'
        },
        'dataset': { 'value': i }
      };
    }
    return {
      'className': 'b-percent-bar-outer',
      'role': 'progressbar',
      'aria-valuemin': j,
      'aria-valuemax': k,
      'aria-valuenow': a,
      'aria-label': (f = g.formatAriaLabel) == null ? void 0 : f.call(g, a, b),
      'tabIndex': 0,
      'children': [{
          'tag': 'div',
          'className': {
            'b-percent-bar': 1,
            'b-zero': a === 0,
            'b-low': a < g.lowThreshold,
            'b-full': a === 100,
            'b-over': a > 100
          },
          'style': { 'width': a + '%' },
          'children': [g.showValue ? {
              'tag': 'span',
              'text': i + '%'
            } : void 0]
        }]
    };
  }
  resizeToFitContent() {
    if (this.mode === 'number') {
      return super.resizeToFitContent(...arguments);
    }
  }
  get align() {
    const a = this.get('align'), {mode: b} = this;
    if (a) {
      return a;
    }
    if (b === 'number') {
      return 'end';
    }
    if (b === 'circle') {
      return 'center';
    }
    return 'start';
  }
  set align(a) {
    this.set('align', a);
  }
  get htmlEncode() {
    return this.mode === 'number';
  }
  get internalCellCls() {
    return this.mode === 'bar' ? 'b-percent-bar-cell' : '';
  }
};
PercentColumn.sum = 'average';
ColumnStore.registerColumnType(PercentColumn, true);
PercentColumn._$name = 'PercentColumn';
const RatingColumn = class extends NumberColumn {
  static fields = [
    'emptyIcon',
    'filledIcon',
    'editable'
  ];
  static fieldType = 'number';
  static type = 'rating';
  static $name = 'RatingColumn';
  static get defaults() {
    return {
      'min': 0,
      'max': 5,
      'emptyIcon': 'b-icon b-icon-star',
      'filledIcon': 'b-icon b-icon-star',
      'editable': true,
      'filterType': 'number',
      'searchable': false,
      'width': '11.2em',
      'htmlEncode': false,
      'minWidth': '11.2em',
      'editor': false,
      'fitMode': 'value'
    };
  }
  defaultRenderer({
    value: a,
    isExport: b
  }) {
    if (b) {
      return '\u2605'.repeat(a);
    }
    return {
      'className': {
        'b-rating-cell-inner': 1,
        'b-not-editable': !this.editable
      },
      'children': ArrayHelper.populate(this.max, c => {
        const e = c < a;
        return {
          'tag': 'i',
          'className': {
            'b-rating-icon': true,
            'b-filled': e,
            'b-empty': !e,
            [e ? this.filledIcon : this.emptyIcon]: true
          }
        };
      })
    };
  }
  onCellClick({
    grid: a,
    column: b,
    record: c,
    target: e,
    event: f
  }) {
    if (e.classList.contains('b-rating-icon') && !a.readOnly && b.editable) {
      let g = [].indexOf.call(e.parentNode.childNodes, e);
      if (e.classList.contains('b-filled') && (f.metaKey || f.shiftKey)) {
        g = g - 1;
      }
      if (c.getValue(b.field) === 1 && g === 0) {
        g = -1;
      }
      c.set(b.field, g + 1);
    }
  }
};
ColumnStore.registerColumnType(RatingColumn, true);
RatingColumn.exposeProperties();
RatingColumn._$name = 'RatingColumn';
const RowNumberColumn = class extends Column {
  static type = 'rownumber';
  static $name = 'RowNumberColumn';
  static get defaults() {
    return {
      'groupable': false,
      'sortable': false,
      'filterable': false,
      'searchable': false,
      'resizable': false,
      'draggable': false,
      'mergeable': false,
      'minWidth': 50,
      'width': 50,
      'align': 'right',
      'text': '#',
      'editor': false,
      'readOnly': true
    };
  }
  construct(a) {
    super.construct(...arguments);
    const b = this, {grid: c} = b;
    b.internalCellCls = 'b-row-number-cell';
    b.externalHeaderRenderer = b.headerRenderer;
    b.setData('headerRenderer', b.internalHeaderRenderer);
    if (c) {
      c.ion({
        'bindStore': 'bindStore',
        'thisObj': b
      });
      b.bindStore({
        'store': c.store,
        'initial': true
      });
      if (c.store.count && !c.rendered) {
        c.ion({
          'paint': 'resizeToFitContent',
          'thisObj': b,
          'once': true
        });
      }
    }
  }
  get groupHeaderReserved() {
    return true;
  }
  bindStore({
    store: a,
    initial: b
  }) {
    const c = this;
    c.detachListeners('grid');
    a.ion({
      'name': 'grid',
      ['change' + c.grid.asyncEventSuffix]: 'onStoreChange',
      'thisObj': c
    });
    if (!b && !c.resizeToFitContent()) {
      c.measureOnRender();
    }
  }
  onStoreChange({
    action: a,
    isMove: b
  }) {
    if (a === 'dataset' || a === 'add' || a === 'remove' || a === 'removeall') {
      if (a === 'remove' && b) {
        return;
      }
      const c = this.resizeToFitContent();
      if (a === 'dataset' && !c && this.grid.store.count) {
        this.measureOnRender();
      }
    }
  }
  measureOnRender() {
    this.grid.rowManager.ion({
      'renderDone'() {
        this.resizeToFitContent();
      },
      'once': true,
      'thisObj': this
    });
  }
  defaultRenderer({
    record: a,
    grid: b
  }) {
    let c, e, f;
    let g = a.isSpecialRow ? '' : b.store.indexOf(a, true) + 1;
    if ((c = b.splitFrom) == null ? void 0 : c.isLockedRows) {
      g += (f = (e = b.splitFrom.features.lockRows) == null ? void 0 : e.numberOfLockedRows) != null ? f : 0;
    }
    return g;
  }
  resizeToFitContent() {
    let a;
    const b = this, c = (a = b.grid.features.lockRows) == null ? void 0 : a.subViews, e = (c == null ? void 0 : c[1]) === b.grid ? c[0] : b.grid, {element: f} = e, {store: g} = e, {count: i} = g;
    if (i && !b.hidden) {
      const j = f.querySelector('.b-grid-cell[data-column-id="' + b.id + '"]');
      if (j) {
        const k = j.isConnected ? parseInt(DomHelper.getStyleValue(j, 'padding-left')) : b._cachedCellPadding || 0, l = DomHelper.measureText(i, j);
        b.width = Math.max(b.minWidth, l + 2 * k);
        b._cachedCellPadding = k;
        return true;
      }
    }
    return false;
  }
  set flex(a) {
  }
  internalHeaderRenderer({
    headerElement: a,
    column: b
  }) {
    let c;
    a.classList.add('b-rownumber-header');
    return ((c = b.externalHeaderRenderer) == null ? void 0 : c.call(this, ...arguments)) || b.headerText;
  }
};
ColumnStore.registerColumnType(RowNumberColumn, true);
RowNumberColumn._$name = 'RowNumberColumn';
const TemplateColumn = class extends Column {
  static fields = ['template'];
  static type = 'template';
  static get defaults() {
    return { 'htmlEncode': false };
  }
  constructor(a, b) {
    super(...arguments);
    const c = this;
    if (!c.template) {
      throw new Error('TemplateColumn needs a template');
    }
    if (typeof c.template !== 'function') {
      throw new Error('TemplateColumn.template must be a function');
    }
  }
  defaultRenderer(a) {
    if (!a.record.isSpecialRow) {
      return this.template({
        'value': a.value,
        'record': a.record,
        'field': this.field
      });
    }
  }
};
ColumnStore.registerColumnType(TemplateColumn, true);
TemplateColumn.exposeProperties();
TemplateColumn._$name = 'TemplateColumn';
const TimeColumn = class extends Column {
  static fields = ['format'];
  static fieldType = 'date';
  static type = 'time';
  static $name = 'TimeColumn';
  static get defaults() {
    return {
      'format': 'LT',
      'minWidth': 140,
      'filterType': 'time'
    };
  }
  defaultRenderer({value: a}) {
    return a ? this.formatValue(a) : '';
  }
  groupRenderer({
    cellElement: a,
    groupRowFor: b
  }) {
    a.innerHTML = this.formatValue(b);
  }
  formatValue(a) {
    if (typeof a === 'string') {
      a = DateHelper.parse(a, this.format);
    }
    return DateHelper.format(a, this.format);
  }
  set format(a) {
    const {editor: b} = this;
    this.set('format', a);
    if (b) {
      b.format = a;
    }
  }
  get format() {
    return this.get('format');
  }
  get defaultEditor() {
    return {
      'name': this.field,
      'type': 'time',
      'format': this.format
    };
  }
};
ColumnStore.registerColumnType(TimeColumn, true);
TimeColumn.exposeProperties();
TimeColumn._$name = 'TimeColumn';
const TreeColumn = class extends Column {
  static type = 'tree';
  static $name = 'TreeColumn';
  static get defaults() {
    return {
      'tree': true,
      'hideable': false,
      'minWidth': 150
    };
  }
  static get fields() {
    return [
      {
        'name': 'expandIconCls',
        'defaultValue': 'b-icon b-icon-tree-expand'
      },
      {
        'name': 'collapseIconCls',
        'defaultValue': 'b-icon b-icon-tree-collapse'
      },
      { 'name': 'collapsedFolderIconCls' },
      { 'name': 'expandedFolderIconCls' },
      {
        'name': 'indentSize',
        'defaultValue': 1.7
      },
      {
        'name': 'leafIconCls',
        'defaultValue': 'b-icon b-icon-tree-leaf'
      },
      {
        'name': 'editTargetSelector',
        'defaultValue': '.b-tree-cell-value'
      },
      'renderer'
    ];
  }
  construct(a, b, ...c) {
    super.construct(a, b, ...c);
    const e = this;
    e.shouldHtmlEncode = e.htmlEncode;
    e.setData('htmlEncode', false);
    const {renderer: f} = e;
    if (f) {
      e.originalRenderer = f;
    }
    e.renderer = e.treeRenderer.bind(e);
  }
  treeRenderer(a) {
    let b, c, e, f, g, i, j;
    const k = this, {
        grid: l,
        cellElement: n,
        row: o,
        record: p,
        isExport: q
      } = a, r = p.instanceMeta(l.store), u = !p.isLeaf && r.collapsed, v = { 'className': 'b-tree-cell-value' }, w = [v], z = {
        'className': { 'b-tree-cell-inner': 1 },
        'tag': p.href ? 'a' : 'div',
        'href': p.href,
        'target': p.target,
        'children': w
      };
    let A, B, {value: C} = a, E = k;
    const F = l.isTreeGrouped && !p.isLeaf && l.features.treeGroup.parentRenderer;
    if (k.originalRenderer || F) {
      let H;
      if (F) {
        if (p.field) {
          E = l.columns.get(p.field);
          if (E) {
            C = E.isWidgetColumn ? C : (c = (b = E === k ? E.originalRenderer : E.renderer) == null ? void 0 : b.call(E, {
              ...a,
              'column': E,
              'value': p.name,
              'isTreeGroup': true
            })) != null ? c : p.name;
          } else {
            C = p.name;
          }
        }
        H = l.features.treeGroup.parentRenderer({
          'field': p.field,
          'value': C,
          'column': E,
          'record': p.firstGroupChild,
          'grid': l
        });
      } else {
        H = k.originalRenderer(a);
      }
      const I = (e = l.hasFrameworkRenderer) == null ? void 0 : e.call(l, {
        'cellContent': H,
        'renderingColumn': E
      });
      A = typeof H === 'object' && !I;
      C = I ? '' : H === false ? n.innerHTML : H;
      a.rendererHtml = H;
    }
    if (!A) {
      C = String(C != null ? C : '');
    }
    if (q) {
      return C;
    }
    if (!p.isLeaf) {
      const J = !p.isExpanded(l.store), K = J ? k.expandIconCls : k.collapseIconCls, L = J ? k.collapsedFolderIconCls : k.expandedFolderIconCls;
      n.classList.add('b-tree-parent-cell');
      o.toggleCls('b-loading-children', Boolean(r.isLoadingChildren));
      w.unshift({
        'tag': 'i',
        'className': {
          'b-tree-expander': 1,
          [K]: 1,
          'b-empty-parent': !r.isLoadingChildren && (p.children !== true && !((f = p.children) == null ? void 0 : f.length))
        }
      });
      p.meta.hasIcon = B = a.iconCls || p.iconCls || L;
    } else {
      n.classList.add('b-tree-leaf-cell');
      B = a.iconCls || p.iconCls || k.leafIconCls;
    }
    if (B) {
      w.splice(w.length - 1, 0, {
        'tag': 'i',
        'className': {
          'b-tree-icon': 1,
          [B]: 1
        }
      });
    }
    if (o.isRow && !p.isLeaf) {
      o.setAttribute('aria-expanded', !u);
      if (u) {
        o.removeAttribute('aria-owns');
      } else {
        for (const N in l.subGrids) {
          const O = o.elements[N];
          DomHelper.setAttributes(O, { 'aria-owns': ((g = p.children) == null ? void 0 : g.length) ? (i = p.children) == null ? void 0 : i.map(P => l.id + '-' + N + '-' + P.id).join(' ') : null });
        }
      }
    }
    if (Array.isArray(C)) {
      v.children = C;
    } else if (A) {
      Object.assign(v, C);
    } else if ((E == null ? void 0 : E.shouldHtmlEncode) || !C.includes('<')) {
      z.className['b-text-value'] = 1;
      v.text = C;
    } else {
      v.html = C;
    }
    let G = p.childLevel * k.indentSize;
    if (p.isLeaf) {
      G += ((j = p.parent) == null ? void 0 : j.meta.hasIcon) ? 2.2 : B ? 0.5 : 0.4;
    } else {
      G += B ? -0.15 : 0;
    }
    z.style = 'padding-inline-start:' + G + 'em';
    return z;
  }
  getCurrentConfig(a) {
    const b = super.getCurrentConfig(a);
    b.renderer = this.originalRenderer;
    return b;
  }
};
ColumnStore.registerColumnType(TreeColumn, true);
TreeColumn.exposeProperties();
TreeColumn._$name = 'TreeColumn';
const GridRowModel = class extends Model {
  static get fields() {
    return [
      {
        'name': 'iconCls',
        'internal': true
      },
      {
        'name': 'cls',
        'internal': true
      },
      {
        'name': 'rowHeight',
        'internal': true
      },
      {
        'name': 'href',
        'internal': true
      },
      {
        'name': 'target',
        'internal': true
      },
      {
        'name': 'fixed',
        'type': 'boolean',
        'internal': true
      }
    ];
  }
};
GridRowModel.exposeProperties();
GridRowModel._$name = 'GridRowModel';
const CopyPasteBase = class extends InstancePlugin.mixin(Clipboardable_default) {
  static configurable = {
    'copyOnly': null,
    'keyMap': {
      'Ctrl+C': 'copy',
      'Ctrl+X': 'cut',
      'Ctrl+V': 'paste'
    },
    'useNativeClipboard': false,
    'toCopyString': null,
    'toPasteValue': null,
    'emptyValueChar': ' ',
    'dateFormat': 'lll',
    'generateNewName'(a, b, c = this.client.store) {
      const {nameField: e} = this;
      let f = a.getValue(e), g = 2;
      if ((b == null ? void 0 : b.meta.generatedName) === f) {
        f = b.meta.originalName;
      }
      let i = f;
      while (c.findRecord(e, i)) {
        i = f + ' - ' + g;
        g += 1;
      }
      a.meta.generatedName = i;
      a.meta.originalName = f;
      return i;
    }
  };
  get clipboardRecords() {
    return this.clipboardData || [];
  }
  cellsToString(a) {
    let b;
    const c = this;
    let e = 0, f = 0, g = '';
    a.sort((i, j) => i.rowIndex === j.rowIndex ? i.columnIndex - j.columnIndex : i.rowIndex - j.rowIndex);
    for (const i of a) {
      const {
        record: j,
        _column: k,
        rowIndex: l,
        columnIndex: n
      } = i;
      if (l > e) {
        if (g.length > 0) {
          g += '\n'.repeat(l - e);
        }
        e = l;
        f = n;
      } else if (n > f) {
        if (g.length > 0) {
          g += '\t'.repeat(n - f);
        }
        f = n;
      }
      let o = (b = k == null ? void 0 : k.toClipboardString) == null ? void 0 : b.call(k, i);
      if (o === void 0) {
        o = j.getValue(k.field);
        if (o instanceof Date) {
          o = DateHelper.format(o, c.dateFormat);
        } else {
          o = o == null ? void 0 : o.toString();
        }
      }
      if (c.toCopyString) {
        o = c.toCopyString({
          'currentValue': o,
          'column': k,
          'record': j
        });
      }
      o = o == null ? void 0 : o.replace(/[\n\t]/, ' ');
      g += o || c.emptyValueChar;
    }
    return g;
  }
  setFromStringData(a, b = false, c = this.client.store, e) {
    let f, g;
    const i = this, {client: j} = i, {
        columns: k,
        _shiftSelectRange: l
      } = j, n = new Set(), o = i.stringAs2dArray(a), p = (f = j.selectedCells) == null ? void 0 : f[0], q = [], r = [];
    if (!b && (l == null ? void 0 : l.some(u => u.equals(p)))) {
      const u = i.cellSelectorsAs2dArray(l);
      if ((u == null ? void 0 : u.length) % o.length === 0 && u.columnCount % o.columnCount === 0) {
        for (let v = 0; v < u.length; v += o.length) {
          for (let w = 0; w < u.columnCount; w += o.columnCount) {
            q.push(u[v][w]);
          }
        }
      }
    }
    if (!q.length) {
      q.push(p);
    }
    for (const z of q) {
      for (let A = 0; A < o.length; A++) {
        const B = o[A], C = b ? new c.modelClass() : c.getAt(z.rowIndex + A);
        if (C && !C.readOnly) {
          for (let E = 0; E < B.length; E++) {
            const F = e ? null : k.visibleColumns[b ? E : z.columnIndex + E], G = (F == null ? void 0 : F.field) || (e == null ? void 0 : e[E]);
            let H = B[E];
            if (G && H && !(F == null ? void 0 : F.readOnly)) {
              if (H === i.emptyValueChar) {
                H = null;
              }
              if (F == null ? void 0 : F.fromClipboardString) {
                H = F.fromClipboardString({
                  'string': H,
                  'record': C
                });
              }
              if (i.toPasteValue) {
                H = i.toPasteValue({
                  'currentValue': H,
                  'record': C,
                  'column': F,
                  'field': G
                });
              }
              if (typeof H === 'string' && ((g = C.getFieldDefinition(G)) == null ? void 0 : g.isDateDataField)) {
                const I = DateHelper.parse(H, i.dateFormat);
                if (!isNaN(I.getTime())) {
                  H = I;
                }
              }
              C.set(G, H, false, false, false, true);
              if (F) {
                r.push(j.normalizeCellContext({
                  'column': F,
                  'record': C
                }));
              }
            }
          }
          n.add(C);
        }
      }
    }
    return {
      'modifiedRecords': [...n],
      'targetCells': r
    };
  }
  cellSelectorsAs2dArray(a) {
    const b = [];
    let c = null, e;
    for (const f of a) {
      if (f.id !== c) {
        c = f.id;
        e = [];
        b.push(e);
      }
      e.push(f);
    }
    b.columnCount = b[0].length;
    if (b.some(g => g.length !== b.columnCount)) {
      return false;
    }
    return b;
  }
  stringAs2dArray(a) {
    const b = [], c = a.split(/\r\n|(?!\r\n)[\n-\r\x85\u2028\u2029]/).filter(e => e.length);
    for (const e of c) {
      const f = e.split('\t');
      if (b.columnCount && f.length !== b.columnCount) {
        return false;
      }
      b.columnCount = f.length;
      b.push(f);
    }
    return b;
  }
};
CopyPasteBase._$name = 'CopyPasteBase';
const consumerToFeatureMap = new Map();
const consumerToDefaultFeatureMap = new Map();
const DEFAULT_FOR_TYPE = 'Grid';
const remapToBase = {
  'Grid': 'GridBase',
  'Scheduler': 'SchedulerBase',
  'SchedulerPro': 'SchedulerProBase',
  'Gantt': 'GanttBase'
};
const classNameFix = /\$\d+$/;
const GridFeatureManager = class {
  static registerFeature(a, b = false, c = null, e = null) {
    e = StringHelper.uncapitalize(e || Object.prototype.hasOwnProperty.call(a, '$name') && a.$$name || a.name);
    e = e.replace(classNameFix, '');
    if (!Array.isArray(c)) {
      c = [c || DEFAULT_FOR_TYPE];
    }
    c.forEach(f => {
      const g = remapToBase[f] || f, i = consumerToFeatureMap.get(g) || new Map(), j = consumerToDefaultFeatureMap.get(g) || new Map();
      i.set(e, a);
      j.set(a, b);
      consumerToFeatureMap.set(g, i);
      consumerToDefaultFeatureMap.set(g, j);
    });
  }
  static getTypeNameFeatures(a = DEFAULT_FOR_TYPE) {
    const b = remapToBase[a] || a, c = consumerToFeatureMap.get(b), e = {};
    if (c) {
      c.forEach((f, g) => e[g] = f);
    }
    return e;
  }
  static getTypeNameDefaultFeatures(a = DEFAULT_FOR_TYPE) {
    const b = remapToBase[a] || a, c = consumerToFeatureMap.get(b), e = consumerToDefaultFeatureMap.get(b);
    const f = {};
    if (c && e) {
      c.forEach((g, i) => {
        if (e.get(g)) {
          f[i] = g;
        }
      });
    }
    return f;
  }
  static getInstanceFeatures(a) {
    return a.$meta.names.reduce((b, c) => Object.assign(b, this.getTypeNameFeatures(c)), {});
  }
  static getInstanceDefaultFeatures(a) {
    return a.$meta.names.reduce((b, c) => Object.entries(this.getTypeNameFeatures(c)).reduce((e, [f, g]) => {
      if (this.isDefaultFeatureForTypeName(g, c)) {
        e[f] = g;
      } else {
        delete e[f];
      }
      return e;
    }, b), {});
  }
  static isDefaultFeatureForTypeName(a, b = DEFAULT_FOR_TYPE) {
    const c = remapToBase[b] || b, e = consumerToDefaultFeatureMap.get(c);
    return e && e.get(a) || false;
  }
  static isDefaultFeatureForInstance(a, b) {
    const c = b.$meta.names.slice().reverse();
    let e = null;
    for (let f = 0, g = c.length; f < g && e === null; ++f) {
      const j = consumerToDefaultFeatureMap.get(c[f]);
      if (j && j.has(a)) {
        e = j.get(a);
      }
    }
    return e || false;
  }
  static reset() {
    consumerToFeatureMap.clear();
    consumerToDefaultFeatureMap.clear();
  }
};
const actions = {
  'cut': 1,
  'copy': 1,
  'paste': 1
};
const CellCopyPaste = class extends CopyPasteBase {
  static configurable = {
    'useNativeClipboard': !VersionHelper.isTestEnv,
    'copyText': 'L{copy}',
    'cutText': 'L{cut}',
    'pasteText': 'L{paste}',
    'allowedDataSources': 'none'
  };
  static $name = 'CellCopyPaste';
  entityName = 'cell';
  constructor() {
    super(...arguments);
  }
  static get pluginConfig() {
    return {
      'chain': [
        'populateCellMenu',
        'afterSelectionModeChange'
      ]
    };
  }
  afterConstruct() {
    super.afterConstruct();
    this.afterSelectionModeChange();
  }
  afterSelectionModeChange() {
    const a = this;
    if (!a.client.selectionMode.cell) {
      a.disabled = true;
    } else if (a._disabledBySelectionMode) {
      a.disabled = false;
      delete a._disabledBySelectionMode;
    }
  }
  set copyOnly(a) {
    this._copyOnly = a;
  }
  get copyOnly() {
    let a;
    if (this._copyOnly == null) {
      return !((a = this.client.features.cellEdit) == null ? void 0 : a.enabled);
    }
    return Boolean(this._copyOnly);
  }
  get canCopy() {
    let a;
    const {client: b} = this;
    return Boolean(!this.disabled && b.selectedCells.length && (!b._selectedRows.length || ((a = b.features.rowCopyPaste) == null ? void 0 : a.disabled) || b.focusedCell && b.isCellSelected(b.focusedCell)));
  }
  get canCutPaste() {
    let a;
    return this.canCopy && !this.copyOnly && !((a = this.client.features.cellEdit) == null ? void 0 : a.isEditing) && !this.client.readOnly;
  }
  isActionAvailable({actionName: a}) {
    if (actions[a]) {
      return globalThis.getSelection().toString().length === 0 && this.canCopy && (a === 'copy' || this.canCutPaste);
    }
  }
  async cut() {
    await this.copy(true);
  }
  async copy(a = false) {
    if (typeof a != 'boolean') {
      a = false;
    }
    const b = this, {selectedCells: c} = b.client, e = a ? c.filter(f => {
        let g;
        return !((g = f.record) == null ? void 0 : g.readOnly);
      }) : c;
    if (e) {
      if (a ? !b.canCutPaste : !b.canCopy) {
        return;
      }
      const f = b.cellsToString(e);
      if (await b.writeToClipboard(f, a, { 'cells': e }) === void 0) {
        return;
      }
      if (a === true) {
        for (const g of e) {
          const {
            column: i,
            record: j
          } = g;
          if (!i.readOnly) {
            let k = null;
            if (i.fromClipboardString) {
              k = i.fromClipboardString({
                'string': k,
                'record': j
              });
            }
            g.record.set(g.column.field, k);
          }
        }
      }
      b.client.trigger('copy', {
        'cells': e,
        'copiedDataString': f,
        'isCut': a,
        'entityName': b.entityName
      });
    }
  }
  async beforeCopy({
    data: a,
    isCut: b,
    cells: c
  }) {
    return await this.client.trigger('beforeCopy', {
      'cells': c,
      'data': a,
      'isCut': b,
      'entityName': this.entityName
    });
  }
  async paste() {
    let a;
    const b = this, {
        client: c,
        entityName: e
      } = b, f = c.selectedCells[0];
    if (!b.canCutPaste || !f) {
      return;
    }
    const g = (a = await b.readFromClipboard({}, true)) == null ? void 0 : a.data;
    if (!g) {
      return;
    }
    const {
      modifiedRecords: i,
      targetCells: j
    } = b.setFromStringData(g);
    if (c.selectedCells.length === 1 && j.length > 1) {
      c.selectCellRange(j[0], j[j.length - 1]);
    }
    c.trigger('paste', {
      'clipboardData': g,
      'targetCell': f,
      'modifiedRecords': [...i],
      'entityName': e
    });
  }
  async beforePaste({data: a}) {
    return await this.client.trigger('beforePaste', {
      'clipboardData': a,
      'targetCell': this.client.selectedCell,
      'entityName': this.entityName
    });
  }
  populateCellMenu({
    record: a,
    items: b
  }) {
    const c = this;
    if (c.canCopy) {
      b.cutCell = {
        'text': c.cutText,
        'localeClass': c,
        'icon': 'b-icon b-icon-cut',
        'weight': 115,
        'disabled': a.readOnly || !c.canCutPaste,
        'onItem': () => c.cut()
      };
      b.pasteCell = {
        'text': c.pasteText,
        'localeClass': c,
        'icon': 'b-icon b-icon-paste',
        'weight': 120,
        'disabled': a.readOnly || !c.canCutPaste || c.hasClipboardData() === false,
        'onItem': () => c.paste()
      };
      b.copyCell = {
        'text': c.copyText,
        'localeClass': c,
        'cls': 'b-separator',
        'icon': 'b-icon b-icon-copy',
        'weight': 110,
        'onItem': () => c.copy()
      };
    }
  }
};
CellCopyPaste._$name = 'CellCopyPaste';
GridFeatureManager.registerFeature(CellCopyPaste);
const editingActions = {
  'finishAndEditNextRow': 1,
  'finishAndEditPrevRow': 1,
  'finishEditing': 1,
  'cancelEditing': 1,
  'finishAndEditNextCell': 1,
  'finishAndEditPrevCell': 1
};
const allActions = {
  ...editingActions,
  'startEditing': 1,
  'finishAllSelected': 1
};
const CellEdit = class extends Delayable_default(InstancePlugin) {
  static $name = 'CellEdit';
  static get defaultConfig() {
    return {
      'autoSelect': true,
      'blurAction': 'complete',
      'continueEditingOnCellClick': true,
      'addNewAtEnd': null,
      'addToCurrentParent': false,
      'autoEdit': null,
      'editNextOnEnterPress': true,
      'editorClass': Editor,
      'triggerEvent': 'celldblclick',
      'touchEditDelay': 300,
      'focusCellAnimationDuration': false,
      'multiEdit': true,
      'keyMap': {
        'Enter': [
          'startEditing',
          'finishAndEditNextRow'
        ],
        'Ctrl+Enter': [
          'finishAllSelected',
          'finishEditing'
        ],
        'Shift+Enter': 'finishAndEditPrevRow',
        'Alt+Enter': 'finishEditing',
        'F2': [
          'startEditing',
          'finishEditing'
        ],
        'Escape': 'cancelEditing',
        'Tab': {
          'handler': 'finishAndEditNextCell',
          'weight': 100
        },
        'Shift+Tab': {
          'handler': 'finishAndEditPrevCell',
          'weight': 100
        }
      },
      'ignoreCSSSelector': 'button,.b-icon,.b-fa,svg',
      'validateTapOut': null
    };
  }
  static get pluginConfig() {
    return {
      'assign': [
        'startEditing',
        'finishEditing',
        'cancelEditing'
      ],
      'before': [
        'onElementKeyDown',
        'onElementPointerUp'
      ],
      'chain': [
        'onElementClick',
        'bindStore'
      ]
    };
  }
  construct(a, b) {
    super.construct(a, b);
    const c = this, e = {
        'renderRows': 'onGridRefreshed',
        'cellClick': 'onCellClick',
        'thisObj': c
      };
    c.grid = a;
    if (c.triggerEvent !== 'cellclick') {
      e[c.triggerEvent] = 'onTriggerEditEvent';
    }
    if (c.autoEdit && !('editNextOnEnterPress' in b)) {
      c.editNextOnEnterPress = false;
    }
    a.ion(e);
    a.rowManager.ion({
      'changeTotalHeight': 'onGridRefreshed',
      'thisObj': c
    });
    c.bindStore(a.store);
  }
  bindStore(a) {
    this.detachListeners('store');
    a.ion({
      'name': 'store',
      'update': 'onStoreUpdate',
      'remove': 'onStoreRemove',
      'beforeSort': 'onStoreBeforeSort',
      'thisObj': this
    });
  }
  async confirm(a) {
    let b = true;
    if (this.editorContext) {
      MessageDialog_default.owner = this.editorContext.editor.inputField;
      a.rootElement = this.grid.rootElement;
      b = await MessageDialog_default.confirm(a);
      MessageDialog_default.owner = null;
    }
    return b === MessageDialog_default.yesButton;
  }
  doDestroy() {
    this.grid.columns.allRecords.forEach(a => {
      let b;
      (b = a._cellEditor) == null ? void 0 : b.destroy();
    });
    super.doDestroy();
  }
  doDisable(a) {
    if (a && !this.isConfiguring) {
      this.cancelEditing(true);
    }
    super.doDisable(a);
  }
  set disabled(a) {
    super.disabled = a;
  }
  get disabled() {
    const {grid: a} = this;
    return Boolean(super.disabled || a.disabled || a.readOnly);
  }
  get isEditing() {
    return Boolean(this.editorContext);
  }
  get activeRecord() {
    let a;
    return ((a = this.editorContext) == null ? void 0 : a.record) || null;
  }
  getEditorForCell({
    id: a,
    cell: b,
    column: c,
    columnId: e,
    editor: f
  }) {
    const g = this, {
        grid: i,
        editorClass: j
      } = g;
    let k = c.cellEditor, l = 0;
    if (c.editTargetSelector) {
      const p = b.querySelector(c.editTargetSelector);
      l = p.offsetLeft;
    }
    f.autoSelect = g.autoSelect;
    if (!(k == null ? void 0 : k.isEditor)) {
      k = c.data.cellEditor = j.create(j.mergeConfigs({
        'type': j.type,
        'constrainTo': null,
        'cls': 'b-cell-editor',
        'inputField': f,
        'blurAction': 'none',
        'invalidAction': c.invalidAction,
        'completeKey': false,
        'cancelKey': false,
        'owner': i,
        'align': {
          'align': 't0-t0',
          'offset': [
            l,
            0
          ]
        },
        'internalListeners': g.getEditorListeners(c.managedCellEditing),
        'onInternalKeyDown': g.onEditorKeydown.bind(g),
        'allowMouseEvents': f.allowMouseEvents,
        'instantUpdate': c.instantUpdate
      }, k));
    }
    k.minHeight = i.rowHeight;
    if (k.inputField !== f) {
      k.remove(k.items[0]);
      k.add(f);
    }
    const n = k.inputField, o = c.instantUpdate && !f.cellEditValueSetter;
    k.instantUpdate = o;
    k.align.offset[0] = l;
    if (o) {
      ObjectHelper.wrapProperty(f, 'value', null, q => {
        const {editorContext: r} = g;
        if (r) {
          const {record: u} = r, {field: v} = r.column;
          if (r.editor.isValid && !ObjectHelper.isEqual(u.getValue(v), q)) {
            if (!(n == null ? void 0 : n.isDateField) || (n == null ? void 0 : n._isPickerInput)) {
              u.setValue(v, q);
              n._outstandingUpdate = false;
            } else {
              n._outstandingUpdate = true;
            }
          }
        }
      });
      f.cellEditValueSetter = true;
    }
    Object.assign(k.element.dataset, {
      'rowId': a,
      'columnId': e,
      'field': c.field
    });
    k.inputField.revertOnEscape = c.revertOnEscape;
    return g.editor = k;
  }
  getEditorListeners(a) {
    return {
      'focusOut': a ? 'onEditorFocusOut' : null,
      'focusIn': 'onEditorFocusIn',
      'start': 'onEditorStart',
      'beforeComplete': 'onEditorBeforeComplete',
      'complete': 'onEditorComplete',
      'beforeCancel': 'onEditorBeforeCancel',
      'cancel': 'onEditorCancel',
      'beforeHide': 'onBeforeEditorHide',
      'finishEdit': 'onEditorFinishEdit',
      'thisObj': this
    };
  }
  onEditorStart({source: a}) {
    let b;
    const c = this, e = c.editorContext = a.cellEditorContext;
    if (e) {
      const {grid: f} = c;
      if (c.triggerEvent !== 'cellclick') {
        c.detachListeners('cellClickWhileEditing');
        f.ion({
          'name': 'cellClickWhileEditing',
          'cellclick': 'onCellClickWhileEditing',
          'thisObj': c
        });
      }
      (b = c.removeEditingListeners) == null ? void 0 : b.call(c);
      if (e.column.managedCellEditing) {
        c.removeEditingListeners = GlobalEvents_default.addListener({
          'globaltap': 'onTapOut',
          'thisObj': c
        });
      }
      f.trigger('startCellEdit', {
        'grid': f,
        'editorContext': e
      });
    }
  }
  onEditorBeforeComplete(a) {
    const {grid: b} = this, c = a.source, e = c.cellEditorContext;
    a.grid = b;
    a.editorContext = e;
    return b.trigger('beforeFinishCellEdit', a);
  }
  onEditorComplete({source: a}) {
    let b;
    const {grid: c} = this, e = a.cellEditorContext;
    e.value = a.inputField.value;
    if (a.dataField.includes('.')) {
      const f = a.dataField.split('.')[0];
      if ((b = a.record.constructor.relations) == null ? void 0 : b[f]) {
        c.rowManager.refresh();
      }
    }
    c.trigger('finishCellEdit', {
      'grid': c,
      'editorContext': e
    });
  }
  onEditorBeforeCancel() {
    const {editorContext: a} = this;
    return this.grid.trigger('beforeCancelCellEdit', { 'editorContext': a });
  }
  onEditorCancel({event: a}) {
    const {
      editorContext: b,
      muteEvents: c,
      grid: e
    } = this;
    if (!c) {
      e.trigger('cancelCellEdit', {
        'grid': e,
        'editorContext': b,
        'event': a
      });
    }
  }
  onBeforeEditorHide({source: a}) {
    let b;
    const c = this, {
        row: e,
        cell: f
      } = a.cellEditorContext;
    f == null ? void 0 : f.classList.remove('b-editing');
    e == null ? void 0 : e.removeCls('b-editing');
    c.detachListeners('cellClickWhileEditing');
    (b = c.removeEditingListeners) == null ? void 0 : b.call(c);
  }
  onEditorFinishEdit({source: a}) {
    a.cellEditorContext = this.editorContext = null;
  }
  getAdjacentEditableCell(a, b) {
    const {grid: c} = this, {
        store: e,
        columns: f
      } = c, {visibleColumns: g} = f;
    let i = a.id, j = f.getAdjacentVisibleLeafColumn(a.columnId, b);
    while (i) {
      if (j) {
        if (j.editor && j.canEdit(a.record)) {
          return {
            'id': i,
            'columnId': j.id
          };
        }
        j = f.getAdjacentVisibleLeafColumn(j, b);
      } else {
        const k = e.getAdjacent(a.id, b, false, true);
        i = k == null ? void 0 : k.id;
        if (k) {
          j = b ? g[0] : g[g.length - 1];
        }
      }
    }
    return null;
  }
  doAddNewAtEnd() {
    const a = typeof this.addNewAtEnd === 'object' ? ObjectHelper.clone(this.addNewAtEnd) : {}, {
        grid: {
          store: b,
          rowManager: c
        },
        addToCurrentParent: e
      } = this;
    let f;
    if (b.tree && e) {
      f = b.last.parent.appendChild(a);
    } else {
      f = b.add(a)[0];
    }
    if (!c.getRowFor(f)) {
      c.displayRecordAtBottom();
    }
    return f;
  }
  getEditingContext(a) {
    a = this.grid.normalizeCellContext(a);
    const {
      column: b,
      record: c
    } = a;
    if ((b == null ? void 0 : b.isVisible) && b.editor && !b.readOnly && c && !c.isSpecialRow && !c.readOnly && b.canEdit(c)) {
      const e = c ? b.getRawValue(c) : c;
      Object.assign(a, {
        'value': e === void 0 ? null : e,
        'editor': b.editor
      });
      return a;
    } else {
      return false;
    }
  }
  startEditingFromKeyMap() {
    const {focusedCell: a} = this.grid, b = a == null ? void 0 : a.column;
    if (!this.disabled && (b == null ? void 0 : b.get('editor')) !== false) {
      return this.startEditing(a);
    }
    return false;
  }
  async startEditing(a = {}) {
    let b, c, e;
    const f = this;
    if (!f.disabled) {
      const {grid: g} = f;
      if (a == null ? void 0 : a.fromKeyMap) {
        a = g.focusedCell;
      }
      if (ObjectHelper.isEmpty(a)) {
        a.id = g.firstVisibleRow.id;
      }
      if (g.store.isTree && g.features.tree) {
        const o = a.id ? g.store.getById(a.id) : (b = a.record) != null ? b : g.store.getAt(a.row);
        if (o) {
          await g.expandTo(o);
        } else {
          return false;
        }
      }
      const i = f.getEditingContext(a);
      if (!i) {
        return false;
      }
      if (f.editorContext) {
        if (f.cancelEditing() === false) {
          return false;
        }
        ;
      }
      if (!((c = g.focusedCell) == null ? void 0 : c.equals(i))) {
        g.focusCell(i);
      }
      if (g.trigger('beforeCellEditStart', {
          'grid': g,
          'editorContext': i
        }) === false) {
        return false;
      }
      const j = i.editor = f.getEditorForCell(i), {
          row: k,
          cell: l,
          record: n
        } = i;
      j.inputField.highlightExternalChange = false;
      j.cellEditorContext = i;
      j.render(l);
      l.classList.add('b-editing');
      k.addCls('b-editing');
      if (!await j.startEdit({
          'target': l,
          'field': j.inputField.name || i.column.field,
          'value': i.value,
          'record': n
        })) {
        l.classList.remove('b-editing');
        k.removeCls('b-editing');
      }
      (e = f.onCellEditStart) == null ? void 0 : e.call(f);
      return true;
    }
    return false;
  }
  cancelEditing(a = false, b) {
    let c;
    const e = this, {editor: f} = e;
    if (!e.isEditing) {
      return;
    }
    if (a.fromKeyMap) {
      b = a;
      a = false;
    }
    e.muteEvents = a;
    const g = f.cancelEdit(b);
    e.muteEvents = false;
    if (g === false) {
      f.inputField.focus();
    } else {
      e.finishEditingPromise = false;
      (c = e.afterCellEdit) == null ? void 0 : c.call(e);
    }
    return g;
  }
  async finishEditing() {
    let a;
    const b = this, {
        editorContext: c,
        grid: e
      } = b;
    let f = false;
    if (b.finishEditingPromise) {
      return b.finishEditingPromise;
    }
    if (c) {
      const {column: g} = c;
      b.finishEditingPromise = c.editor.completeEdit(g.bindCallback(g.finalizeCellEdit));
      f = await b.finishEditingPromise;
      if (e.isDestroyed) {
        return;
      }
      await e.waitForAnimations();
      b.finishEditingPromise = null;
      if (f) {
        (a = b.afterCellEdit) == null ? void 0 : a.call(b);
      }
    }
    return f;
  }
  async onCellClickWhileEditing({
    event: a,
    cellSelector: b
  }) {
    const c = this;
    if (a.target.closest('.b-editor')) {
      return;
    }
    if (DomHelper.isTouchEvent || a.target.matches(c.ignoreCSSSelector)) {
      await c.finishEditing();
      return;
    }
    if (c.finishEditingPromise) {
      return;
    }
    if (c.editorContext && !c.editorContext.editor.owns(a.target)) {
      if (c.getEditingContext(b)) {
        if (await c.finishEditing()) {
          if (c.continueEditingOnCellClick) {
            await c.startEditing(b);
          }
        } else {
          c.grid.focusCell(c.editorContext);
          c.editor.inputField.focus();
        }
      } else {
        await c.finishEditing();
      }
    }
  }
  async onCellClick({
    cellSelector: a,
    target: b,
    event: c,
    column: e
  }) {
    if (e.onCellClick) {
      return;
    }
    const f = this, {focusedCell: g} = f.client;
    if (b.closest('.b-tree-expander')) {
      return false;
    } else if (DomHelper.isTouchEvent && f._lastCellClicked === (g == null ? void 0 : g.cell) && c.timeStamp - f.touchEditDelay > f._lastCellClickedTime) {
      await f.startEditing(a);
    } else if (this.triggerEvent === 'cellclick') {
      await f.onTriggerEditEvent({
        'cellSelector': a,
        'target': b
      });
    }
    f._lastCellClicked = g == null ? void 0 : g.cell;
    f._lastCellClickedTime = c.timeStamp;
  }
  onElementPointerUp(a) {
    if (a.target.closest('.b-tree-expander')) {
      this.cancelEditing(void 0, a);
    }
  }
  async onTriggerEditEvent({
    cellSelector: a,
    target: b,
    event: c
  }) {
    let e;
    const {
      editorContext: f,
      client: g
    } = this;
    if (b.closest('.b-tree-expander') || DomHelper.isTouchEvent && c.type === 'dblclick') {
      return;
    }
    if (c && ((e = g.features.cellMenu) == null ? void 0 : e.triggerEvent) === c.type) {
      return;
    }
    if (f) {
      if (f.equals(this.grid.normalizeCellContext(a)) || !await this.finishEditing()) {
        return;
      }
    }
    await this.startEditing(a);
  }
  onStoreUpdate({
    changes: a,
    record: b
  }) {
    const {editorContext: c} = this;
    if (c == null ? void 0 : c.editor.isVisible) {
      if (b === c.record && c.editor.dataField in a) {
        c.editor.refreshEdit();
      }
    }
  }
  onStoreRemove({
    changes: a,
    records: b
  }) {
    const {editorContext: c} = this;
    if ((c == null ? void 0 : c.editor.isVisible) && b.includes(c.editor.record)) {
      this.cancelEditing();
    }
  }
  onStoreBeforeSort() {
    let a;
    const b = (a = this.editorContext) == null ? void 0 : a.editor;
    if (this.isEditing && !(b == null ? void 0 : b.isFinishing) && !b.isValid) {
      return this.cancelEditing();
    }
  }
  onGridRefreshed() {
    const a = this, {
        grid: b,
        editorContext: c
      } = a;
    if (c && b.isVisible && b.focusedCell) {
      const e = b.getCell(b.focusedCell), {editor: f} = c;
      if (e && DomHelper.isInView(e) && !f.isFinishing) {
        c._cell = e;
        GlobalEvents_default.suspendFocusEvents();
        f.render(e);
        f.showBy(e);
        f.focus();
        GlobalEvents_default.resumeFocusEvents();
      } else {
        a.cancelEditing();
      }
    }
  }
  get gridSelection() {
    return [
      ...this.grid.selectedRows,
      ...this.grid.selectedCells
    ];
  }
  isActionAvailable({
    actionName: a,
    event: b
  }) {
    const c = this;
    if (!allActions[a]) {
      return;
    }
    if (!c.disabled && !b.target.closest('.b-grid-header')) {
      if (c.isEditing) {
        if (a === 'finishAllSelected') {
          return c.multiEdit && c.gridSelection.length > 1;
        } else if (editingActions[a]) {
          return true;
        }
      } else if (a === 'startEditing') {
        return Boolean(c.grid.focusedCell && !DomHelper.isEditable(b.target));
      }
    }
    return false;
  }
  async finishAllSelected() {
    const a = this, {
        dataField: b,
        record: c
      } = a.editor;
    if (await a.finishEditing() && !a.isDestroyed) {
      const e = c.getValue(b);
      for (const f of a.gridSelection) {
        if (f.isModel) {
          if (f !== c) {
            f.setValue(b, e);
          }
        } else {
          f.record.set(f.column.field, e);
        }
      }
    }
  }
  async finishAndEditNextRow(a, b = false) {
    let c;
    const e = this, {grid: f} = e, {
        record: g,
        column: i
      } = e.editorContext;
    let j;
    if (await e.finishEditing()) {
      if (e.isDestroyed) {
        return;
      }
      if (!e.isEditing) {
        if (b) {
          j = f.internalNextPrevRow(false, true, false);
          if (!j && f.splitFrom) {
            const k = f.splitFrom;
            await k.startEditing({
              'id': k.store.last.id,
              'columnId': i.id
            });
          }
        } else {
          if (g === f.store.last) {
            if ((c = f.features.lockRows) == null ? void 0 : c.enabled) {
              const l = f.features.lockRows.subViews[0];
              if (l.store.count > 0) {
                await l.startEditing({
                  'id': l.store.first.id,
                  'columnId': i.id
                });
              }
            } else if (e.addNewAtEnd) {
              await e.doAddNewAtEnd();
            }
          }
          if (!e.isDestroyed) {
            j = f.internalNextPrevRow(true, true);
          }
        }
        if (j && e.editNextOnEnterPress) {
          await e.startEditing(j);
        }
      }
    }
  }
  finishAndEditPrevRow(a) {
    return this.finishAndEditNextRow(a, true);
  }
  async finishAndEditNextCell(a, b = false) {
    let c, e, f, g;
    const i = this, {grid: j} = i, {focusedCell: k} = j;
    if (k && !i.finishEditingPromise) {
      const {
        rowIndex: l,
        columnIndex: n,
        record: o
      } = k;
      let p = i.getAdjacentEditableCell(k, !b);
      if (!p && !b) {
        const r = await i.finishEditing();
        if (r === true) {
          if ((c = j.features.lockRows) == null ? void 0 : c.enabled) {
            const [u] = j.subViews;
            if (u.store.count > 0) {
              const v = u.store.first, w = {
                  'record': v,
                  'id': v.id,
                  'columnId': u.columns.visibleColumns[0].id
                };
              if (u.features.cellEdit.getEditingContext(w)) {
                await u.startEditing(w);
              } else {
                await u.startEditing(i.getAdjacentEditableCell(w));
              }
            }
          } else if (i.addNewAtEnd) {
            await i.doAddNewAtEnd();
          }
        }
      }
      let q = true;
      if (i.isEditing) {
        q = await i.finishEditing();
      }
      if (i.isDestroyed) {
        return;
      }
      p = i.getAdjacentEditableCell(new GridLocation({
        'grid': j,
        'rowIndex': l,
        'columnIndex': n
      }), !b);
      if (!p && b && ((e = j.splitFrom) == null ? void 0 : e.features.lockRows) && o === j.store.first) {
        const z = j.splitFrom, A = z.store.last, B = z.columns.visibleColumns[z.columns.visibleColumns.length - 1], C = {
            'record': A,
            'id': A.id,
            'columnId': B.id
          };
        if (z.features.cellEdit.getEditingContext(C)) {
          await z.startEditing(C);
        } else {
          await z.startEditing(i.getAdjacentEditableCell(C, false));
        }
      }
      if (p) {
        if (q) {
          j.focusCell(p, { 'animate': i.focusCellAnimationDuration });
          if (!await i.startEditing(p)) {
            await i.finishAndEditNextCell(a, b);
          }
        } else {
        }
      } else if (j.isNested && j.owner && !((g = (f = j.owner).catchFocus) == null ? void 0 : g.call(f, {
          'source': j,
          'navigationDirection': b ? 'up' : 'down',
          'editing': true
        }))) {
        j.onTab(a);
      }
    }
  }
  finishAndEditPrevCell(a) {
    return this.finishAndEditNextCell(a, true);
  }
  async onElementKeyDown(a) {
    const b = this, {grid: c} = b, {focusedCell: e} = c;
    if (a.handled || !b.autoEdit || b.isEditing || !e || e.isActionable && DomHelper.isEditable(a.target) || a.ctrlKey) {
      return;
    }
    const {key: f} = a, g = a.key === 'Delete' || a.key === 'Backspace', {gridSelection: i} = g ? b : {}, j = b.multiEdit && (i == null ? void 0 : i.length) > 1;
    if ((f.length <= 1 || g && !j) && await b.startEditing(e)) {
      const {inputField: k} = b.editor, {input: l} = k;
      if (l) {
        k.internalOnKeyEvent(a);
        if (!a.defaultPrevented) {
          l.value = g ? '' : f;
          k.internalOnInput(a);
        }
      }
      a.preventDefault();
    } else if (j) {
      if (c.trigger('beforeCellRangeDelete', {
          'grid': c,
          'gridSelection': i
        }) !== false) {
        for (const n of i) {
          if (n.isModel) {
            c.columns.visibleColumns.forEach(o => {
              !o.readOnly && n.set(o.field, null);
            });
          } else if (!n.column.readOnly) {
            n.record.set(n.column.field, null);
          }
        }
      }
    }
  }
  onEditorKeydown(a) {
    if (a.key && a.key.length !== 1 && this.grid.matchKeyMapEntry(a) && !this.grid.matchKeyMapEntry(a, this.keyMap)) {
      if (!a.key.startsWith('Arrow') && !a.key === 'Backspace') {
        a.preventDefault();
      }
      a.handled = true;
      a.stopPropagation();
      return false;
    }
  }
  async onEditorFocusOut(a) {
    const b = this, {
        grid: c,
        editor: e,
        editorContext: f
      } = b, g = new GridLocation(a.relatedTarget), i = g.grid === c && b.getEditingContext(g);
    if (f && !e.isFinishing && e.owns(a._target)) {
      if (b.blurAction === 'cancel' || !c.store.includes(f.record)) {
        b.cancelEditing(void 0, a);
      } else if (!b.finishEditingPromise && (b.triggerEvent === 'cellclick' || b.triggerEvent !== 'cellclick' && !i)) {
        await b.finishEditing();
      }
    }
  }
  onEditorFocusIn(a) {
    const b = a.toWidget;
    if (b === this.editor.inputField) {
      if (this.autoSelect && b.selectAll && !b.readOnly && !b.disabled) {
        b.selectAll();
      }
    }
  }
  async onTapOut({event: a}) {
    const b = this, {target: c} = a;
    if (!c._shadowRoot && !b.editor.owns(c) && (!b.grid.bodyContainer.contains(c) || a.button)) {
      const e = b.resolveCallback(b.validateTapOut, b, false), f = (e == null ? void 0 : e.handler.call(e.thisObj, a, b.editorContext)) || b.blurAction;
      b.editingStoppedByTapOutside = true;
      if (f === 'cancel') {
        b.cancelEditing(void 0, a);
      } else {
        await b.finishEditing();
      }
      delete b.editingStoppedByTapOutside;
    }
  }
  async onElementClick(a) {
    if (a.target.matches('.b-grid-body-container, .b-grid-subgrid') && this.editorContext) {
      await this.finishEditing();
    }
  }
};
CellEdit._$name = 'CellEdit';
GridFeatureManager.registerFeature(CellEdit, true);
const CellMenu = class extends ContextMenuBase {
  static get $name() {
    return 'CellMenu';
  }
  static get defaultConfig() {
    return {
      'processItems': null,
      'items': null,
      'type': 'cell'
    };
  }
  static get pluginConfig() {
    const a = super.pluginConfig;
    a.chain.push('populateCellMenu');
    return a;
  }
  showContextMenu(a) {
    const b = this, {
        cellSelector: c,
        event: e
      } = a;
    b.client.focusCell(c, {
      'doSelect': !b.client.selectedRecordCollection.includes(c.id),
      'event': e
    });
    super.showContextMenu(a);
  }
  shouldShowMenu({column: a}) {
    return a && a.enableCellContextMenu !== false;
  }
  getDataFromEvent(a) {
    const b = this.client.getCellDataFromEvent(a);
    if (b) {
      return ObjectHelper.assign(super.getDataFromEvent(a), b);
    }
  }
  beforeContextMenuShow({
    record: a,
    items: b,
    column: c
  }) {
    if (c.cellMenuItems === false) {
      return false;
    }
    if (!a || a.isSpecialRow) {
      b.removeRow = false;
    }
  }
  populateCellMenu({
    items: a,
    column: b,
    record: c
  }) {
    const {client: e} = this;
    if (b == null ? void 0 : b.cellMenuItems) {
      ObjectHelper.merge(a, b.cellMenuItems);
    }
    if (!e.readOnly) {
      a.removeRow = {
        'text': 'L{removeRow}',
        'localeClass': this,
        'icon': 'b-fw-icon b-icon-trash',
        'cls': 'b-separator',
        'weight': 100,
        'disabled': c.readOnly,
        'onItem': () => {
          let f, g;
          const i = (g = (f = e.features.treeGroup) == null ? void 0 : f.originalStore) != null ? g : e.store;
          if (e.isCellSelected({
              'record': c,
              'column': b
            }, true)) {
            i.remove(e.selectedRecords.filter(j => !j.readOnly).map(j => j.$original));
          } else {
            i.remove(c);
          }
        }
      };
    }
  }
  get showMenu() {
    return true;
  }
  showMenuFor(a, {targetElement: b} = {}) {
    a = this.client.normalizeCellContext(a);
    DomHelper.triggerMouseEvent(b || this.client.getCell(a), this.triggerEvent);
  }
};
CellMenu.featureClass = '';
CellMenu._$name = 'CellMenu';
GridFeatureManager.registerFeature(CellMenu, true, [
  'Grid',
  'Scheduler'
]);
GridFeatureManager.registerFeature(CellMenu, false, ['Gantt']);
const CellTooltip = class extends InstancePlugin {
  static configurable = { 'tooltipRenderer': null };
  static $name = 'CellTooltip';
  construct(a, b) {
    super.construct(a, this.processConfig(b));
  }
  initTip() {
    const a = this;
    a.tip = Tooltip.new({
      'forElement': a.client.element,
      'forSelector': '.b-grid-row:not(.b-group-row) .b-grid-cell, .b-grid-merged-cells',
      'hoverDelay': 1000,
      'trackMouse': false,
      'cls': 'b-celltooltip-tip',
      'getHtml': a.getTooltipContent.bind(a),
      'internalListeners': {
        'pointerOver': 'onPointerOver',
        'thisObj': a
      },
      'listeners': a.configuredListeners
    }, a.initialConfig);
    a.relayEvents(a.tip, [
      'beforeShow',
      'show'
    ]);
  }
  onPointerOver({target: a}) {
    const b = this.client.getColumnFromElement(a);
    return b.tooltipRenderer !== false && Boolean(b.tooltipRenderer || this.tooltipRenderer);
  }
  processConfig(a) {
    if (typeof a === 'function') {
      return { 'tooltipRenderer': a };
    }
    return a;
  }
  setConfig(a) {
    super.setConfig(this.processConfig(a));
  }
  doDestroy() {
    this.tip && this.tip.destroy();
    super.doDestroy();
  }
  doDisable(a) {
    if (!a) {
      this.initTip();
    } else if (this.tip) {
      this.tip.destroy();
      this.tip = null;
    }
    super.doDisable(a);
  }
  getTooltipContent({
    tip: a,
    activeTarget: b,
    event: c
  }) {
    const e = this, f = e.client.getRecordFromElement(b), g = e.client.getColumnFromElement(b), i = {
        'cellElement': b,
        'record': f,
        'column': g,
        'event': c,
        'tip': a,
        'cellTooltip': e
      };
    let j;
    if (!e.forSelector && f === e.lastRecord && f.generation === e.lastRecordGeneration && g === e.lastColumn) {
      return e.tip._html;
    }
    e.lastRecord = f;
    e.lastRecordGeneration = f.generation;
    e.lastColumn = g;
    if (g.tooltipRenderer) {
      j = g.tooltipRenderer(i);
    } else if (e.tooltipRenderer && g.tooltipRenderer !== false) {
      j = e.tooltipRenderer(i);
    }
    if (Objects.isPromise(j)) {
      e.lastRecord = e.lastRecordGeneration = e.lastColumn = null;
    }
    if (!j) {
      a.hide();
    }
    return j;
  }
};
CellTooltip._$name = 'CellTooltip';
GridFeatureManager.registerFeature(CellTooltip);
const storeListenerName = 'store';
const ColumnAutoWidth = class extends Delayable_default(InstancePlugin) {
  static configurable = {
    'default': null,
    'delay': 0
  };
  static $name = 'ColumnAutoWidth';
  static get pluginConfig() {
    return {
      'after': {
        'bindStore': 'bindStore',
        'unbindStore': 'unbindStore',
        'renderRows': 'syncAutoWidthColumns',
        'onInternalResize': 'onInternalResize'
      },
      'assign': [
        'columnAutoWidthPending',
        'syncAutoWidthColumns'
      ]
    };
  }
  construct(a) {
    super.construct(a);
    const {store: b} = this;
    b && this.bindStore(b);
  }
  doDestroy() {
    this.unbindStore();
    super.doDestroy();
  }
  get store() {
    return this.client.$store;
  }
  bindStore(a) {
    this.lastSync = null;
    a.ion({
      'name': storeListenerName,
      ['change' + this.client.asyncEventSuffix]: 'onStoreChange',
      'thisObj': this
    });
  }
  unbindStore() {
    this.detachListeners(storeListenerName);
  }
  get columnAutoWidthPending() {
    return this.lastSync === null || this.hasTimeout('syncAutoWidthColumns');
  }
  onStoreChange({action: a}) {
    if (a !== 'move' && this.client.isPainted) {
      const b = this, {cellEdit: c} = b.client.features;
      ++b.storeGeneration;
      if (a === 'filter' || (c == null ? void 0 : c.isEditing) && !c.editingStoppedByTapOutside) {
        b.syncAutoWidthColumns();
      } else if (!b.client.refreshSuspended) {
        b.setTimeout({
          'fn': 'syncAutoWidthColumns',
          'delay': b.delay,
          'cancelOutstanding': true
        });
      } else {
        b.client.rowManager.once('renderDone', () => b.syncAutoWidthColumns());
      }
    }
  }
  onInternalResize(a, b, c, e) {
    if (e === 0) {
      this.lastSync = null;
      this.syncAutoWidthColumns();
    }
  }
  syncAutoWidthColumns() {
    let a, b;
    const {client: c} = this;
    if (this.disabled || c.store.count === 0 || c.rowManager.rows.length === 0 || c.splitFrom && !((b = (a = c.splitFrom.features) == null ? void 0 : a.lockRows) == null ? void 0 : b.enabled)) {
      return;
    }
    this.doSyncAutoWidthColumns();
  }
  doSyncAutoWidthColumns() {
    const a = this, {
        client: b,
        storeGeneration: c
      } = a;
    if (b.splitFrom && !b.usesLockRows || !b.isPainted) {
      return;
    }
    if (a.lastSync !== c) {
      a.lastSync = c;
      let e, f;
      for (const g of b.columns.visibleColumns) {
        e = g.autoWidth;
        if (e) {
          if (e === true) {
            e = a.default;
          }
          b.resizingColumns = f = true;
          g.resizeToFitContent(e);
        }
      }
      if (f) {
        b.resizingColumns = false;
        b.afterColumnsResized();
      }
    }
    if (a.hasTimeout('syncAutoWidthColumns')) {
      a.clearTimeout('syncAutoWidthColumns');
    }
  }
};
ColumnAutoWidth.prototype.storeGeneration = 0;
ColumnAutoWidth._$name = 'ColumnAutoWidth';
GridFeatureManager.registerFeature(ColumnAutoWidth, true);
const ColumnDragToolbar = class extends Delayable_default(InstancePlugin) {
  static get $name() {
    return 'ColumnDragToolbar';
  }
  static get pluginConfig() {
    return { 'after': ['render'] };
  }
  construct(a, b) {
    if (a.features.columnReorder) {
      a.features.columnReorder.ion({
        'beforeDestroy': 'onColumnReorderBeforeDestroy',
        'thisObj': this
      });
    }
    this.grid = a;
    super.construct(a, b);
  }
  doDestroy() {
    const a = this;
    if (a.grid.features.columnReorder && !a.grid.features.columnReorder.isDestroyed) {
      a.detachFromColumnReorder();
    }
    a.element && a.element.remove();
    a.element = null;
    super.doDestroy();
  }
  doDisable(a) {
    if (this.initialized) {
      if (a) {
        this.detachFromColumnReorder();
      } else {
        this.init();
      }
    }
    super.doDisable(a);
  }
  init() {
    const a = this, b = a.grid;
    if (!b.features.columnReorder) {
      return;
    }
    a.reorderDetacher = b.features.columnReorder.ion({
      'gridheaderdragstart'({context: c}) {
        const e = b.columns.getById(c.element.dataset.columnId);
        a.showToolbar(e);
      },
      'gridheaderdrag': ({context: c}) => a.onDrag(c),
      'gridheaderabort': () => {
        a.hideToolbar();
      },
      'gridHeaderDrop': a.onDrop,
      'thisObj': a
    });
    a.initialized = true;
  }
  onColumnReorderBeforeDestroy() {
    this.detachFromColumnReorder();
  }
  detachFromColumnReorder() {
    const a = this;
    a.grid.features.columnReorder.un('beforedestroy', a.onColumnReorderBeforeDestroy, a);
    a.reorderDetacher && a.reorderDetacher();
    a.reorderDetacher = null;
  }
  render() {
    if (!this.initialized) {
      this.init();
    }
  }
  showToolbar(a) {
    const b = this, c = b.grid.getColumnDragToolbarItems(a, []), e = [];
    b.clearTimeout(b.buttonHideTimer);
    c.forEach(f => {
      f.text = f.localeClass.L(f.text);
      let g = e.find(i => i.text === f.group);
      if (!g) {
        g = {
          'text': f.localeClass.L(f.group),
          'buttons': []
        };
        e.push(g);
      }
      g.buttons.push(f);
    });
    b.element = DomHelper.append(b.grid.element, b.template(e));
    b.groups = e;
    b.buttons = c;
    b.column = a;
  }
  async hideToolbar() {
    const a = this, b = a.element;
    if (b) {
      b.classList.add('b-remove');
      await EventHelper.waitForTransitionEnd({
        'element': b,
        'mode': 'animation',
        'thisObj': a.client
      });
      b.remove();
      a.element = null;
    }
  }
  onDrag(a) {
    let b;
    const c = this;
    if (a.dragProxy.getBoundingClientRect().top - c.grid.element.getBoundingClientRect().top > 100) {
      c.element.classList.add('b-closer');
    } else {
      c.element.classList.remove('b-closer');
    }
    if (c.hoveringButton) {
      c.hoveringButton.classList.remove('b-hover');
      c.hoveringButton = null;
    }
    if ((b = a.targetElement) == null ? void 0 : b.closest('.b-columndragtoolbar')) {
      c.element.classList.add('b-hover');
      const e = a.targetElement.closest('.b-columndragtoolbar  .b-target-button:not([data-disabled=true])');
      if (e) {
        e.classList.add('b-hover');
        c.hoveringButton = e;
      }
    } else {
      c.element.classList.remove('b-hover');
    }
  }
  onDrop({context: a}) {
    const b = this, {targetElement: c} = a;
    if (c && c.matches('.b-columndragtoolbar .b-target-button:not([data-disabled=true])')) {
      const e = b.buttons.find(f => f.ref === c.dataset.ref);
      if (e) {
        c.classList.add('b-activate');
        b.buttonHideTimer = b.setTimeout(() => {
          b.hideToolbar();
          e.onDrop({ 'column': b.column });
        }, 100);
      }
    } else {
      b.hideToolbar();
    }
  }
  template(a) {
    return TemplateHelper.tpl`
            <div class="b-columndragtoolbar">     
            <div class="b-title"></div>          
            ${ a.map(b => TemplateHelper.tpl`
                <div class="b-group">
                    <div class="b-buttons">
                    ${ b.buttons.map(c => TemplateHelper.tpl`
                        <div class="b-target-button" data-ref="${ c.ref }" data-disabled="${ c.disabled }">
                            <i class="${ c.icon }"></i>
                            ${ c.text }
                        </div>
                    `) }
                    </div>
                    <div class="b-title">${ b.text }</div>
                </div>
            `) }
            </div>`;
  }
};
ColumnDragToolbar.featureClass = 'b-hascolumndragtoolbar';
ColumnDragToolbar._$name = 'ColumnDragToolbar';
GridFeatureManager.registerFeature(ColumnDragToolbar, BrowserHelper.isTouchDevice);
const ColumnPicker = class extends InstancePlugin {
  static configurable = {
    'groupByRegion': false,
    'groupByTag': false,
    'createColumnsFromModel': false,
    'menuCls': 'b-column-picker-menu b-sub-menu'
  };
  static $name = 'ColumnPicker';
  static get pluginConfig() {
    return {
      'chain': [
        'populateHeaderMenu',
        'getColumnDragToolbarItems'
      ]
    };
  }
  get grid() {
    return this.client;
  }
  getColumnPickerItems(a) {
    const b = this, {createColumnsFromModel: c} = b;
    let e;
    if (b.groupByRegion) {
      e = b.grid.regions.map(f => {
        const g = b.grid.getSubGrid(f).columns.topColumns;
        return {
          'text': StringHelper.capitalize(f),
          'menu': b.buildColumnMenu(g),
          'disabled': g.length === 0,
          'region': f
        };
      });
      if (c) {
        e.push({
          'text': b.L('L{newColumns}'),
          'menu': b.createAutoColumnItems()
        });
      }
    } else if (b.groupByTag) {
      const f = {};
      a.topColumns.forEach(g => {
        g.tags && Array.isArray(g.tags) && g.hideable !== false && g.tags.forEach(i => {
          if (!f[i]) {
            f[i] = 1;
          }
        });
      });
      e = Object.keys(f).sort().map(g => ({
        'text': StringHelper.capitalize(g),
        'menu': b.buildColumnMenu(b.getColumnsForTag(g)),
        'tag': g,
        'onBeforeSubMenu': ({
          item: i,
          itemEl: j
        }) => {
          b.refreshTagMenu(i, j);
        }
      }));
      if (c) {
        e.push({
          'text': b.L('L{newColumns}'),
          'menu': b.createAutoColumnItems()
        });
      }
    } else {
      e = b.buildColumnMenu(a.topColumns);
      if (c) {
        e.items.push(...ObjectHelper.transformNamedObjectToArray(b.createAutoColumnItems()));
      }
    }
    return e;
  }
  createAutoColumnItems() {
    const a = this, {grid: b} = a, {
        columns: c,
        store: e
      } = b, {modelClass: f} = e, {allFields: g} = f, j = {};
    for (let k = 0, {length: l} = g; k < l; k++) {
      const n = g[k], o = n.name;
      if (!c.get(o)) {
        if (!n.internal) {
          j[o] = {
            'text': n.text || StringHelper.separate(n.name),
            'checked': false,
            'onToggle': p => {
              const q = c.get(o);
              if (q) {
                q[p.checked ? 'show' : 'hide']();
              } else {
                c.add(c.generateColumnForField(n, { 'region': a.forColumn.region }));
              }
              p.bubbles = false;
            }
          };
        }
      }
    }
    return j;
  }
  getColumnsForTag(a) {
    return this.grid.columns.records.filter(b => b.tags && Array.isArray(b.tags) && b.tags.includes(a) && b.hideable !== false);
  }
  refreshTagMenu(a, b) {
    const c = this.getColumnsForTag(a.tag);
    c.forEach(e => {
      const f = a.items.find(g => g.column === e);
      if (f)
        f.checked = e.hidden !== true;
    });
  }
  buildColumnMenu(a) {
    let b = a.length > 0 && a[0].region;
    const {grid: c} = this, e = a.reduce((f, g) => {
        const i = c.columns.visibleColumns.filter(j => j.region === g.region);
        if (g.hideable !== false) {
          const j = {
            'grid': c,
            'column': g,
            'text': g.htmlEncodeHeaderText ? g.text : DomHelper.stripTags(g.text),
            'checked': g.hidden !== true,
            'disabled': g.hidden !== true && i.length === 1,
            'cls': g.region !== b ? 'b-separator' : ''
          };
          b = g.region;
          if (g.children && !g.isCollapsible) {
            j.menu = this.buildColumnMenu(g.children);
          }
          f.push(j);
        }
        return f;
      }, []);
    return {
      'cls': this.menuCls,
      'items': e
    };
  }
  populateHeaderMenu({
    column: a,
    items: b
  }) {
    const c = this, {columns: e} = c.grid;
    c.forColumn = a;
    if (a.showColumnPicker !== false && e.some(f => f.hideable)) {
      b.columnPicker = {
        'text': 'L{columnsMenu}',
        'localeClass': c,
        'icon': 'b-fw-icon b-icon-columns',
        'cls': 'b-separator',
        'weight': 200,
        'menu': c.getColumnPickerItems(e),
        'onToggle': c.onColumnToggle,
        'disabled': c.disabled
      };
    }
    if (a.hideable !== false && !a.parent.isCollapsible) {
      b.hideColumn = {
        'text': 'L{hideColumn}',
        'localeClass': c,
        'icon': 'b-fw-icon b-icon-hide-column',
        'weight': 210,
        'disabled': !a.allowDrag || c.disabled,
        'onItem': () => a.hide()
      };
    }
  }
  onColumnToggle({
    menu: a,
    item: b,
    checked: c
  }) {
    let e, f;
    if (Boolean(b.column.hidden) !== !c) {
      b.column[c ? 'show' : 'hide']();
      const {
          grid: g,
          column: i
        } = b, {
          columns: j,
          features: k
        } = g, l = a.items, n = j.visibleColumns.filter(q => q.region === b.column.region && q.showColumnPicker !== false), o = ((e = k.headerMenu) == null ? void 0 : e.enabled) && k.headerMenu.menu.widgetMap.hideColumn;
      if (n.length === 1) {
        const q = l.find(r => r.column === n[0]);
        if (q) {
          q.disabled = true;
        }
        if (o && i.region === b.column.region) {
          o.disabled = true;
        }
      } else {
        n.forEach(r => {
          const u = l.find(v => v.column === r);
          if (u) {
            u.disabled = false;
          }
        });
        if (o && i.region === b.column.region) {
          o.disabled = false;
        }
      }
      (f = b.menu) == null ? void 0 : f.eachWidget(r => {
        r.checked = c;
      });
      const p = a.owner;
      if (p && p.column === i.parent) {
        p.checked = l.some(r => r.checked === true);
      }
    }
  }
  getColumnDragToolbarItems(a, b) {
    const c = this.grid.columns.visibleColumns.filter(e => e.region === a.region);
    if (a.hideable !== false && c.length > 1) {
      b.push({
        'text': 'L{hideColumnShort}',
        'ref': 'hideColumn',
        'group': 'L{column}',
        'localeClass': this,
        'icon': 'b-fw-icon b-icon-hide-column',
        'weight': 101,
        'onDrop': ({column: e}) => e.hide()
      });
    }
    return b;
  }
};
ColumnPicker._$name = 'ColumnPicker';
GridFeatureManager.registerFeature(ColumnPicker, true);
const ColumnRename = class extends InstancePlugin {
  static configurable = { 'keyMap': { 'F2': 'startEdit' } };
  static $name = 'ColumnRename';
  doDestroy() {
    let a;
    (a = this.editor) == null ? void 0 : a.destroy();
    super.doDestroy();
  }
  static get pluginConfig() {
    return { 'after': ['populateHeaderMenu'] };
  }
  populateHeaderMenu({
    items: a,
    column: b
  }) {
    a.rename = {
      'weight': 215,
      'icon': 'b-fw-icon b-icon-edit',
      'text': this.L('L{rename}'),
      'disabled': b.readOnly,
      'onItem': () => this.startEdit(b)
    };
  }
  startEdit(a) {
    let b;
    if (a instanceof Event) {
      a = (b = this.client.getHeaderDataFromEvent(a)) == null ? void 0 : b.column;
    }
    if (!a || a.readOnly || this.disabled) {
      return false;
    }
    const {textWrapper: c} = a;
    let {editor: e} = this;
    if (!e) {
      this.editor = e = new Editor({
        'owner': this.client,
        'align': { 'align': 't0-t0' }
      });
    }
    e.render(c);
    e.startEdit({
      'target': c,
      'record': a,
      'field': 'text'
    });
  }
};
ColumnRename._$name = 'ColumnRename';
GridFeatureManager.registerFeature(ColumnRename, false);
const ColumnReorder = class extends Delayable_default(InstancePlugin) {
  static configurable = { 'stretchedDragProxy': null };
  static $name = 'ColumnReorder';
  ignoreSelectors = [
    '.b-grid-header-resize-handle',
    '.b-field'
  ];
  constructor() {
    super(...arguments);
  }
  static get pluginConfig() {
    return {
      'after': [
        'onInternalPaint',
        'renderContents'
      ]
    };
  }
  init() {
    const a = this, {grid: b} = a, c = b.element, e = DomHelper.children(c, '.b-grid-headers');
    e.push(...DomHelper.children(c, '.b-grid-header-children'));
    if (a.dragHelper) {
      a.dragHelper.containers = e;
    } else {
      b.whenVisible(() => a.createDragHelper());
    }
  }
  createDragHelper() {
    const a = this, {grid: b} = a, c = b.element, e = DomHelper.children(c, '.b-grid-headers');
    e.push(...DomHelper.children(c, '.b-grid-header-children'));
    a.dragHelper = new DragHelper({
      'name': 'columnReorder',
      'mode': 'container',
      'dragThreshold': 10,
      'targetSelector': '.b-grid-header',
      'floatRootOwner': b,
      'rtlSource': b,
      'outerElement': b.headerContainer,
      'externalDropTargetSelector': '.b-groupbar, .b-columndragtoolbar',
      'monitoringConfig': { 'scrollables': [{ 'element': '.b-grid-headers' }] },
      'scrollManager': ScrollManager.new({
        'direction': 'horizontal',
        'element': b.headerContainer
      }),
      'containers': e,
      'isElementDraggable'(f) {
        const g = Boolean(f.closest(a.ignoreSelectors.join(',')));
        if (g || a.disabled) {
          return false;
        }
        const i = f.closest(this.targetSelector), j = i && b.columns.getById(i.dataset.columnId), k = (j == null ? void 0 : j.childLevel) === 0 && b.subGrids[j.region].columns.count === 1;
        return Boolean(j) && j.draggable !== false && !k;
      },
      'ignoreSelector': '.b-filter-icon,.b-grid-header-resize-handle',
      'resolveTargetElement'(f) {
        const g = f.closest('.b-grid-cell');
        if (g) {
          return b.columns.getById(g.dataset.columnId).element;
        }
        return f;
      },
      'internalListeners': {
        'beforeDragStart': a.onBeforeDragStart,
        'dragstart': a.onDragStart,
        'drag': a.onDrag,
        'drop': a.onDrop,
        'abort': a.onAbort,
        'thisObj': a
      }
    });
    a.relayEvents(a.dragHelper, [
      'dragStart',
      'drag',
      'drop',
      'abort'
    ], 'gridHeader');
    return a.dragHelper;
  }
  doDestroy() {
    let a, b;
    (a = this.dragHelper) == null ? void 0 : a.scrollManager.destroy();
    (b = this.dragHelper) == null ? void 0 : b.destroy();
    super.doDestroy();
  }
  get grid() {
    return this.client;
  }
  onBeforeDragStart({
    source: a,
    context: b,
    event: c
  }) {
    const e = this, {element: f} = b, g = b.column = e.client.columns.getById(f.dataset.columnId);
    a.autoSizeClonedTarget = !e.stretchedDragProxy && !e.usingGroupBarWidget;
    a.dropTargetSelector = e.stretchedDragProxy ? null : '.b-grid-headers, .b-groupbar, .b-columndragtoolbar';
    return g.allowDrag && e.client.trigger('beforeColumnDragStart', {
      'column': g,
      'event': c
    });
  }
  onDragStart({
    context: a,
    event: b
  }) {
    const c = this, {
        grid: e,
        usingGroupBarWidget: f,
        dragHelper: g
      } = c, {
        column: i,
        dragProxy: j,
        element: k
      } = a, l = e.element.querySelector('.b-grid-header-container').getBoundingClientRect();
    j.style.width = i.element.offsetWidth + 'px';
    g.useCenteredCursor = !c.stretchedDragProxy;
    if (!e.features.columnDragToolbar && !f) {
      g.minY = l.top;
      if (c.stretchedDragProxy) {
        j.classList.add('b-column-reorder-stretched');
        DomHelper.setTranslateXY(j, k.left, k.top);
        g.maxY = e.element.getBoundingClientRect().bottom;
      } else {
        g.maxY = l.bottom;
      }
      j.style.height = e.bodyElement.offsetHeight + 'px';
    } else {
      j.style.height = l.height + 'px';
    }
    e.element.classList.add('b-dragging-header');
    if (f) {
      j.classList.add('b-grid-reordering-columns-with-groupbar');
    }
    if (e.features.filterBar && e.features.filterBar.compactMode) {
      j.classList.add('b-filter-bar-compact');
    }
    j.style.fontSize = DomHelper.getStyleValue(a.element, 'fontSize');
    e.trigger('columnDragStart', {
      'column': i,
      'event': b
    });
  }
  onDrag({
    context: a,
    event: b
  }) {
    const c = this, e = c.client, {
        column: f,
        insertBefore: g
      } = a, i = e.columns.getById(g == null ? void 0 : g.dataset.columnId), j = Widget.fromElement(b.target, 'gridheader');
    if (j == null ? void 0 : j.subGrid.sealedColumns) {
      a.valid = false;
    }
    e.trigger('columnDrag', {
      'column': f,
      'insertBefore': i,
      'event': b,
      'context': a
    });
  }
  onDrop({
    context: a,
    event: b
  }) {
    let c;
    if (!a.valid) {
      return this.onInvalidDrop({
        'context': a,
        'event': b
      });
    }
    const e = this, {grid: f} = e, {column: g} = a, i = a.dragging, j = a.target.closest('.b-grid-header'), k = (c = a.draggedTo) == null ? void 0 : c.dataset.region, l = k || j;
    let n, o, p, q, r;
    f.element.classList.remove('b-dragging-header');
    if (l) {
      const u = j ? f.columns.getById(j.dataset.columnId) : f.subGrids[k].columns.last, v = a.insertBefore;
      q = k || u.region;
      r = g.parent;
      p = v ? f.columns.getById(v.dataset.columnId) : f.subGrids[q].columns.last.nextSibling;
      if (p) {
        o = p.parent;
      } else {
        const w = j == null ? void 0 : j.parentElement.closest('.b-grid-header');
        if (w) {
          o = f.columns.getById(w.dataset.columnId);
        } else {
          o = f.columns.rootNode;
        }
      }
      n = q === g.region && r === o && (u === g.previousSibling || p === g.nextSibling);
      i.remove();
    }
    n = n || f.trigger('beforeColumnDropFinalize', {
      'column': g,
      'newParent': o,
      'insertBefore': p,
      'event': b,
      'region': q
    }) === false;
    if (!n && l) {
      n = !o.insertChild(g, p);
    }
    a.valid = !n;
    if (!n && l) {
      if (g.parent.isRoot && g.region !== q) {
        g.region = q;
      }
      if (r.children.length === 0) {
        r.parent.removeChild(r);
      }
    }
    f.trigger('columnDrop', {
      'column': g,
      'newParent': o,
      'insertBefore': p,
      'valid': a.valid,
      'event': b,
      'region': q
    });
  }
  onAbort({
    context: a,
    event: b
  }) {
    this.onInvalidDrop({
      'context': a,
      'event': b
    });
  }
  onInvalidDrop({
    context: a,
    event: b
  }) {
    const {grid: c} = this, {column: e} = a;
    c.trigger('columnDrop', {
      'column': e,
      'valid': false,
      'event': b
    });
    c.element.classList.remove('b-dragging-header');
  }
  renderContents() {
    this.init();
  }
  onInternalPaint() {
    this.init();
  }
  get isReordering() {
    let a;
    return Boolean((a = this.dragHelper) == null ? void 0 : a.isDragging);
  }
};
ColumnReorder.featureClass = 'b-column-reorder';
ColumnReorder._$name = 'ColumnReorder';
GridFeatureManager.registerFeature(ColumnReorder, true);
const ColumnResize = class extends InstancePlugin {
  static configurable = { 'liveResize': 'auto' };
  static $name = 'ColumnResize';
  construct(a, b) {
    const c = this;
    super.construct(a, b);
    c.resizer = new ResizeHelper({
      'name': 'columnResize',
      'targetSelector': '.b-grid-header',
      'handleSelector': '.b-grid-header-resize-handle',
      'outerElement': a.element,
      'rtlSource': a,
      'constrainToParent': false,
      'internalListeners': {
        'beforeresizestart': c.onBeforeResizeStart,
        'resizestart': c.onResizeStart,
        'resizing': c.onResizing,
        'resize': c.onResize,
        'cancel': c.onCancel,
        'thisObj': c
      }
    });
  }
  doDestroy() {
    let a;
    (a = this.resizer) == null ? void 0 : a.destroy();
    super.doDestroy();
  }
  changeLiveResize(a) {
    if (a === 'auto') {
      return !BrowserHelper.isMobileSafari;
    }
    return a;
  }
  onBeforeResizeStart({
    event: a,
    element: b
  }) {
    const {client: c} = this;
    return !this.disabled && c.trigger('beforeColumnResize', {
      'column': c.columns.getById(b.dataset.columnId),
      'domEvent': a
    });
  }
  onResizeStart({context: a}) {
    const {
        client: b,
        resizer: c
      } = this, e = a.column = b.columns.getById(a.element.dataset.columnId), {allRecords: f} = e.subGrid.columns;
    c.minWidth = e.minWidth;
    a.subGridBoundary = e.element.getBoundingClientRect()[b.rtl ? 'left' : 'right'];
    b.element.classList.add('b-column-resizing');
    f.slice(0, f.indexOf(e)).forEach(g => {
      g.flex && g.element && g !== e.parent && g.set({
        'width': g.element.offsetWidth,
        'flex': ''
      });
    });
    b.trigger('columnResizeStart', {
      'column': e,
      'domEvent': a.event
    });
  }
  onResizing({context: a}) {
    if (a.valid && this.liveResize) {
      const {column: b} = a, {subGrid: c} = b, {
          scrollable: e,
          columns: f
        } = c, {rtl: g} = this.client;
      this.client.resizingColumns = true;
      b.width = a.newWidth;
      c.refreshFakeScroll();
      if (b === f.last && (g ? a.currentX < a.subGridBoundary : a.currentX > a.subGridBoundary)) {
        e.x = e.maxX;
      }
    }
  }
  onResize({context: a}) {
    const {client: b} = this, {
        column: c,
        event: e
      } = a;
    b.element.classList.remove('b-column-resizing');
    if (a.valid) {
      if (this.liveResize) {
        b.resizingColumns = false;
        b.afterColumnsResized(c);
      } else {
        c.width = a.newWidth;
      }
      b.trigger('columnResize', {
        'column': c,
        'domEvent': e
      });
    }
  }
  onCancel({context: a}) {
    const {client: b} = this;
    b.element.classList.remove('b-column-resizing');
    a.column.width = a.elementWidth;
    b.resizingColumns = false;
  }
};
ColumnResize._$name = 'ColumnResize';
GridFeatureManager.registerFeature(ColumnResize, true);
const FillHandle = class extends InstancePlugin.mixin(Delayable_default) {
  static configurable = {
    'calculateFillValue': null,
    'allowCropping': false
  };
  static $name = 'FillHandle';
  delayable = { 'handleSelection': 'raf' };
  constructor() {
    super(...arguments);
  }
  static get pluginConfig() {
    return {
      'before': ['onStoreRemove'],
      'chain': [
        'afterSelectionChange',
        'onContentChange',
        'fixElementHeights',
        'syncFlexedSubCols',
        'beforeSubGridInternalResize'
      ],
      'override': ['getCellDataFromEvent']
    };
  }
  afterConstruct() {
    super.afterConstruct();
    const {client: a} = this;
    if (!a.selectionMode.cell) {
      this.disabled = true;
    }
    a.ion({
      'startCellEdit': 'onStartCellEdit',
      'finishCellEdit': 'onFinishCellEdit',
      'cancelCellEdit': 'onFinishCellEdit',
      'thisObj': this
    });
    this._fillListeners = {};
  }
  onContentChange() {
    this.handleSelection(true);
  }
  fixElementHeights() {
    this.handleSelection();
  }
  syncFlexedSubCols() {
    this.handleSelection();
  }
  beforeSubGridInternalResize() {
    this.removeElements();
  }
  onStoreRemove(a) {
    if (a.isCollapse) {
      this.handleSelection(true);
    }
  }
  onStartCellEdit() {
    this.removeElements(false);
  }
  onFinishCellEdit() {
    this.handleSelection();
  }
  getCellDataFromEvent(a, b) {
    if (b) {
      b = !a.target.classList.contains('b-fill-handle');
    }
    return this.overridden.getCellDataFromEvent(a, b);
  }
  get currentRangeForEvents() {
    if (this.currentRange) {
      let {
        from: a,
        to: b,
        negative: c
      } = this.currentRange;
      if (c) {
        [a, b] = [
          b,
          a
        ];
      }
      return {
        'from': a,
        'to': b
      };
    }
  }
  findPatternsIn2dRange(a, b, c) {
    const e = {};
    for (const f of a) {
      const g = b ? f.id : f.columnId;
      let i = f.record.getValue(f.column.field);
      if (i && typeof i === 'string' && !isNaN(i)) {
        i = parseFloat(i);
      }
      if (!e[g]) {
        e[g] = [];
      }
      e[g].push(i);
    }
    for (const j in e) {
      e[j].pattern = this.findPatternsIn1dRange(e[j], c);
    }
    return e;
  }
  findPatternsIn1dRange(a, b) {
    const c = a[b ? 0 : a.length - 1], e = {
        'next': () => c,
        'lastValue': c
      };
    if (a.every(f => typeof f === 'number') || a.every(f => f instanceof Date)) {
      const f = a.map((g, i) => g - a[i - 1]);
      f.shift();
      if (new Set(f).size === 1) {
        e.increaseBy = f[0] * (b ? -1 : 1);
        e.next = () => {
          if (e.lastValue instanceof Date) {
            e.lastValue = new Date(e.lastValue.getTime() + e.increaseBy);
          } else {
            e.lastValue += e.increaseBy;
          }
          return e.lastValue;
        };
      }
    } else if (a.length > 1) {
      e.stringPattern = [...a];
      e.next = () => {
        if (e.currentIndex === void 0) {
          e.currentIndex = 0;
        } else {
          e.currentIndex += 1;
          if (e.currentIndex >= e.stringPattern.length) {
            e.currentIndex = 0;
          }
        }
        return e.stringPattern[e.currentIndex];
      };
    }
    return e;
  }
  afterSelectionChange() {
    const a = this;
    if (a.client.readOnly || a.disabled) {
      a.removeElements();
      return;
    }
    if (GlobalEvents_default.isMouseDown()) {
      a.client.delayUntilMouseUp(() => a.handleSelection(true));
      a.removeElements();
    } else {
      a.handleSelection(true);
    }
  }
  handleSelection(a = false) {
    if (this.enabled && !this._isExtending) {
      if (a) {
        this._cachedSelectedRange = null;
      }
      const b = this.rangeSelection;
      if (b) {
        this.drawFillHandleAndBorder(b[0], b[b.length - 1]);
      } else {
        this.removeElements();
      }
    }
  }
  onMouseDown(a) {
    const {client: b} = this;
    if (this.enabled && !b.readOnly && b.trigger('beforeFillHandleDragStart', {
        'cell': b.focusedCell,
        'domEvent': a
      }) !== false) {
      this._fillListeners.mouseMoveOrUp = EventHelper.on({
        'element': this.rootElement,
        'keydown': 'onKeyDown',
        'mouseenter': {
          'handler': 'onMouseOver',
          'delegate': '.b-grid-cell',
          'element': b.selectionDragMouseEventListenerElement
        },
        'capture': true,
        'mouseup': 'onMouseUp',
        'thisObj': this
      });
      a.preventDefault();
      a.stopImmediatePropagation();
      a.handled = true;
      b.trigger('fillHandleDragStart', {
        'cell': b.focusedCell,
        'domEvent': a
      });
    }
  }
  async onMouseUp(a) {
    const b = await this.client.trigger('fillHandleBeforeDragFinalize', {
      ...this.currentRangeForEvents,
      'domEvent': a
    }) !== false;
    this.finalize(b, a);
  }
  finalize(a, b = true) {
    let c, e, f;
    const g = this, {
        client: i,
        currentRange: j,
        currentRangeForEvents: k,
        _isCropping: l
      } = g, n = g.rangeSelection, o = n && j && i.internalSelectRange(j.from, j.to), p = (o == null ? void 0 : o.selectedCells) || [], q = l ? g.croppingCells : p.filter(r => !n.some(u => u.equals(r, true)));
    g._isCropping = null;
    if (g._isExtending) {
      i.disableScrollingCloseToEdges(i.items);
      g._isExtending = null;
    }
    if (!(q == null ? void 0 : q.length) || !a) {
      g.currentRange = null;
      g.handleSelection();
      i.trigger('fillHandleDragAbort', { 'cell': i.focusedCell });
      return;
    }
    i.suspendRefresh();
    if (l) {
      q.forEach(r => r.record.set(r.column.field, null, false, false, false, true));
    } else {
      const [r] = q, u = n.some(A => A.record === r.record), v = u ? r.columnIndex < n[0].columnIndex : r.rowIndex < n[0].rowIndex, w = g.findPatternsIn2dRange(n, u, v), z = new Map();
      if (v) {
        q.reverse();
      }
      for (const A of q) {
        const {
          column: B,
          record: C
        } = A;
        if (!C.readOnly && !B.readOnly && B.canFillValue({
            'range': n,
            'record': C,
            'cell': A
          }) && ((c = C.isEditable) == null ? void 0 : c.call(C, B.field)) !== false) {
          let E = (e = g.calculateFillValue) == null ? void 0 : e.call(g, {
              'range': n,
              'column': B,
              'record': C,
              'cell': A
            }), F = z.get(C);
          if (!F) {
            F = {};
            z.set(C, F);
          }
          if (E === void 0) {
            const G = w[u ? A.id : A.columnId].pattern;
            E = G.next();
          }
          F[B.field] = ((f = B.calculateFillValue) == null ? void 0 : f.call(B, {
            'value': E,
            'record': C,
            'range': n
          })) || E;
        }
      }
      for (const [H, I] of z) {
        H.set(I, null, null, null, null, true);
      }
    }
    i.resumeRefresh(true);
    i.performSelection(o);
    g.currentRange = null;
    g.handleSelection();
    i.trigger('fillHandleDragEnd', {
      ...k,
      'domEvent': b
    });
  }
  onMouseOver(a) {
    let b;
    const c = this, {
        client: e,
        rangeSelection: f
      } = c, g = f[0], i = f[f.length - 1], j = e.getCellDataFromEvent(a, true);
    let k = j && e.normalizeCellContext(j.cellSelector);
    if (((b = k == null ? void 0 : k._column) == null ? void 0 : b.region) === g._column.region) {
      const l = f.some(q => q.equals(k, true));
      let n;
      if (!c._isExtending) {
        e.enableScrollingCloseToEdges(e.items);
      }
      c._isCropping = l && c.allowCropping && (k.rowIndex < i.rowIndex || k.columnIndex < i.columnIndex);
      if (!l) {
        if (k.rowIndex >= g.rowIndex && k.rowIndex <= i.rowIndex) {
          n = g.columnIndex > k.columnIndex;
          k = new GridLocation({
            'grid': e,
            'record': n ? g.record : i.record,
            'column': k.column
          });
        } else {
          n = g.rowIndex > k.rowIndex;
          k = new GridLocation({
            'grid': e,
            'record': k.record,
            'column': n ? g.column : i.column
          });
        }
      }
      const o = n ? k : g, p = n || l && !c._isCropping ? i : k;
      c.currentRange = {
        'from': o,
        'to': p,
        'negative': n
      };
      c._isExtending = true;
      c.drawFillHandleAndBorder(o, p, true);
      e.trigger('fillHandleDrag', {
        ...c.currentRangeForEvents,
        'domEvent': a
      });
    }
  }
  drawFillHandleAndBorder(a, b, c = false) {
    let e;
    const f = this, {
        client: g,
        _fillListeners: i
      } = f, j = g.subGrids[a.column.region].element, k = Rectangle.from(a.cell || a.column.element, j), l = Rectangle.from(b.cell || b.column.element, j), {y: n} = g.getRecordCoords(a.record, true), o = g.getRecordCoords(b.record, true).bottom - 1;
    let {
      borderElement: p,
      handleElement: q
    } = f;
    f.removeElements(c);
    if (!p) {
      f.borderElement = p = DomHelper.createElement({ 'className': 'b-fill-selection-border' });
      f.handleElement = q = DomHelper.createElement({ 'className': 'b-fill-handle' });
    }
    if (g.rtl) {
      const r = Rectangle.from(g.subGrids[a.column.region].element), u = r.width - k.right, v = l.left > k.left ? l.right - k.left : k.right - l.left;
      DomHelper.setRect(p, {
        'x': u,
        'y': n,
        'width': v,
        'height': o - n
      });
      DomHelper.setTopInsetInlineStart(q, o, u + v);
      q.classList.toggle('b-fill-handle-left-edge', l.left <= 0);
    } else {
      DomHelper.setRect(p, {
        'y': n,
        'x': k.x,
        'width': l.right - k.x,
        'height': o - n
      });
      DomHelper.setTopInsetInlineStart(q, o, l.right);
      q.classList.toggle('b-fill-handle-right-edge', l.right >= j.scrollWidth - 1);
    }
    j.appendChild(p);
    j.appendChild(q);
    f.toggleCroppingCls(false);
    f.croppingCells = null;
    if (f._isCropping && ((e = f.rangeSelection) == null ? void 0 : e.length)) {
      const w = g.getRange(a, b);
      f.croppingCells = f.rangeSelection.filter(z => !w.some(A => A.equals(z, true)));
      f.toggleCroppingCls();
    }
    if (!i.handleClick) {
      i.handleClick = EventHelper.on({
        'element': g.rootElement,
        'delegate': '.b-fill-handle',
        'mousedown': 'onMouseDown',
        'thisObj': f
      });
    }
    f.hasFillElements = true;
  }
  toggleCroppingCls(a = true) {
    let b;
    (b = this.croppingCells) == null ? void 0 : b.forEach(c => {
      let e;
      return (e = this.client.getCell(c)) == null ? void 0 : e.classList.toggle('b-indicate-crop', a);
    });
  }
  removeElements(a = false) {
    let b, c;
    const e = this;
    (b = e.handleElement) == null ? void 0 : b.remove();
    (c = e.borderElement) == null ? void 0 : c.remove();
    if (!a) {
      e.removeListeners();
    }
    e.hasFillElements = false;
  }
  removeListeners() {
    const a = this;
    for (const b in a._fillListeners) {
      a._fillListeners[b]();
    }
    a._fillListeners = {};
  }
  get rangeSelection() {
    if (!this._cachedSelectedRange) {
      this._cachedSelectedRange = this.currentSelectedRange;
    }
    return this._cachedSelectedRange;
  }
  get currentSelectedRange() {
    const {client: a} = this, {store: b} = a;
    let {selectedCells: c} = a;
    if (!a._selectedRows.length && c.length && c.every(({record: e}) => b.indexOf(e) >= 0)) {
      if (c.length > 1) {
        c = c.sort((o, p) => o.rowIndex - p.rowIndex || o.columnIndex - p.columnIndex);
        const [e] = c, [f] = c.slice(-1), g = e.rowIndex, i = e.columnIndex, j = e.region, k = f.rowIndex, l = f.columnIndex, n = (l - i + 1) * (k - g + 1);
        if (c.length === n && c.every(o => o.region === j && o.rowIndex >= g && o.rowIndex <= k && o.columnIndex >= i && o.columnIndex <= l)) {
          return c;
        }
      } else {
        return c;
      }
    }
  }
  onKeyDown(a) {
    if (a.key === 'Escape') {
      this.abort();
    }
  }
  abort() {
    this.finalize(false);
    this.reset();
  }
  reset() {
    this._isExtending = this._isCropping = this.currentRange = this.croppingCells = null;
  }
  doDisable(a) {
    this.removeElements();
    this.reset();
    super.doDisable(a);
  }
};
FillHandle._$name = 'FillHandle';
GridFeatureManager.registerFeature(FillHandle);
const _GridFieldFilterPicker = class _GridFieldFilterPicker extends FieldFilterPicker {
  static configurable = {
    'grid': null,
    'allowedFieldNames': null
  };
  static get $name() {
    return 'GridFieldFilterPicker';
  }
  static get type() {
    return 'gridfieldfilterpicker';
  }
  afterConstruct() {
    let a;
    const b = this;
    if (!b.grid) {
      throw new Error(b.constructor.$name + ' requires \'grid\' to be configured.');
    }
    b.fields = (a = b.fields) != null ? a : {};
    super.afterConstruct();
  }
  updateGrid(a) {
    let b;
    if (!((b = a.store) == null ? void 0 : b.modelClass)) {
      throw new Error('Grid does not have a store with a modelClass defined.');
    }
    if (!a.columns) {
      throw new Error('Grid does not have a column store.');
    }
  }
  static getColumnFields(a, b, c) {
    let e;
    return Object.fromEntries((e = a == null ? void 0 : a.bottomColumns.reduce((f, {
      field: g,
      text: i,
      filterType: j,
      filterable: k
    }) => {
      let l;
      if (!c || c.includes(g)) {
        const n = g && b.getFieldDefinition(g), o = isFilterableField(n) ? n.type : void 0;
        if (o || j || k) {
          f.push([
            g,
            {
              'title': i || g,
              'type': (l = j != null ? j : isSupportedDurationField(n) ? 'duration' : o) != null ? l : 'auto'
            }
          ]);
        }
      }
      return f;
    }, [])) != null ? e : []);
  }
  changeFields(a) {
    let b;
    const c = ObjectHelper.merge({}, _GridFieldFilterPicker.getColumnFields(this.grid.columns, (b = this.grid.store) == null ? void 0 : b.modelClass), a);
    return this.allowedFieldNames ? Object.fromEntries(this.allowedFieldNames.map(e => [
      e,
      c[e]
    ])) : c;
  }
};
const GridFieldFilterPicker = _GridFieldFilterPicker;
GridFieldFilterPicker.initClass();
GridFieldFilterPicker._$name = 'GridFieldFilterPicker';
const GridFieldFilterPickerGroup = class extends FieldFilterPickerGroup {
  static childPickerType = 'gridfieldfilterpicker';
  static configurable = {
    'grid': null,
    'allowedFieldNames': null
  };
  static get $name() {
    return 'GridFieldFilterPickerGroup';
  }
  static get type() {
    return 'gridfieldfilterpickergroup';
  }
  validateConfig() {
    if (!this.grid) {
      throw new Error(this.constructor.$name + ' requires the \'grid\' config property.');
    }
  }
  getFilterPickerConfig(a) {
    const {
      grid: b,
      allowedFieldNames: c,
      fields: e
    } = this;
    return {
      ...super.getFilterPickerConfig(a),
      'fields': e,
      'grid': b,
      'allowedFieldNames': c
    };
  }
  get store() {
    return this.grid.$store;
  }
  set store(a) {
  }
  canManage(a) {
    const b = this;
    return super.canManage(a) && (!b.allowedFieldNames || b.allowedFieldNames.includes(a.property));
  }
};
GridFieldFilterPickerGroup.initClass();
GridFieldFilterPickerGroup._$name = 'GridFieldFilterPickerGroup';
const fieldTypeMap = {
  'date': 'date',
  'int': 'number',
  'integer': 'number',
  'number': 'number',
  'string': 'string',
  'duration': 'duration',
  'time': 'time',
  'auto': 'auto'
};
const allowedFilterTypes = {
  ...filterableFieldDataTypes,
  'relation': true
};
const menuItemsWithSeparators = {
  'filterDateIsToday': true,
  'filterDateIsThisWeek': true,
  'filterDateIsThisMonth': true,
  'filterDateIsThisYear': true
};
const Filter = class extends InstancePlugin {
  static get $name() {
    return 'Filter';
  }
  static get configurable() {
    return {
      'prioritizeColumns': false,
      'keyMap': { 'f': 'showFilterEditorByKey' },
      'legacyMode': false,
      'pickerConfig': null,
      'closeEmptyPopup': false,
      'value': null,
      'property': null,
      'operator': null,
      'defaultOperators': {
        'date': '=',
        'number': '=',
        'string': 'includes',
        'duration': '=',
        'relation': null,
        'auto': '*'
      },
      'allowedOperators': null,
      'defaultEnabled': true
    };
  }
  construct(a, b) {
    const c = this;
    c.closeFilterEditor = c.closeFilterEditor.bind(c);
    c.inferredTypes = {};
    super.construct(a, b);
    c.bindStore(c.store);
    if (b && typeof b === 'object') {
      const e = ObjectHelper.clone(b);
      delete e.prioritizeColumns;
      delete e.legacyMode;
      delete e.pickerConfig;
      delete e.dateFormat;
      delete e.closeEmptyPopup;
      delete e.defaultOperators;
      delete e.defaultEnabled;
      delete e.allowedOperators;
      if (!ObjectHelper.isEmpty(e)) {
        c.store.filter(e, null, a.isConfiguring);
      }
    }
  }
  doDestroy() {
    let a, b;
    (a = this.filterTip) == null ? void 0 : a.destroy();
    (b = this.filterEditorPopup) == null ? void 0 : b.destroy();
    super.doDestroy();
  }
  get store() {
    return this.client.$store;
  }
  bindStore() {
    this.detachListeners('store');
    this.store.ion({
      'name': 'store',
      'beforeFilter': 'onStoreBeforeFilter',
      'filter': 'onStoreFilter',
      'thisObj': this
    });
    if (this.client.isPainted) {
      this.refreshHeaders(false);
    }
  }
  static get pluginConfig() {
    return {
      'chain': [
        'renderHeader',
        'populateCellMenu',
        'populateHeaderMenu',
        'onElementClick',
        'bindStore'
      ]
    };
  }
  refreshHeaders(a) {
    const b = this, {
        client: c,
        store: e
      } = b, f = c.headerContainer;
    if (f) {
      DomHelper.children(f, '.b-filter-icon.b-latest').forEach(g => g.classList.remove('b-latest'));
      if (!b.filterTip) {
        b.filterTip = new Tooltip({
          'forElement': f,
          'forSelector': '.b-filter-icon',
          'getHtml'({activeTarget: g}) {
            return g.dataset.filterText;
          }
        });
      }
      if (!e.isFiltered) {
        b.filterTip.hide();
      }
      c.columns.visibleColumns.forEach(g => {
        if (b.canFilterColumn(g)) {
          const i = e.filters.allValues.filter(({
              property: l,
              disabled: n,
              internal: o,
              isNoOp: p
            }) => l === g.field && !n && !o && !p), j = i.length > 0, k = g.element;
          if (k) {
            const l = g.textWrapper;
            let n = l == null ? void 0 : l.querySelector('.b-filter-icon'), o;
            if (j) {
              const p = '&#x2022 ';
              o = b.L('L{filter}') + ': ' + (i.length > 1 ? '<br/><br/>' : '') + i.map(q => {
                let r, u, v, w;
                let z = (r = q.value) != null ? r : '';
                const A = Array.isArray(z), B = (u = e == null ? void 0 : e.modelRelations) == null ? void 0 : u.find(({foreignKey: E}) => E === q.property);
                if (q.displayValue) {
                  z = q.displayValue;
                } else {
                  if (!b.legacyMode && B) {
                    const {relatedDisplayField: E} = (w = (v = b.pickerConfig) == null ? void 0 : v.fields) == null ? void 0 : w[q.property];
                    if (E) {
                      const F = G => {
                        let H;
                        return (H = B.foreignStore.getById(G)) == null ? void 0 : H[E];
                      };
                      if (A) {
                        z = z.map(F).sort((G, H) => (G != null ? G : '').localeCompare(H != null ? H : ''));
                      } else {
                        z = F(z);
                      }
                    }
                  } else if (g.formatValue && z) {
                    z = A ? z.map(G => g.formatValue(G)) : g.formatValue(z);
                  }
                  if (A) {
                    z = '[ ' + z.join(', ') + ' ]';
                  }
                }
                const C = FieldFilterPicker.localizeOperator(q.operator, b.getFilterType(g));
                return (i.length > 1 ? p : '') + (typeof q === 'string' ? q : C + ' ' + z);
              }).join('<br/><br/>');
            } else {
              o = b.L('L{applyFilter}');
            }
            if (!n) {
              n = DomHelper.createElement({
                'parent': l,
                'tag': 'div',
                'className': 'b-filter-icon',
                'dataset': { 'filterText': o }
              });
            } else {
              n.dataset.filterText = o;
            }
            if (g.field === e.latestFilterField)
              n.classList.add('b-latest');
            k.classList.add('b-filterable');
            k.classList.toggle('b-filter', j);
          }
          g.meta.isFiltered = j;
        }
      });
      if (a) {
        c.refreshRows();
      }
    }
  }
  applyFilter(a, b) {
    const {store: c} = this;
    a.$filter = c.addFilter(this.injectColumnFilterConfig(a, b), true);
    c.filter();
  }
  injectColumnFilterConfig(a, b) {
    const {filterFn: c} = a.filterable;
    return ObjectHelper.assign(b, {
      ...a.filterable,
      ...b,
      'property': a.field,
      [c ? 'filterBy' : '_']: function (e) {
        return c({
          'value': this.value,
          'record': e,
          'operator': this.operator,
          'property': this.property,
          'column': a
        });
      }
    });
  }
  removeFilter(a, b) {
    let c;
    if (!this.legacyMode && !((c = a.filterable) == null ? void 0 : c.filterField)) {
      for (const e of this.getCurrentMultiFilters(a)) {
        if (!b || e.operator === b) {
          this.store.removeFilter(e);
        }
      }
    } else {
      this.store.removeFilter(this.store.filters.getBy('property', a.field));
    }
  }
  disableFilter(a) {
    for (const b of this.getCurrentMultiFilters(a)) {
      b.disabled = true;
      this.store.filter(b);
    }
    this.store.filter();
  }
  getCurrentMultiFilters(a) {
    return this.store.filters.values.filter(b => b.property === a.field);
  }
  getPopupDateItems(a, b, c, e, f, g, i, j) {
    const k = this, l = g;
    function n() {
      k.removeFilter(a);
    }
    function o({event: q}) {
      if (q.key === 'Enter') {
        g();
      }
    }
    function p({
      source: q,
      value: r
    }) {
      if (r == null) {
        n();
      } else {
        k.clearSiblingsFields(q);
        k.applyFilter(a, {
          'operator': q.operator,
          'value': r,
          'displayValue': q._value,
          'type': 'date'
        });
      }
    }
    return [
      ObjectHelper.assign({
        'type': 'date',
        'ref': 'on',
        'placeholder': 'L{on}',
        'localeClass': k,
        'clearable': true,
        'label': '<i class="b-fw-icon b-icon-filter-equal"></i>',
        'value': (c == null ? void 0 : c.operator) === 'sameDay' ? c.value : e,
        'operator': 'sameDay',
        'onKeydown': o,
        'onChange': p,
        'onClose': l,
        'onClear': n
      }, j),
      ObjectHelper.assign({
        'type': 'date',
        'ref': 'before',
        'placeholder': 'L{before}',
        'localeClass': k,
        'clearable': true,
        'label': '<i class="b-fw-icon b-icon-filter-before"></i>',
        'value': (c == null ? void 0 : c.operator) === '<' ? c.value : null,
        'operator': '<',
        'onKeydown': o,
        'onChange': p,
        'onClose': l,
        'onClear': n
      }, j),
      ObjectHelper.assign({
        'type': 'date',
        'ref': 'after',
        'cls': 'b-last-row',
        'placeholder': 'L{after}',
        'localeClass': k,
        'clearable': true,
        'label': '<i class="b-fw-icon b-icon-filter-after"></i>',
        'value': (c == null ? void 0 : c.operator) === '>' ? c.value : null,
        'operator': '>',
        'onKeydown': o,
        'onChange': p,
        'onClose': l,
        'onClear': n
      }, j)
    ];
  }
  getPopupNumberItems(a, b, c, e, f, g, i, j) {
    const k = this, l = g;
    function n() {
      k.removeFilter(a);
    }
    function o({event: q}) {
      if (q.key === 'Enter') {
        g();
      }
    }
    function p({
      source: q,
      value: r
    }) {
      if (r == null) {
        n();
      } else {
        k.clearSiblingsFields(q);
        k.applyFilter(a, {
          'operator': q.operator,
          'value': r
        });
      }
    }
    return [
      ObjectHelper.assign({
        'type': 'number',
        'placeholder': 'L{Filter.equals}',
        'localeClass': k,
        'clearable': true,
        'label': '<i class="b-fw-icon b-icon-filter-equal"></i>',
        'value': (c == null ? void 0 : c.operator) === '=' ? c.value : e,
        'operator': '=',
        'onKeydown': o,
        'onChange': p,
        'onEsc': l,
        'onClear': n
      }, j),
      ObjectHelper.assign({
        'type': 'number',
        'placeholder': 'L{lessThan}',
        'localeClass': k,
        'clearable': true,
        'label': '<i class="b-fw-icon b-icon-filter-less"></i>',
        'value': (c == null ? void 0 : c.operator) === '<' ? c.value : null,
        'operator': '<',
        'onKeydown': o,
        'onChange': p,
        'onEsc': l,
        'onClear': n
      }, j),
      ObjectHelper.assign({
        'type': 'number',
        'cls': 'b-last-row',
        'placeholder': 'L{moreThan}',
        'localeClass': k,
        'clearable': true,
        'label': '<i class="b-fw-icon b-icon-filter-more"></i>',
        'value': (c == null ? void 0 : c.operator) === '>' ? c.value : null,
        'operator': '>',
        'onKeydown': o,
        'onChange': p,
        'onEsc': l,
        'onClear': n
      }, j)
    ];
  }
  clearSiblingsFields(a) {
    let b;
    (b = this.filterEditorPopup) == null ? void 0 : b.items.forEach(c => {
      c !== a && (c == null ? void 0 : c.clear());
    });
  }
  getPopupDurationItems(a, b, c, e, f, g, i, j) {
    const k = this, l = g, n = () => k.removeFilter(a);
    function o({
      source: p,
      value: q
    }) {
      if (q == null) {
        n();
      } else {
        k.clearSiblingsFields(p);
        k.applyFilter(a, {
          'operator': p.operator,
          'value': q
        });
      }
    }
    return [
      ObjectHelper.assign({
        'type': 'duration',
        'placeholder': 'L{Filter.equals}',
        'localeClass': k,
        'clearable': true,
        'label': '<i class="b-fw-icon b-icon-filter-equal"></i>',
        'value': (c == null ? void 0 : c.operator) === '=' ? c.value : e,
        'operator': '=',
        'onChange': o,
        'onEsc': l,
        'onClear': n
      }, j),
      ObjectHelper.assign({
        'type': 'duration',
        'placeholder': 'L{lessThan}',
        'localeClass': k,
        'clearable': true,
        'label': '<i class="b-fw-icon b-icon-filter-less"></i>',
        'value': (c == null ? void 0 : c.operator) === '<' ? c.value : null,
        'operator': '<',
        'onChange': o,
        'onEsc': l,
        'onClear': n
      }, j),
      ObjectHelper.assign({
        'type': 'duration',
        'cls': 'b-last-row',
        'placeholder': 'L{moreThan}',
        'localeClass': k,
        'clearable': true,
        'label': '<i class="b-fw-icon b-icon-filter-more"></i>',
        'value': (c == null ? void 0 : c.operator) === '>' ? c.value : null,
        'operator': '>',
        'onChange': o,
        'onEsc': l,
        'onClear': n
      }, j)
    ];
  }
  getPopupStringItems(a, b, c, e, f, g, i, j) {
    let k;
    const l = this;
    return [ObjectHelper.assign({
        'type': 'text',
        'cls': 'b-last-row',
        'placeholder': 'L{filter}',
        'localeClass': l,
        'clearable': true,
        'label': '<i class="b-fw-icon b-icon-filter-equal"></i>',
        'value': c ? (k = c.value) != null ? k : c : e,
        'operator': '*',
        'onChange'({
          source: n,
          value: o
        }) {
          if (o === '') {
            i();
          } else {
            l.applyFilter(a, {
              'operator': n.operator,
              'value': o,
              'displayValue': n.displayField && n.records ? n.records.map(p => p[n.displayField]).join(', ') : void 0
            });
            if (!n.multiSelect) {
              g();
            }
          }
        },
        'onClose': g,
        'onClear': i
      }, j)];
  }
  getPopupItems(a, b, c, e, f, g, i, j) {
    const k = this;
    if (k.useLegacyModeForColumn(a) || j) {
      switch (b) {
      case 'date':
        return k.getPopupDateItems(...arguments);
      case 'number':
        return k.getPopupNumberItems(...arguments);
      case 'duration':
        return k.getPopupDurationItems(...arguments);
      default:
        return k.getPopupStringItems(...arguments);
      }
    }
    return [k.getFieldFilterPickerGroup(a)];
  }
  getAllowedOperators(a) {
    let b;
    let {allowedOperators: c} = this;
    if ((b = a == null ? void 0 : a.filterable) == null ? void 0 : b.allowedOperators) {
      c = c != null ? c : [];
      c.push(...a.filterable.allowedOperators);
    }
    return c;
  }
  getFieldFilterPickerGroup(a) {
    let b;
    const c = this, e = c.getFilterType(a), f = c.getAllowedOperators(a), g = {
        'fields': {
          [a.field]: {
            'type': e,
            'title': (b = a.text) != null ? b : a.field
          }
        },
        ...c.pickerConfig,
        'type': 'gridfieldfilterpickergroup',
        'ref': 'pickerGroup',
        'limitToProperty': a.field,
        'grid': c.client,
        'propertyFieldCls': 'b-transparent property-field',
        'operatorFieldCls': 'b-transparent operator-field',
        'valueFieldCls': 'b-transparent value-field',
        'internalListeners': {
          'beforeAddFilter': ({filter: i}) => {
            c.injectColumnFilterConfig(a, i);
          },
          'remove': c.onPopupFilterRemove,
          'keydown': c.onPopupKeydown,
          'thisObj': c
        },
        'triggerChangeOnInput': false
      };
    if (f) {
      g.operators = { [e]: FieldFilterPicker.defaultOperators[e].filter(({value: i}) => f.includes(i)) };
    }
    return g;
  }
  onPopupFilterRemove() {
    if (this.closeEmptyPopup && this.filterEditorPopup.widgetMap.pickerGroup.filters.length === 0) {
      this.delay(this.closeFilterEditor, 0);
    }
  }
  onPopupKeydown({event: a}) {
    if (a.key === 'Enter') {
      this.delay(this.closeFilterEditor, 0);
    }
  }
  showFilterEditor(a, b, c, e = false) {
    let f, g;
    a = this.client.columns.getById(a);
    const i = this, {store: j} = i, k = a.element, l = j.filters.getBy('property', a.field), n = i.getFilterType(a), o = i.useLegacyModeForColumn(a);
    if (!i.canFilterColumn(a)) {
      return;
    }
    i.closeFilterEditor();
    const p = i.getPopupItems(a, n, (l == null ? void 0 : l.internal) ? null : l, b, j, i.closeFilterEditor, () => {
      i.removeFilter(a);
      i.closeFilterEditor();
    }, a.filterable.filterField);
    p.forEach(q => q.placeholder = q.placeholder ? this.L(q.placeholder) : q.placeholder);
    i.filterEditorPopup = new Popup({
      'forElement': k,
      'owner': i.client,
      'cls': new DomClassList('b-filter-popup', { 'b-filter-popup-legacymode': o }),
      'scrollAction': 'realign',
      'layout': {
        'type': 'vbox',
        'align': 'stretch'
      },
      'items': p
    });
    if (!o) {
      if (e || !(j == null ? void 0 : j.filters.find(q => q.property === a.field))) {
        const q = i.getAllowedOperators(a), r = (f = i.filterEditorPopup.widgetMap.pickerGroup.operators) != null ? f : FieldFilterPicker.configurable.operators, u = r[n];
        let v = i.defaultOperators[n];
        if (!u.find(({value: w}) => w === v)) {
          v = (g = u[0]) == null ? void 0 : g.value;
        }
        if (q && !q.includes(v)) {
          v = q[0];
        }
        i.filterEditorPopup.widgetMap.pickerGroup.addFilter({
          'type': n,
          'property': a.field,
          'operator': c != null ? c : v,
          'value': b
        });
      }
      i.filterEditorPopup.items[0].focus();
    }
  }
  closeFilterEditor() {
    let a;
    (a = this.filterEditorPopup) == null ? void 0 : a.setTimeout(this.filterEditorPopup.destroy);
    this.filterEditorPopup = null;
  }
  getFilterType(a) {
    let b, c, e, f;
    const g = this, {store: i} = g, j = a.field, k = j && i.modelClass.getFieldDefinition(j), l = (c = (b = a.filterType) != null ? b : fieldTypeMap[a.type]) != null ? c : fieldTypeMap[k == null ? void 0 : k.type], n = (e = i == null ? void 0 : i.modelRelations) == null ? void 0 : e.find(({foreignKey: o}) => o === j);
    if (n) {
      return 'relation';
    } else if (l === 'auto' && i && !g.useLegacyModeForColumn(a)) {
      if (i.totalCount > 0) {
        g.inferredTypes[j] = FieldFilterPicker.inferFieldType(i, j);
      }
      return (f = g.inferredTypes[j]) != null ? f : 'auto';
    } else if (l) {
      return fieldTypeMap[l];
    }
    return 'auto';
  }
  populateCellMenuWithDateItems({
    column: a,
    record: b,
    items: c
  }) {
    const e = a.field, f = this.getFilterType(a);
    if (f === 'date') {
      const g = this, i = b.getValue(e), j = k => {
          g.applyFilter(a, {
            'operator': k,
            'value': i,
            'displayValue': a.formatValue ? a.formatValue(i) : i,
            'type': 'date'
          });
        };
      c.filterDateEquals = {
        'text': 'L{on}',
        'localeClass': g,
        'icon': 'b-fw-icon b-icon-filter-equal',
        'cls': 'b-separator',
        'weight': 300,
        'disabled': g.disabled,
        'onItem': () => j('=')
      };
      c.filterDateBefore = {
        'text': 'L{before}',
        'localeClass': g,
        'icon': 'b-fw-icon b-icon-filter-before',
        'weight': 310,
        'disabled': g.disabled,
        'onItem': () => j('<')
      };
      c.filterDateAfter = {
        'text': 'L{after}',
        'localeClass': g,
        'icon': 'b-fw-icon b-icon-filter-after',
        'weight': 320,
        'disabled': g.disabled,
        'onItem': () => j('>')
      };
    }
  }
  populateCellMenuWithNumberItems({
    column: a,
    record: b,
    items: c
  }) {
    const e = a.field, f = this.getFilterType(a);
    if (f === 'number') {
      const g = this, i = b.getValue(e), j = k => {
          g.applyFilter(a, {
            'operator': k,
            'value': i
          });
        };
      c.filterNumberEquals = {
        'text': 'L{equals}',
        'localeClass': g,
        'icon': 'b-fw-icon b-icon-filter-equal',
        'cls': 'b-separator',
        'weight': 300,
        'disabled': g.disabled,
        'onItem': () => j('=')
      };
      c.filterNumberLess = {
        'text': 'L{lessThan}',
        'localeClass': g,
        'icon': 'b-fw-icon b-icon-filter-less',
        'weight': 310,
        'disabled': g.disabled,
        'onItem': () => j('<')
      };
      c.filterNumberMore = {
        'text': 'L{moreThan}',
        'localeClass': g,
        'icon': 'b-fw-icon b-icon-filter-more',
        'weight': 320,
        'disabled': g.disabled,
        'onItem': () => j('>')
      };
    }
  }
  populateCellMenuWithDurationItems({
    column: a,
    record: b,
    items: c
  }) {
    const e = this.getFilterType(a);
    if (e === 'duration') {
      const f = this, g = a.getFilterableValue(b), i = j => {
          f.applyFilter(a, {
            'operator': j,
            'value': g
          });
        };
      c.filterDurationEquals = {
        'text': 'L{equals}',
        'localeClass': f,
        'icon': 'b-fw-icon b-icon-filter-equal',
        'cls': 'b-separator',
        'weight': 300,
        'disabled': f.disabled,
        'onItem': () => i('=')
      };
      c.filterDurationLess = {
        'text': 'L{lessThan}',
        'localeClass': f,
        'icon': 'b-fw-icon b-icon-filter-less',
        'weight': 310,
        'disabled': f.disabled,
        'onItem': () => i('<')
      };
      c.filterDurationMore = {
        'text': 'L{moreThan}',
        'localeClass': f,
        'icon': 'b-fw-icon b-icon-filter-more',
        'weight': 320,
        'disabled': f.disabled,
        'onItem': () => i('>')
      };
    }
  }
  populateCellMenuWithStringItems({
    column: a,
    record: b,
    items: c
  }) {
    let e, f;
    const g = this.getFilterType(a);
    if (!/(date|number|duration)/.test(g)) {
      const i = this, j = a.getFilterableValue(b), k = (f = (e = a.filterable.filterField) == null ? void 0 : e.operator) != null ? f : '*';
      c.filterStringEquals = {
        'text': 'L{equals}',
        'localeClass': i,
        'icon': 'b-fw-icon b-icon-filter-equal',
        'cls': 'b-separator',
        'weight': 300,
        'disabled': i.disabled,
        'onItem': () => i.applyFilter(a, {
          'value': j,
          'operator': k
        })
      };
    }
  }
  getMenuItems(a, b) {
    const c = this, e = {}, f = c.getFilterType(a);
    if (a.filterable === false) {
      return {};
    }
    if (allowedFilterTypes[f]) {
      const g = b ? a.getFilterableValue(b) : void 0, i = c.getCurrentMultiFilters(a).filter(o => !o.disabled).map(({operator: o}) => o), j = b ? 1 : void 0, k = c.getAllowedOperators(a), l = FieldFilterPicker.defaultOperators[f].filter(({
          value: o,
          argCount: p,
          isArrayValued: q
        }) => !(p > j || q) && (!k || k.includes(o)));
      let n = 300;
      for (const {
            value: o,
            text: p,
            argCount: q
          } of l) {
        const r = 'filter' + StringHelper.capitalize(f) + StringHelper.capitalize(o);
        e[r] = {
          'text': StringHelper.capitalize(FieldFilterPicker.L(p)),
          'weight': n += 10,
          'icon': i.includes(o) ? 'b-icon b-icon-check' : null,
          'disabled': c.disabled,
          'cls': menuItemsWithSeparators[r] ? 'b-separator' : null,
          'onItem': () => c.onOperatorMenuItem(a, g, o, q)
        };
      }
    }
    if (a.meta.isFiltered) {
      Object.assign(e, c.getMenuItemsForFilteredColumn(a, b !== void 0));
    }
    return {
      'filterMenu': {
        'text': 'L{filter}',
        'localeClass': c,
        'menu': e,
        'icon': 'b-fw-icon b-icon-filter',
        'weight': b ? 400 : 100
      }
    };
  }
  onOperatorMenuItem(a, b, c, e = 1) {
    const f = this, g = f.getFilterType(a), i = f.getCurrentMultiFilters(a).find(j => !j.disabled && j.operator === c);
    if (i) {
      f.removeFilter(a, c);
    } else {
      if (e == 0 || b !== void 0) {
        f.applyFilter(a, {
          'property': a.field,
          'operator': c,
          'type': g,
          'value': e === 1 ? b : null,
          'caseSensitive': false,
          'disabled': b == null && e > 0
        });
      } else {
        f.showFilterEditor(a, null, c, true);
      }
    }
  }
  getMenuItemsForFilteredColumn(a, b) {
    const c = this, e = !c.disabled && (c.legacyMode || c.columnHasRemovableFilters(a));
    return {
      'filterEdit': c.legacyMode ? void 0 : {
        'text': 'L{editFilter}',
        'localeClass': c,
        'icon': 'b-fw-icon b-icon-edit',
        'cls': 'b-separator',
        'weight': 500,
        'disabled': !e,
        'onItem': () => c.showFilterEditor(a)
      },
      'filterRemove': {
        'text': 'L{removeFilter}',
        'localeClass': c,
        'icon': 'b-fw-icon b-icon-remove',
        'cls': 'b-separator',
        'weight': 510,
        'disabled': !e,
        'onItem': () => c.removeFilter(a)
      },
      'filterDisable': {
        'text': 'L{disableFilter}',
        'localeClass': c,
        'icon': 'b-fw-icon b-icon-filter-disable',
        'weight': 520,
        'disabled': c.disabled || !c.columnHasEnabledFilters(a),
        'onItem': () => c.disableFilter(a)
      }
    };
  }
  populateCellMenu({
    column: a,
    record: b,
    items: c,
    ...e
  }) {
    const f = this;
    if (f.canFilterColumn(a) && !b.isSpecialRow) {
      if (f.useLegacyModeForColumn(a)) {
        f.populateCellMenuWithDateItems({
          'column': a,
          'record': b,
          'items': c,
          ...e
        });
        f.populateCellMenuWithNumberItems({
          'column': a,
          'record': b,
          'items': c,
          ...e
        });
        f.populateCellMenuWithDurationItems({
          'column': a,
          'record': b,
          'items': c,
          ...e
        });
        f.populateCellMenuWithStringItems({
          'column': a,
          'record': b,
          'items': c,
          ...e
        });
        if (a.meta.isFiltered) {
          Object.assign(c, f.getMenuItemsForFilteredColumn(a, true));
        }
      } else {
        Object.assign(c, f.getMenuItems(a, b));
      }
    }
  }
  columnHasRemovableFilters(a) {
    const b = this;
    return Boolean(b.getCurrentMultiFilters(a).find(c => !b.canDeleteFilter || b.callback(b.canDeleteFilter, b, [c]) !== false));
  }
  columnHasEnabledFilters(a) {
    return Boolean(this.getCurrentMultiFilters(a).find(b => !b.disabled));
  }
  populateHeaderMenu({
    column: a,
    items: b
  }) {
    const c = this;
    if (c.useLegacyModeForColumn(a)) {
      if (a.meta.isFiltered) {
        b.filterEdit = {
          'text': 'L{editFilter}',
          'localeClass': c,
          'weight': 100,
          'icon': 'b-fw-icon b-icon-filter',
          'cls': 'b-separator',
          'disabled': c.disabled,
          'onItem': () => c.showFilterEditor(a)
        };
        b.filterRemove = {
          'text': 'L{removeFilter}',
          'localeClass': c,
          'weight': 110,
          'icon': 'b-fw-icon b-icon-remove',
          'disabled': c.disabled || !c.legacyMode && !c.columnHasRemovableFilters(a),
          'onItem': () => c.removeFilter(a)
        };
        b.filterDisable = {
          'text': 'L{disableFilter}',
          'localeClass': c,
          'icon': 'b-fw-icon b-icon-filter-disable',
          'weight': 115,
          'disabled': c.disabled || !c.columnHasEnabledFilters(a),
          'onItem': () => c.disableFilter(a)
        };
      } else if (c.canFilterColumn(a)) {
        b.filter = {
          'text': 'L{filter}',
          'localeClass': c,
          'weight': 100,
          'icon': 'b-fw-icon b-icon-filter',
          'cls': 'b-separator',
          'disabled': c.disabled,
          'onItem': () => c.showFilterEditor(a)
        };
      }
    } else {
      Object.assign(b, c.getMenuItems(a));
    }
  }
  useLegacyModeForColumn(a) {
    let b;
    return this.legacyMode || ((b = a.filterable) == null ? void 0 : b.filterField) != void 0;
  }
  canFilterColumn(a) {
    let b, c;
    return a.filterable !== false && (((b = a.filterable) == null ? void 0 : b.filter) === true || ((c = a.filterable) == null ? void 0 : c.filter) !== false && this.defaultEnabled !== false);
  }
  onStoreBeforeFilter({filters: a}) {
    const {columns: b} = this.client, c = ArrayHelper.groupBy(a, 'property');
    for (const [e, f] of Object.entries(c)) {
      const g = b.find(i => this.canFilterColumn(i) && i.field === e);
      if (g) {
        g.$filters = f.map((i, j) => {
          let k, l;
          if ((i.columnOwned || this.prioritizeColumns) && !i.internal && ((k = g == null ? void 0 : g.filterable) == null ? void 0 : k.filterFn)) {
            const n = new CollectionFilter({
              'id': (l = i.id) != null ? l : e + '-' + j + '-' + Date.now(),
              'columnOwned': true,
              'property': e,
              'operator': i.operator,
              'value': i.value,
              'displayValue': i.displayValue,
              'disabled': i.disabled,
              'filterBy'(o) {
                return g.filterable.filterFn.call(this, {
                  'value': this.value,
                  'record': o,
                  'operator': this.operator,
                  'property': this.property,
                  'column': g
                });
              }
            });
            a.splice(a.indexOf(i), 1, n);
            return n;
          }
          return i;
        });
      }
    }
  }
  onStoreFilter() {
    this.refreshHeaders(false);
  }
  renderHeader() {
    this.refreshHeaders(false);
  }
  onElementClick({target: a}) {
    if (this.filterEditorPopup) {
      this.closeFilterEditor();
    }
    if (a.classList.contains('b-filter-icon')) {
      const b = a.closest('.b-grid-header');
      this.showFilterEditor(b.dataset.columnId);
      return false;
    }
  }
  showFilterEditorByKey({target: a}) {
    const b = a.matches('.b-grid-header') && a;
    if (b) {
      this.showFilterEditor(b.dataset.columnId);
    }
    return Boolean(b);
  }
  isActionAvailable({
    event: a,
    actionName: b
  }) {
    if (b === 'showFilterEditorByKey') {
      const c = a.target.closest('.b-grid-header'), e = c && this.client.columns.find(f => f.id === c.dataset.columnId);
      return Boolean(e == null ? void 0 : e.filterable);
    }
  }
};
Filter._$name = 'Filter';
GridFeatureManager.registerFeature(Filter);
const complexOperators = {
  '*': null,
  'isIncludedIn': null,
  'startsWith': null,
  'endsWidth': null
};
const FilterBar = class extends InstancePlugin {
  static get $name() {
    return 'FilterBar';
  }
  static get configurable() {
    return {
      'prioritizeColumns': false,
      'keyStrokeFilterDelay': 300,
      'compactMode': false,
      'clearStoreFiltersOnHide': true,
      'hidden': false,
      'filter': null,
      'keyMap': {
        'ArrowUp': {
          'handler': 'disableGridNavigation',
          'preventDefault': false
        },
        'ArrowRight': {
          'handler': 'disableGridNavigation',
          'preventDefault': false
        },
        'ArrowDown': {
          'handler': 'disableGridNavigation',
          'preventDefault': false
        },
        'ArrowLeft': {
          'handler': 'disableGridNavigation',
          'preventDefault': false
        },
        'Enter': {
          'handler': 'disableGridNavigation',
          'preventDefault': false
        }
      },
      'defaultEnabled': true
    };
  }
  static get pluginConfig() {
    return {
      'before': ['renderContents'],
      'chain': [
        'afterColumnsChange',
        'renderHeader',
        'populateHeaderMenu',
        'bindStore'
      ]
    };
  }
  static get properties() {
    return {
      'filterFieldCls': 'b-filter-bar-field',
      'filterFieldInputCls': 'b-filter-bar-field-input',
      'filterableColumnCls': 'b-filter-bar-enabled',
      'filterFieldInputSelector': '.b-filter-bar-field-input',
      'filterableColumnSelector': '.b-filter-bar-enabled',
      'filterParseRegExp': /^\s*([<>=*])?(.*)$/,
      'storeTrackingSuspended': 0
    };
  }
  construct(a, b) {
    const c = this;
    c.onColumnFilterFieldChange = c.onColumnFilterFieldChange.bind(c);
    super.construct(a, Array.isArray(b) ? { 'filter': b } : b);
    c.bindStore(c.store);
    if (c.filter) {
      c.store.filter(c.filter);
    }
    c.gridDetacher = a.ion({
      'beforeElementClick': 'onBeforeElementClick',
      'thisObj': c
    });
  }
  get store() {
    return this.client.$store;
  }
  bindStore(a) {
    this.detachListeners('store');
    a.ion({
      'name': 'store',
      'beforeFilter': 'onStoreBeforeFilter',
      'filter': 'onStoreFilter',
      'thisObj': this
    });
  }
  doDestroy() {
    let a;
    this.destroyFilterBar();
    (a = this.gridDetacher) == null ? void 0 : a.call(this);
    super.doDestroy();
  }
  doDisable(a) {
    const {columns: b} = this.client;
    b == null ? void 0 : b.forEach(c => {
      const e = this.getColumnFilterField(c);
      if (e) {
        e.disabled = a;
      }
    });
    super.doDisable(a);
  }
  updateHidden(a) {
    if (!this.isConfiguring) {
      if (a) {
        this.hideFilterBar();
      } else {
        this.showFilterBar();
      }
    }
  }
  updateCompactMode(a) {
    this.client.headerContainer.classList[a ? 'add' : 'remove']('b-filter-bar-compact');
    for (const b in this._columnFilters) {
      const c = this._columnFilters[b];
      c.placeholder = a ? c.column.headerText : null;
    }
  }
  destroyFilterBar() {
    let a;
    (a = this.client.columns) == null ? void 0 : a.forEach(this.destroyColumnFilterField, this);
  }
  hideFilterBar() {
    let a;
    const b = this;
    b.clearStoreFiltersOnHide && b.suspendStoreTracking();
    (a = b.client.columns) == null ? void 0 : a.forEach(c => b.hideColumnFilterField(c, true));
    b.store.filter();
    b.clearStoreFiltersOnHide && b.resumeStoreTracking();
    b._hidden = true;
  }
  showFilterBar() {
    this.suspendStoreTracking();
    this.renderFilterBar(this.clearStoreFiltersOnHide);
    this.resumeStoreTracking();
    this._hidden = false;
  }
  toggleFilterBar() {
    if (this.hidden) {
      this.showFilterBar();
    } else {
      this.hideFilterBar();
    }
  }
  renderFilterBar(a) {
    if (this.client.hideHeaders) {
      return;
    }
    this.client.columns.visibleColumns.forEach(b => this.renderColumnFilterField(b, a));
    this.rendered = true;
  }
  renderColumnFilterField(a, b) {
    const c = this, {
        client: e,
        store: f
      } = c, g = c.getColumnFilterable(a);
    if (g && a.isVisible) {
      const i = a.element, j = f.filters.get(a.id) || f.filters.getBy('property', a.field);
      let k = c.getColumnFilterField(a);
      if (!k) {
        const l = (a.filterType || 'text') + 'field', {filterField: n} = g, o = n == null ? void 0 : n.cls;
        if (o) {
          delete g.filterField.cls;
        }
        k = Widget.append(ObjectHelper.assign({
          'type': l,
          'cls': {
            [c.filterFieldCls]: 1,
            [o]: o
          },
          'dataset': { 'column': a.field },
          'column': a,
          'owner': e,
          'clearable': true,
          'revertOnEscape': true,
          'name': a.field,
          'value': j && !j._filterBy && !j.internal ? c.buildFilterValue(j) : '',
          'inputCls': c.filterFieldInputCls,
          'keyStrokeChangeDelay': c.keyStrokeFilterDelay,
          'disabled': c.disabled,
          'placeholder': c.compactMode ? a.headerText : null,
          'format': a.format,
          'internalListeners': {
            'change': c.onColumnFilterFieldChange,
            'clear': c.onColumnFilterFieldChange
          }
        }, n), i)[0];
        if (!(n == null ? void 0 : n.hasOwnProperty('min'))) {
          Object.defineProperty(k, 'min', {
            'get': () => {
              let q;
              return (q = a.editor) == null ? void 0 : q.min;
            },
            'set': () => null
          });
        }
        if (!(n == null ? void 0 : n.hasOwnProperty('max'))) {
          Object.defineProperty(k, 'max', {
            'get': () => {
              let q;
              return (q = a.editor) == null ? void 0 : q.max;
            },
            'set': () => null
          });
        }
        if (!(n == null ? void 0 : n.hasOwnProperty('strictParsing'))) {
          Object.defineProperty(k, 'strictParsing', {
            'get': () => {
              let q;
              return (q = a.editor) == null ? void 0 : q.strictParsing;
            },
            'set': () => null
          });
        }
        k.element.retainElement = true;
        c.setColumnFilterField(a, k);
        const p = (n == null ? void 0 : n.store) && (n.store.readUrl || n.store.data || n.store.isChained) || (n == null ? void 0 : n.items);
        if (k.isCombo && !p && k.store.count === 0) {
          const q = k.value, r = () => {
              if (!(k.store.readUrl || k.store.isChained)) {
                k.store.data = e.store.getDistinctValues(a.field, true).map(u => e.store.modelClass.new({
                  'id': u,
                  [a.field]: u
                }));
              }
            };
          k.value = null;
          if (!k.store.isSorted) {
            k.store.sort({
              'field': a.field,
              'ascending': true
            });
          }
          k.picker.ion({ 'beforeShow': r });
          r();
          k.value = q;
        }
        if (!c.filter && k.value && f.autoLoad !== false) {
          c.onColumnFilterFieldChange({
            'source': k,
            'value': k.value,
            'silent': true
          });
        }
      } else {
        if (b) {
          c.onColumnFilterFieldChange({
            'source': k,
            'value': k.value
          });
        }
        k.render(i);
        k.show();
      }
      i.classList.add(c.filterableColumnCls);
    }
  }
  updateColumnFilterFields() {
    const a = this, {store: b} = a, {columns: c} = a.client;
    let e, f;
    a._updatingFields = true;
    for (const g of c.visibleColumns) {
      e = a.getColumnFilterField(g);
      if (e && !g.$isApplyingFilter) {
        f = b.filters.get(g.id) || b.filters.getBy('property', g.field);
        if (f && !f.internal) {
          if (!f._filterBy) {
            e.value = a.buildFilterValue(f);
          } else {
            e.value = f.value;
          }
        } else {
          e.value = '';
        }
      }
    }
    a._updatingFields = false;
  }
  getColumnFilterable(a) {
    if (!a.isRoot && this.canFilterColumn(a) && a.field && a.isLeaf) {
      if (typeof a.filterable === 'function') {
        a.filterable = { 'filterFn': a.filterable };
      }
      return a.filterable;
    }
  }
  destroyColumnFilterField(a) {
    const b = this.getColumnFilterField(a);
    if (b) {
      this.hideColumnFilterField(a, true);
      b.destroy();
      this.setColumnFilterField(a, void 0);
    }
  }
  hideColumnFilterField(a, b) {
    const c = this, {store: e} = c, f = a.element, g = c.getColumnFilterField(a);
    if (g) {
      if (!c.isDestroying) {
        g.hide();
      }
      const {$filter: i} = a;
      if (!e.isDestroyed && c.clearStoreFiltersOnHide && i) {
        e.removeFilter(i, b);
      }
      f == null ? void 0 : f.classList.remove(c.filterableColumnCls);
    }
  }
  getColumnFilterField(a) {
    let b;
    return (b = this._columnFilters) == null ? void 0 : b[a.id];
  }
  setColumnFilterField(a, b) {
    this._columnFilters = this._columnFilters || {};
    this._columnFilters[a.data.id] = b;
  }
  parseFilterValue(a, b, c) {
    let e;
    if (Array.isArray(b) || b instanceof Duration || typeof b === 'number') {
      return { 'value': b };
    }
    if (ObjectHelper.isDate(b)) {
      return {
        'operator': c.isDateField ? 'sameDay' : c.isTimeField ? 'sameTime' : '=',
        'value': b
      };
    }
    const f = String(b).match(this.filterParseRegExp), g = typeof b === 'number' ? '=' : '*';
    return {
      'operator': f[1] || ((e = a.filterable) == null ? void 0 : e.operator) || g,
      'value': f[2]
    };
  }
  buildFilterValue({
    operator: a,
    value: b
  }) {
    return typeof b === 'number' || b instanceof Date || Array.isArray(b) || b instanceof Duration ? b : (a in complexOperators ? '' : a) + b;
  }
  onStoreBeforeFilter({filters: a}) {
    let b;
    const {columns: c} = this.client;
    for (let e = 0; e < a.count; e++) {
      const f = a.getAt(e), g = (f.columnOwned || this.prioritizeColumns) && c.find(j => this.canFilterColumn(j) && j.field === f.property);
      if ((b = g == null ? void 0 : g.filterable) == null ? void 0 : b.filterFn) {
        if (!g.$filter) {
          g.$filter = new CollectionFilter({
            'columnOwned': true,
            'property': f.property,
            'id': g.id,
            'filterBy'(j) {
              return g.filterable.filterFn({
                'value': this.value,
                'record': j,
                'property': this.property,
                'column': g
              });
            }
          });
        }
        g.$filter.value = f.value;
        a.splice(e, 1, g.$filter);
      }
    }
  }
  onStoreFilter() {
    if (!this.storeTrackingSuspended && this.rendered) {
      this.updateColumnFilterFields();
    }
  }
  afterColumnsChange({
    action: a,
    changes: b,
    column: c,
    columns: e
  }) {
    let f, g, i;
    const j = this;
    for (const k of j.client.columns.visibleColumns) {
      if (j.canFilterColumn(k) && (((f = k.filterable) == null ? void 0 : f.filter) === true || ((g = j.client.features.filter) == null ? void 0 : g.defaultEnabled) && ((i = k.filterable) == null ? void 0 : i.filter) !== false)) {
        throw new Error('Columns do not currently support both `filter` and `filterBar` features enabled on the same column. Please check column with id ' + k.id + '.');
      }
    }
    if (!j.hidden && (b == null ? void 0 : b.hidden)) {
      const l = b.hidden.value;
      if (l) {
        j.destroyColumnFilterField(c);
      } else {
        j.renderColumnFilterField(c);
      }
    }
    if (a === 'remove') {
      e.forEach(n => j.destroyColumnFilterField(n));
    }
  }
  suspendStoreTracking() {
    this.storeTrackingSuspended++;
  }
  resumeStoreTracking() {
    this.storeTrackingSuspended--;
  }
  renderHeader() {
    if (!this.hidden) {
      this.renderFilterBar();
    }
  }
  renderContents() {
    if (this._columnFilters) {
      for (const a of Object.values(this._columnFilters)) {
        a == null ? void 0 : a.element.remove();
      }
    }
  }
  disableGridNavigation(a) {
    return a.target.matches(this.filterFieldInputSelector);
  }
  onBeforeElementClick({event: a}) {
    if (a.target.closest('.' + this.filterFieldCls)) {
      return false;
    }
  }
  onColumnFilterFieldChange({
    source: a,
    value: b,
    silent: c
  }) {
    let e, f, g, i, j;
    const k = this, {column: l} = a, {filterFn: n} = l.filterable, {store: o} = k, p = l.$filter || o.filters.find(r => (r.id === l.id || r.property === l.field) && !r.internal);
    if (k._updatingFields) {
      return;
    }
    const q = b == null || b === '' || Array.isArray(b) && b.length === 0;
    o.removeFilter(p, true);
    l.$filter = null;
    l.$isApplyingFilter = true;
    if (q) {
      if (!p) {
        l.$isApplyingFilter = false;
        return;
      }
    } else {
      l.$filter = o.addFilter({
        'property': a.name,
        ...k.parseFilterValue(l, b, a),
        [typeof ((e = l.filterable) == null ? void 0 : e.caseSensitive) === 'boolean' ? 'caseSensitive' : void 0]: (f = l.filterable) == null ? void 0 : f.caseSensitive,
        [n ? 'filterBy' : '_']: function (r) {
          return n({
            'value': this.value,
            'record': r,
            'operator': this.operator,
            'property': this.property,
            'column': l
          });
        }
      }, true);
    }
    o.filter({ 'silent': c });
    l.$isApplyingFilter = false;
    if ((g = a.picker) == null ? void 0 : g.isVisible) {
      (j = (i = a.picker) == null ? void 0 : i.realign) == null ? void 0 : j.call(i);
    }
  }
  populateHeaderMenu({items: a}) {
    a.toggleFilterBar = {
      'text': this.hidden ? 'L{enableFilterBar}' : 'L{disableFilterBar}',
      'localeClass': this,
      'weight': 120,
      'icon': 'b-fw-icon b-icon-filter',
      'cls': 'b-separator',
      'onItem': () => this.toggleFilterBar()
    };
  }
  canFilterColumn(a) {
    let b, c;
    return a.filterable !== false && (((b = a.filterable) == null ? void 0 : b.filterBar) === true || ((c = a.filterable) == null ? void 0 : c.filterBar) !== false && this.defaultEnabled !== false);
  }
};
FilterBar.featureClass = 'b-filter-bar';
FilterBar._$name = 'FilterBar';
GridFeatureManager.registerFeature(FilterBar);
const Group = class extends InstancePlugin {
  static get $name() {
    return 'Group';
  }
  static get configurable() {
    return {
      'field': null,
      'expandIconCls': 'b-icon-group-expand',
      'collapseIconCls': 'b-icon-group-collapse',
      'ascending': true,
      'headerHeight': null,
      'showCount': true,
      'groupSortFn': null,
      'renderer': null,
      'keyMap': { ' ': 'toggleGroup' },
      'toggleOnRowClick': true
    };
  }
  construct(a, b) {
    let c;
    const e = this;
    if (a.features.tree) {
      return;
    }
    e._thisIsAUsedExpression(a.features.groupSummary);
    b = e.processConfig(b);
    e.grid = a;
    super.construct(a, b);
    a.ion({
      'lockRows': 'onLockRows',
      'beforeUnlockRows': 'onUnlockRows',
      'thisObj': e
    });
    if (!((c = a.features.lockRows) == null ? void 0 : c.enabled) || a.splitFrom) {
      e.init(a);
    }
  }
  init(a) {
    this.grid = a;
    this.bindStore(a.store);
    this.detachListeners('rowManager');
    a.rowManager.ion({
      'name': 'rowManager',
      'beforeRenderRow': 'onBeforeRenderRow',
      'renderCell': 'renderCell',
      'prio': 1100,
      'thisObj': this
    });
    if (!a.isConfiguring) {
      a.renderRows();
    }
  }
  processConfig(a) {
    if (typeof a === 'string') {
      return { 'field': a };
    }
    return a;
  }
  setConfig(a) {
    if (a === null) {
      this.store.clearGroupers();
    } else {
      super.setConfig(this.processConfig(a));
    }
  }
  bindStore(a) {
    const b = this, {client: c} = b;
    b.detachListeners('store');
    if (c.isLockedRows) {
      return;
    }
    a.ion({
      'name': 'store',
      'group': 'onStoreGroup',
      'change': 'onStoreChange',
      'toggleGroup': 'onStoreToggleGroup',
      'thisObj': b
    });
    if ((b.field || a.isGrouped) && !c.isConfiguring) {
      a.group({
        'field': b.field,
        'ascending': b.ascending,
        'fn': b.groupSortFn
      });
    } else {
      b.onStoreGroup({ 'groupers': a.groupers });
    }
  }
  updateRenderer(a) {
    this.groupRenderer = a;
  }
  updateField(a) {
    let b, c;
    const e = this;
    if (((b = e.client.features.lockRows) == null ? void 0 : b.enabled) && !e.client.splitFrom) {
      return;
    }
    if (!e.isConfiguring || !((c = e.store.groupers) == null ? void 0 : c.some(f => f.field === a))) {
      e.store.group({
        'field': a,
        'ascending': e.ascending,
        'fn': e.groupSortFn
      });
    }
  }
  updateGroupSortFn(a) {
    if (!this.isConfiguring) {
      this.store.group({
        'field': this.field,
        'ascending': this.ascending,
        'fn': a
      });
    }
  }
  updateAscending(a) {
    const b = this;
    if (!b.isConfiguring) {
      b.store.group({
        'field': b.field,
        'ascending': a,
        'fn': b.groupSortFn
      });
    }
  }
  doDisable(a) {
    const {store: b} = this;
    if (a && b.isGrouped) {
      const {sorters: c} = b;
      c.unshift(...b.groupers);
      this.currentGroupers = b.groupers;
      b.clearGroupers();
      b.sort(c);
    } else if (!a && this.currentGroupers) {
      b.group(this.currentGroupers[0]);
      this.currentGroupers = null;
    }
    super.doDisable(a);
  }
  get store() {
    return this.grid.store;
  }
  static get pluginConfig() {
    return {
      'assign': [
        'collapseAll',
        'expandAll'
      ],
      'chain': [
        'renderHeader',
        'populateHeaderMenu',
        'getColumnDragToolbarItems',
        'onElementTouchStart',
        'onElementClick',
        'bindStore'
      ]
    };
  }
  refreshGrid(a) {
    const {
      store: b,
      rowManager: c
    } = this.grid;
    if (c.rowCount > b.count || !c.getRowFor(a)) {
      c.renderFromRow();
    } else {
      c.renderFromRecord(a);
    }
  }
  toggleCollapse(a, b) {
    this.internalToggleCollapse(a, b);
  }
  internalToggleCollapse(a, b, c = false, e, f = false) {
    const g = this, {
        store: i,
        grid: j
      } = g;
    let k = i.getById(a);
    if (!k.isGroupHeader) {
      k = i.getGroupHeaderForRecord(k);
    }
    if (!k.isGroupHeader) {
      return;
    }
    b = b === void 0 ? !k.meta.collapsed : b;
    if (j.trigger('beforeToggleGroup', {
        'groupRecord': k,
        'collapse': b,
        'domEvent': e
      }) === false) {
      return;
    }
    g.isToggling = true;
    if (b) {
      i.collapse(k);
    } else {
      i.expand(k);
    }
    g.isToggling = false;
    if (!c) {
      g.refreshGrid(k);
    }
    j.trigger('toggleGroup', {
      'groupRecord': k,
      'collapse': b,
      'allRecords': f
    });
    j.afterToggleGroup();
  }
  collapseAll() {
    const a = this, {
        store: b,
        grid: c
      } = a;
    if (b.isGrouped && !a.disabled) {
      b.groupRecords.forEach(e => a.internalToggleCollapse(e, true, true, void 0, true));
      c.trigger('collapseAllGroups');
      c.refreshRows(null, true);
    }
  }
  expandAll() {
    const a = this, {
        store: b,
        grid: c
      } = a;
    if (b.isGrouped && !a.disabled) {
      b.groupRecords.forEach(e => a.internalToggleCollapse(e, false, true, void 0, true));
      c.trigger('expandAllGroups');
      c.refreshRows();
    }
  }
  onLockRows({clone: a}) {
    this.grid = a;
  }
  onUnlockRows({source: a}) {
    const {client: b} = this;
    if (!b.isDestroying) {
      this.init(b);
    }
  }
  onBeforeRenderRow({row: a}) {
    const b = a.grid.store.getById(a.id);
    a.forceInnerHTML = a.forceInnerHTML || (b == null ? void 0 : b.isGroupHeader);
  }
  renderCell(a) {
    let b;
    const c = this, {
        cellElement: e,
        row: f,
        column: g,
        grid: i
      } = a, {meta: j} = a.record, k = {
        'b-group-row': 0,
        'b-grid-group-collapsed': 0
      };
    if (!c.disabled && c.store.isGrouped && 'groupRowFor' in j) {
      if (g.type === 'action') {
        return;
      }
      g.clearCell(e);
      k['b-grid-group-collapsed'] = j.collapsed;
      k['b-group-row'] = 1;
      if (i.buildGroupHeader) {
        i.buildGroupHeader(a);
      } else {
        c.buildGroupHeader(a);
      }
      if (g === c.groupHeaderColumn) {
        DomHelper.createElement({
          'parent': e,
          'tag': 'i',
          'className': {
            'b-group-state-icon': 1,
            'b-fw-icon': 1,
            [c.expandIconCls]: j.collapsed,
            [c.collapseIconCls]: !j.collapsed
          },
          'nextSibling': e.firstChild
        });
        e.classList.add('b-group-title');
        e.$groupHeader = e._hasHtml = true;
      }
    } else if (e.$groupHeader) {
      (b = e.querySelector('.b-group-state-icon')) == null ? void 0 : b.remove();
      e.classList.remove('b-group-title');
      e.$groupHeader = false;
    }
    f.assignCls(k);
  }
  buildGroupHeader(a) {
    let b;
    const c = this, {
        record: e,
        cellElement: f,
        column: g,
        persist: i
      } = a, {grid: j} = c, k = e.meta, l = k.emptyArray ? j.L('L{Object.None}') : k.groupRowFor, {groupSummary: n} = j.features, o = k.childCount - (n && n.target !== 'header' ? 1 : 0);
    let p = null, q = true;
    if (i || g) {
      const r = j.columns.get(k.groupField), u = a.isFirstColumn = g === c.groupHeaderColumn;
      if (r == null ? void 0 : r.groupRenderer) {
        if (u) {
          p = r.groupRenderer({
            ...a,
            'groupRowFor': l,
            'groupRecords': e.groupChildren,
            'groupColumn': r,
            'count': o
          });
          q = false;
        }
      } else if (c.groupRenderer) {
        p = c.groupRenderer({
          ...a,
          'groupRowFor': l,
          'groupRecords': e.groupChildren,
          'groupColumn': r,
          'count': o,
          'isFirstColumn': u
        });
      }
      if (!((b = a.size) == null ? void 0 : b.height) && c.headerHeight) {
        a.size.height = c.headerHeight;
      }
      if (u && p == null && q && DomHelper.getChildElementCount(f) === 0) {
        p = StringHelper.encodeHtml('' + (l === '__novalue__' ? '' : l) + (c.showCount ? ' (' + o + ')' : ''));
      }
    } else if (c.groupRenderer) {
      p = c.groupRenderer(a);
    }
    if (typeof p === 'string') {
      f.innerHTML = p;
    } else if (typeof p === 'object') {
      DomSync.sync({
        'targetElement': f,
        'domConfig': {
          'onlyChildren': true,
          'children': ArrayHelper.asArray(p)
        }
      });
    }
    if (DomHelper.getChildElementCount(f) > 0) {
      f._hasHtml = true;
    }
    return f.innerHTML;
  }
  get groupHeaderColumn() {
    return this.grid.columns.visibleColumns.find(a => !a.groupHeaderReserved);
  }
  renderHeader(a = this.grid.headerContainer) {
    let b;
    const {
      store: c,
      grid: e
    } = this;
    if (a && c.isGrouped) {
      for (const f of c.groupers) {
        const g = e.columns.get(f.field), i = g && e.getHeaderElement(g.id);
        i == null ? void 0 : i.classList.add('b-group', f.ascending ? 'b-asc' : 'b-desc');
        if (i && (!((b = e.features.sort) == null ? void 0 : b.enabled) || g.sortable === false)) {
          const j = g.textWrapper;
          if (!(j == null ? void 0 : j.querySelector('.b-sort-icon'))) {
            DomHelper.createElement({
              'parent': j,
              'className': 'b-sort-icon'
            });
          }
        }
      }
    }
  }
  populateHeaderMenu({
    column: a,
    items: b
  }) {
    const c = this;
    if (a.groupable !== false) {
      b.groupAsc = {
        'text': 'L{groupAscending}',
        'localeClass': c,
        'icon': 'b-fw-icon b-icon-group-asc',
        'cls': 'b-separator',
        'weight': 400,
        'disabled': c.disabled,
        'onItem': () => c.store.group(a.field, true)
      };
      b.groupDesc = {
        'text': 'L{groupDescending}',
        'localeClass': c,
        'icon': 'b-fw-icon b-icon-group-desc',
        'weight': 410,
        'disabled': c.disabled,
        'onItem': () => c.store.group(a.field, false)
      };
    }
    if (c.store.isGrouped) {
      b.groupRemove = {
        'text': 'L{stopGrouping}',
        'localeClass': c,
        'icon': 'b-fw-icon b-icon-clear',
        'cls': a.groupable ? '' : 'b-separator',
        'weight': 420,
        'disabled': c.disabled,
        'onItem': () => c.store.clearGroupers()
      };
    }
  }
  getColumnDragToolbarItems(a, b) {
    let c;
    const e = this, {
        store: f,
        disabled: g
      } = e;
    b.push({
      'text': 'L{groupAscendingShort}',
      'group': 'L{group}',
      'localeClass': e,
      'icon': 'b-icon b-icon-group-asc',
      'ref': 'groupAsc',
      'cls': 'b-separator',
      'weight': 110,
      'disabled': g,
      'onDrop': ({column: j}) => f.group(j.field, true)
    });
    b.push({
      'text': 'L{groupDescendingShort}',
      'group': 'L{group}',
      'localeClass': e,
      'icon': 'b-icon b-icon-group-desc',
      'ref': 'groupDesc',
      'weight': 110,
      'disabled': g,
      'onDrop': ({column: j}) => f.group(j.field, false)
    });
    const i = ((c = f.groupers) == null ? void 0 : c.some(j => j.field === a.field)) && !g;
    b.push({
      'text': 'L{stopGroupingShort}',
      'group': 'L{group}',
      'localeClass': e,
      'icon': 'b-icon b-icon-clear',
      'ref': 'groupRemove',
      'disabled': !i,
      'weight': 110,
      'onDrop': ({column: j}) => f.removeGrouper(j.field)
    });
    return b;
  }
  onStoreGroup({groupers: a}) {
    const {grid: b} = this, {element: c} = b, e = c && DomHelper.children(c, '.b-grid-header.b-group');
    if (c) {
      for (const f of e) {
        f.classList.remove('b-group', 'b-asc', 'b-desc');
      }
      if (a) {
        this.renderHeader();
      }
    }
  }
  onStoreChange({
    action: a,
    records: b
  }) {
    const {client: c} = this, {
        rowManager: e,
        store: f
      } = c;
    if (f.isGrouped && a === 'move') {
      const {field: g} = f.groupers[0], i = Math.min(...b.reduce((j, k) => {
          j.push(f.indexOf(k.instanceMeta(f).groupParent));
          if (g in k.meta.modified) {
            const l = f.groupRecords.find(n => n.meta.groupRowFor === k.meta.modified[g]);
            if (l) {
              j.push(f.indexOf(l));
            }
          }
          return j;
        }, []));
      e.renderFromRow(e.getRow(i));
    }
  }
  onStoreToggleGroup({groupRecord: a}) {
    if (!this.isToggling) {
      this.refreshGrid(a);
    }
  }
  onElementTouchStart(a) {
    const b = this, {target: c} = a, e = c.closest('.b-grid-header'), f = e && b.grid.getColumnFromElement(e);
    if (a.touches.length > 1 && f && f.groupable !== false && !b.disabled) {
      b.store.group(f.field);
    }
  }
  onElementClick(a) {
    let b;
    const c = this, {store: e} = c, {target: f} = a, g = f.closest('.b-group-row'), i = f.closest('.b-grid-header'), j = i == null ? void 0 : i.dataset.column;
    if (f.classList.contains('b-resizer') || c.disabled || f.classList.contains('b-action-item') || a.handled) {
      return;
    }
    if (i && j) {
      const k = (b = e.groupers) == null ? void 0 : b.find(l => l.field === j);
      if (k && !a.shiftKey) {
        k.ascending = !k.ascending;
        e.group();
        return false;
      } else if (a.shiftKey) {
        const l = c.grid.columns.get(j);
        if (l.groupable !== false) {
          if (a.altKey) {
            e.removeGrouper(j);
          } else {
            e.group(j);
          }
        }
      }
    }
    if (g && (c.toggleOnRowClick || a.target.classList.contains('b-group-state-icon'))) {
      c.internalToggleCollapse(DomDataStore.get(g).id, void 0, void 0, a);
      return false;
    }
  }
  toggleGroup() {
    let a;
    const {grid: b} = this, {focusedCell: c} = b;
    if (!this.disabled && !c.isActionable && ((a = c.record) == null ? void 0 : a.isGroupHeader)) {
      this.internalToggleCollapse(c.id);
      return true;
    }
    return false;
  }
  updateHeaderHeight() {
    if (!this.isConfiguring) {
      this.client.refreshRows();
    }
  }
  updateShowCount() {
    if (!this.isConfiguring) {
      this.client.refreshRows();
    }
  }
};
Group._$name = 'Group';
GridFeatureManager.registerFeature(Group, true, [
  'Grid',
  'Scheduler'
]);
GridFeatureManager.registerFeature(Group, false, ['TreeGrid']);
const SummaryFormatter_default = a => class b extends (a || Base) {
  static get $name() {
    return 'SummaryFormatter';
  }
  generateHtml(c, e, f, g, i, j) {
    const k = this.store, l = c.summaries || (c.sum ? [{
          'sum': c.sum,
          'renderer': c.summaryRenderer
        }] : []);
    let n = '<div class="b-summary-wrap ' + f + '">';
    l.forEach(o => {
      let p = o.sum, q = null;
      if (p === true)
        p = 'sum';
      switch (p) {
      case 'sum':
      case 'add':
        q = k.sum(c.field, e);
        break;
      case 'max':
        q = k.max(c.field, e);
        break;
      case 'min':
        q = k.min(c.field, e);
        break;
      case 'average':
      case 'avg':
        q = k.average(c.field, e);
        break;
      case 'count':
        q = e.length;
        break;
      case 'countNotEmpty':
        q = e.reduce((r, u) => {
          const v = u.getValue(c.field);
          return r + (v != null ? 1 : 0);
        }, 0);
        break;
      }
      if (typeof p === 'function') {
        q = e.reduce(p, 'seed' in o ? o.seed : 0);
      }
      if (q !== null) {
        const r = 'b-grid-summary-value', u = o.label ? '<div class="b-grid-summary-label">' + o.label + '</div>' : '';
        let v = o.renderer ? o.renderer({
            'config': o,
            'sum': q
          }) : q, w;
        if (v == null) {
          v = '';
        }
        if (!String(v).includes('<div>')) {
          w = u ? u + '<div class="' + r + '">' + v + '</div>' : '<div class="' + r + ' b-nolabel">' + v + '</div>';
        } else {
          w = v;
        }
        n += w;
      }
    });
    return n + '</div>';
  }
};
const GroupSummary = class extends SummaryFormatter_default(InstancePlugin) {
  static get $name() {
    return 'GroupSummary';
  }
  static get configurable() {
    return {
      'collapseToHeader': null,
      'target': 'footer'
    };
  }
  construct(a, b) {
    this.grid = a;
    super.construct(a, b);
    if (!a.features.group) {
      throw new Error('Requires Group feature to work, please enable');
    }
    this.bindStore(a.store);
    a.rowManager.ion({
      'beforeRenderRow': 'onBeforeRenderRow',
      'renderCell': 'renderCell',
      'prio': 1000,
      'thisObj': this
    });
  }
  bindStore(a) {
    this.detachListeners('store');
    a.ion({
      'name': 'store',
      'update': 'onStoreUpdate',
      'prio': 1,
      'thisObj': this
    });
  }
  get store() {
    return this.grid.store;
  }
  doDisable(a) {
    this.updateTarget(this.target);
    super.doDisable(a);
  }
  changeTarget(a) {
    ObjectHelper.assertString(a, 'target');
    return a;
  }
  updateTarget(a) {
    this.store.useGroupFooters = !this.disabled && a === 'footer';
    if (!this.isConfiguring) {
      this.store.group();
    }
  }
  changeCollapseToHeader(a) {
    ObjectHelper.assertBoolean(a, 'collapseToHeader');
    return a;
  }
  updateCollapseToHeader() {
    if (!this.isConfiguring) {
      this.store.group();
    }
  }
  static get pluginConfig() {
    return { 'chain': ['bindStore'] };
  }
  onBeforeRenderRow({
    row: a,
    record: b
  }) {
    if (a.isGroupFooter && !('groupFooterFor' in b.meta)) {
      a.isGroupFooter = false;
      a.forceInnerHTML = true;
    } else if (a.isGroupHeader && !b.meta.collapsed) {
      a.eachElement(this.removeSummaryElements);
    }
  }
  removeSummaryElements(a) {
  }
  renderCell({
    column: a,
    cellElement: b,
    row: c,
    record: e,
    size: f,
    isFirstColumn: g
  }) {
    const i = this, {meta: j} = e, {rowHeight: k} = i.grid, l = 'groupRowFor' in j, n = 'groupFooterFor' in j, o = i.target === 'header', p = {
        'b-group-footer': 0,
        'b-header-summary': 0
      }, q = l && (o || i.collapseToHeader && j.collapsed) && !g || n && !o;
    if (l || n) {
      f.height = l ? f.height || k : k;
    }
    if (i.store.isGrouped && q && !i.disabled) {
      a.clearCell(b);
      const r = l ? e : j.groupRecord;
      c.isGroupFooter = n;
      c.isGroupHeader = l;
      if (n) {
        p['b-group-footer'] = 1;
      } else {
        p['b-header-summary'] = 1;
      }
      const u = i.updateSummaryHtml(b, a, r), v = typeof u === 'number' ? u : u.count;
      if (v > 1) {
        f.height += j.collapsed && !o ? 0 : v * k * 0.1;
      }
      if (u.height) {
        f.height += u.height;
      }
    }
    c.assignCls(p);
  }
  updateSummaryHtml(a, b, c) {
    const e = c.groupChildren.slice();
    if (e[e.length - 1].isGroupFooter) {
      e.pop();
    }
    const f = this.generateHtml(b, e, 'b-grid-group-summary', c, c.meta.groupField, c.meta.groupRowFor);
    if (!a.children.length) {
      a.innerHTML = f;
    } else {
      DomHelper.sync(f, a.firstElementChild);
    }
    return b.summaries ? b.summaries.length : b.sum ? 1 : 0;
  }
  onStoreUpdate({
    source: a,
    changes: b
  }) {
    if (!this.disabled && a.isGrouped) {
      if (b && a.groupers.find(e => e.field in b)) {
        return;
      }
      const c = Object.keys(b).some(e => {
        const f = this.grid.columns.get(e);
        return Boolean(f) && (Boolean(f.sum) || Boolean(f.summaries));
      });
      if (c) {
        this.grid.forceFullRefresh = true;
      }
    }
  }
  refresh() {
    this.grid.columns.visibleColumns.forEach(a => {
      if (this.hasSummary(a)) {
        this.grid.refreshColumn(a);
      }
    });
  }
  hasSummary(a) {
    return a.sum || a.summaries;
  }
};
GroupSummary.featureClass = 'b-group-summary';
GroupSummary._$name = 'GroupSummary';
GridFeatureManager.registerFeature(GroupSummary);
const startScrollOptions = Object.freeze({
  'animate': false,
  'block': 'start'
});
const endScrollOptions = Object.freeze({
  'animate': false,
  'block': 'end'
});
const splitterWidth = 7;
const ignoreListeners = {
  'split': 1,
  'unsplit': 1
};
let _ignoreColumnChanges;
const Split = class extends InstancePlugin {
  static configurable = {
    'subViews': [],
    'relayProperties': {
      'value': {
        'readOnly': 1,
        'rowHeight': 1
      },
      '$config': { 'merge': 'merge' }
    }
  };
  static featureClass = '';
  static $name = 'Split';
  restorers = [];
  constructor() {
    super(...arguments);
    __privateAdd(this, _ignoreColumnChanges, false);
  }
  static get pluginConfig() {
    return {
      'chain': [
        'populateCellMenu',
        'afterConfigChange',
        'afterAddListener',
        'afterRemoveListener',
        {
          'fn': 'onInternalPaint',
          'prio': -1000
        }
      ],
      'assign': [
        'split',
        'unsplit',
        'subViews',
        'syncSplits'
      ]
    };
  }
  doDestroy() {
    this.unsplit(true);
    super.doDestroy();
  }
  doDisable(a) {
    const b = this;
    if (!b.isConfiguring) {
      if (a) {
        b._disabledSplitOptions = b._splitOptions;
        b.unsplit();
      } else if (b._disabledSplitOptions) {
        b.split(b._disabledSplitOptions);
        b._disabledSplitOptions = null;
      }
    }
  }
  onInternalPaint() {
  }
  get isSplit() {
    let a;
    return Boolean((a = this.widgets) == null ? void 0 : a.length);
  }
  getClientConfig(a, b, c, e = {}) {
    const {client: f} = this, {
        subGrids: g,
        regions: j
      } = f, k = f.columns.records.slice(), l = ObjectHelper.assign({}, f.subGridConfigs);
    f.eachSubGrid(p => {
      const q = l[p.region];
      if (p.flex) {
        q.flex = p.flex;
      } else {
        q.width = p.element.style.width;
      }
    });
    if (c.atColumn && j.length > 1 && b > 0) {
      const p = j.indexOf(c.atColumn.region);
      for (let q = 0; q < p; q++) {
        const r = g[j[q]];
        ArrayHelper.remove(k, ...r.columns.records);
        delete l[j[q]];
      }
    }
    const n = ObjectHelper.assign({}, f.initialConfig, {
      'appendTo': a,
      'insertFirst': null,
      'insertBefore': null,
      'splitFrom': f,
      'owner': f.owner,
      'tbar': f.initialConfig.tbar && b === 1 ? {
        'height': f.tbar.height,
        'items': [' ']
      } : null,
      'store': f.store,
      'selectedRecordCollection': f.selectedRecordCollection,
      'subGridConfigs': l,
      'columns': this.cloneColumns(k),
      'minHeight': 0,
      'minWidth': 0
    }, e);
    for (const u of ObjectHelper.keys(this.relayProperties)) {
      n[u] = f[u];
    }
    const o = {};
    for (const v in f.listeners) {
      if (!ignoreListeners[v]) {
        const w = f.listeners[v].filter(z => !z.$internal);
        if (w.length) {
          o[v] = w;
        }
      }
    }
    n.listeners = o;
    if (c.direction === 'horizontal') {
      n.hideHeaders = true;
    } else if (c.direction === 'both' && b !== 1) {
      n.hideHeaders = true;
    }
    delete n.data;
    delete n.adopt;
    return n;
  }
  cloneColumns(a) {
    return a.flatMap(b => {
      if (b.meta.isSelectionColumn || b.field === 'expanderActionColumn') {
        return [];
      }
      const c = { ...b.data };
      if (b.children) {
        c.children = b.children.map(e => ({ ...e.data }));
      }
      delete c.headerRenderer;
      delete c.parentId;
      delete c.onBeforeWidgetSetValue;
      delete c.onAfterWidgetSetValue;
      return c;
    });
  }
  cloneClient(a, b, c, e) {
    const f = this.getClientConfig(a, b, c, e), g = new this.client.constructor(f);
    g.element.classList.add('b-split-clone');
    return g;
  }
  processOptions(a) {
    let b;
    const {client: c} = this, {
        atRecord: e,
        atColumn: f,
        direction: g
      } = a;
    if (!g) {
      if (e && f) {
        a.direction = 'both';
      } else if (f) {
        a.direction = 'vertical';
      } else {
        a.direction = 'horizontal';
      }
    } else {
      if (g !== 'vertical' && !e && c.store.count) {
        const k = c._bodyRectangle.height / 2 + c.scrollable.y, l = (b = c.rowManager.getRowAt(k, true)) != null ? b : c.rowManager.rows[Math.ceil(c.rowManager.rows.length / 2)];
        a.atRecord = c.store.getById(l.id);
      }
      if (g !== 'horizontal' && !f) {
        const n = Rectangle.from(c.element);
        let o = n.center.x - n.x, p = c.subGrids[c.regions[0]], q = 0, r = null;
        while (o > p.width) {
          o -= p.width;
          p = c.subGrids[c.regions[++q]];
        }
        o += p.scrollable.x;
        const {visibleColumns: u} = p.columns;
        let v = 0, w = 0;
        while (v < o && w < u.length) {
          r = u[w++];
          v += r.element.offsetWidth;
        }
        a.atColumn = r;
      }
    }
    return a;
  }
  createSplitContainer({direction: a}) {
    const {client: b} = this, {element: c} = b;
    return this.splitContainer = DomHelper.createElement({
      'parent': c.parentElement,
      'className': {
        'b-split-container': 1,
        ['b-split-' + a]: 1,
        'b-rtl': b.rtl
      },
      'style': {
        'width': c.style.width,
        'height': c.style.height
      },
      'children': [
        a !== 'both' && c,
        a === 'both' && {
          'className': 'b-split-top',
          'children': [c]
        },
        a === 'both' && { 'className': 'b-split-bottom' }
      ]
    });
  }
  syncHeaderHeights() {
    let a = 0;
    for (const b of this.subViews) {
      b.eachSubGrid(c => {
        if (c.header.height > a) {
          a = c.header.height;
        }
      });
    }
    for (const c of this.subViews) {
      c.eachSubGrid(e => {
        e.header.height = a;
      });
    }
  }
  toggleOriginalSubGrids(a) {
    const b = this, {client: c} = b, {regions: e} = c;
    if (a.atColumn && e.length > 1) {
      const f = e.indexOf(a.atColumn.region), g = [c];
      if (a.direction === 'both') {
        g.push(b.subViews[2]);
      }
      for (const j of g) {
        if (f + 1 < e.length) {
          const k = j === c;
          const l = j.subGrids[e[f]];
          l.hideSplitter();
          k && b.restorers.push(() => l.showSplitter());
          if (!l.flex) {
            c.inForEachOther = true;
            l.flex = 1;
            c.inForEachOther = false;
            k && b.restorers.push(() => {
              l.flex = null;
              l.width = l._initialWidth;
            });
          }
          for (let n = f + 1; n < e.length; n++) {
            const o = j.subGrids[e[n]];
            o.hide();
            k && b.restorers.push(() => {
              o.show();
            });
          }
          if (e.length === 2) {
            j._initialWidth = j.element.style.width;
            j._initialFlex = j.flex;
            j.width = l._initialWidth;
            k && b.restorers.push(() => {
              if (j._initialFlex !== null) {
                j.flex = j._initialFlex;
              } else if (j._initialWidth !== null) {
                j.width = j._initialWidth;
              }
            });
          }
        }
      }
    }
  }
  async split(a = {}) {
    const b = this, {client: c} = b;
    if (c.splitFrom) {
      return;
    }
    if (b.isSplit) {
      await b.unsplit(true);
    }
    const {rtl: e} = c, {
        atRecord: f,
        atColumn: g,
        direction: i
      } = b.processOptions(a);
    let {
        splitX: j,
        remainingWidth: k
      } = a, l = null, n = null;
    if (f) {
      await c.scrollRowIntoView(f);
      const w = c.getRowFor(f);
      if (!w) {
        throw new Error('Could not find row for record ' + f.id);
      }
      l = Rectangle.from(w.cells[0], c.element).bottom;
      n = Rectangle.from(c.element).height - l;
    }
    if (g && !j) {
      j = Rectangle.from(g.element, c.element).getEnd(e);
      k = Rectangle.from(c.element).width - j - DomHelper.scrollBarWidth;
      if (e) {
        const z = j;
        j = k + DomHelper.scrollBarWidth;
        k = z - DomHelper.scrollBarWidth;
      }
    }
    const o = [], p = b.createSplitContainer(a), {visibleColumns: q} = c.columns, r = g ? q[q.indexOf(g) + 1] : null, u = f ? c.store.getNext(f) : null;
    c.eachSubGrid(A => A._initialWidth = A.width);
    c.columns.commit();
    if (i !== 'both') {
      const A = {
        'flex': '0 0 ' + ((l != null ? n : k) - splitterWidth) + 'px',
        'height': null
      };
      const [, B] = b.widgets = [
        new Splitter({ 'appendTo': p }),
        b.cloneClient(p, i === 'vertical' ? 1 : 0, a, A)
      ];
      if (j != null) {
        c.renderRows();
        if (!a.atDate) {
          o.push(c.scrollColumnIntoView(g, endScrollOptions));
          r && o.push(B.scrollColumnIntoView(r, startScrollOptions));
        }
      }
      if (l != null) {
        o.push(B.scrollRowIntoView(u, startScrollOptions));
      }
      c.element.classList.add('b-split-start');
      B.element.classList.add('b-split-end');
      c.scrollable.addPartner(B.scrollable, {
        'x': i === 'horizontal',
        'y': i !== 'horizontal'
      });
    } else {
      const C = { 'flex': '0 0 ' + (k - splitterWidth) + 'px' };
      p.lastElementChild.style.flex = '0 0 ' + (n - splitterWidth) + 'px';
      b.widgets = [
        new Splitter({ 'insertBefore': p.lastElementChild }),
        b.topSplitter = new Splitter({ 'appendTo': p.firstElementChild }),
        b.cloneClient(p.firstElementChild, 1, a, C),
        b.cloneClient(p.lastElementChild, 0, a),
        b.bottomSplitter = new Splitter({ 'appendTo': p.lastElementChild }),
        b.cloneClient(p.lastElementChild, 2, a, C)
      ];
      const E = c, F = b.widgets[2], G = b.widgets[3], H = b.widgets[5];
      E.element.classList.add('b-split-top-start');
      F.element.classList.add('b-split-top-end');
      G.element.classList.add('b-split-bottom-start');
      H.element.classList.add('b-split-bottom-end');
      if (j != null) {
        E.renderRows();
        G.renderRows();
        if (g && !a.atDate) {
          o.push(c.scrollColumnIntoView(g, endScrollOptions));
          r && o.push(F.scrollColumnIntoView(r, startScrollOptions));
        }
      }
      if (l != null) {
        o.push(G.scrollRowIntoView(u, startScrollOptions), H.scrollRowIntoView(u, startScrollOptions));
      }
      E.scrollable.addPartner(F.scrollable, 'y');
      E.scrollable.addPartner(G.scrollable, 'x');
      F.scrollable.addPartner(H.scrollable, 'x');
      G.scrollable.addPartner(H.scrollable, 'y');
      b.topSplitter.ion({
        'splitterMouseDown': 'onSplitterMouseDown',
        'splitterMouseEnter': 'onSplitterMouseEnter',
        'splitterMouseLeave': 'onSplitterMouseLeave',
        'drag': 'onSplitterDrag',
        'drop': 'onSplitterDrop',
        'thisObj': b
      });
      b.bottomSplitter.ion({
        'splitterMouseDown': 'onSplitterMouseDown',
        'splitterMouseEnter': 'onSplitterMouseEnter',
        'splitterMouseLeave': 'onSplitterMouseLeave',
        'drag': 'onSplitterDrag',
        'drop': 'onSplitterDrop',
        'thisObj': b
      });
    }
    b.subViews = [
      c,
      ...b.widgets.filter(I => I.isGridBase)
    ];
    b.toggleOriginalSubGrids(a);
    b.syncHeaderHeights();
    b._splitOptions = a;
    await Promise.all(o);
    const v = Rectangle.from(c.element);
    c.onInternalResize(c.element, v.width, v.height);
    c.eachSubGrid(I => {
      const J = Rectangle.from(I.element);
      I.onInternalResize(I.element, J.width, J.height);
    });
    c.scrollable.x += 0.5;
    c.scrollable.y += 0.5;
    b.startSyncingColumns();
    c.trigger('split', {
      'subViews': b.subViews,
      'options': a
    });
    return b.subViews;
  }
  async unsplit(a = false) {
    let b;
    const c = this, {
        client: e,
        splitContainer: f
      } = c, {element: g} = e;
    if (c.isSplit) {
      c.stopSyncingColumns();
      (b = c.widgets) == null ? void 0 : b.forEach(i => i.destroy());
      c.widgets = null;
      e.eachSubGrid(i => i.scrollable.x);
      e.scrollable.y;
      f.parentElement.appendChild(g);
      f.remove();
      c.splitContainer = null;
      g.style.flexBasis = g.style.flexGrow = '';
      g.classList.remove('b-split-top-start', 'b-split-start');
      c.subViews.length = 0;
      if (!c.isDestroying) {
        e.renderRows();
        c.unsplitCleanup();
        for (const i of c.restorers) {
          i();
        }
        c.restorers.length = 0;
        await AsyncHelper.animationFrame();
        await AsyncHelper.animationFrame();
        if (c.isDestroyed) {
          return;
        }
        !a && e.trigger('unsplit');
        c._splitOptions = null;
      }
    }
  }
  unsplitCleanup() {
  }
  populateCellMenu({
    record: a,
    column: b,
    items: c
  }) {
    const e = this, {isSplit: f} = e, {splitFrom: g} = e.client;
    if (!e.disabled) {
      c.splitGrid = {
        'text': 'L{split}',
        'localeClass': e,
        'icon': 'b-icon b-icon-split-vertical',
        'weight': 500,
        'separator': true,
        'hidden': f || g,
        'menu': {
          'splitHorizontally': {
            'text': 'L{horizontally}',
            'icon': 'b-icon b-icon-split-horizontal',
            'localeClass': e,
            'weight': 100,
            'onItem'() {
              e.split({ 'atRecord': a });
            }
          },
          'splitVertically': {
            'text': 'L{vertically}',
            'icon': 'b-icon b-icon-split-vertical',
            'localeClass': e,
            'weight': 200,
            'onItem'() {
              e.split({ 'atColumn': b });
            }
          },
          'splitBoth': {
            'text': 'L{both}',
            'icon': 'b-icon b-icon-split-both',
            'localeClass': e,
            'weight': 300,
            'onItem'() {
              e.split({
                'atColumn': b,
                'atRecord': a
              });
            }
          }
        }
      };
      c.unsplitGrid = {
        'text': 'L{unsplit}',
        'localeClass': e,
        'icon': 'b-icon b-icon-clear',
        'hidden': !(f || g),
        'weight': 400,
        'separator': true,
        'onItem'() {
          (g || e).unsplit();
        }
      };
    }
  }
  startSyncingColumns() {
    this.detachListeners('columns');
    for (const a of this.subViews) {
      a.columns.ion({
        'name': 'columns',
        'change': 'onColumnsChange',
        'thisObj': this
      });
    }
  }
  stopSyncingColumns() {
    this.detachListeners('columns');
  }
  onColumnsChange({
    source: a,
    isMove: b,
    action: c,
    parent: e,
    records: f,
    changes: g
  }) {
    let i, j;
    const k = this;
    if (!__privateGet(k, _ignoreColumnChanges)) {
      __privateSet(k, _ignoreColumnChanges, true);
      for (const l of k.subViews) {
        const {columns: n} = l;
        if (a !== n) {
          if (c === 'update' && g.region && Object.keys(g).length === 1) {
            if (!n.getById(f[0].id)) {
              const [o] = f, p = (i = n.getById(k.$before.parent.id)) != null ? i : n.rootNode, q = k.$before.id !== null && n.getById(k.$before.id);
              p.insertChild(o.data, q);
            } else {
              n.remove(f[0].id);
            }
            k.$before = null;
          } else if (!(b == null ? void 0 : b[f[0].id]) && b !== true) {
            if (c === 'add') {
              const r = f.filter(u => l.getSubGridFromColumn(u));
              n.add(k.cloneColumns(r));
            } else {
              n.applyChangesFromStore(a);
            }
          } else if (c === 'add') {
            const u = f[0], v = u.nextSibling, w = n.getById(u.id);
            if (!w) {
              k.$before = {
                'id': v == null ? void 0 : v.id,
                'parent': e
              };
              __privateSet(k, _ignoreColumnChanges, false);
              continue;
            }
            if (u.meta.isSelectionColumn) {
              __privateSet(k, _ignoreColumnChanges, false);
              continue;
            }
            const z = (j = n.getById(e.id)) != null ? j : n.rootNode, A = v && n.getById(v.id);
            z.insertChild(w, A);
          }
          n.commit();
        }
      }
      a.commit();
      __privateSet(k, _ignoreColumnChanges, false);
    }
  }
  getOtherSplitter(a) {
    return a === this.topSplitter ? this.bottomSplitter : this.topSplitter;
  }
  onSplitterMouseDown({
    source: a,
    event: b
  }) {
    if (!b.handled) {
      b.handled = true;
      this.getOtherSplitter(a).onMouseDown(b);
    }
  }
  onSplitterMouseEnter({
    source: a,
    event: b
  }) {
    this.getOtherSplitter(a).element.classList.add('b-hover');
  }
  onSplitterMouseLeave({
    source: a,
    event: b
  }) {
    this.getOtherSplitter(a).element.classList.remove('b-hover');
  }
  onSplitterDrag({
    source: a,
    event: b
  }) {
    if (!b.handled) {
      b.handled = true;
      this.getOtherSplitter(a).onMouseMove(b);
    }
  }
  onSplitterDrop({
    source: a,
    event: b
  }) {
    if (!b.handled) {
      b.handled = true;
      this.getOtherSplitter(a).onMouseUp(b);
    }
  }
  afterConfigChange({
    name: a,
    value: b
  }) {
    if (this.isSplit && this.relayProperties[a]) {
      this.syncSplits(c => {
        c[a] = b;
      });
    }
  }
  afterAddListener(a, b) {
    if (this.isSplit && !b.$internal && !ignoreListeners[a]) {
      this.syncSplits(c => c.on(a, b));
    }
  }
  afterRemoveListener(a, b) {
    if (!b.$internal) {
      this.syncSplits(c => c.un(a, b));
    }
  }
  forEachOther(a) {
    let b;
    const c = this.client.splitFrom || this.client, e = StringHelper.uncapitalize(this.constructor.$name);
    if (((b = c.features[e]) == null ? void 0 : b.enabled) && !c.inForEachOther) {
      c.inForEachOther = true;
      for (const f of c.subViews) {
        if (f !== this.client) {
          a(f);
        }
      }
      c.inForEachOther = false;
    }
  }
  syncSplits(a) {
    this.forEachOther(a);
  }
};
_ignoreColumnChanges = new WeakMap();
Split._$name = 'Split';
GridFeatureManager.registerFeature(Split, false);
const LockRows = class extends Split {
  static configurable = {
    'fieldName': 'fixed',
    'filterFn': null
  };
  static $name = 'LockRows';
  static get pluginConfig() {
    const a = {};
    for (const b in super.pluginConfig) {
      a[b] = [...super.pluginConfig[b]];
    }
    a.chain.push('selectAll', 'deselectAll', 'afterRenderRow');
    if (!a.override) {
      a.override = [];
    }
    a.override.push('isAllRecordsSelected');
    return a;
  }
  construct() {
    super.construct(...arguments);
    if (this.enabled) {
      this.refreshSuspended = true;
      this.client.suspendRefresh();
    }
  }
  onInternalPaint({firstPaint: a}) {
    if (a) {
      if (this.refreshSuspended) {
        this.client.resumeRefresh();
      }
      this.lockRows();
    }
  }
  async lockRows(a = {
    'fieldName': this.fieldName,
    'filterFn': this.filterFn
  }) {
    let b;
    const c = this, {client: e} = c;
    if (e.splitFrom || c.disabled || c.$isLocking) {
      return;
    }
    c.$isLocking = true;
    if (c.isSplit) {
      await c.unsplit(true);
    }
    a = c.processOptions(a);
    const {
        store: f,
        scrollable: g
      } = e, {fieldName: i} = a, j = e.id + '-sticky-record-filter', k = ObjectHelper.copyProperties({}, e, [
        'flex',
        'minHeight',
        'maxHeight',
        'autoHeight',
        'hideHorizontalScrollbar'
      ]), l = c.createSplitContainer(a);
    let {filterFn: n} = a;
    if (k.maxHeight === 0) {
      k.maxHeight = null;
    }
    k.height = e._lastHeight || null;
    c.restorers.push(() => {
      e.setConfig(k);
      f.removeFilter(j);
      e.element.classList.remove('b-locked-rows');
      e.store = e.store.$master;
      e.isLockedRows = e.originalStore = null;
    });
    if (i && !f.modelClass.fieldMap[i]) {
      f.modelClass.addField({
        'name': i,
        'type': 'boolean'
      });
    }
    e.setConfig({
      'height': null,
      'flex': '0 0 auto',
      'minHeight': 0,
      'maxHeight': a.maxHeight || '50%',
      'autoHeight': true,
      'hideHorizontalScrollbar': true
    });
    e.element.classList.add('b-locked-rows');
    const o = i ? [i] : '*';
    n = n || (r => r[i]);
    const [p] = c.widgets = [c.cloneClient(l, 0, a, {
        'flex': '1 1 0%',
        'hideHeaders': true,
        'usesLockRows': true
      })];
    p.element.classList.add('b-locked-rows-clone');
    c.subViews = [
      p,
      e
    ];
    e.originalStore = f;
    e.isLockedRows = true;
    e.store = f.chain(r => n(r), o, { 'chainFilters': true });
    p.store = f.chain(r => !n(r), o, { 'chainFilters': true });
    if (DomHelper.scrollBarWidth && g.hasOverflowY) {
      g.overflowY = 'scroll';
    }
    !e.scrollable.isPartneredWith(p.scrollable) && e.scrollable.addPartner(p.scrollable, { 'x': true });
    p.columns.leaves.forEach(r => {
      if (r.isRowNumberColumn) {
        p.refreshColumn(r);
      }
    });
    c._splitOptions = a;
    const q = Rectangle.from(e.element);
    e.onInternalResize(e.element, q.width, q.height);
    e.eachSubGrid(r => {
      const u = Rectangle.from(r.element);
      r.onInternalResize(r.element, u.width, u.height);
    });
    if ((b = e.features.columnAutoWidth) == null ? void 0 : b.enabled) {
      e.features.columnAutoWidth.syncAutoWidthColumns();
    }
    e.scrollable.x += 0.5;
    e.scrollable.y += 0.5;
    c.startSyncingColumns();
    c.$isLocking = false;
    e.trigger('lockRows', { 'clone': p });
    return c.subViews;
  }
  processOptions(a) {
    const {fieldName: b} = a;
    a.direction = 'horizontal';
    if (!a.filterFn && !b) {
      throw new Error('LockRows needs `fieldName` or `filterFn`');
    }
    if (typeof a.filterFn === 'string') {
      a.filterFn = this.resolveCallback(a.filterFn);
    }
    return a;
  }
  get numberOfLockedRows() {
    return this.isSplit ? this.subViews[1].store.count : 0;
  }
  isAllRecordsSelected() {
    const a = this.isSplit && this.subViews[0];
    return this.overridden.isAllRecordsSelected() && (!a || a.isAllRecordsSelected());
  }
  selectAll(a) {
    if (this.isSplit) {
      this.subViews[0].selectAll(a);
    }
  }
  deselectAll(a, b) {
    if (this.isSplit) {
      this.subViews[0].deselectAll(a, b);
    }
  }
  afterRenderRow({
    row: a,
    recordIndex: b
  }) {
    if (this.isSplit) {
      a.toggleCls('b-last', b === this.client.store.count - 1);
    }
  }
  populateCellMenu({
    record: a,
    items: b
  }) {
    let c, e;
    const f = this, g = ((c = f.client.splitFrom) == null ? void 0 : c.features.lockRows) || f, i = (e = g._splitOptions) == null ? void 0 : e.fieldName;
    if (!f.disabled && i && !g.filterFn && !f.client.readOnly && !a.isSpecialRow) {
      b.toggleLocked = {
        'text': a[i] ? 'L{unlockRow}' : 'L{lockRow}',
        'localeClass': f,
        'icon': 'b-icon ' + (a[i] ? 'b-icon-unlock-row' : 'b-icon-lock-row'),
        'weight': 500,
        'separator': true,
        'closeParent': true,
        'onItem': () => {
          a[i] = !a[i];
        }
      };
    }
  }
  async unsplit(a = false) {
    const b = this, c = b.subViews[0];
    b.client.trigger('beforeUnlockRows', { 'clone': c });
    await super.unsplit(a);
    !b.isDestroyed && b.client.trigger('unlockRows', { 'clone': c });
  }
  doDisable(a) {
    if (!this.isConfiguring) {
      if (a) {
        this.unsplit();
      } else {
        this.lockRows();
      }
    }
  }
};
LockRows._$name = 'LockRows';
GridFeatureManager.registerFeature(LockRows, false);
const HeaderMenu = class extends ContextMenuBase {
  static get $name() {
    return 'HeaderMenu';
  }
  static get configurable() {
    return {
      'type': 'header',
      'items': null,
      'moveColumns': null
    };
  }
  static get defaultConfig() {
    return { 'processItems': null };
  }
  static get pluginConfig() {
    const a = super.pluginConfig;
    a.chain.push('populateHeaderMenu');
    return a;
  }
  shouldShowMenu(a) {
    const {column: b} = a;
    return b && b.enableHeaderContextMenu !== false && b !== this.client.timeAxisColumn;
  }
  getDataFromEvent(a) {
    return ObjectHelper.assign(super.getDataFromEvent(a), this.client.getHeaderDataFromEvent(a));
  }
  populateHeaderMenu({
    items: a,
    column: b
  }) {
    const c = this;
    if (b) {
      if (b.headerMenuItems) {
        ObjectHelper.merge(a, b.headerMenuItems);
      }
      if (b.isCollapsible) {
        const {collapsed: e} = b, f = e ? c.client.rtl ? 'left' : 'right' : c.client.rtl ? 'right' : 'left';
        a.toggleCollapse = {
          'weight': 215,
          'icon': 'b-fw-icon b-icon-collapse-' + f,
          'text': c.L(e ? 'L{expandColumn}' : 'L{collapseColumn}'),
          'onItem': () => b.collapsed = !e
        };
      }
      if (c.moveColumns) {
        const g = c.getColumnToMoveBefore(b), i = c.getColumnToMoveAfter(b);
        if (g) {
          a.movePrev = {
            'weight': 220,
            'icon': 'b-fw-icon b-icon-column-move-left',
            'text': c.L('L{moveBefore}', StringHelper.encodeHtml(g.text)),
            'onItem': () => {
              let j;
              const {parent: k} = b;
              if (g.parent.insertChild(b, g)) {
                b.region = g.region;
                if (!((j = k.children) == null ? void 0 : j.length)) {
                  k.remove();
                }
              }
            }
          };
        }
        if (i) {
          a.moveNext = {
            'weight': 230,
            'icon': 'b-fw-icon b-icon-column-move-right',
            'text': c.L('L{moveAfter}', StringHelper.encodeHtml(i.text)),
            'onItem': () => {
              let j;
              const {parent: k} = b;
              if (i.parent.insertChild(b, i.nextSibling)) {
                b.region = i.region;
                if (!((j = k.children) == null ? void 0 : j.length)) {
                  k.remove();
                }
              }
            }
          };
        }
      }
    }
    return a;
  }
  getColumnToMoveBefore(a) {
    const {
      previousSibling: b,
      parent: c
    } = a;
    if (b) {
      return b.children && !a.children ? b.children[b.children.length - 1] : b;
    }
    if (!c.isRoot) {
      return c;
    }
  }
  getColumnToMoveAfter(a) {
    const {
      nextSibling: b,
      parent: c
    } = a;
    if (b) {
      return b;
    }
    if (!c.isRoot) {
      return c;
    }
  }
};
HeaderMenu.featureClass = '';
HeaderMenu._$name = 'HeaderMenu';
GridFeatureManager.registerFeature(HeaderMenu, true);
const camelCase = {
  'mousedown': 'mouseDown',
  'mousemove': 'mouseMove',
  'mouseup': 'mouseUp',
  'touchdown': 'touchDown',
  'touchmove': 'touchMove',
  'touchup': 'touchUp',
  'pointerover': 'mouseOver',
  'mouseout': 'mouseOut',
  'dblclick': 'dblClick',
  'keydown': 'keyDown',
  'keypress': 'keyPress',
  'keyup': 'keyUp',
  'contextmenu': 'contextMenu'
};
const MergeCells = class extends InstancePlugin {
  static configurable = {
    'passthrough': true,
    'sortedOnly': true,
    'shouldMerge': null
  };
  static $name = 'MergeCells';
  mergedMap = {};
  mergedRanges = [];
  constructor() {
    super(...arguments);
  }
  static get pluginConfig() {
    return {
      'chain': [
        'beforeRenderCell',
        'afterRenderRow',
        'bindStore',
        'afterColumnsChange',
        'afterRemove',
        'afterToggleGroup',
        'afterToggleSubGrid',
        'handleEvent',
        'populateHeaderMenu',
        'afterSelectionChange'
      ],
      'before': ['onInternalResize'],
      'override': [
        'getColumnFromElement',
        'getRecordFromElement'
      ]
    };
  }
  afterConstruct() {
    const a = this, {client: b} = a;
    b.eachSubGrid(c => a.setupSubGrid(c));
    b.rowManager.ion({
      'renderDone': 'onRenderDone',
      'offsetRows': 'onOffsetRowsUpdated',
      'thisObj': a
    });
    a.bindStore(b.store);
  }
  setupSubGrid(a) {
    a.$mergedCellsElement = DomHelper.createElement({
      'parent': a.element,
      'className': { 'b-grid-merged-cells-container': 1 }
    });
    a.ion({
      'beforeInternalResize': 'refreshBounds',
      'thisObj': this
    });
  }
  doDisable(a) {
    if (!this.isConfiguring) {
      this.isDisabling = true;
      this.reset();
      this.isDisabling = false;
    }
    super.doDisable(a);
  }
  updatePassthrough(a) {
    this.client.element.classList.toggle('b-mergecells-passthrough', a);
  }
  updateSortedOnly(a) {
    if (!this.isConfiguring) {
      this.reset(true, a);
    }
  }
  updateShouldMerge(a) {
    if (!this.isConfiguring) {
      this.reset(true, a);
    }
  }
  bindStore(a) {
    this.detachListeners('store');
    a.ion({
      'name': 'store',
      'change': 'onStoreChange',
      'refresh': {
        'prio': 1,
        'fn': 'onStoreRefresh'
      },
      'thisObj': this
    });
  }
  onInternalResize() {
    this.refreshBounds();
  }
  beforeRenderCell(a) {
    const b = this, {
        column: c,
        record: e,
        cellElement: f
      } = a, g = b.client.subGrids[c.region];
    if (!b.disabled && c.mergeCells && (c.isSorted || !b.sortedOnly) && !e.isSpecialRow && !g.collapsed) {
      const i = b.getMergeRange(e, c);
      if (i == null ? void 0 : i.use) {
        f.classList.add('b-merged-cell');
        f.innerHTML = '';
        a.cellElement = i.cellElement;
      }
    }
  }
  afterRenderRow({
    row: a,
    oldId: b,
    oldHeight: c
  }) {
    if (b === a.id && c !== a.height) {
      this.heightChanged = true;
    }
  }
  afterColumnsChange({
    action: a,
    changes: b,
    column: c
  }) {
    if (this.disabled) {
      return;
    }
    if (a === 'update' && b.mergeCells && !c.mergeCells) {
      for (const e of this.mergedRanges.slice()) {
        if (e.column === c) {
          this.removeRange(e);
        }
      }
      this.syncDom();
      return;
    }
    this.refreshBounds();
  }
  afterRemove() {
    !this.disabled && this.reset();
  }
  afterToggleGroup() {
    !this.disabled && this.reset();
  }
  afterToggleSubGrid() {
    !this.disabled && this.reset();
  }
  afterSelectionChange() {
    if (!this.passthrough) {
      const {client: a} = this, b = a.selectedRecords.map(e => a.store.indexOf(e));
      let c = false;
      for (const e of this.mergedRanges) {
        let f = true;
        for (let g = e.fromIndex; g <= e.toIndex && f; g++) {
          f = b.includes(g);
        }
        if (e.isSelected !== f) {
          e.isSelected = f;
          c = true;
        }
      }
      c && this.syncDom();
    }
  }
  getRecordFromElement(a) {
    let b;
    if ((b = a.elementData) == null ? void 0 : b.range) {
      return this.client.store.getAt(a.elementData.range.fromIndex);
    }
    return this.overridden.getRecordFromElement(a);
  }
  getColumnFromElement(a) {
    let b;
    if ((b = a.elementData) == null ? void 0 : b.range) {
      return a.elementData.range.column;
    }
    return this.overridden.getColumnFromElement(a);
  }
  populateHeaderMenu({
    column: a,
    items: b
  }) {
    if (a.mergeable !== false) {
      b.mergeCells = {
        'text': 'L{MergeCells.mergeCells}',
        'icon': 'b-fw-icon ' + (a.mergeCells ? 'b-icon-checked' : 'b-icon-unchecked'),
        'tooltip': 'L{MergeCells.menuTooltip}',
        'cls': 'b-separator',
        'weight': 500,
        'disabled': this.disabled,
        'onItem': () => a.mergeCells = !a.mergeCells
      };
    }
  }
  async handleEvent(a) {
    let b;
    if (!this.passthrough) {
      const c = a.target.closest('.b-grid-merged-cells');
      if (c) {
        const {client: e} = this, {range: f} = c.elementData, {cellEdit: g} = e.features, {column: j} = f, k = StringHelper.capitalize((b = camelCase[a.type]) != null ? b : a.type), l = {
            'grid': e,
            'records': [],
            'column': j,
            'cellElement': c.firstElementChild,
            'target': a.target,
            'event': a
          };
        for (let n = f.fromIndex; n <= f.toIndex; n++) {
          l.records.push(e.store.getAt(n));
        }
        e.trigger('mergedCell' + k, l);
        if ((g == null ? void 0 : g.triggerEvent.toLowerCase()) === ('cell' + k).toLowerCase()) {
          const o = e.rowManager.getRowAt(a.clientY);
          await g.startEditing({
            'id': o.id,
            'columnId': j.id
          });
        } else if (a.type === 'click') {
          this.onRangeClick({ 'range': f });
        }
      }
    }
  }
  onRangeClick({range: a}) {
    const b = [];
    for (let c = a.fromIndex; c <= a.toIndex; c++) {
      b.push(this.client.store.getAt(c));
    }
    this.client.selectedRecords = b;
  }
  onStoreChange() {
    !this.disabled && this.reset();
  }
  onStoreRefresh({
    source: a,
    action: b
  }) {
    if (b !== 'filter' && b !== 'dataset' && !this.disabled) {
      this.reset(Boolean(a.isResourceStore));
    }
  }
  createRangeElement(a) {
    let b, c, e;
    const f = this.client.getSubGridFromColumn(a.column).$mergedCellsElement, g = a.fromIndex + '-@-' + a.column.id;
    let i = (e = (b = f.syncIdMap) == null ? void 0 : b[g]) != null ? e : (c = f.releasedIdMap) == null ? void 0 : c[g];
    if (f.releasedIdMap) {
      delete f.releasedIdMap[g];
    }
    if (!i) {
      i = DomHelper.createElement(this.createRangeDomConfig(a));
    }
    DomSync.addChild(f, i, g);
    a.element = i;
    a.cellElement = i.firstElementChild;
    return i;
  }
  createRangeDomConfig(a) {
    let b;
    const {
        column: c,
        fromIndex: e,
        toIndex: f,
        top: g,
        left: i,
        right: j,
        width: k,
        height: l
      } = a, {type: n} = c, o = this.client.store.getAt(e), p = {
        'className': {
          'b-grid-merged-cells': 1,
          'b-selected': a.isSelected
        },
        'elementData': { 'range': a },
        'style': {
          'top': g,
          'left': i,
          'right': j,
          'height': l,
          'width': k
        },
        'dataset': {
          'syncId': e + '-@-' + c.id,
          'fromIndex': e,
          'toIndex': f,
          'column': c.field,
          'columnId': c.id
        },
        'children': [{
            'className': new DomClassList(this.client.cellCls).assign({
              ['b-' + (n == null ? void 0 : n.toLowerCase()) + '-cell']: n,
              [c.cellCls]: c.cellCls,
              [c.internalCellCls]: c.internalCellCls
            }).assign(c.autoCls),
            'keepChildren': true,
            'elementData': { 'range': a },
            'dataset': {
              'column': c.field,
              'columnId': c.id
            },
            'style': { 'height': this.client.rowManager.rowHeight }
          }]
      };
    (b = c.mergedRenderer) == null ? void 0 : b.call(c, {
      'domConfig': p,
      'value': c.getRawValue(o),
      'record': o,
      'column': c,
      'fromIndex': e,
      'toIndex': f
    });
    return p;
  }
  buildMergedCellsConfig(a, b, c) {
    let e, f;
    const g = this, {client: i} = g, {rowManager: j} = i;
    a = a != null ? a : (e = j.topRow) == null ? void 0 : e.dataIndex;
    b = b != null ? b : (f = j.bottomRow) == null ? void 0 : f.dataIndex;
    const k = i.regions.reduce((l, n) => {
      l[n] = {
        'className': { 'b-grid-merged-cells-container': 1 },
        'children': []
      };
      return l;
    }, {});
    for (const l of g.mergedRanges) {
      if (l.use && l.fromIndex <= b && l.toIndex >= a) {
        g.updateRange(l, g.heightChanged, c);
        k[l.column.region].children.push(g.createRangeDomConfig(l));
      }
    }
    return k;
  }
  syncDom(a = false) {
    const b = this, {client: c} = b, {rowManager: e} = c;
    if (!a && !e.topRow || b.disabled && !b.isDisabling) {
      return;
    }
    const f = this.buildMergedCellsConfig();
    c.eachSubGrid(g => {
      DomSync.sync({
        'targetElement': g.$mergedCellsElement,
        'syncIdField': 'syncId',
        'domConfig': f[g.region]
      });
    });
    b.heightChanged = false;
  }
  onRenderDone() {
    this.syncDom();
  }
  onOffsetRowsUpdated() {
    this.reset(true);
  }
  removeRange(a) {
    ArrayHelper.remove(this.mergedRanges, a);
    for (let b = a.fromIndex; b <= a.toIndex; b++) {
      delete this.mergedMap[b + '-@-' + a.column.id];
    }
  }
  reset(a = true, b = false) {
    const c = this, e = c.client.columns.visibleColumns.filter(g => g.mergeCells && (g.isSorted || !c.sortedOnly || b), true), f = c.mergedRanges.length;
    c.mergedRanges = [];
    c.mergedMap = {};
    if (a && !c.client.refreshSuspended) {
      for (const g of c.client.rowManager.rows) {
        for (const i of e) {
          g.renderCell(g.getCell(i.id));
        }
      }
      c.syncDom(f && !c.client.rowManager.rowCount);
    }
  }
  refreshBounds() {
    let a;
    const {mergedRanges: b} = this, c = this.client.rowManager.topRow, e = new Map(), f = this.client.rtl;
    for (const g of b.slice()) {
      const {column: i} = g, j = c.getCell(i.id);
      if (j) {
        let {
          rowWidth: k,
          bounds: l
        } = (a = e.get(i)) != null ? a : {};
        if (!l) {
          k = j.parentElement.offsetWidth;
          l = Rectangle.from(j, j.parentElement);
          e.set(i, {
            'rowWidth': k,
            'bounds': l
          });
        }
        if (f) {
          g.right = k - l.right;
        } else {
          g.left = l.left;
        }
        g.width = l.width;
      } else {
        this.removeRange(g);
      }
    }
    this.syncDom();
  }
  updateRange(a, b, c = null) {
    const {
        store: e,
        rowManager: f
      } = this.client, {
        topRendered: g,
        bottomRendered: i
      } = a, {rowOffsetHeight: j} = f;
    let {
        fromIndex: k,
        toIndex: l
      } = a, n, o;
    b = c ? c.length > 0 : b;
    if (!b && g && i) {
      return;
    }
    if (!g || b) {
      if (c) {
        const q = c.findIndex(r => r.dataIndex >= k);
        n = c[q];
        k = n.dataIndex;
      } else {
        do {
          n = f.getRowById(e.getAt(k));
        } while (!n && k++ < l);
      }
      a.topRendered = k === a.fromIndex;
      const p = k - a.fromIndex;
      a.top = n.top - p * (c ? n.offsetHeight : j);
    }
    if (!i || b) {
      if (c) {
        const u = c.findIndex(v => v.dataIndex === l);
        o = c[u === -1 ? c.length - 1 : u];
        k = o.dataIndex;
      } else {
        do {
          o = f.getRowById(e.getAt(l));
        } while (!o && l-- > k);
      }
      a.bottomRendered = l === a.toIndex;
      const r = a.toIndex - l;
      a.bottom = o.bottom + r * (c ? o.offsetHeight : j);
    }
    a.height = a.bottom - a.top;
  }
  getMergeRange(a, b) {
    if (a.isSpecialRow) {
      return;
    }
    const c = this, {mergedMap: e} = c, {store: f} = c.client, g = c.client.rowManager.topRow, j = b.id, k = f.indexOf(a), l = k + '-@-' + j, n = this.client.rtl, o = b.getRawValue(a);
    let p = e[l];
    if (!p) {
      p = e[l] = { 'column': b };
      c.mergedRanges.push(p);
      let q, r, u = k - 1;
      do {
        q = f.getAt(u);
        r = q && b.getRawValue(q);
        if (e[u + '-@-' + j]) {
          q = null;
        }
        u--;
      } while (q && !q.isSpecialRow && r === o);
      let v, w, z = k + 1;
      do {
        v = f.getAt(z);
        w = v && b.getRawValue(v);
        if (e[z + '-@-' + j]) {
          v = null;
        }
        z++;
      } while (v && !v.isSpecialRow && w === o);
      p.fromIndex = u + 2;
      p.toIndex = z - 2;
      if (c.shouldMerge && p.toIndex - p.fromIndex > 0) {
        for (let A = p.fromIndex + 1; A <= p.toIndex; A++) {
          const B = f.getAt(A), C = f.getAt(A - 1);
          if (c.shouldMerge({
              'column': b,
              'record': B,
              'previousRecord': C,
              'value': o
            }) === false) {
            p.toIndex = A - 1;
            break;
          }
        }
      }
      if (p.toIndex - p.fromIndex > 0) {
        for (let H = p.fromIndex; H <= p.toIndex; H++) {
          e[H + '-@-' + j] = p;
        }
        const E = g.getCell(j), F = E.parentElement.offsetWidth, G = Rectangle.from(E, E.parentElement);
        if (n) {
          p.right = F - G.right;
        } else {
          p.left = G.left;
        }
        p.width = G.width;
        p.use = true;
      }
    }
    if ((p == null ? void 0 : p.use) && !p.element) {
      c.createRangeElement(p);
    }
    return p;
  }
};
MergeCells._$name = 'MergeCells';
GridFeatureManager.registerFeature(MergeCells);
const actions2 = {
  'goToNextHit': 1,
  'goToPrevHit': 1,
  'showFilterEditor': 1,
  'clearSearch': 1
};
const QuickFind = class extends InstancePlugin {
  static configurable = {
    'mode': 'header',
    'find': '',
    'keyMap': {
      'F3': 'gotoNextHit',
      'Shift+F3': 'gotoPrevHit',
      'Ctrl+g': 'gotoNextHit',
      'Ctrl+Shift+g': 'gotoPrevHit',
      'Ctrl+Shift+f': 'showFilterEditor',
      'Escape': 'clearSearch',
      'Backspace': 'onBackspace'
    }
  };
  static $name = 'QuickFind';
  static get pluginConfig() {
    return {
      'chain': [
        'onElementKeyPress',
        'onCellNavigate'
      ]
    };
  }
  static get properties() {
    return {
      'hitCls': 'b-quick-hit',
      'hitCellCls': 'b-quick-hit-cell',
      'hitCellBadgeCls': 'b-quick-hit-cell-badge',
      'hitTextCls': 'b-quick-hit-text'
    };
  }
  construct(a, b) {
    super.construct(a, b);
    Object.assign(this, {
      'grid': a,
      'treeWalker': a.setupTreeWalker(a.element, DomHelper.NodeFilter.SHOW_TEXT, () => DomHelper.NodeFilter.FILTER_ACCEPT)
    });
  }
  isActionAvailable({actionName: a}) {
    if (actions2[a]) {
      const {focusedCell: b} = this.grid;
      return !this.disabled && (b == null ? void 0 : b.record) && !b.isActionable && this.find.length > 0;
    }
  }
  doDisable(a) {
    if (a) {
      this.clear();
    }
    super.doDisable(a);
  }
  get store() {
    return this.grid.store;
  }
  showQuickFind() {
    const a = this, {grid: b} = a, c = b.getHeaderElement(a.columnId);
    if (c) {
      if (!a.headerField) {
        const [e, f, g] = DomHelper.createElement({
          'tag': 'div',
          'className': 'b-quick-hit-header',
          'children': [
            {
              'tag': 'div',
              'className': 'b-quick-hit-field'
            },
            {
              'tag': 'div',
              'className': 'b-quick-hit-badge'
            }
          ]
        }, { 'returnAll': true });
        if (a.mode === 'header') {
          c.appendChild(e);
        } else {
          e.className += ' b-quick-hit-mode-grid';
          b.element.appendChild(e);
        }
        a.headerField = {
          'header': e,
          'field': f,
          'badge': g,
          'colHeader': c
        };
      }
      a.headerField.field.innerHTML = a.find;
      a.headerField.badge.innerHTML = a.found.length;
      c.classList.add('b-quick-find-header');
    }
    if ((c || b.hideHeaders) && !a.renderListenerInitialized) {
      b.rowManager.ion({
        'rendercell': a.renderCell,
        'thisObj': a
      });
      a.renderListenerInitialized = true;
    }
  }
  hideQuickFind() {
    const a = this, {
        grid: b,
        headerField: c
      } = a;
    for (const e of a.prevFound || a.found) {
      const f = b.getRowById(e.id);
      if (f) {
        f.forceInnerHTML = true;
        const g = f.getCell(a.columnId);
        g._content = null;
        f.renderCell(g);
        f.forceInnerHTML = false;
      }
    }
    if (c) {
      c.header.parentNode.removeChild(c.header);
      c.colHeader.classList.remove('b-quick-find-header');
      a.headerField = null;
    }
    if (a.renderListenerInitialized) {
      b.rowManager.un({ 'rendercell': a.renderCell }, a);
      a.renderListenerInitialized = false;
    }
    b.trigger('hideQuickFind');
  }
  search(a, b = this.columnId, c = false) {
    let e;
    const f = this, {grid: g} = f, j = g.columns.getById(b) || g.columns.get(b), k = f.store.findByField(j.field, a, j.mergeCells && j.isSorted);
    let l = 1;
    Object.assign(f, {
      'foundMap': {},
      'prevFound': f.found,
      'found': k,
      'find': a,
      'columnId': j.id,
      'findRe': new RegExp('(\\s+)?(' + StringHelper.escapeRegExp(String(a)) + ')(\\s+)?', 'ig')
    });
    if (a) {
      f.showQuickFind();
    } else {
      f.hideQuickFind();
    }
    for (const n of DomHelper.children(g.element, '.' + f.hitCls)) {
      n.classList.remove(f.hitCls, f.hitCellCls);
      if (n._originalContent) {
        n.innerHTML = n._originalContent;
        n._originalContent = null;
      }
    }
    if (!k) {
      return;
    }
    if (k.length > 0 && !c) {
      f.gotoClosestHit(g.focusedCell, k);
    }
    for (const o of k) {
      f.foundMap[o.id] = l++;
      const p = g.getRowById(o.data.id);
      p == null ? void 0 : p.renderCell(p.getCell(j.id));
      if (l > 1000) {
        break;
      }
    }
    (e = g.syncSplits) == null ? void 0 : e.call(g, q => q.features.quickFind.search(a, b, true));
    g.trigger('quickFind', {
      'find': a,
      'found': k
    });
  }
  clear() {
    let a;
    if (this.find || ((a = this.found) == null ? void 0 : a.length)) {
      this.search('');
    }
  }
  get foundCount() {
    let a, b;
    return (b = (a = this.found) == null ? void 0 : a.length) != null ? b : 0;
  }
  gotoHit(a) {
    const b = this.found[a];
    if (b) {
      this.grid.focusCell({
        'columnId': this.columnId,
        'id': b.id
      }, { 'doSelect': true });
    }
    return !!b;
  }
  gotoClosestHit(a, b) {
    const c = a ? this.grid.store.indexOf(a.id) : 0, e = b.slice().sort((f, g) => Math.abs(f.index - c) - Math.abs(g.index - c));
    this.gotoHit(b.indexOf(e[0]));
  }
  gotoFirstHit() {
    this.gotoHit(0);
  }
  gotoLastHit() {
    this.gotoHit(this.found.length - 1);
  }
  gotoNextHit() {
    let a, b, c, e;
    const f = this;
    if (!((a = f.found) == null ? void 0 : a.length)) {
      return;
    }
    const {grid: g} = f, i = (e = (b = g._focusedCell) == null ? void 0 : b.id) != null ? e : (c = g.lastFocusedCell) == null ? void 0 : c.id, j = g.store.indexOf(i) || 0, k = f.found.find(l => l.index > j);
    if (k) {
      g.focusCell({
        'columnId': f.columnId,
        'id': k.id
      }, { 'doSelect': true });
    } else {
      f.gotoFirstHit();
    }
  }
  gotoPrevHit() {
    let a, b, c, e;
    const f = this;
    if (!((a = f.found) == null ? void 0 : a.length)) {
      return;
    }
    const {
        grid: g,
        found: j
      } = f, k = (e = (b = g._focusedCell) == null ? void 0 : b.id) != null ? e : (c = g.lastFocusedCell) == null ? void 0 : c.id, l = g.store.indexOf(k) || 0;
    let n;
    if (!j.length) {
      return;
    }
    for (let o = j.length - 1; o--; o >= 0) {
      if (j[o].index < l) {
        n = j[o];
        break;
      }
    }
    if (n) {
      g.focusCell({
        'columnId': f.columnId,
        'id': n.id
      }, { 'doSelect': true });
    } else {
      f.gotoLastHit();
    }
  }
  renderCell({
    cellElement: a,
    column: b,
    record: c
  }) {
    let e;
    const f = this, {classList: g} = a, {
        treeWalker: i,
        findRe: j,
        hitTextCls: k
      } = f, l = f.columnId === b.id && ((e = f.foundMap) == null ? void 0 : e[c.id]);
    if (l) {
      g.add(f.hitCls);
      a.isQuickHit = true;
      a._originalContent = a.innerHTML;
      const n = i.currentNode = DomHelper.down(a, '.b-grid-cell-value,.b-tree-cell-value') || a;
      for (let o = i.nextNode(); o && n.contains(o);) {
        const p = o, q = o.nodeValue, r = ['<span>'];
        o = i.nextNode();
        let u = j.lastIndex;
        for (let w = j.exec(q); w; w = j.exec(q)) {
          const z = q.substring(u, w.index), A = w[1] ? '\xA0' : '', B = w[2], C = w[3] ? '\xA0' : '';
          r.push('' + StringHelper.encodeHtml(z) + A + '<span class="' + k + '">' + B + '</span>' + C);
          u = j.lastIndex;
        }
        r.push(StringHelper.encodeHtml(q.substring(u)), '</span>');
        p.parentNode.insertBefore(DomHelper.createElementFromTemplate(r.join(''), { 'fragment': true }), p);
        p.remove();
      }
      DomHelper.createElement({
        'parent': a,
        'className': f.hitCellBadgeCls,
        'text': l
      });
    }
  }
  onBackspace(a) {
    const b = this;
    if (b.find) {
      b.find = b.find.substr(0, b.find.length - 1);
      b.search(b.find);
      return true;
    }
    return false;
  }
  clearSearch() {
    if (this.find) {
      this.find = '';
      this.search(this.find);
      return true;
    }
    return false;
  }
  showFilterEditor() {
    const a = this, {filter: b} = a.client.features;
    if (b && a.columnId && a.foundCount) {
      a.clear();
      b.showFilterEditor(a.client.columns.getById(a.columnId), a.find);
    }
  }
  onElementKeyPress(a) {
    let b;
    const c = this, {grid: e} = c, {focusedCell: f} = e;
    if (!a.handled && !c.disabled && (f == null ? void 0 : f.record) && !f.isActionable && ((b = a.key) == null ? void 0 : b.length) === 1) {
      const g = e.columns.getById(e._focusedCell.columnId);
      if (g && g.searchable !== false) {
        c.columnId = e._focusedCell.columnId;
        c.find += a.key;
        c.search(c.find);
      }
    }
  }
  onCellNavigate(a, b, c) {
    const e = this;
    if (e.find && (!c || c.columnId !== e.columnId)) {
      e.clear();
    }
  }
};
QuickFind._$name = 'QuickFind';
GridFeatureManager.registerFeature(QuickFind);
const RegionResize = class extends InstancePlugin {
  static configurable = {
    'showSplitterButtons': true,
    'animateCollapseExpand': true,
    'enableDragging': true
  };
  static $name = 'RegionResize';
  static get pluginConfig() {
    return {
      'chain': [
        'onElementPointerDown',
        'onElementDblClick',
        'onElementTouchMove',
        'onSubGridCollapse',
        'onSubGridExpand',
        'render'
      ]
    };
  }
  updateShowSplitterButtons(a) {
    this.client.element.classList.toggle('b-hide-splitter-buttons', !a);
  }
  onElementDblClick(a) {
    const b = this, {client: c} = b, e = a.target.closest('.b-grid-splitter-collapsed');
    if (e && !b.expanding) {
      b.expanding = true;
      let f = e.dataset.region, g = c.getSubGrid(f);
      if (!g.collapsed) {
        f = c.getLastRegions()[1];
        g = c.getSubGrid(f);
      }
      g.expand().then(() => b.expanding = false);
    }
  }
  startMove(a, b) {
    const c = this, {clientX: e} = b, {client: f} = c, g = a.dataset.region, i = f.element, j = f.regions[f.regions.indexOf(g) + 1], k = f.getSubGrid(j), l = f.getSubGrid(g);
    if (a.classList.contains('b-grid-splitter-collapsed')) {
      return;
    }
    let n = l, o = 1;
    if (n.flex != null) {
      if (k.flex == null) {
        n = k;
        o = -1;
      }
    }
    if (f.rtl) {
      o *= -1;
    }
    const p = Object.values(f.subGrids).reduce((q, r) => {
      return r.width + q;
    }, 0);
    f.trigger('splitterDragStart', {
      'subGrid': n,
      'domEvent': b
    });
    c.dragContext = {
      'element': a,
      'headerEl': n.header.element,
      'subGridEl': n.element,
      'subGrid': n,
      'splitterSubGrid': l,
      'originalWidth': n.element.offsetWidth,
      'originalX': e,
      'minWidth': n.minWidth || 0,
      'maxWidth': Math.min(p, n.maxWidth || p),
      'flip': o
    };
    i.classList.add('b-moving-splitter');
    l.toggleSplitterCls('b-moving');
    c.pointerDetacher = EventHelper.on({
      'element': document,
      'pointermove': 'onPointerMove',
      'pointerup': 'onPointerUp',
      'thisObj': c
    });
  }
  endMove(a) {
    const b = this, {
        dragContext: c,
        client: e
      } = b;
    if (c) {
      const {subGrid: f} = c;
      a.preventDefault();
      b.pointerDetacher();
      e.element.classList.remove('b-moving-splitter');
      c.splitterSubGrid.toggleSplitterCls('b-moving', false);
      b.dragContext = null;
      e.trigger('splitterDragEnd', {
        'subGrid': f,
        'domEvent': a
      });
    }
  }
  onCollapseClick(a, b, c) {
    const e = this, {client: f} = e, g = b.dataset.region, i = f.getLastRegions();
    if (f.trigger('splitterCollapseClick', {
        'subGrid': a,
        'domEvent': c
      }) === false) {
      return;
    }
    if (i[0] === g) {
      const j = f.getSubGrid(i[1]);
      if (j.collapsed) {
        j.expand(e.animateCollapseExpand);
        return;
      }
    }
    a.collapse(e.animateCollapseExpand);
  }
  onExpandClick(a, b, c) {
    const e = this, {client: f} = e, g = b.dataset.region, i = f.getLastRegions();
    if (f.trigger('splitterExpandClick', {
        'subGrid': a,
        'domEvent': c
      }) === false) {
      return;
    }
    if (i[0] === g) {
      if (!a.collapsed) {
        const j = f.getSubGrid(i[1]);
        j.collapse(e.animateCollapseExpand);
        return;
      }
    }
    a.expand(e.animateCollapseExpand);
  }
  updateMove(a) {
    const {dragContext: b} = this;
    if (b) {
      const c = a - b.originalX, e = Math.max(Math.min(b.maxWidth, b.originalWidth + c * b.flip), 0);
      b.subGrid.width = Math.max(e, b.minWidth);
    }
  }
  onElementPointerDown(a) {
    let b;
    const c = this, {target: e} = a, f = a.button === 0 && e.closest(':not(.b-row-reordering):not(.b-dragging-event):not(.b-dragging-task):not(.b-dragging-header):not(.b-dragselecting) .b-grid-splitter'), g = f && c.client.getSubGrid(f.dataset.region);
    let i;
    if (f) {
      if (e.closest('.b-grid-splitter-button-collapse')) {
        c.onCollapseClick(g, f, a);
      } else if (e.closest('.b-grid-splitter-button-expand')) {
        c.onExpandClick(g, f, a);
      } else if (c.enableDragging) {
        c.startMove(f, a);
        i = f;
      }
    }
    if (a.pointerType === 'touch' || ((b = c.touchedSplitter) == null ? void 0 : b.classList.contains('b-touching'))) {
      c.toggleTouchSplitter(i);
    }
  }
  onPointerMove(a) {
    if (this.dragContext) {
      this.updateMove(a.clientX);
      a.preventDefault();
    }
  }
  onElementTouchMove(a) {
    if (this.dragContext) {
      a.preventDefault();
    }
  }
  onPointerUp(a) {
    if (this.enableDragging) {
      this.endMove(a);
    }
  }
  onSubGridCollapse({subGrid: a}) {
    const b = this.client.resolveSplitter(a), c = this.client.getLastRegions();
    if (c[1] === a.region) {
      b.classList.add('b-grid-splitter-allow-collapse');
    }
  }
  onSubGridExpand({subGrid: a}) {
    const b = this.client.resolveSplitter(a);
    b.classList.remove('b-grid-splitter-allow-collapse');
  }
  toggleTouchSplitter(a) {
    const b = this, {touchedSplitter: c} = b;
    if (a && c && a.dataset.region !== c.dataset.region) {
      b.toggleTouchSplitter();
    }
    const e = b.client.getSubGrid(a ? a.dataset.region : c == null ? void 0 : c.dataset.region);
    if (e) {
      e.toggleSplitterCls('b-touching', Boolean(a));
      if (a) {
        e.startSplitterButtonSyncing();
      } else {
        e.stopSplitterButtonSyncing();
      }
    }
    b.touchedSplitter = a;
  }
  render() {
    const {
      regions: a,
      subGrids: b
    } = this.client;
    if (a.length === 2) {
      const c = b[a[0]];
      if (c.maxWidth && c.width >= c.maxWidth) {
        c.splitterElement.classList.add('b-left-only');
      }
    } else if (a.length > 2) {
      b[a[0]].splitterElement.classList.add('b-left-only');
      b[a[1]].splitterElement.classList.add('b-right-only');
    }
  }
  updateEnableDragging(a) {
    this.client.element.classList.toggle('b-grid-splitter-no-drag', !a);
  }
};
RegionResize.featureClass = 'b-split';
RegionResize._$name = 'RegionResize';
GridFeatureManager.registerFeature(RegionResize);
const actions3 = {
  'cut': 1,
  'copy': 1,
  'paste': 1
};
const isKeyboardEvent = a => a instanceof KeyboardEvent || (a == null ? void 0 : a.type) === 'keydown' && (a == null ? void 0 : a.target);
const RowCopyPaste = class extends CopyPasteBase {
  static configurable = {
    'nameField': 'name',
    'keyMap': {
      'Ctrl+C': {
        'weight': 10,
        'handler': 'copy'
      },
      'Ctrl+X': {
        'weight': 10,
        'handler': 'cut'
      },
      'Ctrl+V': {
        'weight': 10,
        'handler': 'paste'
      }
    },
    'copyRecordText': 'L{copyRecord}',
    'cutRecordText': 'L{cutRecord}',
    'pasteRecordText': 'L{pasteRecord}',
    'rowSpecifierText': 'L{row}',
    'rowSpecifierTextPlural': 'L{rows}',
    'localizableProperties': [
      'copyRecordText',
      'cutRecordText',
      'pasteRecordText',
      'rowSpecifierText',
      'rowSpecifierTextPlural'
    ],
    'rowOptionsOnCellContextMenu': false,
    'cutOnly': null,
    'allowedDataSources': ['RowCopyPaste']
  };
  static type = 'rowCopyPaste';
  static $name = 'RowCopyPaste';
  entityName = 'row';
  constructor() {
    super(...arguments);
  }
  static get pluginConfig() {
    return {
      'assign': [
        'copyRows',
        'pasteRows'
      ],
      'chain': ['populateCellMenu']
    };
  }
  construct(a, b) {
    super.construct(a, b);
    a.rowManager.ion({
      'beforeRenderRow': 'onBeforeRenderRow',
      'thisObj': this
    });
    this.grid = a;
  }
  onBeforeRenderRow({
    row: a,
    record: b
  }) {
    let c;
    a.cls['b-cut-row'] = this.isCut && ((c = this.cutData) == null ? void 0 : c.includes(b));
  }
  isActionAvailable({
    actionName: a,
    event: b
  }) {
    let c;
    const {grid: e} = this, {cellEdit: f} = e.features, {target: g} = b;
    if (actions3[a]) {
      if ((a === 'cut' || a === 'paste') && e.isLockedRows) {
        return false;
      }
      return !this.disabled && globalThis.getSelection().toString().length === 0 && (!f || !f.isEditing) && (a === 'copy' || !this.copyOnly) && ((c = e.selectedRecords) == null ? void 0 : c.length) > 0 && (!g || Boolean(g.closest('.b-gridbase:not(.b-schedulerbase) .b-grid-subgrid,.b-grid-subgrid:not(.b-timeaxissubgrid)')));
    }
  }
  async copy(a) {
    await this.copyRows(false, a);
  }
  async cut(a) {
    await this.copyRows(true, a);
  }
  paste(a) {
    if (isKeyboardEvent(a)) {
      a = this.client.getRecordFromElement(a.target);
    }
    return this.pasteRows((a == null ? void 0 : a.isModel) ? a : null);
  }
  async copyRows(a = false, b) {
    let c, e;
    const {
      client: f,
      entityName: g
    } = this;
    if (this.cutOnly) {
      a = true;
    }
    let i = this.selectedRecords.filter(k => !k.readOnly || !a);
    if (b) {
      let k = b;
      if (isKeyboardEvent(b)) {
        k = (e = (c = f.resolveRowRecord) == null ? void 0 : c.call(f, b.target)) != null ? e : f.getRecordFromElement(b.target);
      }
      if (!f.isSelected(k)) {
        i = [k];
      }
    }
    const j = await this.writeToClipboard(i, a);
    if (j === void 0) {
      return;
    }
    f.trigger('copy', {
      'records': j.data,
      'originalRecords': i,
      'isCut': a,
      'entityName': g
    });
  }
  setIsCut(a, b) {
    let c;
    (c = this.grid.rowManager.getRowById(a)) == null ? void 0 : c.toggleCls('b-cut-row', b);
    a.meta.isCut = b;
  }
  handleCutData({source: a}) {
    let b;
    if (a !== this && ((b = this.cutData) == null ? void 0 : b.length)) {
      this.grid.store.remove(this.cutData);
    }
  }
  stringConverter(a) {
    const {rowManager: b} = this.grid, c = a.flatMap(e => {
        let f;
        return (f = b.getRowById(e)) == null ? void 0 : f.cells.map(g => new GridLocation(g));
      });
    return this.cellsToString(c.filter(e => e));
  }
  cloneData({
    data: a,
    isPaste: b
  }) {
    if ((a == null ? void 0 : a.length) && !this.cutOnly) {
      this.sortByIndex(a);
      a = this.extractParents([...a], {}, b);
    }
    return a;
  }
  async beforeCopy({
    data: a,
    isCut: b
  }) {
    return await this.client.trigger('beforeCopy', {
      'records': a,
      'isCut': b,
      'entityName': this.entityName
    });
  }
  async pasteRows(a) {
    let b, c, e;
    const f = this, {
        client: g,
        entityName: i
      } = f, j = a || g.selectedRecord, {tree: k} = g.store;
    if (g.readOnly || g.isTreeGrouped) {
      return [];
    }
    const l = await f.readFromClipboard({ 'referenceRecord': j }, true), n = l == null ? void 0 : l.isCut;
    let o = l == null ? void 0 : l.data;
    if (!l || !Array.isArray(o) || !(o == null ? void 0 : o.length) || k && n && o.some(p => p.contains(j, true))) {
      return [];
    }
    if (n && k) {
      o = f.extractParents(o, {});
    }
    await f.insertCopiedRecords(o, j);
    if (g.isDestroying) {
      return;
    }
    if (n) {
      await f.clearClipboard();
    } else {
      g.selectedRecords = o;
    }
    g.trigger('paste', {
      'records': o,
      'originalRecords': l.originalData,
      'referenceRecord': j,
      'isCut': n,
      'entityName': i
    });
    f.clipboard.triggerPaste(f);
    (e = (c = (b = g.getRowFor(o[o.length - 1])) == null ? void 0 : b.cells) == null ? void 0 : c[0]) == null ? void 0 : e.focus();
    return o;
  }
  async beforePaste({
    referenceRecord: a,
    data: b,
    originalData: c,
    text: e,
    isCut: f
  }) {
    const g = b !== e ? b : [];
    return await this.client.trigger('beforePaste', {
      'records': g,
      'referenceRecord': a,
      'isCut': f,
      'entityName': this.entityName,
      'data': b,
      'originalRecords': c
    });
  }
  stringParser(a) {
    return this.setFromStringData(a, true).modifiedRecords;
  }
  insertCopiedRecords(a, b) {
    const c = this.client.store.$master, e = c.indexOf(b) + 1;
    if (c.isTree) {
      return b.parent.insertChild(a, b.nextSibling, false, { 'orderedBeforeNode': b.nextOrderedSibling });
    } else {
      return c.insert(e, a);
    }
  }
  get selectedRecords() {
    const a = [...this.client.selectedRecords];
    this.client.selectedCells.forEach(b => {
      if (!a.includes(b.record)) {
        a.push(b.record);
      }
    });
    return a;
  }
  getMenuItemText(a, b = false) {
    const c = this;
    let e = c[a + 'RecordText'];
    if (b) {
      e += ' (' + (c.selectedRecords.length > 1 ? c.rowSpecifierTextPlural : c.rowSpecifierText) + ')';
    }
    return e;
  }
  populateCellMenu({
    record: a,
    items: b,
    cellSelector: c
  }) {
    let e;
    const f = this, {
        client: g,
        rowOptionsOnCellContextMenu: i
      } = f, j = ((e = g.features.cellCopyPaste) == null ? void 0 : e.enabled) === true, k = j && g.isCellSelected(c);
    if (!g.isTreeGrouped && (a == null ? void 0 : a.isSpecialRow) === false && (j ? g.selectedRows.length : g.selectedRecords.length) && (!k || f.rowOptionsOnCellContextMenu)) {
      if (!f.copyOnly) {
        b.cut = {
          'text': f.getMenuItemText('cut', k && i),
          'localeClass': f,
          'icon': 'b-icon b-icon-cut',
          'weight': 135,
          'disabled': a.readOnly || g.isLockedRows || g.readOnly,
          'onItem': () => f.cut(a)
        };
        b.paste = {
          'text': f.getMenuItemText('paste', k && i),
          'localeClass': f,
          'icon': 'b-icon b-icon-paste',
          'weight': 140,
          'onItem': () => f.paste(a),
          'disabled': g.readOnly || f.hasClipboardData() === false || g.isLockedRows
        };
      }
      if (!f.cutOnly) {
        b.copy = {
          'text': f.getMenuItemText('copy', k && i),
          'localeClass': f,
          'cls': 'b-separator',
          'icon': 'b-icon b-icon-copy',
          'weight': 120,
          'onItem': () => f.copy(a)
        };
      }
    }
  }
  sortByIndex(a) {
    const {store: b} = this.client;
    return a.sort((c, e) => {
      const f = c.indexPath, g = e.indexPath;
      if (!a.includes(c.parent) && !a.includes(e.parent)) {
        return b.indexOf(c) - b.indexOf(e);
      }
      if (f.length === g.length) {
        for (let j = 0; j < f.length; j++) {
          if (f[j] < g[j]) {
            return -1;
          }
          if (f[j] > g[j]) {
            return 1;
          }
        }
        return 0;
      } else {
        return f.length - g.length;
      }
    });
  }
  extractParents(a, b, c = true) {
    const e = this, {store: f} = e.client;
    if (f.tree) {
      a.forEach(i => {
        i.traverse(j => {
          const k = j.getTopParent(true);
          if (!a.includes(j) && (!e.isCut || !a.some(l => k.includes(l)))) {
            a.push(j);
          }
        });
      });
    }
    const g = a.reduce((i, j) => {
      let k;
      const l = j.parentId || j.meta.modified;
      if (e.isCut) {
        k = j;
        k.meta.isCut = false;
      } else {
        k = j.copy();
        if (c) {
          k[e.nameField] = e.generateNewName(k, j);
        }
        k.data.expanded = j.isExpanded(e.client.store);
      }
      b[j.id] = k;
      if (j.parent === f.rootNode) {
        i.push(k);
      } else if (l in b) {
        b[l].appendChild(k, true);
      } else {
        i.push(k);
      }
      return i;
    }, []);
    g.forEach(i => {
      i.sortOrderedChildren(true, true);
    });
    return g;
  }
};
RowCopyPaste.featureClass = 'b-row-copypaste';
RowCopyPaste._$name = 'RowCopyPaste';
GridFeatureManager.registerFeature(RowCopyPaste, true, 'Grid');
GridFeatureManager.registerFeature(RowCopyPaste, false, 'Gantt');
GridFeatureManager.registerFeature(RowCopyPaste, false, 'SchedulerPro');
GridFeatureManager.registerFeature(RowCopyPaste, false, 'ResourceHistogram');
const actions4 = {
  'onTab': 1,
  'onShiftTab': 1
};
const storeRemoveActions = {
  'remove': 1,
  'filter': 1,
  'dataset': 1,
  'replace': 1
};
const RowExpander = class extends InstancePlugin.mixin(Delayable_default) {
  static deprecatedEvents = {
    'beforeExpand': {
      'product': 'grid',
      'invalidAsOfVersion': '7.0.0',
      'message': 'RowExpander `beforeExpand` was replaced by the `beforeRowExpand` event on the Grid itself'
    },
    'expand': {
      'product': 'grid',
      'invalidAsOfVersion': '7.0.0',
      'message': 'RowExpander `expand` was replaced by the `rowExpand` event on the Grid itself'
    },
    'beforeCollapse': {
      'product': 'grid',
      'invalidAsOfVersion': '7.0.0',
      'message': 'RowExpander `beforeCollapse` was replaced by the `beforeRowCollapse` event on the Grid itself'
    },
    'collapse': {
      'product': 'grid',
      'invalidAsOfVersion': '7.0.0',
      'message': 'RowExpander `collapse` was replaced by the `rowCollapse` event on the Grid itself'
    }
  };
  static configurable = {
    'renderer': null,
    'triggerEvent': null,
    'column': {},
    'columnPosition': 'first',
    'refreshOnRecordChange': false,
    'loadingIndicatorHeight': 100,
    'loadingIndicatorText': 'L{loading}',
    'enableAnimations': true,
    'widget': null,
    'dataField': null,
    'keyMap': {
      'Tab': {
        'handler': 'onTab',
        'weight': 50
      },
      'Shift+Tab': {
        'handler': 'onShiftTab',
        'weight': 50
      }
    },
    'autoScroll': false,
    'spanRegions': false,
    'callOnFunctions': true
  };
  static $name = 'RowExpander';
  static get properties() {
    return {
      'expanderBodyClass': 'b-rowexpander-body',
      'expandedRowClass': 'b-rowexpander-row-expanded',
      'shadowRootContainerClass': 'b-rowexpander-shadowroot-container',
      'recordStateMap': new Map(),
      'collapsingStateMap': new Map()
    };
  }
  static get pluginConfig() {
    return {
      'chain': [
        'afterColumnsChange',
        'beforeRenderRow',
        'processRowHeight',
        'bindStore',
        'navigateUp'
      ],
      'override': [
        'onGridBodyFocusIn',
        'navigateDown',
        'catchFocus',
        'keyMapOnKeyDown'
      ],
      'assign': [
        'expandRow',
        'collapseRow'
      ]
    };
  }
  afterConstruct() {
    const a = this, {client: b} = a;
    if (!a.renderer && !a.widget) {
      a.disabled = true;
      console.warn('RowExpander requires either a widget config or implementing the renderer function.');
      return;
    }
    if (b.isGanttBase && b.fixedRowHeight !== false) {
      console.warn('When using RowExpander on a Gantt, the Gantt`s fixedRowHeight config must be set to `false`.');
    }
    if (a.widget) {
      GlobalEvents_default.ion({
        'theme': a.onThemeChange,
        'thisObj': a
      });
    }
    a.bindStore(b.store);
    if (a.triggerEvent) {
      b.ion({
        [a.triggerEvent]: 'onTriggerEvent',
        'thisObj': a
      });
    }
    a.addColumn();
  }
  bindStore(a) {
    const b = this;
    b.recordStateMap.clear();
    b.collapsingStateMap.clear();
    b.detachListeners('clientStoreChange');
    a.ion({
      'name': 'clientStoreChange',
      'change': b.onStoreChange,
      'thisObj': b
    });
  }
  doDisable(a) {
    const {client: b} = this;
    if (a) {
      this.recordStateMap.clear();
      this.collapsingStateMap.clear();
    }
    if (!b.isConfiguring) {
      b.rowManager.renderFromRow();
    }
    super.doDisable(a);
  }
  changeLoadingIndicatorText(a) {
    return a ? this.L(a) : a;
  }
  onGridBodyFocusIn(a) {
    let b, c;
    const e = this;
    if (e.hasWidget ? !a.target.matches('.' + e.expanderBodyClass + ', .' + e.shadowRootContainerClass) : !((c = (b = e.client.lastMousedownEvent) == null ? void 0 : b.target) == null ? void 0 : c.closest('.' + e.expanderBodyClass))) {
      e.overridden.onGridBodyFocusIn(a);
    }
  }
  keyMapOnKeyDown({target: a}) {
    let b;
    if (!this.hasWidget || !((b = a.classList) == null ? void 0 : b.contains(this.shadowRootContainerClass))) {
      this.overridden.keyMapOnKeyDown(...arguments);
    }
  }
  get isAnimating() {
    return this.client.isAnimating;
  }
  set isAnimating(a) {
    const {client: b} = this, c = b.isAnimating;
    b.isAnimating = a;
    if (b.isAnimating !== c) {
      b.element.classList.toggle('b-rowexpander-animating');
    }
  }
  changeWidget(a) {
    if (a && this.isWidgetConfig(a)) {
      return { [this.client.regions[0]]: a };
    }
    return a;
  }
  afterColumnsChange() {
    this.addColumn();
  }
  changeColumn(a) {
    const b = this, {client: c} = b;
    if (a == null) {
      return a;
    }
    let e = b.columnPosition === 'first' ? 'left' : 'right';
    if (c.isRTL) {
      e = b.columnPosition === 'first' ? 'right' : 'left';
    }
    return ObjectHelper.assign({
      'type': 'action',
      'draggable': false,
      'resizable': false,
      'showColumnPicker': false,
      'actions': [{
          'cls': 'b-icon b-icon-collapse-' + e,
          'tooltip': ({record: f}) => b.L(b.recordStateMap.has(f) ? 'L{RowExpander.collapse}' : 'L{RowExpander.expand}'),
          'onClick': ({record: f}) => b.toggleExpand(f)
        }],
      'width': 40,
      'hideable': false,
      'align': 'center',
      'region': c.regions[0],
      'field': 'expanderActionColumn'
    }, a);
  }
  addColumn() {
    const a = this, {column: b} = a, {columns: c} = a.client;
    if (!a._isAddingExpanderColumn && b && (!a._expander || !c.includes(a._expander))) {
      a._isAddingExpanderColumn = true;
      if (a.columnPosition === 'last') {
        [a._expander] = c.add(b);
        a.client.fillLastColumn = false;
      } else {
        [a._expander] = c.insert(0, b);
      }
      a._isAddingExpanderColumn = false;
    }
  }
  onTriggerEvent({target: a}) {
    if (this.disabled || (a == null ? void 0 : a.closest('.b-action-cell')) || !a.closest('.b-grid-cell')) {
      return;
    }
    this.toggleExpand(this.client.getRecordFromElement(a));
  }
  toggleExpand(a) {
    if (a) {
      if (this.recordStateMap.has(a)) {
        this.collapse(a);
      } else {
        this.expand(a);
      }
    }
  }
  onExpanderBodyResize(a) {
    let b;
    for (const c of a) {
      const e = this.client.store.getById((b = c.target.dataset) == null ? void 0 : b.id), f = e && this.recordStateMap.get(e);
      if (f && !f.ignoreResize && !f.isAnimating) {
        const g = f.expanderBodyHeight;
        f.expanderBodyHeight = null;
        if (this.processRowHeight(e, 0) !== g) {
          this.renderRowsWithAnimation(e);
        }
      }
    }
  }
  get shouldSpanRegions() {
    return this.spanRegions && this.client.regions.length > 1;
  }
  onStoreChange({
    action: a,
    source: b,
    records: c,
    changes: e
  }) {
    let f;
    const g = this, {
        recordStateMap: i,
        collapsingStateMap: j
      } = g, k = e && Object.keys(e);
    if ((k == null ? void 0 : k.length) === 1 && ((f = b.modelClass.fieldMap[k[0]]) == null ? void 0 : f.type) === 'store') {
      return;
    }
    if (g.disabled) {
      return;
    }
    if (a === 'removeAll') {
      i.clear();
      j.clear();
    } else if (storeRemoveActions[a]) {
      for (const [l, n] of i) {
        if (!b.includes(l)) {
          g.destroyWidgets(n);
          i.delete(l);
          j.delete(l);
        }
      }
    } else if (g.refreshOnRecordChange && (c == null ? void 0 : c.length)) {
      if (a === 'update') {
        const o = i.get(c[0]);
        if (o == null ? void 0 : o.isCreated) {
          o.isCreated = false;
          g.client.rowManager.renderFromRecord(c[0]);
        }
      }
    }
  }
  processRowHeight(a, b) {
    let c, e;
    const f = this, {shouldSpanRegions: g} = f;
    let i = f.recordStateMap.get(a), j = false;
    if (i && !i.expanderBodyHeight) {
      for (const k of f.client.regions) {
        const l = (c = i.expandedBodyElements[k]) == null ? void 0 : c.offsetHeight;
        if (l > i.expanderBodyHeight) {
          i.expanderBodyHeight = l;
          j = true;
        }
      }
    } else if (!i && (i = f.collapsingStateMap.get(a)) && i.expanderBodyHeight) {
      i.expanderBodyHeight = 0;
      j = true;
    }
    if (j && f.enableAnimations && g) {
      f.animateSpannedExpander(i);
    }
    return ((e = i == null ? void 0 : i.expanderBodyHeight) != null ? e : 0) + b;
  }
  animateSpannedExpander(a) {
    const b = this, c = a.expandedBodyElements[b.client.regions[0]], e = b.client.rowManager.getRowById(a.record).element;
    a.isAnimating = b.isAnimating = true;
    a.ignoreResize = true;
    c.style.height = e.offsetHeight - a.cellHeight + 'px';
    c.offsetHeight;
    c.style.height = a.expanderBodyHeight + 'px';
    b.waitForTransition(c, () => {
      c.style.height = '';
      if (!a.expanderBodyHeight) {
        c.remove();
      }
      a.isAnimating = b.isAnimating = false;
    });
  }
  beforeRenderRow({
    row: a,
    record: b
  }) {
    const c = this, {regions: e} = c.client, {
        expandedRowClass: f,
        collapsingStateMap: g,
        shouldSpanRegions: i
      } = c, j = c.recordStateMap.get(b);
    a.cls.toggle('b-rowexpander-disabled', c.disabled);
    if (a.cls[f]) {
      let k = g.get(b);
      if (k) {
        const l = () => {
          k = g.get(b);
          if (k) {
            g.delete(b);
            c.removeExpander(a);
            c.destroyWidgets(k);
          }
        };
        if (c.enableAnimations && c.isAnimating) {
          c.waitForTransition(a, l);
        } else {
          l();
        }
      } else if (!j) {
        c.removeExpander(a);
      }
    } else {
      g.delete(b);
    }
    if (!c.disabled && j) {
      if (!j.isCreated) {
        j.ignoreResize = true;
        c.renderExpander(b, a, j);
      }
      a.cls.add(f);
      e.forEach((n, o) => {
        const p = a.getElement(n);
        if (j.isCreated && (!i || o === 0)) {
          const q = j.expandedBodyElements[n], r = i ? c.client.verticalScroller : p;
          if (q.parentElement !== r) {
            if (!i) {
              DomHelper.removeEachSelector(p, '.' + c.expanderBodyClass);
            }
            r.appendChild(q);
            c.observeResize(q);
          }
          if (i) {
            q.style.top = a.top + j.cellHeight + 'px';
          }
          j.ignoreResize = false;
          if (j.renderPromiseResolver && !j.isRenderingAsync) {
            c.delay(j.renderPromiseResolver);
            j.renderPromiseResolver = null;
          }
        }
        c.lockCellHeight(p, j.cellHeight, false);
      });
      if (c._shouldScrollIntoView && c.autoScroll) {
        c._shouldScrollIntoView = false;
        if (!DomHelper.isInView(j.expandedBodyElements[e[0]], true)) {
          c.client.rowManager.ion({
            'once': true,
            'thisObj': c,
            'renderDone': () => c.scrollRowIntoView(a, b)
          });
        }
      }
    }
  }
  scrollRowIntoView(a, b) {
    if (this.isAnimating) {
      this.waitForTransition(a, () => this.client.scrollRowIntoView(b));
    } else {
      this.client.scrollRowIntoView(b);
    }
  }
  waitForTransition(a, b) {
    let c;
    const e = DomHelper.isElement(a) ? a : a.element;
    EventHelper.onTransitionEnd({
      'element': e,
      'property': 'height',
      'handler': b,
      'thisObj': this,
      'duration': (c = DomHelper.getPropertyTransitionDuration(e, 'height')) != null ? c : 1
    });
  }
  removeExpander(a) {
    let b, c;
    const e = this;
    a.cls.remove(e.expandedRowClass);
    for (const f of e.client.regions) {
      const g = a.getElement(f);
      for (const i of g.querySelectorAll('.' + e.expanderBodyClass)) {
        e.unobserveResize(i);
        i.remove();
      }
      g.classList.remove(e.expandedRowClass);
      e.lockCellHeight(g, null, false);
      if (e.shouldSpanRegions) {
        (c = (b = a._spannedExpandedBodyElement) == null ? void 0 : b.remove) == null ? void 0 : c.call(b);
        a._spannedExpandedBodyElement = null;
      }
    }
  }
  destroyWidgets(a) {
    let b, c, e;
    for (const f of this.client.regions) {
      (e = (c = (b = a.widgets) == null ? void 0 : b[f]) == null ? void 0 : c.destroy) == null ? void 0 : e.call(c);
    }
  }
  isWidgetConfig(a) {
    return a.type && !a.tag;
  }
  renderExpander(a, b, c) {
    let e, f;
    const g = this, {
        client: j,
        widget: k,
        shouldSpanRegions: l
      } = g, n = (e = b.cells[0]) == null ? void 0 : e.offsetHeight, {
        expandedBodyElements: expandedBodyElements = {}
      } = c, o = [], p = (r, u, v) => {
        if (r != null) {
          if (typeof r === 'string') {
            const w = [...u.childNodes].map(z => u.removeChild(z));
            u.innerHTML = r;
            w.reverse().forEach(z => u.insertBefore(z, u.firstChild));
          } else if (g.isWidgetConfig(r)) {
            q(r, u, v);
          } else {
            r = DomHelper.createElement(r);
            u.appendChild(r);
          }
        }
      }, q = (r, u, v) => {
        const w = DomHelper.createElement({
            'parent': u,
            'className': g.shadowRootContainerClass,
            'style': 'flex : 1'
          }), z = w._shadowRoot = w.attachShadow({ 'mode': 'closed' });
        o.push(DomHelper.cloneStylesIntoShadowRoot(z).then(() => {
          let A, B;
          const C = (A = DomHelper.getThemeInfo()) == null ? void 0 : A.name;
          if (j.isDestroyed) {
            return;
          }
          const E = (B = r.dataField) != null ? B : g.dataField;
          if (E) {
            const F = a.getValue(E);
            if (F == null ? void 0 : F.isStore) {
              r.store = F;
            } else if (j.store[E + 'Store']) {
              const G = j.store[E + 'Store'];
              r.store = G.chain(H => a.getValue(E).includes(H));
            } else {
              r.store = {
                'data': F,
                'internalListeners': {
                  'change': () => !b.isDestroyed && b.render(),
                  'thisObj': g
                }
              };
            }
          }
          if (C) {
            const {cls: H} = r, I = 'b-theme-' + C.toLowerCase();
            r.cls = H ? H + ' ' + I : I;
          }
          if (!c.widgets) {
            c.widgets = {};
          }
          c.widgets[v] = u.widget = Widget.create(ObjectHelper.assign({
            'appendTo': z,
            'owner': j,
            'flex': 1,
            'minHeight': '5em',
            'isNested': true,
            'expandedRecord': a
          }, r));
        }).catch(A => {
          throw new Error('Could not load stylesheet ' + A);
        }));
      };
    if (c.isRenderingAsync) {
      return;
    }
    b.addCls(g.expandedRowClass);
    Object.assign(c, {
      'cellHeight': n,
      'expandedBodyElements': expandedBodyElements,
      'expanderBodyHeight': 0,
      'loadingIndicators': []
    });
    for (const [r, u] of j.regions.entries()) {
      if (l && r > 0) {
        break;
      }
      const v = b.getElement(u);
      let w = expandedBodyElements[u], z;
      if (w) {
        g.unobserveResize(w);
        w.innerHTML = '';
      } else {
        w = DomHelper.createElement({
          'parent': l ? j.verticalScroller : v,
          'tabIndex': -1,
          'className': g.expanderBodyClass,
          'style': { 'top': l ? b.top + n : n + 'px' },
          'dataset': { 'id': a.id }
        });
      }
      if (l) {
        b._spannedExpandedBodyElement = w;
      }
      g.observeResize(w);
      if (k == null ? void 0 : k[u]) {
        q(k[u], w, u);
      } else {
        z = (f = g.renderer) == null ? void 0 : f.call(g, {
          'record': a,
          'expanderElement': w,
          'rowElement': v,
          'region': u,
          'grid': j
        });
      }
      if (Objects.isPromise(z)) {
        o.push(z.then(A => p(A, w, u)));
      } else {
        p(z, w, u);
      }
      expandedBodyElements[u] = w;
    }
    if (o.length) {
      c.isRenderingAsync = true;
      g.showLoadingIndicators(c);
      Promise.all(o).then(() => {
        Promise.all(o).then(() => {
          let A, B;
          if (j.isDestroyed) {
            return;
          }
          (A = c.loadingIndicators) == null ? void 0 : A.forEach(C => C.remove());
          c.loadingIndicators.length = 0;
          c.ignoreResize = false;
          c.isRenderingAsync = false;
          for (const [, C] of g.recordStateMap) {
            if (C.isRenderingAsync) {
              return;
            }
          }
          (B = g.renderRowsWithAnimation) == null ? void 0 : B.call(g, a, true);
        });
      });
    }
    c.isCreated = true;
  }
  showLoadingIndicators(a) {
    for (const b in a.expandedBodyElements) {
      a.loadingIndicators.push(DomHelper.createElement({
        'parent': a.expandedBodyElements[b],
        'className': 'b-rowexpander-loading',
        'style': { 'height': this.loadingIndicatorHeight + 'px' },
        'children': [
          {
            'tag': 'i',
            'className': 'b-icon b-icon-spinner'
          },
          this.loadingIndicatorText
        ]
      }));
    }
  }
  renderRowsWithAnimation(a) {
    const b = this;
    if (b.enableAnimations) {
      const c = b.client.rowManager.getRowById(a);
      if (c) {
        b.isAnimating = true;
        if (b.collapsingStateMap.has(a)) {
          c.addCls('b-row-is-collapsing');
        }
        b.waitForTransition(c, () => {
          let e;
          b.isAnimating = false;
          if (!c.isDestroyed) {
            (e = c.removeCls) == null ? void 0 : e.call(c, 'b-row-is-collapsing');
          }
        });
      }
    }
    return b.bufferedRenderer(a);
  }
  bufferedRenderer(a) {
    let b;
    ((b = this._bufferedRecords) != null ? b : this._bufferedRecords = []).push(a);
    if (!this._rafPromise) {
      this._rafPromise = new Promise(c => {
        requestAnimationFrame(() => {
          let e;
          (e = this.internalRender) == null ? void 0 : e.call(this, c);
          this._rafPromise = null;
        });
      });
    }
    return this._rafPromise;
  }
  internalRender(a) {
    const b = this, {_bufferedRecords: c} = b, {store: e} = b.client;
    b.recordStateMap.forEach((g, i) => {
      if (g.renderPromiseResolver && g.isCreated && !c.includes(i)) {
        c.push(i);
      }
    });
    const [f] = c.sort((g, i) => e.indexOf(g) - e.indexOf(i));
    b.client.rowManager.renderFromRecord(f);
    c.length = 0;
    b.delay(a);
  }
  lockCellHeight(b, c, e) {
    for (let f = 0; f < b.children.length; f++) {
      const g = b.children[f];
      if (!g.classList.contains(this.expanderBodyClass)) {
        g.style.height = e ? '' : c + 'px';
      }
    }
  }
  async expandRow(...a) {
    return this.expand(...a);
  }
  async collapseRow(...a) {
    return this.collapse(...a);
  }
  async expand(a, b = false) {
    const c = this, {client: e} = c;
    if (c.disabled || c.recordStateMap.has(a) || await c.trigger('beforeExpand', { 'record': a }) === false || await e.trigger('beforeRowExpand', { 'record': a }) === false) {
      return;
    }
    let f;
    return new Promise(g => {
      let i;
      f = {
        'isCreated': false,
        'renderPromiseResolver': g,
        'record': a
      };
      c.recordStateMap.set(a, f);
      c.collapsingStateMap.delete(a);
      c._shouldScrollIntoView = true;
      c.renderRowsWithAnimation(a);
      if (!b) {
        (i = e.syncSplits) == null ? void 0 : i.call(e, j => j.features.rowExpander.expand(a, true));
      }
    }).then(() => {
      let g, i, j, k;
      (i = c.trigger) == null ? void 0 : i.call(c, 'expand', {
        'record': a,
        'expandedElements': f.expandedBodyElements,
        'widget': (g = f.widgets) == null ? void 0 : g[c.client.regions[0]],
        'widgets': f.widgets
      });
      (k = e.trigger) == null ? void 0 : k.call(e, 'rowExpand', {
        'record': a,
        'expandedElements': f.expandedBodyElements,
        'widget': (j = f.widgets) == null ? void 0 : j[c.client.regions[0]],
        'widgets': f.widgets
      });
    });
  }
  async collapse(a, b = false) {
    let c;
    const e = this, {client: f} = e, g = e.recordStateMap.get(a);
    if (e.disabled || await e.trigger('beforeCollapse', { 'record': a }) === false || await f.trigger('beforeRowCollapse', { 'record': a }) === false) {
      return;
    }
    if (g == null ? void 0 : g.expandedBodyElements) {
      for (const i in g.expandedBodyElements) {
        e.unobserveResize(g.expandedBodyElements[i]);
      }
    }
    e.recordStateMap.delete(a);
    e.collapsingStateMap.set(a, g);
    await e.renderRowsWithAnimation(a);
    e.trigger('collapse', { 'record': a });
    f.trigger('rowCollapse', { 'record': a });
    if (!b) {
      (c = f.syncSplits) == null ? void 0 : c.call(f, j => j.features.rowExpander.collapse(a, true));
    }
  }
  get hasWidget() {
    return Boolean(this.widget) || [...this.recordStateMap.values()].some(a => a.widgets);
  }
  navigateDown() {
    if (!this.onKeyboardIn()) {
      return this.overridden.navigateDown(...arguments);
    }
  }
  navigateUp() {
    this.onKeyboardIn(true);
  }
  catchFocus({
    navigationDirection: a,
    source: b,
    editing: c
  }) {
    let e;
    const {client: f} = this, {
        focusedCell: g,
        regions: i
      } = f;
    for (let [j, k] of this.recordStateMap.entries()) {
      for (const l in k.expandedBodyElements) {
        if (k.expandedBodyElements[l].widget === b) {
          if (c) {
            const p = i[i.indexOf(l) + (a === 'down' ? 1 : -1)];
            if (p) {
              return this.startEditInWidget((e = k.widgets) == null ? void 0 : e[p], a === 'down');
            }
          }
          if (a === 'down') {
            j = f.store.getNext(j, void 0, true);
          }
          let n;
          if (!c && g && !g._isDefaultFocus) {
            n = g.column;
          } else {
            n = this.getNavigateableColumn(f, true, c && a === 'up');
          }
          f._focusedCell = null;
          const o = f.normalizeCellContext({
            'record': j,
            'column': n
          });
          f.focusCell(o);
          if (c) {
            f.startEditing(o);
          }
          return true;
        }
      }
    }
  }
  onKeyboardIn(a) {
    let b, c, e;
    const {focusedCell: f} = this.client, g = f && this.recordStateMap.get(f.record), i = (e = (c = g == null ? void 0 : g.expandedBodyElements) == null ? void 0 : c[(b = f == null ? void 0 : f.column) == null ? void 0 : b.region]) == null ? void 0 : e.widget;
    if (i == null ? void 0 : i.isGrid) {
      let j;
      if (i.focusedCell && !i.focusedCell._isDefaultFocus) {
        j = i.focusedCell.column;
      } else {
        j = this.getNavigateableColumn(i);
      }
      i._focusedCell = null;
      i.focusCell(i.normalizeCellContext({
        'record': i[(a ? 'last' : 'first') + 'VisibleRow'],
        'column': j
      }));
      return true;
    }
    return false;
  }
  getNavigateableColumn(a, b = true, c = false) {
    let e;
    const f = c ? [...a.columns.visibleColumns].reverse() : a.columns.visibleColumns;
    return (e = f.find(g => {
      let i;
      return g !== a.checkboxSelectionColumn && g !== ((i = a.features.rowExpander) == null ? void 0 : i._expander) && (!b || g.editor);
    })) != null ? e : f[0];
  }
  isActionAvailable({actionName: a}) {
    let b;
    if (actions4[a]) {
      return !this.isDisabled && ((b = this.client.features.cellEdit) == null ? void 0 : b.isEditing);
    }
  }
  onTab(a) {
    let b;
    const {client: c} = this, {cellEdit: e} = c.features;
    if (e == null ? void 0 : e.enabled) {
      const {
          columns: f,
          regions: g
        } = c, {activeRecord: i} = e, j = a !== true, k = e.getAdjacentEditableCell(c.focusedCell, j), l = j ? i : k ? c.store.getById(k.id) : null, n = l && ((b = this.recordStateMap.get(l)) == null ? void 0 : b.widgets);
      if (n && (i == null ? void 0 : i.id) !== (k == null ? void 0 : k.id)) {
        let o = (k ? f.getById(k.columnId) : f.visibleColumns[0]).region;
        while (o) {
          const p = n[o];
          if (this.startEditInWidget(p, j)) {
            return true;
          }
          o = g[g.indexOf(o) + (j ? 1 : -1)];
        }
      }
    }
    return false;
  }
  startEditInWidget(a, b = true) {
    let c, e;
    const {cellEdit: f} = this.client.features;
    if ((f == null ? void 0 : f.enabled) && ((e = (c = a == null ? void 0 : a.features) == null ? void 0 : c.cellEdit) == null ? void 0 : e.enabled)) {
      f.finishEditing().then(() => {
        const g = a[(b ? 'first' : 'last') + 'VisibleRow'], i = this.getNavigateableColumn(a, true, !b);
        a.startEditing(a.normalizeCellContext({
          'record': g,
          'column': i
        }));
      });
      return true;
    }
  }
  onShiftTab() {
    return this.onTab(true);
  }
  observeResize(a) {
    const b = this;
    if (!b.resizeObserver) {
      b.resizeObserver = new ResizeObserver(c => b.onExpanderBodyResize(c));
    }
    b.resizeObserver.observe(a);
  }
  unobserveResize(a) {
    let b;
    (b = this.resizeObserver) == null ? void 0 : b.unobserve(a);
  }
  doDestroy() {
    let a;
    (a = this.resizeObserver) == null ? void 0 : a.disconnect();
    delete this.resizeObserver;
    for (const [, b] of this.recordStateMap) {
      this.destroyWidgets(b);
    }
    super.doDestroy();
  }
  onThemeChange({
    prev: a,
    theme: b
  }) {
    for (const [, c] of this.recordStateMap) {
      Object.values(c.expandedBodyElements).forEach(e => {
        let f, g, i, j;
        const k = e.querySelector('.' + this.shadowRootContainerClass);
        if (k == null ? void 0 : k._shadowRoot) {
          DomHelper.cloneStylesIntoShadowRoot(k == null ? void 0 : k._shadowRoot, true);
          (g = (f = e.widget) == null ? void 0 : f.element) == null ? void 0 : g.classList.remove('b-theme-' + a);
          (j = (i = e.widget) == null ? void 0 : i.element) == null ? void 0 : j.classList.add('b-theme-' + b);
        }
      });
    }
  }
  getExpandedRecord(a) {
    let b;
    for (const [c, {
            widgets: e,
            expandedBodyElements: f
          }] of this.recordStateMap.entries()) {
      if (e && a.isWidget) {
        for (const g in e) {
          if (e[g] === a) {
            return c;
          }
        }
        if ((b = a.expandedRecord) == null ? void 0 : b.isModel) {
          return a.expandedRecord;
        }
      } else {
        for (const i in f) {
          const j = f[i];
          if (j === a || j.contains(a)) {
            return c;
          }
        }
      }
    }
    return null;
  }
  getExpandedWidgets(a) {
    let b;
    return (b = this.recordStateMap.get(a)) == null ? void 0 : b.widgets;
  }
};
RowExpander._$name = 'RowExpander';
GridFeatureManager.registerFeature(RowExpander);
const RowReorder = class extends Delayable_default(InstancePlugin) {
  static configurable = {
    'showGrip': null,
    'gripOnly': null,
    'hoverExpandTimeout': 1000,
    'touchStartDelay': 300,
    'dropOnLeaf': null,
    'copyIconCls': 'b-icon b-icon-add',
    'allowCrossGridDrag': false,
    'dragHelperConfig': null,
    'preserveSorters': null,
    'animateDrop': true,
    'targetGrid': null
  };
  static $name = 'RowReorder';
  construct(a, b) {
    this.grid = a;
    if (a.isGanttBase && !('dropOnLeaf' in b)) {
      b.dropOnLeaf = true;
    }
    super.construct(...arguments);
  }
  doDestroy() {
    let a;
    (a = this.dragHelper) == null ? void 0 : a.destroy();
    super.doDestroy();
  }
  init() {
    const a = this, {
        grid: b,
        allowCrossGridDrag: c
      } = a;
    a.dragHelper = DragHelper.new({
      'name': 'rowReorder',
      'cloneTarget': true,
      'dragThreshold': 10,
      'proxyTopOffset': 10,
      'targetSelector': '.b-grid-row',
      'lockX': !c,
      'allowDropOutside': true,
      'outerElement': a.targetSubGridElement,
      'touchStartDelay': a.touchStartDelay,
      'isElementDraggable': a.isElementDraggable.bind(a),
      'dragWithin': c ? b.floatRoot : b.bodyContainer,
      'scrollManager': b.scrollManager,
      'positioning': 'inset',
      'monitoringConfig': {
        'scrollables': [{
            'element': b.scrollable.element,
            'direction': 'vertical'
          }]
      },
      'setXY'(e, f, g) {
        const {context: i} = this;
        if (!i.started) {
          const k = Rectangle.from(i.element, this.dragWithin), l = i.startPageY - globalThis.pageYOffset - i.element.getBoundingClientRect().top;
          this._elementHeight = k.height;
          g = k.top + l + this.proxyTopOffset;
          this._startScrollY = b.scrollable.y;
        }
        const j = c ? g : Math.min(b.scrollable.element.scrollHeight - this._elementHeight - 1, g + b.scrollable.y - this._startScrollY);
        DomHelper.setTopInsetInlineStart(e, j, f);
      },
      'ignoreSamePositionDrop': false,
      'createProxy'(e) {
        const f = e.cloneNode(true), g = document.createElement('div'), i = document.createElement('i');
        g.classList.add('b-row-reorder-proxy');
        i.className = 'b-row-proxy-copy ' + a.copyIconCls;
        f.removeAttribute('id');
        f.style.transform = f.style.insetInlineStart = f.style.top = '';
        f.style.width = e.offsetWidth + 'px';
        g.appendChild(i);
        g.appendChild(f);
        if (b.selectedRecords.length > 1) {
          const j = f.cloneNode(true);
          j.classList.add('b-row-dragging-multiple');
          g.appendChild(j);
        }
        DomHelper.removeClsGlobally(g, 'b-selected', 'b-hover', 'b-focused');
        return g;
      },
      'internalListeners': {
        'beforedragstart': 'onBeforeDragStart',
        'dragstart': 'onDragStart',
        'drag': 'onDrag',
        'drop': 'onDrop',
        'abort': 'onAbort',
        'reset': 'onReset',
        'prio': 10000,
        'thisObj': a
      }
    }, a.dragHelperConfig);
    b.relayEvents(a.dragHelper, [
      'beforeDragStart',
      'dragStart',
      'drag'
    ], 'gridRow', true);
    a.dropIndicator = DomHelper.createElement({ 'className': 'b-row-drop-indicator' });
    a.dropOverTargetCls = [
      'b-row-reordering-target',
      'b-hover'
    ];
  }
  static get pluginConfig() {
    return {
      'chain': [
        'beforeRenderCell',
        'onInternalPaint'
      ]
    };
  }
  get targetSubGridElement() {
    const a = this.grid.regions[0];
    return this.grid.subGrids[a].element;
  }
  beforeRenderCell({
    cellElement: a,
    column: b,
    record: c,
    cellCls: e
  }) {
    const {grid: f} = this;
    e['b-row-reorder-grip'] = this.showGrip && this.enabled && !c.isSpecialRow && b === f.columns.visibleColumns[0];
  }
  isElementDraggable(a, b) {
    let c;
    const e = (c = this.client.features.rowResize) == null ? void 0 : c.isEventOverResizeHandle(b);
    if (!e && !a.closest('.b-grid-cell .b-widget')) {
      if (this.gripOnly) {
        const f = a.closest('.b-grid-cell:first-child');
        if (f) {
          const g = getComputedStyle(f, ':before'), i = this.grid.rtl ? f.getBoundingClientRect().width - b.borderOffsetX : b.borderOffsetX, j = i <= parseFloat(g.width);
          if (j) {
            this.client.preventDragSelect = true;
          }
          return j;
        }
      } else {
        return true;
      }
    }
  }
  onBeforeDragStart({
    event: a,
    source: b,
    context: c
  }) {
    const e = this, {grid: f} = e, {group: g} = f.features, i = e.targetSubGridElement;
    if (a.target.classList.contains('b-rowexpander-shadowroot-container') || e.disabled || f.readOnly || f.isTreeGrouped || !i.contains(c.element)) {
      return false;
    }
    const j = c.startRecord = f.getRecordFromElement(c.element);
    if ((g == null ? void 0 : g.enabled) && Array.isArray(j[f.features.group.field])) {
      return false;
    }
    if (j.readOnly || j.isSpecialRow) {
      return false;
    }
    c.originalRowTop = f.rowManager.getRowFor(j).top;
    if (!f.selectionMode.checkboxOnly) {
      if (b.startEvent.pointerType === 'touch') {
        if (!f.isSelected(j)) {
          f.selectRow({
            'record': j,
            'addToSelection': false
          });
        }
      } else if (!f.isSelected(j) && !a.shiftKey && !a.ctrlKey) {
        f.selectRow({ 'record': j });
      }
    }
    const k = f.selectedRecords.filter(l => !l.readOnly);
    c.records = [j];
    if (k.includes(j)) {
      c.records.push(...k.filter(l => l !== j));
      c.records.sort((l, n) => f.store.indexOf(l) - f.store.indexOf(n));
    }
    return true;
  }
  onDragStart({context: a}) {
    let b, c;
    const e = this, {grid: f} = e, {
        cellEdit: g,
        cellMenu: i,
        headerMenu: j
      } = f.features;
    e.targetGrid = f;
    if (g) {
      e.cellEditDisabledState = g.disabled;
      g.disabled = true;
    }
    (b = i == null ? void 0 : i.hideContextMenu) == null ? void 0 : b.call(i, false);
    (c = j == null ? void 0 : j.hideContextMenu) == null ? void 0 : c.call(j, false);
    const k = a.element.querySelector('.b-focused');
    k == null ? void 0 : k.classList.remove('b-focused');
    a.element.firstElementChild.classList.remove('b-selected', 'b-hover');
  }
  resolveGrid(a, b) {
    let {target: c} = a;
    if (/^touch/.test(a.type)) {
      const e = Rectangle.from(b.element, null, true).center;
      c = DomHelper.elementFromPoint(e.x, e.y);
    }
    return this.client.constructor.fromElement(c, f => f.isGridBase);
  }
  onDrag({
    context: a,
    event: b
  }) {
    let c;
    const e = this, {clientY: f} = b;
    let g = e.grid;
    if (e.allowCrossGridDrag) {
      g = e.resolveGrid(b, a);
      e.targetGrid = g;
      if (!g) {
        a.valid = false;
        return;
      }
    }
    const {
      store: i,
      rowManager: j
    } = g;
    let k = true, l = j.getRowAt(f), n, o, p, q, r;
    if (l) {
      const u = l.top + g.scrollable.element.getBoundingClientRect().top - g.scrollable.y, v = l.height / 4, w = u + v, z = u + l.height / 2, A = u + v * 3;
      o = l.dataIndex;
      n = i.storage.getAt(o);
      if (i.isTree) {
        q = (n.isParent || e.dropOnLeaf) && f > w && f < A;
      } else if (i.isGrouped) {
        q = n.isGroupHeader && n.meta.collapsed;
      }
      p = !q && b.clientY >= z;
    } else {
      if (b.pageY < g._bodyRectangle.y) {
        o = 0;
        n = i.first;
        p = false;
      } else {
        o = i.count - 1;
        n = i.last;
        p = true;
      }
      l = j.getRow(o);
    }
    if (n === e.overRecord && e.after === p && e.over === q) {
      a.valid = e.reorderValid;
      return;
    }
    if (e.overRecord !== n) {
      (c = j.getRowById(e.overRecord)) == null ? void 0 : c.removeCls(e.dropOverTargetCls);
    }
    e.overRecord = n;
    e.after = p;
    e.over = q;
    if (e.targetGrid.readOnly || n === a.startRecord || !p && !q && o === 0 && i.isGrouped || p && n && n.isGroupHeader && n.meta.collapsed && i.indexOf(n) === i.count - 1) {
      k = false;
    }
    if (i.isTree) {
      if (n) {
        r = p ? n.nextSibling : n;
        if (a.records.some(B => B.contains(n))) {
          k = false;
        }
        a.parent = k && q ? n : n.parent;
        e.clearTimeout(e.hoverTimer);
        if (n && n.isParent && !n.isExpanded(i)) {
          e.hoverTimer = e.setTimeout(() => g.expand(n), e.hoverExpandTimeout);
        }
      } else {
        a.parent = g.store.rootNode;
      }
    } else {
      r = p ? i.storage.getAt(o + 1) : n;
    }
    if (l) {
      DomHelper.setTranslateY(e.dropIndicator, Math.max(l.top + (p ? l.element.getBoundingClientRect().height : 0), 1));
      l == null ? void 0 : l.toggleCls(e.dropOverTargetCls, k && q);
    } else if (g.store.count === 0) {
      DomHelper.setTranslateY(e.dropIndicator, 0);
    }
    if (!q && o === i.indexOf(a.startRecord) + (p ? -1 : 1) && a.parent && a.startRecord.parent === a.parent) {
      k = false;
    }
    e.dropIndicator.style.visibility = q ? 'hidden' : 'visible';
    e.dropIndicator.classList.toggle('b-drag-invalid', !k);
    a.insertBefore = r;
    a.valid = e.reorderValid = k;
  }
  async onDrop(a) {
    const b = this, {
        grid: c,
        targetGrid: e
      } = b, {context: f} = a, {ctrlKeyDown: g} = GlobalEvents_default;
    f.valid = f.valid && b.reorderValid;
    a.source = c;
    if (f.valid) {
      f.async = true;
      if (e.store.isTree) {
        f.oldPositionContext = f.records.map(i => {
          let j;
          return {
            'record': i,
            'parentId': (j = i.parent) == null ? void 0 : j.id,
            'parentIndex': i.parentIndex
          };
        });
      }
      if (await c.trigger('gridRowBeforeDropFinalize', a) === false) {
        f.valid = false;
      }
      b.animateDrop && await b.dragHelper.animateProxyTo(b.dropIndicator, { 'align': 'l0-l0' });
      await b.finalizeReorder(f, g);
    }
    b.clearTimeout(b.hoverTimer);
    c.trigger('gridRowDrop', a);
    b.overRecord = b.after = b.over = b.targetGrid = null;
  }
  onAbort(a) {
    a.source = this.client;
    this.client.trigger('gridRowDragAbort', a);
    this.targetGrid = null;
  }
  async finalizeReorder(a, b) {
    let c, e;
    const f = this, {
        client: g,
        allowCrossGridDrag: i
      } = f, j = a.targetGrid || g, {
        store: k,
        focusedCell: l
      } = j, {parent: n} = a;
    let {
      records: o,
      insertBefore: p
    } = a;
    if (!i && a.valid) {
      a.valid = !o.some(q => !k.includes(q));
    }
    if (a.valid) {
      const q = f.preserveSorters && k.isSorted && k.sorterFn;
      let r;
      if (b) {
        o = o.map(v => v.copy());
      }
      if (q) {
        o.sort(q);
      }
      if (k.isTree) {
        o = o.filter(v => !v.parent || v.bubbleWhile(w => !o.includes(w), true));
        r = await n.tryInsertChild(o, f.over ? (c = n.children) == null ? void 0 : c[0] : p);
        j.rowManager.forEach(v => v.removeCls(f.dropOverTargetCls));
        if (!n.isExpanded() && ((e = n.children) == null ? void 0 : e.length)) {
          j.expand(n);
        }
        a.valid = r !== false;
      } else if (k.isGrouped && f.over) {
        if (b) {
          k.insert(k.indexOf(p), o);
        } else {
          k.move(o, k.getNext(p));
        }
      } else {
        if (o.length > 1) {
          while (p && o.includes(p)) {
            p = k.getNext(p, false, true);
          }
        }
        if (k === g.store) {
          if (b) {
            k.insert(k.indexOf(p), o);
          } else {
            k.move(o, p);
          }
        } else if (f.allowCrossGridDrag.transferData) {
          await f.allowCrossGridDrag.transferData({
            'sourceGrid': g,
            'targetGrid': j,
            'records': o,
            'insertBefore': p
          });
          j.focusCell({
            'grid': j,
            'record': o[0]
          });
        } else {
          if (!b) {
            g.store.remove(o);
          }
          k.insert(p ? k.indexOf(p) : k.count, o);
        }
      }
      if ((l == null ? void 0 : l._rowIndex) >= 0 && j === g) {
        j._focusedCell = null;
        j.focusCell({
          'grid': g,
          'record': l.record,
          'columnId': l.columnId
        });
      }
      let u = true;
      if (a.valid && q) {
        const v = o[0], w = o[o.length - 1], z = v.previousSibling, A = w.nextSibling;
        if ((!z || q(z, v) <= 0) && (!A || q(w, A) <= 0)) {
          u = false;
        }
      }
      if (a.valid && u) {
        k.clearSorters();
      }
    }
    a.finalize(a.valid);
    j.element.classList.remove('b-row-reordering');
  }
  onReset() {
    const a = this, {
        targetGrid: b,
        grid: c
      } = a, e = c.features.cellEdit;
    c.element.classList.remove('b-row-reordering');
    if (e) {
      e.disabled = a.cellEditDisabledState;
    }
    DomHelper.removeClsGlobally(c.element, ...a.dropOverTargetCls);
    c.scrollManager.stopMonitoring();
    if (b && c !== b) {
      b.element.classList.remove('b-row-reordering');
      b.scrollManager.stopMonitoring();
      DomHelper.removeClsGlobally(b.element, ...a.dropOverTargetCls);
    }
  }
  onInternalPaint({firstPaint: a}) {
    if (!this._initialized && this.grid.regions.length > 0) {
      this._initialized = true;
      this.init();
    }
  }
  updateShowGrip(a) {
    if (this.enabled) {
      this.grid.columns.visibleColumns[0].refreshCells();
    }
  }
  updateDisabled(a, b) {
    super.updateDisabled(a, b);
    if (this.dragHelper) {
      this.dragHelper.disabled = a;
    }
    if (this.showGrip) {
      this.grid.columns.visibleColumns[0].refreshCells();
    }
  }
  updateTargetGrid(a, b) {
    if (b) {
      b.element.classList.remove('b-row-reordering');
      b.scrollManager.stopMonitoring();
    }
    if (this.dragHelper.isDragging) {
      this.dragHelper.context.targetGrid = a;
    }
    if (a) {
      a.element.classList.add('b-row-reordering');
      a.bodyContainer.appendChild(this.dropIndicator);
      a.scrollManager.startMonitoring({
        'scrollables': [{
            'element': a.scrollable.element,
            'direction': 'vertical'
          }]
      });
    } else {
      this.dropIndicator.remove();
    }
  }
  get isDragging() {
    return this.dragHelper.isDragging;
  }
};
RowReorder.featureClass = '';
RowReorder._$name = 'RowReorder';
GridFeatureManager.registerFeature(RowReorder, false);
const RowResize = class extends InstancePlugin {
  static configurable = {
    'applyToAllRows': null,
    'cellSelector': '.b-grid-cell',
    'minHeight': 20,
    'maxHeight': 0
  };
  static $name = 'RowResize';
  construct(a, b) {
    const c = this;
    super.construct(...arguments);
    c.resizer = new ResizeHelper({
      'name': 'rowResize',
      'targetSelector': '.b-grid-row:not(.b-group-header)',
      'handleContainerSelector': '.b-grid-row',
      'outerElement': a.element,
      'direction': 'vertical',
      'dragThreshold': 1,
      'handleSize': Math.min(5, a.rowHeight * 0.1),
      'internalListeners': {
        'beforeresizestart': c.onBeforeResizeStart,
        'resizestart': c.onResizeStart,
        'resizing': c.onResizing,
        'resize': c.onResize,
        'cancel': c.onCancel,
        'thisObj': c
      },
      'allowResize'(e, f) {
        let g;
        return (g = a.hoveredCell) == null ? void 0 : g.matches(c.cellSelector);
      },
      'overTopHandle'(e, f) {
        if (this.canResize(f, e) && f.dataset.index !== '0') {
          const g = Rectangle.from(f);
          g.height = this.handleSize;
          return g.height > 0 && Math.abs(g.top - EventHelper.getPagePoint(e).top) < this.handleSize;
        }
        return false;
      },
      'internalBeforeStart'(e) {
        const {
          edge: f,
          element: g
        } = e;
        if (f === 'top') {
          return {
            'edge': 'bottom',
            'element': g.previousElementSibling
          };
        }
      },
      'onResizeHandlePointerDown'(e) {
        a.selectionDisabled = true;
        if (a.features.rowReorder) {
          c._oldReorderDisabled = a.features.rowReorder.dragHelper.disabled;
          a.features.rowReorder.dragHelper.disabled = true;
        }
        a.setTimeout(() => {
          a.selectionDisabled = false;
        }, 10);
      }
    });
  }
  doDestroy() {
    let a;
    (a = this.resizer) == null ? void 0 : a.destroy();
    super.doDestroy();
  }
  onBeforeResizeStart() {
    return !this.disabled;
  }
  onResizeStart({
    source: a,
    context: b
  }) {
    const {client: c} = this, e = this.applyToAllRows || c.fixedRowHeight;
    b.record = c.store.getById(b.element.dataset.id);
    b.oldHeight = b.record.rowHeight;
    c.store.suspendAutoCommit();
    Object.assign(a, {
      'minHeight': this.minHeight,
      'maxHeight': this.maxHeight,
      'skipUpdatingElement': !e
    });
    c.element.classList.add('b-row-resizing');
  }
  onResizing({context: a}) {
    if (!this.applyToAllRows && !this.client.fixedRowHeight && a.valid) {
      a.record.rowHeight = a.newHeight;
    }
  }
  onResize({context: a}) {
    const {client: b} = this;
    if (this.applyToAllRows || b.fixedRowHeight) {
      b.rowHeight = a.newHeight;
    } else if (b.selectionMode.multiSelect && b.selectedRecords.includes(a.record)) {
      b.selectedRecords.forEach(c => c.rowHeight = a.newHeight);
    }
    this.cleanup();
  }
  onCancel({context: a}) {
    if (!this.applyToAllRows && !this.client.fixedRowHeight) {
      a.record.rowHeight = a.oldHeight;
    }
    this.cleanup();
  }
  cleanup() {
    const {client: a} = this;
    a.element.classList.remove('b-row-resizing');
    a.store.resumeAutoCommit();
    if (typeof this._oldReorderDisabled === 'boolean') {
      a.features.rowReorder.dragHelper.disabled = this._oldReorderDisabled;
    }
  }
  isEventOverResizeHandle(a) {
    return this.enabled && this.resizer.checkResizeHandles(a);
  }
};
RowResize._$name = 'RowResize';
GridFeatureManager.registerFeature(RowResize, false);
const actions5 = {
  'goToNextHit': 1,
  'goToPrevHit': 1
};
const Search = class extends Delayable_default(InstancePlugin) {
  static configurable = {
    'limit': 1000,
    'showHitIndex': true,
    'keyMap': {
      'F3': 'gotoNextHit',
      'Ctrl+g': 'gotoNextHit',
      'Shift+F3': 'gotoPrevHit',
      'Ctrl+Shift+g': 'gotoPrevHit'
    }
  };
  static $name = 'Search';
  static get properties() {
    return {
      'hitCls': 'b-search-hit',
      'hitCellCls': 'b-search-hit-cell',
      'hitCellBadgeCls': 'b-search-hit-cell-badge',
      'hitTextCls': 'b-search-hit-text'
    };
  }
  construct(a, b) {
    super.construct(a, b);
    Object.assign(this, {
      'grid': a,
      'text': '',
      'hitEls': [],
      'treeWalker': a.setupTreeWalker(a.element, DomHelper.NodeFilter.SHOW_TEXT, () => DomHelper.NodeFilter.FILTER_ACCEPT)
    });
    a.ion({
      'expandNode': 'onTreeNodeExpand',
      'thisObj': this
    });
  }
  isActionAvailable({actionName: a}) {
    if (actions5[a]) {
      return Boolean(this.text);
    }
  }
  onTreeNodeExpand() {
    if (this.text) {
      this.requestAnimationFrame(this.search, [
        this.text,
        false,
        true,
        void 0,
        void 0,
        false
      ]);
    }
  }
  doDestroy() {
    this.clear(true);
    super.doDestroy();
  }
  doDisable(a) {
    if (a) {
      this.clear();
    }
    super.doDisable(a);
  }
  get store() {
    return this.grid.store;
  }
  static get pluginConfig() {
    return { 'chain': ['populateCellMenu'] };
  }
  async search(a, b = true, c = false, e, f = false, g = this.client.store.isTree) {
    let j;
    const k = this;
    if (!a) {
      return k.clear();
    }
    if (!c && a === k.text || k.disabled) {
      return;
    }
    const {
        grid: l,
        store: n
      } = k, o = l.columns.visibleColumns.filter(u => u.searchable !== false), p = [];
    e = e || o.map(u => {
      let v;
      p.push((v = u.formatValue) == null ? void 0 : v.bind(u));
      return u.field;
    });
    const q = n.search(a, e, p);
    if (g && q.length) {
      await l.expandTo(q.map(u => u.id));
    }
    for (const u of o) {
      if (u.mergeCells && u.isSorted) {
        let v = null, w = null;
        for (const z of q) {
          if (z.field === u.field) {
            const A = z.data[z.field];
            if (A === v) {
              z.belongsTo = w;
            }
            v = A;
            w = z.field + '-' + z.id;
          }
        }
      }
    }
    let r = 1;
    Object.assign(k, {
      'foundMap': {},
      'prevFound': k.found,
      'found': q,
      'text': a,
      'findRe': new RegExp('(\\s+)?(' + StringHelper.escapeRegExp(String(a)) + ')(\\s+)?', 'ig')
    });
    k.clearHits();
    if (!q) {
      return;
    }
    for (const B of q) {
      k.foundMap[B.field + '-' + B.id] = B.belongsTo ? k.foundMap[B.belongsTo] : r++;
      if (r > k.limit) {
        break;
      }
    }
    if (!k.listenersInitialized) {
      l.rowManager.ion({
        'name': 'renderCell',
        'renderCell': 'renderCell',
        'thisObj': k
      });
      n.ion({
        'name': 'storeRefresh',
        ['refresh' + l.asyncEventSuffix]: 'onStoreRefresh',
        'thisObj': k
      });
      k.listenersInitialized = true;
    }
    l.refreshRows();
    l.trigger('search', {
      'grid': l,
      'find': a,
      'found': q
    });
    if (b && !k.isHitFocused && !f) {
      k.gotoNextHit(true);
    }
    (j = l.syncSplits) == null ? void 0 : j.call(l, C => C.features.search.search(a, b, c, e, true));
    return q;
  }
  clearHits() {
    for (const a of DomHelper.children(this.grid.element, '.' + this.hitCls)) {
      a.classList.remove(this.hitCls, this.hitCellCls);
      const b = DomDataStore.get(a).row;
      if (b) {
        b.forceInnerHTML = true;
        b.renderCell(a);
        b.forceInnerHTML = false;
      }
    }
  }
  clear(a = false) {
    let b;
    const c = this, {grid: e} = c;
    if (c.foundMap) {
      delete c.foundMap;
    }
    c.text = null;
    c.clearHits();
    if (c.listenersInitialized) {
      c.detachListeners('renderCell');
      c.detachListeners('storeRefresh');
      c.listenersInitialized = false;
    }
    if (!a) {
      e.trigger('clearSearch', { 'grid': e });
      (b = e.syncSplits) == null ? void 0 : b.call(e, f => f.features.search.clear());
    }
  }
  get foundCount() {
    let a, b;
    return (b = (a = this.found) == null ? void 0 : a.length) != null ? b : 0;
  }
  get isHitFocused() {
    let a;
    const b = this, {grid: c} = b, {focusedCell: e} = c;
    if ((a = e == null ? void 0 : e.cell) == null ? void 0 : a.contains(DomHelper.getActiveElement(c.element))) {
      const {
        rowIndex: f,
        column: g
      } = e;
      return f !== -1 && b.found.some(i => i.index === f && g && i.field === g.field);
    }
  }
  gotoNextHit(a = false) {
    let b;
    const c = this;
    if (!((b = c.found) == null ? void 0 : b.length)) {
      return;
    }
    const {
        grid: e,
        store: f
      } = c, g = e.focusedCell || e.lastFocusedCell, i = g && a !== true ? f.indexOf(g.record, void 0, true) : -1, j = c.found.findIndex(k => k.index > i);
    if (j !== -1) {
      c.gotoHit(j);
    }
  }
  gotoPrevHit() {
    let a;
    const b = this, {store: c} = b;
    if (!((a = b.found) == null ? void 0 : a.length)) {
      return;
    }
    const {
        grid: e,
        found: f
      } = b, g = e.focusedCell || e.lastFocusedCell, j = g ? c.indexOf(g.record, void 0, true) : 0;
    for (let k = f.length - 1; k--; k >= 0) {
      const l = f[k];
      if (l.index < j) {
        b.gotoHit(k);
        break;
      }
    }
  }
  gotoHit(a) {
    const {grid: b} = this, c = this.found[a];
    if (c) {
      b.focusCell({
        'field': c.field,
        'id': c.id
      });
    }
    return Boolean(c);
  }
  gotoFirstHit() {
    this.gotoHit(0);
  }
  gotoLastHit() {
    this.gotoHit(this.found.length - 1);
  }
  renderCell({
    cellElement: a,
    column: b,
    record: c,
    value: e
  }) {
    let f, g;
    const i = this, {
        treeWalker: j,
        findRe: k,
        hitTextCls: l,
        showHitIndex: n
      } = i, o = (f = i.foundMap) == null ? void 0 : f[b.field + '-' + c.id];
    if (o) {
      a.classList.add(i.hitCls);
      n && ((g = a.querySelector('.' + i.hitCellBadgeCls)) == null ? void 0 : g.remove());
      const p = DomHelper.down(a, '.b-grid-cell-value,.b-tree-cell-value') || a;
      if (String(e).toLowerCase() === String(i.text).toLowerCase()) {
        p.innerHTML = '<span class="' + i.hitTextCls + '">' + p.innerHTML + '</span>' + (n ? '<div class="' + i.hitCellBadgeCls + '">' + o + '</div>' : '');
      } else {
        j.currentNode = p;
        for (let q = j.nextNode(); q && p.contains(q);) {
          const r = q, u = q.nodeValue, w = ['<span>'];
          q = j.nextNode();
          let z = k.lastIndex;
          for (let A = k.exec(u); A; A = k.exec(u)) {
            const B = u.substring(z, A.index), C = A[1] ? '\xA0' : '', E = A[2], F = A[3] ? '\xA0' : '';
            w.push('' + StringHelper.encodeHtml(B) + C + '<span class="' + l + '">' + StringHelper.encodeHtml(E) + '</span>' + F);
            z = k.lastIndex;
          }
          w.push(StringHelper.encodeHtml(u.substring(z)), '<span>');
          r.parentNode.insertBefore(DomHelper.createElementFromTemplate(w.join(''), { 'fragment': true }), r);
          r.remove();
        }
        if (n) {
          DomHelper.createElement({
            'parent': a,
            'className': i.hitCellBadgeCls,
            'text': o
          });
        }
      }
      i.hitEls.push(a);
    }
  }
  populateCellMenu({
    column: a,
    record: b,
    items: c,
    cellElement: e
  }) {
    const f = this;
    if (a.searchable) {
      c.search = {
        'text': 'L{searchForValue}',
        'localeClass': f,
        'icon': 'b-fw-icon b-icon-search',
        'cls': 'b-separator',
        'weight': 200,
        'disabled': f.disabled,
        'onItem': () => {
          let g = globalThis.getSelection().toString();
          if (!g) {
            g = e.innerText;
          }
          f.search(g);
        }
      };
    }
  }
  onStoreRefresh() {
    this.search(this.text, false, true);
  }
};
Search.featureClass = 'b-search';
Search._$name = 'Search';
GridFeatureManager.registerFeature(Search);
const emptyArray11 = Object.freeze([]);
const Sort = class extends InstancePlugin {
  static configurable = {
    'multiSort': true,
    'prioritizeColumns': false,
    'toggleOnHeaderClick': true
  };
  static $name = 'Sort';
  static get properties() {
    return {
      'ignoreRe': new RegExp([
        'b-grid-header-resize-handle',
        'b-filter-icon',
        'b-widget'
      ].join('|')),
      'sortableCls': 'b-sortable',
      'sortedCls': 'b-sort',
      'sortedAscCls': 'b-asc',
      'sortedDescCls': 'b-desc'
    };
  }
  construct(a, b) {
    b = this.processConfig(b);
    this.grid = a;
    this.bindStore(this.store);
    super.construct(a, b);
  }
  processConfig(a) {
    if (typeof a === 'string' || Array.isArray(a)) {
      return {
        'field': a,
        'ascending': null
      };
    }
    return a;
  }
  setConfig(a) {
    super.setConfig(this.processConfig(a));
  }
  bindStore(a) {
    let b;
    this.detachListeners('store');
    a.ion({
      'name': 'store',
      'beforeSort': 'onStoreBeforeSort',
      'sort': 'syncHeaderSortState',
      'thisObj': this
    });
    if ((b = this.client) == null ? void 0 : b.isPainted) {
      this.syncHeaderSortState();
    }
  }
  set field(a) {
    let b;
    const c = this.grid.columns.get(a);
    if (c && typeof c.sortable === 'object') {
      c.sortable.field = c.sortable.property || a;
      a = c.sortable;
    }
    if (!((b = this.store.sorters) == null ? void 0 : b.some(e => e.field === a))) {
      this.store.sort(a, this.ascending);
    }
  }
  get store() {
    return this.grid[this.grid.sortFeatureStore];
  }
  static get pluginConfig() {
    return {
      'chain': [
        'onElementClick',
        'populateHeaderMenu',
        'getColumnDragToolbarItems',
        'renderHeader',
        'onInternalPaint',
        'bindStore'
      ]
    };
  }
  syncHeaderSortState() {
    let a;
    const b = this, {grid: c} = b;
    if (!c.hideHeaders && c.isPainted) {
      const e = b.store.sorters.concat(b.store.groupers || emptyArray11), f = e.length, g = new DomClassList();
      let i;
      for (const j of c.columns.visibleColumns) {
        if (!j.sortable) {
          continue;
        }
        const k = j.element, l = j.textWrapper, n = l == null ? void 0 : l.dataset;
        let o = 'none';
        (n == null ? void 0 : n.sortIndex) && delete n.sortIndex;
        g.value = k.classList;
        if (j.sortable === false) {
          g.remove(b.sortableCls);
          (a = l == null ? void 0 : l.querySelector('.b-sort-icon')) == null ? void 0 : a.remove();
        } else {
          if (!(l == null ? void 0 : l.querySelector('.b-sort-icon'))) {
            DomHelper.createElement({
              'parent': l,
              'className': 'b-sort-icon'
            });
          }
          g.add(b.sortableCls);
          i = e.find(p => p.field === j.field || p.sortFn && p.sortFn === j.sortable.sortFn);
          if (i) {
            if (f > 1 && n) {
              n.sortIndex = e.indexOf(i) + 1;
            }
            g.add(b.sortedCls);
            if (i.ascending) {
              g.add(b.sortedAscCls);
              g.remove(b.sortedDescCls);
              o = 'ascending';
            } else {
              g.add(b.sortedDescCls);
              g.remove(b.sortedAscCls);
              o = 'descending';
            }
          } else {
            g.remove(b.sortedCls);
            if (!g['b-group']) {
              g.remove(b.sortedAscCls);
              g.remove(b.sortedDescCls);
            }
          }
        }
        DomHelper.syncClassList(k, g);
        DomHelper.setAttributes(k, { 'aria-sort': o });
      }
    }
  }
  populateHeaderMenu({
    column: a,
    items: b
  }) {
    const c = this, {store: e} = c, f = {
        ...a.sortable,
        'field': a.field,
        'columnOwned': true
      };
    if (a.sortable !== false) {
      b.sortAsc = {
        'text': 'L{sortAscending}',
        'localeClass': c,
        'icon': 'b-fw-icon b-icon-sort-asc',
        'cls': 'b-separator',
        'weight': 300,
        'disabled': c.disabled,
        'onItem': () => e.sort(f, true)
      };
      b.sortDesc = {
        'text': 'L{sortDescending}',
        'localeClass': c,
        'icon': 'b-fw-icon b-icon-sort-desc',
        'weight': 310,
        'disabled': c.disabled,
        'onItem': () => e.sort(f, false)
      };
      if (c.multiSort && c.grid.columns.records.some(g => g.sortable)) {
        const g = e.sorters.find(i => i.field === a.field || a.sortable.sortFn && a.sortable.sortFn === i.sortFn);
        b.multiSort = {
          'text': 'L{multiSort}',
          'localeClass': c,
          'icon': 'b-fw-icon b-icon-sort',
          'weight': 320,
          'disabled': c.disabled,
          'menu': {
            'addSortAsc': {
              'text': g ? 'L{toggleSortAscending}' : 'L{addSortAscending}',
              'localeClass': c,
              'icon': 'b-fw-icon b-icon-sort-asc',
              'disabled': g && (g == null ? void 0 : g.ascending),
              'weight': 330,
              'onItem': () => e.addSorter(f, true)
            },
            'addSortDesc': {
              'text': g ? 'L{toggleSortDescending}' : 'L{addSortDescending}',
              'localeClass': c,
              'icon': 'b-fw-icon b-icon-sort-desc',
              'disabled': g && !g.ascending,
              'weight': 340,
              'onItem': () => e.addSorter(f, false)
            },
            'removeSorter': {
              'text': 'L{removeSorter}',
              'localeClass': c,
              'icon': 'b-fw-icon b-icon-remove',
              'weight': 350,
              'disabled': !g,
              'onItem': () => {
                e.removeSorter(f.field);
              }
            }
          }
        };
      }
    }
  }
  getColumnDragToolbarItems(a, b) {
    const c = this, {
        store: e,
        disabled: f
      } = c;
    if (a.sortable !== false) {
      const g = e.sorters.find(i => i.field === a.field);
      b.push({
        'text': 'L{sortAscendingShort}',
        'group': 'L{sort}',
        'localeClass': c,
        'icon': 'b-icon b-icon-sort-asc',
        'ref': 'sortAsc',
        'cls': 'b-separator',
        'weight': 105,
        'disabled': f,
        'onDrop': ({column: i}) => e.sort(i.field, true)
      }, {
        'text': 'L{sortDescendingShort}',
        'group': 'L{sort}',
        'localeClass': c,
        'icon': 'b-icon b-icon-sort-desc',
        'ref': 'sortDesc',
        'weight': 105,
        'disabled': f,
        'onDrop': ({column: i}) => e.sort(i.field, false)
      }, {
        'text': 'L{addSortAscendingShort}',
        'group': 'L{multiSort}',
        'localeClass': c,
        'icon': 'b-icon b-icon-sort-asc',
        'ref': 'multisortAddAsc',
        'disabled': f || g && g.ascending,
        'weight': 105,
        'onDrop': ({column: i}) => e.addSorter(i.field, true)
      }, {
        'text': 'L{addSortDescendingShort}',
        'group': 'L{multiSort}',
        'localeClass': c,
        'icon': 'b-icon b-icon-sort-desc',
        'ref': 'multisortAddDesc',
        'disabled': f || g && !g.ascending,
        'weight': 105,
        'onDrop': ({column: i}) => e.addSorter(i.field, false)
      }, {
        'text': 'L{removeSorterShort}',
        'group': 'L{multiSort}',
        'localeClass': c,
        'icon': 'b-icon b-icon-remove',
        'ref': 'multisortRemove',
        'weight': 105,
        'disabled': f || !g,
        'onDrop': ({column: i}) => e.removeSorter(i.field)
      });
    }
    return b;
  }
  onStoreBeforeSort({sorters: a}) {
    let b;
    const {columns: c} = this.client;
    for (let e = 0; e < a.length; e++) {
      const f = a[e], g = (f.columnOwned || this.prioritizeColumns) && c.get(f.field);
      if ((b = g == null ? void 0 : g.sortable) == null ? void 0 : b.sortFn) {
        a[e] = {
          ...f,
          ...g.sortable,
          'columnOwned': true
        };
      }
    }
  }
  onElementClick(a) {
    const b = this, {store: c} = b, {target: e} = a, f = e.closest('.b-grid-header.b-sortable'), g = f == null ? void 0 : f.dataset.column;
    if (b.ignoreRe.test(e.className) || e.closest('.b-grid-headerwidget') || b.disabled || a.handled) {
      return;
    }
    if (f && g && (b.toggleOnHeaderClick || e.closest('.b-sort-icon'))) {
      const i = b.grid.columns.getById(f.dataset.columnId), j = c.isGrouped && c.groupers.find(k => k.field === g);
      if (j && !a.shiftKey) {
        return;
      }
      if (i.sortable && !a.shiftKey) {
        if (a.ctrlKey && a.altKey) {
          c.removeSorter(i.field);
        } else {
          const k = {
            'columnOwned': true,
            'field': i.field
          };
          if (typeof i.sortable === 'object') {
            ObjectHelper.assign(k, i.sortable);
          }
          c.sort(k, null, a.ctrlKey && b.multiSort);
        }
      }
    }
  }
  renderHeader() {
    this.syncHeaderSortState();
  }
  onInternalPaint() {
    this.syncHeaderSortState();
  }
};
Sort.featureClass = 'b-sort';
Sort._$name = 'Sort';
GridFeatureManager.registerFeature(Sort, true);
const StickyCells = class extends InstancePlugin {
  static get $name() {
    return 'StickyCells';
  }
  static get defaultConfig() {
    return {
      'contentSelector': null,
      'currentTopRowCls': 'b-sticky-cells-current-top-row'
    };
  }
  static get pluginConfig() {
    return { 'before': ['renderRows'] };
  }
  construct(a, b) {
    super.construct(a, b);
    a.ion({
      'scroll': 'onGridScroll',
      'thisObj': this
    });
    Object.assign(this, DomHelper.createElement({
      'reference': 'element',
      'parent': a.element,
      'className': 'b-grid-sticky-row',
      'children': [{
          'reference': 'contentElement',
          'className': 'b-grid-cell'
        }]
    }));
    this.removeClasses = {
      'b-focused': false,
      'b-hover': false,
      'b-selected': false,
      [this.currentTopRowCls]: false
    };
  }
  renderRows() {
    this.element.classList.add('b-hide-visibility');
  }
  onGridScroll() {
    const a = this, {
        client: b,
        element: c,
        contentElement: e
      } = a, f = Rectangle.client(b.bodyContainer).roundPx(), g = b.rowManager.getRowAt(f.y), i = g !== a.currentTopRow;
    if (g) {
      if (i) {
        if (a.currentTopRow) {
          a.currentTopRow.removeCls(a.currentTopRowCls);
          a.currentTopRow.removeCls('b-not-enough-height');
        }
        a.currentTopRow = g;
        g.addCls(a.currentTopRowCls);
        e.innerHTML = '';
        e.appendChild(a.updateStickyContent());
      }
      const j = a.currentTopRow.bottom - a.client.scrollable.y <= a.stickyContentHeight;
      a.element.classList[j ? 'add' : 'remove']('b-hide-visibility');
      a.currentTopRow[j ? 'addCls' : 'removeCls']('b-not-enough-height');
      f.y += a.stickyContentTop;
      DomHelper.alignTo(c, a.stickyEls[0], {
        'align': 't0-t0',
        'constrainTo': f
      }, true);
    }
    a.lastProcessedTopRow = g;
  }
  updateStickyContent() {
    const a = this, {
        currentTopRow: b,
        removeClasses: c
      } = a, e = { 'b-grid-sticky-row': 1 }, f = {}, g = a.stickyContent || (a.stickyContent = document.createDocumentFragment()), i = a.stickyEls || (a.stickyEls = []);
    i.forEach(j => {
      j.classList.remove('b-sticky-content-el');
    });
    i.length = 0;
    b.eachElement(j => {
      i.push(...j.querySelectorAll(a.contentSelector));
    });
    while (g.firstChild) {
      g.remove(g.firstChild);
    }
    a.stickyContentHeight = 0;
    a.stickyContentTop = 0;
    i.map(j => {
      j.closest('.b-grid-cell').classList.forEach(n => f[n] = 1);
      j.closest('.b-grid-row').classList.forEach(n => e[n] = 1);
      Object.assign(f, c);
      Object.assign(e, c);
      const k = j.offsetTop;
      a.stickyContentTop = Math.max(a.stickyContentTop, k);
      j.style.alignSelf = 'flex-end';
      a.stickyContentHeight = Math.max(a.stickyContentHeight, a.currentTopRow.height - j.offsetTop + k);
      j.style.alignSelf = '';
      const l = j.cloneNode(true);
      j.classList.add('b-sticky-content-el');
      g.appendChild(l);
      return l;
    });
    f['b-focused'] = false;
    DomHelper.syncClassList(a.contentElement, f);
    DomHelper.syncClassList(a.element, e);
    return g;
  }
};
StickyCells._$name = 'StickyCells';
GridFeatureManager.registerFeature(StickyCells, false);
const Stripe = class extends InstancePlugin {
  static $name = 'Stripe';
  static get pluginConfig() {
    return { 'chain': ['beforeRenderRow'] };
  }
  doDisable(a) {
    if (!this.isConfiguring) {
      this.client.refreshRows();
    }
    super.doDisable(a);
  }
  beforeRenderRow({
    row: a,
    record: b
  }) {
    let c, e;
    const {enabled: f} = this, {isGroupHeader: g} = b, i = this.client.store.isGrouped && b.instanceMeta(this.client.store).groupParent, j = ((e = (c = i == null ? void 0 : i.groupChildren) == null ? void 0 : c.indexOf(b)) != null ? e : a.dataIndex) % 2 === 0;
    a.assignCls({
      'b-even': f && !g && j,
      'b-odd': f && !g && !j
    });
  }
};
Stripe._$name = 'Stripe';
GridFeatureManager.registerFeature(Stripe);
const Summary = class extends SummaryFormatter_default(InstancePlugin) {
  static get configurable() {
    return {
      'selectedOnly': null,
      'refreshOnlyAffectedSummaries': null,
      'hideFooters': false
    };
  }
  static get pluginConfig() {
    return {
      'chain': [
        'renderRows',
        'bindStore'
      ]
    };
  }
  static get $name() {
    return 'Summary';
  }
  construct(a, b) {
    this.grid = a;
    super.construct(a, b);
    this.bindStore(a.store);
    a.hideFooters = this.hideFooters;
  }
  bindStore(a) {
    this.detachListeners('store');
    a.ion({
      'name': 'store',
      'change': 'onStoreChange',
      'thisObj': this
    });
    if (!this.isConfiguring) {
      this.refreshSummaries();
    }
  }
  get store() {
    return this.grid.store;
  }
  doDestroy() {
    super.doDestroy();
  }
  doDisable(a) {
    super.doDisable(a);
    const {client: b} = this;
    if (a) {
      b.element.classList.add('b-summary-disabled');
    } else {
      this.refreshSummaries();
      b.element.classList.remove('b-summary-disabled');
      b.eachSubGrid(c => c.scrollable.syncPartners());
    }
  }
  renderRows() {
    this.refreshSummaries();
  }
  refreshSummaries(a) {
    const b = this, {
        grid: c,
        store: e
      } = b, f = a ? null : DomHelper.children(c.element, '.b-grid-footer'), g = b.selectedOnly && c.selectedRecords.length > 0, i = (e.isFiltered ? e.storage.values : e.allRecords).filter(k => !k.isSpecialRow && (!g || c.isSelected(k))), j = a ? a.map(k => c.columns.get(k)) : c.columns.visibleColumns;
    j.forEach((k, l) => {
      let n;
      (n = k.summaries) == null ? void 0 : n.forEach(q => {
        if ('seed' in q) {
          if (!('initialSeed' in q)) {
            q.initialSeed = q.seed;
          }
          if ([
              'number',
              'string',
              'date'
            ].includes(typeof q.initialSeed)) {
            q.seed = q.initialSeed;
          } else {
            q.seed = Object.assign({}, q.initialSeed);
          }
        }
      });
      const o = a ? c.element.querySelector('.b-grid-footer[data-column-id=' + k.id + ']') : f[l];
      if (!(o == null ? void 0 : o.dataset.column)) {
        return;
      }
      const p = b.generateHtml(k, i, 'b-grid-footer-summary');
      if (k.summaries ? k.summaries.length : k.sum ? 1 : 0) {
        if (!o.children.length) {
          o.innerHTML = p;
        } else {
          DomHelper.sync(p, o.firstElementChild);
        }
      }
    });
  }
  onStoreChange({
    action: a,
    changes: b
  }) {
    let c = true;
    if (this.disabled) {
      return;
    }
    if (a === 'update') {
      c = Object.keys(b).some(e => {
        const f = this.grid.columns.get(e);
        return Boolean(f) && (Boolean(f.sum) || Boolean(f.summaries));
      });
    }
    if (c) {
      this.refreshSummaries(a === 'update' && this.refreshOnlyAffectedSummaries ? Object.keys(b) : void 0);
    }
  }
  updateSelectedOnly(a) {
    const b = this;
    b.detachListeners('selectionChange');
    if (a) {
      b.grid.ion({
        'name': 'selectionChange',
        'selectionChange': b.refresh,
        'thisObj': b
      });
    }
    b.refresh();
  }
  refresh() {
    this.refreshSummaries();
  }
};
Summary.featureClass = 'b-summary';
Summary._$name = 'Summary';
GridFeatureManager.registerFeature(Summary);
const immediatePromise5 = Promise.resolve();
const Tree = class extends InstancePlugin.mixin(Delayable_default) {
  static configurable = {
    'expandOnCellClick': false,
    'keyMap': {
      ' ': 'toggleCollapseByKey',
      'ArrowRight': 'expandIfSingleColumn',
      'Shift+ArrowRight': 'expandByKey',
      'ArrowLeft': 'collapseIfSingleColumn',
      'Shift+ArrowLeft': 'collapseByKey'
    }
  };
  static $name = 'Tree';
  static get pluginConfig() {
    return {
      'assign': [
        'collapseAll',
        'expandAll',
        'collapse',
        'expand',
        'expandTo',
        'toggleCollapse'
      ],
      'chain': [
        'onElementClick',
        'bindStore',
        'beforeRenderRow'
      ]
    };
  }
  construct(a, b) {
    let c;
    super.construct(a, b);
    if (!this.treeColumn && ((c = a.initialConfig.columns) == null ? void 0 : c.length) > 0) {
      console.info('To use the tree feature, one column should be configured with `type: \'tree\'`');
    }
    a.store && this.bindStore(a.store);
  }
  doDisable(a) {
    if (a) {
      throw new Error('Tree feature cannot be disabled');
    }
  }
  get store() {
    return this.client.store;
  }
  get treeColumn() {
    const a = this, {columns: b} = a.client;
    if (!a._treeColumn || !b.includes(a._treeColumn)) {
      a._treeColumn = b.find(c => c.isTreeColumn);
    }
    return a._treeColumn;
  }
  bindStore(a) {
    this.detachListeners('store');
    a.ion({
      'name': 'store',
      'beforeLoadChildren': 'onBeforeLoadChildren',
      'loadChildren': 'onLoadChildren',
      'loadChildrenException': 'onLoadChildrenException',
      'beforeToggleNode': 'onBeforeToggleNode',
      'beforeLazyLoad': 'onBeforeLoadChildren',
      'afterLazyLoad': 'onLoadChildren',
      'thisObj': this
    });
  }
  async toggleCollapse(a, b) {
    if (a == null) {
      throw new Error('Tree#toggleCollapse must be passed a record');
    }
    const c = this, {
        store: e,
        client: f
      } = c, {rowManager: g} = f, i = e.getById(a), j = i.instanceMeta(e);
    let k = g.getRowFor(i);
    i.generation++;
    if (await e.toggleCollapse(i, b)) {
      k = g.getRowFor(i);
      if (k && i.ancestorsExpanded()) {
        k.render(null, null, false);
      }
      if (!c.isTogglingNode) {
        f.element.classList.add('b-toggling-node');
        c.isTogglingNode = true;
        c.requestAnimationFrame(() => {
          f.element.classList.remove('b-toggling-node');
          c.isTogglingNode = false;
        });
      }
      f.trigger(j.collapsed ? 'collapseNode' : 'expandNode', { 'record': i });
      f.trigger('toggleNode', {
        'record': i,
        'collapse': j.collapsed
      });
    }
  }
  async collapse(a) {
    return this.toggleCollapse(a, true);
  }
  async expand(a) {
    return this.toggleCollapse(a, false);
  }
  onBeforeToggleNode({
    record: a,
    collapse: b
  }) {
    this.client.trigger('beforeToggleNode', {
      'record': a,
      'collapse': b
    });
  }
  onBeforeLoadChildren({
    source: a,
    params: b,
    parent: parent = a.getById(b[a.modelClass.idField])
  }) {
    let c;
    (c = this.client.rowManager.getRowFor(parent)) == null ? void 0 : c.addCls('b-loading-children');
  }
  onLoadChildren({
    source: a,
    params: b,
    parent: parent = a.getById(b[a.modelClass.idField])
  }) {
    let c;
    (c = this.client.rowManager.getRowFor(parent)) == null ? void 0 : c.removeCls('b-loading-children');
  }
  onLoadChildrenException({record: a}) {
    const b = this.client.rowManager.getRowFor(a);
    b == null ? void 0 : b.removeCls('b-loading-children');
  }
  async expandOrCollapseAll(a = true, b = this.store.rootNode) {
    const {
        client: c,
        store: e
      } = this, {animateTreeNodeToggle: f} = c, g = [], i = [];
    c.trigger('beforeToggleAllNodes', { 'collapse': a });
    c.animateTreeNodeToggle = false;
    e.suspendEvents();
    e.traverse(j => {
      const k = j.instanceMeta(e);
      if (!j.isLeaf) {
        if (a && !k.collapsed) {
          this.toggleCollapse(j, true);
          i.push(...j.children);
        } else if (!a && k.collapsed) {
          if (Array.isArray(j.children)) {
            i.push(...j.children);
          }
          g.push(this.toggleCollapse(j, false));
        }
      }
    }, b, b === e.rootNode);
    e.resumeEvents();
    return (a ? immediatePromise5 : Promise.all(g)).then(() => {
      if (i.length) {
        if (a) {
          e.trigger('remove', {
            'records': i,
            'isCollapse': true,
            'isCollapseAll': true
          });
        } else {
          e.trigger('add', {
            'records': i,
            'isExpand': true,
            'isExpandAll': true
          });
        }
      }
      c.refreshRows(null, a);
      c.trigger('toggleAllNodes', { 'collapse': a });
      c.animateTreeNodeToggle = f;
    });
  }
  async collapseAll() {
    return this.expandOrCollapseAll(true);
  }
  async expandAll() {
    return this.expandOrCollapseAll(false);
  }
  async expandTo(a, b = true) {
    let c, e, f, g;
    const j = this, {
        store: k,
        client: l
      } = j, {animateTreeNodeToggle: n} = l;
    if (Array.isArray(a)) {
      if (a.length > 0) {
        l.suspendRefresh();
        for (let p = a.length - 1; p >= 0; p--) {
          const q = k.getById(a[p]);
          if (p === 0) {
            (c = l.resumeRefresh) == null ? void 0 : c.call(l, false);
            l.rowManager.refresh();
          }
          await ((e = j.expandTo) == null ? void 0 : e.call(j, q, p === 0));
        }
      }
      return;
    }
    const o = k.getById(a);
    if (o.instanceMeta(j.store).hiddenByCollapse === false) {
      return;
    }
    l.animateTreeNodeToggle = false;
    if (!o.ancestorsExpanded()) {
      const r = [];
      for (let u = o.parent; u && !u.isRoot; u = u.parent) {
        if (!u.isExpanded(k)) {
          r.unshift(u);
        }
      }
      l.suspendRefresh();
      for (const v of r) {
        if (!j.isDestroyed) {
          await j.toggleCollapse(v, false);
        }
      }
      (f = l.resumeRefresh) == null ? void 0 : f.call(l, false);
      (g = l.refreshRows) == null ? void 0 : g.call(l);
    }
    l.animateTreeNodeToggle = n;
    if (!j.isDestroyed && b) {
      await l.scrollRowIntoView(o);
    }
  }
  async expandToLevel(a, b = false) {
    const {
        store: c,
        client: e
      } = this, {animateTreeNodeToggle: f} = e, g = i => {
        i.children.forEach(j => {
          if (!j.isLeaf) {
            if (j.instanceMeta(c).collapsed) {
              this.toggleCollapse(j, false);
            }
            if (j.childLevel < a) {
              g(j);
            }
          }
        });
      };
    c.suspendEvents();
    e.suspendRefresh();
    if (b) {
      await this.collapseAll();
    }
    if (!e.isDestroyed) {
      e.animateTreeNodeToggle = false;
      g(c.rootNode);
      c.resumeEvents();
      e.resumeRefresh(true);
      e.animateTreeNodeToggle = f;
    }
  }
  onElementClick(a) {
    const b = this, c = a.target, e = b.client.getCellDataFromEvent(a), f = c.closest('.b-tree-expander');
    if (f) {
      a.preventDefault();
    }
    if (f || b.expandOnCellClick && (e == null ? void 0 : e.record.isParent)) {
      b.toggleCollapse(e.record);
    }
  }
  toggleCollapseByKey() {
    let a;
    const {focusedCell: b} = this.client;
    if ((b == null ? void 0 : b.rowIndex) > -1 && !((a = this.client.features.cellEdit) == null ? void 0 : a.isEditing) && b.record.isParent && document.activeElement.closest('.b-grid-cell')) {
      this.toggleCollapse(b.id);
      return true;
    }
    return false;
  }
  beforeRenderRow({
    record: a,
    cls: b
  }) {
    if (!a.isLeaf) {
      const {store: c} = this.client, e = !a.isExpanded(c);
      b['b-tree-parent-row'] = 1;
      b['b-tree-collapsed'] = e;
      b['b-tree-expanded'] = !e;
      b['b-loading-children'] = a.instanceMeta(c).isLoadingChildren;
    } else {
      b['b-tree-parent-row'] = 0;
      b['b-tree-collapsed'] = 0;
      b['b-tree-expanded'] = 0;
      b['b-loading-children'] = 0;
    }
  }
  expandIfSingleColumn() {
    if (this.client.columns.count === 1) {
      return this.expandByKey();
    }
    return false;
  }
  expandByKey() {
    const a = this, {client: b} = a, {focusedCell: c} = b, e = c == null ? void 0 : c.record;
    if (e && (c == null ? void 0 : c.column.tree) && e.isParent && e.instanceMeta(b.store).collapsed) {
      a.expand(e);
      return true;
    }
    return false;
  }
  collapseIfSingleColumn() {
    if (this.client.columns.count === 1) {
      return this.collapseByKey();
    }
    return false;
  }
  collapseByKey() {
    const a = this, {client: b} = a, {focusedCell: c} = b, e = c == null ? void 0 : c.record;
    if ((c == null ? void 0 : c.column.tree) && e) {
      if (e.isParent && !e.instanceMeta(b.store).collapsed) {
        a.collapse(e);
        return true;
      }
      if (e.parent && !e.parent.isRoot) {
        b.deselectAll();
        b.focusCell({
          'record': e.parent,
          'column': c.column
        });
        return true;
      }
    }
    return false;
  }
};
Tree.featureClass = 'b-tree';
Tree._$name = 'Tree';
GridFeatureManager.registerFeature(Tree, false, 'Grid');
GridFeatureManager.registerFeature(Tree, true, 'TreeGrid');
const TreeGroup = class extends InstancePlugin {
  static properties = {
    'isApplying': 0,
    'originalStore': null
  };
  static configurable = {
    'levels': [],
    'parentCls': 'b-generated-parent',
    'parentRenderer': null,
    'hideGroupedColumns': null,
    'refreshGroupsTimeout': 100
  };
  static $name = 'TreeGroup';
  static get pluginConfig() {
    return {
      'chain': ['populateHeaderMenu'],
      'assign': [
        'group',
        'clearGroups',
        'refreshGroups'
      ]
    };
  }
  construct(a, b) {
    this.treeColumn = a.columns.find(c => c.isTreeColumn);
    super.construct(a, b);
    this._levels = this._levels || [];
    if (!a.hasFeature('tree')) {
      throw new Error('The TreeGroup feature requires the Tree feature to be enabled');
    }
  }
  applyPluginConfig() {
    this.scheduleRefreshGroups = FunctionHelper.createBuffered(this.refreshGroups, this.refreshGroupsTimeout, this);
    return super.applyPluginConfig(...arguments);
  }
  processParentData(a) {
    const b = this;
    if (b.parentCls) {
      a.cls = b.parentCls;
    }
  }
  processTransformedData(a) {
  }
  async waitForReadiness() {
    let a;
    const b = this;
    if (b.originalStore.isLoading) {
      await b.originalStore.await('load', false);
      if (b.isDestroyed) {
        return;
      }
    }
    const {crudManager: c} = this.client;
    if (c) {
      if (c.isLoadingOrSyncing || c._autoLoadPromise) {
        await c.await('requestDone');
      }
      if (b.isDestroyed) {
        return;
      }
      await ((a = b.client.project) == null ? void 0 : a.commitAsync());
    }
  }
  async applyLevels(a) {
    let b;
    const c = this, {
        client: e,
        treeColumn: f
      } = c;
    let {store: g} = e, i = null, j;
    const {modelClass: k} = g;
    if ((f == null ? void 0 : f.field) && k.getFieldDefinition(f.field)) {
      j = k.getFieldDataSource(f.field);
    }
    a = a || [];
    if (a.length === 0 && c.isConfiguring) {
      return;
    }
    c._levels = a;
    c.isApplying++;
    e.suspendRefresh();
    if (!c.originalStore) {
      c.originalStore = g;
      g = new g.constructor({
        'reapplyFilterOnAdd': true,
        'reapplyFilterOnUpdate': true,
        'tree': true,
        'modelClass': g.modelClass,
        'load': (b = g.load) == null ? void 0 : b.bind(g),
        'commit': g.commit.bind(g),
        'remove': (l, n) => c.originalStore.remove(l.map(o => o.$original), n),
        'removeAll': g.removeAll.bind(g)
      });
      e.store = g;
      c.originalStore.ion({
        'name': 'originalStore',
        'refresh': c.onOriginalStoreRefresh,
        'add': c.onOriginalStoreChanged,
        'update': c.onOriginalStoreUpdate,
        'remove': c.onOriginalStoreChanged,
        'removeAll': c.onOriginalStoreChanged,
        'thisObj': c
      });
    }
    await c.waitForReadiness();
    if (c.isDestroyed) {
      return;
    }
    if (a.length > 0) {
      const l = c.originalStore.getAllDataRecords(false).flatMap(n => n.isLeaf ? n.link() : []);
      l.forEach(n => n.setInstanceMeta(g.id, { 'collapsed': false }));
      g.data = l;
      i = g.treeify(a, n => {
        if (j) {
          ObjectHelper.setPath(n, j, n.key);
        }
        c.processParentData(n);
      }, false, (n, o) => {
        const p = o && e.columns.get(o);
        if (p == null ? void 0 : p.formatValue) {
          n = p.formatValue(n);
        }
        return n;
      });
      c.processTransformedData(i);
      await c.trigger('beforeDataLoad', {
        'store': g,
        'data': i.children
      });
      g.data = i.children;
    } else {
      e.store = c.originalStore;
      c.detachListeners('originalStore');
      c.originalStore = null;
    }
    c.isApplying--;
    e.resumeRefresh(false);
    if (e.isPainted) {
      e.renderRows(false);
    }
    e.trigger('treeGroupChange', { 'levels': a });
  }
  doDisable(a) {
    if (a) {
      this.clearGroups();
    }
    super.doDisable(a);
  }
  onOriginalStoreUpdate({
    records: a,
    changes: b
  }) {
    if (this.levels.some(c => (c.fieldName || c) in b)) {
      this.scheduleRefreshGroups();
    }
  }
  onOriginalStoreChanged() {
    this.scheduleRefreshGroups();
  }
  onOriginalStoreRefresh({action: a}) {
    if (a === 'dataset' || a === 'filter') {
      this.scheduleRefreshGroups();
    }
  }
  updateLevels(a, b) {
    const c = this, {client: e} = c;
    if (c.hideGroupedColumns) {
      b == null ? void 0 : b.forEach(f => {
        f = f.fieldName || f;
        if (!a.some(g => (g.fieldName || g) === f)) {
          e.columns.get(f).show();
        }
      });
      a == null ? void 0 : a.forEach(f => {
        f = f.fieldName || f;
        if (!b || !b.some(g => g.fieldName === f)) {
          e._suspendRenderContentsOnColumnsChanged = true;
          e.columns.get(f).hide();
          e._suspendRenderContentsOnColumnsChanged = false;
        }
      });
    }
    if (a || !c.isConfiguring) {
      c.updatePromise = c.applyLevels(a);
      e.renderContents();
    }
  }
  async group(a) {
    ObjectHelper.assertArray(a, 'group()');
    await this.applyLevels(a);
  }
  async clearGroups() {
    if (this.isGrouped) {
      this.levels = [];
      await this.updatePromise;
    }
  }
  refreshGroups() {
    let a;
    return (a = this.applyLevels) == null ? void 0 : a.call(this, this._levels);
  }
  get isGrouped() {
    return this._levels.length > 0;
  }
  populateHeaderMenu({
    column: a,
    items: b
  }) {
    const c = this, e = a.groupable !== false && !a.isTreeColumn;
    let f = false;
    if (e && !c.isGroupedByField(a.field)) {
      b.groupAsc = {
        'text': 'L{group}',
        'localeClass': c,
        'icon': 'b-fw-icon b-icon-group-asc',
        'separator': true,
        'weight': 400,
        'disabled': c.disabled,
        'onItem': () => c.addGrouper(a)
      };
      f = true;
    }
    if (c.isGrouped) {
      if (c.isGroupedByField(a.field)) {
        b.groupRemove = {
          'text': 'L{stopGroupingThisColumn}',
          'localeClass': c,
          'icon': 'b-fw-icon b-icon-clear',
          'separator': !f,
          'weight': 420,
          'disabled': c.disabled,
          'onItem': () => c.removeGrouper(a)
        };
        f = true;
      }
      b.groupRemoveAll = {
        'text': 'L{stopGrouping}',
        'localeClass': c,
        'icon': 'b-fw-icon b-icon-clear',
        'separator': !f,
        'weight': 420,
        'disabled': c.disabled,
        'onItem': () => c.clearGroups()
      };
    }
  }
  addGrouper(a) {
    this.levels = this.levels.concat(a.field);
  }
  isGroupedByField(a) {
    return this.levels.find(b => b.fieldName === a);
  }
  removeGrouper(a) {
    this.levels.splice(this.levels.findIndex(b => b.fieldName === a.field), 1);
    this.levels = this.levels.slice();
  }
};
TreeGroup._$name = 'TreeGroup';
GridFeatureManager.registerFeature(TreeGroup);
const RowsRange = {
  'all': 'all',
  'visible': 'visible'
};
const PaperFormat = {
  'A6': {
    'width': 4.11,
    'height': 5.81
  },
  'A5': {
    'width': 5.81,
    'height': 8.25
  },
  'A4': {
    'width': 8.25,
    'height': 11.69,
    'portraitWidth': 8.3,
    'portraitHeight': 11.7,
    'landscapeWidth': 11.7,
    'landscapeHeight': 8.26
  },
  'A3': {
    'width': 11.69,
    'height': 16.49,
    'portraitWidth': 11.7,
    'portraitHeight': 16.49,
    'landscapeWidth': 16.54,
    'landscapeHeight': 11.68
  },
  'Legal': {
    'width': 8.5,
    'height': 14,
    'portraitWidth': 8.5,
    'portraitHeight': 14,
    'landscapeWidth': 14,
    'landscapeHeight': 8.15
  },
  'Letter': {
    'width': 8.5,
    'height': 11,
    'portraitWidth': 8.5,
    'portraitHeight': 10.95,
    'landscapeWidth': 11,
    'landscapeHeight': 8.3
  }
};
function getPrintPaperSizeAdjustments() {
  if (BrowserHelper.isFirefox) {
    return {
      'portraitWidth': 0,
      'portraitHeight': 0,
      'landscapeWidth': 0,
      'landscapeHeight': 0.26
    };
  }
  if (BrowserHelper.isSafari) {
    return {
      'portraitWidth': 0,
      'portraitHeight': 0.1,
      'landscapeWidth': 0,
      'landscapeHeight': 0.5
    };
  }
  return {
    'portraitWidth': 0,
    'portraitHeight': 0,
    'landscapeWidth': 0,
    'landscapeHeight': 0.25
  };
}
const Orientation = {
  'portrait': 'portrait',
  'landscape': 'landscape'
};
const FileFormat = {
  'pdf': 'pdf',
  'png': 'png'
};
const FileMIMEType = {
  'pdf': 'application/pdf',
  'png': 'image/png'
};
const ExportRowsCombo = class extends Combo {
  static get $name() {
    return 'ExportRowsCombo';
  }
  static get type() {
    return 'exportrowscombo';
  }
  static get defaultConfig() {
    return { 'editable': false };
  }
  buildItems() {
    const a = this;
    return [
      {
        'id': RowsRange.all,
        'text': a.L('L{all}')
      },
      {
        'id': RowsRange.visible,
        'text': a.L('L{visible}')
      }
    ];
  }
};
ExportRowsCombo.initClass();
ExportRowsCombo._$name = 'ExportRowsCombo';
const ExportOrientationCombo = class extends Combo {
  static get $name() {
    return 'ExportOrientationCombo';
  }
  static get type() {
    return 'exportorientationcombo';
  }
  static get defaultConfig() {
    return { 'editable': false };
  }
  buildItems() {
    const a = this;
    return [
      {
        'id': Orientation.portrait,
        'text': a.L('L{portrait}')
      },
      {
        'id': Orientation.landscape,
        'text': a.L('L{landscape}')
      }
    ];
  }
};
ExportOrientationCombo.initClass();
ExportOrientationCombo._$name = 'ExportOrientationCombo';
function buildComboItems(a, b = c => c) {
  return Object.keys(a).map(c => ({
    'id': c,
    'text': b(c)
  }));
}
const ExportDialog = class extends Popup {
  static get $name() {
    return 'ExportDialog';
  }
  static get type() {
    return 'exportdialog';
  }
  static get configurable() {
    return {
      'autoShow': false,
      'autoClose': false,
      'closable': true,
      'centered': true,
      'client': null,
      'autoSelectVisibleColumns': true,
      'hidePNGMultipageOption': true,
      'useBrowserPrint': false,
      'title': 'L{exportSettings}',
      'maxHeight': '80%',
      'scrollable': { 'overflowY': true },
      'defaults': { 'localeClass': this },
      'items': {
        'columnsField': {
          'type': 'combo',
          'label': 'L{ExportDialog.columns}',
          'name': 'columns',
          'store': {},
          'valueField': 'id',
          'displayField': 'text',
          'multiSelect': true,
          'weight': 100,
          'maxHeight': 100
        },
        'rowsRangeField': {
          'type': 'exportrowscombo',
          'label': 'L{ExportDialog.rows}',
          'name': 'rowsRange',
          'value': 'all',
          'weight': 200
        },
        'exporterTypeField': {
          'type': 'combo',
          'label': 'L{ExportDialog.exporterType}',
          'name': 'exporterType',
          'editable': false,
          'value': 'singlepage',
          'displayField': 'text',
          'buildItems'() {
            const a = this.parent;
            return a.exporters.map(b => ({
              'id': b.type,
              'text': a.optionalL(b.title, this)
            }));
          },
          'onChange'({value: a}) {
            this.owner.widgetMap.alignRowsField.hidden = a.match(/singlepage/) || a.match(/^vertical/);
            this.owner.widgetMap.repeatHeaderField.hidden = !a.match(/multipagevertical/);
          },
          'weight': 300
        },
        'alignRowsField': {
          'type': 'checkbox',
          'label': 'L{ExportDialog.alignRows}',
          'name': 'alignRows',
          'checked': false,
          'hidden': true,
          'weight': 400
        },
        'repeatHeaderField': {
          'type': 'checkbox',
          'label': 'L{ExportDialog.repeatHeader}',
          'name': 'repeatHeader',
          'localeClass': this,
          'hidden': true,
          'weight': 500
        },
        'fileFormatField': {
          'type': 'combo',
          'label': 'L{ExportDialog.fileFormat}',
          'name': 'fileFormat',
          'localeClass': this,
          'editable': false,
          'value': 'pdf',
          'items': [],
          'onChange'({
            value: a,
            oldValue: b
          }) {
            const c = this.parent;
            if (c.hidePNGMultipageOption) {
              const e = c.widgetMap.exporterTypeField, f = e.store.find(g => g.id === 'singlepage');
              if (a === FileFormat.png && f) {
                this._previousDisabled = e.disabled;
                e.disabled = true;
                this._previousValue = e.value;
                e.value = 'singlepage';
              } else if (b === FileFormat.png && this._previousValue) {
                e.disabled = this._previousDisabled;
                e.value = this._previousValue;
              }
            }
          },
          'weight': 600
        },
        'paperFormatField': {
          'type': 'combo',
          'label': 'L{ExportDialog.paperFormat}',
          'name': 'paperFormat',
          'editable': false,
          'value': 'A4',
          'items': [],
          'weight': 700
        },
        'orientationField': {
          'type': 'exportorientationcombo',
          'label': 'L{ExportDialog.orientation}',
          'name': 'orientation',
          'value': 'portrait',
          'weight': 800
        }
      },
      'bbar': {
        'defaults': { 'localeClass': this },
        'items': {
          'exportButton': {
            'color': 'b-green',
            'text': 'L{ExportDialog.export}',
            'weight': 100,
            'onClick': 'up.onExportClick'
          },
          'cancelButton': {
            'color': 'b-gray',
            'text': 'L{ExportDialog.cancel}',
            'weight': 200,
            'onClick': 'up.onCancelClick'
          }
        }
      },
      'internalListeners': { 'beforeShow': 'onInternalBeforeShow' }
    };
  }
  construct(a = {}) {
    const b = this, {client: c} = a;
    if (!c) {
      throw new Error('`client` config is required');
    }
    b.columnsStore = c.columns.chain(e => e.isLeaf && e.exportable, null, { 'excludeCollapsedRecords': false });
    b.applyInitialValues(a);
    super.construct(a);
    LocaleManager_default.ion({
      'locale': 'onLocaleChange',
      'prio': -1,
      'thisObj': b
    });
  }
  updateUseBrowserPrint(a) {
    const b = this;
    b.whenVisible(() => {
      const {
          exportButton: c,
          fileFormatField: e
        } = b.widgetMap, f = c.initialConfig.text, g = !('title' in b.initialConfig);
      if (!f || f === 'L{ExportDialog.export}' && a) {
        c.text = a ? b.L('L{ExportDialog.print}') : b.L('L{ExportDialog.export}');
      }
      if (g) {
        b.title = a ? b.L('L{printSettings}') : b.L('L{exportSettings}');
      }
      e.hidden = a || Boolean(e.initialConfig.hidden);
    });
  }
  applyInitialValues(a) {
    const b = this, c = a.items = a.items || {};
    a.width = a.width || b.L('L{width}');
    a.defaults = a.defaults || {};
    a.defaults.labelWidth = a.defaults.labelWidth || b.L('L{ExportDialog.labelWidth}');
    c.columnsField = c.columnsField || {};
    c.fileFormatField = c.fileFormatField || {};
    c.paperFormatField = c.paperFormatField || {};
    c.fileFormatField.items = buildComboItems(FileFormat, e => e.toUpperCase());
    c.paperFormatField.items = buildComboItems(PaperFormat);
    c.columnsField.store = b.columnsStore;
  }
  onInternalBeforeShow() {
    const {
      columnsField: a,
      alignRowsField: b,
      exporterTypeField: c,
      repeatHeaderField: e
    } = this.widgetMap;
    if (this.autoSelectVisibleColumns) {
      a.value = this.columnsStore.query(g => !g.hidden);
    }
    const f = c.value;
    b.hidden = f.match(/singlepage/) || f.match(/^vertical/);
    e.hidden = !f.match('multipagevertical');
  }
  onLocaleChange() {
    const a = this.L('L{labelWidth}');
    this.width = this.L('L{width}');
    this.eachWidget(b => {
      if (b instanceof Field) {
        b.labelWidth = a;
      }
    });
  }
  onExportClick() {
    const a = this.values;
    this.trigger('export', { 'values': a });
  }
  onCancelClick() {
    this.trigger('cancel');
    this.hide();
  }
};
ExportDialog.initClass();
ExportDialog._$name = 'ExportDialog';
const ExporterBase = class extends Delayable_default(Events_default(Localizable_default(Base))) {
  static get defaultConfig() {
    return {
      'translateURLsToAbsolute': true,
      'keepPathName': true,
      'filterStyles': a => a
    };
  }
  static inchToPx(a) {
    return a * 96;
  }
  pageTpl(a) {
    const {
      title: b,
      header: c,
      footer: e,
      styles: f,
      htmlClasses: g,
      bodyClasses: bodyClasses = [],
      paperHeight: i,
      paperWidth: j,
      html: k,
      currentPage: l,
      isPrint: n
    } = a;
    let {
      htmlStyle: htmlStyle = '',
      bodyStyle: bodyStyle = ''
    } = a;
    bodyClasses.push('b-' + this.constructor.type);
    if (DomHelper.scrollBarWidth) {
      bodyClasses.push('b-visible-scrollbar');
    } else {
      bodyClasses.push('b-overlay-scrollbar');
    }
    if (BrowserHelper.isChrome) {
      bodyClasses.push('b-chrome');
    } else if (BrowserHelper.isSafari) {
      bodyClasses.push('b-safari');
    } else if (BrowserHelper.isFirefox) {
      bodyClasses.push('b-firefox');
    }
    htmlStyle = n ? htmlStyle : htmlStyle + '; width: ' + j + 'in; height: ' + i + 'in;';
    bodyStyle = n ? bodyStyle + '; width: ' + j + 'in;' : bodyStyle + '; width: ' + j + 'in; height: ' + i + 'in;';
    return TemplateHelper.tpl`
            <!DOCTYPE html>
            <html class="${ g } b-print-root" style="${ htmlStyle }">
                <head>
                    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
                    <title>${ b }</title>
                    ${ f.join('') }
                </head>
                <body class="b-export ${ bodyClasses.join(' ') }" style="${ bodyStyle }">
                    <div class="b-export-content b-page-${ l }">
                        ${ c && '<div class="b-export-header" style="width: 100%">' + c + '</div>' }
                        <div class="b-export-body"><div class="b-export-viewport">${ k }</div></div>
                        ${ e && '<div class="b-export-footer" style="width: 100%">' + e + '</div>' }
                    </div>
                </body>
            </html>`;
  }
  get stylesheets() {
    const a = this;
    if (a._stylesheets) {
      return a._stylesheets;
    }
    const b = a.translateURLsToAbsolute, c = globalThis.origin, e = Array.from(document.querySelectorAll('link[rel="stylesheet"], style')), f = [];
    e.forEach(g => {
      g = g.cloneNode(true);
      if (b && g.href) {
        let j;
        if (b === true) {
          j = g.href;
        } else if (this.keepPathName) {
          j = g.href.replace(c, b);
        } else {
          j = new URL(g.getAttribute('href'), b);
        }
        g.setAttribute('href', j);
      }
      let i = g.outerHTML;
      if (b && /style/i.test(g.tagName)) {
        const k = a.getStyleTagURLConverter(b);
        i = i.replace(/url\(['"]?(.+?)['"]?\)/g, k);
      }
      f.push(i);
    });
    return a._stylesheets = a.filterStyles(f);
  }
  set stylesheets(a) {
    this._stylesheets = a ? this.filterStyles(a) : a;
  }
  getStyleTagURLConverter(a) {
    return function (b, c) {
      let f;
      try {
        let g;
        if (/^#/.test(c)) {
          f = b;
        } else {
          if (a === true) {
            g = globalThis.location.href;
          } else if (this.keepPathName) {
            g = globalThis.location.href.replace(globalThis.location.origin, a);
          } else {
            g = a;
          }
          f = 'url(\'' + new URL(c, g).href + '\')';
        }
      } catch (i) {
        f = b;
      }
      return f;
    }.bind(this);
  }
  saveState({client: a}) {
    const {disableGridColumnIdWarning: b} = a;
    a.disableGridColumnIdWarning = true;
    this.state = a.state;
    a.disableGridColumnIdWarning = b;
  }
  async restoreState({client: a}) {
    const b = [], c = new Set();
    c.add(a.scrollable.ion({
      'scroll'() {
        b.push(a.scrollable.await('scrollEnd'));
      }
    }));
    a.eachSubGrid(({
      header: e,
      scrollable: f
    }) => {
      c.add(f.ion({
        'scroll'() {
          b.push(f.await('scrollEnd'));
        }
      }));
      c.add(e.scrollable.ion({
        'scroll'() {
          b.push(e.scrollable.await('scrollEnd'));
        }
      }));
    });
    a.state = this.state;
    await Promise.all(b);
    c.forEach(e => e());
  }
  beforeExport() {
    this.element = document.createElement('div');
  }
  cloneElement(a, b = this.element, c = true) {
    if (c) {
      b.innerHTML = '';
    }
    b.appendChild(a.cloneNode(true));
    const e = b.querySelector('.b-gridbase > .b-mask');
    e == null ? void 0 : e.remove();
  }
  createPlaceholder(a, b = true, c = {}) {
    if (b) {
      a.innerHTML = '';
    }
    return DomHelper.createElement(Object.assign({
      'parent': a,
      'id': IdHelper.generateId('export')
    }, c));
  }
  prepareElement({client: a}) {
    const {
      tbar: b,
      bbar: c
    } = a;
    if (b) {
      this.element.querySelector('#' + b.id).remove();
    }
    if (c) {
      this.element.querySelector('#' + c.id).remove();
    }
  }
  measureElement(a = '', b = '') {
    if (a instanceof HTMLElement) {
      a = a.outerHTML;
    }
    const c = DomHelper.createElement({
      'parent': document.body,
      'style': {
        'visibility': 'hidden',
        'position': 'absolute'
      },
      'className': b,
      'html': '<div style="height: 1px"></div>' + a + '<div style="height: 1px"></div>'
    });
    const e = c.offsetHeight - 2;
    c.remove();
    return e;
  }
  prepareHTML(a) {
    if (a instanceof HTMLElement) {
      a = a.outerHTML;
    }
    const b = DomHelper.createElement({
        'parent': document.body,
        'style': {
          'visibility': 'hidden',
          'position': 'absolute'
        },
        'html': a
      }), c = b.querySelectorAll('img');
    for (let f = 0, g = c.length; f < g; f++) {
      c[f].setAttribute('src', c[f].src);
    }
    const e = b.innerHTML;
    b.remove();
    return e;
  }
  getVirtualScrollerHeight(a) {
    let b = 0;
    a.eachSubGrid(c => {
      if (c.overflowingHorizontally) {
        b = DomHelper.scrollBarWidth;
      }
    });
    return b === 0 ? b : b + 1;
  }
  getPaperWidth(a, b) {
    if ('portraitWidth' in a) {
      const c = b ? 'portraitWidth' : 'landscapeWidth';
      return a[c] - (this.exportMeta.isPrinting ? getPrintPaperSizeAdjustments()[c] : 0);
    } else {
      return b ? a.width : a.height;
    }
  }
  getPaperHeight(a, b) {
    if ('portraitHeight' in a) {
      const c = b ? 'portraitHeight' : 'landscapeHeight';
      return a[c] - (this.exportMeta.isPrinting ? getPrintPaperSizeAdjustments()[c] : 0);
    } else {
      return b ? a.height : a.width;
    }
  }
  inchToPx(a) {
    return a * 96;
  }
  getScaleValue(a, b) {
    return Math.floor(a * 10000 / b) / 10000;
  }
  async export(a) {
    const b = this;
    let c;
    b.beforeExport();
    b.saveState(a);
    await b.prepareComponent(a);
    try {
      c = await b.getPages(a);
    } finally {
      await b.restoreComponent(a);
      b.stylesheets = null;
      await AsyncHelper.animationFrame();
      await b.restoreState(a);
    }
    return c;
  }
  async getPages(a) {
    const b = this.pagesExtractor(a), c = [];
    let e;
    while ((e = await b.next()) && !e.done) {
      c.push(e.value);
    }
    return c;
  }
  prepareExportElement() {
    const a = this, {
        element: b,
        exportMeta: c
      } = a;
    Object.values(c.subGrids).forEach(({
      width: e,
      id: f,
      headerId: g,
      footerId: i
    }) => {
      [
        f,
        g,
        i
      ].forEach(j => {
        if (j) {
          const k = b.querySelector('[id="' + j + '"]');
          if (k) {
            k.style.width = e + 'px';
            k.style.flex = '';
          }
        }
      });
    });
    return b.innerHTML;
  }
  async prepareComponent(a) {
    const b = this, {
        client: c,
        columns: e,
        keepRegionSizes: f,
        useRenderedColumnWidth: g
      } = a, {rowManager: i} = c, j = b.exportMeta = {
        'totalWidth': 0,
        'totalHeight': 0,
        'subGrids': {},
        'isPrinting': a.useBrowserPrint,
        'title': a.title || c.description
      }, k = PaperFormat[a.paperFormat], l = a.orientation === Orientation.portrait, n = b.getPaperWidth(k, l), o = b.getPaperHeight(k, l), p = b.inchToPx(n), q = b.inchToPx(o);
    Object.assign(j, {
      'paperWidth': n,
      'paperHeight': o,
      'realPaperWidth': b.getPaperWidth(k, l),
      'realPaperHeight': b.getPaperHeight(k, l),
      'pageWidth': p,
      'pageHeight': q
    });
    c.columns.forEach(u => {
      if (e.includes(u.id)) {
        u.show();
      } else {
        u.hide();
      }
    });
    await AsyncHelper.animationFrame();
    const {element: r} = b;
    b.cloneElement(c.element);
    b.prepareElement(a);
    c.eachSubGrid(u => {
      let v, w, z;
      const A = b.createPlaceholder(r.querySelector('[id="' + u.id + '"]'), false);
      let B;
      if (f == null ? void 0 : f[u.region]) {
        B = u.element.offsetWidth;
      } else {
        const {visibleColumns: E} = u.columns;
        if (E.length === 1 && !E[0].isTimeAxisColumn && b.state.columns.filter(F => F.region === u.region && !F.hidden && F.id !== E[0].id).length === 0) {
          B = u.element.offsetWidth;
        } else {
          B = u.columns.visibleColumns.reduce((F, G) => {
            if (!g && typeof G.width === 'number') {
              F += G.width;
            } else {
              F += (c.hideHeaders ? i.rows[0].getCell(G.id) : G.element).offsetWidth;
            }
            return F;
          }, 0);
        }
      }
      j.totalWidth += B;
      const C = ((v = u.splitterElement) == null ? void 0 : v.offsetWidth) || 0;
      j.totalWidth += C;
      j.subGrids[u.region] = {
        'id': u.id,
        'headerId': ((w = u.header) == null ? void 0 : w.id) || null,
        'footerId': ((z = u.footer) == null ? void 0 : z.id) || null,
        'rows': [],
        'splitterWidth': C,
        'placeHolder': A,
        'width': B
      };
    });
  }
  async restoreComponent(a) {
  }
  collectRow(a) {
    const b = this.exportMeta.subGrids, c = /data-owner-cmp=".+?"/;
    Object.entries(a.elements).forEach(([e, f]) => {
      f.classList.remove('b-export-row');
      b[e].rows.push(f.outerHTML.replace(c, ''));
      f.classList.add('b-export-row');
    });
  }
};
ExporterBase.prototype.pagesExtractor = async function* pagesExtractor() {
  throw new Error('Implement this method in a subclass');
};
ExporterBase._$name = 'ExporterBase';
const Exporter = class extends ExporterBase {
  cloneElement(a, b = this.element, c = true) {
    super.cloneElement(a, b, c);
    DomHelper.removeEachSelector(b, '.b-grid-row,.b-grid-merged-cells-container');
  }
  getVisibleRowsCount(a) {
    const b = a.rowManager, c = b.rows.indexOf(b.firstVisibleRow), e = b.rows.indexOf(b.lastVisibleRow);
    let f;
    if (c !== -1) {
      if (e === -1) {
        f = a.store.count - c;
      } else {
        f = e - c + 1;
      }
    } else {
      f = a.store.count;
    }
    return f;
  }
  async prepareComponent(a) {
    const {client: b} = a, {rowManager: c} = b;
    await super.prepareComponent(a);
    const e = this.exportMeta.fakeRow = c.rowClass.new({
      'cls': {
        [b.rowCls]: 1,
        'b-export-row': 1
      },
      'rowManager': c,
      'grid': b,
      'index': -10,
      'dataIndex': -10
    });
    b.eachSubGrid(f => {
      f.onAddRow({ 'rows': [e] });
      e.element.dataset.ownerCmp = b.id;
    });
  }
  async restoreComponent(a) {
    await super.restoreComponent(a);
    if (this.exportMeta.fakeRow) {
      this.exportMeta.fakeRow.destroy();
      delete this.exportMeta.fakeRow;
    }
  }
  renderMergedCells(a, b, c, e) {
    const f = this, {client: g} = a, {subGrids: i} = f.exportMeta, j = g.features.mergeCells.buildMergedCellsConfig(b, c, e);
    for (const k in i) {
      const l = i[k], n = document.createElement('div');
      DomSync.sync({
        'targetElement': n,
        'domConfig': { 'children': j[k].children }
      });
      if (n.childNodes.length) {
        n.childNodes.forEach(o => {
          const {syncId: p} = o.dataset, q = g.features.mergeCells.mergedRanges.find(r => {
              let u;
              return ((u = r.cellElement) == null ? void 0 : u.parentNode.dataset.syncId) === p;
            });
          if (q) {
            o.innerHTML = q.cellElement.outerHTML;
          }
        });
        l.mergedCellsHtml = [n.innerHTML];
      }
    }
  }
};
Exporter._$name = 'Exporter';
const MultiPageExporter = class extends Exporter {
  static get $name() {
    return 'MultiPageExporter';
  }
  static get type() {
    return 'multipage';
  }
  static get title() {
    return this.L('L{multipage}');
  }
  static get exportingPageText() {
    return 'L{exportingPage}';
  }
  async stateNextPage({
    client: a,
    rowsRange: b
  }) {
    const {exportMeta: c} = this;
    ++c.currentPage;
    ++c.verticalPosition;
    if (c.lastRowIndex >= c.finishRowIndex) {
      c.verticalPages = c.verticalPosition;
      c.totalPages = c.verticalPages * c.horizontalPages;
    } else {
      c.verticalPages = Math.max(c.verticalPages, c.verticalPosition + 1);
    }
    delete c.lastExportedRowBottom;
    if (c.verticalPosition >= c.verticalPages && c.horizontalPosition < c.horizontalPages - 1) {
      Object.assign(c, {
        'verticalPosition': 0,
        'horizontalPosition': c.horizontalPosition + 1,
        'lastTop': 0,
        'lastRowIndex': b === RowsRange.visible ? a.rowManager.firstVisibleRow.dataIndex : 0
      });
    }
  }
  async prepareComponent(a) {
    await super.prepareComponent(a);
    const b = this, {exportMeta: c} = b, {
        client: e,
        headerTpl: f,
        footerTpl: g,
        alignRows: i,
        rowsRange: j
      } = a, k = PaperFormat[a.paperFormat], l = a.orientation === Orientation.portrait, n = b.getPaperWidth(k, l), o = b.getPaperHeight(k, l), p = b.inchToPx(n), q = b.inchToPx(o), r = j === RowsRange.visible, u = Math.ceil(c.totalWidth / p);
    let v = q;
    if (f) {
      v -= b.measureElement(f({
        'totalWidth': c.totalWidth,
        'totalPages': -1,
        'currentPage': -1
      }), 'b-export-header');
    }
    if (g) {
      v -= b.measureElement(g({
        'totalWidth': c.totalWidth,
        'totalPages': -1,
        'currentPage': -1
      }), 'b-export-footer');
    }
    let w, z, A = e.store.count;
    if (r) {
      A = b.getVisibleRowsCount(e);
      w = c.totalHeight + e.headerHeight + e.footerHeight + e.bodyHeight;
      c.finishRowIndex = e.rowManager.lastVisibleRow.dataIndex;
    } else {
      w = c.totalHeight + e.headerHeight + e.footerHeight + e.scrollable.scrollHeight;
      c.finishRowIndex = e.store.count - 1;
    }
    if (i && !r) {
      const B = e.rowManager.rowOffsetHeight, C = Math.floor((v - e.headerHeight) / B), E = Math.floor(v / B), F = A - C;
      z = 1 + Math.ceil(F / E);
    } else {
      z = Math.ceil(w / v);
    }
    Object.assign(c, {
      'paperWidth': n,
      'paperHeight': o,
      'pageWidth': p,
      'pageHeight': q,
      'horizontalPages': u,
      'verticalPages': z,
      'totalHeight': w,
      'contentHeight': v,
      'totalRows': A,
      'totalPages': u * z,
      'currentPage': 0,
      'verticalPosition': 0,
      'horizontalPosition': 0,
      'lastTop': 0,
      'lastRowIndex': r ? e.rowManager.firstVisibleRow.dataIndex : 0
    });
  }
  async renderRows(a) {
    const b = this, {exportMeta: c} = b, {
        client: e,
        alignRows: f,
        repeatHeader: g
      } = a, {
        verticalPosition: i,
        contentHeight: j,
        lastRowIndex: k,
        fakeRow: l
      } = c, n = [], o = g ? 0 : e.headerHeight, {store: p} = e, q = e.hasActiveFeature('mergeCells');
    let r = k + (!f || i === 0 ? 0 : 1), u = 0, v = j, w;
    if (i === 0) {
      v -= o;
    }
    while (v > 0) {
      l.render(r, p.getAt(r), true, false, true);
      if (f && v < l.offsetHeight) {
        break;
      } else {
        w = u;
        c.lastExportedRowBottom = u = l.translate(u);
        c.lastRowIndex = l.dataIndex;
        v -= l.offsetHeight;
        b.collectRow(l);
        r++;
        n.push({
          'top': l.top,
          'bottom': l.bottom,
          'offsetHeight': l.offsetHeight,
          'dataIndex': l.dataIndex
        });
        if (l.dataIndex === c.finishRowIndex) {
          if (v >= 0) {
            break;
          } else {
            if (l.offsetHeight > j) {
              c.lastRowIndex--;
            } else {
              c.lastRowIndex--;
            }
          }
        }
      }
    }
    if (q) {
      b.renderMergedCells(a, k, r, n);
    }
    c.lastTop = f ? u : w;
    c.exactGridHeight = l.bottom + e.footerContainer.offsetHeight + (i === 0 || g ? o : 0);
    await b.onRowsCollected(n, a);
  }
  async buildPage(a) {
    const b = this, {exportMeta: c} = b, {
        client: e,
        headerTpl: f,
        footerTpl: g
      } = a, {
        totalWidth: i,
        totalPages: j,
        currentPage: k,
        subGrids: l
      } = c;
    Object.values(l).forEach(q => q.rows = []);
    if (a.rowsRange === RowsRange.all) {
      c.totalHeight = e.height - e.bodyHeight + e.scrollable.scrollHeight - b.getVirtualScrollerHeight(e);
    }
    let n, o;
    if (f) {
      n = b.prepareHTML(f({
        'totalWidth': i,
        'totalPages': j,
        'currentPage': k
      }));
    }
    if (g) {
      o = b.prepareHTML(g({
        'totalWidth': i,
        'totalPages': j,
        'currentPage': k
      }));
    }
    await b.renderRows(a);
    const p = b.buildPageHtml(a);
    return {
      'html': p,
      'header': n,
      'footer': o
    };
  }
  async onRowsCollected() {
  }
  buildPageHtml() {
    const a = this, {subGrids: b} = a.exportMeta;
    let c = a.prepareExportElement();
    Object.values(b).forEach(({
      placeHolder: e,
      rows: f,
      mergedCellsHtml: g
    }) => {
      const i = e.outerHTML;
      let j = f.join('');
      if (g == null ? void 0 : g.length) {
        j += '<div class="b-grid-merged-cells-container">' + g.join('') + '</div>';
      }
      c = c.replace(i, j);
    });
    return c;
  }
};
MultiPageExporter.prototype.pagesExtractor = async function* pagesExtractor2(a) {
  const b = this, {
      exportMeta: c,
      stylesheets: e
    } = b, {
      totalWidth: f,
      totalPages: g,
      paperWidth: i,
      paperHeight: j,
      realPaperWidth: k,
      realPaperHeight: l,
      title: n
    } = c, o = a.useBrowserPrint;
  let p;
  while (c.lastRowIndex < c.finishRowIndex) {
    p = c.currentPage;
    b.trigger('exportStep', {
      'text': b.L(MultiPageExporter.exportingPageText, {
        'currentPage': p,
        'totalPages': g
      }),
      'progress': Math.round((p + 1) / g * 90)
    });
    const {
      html: q,
      header: r,
      footer: u
    } = await b.buildPage(a);
    const v = [
      ...e,
      '\n                <style>\n                    .b-page-wrap {\n                        width: ' + i + 'in;\n                        height: ' + j + 'in;\n                    }\n                    .b-page-' + p + ' #' + a.client.id + ' {\n                        height: ' + c.exactGridHeight + 'px !important;\n                        width: ' + f + 'px !important;\n                    }\n                    .b-page-' + p + ' .b-export-body .b-export-viewport {\n                        transform: translate(' + -i * c.horizontalPosition + 'in, 0);\n                    }\n                    ' + (c.verticalPosition === 0 ? '' : '.b-page-' + p + ' header.b-grid-header-container {\n                        display: none;\n                    }') + '\n                </style>\n            '
    ];
    await b.stateNextPage(a);
    yield {
      'html': b.pageTpl({
        'html': q,
        'title': n,
        'header': r,
        'footer': u,
        'styles': v,
        'paperWidth': i,
        'paperHeight': j,
        'realPaperWidth': k,
        'realPaperHeight': l,
        'currentPage': p,
        'isPrint': o
      })
    };
  }
};
MultiPageExporter._$name = 'MultiPageExporter';
const MultiPageVerticalExporter = class extends Exporter {
  static get $name() {
    return 'MultiPageVerticalExporter';
  }
  static get type() {
    return 'multipagevertical';
  }
  static get title() {
    return this.L('L{multipagevertical}');
  }
  static get exportingPageText() {
    return 'L{exportingPage}';
  }
  async stateNextPage() {
    const {exportMeta: a} = this, {
        totalRows: b,
        processedRows: c,
        totalPages: e
      } = a;
    ++a.currentPage;
    ++a.verticalPosition;
    if (a.currentPage === e && c.size !== b) {
      ++a.totalPages;
      ++a.verticalPages;
    }
  }
  calculateScale() {
    const a = this, {exportMeta: b} = a, {
        pageWidth: c,
        totalWidth: e
      } = b, f = a.getScaleValue(c, e);
    a.exportMeta.scale = f;
    return f;
  }
  estimateTotalPages(a) {
    const b = this, {exportMeta: c} = b, {
        client: e,
        headerTpl: f,
        footerTpl: g,
        alignRows: i,
        rowsRange: j,
        repeatHeader: k
      } = a, {
        pageWidth: l,
        pageHeight: n,
        totalWidth: o
      } = c, p = b.calculateScale(l, o);
    let q = e.store.count, r = q ? 0 - b.getVirtualScrollerHeight(e) + e.height - e.bodyElement.offsetHeight + e.scrollable.scrollHeight : 0, u = n / p, v = r, w;
    if (f) {
      u -= b.measureElement(f({
        'totalWidth': o,
        'totalPages': -1,
        'currentPage': -1
      }), 'b-export-header');
    }
    if (g) {
      u -= b.measureElement(g({
        'totalWidth': o,
        'totalPages': -1,
        'currentPage': -1
      }), 'b-export-footer');
    }
    if (k) {
      u -= e.headerHeight + e.footerHeight;
      r -= e.headerHeight + e.footerHeight;
    }
    if (j === RowsRange.visible) {
      const z = e.rowManager, A = z.firstVisibleRow, B = z.lastVisibleRow;
      q = b.getVisibleRowsCount(e);
      v = B.bottom - A.top;
      r = e.headerHeight + e.footerHeight + v;
      c.lastRowIndex = A.dataIndex;
      c.finishRowIndex = B.dataIndex;
    } else {
      c.finishRowIndex = e.store.count - 1;
    }
    if (i && !k && j !== RowsRange.visible) {
      const C = e.rowManager.rowOffsetHeight, E = Math.floor((u - e.headerHeight) / C), F = Math.floor(u / C), G = q - E;
      w = 1 + Math.ceil(G / F);
    } else {
      w = Math.ceil(v / u);
    }
    Object.assign(c, {
      'scale': p,
      'contentHeight': u,
      'totalRows': q,
      'rowsHeight': v,
      'totalHeight': r,
      'verticalPages': w,
      'initialScroll': 0,
      'actualRowsHeight': 0,
      'horizontalPages': 1,
      'totalPages': w
    });
  }
  async prepareComponent(a) {
    await super.prepareComponent(a);
    const b = this, {exportMeta: c} = b, e = PaperFormat[a.paperFormat], f = a.orientation === Orientation.portrait, g = b.getPaperWidth(e, f), i = b.getPaperHeight(e, f), j = b.inchToPx(g), k = b.inchToPx(i), l = 1;
    Object.assign(c, {
      'paperWidth': g,
      'paperHeight': i,
      'realPaperWidth': b.getPaperWidth(e, f),
      'realPaperHeight': b.getPaperHeight(e, f),
      'pageWidth': j,
      'pageHeight': k,
      'horizontalPages': l,
      'currentPage': 0,
      'verticalPosition': 0,
      'horizontalPosition': 0,
      'lastTop': 0,
      'lastRowIndex': -1,
      'processedRows': new Set()
    });
    b.estimateTotalPages(a);
  }
  async renderRows(a) {
    const b = this, {exportMeta: c} = b, {
        client: e,
        alignRows: f,
        repeatHeader: g
      } = a, {
        verticalPosition: i,
        contentHeight: j,
        lastRowIndex: k,
        fakeRow: l
      } = c, n = [], o = g || i > 0 ? 0 : e.headerHeight, {store: p} = e, q = e.hasActiveFeature('mergeCells');
    let r = Math.max(0, k) + (!f || i === 0 ? 0 : 1), u = j, v = 0, w, z;
    if (i === 0) {
      u -= o;
    }
    while (u > 0) {
      l.render(r, p.getAt(r), true, false, true);
      const {offsetHeight: A} = l;
      if (f && u < A) {
        break;
      } else {
        z = v;
        v = l.translate(v);
        u -= A;
        c.actualRowsHeight += A;
        w = l.bottom;
        c.lastRowIndex = l.dataIndex;
        b.collectRow(l);
        n.push({
          'top': l.top,
          'bottom': l.bottom,
          'dataIndex': l.dataIndex,
          'offsetHeight': A
        });
        if (u > 0) {
          c.processedRows.add(r);
        }
        r++;
        if (l.dataIndex === c.finishRowIndex) {
          if (u >= 0) {
            break;
          } else {
            if (l.offsetHeight > j) {
              c.lastRowIndex--;
            } else {
              c.lastRowIndex--;
            }
          }
          c.verticalPages = c.verticalPosition;
          c.totalPages = c.verticalPages & c.horizontalPages;
          c.totalHeight = c.totalHeight - c.rowsHeight + c.actualRowsHeight;
          c.rowsHeight = c.actualRowsHeight;
          b.calculateScale();
        }
      }
    }
    if (q) {
      b.renderMergedCells(a, k, r, n);
    }
    c.lastTop = z;
    c.exactGridHeight = w + e.footerContainer.offsetHeight + (i === 0 || g ? e.headerHeight : 0);
    await b.onRowsCollected(n, a);
  }
  async buildPage(a) {
    const b = this, {exportMeta: c} = b, {
        headerTpl: e,
        footerTpl: f
      } = a, {
        totalWidth: g,
        totalPages: i,
        currentPage: j,
        subGrids: k
      } = c;
    Object.values(k).forEach(p => p.rows = []);
    let l, n;
    if (e) {
      l = b.prepareHTML(e({
        'totalWidth': g,
        'totalPages': i,
        'currentPage': j
      }));
    }
    if (f) {
      n = b.prepareHTML(f({
        'totalWidth': g,
        'totalPages': i,
        'currentPage': j
      }));
    }
    await b.renderRows(a);
    const o = b.buildPageHtml(a);
    return {
      'html': o,
      'header': l,
      'footer': n
    };
  }
  async onRowsCollected() {
  }
  buildPageHtml() {
    const a = this, {subGrids: b} = a.exportMeta;
    let c = a.prepareExportElement();
    Object.values(b).forEach(({
      placeHolder: e,
      rows: f,
      mergedCellsHtml: g
    }) => {
      const i = e.outerHTML;
      let j = f.join('');
      if (g == null ? void 0 : g.length) {
        j += '<div class="b-grid-merged-cells-container">' + g.join('') + '</div>';
      }
      c = c.replace(i, j);
    });
    return c;
  }
};
MultiPageVerticalExporter.prototype.pagesExtractor = async function* pagesExtractor3(a) {
  const b = this, {
      exportMeta: c,
      stylesheets: e
    } = b, {
      totalWidth: f,
      paperWidth: g,
      paperHeight: i,
      realPaperWidth: j,
      realPaperHeight: k,
      title: l
    } = c, n = a.useBrowserPrint;
  let {totalPages: o} = c, p, q;
  while (c.lastRowIndex < c.finishRowIndex) {
    p = c.currentPage;
    b.trigger('exportStep', {
      'text': b.L(MultiPageVerticalExporter.exportingPageText, {
        'currentPage': p,
        'totalPages': o
      }),
      'progress': Math.round((p + 1) / o * 90)
    });
    const {
      html: r,
      header: u,
      footer: v
    } = await b.buildPage(a);
    const {scale: w} = b.exportMeta;
    q = '\n            ' + (n ? '\n                        .b-page-wrap {\n                            width: ' + j + 'in;\n                            height: ' + k + 'in;\n                        }\n                        .b-print:not(.b-firefox) .b-export-content {\n                            zoom: ' + w + ';\n                            height: 100%;\n                        }\n                        .b-print.b-firefox .b-export-content {\n                            transform: scale(' + w + ');\n                            transform-origin: top left;\n                            height: ' + 100 / w + '%;\n                            width: ' + 100 / w + '%;\n                        }\n                    ' : '\n                        .b-export .b-page-' + p + '.b-export-content {\n                            transform: scale(' + w + ');\n                            transform-origin: top left;\n                            height: ' + 100 / w + '%;\n                            width: ' + 100 / w + '%;\n                        }\n                    ') + '\n        ';
    if (a.repeatHeader) {
      q = '\n                ' + q + '\n                .b-page-' + p + ' #' + a.client.id + ' {\n                    height: ' + c.exactGridHeight + 'px !important;\n                    width: ' + f + 'px !important;\n                }\n                .b-export-body {\n                    height: 100%;\n                    display: flex;\n                }\n                .b-export-viewport {\n                    height: 100%;\n                }\n            ';
    } else {
      q = '\n                ' + q + '\n                .b-page-' + p + ' #' + a.client.id + ' {\n                    height: ' + c.exactGridHeight + 'px !important;\n                    width: ' + f + 'px !important;\n                }\n                ' + (p === 0 ? '' : '.b-page-' + p + ' header.b-grid-header-container {\n                    display: none;\n                }') + '\n                .b-export-body {\n                    overflow: hidden;\n                }\n            ';
    }
    const z = [
      ...e,
      '<style>' + q + '</style>'
    ];
    await b.stateNextPage();
    ({totalPages: o} = c);
    yield {
      'html': b.pageTpl({
        'html': r,
        'title': l,
        'header': u,
        'footer': v,
        'styles': z,
        'paperWidth': g,
        'paperHeight': i,
        'realPaperWidth': j,
        'realPaperHeight': k,
        'currentPage': p,
        'isPrint': n
      })
    };
  }
};
MultiPageVerticalExporter._$name = 'MultiPageVerticalExporter';
const SinglePageExporter = class extends Exporter {
  static get $name() {
    return 'SinglePageExporter';
  }
  static get type() {
    return 'singlepage';
  }
  static get title() {
    return this.localize('L{singlepage}');
  }
  static get defaultConfig() {
    return { 'centerContentHorizontally': false };
  }
  async prepareComponent(a) {
    await super.prepareComponent(a);
    Object.assign(this.exportMeta, {
      'verticalPages': 1,
      'horizontalPages': 1,
      'totalPages': 1,
      'currentPage': 0,
      'verticalPosition': 0,
      'horizontalPosition': 0
    });
  }
  async onRowsCollected() {
  }
  async renderRows(a) {
    const b = this, {
        client: c,
        rowsRange: e
      } = a, {
        rowManager: f,
        store: g
      } = c, j = c.hasActiveFeature('mergeCells'), k = e === RowsRange.visible;
    let {totalHeight: l} = b.exportMeta;
    if (g.count) {
      const {fakeRow: n} = b.exportMeta, {firstVisibleRow: o} = f, p = k ? o.dataIndex : 0, q = k ? f.lastVisibleRow.dataIndex : g.count - 1, r = [];
      let u = 0;
      if (n.cells.length) {
        for (let v = p; v <= q; v++) {
          n.render(v, g.getAt(v), true, false, true);
          u = n.translate(u);
          b.collectRow(n);
          r.push({
            'top': n.top,
            'bottom': n.bottom,
            'offsetHeight': n.offsetHeight,
            'dataIndex': n.dataIndex
          });
        }
        await b.onRowsCollected(r, a);
      }
      l += u;
      if (j) {
        b.renderMergedCells(a, p, q, r);
      }
    }
    b.exportMeta.exactGridHeight = l;
    return l;
  }
  buildPageHtml(a) {
    const b = this, {subGrids: c} = b.exportMeta;
    let e = b.prepareExportElement();
    Object.values(c).forEach(({
      placeHolder: f,
      rows: g,
      mergedCellsHtml: i
    }) => {
      const j = f.outerHTML;
      let k = g.join('');
      if (i == null ? void 0 : i.length) {
        k += '<div class="b-grid-merged-cells-container">' + i.join('') + '</div>';
      }
      e = e.replace(j, k);
    });
    return e;
  }
};
SinglePageExporter.prototype.pagesExtractor = async function* pagesExtractor4(a) {
  const b = this, {client: c} = a, {totalWidth: e} = b.exportMeta, f = b.stylesheets, g = a.orientation === Orientation.portrait, i = PaperFormat[a.paperFormat], j = a.useBrowserPrint, k = b.getPaperWidth(i, g), l = b.getPaperHeight(i, g), n = b.getPaperWidth(i, g), o = b.getPaperHeight(i, g);
  let p, q, r;
  p = await b.renderRows(a) + c.headerHeight + c.footerHeight;
  const u = b.buildPageHtml(a);
  const v = p;
  if (a.headerTpl) {
    q = b.prepareHTML(a.headerTpl({ 'totalWidth': e }));
    const B = b.measureElement(q, 'b-export-header');
    p += B;
  }
  if (a.footerTpl) {
    r = b.prepareHTML(a.footerTpl({ 'totalWidth': e }));
    const C = b.measureElement(r, 'b-export-footer');
    p += C;
  }
  const w = Math.min(1, b.getScaleValue(b.inchToPx(k), e)), z = Math.min(1, b.getScaleValue(b.inchToPx(l), p)), A = Math.min(w, z);
  f.push('<style>\n                #' + c.id + ' {\n                    height: ' + v + 'px !important;\n                    width: ' + e + 'px !important;\n                }\n                ' + (j ? '\n                            html, body {\n                                overflow: hidden;\n                            }\n                            .b-page-wrap {\n                                width: ' + n + 'in;\n                                height: ' + o + 'in;\n                            }\n                            .b-print:not(.b-firefox) .b-export-content {\n                                zoom: ' + A + ';\n                                height: 100%;\n                            }\n                            .b-print.b-firefox .b-export-content {\n                                transform: scale(' + A + ') ' + (b.centerContentHorizontally ? 'translateX(-50%)' : '') + ';\n                                transform-origin: top left;\n                                height: ' + (A === 1 ? 'inherit' : 'auto !important') + ';\n                            }\n                            .b-export-content {\n                                ' + (b.centerContentHorizontally ? 'left: 50%;' : '') + '\n                            }\n                        ' : '\n                            .b-export-content {\n                                ' + (b.centerContentHorizontally ? 'left: 50%;' : '') + '\n                                transform: scale(' + A + ') ' + (b.centerContentHorizontally ? 'translateX(-50%)' : '') + ';\n                                transform-origin: top left;\n                                height: ' + (A === 1 ? 'inherit' : 'auto !important') + ';\n                            }\n                        ') + '\n            </style>');
  yield {
    'html': b.pageTpl({
      'html': u,
      'header': q,
      'footer': r,
      'styles': f,
      'paperWidth': k,
      'paperHeight': l,
      'realPaperHeight': o,
      'realPaperWidth': n,
      'currentPage': 0,
      'isPrint': a.useBrowserPrint
    })
  };
};
SinglePageExporter._$name = 'SinglePageExporter';
const PdfExport = class extends InstancePlugin {
  static get $name() {
    return 'PdfExport';
  }
  static get configurable() {
    return {
      'dialogClass': ExportDialog,
      'exportServer': void 0,
      'exportDialog': {
        'value': true,
        '$config': ['lazy']
      },
      'fileName': null,
      'fileFormat': 'pdf',
      'clientURL': null,
      'paperFormat': 'A4',
      'orientation': 'portrait',
      'rowsRange': 'all',
      'alignRows': false,
      'repeatHeader': false,
      'keepRegionSizes': null,
      'pagesPerRequest': 0,
      'exporterConfig': null,
      'exporterType': 'singlepage',
      'exporters': [
        SinglePageExporter,
        MultiPageExporter,
        MultiPageVerticalExporter
      ],
      'translateURLsToAbsolute': true,
      'keepPathName': true,
      'openAfterExport': true,
      'sendAsBinary': false,
      'openInNewTab': false,
      'headerTpl': null,
      'footerTpl': null,
      'fetchOptions': null,
      'exportMask': 'L{Generating pages}',
      'exportProgressMask': 'L{Waiting for response from server}',
      'showErrorToast': true,
      'localizableProperties': [
        'exportMask',
        'exportProgressMask'
      ],
      'filterStyles': a => a,
      'useRenderedColumnWidth': false,
      'useBrowserPrint': false,
      'testConfig': { 'openAfterExport': false }
    };
  }
  doDestroy() {
    let a;
    (a = this.exportDialog) == null ? void 0 : a.destroy();
    this.exportersMap.forEach(b => b.destroy());
    super.doDestroy();
  }
  get currentExportPromise() {
    return this._currentExportPromise;
  }
  set currentExportPromise(a) {
    this._currentExportPromise = a;
  }
  get exportersMap() {
    return this._exportersMap || (this._exportersMap = new Map());
  }
  getExporter(a = {}) {
    const b = this, {exportersMap: c} = b, {type: e} = a;
    let f;
    if (c.has(e)) {
      f = c.get(e);
      Object.assign(f, a);
    } else {
      const g = this.exporters.find(i => i.type === e);
      if (!g) {
        throw new Error('Exporter type ' + e + ' is not found. Make sure you\'ve configured it');
      }
      a = ObjectHelper.clone(a);
      delete a.type;
      f = new g(a);
      f.relayAll(b);
      c.set(e, f);
    }
    return f;
  }
  buildExportConfig(a = {}) {
    const b = this, {
        client: c,
        exportServer: e,
        clientURL: f,
        fileFormat: g,
        fileName: i,
        paperFormat: j,
        rowsRange: k,
        alignRows: l,
        repeatHeader: n,
        keepRegionSizes: o,
        orientation: p,
        translateURLsToAbsolute: q,
        keepPathName: r,
        sendAsBinary: u,
        headerTpl: v,
        footerTpl: w,
        filterStyles: z,
        useRenderedColumnWidth: A,
        useBrowserPrint: B
      } = b;
    if (!a.columns) {
      a.columns = c.columns.visibleColumns.filter(E => E.exportable).map(E => E.id);
    }
    const C = ObjectHelper.assign({
      'client': c,
      'exportServer': e,
      'clientURL': f,
      'fileFormat': g,
      'paperFormat': j,
      'rowsRange': k,
      'alignRows': l,
      'repeatHeader': n,
      'keepRegionSizes': o,
      'orientation': p,
      'translateURLsToAbsolute': q,
      'keepPathName': r,
      'sendAsBinary': u,
      'headerTpl': v,
      'footerTpl': w,
      'useRenderedColumnWidth': A,
      'useBrowserPrint': B,
      'exporterType': b.exporterType,
      'fileName': i || c.$$name
    }, a);
    C.columns = a.columns.slice();
    if (!(C.exporterType === 'multipagevertical' || C.exporterType === 'verticalmultipagevertical')) {
      C.repeatHeader = false;
    }
    if (!('alignRows' in a) && a.repeatHeader) {
      C.alignRows = true;
    }
    C.exporterConfig = ObjectHelper.assign({
      'type': C.exporterType,
      'translateURLsToAbsolute': C.translateURLsToAbsolute,
      'keepPathName': C.keepPathName,
      'filterStyles': z
    }, C.exporterConfig || {});
    delete C.exporterType;
    delete C.translateURLsToAbsolute;
    delete C.keepPathName;
    return C;
  }
  async export(a = {}) {
    let b, c;
    const e = this, {
        client: f,
        pagesPerRequest: g
      } = e;
    a = e.buildExportConfig(a);
    let i;
    if (f.trigger('beforePdfExport', { 'config': a }) !== false) {
      f.isExporting = true;
      f.mask(e.exportMask);
      try {
        const j = e.getExporter(a.exporterConfig);
        if (g === 0) {
          const k = await j.export(a);
          if (e.isDestroying) {
            return;
          }
          (b = e.exportDialog) == null ? void 0 : b.close();
          f.unmask();
          e.trigger('exportStep', {
            'progress': 90,
            'text': e.exportProgressMask,
            'contentGenerated': true
          });
          if (a.useBrowserPrint) {
            await e.doPrint(k, a);
            i = {};
          } else {
            const l = e.receiveExportContent(k, a);
            e.toast = e.showLoadingToast(l);
            const n = await l;
            i = { 'response': n };
            await e.processExportContent(n, a);
          }
        }
      } catch (o) {
        if (o instanceof Response) {
          i = { 'response': o };
        } else {
          i = { 'error': o };
        }
        throw o;
      } finally {
        if (e.toast && !e.toast.isDestroying) {
          e.toast.hide();
        }
        if (!e.isDestroying) {
          (c = e.exportDialog) == null ? void 0 : c.close();
          f.unmask();
          if (e.showErrorToast) {
            if (i.error) {
              if (i.error.name !== 'AbortError') {
                Toast.show({
                  'html': e.L('L{Export failed}'),
                  'rootElement': e.rootElement
                });
              }
            } else if (i.response && !i.response.ok) {
              Toast.show({
                'html': e.L('L{Server error}'),
                'rootElement': e.rootElement
              });
            }
          }
          f.trigger('pdfExport', i);
          f.isExporting = false;
        }
      }
    }
    return i;
  }
  receiveExportContent(a, b) {
    return AjaxHelper.fetch(b.exportServer, Object.assign({
      'method': 'POST',
      'credentials': 'omit',
      'headers': { 'Content-Type': 'application/json' },
      'body': JSON.stringify({
        'html': a,
        'orientation': b.orientation,
        'format': b.paperFormat,
        'fileFormat': b.fileFormat,
        'fileName': b.fileName,
        'clientURL': b.clientURL,
        'sendAsBinary': b.sendAsBinary
      })
    }, this.fetchOptions));
  }
  async processExportContent(a, b) {
    const c = this;
    if (a.ok && c.openAfterExport) {
      a = a.clone();
      const e = a.headers.get('content-type');
      if (e.match(/application\/octet-stream/)) {
        const f = FileMIMEType[b.fileFormat], g = await c.responseBlobToObjectURL(a, f), i = c.getDownloadLink(b.fileName, g);
        i.click();
      } else if (e.match(/application\/json/)) {
        const j = await a.json();
        if (j.success) {
          const k = c.getDownloadLink(b.fileName, j.url);
          k.click();
        } else {
          Toast.show({
            'html': j.msg,
            'rootElement': this.rootElement
          });
        }
      }
    }
  }
  doPrint(a) {
  }
  async responseBlobToObjectURL(a, b) {
    const c = await a.blob();
    return URL.createObjectURL(c.slice(0, c.size, b));
  }
  getDownloadLink(a, b) {
    const c = document.createElement('a');
    c.download = a;
    c.href = b;
    if (this.openInNewTab) {
      c.target = '_blank';
    }
    return c;
  }
  get defaultExportDialogConfig() {
    const a = this;
    return {
      'client': a.client,
      'exporters': a.exporters,
      'useBrowserPrint': a.useBrowserPrint,
      'items': {
        'rowsRangeField': { 'value': a.rowsRange },
        'exporterTypeField': { 'value': a.exporterType },
        'orientationField': { 'value': a.orientation },
        'paperFormatField': { 'value': a.paperFormat },
        'repeatHeaderField': { 'value': a.repeatHeader },
        'fileFormatField': { 'value': a.fileFormat },
        'alignRowsField': { 'checked': a.alignRows }
      }
    };
  }
  changeExportDialog(a, b) {
    const c = this;
    b == null ? void 0 : b.destroy();
    if (a) {
      const e = c.dialogClass.mergeConfigs({
        'rootElement': c.rootElement,
        'client': c.client,
        'modal': { 'transparent': true },
        'items': {
          'rowsRangeField': { 'value': c.rowsRange },
          'exporterTypeField': { 'value': c.exporterType },
          'orientationField': { 'value': c.orientation },
          'paperFormatField': { 'value': c.paperFormat },
          'repeatHeaderField': { 'value': c.repeatHeader },
          'fileFormatField': { 'value': c.fileFormat },
          'alignRowsField': { 'checked': c.alignRows }
        }
      }, c.defaultExportDialogConfig, a);
      a = c.dialogClass.new(e);
      a.ion({
        'export': c.onExportButtonClick,
        'thisObj': c
      });
    }
    return a;
  }
  async showExportDialog() {
    return this.exportDialog.show();
  }
  onExportButtonClick({values: a}) {
    const b = this, c = b.exportDialog.mask({
        'progress': 0,
        'maxProgress': 100,
        'text': b.exportMask
      });
    const e = b.ion({
      'exportstep'({
        progress: f,
        text: g,
        contentGenerated: i
      }) {
        if (i) {
          b.exportDialog.unmask();
          e();
        } else {
          c.progress = f;
          if (g != null) {
            c.text = g;
          }
        }
      }
    });
    b.currentExportPromise = b.export(a);
    b.currentExportPromise.catch(() => {
    }).finally(() => {
      let f;
      e();
      (f = b.exportDialog) == null ? void 0 : f.unmask();
      b.currentExportPromise = null;
    });
  }
  showLoadingToast(a) {
    const b = Toast.show({
      'timeout': 0,
      'showProgress': false,
      'rootElement': this.rootElement,
      'html': '\n    <span class="b-mask-icon b-icon b-icon-spinner"></span>\n    <span>' + this.exportProgressMask + '</span>\n    <button class="b-button">' + this.L('L{Click to abort}') + '</button>'
    });
    EventHelper.on({
      'element': b.element,
      'click'() {
        let c;
        (c = a.abort) == null ? void 0 : c.call(a);
      }
    });
    return b;
  }
};
PdfExport._$name = 'PdfExport';
GridFeatureManager.registerFeature(PdfExport, false, 'Grid');
const PrintMixin_default = a => class b extends a {
  static get $name() {
    return 'PrintMixin';
  }
  static get configurable() {
    return {
      'useBrowserPrint': true,
      'exporterType': 'multipagevertical',
      'test': false
    };
  }
  static get pluginConfig() {
    return {
      'assign': [
        'print',
        'showPrintDialog'
      ]
    };
  }
  showPrintDialog() {
    return this.showExportDialog();
  }
  print(c) {
    return this.export(c);
  }
  async onPrintIFrameLoad(c) {
  }
  async showBrowserPrintDialog(c, e) {
    await this.onPrintIFrameLoad(c);
    if (await this.client.trigger('beforeShowPrintDialog', { 'iframe': c }) !== false) {
      if (!this.test) {
        const {contentWindow: f} = c;
        f.onafterprint = () => {
          c.remove();
          e();
        };
        f.print();
      }
    } else {
      c.remove();
      e();
    }
  }
  doPrint(c) {
    const e = this;
    return new Promise((f, g) => {
      const i = document.createElement('iframe');
      i.className = 'b-print-wrapper';
      i.style.visibility = 'hidden';
      i.style.height = '0';
      i.onload = () => {
        let j;
        const k = i.contentWindow, l = k.document, {body: n} = l, o = new DOMParser();
        let p;
        c.forEach(({html: r}) => {
          const u = o.parseFromString(r, 'text/html'), v = l.adoptNode(u.body.firstChild), w = l.createElement('div');
          u.head.querySelectorAll('style,link[rel="stylesheet"],link[as="style"]').forEach(z => {
            const A = l.adoptNode(z);
            l.head.appendChild(A);
          });
          w.classList.add('b-page-wrap');
          w.style.height = u.body.parentElement.style.height;
          w.style.width = u.body.parentElement.style.width;
          p = parseFloat(u.body.parentElement.style.height);
          n.appendChild(w);
          w.appendChild(v);
        });
        const {html: q} = c[0];
        n.className = ((j = q.match(/<body class="(.+?)"/)) == null ? void 0 : j[1]) || '';
        n.classList.add('b-print');
        n.parentElement.classList.add('b-print-root');
        n.parentElement.style.height = p * c.length + 'in';
        Promise.all(Array.from(l.head.querySelectorAll('link[rel="stylesheet"]')).map(r => {
          return new Promise((u, v) => {
            r.onload = u;
            r.onerror = v;
          });
        })).then(() => l.fonts.ready).then(() => e.showBrowserPrintDialog(i, f)).catch(r => {
          console.warn('Failed to load stylesheets ' + (r.message ? ': ' + r.message : ''));
          g(e.L('L{PdfExport.Export failed}'));
        });
      };
      e.client.element.parentElement.appendChild(i);
    });
  }
};
const Print = class extends PrintMixin_default(PdfExport) {
  static $name = 'Print';
};
Print._$name = 'Print';
GridFeatureManager.registerFeature(Print, false, 'Grid');
const TableExporter = class extends Base {
  static get defaultConfig() {
    return {
      'target': null,
      'defaultColumnWidth': 100,
      'exportDateAsInstance': true,
      'showGroupHeader': true,
      'columns': null,
      'indent': true,
      'indentationSymbol': '\xA0\xA0\xA0\xA0'
    };
  }
  export(a = {}) {
    const b = this;
    a = ObjectHelper.assign({}, b.config, a);
    b.normalizeColumns(a);
    return b.generateExportData(a);
  }
  generateExportData(a) {
    const b = this, c = b.generateColumns(a), e = b.generateRows(a);
    return {
      'rows': e,
      'columns': c
    };
  }
  normalizeColumns(a) {
    let b;
    const c = a.columns || ((b = this.target.columns) == null ? void 0 : b.visibleColumns.filter(e => e.exportable !== false));
    a.columns = c.map(e => {
      if (typeof e === 'string') {
        return this.target.columns.find(f => f.field === e) || { 'field': e };
      } else {
        return e;
      }
    });
  }
  generateColumns(a) {
    return a.columns.map(b => this.processColumn(b, a));
  }
  generateRows(a) {
    const {
      columns: b,
      rows: c
    } = a;
    if (b.length === 0 || (c == null ? void 0 : c.length) === 0) {
      return [];
    }
    const e = this, {target: f} = e;
    return (c || f.store).map(g => e.processRecord(g, b, a)).filter(g => g == null ? void 0 : g.length);
  }
  getColumnType(a, b = this.target.store) {
    let c = a.exportedType || 'object';
    if (a.exportedType === void 0) {
      if (a.field) {
        const e = b.modelClass.getFieldDefinition(a.field);
        if (e && e.type !== 'auto') {
          c = e.type;
        }
      }
    }
    return c;
  }
  processColumn(a, b) {
    const c = this, {target: e} = c, {defaultColumnWidth: f} = b;
    let {
      field: g,
      text: i,
      width: j,
      minWidth: k
    } = a;
    if (e.store && !(g in e.store.modelClass.fieldMap)) {
      g = '';
    }
    if (!i || !j) {
      const l = e.columns.find(n => n.field === g);
      if (!i) {
        i = l && (l.text || l.headerRenderer && l.textElement.innerText) || g;
      }
      if (j == null) {
        j = l && l.width || f;
      }
    }
    j = Math.max(j || f, k || f);
    return {
      'field': g,
      'value': i,
      'width': j,
      'type': c.getColumnType(a)
    };
  }
  processRecord(a, b, c) {
    const {target: e} = this, {
        showGroupHeader: f,
        indent: g,
        indentationSymbol: i
      } = c;
    let j;
    if (!a) {
      j = b.map(() => '');
    } else if (a.isSpecialRow) {
      if (f && a.meta.groupRowFor) {
        j = b.map(k => {
          return e.features.group.buildGroupHeader({
            'cellElement': DomHelper.createElement(),
            'grid': e,
            'record': a,
            'column': k
          });
        });
      }
    } else {
      j = b.map(k => {
        let l = a.getValue(k.field);
        const n = k.renderer;
        if (n && !(l && k.isDateColumn && c.exportDateAsInstance)) {
          l = n.call(k, {
            'value': l,
            'record': a,
            'column': k,
            'grid': e,
            'isExport': true
          });
        }
        if (g && k.tree) {
          l = '' + i.repeat(a.childLevel) + l;
        }
        return l;
      });
    }
    return j;
  }
};
TableExporter._$name = 'TableExporter';
const BooleanUnicodeSymbol = class {
  constructor(a) {
    this._value = a;
  }
  get value() {
    return this._value;
  }
  toString() {
    return Boolean(this.value) ? '\u2713' : '';
  }
};
BooleanUnicodeSymbol._$name = 'BooleanUnicodeSymbol';
const ExcelExporter = class extends InstancePlugin {
  static configurable = {
    'filename': null,
    'dateFormat': 'YYYY-MM-DD',
    'exporterClass': TableExporter,
    'exporterConfig': null,
    'zipcelx': null,
    'convertEmptyValueToEmptyString': true
  };
  static $name = 'ExcelExporter';
  processValue(a) {
    if (a === void 0 || a === null || Number.isNaN(a) || typeof a === 'function' || typeof a === 'object' && String(a) === '[object Object]') {
      return '';
    }
    return a;
  }
  generateExportData(a) {
    const b = this, {
        rows: c,
        columns: e
      } = b.exporter.export(a.exporterConfig);
    return {
      'rows': c.map(f => {
        return f.map((g, i) => {
          let j;
          if (g instanceof Date) {
            g = DateHelper.format(g, a.dateFormat);
          } else if (typeof g === 'boolean') {
            g = new BooleanUnicodeSymbol(g);
          }
          if (b.convertEmptyValueToEmptyString) {
            g = b.processValue(g);
          }
          const k = ((j = e[i]) == null ? void 0 : j.type) === 'number' && typeof g !== 'string' ? 'number' : 'string';
          return {
            'value': g,
            'type': k
          };
        });
      }),
      'columns': e.map(f => {
        let {
          field: g,
          value: i,
          width: j,
          type: k
        } = f;
        k = 'string';
        return {
          'field': g,
          'value': i,
          'width': j,
          'type': k
        };
      })
    };
  }
  export(a = {}) {
    const b = this, {client: c} = b, e = b.zipcelx || globalThis.zipcelx;
    if (!e) {
      throw new Error('ExcelExporter: "zipcelx" library is required');
    }
    if (b.disabled) {
      return;
    }
    a = ObjectHelper.assign({}, b.config, a);
    if (!a.filename) {
      a.filename = c.$$name;
    }
    const {filename: f} = a, {
        rows: g,
        columns: i
      } = b.generateExportData(a);
    if (a.csv) {
      const j = a.csv.columnDelimiter || ',', k = a.csv.lineDelimiter || '\n';
      if (c.trigger('beforeCSVExport', {
          'config': a,
          'rows': g,
          'columns': i,
          'lineDelimiter': k,
          'columnDelimiter': j
        }) !== false) {
        const l = i.map(q => this.processCsvValue(q.value)).join(j) + k, n = g.map(q => q.map(r => this.processCsvValue(r.value)).join('' + j)).join(k), o = new Uint8Array([
            239,
            187,
            191
          ]), p = new Blob([
            o,
            l + n
          ], { 'type': 'text/csv' });
        BrowserHelper.downloadBlob(p, f);
      }
      return Promise.resolve();
    }
    if (c.trigger('beforeExcelExport', {
        'config': a,
        'rows': g,
        'columns': i
      }) !== false) {
      return e({
        'filename': f,
        'sheet': {
          'data': [i].concat(g),
          'cols': i
        }
      });
    }
    return Promise.resolve();
  }
  processCsvValue(a = '') {
    return '"' + String(a).replace(/"/g, '""') + '"';
  }
  construct(a, b) {
    super.construct(a, b);
    if (!this.zipcelx) {
      if (typeof zipcelx !== 'undefined') {
        this.zipcelx = globalThis.zipcelx;
      }
    }
  }
  get exporter() {
    const a = this;
    return a._exporter || (a._exporter = a.exporterClass.new({ 'target': a.client }, a.exporterConfig));
  }
};
ExcelExporter._$name = 'ExcelExporter';
GridFeatureManager.registerFeature(ExcelExporter, false, 'Grid');
const FileDrop = class extends InstancePlugin {
  static $name = 'FileDrop';
  construct(a, b) {
    const c = this;
    super.construct(a, b);
    EventHelper.on({
      'element': a.element,
      'thisObj': c,
      'drop': c.onFileDrop,
      'dragover': c.onFileDragOver,
      'dragenter': c.onFileDragEnter,
      'dragleave': c.onFileDragLeave
    });
  }
  onFileLoad(a) {
    this.client.trigger('fileDrop', {
      'file': this.file,
      'domEvent': a
    });
  }
  onFileDragEnter() {
    this.client.element.classList.add('b-dragging-file');
  }
  onFileDragOver(a) {
    a.preventDefault();
  }
  onFileDragLeave(a) {
    const {element: b} = this.client;
    if (a.relatedTarget && !b.contains(a.relatedTarget)) {
      this.client.element.classList.remove('b-dragging-file');
    }
  }
  onFileDrop(a) {
    a.preventDefault();
    const b = a.dataTransfer.items[0].getAsFile();
    this.client.trigger('fileDrop', {
      'file': b,
      'domEvent': a
    });
    this.onFileDragLeave(a);
  }
};
FileDrop._$name = 'FileDrop';
GridFeatureManager.registerFeature(FileDrop, false, 'Grid');
const cellContentRange = document.createRange();
const Row = class extends Base {
  static $name = 'Row';
  static get configurable() {
    return { 'cls': { '$config': { 'equal': (a, b) => (a == null ? void 0 : a.isDomClassList) && (b == null ? void 0 : b.isDomClassList) && a.isEqual(b) } } };
  }
  construct(a) {
    Object.assign(this, {
      '_elements': {},
      '_elementsArray': [],
      '_cells': {},
      '_allCells': [],
      '_regions': [],
      'lastHeight': 0,
      'lastTop': -1,
      '_dataIndex': 0,
      '_top': 0,
      '_height': 0,
      '_id': null,
      'forceInnerHTML': false,
      'isGroupFooter': false,
      'cellContext': new GridLocation({
        'grid': a.grid,
        'id': null,
        'columnIndex': 0
      })
    });
    super.construct(a);
    if (this.grid.positionMode === 'position') {
      this.translateElements = this.positionElements;
    }
  }
  doDestroy() {
    const a = this;
    if (!a.rowManager.isDestroying) {
      a.removeElements();
      if (a.rowManager.idMap[a.id] === a) {
        delete a.rowManager.idMap[a.id];
      }
    }
    super.doDestroy();
  }
  get index() {
    return this._index;
  }
  set index(a) {
    this._index = a;
  }
  get dataIndex() {
    return this._dataIndex;
  }
  set dataIndex(a) {
    if (this._dataIndex !== a) {
      this._dataIndex = a;
      this.eachElement(b => {
        b.dataset.index = a;
        b.ariaRowIndex = this.grid.hideHeaders ? a + 1 : a + 2;
      });
    }
  }
  get id() {
    return this._id;
  }
  set id(a) {
    const b = this, c = { 'id': a }, e = b.rowManager.idMap;
    if (b._id !== a || e[a] !== b) {
      if (e[b._id] === b)
        delete e[b._id];
      e[a] = b;
      b._id = a;
      b.eachElement(f => {
        DomDataStore.assign(f, c);
        f.dataset.id = a;
      });
      b.eachCell(f => DomDataStore.assign(f, c));
    }
  }
  addElement(a, b) {
    const c = this, {dataIndex: e} = c;
    let f = b.firstElementChild;
    c._elements[a] = b;
    c._elementsArray.push(b);
    c._regions.push(a);
    DomDataStore.assign(b, { 'index': c.index });
    c._cells[a] = [];
    while (f) {
      c._cells[a].push(f);
      c._allCells.push(f);
      DomDataStore.set(f, {
        'column': f.dataset.column,
        'columnId': f.dataset.columnId,
        'rowElement': f.parentNode,
        'row': c
      });
      f = f.nextElementSibling;
    }
    b.dataset.index = e;
    b.ariaRowIndex = c.grid.hideHeaders ? e + 1 : e + 2;
    if (c.top !== null) {
      c.translateElements(true);
    }
  }
  getElement(a) {
    return this._elements[a];
  }
  getRectangle(a) {
    return Rectangle.from(this.getElement(a));
  }
  eachElement(a) {
    this._elementsArray.forEach(a);
  }
  eachCell(a) {
    this._allCells.forEach(a);
  }
  get elements() {
    return this._elements;
  }
  get element() {
    const a = Object.keys(this._elements)[0];
    return this._elements[a];
  }
  get cells() {
    return this._allCells;
  }
  getCells(a) {
    return this._cells[a];
  }
  getCell(a, b = false) {
    return this._allCells.find(c => {
      const e = DomDataStore.get(c);
      return e.columnId == a || !b && e.column == a;
    });
  }
  removeElements(a = false) {
    const b = this;
    b.rowManager.trigger('removeRow', { 'row': b });
    if (!a) {
      b.eachElement(c => c.remove());
    }
    b._elements = {};
    b._cells = {};
    b._elementsArray.length = b._regions.length = b._allCells.length = b.lastHeight = b.height = 0;
    b.lastTop = -1;
  }
  get height() {
    return this._height;
  }
  set height(a) {
    this._height = a;
  }
  get offsetHeight() {
    return this.height + this.grid._rowBorderHeight;
  }
  updateElementsHeight(a) {
    const b = this;
    if (!a) {
      b.rowManager.storeKnownHeight(b.id, b.height);
    }
    if (b.lastHeight !== b.height) {
      this.eachElement(c => c.style.height = b.offsetHeight + 'px');
      b.lastHeight = b.height;
    }
  }
  addCls(a) {
    this.updateCls(this.cls.add(a));
  }
  removeCls(a) {
    this.updateCls(this.cls.remove(a));
  }
  toggleCls(a, b) {
    this.updateCls(this.cls[b ? 'add' : 'remove'](a));
  }
  assignCls(a) {
    this.updateCls(this.cls.assign(a));
  }
  changeCls(a) {
    return new DomClassList(a);
  }
  updateCls(a) {
    this.eachElement(b => DomHelper.syncClassList(b, a));
  }
  setAttribute(a, b) {
    this.eachElement(c => c.setAttribute(a, b));
  }
  removeAttribute(a) {
    this.eachElement(b => b.removeAttribute(a));
  }
  get isFirst() {
    return this.dataIndex === 0;
  }
  get top() {
    return this._top;
  }
  get bottom() {
    return this._top + this._height + this.grid._rowBorderHeight;
  }
  setTop(a, b) {
    if (this._top !== a) {
      this._top = a;
      this.translateElements(b);
    }
  }
  setBottom(a, b) {
    this.setTop(a - this.offsetHeight, b);
  }
  translate(a, b = false) {
    this.setTop(a, b);
    return a + this.offsetHeight;
  }
  translateElements(a) {
    const b = this, {
        top: c,
        _elementsArray: e
      } = b;
    if (b.lastTop !== c) {
      for (let f = 0, {length: g} = e; f < g; f++) {
        e[f].style.transform = 'translate(0,' + c + 'px)';
      }
      !a && b.rowManager.trigger('translateRow', { 'row': b });
      b.lastTop = c;
    }
  }
  positionElements(a) {
    const b = this, {
        top: c,
        _elementsArray: e
      } = b;
    if (b.lastTop !== c) {
      for (let f = 0, {length: g} = e; f < g; f++) {
        e[f].style.top = c + 'px';
      }
      !a && b.rowManager.trigger('translateRow', { 'row': b });
      b.lastTop = c;
    }
  }
  offset(a) {
    let b = this._top + a;
    if (b < 0) {
      a -= b;
      b = 0;
    }
    this.setTop(b);
    return a;
  }
  renderPlaceholder(a, b, c, e, f) {
    const g = this, {rowManager: i} = g, {rowHeight: j} = i;
    g.addCls('b-row-placeholder');
    if (g.height !== j) {
      g.height = j;
      g.updateElementsHeight(true);
      i.bufferedTranslateFromRow(g);
    }
    g.dataIndex = a;
    b.then(k => {
      if (k && g.dataIndex === a && g.grid.store.indexOf(k) === a) {
        g.render(a, k, c, e, f, true);
      }
    });
  }
  render(a, b, c = true, e = false, f = false, g = false) {
    let j, k, l, n;
    const o = this, {
        cellContext: p,
        cls: q,
        elements: r,
        grid: u,
        rowManager: v,
        height: w,
        _id: z
      } = o, A = DomDataStore.get(o._elementsArray[0]), B = v._rowHeight, {store: C} = u, E = u.features.tree && C.isTree;
    let F = 0, G;
    if (!b && b !== false) {
      if (a != null) {
        b = (j = C.lazyLoad) == null ? void 0 : j.getAt(a);
        if (b && !b.isModel) {
          return o.renderPlaceholder(a, b, c, e, f);
        }
      }
      if (!b) {
        b = C.getById(A.id);
        a = C.indexOf(b);
      }
    }
    if (!b) {
      return;
    }
    o.removeCls('b-row-placeholder');
    const H = b == null ? void 0 : b.cls, I = H ? H.isDomClassList ? H : new DomClassList(H) : null;
    q.assign({
      'b-grid-row-updating': c && u.transitionDuration && !f,
      'b-selected': u.isSelected(b == null ? void 0 : b.id),
      'b-readonly': b.readOnly,
      'b-linked': b.isLinked,
      'b-original': b.hasLinks
    });
    if (o.lastRecordCls) {
      q.remove(o.lastRecordCls);
    }
    if (I) {
      q.add(I);
      o.lastRecordCls = Object.assign({}, I);
    } else {
      o.lastRecordCls = null;
    }
    v.trigger('beforeRenderRow', {
      'row': o,
      'record': b,
      'recordIndex': a,
      'oldId': z
    });
    u.beforeRenderRow({
      'row': o,
      'record': b,
      'recordIndex': a,
      'oldId': z,
      'cls': q
    });
    o.updateCls(q);
    if (c && u.transitionDuration && !f) {
      u.setTimeout(() => {
        if (!o.isDestroyed) {
          q.remove('b-grid-row-updating');
          o.updateCls(q);
        }
      }, u.transitionDuration + 50);
    }
    o.id = b.id;
    o.dataIndex = a;
    const J = !u.fixedRowHeight && u.getRowHeight(b) || B;
    let K = o.maxRequestedHeight = null;
    if (E) {
      for (const N in r) {
        const O = r[N];
        O.id = u.id + '-' + N + '-' + o.id;
        DomHelper.setAttributes(O, {
          'aria-level': b.childLevel + 1,
          'aria-setsize': b.parent.children.length,
          'aria-posinset': b.parentIndex + 1
        });
        if (b.isExpanded(C)) {
          DomHelper.setAttributes(O, {
            'aria-expanded': true,
            'aria-owns': ((k = b.children) == null ? void 0 : k.length) ? (l = b.children) == null ? void 0 : l.map(P => u.id + '-' + N + '-' + P.id).join(' ') : null
          });
        } else {
          if (b.isLeaf) {
            O.removeAttribute('aria-expanded');
          } else {
            O.setAttribute('aria-expanded', false);
          }
          O.removeAttribute('aria-owns');
        }
      }
    }
    p._record = b;
    p._id = b.id;
    p._rowIndex = a;
    for (F = 0; F < u.columns.visibleColumns.length; F++) {
      const P = u.columns.visibleColumns[F];
      p._columnId = P.id;
      p._column = P;
      p._columnIndex = F;
      p._cell = o.getCell(P.id, true);
      p.height = J;
      p.maxRequestedHeight = K;
      p.updatingSingleRow = c;
      G = o.renderCell(p);
      if (!v.fixedRowHeight) {
        if (G.height != null) {
          K = Math.max(K, G.height);
          if (!G.transient) {
            o.maxRequestedHeight = K;
          }
        }
      }
    }
    const L = K != null ? K : J;
    o.height = (n = u.processRowHeight(b, L)) != null ? n : L;
    o.updateElementsHeight(f);
    if (g && w !== o.height) {
      v.bufferedTranslateFromRow(o, e);
    }
    if (c && !f) {
      if (w !== o.height) {
        v.translateFromRow(o, e);
      }
      v.trigger('updateRow', {
        'row': o,
        'record': b,
        'recordIndex': a,
        'oldId': z
      });
      v.trigger('renderDone');
    }
    u.afterRenderRow({
      'row': o,
      'record': b,
      'recordIndex': a,
      'oldId': z,
      'oldHeight': w,
      'isExport': f
    });
    v.trigger('renderRow', {
      'row': o,
      'record': b,
      'recordIndex': a,
      'oldId': z,
      'isExport': f
    });
    if (w && o.height !== w) {
      v.trigger('rowRowHeight', {
        'row': o,
        'record': b,
        'height': o.height,
        'oldHeight': w
      });
    }
    o.forceInnerHTML = false;
  }
  renderCell(a) {
    let b, c, e, f;
    if (!a.isLocation) {
      a = new GridLocation(a);
    }
    let {
      cell: g,
      record: i
    } = a;
    const j = this, {
        grid: k,
        column: l,
        height: n,
        maxRequestedHeight: o,
        updatingSingleRow: updatingSingleRow = true,
        isMeasuring: isMeasuring = false
      } = a, p = (b = k.features) == null ? void 0 : b.cellEdit, q = DomDataStore.get(g), r = q.rowElement, u = DomDataStore.get(r);
    if (!i) {
      i = a._record = k.store.getById(u.id);
      if (!i) {
        return;
      }
    }
    let v = l.getRawValue(i);
    const w = i.fieldMap[l.field], z = {
        'configuredHeight': n,
        'height': null,
        'maxRequestedHeight': o
      }, A = l.getCellClass(a), B = {
        'cellElement': g,
        'cellCls': A,
        'dataField': w,
        'rowElement': r,
        'value': v,
        'record': i,
        'column': l,
        'size': z,
        'grid': k,
        'row': q.row,
        'updatingSingleRow': updatingSingleRow,
        'isMeasuring': isMeasuring
      }, C = l.renderer;
    if (l.readOnly || i.readOnly || k.readOnly) {
      g.setAttribute('aria-readonly', '');
    } else {
      g.removeAttribute('aria-readonly');
    }
    k.beforeRenderCell(B);
    if (B.cellElement !== g) {
      g = B.cellElement;
    }
    DomHelper.syncClassList(g, A);
    let E = true;
    if (C) {
      v = l.callback(C, l, [B]);
      if (v === void 0) {
        if (i.generatedParent && l.rendererReturningContent) {
          v = '';
        } else if (l.alwaysClearCell === false) {
          E = false;
        }
      }
    } else if (w) {
      v = w.print(v);
    }
    const F = (c = k.hasFrameworkRenderer) == null ? void 0 : c.call(k, {
      'cellContent': v,
      'column': l
    });
    if (F && i.isSpecialRow) {
      v = '';
    }
    const G = F && !l.data.tree && !i.isSpecialRow;
    if (E && !G) {
      let H = g;
      if (((e = p == null ? void 0 : p.editorContext) == null ? void 0 : e.equals(a)) && !p.editor.isFinishing) {
        H = j.moveContentFromCell(g, p.editor.element);
      }
      const I = v != null && typeof v === 'object', J = typeof v === 'string', K = I || v == null ? '' : String(v);
      if (j.forceInnerHTML) {
        H.innerHTML = '';
        delete H._content;
        g.lastDomConfig = null;
      }
      if (!I && l.htmlEncode && !l.disableHtmlEncode) {
        if (g._hasHtml) {
          H.innerText = K;
          g._hasHtml = false;
        } else {
          DomHelper.setInnerText(H, K);
        }
        delete H._content;
      } else {
        if (l.autoSyncHtml && (!J || DomHelper.getChildElementCount(H))) {
          if (J) {
            DomHelper.sync(K, H.firstElementChild);
          } else if (I) {
            DomSync.sync({
              'domConfig': v,
              'targetElement': H
            });
            delete H._content;
          }
        } else if (I) {
          DomSync.sync({
            'targetElement': H,
            'domConfig': {
              'onlyChildren': true,
              'children': ArrayHelper.asArray(v)
            }
          });
          delete H._content;
        } else if (H._content !== K) {
          H.innerHTML = H._content = K;
        }
      }
      if (H !== g) {
        const {firstChild: L} = g;
        for (const N of [...H.childNodes]) {
          g.insertBefore(N, L);
        }
      }
    }
    if (!i.isSpecialRow) {
      (f = k.processCellContent) == null ? void 0 : f.call(k, {
        'cellElementData': q,
        'rendererData': B,
        'rendererHtml': B.rendererHtml || v
      });
    }
    if (l.autoHeight && z.height == null) {
      g.classList.add('b-measuring-auto-height');
      z.height = Math.max(g.offsetHeight, k.rowHeight);
      g.classList.remove('b-measuring-auto-height');
    }
    if (!isMeasuring) {
      j.rowManager.trigger('renderCell', B);
    }
    if (l.afterRenderCell) {
      l.callback(l.afterRenderCell, l, [B]);
    }
    return z;
  }
  moveContentFromCell(a, b) {
    cellContentRange.setStart(a, 0);
    cellContentRange.setEndBefore(b);
    const c = document.createElement('div');
    c.appendChild(cellContentRange.extractContents());
    return c;
  }
};
Row.initClass();
Row._$name = 'Row';
const Bar = class extends Widget {
  static get $name() {
    return 'Bar';
  }
  static get type() {
    return 'gridbar';
  }
  static get defaultConfig() {
    return {
      'htmlCls': '',
      'scrollable': { 'overflowX': 'hidden-scroll' }
    };
  }
  get columns() {
    return this._columns || this.subGrid.columns;
  }
  set columns(a) {
    this._columns = a;
  }
  fixCellWidths() {
    const a = this, {hasFlex: b} = a.columns;
    let c;
    a.columns.traverse(e => {
      const f = a.getBarCellElement(e.id), g = DomHelper.setLength(e.width), i = DomHelper.setLength(e.minWidth), j = DomHelper.setLength(e.maxWidth);
      if (f) {
        c = g;
        f.style.maxWidth = j;
        if (e.isParent && e.width == null && e.flex == null) {
          const k = e.children.reduce((l, n) => l += !n.hidden && n.flex || 0, 0);
          f.style.flex = k > 0 ? k + ' 0 auto' : '';
          f.style.minWidth = null;
          if (k > 0) {
            e.traverse(l => l.data.minWidth = null);
          }
        } else {
          if (parseInt(e.minWidth) >= 0) {
            f.style.minWidth = i;
          }
          f.style.flex = f.style.flexBasis = f.style.width = '';
          if (e.flex) {
            if (!isNaN(parseInt(e.flex)) && e.children) {
              f.style.flex = e.flex + ' 0 auto';
            } else {
              f.style.flex = e.flex;
            }
          } else if (parseInt(e.width) >= 0) {
            const l = e.parent;
            if (a.isHeader && !l.isRoot && !l.width) {
              f.style.width = g;
            } else {
              f.style.flexBasis = c;
            }
          }
        }
        if (e.height >= 0) {
          f.style.height = DomHelper.setLength(e.height);
        }
      }
    });
    a.scrollable.element.classList.toggle('b-has-flex', b);
  }
  getLrPadding(a) {
    if (!this.cellLrPadding) {
      const b = a.ownerDocument.defaultView.getComputedStyle(a);
      this.cellLrPadding = parseInt(b.getPropertyValue('padding-left')) + parseInt(b.getPropertyValue('padding-right')) + parseInt(b.getPropertyValue('border-left-width')) + parseInt(b.getPropertyValue('border-right-width'));
    }
    return this.cellLrPadding;
  }
  getBarCellElement(a) {
    return this.element.querySelector('[data-column-id="' + a + '"]');
  }
};
Bar.initClass();
Bar._$name = 'Bar';
const Footer = class extends Bar {
  static get $name() {
    return 'Footer';
  }
  static get type() {
    return 'gridfooter';
  }
  get subGrid() {
    return this._subGrid;
  }
  set subGrid(a) {
    this._subGrid = this.owner = a;
  }
  refreshContent() {
    this.element.firstElementChild.innerHTML = this.contentTemplate();
    this.fixFooterWidths();
  }
  onInternalPaint({firstPaint: a}) {
    if (a) {
      this.refreshContent();
    }
  }
  template() {
    const a = this.subGrid.region;
    return TemplateHelper.tpl`
            <div class="b-grid-footer-scroller b-grid-footer-scroller-${ a }" role="presentation">
                <div data-reference="footersElement" class="b-grid-footers b-grid-footers-${ a }" data-region="${ a }" role="presentation"></div>
            </div>
        `;
  }
  get overflowElement() {
    return this.footersElement;
  }
  getFooter(a) {
    return this.getBarCellElement(a);
  }
  contentTemplate() {
    const a = this;
    return a.columns.visibleColumns.map(b => {
      return TemplateHelper.tpl`
                <div
                    class="b-grid-footer ${ b.align ? 'b-grid-footer-align-' + b.align : '' } ${ b.cls || '' }"
                    data-column="${ b.field || '' }" data-column-id="${ b.id }" data-all-index="${ b.allIndex }"
                    role="presentation">
                    ${ b.footerText || '' }
                </div>`;
    }).join('');
  }
  fixFooterWidths() {
    this.fixCellWidths();
  }
};
Footer.initClass();
Footer._$name = 'Footer';
const cloneRowEl = a => {
  const b = a.parentElement, c = a.cloneNode(true);
  c.classList.add('b-removing');
  b.insertBefore(c, a);
  return c;
};
let nodeAnimationCleanup;
let nodeAnimationCleanupListener;
const RowManager = class extends InstancePlugin.mixin(Delayable_default) {
  static delayable = { 'bufferedTranslateFromRow': 'raf' };
  static get pluginConfig() {
    return {
      'chain': ['destroy'],
      'assign': [
        'topRow',
        'bottomRow',
        'firstVisibleRow',
        'lastVisibleRow',
        'firstFullyVisibleRow',
        'lastFullyVisibleRow',
        'getRowById',
        'getRecordCoords',
        'getRow',
        'getRowFor',
        'getRowFromElement'
      ]
    };
  }
  static get defaultConfig() {
    return {
      'rowClass': Row,
      'prependRowBuffer': 5,
      'appendRowBuffer': 5,
      'rowHeight': null,
      'fixedRowHeight': null,
      'autoHeight': false
    };
  }
  static get properties() {
    return {
      'idMap': {},
      'topIndex': 0,
      'lastScrollTop': 0,
      '_rows': [],
      'heightMap': new Map(),
      'totalKnownHeight': 0,
      '_totalHeight': 0,
      'averageRowHeight': 0,
      'scrollTargetRecordId': null,
      'refreshDetails': {
        'topRowIndex': 0,
        'topRowTop': 0
      }
    };
  }
  construct(a) {
    a.grid._rowManager = this;
    super.construct(a.grid, a);
  }
  doDestroy() {
    this.removeAllRows();
    super.doDestroy();
  }
  initWithHeight(a, b = false) {
    const c = this;
    if (c.autoHeight) {
      a = c.store.totalCount * c.preciseRowOffsetHeight;
    }
    c.viewHeight = a;
    c.calculateRowCount(b);
    return a;
  }
  reinitialize(a = false) {
    const b = this;
    b.calculateRowCount(false, true, true);
    if (b.topIndex + b.rowCount - 1 > b.storeCount) {
      a = true;
    }
    if (a) {
      b.topIndex = b.lastScrollTop = 0;
    }
    b.scrollTargetRecordId = null;
    const {topRow: c} = b;
    if (c) {
      c.dataIndex = b.topIndex;
      c.setTop(b.calculateTop(b.topIndex), true);
    }
    b.estimateTotalHeight();
    b.renderFromRow(c);
  }
  matchRowCount(a = false) {
    let b;
    const c = this, {
        rows: e,
        grid: f,
        rowClass: g
      } = c, i = e.length, j = i - c.rowCount;
    if (j) {
      if (j < 0) {
        const k = [];
        for (let l = i, n = i ? e[i - 1].dataIndex + 1 : 0; l < c.rowCount; l++, n++) {
          k.push(g.new({
            'cls': f.rowCls,
            'rowManager': c,
            'grid': f,
            'index': l,
            'dataIndex': n
          }));
        }
        e.push.apply(e, k);
        c.trigger('addRows', { 'rows': k });
        if (!a) {
          c.renderFromRow(e[Math.max(0, i - 1)]);
        }
      } else {
        const o = (b = DomHelper.getActiveElement(f)) == null ? void 0 : b.closest('.b-grid-cell'), p = e.splice(i - j, j);
        if (i !== j && p.some(q => q.cells.includes(o))) {
          e[e.length - 1].cells[f.focusedCell.columnIndex].focus();
        }
        c.trigger('removeRows', { 'rows': p });
        p.forEach(q => q.destroy());
        !a && e.length && c.fillBelow(c.lastScrollTop);
      }
    }
  }
  calculateRowCount(a = false, b = true, c = false) {
    let e, f;
    const g = this, {storeCount: i} = g, j = Math.ceil(g.viewHeight / g.minRowOffsetHeight), k = j + g.prependRowBuffer + g.appendRowBuffer;
    if (!((e = g.grid.columns) == null ? void 0 : e.count) || isNaN(j)) {
      g.rowCount = 0;
      return 0;
    }
    if (k < g.rowCount && !b) {
      return g.rowCount;
    }
    g.visibleRowCount = j;
    g.rowCount = Math.min(i, k);
    if (!a) {
      if (g.rows && g.rowCount !== g.rows.length) {
        g.matchRowCount(c);
        if (((f = g.bottomRow) == null ? void 0 : f.dataIndex) >= i && g.topRow.dataIndex !== 0) {
          const l = g.bottomRow.dataIndex - i + 1;
          for (const n of g.rows) {
            n.dataIndex -= l;
          }
          g.topIndex -= l;
        }
      } else if (!g.rowCount) {
        g.trigger('changeTotalHeight', { 'totalHeight': g.totalHeight });
      }
      g.grid.toggleEmptyText();
    }
    return g.rowCount;
  }
  insert(a, b) {
    let c, e, f, g, j;
    nodeAnimationCleanup == null ? void 0 : nodeAnimationCleanup();
    const k = this, {
        rows: l,
        rowCount: n,
        rowClass: o,
        grid: p
      } = k, {element: q} = p, {rowCls: r} = p, u = ((c = l[a - 1]) == null ? void 0 : c.dataIndex) + 1 || 0, v = [], w = DateHelper.as('ms', DomHelper.getStyleValue(p.element, '--row-splice-duration')), z = k.lastVisibleRow.index + 1 - a;
    let A;
    if (b < 0) {
      b = Math.abs(b);
      const B = u >= p.store.count ? b : Math.min(b, z), C = l.slice(a, a + B), E = B ? C[C.length - 1].bottom - C[0].top : 0, F = [], G = [], H = [];
      for (let I = 0, {length: J} = C; I < J; I++) {
        const K = C[I]._elementsArray.map(cloneRowEl);
        G.push(o.new({
          'rowManager': k,
          '_elementsArray': K,
          'grid': p
        }));
        F.push(...K);
      }
      for (let L = a; L < n; L++) {
        H.push(l[L]);
        l[L].addCls('b-repositioning');
        l[L].setTop(l[L].top + E);
      }
      A = l.length > p.store.count;
      (e = F[0]) == null ? void 0 : e.getBoundingClientRect();
      q.classList.add('b-splicing-rows');
      for (let N = 0, {length: O} = C; N < O; N++) {
        G[N].setTop(((f = l[a - 1]) == null ? void 0 : f.bottom) || 0, true);
      }
      nodeAnimationCleanup = () => {
        if (nodeAnimationCleanupListener && !k.isDestroyed) {
          nodeAnimationCleanupListener = nodeAnimationCleanupListener();
          nodeAnimationCleanup = null;
          q.classList.remove('b-splicing-rows');
          F.forEach(P => P.remove());
          H.forEach(P => {
            let Q;
            return (Q = P.removeCls) == null ? void 0 : Q.call(P, 'b-repositioning');
          });
        }
      };
    } else if (b > 0) {
      const P = [];
      b = k.grid.hasVerticalOverflow ? Math.min(b, k.lastVisibleRow.index + 1 - a) : Math.min(b, Math.ceil((k.grid.scrollable.clientHeight - (((g = l[a - 1]) == null ? void 0 : g.bottom) || 0)) / k.rowHeight));
      let Q = a, R = u;
      for (let S = 0; S < b; S++, Q++, R++) {
        v.push(o.new({
          'cls': r + ' b-adding',
          'rowManager': k,
          '_top': ((j = l[a - 1]) == null ? void 0 : j.bottom) || 0,
          'grid': p,
          'index': Q,
          'dataIndex': R
        }));
      }
      for (let T = a; T < k.rowCount; T++, Q++, R++) {
        P.push(l[T]);
        l[T].addCls('b-repositioning');
        l[T].index = Q;
        l[T].dataIndex = R;
      }
      A = v.length;
      nodeAnimationCleanup = () => {
        if (nodeAnimationCleanupListener && !k.isDestroyed) {
          nodeAnimationCleanupListener = nodeAnimationCleanupListener();
          nodeAnimationCleanup = null;
          q.classList.remove('b-splicing-rows');
          v.forEach(U => !U.isDestroyed && U.removeCls('b-adding'));
          P.forEach(U => {
            let V;
            return (V = U.removeCls) == null ? void 0 : V.call(U, 'b-repositioning');
          });
        }
      };
      l.splice(a, 0, ...v);
      if (b > 0) {
        k.rowCount += b;
        k.trigger('addRows', { 'rows': v });
      }
      q.classList.add('b-splicing-rows');
    }
    nodeAnimationCleanupListener = EventHelper.on({
      'element': p.element,
      'animationcancel': nodeAnimationCleanup,
      'transitioncancel': nodeAnimationCleanup,
      'once': true,
      'expires': {
        'delay': w,
        'alt': nodeAnimationCleanup
      }
    });
    k.renderFromRow(l[a], true);
    if (A) {
      k.calculateRowCount(false, true, true);
    }
    k.estimateTotalHeight();
    return v;
  }
  removeAllRows() {
    const a = this, {topRow: b} = a, c = b ? a.refreshDetails = {
        'topRowIndex': b.dataIndex,
        'topRowTop': b.top
      } : a.refreshDetails, e = a.rows.slice();
    a.trigger('removeRows', { 'rows': e });
    a.rows.forEach(f => f.destroy());
    a.rows.length = 0;
    a.idMap = {};
    return c;
  }
  setPosition(a) {
    const {topRow: b} = this, {
        topRowIndex: c,
        topRowTop: e
      } = a;
    if (b) {
      b.setTop(e);
      b.dataIndex = c;
    }
  }
  get store() {
    return this.client.store;
  }
  get storeCount() {
    let {
      count: a,
      lazyLoad: b
    } = this.store;
    if (a && b && b.totalCount == null) {
      a += b.chunkSize;
    }
    return a;
  }
  get rows() {
    return this._rows;
  }
  getRow(a) {
    if (this.rowCount) {
      return this.rows[a - this.topIndex];
    }
  }
  getRowById(a) {
    if (a && a.isModel) {
      a = a.id;
    }
    return this.idMap[a];
  }
  getRowFromElement(a) {
    a = a.closest('.b-grid-row');
    return a && this.getRow(a.dataset.index);
  }
  getRowAt(a, b = false) {
    if (!b) {
      a -= Rectangle.from(this.grid.bodyContainer, null, true).roundPx(1).top;
      a += this.grid.scrollable.y;
    }
    a = DomHelper.roundPx(a);
    return this.rows.find(c => a >= c.top && a < c.bottom);
  }
  getRowFor(a) {
    if (a instanceof HTMLElement) {
      return this.getRowFromElement(a);
    }
    return this.getRowById(a);
  }
  getNextRow(a) {
    const b = typeof a === 'number' ? a : a.index;
    return this.getRow((b + 1) % this.rowCount);
  }
  get topRow() {
    return this.rows[0];
  }
  get bottomRow() {
    const a = Math.min(this.rowCount, this.storeCount);
    return this.rows[a - 1];
  }
  get firstVisibleRow() {
    const a = Math.ceil(this.grid.scrollable.y);
    return this.rows.find(b => b.bottom > a);
  }
  get firstFullyVisibleRow() {
    const a = Math.ceil(this.grid.scrollable.y);
    return this.rows.find(b => b.top >= a) || this.rows.find(b => b.top < a && b.bottom > a);
  }
  get lastVisibleRow() {
    const {grid: a} = this;
    return ArrayHelper.findLast(this.rows, b => b.top < a.scrollable.y + a.bodyHeight);
  }
  get lastFullyVisibleRow() {
    const {grid: a} = this;
    return ArrayHelper.findLast(this.rows, b => b.bottom < a.scrollable.y + a.bodyHeight);
  }
  offsetRows(a) {
    if (a !== 0) {
      const {rows: b} = this, {length: c} = b;
      for (let e = 0; e < c; e++) {
        b[e].offset(a);
      }
    }
    this.trigger('offsetRows', { 'offset': a });
  }
  get prependBufferHeight() {
    return this.prependRowBuffer * this.rowOffsetHeight;
  }
  get appendBufferHeight() {
    return this.appendRowBuffer * this.rowOffsetHeight;
  }
  get rowHeight() {
    return this._rowHeight;
  }
  set rowHeight(a) {
    const b = this, {
        grid: c,
        fixedRowHeight: e
      } = b, f = b.rowHeight;
    if (f === a) {
      return;
    }
    ObjectHelper.assertNumber(a, 'rowHeight');
    if (a < 10) {
      a = 10;
    }
    b.trigger('beforeRowHeight', { 'height': a });
    b.minRowHeight = b._rowHeight = a;
    if (e) {
      b.averageRowHeight = a;
    }
    if (b.rows.length) {
      const g = c.scrollable.y, i = b.getRowAt(g, true), j = i ? i.top - g : 0;
      let k, l;
      if (e) {
        k = a;
        l = f;
      } else {
        l = k = b.averageRowHeight;
        b.clearKnownHeights();
        k *= a / f;
      }
      b.calculateRowCount(false, true, true);
      b.topRow.setTop(b.topRow.dataIndex * (k + c._rowBorderHeight), true);
      b.refresh();
      const n = g * (k / l);
      if (n !== g) {
        c.scrollRowIntoView(i.id, {
          'block': 'start',
          'edgeOffset': j
        });
      }
    }
    b.trigger('rowHeight', {
      'height': a,
      'oldHeight': f
    });
  }
  get rowOffsetHeight() {
    return Math.floor(this.preciseRowOffsetHeight);
  }
  get preciseRowOffsetHeight() {
    return (this.averageRowHeight || this._rowHeight) + this.grid._rowBorderHeight;
  }
  get minRowOffsetHeight() {
    return (this.minRowHeight || this._rowHeight) + this.grid._rowBorderHeight;
  }
  get allHeightsKnown() {
    return this.fixedRowHeight || this.heightMap.size >= this.storeCount;
  }
  storeKnownHeight(a, b) {
    const c = this, {heightMap: e} = c;
    if (!c.fixedRowHeight) {
      if (e.has(a)) {
        c.totalKnownHeight -= e.get(a);
      }
      e.set(a, b);
      c.totalKnownHeight += b;
      if (b < c.minRowHeight) {
        c.minRowHeight = b;
      }
      c.averageRowHeight = c.totalKnownHeight / e.size;
    }
  }
  getOffsetHeight(a) {
    const b = this;
    return (a && b.heightMap.get(a.id) || a && b.grid.getRowHeight(a) || b.averageRowHeight || b.rowHeight) + b.grid._rowBorderHeight;
  }
  invalidateKnownHeight(a) {
    const b = this;
    if (!b.fixedRowHeight) {
      const {heightMap: c} = b;
      a = ArrayHelper.asArray(a);
      a.forEach(e => {
        if (e) {
          if (c.has(e.id)) {
            b.totalKnownHeight -= c.get(e.id);
            c.delete(e.id);
          }
        }
      });
      b.averageRowHeight = b.totalKnownHeight / c.size;
    }
  }
  clearKnownHeights() {
    this.heightMap.clear();
    this.averageRowHeight = this.totalKnownHeight = 0;
  }
  calculateTop(a) {
    if (this.fixedRowHeight) {
      return a * this.rowOffsetHeight;
    }
    const {storage: b} = this.store;
    let c = 0;
    for (let e = 0; e < a; e++) {
      const f = b.getAt(e);
      c += this.getOffsetHeight(f);
    }
    return Math.floor(c);
  }
  getRecordCoords(a, b = false, c = false) {
    const e = this, f = e.getRowById(a);
    let g = e.client._bodyRectangle;
    if (!b) {
      g = e.client.refreshBodyRectangle();
    }
    if (f) {
      return new Rectangle(g.x, b ? Math.round(f.top) : Math.round(f.top + g.y - e.client.scrollable.y), g.width, f.offsetHeight);
    }
    return e.getRecordCoordsByIndex(e.store.indexOf(a), b, c);
  }
  getRecordCoordsByIndex(a, b = false, c = false) {
    let e;
    const f = this, {
        topRow: g,
        bottomRow: i
      } = f, j = f.client._bodyRectangle, {id: k} = f.store.getAt(a), l = f.preciseRowOffsetHeight, n = g.dataIndex, o = i.dataIndex, p = Math.floor((e = f.heightMap.get(k)) != null ? e : l);
    let q;
    if (f.allHeightsKnown && !c) {
      const u = f.calculateTop(a);
      q = b ? u : u + j.y - f.client.scrollable.y;
    } else {
      const v = a > o ? {
          'index': a - o - 1,
          'y': i.bottom,
          'from': 'bottomRow'
        } : a > n / 2 ? {
          'index': a - n,
          'y': g.top,
          'from': 'topRow'
        } : {
          'index': a,
          'y': 0,
          'from': 'top'
        }, w = Math.floor(v.y + v.index * l);
      q = b ? v.from === 'topRow' ? w + l - p : w : w + j.y - f.client.scrollable.y;
    }
    const r = new Rectangle(j.x, q, j.width, p);
    r.virtual = true;
    r.block = r.bottom < j.y ? 'start' : r.y > j.bottom ? 'end' : 'nearest';
    return r;
  }
  get totalHeight() {
    return this._totalHeight;
  }
  forEach(a) {
    this.rows.forEach(a);
  }
  [Symbol.iterator]() {
    return this.rows[Symbol.iterator]();
  }
  refreshCell(a, b) {
    const c = new GridLocation({
      'grid': this.grid,
      'record': a,
      'columnId': b
    });
    return Boolean(c.cell && c.row.renderCell(c));
  }
  returnToTop() {
    const a = this;
    a.topIndex = 0;
    a.lastScrollTop = 0;
    if (a.topRow) {
      a.topRow.dataIndex = 0;
      a.topRow.setTop(0, true);
    }
    a.refresh();
    a.grid.scrollable.y = 0;
  }
  renderFromRecord(a) {
    const b = this.getRowById(a.id);
    if (b) {
      this.renderFromRow(b);
    }
  }
  renderFromRow(a = null, b) {
    const c = this, {
        rows: e,
        store: f,
        topIndex: g,
        topRow: j,
        storeCount: k
      } = c;
    let l = a ? e.indexOf(a) : 0;
    if (c.calculateRowCount(false, k < e.length && !b, true) === 0 || l >= e.length) {
      c.estimateTotalHeight(true);
      return;
    }
    if (c.topIndex < g) {
      c.topRow.setTop(c.calculateTop(c.topIndex), true);
      l = 0;
      a = c.topRow;
    } else if (a && e.indexOf(a) < 0) {
      a = e[l] || j;
    }
    let n = a ? a.dataIndex : e[0].dataIndex;
    const o = k - n - 1, p = Math.min(e.length - 1, l + o);
    let q = e.length - p - 1, r = l > 0 ? e[l - 1].bottom : e[l].top, u;
    const v = [];
    for (let w = l; w <= p; w++) {
      u = e[w];
      u.dataIndex = n;
      if (b) {
        v[w] = r;
      } else {
        u.setTop(r, true);
      }
      u.render(n, f.getAt(n++), false);
      r += u.offsetHeight;
    }
    if (b) {
      e[l].element.getBoundingClientRect();
      for (let z = l; z <= p; z++) {
        e[z].setTop(v[z], true);
      }
    }
    while (q-- > 0) {
      c.displayRecordAtTop();
    }
    if (c.bottomRow.bottom < c.viewHeight) {
      c.calculateRowCount();
    }
    c.estimateTotalHeight(true);
    c.trigger('renderDone');
  }
  renderRows(a) {
    let b, c = false;
    a = Array.from(a);
    a.sort((e, f) => e.dataIndex - f.dataIndex);
    for (const e of a) {
      b = e.height;
      e.render(null, null, false);
      c |= e.height !== b;
    }
    if (c) {
      this.translateFromRow(a[0]);
    }
    this.trigger('renderDone');
  }
  bufferedTranslateFromRow() {
    this.translateFromRow(this.topRow);
  }
  translateFromRow(a, b = false) {
    const c = this;
    let e = a.bottom, f, g;
    for (g = a.dataIndex + 1, f = c.getRow(g); f; f = c.getRow(++g)) {
      e = f.translate(e);
    }
    if (!b) {
      c.estimateTotalHeight(true);
    }
  }
  refresh(a = false) {
    const b = this, {topRow: c} = b;
    if (!c && !a || b.grid.refreshSuspended) {
      return;
    }
    b.idMap = {};
    b.renderFromRow(c);
    b.trigger('refresh');
  }
  jumpToPosition(a, b) {
    const c = this, {
        store: e,
        heightMap: f,
        storeCount: g
      } = c;
    if (c.allHeightsKnown && !c.fixedRowHeight) {
      const i = a - c.prependBufferHeight, j = c.grid._rowBorderHeight;
      let k = 0, l = 0;
      while (k < i) {
        const o = e.records[l];
        k += f.get(o.id) + j;
        l++;
      }
      const n = Math.max(Math.min(l, g - c.rowCount), 0);
      c.lastScrollTop = a;
      c.topRow.dataIndex = c.topIndex = n;
      c.topRow.setTop(c.calculateTop(n), false);
      c.refresh();
    } else {
      const p = c.preciseRowOffsetHeight, q = b == null ? Math.floor(a / p) - c.prependRowBuffer : b - Math.floor(c.rowCount / 2), r = Math.max(Math.min(q, g - c.rowCount), 0), u = c.client.scrollable.y, v = Math.min(c.client._bodyRectangle.height + u + c.appendBufferHeight, c.totalHeight);
      c.lastScrollTop = a;
      c.topRow.dataIndex = c.topIndex = r;
      c.topRow.setTop(Math.floor(r * p), false);
      c.refresh();
      if (c.bottomRow.bottom < v) {
        c.calculateRowCount(false, false, false);
        while (c.bottomRow.bottom < v && c._rows[c.prependRowBuffer].top < u && c.bottomRow.dataIndex < g - 1) {
          c.displayRecordAtBottom();
        }
      }
      c.estimateTotalHeight();
    }
    if (b != null) {
      const {scrollable: w} = c.grid, z = c.getRow(b), A = z && Rectangle.from(z._elementsArray[0]).center.y, B = w.viewport.center.y;
      if (z) {
        w.y = a = Math.floor(w.y + (A - B));
      }
    }
    return a;
  }
  warpIfNeeded(a) {
    const b = this, c = {
        'newScrollTop': a,
        'deltaTop': a - b.lastScrollTop
      };
    if (Math.abs(c.deltaTop) > b.rowCount * b.rowOffsetHeight * 3) {
      let e;
      if (b.scrollTargetRecordId) {
        e = b.store.indexOf(b.scrollTargetRecordId);
      }
      b.grid.onFocusedRowDerender();
      c.newScrollTop = b.jumpToPosition(a, e);
      c.deltaTop = 0;
    }
    return c;
  }
  updateRenderedRows(a, b, c = false) {
    const e = this;
    if (e.rowCount === 0) {
      return 0;
    }
    let f = e.totalHeight;
    if (b || Math.abs(a - e.lastScrollTop) >= e.rowOffsetHeight || e.topRow.top > a || e.bottomRow.bottom < a + e.viewHeight) {
      const g = e.warpIfNeeded(a);
      e.scrollTargetRecordId = null;
      e.lastScrollTop = g.newScrollTop;
      if (g.deltaTop > 0) {
        e.fillBelow(g.newScrollTop);
      } else if (g.deltaTop < 0) {
        e.fillAbove(g.newScrollTop);
      }
      if (!e.fixedRowHeight && !c) {
        e.correctError(g, a);
      }
      f = e.estimateTotalHeight();
    }
    return f;
  }
  correctError(a, b) {
    const c = this;
    let e = 0;
    if (c.allHeightsKnown) {
      e = c.topRow.top - c.calculateTop(c.topRow.dataIndex);
    } else {
      if (a.deltaTop < 0 && b < c.viewHeight * 2 || a.deltaTop > 0 && b > c.totalHeight - c.viewHeight * 2 - 3) {
        e = c.topRow.top - c.calculateTop(c.topRow.dataIndex);
      }
    }
    if (e) {
      c.offsetRows(-e);
      c.grid.scrollable.y = c.lastScrollTop = c.grid.scrollable.y - e;
    }
  }
  fillAbove(a) {
    const b = this, {rows: c} = b, e = Math.min(b.prependRowBuffer, c.length - 1);
    while (c[e].top > a && b.topIndex > 0) {
      b.displayRecordAtTop();
    }
    b.trigger('renderDone');
  }
  fillBelow(a) {
    const b = this, {
        rows: c,
        storeCount: e
      } = b, {length: f} = c, g = Math.min(f - Math.min(b.appendRowBuffer, f - 1), f - 1), i = a + b.viewHeight;
    while (c[g].top < i && b.bottomRow.dataIndex < e - 1) {
      b.displayRecordAtBottom();
    }
    b.trigger('renderDone');
  }
  estimateTotalHeight(a = false) {
    const b = this;
    if (b.grid.renderingRows) {
      return;
    }
    const {
        bottomRow: c,
        storeCount: e
      } = b, f = e - b.heightMap.size;
    let g;
    if (b.fixedRowHeight) {
      g = e * b.rowOffsetHeight;
    } else {
      g = b.totalKnownHeight + b.heightMap.size * b.grid._rowBorderHeight + f * b.preciseRowOffsetHeight;
      if (c && f) {
        const i = c.bottom;
        if (i > g || b.topIndex + b.rowCount >= e && g > i && i > 0) {
          g = i;
          if (c.dataIndex < e - 1) {
            g += (e - 1 - c.dataIndex) * b.preciseRowOffsetHeight;
          }
        }
      }
      g = Math.floor(g);
    }
    if (g !== b.totalHeight) {
      if (b.trigger('changeTotalHeight', {
          'totalHeight': g,
          'immediate': a
        }) !== false) {
        b._totalHeight = g;
      }
    }
    return g;
  }
  displayRecordAtTop() {
    let a;
    const b = this, {grid: c} = b, e = b.topIndex - 1, f = b.store.getAt(e), g = b.bottomRow, i = g.top;
    b.trigger('beforeTranslateRow', {
      'row': g,
      'newRecord': f
    });
    if (g.dataIndex === ((a = c.focusedCell) == null ? void 0 : a.rowIndex)) {
      c.onFocusedRowDerender();
    }
    g._top = b.topRow.top - b.getOffsetHeight(f);
    g.estimatedTop = !b.fixedRowHeight;
    g.render(e, f, false);
    g._top = i;
    g.setBottom(b.topRow.top);
    g.estimatedTop = false;
    b.topIndex--;
    b._rows.unshift(b._rows.pop());
    b.fixIndices();
    return g.offsetHeight;
  }
  displayRecordAtBottom() {
    let a;
    const b = this, {grid: c} = b, e = b.topIndex + b.rowCount, f = b.store.getAt(e), g = b.topRow;
    b.trigger('beforeTranslateRow', {
      'row': g,
      'newRecord': f
    });
    if (g.dataIndex === ((a = c.focusedCell) == null ? void 0 : a.rowIndex)) {
      c.onFocusedRowDerender();
    }
    g.dataIndex = e;
    g.setTop(b.bottomRow.bottom);
    g.render(e, f, false);
    b.topIndex++;
    b._rows.push(b._rows.shift());
    b.fixIndices();
    return g.offsetHeight;
  }
  fixIndices() {
    for (let a = 0, {rows: b} = this, {length: c} = b; a < c; a++) {
      b[a].index = a;
    }
  }
};
RowManager.featureClass = '';
RowManager._$name = 'RowManager';
const xAxis2 = { 'x': 1 };
const subGridFilter = a => a.isSubGrid;
const GridScroller = class extends Scroller {
  static $name = 'GridScroller';
  addScroller(a) {
    (this.xScrollers || (this.xScrollers = [])).push(a);
  }
  addPartner(a, b = xAxis2, c = true) {
    if (typeof b === 'string') {
      b = { [b]: 1 };
    }
    if (b.x) {
      a.owner.initScroll();
      const e = this.widget.items.filter(subGridFilter), f = a.widget.items.filter(subGridFilter);
      for (let g = 0, {length: j} = e; g < j; g++) {
        e[g].scrollable.addPartner(f[g].scrollable, 'x');
      }
    }
    if (c && DomHelper.scrollBarWidth && a.isGridScroller) {
      a.ion({
        'overflowChange': 'onPartnerOverflowChange',
        'thisObj': this
      });
      this.onPartnerOverflowChange({
        'source': a,
        'y': a.hasOverflowY
      });
    }
    super.addPartner(a, {
      ...b,
      'x': false
    });
  }
  onPartnerOverflowChange({
    source: a,
    y: b
  }) {
    const c = this, e = c.hasOverflowY, f = b;
    if (e && !f) {
      a.overflowY = 'scroll';
    }
    if (!e && f) {
      c.overflowY = 'scroll';
    } else if (!e && !f) {
      c.overflowY = a.overflowY = true;
    }
    c.owner.refreshVirtualScrollbars();
  }
  removePartner(a) {
    if (DomHelper.scrollBarWidth && a.isGridScroller) {
      a.un({
        'overflowChange': 'onPartnerOverflowChange',
        'thisObj': this
      });
    }
    this.xScrollers.forEach((b, c) => {
      if (!b.isDestroyed) {
        b.removePartner(a.xScrollers[c]);
      }
    });
    super.removePartner(a);
  }
  updateOverflowX(a) {
    let b;
    const c = a === false;
    (b = this.xScrollers) == null ? void 0 : b.forEach(e => e.overflowX = c ? 'hidden' : 'hidden-scroll');
    this.widget.virtualScrollers.classList.toggle('b-hide-display', c);
  }
  scrollIntoView(a, b) {
    if (a.nodeType === Element.ELEMENT_NODE && this.element.contains(a)) {
      for (const c of this.xScrollers) {
        if (c.element.contains(a)) {
          return c.scrollIntoView(a, b);
        }
      }
    } else {
      return super.scrollIntoView(a, b);
    }
  }
  hasOverflow(a = 'y') {
    return a === 'y' ? this.scrollHeight > this.clientHeight : false;
  }
  set x(a) {
    if (this.xScrollers) {
      this.xScrollers[0].x = a;
    }
  }
  get x() {
    return this.xScrollers ? this.xScrollers[0].x : 0;
  }
};
GridScroller._$name = 'GridScroller';
const Header = class extends Bar {
  static type = 'gridheader';
  static $name = 'Header';
  get subGrid() {
    return this._subGrid;
  }
  set subGrid(a) {
    this._subGrid = this.owner = a;
  }
  get region() {
    let a;
    return (a = this.subGrid) == null ? void 0 : a.region;
  }
  changeElement(a, b) {
    const {region: c} = this;
    this.getConfig('columns');
    return super.changeElement({
      'className': {
        'b-grid-header-scroller': 1,
        ['b-grid-header-scroller-' + c]: c
      },
      'children': [{
          'reference': 'headersElement',
          'className': {
            'b-grid-headers': 1,
            ['b-grid-headers-' + c]: c
          },
          'dataset': {
            'region': c,
            'reference': 'headersElement',
            'maxDepth': this.maxDepth
          }
        }]
    }, b);
  }
  get overflowElement() {
    return this.headersElement;
  }
  getColumnConfig(a) {
    const {
        id: b,
        align: c,
        resizable: e,
        isLeaf: f,
        isParent: g,
        isLastInSubGrid: i,
        cls: j,
        childLevel: k,
        field: l,
        tooltip: n,
        children: o,
        isFocusable: p,
        grid: q
      } = a, r = q == null ? void 0 : q.focusedCell, u = (r == null ? void 0 : r.rowIndex) === -1 && (r == null ? void 0 : r.column) === a, v = {};
    if (a.isVisible) {
      if (a.flex) {
        v.flex = a.flex;
      } else if (a.width) {
        v.width = DomHelper.setLength(a.width);
      }
      return {
        'style': v,
        'className': {
          'b-grid-header': 1,
          'b-grid-header-parent': g,
          ['b-level-' + k]: 1,
          ['b-depth-' + a.meta.depth]: 1,
          ['b-grid-header-align-' + c]: c,
          'b-grid-header-resizable': e && f,
          [j]: j,
          'b-collapsible': a.collapsible,
          'b-last-parent': g && i,
          'b-last-leaf': f && i
        },
        'role': p ? 'columnheader' : 'presentation',
        'aria-sort': 'none',
        'aria-label': a.ariaLabel,
        [p ? 'tabIndex' : '']: u ? 0 : -1,
        'dataset': {
          ...Tooltip.encodeConfig(n),
          'columnId': b,
          [l ? 'column' : '']: l
        },
        'children': [
          {
            'className': 'b-grid-header-text',
            'children': [{
                [q && p ? 'id' : '']: (q == null ? void 0 : q.id) + '-column-' + a.id,
                'className': 'b-grid-header-text-content'
              }]
          },
          o ? {
            'className': 'b-grid-header-children',
            'children': o.map(w => this.getColumnConfig(w)),
            'syncOptions': { 'syncIdField': 'columnId' }
          } : null,
          { 'className': 'b-grid-header-resize-handle' }
        ]
      };
    }
  }
  calculateMinWidthForSafari() {
    let a = 0;
    this.columns.visibleColumns.forEach(b => {
      a += b.calculateMinWidth();
    });
    return a;
  }
  fixHeaderWidths() {
    this.fixCellWidths();
  }
  refreshHeaders() {
    const a = this;
    a.columns.traverse(b => a.refreshColumn(b));
    a.fixHeaderWidths();
  }
  refreshColumn(a) {
    const b = this, c = b.getBarCellElement(a.id);
    if (c) {
      let e = a.headerText;
      if (a.headerRenderer) {
        e = a.headerRenderer.call(a.thisObj || b, {
          'column': a,
          'headerElement': c
        });
      }
      if (a.headerWidgetMap) {
        Object.values(a.headerWidgetMap).forEach(g => {
          g.render(a.textWrapper);
        });
      }
      if (a.icon) {
        e = '<i class="' + StringHelper.encodeHtml(a.icon) + '"></i>' + (e || '');
      }
      const f = c.querySelector('.b-grid-header-text-content');
      if (f) {
        f.innerHTML = e || '';
      }
    }
  }
  get columns() {
    const a = this, b = super.columns;
    if (!a.columnsDetacher) {
      a.columnsDetacher = b.ion({
        'change'() {
          a.initDepths();
        },
        'thisObj': a
      });
      a.initDepths();
    }
    return b;
  }
  set columns(a) {
    super.columns = a;
  }
  initDepths(a = this.columns.topColumns, b = null) {
    const c = this;
    let e = 0;
    if (b == null ? void 0 : b.meta) {
      b.meta.depth++;
    }
    for (const f of a) {
      const {meta: g} = f;
      g.depth = 0;
      if (f.children) {
        c.initDepths(f.children.filter(c.columns.chainedFilterFn), f);
        if (g.depth && b) {
          b.meta.depth += g.depth;
        }
      }
      if (g.depth > e) {
        e = g.depth;
      }
    }
    if (!b) {
      c.maxDepth = e;
    }
    return e;
  }
  getHeader(a) {
    return this.getBarCellElement(a);
  }
  get contentElement() {
    return this.element.firstElementChild;
  }
  refreshContent() {
    const a = this;
    DomSync.sync({
      'domConfig': {
        'children': a.columns.topColumns.map(b => a.getColumnConfig(b)),
        'onlyChildren': true
      },
      'targetElement': a.contentElement,
      'strict': true,
      'syncIdField': 'columnId',
      'releaseThreshold': 0
    });
    a.refreshHeaders();
  }
  onInternalPaint({firstPaint: a}) {
    if (a) {
      this.refreshContent();
    }
  }
};
Header.initClass();
Header._$name = 'Header';
const gridBodyElementEventHandlers = {
  'touchstart': 'onElementTouchStart',
  'touchmove': 'onElementTouchMove',
  'touchend': 'onElementTouchEnd',
  'pointerover': 'onElementMouseOver',
  'mouseout': 'onElementMouseOut',
  'mousedown': 'onElementMouseDown',
  'mousemove': 'onElementMouseMove',
  'mouseup': 'onElementMouseUp',
  'click': 'onHandleElementClick',
  'dblclick': 'onElementDblClick',
  'keyup': 'onElementKeyUp',
  'keypress': 'onElementKeyPress',
  'contextmenu': 'onElementContextMenu',
  'pointerdown': 'onElementPointerDown',
  'pointerup': 'onElementPointerUp'
};
const eventProps2 = [
  'pageX',
  'pageY',
  'clientX',
  'clientY',
  'screenX',
  'screenY'
];
const textInputs = 'input:is(:not([type]),[type="text"],[type="number"],[type="date"],[type="datetime-local"],[type="email"],[type="month"],[type="password"],[type="range"],[type="search"],[type="tel"],[type="time"],[type="url"],[type="week"])';
const textKeys = {
  'ArrowUp': 1,
  'ArrowDown': 1,
  'ArrowLeft': 1,
  'ArrowRight': 1,
  'Backspace': 1,
  'Delete': 1
};
function toggleHover(a, b = true) {
  a == null ? void 0 : a.classList.toggle('b-hover', b);
}
function setCellHover(a, b, c = true) {
  b && a && toggleHover(b.getCell(a), c);
}
const GridElementEvents_default = a => class b extends (a || Base) {
  static get $name() {
    return 'GridElementEvents';
  }
  static get configurable() {
    return {
      'hoveredCell': null,
      'longPressTime': 400,
      'enableUndoRedoKeys': true,
      'hoverCls': 'b-hover',
      'keyMap': {
        'Ctrl+z': 'undoRedoKeyPress',
        'Ctrl+Shift+z': 'undoRedoKeyPress',
        ' ': {
          'handler': 'clickCellByKey',
          'weight': 1000
        }
      }
    };
  }
  initInternalEvents() {
    const c = Object.keys(gridBodyElementEventHandlers), e = c.length, f = {
        'element': this.bodyElement,
        'thisObj': this
      };
    for (let g = 0; g < e; g++) {
      const j = c[g];
      f[j] = { 'handler': 'handleEvent' };
      if (j.startsWith('touch')) {
        f[j].passive = false;
      }
    }
    EventHelper.on(f);
    EventHelper.on({
      'mouseenter': 'onGridBodyMouseEnterOrLeave',
      'mouseleave': 'onGridBodyMouseEnterOrLeave',
      'focusin': 'onGridBodyFocusIn',
      'element': this.bodyElement,
      'thisObj': this,
      'capture': true
    });
  }
  getCellDataFromEvent(c, e = false) {
    let f, g;
    const i = this, {target: j} = c, k = j.classList.contains('b-grid-row');
    let l = j.closest('.b-grid-cell');
    if (!l && e && !k && !j.classList.contains('b-grid-subgrid')) {
      const {columns: n} = i, {
          top: o,
          left: p,
          right: q,
          bottom: r
        } = i.bodyContainer.getBoundingClientRect();
      let u, {
          x: v,
          y: w
        } = c;
      if (v >= p && v <= q) {
        w = u = Math.ceil(i[(w < o ? 'first' : 'last') + 'FullyVisibleRow'].element.getBoundingClientRect().y);
      } else if (w >= o && w <= r) {
        v = u = Math.ceil(n.visibleColumns[v < p ? 0 : n.visibleColumns.length - 1].element.getBoundingClientRect().x);
      }
      if (u !== void 0) {
        l = (f = DomHelper.childFromPoint(j, c.offsetX, c.offsetY)) == null ? void 0 : f.closest('.b-grid-cell');
      }
    } else if (k) {
      l = (g = DomHelper.childFromPoint(j, c.offsetX, c.offsetY - j.offsetHeight / 2)) == null ? void 0 : g.closest('.b-grid-cell');
    }
    if (l) {
      return i.getCellDataFromElement(l);
    }
  }
  getCellDataFromElement(c) {
    const e = this;
    if (c = c.closest('.b-grid-cell')) {
      const f = DomDataStore.get(c), {
          id: g,
          columnId: i
        } = f, j = e.store.getById(g), k = e.columns.getById(i);
      return j ? {
        'cellElement': c,
        'cellData': f,
        'columnId': i,
        'id': g,
        'record': j,
        'column': k,
        'cellSelector': {
          'id': g,
          'columnId': i
        }
      } : null;
    }
  }
  getHeaderDataFromEvent(c) {
    const e = c.target.closest('.b-grid-header');
    if (e) {
      const f = ObjectHelper.assign({}, e.dataset), {columnId: g} = f, i = this.columns.getById(g);
      return i ? {
        'headerElement': e,
        'headerData': f,
        'columnId': g,
        'column': i
      } : null;
    }
  }
  handleEvent(c) {
    if (!this.disabled && gridBodyElementEventHandlers[c.type]) {
      this[gridBodyElementEventHandlers[c.type]](c);
    }
  }
  onElementTouchStart(c) {
    const e = this;
    DomHelper.isTouchEvent = true;
    if (c.touches.length === 1) {
      e.longPressTimeout = e.setTimeout(() => {
        e.onElementLongPress(c);
        c.preventDefault();
        e.longPressPerformed = true;
      }, e.longPressTime);
    }
  }
  onElementTouchMove(c) {
    const e = this, {lastTouchTarget: f} = e, g = c.changedTouches[0], {
        pageX: i,
        pageY: j
      } = g, k = c.target.getRootNode().elementFromPoint(i, j);
    if (e.longPressTimeout) {
      e.clearTimeout(e.longPressTimeout);
      e.longPressTimeout = null;
    }
    if (k !== f && f) {
      const l = new MouseEvent('mouseout', ObjectHelper.copyProperties({
        'relatedTarget': k,
        'pointerType': 'touch',
        'bubbles': true
      }, g, eventProps2));
      l.preventDefault = () => c.preventDefault();
      k == null ? void 0 : k.dispatchEvent(l);
      const n = new MouseEvent('mouseover', ObjectHelper.copyProperties({
        'relatedTarget': f,
        'pointerType': 'touch',
        'bubbles': true
      }, g, eventProps2));
      n.preventDefault = () => c.preventDefault();
      f == null ? void 0 : f.dispatchEvent(n);
    }
    e.lastTouchTarget = k;
  }
  onElementTouchEnd(c) {
    const e = this;
    if (e.longPressPerformed) {
      if (c.cancelable) {
        c.preventDefault();
      }
      e.longPressPerformed = false;
    }
    if (e.longPressTimeout) {
      e.clearTimeout(e.longPressTimeout);
      e.longPressTimeout = null;
    }
  }
  onElementLongPress(c) {
  }
  triggerCellMouseEvent(c, e, f = this.getCellDataFromEvent(e), g = 'cell') {
    let i;
    const j = this;
    if (f) {
      const k = j.columns.getById(f.columnId), l = {
          'grid': j,
          'record': f.record,
          'column': k,
          'cellSelector': f.cellSelector,
          'cellElement': f.cellElement,
          'target': e.target,
          'event': e
        };
      j.trigger(g + StringHelper.capitalize(c), l);
      if (g === 'cell' && c === 'click') {
        (i = k.onCellClick) == null ? void 0 : i.call(k, l);
      }
    }
  }
  onElementMouseDown(c) {
    const e = this, f = e.getCellDataFromEvent(c);
    e.skipFocusSelection = true;
    if (e.isScrollbarOrRowBorderOrSplitterClick(c)) {
      c.preventDefault();
    } else {
      e.triggerCellMouseEvent('mousedown', c, f);
      if (f && !c.defaultPrevented) {
        e.onFocusGesture(c);
      }
    }
  }
  isScrollbarOrRowBorderOrSplitterClick({
    target: c,
    x: e,
    y: f
  }) {
    if (c.closest('.b-grid-splitter') || c.matches('.b-grid-row, .b-virtual-width')) {
      return true;
    }
    if (c.matches('.b-vertical-overflow')) {
      const g = c.getBoundingClientRect();
      return e > g.right - DomHelper.scrollBarWidth;
    } else if (c.matches('.b-horizontal-overflow')) {
      const i = c.getBoundingClientRect();
      return f > i.bottom - DomHelper.scrollBarWidth - 1;
    }
  }
  onElementMouseMove(c) {
    this.mouseMoveEvent = c;
  }
  onElementMouseUp(c) {
  }
  onElementPointerDown(c) {
  }
  onElementPointerUp(c) {
  }
  onHandleElementClick(c) {
    if (this.trigger('beforeElementClick', { 'event': c }) !== false) {
      this.onElementClick(c);
    }
  }
  onElementClick(c) {
    let e;
    const f = this, g = f.getCellDataFromEvent(c);
    if (g) {
      return f.triggerCellMouseEvent('click', c, g);
    } else {
      const i = (e = c.target) == null ? void 0 : e.closest('.b-grid-header'), j = i && f.columns.getById(i.dataset.columnId);
      if (j) {
        return f.trigger('headerClick', {
          'domEvent': c,
          'column': j
        });
      }
    }
  }
  onFocusGesture(c) {
    let e;
    const f = this, {navigationEvent: g} = f, {target: i} = c, j = c.button === 2, k = !j && i.matches('.b-icon-tree-expand, .b-icon-tree-collapse'), l = !document.hasFocus() && BrowserHelper.isMac && j;
    if (k || l) {
      c.preventDefault();
    } else {
      f.navigationEvent = c;
      const n = new GridLocation(i);
      if (j || ((e = f.focusedCell) == null ? void 0 : e.equals(n))) {
        const o = { 'scroll': false };
        if (globalThis.TouchEvent && c instanceof MouseEvent && g instanceof TouchEvent && i === g.target) {
          o.doSelect = false;
        }
        f.focusCell(n, o);
      }
    }
  }
  onElementDblClick(c) {
    const {target: e} = c;
    this.triggerCellMouseEvent('dblClick', c);
    if (e.classList.contains('b-grid-header-resize-handle')) {
      const f = e.closest('.b-grid-header'), g = this.columns.getById(f.dataset.columnId);
      if (g.resizeToFitContent()) {
        this.trigger('columnResize', {
          'column': g,
          'domEvent': c
        });
      }
    }
  }
  shouldHover(c) {
    return (typeof c.buttons !== 'number' || c.buttons === 0) && c.pointerType !== 'touch';
  }
  onElementMouseOver(c) {
    let e;
    if (!this.scrolling) {
      const f = this, {hoveredCell: g} = f, i = this.shouldHover(c);
      let j = c.target.closest('.b-grid-cell');
      f._lastMouseOverOutEvent = c;
      if (!j && c.target.classList.contains('b-grid-row')) {
        j = (e = DomHelper.childFromPoint(c.target, c.offsetX, c.offsetY - 2)) == null ? void 0 : e.closest('.b-grid-cell');
      }
      if (j) {
        if (i) {
          f.hoveredCell = j;
        }
        if (g !== f.hoveredCell) {
          f.triggerCellMouseEvent('mouseOver', c);
        }
      }
      f.trigger('mouseOver', { 'event': c });
    }
  }
  onElementMouseOut(c) {
    let e;
    const f = this, {
        target: g,
        relatedTarget: i
      } = c, j = (typeof c.buttons !== 'number' || c.buttons === 0) && c.pointerType !== 'touch';
    f._lastMouseOverOutEvent = c;
    if (j && i && g.matches('.b-grid-cell') && !g.contains(i)) {
      if (!i.matches('.b-grid-row')) {
        f.hoveredCell = i.closest('.b-grid-cell');
      }
    } else if (!(i == null ? void 0 : i.matches('.b-grid-row,.b-grid-cell')) && !((e = f.hoveredCell) == null ? void 0 : e.contains(i))) {
      f.hoveredCell = null;
    }
    if (!f.scrolling) {
      if ((i == null ? void 0 : i.closest('.b-grid-cell')) !== (g == null ? void 0 : g.closest('.b-grid-cell'))) {
        if (g == null ? void 0 : g.closest('.b-grid-cell')) {
          f.triggerCellMouseEvent('mouseOut', c);
        }
      }
      f.trigger('mouseOut', { 'event': c });
    }
  }
  updateHoveredCell(c, e) {
    let f, g, i;
    const j = this, {selectionMode: k} = j, l = k.rowNumber && ((f = j.columns.find(q => q.type == 'rownumber')) == null ? void 0 : f.id), n = k.checkbox && ((g = j.checkboxSelectionColumn) == null ? void 0 : g.id);
    let o, p;
    if (e) {
      toggleHover(e, false);
      const q = DomDataStore.get(e);
      o = q.row;
      if (o && !o.isDestroyed) {
        setCellHover(l, o, false);
        setCellHover(n, o, false);
      }
      if (q == null ? void 0 : q.columnId) {
        toggleHover((i = j.columns.getById(q.columnId)) == null ? void 0 : i.element, false);
      }
    }
    if (j._hoveredRow && !j._hoveredRow.isDestroyed) {
      j.toggleRowHover(j._hoveredRow, false);
    }
    j._hoveredRow = null;
    if (c && !j.scrolling) {
      const r = DomDataStore.get(c);
      p = r.row;
      if (p) {
        if (k.cell && r.columnId !== l && r.columnId !== n) {
          const u = j.columns.getById(r.columnId);
          toggleHover(c);
          setCellHover(n, p);
          setCellHover(l, p);
          if ((u == null ? void 0 : u.element) && u.headerHoverable !== false) {
            toggleHover(u.element);
          }
        } else {
          j._hoveredRow = p;
          j.toggleRowHover(p, true);
        }
      } else {
        j.hoveredCell = null;
      }
    }
    if (p !== o) {
      const {_lastMouseOverOutEvent: v} = j;
      if (o && !o.isDestroyed) {
        j.trigger('rowMouseLeave', {
          'event': v,
          'record': o.cellContext.record,
          'cellElement': e
        });
      }
      if (p) {
        const w = j.getCellDataFromEvent(v);
        j.triggerCellMouseEvent('mouseEnter', v, w, 'row');
      }
    }
  }
  toggleRowHover(c, e) {
    if (this.hoverCls) {
      c.toggleCls(this.hoverCls, e);
    }
  }
  keyMapOnKeyDown(c) {
    const {target: e} = c;
    if (this.element.contains(e)) {
      this.onElementKeyDown(c);
      if (textKeys[c.key] && DomHelper.isEditable(e) && !e.getAttribute('readOnly') && this.matchKeyMapEntry(c) && e.matches(textInputs)) {
        if (!c.key.startsWith('Arrow') && !c.key === 'Backspace') {
          c.preventDefault();
        }
        c.handled = true;
        c.stopPropagation();
        return false;
      } else {
        super.keyMapOnKeyDown(c);
      }
    }
  }
  onElementKeyDown(c) {
    let e, f;
    if (c.handled || !this.element.contains(c.target)) {
      return;
    }
    const g = this, i = g.focusedCell;
    if ((i == null ? void 0 : i.isCell) && !i.isActionable) {
      const j = c.target.closest('.b-grid-cell') ? g.getCellDataFromEvent(c) : g.getCellDataFromElement(i.cell);
      (f = j == null ? void 0 : (e = j.column).onCellKeyDown) == null ? void 0 : f.call(e, {
        ...j,
        'event': c
      });
    }
  }
  undoRedoKeyPress(c) {
    let e;
    const {stm: f} = this.store;
    if (f && this.enableUndoRedoKeys && !((e = this.features.cellEdit) == null ? void 0 : e.isEditing)) {
      f.onUndoKeyPress(c);
      return true;
    }
    return false;
  }
  clickCellByKey(c) {
    const e = this, f = e.focusedCell, g = f == null ? void 0 : f.cell, i = e.columns.getById(g.dataset.columnId);
    if ((f == null ? void 0 : f.isCell) && !f.isActionable) {
      if (i.onCellClick) {
        i.onCellClick({
          'grid': e,
          'column': i,
          'record': e.store.getById(f.id),
          'cellElement': g,
          'target': c.target,
          'event': c
        });
        return true;
      }
    }
    return false;
  }
  onElementKeyPress(c) {
  }
  onElementKeyUp(c) {
  }
  onElementContextMenu(c) {
    const e = this, f = e.getCellDataFromEvent(c);
    if (f) {
      e.triggerCellMouseEvent('contextMenu', c, f);
      if (DomHelper.isTouchEvent) {
        e.onFocusGesture(c);
      }
    }
  }
  onGridBodyMouseEnterOrLeave(c) {
    const e = c.target.classList.contains('b-grid-cell') && this.getCellDataFromEvent(c);
    if (e) {
      this.triggerCellMouseEvent(c.type, c, e);
    }
  }
  onInternalResize(c, e, f, g, i) {
    const j = this;
    if (j._devicePixelRatio && j._devicePixelRatio !== globalThis.devicePixelRatio) {
      DomHelper.resetScrollBarWidth();
    }
    j._devicePixelRatio = globalThis.devicePixelRatio;
    j._bodyRectangle = Rectangle.client(j.bodyContainer);
    super.onInternalResize(...arguments);
    if (f !== i) {
      j._bodyHeight = j.bodyContainer.offsetHeight;
      if (j.isPainted) {
        j.rowManager.initWithHeight(j._bodyHeight);
      }
    }
    j.refreshVirtualScrollbars();
    if (e !== g) {
      j.setTimeout(() => {
        if (!j.isDestroyed) {
          j.updateResponsive(e, g);
        }
      }, 0);
    }
  }
  get widgetClass() {
  }
};
const validConfigTypes = {
  'string': 1,
  'object': 1,
  'function': 1
};
const GridFeatures_default = a => class b extends (a || Base) {
  static get $name() {
    return 'GridFeatures';
  }
  set features(c) {
    const e = this, f = GridFeatureManager.getInstanceDefaultFeatures(this);
    c = e._features = ObjectHelper.merge({}, this.constructor.configurable.features, ObjectHelper.isObject(c) ? c : {});
    if (f) {
      Object.keys(f).forEach(i => {
        if (!(i in c)) {
          c[i] = true;
        }
      });
    }
    const g = GridFeatureManager.getInstanceFeatures(this);
    for (const i of Object.keys(c)) {
      const j = c[i];
      if (j) {
        const k = !globalThis.__bryntum_code_editor_changed;
        if (StringHelper.uncapitalize(i) !== i) {
          const n = 'Invalid feature name \'' + i + '\', must start with a lowercase letter';
          if (k) {
            throw new Error(n);
          }
          console.error(n);
          e._errorDuringConfiguration = n;
        }
        const l = g[i];
        if (!l) {
          const o = 'Feature \'' + i + '\' not available, make sure you have imported it';
          if (k) {
            throw new Error(o);
          }
          console.error(o);
          e._errorDuringConfiguration = o;
          return;
        }
        Reflect.defineProperty(c, i, e.createFeatureInitializer(c, i, l, j));
      }
    }
  }
  get features() {
    return this._features;
  }
  createFeatureInitializer(c, e, f, g) {
    const i = [this], j = f.prototype.construct;
    if (g === true) {
      g = {};
    }
    if (validConfigTypes[typeof g]) {
      i[1] = g;
    }
    return {
      'configurable': true,
      'get'() {
        delete c[e];
        f.prototype.construct = function (...k) {
          c[e] = this;
          j.apply(this, k);
          f.prototype.construct = j;
        };
        return new f(...i);
      }
    };
  }
  hasFeature(c) {
    const {features: e} = this;
    if (e) {
      const f = Object.getOwnPropertyDescriptor(this.features, c);
      if (f) {
        return Boolean(f.value || f.get);
      }
    }
    return false;
  }
  hasActiveFeature(c) {
    let e, f;
    return Boolean((f = (e = this.features) == null ? void 0 : e[c]) == null ? void 0 : f.enabled);
  }
  getConfigValue(c, e) {
    let f, g;
    if (c === 'features') {
      const i = {};
      for (const j in this.features) {
        const k = (g = (f = this.features[j]) == null ? void 0 : f.getCurrentConfig) == null ? void 0 : g.call(f, e);
        if (k) {
          if (ObjectHelper.isEmpty(k)) {
            if (!GridFeatureManager.isDefaultFeatureForInstance(this.features[j].constructor, this)) {
              i[j] = true;
            }
          } else {
            i[j] = k;
          }
        } else {
          i[j] = false;
        }
      }
      return i;
    }
    return super.getConfigValue(c, e);
  }
  get widgetClass() {
  }
};
const defaultFocusOptions = Object.freeze({});
const containedFocusable = function (a) {
  if (!this.focusableFinderCell.contains(a)) {
    return DomHelper.NodeFilter.FILTER_REJECT;
  }
  if (DomHelper.isFocusable(a) && !a.disabled) {
    return DomHelper.NodeFilter.FILTER_ACCEPT;
  }
  return DomHelper.NodeFilter.FILTER_SKIP;
};
const GridNavigation_default = a => {
  let b;
  return b = class extends (a || Base) {
    static get $name() {
      return 'GridNavigation';
    }
    onStoreRecordIdChange(c) {
      let e;
      (e = super.onStoreRecordIdChange) == null ? void 0 : e.call(this, c);
      const {focusedCell: f} = this, {
          oldValue: g,
          value: i
        } = c;
      if (f && f.id === g) {
        f._id = i;
      }
    }
    onFocusedRowDerender() {
      const c = this, {focusedCell: e} = c;
      if ((e == null ? void 0 : e.id) != null && e.cell) {
        const f = e.cell.contains(DomHelper.getActiveElement(c));
        if (c.hideHeaders) {
          if (f) {
            c.revertFocus();
          }
        } else {
          const g = c.normalizeCellContext({
            'rowIndex': -1,
            'columnIndex': f ? e.columnIndex : 0
          });
          if (f) {
            c.focusCell(g);
          } else {
            g.cell.tabIndex = 0;
          }
        }
        e.cell.tabIndex = -1;
      }
    }
    navigateFirstCell() {
      this.focusCell(GridLocation.FIRST_CELL);
    }
    navigateFirstColumn() {
      this.focusCell(GridLocation.FIRST_COLUMN);
    }
    navigateLastCell() {
      this.focusCell(GridLocation.LAST_CELL);
    }
    navigateLastColumn() {
      this.focusCell(GridLocation.LAST_COLUMN);
    }
    navigatePrevPage() {
      this.focusCell(GridLocation.PREV_PAGE);
    }
    navigateNextPage() {
      this.focusCell(GridLocation.NEXT_PAGE);
    }
    activateHeader(c) {
      let e;
      if (c.target.classList.contains('b-grid-header') && this.focusedCell.isColumnHeader) {
        const {column: f} = this.focusedCell;
        (e = f.onKeyDown) == null ? void 0 : e.call(f, c);
        this.getHeaderElement(f.id).click();
      }
      return false;
    }
    onEscape(c) {
      let e, f;
      const g = this, {focusedCell: i} = g;
      if (!c.target.closest('.b-dragging') && (i == null ? void 0 : i.isActionable)) {
        c.stopImmediatePropagation();
        g._focusedCell = null;
        g.focusCell({
          'rowIndex': i.rowIndex,
          'column': i.column
        }, { 'disableActionable': true });
      } else if (g.isNested && g.owner && !((f = (e = g.owner).catchFocus) == null ? void 0 : f.call(e, { 'source': g }))) {
        g.revertFocus(true);
      }
    }
    onTab(c) {
      const {target: e} = c, {
          focusedCell: f,
          bodyElement: g
        } = this, {
          isActionable: i,
          actionTargets: actionTargets = []
        } = f, j = i && DomHelper.isEditable(e) && !e.readOnly;
      if (j && e === actionTargets[actionTargets.length - 1]) {
        c.preventDefault();
        this.navigateRight(c);
      } else if (!i || e === actionTargets[actionTargets.length - 1]) {
        g.style.display = 'none';
        this.requestAnimationFrame(() => g.style.display = '');
        return false;
      }
    }
    onShiftTab(c) {
      const e = this, {target: g} = c, {
          focusedCell: i,
          bodyElement: j
        } = e, {
          cell: k,
          isActionable: l,
          actionTargets: n
        } = i, o = l && DomHelper.isEditable(g) && !g.readOnly, p = i.columnIndex === 0 && i.rowIndex === (e.hideHeaders ? 0 : -1);
      if (!p && o && g === n[0]) {
        c.preventDefault();
        e.navigateLeft(c);
      } else if (!l || g === n[0]) {
        const q = !p && !e.hideHeaders && e.focusCell({
          'rowIndex': -1,
          'column': 0
        }, { 'disableActionable': true });
        if (q) {
          q.cell.tabIndex = -1;
          k.tabIndex = 0;
          e._focusedCell = i;
        } else {
          j.style.display = 'none';
          e.requestAnimationFrame(() => j.style.display = '');
        }
        return false;
      }
    }
    onSpace(c) {
      if (!this.focusedCell.isActionable) {
        c.preventDefault();
      }
      return false;
    }
    get focusedCell() {
      return this._focusedCell;
    }
    get isActionableLocation() {
      let c;
      return (c = this._focusedCell) == null ? void 0 : c.isActionable;
    }
    set focusedCell(c) {
      this.focusCell(c);
    }
    get focusedRecord() {
      let c;
      return (c = this._focusedCell) == null ? void 0 : c.record;
    }
    get cellCSSSelector() {
      const c = this._focusedCell;
      return c ? '[data-index=' + c.rowIndex + '] [data-column-id=' + c.columnId + ']' : '';
    }
    afterHide() {
      super.afterHide(...arguments);
      this.lastFocusedCell = null;
    }
    isFocused(c) {
      let e;
      return Boolean((e = this._focusedCell) == null ? void 0 : e.equals(this.normalizeCellContext(c)));
    }
    get focusElement() {
      let c;
      if (!this.isDestroying) {
        let e;
        if (this.store.count && this._focusedCell) {
          e = this._focusedCell.target;
        } else if (!this.hideHeaders) {
          e = this.normalizeCellContext({
            'rowIndex': -1,
            'columnIndex': ((c = this._focusedCell) == null ? void 0 : c.columnIndex) || 0
          }).target;
        }
        const f = super.focusElement;
        if (f && (!e || e.compareDocumentPosition(f) === Node.DOCUMENT_POSITION_PRECEDING)) {
          return f;
        }
        return e;
      }
    }
    afterInternalPaint({firstPaint: c}) {
      let e, f;
      const g = this;
      (e = super.afterInternalPaint) == null ? void 0 : e.call(this, ...arguments);
      const i = this.normalizeCellContext({
        'rowIndex': g.hideHeaders ? 0 : -1,
        'column': g.hideHeaders ? 0 : g.columns.find(j => !j.hidden && j.isFocusable)
      });
      if ((f = i.cell) == null ? void 0 : f.getAttribute('tabIndex')) {
        i._isDefaultFocus = true;
        g._focusedCell = i;
        const {target: j} = i;
        if (j === i.cell) {
          i.cell.tabIndex = 0;
        }
      }
    }
    onGridBodyFocusIn(c) {
      let e, f, g, i;
      const j = this, {bodyElement: k} = j, l = j.focusedCell, n = (l == null ? void 0 : l.initialTarget) || (l == null ? void 0 : l.target), {
          target: o,
          relatedTarget: p
        } = c, q = o.closest(j.focusableSelector), r = o.closest('.b-grid-row');
      if (q && (!GlobalEvents_default.currentMouseDown || GlobalEvents_default.isMouseDown(0) || GlobalEvents_default.isMouseDown(2))) {
        const u = new GridLocation(o), {cell: v} = u, w = l == null ? void 0 : l.cell, z = u.actionTargets = j.findFocusables(q), A = (!j._fromFocusCell || j.selectOnFocus) && (o === v || j._selectActionCell) && !(o == null ? void 0 : o._isRevertingFocus);
        if (r && u.rowIndex === -1) {
          v.focus({ 'preventScroll': true });
          return;
        }
        if (o.matches(j.focusableSelector)) {
          if (j.disableActionable) {
            u._target = v;
          } else if (z.length) {
            const B = (e = GlobalEvents_default.currentMouseDown) == null ? void 0 : e.target;
            if (!z.some(C => C.contains(B))) {
              (f = j.onCellNavigate) == null ? void 0 : f.call(j, j, l, u, A);
              j.trigger('navigate', {
                'lastFocusedCell': l,
                'focusedCell': u,
                'event': c
              });
            }
            j._selectActionCell = B === o;
            z[0].focus();
            delete j._selectActionCell;
            return;
          }
        } else {
          if ((l == null ? void 0 : l.target) && p && (!GlobalEvents_default.isMouseDown() || !k.contains((g = GlobalEvents_default.currentMouseDown) == null ? void 0 : g.target)) && !k.contains(p) && !u.equals(l)) {
            n.focus();
            return;
          }
          u._target = o;
        }
        if (w) {
          w.classList.remove('b-focused');
          w.tabIndex = -1;
        }
        if (v) {
          v.classList.add('b-focused');
          u.column.onCellFocus(u);
          if (v === o) {
            v.tabIndex = 0;
          }
          if (v.contains(c.relatedTarget)) {
            if (n === o) {
              return;
            }
          }
        }
        j._focusedCell = u;
        (i = j.onCellNavigate) == null ? void 0 : i.call(j, j, l, u, A);
        j.trigger('navigate', {
          'lastFocusedCell': l,
          'focusedCell': u,
          'event': c
        });
      } else if (!o.closest('.b-rowexpander-body')) {
        n == null ? void 0 : n.focus({ 'preventScroll': true });
      }
    }
    findFocusables(c) {
      const {focusableFinder: e} = this, f = [];
      e.currentNode = this.focusableFinderCell = c;
      for (let g = e.nextNode(); g; g = e.nextNode()) {
        f.push(g);
      }
      return f;
    }
    get focusableFinder() {
      const c = this;
      if (!c._focusableFinder) {
        c._focusableFinder = c.setupTreeWalker(c.bodyElement, DomHelper.NodeFilter.SHOW_ELEMENT, { 'acceptNode': containedFocusable.bind(c) });
      }
      return c._focusableFinder;
    }
    restoreActiveItem(c = this._focusedCell) {
      if (this.rowManager.count) {
        if (!isNaN(c)) {
          c = this.store.getAt(c);
        } else if (!c.isModel) {
          c = this.store.getById(c);
        }
        return this.focusCell(c);
      }
    }
    focusCell(c, e = defaultFocusOptions) {
      let f, g, i, j;
      const k = this, {_focusedCell: l} = k, {
          scroll: n,
          disableActionable: o
        } = e, p = c === GridLocation.DOWN, q = c === GridLocation.UP, r = c === GridLocation.PREV_CELL || c === GridLocation.NEXT_CELL;
      if ((c == null ? void 0 : c.rowIndex) === -1 && k.hideHeaders) {
        k.revertFocus();
        return;
      }
      if (typeof c === 'number' && !(l == null ? void 0 : l.isLocation)) {
        return;
      }
      c = typeof c === 'number' && (l == null ? void 0 : l.isLocation) ? l.move(c) : k.normalizeCellContext(c);
      const u = 'doSelect' in e ? e.doSelect : !c.isActionable || c.initialTarget === c.cell;
      if (c.equals(l)) {
        if (k.isNested && (p || q)) {
          if (!((g = (f = k.owner) == null ? void 0 : f.catchFocus) == null ? void 0 : g.call(f, {
              'source': k,
              'navigationDirection': p ? 'down' : 'up'
            }))) {
            k.revertFocus(true);
          }
        } else {
          (i = k.onCellNavigate) == null ? void 0 : i.call(k, k, l, c, u);
        }
        return l;
      }
      const v = k.getSubGridFromColumn(c.columnId), {cell: w} = c, z = w || k.getCell({
          'rowIndex': k.rowManager.topIndex,
          'columnId': c.columnId
        }), A = Rectangle.from(v.element), B = Rectangle.from(k.bodyElement), C = Rectangle.from(z).moveTo(null, A.y);
      if (n !== false) {
        if (c.rowIndex === -1) {
          if (r) {
            k.scrollColumnIntoView(c.column);
          }
        } else {
          e = Object.assign({}, e, n);
          if (C.width > A.width || C.height > B.height) {
            e.x = e.y = false;
          } else {
            e.column = c.columnId;
          }
          k.scrollRowIntoView(c.id, e);
        }
      }
      if (k._hoveredRow || k.hoveredCell) {
        k.hoveredCell = null;
      }
      k.disableActionable = o;
      k.selectOnFocus = u;
      k._fromFocusCell = true;
      (j = c[o ? 'cell' : 'target']) == null ? void 0 : j.focus({ 'preventScroll': true });
      k.disableActionable = k.selectOnFocus = false;
      delete k._fromFocusCell;
      return c;
    }
    blurCell(c) {
      const e = this, f = e.getCell(c);
      if (f) {
        f.classList.remove('b-focused');
      }
    }
    clearFocus(c) {
      const e = this;
      if (e._focusedCell) {
        e.lastFocusedCell = c ? null : e._focusedCell;
        e.blurCell(e._focusedCell);
        e._focusedCell = null;
      }
    }
    catchFocus() {
    }
    internalNextPrevCell(c = true) {
      const e = this, f = e._focusedCell;
      if (f) {
        return e.focusCell({
          'id': f.id,
          'columnId': e.columns.getAdjacentVisibleLeafColumn(f.column, c, true).id
        });
      }
      return null;
    }
    navigateRight() {
      let c;
      if ((c = arguments[0]) == null ? void 0 : c.fromKeyMap) {
        return this.focusCell(this.rtl ? GridLocation.PREV_CELL : GridLocation.NEXT_CELL);
      }
      return this.internalNextPrevCell(!this.rtl);
    }
    navigateLeft() {
      let c;
      if ((c = arguments[0]) == null ? void 0 : c.fromKeyMap) {
        return this.focusCell(this.rtl ? GridLocation.NEXT_CELL : GridLocation.PREV_CELL);
      }
      return this.internalNextPrevCell(Boolean(this.rtl));
    }
    internalNextPrevRow(c, e = true, f = true) {
      const g = this, i = g._focusedCell;
      if (!i)
        return false;
      const j = g.store['get' + (c ? 'Next' : 'Prev')](i.id, false, e);
      if (j) {
        return g.focusCell({
          'id': j.id,
          'columnId': i.columnId,
          'scroll': { 'x': false }
        });
      } else if (!c && f && !i.isColumnHeader) {
        this.clearFocus();
        this.getHeaderElement(i.columnId).focus();
      }
      return false;
    }
    navigateDown() {
      let c;
      if ((c = arguments[0]) == null ? void 0 : c.fromKeyMap) {
        return this.focusCell(GridLocation.DOWN);
      }
      return this.internalNextPrevRow(true, false);
    }
    navigateUp() {
      let c;
      if ((c = arguments[0]) == null ? void 0 : c.fromKeyMap) {
        return this.focusCell(GridLocation.UP);
      }
      return this.internalNextPrevRow(false, false);
    }
    get widgetClass() {
    }
  }, __publicField(b, 'configurable', {
    'focusable': false,
    'focusableSelector': '.b-grid-cell,.b-grid-header.b-depth-0[tabIndex]',
    'isNested': false,
    'keyMap': {
      'ArrowUp': {
        'handler': 'navigateUp',
        'weight': 10
      },
      'ArrowRight': {
        'handler': 'navigateRight',
        'weight': 10
      },
      'ArrowDown': {
        'handler': 'navigateDown',
        'weight': 10
      },
      'ArrowLeft': {
        'handler': 'navigateLeft',
        'weight': 10
      },
      'Ctrl+Home': 'navigateFirstCell',
      'Home': 'navigateFirstColumn',
      'Ctrl+End': 'navigateLastCell',
      'End': 'navigateLastColumn',
      'PageUp': 'navigatePrevPage',
      'PageDown': 'navigateNextPage',
      'Enter': 'activateHeader',
      'Escape': {
        'handler': 'onEscape',
        'weight': 10
      },
      'Shift+Tab': {
        'handler': 'onShiftTab',
        'preventDefault': false,
        'weight': 200
      },
      'Tab': {
        'handler': 'onTab',
        'preventDefault': false,
        'weight': 200
      },
      ' ': {
        'handler': 'onSpace',
        'preventDefault': false
      }
    }
  }), b;
};
const GridResponsive_default = a => class b extends (a || Base) {
  static get $name() {
    return 'GridResponsive';
  }
  static get defaultConfig() {
    return {
      'responsiveLevels': Object.freeze({
        'small': 400,
        'medium': 600,
        'large': '*'
      })
    };
  }
  getClosestBiggerLevel(c) {
    const e = this, f = Object.keys(ObjectHelper.assign({}, e.responsiveLevels));
    let g = null, i = 99995, j = null;
    f.forEach(k => {
      let l = e.responsiveLevels[k];
      if (![
          'number',
          'string'
        ].includes(typeof l)) {
        l = l.levelWidth;
      }
      if (l === '*') {
        j = k;
      } else if (c < l) {
        const n = l - c;
        if (n < i) {
          i = n;
          g = k;
        }
      }
    });
    return g || j;
  }
  get responsiveLevel() {
    return this.getClosestBiggerLevel(this.width);
  }
  updateResponsive(c, e) {
    const f = this, g = f.getClosestBiggerLevel(e), i = f.getClosestBiggerLevel(c);
    if (e === 0 || g !== i) {
      const j = f.responsiveLevels[i];
      if (![
          'number',
          'string'
        ].includes(typeof j)) {
        f.applyState(j);
      }
      f.columns.forEach(k => {
        const l = k.responsiveLevels;
        if (l) {
          if (l[i]) {
            k.applyState(l[i]);
          } else if (l['*']) {
            k.applyState(l['*']);
          }
        }
      });
      f.element.classList.remove('b-responsive-' + g);
      f.element.classList.add('b-responsive-' + i);
      f.trigger('responsive', {
        'level': i,
        'width': c,
        'oldLevel': g,
        'oldWidth': e
      });
    }
  }
  get widgetClass() {
  }
};
const validIdTypes = {
  'string': 1,
  'number': 1
};
const isDataLoadAction = {
  'dataset': 1,
  'batch': 1
};
const GridSelection_default = a => {
  let b;
  return b = class extends (a || Base) {
    static get $name() {
      return 'GridSelection';
    }
    construct(c) {
      this._selectedCells = [];
      this._selectedRows = [];
      super.construct(c);
      if (c == null ? void 0 : c.selectedRecords) {
        this.selectedRecords = c.selectedRecords;
      }
    }
    getDefaultGridSelection(c) {
      if (c.$name === 'GridSelection') {
        return c.configurable.selectionMode;
      } else if (c.superclass) {
        return this.getDefaultGridSelection(c.superclass);
      }
    }
    changeSelectionMode(c) {
      const e = this;
      if (e.selectionMode) {
        ObjectHelper.assign(e.selectionMode, c);
        return e.selectionMode;
      }
      e.$defaultGridSelection = e.getDefaultGridSelection(e.constructor);
      return new Proxy(c, {
        'set'(f, g, i) {
          const j = ObjectHelper.assign({}, f);
          f[g] = i;
          e.updateSelectionMode(f, j);
          return true;
        }
      });
    }
    updateSelectionMode(c, e = this.$defaultGridSelection) {
      let f, g;
      const i = this, {
          columns: j,
          checkboxSelectionColumn: k
        } = i, l = {}, {rowReorder: n} = i.features;
      for (const o in c) {
        if (c[o] != e[o]) {
          l[o] = c[o];
        }
      }
      if (l.column) {
        c.cell = true;
        c.multiSelect = true;
      }
      if (l.cell) {
        c.checkboxOnly = false;
      }
      if (l.cell === false) {
        c.column = false;
      }
      if (l.alwaysMultiSelect) {
        c.multiSelect = true;
        c.deselectOnClick = true;
      }
      if (l.checkboxOnly) {
        if (!c.checkbox) {
          c.checkbox = true;
        }
        c.cell = false;
      }
      if (l.checkbox === false) {
        l.checkboxOnly = false;
        l.showCheckAll = false;
      }
      if (l.showCheckAll) {
        c.checkbox = c.checkbox || true;
        c.multiSelect = true;
      }
      if (l.includeChildren || l.includeParents) {
        c.multiSelect = true;
      }
      if (l.multiSelect === false) {
        c.column = c.showCheckAll = c.dragSelect = c.includeChildren = c.includeParents = c.alwaysMultiSelect = false;
      }
      if (l.deselectOnClick === false) {
        c.alwaysMultiSelect = false;
      }
      if (l.dragSelect) {
        if ((n == null ? void 0 : n.enabled) && n.gripOnly !== true) {
          n.showGrip = n.gripOnly = true;
        }
        c.multiSelect = true;
        i._selectionListenersDetachers = {};
      }
      if (l.dragSelect === false && i._selectionListenersDetachers) {
        (g = (f = i._selectionListenersDetachers).selectiondrag) == null ? void 0 : g.call(f);
        delete i._selectionListenersDetachers.selectiondrag;
      }
      if (e && (l.cell !== void 0 || l.deselectFilteredOutRecords !== void 0 || l.multiSelect !== void 0)) {
        i.deselectAll();
      }
      if (l.rowNumber) {
        if (!j.findRecord('type', 'rownumber')) {
          const p = i.isGanttBase ? {
            'type': 'sequence',
            'text': ''
          } : { 'type': 'rownumber' };
          j.insert(0, {
            ...p,
            ...typeof c.rowNumber == 'object' ? c.rowNumber : {}
          });
          i._selectionAddedRowNumberColumn = true;
        }
      } else if (l.rowNumber === false && i._selectionAddedRowNumberColumn) {
        j.remove(j.findRecord('type', 'rownumber'));
        delete i._selectionAddedRowNumberColumn;
      }
      if (c.checkbox !== (e == null ? void 0 : e.checkbox) || c.checkbox && c.showCheckAll !== (e == null ? void 0 : e.showCheckAll)) {
        if (i.isConfiguring) {
          i.shouldInitCheckboxSelection = true;
        } else {
          if (e) {
            i.deselectAll();
          }
          i.initCheckboxSelection();
        }
      }
      if (e && c.checkbox && e.checkbox && c.checkboxIndex !== e.checkboxIndex && k) {
        k.parent.insertChild(k, j.getAt(i.checkboxSelectionColumnInsertIndex));
      }
      i.trigger('selectionModeChange', ObjectHelper.clone(c));
      i.afterSelectionModeChange(c);
    }
    afterConfigure() {
      const c = this;
      if (c.shouldInitCheckboxSelection) {
        c.shouldInitCheckboxSelection = false;
        c.initCheckboxSelection();
      }
      c.scrollManager.ion({
        'scroll': ({
          event: e,
          deltaY: f
        }) => {
          if (f !== 0) {
            c.onSelectionDrag(e);
          }
        }
      });
      super.afterConfigure();
    }
    initCheckboxSelection() {
      let c, e;
      const f = this, {
          selectionMode: g,
          columns: i,
          checkboxSelectionColumn: j
        } = f, {checkbox: k} = g;
      if (j) {
        f.checkboxSelectionColumn = null;
        i.remove(j);
        j.destroy();
      }
      if (k) {
        const l = ColumnStore.getColumnClass('check'), n = k === true ? null : k;
        if (!l) {
          throw new Error('CheckColumn must be imported for checkbox selection mode to work');
        }
        const o = f.checkboxSelectionColumn = new l(ObjectHelper.assign({
          'id': f.id + '-selection-column',
          'width': '4em',
          'field': null,
          'sortable': false,
          'filterable': false,
          'groupable': false,
          'hideable': false,
          'cellCls': 'b-checkbox-selection',
          'region': (e = (c = f.items) == null ? void 0 : c[0]) == null ? void 0 : e.region,
          'showCheckAll': g.showCheckAll,
          'draggable': false,
          'resizable': false,
          'widgets': [{
              'type': 'checkbox',
              'valueProperty': 'checked',
              'ariaLabel': 'L{Checkbox.toggleRowSelect}'
            }]
        }, n), i, { 'isSelectionColumn': true });
        o.meta.depth = 0;
        o._grid = f;
        const p = o.renderer;
        o.setData('renderer', q => {
          q.value = f.isSelected(q.record);
          p.call(o, q);
        });
        o.ion({
          'toggle': 'onCheckChange',
          'toggleAll': 'onCheckAllChange',
          'thisObj': f
        });
        i.insert(f.checkboxSelectionColumnInsertIndex, o);
      }
    }
    get checkboxSelectionColumnInsertIndex() {
      const {columns: c} = this;
      let {checkboxIndex: e} = this.selectionMode;
      if (!e) {
        e = c.indexOf(c.findRecord('type', 'rownumber')) + 1;
      } else if (typeof e === 'string') {
        e = c.indexOf(c.getById(e));
      }
      return e;
    }
    beforeColumnsChange({
      type: c,
      action: e,
      changes: f,
      columns: g,
      column: i
    }) {
      if (this.columns.count === 0) {
        this.deselectAll(false, true);
      }
    }
    afterColumnsChange({
      type: c,
      action: e,
      changes: f,
      columns: g,
      column: i
    }) {
      let j;
      const {_selectedCells: k} = this;
      if (k == null ? void 0 : k.length) {
        if (!g && i) {
          g = [i];
        }
        if (c === 'move' || (f == null ? void 0 : f.hidden) || (f == null ? void 0 : f.region) || e === 'add' || e === 'remove') {
          let l = k.map(n => ({
            'id': n.id,
            'columnId': n._column.id
          }));
          if ((e === 'remove' || ((j = f == null ? void 0 : f.hidden) == null ? void 0 : j.value)) && (g == null ? void 0 : g.length)) {
            l = l.filter(n => !g.some(o => n.columnId === o.id));
          }
          this.deselectAll(false, true);
          this.selectCells({
            'cells': l,
            'scrollIntoView': false
          });
        }
      }
    }
    afterSelectionChange() {
    }
    afterSelectionModeChange() {
    }
    changeSelectedRecordCollection(c) {
      if (c == null ? void 0 : c.isCollection) {
        if (!c.owner) {
          c.owner = this;
        }
        return c;
      }
      return Collection.new(c, { 'owner': this });
    }
    updateSelectedRecordCollection(c) {
      c.ion({
        'change': 'onSelectedRecordCollectionChange',
        'thisObj': this
      });
    }
    onSelectedRecordCollectionChange({
      added: added = [],
      removed: c
    }) {
      if (this.selectedRecordCollection._fromSelection !== this) {
        added = added.filter(e => this.isSelectable(e));
        this.performSelection({
          'selectedCells': [],
          'deselectedCells': [],
          'selectedRecords': added,
          'deselectedRecords': c
        });
      }
    }
    changeSelectedRecordCollectionSilent(c) {
      this.selectedRecordCollection._fromSelection = this;
      const e = c(this.selectedRecordCollection);
      delete this.selectedRecordCollection._fromSelection;
      return e;
    }
    bindStore(c) {
      let e;
      this.detachListeners('selectionStoreFilter');
      c.ion({
        'name': 'selectionStoreFilter',
        'filter': 'onStoreFilter',
        'thisObj': this
      });
      (e = super.bindStore) == null ? void 0 : e.call(this, c);
    }
    unbindStore(c) {
      this.detachListeners('selectionStoreFilter');
      super.unbindStore(c);
    }
    onStoreFilter({source: c}) {
      const e = this, f = e.selectedRows.filter(i => !c.includes(i)), g = e.prepareSelection(e.selectionMode.deselectFilteredOutRecords ? f : []);
      if (e.isCellSelectionMode) {
        const {deselectedCells: i} = e.prepareSelection(e._selectedCells.filter(j => !c.records.includes(j.record)));
        if (i == null ? void 0 : i.length) {
          g.deselectedCells = (g.deselectedCells || []).concat(i);
        }
      }
      if (g.deselectedCells.length || g.deselectedRecords.length) {
        e.performSelection(g, false);
        e.updateCheckboxHeader();
      }
    }
    onStoreRecordIdChange({
      record: c,
      oldValue: e
    }) {
      let f;
      (f = super.onStoreRecordIdChange) == null ? void 0 : f.call(this, ...arguments);
      const g = this.selectedRecordCollection.get(e);
      if (g === c) {
        this.selectedRecordCollection.rebuildIndices();
      }
    }
    onStoreRemove(c) {
      let e;
      (e = super.onStoreRemove) == null ? void 0 : e.call(this, c);
      if (!c.isCollapse) {
        const f = this, g = c.records.filter(j => f.isSelected(j)), i = f.prepareSelection(g);
        if (f.isCellSelectionMode) {
          const {deselectedCells: j} = f.prepareSelection(f.getSelectedCellsForRecords(c.records));
          if (j == null ? void 0 : j.length) {
            i.deselectedCells = (i.deselectedCells || []).concat(j);
          }
        }
        if (g.length || i.deselectedCells.length) {
          f.performSelection(i);
        }
      }
    }
    onStoreDataChange({
      action: c,
      source: e
    }) {
      let f;
      const g = this, {selectionMode: i} = g;
      let j;
      (f = super.onStoreDataChange) == null ? void 0 : f.call(this, ...arguments);
      if (c === 'pageLoad') {
        if (!i.preserveSelectionOnPageChange) {
          j = g.prepareSelection(null, null, true);
        }
        g.updateCheckboxHeader();
      } else if (isDataLoadAction[c]) {
        const k = [];
        if (i.preserveSelectionOnDatasetChange === false) {
          j = g.prepareSelection(null, null, true);
        } else {
          k.push(...g.changeSelectedRecordCollectionSilent(l => l.match(e.storage)));
          for (const l of g._selectedCells) {
            if (!e.getById(l.id)) {
              k.push(l);
            }
          }
          j = g.prepareSelection(k);
        }
      }
      if (j && (j.deselectAll || j.deselectedCells.length || j.deselectedRecords.length || j.selectedCells.length || j.selectedRecords.length)) {
        g.performSelection(j, false);
        g.updateCheckboxHeader();
      }
    }
    onStoreRemoveAll() {
      let c;
      (c = super.onStoreRemoveAll) == null ? void 0 : c.call(this);
      this.performSelection(this.prepareSelection(null, null, true), false);
    }
    onCheckChange({
      checked: c,
      record: e,
      checkbox: f
    }) {
      const g = this, {multiSelect: i} = g.selectionMode, j = !i && c, k = !j && !c ? [e] : null, l = c ? [e] : null;
      g._isCheckboxSelecting = true;
      if (c && !GlobalEvents_default.shiftKeyDown) {
        g._lastSelectionChecked = e;
      }
      if (c && i && g._lastSelectionChecked && GlobalEvents_default.shiftKeyDown) {
        g.performSelection(g.internalSelectRange(g._lastSelectionChecked, e, true));
      } else if (g.performSelection(g.prepareSelection(k, l, j, true)) === false) {
        f.checked = !f.checked;
      }
      g._isCheckboxSelecting = false;
    }
    updateCheckboxHeader() {
      const {
        selectionMode: c,
        checkboxSelectionColumn: e
      } = this;
      if (!this.hideHeaders && !this._isCheckAllSelecting && c.checkbox && c.showCheckAll && (e == null ? void 0 : e.headerCheckbox)) {
        const f = this.isAllRecordsSelected();
        if (e.headerCheckbox.checked !== f) {
          e.suspendEvents();
          e.headerCheckbox.checked = f;
          e.resumeEvents();
        }
      }
    }
    isAllRecordsSelected() {
      return this.store.count && !this.store.some(c => this.isSelectable(c) && !this.isSelected(c));
    }
    onCheckAllChange({checked: c}) {
      const e = this;
      e._isCheckboxSelecting = e._isCheckAllSelecting = true;
      e[c ? 'selectAll' : 'deselectAll'](e.store.isPaged && e.selectionMode.preserveSelectionOnPageChange);
      e._isCheckboxSelecting = e._isCheckAllSelecting = false;
    }
    get selectionDragMouseEventListenerElement() {
      return globalThis;
    }
    onSelectionDrag(c) {
      let e;
      const f = this, g = c.type === 'scroll', {
          _selectionStartCell: i,
          lastPointerMoveEvent: j
        } = f;
      if (g && !f._isSelectionDragging) {
        return;
      }
      if (!GlobalEvents_default.isMouseDown()) {
        f.onSelectionEnd();
      }
      if (!i) {
        return;
      }
      const {
        items: k,
        _lastSelectionDragRegion: l
      } = f;
      let n, o, p;
      if (g && j) {
        const q = document.elementFromPoint(j.clientX, j.clientY), r = q == null ? void 0 : q.closest('.b-grid-cell');
        if (r) {
          o = f._lastSelectionDragRegion;
          p = f.normalizeCellContext({
            'columnId': r.dataset.columnId,
            'record': f.store.getById(r.parentElement.dataset.id)
          });
        }
      } else {
        n = f.getCellDataFromEvent(c, true);
        o = n == null ? void 0 : n.column.region;
        p = (n == null ? void 0 : n.cellSelector) && f.normalizeCellContext(n.cellSelector);
        if (n) {
          f.lastPointerMoveEvent = c;
        }
      }
      if (p && !p.equals(f._lastSelectionDragCell, true)) {
        if (!f._isSelectionDragging) {
          f.enableScrollingCloseToEdges(k);
        }
        if (f._clearSelectionOnSelectionDrag && !i.equals(p, true)) {
          f.deselectAll();
          delete f._clearSelectionOnSelectionDrag;
        }
        if (l && o !== l) {
          const v = f.subGrids[l], w = f.subGrids[o], z = v.scrollable, A = w.scrollable, B = k.indexOf(v) - k.indexOf(w) < 0;
          A.x = B ? 0 : A.maxX;
          if (B ? z.x < z.maxX - 1 : z.x > 1) {
            return;
          }
          const C = (e = f.scrollManager._activeScroll) == null ? void 0 : e.horizontal;
          if (C && C.element !== A.element) {
            C.stopScroll(true);
          }
        }
        f._lastSelectionDragRegion = o;
        f._lastSelectionDragCell = p;
        f._isSelectionDragging = true;
        const u = f._lastSelectionDragChange = f.internalSelectRange(i, p, f.isRowNumberSelecting(p) || f.isRowNumberSelecting(i));
        u.deselectedCells = u.deselectedCells.filter(E => !f.isCellSelected(E));
        u.deselectedRecords = u.deselectedRecords.filter(E => !f.isSelected(E));
        f.hoveredCell = null;
        f.refreshGridSelectionUI(u);
        f.trigger('dragSelecting', u);
      }
    }
    onSelectionEnd() {
      let c, e;
      const f = this, g = f._lastSelectionDragChange;
      if (f._isSelectionDragging && !f._selectionStartCell.equals(f._lastSelectionDragCell, true) && g) {
        f.performSelection(g, false);
      }
      f.disableScrollingCloseToEdges(f.items);
      f._isSelectionDragging = false;
      f._lastSelectionDragChange = f._lastSelectionDragCell = f._lastSelectionDragRegion = null;
      (e = (c = f._selectionListenersDetachers).selectiondrag) == null ? void 0 : e.call(c);
      delete f._selectionListenersDetachers.selectiondrag;
    }
    onHandleElementClick(c) {
      const e = this;
      if (e.selectionMode.rowNumber && c.target.closest('.b-rownumber-header')) {
        c.handled = true;
        if (e.store.count && e.store.some(f => !e.isSelected(f))) {
          e.selectAll();
        } else {
          e.deselectAll();
        }
      } else if (e.selectionMode.column && c.target.closest('.b-grid-header')) {
        c.handled = true;
        e.selectColumn(c, c.ctrlKey);
      }
      super.onHandleElementClick(c);
    }
    selectColumn(c, e = false) {
      const f = this, {store: g} = f, {columnId: i} = f.getHeaderDataFromEvent(c);
      f._shiftSelectRange = null;
      if (!c.shiftKey) {
        f._shiftSelectColumn = i;
      }
      const j = c.shiftKey && f._shiftSelectColumn || i, k = f.internalSelectRange(f.normalizeCellContext({
          'id': g.first.id,
          'columnId': j
        }), f.normalizeCellContext({
          'id': g.last.id,
          'columnId': i
        }));
      if (e && !k.selectedCells.some(l => !f.isCellSelected(l))) {
        k.deselectedCells = k.selectedCells;
        k.selectedCells = [];
      }
      if (!e) {
        k.deselectedCells = f._selectedCells;
      }
      f.cleanSelectionChange(k);
      f.performSelection(k);
    }
    isSelected(c) {
      if (c == null ? void 0 : c.id) {
        c = c.id;
      }
      if (validIdTypes[typeof c]) {
        return this.selectedRows.some(e => e.id === c);
      }
      return false;
    }
    isCellSelected(c, e) {
      c = this.normalizeCellContext(c);
      return this.isCellSelectionMode && this._selectedCells.some(f => c.equals(f, true)) || e && this.isSelected(c);
    }
    isSelectable(c) {
      return this.normalizeCellContext({ 'id': c.id || c }).isSelectable;
    }
    get selectedRecord() {
      return this.selectedRecords[this.selectedRecords.length - 1] || null;
    }
    set selectedRecord(c) {
      this.selectRow({ 'record': c });
    }
    get selectedRecords() {
      return this.selectedRecordCollection.values;
    }
    set selectedRecords(c) {
      this.selectRows(c);
    }
    get selectedRows() {
      return [...this._selectedRows];
    }
    set selectedRows(c) {
      this.selectRows(c);
    }
    spliceSelectedRecords(c, e, f) {
      const g = this;
      if (typeof e == 'number') {
        const i = [...g.selectedRecords];
        i.splice(c, e, ...ArrayHelper.asArray(f));
        g.performSelection(g.prepareSelection(null, i, true, true));
      } else {
        g.performSelection(g.prepareSelection(e, f, false, true));
      }
    }
    selectRow(c) {
      if (typeof c === 'number' || typeof c === 'string' || c.isModel || !('record' in c)) {
        c = { 'records': [c] };
      }
      ObjectHelper.assignIf(c, { 'scrollIntoView': true });
      this.selectRows(c);
    }
    selectRows(c) {
      if (!c || Array.isArray(c) || c.isModel || typeof c === 'number' || !('records' in c) && !('record' in c)) {
        c = { 'records': ArrayHelper.asArray(c) || [] };
      }
      const e = this, {store: f} = e, g = [], {
          records: records = c.record ? [c.record] : [],
          column: column = e.columns.visibleColumns[0],
          scrollIntoView: i,
          addToSelection: addToSelection = arguments[1]
        } = c;
      for (let j of records) {
        j = f.getById(j);
        if (j) {
          g.push(j);
        }
      }
      if (!addToSelection) {
        e._shiftSelectRange = null;
      }
      e.performSelection(e.prepareSelection(null, g, !addToSelection, true));
      if (g.length && i) {
        return e.scrollRowIntoView(g[0].id, {
          ...i,
          'column': column
        });
      }
    }
    selectAll(c = false) {
      const {store: e} = this, f = (e.isGrouped ? e.getAllDataRecords() : e.records).filter(g => !g.isSpecialRow);
      this.performSelection(this.prepareSelection(null, f, false, true), true, c);
    }
    deselectAll(c = false, e = false) {
      const {store: f} = this, g = c ? (f.isGrouped ? f.getAllDataRecords() : f.records).filter(i => !i.isSpecialRow) : null;
      this.performSelection(this.prepareSelection(g, null, !c), true, e);
    }
    deselectRow(c) {
      this.deselectRows(c);
    }
    deselectRows(c) {
      const {store: e} = this, f = ArrayHelper.asArray(c).map(g => e.getById(g)).filter(g => g);
      this.performSelection(this.prepareSelection(f));
    }
    selectRange(c, e, f = false) {
      const g = this, {store: i} = g, j = g.internalSelectRange(i.getById(c), i.getById(e), true);
      g._shiftSelectRange = null;
      g.performSelection(j);
    }
    get selectedCell() {
      return this._selectedCells[this._selectedCells.length - 1];
    }
    set selectedCell(c) {
      this.selectCells([c]);
    }
    get selectedCells() {
      return [...this._selectedCells];
    }
    set selectedCells(c) {
      this.selectCells(c);
    }
    get selectedCellCSSSelector() {
      const c = this.selectedCell, e = c && this.getRowById(c.id);
      if (!c || !e)
        return '';
      return '[data-index=' + e.dataIndex + '] [data-column-id=' + c.columnId + ']';
    }
    selectCell(c) {
      let e;
      if ('id' in c) {
        c = { 'cell': c };
        c = Object.assign({
          'scrollIntoView': arguments[1],
          'addToSelection': arguments[2],
          'silent': arguments[3]
        }, c);
      }
      return (e = this.selectCells(c)) == null ? void 0 : e[0];
    }
    selectCells(c) {
      if (Array.isArray(c)) {
        c = { 'cells': c };
      }
      const e = this, {
          cells: cells = c.cell ? [c.cell] : [],
          scrollIntoView: scrollIntoView = true,
          addToSelection: addToSelection = false,
          silent: silent = false
        } = c, f = e.prepareSelection(null, cells, !addToSelection);
      if (!addToSelection) {
        e._shiftSelectRange = null;
      }
      e.performSelection(f, true, silent);
      if (scrollIntoView) {
        e.scrollRowIntoView(cells[0].id, {
          ...scrollIntoView,
          'column': cells[0].columnId
        });
      }
      return e.isCellSelectionMode ? f.selectedCells : f.selectedRecords;
    }
    deselectCell(c) {
      let e;
      return (e = this.deselectCells([c])) == null ? void 0 : e[0];
    }
    deselectCells(c) {
      const e = this.prepareSelection(c);
      this.performSelection(e);
      return this.isCellSelectionMode ? e.deselectedCells : e.deselectedRecords;
    }
    toggleSelection(c) {
      const e = this, {
          _focusedCell: f,
          selectionMode: g
        } = e, i = e.isRowNumberSelecting(f), j = e.isCellSelected(f, true);
      if (g.selectOnKeyboardNavigation === true || f.isActionable) {
        return false;
      }
      e.performSelection(e.prepareSelection(j ? f : null, j ? null : f, !g.multiSelect, i));
      c.preventDefault();
    }
    selectCellRange(c, e) {
      this.performSelection(this.internalSelectRange(c, e));
    }
    getSelection() {
      if (this.isRowSelectionMode) {
        return this.selectedRecords;
      } else {
        return this.selectedCells;
      }
    }
    cleanSelectionChange(c) {
      let e, f;
      const {
          deselectedRecords: g,
          deselectedCells: i,
          selectedCells: j,
          deselectedCellRecords: k
        } = c, l = (e = c.selectedRecords) == null ? void 0 : e.map(o => o.id), n = (f = c.selectedCellRecords) == null ? void 0 : f.map(o => o.id);
      if ((g == null ? void 0 : g.length) && (l == null ? void 0 : l.length)) {
        c.deselectedRecords = g.filter(o => !l.includes(o.id));
      }
      if ((i == null ? void 0 : i.length) && (j == null ? void 0 : j.length)) {
        c.deselectedCells = i.filter(o => !j.some(p => o.equals(p, true)));
      }
      if (k.length && (n.length || (l == null ? void 0 : l.length))) {
        c.deselectedCellRecords = k.filter(o => {
          return !n.includes(o.id) && !l.includes(o.id);
        });
      }
      return c;
    }
    getSelectedCellsForRecords(c) {
      return this._selectedCells.filter(e => e.id && c.some(f => f.id === e.id));
    }
    delayUntilMouseUp(c) {
      const e = EventHelper.on({
        'element': this.selectionDragMouseEventListenerElement,
        'blur': f => c(f, e),
        'mouseup': f => c(f, e),
        'thisObj': this,
        'once': true
      });
    }
    get isRowSelectionMode() {
      return !this.isCellSelectionMode;
    }
    get isCellSelectionMode() {
      return this.selectionMode.cell === true;
    }
    isRowNumberSelecting(...c) {
      return this.selectionMode.rowNumber && !c.some(e => e.column.type !== 'rownumber');
    }
    selectionShouldIncludeChildren(c) {
      let e;
      const {
        includeChildren: f,
        multiSelect: g
      } = this.selectionMode;
      return (f === 'always' || f === true && this._isCheckboxSelecting) && g && !c.isLeaf && ((e = c.allChildren) == null ? void 0 : e.length);
    }
    extendSelectionLeft() {
      this.extendSelection('Left');
    }
    extendSelectionRight() {
      this.extendSelection('Right');
    }
    extendSelectionUp() {
      this.extendSelection('Up');
    }
    extendSelectionDown() {
      this.extendSelection('Down');
    }
    extendSelection(c) {
      this._isKeyboardRangeSelecting = true;
      this['navigate' + c]();
      this._isKeyboardRangeSelecting = false;
    }
    onCellNavigate(c, e, f, g) {
      let i, j;
      const {
          selectionMode: k,
          _selectionListenersDetachers: l
        } = c, {
          multiSelect: n,
          alwaysMultiSelect: o,
          deselectOnClick: p,
          dragSelect: q
        } = k, {
          ctrlKeyDown: r,
          shiftKeyDown: u
        } = GlobalEvents_default, v = GlobalEvents_default.isMouseDown(), w = GlobalEvents_default.isMouseDown(2), z = GlobalEvents_default.currentMouseDown || GlobalEvents_default.currentKeyDown;
      f = c.normalizeCellContext(f);
      if (c.selectionDisabled || !g || f.rowIndex === -1 || ((i = f.record) == null ? void 0 : i.isGroupHeader) || (z == null ? void 0 : z.fromKeyMap) && !k.selectOnKeyboardNavigation || c.columns.getById(f.columnId) === c.checkboxSelectionColumn || k.checkboxOnly || (z == null ? void 0 : z.handled) === true) {
        return;
      }
      if (!u) {
        c._isAddingToSelection = (r || o) && n;
        c._selectionStartCell = f;
      }
      if (n && q && v && !l.selectiondrag && !c.preventDragSelect) {
        l.selectiondrag = EventHelper.on({
          'name': 'selectiondrag',
          'element': c.selectionDragMouseEventListenerElement,
          'blur': 'onSelectionEnd',
          'mouseup': {
            'handler': 'onSelectionEnd',
            'element': globalThis
          },
          'mousemove': 'onSelectionDrag',
          'thisObj': c
        });
      }
      c.preventDragSelect = false;
      const A = c._selectionStartCell, B = c._isAddingToSelection;
      if ((u && v || c._isKeyboardRangeSelecting) && A && n) {
        c.performSelection(c.internalSelectRange(A, f, c.isRowNumberSelecting(A, f)));
      } else {
        let C = false, E = true, F;
        if (c.isCellSelected(f, true)) {
          if (w) {
            return;
          }
          if (B || p) {
            F = [f];
            E = false;
          } else if (c.selectedRecords.length + (c.isCellSelectionMode ? c._selectedCells.length : 0) <= 1) {
            return;
          }
          C = p || n;
        }
        if (!F && !B) {
          F = null;
          if (q && C && l.selectiondrag) {
            c._clearSelectionOnSelectionDrag = true;
          }
        }
        const G = (H, I) => {
          let J;
          I == null ? void 0 : I();
          if (((J = H == null ? void 0 : H.target) == null ? void 0 : J.nodeType) === Node.ELEMENT_NODE) {
            const K = new GridLocation(H.target);
            if ((K == null ? void 0 : K.grid) && !K.equals(f, true)) {
              return;
            }
          }
          if (!u) {
            c._shiftSelectRange = null;
          }
          c.performSelection(c.prepareSelection(F, E && [f], F === null, E && c.isRowNumberSelecting(f)));
        };
        if ((j = c.features.rowReorder) == null ? void 0 : j.isDragging) {
          return;
        }
        if (C) {
          c.delayUntilMouseUp(G);
        } else {
          G();
        }
      }
    }
    prepareSelection(c, e, f = false, g = false) {
      const i = this, j = i._isSelectionDragging, {
          includeParents: k,
          selectRecordOnCell: l
        } = i.selectionMode, n = [], o = f ? i._selectedCells : [], p = [], q = f ? i._selectedRows : [], r = ArrayHelper.keyBy(q, 'id', () => 1), u = f ? i.selectedRecords.filter(w => !r[w.id]) : [];
      let v = [];
      if (!f && c) {
        for (const w of ArrayHelper.asArray(c)) {
          const z = i.normalizeCellContext(w), A = (z == null ? void 0 : z.record) || (w.isModel ? w : i.store.getById(z.id));
          if (z.isSpecialRow) {
            continue;
          }
          o.push(z);
          if (A && !r[A.id]) {
            if (j || i.isSelected(A)) {
              q.push(A);
              r[A.id] = 1;
            } else if (l && i.selectedRecordCollection.get(A.id) && !u.some(B => B.id === A.id)) {
              u.push(A);
            }
            if (i.selectionShouldIncludeChildren(A)) {
              for (const B of A.allChildren) {
                if (!r[B.id] && (j || i.isSelected(B))) {
                  q.push(B);
                  r[A.id] = 1;
                }
              }
            }
          }
        }
      }
      if (e) {
        const C = {};
        for (const E of ArrayHelper.asArray(e)) {
          const F = i.normalizeCellContext(E), G = (F == null ? void 0 : F.record) || (E.isModel ? E : i.store.getById(F.id));
          if (!G || F.isSpecialRow) {
            continue;
          }
          if (i.isCellSelectionMode && !g) {
            n.push(F);
          }
          if ((i.isRowSelectionMode || g) && !C[G.id]) {
            p.push(G);
            C[G.id] = 1;
            if (i.selectionShouldIncludeChildren(G)) {
              for (const H of G.allChildren) {
                if (!C[H.id]) {
                  p.push(H);
                  C[H.id] = 1;
                }
              }
            }
          }
        }
        if (l && n.length) {
          v = ArrayHelper.unique(n.map(I => I.record)).filter(I => !C[I.id]);
        }
      }
      if (k && (q.length || p.length)) {
        const I = [
            ...q,
            ...p
          ], J = ArrayHelper.unique(I.filter(K => K.parent && !K.allChildren.some(L => I.includes(L))).map(K => K.parent));
        J.forEach(K => i.toggleParentSelection(K, p, q));
      }
      return i.cleanSelectionChange({
        'selectedCells': n,
        'selectedRecords': p,
        'deselectedCells': o,
        'deselectedRecords': q,
        'deselectAll': f,
        'action': (p == null ? void 0 : p.length) || (n == null ? void 0 : n.length) ? 'select' : 'deselect',
        'selectedCellRecords': v,
        'deselectedCellRecords': u
      });
    }
    toggleParentSelection(c, e, f) {
      if (!c || c.isRoot) {
        return;
      }
      const g = this.isSelected(c), i = e.includes(c), j = f.includes(c), k = l => this.isSelected(l) && !f.includes(l) || e.includes(l);
      if (this.selectionMode.includeParents === 'some') {
        if (c.allChildren.some(k)) {
          if ((!g || j) && !i) {
            e.push(c);
          }
        } else if (g && !j) {
          f.push(c);
        }
      } else {
        if (g) {
          if (!j && !i && c.allChildren.some(l => f.includes(l))) {
            f.push(c);
          }
        } else if (!i) {
          if (c.allChildren.every(k)) {
            e.push(c);
          }
        }
      }
      if (c.parent) {
        this.toggleParentSelection(c.parent, e, f);
      }
    }
    internalSelectRange(c, e, f = false) {
      const g = this, i = g.isRowSelectionMode || f, j = g.prepareSelection(g._shiftSelectRange, g.getRange(c, e, i), false, f);
      g._shiftSelectRange = j['selected' + (i ? 'Records' : 'Cells')];
      return j;
    }
    getRange(c, e, f = false) {
      const g = this, {store: i} = g, j = g.normalizeCellContext(c), k = g.normalizeCellContext(e), l = Math.min(j.rowIndex, k.rowIndex), n = Math.max(j.rowIndex, k.rowIndex), o = [], p = Math.min(j.columnIndex, k.columnIndex), q = Math.max(j.columnIndex, k.columnIndex);
      if (l === -1 || n === -1) {
        throw new Error('Record not found in selectRange');
      }
      if (f) {
        const r = i.getRange(l, n + 1, false);
        if (k.rowIndex < j.rowIndex) {
          r.reverse();
        }
        o.push(...r);
      } else {
        for (let u = l; u <= n; u++) {
          for (let v = p; v <= q; v++) {
            o.push({
              'rowIndex': u,
              'columnIndex': v
            });
          }
        }
      }
      return o.map(w => g.normalizeCellContext(w));
    }
    performSelection(c, e = true, f = false) {
      const g = this, {
          selectedRecords: selectedRecords = [],
          selectedCells: selectedCells = [],
          selectedCellRecords: selectedCellRecords = [],
          deselectedRecords: deselectedRecords = [],
          deselectedCells: deselectedCells = [],
          deselectedCellRecords: deselectedCellRecords = [],
          action: i
        } = c, j = [
          ...selectedRecords,
          ...selectedCellRecords
        ], k = [
          ...deselectedRecords,
          ...deselectedCellRecords
        ], l = g.isRowSelectionMode;
      if (g.trigger('beforeSelectionChange', {
          'mode': l ? 'row' : 'cell',
          'action': i,
          'selected': j,
          'deselected': k,
          'selection': g.selectedRecords,
          'selectedCells': selectedCells,
          'deselectedCells': deselectedCells,
          'cellSelection': g.selectedCells
        }) === false) {
        return false;
      }
      if (g._selectedCells === deselectedCells) {
        g._selectedCells = [];
      } else {
        const n = [];
        for (const o of g._selectedCells) {
          if (!deselectedCells.some(p => o.equals(p, true))) {
            n.push(o);
          }
        }
        g._selectedCells = n;
      }
      c.deselectedRecords = [...deselectedRecords];
      if (deselectedRecords === g._selectedRows) {
        g._selectedRows.length = 0;
        g.changeSelectedRecordCollectionSilent(p => p.clear());
      } else {
        const p = [], q = [], r = ArrayHelper.keyBy(k, 'id', () => 1);
        for (const u of g.selectedRecords) {
          if (!r[u.id]) {
            if (g.isSelected(u)) {
              p.push(u);
            } else {
              q.push(u);
            }
          }
        }
        g._selectedRows = p;
        g.changeSelectedRecordCollectionSilent(v => v.values = [
          ...p,
          ...q
        ]);
      }
      if (selectedCells.length) {
        for (const v of selectedCells) {
          if (!g._selectedCells.some(w => w.equals(v, true))) {
            g._selectedCells.push(v);
          }
        }
      }
      if (selectedRecords.length) {
        g._selectedRows.push(...selectedRecords.filter(w => !g._selectedRows.some(z => z.id === w.id)));
        g.changeSelectedRecordCollectionSilent(w => w.add(...selectedRecords));
      }
      if (selectedCellRecords.length) {
        g.changeSelectedRecordCollectionSilent(w => w.add(...selectedCellRecords));
      }
      if (e) {
        g.refreshGridSelectionUI(c);
      }
      g.afterSelectionChange(c);
      if (!f) {
        g.triggerSelectionChangeEvent(c);
      }
    }
    refreshGridSelectionUI({
      selectedRecords: c,
      selectedCells: e,
      deselectedRecords: f,
      deselectedCells: g
    }) {
      const i = this, {checkboxSelectionColumn: j} = i;
      j == null ? void 0 : j.suspendEvents();
      i.updateGridSelectionRecords(c, true);
      i.updateGridSelectionRecords(f, false);
      i.updateCheckboxHeader();
      j == null ? void 0 : j.resumeEvents();
      if (i.isCellSelectionMode) {
        i.updateGridSelectionCells(e, true);
        if (i.selectionMode.column) {
          i.updateGridSelectionColumns(e);
        }
      }
      i.updateGridSelectionCells(g, false);
    }
    updateGridSelectionRecords(c, e) {
      const {checkboxSelectionColumn: f} = this;
      if (c == null ? void 0 : c.length) {
        for (let g = 0; g < c.length; g++) {
          const j = this.getRowFor(c[g]);
          if (j) {
            j.toggleCls('b-selected', e);
            j.setAttribute('aria-selected', e);
            if (f && !f.hidden && !c[g].isSpecialRow) {
              j.getCell(f.id).widget.checked = e;
            }
          }
        }
      }
    }
    updateGridSelectionCells(c, e) {
      if (c == null ? void 0 : c.length) {
        for (let f = 0; f < c.length; f++) {
          const g = this.getCell(c[f]);
          if (g) {
            g.setAttribute('aria-selected', e);
            g.classList.toggle('b-selected', e);
          }
        }
      }
    }
    updateGridSelectionColumns(c) {
      let e;
      const {count: f} = this.store;
      for (const g of this.columns.visibleColumns) {
        (e = g.element) == null ? void 0 : e.classList.toggle('b-selected', (c == null ? void 0 : c.filter(i => i.columnId === g.id).length) === f);
      }
    }
    triggerSelectionChangeEvent(c) {
      const {
          selectedRecords: selectedRecords = [],
          selectedCells: selectedCells = [],
          selectedCellRecords: selectedCellRecords = [],
          deselectedRecords: deselectedRecords = [],
          deselectedCells: deselectedCells = [],
          deselectedCellRecords: deselectedCellRecords = []
        } = c, e = [
          ...selectedRecords,
          ...selectedCellRecords
        ], f = [
          ...deselectedRecords,
          ...deselectedCellRecords
        ], g = this.isRowSelectionMode;
      this.trigger('selectionChange', {
        'mode': g ? 'row' : 'cell',
        'action': c.action,
        'selected': e,
        'deselected': f,
        'selection': this.selectedRecords,
        'selectedCells': selectedCells,
        'deselectedCells': deselectedCells,
        'cellSelection': this.selectedCells
      });
    }
    doDestroy() {
      let c;
      ((c = this.selectedRecordCollection) == null ? void 0 : c.owner) === this && this.selectedRecordCollection.destroy();
      this._selectedCells.length = 0;
      this._selectedRows.length = 0;
      for (const e in this._selectionListenersDetachers) {
        this._selectionListenersDetachers[e]();
      }
      super.doDestroy();
    }
    get widgetClass() {
    }
  }, __publicField(b, 'configurable', {
    'selectionMode': {
      'cell': false,
      'multiSelect': true,
      'checkboxOnly': false,
      'checkbox': false,
      'checkboxPosition': null,
      'showCheckAll': false,
      'deselectFilteredOutRecords': false,
      'includeChildren': false,
      'includeParents': false,
      'preserveSelectionOnPageChange': false,
      'preserveSelectionOnDatasetChange': true,
      'deselectOnClick': false,
      'dragSelect': false,
      'selectOnKeyboardNavigation': true,
      'column': false,
      'rowNumber': false,
      'selectRecordOnCell': true
    },
    'keyMap': {
      'Shift+ArrowUp': 'extendSelectionUp',
      'Shift+ArrowDown': 'extendSelectionDown',
      'Shift+ArrowLeft': 'extendSelectionLeft',
      'Shift+ArrowRight': 'extendSelectionRight',
      ' ': {
        'handler': 'toggleSelection',
        'weight': 10
      }
    },
    'selectedRecordCollection': {},
    'selectionDisabled': false
  }), b;
};
const suspendStoreEvents = a => a.columns.suspendEvents();
const resumeStoreEvents = a => a.columns.resumeEvents();
const fillSubGridColumns = a => {
  a.columns.clearCaches();
  a.columns.fillFromMaster();
};
const compareStateSortIndex = (c, e) => c.stateSortIndex - e.stateSortIndex;
const GridState_default = a => {
  let b;
  return b = class extends (a || Base) {
    updateStore(c, e) {
      let f;
      (f = super.updateStore) == null ? void 0 : f.call(this, c, e);
      this.detachListeners('stateStoreListeners');
      c == null ? void 0 : c.ion({
        'name': 'stateStoreListeners',
        'filter': 'triggerUpdate',
        'group': 'triggerUpdate',
        'sort': 'triggerUpdate',
        'thisObj': this
      });
    }
    updateColumns(c, e) {
      let f;
      (f = super.updateColumns) == null ? void 0 : f.call(this, c, e);
      this.detachListeners('stateColumnListeners');
      c.ion({
        'name': 'stateColumnListeners',
        'change': 'triggerUpdate',
        'sort': 'triggerUpdate',
        'thisObj': this
      });
    }
    updateRowManager(c, e) {
      let f;
      (f = super.updateRowManager) == null ? void 0 : f.call(this, c, e);
      this.detachListeners('stateRowManagerListeners');
      c.ion({
        'name': 'stateRowManagerListeners',
        'rowHeight': 'triggerUpdate',
        'thisObj': this
      });
    }
    triggerUpdate() {
      this.trigger('stateChange');
    }
    finalizeInit() {
      super.finalizeInit();
      this.ion({
        'selectionChange': 'triggerUpdate',
        'toggleGroup': ({allRecords: c}) => !c && this.triggerUpdate(),
        'thisObj': this
      });
    }
    getState() {
      let c;
      const e = this, f = e.element.style.cssText, g = { 'rowHeight': e.rowHeight };
      if (f) {
        g.style = f;
      }
      if (e.selectedCell) {
        const {
          id: j,
          columnId: k
        } = e.selectedCell;
        g.selectedCell = {
          'id': j,
          'columnId': k
        };
      }
      if (!e.disableGridColumnIdWarning && e.columns.find(l => l.meta.generatedId === l.id)) {
        console.warn('Column(s) without id detected. When saving and restoring state, columns without a configured id can be restored incorrectly.');
      }
      g.selectedRecords = e.selectedRecords.map(l => l.id);
      g.columns = e.columns.allRecords.map(l => l.getState());
      g.store = e.store.state;
      g.scroll = e.storeScroll();
      const i = (c = e.features.treeGroup) == null ? void 0 : c.levels;
      if ((i == null ? void 0 : i.length) > 0 && !i.some(l => !l.fieldName)) {
        g.treeGroupLevels = e.features.treeGroup.levels.map(l => l.fieldName);
      }
      g.subGrids = {};
      e.eachSubGrid(l => {
        let n;
        const o = g.subGrids[l.region] = g.subGrids[l.region] || {};
        if (l.isPainted) {
          if (l.flex == null) {
            o.width = l.width;
          }
        } else {
          if (l.config.width != null) {
            o.width = l.config.width;
          } else {
            o.flex = l.config.flex;
          }
        }
        o.collapsed = (n = l.collapsed) != null ? n : false;
        if (o.collapsed) {
          o._beforeCollapseState = l._beforeCollapseState;
        }
      });
      for (const [l, n] of Object.entries(this.features)) {
        const o = n.state;
        if (o) {
          g.features = g.features || {};
          g.features[l] = o;
        }
      }
      return g;
    }
    applyState(c) {
      let e, f;
      const g = this, {columns: i} = g;
      c = Object.assign({}, c);
      g.suspendRefresh();
      if ('columns' in c) {
        let k = false, l = false;
        i.suspendEvents();
        g.eachSubGrid(suspendStoreEvents);
        c.columns.forEach((n, o) => {
          const p = i.lookupStateColumn(n);
          if (p) {
            const q = p.generation;
            if ('region' in n && !(n.region in g.subGrids)) {
              delete n.region;
              delete n.locked;
            }
            p.applyState(n);
            k = k || p.generation !== q;
            p.stateSortIndex = o;
            if (p.allIndex !== o) {
              l = k = true;
            }
          }
        });
        c.columns.forEach(n => {
          if ('parentId' in n) {
            const o = c.columns.find(r => r.id === n.parentId), p = o && i.lookupStateColumn(o), q = i.lookupStateColumn(n);
            if (p && q && q.parent !== p) {
              p.insertChild(q);
            }
          }
        });
        if (k) {
          g.eachSubGrid(fillSubGridColumns);
        }
        if (l) {
          g.eachSubGrid(n => {
            n.columns.records.sort(compareStateSortIndex);
            n.columns.allRecords.sort(compareStateSortIndex);
          });
          i.sort({
            'fn': compareStateSortIndex,
            'ascending': true
          });
        }
        if (g.isPainted && k) {
          g.renderContents();
        }
        i.resumeEvents();
        g.eachSubGrid(resumeStoreEvents);
        delete c.columns;
      }
      if ('subGrids' in c) {
        g.eachSubGrid(n => {
          if (n.region in c.subGrids) {
            const o = c.subGrids[n.region];
            if ('width' in o) {
              n.width = o.width;
            } else if ('flex' in o) {
              n.flex = o.flex;
            }
            if ('collapsed' in o) {
              n.collapsed = o.collapsed;
              n._beforeCollapseState = o._beforeCollapseState;
            }
          }
          n.clearWidthCache();
        });
        delete c.subGrids;
      }
      if ('store' in c) {
        g.store.state = c.store;
        delete c.store;
      }
      const j = c.scroll;
      delete c.scroll;
      if ('features' in c) {
        for (const n in c.features) {
          (f = (e = this.features[n]) == null ? void 0 : e.setConfig) == null ? void 0 : f.call(e, c.features[n]);
        }
        delete c.features;
      }
      ObjectHelper.assign(g, c);
      if ('treeGroupLevels' in c && g.features.treeGroup) {
        g.features.treeGroup.levels = c.treeGroupLevels;
      }
      g.resumeRefresh(true);
      g.whenVisible(() => g.applyScrollState(j));
    }
    applyScrollState(c) {
      const e = this;
      e.eachSubGrid(f => f.refreshFakeScroll());
      if (c) {
        e.restoreScroll(c);
        if (c.scrollTop) {
          e.element.querySelectorAll('.b-resize-monitored').forEach(f => {
            const g = Widget.fromElement(f);
            if (g) {
              g.onElementResize(f);
            }
          });
        }
      }
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'GridState'), __publicField(b, 'configurable', {
    'statefulEvents': [
      'subGridCollapse',
      'subGridExpand',
      'horizontalScrollEnd',
      'stateChange',
      'selectionChange',
      'collapseAllGroups',
      'expandAllGroups',
      'treeGroupChange'
    ]
  }), b;
};
const immediatePromise6 = Promise.resolve();
const defaultScrollOptions2 = { 'block': 'nearest' };
const SubGridScroller = class extends Scroller {
  static configurable = {
    'propagate': false,
    'overflowX': 'hidden-scroll',
    'yScroller': null
  };
  updateYScroller(a) {
    a == null ? void 0 : a.addScroller(this);
  }
  scrollIntoView(a, b = defaultScrollOptions2) {
    const c = this, {
        xDelta: e,
        yDelta: f
      } = c.getDeltaTo(a, b);
    let g = e || f ? c.scrollBy(e, f, b) : immediatePromise6;
    if (b.highlight || b.focus) {
      const i = g;
      g = g.then(() => {
        let j, k;
        if (i.cancelled) {
          return;
        }
        a = a instanceof Rectangle ? a : (k = (j = b.elementAfterScroll) == null ? void 0 : j.call(b)) != null ? k : a;
        if (b.highlight) {
          if (a instanceof Rectangle) {
            a.translate(-e, -f).highlight();
          } else {
            DomHelper.highlight(a);
          }
        }
        if (b.focus) {
          a.focus({ 'preventScroll': true });
        }
      });
      g.cancel = () => {
        let j;
        return (j = i.cancel) == null ? void 0 : j.call(i);
      };
    }
    return g;
  }
  scrollBy(a, b, c) {
    const e = b && this.yScroller.scrollBy(0, b, c), f = a && super.scrollBy(a, 0, c), g = Promise.all([
        f,
        e
      ]);
    g.cancel = () => {
      let i, j;
      (i = f == null ? void 0 : f.cancel) == null ? void 0 : i.call(f);
      (j = e == null ? void 0 : e.cancel) == null ? void 0 : j.call(e);
      g.cancelled = true;
    };
    if ((f == null ? void 0 : f.cancel) && (e == null ? void 0 : e.cancel)) {
      c.force = true;
    }
    return g;
  }
  scrollTo(a, b, c) {
    const e = b != null && this.yScroller.scrollTo(null, b, c), f = a != null && super.scrollTo(a, null, c);
    if (!(c && c.animate)) {
      this.syncPartners();
    }
    const g = Promise.all([
      f,
      e
    ]);
    g.cancel = () => {
      let i, j;
      (i = f == null ? void 0 : f.cancel) == null ? void 0 : i.call(f);
      (j = e == null ? void 0 : e.cancel) == null ? void 0 : j.call(e);
      g.cancelled = true;
    };
    if ((f == null ? void 0 : f.cancel) && (e == null ? void 0 : e.cancel)) {
      c.force = true;
    }
    return g;
  }
  get viewport() {
    const a = Rectangle.from(this.element), b = a.intersect(Rectangle.from(this.yScroller.element));
    return b || new Rectangle(a.x, a.y, a.width, 0);
  }
  set y(a) {
    if (this.yScroller) {
      this.yScroller.y = a;
    }
  }
  get y() {
    return this.yScroller ? this.yScroller.y : 0;
  }
  get maxY() {
    return this.yScroller ? this.yScroller.maxY : 0;
  }
  get scrollHeight() {
    return this.yScroller ? this.yScroller.scrollHeight : 0;
  }
  get clientHeight() {
    return this.yScroller ? this.yScroller.clientHeight : 0;
  }
  suspendEvents() {
    let a;
    super.suspendEvents();
    (a = this.yScroller) == null ? void 0 : a.suspendEvents();
  }
  resumeEvents() {
    let a;
    super.resumeEvents();
    (a = this.yScroller) == null ? void 0 : a.resumeEvents();
  }
};
SubGridScroller._$name = 'SubGridScroller';
const sumWidths = (a, b) => a + b.getBoundingClientRect().width;
const SubGrid = class extends Widget {
  static delayable = { 'hideOverlayScroller': 1000 };
  static configurable = {
    'element': true,
    'header': {},
    'footer': {},
    'virtualScrollerElement': true,
    'splitterElement': true,
    'headerSplitter': true,
    'scrollerSplitter': true,
    'footerSplitter': true,
    'collapsed': false,
    'resizable': null,
    'role': 'presentation'
  };
  static defaultConfig = {
    'insertRowsBefore': null,
    'appendTo': null,
    'monitorResize': true,
    'headerClass': null,
    'footerClass': null,
    'weight': null,
    'defaultBindProperty': null,
    'scrollable': { 'overflowX': 'hidden-scroll' },
    'scrollerClass': SubGridScroller,
    'hasCalculatedWidth': null,
    'sealedColumns': null
  };
  static type = 'subgrid';
  static $name = 'SubGrid';
  construct(a) {
    super.construct(a);
    this.rowManager.ion({
      'addRows': 'onAddRow',
      'thisObj': this
    });
    if (VersionHelper.isTestEnv) {
      this.hideOverlayScroller.delay = 50;
    }
  }
  doDestroy() {
    let a;
    const b = this;
    b.header.destroy();
    b.footer.destroy();
    (a = b.fakeScroller) == null ? void 0 : a.destroy();
    b.virtualScrollerElement.remove();
    b.splitterElements.forEach(c => c.remove());
    super.doDestroy();
  }
  get barConfig() {
    const a = this, {
        width: b,
        flex: c
      } = a.element.style, e = {
        'subGrid': a,
        'parent': a,
        'maxWidth': a.maxWidth || void 0,
        'minWidth': a.minWidth || void 0
      };
    if (c) {
      e.flex = c;
    } else if (b) {
      e.width = b;
    }
    return e;
  }
  changeHeader(a) {
    return new this.headerClass(ObjectHelper.assign({ 'id': this.id + '-header' }, this.barConfig, a));
  }
  changeFooter(a) {
    return new this.footerClass(ObjectHelper.assign({ 'id': this.id + '-footer' }, this.barConfig, a));
  }
  get splitterElements() {
    return [
      this.splitterElement,
      this.headerSplitter,
      this.scrollerSplitter,
      this.footerSplitter
    ];
  }
  toggleSplitterCls(a, b = true, c = true) {
    this.splitterElements.forEach(e => e == null ? void 0 : e.classList[b ? 'add' : 'remove'](a));
    if (c) {
      this.scrollable.eachPartner(({scroller: e}) => {
        let f, g;
        if (e.owner !== this) {
          (g = (f = e.owner).toggleSplitterCls) == null ? void 0 : g.call(f, a === 'b-hover' ? 'b-partner-splitter-hover' : 'b-hover', b, false);
        }
      });
    }
  }
  hideSplitter() {
    this.splitterElements.forEach(a => a.classList.add('b-hide-display'));
    this.$showingSplitter = false;
  }
  showSplitter() {
    this.splitterElements.forEach(a => a.classList.remove('b-hide-display'));
    this.$showingSplitter = true;
  }
  changeElement(a, b) {
    const {region: c} = this;
    return super.changeElement({
      'aria-label': c,
      'className': {
        'b-grid-subgrid': 1,
        ['b-grid-subgrid-' + c]: c,
        'b-grid-subgrid-collapsed': this.collapsed
      },
      'dataset': { 'region': c }
    }, b);
  }
  get rowElementConfig() {
    const {grid: a} = this;
    return {
      'role': 'row',
      'className': a.rowCls,
      'children': this.columns.visibleColumns.map((b, c) => ({
        'role': 'gridcell',
        'aria-colindex': c + 1,
        'tabIndex': a.cellTabIndex,
        'className': 'b-grid-cell',
        'dataset': {
          'column': b.field || '',
          'columnId': b.id
        }
      }))
    };
  }
  changeVirtualScrollerElement() {
    const a = DomHelper.createElement({
      'role': 'presentation',
      'reference': 'virtualScrollerElement',
      'className': 'b-virtual-scroller',
      'tabIndex': -1,
      'dataset': { 'region': this.region },
      'children': [{
          'reference': 'virtualScrollerWidth',
          'className': 'b-virtual-width'
        }]
    });
    this.virtualScrollerWidth = a.virtualScrollerWidth;
    return a.virtualScrollerElement;
  }
  changeSplitterElement() {
    const a = DomHelper.createElement({
      'reference': 'splitterElement',
      'className': {
        'b-grid-splitter': 1,
        'b-grid-splitter-collapsed': this.collapsed,
        'b-hide-display': 1
      },
      'dataset': { 'region': this.region },
      'children': [
        BrowserHelper.isTouchDevice ? { 'className': 'b-splitter-touch-area' } : null,
        {
          'className': 'b-grid-splitter-inner b-grid-splitter-main',
          'children': [{
              'className': 'b-grid-splitter-buttons',
              'reference': 'splitterButtons',
              'children': [
                {
                  'className': 'b-grid-splitter-button-collapse',
                  'children': [
                    BrowserHelper.isTouchDevice ? { 'className': 'b-splitter-button-touch-area' } : null,
                    {
                      'tag': 'svg',
                      'ns': 'http://www.w3.org/2000/svg',
                      'version': '1.1',
                      'className': 'b-grid-splitter-button-icon b-gridregion-collapse-arrow',
                      'viewBox': '0 0 256 512',
                      'children': [{
                          'tag': 'path',
                          'd': 'M192 448c-8.188 0-16.38-3.125-22.62-9.375l-160-160c-12.5-12.5-12.5-32.75 0-45.25l160-160c12.5-12.5 32.75-12.5 45.25 0s12.5 32.75 0 45.25L77.25 256l137.4 137.4c12.5 12.5 12.5 32.75 0 45.25C208.4 444.9 200.2 448 192 448z'
                        }]
                    }
                  ]
                },
                {
                  'className': 'b-grid-splitter-button-expand',
                  'children': [
                    BrowserHelper.isTouchDevice ? { 'className': 'b-splitter-button-touch-area' } : null,
                    {
                      'tag': 'svg',
                      'ns': 'http://www.w3.org/2000/svg',
                      'version': '1.1',
                      'className': 'b-grid-splitter-button-icon b-gridregion-expand-arrow',
                      'viewBox': '0 0 256 512',
                      'children': [{
                          'tag': 'path',
                          'd': 'M64 448c-8.188 0-16.38-3.125-22.62-9.375c-12.5-12.5-12.5-32.75 0-45.25L178.8 256L41.38 118.6c-12.5-12.5-12.5-32.75 0-45.25s32.75-12.5 45.25 0l160 160c12.5 12.5 12.5 32.75 0 45.25l-160 160C80.38 444.9 72.19 448 64 448z'
                        }]
                    }
                  ]
                }
              ]
            }]
        }
      ]
    });
    this.splitterButtons = a.splitterButtons;
    return a.splitterElement;
  }
  get splitterConfig() {
    return {
      'className': this.splitterElement.className.trim(),
      'children': [
        BrowserHelper.isTouchDevice ? { 'className': 'b-splitter-touch-area' } : null,
        { 'className': 'b-grid-splitter-inner' }
      ],
      'dataset': { 'region': this.region }
    };
  }
  changeHeaderSplitter() {
    return DomHelper.createElement(this.splitterConfig);
  }
  changeScrollerSplitter() {
    return DomHelper.createElement(this.splitterConfig);
  }
  changeFooterSplitter() {
    return DomHelper.createElement(this.splitterConfig);
  }
  render(...a) {
    const b = this;
    super.render(...a);
    if (b.grid) {
      b.updateHasFlex();
      b.element.parentNode.insertBefore(b.splitterElement, b.element.nextElementSibling);
      b.splitterElements.forEach(c => EventHelper.on({
        'element': c,
        'mouseenter': 'onSplitterMouseEnter',
        'mouseleave': 'onSplitterMouseLeave',
        'thisObj': b
      }));
      b._collapsed && b.collapse();
    }
  }
  toggleHeaders(a) {
    const b = this;
    if (a) {
      b.headerSplitter.remove();
      b.header.element.remove();
      b.scrollable.removePartner(b.header.scrollable, 'x');
    } else {
      const {grid: c} = b;
      if (!b.isConfiguring) {
        const e = c.items.indexOf(b) * 2;
        DomHelper.insertAt(c.headerContainer, b.headerSplitter, e);
        DomHelper.insertAt(c.headerContainer, b.header.element, e);
        b.refreshHeader();
      }
      b.scrollable.addPartner(b.header.scrollable, 'x');
    }
  }
  refreshHeader() {
    this.header.refreshContent();
  }
  refreshColumnHeader(a) {
    this.header.refreshColumn(a);
  }
  refreshFooter() {
    let a;
    (a = this.footer) == null ? void 0 : a.refreshContent();
  }
  get childItems() {
    return [
      this.header,
      this.footer
    ];
  }
  fixCellWidths(a) {
    const {visibleColumns: b} = this.columns;
    let c = a.firstElementChild, e = 0;
    while (c) {
      const f = b[e], {element: g} = f;
      if (f.minWidth) {
        c.style.minWidth = DomHelper.setLength(f.minWidth);
      }
      if (f.maxWidth) {
        c.style.maxWidth = DomHelper.setLength(f.maxWidth);
      }
      if (f.flex) {
        if (f.childLevel && g) {
          c.style.flex = '0 0 ' + g.getBoundingClientRect().width + 'px';
          c.style.width = '';
        } else {
          c.style.flex = f.flex;
          c.style.width = '';
        }
      } else if (f.width) {
        c.style.width = DomHelper.setLength(f.width);
      } else {
        c.style.flex = c.style.width = c.style.minWidth = '';
      }
      c = c.nextElementSibling;
      e++;
    }
  }
  get totalFixedWidth() {
    return this.columns.totalFixedWidth;
  }
  get headerScrollWidth() {
    return [...this.header.contentElement.children].reduce(sumWidths, 0) || this.columns.totalFixedWidth;
  }
  fixWidths() {
    const a = this, {
        element: b,
        header: c,
        footer: e
      } = a;
    if (!a.collapsed) {
      if (a.flex) {
        c.flex = a.flex;
        if (e) {
          e.flex = a.flex;
        }
        b.style.flex = a.flex;
      } else {
        if (a.hasCalculatedWidth && !a.columns.some(g => !g.hidden && g.flex) && a.totalFixedWidth !== a.width) {
          a.width = a.totalFixedWidth;
          a.hasCalculatedWidth = true;
          return;
        }
        a.clearWidthCache();
        let f = a.width;
        if (!f && a.hasCalculatedWidth) {
          f = 0;
          for (const g of a.columns) {
            if (!g.flex && !g.hidden)
              f += g.width;
          }
        }
        b.style.width = f + 'px';
        c.width = f;
        if (e) {
          e.width = f;
        }
      }
      a.handleHorizontalScroll(false);
    }
  }
  fixRowWidthsInSafariEdge() {
    if (BrowserHelper.isSafari) {
      const a = this, {
          region: b,
          header: c
        } = a, e = c.calculateMinWidthForSafari();
      a.rowManager.forEach(f => {
        const g = f.getElement(b);
        if (g) {
          g.style.width = e + 'px';
        }
      });
      c.headersElement.style.width = e + 'px';
    }
  }
  set width(a) {
    let b, c;
    const e = this;
    e.hasCalculatedWidth = false;
    super.width = a;
    e.header.width = a;
    e.footer.width = a;
    if (e.isPainted) {
      e.onElementResize();
    }
    if (!e.isExpanding && !e.isCollapsing && !e.isConfiguring) {
      (c = (b = e.grid).syncSplits) == null ? void 0 : c.call(b, f => f.subGrids[e.region] && (f.subGrids[e.region].width = a));
    }
  }
  get width() {
    return super.width;
  }
  set flex(a) {
    let b, c;
    const e = this;
    e.hasCalculatedWidth = false;
    e.header.flex = a;
    e.footer.flex = a;
    super.flex = a;
    if (e.isPainted) {
      e.onElementResize();
    }
    if (!e.isExpanding && !e.isCollapsing && !e.isConfiguring) {
      (c = (b = e.grid).syncSplits) == null ? void 0 : c.call(b, f => f.subGrids[e.region] && (f.subGrids[e.region].flex = a));
    }
  }
  get flex() {
    return super.flex;
  }
  onInternalResize(a, b, c, e, f) {
    let g;
    const i = this, {grid: j} = i;
    super.onInternalResize(...arguments);
    if (j == null ? void 0 : j.isPainted) {
      i.syncSplitterButtonPosition();
      if (b !== e) {
        i.trigger('beforeInternalResize', i);
        (g = j.beforeSubGridInternalResize) == null ? void 0 : g.call(j, i);
        j.trigger('horizontalScroll', {
          'grid': j,
          'subGrid': i,
          'scrollLeft': i.scrollable.scrollLeft,
          'scrollX': i.scrollable.x
        });
        j.trigger('horizontalScrollEnd', {
          'subGrid': i,
          'scrollLeft': i.scrollable.scrollLeft
        });
        i.fakeScroller && i.refreshFakeScroll();
        j.syncFlexedSubCols();
        i.fixRowWidthsInSafariEdge();
      }
      if (c !== f) {
        j.onHeightChange();
      }
      i.trigger('afterInternalResize', i);
    }
  }
  syncParallelSplitters(a) {
    const b = this, {grid: c} = b;
    if (b.splitterElement && b.$showingSplitter) {
      b.toggleSplitterCls('b-grid-splitter-collapsed', a);
    } else {
      const e = c.getSubGrid(c.getPreviousRegion(b.region));
      if (e && e.splitterElement) {
        e.syncParallelSplitters(a);
      }
    }
  }
  onSplitterMouseEnter() {
    const a = this, {nextSibling: b} = a;
    if (!a.collapsed && (!b || !b.collapsed)) {
      a.toggleSplitterCls('b-hover');
    }
    a.startSplitterButtonSyncing();
  }
  onSplitterMouseLeave() {
    const a = this, {nextSibling: b} = a;
    a.toggleSplitterCls('b-hover', false);
    if (!a.collapsed && (!b || !b.collapsed)) {
      a.stopSplitterButtonSyncing();
    }
  }
  startSplitterButtonSyncing() {
    const a = this;
    if (a.splitterElement) {
      a.syncSplitterButtonPosition();
      if (!a.splitterSyncScrollListener) {
        a.splitterSyncScrollListener = a.grid.scrollable.ion({
          'scroll': 'syncSplitterButtonPosition',
          'thisObj': a
        });
      }
    }
  }
  stopSplitterButtonSyncing() {
    if (this.splitterSyncScrollListener) {
      this.splitterSyncScrollListener();
      this.splitterSyncScrollListener = null;
    }
  }
  syncSplitterButtonPosition() {
    const {grid: a} = this;
    this.splitterButtons.style.top = a.scrollable.y + (a.bodyHeight - (this.headerSplitter ? a.headerHeight : 0)) / 2 + 'px';
  }
  get viewRectangle() {
    const {scrollable: a} = this;
    return new Rectangle(a.x, a.y, this.width || 0, this.rowManager.viewHeight);
  }
  updateHasFlex() {
    this.scrollable.element.classList.toggle('b-has-flex', this.columns.hasFlex);
  }
  updateResizable(a) {
    this.splitterElements.forEach(b => DomHelper.toggleClasses(b, ['b-disabled'], !a));
  }
  resizeColumnsToFitContent() {
    this.grid.beginGridMeasuring();
    this.columns.visibleColumns.forEach(a => {
      a.resizeToFitContent(null, null, true);
    });
    this.grid.endGridMeasuring();
  }
  get overflowingHorizontally() {
    return !this.collapsed && (this.grid.hideHeaders ? this.scrollable.hasOverflow('x') : this.header.scrollable.hasOverflow('x'));
  }
  get overflowingVertically() {
    return false;
  }
  refreshFakeScroll() {
    const a = this, {
        element: b,
        virtualScrollerElement: c,
        virtualScrollerWidth: e,
        header: f,
        footer: g,
        scrollable: i
      } = a, j = [...f.contentElement.children].reduce(sumWidths, 0);
    i.scrollWidth = j;
    b.style.setProperty('--total-column-width', a.headerScrollWidth + 'px');
    c.style.width = b.style.width;
    c.style.flex = b.style.flex;
    c.style.minWidth = b.style.minWidth;
    c.style.maxWidth = b.style.maxWidth;
    f.scrollable.syncOverflowState();
    g.scrollable.syncOverflowState();
    if (!a.collapsed) {
      if (a.overflowingHorizontally) {
        e.style.width = (i.scrollWidth || 0) + 'px';
        a.grid.virtualScrollers.classList.remove('b-hide-display');
      } else {
        e.style.width = 0;
      }
    }
  }
  initScroll() {
    const a = this, {
        scrollable: b,
        virtualScrollerElement: c,
        grid: e
      } = a;
    a.fakeScroller = new Scroller({
      'element': c,
      'overflowX': true,
      'widget': a
    });
    b.ion({
      'scroll': 'onSubGridScroll',
      'scrollend': 'onSubGridScrollEnd',
      'thisObj': a
    });
    if (!e.hideHorizontalScrollbar) {
      b.addPartner(a.fakeScroller, 'x');
      a.refreshFakeScroll();
    }
    if (!e.hideHeaders) {
      b.addPartner(a.header.scrollable, 'x');
    }
    if (!e.hideFooters) {
      b.addPartner(a.footer.scrollable, 'x');
    }
  }
  onSubGridScrollEnd(a) {
    const b = this, {grid: c} = b;
    b.scrollingToCenter = a == null ? void 0 : a.scrollingToCenter;
    b.scrolling = false;
    b.handleHorizontalScroll(false);
    if (!DomHelper.scrollBarWidth) {
      c.virtualScrollers.classList.remove('b-scrolling');
      b.hideOverlayScroller();
    }
    c.trigger('horizontalScrollEnd', { 'subGrid': b });
  }
  onSubGridScroll(a) {
    const b = this;
    b.scrollingToCenter = a == null ? void 0 : a.scrollingToCenter;
    b.handleHorizontalScroll();
  }
  showOverlayScroller() {
    this.hideOverlayScroller.cancel();
    this.virtualScrollerElement.classList.add('b-show-virtual-scroller');
  }
  hideOverlayScroller() {
    this.virtualScrollerElement.classList.remove('b-show-virtual-scroller');
  }
  set scrolling(a) {
    this._scrolling = a;
  }
  get scrolling() {
    return this._scrolling;
  }
  handleHorizontalScroll(a = true) {
    const b = this, {grid: c} = b;
    if (!b.scrolling && a) {
      b.scrolling = true;
      if (!DomHelper.scrollBarWidth) {
        c.virtualScrollers.classList.add('b-scrolling');
        b.showOverlayScroller();
      }
    }
    c.trigger('horizontalScroll', {
      'subGrid': b,
      'grid': c,
      'scrollLeft': b.scrollable.scrollLeft,
      'scrollX': b.scrollable.x,
      'scrollingToCenter': b == null ? void 0 : b.scrollingToCenter
    });
  }
  scrollColumnIntoView(a, b) {
    const {
        columns: c,
        header: e
      } = this, f = e.scrollable;
    a = a instanceof Column ? a : c.get(a) || c.getById(a) || c.getAt(a);
    if (a) {
      const g = e.getHeader(a.id);
      if (g) {
        return f.scrollIntoView(Rectangle.from(g, null, true), b);
      }
    }
  }
  onAddRow({
    source: source = this.grid.rowManager,
    rows: a
  }) {
    let b;
    const c = this, {region: e} = c, f = c.rowElementConfig, g = document.createDocumentFragment(), i = (b = source.rows[a[a.length - 1].index + 1]) == null ? void 0 : b.elements[e];
    a.forEach(j => {
      const k = DomHelper.createElement(f);
      DomHelper.syncClassList(k, j.cls);
      g.appendChild(k);
      j.addElement(e, k);
      c.fixCellWidths(k);
    });
    c.fixRowWidthsInSafariEdge();
    c.element.insertBefore(g, i || c.insertRowsBefore);
  }
  get rowElements() {
    return this.fromCache('.b-grid-row', true);
  }
  clearRows() {
    this.emptyCache();
    const a = this.element.querySelectorAll('.b-grid-row'), b = document.createRange();
    if (a.length) {
      b.setStartBefore(a[0]);
      b.setEndAfter(a[a.length - 1]);
      b.deleteContents();
    }
  }
  addNewRowElement() {
    const a = DomHelper.append(this.element, this.rowElementConfig);
    this.fixCellWidths(a);
    return a;
  }
  get store() {
    return this.grid.store;
  }
  get rowManager() {
    let a;
    return (a = this.grid) == null ? void 0 : a.rowManager;
  }
  changeCollapsed(a) {
    return Boolean(a);
  }
  updateCollapsed(a, b) {
    if (!this.isConfiguring) {
      this._fromPropertyUpdate = true;
      if (a) {
        this.collapse();
      } else {
        this.expand();
      }
      this._fromPropertyUpdate = false;
    }
  }
  async collapse(a, b = false) {
    const c = this, {grid: e} = c, {partners: f} = e;
    a = a && Object.keys(e.subGrids).length <= 2;
    if (e.rendered && c._collapsed === true && !c._fromPropertyUpdate) {
      return;
    }
    if (e.partners && !b) {
      e.isSyncingFromPartner = true;
      f.forEach(g => g.isSyncingFromPartner = true);
      await Promise.all([
        c.collapse(a, true),
        ...f.map(g => {
          let i;
          return (i = g.subGrids[c.region]) == null ? void 0 : i.collapse(a, true);
        })
      ]);
      f.forEach(g => g.isSyncingFromPartner = false);
      e.isSyncingFromPartner = false;
      return;
    }
    if (a) {
      e.element.classList.add('b-subgrid-width-transition');
      await e.runWithTransition(() => c._collapseInternal(true), true);
      if (c.isDestroyed) {
        return;
      }
      e.element.classList.remove('b-subgrid-width-transition');
    } else {
      c._collapseInternal(false);
    }
    e.trigger('subGridCollapse', { 'subGrid': c });
    e.afterToggleSubGrid({
      'subGrid': c,
      'collapsed': true
    });
    c.isCollapsing = false;
    e.trigger('afterInternalResize');
  }
  async _collapseInternal(a = false) {
    let b, c;
    const e = this, {
        grid: f,
        element: g
      } = e, i = f.getSubGrid(f.getNextRegion(e.region)), j = e.splitterElement ? e : e.previousSibling;
    let {_beforeCollapseState: k} = e, l = 0;
    e.isCollapsing = true;
    f.eachSubGrid(o => {
      o !== e && !o._collapsed && ++l;
    });
    (b = f.syncSplits) == null ? void 0 : b.call(f, o => {
      let p;
      return (p = o.subGrids[e.region]) == null ? void 0 : p.collapse(a);
    });
    if (l === 0) {
      if (!i) {
        return;
      }
      await i.expand();
    }
    let n = false;
    if (!k) {
      k = e._beforeCollapseState = {};
      if (e.width) {
        n = true;
      }
      k.width = e.width;
      k.elementWidth = g.style.width;
      if (g.style.flex !== '') {
        k.flex = g.style.flex;
        e.header.element.style.flex = g.style.flex = '';
      }
      if (i.element.style.flex === '') {
        k.nextRegionWidth = i.width;
        i.width = '';
        i.flex = '1 1 ' + k.nextRegionWidth + 'px';
      }
      g.classList.add('b-grid-subgrid-collapsed');
      e.virtualScrollerElement.classList.add('b-collapsed');
      e.header.element.classList.add('b-collapsed');
      e.footer.element.classList.add('b-collapsed');
      e._collapsed = true;
      e.width = '';
      if (!n) {
        e.syncParallelSplitters(true);
      }
    }
    if (!e.isDestroyed) {
      if (n) {
        f.refreshVirtualScrollbars();
        e.syncParallelSplitters(true);
        (c = j.startSplitterButtonSyncing) == null ? void 0 : c.call(j);
      }
    }
  }
  async expand(a, b = false) {
    const c = this, {grid: e} = c, {partners: f} = e;
    a = a && Object.keys(e.subGrids).length <= 2;
    if (e.rendered && c._collapsed !== true && !c._fromPropertyUpdate) {
      return;
    }
    if (f && !b) {
      e.isSyncingFromPartner = true;
      f.forEach(g => g.isSyncingFromPartner = true);
      await Promise.all([
        c.expand(a, true),
        ...f.map(g => {
          let i;
          return (i = g.subGrids[c.region]) == null ? void 0 : i.expand(a, true);
        })
      ]);
      f.forEach(g => g.isSyncingFromPartner = false);
      e.isSyncingFromPartner = false;
      return;
    }
    if (a) {
      e.element.classList.add('b-subgrid-width-transition');
      await e.runWithTransition(() => c._expandInternal(true), true);
      if (c.isDestroyed) {
        return;
      }
      e.element.classList.remove('b-subgrid-width-transition');
    } else {
      c._expandInternal(false);
    }
    c.columns.clearCaches();
    e.trigger('subGridExpand', { 'subGrid': c });
    e.afterToggleSubGrid({
      'subGrid': c,
      'collapsed': false
    });
    c.isExpanding = false;
    e.trigger('afterInternalResize');
  }
  async _expandInternal(a = false) {
    let b;
    const c = this, {
        grid: e,
        _beforeCollapseState: f
      } = c, g = e.getSubGrid(e.getNextRegion(c.region)), i = c.splitterElement ? c : c.previousSibling;
    c.isExpanding = true;
    (b = e.syncSplits) == null ? void 0 : b.call(e, j => {
      let k;
      return (k = j.subGrids[c.region]) == null ? void 0 : k.expand(a);
    });
    if (f != null) {
      let j = false;
      if (c.width !== f.elementWidth) {
        j = true;
      }
      if (f.nextRegionWidth) {
        g.width = f.nextRegionWidth;
        g.flex = null;
      }
      c.element.classList.remove('b-grid-subgrid-collapsed');
      c._collapsed = false;
      c.virtualScrollerElement.classList.remove('b-collapsed');
      c.header.element.classList.remove('b-collapsed');
      c.footer.element.classList.remove('b-collapsed');
      if (f.flex) {
        c.width = f.width;
        c.header.flex = c.flex = f.flex;
        c.footer.flex = f.flex;
        c._width = null;
      } else {
        c.width = f.elementWidth;
      }
      if (j) {
        i.stopSplitterButtonSyncing();
        c.syncParallelSplitters(false);
      }
      delete c._beforeCollapseState;
    }
  }
  hide() {
    let a, b;
    (a = this.header) == null ? void 0 : a.hide();
    (b = this.footer) == null ? void 0 : b.hide();
    this.hideSplitter();
    return super.hide();
  }
  show() {
    let a, b;
    const c = this;
    (a = c.header) == null ? void 0 : a.show();
    (b = c.footer) == null ? void 0 : b.show();
    if (c.region !== c.grid.regions[c.grid.regions.length - 1]) {
      c.showSplitter();
    }
    return super.show();
  }
};
SubGrid.initClass();
SubGrid._$name = 'SubGrid';
const GridSubGrids_default = a => class b extends (a || Base) {
  static get $name() {
    return 'GridSubGrids';
  }
  static get properties() {
    return { 'subGrids': {} };
  }
  changeSubGridConfigs(c) {
    const e = this, f = new Set();
    for (const g of e.columns) {
      const {region: i} = g;
      if (i) {
        if (!c[i]) {
          c[i] = {};
        }
        f.add(i);
      }
    }
    if (c.normal && ObjectHelper.isEmpty(c.normal)) {
      c.normal = { 'flex': 1 };
    }
    for (const j of f) {
      e.createSubGrid(j, c[j]);
    }
    e.items = e.subGrids;
    return c;
  }
  createSubGrid(c, e = null) {
    const f = this, g = f.columns.makeChained(l => l.region === c, ['region']), i = ObjectHelper.assign({
        'type': 'subgrid',
        'id': f.id + '-' + c + 'Subgrid',
        'parent': f,
        'grid': f,
        'region': c,
        'headerClass': f.headerClass,
        'footerClass': f.footerClass,
        'columns': g,
        'weight': c,
        'scrollable': { 'yScroller': f.scrollable }
      }, e || f.subGridConfigs[c]);
    let j = false;
    if (!i.flex && !i.width) {
      i.width = g.totalFixedWidth;
      j = true;
    }
    const k = f.subGrids[c] = SubGrid.create(i);
    k.element.style.setProperty('--total-column-width', g.totalFixedWidth);
    k.hasCalculatedWidth = j;
    if (c === f.regions[0]) {
      k.isFirstRegion = true;
    }
    return k;
  }
  onChildAdd(c) {
    if (c.isSubGrid) {
      const e = this, {
          items: f,
          headerContainer: g,
          virtualScrollers: i,
          footerContainer: j
        } = e, k = f.indexOf(c) * 2;
      if (!e.hideHeaders || e.columns.some(l => l.flex)) {
        DomHelper.insertAt(g, c.headerSplitter, k);
        DomHelper.insertAt(g, c.header.element, k);
      }
      DomHelper.insertAt(i, c.scrollerSplitter, k);
      DomHelper.insertAt(i, c.virtualScrollerElement, k);
      DomHelper.insertAt(j, c.footerSplitter, k);
      DomHelper.insertAt(j, c.footer.element, k);
      f.forEach((l, n) => {
        if (n < f.length - 1) {
          l.showSplitter();
        }
      });
      if (k === 0 && e.emptyTextEl) {
        c.element.appendChild(e.emptyTextEl);
      }
    }
    return super.onChildAdd(c);
  }
  onChildRemove(c) {
    super.onChildRemove(c);
    if (c.isSubGrid) {
      const {items: e} = this;
      delete this.subGrids[c.region];
      ArrayHelper.remove(this.regions, c.region);
      c.destroy();
      if (e.length) {
        e[e.length - 1].hideSplitter();
      }
    }
  }
  doDestroy() {
    this.eachSubGrid(c => c.destroy());
    super.doDestroy();
  }
  eachSubGrid(c, e = null) {
    this.items.forEach((f, g) => {
      f.isSubGrid && c.call(e || f, f, g++);
    });
  }
  callEachSubGrid(c, ...e) {
    let f = null;
    this.items.forEach((g, j) => {
      if (g.isSubGrid && g[c]) {
        const k = g[c](...e);
        if (j === 0)
          f = k;
      }
    });
    return f;
  }
  get regions() {
    return this.items.map(c => c.region);
  }
  getLastRegions() {
    const c = this.regions.slice(-2);
    return c.length === 2 ? c : [
      c[0],
      c[0]
    ];
  }
  getNextRegion(c) {
    const e = this.regions;
    return e[e.indexOf(c) + 1] || e[e.length - 2];
  }
  getPreviousRegion(c) {
    return this.regions[this.regions.indexOf(c) - 1];
  }
  getSubGrid(c) {
    return this.subGrids[c];
  }
  getSubGridFromColumn(c) {
    c = c instanceof Column ? c : this.columns.getById(c) || this.columns.get(c);
    return this.getSubGrid(c.region);
  }
  resolveSplitter(c) {
    const e = this.getLastRegions();
    let f = c instanceof SubGrid ? c.region : c;
    if (e[1] === f) {
      f = e[0];
    }
    return this.subGrids[f].splitterElement;
  }
  get widgetClass() {
  }
};
const emptyString2 = new String();
const locale3 = {
  'localeName': 'En',
  'localeDesc': 'English (US)',
  'localeCode': 'en-US',
  'ColumnPicker': {
    'column': 'Column',
    'columnsMenu': 'Columns',
    'hideColumn': 'Hide column',
    'hideColumnShort': 'Hide',
    'newColumns': 'New columns'
  },
  'Filter': {
    'applyFilter': 'Apply filter',
    'filter': 'Filter',
    'editFilter': 'Edit filter',
    'on': 'On',
    'before': 'Before',
    'after': 'After',
    'equals': 'Equals',
    'lessThan': 'Less than',
    'moreThan': 'More than',
    'removeFilter': 'Remove filter',
    'disableFilter': 'Disable filter'
  },
  'FilterBar': {
    'enableFilterBar': 'Show filter bar',
    'disableFilterBar': 'Hide filter bar'
  },
  'Group': {
    'group': 'Group',
    'groupAscending': 'Group ascending',
    'groupDescending': 'Group descending',
    'groupAscendingShort': 'Ascending',
    'groupDescendingShort': 'Descending',
    'stopGrouping': 'Stop grouping',
    'stopGroupingShort': 'Stop'
  },
  'HeaderMenu': {
    'moveBefore': a => 'Move before "' + a + '"',
    'moveAfter': a => 'Move after "' + a + '"',
    'collapseColumn': 'Collapse column',
    'expandColumn': 'Expand column'
  },
  'ColumnRename': { 'rename': 'Rename' },
  'MergeCells': {
    'mergeCells': 'Merge cells',
    'menuTooltip': 'Merge cells with same value when sorted by this column'
  },
  'Search': { 'searchForValue': 'Search for value' },
  'Sort': {
    'sort': 'Sort',
    'sortAscending': 'Sort ascending',
    'sortDescending': 'Sort descending',
    'multiSort': 'Multi sort',
    'removeSorter': 'Remove sorter',
    'addSortAscending': 'Add ascending sorter',
    'addSortDescending': 'Add descending sorter',
    'toggleSortAscending': 'Change to ascending',
    'toggleSortDescending': 'Change to descending',
    'sortAscendingShort': 'Ascending',
    'sortDescendingShort': 'Descending',
    'removeSorterShort': 'Remove',
    'addSortAscendingShort': '+ Ascending',
    'addSortDescendingShort': '+ Descending'
  },
  'Split': {
    'split': 'Split',
    'unsplit': 'Unsplit',
    'horizontally': 'Horizontally',
    'vertically': 'Vertically',
    'both': 'Both'
  },
  'LockRows': {
    'lockRow': 'Lock row',
    'unlockRow': 'Unlock row'
  },
  'Column': {
    'columnLabel': a => (a.text ? a.text + ' column. ' : '') + 'SPACE for context menu' + (a.sortable ? ', ENTER to sort' : ''),
    'cellLabel': emptyString2
  },
  'Checkbox': {
    'toggleRowSelect': 'Toggle row selection',
    'toggleSelection': 'Toggle selection of entire dataset'
  },
  'RatingColumn': {
    'cellLabel': a => {
      let b;
      return (a.text ? a.text : '') + ' ' + (((b = a.location) == null ? void 0 : b.record) ? 'rating : ' + (a.location.record.get(a.field) || 0) : '');
    }
  },
  'GridBase': {
    'loadFailedMessage': 'Data loading failed!',
    'syncFailedMessage': 'Data synchronization failed!',
    'unspecifiedFailure': 'Unspecified failure',
    'networkFailure': 'Network error',
    'parseFailure': 'Failed to parse server response',
    'serverResponse': 'Server response:',
    'noRows': 'No records to display',
    'moveColumnLeft': 'Move to left section',
    'moveColumnRight': 'Move to right section',
    'moveColumnTo': a => 'Move column to ' + a
  },
  'CellMenu': { 'removeRow': 'Delete' },
  'RowCopyPaste': {
    'copyRecord': 'Copy',
    'cutRecord': 'Cut',
    'pasteRecord': 'Paste',
    'rows': 'rows',
    'row': 'row'
  },
  'CellCopyPaste': {
    'copy': 'Copy',
    'cut': 'Cut',
    'paste': 'Paste'
  },
  'PdfExport': {
    'Waiting for response from server': 'Waiting for response from server...',
    'Export failed': 'Export failed',
    'Server error': 'Server error',
    'Generating pages': 'Generating pages...',
    'Click to abort': 'Cancel'
  },
  'ExportDialog': {
    'width': '40em',
    'labelWidth': '12em',
    'exportSettings': 'Export settings',
    'export': 'Export',
    'printSettings': 'Print settings',
    'print': 'Print',
    'exporterType': 'Control pagination',
    'cancel': 'Cancel',
    'fileFormat': 'File format',
    'rows': 'Rows',
    'alignRows': 'Align rows',
    'columns': 'Columns',
    'paperFormat': 'Paper format',
    'orientation': 'Orientation',
    'repeatHeader': 'Repeat header'
  },
  'ExportRowsCombo': {
    'all': 'All rows',
    'visible': 'Visible rows'
  },
  'ExportOrientationCombo': {
    'portrait': 'Portrait',
    'landscape': 'Landscape'
  },
  'SinglePageExporter': { 'singlepage': 'Single page' },
  'MultiPageExporter': {
    'multipage': 'Multiple pages',
    'exportingPage': ({
      currentPage: a,
      totalPages: b
    }) => 'Exporting page ' + a + '/' + b
  },
  'MultiPageVerticalExporter': {
    'multipagevertical': 'Multiple pages (vertical)',
    'exportingPage': ({
      currentPage: a,
      totalPages: b
    }) => 'Exporting page ' + a + '/' + b
  },
  'RowExpander': {
    'loading': 'Loading',
    'expand': 'Expand',
    'collapse': 'Collapse'
  },
  'TreeGroup': {
    'group': 'Group by',
    'stopGrouping': 'Stop grouping',
    'stopGroupingThisColumn': 'Ungroup column'
  }
};
const En_default2 = LocaleHelper.publishLocale(locale3);
const resolvedPromise = new Promise(a => a());
const storeListenerName2 = 'GridBase:store';
const defaultScrollOptions3 = {
  'block': 'nearest',
  'inline': 'nearest'
};
const datasetReplaceActions = {
  'dataset': 1,
  'pageLoad': 1,
  'filter': 1
};
const fullRefreshAfterUpdateFields = {
  'hidden': 1,
  'mergeCells': 1,
  'region': 1
};
const emptyArray12 = Object.freeze([]);
const ascending = (a, b) => a - b;
const GridBase = class extends Panel.mixin(Pluggable_default, State_default, GridElementEvents_default, GridFeatures_default, GridNavigation_default, GridResponsive_default, GridSelection_default, GridState_default, GridSubGrids_default, LoadMaskable_default) {
  static bindStoreChangeset = true;
  _animationSuspendedCounter = 0;
  constructor() {
    super(...arguments);
  }
  static get $name() {
    return 'GridBase';
  }
  static get type() {
    return 'gridbase';
  }
  static get delayable() {
    return {
      'onGridVerticalScroll': { 'type': 'raf' },
      'bufferedAfterColumnsResized': 250,
      'bufferedElementResize': 250
    };
  }
  static get configurable() {
    return {
      'autoHeight': null,
      'disableAutoHeightWarning': null,
      'disableNoIDWarning': null,
      'enableSticky': null,
      'enableTextSelection': null,
      'fillLastColumn': true,
      'positionMode': 'translate',
      'showDirty': null,
      'subGridConfigs': { 'normal': { 'flex': 1 } },
      'store': {
        'value': {},
        '$config': 'nullify'
      },
      'rowManager': {
        'value': {},
        '$config': [
          'nullify',
          'lazy'
        ]
      },
      'scrollManager': {
        'value': {},
        '$config': [
          'nullify',
          'lazy'
        ]
      },
      'columns': {
        'value': [],
        '$config': 'nullify'
      },
      'minHeight': '10em',
      'hideHeaders': null,
      'hideFooters': true,
      'hideHorizontalScrollbar': null,
      'contentElMutationObserver': false,
      'trapFocus': false,
      'ariaElement': 'bodyElement',
      'cellTabIndex': -1,
      'rowCls': {
        'value': 'b-grid-row',
        '$config': { 'merge': this.mergeCls }
      },
      'cellCls': {
        'value': 'b-grid-cell',
        '$config': { 'merge': this.mergeCls }
      },
      'emptyText': 'L{noRows}',
      'sortFeatureStore': '$store',
      'rowHeight': null,
      'preserveScroll': false,
      'animateTreeNodeToggle': !VersionHelper.isTestEnv,
      'columnLines': true,
      'rowLines': true,
      'fixedRowHeight': null
    };
  }
  static get defaultConfig() {
    return {
      'defaultRowHeight': 45,
      'fullRowRefresh': true,
      'preserveScrollOnDatasetChange': null,
      'preserveFocusOnDatasetChange': true,
      'data': null,
      'defaultRegion': 'normal',
      'destroyStore': null,
      'maskDefaults': {
        'cover': 'body',
        'target': 'element'
      },
      'resizeToFitIncludesHeader': true,
      'animateRemovingRows': true,
      'disableGridRowModelWarning': null,
      'disableGridColumnIdWarning': null,
      'headerClass': Header,
      'footerClass': Footer,
      'testPerformance': false,
      'rowScrollMode': 'move',
      'monitorResize': true,
      'features': true,
      'scrollable': { 'overflowY': true },
      'scrollerClass': GridScroller,
      'refreshSuspended': 0,
      'transitionDuration': 500,
      'contextMenuTriggerEvent': 'contextmenu',
      'localizableProperties': ['emptyText'],
      'asyncEventSuffix': '',
      'fixElementHeightsBuffer': 350,
      'testConfig': {
        'transitionDuration': 50,
        'fixElementHeightsBuffer': 50
      }
    };
  }
  static get properties() {
    return {
      '_selectedRecords': [],
      '_verticalScrollHeight': 0,
      'virtualScrollHeight': 0,
      '_scrollTop': null
    };
  }
  finishConfigure(a) {
    const b = this, {initScroll: c} = b;
    b.initScroll = () => !b.scrollInitialized && c.call(b);
    if (VersionHelper.isTestEnv) {
      b.bufferedAfterColumnsResized.delay = 50;
      b.bufferedElementResize.delay = 50;
    }
    super.finishConfigure(a);
    LocaleManager_default.ion({
      'locale': 'onBeforeLocaleChange',
      'prio': 1,
      'thisObj': b
    });
    LocaleManager_default.ion({
      'locale': 'onLocaleChange',
      'prio': -1,
      'thisObj': b
    });
    GlobalEvents_default.ion({
      'theme': 'onThemeChange',
      'thisObj': b
    });
    b.ion({
      'subGridExpand': 'internalOnSubGridExpand',
      'prio': -1,
      'thisObj': b
    });
    b.bufferedFixElementHeights = b.buffer('fixElementHeights', b.fixElementHeightsBuffer, b);
    b.setGridClassList(b.element.classList);
    b.verticalScroller.classList.remove('b-content-element', 'b-auto-container');
    b.bodyWrapElement.classList.remove('b-auto-container-panel');
  }
  internalOnSubGridExpand() {
    this.renderContents();
  }
  onBeforeLocaleChange() {
    this._suspendRenderContentsOnColumnsChanged = true;
  }
  onLocaleChange() {
    this._suspendRenderContentsOnColumnsChanged = false;
    if (this.isPainted) {
      this.renderContents();
    }
  }
  finalizeInit() {
    super.finalizeInit();
    if (this.store.isLoading) {
      this.onStoreBeforeRequest();
    }
  }
  changeScrollManager(a, b) {
    b == null ? void 0 : b.destroy();
    if (a) {
      return ScrollManager.new({
        'element': this.element,
        'owner': this
      }, a);
    } else {
      return null;
    }
  }
  doDestroy() {
    let a, b;
    const c = this;
    c.detachListeners(storeListenerName2);
    (a = c.scrollManager) == null ? void 0 : a.destroy();
    for (const e of Object.values(c.features)) {
      (b = e.destroy) == null ? void 0 : b.call(e);
    }
    c._focusedCell = null;
    c.columns.destroy();
    super.doDestroy();
  }
  setGridClassList(a) {
    const b = this;
    Object.values(b.features).forEach(c => {
      if (c.disabled || c === false) {
        return;
      }
      let e;
      if (Object.prototype.hasOwnProperty.call(c.constructor, 'featureClass')) {
        e = c.constructor.featureClass;
      } else {
        e = 'b-' + (c instanceof Base ? c.$$name : c.constructor.name);
      }
      if (e) {
        a.add(e.toLowerCase());
      }
    });
  }
  compose() {
    const {
      autoHeight: a,
      enableSticky: b,
      enableTextSelection: c,
      fillLastColumn: e,
      positionMode: f,
      showDirty: g
    } = this;
    return {
      'class': {
        ['b-grid-' + f]: 1,
        'b-enable-sticky': b,
        'b-grid-notextselection': !c,
        'b-autoheight': a,
        'b-fill-last-column': e,
        'b-show-dirty': g,
        'b-show-dirty-during-edit': g == null ? void 0 : g.duringEdit
      }
    };
  }
  async recomposeInternal() {
    let a;
    let b;
    const {isConstructing: c} = this;
    if (!c) {
      b = this.storeScroll();
    }
    await super.recomposeInternal();
    if (!c) {
      const e = [
        ...Object.keys(b.scrollLeft).map(f => b.scrollLeft[f]),
        b.scrollTop
      ];
      if (Math.max(...e)) {
        const f = this.storeScroll();
        if (!Math.max([
            ...Object.keys(f.scrollLeft).map(g => f.scrollLeft[g]),
            f.scrollTop
          ])) {
          (a = this.restoreScroll) == null ? void 0 : a.call(this, b);
        }
      }
    }
  }
  get cellCls() {
    const {_cellCls: a} = this;
    return a.value || a;
  }
  get bodyConfig() {
    const {
      autoHeight: a,
      hideFooters: b,
      hideHeaders: c
    } = this;
    return {
      'reference': 'bodyElement',
      'className': {
        'b-autoheight': a,
        'b-grid-panel-body': 1
      },
      [this.hasHeader ? 'ariaLabelledBy' : '']: this.id + '-panel-title',
      'children': {
        'headerContainer': {
          'tag': 'header',
          'role': 'row',
          'aria-rowindex': 1,
          'className': {
            'b-grid-header-container': 1,
            'b-hidden': c
          }
        },
        'bodyContainer': {
          'className': 'b-grid-body-container',
          'tabIndex': -1,
          'role': 'presentation',
          'children': { 'verticalScroller': { 'className': 'b-grid-vertical-scroller' } }
        },
        'virtualScrollers': { 'className': 'b-virtual-scrollers b-hide-display' },
        'footerContainer': {
          'tag': 'footer',
          'className': {
            'b-grid-footer-container': 1,
            'b-hidden': b
          }
        }
      }
    };
  }
  get contentElement() {
    return this.verticalScroller;
  }
  get overflowElement() {
    return this.bodyContainer;
  }
  updateHideHeaders(a) {
    let b;
    a = Boolean(a);
    (b = this.headerContainer) == null ? void 0 : b.classList.toggle('b-hidden', a);
    this.eachSubGrid(c => c.toggleHeaders(a));
  }
  updateHideFooters(a) {
    let b;
    a = Boolean(a);
    (b = this.footerContainer) == null ? void 0 : b.classList.toggle('b-hidden', a);
    this.eachSubGrid(c => {
      c.scrollable[a ? 'removePartner' : 'addPartner'](c.footer.scrollable, 'x');
    });
  }
  updateHideHorizontalScrollbar(a) {
    a = Boolean(a);
    this.eachSubGrid(b => {
      b.virtualScrollerElement.classList.toggle('b-hide-display', a);
      b.scrollable[a ? 'removePartner' : 'addPartner'](b.fakeScroller, 'x');
      if (!a) {
        b.refreshFakeScroll();
      }
    });
  }
  changeColumns(a, b) {
    const c = this;
    if (!a && b) {
      if (c.isDestroying) {
        b.owner === c && b.destroy();
      } else {
        b.removeAll();
      }
      return b;
    }
    if (a.isStore) {
      (b == null ? void 0 : b.owner) === c && b.destroy();
      a.grid = c;
      return a;
    }
    if (Array.isArray(a)) {
      if (b) {
        const e = b.allRecords.slice();
        b.data = a;
        for (const f of e) {
          if (!b.includes(f)) {
            f.destroy();
          }
        }
        return b;
      }
      a = { 'data': a };
    }
    if (b) {
      throw new Error('Replacing ColumnStore is not supported');
    }
    return ColumnStore.new({
      'grid': c,
      'owner': c
    }, a);
  }
  updateColumns(a, b) {
    let c, e;
    const f = this;
    (c = super.updateColumns) == null ? void 0 : c.call(this, a, b);
    a.ion({
      'refresh': f.onColumnsChanged,
      'sort': f.onColumnsChanged,
      'change': f.onColumnsChanged,
      'move': f.onColumnsChanged,
      'thisObj': f
    });
    a.ion(columnResizeEvent(f.onColumnsResized, f));
    if (BrowserHelper.isTouchDevice) {
      f.touch = true;
      a.forEach(g => {
        const {touchConfig: i} = g;
        if (i) {
          g.applyState(i);
        }
      });
    }
    (e = f.bodyElement) == null ? void 0 : e.setAttribute('aria-colcount', a.visibleColumns.length);
  }
  onColumnsChanged({
    type: a,
    action: b,
    changes: c,
    record: e,
    records: f,
    isMove: g
  }) {
    let i;
    const j = this, {
        columns: k,
        checkboxSelectionColumn: l
      } = j, n = c && Object.keys(c), o = (n == null ? void 0 : n.length) === 1;
    g = g === true ? true : g && Object.values(g).some(q => q);
    j.beforeColumnsChange({
      'type': a,
      'action': b,
      'changes': c,
      'column': e,
      'columns': f
    });
    if (g || a === 'refresh' && b !== 'batch' && b !== 'sort' || b === 'update' && o && 'parentIndex' in c || b === 'sort' && k.isSyncingDataOnLoad) {
      return;
    }
    const p = b === 'add' && f.some(q => q.region && !j.subGrids[q.region]);
    if (j.isConfiguring || !p && (!j.isPainted || g && b === 'remove')) {
      return;
    }
    if (b === 'add') {
      for (const q of f) {
        const {region: r} = q;
        if (!j.subGrids[r]) {
          j.add(j.createSubGrid(r, (i = j.subGridConfigs) == null ? void 0 : i[r]));
        }
      }
    } else if (b === 'update') {
      const u = n[0];
      if (('width' in c || 'minWidth' in c || 'maxWidth' in c || 'flex' in c) && !('region' in c)) {
        const {region: v} = e;
        k.visibleColumns.forEach(w => {
          if (w.region === v && w.repaintOnResize) {
            j.refreshColumn(w);
          }
        });
        j.afterColumnsChange({
          'type': a,
          'action': b,
          'changes': c,
          'column': e
        });
        return;
      }
      if ('text' in c && o) {
        e.subGrid.refreshHeader();
        return;
      }
      if ('hidden' in c) {
        const w = j.getSubGridFromColumn(e.id);
        w.header.fixHeaderWidths();
        w.footer.fixFooterWidths();
        w.updateHasFlex();
      } else if (o && !(u in fullRefreshAfterUpdateFields) && !e.hidden && !e.subGrid.collapsed) {
        e.subGrid.refreshHeader();
        j.refreshColumn(e);
        return;
      }
    } else if (b === 'remove') {
      f.forEach(z => {
        let A;
        return (A = z.freeColumnWidgets) == null ? void 0 : A.call(z);
      });
    }
    if (b === 'dataset' || b === 'batch' || b === 'update' && 'region' in c) {
      const z = k.getDistinctValues('region', true), {
          toRemove: A,
          toAdd: B
        } = ArrayHelper.delta(z, j.regions, true);
      j.remove(A.map(C => j.getSubGrid(C)));
      j.add(B.map(C => j.createSubGrid(C, j.subGridConfigs[C])));
    }
    if (l && !k.includes(l)) {
      const C = k.indexOf(k.findRecord('type', 'rownumber')) + 1;
      k.insert(C, l, true);
    }
    if (!j._suspendRenderContentsOnColumnsChanged) {
      j.renderContents();
    }
    j.syncFlexedSubCols();
    j.bodyElement.setAttribute('aria-colcount', k.visibleColumns.length);
    j.afterColumnsChange({
      'type': a,
      'action': b,
      'changes': c,
      'column': e,
      'columns': f
    });
  }
  onColumnsResized({
    changes: a,
    record: b
  }) {
    let c;
    const e = this;
    if (e.isConfiguring) {
      return;
    }
    const f = DomHelper.setLength(b.width), g = DomHelper.setLength(b.minWidth), i = DomHelper.setLength(b.maxWidth), j = e.getSubGridFromColumn(b.id);
    j.header.fixHeaderWidths();
    j.footer.fixFooterWidths();
    j.updateHasFlex();
    if (!(b.flex && b.childLevel)) {
      if (!e.cellEls || b !== e.lastColumnResized) {
        e.cellEls = DomHelper.children(e.element, '.b-grid-cell[data-column-id="' + b.id + '"]');
        e.lastColumnResized = b;
      }
      for (const k of e.cellEls) {
        if ('width' in a) {
          k.style.width = f;
        }
        if ('minWidth' in a) {
          k.style.minWidth = g;
        }
        if ('maxWidth' in a) {
          k.style.maxWidth = i;
        }
        if ('flex' in a) {
          k.style.flex = (c = b.flex) != null ? c : null;
        }
      }
    }
    if (!e.resizingColumns) {
      e.afterColumnsResized(b);
    }
    e.syncFlexedSubCols();
  }
  afterColumnsResized(a) {
    const b = this;
    b.eachSubGrid(c => {
      if (!c.collapsed && (!a || a.region === c.region)) {
        c.fixWidths();
        c.fixRowWidthsInSafariEdge();
      }
    });
    b.lastColumnResized = b.cellEls = null;
    b.bufferedAfterColumnsResized(a);
    b.onHeightChange();
  }
  syncFlexedSubCols() {
    const a = this.columns.query(b => b.flex && b.childLevel && b.element);
    if (a) {
      for (const b of a) {
        const c = b.element.getBoundingClientRect().width, e = DomHelper.children(this.element, '.b-grid-cell[data-column-id="' + b.id + '"]');
        for (const f of e) {
          f.style.flex = '0 0 ' + c + 'px';
        }
      }
    }
  }
  bufferedAfterColumnsResized(a) {
    if (this.columns.usesAutoHeight) {
      this.refreshRows();
    }
    this.refreshVirtualScrollbars();
    this.eachSubGrid(b => {
      if (!b.collapsed && (!a || a.region === b.region)) {
        b.refreshFakeScroll();
      }
    });
  }
  bufferedElementResize() {
    this.refreshRows();
  }
  onInternalResize(a, b, c, e, f) {
    if (DomHelper.scrollBarWidth && b < e) {
      this.eachSubGrid(g => {
        if (g.flex) {
          g.onElementResize(g.element);
        }
      });
    }
    super.onInternalResize(...arguments);
    if (this.isPainted && b !== e && this.columns.usesFlexAutoHeight) {
      this.bufferedElementResize();
    }
  }
  changeRowManager(a, b) {
    const c = this;
    if (!c._isRowMeasured) {
      c.measureRowHeight();
    }
    b == null ? void 0 : b.destroy();
    if (a) {
      const e = RowManager.new({
        'grid': c,
        'rowHeight': c.rowHeight,
        'rowScrollMode': c.rowScrollMode || 'move',
        'autoHeight': c.autoHeight,
        'fixedRowHeight': c.fixedRowHeight,
        'internalListeners': {
          'changeTotalHeight': 'onRowManagerChangeTotalHeight',
          'requestScrollChange': 'onRowManagerRequestScrollChange',
          'thisObj': c
        }
      }, a);
      c.relayEvents(e, [
        'beforeRenderRow',
        'renderRow',
        'removeRows'
      ]);
      c._rowManager = null;
      return e;
    }
  }
  updateRowHeight(a) {
    if (!this.isConfiguring && this.rowManager) {
      this.rowManager.rowHeight = a;
    }
  }
  updateFixedRowHeight(a) {
    let b, c;
    if (a && ((c = (b = this.columns || this.peekConfig('columns')) == null ? void 0 : b.some) == null ? void 0 : c.call(b, e => e.autoHeight))) {
      console.warn('One or more columns have `autoHeight` set, which is incompatible with `fixedRowHeight`.');
    }
  }
  getRowHeight(a) {
    return a.rowHeight;
  }
  processRowHeight(a, b) {
  }
  getAsyncEventSuffixForStore(a) {
    return this.asyncEventSuffix;
  }
  bindStore(a) {
    const b = this.getAsyncEventSuffixForStore(a), {masterStore: c} = a;
    a.ion({
      'name': storeListenerName2,
      ['refresh' + b]: 'onStoreDataChange',
      ['add' + b]: 'onStoreAdd',
      ['remove' + b]: 'onStoreRemove',
      ['replace' + b]: 'onStoreReplace',
      ['removeAll' + b]: 'onStoreRemoveAll',
      ['move' + b]: a.tree ? null : 'onFlatStoreMove',
      'change': 'relayStoreDataChange',
      'idChange': 'onStoreRecordIdChange',
      'update': 'onStoreUpdateRecord',
      'beforeRequest': 'onStoreBeforeRequest',
      'afterRequest': 'onStoreAfterRequest',
      'exception': 'onStoreException',
      'commit': 'onStoreCommit',
      'clearChanges': 'onStoreRecordClearChanges',
      'startApplyChangeset': 'internalOnStoreStartApplyChangeset',
      'endApplyChangeset': 'internalOnStoreEndApplyChangeset',
      'lazyLoadStarted': 'onStoreBeforeRequest',
      'lazyLoadEnded': 'onStoreAfterRequest',
      'thisObj': this
    });
    c == null ? void 0 : c.ion({
      'name': storeListenerName2,
      'beforeRequest': 'onStoreBeforeRequest',
      'afterRequest': 'onStoreAfterRequest',
      'lazyLoadEnded': 'onStoreAfterRequest',
      'thisObj': this
    });
    super.bindStore(a);
  }
  unbindStore(a) {
    this.detachListeners(storeListenerName2);
    if (this.destroyStore) {
      a.destroy();
    }
  }
  get $store() {
    return this.originalStore || this.store;
  }
  changeStore(a) {
    let b;
    if (a == null) {
      return null;
    }
    if (typeof a === 'string') {
      a = Store.getStore(a);
    }
    if (!a.isStore) {
      a = ObjectHelper.assign({
        'data': this.data,
        'tree': Boolean((b = this.initialConfig.features) == null ? void 0 : b.tree)
      }, a);
      if (!a.data) {
        delete a.data;
      }
      if (!a.modelClass) {
        a.modelClass = GridRowModel;
      }
      a = new (a.readUrl ? AjaxStore : Store)(a);
    }
    return a;
  }
  updateStore(a, b) {
    let c, e;
    const f = this;
    (c = super.updateStore) == null ? void 0 : c.call(this, a, b);
    if (b) {
      f.unbindStore(b);
    }
    if (a) {
      if (b) {
        f.deselectAll();
      }
      f.onStoreLoadOverride();
      f.bindStore(a);
    }
    f.trigger('bindStore', {
      'store': a,
      'oldStore': b
    });
    if (!f.isDestroying && f.isPainted && !f.refreshSuspended) {
      (e = f._rowManager) == null ? void 0 : e.reinitialize();
    }
  }
  onStoreUpdateRecord({
    source: a,
    record: b,
    changes: c
  }) {
    const e = this;
    if (e.refreshSuspended) {
      return;
    }
    if (e.forceFullRefresh) {
      e.rowManager.refresh();
      e.forceFullRefresh = false;
    } else {
      let f;
      if (b.isFieldModified('id')) {
        f = e.getRowFor(b.meta.modified.id);
      }
      f = f || e.getRowFor(b);
      if (!f) {
        return;
      }
      if (e.fullRowRefresh || b.isSpecialRow) {
        const g = a.indexOf(b);
        if (g !== -1) {
          f.render(g, b);
        }
      } else {
        e.columns.visibleColumns.forEach(i => {
          const j = i.field, k = i.constructor.simpleRenderer && !Object.prototype.hasOwnProperty.call(i.data, 'renderer');
          if (!k || c[j]) {
            const l = f.getCell(j);
            if (l) {
              f.renderCell(l);
            }
          }
        });
      }
    }
  }
  refreshFromRowOnStoreAdd(a, b) {
    const c = this, {rowManager: e} = c;
    e.renderFromRow(a);
    e.trigger('changeTotalHeight', { 'totalHeight': e.totalHeight });
    if (c.store.count === 1) {
      c.callEachSubGrid('refreshFakeScroll');
    }
  }
  onMaskAutoClose(a) {
    super.onMaskAutoClose(a);
    this.toggleEmptyText();
  }
  onStoreAdd({
    source: a,
    records: b,
    index: c,
    oldIndex: e,
    isChild: f,
    oldParent: g,
    parent: i,
    isMove: j,
    isExpand: k,
    isExpandAll: l
  }) {
    const n = this, {rowManager: o} = n;
    if (!n.isPainted || l || n.refreshSuspended) {
      return;
    }
    if (k && n.animateTreeNodeToggle) {
      return o.insert(o.getRowFor(i).index + 1, b.length);
    }
    const p = j && b.some(B => {
      if (j[B.id]) {
        const C = a.getById(B.meta.modified.parentId);
        return (C == null ? void 0 : C.isExpanded(a)) && (C == null ? void 0 : C.ancestorsExpanded(a));
      }
    });
    if (f && !b[0].ancestorsExpanded(a) && !p) {
      if (!i.isLeaf) {
        const B = o.getRowById(i);
        if (B) {
          o.renderRows([B]);
        }
      }
      return;
    }
    o.calculateRowCount(false, true, true);
    if (a.isFiltered) {
      c = a.indexOf(b[0]);
    }
    const {
        topIndex: q,
        rows: r,
        rowCount: u
      } = o, v = o.topIndex + o.rowCount - 1, w = c, z = c + b.length - 1, A = v >= a.count - b.length - 1;
    if (g || e > -1 || f && j && Object.values(j).some(C => C)) {
      o.refresh();
    } else if (w >= q && w < q + u) {
      n.refreshFromRowOnStoreAdd(r[w - q], ...arguments);
    } else if (z >= q && z < q + u) {
      o.refresh();
    } else {
      if (A && c > v) {
        o.fillBelow(n._scrollTop || 0);
      } else if (c < q) {
        o.refresh();
      }
      o.estimateTotalHeight(true);
    }
  }
  onStoreException({
    action: a,
    type: b,
    response: c,
    exceptionType: e,
    error: f
  }) {
    let g;
    const i = this;
    let j;
    switch (b) {
    case 'server':
      j = c.message || i.L('L{unspecifiedFailure}');
      break;
    case 'exception':
      j = e === 'network' ? i.L('L{networkFailure}') : (f == null ? void 0 : f.message) || ((g = c == null ? void 0 : c.parsedJson) == null ? void 0 : g.message) || i.L('L{parseFailure}');
      break;
    }
    i.applyMaskError('<div class="b-grid-load-failure">\n                <div class="b-grid-load-fail">' + i.L(a === 'read' ? 'L{loadFailedMessage}' : 'L{syncFailedMessage}') + '</div>\n                ' + ((c == null ? void 0 : c.url) ? '<div class="b-grid-load-fail">' + c.url + '</div>' : '') + '\n                <div class="b-grid-load-fail">' + i.L('L{serverResponse}') + '</div>\n                <div class="b-grid-load-fail">' + j + '</div>\n            </div>');
  }
  onStoreDataChange({
    action: a,
    changes: b,
    source: c,
    syncInfo: e
  }) {
    let f;
    (f = super.onStoreDataChange) == null ? void 0 : f.call(this, ...arguments);
    const g = this;
    if (g.refreshSuspended || !g.rowManager) {
      return;
    }
    if (a === 'dataset') {
      g.rowManager.clearKnownHeights();
      if (c.isTree && c.isFiltered) {
        return;
      }
    }
    const i = c.isGrouped && b && c.groupers.some(j => j.rootFieldName in b);
    if (g.isPainted && !i) {
      g.renderRows(Boolean(!(a in datasetReplaceActions) || g.preserveScrollOnDatasetChange || c.isChained && c.isFillingFromMaster));
    }
    g.toggleEmptyText();
  }
  onStoreRecordIdChange() {
    let a;
    (a = super.onStoreRecordIdChange) == null ? void 0 : a.call(this, ...arguments);
  }
  onStoreBeforeRequest() {
    if (!this.isLockedRows && (!this.store.count || !this.store.lazyLoad)) {
      this.applyLoadMask();
    }
  }
  onStoreAfterRequest(a) {
    if (this.loadMask && !a.exception) {
      this.masked = null;
      this.toggleEmptyText();
    }
  }
  needsFullRefreshOnStoreRemove({isCollapse: a}) {
    let b, c;
    const {store: e} = this;
    return ((c = (b = this.features) == null ? void 0 : b.group) == null ? void 0 : c.enabled) && e.isGrouped || e.tree && !a && e.modelClass.convertEmptyParentToLeaf;
  }
  onStoreRemove({
    source: a,
    records: b,
    isCollapse: c,
    isChild: e,
    isMove: f,
    isCollapseAll: g
  }) {
    let i;
    if (!this.isPainted || f || g) {
      return;
    }
    (i = super.onStoreRemove) == null ? void 0 : i.call(this, ...arguments);
    const j = this, {rowManager: k} = j;
    k.invalidateKnownHeight(b);
    if (j.refreshSuspended) {
      return;
    }
    if (j.animateRemovingRows && !c && !e) {
      const l = b.reduce((n, o) => {
        const p = k.getRowById(o.id);
        p && n.push(p);
        return n;
      }, []);
      if (l.length) {
        const n = l[0];
        j.isAnimating = true;
        EventHelper.onTransitionEnd({
          'element': n._elementsArray[0],
          'property': 'left',
          'duration': j.transitionDuration + 50,
          'thisObj': j,
          'handler': () => {
            j.isAnimating = false;
            l.forEach(o => !o.isDestroyed && o.removeCls('b-removing'));
            k.refresh();
            j.trigger('rowRemove');
            j.afterRemove(arguments[0]);
          }
        });
        l.forEach(o => o.addCls('b-removing'));
      }
    } else if (c && j.animateTreeNodeToggle) {
      const o = b.flatMap(q => {
          const r = k.getRowFor(q);
          return r ? r.index : emptyArray12;
        }).sort(ascending), {length: p} = o;
      if (p && o[p - 1] === o[0] + p - 1) {
        return k.insert(o[0], -o.length);
      }
    } else if (j.needsFullRefreshOnStoreRemove(...arguments)) {
      k.refresh();
      j.afterRemove(arguments[0]);
    } else {
      const {rows: q} = k, r = b.reduce((u, v) => {
          const w = k.getRowById(v.id);
          if (w) {
            u = Math.min(u, q.indexOf(w));
          }
          return u;
        }, q.length);
      if (q[r]) {
        !j.refreshSuspended && k.renderFromRow(q[r]);
      } else {
        k.trigger('changeTotalHeight', { 'totalHeight': k.totalHeight });
      }
      j.trigger('rowRemove', { 'isCollapse': c });
      j.afterRemove(arguments[0]);
    }
  }
  onFlatStoreMove({
    from: a,
    to: b
  }) {
    const {
        rowManager: c,
        store: e
      } = this, {
        topIndex: f,
        rowCount: g
      } = c, [i, j] = [
        a,
        b
      ].sort((n, o) => n - o), k = e.indexOf(e.getAt(i, true)), l = e.indexOf(e.getAt(j, true));
    if (k >= f && k < f + g) {
      c.renderFromRow(c.rows[k - f]);
    } else if (l >= f && l < f + g) {
      c.refresh();
    }
  }
  onStoreReplace({
    records: a,
    all: b
  }) {
    const {rowManager: c} = this;
    if (b) {
      c.clearKnownHeights();
      c.refresh();
    } else {
      const e = a.reduce((f, [, g]) => {
        const i = this.getRowFor(g);
        if (i) {
          f.push(i);
        }
        return f;
      }, []);
      c.invalidateKnownHeight(a);
      c.renderRows(e);
    }
  }
  relayStoreDataChange(a) {
    let b;
    (b = this.ariaElement) == null ? void 0 : b.setAttribute('aria-rowcount', this.store.count + 1);
    if (!this.project) {
      return this.trigger('dataChange', {
        ...a,
        'store': a.source,
        'source': this
      });
    }
  }
  onStoreRemoveAll() {
    let a;
    (a = super.onStoreRemoveAll) == null ? void 0 : a.call(this, ...arguments);
    if (this.isPainted) {
      this.rowManager.clearKnownHeights();
      this.renderRows(false);
      this.toggleEmptyText();
    }
  }
  onStoreCommit({changes: a}) {
    if (this.showDirty && a.modified.length) {
      const b = [];
      a.modified.forEach(c => {
        const e = this.rowManager.getRowFor(c);
        e && b.push(e);
      });
      this.rowManager.renderRows(b);
    }
  }
  onStoreRecordClearChanges({record: a}) {
    const b = this.rowManager.getRowFor(a);
    if (b) {
      this.rowManager.renderRows([b]);
    }
  }
  internalOnStoreStartApplyChangeset() {
    this.suspendRefresh();
    if (this.constructor.bindStoreChangeset) {
      this.captureScrollTargetRow();
    }
  }
  internalOnStoreEndApplyChangeset() {
    this.resumeRefresh(true);
    if (this.constructor.bindStoreChangeset) {
      this.restoreScrollTargetRow();
    }
  }
  captureScrollTargetRow() {
    const a = this;
    if (a.preserveScroll) {
      const {
        firstFullyVisibleRow: b,
        lastVisibleRow: c
      } = a.rowManager;
      if (b) {
        a.lastVisibleRowIds = [b.id];
        for (let e = b.dataIndex + 1; e <= c.dataIndex; e++) {
          a.lastVisibleRowIds.push(a.rowManager.getRow(e).id);
        }
        a.lastTopRowOffset = a.scrollable.getDeltaTo(b.element, {
          'block': 'start',
          'x': false,
          'constrainToScrollable': false
        });
      }
    }
  }
  restoreScrollTargetRow() {
    const a = this;
    let b = false;
    if (a.preserveScroll) {
      if (a.lastVisibleRowIds) {
        a.rowManager.refresh();
        if (a.scrollable.scrollHeight >= a.lastOverscrollHeight && a.scrollable.y === a.lastOverscrollY) {
          a.rowManager.fillBelow(a.scrollable.y);
        } else {
          a.lastOverscrollHeight = null;
          a.lastOverscrollY = null;
        }
        const c = a.lastVisibleRowIds.find(e => a.store.getById(e));
        if (c != void 0 && c !== a.rowManager.firstFullyVisibleRow.id) {
          if (a.preserveScroll.overscroll) {
            const e = a.scrollable.getDeltaTo(a.getRecordCoords(c), {
              'block': 'start',
              'x': false,
              'constrainToScrollable': false,
              'edgeOffset': a.lastTopRowOffset.yDelta
            }).yDelta;
            if (e > a.scrollable.maxY - a.scrollable.y) {
              a.scrollable.scrollHeight += e;
              b = true;
            }
          }
          a.scrollRowIntoView(c, {
            'block': 'start',
            'edgeOffset': a.lastTopRowOffset.yDelta,
            'x': false
          });
        }
      }
      a.lastVisibleRowIds = void 0;
      a.lastTopRowOffset = void 0;
      if (b) {
        a.lastOverscrollHeight = a.scrollable.scrollHeight;
        a.lastOverscrollY = a.scrollable.y;
      }
    }
  }
  get data() {
    if (this._store) {
      return this._store.records;
    } else {
      return this._data;
    }
  }
  set data(a) {
    if (this._store) {
      this._store.data = a;
    } else {
      this._data = a;
    }
  }
  populateHeaderMenu({
    column: a,
    items: b
  }) {
    const c = this, {
        subGrids: e,
        regions: f
      } = c, {parent: g} = a;
    let i = true;
    Object.entries(e).forEach(([j, k]) => {
      if (k.sealedColumns) {
        return;
      }
      if (a.draggable && j !== a.region && (!g && e[a.region].columns.count > 1 || g && g.children.length > 1)) {
        const l = k.element.compareDocumentPosition(e[a.region].element) === document.DOCUMENT_POSITION_PRECEDING, n = c.rtl ? !l : l, o = f.length > 2 ? c.L('L{moveColumnTo}', c.optionalL(j)) : c.L(n ? 'L{moveColumnRight}' : 'L{moveColumnLeft}');
        b[j + 'Region'] = {
          'targetSubGrid': j,
          'text': o,
          'icon': 'b-fw-icon b-icon-column-move-' + (n ? 'right' : 'left'),
          'separator': i,
          'disabled': !a.allowDrag,
          'onItem': ({item: p}) => {
            const q = c.columns.indexOf(e[p.targetSubGrid].columns.last);
            a.traverse(r => r.region = j);
            c.columns.insert(q + 1, a);
            c.scrollColumnIntoView(a);
          }
        };
        i = false;
      }
    });
  }
  populateCellMenu({
    record: a,
    items: b
  }) {
  }
  getColumnDragToolbarItems(a, b) {
    return b;
  }
  normalizeCellContext(a) {
    const b = this, {columns: c} = b;
    if (a.isLocation) {
      return a;
    }
    if (a.isModel) {
      return new GridLocation({
        'grid': b,
        'id': a.id,
        'columnId': c.visibleColumns[0].id
      });
    }
    return new GridLocation(ObjectHelper.assign({ 'grid': b }, a));
  }
  getCell(a) {
    const {
        store: b,
        columns: c
      } = this, {visibleColumns: e} = this.columns, f = !isNaN(a.row) ? a.row : !isNaN(a.rowIndex) ? a.rowIndex : b.indexOf(a.record || a.id), g = !isNaN(a.column) ? a.column : !isNaN(a.columnIndex) ? a.columnIndex : e.indexOf(a.column || c.getById(a.columnId) || c.get(a.field) || e[0]);
    return f > -1 && f < b.count && g > -1 && g < e.length && this.normalizeCellContext(a).cell || null;
  }
  getHeaderElement(a) {
    if (a.isModel) {
      a = a.id;
    }
    return this.fromCache('.b-grid-header[data-column-id="' + a + '"]');
  }
  getHeaderElementByField(a) {
    const b = this.columns.get(a);
    return b ? this.getHeaderElement(b) : null;
  }
  get bodyHeight() {
    return this._bodyHeight;
  }
  get headerHeight() {
    const a = this;
    if (a.isPainted && !a._headerHeight) {
      a._headerHeight = a.headerContainer.offsetHeight;
    }
    return a._headerHeight;
  }
  get footerHeight() {
    const a = this;
    if (a.isPainted && !a._footerHeight) {
      a._footerHeight = a.footerContainer.offsetHeight;
    }
    return a._footerHeight;
  }
  get isTreeGrouped() {
    let a;
    return Boolean((a = this.features.treeGroup) == null ? void 0 : a.isGrouped);
  }
  getRecordFromElement(a) {
    const b = a.closest('.b-grid-row');
    if (!b) {
      return null;
    }
    return this.store.storage.getAt(b.dataset.index);
  }
  getColumnFromElement(a) {
    const b = a.closest('.b-grid-cell, .b-grid-header');
    if (!b)
      return null;
    if (b.matches('.b-grid-header')) {
      return this.columns.getById(b.dataset.columnId);
    }
    const c = DomDataStore.get(b);
    return this.columns.getById(c.columnId);
  }
  updateAutoHeight(a) {
    ObjectHelper.assertBoolean(a, 'autoHeight');
  }
  updateColumnLines(a) {
    ObjectHelper.assertBoolean(a, 'columnLines');
    DomHelper.toggleClasses(this.element, 'b-no-column-lines', !a);
  }
  updateRowLines(a) {
    DomHelper.toggleClasses(this.element, 'b-no-row-lines', !a);
  }
  get keyMapElement() {
    return this.bodyElement;
  }
  fixSizes() {
    this.callEachSubGrid('fixWidths');
    const a = this.headerContainer.querySelectorAll('.b-grid-header.b-depth-0');
    for (let b = 0, {length: c} = a; b < c; b++) {
      a[b].setAttribute('aria-colindex', b + 1);
    }
  }
  onRowManagerChangeTotalHeight({
    totalHeight: a,
    immediate: b
  }) {
    return this.refreshTotalHeight(a, b);
  }
  refreshTotalHeight(a = this.rowManager.totalHeight, b = false) {
    const c = this;
    if (c.renderingRows || !c.isVisible) {
      return false;
    }
    const e = c.scrollable, f = Math.abs(c.virtualScrollHeight - a), g = c._bodyRectangle.height, i = a - g;
    if (f) {
      const j = i - c._scrollTop < g * 2 || c._verticalScrollHeight && c._verticalScrollHeight - g < c._scrollTop;
      e.scrollHeight = c.virtualScrollHeight = a;
      if (c.isPainted && (c.scrolling && !j || f < 100) && !b) {
        c.bufferedFixElementHeights();
      } else {
        c.virtualScrollHeightDirty && c.virtualScrollHeightDirty();
        c.bufferedFixElementHeights.cancel();
        c.fixElementHeights();
      }
    }
  }
  fixElementHeights() {
    const a = this, b = a.virtualScrollHeight, c = b + 'px';
    a._verticalScrollHeight = b;
    a.verticalScroller.style.height = c;
    a.virtualScrollHeightDirty = false;
    if (a.autoHeight) {
      a._height = null;
      a.bodyContainer.style.height = c;
      a._bodyHeight = b;
      a.refreshBodyRectangle();
    }
    a.refreshVirtualScrollbars();
  }
  refreshBodyRectangle() {
    return this._bodyRectangle = Rectangle.client(this.bodyContainer);
  }
  set scrolling(a) {
    this._scrolling = a;
  }
  get scrolling() {
    return this._scrolling;
  }
  enableScrollingCloseToEdges(a) {
    this.scrollManager.startMonitoring({
      'scrollables': [
        {
          'element': this.scrollable.element,
          'direction': 'vertical'
        },
        ...ArrayHelper.asArray(a || []).map(b => ({ 'element': (typeof b === 'string' ? this.subGrids[b] : b).scrollable.element }))
      ],
      'direction': 'horizontal'
    });
  }
  disableScrollingCloseToEdges(a) {
    this.scrollManager.stopMonitoring([
      this.scrollable.element,
      ...ArrayHelper.asArray(a || []).map(b => (typeof b === 'string' ? this.subGrids[b] : b).element)
    ]);
  }
  onRowManagerRequestScrollChange({bottom: a}) {
    this.scrollable.y = a - this.bodyHeight;
  }
  initScroll() {
    const a = this, {scrollable: b} = a;
    if (!a.scrollInitialized) {
      a.scrollInitialized = true;
      b.contentElement = a.contentElement;
      b.ion({
        'scroll': 'onGridVerticalScroll',
        'scrollend': 'onGridVerticalScrollEnd',
        'thisObj': a
      });
      a.callEachSubGrid('initScroll');
      if (BrowserHelper.isMobileSafari) {
        b.element.style.transform = 'translate3d(0, 0, 0)';
      }
    }
  }
  onGridVerticalScroll({source: a}) {
    const b = this, {y: c} = a;
    if (c !== b._scrollTop) {
      b._scrollTop = c;
      if (!b.scrolling) {
        b.scrolling = true;
        b.eachSubGrid(e => e.suspendResizeMonitor = true);
      }
      b.rowManager.updateRenderedRows(c);
      b.afterScroll({ 'scrollTop': c });
      b.trigger('scroll', { 'scrollTop': c });
    }
  }
  onGridVerticalScrollEnd() {
    this.scrolling = false;
    this.eachSubGrid(a => a.suspendResizeMonitor = false);
  }
  async scrollRowIntoView(a, b = defaultScrollOptions3) {
    let c;
    const e = this, f = b.block || 'nearest', {rowManager: g} = e, i = e.store.getById(a);
    if (i) {
      let j;
      if (e.store.indexOf(i) === -1) {
        return resolvedPromise;
      }
      if (!e.rowManager.topRow) {
        await e.await('renderRows');
      }
      let k = e.scrollable, l = e.getRecordCoords(i);
      const n = Rectangle.from(k.element);
      if (l.virtual) {
        const o = l.block, p = f !== 'nearest' ? b : { 'block': o };
        j = k.scrollIntoView(l, { 'block': 'center' });
        g.scrollTargetRecordId = i;
        g.updateRenderedRows(k.y, true);
        l = e.getRecordCoords(i);
        g.lastScrollTop = k.y;
        if (l.virtual) {
          return resolvedPromise;
        }
        if (b.animate) {
          k.suspendEvents();
          if (f === 'end' || f === 'nearest' && o === 'end') {
            k.y -= n.bottom - l.bottom;
          } else if (f === 'start' || f === 'nearest' && o === 'start') {
            k.y += l.y - n.y;
          }
          g.updateRenderedRows(k.y, false, true);
          if (o === 'end') {
            k.y -= g.appendRowBuffer * g.rowHeight - 1;
          } else {
            k.y += g.prependRowBuffer * g.rowHeight - 1;
          }
          k.resumeEvents();
          await k.scrollIntoView(e.getRecordCoords(i), Object.assign({}, b, p));
        } else {
          if (!b.recursive) {
            await j;
          }
          await ((c = e.scrollRowIntoView) == null ? void 0 : c.call(e, i, Object.assign({ 'recursive': true }, b, p)));
        }
      } else {
        let {column: q} = b;
        if (q) {
          if (!q.isModel) {
            q = e.columns.getById(q) || e.columns.get(q);
          }
          if (q) {
            k = e.getSubGridFromColumn(q).scrollable;
            const r = Rectangle.from(g.getRowFor(i).getCell(q.id));
            l.x = r.x;
            l.width = r.width;
          }
        } else {
          b = ObjectHelper.assign({}, b, { 'x': false });
        }
        await k.scrollIntoView(l, b);
      }
    }
  }
  scrollColumnIntoView(a, b) {
    a = a instanceof Column ? a : this.columns.get(a) || this.columns.getById(a) || this.columns.getAt(a);
    return this.getSubGridFromColumn(a).scrollColumnIntoView(a, b);
  }
  scrollCellIntoView(a, b) {
    const c = this.normalizeCellContext(a);
    return this.scrollRowIntoView(c.id, {
      'column': c.columnId,
      ...typeof b === 'boolean' ? { 'animate': b } : b
    });
  }
  scrollToBottom(a) {
    return this.scrollRowIntoView(this.store.last, a);
  }
  scrollToTop(a) {
    return this.scrollable.scrollBy(0, -this.scrollable.y, a);
  }
  storeScroll() {
    const a = this.storedScrollState = {
      'scrollTop': this.scrollable.y,
      'scrollLeft': {}
    };
    this.eachSubGrid(b => {
      a.scrollLeft[b.region] = b.scrollable.x;
    });
    return a;
  }
  restoreScroll(a = this.storedScrollState) {
    const b = this;
    b.eachSubGrid(c => {
      let e;
      const f = a.scrollLeft[c.region];
      if (f != null) {
        c.scrollable.updateX(f);
        c.header.scrollable.updateX(f);
        c.footer.scrollable.updateX(f);
        (e = c.fakeScroller) == null ? void 0 : e.updateX(f);
      }
    });
    b.scrollable.updateY(a.scrollTop);
  }
  beginGridMeasuring() {
    const a = this;
    if (!a.$measureCellElements) {
      a.$measureCellElements = DomHelper.createElement({
        'className': 'b-grid-subgrid ' + (!a._isRowMeasured && a.hasFeature('stripe') ? 'b-stripe' : ''),
        'reference': 'subGridElement',
        'style': {
          'position': 'absolute',
          'top': '-10000px',
          'left': '-100000px',
          'visibility': 'hidden',
          'contain': 'strict'
        },
        'children': [{
            'className': 'b-grid-row',
            'reference': 'rowElement',
            'children': [{
                'className': 'b-grid-cell',
                'reference': 'cellElement',
                'style': {
                  'width': 'auto',
                  'contain': BrowserHelper.isFirefox ? 'layout paint' : 'layout style paint'
                }
              }]
          }]
      });
    }
    a.getConfig('element');
    a.verticalScroller.appendChild(a.$measureCellElements.subGridElement);
    if (!a.rendered) {
      const b = a.appendTo || a.insertBefore || document.body, c = DomHelper.getRootElement(typeof b === 'string' ? document.getElementById(b) : b) || document.body;
      if (!a.adopt || !c.contains(a.element)) {
        c.appendChild(a.element);
        a.$removeAfterMeasuring = true;
      }
    }
    return a.$measureCellElements;
  }
  endGridMeasuring() {
    if (this.$removeAfterMeasuring) {
      this.element.remove();
      this.$removeAfterMeasuring = false;
    }
    this.$measureCellElements.subGridElement.remove();
  }
  measureRowHeight() {
    const a = this, {rowElement: b} = a.beginGridMeasuring(), c = parseFloat(DomHelper.getStyleValue(document.body, 'zoom')), e = DomHelper.getStyleValue(b, [
        'height',
        'border-top-width',
        'border-bottom-width'
      ]), f = parseInt(e.height), g = c * (BrowserHelper.isFirefox ? globalThis.devicePixelRatio / Math.max(Math.trunc(globalThis.devicePixelRatio), 1) : 1), i = e['border-top-width'] ? Math.round(g * parseFloat(e['border-top-width'])) : 0, j = e['border-bottom-width'] ? Math.round(g * parseFloat(e['border-bottom-width'])) : 0;
    if (a.rowHeight == null || a.rowHeight === a._rowHeightFromStyle) {
      a.rowHeight = !isNaN(f) && f ? f : a.defaultRowHeight;
      a._rowHeightFromStyle = a.rowHeight;
    }
    a._rowBorderHeight = i + j;
    a._isRowMeasured = true;
    a.endGridMeasuring();
  }
  onThemeChange({theme: a}) {
    this.whenVisible('measureRowHeight');
    this.trigger('theme', { 'theme': a });
  }
  refreshRow(a) {
    this.refreshRows([a]);
  }
  refreshRows(a = null, b = false) {
    if (this.refreshSuspended) {
      return;
    }
    const {
      element: c,
      rowManager: e
    } = this;
    if (!a) {
      c.classList.add('b-notransition');
      if (b) {
        e.returnToTop();
      } else {
        e.refresh(true);
      }
      c.classList.remove('b-notransition');
    } else {
      const f = a.flatMap(g => {
        let i;
        return (i = e.getRowById(g)) != null ? i : [];
      });
      e.renderRows(f);
    }
  }
  refreshColumn(a) {
    if (a.isVisible) {
      if (a.isLeaf) {
        this.rowManager.forEach(b => b.renderCell(b.getCell(a.id)));
      } else {
        a.children.forEach(b => this.refreshColumn(b));
      }
    }
  }
  refreshVirtualScrollbars() {
    const a = this, {
        headerContainer: b,
        footerContainer: c,
        virtualScrollers: e,
        scrollable: f,
        hasVerticalOverflow: g
      } = a, {classList: i} = e, j = !i.contains('b-hide-display'), k = Object.values(a.subGrids).some(n => n.overflowingHorizontally), l = k !== j;
    if (l) {
      e.classList.toggle('b-hide-display', !k);
    }
    if (DomHelper.scrollBarWidth) {
      const n = g || f.overflowY === 'scroll';
      b.classList.toggle('b-show-yscroll-padding', n);
      c.classList.toggle('b-show-yscroll-padding', n);
      e.classList.toggle('b-show-yscroll-padding', n);
      if (l || !a.$virtualScrollbarsRefreshed) {
        a.callEachSubGrid('refreshFakeScroll');
        a.onHeightChange();
      }
    }
  }
  get hasVerticalOverflow() {
    return this.scrollable.hasOverflow('y');
  }
  get contentHeight() {
    const a = this.rowManager;
    return Math.max(a.totalHeight, a.bottomRow ? a.bottomRow.bottom : 0);
  }
  onContentChange() {
    const a = this, b = a.rowManager;
    if (a.isVisible) {
      b.estimateTotalHeight(a.autoHeight);
      a.paintListener = null;
      a.refreshTotalHeight(a.contentHeight);
      a.callEachSubGrid('refreshFakeScroll');
      a.onHeightChange();
    } else if (!a.paintListener) {
      a.paintListener = a.ion({
        'paint': 'onContentChange',
        'once': true,
        'thisObj': a
      });
    }
  }
  triggerPaint() {
    if (!this.isPainted) {
      this.refreshBodyRectangle();
    }
    super.triggerPaint();
  }
  onHeightChange() {
    const a = this;
    a.refreshBodyRectangle();
    a._bodyHeight = a.autoHeight ? a.contentHeight : a.bodyContainer.offsetHeight;
  }
  suspendRefresh() {
    this.refreshSuspended++;
  }
  resumeRefresh(a = true) {
    if (this.refreshSuspended && !--this.refreshSuspended) {
      if (a) {
        this.refreshRows();
      }
      this.trigger('resumeRefresh', { 'trigger': a });
    }
  }
  renderRows(a = true) {
    const b = this, c = a && b.storeScroll();
    if (b.refreshSuspended) {
      return;
    }
    b.trigger('beforeRenderRows');
    b.renderingRows = true;
    b.element.classList.add('b-grid-refreshing');
    if (!a) {
      b.scrollable.y = b._scrollTop = 0;
    }
    b.rowManager.reinitialize(!a);
    b.trigger('renderRows');
    b.renderingRows = false;
    b.onContentChange();
    if (a) {
      b.restoreScroll(c);
    }
    if (b.autoHeight && b.store.count > 200 && !b.autoHeightWarning && !b.disableAutoHeightWarning) {
      console.warn('Warning: \'autoHeight\' is enabled for a large number of rows (200+). While \'autoHeight\' provides flexibility, it should be used cautiously as it disables virtualization, leading to significant performance degradation with large datasets. Consider alternatives to maintain optimal performance.');
      b.autoHeightWarning = true;
    }
    b.element.classList.remove('b-grid-refreshing');
  }
  renderContents() {
    const a = this, {
        element: b,
        headerContainer: c,
        footerContainer: e,
        rowManager: f
      } = a;
    a.emptyCache();
    if (a.isPainted) {
      a._headerHeight = null;
      a.callEachSubGrid('refreshHeader');
      a.callEachSubGrid('refreshFooter');
      a.renderHeader(c, b);
      a.renderFooter(e, b);
      a.fixSizes();
      const g = f.removeAllRows();
      f.calculateRowCount(false, true, true);
      f.setPosition(g);
      a.renderRows();
    }
  }
  refreshHeaders() {
    this.callEachSubGrid('refreshHeader');
  }
  refreshHeader(a) {
    a.subGrid.refreshHeader();
  }
  onPaintOverride() {
  }
  onStoreLoadOverride() {
  }
  onInternalPaint({firstPaint: a}) {
    let b;
    const c = this;
    c.ariaElement.setAttribute('aria-rowcount', c.store.count + 1);
    (b = super.onInternalPaint) == null ? void 0 : b.call(this, ...arguments);
    if (c.onPaintOverride() || !a) {
      return;
    }
    const {
        rowManager: e,
        store: f,
        element: g,
        headerContainer: i,
        bodyContainer: j,
        footerContainer: k
      } = c, l = DomHelper.scrollBarPadElement;
    let n, o = 0;
    c.role = (f == null ? void 0 : f.isTree) ? 'treegrid' : 'grid';
    c.columns.ion({
      'change': () => n = true,
      'once': true
    });
    c.updateResponsive(c.width, 0);
    if (n) {
      c.callEachSubGrid('refreshHeader', i);
      c.callEachSubGrid('refreshFooter', k);
    }
    c.renderHeader(i, g);
    c.renderFooter(k, g);
    DomHelper.append(i, l);
    DomHelper.append(k, l);
    DomHelper.append(c.virtualScrollers, l);
    c.refreshBodyRectangle();
    const p = c.bodyContainer.offsetHeight;
    if (c.autoHeight) {
      c._bodyHeight = e.initWithHeight(g.offsetHeight - i.offsetHeight - k.offsetHeight, true);
      j.style.height = c.bodyHeight + 'px';
    } else {
      c._bodyHeight = p;
      e.initWithHeight(c._bodyHeight, true);
    }
    c.eachSubGrid(q => {
      if (q.header.maxDepth > o) {
        o = q.header.maxDepth;
      }
    });
    i.dataset.maxDepth = o;
    c.fixSizes();
    if (f.count || !f.isLoading) {
      c.renderRows();
    }
    if (c.columns.usesAutoHeight) {
      const {fonts: q} = document;
      if ((q == null ? void 0 : q.status) !== 'loaded') {
        q.ready.then(() => !c.isDestroyed && c.refreshRows());
      }
    }
    c.initScroll();
    c.initInternalEvents();
  }
  render() {
    let a;
    const b = this;
    b.requireSize = Boolean(b.owner);
    super.render(...arguments);
    b.setupFocusListeners();
    if (!b.autoHeight) {
      if (b.headerContainer.offsetHeight && !b.bodyContainer.offsetHeight) {
        console.warn('Grid element not sized correctly, please check your CSS styles and review how you size the widget');
      }
      if (!b.splitFrom && !((a = b.features.split) == null ? void 0 : a.owner) && !('minHeight' in b.initialConfig) && !('height' in b.initialConfig) && parseInt(globalThis.getComputedStyle(b.element).minHeight) === b.height) {
        console.warn('The ' + b.$$name + ' is sized by its predefined minHeight, likely this is not intended. Please check your CSS and review how you size the widget, or assign a fixed height in the config. For more information, see the "Basics/Sizing the component" guide in docs.');
      }
    }
  }
  renderHeader(a, b) {
  }
  renderFooter(a, b) {
  }
  beforeRenderCell() {
  }
  beforeRenderRow() {
  }
  afterRenderRow() {
  }
  afterScroll() {
  }
  processCellEditor(a) {
  }
  afterColumnsChange() {
    super.afterColumnsChange(...arguments);
  }
  afterRemove(a) {
  }
  afterToggleGroup() {
  }
  afterToggleSubGrid() {
  }
  onConfigChange(a) {
    super.onConfigChange(a);
    if (!this.isConfiguring) {
      this.afterConfigChange(a);
    }
  }
  afterConfigChange(a) {
  }
  afterAddListener(a, b) {
  }
  afterRemoveListener(a, b) {
  }
  syncMaskCover(a = this.masked) {
    if (a) {
      const b = a.cover === 'body' && this.rectangleOf('bodyContainer'), c = b && DomHelper.scrollBarWidth && this.rectangleOf('virtualScrollers'), {style: e} = a.element;
      e.marginTop = b ? b.y + 'px' : '';
      e.height = b ? b.height + ((c == null ? void 0 : c.height) || 0) + 'px' : '';
    }
  }
  changeMasked(a, b) {
    if (a) {
      const c = this;
      if (typeof a === 'string') {
        a = { 'text': a };
      }
      if (c.isLockedRows || c.splitFrom) {
        a.cover = null;
        a.target = c.element.parentElement;
      } else {
        a.cover = 'body';
        a.target = c.element;
      }
    }
    return super.changeMasked(a, b);
    ;
  }
  maskBody(a) {
    let b;
    if (this.bodyContainer) {
      this.masked = Mask.mergeConfigs(this.loadMaskDefaults, a);
      b = this.masked;
    }
    return b;
  }
  unmaskBody() {
    this.masked = null;
  }
  updateEmptyText(a) {
    let b, c;
    (b = this.emptyTextEl) == null ? void 0 : b.remove();
    const e = typeof a === 'object';
    if (e) {
      a = ArrayHelper.asArray(a);
    }
    this.emptyTextEl = DomHelper.createElement({
      'parent': (c = this.firstItem) == null ? void 0 : c.element,
      'className': 'b-empty-text',
      [e ? 'children' : (a == null ? void 0 : a.includes('<')) ? 'html' : 'text']: a
    });
  }
  toggleEmptyText() {
    const {store: a} = this, b = !(a.count > 0 || a.isLoading || a.isCommitting);
    this.element.classList.toggle('b-grid-empty', b);
  }
  updateReadOnly(a, b) {
    let c;
    super.updateReadOnly(a, b);
    if (!this.isConfiguring) {
      for (const e of this.columns.bottomColumns) {
        (c = e.updateReadOnly) == null ? void 0 : c.call(e, a);
      }
    }
  }
  getCurrentConfig(a) {
    const b = super.getCurrentConfig(a), {store: c} = this, e = c.getInlineData(a), f = c.getCurrentConfig(a) || b.store;
    if (e.length) {
      b.data = e;
    }
    if (f && c.originalModelClass === GridRowModel) {
      delete f.modelClass;
    }
    if (!ObjectHelper.isEmpty(f)) {
      b.store = f;
    }
    if (b.store) {
      delete b.store.data;
    }
    return b;
  }
  suspendAnimations() {
    this._animationSuspendedCounter++;
  }
  resumeAnimations() {
    this._animationSuspendedCounter--;
  }
  async runWithTransition(a, b) {
    const c = this;
    if (!b || !c.isVisible || c._animationSuspendedCounter !== 0 || c.enableEventAnimations === false) {
      return a();
    }
    c.isAnimating = true;
    await c.executeAndAwaitAnimations(c.element, a);
    c.isAnimating = false;
    if (!c.isDestroyed && !c.isAnimating) {
      c.trigger('transitionEnd');
    }
  }
};
GridBase.initClass();
VersionHelper.setVersion('grid', '6.0.4');
GridBase._$name = 'GridBase';
const Grid = class extends GridBase {
  static get $name() {
    return 'Grid';
  }
  static get type() {
    return 'grid';
  }
};
Grid.initClass();
Grid._$name = 'Grid';
const TreeGrid = class extends Grid {
  static get $name() {
    return 'TreeGrid';
  }
  static get type() {
    return 'treegrid';
  }
  static get configurable() {
    return { 'store': { 'tree': true } };
  }
  updateStore(a, b) {
    if (a && !(a.tree || a.isTree)) {
      throw new Error('TreeGrid requires a Store configured with tree : true');
    }
    super.updateStore(a, b);
  }
};
TreeGrid.initClass();
TreeGrid._$name = 'TreeGrid';
const GroupBar = class extends ChipView {
  static configurable = {
    'selectedCls': 'not-used',
    'itemTpl'(a) {
      return StringHelper.encodeHtml(StringHelper.capitalize(a.getValue(this.displayField)));
    }
  };
  static $name = 'GroupBar';
  static type = 'groupbar';
  construct() {
    super.construct(...arguments);
    const a = this.treeGrid = this.up('gridbase', true);
    if (!a) {
      throw new Error('GroupBar must be used inside a Grid component');
    }
    a.ion({
      'paint': this.onTreePaint,
      'once': true,
      'thisObj': this
    });
  }
  onTreePaint() {
    const a = this, {treeGrid: b} = a, {
        treeGroup: c,
        columnReorder: e
      } = b.features;
    if (!c) {
      throw new Error('GroupBar widget requires the TreeGroup feature to be present');
    }
    e.usingGroupBarWidget = true;
    a.store = new Store({
      'fields': [
        'cls',
        'ascending'
      ],
      'internalListeners': {
        'add': a.onStoreChanged,
        'remove': a.onStoreChanged,
        'thisObj': a
      }
    });
    b.ion({
      'treeGroupChange': a.onTreeGroupChanged,
      'beforeColumnDropFinalize': a.onBeforeColumnDropFinalize,
      'columnDrag': a.onColumnDrag,
      'columnDragStart': a.onColumnDragStart,
      'columnDrop': a.onColumnDrop,
      'thisObj': a
    });
    a.onTreeGroupChanged({ 'levels': c.levels });
  }
  onStoreChanged({records: a}) {
    let b;
    const c = this, {treeGrid: e} = c;
    if (!c.treeGrid.isConstructing && ((b = a == null ? void 0 : a[0]) == null ? void 0 : b.cls) !== 'b-drop-target') {
      c.ignoreGroupChange = true;
      e.features.treeGroup.levels = c.store.map(({field: f}) => f);
      c.ignoreGroupChange = false;
    }
  }
  onTreeGroupChanged({levels: a}) {
    if (!this.ignoreGroupChange) {
      if (a.some(b => b instanceof Function && !b.fieldName)) {
        throw new Error('GroupBar only supports column grouping');
      }
      this.store.data = (a || []).map(b => {
        b = b.fieldName || b;
        return this.treeGrid.columns.get(b);
      });
    }
  }
  onColumnDragStart() {
    this.store.add({
      'id': 'placeholder',
      'cls': 'b-drop-target'
    });
  }
  onColumnDrag({
    context: a,
    column: b,
    event: c
  }) {
    const e = c.target.closest('.b-groupbar');
    if (e) {
      a.valid = true;
    }
  }
  onBeforeColumnDropFinalize({
    column: a,
    event: b
  }) {
    const c = b.target.closest('.b-groupbar');
    if (c) {
      if (!a.isTreeColumn) {
        this.store.getById('placeholder').remove();
        this.store.add(a);
      }
    }
  }
  onColumnDrop() {
    let a;
    (a = this.store.getById('placeholder')) == null ? void 0 : a.remove();
  }
};
GroupBar.initClass();
GroupBar._$name = 'GroupBar';
const TreeCombo = class extends Combo {
  static configurable = {
    'multiSelect': true,
    'picker': {
      'type': 'treegrid',
      'minWidth': '35em',
      'disableGridRowModelWarning': true,
      'selectionMode': {
        'row': true,
        'checkbox': true
      }
    },
    'chipView': {
      'itemTpl'(a) {
        return StringHelper.xss`${ a.name }`;
      },
      'scrollable': { 'overflowX': 'hidden-scroll' }
    }
  };
  static type = 'treecombo';
  static $name = 'TreeCombo';
  changePicker(a, b) {
    if (!this.isDestroying && !a.columns) {
      console.warn('No `columns` provided for the TreeCombo `picker` config');
      a.columns = [{ 'type': 'tree' }];
    }
    return super.changePicker(a ? {
      ...a,
      'selectedRecordCollection': this.valueCollection
    } : a, b);
  }
  updateMultiSelect(a) {
    super.updateMultiSelect(...arguments);
    this.picker.selectionMode.multiSelect = a;
  }
  updateReadOnly(a) {
    super.updateReadOnly(...arguments);
    if (this._picker) {
      this.picker.readOnly = a;
    }
  }
};
TreeCombo.initClass();
TreeCombo._$name = 'TreeCombo';
const DurationColumn = class extends NumberColumn {
  static defaults = {
    'abbreviatedUnit': null,
    'min': null,
    'max': null,
    'step': 1,
    'largeStep': 0,
    'field': 'fullDuration',
    'text': 'L{Duration}',
    'instantUpdate': true,
    'filterType': 'duration',
    'sortable'(a, b) {
      const c = a.getValue(this.field), e = b.getValue(this.field);
      return c - e;
    }
  };
  static fields = [
    { 'name': 'decimalPrecision' },
    'abbreviatedUnit'
  ];
  static isGanttColumn = true;
  static type = 'duration';
  static $name = 'DurationColumn';
  compositeField = true;
  constructor() {
    super(...arguments);
  }
  construct() {
    super.construct(...arguments);
    const a = this.sortable;
    this.sortable = (...b) => a.call(this, ...b);
  }
  get defaultEditor() {
    const {
      max: a,
      min: b,
      step: c,
      largeStep: e
    } = this;
    return ObjectHelper.cleanupProperties({
      'type': 'duration',
      'name': this.field,
      'max': a,
      'min': b,
      'step': c,
      'largeStep': e
    });
  }
  get durationUnitField() {
    return this.field + 'Unit';
  }
  roundValue(a) {
    let b = typeof this.decimalPrecision === 'number' ? this.decimalPrecision : this.grid.durationDisplayPrecision;
    if (typeof b !== 'number') {
      b = 1;
    }
    const c = Math.pow(10, b), e = Math.round(a * c) / c;
    return e;
  }
  formatValue(a, b) {
    if (a instanceof Duration) {
      b = a.unit;
      a = a.magnitude;
    }
    a = this.roundValue(a);
    const c = this.abbreviatedUnit ? 'getShortNameOfUnit' : 'getLocalizedNameOfUnit';
    return super.formatValue(a) + (this.abbreviatedUnit ? '' : ' ') + DateHelper[c](b, a !== 1);
  }
  defaultRenderer({
    value: a,
    record: b,
    isExport: c
  }) {
    const e = typeof a, f = e === 'number' ? a : a == null ? void 0 : a.magnitude, g = e === 'number' ? b.getValue(this.durationUnitField) : a == null ? void 0 : a.unit;
    if (typeof f !== 'number') {
      return c ? '' : null;
    }
    return this.formatValue(f, g);
  }
  toClipboardString({record: a}) {
    return a.getValue(this.field).toString();
  }
  fromClipboardString({
    string: a,
    record: b
  }) {
    const c = DateHelper.parseDuration(a, true, this.durationUnit);
    if (c && 'magnitude' in c) {
      return c;
    }
    return b.fullDuration;
  }
  calculateFillValue({
    value: a,
    record: b
  }) {
    return this.fromClipboardString({
      'string': a,
      'record': b
    });
  }
};
ColumnStore.registerColumnType(DurationColumn);
DurationColumn._$name = 'DurationColumn';
const dateFieldsToConvert = {
  'startDate': 1,
  'endDate': 1,
  'constraintDate': 1,
  'deadlineDate': 1
};
const TimeZonedDatesMixin_default = a => {
  let b;
  return b = class extends a {
    get timeZone() {
      return this.getData('timeZone');
    }
    set timeZone(c) {
      this.setData('timeZone', c);
    }
    setLocalDate(c, e) {
      const f = this;
      if (f.timeZone != null) {
        e = TimeZoneHelper.toTimeZone(e, f.timeZone);
      }
      f.set(c, e, true);
      f['_' + c] = e;
    }
    getLocalDate(c) {
      if (this.timeZone != null && this[c] && !isNaN(this[c])) {
        return TimeZoneHelper.fromTimeZone(this[c], this.timeZone);
      }
      return this[c];
    }
    applyChangeset(c) {
      if (this.timeZone != null) {
        delete c.timeZone;
        for (const e in dateFieldsToConvert) {
          if (c[e]) {
            this.setLocalDate(e, new Date(c[e]));
            delete c[e];
          }
        }
      }
      return super.applyChangeset(...arguments);
    }
    getFieldPersistentValue(c) {
      let e, f;
      if (this.timeZone != null) {
        const g = (f = (e = c == null ? void 0 : c.field) != null ? e : c == null ? void 0 : c.name) != null ? f : c;
        if (dateFieldsToConvert[g]) {
          const i = this.getLocalDate(g);
          return c.serialize ? c.serialize(i) : i;
        }
      }
      return super.getFieldPersistentValue(c);
    }
    convertToTimeZone(c) {
      let e;
      const f = this, g = { ...f.meta.modified }, i = { ...dateFieldsToConvert };
      if (f.isTask && !f.manuallyScheduled) {
        delete i.startDate;
        delete i.endDate;
      }
      for (const j in i) {
        if (f[j] != null) {
          i[j] = f[j];
          if (f.timeZone != null) {
            i[j] = f.getLocalDate(j);
            if (g[j]) {
              g[j] = TimeZoneHelper.fromTimeZone(g[j], f.timeZone);
            }
          }
        } else {
          delete i[j];
        }
      }
      f.timeZone = c;
      for (const k in i) {
        f.setLocalDate(k, i[k], false);
        i[k] = 1;
        if (f.timeZone != null && g[k]) {
          g[k] = TimeZoneHelper.toTimeZone(g[k], f.timeZone);
        }
      }
      f.internalClearChanges(true, true, i);
      if (!ObjectHelper.isEmpty(g)) {
        f.meta.modified = g;
        (e = f.stores) == null ? void 0 : e.forEach(l => l.modified.add(f));
      }
    }
  }, __publicField(b, '$name', 'TimeZonedDatesMixin'), __publicField(b, 'fields', [{
      'name': 'timeZone',
      'persist': false
    }]), b;
};
const TimeSpan = class extends Model.mixin(TimeZonedDatesMixin_default) {
  static fields = [
    {
      'name': 'startDate',
      'type': 'date'
    },
    {
      'name': 'endDate',
      'type': 'date'
    },
    {
      'name': 'duration',
      'type': 'number',
      'allowNull': true,
      'internal': true
    },
    {
      'type': 'durationunit',
      'name': 'durationUnit',
      'defaultValue': 'd',
      'internal': true
    },
    {
      'name': 'fullDuration',
      'persist': false,
      'column': { 'type': 'duration' },
      'useProp': true
    },
    {
      'name': 'cls',
      'defaultValue': '',
      'internal': true
    },
    {
      'name': 'iconCls',
      'internal': true
    },
    {
      'name': 'style',
      'type': 'object',
      'internal': true
    },
    {
      'name': 'name',
      'type': 'string',
      'defaultValue': ''
    }
  ];
  static $name = 'TimeSpan';
  construct(a, ...b) {
    const c = this;
    if (a == null ? void 0 : a.fullDuration) {
      const {
        magnitude: e,
        unit: f
      } = a.fullDuration;
      a.duration = e;
      a.unit = f;
      delete a.fullDuration;
    }
    super.construct(a, ...b);
    c.normalize();
    if (c.startDateMS && c.endDateMS && c.startDateMS > c.endDateMS) {
      console.error('startDate > endDate for ' + c.constructor.$name + ' record with id: ' + c.id);
    }
  }
  internalCalculateStartDate(a, b, c) {
    return DateHelper.add(a, -b, c);
  }
  internalCalculateEndDate(a, b, c) {
    return DateHelper.add(a, b, c);
  }
  internalCalculateDuration(a, b, c) {
    return DateHelper.as(c, DateHelper.diff(a, b, 'h'), 'h');
  }
  internalNormalize(a, b, c, e) {
    const f = this, g = c != null;
    if (a && b && !g) {
      f.setData('duration', f.internalCalculateDuration(a, b, e));
    } else if (a && !b && g) {
      f.setData('endDate', f.internalCalculateEndDate(a, c, e));
    } else if (!a && b && g) {
      f.setData('startDate', f.internalCalculateStartDate(b, c, e));
    }
  }
  normalize() {
    this.internalNormalize(this.startDate, this.endDate, this.duration, this.durationUnit || this.constructor.defaultValues.durationUnit);
  }
  get eventStore() {
    let a;
    const b = this;
    if (b.isOccurrence) {
      return b.recurringTimeSpan.eventStore;
    }
    if (!b._eventStore) {
      b._eventStore = (a = b.stores) == null ? void 0 : a.find(c => c.isEventStore);
    }
    return b._eventStore;
  }
  updateInternalCls(a) {
    if (this._cls) {
      this._cls.value = a;
    } else {
      this._cls = new DomClassList(a);
    }
  }
  set internalCls(a) {
    this.updateInternalCls(a);
    this.set('cls', this._cls.value);
  }
  get internalCls() {
    const {cls: a} = this;
    if (a == null ? void 0 : a.isDomClassList) {
      return a;
    }
    this.updateInternalCls(a);
    this.setData('cls', this._cls.value);
    return this._cls;
  }
  get cls() {
    if (!this._cls) {
      this._cls = new DomClassList(super.get('cls'));
    }
    return this._cls;
  }
  set cls(a) {
    this.internalCls = a;
  }
  get startDate() {
    return this.get('startDate');
  }
  set startDate(a) {
    this.setStartDate(a);
  }
  get endDate() {
    return this.get('endDate');
  }
  set endDate(a) {
    this.setEndDate(a);
  }
  get endingDate() {
    const a = this, {
        endDate: b,
        startDate: c
      } = a;
    if (b) {
      return b;
    }
    return DateHelper.add(c, a.duration, a.durationUnit);
  }
  get duration() {
    return this.get('duration');
  }
  set duration(a) {
    this.setDuration(a, this.durationUnit);
  }
  get durationUnit() {
    return this.get('durationUnit');
  }
  setDuration(a, b = this.durationUnit) {
    a = parseFloat(a);
    this.set({
      'duration': a,
      'durationUnit': b,
      ...this.updateDatesFromDuration(a, b)
    });
  }
  updateDatesFromDuration(a, b, c = this.startDate, e = this.endDate) {
    const f = {};
    if (c) {
      f.endDate = this.internalCalculateEndDate(c, a, b);
    } else if (e) {
      f.startDate = this.internalCalculateStartDate(e, a, b);
    }
    return f;
  }
  getDurationInUnit(a, b) {
    const c = this;
    if (c.startDate && c.endDate) {
      return DateHelper.getDurationInUnit(c.startDate, c.endDate, a, b);
    } else {
      return DateHelper.as(a, c.duration, c.durationUnit);
    }
  }
  get fullDuration() {
    return new Duration({
      'unit': this.durationUnit,
      'magnitude': this.duration
    });
  }
  set fullDuration(a) {
    if (typeof a === 'string') {
      a = DateHelper.parseDuration(a, true, this.durationUnit);
    }
    this.setDuration(a.magnitude, a.unit);
  }
  setStartDate(a, b = true) {
    const c = this, e = { 'startDate': a };
    if (a) {
      let f;
      if (b) {
        f = c.duration != null;
      } else {
        if (c.endDate) {
          e.duration = c.internalCalculateDuration(a, c.endDate, c.durationUnit);
          if (e.duration < 0) {
            throw new Error('Negative duration');
          }
        } else {
          f = c.duration != null;
        }
      }
      if (f) {
        e.endDate = c.internalCalculateEndDate(a, c.duration, c.durationUnit);
      }
    } else {
      e.duration = null;
    }
    c.set(e);
  }
  setEndDate(a, b = false) {
    const c = this, e = { 'endDate': a };
    if (a) {
      let f;
      if (b === true) {
        f = c.duration != null;
      } else {
        if (c.startDate) {
          e.duration = c.internalCalculateDuration(c.startDate, a, c.durationUnit);
          if (e.duration < 0)
            throw new Error('Negative duration');
        } else {
          f = this.duration != null;
        }
      }
      if (f) {
        e.startDate = c.internalCalculateStartDate(a, c.duration, c.durationUnit);
      }
    }
    c.set(e);
  }
  setStartEndDate(a, b, c) {
    if (a > b) {
      throw new Error('Start date must be less or equal to end date');
    }
    this.set({
      'startDate': a,
      'endDate': b
    }, null, c);
  }
  get dates() {
    const a = [], b = DateHelper.startOf(this.startDate, 'day'), c = this.endDate;
    for (let e = b; e < c; e = DateHelper.add(e, 1, 'day')) {
      a.push(e);
    }
    return a;
  }
  get startDateMS() {
    let a;
    return (a = this.batching && this.hasBatchedChange('startDate') ? this.get('startDate') : this.startDate) == null ? void 0 : a.getTime();
  }
  get endDateMS() {
    let a;
    return (a = this.batching && this.hasBatchedChange('endDate') ? this.get('endDate') : this.endDate) == null ? void 0 : a.getTime();
  }
  get durationMS() {
    const {
      endDateMS: a,
      startDateMS: b
    } = this;
    if (a && b) {
      return a - b;
    }
    return DateHelper.asMilliseconds(this.duration || 0, this.durationUnit);
  }
  get isMilestone() {
    return this.duration === 0;
  }
  inSetNormalize(a) {
    if (typeof a !== 'string') {
      let {
        startDate: b,
        endDate: c,
        duration: e,
        durationUnit: durationUnit = this.durationUnit
      } = a;
      if (typeof b === 'string') {
        b = this.getFieldDefinition('startDate').convert(b);
      }
      if (typeof c === 'string') {
        c = this.getFieldDefinition('endDate').convert(c);
      }
      if ('duration' in a) {
        if (b && !c) {
          c = this.internalCalculateEndDate(b, e, durationUnit);
        }
        if (!b && c) {
          b = this.internalCalculateStartDate(c, e, durationUnit);
        }
      } else if (b && c) {
        e = this.internalCalculateDuration(b, c, durationUnit);
      }
      const f = Object.isFrozen(a) ? ObjectHelper.clone(a) : a;
      b && (f.startDate = b);
      c && (f.endDate = c);
      e != null && (f.duration = e);
      return f;
    }
  }
  fieldToKeys(a, b) {
    let c, e;
    const f = super.fieldToKeys(a, b);
    if (f.fullDuration) {
      const {
        magnitude: g,
        unit: i
      } = f.fullDuration;
      f.duration = g;
      f.durationUnit = i;
    }
    if (!this.isEventModel && !this.isTaskModel) {
      if (('duration' in f || f.durationUnit) && !(f.startDate && f.endDate)) {
        Object.assign(f, this.updateDatesFromDuration((c = f.duration) != null ? c : this.duration, (e = f.durationUnit) != null ? e : this.durationUnit, f.startDate, f.endDate));
      }
    }
    return f;
  }
  inSet(a, b, c, e, f, g, i) {
    let j;
    const k = this;
    if (!f) {
      a = k.inSetNormalize(a) || a;
    }
    const l = super.inSet(a, b, c, e, f, g, i);
    if (!((j = k.project) == null ? void 0 : j.isWritingData) && a.startDate && a.endDate) {
      let n;
      if (a.startDate instanceof Date && a.endDate instanceof Date) {
        n = a.startDate > a.endDate;
      } else if (k.fieldMap.startDate.convert(a.startDate) > k.fieldMap.endDate.convert(a.endDate)) {
        n = true;
      }
      if (n) {
        console.error('startDate > endDate for ' + k.constructor.$name + ' record with id: ' + k.id);
      }
    }
    return l;
  }
  applyValue(a, b, c, e, f) {
    if (b === 'cls') {
      this.updateInternalCls(c);
    }
    super.applyValue(a, b, c, e, f);
  }
  forEachDate(a, b) {
    return this.dates.forEach(a.bind(b));
  }
  get isScheduled() {
    const {
      startDateMS: a,
      endDateMS: b
    } = this;
    return b - a >= 0;
  }
  get isValid() {
    const {
      startDate: a,
      endDate: b
    } = this;
    return !a || !b || b - a >= 0;
  }
  shift(a, b = this.durationUnit) {
    return this.setStartDate(DateHelper.add(this.startDate, a, b, true), true);
  }
  get wbsCode() {
    return this._wbsCode || this.indexPath.join('.');
  }
  set wbsCode(a) {
    this._wbsCode = a;
  }
  fullCopy() {
    return this.copy.apply(this, arguments);
  }
  intersects(a) {
    return this.intersectsRange(a.startDate, a.endDate);
  }
  intersectsRange(a, b) {
    const c = this.startDate, e = this.endDate;
    return c && e && DateHelper.intersectSpans(c, e, a, b);
  }
  split(a = 0.5) {
    const b = this, c = b.copy(), {
        fullDuration: e,
        eventStore: f,
        assignmentStore: g
      } = b, i = new Duration(e), j = new Duration(e);
    let k, l;
    if (typeof a === 'string') {
      k = new Duration(a);
      if (k.unit === i.unit) {
        j.magnitude -= k.magnitude;
      } else {
        j.magnitude = DateHelper.as(k.unit, i) - k.magnitude;
        j.unit = k.unit;
        l = true;
      }
    } else {
      k = new Duration(a > 1 ? a : b.duration * a, b.durationUnit);
      j.magnitude -= k.magnitude;
    }
    c.startDate = DateHelper.add(b.startDate, k.magnitude, k.unit);
    if (l) {
      c.fullDuration = j;
      b.fullDuration = k;
    } else {
      c.duration = j.magnitude;
      b.duration = k.magnitude;
    }
    if (f) {
      f.add(c);
      if (g && !f.usesSingleAssignment) {
        g.add(b.assignments.map(n => {
          const o = Object.assign({}, n.data, {
            'eventId': c.id,
            'event': null,
            'resource': null
          });
          delete o.id;
          return o;
        }));
      }
    }
    return c;
  }
  toICSString(a = {}) {
    if (!this.isScheduled) {
      return '';
    }
    const {
        startDate: b,
        endDate: c
      } = this, e = a.DTSTAMP || DateHelper.format(new Date(), 'uu');
    delete a.DTSTAMP;
    let f = {};
    if (this.allDay) {
      f = {
        'DTSTART;VALUE=DATE': DateHelper.format(b, 'u'),
        'DTEND;VALUE=DATE': DateHelper.format(c, 'u')
      };
    } else {
      f = {
        'DTSTART': DateHelper.format(b, 'uu'),
        'DTEND': DateHelper.format(c, 'uu')
      };
    }
    const g = VersionHelper.scheduler && VersionHelper.getVersion('scheduler') || VersionHelper.calendar && VersionHelper.getVersion('calendar') || '', i = {
        'BEGIN': 'VCALENDAR',
        'VERSION': '2.0',
        'CALSCALE': 'GREGORIAN',
        'PRODID': '-//Bryntum AB//Bryntum Scheduler ' + g + ' //EN',
        'END': 'VCALENDAR'
      }, j = {
        'BEGIN': 'VEVENT',
        'UID': this.id + '@bryntum.com',
        'CLASS': 'PUBLIC',
        'SUMMARY': this.name,
        'DTSTAMP': e,
        ...f,
        ...this.recurrenceRule ? { 'RRULE': this.recurrenceRule } : {},
        ...a,
        'END': 'VEVENT'
      }, k = Object.keys(i).map(n => n + ':' + i[n]), l = Object.keys(j).map(n => n + ':' + j[n]);
    k.splice(k.length - 1, 0, ...l);
    return k.join('\n');
  }
  exportToICS(a) {
    if (this.isScheduled) {
      const b = new Blob([this.toICSString(a)], { 'type': 'text/calendar' });
      BrowserHelper.downloadBlob(b, (this.name || 'Event') + '.ics');
    }
  }
  isEditable(a) {
    return this.getFieldDefinition(a) ? true : void 0;
  }
  isFieldModified(a) {
    if (a === 'fullDuration') {
      return super.isFieldModified('duration') || super.isFieldModified('durationUnit');
    }
    return super.isFieldModified(a);
  }
};
TimeSpan._$name = 'TimeSpan';
const ViewPreset = class extends Model {
  static $name = 'ViewPreset';
  static get fields() {
    return [
      {
        'name': 'base',
        'type': 'string'
      },
      {
        'name': 'name',
        'type': 'string'
      },
      {
        'name': 'rowHeight',
        'defaultValue': 24
      },
      {
        'name': 'tickWidth',
        'defaultValue': 50
      },
      {
        'name': 'tickHeight',
        'defaultValue': 50
      },
      {
        'name': 'displayDateFormat',
        'defaultValue': 'HH:mm'
      },
      {
        'name': 'shiftUnit',
        'defaultValue': 'hour'
      },
      {
        'name': 'shiftIncrement',
        'defaultValue': 1
      },
      {
        'name': 'defaultSpan',
        'defaultValue': 12
      },
      { 'name': 'mainUnit' },
      { 'name': 'start' },
      'timeResolution',
      'headers',
      'mainHeaderLevel',
      'columnLinesFor'
    ];
  }
  construct() {
    super.construct(...arguments);
    this.normalizeUnits();
  }
  generateId(a) {
    const b = this, {headers: c} = b, e = [];
    let f = Object.getPrototypeOf(b.data).id;
    if (!f) {
      for (let {length: g} = c, j = g - 1; j >= 0; j--) {
        const {
            unit: k,
            increment: l
          } = c[j], n = l > 1;
        e.push('' + (n ? l : '') + (j ? k : StringHelper.capitalize(k)) + (n ? 's' : ''));
      }
      f = e.join('And');
    }
    if (a.count && a.includes(f)) {
      f += '-' + b.tickWidth + 'by' + (b.tickHeight || b.tickWidth);
      if (a.includes(f)) {
        f += '-' + b.bottomHeader.increment;
        if (a.includes(f)) {
          f = IdHelper.generateId(f + '-');
        }
      }
    }
    return f;
  }
  normalizeUnits() {
    const a = this, {
        timeResolution: b,
        headers: c,
        shiftUnit: e
      } = a;
    if (c) {
      for (let f = 0, {length: g} = c; f < g; f++) {
        const j = c[f];
        j.unit = DateHelper.normalizeUnit(j.unit);
        if (j.splitUnit) {
          j.splitUnit = DateHelper.normalizeUnit(j.splitUnit);
        }
        if (!('increment' in j)) {
          c[f] = Object.assign({ 'increment': 1 }, j);
        }
      }
    }
    if (b) {
      b.unit = DateHelper.normalizeUnit(b.unit);
    }
    if (e) {
      a.shiftUnit = DateHelper.normalizeUnit(e);
    }
  }
  static normalizeHeaderConfig(a) {
    const {
        headerConfig: b,
        columnLinesFor: c,
        mainHeaderLevel: e
      } = a, f = a.headers = [];
    if (b.top) {
      if (c === 'top') {
        a.columnLinesFor = 0;
      }
      if (e === 'top') {
        a.mainHeaderLevel = 0;
      }
      f[0] = b.top;
    }
    if (b.middle) {
      if (c === 'middle') {
        a.columnLinesFor = f.length;
      }
      if (e === 'middle') {
        a.mainHeaderLevel = f.length;
      }
      f.push(b.middle);
    } else {
      throw new Error('ViewPreset.headerConfig must be configured with a middle');
    }
    if (b.bottom) {
      a.mainHeaderLevel = f.length - 1;
      if (c == null) {
        a.columnLinesFor = f.length - 1;
      } else if (c === 'bottom') {
        a.columnLinesFor = f.length;
      }
      if (e == null) {
        a.mainHeaderLevel = f.length - 1;
      }
      if (e === 'bottom') {
        a.mainHeaderLevel = f.length;
      }
      f.push(b.bottom);
    }
  }
  set() {
  }
  inSet() {
  }
  get columnLinesFor() {
    return 'columnLinesFor' in this.data ? this.data.columnLinesFor : this.headers.length - 1;
  }
  get tickSize() {
    return this._tickSize || this.tickWidth;
  }
  get tickWidth() {
    return 'tickWidth' in this.data ? this.data.tickWidth : 50;
  }
  get tickHeight() {
    return 'tickHeight' in this.data ? this.data.tickHeight : 50;
  }
  get headerConfig() {
    if (this.data.headerConfig) {
      return this.data.headerConfig;
    }
    const a = {}, {headers: b} = this, {length: c} = b;
    switch (c) {
    case 1:
      a.middle = b[0];
      break;
    case 2:
      if (this.mainHeaderLevel === 0) {
        a.middle = b[0];
        a.bottom = b[1];
      } else {
        a.top = b[0];
        a.middle = b[1];
      }
      break;
    case 3:
      a.top = b[0];
      a.middle = b[1];
      a.bottom = b[2];
      break;
    default:
      throw new Error('headerConfig object not supported for >3 header levels');
    }
    return a;
  }
  set mainHeaderLevel(a) {
    this.data.mainHeaderLevel = a;
  }
  get mainHeaderLevel() {
    if ('mainHeaderLevel' in this.data) {
      return this.data.mainHeaderLevel;
    }
    if (this.data.headers.length === 3) {
      return 1;
    }
    return this.headers.length - 1;
  }
  get mainHeader() {
    return this.headers[this.mainHeaderLevel];
  }
  get topHeader() {
    return this.headers[0];
  }
  get topUnit() {
    return this.topHeader.unit;
  }
  get topIncrement() {
    return this.topHeader.increment;
  }
  get bottomHeader() {
    return this.headers[this.headers.length - 1];
  }
  get leafUnit() {
    return this.bottomHeader.unit;
  }
  get leafIncrement() {
    return this.bottomHeader.increment;
  }
  get mainUnit() {
    if ('mainUnit' in this.data) {
      return this.data.mainUnit;
    }
    return this.mainHeader.unit;
  }
  get msPerPixel() {
    const {bottomHeader: a} = this;
    return Math.round(DateHelper.asMilliseconds(a.increment || 1, a.unit) / this.tickWidth);
  }
  get isValid() {
    const a = this;
    let b = true;
    for (const c of a.headers) {
      b = b && Boolean(DateHelper.normalizeUnit(c.unit));
    }
    if (a.timeResolution) {
      b = b && DateHelper.normalizeUnit(a.timeResolution.unit);
    }
    if (a.shiftUnit) {
      b = b && DateHelper.normalizeUnit(a.shiftUnit);
    }
    return b;
  }
};
ViewPreset._$name = 'ViewPreset';
const locale4 = {
  'localeName': 'En',
  'localeDesc': 'English (US)',
  'localeCode': 'en-US',
  'Object': { 'newEvent': 'New event' },
  'ResourceInfoColumn': { 'eventCountText': a => a + ' event' + (a !== 1 ? 's' : '') },
  'Dependencies': {
    'from': 'From',
    'to': 'To',
    'valid': 'Valid',
    'invalid': 'Invalid'
  },
  'DependencyType': {
    'SS': 'SS',
    'SF': 'SF',
    'FS': 'FS',
    'FF': 'FF',
    'StartToStart': 'Start-to-Start',
    'StartToEnd': 'Start-to-Finish',
    'EndToStart': 'Finish-to-Start',
    'EndToEnd': 'Finish-to-Finish',
    'short': [
      'SS',
      'SF',
      'FS',
      'FF'
    ],
    'long': [
      'Start-to-Start',
      'Start-to-Finish',
      'Finish-to-Start',
      'Finish-to-Finish'
    ]
  },
  'DependencyEdit': {
    'From': 'From',
    'To': 'To',
    'Type': 'Type',
    'Lag': 'Lag',
    'Edit dependency': 'Edit dependency',
    'Save': 'Save',
    'Delete': 'Delete',
    'Cancel': 'Cancel',
    'StartToStart': 'Start to Start',
    'StartToEnd': 'Start to End',
    'EndToStart': 'End to Start',
    'EndToEnd': 'End to End'
  },
  'EventEdit': {
    'Name': 'Name',
    'Resource': 'Resource',
    'Start': 'Start',
    'End': 'End',
    'Save': 'Save',
    'Delete': 'Delete',
    'Cancel': 'Cancel',
    'Edit event': 'Edit',
    'Repeat': 'Repeat'
  },
  'EventDrag': {
    'eventOverlapsExisting': 'Event overlaps existing event for this resource',
    'noDropOutsideTimeline': 'Event may not be dropped completely outside the timeline'
  },
  'SchedulerBase': {
    'Add event': 'Add event',
    'Delete event': 'Delete',
    'Unassign event': 'Unassign',
    'color': 'Color'
  },
  'TimeAxisHeaderMenu': {
    'pickZoomLevel': 'Zoom',
    'activeDateRange': 'Date range',
    'startText': 'Start date',
    'endText': 'End date',
    'todayText': 'Today'
  },
  'EventCopyPaste': {
    'copyEvent': 'Copy',
    'cutEvent': 'Cut',
    'pasteEvent': 'Paste'
  },
  'EventFilter': {
    'filterEvents': 'Filter tasks',
    'byName': 'By name'
  },
  'TimeRanges': { 'showCurrentTimeLine': 'Show current timeline' },
  'PresetManager': {
    'secondAndMinute': {
      'displayDateFormat': 'll LTS',
      'name': 'Seconds'
    },
    'minuteAndHour': {
      'topDateFormat': 'ddd MM/DD, hA',
      'displayDateFormat': 'll LST'
    },
    'hourAndDay': {
      'topDateFormat': 'ddd MM/DD',
      'middleDateFormat': 'LST',
      'displayDateFormat': 'll LST',
      'name': 'Day'
    },
    'day': { 'name': 'Day/hours' },
    'week': { 'name': 'Week/hours' },
    'dayAndWeek': {
      'displayDateFormat': 'll LST',
      'name': 'Week/days'
    },
    'dayAndMonth': { 'name': 'Month' },
    'weekAndDay': {
      'displayDateFormat': 'll LST',
      'name': 'Week'
    },
    'weekAndMonth': { 'name': 'Weeks' },
    'weekAndDayLetter': { 'name': 'Weeks/weekdays' },
    'weekDateAndMonth': { 'name': 'Months/weeks' },
    'monthAndYear': { 'name': 'Months' },
    'year': { 'name': 'Years' },
    'manyYears': { 'name': 'Multiple years' }
  },
  'RecurrenceConfirmationPopup': {
    'delete-title': 'You are deleting an event',
    'delete-all-message': 'Do you want to delete all occurrences of this event?',
    'delete-further-message': 'Do you want to delete this and all future occurrences of this event, or only the selected occurrence?',
    'delete-further-btn-text': 'Delete All Future Events',
    'delete-only-this-btn-text': 'Delete Only This Event',
    'update-title': 'You are changing a repeating event',
    'update-all-message': 'Do you want to change all occurrences of this event?',
    'update-further-message': 'Do you want to change only this occurrence of the event, or this and all future occurrences?',
    'update-further-btn-text': 'All Future Events',
    'update-only-this-btn-text': 'Only This Event',
    'Yes': 'Yes',
    'Cancel': 'Cancel',
    'width': 600
  },
  'RecurrenceLegend': {
    ' and ': ' and ',
    'Daily': 'Daily',
    'Weekly on {1}': ({days: a}) => 'Weekly on ' + a,
    'Monthly on {1}': ({days: a}) => 'Monthly on ' + a,
    'Yearly on {1} of {2}': ({
      days: a,
      months: b
    }) => 'Yearly on ' + a + ' of ' + b,
    'Every {0} days': ({interval: a}) => 'Every ' + a + ' days',
    'Every {0} weeks on {1}': ({
      interval: a,
      days: b
    }) => 'Every ' + a + ' weeks on ' + b,
    'Every {0} months on {1}': ({
      interval: a,
      days: b
    }) => 'Every ' + a + ' months on ' + b,
    'Every {0} years on {1} of {2}': ({
      interval: a,
      days: b,
      months: c
    }) => 'Every ' + a + ' years on ' + b + ' of ' + c,
    'position1': 'the first',
    'position2': 'the second',
    'position3': 'the third',
    'position4': 'the fourth',
    'position5': 'the fifth',
    'position-1': 'the last',
    'day': 'day',
    'weekday': 'weekday',
    'weekend day': 'weekend day',
    'daysFormat': ({
      position: a,
      days: b
    }) => a + ' ' + b
  },
  'RecurrenceEditor': {
    'Repeat event': 'Repeat event',
    'Cancel': 'Cancel',
    'Save': 'Save',
    'Frequency': 'Frequency',
    'Every': 'Every',
    'DAILYintervalUnit': 'day(s)',
    'WEEKLYintervalUnit': 'week(s)',
    'MONTHLYintervalUnit': 'month(s)',
    'YEARLYintervalUnit': 'year(s)',
    'Each': 'Each',
    'on': 'On',
    'the': 'The',
    'On the': 'On the',
    'End repeat': 'End repeat',
    'time(s)': 'time(s)',
    'Days': 'Days',
    'Months': 'Months'
  },
  'RecurrenceDaysCombo': {
    'day': 'day',
    'weekday': 'weekday',
    'weekend day': 'weekend day'
  },
  'RecurrencePositionsCombo': {
    'position1': 'first',
    'position2': 'second',
    'position3': 'third',
    'position4': 'fourth',
    'position5': 'fifth',
    'position-1': 'last'
  },
  'RecurrenceStopConditionCombo': {
    'Never': 'Never',
    'After': 'After',
    'On date': 'On date'
  },
  'RecurrenceFrequencyCombo': {
    'None': 'No repeat',
    'Daily': 'Daily',
    'Weekly': 'Weekly',
    'Monthly': 'Monthly',
    'Yearly': 'Yearly'
  },
  'RecurrenceCombo': {
    'None': 'None',
    'Custom': 'Custom...'
  },
  'Summary': { 'Summary for': a => 'Summary for ' + a },
  'ScheduleRangeCombo': {
    'completeview': 'Complete schedule',
    'currentview': 'Visible schedule',
    'daterange': 'Date range',
    'completedata': 'Complete schedule (for all events)'
  },
  'SchedulerExportDialog': {
    'Schedule range': 'Schedule range',
    'Export from': 'From',
    'Export to': 'To'
  },
  'ExcelExporter': { 'No resource assigned': 'No resource assigned' },
  'CrudManagerView': { 'serverResponseLabel': 'Server response:' },
  'DurationColumn': { 'Duration': 'Duration' }
};
const En_default3 = LocaleHelper.publishLocale(locale4);
const PresetStore = class extends Localizable_default(Store) {
  static get $name() {
    return 'PresetStore';
  }
  static get defaultConfig() {
    return {
      'useRawData': true,
      'modelClass': ViewPreset,
      'zoomOrder': 1
    };
  }
  set storage(a) {
    super.storage = a;
    this.storage.addSorter((b, c) => {
      const e = b.bottomHeader, f = c.bottomHeader;
      const g = c.msPerPixel - b.msPerPixel || unitMagnitudes[e.unit] - unitMagnitudes[f.unit] || e.increment - f.increment;
      return g * this.zoomOrder;
    });
  }
  get storage() {
    return super.storage;
  }
  getById(a) {
    return super.getById(a) || !this.isPresetManager && globalThis.bryntum.PresetManager.getById(a);
  }
  createRecord(a, ...b) {
    let c;
    if (a.isViewPreset) {
      return a;
    }
    if (typeof a === 'string') {
      c = this.getById(a);
    } else if (typeof a === 'number') {
      c = this.getAt(a);
    } else {
      if (a.base) {
        a = this.copyBaseValues(a);
      }
      return super.createRecord(a, ...b);
    }
    if (!c) {
      throw new Error('ViewPreset ' + a + ' does not exist');
    }
    return c;
  }
  updateLocalization() {
    super.updateLocalization();
    const a = this;
    let b = a.allRecords;
    if (a.isPresetManager) {
      b = new Set(b.concat(Object.values(a.basePresets)));
    }
    b.forEach(c => {
      let e = a.optionalL('L{PresetManager.' + c.id + '}', null, true);
      if (typeof e === 'string' && c.baseId) {
        e = a.optionalL('L{PresetManager.' + c.baseId + '}', null, true);
      }
      if (e && typeof e === 'object') {
        if (!c.originalDisplayDateFormat) {
          c.originalDisplayDateFormat = c.displayDateFormat;
        }
        if (c.mainHeaderLevel === 0 && e.topDateFormat) {
          e.middleDateFormat = e.middleDateFormat || e.topDateFormat;
        }
        c.setData('displayDateFormat', e.displayDateFormat || c.originalDisplayDateFormat);
        [
          'top',
          'middle',
          'bottom'
        ].forEach(f => {
          const g = c.headerConfig[f], i = e[f + 'DateFormat'];
          if (g) {
            if (!g.originalDateFormat && g.dateFormat) {
              g.originalDateFormat = g.dateFormat;
            }
            if (i && g.renderer) {
              g.renderer = null;
            }
            const j = i || g.originalDateFormat;
            if (j) {
              g.dateFormat = j;
            }
          }
        });
        if (e.name) {
          if (!c.unlocalizedName) {
            c.unlocalizedName = c.name;
          }
          c.setData('name', e.name);
        } else if (c.unlocalizedName && c.unlocalizedName !== c.name) {
          c.name = c.unlocalizedName;
          c.unlocalizedName = null;
        }
      }
    });
  }
  getCurrentConfig(a) {
    return super.getCurrentConfig(a).data;
  }
  copyBaseValues(a) {
    let b = this.getById(a.base);
    if (!b) {
      throw new Error('ViewPreset base \'' + a.base + '\' does not exist.');
    }
    b = ObjectHelper.clone(b.data);
    delete b.id;
    if (a.name) {
      delete b.name;
    }
    return ObjectHelper.merge(b, a);
  }
  add(a) {
    a = Array.isArray(a) ? a : [a];
    a.forEach(b => {
      if (b.isViewPreset && b.base) {
        b.data = this.copyBaseValues(b.originalData);
      }
    });
    return super.add(...arguments);
  }
};
PresetStore._$name = 'PresetStore';
const PresetManager = class extends PresetStore {
  static get $name() {
    return 'PresetManager';
  }
  static get defaultConfig() {
    return {
      'preventSubClassingModel': true,
      'basePresets': {
        'secondAndMinute': {
          'name': 'Seconds',
          'tickWidth': 30,
          'tickHeight': 40,
          'displayDateFormat': 'll LTS',
          'shiftIncrement': 10,
          'shiftUnit': 'minute',
          'defaultSpan': 24,
          'timeResolution': {
            'unit': 'second',
            'increment': 5
          },
          'headers': [
            {
              'unit': 'minute',
              'dateFormat': 'lll'
            },
            {
              'unit': 'second',
              'increment': 10,
              'dateFormat': 'ss'
            }
          ]
        },
        'minuteAndHour': {
          'name': 'Minutes',
          'tickWidth': 60,
          'tickHeight': 60,
          'displayDateFormat': 'll LT',
          'shiftIncrement': 1,
          'shiftUnit': 'hour',
          'defaultSpan': 24,
          'timeResolution': {
            'unit': 'minute',
            'increment': 15
          },
          'headers': [
            {
              'unit': 'hour',
              'dateFormat': 'ddd MM/DD, hA'
            },
            {
              'unit': 'minute',
              'increment': 30,
              'dateFormat': 'mm'
            }
          ]
        },
        'hourAndDay': {
          'name': 'Day',
          'tickWidth': 70,
          'tickHeight': 40,
          'displayDateFormat': 'll LT',
          'shiftIncrement': 1,
          'shiftUnit': 'day',
          'defaultSpan': 24,
          'timeResolution': {
            'unit': 'minute',
            'increment': 30
          },
          'headers': [
            {
              'unit': 'day',
              'dateFormat': 'ddd DD/MM'
            },
            {
              'unit': 'hour',
              'dateFormat': 'LT'
            }
          ]
        },
        'day': {
          'name': 'Day/hours',
          'displayDateFormat': 'LT',
          'shiftIncrement': 1,
          'shiftUnit': 'day',
          'defaultSpan': 1,
          'timeResolution': {
            'unit': 'minute',
            'increment': 30
          },
          'mainHeaderLevel': 0,
          'headers': [
            {
              'unit': 'day',
              'dateFormat': 'ddd DD/MM',
              'splitUnit': 'day'
            },
            {
              'unit': 'hour',
              'renderer'(a) {
                return '\n                                    <div class="b-sch-calendarcolumn-ct"><span class="b-sch-calendarcolumn-hours">' + DateHelper.format(a, 'HH') + '</span>\n                                    <span class="b-sch-calendarcolumn-minutes">' + DateHelper.format(a, 'mm') + '</span></div>\n                                ';
              }
            }
          ]
        },
        'week': {
          'name': 'Week/hours',
          'displayDateFormat': 'LT',
          'shiftIncrement': 1,
          'shiftUnit': 'week',
          'defaultSpan': 24,
          'timeResolution': {
            'unit': 'minute',
            'increment': 30
          },
          'mainHeaderLevel': 0,
          'headers': [
            {
              'unit': 'week',
              'dateFormat': 'D d',
              'splitUnit': 'day'
            },
            {
              'unit': 'hour',
              'dateFormat': 'LT',
              'renderer'(a) {
                return '\n                                    <div class="sch-calendarcolumn-ct">\n                                    <span class="sch-calendarcolumn-hours">' + DateHelper.format(a, 'HH') + '</span>\n                                    <span class="sch-calendarcolumn-minutes">' + DateHelper.format(a, 'mm') + '</span>\n                                    </div>\n                                ';
              }
            }
          ]
        },
        'dayAndWeek': {
          'name': 'Days & Weeks',
          'tickWidth': 100,
          'tickHeight': 80,
          'displayDateFormat': 'll LT',
          'shiftUnit': 'day',
          'shiftIncrement': 1,
          'defaultSpan': 5,
          'timeResolution': {
            'unit': 'hour',
            'increment': 1
          },
          'headers': [
            {
              'unit': 'week',
              'renderer'(a) {
                return DateHelper.getShortNameOfUnit('week') + '.' + DateHelper.format(a, 'WW MMM YYYY');
              }
            },
            {
              'unit': 'day',
              'dateFormat': 'dd DD'
            }
          ]
        },
        'dayAndMonth': {
          'name': 'Month',
          'tickWidth': 100,
          'tickHeight': 80,
          'displayDateFormat': 'll LT',
          'shiftUnit': 'month',
          'shiftIncrement': 1,
          'defaultSpan': 1,
          'mainUnit': 'month',
          'timeResolution': {
            'unit': 'hour',
            'increment': 1
          },
          'headers': [
            {
              'unit': 'month',
              'dateFormat': 'MMMM YYYY'
            },
            {
              'unit': 'day',
              'dateFormat': 'DD'
            }
          ]
        },
        'weekAndDay': {
          'name': 'Week',
          'tickWidth': 100,
          'tickHeight': 80,
          'displayDateFormat': 'll hh:mm A',
          'shiftUnit': 'week',
          'shiftIncrement': 1,
          'defaultSpan': 1,
          'timeResolution': {
            'unit': 'day',
            'increment': 1
          },
          'mainHeaderLevel': 0,
          'headers': [
            {
              'unit': 'week',
              'dateFormat': 'YYYY MMMM DD'
            },
            {
              'unit': 'day',
              'increment': 1,
              'dateFormat': 'DD MMM'
            }
          ]
        },
        'weekAndMonth': {
          'name': 'Weeks',
          'tickWidth': 100,
          'tickHeight': 105,
          'displayDateFormat': 'll',
          'shiftUnit': 'week',
          'shiftIncrement': 5,
          'defaultSpan': 6,
          'timeResolution': {
            'unit': 'day',
            'increment': 1
          },
          'headers': [
            {
              'unit': 'month',
              'dateFormat': 'MMM YYYY'
            },
            {
              'unit': 'week',
              'dateFormat': 'DD MMM'
            }
          ]
        },
        'weekAndDayLetter': {
          'name': 'Weeks/weekdays',
          'tickWidth': 20,
          'tickHeight': 50,
          'displayDateFormat': 'll',
          'shiftUnit': 'week',
          'shiftIncrement': 1,
          'defaultSpan': 10,
          'timeResolution': {
            'unit': 'day',
            'increment': 1
          },
          'mainHeaderLevel': 0,
          'headers': [
            {
              'unit': 'week',
              'dateFormat': 'ddd DD MMM YYYY',
              'verticalColumnWidth': 115
            },
            {
              'unit': 'day',
              'dateFormat': 'd1',
              'verticalColumnWidth': 25
            }
          ]
        },
        'weekDateAndMonth': {
          'name': 'Months/weeks',
          'tickWidth': 30,
          'tickHeight': 40,
          'displayDateFormat': 'll',
          'shiftUnit': 'week',
          'shiftIncrement': 1,
          'defaultSpan': 10,
          'timeResolution': {
            'unit': 'day',
            'increment': 1
          },
          'headers': [
            {
              'unit': 'month',
              'dateFormat': 'YYYY MMMM'
            },
            {
              'unit': 'week',
              'dateFormat': 'DD'
            }
          ]
        },
        'monthAndYear': {
          'name': 'Months',
          'tickWidth': 110,
          'tickHeight': 110,
          'displayDateFormat': 'll',
          'shiftIncrement': 3,
          'shiftUnit': 'month',
          'defaultSpan': 12,
          'timeResolution': {
            'unit': 'day',
            'increment': 1
          },
          'headers': [
            {
              'unit': 'year',
              'dateFormat': 'YYYY'
            },
            {
              'unit': 'month',
              'dateFormat': 'MMM YYYY'
            }
          ]
        },
        'year': {
          'name': 'Years',
          'tickWidth': 100,
          'tickHeight': 100,
          'resourceColumnWidth': 100,
          'displayDateFormat': 'll',
          'shiftUnit': 'year',
          'shiftIncrement': 1,
          'defaultSpan': 1,
          'mainHeaderLevel': 0,
          'timeResolution': {
            'unit': 'month',
            'increment': 1
          },
          'headers': [
            {
              'unit': 'year',
              'dateFormat': 'YYYY'
            },
            {
              'unit': 'quarter',
              'renderer'(a, b, c) {
                return DateHelper.getShortNameOfUnit('quarter').toUpperCase() + (Math.floor(a.getMonth() / 3) + 1);
              }
            }
          ]
        },
        'manyYears': {
          'name': 'Multiple years',
          'tickWidth': 40,
          'tickHeight': 50,
          'displayDateFormat': 'll',
          'shiftUnit': 'year',
          'shiftIncrement': 1,
          'defaultSpan': 10,
          'timeResolution': {
            'unit': 'year',
            'increment': 1
          },
          'mainHeaderLevel': 0,
          'headers': [
            {
              'unit': 'year',
              'increment': 5,
              'renderer': (a, b) => a.getFullYear() + ' - ' + b.getFullYear()
            },
            {
              'unit': 'year',
              'dateFormat': 'YY',
              'increment': 1
            }
          ]
        }
      },
      'defaultPresets': [
        'manyYears',
        {
          'width': 80,
          'increment': 1,
          'resolution': 1,
          'base': 'manyYears',
          'resolutionUnit': 'YEAR'
        },
        'year',
        {
          'width': 30,
          'increment': 1,
          'resolution': 1,
          'base': 'year',
          'resolutionUnit': 'MONTH'
        },
        {
          'width': 50,
          'increment': 1,
          'resolution': 1,
          'base': 'year',
          'resolutionUnit': 'MONTH'
        },
        {
          'width': 200,
          'increment': 1,
          'resolution': 1,
          'base': 'year',
          'resolutionUnit': 'MONTH'
        },
        'monthAndYear',
        'weekDateAndMonth',
        'weekAndMonth',
        'weekAndDayLetter',
        'dayAndMonth',
        'weekAndDay',
        {
          'width': 54,
          'increment': 1,
          'resolution': 1,
          'base': 'weekAndDay',
          'resolutionUnit': 'HOUR'
        },
        'hourAndDay',
        {
          'width': 64,
          'increment': 6,
          'resolution': 30,
          'base': 'hourAndDay',
          'resolutionUnit': 'MINUTE'
        },
        {
          'width': 100,
          'increment': 6,
          'resolution': 30,
          'base': 'hourAndDay',
          'resolutionUnit': 'MINUTE'
        },
        {
          'width': 64,
          'increment': 2,
          'resolution': 30,
          'base': 'hourAndDay',
          'resolutionUnit': 'MINUTE'
        },
        'minuteAndHour',
        {
          'width': 60,
          'increment': 15,
          'resolution': 5,
          'base': 'minuteAndHour'
        },
        {
          'width': 130,
          'increment': 15,
          'resolution': 5,
          'base': 'minuteAndHour'
        },
        {
          'width': 60,
          'increment': 5,
          'resolution': 5,
          'base': 'minuteAndHour'
        },
        {
          'width': 100,
          'increment': 5,
          'resolution': 5,
          'base': 'minuteAndHour'
        },
        'secondAndMinute',
        {
          'width': 60,
          'increment': 10,
          'resolution': 5,
          'base': 'secondAndMinute'
        },
        {
          'width': 130,
          'increment': 5,
          'resolution': 5,
          'base': 'secondAndMinute'
        }
      ],
      'internalListeners': { 'locale': 'updateLocalization' }
    };
  }
  set basePresets(a) {
    const b = this._basePresets = {};
    this.getConfig('modelClass');
    for (const c in a) {
      a[c].id = c;
      b[c] = this.createRecord(a[c]);
    }
  }
  get basePresets() {
    return this._basePresets;
  }
  set defaultPresets(a) {
    for (let b = 0, {length: c} = a; b < c; b++) {
      const e = a[b], f = typeof e === 'string', g = f ? e : e.base;
      let j;
      if (f) {
        j = this.basePresets[g];
      } else {
        const k = Object.setPrototypeOf(ObjectHelper.clone(this.basePresets[g].data), { 'id': g }), {timeResolution: l} = k, n = k.headers[k.headers.length - 1];
        k.id = void 0;
        if ('width' in e) {
          k.tickWidth = e.width;
        }
        if ('height' in e) {
          k.tickHeight = e.height;
        }
        if ('increment' in e) {
          n.increment = e.increment;
        }
        if ('resolution' in e) {
          l.increment = e.resolution;
        }
        if ('resolutionUnit' in e) {
          l.unit = DateHelper.getUnitByName(e.resolutionUnit);
        }
        j = this.createRecord(k);
        j.baseId = g;
      }
      this.add(j);
    }
  }
  getById(a) {
    return super.getById(a) || this.basePresets[a];
  }
  registerPreset(a, b) {
    const c = this.createRecord(Object.assign({ 'id': a }, b)), e = this.find(f => f.equals(c));
    if (e) {
      return e;
    }
    if (c.isValid) {
      this.add(c);
    } else {
      throw new Error('Invalid preset, please check your configuration');
    }
    return c;
  }
  getPreset(a) {
    if (typeof a === 'number') {
      a = this.getAt(a);
    }
    if (typeof a === 'string') {
      a = this.getById(a);
    } else if (!(a instanceof ViewPreset)) {
      a = this.createRecord(a);
    }
    return a;
  }
  normalizePreset(a) {
    const b = this;
    if (!(a instanceof ViewPreset)) {
      if (typeof a === 'string') {
        a = b.getPreset(a);
        if (!a) {
          throw new Error('You must define a valid view preset. See PresetManager for reference');
        }
      } else if (typeof a === 'object') {
        if (a.base) {
          const c = this.getById(a.base);
          if (!c) {
            throw new Error('ViewPreset base \'' + a.base + '\' does not exist');
          }
          a = ObjectHelper.merge(ObjectHelper.clone(c.data), a);
        }
        if (a.id) {
          a = b.createRecord(a);
        } else {
          a = b.createRecord(ObjectHelper.assign({}, a));
          a.id = a.generateId(a);
        }
      }
    }
    return a;
  }
  deletePreset(a) {
    if (typeof a === 'string') {
      a = this.getById(a);
    } else if (typeof a === 'number') {
      a = this.getAt(a);
    }
    if (a) {
      this.remove(a);
      delete this.basePresets[a.id];
    }
  }
};
const pm = new PresetManager();
globalThis.bryntum.PresetManager = pm;
const Tick = class extends TimeSpan {
  get startDate() {
    return this.data.startDate;
  }
  get endDate() {
    return this.data.endDate;
  }
};
const TimeAxis = class extends Store {
  static get defaultConfig() {
    return {
      'modelClass': Tick,
      'continuous': true,
      'originalContinuous': null,
      'include': null,
      'autoAdjust': true,
      'adjustedStart': null,
      'adjustedEnd': null,
      'visibleTickStart': null,
      'visibleTickEnd': null,
      'tickCache': {},
      'viewPreset': null,
      'maxTraverseTries': 100,
      'useRawData': {
        'disableDuplicateIdCheck': true,
        'disableDefaultValue': true,
        'disableTypeConversion': true
      }
    };
  }
  static get configurable() {
    return {
      'generateTicks': null,
      'unit': null,
      'increment': null,
      'resolutionUnit': null,
      'resolutionIncrement': null,
      'mainUnit': null,
      'shiftUnit': null,
      'shiftIncrement': 1,
      'defaultSpan': 1,
      'weekStartDay': null,
      'forceFullTicks': null
    };
  }
  construct(a) {
    const b = this;
    super.construct(a);
    b.originalContinuous = b.continuous;
    b.ion({
      'change': ({action: c}) => {
        if (c !== 'filter') {
          b.trigger('reconfigure', { 'supressRefresh': false });
        }
      },
      'refresh': () => b.trigger('reconfigure', { 'supressRefresh': false }),
      'endreconfigure': c => b.trigger('reconfigure', c)
    });
    if (b.startDate) {
      b.internalOnReconfigure();
      b.trigger('reconfigure');
    } else if (b.viewPreset) {
      const c = b.getAdjustedDates(new Date());
      b.startDate = c.startDate;
      b.endDate = c.endDate;
    }
  }
  get isTimeAxis() {
    return true;
  }
  reconfigure(a, b = false, c = false) {
    const e = this, f = e.getAdjustedDates(a.startDate, a.endDate), g = {};
    if (e.trigger('beforeReconfigure', {
        'startDate': f.startDate,
        'endDate': f.endDate,
        'config': a
      }) !== false) {
      e.trigger('beginReconfigure');
      e._configuredStartDate = a.startDate;
      e._configuredEndDate = a.endDate;
      for (const j in a) {
        g[j] = e[j];
      }
      const i = a.viewPreset && a.viewPreset !== e.viewPreset;
      if (i) {
        c = e.isFiltered;
        e.filters.forEach(k => k.disabled = false);
      }
      Object.assign(e, a);
      if (e.internalOnReconfigure(c, i) === false) {
        return false;
      }
      e.trigger('endReconfigure', {
        'suppressRefresh': b,
        'config': a,
        'oldConfig': g
      });
    }
  }
  internalOnReconfigure(a = false, b) {
    const c = this;
    c.isConfigured = true;
    const e = c.getAdjustedDates(c.startDate, c.endDate, true), f = c.getAdjustedDates(c.startDate, c.endDate), g = f.startDate, i = f.endDate;
    if (g >= i) {
      throw new Error('Invalid start/end dates. Start date must be less than end date. Start date: ' + g + '. End date: ' + i + '.');
    }
    const {
        unit: j,
        increment: increment = 1
      } = c, k = c.generateTicks(g, i, j, increment) || c.constructor.prototype.generateTicks.call(c, g, i, j, increment);
    c.suspendEvents();
    c.maintainFilter = a;
    c.data = k;
    c.maintainFilter = false;
    const {count: l} = c;
    if (l === 0) {
      if (a) {
        if (b) {
          c.disableFilters();
        }
        c.resumeEvents();
        return false;
      }
      throw new Error('Invalid time axis configuration or filter, please check your input data.');
    }
    c.startDate = c.first.startDate;
    c.endDate = c.last.endDate;
    c.resumeEvents();
    if (c.isContinuous) {
      c.adjustedStart = e.startDate;
      c.adjustedEnd = DateHelper.getNext(l > 1 ? k[l - 1].startDate : e.startDate, j, increment, c.weekStartDay);
    } else {
      c.adjustedStart = c.startDate;
      c.adjustedEnd = c.endDate;
    }
    c.updateVisibleTickBoundaries();
    c.updateTickCache(true);
  }
  updateVisibleTickBoundaries() {
    const a = this, {
        count: b,
        unit: c,
        startDate: e,
        endDate: f,
        weekStartDay: g,
        increment: increment = 1
      } = a;
    const i = DateHelper.getNormalizedUnitDuration(e, c) * increment, j = DateHelper.getNormalizedUnitDuration(f, c) * increment;
    do {
      a.visibleTickStart = (e - a.adjustedStart) / i;
      if (a.autoAdjust)
        a.visibleTickStart = Math.floor(a.visibleTickStart);
      if (a.visibleTickStart >= 1)
        a.adjustedStart = DateHelper.getNext(a.adjustedStart, c, increment, g);
    } while (a.visibleTickStart >= 1);
    do {
      a.visibleTickEnd = b - (a.adjustedEnd - f) / j;
      if (b - a.visibleTickEnd >= 1)
        a.adjustedEnd = DateHelper.getNext(a.adjustedEnd, c, -1, g);
    } while (b - a.visibleTickEnd >= 1);
    a.fullTicks = !a.visibleTickStart && a.visibleTickEnd === b;
  }
  get viewPreset() {
    return this._viewPreset;
  }
  set viewPreset(a) {
    const b = this;
    a = pm.getPreset(a);
    if (!a.isViewPreset) {
      throw new Error('TimeAxis must be configured with the ViewPreset instance that the Scheduler is using');
    }
    b._viewPreset = a;
    Object.assign(b, {
      'unit': a.bottomHeader.unit,
      'increment': a.bottomHeader.increment || 1,
      'resolutionUnit': a.timeResolution.unit,
      'resolutionIncrement': a.timeResolution.increment,
      'mainUnit': a.mainHeader.unit,
      'shiftUnit': a.shiftUnit || a.mainHeader.unit,
      'shiftIncrement': a.shiftIncrement || 1,
      'defaultSpan': a.defaultSpan || 1,
      'presetName': a.id,
      'headers': a.headers
    });
  }
  get weekStartDay() {
    let a;
    return (a = this._weekStartDay) != null ? a : DateHelper.weekStartDay;
  }
  get resolution() {
    return {
      'unit': this.resolutionUnit,
      'increment': this.resolutionIncrement
    };
  }
  set resolution(a) {
    this.resolutionUnit = a.unit;
    this.resolutionIncrement = a.increment;
  }
  get resolutionUnit() {
    return this.forceFullTicks ? this.unit : this._resolutionUnit;
  }
  get resolutionIncrement() {
    return this.forceFullTicks ? this.increment : this._resolutionIncrement;
  }
  setTimeSpan(a, b, c = false) {
    if (b && a - b === 0) {
      b = null;
    }
    const e = this, {
        startDate: f,
        endDate: g
      } = e.getAdjustedDates(a, b);
    if (e.startDate - f !== 0 || e.endDate - g !== 0) {
      return e.reconfigure({
        'startDate': f,
        'endDate': g
      }, false, c);
    }
  }
  shift(a, b = this.shiftUnit) {
    const c = this;
    let {
      startDate: e,
      endDate: f
    } = c;
    if (c.isFiltered) {
      e = c.allRecords[0].startDate;
      f = c.allRecords[c.totalCount - 1].endDate;
    }
    let g = 0;
    do {
      e = DateHelper.add(e, a, b);
      f = DateHelper.add(f, a, b);
    } while (g++ < c.maxTraverseTries && c.setTimeSpan(e, f, { 'preventThrow': true }) === false);
  }
  shiftNext(a = this.shiftIncrement) {
    this.shift(a);
  }
  shiftPrevious(a = this.shiftIncrement) {
    this.shift(-a);
  }
  filterBy(a, b = this) {
    const c = this;
    c.filters.clear();
    super.filterBy((e, f) => a.call(b, e.data, f));
  }
  filter() {
    let a;
    const b = this;
    b.viewportCenterDate = (a = b.owner) == null ? void 0 : a.viewportCenterDate;
    const c = super.filter(...arguments);
    if (!b.maintainFilter && b.count === 0) {
      b.resumeEvents();
      b.trigger('invalidFilter');
      b.disableFilters();
    }
    return c;
  }
  disableFilters() {
    this.filters.forEach(a => a.disabled = true);
    this.filter();
  }
  triggerFilterEvent(a) {
    const b = this, {owner: c} = b;
    if (!a.filters.count) {
      b.continuous = b.originalContinuous;
    } else {
      b.continuous = false;
    }
    b.updateTickCache();
    if ((c == null ? void 0 : c.infiniteScroll) && !b.continuous) {
      c.timeAxisViewModel.ion({
        'update': () => {
          c.shiftToDate(b.viewportCenterDate, {
            'visibleDate': b.viewportCenterDate,
            'block': 'center'
          });
        },
        'once': true
      });
    }
    super.triggerFilterEvent(a);
  }
  get isContinuous() {
    return this.continuous !== false && !this.isFiltered;
  }
  getAdjustedDates(a, b, c = false) {
    const e = this;
    if (b && a - b === 0) {
      b = null;
    }
    a = a || e.startDate;
    b = b || DateHelper.add(a, e.defaultSpan, e.mainUnit);
    return e.autoAdjust || c ? {
      'startDate': e.floorDate(a, false, e.autoAdjust ? e.mainUnit : e.unit, 1),
      'endDate': e.ceilDate(b, false, e.autoAdjust ? e.mainUnit : e.unit, 1)
    } : {
      'startDate': a,
      'endDate': b
    };
  }
  get startDate() {
    return this._start || (this.first ? new Date(this.first.startDate) : null);
  }
  set startDate(a) {
    this._start = DateHelper.parse(a);
  }
  get endDate() {
    return this._end || (this.last ? new Date(this.last.endDate) : null);
  }
  set endDate(a) {
    if (a)
      this._end = DateHelper.parse(a);
  }
  get startMS() {
    return this._startMS;
  }
  get endMS() {
    return this._endMS;
  }
  floorDate(a, b, c, e) {
    b = b !== false;
    const f = this, g = b ? DateHelper.clone(f.startDate) : null, i = e || f.resolutionIncrement, j = c || (b ? f.resolutionUnit : f.mainUnit), k = (n, o) => Math.floor(n / o) * o;
    if (b) {
      return DateHelper.floor(a, {
        'unit': j,
        'magnitude': i
      }, g);
    }
    const l = DateHelper.clone(a);
    if (j === 'week') {
      const n = l.getDay() || 7, o = f.weekStartDay || 7;
      DateHelper.add(DateHelper.startOf(l, 'day', false), n >= o ? o - n : -(7 - o + n), 'day', false);
      if (l.getDay() !== o && l.getHours() === 23) {
        DateHelper.add(l, 1, 'hour', false);
      }
    } else {
      DateHelper.startOf(l, j, false);
      const p = [
          'day',
          'year'
        ].includes(j) ? 1 : 0, q = j === 'day' ? 'date' : j, r = k(DateHelper.get(l, q) - p, i) + p;
      DateHelper.set(l, q, r);
    }
    return l;
  }
  roundDate(a, b, c = this.resolutionUnit, e = this.resolutionIncrement || 1) {
    const f = this, g = DateHelper.clone(a);
    b = DateHelper.clone(b || f.startDate);
    switch (c) {
    case 'week': {
        DateHelper.startOf(g, 'day', false);
        let i = g.getDay() - f.weekStartDay, j;
        if (i < 0) {
          i = 7 + i;
        }
        if (Math.round(i / 7) === 1) {
          j = 7 - i;
        } else {
          j = -i;
        }
        return DateHelper.add(g, j, 'day', false);
      }
    case 'month': {
        const k = DateHelper.diff(b, g, 'month') + DateHelper.as('month', g.getDay() / DateHelper.daysInMonth(g)), l = Math.round(k / e) * e;
        return DateHelper.add(b, l, 'month', false);
      }
    case 'quarter':
      DateHelper.startOf(g, 'month', false);
      return DateHelper.add(g, 3 - g.getMonth() % 3, 'month', false);
    default: {
        const n = DateHelper.as(c, DateHelper.diff(b, g)), o = c === 'year' ? 0 : DateHelper.as(c, b.getTimezoneOffset() - g.getTimezoneOffset(), 'minute'), p = Math.round((n + o) / e) * e;
        return DateHelper.add(b, p - o, c, false);
      }
    }
  }
  ceilDate(a, b, c, e) {
    const f = this;
    b = b !== false;
    e = e || (b ? f.resolutionIncrement : 1);
    const g = c || (b ? f.resolutionUnit : f.mainUnit), i = DateHelper.clone(a);
    let j = false;
    switch (g) {
    case 'minute':
      j = !DateHelper.isStartOf(i, 'minute');
      break;
    case 'hour':
      j = !DateHelper.isStartOf(i, 'hour');
      break;
    case 'day':
    case 'date':
      j = !DateHelper.isStartOf(i, 'day');
      break;
    case 'week':
      DateHelper.startOf(i, 'day', false);
      j = i.getDay() !== f.weekStartDay || !DateHelper.isEqual(i, a);
      break;
    case 'month':
      DateHelper.startOf(i, 'day', false);
      j = i.getDate() !== 1 || !DateHelper.isEqual(i, a);
      break;
    case 'quarter':
      DateHelper.startOf(i, 'day', false);
      j = i.getMonth() % 3 !== 0 || i.getDate() !== 1 || !DateHelper.isEqual(i, a);
      break;
    case 'year':
      DateHelper.startOf(i, 'day', false);
      j = i.getMonth() !== 0 || i.getDate() !== 1 || !DateHelper.isEqual(i, a);
      break;
    }
    if (j) {
      return DateHelper.getNext(i, g, e, f.weekStartDay);
    }
    return i;
  }
  get include() {
    return this._include;
  }
  set include(a) {
    const b = this;
    b._include = a;
    b.continuous = !a;
    if (!b.isConfiguring) {
      b.startDate = b._configuredStartDate;
      b.endDate = b._configuredEndDate;
      b.internalOnReconfigure();
      b.trigger('includeChange');
    }
  }
  processExclusion(a, b, c) {
    const {include: e} = this;
    if (e) {
      return Object.entries(e).some(([f, g]) => {
        if (!g) {
          return false;
        }
        const {
          from: i,
          to: j
        } = g;
        if (DateHelper.compareUnits('day', c) >= 0 && DateHelper.getLargerUnit(f) === c) {
          if (i) {
            DateHelper.set(a, f, i);
          }
          if (j) {
            let k = c;
            if (c === 'day') {
              k = 'date';
            }
            DateHelper.set(b, {
              [k]: DateHelper.get(b, k) - 1,
              [f]: j
            });
          }
        }
        if (DateHelper.compareUnits(f, c) >= 0) {
          const l = f === 'day' ? a.getDay() : DateHelper.get(a, f);
          if (i && l < i || j && l >= j) {
            return true;
          }
        }
      });
    }
    return false;
  }
  initExclusion() {
    Object.entries(this.include).forEach(([a, b]) => {
      if (b) {
        const {
          from: c,
          to: e
        } = b;
        b.lengthFactor = DateHelper.getUnitToBaseUnitRatio(a, DateHelper.getLargerUnit(a)) / (e - c);
        b.lengthFactorExcl = DateHelper.getUnitToBaseUnitRatio(a, DateHelper.getLargerUnit(a)) / (e - c - 1);
        b.center = c + c / (b.lengthFactor - 1);
      }
    });
  }
  updateGenerateTicks() {
    if (!this.isConfiguring) {
      this.reconfigure(this);
    }
  }
  _generateTicks(a, b, c = this.unit, e = this.increment) {
    const f = this, g = [], i = Boolean(f.include);
    let j, k, l, n = 0, {
        startDate: o,
        endDate: p
      } = f.getAdjustedDates(a, b), q = false;
    f.tickCache = {};
    if (i) {
      f.initExclusion();
    }
    while (o < p) {
      j = DateHelper.getNext(o, c, e, f.weekStartDay);
      if (!f.autoAdjust && j > p && !q) {
        j = new Date(p.getTime());
      }
      if (c === 'hour' && e > 1 && g.length > 0 && n === 0) {
        const r = g[g.length - 1];
        n = (r.startDate.getHours() + e) % 24 - r.endDate.getHours();
        if (n !== 0) {
          j = DateHelper.add(j, n, 'hour');
        }
      }
      l = false;
      if (i) {
        k = new Date(j.getTime());
        l = f.processExclusion(o, j, c);
        if (q) {
          if (!f.autoAdjust && j > p) {
            o = k;
            continue;
          }
        } else if (j < o) {
          q = true;
          continue;
        }
      } else {
        k = j;
      }
      if (!l) {
        g.push({
          'id': g.length + 1,
          'startDate': o,
          'endDate': j
        });
        f.tickCache[o.getTime()] = g.length - 1;
      }
      o = k;
    }
    return g;
  }
  get visibleTickTimeSpan() {
    const a = this;
    return a.isContinuous ? a.visibleTickEnd - a.visibleTickStart : a.count;
  }
  getTickFromDate(a) {
    let b, c;
    const e = this, f = e.records, g = (c = (b = a.getTime) == null ? void 0 : b.call(a)) != null ? c : a;
    let j = 0, k = f.length - 1, l, n, o, p;
    if (!f.length || g < f[0].startDateMS || g > f[k].endDateMS) {
      return -1;
    }
    if (e.isContinuous) {
      while (j < k) {
        l = j + k + 1 >> 1;
        if (g > f[l].endDateMS) {
          j = l + 1;
        } else if (g < f[l].startDateMS) {
          k = l - 1;
        } else {
          j = l;
        }
      }
      n = f[j];
      o = n.startDateMS;
      if (g > o) {
        p = n.endDateMS;
        j += (g - o) / (p - o);
      }
      return Math.min(Math.max(j, e.visibleTickStart), e.visibleTickEnd);
    } else {
      for (let q = 0; q <= k; q++) {
        p = f[q].endDateMS;
        if (g <= p) {
          o = f[q].startDateMS;
          n = q + (g > o ? (g - o) / (p - o) : 0);
          return n;
        }
      }
    }
  }
  getSnappedTickFromDate(a) {
    const b = Math.floor(this.getTickFromDate(a));
    return this.getAt(b);
  }
  getDateFromTick(a, b) {
    const c = this;
    if (a === c.visibleTickEnd) {
      return c.endDate;
    }
    const e = Math.floor(a), f = a - e, g = c.getAt(e);
    if (!g) {
      return null;
    }
    const i = e === 0 && c.isContinuous ? c.adjustedStart : g.startDate, j = e === c.count - 1 && c.isContinuous ? c.adjustedEnd : g.endDate;
    let k = DateHelper.add(i, f * (j - i), 'millisecond');
    if (b) {
      k = c[b + 'Date'](k);
    }
    return k;
  }
  get ticks() {
    return this.records;
  }
  updateTickCache(a = false) {
    const b = this;
    if (b.count) {
      b._start = b.first.startDate;
      b._end = b.last.endDate;
      b._startMS = b.startDate.getTime();
      b._endMS = b.endDate.getTime();
    } else {
      b._start = b._end = b._startMs = b._endMS = null;
    }
    if (!a) {
      b.tickCache = {};
      b.forEach((c, e) => b.tickCache[c.startDate.getTime()] = e);
    }
  }
  dateInAxis(a, b = false) {
    const c = this, e = c.startDate, f = c.endDate;
    if (c.isContinuous) {
      return b ? DateHelper.betweenLesserEqual(a, e, f) : DateHelper.betweenLesser(a, e, f);
    } else {
      const g = c.count;
      let j, k, l;
      for (let n = 0; n < g; n++) {
        l = c.getAt(n);
        j = l.startDate;
        k = l.endDate;
        if (b && a <= k || !b && a < k) {
          return a >= j;
        }
      }
    }
    return false;
  }
  timeSpanInAxis(a, b) {
    const c = this;
    if (!b || b.getTime() === a.getTime()) {
      return this.dateInAxis(a, true);
    }
    if (c.isContinuous) {
      return DateHelper.intersectSpans(a, b, c.startDate, c.endDate);
    }
    return a < c.startDate && b > c.endDate || c.getTickFromDate(a) !== c.getTickFromDate(b);
  }
  isTimeSpanInAxis(a) {
    let b;
    const c = this, {
        startMS: e,
        endMS: f
      } = c, {startDateMS: g} = a, i = (b = a.endDateMS) != null ? b : a.meta.endDateCached;
    if (!g || !i) {
      return false;
    }
    if (i === g) {
      return c.dateInAxis(a.startDate, true);
    }
    if (c.isContinuous) {
      return i > e && g < f;
    }
    const j = c.getTickFromDate(g), k = c.getTickFromDate(i);
    if (j === c.count && DateHelper.isEqual(a.startDate, c.last.endDate) || k === 0 && DateHelper.isEqual(a.endDate, c.first.startDate)) {
      return false;
    }
    return g < e && i > f || j !== k;
  }
  forEachAuxInterval(a, b = 1, c, e = this) {
    const f = this.endDate;
    let g = this.startDate, j = 0, k;
    if (g > f) {
      throw new Error('Invalid time axis configuration');
    }
    while (g < f) {
      k = DateHelper.min(DateHelper.getNext(g, a, b, this.weekStartDay), f);
      c.call(e, g, k, j, k >= f);
      g = k;
      j++;
    }
  }
};
TimeAxis._$name = 'TimeAxis';
const TimeAxisViewModel = class extends Events_default() {
  static get defaultConfig() {
    return {
      'timeAxis': null,
      'availableSpace': null,
      'tickSize': 100,
      'snap': false,
      'forceFit': false,
      'headers': null,
      'mode': 'horizontal',
      'suppressFit': false,
      'columnConfig': [],
      'viewPreset': null,
      'columnLinesFor': null,
      'originalTickSize': null,
      'headersDatesCache': []
    };
  }
  construct(a) {
    const b = this;
    b.unitToPixelsCache = {};
    super.construct(a);
    const c = b.timeAxis.viewPreset || b.viewPreset;
    if (c) {
      if (c instanceof ViewPreset) {
        b.consumeViewPreset(c);
      } else {
        const e = pm.getPreset(c);
        e && b.consumeViewPreset(e);
      }
    }
    b.timeAxis.ion({
      'reconfigure': 'onTimeAxisReconfigure',
      'thisObj': b
    });
    b.configured = true;
  }
  doDestroy() {
    let a, b;
    (b = (a = this.timeAxis).un) == null ? void 0 : b.call(a, 'reconfigure', this.onTimeAxisReconfigure, this);
    super.doDestroy();
  }
  get columnConfig() {
    return this._columnConfig;
  }
  set columnConfig(a) {
    this._columnConfig = a;
  }
  get headers() {
    return this._headers;
  }
  set headers(a) {
    if (a && a.length && a[a.length - 1].cellGenerator) {
      throw new Error('`cellGenerator` cannot be used for the bottom level of your headers. Use TimeAxis#generateTicks() instead.');
    }
    this._headers = a;
  }
  get isTimeAxisViewModel() {
    return true;
  }
  get isHorizontal() {
    return this.mode !== 'vertical';
  }
  get isVertical() {
    return this.mode === 'vertical';
  }
  set forceFit(a) {
    if (a !== this._forceFit) {
      this._forceFit = a;
      this.update();
    }
  }
  reconfigure(a) {
    this.headers = null;
    this.setConfig(a);
    this.trigger('reconfigure');
  }
  onTimeAxisReconfigure({
    source: a,
    suppressRefresh: b
  }) {
    if (this.viewPreset !== a.viewPreset) {
      this.consumeViewPreset(a.viewPreset);
    }
    if (!b && a.count > 0) {
      this.update();
    }
  }
  update(a, b = false, c = false) {
    const e = this, {
        timeAxis: f,
        headers: g
      } = e, i = a !== 0;
    if (e.isConfiguring || i && e._availableSpace === a) {
      if (c) {
        e.trigger('update');
      }
      return;
    }
    e._availableSpace = Math.max(a || e.availableSpace || 0, 0);
    if (typeof e.availableSpace !== 'number') {
      throw new Error('Invalid available space provided to TimeAxisModel');
    }
    e.columnConfig = [];
    const j = e._tickSize = e.calculateTickSize(e.originalTickSize);
    if (typeof j !== 'number' || j < 0) {
      throw new Error('Invalid timeAxis tick size');
    }
    e.unitToPixelsCache = {};
    e._totalSize = null;
    for (let k = 0, {length: l} = g; k < l; k++) {
      const n = g[k];
      if (n.cellGenerator) {
        const o = n.cellGenerator.call(e, f.startDate, f.endDate);
        e.columnConfig[k] = e.createHeaderRow(k, n, o);
      } else {
        e.columnConfig[k] = e.createHeaderRow(k, n);
      }
    }
    if (!b) {
      e.trigger('update');
    }
  }
  getDistanceBetweenDates(a, b) {
    return this.getPositionFromDate(b) - this.getPositionFromDate(a);
  }
  getDistanceForDuration(a) {
    return this.getSingleUnitInPixels('millisecond') * a;
  }
  getPositionFromDate(a, b = {}) {
    const c = this.getScaledTick(a, b);
    if (c === -1) {
      return -1;
    }
    return this.tickSize * (c - this.timeAxis.visibleTickStart);
  }
  getScaledTick(a, {
    respectExclusion: b,
    snapToNextIncluded: c,
    isEnd: e,
    min: f,
    max: g
  }) {
    const {timeAxis: i} = this, {
        include: j,
        unit: k
      } = i;
    let l = i.getTickFromDate(a);
    if (l !== -1 && b && j) {
      let n = false;
      if (j.hour && DateHelper.compareUnits(k, 'hour') > 0 && k !== 'day') {
        const {
            from: o,
            to: p,
            lengthFactor: q,
            center: r
          } = j.hour, u = a.getHours(), v = Math.min(Math.max(u, o), p);
        if (!c && v !== u) {
          return -1;
        }
        const w = v + a.getMinutes() / 60, z = r - w, A = r - z * q;
        a = DateHelper.add(a, A - u, 'h');
        n = true;
      }
      if (j.day && DateHelper.compareUnits(k, 'day') > 0) {
        const {
          from: B,
          to: C,
          lengthFactor: E,
          center: F
        } = j.day;
        let G = a.getDay();
        if (e && a.getHours() === 0 && a.getMinutes() === 0 && a.getSeconds() === 0 && a.getMilliseconds() === 0) {
          if (--G < 0) {
            G = 6;
          }
        }
        let H = 0;
        if (G < B || G >= C) {
          if (c) {
            if (e) {
              H = (C - G - 8) % 7;
            } else {
              H = (B - G + 7) % 7;
            }
            a = DateHelper.add(a, H, 'd');
            a = DateHelper.startOf(a, 'd', false);
            if (g && a.getTime() >= g || f && a.getTime() <= f) {
              return -1;
            }
          } else {
            return -1;
          }
        }
        const {weekStartDay: I} = i, J = a.getDay() === 0 ? 0 : F, K = a.getDay() + a.getHours() / 24, L = J - K, N = J - L * E;
        a = DateHelper.add(a, N - K + I, 'd');
        n = true;
      }
      if (n) {
        a = DateHelper.constrain(a, i.startDate, i.endDate);
        l = i.getTickFromDate(a);
      }
    }
    return l;
  }
  getDateFromPosition(a, b, c = false) {
    const e = this, {timeAxis: f} = e, g = e.getScaledPosition(a) / e.tickSize + f.visibleTickStart;
    if (g < 0 || g > f.count) {
      if (c) {
        let i;
        if (g < 0) {
          i = DateHelper.add(f.startDate, g, f.unit);
        } else {
          i = DateHelper.add(f.endDate, g - f.count, f.unit);
        }
        if (b) {
          i = f[b + 'Date'](i);
        }
        return i;
      }
      return null;
    }
    return f.getDateFromTick(g, b);
  }
  getScaledPosition(a) {
    const {
      include: b,
      unit: c,
      weekStartDay: e
    } = this.timeAxis;
    if (b) {
      const f = this.getSingleUnitInPixels('day');
      if (b.day && DateHelper.compareUnits(c, 'day') > 0) {
        const {
            from: g,
            lengthFactor: i
          } = b.day, j = a % (f * 7), k = a - j;
        a = j / i + (g - e) * f + k;
      }
      if (b.hour && DateHelper.compareUnits(c, 'hour') > 0 && c !== 'day') {
        const {
            from: l,
            lengthFactorExcl: n
          } = b.hour, o = this.getSingleUnitInPixels('hour'), p = a % f, q = a - p;
        a = p / n + l * o + q;
      }
    }
    return a;
  }
  getSingleUnitInPixels(a) {
    const b = this;
    return b.unitToPixelsCache[a] || (b.unitToPixelsCache[a] = DateHelper.getUnitToBaseUnitRatio(b.timeAxis.unit, a, true) * b.tickSize / b.timeAxis.increment);
  }
  get snapPixelAmount() {
    if (this.snap) {
      const {resolution: a} = this.timeAxis;
      return (a.increment || 1) * this.getSingleUnitInPixels(a.unit);
    }
    return 1;
  }
  get tickSize() {
    return this._tickSize;
  }
  set tickSize(a) {
    this.setTickSize(a, false);
  }
  setTickSize(a, b) {
    this._tickSize = this.originalTickSize = a;
    this.update(void 0, b);
  }
  get timeResolution() {
    return this.timeAxis.resolution;
  }
  calculateTickSize(a) {
    const b = this, {
        forceFit: c,
        timeAxis: e,
        suppressFit: f
      } = b, g = e.unit;
    let i = 0, j = 1;
    if (b.snap) {
      const k = e.resolution;
      j = DateHelper.getUnitToBaseUnitRatio(g, k.unit) * k.increment;
    }
    if (!f) {
      const l = b.availableSpace / e.visibleTickTimeSpan;
      i = c || a < l ? l : a;
      if (j > 0 && (!c || j < 1)) {
        i = Math.max(1, j * i) / j;
      }
    } else {
      i = a;
    }
    return i;
  }
  get totalSize() {
    return this._totalSize || (this._totalSize = Math.floor(this.tickSize * this.timeAxis.visibleTickTimeSpan));
  }
  get availableSpace() {
    return this._availableSpace;
  }
  set availableSpace(a) {
    const b = this;
    b._availableSpace = Math.max(0, a);
    if (b._availableSpace > 0) {
      const c = b.calculateTickSize(b.originalTickSize);
      if (c > 0 && c !== b.tickSize) {
        b.update();
      }
    }
  }
  getDates(a = this.columnLinesFor, b = false, c = false) {
    const e = this, f = [], g = b ? e.lowestHeader : a, j = e.majorHeaderLevel, k = e.headers && e.headers[a].unit, n = j != null && e.headers && e.headers[j].unit, o = j != null && DateHelper.doesUnitsAlign(n, k), p = !!(e.headers && e.headers[g].cellGenerator);
    if (p) {
      const q = e.columnConfig[g];
      for (let r = 1, u = q.length; r < u; r++) {
        f.push({ 'date': q[r].startDate });
      }
    } else {
      e.forEachInterval(g, (v, w) => {
        f.push({
          'date': c ? w : v,
          'isMajor': j !== a && o && e.isMajorTick(c ? w : v)
        });
      });
    }
    return f;
  }
  get forceFit() {
    return this._forceFit;
  }
  fitToAvailableSpace(a) {
    const b = Math.floor(this.availableSpace / this.timeAxis.visibleTickTimeSpan);
    this.setTickSize(b, a);
  }
  get snap() {
    return this._snap;
  }
  set snap(a) {
    if (a !== this._snap) {
      this._snap = a;
      if (this.configured) {
        this.update();
      }
    }
  }
  createHeaderRow(a, b, c) {
    const e = this, f = [], {
        align: g,
        headerCellCls: headerCellCls = ''
      } = b, i = DateHelper.clearTime(new Date()), {timeAxis: j} = e, k = e.headers.length - 1, l = (o, p, q, r, u) => {
        let v = DateHelper.format(o, b.dateFormat);
        const w = q > 0 && !r, z = {
            'align': g,
            'start': o,
            'end': p,
            'value': u ? u.header : v,
            'headerCellCls': headerCellCls,
            'width': k === a && e.owner && (j.fullTicks || w) ? e.owner.tickSize : e.getDistanceBetweenDates(o, p),
            'index': q
          };
        if (z.width === 0) {
          return;
        }
        z.coord = n - 1;
        n += z.width;
        e.headersDatesCache[a][o.getTime()] = 1;
        if (b.renderer) {
          v = b.renderer.call(b.thisObj || e, o, p, z, q, e.owner);
          z.value = v == null ? '' : v;
        }
        if (b.unit === 'day' && (!b.increment || b.increment === 1)) {
          z.headerCellCls += ' b-sch-dayheadercell-' + o.getDay();
          if (DateHelper.clearTime(o, true) - i === 0) {
            z.headerCellCls += ' b-sch-dayheadercell-today';
          }
        }
        f.push(z);
      };
    let n = 0;
    e.headersDatesCache[a] = {};
    if (c) {
      c.forEach((o, p) => l(o.start, o.end, p, p === c.length - 1, o));
    } else {
      e.forEachInterval(a, l);
    }
    return f;
  }
  get mainHeader() {
    return 'mainHeaderLevel' in this ? this.headers[this.mainHeaderLevel] : this.bottomHeader;
  }
  get bottomHeader() {
    return this.headers[this.headers.length - 1];
  }
  get lowestHeader() {
    return this.headers.length - 1;
  }
  get majorHeaderLevel() {
    const {headers: a} = this;
    if (a) {
      return Math.max(a.length - 2, 0);
    }
    return null;
  }
  isMajorTick(a) {
    const b = this.majorHeaderLevel;
    return b != null && this.headersDatesCache[b] && this.headersDatesCache[b][a.getTime()] || false;
  }
  forEachInterval(a, b, c = this) {
    const {
      headers: e,
      timeAxis: f
    } = this;
    if (e) {
      if (a === e.length - 1) {
        f.forEach((g, i) => b.call(c, g.startDate, g.endDate, i, i === f.count - 1));
      } else {
        const g = e[a];
        f.forEachAuxInterval(g.unit, g.increment, b, c);
      }
    }
  }
  forEachMainInterval(a, b) {
    this.forEachInterval(this.mainHeaderLevel, a, b);
  }
  consumeViewPreset(a) {
    const b = this;
    b.headers = null;
    b.getConfig('tickSize');
    b.viewPreset = a;
    Object.assign(b, {
      'headers': a.headers,
      'columnLinesFor': a.columnLinesFor,
      'mainHeaderLevel': a.mainHeaderLevel,
      '_tickSize': b.isHorizontal ? a.tickWidth : a.tickHeight
    });
    b.originalTickSize = b.tickSize;
  }
};
TimeAxisViewModel._$name = 'TimeAxisViewModel';
const tempDate2 = new Date();
const TimelineDateMapper_default = a => {
  let b;
  return b = class extends (a || Base) {
    getRtlX(c) {
      if (this.rtl && this.isHorizontal) {
        c = this.timeAxisViewModel.totalSize - c;
      }
      return c;
    }
    getDateFromCoordinate(c, e, f = true, g = false, i = false) {
      if (!f) {
        c = this.currentOrientation.translateToScheduleCoordinate(c);
      }
      if (!i) {
        c = this.getRtlX(c);
      }
      return this.timeAxisViewModel.getDateFromPosition(c, e, g);
    }
    getDateFromCoord(c) {
      return this.getDateFromCoordinate(c.coord, c.roundingMethod, c.local, c.allowOutOfRange, c.ignoreRTL);
    }
    getDateFromVirtualCoord(c) {
      return this.getDateFromCoordinate(c.coord, c.roundingMethod, c.local, c.allowOutOfRange, true);
    }
    getDateFromXY(c, e, f = true, g = false) {
      return this.currentOrientation.getDateFromXY(c, e, f, g);
    }
    getDateFromDomEvent(c, f, g = false) {
      return this.getDateFromXY([
        c.pageX,
        c.pageY
      ], f, false, g);
    }
    getStartEndDatesFromRectangle(c, e, f, g = false) {
      const i = this, {isHorizontal: j} = i, k = j ? c.x : c.top, l = j ? c.right : c.bottom;
      let n, o;
      if (k >= 0 && l < i.timeAxisViewModel.totalSize) {
        n = i.getDateFromCoordinate(k, e, true);
        o = i.getDateFromCoordinate(l, e, true);
      } else if (k < 0) {
        o = i.getDateFromCoordinate(l, e, true, g);
        n = o && DateHelper.add(o, -f, 'ms');
      } else {
        n = i.getDateFromCoordinate(k, e, true, g);
        o = n && DateHelper.add(n, f, 'ms');
      }
      return {
        'start': n,
        'end': o
      };
    }
    getDisplayEndDate(c, e) {
      if (c.getHours() === 0 && c.getMinutes() === 0 && (!e || !(c.getYear() === e.getYear() && c.getMonth() === e.getMonth() && c.getDate() === e.getDate())) && !DateHelper.formatContainsHourInfo(this.displayDateFormat)) {
        c = DateHelper.add(c, -1, 'day');
      }
      return c;
    }
    getFormattedEndDate(c, e) {
      return this.getFormattedDate(this.getDisplayEndDate(c, e));
    }
    getCoordinateFromDate(c, e = true) {
      const f = this, {timeAxisViewModel: g} = f, {
          isContinuous: i,
          startMS: j,
          endMS: k,
          startDate: l,
          endDate: n,
          unit: o
        } = f.timeAxis, p = c.valueOf();
      if (e === true) {
        e = { 'local': true };
      } else if (!e) {
        e = { 'local': false };
      } else if (!('local' in e)) {
        e.local = true;
      }
      let q;
      if (!(c instanceof Date)) {
        tempDate2.setTime(c);
        c = tempDate2;
      }
      if (i && c.getTimezoneOffset() === l.getTimezoneOffset() && l.getTimezoneOffset() === n.getTimezoneOffset() && DateHelper.getUnitToBaseUnitRatio(o, 'day') !== -1) {
        if (p < j || p > k) {
          return -1;
        }
        q = (p - j) / (k - j) * g.totalSize;
      } else {
        q = g.getPositionFromDate(c, e);
      }
      if (f.rtl && f.isHorizontal && !(e == null ? void 0 : e.ignoreRTL)) {
        q = g.totalSize - q;
      }
      if (!e.local) {
        q = f.currentOrientation.translateToPageCoordinate(q);
      }
      return q;
    }
    getVirtualCoordFromDate(c, e) {
      return this.getCoordinateFromDate(c, {
        ...e,
        'ignoreRTL': true
      });
    }
    getTimeSpanDistance(c, e) {
      return this.timeAxisViewModel.getDistanceBetweenDates(c, e);
    }
    get viewportCenterDate() {
      if (this.isVisible) {
        const {
          timeAxis: c,
          timelineScroller: e
        } = this;
        if (c.isContinuous) {
          const f = (e.position + e.clientSize / 2) / e.scrollSize;
          return new Date(c.startMS + (c.endMS - c.startMS) * f);
        }
        return this.getDateFromCoordinate(e.position + e.clientSize / 2);
      }
    }
    get viewportCenterDateCached() {
      return this.cachedCenterDate || (this.cachedCenterDate = this.viewportCenterDate);
    }
    get timeResolution() {
      return this.timeAxis.resolution;
    }
    set timeResolution(c) {
      this.timeAxis.resolution = typeof c === 'number' ? {
        'increment': c,
        'unit': this.timeAxis.resolution.unit
      } : c;
    }
    get snap() {
      let c, e;
      return (e = (c = this._timeAxisViewModel) == null ? void 0 : c.snap) != null ? e : this._snap;
    }
    updateSnap(c) {
      if (!this.isConfiguring) {
        this.timeAxisViewModel.snap = c;
        this.timeAxis.forceFullTicks = c && this.fillTicks;
      }
    }
    onSchedulerHorizontalScroll(c, e, f, g) {
      if (!g) {
        this.cachedCenterDate = null;
      }
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'TimelineDateMapper'), __publicField(b, 'configurable', { 'snap': false }), b;
};
const {eventNameMap} = EventHelper;
const TimelineDomEvents_default = a => {
  let b;
  return b = class extends (a || Base) {
    hoveredEvents = new Set();
    constructor() {
      super(...arguments);
    }
    initDomEvents() {
      const c = this, {schedulerEvents: e} = c;
      e.element = c.timeAxisSubGridElement;
      e.thisObj = c;
      EventHelper.on(e);
      EventHelper.on({
        'element': c.timeAxisSubGridElement,
        'mouseleave': 'handleScheduleLeaveEvent',
        'capture': true,
        'thisObj': c
      });
      if (c.updateTimelineContextOnScroll && BrowserHelper.supportsPointerEventConstructor) {
        EventHelper.on({
          'element': document,
          'scroll': 'onScheduleScroll',
          'capture': true,
          'thisObj': c
        });
      }
    }
    getTimeSpanMouseEventParams(c, e) {
      throw new Error('Implement in subclass');
    }
    getScheduleMouseEventParams(c, e) {
      throw new Error('Implement in subclass');
    }
    handleScheduleEvent(c) {
      const e = this;
      if (e.ignoreDomEventsWhileScrolling && (e.scrolling || e.timeAxisSubGrid.scrolling)) {
        return;
      }
      const f = e.getTimelineEventContext(c);
      e.lastPointerEvent = c;
      if (f) {
        e.trigger('' + (f.eventElement ? e.scheduledEventName : 'schedule') + (eventNameMap[f.domEvent.type] || StringHelper.capitalize(f.domEvent.type)), f);
      }
      e.timelineContext = f;
    }
    handleScheduleLeaveEvent(c) {
      if (c.target === this.timeAxisSubGridElement) {
        this.handleScheduleEvent(c);
      }
    }
    onScheduleScroll({target: c}) {
      let e, f, g, i, j;
      const k = this;
      if (c && k.updateTimelineContextOnScroll && !((e = k.features.pan) == null ? void 0 : e.isActive) && !k.partners.some(l => {
          let n;
          return (n = l.features.pan) == null ? void 0 : n.isActive;
        }) && (c.contains(k.element) || k.bodyElement.contains(c))) {
        const {
          timelineContext: l,
          lastPointerEvent: n
        } = k;
        if (l) {
          const o = DomHelper.elementFromPoint(l.domEvent.clientX, l.domEvent.clientY), p = new BrowserHelper.PointerEventConstructor('pointermove', n), q = new MouseEvent('mousemove', n);
          Object.defineProperty(p, 'pointerId', { 'value': (j = (i = (f = GlobalEvents_default.currentPointerDown) == null ? void 0 : f.pointerId) != null ? i : (g = GlobalEvents_default.currentTouch) == null ? void 0 : g.identifier) != null ? j : 1 });
          p.scrollInitiated = q.scrollInitiated = true;
          o == null ? void 0 : o.dispatchEvent(p);
          o == null ? void 0 : o.dispatchEvent(q);
        }
      }
    }
    updateTimelineContext(c, e) {
      this.trigger('timelineContextChange', {
        'oldContext': e,
        'context': c
      });
      if (c && !e) {
        this.trigger('scheduleMouseEnter', c);
      } else if (!c) {
        this.trigger('scheduleMouseLeave', { 'event': e.event });
      }
    }
    getTimelineEventContext(c) {
      let e, f;
      const g = this, {scheduleContext: i} = g.features;
      if (i) {
        const {context: l} = i, {type: n} = c, o = n.endsWith('leave') || n.endsWith('out') ? c.relatedTarget : c.target;
        if (l && (((e = i.widget) == null ? void 0 : e.owns(o)) || i.element.contains(o))) {
          i.element.style.pointerEvents = 'none';
          l.eventElement = (f = document.elementFromPoint(c.clientX, c.clientY)) == null ? void 0 : f.closest(g.eventSelector);
          l.eventRecord = g.resolveEventRecord(l.eventElement);
          i.element.style.pointerEvents = '';
          g._timelineContext = null;
          return l;
        }
      }
      const j = c.target.closest(g.eventInnerSelector), k = g.getCellElementFromDomEvent(c);
      if (k) {
        const p = g.getDateFromDomEvent(c, 'floor');
        if (!p) {
          return;
        }
        const q = DomDataStore.get(k), r = j ? g.getTimeSpanMouseEventParams(j, c) : g.getScheduleMouseEventParams(q, c);
        if (!r) {
          return;
        }
        const u = g.isVertical ? g.resourceStore.indexOf(r.resourceRecord) : q.row.dataIndex, v = g.timeAxis.getTickFromDate(p), w = g.timeAxis.getAt(Math.floor(v));
        if (w) {
          return {
            'scheduler': g,
            'isTimelineContext': true,
            'domEvent': c,
            'eventElement': j,
            'cellElement': k,
            'index': u,
            'tick': w,
            'tickIndex': v,
            'date': p,
            'tickStartDate': w.startDate,
            'tickEndDate': w.endDate,
            'tickParentIndex': w.parentIndex,
            'row': q.row,
            'event': c,
            ...r
          };
        }
      }
    }
    getCellElementFromDomEvent({
      target: c,
      clientY: e,
      type: f
    }) {
      let g;
      const i = this, {
          isVertical: j,
          foregroundCanvas: k
        } = i, l = c.closest(i.eventSelector);
      if (l) {
        const n = !j && (i.resolveRowRecord(l) || i.store.getAt(i.rowManager.getRowAt(e, false).dataIndex));
        return i.getCell({
          [j ? 'row' : 'record']: j ? 0 : n,
          'column': i.timeAxisColumn
        });
      } else if (k.contains(c)) {
        if (c === k || c.closest('.b-sch-resourcetimerange') || f === 'mousemove') {
          return (g = i.rowManager.getRowAt(e, false)) == null ? void 0 : g.getCell(i.timeAxisColumn.id);
        }
      } else {
        return c.matches('.b-grid-row') ? c.firstElementChild : c.closest(i.timeCellSelector);
      }
    }
    matchScheduleCell(c) {
      return c.closest(this.timeCellSelector);
    }
    onElementMouseButtonEvent(c) {
      const e = c.target.closest('.b-sch-header-timeaxis-cell');
      if (e) {
        const f = this, g = e.parentElement.dataset.headerPosition, i = f.timeAxisViewModel.columnConfig[g], j = f.timeAxis.isFiltered ? i.findIndex(n => n.index == e.dataset.tickIndex) : e.dataset.tickIndex, k = i[j], l = f.features.contextMenu;
        if (!l || c.type !== l.triggerEvent) {
          this.trigger('timeAxisHeader' + StringHelper.capitalize(c.type), {
            'startDate': k.start,
            'endDate': k.end,
            'event': c
          });
        }
      }
    }
    onElementMouseDown(c) {
      this.onElementMouseButtonEvent(c);
      super.onElementMouseDown(c);
    }
    onElementClick(c) {
      this.onElementMouseButtonEvent(c);
      super.onElementClick(c);
    }
    onElementDblClick(c) {
      this.onElementMouseButtonEvent(c);
      super.onElementDblClick(c);
    }
    onElementContextMenu(c) {
      this.onElementMouseButtonEvent(c);
      super.onElementContextMenu(c);
    }
    onElementMouseOver(c) {
      let e;
      const f = this;
      if (f.ignoreDomEventsWhileScrolling && (f.scrolling || f.timeAxisSubGrid.scrolling)) {
        return;
      }
      super.onElementMouseOver(c);
      const {target: g} = c, {hoveredEvents: i} = f;
      if ((g.closest(f.eventInnerSelector) || g.matches('.b-sch-terminal-hover-area')) && !((e = f.features.eventDrag) == null ? void 0 : e.isDragging)) {
        const j = g.closest(f.eventSelector);
        if (!i.has(j) && !f.preventOverCls) {
          i.add(j);
          j.classList.add(f.overScheduledEventClass);
          const k = f.getTimeSpanMouseEventParams(j, c);
          if (k) {
            f.trigger(f.scheduledEventName + 'MouseEnter', k);
          }
        }
      } else if (i.size) {
        f.unhoverAll(c);
      }
    }
    onElementMouseOut(c) {
      let e;
      const f = this, {features: g} = f, {
          target: i,
          relatedTarget: j
        } = c;
      if (!(j == null ? void 0 : j.closest('.b-sch-canvas *'))) {
        super.onElementMouseOut(c);
      }
      const k = i.closest(f.eventSelector), l = k == null ? void 0 : k.querySelector(f.eventInnerSelector), n = k && f.resolveTimeSpanRecord(k);
      if (l && n && f.hoveredEvents.has(k) && !((e = g.eventDrag) == null ? void 0 : e.isDragging)) {
        if (j && (DomHelper.isDescendant(l, j) || j.matches('.b-sch-terminal-hover-area'))) {
          return;
        }
      }
      if (k && f.hoveredEvents.size) {
        f.unhover(k, c);
      }
    }
    unhover(c, e) {
      const f = this;
      c.classList.remove(f.overScheduledEventClass);
      const g = f.getTimeSpanMouseEventParams(c, e);
      if (g) {
        f.trigger(f.scheduledEventName + 'MouseLeave', f.getTimeSpanMouseEventParams(c, e));
      }
      f.hoveredEvents.delete(c);
    }
    unhoverAll(c) {
      for (const e of this.hoveredEvents) {
        !e.isReleased && !e.classList.contains('b-released') && this.unhover(e, c);
      }
      this.hoveredEvents.clear();
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'TimelineDomEvents'), __publicField(b, 'configurable', {
    'timelineContext': {
      '$config': {
        'equal'(c, e) {
          return (c == null ? void 0 : c.index) === (e == null ? void 0 : e.index) && (c == null ? void 0 : c.tickParentIndex) === (e == null ? void 0 : e.tickParentIndex) && !(((c == null ? void 0 : c.tickStartDate) || 0) - ((e == null ? void 0 : e.tickStartDate) || 0)) && (c == null ? void 0 : c.eventRecord) === (e == null ? void 0 : e.eventRecord);
        }
      }
    },
    'updateTimelineContextOnScroll': false,
    'ignoreDomEventsWhileScrolling': null
  }), __publicField(b, 'properties', {
    'schedulerEvents': {
      'pointermove': 'handleScheduleEvent',
      'mouseover': 'handleScheduleEvent',
      'mousedown': 'handleScheduleEvent',
      'mouseup': 'handleScheduleEvent',
      'click': 'handleScheduleEvent',
      'dblclick': 'handleScheduleEvent',
      'contextmenu': 'handleScheduleEvent',
      'mousemove': 'handleScheduleEvent',
      'mouseout': 'handleScheduleEvent'
    }
  }), __publicField(b, 'delayable', { 'onScheduleScroll': 'raf' }), b;
};
const TimelineViewPresets_default = a => class b extends (a || Base) {
  static get $name() {
    return 'TimelineViewPresets';
  }
  static get configurable() {
    return {
      'viewPreset': 'weekAndDayLetter',
      'presets': true,
      'displayDateFormat': null
    };
  }
  changePresets(c) {
    const e = { 'owner': this };
    let f = [];
    if (c === true) {
      f = pm.allRecords;
    } else if (Array.isArray(c)) {
      for (const i of c) {
        if (typeof i === 'string') {
          const j = pm.getById(i);
          if (j) {
            f.push(j);
          }
        } else {
          f.push(i);
        }
      }
    } else {
      ObjectHelper.assign(e, c);
    }
    const g = new PresetStore(e);
    g.add(f);
    return g;
  }
  changeViewPreset(c, e) {
    const f = this, {presets: g} = f;
    if (c) {
      c = g.createRecord(c);
      if (!g.includes(c)) {
        g.add(c);
      }
    } else {
      c = g.first;
    }
    const i = f.lastViewPresetOptions || {}, j = c.options || (c.options = {}), k = {
        ...j,
        'from': e,
        'to': c,
        'preset': c
      }, l = !f._viewPreset || !f._viewPreset.equals(c);
    delete i.event;
    if (l || !ObjectHelper.isEqual(j, i)) {
      j.event = k;
      if (!l) {
        f._viewPreset = null;
      }
      if (f.isConfiguring || f.trigger('beforePresetChange', k) !== false) {
        return c;
      }
    }
  }
  get displayDateFormat() {
    return this._displayDateFormat || this.viewPreset.displayDateFormat;
  }
  updateDisplayDateFormat(c) {
    this.trigger('displayDateFormatChange', { 'format': c });
  }
  getFormattedDate(c) {
    return DateHelper.format(c, this.displayDateFormat);
  }
  updateViewPreset(c) {
    let e;
    const f = this, {options: g} = c, {
        event: i,
        startDate: j,
        endDate: k
      } = g, {
        isHorizontal: l,
        _timeAxis: n,
        _timeAxisViewModel: o
      } = f, p = l && f.rtl;
    let {
        centerDate: q,
        zoomDate: r,
        zoomPosition: u
      } = g, v = false;
    (e = f.syncSplits) == null ? void 0 : e.call(f, w => w.viewPreset = c);
    delete c.options;
    f._viewPresetChanging = true;
    if (n && !f.isConfiguring) {
      const {timelineScroller: w} = f;
      f.lastViewPresetOptions = g;
      if (n.isConfigured) {
        f.suspendRefresh();
        const z = ObjectHelper.copyProperties({}, f, [
          'weekStartDay',
          'startTime',
          'endTime'
        ]);
        if (f.infiniteScroll) {
          Object.assign(z, f.calculateInfiniteScrollingDateRange(q || (j && k ? new Date((j.getTime() + k.getTime()) / 2) : f.viewportCenterDateCached), true, c));
        } else if (j) {
          z.startDate = j;
          z.endDate = k;
          if (!q && k) {
            q = new Date((j.getTime() + k.getTime()) / 2);
          }
        } else {
          z.startDate = n.startDate;
          z.endDate = k || n.endDate;
          if (!q) {
            q = f.viewportCenterDate;
          }
        }
        n.isConfigured = false;
        z.viewPreset = c;
        n.reconfigure(z, true);
        o.reconfigure({
          'viewPreset': c,
          'headers': c.headers,
          'columnLinesFor': c.columnLinesFor != null ? c.columnLinesFor : c.headers.length - 1,
          'tickSize': g.tickSize || (l ? c.tickWidth : c.tickHeight || c.tickWidth || 60)
        });
        if (g.tickSize) {
          f.tickSize = g.tickSize;
        }
        f.resumeRefresh(false);
      }
      f.refresh();
      if (!g.notScroll && f.isPainted) {
        if ('scrollPosition' in g) {
          v = w.position === g.scrollPosition;
          w.scrollTo(g.scrollPosition);
        } else if (g.visibleDate) {
          f.visibleDate = g.visibleDate;
        } else if (r && u) {
          const A = unitMagnitudes[n.resolutionUnit], B = A > 3 ? 'hour' : 'minute', C = DateHelper.asMilliseconds(B === 'minute' ? 15 : 1, B), E = new Date(Math.round(r / C) * C);
          i.zoomDate = r;
          i.zoomPosition = u;
          i.zoomLevel = g.zoomLevel;
          if (p) {
            w.position = w.scrollWidth - (f.getCoordinateFromDate(E) + u);
          } else {
            w.position = f.getCoordinateFromDate(E) - u;
          }
        } else if (q) {
          f.cachedCenterDate = q;
          i.centerDate = q;
          const F = f.timelineScroller.clientSize, G = p ? f.timeAxisViewModel.totalSize - f.getCoordinateFromDate(q, true) : f.getCoordinateFromDate(q, true), H = Math.max(G - F / 2, 0);
          if (H === (f.isHorizontal ? f.scrollLeft : f.scrollTop)) {
            v = true;
          } else if (f.isHorizontal) {
            f.scrollHorizontallyTo(H, { 'scrollingToCenter': true });
          } else {
            f.scrollVerticallyTo(H, { 'scrollingToCenter': true });
          }
        } else {
          if ((f.isHorizontal ? f.scrollLeft : f.scrollTop) === 0) {
            v = true;
          } else {
            f.timelineScroller.scrollTo(0);
          }
        }
      }
    }
    f.dataset.presetId = c.id;
    f.trigger('presetChange', i);
    f._viewPresetChanging = false;
    if (v) {
      if (f.isHorizontal) {
        f.currentOrientation.updateFromHorizontalScroll(f.scrollLeft, true);
      } else {
        f.currentOrientation.updateFromVerticalScroll(f.scrollTop);
      }
    }
  }
  doDestroy() {
    if (this._presets.owner === this) {
      this._presets.destroy();
    }
    super.doDestroy();
  }
  getCurrentConfig(c) {
    const e = super.getCurrentConfig(c);
    if (e.viewPreset && e.viewPreset.name && !e.viewPreset.base) {
      delete e.viewPreset.name;
    }
    return e;
  }
  get widgetClass() {
  }
};
const TimelineZoomable_default = a => {
  let b;
  return b = class extends (a || Base) {
    lastZoomPosition = -1;
    lastWheelTime = -1;
    constructor() {
      super(...arguments);
    }
    construct(c) {
      const e = this;
      super.construct(c);
      if (e.zoomOnMouseWheel) {
        EventHelper.on({
          'element': e.timeAxisSubGridElement,
          'wheel': 'onWheel',
          'throttled': {
            'buffer': 100,
            'alt': f => f.ctrlKey && f.preventDefault()
          },
          'thisObj': e,
          'capture': true,
          'passive': false
        });
      }
      if (e.zoomOnTimeAxisDoubleClick) {
        e.ion({
          'timeaxisheaderdblclick': ({
            startDate: f,
            endDate: g
          }) => {
            if (!e.forceFit) {
              e.zoomToSpan({
                'startDate': f,
                'endDate': g
              });
            }
          }
        });
      }
    }
    get maxZoomLevel() {
      return this._maxZoomLevel || this.presets.count - 1;
    }
    set maxZoomLevel(c) {
      if (typeof c !== 'number') {
        c = this.presets.count - 1;
      }
      if (c < 0 || c >= this.presets.count) {
        throw new Error('Invalid range for `maxZoomLevel`');
      }
      this._maxZoomLevel = c;
    }
    get minZoomLevel() {
      return this._minZoomLevel;
    }
    set minZoomLevel(c) {
      if (typeof c !== 'number') {
        c = 0;
      }
      if (c < 0 || c >= this.presets.count) {
        throw new Error('Invalid range for `minZoomLevel`');
      }
      this._minZoomLevel = c;
    }
    get zoomLevel() {
      return this.presets.indexOf(this.viewPreset);
    }
    set zoomLevel(c) {
      this.zoomToLevel(c);
    }
    getMilliSecondsPerPixelForZoomLevel(c, e) {
      const {bottomHeader: f} = c, g = this.isHorizontal ? c.tickWidth : c.tickHeight;
      return Math.round((DateHelper.add(new Date(1, 0, 1), f.increment || 1, f.unit) - new Date(1, 0, 1)) / (e ? g : c.actualWidth || g));
    }
    zoomTo(c) {
      const e = this;
      if (typeof c === 'object') {
        if (c.preset) {
          e.zoomToLevel(c.preset, c);
        } else if (c.level != null) {
          e.zoomToLevel(c.level, c);
        } else {
          e.zoomToSpan(c);
        }
      } else {
        e.zoomToLevel(c);
      }
    }
    zoomToLevel(c, e = {}) {
      if (this.forceFit) {
        console.warn('Warning: The forceFit setting and zooming cannot be combined');
        return;
      }
      if (typeof c === 'number') {
        c = Math.min(Math.max(c, this.minZoomLevel), this.maxZoomLevel);
      }
      const f = this, {presets: g} = f, i = f.isVertical ? 'tickHeight' : 'tickWidth', j = g.createRecord(c), k = j[i], l = e.startDate ? new Date(e.startDate) : null, n = e.endDate ? new Date(e.endDate) : null;
      g.add(j);
      let o = l && n ? {
        'startDate': l,
        'endDate': n
      } : null;
      const p = e.centerDate ? new Date(e.centerDate) : o ? new Date((l.getTime() + n.getTime()) / 2) : f.viewportCenterDateCached;
      let q = f.isVertical ? f.scrollable.clientHeight : f.timeAxisSubGrid.width;
      if (q === 0) {
        const {_beforeCollapseState: r} = f.timeAxisSubGrid;
        if (f.isHorizontal && f.timeAxisSubGrid.collapsed && (r == null ? void 0 : r.width)) {
          q = r.width;
        } else {
          return null;
        }
      }
      if (!o) {
        o = f.calculateOptimalDateRange(p, q, j);
      }
      if ('width' in e) {
        j.setData(i, e.width);
      }
      f.isZooming = true;
      j.options = {
        ...e,
        'startDate': o.startDate || f.startDate,
        'endDate': o.endDate || f.endDate,
        'centerDate': p
      };
      f.viewPreset = j;
      j.actualWidth = f.timeAxisViewModel.tickSize;
      f.isZooming = false;
      j.setData(i, k);
      return f.zoomLevel;
    }
    zoomToFit(c) {
      const e = this.eventStore, f = e.getTotalTimeSpan();
      c = {
        'leftMargin': 0,
        'rightMargin': 0,
        ...c,
        ...f
      };
      if (c.startDate && c.endDate) {
        if (c.endDate > c.startDate) {
          this.zoomToSpan(c);
        } else {
          this.scrollToDate(c.startDate);
        }
      }
    }
    zoomToSpan(c = {}) {
      if (c.leftMargin || c.rightMargin) {
        c.adjustStart = 0;
        c.adjustEnd = 0;
      }
      if (!c.leftMargin)
        c.leftMargin = 0;
      if (!c.rightMargin)
        c.rightMargin = 0;
      if (!c.startDate || !c.endDate)
        throw new Error('zoomToSpan: must provide startDate + endDate dates');
      const e = this, {timeAxis: f} = e, g = c.adjustStart >= 0 || c.adjustEnd >= 0;
      let {
        startDate: i,
        endDate: j
      } = c;
      if (g) {
        i = DateHelper.add(i, -c.adjustStart, f.mainUnit);
        j = DateHelper.add(j, c.adjustEnd, f.mainUnit);
      }
      if (i <= j) {
        const {availableSpace: k} = e.timeAxisViewModel, l = e.presets.allRecords, n = j - i || 1;
        let o = e.zoomLevel, p, q;
        if (o === -1)
          o = 0;
        let r = e.getMilliSecondsPerPixelForZoomLevel(l[o], true), u = o + (p = n / r + c.leftMargin + c.rightMargin > k ? -1 : 1), v, w = null;
        while (u >= 0 && u <= l.length - 1) {
          v = l[u];
          r = e.getMilliSecondsPerPixelForZoomLevel(v, true);
          const F = n / r + c.leftMargin + c.rightMargin;
          if (p === -1) {
            if (F <= k) {
              w = u;
              break;
            }
          } else {
            if (F <= k) {
              if (o !== u - p) {
                w = u;
              }
            } else {
              break;
            }
          }
          u += p;
        }
        w = w != null ? w : u - p;
        v = l[w];
        const z = v.bottomHeader.unit;
        r = e.getMilliSecondsPerPixelForZoomLevel(v, true);
        if (c.leftMargin || c.rightMargin) {
          i = new Date(i.getTime() - r * c.leftMargin);
          j = new Date(j.getTime() + r * c.rightMargin);
        }
        const A = DateHelper.getDurationInUnit(i, j, z, true) / v.bottomHeader.increment;
        if (A === 0) {
          return null;
        }
        const B = Math.floor(k / A), C = c.centerDate || new Date((i.getTime() + j.getTime()) / 2);
        if (g) {
          q = {
            'startDate': i,
            'endDate': j
          };
        } else {
          q = e.calculateOptimalDateRange(C, k, v);
        }
        let E = e.zoomLevel;
        if (e.zoomLevel === w) {
          f.reconfigure(q);
        } else {
          E = e.zoomToLevel(w, Object.assign(q, {
            'width': B,
            'centerDate': C
          }));
        }
        this.trigger('zoomToSpan', {
          'zoomLevel': w,
          'startDate': i,
          'endDate': j,
          'centerDate': C
        });
        return E;
      }
      return null;
    }
    zoomIn(c = 1, e) {
      if (typeof c === 'object') {
        e = c;
        c = 1;
      }
      const f = this.zoomLevel;
      if (f >= this.maxZoomLevel) {
        return null;
      }
      return this.zoomToLevel(f + c, e);
    }
    zoomOut(c = 1, e) {
      if (typeof c === 'object') {
        e = c;
        c = 1;
      }
      const f = this.zoomLevel;
      if (f <= this.minZoomLevel) {
        return null;
      }
      return this.zoomToLevel(f - c, e);
    }
    zoomInFull(c) {
      return this.zoomToLevel(this.maxZoomLevel, c);
    }
    zoomOutFull(c) {
      return this.zoomToLevel(this.minZoomLevel, c);
    }
    calculateOptimalDateRange(c, e, f) {
      const g = this, {timeAxis: i} = g, {bottomHeader: j} = f, k = g.isHorizontal ? f.tickWidth : f.tickHeight;
      if (g.zoomKeepsOriginalTimespan) {
        return {
          'startDate': i.startDate,
          'endDate': i.endDate
        };
      }
      const l = j.unit, n = Math.ceil(e / k * j.increment * g.visibleZoomFactor / 2), o = DateHelper.add(c, -n, l), p = DateHelper.add(c, n, l);
      return {
        'startDate': i.floorDate(o, false, l, j.increment),
        'endDate': i.ceilDate(p, false, l, j.increment)
      };
    }
    onElementMouseMove(c) {
      const {
        isHorizontal: e,
        zoomContext: f
      } = this;
      super.onElementMouseMove(c);
      if (c.isTrusted && f) {
        if (Math.abs(c['client' + (e ? 'X' : 'Y')] - f.coordinate) > 10) {
          this.zoomContext = null;
        }
      }
    }
    async onWheel(c) {
      if ((c.ctrlKey || c.metaKey) && !this.forceFit) {
        c.preventDefault();
        const e = this, {
            zoomContext: f,
            isHorizontal: g,
            timelineScroller: i,
            zoomLevel: j
          } = e, k = performance.now(), l = c['client' + (g ? 'X' : 'Y')];
        let n = l - i.viewport['' + (g ? 'x' : 'y')];
        if (g && e.rtl) {
          n = i.viewport.width + i.viewport.x - l;
        }
        if (k - e.lastWheelTime > 200 || !f || Math.abs(l - e.zoomContext.coordinate) > 20) {
          e.zoomContext = {
            'zoomLevel': j,
            'coordinate': l,
            'zoomPosition': n,
            'zoomDate': e.getDateFromDomEvent(c)
          };
        } else {
          if (j > f.zoomLevel) {
            f.zoomDate = e.getDateFromDomEvent(c);
            f.zoomLevel = j;
          }
          f.zoomPosition = n;
        }
        e.lastWheelTime = k;
        e['zoom' + (c.deltaY > 0 ? 'Out' : 'In')](void 0, e.zoomContext);
      }
    }
    setTimeSpan(c, e, f) {
      this.timeAxis.setTimeSpan(c, e, f);
    }
    shift(c, e) {
      this.timeAxis.shift(c, e);
    }
    shiftNext(c) {
      this.timeAxis.shiftNext(c);
    }
    shiftPrevious(c) {
      this.timeAxis.shiftPrevious(c);
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'TimelineZoomable'), __publicField(b, 'defaultConfig', {
    'zoomOnMouseWheel': true,
    'zoomOnTimeAxisDoubleClick': true,
    'minZoomLevel': 0,
    'maxZoomLevel': null,
    'visibleZoomFactor': 5,
    'zoomKeepsOriginalTimespan': null
  }), b;
};
const getAncestorWidget = a => {
  let b;
  return (b = a.enableRecurringEvents) == null ? void 0 : b.defaultAction;
};
const _RecurrenceConfirmationPopup = class _RecurrenceConfirmationPopup extends Popup {
  static configurable = {
    'localizableProperties': [],
    'align': 'b-t',
    'autoShow': false,
    'autoClose': false,
    'closeAction': 'onRecurrenceClose',
    'modal': true,
    'centered': true,
    'scrollAction': 'realign',
    'constrainTo': globalThis,
    'draggable': true,
    'closable': true,
    'floating': true,
    'eventRecord': null,
    'cls': 'b-sch-recurrenceconfirmation',
    'bbar': {
      'defaults': { 'localeClass': _RecurrenceConfirmationPopup },
      'items': {
        'changeSingleButton': {
          'weight': 100,
          'cls': 'b-raised',
          'color': 'b-blue',
          'text': 'L{update-only-this-btn-text}',
          'onClick': 'up.onChangeSingleButtonClick'
        },
        'changeMultipleButton': {
          'weight': 200,
          'color': 'b-green',
          'text': 'L{Object.Yes}',
          'onClick': 'up.onChangeMultipleButtonClick'
        },
        'cancelButton': {
          'weight': 300,
          'color': 'b-gray',
          'text': 'L{Object.Cancel}',
          'onClick': 'up.onCancelButtonClick'
        }
      }
    }
  };
  static type = 'recurrenceconfirmation';
  static $name = 'RecurrenceConfirmationPopup';
  get changeMultipleButton() {
    return this.widgetMap.changeMultipleButton;
  }
  get changeSingleButton() {
    return this.widgetMap.changeSingleButton;
  }
  get cancelButton() {
    return this.widgetMap.cancelButton;
  }
  onChangeMultipleButtonClick() {
    this.processMultipleRecords();
    this.hide();
  }
  onChangeSingleButtonClick() {
    this.processSingleRecord();
    this.hide();
  }
  onCancelButtonClick() {
    this.cancelFn && this.cancelFn.call(this.thisObj);
    this.hide();
  }
  onRecurrenceClose() {
    if (this.cancelFn) {
      this.cancelFn.call(this.thisObj);
    }
    this.hide();
  }
  confirm(a = {}) {
    let b;
    const c = this, e = c.closest(getAncestorWidget), f = (b = e == null ? void 0 : e.enableRecurringEvents) == null ? void 0 : b.defaultAction;
    [
      'actionType',
      'eventRecord',
      'title',
      'html',
      'changerFn',
      'cancelFn',
      'finalizerFn',
      'thisObj'
    ].forEach(g => {
      if (g in a)
        c[g] = a[g];
    });
    if (f === 'single') {
      c.processSingleRecord();
    } else if (f === 'future') {
      c.processMultipleRecords();
    } else {
      c.updatePopupContent();
      return super.show(a);
    }
  }
  updatePopupContent() {
    const a = this, {
        changeMultipleButton: b,
        changeSingleButton: c,
        cancelButton: e
      } = a.widgetMap, {
        eventRecord: f,
        actionType: actionType = 'update'
      } = a, g = f == null ? void 0 : f.isRecurring;
    if (g) {
      b.text = a.L('L{Object.Yes}');
      a.html = a.L(actionType + '-all-message');
    } else {
      b.text = a.L(actionType + '-further-btn-text');
      a.html = a.L(actionType + '-further-message');
    }
    c.text = a.L(actionType + '-only-this-btn-text');
    e.text = a.L('L{Object.Cancel}');
    a.width = a.L('L{width}');
    a.title = a.L(actionType + '-title');
  }
  processMultipleRecords() {
    const {
      eventRecord: a,
      changerFn: b,
      thisObj: c,
      finalizerFn: e
    } = this;
    a.beginBatch();
    b && this.callback(b, c, [a]);
    a.endBatch();
    e && this.callback(e, c, [a]);
  }
  processSingleRecord() {
    const {
      eventRecord: a,
      changerFn: b,
      thisObj: c,
      finalizerFn: e
    } = this;
    a.beginBatch();
    let f;
    if (a == null ? void 0 : a.isRecurring) {
      a.recurrence.forEachOccurrence(a.startDate, null, (g, i, j) => {
        if (j > 1) {
          f = g;
          return false;
        }
      });
    }
    f == null ? void 0 : f.convertToRealEvent();
    a.recurrence = null;
    b && this.callback(b, c, [a]);
    a.recurrenceRule = null;
    a.endBatch();
    e && this.callback(e, c, [a]);
  }
  updateLocalization() {
    this.updatePopupContent();
    super.updateLocalization();
  }
};
const RecurrenceConfirmationPopup = _RecurrenceConfirmationPopup;
RecurrenceConfirmationPopup.initClass();
RecurrenceConfirmationPopup._$name = 'RecurrenceConfirmationPopup';
const RecurringEvents_default = a => {
  let b;
  return b = class extends (a || Base) {
    construct(c) {
      super.construct(c);
      this.ion({
        'beforeEventDropFinalize': 'onRecurrableBeforeEventDropFinalize',
        'beforeEventResizeFinalize': 'onRecurrableBeforeEventResizeFinalize',
        'beforeAssignmentDelete': 'onRecurrableAssignmentBeforeDelete'
      });
    }
    changeRecurrenceConfirmationPopup(c, e) {
      const f = this.constructor.reconfigure(e, c, 'recurrenceconfirmation');
      f.owner = this;
      return f;
    }
    findRecurringEventToConfirmDelete(c) {
      return c.find(e => e.supportsRecurring && (e.isRecurring || e.isOccurrence));
    }
    onRecurrableAssignmentBeforeDelete({
      assignmentRecords: c,
      context: e
    }) {
      const f = c.map(i => i.event), g = this.findRecurringEventToConfirmDelete(f);
      if (this.enableRecurringEvents && g) {
        this.recurrenceConfirmationPopup.confirm({
          'actionType': 'delete',
          'eventRecord': g,
          'changerFn'() {
            e.finalize(true);
          },
          'cancelFn'() {
            e.finalize(false);
          }
        });
        return false;
      }
    }
    onRecurrableBeforeEventDropFinalize({context: c}) {
      if (this.enableRecurringEvents) {
        const {eventRecords: e} = c, f = e.filter(g => g.supportsRecurring && (g.isRecurring || g.isOccurrence));
        if (f.length) {
          c.async = true;
          this.recurrenceConfirmationPopup.confirm({
            'actionType': 'update',
            'eventRecord': f[0],
            'changerFn'() {
              c.finalize(true);
            },
            'cancelFn'() {
              c.finalize(false);
            }
          });
        }
      }
    }
    onRecurrableBeforeEventResizeFinalize({context: c}) {
      if (this.enableRecurringEvents) {
        const {eventRecord: e} = c, f = e.supportsRecurring && (e.isRecurring || e.isOccurrence);
        if (f) {
          c.async = true;
          this.recurrenceConfirmationPopup.confirm({
            'actionType': 'update',
            'eventRecord': e,
            'changerFn'() {
              c.finalize(true);
            },
            'cancelFn'() {
              c.finalize(false);
            }
          });
        }
      }
    }
    onAssignmentChange({
      action: c,
      records: e,
      changes: f
    }) {
      let g;
      if (c === 'update' && f.event && f.resource && Object.keys(f).length === 2) {
        return;
      }
      if (c !== 'dataset' && Array.isArray(e)) {
        for (const i of e) {
          if (((g = i.event) == null ? void 0 : g.isRecurring) && !i.event.isBatchUpdating) {
            i.event.removeOccurrences(this.eventStore);
          }
        }
      }
    }
    getOccurrencesFor(c) {
      return this.eventStore.getOccurrencesForTimeSpan(c, this.timeAxis.startDate, this.timeAxis.endDate);
    }
    async removeEvents(c, e = null, f = this) {
      const g = this;
      if (!g.readOnly && c.length) {
        const i = {
          'finalize'(k = true) {
            if (e) {
              e(k);
            }
            if (k !== false) {
              if (c.some(l => {
                  let n;
                  return l.isOccurrence || ((n = l.event) == null ? void 0 : n.isOccurrence);
                })) {
                c.forEach(l => l.isOccurrenceAssignment ? l.event.remove() : l.remove());
              } else {
                const l = c[0].isAssignment ? g.assignmentStore : g.eventStore;
                l.remove(c);
              }
            }
          }
        };
        let j;
        if (c[0].isAssignment) {
          j = g.trigger('beforeAssignmentDelete', {
            'assignmentRecords': c,
            'context': i
          });
        } else {
          j = await g.trigger('beforeEventDelete', {
            'eventRecords': c,
            'context': i
          });
        }
        if (j !== false) {
          const k = c.find(l => l.isRecurring || l.isOccurrence);
          if (k) {
            g.recurrenceConfirmationPopup.owner = f;
            g.recurrenceConfirmationPopup.confirm({
              'actionType': 'delete',
              'eventRecord': k,
              'changerFn'() {
                i.finalize(true);
              },
              'cancelFn'() {
                i.finalize(false);
              }
            });
          } else {
            i.finalize(true);
          }
          return true;
        }
      }
      return false;
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'RecurringEvents'), __publicField(b, 'configurable', {
    'enableRecurringEvents': false,
    'recurrenceConfirmationPopup': {
      '$config': ['lazy'],
      'value': { 'type': 'recurrenceconfirmation' }
    }
  }), b;
};
const TimelineEventRendering_default = a => {
  let b;
  return b = class extends (a || Base) {
    static get $name() {
      return 'TimelineEventRendering';
    }
    static get defaultConfig() {
      return {
        'managedEventSizing': true,
        'generatedIdCls': 'b-sch-dirty-new',
        'dirtyCls': 'b-sch-dirty',
        'committingCls': 'b-sch-committing',
        'endsOutsideViewCls': 'b-sch-event-endsoutside',
        'startsOutsideViewCls': 'b-sch-event-startsoutside',
        'fixedEventCls': 'b-sch-event-fixed'
      };
    }
    updateFillTicks(c) {
      const e = this;
      if (!e.isConfiguring) {
        const {timeAxis: f} = e;
        f.forceFullTicks = c && e.snap;
        if (c) {
          e.timeResolution = {
            'unit': f.mainUnit,
            'increment': 1
          };
        }
        e.refreshWithTransition();
        e.trigger('stateChange');
      }
    }
    changeBarMargin(c) {
      ObjectHelper.assertNumber(c, 'barMargin');
      if (this._resourceMargin == null) {
        this.changeResourceMargin(c);
      }
      if (this.isHorizontal && this.rowHeight) {
        return Math.min(Math.ceil(this.rowHeight / 2), c);
      }
      return c;
    }
    updateBarMargin() {
      if (this.rendered) {
        this.currentOrientation.onBeforeRowHeightChange();
        this.refreshWithTransition();
        this.trigger('stateChange');
      }
    }
    get resourceMargin() {
      return this._resourceMargin == null ? this.barMargin : this._resourceMargin;
    }
    changeResourceMargin(c) {
      const e = this;
      if (typeof c === 'number') {
        if (e.isHorizontal && e.rowHeight) {
          c = Math.min(Math.ceil(e.rowHeight / 2), c);
        }
        e.resourceMarginObject = {
          'start': c,
          'end': c,
          'total': c * 2
        };
        return c;
      }
      if (!(c == null ? void 0 : c.start)) {
        c.start = 0;
      }
      if (!(c == null ? void 0 : c.end)) {
        c.end = 0;
      }
      if (e.isHorizontal && e.rowHeight) {
        c.start = e.rowHeight < c.start + c.end ? Math.ceil(e.rowHeight / 2) : c.start;
        c.end = e.rowHeight < c.start + c.end ? Math.ceil(e.rowHeight / 2) : c.end;
      }
      e.resourceMarginObject = ObjectHelper.assign({ 'total': c.start + c.end }, c);
      return c;
    }
    updateResourceMargin() {
      const c = this;
      if (c.rendered) {
        c.currentOrientation.onBeforeRowHeightChange();
        c.refreshWithTransition();
      }
    }
    changeTickSize(c) {
      ObjectHelper.assertNumber(c, 'tickSize');
      return c;
    }
    updateTickSize(c) {
      this.timeAxisViewModel.tickSize = c;
    }
    get tickSize() {
      return this.timeAxisViewModel.tickSize;
    }
    static get eventColors() {
      return [
        'red',
        'pink',
        'purple',
        'magenta',
        'violet',
        'indigo',
        'blue',
        'cyan',
        'teal',
        'green',
        'gantt-green',
        'lime',
        'yellow',
        'orange',
        'deep-orange',
        'gray',
        'light-gray'
      ];
    }
    static get eventStyles() {
      return [
        'plain',
        'border',
        'hollow',
        'colored',
        'line',
        'dashed',
        'minimal',
        'rounded'
      ];
    }
    updateEventStyle(c) {
      if (!this.isConfiguring) {
        this.refreshWithTransition();
        this.trigger('stateChange');
      }
    }
    updateEventColor(c) {
      if (!this.isConfiguring) {
        this.refreshWithTransition();
        this.trigger('stateChange');
      }
    }
    get widgetClass() {
    }
  }, __publicField(b, 'configurable', {
    'barMargin': 10,
    'fillTicks': false,
    'resourceMargin': null,
    'eventColor': 'green',
    'eventStyle': 'plain',
    'tickSize': null
  }), b;
};
const MIN_DATE = new Date(-8640000000000000);
const MAX_DATE = new Date(8640000000000000);
const TimelineEarlyPositioning_default = a => class b extends (a || Base) {
  static get $name() {
    return 'TimelineEarlyPositioning';
  }
  applyViewStartEndDates(c, e) {
    const f = this, {
        visibleDate: visibleDate = {}
      } = f, g = visibleDate == null ? void 0 : visibleDate.date, i = g ? DateHelper.min(c, g) : c != null ? c : new Date(), j = g ? e ? DateHelper.max(e, g) : DateHelper.add(i, f.visibleDateRange.endDate - f.visibleDateRange.startDate) : e;
    if (i && j) {
      f.setTimeSpan(i, j, {
        ...visibleDate,
        'visibleDate': g
      });
      return true;
    } else if (i) {
      f.startDate = i;
      return true;
    }
    return false;
  }
  onEventStoreDataRefresh(c) {
    if (this._initialStartDateFilled === 'pending') {
      this.tryToApplyPreCommitStartEndDates(c.source);
    }
  }
  tryToApplyPreCommitStartEndDates(c) {
    let e, f;
    const g = this, i = g.project, j = (e = i.get('startDate')) != null ? e : !i.replica ? i.startDate : null;
    if (j) {
      g.applyViewStartEndDates(j, (f = i.get('endDate')) != null ? f : !i.replica ? i.endDate : null);
      g._initialStartDateFilled = 'applied';
    } else if (c.count > 0) {
      let k = MAX_DATE, l = MIN_DATE;
      c.forEach(n => {
        const o = n.get('startDate'), p = n.get('endDate');
        if (o && o < k) {
          k = o;
        }
        if (p && p > l) {
          l = p;
        }
      });
      g.applyViewStartEndDates(k.getTime() !== MAX_DATE.getTime() ? k : null, l.getTime() !== MIN_DATE.getTime() ? l : null);
      g._initialStartDateFilled = 'applied';
    }
  }
  bindEventStoreForEarlyPositioning(c) {
    let e;
    const f = this, g = f.initialConfig;
    if (f._initialStartDateFilled === void 0 && !g.startDate && !g.date && !((e = g.timeAxis) == null ? void 0 : e.startDate) && !g.visibleDate && !g.partner) {
      f._initialStartDateFilled = 'pending';
      f.tryToApplyPreCommitStartEndDates(c);
    }
    f.detachListeners('eventStoreDataRefresh');
    c.ion({
      'name': 'eventStoreDataRefresh',
      'refreshPreCommit': 'onEventStoreDataRefresh',
      'once': true,
      'thisObj': f
    });
  }
  get widgetClass() {
  }
};
const maintainVisibleStart = { 'maintainVisibleStart': true };
const defaultScrollOptions4 = { 'block': 'nearest' };
const TimelineScroll_default = a => {
  let b;
  return b = class extends (a || Base).mixin(TimelineZoomable_default) {
    onZoomToSpan({startDate: c}) {
      if (this.infiniteScroll) {
        this.scrollToDate(c, { 'block': 'start' });
      }
    }
    calculateOptimalDateRange(c, e, f) {
      if (this.infiniteScroll) {
        if (this.zoomKeepsOriginalTimespan) {
          const {
            startDate: g,
            endDate: i
          } = this.timeAxis;
          return {
            'startDate': g,
            'endDate': i
          };
        }
        return this.calculateInfiniteScrollingDateRange(c, true);
      }
      return super.calculateOptimalDateRange(...arguments);
    }
    initScroll() {
      let c;
      const e = this, {
          isHorizontal: f,
          visibleDate: g
        } = e;
      super.initScroll();
      const {scrollable: i} = f ? e.timeAxisSubGrid : e;
      i.ion({
        'scroll': 'onTimelineScroll',
        'thisObj': e
      });
      if (e.infiniteScroll) {
        if ((c = e.partneredWith) == null ? void 0 : c.values.some(j => j.infiniteScroll)) {
          const j = i.element.getBoundingClientRect()[e.isHorizontal ? 'width' : 'height'];
          e.timeAxisViewModel.update(j, false, true);
        } else {
          const k = g ? {
              ...g,
              'visibleDate': g.date
            } : {
              'visibleDate': e.viewportCenterDate,
              'block': 'center'
            }, {
              startDate: l,
              endDate: n
            } = e.calculateInfiniteScrollingDateRange(k.visibleDate, k.block === 'center');
          e.setTimeSpan(l, n, k);
        }
      }
    }
    get timelineScroller() {
      const c = this;
      if (!c.scrollInitialized) {
        c.initScroll();
      }
      return c._timelineScroller || (c._timelineScroller = new TimelineScroller({
        'widget': c,
        'scrollable': c.isHorizontal ? c.timeAxisSubGrid.scrollable : c.scrollable,
        'isHorizontal': c.isHorizontal
      }));
    }
    calculateInfiniteScrollingDateRange(c, e, f = this.timeAxisViewModel.viewPreset) {
      const {
          timeAxis: g,
          availableSpace: i
        } = this.timeAxisViewModel, j = Math.ceil(g.totalCount / g.count), {bufferCoef: k} = this, {
          leafUnit: l,
          leafIncrement: n,
          topUnit: o,
          topIncrement: p,
          tickSize: q
        } = f, r = l === o && Math.round(p) === p && Math.round(n) === n, u = r ? p : n, v = r ? o : l;
      if (e) {
        const w = Math.ceil((i * k + i / 2) / q) * j;
        return {
          'startDate': g.floorDate(DateHelper.add(c, -w * n, l), false, v, u),
          'endDate': g.ceilDate(DateHelper.add(c, w * n, l), false, v, u)
        };
      } else {
        const z = Math.ceil(i * k / q) * j;
        return {
          'startDate': g.floorDate(DateHelper.add(c, -z * n, l), false, v, u),
          'endDate': g.ceilDate(DateHelper.add(c, Math.ceil((i / q + z) * n), l), false, v, u)
        };
      }
    }
    doDestroy() {
      let c;
      (c = this._timelineScroller) == null ? void 0 : c.destroy();
      super.doDestroy();
    }
    onTimelineScroll() {
      if (this.infiniteScroll) {
        this.checkTimeAxisScroll();
      }
    }
    checkTimeAxisScroll() {
      const c = this, e = c.timelineScroller, {
          position: f,
          clientSize: g
        } = e, i = g * c.bufferCoef, j = i * c.bufferThreshold, k = e.maxPosition, {style: l} = c.timeAxisSubGrid.virtualScrollerElement, n = f < j, o = k - f < j;
      if (n || o) {
        l.overflow = 'hidden';
        l.pointerEvents = 'none';
        l.paddingBottom = DomHelper.scrollBarWidth + 'px';
        c.setTimeout(() => {
          l.overflow = '';
          l.paddingBottom = '';
          l.pointerEvents = '';
        }, 100);
        let p;
        if (n && o) {
          p = e.position + g / 2;
        } else {
          p = e.position;
        }
        c.shiftToDate(c.getDateFromCoordinate(p, 'floor', true, false, true), n && o);
      }
    }
    shiftToDate(c, e) {
      const f = this.calculateInfiniteScrollingDateRange(c, e);
      this.setTimeSpan(f.startDate, f.endDate, e ? e === true ? {
        'visibleDate': this.viewportCenterDate,
        'block': 'center'
      } : e : maintainVisibleStart);
    }
    updateInfiniteScroll(c) {
      if (!this.isConfiguring && c) {
        this.checkTimeAxisScroll();
      }
    }
    async scrollToDate(c, e = {}) {
      const f = this, {
          timeAxis: g,
          visibleDateRange: i,
          infiniteScroll: j
        } = f, {
          unit: k,
          increment: l
        } = g, n = e.edgeOffset || 0, o = DateHelper.ceil(i.endDate, l + ' ' + k) - DateHelper.floor(i.startDate, l + ' ' + k), p = c > f.viewportCenterDate ? 1 : -1, q = (j ? o * f.bufferCoef * f.bufferThreshold : e.block === 'center' ? o / 2 : n ? f.getMilliSecondsPerPixelForZoomLevel(f.viewPreset) * n : 0) * p, r = new Date(c.getTime() + q), u = r > g.endDate ? 1 : r < g.startDate ? -1 : 0;
      if (u && f.infiniteScroll) {
        await f.nextAnimationFrame();
        f.shiftToDate(new Date(c - q), null, true);
        await f.nextAnimationFrame();
      }
      const v = f.timelineScroller.viewport, w = f.getCoordinateFromDate(c, true), z = Math.min(f.timeAxisViewModel.tickSize, f.timeAxisViewModel.availableSpace), A = f.isHorizontal ? new Rectangle(f.getCoordinateFromDate(c, false) - (f.rtl ? z : 0), v.y, z, v.height) : new Rectangle(v.x, f.getCoordinateFromDate(c, false), v.width, f.timeAxisViewModel.tickSize);
      await f.scrollToCoordinate(w, A, c, e);
    }
    scrollToNow(c = {}) {
      return this.scrollToDate(new Date(), c);
    }
    async scrollToCoordinate(c, e, f, g = {}) {
      const i = this, {
          startDate: j,
          endDate: k
        } = i, {
          isContinuous: l,
          unit: n,
          increment: o
        } = i.timeAxis;
      if (c < 0 || l && (f < j || f > k)) {
        const p = k - j, q = DateHelper.floor(new Date(f.getTime() - p / 2), o + ' ' + n), r = DateHelper.add(q, p);
        if (q - j !== 0 && r - k !== 0) {
          await i.setTimeSpan(q, r);
          return i.scrollToDate(f, g);
        }
        return;
      }
      await i.timelineScroller.scrollIntoView(e, g);
      return !i.isDestroyed && i.nextAnimationFrame();
    }
    set scrollLeft(c) {
      this.timeAxisSubGrid.scrollable.element.scrollLeft = c;
    }
    get scrollLeft() {
      return this.timeAxisSubGrid.scrollable.element.scrollLeft;
    }
    set scrollX(c) {
      this.timeAxisSubGrid.scrollable.x = c;
    }
    get scrollX() {
      return this.timeAxisSubGrid.scrollable.x;
    }
    set scrollTop(c) {
      this.scrollable.y = c;
    }
    get scrollTop() {
      return this.scrollable.y;
    }
    scrollHorizontallyTo(c, e = true) {
      return this.timeAxisSubGrid.scrollable.scrollTo(c, null, e);
    }
    scrollVerticallyTo(c, e = true) {
      return this.scrollable.scrollTo(null, c, e);
    }
    scrollTo(c, e = true) {
      return this.timeAxisSubGrid.scrollable.scrollTo(c, null, e);
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'TimelineScroll'), __publicField(b, 'configurable', {
    'bufferCoef': 5,
    'bufferThreshold': 0.2,
    'infiniteScroll': false
  }), b;
};
var TimelineScroller = class extends Scroller {
  static get configurable() {
    return {
      'position': null,
      'x': null,
      'y': null
    };
  }
  updateOverflowX() {
  }
  updateOverflowY() {
  }
  onScroll(a) {
    super.onScroll(a);
    this._position = null;
  }
  syncPartners(a) {
    this.scrollable.syncPartners(a);
  }
  updatePosition(a) {
    this.scrollable[this.isHorizontal ? 'x' : 'y'] = a;
  }
  get viewport() {
    return this.scrollable.viewport;
  }
  get position() {
    return this._position = this.scrollable[this.isHorizontal ? 'x' : 'y'];
  }
  get clientSize() {
    return this.scrollable['client' + (this.isHorizontal ? 'Width' : 'Height')];
  }
  get scrollSize() {
    return this.scrollable['scroll' + (this.isHorizontal ? 'Width' : 'Height')];
  }
  get maxPosition() {
    return this.scrollable['max' + (this.isHorizontal ? 'X' : 'Y')];
  }
  scrollTo(a, b) {
    return this.isHorizontal ? this.scrollable.scrollTo(a, null, b) : this.scrollable.scrollTo(null, a, b);
  }
  scrollBy(a = 0, b = 0, c = defaultScrollOptions4) {
    return this.isHorizontal ? this.scrollable.scrollBy(a || b, 0, c) : this.scrollable.scrollBy(0, b || a, c);
  }
  scrollIntoView() {
    return this.scrollable.scrollIntoView(...arguments);
  }
  changeX(a) {
    this.position = a;
  }
  changeY(a) {
    this.position = a;
  }
  get x() {
    return this.position;
  }
  set x(a) {
    this.scrollable[this.isHorizontal ? 'x' : 'y'] = a;
  }
  get y() {
    return this.position;
  }
  set y(a) {
    this.scroller[this.isHorizontal ? 'x' : 'y'] = a;
  }
  get clientWidth() {
    return this.clientSize;
  }
  get clientHeight() {
    return this.clientSize;
  }
  get scrollWidth() {
    return this.scrollSize;
  }
  get scrollHeight() {
    return this.scrollSize;
  }
  get maxX() {
    return this.maxPosition;
  }
  get maxY() {
    return this.maxPosition;
  }
};
const copyProperties = ['barMargin'];
const TimelineState_default = a => class b extends (a || Base) {
  static get $name() {
    return 'TimelineState';
  }
  getState() {
    const c = this, e = ObjectHelper.copyProperties(super.getState(), c, copyProperties);
    e.zoomLevel = c.zoomLevel;
    e.zoomLevelOptions = {
      'startDate': c.startDate,
      'endDate': c.endDate,
      'centerDate': !c.infiniteScroll || c.timeAxisViewModel.availableSpace ? c.viewportCenterDate : void 0,
      'width': c.tickSize
    };
    return e;
  }
  applyState(c) {
    let e;
    const f = this;
    c = Object.assign({}, c);
    f.suspendRefresh();
    if (c.zoomLevel != null) {
      delete c.startDate;
      delete c.endDate;
      f.timeAxis.suspendEvents();
    }
    const {
      zoomLevel: g,
      zoomLevelOptions: i
    } = c;
    delete c.zoomLevel;
    delete c.zoomLevelOptions;
    super.applyState(c);
    if (g != null) {
      f.timeAxis.resumeEvents();
      if (f.infiniteScroll && ((e = c == null ? void 0 : c.scroll) == null ? void 0 : e.scrollLeft)) {
        c.scroll.scrollLeft = {};
      }
      f.whenVisible(() => f.zoomToLevel(g, i));
    }
    f.resumeRefresh(true, false);
  }
  get widgetClass() {
  }
};
const Header2 = class extends Header {
  static get $name() {
    return 'SchedulerHeader';
  }
  refreshContent() {
    let a;
    if (!((a = this.headersElement) == null ? void 0 : a.querySelector('.b-sch-timeaxiscolumn'))) {
      super.refreshContent();
    }
  }
};
Header2._$name = 'Header';
const TimeAxisSubGrid = class extends SubGrid {
  static get $name() {
    return 'TimeAxisSubGrid';
  }
  static get type() {
    return 'timeaxissubgrid';
  }
  static get configurable() {
    return {
      'sealedColumns': true,
      'headerClass': Header2
    };
  }
  startConfigure(a) {
    const {grid: b} = a;
    b.timeAxisSubGrid = this;
    super.startConfigure(a);
    if (b.isHorizontal) {
      a.header = { 'cls': { 'b-sticky-headers': b.stickyHeaders } };
    }
    delete a.headerClass;
    if (!('flex' in a || 'width' in a)) {
      a.flex = 1;
    }
  }
  changeScrollable() {
    const a = this, b = super.changeScrollable(...arguments);
    if (b) {
      Object.defineProperty(b, 'scrollWidth', {
        'get'() {
          let c, e;
          return (e = (c = this.element) == null ? void 0 : c.scrollWidth) != null ? e : 0;
        },
        'set'() {
          a.positionDirty = true;
          a.grid.updateCanvasSize();
        }
      });
    }
    return b;
  }
  handleHorizontalScroll(a = true) {
    if (!this.grid._viewPresetChanging) {
      super.handleHorizontalScroll(a);
    }
  }
  onInternalResize(a, b, c, e, f) {
    const g = this;
    super.onInternalResize(...arguments);
    if (g.isPainted && b !== e) {
      const i = g.grid, j = i._bodyRectangle.height, k = g.monitorResize && DomHelper.scrollBarWidth && b < e;
      if (k) {
        g.monitorResize = false;
      }
      i.onSchedulerViewportResize(b, j, e, j);
      if (k) {
        g.queueMicrotask(() => g.monitorResize = true);
      }
    }
  }
  get headerScrollWidth() {
    return this.grid.isVertical ? super.headerScrollWidth : this.grid.timeAxisViewModel.totalSize;
  }
  clearWidthCache() {
    super.clearWidthCache();
    if (this.owner.isHorizontal) {
      this.owner.updateViewModelAvailableSpace(this.width);
    }
  }
  async expand(a = false, b = false) {
    const {owner: c} = this;
    await super.expand(a, b);
    if (c.isPainted) {
      c.timeAxisViewModel.update(this.width, false, true);
    }
  }
};
TimeAxisSubGrid.initClass();
TimeAxisSubGrid._$name = 'TimeAxisSubGrid';
const inRange = (a, b, c) => b == null ? c == null || a < c : c == null ? a >= b : b < c ? b <= a && a < c : a < c || b <= a;
const isWorkingTime = (a, b) => inRange(a.getDay(), b.fromDay, b.toDay) && inRange(a.getHours(), b.fromHour, b.toHour);
const emptyObject11 = {};
const TimelineBase = class extends GridBase.mixin(TimelineDateMapper_default, TimelineDomEvents_default, TimelineEventRendering_default, TimelineEarlyPositioning_default, TimelineZoomable_default, TimelineScroll_default, TimelineState_default, TimelineViewPresets_default, RecurringEvents_default) {
  static configurable = {
    'animateTreeNodeToggle': false,
    'partnerSharedConfigs': {
      'value': [
        'timeAxisViewModel',
        'timeAxis',
        'viewPreset',
        'infiniteScroll'
      ],
      '$config': { 'merge': 'distinct' }
    },
    'startDate': {
      '$config': { 'equal': 'date' },
      'value': null
    },
    'endDate': {
      '$config': { 'equal': 'date' },
      'value': null
    },
    'partner': null,
    'stickyHeaders': true,
    'visibleDate': null,
    'eventCls': null,
    'forceFit': false,
    'timeZone': null,
    'hideRowHover': {
      '$config': 'lazy',
      'value': true
    }
  };
  static type = 'timelinebase';
  static $name = 'TimelineBase';
  timeCellSelector = null;
  constructor() {
    super(...arguments);
  }
  static get defaultConfig() {
    return {
      'workingTime': null,
      'timeAxis': null,
      'timeAxisViewModel': null,
      'autoAdjustTimeAxis': true,
      'snapRelativeToEventStartDate': false,
      'suppressFit': false,
      'timeCellCls': null,
      'scheduledEventName': null,
      'overScheduledEventClass': null,
      'preventOverCls': false,
      'enableEventAnimations': true,
      'disableGridRowModelWarning': true,
      'animateRemovingRows': false,
      'schedulerRegion': 'normal',
      'transitionDuration': 200,
      'animationTimeout': null,
      'defaultRegion': 'locked',
      'durationDisplayPrecision': 1,
      'asyncEventSuffix': 'PreCommit'
    };
  }
  updateTimeZone(a) {
    if (this.project) {
      if (this.isConfiguring) {
        this.project._isConfiguringTimeZone = true;
      }
      this.project.timeZone = a;
    }
  }
  get timeZone() {
    let a;
    return (a = this.project) == null ? void 0 : a.timeZone;
  }
  populateEventMenu() {
  }
  populateScheduleMenu() {
  }
  internalOnVisibleDateRangeChange(a) {
    if (!this.handlingVisibleDateRangeChange) {
      const b = this, {_visibleDateRange: c} = b, e = !c || (c.startDate - a.startDate || c.endDate - a.endDate);
      if (e) {
        b.timeView.range = a;
        b.handlingVisibleDateRangeChange = true;
        b.trigger('visibleDateRangeChange', {
          'old': c,
          'new': a
        });
        b.handlingVisibleDateRangeChange = false;
        b._visibleDateRange = a;
      }
    }
  }
  onVisibleResourceRangeChange() {
  }
  onVisibleResourceColumnChange() {
  }
  construct(a = {}) {
    const b = this;
    super.construct(a);
    b.$firstVerticalOverflow = true;
    b.initDomEvents();
    b.currentOrientation.init();
    b.rowManager.ion({
      'refresh': () => {
        b.forceLayout = false;
      }
    });
  }
  createSubGrid(a, b = {}) {
    const c = this, {stickyHeaders: e} = c;
    if (a === (c.schedulerRegion || 'normal')) {
      b.type = 'timeaxissubgrid';
    } else if (a === 'locked' && e && c.isVertical) {
      b.scrollable = {
        'overflowX': 'visible',
        'overflowY': 'visible'
      };
      c.bodyContainer.classList.add('b-sticky-headers');
    }
    return super.createSubGrid(a, b);
  }
  doDestroy() {
    const a = this, {
        partneredWith: b,
        currentOrientation: c
      } = a;
    c == null ? void 0 : c.destroy();
    if (b) {
      for (let e = b.count - 1; e > -1; --e) {
        const f = b.getAt(e);
        a.removePartner(f);
      }
      b.destroy();
    } else {
      a.timeAxisViewModel.destroy();
      a.timeAxis.destroy();
    }
    super.doDestroy();
  }
  startConfigure(a) {
    super.startConfigure(a);
    ResizeMonitor.addResizeListener(this.bodyContainer, this.onBodyResize.bind(this));
    this.getConfig('partner');
  }
  changeStartDate(a) {
    if (typeof a === 'string') {
      a = DateHelper.parse(a);
    }
    return a;
  }
  onInternalPaint({firstPaint: a}) {
    let b, c;
    if (a) {
      const e = this, f = e.isHorizontal ? e.timeAxisSubGrid.scrollable : e.scrollable, g = f.element.getBoundingClientRect()[e.isHorizontal ? 'width' : 'height'];
      e.timeAxisViewModel.update(g, e.infiniteScroll, true);
      if (e.infiniteScroll) {
        (c = (b = e.currentOrientation).doUpdateTimeView) == null ? void 0 : c.call(b);
      }
      e.getConfig('hideRowHover');
    }
    super.onInternalPaint(...arguments);
  }
  onSchedulerHorizontalScroll(a, b, c, e) {
    this.currentOrientation.updateFromHorizontalScroll(c);
    super.onSchedulerHorizontalScroll(a, b, c, e);
  }
  initScroll() {
    const a = this;
    let b = 0;
    super.initScroll();
    a.ion({
      'horizontalScroll': ({
        subGrid: c,
        scrollLeft: e,
        scrollX: f,
        scrollingToCenter: g
      }) => {
        if (a.isPainted && c === a.timeAxisSubGrid && !a.isDestroying && !a.refreshSuspended) {
          a.onSchedulerHorizontalScroll(c, e, f, g);
        }
        b++;
      }
    });
    if (a.testPerformance === 'horizontal') {
      a.setTimeout(() => {
        const c = performance.now();
        let e = 5, f = 1;
        const g = a.setInterval(() => {
          e = e + 5;
          a.scrollX += (10 + Math.floor(e)) * f;
          if (f === 1 && a.scrollX > 5500) {
            f = -1;
            e = 5;
          }
          if (f === -1 && a.scrollX <= 0) {
            const i = performance.now(), j = i - c;
            const k = j / b, l = Math.round(1000 / k * 10) / 10;
            clearInterval(g);
            console.log(a.eventPositionMode, a.eventScrollMode, l + 'fps');
          }
        }, 0);
      }, 500);
    }
  }
  preserveViewCenter(a, b = this, ...c) {
    const e = this, f = e.viewportCenterDate, g = a.apply(b, c), i = e.timelineScroller, {clientSize: j} = i, k = Math.max(Math.floor(e.getCoordinateFromDate(f, true) - j / 2), 0);
    i.scrollTo(k, { 'scrollingToCenter': true });
    return g;
  }
  setTimeSpan(a, b, c = emptyObject11) {
    const e = this, {timeAxis: f} = e, {
        preventThrow: preventThrow = false,
        maintainVisibleStart: g = false,
        visibleDate: i
      } = c, {
        startDate: j,
        endDate: k
      } = f.getAdjustedDates(a, b), l = f.startDate - j !== 0, n = f.endDate - k !== 0;
    if (l || n) {
      if (g) {
        const {timeAxisViewModel: p} = e, {totalSize: q} = p, r = p.tickSize, u = e.timelineScroller, v = u.position, w = p.getDateFromPosition(v);
        if (w >= j && w < k) {
          p.ion({
            'update'() {
              const z = p.tickSize !== r;
              e.updateCanvasSize();
              if (l && !n && !z) {
                u.position += p.totalSize - q;
              } else if (!l && !z) {
                u.position = v;
              } else {
                u.position = p.getPositionFromDate(w);
              }
              u.syncPartners(true);
            },
            'prio': 10000,
            'once': true
          });
        }
      }
      if (e.lastViewPresetOptions) {
        e.lastViewPresetOptions = {
          'startDate': j,
          'endDate': k
        };
      }
      const o = f.reconfigure({
        'startDate': j,
        'endDate': k
      }, false, preventThrow);
      if (i) {
        return e.scrollToDate(i, c).then(() => o);
      }
      return o;
    }
  }
  get hasVisibleEvents() {
    return !this.noFeatureElementsInAxis() || this.eventStore.storage.values.some(a => this.timeAxis.isTimeSpanInAxis(a));
  }
  noFeatureElementsInAxis() {
  }
  get capitalizedEventName() {
    if (!this._capitalizedEventName) {
      this._capitalizedEventName = StringHelper.capitalize(this.scheduledEventName);
    }
    return this._capitalizedEventName;
  }
  updatePartner(a) {
    if (a) {
      this.addPartner(a);
    }
  }
  addPartner(a) {
    const b = this;
    if (!b.isPartneredWith(a)) {
      if (b._partner == null) {
        b._partner = a;
      }
      const c = b.partneredWith || (b.partneredWith = new Collection());
      c.add(a);
      (a.partneredWith || (a.partneredWith = new Collection())).add(b);
      b.getConfig('viewPreset');
      b.getConfig('infiniteScroll');
      a.ion({
        'presetchange': 'onPartnerPresetChange',
        'thisObj': b
      });
      const e = b.partnerSharedConfigs.reduce((f, g) => {
        f[g] = a[g];
        return f;
      }, {});
      e.viewPreset.options = {
        'scrollPosition': a.timelineScroller.position,
        'tickSize': a.tickSize
      };
      b._ownTimeAxis = b.timeAxis;
      b._ownTimeAxisViewModel = b.timeAxisViewModel;
      b.setConfig(e);
      b.ion({
        'presetchange': 'onPartnerPresetChange',
        'thisObj': a
      });
      if (b.isPainted) {
        a.scrollable.addPartner(b.scrollable, b.isHorizontal ? 'x' : 'y');
        a.syncPartnerSubGrids();
      } else {
        b.initScroll = FunctionHelper.createSequence(b.initScroll, () => {
          a.scrollable.addPartner(b.scrollable, b.isHorizontal ? 'x' : 'y');
          a.syncPartnerSubGrids();
        }, b);
      }
    }
  }
  removePartner(a) {
    const b = this, {
        partneredWith: c,
        timeAxis: e
      } = b;
    if (b.isPartneredWith(a)) {
      if (b._partner === a) {
        b._partner = null;
      }
      c.remove(a);
      b.scrollable.removePartner(a.scrollable);
      b.un({
        'presetchange': 'onPartnerPresetChange',
        'thisObj': a
      });
      const {
        scrollLeft: f,
        scrollTop: g
      } = a;
      a.removePartner(b, true);
      if (e.owner !== b && !b.isDestroying) {
        b.timeAxis = {
          ...a.timeAxis.getCurrentConfig({ 'skip': { 'owner': 1 } }),
          'owner': b
        };
        const i = a.timeAxisViewModel.getCurrentConfig({
          'skip': {
            'owner': 1,
            'timeAxis': 1,
            'internalListeners': 1
          }
        });
        b.timeAxisViewModel = {
          ...i,
          'owner': b,
          'timeAxis': b.timeAxis
        };
        b.scrollLeft = f;
        b.scrollTop = g;
        b.timeAxisViewModel.update(b.timeAxisSubGridElement.getBoundingClientRect()[b.isHorizontal ? 'width' : 'height']);
      }
    }
  }
  isPartneredWith(a) {
    let b;
    return Boolean((b = this.partneredWith) == null ? void 0 : b.includes(a));
  }
  onPartnerPresetChange({
    preset: a,
    startDate: b,
    endDate: c,
    centerDate: e,
    zoomDate: f,
    zoomPosition: g,
    zoomLevel: i
  }) {
    if (!this._viewPresetChanging && this.viewPreset !== a) {
      a.options = {
        'startDate': b,
        'endDate': c,
        'centerDate': e,
        'zoomDate': f,
        'zoomPosition': g,
        'zoomLevel': i
      };
      this.viewPreset = a;
    }
  }
  get partners() {
    const a = this.partner ? [this.partner] : [];
    if (this.partneredWith) {
      a.push.apply(a, this.partneredWith.allValues);
    }
    return [...new Set(a)];
  }
  get timeAxisColumn() {
    return this.columns && this._timeAxisColumn;
  }
  changeColumns(a, b) {
    const c = this;
    let e, {timeAxisColumnConfig: f} = c;
    if (a) {
      const g = Array.isArray(a);
      let i = a;
      if (!g) {
        i = a.data;
      }
      e = i && i.length;
      i.some((j, k) => {
        if (j.type === 'timeAxis') {
          e = k;
          f = ObjectHelper.assign(j, c.timeAxisColumn);
          return true;
        }
        return false;
      });
      if (c.isVertical) {
        i = [
          ObjectHelper.assign({ 'type': 'verticalTimeAxis' }, c.verticalTimeAxisColumn),
          i[e]
        ];
        e = 1;
      } else {
        i = i.slice();
      }
      i[e] = this._timeAxisColumn || ObjectHelper.assign({
        'type': 'timeAxis',
        'cellCls': c.timeCellCls,
        'mode': c.mode
      }, f);
      if (g || a.isStore && a.owner !== this) {
        a = i;
      } else {
        a.data = i;
      }
    }
    return super.changeColumns(a, b);
  }
  updateColumns(a, b) {
    super.updateColumns(a, b);
    if (a) {
      const c = this, e = c._timeAxisColumn = c.columns.find(f => f.isTimeAxisColumn);
      if (c.isVertical) {
        c.verticalTimeAxisColumn = c.columns.find(f => f.isVerticalTimeAxisColumn);
        c.verticalTimeAxisColumn.relayAll(c);
      }
      e.relayAll(c);
    }
  }
  onColumnsChanged({
    action: a,
    changes: b,
    record: c,
    records: e
  }) {
    let f;
    const {
      timeAxisColumn: g,
      columns: i
    } = this;
    if ((a === 'dataset' || a === 'batch') && !i.includes(g)) {
      i.add(g, true);
    } else if (c === g && 'width' in b) {
      this.updateCanvasSize();
    }
    c && ((f = this.partneredWith) == null ? void 0 : f.forEach(j => {
      const l = j.columns.getAt(c.allIndex);
      if (l == null ? void 0 : l.shouldSync(c)) {
        const n = {};
        for (const o in b) {
          n[o] = b[o].value;
        }
        l.set(n);
      }
    }));
    super.onColumnsChanged(...arguments);
  }
  get timeView() {
    let a, b;
    const c = this;
    return c.columns && c.isVertical ? (a = c.verticalTimeAxisColumn) == null ? void 0 : a.view : (b = c.timeAxisColumn) == null ? void 0 : b.timeAxisView;
  }
  updateEventCls(a) {
    const b = this;
    if (!b.eventSelector) {
      b.unreleasedEventSelector = b.eventSelector = '.' + a + '-wrap';
    }
    if (!b.eventInnerSelector) {
      b.eventInnerSelector = '.' + a;
    }
  }
  set timeAxisViewModel(a) {
    let b;
    const c = this, e = c._timeAxisViewModel, f = {
        'name': 'timeAxisViewModel',
        'update': 'onTimeAxisViewModelUpdate',
        'prio': 100,
        'thisObj': c
      };
    if (c.isConfiguring && c.initialConfig.partner) {
      a = c.initialConfig.partner.timeAxisViewModel;
    }
    if (e && e === a) {
      return;
    }
    if ((e == null ? void 0 : e.owner) === c) {
      e.destroy();
    }
    c.detachListeners('timeAxisViewModel');
    if (a == null ? void 0 : a.isTimeAxisViewModel) {
      a.ion(f);
    } else {
      a = TimeAxisViewModel.new({
        'mode': c._mode,
        'snap': c.snap,
        'forceFit': c.forceFit,
        'timeAxis': c.timeAxis,
        'suppressFit': c.suppressFit,
        'internalListeners': f,
        'owner': c
      }, a);
    }
    c._timeAxisViewModel = a;
    if (c.isHorizontal && c.timeAxisColumn) {
      c.timeAxisColumn.timeAxisViewModel = a;
    } else if (c.isVertical && ((b = c.verticalTimeAxisColumn) == null ? void 0 : b.view)) {
      c.verticalTimeAxisColumn.view.model = a;
    }
    c.relayEvents(a, ['update'], 'timeAxisViewModel');
    if (e && a) {
      c.trigger('timeAxisViewModelChange', { 'timeAxisViewModel': a });
    }
  }
  get timeAxisViewModel() {
    if (!this._timeAxisViewModel) {
      this.timeAxisViewModel = null;
    }
    return this._timeAxisViewModel;
  }
  get suppressFit() {
    let a, b;
    return (b = (a = this._timeAxisViewModel) == null ? void 0 : a.suppressFit) != null ? b : this._suppressFit;
  }
  set suppressFit(a) {
    if (this._timeAxisViewModel) {
      this.timeAxisViewModel.suppressFit = a;
    } else {
      this._suppressFit = a;
    }
  }
  set timeAxis(a) {
    const b = this, c = b._timeAxis, e = {
        'name': 'timeAxis',
        'reconfigure': 'onTimeAxisReconfigure',
        'thisObj': b
      };
    if (b._partner && !a || c && c === a) {
      return;
    }
    if (c) {
      if (c.owner === b) {
        c.destroy();
      }
    }
    b.detachListeners('timeAxis');
    if (!(a == null ? void 0 : a.isTimeAxis)) {
      a = ObjectHelper.assign({
        'owner': b,
        'viewPreset': b.viewPreset,
        'autoAdjust': b.autoAdjustTimeAxis,
        'weekStartDay': b.weekStartDay,
        'forceFullTicks': b.fillTicks && b.snap
      }, a);
      if (b.startDate) {
        a.startDate = b.startDate;
      }
      if (b.endDate) {
        a.endDate = b.endDate;
      }
      if (b.workingTime) {
        b.applyWorkingTime(a);
      }
      a = new TimeAxis(a);
    }
    a.ion(e);
    b._timeAxis = a;
  }
  onTimeAxisReconfigure({
    config: a,
    oldConfig: b
  }) {
    if (a) {
      const c = !b || (b.startDate - a.startDate || b.endDate - a.endDate);
      if (c) {
        this.trigger('dateRangeChange', {
          'old': {
            'startDate': b.startDate,
            'endDate': b.endDate
          },
          'new': {
            'startDate': a.startDate,
            'endDate': a.endDate
          }
        });
      }
    }
    this.trigger('timeAxisChange', { 'config': a });
  }
  get timeAxis() {
    if (!this._timeAxis) {
      this.timeAxis = null;
    }
    return this._timeAxis;
  }
  updateForceFit(a) {
    if (this._timeAxisViewModel) {
      this._timeAxisViewModel.forceFit = a;
    }
  }
  set workingTime(a) {
    this._workingTime = a;
    if (!this.isConfiguring) {
      this.applyWorkingTime(this.timeAxis);
    }
  }
  get workingTime() {
    return this._workingTime;
  }
  applyWorkingTime(a) {
    let b;
    const c = this, e = c._workingTime;
    if (e) {
      let f = null;
      if (e.fromHour >= 0 && e.fromHour < 24 && e.toHour > e.fromHour && e.toHour <= 24 && e.toHour - e.fromHour < 24) {
        f = {
          'from': e.fromHour,
          'to': e.toHour
        };
      }
      let g = null;
      if (e.fromDay >= 0 && e.fromDay < 7 && e.toDay > e.fromDay && e.toDay <= 7 && e.toDay - e.fromDay < 7) {
        g = {
          'from': e.fromDay,
          'to': e.toDay
        };
      }
      if (f || g) {
        a.include = {
          'hour': f,
          'day': g
        };
      } else {
        a.include = null;
      }
    } else {
      a.include = null;
    }
    if (c.isPainted) {
      c.timeAxisColumn.refreshHeader();
      (b = c.features.columnLines) == null ? void 0 : b.refresh();
      c.refreshWithTransition();
    }
  }
  updateStartDate(a) {
    this.setStartDate(a);
  }
  setStartDate(a, b = true) {
    const c = this, e = c._timeAxis, {
        startDate: f,
        endDate: g,
        mainUnit: i
      } = e || emptyObject11;
    if (typeof a === 'string') {
      a = DateHelper.parse(a);
    }
    if (e && g) {
      if (a) {
        let j = g;
        if (b && f) {
          const k = DateHelper.diff(f, g, i, true);
          j = DateHelper.add(a, k, i);
        }
        c.setTimeSpan(a, j);
      }
    } else {
      c._tempStartDate = a;
    }
  }
  get startDate() {
    let a;
    let b = ((a = this._timeAxis) == null ? void 0 : a.startDate) || this._tempStartDate;
    if (!b) {
      b = new Date();
      const {workingTime: c} = this;
      if (c) {
        while (!isWorkingTime(b, c)) {
          b.setHours(b.getHours() + 1);
        }
      }
      this._tempStartDate = b;
    }
    return b;
  }
  changeEndDate(a) {
    if (typeof a === 'string') {
      a = DateHelper.parse(a);
    }
    this.setEndDate(a);
  }
  setEndDate(a, b = false) {
    const c = this, e = c._timeAxis, {
        startDate: f,
        endDate: g,
        mainUnit: i
      } = e || emptyObject11;
    if (typeof a === 'string') {
      a = DateHelper.parse(a);
    }
    if (e && f) {
      if (a) {
        let j = f;
        if (b && g) {
          const k = DateHelper.diff(f, g, i, true);
          j = DateHelper.add(a, -k, i);
        }
        c.setTimeSpan(j, a);
      }
    } else {
      c._tempEndDate = a;
    }
  }
  get endDate() {
    const a = this;
    if (a._timeAxis) {
      return a._timeAxis.endDate;
    }
    return a._tempEndDate || DateHelper.add(a.startDate, a.viewPreset.defaultSpan, a.viewPreset.mainHeader.unit);
  }
  changeVisibleDate(a) {
    if (typeof a === 'string') {
      return this.changeVisibleDate({ 'date': a });
    }
    if (a instanceof Date) {
      return {
        'date': a,
        'block': this.isConfiguring ? 'start' : 'nearest'
      };
    }
    if (a instanceof Object) {
      return {
        ...a,
        'date': typeof a.date === 'string' ? DateHelper.parse(a.date) : a.date,
        'block': a.block || (this.isConfiguring ? 'start' : 'nearest')
      };
    }
  }
  updateVisibleDate(a) {
    const b = this;
    if (!(b.infiniteScroll && b.isConfiguring)) {
      if (b.isPainted) {
        b.scrollToDate(a.date, a);
      } else if (!b.visibleDateProcessed) {
        b.whenVisible(() => b.updateVisibleDate(b.visibleDate));
        b.visibleDateProcessed = true;
      }
    }
  }
  get features() {
    return super.features;
  }
  set features(a) {
    a = a === true ? {} : a;
    if (!('regionResize' in a)) {
      a.regionResize = true;
    }
    super.features = a;
  }
  onLocaleChange() {
    super.onLocaleChange();
    const a = this.timeAxis.autoAdjust;
    this.timeAxis.reconfigure({ 'autoAdjust': false });
    this.timeAxis.autoAdjust = a;
  }
  onBodyResize(a, b, {
    width: c,
    height: e
  }) {
    if (this.isVertical && b && c !== b.width) {
      delete this.timeAxisSubGrid._width;
    }
    const f = this.timeAxisSubGrid.element.offsetWidth;
    if (this._bodyRectangle && b && e !== b.height) {
      this.onSchedulerViewportResize(f, e, f, b.height);
    }
  }
  onSchedulerViewportResize(a, b, c, e) {
    if (this.isPainted) {
      const f = this, {
          isHorizontal: g,
          partneredWith: i
        } = f;
      f.currentOrientation.onViewportResize(a, b, c, e);
      f.updateViewModelAvailableSpace(g ? a : Math.floor(b));
      if (i && !f.isSyncingFromPartner) {
        f.syncPartnerSubGrids();
      }
      f.trigger('timelineViewportResize', {
        'width': a,
        'height': b,
        'oldWidth': c,
        'oldHeight': e
      });
    }
  }
  updateViewModelAvailableSpace(a) {
    this.timeAxisViewModel.availableSpace = a;
  }
  onTimeAxisViewModelUpdate() {
    if (!this._viewPresetChanging && this.timeAxisSubGrid && !this.timeAxisSubGrid.collapsed) {
      this.updateCanvasSize();
      this.currentOrientation.onTimeAxisViewModelUpdate();
    }
  }
  syncPartnerSubGrids() {
    this.partneredWith.forEach(a => {
      if (!a.isSyncingFromPartner) {
        a.isSyncingFromPartner = true;
        this.eachSubGrid(b => {
          const c = a.subGrids[b.region];
          if (c.width !== b.width) {
            if (b.flex) {
              if (b.flex !== c.flex) {
                c.flex = b.flex;
              }
            } else {
              c.width = b.width;
            }
          }
        });
        a.isSyncingFromPartner = false;
      }
    });
  }
  get currentOrientation() {
    throw new Error('Implement in subclass');
  }
  get isHorizontal() {
    return true;
  }
  get foregroundCanvas() {
    return this._foregroundCanvas;
  }
  get svgCanvas() {
    const a = this;
    if (!a._svgCanvas) {
      const b = a._svgCanvas = document.createElementNS('http://www.w3.org/2000/svg', 'svg');
      b.setAttribute('id', IdHelper.generateId('svg'));
      b.classList.add('b-sch-canvas', 'b-sch-dependencies-canvas');
      b.retainElement = true;
      a.timeAxisSubGridElement.appendChild(b);
      a.trigger('svgCanvasCreated', { 'svg': b });
    }
    return a._svgCanvas;
  }
  get timeAxisSubGridElement() {
    let a;
    if (!this._timeAxisSubGridElement) {
      this.getConfig('regions');
      this._timeAxisSubGridElement = (a = this.timeAxisColumn) == null ? void 0 : a.subGridElement;
    }
    return this._timeAxisSubGridElement;
  }
  updateCanvasSize() {
    const a = this;
    let b = false;
    if (a.isVertical) {
      const {totalSize: c} = a.timeAxisViewModel;
      if (a.isPainted) {
        a.refreshTotalHeight(c + a._rowBorderHeight, true);
      }
      if (a.suppressFit) {
        DomHelper.setLength(a.foregroundCanvas, 'height', c);
      }
      b = true;
    }
    return b;
  }
  getHeaderDomConfigs(a) {
  }
  getForegroundDomConfigs(a) {
  }
  async onStoreDataChange({action: a}) {
    let b;
    const c = this;
    if (c.isVisible) {
      if (a === 'dataset' && ((b = c.project) == null ? void 0 : b.isRepopulatingStores)) {
        await c.project.await('refresh', false);
      }
      super.onStoreDataChange(...arguments);
    } else {
      c.whenVisible('refresh', c, [true]);
    }
  }
  refresh(a = true) {
    const b = this;
    if (b.isPainted && !b.refreshSuspended) {
      if (b.isVertical || b.hasVisibleEvents || b.timeAxisSubGridElement.querySelector(b.eventSelector)) {
        if (!b.project || b.isEngineReady) {
          b.refreshRows(null, false, a);
        } else {
          b.refreshAfterProjectRefresh = true;
          b.currentOrientation.refreshAllWhenReady = true;
        }
      } else {
        b.rowManager.trigger('refresh');
      }
    }
  }
  render() {
    const a = this, {total: b} = a.resourceMarginObject, c = a.timeAxisSubGridElement;
    const e = a._foregroundCanvas = DomHelper.createElement({
      'className': 'b-sch-canvas b-sch-foreground-canvas',
      'style': 'font-size:' + (a.rowHeight - b) + 'px',
      'parent': c
    });
    a.canvases = { 'event': e };
    a.timeAxisSubGrid.insertRowsBefore = e;
    if (a.isVertical && a.suppressFit) {
      a.updateCanvasSize();
    }
    super.render(...arguments);
  }
  refreshRows(a = null, b = false, c = true) {
    const e = this;
    if (e.isConfiguring) {
      return;
    }
    e.currentOrientation.refreshRows(a, c);
    super.refreshRows(a, b);
  }
  updateHideHeaders(a) {
    const b = this, c = b.isPainted ? b.scrollLeft : 0;
    super.updateHideHeaders(a);
    if (b.isPainted) {
      if (!a) {
        b.timeAxisColumn.refreshHeader(null, true);
      }
      b.nextAnimationFrame().then(() => b.scrollLeft = c);
    }
  }
  updateHideRowHover(a) {
    this.timeAxisSubGridElement.classList.toggle('b-hide-row-hover', a);
  }
  getCellDataFromEvent(a, b) {
    if (b) {
      b = !Boolean(a.target.closest('.b-sch-foreground-canvas'));
    }
    return super.getCellDataFromEvent(a, b);
  }
  onCellNavigate(a, b, c) {
    let e, f;
    const g = c.target;
    if (((e = c.cell) == null ? void 0 : e.classList.contains('b-timeaxis-cell')) && !((f = GlobalEvents_default.currentMouseDown) == null ? void 0 : f.target.classList.contains('b-grid-cell'))) {
      this.preventDragSelect = true;
    }
    if ((!(g == null ? void 0 : g.matches(this.eventSelector)) || this.selectResourceOnEventNavigate !== false) && (!(g == null ? void 0 : g.matches('.b-timeaxis-cell')) || this.selectResourceOnScheduleClick !== false)) {
      super.onCellNavigate(...arguments);
    }
  }
  async waitForAnimations() {
    if (!this.isEngineReady && this.project) {
      await this.project.await('dataReady', false);
    }
    await super.waitForAnimations();
  }
  refreshWithTransition(a, b = true) {
    const c = this;
    if (!c.refreshSuspended && c.isPainted) {
      if (!c.rowManager.topRow) {
        c.rowManager.reinitialize();
      } else {
        c.runWithTransition(() => c.refresh(a), b);
      }
    }
  }
  get visibleDateRange() {
    return this.currentOrientation.visibleDateRange;
  }
  isRowNumberSelecting(...a) {
    return super.isRowNumberSelecting(...a) || a.some(b => {
      let c;
      return b.column ? b.column.isTimeAxisColumn : (c = b.cell) == null ? void 0 : c.closest('.b-timeaxis-cell');
    });
  }
  formatDuration(a, b = this.durationDisplayPrecision) {
    const c = Math.pow(10, b);
    return Math.round(a * c) / c;
  }
  beginListeningForBatchedUpdates() {
    let a;
    this.listenToBatchedUpdates = (this.listenToBatchedUpdates || 0) + 1;
    (a = this.syncSplits) == null ? void 0 : a.call(this, b => b.beginListeningForBatchedUpdates());
  }
  endListeningForBatchedUpdates() {
    let a;
    if (this.listenToBatchedUpdates) {
      this.listenToBatchedUpdates -= 1;
    }
    (a = this.syncSplits) == null ? void 0 : a.call(this, b => b.endListeningForBatchedUpdates());
  }
  onConnectedCallback(a, b) {
    if (a && !b) {
      this.timeAxisSubGrid.scrollable.x += 0.5;
    }
  }
  updateRtl(a) {
    const b = this, {isConfiguring: c} = b;
    let e;
    if (!c) {
      e = b.visibleDateRange;
    }
    super.updateRtl(a);
    if (!c) {
      b.currentOrientation.clearAll();
      if (b.infiniteScroll) {
        b.shiftToDate(e.startDate);
        b.scrollToDate(e.startDate, { 'block': 'start' });
      } else {
        b.timelineScroller.position += 0.5;
      }
    }
  }
  applyStartEndParameters(a) {
    const b = this, c = b.eventStore.modelClass.fieldMap.startDate;
    if (b.passStartEndParameters) {
      a[b.startParamName] = c.print(b.startDate);
      a[b.endParamName] = c.print(b.endDate);
    }
  }
  intersectsVisibleTimeSpan(a) {
    return this.timeAxis.isTimeSpanInAxis(a);
  }
};
TimelineBase.initClass();
VersionHelper.setVersion('scheduler', '6.0.4');
TimelineBase._$name = 'TimelineBase';
const ResourceStoreMixin_default = a => class b extends (a || Base) {
  static get $name() {
    return 'ResourceStoreMixin';
  }
  get isResourceStore() {
    return true;
  }
  static get defaultConfig() {
    return {
      'loadPriority': 200,
      'syncPriority': 100,
      'id': 'resources',
      'autoTree': true
    };
  }
  construct(c) {
    super.construct(c);
    if (!this.modelClass.isResourceModel) {
      throw new Error('Model for ResourceStore must subclass ResourceModel');
    }
  }
  removeAll() {
    const c = super.removeAll(...arguments);
    c && this.assignmentStore.removeAll();
    return c;
  }
  onRecordIdChange({
    record: c,
    oldValue: e,
    value: f
  }) {
    super.onRecordIdChange({
      'record': c,
      'oldValue': e,
      'value': f
    });
    if (c.isFieldModified('id')) {
      this.isChangingId = true;
      c.updateAssignmentResourceIds();
      this.isChangingId = false;
    }
  }
  get allResourceRecords() {
    return this._allResourceRecords || (this._allResourceRecords = this.getAllDataRecords());
  }
  getAvailableResources({
    startDate: c,
    endDate: e
  }) {
    return this.query(f => this.eventStore.isDateRangeAvailable(c, e, null, f));
  }
};
const PartOfProject_default = a => class b extends (a || Base) {
  static get $name() {
    return 'PartOfProject';
  }
};
const ResourceModelMixin_default = a => class b extends a {
  static get $name() {
    return 'ResourceModelMixin';
  }
  static get isResourceModel() {
    return true;
  }
  static get fields() {
    return [
      {
        'name': 'name',
        'type': 'string'
      },
      'eventColor',
      'eventStyle',
      'imageUrl',
      'image',
      'resourceMargin',
      {
        'name': 'barMargin',
        'type': 'number'
      },
      'eventLayout',
      {
        'name': 'allowOverlap',
        'type': 'boolean'
      },
      {
        'name': 'showAvatar',
        'type': 'boolean'
      }
    ];
  }
  updateAssignmentResourceIds() {
    this.assigned.forEach(c => {
      c.resourceId = this.id;
    });
  }
  syncId(c) {
    super.syncId(c);
    this.updateAssignmentResourceIds();
  }
  get events() {
    return this.assignments.reduce((c, e) => {
      if (e.event) {
        c.push(e.event);
      }
      return c;
    }, []);
  }
  get assignments() {
    return this.assigned ? [...this.assigned] : [];
  }
  set assignments(c) {
    this.assignmentStore.remove(this.assignments);
    c.forEach(e => {
      e.resource = this;
    });
  }
  get isPersistable() {
    return super.isPersistable && (!this.parent || this.parent.isPersistable);
  }
  unassignAll() {
    this.assignments && this.assignmentStore.remove(this.assignments);
  }
  get initials() {
    const {
      name: name = ''
    } = this;
    if (!name) {
      return '';
    }
    const c = name.split(' '), e = c[0][0], f = c.length > 1 ? c[c.length - 1][0] : '';
    return e + f;
  }
  isWorkingTime(c) {
    let e, f;
    const g = this.effectiveCalendar || ((e = this.project) == null ? void 0 : e.calendar);
    return !g || ((f = g.isWorkingTime) == null ? void 0 : f.call(g, c));
  }
  getEventStartingAfter(c) {
    let e = Number.MAX_VALUE, f;
    this.events.forEach(g => {
      if (g.isScheduled && g.startDate >= c && g.startDate < e) {
        f = g;
        e = g.startDate;
      }
    });
    return f;
  }
  getEventEndingBefore(c) {
    let e = 0, f;
    this.events.forEach(g => {
      if (g.isScheduled && g.endDate <= c && g.endDate > e) {
        f = g;
        e = g.endDate;
      }
    });
    return f;
  }
};
function split(a) {
  const b = [];
  const c = [];
  let e;
  const f = function* () {
    if (!e)
      e = a[Symbol.iterator]();
    while (true) {
      if (b.length) {
        yield* b;
        b.length = 0;
      }
      if (!e)
        break;
      const {
        value: i,
        done: j
      } = e.next();
      if (j) {
        e = null;
        a = null;
        break;
      }
      c.push(i);
      yield i;
    }
  };
  const g = function* () {
    if (!e)
      e = a[Symbol.iterator]();
    while (true) {
      if (c.length) {
        yield* c;
        c.length = 0;
      }
      if (!e)
        break;
      const {
        value: i,
        done: j
      } = e.next();
      if (j) {
        e = null;
        a = null;
        break;
      }
      b.push(i);
      yield i;
    }
  };
  return [
    f(),
    g()
  ];
}
function* inBatchesBySize(a, b) {
  if (b < 0)
    throw new Error('Batch size needs to a natural number');
  b = b | 0;
  const c = [];
  for (const e of a) {
    if (c.length === b) {
      yield c;
      c.length = 0;
    }
    c.push(e);
  }
  if (c.length > 0)
    yield c;
}
function* filter(a, b) {
  let c = 0;
  for (const e of a) {
    if (b(e, c++))
      yield e;
  }
}
function* drop(a, b) {
  let c = 0;
  for (const e of a) {
    if (++c > b)
      yield e;
  }
}
function every(a, b) {
  let c = 0;
  for (const e of a) {
    if (!b(e, c++))
      return false;
  }
  return true;
}
function some(a, b) {
  let c = 0;
  for (const e of a) {
    if (b(e, c++))
      return true;
  }
  return false;
}
function* map(a, b) {
  let c = 0;
  for (const e of a)
    yield b(e, c++);
}
function reduce(a, b, c) {
  let e = 0;
  let f = c;
  for (const g of a) {
    f = b(f, g, e++);
  }
  return f;
}
function* uniqueOnly(a) {
  const b = new Set();
  for (const c of a) {
    if (!b.has(c)) {
      b.add(c);
      yield c;
    }
  }
}
function* uniqueOnlyBy(a, b) {
  const c = new Set();
  for (const e of a) {
    const f = b(e);
    if (!c.has(f)) {
      c.add(f);
      yield e;
    }
  }
}
function* takeWhile(a, b) {
  let c = 0;
  for (const e of a) {
    if (b(e, c++))
      yield e;
    else
      return;
  }
}
function* concat(...a) {
  for (let b = 0; b < a.length; b++)
    yield* a[b];
}
function* concatIterable(a) {
  for (const b of a)
    yield* b;
}
const ChainedIteratorClass = class _ChainedIteratorClass {
  constructor(a) {
    this.iterable = void 0;
    if (!a)
      throw new Error('Require an iterable instance for chaining');
    this.iterable = a;
  }
  derive(a) {
    this.iterable = void 0;
    return new _ChainedIteratorClass(a);
  }
  copy() {
    const [a, b] = split(this.iterable);
    this.iterable = b;
    return new _ChainedIteratorClass(a);
  }
  split() {
    const [a, b] = split(this.iterable);
    return [
      new _ChainedIteratorClass(a),
      this.derive(b)
    ];
  }
  inBatchesBySize(a) {
    return this.derive(inBatchesBySize(this.iterable, a));
  }
  filter(a) {
    return this.derive(filter(this.iterable, a));
  }
  drop(a) {
    return this.derive(drop(this.iterable, a));
  }
  map(a) {
    return this.derive(map(this.iterable, a));
  }
  reduce(a, b) {
    return reduce(this, a, b);
  }
  concat() {
    return this.derive(concatIterable(this.iterable));
  }
  uniqueOnly() {
    return this.derive(uniqueOnly(this.iterable));
  }
  uniqueOnlyBy(a) {
    return this.derive(uniqueOnlyBy(this.iterable, a));
  }
  every(a) {
    return every(this, a);
  }
  some(a) {
    return some(this, a);
  }
  takeWhile(a) {
    return this.derive(takeWhile(this.iterable, a));
  }
  *[Symbol.iterator]() {
    let a = this.iterable;
    if (!a)
      throw new Error('Chained iterator already exhausted or used to derive the new one');
    this.iterable = void 0;
    yield* a;
    a = void 0;
  }
  toArray() {
    return Array.from(this);
  }
  sort(a) {
    return Array.from(this).sort(a);
  }
  toSet() {
    return new Set(this);
  }
  toMap() {
    return new Map(this);
  }
  flush() {
    for (const a of this) {
    }
  }
  memoize() {
    return new MemoizedIteratorClass(this);
  }
};
const ChainedIterator = a => new ChainedIteratorClass(a);
const CI = ChainedIterator;
var MemoizedIteratorClass = class extends ChainedIteratorClass {
  constructor() {
    super(...arguments);
    this.elements = [];
    this.$iterator = void 0;
  }
  set iterable(a) {
    this.$iterable = a;
  }
  get iterable() {
    return this;
  }
  derive(a) {
    return new ChainedIteratorClass(a);
  }
  *[Symbol.iterator]() {
    const a = this.elements;
    if (this.$iterable) {
      if (!this.$iterator)
        this.$iterator = this.$iterable[Symbol.iterator]();
      let b = this.$iterator;
      let c = a.length;
      if (c > 0)
        yield* a;
      while (true) {
        if (a.length > c) {
          for (let g = c; g < a.length; g++)
            yield a[g];
          c = a.length;
        }
        if (!b)
          break;
        const {
          value: e,
          done: f
        } = b.next();
        if (f) {
          b = this.$iterator = null;
          this.$iterable = null;
        } else {
          a.push(e);
          c++;
          yield e;
        }
      }
    } else {
      yield* a;
    }
  }
};
const MemoizedIterator = a => new MemoizedIteratorClass(a);
const MI = MemoizedIterator;
const MixinInstanceOfProperty = Symbol('MixinIdentity');
const MixinStateProperty = Symbol('MixinStateProperty');
const MixinWalkDepthState = class {
  constructor() {
    this.baseEl = void 0;
    this.sourceEl = void 0;
    this.$elementsByTopoLevel = void 0;
    this.$topoLevels = void 0;
    this.linearizedByTopoLevelsSource = MI(this.linearizedByTopoLevels());
  }
  static new(a) {
    const b = new this();
    a && Object.assign(b, a);
    return b;
  }
  get topoLevels() {
    if (this.$topoLevels !== void 0)
      return this.$topoLevels;
    return this.$topoLevels = this.buildTopoLevels();
  }
  buildTopoLevels() {
    return Array.from(this.elementsByTopoLevel.keys()).sort((a, b) => a - b);
  }
  get elementsByTopoLevel() {
    if (this.$elementsByTopoLevel !== void 0)
      return this.$elementsByTopoLevel;
    return this.$elementsByTopoLevel = this.buildElementsByTopoLevel();
  }
  getOrCreateLevel(a, b) {
    let c = a.get(b);
    if (!c) {
      c = [];
      a.set(b, c);
    }
    return c;
  }
  buildElementsByTopoLevel() {
    let a = 0;
    const b = this.baseEl ? CI(this.baseEl.walkDepthState.elementsByTopoLevel.values()).concat().toSet() : new Set();
    const c = CI(this.sourceEl.requirements).map(e => e.walkDepthState.elementsByTopoLevel).concat().reduce((e, [f, g]) => {
      if (f > a)
        a = f;
      this.getOrCreateLevel(e, f).push(g);
      return e;
    }, new Map());
    this.getOrCreateLevel(c, a + 1).push([this.sourceEl]);
    return CI(c).map(([e, f]) => {
      return [
        e,
        CI(f).concat().uniqueOnly().filter(g => !b.has(g)).sort((g, i) => g.id - i.id)
      ];
    }).toMap();
  }
  *linearizedByTopoLevels() {
    yield* CI(this.topoLevels).map(a => this.elementsByTopoLevel.get(a)).concat();
  }
};
let MIXIN_ID = 1;
const identity = b => class extends b {
};
const ZeroBaseClass = class {
};
const MixinState = class {
  constructor() {
    this.id = MIXIN_ID++;
    this.requirements = [];
    this.baseClass = ZeroBaseClass;
    this.identitySymbol = void 0;
    this.mixinLambda = identity;
    this.walkDepthState = void 0;
    this.$minimalClass = void 0;
    this.name = '';
  }
  static new(a) {
    const b = new this();
    a && Object.assign(b, a);
    b.walkDepthState = MixinWalkDepthState.new({
      'sourceEl': b,
      'baseEl': getMixinState(b.baseClass)
    });
    const c = b.mixinLambda;
    const e = b.identitySymbol = Symbol(c.name);
    const f = Object.assign(function (g) {
      const i = c(g);
      i.prototype[e] = true;
      return i;
    }, {
      [MixinInstanceOfProperty]: e,
      [MixinStateProperty]: b
    });
    Object.defineProperty(f, Symbol.hasInstance, { 'value': isInstanceOfStatic });
    b.mixinLambda = f;
    return b;
  }
  get minimalClass() {
    if (this.$minimalClass !== void 0)
      return this.$minimalClass;
    return this.$minimalClass = this.buildMinimalClass();
  }
  getBaseClassMixinId(a) {
    const b = this.constructor;
    const c = b.baseClassesIds.get(a);
    if (c !== void 0)
      return c;
    const e = MIXIN_ID++;
    b.baseClassesIds.set(a, e);
    return e;
  }
  buildMinimalClass() {
    const a = this.constructor;
    const b = this.baseClass;
    const c = this.walkDepthState.linearizedByTopoLevelsSource.reduce((f, g) => {
      const {
        cls: i,
        hash: j
      } = f;
      const k = j + String.fromCharCode(g.id);
      let l = a.minimalClassesByLinearHash.get(k);
      if (!l) {
        l = g.mixinLambda(i);
        g.name = l.name;
        a.minimalClassesByLinearHash.set(k, l);
      }
      f.cls = l;
      f.hash = k;
      return f;
    }, {
      'cls': b,
      'hash': String.fromCharCode(this.getBaseClassMixinId(b))
    }).cls;
    const e = Object.assign(c, {
      [MixinInstanceOfProperty]: this.identitySymbol,
      [MixinStateProperty]: this,
      'mix': this.mixinLambda,
      'derive': f => Mixin([
        e,
        f
      ], g => class extends g {
      }),
      '$': this,
      'toString': this.toString.bind(this)
    });
    Object.defineProperty(e, Symbol.hasInstance, { 'value': isInstanceOfStatic });
    return e;
  }
  toString() {
    return this.walkDepthState.linearizedByTopoLevelsSource.reduce((a, b) => b.name + '(' + a + ')', this.baseClass.name);
  }
};
MixinState.minimalClassesByLinearHash = new Map();
MixinState.baseClassesIds = new Map();
const isMixinClass = a => {
  return Object.getPrototypeOf(a.prototype).constructor.hasOwnProperty(MixinStateProperty);
};
var getMixinState = a => {
  return Object.getPrototypeOf(a.prototype).constructor[MixinStateProperty];
};
const mixin = (a, b) => {
  let c;
  if (a.length > 0) {
    const g = a[a.length - 1];
    if (!isMixinClass(g) && g !== ZeroBaseClass)
      c = g;
  }
  const e = [];
  a.forEach((i, j) => {
    const k = i[MixinStateProperty];
    if (k !== void 0) {
      const l = k.baseClass;
      if (l !== ZeroBaseClass) {
        if (c) {
          if (c !== l) {
            const n = l.prototype.isPrototypeOf(c.prototype);
            const o = c.prototype.isPrototypeOf(l.prototype);
            if (!n && !o)
              throw new Error('Base class mismatch');
            c = o ? l : c;
          }
        } else
          c = l;
      }
      e.push(k);
    } else {
      if (j !== a.length - 1)
        throw new Error('Base class should be provided as the last element of the requirements array');
    }
  });
  const f = MixinState.new({
    'requirements': e,
    'mixinLambda': b,
    'baseClass': c || ZeroBaseClass
  });
  return f.minimalClass;
};
var isInstanceOfStatic = function (a) {
  return Boolean(a && a[this[MixinInstanceOfProperty]]);
};
const isInstanceOf = (a, b) => {
  return Boolean(a && a[b[MixinInstanceOfProperty]]);
};
var Mixin = mixin;
const MixinAny = mixin;
const Base2 = class {
  initialize(a) {
    a && Object.assign(this, a);
  }
  static new(a) {
    const b = new this();
    b.initialize(a);
    return b;
  }
};
const AbstractPartOfProjectGenericMixin = class extends Mixin([], a => {
  const b = a.prototype;
  class c extends a {
    async commitAsync() {
      return this.project.commitAsync();
    }
    set project(e) {
      this.$project = e;
    }
    get project() {
      return this.$project;
    }
    calculateProject() {
      throw new Error('Implement me');
    }
    setProject(e) {
      return this.project = e;
    }
    getProject() {
      if (this.project)
        return this.project;
      return this.setProject(this.calculateProject());
    }
    getAssignmentStore() {
      const e = this.getProject();
      return e == null ? void 0 : e.assignmentStore;
    }
    getDependencyStore() {
      const e = this.getProject();
      return e == null ? void 0 : e.dependencyStore;
    }
    getEventStore() {
      const e = this.getProject();
      return e == null ? void 0 : e.eventStore;
    }
    getResourceStore() {
      const e = this.getProject();
      return e == null ? void 0 : e.resourceStore;
    }
    getCalendarManagerStore() {
      const e = this.getProject();
      return e == null ? void 0 : e.calendarManagerStore;
    }
  }
  return c;
}) {
};
const CorePartOfProjectGenericMixin = class extends Mixin([AbstractPartOfProjectGenericMixin], a => {
  const b = a.prototype;
  class c extends a {
    get eventStore() {
      let e;
      return (e = this.project) == null ? void 0 : e.eventStore;
    }
    get resourceStore() {
      let e;
      return (e = this.project) == null ? void 0 : e.resourceStore;
    }
    get assignmentStore() {
      let e;
      return (e = this.project) == null ? void 0 : e.assignmentStore;
    }
    get dependencyStore() {
      let e;
      return (e = this.project) == null ? void 0 : e.dependencyStore;
    }
    get calendarManagerStore() {
      let e;
      return (e = this.project) == null ? void 0 : e.calendarManagerStore;
    }
    getEventById(e) {
      let f;
      return (f = this.eventStore) == null ? void 0 : f.getById(e);
    }
    getDependencyById(e) {
      let f;
      return (f = this.dependencyStore) == null ? void 0 : f.getById(e);
    }
    getResourceById(e) {
      let f;
      return (f = this.resourceStore) == null ? void 0 : f.getById(e);
    }
    getAssignmentById(e) {
      let f;
      return (f = this.assignmentStore) == null ? void 0 : f.getById(e);
    }
    getCalendarById(e) {
      let f;
      return (f = this.calendarManagerStore) == null ? void 0 : f.getById(e);
    }
  }
  return c;
}) {
};
const AbstractPartOfProjectStoreMixin = class extends Mixin([
  AbstractPartOfProjectGenericMixin,
  Store
], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.isLoadingData = false;
      this.disableHasLoadedDataToCommitFlag = false;
    }
    static get $name() {
      return 'AbstractPartOfProjectStoreMixin';
    }
    construct(e = {}) {
      e.asyncEvents = {
        'add': true,
        'remove': true,
        'removeAll': true,
        'change': true,
        'refresh': true,
        'replace': true,
        'move': true,
        'update': true
      };
      return b.construct.call(this, e);
    }
    trigger(e, f) {
      const g = this, {
          asyncEvents: i,
          project: j
        } = g, k = i == null ? void 0 : i[e], l = k && (k === true || k[f.action]);
      if (!l) {
        return b.trigger.call(g, e, f);
      }
      b.trigger.call(g, e + 'PreCommit', { ...f });
      if (!j || j.isEngineReady() && !j.isWritingData) {
        b.trigger.call(g, e, f);
      } else if (!g.eventsSuspended && j) {
        if (!j.dataReadyDetacher) {
          j.queuedDataReadyEvents = [];
          j.dataReadyDetacher = j.ion({
            'dataReady'() {
              this.queuedDataReadyEvents.forEach(([n, o, p, q]) => {
                n.trigger.call(o, p, q);
              });
              j.queuedDataReadyEvents = null;
              j.dataReadyDetacher();
              j.dataReadyDetacher = null;
            },
            'once': true
          });
        }
        j.queuedDataReadyEvents.push([
          b,
          g,
          e,
          f
        ]);
      }
      return true;
    }
    calculateProject() {
      return this.project;
    }
    setStoreData(e) {
      let f;
      if (this.project && !(this.syncDataOnLoad || this.disableHasLoadedDataToCommitFlag)) {
        this.project.hasLoadedDataToCommit = true;
      }
      this.isLoadingData = true;
      b.setStoreData.call(this, e);
      this.isLoadingData = false;
      (f = this.project) == null ? void 0 : f.trigger('storeRefresh', { 'store': this });
    }
    async doAutoCommit() {
      if (this.suspendCount <= 0 && this.project && !this.project.isEngineReady()) {
        await this.project.commitAsync();
      }
      b.doAutoCommit.call(this);
    }
    async addAsync(e, f) {
      const g = this.add(e, f);
      await this.project.commitAsync();
      return g;
    }
    async insertAsync(e, f, g) {
      const i = this.insert(e, f, g);
      await this.project.commitAsync();
      return i;
    }
    async loadDataAsync(e) {
      this.data = e;
      await this.project.commitAsync();
    }
  }
  return c;
}) {
};
const AbstractPartOfProjectModelMixin = class extends Mixin([
  AbstractPartOfProjectGenericMixin,
  Model
], a => {
  const b = a.prototype;
  class c extends a {
    joinStore(e) {
      let f = null;
      if (isInstanceOf(e, AbstractPartOfProjectStoreMixin)) {
        const g = e.getProject();
        if (g && !this.getProject()) {
          this.setProject(g);
          f = g;
        }
      }
      b.joinStore.call(this, e);
      if (f && !f.isRepopulatingStores)
        this.joinProject();
    }
    unjoinStore(e, f = false) {
      b.unjoinStore.call(this, e, f);
      const {project: g} = this;
      const i = isInstanceOf(e, AbstractPartOfProjectStoreMixin) && !e.isFillingFromMaster && g === (e.isChained && e.project ? e.masterStore.project : e.project);
      if (g && !g.isDestroying && !g.isRepopulatingStores && i) {
        this.leaveProject(f);
        this.setProject(null);
      }
      if (i)
        this.graph = null;
    }
    joinProject() {
    }
    leaveProject(e = false) {
    }
    calculateProject() {
      let e;
      const f = (e = this.stores) == null ? void 0 : e.find(g => isInstanceOf(g, AbstractPartOfProjectStoreMixin) && !!g.getProject());
      return f == null ? void 0 : f.getProject();
    }
    async setAsync(e, f, g) {
      let i;
      const j = this.set(e, f, g);
      await ((i = this.project) == null ? void 0 : i.commitAsync());
      return j;
    }
    async getAsync(e) {
      let f;
      await ((f = this.project) == null ? void 0 : f.commitAsync());
      return this.get(e);
    }
    get isStmRestoring() {
      const e = this.getProject();
      return (e == null ? void 0 : e.isRestoringData) || (e == null ? void 0 : e.stm.isRestoring) || false;
    }
  }
  return c;
}) {
};
const CorePartOfProjectModelMixin = class extends Mixin([
  AbstractPartOfProjectModelMixin,
  CorePartOfProjectGenericMixin,
  Model
], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.$isCalculating = false;
      this.$changed = {};
      this.$beforeChange = {};
    }
    get isInActiveTransaction() {
      return true;
    }
    joinProject() {
      !this.isRoot && this.invalidate();
    }
    leaveProject(e = false) {
      let f;
      this.project.$leaving.add(this);
      b.leaveProject.call(this, e);
      (f = this.project) == null ? void 0 : f.bufferedCommitAsync();
    }
    invalidate() {
      let e;
      (e = this.project) == null ? void 0 : e.invalidate(this);
    }
    getCurrentOrProposed(e) {
      let f;
      if (e in this.$changed && this.$changed[e] !== true) {
        return this.$changed[e];
      }
      return (f = this.get(e)) != null ? f : null;
    }
    hasCurrentOrProposed(e) {
      return e in this.$changed && this.$changed[e] != true || this.get(e) != null;
    }
    propose(e) {
      let f;
      if (this.project || ((f = this.recurringTimeSpan) == null ? void 0 : f.project)) {
        const g = Object.keys(e);
        for (let j = 0; j < g.length; j++) {
          const k = g[j];
          this.$changed[k] = e[k];
        }
        this.invalidate();
      } else {
        this.set(e);
      }
    }
    setChanged(e, f, g = true, i = false) {
      const j = this;
      j.$changed[e] = f;
      if (i) {
        if (!(e in j.$beforeChange)) {
          j.$beforeChange[e] = j.get(e);
        }
        j.setData(e, f);
      }
      g && j.invalidate();
    }
    calculateInvalidated() {
    }
    finalizeInvalidated(e = false) {
      const f = this;
      f.$isCalculating = true;
      if (!e) {
        f.setData(f.$beforeChange);
        f.set(f.$changed);
      } else {
        f.setData(f.$changed);
      }
      f.$changed = {};
      f.$beforeChange = {};
      f.$isCalculating = false;
    }
  }
  return c;
}) {
};
const CoreResourceMixin = class extends Mixin([CorePartOfProjectModelMixin], a => {
  const b = a.prototype;
  class c extends a {
    get assigned() {
      let e;
      return (e = this.project) == null ? void 0 : e.assignmentStore.getResourcesAssignments(this);
    }
    joinProject() {
      let e;
      if (this.resourceStore && !this.resourceStore.isLoadingData) {
        (e = this.assignmentStore) == null ? void 0 : e.query(f => f.get('resource') === this.id).forEach(f => f.setChanged('resource', this));
      }
      b.joinProject.call(this);
    }
    leaveProject(e = false) {
      let f;
      if (this.assigned && !e && !((f = this.resourceStore) == null ? void 0 : f.isLoadingData)) {
        const g = this.resourceStore;
        this.assigned.forEach(i => g.assignmentsForRemoval.add(i));
      }
      b.leaveProject.call(this);
    }
    applyValue(e, f, g, i, j) {
      if ((j == null ? void 0 : j.name) === 'id') {
        this.assigned.forEach(k => {
          k.set('resourceId', g);
        });
      }
      b.applyValue.call(this, e, f, g, i, j);
    }
  }
  return c;
}) {
};
const EngineMixin = CoreResourceMixin;
const ResourceModel = class extends ResourceModelMixin_default(PartOfProject_default(EngineMixin.derive(GridRowModel))) {
  static $name = 'ResourceModel';
};
ResourceModel.exposeProperties();
ResourceModel._$name = 'ResourceModel';
const CorePartOfProjectStoreMixin = class extends Mixin([
  AbstractPartOfProjectStoreMixin,
  CorePartOfProjectGenericMixin,
  Store
], a => {
  const b = a.prototype;
  class c extends a {
    setProject(e) {
      const f = b.setProject.call(this, e);
      if (e)
        this.joinProject(e);
      return f;
    }
    joinProject(e) {
    }
    onCommitAsync() {
    }
  }
  return c;
}) {
};
const dataAddRemoveActions2 = {
  'splice': 1,
  'clear': 1
};
const AbstractResourceStoreMixin = class extends Mixin([AbstractPartOfProjectStoreMixin], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.assignmentsForRemoval = new Set();
    }
    onDataChange(e) {
      let f;
      const g = dataAddRemoveActions2[e.action];
      super.onDataChange(e);
      if (g && ((f = e.removed) == null ? void 0 : f.length))
        this.afterResourceRemoval();
    }
    remove(e, f) {
      const g = b.remove.call(this, e, f);
      this.afterResourceRemoval();
      return g;
    }
    removeAll(e) {
      const f = b.removeAll.call(this, e);
      this.afterResourceRemoval();
      return f;
    }
    afterResourceRemoval() {
      const e = this.getAssignmentStore();
      if (e && !e.allAssignmentsForRemoval) {
        const f = [...this.assignmentsForRemoval].filter(g => !e.assignmentsForRemoval.has(g));
        f.length > 0 && e.remove(f);
      }
      this.assignmentsForRemoval.clear();
    }
    processRecord(e, f = false) {
      const g = this.getById(e.id);
      const i = g && g !== e;
      if (i) {
        for (const j of g.assigned || []) {
          j.resource = e;
        }
      }
      return e;
    }
  }
  return c;
}) {
};
const CoreResourceStoreMixin = class extends Mixin([
  AbstractResourceStoreMixin,
  CorePartOfProjectStoreMixin
], a => {
  const b = a.prototype;
  class c extends a {
    static get defaultConfig() {
      return { 'modelClass': CoreResourceMixin };
    }
    joinProject() {
      let e;
      (e = this.assignmentStore) == null ? void 0 : e.linkAssignments(this, 'resource');
    }
    afterLoadData() {
      let e, f;
      (e = b.afterLoadData) == null ? void 0 : e.call(this);
      (f = this.assignmentStore) == null ? void 0 : f.linkAssignments(this, 'resource');
    }
    clear(e) {
      let f;
      b.clear.call(this, e);
      (f = this.assignmentStore) == null ? void 0 : f.unlinkAssignments('resource');
    }
  }
  return c;
}) {
};
const PartOfBaseProject_default = a => class b extends a {
  static get $name() {
    return 'PartOfBaseProject';
  }
  get assignmentStore() {
    return this.project.assignmentStore;
  }
  get calendarManagerStore() {
    return this.project.calendarManagerStore;
  }
  get dependencyStore() {
    return this.project.dependencyStore;
  }
  get eventStore() {
    return this.project.eventStore;
  }
  get resourceStore() {
    return this.project.resourceStore;
  }
};
const EngineMixin2 = PartOfProject_default(CoreResourceStoreMixin.derive(AjaxStore));
const ResourceStore = class extends ResourceStoreMixin_default(EngineMixin2) {
  static $name = 'ResourceStore';
  static get defaultConfig() {
    return { 'modelClass': ResourceModel };
  }
};
ResourceStore._$name = 'ResourceStore';
const DateRanges = class {
  promises = [];
  ranges = [];
  constructor() {
  }
  addPromise(a) {
    if (a == null ? void 0 : a.then) {
      this.promises.push(a);
      a.then(() => {
        this.promises.splice(this.promises.indexOf(a), 1);
      });
    }
  }
  addRange(a, b, c) {
    this.addPromise(c);
    const e = this.getRange(a);
    if ((e == null ? void 0 : e.endDate) < b) {
      e.endDate.setTime(b.getTime());
      return;
    }
    const f = this.getRange(b);
    if ((f == null ? void 0 : f.startDate) > a) {
      f.startDate.setTime(a.getTime());
      return;
    }
    a = new Date(a.getTime());
    b = new Date(b.getTime());
    this.ranges.push({
      'startDate': a,
      'endDate': b
    });
  }
  containsRange(a, b) {
    return this.ranges.some(c => DateHelper.timeSpanContains(c.startDate, c.endDate, a, b));
  }
  getRange(a) {
    return this.ranges.filter(b => DateHelper.betweenLesserEqual(a, b.startDate, b.endDate))[0];
  }
};
const DateStoreLazyLoadPlugin = class extends AjaxStoreLazyLoad.mixin(Delayable_default) {
  static configurable = {
    'bufferUnit': null,
    'bufferAmount': null,
    'dateFormat': 'YYYY-MM-DD'
  };
  static $name = 'DateStoreLazyLoadPlugin';
  static get pluginConfig() {
    let a;
    const b = {};
    for (const e in super.pluginConfig) {
      b[e] = [...super.pluginConfig[e]];
    }
    b.assign.push('updateProject');
    const c = b.assign.indexOf('lazyGetAt');
    if (c >= 0) {
      b.assign.splice(c, 1);
    }
    b.before = (a = b.before) != null ? a : [];
    b.before.push('getEventsAsArray');
    return b;
  }
  get project() {
    return this.client.project;
  }
  get resourceStore() {
    let a;
    return (a = this.project) == null ? void 0 : a.resourceStore;
  }
  updateProject(a) {
    let b;
    (b = this.$projectListenersDetacher) == null ? void 0 : b.call(this);
    if (a) {
      this.$projectListenersDetacher = a.ion({
        'resourceStoreChange': 'listenToResourceStore',
        'thisObj': this
      });
    } else {
      this.listenToResourceStore();
    }
  }
  listenToResourceStore() {
    let a, b;
    (a = this.$resourceStoreListenersDetacher) == null ? void 0 : a.call(this);
    if ((b = this.resourceStore) == null ? void 0 : b.lazyLoad) {
      this.$resourceStoreListenersDetacher = this.resourceStore.ion({
        'sort': 'clearLoaded',
        'filter': 'clearLoaded',
        'thisObj': this
      });
    }
  }
  getEventsAsArray({
    visibleDateRange: a,
    startDate: b,
    endDate: c,
    fromRowHeight: e,
    resourceRecord: f
  }) {
    if (!e) {
      this.bufferedLazyLoad({
        'startDate': b,
        'endDate': c,
        'visibleStart': a.startDate,
        'visibleEnd': a.endDate,
        'resourceRecord': f
      });
    }
  }
  get isLoading() {
    return super.isLoading || Object.values(this.loadQueue).some(a => a.promises.length);
  }
  internalOnBeforeAdd() {
  }
  internalOnChange() {
  }
  internalOnCommit() {
  }
  calculateDateRange({
    startDate: a,
    endDate: b,
    visibleStart: c,
    visibleEnd: e,
    resourceIndex: f
  }) {
    let g = this.getResourceQueue(f);
    if (c && e) {
      const {
          bufferAmount: j,
          bufferUnit: k,
          $bufferedResourceRecords: l,
          resourceStore: n
        } = this, o = k && j != null ? DateHelper.asMilliseconds(j, k) : e - c, p = (u, v) => {
          const w = DateHelper.clearTime(new Date(u.getTime() + v));
          if (v < 0 && w < a) {
            w.setTime(a.getTime());
          } else if (v > 0 && w > b) {
            w.setTime(b.getTime());
          }
          return w;
        }, q = p(c, o / -2), r = p(e, o / 2);
      if (q > r) {
        return false;
      }
      if (g == null ? void 0 : g.containsRange(q, r)) {
        let u = false;
        if ((l == null ? void 0 : l.length) > 1) {
          for (let v = 1; v < l.length; v++) {
            f = n.indexOf(l[v]);
            g = this.getResourceQueue(f);
            if (!(g == null ? void 0 : g.containsRange(q, r))) {
              u = true;
              break;
            }
          }
        }
        if (!u) {
          return false;
        }
      }
      a = p(c, o * -1);
      b = p(e, o);
    } else if (a > b) {
      return false;
    } else {
      a = new Date(a.getTime());
      b = new Date(b.getTime());
    }
    if (g) {
      const w = g.getRange(a);
      if (w) {
        a.setTime(w.endDate.getTime());
      } else {
        const z = g.getRange(b);
        if (z) {
          b.setTime(z.startDate.getTime());
        }
      }
    }
    return {
      'startDate': a,
      'endDate': b,
      'resourceIndex': f
    };
  }
  calculateResourceRange({
    resourceIndex: a,
    startDate: b,
    endDate: c
  }) {
    let e, f;
    const {
      resourceStore: g,
      $bufferedResourceRecords: i
    } = this;
    let j = 0, k = g.count - 1;
    if (g.lazyLoad) {
      const {chunkSize: l} = g.lazyLoad;
      j = a - l;
      k = a + l;
      if ((i == null ? void 0 : i.length) > l) {
        const n = g.indexOf(i.pop());
        if (n > k) {
          k = n + l;
        }
      }
      j = Math.max(j, 0);
      while (!g.records[j] && j < k) {
        j += 1;
      }
      while (!g.records[k] && j < k) {
        k -= 1;
      }
    }
    while (j < a && ((e = this.getResourceQueue(j)) == null ? void 0 : e.containsRange(b, c))) {
      j += 1;
    }
    while (k > a && ((f = this.getResourceQueue(k)) == null ? void 0 : f.containsRange(b, c))) {
      k -= 1;
    }
    return {
      'startIndex': j,
      'count': k - j + 1
    };
  }
  getResourceQueue(a) {
    if (a != null) {
      const {resourceStore: b} = this;
      if (!this.client.isTimeRangeStore && (b == null ? void 0 : b.lazyLoad) && b.isTree) {
        a = b.records[a].id;
      }
      return this.loadQueue[a];
    }
  }
  addToQueue({
    startDate: a,
    endDate: b,
    startIndex: startIndex = 0,
    count: count = 1,
    promise: c,
    resourceIds: e
  }) {
    const {loadQueue: f} = this, g = j => (f[j] || (f[j] = new DateRanges())).addRange(a, b, c);
    if (e) {
      e.forEach(g);
    } else {
      for (let j = startIndex; j < startIndex + count; j++) {
        g(j);
      }
    }
  }
  bufferedLazyLoad({resourceRecord: a}) {
    const b = this;
    if (!a) {
      b.doLazyLoad(...arguments);
    }
    if (b.$bufferedResourceRecords) {
      b.$bufferedResourceRecords.push(a);
    } else {
      b.$bufferedResourceRecords = [a];
      b.requestAnimationFrame(() => {
        b.doLazyLoad(...arguments);
        b.$bufferedResourceRecords = null;
      });
    }
  }
  async doLazyLoad({
    startDate: a,
    endDate: b,
    visibleStart: c,
    visibleEnd: e,
    resourceRecord: f
  }) {
    let g, i, j;
    const k = this, {
        client: l,
        dateFormat: n
      } = k, {resourceStore: o} = k.project, p = !l.isTimeRangeStore && o.lazyLoad && o.isTree, q = k.calculateDateRange({
        'startDate': a,
        'endDate': b,
        'visibleStart': c,
        'visibleEnd': e,
        'resourceIndex': f ? o.indexOf(f) : 0
      }), {resourceIndex: r} = q;
    if (!l.requestData || q === false || p && o.isLoading || r < 0) {
      return;
    }
    if (!l.isTimeRangeStore) {
      const z = k.calculateResourceRange(q);
      if (p) {
        const A = o.getRange(z.startIndex, z.startIndex + z.count + 1), B = A.filter(C => {
            let E;
            return !C.isPhantom && !((E = k.loadQueue[C.id]) == null ? void 0 : E.containsRange(q.startDate, q.endDate));
          });
        q.resourceIds = B.map(C => C.id);
      } else {
        Object.assign(q, z);
        if (o.filterParamName && o.isFiltered) {
          const {values: C} = o.filters;
          if (l.readUrl) {
            let {filterParamName: E} = o;
            if (l.filterParamName === E) {
              E = 'resource' + E;
            }
            q[E] = o.encodeFilterParams(C);
          } else {
            q.resourceFilters = o.filters.values;
          }
        }
        if (o.remoteSort && o.isSorted) {
          const {sorters: F} = o;
          if (l.readUrl) {
            let {sortParamName: G} = o;
            if (l.sortParamName === G) {
              G = 'resource' + G;
            }
            q[G] = o.encodeSorterParams(F);
          } else {
            q.resourceFilters = F;
          }
        }
      }
    }
    delete q.resourceIndex;
    if (l.isEventStore && !((j = (i = (g = l.crudManager) == null ? void 0 : g.transport) == null ? void 0 : i.load) == null ? void 0 : j.url)) {
      k.loadAssignmentStore(q);
    }
    k.triggerLazyLoadStart(q);
    let u = q;
    if (l.readUrl && n) {
      u = {
        ...q,
        'startDate': DateHelper.format(q.startDate, n),
        'endDate': DateHelper.format(q.endDate, n)
      };
    }
    const v = l.requestData(u);
    k.addToQueue({
      ...q,
      'promise': v
    });
    const {data: w} = await v;
    k.addData(w, q);
    k.triggerLazyLoadEnd(q, w);
  }
  addData(a, {
    resourceIds: b,
    startDate: c,
    endDate: e
  } = {}) {
    if ((a == null ? void 0 : a.length) && b) {
      const f = a.filter(g => !b.includes(g.resourceId));
      if (f.length) {
        this.addToQueue({
          'startDate': c,
          'endDate': e,
          'resourceIds': f.map(g => g.resourceId)
        });
      }
    }
    super.addData(a);
  }
  async loadAssignmentStore(a) {
    const {assignmentStore: b} = this.project, {lazyLoad: c} = b;
    if (c) {
      c.triggerLazyLoadStart();
      const e = b.requestData(a);
      c.addToQueue({
        ...a,
        'promise': e
      });
      const {data: f} = await e;
      c.addData(f);
      c.triggerLazyLoadEnd();
    }
  }
  load(a = {}) {
    let b;
    (b = this.beforeLoadCall) == null ? void 0 : b.call(this, a);
    this.clearLoaded();
    return this.doLazyLoad(a);
  }
  doDestroy() {
    let a, b;
    (a = this.$projectListenersDetacher) == null ? void 0 : a.call(this);
    (b = this.$resourceStoreListenersDetacher) == null ? void 0 : b.call(this);
    super.doDestroy();
  }
};
DateStoreLazyLoadPlugin._$name = 'DateStoreLazyLoadPlugin';
const EventStoreMixin_default = a => {
  let b;
  return b = class extends (a || Base) {
    static get defaultConfig() {
      return {
        'loadPriority': 100,
        'syncPriority': 200,
        'id': 'events',
        'removeUnassignedEvent': true,
        'singleAssignment': null
      };
    }
    construct(c) {
      super.construct(c);
      this.autoTree = true;
      if (this.singleAssignment) {
        this.usesSingleAssignment = true;
      }
      if (!this.modelClass.isEventModel) {
        throw new Error('The model for the EventStore must subclass EventModel');
      }
    }
    set filtersFunction(c) {
      super.filtersFunction = c;
    }
    get filtersFunction() {
      const c = super.filtersFunction;
      if (c && c !== FunctionHelper.returnTrue) {
        return e => e.isCreating || c(e);
      }
      return c;
    }
    getEventCounts(c) {
      const e = this, {
          filtersFunction: f,
          added: g
        } = e, i = e.getEvents({
          ...c,
          'storeFilterFn': e.isFiltered ? e.reapplyFilterOnAdd ? f : j => g.includes(j) ? e.indexOf(j) > -1 : f(j) : null,
          'dateMap': c.dateMap || true
        });
      i.forEach((j, k) => i.set(k, j.length));
      return i;
    }
    forEachScheduledEvent(c, e = this) {
      this.forEach(f => {
        const {
          startDate: g,
          endDate: i
        } = f;
        if (g && i) {
          return c.call(e, f, g, i);
        }
      });
    }
    getTotalTimeSpan() {
      let c = new Date(9999, 0, 1), e = new Date(0);
      this.forEach(f => {
        if (f.startDate) {
          c = DateHelper.min(f.startDate, c);
        }
        if (f.endDate) {
          e = DateHelper.max(f.endDate, e);
        }
      });
      c = c < new Date(9999, 0, 1) ? c : null;
      e = e > new Date(0) ? e : null;
      return this.lastTotalTimeSpan = {
        'startDate': c || null,
        'endDate': e || c || null
      };
    }
    isEventPersistable(c) {
      return c.isPersistable;
    }
    filterEventsForResource(c, e, f = this) {
      return c.getEvents(this).filter(e.bind(f));
    }
    getResourcesForEvent(c) {
      if (c.isOccurrence) {
        c = c.recurringTimeSpan;
      }
      return this.assignmentStore.getResourcesForEvent(c);
    }
    getEventsForResource(c) {
      return this.assignmentStore.getEventsForResource(c);
    }
    getAssignmentsForEvent(c) {
      return this.assignmentStore.getAssignmentsForEvent(c) || [];
    }
    getAssignmentsForResource(c) {
      return this.assignmentStore.getAssignmentsForResource(c) || [];
    }
    assignEventToResource(c, e, f = false) {
      return this.assignmentStore.assignEventToResource(c, e, void 0, f);
    }
    unassignEventFromResource(c, e) {
      this.assignmentStore.unassignEventFromResource(c, e);
    }
    reassignEventFromResourceToResource(c, e, f) {
      const g = this, i = Model.asId(f), j = g.assignmentStore.getAssignmentForEventAndResource(c, e);
      if (j) {
        j.resourceId = i;
      } else {
        g.assignmentStore.assignEventToResource(c, f);
      }
    }
    isEventAssignedToResource(c, e) {
      return this.assignmentStore.isEventAssignedToResource(c, e);
    }
    removeAssignmentsForEvent(c) {
      this.assignmentStore.removeAssignmentsForEvent(c);
    }
    removeAssignmentsForResource(c) {
      this.assignmentStore.removeAssignmentsForResource(c);
    }
    append(c) {
      return this.add(c);
    }
    get project() {
      return super.project;
    }
    set project(c) {
      let e, f;
      super.project = c;
      (e = this.updateProject) == null ? void 0 : e.call(this, c);
      this.detachListeners('project');
      if (c) {
        if ((f = c.assignmentStore) == null ? void 0 : f.isAssignmentStore) {
          this.attachToAssignmentStore(c.assignmentStore);
        }
        c.ion({
          'name': 'project',
          'assignmentStoreChange': 'onProjectAssignmentStoreChange',
          'thisObj': this,
          'prio': 200
        });
      }
    }
    get usesResourceIds() {
      let c, e;
      return (e = (c = this.modelClass.fieldMap) == null ? void 0 : c.resourceIds.persist) != null ? e : false;
    }
    get usesSingleAssignment() {
      if (this.isChained) {
        return this.masterStore.usesSingleAssignment;
      }
      return this._usesSingleAssignment;
    }
    set usesSingleAssignment(c) {
      this._usesSingleAssignment = c;
      const {assignmentStore: e} = this;
      if ((e == null ? void 0 : e.isStore) && !e.hasGenerateIdOverride) {
        e.modelClass.generateId = function () {
          if (this.singleAssignmentIdCounter == null) {
            this.singleAssignmentIdCounter = 0;
          }
          return 'a-' + ++this.singleAssignmentIdCounter;
        };
        e.hasGenerateIdOverride = true;
      }
    }
    processRecords(c, e, f) {
      let g;
      const {assignmentStore: i} = this, j = [];
      if (i) {
        i.skipInvalidateIndices = true;
      }
      c = super.processRecords(c, i && !((g = this.stm) == null ? void 0 : g.isRestoring) && (k => {
        const l = k.get('resourceId');
        if (!k.reassignedFromReplace && l != null) {
          if (!i.includesAssignment(k.id, l)) {
            j.push({
              'id': i.modelClass.generateId(''),
              'resourceId': l,
              'eventId': k.id
            });
          }
        }
        k.reassignedFromReplace = false;
      }) || void 0);
      if (i) {
        i.storage.invalidateIndices();
        i.skipInvalidateIndices = false;
        i.add(j, f);
      }
      return c;
    }
    joinRecordsToStore(c) {
      const {assignmentStore: e} = this;
      if (e) {
        e.skipInvalidateIndices = true;
        super.joinRecordsToStore(c);
        e.storage.invalidateIndices();
        e.skipInvalidateIndices = false;
      } else {
        super.joinRecordsToStore(c);
      }
    }
    processRecord(c, e = false) {
      let f, g;
      c = super.processRecord(c, e);
      const i = this, j = (g = i.assignmentStore) != null ? g : (f = i.crudManager) == null ? void 0 : f.assignmentStore, k = c.get('resourceId'), {resourceIds: l} = c;
      if ((l == null ? void 0 : l.length) && c.meta.skipEnforcingSingleAssignment !== false && i.usesResourceIds) {
        if (j) {
          j.add(l.filter(n => !j.some(o => o.eventId === c.id && o.resourceId === n)).map(n => ({
            'resource': n,
            'event': c
          })));
        } else {
          i.$processResourceIds = true;
        }
      } else if (k != null && !c.meta.skipEnforcingSingleAssignment) {
        const n = i.getById(c.id), o = n && n !== c && !e;
        if (o) {
          const p = j.storage.findItem('eventId', c.id);
          if (p == null ? void 0 : p.size) {
            const q = p.values().next().value;
            q.resource = k;
            c.reassignedFromReplace = true;
          }
        } else {
          i.$processResourceIds = true;
        }
        i.usesSingleAssignment = true;
      }
      return c;
    }
    processResourceIds() {
      let c, e, f;
      const g = this, i = (e = g.assignmentStore) != null ? e : (c = g.crudManager) == null ? void 0 : c.assignmentStore;
      if (g.$processResourceIds && (i == null ? void 0 : i.isAssignmentStore) && !(((f = g.project) == null ? void 0 : f.isSharingAssignmentStore) && g.isChained)) {
        const j = [];
        g.forEach(k => {
          const {
            resourceId: l,
            resourceIds: n,
            id: o
          } = k;
          if (l != null) {
            j.push({
              'id': i.modelClass.generateId(''),
              'resourceId': l,
              'eventId': o
            });
          } else if (n == null ? void 0 : n.length) {
            n.forEach(p => {
              j.push({
                'id': i.modelClass.generateId(''),
                'resourceId': p,
                'eventId': o
              });
            });
          }
        }, g, { 'includeFilteredOutRecords': true });
        i.useRawData = {
          'disableDefaultValue': true,
          'disableDuplicateIdCheck': true,
          'disableTypeConversion': true
        };
        i.usesSingleAssignment = false;
        i.verifyNoGeneratedIds = false;
        i.data = j;
        i.usesSingleAssignment = true;
        g.$processResourceIds = false;
      }
    }
    loadTreeData() {
      super.loadTreeData(...arguments);
      this.processResourceIds();
    }
    loadData() {
      super.loadData(...arguments);
      this.processResourceIds();
    }
    onBeforeRemoveAssignment({records: c}) {
      let e;
      const f = this;
      if (f.removeUnassignedEvent && !f.isRemoving && !f.isSettingData && !((e = f.stm) == null ? void 0 : e.isRestoring) && !f.usesSingleAssignment && !f.assignmentStore.isSyncingDataOnLoad && !f.resourceStore.isSyncingDataOnLoad) {
        const g = new Set();
        c.forEach(i => {
          const {event: j} = i;
          if (j && !j.isRemoved && j.assignments.every(k => c.includes(k))) {
            g.add(j);
          }
        });
        if (g.size) {
          f.remove([...g]);
        }
      }
    }
    onProjectAssignmentStoreChange({store: c}) {
      this.attachToAssignmentStore(c);
    }
    attachToAssignmentStore(c) {
      const e = this;
      e.detachListeners('assignmentStore');
      if (c) {
        e.processResourceIds();
        c.ion({
          'name': 'assignmentStore',
          'addPreCommit'({records: f}) {
            if (!e.isSettingData && !e.isAssigning) {
              if (e.usesSingleAssignment) {
                f.forEach(g => {
                  const {event: i} = g;
                  if ((i == null ? void 0 : i.isEvent) && i.resourceId !== g.resourceId) {
                    i.meta.isAssigning = true;
                    i.set('resourceId', g.resourceId);
                    i.meta.isAssigning = false;
                  }
                });
              } else if (e.usesResourceIds) {
                f.forEach(g => {
                  let i;
                  const {event: j} = g;
                  if (j == null ? void 0 : j.isEvent) {
                    j.meta.isAssigning = true;
                    const k = (i = j.resourceIds) != null ? i : [];
                    if (!k.includes(g.resourceId)) {
                      j.resourceIds = [
                        ...k,
                        g.resourceId
                      ];
                    }
                    j.meta.isAssigning = false;
                  }
                });
              } else {
                e.updateChainedStores();
              }
            }
          },
          'beforeRemove': 'onBeforeRemoveAssignment',
          'removePreCommit'({records: f}) {
            if (!e.isSettingData && !e.isAssigning) {
              if (e.usesSingleAssignment) {
                f.forEach(g => {
                  let i;
                  (i = e.getById(g.eventId)) == null ? void 0 : i.set('resourceId', null);
                });
              } else if (e.usesResourceIds) {
                f.forEach(({
                  event: g,
                  resourceId: i
                }) => {
                  const j = g.resourceIds.slice(), k = j == null ? void 0 : j.indexOf(i);
                  if (k >= 0) {
                    j.splice(k, 1);
                    g.resourceIds = j;
                  }
                });
              } else {
                e.updateChainedStores();
                ;
              }
            }
          },
          'removeAllPreCommit'() {
            if (!e.isSettingData && !e.isAssigning) {
              if (e.usesSingleAssignment) {
                e.allRecords.forEach(f => f.set('resourceId', null));
              } else if (e.usesResourceIds) {
                e.allRecords.forEach(f => {
                  f.resourceIds = [];
                });
              } else {
                e.updateChainedStores();
              }
            }
          },
          'update'({
            record: f,
            changes: g
          }) {
            if ('resourceId' in g) {
              const {event: i} = f;
              if (e.usesSingleAssignment) {
                i.meta.isAssigning = true;
                i.set('resourceId', g.resourceId.value);
                i.meta.isAssigning = false;
              } else if (e.usesResourceIds) {
                i.meta.isAssigning = true;
                const j = i.resourceIds.slice(), k = j == null ? void 0 : j.indexOf(g.resourceId.oldValue);
                if (k >= 0) {
                  j.splice(k, 1);
                }
                if (!(j == null ? void 0 : j.includes(g.resourceId.value))) {
                  j.push(g.resourceId.value);
                  i.resourceIds = j;
                }
                i.meta.isAssigning = false;
              } else {
                e.updateChainedStores();
              }
            }
          },
          'change'({
            action: f,
            records: g
          }) {
            if (f === 'dataset') {
              if (e.usesResourceIds) {
                g.forEach(({
                  event: i,
                  resourceId: j
                }) => {
                  let k;
                  const l = (k = i.resourceIds) != null ? k : [];
                  if (!l.includes(j)) {
                    l.push(j);
                    i.meta.isAssigning = true;
                    i.setData('resourceIds', l);
                    i.meta.isAssigning = false;
                  }
                });
              }
              if (!e.usesSingleAssignment && !e.usesResourceIds) {
                e.updateChainedStores();
              }
            }
          },
          'thisObj': e
        });
      }
    }
    set data(c) {
      const e = this;
      e.isSettingData = true;
      if (e.usesSingleAssignment && !e.syncDataOnLoad && !e.isChained) {
        e.assignmentStore.removeAll(true);
      }
      super.data = c;
      e.isSettingData = false;
    }
    trigger(c, e) {
      let f;
      const {changes: g} = e || {};
      if (g && 'resourceId' in g && Object.keys(g).length === 1 && !((f = this.stm) == null ? void 0 : f.isRestoring)) {
        e.isAssign = true;
      }
      return super.trigger(...arguments);
    }
    remove(c, ...e) {
      let f, g;
      const i = super.remove(c, ...e);
      if (i.length && this.usesSingleAssignment) {
        for (const j of i) {
          if (!j.isOccurrence) {
            (g = this.assignmentStore || ((f = this.crudManager) == null ? void 0 : f.assignmentStore)) == null ? void 0 : g.remove(j.assignments, true);
          }
        }
      }
      return i;
    }
  }, __publicField(b, '$name', 'EventStoreMixin'), __publicField(b, '$lazyLoadPluginClass', DateStoreLazyLoadPlugin), b;
};
const returnTrue2 = () => true;
const notRecurring = a => !a.isRecurring;
const GetEventsMixin_default = a => {
  let b;
  return b = class extends a {
    getEvents({
      filter: c,
      date: e,
      startDate: f,
      endDate: g,
      startOnly: i,
      includeOccurrences: j,
      allowPartial: k,
      onlyAssigned: onlyAssigned = false,
      dateMap: dateMap = false,
      dayTime: dayTime = null,
      getDateIndex: l,
      fromRowHeight: n
    }) {
      const o = this, p = arguments[0], {
          lastDateRange: q,
          added: r,
          filtersFunction: u
        } = o, v = c;
      if (onlyAssigned) {
        p.filter = v ? A => v(A) && A.resources.length : A => A.resources.length;
      }
      if (!('startDate' in p)) {
        f = p.startDate = e;
      }
      if (!('includeOccurrences' in p)) {
        j = p.includeOccurrences = true;
      }
      if (!('allowPartial' in p)) {
        k = p.allowPartial = !i;
      }
      p.storeFilterFn = o.isFiltered && !p.ignoreFilters ? o.reapplyFilterOnAdd ? u : A => r.includes(A) ? o.indexOf(A) > -1 : u(A) : null;
      if (!g) {
        if (f) {
          g = p.endDate = DateHelper.clearTime(f);
          g.setDate(g.getDate() + 1);
        } else {
          if (j) {
            throw new Error('getEvents MUST be passed startDate and endDate if recurring occurrences are requested');
          }
          p.dateFilter = returnTrue2;
        }
      }
      if (!p.dateFilter) {
        if (i) {
          p.dateFilter = A => {
            const B = A.hasBatchedChange('startDate') ? A.get('startDate') : A.startDate;
            return B && !(DateHelper.clearTime(B) - f);
          };
        } else if (k) {
          p.dateFilter = A => {
            const B = A.hasBatchedChange('startDate') ? A.get('startDate') : A.startDate, C = A.hasBatchedChange('endDate') ? A.get('endDate') : A.endDate || B, E = !(B - C);
            return B && (E ? DateHelper.betweenLesserEqual(B, f, g) : DateHelper.intersectSpans(B, C, f, g));
          };
        } else {
          p.dateFilter = A => {
            const B = A.hasBatchedChange('startDate') ? A.get('startDate') : A.startDate, C = A.hasBatchedChange('endDate') ? A.get('endDate') : A.endDate || B;
            return B && B >= f && C <= g;
          };
        }
      }
      const w = {
          'startDate': f,
          'endDate': g
        }, z = Boolean(!q || (q.startDate - w.startDate || q.endDate - w.endDate));
      o.processConfiguredListeners();
      o.trigger('loadDateRange', {
        'old': q || {},
        'new': Objects.clone(w),
        'changed': z
      });
      o.lastDateRange = Objects.clone(w);
      return dateMap ? o.getEventsAsMap(p) : o.getEventsAsArray(p);
    }
    getEventsAsArray({
      filter: c,
      date: f,
      resourceRecord: g,
      startDate: startDate = f,
      endDate: j,
      startOnly: k,
      includeOccurrences: includeOccurrences = true,
      dayTime: dayTime = null,
      dateFilter: l,
      storeFilterFn: n,
      getDateIndex: o
    }) {
      const p = this, q = [], r = n ? p.count : p.totalCount;
      if (r) {
        let u = g ? p.getEventsForResource(g) : null;
        if (!g) {
          const v = p.useDayIndex(dayTime), w = new Set(), z = k ? 'startDate' : 'date';
          p.recurringEvents.forEach(A => {
            if (v.dayTime.startOfDay(A.startDate) <= startDate) {
              w.add(A);
            }
          });
          for (const A = new Date(startDate); A < j; A.setDate(A.getDate() + 1)) {
            const B = v.get(o ? o(A) : z, A);
            B == null ? void 0 : B.forEach(C => w.add(C));
          }
          u = [...w];
        }
        if (n) {
          u = u.filter(n);
        }
        for (let C = 0, {length: E} = u; C < E; C++) {
          const F = u[C];
          if (includeOccurrences && F.isRecurring) {
            q.push.apply(q, F.getOccurrencesForDateRange(startDate, j).filter(l));
          } else if (l(F)) {
            q.push(F);
          }
        }
      }
      return c ? q.filter(c) : q;
    }
    getEventsAsMap({
      filter: c,
      date: f,
      resourceRecord: g,
      startDate: startDate = f,
      endDate: j,
      startOnly: k,
      includeOccurrences: includeOccurrences = true,
      dateMap: l,
      dayTime: dayTime = null,
      storeFilterFn: n,
      getDateIndex: o
    }) {
      let p;
      const q = this;
      if (l == null ? void 0 : l.clear) {
        l.clear();
      } else {
        l = new Map();
      }
      if (q.count) {
        const r = q.useDayIndex(dayTime), u = k ? 'startDate' : 'date', v = [], w = A => (!c || c(A)) && (!n || n(A)), z = A => notRecurring(A) && w(A);
        dayTime = r.dayTime;
        if (g) {
          throw new Error('Querying for events for a resource and returning a date-keyed Map is not supported');
        } else {
          (p = q.recurringEvents) == null ? void 0 : p.forEach(A => {
            if (dayTime.startOfDay(A.startDate) < j) {
              v.push(A);
            }
          });
          for (const A = new Date(startDate); A < j; A.setDate(A.getDate() + 1)) {
            let [B, C] = r.get(o ? o(A) : u, A, true);
            if (B == null ? void 0 : B.size) {
              B = [...B].filter(z);
              if (B.length) {
                (l.get(C) || l.set(C, []).get(C)).push(...B);
              }
            }
          }
        }
        for (let E = 0, {length: F} = v; E < F; E++) {
          const G = v[E], H = (includeOccurrences ? G.getOccurrencesForDateRange(startDate, j) : [G]).filter(w), I = DateHelper.add(j, 1, 'day');
          for (let J, K = 0, {length: L} = H; K < L; K++) {
            const N = H[K], O = dayTime.startOfDay(N.startDate), P = o ? o(O) : k ? 'startDate' : 'date', Q = P === 'startDate' || !N.durationMS ? DateHelper.add(O, 1, 'day') : DateHelper.min(N.endDate || DateHelper.add(N.startDate, N.duration, N.durationUnit), I);
            for (; O < Q; O.setDate(O.getDate() + 1)) {
              const R = dayTime.dateKey(O);
              (J = l.get(R)) || l.set(R, J = []);
              J.push(N);
            }
          }
        }
      }
      return l;
    }
    isDateRangeAvailable(c, e, f, g) {
      let i;
      if (arguments.length === 1) {
        e = c.endDate;
        f = c.excludeEvent;
        g = c.resourceRecord;
        i = c.allowOverlap;
        c = c.startDate;
      }
      g = Array.isArray(g) ? g : [g];
      return g.every(j => this.isResourceAvailable(c, e, f, j, i));
    }
    isResourceAvailable(c, e, f, g, i) {
      let j;
      if (g.data.generatedParent) {
        return false;
      }
      i = typeof g.allowOverlap === 'boolean' ? g.allowOverlap : i;
      if (i) {
        return true;
      }
      const k = this.getEventsForResource(g);
      if (((j = this.recurringEvents) == null ? void 0 : j.size) > 0) {
        k.push(...k.flatMap(o => o.occurrences));
      }
      const l = new Set(k);
      if (f) {
        f = ArrayHelper.asArray(f);
        f.forEach(o => {
          if (o.isAssignment) {
            const q = o.event, r = q.resources;
            r.forEach(u => {
              if (u.id !== o.resourceId) {
                this.getEventsForResource(u).forEach(v => l.add(v));
              }
            });
          }
          const p = o.isAssignment ? o.event : o;
          l.delete(p);
        });
      }
      const n = Array.from(l);
      if (c.getTime() === e.getTime()) {
        return !n.some(o => o.isScheduled && (o.duration === 0 ? o.startDate.getTime() === c.getTime() : DateHelper.intersectSpans(c, e, o.startDate, o.endDate)));
      }
      return !n.some(o => o.isScheduled && DateHelper.intersectSpans(c, e, o.startDate, o.endDate));
    }
  }, __publicField(b, '$name', 'GetEventsMixin'), b;
};
const indexNameMap = {
  'date': '_dateIndex',
  'startDate': '_startDateIndex'
};
const indexProps = Object.values(indexNameMap);
const emptyArray13 = Object.freeze([]);
const {MILLIS_PER_DAY: MILLIS_PER_DAY2} = DayTime;
const EventDayIndex = class {
  constructor(a, b) {
    this.dayTime = b || DayTime.MIDNIGHT;
    this.store = a;
    this.users = [this.dayTime];
  }
  add(a, b, c) {
    const e = this[indexNameMap[a]], f = this.dayTime.dateKey(b), g = e[f] || (e[f] = new Set());
    g.add(c);
  }
  addEvent(a) {
    let b, c, e;
    let f = (b = this.dayTime.startOfDay(a.startDate)) == null ? void 0 : b.getTime(), g;
    if (f) {
      g = (e = (c = a.endDate) == null ? void 0 : c.getTime()) != null ? e : f;
      this.add('startDate', f, a);
      do {
        this.add('date', f, a);
        f += MILLIS_PER_DAY2;
      } while (f < g);
    }
  }
  clear() {
    indexProps.forEach(a => this[a] = Object.create(null));
  }
  get(a, b, c) {
    !this.initialized && this.initialize();
    let e = this[indexNameMap[a]], f;
    if (b) {
      f = this.dayTime.dateKey(b);
      e = c ? [
        e[f],
        f
      ] : e[f];
    }
    return e;
  }
  initialize() {
    this.initialized = true;
    this.clear();
    this.sync('splice', this.store.storage.allValues);
  }
  invalidate() {
    this.initialized = false;
    indexProps.forEach(a => this[a] = null);
  }
  matches(a) {
    return this.dayTime.startShift === a.startShift;
  }
  remove(a, b, c) {
    const e = this[indexNameMap[a]], f = this.dayTime.dateKey(b), g = e[f];
    if (g) {
      g.delete(c);
    }
  }
  removeEvent(a, b, c) {
    let e, f;
    let g = (e = this.dayTime.startOfDay(b)) == null ? void 0 : e.getTime(), i;
    if (g) {
      i = (f = c == null ? void 0 : c.getTime()) != null ? f : g;
      this.remove('startDate', g, a);
      do {
        this.remove('date', g, a);
        g += MILLIS_PER_DAY2;
      } while (g < i);
    }
  }
  sync(a, b, c, e, f) {
    let g, j;
    b = b || emptyArray13;
    c = c || emptyArray13;
    const k = this, l = b.length, n = c.length, o = e == null ? void 0 : e.length;
    let p, q, r;
    if (!k.initialized) {
      return;
    }
    switch (a) {
    case 'clear':
      k.clear();
      break;
    case 'splice':
      if (o) {
        b = b.slice();
        c = c.slice();
        for (p = 0; p < o; p++) {
          c.push(e[p][0]);
          b.push(e[p][1]);
        }
      }
      if (n) {
        for (p = 0; p < n; p++) {
          r = c[p];
          k.removeEvent(r, r.startDate, r.endDate);
        }
      }
      if (l) {
        for (p = 0; p < l; p++) {
          q = b[p];
          if (q.isScheduled && !q.isParent) {
            k.addEvent(q);
          }
        }
      }
      break;
    case 'reschedule':
      r = b[0];
      k.removeEvent(r, ((g = f.startDate) == null ? void 0 : g.oldValue) || r.startDate, ((j = f.endDate) == null ? void 0 : j.oldValue) || r.endDate);
      k.sync('splice', b);
      break;
    }
  }
  register(a) {
    this.users.push(a);
  }
  unregister(a) {
    const {users: b} = this, c = b.indexOf(a);
    if (c > -1) {
      b.splice(c, 1);
    }
    return !b.length;
  }
};
const proto3 = EventDayIndex.prototype;
indexProps.forEach(a => proto3[a] = null);
proto3.initialized = false;
EventDayIndex._$name = 'EventDayIndex';
const {MIDNIGHT} = DayTime;
const DayIndexMixin_default = a => {
  let b;
  return b = class extends a {
    construct(c) {
      super.construct(c);
      this.dayIndices = null;
    }
    afterLoadData() {
      let c;
      this.syncIndices('splice', this.storage.allValues);
      (c = super.afterLoadData) == null ? void 0 : c.call(this);
    }
    onDataChange({
      action: c,
      added: e,
      removed: f,
      replaced: g
    }) {
      this.syncIndices(c, e, f, g);
      super.onDataChange(...arguments);
    }
    onDataReplaced(c, e) {
      this.syncIndices('clear');
      this.syncIndices('splice', this.storage.values);
      super.onDataReplaced(c, e);
    }
    onModelChange(c, e, f, g, i) {
      if ('startDate' in f || 'endDate' in f) {
        this.syncIndices('reschedule', [c], null, null, f);
      }
      super.onModelChange(...arguments);
    }
    invalidateDayIndices() {
      let c;
      (c = this.dayIndices) == null ? void 0 : c.forEach(e => e.invalidate());
    }
    registerDayIndex(c) {
      const e = this, f = e.dayIndices || (e.dayIndices = []);
      let g, j;
      for (j = 0; !g && j < f.length; ++j) {
        if (f[j].matches(c)) {
          (g = f[j]).register(c);
        }
      }
      !g && f.push(g = new EventDayIndex(e, c));
      return g;
    }
    syncIndices(...c) {
      let e;
      (e = this.dayIndices) == null ? void 0 : e.forEach(f => f.sync(...c));
    }
    unregisterDayIndex(c) {
      const e = this, {dayIndices: f} = e;
      for (let g = f == null ? void 0 : f.length; g-- > 0;) {
        if (f[g].matches(c)) {
          if (f[g].unregister(c)) {
            f.splice(g, 1);
          }
          break;
        }
      }
    }
    useDayIndex(c) {
      const e = this, {dayIndices: f} = e;
      c = c || MIDNIGHT;
      for (let g = 0; f && g < f.length; ++g) {
        if (f[g].matches(c)) {
          return f[g];
        }
      }
      if (c.startShift) {
        throw new Error('No day index registered for ' + c + ' on ' + e.id);
      }
      return e.registerDayIndex(MIDNIGHT);
    }
  }, __publicField(b, '$name', 'DayIndexMixin'), b;
};
const emptyArray14 = Object.freeze([]);
const RecurringTimeSpansMixin_default = a => class b extends (a || Base) {
  static get $name() {
    return 'RecurringTimeSpansMixin';
  }
  construct(...c) {
    const e = this;
    e.globalOccurrences = new Map();
    e.recurringEvents = new Set();
    e.ion({
      'removeAll'() {
        e.globalOccurrences.clear();
      }
    });
    super.construct(...c);
  }
  afterLoadData() {
    let c;
    this.globalOccurrences.clear();
    this.refreshRecurringEventsCache('clear');
    this.refreshRecurringEventsCache('splice', this.storage.allValues);
    (c = super.afterLoadData) == null ? void 0 : c.call(this);
  }
  onDataChange({
    action: c,
    added: e,
    removed: f,
    replaced: g
  }) {
    this.refreshRecurringEventsCache(c, e, f, g);
    super.onDataChange(...arguments);
  }
  refreshRecurringEventsCache(c, e = emptyArray14, f = emptyArray14, g) {
    const j = this, {recurringEvents: k} = j, l = g == null ? void 0 : g.length;
    switch (c) {
    case 'clear':
      k.clear();
      break;
    case 'splice': {
        if (l) {
          e = e.slice();
          f = f.slice();
          for (let p = 0; p < l; p++) {
            f.push(g[p][0]);
            e.push(g[p][1]);
          }
        }
        const n = e.length, o = f.length;
        if (o && k.size) {
          for (let q = 0; q < o; q++) {
            k.delete(f[q]);
          }
        }
        if (n) {
          for (let r = 0; r < n; r++) {
            const u = e[r];
            if (u.isRecurring) {
              k.add(u);
            }
          }
        }
        break;
      }
    }
  }
  getById(c) {
    let e = super.getById(c);
    if (!e) {
      e = this.globalOccurrences.get(this.modelClass.asId(c));
    }
    return e;
  }
  onModelChange(c, e, f, g, i) {
    const j = !g && this.isRecurrenceRelatedFieldChange(c, f);
    if (j) {
      c.removeOccurrences();
    }
    super.onModelChange(...arguments);
    if (j) {
      const k = {
        'action': 'batch',
        'records': this.storage.values
      };
      this.trigger('refresh', k);
      this.trigger('change', k);
    }
  }
  isRecurrenceRelatedFieldChange(c, e) {
    return c.isRecurring || 'recurrenceRule' in e;
  }
  getOccurrencesForTimeSpan(c, e, f) {
    const g = [];
    if (c.isRecurring) {
      c.recurrence.forEachOccurrence(e, f, i => g.push(i));
    }
    return g;
  }
  set data(c) {
    this.globalOccurrences.clear();
    super.data = c;
  }
  getRecurringTimeSpans() {
    return [...this.recurringEvents];
  }
};
const RecurringEventsMixin_default = a => class b extends RecurringTimeSpansMixin_default(a || Base) {
  static get $name() {
    return 'RecurringEventsMixin';
  }
  getRecurringEvents() {
    return this.getRecurringTimeSpans();
  }
  isEventPersistable(c) {
    return super.isEventPersistable(c) && (!c.supportsRecurring || !c.isOccurrence);
  }
};
const dayParseRegExp = /^([+-]?[0-9])?(SU|MO|TU|WE|TH|FR|SA)$/;
const days = [
  'SU',
  'MO',
  'TU',
  'WE',
  'TH',
  'FR',
  'SA'
];
const RecurrenceDayRuleEncoder = class extends Base {
  static decodeDay(a) {
    let b, c;
    if (b = dayParseRegExp.exec(a)) {
      c = [days.indexOf(b[2])];
      if (b[1]) {
        b[1] = parseInt(b[1], 10);
        c.push(b[1]);
      }
    }
    return c;
  }
  static encodeDay(a) {
    let b;
    if (Array.isArray(a)) {
      [a, b] = a;
    }
    return (b ? b.toString() : '') + days[a];
  }
  static decode(a) {
    const b = [];
    let c;
    if (a) {
      for (let e = 0; e < a.length; e++) {
        if (c = this.decodeDay(a[e])) {
          b.push(c);
        }
      }
    }
    return b;
  }
  static encode(a) {
    const b = [];
    let c;
    if (a) {
      for (let e = 0; e < a.length; e++) {
        if (c = this.encodeDay(a[e])) {
          b.push(c);
        }
      }
    }
    return b;
  }
};
RecurrenceDayRuleEncoder._$name = 'RecurrenceDayRuleEncoder';
const frequencyToUnitRe = /ly$/i;
const frequencyToUnit = a => {
  const b = a.replace(frequencyToUnitRe, '');
  return b === 'DAI' ? 'DAY' : b;
};
const fn = (a, b, c, e) => e.buildOccurrence(a, c);
const captureLastOccurrence = a => lastOccurrenceDate = a;
let lastOccurrenceDate;
const AbstractRecurrenceIterator = class extends Base {
  static MAX_OCCURRENCES_COUNT = 1000000;
  static frequency = 'NONE';
  static processIterationConfig(a) {
    const {recurrence: b} = a, {
        frequency: c,
        interval: e,
        timeSpan: f,
        endDate: g,
        count: i
      } = b;
    if (!a.syncingStartDate && !f.meta.isSyncedWithRule) {
      const l = DateHelper.add(f.startDate, e, frequencyToUnit(c)), n = DateHelper.min(l, a.endDate || l);
      this.forEachDate({
        'syncingStartDate': true,
        'startDate': f.startDate,
        'endDate': n,
        'recurrence': b,
        'fn': fn
      });
    }
    const j = f.startDate;
    let {
      startDate: startDate = j,
      endDate: endDate = g
    } = a;
    if (startDate < j) {
      startDate = j;
    }
    if (g) {
      if (!endDate || endDate > g) {
        endDate = g;
      }
    } else if (i && startDate > j) {
      this.forEachDate({
        'recurrence': b,
        'fn': captureLastOccurrence
      });
      if (!endDate || endDate > lastOccurrenceDate) {
        endDate = new Date(lastOccurrenceDate.getTime() + 1);
      }
    }
    const k = startDate;
    if (!a.startOnly) {
      startDate = new Date(DateHelper.max(DateHelper.add(startDate, -e, frequencyToUnit(c)), j));
    }
    return Object.assign({
      'extraArgs': [],
      'startOnly': !Boolean(f.durationMS)
    }, a, {
      'startDate': startDate,
      'endDate': endDate,
      'timeSpan': f,
      'timeSpanStart': j,
      'earliestVisibleDate': k,
      'endDateMS': endDate == null ? void 0 : endDate.getTime(),
      'timeSpanStartMS': j.getTime(),
      'earliestVisibleDateMS': k.getTime(),
      'durationMS': f.durationMS,
      'spansStart': startDate <= j && endDate > j
    });
  }
  static getOccurrenceIndex(a) {
    if (a.isOccurrence) {
      return DateHelper.diff(a.recurringTimeSpan.startDate, a.startDate, frequencyToUnit(a.recurringTimeSpan.recurrence.frequency));
    }
  }
  static getNthDayInPeriod(a, b, c, e) {
    let f, g, i;
    if (e) {
      if (e > 0) {
        g = 1;
        i = a;
      } else {
        g = -1;
        i = b;
      }
      const j = c - i.getDay();
      if (g * j < 0) {
        e += g;
      }
      f = new Date(i);
      f.setDate(i.getDate() + (e - g) * 7 + j);
      if (f < a || f > b) {
        f = null;
      }
    }
    return f;
  }
  static buildDate(a, b, c) {
    const e = new Date(a, b, c);
    if (e.getFullYear() === a && e.getMonth() === b && e.getDate() === c) {
      return e;
    }
  }
  static isValidPosition(a) {
    return Boolean(a);
  }
  static forEachDateAtPositions(a, b, c, e) {
    const f = a.length, g = {};
    for (let j = 0; j < b.length; j++) {
      const k = b[j];
      if (this.isValidPosition(k)) {
        const l = k > 0 ? a[k - 1] : a[f + k];
        if (l && !g[l.getTime()]) {
          g[l.getTime()] = true;
          if (c.call(e, l) === false) {
            return false;
          }
        }
      }
    }
  }
  static isInView(a, b, c, e, f) {
    return (a ? b >= c : b.valueOf() + e > c) && !f.hasException(b);
  }
  static isInViewMS(a, b, c, e, f, g) {
    return (a ? c >= e : c + f > e) && !g.hasException(b);
  }
};
AbstractRecurrenceIterator._$name = 'AbstractRecurrenceIterator';
const DailyRecurrenceIterator = class extends AbstractRecurrenceIterator {
  static frequency = 'DAILY';
  static forEachDate(a) {
    const {
        startOnly: b,
        startDate: c,
        endDate: e,
        endDateMS: f,
        timeSpan: g,
        timeSpanStart: i,
        earliestVisibleDateMS: j,
        durationMS: k,
        spansStart: l,
        recurrence: n,
        fn: o,
        extraArgs: p,
        scope: scope = this
      } = this.processIterationConfig(a), {interval: q} = n, r = c - i, u = q * 86400000, v = Math.floor(r / u);
    let {count: w} = n, z = 0, A = DateHelper.add(i, v * q, 'day'), B = A.getTime();
    if (!e && !w) {
      w = this.MAX_OCCURRENCES_COUNT;
    }
    while (!f || B < f) {
      const C = this.isInViewMS(b, A, B, j, k, g);
      z++;
      if (w && z > w || f && B >= f || C && o.apply(scope, [
          A,
          z,
          z === 1 && l,
          g,
          ...p
        ]) === false) {
        break;
      }
      A = DateHelper.add(A, q, 'day');
      B = A.getTime();
    }
  }
};
DailyRecurrenceIterator._$name = 'DailyRecurrenceIterator';
const WeeklyRecurrenceIterator = class extends AbstractRecurrenceIterator {
  static frequency = 'WEEKLY';
  static forEachDate(a) {
    const {
        startOnly: b,
        startDate: c,
        endDateMS: e,
        timeSpan: f,
        timeSpanStart: g,
        timeSpanStartMS: j,
        earliestVisibleDateMS: k,
        durationMS: l,
        spansStart: n,
        recurrence: o,
        fn: p,
        extraArgs: q,
        scope: scope = this
      } = this.processIterationConfig(a), {
        interval: r,
        days: u
      } = o, {weekStartDay: v} = DateHelper, w = g.getHours(), z = g.getMinutes(), A = g.getSeconds(), B = g.getMilliseconds();
    let C = 0, {count: E} = o, F = RecurrenceDayRuleEncoder.decode(u), G, H;
    if (!(F == null ? void 0 : F.length)) {
      F = [[g.getDay()]];
    }
    if (v > 0) {
      for (let I = 0; I < F.length; I++) {
        if (v > F[I][0]) {
          F[I][0] = 7 - v - F[I][0];
        } else {
          F[I][0] -= v;
        }
      }
    }
    F.sort((J, K) => J[0] - K[0]);
    G = DateHelper.getNext(E || r > 1 ? g : c, 'week', 0);
    if (!e && !E) {
      E = this.MAX_OCCURRENCES_COUNT;
    }
    while (!e || G.getTime() < e) {
      for (let J = 0; J < F.length; J++) {
        H = new Date(G.getFullYear(), G.getMonth(), G.getDate() + F[J][0], w, z, A, B);
        const K = H.getTime();
        if (K >= j) {
          const L = this.isInViewMS(b, H, K, k, l, f);
          C++;
          if (E && C > E || e && K >= e || L && p.apply(scope, [
              H,
              C,
              C === 1 && n,
              f,
              ...q
            ]) === false) {
            return;
          }
        }
      }
      G = DateHelper.getNext(G, 'week', r);
    }
  }
};
WeeklyRecurrenceIterator._$name = 'WeeklyRecurrenceIterator';
const MonthlyRecurrenceIterator = class extends AbstractRecurrenceIterator {
  static frequency = 'MONTHLY';
  static getNthDayOfMonth(a, b) {
    const c = DateHelper.daysInMonth(a);
    let e = null;
    if (b && Math.abs(b) <= c) {
      e = new Date(a.getFullYear(), a.getMonth(), b < 0 ? c + b + 1 : b);
    }
    return e;
  }
  static isValidPosition(a) {
    return a && Math.abs(a) > 0 && Math.abs(a) <= 31;
  }
  static forEachDate(a) {
    const {
        startOnly: b,
        startDate: c,
        endDate: e,
        timeSpan: f,
        timeSpanStart: g,
        earliestVisibleDate: j,
        durationMS: k,
        spansStart: l,
        recurrence: n,
        fn: o,
        extraArgs: p,
        scope: scope = this
      } = this.processIterationConfig(a), {
        interval: q,
        days: r,
        count: u,
        positions: v
      } = n, w = RecurrenceDayRuleEncoder.decode(r), z = v && v.length, A = {};
    let {monthDays: B} = n, C = 0, E, F, G, H, I, J;
    F = DateHelper.startOf(u || q > 1 ? g : c, 'month');
    G = new Date(DateHelper.getNext(F, 'month', 1) - 1);
    if (!(B && B.length) && !(w && w.length)) {
      B = [g.getDate()];
    }
    if (w && w.length) {
      w.forEach(K => {
        if (K[1]) {
          E = E || {};
          E[K[0]] = K[1];
        }
      });
    }
    while ((!e || e > F) && (!u || C < u)) {
      H = [];
      if (w == null ? void 0 : w.length) {
        w.forEach(K => {
          const L = K[0];
          let N = 1, O = 53;
          if (K[1]) {
            N = O = K[1];
          }
          for (J = N; J <= O; J++) {
            if (I = this.getNthDayInPeriod(F, G, L, J)) {
              I = DateHelper.copyTimeValues(I, g);
              if (!A[I.getTime()]) {
                A[I.getTime()] = true;
                H.push(I);
              }
            }
          }
        });
        H.sort((K, L) => K - L);
        if (!z) {
          for (J = 0; J < H.length; J++) {
            I = H[J];
            if (I >= g) {
              const K = this.isInView(b, I, j, k, f);
              C++;
              if (u && C > u || e && I >= e || K && o.apply(scope, [
                  I,
                  C,
                  C === 1 && l,
                  f,
                  ...p
                ]) === false) {
                return false;
              }
            }
          }
        }
      } else {
        const L = [];
        for (J = 0; J < B.length; J++) {
          if ((I = this.getNthDayOfMonth(F, B[J])) && !A[I.getTime()]) {
            A[I.getTime()] = true;
            L.push(I);
          }
        }
        L.sort((N, O) => N - O);
        for (J = 0; J < L.length; J++) {
          I = DateHelper.copyTimeValues(L[J], g);
          if (z) {
            H.push(I);
          } else if (I >= g) {
            const N = this.isInView(b, I, j, k, f);
            C++;
            if (u && C > u || e && I >= e || N && o.apply(scope, [
                I,
                C,
                C === 1 && l,
                f,
                ...p
              ]) === false) {
              return;
            }
          }
        }
      }
      if (z && H.length) {
        if (this.forEachDateAtPositions(H, v, O => {
            if (O >= g) {
              const P = b ? O >= j : O.valueOf() + k > j && !f.hasException(O);
              C++;
              if (u && C > u || e && O >= e || P && o.apply(scope, [
                  O,
                  C,
                  C === 1 && l,
                  f,
                  ...p
                ]) === false) {
                return false;
              }
            }
          }) === false) {
          return false;
        }
      }
      F = DateHelper.getNext(F, 'month', q);
      G = new Date(DateHelper.getNext(F, 'month', 1) - 1);
    }
  }
};
MonthlyRecurrenceIterator._$name = 'MonthlyRecurrenceIterator';
const YearlyRecurrenceIterator = class extends AbstractRecurrenceIterator {
  static frequency = 'YEARLY';
  static forEachDate(a) {
    const {
        startOnly: b,
        startDate: c,
        endDate: e,
        timeSpan: f,
        timeSpanStart: g,
        earliestVisibleDate: j,
        durationMS: k,
        spansStart: l,
        recurrence: n,
        fn: o,
        extraArgs: p,
        scope: scope = this
      } = this.processIterationConfig(a), {
        interval: q,
        days: r,
        count: u,
        positions: v
      } = n, w = RecurrenceDayRuleEncoder.decode(r), z = v == null ? void 0 : v.length, A = {};
    let {months: B} = n, C = 0, E, F, G, H, I, J;
    H = DateHelper.startOf(u || q > 1 ? g : c, 'year');
    I = new Date(DateHelper.getNext(H, 'year', 1) - 1);
    B == null ? void 0 : B.sort((K, L) => K - L);
    if (!(B == null ? void 0 : B.length) && !(w == null ? void 0 : w.length)) {
      B = [g.getMonth() + 1];
    }
    w == null ? void 0 : w.forEach(K => {
      if (K[1]) {
        J = J || {};
        J[K[0]] = K[1];
      }
    });
    while ((!e || e > H) && (!u || C < u)) {
      G = [];
      if ((r == null ? void 0 : r.length) && (B == null ? void 0 : B.length)) {
        for (const K of B) {
          H = new Date(H.getFullYear(), K - 1);
          I = new Date(H.getFullYear(), K, -1);
          const L = {
            ...a,
            'fn': (N, O, P, ...Q) => {
              if (N >= g) {
                const R = this.isInView(b, N, j, k, f);
                C++;
                if (R) {
                  return o.apply(scope, [
                    N,
                    C,
                    C === 1 && P,
                    ...Q
                  ]);
                }
              }
            },
            'startDate': H,
            'endDate': I,
            'recurrence': n.constructor.new({
              'timeSpan': n.timeSpan,
              'frequency': 'MONTHLY',
              'days': r,
              'positions': v
            })
          };
          MonthlyRecurrenceIterator.forEachDate(L);
        }
      } else {
        if (w == null ? void 0 : w.length) {
          w.forEach(N => {
            const O = N[0];
            let P = 1, Q = 53;
            if (N[1]) {
              P = Q = N[1];
            }
            for (E = P; E <= Q; E++) {
              if (F = this.getNthDayInPeriod(H, I, O, E)) {
                F = DateHelper.copyTimeValues(F, g);
                if (!A[F.getTime()]) {
                  A[F.getTime()] = true;
                  G.push(F);
                }
              }
            }
          });
          G.sort((N, O) => N - O);
          if (!z) {
            for (E = 0; E < G.length; E++) {
              F = G[E];
              if (F >= g) {
                const N = this.isInView(b, F, j, k, f);
                C++;
                if (u && C > u || e && F >= e || N && o.apply(scope, [
                    F,
                    C,
                    C === 1 && l,
                    f,
                    ...p
                  ]) === false) {
                  return;
                }
              }
            }
          }
        } else {
          for (E = 0; E < B.length; E++) {
            if (F = this.buildDate(H.getFullYear(), B[E] - 1, g.getDate())) {
              F = DateHelper.copyTimeValues(F, g);
              if (!A[F.getTime()]) {
                A[F.getTime()] = true;
                if (z) {
                  G.push(F);
                } else if (F >= g) {
                  const O = b ? F >= j : F.valueOf() + k > j && !f.hasException(F);
                  C++;
                  if (u && C > u || e && F >= e || O && o.apply(scope, [
                      F,
                      C,
                      C === 1 && l,
                      f,
                      ...p
                    ]) === false) {
                    return;
                  }
                }
              }
            }
          }
        }
        if (z && G.length) {
          if (this.forEachDateAtPositions(G, v, P => {
              if (P >= g) {
                const Q = b ? P >= j : P.valueOf() + k > j && !f.hasException(P);
                C++;
                if (Q && (!e || P < e)) {
                  if (o.apply(scope, [
                      P,
                      C,
                      C === 1 && l,
                      f,
                      ...p
                    ]) === false || u && C >= u) {
                    return false;
                  }
                }
              }
            }) === false) {
            return false;
          }
        }
      }
      H = DateHelper.getNext(H, 'year', q);
      I = new Date(DateHelper.getNext(H, 'year', 1) - 1);
    }
  }
};
YearlyRecurrenceIterator._$name = 'YearlyRecurrenceIterator';
const recurrenceIterators = {};
[
  DailyRecurrenceIterator,
  WeeklyRecurrenceIterator,
  MonthlyRecurrenceIterator,
  YearlyRecurrenceIterator
].forEach(a => {
  recurrenceIterators[a.frequency] = a;
});
const frequencyToLater = {
  'DAILY': 'days',
  'WEEKLY': 'weeks',
  'MONTHLY': 'months',
  'YEARLY': 'years'
};
const dayNameToLater = {
  'MO': 'Mon',
  'TU': 'Tue',
  'WE': 'Wed',
  'TH': 'Thu',
  'FR': 'Fri',
  'SA': 'Sat',
  'SU': 'Sun'
};
function convertStringOfIntegerItemsValue(a) {
  if (a) {
    if (typeof a == 'string') {
      a = a.split(',').map(b => parseInt(b, 10));
    }
  } else {
    a = null;
  }
  return a;
}
function convertStringOfItemsValue(a) {
  if (a) {
    if (typeof a == 'string') {
      a = a.split(',');
    }
  } else {
    a = null;
  }
  return a;
}
function isEqualAsString(a, b) {
  return String(a) === String(b);
}
function convertInteger(a) {
  if (this.defaultValue && a === void 0) {
    return this.defaultValue;
  }
  if (this.allowNull && a == null) {
    return null;
  }
  a = parseInt(a);
  return isNaN(a) ? void 0 : a;
}
const RecurrenceModel = class extends Model {
  static fields = [
    {
      'name': 'date',
      'type': 'date'
    },
    {
      'name': 'frequency',
      'defaultValue': 'DAILY'
    },
    {
      'name': 'interval',
      'defaultValue': 1,
      'convert': convertInteger
    },
    {
      'name': 'endDate',
      'type': 'date'
    },
    {
      'name': 'count',
      'allowNull': true,
      'convert': convertInteger
    },
    {
      'name': 'days',
      'convert': convertStringOfItemsValue,
      'isEqual': isEqualAsString
    },
    {
      'name': 'monthDays',
      'convert': convertStringOfIntegerItemsValue,
      'isEqual': isEqualAsString
    },
    {
      'name': 'months',
      'convert': convertStringOfIntegerItemsValue,
      'isEqual': isEqualAsString
    },
    {
      'name': 'positions',
      'convert': convertStringOfIntegerItemsValue,
      'isEqual': isEqualAsString
    }
  ];
  static $name = 'RecurrenceModel';
  get isRecurrenceModel() {
    return true;
  }
  construct(a = {}) {
    const b = this, {
        rule: c,
        timeSpan: e,
        laterJsSchedule: f
      } = a;
    b._suspendedTimeSpanNotifying = 0;
    delete a.timeSpan;
    delete a.rule;
    delete a.laterJsSchedule;
    super.construct(...arguments);
    if (c) {
      b.suspendTimeSpanNotifying();
      b.rule = c;
      b.resumeTimeSpanNotifying();
    }
    b.timeSpan = e;
    if (f) {
      b.laterJsSchedule = f;
    }
  }
  static processData(a, b = false, c, e, f) {
    const {fieldMap: g} = this, {
        name: i,
        dataSource: j
      } = g.date;
    let k = a[j || i];
    if (typeof k === 'string') {
      k = g.date.convert(k, a, e);
    }
    if (k) {
      Object.assign(a, this.getDateValues(k));
      a[j || i] = k;
    }
    return super.processData(...arguments);
  }
  get dateFormat() {
    return this._dateFormat || 'YYYYMMDDTHHmmss';
  }
  set dateFormat(a) {
    this._dateFormat = a;
  }
  get recurrenceIterator() {
    return recurrenceIterators[this.frequency];
  }
  get timeSpan() {
    return this._timeSpan;
  }
  set timeSpan(a) {
    this._timeSpan = a;
  }
  get timeSpanDate() {
    let a;
    return (a = this.timeSpan) == null ? void 0 : a.startDate;
  }
  static getDateValues(a) {
    if (a) {
      return {
        'days': [RecurrenceDayRuleEncoder.encodeDay(a.getDay())],
        'monthDays': [a.getDate()],
        'months': [a.getMonth() + 1]
      };
    }
  }
  getDateValues(a) {
    a = this.date || this.timeSpanDate;
    return this.constructor.getDateValues(a);
  }
  getRRule(a = false) {
    let b;
    const c = this, e = c.date || c.timeSpanDate, f = [];
    if (c.frequency) {
      f.push('FREQ=' + c.frequency);
      let {
        days: g,
        monthDays: i,
        months: j
      } = c;
      if (a && e) {
        const {
          days: k,
          monthDays: l,
          months: n
        } = c.constructor.getDateValues(e);
        switch (c.frequency) {
        case 'WEEKLY':
          if (!g) {
            g = k;
          }
          break;
        case 'MONTHLY':
          if (!(i == null ? void 0 : i.length)) {
            i = l;
          }
          break;
        case 'YEARLY':
          if (!(i == null ? void 0 : i.length)) {
            i = l;
          }
          if (!(j == null ? void 0 : j.length)) {
            j = n;
          }
          break;
        }
      }
      if (c.interval > 1) {
        f.push('INTERVAL=' + c.interval);
      }
      if (g == null ? void 0 : g.length) {
        f.push('BYDAY=' + g.join(','));
      }
      if (i == null ? void 0 : i.length) {
        f.push('BYMONTHDAY=' + i.join(','));
      }
      if (j == null ? void 0 : j.length) {
        f.push('BYMONTH=' + j.join(','));
      }
      if (c.count) {
        f.push('COUNT=' + c.count);
      }
      if (c.endDate) {
        f.push('UNTIL=' + DateHelper.format(c.endDate, c.dateFormat));
      }
      if ((b = c.positions) == null ? void 0 : b.length) {
        f.push('BYSETPOS=' + c.positions.join(','));
      }
    }
    return f.join(';');
  }
  get rule() {
    return this.getRRule();
  }
  set rule(a) {
    const b = this, c = {
        'frequency': null,
        'interval': null,
        'count': null,
        'endDate': null,
        'days': null,
        'monthDays': null,
        'months': null,
        'positions': null
      };
    b.beginBatch();
    if (a) {
      const e = a.split(';');
      for (let f = 0, g = e.length; f < g; f++) {
        const j = e[f].split('='), k = j[1];
        switch (j[0]) {
        case 'FREQ':
          c.frequency = k;
          break;
        case 'INTERVAL':
          c.interval = k;
          break;
        case 'COUNT':
          c.count = k;
          c.until = null;
          break;
        case 'UNTIL':
          if (k) {
            c.endDate = DateHelper.parse(k, b.dateFormat);
          } else {
            c.endDate = null;
          }
          c.count = null;
          break;
        case 'BYDAY':
          c.days = k;
          break;
        case 'BYMONTHDAY':
          c.monthDays = k;
          break;
        case 'BYMONTH':
          c.months = k;
          break;
        case 'BYSETPOS':
          c.positions = k;
          break;
        }
      }
    }
    b.set(c);
    if (a) {
      b.sanitize();
    }
    b.endBatch();
  }
  forEachOccurrence(a, b, c) {
    if (this.timeSpanDate) {
      this.recurrenceIterator.forEachDate({
        'recurrence': this,
        'startDate': a,
        'endDate': b,
        'fn'(e, f, g, i) {
          return c(i.buildOccurrence(e, g), g, f, e);
        }
      });
    }
  }
  sanitize() {
    let a;
    const b = this, {timeSpanDate: c} = b, e = {};
    b.isSanitizingSuspended = true;
    switch (b.frequency) {
    case 'DAILY':
      e.positions = null;
      e.days = null;
      e.monthDays = null;
      e.months = null;
      break;
    case 'WEEKLY': {
        e.positions = null;
        e.monthDays = null;
        e.months = null;
        const {days: f} = b;
        if (c && (f == null ? void 0 : f.length) === 1 && f[0] === RecurrenceDayRuleEncoder.encodeDay(c.getDay())) {
          e.days = null;
        }
        break;
      }
    case 'MONTHLY': {
        if ((a = b.monthDays) == null ? void 0 : a.length) {
          e.positions = null;
          e.days = null;
        }
        e.months = null;
        const {monthDays: g} = b;
        if (c && (g == null ? void 0 : g.length) === 1 && g[0] === c.getDate()) {
          e.monthDays = null;
        }
        break;
      }
    case 'YEARLY': {
        const {months: i} = b;
        if (c && (i == null ? void 0 : i.length) === 1 && i[0] === c.getMonth() + 1) {
          e.months = null;
        }
        break;
      }
    }
    b.set(e);
    b.isSanitizingSuspended = false;
  }
  copy(...a) {
    const b = super.copy(...a);
    b.dateFormat = this.dateFormat;
    b.timeSpan = this.timeSpan;
    return b;
  }
  afterChange(a, b, c) {
    const e = super.afterChange(a, b, c), {timeSpan: f} = this;
    if (!this.isSanitizingSuspended) {
      this.sanitize();
    }
    if (f) {
      f.sanitizeRecurrenceData(this);
      if (!this.isTimeSpanNotifyingSuspended) {
        f.onRecurrenceChanged(this, ...arguments);
      }
    }
    return e;
  }
  setValue(a, b) {
    if (a === 'monthDays' && Number.isInteger(b)) {
      b = [b];
    }
    return super.setValue(a, b);
  }
  set(a, b, ...c) {
    const e = typeof a === 'object' ? a : { [a]: b };
    if (e.count) {
      e.endDate = null;
    } else if (e.endDate) {
      e.count = null;
    }
    return super.set(e, void 0, ...c);
  }
  get isTimeSpanNotifyingSuspended() {
    return Boolean(this._suspendedTimeSpanNotifying);
  }
  suspendTimeSpanNotifying() {
    this._suspendedTimeSpanNotifying++;
  }
  resumeTimeSpanNotifying() {
    if (this._suspendedTimeSpanNotifying)
      this._suspendedTimeSpanNotifying--;
  }
  reset() {
    this.allFields.forEach(({name: a}) => this.set(a, null));
  }
  set laterJsSchedule(a) {
    let b;
    a = ((b = a == null ? void 0 : a.schedules) == null ? void 0 : b[0]) || a;
    if (!a) {
      return;
    }
    this.reset();
    const {
      s: c,
      m: e,
      h: f,
      t: g,
      D: i,
      d: j,
      M: k
    } = a;
    const l = { 'interval': 1 };
    if (c || e || f || g) {
      l.frequency = 'DAILY';
    }
    if (j) {
      l.frequency = 'WEEKLY';
      l.days = j.map(n => RecurrenceDayRuleEncoder.encodeDay(n - 1));
    }
    if (i) {
      l.frequency = 'MONTHLY';
      l.monthDays = i.map(n => n || -1);
    }
    if (k) {
      l.frequency = 'YEARLY';
      l.months = k.slice();
    }
    Object.assign(this, l);
  }
  get laterJsSchedule() {
    const {
      frequency: a,
      interval: b,
      days: c,
      monthDays: e,
      months: f,
      positions: g
    } = this;
    let i = b > 1 ? 'every ' + b + ' ' + frequencyToLater[a] + ' ' : '';
    switch (this.frequency) {
    case 'YEARLY':
      if (f == null ? void 0 : f.length) {
        i += 'on the ' + f.join(',') + ' month ';
      }
    case 'MONTHLY':
      if (e == null ? void 0 : e.length) {
        i += 'on the ' + e.join(',') + ' day ';
        break;
      }
    case 'WEEKLY':
      if (c == null ? void 0 : c.length) {
        i += 'on ' + c.map(j => dayNameToLater[j]).join(',') + ' ';
      }
      if (g == null ? void 0 : g.length) {
        i += 'on the ' + g.join(',') + ' day instance ';
      }
    }
    return i.trim();
  }
};
RecurrenceModel._$name = 'RecurrenceModel';
function convertExceptionDatesValue(a) {
  const b = {}, {dateFormat: c} = this;
  if (a) {
    a = typeof a == 'string' ? a.split(',') : ArrayHelper.asArray(a);
    a.forEach(e => {
      if (typeof e == 'string') {
        e = DateHelper.parse(e, c);
      }
      if (!isNaN(e)) {
        b[DateHelper.makeKey(e)] = 1;
      }
    });
  }
  return b;
}
function serializeExceptionDatesValue(a) {
  const b = [], {dateFormat: c} = this;
  for (const e in a) {
    if (a[e]) {
      b.push(DateHelper.format(DateHelper.parseKey(e), c));
    }
  }
  return b;
}
const emptyArray15 = [];
const RecurringTimeSpan_default = a => {
  let b;
  return b = class extends (a || TimeSpan) {
    get supportsRecurring() {
      return true;
    }
    remove() {
      if (this.isOccurrence) {
        const c = this, {recurringTimeSpan: e} = c;
        c.cancelBatch();
        e.beginBatch();
        c.detachFromRecurringEvent();
        e.endBatch();
      } else {
        return super.remove(...arguments);
      }
    }
    get eventStore() {
      let c, e;
      let f = this.isOccurrence && ((c = this.recurringEvent) == null ? void 0 : c.eventStore) || super.eventStore;
      if (!f && ((e = this.firstStore) == null ? void 0 : e.isRecurringTimeSpansMixin)) {
        f = this.firstStore;
      }
      return f;
    }
    get recurrenceModel() {
      return this._recurrenceModel || RecurrenceModel;
    }
    set recurrenceModel(c) {
      this._recurrenceModel = c;
    }
    setRecurrence(c, e, f) {
      const g = this;
      let i;
      if (c) {
        if (!c.isRecurrenceModel) {
          if (typeof c === 'string') {
            c = { 'frequency': c };
            if (e) {
              c.interval = e;
            }
            if (f) {
              if (f instanceof Date) {
                c.endDate = f;
              } else {
                c.count = f;
              }
            }
          }
          c = new g.recurrenceModel(c);
        }
        c.timeSpan = g;
        i = c.rule;
      }
      g.recurrence = c;
      g.recurrenceRule = i;
    }
    get recurrence() {
      const c = this, e = c.recurrenceRule;
      if (!c._recurrence && e) {
        c._recurrence = new c.recurrenceModel({
          'rule': e,
          'timeSpan': c,
          'id': c.id + '-recurrence'
        });
      }
      return c._recurrence;
    }
    set recurrence(c) {
      const e = this;
      e._recurrence = c;
      if (c) {
        c.timeSpan = e;
        e.recurrenceRule = c.rule;
      } else {
        if (e.isOccurrence) {
          e.setData('recurrenceRule', null);
        } else {
          e.recurrenceRule = null;
        }
      }
    }
    get isRecurring() {
      return Boolean(!this.isOccurrence && this.recurrence);
    }
    get isOccurrence() {
      return Boolean(this.recurringTimeSpan);
    }
    get recurringTimeSpan() {
      return this._recurringTimeSpan;
    }
    get isPersistable() {
      return super.isPersistable && (!this.supportsRecurring || !this.isOccurrence);
    }
    set recurringTimeSpan(c) {
      this._recurringTimeSpan = c;
    }
    getOccurrencesForDateRange(c, e = c) {
      return this.eventStore.getOccurrencesForTimeSpan(this, c, e);
    }
    get occurrences() {
      if (this.isRecurring) {
        const c = [];
        this.occurrenceMap.forEach(e => {
          if (e !== this) {
            c.push(e);
          }
        });
        return c;
      }
      return emptyArray15;
    }
    get occurrenceMap() {
      return this._occurrencesMap || (this._occurrencesMap = new Map());
    }
    removeOccurrence(c, e = this.eventStore) {
      const f = c.isTimeSpan ? c.occurrenceDate : c;
      e == null ? void 0 : e.globalOccurrences.delete(this.createRecurrenceKey(f));
      this.occurrenceMap.delete(DateHelper.makeKey(f));
    }
    removeOccurrencesFrom(c) {
      this.occurrenceMap.forEach((e, f) => {
        if (DateHelper.parseKey(f) >= c) {
          this.removeOccurrence(e);
        }
      });
    }
    removeOccurrences(c) {
      [
        this,
        ...this.occurrences
      ].forEach(e => this.removeOccurrence(e, c));
    }
    onRecurrenceChanged() {
      let c;
      this.recurrenceRule = ((c = this.recurrence) == null ? void 0 : c.rule) || null;
    }
    sanitizeRecurrenceData(c = this.recurrence) {
      if (c.endDate) {
        const e = DateHelper.clearTime(c.endDate), {exceptionDates: f} = this;
        this.removeOccurrencesFrom(e);
        if (f) {
          for (const g in f) {
            const i = DateHelper.parseKey(g);
            if (i >= e) {
              delete f[g];
            }
          }
        }
      }
    }
    get occurrenceDate() {
      let c;
      return ((c = this.meta.modified) == null ? void 0 : c.startDate) || this.startDate;
    }
    get occurrenceIndex() {
      return AbstractRecurrenceIterator.getOccurrenceIndex(this);
    }
    buildOccurrence(c, e) {
      let f;
      const g = this, {
          occurrenceMap: i,
          recurrence: j,
          meta: k
        } = g, l = (f = g.eventStore) == null ? void 0 : f.globalOccurrences, n = DateHelper.makeKey(c), o = g.createRecurrenceKey(c, n), p = !(c - g.startDate), {fieldMap: q} = g.constructor;
      let r = l == null ? void 0 : l.get(o), {duration: u} = g;
      if (g.endDate && (g.allDay || !u)) {
        u = DateHelper.as(g.durationUnit, g.endDate.getTime() - g.startDate.getTime());
      }
      const v = u !== void 0 ? DateHelper.add(c, u, g.durationUnit) : void 0;
      if (!r) {
        if (e || p) {
          r = g;
          if (!p) {
            g.setStartEndDate(c, v, true);
            j.suspendTimeSpanNotifying();
            j.sanitize();
            j.resumeTimeSpanNotifying();
          }
          k.isSyncedWithRule = true;
        } else {
          r = g.copy({
            [q.id.dataSource]: o,
            [q.startDate.dataSource]: c,
            [q.endDate.dataSource]: v,
            [q.duration.dataSource]: u,
            'constraintDate': null,
            'constraintType': null
          }, { 'creatingOccurrence': true });
          r.recurringTimeSpan = g;
        }
        l == null ? void 0 : l.set(o, r);
        i.set(n, r);
      }
      return r;
    }
    createRecurrenceKey(c = this.startDate, e = null) {
      return '_generated:' + this.id + ':' + (e || DateHelper.makeKey(c));
    }
    convertToRealEvent(c, e) {
      let f;
      if (!this.isOccurrence) {
        return;
      }
      const g = this, {
          recurringTimeSpan: i,
          resource: j,
          occurrenceIndex: k,
          recurrence: l
        } = g, n = l && i.recurrence.count, o = ((f = c == null ? void 0 : c.resourceRecords) == null ? void 0 : f.value) || g.data.newResource;
      i.beginBatch();
      g.detachFromRecurringEvent();
      g.internalClearChanges();
      g._id = g.generateId(i.eventStore);
      g.setData('id', g._id);
      if (o) {
        delete g.data.resourceId;
      }
      i.eventStore.add(g, e);
      if (n) {
        g.recurrence.count = n - k;
      }
      if (o || j) {
        g.assign(o || j);
      }
      if (o) {
        delete g.data.resourceRecords;
      }
      if (c) {
        delete c.resourceRecords;
      }
      i.endBatch();
    }
    afterChange(c, e, f, ...g) {
      const i = this, {eventStore: j} = i;
      if ('recurrenceRule' in e) {
        i._recurrence = null;
        j == null ? void 0 : j.recurringEvents[e.recurrenceRule.value ? 'add' : 'delete'](i);
      }
      if (i.isOccurrence) {
        i.convertToRealEvent(e, f);
      } else if ('newExceptionDate' in e) {
        i.meta.modified.exceptionDates = true;
        delete i.meta.modified.newExceptionDate;
        i.removeOccurrence(e.newExceptionDate.value);
      }
      return super.afterChange(c, e, f, ...g);
    }
    detachFromRecurringEvent() {
      const c = this, {
          recurringTimeSpan: e,
          occurrenceDate: f,
          startDate: g
        } = c;
      c.recurringTimeSpan = null;
      e.addExceptionDate(f);
      if (c.recurrenceRule) {
        e.recurrence.endDate = DateHelper.add(g, -1, 'minute');
      }
    }
    set newExceptionDate(c) {
      if (c) {
        const e = this.exceptionDates || (this.exceptionDates = {});
        e[DateHelper.makeKey(c)] = 1;
      }
    }
    addExceptionDate(c) {
      this.set({ 'newExceptionDate': c });
    }
    hasException(c) {
      let e;
      return Boolean((e = this.exceptionDates) == null ? void 0 : e[DateHelper.makeKey(c)]);
    }
  }, __publicField(b, '$name', 'RecurringTimeSpan'), __publicField(b, 'fields', [
    {
      'name': 'recurrenceRule',
      'internal': true
    },
    {
      'name': 'exceptionDates',
      'convert': convertExceptionDatesValue,
      'serialize': serializeExceptionDatesValue,
      'internal': true
    }
  ]), b;
};
const oneDayMS = 1000 * 60 * 60 * 24;
const EventModelMixin_default = a => class b extends a {
  static get $name() {
    return 'EventModelMixin';
  }
  static get isEventModel() {
    return true;
  }
  static get fields() {
    return [
      {
        'name': 'resourceId',
        'internal': true
      },
      {
        'name': 'resourceIds',
        'type': 'array',
        'persist': false,
        'internal': true
      },
      {
        'name': 'resources',
        'column': { 'type': 'resourceassignment' },
        'persist': false,
        'internal': true,
        'useProp': true
      },
      {
        'name': 'draggable',
        'type': 'boolean',
        'persist': false,
        'defaultValue': true,
        'internal': true
      },
      {
        'name': 'resizable',
        'persist': false,
        'defaultValue': true,
        'internal': true
      },
      {
        'name': 'allDay',
        'type': 'boolean',
        'defaultValue': false
      },
      {
        'name': 'eventStyle',
        'internal': true
      },
      {
        'name': 'eventColor',
        'internal': true
      },
      {
        'name': 'milestoneWidth',
        'internal': true
      },
      {
        'name': 'stickyContents',
        'internal': true
      }
    ];
  }
  updateAssignmentEventIds() {
    this.assigned.forEach(c => {
      c.eventId = this.id;
    });
  }
  syncId(c) {
    super.syncId(c);
    this.updateAssignmentEventIds();
  }
  setValue(c, e) {
    let f;
    if (c === 'resource' && !this.usesSingleAssignment) {
      this.resources = (f = ArrayHelper.asArray(e)) != null ? f : [];
    } else {
      return super.setValue(c, e);
    }
  }
  get resources() {
    return this.assignments.reduce((c, {resource: e}) => {
      e && c.push(e.$original);
      return c;
    }, []);
  }
  set resources(c) {
    let e;
    c = ArrayHelper.asArray(c);
    const f = this, g = c.map(f.constructor.asId);
    if (f.usesSingleAssignment) {
      f.set('resourceId', g[0]);
    } else if ((e = f.fieldMap) == null ? void 0 : e.resourceIds.persist) {
      f.resourceIds = g;
    } else {
      const i = f.assignments.map(l => l.resource.id), {
          onlyInA: j,
          onlyInB: k
        } = ArrayHelper.delta(g, i);
      f.assignmentStore.add(j.map(l => ({
        'resource': l,
        'event': f
      })));
      f.assignmentStore.remove(k.map(l => f.assignments.find(n => n.resource.id === l)));
    }
  }
  forEachResource(c, e = this) {
    for (const f of this.resources) {
      if (c.call(e, f) === false) {
        return;
      }
    }
  }
  getResource(c) {
    if (c == null) {
      return this.resource;
    }
    return this.resourceStore ? this.resourceStore.getById(c) : null;
  }
  get startDate() {
    let c;
    let e;
    if (this.isOccurrence) {
      e = this.get('startDate');
    } else {
      e = (c = this._startDate) != null ? c : super.startDate;
    }
    if (this.allDay) {
      e = this.constructor.getAllDayStartDate(e);
    }
    return e;
  }
  set startDate(c) {
    if (this.batching) {
      this._startDate = c;
      this.set({ 'startDate': c });
    } else {
      super.startDate = c;
    }
  }
  get endDate() {
    let c;
    let e;
    if (this.isOccurrence) {
      e = this.get('endDate');
    } else {
      e = (c = this._endDate) != null ? c : super.endDate;
    }
    if (this.allDay) {
      e = this.constructor.getAllDayEndDate(e);
    }
    return e;
  }
  set endDate(c) {
    if (this.batching) {
      this._endDate = c;
      this.set({ 'endDate': c });
    } else {
      super.endDate = c;
    }
  }
  get wrapStartDate() {
    return this.startDate;
  }
  set wrapStartDate(c) {
  }
  get wrapEndDate() {
    return this.endDate;
  }
  set wrapEndDate(c) {
  }
  get isEvent() {
    return true;
  }
  get isDraggable() {
    return !this.readOnly && this.draggable;
  }
  get isResizable() {
    return !this.isMilestone && (!this.isParent || this.manuallyScheduled) && this.resizable;
  }
  get isPersistable() {
    return super.isPersistable && !this.isCreating;
  }
  endBatch(c, e, f) {
    const g = this, i = g.isPersistable;
    super.endBatch(c, e, f);
    if (g.isPersistable && !i && !g.ignoreBag && g.assigned) {
      for (const j of g.assigned) {
        j.stores.forEach(k => k.updateModifiedBagForRecord(j));
      }
    }
  }
  get isCreating() {
    return super.isCreating;
  }
  set isCreating(c) {
    let e, f;
    super.isCreating = c;
    (e = this.assignmentStore) == null ? void 0 : e.suspendAutoCommit();
    this.assignments.forEach(g => g.isCreating = c);
    (f = this.assignmentStore) == null ? void 0 : f.resumeAutoCommit();
  }
  get usesSingleAssignment() {
    return !this.eventStore || this.eventStore.usesSingleAssignment;
  }
  copy(...c) {
    const e = super.copy(...c);
    if (!this.usesSingleAssignment) {
      e.resourceId = null;
    }
    return e;
  }
  get persistableData() {
    const c = super.persistableData;
    if (!this.usesSingleAssignment) {
      delete c.resourceId;
    }
    return c;
  }
  get resource() {
    const {resources: c} = this;
    return c.length ? c[0] : null;
  }
  set resource(c) {
    this.resourceId = this.constructor.asId(c);
  }
  get resourceId() {
    let c;
    return this.usesSingleAssignment ? this.get('resourceId') : (c = this.assignments[0]) == null ? void 0 : c.resourceId;
  }
  set resourceId(c) {
    this.applyResourceId(c);
  }
  get resourceIds() {
    let c, e, f;
    if ((c = this.fieldMap) == null ? void 0 : c.resourceIds.persist) {
      return this.get('resourceIds');
    } else {
      return (f = (e = this.resources) == null ? void 0 : e.map(g => g.id)) != null ? f : [];
    }
  }
  set resourceIds(c) {
    this.set('resourceIds', c);
  }
  get $linkedResources() {
    let c, e;
    return (e = (c = this.resources) == null ? void 0 : c.flatMap(f => [
      f,
      ...f.$links
    ])) != null ? e : [];
  }
  applyResourceId(c, e = false) {
    let f, g;
    const i = this, {
        eventStore: j,
        assignments: k
      } = i;
    if (j) {
      const l = j.assignmentStore || ((f = j.crudManager) == null ? void 0 : f.assignmentStore);
      if (c != null) {
        if (!i.meta.skipEnforcingSingleAssignment) {
          j.usesSingleAssignment = true;
        }
        if ((k == null ? void 0 : k.length) && c !== k[0].resourceId) {
          const n = Boolean(j.eventsSuspended);
          n && l.suspendEvents();
          k[0].resource = c;
          n && l.resumeEvents();
        } else {
          l.assignEventToResource(i, c);
        }
      } else if (i.usesSingleAssignment || ((g = i.resourceIds) == null ? void 0 : g.length)) {
        l.remove(k);
      }
    } else if (!e) {
      i.set({ 'resourceId': c });
    }
  }
  applyResourceIds(c, e = false) {
    let f;
    const g = this, {
        eventStore: i,
        assignments: j
      } = g;
    if (g.fieldMap.resourceIds.persist === false) {
      return false;
    }
    c = [...new Set(c)];
    if (i) {
      const k = i.assignmentStore || ((f = i.crudManager) == null ? void 0 : f.assignmentStore);
      if (c == null ? void 0 : c.length) {
        if (j == null ? void 0 : j.length) {
          const l = Boolean(i.eventsSuspended);
          l && k.suspendEvents();
          j.forEach(p => {
            const q = c.find(r => !j.some(u => u.resourceId === r));
            if (q) {
              p.resource = q;
            }
          });
          const {
            onlyInA: n,
            onlyInB: o
          } = ArrayHelper.delta(c, j.map(p => p.resourceId));
          k.add(n.map(p => ({
            'resource': p,
            'event': g
          })));
          k.remove(o.map(p => j.find(q => q.resource.id === p)));
          l && k.resumeEvents();
        } else {
          k.add(c.map(p => ({
            'resource': p,
            'event': g
          })));
        }
      } else {
        k.remove(j);
      }
    } else if (!e) {
      g.set({ 'resourceIds': c });
    }
  }
  applyValue(c, e, f, g, i) {
    if (i && !this.meta.isAssigning) {
      const {eventStore: j} = this;
      switch (i.name) {
      case 'resourceId':
        j && (j.isAssigning = true);
        this.applyResourceId(f, true);
        break;
      case 'resourceIds':
        j && (j.isAssigning = true);
        this.applyResourceIds(f, true);
        break;
      }
      j && (j.isAssigning = false);
    }
    super.applyValue(c, e, f, g, i);
  }
  get assignments() {
    return [...this.assigned || []];
  }
  assign(c, e = false) {
    const {eventStore: f} = this;
    if (f && !f.usesSingleAssignment) {
      f.assignEventToResource(this, c, e);
    } else {
      this.resourceId = this.constructor.asId(c);
      if (!f) {
        this.meta.skipEnforcingSingleAssignment = true;
      }
    }
  }
  unassign(c, e = false) {
    let f;
    const g = this;
    c = g.constructor.asId(c);
    g.meta.removingResource = e;
    (f = g.eventStore) == null ? void 0 : f.unassignEventFromResource(g, c);
    g.meta.removingResource = null;
  }
  reassign(c, e) {
    this.eventStore && this.eventStore.reassignEventFromResourceToResource(this, c, e);
  }
  isAssignedTo(c) {
    const e = this.constructor.asId(c);
    return this.assignments.some(f => f.resourceId === e);
  }
  get predecessors() {
    let c;
    return [...(c = this.incomingDeps) != null ? c : []];
  }
  get successors() {
    let c;
    return [...(c = this.outgoingDeps) != null ? c : []];
  }
  get dependencies() {
    let c, e;
    return [
      ...(c = this.incomingDeps) != null ? c : [],
      ...(e = this.outgoingDeps) != null ? e : []
    ];
  }
  normalize() {
  }
  inSetNormalize() {
  }
  get recurringEvent() {
    return this.recurringTimeSpan;
  }
  get isInterDay() {
    const {durationMS: c} = this;
    if (c >= oneDayMS || !c && this.allDay) {
      return true;
    }
    const {
        endDate: e,
        startDate: f
      } = this, g = DateHelper.clearTime(f);
    if (f && e) {
      g.setDate(g.getDate() + 1);
      return (e || DateHelper.add(f, c)) > g;
    }
  }
  static getAllDayStartDate(c) {
    if (c && c.isEvent) {
      c = c.get('startDate');
    }
    if (c) {
      c = DateHelper.clearTime(c, true);
    }
    return c;
  }
  static getAllDayEndDate(c) {
    if (c && c.isEvent) {
      c = c.get('endDate');
    }
    if (c && (c.getHours() > 0 || c.getMinutes() > 0 || c.getSeconds() > 0 || c.getMilliseconds() > 0)) {
      c = DateHelper.getNext(c, 'd', 1);
    }
    return c;
  }
  static getAllDayDisplayStartDate(c) {
    if (c && c.isEvent) {
      c = c.get('startDate');
    }
    return DateHelper.clearTime(c, true);
  }
  static getAllDayDisplayEndDate(c, e) {
    if (c && c.isEvent) {
      e = c.get('endDate');
      c = c.get('startDate');
    }
    if (e) {
      c = this.constructor.getAllDayDisplayStartDate(c);
      if (DateHelper.clearTime(e, true).valueOf() === e.valueOf()) {
        e = DateHelper.add(e, DateHelper.DAY, -1);
      } else if (c.valueOf() !== e.valueOf()) {
        e = DateHelper.clearTime(e, true);
      }
    }
    return e;
  }
  isEditable(c) {
    switch (c) {
    case 'endDate':
    case 'duration':
    case 'fullDuration':
      return this.isLeaf;
    }
    return super.isEditable(c);
  }
};
const CoreEventMixin = class extends Mixin([CorePartOfProjectModelMixin], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this._startDate = null;
      this._endDate = null;
      this._duration = null;
    }
    static get fields() {
      return [
        {
          'name': 'startDate',
          'type': 'date'
        },
        {
          'name': 'endDate',
          'type': 'date'
        },
        {
          'name': 'duration',
          'type': 'number'
        },
        {
          'name': 'durationUnit',
          'type': 'durationunit',
          'defaultValue': 'day'
        }
      ];
    }
    get startDate() {
      let e, f;
      return (f = (e = this._startDate) != null ? e : this.get('startDate')) != null ? f : null;
    }
    get endDate() {
      let e, f;
      return (f = (e = this._endDate) != null ? e : this.get('endDate')) != null ? f : null;
    }
    get duration() {
      let e, f;
      return (f = (e = this._duration) != null ? e : this.get('duration')) != null ? f : null;
    }
    set startDate(e) {
      this.proposeStartDate(e);
    }
    set endDate(e) {
      this.proposeEndDate(e);
    }
    set duration(e) {
      this.proposeDuration(e);
    }
    inSet(e, f, g, i, j) {
      const k = this;
      if (k.project && !k.project.isWritingData && typeof e !== 'string' && !j) {
        if ('startDate' in e && !('startDate' in k.$changed)) {
          k.$changed.startDate = true;
          k.invalidate();
        }
        if ('endDate' in e && !('endDate' in k.$changed)) {
          k.$changed.endDate = true;
          k.invalidate();
        }
      }
      return b.inSet.call(k, e, f, g, i, j);
    }
    getStartDate() {
      return this.startDate;
    }
    proposeStartDate(e, f = !('endDate' in this.$changed)) {
      let g;
      if (this.inSetting || (e == null ? void 0 : e.getTime()) !== ((g = this.startDate) == null ? void 0 : g.getTime())) {
        this._startDate = e;
        this.propose({
          'startDate': e,
          'keepDuration': f
        });
      }
    }
    async setStartDate(e, f = true) {
      let g;
      this.proposeStartDate(e, f);
      return (g = this.project) == null ? void 0 : g.commitAsync();
    }
    getEndDate() {
      return this.endDate;
    }
    proposeEndDate(e, f = false) {
      let g;
      if (this.inSetting || (e == null ? void 0 : e.getTime()) !== ((g = this.endDate) == null ? void 0 : g.getTime())) {
        this._endDate = e;
        this.propose({
          'endDate': e,
          'keepDuration': f
        });
      }
    }
    async setEndDate(e, f = false) {
      let g;
      this.proposeEndDate(e, f);
      return (g = this.project) == null ? void 0 : g.commitAsync();
    }
    getDuration() {
      return this.duration;
    }
    proposeDuration(e, f, g = true) {
      this._duration = e;
      this.propose({
        'duration': e,
        'keepStart': g
      });
      if (f)
        this.propose({ 'durationUnit': f });
    }
    async setDuration(e, f, g = true) {
      let i;
      this.proposeDuration(e, f, g);
      return (i = this.project) == null ? void 0 : i.commitAsync();
    }
    getDurationUnit() {
      return this.durationUnit;
    }
    joinProject() {
      let e, f;
      const g = this;
      const i = g.$changed;
      const j = g.getCurrentOrProposed('startDate');
      const k = g.getCurrentOrProposed('endDate');
      const l = g.getCurrentOrProposed('duration');
      if (j != null)
        i.startDate = g._startDate = j;
      if (k != null)
        i.endDate = g._endDate = k;
      if (l != null)
        i.duration = g._duration = l;
      if (g.eventStore && !g.eventStore.isLoadingData) {
        const n = (e = g.assignmentStore) == null ? void 0 : e.storage.findItem('event', null);
        if (n) {
          const o = (f = g.assignmentStore) == null ? void 0 : f.storage.findItem('eventId', g.id);
          if (o) {
            for (const p of o) {
              p.setChanged('event', g);
            }
          } else {
            for (const q of n) {
              if (q.getCurrentOrProposed('event') === g.id) {
                q.setChanged('event', g);
              }
            }
          }
        }
      }
      b.joinProject.call(g);
    }
    applyValue(e, f, g, i, j) {
      let k;
      if ((this.project || ((k = this.recurringTimeSpan) == null ? void 0 : k.project)) && j) {
        const {name: l} = j;
        if (l === 'startDate' || l == 'duration' || l === 'endDate') {
          e = true;
          this['_' + l] = g;
        }
        if (i) {
          e = false;
        }
      }
      b.applyValue.call(this, e, f, g, i, j);
    }
    afterChange(e, f, g, i, j) {
      if (!this.$isCalculating && !j) {
        this.setData({
          'startDate': this.getCurrentOrProposed('startDate'),
          'endDate': this.getCurrentOrProposed('endDate'),
          'duration': this.getCurrentOrProposed('duration'),
          'durationUnit': this.getCurrentOrProposed('durationUnit')
        });
      }
      b.afterChange.call(this, e, f, g, i, j);
    }
    calculateInvalidated() {
      let e, f;
      const g = this;
      const i = g.$changed;
      const j = 'startDate' in i;
      const k = 'endDate' in i;
      const l = 'duration' in i;
      const {
        startDate: n,
        endDate: o,
        duration: p,
        keepDuration: q,
        keepStart: r
      } = i;
      let u = null;
      if (j && !k && !l) {
        if (n === null) {
          i.endDate = null;
        } else if (g.hasCurrentOrProposed('endDate') && n > g.getCurrentOrProposed('endDate') && !q) {
          i.endDate = n;
          i.duration = 0;
        } else if (g.hasCurrentOrProposed('duration') && (q || !g.hasCurrentOrProposed('endDate'))) {
          u = 'endDate';
        } else if (g.hasCurrentOrProposed('endDate')) {
          u = 'duration';
        }
      } else if (!j && k && !l) {
        if (o === null) {
          i.startDate = null;
        } else if (g.hasCurrentOrProposed('startDate') && !q && o !== true && o < g.getCurrentOrProposed('startDate')) {
          i.startDate = o;
          i.duration = 0;
        } else if (g.hasCurrentOrProposed('duration') && (q || !g.hasCurrentOrProposed('startDate'))) {
          u = 'startDate';
        } else if (g.hasCurrentOrProposed('startDate')) {
          u = 'duration';
        }
      } else if (!j && !k && l) {
        if (p === null) {
          i.endDate = null;
        } else if (g.hasCurrentOrProposed('startDate') && (r || !g.hasCurrentOrProposed('endDate'))) {
          if (r && i.duration < 0) {
            i.duration = 0;
          }
          u = 'endDate';
        } else if (g.hasCurrentOrProposed('endDate')) {
          u = 'startDate';
        }
      } else if (j && k && !l) {
        if (n === null && o === null) {
          i.duration = null;
        } else {
          u = 'duration';
        }
      } else if (j && !k && l) {
        u = 'endDate';
      } else if (!j && k && l) {
        u = 'startDate';
      } else if (j && k && l) {
        if (p == null) {
          u = 'duration';
        } else if (n == null) {
          u = 'startDate';
        } else {
          u = 'endDate';
        }
      }
      const v = g.getCurrentOrProposed('startDate');
      const w = g.getCurrentOrProposed('endDate');
      const z = g.getCurrentOrProposed('duration');
      const A = g.getCurrentOrProposed('durationUnit');
      const B = (f = (e = g.getProject()) == null ? void 0 : e.adjustDurationToDST) != null ? f : false;
      switch (u) {
      case 'startDate':
        const C = DateHelper.add(w, -z, A);
        if (B) {
          const G = w.getTimezoneOffset() - C.getTimezoneOffset();
          C.setTime(C.getTime() - G * 60 * 1000);
        }
        i.startDate = C;
        break;
      case 'endDate':
        const E = DateHelper.add(v, z, A);
        if (B) {
          const H = v.getTimezoneOffset() - E.getTimezoneOffset();
          E.setTime(E.getTime() - H * 60 * 1000);
        }
        i.endDate = E;
        break;
      case 'duration':
        let F = DateHelper.diff(v, w, 'millisecond');
        if (B) {
          const I = v.getTimezoneOffset() - w.getTimezoneOffset();
          F += I * 60 * 1000;
        }
        i.duration = DateHelper.as(A, F, 'millisecond');
        break;
      }
      if ('startDate' in i && i.startDate !== true)
        this._startDate = i.startDate;
      if ('endDate' in i && i.endDate !== true)
        this._endDate = i.endDate;
      if ('duration' in i && i.duration !== true)
        this._duration = i.duration;
      if (i.startDate === true)
        delete i.startDate;
      if (i.endDate === true)
        delete i.endDate;
      delete i.keepDuration;
      delete i.keepStart;
    }
  }
  return c;
}) {
};
const AbstractHasAssignmentsMixin = class extends Mixin([AbstractPartOfProjectModelMixin], a => {
  const b = a.prototype;
  class c extends a {
    getAssignmentFor(e) {
      let f;
      for (const g of (f = this.assigned) != null ? f : []) {
        if (g.resource === e)
          return g;
      }
      return null;
    }
    isAssignedTo(e) {
      return Boolean(this.getAssignmentFor(e));
    }
    async assign(e) {
      const f = this.project.assignmentStore.modelClass;
      this.addAssignment(new f({
        'event': this,
        'resource': e
      }));
      return this.commitAsync();
    }
    async unassign(e) {
      const f = this.getAssignmentFor(e);
      this.removeAssignment(f);
      return this.commitAsync();
    }
    leaveProject() {
      if (this.isInActiveTransaction && this.assigned) {
        const e = this.getEventStore();
        this.assigned.forEach(f => e.assignmentsForRemoval.add(f));
      }
      b.leaveProject.call(this, ...arguments);
    }
    remove() {
      if (this.parent) {
        const e = this.getEventStore();
        b.remove.call(this);
        e && e.afterEventRemoval();
      } else {
        return b.remove.call(this);
      }
    }
    addAssignment(e) {
      this.getProject().assignmentStore.add(e);
      return e;
    }
    removeAssignment(e) {
      this.getProject().assignmentStore.remove(e);
      return e;
    }
  }
  return c;
}) {
};
const CoreHasAssignmentsMixin = class extends Mixin([
  CoreEventMixin,
  AbstractHasAssignmentsMixin
], a => {
  const b = a.prototype;
  class c extends a {
    get assigned() {
      let e, f;
      return (f = (e = this.project) == null ? void 0 : e.assignmentStore.getEventsAssignments(this)) != null ? f : this.$cachedAssignments;
    }
    leaveProject(e = false) {
      this.$cachedAssignments = this.assigned;
      super.leaveProject(e);
    }
    applyValue(e, f, g, i, j) {
      let k;
      if (f === 'id') {
        (k = this.assigned) == null ? void 0 : k.forEach(l => l.set('eventId', g));
      }
      b.applyValue.call(this, e, f, g, i, j);
    }
    copy(e = null, f = null) {
      const g = b.copy.call(this, e, f);
      if (ObjectHelper.isObject(f) && !f.skipFieldIdentifiers || !ObjectHelper.isObject(f)) {
        g.$cachedAssignments = this.assigned;
      }
      return g;
    }
  }
  return c;
}) {
};
const CoreHasDependenciesMixin = class extends Mixin([CoreEventMixin], a => {
  const b = a.prototype;
  class c extends a {
    get outgoingDeps() {
      return this.project.dependencyStore.getOutgoingDepsForEvent(this);
    }
    get incomingDeps() {
      return this.project.dependencyStore.getIncomingDepsForEvent(this);
    }
    leaveProject() {
      const e = this.eventStore;
      if (this.outgoingDeps) {
        this.outgoingDeps.forEach(f => e.dependenciesForRemoval.add(f));
      }
      if (this.incomingDeps) {
        this.incomingDeps.forEach(f => e.dependenciesForRemoval.add(f));
      }
      b.leaveProject.call(this);
    }
  }
  return c;
}) {
};
const SchedulerCoreEvent = class extends Mixin([
  CoreEventMixin,
  CoreHasAssignmentsMixin,
  CoreHasDependenciesMixin
], a => {
  const b = a.prototype;
  class c extends a {
  }
  return c;
}) {
};
const EngineMixin3 = SchedulerCoreEvent;
const EventModel = class extends EngineMixin3.derive(TimeSpan).mixin(RecurringTimeSpan_default, PartOfProject_default, EventModelMixin_default) {
  static get $name() {
    return 'EventModel';
  }
};
EventModel.exposeProperties();
EventModel._$name = 'EventModel';
const dataAddRemoveActions3 = {
  'splice': 1,
  'clear': 1
};
const AbstractEventStoreMixin = class extends Mixin([AbstractPartOfProjectStoreMixin], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.assignmentsForRemoval = new Set();
      this.dependenciesForRemoval = new Set();
    }
    onDataChange(e) {
      let f;
      const g = dataAddRemoveActions3[e.action];
      super.onDataChange(e);
      if (g && ((f = e.removed) == null ? void 0 : f.length))
        this.afterEventRemoval();
    }
    remove(e, f) {
      const g = b.remove.call(this, e, f);
      this.afterEventRemoval();
      return g;
    }
    removeAll(e) {
      const f = b.removeAll.call(this, e);
      this.afterEventRemoval();
      return f;
    }
    onNodeRemoveChild(e, f, g, i) {
      const j = b.onNodeRemoveChild.call(this, ...arguments);
      this.afterEventRemoval();
      return j;
    }
    afterEventRemoval() {
      const {
        assignmentsForRemoval: e,
        dependenciesForRemoval: f
      } = this;
      if (!e)
        return;
      const g = this.getAssignmentStore();
      if (g && !g.allAssignmentsForRemoval && e.size) {
        const j = [...e].filter(k => !g.assignmentsForRemoval.has(k));
        j.length > 0 && g.remove(j);
      }
      e.clear();
      const i = this.getDependencyStore();
      if (i && !i.allDependenciesForRemoval && f.size) {
        const k = [...f].filter(l => !i.dependenciesForRemoval.has(l));
        k.length > 0 && i.remove(k);
      }
      f.clear();
    }
    processRecord(e, f = false) {
      let g;
      if (!((g = this.project) == null ? void 0 : g.isRepopulatingStores)) {
        const i = this.getById(e.id);
        const j = i && i !== e;
        if (j && i.assigned) {
          for (const k of i.assigned) {
            k.event = e;
          }
        }
      }
      return e;
    }
  }
  return c;
}) {
};
const CoreEventStoreMixin = class extends Mixin([
  AbstractEventStoreMixin,
  CorePartOfProjectStoreMixin
], a => {
  const b = a.prototype;
  class c extends a {
    static get defaultConfig() {
      return { 'modelClass': SchedulerCoreEvent };
    }
    joinProject() {
      let e;
      (e = this.assignmentStore) == null ? void 0 : e.linkAssignments(this, 'event');
    }
    afterLoadData() {
      let e;
      this.afterEventRemoval();
      (e = this.assignmentStore) == null ? void 0 : e.linkAssignments(this, 'event');
    }
  }
  return c;
}) {
};
const EngineMixin4 = PartOfProject_default(CoreEventStoreMixin.derive(AjaxStore));
const EventStore = class extends EngineMixin4.mixin(RecurringEventsMixin_default, EventStoreMixin_default, DayIndexMixin_default, GetEventsMixin_default) {
  static $name = 'EventStore';
  static get defaultConfig() {
    return { 'modelClass': EventModel };
  }
};
EventStore._$name = 'EventStore';
const getPath = ObjectHelper.getPath;
const setFieldValue = (a, b, c, e) => {
  if (c) {
    ObjectHelper.setPath(a, b, e);
  } else {
    a[b] = e;
  }
};
const setModelField = (a, b, c, e) => {
  let f;
  if ((e == null ? void 0 : e.isModel) && !((f = getPath(a, b)) == null ? void 0 : f.model)) {
    setFieldValue(a, b, c, e);
  }
};
const AssignmentModelMixin_default = a => class b extends a {
  static get $name() {
    return 'AssignmentModelMixin';
  }
  static get fields() {
    return [
      'resourceId',
      'eventId',
      {
        'name': 'drawDependencies',
        'type': 'boolean'
      },
      'event',
      'resource'
    ];
  }
  construct(c, ...e) {
    let f, g;
    c = c ? Object.isExtensible(c) ? c : Object.assign({}, c) : {};
    const {fieldMap: i} = this, j = i.eventId.dataSource, k = i.resourceId.dataSource, l = i.event.dataSource, n = i.resource.dataSource, o = i.eventId.complexMapping, p = i.resourceId.complexMapping, q = i.event.complexMapping, r = i.resource.complexMapping, u = o ? getPath(c, j) : c[j], v = p ? getPath(c, k) : c[k], w = ((f = c.event) == null ? void 0 : f.isModel) ? c.event : q ? getPath(c, l) : c[l], z = ((g = c.resource) == null ? void 0 : g.isModel) ? c.resource : r ? getPath(c, n) : c[n];
    if (u != null) {
      setFieldValue(c, l, q, u);
    } else if (w != null) {
      const A = w.isModel ? w.id : w;
      setFieldValue(c, j, o, A);
    }
    if (v != null) {
      setFieldValue(c, n, r, v);
    } else if (z != null) {
      const B = z.isModel ? z.id : z;
      setFieldValue(c, k, p, B);
    }
    setModelField(c, l, q, c.event);
    setModelField(c, n, r, c.resource);
    super.construct(c, ...e);
  }
  get eventResourceKey() {
    return this.buildEventResourceKey(this.event, this.resource);
  }
  buildEventResourceKey(c, e) {
    let f, g;
    if (c) {
      f = c.isModel ? c.id : c;
    } else {
      f = this.internalId;
    }
    if (e) {
      g = e.isModel ? e.id : e;
    } else {
      g = this.internalId;
    }
    return f + '-' + g;
  }
  buildIndexKey({
    event: c,
    resource: e
  }) {
    return this.buildEventResourceKey(c, e);
  }
  set(c, e, ...f) {
    let g, i;
    const j = this.fieldToKeys(c, e);
    if ('resource' in j) {
      if (((g = j.resource) == null ? void 0 : g.id) !== void 0) {
        j.resourceId = j.resource.id;
      }
    } else if ('resourceId' in j && this.constructor.isProAssignmentModel) {
      j.resource = j.resourceId;
    }
    if ('event' in j) {
      if (((i = j.event) == null ? void 0 : i.id) !== void 0) {
        j.eventId = j.event.id;
      }
    } else if ('eventId' in j && this.constructor.isProAssignmentModel) {
      j.event = j.eventId;
    }
    return super.set(j, null, ...f);
  }
  afterChange(c, e, f, g, i) {
    let j, k;
    const l = this;
    if (!l.constructor.isProAssignmentModel && (e == null ? void 0 : e.resourceId) && ((j = l.resource) == null ? void 0 : j.id) !== e.resourceId.value) {
      l.resource = e.resourceId.value;
    } else if (l.constructor.isProAssignmentModel && ((k = l.project) == null ? void 0 : k.propagatingSyncChanges) && (e == null ? void 0 : e.eventId) && !(e == null ? void 0 : e.event) && (c.event.value === e.eventId.value || c.event.value.id === e.eventId.value)) {
      delete e.eventId;
      delete l.meta.modified.eventId;
    }
    return super.afterChange(...arguments);
  }
  set resourceId(c) {
    const {resource: e} = this;
    if ((e == null ? void 0 : e.isModel) && e.id === c) {
      this.set('resourceId', c);
    } else {
      this.resource = c;
    }
  }
  get resourceId() {
    let c, e;
    return (e = (c = this.resource) == null ? void 0 : c.id) != null ? e : this.get('resourceId');
  }
  set eventId(c) {
    const {event: e} = this;
    if ((e == null ? void 0 : e.isModel) && e.id === c) {
      this.set('eventId', c);
    } else {
      this.event = c;
    }
  }
  get eventId() {
    let c, e;
    return (e = (c = this.event) == null ? void 0 : c.id) != null ? e : this.get('eventId');
  }
  get eventName() {
    let c;
    return (c = this.event) == null ? void 0 : c.name;
  }
  get resourceName() {
    let c;
    return (c = this.resource) == null ? void 0 : c.name;
  }
  getResource() {
    return this.resource;
  }
  get isAssignment() {
    return true;
  }
  get isPersistable() {
    const {
        event: c,
        resource: e,
        unjoinedStores: f,
        assignmentStore: g
      } = this, i = g == null ? void 0 : g.crudManager;
    let j;
    if (g) {
      j = this.isValid && !(c == null ? void 0 : c.isCreating) && (i || !(c == null ? void 0 : c.isPhantom) && !(e == null ? void 0 : e.isPhantom));
    } else {
      j = !this.isPhantom && Boolean(f[0]);
    }
    return j && super.isPersistable && !(c == null ? void 0 : c.isCreating);
  }
  get isValid() {
    return this.resource != null && this.event != null;
  }
  toString() {
    if (this.resourceName) {
      return this.resourceName + ' ' + Math.round(this.units) + '%';
    }
    return '';
  }
  shouldRecordFieldChange(c, e, f) {
    let g, i;
    if (!super.shouldRecordFieldChange(c, e, f)) {
      return false;
    }
    if (c === 'event' || c === 'eventId') {
      const j = (g = this.project) == null ? void 0 : g.eventStore;
      if (j && j.oldIdMap[e] === j.getById(f)) {
        return false;
      }
    }
    if (c === 'resource' || c === 'resourceId') {
      const k = (i = this.project) == null ? void 0 : i.resourceStore;
      if (k && k.oldIdMap[e] === k.getById(f)) {
        return false;
      }
    }
    return true;
  }
};
function asId(a) {
  return (a == null ? void 0 : a.isModel) ? a.id : a;
}
const CoreAssignmentMixin = class extends Mixin([CorePartOfProjectModelMixin], a => {
  const b = a.prototype;
  class c extends a {
    static get fields() {
      return [
        {
          'name': 'resource',
          'isEqual': (e, f) => e === f,
          'persist': false
        },
        {
          'name': 'event',
          'isEqual': (e, f) => e === f,
          'persist': false
        }
      ];
    }
    setChanged(e, f, g) {
      const {
        assignmentStore: i,
        eventStore: j,
        resourceStore: k,
        project: l
      } = this;
      let n = false;
      if (e === 'event') {
        const o = isInstanceOf(f, CoreEventMixin) ? f : j == null ? void 0 : j.$master.getById(f);
        if (o)
          n = true;
        f = o || f;
      }
      if (e === 'resource') {
        const p = isInstanceOf(f, CoreResourceMixin) ? f : k == null ? void 0 : k.$master.getById(f);
        if (p)
          n = true;
        f = p || f;
      }
      b.setChanged.call(this, e, f, g, true);
      if (i && n && !l.isPerformingCommit && !i.isLoadingData && !(k == null ? void 0 : k.isLoadingData) && !i.skipInvalidateIndices) {
        i.invalidateIndices();
      }
    }
    joinProject() {
      b.joinProject.call(this);
      this.setChanged('event', this.get('event'));
      this.setChanged('resource', this.get('resource'));
    }
    calculateInvalidated() {
      let e, f;
      const {
        event: event = this.event,
        resource: resource = this.resource
      } = this.$changed;
      if (event !== null && !isInstanceOf(event, CoreEventMixin)) {
        const g = (e = this.eventStore) == null ? void 0 : e.getById(event);
        if (g)
          this.setChanged('event', g, false);
      }
      if (resource !== null && !isInstanceOf(resource, CoreResourceMixin)) {
        const i = (f = this.resourceStore) == null ? void 0 : f.getById(resource);
        if (i)
          this.setChanged('resource', i, false);
      }
    }
    finalizeInvalidated(e) {
      const f = this.$changed;
      if ('resource' in f) {
        f.resourceId = asId(f.resource);
      }
      if ('event' in f) {
        f.eventId = asId(f.event);
      }
      b.finalizeInvalidated.call(this, e);
    }
    set event(e) {
      this.setChanged('event', e);
      this.setChanged('eventId', asId(e));
    }
    get event() {
      const e = this.get('event');
      return (e == null ? void 0 : e.id) != null ? e : null;
    }
    set resource(e) {
      this.setChanged('resource', e);
      this.setChanged('resourceId', asId(e));
    }
    get resource() {
      const e = this.get('resource');
      return (e == null ? void 0 : e.id) != null ? e : null;
    }
  }
  return c;
}) {
};
const EngineMixin5 = CoreAssignmentMixin;
const AssignmentModel = class extends AssignmentModelMixin_default(PartOfProject_default(EngineMixin5.derive(Model))) {
  static get $name() {
    return 'AssignmentModel';
  }
};
AssignmentModel.exposeProperties();
AssignmentModel._$name = 'AssignmentModel';
const AssignmentStoreMixin_default = a => {
  let b;
  return b = class extends a {
    static get $name() {
      return 'AssignmentStoreMixin';
    }
    static get defaultConfig() {
      return {
        'loadPriority': 300,
        'syncPriority': 300,
        'id': 'assignments'
      };
    }
    add(c, ...e) {
      let f;
      c = ArrayHelper.asArray(c);
      if (!(c == null ? void 0 : c.length)) {
        return;
      }
      for (let g = 0; g < c.length; g++) {
        let j = c[g];
        if (!(j instanceof Model)) {
          c[g] = j = this.createRecord(j);
        }
        if (!this.isSyncingDataOnLoad && this.storage.findIndex('eventResourceKey', j.eventResourceKey, true) !== -1) {
          throw new Error('Duplicate assignment Event: ' + j.eventId + ' to resource: ' + j.resourceId);
        }
        if ((f = j.event) == null ? void 0 : f.isCreating) {
          j.isCreating = true;
        }
      }
      return super.add(c, ...e);
    }
    includesAssignment(c, e) {
      return this.storage.findIndex('eventResourceKey', c + '-' + e, true) !== -1;
    }
    setStoreData(c) {
      if (this.usesSingleAssignment) {
        throw new Error('Data loading into AssignmentStore (multi-assignment mode) cannot be combined with EventStore data containing resourceId (single-assignment mode)');
      }
      super.setStoreData(c);
    }
    set storage(c) {
      super.storage = c;
      this.storage.addIndex({
        'property': 'eventResourceKey',
        'dependentOn': {
          'event': true,
          'resource': true
        },
        'onDuplicate'(e) {
          console.warn('Duplicate assignment of event ' + e.eventId + ' to resource ' + e.resourceId);
        }
      });
    }
    get storage() {
      return this._storage || super.storage;
    }
    get isAssignmentStore() {
      return true;
    }
    getOccurrence(c, e) {
      if (!c || !(e == null ? void 0 : e.isOccurrence)) {
        return c;
      }
      const f = this;
      return {
        'id': e.id + ':a' + c.id,
        'event': e,
        'resource': c.resource,
        'eventId': e.id,
        'resourceId': c.resource.id,
        'isAssignment': true,
        'isOccurrenceAssignment': true,
        'instanceMeta'(g) {
          return f.occurrenceInstanceMeta(this, g);
        }
      };
    }
    occurrenceInstanceMeta(c, e) {
      const f = this, g = e.id || e, {id: i} = c;
      let {occurrenceMeta: j} = f;
      if (!j) {
        j = f.occurrenceMeta = {};
      }
      if (!j[i]) {
        j[i] = {};
      }
      return j[i][g] || (j[i][g] = {});
    }
    mapAssignmentsForEvent(c, e, f) {
      c = this.eventStore.getById(c);
      const g = Boolean(e), i = Boolean(f);
      if (g || i) {
        return c.assignments.reduce((j, k) => {
          const l = g ? e(k) : k;
          if (!i || f(l)) {
            j.push(l);
          }
          return j;
        }, []);
      }
      return c.assignments;
    }
    mapAssignmentsForResource(c, e, f) {
      c = this.resourceStore.getById(c);
      const g = Boolean(e), i = Boolean(f);
      if (g || i) {
        return c.assignments.reduce((j, k) => {
          const l = g ? e(k) : k;
          if (!i || f(l)) {
            j.push(l);
          }
          return j;
        }, []);
      }
      return c.assignments;
    }
    getAssignmentsForEvent(c) {
      return c.assignments;
    }
    removeAssignmentsForEvent(c) {
      return this.remove(c.assignments);
    }
    getAssignmentsForResource(c) {
      c = this.resourceStore.getById(c);
      return c.assignments;
    }
    removeAssignmentsForResource(c) {
      this.remove(this.getAssignmentsForResource(c));
    }
    getResourcesForEvent(c) {
      return c.resources;
    }
    getEventsForResource(c) {
      c = this.resourceStore.getById(c);
      return c == null ? void 0 : c.events;
    }
    assignEventToResource(c, e, f = null, g = false) {
      let i, j, k;
      const l = this, n = g ? new Set(c.assignments) : null;
      e = ArrayHelper.asArray(e).map(p => {
        let q;
        return (q = p.$original) != null ? q : p;
      });
      if ((i = l.eventStore) == null ? void 0 : i.usesSingleAssignment) {
        if ((j = c.assignments) == null ? void 0 : j.length) {
          if (!l.isEventAssignedToResource(c, e[0])) {
            c.resource = e[0];
          }
          return [];
        } else {
          c.resourceId = (k = e[0]) == null ? void 0 : k.id;
        }
      }
      let o = [];
      l.suspendAutoCommit();
      e.forEach(p => {
        let q;
        const r = l.getAssignmentForEventAndResource(c, p);
        if (!r) {
          const u = {
            'event': c,
            'resource': p
          };
          o.push((q = f == null ? void 0 : f(u)) != null ? q : u);
        } else if (g) {
          n.delete(r);
        }
      });
      o = l.add(o);
      if (g) {
        l.remove(Array.from(n));
      }
      l.resumeAutoCommit();
      return o;
    }
    unassignEventFromResource(c, e) {
      const f = this, g = [];
      if (!e) {
        return f.removeAssignmentsForEvent(c);
      }
      e = ArrayHelper.asArray(e);
      for (let j = 0; j < e.length; j++) {
        if (f.isEventAssignedToResource(c, e[j])) {
          g.push(f.getAssignmentForEventAndResource(c, e[j]));
        }
      }
      return f.remove(g);
    }
    isEventAssignedToResource(c, e) {
      return Boolean(this.getAssignmentForEventAndResource(c, e));
    }
    getAssignmentForEventAndResource(c, e) {
      let f;
      if (!(c = this.eventStore.getById(c)) || !(f = c.assignments) || !(e = this.resourceStore.$master.getById(e))) {
        return null;
      }
      return this.getOccurrence(f.find(g => {
        let i;
        return ((i = g.resource) == null ? void 0 : i.$original) === e.$original;
      }), c);
    }
  }, __publicField(b, '$lazyLoadPluginClass', DateStoreLazyLoadPlugin), b;
};
const isNotNumber = a => Number(a) !== a;
const CIFromSetOrArrayOrValue = a => {
  if (a instanceof Set || a instanceof Array)
    return CI(a);
  return CI([a]);
};
const delay = a => new Promise(b => setTimeout(b, a));
const format = (a, ...b) => {
  return a.replace(/{(\d+)}/g, (c, e) => typeof b[e] !== 'undefined' ? b[e] : c);
};
const AbstractAssignmentStoreMixin = class extends Mixin([AbstractPartOfProjectStoreMixin], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.assignmentsForRemoval = new Set();
      this.allAssignmentsForRemoval = false;
    }
    remove(e, f) {
      this.assignmentsForRemoval = CIFromSetOrArrayOrValue(e).toSet();
      const g = b.remove.call(this, e, f);
      this.assignmentsForRemoval.clear();
      return g;
    }
    removeAll(e) {
      this.allAssignmentsForRemoval = true;
      const f = b.removeAll.call(this, e);
      this.allAssignmentsForRemoval = false;
      return f;
    }
  }
  return c;
}) {
};
const emptySet = new Set();
const CoreAssignmentStoreMixin = class extends Mixin([
  AbstractAssignmentStoreMixin,
  CorePartOfProjectStoreMixin
], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.skipInvalidateIndices = false;
    }
    static get defaultConfig() {
      return {
        'modelClass': CoreAssignmentMixin,
        'storage': {
          'extraKeys': [
            {
              'property': 'event',
              'unique': false
            },
            {
              'property': 'resource',
              'unique': false
            },
            {
              'property': 'eventId',
              'unique': false
            }
          ]
        }
      };
    }
    set data(e) {
      this.allAssignmentsForRemoval = true;
      super.data = e;
      this.allAssignmentsForRemoval = false;
    }
    getEventsAssignments(e) {
      return this.storage.findItem('event', e, true) || emptySet;
    }
    getResourcesAssignments(e) {
      return this.storage.findItem('resource', e.$original, true) || emptySet;
    }
    updateIndices() {
      this.storage.rebuildIndices();
    }
    invalidateIndices() {
      this.storage.invalidateIndices();
    }
    afterLoadData() {
      this.eventStore && this.linkAssignments(this.eventStore, 'event');
      this.resourceStore && this.linkAssignments(this.resourceStore, 'resource');
    }
    linkAssignments(e, f) {
      e = e.masterStore || e;
      const g = this.count && this.storage.findItem(f, null, true);
      if (g) {
        for (const i of g) {
          const j = e.getById(i.getCurrentOrProposed(f));
          if (j)
            i.setChanged(f, j);
        }
        this.invalidateIndices();
      }
    }
    unlinkAssignments(e) {
      this.forEach(f => {
        let g, i, j;
        return f.setChanged(e, (j = (i = (g = f[e]) == null ? void 0 : g.id) != null ? i : f == null ? void 0 : f.getData(e)) != null ? j : f[e + 'Id']);
      });
      this.invalidateIndices();
    }
    onCommitAsync() {
      this.updateIndices();
    }
  }
  return c;
}) {
};
const EngineMixin6 = PartOfProject_default(CoreAssignmentStoreMixin.derive(AjaxStore));
const AssignmentStore = class extends AssignmentStoreMixin_default(EngineMixin6) {
  static $name = 'AssignmentStore';
  static get defaultConfig() {
    return { 'modelClass': AssignmentModel };
  }
};
AssignmentStore._$name = 'AssignmentStore';
const canonicalDependencyTypes = [
  'SS',
  'SF',
  'FS',
  'FF'
];
const DependencyBaseModel = class _DependencyBaseModel extends Model {
  static get $name() {
    return 'DependencyBaseModel';
  }
  static get Type() {
    return {
      'StartToStart': 0,
      'StartToEnd': 1,
      'EndToStart': 2,
      'EndToEnd': 3
    };
  }
  static get fields() {
    return [
      { 'name': 'from' },
      { 'name': 'to' },
      {
        'name': 'type',
        'type': 'int',
        'defaultValue': 2
      },
      {
        'name': 'cls',
        'defaultValue': ''
      },
      {
        'name': 'bidirectional',
        'type': 'boolean'
      },
      {
        'name': 'fromSide',
        'type': 'string'
      },
      {
        'name': 'toSide',
        'type': 'string'
      },
      {
        'name': 'lag',
        'type': 'number',
        'allowNull': true,
        'defaultValue': 0
      },
      {
        'name': 'lagUnit',
        'type': 'string',
        'defaultValue': 'd'
      },
      {
        'name': 'highlighted',
        'persist': false,
        'internal': true
      }
    ];
  }
  construct(a) {
    const b = a[this.fieldMap.from.dataSource], c = a[this.fieldMap.to.dataSource];
    if (b != null) {
      a.fromEvent = b;
    }
    if (c != null) {
      a.toEvent = c;
    }
    super.construct(...arguments);
  }
  get eventStore() {
    let a;
    return this.eventStore || ((a = this.unjoinedStores[0]) == null ? void 0 : a.eventStore);
  }
  set from(a) {
    const {fromEvent: b} = this;
    if ((b == null ? void 0 : b.isModel) && b.id === a) {
      this.set('from', a);
    } else {
      this.fromEvent = a;
    }
  }
  get from() {
    return this.get('from');
  }
  set to(a) {
    const {toEvent: b} = this;
    if ((b == null ? void 0 : b.isModel) && b.id === a) {
      this.set('to', a);
    } else {
      this.toEvent = a;
    }
  }
  get to() {
    return this.get('to');
  }
  get hardType() {
    return this.getHardType();
  }
  set hardType(a) {
    this.setHardType(a);
  }
  getHardType() {
    return this.get('type');
  }
  setHardType(a) {
    let b;
    if (a !== this.hardType) {
      b = this.set({
        'type': a,
        'fromSide': null,
        'toSide': null
      });
    }
    return b;
  }
  get lag() {
    return this.get('lag');
  }
  set lag(a) {
    this.setLag(a);
  }
  setLag(a, b = this.lagUnit) {
    if (arguments.length === 1) {
      if (typeof a === 'number') {
        this.lag = a;
      } else {
        a = DateHelper.parseDuration(a);
        this.set({
          'lag': a.magnitude,
          'lagUnit': a.unit
        });
      }
      return;
    }
    a = parseFloat(a);
    this.set({
      'lag': a,
      'lagUnit': b
    });
  }
  getLag() {
    if (this.lag) {
      return '' + (this.lag < 0 ? '-' : '+') + Math.abs(this.lag) + DateHelper.getShortNameOfUnit(this.lagUnit);
    }
    return '';
  }
  get fullLag() {
    return new Duration({
      'unit': this.lagUnit,
      'magnitude': this.lag
    });
  }
  set fullLag(a) {
    if (typeof a === 'string') {
      this.setLag(a);
    } else {
      this.setLag(a.magnitude, a.unit);
    }
  }
  get isPersistable() {
    const a = this, {
        stores: b,
        unjoinedStores: c
      } = a, e = b[0];
    let f;
    if (e) {
      const {
          fromEvent: g,
          toEvent: i
        } = a, j = e.crudManager;
      f = g && (j || !g.hasGeneratedId) && i && (j || !i.hasGeneratedId);
    } else {
      f = Boolean(c[0]);
    }
    return f && super.isPersistable;
  }
  getDateRange() {
    const {
      fromEvent: a,
      toEvent: b
    } = this;
    if ((a == null ? void 0 : a.isScheduled) && (b == null ? void 0 : b.isScheduled)) {
      const c = _DependencyBaseModel.Type;
      let e, f;
      switch (this.type) {
      case c.StartToStart:
        e = a.startDateMS;
        f = b.startDateMS;
        break;
      case c.StartToEnd:
        e = a.startDateMS;
        f = b.endDateMS;
        break;
      case c.EndToEnd:
        e = a.endDateMS;
        f = b.endDateMS;
        break;
      case c.EndToStart:
        e = a.endDateMS;
        f = b.startDateMS;
        break;
      default:
        throw new Error('Invalid dependency type: ' + this.type);
      }
      return {
        'start': Math.min(e, f),
        'end': Math.max(e, f)
      };
    }
    return null;
  }
  highlight(a) {
    let b, c;
    const e = (c = (b = this.highlighted) == null ? void 0 : b.split(' ')) != null ? c : [];
    if (!e.includes(a)) {
      this.highlighted = e.concat(a).join(' ');
    }
  }
  unhighlight(a) {
    const {highlighted: b} = this;
    if (b) {
      const c = b.split(' '), e = c.indexOf(a);
      if (e >= 0) {
        c.splice(e, 1);
        this.highlighted = c.join(' ');
      }
    }
  }
  isHighlightedWith(a) {
    return this.highlighted && this.highlighted.split(' ').includes(a);
  }
  getConnectorString(a) {
    const b = canonicalDependencyTypes[this.type];
    if (a) {
      return b;
    }
    if (this.type === _DependencyBaseModel.Type.EndToStart) {
      return '';
    }
    return b;
  }
  toString() {
    return '' + this.from + this.getConnectorString() + this.getLag();
  }
  get isValid() {
    const {
      fromEvent: a,
      toEvent: b,
      type: c
    } = this;
    return typeof c === 'number' && a && b && a !== b;
  }
  get fromEventName() {
    let a;
    return ((a = this.fromEvent) == null ? void 0 : a.name) || '';
  }
  get toEventName() {
    let a;
    return ((a = this.toEvent) == null ? void 0 : a.name) || '';
  }
  shouldRecordFieldChange(a, b, c) {
    let e;
    if (!super.shouldRecordFieldChange(a, b, c)) {
      return false;
    }
    if (a === 'from' || a === 'to' || a === 'fromEvent' || a === 'toEvent') {
      const f = (e = this.project) == null ? void 0 : e.eventStore;
      if (f && f.oldIdMap[b] === f.getById(c)) {
        return false;
      }
    }
    return true;
  }
  get skipCopyFields() {
    let a, b, c, e;
    const {fieldMap: f} = this;
    return {
      [f.from.dataSource]: true,
      [f.to.dataSource]: true,
      [(a = f.fromEvent) == null ? void 0 : a.dataSource]: true,
      [(b = f.toEvent) == null ? void 0 : b.dataSource]: true,
      [(c = f.fromTask) == null ? void 0 : c.dataSource]: true,
      [(e = f.toTask) == null ? void 0 : e.dataSource]: true
    };
  }
};
DependencyBaseModel.exposeProperties();
DependencyBaseModel._$name = 'DependencyBaseModel';
const CoreDependencyMixin = class extends Mixin([CorePartOfProjectModelMixin], a => {
  const b = a.prototype;
  class c extends a {
    static get fields() {
      return [
        {
          'name': 'fromEvent',
          'isEqual': (e, f) => e === f,
          'persist': false
        },
        {
          'name': 'toEvent',
          'isEqual': (e, f) => e === f,
          'persist': false
        }
      ];
    }
    setChanged(e, f, g) {
      let i, j, k;
      let l = false;
      if (e === 'fromEvent' || e === 'toEvent') {
        const n = isInstanceOf(f, CoreEventMixin) ? f : (i = this.eventStore) == null ? void 0 : i.getById(f);
        if (n)
          l = true;
        f = n || f;
      }
      b.setChanged.call(this, e, f, g, true);
      if (l && !this.project.isPerformingCommit && !((j = this.dependencyStore) == null ? void 0 : j.isLoadingData)) {
        (k = this.dependencyStore) == null ? void 0 : k.invalidateIndices();
      }
    }
    joinProject() {
      b.joinProject.call(this);
      this.setChanged('fromEvent', this.get('fromEvent'));
      this.setChanged('toEvent', this.get('toEvent'));
    }
    calculateInvalidated() {
      let e, f;
      const {
        fromEvent: g,
        toEvent: i
      } = this.$changed;
      if (g !== null && !isInstanceOf(g, CoreEventMixin)) {
        const j = (e = this.eventStore) == null ? void 0 : e.getById(g);
        if (j)
          this.$changed.fromEvent = j;
      }
      if (i !== null && !isInstanceOf(i, CoreEventMixin)) {
        const k = (f = this.eventStore) == null ? void 0 : f.getById(i);
        if (k)
          this.$changed.toEvent = k;
      }
    }
    set fromEvent(e) {
      this.setChanged('fromEvent', e);
    }
    get fromEvent() {
      const e = this.get('fromEvent');
      return (e == null ? void 0 : e.id) != null ? e : null;
    }
    set toEvent(e) {
      this.setChanged('toEvent', e);
    }
    get toEvent() {
      const e = this.get('toEvent');
      return (e == null ? void 0 : e.id) != null ? e : null;
    }
  }
  return c;
}) {
};
const EngineMixin7 = CoreDependencyMixin;
const DependencyModel = class extends PartOfProject_default(EngineMixin7.derive(DependencyBaseModel)) {
  static get $name() {
    return 'DependencyModel';
  }
  getTypeFromSides(a, b, c) {
    const e = DependencyBaseModel.Type, f = c ? 'right' : 'left', g = c ? 'left' : 'right';
    if (a === f) {
      return b === f ? e.StartToStart : e.StartToEnd;
    }
    return b === g ? e.EndToEnd : e.EndToStart;
  }
};
DependencyModel.exposeProperties();
DependencyModel._$name = 'DependencyModel';
const DependencyStoreMixin_default = a => class b extends a {
  static get $name() {
    return 'DependencyStoreMixin';
  }
  static get defaultConfig() {
    return {
      'loadPriority': 400,
      'syncPriority': 400,
      'id': 'dependencies'
    };
  }
  reduceEventDependencies(c, e, f, g = true, i) {
    i = i || (j => this.getEventDependencies(j));
    c = ArrayHelper.asArray(c);
    c.reduce((j, k) => {
      if (k.children && !g) {
        k.traverse(l => {
          j = i(l).reduce(e, j);
        });
      } else {
        j = i(k).reduce(e, j);
      }
    }, f);
    return f;
  }
  mapEventDependencies(c, e, f, g, i) {
    return this.reduceEventDependencies(c, (j, k) => {
      f(k) && j.push(k);
      return j;
    }, [], g, i);
  }
  mapEventPredecessors(c, e, f, g) {
    return this.reduceEventPredecessors(c, (i, j) => {
      f(j) && i.push(j);
      return i;
    }, [], g);
  }
  mapEventSuccessors(c, e, f, g) {
    return this.reduceEventSuccessors(c, (i, j) => {
      f(j) && i.push(j);
      return i;
    }, [], g);
  }
  getEventDependencies(c) {
    return [].concat(c.predecessors || [], c.successors || []);
  }
  removeEventDependencies(c) {
    this.remove(this.getEventDependencies(c));
  }
  removeEventPredecessors(c) {
    this.remove(c.predecessors);
  }
  removeEventSuccessors(c, e) {
    this.remove(c.successors);
  }
  getBySourceTargetId(c) {
    return this.records.find(e => c == this.constructor.makeDependencySourceTargetCompositeKey(e.from, e.to));
  }
  getDependencyForSourceAndTargetEvents(c, e) {
    c = Model.asId(c);
    e = Model.asId(e);
    return this.getBySourceTargetId(this.constructor.makeDependencySourceTargetCompositeKey(c, e));
  }
  getEventsLinkingDependency(c, e) {
    return this.getDependencyForSourceAndTargetEvents(c, e) || this.getDependencyForSourceAndTargetEvents(e, c);
  }
  async isValidDependency(c, e, f) {
    let g = c, i = e;
    if (c == null) {
      return false;
    }
    if (c.isDependencyModel) {
      ({
        fromEvent: g,
        toEvent: i
      } = c);
    }
    g = this.eventStore.getById(g);
    i = this.eventStore.getById(i);
    if (g && i) {
      if (!g.project || !i.project) {
        return false;
      }
      return this.project.isValidDependency(g, i, f);
    }
    return c !== e;
  }
  isValidDependencyToCreate(c, e, f) {
    return this.isValidDependency(c, e, f);
  }
  getHighlightedDependencies(c) {
    return this.records.reduce((e, f) => {
      if (f.isHighlightedWith(c))
        e.push(f);
      return e;
    }, []);
  }
  static makeDependencySourceTargetCompositeKey(c, e) {
    return 'source(' + c + ')-target(' + e + ')';
  }
  getTimeSpanDependencies(c) {
    return this.getEventDependencies(c);
  }
};
const AbstractDependencyStoreMixin = class extends Mixin([AbstractPartOfProjectStoreMixin], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.dependenciesForRemoval = new Set();
      this.allDependenciesForRemoval = false;
    }
    remove(e, f) {
      this.dependenciesForRemoval = CIFromSetOrArrayOrValue(e).toSet();
      const g = b.remove.call(this, e, f);
      this.dependenciesForRemoval.clear();
      return g;
    }
    removeAll(e) {
      this.allDependenciesForRemoval = true;
      const f = b.removeAll.call(this, e);
      this.allDependenciesForRemoval = false;
      return f;
    }
  }
  return c;
}) {
};
const emptySet2 = new Set();
const CoreDependencyStoreMixin = class extends Mixin([
  AbstractDependencyStoreMixin,
  CorePartOfProjectStoreMixin
], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.dependenciesForRemoval = new Set();
      this.allDependenciesForRemoval = false;
    }
    static get defaultConfig() {
      return {
        'modelClass': CoreDependencyMixin,
        'storage': {
          'extraKeys': [
            {
              'property': 'fromEvent',
              'unique': false
            },
            {
              'property': 'toEvent',
              'unique': false
            }
          ]
        }
      };
    }
    getIncomingDepsForEvent(e) {
      return this.storage.findItem('toEvent', e) || emptySet2;
    }
    getOutgoingDepsForEvent(e) {
      return this.storage.findItem('fromEvent', e) || emptySet2;
    }
    set data(e) {
      this.allDependenciesForRemoval = true;
      super.data = e;
      this.allDependenciesForRemoval = false;
    }
    updateIndices() {
      this.storage.rebuildIndices();
    }
    invalidateIndices() {
      this.storage.invalidateIndices();
    }
    onCommitAsync() {
      this.updateIndices();
    }
  }
  return c;
}) {
};
const EngineMixin8 = PartOfProject_default(CoreDependencyStoreMixin.derive(AjaxStore));
const DependencyStore = class extends DependencyStoreMixin_default(EngineMixin8.derive(AjaxStore)) {
  static get defaultConfig() {
    return { 'modelClass': DependencyModel };
  }
};
DependencyStore._$name = 'DependencyStore';
const AbstractCrudManagerValidation_default = a => class b extends a {
  static get $name() {
    return 'AbstractCrudManagerValidation';
  }
  static get configurable() {
    return {
      'validateResponse': true,
      'skipSuccessProperty': true,
      'crudLoadValidationWarningPrefix': 'CrudManager load response error(s):',
      'crudSyncValidationWarningPrefix': 'CrudManager sync response error(s):',
      'supportShortSyncResponseNote': 'Note: Please consider enabling "supportShortSyncResponse" option to allow less detailed sync responses (https://bryntum.com/products/scheduler/docs/api/Scheduler/crud/AbstractCrudManagerMixin#config-supportShortSyncResponse)',
      'disableValidationNote': 'Note: To disable this validation please set the "validateResponse" config to false'
    };
  }
  get crudLoadValidationMandatoryStores() {
    return [];
  }
  getStoreLoadResponseWarnings(c, e, f) {
    const g = [], {storeId: i} = c, j = this.crudLoadValidationMandatoryStores, k = { [i]: {} };
    if (e) {
      if (!e.rows) {
        g.push('- "' + i + '" store section should have a "rows" property with an array of the store records.');
        k[i].rows = ['...'];
      }
    } else if (j == null ? void 0 : j.includes(i)) {
      g.push('- No "' + i + '" store section found. It should contain the store data.');
      k[i].rows = ['...'];
    }
    if (g.length) {
      Object.assign(f, k);
    }
    return g;
  }
  getLoadResponseWarnings(c, e) {
    const f = [], g = {};
    if (!this.skipSuccessProperty) {
      g.success = true;
    }
    this.forEachCrudStore((i, j, k) => {
      let l;
      if (!((l = e == null ? void 0 : e.stores) == null ? void 0 : l.length) || e.stores.includes(j)) {
        f.push(...this.getStoreLoadResponseWarnings(k, c == null ? void 0 : c[j], g));
      }
    });
    if (f.length) {
      f.push('Please adjust your response to look like this:\n' + JSON.stringify(g, null, 4).replace(/"\.\.\."/g, '...'));
      f.push(this.disableValidationNote);
    }
    return f;
  }
  validateLoadResponse(c, e) {
    const f = this.getLoadResponseWarnings(c, e);
    if (f.length) {
      console.warn(this.crudLoadValidationWarningPrefix + '\n' + f.join('\n'));
    }
  }
  getStoreSyncResponseWarnings(c, e, f, g) {
    const i = [], j = [], k = [], {storeId: l} = c, n = { [l]: {} }, o = c.phantomIdField || this.phantomIdField, {modelClass: p} = c.store, {idField: q} = p, r = (f == null ? void 0 : f.rows) || [], u = (f == null ? void 0 : f.removed) || [];
    let v = false;
    if (e == null ? void 0 : e.added) {
      j.push(...e.added.filter(w => {
        return !r.find(z => z[o] == w[o]) && !u.find(z => z[o] == w[o] || z[q] == w[o]);
      }).map(w => ({
        [o]: w[o],
        [q]: '...'
      })));
      if (j.length) {
        const w = j.map(z => '#' + z[o]).join(', ');
        i.push('- "' + l + '" store "rows" section should mention added record(s) ' + w + ' sent in the request. It should contain the added records identifiers (both phantom and "real" ones assigned by the backend).');
      }
    }
    if (this.supportShortSyncResponse) {
      if (!j.length && f) {
        if (typeof f !== 'object' || Array.isArray(f)) {
          i.push('- "' + l + '" store section should be an Object.');
          n[l]['...'] = '...';
        }
        if (f.rows && !Array.isArray(f.rows)) {
          i.push('- "' + l + '" store "rows" section should be an array');
          j.push('...');
        }
        if (f.removed && !Array.isArray(f.removed)) {
          i.push('- "' + l + '" store "removed" section should be an array:');
          k.push('...');
        }
      }
    } else {
      if (e == null ? void 0 : e.updated) {
        const z = e.updated.filter(A => !r.find(B => B[q] == A[q])).map(A => ({ [q]: A[q] }));
        j.push(...z);
        if (z.length) {
          const A = z.map(B => '#' + B[q]).join(', ');
          i.push('- "' + l + '" store "rows" section should mention updated record(s) ' + A + ' sent in the request. It should contain the updated record identifiers.');
          v = true;
        }
      }
      if (j.length) {
        j.push('...');
      }
      if (e == null ? void 0 : e.removed) {
        k.push(...e.removed.filter(B => !r.find(C => C[q] == B[q])).map(B => ({ [q]: B[q] })));
        if (k.length) {
          const B = k.map(C => '#' + C[q]).join(', ');
          i.push('- "' + l + '" store "removed" section should mention removed record(s) ' + B + ' sent in the request. It should contain the removed record identifiers.');
          n[l].removed = k;
          k.push('...');
          v = true;
        }
      }
    }
    if (j.length) {
      n[l].rows = j;
    }
    if (!i.length) {
      delete n[l];
    }
    Object.assign(g, n);
    return {
      'messages': i,
      'showSupportShortSyncResponseNote': v
    };
  }
  getSyncResponseWarnings(c, e) {
    const f = [], g = {}, i = e.pack;
    if (!this.skipSuccessProperty) {
      g.success = true;
    }
    let j = false;
    this.forEachCrudStore((k, l, n) => {
      const o = this.getStoreSyncResponseWarnings(n, i == null ? void 0 : i[l], c[l], g);
      j = j || o.showSupportShortSyncResponseNote;
      f.push(...o.messages);
    });
    if (f.length) {
      f.push('Please adjust your response to look like this:\n' + JSON.stringify(g, null, 4).replace(/"\.\.\.":\s*"\.\.\."/g, ',,,').replace(/"\.\.\."/g, '...'));
      if (j) {
        f.push(this.supportShortSyncResponseNote);
      }
      f.push(this.disableValidationNote);
    }
    return f;
  }
  validateSyncResponse(c, e) {
    const f = this.getSyncResponseWarnings(c, e);
    if (f.length) {
      console.warn(this.crudSyncValidationWarningPrefix + '\n' + f.join('\n'));
    }
  }
};
const AbstractCrudManagerError = class extends Error {
};
const CrudManagerRequestError = class extends AbstractCrudManagerError {
  constructor(a = {}) {
    let b, c;
    super(a.message || a.request && StringHelper.capitalize((b = a.request) == null ? void 0 : b.type) + ' failed' || 'Crud Manager request failed');
    Object.assign(this, a);
    this.action = (c = this.request) == null ? void 0 : c.type;
  }
};
const storeSortFn = function (a, b, c) {
  if (a.store) {
    a = a.store;
  }
  if (b.store) {
    b = b.store;
  }
  a = a[c] || 0;
  b = b[c] || 0;
  return a < b ? -1 : a > b ? 1 : 0;
};
const storeLoadSortFn = function (a, b) {
  return storeSortFn(a, b, 'loadPriority');
};
const storeSyncSortFn = function (a, b) {
  return storeSortFn(a, b, 'syncPriority');
};
const AbstractCrudManagerMixin_default = a => {
  let b;
  a.$$meta = a.$meta;
  const c = [];
  if (!a.isEvents) {
    c.push(Events_default);
  }
  if (!a.isDelayable) {
    c.push(Delayable_default);
  }
  c.push(AbstractCrudManagerValidation_default);
  return b = class extends (a || Base).mixin(...c) {
    static get $name() {
      return 'AbstractCrudManagerMixin';
    }
    static get defaultConfig() {
      return {
        'crudRevision': null,
        'crudStores': [],
        'storeIdProperty': 'id',
        'crudFilterParam': 'filter',
        'transport': {},
        'trackResponseType': false,
        'supportShortSyncResponse': true,
        'phantomIdField': '$PhantomId',
        'phantomParentIdField': '$PhantomParentId',
        'autoLoad': false,
        'autoSyncTimeout': 100,
        'autoSync': false,
        'resetIdsBeforeSync': true,
        'syncApplySequence': [],
        'orderedCrudStores': [],
        'writeAllFields': false,
        'crudIgnoreUpdates': 0,
        'autoSyncSuspendCounter': 0,
        'crudLoaded': false,
        'applyingLoadResponse': false,
        'applyingSyncResponse': false,
        'callOnFunctions': true
      };
    }
    get isCrudManager() {
      return true;
    }
    construct(e = {}) {
      this._requestId = 0;
      this.activeRequests = {};
      this.crudStoresIndex = {};
      super.construct(e);
    }
    afterConstruct() {
      super.afterConstruct();
      if (this.autoLoad) {
        this._autoLoadPromise = this.doAutoLoad();
      }
    }
    get loadUrl() {
      let e, f;
      return (f = (e = this.transport) == null ? void 0 : e.load) == null ? void 0 : f.url;
    }
    updateLoadUrl(e) {
      ObjectHelper.setPath(this, 'transport.load.url', e);
    }
    get syncUrl() {
      let e, f;
      return (f = (e = this.transport) == null ? void 0 : e.sync) == null ? void 0 : f.url;
    }
    updateSyncUrl(e) {
      ObjectHelper.setPath(this, 'transport.sync.url', e);
    }
    get shouldIgnoreRemoteChangesInSTM() {
      return this.ignoreRemoteChangesInSTM;
    }
    getStoreDescriptor(e) {
      if (!e) {
        return null;
      }
      if (e instanceof Store) {
        return this.crudStores.find(f => f.store === e);
      }
      if (typeof e === 'object') {
        return this.crudStoresIndex[e.storeId];
      }
      return this.crudStoresIndex[e] || this.getStoreDescriptor(Store.getStore(e));
    }
    fillStoreDescriptor(e) {
      const {store: f} = e, {
          storeIdProperty: storeIdProperty = this.storeIdProperty,
          modelClass: g
        } = f;
      if (!e.storeId) {
        e.storeId = f[storeIdProperty] || f.id;
      }
      if (!e.idField) {
        e.idField = g.idField;
      }
      if (!e.phantomIdField) {
        e.phantomIdField = g.phantomIdField;
      }
      if (!e.phantomParentIdField) {
        e.phantomParentIdField = g.phantomParentIdField;
      }
      if (!('writeAllFields' in e)) {
        e.writeAllFields = f.writeAllFields;
      }
      return e;
    }
    updateCrudStoreIndex() {
      const e = this.crudStoresIndex = {};
      this.crudStores.forEach(f => f.storeId && (e[f.storeId] = f));
    }
    getCrudStore(e) {
      const f = this.getStoreDescriptor(e);
      return f == null ? void 0 : f.store;
    }
    forEachCrudStore(e, f = this) {
      if (!e) {
        throw new Error('Iterator function must be provided');
      }
      this.crudStores.every(g => e.call(f, g.store, g.storeId, g) !== false);
    }
    set crudStores(e) {
      this._crudStores = [];
      this.addCrudStore(e);
      for (const f of this._crudStores) {
        f.loadPriority = f.syncPriority = 0;
      }
    }
    get crudStores() {
      return this._crudStores;
    }
    get orderedCrudStores() {
      return this._orderedCrudStores;
    }
    set orderedCrudStores(e) {
      return this._orderedCrudStores = e;
    }
    set syncApplySequence(e) {
      this._syncApplySequence = [];
      this.addStoreToApplySequence(e);
    }
    get syncApplySequence() {
      return this._syncApplySequence;
    }
    internalAddCrudStore(e) {
      const f = this;
      let g;
      if (e instanceof Store) {
        g = { 'store': e };
      } else if (typeof e === 'object') {
        if (!e.store) {
          e = {
            'storeId': e.id,
            'store': new Store(e)
          };
        }
        g = e;
      } else {
        g = { 'store': Store.getStore(e) };
      }
      f.fillStoreDescriptor(g);
      e = g.store;
      if (e.setCrudManager) {
        e.setCrudManager(f);
      } else {
        e.crudManager = f;
      }
      e.pageSize = null;
      if (f.loadUrl || f.syncUrl) {
        e.autoCommit = false;
        e.autoLoad = false;
        if (e.createUrl || e.updateUrl || e.deleteUrl || e.readUrl) {
          console.warn('You have configured an URL on a Store that is handled by a CrudManager that is also configured with an URL. The Store URL\'s should be removed.');
        }
      }
      f.bindCrudStoreListeners(e);
      return g;
    }
    addCrudStore(e, f, g) {
      let i;
      e = ArrayHelper.asArray(e);
      if (!(e == null ? void 0 : e.length)) {
        return;
      }
      const j = this, k = e.map(j.internalAddCrudStore, j);
      if (typeof f === 'undefined') {
        j.crudStores.push(...k);
      } else {
        if (g) {
          if (g instanceof Store || typeof g !== 'object')
            g = j.getStoreDescriptor(g);
          f += j.crudStores.indexOf(g);
        }
        j.crudStores.splice(f, 0, ...k);
      }
      j.orderedCrudStores.push(...k);
      j.updateCrudStoreIndex();
      (i = j.afterAddStore) == null ? void 0 : i.call(j, k);
    }
    addPrioritizedStore(e) {
      const f = this;
      if (!f.hasCrudStore(e)) {
        f.addCrudStore(e, ArrayHelper.findInsertionIndex(e, f.crudStores, storeLoadSortFn));
      }
      if (!f.hasApplySequenceStore(e)) {
        f.addStoreToApplySequence(e, ArrayHelper.findInsertionIndex(e, f.syncApplySequence, storeSyncSortFn));
      }
    }
    hasCrudStore(e) {
      let f;
      return (f = this.crudStores) == null ? void 0 : f.some(g => g === e || g.store === e || g.storeId === e);
    }
    removeCrudStore(e) {
      const f = this, g = f.crudStores, i = g.find(j => j === e || j.store === e || j.storeId === e);
      if (i) {
        f.unbindCrudStoreListeners(i.store);
        delete f.crudStoresIndex[i.storeId];
        ArrayHelper.remove(g, i);
        if (f.syncApplySequence) {
          f.removeStoreFromApplySequence(e);
        }
      } else {
        throw new Error('Store not found in stores collection');
      }
    }
    bindCrudStoreListeners(e) {
      e.ion({
        'name': e.id,
        'addConfirmed': 'onCrudStoreChange',
        'change': 'onCrudStoreChange',
        'destroy': 'onCrudStoreDestroy',
        'thisObj': this
      });
    }
    unbindCrudStoreListeners(e) {
      this.detachListeners(e.id);
    }
    addStoreToApplySequence(e, f, g) {
      if (!e) {
        return;
      }
      e = ArrayHelper.asArray(e);
      const i = this, j = e.reduce((l, n) => {
          const o = i.getStoreDescriptor(n);
          o && l.push(o);
          return l;
        }, []);
      if (typeof f === 'undefined') {
        i.syncApplySequence.push(...j);
      } else {
        let l = f;
        if (g) {
          if (g instanceof Store || typeof g !== 'object')
            g = i.getStoreDescriptor(g);
          l += i.syncApplySequence.indexOf(g);
        }
        i.syncApplySequence.splice(l, 0, ...j);
      }
      const k = i.syncApplySequence.map(({storeId: n}) => n);
      i.orderedCrudStores = [...i.syncApplySequence];
      i.crudStores.forEach(n => {
        if (!k.includes(n.storeId)) {
          i.orderedCrudStores.push(n);
        }
      });
    }
    removeStoreFromApplySequence(e) {
      const f = this.syncApplySequence.findIndex(g => g === e || g.store === e || g.storeId === e);
      if (f > -1) {
        this.syncApplySequence.splice(f, 1);
        this.orderedCrudStores.splice(f, 1);
      }
    }
    hasApplySequenceStore(e) {
      return this.syncApplySequence.some(f => f === e || f.store === e || f.storeId === e);
    }
    onCrudStoreDestroy({source: e}) {
      this.removeCrudStore(e);
    }
    onCrudStoreChange(e) {
      const f = this;
      if (f.crudIgnoreUpdates) {
        return;
      }
      if (f.crudStoreHasChanges(e == null ? void 0 : e.source)) {
        f.trigger('hasChanges');
        if (f.autoSync) {
          f.scheduleAutoSync();
        }
      } else {
        f.trigger('noChanges');
      }
    }
    suspendAutoSync() {
      this.autoSyncSuspendCounter++;
    }
    resumeAutoSync(e = true) {
      const f = this;
      f.autoSyncSuspendCounter--;
      if (f.autoSyncSuspendCounter <= 0) {
        f.autoSyncSuspendCounter = 0;
        if (e && f.autoSync && f.crudStoreHasChanges()) {
          f.scheduleAutoSync();
        }
      }
    }
    get isAutoSyncSuspended() {
      return this.autoSyncSuspendCounter > 0;
    }
    scheduleAutoSync() {
      const e = this;
      if (!e.hasTimeout('autoSync') && !e.isAutoSyncSuspended) {
        e.setTimeout({
          'name': 'autoSync',
          'fn': () => {
            e.sync().catch(f => {
            });
          },
          'delay': e.autoSyncTimeout
        });
      }
    }
    async triggerFailedRequestEvents(e, f, g, i) {
      const {
        options: j,
        type: k
      } = e;
      this.trigger('requestFail', {
        'requestType': k,
        'response': f,
        'responseText': g,
        'responseOptions': i
      });
      this.trigger(k + 'Fail', {
        'response': f,
        'responseOptions': i,
        'responseText': g,
        'options': j
      });
    }
    async internalOnResponse(e, f, g) {
      const i = this, j = f ? i.decode(f) : null, {
          options: k,
          type: l
        } = e;
      if (f && !j) {
        console.error('Failed to parse response: ' + f);
      }
      if (!j || (i.skipSuccessProperty ? j.success === false : !j.success)) {
        i.triggerFailedRequestEvents(e, j, f, g);
      } else if (i.trigger('beforeResponseApply', {
          'requestType': l,
          'response': j
        }) !== false && i.trigger('before' + StringHelper.capitalize(l) + 'Apply', {
          'response': j,
          'options': k
        }) !== false) {
        i.crudRevision = j.revision;
        await i.applyResponse(e, j, k);
        if (i.isDestroyed) {
          return;
        }
        i.trigger('requestDone', {
          'requestType': l,
          'response': j,
          'responseOptions': g
        });
        i.trigger(l, {
          'response': j,
          'responseOptions': g,
          'options': k
        });
        if (l === 'load' || !i.crudStoreHasChanges()) {
          i.trigger('noChanges');
          if (l === 'load') {
            i.emitCrudStoreEvents(e.pack.stores, 'afterRequest');
          }
        }
      }
      return j;
    }
    suspendChangeTracking() {
      this.crudIgnoreUpdates++;
      this.crudStores.forEach(e => e.store.suspendFilterOnUpdate());
    }
    resumeChangeTracking(e) {
      this.crudStores.forEach(f => f.store.resumeFilterOnUpdate());
      if (this.crudIgnoreUpdates && !--this.crudIgnoreUpdates && !e) {
        this.onCrudStoreChange();
      }
    }
    get isChangeTrackingSuspended() {
      return this.crudIgnoreUpdates > 0;
    }
    crudStoreHasChanges(e) {
      return e ? this.isCrudStoreDirty(this.getCrudStore(e)) : this.crudStores.some(f => this.isCrudStoreDirty(f.store));
    }
    isCrudStoreDirty(e) {
      return e.hasChanges;
    }
    doAutoLoad() {
      return this.load().catch(e => {
      });
    }
    emitCrudStoreEvents(e, f, g) {
      const i = {
        'action': 'read' + f,
        ...g
      };
      for (const j of this.crudStores) {
        if (e.includes(j.storeId)) {
          j.store.trigger(f, i);
        }
      }
    }
    getLoadPackage(e) {
      const f = {
          'type': 'load',
          'requestId': this.requestId
        }, g = this.crudStores, i = Object.assign({}, e);
      delete i.request;
      f.stores = g.map(j => {
        let k;
        const l = i == null ? void 0 : i[j.storeId], n = j.pageSize || ((k = j.store) == null ? void 0 : k.pageSize);
        if (l || n) {
          const o = Object.assign({
            'storeId': j.storeId,
            'page': 1
          }, l);
          if (n) {
            o.pageSize = n;
          }
          j.currentPage = o.page;
          if (l) {
            delete i[j.storeId];
          }
          return o;
        }
        return j.storeId;
      });
      Object.assign(f, i);
      return f;
    }
    loadCrudStore(e, f, g) {
      const i = f == null ? void 0 : f.rows;
      if ((g == null ? void 0 : g.append) || (f == null ? void 0 : f.append)) {
        e.add(i, false, { 'clean': true });
      } else {
        e.data = i;
      }
      e.trigger('load', { 'data': i });
    }
    loadDataToCrudStore(e, f, g) {
      const i = e.store, j = f == null ? void 0 : f.rows;
      i.__loading = true;
      if (j) {
        this.loadCrudStore(i, f, g, e);
      }
      i.__loading = false;
    }
    loadCrudManagerData(e, f = {}) {
      const g = this;
      g.trigger('beforeLoadCrudManagerData');
      g.suspendChangeTracking();
      g.crudStores.forEach(i => {
        const j = i.storeId, k = e[j];
        if (k) {
          g.loadDataToCrudStore(i, k, f[j]);
        }
      });
      g.resumeChangeTracking(true);
      g.trigger('loadCrudManagerData');
    }
    get isCrudManagerLoading() {
      return Boolean(this.activeRequests.load || this.applyingLoadResponse);
    }
    get isCrudManagerSyncing() {
      return Boolean(this.activeRequests.sync || this.applyingSyncResponse);
    }
    get isLoadingOrSyncing() {
      return Boolean(this.isCrudManagerLoading || this.isCrudManagerSyncing);
    }
    load(e) {
      if (typeof e === 'string') {
        e = { 'request': { 'url': e } };
      }
      const f = this, g = f.getLoadPackage(e);
      f._autoLoadPromise = null;
      return new Promise((i, j) => {
        if (f.trigger('beforeLoad', { 'pack': g }) !== false) {
          const {load: k} = f.activeRequests;
          if (k) {
            f.cancelRequest(k.desc, k.reject);
            f.trigger('loadCanceled', { 'pack': g });
          }
          const l = Objects.assign({
            'id': g.requestId,
            'data': f.encode(g),
            'type': 'load',
            'success': f.onCrudRequestSuccess,
            'failure': f.onCrudRequestFailure,
            'thisObj': f
          }, e == null ? void 0 : e.request);
          f.activeRequests.load = {
            'type': 'load',
            'options': e,
            'pack': g,
            'resolve': i,
            'reject'(...n) {
              l.success = l.failure = null;
              j(...n);
            },
            'id': g.requestId,
            'desc': f.sendRequest(l)
          };
          f.emitCrudStoreEvents(g.stores, 'loadStart');
          f.trigger('loadStart', { 'pack': g });
        } else {
          f.trigger('loadCanceled', { 'pack': g });
          j({ 'cancelled': true });
        }
      });
    }
    getActiveCrudManagerRequest(e) {
      let f = this.activeRequests[e];
      if (!f && this.trackResponseType) {
        f = Object.values(this.activeRequests)[0];
      }
      return f;
    }
    prepareAddedRecordData(e, f) {
      const g = this, {store: i} = f, {isTree: j} = i, k = f.phantomIdField || g.phantomIdField, l = f.phantomParentIdField || g.phantomParentIdField, n = i.modelClass.allFields.filter(q => q.subStore), o = e.constructor, p = Object.assign(e.persistableData, { [k]: e.id });
      if (j) {
        const {parent: q} = e;
        if (q && !q.isRoot && q.isPhantom) {
          p[l] = q.id;
        }
      }
      if (g.resetIdsBeforeSync) {
        ObjectHelper.deletePath(p, o.idField);
      }
      n.forEach(r => {
        const u = e.get(r.name);
        if (u.totalCount) {
          p[r.dataSource] = { 'added': u.getRange().map(v => g.prepareAddedRecordData(v, { 'store': u })) };
        }
      });
      return p;
    }
    prepareAdded(e, f) {
      return e.filter(g => g.isValid).map(g => this.prepareAddedRecordData(g, f));
    }
    prepareUpdated(e, f) {
      const {store: g} = f, {isTree: i} = g, j = f.writeAllFields || f.writeAllFields !== false && this.writeAllFields, k = f.phantomParentIdField || this.phantomParentIdField, l = g.modelClass.allFields.filter(n => n.subStore);
      if (f.store.tree) {
        const n = f.store.rootNode;
        e = e.filter(o => o !== n);
      }
      return e.filter(o => o.isValid).reduce((o, p) => {
        let q;
        if (j) {
          q = p.persistableData;
        } else {
          q = p.modificationDataToWrite;
        }
        if (i) {
          const {parent: r} = p;
          if (r && !r.isRoot && r.isPhantom) {
            q[k] = r.id;
          }
        }
        l.forEach(u => {
          const v = p.get(u.name);
          q[u.dataSource] = this.getCrudStoreChanges({ 'store': v });
        });
        if (!ObjectHelper.isEmpty(q)) {
          o.push(q);
        }
        return o;
      }, []);
    }
    prepareRemoved(e, {store: f}) {
      let g, i;
      let j;
      if (!this.includeChildrenInRemoveRequest) {
        j = [];
        for (const k of e) {
          const l = (i = k.parentId) != null ? i : (g = k.meta.modified) == null ? void 0 : g.parentId;
          if (l != null && (l === f.rootNode.id || !e.some(n => n.id === l))) {
            j.push(k);
          }
        }
      } else {
        j = e;
      }
      return j.map(n => ObjectHelper.setPath({}, n.constructor.idField, n.id));
    }
    getCrudStoreChanges(e) {
      const {store: f} = e;
      let {
          added: added = [],
          modified: g = [],
          removed: removed = []
        } = f.changes || {}, i;
      if (added.length)
        added = this.prepareAdded(added, e);
      if (g.length)
        g = this.prepareUpdated(g, e);
      if (removed.length)
        removed = this.prepareRemoved(removed, e);
      if (added.length || g.length || removed.length) {
        i = {};
        if (added.length)
          i.added = added;
        if (g.length)
          i.updated = g;
        if (removed.length)
          i.removed = removed;
      }
      return i;
    }
    getChangesetPackage() {
      const {changes: e} = this;
      return e || this.forceSync ? {
        'type': 'sync',
        'requestId': this.requestId,
        'revision': this.crudRevision,
        ...e
      } : null;
    }
    get changes() {
      const e = {};
      this.crudStores.forEach(f => {
        const g = this.getCrudStoreChanges(f);
        if (g) {
          e[f.storeId] = g;
        }
      });
      return Object.keys(e).length > 0 ? e : null;
    }
    getRowsToApplyChangesTo({
      store: e,
      storeId: f
    }, g, i) {
      let j, k;
      const l = this, {modelClass: n} = e, o = n.idField, {
          updated: p,
          removed: q
        } = i || {};
      let r, u, v;
      if (g) {
        v = true;
        const w = {};
        r = ((j = g.rows) == null ? void 0 : j.slice()) || [];
        u = ((k = g.removed) == null ? void 0 : k.slice()) || [];
        [
          ...r,
          ...u
        ].forEach(z => {
          const A = ObjectHelper.getPath(z, o);
          w[A] = true;
        });
        if (l.supportShortSyncResponse) {
          p == null ? void 0 : p.forEach(z => {
            const A = ObjectHelper.getPath(z, o);
            if (!w[A]) {
              r.push({ [o]: A });
            }
          });
          q == null ? void 0 : q.forEach(z => {
            const A = ObjectHelper.getPath(z, o);
            if (!w[A]) {
              u.push({ [o]: A });
            }
          });
        }
      } else if (p || q) {
        v = false;
        r = p;
        u = q;
      }
      r = (r == null ? void 0 : r.length) ? r : null;
      u = (u == null ? void 0 : u.length) ? u : null;
      return {
        'rows': r,
        'removed': u,
        'remote': v
      };
    }
    applyChangesToStore(e, f, g) {
      let i;
      const j = this, k = e.phantomIdField || j.phantomIdField, {store: l} = e, n = l.modelClass.getFieldDataSource('id'), o = l.modelClass.allFields.filter(A => A.subStore), {
          rows: p,
          removed: q,
          remote: r
        } = j.getRowsToApplyChangesTo(e, f, g), u = [], v = [];
      if (p) {
        for (const A of p) {
          if (l.getById((i = A[k]) != null ? i : A[n])) {
            v.push(A);
          } else {
            u.push(A);
          }
        }
      }
      const w = [];
      if (v.length && o.length) {
        v.forEach(B => {
          let C, E, F;
          const G = l.getById((C = B[k]) != null ? C : B[n]), H = ((E = g.added) == null ? void 0 : E.find(J => J[k] == B[k])) || ((F = g.updated) == null ? void 0 : F.find(J => J[n] == B[n]));
          const I = {};
          o.forEach(J => {
            const K = G.get(J.name);
            j.applyChangesToStore({ 'store': K }, B[J.dataSource], H == null ? void 0 : H[J.dataSource]);
            I[J.dataSource] = 'foo';
            delete B[J.dataSource];
          });
          w.push([
            G,
            I
          ]);
        });
      }
      const z = l.applyChangeset({
        'removed': q,
        'added': u,
        'updated': v
      }, null, k, r, true);
      w.forEach(([B, C]) => Object.assign(z.get(B.id), C));
      return z;
    }
    applySyncResponse(e, f) {
      let g;
      const i = this, j = i.orderedCrudStores;
      i.applyingChangeset = i.applyingSyncResponse = true;
      i.suspendChangeTracking();
      for (const k of j) {
        i.applyChangesToStore(k, e[k.storeId], (g = f == null ? void 0 : f.pack) == null ? void 0 : g[k.storeId]);
      }
      i.resumeChangeTracking(true);
      i.applyingChangeset = i.applyingSyncResponse = false;
    }
    applyLoadResponse(e, f) {
      this.applyingLoadResponse = true;
      this.loadCrudManagerData(e, f);
      this.applyingLoadResponse = false;
    }
    async applyResponse(e, f, g) {
      const i = this, j = i.trackResponseType && f.type || e.type;
      switch (j) {
      case 'load':
        if (i.validateResponse) {
          i.validateLoadResponse(f, g);
        }
        i.applyLoadResponse(f, g);
        break;
      case 'sync':
        if (i.validateResponse) {
          i.validateSyncResponse(f, e);
        }
        i.applySyncResponse(f, e);
        break;
      }
    }
    applyChangeset(e, f = null, g, i = false) {
      const j = this, k = i ? new Map() : void 0;
      j.suspendAutoSync();
      j.suspendChangeTracking();
      for (const {
            store: l,
            phantomIdField: n
          } of j.orderedCrudStores) {
        if (e[l.id]) {
          const o = l.applyChangeset(e[l.id], f, n || j.phantomIdField, true, i);
          if (o) {
            k.set(l.id, o);
          }
        }
      }
      j.resumeChangeTracking(true);
      j.resumeAutoSync(false);
      return k;
    }
    get requestId() {
      return Number.parseInt('' + Date.now() + this._requestId++);
    }
    sync() {
      const e = this;
      e.clearTimeout('autoSync');
      if (e.activeRequests.sync) {
        e.trigger('syncDelayed');
        return e.activeSyncPromise = e.activeSyncPromise.finally(() => e.sync());
      }
      return e.activeSyncPromise = new Promise((f, g) => {
        const i = e.getChangesetPackage();
        if (!i) {
          f(null);
          return;
        }
        if (e.trigger('beforeSync', { 'pack': i }) !== false) {
          e.trigger('syncStart', { 'pack': i });
          e.activeRequests.sync = {
            'type': 'sync',
            'pack': i,
            'resolve': f,
            'reject': g,
            'id': i.requestId,
            'desc': e.sendRequest({
              'id': i.requestId,
              'data': e.encode(i),
              'type': 'sync',
              'success': e.onCrudRequestSuccess,
              'failure': e.onCrudRequestFailure,
              'thisObj': e
            })
          };
        } else {
          e.trigger('syncCanceled', { 'pack': i });
          g({ 'cancelled': true });
        }
      }).catch(f => {
        if (f && !f.cancelled) {
          throw f;
        }
        return f;
      });
    }
    async onCrudRequestSuccess(f, g, i) {
      const j = this, {
          type: k,
          id: l
        } = i;
      if (j.isDestroyed)
        return;
      let n = '';
      i = j.activeRequests[k];
      try {
        n = await f.text();
      } catch (p) {
      }
      if (j.isDestroyed)
        return;
      if ((i == null ? void 0 : i.id) !== l) {
        throw new Error('Interleaved ' + k + ' operation detected');
      }
      j.activeRequests[k] = null;
      const o = await j.internalOnResponse(i, n, g);
      if (j.isDestroyed)
        return;
      if (!o || (j.skipSuccessProperty ? (o == null ? void 0 : o.success) === false : !(o == null ? void 0 : o.success))) {
        const q = {
          'rawResponse': f,
          'response': o,
          'request': i
        };
        if (o == null ? void 0 : o.message) {
          q.message = o.message;
        }
        i.reject(new CrudManagerRequestError(q));
      }
      j['crud' + StringHelper.capitalize(i.type) + 'ed'] = true;
      i.resolve({
        'response': o,
        'rawResponse': f,
        'responseText': n,
        'request': i
      });
    }
    async onCrudRequestFailure(f, g, i) {
      let j;
      const k = this;
      if (k.isDestroyed)
        return;
      i = k.activeRequests[i.type];
      const l = (j = g == null ? void 0 : g.abortController) == null ? void 0 : j.signal, n = Boolean(l == null ? void 0 : l.aborted);
      if (!n) {
        let o, p = '';
        try {
          p = await f.text();
          o = k.decode(p);
        } catch (q) {
        }
        if (k.isDestroyed)
          return;
        k.triggerFailedRequestEvents(i, o, p, g);
        if (k.isDestroyed)
          return;
        i.reject(new CrudManagerRequestError({
          'rawResponse': f,
          'request': i
        }));
      }
      k.activeRequests[i.type] = null;
    }
    acceptChanges() {
      this.crudStores.forEach(e => e.store.acceptChanges());
    }
    revertChanges() {
      this.revertCrudStoreChanges();
    }
    revertCrudStoreChanges() {
      const {usesSingleAssignment: e} = this.eventStore;
      this.orderedCrudStores.forEach(({store: f}) => (!f.isAssignmentStore || !e) && f.revertChanges());
    }
    doDestroy() {
      const e = this, {
          load: f,
          sync: g
        } = e.activeRequests;
      f && e.cancelRequest(f.desc, f.reject);
      g && e.cancelRequest(g.desc, g.reject);
      while (e.crudStores.length > 0) {
        e.removeCrudStore(e.crudStores[0]);
      }
      super.doDestroy && super.doDestroy();
    }
    get crudStoresJSON() {
      let e;
      const f = {};
      this.forEachCrudStore((g, i) => f[i + 'Data'] = g.toJSON());
      if ((e = this.assignmentStore) == null ? void 0 : e.usesSingleAssignment) {
        delete f.assignmentsData;
      }
      return f;
    }
    set crudStoresJSON(e) {
      this.forEachCrudStore((f, g) => {
        const i = g + 'Data';
        if (e[i]) {
          f.data = e[i];
        }
      });
    }
  }, __publicField(b, 'configurable', {
    'loadUrl': null,
    'syncUrl': null,
    'forceSync': null,
    'ignoreRemoteChangesInSTM': false,
    'includeChildrenInRemoveRequest': true
  }), __publicField(b, 'delayable', { 'doAutoLoad': 'raf' }), b;
};
const AjaxTransport_default = a => class b extends (a || Base) {
  static get $name() {
    return 'AjaxTransport';
  }
  static get defaultMethod() {
    return {
      'load': 'GET',
      'sync': 'POST'
    };
  }
  cancelRequest(c, e) {
    let f;
    (f = c.abort) == null ? void 0 : f.call(c);
    if (!this.isDestroying) {
      e({ 'cancelled': true });
    }
  }
  shouldUseBodyForRequestData(c, e, f) {
    return !(e === 'HEAD' || e === 'GET') && !f;
  }
  sendRequest(c) {
    const e = this, {data: f} = c, g = e.transport[c.type] || {}, i = Objects.assign({}, g);
    if (c.url) {
      i.url = c.url;
    }
    i.method = i.method || b.defaultMethod[c.type];
    i.params = Objects.assign(i.params || {}, c.params);
    let {paramName: j} = i;
    if (e.shouldUseBodyForRequestData(g, i.method, j)) {
      i.body = f;
      i.headers = i.headers || {};
      i.headers['Content-Type'] = i.headers['Content-Type'] || 'application/json';
    } else {
      j = j || 'data';
      i.params[j] = f;
    }
    if (!i.url) {
      throw new Error('Trying to request without URL specified');
    }
    delete i.paramName;
    let k, l;
    function n() {
      i.queryParams = i.params;
      delete i.params;
      let p = false;
      const q = Objects.assign({}, i);
      k = AjaxHelper.fetch(i.url, q);
      return k.catch(r => {
        let u, v;
        k.done = true;
        (u = e.trigger) == null ? void 0 : u.call(e, 'responseReceived', { 'success': false });
        const w = (v = q.abortController) == null ? void 0 : v.signal;
        if (w) {
          p = w.aborted;
          if (!p) {
            console.warn(r);
          }
        }
        return {
          'error': r,
          'cancelled': p
        };
      }).then(r => {
        let u;
        k.done = true;
        (u = e.trigger) == null ? void 0 : u.call(e, 'responseReceived', { 'success': Boolean(r == null ? void 0 : r.ok) });
        const v = (r == null ? void 0 : r.ok) ? c.success : c.failure;
        return v == null ? void 0 : v.call(c.thisObj || e, r, q, c);
      });
    }
    const o = e.trigger('beforeSend', {
      'params': i.params,
      'requestType': c.type,
      'requestConfig': i,
      'config': c
    });
    if (Objects.isPromise(o)) {
      l = o.then(n);
    } else {
      l = n();
    }
    l.abort = () => {
      let p;
      if (!k.done) {
        (p = k.abort) == null ? void 0 : p.call(k);
      }
    };
    return l;
  }
};
const JsonEncoder_default = a => class b extends (a || Base) {
  static get $name() {
    return 'JsonEncoder';
  }
  static get defaultConfig() {
    return { 'encoder': {} };
  }
  encode(c) {
    let e;
    c = Object.assign({}, (e = this.encoder) == null ? void 0 : e.requestData, c);
    return StringHelper.safeJsonStringify(c);
  }
  decode(c) {
    return StringHelper.safeJsonParse(c);
  }
};
const ProjectCrudManager_default = a => class b extends (a || Base).mixin(AbstractCrudManagerMixin_default, AjaxTransport_default, JsonEncoder_default) {
  static get defaultConfig() {
    return { 'project': null };
  }
  startConfigure(c) {
    this.getConfig('project');
    super.startConfigure(c);
    this._changesToClear = new Map();
  }
  async doAutoLoad() {
    const {project: c} = this;
    if (c) {
      await c.commitAsync();
    }
    return super.doAutoLoad();
  }
  applyProjectResponse(c) {
    const e = this, {project: f} = e;
    e.applyingProjectResponse = true;
    if ('$input' in c) {
      c = c.$input;
    }
    const g = f.fieldMap.startDate, i = f.fieldMap.endDate, j = ObjectHelper.getPath(c, g.dataSource), k = ObjectHelper.getPath(c, i.dataSource);
    if (typeof j === 'string') {
      ObjectHelper.setPath(c, g.dataSource, g.convert(j));
    }
    if (typeof k === 'string') {
      ObjectHelper.setPath(c, i.dataSource, i.convert(k));
    }
    f.setByDataSource(c);
    e._changesToClear.set(e, c);
    e.applyingProjectResponse = false;
  }
  async loadCrudManagerData(c, e = {}) {
    const f = this, {project: g} = f;
    f.suspendChangeTracking();
    await super.loadCrudManagerData(...arguments);
    if (c == null ? void 0 : c.project) {
      if (g.delayEnteringReplica && g.hasDataInStores) {
        g.ion({
          'recordsUnlinked': () => {
            f.suspendChangeTracking();
            f.applyProjectResponse(c.project);
            f.resumeChangeTracking();
          },
          'once': true
        });
      } else {
        f.applyProjectResponse(c.project);
      }
    }
    f.resumeChangeTracking();
    await (g == null ? void 0 : g.commitAsync());
  }
  async sync() {
    const {project: c} = this;
    this.suspendAutoSync();
    if (c) {
      await c.commitAsync();
    }
    if (this.isDestroying) {
      return;
    }
    this.resumeAutoSync(false);
    return super.sync();
  }
  async applyResponse(c, e, f) {
    let g, i, j, k, l, n, o;
    const p = this;
    if (p.isDestroyed || ((g = p.project) == null ? void 0 : g.isDestroyed)) {
      return;
    }
    p.trigger('beforeApplyResponse');
    p.crudStores.forEach(r => r.store.suspendFilterOnUpdate());
    const q = (e == null ? void 0 : e.project) || p.supportShortSyncResponse && ((i = c == null ? void 0 : c.pack) == null ? void 0 : i.project);
    if (q) {
      const r = {};
      if (q.startDate) {
        r.startDate = q.startDate;
      }
      if (q.endDate) {
        r.endDate = q.endDate;
      }
      if (q.calendar) {
        r.calendar = (k = (j = p.project) == null ? void 0 : j.getCalendarById(q.calendar)) != null ? k : q.calendar;
        delete q.calendar;
      }
      p.applyProjectResponse(r);
    }
    await super.applyResponse(c, e, f);
    if (q) {
      const u = p._changesToClear.get(p);
      p.applyProjectResponse(q);
      p._changesToClear.set(p, ObjectHelper.assign({}, u, q));
    }
    p.crudStores.forEach(v => v.store.resumeFilterOnUpdate());
    if (p.project) {
      let v = c.type;
      if (p.trackResponseType) {
        v = e.type || v;
      }
      const w = 'propagating' + StringHelper.capitalize(v) + 'Changes';
      p.suspendAutoSync();
      p[w] = true;
      const z = p.project.isInitialCommit && !p.project.silenceInitialCommit;
      await p.project.commitAsync();
      p[w] = false;
      (l = p.resumeAutoSync) == null ? void 0 : l.call(p, z);
      (n = p.commitRespondedChanges) == null ? void 0 : n.call(p);
    }
    (o = p.trigger) == null ? void 0 : o.call(p, 'applyResponse');
  }
  applySyncResponse(...c) {
    let e;
    const f = this, g = (e = f.project) == null ? void 0 : e.stm.disabled;
    if (g === false && f.shouldIgnoreRemoteChangesInSTM) {
      f.project.stm.disable();
    }
    f.suspendAutoSync();
    super.applySyncResponse(...c);
    f.resumeAutoSync(false);
    if (g === false) {
      f.project.stm.enable();
    }
  }
  shouldClearRecordFieldChange(c, e, f) {
    const g = c.getValue(e);
    e = c.getFieldDefinition(e);
    return (e == null ? void 0 : e.isEqual) ? e.isEqual(g, f) : ObjectHelper.isEqual(g, f);
  }
  commitRespondedChanges() {
    this._changesToClear.forEach((c, e) => {
      Object.entries(c).forEach(([f, g]) => {
        if (!(e === this && this.project !== this) && this.shouldClearRecordFieldChange(e, f, g)) {
          delete e.meta.modified[f];
        }
      });
    });
    this._changesToClear.clear();
  }
  applyChangesToStore(c, e, f, ...g) {
    const i = super.applyChangesToStore(c, e, f, ...g);
    if (i.size && this.project) {
      for (const [j, k] of i) {
        const l = c.store.getById(j);
        l && this._changesToClear.set(l, k);
      }
    }
    return i;
  }
};
const lazyDateStores = [
  'events',
  'resourceTimeRanges',
  'assignments',
  'timeRanges'
];
const LazyLoadCrudManager_default = a => {
  let b;
  return b = class extends (a || Base) {
    updateLazyLoad(c, e) {
      if (!c && !e) {
        return;
      }
      const f = this, {
          internalLoad: g,
          crudStores: i
        } = f;
      i.forEach(({store: j}) => f.storeShouldLazyLoad(j) && f.setupLazyStore(j));
      if (c) {
        if (!g) {
          f.internalLoad = f.load;
          f.load = f.internalLazyLoad;
        }
      } else if (g) {
        f.load = g;
      }
    }
    afterAddStore(c) {
      c.forEach(({store: e}) => this.storeShouldLazyLoad(e) && this.setupLazyStore(e));
    }
    setupLazyStore(c) {
      let e;
      let {lazyLoad: f} = this;
      if (f) {
        const {isResourceStore: g} = c;
        if (f !== true) {
          const {
            chunkSize: i,
            bufferUnit: j,
            bufferAmount: k
          } = f;
          f = {};
          if (g && i) {
            f.chunkSize = i;
          } else if (!g) {
            if (j) {
              f.bufferUnit = j;
            }
            if (k) {
              f.bufferAmount = k;
            }
          }
        }
        c.lazyLoad = f;
        c.lazyLoad.$isCrudManager = true;
        c.syncDataOnLoad = false;
        if (g || c.isEventStore) {
          c.requestData = l => this.requestData(l, g);
        } else {
          c.requestData = null;
        }
      } else if ((e = c.lazyLoad) == null ? void 0 : e.$isCrudManager) {
        c.lazyLoad = false;
      }
    }
    internalLazyLoad() {
      return this.resourceStore.load();
    }
    loadCrudStore(c, e, f) {
      if (this.lazyLoad && this.storeShouldLazyLoad(c)) {
        return;
      }
      return super.loadCrudStore(c, e, f);
    }
    getLazyDateStores(c) {
      return this.crudStores.filter(e => lazyDateStores.includes(e.storeId) && (!c || !e.store.isEventStore));
    }
    storeShouldLazyLoad(c) {
      return c.isResourceStore || this.getLazyDateStores().some(e => e.store === c);
    }
    async requestData(c, e) {
      if (!c.startDate || !c.endDate) {
        return;
      }
      const f = this, g = f.getLazyDateStores(!e), i = e && f.resourceStore.isTree, j = f._lazyRequestPromises || (f._lazyRequestPromises = []), k = {
          'type': 'load',
          'params': c,
          'stores': f.crudStores.filter(r => !f.crudLoaded || (lazyDateStores.includes(r.storeId) || e && r.store.isResourceStore)).map(r => r.storeId)
        }, l = Promise.all(j).then(() => f.internalLoad(k)), n = {
          ...c,
          'promise': l
        };
      if (i) {
        n.startIndex = -1;
        n.count = 1;
      }
      g.forEach(({store: r}) => {
        r.lazyLoad.addToQueue(n);
        r.lazyLoad.triggerLazyLoadStart();
      });
      j.push(l);
      const {response: o} = await l, {
          rows: p,
          total: q
        } = e ? o.resources : o.events;
      if (i) {
        const {idField: r} = f.resourceStore.modelClass;
        n.resourceIds = p.map(u => u[r]);
      }
      g.forEach(({
        store: {lazyLoad: u},
        storeId: v
      }) => {
        let w;
        if (i) {
          delete u.loadQueue[-1];
          u.addToQueue(n);
        }
        u.addData((w = o[v]) == null ? void 0 : w.rows, n);
        u.triggerLazyLoadEnd();
      });
      j.splice(j.indexOf(l), 1);
      return {
        'data': p,
        'total': e ? q : void 0
      };
    }
  }, __publicField(b, '$name', 'LazyLoadCrudManager'), __publicField(b, 'configurable', { 'lazyLoad': false }), b;
};
const AbstractCrudManager = class extends Base.mixin(AbstractCrudManagerMixin_default) {
  get revision() {
    return this.crudRevision;
  }
  set revision(a) {
    this.crudRevision = a;
  }
  get json() {
    const a = this.crudStoresJSON;
    return StringHelper.safeJsonStringify(a);
  }
  set json(a) {
    if (typeof a === 'string') {
      a = StringHelper.safeJsonParse(a);
    }
    this.crudStoresJSON = a;
  }
  static get defaultConfig() {
    return { 'stores': null };
  }
  construct(a = {}) {
    if (a.stores) {
      a.crudStores = a.stores;
      delete a.stores;
    }
    super.construct(a);
  }
  toJSON() {
    return this.crudStoresJSON;
  }
  get inlineData() {
    return this.crudStoresJSON;
  }
  set inlineData(a) {
    this.crudStoresJSON = a;
  }
  set stores(a) {
    if (a !== this.crudStores) {
      this.crudStores = a;
    }
  }
  get stores() {
    return this.crudStores;
  }
  get isLoading() {
    return this.isCrudManagerLoading;
  }
  addStore(...a) {
    return this.addCrudStore(...a);
  }
  removeStore(...a) {
    return this.removeCrudStore(...a);
  }
  getStore(...a) {
    return this.getCrudStore(...a);
  }
  hasChanges(...a) {
    return this.crudStoreHasChanges(...a);
  }
  loadData(...a) {
    return this.loadCrudManagerData(...a);
  }
};
AbstractCrudManager._$name = 'AbstractCrudManager';
const ProjectModelCommon_default = a => {
  let b;
  return b = class extends (a || Model) {
    static get configurable() {
      return {
        'assignments': null,
        'dependencies': null,
        'resources': null,
        'timeRanges': null,
        'json': null,
        'shouldSyncDataOnLoad': null,
        'toJSONResultFormat': 'inlineData',
        'useRawData': null
      };
    }
    static get autoExposeFields() {
      return false;
    }
    attachStore(c) {
      super.attachStore(c);
      if (this.useRawData != null) {
        c.useRawData = this.useRawData;
      }
    }
    toJSON() {
      switch (this.toJSONResultFormat) {
      case 'inlineData':
        return this.inlineData;
      case 'model':
        return super.toJSON();
      }
    }
    get inlineData() {
      const c = this;
      let e;
      if (c.isAbstractCrudManagerMixin) {
        e = c.crudStoresJSON;
      } else {
        e = {
          'eventsData': c.eventStore.toJSON(),
          'resourcesData': c.resourceStore.toJSON(),
          'dependenciesData': c.dependencyStore.toJSON(),
          'timeRangesData': c.timeRangeStore.toJSON(),
          'resourceTimeRangesData': c.resourceTimeRangeStore.toJSON(),
          'assignmentsData': c.assignmentStore.toJSON()
        };
      }
      if (c.eventStore.usesSingleAssignment && e.assignmentsData) {
        delete e.assignmentsData;
      }
      e.project = super.toJSON();
      ObjectHelper.deletePath(e, 'project.id');
      ObjectHelper.deletePath(e, 'project.children');
      return e;
    }
    set inlineData(c) {
      this.loadInlineData(c);
    }
    get json() {
      const {toJSONResultFormat: c} = this;
      this.toJSONResultFormat = 'inlineData';
      const e = super.json;
      this.toJSONResultFormat = c;
      return e;
    }
    changeJson(c) {
      if (typeof c === 'string') {
        c = StringHelper.safeJsonParse(c);
      }
      return c;
    }
    updateJson(c) {
      c && this.loadInlineData(c);
    }
    get assignments() {
      return this.assignmentStore.allRecords;
    }
    updateAssignments(c) {
      this.assignmentStore.data = c;
    }
    get dependencies() {
      return this.dependencyStore.allRecords;
    }
    updateDependencies(c) {
      this.dependencyStore.data = c;
    }
    get resources() {
      return this.resourceStore.allRecords;
    }
    updateResources(c) {
      this.resourceStore.data = c;
    }
    get timeRanges() {
      return this.timeRangeStore.allRecords;
    }
    getTimeRanges(c, e) {
      const f = this.timeRangeStore, g = [];
      for (const i of f) {
        if (i.isRecurring) {
          g.push(...i.getOccurrencesForDateRange(c, e));
        } else if (i.startDate < e && c < i.endDate) {
          g.push(i);
        }
      }
      return g;
    }
    updateTimeRanges(c) {
      this.timeRangeStore.data = c;
    }
    getResourceTimeRanges(c, e) {
      const f = this.resourceTimeRangeStore, g = [];
      for (const i of f) {
        if (i.isRecurring) {
          g.push(...i.getOccurrencesForDateRange(c, e));
        } else if (i.startDate < e && (!i.endDate || c < i.endDate)) {
          g.push(i);
        }
      }
      return g;
    }
  }, __publicField(b, '$name', 'ProjectModelCommon'), b;
};
const ResourceTimeRangeModel = class extends TimeSpan.mixin(RecurringTimeSpan_default) {
  static domIdPrefix = 'resourcetimerange';
  static relations = {
    'resource': {
      'foreignKey': 'resourceId',
      'foreignStore': 'resourceStore',
      'relatedCollectionName': 'timeRanges',
      'nullFieldOnRemove': true
    }
  };
  static fields = [
    'resourceId',
    'timeRangeColor'
  ];
  static $name = 'ResourceTimeRangeModel';
  get domId() {
    return this.constructor.domIdPrefix + '-' + this.id;
  }
  get isResourceTimeRange() {
    return true;
  }
  get resources() {
    return this.resource ? [this.resource] : [];
  }
  get $linkedResources() {
    return this.resources;
  }
};
ResourceTimeRangeModel._$name = 'ResourceTimeRangeModel';
const ResourceTimeRangeStore = class extends AjaxStore.mixin(RecurringTimeSpansMixin_default) {
  static $lazyLoadPluginClass = DateStoreLazyLoadPlugin;
  static $name = 'ResourceTimeRangeStore';
  static get defaultConfig() {
    return {
      'loadPriority': 500,
      'syncPriority': 500,
      'resourceStore': null,
      'modelClass': ResourceTimeRangeModel,
      'id': 'resourceTimeRanges',
      'project': null
    };
  }
  set resourceStore(a) {
    this._resourceStore = a;
    if (!this.isConfiguring) {
      this.initRelations(true);
    }
  }
  get resourceStore() {
    return this._resourceStore;
  }
  getResourcesForEvent(a) {
    return [a.resource];
  }
  getRanges({
    resourceRecord: a,
    startDate: b,
    endDate: c
  }) {
    const e = a.timeRanges.flatMap(f => {
      if (f.isRecurring) {
        return f.getOccurrencesForDateRange(b, c);
      }
      if (f.intersectsRange(b, c)) {
        return f;
      }
      return [];
    });
    return e.sort((f, g) => f.startDate - g.startDate);
  }
};
ResourceTimeRangeStore._$name = 'ResourceTimeRangeStore';
const StoreTimeZonePlugin = class extends InstancePlugin {
  static $name = 'storeTimeZonePlugin';
  static get pluginConfig() {
    return {
      'before': ['processRecord'],
      'assign': [
        'beforeSyncRecord',
        'afterSyncRecord'
      ]
    };
  }
  get timeZone() {
    return this.client.project.timeZone;
  }
  processRecord(a, b) {
    if (b || this.client.isLoadingData || a.timeZone !== void 0) {
      this.convertRecordToTimeZone(a);
    }
  }
  convertRecordToTimeZone(a, b = this.timeZone) {
    let c;
    if (a.timeZone !== b) {
      a.$ignoreChange = true;
      if ((c = a.baselines) == null ? void 0 : c.count) {
        for (const e of a.baselines) {
          if (a.timeZone !== e.timeZone) {
            e.timeZone = a.timeZone;
          }
          e.convertToTimeZone(b);
        }
      }
      if (a.isRecurring) {
        a.removeOccurrences();
      }
      a.convertToTimeZone(b);
      a.$ignoreChange = false;
    }
  }
  beforeSyncRecord({record: a}) {
    if (a.timeZone != null) {
      a.$restoreTimeZone = a.timeZone;
      a.convertToTimeZone();
    }
  }
  afterSyncRecord({record: a}) {
    if (a.$restoreTimeZone) {
      a.convertToTimeZone(a.$restoreTimeZone);
      a.$restoreTimeZone = null;
    }
  }
};
StoreTimeZonePlugin._$name = 'StoreTimeZonePlugin';
const ProjectModelTimeZoneMixin_default = a => {
  let b;
  return b = class extends (a || Model) {
    get _storesWithDates() {
      return [
        this.taskStore,
        this.timeRangeStore,
        this.resourceTimeRangeStore
      ].filter(c => c);
    }
    plugStore(c) {
      if (!c.hasPlugin(StoreTimeZonePlugin)) {
        c.addPlugins(StoreTimeZonePlugin);
      }
    }
    unplugStore(c) {
      let e;
      (e = c.plugins.storeTimeZonePlugin) == null ? void 0 : e.destroy();
    }
    attachStore(c) {
      super.attachStore(c);
      if (c && this.data && this.timeZone != null && this._storesWithDates.includes(c)) {
        this.plugStore(c);
        this.convertStoresToTimeZone([c]);
      }
    }
    detachStore(c) {
      super.detachStore(c);
      if (c && !c.isDestroyed && this.timeZone != null) {
        this.convertStoresToTimeZone([c], null);
        this.unplugStore(c);
      }
    }
    relayStoreChange({
      source: c,
      action: e,
      records: f,
      replaced: g
    }) {
      const i = this;
      if (i.timeZone != null && i._storesWithDates.includes(c)) {
        if ([
            'add',
            'replace'
          ].includes(e)) {
          if (!(f == null ? void 0 : f.length) && (g == null ? void 0 : g.length)) {
            f = g;
          }
          if (f.length) {
            f.forEach(j => j.timeZone = i.timeZone);
          }
        }
      }
    }
    convertStoresToTimeZone(c, e = this.timeZone) {
      const {stm: f} = this, g = f == null ? void 0 : f.enabled;
      if (g) {
        f.disabled = true;
      }
      for (const i of c) {
        i == null ? void 0 : i.forEach(j => i.plugins.storeTimeZonePlugin.convertRecordToTimeZone(j, e));
      }
      if (g) {
        this.commitAsync().then(() => {
          f.disabled = false;
        });
      }
    }
    updateTimeZone(c, e) {
      const f = this, g = f._isConfiguringTimeZone || f.isConfiguring;
      f.trigger('beforeTimeZoneChange', {
        'timeZone': c,
        'oldTimeZone': e,
        'isConfiguring': g
      });
      f.calendarManagerStore.forEach(i => i.bumpVersion());
      f._storesWithDates.forEach(i => f.plugStore(i));
      f.convertStoresToTimeZone(f._storesWithDates);
      if (f.startDate) {
        const i = e != null ? TimeZoneHelper.fromTimeZone(f.startDate, e) : f.startDate;
        f.startDate = c != null ? TimeZoneHelper.toTimeZone(i, c) : i;
      }
      f.ignoreRecordChanges = true;
      f.commitAsync().then(() => {
        if (!f.isDestroyed) {
          f.trigger('timeZoneChange', {
            'timeZone': c,
            'oldTimeZone': e,
            'isConfiguring': g
          });
        }
        delete f._isConfiguringTimeZone;
      });
    }
  }, __publicField(b, '$name', 'ProjectModelTimeZoneMixin'), __publicField(b, 'configurable', {
    'timeZone': {
      '$config': 'lazy',
      'value': null
    }
  }), b;
};
const TimeRangeModel = class extends TimeSpan.mixin(RecurringTimeSpan_default) {
  static $name = 'TimeRangeModel';
  afterConstruct() {
    if (!this.endDate) {
      this.endDate = this.startDate;
    }
    super.afterConstruct();
  }
};
TimeRangeModel._$name = 'TimeRangeModel';
const TimeRangeStore = class extends AjaxStore.mixin(RecurringTimeSpansMixin_default) {
  static defaultConfig = {
    'loadPriority': 500,
    'syncPriority': 500,
    'modelClass': TimeRangeModel,
    'id': 'timeRanges'
  };
  static $lazyLoadPluginClass = DateStoreLazyLoadPlugin;
  static $name = 'TimeRangeStore';
};
TimeRangeStore._$name = 'TimeRangeStore';
const ProjectModelMixin_default = a => {
  let b;
  return b = class extends (a || Model).mixin(ProjectModelCommon_default, ProjectModelTimeZoneMixin_default) {
    static get $name() {
      return 'ProjectModelMixin';
    }
    static get defaultConfig() {
      return {
        'stm': {},
        'timeRangeModelClass': TimeRangeModel,
        'resourceTimeRangeModelClass': ResourceTimeRangeModel,
        'timeRangeStoreClass': TimeRangeStore,
        'resourceTimeRangeStoreClass': ResourceTimeRangeStore,
        'eventStore': {},
        'assignmentStore': {},
        'dependencyStore': {},
        'resourceStore': {},
        'timeRangesData': null,
        'resourceTimeRangesData': null
      };
    }
    construct(c = {}) {
      super.construct(...arguments);
      if (c.timeRangesData) {
        this.timeRangeStore.data = c.timeRangesData;
      }
      if (c.resourceTimeRangesData) {
        this.resourceTimeRangeStore.data = c.resourceTimeRangesData;
      }
    }
    afterConstruct() {
      super.afterConstruct();
      const c = this;
      !c.timeRangeStore.stm && c.stm.addStore(c.timeRangeStore);
      !c.resourceTimeRangeStore.stm && c.stm.addStore(c.resourceTimeRangeStore);
    }
    attachStore(c) {
      if (c) {
        c.ion({
          'name': c.$$name,
          'change': 'relayStoreChange',
          'thisObj': this
        });
      }
      super.attachStore(c);
    }
    detachStore(c) {
      if (c) {
        this.detachListeners(c.$$name);
        super.detachStore(c);
      }
    }
    relayStoreChange(c) {
      super.relayStoreChange(c);
      return this.trigger('change', {
        'store': c.source,
        ...c,
        'source': this
      });
    }
    updateTimeRangeStore(c, e) {
      this.detachStore(e);
      this.attachStore(c);
      if (e) {
        e.project = null;
      }
      if (c) {
        c.project = this;
      }
    }
    setTimeRangeStore(c) {
      this.timeRangeStore = c;
    }
    changeTimeRangeStore(c) {
      if (c && !c.isStore) {
        c = this.timeRangeStoreClass.new({ 'modelClass': this.timeRangeModelClass }, c);
      }
      return c;
    }
    updateResourceTimeRangeStore(c, e) {
      this.detachStore(e);
      this.attachStore(c);
      if (e) {
        e.project = null;
      }
      if (c) {
        c.project = this;
      }
    }
    changeResourceTimeRangeStore(c) {
      if (c && !c.isStore) {
        c = this.resourceTimeRangeStoreClass.new({ 'modelClass': this.resourceTimeRangeModelClass }, c);
      }
      return c;
    }
    setResourceTimeRangeStore(c) {
      this.resourceTimeRangeStore = c;
    }
    get events() {
      return this.eventStore.allRecords;
    }
    updateEvents(c) {
      this.eventStore.data = c;
    }
    get resourceTimeRanges() {
      return this.resourceTimeRangeStore.allRecords;
    }
    updateResourceTimeRanges(c) {
      this.resourceTimeRangeStore.data = c;
    }
    async loadInlineData(c) {
      this.isLoadingInlineData = true;
      if (c.resourceTimeRangesData) {
        this.resourceTimeRangeStore.data = c.resourceTimeRangesData;
      }
      if (c.timeRangesData) {
        this.timeRangeStore.data = c.timeRangesData;
      }
      return super.loadInlineData(c);
    }
    afterChange(c, e) {
      super.afterChange(...arguments);
      if (e.calendar) {
        this.trigger('calendarChange');
      }
    }
    doDestroy() {
      this.timeRangeStore.destroy();
      this.resourceTimeRangeStore.destroy();
      super.doDestroy();
    }
  }, __publicField(b, 'configurable', {
    'timeRangeStore': {
      'value': {},
      '$config': 'nullify'
    },
    'resourceTimeRangeStore': {
      'value': {},
      '$config': 'nullify'
    },
    'events': null,
    'resourceTimeRanges': null
  }), b;
};
const ProjectCurrentConfig_default = a => class b extends a {
  getCurrentConfig(c) {
    const e = this, f = super.getCurrentConfig(c);
    if (f) {
      for (const g of [
          'eventStore',
          'resourceStore',
          'assignmentStore',
          'dependencyStore',
          'timeRangeStore',
          'resourceTimeRangeStore'
        ]) {
        const i = e[g];
        if (i) {
          if (i.count) {
            f[i.id + 'Data'] = i.getInlineData(c);
          }
          const j = i.getCurrentConfig(c);
          if (j && Object.keys(j).length > 0) {
            if (Object.keys(j).length === 1 && j.data) {
              delete f[g];
            } else {
              f[g] = Object.assign(f[g] || {}, j);
            }
          } else if (f[g] && Object.keys(f[g]).length === 0) {
            delete f[g];
          }
        }
      }
      if (e.taskStore.isTaskStore) {
        delete f.eventModelClass;
        delete f.eventStoreClass;
        delete f.children;
      }
      return f;
    }
  }
};
const ModelPersistencyManager = class extends Base {
  set eventStore(a) {
    const b = this;
    b.eventStoreDetacher && b.eventStoreDetacher();
    b._eventStore = a;
    if (a && a.autoCommit) {
      b.eventStoreDetacher = a.ion({
        'beforecommit': b.onEventStoreBeforeSync,
        'thisObj': b,
        'detachable': true,
        'prio': 100
      });
    }
  }
  get eventStore() {
    return this._eventStore;
  }
  set resourceStore(a) {
    const b = this;
    b.resourceStoreDetacher && b.resourceStoreDetacher();
    b._resourceStore = a;
    if (a && a.autoCommit) {
      b.resourceStoreDetacher = a.ion({
        'beforecommit': b.onResourceStoreBeforeSync,
        'thisObj': b,
        'detachable': true,
        'prio': 100
      });
    }
  }
  get resourceStore() {
    return this._resourceStore;
  }
  set assignmentStore(a) {
    const b = this;
    b.assignmentStoreDetacher && b.assignmentStoreDetacher();
    b._assignmentStore = a;
    if (a && a.autoSync) {
      b.assignmentStoreDetacher = a.ion({
        'beforecommit': b.onAssignmentStoreBeforeSync,
        'thisObj': b,
        'detachable': true,
        'prio': 100
      });
    }
  }
  get assignmentStore() {
    return this._assignmentStore;
  }
  set dependencyStore(a) {
    const b = this;
    b.dependencyStoreDetacher && b.dependencyStoreDetacher();
    b._dependencyStore = a;
    if (a && a.autoSync) {
      b.dependencyStoreDetacher = a.ion({
        'beforecommit': b.onDependencyStoreBeforeSync,
        'thisObj': b,
        'detachable': true,
        'prio': 100
      });
    }
  }
  get dependencyStore() {
    return this._dependencyStore;
  }
  onEventStoreBeforeSync({changes: a}) {
    const b = this;
    b.removeNonPersistableRecordsToCreate(a);
    return b.shallContinueSync(a);
  }
  onResourceStoreBeforeSync({changes: a}) {
    const b = this;
    b.removeNonPersistableRecordsToCreate(a);
    return b.shallContinueSync(a);
  }
  onAssignmentStoreBeforeSync({changes: a}) {
    const b = this;
    b.removeNonPersistableRecordsToCreate(a);
    return b.shallContinueSync(a);
  }
  onDependencyStoreBeforeSync({changes: a}) {
    const b = this;
    b.removeNonPersistableRecordsToCreate(a);
    return b.shallContinueSync(a);
  }
  removeNonPersistableRecordsToCreate(a) {
    const b = a.added || [];
    let c, e;
    for (e = b.length - 1; e >= 0; --e) {
      c = b[e];
      if (!c.isPersistable) {
        b.splice(b.indexOf(c), 1);
      }
    }
    if (b.length === 0) {
      a.added.length = 0;
    }
  }
  shallContinueSync(a) {
    return Boolean(a.added && a.added.length > 0 || a.modified && a.modified.length > 0 || a.removed && a.removed.length > 0);
  }
};
ModelPersistencyManager._$name = 'ModelPersistencyManager';
const diffSecond = (a, b) => {
  return new Date(a.getFullYear(), a.getMonth(), a.getDate(), a.getHours(), a.getMinutes(), a.getSeconds() + b, a.getMilliseconds());
};
const later = function () {
  'use strict';
  const a = { 'version': '1.2.0' };
  if (!Array.prototype.indexOf) {
    Array.prototype.indexOf = function (b) {
      'use strict';
      if (this == null) {
        throw new TypeError();
      }
      const c = Object(this);
      const e = c.length >>> 0;
      if (e === 0) {
        return -1;
      }
      let f = 0;
      if (arguments.length > 1) {
        f = Number(arguments[1]);
        if (f != f) {
          f = 0;
        } else if (f != 0 && f != Infinity && f != -Infinity) {
          f = (f > 0 || -1) * Math.floor(Math.abs(f));
        }
      }
      if (f >= e) {
        return -1;
      }
      let g = f >= 0 ? f : Math.max(e - Math.abs(f), 0);
      for (; g < e; g++) {
        if (g in c && c[g] === b) {
          return g;
        }
      }
      return -1;
    };
  }
  if (!String.prototype.trim) {
    String.prototype.trim = function () {
      return this.replace(/^\s+|\s+$/g, '');
    };
  }
  a.array = {};
  a.array.sort = function (b, c) {
    b.sort(function (e, f) {
      return +e - +f;
    });
    if (c && b[0] === 0) {
      b.push(b.shift());
    }
  };
  a.array.next = function (b, c, e) {
    let f, g = e[0] !== 0, j = 0;
    for (let k = c.length - 1; k > -1; --k) {
      f = c[k];
      if (f === b) {
        return f;
      }
      if (f > b || f === 0 && g && e[1] > b) {
        j = k;
        continue;
      }
      break;
    }
    return c[j];
  };
  a.array.nextInvalid = function (b, c, e) {
    let f = e[0], g = e[1], j = c.length, k = c[j - 1] === 0 && f !== 0 ? g : 0, l = b, n = c.indexOf(b), o = l;
    while (l === (c[n] || k)) {
      l++;
      if (l > g) {
        l = f;
      }
      n++;
      if (n === j) {
        n = 0;
      }
      if (l === o) {
        return void 0;
      }
    }
    return l;
  };
  a.array.prev = function (b, c, e) {
    let f, g = c.length, j = e[0] !== 0, k = g - 1;
    for (let l = 0; l < g; l++) {
      f = c[l];
      if (f === b) {
        return f;
      }
      if (f < b || f === 0 && j && e[1] < b) {
        k = l;
        continue;
      }
      break;
    }
    return c[k];
  };
  a.array.prevInvalid = function (b, c, e) {
    let f = e[0], g = e[1], j = c.length, k = c[j - 1] === 0 && f !== 0 ? g : 0, l = b, n = c.indexOf(b), o = l;
    while (l === (c[n] || k)) {
      l--;
      if (l < f) {
        l = g;
      }
      n--;
      if (n === -1) {
        n = j - 1;
      }
      if (l === o) {
        return void 0;
      }
    }
    return l;
  };
  a.day = a.D = {
    'name': 'day',
    'range': 86400,
    'val': function (b) {
      return b.D || (b.D = a.date.getDate.call(b));
    },
    'isValid': function (b, c) {
      return a.D.val(b) === (c || a.D.extent(b)[1]);
    },
    'extent': function (b) {
      if (b.DExtent)
        return b.DExtent;
      let c = a.M.val(b), e = a.DAYS_IN_MONTH[c - 1];
      if (c === 2 && a.dy.extent(b)[1] === 366) {
        e = e + 1;
      }
      return b.DExtent = [
        1,
        e
      ];
    },
    'start': function (b) {
      return b.DStart || (b.DStart = a.date.next(a.Y.val(b), a.M.val(b), a.D.val(b)));
    },
    'end': function (b) {
      return b.DEnd || (b.DEnd = a.date.prev(a.Y.val(b), a.M.val(b), a.D.val(b)));
    },
    'next': function (b, c) {
      c = c > a.D.extent(b)[1] ? 1 : c;
      const e = a.date.nextRollover(b, c, a.D, a.M), f = a.D.extent(e)[1];
      c = c > f ? 1 : c || f;
      return a.date.next(a.Y.val(e), a.M.val(e), c);
    },
    'prev': function (b, c) {
      const e = a.date.prevRollover(b, c, a.D, a.M), f = a.D.extent(e)[1];
      return a.date.prev(a.Y.val(e), a.M.val(e), c > f ? f : c || f);
    }
  };
  a.dayOfWeekCount = a.dc = {
    'name': 'day of week count',
    'range': 604800,
    'val': function (b) {
      return b.dc || (b.dc = Math.floor((a.D.val(b) - 1) / 7) + 1);
    },
    'isValid': function (b, c) {
      return a.dc.val(b) === c || c === 0 && a.D.val(b) > a.D.extent(b)[1] - 7;
    },
    'extent': function (b) {
      return b.dcExtent || (b.dcExtent = [
        1,
        Math.ceil(a.D.extent(b)[1] / 7)
      ]);
    },
    'start': function (b) {
      return b.dcStart || (b.dcStart = a.date.next(a.Y.val(b), a.M.val(b), Math.max(1, (a.dc.val(b) - 1) * 7 + 1 || 1)));
    },
    'end': function (b) {
      return b.dcEnd || (b.dcEnd = a.date.prev(a.Y.val(b), a.M.val(b), Math.min(a.dc.val(b) * 7, a.D.extent(b)[1])));
    },
    'next': function (b, c) {
      c = c > a.dc.extent(b)[1] ? 1 : c;
      let e = a.date.nextRollover(b, c, a.dc, a.M), f = a.dc.extent(e)[1];
      c = c > f ? 1 : c;
      const g = a.date.next(a.Y.val(e), a.M.val(e), c === 0 ? a.D.extent(e)[1] - 6 : 1 + 7 * (c - 1));
      if (g.getTime() <= b.getTime()) {
        e = a.M.next(b, a.M.val(b) + 1);
        return a.date.next(a.Y.val(e), a.M.val(e), c === 0 ? a.D.extent(e)[1] - 6 : 1 + 7 * (c - 1));
      }
      return g;
    },
    'prev': function (b, c) {
      const e = a.date.prevRollover(b, c, a.dc, a.M), f = a.dc.extent(e)[1];
      c = c > f ? f : c || f;
      return a.dc.end(a.date.prev(a.Y.val(e), a.M.val(e), 1 + 7 * (c - 1)));
    }
  };
  a.dayOfWeek = a.dw = a.d = {
    'name': 'day of week',
    'range': 86400,
    'val': function (b) {
      return b.dw || (b.dw = a.date.getDay.call(b) + 1);
    },
    'isValid': function (b, c) {
      return a.dw.val(b) === (c || 7);
    },
    'extent': function () {
      return [
        1,
        7
      ];
    },
    'start': function (b) {
      return a.D.start(b);
    },
    'end': function (b) {
      return a.D.end(b);
    },
    'next': function (b, c) {
      c = c > 7 ? 1 : c || 7;
      return a.date.next(a.Y.val(b), a.M.val(b), a.D.val(b) + (c - a.dw.val(b)) + (c <= a.dw.val(b) ? 7 : 0));
    },
    'prev': function (b, c) {
      c = c > 7 ? 7 : c || 7;
      return a.date.prev(a.Y.val(b), a.M.val(b), a.D.val(b) + (c - a.dw.val(b)) + (c >= a.dw.val(b) ? -7 : 0));
    }
  };
  a.dayOfYear = a.dy = {
    'name': 'day of year',
    'range': 86400,
    'val': function (b) {
      return b.dy || (b.dy = Math.ceil(1 + (a.D.start(b).getTime() - a.Y.start(b).getTime()) / a.DAY));
    },
    'isValid': function (b, c) {
      return a.dy.val(b) === (c || a.dy.extent(b)[1]);
    },
    'extent': function (b) {
      const c = a.Y.val(b);
      return b.dyExtent || (b.dyExtent = [
        1,
        c % 4 ? 365 : 366
      ]);
    },
    'start': function (b) {
      return a.D.start(b);
    },
    'end': function (b) {
      return a.D.end(b);
    },
    'next': function (b, c) {
      c = c > a.dy.extent(b)[1] ? 1 : c;
      const e = a.date.nextRollover(b, c, a.dy, a.Y), f = a.dy.extent(e)[1];
      c = c > f ? 1 : c || f;
      return a.date.next(a.Y.val(e), a.M.val(e), c);
    },
    'prev': function (b, c) {
      const e = a.date.prevRollover(b, c, a.dy, a.Y), f = a.dy.extent(e)[1];
      c = c > f ? f : c || f;
      return a.date.prev(a.Y.val(e), a.M.val(e), c);
    }
  };
  a.hour = a.h = {
    'name': 'hour',
    'range': 3600,
    'val': function (b) {
      return b.h || (b.h = a.date.getHour.call(b));
    },
    'isValid': function (b, c) {
      return a.h.val(b) === c;
    },
    'extent': function () {
      return [
        0,
        23
      ];
    },
    'start': function (b) {
      return b.hStart || (b.hStart = a.date.next(a.Y.val(b), a.M.val(b), a.D.val(b), a.h.val(b)));
    },
    'end': function (b) {
      return b.hEnd || (b.hEnd = a.date.prev(a.Y.val(b), a.M.val(b), a.D.val(b), a.h.val(b)));
    },
    'next': function (b, c) {
      c = c > 23 ? 0 : c;
      let e = a.date.next(a.Y.val(b), a.M.val(b), a.D.val(b) + (c <= a.h.val(b) ? 1 : 0), c);
      if (!a.date.isUTC && e.getTime() <= b.getTime()) {
        e = a.date.next(a.Y.val(e), a.M.val(e), a.D.val(e), c + 1);
      }
      return e;
    },
    'prev': function (b, c) {
      c = c > 23 ? 23 : c;
      return a.date.prev(a.Y.val(b), a.M.val(b), a.D.val(b) + (c >= a.h.val(b) ? -1 : 0), c);
    }
  };
  a.minute = a.m = {
    'name': 'minute',
    'range': 60,
    'val': function (b) {
      return b.m || (b.m = a.date.getMin.call(b));
    },
    'isValid': function (b, c) {
      return a.m.val(b) === c;
    },
    'extent': function (b) {
      return [
        0,
        59
      ];
    },
    'start': function (b) {
      return b.mStart || (b.mStart = a.date.next(a.Y.val(b), a.M.val(b), a.D.val(b), a.h.val(b), a.m.val(b)));
    },
    'end': function (b) {
      return b.mEnd || (b.mEnd = a.date.prev(a.Y.val(b), a.M.val(b), a.D.val(b), a.h.val(b), a.m.val(b)));
    },
    'next': function (b, c) {
      let e = a.m.val(b), f = a.s.val(b), g = c > 59 ? 60 - e : c <= e ? 60 - e + c : c - e, i = new Date(b.getTime() + g * a.MIN - f * a.SEC);
      if (!a.date.isUTC && i.getTime() <= b.getTime()) {
        i = new Date(b.getTime() + (g + 120) * a.MIN - f * a.SEC);
      }
      return i;
    },
    'prev': function (b, c) {
      c = c > 59 ? 59 : c;
      return a.date.prev(a.Y.val(b), a.M.val(b), a.D.val(b), a.h.val(b) + (c >= a.m.val(b) ? -1 : 0), c);
    }
  };
  a.month = a.M = {
    'name': 'month',
    'range': 2629740,
    'val': function (b) {
      return b.M || (b.M = a.date.getMonth.call(b) + 1);
    },
    'isValid': function (b, c) {
      return a.M.val(b) === (c || 12);
    },
    'extent': function () {
      return [
        1,
        12
      ];
    },
    'start': function (b) {
      return b.MStart || (b.MStart = a.date.next(a.Y.val(b), a.M.val(b)));
    },
    'end': function (b) {
      return b.MEnd || (b.MEnd = a.date.prev(a.Y.val(b), a.M.val(b)));
    },
    'next': function (b, c) {
      c = c > 12 ? 1 : c || 12;
      return a.date.next(a.Y.val(b) + (c > a.M.val(b) ? 0 : 1), c);
    },
    'prev': function (b, c) {
      c = c > 12 ? 12 : c || 12;
      return a.date.prev(a.Y.val(b) - (c >= a.M.val(b) ? 1 : 0), c);
    }
  };
  a.second = a.s = {
    'name': 'second',
    'range': 1,
    'val': function (b) {
      return b.s || (b.s = a.date.getSec.call(b));
    },
    'isValid': function (b, c) {
      return a.s.val(b) === c;
    },
    'extent': function () {
      return [
        0,
        59
      ];
    },
    'start': function (b) {
      return b;
    },
    'end': function (b) {
      return b;
    },
    'next': function (b, c) {
      let e = a.s.val(b), f = c > 59 ? 60 - e : c <= e ? 60 - e + c : c - e, g = new Date(b.getTime() + f * a.SEC);
      if (!a.date.isUTC && g.getTime() <= b.getTime()) {
        g = new Date(b.getTime() + (f + 7200) * a.SEC);
      }
      return g;
    },
    'prev': function (b, c, e) {
      c = c > 59 ? 59 : c;
      return a.date.prev(a.Y.val(b), a.M.val(b), a.D.val(b), a.h.val(b), a.m.val(b) + (c >= a.s.val(b) ? -1 : 0), c);
    }
  };
  a.time = a.t = {
    'name': 'time',
    'range': 1,
    'val': function (b) {
      return b.t || (b.t = a.h.val(b) * 3600 + a.m.val(b) * 60 + a.s.val(b));
    },
    'isValid': function (b, c) {
      return a.t.val(b) === c;
    },
    'extent': function () {
      return [
        0,
        86399
      ];
    },
    'start': function (b) {
      return b;
    },
    'end': function (b) {
      return b;
    },
    'next': function (b, c) {
      c = c > 86399 ? 0 : c;
      let e = a.date.next(a.Y.val(b), a.M.val(b), a.D.val(b) + (c <= a.t.val(b) ? 1 : 0), 0, 0, c);
      if (!a.date.isUTC && e.getTime() < b.getTime()) {
        e = a.date.next(a.Y.val(e), a.M.val(e), a.D.val(e), a.h.val(e), a.m.val(e), c + 7200);
      }
      return e;
    },
    'prev': function (b, c) {
      c = c > 86399 ? 86399 : c;
      return a.date.next(a.Y.val(b), a.M.val(b), a.D.val(b) + (c >= a.t.val(b) ? -1 : 0), 0, 0, c);
    }
  };
  a.weekOfMonth = a.wm = {
    'name': 'week of month',
    'range': 604800,
    'val': function (b) {
      return b.wm || (b.wm = (a.D.val(b) + (a.dw.val(a.M.start(b)) - 1) + (7 - a.dw.val(b))) / 7);
    },
    'isValid': function (b, c) {
      return a.wm.val(b) === (c || a.wm.extent(b)[1]);
    },
    'extent': function (b) {
      return b.wmExtent || (b.wmExtent = [
        1,
        (a.D.extent(b)[1] + (a.dw.val(a.M.start(b)) - 1) + (7 - a.dw.val(a.M.end(b)))) / 7
      ]);
    },
    'start': function (b) {
      return b.wmStart || (b.wmStart = a.date.next(a.Y.val(b), a.M.val(b), Math.max(a.D.val(b) - a.dw.val(b) + 1, 1)));
    },
    'end': function (b) {
      return b.wmEnd || (b.wmEnd = a.date.prev(a.Y.val(b), a.M.val(b), Math.min(a.D.val(b) + (7 - a.dw.val(b)), a.D.extent(b)[1])));
    },
    'next': function (b, c) {
      c = c > a.wm.extent(b)[1] ? 1 : c;
      const e = a.date.nextRollover(b, c, a.wm, a.M), f = a.wm.extent(e)[1];
      c = c > f ? 1 : c || f;
      return a.date.next(a.Y.val(e), a.M.val(e), Math.max(1, (c - 1) * 7 - (a.dw.val(e) - 2)));
    },
    'prev': function (b, c) {
      const e = a.date.prevRollover(b, c, a.wm, a.M), f = a.wm.extent(e)[1];
      c = c > f ? f : c || f;
      return a.wm.end(a.date.next(a.Y.val(e), a.M.val(e), Math.max(1, (c - 1) * 7 - (a.dw.val(e) - 2))));
    }
  };
  a.weekOfYear = a.wy = {
    'name': 'week of year (ISO)',
    'range': 604800,
    'val': function (b) {
      if (b.wy)
        return b.wy;
      const c = a.dw.next(a.wy.start(b), 5), e = a.dw.next(a.Y.prev(c, a.Y.val(c) - 1), 5);
      return b.wy = 1 + Math.ceil((c.getTime() - e.getTime()) / a.WEEK);
    },
    'isValid': function (b, c) {
      return a.wy.val(b) === (c || a.wy.extent(b)[1]);
    },
    'extent': function (b) {
      if (b.wyExtent)
        return b.wyExtent;
      const c = a.dw.next(a.wy.start(b), 5), e = a.dw.val(a.Y.start(c)), f = a.dw.val(a.Y.end(c));
      return b.wyExtent = [
        1,
        e === 5 || f === 5 ? 53 : 52
      ];
    },
    'start': function (b) {
      return b.wyStart || (b.wyStart = a.date.next(a.Y.val(b), a.M.val(b), a.D.val(b) - (a.dw.val(b) > 1 ? a.dw.val(b) - 2 : 6)));
    },
    'end': function (b) {
      return b.wyEnd || (b.wyEnd = a.date.prev(a.Y.val(b), a.M.val(b), a.D.val(b) + (a.dw.val(b) > 1 ? 8 - a.dw.val(b) : 0)));
    },
    'next': function (b, c) {
      c = c > a.wy.extent(b)[1] ? 1 : c;
      let e = a.dw.next(a.wy.start(b), 5), f = a.date.nextRollover(e, c, a.wy, a.Y);
      if (a.wy.val(f) !== 1) {
        f = a.dw.next(f, 2);
      }
      const g = a.wy.extent(f)[1], i = a.wy.start(f);
      c = c > g ? 1 : c || g;
      return a.date.next(a.Y.val(i), a.M.val(i), a.D.val(i) + 7 * (c - 1));
    },
    'prev': function (b, c) {
      let e = a.dw.next(a.wy.start(b), 5), f = a.date.prevRollover(e, c, a.wy, a.Y);
      if (a.wy.val(f) !== 1) {
        f = a.dw.next(f, 2);
      }
      const g = a.wy.extent(f)[1], i = a.wy.end(f);
      c = c > g ? g : c || g;
      return a.wy.end(a.date.next(a.Y.val(i), a.M.val(i), a.D.val(i) + 7 * (c - 1)));
    }
  };
  a.year = a.Y = {
    'name': 'year',
    'range': 31556900,
    'val': function (b) {
      return b.Y || (b.Y = a.date.getYear.call(b));
    },
    'isValid': function (b, c) {
      return a.Y.val(b) === c;
    },
    'extent': function () {
      return [
        1970,
        2099
      ];
    },
    'start': function (b) {
      return b.YStart || (b.YStart = a.date.next(a.Y.val(b)));
    },
    'end': function (b) {
      return b.YEnd || (b.YEnd = a.date.prev(a.Y.val(b)));
    },
    'next': function (b, c) {
      return c > a.Y.val(b) && c <= a.Y.extent()[1] ? a.date.next(c) : a.NEVER;
    },
    'prev': function (b, c) {
      return c < a.Y.val(b) && c >= a.Y.extent()[0] ? a.date.prev(c) : a.NEVER;
    }
  };
  a.fullDate = a.fd = {
    'name': 'full date',
    'range': 1,
    'val': function (b) {
      return b.fd || (b.fd = b.getTime());
    },
    'isValid': function (b, c) {
      return a.fd.val(b) === c;
    },
    'extent': function () {
      return [
        0,
        32503680000000
      ];
    },
    'start': function (b) {
      return b;
    },
    'end': function (b) {
      return b;
    },
    'next': function (b, c) {
      return a.fd.val(b) < c ? new Date(c) : a.NEVER;
    },
    'prev': function (b, c) {
      return a.fd.val(b) > c ? new Date(c) : a.NEVER;
    }
  };
  a.modifier = {};
  a.modifier.after = a.modifier.a = function (b, c) {
    const e = c[0];
    return {
      'name': 'after ' + b.name,
      'range': (b.extent(new Date())[1] - e) * b.range,
      'val': b.val,
      'isValid': function (f, g) {
        return this.val(f) >= e;
      },
      'extent': b.extent,
      'start': b.start,
      'end': b.end,
      'next': function (f, g) {
        if (g != e)
          g = b.extent(f)[0];
        return b.next(f, g);
      },
      'prev': function (f, g) {
        g = g === e ? b.extent(f)[1] : e - 1;
        return b.prev(f, g);
      }
    };
  };
  a.modifier.before = a.modifier.b = function (b, c) {
    const e = c[c.length - 1];
    return {
      'name': 'before ' + b.name,
      'range': b.range * (e - 1),
      'val': b.val,
      'isValid': function (f, g) {
        return this.val(f) < e;
      },
      'extent': b.extent,
      'start': b.start,
      'end': b.end,
      'next': function (f, g) {
        g = g === e ? b.extent(f)[0] : e;
        return b.next(f, g);
      },
      'prev': function (f, g) {
        g = g === e ? e - 1 : b.extent(f)[1];
        return b.prev(f, g);
      }
    };
  };
  a.compile = function (b) {
    let c = [], e = 0, f;
    for (const g in b) {
      const i = g.split('_'), j = i[0], k = i[1], l = b[g], n = k ? a.modifier[k](a[j], l) : a[j];
      c.push({
        'constraint': n,
        'vals': l
      });
      e++;
    }
    c.sort(function (p, q) {
      const r = p.constraint.range, u = q.constraint.range;
      return u < r ? -1 : u > r ? 1 : 0;
    });
    f = c[e - 1].constraint;
    function o(p) {
      return p === 'next' ? function (q, r) {
        return q.getTime() > r.getTime();
      } : function (q, r) {
        return r.getTime() > q.getTime();
      };
    }
    return {
      'start': function (p, q) {
        let r = q, u = a.array[p], v = 1000, w;
        while (v-- && !w && r) {
          w = true;
          for (let z = 0; z < e; z++) {
            const A = c[z].constraint, B = A.val(r), C = A.extent(r), E = u(B, c[z].vals, C);
            if (!A.isValid(r, E)) {
              r = A[p](r, E);
              w = false;
              break;
            }
          }
        }
        if (r !== a.NEVER) {
          r = p === 'next' ? f.start(r) : f.end(r);
        }
        return r;
      },
      'end': function (p, q) {
        let r, u = a.array[p + 'Invalid'], v = o(p);
        for (let w = e - 1; w >= 0; w--) {
          var z = c[w].constraint, A = z.val(q), B = z.extent(q), C = u(A, c[w].vals, B), E;
          if (C !== void 0) {
            E = z[p](q, C);
            if (E && (!r || v(r, E))) {
              r = E;
            }
          }
        }
        return r;
      },
      'tick': function (p, q) {
        return new Date(p === 'next' ? f.end(q).getTime() + a.SEC : f.start(q).getTime() - a.SEC);
      },
      'tickSafe': function (p, q) {
        return p === 'next' ? diffSecond(f.end(q), 1) : diffSecond(f.start(q), -1);
      },
      'tickStart': function (p) {
        return f.start(p);
      }
    };
  };
  a.schedule = function (b) {
    if (!b)
      throw new Error('Missing schedule definition.');
    if (!b.schedules)
      throw new Error('Definition must include at least one schedule.');
    const c = [], e = b.schedules.length, f = [], g = b.exceptions ? b.exceptions.length : 0;
    for (let k = 0; k < e; k++) {
      c.push(a.compile(b.schedules[k]));
    }
    for (let l = 0; l < g; l++) {
      f.push(a.compile(b.exceptions[l]));
    }
    function n(F, G, H, I, J) {
      let K = C(F), L = G, N = 1000000, O = [], P = [], Q, R, S = [], T = F === 'next', U, V = T ? 0 : 1, W = T ? 1 : 0;
      H = H ? new Date(H) : new Date();
      if (!H || !H.getTime())
        throw new Error('Invalid start date.');
      p(F, c, O, H);
      r(F, f, P, H);
      while (N-- && L && (Q = E(O, K))) {
        if (I && K(Q, I)) {
          break;
        }
        if (g) {
          u(F, f, P, Q);
          if (R = z(F, P, Q)) {
            q(F, c, O, R);
            continue;
          }
        }
        if (J) {
          const X = A(P, K);
          R = B(F, c, O, Q, X);
          const Y = T ? [
            new Date(Math.max(H, Q)),
            R ? new Date(I ? Math.min(R, I) : R) : void 0
          ] : [
            R ? new Date(I ? Math.max(I, R.getTime() + a.SEC) : R.getTime() + a.SEC) : void 0,
            new Date(Math.min(H, Q.getTime() + a.SEC))
          ];
          if (U && Y[V].getTime() === U[W].getTime()) {
            U[W] = Y[W];
            L++;
          } else {
            U = Y;
            S.push(U);
          }
          if (!R)
            break;
          q(F, c, O, R);
        } else {
          S.push(T ? new Date(Math.max(H, Q)) : w(c, O, Q, I));
          v(F, c, O, Q);
        }
        L--;
      }
      for (let Z = 0, a0 = S.length; Z < a0; Z++) {
        const a1 = S[Z];
        S[Z] = Object.prototype.toString.call(a1) === '[object Array]' ? [
          o(a1[0]),
          o(a1[1])
        ] : o(a1);
      }
      return S.length === 0 ? a.NEVER : G === 1 ? S[0] : S;
    }
    function o(F) {
      if (F instanceof Date && !isNaN(F.valueOf())) {
        return new Date(F);
      }
      return void 0;
    }
    function p(F, G, H, I) {
      for (let J = 0, K = G.length; J < K; J++) {
        H[J] = G[J].start(F, I);
      }
    }
    function q(F, G, H, I) {
      const J = C(F);
      for (let K = 0, L = G.length; K < L; K++) {
        if (H[K] && !J(H[K], I)) {
          H[K] = G[K].start(F, I);
        }
      }
    }
    function r(F, G, H, I) {
      const J = C(F);
      for (let K = 0, L = G.length; K < L; K++) {
        const N = G[K].start(F, I);
        if (!N) {
          H[K] = a.NEVER;
        } else {
          H[K] = [
            N,
            G[K].end(F, N)
          ];
        }
      }
    }
    function u(F, G, H, I) {
      const J = C(F);
      for (let K = 0, L = G.length; K < L; K++) {
        if (H[K] && !J(H[K][0], I)) {
          const N = G[K].start(F, I);
          if (!N) {
            H[K] = a.NEVER;
          } else {
            H[K] = [
              N,
              G[K].end(F, N)
            ];
          }
        }
      }
    }
    function v(F, G, H, I) {
      for (let J = 0, K = G.length; J < K; J++) {
        if (H[J] && H[J].getTime() === I.getTime()) {
          const L = G[J].start(F, G[J].tick(F, I));
          if (L !== a.NEVER && L.getTime() === H[J].getTime()) {
            H[J] = G[J].start(F, G[J].tickSafe(F, I));
          } else {
            H[J] = L;
          }
        }
      }
    }
    function w(F, G, H, I) {
      let J;
      for (let K = 0, L = G.length; K < L; K++) {
        if (G[K] && G[K].getTime() === H.getTime()) {
          const N = F[K].tickStart(H);
          if (I && N < I) {
            return I;
          }
          if (!J || N > J) {
            J = N;
          }
        }
      }
      return J;
    }
    function z(F, G, H) {
      let I = C(F), J;
      for (let K = 0, L = G.length; K < L; K++) {
        const N = G[K];
        if (N && !I(N[0], H) && (!N[1] || I(N[1], H))) {
          if (!J || I(N[1], J)) {
            J = N[1];
          }
        }
      }
      return J;
    }
    function A(F, G) {
      let H;
      for (let I = 0, J = F.length; I < J; I++) {
        if (F[I] && (!H || G(H, F[I][0]))) {
          H = F[I][0];
        }
      }
      return H;
    }
    function B(F, G, H, I, J) {
      let K = C(F), L;
      for (let N = 0, O = G.length; N < O; N++) {
        const P = H[N];
        if (P && P.getTime() === I.getTime()) {
          const Q = G[N].end(F, P);
          if (J && (!Q || K(Q, J))) {
            return J;
          }
          if (!L || K(Q, L)) {
            L = Q;
          }
        }
      }
      return L;
    }
    function C(F) {
      return F === 'next' ? function (G, H) {
        return !H || G.getTime() > H.getTime();
      } : function (G, H) {
        return !G || H.getTime() > G.getTime();
      };
    }
    function E(F, G) {
      let H = F[0];
      for (let I = 1, J = F.length; I < J; I++) {
        if (F[I] && G(H, F[I])) {
          H = F[I];
        }
      }
      return H;
    }
    return {
      'isValid': function (F) {
        return n('next', 1, F, F) !== a.NEVER;
      },
      'next': function (F, G, H) {
        return n('next', F || 1, G, H);
      },
      'prev': function (F, G, H) {
        return n('prev', F || 1, G, H);
      },
      'nextRange': function (F, G, H) {
        return n('next', F || 1, G, H, true);
      },
      'prevRange': function (F, G, H) {
        return n('prev', F || 1, G, H, true);
      }
    };
  };
  a.setTimeout = function (b, c) {
    let e = a.schedule(c), f;
    if (b) {
      g();
    }
    function g() {
      const i = Date.now(), j = e.next(2, i);
      if (!j[0]) {
        f = void 0;
        return;
      }
      let k = j[0].getTime() - i;
      if (k < 1000) {
        k = j[1] ? j[1].getTime() - i : 1000;
      }
      if (k < 2147483647) {
        f = setTimeout(b, k);
      } else {
        f = setTimeout(g, 2147483647);
      }
    }
    return {
      'isDone': function () {
        return !f;
      },
      'clear': function () {
        clearTimeout(f);
      }
    };
  };
  a.setInterval = function (b, c) {
    if (!b) {
      return;
    }
    let e = a.setTimeout(g, c), f = e.isDone();
    function g() {
      if (!f) {
        b();
        e = a.setTimeout(g, c);
      }
    }
    return {
      'isDone': function () {
        return e.isDone();
      },
      'clear': function () {
        f = true;
        e.clear();
      }
    };
  };
  a.date = {};
  a.date.timezone = function (b) {
    a.date.build = b ? function (f, g, i, j, k, l) {
      return new Date(f, g, i, j, k, l);
    } : function (f, g, i, j, k, l) {
      return new Date(Date.UTC(f, g, i, j, k, l));
    };
    const c = b ? 'get' : 'getUTC', e = Date.prototype;
    a.date.getYear = e[c + 'FullYear'];
    a.date.getMonth = e[c + 'Month'];
    a.date.getDate = e[c + 'Date'];
    a.date.getDay = e[c + 'Day'];
    a.date.getHour = e[c + 'Hours'];
    a.date.getMin = e[c + 'Minutes'];
    a.date.getSec = e[c + 'Seconds'];
    a.date.isUTC = !b;
  };
  a.date.UTC = function () {
    a.date.timezone(false);
  };
  a.date.localTime = function () {
    a.date.timezone(true);
  };
  a.date.UTC();
  a.SEC = 1000;
  a.MIN = a.SEC * 60;
  a.HOUR = a.MIN * 60;
  a.DAY = a.HOUR * 24;
  a.WEEK = a.DAY * 7;
  a.DAYS_IN_MONTH = [
    31,
    28,
    31,
    30,
    31,
    30,
    31,
    31,
    30,
    31,
    30,
    31
  ];
  a.NEVER = 0;
  a.date.next = function (b, c, e, f, g, i) {
    return a.date.build(b, c !== void 0 ? c - 1 : 0, e !== void 0 ? e : 1, f || 0, g || 0, i || 0);
  };
  a.date.nextRollover = function (b, c, e, f) {
    const g = e.val(b), i = e.extent(b)[1];
    return (c || i) <= g || c > i ? new Date(f.end(b).getTime() + a.SEC) : f.start(b);
  };
  a.date.prev = function (b, c, e, f, g, i) {
    const j = arguments.length;
    c = j < 2 ? 11 : c - 1;
    e = j < 3 ? a.D.extent(a.date.next(b, c + 1))[1] : e;
    f = j < 4 ? 23 : f;
    g = j < 5 ? 59 : g;
    i = j < 6 ? 59 : i;
    return a.date.build(b, c, e, f, g, i);
  };
  a.date.prevRollover = function (b, c, e, f) {
    const g = e.val(b);
    return c >= g || !c ? f.start(f.prev(b, f.val(b) - 1)) : f.start(b);
  };
  a.parse = {};
  a.parse.cron = function (b, c) {
    const f = {
      'JAN': 1,
      'FEB': 2,
      'MAR': 3,
      'APR': 4,
      'MAY': 5,
      'JUN': 6,
      'JUL': 7,
      'AUG': 8,
      'SEP': 9,
      'OCT': 10,
      'NOV': 11,
      'DEC': 12,
      'SUN': 1,
      'MON': 2,
      'TUE': 3,
      'WED': 4,
      'THU': 5,
      'FRI': 6,
      'SAT': 7
    };
    const g = {
      '* * * * * *': '0/1 * * * * *',
      '@YEARLY': '0 0 1 1 *',
      '@ANNUALLY': '0 0 1 1 *',
      '@MONTHLY': '0 0 1 * *',
      '@WEEKLY': '0 0 * * 0',
      '@DAILY': '0 0 * * *',
      '@HOURLY': '0 * * * *'
    };
    const i = {
      's': [
        0,
        0,
        59
      ],
      'm': [
        1,
        0,
        59
      ],
      'h': [
        2,
        0,
        23
      ],
      'D': [
        3,
        1,
        31
      ],
      'M': [
        4,
        1,
        12
      ],
      'Y': [
        6,
        1970,
        2099
      ],
      'd': [
        5,
        1,
        7,
        1
      ]
    };
    function j(A, B, C) {
      return isNaN(A) ? f[A] || null : Math.min(+A + (B || 0), C || 9999);
    }
    function k(A) {
      let B = {}, C;
      for (C in A) {
        if (C !== 'dc' && C !== 'd') {
          B[C] = A[C].slice(0);
        }
      }
      return B;
    }
    function l(A, B, C, E, F) {
      let G = C;
      if (!A[B]) {
        A[B] = [];
      }
      while (G <= E) {
        if (A[B].indexOf(G) < 0) {
          A[B].push(G);
        }
        G += F || 1;
      }
      A[B].sort(function (H, I) {
        return H - I;
      });
    }
    function n(A, B, C, E) {
      if (B.d && !B.dc || B.dc && B.dc.indexOf(E) < 0) {
        A.push(k(B));
        B = A[A.length - 1];
      }
      l(B, 'd', C, C);
      l(B, 'dc', E, E);
    }
    function o(A, B, C) {
      const E = {}, F = {};
      if (C === 1) {
        l(B, 'D', 1, 3);
        l(B, 'd', f.MON, f.FRI);
        l(E, 'D', 2, 2);
        l(E, 'd', f.TUE, f.FRI);
        l(F, 'D', 3, 3);
        l(F, 'd', f.TUE, f.FRI);
      } else {
        l(B, 'D', C - 1, C + 1);
        l(B, 'd', f.MON, f.FRI);
        l(E, 'D', C - 1, C - 1);
        l(E, 'd', f.MON, f.THU);
        l(F, 'D', C + 1, C + 1);
        l(F, 'd', f.TUE, f.FRI);
      }
      A.exceptions.push(E);
      A.exceptions.push(F);
    }
    function p(A, B, C, E, F, G) {
      const H = A.split('/'), I = +H[1], J = H[0];
      if (J !== '*' && J !== '0') {
        const K = J.split('-');
        E = j(K[0], G, F);
        F = j(K[1], G, F) || F;
      }
      l(B, C, E, F, I);
    }
    function q(A, B, C, E, F, G) {
      let H, I, J = B.schedules, K = J[J.length - 1];
      if (A === 'L') {
        A = E - 1;
      }
      if ((H = j(A, G, F)) !== null) {
        l(K, C, H, H);
      } else if ((H = j(A.replace('W', ''), G, F)) !== null) {
        o(B, K, H);
      } else if ((H = j(A.replace('L', ''), G, F)) !== null) {
        n(J, K, H, E - 1);
      } else if ((I = A.split('#')).length === 2) {
        H = j(I[0], G, F);
        n(J, K, H, j(I[1]));
      } else {
        p(A, K, C, E, F, G);
      }
    }
    function r(A) {
      return A.indexOf('#') > -1 || A.indexOf('L') > 0;
    }
    function u(A, B) {
      return r(A) && !r(B) ? 1 : A - B;
    }
    function v(A) {
      let B = {
          'schedules': [{}],
          'exceptions': []
        }, C = A.replace(/(\s)+/g, ' ').split(' '), E, F, G, H;
      for (E in i) {
        F = i[E];
        G = C[F[0]];
        if (G && G !== '*' && G !== '?') {
          H = G.split(',').sort(u);
          var I, J = H.length;
          for (I = 0; I < J; I++) {
            q(H[I], B, E, F[1], F[2], F[3]);
          }
        }
      }
      return B;
    }
    function w(A) {
      const B = A.toUpperCase();
      return g[B] || B;
    }
    const z = w(b);
    return v(c ? z : '0 ' + z);
  };
  a.parse.recur = function () {
    let b = [], c = [], e, f = b, g, j, k, l, n, o, p, q;
    function r(u, v, w) {
      u = l ? u + '_' + l : u;
      if (!e) {
        f.push({});
        e = f[0];
      }
      if (!e[u]) {
        e[u] = [];
      }
      g = e[u];
      if (k) {
        j = [];
        for (p = v; p <= w; p += k) {
          j.push(p);
        }
        q = {
          'n': u,
          'x': k,
          'c': g.length,
          'm': w
        };
      }
      j = n ? [v] : o ? [w] : j;
      const z = j.length;
      for (p = 0; p < z; p += 1) {
        const A = j[p];
        if (g.indexOf(A) < 0) {
          g.push(A);
        }
      }
      j = k = l = n = o = 0;
    }
    return {
      'schedules': b,
      'exceptions': c,
      'on': function () {
        j = arguments[0] instanceof Array ? arguments[0] : arguments;
        return this;
      },
      'every': function (u) {
        k = u || 1;
        return this;
      },
      'after': function (u) {
        l = 'a';
        j = [u];
        return this;
      },
      'before': function (u) {
        l = 'b';
        j = [u];
        return this;
      },
      'first': function () {
        n = 1;
        return this;
      },
      'last': function () {
        o = 1;
        return this;
      },
      'time': function () {
        for (let u = 0, v = j.length; u < v; u++) {
          const w = j[u].split(':');
          if (w.length < 3)
            w.push(0);
          j[u] = +w[0] * 3600 + +w[1] * 60 + +w[2];
        }
        r('t');
        return this;
      },
      'second': function () {
        r('s', 0, 59);
        return this;
      },
      'minute': function () {
        r('m', 0, 59);
        return this;
      },
      'hour': function () {
        r('h', 0, 23);
        return this;
      },
      'dayOfMonth': function () {
        r('D', 1, o ? 0 : 31);
        return this;
      },
      'dayOfWeek': function () {
        r('d', 1, 7);
        return this;
      },
      'onWeekend': function () {
        j = [
          1,
          7
        ];
        return this.dayOfWeek();
      },
      'onWeekday': function () {
        j = [
          2,
          3,
          4,
          5,
          6
        ];
        return this.dayOfWeek();
      },
      'dayOfWeekCount': function () {
        r('dc', 1, o ? 0 : 5);
        return this;
      },
      'dayOfYear': function () {
        r('dy', 1, o ? 0 : 366);
        return this;
      },
      'weekOfMonth': function () {
        r('wm', 1, o ? 0 : 5);
        return this;
      },
      'weekOfYear': function () {
        r('wy', 1, o ? 0 : 53);
        return this;
      },
      'month': function () {
        r('M', 1, 12);
        return this;
      },
      'year': function () {
        r('Y', 1970, 2450);
        return this;
      },
      'fullDate': function () {
        for (let u = 0, v = j.length; u < v; u++) {
          j[u] = j[u].getTime();
        }
        r('fd');
        return this;
      },
      'customModifier': function (u, v) {
        const w = a.modifier[u];
        if (!w)
          throw new Error('Custom modifier ' + u + ' not recognized!');
        l = u;
        j = arguments[1] instanceof Array ? arguments[1] : [arguments[1]];
        return this;
      },
      'customPeriod': function (u) {
        const v = a[u];
        if (!v)
          throw new Error('Custom time period ' + u + ' not recognized!');
        r(u, v.extent(new Date())[0], v.extent(new Date())[1]);
        return this;
      },
      'startingOn': function (u) {
        return this.between(u, q.m);
      },
      'between': function (u, v) {
        e[q.n] = e[q.n].splice(0, q.c);
        k = q.x;
        r(q.n, u, v);
        return this;
      },
      'and': function () {
        e = f[f.push({}) - 1];
        return this;
      },
      'except': function () {
        f = c;
        e = null;
        return this;
      }
    };
  };
  a.parse.text = function (b) {
    let c = a.parse.recur, e = 0, f = '', g;
    const i = {
      'eof': /^$/,
      'fullDate': /^(\d\d\d\d-\d\d-\d\d(t\d\d(:\d\d(:\d\d)?)?)?)\b/,
      'rank': /^((\d\d\d\d)|([2-5]?1(st)?|[2-5]?2(nd)?|[2-5]?3(rd)?|(0|[1-5]?[4-9]|[1-5]0|1[1-3])(th)?))\b/,
      'time': /^((([0]?[1-9]|1[0-2]):[0-5]\d(\s)?(am|pm))|(([0]?\d|1\d|2[0-3]):[0-5]\d))\b/,
      'dayName': /^((sun|mon|tue(s)?|wed(nes)?|thu(r(s)?)?|fri|sat(ur)?)(day)?)\b/,
      'monthName': /^(jan(uary)?|feb(ruary)?|ma((r(ch)?)?|y)|apr(il)?|ju(ly|ne)|aug(ust)?|oct(ober)?|(sept|nov|dec)(ember)?)\b/,
      'yearIndex': /^(\d\d\d\d)\b/,
      'every': /^every\b/,
      'after': /^after\b/,
      'before': /^before\b/,
      'second': /^(s|sec(ond)?(s)?)\b/,
      'minute': /^(m|min(ute)?(s)?)\b/,
      'hour': /^(h|hour(s)?)\b/,
      'day': /^(day(s)?( of the month)?)\b/,
      'dayInstance': /^day instance\b/,
      'dayOfWeek': /^day(s)? of the week\b/,
      'dayOfYear': /^day(s)? of the year\b/,
      'weekOfYear': /^week(s)?( of the year)?\b/,
      'weekOfMonth': /^week(s)? of the month\b/,
      'weekday': /^weekday\b/,
      'weekend': /^weekend\b/,
      'month': /^month(s)?\b/,
      'year': /^year(s)?\b/,
      'between': /^between (the)?\b/,
      'start': /^(start(ing)? (at|on( the)?)?)\b/,
      'at': /^(at|@)\b/,
      'and': /^(,|and\b)/,
      'except': /^(except\b)/,
      'also': /(also)\b/,
      'first': /^(first)\b/,
      'last': /^last\b/,
      'in': /^in\b/,
      'of': /^of\b/,
      'onthe': /^on the\b/,
      'on': /^on\b/,
      'through': /(-|^(to|through)\b)/
    };
    const j = {
      'jan': 1,
      'feb': 2,
      'mar': 3,
      'apr': 4,
      'may': 5,
      'jun': 6,
      'jul': 7,
      'aug': 8,
      'sep': 9,
      'oct': 10,
      'nov': 11,
      'dec': 12,
      'sun': 1,
      'mon': 2,
      'tue': 3,
      'wed': 4,
      'thu': 5,
      'fri': 6,
      'sat': 7,
      '1st': 1,
      'fir': 1,
      '2nd': 2,
      'sec': 2,
      '3rd': 3,
      'thi': 3,
      '4th': 4,
      'for': 4
    };
    function k(C, E, F, G) {
      return {
        'startPos': C,
        'endPos': E,
        'text': F,
        'type': G
      };
    }
    function l(C) {
      let E = C instanceof Array ? C : [C], F = /\s+/, G, H, I, J, K, L;
      E.push(F);
      K = e;
      while (!G || G.type === F) {
        L = -1;
        H = f.substring(K);
        G = k(K, K, f.split(F)[0]);
        var N, O = E.length;
        for (N = 0; N < O; N++) {
          J = E[N];
          I = J.exec(H);
          if (I && I.index === 0 && I[0].length > L) {
            L = I[0].length;
            G = k(K, K + L, H.substring(0, L), J);
          }
        }
        if (G.type === F) {
          K = G.endPos;
        }
      }
      return G;
    }
    function n(C) {
      const E = l(C);
      e = E.endPos;
      return E;
    }
    function o(C) {
      const E = +A(C), F = w(i.through) ? +A(C) : E, G = [];
      for (let H = E; H <= F; H++) {
        G.push(H);
      }
      return G;
    }
    function p(C) {
      const E = o(C);
      while (w(i.and)) {
        E.push.apply(E, o(C));
      }
      if (C === i.dayName) {
        E.sort((F, G) => F - G);
      }
      return E;
    }
    function q(C) {
      let E, F, G, H;
      if (w(i.weekend)) {
        C.on(j.sun, j.sat).dayOfWeek();
      } else if (w(i.weekday)) {
        C.on(j.mon, j.tue, j.wed, j.thu, j.fri).dayOfWeek();
      } else {
        E = A(i.rank);
        C.every(E);
        F = v(C);
        if (w(i.start)) {
          E = A(i.rank);
          C.startingOn(E);
          z(F.type);
        } else if (w(i.between)) {
          G = A(i.rank);
          if (w(i.and)) {
            H = A(i.rank);
            C.between(G, H);
          }
        }
      }
    }
    function r(C) {
      if (w(i.first)) {
        C.first();
      } else if (w(i.last)) {
        C.last();
      } else {
        C.on(p(i.rank));
      }
      v(C);
    }
    function u(C) {
      e = 0;
      f = C;
      g = -1;
      const E = c();
      while (e < f.length && g < 0) {
        const F = z([
          i.every,
          i.after,
          i.before,
          i.onthe,
          i.on,
          i.of,
          i.in,
          i.at,
          i.and,
          i.except,
          i.also
        ]);
        switch (F.type) {
        case i.every:
          q(E);
          break;
        case i.after:
          if (l(i.time).type !== void 0) {
            E.after(A(i.time));
            E.time();
          } else if (l(i.fullDate).type !== void 0) {
            E.after(A(i.fullDate));
            E.fullDate();
          } else {
            E.after(A(i.rank));
            v(E);
          }
          break;
        case i.before:
          if (l(i.time).type !== void 0) {
            E.before(A(i.time));
            E.time();
          } else if (l(i.fullDate).type !== void 0) {
            E.before(A(i.fullDate));
            E.fullDate();
          } else {
            E.before(A(i.rank));
            v(E);
          }
          break;
        case i.onthe:
          r(E);
          break;
        case i.on:
          E.on(p(i.dayName)).dayOfWeek();
          break;
        case i.of:
          E.on(p(i.monthName)).month();
          break;
        case i.in:
          E.on(p(i.yearIndex)).year();
          break;
        case i.at:
          E.on(A(i.time)).time();
          while (w(i.and)) {
            E.on(A(i.time)).time();
          }
          break;
        case i.and:
          break;
        case i.also:
          E.and();
          break;
        case i.except:
          E.except();
          break;
        default:
          g = e;
        }
      }
      return {
        'schedules': E.schedules,
        'exceptions': E.exceptions,
        'error': g
      };
    }
    function v(C) {
      const E = z([
        i.second,
        i.minute,
        i.hour,
        i.dayOfYear,
        i.dayOfWeek,
        i.dayInstance,
        i.day,
        i.month,
        i.year,
        i.weekOfMonth,
        i.weekOfYear
      ]);
      switch (E.type) {
      case i.second:
        C.second();
        break;
      case i.minute:
        C.minute();
        break;
      case i.hour:
        C.hour();
        break;
      case i.dayOfYear:
        C.dayOfYear();
        break;
      case i.dayOfWeek:
        C.dayOfWeek();
        break;
      case i.dayInstance:
        C.dayOfWeekCount();
        break;
      case i.day:
        C.dayOfMonth();
        break;
      case i.weekOfMonth:
        C.weekOfMonth();
        break;
      case i.weekOfYear:
        C.weekOfYear();
        break;
      case i.month:
        C.month();
        break;
      case i.year:
        C.year();
        break;
      default:
        g = e;
      }
      return E;
    }
    function w(C) {
      const E = l(C).type === C;
      if (E) {
        n(C);
      }
      return E;
    }
    function z(C) {
      const E = n(C);
      if (E.type) {
        E.text = B(E.text, C);
      } else {
        g = e;
      }
      return E;
    }
    function A(C) {
      return z(C).text;
    }
    function B(C, E) {
      let F = C;
      switch (E) {
      case i.time:
        var G = C.split(/(:|am|pm)/), H = G[3] === 'pm' && G[0] < 12 ? parseInt(G[0], 10) + 12 : G[0], I = G[2].trim();
        F = (H.length === 1 ? '0' : '') + H + ':' + I;
        break;
      case i.rank:
        F = parseInt(/^\d+/.exec(C)[0], 10);
        break;
      case i.monthName:
      case i.dayName:
        F = j[C.substring(0, 3)];
        break;
      case i.fullDate:
        F = new Date(C.toUpperCase());
        break;
      }
      return F;
    }
    return u(b.toLowerCase());
  };
  return a;
}();
later.date.localTime();
const later_default = later;
let TimeUnit;
(function (a) {
  a.Millisecond = 'millisecond';
  a.Second = 'second';
  a.Minute = 'minute';
  a.Hour = 'hour';
  a.Day = 'day';
  a.Week = 'week';
  a.Month = 'month';
  a.Quarter = 'quarter';
  a.Year = 'year';
}(TimeUnit || (TimeUnit = {})));
let ProjectConstraintResolution;
(function (a) {
  a.Honor = 'honor';
  a.Ignore = 'ignore';
  a.Conflict = 'conflict';
}(ProjectConstraintResolution || (ProjectConstraintResolution = {})));
let ConstraintType;
(function (a) {
  a.MustStartOn = 'muststarton';
  a.MustFinishOn = 'mustfinishon';
  a.StartNoEarlierThan = 'startnoearlierthan';
  a.StartNoLaterThan = 'startnolaterthan';
  a.FinishNoEarlierThan = 'finishnoearlierthan';
  a.FinishNoLaterThan = 'finishnolaterthan';
  a.AsSoonAsPossible = 'assoonaspossible';
  a.AsLateAsPossible = 'aslateaspossible';
}(ConstraintType || (ConstraintType = {})));
let SchedulingMode;
(function (a) {
  a.Normal = 'Normal';
  a.FixedDuration = 'FixedDuration';
  a.FixedEffort = 'FixedEffort';
  a.FixedUnits = 'FixedUnits';
}(SchedulingMode || (SchedulingMode = {})));
let DependencyValidationResult;
(function (a) {
  a[a.NoError = 0] = 'NoError';
  a[a.CyclicDependency = 1] = 'CyclicDependency';
  a[a.DuplicatingDependency = 2] = 'DuplicatingDependency';
}(DependencyValidationResult || (DependencyValidationResult = {})));
let DependencyType;
(function (a) {
  a[a.StartToStart = 0] = 'StartToStart';
  a[a.StartToEnd = 1] = 'StartToEnd';
  a[a.EndToStart = 2] = 'EndToStart';
  a[a.EndToEnd = 3] = 'EndToEnd';
}(DependencyType || (DependencyType = {})));
let DependenciesCalendar;
(function (a) {
  a.Project = 'Project';
  a.FromEvent = 'FromEvent';
  a.ToEvent = 'ToEvent';
}(DependenciesCalendar || (DependenciesCalendar = {})));
let StartedTaskScheduling;
(function (a) {
  a.Auto = 'Auto';
  a.Manual = 'Manual';
}(StartedTaskScheduling || (StartedTaskScheduling = {})));
let ProjectType;
(function (a) {
  a[a.SchedulerBasic = 1] = 'SchedulerBasic';
  a[a.SchedulerPro = 2] = 'SchedulerPro';
  a[a.Gantt = 3] = 'Gantt';
}(ProjectType || (ProjectType = {})));
let Direction;
(function (a) {
  a.Forward = 'Forward';
  a.Backward = 'Backward';
  a.None = 'None';
}(Direction || (Direction = {})));
const isEqualEffectiveDirection = (c, e) => {
  if (c && !e || !c && e)
    return false;
  if (!c && !e)
    return true;
  return c.direction === e.direction && (c.kind === 'own' && e.kind === 'own' || c.kind === 'enforced' && e.kind === 'enforced' && c.enforcedBy === e.enforcedBy || c.kind === 'inherited' && e.kind === 'inherited' && c.inheritedFrom === e.inheritedFrom);
};
let ConstraintIntervalSide;
(function (a) {
  a.Start = 'Start';
  a.End = 'End';
}(ConstraintIntervalSide || (ConstraintIntervalSide = {})));
let CalendarIntervalType;
(function (a) {
  a.Week = 'Week';
  a.Exception = 'Exception';
}(CalendarIntervalType || (CalendarIntervalType = {})));
function convertAvailabilityValue(a) {
  if (a) {
    if (typeof a == 'string') {
      a = a.split(',');
      a.map(b => {
        const [c, e] = b.split('-');
        return {
          'startDate': c,
          'endDate': e
        };
      });
    }
  } else {
    a = null;
  }
  return a;
}
const CalendarIntervalMixin = class extends Mixin([AbstractPartOfProjectModelMixin], a => {
  class b extends a {
    static get fields() {
      return [
        'name',
        {
          'name': 'startDate',
          'type': 'date'
        },
        {
          'name': 'endDate',
          'type': 'date'
        },
        'recurrentStartDate',
        'recurrentEndDate',
        'cls',
        'iconCls',
        {
          'name': 'isWorking',
          'type': 'boolean',
          'defaultValue': false
        },
        {
          'name': 'priority',
          'type': 'number'
        },
        { 'name': 'type' },
        {
          'name': 'availability',
          'convert': convertAvailabilityValue
        },
        { 'name': 'compositeCode' }
      ];
    }
    getCalendar() {
      return this.stores[0].calendar;
    }
    get compositeIntervalCode() {
      let c, e;
      return this.compositeCode || (((c = this.startDate) == null ? void 0 : c.getTime()) || '') + '-' + (((e = this.endDate) == null ? void 0 : e.getTime()) || '') + '-' + (this.type || '');
    }
    resetPriority() {
      this.prioritySortValue = null;
      this.getCalendar().getDepth();
    }
    getPrioritySortValue() {
      if (this.prioritySortValue != null)
        return this.prioritySortValue;
      let c = 10000;
      let e = this.priority;
      if (e == null) {
        switch (this.type) {
        case CalendarIntervalType.Week:
          c += this.getCalendar().getDepth() * 100;
          e = this.isOverride() ? 30 : 20;
          break;
        case CalendarIntervalType.Exception:
          c += this.getCalendar().getDepth() * 10000;
          e = 40;
          break;
        default:
          c += this.getCalendar().getDepth() * 100;
          if (this.isRecurrent()) {
            e = 20;
            if (this.isOverride()) {
              e += 10;
            }
          } else {
            e = 40;
          }
        }
        e += this.parentIndex;
      }
      return this.prioritySortValue = c + e;
    }
    isRecurrent() {
      return Boolean(this.getStartDateSchedule() || this.type === CalendarIntervalType.Exception && this.getAvailabilityStartSchedule());
    }
    isStatic() {
      return Boolean(this.startDate && this.endDate && !this.isRecurrent());
    }
    isOverride() {
      return Boolean(this.startDate || this.endDate);
    }
    parseDateSchedule(c) {
      let f = c;
      if (c && c !== Object(c)) {
        try {
          f = JSON.parse(c);
        } catch (g) {
          f = later_default.parse.text(c.trim());
          if (f.error >= 0) {
            throw new Error('Parsing the \'later\' schedule has failed: "' + c + '". Please consult the \'later\' documentation for recognizable formats: https://bunkat.github.io/later/parsers.html');
          }
        }
      }
      return f;
    }
    getStartDateSchedule() {
      let c;
      if (!this.recurrentStartDate)
        return null;
      if (this.startDateSchedule)
        return this.startDateSchedule;
      const e = this.parseDateSchedule(this.recurrentStartDate);
      if ((c = e == null ? void 0 : e.schedules) == null ? void 0 : c[0]) {
        const f = e.schedules[0];
        if (!this.startDate && f.fd_a) {
          this.startDate = new Date(f.fd_a[0]);
          delete f.fd_a;
        }
        if (!this.endDate && f.fd_b) {
          this.endDate = new Date(f.fd_b[0]);
          delete f.fd_b;
        }
      }
      return this.startDateSchedule = later_default.schedule(e);
    }
    getEndDateSchedule() {
      let c, e, f, g, i, j, k, l, n;
      if (this.endDateSchedule)
        return this.endDateSchedule;
      if (!this.recurrentEndDate || this.recurrentEndDate === 'EOD')
        return null;
      const o = this.parseDateSchedule(this.recurrentEndDate);
      if ((c = o == null ? void 0 : o.schedules) == null ? void 0 : c[0]) {
        const p = o.schedules[0];
        if (((e = this.startDate) == null ? void 0 : e.getTime()) && ((f = p.fd_a) == null ? void 0 : f[0]) && ((g = this.startDate) == null ? void 0 : g.getTime()) === ((i = p.fd_a) == null ? void 0 : i[0])) {
          delete p.fd_a;
        }
        if (((j = this.endDate) == null ? void 0 : j.getTime()) && ((k = p.fd_b) == null ? void 0 : k[0]) && ((l = this.endDate) == null ? void 0 : l.getTime()) === ((n = p.fd_b) == null ? void 0 : n[0])) {
          delete p.fd_b;
        }
      }
      return this.endDateSchedule = later_default.schedule(o);
    }
    getAvailabilityStartSchedule() {
      let c;
      if ((c = this.availability) == null ? void 0 : c.length) {
        if (this.availabilityStartSchedule)
          return this.availabilityStartSchedule;
        const e = 'at ' + this.availability.map(g => g.startDate).sort().join(',');
        const f = this.parseDateSchedule(e);
        return this.availabilityStartSchedule = later_default.schedule(f);
      }
    }
    getAvailabilityEndSchedule() {
      let c;
      if ((c = this.availability) == null ? void 0 : c.length) {
        if (this.availabilityEndSchedule)
          return this.availabilityEndSchedule;
        const e = 'at ' + this.availability.map(g => g.endDate).sort().join(',');
        const f = this.parseDateSchedule(e);
        return this.availabilityEndSchedule = later_default.schedule(f);
      }
    }
    afterChange(c, e, f, g, i) {
      super.afterChange(c, e, f, g, i);
      if (e == null ? void 0 : e.recurrentStartDate) {
        this.startDateSchedule = void 0;
      }
      if (e == null ? void 0 : e.recurrentEndDate) {
        this.endDateSchedule = void 0;
      }
      if (e == null ? void 0 : e.availability) {
        this.availabilityStartSchedule = void 0;
        this.availabilityEndSchedule = void 0;
      }
    }
  }
  return b;
}) {
};
const CalendarIntervalStore = class extends Mixin([AbstractPartOfProjectStoreMixin], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.disableHasLoadedDataToCommitFlag = true;
    }
    static get defaultConfig() {
      return { 'modelClass': CalendarIntervalMixin };
    }
    acceptChanges() {
      if (!this.isSyncingDataOnLoad) {
        return super.acceptChanges(...arguments);
      }
    }
  }
  return c;
}) {
};
const MIN_DATE2 = new Date(-8640000000000000);
const MAX_DATE2 = new Date(8640000000000000);
const isDateFinite = a => {
  if (!a)
    return false;
  const b = a.getTime();
  return b !== MIN_DATE2.getTime() && b !== MAX_DATE2.getTime();
};
let EdgeInclusion;
(function (a) {
  a[a.Left = 0] = 'Left';
  a[a.Right = 1] = 'Right';
}(EdgeInclusion || (EdgeInclusion = {})));
let CalendarIteratorResult;
(function (a) {
  a[a.FullRangeIterated = 0] = 'FullRangeIterated';
  a[a.StoppedByIterator = 1] = 'StoppedByIterator';
  a[a.MaxCacheExtendCyclesReached = 2] = 'MaxCacheExtendCyclesReached';
  a[a.MaxRangeReached = 3] = 'MaxRangeReached';
}(CalendarIteratorResult || (CalendarIteratorResult = {})));
const CalendarCache = class {
  constructor(a) {
    this.cacheFilledStartDate = MAX_DATE2;
    this.cacheFilledEndDate = MIN_DATE2;
    this.intervalsCachingChunkDuration = 30;
    this.intervalsCachingChunkUnit = TimeUnit.Day;
    this.maxCacheExtendCycles = 1000;
    this.maxRange = 5 * 365 * 24 * 60 * 60 * 1000;
    a && Object.assign(this, a);
  }
  includeWrappingRangeFrom(a, b, c) {
    a.ensureCacheFilledForInterval(b, c);
    this.intervalCache.includeWrappingRange(a.intervalCache, b, c);
  }
  ensureCacheFilledForInterval(a, b) {
    const c = this.cacheFilledStartDate.getTime();
    const e = this.cacheFilledEndDate.getTime();
    if (c !== MAX_DATE2.getTime()) {
      const f = a.getTime();
      const g = b.getTime();
      if (c <= f && g <= e)
        return;
      if (g <= c) {
        b = new Date(c - 1);
      } else if (f >= e) {
        a = new Date(e);
      } else if (c <= f && f <= e) {
        a = new Date(e + 1);
      } else if (c <= g && g <= e) {
        b = new Date(c - 1);
      } else {
        this.ensureCacheFilledForInterval(a, new Date(c - 1));
        this.ensureCacheFilledForInterval(new Date(e + 1), b);
        return;
      }
    }
    if (c === MAX_DATE2.getTime() || a.getTime() < e) {
      this.cacheFilledStartDate = a;
    }
    if (e === MIN_DATE2.getTime() || e < b.getTime()) {
      this.cacheFilledEndDate = b;
    }
    this.fillCache(a, b);
  }
  fillCache(a, b) {
    throw new Error('Abstract method');
  }
  clear() {
    this.cacheFilledStartDate = MAX_DATE2;
    this.cacheFilledEndDate = MIN_DATE2;
    this.intervalCache.clear();
  }
  forEachAvailabilityInterval(a, b, c) {
    let e;
    c = c || this;
    const f = a.startDate;
    const g = a.endDate;
    const i = f && f.getTime();
    const j = g && g.getTime();
    const k = (e = a.maxRange) != null ? e : this.maxRange;
    const l = a.isForward !== false;
    if (l ? !f : !g) {
      throw new Error('At least `startDate` or `endDate` is required, depending from the `isForward` option');
    }
    const n = this.intervalCache;
    let o = l ? f : g;
    let p = l ? f : g;
    const q = p.getTime();
    for (let r = 1; r < this.maxCacheExtendCycles; r++) {
      if (l) {
        this.ensureCacheFilledForInterval(o, g || DateHelper.add(o, this.intervalsCachingChunkDuration, this.intervalsCachingChunkUnit));
      } else {
        this.ensureCacheFilledForInterval(f || DateHelper.add(o, -this.intervalsCachingChunkDuration, this.intervalsCachingChunkUnit), o);
      }
      let u = n.getIntervalOf(p, l ? EdgeInclusion.Left : EdgeInclusion.Right);
      while (u) {
        const v = u.startDate;
        const w = u.endDate;
        if (l && j && v.getTime() >= j || !l && i && w.getTime() <= i) {
          return CalendarIteratorResult.FullRangeIterated;
        }
        if (l && v.getTime() - q >= k || !l && q - w.getTime() >= k) {
          return CalendarIteratorResult.MaxRangeReached;
        }
        if (l && v.getTime() >= this.cacheFilledEndDate.getTime() || !l && w.getTime() <= this.cacheFilledStartDate.getTime()) {
          break;
        }
        p = l ? w : v;
        const z = i && v.getTime() < i ? f : v;
        const A = j && w.getTime() > j ? g : w;
        if (b.call(c, z, A, u.cacheInterval) === false) {
          return CalendarIteratorResult.StoppedByIterator;
        }
        u = l ? n.getNextInterval(u) : n.getPrevInterval(u);
      }
      if (l && p.getTime() === MAX_DATE2.getTime() || !l && p.getTime() === MIN_DATE2.getTime()) {
        return CalendarIteratorResult.FullRangeIterated;
      }
      o = l ? this.cacheFilledEndDate : this.cacheFilledStartDate;
    }
    return CalendarIteratorResult.MaxCacheExtendCyclesReached;
  }
};
const stripDuplicates = a => Array.from(new Set(a));
const IntervalSorterFn = (a, b) => {
  const c = a.getPrioritySortValue();
  const e = b.getPrioritySortValue();
  return e - c;
};
const CalendarCacheInterval = class _CalendarCacheInterval {
  constructor(a) {
    this.intervals = [];
    a && Object.assign(this, a);
    this.intervals = this.normalizeIntervals(this.intervals);
    if (!this.calendar)
      throw new Error('Required attribute `calendar` is missing');
  }
  includeInterval(a) {
    if (this.intervals.indexOf(a) == -1) {
      const b = this.intervals.slice();
      b.push(a);
      return new _CalendarCacheInterval({
        'intervals': b,
        'calendar': this.calendar
      });
    } else
      return this;
  }
  combineWith(a) {
    return new _CalendarCacheInterval({
      'intervals': this.intervals.concat(a.intervals),
      'calendar': this.calendar
    });
  }
  getIsWorking() {
    if (this.isWorking != null)
      return this.isWorking;
    return this.isWorking = this.intervals[0].isWorking;
  }
  normalizeIntervals(a) {
    const b = stripDuplicates(a);
    b.sort(IntervalSorterFn);
    return b;
  }
};
const binarySearch = (a, b, c = (g, i) => g - i) => {
  let e = 0;
  let f = b.length;
  while (e < f) {
    const g = (e + f) / 2 | 0;
    const i = c(a, b[g]);
    if (i === 0)
      return {
        'found': true,
        'index': g
      };
    else if (i < 0)
      f = g;
    else
      e = g + 1;
  }
  return {
    'found': false,
    'index': f
  };
};
let IndexPosition;
(function (a) {
  a[a.Exact = 0] = 'Exact';
  a[a.Next = 1] = 'Next';
}(IndexPosition || (IndexPosition = {})));
const SortedMap = class {
  constructor(a) {
    this.keys = [];
    this.values = [];
    this.comparator = a || ((c, e) => c - e);
  }
  set(a, b) {
    const c = binarySearch(a, this.keys, this.comparator);
    if (c.found) {
      this.values[c.index] = b;
    } else {
      this.keys.splice(c.index, 0, a);
      this.values.splice(c.index, 0, b);
    }
    return c.index;
  }
  insertAt(a, b, c) {
    this.keys.splice(a, 0, b);
    this.values.splice(a, 0, c);
  }
  setValueAt(a, b) {
    this.values[a] = b;
  }
  get(a) {
    const b = binarySearch(a, this.keys, this.comparator);
    return b.found ? this.values[b.index] : void 0;
  }
  getEntryAt(a) {
    return a < this.keys.length ? {
      'key': this.keys[a],
      'value': this.values[a]
    } : void 0;
  }
  getKeyAt(a) {
    return this.keys[a];
  }
  getValueAt(a) {
    return this.values[a];
  }
  delete(a) {
    const b = binarySearch(a, this.keys, this.comparator);
    if (b.found)
      this.deleteAt(b.index);
  }
  size() {
    return this.keys.length;
  }
  deleteAt(a) {
    this.keys.splice(a, 1);
    this.values.splice(a, 1);
  }
  indexOfKey(a) {
    const b = binarySearch(a, this.keys, this.comparator);
    return {
      'found': b.found ? IndexPosition.Exact : IndexPosition.Next,
      'index': b.index
    };
  }
  map(a) {
    const b = this.keys;
    const c = this.values;
    const e = [];
    for (let f = 0; f < b.length; f++)
      e.push(a(c[f], b[f], f));
    return e;
  }
  getAllEntries() {
    return this.map((a, b) => {
      return {
        'value': a,
        'key': b
      };
    });
  }
  clear() {
    this.keys.length = 0;
    this.values.length = 0;
  }
};
const IntervalCache = class {
  constructor(a) {
    this.points = new SortedMap((c, e) => c.getTime() - e.getTime());
    this.leftInfinityKey = MIN_DATE2;
    this.rightInfinityKey = MAX_DATE2;
    Object.assign(this, a);
    if (this.emptyInterval === void 0 || !this.combineIntervalsFn)
      throw new Error('All of `emptyPoint`, `combineIntervalsFn` are required');
    this.points.set(this.leftInfinityKey, this.emptyInterval);
  }
  size() {
    return this.points.size();
  }
  indexOf(a) {
    return this.points.indexOfKey(a);
  }
  getDateAt(a) {
    return this.points.getKeyAt(a);
  }
  getPointAt(a) {
    return this.points.getValueAt(a);
  }
  getIntervalOf(a, b = EdgeInclusion.Left) {
    const {
      found: c,
      index: e
    } = this.indexOf(a);
    let f;
    if (b === EdgeInclusion.Left) {
      f = c === IndexPosition.Exact ? e : e - 1;
    } else {
      f = e - 1;
    }
    return this.getIntervalWithStartDateIndex(f);
  }
  getPrevInterval(a) {
    if (a.startDateIndex === 0)
      return null;
    return this.getIntervalWithStartDateIndex(a.startDateIndex - 1);
  }
  getNextInterval(a) {
    if (a.startDateIndex >= this.size() - 1)
      return null;
    return this.getIntervalWithStartDateIndex(a.startDateIndex + 1);
  }
  getIntervalWithStartDateIndex(a) {
    return {
      'startDateIndex': a,
      'startDate': this.getDateAt(a),
      'endDate': a + 1 < this.size() ? this.getDateAt(a + 1) : this.rightInfinityKey,
      'cacheInterval': this.getPointAt(a)
    };
  }
  addInterval(a, b, c) {
    const e = this.points;
    const {
      found: f,
      index: g
    } = e.indexOfKey(a);
    let i;
    let j;
    if (f == IndexPosition.Exact) {
      const k = c(j = e.getValueAt(g));
      e.setValueAt(g, k);
      i = g + 1;
    } else {
      const l = c(j = e.getValueAt(g - 1));
      e.insertAt(g, a, l);
      i = g + 1;
    }
    while (i < e.size()) {
      const n = e.getKeyAt(i);
      if (n.getTime() >= b.getTime())
        break;
      const o = c(j = e.getValueAt(i));
      e.setValueAt(i, o);
      i++;
    }
    if (i === e.size()) {
      e.insertAt(e.size(), b, this.emptyInterval);
    } else {
      const p = e.getKeyAt(i);
      if (p.getTime() === b.getTime()) {
      } else {
        e.insertAt(i, b, j);
      }
    }
  }
  includeWrappingRange(a, b, c) {
    let e = a.getIntervalOf(b);
    while (e) {
      this.addInterval(e.startDate, e.endDate, f => this.combineIntervalsFn(f, e.cacheInterval));
      if (e.endDate.getTime() > c.getTime())
        break;
      e = a.getNextInterval(e);
    }
  }
  getSummary() {
    return this.points.map((a, b) => {
      return {
        'label': a,
        'date': b
      };
    });
  }
  clear() {
    this.points.clear();
    this.points.set(this.leftInfinityKey, this.emptyInterval);
  }
};
const everySingleDaySchedule = later_default.schedule(later_default.parse.text('every 1 day'));
const CalendarCacheSingle = class extends CalendarCache {
  constructor(a) {
    super(a);
    this.staticIntervalsCached = false;
    if (!this.unspecifiedTimeInterval)
      throw new Error('Required attribute `unspecifiedTimeInterval` is missing');
    this.intervalUnspecifiedTimeIntervals = new Map();
    this.weekUnspecifiedTimeIntervalsByKey = new Map();
    this.intervalCache = new IntervalCache({
      'emptyInterval': new CalendarCacheInterval({
        'intervals': [this.unspecifiedTimeInterval],
        'calendar': this.calendar
      }),
      'combineIntervalsFn': (b, c) => {
        return b.combineWith(c);
      }
    });
  }
  fillCache(a, b) {
    let c;
    if (!this.staticIntervalsCached) {
      this.cacheStaticIntervals();
      this.staticIntervalsCached = true;
    }
    if (this.parentCache)
      this.includeWrappingRangeFrom(this.parentCache, a, b);
    const e = this.calendar.ignoreTimeZone ? null : (c = this.calendar.project) == null ? void 0 : c.timeZone;
    if (a > b)
      throw new Error('Invalid cache fill interval');
    for (const f of this.intervalStore) {
      if (f.isRecurrent()) {
        this.cacheRecurrentInterval(a, b, e, f);
      }
    }
  }
  cacheRecurrentInterval(a, b, c, e) {
    const f = e.startDate;
    const g = e.endDate;
    if (f && f >= b || g && g <= a)
      return;
    const i = e.getAvailabilityStartSchedule();
    const j = e.getAvailabilityEndSchedule();
    const k = e.getStartDateSchedule() || i && everySingleDaySchedule;
    const l = e.getEndDateSchedule();
    const n = !l || l === 'EOD';
    let o = a;
    let p = b;
    const q = k.prev(2, a);
    let r;
    if (q) {
      o = q[0];
      if (o.getTime() === a.getTime() && q[1]) {
        o = q[1];
      }
    }
    if (n) {
      const w = k.next(1, b);
      if (w) {
        p = DateHelper.getStartOfNextDay(w, true);
      }
    } else {
      r = l.next(2, b);
      if (r) {
        p = r[0];
        if (p.getTime() === b.getTime() && r[1]) {
          p = r[1];
        }
      }
    }
    let u = k.next(Infinity, o, new Date(p.getTime() - 1000));
    if (!(u == null ? void 0 : u.length))
      return;
    let v = n ? u.map(z => DateHelper.getStartOfNextDay(z, true)) : l.next(Infinity, o, p);
    if (!(v == null ? void 0 : v.length))
      return;
    if (u[0].getTime() === v[0].getTime()) {
      v.splice(0, 1);
    }
    while (f && u[0] < f) {
      if (v[0] > f) {
        u[0] = f;
      } else {
        u.shift();
        v.shift();
      }
    }
    while (g && v[v.length - 1] > g) {
      if (u[v.length - 1] < g) {
        v[v.length - 1] = g;
      } else {
        u.pop();
        v.pop();
      }
    }
    if (v.length > u.length) {
      v.length = u.length;
    } else if (v.length < u.length) {
      u.length = v.length;
    }
    if (i) {
      const z = [], A = [], B = this.getUnspecifiedTimeIntervalForException(e, { 'isWorking': false });
      for (let C = 0, E = u.length; C < E; C++) {
        const F = u[C], G = v[C];
        this.intervalCache.addInterval(F, G, J => J.includeInterval(B));
        const H = i.next(Infinity, F, G);
        if (!H)
          return;
        const I = j.next(Infinity, new Date(F.getTime() + 1000), G);
        if (!I)
          return;
        if (I.length > H.length) {
          I.length = H.length;
        } else if (I.length < H.length) {
          H.length = I.length;
        }
        if (I[0] < H[0]) {
          H.unshift(F);
          I.push(G);
        }
        z.push(...H);
        A.push(...I);
      }
      u = z;
      v = A;
    }
    for (let J = 0, K = u.length; J < K; J++) {
      let L = u[J];
      let N = v[J];
      if (c != null) {
        L = TimeZoneHelper.toTimeZone(L, c);
        N = TimeZoneHelper.toTimeZone(N, c);
      }
      this.intervalCache.addInterval(L, N, O => O.includeInterval(e));
    }
  }
  getUnspecifiedTimeIntervalForException(a, b = {}) {
    let c = this.intervalUnspecifiedTimeIntervals.get(a);
    if (!c) {
      c = this.unspecifiedTimeInterval.copy();
      this.intervalUnspecifiedTimeIntervals.set(a, c);
      Object.assign(c, {
        'mainInterval': a,
        'calendar': this.unspecifiedTimeInterval.calendar,
        'name': a.name,
        'cls': a.cls,
        'startDate': a.startDate,
        'endDate': a.endDate,
        'prioritySortValue': a.getPrioritySortValue() - 1,
        ...b
      });
    }
    return c;
  }
  getUnspecifiedTimeIntervalForWeek(a) {
    let b = this.intervalUnspecifiedTimeIntervals.get(a);
    if (!b) {
      const c = a.compositeIntervalCode;
      b = this.weekUnspecifiedTimeIntervalsByKey.get(c);
      if (!b) {
        b = this.unspecifiedTimeInterval.copy();
        this.intervalUnspecifiedTimeIntervals.set(a, b);
        this.weekUnspecifiedTimeIntervalsByKey.set(c, b);
        Object.assign(b, {
          'mainInterval': a,
          'calendar': this.unspecifiedTimeInterval.calendar,
          'name': a.name,
          'cls': a.cls,
          'startDate': a.startDate,
          'endDate': a.endDate,
          'prioritySortValue': a.getPrioritySortValue() - 1
        });
      }
    }
    return b;
  }
  clear() {
    this.staticIntervalsCached = false;
    super.clear();
  }
  cacheStaticIntervals() {
    let a;
    const b = (a = this.calendar.project) == null ? void 0 : a.timeZone;
    const c = this.intervalCache;
    for (let e of this.intervalStore) {
      const f = e.isStatic();
      const g = e.type === CalendarIntervalType.Week;
      if (f || g) {
        if (g) {
          e = this.getUnspecifiedTimeIntervalForWeek(e);
        }
        let {
          startDate: i,
          endDate: j
        } = e;
        if (b != null) {
          i = TimeZoneHelper.toTimeZone(i, b);
          j = TimeZoneHelper.toTimeZone(j, b);
        }
        c.addInterval(i || MIN_DATE2, j || MAX_DATE2, k => k.includeInterval(e));
      }
    }
  }
};
const UnspecifiedTimeIntervalModel = class extends Mixin([CalendarIntervalMixin], a => {
  class b extends a {
    static get $name() {
      return 'UnspecifiedTimeIntervalModel';
    }
    getCalendar() {
      return this.calendar;
    }
    getMainInterval() {
      return this.mainInterval;
    }
    getPrioritySortValue() {
      if (this.prioritySortValue != null)
        return this.prioritySortValue;
      return this.prioritySortValue = this.getCalendar().getDepth();
    }
  }
  return b;
}) {
};
const AbstractCalendarMixin = class extends Mixin([AbstractPartOfProjectModelMixin], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.version = 1;
    }
    static get fields() {
      return [
        {
          'name': 'version',
          'type': 'number'
        },
        'name',
        {
          'name': 'unspecifiedTimeIsWorking',
          'type': 'boolean',
          'defaultValue': true
        },
        {
          'name': 'intervals',
          'type': 'store',
          'subStore': true
        },
        'cls',
        'iconCls'
      ];
    }
    get intervalStoreClass() {
      return CalendarIntervalStore;
    }
    get intervalStore() {
      return this.meta.intervalsStore;
    }
    initIntervalsStore(e) {
      e.storeClass = this.intervalStoreClass;
      e.modelClass = this.getDefaultConfiguration().calendarIntervalModelClass || this.intervalStoreClass.defaultConfig.modelClass;
      e.calendar = this;
      e.syncDataOnLoad = true;
    }
    processIntervalsStoreData(e) {
      this.bumpVersion();
    }
    isDefault() {
      const e = this.getProject();
      if (e) {
        return this === e.defaultCalendar;
      }
      return false;
    }
    getDepth() {
      return this.childLevel + 1;
    }
    forEachAvailabilityInterval(e, f, g) {
      let i, j;
      const k = (j = e.maxRange) != null ? j : (i = this.getProject()) == null ? void 0 : i.maxCalendarRange;
      if (k) {
        e = {
          ...e,
          'maxRange': k
        };
      }
      return this.calendarCache.forEachAvailabilityInterval(e, f, g);
    }
    accumulateWorkingTime(e, f, g) {
      let i, j, k;
      if (f === 0)
        return {
          'finalDate': new Date(e),
          'remainingDurationInMs': 0
        };
      if (isNaN(f))
        throw new Error('Invalid duration');
      let l = e;
      const n = (k = (j = (i = this.getProject()) == null ? void 0 : i.adjustDurationToDST) != null ? j : this.adjustDurationToDST) != null ? k : false;
      this.forEachAvailabilityInterval(g ? {
        'startDate': e,
        'isForward': true
      } : {
        'endDate': e,
        'isForward': false
      }, (o, p, q) => {
        let r = true;
        if (q.getIsWorking()) {
          let u = p.getTime() - o.getTime();
          if (f <= u) {
            if (n) {
              const v = g ? o.getTimezoneOffset() - new Date(o.getTime() + f).getTimezoneOffset() : new Date(p.getTime() - f).getTimezoneOffset() - p.getTimezoneOffset();
              f -= v * 60 * 1000;
            }
            l = g ? new Date(o.getTime() + f) : new Date(p.getTime() - f);
            f = 0;
            r = false;
          } else {
            if (n) {
              const w = o.getTimezoneOffset() - p.getTimezoneOffset();
              u += w * 60 * 1000;
            }
            l = g ? p : o;
            f -= u;
          }
        }
        return r;
      });
      return {
        'finalDate': new Date(l),
        'remainingDurationInMs': f
      };
    }
    calculateDurationMs(e, f, g = false) {
      let i, j, k;
      let l = 0;
      const n = e.getTime() <= f.getTime() || !g ? 1 : -1;
      if (n < 0) {
        [e, f] = [
          f,
          e
        ];
      }
      const o = (k = (j = (i = this.getProject()) == null ? void 0 : i.adjustDurationToDST) != null ? j : this.adjustDurationToDST) != null ? k : false;
      this.forEachAvailabilityInterval({
        'startDate': e,
        'endDate': f
      }, (p, q, r) => {
        if (r.getIsWorking()) {
          l += q.getTime() - p.getTime();
          if (o) {
            const u = p.getTimezoneOffset() - q.getTimezoneOffset();
            l += u * 60 * 1000;
          }
        }
      });
      return l * n;
    }
    calculateEndDate(e, f) {
      const g = f >= 0;
      const i = this.accumulateWorkingTime(e, Math.abs(f), g);
      return i.remainingDurationInMs === 0 ? i.finalDate : null;
    }
    calculateStartDate(e, f) {
      const g = f <= 0;
      const i = this.accumulateWorkingTime(e, Math.abs(f), g);
      return i.remainingDurationInMs === 0 ? i.finalDate : null;
    }
    skipNonWorkingTime(e, f = true) {
      let g;
      const i = this.forEachAvailabilityInterval(f ? {
        'startDate': e,
        'isForward': true
      } : {
        'endDate': e,
        'isForward': false
      }, (j, k, l) => {
        if (l.getIsWorking()) {
          g = f ? j : k;
          return false;
        }
      });
      if (i === CalendarIteratorResult.MaxRangeReached || i === CalendarIteratorResult.FullRangeIterated)
        return 'empty_calendar';
      return g ? new Date(g) : new Date(e);
    }
    addInterval(e) {
      return this.addIntervals([e])[0];
    }
    addIntervals(e) {
      this.bumpVersion();
      return this.intervalStore.add(e);
    }
    removeInterval(e) {
      return this.removeIntervals([e]);
    }
    removeIntervals(e) {
      this.bumpVersion();
      return this.intervalStore.remove(e);
    }
    clearIntervals(e) {
      if (!e) {
        this.bumpVersion();
      }
      return this.intervalStore.removeAll(e);
    }
    bumpVersion() {
      this.clearCache();
      this.version++;
    }
    get calendarCache() {
      if (this.$calendarCache !== void 0)
        return this.$calendarCache;
      const e = new UnspecifiedTimeIntervalModel({ 'isWorking': this.unspecifiedTimeIsWorking });
      e.calendar = this;
      return this.$calendarCache = new CalendarCacheSingle({
        'calendar': this,
        'unspecifiedTimeInterval': e,
        'intervalStore': this.intervalStore,
        'parentCache': this.parent && !this.parent.isRoot ? this.parent.calendarCache : null
      });
    }
    clearCache() {
      this.$calendarCache && this.$calendarCache.clear();
      this.$calendarCache = void 0;
    }
    resetPriorityOfAllIntervals() {
      this.traverse(e => {
        for (const f of e.intervalStore)
          f.resetPriority();
      });
    }
    insertChild(e, f, g) {
      let i = b.insertChild.call(this, ...arguments);
      if (!Array.isArray(i)) {
        i = [i];
      }
      i.forEach(j => {
        j.bumpVersion();
        j.resetPriorityOfAllIntervals();
      });
      return i;
    }
    joinProject() {
      b.joinProject.call(this);
      this.intervalStore.setProject(this.getProject());
    }
    leaveProject() {
      b.leaveProject.call(this);
      this.intervalStore.setProject(null);
      this.clearCache();
    }
    doDestroy() {
      this.leaveProject();
      this.intervalStore.destroy();
      super.doDestroy();
    }
    isDayHoliday(e) {
      const f = DateHelper.clearTime(e), g = DateHelper.getNext(e, TimeUnit.Day);
      let i = false;
      this.forEachAvailabilityInterval({
        'startDate': f,
        'endDate': g,
        'isForward': true
      }, (j, k, l) => {
        i = l.getIsWorking();
        return !i;
      });
      return !i;
    }
    getDayIntervals(e) {
      const f = DateHelper.clearTime(e), g = DateHelper.getNext(e, TimeUnit.Day);
      const i = [];
      this.forEachAvailabilityInterval({
        'startDate': f,
        'endDate': g,
        'isForward': true
      }, (j, k, l) => i.push([
        j,
        k,
        l.intervals[0]
      ]));
      return i;
    }
    getDailyHolidaysRanges(e, f) {
      const g = [];
      e = DateHelper.clearTime(e);
      while (e < f) {
        if (this.isDayHoliday(e)) {
          g.push({
            'startDate': e,
            'endDate': DateHelper.getStartOfNextDay(e, true, true)
          });
        }
        e = DateHelper.getNext(e, TimeUnit.Day);
      }
      return g;
    }
    getWorkingTimeRanges(e, f, g) {
      const i = [];
      this.forEachAvailabilityInterval({
        'startDate': e,
        'endDate': f,
        'isForward': true,
        'maxRange': g
      }, (j, k, l) => {
        if (l.getIsWorking()) {
          const n = l.intervals[0];
          i.push({
            'name': n.name,
            'startDate': j,
            'endDate': k
          });
        }
      });
      return i;
    }
    getNonWorkingTimeRanges(e, f, g) {
      const i = [];
      this.forEachAvailabilityInterval({
        'startDate': e,
        'endDate': f,
        'isForward': true,
        'maxRange': g
      }, (j, k, l) => {
        if (!l.getIsWorking()) {
          const n = l.intervals[0];
          i.push({
            'name': n.name,
            'type': n.type,
            'iconCls': n.iconCls,
            'cls': n.cls,
            'startDate': j,
            'endDate': k
          });
        }
      });
      return i;
    }
    isWorkingTime(e, f, g) {
      if (g) {
        let i;
        const j = this.forEachAvailabilityInterval({
          'startDate': e,
          'endDate': f,
          'isForward': true
        }, (k, l, n) => {
          if (n.getIsWorking() && k <= e && l >= f) {
            i = true;
            return false;
          }
        });
        if (j === CalendarIteratorResult.MaxRangeReached || j === CalendarIteratorResult.FullRangeIterated)
          return false;
        return i;
      } else {
        const k = this.skipNonWorkingTime(e);
        return k && k !== 'empty_calendar' ? f ? k < f : k.getTime() === e.getTime() : false;
      }
    }
  }
  return c;
}) {
};
const CoreCalendarMixin = class extends Mixin([
  AbstractCalendarMixin,
  CorePartOfProjectModelMixin
], a => {
  const b = a.prototype;
  class c extends a {
  }
  return c;
}) {
};
const AbstractCalendarManagerStoreMixin = class extends Mixin([AbstractPartOfProjectStoreMixin], a => {
  class b extends a {
    doDestroy() {
      let c;
      const e = [];
      if (!this.isChained && !((c = this.rootNode) == null ? void 0 : c.isDestroyed)) {
        this.traverse(f => e.push(f));
      }
      super.doDestroy();
      e.forEach(f => f.destroy());
    }
  }
  return b;
}) {
};
const CoreCalendarManagerStoreMixin = class extends Mixin([
  AbstractCalendarManagerStoreMixin,
  CorePartOfProjectStoreMixin
], a => {
  const b = a.prototype;
  class c extends a {
    static get defaultConfig() {
      return {
        'tree': true,
        'modelClass': CoreCalendarMixin
      };
    }
  }
  return c;
}) {
};
const EventsWrapper = class extends Mixin([], Events_default) {
};
const DelayableWrapper = class extends Mixin([], Delayable_default) {
};
const AbstractProjectMixin = class extends Mixin([
  EventsWrapper,
  DelayableWrapper,
  Model
], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.isRestoringData = false;
    }
    get isRepopulatingStores() {
      return false;
    }
    get isInitialCommit() {
      return !this.isInitialCommitPerformed || this.hasLoadedDataToCommit;
    }
    construct(e = {}) {
      this.isInitialCommitPerformed = false;
      this.isLoadingInlineData = false;
      this.isWritingData = false;
      this.hasLoadedDataToCommit = false;
      const f = 'silenceInitialCommit' in e ? e.silenceInitialCommit : true;
      const g = 'adjustDurationToDST' in e ? e.adjustDurationToDST : false;
      this.maxCalendarRange = 'maxCalendarRange' in e ? e.maxCalendarRange : 157680000000;
      delete e.maxCalendarRange;
      delete e.silenceInitialCommit;
      delete e.adjustDurationToDST;
      b.construct.call(this, e);
      this.silenceInitialCommit = f;
      this.adjustDurationToDST = g;
    }
    repopulateStore(e) {
    }
    repopulateReplica() {
    }
    deferUntilRepopulationIfNeeded(e, f, g) {
      f(...g);
    }
    attachStore(e) {
    }
    detachStore(e) {
    }
    async commitAsync() {
      throw new Error('Abstract method called');
    }
    isEngineReady() {
      throw new Error('Abstract method called');
    }
    getStm() {
      throw new Error('Abstract method called');
    }
  }
  return c;
}) {
};
const DelayableWrapper2 = class extends Mixin([], Delayable_default) {
};
const SchedulerCoreProjectMixin = class extends Mixin([
  AbstractProjectMixin,
  CorePartOfProjectGenericMixin,
  DelayableWrapper2,
  Model
], a => {
  const b = a.prototype;
  class c extends a {
    static get fields() {
      return [
        {
          'name': 'startDate',
          'type': 'date'
        },
        {
          'name': 'endDate',
          'type': 'date'
        }
      ];
    }
    static get configurable() {
      return {
        'stm': {},
        'eventStore': {},
        'assignmentStore': {},
        'resourceStore': {},
        'dependencyStore': {},
        'calendarManagerStore': {},
        'eventModelClass': SchedulerCoreEvent,
        'assignmentModelClass': CoreAssignmentMixin,
        'resourceModelClass': CoreResourceMixin,
        'dependencyModelClass': CoreDependencyMixin,
        'calendarModelClass': CoreCalendarMixin,
        'eventStoreClass': CoreEventStoreMixin,
        'assignmentStoreClass': CoreAssignmentStoreMixin,
        'resourceStoreClass': CoreResourceStoreMixin,
        'dependencyStoreClass': CoreDependencyStoreMixin,
        'calendarManagerStoreClass': CoreCalendarManagerStoreMixin,
        'assignmentsData': null,
        'calendarsData': null,
        'dependenciesData': null,
        'eventsData': null,
        'resourcesData': null
      };
    }
    construct(e = {}) {
      let f;
      const g = this;
      g.$invalidated = new Set();
      g.$leaving = new Set();
      g.isPerformingCommit = false;
      g.silenceInitialCommit = true;
      g.ongoing = Promise.resolve();
      if (e.eventStore && !e.assignmentStore) {
        const p = e.eventStore.masterStore || e.eventStore;
        const q = p.assignmentStore || ((f = p.crudManager) == null ? void 0 : f.assignmentStore);
        if (q == null ? void 0 : q.isAssignmentStore) {
          e.assignmentStore = q;
          g.isSharingAssignmentStore = true;
        }
      }
      b.construct.call(g, e);
      g.defaultCalendar = new g.calendarManagerStore.modelClass({ 'unspecifiedTimeIsWorking': g.unspecifiedTimeIsWorking });
      g.defaultCalendar.project = g;
      const {
        calendarsData: i,
        eventsData: j,
        dependenciesData: k,
        resourcesData: l,
        assignmentsData: n
      } = g;
      const o = Boolean(i || j || k || l || n);
      if (o) {
        g.loadInlineData({
          'calendarsData': i,
          'eventsData': j,
          'dependenciesData': k,
          'resourcesData': l,
          'assignmentsData': n
        });
        delete g.calendarsData;
        delete g.eventsData;
        delete g.dependenciesData;
        delete g.resourcesData;
        delete g.assignmentsData;
      } else {
        g.bufferedCommitAsync();
      }
    }
    doDestroy() {
      let e, f, g, i, j, k;
      const l = this;
      (e = l.eventStore) == null ? void 0 : e.destroy();
      (f = l.dependencyStore) == null ? void 0 : f.destroy();
      (g = l.assignmentStore) == null ? void 0 : g.destroy();
      (i = l.resourceStore) == null ? void 0 : i.destroy();
      (j = l.calendarManagerStore) == null ? void 0 : j.destroy();
      l.defaultCalendar.destroy();
      (k = l.stm) == null ? void 0 : k.destroy();
      b.doDestroy.call(this);
    }
    async loadInlineData(e) {
      const f = this;
      f.isLoadingInlineData = true;
      if (e.calendarsData) {
        f.calendarManagerStore.data = e.calendarsData;
      }
      if (e.resourcesData) {
        f.resourceStore.data = e.resourcesData;
      }
      if (e.assignmentsData) {
        f.assignmentStore.data = e.assignmentsData;
      }
      if (e.eventsData) {
        f.eventStore.data = e.eventsData;
      }
      if (e.tasksData) {
        f.eventStore.data = e.tasksData;
      }
      if (e.dependenciesData) {
        f.dependencyStore.data = e.dependenciesData;
      }
      await f.commitLoad();
      f.isLoadingInlineData = false;
      return;
    }
    async commitLoad() {
      await this.commitAsync();
      if (!this.isDestroyed)
        this.trigger('load');
    }
    joinStoreRecords(e) {
      const f = g => {
        g.setProject(this);
        g.joinProject();
      };
      if (e.rootNode) {
        e.rootNode.traverse(f);
      } else {
        e.forEach(f);
      }
    }
    unJoinStoreRecords(e) {
      const f = g => {
        g.leaveProject();
        g.setProject(this);
      };
      if (e.rootNode) {
        e.rootNode.traverse(g => {
          if (g !== e.rootNode)
            f(g);
        });
      } else {
        e.forEach(f);
      }
    }
    resolveStoreAndModelClass(e, f) {
      const g = (f == null ? void 0 : f.storeClass) || this[e + 'StoreClass'];
      let i = f == null ? void 0 : f.modelClass;
      if (!i) {
        if (this.getDefaultConfiguration()[e + 'ModelClass'] !== g.getDefaultConfiguration().modelClass) {
          i = g.getDefaultConfiguration().modelClass;
        } else {
          i = this[e + 'ModelClass'];
        }
      }
      return {
        'storeClass': g,
        'modelClass': i
      };
    }
    setEventStore(e) {
      this.eventStore = e;
    }
    changeEventStore(e, f) {
      const g = this, {stm: i} = g;
      g.detachStore(f);
      if (!(e instanceof Store)) {
        const {
          storeClass: j,
          modelClass: k
        } = g.resolveStoreAndModelClass('event', e);
        e = new j(ObjectHelper.assign({
          'modelClass': k,
          'project': g,
          'stm': i
        }, e));
      } else {
        e.project = g;
        i.addStore(e);
        g.joinStoreRecords(e);
      }
      if (f && i.hasStore(f)) {
        i.removeStore(f);
        g.unJoinStoreRecords(f);
        const {assignmentsForRemoval: l} = f;
        l.forEach(n => {
          const o = n.event;
          if (o) {
            const p = e.getById(o.id);
            if (p) {
              n.event = p;
              l.delete(n);
            }
          }
        });
        f.afterEventRemoval();
      }
      e.setProject(g);
      return e;
    }
    updateEventStore(e, f) {
      this.attachStore(e);
      this.trigger('eventStoreChange', { 'store': e });
    }
    setAssignmentStore(e) {
      this.assignmentStore = e;
    }
    changeAssignmentStore(e, f) {
      const g = this, {stm: i} = g;
      g.detachStore(f);
      if (f && i.hasStore(f)) {
        i.removeStore(f);
        g.unJoinStoreRecords(f);
      }
      if (!(e instanceof Store)) {
        const {
          storeClass: j,
          modelClass: k
        } = g.resolveStoreAndModelClass('assignment', e);
        e = new j(ObjectHelper.assign({
          'modelClass': k,
          'project': g,
          'stm': i
        }, e));
      } else {
        e.project = g;
        i.addStore(e);
        g.joinStoreRecords(e);
      }
      e.setProject(g);
      return e;
    }
    updateAssignmentStore(e, f) {
      this.attachStore(e);
      this.trigger('assignmentStoreChange', { 'store': e });
    }
    setResourceStore(e) {
      this.resourceStore = e;
    }
    changeResourceStore(e, f) {
      const g = this, {stm: i} = g;
      g.detachStore(f);
      if (!(e instanceof Store)) {
        const {
          storeClass: j,
          modelClass: k
        } = g.resolveStoreAndModelClass('resource', e);
        e = new j(ObjectHelper.assign({
          'modelClass': k,
          'project': g,
          'stm': i
        }, e));
      } else {
        e.project = g;
        i.addStore(e);
        g.joinStoreRecords(e);
      }
      if (f && i.hasStore(f)) {
        i.removeStore(f);
        g.unJoinStoreRecords(f);
        const {assignmentsForRemoval: l} = f;
        l.forEach(n => {
          const o = n.resource;
          if (o) {
            const p = e.getById(o.id);
            if (p) {
              n.resource = p;
              l.delete(n);
            }
          }
        });
        f.afterResourceRemoval();
      }
      e.setProject(g);
      return e;
    }
    updateResourceStore(e, f) {
      this.attachStore(e);
      this.trigger('resourceStoreChange', { 'store': e });
    }
    setDependencyStore(e) {
      this.dependencyStore = e;
    }
    changeDependencyStore(e, f) {
      const g = this;
      g.detachStore(f);
      if (!(e instanceof Store)) {
        const {
          storeClass: i,
          modelClass: j
        } = g.resolveStoreAndModelClass('dependency', e);
        e = new i(ObjectHelper.assign({
          'modelClass': j,
          'project': g,
          'stm': g.stm
        }, e));
      } else {
        e.project = g;
        g.stm.addStore(e);
        g.joinStoreRecords(e);
      }
      return e;
    }
    updateDependencyStore(e, f) {
      this.attachStore(e);
      this.trigger('dependencyStoreChange', { 'store': e });
    }
    setCalendarManagerStore(e) {
      this.calendarManagerStore = e;
    }
    changeCalendarManagerStore(e, f) {
      const g = this;
      g.detachStore(f);
      if (!(e instanceof Store)) {
        const i = (e == null ? void 0 : e.storeClass) || g.calendarManagerStoreClass;
        const j = (e == null ? void 0 : e.modelClass) || i.getDefaultConfiguration().modelClass || g.calendarModelClass;
        e = new i(ObjectHelper.assign({
          'modelClass': j,
          'project': g,
          'stm': g.stm
        }, e));
      } else {
        g.stm.addStore(e);
      }
      e.setProject(g);
      return e;
    }
    updateCalendarManagerStore(e, f) {
      this.attachStore(e);
      this.trigger('calendarManagerStoreChange', { 'store': e });
    }
    get calendar() {
      return this.$calendar || this.defaultCalendar;
    }
    set calendar(e) {
      this.$calendar = e;
    }
    get effectiveCalendar() {
      return this.calendar;
    }
    async addEvent(e) {
      this.eventStore.add(e);
      return this.commitAsync();
    }
    async addAssignment(e) {
      this.assignmentStore.add(e);
      return this.commitAsync();
    }
    async addResource(e) {
      this.resourceStore.add(e);
      return this.commitAsync();
    }
    async addDependency(e) {
      this.dependencyStore.add(e);
      return this.commitAsync();
    }
    bufferedCommitAsync() {
      if (!this.hasPendingAutoCommit) {
        this.setTimeout({
          'fn': () => {
            let e;
            return (e = this.commitAsync) == null ? void 0 : e.call(this);
          },
          'name': 'commitAsync',
          'delay': 10
        });
      }
    }
    get hasPendingAutoCommit() {
      return this.hasTimeout('commitAsync');
    }
    unScheduleAutoCommit() {
      this.clearTimeout('commitAsync');
    }
    async commitAsync() {
      if (this.isPerformingCommit)
        return this.ongoing;
      this.trigger('beforeCommit');
      return this.ongoing = this.doCommitAsync();
    }
    async doCommitAsync() {
      const e = this;
      if (!e.$invalidated.size && !e.$leaving.size && !e.hasLoadedDataToCommit && !e.isInitialCommit) {
        e.trigger('commitFinalized');
        return;
      }
      e.$leaving.clear();
      e.isPerformingCommit = true;
      await delay(0);
      if (!e.isDestroyed) {
        e.unScheduleAutoCommit();
        for (const k of e.$invalidated) {
          k.calculateInvalidated();
        }
        const {
          isInitialCommit: f,
          silenceInitialCommit: g
        } = e;
        const i = f && g;
        e.assignmentStore.onCommitAsync();
        e.dependencyStore.onCommitAsync();
        e.isInitialCommitPerformed = true;
        e.hasLoadedDataToCommit = false;
        e.isPerformingCommit = false;
        const j = [
          e.assignmentStore,
          e.dependencyStore,
          e.eventStore,
          e.resourceStore,
          e.calendarManagerStore
        ];
        j.forEach(l => {
          let n;
          return (n = l.suspendAutoCommit) == null ? void 0 : n.call(l);
        });
        e.isWritingData = true;
        e.trigger('refresh', {
          'isInitialCommit': f,
          'isCalculated': true
        });
        if (i) {
          for (const l of e.$invalidated) {
            l.finalizeInvalidated(true);
          }
        } else {
          for (const n of e.$invalidated) {
            n.beginBatch(true);
            n.finalizeInvalidated();
          }
          for (const o of e.$invalidated) {
            o.endBatch(false, true);
          }
        }
        e.isWritingData = false;
        e.$invalidated.clear();
        e.trigger('dataReady');
        j.forEach(p => {
          let q;
          return (q = p.resumeAutoCommit) == null ? void 0 : q.call(p);
        });
        e.trigger('commitFinalized');
        return true;
      }
    }
    async propagateAsync() {
      return this.commitAsync();
    }
    invalidate(e) {
      this.$invalidated.add(e);
      this.bufferedCommitAsync();
    }
    async isValidDependency() {
      return true;
    }
    getStm() {
      return this.stm;
    }
    set stm(e) {
      e = this.$stm = new StateTrackingManager(ObjectHelper.assign({ 'disabled': true }, e));
      e.ion({
        'restoringStop': async () => {
          e.disable();
          await this.commitAsync();
          if (!this.isDestroyed) {
            e.enable();
            this.trigger('stateRestoringDone');
          }
        }
      });
    }
    get stm() {
      return this.$stm;
    }
    isEngineReady() {
      return !this.hasPendingAutoCommit && !this.isPerformingCommit && this.isInitialCommitPerformed;
    }
  }
  c.applyConfigs = true;
  return c;
}) {
};
const EngineMixin9 = SchedulerCoreProjectMixin;
const ProjectModel = class extends ProjectCurrentConfig_default(ProjectModelMixin_default(EngineMixin9)) {
  static get $name() {
    return 'ProjectModel';
  }
  static get defaultConfig() {
    return {
      'eventModelClass': EventModel,
      'dependencyModelClass': DependencyModel,
      'resourceModelClass': ResourceModel,
      'assignmentModelClass': AssignmentModel,
      'eventStoreClass': EventStore,
      'dependencyStoreClass': DependencyStore,
      'resourceStoreClass': ResourceStore,
      'assignmentStoreClass': AssignmentStore
    };
  }
  construct(...a) {
    super.construct(...a);
    if (VersionHelper.isTestEnv) {
      globalThis.bryntum.testProject = this;
    }
    this.modelPersistencyManager = this.createModelPersistencyManager();
  }
  createModelPersistencyManager() {
    return new ModelPersistencyManager({
      'eventStore': this,
      'resourceStore': this.resourceStore,
      'assignmentStore': this.assignmentStore,
      'dependencyStore': this.dependencyStore
    });
  }
  doDestroy() {
    this.modelPersistencyManager.destroy();
    super.doDestroy();
  }
  get taskStore() {
    return this.eventStore;
  }
};
ProjectModel.applyConfigs = true;
ProjectModel.initClass();
ProjectModel._$name = 'ProjectModel';
const CrudManager = class extends AbstractCrudManager.mixin(ProjectCrudManager_default, AjaxTransport_default, JsonEncoder_default, LazyLoadCrudManager_default) {
  static $name = 'CrudManager';
  static get defaultConfig() {
    return {
      'projectClass': ProjectModel,
      'resourceStoreClass': ResourceStore,
      'eventStoreClass': EventStore,
      'assignmentStoreClass': AssignmentStore,
      'dependencyStoreClass': DependencyStore,
      'resourceStore': {},
      'eventStore': {},
      'assignmentStore': {},
      'dependencyStore': {},
      'project': null
    };
  }
  buildProject() {
    return new this.projectClass(this.buildProjectConfig());
  }
  buildProjectConfig() {
    return ObjectHelper.cleanupProperties({
      'eventStore': this.eventStore,
      'resourceStore': this.resourceStore,
      'assignmentStore': this.assignmentStore,
      'dependencyStore': this.dependencyStore,
      'resourceTimeRangeStore': this.resourceTimeRangeStore
    });
  }
  set project(a) {
    const b = this;
    if (a !== b._project) {
      b.detachListeners('beforeDataReady');
      b.detachListeners('afterDataReady');
      b._project = a;
      if (a) {
        b.eventStore = a.eventStore;
        b.resourceStore = a.resourceStore;
        b.assignmentStore = a.assignmentStore;
        b.dependencyStore = a.dependencyStore;
        b.timeRangeStore = a.timeRangeStore;
        b.resourceTimeRangeStore = a.resourceTimeRangeStore;
        a.ion({
          'name': 'beforeDataReady',
          'dataReady': () => b.suspendChangeTracking(),
          'prio': 100,
          'thisObj': b
        });
        a.ion({
          'name': 'afterDataReady',
          'dataReady': () => b.resumeChangeTracking(),
          'prio': -100,
          'thisObj': b
        });
      }
      if (!b.eventStore) {
        b.eventStore = {};
      }
      if (!b.resourceStore) {
        b.resourceStore = {};
      }
      if (!b.assignmentStore) {
        b.assignmentStore = {};
      }
      if (!b.dependencyStore) {
        b.dependencyStore = {};
      }
    }
  }
  get project() {
    return this._project;
  }
  get timeRangeStore() {
    let a;
    return (a = this._timeRangeStore) == null ? void 0 : a.store;
  }
  set timeRangeStore(a) {
    let b;
    this.setFeaturedStore('_timeRangeStore', a, (b = this.project) == null ? void 0 : b.timeRangeStoreClass);
  }
  get resourceTimeRangeStore() {
    let a;
    return (a = this._resourceTimeRangeStore) == null ? void 0 : a.store;
  }
  set resourceTimeRangeStore(a) {
    let b;
    this.setFeaturedStore('_resourceTimeRangeStore', a, (b = this.project) == null ? void 0 : b.resourceTimeRangeStoreClass);
  }
  get resourceStore() {
    let a;
    return (a = this._resourceStore) == null ? void 0 : a.store;
  }
  set resourceStore(a) {
    this.setFeaturedStore('_resourceStore', a, this.resourceStoreClass);
  }
  get eventStore() {
    let a;
    return (a = this._eventStore) == null ? void 0 : a.store;
  }
  set eventStore(a) {
    this.setFeaturedStore('_eventStore', a, this.eventStoreClass);
  }
  get assignmentStore() {
    let a;
    return (a = this._assignmentStore) == null ? void 0 : a.store;
  }
  set assignmentStore(a) {
    this.setFeaturedStore('_assignmentStore', a, this.assignmentStoreClass);
  }
  get dependencyStore() {
    let a;
    return (a = this._dependencyStore) == null ? void 0 : a.store;
  }
  set dependencyStore(a) {
    this.setFeaturedStore('_dependencyStore', a, this.dependencyStoreClass);
  }
  setFeaturedStore(a, b, c) {
    let e;
    const f = this, g = (e = f[a]) == null ? void 0 : e.store;
    if (g !== b) {
      b = Store.getStore(b, (b == null ? void 0 : b.storeClass) || c);
      if (g) {
        f.removeStore(g);
      }
      f[a] = b && { 'store': b } || null;
      f.addPrioritizedStore(f[a]);
    }
    return f[a];
  }
  getChangesetPackage() {
    let a, b;
    const c = this, e = super.getChangesetPackage();
    if (e && (c.eventStore.usesSingleAssignment || ((b = (a = c.eventStore.modelClass.fieldMap) == null ? void 0 : a.resourceIds) == null ? void 0 : b.persist))) {
      delete e[c.assignmentStore.id];
      if (!c.forceSync && !c.crudStores.some(f => e[f.storeId])) {
        return null;
      }
    }
    return e;
  }
  get crudLoadValidationMandatoryStores() {
    return [
      this._eventStore.storeId,
      this._resourceStore.storeId
    ];
  }
};
CrudManager._$name = 'CrudManager';
const stores = [
  'eventStore',
  'taskStore',
  'assignmentStore',
  'resourceStore',
  'dependencyStore',
  'timeRangeStore',
  'resourceTimeRangeStore'
];
const inlineProperties = [
  'events',
  'tasks',
  'resources',
  'assignments',
  'dependencies',
  'timeRanges',
  'resourceTimeRanges'
];
const CurrentConfig_default = a => class b extends a {
  static get $name() {
    return 'CurrentConfig';
  }
  preProcessCurrentConfigs(c) {
    for (const e of inlineProperties) {
      delete c[e];
    }
    super.preProcessCurrentConfigs(c);
  }
  getCurrentConfig(c) {
    let e, f;
    const g = (f = (e = this.project).getCurrentConfig) == null ? void 0 : f.call(e, c), i = super.getCurrentConfig(c);
    if (g) {
      i.project = g;
      const {crudManager: j} = i;
      if (j) {
        for (const k of stores) {
          if (j[k]) {
            g[k] = j[k];
          }
        }
      }
      if (Object.keys(g).length === 0) {
        delete i.project;
      }
    }
    delete i.data;
    delete i.store;
    delete i.eventStore;
    delete i.resourceStore;
    delete i.crudManager;
    return i;
  }
  get widgetClass() {
  }
};
const arrayify = a => !a || Array.isArray(a) ? a : [a];
const pickFormat = (a, b, c) => a && a[b] !== true ? a[b] : c;
const Describable_default = a => {
  let b;
  return b = class extends (a || Base) {
    get dateBounds() {
      return [this.date];
    }
    get description() {
      const c = this, {descriptionRenderer: e} = c;
      return e ? c.callback(e, c, [c]) : c.formattedDescription;
    }
    get formattedDescription() {
      let c;
      const e = this, {
          dateBounds: f,
          dateFormat: g
        } = e, i = (c = e.descriptionFormat) != null ? c : arrayify(e.defaultDescriptionFormat), j = pickFormat(i, 0, g), k = f.length > 1 && (i == null ? void 0 : i.length) > 1 && DateHelper.format(f[0], j) !== DateHelper.format(f[1], j);
      let l = DateHelper.format(f[0], j);
      if (k) {
        l = DateHelper.formatRange(f, pickFormat(i, 1, 'S' + g + e.dateSeparator + 'E' + g));
      }
      return l;
    }
    changeDescriptionFormat(c) {
      return arrayify(c);
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'Describable'), __publicField(b, 'configurable', {
    'dateFormat': 'MMMM d, YYYY',
    'dateSeparator': ' - ',
    'descriptionFormat': null,
    'descriptionRenderer': null
  }), b;
};
const SchedulerDom_default = a => class b extends (a || Base) {
  static get $name() {
    return 'SchedulerDom';
  }
  getElementFromAssignmentRecord(c, e = false, f = false) {
    let g, i, j, k, l, n;
    if (this.isPainted && c) {
      let o = (g = this.foregroundCanvas.syncIdMap) == null ? void 0 : g[c.id];
      if (!o && ((i = c.resource) == null ? void 0 : i.hasLinks)) {
        for (const p of c.resource.$links) {
          o = (j = this.foregroundCanvas.syncIdMap) == null ? void 0 : j[c.id + '_' + p.id];
          if (!o) {
            if (f) {
              o = (k = this.foregroundCanvas.releasedIdMap) == null ? void 0 : k[c.id + '_' + p.id];
              if (o) {
                break;
              }
            }
          } else {
            break;
          }
        }
      }
      if (!o && f) {
        o = (l = this.foregroundCanvas.releasedIdMap) == null ? void 0 : l[c.id];
      }
      return e ? o : (n = o == null ? void 0 : o.syncIdMap) == null ? void 0 : n.event;
    }
    return null;
  }
  getElementFromEventRecord(c, e = (k => (k = c.resources) == null ? void 0 : k[0])(), f = false, g = false) {
    let i;
    if (c.isResourceTimeRange) {
      const k = (i = this.foregroundCanvas.syncIdMap) == null ? void 0 : i[c.domId];
      return f ? k : k == null ? void 0 : k.syncIdMap.event;
    }
    const j = this.assignmentStore.getAssignmentForEventAndResource(c, e);
    return this.getElementFromAssignmentRecord(j, f, g);
  }
  getElementsFromEventRecord(c, e, f = false, g = false) {
    if (e) {
      return [this.getElementFromEventRecord(c, e, f, g)];
    } else {
      return c.resources.reduce((i, j) => {
        const k = this.getElementFromEventRecord(c, j, f, g);
        k && i.push(k);
        return i;
      }, []);
    }
  }
  resolveResourceRecord(c, e) {
    const f = this.currentOrientation.resolveRowRecord(c, e);
    if (f == null ? void 0 : f.isSpecialRow) {
      return null;
    }
    return f;
  }
  resolveRowRecord(c) {
    return this.resolveResourceRecord(c);
  }
  resolveEventRecord(c) {
    if (c instanceof Event) {
      c = c.target;
    }
    const e = c == null ? void 0 : c.closest(this.eventSelector);
    if (e) {
      if ('eventId' in e.dataset) {
        return this.eventStore.getById(e.dataset.eventId);
      }
      if ('assignmentId' in e.dataset) {
        return this.assignmentStore.getById(e.dataset.assignmentId).event;
      }
    }
    return null;
  }
  resolveTimeSpanRecord(c) {
    return this.resolveEventRecord(c);
  }
  resolveAssignmentRecord(c, e = false) {
    const f = c.closest(e ? '[data-assignment-id][data-event-id]' : this.eventSelector), g = f && this.assignmentStore.getById(f.dataset.assignmentId), i = f && this.eventStore.getById(f.dataset.eventId);
    return this.assignmentStore.getOccurrence(g, i);
  }
  isRowVisible(c) {
    return this.store.indexOf(c) >= 0;
  }
  get widgetClass() {
  }
  isEventElement(c) {
    const e = c == null ? void 0 : c.className;
    return Boolean(e == null ? void 0 : e[this.eventCls + '-wrap']);
  }
};
const SchedulerDomEvents_default = a => class b extends (a || Base) {
  static get $name() {
    return 'SchedulerDomEvents';
  }
  getTimeSpanMouseEventParams(c, e) {
    const f = this.resolveEventRecord(c);
    return f && {
      'eventRecord': f,
      'resourceRecord': this.resolveResourceRecord(c),
      'assignmentRecord': this.resolveAssignmentRecord(c),
      'eventElement': c,
      'event': e
    };
  }
  getScheduleMouseEventParams(c, e) {
    const f = this.isVertical ? this.resolveResourceRecord(e) : this.store.getById(c.id);
    return f ? { 'resourceRecord': f } : null;
  }
  onElementKeyDown(c) {
    const e = super.onElementKeyDown(c), f = this;
    if (f.selectedEvents.length) {
      f.trigger(f.scheduledEventName + 'KeyDown', {
        'eventRecords': f.selectedEvents,
        'assignmentRecords': f.selectedAssignments,
        'event': c
      });
    }
    return e;
  }
  onElementKeyUp(c) {
    super.onElementKeyUp(c);
    const e = this;
    if (e.selectedEvents.length) {
      e.trigger(e.scheduledEventName + 'KeyUp', {
        'eventRecords': e.selectedEvents,
        'assignmentRecords': e.selectedAssignments,
        'event': c
      });
    }
  }
  get widgetClass() {
  }
};
const HorizontalLayout = class extends Base {
  static get defaultConfig() {
    return {
      'nbrOfBandsByResource': {},
      'bandIndexToPxConvertFn': null,
      'bandIndexToPxConvertThisObj': null
    };
  }
  clearCache(a) {
    if (a) {
      delete this.nbrOfBandsByResource[a.id];
    } else {
      this.nbrOfBandsByResource = {};
    }
  }
  applyLayout(a, b) {
    return this.nbrOfBandsByResource[b.id] = this.layoutEventsInBands(a, b);
  }
  layoutEventsInBands(a, b) {
    throw new Error('Implement in subclass');
  }
};
HorizontalLayout._$name = 'HorizontalLayout';
const HorizontalLayoutStack = class extends HorizontalLayout {
  static get $name() {
    return 'HorizontalLayoutStack';
  }
  static get configurable() {
    return { 'type': 'stack' };
  }
  layoutEventsInBands(a, b, c = false) {
    let e = 0;
    do {
      let f = 0, g = a[0];
      while (g) {
        if (!c) {
          g.top = this.bandIndexToPxConvertFn.call(this.bandIndexToPxConvertThisObj || this, e, g.eventRecord, g.resourceRecord);
        }
        a.splice(f, 1);
        f = this.findClosestSuccessor(g, a);
        g = a[f];
      }
      e++;
    } while (a.length > 0);
    return e;
  }
  findClosestSuccessor(a, b) {
    let c, e;
    const {
        endMS: f,
        group: g
      } = a, j = ((c = a.eventRecord) == null ? void 0 : c.duration) === 0;
    let k = Infinity, n, o, p, q;
    for (let r = 0, u = b.length; r < u; r++) {
      p = b[r];
      o = p.startMS - f;
      q = p.endMS - p.startMS === 0 && !((e = p.eventRecord) == null ? void 0 : e.meta.isDragCreating);
      if (o >= 0 && o < k && (o > 0 || !q || !j)) {
        if (this.grouped && g !== p.group) {
          break;
        }
        n = r;
        k = o;
      }
    }
    return n;
  }
};
HorizontalLayoutStack._$name = 'HorizontalLayoutStack';
const rangesIntersect = (a, b, c, e) => {
  return c <= a && a < e || a <= c && c < b;
};
const PackMixin_default = a => {
  let b;
  return b = class extends (a || Base) {
    isSameGroup(c, e) {
      return this.grouped ? c.group === e.group : true;
    }
    packEventsInBands(c, e) {
      const f = this, {
          coordProp: g,
          sizeProp: k
        } = f;
      let n, o, p, q;
      for (let r = 0, u = c.length; r < u; r++) {
        o = c[r];
        n = f.findStartSlot(c, o);
        p = f.getCluster(c, r);
        if (p.length > 1) {
          o[g] = n.start;
          o[k] = n.end - n.start;
          q = 1;
          while (q < p.length - 1 && p[q + 1].start - o.start === 0) {
            q++;
          }
          const z = f.findStartSlot(c, p[q]);
          if (z && z.start < 0.8) {
            p.length = q;
          }
        }
        const v = p.length, w = (n.end - n.start) / v;
        for (q = 0; q < v; q++) {
          e(p[q], q, n, w);
        }
        r += v - 1;
      }
      return 1;
    }
    findStartSlot(c, e) {
      const {
          inBandSizeProp: f,
          inBandCoordProp: g,
          coordProp: j,
          sizeProp: k
        } = this, l = this.getPriorOverlappingEvents(c, e);
      let n;
      if (l.length === 0) {
        return {
          'start': 0,
          'end': 1
        };
      }
      for (n = 0; n < l.length; n++) {
        const o = l[n], p = g in o ? g : j, q = f in o ? f : k;
        if (n === 0 && o[p] > 0) {
          return {
            'start': 0,
            'end': o[p]
          };
        } else {
          if (o[p] + o[q] < (n < l.length - 1 ? l[n + 1][p] : 1)) {
            return {
              'start': o[p] + o[q],
              'end': n < l.length - 1 ? l[n + 1][p] : 1
            };
          }
        }
      }
      return false;
    }
    getPriorOverlappingEvents(c, e) {
      const {
          startMS: f,
          endMS: g
        } = e, j = [];
      for (let k = 0, n = c.indexOf(e); k < n; k++) {
        const o = c[k];
        if (this.isSameGroup(o, e) && rangesIntersect(f, g, o.startMS, o.endMS)) {
          j.push(o);
        }
      }
      j.sort(this.sortOverlappers.bind(this));
      return j;
    }
    sortOverlappers(c, e) {
      const {coordProp: f} = this;
      return c[f] - e[f];
    }
    getCluster(c, e) {
      const f = c[e], g = [f];
      if (e >= c.length - 1) {
        return g;
      }
      let {
        startMS: j,
        endMS: k
      } = f;
      for (let n = e + 1, o = c.length; n < o; n++) {
        const p = c[n];
        if (!this.isSameGroup(p, f) || !rangesIntersect(j, k, p.startMS, p.endMS)) {
          break;
        }
        g.push(p);
        j = Math.max(j, p.startMS);
        k = Math.min(p.endMS, k);
      }
      return g;
    }
  }, __publicField(b, '$name', 'PackMixin'), __publicField(b, 'configurable', {
    'coordProp': 'top',
    'sizeProp': 'height',
    'inBandCoordProp': 'inBandTop',
    'inBandSizeProp': 'inBandHeight'
  }), b;
};
const HorizontalLayoutPack = class extends HorizontalLayout.mixin(PackMixin_default) {
  static configurable = { 'type': 'pack' };
  static $name = 'HorizontalLayoutPack';
  layoutEventsInBands(a) {
    const b = this.packEventsInBands(a, (c, e, f, g) => {
      c.height = g;
      c.top = f.start + e * g;
    });
    a.forEach(c => {
      Object.assign(c, this.bandIndexToPxConvertFn.call(this.bandIndexToPxConvertThisObj || this, c.top, c.height, c.eventRecord, c.resourceRecord));
    });
    return b;
  }
};
HorizontalLayoutPack._$name = 'HorizontalLayoutPack';
const SchedulerResourceRendering_default = a => {
  let b;
  return b = class extends (a || Base) {
    get resourceColumns() {
      let c;
      return ((c = this.timeAxisColumn) == null ? void 0 : c.resourceColumns) || this._resourceColumns;
    }
    get resourceColumnWidth() {
      let c;
      return ((c = this.resourceColumns) == null ? void 0 : c.columnWidth) || null;
    }
    getResourceMarginObject(c) {
      let e;
      if (c == null ? void 0 : c.resourceMargin) {
        const f = (e = c == null ? void 0 : c.resourceMargin) != null ? e : this.resourceMargin;
        if (typeof f === 'number') {
          return {
            'start': f,
            'end': f,
            'total': f * 2
          };
        } else {
          f.total = f.start + f.end;
          return f;
        }
      } else {
        return this.resourceMarginObject;
      }
    }
    getBarMargin(c) {
      let e;
      return (e = c == null ? void 0 : c.barMargin) != null ? e : this.barMargin;
    }
    getResourceHeight(c) {
      let e;
      return (e = c.rowHeight) != null ? e : this.isHorizontal ? this.rowHeight : this.getResourceWidth(c);
    }
    getResourceWidth(c) {
      let e;
      return (e = c.instanceMeta(this).columnWidth) != null ? e : this.resourceColumnWidth;
    }
    getAppliedResourceHeight(c) {
      let e;
      const f = this.getRowById(c);
      return (e = f == null ? void 0 : f.maxRequestedHeight) != null ? e : this.getResourceHeight(c);
    }
    getResourceLayoutSettings(c, e = null) {
      const f = this.getResourceMarginObject(c, e), g = this.getAppliedResourceHeight(c, e);
      return {
        'barMargin': this.getBarMargin(c, e),
        'contentHeight': Math.max(g - f.total, 1),
        'rowHeight': g,
        'resourceMarginObject': f
      };
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'SchedulerResourceRendering'), __publicField(b, 'configurable', {
    'resourceMargin': null,
    'resourceColumns': null,
    'resourceImagePath': null,
    'defaultResourceImageName': null,
    'resourceImageExtension': '.jpg'
  }), b;
};
const SchedulerEventRendering_default = a => class b extends SchedulerResourceRendering_default(a || Base) {
  static get $name() {
    return 'SchedulerEventRendering';
  }
  static get configurable() {
    return {
      'milestoneTextPosition': 'outside',
      'milestoneAlign': 'center',
      'milestoneCharWidth': 10,
      'milestoneLayoutMode': 'default',
      'eventLayout': 'stack',
      'overlappingEventSorter': null,
      'useInitialAnimation': true,
      'eventRenderer': null,
      'eventRendererThisObj': null,
      'eventBarTextField': 'name',
      'horizontalLayoutPackClass': HorizontalLayoutPack,
      'horizontalLayoutStackClass': HorizontalLayoutStack,
      'isFirstRender': true,
      'initialAnimationDuration': 2000,
      'narrowEventWidth': 10,
      'internalEventLayout': null,
      'eventPositionMode': 'translate',
      'eventScrollMode': 'move'
    };
  }
  changeEventLayout(c) {
    this.internalEventLayout = c;
    return this.internalEventLayout.type;
  }
  changeInternalEventLayout(c) {
    return this.getEventLayout(c);
  }
  updateInternalEventLayout(c, e) {
    const f = this;
    if (e) {
      f.element.classList.remove('b-eventlayout-' + e.type);
    }
    f.element.classList.add('b-eventlayout-' + c.type);
    if (c.type === 'layoutFn' && f.isVertical) {
      f.resourceColumns.fitWidth = f.resourceColumns.fillWidth = false;
    }
    if (!f.isConfiguring) {
      f.refreshWithTransition();
      f.trigger('stateChange');
    }
  }
  updateOverlappingEventSorter(c) {
    if (!this.isConfiguring) {
      this.refreshWithTransition();
    }
  }
  getEventLayout(c) {
    if (c == null ? void 0 : c.isModel) {
      c = c.eventLayout || this.internalEventLayout;
    }
    if (typeof c === 'string') {
      c = { 'type': c };
    }
    return c;
  }
  getEventLayoutHandler(c) {
    const e = this;
    if (!e.isHorizontal) {
      return null;
    }
    const {
        timeAxisViewModel: f,
        horizontal: g
      } = e, {type: i} = c;
    if (!e.layouts) {
      e.layouts = {};
    }
    switch (i) {
    case 'stack': {
        if (!e.layouts.horizontalStack) {
          e.layouts.horizontalStack = new e.horizontalLayoutStackClass(ObjectHelper.assign({
            'scheduler': e,
            'timeAxisViewModel': f,
            'bandIndexToPxConvertFn': g.layoutEventVerticallyStack,
            'bandIndexToPxConvertThisObj': g
          }, c));
        }
        return e.layouts.horizontalStack;
      }
    case 'pack': {
        if (!e.layouts.horizontalPack) {
          e.layouts.horizontalPack = new e.horizontalLayoutPackClass(ObjectHelper.assign({
            'scheduler': e,
            'timeAxisViewModel': f,
            'bandIndexToPxConvertFn': g.layoutEventVerticallyPack,
            'bandIndexToPxConvertThisObj': g
          }, c));
        }
        return e.layouts.horizontalPack;
      }
    default:
      return null;
    }
  }
  getEventsToRender(c, e) {
    return e;
  }
  repaintEventsForResource(c) {
    this.currentOrientation.repaintEventsForResource(c);
  }
  repaintEvent(c) {
    const e = this.eventStore.getResourcesForEvent(c);
    e.forEach(f => this.repaintEventsForResource(f));
  }
  getEventStyle(c, e) {
    return c.eventStyle || e.eventStyle || this.eventStyle;
  }
  getEventColor(c, e) {
    let f, g;
    return c.eventColor || ((f = c.event) == null ? void 0 : f.eventColor) || ((g = c.parent) == null ? void 0 : g.eventColor) || e.eventColor || this.eventColor;
  }
  generateRenderData(c, e, f = { 'viewport': true }) {
    let g, i;
    const j = this, k = j.currentOrientation.getTimeSpanRenderData(c, e, f), {isEvent: l} = c, {eventResize: n} = j.features, o = !c.meta.isDragCreating && c.isMilestone, p = l && c.assignments.find(r => r.resourceId === e.$originalId), q = {
        'className': 'b-sch-event-content',
        'role': 'presentation',
        'dataset': { 'taskBarFeature': 'content' }
      };
    if (k) {
      k.tabIndex = '0';
      let r = c.isResizable;
      if (n && r) {
        if (k.startsOutsideView) {
          if (r === true) {
            r = 'end';
          } else if (r === 'start') {
            r = false;
          }
        }
        if (k.endsOutsideView) {
          if (r === true) {
            r = 'start';
          } else if (r === 'end') {
            r = false;
          }
        }
        if (r) {
          if (j.isHorizontal) {
            if (!j.rtl && !n.leftHandle || j.rtl && !n.rightHandle) {
              r = r === 'start' ? false : 'end';
            } else if (!j.rtl && !n.rightHandle || j.rtl && !n.leftHandle) {
              r = r === 'end' ? false : 'start';
            }
          } else {
            if (!n.topHandle) {
              r = r === 'start' ? false : 'end';
            } else if (!n.bottomHandle) {
              r = r === 'end' ? false : 'start';
            }
          }
        }
      }
      const u = Boolean(c.hasPersistableChanges || (p == null ? void 0 : p.hasPersistableChanges)), v = {
          [e.cls]: e.cls,
          [j.generatedIdCls]: !c.isOccurrence && c.hasGeneratedId,
          [j.dirtyCls]: u,
          [j.committingCls]: c.isCommitting,
          [j.endsOutsideViewCls]: k.endsOutsideView,
          [j.startsOutsideViewCls]: k.startsOutsideView,
          'b-clipped-start': k.clippedStart,
          'b-clipped-end': k.clippedEnd,
          'b-iscreating': c.isCreating,
          'b-rtl': j.rtl
        }, w = {
          [j.eventCls + '-parent']: e.isParent,
          'b-readonly': c.readOnly || (p == null ? void 0 : p.readOnly),
          'b-linked-resource': e.isLinked,
          'b-original-resource': e.hasLinks
        }, z = c.isResourceTimeRange ? new DomClassList() : c.internalCls.clone(), A = c.isResourceTimeRange ? c.internalCls.clone() : new DomClassList();
      k.wrapperStyle = '';
      k.isWrap = true;
      if (l) {
        const F = p && j.isAssignmentSelected(p);
        ObjectHelper.assign(v, {
          [j.eventCls]: 1,
          'b-milestone': o,
          'b-sch-event-narrow': !o && k.width < j.narrowEventWidth,
          [j.fixedEventCls]: c.isDraggable === false,
          ['b-sch-event-resizable-' + r]: Boolean(n && !c.readOnly),
          [j.eventSelectedCls]: F,
          [j.eventAssignHighlightCls]: j.eventAssignHighlightCls && !F && j.isEventSelected(c),
          'b-recurring': c.isRecurring,
          'b-occurrence': c.isOccurrence,
          'b-inactive': c.inactive
        });
        k.eventId = c.id;
        const G = j.getEventStyle(c, e), H = j.getEventColor(c, e), I = j.isFirstRender && j.useInitialAnimation && globalThis.bryntum.noAnimations !== true;
        ObjectHelper.assign(w, {
          [j.eventCls + '-wrap']: 1,
          'b-milestone-wrap': o,
          [j.navigator.focusCls]: c.assignments.some(J => J === j.activeAssignment)
        });
        if (I) {
          const J = k.row ? k.row.index : (k.top - j.scrollTop) / j.tickSize, K = J / 20 * 1000;
          k.wrapperStyle = 'animation-delay: ' + K + 'ms;';
          j.maxDelay = Math.max(j.maxDelay || 0, K);
          if (!j.initialAnimationDetacher) {
            j.initialAnimationDetacher = EventHelper.on({
              'element': j.foregroundCanvas,
              'delegate': j.eventSelector,
              'once': true,
              'animationend': () => j.setTimeout({
                'fn': 'stopInitialAnimation',
                'delay': j.maxDelay,
                'cancelOutstanding': true
              }),
              'expires': {
                'alt': 'stopInitialAnimation',
                'delay': j.initialAnimationDuration + j.maxDelay
              },
              'thisObj': j
            });
          }
        }
        k.eventColor = H;
        k.eventStyle = G;
        k.assignmentRecord = k.assignment = p;
      }
      k.wrapperCls = ObjectHelper.assign(A, w);
      k.cls = ObjectHelper.assign(z, v);
      k.iconCls = new DomClassList(c.getValue(j.eventBarIconClsField) || c.iconCls);
      if (c.isResourceTimeRange) {
        k.style = '';
        k.wrapperStyle += c.style || '';
      } else {
        k.style = c.style || '';
      }
      k.resource = k.resourceRecord = e;
      k.resourceId = k.rowId;
      if (l) {
        let L = null, N = null, O;
        if (j.eventRenderer) {
          const P = j.eventRenderer.call(j.eventRendererThisObj || j, {
            'eventRecord': c,
            'resourceRecord': e,
            'assignmentRecord': k.assignmentRecord,
            'scheduler': j,
            'renderData': k
          });
          if (typeof k.cls === 'string') {
            k.cls = new DomClassList(k.cls);
          }
          if (typeof k.wrapperCls === 'string') {
            k.wrapperCls = new DomClassList(k.wrapperCls);
          }
          if (typeof k.iconCls === 'string') {
            k.iconCls = new DomClassList(k.iconCls);
          }
          O = P;
        } else if (j.eventBarTextField) {
          O = StringHelper.encodeHtml(c.getValue(j.eventBarTextField) || '');
        }
        q.children = [];
        if (o && (j.milestoneLayoutMode === 'default' || j.milestoneTextPosition === 'always-outside') && O != null && O !== '') {
          q.children.unshift(N = {
            'tag': 'label',
            'children': [],
            'dataset': { 'isMilestone': true }
          });
        }
        if ((g = k.iconCls) == null ? void 0 : g.length) {
          q.children.unshift({
            'tag': 'i',
            'className': k.iconCls
          });
        }
        if (Array.isArray(O)) {
          (N || q).children.push(...O);
        } else if (StringHelper.isHtml(O)) {
          if (q.children.length) {
            L = {
              'tag': 'span',
              'class': 'b-event-text-wrap',
              'html': O
            };
          } else {
            q.children = null;
            q.html = O;
          }
        } else if (typeof O === 'string' || typeof O === 'object') {
          L = O;
        } else if (O != null) {
          L = String(O);
        }
        if (L != null) {
          (N || q).children.push(L);
          k.cls.add('b-has-content');
        }
        if (q.html != null || q.children.length) {
          k.children.push(q);
        }
      }
      const {
        eventStyle: B,
        eventColor: C,
        wrapperCls: E
      } = k;
      E['b-sch-style-' + (B || 'none')] = 1;
      if (DomHelper.isNamedColor(C)) {
        E['b-sch-color-' + C] = C;
      } else if (C) {
        const Q = B ? 'color' : 'background-color', R = Q + ':' + C + ';';
        k.style = R + k.style;
        E['b-sch-custom-color'] = 1;
        k._customColorStyle = R;
      } else {
        E['b-sch-color-none'] = 1;
      }
      if (k.style && o && q) {
        q.style = k.style;
        delete k.style;
      }
      k.cls['b-sch-event-withicon'] = (i = k.iconCls) == null ? void 0 : i.length;
      k.eventContent = q;
      k.wrapperChildren = [];
      j.onEventDataGenerated(k);
    }
    return k;
  }
  onEventDataGenerated(c) {
  }
  changeUseInitialAnimation(c) {
    return c === true ? 'fade-in' : c;
  }
  updateUseInitialAnimation(c, e) {
    const {classList: f} = this.element;
    if (e) {
      f.remove('b-initial-' + e);
    }
    if (c) {
      f.add('b-initial-' + c);
      if (BrowserHelper.isFirefox) {
        f.add('b-prevent-event-transitions');
      }
    }
  }
  restartInitialAnimation(c) {
    let e;
    const f = this;
    (e = f.initialAnimationDetacher) == null ? void 0 : e.call(f);
    f.initialAnimationDetacher = null;
    f.useInitialAnimation = c;
    f.isFirstRender = true;
    f.refresh();
  }
  stopInitialAnimation() {
    const c = this;
    c.initialAnimationDetacher();
    c.isFirstRender = false;
    c.useInitialAnimation = false;
    if (BrowserHelper.isFirefox) {
      c.setTimeout(() => c.element.classList.remove('b-prevent-event-transitions'), 100);
    }
  }
  getMilestoneLabelWidth(c, e) {
    const f = this, g = f.milestoneLayoutMode, i = f.getResourceLayoutSettings(e).contentHeight;
    if (g === 'measure') {
      const j = StringHelper.encodeHtml(c.name), k = f.getEventColor(c, e), l = f.getEventStyle(c, e), n = f.milestoneMeasureElement || (f.milestoneMeasureElement = DomHelper.createElement({
          'className': {
            'b-sch-event-wrap': 1,
            'b-milestone-wrap': 1,
            'b-measure': 1,
            ['b-sch-color-' + k]: k,
            ['b-sch-style-' + l]: l
          },
          'children': [{
              'className': 'b-sch-event b-milestone',
              'children': [{
                  'className': 'b-sch-event-content',
                  'children': [{ 'tag': 'label' }]
                }]
            }],
          'parent': f.foregroundCanvas
        }));
      n.retainElement = true;
      n.style.fontSize = i + 'px';
      if (f.milestoneTextPosition === 'always-outside') {
        const o = n.firstElementChild.firstElementChild.firstElementChild;
        o.innerHTML = j;
        const p = Rectangle.from(o, o.parentElement);
        return p.left + p.width + 2;
      } else {
        n.firstElementChild.firstElementChild.innerHTML = '<label></label>' + j;
        return n.firstElementChild.offsetWidth;
      }
    }
    if (g === 'estimate') {
      return c.name.length * f.milestoneCharWidth + (f.milestoneTextPosition === 'always-outside' ? i : 0);
    }
    if (g === 'data') {
      return c.milestoneWidth + (f.milestoneTextPosition === 'always-outside' ? i : 0);
    }
    return 0;
  }
  updateMilestoneLayoutMode(c) {
    const e = this, f = e.milestoneTextPosition === 'always-outside';
    e.element.classList.toggle('b-sch-layout-milestones', c !== 'default' && !f);
    e.element.classList.toggle('b-sch-layout-milestone-labels', c !== 'default' && f);
    if (!e.isConfiguring) {
      e.refreshWithTransition();
    }
  }
  updateMilestoneTextPosition(c) {
    this.element.classList.toggle('b-sch-layout-milestone-text-position-inside', c === 'inside');
    this.updateMilestoneLayoutMode(this.milestoneLayoutMode);
  }
  updateMilestoneAlign() {
    if (!this.isConfiguring) {
      this.refreshWithTransition();
    }
  }
  updateMilestoneCharWidth() {
    if (!this.isConfiguring) {
      this.refreshWithTransition();
    }
  }
  get widgetClass() {
  }
};
const engineStoreNames = [
  'assignmentStore',
  'dependencyStore',
  'eventStore',
  'resourceStore'
];
const ProjectConsumer_default = a => {
  let b, c;
  return c = class extends (a || Base) {
    constructor() {
      super(...arguments);
      __privateAdd(this, b, void 0);
    }
    static get $name() {
      return 'ProjectConsumer';
    }
    static get declarable() {
      return ['projectStores'];
    }
    static get configurable() {
      return {
        'projectModelClass': ProjectModel,
        'project': {},
        'destroyStores': null,
        'projectSubscribers': []
      };
    }
    startConfigure(e) {
      this.getConfig('project');
      super.startConfigure(e);
    }
    changeProject(e, f) {
      const g = this, {projectDataNames: i} = g.constructor;
      g.projectCallbacks = new Set();
      if (e) {
        g.buildingProjectConfig = true;
        if (!e.isModel) {
          if (g.isConfiguring) {
            g._project = e;
            const {crudManager: l} = g;
            if (l) {
              this.updateStoresFromCrudManager();
            }
            g.getConfig('projectStores');
            for (const n of i) {
              g.getConfig(n);
            }
          }
          const {eventStore: j} = e;
          let {_sharedProject: k} = g;
          if (j && !j.isEventStoreMixin && j.autoLoad && !j.count) {
            j.autoLoad = false;
            g.delayAutoLoad = true;
          }
          if (k && engineStoreNames.some(o => e[o] && e[o] !== k[o])) {
            for (const o of engineStoreNames) {
              if (e[o] && e[o] === k[o]) {
                e[o] = e[o].chain();
              }
            }
            k = null;
          }
          e = k || new g.projectModelClass(e);
          delete g._project;
        }
        g.buildingProjectConfig = false;
      }
      return e;
    }
    updateStoresFromCrudManager() {
      const {crudManager: e} = this, {isCrudManager: f} = e;
      for (const g of this.constructor.projectStoreNames) {
        if (e[g]) {
          this[g] = e[g];
          if (!f) {
            delete e[g];
          }
        }
      }
    }
    updateProject(e, f) {
      let g;
      const i = this, {
          projectListeners: j,
          crudManager: k
        } = i;
      i.detachListeners('projectConsumer');
      i.detachListeners('projectDestroy');
      delete i._crudManager;
      if (e) {
        j.thisObj = i;
        e.ion(j);
        if (e.isCrudManager) {
          i.crudManager = e;
        } else if (k) {
          k.project = e;
          i.crudManager = k;
        }
        i.projectSubscribers.forEach(l => {
          l.detachFromProject(f);
          l.attachToProject(e);
        });
        for (const l of i.constructor.projectStoreNames) {
          i[l] = e[l];
        }
        if (i.delayAutoLoad) {
          e.eventStore.autoLoad = true;
          e.eventStore.load();
        }
        (g = e.stm) == null ? void 0 : g.ion({
          'name': 'projectConsumer',
          'restoringStart': 'onProjectRestoringStart',
          'restoringStop': 'onProjectRestoringStop',
          'thisObj': i
        });
        e.ion({
          'name': 'projectDestroy',
          'beforeDestroy': 'onProjectBeforeDestroy',
          'thisObj': i
        });
      }
      i.trigger('projectChange', { 'project': e });
    }
    changeCrudManager(e) {
      if (this.buildingProjectConfig) {
        this._crudManager = e.isCrudManager ? e : Object.assign({}, e);
      } else {
        return super.changeCrudManager(e);
      }
    }
    updateCrudManager(e) {
      if (!this.isConfiguring && e) {
        this.updateStoresFromCrudManager();
      }
    }
    onProjectBeforeDestroy(e) {
      const f = new e.source.constructor();
      this.project = f;
    }
    onProjectDataReady() {
      const e = this;
      e.whenVisible(() => {
        if (e.projectCallbacks.size) {
          e.projectCallbacks.forEach(f => f());
          e.projectCallbacks.clear();
        }
      }, null, null, 'onProjectDataReady');
    }
    onProjectRestoringStart({stm: e}) {
      const {rawQueue: f} = e;
      if (f.length && f[f.length - 1].length > 1) {
        __privateSet(this, b, true);
        this.suspendRefresh();
      }
    }
    onProjectRestoringStop() {
      if (__privateGet(this, b)) {
        __privateSet(this, b, false);
        this.resumeRefresh(true);
      }
    }
    onBeforeTimeZoneChange() {
    }
    onTimeZoneChange({
      timeZone: e,
      oldTimeZone: f
    }) {
      const g = this;
      if (g.startDate && g.timeAxis.timeZone !== e) {
        const i = f != null ? TimeZoneHelper.fromTimeZone(g.startDate, f) : g.startDate;
        g.startDate = e != null ? TimeZoneHelper.toTimeZone(i, e) : i;
        g.timeAxis.timeZone = e;
      }
    }
    onStartApplyChangeset() {
      this.suspendRefresh();
    }
    onEndApplyChangeset() {
      this.resumeRefresh(true);
    }
    whenProjectReady(e) {
      if (this.isEngineReady) {
        e();
      } else {
        this.projectCallbacks.add(e);
      }
    }
    get isEngineReady() {
      let e, f;
      return Boolean((f = (e = this.project).isEngineReady) == null ? void 0 : f.call(e));
    }
    doDestroy() {
      super.doDestroy();
      if (this.destroyStores) {
        !this.project.isDestroyed && this.project.destroy();
      }
    }
    get projectStores() {
      const {projectStoreNames: e} = this.constructor;
      return e.map(f => this[f]);
    }
    static get projectStoreNames() {
      return Object.keys(this.projectStores);
    }
    static get projectDataNames() {
      return this.projectStoreNames.reduce((e, f) => {
        const {dataName: g} = this.projectStores[f];
        if (g) {
          e.push(g);
        }
        return e;
      }, []);
    }
    static setupProjectStores(e, f) {
      const {projectStores: g} = e;
      if (g) {
        const i = {
            'name': 'projectConsumer',
            'dataReady': 'onProjectDataReady',
            'change': 'relayProjectDataChange',
            'beforeTimeZoneChange': 'onBeforeTimeZoneChange',
            'timeZoneChange': 'onTimeZoneChange',
            'startApplyChangeset': 'onStartApplyChangeset',
            'endApplyChangeset': 'onEndApplyChangeset',
            'startApplyingRevision': 'onStartApplyChangeset',
            'stopApplyingRevision': 'onEndApplyChangeset'
          }, j = { 'projectListeners': i };
        let k;
        for (const l in g) {
          const {dataName: n} = g[l];
          j[l] = j[n] = null;
          if (n) {
            Object.defineProperty(f.class.prototype, n, {
              'configurable': true,
              'get'() {
                let o;
                return (o = this.project[l]) == null ? void 0 : o.records;
              }
            });
            this.createDataUpdater(l, n, k, f);
          }
          this.createStoreDescriptor(f, l, g[l], i);
          k = n;
        }
        this.setupConfigs(f, j);
      }
    }
    static createDataUpdater(e, f, g, i) {
      i.class.prototype['update' + StringHelper.capitalize(f)] = function (j) {
        const {project: k} = this;
        g && this.getConfig(g);
        if (this.buildingProjectConfig) {
          k[f + 'Data'] = j;
        } else {
          k[e].data = j;
        }
      };
    }
    static createStoreDescriptor(e, f, {listeners: g}, i) {
      const {prototype: j} = e.class, k = StringHelper.capitalize(f);
      i[f + 'Change'] = function ({store: l}) {
        this[f] = l;
      };
      j['change' + k] = function (l, n) {
        const o = this, {project: p} = o, q = l == null ? void 0 : l.project;
        if (o.buildingProjectConfig) {
          if (q == null ? void 0 : q.isProjectModel) {
            o._sharedProject = q;
          }
          p[f] = l;
          return;
        }
        if (!o.initializingProject) {
          if (p[f] !== l) {
            p['set' + k](l);
            l = p[f];
          }
        }
        if (l !== n) {
          if (g) {
            g.thisObj = o;
            g.name = f + 'Listeners';
            g.startApplyChangeset = 'onProjectStoreStartApplyChangeset';
            g.endApplyChangeset = 'onProjectStoreEndApplyChangeset';
            o.detachListeners(g.name);
            l.ion(g);
          }
          o['_' + f] = l;
          o.projectSubscribers.forEach(r => {
            let u;
            (u = r['attachTo' + k]) == null ? void 0 : u.call(r, l);
          });
          o['_' + f] = null;
        }
        return l;
      };
    }
    onProjectStoreStartApplyChangeset() {
      this.suspendRefresh();
    }
    onProjectStoreEndApplyChangeset() {
      this.resumeRefresh(true);
    }
    relayProjectDataChange(e) {
      if ((e.isExpand || e.isCollapse) && !e.records[0].fieldMap.expanded.persist) {
        return;
      }
      return this.trigger('dataChange', {
        'project': e.source,
        ...e,
        'source': this
      });
    }
    get widgetClass() {
    }
  }, b = new WeakMap(), c;
};
const SchedulerStores_default = a => class b extends ProjectConsumer_default(a || Base) {
  static get $name() {
    return 'SchedulerStores';
  }
  static get projectStores() {
    return {
      'resourceStore': { 'dataName': 'resources' },
      'eventStore': {
        'dataName': 'events',
        'listeners': {
          'batchedUpdate': 'onEventStoreBatchedUpdate',
          'changePreCommit': 'onInternalEventStoreChange',
          'commitStart': 'onEventCommitStart',
          'commit': 'onEventCommit',
          'exception': 'onEventException',
          'idchange': 'onEventIdChange',
          'beforeLoad': 'onBeforeLoad'
        }
      },
      'assignmentStore': {
        'dataName': 'assignments',
        'listeners': {
          'changePreCommit': 'onAssignmentChange',
          'commitStart': 'onAssignmentCommitStart',
          'commit': 'onAssignmentCommit',
          'exception': 'onAssignmentException',
          'beforeRemove': {
            'fn': 'onAssignmentBeforeRemove',
            'prio': -1000
          }
        }
      },
      'dependencyStore': { 'dataName': 'dependencies' },
      'calendarManagerStore': {},
      'timeRangeStore': {},
      'resourceTimeRangeStore': {}
    };
  }
  static get configurable() {
    return {
      'store': null,
      'startParamName': 'startDate',
      'endParamName': 'endDate',
      'passStartEndParameters': true,
      'crudManagerClass': null,
      'crudManager': null
    };
  }
  updateProject(c, e) {
    super.updateProject(c, e);
    this.detachListeners('schedulerStores');
    c.ion({
      'name': 'schedulerStores',
      'refresh': 'onProjectRefresh',
      'thisObj': this
    });
    this.bindEventStoreForEarlyPositioning(c.eventStore);
  }
  onProjectRefresh({isInitialCommit: c}) {
    const e = this;
    if (e.isVisible) {
      if (c) {
        if (e.isVertical) {
          e.refreshAfterProjectRefresh = false;
          e.refreshWithTransition();
        }
      }
      if (e.refreshAfterProjectRefresh) {
        e.refreshWithTransition(false, !c);
        e.refreshAfterProjectRefresh = false;
      }
      if (e.navigateToAfterRefresh) {
        e.navigateTo(e.navigateToAfterRefresh, { 'scrollIntoView': false });
        e.navigateToAfterRefresh = null;
      }
    } else {
      e.whenVisible('refresh', e, [true]);
    }
  }
  changeCrudManager(c) {
    const e = this;
    if (c && !c.isCrudManager) {
      c = e.crudManagerClass.new({ 'scheduler': e }, c);
    }
    e._crudManager = c;
    e.bindCrudManager(c);
  }
  get store() {
    if (!this._store && this.isVertical) {
      this._store = new Store({
        'data': [{
            'id': 'verticalTimeAxisRow',
            'cls': 'b-verticaltimeaxis-row'
          }]
      });
    }
    return super.store;
  }
  set store(c) {
    super.store = c;
  }
  refreshFromRowOnStoreAdd(c, {
    isExpand: e,
    records: f
  }) {
    const g = arguments, i = this;
    i.runWithTransition(() => {
      i.currentOrientation.suspended = !e && !f.some(j => j.isLinked);
      i.element.classList.add('b-no-transitions');
      super.refreshFromRowOnStoreAdd(c, ...g);
      i.element.classList.remove('b-no-transitions');
      i.currentOrientation.suspended = false;
    }, !e);
  }
  onStoreAdd(c) {
    super.onStoreAdd(c);
    if (this.isPainted) {
      this.calculateRowHeights(c.records);
    }
  }
  onStoreUpdateRecord({
    source: c,
    record: e,
    changes: f
  }) {
    let g = 0;
    if ('assigned' in f) {
      g++;
    }
    if ('calendar' in f) {
      g++;
    }
    if (g !== Object.keys(f).length) {
      super.onStoreUpdateRecord(...arguments);
    }
  }
  updateResourceStore(c) {
    if (c && this.isHorizontal) {
      c.metaMapId = this.id;
      this.store = c;
    }
  }
  get usesDisplayStore() {
    return this.store !== this.resourceStore;
  }
  onEventIdChange(c) {
    let e, f;
    (f = (e = this.currentOrientation).onEventStoreIdChange) == null ? void 0 : f.call(e, c);
  }
  onEventStoreBatchedUpdate(c) {
    if (this.listenToBatchedUpdates) {
      return this.onInternalEventStoreChange(c);
    }
  }
  onInternalEventStoreChange(c) {
    if (!this.isPainted || !this._mode || c.isAssign || this.assignmentStore.isRemovingAssignment) {
      return;
    }
    if (this.isVisible) {
      this.currentOrientation.onEventStoreChange(c);
    } else {
      this.whenVisible(this.onInternalEventStoreChange, this, [c]);
    }
  }
  onEventCommit({changes: c}) {
    let e = [
      ...c.added,
      ...c.modified
    ].map(f => this.eventStore.getResourcesForEvent(f));
    e = Array.prototype.concat.apply([], e);
    new Set(e).forEach(f => this.repaintEventsForResource(f));
  }
  onEventCommitStart({changes: c}) {
    const {
      currentOrientation: e,
      committingCls: f
    } = this;
    [
      ...c.added,
      ...c.modified
    ].forEach(g => g.assignments.forEach(i => e.toggleCls(i, f, true)));
  }
  onEventException({action: c}) {
    if (c === 'commit') {
      const {changes: e} = this.eventStore;
      [
        ...e.added,
        ...e.modified,
        ...e.removed
      ].forEach(f => this.repaintEvent(f));
    }
  }
  onAssignmentCommit({changes: c}) {
    this.repaintEventsForAssignmentChanges(c);
  }
  onAssignmentCommitStart({changes: c}) {
    const {
      currentOrientation: e,
      committingCls: f
    } = this;
    [
      ...c.added,
      ...c.modified
    ].forEach(g => {
      e.toggleCls(g, f, true);
    });
  }
  onAssignmentException({action: c}) {
    if (c === 'commit') {
      this.repaintEventsForAssignmentChanges(this.assignmentStore.changes);
    }
  }
  repaintEventsForAssignmentChanges(c) {
    const e = [
      ...c.added,
      ...c.modified,
      ...c.removed
    ].map(f => f.getResource());
    new Set(e).forEach(f => this.repaintEventsForResource(f));
  }
  onAssignmentBeforeRemove({
    records: c,
    removingAll: e
  }) {
    if (e) {
      return;
    }
    const f = this, {activeAssignment: g} = f, i = c.includes(g);
    let j;
    if (!f.isConfiguring && (f.navigateToAfterRefresh || g && i)) {
      if (c.includes(f.navigateToAfterRefresh)) {
        f.navigateToAfterRefresh = null;
      }
      const k = f.getElementFromAssignmentRecord(g, true);
      if (i && k) {
        const l = f.timeAxisSubGrid.rectangle().intersect(f._bodyRectangle), n = Rectangle.from(k), o = n.center, p = f.isHorizontal ? 'y' : 'x', q = ({
            element: u,
            distance: v,
            center: w,
            edgeDistance: z
          }, {
            element: A,
            distance: B,
            center: C,
            edgeDistance: E
          }) => {
            if (u.dataset.resourceId !== A.dataset.resourceId) {
              if (Math.abs(w[p] - o[p]) < Math.abs(C[p] - o[p])) {
                return -1;
              }
              if (Math.abs(w[p] - o[p]) > Math.abs(C[p] - o[p])) {
                return 1;
              }
            }
            return Math.min(z, v) - Math.min(E, B) || C[p] - w[p];
          }, r = Array.from(f.foregroundCanvas.querySelectorAll(f.navigator.itemSelector)).reduce((u, v) => {
            if (v !== k) {
              const w = Rectangle.from(v).intersect(l), z = w && f.resolveAssignmentRecord(v);
              if (z) {
                const {center: A} = w, B = o.getDelta(A), C = A.y < o.y ? n.y - w.bottom : A.y > o.y ? w.y - n.bottom : 0, E = A.x < o.x ? n.x - w.right : A.x > o.x ? w.x - n.right : 0;
                u.push({
                  'element': v,
                  'assignment': z,
                  'center': A,
                  'distance': Math.sqrt(B[0] ** 2 + B[1] ** 2),
                  'edgeDistance': Math.max(C, E)
                });
              }
            }
            return u;
          }, []).sort(q)[0];
        if (r) {
          j = r.assignment;
        }
      }
      if (j) {
        f.navigateTo(j, { 'scrollIntoView': false });
        f.navigateToAfterRefresh = j;
      } else {
        f.focusElement.focus({ 'preventScroll': true });
      }
    }
  }
  set timeRanges(c) {
    this.project.timeRanges = c;
  }
  get timeRanges() {
    return this.project.timeRanges;
  }
  set resourceTimeRanges(c) {
    this.project.resourceTimeRanges = c;
  }
  get resourceTimeRanges() {
    return this.project.resourceTimeRanges;
  }
  onBeforeLoad({params: c}) {
    this.applyStartEndParameters(c);
  }
  getResourcesEventsPerTick(c, e) {
    const {
        timeAxis: f,
        resourceStore: g
      } = this, i = [];
    c = c || g.records;
    c.forEach(j => {
      j.events.forEach(k => {
        if (!f.isTimeSpanInAxis(k) || e && !e.call(this, {
            'resource': j,
            'event': k
          })) {
          return;
        }
        let l = Math.floor(f.getTickFromDate(k.startDate)), n = Math.ceil(f.getTickFromDate(k.endDate));
        if (l == -1) {
          l = 0;
        }
        if (n === -1) {
          n = f.ticks.length;
        }
        do {
          if (!i[l]) {
            i[l] = [k];
          } else {
            i[l].push(k);
          }
        } while (++l < n);
      });
    });
    return i;
  }
  get widgetClass() {
  }
};
const defaultScrollOptions5 = {
  'block': 'nearest',
  'edgeOffset': 20
};
const SchedulerScroll_default = a => {
  let b;
  return b = class extends (a || Base) {
    static get $name() {
      return 'SchedulerScroll';
    }
    async scrollEventIntoView(c, e = defaultScrollOptions5) {
      const f = this, g = c.resources || [c];
      if (g.length > 1) {
        throw new Error('scrollEventIntoView() is not applicable for events with multiple assignments, please use scrollResourceEventIntoView() instead.');
      }
      if (!g.length) {
        console.warn('You have asked to scroll to an event which is not assigned to a resource');
      }
      await f.scrollResourceEventIntoView(g[0], c, e);
    }
    scrollAssignmentIntoView(c, ...e) {
      return this.scrollResourceEventIntoView(c.resource, c.event, ...e);
    }
    async scrollResourceEventIntoView(c, e, f = defaultScrollOptions5) {
      let g, i;
      const j = this, {
          store: k,
          timeAxis: l,
          visibleDateRange: n,
          scrollExtensionThreshold: o
        } = j, p = l.endDate - l.startDate, q = e.startDate, r = e.endDate, u = e.isScheduled && q.getTime(), v = e.isScheduled && r.getTime(), w = e.isScheduled && q < j.timeAxis.startDate | (r > j.timeAxis.endDate) << 1;
      let z = false;
      let A;
      if (f.edgeOffset == null) {
        f.edgeOffset = 20;
      }
      if (w && f.extendTimeAxis !== false) {
        if (w === 3) {
          await j.setTimeSpan(new Date(u - p / 2), new Date(v + p / 2));
        } else if (j.infiniteScroll) {
          const B = {
            'start': l.startDate.getTime(),
            'end': l.endDate.getTime()
          };
          const C = o ? new Duration(o).milliseconds : p;
          if (w === 1) {
            B.start -= C;
          } else {
            B.end += C;
          }
          const E = u >= B.start && v <= B.end;
          if (E) {
            await j.setTimeSpan(new Date(B.start), new Date(B.end), { 'visibleDate': n.startDate });
            z = true;
          } else {
            const F = n.endMS - n.startMS, G = w & 1 ? 1 : -1, H = G === 1 ? new Date(v + F) : new Date(u - F);
            await j.setTimeSpan(new Date(u - p / 2), new Date(u + p / 2), { 'visibleDate': H });
          }
        } else {
          const I = j.presets.allRecords[j.zoomLevel], J = f.edgeOffset * j.getMilliSecondsPerPixelForZoomLevel(I, false);
          if (w & 1) {
            await j.setTimeSpan(new Date(u - J), new Date(u - J + p));
          } else {
            await j.setTimeSpan(new Date(v + J - p), new Date(v + J));
          }
        }
      }
      if (j.isDestroyed) {
        return;
      }
      if (k.tree) {
        await ((g = j.expandTo) == null ? void 0 : g.call(j, c));
      }
      if (k.isGrouped) {
        await k.expand(k.getGroupHeaderForRecord(c));
      }
      if (((i = j.features.nestedEvents) == null ? void 0 : i.enabled) && e.parent && !e.parent.isRoot) {
        await j.scrollEventIntoView(e.parent);
      }
      A = j.getElementFromEventRecord(e, c);
      if (A) {
        if (!DomHelper.isFocusable(A)) {
          A = A.parentNode;
        }
        const K = j.timeAxisSubGrid.scrollable;
        await K.scrollIntoView(A, f);
        if (j.isDestroyed) {
          return;
        }
        let L;
        do {
          L = j.getElementFromEventRecord(e, c);
          if (!L) {
            await AsyncHelper.animationFrame();
          }
          if (j.isDestroyed) {
            return;
          }
        } while (!L);
      } else if (w === 3 && f.extendTimeAxis === false) {
        console.warn('You have asked to scroll to an event which is outside the current view and extending timeaxis is disabled');
      } else if (!e.isOccurrence && j.eventStore.isFilteredOut(e)) {
        console.warn('You have asked to scroll to an event which is not available');
      } else if (e.isScheduled) {
        await j.scrollUnrenderedEventIntoView(c, e, f);
      } else {
        await j.scrollResourceIntoView(c, f);
      }
      if (z) {
        if (w === 1) {
          await j.setTimeSpan(l.startDate, new Date(l.endDate.getTime() - p), { 'maintainVisibleStart': true });
        } else {
          await j.setTimeSpan(new Date(l.startDate.getTime() + p), l.endDate, { 'maintainVisibleStart': true });
        }
      }
    }
    scrollUnrenderedEventIntoView(c, e, f = defaultScrollOptions5) {
      return new Promise(async g => {
        const i = this, j = i.timeAxisSubGrid.scrollable, k = j.viewport, {rowManager: l} = i, n = j.y;
        if (!k) {
          g();
          return;
        }
        const o = Object.assign({}, defaultScrollOptions5);
        let p, q, r = 0;
        do {
          if (++r >= 50) {
            throw new Error('Too many preparational scrolls during \'scrollIntoView\' for event id = ' + e.id);
          }
          const v = i.getResourceEventBox(e, c);
          if (!v) {
            g();
            return;
          }
          v.x = Math.ceil(v.x);
          v.y = Math.ceil(v.y);
          if (i.rtl) {
            v._x = -(v.x - k.width);
          }
          v.translate(k.x - j.scrollLeft, k.y - j.y);
          if (q === void 0) {
            q = j.getDeltaTo(v, o);
          }
          const w = j.scrollIntoView(v, f);
          await w;
          if (w.cancelled || i.isDestroyed) {
            g();
            return true;
          }
          await AsyncHelper.animationFrame();
          if (i.isDestroyed) {
            g();
            return true;
          }
          p = i.getElementFromEventRecord(e, c);
        } while (!p);
        j.suspendEvents();
        if (q.yDelta >= 0) {
          j.y = Math.max(l.topRow.top - j.viewport.height, n);
        } else {
          j.y = Math.min(l.bottomRow.bottom, n);
        }
        i.fixElementHeights();
        j.resumeEvents();
        const u = j.scrollIntoView(p, o);
        await u;
        if (u.canceled || i.isDestroyed) {
          g();
          return true;
        }
        await AsyncHelper.animationFrame();
        g();
      });
    }
    scrollResourceIntoView(c, e = defaultScrollOptions5) {
      if (this.isVertical) {
        return this.currentOrientation.scrollResourceIntoView(c, e);
      }
      return this.scrollRowIntoView(c, e);
    }
    get widgetClass() {
    }
  }, __publicField(b, 'configurable', { 'scrollExtensionThreshold': null }), b;
};
const SchedulerRegions_default = a => class b extends (a || Base) {
  static get $name() {
    return 'SchedulerRegions';
  }
  getScheduleRegion(c, e, f = true, g) {
    return this.currentOrientation.getScheduleRegion(...arguments);
  }
  getResourceRegion(c, e, f) {
    return this.currentOrientation.getRowRegion(...arguments);
  }
  getAssignmentEventBox(c, e) {
    return this.getResourceEventBox(c.event, c.resource, e);
  }
  getResourceEventBox(c, e, f = false, g = false) {
    return this.currentOrientation.getResourceEventBox(...arguments);
  }
  getItemBox(c, e = false) {
    return c.resources.map(f => this.getResourceEventBox(c, f, e));
  }
  get widgetClass() {
  }
};
const copyProperties2 = [
  'startDate',
  'endDate',
  'eventLayout',
  'barMargin',
  'mode',
  'tickSize',
  'eventColor',
  'eventStyle',
  'fillTicks'
];
const SchedulerState_default = a => {
  let b;
  return b = class extends (a || Base) {
    getState() {
      return ObjectHelper.copyProperties(super.getState(), this, copyProperties2);
    }
    applyState(c) {
      let e;
      c = Object.assign({}, c);
      this.suspendRefresh();
      if ((c == null ? void 0 : c.eventLayout) === 'layoutFn') {
        delete c.eventLayout;
      }
      if ((e = c == null ? void 0 : c.zoomLevelOptions) == null ? void 0 : e.width) {
        delete c.tickSize;
      }
      super.applyState(c);
      this.resumeRefresh(true, false);
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'SchedulerState'), __publicField(b, 'configurable', { 'statefulEvents': ['dateRangeChange'] }), b;
};
const EventSelection_default = a => class b extends (a || Base) {
  static get $name() {
    return 'EventSelection';
  }
  static get configurable() {
    return {
      'highlightPredecessors': false,
      'highlightSuccessors': false,
      'deselectOnClick': false,
      'deselectAllOnScheduleClick': true,
      'selectResourceOnEventNavigate': true,
      'selectResourceOnScheduleClick': true,
      'selectedCollection': {}
    };
  }
  static get defaultConfig() {
    return {
      'multiEventSelect': false,
      'eventSelectionDisabled': false,
      'eventSelectedCls': 'b-sch-event-selected',
      'triggerSelectionChangeOnRemove': false,
      'maintainSelectionOnDatasetChange': true,
      'eventAssignHighlightCls': 'b-sch-event-assign-selected'
    };
  }
  afterConstruct() {
    let c;
    super.afterConstruct();
    (c = this.navigator) == null ? void 0 : c.ion({
      'navigate': 'onEventNavigate',
      'thisObj': this
    });
  }
  changeSelectedCollection(c) {
    if (!c.isCollection) {
      c = new Collection(c);
    }
    return c;
  }
  updateSelectedCollection(c) {
    const e = this;
    if (!c.owner) {
      c.owner = e;
    }
    c.ion({
      'change': (...f) => e.project.deferUntilRepopulationIfNeeded('onSelectedCollectionChange', (...g) => !e.isDestroying && e.onSelectedCollectionChange(...g), f),
      'beforeSplice': 'onBeforeSelectedCollectionSplice',
      'thisObj': e
    });
  }
  get selectedCollection() {
    return this._selectedCollection;
  }
  getActionType(c, e, f, g) {
    if (g && c.length > 0 && f.length === c.length && e.length === 0 || !g && c.length === 0 && f.length > 0) {
      return 'clear';
    }
    return e.length > 0 && f.length > 0 ? 'update' : e.length > 0 ? 'select' : 'deselect';
  }
  getEventsFromAssignments(c) {
    return ArrayHelper.unique(c.map(e => e.event));
  }
  get selectedEvents() {
    return this.getEventsFromAssignments(this.selectedCollection.values);
  }
  set selectedEvents(c) {
    const e = [];
    c = ArrayHelper.asArray(c);
    c == null ? void 0 : c.forEach(f => {
      if (this.isEventSelectable(f) !== false) {
        if (f.isOccurrence) {
          f.assignments.forEach(g => {
            e.push(this.assignmentStore.getOccurrence(g, f));
          });
        } else {
          e.push(...f.assignments);
        }
      }
    });
    this.selectedCollection.splice(0, this.selectedCollection.count, e);
  }
  get selectedAssignments() {
    return this.selectedCollection.values;
  }
  set selectedAssignments(c) {
    this.selectedCollection.splice(0, this.selectedCollection.count, c || []);
  }
  isEventSelected(c) {
    const {selectedCollection: e} = this;
    return Boolean(e.count && e.includes(c.assignments));
  }
  isEventSelectable(c) {
  }
  isAssignmentSelected(c) {
    return this.selectedCollection.includes(c);
  }
  select(c, e = false) {
    if (c.isAssignment) {
      this.selectAssignment(c, e);
    } else {
      this.selectEvent(c, e);
    }
  }
  selectEvent(c, e = false) {
    if (!this.isEventSelected(c)) {
      this.selectEvents([c], e);
    }
  }
  selectAssignment(c, e = false, f) {
    if (!this.isAssignmentSelected(c)) {
      e ? this.selectedCollection.add(c) : this.selectedAssignments = c;
    }
  }
  deselect(c) {
    if (c.isAssignment) {
      this.deselectAssignment(c);
    } else {
      this.deselectEvent(c);
    }
  }
  deselectEvent(c) {
    if (this.isEventSelected(c)) {
      this.selectedCollection.remove(...c.assignments);
    }
  }
  deselectAssignment(c) {
    if (this.isAssignmentSelected(c)) {
      this.selectedCollection.remove(c);
    }
  }
  selectEvents(c, e = false) {
    if (e) {
      const f = c.reduce((g, i) => {
        if (this.isEventSelectable(i) !== false) {
          g.push(...i.assignments);
        }
        return g;
      }, []);
      this.selectedCollection.add(f);
    } else {
      this.selectedEvents = c;
    }
  }
  deselectEvents(c) {
    this.selectedCollection.remove(c.reduce((e, f) => {
      e.push(...f.assignments);
      return e;
    }, []));
  }
  selectAssignments(c) {
    this.selectedCollection.add(c);
  }
  deselectAssignments(c) {
    this.selectedCollection.remove(c);
  }
  clearEventSelection() {
    this.selectedAssignments = [];
  }
  onBeforeSelectedCollectionSplice({
    toAdd: c,
    toRemove: e,
    index: f
  }) {
    const g = this, i = g._selectedCollection.values, j = c, k = e > 0 ? i.slice(f, e + f) : [], l = g.getActionType(i, j, k, true);
    if (c.length === 0 && e === 0) {
      return;
    }
    if (g.trigger('beforeEventSelectionChange', {
        'action': l,
        'selection': g.getEventsFromAssignments(i) || [],
        'selected': g.getEventsFromAssignments(j) || [],
        'deselected': g.getEventsFromAssignments(k) || []
      }) === false) {
      return false;
    }
    if (g.trigger('beforeAssignmentSelectionChange', {
        'action': l,
        'selection': i,
        'selected': j,
        'deselected': k
      }) === false) {
      return false;
    }
  }
  onSelectedCollectionChange({
    added: c,
    removed: e
  }) {
    const f = this, g = f.selectedAssignments, i = c || [], j = e || [];
    function k(l, n) {
      const o = l.event;
      if (o) {
        const p = false, q = true, {eventAssignHighlightCls: r} = f, u = f.getElementFromAssignmentRecord(l, p, q);
        f.currentOrientation.toggleCls(l, f.eventSelectedCls, n, p, q);
        r && f.getElementsFromEventRecord(o, null, p, q).forEach(v => {
          if (v !== u) {
            const w = f.resolveAssignmentRecord(v, q);
            f.currentOrientation.toggleCls(w, r, n, p, q);
            if (n && !v.parentElement.isReleased) {
              v.style.animation = 'none';
              v.offsetHeight;
              v.style.animation = '';
            }
            v.classList.toggle(r, n);
          }
        });
      }
    }
    j.forEach(l => k(l, false));
    i.forEach(l => k(l, true));
    if (f.highlightSuccessors || f.highlightPredecessors) {
      f.highlightLinkedEvents(f.selectedEvents);
    }
    f.$selectedAssignments = g.map(l => ({
      'eventId': l.eventId,
      'resourceId': l.resourceId
    }));
    if (!f.silent) {
      const l = this.getActionType(g, i, j);
      f.trigger('assignmentSelectionChange', {
        'action': l,
        'selection': g,
        'selected': i,
        'deselected': j
      });
      f.trigger('eventSelectionChange', {
        'action': l,
        'selection': f.selectedEvents,
        'selected': f.getEventsFromAssignments(i),
        'deselected': f.getEventsFromAssignments(j)
      });
    }
  }
  onAssignmentChange(c) {
    super.onAssignmentChange(c);
    const e = this, {
        action: f,
        records: g
      } = c;
    e.silent = !e.triggerSelectionChangeOnRemove;
    if (f === 'remove') {
      e.deselectAssignments(g);
    } else if (f === 'removeall' && !e.eventStore.isSettingData) {
      e.clearEventSelection();
    } else if (f === 'dataset' && e.$selectedAssignments) {
      if (!e.maintainSelectionOnDatasetChange) {
        e.clearEventSelection();
      } else {
        const i = e.$selectedAssignments.map(j => g.find(k => k.eventId === j.eventId && k.resourceId === j.resourceId));
        e.selectedAssignments = ArrayHelper.clean(i);
      }
    }
    e.silent = false;
  }
  onInternalEventStoreChange({
    source: c,
    action: e,
    records: f
  }) {
    if (!c.isResourceTimeRangeStore && e === 'dataset' && !f.length) {
      this.clearEventSelection();
    }
    super.onInternalEventStoreChange(...arguments);
  }
  onAssignmentSelectionClick(c, e) {
    const f = this;
    if (f.isAssignmentSelected(e)) {
      if (f.deselectOnClick || c.ctrlKey) {
        f.deselectAssignment(e, f.multiEventSelect, c);
      }
    } else if (this.isEventSelectable(e.event) !== false) {
      f.selectAssignment(e, c.ctrlKey && f.multiEventSelect, c);
    }
  }
  onEventNavigate({
    event: c,
    item: e
  }) {
    if (!this.eventSelectionDisabled) {
      const f = e && (e.nodeType === Element.ELEMENT_NODE ? this.resolveAssignmentRecord(e) : e);
      if (f) {
        this.onAssignmentSelectionClick(c, f);
      } else if (this.deselectAllOnScheduleClick) {
        this.clearEventSelection();
      }
    }
  }
  changeHighlightSuccessors(c) {
    return this.changeLinkedEvents(c);
  }
  changeHighlightPredecessors(c) {
    return this.changeLinkedEvents(c);
  }
  changeLinkedEvents(c) {
    const e = this;
    if (c) {
      e.highlighted = e.highlighted || new Set();
      e.highlightLinkedEvents(e.selectedEvents);
    } else if (e.highlighted) {
      e.highlightLinkedEvents();
    }
    return c;
  }
  highlightLinkedEvents(c = []) {
    const e = this, {
        highlighted: f,
        eventStore: g
      } = e, i = e.features.dependencies;
    f.forEach(j => {
      if (!c.includes(j)) {
        j.meta.highlight = false;
        f.delete(j);
        if (g.includes(j)) {
          j.dependencies.forEach(k => i.unhighlight(k, 'b-highlight'));
        }
      }
    });
    c.forEach(j => {
      const k = [j];
      while (k.length) {
        const l = k.pop();
        f.add(l);
        if (e.highlightSuccessors) {
          l.outgoingDeps.forEach(n => {
            i.highlight(n, 'b-highlight');
            !f.has(n.toEvent) && k.push(n.toEvent);
          });
        }
        if (e.highlightPredecessors) {
          l.incomingDeps.forEach(n => {
            i.highlight(n, 'b-highlight');
            !f.has(n.fromEvent) && k.push(n.fromEvent);
          });
        }
      }
      f.forEach(n => n.meta.highlight = true);
    });
    e.element.classList.toggle('b-highlighting', c.length > 0);
    e.refreshWithTransition();
  }
  onEventDataGenerated(c) {
    if (this.highlightSuccessors || this.highlightPredecessors) {
      c.cls['b-highlight'] = c.eventRecord.meta.highlight;
    }
    super.onEventDataGenerated(c);
  }
  updateProject(c, e) {
    this.clearEventSelection();
    super.updateProject(c, e);
  }
  doDestroy() {
    let c;
    super.doDestroy();
    ((c = this._selectedCollection) == null ? void 0 : c.owner) === this && this._selectedCollection.destroy();
  }
  get widgetClass() {
  }
};
const preventDefault2 = a => a.preventDefault();
const isArrowKey = {
  'ArrowRight': 1,
  'ArrowLeft': 1,
  'ArrowUp': 1,
  'ArrowDown': 1
};
const animate100 = { 'animate': 100 };
const emptyObject12 = Object.freeze({});
const EventNavigation_default = a => class b extends Delayable_default(a || Base) {
  static get $name() {
    return 'EventNavigation';
  }
  static get configurable() {
    return {
      'navigator': {
        'allowCtrlKey': true,
        'scrollSilently': true,
        'keys': {
          'Space': 'onEventSpaceKey',
          'Enter': 'onEventEnterKey',
          'Delete': 'onDeleteKey',
          'Backspace': 'onDeleteKey',
          'ArrowUp': 'onArrowUpKey',
          'ArrowDown': 'onArrowDownKey',
          'Escape': 'onEscapeKey',
          'Tab': 'onTab',
          'SHIFT+Tab': 'onShiftTab'
        }
      },
      'isNavigationKey': {
        'ArrowDown': 1,
        'ArrowUp': 1,
        'ArrowLeft': 1,
        'ArrowRight': 1
      }
    };
  }
  static get defaultConfig() {
    return {
      'focusCls': 'b-active',
      'enableDeleteKey': true,
      'onDeleteKeyBuffer': 500,
      'navigatePreviousBuffer': 200,
      'navigateNextBuffer': 200,
      'testConfig': { 'onDeleteKeyBuffer': 1 }
    };
  }
  construct(c) {
    const e = this;
    super.construct(c);
    e.isInTimeAxis = e.isInTimeAxis.bind(e);
    e.onDeleteKey = e.throttle(e.onDeleteKey, e.onDeleteKeyBuffer, e);
  }
  changeNavigator(c) {
    const e = this;
    e.getConfig('subGridConfigs');
    return new Navigator(e.constructor.mergeConfigs({
      'ownerCmp': e,
      'target': e.timeAxisSubGridElement,
      'processEvent': e.processEvent,
      'itemSelector': '.' + e.eventCls + '-wrap',
      'focusCls': e.focusCls,
      'navigatePrevious': e.throttle(e.navigatePrevious, {
        'delay': e.navigatePreviousBuffer,
        'throttled': preventDefault2
      }),
      'navigateNext': e.throttle(e.navigateNext, {
        'delay': e.navigateNextBuffer,
        'throttled': preventDefault2
      })
    }, c));
  }
  doDestroy() {
    this.navigator.destroy();
    super.doDestroy();
  }
  isInTimeAxis(c) {
    return !c.instanceMeta(this).excluded && this.timeAxis.isTimeSpanInAxis(c);
  }
  onElementKeyDown(c) {
    let e, f, g;
    const i = this, {navigator: j} = i;
    if (((e = i.focusedCell) == null ? void 0 : e.rowIndex) !== -1 && ((f = i.focusedCell) == null ? void 0 : f.column) === i.timeAxisColumn && !c.target.closest(j.itemSelector) && c.key === 'Enter') {
      const k = i.getFirstVisibleAssignment();
      if (k) {
        i.navigateTo(k, { 'uiEvent': c });
        return false;
      }
    } else {
      (g = super.onElementKeyDown) == null ? void 0 : g.call(this, c);
    }
  }
  getFirstVisibleAssignment(c = this.focusedCell) {
    let e, f, g;
    const i = this, {
        currentOrientation: j,
        rowManager: k,
        eventStore: l
      } = i;
    if (i.isHorizontal) {
      let n = j.rowMap.get(k.getRow(c.rowIndex));
      if (n == null ? void 0 : n.length) {
        return (e = n[0]) == null ? void 0 : e.elementData.assignmentRecord;
      } else {
        n = (f = j.resourceMap.get(c.id)) == null ? void 0 : f.eventsData;
        if (n == null ? void 0 : n.length) {
          return (g = n.filter(o => l.isAvailable(o.eventRecord))[0]) == null ? void 0 : g.assignmentRecord;
        }
      }
    } else {
      const o = [...j.resourceMap.values()][0], p = o && Object.values(o);
      if (p == null ? void 0 : p.length) {
        return p.filter(q => l.isAvailable(q.renderData.eventRecord))[0].renderData.assignmentRecord;
      }
    }
  }
  onGridBodyFocusIn(c) {
    let e;
    const f = c.target.closest(this.focusableSelector);
    if (this.timeAxisSubGridElement.contains(c.target)) {
      const g = this, {navigationEvent: i} = g, {target: j} = c, k = j.closest(g.navigator.itemSelector), l = k ? g.normalizeCellContext({
          'rowIndex': g.isVertical ? 0 : g.resourceStore.indexOf(g.resolveResourceRecord(j)),
          'column': g.timeAxisColumn,
          'target': j
        }) : new GridLocation(j);
      if (k) {
        const {_focusedCell: n} = g;
        g._focusedCell = l;
        (e = g.onCellNavigate) == null ? void 0 : e.call(g, g, n, l, i, true);
        return;
      }
      if (f && (!i || isArrowKey[i.key])) {
        const o = g.getFirstVisibleAssignment(l);
        if (o) {
          g.navigateTo(o, {
            'scrollIntoView': Boolean(i && i.type !== 'mousedown'),
            'uiEvent': i || c
          });
          return;
        }
      }
    }
    if (f) {
      super.onGridBodyFocusIn(c);
    }
  }
  accessibleFocusCell(c, e) {
    const f = this;
    c = f.normalizeCellContext(c);
    if (c.columnId !== f.timeAxisColumn.id) {
      return super.focusCell(c, e);
    }
  }
  normalizeTarget(c) {
    return c.assignmentRecord;
  }
  getPrevious(c, e) {
    const f = this, {
        resourceStore: g,
        visibleDateRange: i
      } = f, {eventSorter: j} = f.currentOrientation, {
        startDate: k,
        endDate: l
      } = f.timeAxis, n = c.event, o = f.eventStore.getEvents({
        'resourceRecord': c.resource,
        'startDate': k,
        'endDate': l,
        'visibleDateRange': i
      }).filter(this.isInTimeAxis).sort(j);
    let p = c.resource, q = o[o.indexOf(n) - 1];
    if (!q) {
      for (let r = g.indexOf(p) - 1; (!q || e && q === n) && r >= 0; r--) {
        p = g.getAt(r);
        const u = f.eventStore.getEvents({
          'resourceRecord': p,
          'startDate': k,
          'endDate': l
        }).filter(f.isInTimeAxis).sort(j);
        q = u.length && u[u.length - 1];
      }
    }
    return f.assignmentStore.getAssignmentForEventAndResource(q, p);
  }
  navigatePrevious(c) {
    const e = this, f = e.getPrevious(e.normalizeTarget(c));
    c.preventDefault();
    if (f) {
      if (!c.ctrlKey) {
        e.clearEventSelection();
      }
      return e.navigateTo(f, { 'uiEvent': c });
    }
    return e.doGridNavigation(c);
  }
  getNext(c, e) {
    const f = this, {
        resourceStore: g,
        visibleDateRange: i
      } = f, {eventSorter: j} = f.currentOrientation, {
        startDate: k,
        endDate: l
      } = f.timeAxis, n = c.event, o = f.eventStore.getEvents({
        'resourceRecord': c.resource,
        'startDate': k,
        'endDate': l,
        'visibleDateRange': i
      }).filter(this.isInTimeAxis).sort(j);
    let p = c.resource, q = o[o.indexOf(n) + 1];
    if (!q) {
      for (let r = g.indexOf(p) + 1; (!q || e && q === n) && r < g.count; r++) {
        p = g.getAt(r);
        const u = f.eventStore.getEvents({
          'resourceRecord': p,
          'startDate': k,
          'endDate': l
        }).filter(f.isInTimeAxis).sort(j);
        q = u[0];
      }
    }
    return f.assignmentStore.getAssignmentForEventAndResource(q, p);
  }
  navigateNext(c) {
    const e = this, f = e.getNext(e.normalizeTarget(c));
    c.preventDefault();
    if (f) {
      if (!c.ctrlKey) {
        e.clearEventSelection();
      }
      return e.navigateTo(f, { 'uiEvent': c });
    }
    return e.doGridNavigation(c);
  }
  doGridNavigation(c) {
    if (!c.handled && c.key.indexOf('Arrow') === 0) {
      this['navigate' + c.key.substring(5) + 'ByKey'](c);
    }
  }
  async navigateTo(c, {
    scrollIntoView: scrollIntoView = true,
    uiEvent: uiEvent = {}
  } = emptyObject12) {
    const e = this, {navigator: f} = e, {skipScrollIntoView: g} = f;
    if (c) {
      if (scrollIntoView) {
        f.disabled = true;
        await e.scrollAssignmentIntoView(c, {
          'extendTimeAxis': e.infiniteScroll,
          ...animate100
        });
        f.disabled = false;
      } else {
        f.skipScrollIntoView = true;
      }
      if (!e.isDestroyed && this.getElementFromAssignmentRecord(c)) {
        e.activeAssignment = c;
        f.skipScrollIntoView = g;
        f.trigger('navigate', {
          'event': uiEvent,
          'item': e.getElementFromAssignmentRecord(c).closest(f.itemSelector)
        });
      }
    }
  }
  set activeAssignment(c) {
    const e = this.getElementFromAssignmentRecord(c, true);
    if (e) {
      this.navigator.activeItem = e;
    }
  }
  get activeAssignment() {
    const {activeItem: c} = this.navigator;
    if (c) {
      return this.resolveAssignmentRecord(c);
    }
  }
  get previousActiveEvent() {
    const {previousActiveItem: c} = this.navigator;
    if (c) {
      return this.resolveEventRecord(c);
    }
  }
  processEvent(c) {
    const e = this, f = c.target.closest(e.eventSelector);
    if (!e.navigator.disabled && f) {
      c.assignmentRecord = e.resolveAssignmentRecord(f);
      c.eventRecord = e.resolveEventRecord(f);
      c.resourceRecord = e.resolveResourceRecord(f);
    }
    return c;
  }
  onDeleteKey(c) {
    const e = this;
    if (!e.readOnly && e.enableDeleteKey) {
      const f = e.eventStore.usesSingleAssignment ? e.selectedEvents : e.selectedAssignments;
      e.removeEvents(f.filter(g => !g.readOnly));
    }
  }
  onArrowUpKey(c) {
    this.focusCell({
      'rowIndex': this.focusedCell.rowIndex - 1,
      'column': this.timeAxisColumn
    });
    c.handled = true;
  }
  onArrowDownKey(c) {
    if (this.focusedCell.rowIndex < this.resourceStore.count - 1) {
      this.focusCell({
        'rowIndex': this.focusedCell.rowIndex + 1,
        'column': this.timeAxisColumn
      });
      c.handled = true;
    }
  }
  onEscapeKey(c) {
    if (!c.target.closest('.b-dragging')) {
      this.focusCell({
        'rowIndex': this.focusedCell.rowIndex,
        'column': this.timeAxisColumn
      });
      c.handled = true;
    }
  }
  onEventSpaceKey(c) {
  }
  onEventEnterKey(c) {
  }
  get isActionableLocation() {
    if (!this.navigator.activeItem) {
      return super.isActionableLocation;
    }
  }
  get widgetClass() {
  }
};
const TransactionalFeatureMixin_default = a => {
  let b;
  return b = class extends a {
    static get $name() {
      return 'TransactionalFeatureMixin';
    }
    get widgetClass() {
    }
    get transactionalFeaturesEnabled() {
      return this.enableTransactionalFeatures && this.project && !!this.project._queue;
    }
    get isTransactionalFeatureActive() {
      return this._featureTransactionId != null;
    }
    construct(...c) {
      super.construct(...c);
      this.ion({
        'beforeFeatureTransactionStart': ({id: e}) => this._featureTransactionId = e,
        'featureTransactionComplete': () => delete this._featureTransactionId
      });
    }
    afterFeatureTransaction(c) {
      if (!this.transactionalFeaturesEnabled) {
        return;
      }
      if (this.isTransactionalFeatureActive) {
        this.once('featureTransactionComplete', c);
      } else {
        c.call(this);
      }
    }
  }, __publicField(b, 'configurable', {
    'enableTransactionalFeatures': false,
    'testConfig': { 'enableTransactionalFeatures': false }
  }), b;
};
const CrudManagerView_default = a => {
  let b;
  return b = class extends a.mixin(LoadMaskable_default) {
    static get $name() {
      return 'CrudManagerView';
    }
    afterConstruct() {
      super.afterConstruct();
      const {
        crudManager: c,
        project: e
      } = this;
      if (this.loadMask && (c || e).isCrudManagerLoading) {
        this.onCrudManagerLoadStart();
      }
    }
    applySyncMask() {
      const {syncMask: c} = this;
      if (c) {
        this.masked = Mask.mergeConfigs(this.loadMaskDefaults, c);
      }
    }
    bindCrudManager(c) {
      this.detachListeners('crudManager');
      let e = {};
      if (!this.isTaskBoardBase) {
        e = {
          'beforeApplyResponse': 'onCrudManagerBeforeApplyResponse',
          'applyResponse': 'onCrudManagerApplyResponse',
          'beforeLoadCrudManagerData': 'onCrudManagerBeforeLoadCrudManagerData',
          'loadCrudManagerData': 'onCrudManagerLoadCrudManagerData'
        };
      }
      c == null ? void 0 : c.ion({
        'name': 'crudManager',
        'loadStart': 'onCrudManagerLoadStart',
        'beforeSend': 'onCrudManagerBeforeSend',
        'load': 'onCrudManagerLoad',
        'loadCanceled': 'onCrudManagerLoadCanceled',
        'syncStart': 'onCrudManagerSyncStart',
        'sync': 'onCrudManagerSync',
        'syncCanceled': 'onCrudManagerSyncCanceled',
        'requestFail': 'onCrudManagerRequestFail',
        'responseReceived': 'onAjaxTransportResponseReceived',
        ...e,
        'thisObj': this
      });
    }
    onCrudManagerBeforeSend({params: c}) {
      let e;
      (e = this.applyStartEndParameters) == null ? void 0 : e.call(this, c);
    }
    onCrudManagerLoadStart() {
      let c;
      this.applyLoadMask();
      (c = this.toggleEmptyText) == null ? void 0 : c.call(this);
    }
    onCrudManagerSyncStart() {
      this.applySyncMask();
    }
    onCrudManagerBeforeApplyResponse() {
      this.suspendRefresh();
    }
    onCrudManagerApplyResponse() {
      this.resumeRefresh(true);
    }
    onCrudManagerBeforeLoadCrudManagerData() {
      if (!this.crudManager.applyingLoadResponse) {
        this.suspendRefresh();
      }
    }
    async onCrudManagerLoadCrudManagerData() {
      if (!this.crudManager.applyingLoadResponse) {
        await this.project.commitAsync();
        !this.isDestroyed && this.resumeRefresh(true);
      }
    }
    onCrudManagerRequestFinalize(c = true, e, f) {
      let g;
      const i = this;
      if (c) {
        (g = i.toggleEmptyText) == null ? void 0 : g.call(i);
      } else {
        if (!i.masked) {
          i.applyLoadMask();
        }
        i.applyMaskError('<div class="b-grid-load-failure">\n                    <div class="b-grid-load-fail">' + i.L('L{GridBase.' + e + 'FailedMessage}') + '</div>\n                    ' + (f && f.message ? '<div class="b-grid-load-fail">' + i.L('L{CrudManagerView.serverResponseLabel}') + ' ' + f.message + '</div>' : '') + '\n                </div>');
      }
    }
    onCrudManagerLoadCanceled() {
      this.onCrudManagerRequestFinalize(true, 'load');
    }
    onCrudManagerSyncCanceled() {
      this.onCrudManagerRequestFinalize(true, 'sync');
    }
    onCrudManagerLoad() {
      this.onCrudManagerRequestFinalize(true, 'load');
    }
    onCrudManagerSync() {
      this.onCrudManagerRequestFinalize(true, 'sync');
    }
    onCrudManagerRequestFail({
      requestType: c,
      response: e
    }) {
      this.onCrudManagerRequestFinalize(false, c, e);
    }
    onAjaxTransportResponseReceived() {
      const c = this;
      if (c.clearMaskDelay != null) {
        c.setTimeout(() => c.masked = null, c.clearMaskDelay);
      } else {
        c.masked = null;
      }
    }
    get widgetClass() {
    }
  }, __publicField(b, 'config', {
    'clearMaskDelay': null,
    'testConfig': { 'clearMaskDelay': 0 }
  }), b;
};
const LazyLoadView_default = a => {
  let b;
  return b = class extends a {
    construct(c) {
      let e;
      const f = this;
      f.$lazyLoadDetachers = new Map();
      f.$lazyLoadCount = 0;
      super.construct(c);
      if ((e = f.crudManager) == null ? void 0 : e.lazyLoad) {
        f.$lazyLoadDetachers.set(f, FunctionHelper.before(f.resourceStore.lazyLoad, 'doLazyLoad', g => {
          const i = f.eventStore.lazyLoad.calculateDateRange({
            'startDate': f.startDate,
            'endDate': f.endDate,
            'visibleStart': f.visibleDateRange.startDate,
            'visibleEnd': f.visibleDateRange.endDate
          });
          Object.assign(g, i);
        }));
        f.passStartEndParameters = false;
      }
    }
    updateEventStore(c, e) {
      let f;
      (f = super.updateEventStore) == null ? void 0 : f.call(this, c, e);
      this.lazyLoadAttachStore(c, e);
      if (c.lazyLoad) {
        this.passStartEndParameters = false;
      }
    }
    updateResourceTimeRangeStore(c, e) {
      let f;
      (f = super.updateResourceTimeRangeStore) == null ? void 0 : f.call(this, c, e);
      this.lazyLoadAttachStore(c, e);
    }
    updateAssignmentStore(c, e) {
      let f;
      (f = super.updateAssignmentStore) == null ? void 0 : f.call(this, c, e);
      this.lazyLoadAttachStore(c, e);
    }
    lazyLoadAttachStore(c, e) {
      let f, g;
      const i = this, {$lazyLoadDetachers: j} = i;
      if (e) {
        (f = j.get(e)) == null ? void 0 : f();
        j.delete(e);
        if (e.lazyLoad.beforeLoadCall) {
          e.lazyLoad.beforeLoadCall = null;
        }
      }
      if (c == null ? void 0 : c.lazyLoad) {
        (g = j.get(c)) == null ? void 0 : g();
        j.set(c, c.ion({
          'lazyLoadStarted': 'storeLazyLoadStarted',
          'lazyLoadEnded': 'storeLazyLoadEnded',
          'thisObj': i
        }));
        if (c.lazyLoad.isDateStoreLazyLoadPlugin) {
          c.lazyLoad.beforeLoadCall = k => {
            let l, n;
            Object.assign(k, {
              'startDate': i.startDate,
              'endDate': i.endDate,
              'visibleStart': i.visibleDateRange.startDate,
              'visibleEnd': i.visibleDateRange.endDate,
              'resourceRecord': (n = (l = i.firstVisibleRow) == null ? void 0 : l.cellContext) == null ? void 0 : n.record
            });
          };
        }
      }
    }
    set $lazyLoadCount(c) {
      if (this.$_lazyLoadCount === 0 && c > 0) {
        this.lazyLoadShowLoadingIndicator();
      } else if (this.$_lazyLoadCount > 0 && c === 0) {
        this.lazyLoadHideLoadingIndicator();
      }
      this.$_lazyLoadCount = c;
    }
    get $lazyLoadCount() {
      return this.$_lazyLoadCount;
    }
    storeLazyLoadStarted() {
      this.$lazyLoadCount += 1;
    }
    storeLazyLoadEnded() {
      this.$lazyLoadCount -= 1;
    }
    lazyLoadShowLoadingIndicator() {
      const c = this;
      if (!c.$lazyLoadingIndicatorElement) {
        c.$lazyLoadingIndicatorElement = DomHelper.createElement({
          'parent': c.timeAxisSubGrid.header.element,
          'retainElement': true,
          'className': 'b-timeline-loading-indicator-wrap',
          'children': [{ 'className': 'b-timeline-loading-indicator' }]
        });
      } else {
        c.timeAxisSubGrid.header.element.appendChild(c.$lazyLoadingIndicatorElement);
      }
      c.readOnly = true;
    }
    lazyLoadHideLoadingIndicator() {
      let c;
      (c = this.$lazyLoadingIndicatorElement) == null ? void 0 : c.remove();
      this.readOnly = false;
    }
    doDestroy() {
      let c;
      (c = this.$lazyLoadDetachers) == null ? void 0 : c.forEach(e => e());
      super.doDestroy();
    }
  }, __publicField(b, '$name', 'LazyLoadView'), b;
};
const AttachToProjectMixin_default = a => class b extends a {
  static get $name() {
    return 'AttachToProjectMixin';
  }
  async afterConstruct() {
    let c;
    super.afterConstruct();
    const e = this, f = e.client || e.grid, {project: g} = f;
    (c = f.projectSubscribers) == null ? void 0 : c.push(e);
    if (g) {
      e.attachToProject(g);
      e.attachToResourceStore(g.resourceStore);
      e.attachToEventStore(g.eventStore);
      e.attachToAssignmentStore(g.assignmentStore);
      e.attachToDependencyStore(g.dependencyStore);
      e.attachToCalendarManagerStore(g.calendarManagerStore);
    }
  }
  attachToProject(c) {
    let e;
    this.detachListeners('project');
    this._project = c;
    (e = super.attachToProject) == null ? void 0 : e.call(this, c);
  }
  detachFromProject(c) {
    let e;
    (e = super.detachFromProject) == null ? void 0 : e.call(this, c);
  }
  attachToEventStore(c) {
    let e;
    this.detachListeners('eventStore');
    (e = super.attachToEventStore) == null ? void 0 : e.call(this, c);
  }
  attachToResourceStore(c) {
    let e;
    this.detachListeners('resourceStore');
    (e = super.attachToResourceStore) == null ? void 0 : e.call(this, c);
  }
  attachToAssignmentStore(c) {
    let e;
    this.detachListeners('assignmentStore');
    (e = super.attachToAssignmentStore) == null ? void 0 : e.call(this, c);
  }
  attachToDependencyStore(c) {
    let e;
    this.detachListeners('dependencyStore');
    (e = super.attachToDependencyStore) == null ? void 0 : e.call(this, c);
  }
  attachToCalendarManagerStore(c) {
    let e;
    this.detachListeners('calendarManagerStore');
    (e = super.attachToCalendarManagerStore) == null ? void 0 : e.call(this, c);
  }
  get project() {
    return this._project;
  }
  get calendarManagerStore() {
    return this.project.calendarManagerStore;
  }
  get assignmentStore() {
    return this.project.assignmentStore;
  }
  get resourceStore() {
    return this.project.resourceStore;
  }
  get eventStore() {
    return this.project.eventStore;
  }
  get dependencyStore() {
    return this.project.dependencyStore;
  }
};
const releaseEventActions = {
  'releaseElement': 1,
  'reuseElement': 1
};
const renderEventActions = {
  'newElement': 1,
  'reuseOwnElement': 1,
  'reuseElement': 1
};
const MAX_WIDTH = 9999999;
const heightEventSorter = ({startDateMS: a}, {startDateMS: b}) => a - b;
const chronoFields = {
  'startDate': 1,
  'endDate': 1,
  'duration': 1
};
function getStartEnd(a, b, c, e, f) {
  let g, i;
  const {timeAxis: j} = a, k = b.isBatchUpdating && !f ? b.get(e) : b[e], l = (g = b.hasBatchedChange) == null ? void 0 : g.call(b, e), n = a.fillTicks && (!((i = b.meta) == null ? void 0 : i.isResizing) || !l);
  if (n) {
    let o = j.getTickFromDate(k);
    if (o >= 0) {
      if (c && o === Math.round(o) && o > 0) {
        o--;
      }
      const p = Math.floor(o), q = j.getAt(p);
      return q[e].getTime();
    }
  }
  return k == null ? void 0 : k.getTime();
}
const HorizontalRendering = class extends Base.mixin(AttachToProjectMixin_default) {
  static $name = 'HorizontalRendering';
  static get configurable() {
    return {
      'bufferSize': 150,
      'verticalBufferSize': 150,
      'maxZIndexStacks': 50
    };
  }
  static get properties() {
    return {
      'resourceMap': new Map(),
      'rowMap': new Map(),
      'eventConfigs': [],
      'entityNames': new Set(),
      'isFirstRefresh': true,
      'toDrawOnProjectRefresh': new Set(),
      'toDrawOnDataReady': new Set()
    };
  }
  construct(a) {
    const b = this;
    b.client = b.scheduler = a;
    b.eventSorter = b.eventSorter.bind(a);
    b.scrollBuffer = a.scrollBuffer;
    a.scrollable.ion({
      'scroll': 'onEarlyScroll',
      'prio': 1,
      'thisObj': b
    });
    a.rowManager.ion({
      'name': 'rowManager',
      'renderDone': 'onRenderDone',
      'removeRows': 'onRemoveRows',
      'translateRow': 'onTranslateRow',
      'offsetRows': 'onOffsetRows',
      'beforeRowHeight': 'onBeforeRowHeightChange',
      'thisObj': b
    });
    super.construct({});
  }
  init() {
  }
  updateVerticalBufferSize() {
    const {rowManager: a} = this.scheduler;
    if (this.scheduler.isPainted) {
      a.renderRows(a.rows);
    }
  }
  get visibleDateRange() {
    return this._visibleDateRange;
  }
  getDateFromXY(a, b, c, e = false) {
    const {scheduler: f} = this;
    let g = a[0];
    if (!c) {
      g = this.translateToScheduleCoordinate(g);
    }
    g = f.getRtlX(g);
    return f.timeAxisViewModel.getDateFromPosition(g, b, e);
  }
  translateToScheduleCoordinate(a) {
    const {scheduler: b} = this, {scrollable: c} = b.timeAxisSubGrid;
    let e = a - b.timeAxisSubGridElement.getBoundingClientRect().left - globalThis.scrollX;
    if (b.rtl) {
      e += c.maxX - Math.abs(b.scrollLeft);
    } else {
      e += b.scrollLeft;
    }
    return e;
  }
  translateToPageCoordinate(a) {
    const {scheduler: b} = this, {scrollable: c} = b.timeAxisSubGrid;
    let e = a + b.timeAxisSubGridElement.getBoundingClientRect().left;
    if (b.rtl) {
      e -= c.maxX - Math.abs(b.scrollLeft);
    } else {
      e -= b.scrollLeft;
    }
    return e;
  }
  getScheduleRegion(a, b, c = true, e, f = false) {
    let g;
    const i = this, {scheduler: j} = i, {
        timeAxisSubGridElement: k,
        timeAxis: l
      } = j, {
        start: n,
        end: o
      } = (!f || a) && j.getResourceMarginObject(a) || {
        'start': 0,
        'end': 0
      };
    let p;
    if (a) {
      const z = j.getRowById(a.id), A = b && j.getElementsFromEventRecord(b, a)[0];
      p = z ? Rectangle.from(z.getElement('normal'), k) : j.getRecordCoords(a, true);
      if (A) {
        const B = Rectangle.from(A, k);
        p.y = B.y;
        p.bottom = B.bottom;
      } else {
        p.y = p.y + n;
        p.bottom = p.bottom - o;
      }
    } else {
      p = Rectangle.from(k).moveTo(null, 0);
      p.width = k.scrollWidth;
      p.y = p.y + n;
      p.bottom = p.bottom - o;
    }
    const q = l.startDate, r = l.endDate;
    e = (e == null ? void 0 : e.start) && e || ((g = j.getDateConstraints) == null ? void 0 : g.call(j, a, b)) || {
      'start': q,
      'end': r
    };
    const u = e.end <= q || e.start >= r;
    let v = u ? -1 : j.getVirtualCoordFromDate(e.start ? DateHelper.max(q, e.start) : q), w = u ? -1 : j.getVirtualCoordFromDate(e.end ? DateHelper.min(r, e.end) : r);
    if (!c) {
      v = i.translateToPageCoordinate(v);
      w = i.translateToPageCoordinate(w);
    }
    p.left = Math.min(v, w);
    p.right = Math.max(v, w);
    return p;
  }
  getRowRegion(a, b, c) {
    const {scheduler: e} = this, {timeAxis: f} = e, g = e.getRowById(a.id);
    if (!g) {
      return null;
    }
    const i = f.startDate, j = f.endDate, k = b ? DateHelper.max(i, b) : i, l = c ? DateHelper.min(j, c) : j, n = e.getCoordinateFromDate(k), o = e.getCoordinateFromDate(l, true, true), p = g.top, q = Math.min(n, o), r = p + g.offsetHeight;
    return new Rectangle(q, p, Math.max(n, o) - q, r - p);
  }
  getResourceEventBox(a, b, c, e = false) {
    let f;
    const {scheduler: g} = this, i = this.resourceMap.get(b.id);
    let j = null, k = false;
    if (i) {
      j = i.eventsData.find(l => l.eventRecord === a);
    }
    if (!j) {
      j = this.getTimeSpanRenderData(a, b, {
        'viewport': true,
        'timeAxis': c
      });
      k = true;
    }
    if (j) {
      const l = (f = j.height) != null ? f : g.getResourceLayoutSettings(b).contentHeight, n = g.rowManager.getRecordCoords(b, true, e), o = j.top + n.top, p = new Rectangle(j.left, o, j.width, l);
      p.layout = !k;
      p.rowTop = n.top;
      p.rowBottom = n.bottom;
      p.resourceId = b.id;
      return p;
    }
    return null;
  }
  resolveRowRecord(a) {
    const b = this, {scheduler: c} = b, e = a.nodeType ? a : a.target, f = e.nodeType === Element.TEXT_NODE ? e.parentElement : e, g = f.closest('[data-resource-id]');
    if (g) {
      return b.resourceStore.getById(g.dataset.resourceId);
    }
    return c.getRecordFromElement(f);
  }
  attachToProject(a) {
    super.attachToProject(a);
    this.refreshAllWhenReady = true;
    if (!this.scheduler.isConfiguring) {
      this.clearAll({ 'clearDom': true });
    }
    a == null ? void 0 : a.ion({
      'name': 'project',
      'refresh': 'onProjectRefresh',
      'commitFinalized': 'onProjectCommitFinalized',
      'thisObj': this
    });
  }
  onProjectCommitFinalized() {
    const {
      scheduler: a,
      toDrawOnDataReady: b,
      project: c
    } = this;
    if (a.isVisible) {
      if (a.isPainted && !a.refreshSuspended) {
        if (!b.size && c.timeZone != null && c.ignoreRecordChanges) {
          c.resourceStore.forEach(e => b.add(e.id));
        }
        if (b.size) {
          this.clearResources(b);
          this.refreshResources(b);
        }
        b.clear();
      }
    } else {
      a.whenVisible('refreshRows');
    }
  }
  onProjectRefresh({
    isCalculated: a,
    isInitialCommit: b
  }) {
    const c = this, {
        scheduler: e,
        toDrawOnProjectRefresh: f
      } = c;
    if (e.isVisible) {
      if (e.isPainted && !e.isConfiguring && !e.refreshSuspended) {
        if (c.refreshAllWhenReady || b && a) {
          e.calculateAllRowHeights(true);
          const {rowManager: g} = e;
          if (g.topRow) {
            c.clearAll();
            if (!e.refreshAfterProjectRefresh) {
              if (g.topRow.dataIndex >= e.store.count) {
                e.renderRows(false);
              } else {
                e.refreshWithTransition(false, !c.isFirstRefresh && a && !b);
              }
            }
            c.isFirstRefresh = false;
          } else {
            g.reinitialize();
          }
          c.refreshAllWhenReady = false;
        } else if (f.size) {
          c.refreshResources(f);
        }
        f.clear();
      }
    } else {
      e.whenVisible('refresh', e, [true]);
    }
  }
  attachToAssignmentStore(a) {
    this.refreshAllWhenReady = true;
    super.attachToAssignmentStore(a);
    if (a) {
      a.ion({
        'name': 'assignmentStore',
        'changePreCommit': 'onAssignmentStoreChange',
        'refreshPreCommit': 'onAssignmentStoreRefresh',
        'thisObj': this
      });
    }
  }
  onAssignmentStoreChange({
    source: a,
    action: b,
    records: c = [],
    replaced: e,
    changes: f
  }) {
    const g = this, {scheduler: i} = g, j = new Set(c.flatMap(k => {
        let l, n, o;
        return [
          k.resourceId,
          ...(o = (n = (l = k.resource) == null ? void 0 : l.$links) == null ? void 0 : n.map(p => p.id)) != null ? o : []
        ];
      }));
    if (g.resourceStore.isRemoving || g.resourceStore.isChangingId) {
      return;
    }
    switch (b) {
    case 'dataset': {
        if (!g.eventStore.usesSingleAssignment) {
          if (j.size) {
            g.refreshResourcesWhenReady(j);
          } else {
            g.clearAll();
            i.refreshWithTransition();
          }
        }
        return;
      }
    case 'add':
    case 'remove':
      g.refreshResourcesWhenReady(j);
      return;
    case 'removeall':
      g.refreshAllWhenReady = true;
      return;
    case 'replace':
      e.forEach(([k, l]) => {
        j.add(k.resourceId);
        j.add(l.resourceId);
      });
      g.refreshResourcesWhenReady(j);
      return;
    case 'filter':
      g.clearAll();
      i.calculateAllRowHeights(true);
      i.refreshWithTransition();
      return;
    case 'update': {
        if ('eventId' in f || 'resourceId' in f || 'id' in f) {
          if ('resourceId' in f) {
            j.add(f.resourceId.oldValue);
          }
          if (a === i.project.assignmentStore) {
            g.refreshResourcesOnDataReady(j);
          } else {
            g.refreshResources(j);
          }
        }
        break;
      }
    case 'clearchanges': {
        const {
          added: k,
          modified: l,
          removed: n
        } = f;
        if (l.length) {
          i.refreshWithTransition();
        } else {
          k.forEach(o => j.add(o.resourceId));
          n.forEach(o => j.add(o.resourceId));
          g.refreshResourcesOnDataReady(j);
        }
      }
    }
  }
  onAssignmentStoreRefresh({
    action: a,
    records: b
  }) {
    if (a === 'batch') {
      this.clearAll();
      this.scheduler.refreshWithTransition();
    }
  }
  attachToEventStore(a) {
    this.refreshAllWhenReady = true;
    super.attachToEventStore(a);
    if (a) {
      a.ion({
        'name': 'eventStore',
        'addConfirmed': 'onEventStoreAddConfirmed',
        'refreshPreCommit': 'onEventStoreRefresh',
        'thisObj': this
      });
    }
  }
  onEventStoreAddConfirmed({record: a}) {
    for (const b of this.client.getElementsFromEventRecord(a)) {
      b.classList.remove('b-iscreating');
    }
  }
  onEventStoreRefresh({action: a}) {
    if (a === 'batch') {
      const {scheduler: b} = this;
      if (b.isEngineReady && b.isPainted) {
        this.clearAll();
        b.refreshWithTransition();
      }
    }
  }
  onEventStoreChange({
    action: a,
    records: b = [],
    record: c,
    replaced: e,
    changes: f,
    source: g
  }) {
    let i, j, k, l;
    const n = this, {scheduler: o} = n, p = g.isResourceTimeRangeStore, q = new Set();
    if (!o.isPainted) {
      return;
    }
    b.forEach(r => {
      let u;
      const v = (u = r.$linkedResources) == null ? void 0 : u.filter(w => n.resourceStore.includes(w));
      v == null ? void 0 : v.forEach(w => q.add(w.id));
    });
    if (p) {
      switch (a) {
      case 'removeall':
      case 'dataset':
        n.clearAll();
        o.refreshWithTransition();
        return;
      }
      n.refreshResources(q, a !== 'lazyload');
    } else {
      switch (a) {
      case 'batch':
      case 'sort':
      case 'group':
      case 'move':
        return;
      case 'remove':
        return;
      case 'clearchanges':
        n.clearAll();
        o.refreshWithTransition();
        return;
      case 'dataset': {
          n.clearAll();
          if (o.isEngineReady) {
            o.refreshWithTransition();
          } else {
            n.refreshAllWhenReady = true;
          }
          return;
        }
      case 'add':
        break;
      case 'replace':
        e.forEach(([, r]) => {
          r.resources.map(u => q.add(u.id));
        });
        break;
      case 'removeall':
      case 'filter':
        if (!o.isEngineReady) {
          n.refreshAllWhenReady = true;
          return;
        }
        n.clearAll();
        o.calculateAllRowHeights(true);
        o.refreshWithTransition();
        return;
      case 'update': {
          const r = c.$entity ? !Object.keys(f).some(v => !c.$entity.getField(v)) : !Object.keys(f).some(v => !chronoFields[v]);
          let u = 0;
          'startDate' in f && u++;
          'endDate' in f && u++;
          'duration' in f && u++;
          if ('resourceId' in f) {
            q.add(f.resourceId.oldValue);
          }
          if (q.size && (!r || u && !('duration' in f && u === 1) || 'percentDone' in f || 'inactive' in f || 'constraintDate' in f || 'constraintType' in f || 'segments' in f)) {
            const {
                eventResize: v,
                eventDragCreate: w
              } = o.features, z = v == null ? void 0 : v.isResizing, A = w == null ? void 0 : w.isResizing, {store: B} = o, C = c.isBatchUpdating && [...q].every(E => {
                const F = B.getById(E), G = o.getEventLayout(F);
                return !o.getEventLayoutHandler(G);
              });
            if (c.isBatchUpdating && (z && v.lockLayout || A && w.lockLayout || (z || A) && C)) {
              const E = z && v.lockLayout === 'minimal-updates' || A && w.lockLayout === 'minimal-updates';
              for (const F of q) {
                const G = o.store.getById(F);
                if (G) {
                  if (E) {
                    const H = (i = n.resourceMap.get(F)) == null ? void 0 : i.eventsData.find(K => K.eventRecord === c), I = o.getRowFor(F), J = (j = n.rowMap.get(I)) == null ? void 0 : j.find(K => K.elementData.eventRecord === c);
                    if (H && J) {
                      const K = o.generateRenderData(c, B.getById(F), true);
                      Object.assign(H, K);
                      const L = this.renderEvent(H);
                      Object.assign(J, L);
                    }
                  } else {
                    n.refreshEventsForResource(G, void 0, false);
                  }
                }
              }
              n.onRenderDone();
            } else if (((k = n.project) == null ? void 0 : k.propagatingLoadChanges) || ((l = n.project) == null ? void 0 : l.isWritingData)) {
              n.refreshResourcesOnDataReady(q);
            } else {
              n.refreshResources(q);
            }
          }
          return;
        }
      }
      n.refreshResourcesWhenReady(q);
    }
  }
  attachToResourceStore(a) {
    this.refreshAllWhenReady = true;
    super.attachToResourceStore(a);
    if (a) {
      this.clearAll({ 'clearLayoutCache': true });
      a.ion({
        'name': 'resourceStore',
        'changePreCommit': 'onResourceStoreChange',
        'thisObj': this
      });
    }
  }
  get resourceStore() {
    return this.client.store;
  }
  onResourceStoreChange({
    action: a,
    isExpand: b,
    records: c,
    changes: e
  }) {
    const f = this, g = c == null ? void 0 : c.flatMap(i => i.isLinked ? [
        i.id,
        i.$originalId
      ] : [i.id]);
    if (!f.scheduler.isPainted) {
      return;
    }
    switch (a) {
    case 'add':
      if (!b) {
        if (c.every(i => i.isLinked)) {
          f.refreshResources(g);
        } else {
          f.refreshResourcesWhenReady(g);
        }
      }
      return;
    case 'update': {
        if (!f.project.isChangeTrackingSuspended && !e.isLeaf) {
          f.refreshResources(g);
        }
        return;
      }
    case 'filter':
      return;
    case 'removeall':
      f.clearAll({ 'clearLayoutCache': true });
      return;
    case 'dataset':
      return;
    }
    g && f.clearResources(g);
  }
  onTranslateRow({row: a}) {
    if (a.id != null) {
      this.refreshEventsForResource(a, false);
    }
  }
  onOffsetRows() {
    this.clearAll();
    this.doUpdateTimeView();
  }
  calculateRowHeight(a) {
    let b, c, e;
    const {scheduler: f} = this, g = f.getResourceHeight(a), i = f.getEventLayout(a), j = i.type, k = (b = f.features.eventResize) == null ? void 0 : b.lockLayout, l = (c = f.features.eventDragCreate) == null ? void 0 : c.lockLayout;
    if (j === 'stack' && f.isEngineReady && !a.isSpecialRow && ((e = a.assigned) == null ? void 0 : e.size) > 1) {
      const {
          assignmentStore: n,
          eventStore: o,
          timeAxis: p
        } = f, {
          barMargin: q,
          resourceMarginObject: {total: r},
          contentHeight: u
        } = f.getResourceLayoutSettings(a), v = (o.isFiltered || n.isFiltered) && (B => B.assignments.some(C => C.resource === a.$original && n.includes(C))), w = o.getEvents({
          'resourceRecord': a,
          'includeOccurrences': f.enableRecurringEvents,
          'startDate': p.startDate,
          'endDate': p.endDate,
          'filter': v,
          'fromRowHeight': true
        }).sort(heightEventSorter).map(B => {
          let C, E;
          const F = B.isBatchUpdating && (!(k && ((C = B.meta) == null ? void 0 : C.isResizing)) && !(l && ((E = B.meta) == null ? void 0 : E.isCreating))), G = F ? B.get('startDate') : B.startDate, H = F ? B.get('endDate') : B.endDate || G;
          return {
            'eventRecord': B,
            'resourceRecord': a,
            'startMS': G.getTime(),
            'endMS': H.getTime()
          };
        }), z = f.getEventLayoutHandler(i), A = z.layoutEventsInBands(w, a, true);
      if (z.type === 'layoutFn') {
        return A;
      }
      return A * u + (A - 1) * q + r;
    }
    return g;
  }
  doUpdateTimeView() {
    const {scrollable: a} = this.scheduler.timeAxisSubGrid;
    this.updateFromHorizontalScroll(a.x, true);
  }
  onTimeAxisViewModelUpdate() {
    const a = this, {scheduler: b} = a;
    a.clearAll();
    if (b.refreshSuspended) {
      a.detachListeners('renderingSuspend');
      b.ion({
        'name': 'renderingSuspend',
        'resumeRefresh'({trigger: c}) {
          if (b.isEngineReady && c) {
            a.doUpdateTimeView();
          }
        },
        'thisObj': a,
        'once': true
      });
    }
    a.doUpdateTimeView();
  }
  getConnectorStartSide(a) {
    return 'start';
  }
  getConnectorEndSide(a) {
    return 'end';
  }
  refreshRows(a, b) {
    if (a) {
      this.clearResources(a);
    } else if (b) {
      this.clearAll();
    }
  }
  onLocaleChange() {
    this.clearAll();
  }
  onViewportResize(a, b, c, e) {
    if (b > e) {
      this.onRenderDone();
    }
  }
  onDragAbort({
    context: a,
    dragData: b
  }) {
    if (this.resourceStore.indexOf(b.record.resource) < this.scheduler.topRow.dataIndex) {
      a.element.remove();
    }
  }
  toggleCls(a, b, c = true, e = false, f = false) {
    const g = this.client.getElementFromAssignmentRecord(a, e, f), i = this.resourceMap.get(a.isModel ? a.get('resourceId') : a.resourceId), j = i == null ? void 0 : i.eventsData.find(k => k.eventId === a.eventId);
    if (j) {
      j[e ? 'wrapperCls' : 'cls'][b] = c;
    }
    if (g) {
      g.classList.toggle(b, c);
      g.lastDomConfig.className[b] = c;
    }
  }
  onRemoveRows({rows: a}) {
    a.forEach(b => this.rowMap.delete(b));
    this.onRenderDone();
  }
  onEarlyScroll() {
    this.rendererCalled = false;
  }
  updateFromVerticalScroll() {
    this.fromScroll = true;
    if (!this.rendererCalled) {
      this.onRenderDone();
    }
  }
  updateFromHorizontalScroll(a, b) {
    let c, e;
    const f = this, {
        scheduler: g,
        scrollBuffer: i
      } = f, j = i === -1, {
        timeAxisSubGrid: k,
        timeAxis: l
      } = g, {width: n} = k, {totalSize: o} = g.timeAxisViewModel, p = a, q = Math.abs(k.scrollable.maxX) > 1 && Math.abs(k.scrollable.maxX) <= Math.round(p) + 5, r = j ? l.startDate : g.getDateFromVirtualCoord({ 'coord': Math.max(0, p - i) }), u = q || j ? l.endDate : g.getDateFromVirtualCoord({ 'coord': p + n + i }) || l.endDate;
    if (r && !g._viewPresetChanging && (!j || b || r - (((c = f._visibleDateRange) == null ? void 0 : c.startDate) || 0) || u - (((e = f._visibleDateRange) == null ? void 0 : e.endDate) || 0))) {
      f._visibleDateRange = {
        'startDate': r,
        'endDate': u,
        'startMS': r.getTime(),
        'endMS': u.getTime()
      };
      f.viewportCoords = j ? {
        'left': 0,
        'right': o
      } : {
        'left': a - i,
        'right': a + n + i
      };
      const v = g.timeView.range = {
        'startDate': r,
        'endDate': u
      };
      g.internalOnVisibleDateRangeChange(v);
      if (!g.refreshSuspended && g.rowManager.rows.length) {
        if (g.rowManager.rows[0].id === null) {
          return;
        }
        f.fromScroll = true;
        g.rowManager.rows.forEach(w => f.refreshEventsForResource(w, false, false));
        f.onRenderDone();
      }
    }
  }
  repaintEventsForResource(a) {
    this.refreshResources([a.id]);
  }
  onBeforeRowHeightChange() {
    this.clearAll();
  }
  refreshResourcesOnDataReady(a) {
    a.forEach(b => this.toDrawOnDataReady.add(b));
  }
  refreshResourcesWhenReady(a) {
    this.clearResources(a);
    a.forEach(b => this.toDrawOnProjectRefresh.add(b));
  }
  refreshResources(a, b = true) {
    const c = this, {scheduler: e} = c;
    c.clearResources(a);
    if (!e.refreshSuspended) {
      const f = [], g = [];
      a.forEach(i => {
        const j = e.getRowById(i);
        if (j) {
          f.push(j);
        } else {
          g.push(j);
        }
      });
      e.runWithTransition(() => {
        e.calculateRowHeights(g.map(i => this.resourceStore.getById(i)), true);
        e.rowManager.renderRows(f);
      }, b);
    }
  }
  layoutEventVerticallyStack(a, b, c) {
    const {
      barMargin: e,
      resourceMarginObject: {start: f},
      contentHeight: g
    } = this.scheduler.getResourceLayoutSettings(c, b.parent);
    return a === 0 ? f : f + a * g + a * e;
  }
  layoutEventVerticallyPack(a, b, c, e) {
    const {
        barMargin: f,
        resourceMarginObject: {start: g},
        contentHeight: i
      } = this.scheduler.getResourceLayoutSettings(e, c.parent), j = 1 / b, k = a * j, l = (i - (j - 1) * f) * b, n = g + k * l + k * f;
    return {
      'top': n,
      'height': l
    };
  }
  addTemporaryDragElement(a, b = a.resource) {
    const {scheduler: c} = this, e = c.generateRenderData(a, b, {
        'timeAxis': true,
        'viewport': true
      });
    e.absoluteTop = e.row ? e.top + e.row.top : c.getResourceEventBox(a, b, true).top;
    const f = this.renderEvent(e), {dataset: g} = f;
    delete f.tabIndex;
    delete g.eventId;
    delete g.resourceId;
    delete g.assignmentId;
    delete g.syncId;
    g.transient = true;
    f.parent = this.scheduler.foregroundCanvas;
    f.retainElement = true;
    const i = DomHelper.createElement(f, {
      'reactCallback'({
        jsx: j,
        domConfig: k,
        targetElement: l
      }) {
        let n;
        const {reactComponent: o} = c;
        j && ((n = c.processEventContent) == null ? void 0 : n.call(c, {
          'jsx': j,
          'action': 'add',
          'domConfig': k,
          'targetElement': l,
          'isRelease': false,
          'reactComponent': o
        }));
      }
    });
    i.innerElement = i.firstChild;
    a.instanceMeta(c).hasTemporaryDragElement = true;
    return i;
  }
  eventSorter(c, e) {
    if (this.overlappingEventSorter) {
      return this.overlappingEventSorter(c.eventRecord || c, e.eventRecord || e);
    }
    const f = c.isModel ? c.startDateMS : c.dataStartMS || c.startMS, g = c.isModel ? c.endDateMS : c.dataEndMS || c.endMS, i = e.isModel ? e.startDateMS : e.dataStartMS || e.startMS, j = e.isModel ? e.endDateMS : e.dataEndMS || e.endMS, k = c.isModel ? c.name : c.eventRecord.name, l = e.isModel ? e.name : e.eventRecord.name;
    return f - i || j - g || (k < l ? -1 : k == l ? 0 : 1);
  }
  calculateMS(a, b, c, e, f) {
    let g;
    const i = this, {scheduler: j} = i, {timeAxisViewModel: k} = j, {eventResize: l} = j.features;
    let n = getStartEnd(j, a, false, b, e), o = getStartEnd(j, a, true, c, e), p = o - n;
    if (j.milestoneLayoutMode !== 'default' && p === 0) {
      const v = k.getSingleUnitInPixels('minute'), w = j.getMilestoneLabelWidth(a, f), z = w * (1 / v);
      p = z * 60 * 1000;
      if (j.milestoneTextPosition === 'always-outside') {
        const A = j.getResourceLayoutSettings(f, a.parent).contentHeight, B = A * (1 / v) * 60 * 1000;
        n -= B / 2;
        o = n + p;
      } else {
        switch (j.milestoneAlign) {
        case 'start':
        case 'left':
          o = n + p;
          break;
        case 'end':
        case 'right':
          o = n;
          n = o - p;
          break;
        default:
          o = n + p / 2;
          n = o - p;
          break;
        }
      }
    }
    const q = n, r = o, u = p;
    if (((g = a.meta) == null ? void 0 : g.isResizing) && (l == null ? void 0 : l.isResizing) && l.lockLayout && !l.isMultiResizing) {
      n = a.startDate.getTime();
      o = a.endDate.getTime();
      p = r - q;
    }
    return {
      'startMS': n,
      'endMS': o,
      'durationMS': p,
      'elementStartMS': q,
      'elementEndMS': r,
      'elementDurationMS': u
    };
  }
  setupRenderData(a, b) {
    let c;
    const e = this, {scheduler: f} = e, {
        timeAxis: g,
        timeAxisViewModel: i
      } = f, {
        preamble: j,
        postamble: k
      } = a, l = e.isProHorizontalRendering && ((c = f.features.eventBuffer) == null ? void 0 : c.enabled) && (j || k) && !a.isMilestone, n = i.getSingleUnitInPixels('minute'), {isBatchUpdating: o} = a, p = l ? 'wrapStartDate' : 'startDate', q = l ? 'wrapEndDate' : 'endDate', r = o && !l ? a.get(p) : a[p], u = o && !l ? a.get(q) : a[q] || r, v = g.startMS, w = g.endMS, z = e.calculateMS(a, p, q, l, b), {
        startMS: A,
        endMS: B,
        durationMS: C
      } = z, E = A < v | (A > w) << 1, F = B > w | (B <= v) << 1, G = C / (1000 * 60), H = F ? n * G : null, I = f.getRowById(b);
    if (C === 0) {
      z.startMS--;
    }
    return {
      'eventRecord': a,
      'taskRecord': a,
      'start': r,
      'end': u,
      'rowId': b.id,
      'children': [],
      'startsOutsideView': E,
      'endsOutsideView': F,
      'width': H,
      'row': I,
      'useEventBuffer': l,
      ...z
    };
  }
  fillTimeSpanHorizontalPosition(a) {
    const {
        elementStartMS: b,
        elementEndMS: c,
        elementDurationMS: e
      } = a, f = b != null && c != null && this.calculateHorizontalPosition(a, b, c, e);
    if (f) {
      Object.assign(a, f);
      return true;
    }
    return false;
  }
  calculateHorizontalPosition(a, b, c, e) {
    const {scheduler: f} = this, {
        timeAxis: g,
        timeAxisViewModel: i
      } = f, {
        startsOutsideView: j,
        endsOutsideView: k,
        eventRecord: l
      } = a, n = g.startMS, o = i.getSingleUnitInPixels('minute');
    let p = null, q = f.getVirtualCoordFromDate(c, {
        'local': true,
        'respectExclusion': true,
        'isEnd': true
      }), r, u = false, v = false;
    if (j) {
      r = (b - n) / (1000 * 60) * o;
    } else {
      r = f.getVirtualCoordFromDate(b, {
        'local': true,
        'respectExclusion': true,
        'isEnd': false,
        'snapToNextIncluded': q !== -1
      });
      u = r === -1;
    }
    if (k) {
      const w = i.totalSize - r, z = c - g.endMS, A = z / (1000 * 60);
      p = w + o * A;
      if (BrowserHelper.isSafari && BrowserHelper.safariVersion < 16 && f.features.stickyEvents && g.endMS && !j) {
        q = f.getCoordinateFromDate(g.endMS);
      } else {
        q = r + p;
      }
    } else {
      v = q === -1;
    }
    if (v && !u) {
      q = f.getVirtualCoordFromDate(c, {
        'local': true,
        'respectExclusion': true,
        'isEnd': true,
        'snapToNextIncluded': true
      });
    }
    if (p > MAX_WIDTH) {
      if (j === 1) {
        if (k === 1) {
          r = -100;
          q = f.timeAxisColumn.width + 100;
        } else {
          r = q - MAX_WIDTH;
        }
      } else if (k === 1) {
        q = r + MAX_WIDTH;
      }
    }
    if (u && v) {
      r = f.getVirtualCoordFromDate(b, {
        'local': true,
        'respectExclusion': true,
        'isEnd': false,
        'snapToNextIncluded': true,
        'max': c
      });
      q = f.getVirtualCoordFromDate(c, {
        'local': true,
        'respectExclusion': true,
        'isEnd': true,
        'snapToNextIncluded': true,
        'min': b
      });
      if (r === q) {
        l.instanceMeta(f).excluded = true;
        return null;
      }
    }
    return {
      'left': Math.min(r, q),
      'width': Math.abs(q - r) || (l.isMilestone && !l.meta.isDragCreating ? 0 : 6),
      'clippedStart': u,
      'clippedEnd': v
    };
  }
  fillTimeSpanVerticalPosition(a, b) {
    const {scheduler: c} = this, {
        start: e,
        end: f
      } = a, {
        resourceMarginObject: {start: g},
        contentHeight: i
      } = c.getResourceLayoutSettings(b);
    if (c.fillTicks) {
      a.dataStartMS = e.getTime();
      a.dataEndMS = f.getTime();
    }
    a.top = Math.max(0, g);
    if (c.managedEventSizing) {
      a.height = i;
    }
  }
  getTimeSpanRenderData(a, b, c = false) {
    const e = this.setupRenderData(a, b);
    if (!this.fillTimeSpanHorizontalPosition(e)) {
      return null;
    }
    this.fillTimeSpanVerticalPosition(e, b);
    return e;
  }
  layoutEvents(a, b, c = false, e, f) {
    const g = this, {scheduler: j} = g, {timeAxis: k} = j, l = b.reduce((r, u) => {
        if (c || k.isTimeSpanInAxis(u)) {
          const v = j.generateRenderData(u, a, c);
          if (v) {
            r.push(v);
          }
        }
        return r;
      }, []);
    l.sort(f != null ? f : g.eventSorter);
    let n = j.getAppliedResourceHeight(a, e);
    const o = l.filter(({eventRecord: r}) => r.isEvent && !r.meta.excludeFromLayout), p = j.getEventLayout(a, e), q = j.getEventLayoutHandler(p);
    if (q) {
      const {
          barMargin: r,
          resourceMarginObject: {total: u},
          contentHeight: v
        } = j.getResourceLayoutSettings(a, e), w = q.applyLayout(o, a, j) || 1;
      if (q.type === 'layoutFn') {
        n = w;
      } else {
        n = w * v + (w - 1) * r + u;
      }
    } else if (o.length > 0) {
      const z = Math.min(o.length, g.maxZIndexStacks);
      for (let A = 0; A < z; A++) {
        const B = o[A];
        B.wrapperStyle += ';z-index:' + (A + 5);
      }
    }
    return {
      'rowHeight': n,
      'eventsData': l
    };
  }
  layoutResourceEvents(a, b = false) {
    const c = this, {scheduler: e} = c, {
        eventStore: f,
        assignmentStore: g,
        timeAxis: i
      } = e, j = f.getEvents({
        'includeOccurrences': e.enableRecurringEvents,
        'resourceRecord': a,
        'startDate': i.startDate,
        'endDate': i.endDate,
        'visibleDateRange': c.visibleDateRange,
        'filter': (g.isFiltered || f.isFiltered) && (l => l.assignments.some(n => n.resource === a.$original && g.includes(n)))
      }), k = e.getEventsToRender(a, j) || [];
    return c.layoutEvents(a, k, b);
  }
  renderEvent(a, b) {
    const {scheduler: c} = this, {
        resourceRecord: e,
        assignmentRecord: f,
        eventRecord: g
      } = a, i = f ? this.assignmentStore.getOccurrence(f, g).id : a.eventId, j = {
        'className': a.cls,
        'style': a.style || '',
        'children': a.children,
        'role': 'presentation',
        'dataset': { 'taskFeature': 'event' },
        'syncOptions': { 'syncIdField': 'taskBarFeature' }
      }, k = {
        'className': a.wrapperCls,
        'tabIndex': 'tabIndex' in a ? a.tabIndex : -1,
        'children': [
          j,
          ...a.wrapperChildren
        ],
        'style': {
          'insetBlockStart': a.absoluteTop,
          'insetInlineStart': a.left,
          'height': a.fillSize ? b : a.height,
          'width': a.width,
          'style': a.wrapperStyle,
          'fontSize': a.height + 'px'
        },
        'dataset': {
          'resourceId': e.id,
          'eventId': a.eventId,
          'syncId': e.isLinked ? i + '_' + e.id : i
        },
        'elementData': a,
        'retainElement': (f == null ? void 0 : f.instanceMeta(c).retainElement) || g.instanceMeta(c).retainElement,
        'syncOptions': {
          'syncIdField': 'taskFeature',
          'releaseThreshold': 0
        }
      };
    if (g.isMilestone && c.milestoneLayoutMode !== 'default' && c.milestoneTextPosition === 'always-outside' && c.getEventLayout(e).type === 'pack') {
      k.style.insetInlineStart += (c.getResourceLayoutSettings(e, g.parent).contentHeight - a.height) / 2;
    }
    if (a.fillSize) {
      a.height = b;
    }
    if (a.zIndex) {
      k.zIndex = a.zIndex;
    }
    if (f) {
      k.dataset.assignmentId = f.id;
    }
    a.elementConfig = k;
    c.afterRenderEvent({
      'renderData': a,
      'rowHeight': b,
      'domConfig': k
    });
    return k;
  }
  refreshEventsForResource(a, b = true, c = true) {
    const e = this, {store: f} = e.scheduler, g = a.isRow ? f.getAt(a.dataIndex) : f.getById(a), i = e.scheduler.rowManager.getRowFor(g);
    if (b) {
      e.clearResources([g]);
    }
    if (i && g) {
      e.renderer({
        'row': i,
        'record': g
      });
      if (b && c) {
        e.onRenderDone();
      }
    }
  }
  getResourceLayout(a) {
    const b = this;
    let c = b.resourceMap.get(a.id);
    if (!c || c.invalid || b.eventStore.lazyLoad) {
      if (b.suspended) {
        return;
      }
      c = b.layoutResourceEvents(a, false);
      b.resourceMap.set(a.id, c);
    }
    return c;
  }
  getEventDOMConfigForCurrentView(a, b, c, e) {
    let f;
    const g = this, {
        bufferSize: j,
        scheduler: k
      } = g, {
        labels: l,
        eventBuffer: n
      } = k.features, o = (n == null ? void 0 : n.enabled) || (l == null ? void 0 : l.enabled) && (l.left || l.right || l.before || l.after), {eventsData: p} = a, q = g.fromScroll ? g.rowMap.get(b) : null, r = [];
    let u, v;
    for (let w = 0; w < p.length; w++) {
      const z = p[w];
      u = c;
      v = e;
      if (o || z.width === 0) {
        u -= j;
        v += j;
      }
      if (z.left + z.width >= u && z.left <= v) {
        z.absoluteTop = z.top + b.top;
        const A = q == null ? void 0 : q.find(B => B.elementData.eventId === z.eventId && B.elementData.resourceId === z.resourceId);
        if (A) {
          A.style.top = z.absoluteTop;
        }
        z.hidden = (f = b.cls) == null ? void 0 : f.contains('b-row-placeholder');
        r.push(A != null ? A : g.renderEvent(z, a.rowHeight));
      }
    }
    return r;
  }
  renderer({
    row: a,
    record: b,
    size: size = {}
  }) {
    const c = this;
    if (b.isSpecialRow) {
      c.rowMap.delete(a);
      return;
    }
    const {
        left: e,
        right: f
      } = c.viewportCoords, g = c.getResourceLayout(b);
    if (!g) {
      return;
    }
    size.height = g.rowHeight;
    size.transient = true;
    const i = c.getEventDOMConfigForCurrentView(g, a, e, f);
    c.rowMap.set(a, i);
    c.rendererCalled = true;
  }
  onRenderDone() {
    let a;
    const {
      scheduler: b,
      rowMap: c,
      verticalBufferSize: e,
      fromScroll: f
    } = this;
    if (b.refreshSuspended) {
      return;
    }
    const {entityNames: g} = this, i = { 'event': [] }, j = (a = b._scrollTop) != null ? a : 0, k = j - e, l = j + b._bodyRectangle.height + e, n = e < 0, o = !b.managedEventSizing;
    for (const p of g) {
      i[p] = [];
    }
    c.forEach((q, r) => {
      if (!r.isDestroyed && !r.cls.contains('b-row-placeholder') && (n || r.bottom > k && r.top < l)) {
        for (let u = 0; u < q.length; u++) {
          const v = q[u], w = v.elementData, {
              absoluteTop: z,
              eventRecord: A,
              entityName: entityName = 'event'
            } = w;
          if (n || o || A.meta.isDragCreating || A.meta.isResizing || z + w.height > k && z < l) {
            if (!i[entityName]) {
              i[entityName] = [];
              g.add(entityName);
            }
            i[entityName].push(v);
          }
        }
      }
      for (let B = 0; B < q.length; B++) {
        q[B] = { ...q[B] };
      }
    });
    this.fromScroll = false;
    for (const q in i) {
      DomSync.sync({
        'domConfig': {
          'onlyChildren': true,
          'children': i[q]
        },
        'targetElement': b.canvases[q],
        'syncIdField': 'syncId',
        'callback': ({
          action: r,
          domConfig: u,
          lastDomConfig: v,
          targetElement: w,
          jsx: z
        }) => horizontalRenderingDomSyncCallback(b, f, {
          'action': r,
          'domConfig': u,
          'lastDomConfig': v,
          'targetElement': w,
          'jsx': z
        })
      });
    }
  }
  clearResources(a) {
    a = ArrayHelper.asArray(a);
    const b = a.map(Model.asId);
    b.forEach(c => {
      const e = this.resourceMap.get(c);
      if (e) {
        e.invalid = true;
      }
      const f = this.scheduler.getRowById(c);
      f && this.rowMap.delete(f);
    });
  }
  clearAll({
    clearDom: clearDom = false,
    clearLayoutCache: clearLayoutCache = false
  } = {}) {
    const a = this, {
        layouts: b,
        foregroundCanvas: c
      } = a.scheduler;
    if (clearLayoutCache && b) {
      for (const e in b) {
        b[e].clearCache();
      }
    }
    if (c && clearDom) {
      c.syncIdMap = c.lastDomConfig = c.releasedIdMap = null;
      for (const f of c.children) {
        f.lastDomConfig = f.elementData = null;
      }
    }
    a.resourceMap.clear();
    a.rowMap.clear();
  }
};
function horizontalRenderingDomSyncCallback(a, b, {
  action: c,
  domConfig: e,
  lastDomConfig: f,
  targetElement: g,
  jsx: i
}) {
  let j, k, l;
  const {reactComponent: n} = a, o = releaseEventActions[c], p = renderEventActions[c];
  !o && ((j = a.processEventContent) == null ? void 0 : j.call(a, {
    'jsx': i,
    'action': c,
    'domConfig': e,
    'targetElement': g,
    'isRelease': o,
    'reactComponent': n,
    'scrolling': b
  }));
  if (c === 'none' || !((k = e == null ? void 0 : e.elementData) == null ? void 0 : k.isWrap)) {
    return;
  }
  if (o && (f == null ? void 0 : f.elementData)) {
    const {
        eventRecord: q,
        resourceRecord: r,
        assignmentRecord: u
      } = f.elementData, v = {
        'renderData': f.elementData,
        'element': g,
        'eventRecord': q,
        'resourceRecord': r,
        'assignmentRecord': u
      };
    (l = a.processEventContent) == null ? void 0 : l.call(a, {
      'isRelease': o,
      'domConfig': f,
      'targetElement': g,
      'reactComponent': n
    });
    if (g === DomHelper.getActiveElement(g)) {
      a.focusElement.focus();
    }
    a.trigger('releaseEvent', v);
  }
  if (p) {
    const {
        eventRecord: w,
        resourceRecord: z,
        assignmentRecord: A
      } = e.elementData, B = {
        'renderData': e.elementData,
        'element': g,
        'isReusingElement': c === 'reuseElement',
        'isRepaint': c === 'reuseOwnElement',
        'eventRecord': w,
        'resourceRecord': z,
        'assignmentRecord': A
      };
    if (c === 'reuseElement' && a.isAnimating) {
      DomHelper.addTemporaryClass(g, 'b-reusing-own', 50, a, 'b-reusing-own-' + (A || w).id);
    }
    a.trigger('renderEvent', B);
  }
}
HorizontalRendering._$name = 'HorizontalRendering';
const VerticalLayout = class extends PackMixin_default() {
  static get defaultConfig() {
    return {
      'coordProp': 'leftFactor',
      'sizeProp': 'widthFactor'
    };
  }
  applyLayout(a, b, c) {
    const e = this, f = e.scheduler.getResourceWidth(b, c), {
        barMargin: g,
        resourceMarginObject: {
          start: i,
          total: j
        }
      } = e.scheduler.getResourceLayoutSettings(b, c), k = e.scheduler.getEventLayout(b, c), l = k.type;
    return e.packEventsInBands(a, (n, o, p, q) => {
      if (l === 'none') {
        n.width = f - j;
        n.left += i;
      } else {
        n.widthFactor = q;
        const r = n.leftFactor = p.start + o * q, u = Math.round(1 / q), v = r / q, w = f - j - g * (u - 1);
        if (l === 'mixed' && u === 2) {
          n.left += r * f / 5 + g;
          n.width = f - r * f / 5 - g * 2;
          n.zIndex = 5 + v;
        } else {
          n.width = q * w;
          n.left += r * w + i + g * v;
        }
      }
      n.cls['b-sch-event-narrow'] = n.width < e.scheduler.narrowEventWidth;
    });
  }
};
VerticalLayout._$name = 'VerticalLayout';
const releaseEventActions2 = {
  'releaseElement': 1,
  'reuseElement': 1
};
const renderEventActions2 = {
  'newElement': 1,
  'reuseOwnElement': 1,
  'reuseElement': 1
};
const chronoFields2 = {
  'startDate': 1,
  'endDate': 1,
  'duration': 1
};
const emptyObject13 = Object.freeze({});
const VerticalRendering = class extends Base.mixin(Delayable_default, AttachToProjectMixin_default) {
  static get properties() {
    return {
      'eventMap': new Map(),
      'resourceMap': new Map(),
      'releasedElements': {},
      'toDrawOnProjectRefresh': new Set(),
      'resourceBufferSize': 1
    };
  }
  construct(a) {
    this.client = this.scheduler = a;
    this.verticalLayout = new VerticalLayout({ 'scheduler': a });
    super.construct({});
  }
  init() {
    const a = this, {
        scheduler: b,
        resourceColumns: c
      } = a;
    c.resourceStore = a.resourceStore;
    c.ion({
      'name': 'resourceColumns',
      'columnWidthChange': 'onResourceColumnWidthChange',
      'thisObj': a
    });
    a.initialized = true;
    if (b.isPainted) {
      a.renderer();
    }
    c.availableWidth = b.timeAxisSubGridElement.offsetWidth;
  }
  resolveRowRecord(a, b) {
    const c = this, {scheduler: e} = c, f = a.nodeType ? null : a, g = f ? f.target : a, i = f ? [
        f.borderOffsetX,
        f.borderOffsetY
      ] : b, j = g.nodeType === Element.TEXT_NODE ? g.parentElement : g, k = j.closest(e.eventSelector);
    if (k) {
      return e.resourceStore.getById(k.dataset.resourceId);
    }
    if (!g.closest('.b-sch-timeaxis-cell')) {
      return null;
    }
    if (!i) {
      throw new Error('Vertical mode needs coordinates to resolve this element. Can also be called with a browser\n                event instead of element to extract element and coordinates from');
    }
    if (e.variableColumnWidths || e.resourceStore.isGrouped) {
      let n = 0;
      for (const o of c.resourceStore) {
        if (!o.isSpecialRow) {
          n += e.getResourceWidth(o);
        }
        if (n >= i[0]) {
          return o;
        }
      }
      return null;
    }
    const l = Math.floor(i[0] / c.resourceColumns.columnWidth);
    return c.allResourceRecords[l];
  }
  toggleCls(a, b, c = true, e = false) {
    let f;
    const g = (f = this.eventMap.get(a.eventId)) == null ? void 0 : f[a.resourceId];
    if (g) {
      g.renderData[e ? 'wrapperCls' : 'cls'][b] = c;
      const i = this.client.getElementFromAssignmentRecord(a, e);
      if (i) {
        i.classList[c ? 'add' : 'remove'](b);
      }
    }
  }
  getDateFromXY(a, b, c, e = false) {
    let f = a[1];
    if (!c) {
      f = this.translateToScheduleCoordinate(f);
    }
    return this.scheduler.timeAxisViewModel.getDateFromPosition(f, b, e);
  }
  translateToScheduleCoordinate(a) {
    return a - this.scheduler.timeAxisSubGridElement.getBoundingClientRect().top - globalThis.scrollY;
  }
  translateToPageCoordinate(a) {
    return a + this.scheduler.timeAxisSubGridElement.getBoundingClientRect().top + globalThis.scrollY;
  }
  getResourceEventBox(a, b) {
    let c, e, f;
    const g = a.id, i = b.id;
    let {renderData: j} = ((c = this.eventMap.get(g)) == null ? void 0 : c[i]) || emptyObject13;
    if (!j) {
      this.layoutResourceEvents(this.scheduler.resourceStore.getById(i));
      j = (f = (e = this.eventMap.get(g)) == null ? void 0 : e[i]) == null ? void 0 : f.renderData;
    }
    return j ? new Rectangle(j.left, j.top, j.width, j.bottom - j.top) : null;
  }
  getScheduleRegion(a, b, c) {
    let e;
    const f = this, {scheduler: g} = f, i = Rectangle.from(g.timeAxisSubGridElement, g.timeAxisSubGridElement);
    if (a) {
      i.left = f.allResourceRecords.indexOf(a) * g.resourceColumnWidth;
      i.right = i.left + g.resourceColumnWidth;
    }
    const j = g.timeAxis.startDate, k = g.timeAxis.endDate, l = ((e = g.getDateConstraints) == null ? void 0 : e.call(g, a, b)) || {
        'start': j,
        'end': k
      }, n = g.getCoordinateFromDate(DateHelper.max(j, l.start)), o = g.getCoordinateFromDate(DateHelper.min(k, l.end));
    if (!c) {
      i.top = f.translateToPageCoordinate(n);
      i.bottom = f.translateToPageCoordinate(o);
    } else {
      i.top = n;
      i.bottom = o;
    }
    return i;
  }
  getRowRegion(a, b, c) {
    const e = this, {scheduler: f} = e, g = e.allResourceRecords.indexOf(a) * f.resourceColumnWidth, i = f.timeAxis.startDate, j = f.timeAxis.endDate, k = b ? DateHelper.max(i, b) : i, l = c ? DateHelper.min(j, c) : j, n = f.getCoordinateFromDate(k), o = f.getCoordinateFromDate(l, true, true), p = Math.min(n, o), q = Math.abs(n - o);
    return new Rectangle(g, p, f.resourceColumnWidth, q);
  }
  get visibleDateRange() {
    const a = this.scheduler, b = a.scrollable.y, c = a.scrollable.clientHeight, e = a.getDateFromCoordinate(b) || a.timeAxis.startDate, f = a.getDateFromCoordinate(b + c) || a.timeAxis.endDate;
    return {
      'startDate': e,
      'endDate': f,
      'startMS': e.getTime(),
      'endMS': f.getTime()
    };
  }
  onResourceColumnWidthChange({
    width: a,
    oldWidth: b
  }) {
    const c = this, {scheduler: e} = c;
    c.resourceColumns.width = e.timeAxisColumn.width = c.allResourceRecords.length * a;
    c.clearAll();
    c.refresh(Math.abs(a - b) > 30);
    e.onVisibleResourceColumnChange(a, b);
  }
  attachToProject(a) {
    super.attachToProject(a);
    if (a) {
      a.ion({
        'name': 'project',
        'refresh': 'onProjectRefresh',
        'thisObj': this
      });
    }
  }
  onProjectRefresh() {
    const a = this, {
        scheduler: b,
        toDrawOnProjectRefresh: c
      } = a;
    if (b.isVisible) {
      if (b.rendered && !b.refreshSuspended) {
        if (a.refreshAllWhenReady) {
          a.clearAll();
          a.refresh();
          a.refreshAllWhenReady = false;
        } else if (c.size) {
          a.refresh();
        }
        c.clear();
      }
    } else {
      b.whenVisible('refresh', b, [true]);
    }
  }
  attachToEventStore(a) {
    super.attachToEventStore(a);
    this.refreshAllWhenReady = true;
    if (a) {
      a.ion({
        'name': 'eventStore',
        'addConfirmed': 'onEventStoreAddConfirmed',
        'refreshPreCommit': 'onEventStoreRefresh',
        'thisObj': this
      });
    }
  }
  onEventStoreAddConfirmed({record: a}) {
    for (const b of this.client.getElementsFromEventRecord(a)) {
      b.classList.remove('b-iscreating');
    }
  }
  onEventStoreRefresh({action: a}) {
    if (a === 'batch') {
      this.refreshAllWhenReady = true;
    }
  }
  onEventStoreChange({
    action: a,
    records: b = [],
    record: c,
    replaced: e,
    changes: f,
    isAssign: g
  }) {
    const i = this, j = new Set();
    b.forEach(k => {
      let l;
      const n = (l = k.$linkedResources) == null ? void 0 : l.filter(o => i.resourceStore.includes(o));
      n == null ? void 0 : n.forEach(o => j.add(o.id));
    });
    switch (a) {
    case 'sort':
    case 'group':
    case 'move':
    case 'remove':
    case 'batch':
      return;
    case 'dataset':
      i.refreshAllResourcesWhenReady();
      return;
    case 'add':
      break;
    case 'replace':
      e.forEach(([, k]) => {
        k.resources.map(l => j.add(l.id));
      });
      i.clearResources(j);
      break;
    case 'removeall':
    case 'filter':
      i.clearAll();
      i.refresh();
      return;
    case 'update': {
        const k = c.$entity ? !Object.keys(f).some(n => !c.$entity.getField(n)) : !Object.keys(f).some(n => !chronoFields2[n]);
        let l = 0;
        if ('startDate' in f)
          l++;
        if ('endDate' in f)
          l++;
        if ('duration' in f)
          l++;
        if (!k || l || 'percentDone' in f || 'inactive' in f || 'segments' in f) {
          if (i.shouldWaitForInitializeAndEngineReady) {
            i.refreshResourcesWhenReady(j);
          } else {
            i.clearResources(j);
            i.refresh();
          }
        }
        return;
      }
    }
    i.refreshResourcesWhenReady(j);
  }
  attachToResourceStore(a) {
    const b = this;
    super.attachToResourceStore(a);
    b.refreshAllWhenReady = true;
    if (b.resourceColumns) {
      b.resourceColumns.resourceStore = a;
    }
    a.ion({
      'name': 'resourceStore',
      'changePreCommit': 'onResourceStoreChange',
      'refreshPreCommit': 'onResourceStoreRefresh',
      'load': () => b.scheduler.unmaskBody(),
      'thisObj': b,
      'prio': 1
    });
    if (b.initialized && b.scheduler.isPainted) {
      b.firstResource = b.lastResource = null;
      b.clearAll();
      b.renderer();
    }
  }
  onResourceStoreChange({
    source: a,
    action: b,
    records: records = [],
    record: c,
    replaced: e,
    changes: f
  }) {
    const g = this, j = e ? e.map(n => n[1]) : records, k = new Set(j.map(n => n.id));
    g.firstResource = g.lastResource = null;
    a._allResourceRecords = null;
    const {allResourceRecords: l} = a;
    if (g.scheduler.isEngineReady) {
      switch (b) {
      case 'update':
        if (f == null ? void 0 : f.id) {
          g.clearResources([
            f.id.oldValue,
            f.id.value
          ]);
        } else {
          g.clearResources([c.id]);
        }
        break;
      case 'filter':
        g.clearAll();
        break;
      }
      if (f && 'columnWidth' in f) {
        c.instanceMeta(g.scheduler).columnWidth = c.columnWidth;
        g.clearAll();
      }
      g.refresh(true);
    } else {
      switch (b) {
      case 'dataset':
      case 'remove':
      case 'removeall':
        g.refreshAllResourcesWhenReady();
        return;
      case 'replace':
      case 'add': {
          if (!a.isGrouped) {
            const n = j.reduce((o, p) => Math.min(o, l.indexOf(p)), l.length);
            for (let o = n; o < l.length; o++) {
              k.add(l[o].id);
            }
          }
        }
      }
      g.refreshResourcesWhenReady(k);
    }
  }
  onResourceStoreRefresh({action: a}) {
    const b = this;
    if (a === 'sort' || a === 'group') {
      b.firstResource = b.lastResource = b.resourceStore._allResourceRecords = null;
      b.clearAll();
      b.refresh();
    }
  }
  attachToAssignmentStore(a) {
    super.attachToAssignmentStore(a);
    this.refreshAllWhenReady = true;
    if (a) {
      a.ion({
        'name': 'assignmentStore',
        'changePreCommit': 'onAssignmentStoreChange',
        'refreshPreCommit': 'onAssignmentStoreRefresh',
        'thisObj': this
      });
    }
  }
  onAssignmentStoreChange({
    action: a,
    records: b = [],
    replaced: c,
    changes: e
  }) {
    const f = this, g = new Set(b.map(i => i.resourceId));
    if (f.scheduler.isEngineReady) {
      switch (a) {
      case 'add':
      case 'remove':
        f.clearResources(g);
        break;
      case 'filter':
        f.clearAll();
        break;
      case 'update': {
          if ('resourceId' in e) {
            g.add(e.resourceId.oldValue);
          }
          if (!Object.keys(e).filter(i => i !== 'resource' && i !== 'event').length) {
            return;
          }
          f.clearResources(g);
        }
      }
      f.refresh(true);
    } else {
      if (e && 'resourceId' in e) {
        g.add(e.resourceId.oldValue);
      }
      switch (a) {
      case 'removeall':
        f.refreshAllResourcesWhenReady();
        return;
      case 'replace':
        c.forEach(([i, j]) => {
          g.add(i.resourceId);
          g.add(j.resourceId);
        });
      }
      f.refreshResourcesWhenReady(g);
    }
  }
  onAssignmentStoreRefresh({
    action: a,
    records: b
  }) {
    if (a === 'batch') {
      this.clearAll();
      this.refreshAllResourcesWhenReady();
    }
  }
  refreshRows(a, b) {
    if (b) {
      this.clearAll();
      this.scheduler.refreshFromRerender = false;
    }
  }
  repaintEventsForResource(a) {
    const {resourceColumns: b} = this.scheduler;
    this.renderResource(a);
    if (this.scheduler.internalEventLayout.type === 'layoutFn') {
      b.updateWidthCache();
      b.refresh();
    }
  }
  updateFromHorizontalScroll(a) {
    if (a !== this.prevScrollX) {
      this.fromScroll = true;
      this.renderer();
      this.prevScrollX = a;
    }
  }
  updateFromVerticalScroll() {
    this.fromScroll = true;
    this.renderer();
  }
  scrollResourceIntoView(a, b) {
    const {scheduler: c} = this, e = this.allResourceRecords.indexOf(a) * c.resourceColumnWidth;
    return c.scrollHorizontallyTo(e, b);
  }
  get allResourceRecords() {
    return this.scheduler.resourceStore.allResourceRecords;
  }
  onViewportResize(a) {
    if (this.resourceColumns.availableWidth !== a) {
      this.resourceColumns.availableWidth = a;
      this.scheduler.refreshVirtualScrollbars();
    }
    this.renderer();
  }
  get resourceColumns() {
    let a;
    return (a = this.scheduler.timeAxisColumn) == null ? void 0 : a.resourceColumns;
  }
  onLocaleChange() {
    this.clearAll();
  }
  onDragAbort() {
  }
  onBeforeRowHeightChange() {
  }
  onTimeAxisViewModelUpdate() {
  }
  updateElementId() {
  }
  releaseTimeSpanDiv() {
  }
  getConnectorStartSide(a) {
    return 'top';
  }
  getConnectorEndSide(a) {
    return 'bottom';
  }
  refreshResourcesWhenReady(a) {
    this.clearResources(a);
    a.forEach(b => this.toDrawOnProjectRefresh.add(b));
  }
  refreshAllResourcesWhenReady() {
    this.clearAll();
    this.refreshAllWhenReady = true;
  }
  get resourceRange() {
    return this.getResourceRange(true);
  }
  get visibleResources() {
    const {
      first: a,
      last: b
    } = this.getResourceRange();
    return {
      'first': this.allResourceRecords[a],
      'last': this.allResourceRecords[b]
    };
  }
  getResourceRange(a) {
    const {
        scheduler: b,
        resourceStore: c
      } = this, {
        resourceColumnWidth: e,
        scrollX: f
      } = b, {scrollWidth: g} = b.timeAxisSubGrid.scrollable, i = a ? this.resourceBufferSize : 0, j = f - i, k = f + g + i;
    if (!(c == null ? void 0 : c.count)) {
      return {
        'first': -1,
        'last': -1
      };
    }
    if (b.variableColumnWidths) {
      let l, n = 0, o, p = 0;
      this.allResourceRecords.forEach((q, r) => {
        q.instanceMeta(b).insetStart = o = p;
        p = o + b.getResourceWidth(q);
        if (o > k) {
          return false;
        }
        if (p > j && l == null) {
          l = r;
        } else if (o < k) {
          n = r;
        }
      });
      return {
        'first': l,
        'last': n
      };
    } else {
      return {
        'first': Math.max(Math.floor(f / e) - i, 0),
        'last': Math.min(Math.floor((f + b.timeAxisSubGrid.width) / e) + i, this.allResourceRecords.length - 1)
      };
    }
  }
  get dateRange() {
    const {scheduler: a} = this;
    let b = a.getDateFromCoordinate(Math.min(a.scrollTop + a.bodyHeight + a.tickSize - 1, (a.virtualScrollHeight || a.scrollable.scrollHeight) - 1));
    if (!b) {
      b = a.timeAxis.last.endDate;
    }
    let c = a.getDateFromCoordinate(Math.max(a.scrollTop - a.tickSize, 0));
    if (!c) {
      c = a.timeAxis.first.startDate;
      b = a.getDateFromCoordinate(a.bodyHeight + a.tickSize - 1);
    }
    return {
      'topDate': c,
      'bottomDate': b
    };
  }
  getTimeSpanRenderData(a, b, c = false) {
    let e;
    const f = this, {scheduler: g} = f, {
        preamble: i,
        postamble: j
      } = a, {variableColumnWidths: k} = g, l = ((e = g.features.eventBuffer) == null ? void 0 : e.enabled) && f.isProVerticalRendering && (i || j) && !a.isMilestone, n = l ? 'wrapStartDate' : 'startDate', o = l ? 'wrapEndDate' : 'endDate', p = a.isBatchUpdating && a.hasBatchedChange(n) && !l ? a.get(n) : a[n], q = a.isBatchUpdating && a.hasBatchedChange(o) && !l ? a.get(o) : a[o], {
        resourceMarginObject: {total: r}
      } = g.getResourceLayoutSettings(b), u = g.getCoordinateFromDate(p), v = b.instanceMeta(g), w = k ? v.insetStart : f.allResourceRecords.indexOf(b) * g.resourceColumnWidth, z = g.getResourceWidth(b), A = z - r, B = p.getTime(), C = q.getTime();
    let E = g.getCoordinateFromDate(q), F = E - u;
    if (E === -1) {
      F = Math.round((C - B) * g.timeAxisViewModel.getSingleUnitInPixels('millisecond'));
      E = u + F;
    }
    return {
      'eventRecord': a,
      'resourceRecord': b,
      'left': w,
      'top': u,
      'bottom': E,
      'resourceWidth': z,
      'width': A,
      'height': F,
      'startDate': p,
      'endDate': q,
      'startDateMS': B,
      'endDateMS': C,
      'useEventBuffer': l,
      'children': [],
      'start': p,
      'end': q,
      'startMS': B,
      'endMS': C
    };
  }
  eventSorter(c, e) {
    const f = c.dataStartMs || c.startDateMS, g = c.dataEndMs || c.endDateMS, i = e.dataStartMs || e.startDateMS, j = e.dataEndMs || e.endDateMS, k = c.isModel ? c.name : c.eventRecord.name, l = e.isModel ? e.name : e.eventRecord.name;
    return f - i || j - g || (k < l ? -1 : k == l ? 0 : 1);
  }
  layoutEvents(a, b, c = false, e, f) {
    const g = this, {scheduler: i} = g, {variableColumnWidths: j} = i, {id: k} = a, l = a.instanceMeta(i), n = e ? k + '-' + e.id : k, o = g.resourceMap.set(n, {}).get(n), p = g.allResourceRecords.indexOf(a);
    const q = b.reduce((r, u) => {
      if (u.isScheduled) {
        const v = i.generateRenderData(u, a, false), w = { 'renderData': v }, z = ObjectHelper.getMapPath(g.eventMap, v.eventId, {});
        z[k] = w;
        o[v.eventId] = w;
        if (v.fillSize) {
          v.left = j ? l.insetStart : p * i.resourceColumnWidth;
          v.width = i.getResourceWidth(a);
        } else {
          r.push(v);
        }
      }
      return r;
    }, []);
    q.sort(f != null ? f : g.eventSorter);
    g.verticalLayout.applyLayout(q, a, e);
    return o;
  }
  layoutResourceEvents(a, b, c) {
    const e = this, {scheduler: f} = e, {
        assignmentStore: g,
        eventStore: i,
        timeAxis: j
      } = f;
    let k = i.getEvents({
      'includeOccurrences': f.enableRecurringEvents,
      'resourceRecord': a,
      'startDate': b != null ? b : j.startDate,
      'endDate': c != null ? c : j.endDate,
      'filter': (g.isFiltered || i.isFiltered) && (l => l.assignments.some(n => n.resource === a && g.includes(n)))
    });
    k = f.getEventsToRender(a, k);
    return e.layoutEvents(a, k);
  }
  addTemporaryDragElement(a) {
    const {scheduler: b} = this, c = b.generateRenderData(a, a.resource, {
        'timeAxis': true,
        'viewport': true
      });
    c.top = c.row ? c.top + c.row.top : b.getResourceEventBox(a, a.resource, true).top;
    const e = this.renderEvent({ 'renderData': c }), {dataset: f} = e;
    delete e.tabIndex;
    delete f.eventId;
    delete f.resourceId;
    delete f.assignmentId;
    delete f.syncId;
    f.transient = true;
    e.parent = this.scheduler.foregroundCanvas;
    e.retainElement = true;
    const g = DomHelper.createElement(e, {
      'reactCallback'({
        jsx: i,
        domConfig: j,
        targetElement: k
      }) {
        let l;
        const {reactComponent: n} = b;
        i && ((l = b.processEventContent) == null ? void 0 : l.call(b, {
          'jsx': i,
          'action': 'add',
          'domConfig': j,
          'targetElement': k,
          'isRelease': false,
          'reactComponent': n
        }));
      }
    });
    g.innerElement = g.firstChild;
    a.instanceMeta(b).hasTemporaryDragElement = true;
    return g;
  }
  renderEvent(a) {
    const {scheduler: b} = this, c = a.renderData, {
        resourceRecord: e,
        assignmentRecord: f,
        eventRecord: g
      } = c, i = {
        'className': c.wrapperCls,
        'tabIndex': -1,
        'children': [
          {
            'role': 'presentation',
            'className': c.cls,
            'style': (c.internalStyle || '') + (c.style || ''),
            'children': c.children,
            'dataset': { 'taskFeature': 'event' },
            'syncOptions': { 'syncIdField': 'taskBarFeature' }
          },
          ...c.wrapperChildren
        ],
        'style': {
          'top': c.top,
          [b.rtl ? 'right' : 'left']: c.left,
          'height': g.isMilestone ? '1em' : c.height,
          'width': c.width,
          'style': c.wrapperStyle || '',
          'fontSize': g.isMilestone ? Math.min(c.width, 40) : null
        },
        'dataset': {
          'resourceId': e.id,
          'eventId': c.eventId,
          'syncId': f ? this.assignmentStore.getOccurrence(f, g).id : c.eventId
        },
        'elementData': a,
        'retainElement': (f || g).instanceMeta(this.scheduler).retainElement,
        'syncOptions': {
          'syncIdField': 'taskFeature',
          'releaseThreshold': 0
        }
      };
    i.className['b-sch-vertical'] = 1;
    if (c.zIndex) {
      i.zIndex = c.zIndex;
    }
    if (f) {
      i.dataset.assignmentId = f.id;
    }
    c.elementConfig = a.elementConfig = i;
    b.afterRenderEvent({
      'renderData': c,
      'domConfig': i
    });
    return i;
  }
  renderResource(a, b, c) {
    let e, f, g;
    const i = this, j = (e = b == null ? void 0 : b.getTime()) != null ? e : i.topDateMS, k = (f = c == null ? void 0 : c.getTime()) != null ? f : i.bottomDateMS, l = [];
    let n = i.resourceMap.get(a.id);
    if (!n) {
      n = i.layoutResourceEvents(a, b, c);
    }
    for (const o in n) {
      const p = n[o], {
          endDateMS: q,
          startDateMS: r,
          eventRecord: u
        } = p.renderData;
      if (q >= j && r <= k && !u.instanceMeta(i.scheduler).hasTemporaryDragElement) {
        const v = ((g = p.elementConfig) == null ? void 0 : g.className) !== 'b-released' && p.elementConfig || i.renderEvent(p);
        l.push(v);
      }
    }
    return l;
  }
  get shouldWaitForInitializeAndEngineReady() {
    return !this.initialized || !this.scheduler.isEngineReady && !this.scheduler.isCreating;
  }
  renderer() {
    const a = this, {scheduler: b} = a, {
        topDate: c,
        bottomDate: e
      } = a.dateRange, f = [], g = [], j = { 'event': [] }, {
        allResourceRecords: k,
        fromScroll: l
      } = a;
    let {
      first: n,
      last: o
    } = a.resourceRange;
    if (a.shouldWaitForInitializeAndEngineReady) {
      return;
    }
    if (!DateHelper.isEqual(c, a.topDate) || !DateHelper.isEqual(e, a.bottomDate)) {
      a.topDate = c;
      a.bottomDate = e;
      a.topDateMS = c.getTime();
      a.bottomDateMS = e.getTime();
      const p = a.timeView.range = {
        'startDate': c,
        'endDate': e
      };
      b.internalOnVisibleDateRangeChange(p);
    }
    if (n !== -1 && o !== -1) {
      if (b.internalEventLayout.type === 'layoutFn' && n !== void 0) {
        a.renderResourceCustomLayout(f);
      } else {
        for (let r = n; r <= o; r++) {
          f.push.apply(f, a.renderResource(k[r]));
        }
      }
      const {resourceRange: q} = a;
      n = q.first;
      o = q.last;
    }
    b.getForegroundDomConfigs(g);
    f.push.apply(f, g);
    for (const u of f) {
      const {
        entityName: entityName = 'event'
      } = u.elementData.renderData;
      if (!j[entityName]) {
        j[entityName] = [];
      }
      j[entityName].push(u);
    }
    for (const v in j) {
      DomSync.sync({
        'domConfig': {
          'onlyChildren': true,
          'children': j[v]
        },
        'targetElement': b.canvases[v],
        'syncIdField': 'syncId',
        'callback'({
          action: w,
          domConfig: z,
          lastDomConfig: A,
          targetElement: B,
          jsx: C
        }) {
          verticalRenderingDomSyncCallback(b, l, {
            'domConfig': z,
            'jsx': C,
            'action': w,
            'targetElement': B,
            'lastDomConfig': A
          });
        }
      });
    }
    if (a.firstResource !== n || a.lastResource !== o) {
      const w = a.resourceColumns.visibleResources = {
        'firstResource': n,
        'lastResource': o
      };
      a.firstResource = n;
      a.lastResource = o;
      b.onVisibleResourceRangeChange(w);
      b.trigger('resourceRangeChange', w);
    }
    this.fromScroll = false;
  }
  refresh(a) {
    this.scheduler.runWithTransition(() => this.renderer(), a);
  }
  refreshResources(a) {
    a = a.map(b => {
      let c;
      return (c = b.id) != null ? c : b;
    });
    this.clearResources(a);
    this.refresh();
  }
  refreshEventsForResource(a, b = true, c = true) {
    this.refreshResources([a.id]);
  }
  onRenderDone() {
  }
  get timeView() {
    return this.scheduler.timeView;
  }
  clearResources(a) {
    const {
      resourceMap: b,
      eventMap: c
    } = this;
    a.forEach(e => {
      if (b.has(e)) {
        Object.values(b.get(e)).forEach(({
          renderData: {eventId: f}
        }) => {
          delete c.get(f)[e];
        });
        b.delete(e);
      }
    });
  }
  clearAll() {
    this.resourceMap.clear();
    this.eventMap.clear();
  }
};
function verticalRenderingDomSyncCallback(a, b, {
  domConfig: c,
  jsx: e,
  action: f,
  targetElement: g,
  lastDomConfig: i
}) {
  let j, k, l;
  const {reactComponent: n} = a;
  if (a.isEventElement(c) || e || ((j = c == null ? void 0 : c.elementData) == null ? void 0 : j.jsx)) {
    const o = releaseEventActions2[f], p = renderEventActions2[f];
    if ((k = a.processEventContent) == null ? void 0 : k.call(a, {
        'action': f,
        'domConfig': c,
        'isRelease': false,
        'targetElement': g,
        'reactComponent': n,
        'jsx': e,
        'scrolling': b
      }))
      return;
    if (o && a.isEventElement(i) && !i.isReleased) {
      const q = i.elementData.renderData, r = {
          'renderData': q,
          'assignmentRecord': q.assignmentRecord,
          'eventRecord': q.eventRecord,
          'resourceRecord': q.resourceRecord,
          'element': g
        };
      (l = a.processEventContent) == null ? void 0 : l.call(a, {
        'isRelease': o,
        'domConfig': i,
        'targetElement': g,
        'reactComponent': n
      });
      if (g === DomHelper.getActiveElement(g)) {
        a.focusElement.focus();
      }
      a.trigger('releaseEvent', r);
    }
    if (p) {
      const u = c.elementData.renderData, v = {
          'renderData': u,
          'assignmentRecord': u.assignmentRecord,
          'eventRecord': u.eventRecord,
          'resourceRecord': u.resourceRecord,
          'element': g,
          'isReusingElement': f === 'reuseElement',
          'isRepaint': f === 'reuseOwnElement'
        };
      v.reusingElement = f === 'reuseElement';
      a.trigger('renderEvent', v);
    }
  }
}
VerticalRendering._$name = 'VerticalRendering';
function isLastLevel(a, b) {
  return a === b.length - 1;
}
function isLastCell(a, b) {
  return b === a.cells[a.cells.length - 1];
}
const TimeAxisBase = class extends Widget {
  static configurable = {
    'compactCellWidthThreshold': 15,
    'model': null,
    'cls': null,
    'sizeProperty': null,
    'positionProperty': null
  };
  static $name = 'TimeAxisBase';
  size = null;
  levels = [];
  endDate = null;
  startDate = null;
  constructor() {
    super(...arguments);
  }
  set range({
    startDate: a,
    endDate: b
  }) {
    let c;
    const e = this;
    if (e.startDate - a || e.endDate - b) {
      const {client: f} = e;
      e.startDate = a;
      e.endDate = b;
      if (e.sizeProperty === 'width' && (f == null ? void 0 : f.hideHeaders) || e.sizeProperty === 'height' && ((c = f == null ? void 0 : f.verticalTimeAxisColumn) == null ? void 0 : c.hidden)) {
        return;
      }
      e.refresh();
    }
  }
  buildCells(a = this.startDate, b = this.endDate) {
    let c;
    const e = this, {sizeProperty: f} = e, {
        stickyHeaders: g,
        isVertical: i
      } = e.client || {}, j = [], {length: k} = e.levels;
    const l = e.levels.map((n, o) => {
      let p;
      const q = g && (i || o < k - 1);
      return {
        'className': {
          'b-sch-header-row': 1,
          ['b-sch-header-row-' + n.position]: 1,
          'b-sch-header-row-main': o === e.model.viewPreset.mainHeaderLevel,
          'b-lowest': isLastLevel(o, e.levels),
          'b-sticky-header': q
        },
        'syncOptions': {
          'releaseThreshold': 5,
          'syncIdField': 'tickIndex'
        },
        'dataset': {
          'headerFeature': 'headerRow' + o,
          'headerPosition': n.position
        },
        'children': (p = n.cells) == null ? void 0 : p.filter(r => r.start < b && r.end > a).map((r, u) => ({
          'role': 'presentation',
          'className': {
            'b-sch-header-timeaxis-cell': 1,
            [r.headerCellCls]: r.headerCellCls,
            ['b-align-' + r.align]: r.align,
            'b-last': isLastCell(n, r)
          },
          'dataset': {
            'tickIndex': r.index,
            ...globalThis.DEBUG && { 'date': r.start.getTime() }
          },
          'style': {
            [e.positionProperty]: r.coord,
            [f]: r.width,
            ['min-' + f]: r.width
          },
          'children': [{
              'tag': 'span',
              'role': 'presentation',
              'className': {
                'b-sch-header-text': 1,
                'b-sticky-header': q
              },
              'html': r.value
            }]
        }))
      };
    });
    (c = e.client) == null ? void 0 : c.getHeaderDomConfigs(j);
    l.push(...j);
    return {
      'onlyChildren': true,
      'syncOptions': { 'releaseThreshold': 0 },
      'children': l
    };
  }
  render(a) {
    super.render(a);
    this.refresh(true);
  }
  refresh(a = !this.levels.length) {
    const b = this, {columnConfig: c} = b.model, {levels: e} = b, f = e.length;
    if (a) {
      e.length = 0;
      c.forEach((i, j) => e[j] = {
        'position': j,
        'cells': i
      });
      b.size = e[0].cells.reduce((i, j) => i + j.width, 0);
      const {parentElement: g} = b.element;
      if (g && (e.length !== f || a)) {
        g.classList.remove('b-sch-timeaxiscolumn-levels-' + f);
        g.classList.add('b-sch-timeaxiscolumn-levels-' + e.length);
      }
    }
    if (!b.startDate || !b.endDate) {
      return;
    }
    DomSync.sync({
      'domConfig': b.buildCells(),
      'targetElement': b.element,
      'syncIdField': 'headerFeature'
    });
    b.trigger('refresh');
  }
  get widgetClass() {
    return 'b-timeaxis';
  }
};
TimeAxisBase._$name = 'TimeAxisBase';
const HorizontalTimeAxis = class extends TimeAxisBase {
  static configurable = {
    'model': null,
    'sizeProperty': 'width'
  };
  static type = 'horizontaltimeaxis';
  static $name = 'HorizontalTimeAxis';
  get positionProperty() {
    return 'insetInlineStart';
  }
  get width() {
    return this.size;
  }
  onModelUpdate() {
    let a;
    if (!((a = this.owner) == null ? void 0 : a.hideHeaders) && this.model.availableSpace > 0 && this.model.availableSpace !== this.width) {
      this.refresh(true);
    }
  }
  updateModel(a) {
    this.detachListeners('tavm');
    a == null ? void 0 : a.ion({
      'name': 'tavm',
      'update': 'onModelUpdate',
      'thisObj': this
    });
  }
};
HorizontalTimeAxis._$name = 'HorizontalTimeAxis';
const ResourceHeader = class extends Widget {
  static configurable = {
    'resourceStore': null,
    'headerRenderer': null,
    'showAvatars': {
      'value': true,
      '$config': 'nullify'
    },
    'fillWidth': true,
    'fitWidth': false,
    'columnWidth': 150,
    'imagePath': null,
    'imageExtension': null,
    'defaultImageName': null,
    'availableWidth': null
  };
  static type = 'resourceheader';
  static $name = 'ResourceHeader';
  lastResource = -1;
  firstResource = -1;
  constructor() {
    super(...arguments);
  }
  construct(a) {
    const b = this;
    a.scheduler._resourceColumns = b;
    super.construct(a);
    if (b.imagePath != null) {
      b.element.classList.add('b-has-images');
    }
    EventHelper.on({
      'element': b.element,
      'delegate': '.b-resourceheader-cell',
      'capture': true,
      'click': 'onResourceMouseEvent',
      'dblclick': 'onResourceMouseEvent',
      'contextmenu': 'onResourceMouseEvent',
      'thisObj': b
    });
  }
  changeShowAvatars(a) {
    let b;
    (b = this.avatarRendering) == null ? void 0 : b.destroy();
    if (a) {
      this.avatarRendering = new AvatarRendering({ 'element': this.element });
    }
    return a;
  }
  updateShowAvatars() {
    if (!this.isConfiguring) {
      this.refresh();
    }
  }
  updateResourceStore(a) {
    const b = this;
    b.detachListeners('resourceStore');
    if (a) {
      a.ion({
        'name': 'resourceStore',
        'changePreCommit': 'onResourceStoreDataChange',
        'thisObj': b
      });
      if (a.count) {
        b.onResourceStoreDataChange({});
      }
    }
  }
  onResourceStoreDataChange({action: a}) {
    const b = this;
    b.getConfig('fillWidth');
    b.getConfig('fitWidth');
    b.updateWidthCache();
    const {element: c} = b, e = b.totalWidth;
    if (b.scheduler.variableColumnWidths) {
      b._fillWidth = b._fitWidth = false;
    } else {
      b._fillWidth = b.configuredFillWidth;
      b._fitWidth = b.configuredFitWidth;
    }
    if (e !== b.width) {
      DomHelper.setLength(c, 'width', e);
      b.column.set('width', e, b.column.grid.isConfiguring);
    }
    if (a === 'removeall') {
      c.innerHTML = '';
    }
    if (a === 'remove' || a === 'add' || a === 'filter' || b.fitWidth || b.fillWidth) {
      b.refreshWidths();
    }
    b.column.grid.toggleEmptyText();
  }
  get totalWidth() {
    return this.updateWidthCache();
  }
  updateWidthCache() {
    let a = 0;
    const {scheduler: b} = this;
    b.variableColumnWidths = false;
    b.resourceStore.forEach(c => {
      const e = c.instanceMeta(b);
      if (e.columnWidth == null && c.columnWidth) {
        e.columnWidth = c.columnWidth;
      }
      e.insetStart = a;
      e.insetEnd = a + b.getResourceWidth(c);
      if (e.columnWidth == null) {
        a += b.resourceColumnWidth;
      } else {
        a += e.columnWidth;
        b.variableColumnWidths = true;
      }
    });
    return a;
  }
  changeColumnWidth(a) {
    if (!this.refreshingWidths) {
      this.configuredColumnWidth = a;
    }
    return a;
  }
  updateColumnWidth(a, b) {
    const c = this;
    if (!c.refreshingWidths) {
      c.refreshWidths();
    }
    if (!c.isConfiguring) {
      if (c.resourceStore.isGrouped) {
        c.updateWidthCache();
      }
      c.refresh();
      c.trigger('columnWidthChange', {
        'width': a,
        'oldWidth': b
      });
    }
  }
  changeFillWidth(a) {
    return this.configuredFillWidth = a;
  }
  updateFillWidth() {
    if (!this.isConfiguring) {
      this.refreshWidths();
    }
  }
  changeFitWidth(a) {
    return this.configuredFitWidth = a;
  }
  updateFitWidth() {
    if (!this.isConfiguring) {
      this.refreshWidths();
    }
  }
  getImageURL(a) {
    return StringHelper.joinPaths([
      this.imagePath || '',
      a || ''
    ]);
  }
  updateImagePath() {
    if (!this.isConfiguring) {
      this.refresh();
    }
  }
  updateAvailableWidth(a) {
    this.refreshWidths();
  }
  refreshWidths() {
    let a;
    const b = this, {
        availableWidth: c,
        configuredColumnWidth: e
      } = b, f = (a = b.resourceStore) == null ? void 0 : a.count;
    if (!c || !f || b.scheduler.variableColumnWidths) {
      return;
    }
    b.refreshingWidths = true;
    const g = b.fitWidth || b.fillWidth && e * f < c, i = g ? Math.floor(c / f) : e, j = b.column.grid.enableEventAnimations && Math.abs(b._columnWidth - i) > 30;
    DomHelper.addTemporaryClass(b.element, 'b-animating', j ? 300 : 0, b);
    b.columnWidth = i;
    b.refreshingWidths = false;
  }
  set visibleResources({
    firstResource: a,
    lastResource: b
  }) {
    this.firstResource = a;
    this.lastResource = b;
    this.updateWidthCache();
    this.refresh();
  }
  refresh() {
    const a = this, {
        firstResource: b,
        scheduler: c,
        resourceStore: e,
        lastResource: f
      } = a, g = e.isGrouped && e.groupers[0].field;
    a.element.classList.toggle('b-grouped', Boolean(g));
    const i = a.renderResourceHeaders(b, f);
    DomSync.sync({
      'domConfig': {
        'onlyChildren': true,
        'children': i
      },
      'targetElement': a.element,
      'syncIdField': 'resourceId',
      'callback': ({
        jsx: j,
        targetElement: k,
        domConfig: l
      }) => {
        let n;
        (n = c.processResourceHeader) == null ? void 0 : n.call(c, {
          'jsx': j,
          'targetElement': k,
          'domConfig': l
        });
      }
    });
  }
  renderResourceHeaders(a, b) {
    let c;
    const e = this, {
        scheduler: f,
        resourceStore: g
      } = e, {variableColumnWidths: j} = f, k = g.isGrouped && g.groupers[0].field, l = [];
    if (!e.column.grid.isConfiguring && a > -1 && b > -1 && b < g.count) {
      let n;
      for (let o = a; o <= b; o++) {
        const p = g.allResourceRecords[o], q = p.instanceMeta(g).groupParent, r = q == null ? void 0 : q.groupChildren;
        if (k && q.id !== (n == null ? void 0 : n.dataset.resourceId)) {
          const A = r[0].instanceMeta(f).insetStart, B = r[r.length - 1].instanceMeta(f).insetEnd - A;
          n = {
            'className': 'b-resourceheader-group-cell',
            'dataset': { 'resourceId': q.id },
            'style': {
              'left': A,
              'width': B
            },
            'children': [
              {
                'tag': 'span',
                'text': r[0][k]
              },
              {
                'className': 'b-resourceheader-group-children',
                'children': []
              }
            ]
          };
          l.push(n);
        }
        const u = p.instanceMeta(f), v = u.columnWidth || e.columnWidth, w = k ? u.insetStart - n.style.left : j ? u.insetStart : o * e.columnWidth, z = {
            'className': new DomClassList({ 'b-resourceheader-cell': 1 }),
            'dataset': { 'resourceId': p.id },
            'style': {
              [f.rtl ? 'right' : 'left']: w,
              'width': v
            },
            'children': []
          };
        if (e.headerRenderer) {
          const C = e.headerRenderer({
            'elementConfig': z,
            'resourceRecord': p
          });
          if (C) {
            if (typeof C === 'string') {
              z.html = C;
            } else if (typeof C === 'object') {
              z.children = [C];
            }
          }
        } else {
          let E;
          if (p.imageUrl) {
            E = p.imageUrl;
          } else {
            if (e.imagePath != null) {
              if (p.image !== false) {
                const F = p.image || ((c = p.name) == null ? void 0 : c.toLowerCase()) + e.imageExtension;
                E = e.getImageURL(F);
              }
            }
          }
          z.children.push(e.showAvatars && e.avatarRendering.getResourceAvatar({
            'resourceRecord': p,
            'initials': p.initials,
            'color': p.eventColor,
            'iconCls': p.iconCls,
            'defaultImageUrl': e.defaultImageName && e.getImageURL(e.defaultImageName),
            'imageUrl': E
          }), {
            'tag': 'span',
            'className': 'b-resource-name',
            'html': StringHelper.encodeHtml(p.name)
          });
        }
        if (k) {
          n.children[1].children.push(z);
        } else {
          l.push(z);
        }
      }
    }
    return l;
  }
  onResourceMouseEvent(a) {
    const b = a.target.closest('.b-resourceheader-cell'), c = this.resourceStore.getById(b.dataset.resourceId);
    this.trigger('resourceHeader' + StringHelper.capitalize(a.type), {
      'resourceRecord': c,
      'event': a
    });
  }
  getCurrentConfig(a) {
    const b = super.getCurrentConfig(a);
    delete b.resourceStore;
    delete b.column;
    delete b.type;
    return b;
  }
};
ResourceHeader._$name = 'ResourceHeader';
const TimeAxisColumn = class extends Events_default(WidgetColumn) {
  static type = 'timeAxis';
  static $name = 'TimeAxisColumn';
  headerHoverable = false;
  constructor() {
    super(...arguments);
  }
  static get fields() {
    return [
      {
        'name': 'locked',
        'persist': false
      },
      {
        'name': 'flex',
        'persist': false
      },
      {
        'name': 'width',
        'persist': false
      },
      {
        'name': 'cellCls',
        'persist': false
      },
      {
        'name': 'field',
        'persist': false
      },
      'mode'
    ];
  }
  static get defaults() {
    return {
      'draggable': false,
      'groupable': false,
      'hideable': false,
      'showColumnPicker': false,
      'filterable': false,
      'sortable': false,
      'resizable': false,
      'searchable': false,
      'editor': false,
      'enableCellContextMenu': false,
      'tooltipRenderer': false,
      'cls': 'b-sch-timeaxiscolumn',
      'needWidth': true,
      'mode': null,
      'region': 'normal',
      'exportable': false,
      'htmlEncode': false,
      'id': 'timeaxiscolumn'
    };
  }
  construct(a) {
    const b = this;
    a.disableRendererWarning = true;
    super.construct(...arguments);
    b.thisObj = b;
    b.grid._timeAxisColumn = b;
    b.timeAxisViewModel = b.grid.timeAxisViewModel;
    b.mode = b.mode;
    b.grid.ion({
      'paint': 'onTimelinePaint',
      'thisObj': b,
      'once': true
    });
  }
  static get autoExposeFields() {
    return true;
  }
  doDestroy() {
    let a, b;
    (a = this.resourceColumns) == null ? void 0 : a.destroy();
    (b = this.timeAxisView) == null ? void 0 : b.destroy();
    super.doDestroy();
  }
  set mode(a) {
    const b = this, {grid: c} = b;
    b.set('mode', a);
    if (a === 'horizontal') {
      b.timeAxisView = new HorizontalTimeAxis({
        'model': b.timeAxisViewModel,
        'compactCellWidthThreshold': b.compactCellWidthThreshold,
        'owner': c,
        'client': c
      });
    } else if (a === 'vertical') {
      b.resourceColumns = ResourceHeader.new({
        'column': b,
        'scheduler': c,
        'resourceStore': c.resourceStore,
        'imagePath': c.resourceImagePath,
        'imageExtension': c.resourceImageExtension,
        'defaultImageName': c.defaultResourceImageName
      }, c.resourceColumns || {});
      b.relayEvents(b.resourceColumns, [
        'resourceheaderclick',
        'resourceheaderdblclick',
        'resourceheadercontextmenu'
      ]);
    }
  }
  get mode() {
    return this.get('mode');
  }
  onViewModelUpdate({source: a}) {
    const b = this;
    if (b.grid.timeAxisSubGrid.collapsed) {
      return;
    }
    if (b.mode === 'horizontal') {
      b.refreshHeader(true);
      b.width = a.totalSize;
      b.grid.refresh();
      b.subGrid.refreshFakeScroll();
    } else if (b.mode === 'vertical') {
      b.grid.refreshRows();
    }
  }
  onTimelinePaint({firstPaint: a}) {
    let b;
    const c = this;
    if (!c.subGrid.insertRowsBefore) {
      return;
    }
    if (a) {
      c.subGridElement.classList.add('b-timeline-subgrid');
      if (c.mode === 'vertical') {
        c.refreshHeader();
        (b = c.grid) == null ? void 0 : b.onHeightChange();
      }
    }
  }
  refreshHeader(a) {
    const b = this, {element: c} = b;
    if (c) {
      if (b.mode === 'horizontal') {
        !a && b.timeAxisViewModel.update(void 0, void 0, true);
        if (!b.timeAxisView.rendered) {
          c.innerHTML = '';
          b.timeAxisView.render(c);
        } else {
          b.timeAxisView.refresh(true);
        }
      } else if (b.mode === 'vertical') {
        if (!b.resourceColumns.currentElement) {
          c.innerHTML = '';
          b.resourceColumns.render(c);
        } else {
          b.resourceColumns.refresh();
        }
      }
    }
  }
  defaultRenderer(a) {
    let b;
    const {grid: c} = this;
    if (c.isTimelineHistogramBase) {
      return;
    }
    if (c.project.isInitialCommitPerformed || c.project.isDelayingCalculation || ((b = c.eventStore) == null ? void 0 : b.lazyLoad)) {
      c.currentOrientation.renderer(a);
      return super.defaultRenderer(a);
    }
    return '';
  }
  get timeAxisViewModel() {
    return this._timeAxisViewModel;
  }
  set timeAxisViewModel(a) {
    const b = this;
    b.detachListeners('tavm');
    a == null ? void 0 : a.ion({
      'name': 'tavm',
      'update': 'onViewModelUpdate',
      'prio': -10000,
      'thisObj': b
    });
    b._timeAxisViewModel = a;
    if (b.timeAxisView) {
      b.timeAxisView.model = a;
    }
  }
  getState() {
    const a = super.getState();
    delete a.width;
    delete a.flex;
    return a;
  }
};
ColumnStore.registerColumnType(TimeAxisColumn);
TimeAxisColumn._$name = 'TimeAxisColumn';
const VerticalTimeAxis = class extends TimeAxisBase {
  static get $name() {
    return 'VerticalTimeAxis';
  }
  static get configurable() {
    return {
      'cls': 'b-verticaltimeaxis',
      'sizeProperty': 'height',
      'positionProperty': 'top',
      'wrapText': true
    };
  }
  buildHorizontalCells() {
    const a = this, {client: b} = a, c = b == null ? void 0 : b.stickyHeaders, e = [], f = a.levels.reduce((g, j, k) => {
        let l;
        if (j.cells) {
          g.push(...(l = j.cells) == null ? void 0 : l.filter(n => n.start < a.endDate && n.end > a.startDate).map((n, o, p) => ({
            'role': 'presentation',
            'className': {
              'b-sch-header-timeaxis-cell': 1,
              [n.headerCellCls]: n.headerCellCls,
              ['b-align-' + n.align]: n.align,
              'b-last': o === p.length - 1,
              'b-lowest': k === a.levels.length - 1
            },
            'dataset': {
              'tickIndex': n.index,
              'cellId': k + '-' + n.index,
              'headerPosition': k,
              ...globalThis.DEBUG && { 'date': n.start.getTime() }
            },
            'style': {
              'top': n.coord,
              'height': n.width,
              'minHeight': n.width
            },
            'children': [{
                'role': 'presentation',
                'className': {
                  'b-sch-header-text': 1,
                  'b-sticky-header': c
                },
                'html': n.value
              }]
          })));
        }
        return g;
      }, []);
    b == null ? void 0 : b.getHeaderDomConfigs(e);
    f.push(...e);
    return {
      'className': a.widgetClassList,
      'dataset': {
        'headerFeature': 'headerRow0',
        'headerPosition': 0
      },
      'syncOptions': {
        'releaseThreshold': 5,
        'syncIdField': 'cellId'
      },
      'children': f
    };
  }
  get height() {
    return this.size;
  }
};
VerticalTimeAxis._$name = 'VerticalTimeAxis';
const VerticalTimeAxisColumn = class extends Column {
  static $name = 'VerticalTimeAxisColumn';
  static get type() {
    return 'verticalTimeAxis';
  }
  static get defaults() {
    return {
      'draggable': false,
      'groupable': false,
      'hideable': false,
      'showColumnPicker': false,
      'filterable': false,
      'sortable': false,
      'searchable': false,
      'editor': false,
      'enableCellContextMenu': false,
      'tooltipRenderer': false,
      'minWidth': 0,
      'resizable': false,
      'cellCls': 'b-verticaltimeaxiscolumn',
      'locked': true,
      'flex': 1,
      'alwaysClearCell': false
    };
  }
  get isFocusable() {
    return false;
  }
  construct(a) {
    super.construct(...arguments);
    this.view = new VerticalTimeAxis({
      'model': this.grid.timeAxisViewModel,
      'client': this.grid
    });
  }
  defaultRenderer({
    cellElement: a,
    size: b
  }) {
    this.view.render(a);
    b.height = this.view.height;
  }
  getCurrentConfig(a) {
    const b = super.getCurrentConfig(a);
    delete b.id;
    delete b.region;
    delete b.type;
    delete b.field;
    delete b.ariaLabel;
    delete b.cellAriaLabel;
    return b;
  }
};
ColumnStore.registerColumnType(VerticalTimeAxisColumn);
VerticalTimeAxisColumn._$name = 'VerticalTimeAxisColumn';
const descriptionFormats = {
  'month': 'MMMM, YYYY',
  'week': [
    'MMMM YYYY (Wp)',
    'S{MMM} - E{MMM YYYY} (S{Wp})'
  ],
  'day': 'MMMM D, YYYY'
};
const SchedulerBase = class extends TimelineBase.mixin(CrudManagerView_default, LazyLoadView_default, Describable_default, SchedulerDom_default, SchedulerDomEvents_default, SchedulerStores_default, SchedulerScroll_default, SchedulerState_default, SchedulerEventRendering_default, SchedulerRegions_default, EventSelection_default, EventNavigation_default, CurrentConfig_default, TransactionalFeatureMixin_default) {
  static configurable = {
    'date': {
      'value': null,
      '$config': { 'equal': 'date' }
    },
    'stepUnit': 'week',
    'range': 'week',
    'getDateConstraints': null,
    'verticalTimeAxisColumn': {},
    'createEventOnDblClick': true,
    'scrollBuffer': 0,
    'schedulableAreaSelector': '.b-sch-timeaxis-cell',
    'scheduledEventName': 'event',
    'sortFeatureStore': 'resourceStore',
    'showEventColorPickers': null,
    'updateTimelineContextOnScroll': true
  };
  static type = 'schedulerbase';
  static $name = 'SchedulerBase';
  resourceTimeRangeSelector = '.b-sch-resourcetimerange';
  timeCellSelector = '.b-sch-timeaxis-cell';
  constructor() {
    super(...arguments);
  }
  static get defaultConfig() {
    return {
      'mode': 'horizontal',
      'eventCls': 'b-sch-event',
      'timeCellCls': 'b-sch-timeaxis-cell',
      'overScheduledEventClass': 'b-sch-event-hover',
      'allowOverlap': true,
      'rowHeight': 60,
      'preCalculateHeightLimit': 10000,
      'crudManagerClass': CrudManager,
      'liveRedrawWhileMovingSplitterThreshold': 100,
      'testConfig': {
        'loadMaskError': {
          'autoClose': 10,
          'showDelay': 0
        }
      }
    };
  }
  afterConstruct() {
    const a = this;
    super.afterConstruct();
    a.ion({
      'scroll': 'onVerticalScroll',
      'thisObj': a
    });
    if (a.createEventOnDblClick) {
      a.ion({ 'scheduledblclick': a.onTimeAxisCellDblClick });
    }
    a.ion({ 'splitterDragStart': 'internalOnSplitterDragStart' });
  }
  onPaintOverride() {
  }
  get store() {
    return super.store;
  }
  set store(a) {
    super.store = a;
  }
  get visibleResources() {
    let a, b;
    const c = this;
    if (c.isVertical) {
      return c.currentOrientation.visibleResources;
    }
    return {
      'first': c.store.getById((a = c.firstVisibleRow) == null ? void 0 : a.id),
      'last': c.store.getById((b = c.lastVisibleRow) == null ? void 0 : b.id)
    };
  }
  onLocaleChange() {
    this.currentOrientation.onLocaleChange();
    super.onLocaleChange();
  }
  onTimeAxisCellDblClick({
    date: a,
    resourceRecord: b,
    row: c
  }) {
    this.createEvent(a, b, c);
  }
  onVerticalScroll({scrollTop: a}) {
    this.currentOrientation.updateFromVerticalScroll(a);
  }
  internalOnSplitterDragStart() {
    if (this.isHorizontal) {
      if (Object.keys(this.foregroundCanvas.syncIdMap).length > this.liveRedrawWhileMovingSplitterThreshold) {
        this.suspendRefresh();
        this.ion({
          'splitterDragEnd': () => this.resumeRefresh(true, false),
          'once': true
        });
      }
    }
  }
  onEventCreated(a) {
  }
  get isHorizontal() {
    return this.mode === 'horizontal';
  }
  get isVertical() {
    return this.mode === 'vertical';
  }
  get mode() {
    return this._mode;
  }
  set mode(a) {
    const b = this;
    b._mode = a;
    if (!b[a]) {
      b.element.classList.add('b-sch-' + a);
      if (a === 'horizontal') {
        b.horizontal = new HorizontalRendering(b);
        if (b.isPainted) {
          b.horizontal.init();
        }
      } else if (a === 'vertical') {
        b.vertical = new VerticalRendering(b);
        if (b.rendered) {
          b.vertical.init();
        }
      }
    }
  }
  get currentOrientation() {
    return this[this.mode];
  }
  onElementKeyDown(a) {
    return super.onElementKeyDown(a);
  }
  onElementKeyUp(a) {
    return super.onElementKeyUp(a);
  }
  onElementMouseOver(a) {
    return super.onElementMouseOver(a);
  }
  onElementMouseOut(a) {
    return super.onElementMouseOut(a);
  }
  processEventDrop() {
  }
  processCrossSchedulerEventDrop() {
  }
  beforeEventDragStart() {
  }
  afterEventDragStart() {
  }
  afterEventDragAbortFinalized() {
  }
  checkEventDragValidity() {
  }
  afterEventResizeStart() {
  }
  afterRenderEvent() {
  }
  get hasEventEditor() {
    return Boolean(this.eventEditingFeature);
  }
  get eventEditingFeature() {
    const {
      eventEdit: a,
      taskEdit: b,
      simpleEventEdit: c
    } = this.features;
    return (a == null ? void 0 : a.enabled) ? a : (b == null ? void 0 : b.enabled) ? b : (c == null ? void 0 : c.enabled) ? c : null;
  }
  async createEvent(a, b) {
    let c, e, f, g, i;
    const j = this, {
        enableEventAnimations: k,
        eventStore: l,
        assignmentStore: n,
        hasEventEditor: o
      } = j, p = [b], q = j.createEventOnDblClick.useEventModelDefaults, r = q ? (c = l.modelClass.defaultValues.duration) != null ? c : 1 : 1, u = q ? (e = l.modelClass.defaultValues.durationUnit) != null ? e : j.timeAxis.unit : j.timeAxis.unit, v = l.createRecord({
        'startDate': a,
        'endDate': DateHelper.add(a, r, u),
        'duration': r,
        'durationUnit': u,
        'name': j.L('L{Object.newEvent}')
      });
    if (j.readOnly || b.isSpecialRow || b.readOnly || !j.isDateRangeAvailable(v.startDate, v.endDate, null, b)) {
      return;
    }
    (f = j.eventEditingFeature) == null ? void 0 : f.captureStm(true);
    v.isCreating = o;
    j.onEventCreated(v);
    const w = n == null ? void 0 : n.assignEventToResource(v, b);
    if (j.trigger('beforeEventAdd', {
        'eventRecord': v,
        'resourceRecords': p,
        'assignmentRecords': w
      }) === false) {
      n == null ? void 0 : n.remove(w);
      (g = j.eventEditingFeature) == null ? void 0 : g.freeStm(false);
      return;
    }
    j.enableEventAnimations = false;
    const z = l.add(v);
    j.project.commitAsync().then(() => j.enableEventAnimations = k);
    if (!z) {
      n == null ? void 0 : n.remove(w);
      (i = j.eventEditingFeature) == null ? void 0 : i.freeStm(false);
      return;
    }
    j.isCreating = true;
    j.refreshRows();
    j.isCreating = false;
    await j.scrollEventIntoView(v);
    if (j.isDestroyed) {
      return;
    }
    j.trigger('eventAutoCreated', {
      'eventRecord': v,
      'resourceRecord': b
    });
    if (o) {
      j.editEvent(v, b, j.getEventElement(v));
    }
  }
  isDateRangeAvailable(a, b, c, e) {
    return this.eventStore.isDateRangeAvailable({
      'startDate': a,
      'endDate': b,
      'excludeEvent': c,
      'resourceRecord': e,
      'allowOverlap': this.allowOverlap
    });
  }
  async resumeRefresh(a = true, b = true) {
    super.resumeRefresh(false);
    const c = this;
    if (!c.refreshSuspended && a) {
      if (!c.isEngineReady) {
        c.currentOrientation.refreshAllWhenReady = true;
        return c.project.commitAsync();
      }
      if (!c.isDestroyed) {
        if (b) {
          c.refreshWithTransition();
        } else {
          c.refresh();
        }
      }
    }
  }
  refreshResource(a) {
    this.refreshResources([a]);
  }
  refreshResources(a) {
    this.currentOrientation.refreshResources(a);
  }
  toggleEmptyText() {
    let a;
    DomHelper.toggleClasses(this.element, 'b-grid-empty', !(this.resourceStore.count > 0 || ((a = this.crudManager) == null ? void 0 : a.isLoading)));
  }
  getRowHeight(a) {
    if (this.isHorizontal) {
      const b = this.currentOrientation.calculateRowHeight(a);
      this.rowManager.storeKnownHeight(a.id, b);
      return b;
    }
  }
  calculateRowHeights(a, b = false) {
    const {store: c} = this;
    for (const e of a) {
      if (e && c.isAvailable(e)) {
        this.getRowHeight(e);
      }
    }
    if (!b) {
      this.rowManager.estimateTotalHeight(true);
    }
  }
  calculateAllRowHeights(a = false) {
    const {
        store: b,
        rowManager: c
      } = this, e = Math.min(b.count, this.preCalculateHeightLimit);
    if (e) {
      c.clearKnownHeights();
      for (let f = 0; f < e; f++) {
        const g = b.records[f];
        if (!g) {
          continue;
        }
        this.getRowHeight(g);
      }
      if (!a) {
        c.estimateTotalHeight(true);
      }
    }
  }
  get dateBounds() {
    const a = this, b = [a.startDate];
    if (a.range === 'week') {
      b.push(a.lastDate);
    }
    return b;
  }
  get defaultDescriptionFormat() {
    return descriptionFormats[this.range];
  }
  get lastDate() {
    const a = this.endDate;
    return a && DateHelper.add(a, -1, 'day');
  }
  getEventRecord(a) {
    a = DomHelper.getEventElement(a);
    return this.resolveEventRecord(a);
  }
  getResourceRecord(a) {
    return this.resolveResourceRecord(a);
  }
  getEventElement(a) {
    return this.getElementFromEventRecord(a);
  }
  changeRange(a) {
    return DateHelper.normalizeUnit(a);
  }
  updateRange(a) {
    if (!this.isConfiguring) {
      const b = this.date, c = this.date = DateHelper.startOf(b, a);
      if (b.getTime() === c.getTime()) {
        this.updateDate(c);
      }
    }
  }
  changeStepUnit(a) {
    return DateHelper.normalizeUnit(a);
  }
  updateDate(a) {
    const b = this, c = DateHelper.startOf(a, b.range);
    b.setTimeSpan(c, DateHelper.add(c, 1, b.range));
    b.visibleDate = {
      'date': DateHelper.max(a, b.timeAxis.startDate),
      'block': 'start',
      'animate': b.isPainted
    };
    b.trigger('descriptionChange');
  }
  updateScrollBuffer(a) {
    if (!this.isConfiguring) {
      this.currentOrientation.scrollBuffer = a;
    }
  }
  previous() {
    this.date = DateHelper.add(this.date, -1, this.stepUnit);
  }
  next() {
    this.date = DateHelper.add(this.date, 1, this.stepUnit);
  }
  async scheduleEvent({
    startDate: a,
    eventRecord: b,
    resourceRecord: c,
    element: e
  }) {
    const f = this;
    if (!f.eventStore.includes(b)) {
      [b] = f.eventStore.add(b);
    }
    b.beginBatch();
    b.startDate = a;
    b.assign(c);
    b.endBatch();
    if (e) {
      const g = Rectangle.from(e, f.foregroundCanvas);
      DomHelper.setTranslateXY(e, 0, 0);
      DomHelper.setTopLeft(e, g.y, g.x);
      DomSync.addChild(f.foregroundCanvas, e, b.assignments[0].id);
    }
    await f.project.commitAsync();
    return b;
  }
  shouldHover(a) {
    let b;
    return ((b = this.features.eventDrag) == null ? void 0 : b.isDragging) || super.shouldHover(a);
  }
};
SchedulerBase.initClass();
SchedulerBase._$name = 'SchedulerBase';
const EventColorPicker = class extends ColorPicker {
  static configurable = {
    'colorClasses': SchedulerBase.eventColors,
    'colorClassPrefix': 'b-sch-',
    'colors': null,
    'record': null
  };
  static type = 'eventcolorpicker';
  static $name = 'EventColorPicker';
  colorSelected({color: a}) {
    if (this.record) {
      this.record.eventColor = a;
    }
  }
};
EventColorPicker.initClass();
EventColorPicker._$name = 'EventColorPicker';
const EventColorColumn = class extends ColorColumn {
  static defaults = {
    'colorEditorType': 'eventcolorpicker',
    'field': 'eventColor'
  };
  static type = 'eventcolor';
  static $name = 'EventColorColumn';
};
ColumnStore.registerColumnType(EventColorColumn);
EventColorColumn._$name = 'EventColorColumn';
const ResourceCollapseColumn = class extends Column {
  static get $name() {
    return 'ResourceCollapseColumn';
  }
  static get type() {
    return 'resourceCollapse';
  }
  static get defaults() {
    return {
      'width': '3em',
      'align': 'center',
      'sortable': false,
      'groupable': false,
      'editor': false,
      'minWidth': 0,
      'cellCls': 'b-resourcecollapse-cell',
      'renderer': ({record: a}) => ({
        'tag': 'i',
        'class': {
          'b-icon': 1,
          'b-icon-expand-resource': 1,
          'b-flip': a.eventLayout !== 'none'
        }
      })
    };
  }
  onCellClick({
    record: a,
    event: b
  }) {
    if (!a.isSpecialRow) {
      b.preventDefault();
      a.eventLayout = a.eventLayout !== 'none' ? 'none' : 'stack';
    }
  }
};
ColumnStore.registerColumnType(ResourceCollapseColumn);
ResourceCollapseColumn._$name = 'ResourceCollapseColumn';
const ResourceInfoColumn = class extends Column {
  static get $name() {
    return 'ResourceInfoColumn';
  }
  static get type() {
    return 'resourceInfo';
  }
  static get fields() {
    return [
      'showEventCount',
      'showRole',
      'showMeta',
      'showImage',
      'autoScaleThreshold',
      'useNameAsImageName'
    ];
  }
  static get defaults() {
    return {
      'showImage': true,
      'showEventCount': true,
      'showMeta': null,
      'showRole': false,
      'autoScaleThreshold': 40,
      'useNameAsImageName': true,
      'field': 'name',
      'width': 140,
      'cellCls': 'b-resourceinfo-cell',
      'editor': VersionHelper.isTestEnv ? false : 'text'
    };
  }
  construct(...a) {
    super.construct(...a);
    this.avatarRendering = new AvatarRendering({ 'element': this.grid.element });
  }
  doDestroy() {
    super.doDestroy();
    this.avatarRendering.destroy();
  }
  getImageURL(a) {
    const b = this.grid.resourceImagePath || '', c = b.split('//'), e = c.length > 1 ? c[1] : b, f = StringHelper.joinPaths([
        e || '',
        a || ''
      ]);
    return c.length > 1 ? c[0] + '//' + f : f;
  }
  template(a, b) {
    const c = this, {
        showImage: e,
        showRole: f,
        showMeta: g,
        showEventCount: i,
        grid: j
      } = c, {
        timeAxis: k,
        resourceImageExtension: resourceImageExtension = '',
        defaultResourceImageName: l
      } = j, n = typeof f === 'string' ? f : 'role', o = i && a.eventStore.getEvents({
        'includeOccurrences': j.enableRecurringEvents,
        'resourceRecord': a,
        'startDate': k.startDate,
        'endDate': k.endDate
      }).length, p = a.showAvatar !== false;
    let q;
    if (p && e && a.image !== false) {
      if (a.imageUrl) {
        q = a.imageUrl;
      } else {
        const r = typeof e === 'string' ? e : a.image || b && c.useNameAsImageName && b.toLowerCase() + resourceImageExtension || l || '';
        q = r && c.getImageURL(r);
        if (q && !r.includes('.')) {
          q += resourceImageExtension;
        }
      }
    }
    return {
      'class': {
        'b-resource-info': 1,
        'b-no-avatar': !p
      },
      'children': [
        e && p && c.avatarRendering.getResourceAvatar({
          'resourceRecord': a,
          'initials': a.initials,
          'color': a.eventColor,
          'iconCls': a.iconCls,
          'imageUrl': q,
          'defaultImageUrl': l && this.getImageURL(l)
        }),
        f || i || g ? {
          'tag': 'dl',
          'children': [
            {
              'tag': 'dt',
              'text': b
            },
            f ? {
              'tag': 'dd',
              'class': 'b-resource-role',
              'text': a.getValue(n)
            } : null,
            i ? {
              'tag': 'dd',
              'class': 'b-resource-events',
              'text': c.L('L{eventCountText}', o)
            } : null,
            g ? {
              'tag': 'dd',
              'class': 'b-resource-meta',
              'html': c.showMeta(a)
            } : null
          ]
        } : b
      ]
    };
  }
  defaultRenderer({
    grid: a,
    record: b,
    cellElement: c,
    value: e,
    isExport: f
  }) {
    let g;
    if (b.isSpecialRow) {
      g = '';
    } else if (f) {
      g = e;
    } else {
      if (this.autoScaleThreshold && a.rowHeight < this.autoScaleThreshold) {
        c.style.fontSize = a.rowHeight / 40 + 'em';
      } else {
        c.style.fontSize = '';
      }
      g = this.template(b, e);
    }
    return g;
  }
};
ColumnStore.registerColumnType(ResourceInfoColumn);
ResourceInfoColumn._$name = 'ResourceInfoColumn';
const ScaleColumn = class extends Column {
  static defaults = {
    'text': '\xA0',
    'width': 40,
    'minWidth': 40,
    'field': 'scalePoints',
    'cellCls': 'b-scale-cell',
    'editor': false,
    'sortable': false,
    'groupable': false,
    'filterable': false,
    'alwaysClearCell': false,
    'scalePoints': null
  };
  static fields = ['scalePoints'];
  static isScaleColumn = true;
  static type = 'scale';
  static $name = 'ScaleColumn';
  onDestroy() {
    this.scaleWidget.destroy();
  }
  set width(a) {
    super.width = a;
    this.scaleWidget.width = a;
  }
  get width() {
    return super.width;
  }
  applyValue(a, b, c) {
    if (b === 'scalePoints') {
      this.scaleWidget[b] = c;
    }
    return super.applyValue(...arguments);
  }
  buildScaleWidget() {
    const a = this;
    const b = new Scale({
      'owner': a.grid,
      'appendTo': a.grid.floatRoot,
      'cls': 'b-hide-offscreen',
      'align': 'right',
      'scalePoints': a.scalePoints,
      'monitorResize': false
    });
    Object.defineProperties(b, {
      'width': {
        'get'() {
          return a.width;
        },
        'set'(c) {
          this.element.style.width = c + 'px';
          this._width = a.width;
        }
      },
      'height': {
        'get'() {
          return this._height;
        },
        'set'(c) {
          this.element.style.height = c + 'px';
          this._height = c;
        }
      }
    });
    b.width = a.width;
    return b;
  }
  get scaleWidget() {
    const a = this;
    if (!a._scaleWidget) {
      a._scaleWidget = a.buildScaleWidget();
    }
    return a._scaleWidget;
  }
  defaultRenderer({
    cellElement: a,
    value: b,
    scaleWidgetConfig: c,
    scaleWidget: scaleWidget = this.scaleWidget
  }) {
    ObjectHelper.assign(scaleWidget, {
      'scalePoints': b || this.scalePoints,
      'height': this.grid.rowHeight
    }, c);
    scaleWidget.refresh();
    const e = scaleWidget.element.cloneNode(true);
    e.removeAttribute('id');
    e.classList.remove('b-hide-offscreen');
    a.innerHTML = '';
    a.appendChild(e);
  }
};
ColumnStore.registerColumnType(ScaleColumn);
ScaleColumn._$name = 'ScaleColumn';
const RecurrenceLegend = class extends Localizable_default() {
  static get $name() {
    return 'RecurrenceLegend';
  }
  static get allDaysValueAsArray() {
    return [
      'SU',
      'MO',
      'TU',
      'WE',
      'TH',
      'FR',
      'SA'
    ];
  }
  static get allDaysValue() {
    return this.allDaysValueAsArray.join(',');
  }
  static get workingDaysValue() {
    return this.allDaysValueAsArray.filter((a, b) => !DateHelper.nonWorkingDays[b]).join(',');
  }
  static get nonWorkingDaysValue() {
    return this.allDaysValueAsArray.filter((a, b) => DateHelper.nonWorkingDays[b]).join(',');
  }
  static getLegend(a, b) {
    const c = this, {
        timeSpan: e,
        interval: f,
        days: g,
        monthDays: i,
        months: j,
        positions: k
      } = a, l = b || e.startDate, n = { 'interval': f };
    let o;
    switch (a.frequency) {
    case 'DAILY':
      return f === 1 ? c.L('L{Daily}') : c.L('L{Every {0} days}', n);
    case 'WEEKLY':
      if (g && g.length) {
        n.days = c.getDaysLegend(g);
      } else if (l) {
        n.days = DateHelper.getDayName(l.getDay());
      }
      return c.L(f === 1 ? 'L{Weekly on {1}}' : 'L{Every {0} weeks on {1}}', n);
    case 'MONTHLY':
      if (g && g.length && k && k.length) {
        n.days = c.getDaysLegend(g, k);
      } else if (i && i.length) {
        i.sort((p, q) => p - q);
        n.days = c.arrayToText(i);
      } else if (l) {
        n.days = l.getDate();
      }
      return c.L(f === 1 ? 'L{Monthly on {1}}' : 'L{Every {0} months on {1}}', n);
    case 'YEARLY':
      if (g && g.length && k && k.length) {
        n.days = c.getDaysLegend(g, k);
      } else {
        n.days = l.getDate();
      }
      if (j && j.length) {
        j.sort((p, q) => p - q);
        if (j.length > 2) {
          o = p => DateHelper.getMonthShortName(p - 1);
        } else {
          o = p => DateHelper.getMonthName(p - 1);
        }
        n.months = c.arrayToText(j, o);
      } else {
        n.months = DateHelper.getMonthName(l.getMonth());
      }
      return c.L(f === 1 ? 'L{Yearly on {1} of {2}}' : 'L{Every {0} years on {1} of {2}}', n);
    }
  }
  static getDaysLegend(a, b) {
    const c = this, e = { 'position': '' };
    let f;
    if (b && b.length) {
      e.position = c.arrayToText(b, g => c.L('L{position' + g + '}'));
    }
    if (a.length) {
      a.sort((g, i) => RecurrenceDayRuleEncoder.decodeDay(g)[0] - RecurrenceDayRuleEncoder.decodeDay(i)[0]);
      switch (a.join(',')) {
      case c.allDaysValue:
        e.days = c.L('L{day}');
        break;
      case c.workingDaysValue:
        e.days = c.L('L{weekday}');
        break;
      case c.nonWorkingDaysValue:
        e.days = c.L('L{weekend day}');
        break;
      default:
        if (a.length > 2) {
          f = g => DateHelper.getDayShortName(RecurrenceDayRuleEncoder.decodeDay(g)[0]);
        } else {
          f = g => DateHelper.getDayName(RecurrenceDayRuleEncoder.decodeDay(g)[0]);
        }
        e.days = c.arrayToText(a, f);
      }
    }
    return c.L('L{daysFormat}', e);
  }
  static arrayToText(a, b) {
    if (b) {
      a = a.map(b);
    }
    return a.join(', ').replace(/,(?=[^,]*$)/, this.L('L{ and }'));
  }
};
RecurrenceLegend._$name = 'RecurrenceLegend';
const ClockTemplate = class extends Base {
  static get defaultConfig() {
    return {
      'minuteHeight': 8,
      'minuteTop': 2,
      'hourHeight': 8,
      'hourTop': 2,
      'handLeft': 10,
      'div': document.createElement('div'),
      'scheduler': null,
      'template'(a) {
        return '<div class="b-sch-clockwrap b-sch-clock-' + (a.mode || this.mode) + ' ' + (a.cls || '') + '">\n                    <div class="b-sch-clock">\n                        <div class="b-sch-hour-indicator">' + (a.date ? DateHelper.format(a.date, 'MMM') : '') + '</div>\n                        <div class="b-sch-minute-indicator">' + (a.date ? DateHelper.format(a.date, 'D') : '') + '</div>\n                        <div class="b-sch-clock-dot"></div>\n                    </div>\n                    <span class="b-sch-clock-text">' + StringHelper.encodeHtml(a.text || '') + '</span>\n                </div>';
      }
    };
  }
  generateContent(a) {
    return this.div.innerHTML = this.template(a);
  }
  updateDateIndicator(a, b) {
    const c = a == null ? void 0 : a.querySelector('.b-sch-hour-indicator'), e = a == null ? void 0 : a.querySelector('.b-sch-minute-indicator');
    if (b && c && e && BrowserHelper.isBrowserEnv) {
      if (this.mode === 'hour') {
        c.style.transform = 'rotate(' + b.getHours() % 12 * 30 + 'deg)';
        e.style.transform = 'rotate(' + b.getMinutes() * 6 + 'deg)';
      } else {
        c.style.transform = 'none';
        e.style.transform = 'none';
      }
    }
  }
  set mode(a) {
    this._mode = a;
  }
  get mode() {
    if (this._mode) {
      return this._mode;
    }
    const a = DateHelper.compareUnits(this.scheduler.timeAxisViewModel.timeResolution.unit, 'day') < 0, b = DateHelper.formatContainsHourInfo(this.scheduler.displayDateFormat);
    return a && b ? 'hour' : 'day';
  }
  set template(a) {
    this._template = a;
  }
  get template() {
    return this._template;
  }
};
ClockTemplate._$name = 'ClockTemplate';
const DragBase = class extends InstancePlugin {
  static get defaultConfig() {
    return {
      'tooltipTemplate': a => '\n                <div class="b-sch-tip-' + (a.valid ? 'valid' : 'invalid') + '">\n                    ' + a.startClockHtml + '\n                    ' + a.endClockHtml + '\n                    <div class="b-sch-tip-message">' + a.message + '</div>\n                </div>\n            ',
      'showTooltip': true,
      'showExactDropPosition': false,
      'store': null,
      'dragHelperConfig': null,
      'tooltipCls': 'b-eventdrag-tooltip',
      'allowNonWorkingTimeSNET': null
    };
  }
  static get configurable() {
    return {
      'constrainDragToTimeline': true,
      'constrainDragToResource': true,
      'constrainDragToTimeSlot': false,
      'tip': {
        '$config': [
          'lazy',
          'nullify'
        ],
        'value': {
          'align': {
            'align': 'b-t',
            'allowTargetOut': true
          },
          'autoShow': true,
          'updateContentOnMouseMove': true
        }
      },
      'throttleDragEvent': true
    };
  }
  static get pluginConfig() {
    return { 'chain': ['onInternalPaint'] };
  }
  internalSnapToPosition(a) {
    let b;
    const {dragData: c} = this;
    (b = this.snapToPosition) == null ? void 0 : b.call(this, {
      'assignmentRecord': c.assignmentRecord,
      'eventRecord': c.eventRecord,
      'resourceRecord': c.newResource || c.resourceRecord,
      'startDate': c.startDate,
      'endDate': c.endDate,
      'snapTo': a
    });
  }
  buildDragHelperConfig() {
    const a = this, {
        client: b,
        constrainDragToTimeline: c,
        constrainDragToResource: e,
        constrainDragToTimeSlot: f,
        dragHelperConfig: dragHelperConfig = {}
      } = a, {
        timeAxisViewModel: g,
        isHorizontal: i
      } = b, j = i ? e : f, k = i ? f : e;
    if (a.externalDropTargetSelector) {
      dragHelperConfig.dropTargetSelector = '.b-timeaxissubgrid,' + a.externalDropTargetSelector;
    }
    return Objects.merge({
      'name': a.constructor.name,
      'positioning': 'inset',
      'rtlSource': b,
      'lockX': k,
      'lockY': j,
      'minX': true,
      'maxX': true,
      'constrain': false,
      'cloneTarget': !c,
      'removeProxyAfterDrop': false,
      'dragWithin': c ? null : b.floatRoot,
      'hideOriginalElement': true,
      'dropTargetSelector': '.b-timelinebase',
      'allowDropOutside': !c,
      'dropTargetCls': a.externalDropTargetSelector ? 'b-drop-target' : '',
      'outerElement': b.timeAxisSubGridElement,
      'targetSelector': b.eventSelector,
      'scrollManager': c ? b.scrollManager : null,
      'createProxy': l => a.createProxy(l),
      'snapCoordinates': ({
        element: l,
        newX: n,
        newY: o
      }) => {
        const {dragData: p} = a;
        if (b && a.constrainDragToTimeline && !a.constrainDragToTimeSlot && (a.showExactDropPosition || g.snap)) {
          const {timeAxis: r} = b, u = p.draggedEntities[0].event || p.draggedEntities[0], {calendar: v} = u, {startDate: w} = p, z = a.getCoordinate(u, l, [
              n,
              o
            ]);
          let A;
          if (b.fillTicks && r.isContinuous) {
            A = b.snap ? r.roundDate(w) : w;
          } else {
            A = g.getDateFromPosition(z, 'round');
            if (b.snapRelativeToEventStartDate || b.snap && b.fillTicks) {
              A = r.roundDate(A, w);
            }
          }
          if (!v || A && v.isWorkingTime(A, DateHelper.add(A, u.fullDuration))) {
            const B = A && g.getPositionFromDate(A);
            if (A && A >= b.startDate && B != null) {
              if (i) {
                n = B;
              } else {
                o = B;
              }
            }
          }
        }
        const q = {
          'x': n,
          'y': o
        };
        a.internalSnapToPosition(q);
        return q;
      },
      'internalListeners': {
        'beforedragstart': 'onBeforeDragStart',
        'dragstart': 'onDragStart',
        'afterdragstart': 'onAfterDragStart',
        'drag': 'onDrag',
        'drop': 'onDrop',
        'abort': 'onDragAbort',
        'abortFinalized': 'onDragAbortFinalized',
        'reset': 'onDragReset',
        'thisObj': a
      }
    }, dragHelperConfig, {
      'isElementDraggable': (l, n) => {
        return (!dragHelperConfig || !dragHelperConfig.isElementDraggable || dragHelperConfig.isElementDraggable(l, n)) && a.isElementDraggable(l, n);
      }
    });
  }
  onInternalPaint({firstPaint: a}) {
    let b;
    const c = this, {client: e} = c;
    (b = c.drag) == null ? void 0 : b.destroy();
    c.drag = DragHelper.new(c.buildDragHelperConfig());
    if (a) {
      e.rowManager.ion({
        'changeTotalHeight': () => {
          let f;
          return c.updateYConstraint((f = c.dragData) == null ? void 0 : f[e.scheduledEventName + 'Record']);
        },
        'thisObj': c
      });
    }
    if (c.showTooltip) {
      c.clockTemplate = new ClockTemplate({ 'scheduler': e });
    }
  }
  doDestroy() {
    let a, b, c;
    (a = this.drag) == null ? void 0 : a.destroy();
    (b = this.clockTemplate) == null ? void 0 : b.destroy();
    (c = this.tip) == null ? void 0 : c.destroy();
    super.doDestroy();
  }
  get tipId() {
    return this.client.id + '-event-drag-tip';
  }
  changeTip(a, b) {
    const c = this;
    if (a) {
      const e = Tooltip.reconfigure(b, Tooltip.mergeConfigs({
        'forElement': c.element,
        'id': c.tipId,
        'getHtml': c.getTipHtml.bind(c),
        'cls': c.tooltipCls,
        'owner': c.client
      }, a), {
        'owner': c.client,
        'defaults': { 'type': 'tooltip' }
      });
      e.ion({
        'innerHtmlUpdate': 'updateDateIndicator',
        'thisObj': c
      });
      return e;
    } else {
      b == null ? void 0 : b.destroy();
    }
  }
  createProxy(a) {
    const b = a.cloneNode(true);
    delete b.id;
    b.classList.add('b-sch-' + this.client.mode);
    return b;
  }
  onBeforeDragStart({
    context: a,
    event: b
  }) {
    let c;
    const e = this, {client: f} = e, g = e.getMinimalDragData(a, b), i = g == null ? void 0 : g[f.scheduledEventName + 'Record'], j = g.resourceRecord;
    if (f.readOnly || e.disabled || !i || i.isDraggable === false || i.readOnly || (j == null ? void 0 : j.readOnly)) {
      return false;
    }
    a.pointerStartDate = f.getDateFromCoord({
      'coord': f.isVertical ? a.startPageY : a.startPageX,
      'local': false
    });
    const k = e.triggerBeforeEventDrag('before' + f.capitalizedEventName + 'Drag', {
      ...g,
      'event': b,
      'context': {
        ...a,
        ...g
      }
    }) !== false;
    if (k) {
      e.updateYConstraint(i, j);
      (c = f['before' + f.capitalizedEventName + 'DragStart']) == null ? void 0 : c.call(f, a, g);
    }
    return k;
  }
  onAfterDragStart({
    context: a,
    event: b
  }) {
  }
  get isDragging() {
    let a;
    return (a = this.drag) == null ? void 0 : a.isDragging;
  }
  get isActivelyDragging() {
    return this.isDragging && !this.finalizing;
  }
  onDragStart({
    context: a,
    event: b
  }) {
    let c, e, f;
    const g = this, i = (c = g.findClientFromTarget(b, a)) != null ? c : g.client;
    g.currentOverClient = i;
    g.drag.unifiedProxy = g.unifiedDrag;
    g.onMouseOverNewTimeline(i, true);
    const j = g.dragData = g.getDragData(a);
    g.suspendElementRedrawing(a.element);
    if (g.showTooltip && g.tip) {
      const o = j.context.dragProxy ? j.context.dragProxy.firstChild : a.element;
      g.tip.showBy(o);
    }
    g.triggerDragStart(j);
    (e = i['after' + i.capitalizedEventName + 'DragStart']) == null ? void 0 : e.call(i, a, j);
    const {
        eventMenu: k,
        taskMenu: l
      } = i.features, n = k || l;
    (f = n == null ? void 0 : n.hideContextMenu) == null ? void 0 : f.call(n, false);
  }
  updateDateIndicator() {
    const {
        startDate: a,
        endDate: b
      } = this.dragData, {
        tip: c,
        clockTemplate: e
      } = this, f = c.element.querySelector('.b-sch-tooltip-enddate');
    e.updateDateIndicator(c.element, a);
    f && e.updateDateIndicator(f, b);
  }
  findClientFromTarget(a, b) {
    let {target: c} = a;
    if (/^touch/.test(a.type)) {
      const f = Rectangle.from(b.element, null, true).center;
      c = DomHelper.elementFromPoint(f.x, f.y);
    }
    const e = Widget.fromElement(c, 'timelinebase');
    return (e == null ? void 0 : e.isResourceHistogram) ? null : e;
  }
  onDrag({
    context: a,
    event: b
  }) {
    const c = this, e = c.dragData, f = e.startDate;
    let g;
    if (c.constrainDragToTimeline) {
      g = c.client;
    } else {
      g = c.findClientFromTarget(b, e.context);
    }
    c.updateDragContext(a, b);
    if (!g) {
      return;
    }
    if (g !== c.currentOverClient) {
      c.onMouseOverNewTimeline(g);
    }
    if (e.dirty || !c.throttleDragEvent) {
      const i = e.valid;
      c.triggerEventDrag(e, f);
      if (i !== e.valid) {
        e.context.valid = e.externalDragValidity = e.valid;
      }
    }
    if (c.showTooltip && c.tip) {
      c.tip.lastAlignSpec.allowTargetOut = !e.valid;
      c.tip.realign();
    }
  }
  onMouseOverNewTimeline(a, b) {
    const c = this, {
        drag: {
          lockX: e,
          lockY: f
        }
      } = c, g = [];
    c.currentOverClient.element.classList.remove('b-dragging-' + c.currentOverClient.scheduledEventName);
    a.element.classList.add('b-dragging-' + a.scheduledEventName);
    if (!b) {
      c.currentOverClient.scrollManager.stopMonitoring();
    }
    if (!e) {
      g.push({
        'element': a.timeAxisSubGrid.scrollable.element,
        'direction': 'horizontal'
      });
    }
    if (!f) {
      g.push({
        'element': a.scrollable.element,
        'direction': 'vertical'
      });
    }
    a.scrollManager.startMonitoring({
      'scrollables': g,
      'callback': c.drag.onScrollManagerScrollCallback
    });
    c.currentOverClient = a;
  }
  triggerBeforeEventDropFinalize(a, b, c) {
    c.trigger(a, b);
  }
  onDrop({
    context: a,
    event: b
  }) {
    let c;
    const e = this, {
        currentOverClient: f,
        dragData: g
      } = e;
    let i = false;
    f == null ? void 0 : f.scrollManager.stopMonitoring();
    (c = e.tip) == null ? void 0 : c.hide();
    a.valid = a.valid && e.isValidDrop(g);
    e.drag.removeProxyAfterDrop = Boolean(g.externalDropTarget);
    if (a.valid && g.startDate && g.endDate) {
      let j = false;
      g.finalize = async k => {
        if (j || g.async) {
          await e.finalize(k);
        } else {
          a.valid = a.valid && k;
        }
      };
      e.triggerBeforeEventDropFinalize('before' + f.capitalizedEventName + 'DropFinalize', {
        'context': g,
        'domEvent': b
      }, f);
      j = true;
      a.async = g.async;
      if (!a.async && !g.externalDropTarget) {
        i = g.startDate - g.origStart !== 0 || g.newResource !== g.resourceRecord;
      }
    }
    if (!a.async) {
      e.finalize(g.valid && a.valid && i);
    }
  }
  onDragAbort({context: a}) {
    let b, c;
    const e = this;
    e.isAborting = true;
    (b = e.currentOverClient) == null ? void 0 : b.scrollManager.stopMonitoring();
    e.client.currentOrientation.onDragAbort({
      'context': a,
      'dragData': e.dragData
    });
    e.resetDraggedElements();
    (c = e.tip) == null ? void 0 : c.hide();
    e.triggerDragAbort(e.dragData);
  }
  onDragAbortFinalized({context: a}) {
    let b, c;
    const e = this;
    e.triggerDragAbortFinalized(e.dragData);
    (c = (b = e.client)['after' + e.client.capitalizedEventName + 'DragAbortFinalized']) == null ? void 0 : c.call(b, a, e.dragData);
    e.isAborting = false;
  }
  onDragReset({source: a}) {
    let b;
    const c = this, e = c.currentOverClient;
    if ((b = a.context) == null ? void 0 : b.started) {
      c.resetDraggedElements();
      e.trigger(e.scheduledEventName + 'DragReset');
    }
    e == null ? void 0 : e.element.classList.remove('b-dragging-' + e.scheduledEventName);
    c.dragData = null;
  }
  resetDraggedElements() {
    const {dragData: a} = this, {
        eventBarEls: b,
        draggedEntities: c
      } = a;
    this.resumeRecordElementRedrawing(a.record);
    c.forEach((e, f) => {
      this.resumeRecordElementRedrawing(e);
      b[f].classList.remove(this.drag.draggingCls);
      b[f].retainElement = false;
    });
    a.context.element.retainElement = false;
  }
  onInternalInvalidDrop(a) {
    let b;
    const c = this, {context: e} = c.drag;
    (b = c.tip) == null ? void 0 : b.hide();
    c.triggerAfterDrop(c.dragData, false);
    e.valid = false;
    if (a) {
      c.drag.abort();
    }
  }
  async finalize(a) {
    const b = this, {
        dragData: c,
        currentOverClient: e
      } = b, f = e.features.taskTooltip || e.features.eventTooltip;
    if (!c || b.finalizing) {
      return;
    }
    const {
      context: g,
      draggedEntities: i,
      externalDropTarget: j
    } = c;
    let k;
    b.finalizing = true;
    i.forEach((l, n) => {
      b.resumeRecordElementRedrawing(l);
      c.eventBarEls[n].classList.remove(b.drag.draggingCls);
      c.eventBarEls[n].retainElement = false;
    });
    g.element.retainElement = false;
    if (j && c.valid || a) {
      k = b.updateRecords(c);
      if (!j && Objects.isPromise(k)) {
        g.async = true;
        await k;
      }
      if (!c.valid) {
        b.onInternalInvalidDrop(true);
      } else {
        if (g.async) {
          g.finalize();
        }
        if (j) {
          b.client.refreshRows(null, false);
        }
        b.triggerAfterDrop(c, true);
      }
    } else {
      b.onInternalInvalidDrop(g.async || c.async);
    }
    b.finalizing = false;
    if (f == null ? void 0 : f.enabled) {
      f.disabled = true;
      e.setTimeout(() => {
        f.disabled = false;
      }, 200);
    }
    return k;
  }
  getEventNewStartEndDates(a, b) {
    let c = this.adjustStartDate(a.startDate, b);
    let f;
    if (a.graph && !this.allowNonWorkingTimeSNET && (!a.manuallyScheduled || a.project.skipNonWorkingTimeWhenSchedulingManually)) {
      try {
        c = a.run('skipNonWorkingTime', c);
        f = a.run('calculateProjectedXDateWithDuration', c, true, a.duration);
      } catch (g) {
        return { 'valid': false };
      }
    } else {
      f = DateHelper.add(c, a.fullDuration);
    }
    return {
      'startDate': c,
      'endDate': f
    };
  }
  updateDragContext(a, b) {
    let c, e, f;
    const g = this, {drag: i} = g, j = g.dragData, k = g.currentOverClient, {
        isHorizontal: l,
        timeAxis: n
      } = k, [o] = j.draggedEntities, p = o.isAssignment ? o.event : o, q = j.startDate, r = g.constrainDragToTimeSlot || (l ? i.lockX : i.lockY);
    j.browserEvent = b;
    Object.assign(j, g.getProductDragContext(j));
    if (r) {
      j.timeDiff = 0;
    } else {
      let v;
      if (n.isContinuous || j.startsOutsideView && j.endsOutsideView) {
        const w = k.isHorizontal ? (c = a.pageX) != null ? c : a.startPageX : (e = a.pageY) != null ? e : a.startPageY, z = k.getDateFromCoordinate(w, null, false, true);
        v = j.timeDiff = z - a.pointerStartDate;
        if (v !== null) {
          Object.assign(j, g.getEventNewStartEndDates(p, v));
          if (j.valid) {
            j.timeDiff = j.startDate - j.origStart;
          }
        }
      } else {
        const A = g.resolveStartEndDates(a.element);
        j.valid = Boolean(A.startDate && A.endDate);
        if (j.valid) {
          v = A.startDate - j.origStart;
        }
        if (v !== void 0) {
          if (p.graph && !this.allowNonWorkingTimeSNET) {
            j.startDate = p.run('skipNonWorkingTime', A.startDate);
            j.endDate = p.run('calculateProjectedXDateWithDuration', A.startDate, true, p.duration);
          } else {
            j.startDate = A.startDate;
            j.endDate = A.endDate;
          }
        }
        j.timeDiff = v;
      }
    }
    const u = j.dirty = j.dirty || q - j.startDate !== 0;
    if (j.valid) {
      if (g.constrainDragToTimeline && (j.endDate <= n.startDate || j.startDate >= n.endDate)) {
        j.valid = false;
        j.context.message = g.L('L{EventDrag.noDropOutsideTimeline}');
      } else if (u || j.externalDropTarget) {
        const B = j.externalDragValidity = !b || a.pageX && g.checkDragValidity(j, b);
        if (!B || typeof B === 'boolean') {
          j.valid = B !== false;
          j.context.message = '';
        } else {
          j.valid = B.valid !== false;
          j.context.message = B.message;
        }
      } else {
        j.valid = j.externalDragValidity !== false && ((f = j.externalDragValidity) == null ? void 0 : f.valid) !== false;
      }
    } else {
      j.valid = false;
    }
    j.context.valid = j.valid;
  }
  suspendRecordElementRedrawing(a, b = true) {
    this.suspendElementRedrawing(this.getRecordElement(a), b);
    a.instanceMeta(this.client).retainElement = b;
  }
  resumeRecordElementRedrawing(a) {
    this.suspendRecordElementRedrawing(a, false);
  }
  suspendElementRedrawing(a, b = true) {
    if (a) {
      a.retainElement = b;
    }
  }
  resumeElementRedrawing(a) {
    this.suspendElementRedrawing(a, false);
  }
  getDragData(a) {
    const b = this, {
        client: c,
        drag: e
      } = b, f = b.setupProductDragData(a), {
        record: g,
        eventBarEls: i,
        draggedEntities: j
      } = f, {startEvent: k} = e, l = g.isAssignment ? g.event : g, n = l.startDate, o = l.endDate, p = c.timeAxis, q = n < p.startDate, r = o > p.endDate, u = c.isSchedulerBase ? c.multiEventSelect : c.selectionMode.multiSelect, v = b.getCoordinate(l, a.element, [
        a.elementStartX,
        a.elementStartY
      ]), w = b.getCoordinate(l, a.element, [
        a.startClientX,
        a.startClientY
      ]);
    b.suspendRecordElementRedrawing(g);
    j.forEach(A => b.suspendRecordElementRedrawing(A));
    if (!c.selectionMode.checkboxOnly) {
      if (g.isAssignment) {
        c.selectAssignment(g, k.ctrlKey && u);
      } else {
        c.selectEvent(g, k.ctrlKey && u);
      }
    }
    const z = {
      'context': a,
      ...f,
      'sourceDate': q ? n : c.getDateFromCoordinate(v),
      'screenSourceDate': c.getDateFromCoordinate(w, null, false),
      'startDate': n,
      'endDate': o,
      'timeDiff': 0,
      'origStart': n,
      'origEnd': o,
      'startsOutsideView': q,
      'endsOutsideView': r,
      'duration': o - n,
      'browserEvent': k
    };
    i.forEach(A => A.classList.remove('b-sch-event-hover', 'b-active'));
    if (i.length > 1) {
      a.relatedElements = i.slice(1);
    }
    return z;
  }
  setupConstraints(a, b, c, e, f) {
    const g = this, i = !g.showExactDropPosition && c > 1 ? c : 0, j = 0;
    if (e) {
      g.setXConstraint(a.left, a.right - b.width, i);
    } else {
      g.setXConstraint(true, true, i);
    }
    if (f) {
      g.setYConstraint(a.top, a.bottom - b.height, j);
    } else {
      g.setYConstraint(0, true, j);
    }
  }
  updateYConstraint(a, b) {
    const c = this, {client: e} = c, {context: f} = c.drag, g = e.timeAxisViewModel.snapPixelAmount;
    if (f && !c.drag.lockY) {
      let i;
      if (c.constrainDragToTimeline) {
        i = e.getScheduleRegion(b, a);
      } else {
        c.setYConstraint(null, null, g);
        return;
      }
      c.setYConstraint(i.top, i.bottom - f.element.offsetHeight, g);
    } else {
      c.setYConstraint(null, null, g);
    }
  }
  setXConstraint(a, b, c) {
    const {drag: e} = this;
    e.minX = a;
    e.maxX = b;
  }
  setYConstraint(a, b, c) {
    const {drag: e} = this;
    e.minY = a;
    e.maxY = b;
  }
  adjustStartDate(a, b) {
    const {currentOverClient: c} = this, e = c.timeAxis.roundDate(new Date(a - 0 + b), c.snapRelativeToEventStartDate || c.snap && c.fillTicks ? a : false);
    if (c.fillTicks && (c.timeAxis.mainUnit === 'day' || DateHelper.compareUnits(c.timeResolution.unit, 'd') === 0)) {
      DateHelper.copyTimeValues(e, this.dragData.origStart);
    }
    return this.constrainStartDate(e);
  }
  resolveStartEndDates(a) {
    const b = this.currentOverClient, {timeAxis: c} = b, e = Rectangle.from(a.querySelector(b.eventInnerSelector), b.timeAxisSubGridElement), f = this.dragData, [g] = f.draggedEntities, i = g.isAssignment ? g.event : g, j = b.fillTicks && b.snapRelativeToEventStartDate, k = i.endDate - i.startDate;
    let {
      start: l,
      end: n
    } = b.getStartEndDatesFromRectangle(e, j ? null : 'round', k, true);
    if (l && n) {
      if (j) {
        const o = i.startDate - DateHelper.startOf(i.startDate, c.unit), p = n - l, q = o / p * e.width;
        e.deflate(q, 0, 0, q);
        const r = e.getStart(b.rtl, !b.isVertical);
        l = b.getDateFromCoordinate(r, null, true);
        l = c.roundDate(l, i.startDate);
      }
      l = this.adjustStartDate(l, 0);
      if (!f.startsOutsideView) {
        if (!c.dateInAxis(l, false)) {
          const u = c.getTickFromDate(l);
          if (u >= 0) {
            l = c.getDateFromTick(u);
          }
        }
        n = l && DateHelper.add(l, k);
      } else if (!f.endsOutsideView) {
        l = n && DateHelper.add(n, -k);
      }
    }
    return {
      'startDate': l,
      'endDate': n
    };
  }
  getTipHtml() {
    const a = this, {
        dragData: b,
        client: c,
        tooltipTemplate: e
      } = a, {
        startDate: f,
        endDate: g,
        draggedEntities: i,
        newResource: j
      } = b, k = c.getFormattedDate(f), l = g && c.getFormattedEndDate(g, f) || '', {
        valid: n,
        message: o,
        element: p,
        dragProxy: q
      } = b.context, r = q ? q.firstChild : p, u = i[0], v = u.isTask ? u : u.event;
    a.tip.lastAlignSpec.target = r;
    return e({
      'valid': n,
      'startDate': f,
      'endDate': g,
      'startText': k,
      'endText': l,
      'dragData': b,
      'newResource': j || b.resourceRecord,
      'message': o || '',
      [c.scheduledEventName + 'Record']: v,
      'startClockHtml': a.clockTemplate.template({
        'date': f,
        'text': k,
        'cls': 'b-sch-tooltip-startdate'
      }),
      'endClockHtml': v.isMilestone ? '' : a.clockTemplate.template({
        'date': g,
        'text': l,
        'cls': 'b-sch-tooltip-enddate'
      })
    });
  }
  updateConstrainDragToTimeSlot(a) {
    const b = this.client.isHorizontal ? 'lockX' : 'lockY';
    if (this.drag) {
      this.drag[b] = a;
    }
  }
  updateConstrainDragToResource(a) {
    const b = this;
    if (b.drag) {
      const {constrainDragToTimeSlot: c} = b, {isHorizontal: e} = b.client;
      if (a) {
        b.constrainDragToTimeline = true;
      }
      b.drag.lockY = e ? a : c;
      b.drag.lockX = e ? c : a;
    }
  }
  updateConstrainDragToTimeline(a) {
    if (!this.isConfiguring) {
      Object.assign(this.drag, {
        'cloneTarget': !a,
        'allowDropOutside': !a,
        'dragWithin': a ? null : this.client.floatRoot,
        'scrollManager': a ? this.client.scrollManager : null
      });
    }
  }
  constrainStartDate(a) {
    const {dragData: b} = this, {dateConstraints: c} = b, e = b.eventRecord || b.taskRecord || b.draggedEntities[0];
    if (c == null ? void 0 : c.start) {
      a = DateHelper.max(c.start, a);
    }
    if (c == null ? void 0 : c.end) {
      a = DateHelper.min(new Date(c.end - e.durationMS), a);
    }
    return a;
  }
  getElementFromContext(a) {
    return a.grabbed || a.dragProxy || a.element;
  }
  getRelatedRecords(a) {
    return [];
  }
  getMinimalDragData(a, b) {
    return {};
  }
  isValidDrop(a) {
    throw new Error('Implement in subclass');
  }
  checkDragValidity(a) {
    throw new Error('Implement in subclass');
  }
  updateRecords(a) {
    throw new Error('Implement in subclass');
  }
  isElementDraggable(a, b) {
    throw new Error('Implement in subclass');
  }
  getCoordinate(a, b, c) {
    throw new Error('Implement in subclass');
  }
  setupProductDragData(a) {
    throw new Error('Implement in subclass');
  }
  getProductDragContext(a) {
    throw new Error('Implement in subclass');
  }
  getRecordElement(a) {
    throw new Error('Implement in subclass');
  }
};
DragBase._$name = 'DragBase';
const tipAlign = {
  'top': 'b-t',
  'right': 'b100-t100',
  'bottom': 't-b',
  'left': 'b0-t0'
};
const EventResize = class extends InstancePlugin.mixin(Draggable_default, Droppable_default) {
  static get $name() {
    return 'EventResize';
  }
  static get configurable() {
    return {
      'draggingItemCls': 'b-sch-event-wrap-resizing',
      'resizingItemInnerCls': 'b-sch-event-resizing',
      'leftHandle': true,
      'rightHandle': true,
      'topHandle': true,
      'bottomHandle': true,
      'dynamicHandleSize': true,
      'allowResizeToZero': null,
      'reservedSpace': 1,
      'dragThreshold': 0,
      'dragTouchStartDelay': 300,
      'draggingClsSelector': '.b-timeline-base',
      'showTooltip': true,
      'showExactResizePosition': false,
      'validatorFn': () => true,
      'validatorFnThisObj': null,
      'tip': {
        '$config': [
          'lazy',
          'nullify'
        ],
        'value': {
          'autoShow': false,
          'axisLock': true,
          'trackMouse': false,
          'updateContentOnMouseMove': true,
          'hideDelay': 0
        }
      },
      'tooltipTemplate': a => '\n                <div class="b-sch-tip-' + (a.valid ? 'valid' : 'invalid') + '">\n                    ' + a.startClockHtml + '\n                    ' + a.endClockHtml + '\n                    <div class="b-sch-tip-message">' + a.message + '</div>\n                </div>\n            ',
      'ignoreSelector': '.b-sch-terminal',
      'dragActiveCls': 'b-resizing-event',
      'lockLayout': true,
      'resizeSelected': true
    };
  }
  static get pluginConfig() {
    return {
      'chain': [
        'render',
        'onEventDataGenerated',
        'isEventElementDraggable'
      ]
    };
  }
  doDestroy() {
    let a;
    super.doDestroy();
    (a = this.dragging) == null ? void 0 : a.destroy();
  }
  render() {
    const a = this, {client: b} = a;
    a.dragSelector = a.dragItemSelector = b.eventSelector;
    a.dragRootElement = a.dropRootElement = b.timeAxisSubGridElement;
    a.dragLock = b.isVertical ? 'y' : 'x';
  }
  isEventElementDraggable(a, b, c, e) {
    const f = this, g = b == null ? void 0 : b.resizable;
    if (f.disabled || !g || b.isMilestone) {
      return true;
    }
    return (g !== true && g !== 'start' || !f.isOverStartHandle(e, a)) && (g !== true && g !== 'end' || !f.isOverEndHandle(e, a));
  }
  onEventDataGenerated({
    eventRecord: a,
    wrapperCls: b,
    cls: c
  }) {
    let e, f;
    if ((f = (e = this.dragging) == null ? void 0 : e.context) == null ? void 0 : f.eventContexts.some(g => g.eventRecord === a)) {
      b['b-active'] = b[this.draggingItemCls] = b['b-over-resize-handle'] = c['b-resize-handle'] = c[this.resizingItemInnerCls] = 1;
    }
  }
  onDragPointerMove(a) {
    let b;
    const {
        client: c,
        dragging: e
      } = this, {
        visibleDateRange: f,
        isHorizontal: g
      } = c, i = g && c.rtl, j = g ? 'X' : 'Y', k = globalThis['page' + j + 'Offset'], l = a['page' + j] + (((b = e.context) == null ? void 0 : b.offset) || 0), n = Rectangle.from(c.timeAxisSubGridElement, null, true), o = n.getStart(i, g), p = n.getEnd(i, g);
    let q = c.getDateFromCoord({
      'coord': l,
      'local': false
    });
    if (i) {
      if (l - k > o) {
        q = f.startDate;
      } else if (l < p) {
        q = f.endDate;
      }
    } else if (l - k < o) {
      q = f.startDate;
    } else if (l - k > p) {
      q = f.endDate;
    }
    e.clientStartCoord = o;
    e.clientEndCoord = p;
    e.date = q;
    super.onDragPointerMove(a);
  }
  get isResizing() {
    return Boolean(this.dragging);
  }
  get isMultiResizing() {
    let a;
    return this.isResizing && ((a = this.dragging) == null ? void 0 : a.context.eventContexts.length) > 1;
  }
  beforeDrag(a) {
    const {client: b} = this, c = b.resolveTimeSpanRecord(a.itemElement), e = !b.isGanttBase && b.resolveResourceRecord(b.isVertical ? a.startEvent : a.itemElement);
    if (this.disabled || b.readOnly || (e == null ? void 0 : e.readOnly) || c && (c.readOnly || !(c.project || c.isOccurrence)) || super.beforeDrag(a) === false) {
      return false;
    }
    a.mousedownDate = a.date = b.getDateFromCoordinate(a.event['page' + (b.isHorizontal ? 'X' : 'Y')], null, false);
    return this.triggerBeforeResize(a);
  }
  initEventRecordContext(a, b) {
    const c = a === b.timespanRecord ? b.element : this.getSelectedRecordElement(a), e = a.isAssignmentModel ? a.event : a, {
        isBatchUpdating: f,
        wrapEndDate: g,
        wrapStartDate: i
      } = e, j = f ? e.get('startDate') : e.startDate, k = f ? e.get('endDate') : e.endDate, l = b.draggingEnd ? k : j;
    return {
      'mainContext': b,
      'eventRecord': e,
      'element': c,
      'startDate': j,
      'endDate': k,
      'date': l,
      'wrapEndDate': g,
      'wrapStartDate': i,
      'oldValue': l,
      'originalStartDate': j,
      'originalEndDate': k,
      'timespanRecord': e
    };
  }
  initResizeContext(a) {
    let b, c, e;
    const f = this, {client: g} = f, {
        startEvent: i,
        itemElement: j
      } = a, k = g.resolveEventRecord(j), l = k.endDate, n = k.startDate, o = (b = g.features.eventBuffer) == null ? void 0 : b.enabled, p = f.dragLock === 'x', q = p && g.rtl, r = f.isOverEndHandle(i, j), u = Rectangle.from(j), v = p ? i.clientX : i.clientY, w = r ? u.getEnd(q, p) : u.getStart(q, p), z = r ? l : n, A = {
        'owner': f,
        'valid': true,
        'offset': o ? 0 : w - v,
        'edge': p ? r ? 'right' : 'left' : r ? 'bottom' : 'top',
        'finalize': f.finalize,
        'event': a.event,
        'toSet': r ? 'endDate' : 'startDate',
        'otherEnd': r ? 'startDate' : 'endDate',
        'setMethod': r ? 'setEndDate' : 'setStartDate',
        'setOtherMethod': r ? 'setStartDate' : 'setEndDate',
        'wrapToSet': !o ? null : r ? 'wrapEndDate' : 'wrapStartDate',
        'oldValue': z,
        'draggingEnd': r,
        'timespanRecord': k,
        [g.scheduledEventName + 'Record']: k,
        'element': ((e = (c = this.eventContexts) == null ? void 0 : c[0]) == null ? void 0 : e.element) || j
      };
    let B;
    if (f.resizeSelected && f.isRecordSelected(k)) {
      B = f.selectedRecords.filter(C => C.isAssignmentModel ? !C.event.readOnly : !C.readOnly);
      B.sort((C, E) => C === k ? -1 : E === k ? 1 : 0);
    } else {
      B = [k];
    }
    A.eventContexts = B.map(C => f.initEventRecordContext(C, A));
    return A;
  }
  get selectedRecords() {
    return this.client.selectedAssignments;
  }
  isRecordSelected(a) {
    let b, c;
    return (c = (b = this.client).isEventSelected) == null ? void 0 : c.call(b, a);
  }
  getSelectedRecordElement(a) {
    return a.isAssignmentModel ? this.client.getElementFromAssignmentRecord(a) : this.client.getElementFromEventRecord(a);
  }
  dragStart(a) {
    const b = this, {
        client: c,
        tip: e
      } = b, f = a.context = b.initResizeContext(a);
    c.element.classList.add(...b.dragActiveCls.split(' '));
    if (!c.listenToBatchedUpdates) {
      c.beginListeningForBatchedUpdates();
    }
    for (const g of f.eventContexts) {
      const {eventRecord: i} = g;
      i.meta.isResizing = true;
      if (!i.isBatchUpdating) {
        b.beginEventRecordBatch(i);
      }
      b.setupProductResizeContext(g, a.startEvent);
    }
    b.triggerEventResizeStart(c.scheduledEventName + 'ResizeStart', b.getTriggerParams(f), f);
    if (e) {
      e.show();
      e.align = tipAlign[f.edge];
      e.showBy(b.getTooltipTarget(a));
    }
  }
  getTriggerParams(a) {
    const b = a.eventContexts.map(g => ({ ...g })), c = this.client.capitalizedEventName.toLowerCase(), e = b.find(({
        element: g,
        mainContext: i
      }) => {
        let j;
        return (j = g.closest('.b-sch-' + c + '-wrap')) == null ? void 0 : j.isEqualNode(i.element);
      }) || b[0], f = {
        ...e,
        'event': a.event,
        'async': a.async,
        'finalize': (...g) => a.finalize(...g),
        'resizeData': b
      };
    delete f.mainContext;
    return f;
  }
  triggerBeforeResize(a) {
    const {client: b} = this, c = b.resolveTimeSpanRecord(a.itemElement);
    return b.trigger('before' + b.capitalizedEventName + 'Resize', {
      [b.scheduledEventName + 'Record']: c,
      'event': a.event,
      ...this.getBeforeResizeParams({
        'event': a.startEvent,
        'element': a.itemElement
      })
    });
  }
  triggerEventResizeStart(a, b, c) {
    let e, f;
    this.client.trigger(a, b);
    (f = (e = this.client)['after' + StringHelper.capitalize(a)]) == null ? void 0 : f.call(e, c, b);
  }
  triggerEventResizeEnd(a, b) {
    this.client.trigger(a, b);
  }
  triggerEventPartialResize(a, b) {
    const c = this.client.scheduledEventName + 'Record', e = new Map();
    for (const f of b) {
      const g = f[c];
      e.set(g, g.batching);
      g.batching = 0;
    }
    this.client.trigger(a, {
      ...b[0],
      'resizeData': b
    });
    for (const i of b) {
      const j = i[c];
      j.batching = e.get(j);
    }
  }
  triggerBeforeEventResizeFinalize(a, b) {
    this.client.trigger(a, b);
  }
  dragEnter(a) {
    let b;
    return ((b = a.context) == null ? void 0 : b.owner) === this;
  }
  resizeEventPartiallyInternal(a, b) {
    let c;
    const {client: e} = this, {toSet: f} = b.mainContext;
    if ((c = e.features.eventBuffer) == null ? void 0 : c.enabled) {
      if (f === 'startDate') {
        const g = b.startDate.getTime() - b.originalStartDate.getTime();
        a.wrapStartDate = new Date(b.wrapStartDate.getTime() + g);
      } else if (f === 'endDate') {
        const i = b.endDate.getTime() - b.originalEndDate.getTime();
        a.wrapEndDate = new Date(b.wrapEndDate.getTime() + i);
      }
    }
    a.set(f, b[f]);
  }
  applyDateConstraints(a, b, c) {
    let e, f;
    const g = (e = c.dateConstraints) == null ? void 0 : e.start, i = (f = c.dateConstraints) == null ? void 0 : f.end;
    if (g || i) {
      a = DateHelper.constrain(a, g, i);
      c.snappedDate = DateHelper.constrain(c.snappedDate, g, i);
    }
    return a;
  }
  resizeEventRecord(a, b, c) {
    const e = this, {client: f} = e, {
        timeAxis: g,
        enableEventAnimations: i
      } = f, {
        eventRecord: j,
        oldValue: k,
        element: l
      } = a, {
        resolutionUnit: n,
        resolutionIncrement: o
      } = g, p = j.get('startDate'), q = j.get('endDate'), r = a.mainContext;
    let {
        toSet: u,
        otherEnd: v
      } = r, w, z;
    if (u === 'endDate') {
      if (c < p) {
        w = -1;
      }
    } else {
      if (c > q) {
        w = 1;
      }
    }
    if (w && e.onDragEndSwitch) {
      e.onDragEndSwitch(a, c, w);
      v = r.otherEnd;
      u = r.toSet;
    }
    if (f.snapRelativeToEventStartDate) {
      c = g.roundDate(c, k);
    }
    a.snappedDate = DateHelper.round(c, g.resolution, null, g.weekStartDay);
    const A = DateHelper.diff(c, a[v], n) * (r.draggingEnd ? -1 : 1);
    if (e.isEventDragCreate && a.eventRecord === a.mainContext.eventRecord) {
      a.mainContext.tooNarrow = Math.abs(A) < o / 2;
    } else if (A < o) {
      if (A < 0) {
        return;
      }
      if (e.allowResizeToZero) {
        a.snappedDate = c = a[v];
      } else {
        const B = v === 'startDate' ? 1 : -1;
        let C = g.ceilDate(DateHelper.add(j.get(v), o * B, n));
        const E = DateHelper.diff(C, a[v], n) * (r.draggingEnd ? -1 : 1);
        if (E > o) {
          C = g.roundDate(DateHelper.add(C, -B, n));
        }
        if ((C - k) * B < 0 || (c - k) * B > 0) {
          c = B > 0 ? DateHelper.max(c, C) : DateHelper.min(c, C);
          a.snappedDate = C;
        } else {
          c = B > 0 ? DateHelper.max(c, k) : DateHelper.min(c, k);
          a.snappedDate = k;
        }
        z = true;
      }
    }
    c = e.applyDateConstraints(c, j, a);
    if (!a.date || c - a.date || z) {
      a.date = c;
      a[u] = e.showExactResizePosition || f.timeAxisViewModel.snap ? a.snappedDate : c;
      a.valid = e.allowResizeToZero || a[u] - a[u === 'startDate' ? 'endDate' : 'startDate'] !== 0;
      if (j.get(u) - a[u]) {
        a.valid = e.checkValidity(a, b.event);
        a.message = '';
        if (a.valid && typeof a.valid !== 'boolean') {
          a.message = a.valid.message;
          a.valid = a.valid.valid;
        }
        a.valid = a.valid !== false;
        if (j.isOccurrence) {
          j.stores = [f.eventStore];
        }
        f.enableEventAnimations = false;
        this.resizeEventPartiallyInternal(j, a);
        f.enableEventAnimations = i;
        if (j.isOccurrence) {
          j.stores = null;
        }
        if (a.tooNarrow) {
          a.valid = false;
        }
        return {
          [f.scheduledEventName + 'Record']: j,
          'startDate': p,
          'endDate': q,
          'element': l,
          ...{ [u]: a[u] }
        };
      }
    }
  }
  moveDrag(a) {
    let b;
    const c = this, {
        context: e,
        event: f
      } = a, {
        client: g,
        tip: i
      } = c, j = c.dragLock === 'x', k = j ? 'X' : 'Y', {visibleDateRange: l} = g, n = j && g.rtl, o = e.offset * (n ? -1 : 1), p = f['client' + k] + o, q = Rectangle.from(g.timeAxisSubGridElement, null, true), r = q.getStart(n, j), u = q.getEnd(n, j), v = [];
    let {date: w} = a;
    e.event = f;
    if (f.isScroll) {
      a.date = g.getDateFromCoordinate(f['page' + k] + o, null, false);
    }
    if (n) {
      if (p > r) {
        w = a.date = l.startDate;
      } else if (p < u) {
        w = a.date = l.endDate;
      }
    } else if (p < r) {
      w = a.date = l.startDate;
    } else if (p > u) {
      w = a.date = l.endDate;
    }
    e.message = '';
    e.valid = true;
    let z = w - e.oldValue;
    if (c.client.capitalizedEventName === 'Task') {
      z = w - e.eventContexts[0].oldValue;
    }
    for (const A of e.eventContexts) {
      const B = new Date(A.oldValue.getTime() + z), C = c.resizeEventRecord(A, a, B);
      if (C) {
        v.push(C);
      }
      if (!A.valid) {
        e.valid = false;
      }
      if ((b = A.message) == null ? void 0 : b.length) {
        e.message = A.message;
      }
    }
    if (i) {
      i.align = tipAlign[e.edge];
      i.alignTo(c.getTooltipTarget(a));
    }
    if (v.length) {
      c.triggerEventPartialResize(c.client.scheduledEventName + 'PartialResize', v);
    }
    super.moveDrag(a);
  }
  dragEnd(a) {
    const {context: b} = a;
    if (b) {
      b.event = a.event;
    }
    if (a.aborted) {
      b == null ? void 0 : b.finalize(false);
    } else if (!this.isEventDragCreate && !a.started && !EventHelper.getPagePoint(a.event).equals(EventHelper.getPagePoint(a.startEvent))) {
      this.dragStart(a);
      this.cleanup(a.context, false);
    }
  }
  async dragDrop({
    context: a,
    event: b
  }) {
    let c;
    const e = this, {client: f} = e, {toSet: g} = a;
    let i = true, j;
    (c = e.tip) == null ? void 0 : c.hide();
    for (const k of a.eventContexts) {
      k[g] = k.snappedDate;
      const {
        startDate: l,
        endDate: n
      } = k;
      k.valid = l && n && (e.allowResizeToZero || n - l > 0) && k[g] - k.oldValue && k.valid !== false;
      if (!k.valid) {
        i = false;
      }
    }
    if (i) {
      const o = e.getTriggerParams(a);
      e.triggerBeforeEventResizeFinalize('before' + f.capitalizedEventName + 'ResizeFinalize', {
        ...o,
        'context': o
      });
      if (o.async) {
        a.async = true;
      }
      j = true;
    }
    if (!a.async) {
      await a.finalize(j);
    }
  }
  async eventRecordFinalize(a, b) {
    const c = this, {client: e} = c;
    let f = [], g = false;
    if (b) {
      f = a.eventContexts.map(i => c.internalUpdateRecord(i, i.eventRecord));
    } else {
      for (const i of a.eventContexts) {
        const {eventRecord: j} = i;
        c.cancelEventRecordBatch(j);
        if (j.isOccurrence) {
          j.resources.forEach(k => e.repaintEventsForResource(k));
        }
      }
    }
    if (f.length) {
      await Promise.all(f);
      if (c.isDestroyed) {
        return;
      }
      g = f.some(k => k);
    }
    c.cleanup(a, g);
  }
  async finalize(a) {
    const b = this.owner, c = this, {
        snapRelativeToEventStartDate: e,
        timeAxis: f
      } = b.client, {toSet: g} = c;
    if (c.finalizing) {
      return;
    }
    c.finalizing = true;
    if (a) {
      c.eventContexts.forEach(i => {
        const {oldValue: j} = i;
        if (e) {
          i[g] = i.snappedDate = f.roundDate(i.date, j);
        }
        b.adjustRecordBeforeUpdating(i, i.eventRecord);
      });
      b.client.endListeningForBatchedUpdates();
    }
    b.eventRecordFinalize(c, a);
  }
  cleanup(a, b) {
    let c;
    const e = this, {client: f} = e;
    for (const {
          element: g,
          eventRecord: i
        } of a.eventContexts) {
      i.meta.isResizing = false;
      e.unHighlightHandle(g);
    }
    f.endListeningForBatchedUpdates();
    (c = e.tip) == null ? void 0 : c.hide();
    f.element.classList.remove(...e.dragActiveCls.split(' '));
    e.triggerEventResizeEnd(f.scheduledEventName + 'ResizeEnd', {
      ...e.getTriggerParams(a),
      'changed': b
    });
  }
  adjustRecordBeforeUpdating(a, b) {
    const {
        startDate: c,
        endDate: e
      } = a, {
        toSet: f,
        draggingEnd: g,
        otherEnd: i
      } = a.mainContext;
    if (!b.isOccurrence && b.isEntity && (!b.manuallyScheduled || b.project.skipNonWorkingTimeWhenSchedulingManually)) {
      const j = b.run('calculateProjectedDuration', c, e);
      b.set({
        [f]: b.run('calculateProjectedXDateWithDuration', g ? c : e, g, j),
        [i]: a[i]
      });
    }
  }
  async internalUpdateRecord(a, b) {
    let c;
    const {client: e} = this, {generation: f} = b, {
        startDate: g,
        endDate: i
      } = a, {
        toSet: j,
        wrapToSet: k,
        setMethod: l
      } = a.mainContext;
    if (b.isOccurrence) {
      b[b.batching > 1 ? 'endBatch' : 'cancelBatch']();
      b.set(TimeSpan.prototype.inSetNormalize.call(b, {
        'startDate': g,
        'endDate': i
      }));
    } else {
      const n = Object.assign({}, b.meta.batchChanges);
      delete n[j];
      delete n.duration;
      this.cancelEventRecordBatch(b);
      if ((c = e.features.eventBuffer) == null ? void 0 : c.enabled) {
        b[k] = null;
      }
      b[l](a[j], false);
      if (Object.keys(n).length) {
        b.set(n);
      }
    }
    await e.project.commitAsync();
    return b.generation !== f;
  }
  onDragItemMouseMove(a) {
    if (a.pointerType !== 'touch' && !this.handleSelector) {
      this.checkResizeHandles(a);
    }
  }
  checkResizeHandles(a) {
    const b = this, {overItem: c} = b;
    if (c && !b.client.readOnly && (!b.allowResize || b.allowResize(c, a))) {
      const e = b.client.resolveTimeSpanRecord(c);
      if (e == null ? void 0 : e.readOnly) {
        return;
      }
      if (b.isOverAnyHandle(a, c)) {
        b.highlightHandle();
      } else {
        b.unHighlightHandle();
      }
    }
  }
  onDragItemMouseLeave(a, b) {
    this.unHighlightHandle(b);
  }
  highlightHandle() {
    let a, b;
    const {
        overItem: c,
        client: e
      } = this, f = (b = (a = c.syncIdMap) == null ? void 0 : a[e.scheduledEventName]) != null ? b : c.querySelector(e.eventInnerSelector);
    f.classList.add('b-resize-handle');
    c.classList.add('b-over-resize-handle');
  }
  unHighlightHandle(a = this.overItem) {
    let b, c;
    if (a) {
      const e = this, f = (c = (b = a.syncIdMap) == null ? void 0 : b[e.client.scheduledEventName]) != null ? c : a.querySelector(e.client.eventInnerSelector);
      if (f) {
        f.classList.remove('b-resize-handle', e.resizingItemInnerCls);
      }
      a.classList.remove('b-over-resize-handle', e.draggingItemCls);
    }
  }
  isOverAnyHandle(a, b) {
    return Boolean(this.isOverStartHandle(a, b) || this.isOverEndHandle(a, b));
  }
  isOverStartHandle(a, b) {
    let c;
    return Boolean((c = this.getHandleRect('start', a, b)) == null ? void 0 : c.contains(EventHelper.getPagePoint(a)));
  }
  isOverEndHandle(a, b) {
    let c;
    return Boolean((c = this.getHandleRect('end', a, b)) == null ? void 0 : c.contains(EventHelper.getPagePoint(a)));
  }
  getHandleRect(a, b, c) {
    if (this.overItem) {
      c = b.target.closest('.' + this.client.eventCls) || c.querySelector('.' + this.client.eventCls);
      if (!c) {
        return;
      }
      const e = this, f = a === 'start', {client: g} = e, i = Boolean(g.rtl), j = e.dragLock, k = j === 'x', l = k ? 'width' : 'height', n = (k ? f ? i ? 'right' : 'left' : i ? 'left' : 'right' : f ? 'top' : 'bottom') + 'Handle', o = g.resolveTimeSpanRecord(c), p = o == null ? void 0 : o.isResizable, q = Rectangle.from(c), r = q.clone(), u = globalThis.getComputedStyle(c, ':before'), v = e.dynamicHandleSize ? e.reservedSpace / 2 : 0, w = [
          0,
          0,
          0,
          0
        ];
      let z = e.handleSize || parseFloat(u[l]);
      if (!e.disabled && e[n] && e.dynamicHandleSize && (p === true || p === a)) {
        const A = !k && !f || k && i === f, B = q[l];
        if (z > 0.5 * B) {
          z = Math.max(Math.floor(B / 4) - 1, 1);
        }
        if (A) {
          r[j] += B - z;
          w[k ? 3 : 0] = B / 2 + v;
        } else {
          w[k ? 1 : 2] = B / 2 + v;
        }
        q.deflate(...w);
        r[l] = z;
        r.constrainTo(q);
        if (r[l]) {
          return r;
        }
      }
    }
  }
  setupDragContext(a) {
    const b = this;
    if (b.overItem && b.isOverAnyHandle(a, b.overItem) && b.isElementResizable(b.overItem, a)) {
      const c = super.setupDragContext(a);
      c.scrollManager = b.client.scrollManager;
      return c;
    }
  }
  changeTip(a, b) {
    let c;
    const e = this;
    if (!e.showTooltip) {
      return null;
    }
    if (a) {
      if (a.isTooltip) {
        a.owner = e;
      } else {
        a = Tooltip.reconfigure(b, Tooltip.mergeConfigs({ 'id': e.tipId }, a, {
          'getHtml': e.getTipHtml.bind(e),
          'owner': e.client
        }, e.tip), {
          'owner': e,
          'defaults': { 'type': 'tooltip' }
        });
      }
      a.ion({
        'innerhtmlupdate': 'updateDateIndicator',
        'thisObj': e
      });
      e.clockTemplate = new ClockTemplate({ 'scheduler': e.client });
    } else if (b) {
      b.destroy();
      (c = e.clockTemplate) == null ? void 0 : c.destroy();
    }
    return a;
  }
  isElementResizable(a, b) {
    let c;
    const e = this, {client: f} = e, g = f.resolveTimeSpanRecord(a);
    if (f.readOnly) {
      return false;
    }
    let i = g == null ? void 0 : g.isResizable;
    const j = (c = a == null ? void 0 : a.syncIdMap[f.scheduledEventName]) != null ? c : a, k = b.target.closest('[class$="-handle"]');
    if (!i || k && k !== j) {
      return false;
    }
    a = b.target.closest(e.dragSelector);
    if (!a) {
      return false;
    }
    const l = a.classList.contains('b-sch-event-startsoutside'), n = a.classList.contains('b-sch-event-endsoutside');
    if (i === true) {
      if (l && n) {
        return false;
      } else if (l) {
        i = 'end';
      } else if (n) {
        i = 'start';
      } else {
        return e.isOverStartHandle(b, a) || e.isOverEndHandle(b, a);
      }
    }
    if (l && i === 'start' || n && i === 'end') {
      return false;
    }
    if (e.isOverStartHandle(b, a) && i === 'start' || e.isOverEndHandle(b, a) && i === 'end') {
      return true;
    }
    return false;
  }
  updateDateIndicator() {
    const {clockTemplate: a} = this, {context: b} = this.dragging, {
        eventRecord: c,
        snappedDate: e
      } = b.eventContexts[0], {draggingEnd: f} = b, g = f ? c.get('startDate') : e, i = f ? e : c.get('endDate'), {element: j} = this.tip;
    a.updateDateIndicator(j.querySelector('.b-sch-tooltip-startdate'), g);
    a.updateDateIndicator(j.querySelector('.b-sch-tooltip-enddate'), i);
  }
  getTooltipTarget({
    itemElement: a,
    context: b
  }) {
    const c = this, {rtl: e} = c.client, f = Rectangle.from(a, null, true);
    if (c.dragLock === 'x') {
      if (!e && b.edge === 'right' || e && b.edge === 'left') {
        f.x = f.right - 1;
      } else {
        f.x -= c.tip.anchorSize[0] / 2;
      }
      f.width = c.tip.anchorSize[0] / 2;
    } else {
      if (b.edge === 'bottom') {
        f.y = f.bottom - 1;
      }
      f.height = c.tip.anchorSize[1] / 2;
    }
    return { 'target': f };
  }
  basicValidityCheck(a, b) {
    return a.startDate && (a.endDate > a.startDate || this.allowResizeToZero) && this.validatorFn.call(this.validatorFnThisObj || this, a, b);
  }
  getTipHtml({tip: a}) {
    const b = this, {context: c} = b.dragging, e = c.eventContexts.find(o => o.eventRecord === c.timespanRecord), {
        startDate: f,
        endDate: g,
        snappedDate: i,
        eventRecord: j
      } = e, {
        toSet: k,
        valid: l,
        message: message = ''
      } = c;
    if (!f || !g) {
      return a.html;
    }
    const n = {
      'record': j,
      'valid': l,
      'message': message,
      'startDate': f,
      'endDate': g,
      [k]: i
    };
    n.startText = b.client.getFormattedDate(n.startDate);
    n.endText = b.client.getFormattedDate(n.endDate);
    n.startClockHtml = b.clockTemplate.template({
      'date': n.startDate,
      'text': n.startText,
      'cls': 'b-sch-tooltip-startdate'
    });
    n.endClockHtml = b.clockTemplate.template({
      'date': n.endDate,
      'text': n.endText,
      'cls': 'b-sch-tooltip-enddate'
    });
    return b.tooltipTemplate(n);
  }
  beginEventRecordBatch(a) {
    a.beginBatch();
  }
  cancelEventRecordBatch(a) {
    a.cancelBatch();
  }
  getBeforeResizeParams(a) {
    const {client: b} = this;
    return { 'resourceRecord': b.resolveResourceRecord(b.isVertical ? a.event : a.element) };
  }
  setupProductResizeContext(a, b) {
    let c, e, f;
    const {client: g} = this, {element: i} = a, j = g.resolveEventRecord(i), k = (c = g.resolveResourceRecord) == null ? void 0 : c.call(g, i), l = (e = g.resolveAssignmentRecord) == null ? void 0 : e.call(g, i);
    Object.assign(a, {
      'eventRecord': j,
      'resourceRecord': k,
      'assignmentRecord': l,
      'dateConstraints': (f = g.getDateConstraints) == null ? void 0 : f.call(g, k, j)
    });
  }
  checkValidity({
    startDate: a,
    endDate: b,
    eventRecord: c,
    resourceRecord: e
  }) {
    const {client: f} = this;
    if (!f.isDateRangeAvailable(a, b, c, c.resources)) {
      return {
        'valid': false,
        'message': this.L('L{EventDrag.eventOverlapsExisting}')
      };
    }
    return this.basicValidityCheck(...arguments);
  }
  get tipId() {
    return this.client.id + '-event-resize-tip';
  }
};
EventResize._$name = 'EventResize';
GridFeatureManager.registerFeature(EventResize, true, 'Scheduler');
GridFeatureManager.registerFeature(EventResize, false, 'ResourceHistogram');
const TaskEditStm_default = a => {
  let b;
  return b = class extends (a || Base) {
    getStmCapture() {
      return {
        'stmInitiallyAutoRecord': this.stmInitiallyAutoRecord,
        'stmInitiallyDisabled': this.stmInitiallyDisabled,
        'transferred': false
      };
    }
    applyStmCapture(c) {
      this.stmInitiallyAutoRecord = c.stmInitiallyAutoRecord;
      this.stmInitiallyDisabled = c.stmInitiallyDisabled;
    }
    captureStm(c = false) {
      const e = this, f = e.project, g = f.getStm();
      if (e.hasStmCapture) {
        return;
      }
      e.hasStmCapture = true;
      e.stmInitiallyDisabled = g.disabled;
      e.stmInitiallyAutoRecord = g.autoRecord;
      if (e.stmInitiallyDisabled) {
        g.enable();
        g.autoRecord = false;
      } else {
        if (e.stmInitiallyAutoRecord) {
          g.autoRecord = false;
        }
        if (g.isRecording) {
          g.stopTransaction();
        }
      }
      if (c) {
        this.startStmTransaction();
      }
    }
    startStmTransaction() {
      this.project.getStm().startTransaction();
    }
    commitStmTransaction() {
      const c = this, e = c.project.getStm();
      if (!c.hasStmCapture) {
        throw new Error('Does not have STM capture, no transaction to commit');
      }
      if (e.enabled) {
        e.stopTransaction();
        if (c.stmInitiallyDisabled) {
          e.resetQueue();
        }
      }
    }
    async rejectStmTransaction() {
      let c;
      const e = this.project.getStm(), {client: f} = this;
      if (!this.hasStmCapture) {
        throw new Error('Does not have STM capture, no transaction to reject');
      }
      if (e.enabled) {
        if ((c = e.transaction) == null ? void 0 : c.length) {
          f.suspendRefresh();
          e.rejectTransaction();
          await f.resumeRefresh(true);
        } else if (e.isRecording) {
          e.stopTransaction();
        }
      }
    }
    enableStm() {
      this.project.getStm().enable();
    }
    disableStm() {
      this.project.getStm().disable();
    }
    async freeStm(c = null) {
      const e = this, f = e.project.getStm(), {
          stmInitiallyDisabled: g,
          stmInitiallyAutoRecord: i
        } = e;
      if (!e.hasStmCapture) {
        return;
      }
      let j;
      e.rejectingStmTransaction = true;
      if (c === true) {
        j = e.commitStmTransaction();
      } else if (c === false) {
        j = e.rejectStmTransaction();
      }
      e.hasStmCapture = false;
      await j;
      if (!f.isDestroying) {
        f.disabled = g;
        f.autoRecord = i;
      }
      if (!e.isDestroying) {
        e.rejectingStmTransaction = true;
      }
    }
  }, __publicField(b, '$name', 'TaskEditStm'), b;
};
const TaskEditTransactional_default = a => class b extends (a || Base) {
  static get $name() {
    return 'TaskEditTransactional';
  }
  captureStm(c) {
    if (this.client.transactionalFeaturesEnabled) {
      super.captureStm();
      return this.startStmTransaction(c);
    } else {
      super.captureStm(c);
    }
  }
  freeStm(c) {
    if (this.hasStmCapture || !this.client.transactionalFeaturesEnabled) {
      return super.freeStm(c);
    }
  }
  async startStmTransaction(c) {
    if (this.client.transactionalFeaturesEnabled) {
      await this.startFeatureTransaction(c);
    } else {
      super.startStmTransaction();
    }
  }
  commitStmTransaction() {
    if (this.client.transactionalFeaturesEnabled) {
      return this.finishFeatureTransaction();
    } else {
      super.commitStmTransaction();
    }
  }
  afterCancel() {
    if (this.client.transactionalFeaturesEnabled) {
      this.rejectFeatureTransaction();
    }
  }
};
const TransactionalFeature_default = a => {
  let b;
  return b = class extends (a || Base).mixin(AttachToProjectMixin_default) {
    detachFromProject(c) {
      this.rejectFeatureTransaction();
      super.detachFromProject(c);
    }
    getStmCapture() {
      const c = super.getStmCapture();
      c._editorPromiseResolve = this._editorPromiseResolve;
      return c;
    }
    applyStmCapture(c) {
      super.applyStmCapture(c);
      this._editorPromiseResolve = c._editorPromiseResolve;
    }
    async startFeatureTransaction() {
      let c, e;
      if (!this.client.transactionalFeaturesEnabled) {
        return;
      }
      const f = this, {project: g} = f.client, {stm: i} = g, j = f._featureTransactionId = IdHelper.generateId('featureTransaction');
      (e = (c = f.client).trigger) == null ? void 0 : e.call(c, 'beforeFeatureTransactionStart', { 'id': j });
      let k;
      if (f.hasStmCapture) {
        i.startTransaction();
      } else {
        k = g.queue(async () => {
          if (!g.isEngineReady()) {
            await g.commitAsync();
          }
        });
      }
      g.queue(() => {
        let l, n;
        if (!f.hasStmCapture) {
          f._stmInitiallyDisabled = i.disabled;
          f._stmInitiallyAutoRecord = i.autoRecord;
          if (i.isRecording) {
            i.stopTransaction();
          } else if (f._stmInitiallyDisabled) {
            i.enable();
          }
          i.autoRecord = false;
        }
        if (!i.isRecording) {
          i.startTransaction();
        }
        (n = (l = f.client).trigger) == null ? void 0 : n.call(l, 'featureTransactionStart', { 'id': j });
        return new Promise(o => f._editorPromiseResolve = o);
      });
      await k;
    }
    rejectFeatureTransaction() {
      let c;
      const e = this, {stm: f} = e.client.project, g = e._featureTransactionId;
      if (!g || e.finishingFeatureTransaction) {
        return;
      }
      (c = e._editorPromiseResolve) == null ? void 0 : c.call(e);
      e._editorPromiseResolve = null;
      delete e._featureTransactionId;
      f.isRecording && f.rejectTransaction();
      if (!e.hasStmCapture && e._stmInitiallyDisabled != null) {
        f.disabled = e._stmInitiallyDisabled;
        f.autoRecord = e._stmInitiallyAutoRecord;
      }
      e.client.trigger('featureTransactionReject', { 'id': g });
      e.client.trigger('featureTransactionComplete', { 'id': g });
    }
    async finishFeatureTransaction() {
      let c;
      const e = this, {
          client: f,
          _stmInitiallyDisabled: g,
          _stmInitiallyAutoRecord: i,
          _featureTransactionId: j
        } = e, {project: k} = f, {stm: l} = k;
      if (!j) {
        return;
      }
      f.trigger('featureTransactionFinalizeStart', { 'id': j });
      e.finishingFeatureTransaction = true;
      if (!k.isEngineReady()) {
        await k.commitAsync();
      }
      if (l.isRecording) {
        l.stopTransaction();
      }
      (c = e._editorPromiseResolve) == null ? void 0 : c.call(e);
      e._editorPromiseResolve = null;
      if (!e.hasStmCapture && l && !l.isDestroying && g != null) {
        l.disabled = g;
        l.autoRecord = i;
      }
      if (!e.isDestroying) {
        delete e._featureTransactionId;
        e.finishingFeatureTransaction = false;
        f.trigger('featureTransactionFinalized', { 'id': j });
        f.trigger('featureTransactionComplete', { 'id': j });
      }
    }
  }, __publicField(b, '$name', 'TransactionalFeature'), b;
};
const getDragCreateDragDistance = function (a) {
  let b, c;
  if ((c = (b = this.source) == null ? void 0 : b.client.features.taskEdit) == null ? void 0 : c._canceling) {
    return false;
  }
  return EventHelper.getDistanceBetween(this.startEvent, a);
};
const DragCreateBase = class extends EventResize.mixin(TaskEditStm_default, TransactionalFeature_default, TaskEditTransactional_default) {
  static configurable = {
    'showTooltip': true,
    'dragTolerance': 2,
    'preventMultiple': false,
    'dragTouchStartDelay': 300,
    'validatorFnThisObj': null,
    'tipTemplate': a => '\n            <div class="b-sch-tip-' + (a.valid ? 'valid' : 'invalid') + '">\n                ' + a.startClockHtml + '\n                ' + a.endClockHtml + '\n                <div class="b-sch-tip-message">' + a.message + '</div>\n            </div>\n        ',
    'dragActiveCls': 'b-dragcreating'
  };
  static get pluginConfig() {
    return {
      'chain': [
        'render',
        'onEventDataGenerated'
      ],
      'before': ['onElementContextMenu']
    };
  }
  construct(a, b) {
    if ((b == null ? void 0 : b.showTooltip) === false) {
      b.tip = null;
    }
    super.construct(...arguments);
  }
  changeValidatorFn(a) {
    this.createValidatorFn = a;
  }
  render() {
    const a = this, {client: b} = a;
    a.dragRootElement = a.dropRootElement = b.timeAxisSubGridElement;
    a.dragLock = b.isVertical ? 'y' : 'x';
  }
  onDragEndSwitch(a) {
    const {client: b} = this, {enableEventAnimations: c} = b, {eventRecord: e} = a, {draggingEnd: f} = a.mainContext, g = this.dragLock === 'x', {initialDate: i} = this.dragging;
    b.enableEventAnimations = false;
    e.set({
      'startDate': i,
      'endDate': i
    });
    if (f) {
      a.endDate = i;
      Object.assign(a.mainContext, {
        'toSet': 'startDate',
        'otherEnd': 'endDate',
        'setMethod': 'setStartDate',
        'setOtherMethod': 'setEndDate',
        'edge': g ? 'left' : 'top'
      });
    } else {
      a.startDate = i;
      Object.assign(a.mainContext, {
        'toSet': 'endDate',
        'otherEnd': 'startDate',
        'setMethod': 'setEndDate',
        'setOtherMethod': 'setStartDate',
        'edge': g ? 'right' : 'bottom'
      });
    }
    a.draggingEnd = this.draggingEnd = !f;
    b.enableEventAnimations = c;
  }
  beforeDrag(a) {
    const b = this, c = super.beforeDrag(a), {
        pan: e,
        eventDragSelect: f
      } = b.client.features;
    if (c !== false && (b.preventMultiple && !b.isRowEmpty(a.rowRecord) || b.disabled || e && !e.disabled || f && !f.disabled)) {
      return false;
    }
    b.client.preventDragSelect = true;
    return c;
  }
  startDrag(a) {
    const b = super.startDrag(a);
    if (b !== false) {
      const [c] = a.context.eventContexts;
      a.initialDate = c.eventRecord.get(this.draggingEnd ? 'startDate' : 'endDate');
      this.client.trigger('dragCreateStart', {
        'proxyElement': a.element,
        'eventElement': a.element,
        'eventRecord': c.eventRecord,
        'resourceRecord': c.resourceRecord
      });
      a.context.offset = 0;
      a.context.oldValue = a.mousedownDate;
    }
    return b;
  }
  isOverEndHandle() {
    return this.draggingEnd;
  }
  setupDragContext(a) {
    let b;
    const {client: c} = this;
    if (c.matchScheduleCell(a.target)) {
      const e = (b = c.resolveResourceRecord(a)) == null ? void 0 : b.$original;
      if (e && !e.isSpecialRow) {
        const f = Draggable_default().prototype.setupDragContext.call(this, a), g = [];
        if (c.isVertical) {
          g.push({
            'element': c.scrollable.element,
            'direction': 'vertical'
          });
        } else {
          g.push({
            'element': c.timeAxisSubGrid.scrollable.element,
            'direction': 'horizontal'
          });
        }
        f.scrollManager = c.scrollManager;
        f.monitoringConfig = { 'scrollables': g };
        f.resourceRecord = f.rowRecord = e;
        f.getDistance = getDragCreateDragDistance;
        return f;
      }
    }
  }
  async dragDrop({
    context: a,
    event: b
  }) {
    let c;
    const {client: e} = this, {toSet: f} = a, [g] = a.eventContexts;
    g[f] = g.snappedDate;
    const {
        startDate: i,
        endDate: j,
        eventRecord: k
      } = g, {generation: l} = k;
    let n;
    (c = this.tip) == null ? void 0 : c.hide();
    await e.project.commitAsync();
    if (k.generation !== l) {
      k[f] = g.oldValue;
      k[f] = g[f];
    }
    a.valid = i && j && j - i > 0 && g[f] - g.oldValue && a.valid !== false;
    if (a.valid) {
      a.startDate = g.startDate;
      a.endDate = g.endDate;
      e.trigger('beforeDragCreateFinalize', {
        'context': a,
        'event': b,
        'proxyElement': g.element,
        'eventElement': g.element,
        'eventRecord': g.eventRecord,
        'resourceRecord': g.resourceRecord
      });
      n = true;
    }
    if (!a.async) {
      await a.finalize(n);
    }
  }
  updateDragTolerance(a) {
    this.dragThreshold = a;
  }
  changeTip(a, b) {
    return super.changeTip(!a || a.isTooltip ? a : ObjectHelper.assign({ 'id': this.client.id + '-drag-create-tip' }, a), b);
  }
  async finalize(a) {
    let b;
    if (this.finalized) {
      return;
    }
    this.finalized = true;
    const c = this.owner, e = this, [f] = e.eventContexts, g = () => {
        if (!c.isDestroyed) {
          c.client.trigger('afterDragCreate', {
            'proxyElement': f.element,
            'eventElement': f.element,
            'eventRecord': f.eventRecord,
            'resourceRecord': f.resourceRecord
          });
          c.cleanup(e);
        }
      };
    if (a) {
      await c.finalizeDragCreate(e);
      g();
    } else {
      await c.cancelDragCreate(e);
      (b = c.onAborted) == null ? void 0 : b.call(c, e);
      g();
    }
  }
  async cancelDragCreate(a) {
  }
  async finalizeDragCreate(a) {
    const b = this, {client: c} = b, [e] = a.eventContexts, {
        eventRecord: f,
        resourceRecord: g
      } = e;
    if (c.snapRelativeToEventStartDate) {
      e[a.toSet] = e.snappedDate = c.timeAxis.roundDate(e.date, e.oldValue);
    }
    b.adjustRecordBeforeUpdating(e, f);
    c.endListeningForBatchedUpdates();
    await b.internalUpdateRecord(e, f);
    const i = b.getStmCapture();
    !c.isDestroyed && c.trigger('dragCreateEnd', {
      'eventRecord': f,
      'resourceRecord': g,
      'event': a.event,
      'eventElement': e.element,
      'stmCapture': i
    });
    !c.isDestroyed && c.trigger('eventAutoCreated', {
      'eventRecord': f,
      'resourceRecord': g
    });
    return i.transferred;
  }
  cleanup(a) {
    let b;
    const {client: c} = this, [e] = a.eventContexts;
    e.eventRecord.meta.isResizing = false;
    c.endListeningForBatchedUpdates();
    (b = this.tip) == null ? void 0 : b.hide();
    c.element.classList.remove(...this.dragActiveCls.split(' '));
    e.element.parentElement.classList.remove('b-sch-dragcreating');
  }
  onElementContextMenu() {
    if (this.proxy) {
      return false;
    }
  }
  prepareCreateContextForFinalization(a, b, c, e = false) {
    return {
      ...a,
      'async': e,
      'event': b,
      'finalize': c
    };
  }
  onEventDataGenerated(a) {
    let b, c;
    if (((c = (b = this.dragging) == null ? void 0 : b.context) == null ? void 0 : c.eventRecord) === a.eventRecord) {
      a.wrapperCls['b-sch-dragcreating'] = true;
      a.wrapperCls['b-too-narrow'] = this.dragging.context.tooNarrow;
    }
  }
  get isDragCreating() {
    const {dragging: a} = this;
    return a && a.state === a.constructor.STATE.DRAGGING;
  }
  triggerBeforeResize() {
  }
  triggerEventResizeStart() {
  }
  checkValidity(a, b) {
    throw new Error('Implement in subclass');
  }
  handleBeforeDragCreate(a, b) {
    throw new Error('Implement in subclass');
  }
  isRowEmpty(a) {
    throw new Error('Implement in subclass');
  }
};
DragCreateBase._$name = 'DragCreateBase';
const DH3 = DateHelper;
const scheduleFields = [
  'startDate',
  'endDate',
  'resource',
  'recurrenceRule'
];
const makeDate = a => {
  if (a.length === 1)
    return a[0].value;
  else if (a.length === 2) {
    const [b, c] = a[0] instanceof DateField ? a : a.reverse(), e = DH3.parse(b.value);
    if (e && c.value) {
      e.setHours(c.value.getHours(), c.value.getMinutes(), c.value.getSeconds(), c.value.getMilliseconds());
    }
    return e ? DateHelper.clone(e) : null;
  }
  return null;
};
const copyTime = (a, b) => {
  const c = new Date(a.getTime());
  c.setHours(b.getHours(), b.getMinutes());
  return c;
};
const adjustEndDate = (a, b, c) => {
  if (!c.editor.assigningValues && a && b && c.endDateField && c.endTimeField) {
    const e = DH3.add(copyTime(c.startDateField.value, c.startTimeField.value), c._durationMS, 'milliseconds');
    c.endDateField.value = e;
    c.endTimeField.value = DH3.clone(e);
  }
};
const EditBase = class extends InstancePlugin {
  static get configurable() {
    return {
      'saveAndCloseOnEnter': true,
      'triggerEvent': null,
      'dateFormat': 'L',
      'timeFormat': 'LT',
      'editorConfig': null,
      'items': null,
      'weekStartDay': null
    };
  }
  construct(a, b) {
    const c = this;
    a.eventEdit = c;
    super.construct(a, ObjectHelper.assign({ 'weekStartDay': a.weekStartDay }, b));
    c.clientListenersDetacher = a.ion({
      [c.triggerEvent]: 'onActivateEditor',
      'dragCreateEnd': 'onDragCreateEnd',
      'eventAutoCreated': 'onEventAutoCreated',
      'thisObj': c
    });
  }
  doDestroy() {
    let a;
    this.clientListenersDetacher();
    (a = this._editor) == null ? void 0 : a.destroy();
    super.doDestroy();
  }
  onEventAutoCreated() {
  }
  changeEditorConfig(a) {
    const {items: b} = this;
    if (b) {
      a = Objects.clone(a);
      a.items = Config.merge(b, a.items);
    }
    return a;
  }
  changeItems(a) {
    this.cleanItemsConfig(a);
    return a;
  }
  cleanItemsConfig(a) {
    for (const b in a) {
      const c = a[b];
      if (c === true) {
        delete a[b];
      } else if (c == null ? void 0 : c.items) {
        this.cleanItemsConfig(c.items);
      }
    }
  }
  onDatesChange({
    value: a,
    source: b
  }) {
    let c, e, f, g, i, j, k, l, n, o;
    const p = this;
    if ((b === p.endDateField || b === p.endTimeField) && p.startDateField) {
      const q = ((c = p.endTimeField) == null ? void 0 : c.value) && ((e = p.endDateField) == null ? void 0 : e.value) ? copyTime(p.endDateField.value, p.endTimeField.value) : (f = p.endDateField) == null ? void 0 : f.value, r = ((g = p.startTimeField) == null ? void 0 : g.value) && ((i = p.startDateField) == null ? void 0 : i.value) ? copyTime(p.startDateField.value, p.startTimeField.value) : (j = p.startDateField) == null ? void 0 : j.value;
      if (q && r) {
        p._durationMS = q - r;
      }
    }
    if (p.startDateField && p.endDateField) {
      p.endDateField.min = p.startDateField.value;
    }
    if (p.endTimeField) {
      if (DH3.isEqual(DH3.clearTime((k = p.startDateField) == null ? void 0 : k.value), DH3.clearTime((l = p.endDateField) == null ? void 0 : l.value))) {
        p.endTimeField.min = p.startTimeField.value;
      } else {
        p.endTimeField.min = null;
      }
    }
    switch (b.ref) {
    case 'startDateField':
      ((n = p.startTimeField) == null ? void 0 : n.value) && adjustEndDate(a, p.startTimeField.value, p);
      break;
    case 'startTimeField':
      ((o = p.startDateField) == null ? void 0 : o.value) && adjustEndDate(p.startDateField.value, a, p);
      break;
    }
  }
  async save() {
    throw new Error('Implement in subclass');
  }
  get values() {
    const a = this, {editor: b} = a, c = [], e = [], {values: f} = b;
    scheduleFields.forEach(g => delete f[g]);
    b.eachWidget(g => {
      let i;
      const {name: j} = g;
      if (!j || g.hidden || g.up(k => k === a.recurrenceEditor)) {
        delete f[j];
        return;
      }
      switch (j) {
      case 'startDate':
        c.push(g);
        break;
      case 'endDate':
        e.push(g);
        break;
      case 'resource':
        f[j] = g.record;
        break;
      case 'recurrenceRule':
        f[j] = ((i = b.widgetMap.recurrenceCombo) == null ? void 0 : i.value) === 'none' ? '' : g.value;
        break;
      }
    }, true);
    if (f.allDay && !a.eventRecord.allDay) {
      c.push(a.startTimeField);
      e.push(a.endTimeField);
    }
    if (c.length) {
      f.startDate = makeDate(c);
    }
    if (e.length) {
      f.endDate = makeDate(e);
    }
    if ('startDate' in f && 'endDate' in f) {
      f.duration = DH3.diff(f.startDate, f.endDate, a.editor.record.durationUnit, true);
    }
    return f;
  }
  onBeforeSave(a) {
  }
  onAfterSave(a) {
  }
  updateRecord(a) {
    let b;
    const {values: c} = this;
    if (this.assignmentStore) {
      delete c.resource;
    }
    this._durationMS = DateHelper.asMilliseconds((b = c.duration) != null ? b : a.duration, a.durationUnit);
    return a.set(c);
  }
  onBeforeEditorShow() {
    const {
        eventRecord: a,
        editor: b
      } = this.editingContext, {nameField: c} = b.widgetMap;
    if (c && a.isCreating) {
      b.assigningValues = true;
      c.value = '';
      b.assigningValues = false;
      c._configuredPlaceholder = c.placeholder;
      c.placeholder = a.name;
    }
  }
  resetEditingContext() {
    let a;
    const b = this;
    if (!b.editingContext) {
      return;
    }
    const {client: c} = b, {
        editor: e,
        eventRecord: f
      } = b.editingContext, {eventStore: g} = c, {nameField: i} = e.widgetMap;
    if (f.isCreating) {
      if (c.isTimelineBase) {
        (a = b.editingContext.eventElement) == null ? void 0 : a.closest('[data-event-id]').classList.add('b-released');
      }
      g.remove(f);
      f.isCreating = false;
    }
    if (i) {
      i.placeholder = i._configuredPlaceholder;
    }
    c.element.classList.remove('b-eventeditor-editing');
    b.targetEventElement = b.editingContext = e._record = null;
  }
  onPopupKeyDown({event: a}) {
    const b = this;
    if (!b.readOnly && a.key === 'Enter' && b.saveAndCloseOnEnter && a.target.tagName.toLowerCase() === 'input') {
      a.preventDefault();
      if (a.target.name === 'startDate') {
        b.startTimeField && adjustEndDate(b.startDateField.value, b.startTimeField.value, b);
      }
      b.onSaveClick();
    }
  }
  async finalizeStmCapture(a) {
  }
  async onSaveClick() {
    this.editor.focus();
    this.isFinalizingEventSave = true;
    const a = await this.save();
    this.isFinalizingEventSave = false;
    if (a) {
      await this.finalizeStmCapture(false);
      this.editor.close();
      this.client.trigger('afterEventEdit');
    }
    return a;
  }
  async onDeleteClick() {
    this.isDeletingEvent = true;
    const a = await this.deleteEvent();
    this.isDeletingEvent = false;
    if (a) {
      await this.finalizeStmCapture(false);
      const {editor: b} = this;
      if (!b.autoClose || b.containsFocus) {
        b.close();
      }
      this.client.trigger('afterEventEdit');
    }
  }
  async onCancelClick() {
    this.isCancelingEdit = true;
    this.editor.close();
    this.isCancelingEdit = false;
    if (this.hasStmCapture) {
      await this.finalizeStmCapture(true);
    }
    this.client.trigger('afterEventEdit');
  }
};
EditBase._$name = 'EditBase';
const ResourceTimeRangesBase = class extends InstancePlugin.mixin(AttachToProjectMixin_default) {
  static configurable = {
    'tabIndex': null,
    'entityName': 'resourceTimeRange'
  };
  static get pluginConfig() {
    return {
      'before': ['onInternalPaint'],
      'chain': [
        'getEventsToRender',
        'onEventDataGenerated',
        'noFeatureElementsInAxis'
      ],
      'override': [
        'matchScheduleCell',
        'resolveResourceRecord'
      ]
    };
  }
  noFeatureElementsInAxis() {
    const {timeAxis: a} = this.client;
    return !this.needsRefresh && this.store && !this.store.storage.values.some(b => a.isTimeSpanInAxis(b));
  }
  doDisable(a) {
    if (this.client.isPainted) {
      this.client.refresh();
    }
    super.doDisable(a);
  }
  updateTabIndex() {
    if (!this.isConfiguring) {
      this.client.refresh();
    }
  }
  onInternalPaint({firstPaint: a}) {
    a && this.setupCanvas();
  }
  setupCanvas() {
    const {
      entityName: a,
      client: b
    } = this;
    if (!this.canvas) {
      this.canvas = b.canvases[a] = DomHelper.createElement({
        'className': 'b-' + StringHelper.hyphenate(a) + '-canvas b-sch-canvas',
        'parent': b.timeAxisSubGridElement,
        'retainElement': true
      });
    }
  }
  getEventsToRender(a, b) {
    throw new Error('Implement in subclass');
  }
  onEventDataGenerated(a) {
    const b = this, {client: c} = b, {
        eventRecord: e,
        iconCls: f
      } = a;
    if (b.shouldInclude(e)) {
      if (c.isVertical) {
        a.width = c.getResourceWidth(a.resourceRecord);
      } else {
        a.top = 0;
      }
      a.entityName = b.entityName;
      a.fillSize = true;
      a.wrapperCls['b-sch-resourcetimerange'] = 1;
      if (b.rangeCls) {
        a.wrapperCls[b.rangeCls] = 1;
      }
      a.wrapperCls['b-sch-color-' + e.timeRangeColor] = e.timeRangeColor;
      b.renderContent(e, a);
      a.children.push(a.eventContent);
      a.tabIndex = b.tabIndex != null ? String(b.tabIndex) : null;
      if ((f == null ? void 0 : f.length) > 0) {
        a.children.unshift({
          'tag': 'i',
          'className': f.toString()
        });
      }
      a.eventId = b.generateElementId(e);
    }
  }
  renderContent(a, b) {
    b.eventContent.text = a.name;
  }
  generateElementId(a) {
    return a.domId;
  }
  resolveResourceTimeRangeRecord(a) {
    let b;
    return (b = a == null ? void 0 : a.closest('.' + this.rangeCls)) == null ? void 0 : b.elementData.eventRecord;
  }
  getElementFromResourceTimeRangeRecord(a) {
    return this.canvas.syncIdMap[a.domId];
  }
  resolveResourceRecord(a) {
    let b;
    const c = this.overridden.resolveResourceRecord(...arguments);
    return c || ((b = this.resolveResourceTimeRangeRecord(a.target || a)) == null ? void 0 : b.resource);
  }
  shouldInclude(a) {
    throw new Error('Implement in subclass');
  }
  onStoreChange(a) {
    if (a.action === 'removeall' || a.action === 'dataset') {
      this.needsRefresh = true;
    }
    this.client.onInternalEventStoreChange(a);
    this.needsRefresh = false;
  }
  matchScheduleCell(a) {
    let b = this.overridden.matchScheduleCell(a);
    if (!b && this.enableMouseEvents) {
      const {client: c} = this, e = a.closest('.' + this.rangeCls);
      b = e && c.getCell({
        'record': c.isHorizontal ? e.elementData.resource : c.store.first,
        'column': c.timeAxisColumn
      });
    }
    return b;
  }
  handleRangeMouseEvent(a) {
    let b;
    const c = this, e = a.target.closest('.' + c.rangeCls);
    if (e) {
      const f = (b = EventHelper.eventNameMap[a.type]) != null ? b : StringHelper.capitalize(a.type), g = c.resolveResourceTimeRangeRecord(e);
      c.client.trigger(c.entityName + f, {
        'feature': c,
        [c.entityName + 'Record']: g,
        'resourceRecord': c.client.resourceStore.getById(g.resourceId),
        'domEvent': a
      });
    }
  }
  updateEnableMouseEvents(a) {
    let b;
    const c = this, {client: e} = c;
    (b = c.mouseEventsDetacher) == null ? void 0 : b.call(c);
    c.mouseEventsDetacher = null;
    e.whenVisible(() => {
      if (a) {
        c.mouseEventsDetacher = EventHelper.on({
          'element': e.canvases[c.entityName],
          'delegate': '.' + c.rangeCls,
          'mousedown': 'handleRangeMouseEvent',
          'mouseup': 'handleRangeMouseEvent',
          'click': 'handleRangeMouseEvent',
          'dblclick': 'handleRangeMouseEvent',
          'contextmenu': 'handleRangeMouseEvent',
          'mouseover': 'handleRangeMouseEvent',
          'mouseout': 'handleRangeMouseEvent',
          'thisObj': c
        });
      }
      c.canvas.classList.toggle('b-interactive', Boolean(a));
    });
  }
};
ResourceTimeRangesBase.featureClass = '';
ResourceTimeRangesBase._$name = 'ResourceTimeRangesBase';
const TimeSpanMenuBase = class extends ContextMenuBase {
};
TimeSpanMenuBase._$name = 'TimeSpanMenuBase';
let id = 0;
const TooltipBase = class extends InstancePlugin {
  static get defaultConfig() {
    return {
      'autoUpdate': false,
      'hoverDelay': 250,
      'hideDelay': 100,
      'template': null,
      'cls': null,
      'align': { 'align': 'b-t' },
      'clockTemplate': null,
      'monitorRecordUpdate': null,
      'testConfig': { 'hoverDelay': 0 }
    };
  }
  static get pluginConfig() {
    return { 'chain': ['onInternalPaint'] };
  }
  construct(a, b) {
    const c = this;
    b = c.processConfig(b);
    super.construct(a, b);
    if (!c.forSelector) {
      c.forSelector = a.eventInnerSelector + ':not(.b-dragproxy,.b-iscreating)';
    }
    c.clockTemplate = new ClockTemplate({ 'scheduler': a });
    a.ion({
      ['before' + a.scheduledEventName + 'drag']: () => {
        let e;
        (e = c.tooltip) == null ? void 0 : e.hide();
      }
    });
  }
  processConfig(a) {
    if (typeof a === 'function') {
      return { 'template': a };
    }
    return a;
  }
  setConfig(a) {
    super.setConfig(this.processConfig(a));
  }
  doDestroy() {
    this.destroyProperties('clockTemplate', 'tooltip');
    super.doDestroy();
  }
  doDisable(a) {
    if (this.tooltip) {
      this.tooltip.disabled = a;
    }
    super.doDisable(a);
  }
  onInternalPaint({firstPaint: a}) {
    let b;
    if (a) {
      const c = this, {client: e} = c, f = ':not(' + [
          '.b-dragselecting',
          '.b-eventeditor-editing',
          '.b-taskeditor-editing',
          '.b-resizing-event',
          '.b-task-percent-bar-resizing-task',
          '.b-dragcreating',
          '.b-dragging-' + e.scheduledEventName,
          '.b-creating-dependency',
          '.b-dragproxy'
        ].join() + ')';
      (b = c.tooltip) == null ? void 0 : b.destroy();
      const g = c.tooltip = new Tooltip({
        'axisLock': 'flexible',
        'id': c.tipId || c.client.id + '-event-tip-' + id++,
        'cls': c.tipCls,
        'forSelector': '.b-timelinebase' + f + ' .b-grid-body-container:not(.b-scrolling) ' + c.forSelector,
        'scrollAction': 'realign',
        'forElement': e.timeAxisSubGridElement,
        'showOnHover': true,
        'anchorToTarget': true,
        'getHtml': c.getTipHtml.bind(c),
        'disabled': c.disabled,
        ...c.config,
        'internalListeners': c.configuredListeners
      });
      g.ion({
        'innerhtmlupdate': 'updateDateIndicator',
        'overtarget': 'onOverNewTarget',
        'show': 'onTipShow',
        'hide': 'onTipHide',
        'thisObj': c
      });
      Object.keys(g.$meta.configs).forEach(i => {
        Object.defineProperty(this, i, {
          'set': j => g[i] = j,
          'get': () => g[i]
        });
      });
    }
  }
  processConfiguredListeners() {
  }
  addListener(...a) {
    let b;
    const c = super.addListener(...a), e = (b = this.tooltip) == null ? void 0 : b.addListener(...a);
    if (c || e) {
      return () => {
        c == null ? void 0 : c();
        e == null ? void 0 : e();
      };
    }
  }
  removeListener(...a) {
    let b;
    super.removeListener(...a);
    (b = this.tooltip) == null ? void 0 : b.removeListener(...a);
  }
  updateDateIndicator() {
    const a = this, b = a.tooltip, c = b.element.querySelector('.b-sch-tooltip-enddate');
    if (!a.record) {
      return;
    }
    a.clockTemplate.updateDateIndicator(b.element, a.record.startDate);
    c && a.clockTemplate.updateDateIndicator(c, a.record.endDate);
  }
  resolveTimeSpanRecord(a) {
    return this.client.resolveTimeSpanRecord(a);
  }
  getTipHtml({
    tip: a,
    activeTarget: b
  }) {
    const c = this, {client: e} = c, f = c.recordType || e.scheduledEventName + 'Record', g = c.resolveTimeSpanRecord(b);
    if ((g == null ? void 0 : g.startDate) instanceof Date) {
      a.eventRecord = a.record = g;
      if (!c.template) {
        return;
      }
      const {
          startDate: i,
          endDate: j
        } = g, k = e.getFormattedDate(i), l = e.getDisplayEndDate(j, i), n = e.getFormattedDate(l);
      return c.template({
        'tip': a,
        ['' + f]: g,
        'startDate': i,
        'endDate': j,
        'startText': k,
        'endText': n,
        'startClockHtml': c.clockTemplate.template({
          'date': i,
          'text': k,
          'cls': 'b-sch-tooltip-startdate'
        }),
        'endClockHtml': g.isMilestone ? '' : c.clockTemplate.template({
          'date': l || '',
          'text': n || '',
          'cls': 'b-sch-tooltip-enddate'
        })
      });
    } else {
      a.hide();
      return '';
    }
  }
  get record() {
    return this.tooltip.eventRecord;
  }
  onTipShow() {
    const a = this;
    if (a.monitorRecordUpdate && !a.updateListener) {
      a.updateListener = a.client.eventStore.ion({
        'change': a.onRecordUpdate,
        'buffer': 300,
        'thisObj': a
      });
    }
  }
  onTipHide() {
    let a;
    this.tooltip.eventRecord = this.tooltip.record = null;
    (a = this.updateListener) == null ? void 0 : a.call(this);
    this.updateListener = null;
  }
  onOverNewTarget({newTarget: a}) {
    const {tooltip: b} = this;
    if (b.isVisible) {
      if (this.client.timeAxisSubGrid.scrolling || this.client.scrolling) {
        b.hide(false);
      } else {
        b.eventRecord = this.resolveTimeSpanRecord(a);
      }
    }
  }
  onRecordUpdate({record: a}) {
    const {tooltip: b} = this;
    if ((b == null ? void 0 : b.isVisible) && a === this.record) {
      b.updateContent();
      if (b.lastAlignSpec.aligningToElement) {
        b.realign();
      } else {
        b.internalOnPointerOver(this.client.lastPointerEvent);
      }
    }
  }
};
TooltipBase._$name = 'TooltipBase';
const AbstractTimeRanges = class extends InstancePlugin.mixin(Delayable_default) {
  static configurable = { 'showHeaderElements': true };
  static get defaultConfig() {
    return {
      'rangeCls': 'b-sch-range',
      'lineCls': 'b-sch-line',
      'enableResizing': false,
      'showTooltip': true,
      'hoverTooltip': null,
      'tooltipTemplate': null,
      'dragTipTemplate': a => '\n                <div class="b-sch-tip-' + (a.valid ? 'valid' : 'invalid') + '">\n                    <div class="b-sch-tip-name">' + (StringHelper.encodeHtml(a.name) || '') + '</div>\n                    ' + a.startClockHtml + '\n                    ' + (a.endClockHtml || '') + '\n                </div>\n            ',
      'baseCls': 'b-sch-timerange',
      'headerRenderer': null,
      'bodyRenderer': null,
      'cls': null,
      'narrowThreshold': 80
    };
  }
  static get pluginConfig() {
    return {
      'chain': [
        'onInternalPaint',
        'populateTimeAxisHeaderMenu',
        'onSchedulerHorizontalScroll',
        'afterScroll',
        'onInternalResize'
      ]
    };
  }
  construct(a, b) {
    const c = this;
    super.construct(a, b);
    if (a.isVertical) {
      a.ion({
        'renderRows': c.onUIReady,
        'thisObj': c,
        'once': true
      });
    }
    c.cls = c.cls || 'b-sch-' + c.constructor.$$name.toLowerCase();
    c.baseSelector = '.' + c.baseCls + '.' + c.cls;
    if (c.enableResizing) {
      c.showHeaderElements = true;
    }
  }
  doDestroy() {
    let a, b, c, e;
    const f = this;
    f.detachListeners('timeAxisViewModel');
    f.detachListeners('timeAxis');
    (a = f.clockTemplate) == null ? void 0 : a.destroy();
    (b = f.tip) == null ? void 0 : b.destroy();
    (c = f.drag) == null ? void 0 : c.destroy();
    (e = f.resize) == null ? void 0 : e.destroy();
    super.doDestroy();
  }
  doDisable(a) {
    if (!this.isConfiguring) {
      this.renderRanges();
    }
    super.doDisable(a);
  }
  setupTimeAxisViewModelListeners() {
    const a = this;
    a.detachListeners('timeAxisViewModel');
    a.detachListeners('timeAxis');
    a.client.timeAxisViewModel.ion({
      'name': 'timeAxisViewModel',
      'update': 'onTimeAxisViewModelUpdate',
      'thisObj': a
    });
    a.client.timeAxis.ion({
      'name': 'timeAxis',
      'includeChange': 'renderRanges',
      'thisObj': a
    });
    a.updateLineBuffer();
  }
  onUIReady() {
    const a = this, {client: b} = a;
    b.ion({
      'timeAxisViewModelChange': a.setupTimeAxisViewModelListeners,
      'thisObj': a
    });
    a.setupTimeAxisViewModelListeners();
    if (!b.hideHeaders) {
      if (a.headerContainerElement) {
        EventHelper.on({
          'click': a.onTimeRangeClick,
          'dblclick': a.onTimeRangeClick,
          'contextmenu': a.onTimeRangeClick,
          'delegate': a.baseSelector,
          'element': a.headerContainerElement,
          'thisObj': a
        });
      }
      if (a.enableResizing) {
        a.drag = DragHelper.new({
          'name': 'rangeDrag',
          'lockX': b.isVertical,
          'lockY': b.isHorizontal,
          'constrain': true,
          'outerElement': a.headerContainerElement,
          'targetSelector': '' + a.baseSelector,
          'isElementDraggable': (c, e) => !b.readOnly && a.isElementDraggable(c, e),
          'rtlSource': b,
          'positioning': 'inset',
          'internalListeners': {
            'dragStart': 'onDragStart',
            'drag': 'onDrag',
            'drop': 'onDrop',
            'reset': 'onDragReset',
            'abort': 'onInvalidDrop',
            'thisObj': a
          }
        }, a.dragHelperConfig);
        a.resize = ResizeHelper.new({
          'direction': b.mode,
          'targetSelector': a.baseSelector + '.b-sch-range',
          'outerElement': a.headerContainerElement,
          'isElementResizable': (c, e) => !c.matches('.b-dragging,.b-readonly') && !e.target.matches('.b-fa'),
          'rtlSource': b,
          'positioning': 'inset',
          'internalListeners': {
            'resizeStart': 'onResizeStart',
            'resizing': 'onResizeDrag',
            'resize': 'onResize',
            'cancel': 'onInvalidResize',
            'reset': 'onResizeReset',
            'thisObj': a
          }
        }, a.resizeHelperConfig);
      }
    }
    a.renderRanges();
    if (a.tooltipTemplate) {
      a.hoverTooltip = new Tooltip(ObjectHelper.assign({
        'forElement': a.headerContainerElement,
        'getHtml'({activeTarget: c}) {
          const e = a.resolveTimeRangeRecord(c);
          return a.tooltipTemplate({ 'timeRange': e });
        },
        'forSelector': '.b-timelinebase:not(.b-dragging-timerange, .b-resizing-timerange) .' + a.baseCls + (a.cls ? '.' + a.cls : '')
      }, a.hoverTooltip));
    }
  }
  refresh() {
    this._timeRanges = null;
    this.renderRanges();
  }
  getDOMConfig(a, b) {
    const c = this, e = [], f = [];
    if (!c.disabled) {
      c._labelRotationMap = {};
      for (const g of c.timeRanges) {
        const i = c.renderRange(g, a, b);
        if (i) {
          e.push(i.bodyConfig);
          f.push(i.headerConfig);
        }
      }
    }
    return [
      e,
      f
    ];
  }
  renderRanges() {
    let a;
    const b = this, {client: c} = b;
    if (c.isPainted && !c.timeAxisSubGrid.collapsed) {
      const {headerContainerElement: e} = b, f = [], [g, i] = b.getDOMConfig();
      if ((a = b.store) == null ? void 0 : a.lazyLoad) {
        b.store.lazyLoad.doLazyLoad({
          'startDate': c.startDate,
          'endDate': c.endDate,
          'visibleStart': c.visibleDateRange.startDate,
          'visibleEnd': c.visibleDateRange.endDate
        });
      }
      if (!b.bodyCanvas) {
        b.bodyCanvas = DomHelper.createElement({
          'className': 'b-timeranges-canvas b-timeranges-body-canvas ' + b.cls + '-canvas b-sch-canvas',
          'parent': c.timeAxisSubGridElement,
          'retainElement': true
        });
      }
      DomSync.sync({
        'targetElement': b.bodyCanvas,
        'domConfig': {
          'children': g,
          'onlyChildren': true,
          'syncOptions': {
            'releaseThreshold': 0,
            'syncIdField': 'id'
          }
        },
        'callback': b.showHeaderElements ? null : ({
          targetElement: j,
          action: k
        }) => {
          if (k === 'reuseElement' || k === 'newElement' || k === 'reuseOwnElement') {
            f.push(j);
          }
        }
      });
      if (b.showHeaderElements && !b.headerCanvas) {
        b.headerCanvas = DomHelper.createElement({
          'className': 'b-timeranges-canvas b-timeranges-header-canvas ' + b.cls + '-canvas',
          'parent': e,
          'retainElement': true
        });
      }
      if (b.headerCanvas) {
        DomSync.sync({
          'targetElement': b.headerCanvas,
          'domConfig': {
            'onlyChildren': true,
            'children': i,
            'syncOptions': {
              'releaseThreshold': 0,
              'syncIdField': 'id'
            }
          }
        });
      }
      for (const j of f) {
        b.cacheRotation(j.elementData.timeRange, j);
      }
      for (const k of f) {
        b.applyRotation(k.elementData.timeRange, k);
      }
    }
  }
  get timeRanges() {
    return [];
  }
  shouldRenderRange(a, b = this.client.visibleDateRange.startDate, c = this.client.visibleDateRange.endDate) {
    const {timeAxis: e} = this.client, {
        startDate: f,
        endDate: g,
        duration: i
      } = a;
    return Boolean(f && (e.isContinuous || e.isTimeSpanInAxis(a)) && DateHelper.intersectSpans(b, c, f, i ? g : DateHelper.add(f, this._lineBufferDurationMS)));
  }
  getRangeDomConfig(a, b, c, e = 0) {
    const f = this, {client: g} = f, {rtl: i} = g, j = g.getVirtualCoordFromDate(DateHelper.max(a.startDate, b), { 'respectExclusion': true }) - e, k = a.endDate ? g.getVirtualCoordFromDate(DateHelper.min(a.endDate, c), {
        'respectExclusion': true,
        'isEnd': true
      }) - e : j, l = Math.abs(k - j), n = l > 0;
    return {
      'className': {
        [f.baseCls]: 1,
        [f.cls]: f.cls,
        [f.rangeCls]: n,
        [f.lineCls]: !n,
        [a.cls]: a.cls,
        'b-narrow-range': n && l < f.narrowThreshold,
        'b-readonly': a.readOnly,
        'b-rtl': i
      },
      'dataset': { 'id': a.id },
      'elementData': { 'timeRange': a },
      'style': g.isVertical ? 'inset-block-start: ' + j + 'px; ' + (n ? 'height:' + l + 'px' : '') + ';' : 'inset-inline-start: ' + j + 'px; ' + (n ? 'width:' + l + 'px' : '') + ';'
    };
  }
  renderRange(a, b, c) {
    const e = this, {client: f} = e, {timeAxis: g} = f;
    if (e.shouldRenderRange(a, b, c) && g.startDate) {
      const i = e.getRangeDomConfig(a, g.startDate, g.endDate), j = a.iconCls && StringHelper.xss`<i class="${ a.iconCls }"></i>`, k = a.name && StringHelper.encodeHtml(a.name), l = k || j ? (j || '') + '<label>' + (k || '&nbsp;') + '</label>' : '', n = {
          ...i,
          'style': i.style + (a.style || ''),
          'html': e.bodyRenderer ? e.bodyRenderer({ 'timeRange': a }) : e.showHeaderElements && !e.showLabelInBody ? '' : l
        };
      let o;
      if (e.showHeaderElements) {
        o = {
          ...i,
          'html': e.headerRenderer ? e.headerRenderer({ 'timeRange': a }) : e.showLabelInBody ? '' : l
        };
      }
      return {
        'bodyConfig': n,
        'headerConfig': o
      };
    }
  }
  cacheRotation(a, b) {
    if (!a.iconCls && !a.name || !a.duration) {
      return;
    }
    const c = b.firstElementChild;
    if (c && !a.recurringTimeSpan) {
      this._labelRotationMap[a.id] = this.client.isVertical ? c.offsetHeight < b.offsetHeight : c.offsetWidth > b.offsetWidth;
    }
  }
  applyRotation(a, b) {
    let c, e, f;
    const g = this._labelRotationMap[(e = (c = a.recurringTimeSpan) == null ? void 0 : c.id) != null ? e : a.id];
    (f = b.firstElementChild) == null ? void 0 : f.classList.toggle('b-vertical', Boolean(g));
  }
  getBodyElementByRecord(a) {
    const b = typeof a === 'string' ? a : a == null ? void 0 : a.id;
    return b != null && DomSync.getChild(this.bodyCanvas, b);
  }
  resolveTimeRangeRecord(a) {
  }
  get headerContainerElement() {
    const a = this, {
        isVertical: b,
        timeAxisColumn: c
      } = a.client;
    if (!a._headerContainerElement) {
      if (b) {
        a._headerContainerElement = a.client.subGrids.locked.element;
      } else if (!b) {
        a._headerContainerElement = c.element;
      }
    }
    return a._headerContainerElement;
  }
  get showHeaderElements() {
    return !this.client.hideHeaders && this._showHeaderElements;
  }
  updateShowHeaderElements(a) {
    const {client: b} = this;
    if (!this.isConfiguring) {
      b.element.classList.toggle('b-sch-timeranges-with-headerelements', Boolean(a));
      this.renderRanges();
    }
  }
  populateTimeAxisHeaderMenu({
    column: a,
    items: b
  }) {
  }
  onInternalPaint({firstPaint: a}) {
    if (a && this.client.isHorizontal) {
      this.onUIReady();
    }
  }
  onSchedulerHorizontalScroll() {
    this.client.isHorizontal && this.renderRanges();
  }
  afterScroll() {
    this.client.isVertical && this.renderRanges();
  }
  updateLineBuffer() {
    const {timeAxisViewModel: a} = this.client;
    this._lineBufferDurationMS = a.getDateFromPosition(300) - a.getDateFromPosition(0);
  }
  onInternalResize(a, b, c, e, f) {
    if (this.client.isVertical && f !== c) {
      this.renderRanges();
    }
  }
  onTimeAxisViewModelUpdate() {
    this.updateLineBuffer();
    this.refresh();
  }
  onTimeRangeClick(a) {
    const b = this.resolveTimeRangeRecord(a.target);
    this.client.trigger('timeRangeHeader' + StringHelper.capitalize(a.type), {
      'domEvent': a,
      'timeRangeRecord': b
    });
  }
  showTip(a) {
    const b = this;
    if (b.showTooltip) {
      b.clockTemplate = new ClockTemplate({ 'scheduler': b.client });
      b.tip = new Tooltip(ObjectHelper.assign({
        'id': b.client.id + '-time-range-tip',
        'cls': 'b-interaction-tooltip',
        'align': 'b-t',
        'autoShow': true,
        'updateContentOnMouseMove': true,
        'forElement': a.element,
        'getHtml': () => b.getTipHtml(a.record, a.element)
      }, b.showTooltip));
    }
  }
  destroyTip() {
    if (this.tip) {
      this.tip.destroy();
      this.tip = null;
    }
  }
  isElementDraggable(a) {
    a = a.closest(this.baseSelector + ':not(.b-resizing):not(.b-readonly)');
    return a && !a.classList.contains('b-over-resize-handle');
  }
  onDragStart({context: a}) {
    const {
      client: b,
      drag: c
    } = this;
    if (b.isVertical) {
      c.minY = 0;
      c.maxY = b.timeAxisViewModel.totalSize - a.element.offsetHeight;
      c.minX = 0;
      c.maxX = Number.MAX_SAFE_INTEGER;
    } else {
      c.minX = 0;
      c.maxX = b.timeAxisViewModel.totalSize - a.element.offsetWidth;
      c.minY = 0;
      c.maxY = Number.MAX_SAFE_INTEGER;
    }
    b.element.classList.add('b-dragging-timerange');
  }
  onDrop({context: a}) {
    this.client.element.classList.remove('b-dragging-timerange');
  }
  onInvalidDrop() {
    this.drag.reset();
    this.client.element.classList.remove('b-dragging-timerange');
    this.destroyTip();
  }
  updateDateIndicator({
    startDate: a,
    endDate: b
  }) {
    const c = this, {tip: e} = c, f = e.element.querySelector('.b-sch-tooltip-enddate');
    c.clockTemplate.updateDateIndicator(e.element, a);
    f && c.clockTemplate.updateDateIndicator(f, b);
  }
  onDrag({context: a}) {
    const b = this, {client: c} = b, e = Rectangle.from(a.element), f = e.getStart(c.rtl, c.isHorizontal), g = e.getEnd(c.rtl, c.isHorizontal), i = c.getDateFromCoordinate(f, 'round', false), j = c.getDateFromCoordinate(g, 'round', false);
    b.updateDateIndicator({
      'startDate': i,
      'endDate': j
    });
  }
  onDragReset() {
  }
  onResizeStart() {
    let a;
    this.client.element.classList.add('b-resizing-timerange');
    (a = this.hoverTooltip) == null ? void 0 : a.hide();
  }
  onResizeDrag() {
  }
  onResize() {
  }
  onInvalidResize() {
  }
  onResizeReset() {
    this.client.element.classList.remove('b-resizing-timerange');
  }
  getTipHtml(a, b) {
    const c = this, {client: e} = c, f = Rectangle.from(b), g = f.getStart(e.rtl, e.isHorizontal), i = f.getEnd(e.rtl, e.isHorizontal), j = e.getDateFromCoordinate(g, 'round', false), k = a.endDate && e.getDateFromCoordinate(i, 'round', false), l = e.getFormattedDate(j), n = k && e.getFormattedEndDate(k, j);
    return c.dragTipTemplate({
      'name': a.name || '',
      'startDate': j,
      'endDate': k,
      'startText': l,
      'endText': n,
      'startClockHtml': c.clockTemplate.template({
        'date': j,
        'text': l,
        'cls': 'b-sch-tooltip-startdate'
      }),
      'endClockHtml': n && c.clockTemplate.template({
        'date': k,
        'text': n,
        'cls': 'b-sch-tooltip-enddate'
      })
    });
  }
};
AbstractTimeRanges._$name = 'AbstractTimeRanges';
const emptyObject14 = Object.freeze({});
const ColumnLines = class extends InstancePlugin.mixin(AttachToProjectMixin_default, Delayable_default) {
  static configurable = { 'renderer': null };
  static $name = 'ColumnLines';
  static get delayable() {
    return {
      'refresh': {
        'type': 'raf',
        'cancelOutstanding': true
      }
    };
  }
  static get pluginConfig() {
    return {
      'after': [
        'render',
        'updateCanvasSize',
        'internalOnVisibleDateRangeChange',
        'onVisibleResourceRangeChange',
        'onVisibleResourceColumnChange'
      ]
    };
  }
  attachToResourceStore(a) {
    const {client: b} = this;
    super.attachToResourceStore(a);
    if (b.isVertical) {
      b.resourceStore.ion({
        'name': 'resourceStore',
        'group'({groupers: c}) {
          if (c.length === 0) {
            this.refresh();
          }
        },
        'thisObj': this
      });
    }
  }
  doDisable(a) {
    super.doDisable(a);
    if (!this.isConfiguring) {
      this.refresh();
    }
  }
  render() {
    this.refresh();
  }
  getColumnLinesDOMConfig(a, b, c, e) {
    const f = this, {client: g} = f, {
        timeAxisViewModel: j,
        isHorizontal: k,
        resourceStore: l,
        variableColumnWidths: n
      } = g, {columnConfig: o} = j;
    const p = j.columnLinesFor, q = Math.max(p - 1, 0), r = a.getTime(), u = b.getTime(), v = [], w = new Set(), z = k ? 'insetInlineStart' : 'insetBlockStart';
    if (!f.disabled) {
      const A = (B, C) => {
        const E = B.start.getTime();
        if (E > r && E < u && !w.has(E)) {
          const F = {
            'role': 'presentation',
            'className': {
              'b-column-line-major': C,
              'b-column-line': !C
            },
            'style': { [z]: B.coord },
            'dataset': { 'line': C ? 'major-' + B.index : 'line-' + B.index }
          };
          w.add(E);
          v.push(F);
          if (f.renderer) {
            f.callback(f.renderer, f, [
              B.start,
              F
            ]);
          }
        }
      };
      if (p !== q) {
        for (let B = 1; B <= o[q].length - 1; B++) {
          A(o[q][B], true);
        }
      }
      for (let C = 1; C <= o[p].length - 1; C++) {
        A(o[p][C], false);
      }
      if (!k && g.columnLines) {
        const {columnWidth: E} = g.resourceColumns;
        if (!c && !e) {
          ({
            first: c,
            last: e
          } = g.currentOrientation.getResourceRange(true));
        }
        let F = 0;
        if (c > -1) {
          for (let G = c; G < e + 1; G++) {
            const H = l.getAt(G);
            if (H.isGroupHeader) {
              e++;
              F++;
              continue;
            }
            const I = H.instanceMeta(g), J = n ? I.insetStart + g.getResourceWidth(H) - 1 : (G - F + 1) * E - 1, K = H.instanceMeta(g.resourceStore).groupParent;
            v.push({
              'className': {
                'b-column-line': 1,
                'b-resource-column-line': 1,
                'b-resource-group-divider': l.isGrouped && (K == null ? void 0 : K.groupChildren[(K == null ? void 0 : K.groupChildren.length) - 1]) === H
              },
              'style': { 'insetInlineStart': J },
              'dataset': { 'line': 'resource-' + G }
            });
          }
        }
      }
    }
    return v;
  }
  refresh() {
    const a = this, {client: b} = a, {timeAxis: c} = b, {
        startDate: e,
        endDate: f
      } = b.visibleDateRange || emptyObject14, g = c.startDate;
    if (!g || !e || a.client.timeAxisSubGrid.collapsed) {
      return;
    }
    if (!a.element) {
      a.element = DomHelper.createElement({
        'parent': b.timeAxisSubGridElement,
        'className': 'b-column-lines-canvas b-sch-canvas'
      });
    }
    const i = a.getColumnLinesDOMConfig(e, f);
    DomSync.sync({
      'targetElement': a.element,
      'domConfig': {
        'onlyChildren': true,
        'children': i,
        'syncOptions': { 'releaseThreshold': 4 }
      },
      'syncIdField': 'line'
    });
  }
  internalOnVisibleDateRangeChange() {
    this.refresh.now();
  }
  onVisibleResourceRangeChange({
    firstResource: a,
    lastResource: b
  }) {
    this.refresh();
  }
  onVisibleResourceColumnChange(a, b) {
    this.refresh();
  }
  updateCanvasSize() {
    this.refresh();
  }
};
ColumnLines._$name = 'ColumnLines';
GridFeatureManager.registerFeature(ColumnLines, true, [
  'Scheduler',
  'Gantt',
  'TimelineHistogram'
]);
const DependencyCreation_default = a => {
  let b;
  return b = class extends (a || Base) {
    static get $name() {
      return 'DependencyCreation';
    }
    static get defaultConfig() {
      return {
        'showCreationTooltip': true,
        'creationTooltip': null,
        'terminalCls': 'b-sch-terminal',
        'terminalSides': [
          'start',
          'top',
          'end',
          'bottom'
        ],
        'allowCreate': true
      };
    }
    construct(c, e) {
      super.construct(c, e);
      const f = this;
      f.eventName = c.scheduledEventName;
      c.ion({ 'readOnly': () => f.updateCreateListeners() });
      f.updateCreateListeners();
      f.chain(c, 'onElementTouchMove', 'onElementTouchMove');
    }
    doDestroy() {
      let c, e;
      const f = this;
      f.detachListeners('view');
      f.creationData = null;
      (c = f.pointerUpMoveDetacher) == null ? void 0 : c.call(f);
      (e = f.creationTooltip) == null ? void 0 : e.destroy();
      super.doDestroy();
    }
    updateCreateListeners() {
      const c = this;
      if (!c.client) {
        return;
      }
      c.detachListeners('view');
      if (c.isCreateAllowed) {
        c.client.ion({
          'name': 'view',
          [c.eventName + 'MouseEnter']: 'onTimeSpanMouseEnter',
          [c.eventName + 'MouseLeave']: 'onTimeSpanMouseLeave',
          'thisObj': c
        });
      }
    }
    set allowCreate(c) {
      this._allowCreate = c;
      this.updateCreateListeners();
    }
    get allowCreate() {
      return this._allowCreate;
    }
    get isCreateAllowed() {
      return this.allowCreate && !this.client.readOnly && !this.disabled;
    }
    updateTerminalOffset(c) {
      this.client.whenVisible(() => {
        this.client.foregroundCanvas.style.setProperty('--scheduler-dependency-terminal-offset', -c + 'px');
      });
    }
    updateTerminalSize(c) {
      if (typeof c === 'number') {
        c = c + 'px';
      }
      this.client.whenVisible(() => {
        this.client.foregroundCanvas.style.setProperty('--scheduler-dependency-terminal-size', c ? '' + c : null);
      });
    }
    onTimeSpanMouseEnter({
      event: c,
      source: e,
      [this.eventName + 'Record']: f,
      [this.eventName + 'Element']: g,
      resourceRecord: i
    }) {
      if (!f.isCreating && !f.readOnly) {
        const j = this, {
            creationData: k,
            client: l
          } = j, n = DomHelper.down(g, e.eventInnerSelector);
        if (f !== (k == null ? void 0 : k.source)) {
          const {parent: o} = f;
          if (f.isEventModel && o && !o.isRoot && l.eventStore.includes(o)) {
            const p = l.getElementFromEventRecord(o, i, true);
            p && j.delayHideTerminals(p);
          }
          j.delayShowTerminals(f, g);
          if (k && c.target.closest(l.eventSelector)) {
            k.timeSpanElement = n;
            j.onOverTargetEventBar(c);
          }
        }
      }
    }
    onTimeSpanMouseLeave(c) {
      let e, f, g, i, j;
      const {
        eventRecord: k,
        event: l
      } = c;
      if (!l) {
        return;
      }
      const n = this, {
          creationData: o,
          client: p
        } = n, q = ((e = l.relatedTarget) == null ? void 0 : e.matches('.b-sch-terminal-hover-area, .b-sch-terminal')) ? null : (f = l.relatedTarget) == null ? void 0 : f.closest(p.eventSelector), r = (j = (g = q == null ? void 0 : q.elementData) == null ? void 0 : g.eventRecord) != null ? j : (i = q == null ? void 0 : q.elementData) == null ? void 0 : i.taskRecord, {parent: u} = k != null ? k : {}, v = c[n.eventName + 'Element'];
      if (l.isTrusted || VersionHelper.isTestEnv || o) {
        n.delayHideTerminals(v);
      }
      if (u && !u.isRoot && p.eventStore.includes(u)) {
        const w = p.getElementFromEventRecord(u);
        w && n.delayShowTerminals(u, w);
      }
      if (o) {
        n.onOverNewTargetWhileCreating(l.relatedTarget, !(r == null ? void 0 : r.readOnly) && !(r == null ? void 0 : r.isOccurence) ? r : null, l);
      }
    }
    onTerminalMouseOver(c) {
      this.clearTimeout('hide-' + c.target.closest(this.client.eventSelector).dataset.syncId);
      if (this.creationData) {
        this.onOverTargetEventBar(c);
      }
    }
    onTerminalMouseOut(c) {
      let e, f, g, i;
      const j = this, {
          creationData: k,
          client: l
        } = j, n = c.target.closest(l.eventSelector), o = (e = c.relatedTarget) == null ? void 0 : e.closest(l.eventSelector), p = (i = (f = o == null ? void 0 : o.elementData) == null ? void 0 : f.eventRecord) != null ? i : (g = o == null ? void 0 : o.elementData) == null ? void 0 : g.taskRecord;
      if (o !== n && n && !j.hasTimeout('show-' + n.dataset.syncId) && (!k || n !== k.timeSpanElement)) {
        j.delayHideTerminals(n);
        l.unhover(n, c);
      }
      if (k) {
        j.onOverNewTargetWhileCreating(c.relatedTarget, !(p == null ? void 0 : p.readOnly) && !(p == null ? void 0 : p.isOccurence) ? p : null, c);
      }
    }
    onTerminalPointerDown(c) {
      let e;
      const f = this;
      if (c.button === 0 && !f.creationData) {
        const {client: g} = f, i = g.timeAxisSubGridElement, j = c.target, k = j.closest(g.eventInnerSelector), l = Rectangle.from(g.element);
        c.stopPropagation();
        f.creationData = {
          'sourceElement': k,
          'source': g.resolveTimeSpanRecord(k).$original,
          'fromSide': j.dataset.side,
          'startPoint': Rectangle.from(j, i).center,
          'startX': c.pageX - l.x + g.scrollLeft,
          'startY': c.pageY - l.y + g.scrollTop,
          'valid': false,
          'sourceResource': (e = g.resolveResourceRecord) == null ? void 0 : e.call(g, c),
          'tooltip': f.creationTooltip
        };
        f.pointerUpMoveDetacher = EventHelper.on({
          'pointerup': {
            'element': DomHelper.getRootElement(g.element),
            'handler': 'onMouseUp',
            'passive': false
          },
          'pointermove': {
            'element': i,
            'handler': 'onMouseMove',
            'passive': false
          },
          'thisObj': f
        });
        f.documentPointerUpDetacher = EventHelper.on({
          'pointerup': {
            'element': document,
            'handler': 'onDocumentMouseUp'
          },
          'keydown': {
            'element': document,
            'handler': ({key: n}) => {
              if (n === 'Escape') {
                f.abort();
              }
            }
          },
          'thisObj': f
        });
      }
    }
    onElementTouchMove(c) {
      let e;
      (e = super.onElementTouchMove) == null ? void 0 : e.call(this, c);
      if (this.connector) {
        c.preventDefault();
      }
    }
    onMouseMove(c) {
      const e = this, {
          client: f,
          creationData: g
        } = e, i = Rectangle.from(f.element), j = c.pageX - i.x + f.scrollLeft - g.startX, k = c.pageY - i.y + f.scrollTop - g.startY, l = Math.round(Math.sqrt(j * j + k * k)) - 3, n = Math.atan2(k, j);
      let {connector: o} = e;
      if (!o) {
        if (e.onRequestDragCreate(c) === false) {
          return;
        }
        o = e.connector;
      }
      o.style.width = l + 'px';
      o.style.transform = 'rotate(' + n + 'rad)';
      e.lastMouseMoveEvent = c;
    }
    onRequestDragCreate(c) {
      const e = this, {
          client: f,
          creationData: g
        } = e;
      if (f.trigger('beforeDependencyCreateDrag', {
          'data': g,
          'source': g.source
        }) === false) {
        e.abort();
        return false;
      }
      f.element.classList.add('b-creating-dependency');
      e.createConnector(g.startPoint.x, g.startPoint.y);
      f.trigger('dependencyCreateDragStart', {
        'data': g,
        'source': g.source
      });
      if (e.showCreationTooltip) {
        const i = e.creationTooltip || (e.creationTooltip = e.createDragTooltip());
        e.creationData.tooltip = i;
        i.disabled = false;
        i.show();
        i.onMouseMove(c);
      }
      f.scrollManager.startMonitoring({
        'scrollables': [
          {
            'element': f.timeAxisSubGrid.scrollable.element,
            'direction': 'horizontal'
          },
          {
            'element': f.scrollable.element,
            'direction': 'vertical'
          }
        ],
        'callback': () => e.lastMouseMoveEvent && e.onMouseMove(e.lastMouseMoveEvent)
      });
    }
    onOverTargetEventBar(c) {
      const e = this, {
          client: f,
          creationData: g,
          allowDropOnEventBar: i
        } = e, {target: j} = c;
      let k = f.resolveTimeSpanRecord(j).$original;
      if (k == null ? void 0 : k.isEventSegment) {
        k = k.event;
      }
      if (Objects.isPromise(g.valid) || !i && !j.classList.contains(e.terminalCls)) {
        return;
      }
      if (k !== g.source) {
        e.onOverNewTargetWhileCreating(j, k, c);
      }
    }
    async onOverNewTargetWhileCreating(c, e, f) {
      let g, i;
      const j = this, {
          client: k,
          creationData: l,
          allowDropOnEventBar: n,
          connector: o
        } = j;
      if (Objects.isPromise(l.valid)) {
        return;
      }
      if (l.finalizing) {
        return;
      }
      if (!o) {
        return;
      }
      o.classList.remove('b-valid', 'b-invalid');
      l.timeSpanElement && DomHelper.removeClsGlobally(l.timeSpanElement, 'b-sch-terminal-active');
      if (!e || e === l.source || !n && !c.classList.contains(j.terminalCls)) {
        l.target = l.toSide = null;
        l.valid = false;
        o.classList.add('b-invalid');
      } else {
        const p = l.target = e, {source: q} = l;
        let r = c.dataset.side;
        if (n && !c.classList.contains(j.terminalCls)) {
          r = j.getTargetSideFromType(j.dependencyStore.modelClass.fieldMap.type.defaultValue || DependencyBaseModel.Type.EndToStart);
        }
        if (k.resolveResourceRecord) {
          l.targetResource = k.resolveResourceRecord(f);
        }
        let u;
        l.toSide = r;
        const v = l.fromSide, w = B => {
            if (!j.isDestroyed) {
              l.valid = B;
              c.classList.add(B ? 'b-valid' : 'b-invalid');
              o.classList.add(B ? 'b-valid' : 'b-invalid');
              k.trigger('dependencyValidationComplete', {
                'data': l,
                'source': q,
                'target': p,
                'dependencyType': u
              });
            }
          };
        switch (true) {
        case v === 'start' && r === 'start':
          u = DependencyBaseModel.Type.StartToStart;
          break;
        case v === 'start' && r === 'end':
          u = DependencyBaseModel.Type.StartToEnd;
          break;
        case v === 'end' && r === 'start':
          u = DependencyBaseModel.Type.EndToStart;
          break;
        case v === 'end' && r === 'end':
          u = DependencyBaseModel.Type.EndToEnd;
          break;
        }
        k.trigger('dependencyValidationStart', {
          'data': l,
          'source': q,
          'target': p,
          'dependencyType': u
        });
        let z = l.valid = j.dependencyStore.isValidDependency(q, p, u);
        if (Objects.isPromise(z)) {
          z = await z;
          w(z);
        } else {
          w(z);
        }
        const A = z ? 'b-valid' : 'b-invalid';
        o.classList.add(A);
        (i = (g = l.timeSpanElement) == null ? void 0 : g.querySelector('.' + j.terminalCls + '[data-side=' + r + ']')) == null ? void 0 : i.classList.add('b-sch-terminal-active', A);
      }
      j.updateCreationTooltip();
    }
    async onMouseUp() {
      let c;
      const e = this, f = e.creationData;
      f.finalizing = true;
      (c = e.pointerUpMoveDetacher) == null ? void 0 : c.call(e);
      if (f.valid) {
        const g = await e.client.trigger('beforeDependencyCreateFinalize', f);
        if (g === false) {
          f.valid = false;
        } else if (Objects.isPromise(f.valid)) {
          f.valid = await f.valid;
        }
        if (f.valid) {
          let i = await e.createDependency(f);
          if (i !== null) {
            if (Objects.isPromise(i)) {
              i = await i;
            }
            f.dependency = i;
            e.client.trigger('dependencyCreateDrop', {
              'data': f,
              'source': f.source,
              'target': f.target,
              'dependency': i
            });
            await e.doAfterDependencyDrop(f);
          }
        } else {
          await e.doAfterDependencyDrop(f);
        }
      } else {
        f.valid = false;
        await e.doAfterDependencyDrop(f);
      }
      e.abort();
    }
    doAfterDependencyDrop(c) {
      this.client.trigger('afterDependencyCreateDrop', {
        'data': c,
        ...c
      });
    }
    onDocumentMouseUp({target: c}) {
      if (!this.client.timeAxisSubGridElement.contains(c)) {
        this.abort();
      }
    }
    abort() {
      let c, e;
      const f = this, {
          client: g,
          creationData: i
        } = f;
      if (i) {
        const {
          source: j,
          sourceResource: k,
          target: l,
          targetResource: n
        } = i;
        if (j) {
          const o = g.getElementFromEventRecord(j, k);
          if (o) {
            f.hideTerminals(o);
          }
        }
        if (l) {
          const p = g.getElementFromEventRecord(l, n);
          if (p) {
            f.hideTerminals(p);
          }
        }
      }
      if (f.creationTooltip) {
        f.creationTooltip.disabled = true;
      }
      f.creationData = f.lastMouseMoveEvent = null;
      (c = f.pointerUpMoveDetacher) == null ? void 0 : c.call(f);
      (e = f.documentPointerUpDetacher) == null ? void 0 : e.call(f);
      f.removeConnector();
    }
    createConnector(c, e) {
      const f = this, {client: g} = f;
      f.clearTimeout(f.removeConnectorTimeout);
      f.connector = DomHelper.createElement({
        'parent': g.timeAxisSubGridElement,
        'className': f.baseCls + '-connector',
        'style': 'left:' + c + 'px;top:' + e + 'px'
      });
      g.element.classList.add('b-creating-dependency');
    }
    createDragTooltip() {
      const c = this, {client: e} = c;
      return c.creationTooltip = Tooltip.new({
        'id': e.id + '-dependency-drag-tip',
        'cls': 'b-sch-dependency-creation-tooltip',
        'loadingMsg': '',
        'anchorToTarget': false,
        'forElement': e.timeAxisSubGridElement,
        'trackMouse': true,
        'constrainTo': null,
        'header': { 'dock': 'right' },
        'internalListeners': {
          'beforeShow': 'updateCreationTooltip',
          'thisObj': c
        }
      }, c.creationTooltip);
    }
    removeConnector() {
      const c = this, {
          connector: e,
          client: f
        } = c;
      if (e) {
        e.classList.add('b-removing');
        e.style.width = '0';
        c.removeConnectorTimeout = c.setTimeout(() => {
          e.remove();
          c.connector = null;
        }, 200);
      }
      f.element.classList.remove('b-creating-dependency');
      c.creationTooltip && c.creationTooltip.hide();
      f.scrollManager.stopMonitoring();
    }
    delayShowTerminals(c, e) {
      const f = this, {syncId: g} = e.dataset;
      f.clearTimeout('hide-' + g);
      f.clearTimeout('show-' + g);
      e.classList.remove('b-hiding-terminals');
      if (!f.terminalShowDelay) {
        f.showTerminals(c, e);
      } else {
        f.setTimeout({
          'fn': () => f.showTerminals(c, e),
          'name': 'show-' + g,
          'args': [
            c,
            e
          ],
          'delay': f.terminalShowDelay
        });
      }
    }
    delayHideTerminals(c) {
      const e = this, {syncId: f} = c.dataset;
      e.clearTimeout('hide-' + f);
      e.clearTimeout('show-' + f);
      c.classList.add('b-hiding-terminals');
      if (!e.terminalHideDelay) {
        e.hideTerminals(c);
      } else {
        e.setTimeout({
          'fn': () => e.hideTerminals(c),
          'name': 'hide-' + f,
          'args': [c],
          'delay': e.terminalHideDelay
        });
      }
    }
    showTerminals(c, e = this.client.getElementFromEventRecord(c)) {
      const f = this;
      if (!f.isCreateAllowed || !c.project || !e) {
        return;
      }
      const {client: g} = f, i = f.terminalCls, j = i + 's-visible';
      if (f.showingTerminalsFor) {
        f.hideTerminals(f.showingTerminalsFor);
      }
      e = DomHelper.down(e, f.client.eventInnerSelector);
      if (!e.classList.contains(j) && !g.element.classList.contains('b-resizing-event') && !g.readOnly) {
        if (g.trigger('beforeShowTerminals', { 'source': c }) === false) {
          return;
        }
        DomHelper.createElement({
          'parent': e.closest(g.eventSelector),
          'className': 'b-sch-terminal-hover-area'
        });
        f.terminalSides.forEach(k => {
          k = f.fixSide(k);
          const l = DomHelper.createElement({
            'parent': e,
            'className': i + ' ' + i + '-' + k,
            'dataset': {
              'side': k,
              'feature': true
            }
          });
          l.detacher = EventHelper.on({
            'element': l,
            'mouseover': 'onTerminalMouseOver',
            'mouseout': 'onTerminalMouseOut',
            'pointerdown': {
              'handler': 'onTerminalPointerDown',
              'capture': true
            },
            'thisObj': f
          });
        });
        e.classList.add(j);
        c.internalCls.add(j);
      }
    }
    fixSide(c) {
      if (c === 'left') {
        return 'start';
      }
      if (c === 'right') {
        return 'end';
      }
      return c;
    }
    hideTerminals(c) {
      let e;
      const f = this, {client: g} = f, i = g.getTimeSpanMouseEventParams(c), j = i == null ? void 0 : i[f.eventName + 'Record'], k = f.terminalCls + 's-visible';
      (e = c.querySelector('.b-sch-terminal-hover-area')) == null ? void 0 : e.remove();
      DomHelper.forEachSelector(c, '> ' + g.eventInnerSelector + ' > .' + f.terminalCls, l => {
        let n;
        (n = l.detacher) == null ? void 0 : n.call(l);
        l.remove();
      });
      DomHelper.down(c, g.eventInnerSelector).classList.remove(k);
      j == null ? void 0 : j.internalCls.remove(k);
      c.classList.remove('b-hiding-terminals');
    }
    createDependency(c) {
      const {
          source: e,
          target: f,
          fromSide: g,
          toSide: i
        } = c, j = (g === 'start' ? 0 : 2) + (i === 'end' ? 1 : 0);
      const k = this.dependencyStore.add({
        'from': e.id,
        'to': f.id,
        'type': j,
        'fromSide': g,
        'toSide': i
      });
      return k !== null ? k[0] : null;
    }
    getTargetSideFromType(c) {
      if (c === DependencyBaseModel.Type.StartToStart || c === DependencyBaseModel.Type.EndToStart) {
        return 'start';
      }
      return 'end';
    }
    updateCreationTooltip() {
      const c = this;
      if (!c.showCreationTooltip) {
        return;
      }
      const e = c.creationData, {valid: f} = e, g = c.creationTooltip, {classList: i} = g.element;
      if (Objects.isPromise(f)) {
        i.remove('b-invalid');
        i.add('b-checking');
        return new Promise(j => f.then(k => {
          e.valid = k;
          if (!g.isDestroyed) {
            j(c.updateCreationTooltip());
          }
        }));
      }
      g.html = c.creationTooltipTemplate(e);
    }
    creationTooltipTemplate(c) {
      let e, f;
      const g = this, {
          tooltip: i,
          valid: j
        } = c, {classList: k} = i.element;
      Object.assign(c, {
        'fromText': StringHelper.encodeHtml(c.source.name),
        'toText': StringHelper.encodeHtml((f = (e = c.target) == null ? void 0 : e.name) != null ? f : ''),
        'fromSide': c.fromSide,
        'toSide': c.toSide || ''
      });
      let l, n;
      k.toggle('b-invalid', !j);
      k.remove('b-checking');
      if (j === true) {
        l = 'valid';
        n = g.L('L{Dependencies.valid}');
      } else {
        l = 'invalid';
        n = g.L('L{Dependencies.invalid}');
      }
      i.title = '<i class="b-icon b-icon-' + l + '"></i>' + n;
      return {
        'children': [{
            'className': 'b-sch-dependency-tooltip',
            'children': [
              {
                'dataset': { 'ref': 'fromLabel' },
                'tag': 'label',
                'text': g.L('L{Dependencies.from}')
              },
              {
                'dataset': { 'ref': 'fromText' },
                'text': c.fromText
              },
              {
                'dataset': { 'ref': 'fromBox' },
                'className': 'b-sch-box b-' + c.fromSide
              },
              {
                'dataset': { 'ref': 'toLabel' },
                'tag': 'label',
                'text': g.L('L{Dependencies.to}')
              },
              {
                'dataset': { 'ref': 'toText' },
                'text': c.toText
              },
              {
                'dataset': { 'ref': 'toBox' },
                'className': 'b-sch-box b-' + c.toSide
              }
            ]
          }]
      };
    }
    doDisable(c) {
      if (!this.isConfiguring) {
        this.updateCreateListeners();
      }
      super.doDisable(c);
    }
  }, __publicField(b, 'configurable', {
    'allowDropOnEventBar': true,
    'terminalSize': null,
    'terminalOffset': null,
    'terminalShowDelay': 0,
    'terminalHideDelay': 0
  }), b;
};
const ROWS_PER_CELL = 25;
const DependencyGridCache_default = a => {
  let b;
  return b = class extends a {
    gridCache = null;
    constructor() {
      super(...arguments);
    }
    getDependenciesToConsider(c, e, f, g) {
      const k = this, {gridCache: l} = k, {timeAxis: n} = k.client;
      if (l) {
        const o = new Set(), p = Math.floor((c - n.startMS) / k.MS_PER_CELL), q = Math.floor((e - n.startMS) / k.MS_PER_CELL), r = Math.floor(f / ROWS_PER_CELL), u = Math.floor(g / ROWS_PER_CELL);
        for (let v = p; v <= q; v++) {
          const w = l[v];
          if (w) {
            for (let z = r; z <= u; z++) {
              const A = w[z];
              if (A) {
                for (let B = 0; B < A.length; B++) {
                  o.add(A[B]);
                }
              }
            }
          }
        }
        return o;
      }
    }
    afterDrawDependency(c, e, f, g, k) {
      let l, n;
      const o = this;
      if (o.constructGridCache) {
        const {MS_PER_CELL: p} = o, {
            startMS: q,
            endMS: r
          } = o.client.timeAxis, u = Math.ceil((r - q) / p), v = Math.floor((g - q) / p), w = Math.floor((k - q) / p), z = Math.floor(e / ROWS_PER_CELL), A = Math.floor(f / ROWS_PER_CELL), B = Math.min(v, w), C = Math.max(v, w), E = Math.min(z, A), F = Math.max(z, A);
        if (B < 0 && C < 0 || B > u && C > u) {
          return;
        }
        const G = Math.max(B, 0), H = Math.min(C, u);
        for (let I = G; I <= H; I++) {
          const J = (l = o.gridCache[I]) != null ? l : o.gridCache[I] = {};
          for (let K = E; K <= F; K++) {
            const L = (n = J[K]) != null ? n : J[K] = [];
            L.push(c);
          }
        }
      }
    }
    beforeDraw() {
      const c = this;
      if (!c.gridCache) {
        const {visibleDateRange: e} = c.client;
        c.constructGridCache = true;
        c.MS_PER_CELL = Math.max(e.endMS - e.startMS, 1000);
        c.gridCache = {};
      }
    }
    afterDraw() {
      this.constructGridCache = false;
    }
    reset() {
      this.gridCache = null;
    }
  }, __publicField(b, '$name', 'DependencyGridCache'), b;
};
const THRESHOLD = Math.min(1 / globalThis.devicePixelRatio, 0.75);
const BOX_PROPERTIES = [
  'start',
  'end',
  'top',
  'bottom'
];
const equalEnough = (c, e) => Math.abs(c - e) < 0.1;
const sideToSide = {
  'l': 'left',
  'r': 'right',
  't': 'top',
  'b': 'bottom'
};
const RectangularPathFinder = class _RectangularPathFinder extends Base {
  static get configurable() {
    return {
      'startSide': 'right',
      'startArrowMargin': 12,
      'startShift': 0,
      'endSide': 'left',
      'endArrowMargin': 12,
      'endShift': 0,
      'verticalMargin': 2,
      'horizontalMargin': 5,
      'otherBoxes': null,
      'client': {}
    };
  }
  findPath(a, b) {
    const c = this, e = a;
    let f, g, i, j, k, l, n, o, p, q, r, u, v, w, z, A, B, C, E, F, G, H, I, J, K, L, N, O, P;
    b = ArrayHelper.asArray(b);
    for (P = 0; a && !O;) {
      f = Object.assign(c.config, a);
      g = f.startBox;
      i = f.endBox;
      j = f.startShift;
      k = f.endShift;
      l = f.startSide;
      n = f.endSide;
      o = f.startArrowMargin;
      p = f.endArrowMargin;
      q = f.horizontalMargin;
      r = f.verticalMargin;
      u = f.hasOwnProperty('startHorizontalMargin') ? f.startHorizontalMargin : q;
      v = f.hasOwnProperty('startVerticalMargin') ? f.startVerticalMargin : r;
      w = f.hasOwnProperty('endHorizontalMargin') ? f.endHorizontalMargin : q;
      z = f.hasOwnProperty('endVerticalMargin') ? f.endVerticalMargin : r;
      A = f.hasOwnProperty('otherHorizontalMargin') ? f.otherHorizontalMargin : q;
      B = f.hasOwnProperty('otherVerticalMargin') ? f.otherVerticalMargin : r;
      C = f.otherBoxes;
      l = c.normalizeSide(l);
      n = c.normalizeSide(n);
      E = c.getConnectionCoordinatesFromBoxSideShift(g, l, j);
      F = c.getConnectionCoordinatesFromBoxSideShift(i, n, k);
      K = c.calcGridBaseBoxFromBoxAndDrawParams(g, l, o, u, v);
      L = c.calcGridBaseBoxFromBoxAndDrawParams(i, n, p, w, z);
      BOX_PROPERTIES.forEach(Q => {
        if (Math.abs(K[Q] - L[Q]) <= THRESHOLD) {
          L[Q] = K[Q];
        }
      });
      if (c.shouldLookForPath(g, i, K, L)) {
        C = C == null ? void 0 : C.map(Q => c.calcGridBaseBoxFromBoxAndDrawParams(Q, false, 0, A, B));
        G = c.getConnectionCoordinatesFromBoxSideShift(K, l, j);
        H = c.getConnectionCoordinatesFromBoxSideShift(L, n, k);
        N = c.buildPathGrid(K, L, G, H, l, n, C);
        I = c.convertDecartPointToGridPoint(N, G);
        J = c.convertDecartPointToGridPoint(N, H);
        O = c.findPathOnGrid(N, I, J, l, n);
      }
      for (a = false; !O && !a && b && P < b.length; P++) {
        a = b[P](f, e);
      }
    }
    if (O) {
      O = c.prependPathWithArrowStaffSegment(O, E, l);
      O = c.appendPathWithArrowStaffSegment(O, F, n);
      O = c.optimizePath(O);
    }
    return O;
  }
  static calculateRelativePosition(a, b, c = false) {
    const e = c ? 'top' : 'start', f = c ? 'bottom' : 'end';
    let g;
    if (a[f] < b[e]) {
      g = 0;
    } else if (a[f] <= b[f] && a[f] >= b[e] && a[e] < b[e]) {
      g = 1;
    } else if (a[e] >= b[e] && a[f] <= b[f]) {
      g = 2;
    } else if (a[e] < b[e] && a[f] > b[f]) {
      g = -2;
    } else if (a[e] <= b[f] && a[f] > b[f]) {
      g = 3;
    } else {
      g = 4;
    }
    return g;
  }
  static boxOverlapChanged(a, b, c, e, f = false) {
    const g = _RectangularPathFinder.calculateRelativePosition, i = g(a, b, f), j = g(c, e, f);
    return i !== j;
  }
  shouldLookForPath(a, b, c, e) {
    let f = true;
    if ((a.end - a.start <= this.startArrowMargin || b.end - b.start <= this.endArrowMargin) && Math.abs(_RectangularPathFinder.calculateRelativePosition(a, b, true)) === 2) {
      f = !_RectangularPathFinder.boxOverlapChanged(a, b, c, e);
    }
    return f;
  }
  getConnectionCoordinatesFromBoxSideShift(a, b, c) {
    let e;
    switch (b) {
    case 'left':
      e = {
        'x': a.start,
        'y': (a.top + a.bottom) / 2 + c
      };
      break;
    case 'right':
      e = {
        'x': a.end,
        'y': (a.top + a.bottom) / 2 + c
      };
      break;
    case 'top':
      e = {
        'x': (a.start + a.end) / 2 + c,
        'y': a.top
      };
      break;
    case 'bottom':
      e = {
        'x': (a.start + a.end) / 2 + c,
        'y': a.bottom
      };
      break;
    }
    return e;
  }
  calcGridBaseBoxFromBoxAndDrawParams(a, b, c, e, f) {
    let g;
    switch (this.normalizeSide(b)) {
    case 'left':
      g = {
        'start': a.start - Math.max(c, e),
        'end': a.end + e,
        'top': a.top - f,
        'bottom': a.bottom + f
      };
      break;
    case 'right':
      g = {
        'start': a.start - e,
        'end': a.end + Math.max(c, e),
        'top': a.top - f,
        'bottom': a.bottom + f
      };
      break;
    case 'top':
      g = {
        'start': a.start - e,
        'end': a.end + e,
        'top': a.top - Math.max(c, f),
        'bottom': a.bottom + f
      };
      break;
    case 'bottom':
      g = {
        'start': a.start - e,
        'end': a.end + e,
        'top': a.top - f,
        'bottom': a.bottom + Math.max(c, f)
      };
      break;
    default:
      g = {
        'start': a.start - e,
        'end': a.end + e,
        'top': a.top - f,
        'bottom': a.bottom + f
      };
    }
    return g;
  }
  normalizeSide(a) {
    const {rtl: b} = this.client;
    c => sideToSide[c] || c;
    if (a === 'start') {
      return b ? 'right' : 'left';
    }
    if (a === 'end') {
      return b ? 'left' : 'right';
    }
    return a;
  }
  buildPathGrid(a, b, c, e, f, g, i) {
    let j, k, l, n, o, p, q, r, u, v, w, z, A;
    const B = {}, C = [];
    j = [
      a.start,
      f === 'left' || f === 'right' ? (a.start + a.end) / 2 : c.x,
      a.end,
      b.start,
      g === 'left' || g === 'right' ? (b.start + b.end) / 2 : e.x,
      b.end
    ];
    k = [
      a.top,
      f === 'top' || f === 'bottom' ? (a.top + a.bottom) / 2 : c.y,
      a.bottom,
      b.top,
      g === 'top' || g === 'bottom' ? (b.top + b.bottom) / 2 : e.y,
      b.bottom
    ];
    if (i) {
      i.forEach(E => {
        j.push(E.start, (E.start + E.end) / 2, E.end);
        k.push(E.top, (E.top + E.bottom) / 2, E.bottom);
      });
    }
    j = [...new Set(j.sort((E, F) => E - F))];
    k = [...new Set(k.sort((E, F) => E - F))];
    for (p = 0, r = k.length; p < r; ++p) {
      B[p] = B[p] || {};
      l = k[p];
      for (o = 0, q = j.length; o < q; ++o) {
        n = j[o];
        z = (n <= a.start || n >= a.end || l <= a.top || l >= a.bottom) && (n <= b.start || n >= b.end || l <= b.top || l >= b.bottom);
        if (i) {
          for (u = 0, v = i.length; z && u < v; ++u) {
            w = i[u];
            z = n <= w.start || n >= w.end || l <= w.top || l >= w.bottom || n === c.x && l === c.y || n === e.x && l === e.y;
          }
        }
        A = {
          'distance': Number.MAX_SAFE_INTEGER,
          'permitted': z,
          'x': n,
          'y': l,
          'ix': o,
          'iy': p
        };
        B[p][o] = A;
        C.push(A);
      }
    }
    return {
      'width': j.length,
      'height': k.length,
      'xs': j,
      'ys': k,
      'points': B,
      'linearPoints': C
    };
  }
  convertDecartPointToGridPoint(a, b) {
    const c = a.xs.indexOf(b.x), e = a.ys.indexOf(b.y);
    return a.points[e][c];
  }
  findPathOnGrid(a, b, c, e, f) {
    const g = this;
    let i = false;
    if (b.permitted && c.permitted) {
      a = g.waveForward(a, b, 0);
      i = g.collectPath(a, c, f);
    }
    return i;
  }
  getGridPointNeighbors(a, b, c) {
    const e = b.ix, f = b.iy, g = [];
    let i;
    if (f < a.height - 1) {
      i = a.points[f + 1][e];
      (!c || c(i)) && g.push(i);
    }
    if (f > 0) {
      i = a.points[f - 1][e];
      (!c || c(i)) && g.push(i);
    }
    if (e < a.width - 1) {
      i = a.points[f][e + 1];
      (!c || c(i)) && g.push(i);
    }
    if (e > 0) {
      i = a.points[f][e - 1];
      (!c || c(i)) && g.push(i);
    }
    return g;
  }
  waveForward(a, b, c) {
    const e = this;
    WalkHelper.preWalkUnordered([
      b,
      c
    ], ([f, g]) => e.getGridPointNeighbors(a, f, i => i.permitted && i.distance > g + 1).map(i => [
      i,
      g + 1
    ]), ([f, g]) => f.distance = g);
    return a;
  }
  collectPath(a, b, c) {
    const e = this, f = [];
    let g = true, i, j, k, l;
    while (g && b.distance) {
      i = e.getGridPointNeighbors(a, b, n => n.permitted && n.distance === b.distance - 1);
      g = i.length > 0;
      if (g) {
        i = i.sort((n, o) => {
          let p, q;
          p = n.ix - b.ix;
          q = n.iy - b.iy;
          const r = (c === 'left' || c === 'right') && q === 0 || (c === 'top' || c === 'bottom') && p === 0 ? -1 : 1;
          p = o.ix - b.ix;
          q = o.iy - b.iy;
          const u = (c === 'left' || c === 'right') && q === 0 || (c === 'top' || c === 'bottom') && p === 0 ? -1 : 1;
          if (r > u)
            return 1;
          if (r < u)
            return -1;
          if (r === u)
            return n.y > o.y ? -1 : n.y < o.y ? 1 : 0;
        });
        j = i[0];
        f.push({
          'x1': j.x,
          'y1': j.y,
          'x2': b.x,
          'y2': b.y
        });
        k = j.ix - b.ix;
        l = j.iy - b.iy;
        switch (true) {
        case !l && k > 0:
          c = 'left';
          break;
        case !l && k < 0:
          c = 'right';
          break;
        case !k && l > 0:
          c = 'top';
          break;
        case !k && l < 0:
          c = 'bottom';
          break;
        }
        b = j;
      }
    }
    return g && f.reverse() || false;
  }
  prependPathWithArrowStaffSegment(a, b, c) {
    if (a.length > 0) {
      const e = a[0], f = {
          'x2': e.x1,
          'y2': e.y1
        };
      switch (c) {
      case 'left':
        f.x1 = b.x;
        f.y1 = e.y1;
        break;
      case 'right':
        f.x1 = b.x;
        f.y1 = e.y1;
        break;
      case 'top':
        f.x1 = e.x1;
        f.y1 = b.y;
        break;
      case 'bottom':
        f.x1 = e.x1;
        f.y1 = b.y;
        break;
      }
      a.unshift(f);
    }
    return a;
  }
  appendPathWithArrowStaffSegment(a, b, c) {
    if (a.length > 0) {
      const e = a[a.length - 1], f = {
          'x1': e.x2,
          'y1': e.y2
        };
      switch (c) {
      case 'left':
        f.x2 = b.x;
        f.y2 = e.y2;
        break;
      case 'right':
        f.x2 = b.x;
        f.y2 = e.y2;
        break;
      case 'top':
        f.x2 = e.x2;
        f.y2 = b.y;
        break;
      case 'bottom':
        f.x2 = e.x2;
        f.y2 = b.y;
        break;
      }
      a.push(f);
    }
    return a;
  }
  optimizePath(a) {
    const b = [];
    let c, e;
    if (a.length > 0) {
      c = a.shift();
      b.push(c);
      while (a.length > 0) {
        e = a.shift();
        if (equalEnough(c.x1, e.x1) && equalEnough(c.y1, e.y1) && equalEnough(c.x2, e.x2) && equalEnough(c.y2, e.y2)) {
          c = e;
        } else if (equalEnough(c.y1, c.y2) && equalEnough(e.y1, e.y2)) {
          c.x2 = e.x2;
        } else if (equalEnough(c.x1, c.x2) && equalEnough(e.x1, e.x2)) {
          c.y2 = e.y2;
        } else {
          b.push(e);
          c = e;
        }
      }
    }
    return b;
  }
};
RectangularPathFinder._$name = 'RectangularPathFinder';
function drawingDirection(a) {
  if (a.x1 === a.x2) {
    return a.y2 > a.y1 ? 'd' : 'u';
  }
  return a.x2 > a.x1 ? 'r' : 'l';
}
function segmentLength(a) {
  return a.x1 === a.x2 ? a.y2 - a.y1 : a.x2 - a.x1;
}
function arc(a, b, c) {
  const e = drawingDirection(a) + drawingDirection(b), f = c * (e.includes('l') ? -1 : 1), g = c * (e.includes('u') ? -1 : 1), i = e === 'ur' || e === 'lu' || e === 'dl' || e === 'rd' ? 1 : 0;
  return 'a' + f + ',' + g + ' 0 0 ' + i + ' ' + f + ',' + g;
}
function line(a, b, c, e, f) {
  let g = a.x1 === a.x2 ? 'v' : 'h', i = e;
  if (e) {
    const j = segmentLength(a), k = b ? Math.abs(segmentLength(b)) : Number.MAX_SAFE_INTEGER, l = Math.sign(j);
    if (f == null) {
      f = e;
    }
    if (Math.abs(j) < e * 2 || k < e * 2) {
      i = Math.min(Math.abs(j), k) / 2;
    }
    const n = c === 'single' ? 0 : c === 'first' ? i : c === 'between' ? f + i : f, o = j - n * l;
    g += Math.sign(o) !== l ? 0 : o;
    if (c !== 'last' && c !== 'single' && i > 0) {
      g += ' ' + arc(a, b, i);
    }
  } else {
    g += segmentLength(a);
  }
  return {
    'line': g,
    'currentRadius': e !== i ? i : null
  };
}
function pathMapper(a, b) {
  const {length: c} = b;
  if (!c) {
    return '';
  }
  let e = null;
  return 'M' + b[0].x1 + ',' + b[0].y1 + ' ' + b.map((f, g) => {
    const j = c === 1 ? 'single' : g === c - 1 ? 'last' : g === 0 ? 'first' : 'between', k = line(f, b[g + 1], j, a, e);
    ({currentRadius: e} = k);
    return k.line;
  }).join(' ');
}
const DependencyLineGenerator_default = a => {
  let b;
  return b = class extends a {
    lineCache = {};
    constructor() {
      super(...arguments);
    }
    onSVGReady() {
      const c = this;
      c.pathFinder = new RectangularPathFinder({
        ...c.pathFinderConfig,
        'client': c.client
      });
      c.lineDefAdjusters = c.createLineDefAdjusters();
      c.createMarker();
    }
    changeRadius(c) {
      if (c !== null) {
        ObjectHelper.assertNumber(c, 'radius');
      }
      return c;
    }
    resetAtRuntime() {
      if (!this.isConfiguring) {
        this.reset();
      }
    }
    updateRadius() {
      this.resetAtRuntime();
    }
    updateRenderer() {
      this.resetAtRuntime();
    }
    changeClickWidth(c) {
      if (c !== null) {
        ObjectHelper.assertNumber(c, 'clickWidth');
      }
      return c;
    }
    updateClickWidth() {
      this.resetAtRuntime();
    }
    updateDrawAroundParents() {
      this.resetAtRuntime();
    }
    createMarker() {
      let c, e;
      const f = this, {markerDef: g} = f, i = this.client.svgCanvas, j = g ? f.client.id + '-arrowEnd' : 'arrowEnd';
      (c = f.marker) == null ? void 0 : c.remove();
      i.style.setProperty('--scheduler-dependency-marker', 'url(#' + j + ')');
      f.marker = DomHelper.createElement({
        'parent': i,
        'id': j,
        'tag': 'marker',
        'className': 'b-sch-dependency-arrow',
        'ns': 'http://www.w3.org/2000/svg',
        'markerHeight': 11,
        'markerWidth': 11,
        'refX': 8.5,
        'refY': 3,
        'viewBox': '0 0 9 6',
        'orient': 'auto-start-reverse',
        'markerUnits': 'userSpaceOnUse',
        'retainElement': true,
        'children': [{
            'tag': 'path',
            'ns': 'http://www.w3.org/2000/svg',
            'd': (e = f.markerDef) != null ? e : 'M3,0 L3,6 L9,3 z'
          }]
      });
    }
    updateMarkerDef() {
      if (!this.isConfiguring) {
        this.createMarker();
        if (BrowserHelper.isSafari) {
          this.client.svgCanvas.innerHTML += '';
        }
      }
    }
    getAssignmentElement(c) {
      let e, f;
      const g = (f = (e = this.client.features.eventDrag) == null ? void 0 : e.getProxyElement) == null ? void 0 : f.call(e, c);
      return g || this.client.getElementFromAssignmentRecord(c);
    }
    getDomConfigs(c, e, f, g) {
      let i, j;
      const k = this, l = k.getDependencyKey(c, e, f), n = k.lineCache[l];
      if (k.constructLineCache || !n || g || k.drawingLive && (k.getAssignmentElement(e) || k.getAssignmentElement(f))) {
        const o = k.prepareLineDef(c, e, f, g), p = o && k.pathFinder.findPath(o, k.lineDefAdjusters), {
            client: q,
            clickWidth: r
          } = k, {toEvent: u} = c;
        if (p) {
          const v = k.highlighted.get(c), w = {
              'tag': 'path',
              'ns': 'http://www.w3.org/2000/svg',
              'd': pathMapper((i = k.radius) != null ? i : 0, p),
              'role': 'presentation',
              'dataset': {
                'syncId': l,
                'depId': c.id,
                'fromId': e.id,
                'toId': f.id
              },
              'elementData': {
                'dependency': c,
                'points': p
              },
              'class': {
                [k.baseCls]: 1,
                [c.cls]: c.cls,
                [c.highlighted]: c.highlighted,
                [v && [...v].join(' ')]: v,
                [k.noMarkerCls]: o.hideMarker,
                'b-inactive': c.active === false,
                'b-sch-bidirectional-line': c.bidirectional,
                'b-readonly': c.readOnly,
                'b-sch-dependency-ends-outside': !u.milestone && (u.endDate <= q.startDate || q.endDate <= u.startDate) || u.milestone && (u.endDate < q.startDate || q.endDate < u.startDate)
              }
            };
          (j = k.renderer) == null ? void 0 : j.call(k, {
            'domConfig': w,
            'points': p,
            'dependencyRecord': c,
            'fromAssignmentRecord': e,
            'toAssignmentRecord': f,
            'fromBox': o.startBox,
            'toBox': o.endBox,
            'fromSide': o.startSide,
            'toSide': o.endSide
          });
          const z = [w];
          if (r > 1) {
            z.push({
              ...w,
              'class': {
                ...w.class,
                'b-click-area': 1
              },
              'dataset': {
                ...w.dataset,
                'syncId': w.dataset.syncId + '-click-area'
              },
              'style': { 'strokeWidth': r }
            });
          }
          return k.lineCache[l] = z;
        }
        return k.lineCache[l] = null;
      }
      return n;
    }
    generateBoundaryBoxes(c, e) {
      if (e === 'top') {
        return [];
      }
      if (e === 'bottom') {
        return [
          {
            'start': c.left,
            'end': c.left + c.width / 2,
            'top': c.rowTop,
            'bottom': c.rowBottom
          },
          {
            'start': c.left + c.width / 2,
            'end': c.right,
            'top': c.rowTop,
            'bottom': c.rowBottom
          }
        ];
      } else {
        return [{
            'start': c.left - this.pathFinder.startArrowMargin,
            'end': c.right + this.pathFinder.startArrowMargin,
            'top': c.rowTop,
            'bottom': c.rowBottom
          }];
      }
    }
    getAssignmentBounds(c) {
      const {client: e} = this, f = this.getAssignmentElement(c);
      if (f && !e.isExporting) {
        const g = Rectangle.from(f, this.relativeTo);
        g.fromElement = true;
        if (e.isHorizontal) {
          let i = e.getRowById(c.resource.id);
          if (i) {
            if (g.y < i.top || g.bottom > i.bottom) {
              const j = e.rowManager.getRowAt(g.center.y, true);
              if (j) {
                i = j;
              }
            }
            g.rowTop = i.top;
            g.rowBottom = i.bottom;
          } else {
            return e.getAssignmentEventBox(c, true);
          }
        }
        return g;
      }
      return e.isEngineReady && e.getAssignmentEventBox(c, true);
    }
    getConnectorStartSide(c) {
      return this.client.currentOrientation.getConnectorStartSide(c);
    }
    getConnectorEndSide(c) {
      return this.client.currentOrientation.getConnectorEndSide(c);
    }
    getDependencyStartSide(c) {
      const {
        fromEvent: e,
        type: f,
        fromSide: g
      } = c;
      if (g) {
        return g;
      }
      switch (true) {
      case f === DependencyModel.Type.StartToEnd:
      case f === DependencyModel.Type.StartToStart:
        return this.getConnectorStartSide(e);
      case f === DependencyModel.Type.EndToStart:
      case f === DependencyModel.Type.EndToEnd:
        return this.getConnectorEndSide(e);
      default:
        return this.getConnectorEndSide(e);
      }
    }
    getDependencyEndSide(c) {
      const {
        toEvent: e,
        type: f,
        toSide: g
      } = c;
      if (g) {
        return g;
      }
      switch (true) {
      case f === DependencyModel.Type.EndToEnd:
      case f === DependencyModel.Type.StartToEnd:
        return this.getConnectorEndSide(e);
      case f === DependencyModel.Type.EndToStart:
      case f === DependencyModel.Type.StartToStart:
        return this.getConnectorStartSide(e);
      default:
        return this.getConnectorStartSide(e);
      }
    }
    createLineDefAdjusters() {
      let c;
      const {client: e} = this;
      function f(k) {
        const {barMargin: l} = e;
        if (k.startArrowMargin > l || k.endArrowMargin > l) {
          k.startArrowMargin = k.endArrowMargin = l;
          return k;
        }
        return false;
      }
      function g(k) {
        if (k.startArrowMargin > 0 || k.endArrowMargin > 0) {
          k.startArrowMargin = k.endArrowMargin = 0;
          return k;
        }
        return false;
      }
      function i(k, l) {
        if (k.horizontalMargin > 2) {
          k.horizontalMargin = 1;
          l.hideMarker = true;
          return k;
        }
        return false;
      }
      const j = [
        f,
        g,
        i
      ];
      if ((c = e.features.nestedEvents) == null ? void 0 : c.enabled) {
        j.unshift(k => {
          if (k.otherBoxes.length) {
            k.otherBoxes.length = k.otherBoxes.nestedStart;
          }
          return k;
        });
      }
      return j;
    }
    adjustLineDef(c, e) {
      return e;
    }
    prepareLineDef(c, e, f, g) {
      let i, j, k, l;
      const n = this, {client: o} = n, p = n.getDependencyStartSide(c), q = n.getDependencyEndSide(c), r = (i = g == null ? void 0 : g.from) != null ? i : n.getAssignmentBounds(e), u = (j = g == null ? void 0 : g.to) != null ? j : n.getAssignmentBounds(f), v = [];
      if (!r || !u) {
        return null;
      }
      let {
        startArrowMargin: w,
        verticalMargin: z
      } = n.pathFinder;
      if (o.isHorizontal) {
        if (o.rtl) {
          const {totalSize: A} = o.timeAxisViewModel;
          if (r && !r.fromElement) {
            r._x = A - r.x - r.width;
          }
          if (u && !u.fromElement) {
            u._x = A - u.x - u.width;
          }
        }
        if (r.rowTop != null && r.rowTop !== u.rowTop) {
          v.push(...n.generateBoundaryBoxes(r, p));
        }
        if (((k = o.features.nestedEvents) == null ? void 0 : k.enabled) && n.drawAroundParents) {
          const {resourceStore: B} = o, C = e.resource, E = f.resource, F = B.indexOf(C), G = B.indexOf(E), H = Math.min(F, G), I = Math.max(F, G);
          v.nestedStart = v.length;
          for (const J of o.assignmentStore) {
            if (J !== e && J !== f && ((l = J.event) == null ? void 0 : l.isParent) && e.event.parent !== J.event && f.event.parent !== J.event) {
              const K = B.indexOf(J.resource);
              if (K >= H && K <= I) {
                const L = n.getAssignmentBounds(J).inflate(w);
                L.isNestedParent = true;
                v.push(L);
              }
            }
          }
        }
        if (!c.bidirectional) {
          if (/(top|bottom)/.test(p)) {
            w = o.barMargin / 2;
          }
          z = o.barMargin / 2;
        }
      }
      return n.adjustLineDef(c, {
        'startBox': r,
        'endBox': u,
        'otherBoxes': v,
        'startArrowMargin': w,
        'verticalMargin': z,
        'otherVerticalMargin': 0,
        'otherHorizontalMargin': 0,
        'startSide': p,
        'endSide': q
      });
    }
    beforeDraw() {
      super.beforeDraw();
      if (!Object.keys(this.lineCache).length) {
        this.constructLineCache = true;
      }
    }
    afterDraw() {
      super.afterDraw();
      this.constructLineCache = false;
    }
    reset() {
      super.reset();
      this.lineCache = {};
    }
  }, __publicField(b, '$name', 'DependencyLineGenerator'), b;
};
const fromBoxSide = [
  'start',
  'start',
  'end',
  'end'
];
const toBoxSide = [
  'start',
  'end',
  'start',
  'end'
];
const DependencyTooltip_default = a => {
  let b;
  return b = class extends a {
    changeTooltip(c, e) {
      const f = this;
      e == null ? void 0 : e.destroy();
      if (!c) {
        return null;
      }
      const g = Tooltip.new({
        'align': 'b-t',
        'id': f.client.id + '-dependency-tip',
        'forSelector': '.b-timelinebase:not(.b-eventeditor-editing,.b-taskeditor-editing,.b-resizing-event,.b-dragcreating,.b-dragging-event,.b-creating-dependency) .' + f.baseCls,
        'forElement': f.client.timeAxisSubGridElement,
        'showOnHover': true,
        'hoverDelay': 0,
        'hideDelay': 0,
        'anchorToTarget': false,
        'textContent': false,
        'trackMouse': false,
        'getHtml': f.getHoverTipHtml.bind(f)
      }, c);
      g.element.classList.add('b-sch-dependency-tooltip');
      return g;
    }
    updateShowTooltip(c) {
      const e = this;
      if (e.isConfiguring && !c) {
        return;
      }
      if (c && !e._tooltip) {
        e.tooltip;
      }
      if (e._tooltip) {
        e._tooltip.disabled = !c;
      }
    }
    getHoverTipHtml({activeTarget: c}) {
      return this.tooltipTemplate(this.resolveDependencyRecord(c));
    }
  }, __publicField(b, '$name', 'DependencyTooltip'), __publicField(b, 'configurable', {
    'showTooltip': true,
    'showLagInTooltip': false,
    'tooltipTemplate'(c) {
      const e = this;
      return [
        {
          'tag': 'label',
          'text': e.L('L{Dependencies.from}')
        },
        { 'text': c.fromEvent.name },
        { 'className': 'b-sch-box b-' + (c.fromSide || fromBoxSide[c.type]) },
        {
          'tag': 'label',
          'text': e.L('L{Dependencies.to}')
        },
        { 'text': c.toEvent.name },
        { 'className': 'b-sch-box b-' + (c.toSide || toBoxSide[c.type]) },
        e.showLagInTooltip && {
          'tag': 'label',
          'text': e.L('L{DependencyEdit.Lag}')
        },
        e.showLagInTooltip && { 'text': (c.lag || 0) + ' ' + DateHelper.getLocalizedNameOfUnit(c.lagUnit, c.lag !== 1) }
      ];
    },
    'tooltip': {
      '$config': [
        'nullify',
        'lazy'
      ],
      'value': {}
    }
  }), b;
};
const eventNameMap2 = {
  'click': 'Click',
  'dblclick': 'DblClick',
  'contextmenu': 'ContextMenu'
};
const collectLinkedAssignments = a => {
  let b;
  const c = [a];
  if ((b = a.resource) == null ? void 0 : b.hasLinks) {
    c.push(...a.resource.$links.map(e => ({
      'id': e.id + '_' + a.id,
      'resource': e,
      'event': a.event,
      'drawDependencies': a.drawDependencies
    })));
  }
  return c;
};
const Dependencies = class extends InstancePlugin.mixin(AttachToProjectMixin_default, Delayable_default, DependencyCreation_default, DependencyGridCache_default, DependencyLineGenerator_default, DependencyTooltip_default) {
  static delayable = {
    'doRefresh': 10,
    'rafRefresh': 'raf'
  };
  static configurable = {
    'overCls': 'b-sch-dependency-over',
    'baseCls': 'b-sch-dependency',
    'noMarkerCls': 'b-sch-dependency-markerless',
    'markerDef': null,
    'radius': null,
    'renderer': null,
    'highlightDependenciesOnEventHover': null,
    'drawOnScroll': true,
    'clickWidth': null,
    'immediateRefresh': null,
    'drawAroundParents': null
  };
  static $name = 'Dependencies';
  generation = 0;
  relativeTo = null;
  visibleDateRange = null;
  usingLinks = null;
  visibleResources = null;
  highlighted = new Map();
  lastScrollX = null;
  drawingLive = false;
  domConfigs = new Map();
  constructor() {
    super(...arguments);
  }
  static get pluginConfig() {
    return {
      'chain': [
        'render',
        'onInternalPaint',
        'onElementClick',
        'onElementDblClick',
        'onElementContextMenu',
        'onElementMouseOver',
        'onElementMouseOut',
        'bindStore'
      ],
      'assign': [
        'getElementForDependency',
        'getElementsForDependency',
        'resolveDependencyRecord'
      ]
    };
  }
  construct(a, b) {
    super.construct(a, b);
    const {scheduledEventName: c} = a;
    a.ion({
      'svgCanvasCreated': 'onSVGReady',
      'animationStart': 'refresh',
      [c + 'DragStart']: 'refresh',
      [c + 'DragAbort']: 'refresh',
      [c + 'ResizeStart']: 'refresh',
      [c + 'SegmentDragStart']: 'refresh',
      [c + 'SegmentResizeStart']: 'refresh',
      'timelineViewportResize': 'immediateReset',
      'timeAxisViewModelUpdate': 'reset',
      'toggleNode': 'reset',
      'thisObj': this
    });
    a.rowManager.ion({
      'refresh': 'reset',
      'changeTotalHeight': 'reset',
      'thisObj': this
    });
    this.bindStore(a.store);
  }
  doDisable(a) {
    if (!this.isConfiguring) {
      this._isDisabling = a;
      this.draw();
      this._isDisabling = false;
    }
    super.doDisable(a);
  }
  get rowStore() {
    return this.client.isVertical ? this.client.resourceStore : this.client.store;
  }
  bindStore(a) {
    const b = this;
    if (!b.client.isVertical) {
      b.detachListeners('store');
      if (b.client.usesDisplayStore) {
        a == null ? void 0 : a.ion({
          'name': 'store',
          'refresh': 'onStoreRefresh',
          'thisObj': b
        });
      }
      b.immediateReset();
    }
  }
  onStoreRefresh() {
    this.reset();
  }
  attachToProject(a) {
    super.attachToProject(a);
    a == null ? void 0 : a.ion({
      'name': 'project',
      'commitFinalized': 'reset',
      'thisObj': this
    });
  }
  attachToResourceStore(a) {
    super.attachToResourceStore(a);
    a == null ? void 0 : a.ion({
      'name': 'resourceStore',
      'change': 'onResourceStoreChange',
      'refresh': 'onResourceStoreChange',
      'thisObj': this
    });
  }
  onResourceStoreChange() {
    this.usingLinks = null;
    this.reset();
  }
  attachToEventStore(a) {
    super.attachToEventStore(a);
    a == null ? void 0 : a.ion({
      'name': 'eventStore',
      'refresh': 'reset',
      'thisObj': this
    });
  }
  attachToAssignmentStore(a) {
    super.attachToAssignmentStore(a);
    a == null ? void 0 : a.ion({
      'name': 'assignmentStore',
      'refresh': 'reset',
      'thisObj': this
    });
  }
  attachToDependencyStore(a) {
    super.attachToDependencyStore(a);
    a == null ? void 0 : a.ion({
      'name': 'dependencyStore',
      'change': 'reset',
      'refresh': 'onDependencyStoreRefresh',
      'thisObj': this
    });
  }
  onDependencyStoreRefresh({action: a}) {
    if (a === 'dataset') {
      this.generation++;
    }
    this.reset();
  }
  updateDrawOnScroll(a) {
    const b = this;
    b.detachListeners('scroll');
    if (a) {
      b.client.ion({
        'name': 'scroll',
        'scroll': 'doRefresh',
        'horizontalScroll': 'onHorizontalScroll',
        'prio': -100,
        'thisObj': b
      });
    } else {
      b.client.scrollable.ion({
        'name': 'scroll',
        'scrollEnd': 'draw',
        'thisObj': b
      });
      b.client.timeAxisSubGrid.scrollable.ion({
        'name': 'scroll',
        'scrollEnd': 'draw',
        'thisObj': b
      });
    }
  }
  onHorizontalScroll({
    subGrid: a,
    scrollX: b
  }) {
    if (b !== this.lastScrollX && a === this.client.timeAxisSubGrid) {
      this.lastScrollX = b;
      this.draw();
    }
  }
  onInternalPaint() {
    this.refresh();
  }
  static getLocalizedDependencyType(a) {
    return a ? this.L('L{DependencyType.' + a + '}') : '';
  }
  getElementForDependency(a, b, c) {
    return this.getElementsForDependency(a, b, c)[0];
  }
  getElementsForDependency(a, b, c) {
    let e = '[data-dep-id="' + a.id + '"]';
    if (b) {
      e += '[data-from-id="' + b.id + '"]';
    }
    if (c) {
      e += '[data-to-id="' + c.id + '"]';
    }
    return Array.from(this.client.svgCanvas.querySelectorAll(e));
  }
  resolveDependencyRecord(a) {
    let b;
    return (b = a.elementData) == null ? void 0 : b.dependency;
  }
  isDependencyElement(a) {
    return a.matches('.' + this.baseCls);
  }
  onElementClick(a) {
    const b = this.resolveDependencyRecord(a.target);
    if (b) {
      const c = eventNameMap2[a.type];
      this.client.trigger('dependency' + c, {
        'dependency': b,
        'event': a
      });
    }
  }
  onElementDblClick(a) {
    return this.onElementClick(a);
  }
  onElementContextMenu(a) {
    return this.onElementClick(a);
  }
  onElementMouseOver(a) {
    const b = this, c = b.resolveDependencyRecord(a.target);
    if (c) {
      b.client.trigger('dependencyMouseOver', {
        'dependency': c,
        'event': a
      });
      if (b.overCls) {
        b.highlight(c);
      }
    }
  }
  onElementMouseOut(a) {
    const b = this, c = b.resolveDependencyRecord(a.target);
    if (c) {
      b.client.trigger('dependencyMouseOut', {
        'dependency': c,
        'event': a
      });
      if (b.overCls) {
        b.unhighlight(c);
      }
    }
  }
  isDependencyVisible(a) {
    const b = this, {rowStore: c} = b, {
        fromEvent: e,
        toEvent: f
      } = a;
    if (!e || !f) {
      return false;
    }
    const g = e.resource, i = f.resource;
    if (!c.isAvailable(g) || !c.isAvailable(i)) {
      return false;
    }
    return e.isModel && !g.instanceMeta(c).hidden && !i.instanceMeta(c).hidden;
  }
  updateHighlightDependenciesOnEventHover(a) {
    const b = this;
    if (a) {
      const {client: c} = b;
      c.ion({
        'name': 'highlightOnHover',
        [c.scheduledEventName + 'MouseEnter']: e => b.highlightEventDependencies(e.eventRecord || e.taskRecord),
        [c.scheduledEventName + 'MouseLeave']: e => b.unhighlightEventDependencies(e.eventRecord || e.taskRecord),
        'thisObj': b
      });
    } else {
      b.detachListeners('highlightOnHover');
    }
  }
  highlight(a, b = this.overCls) {
    let c = this.highlighted.get(a);
    if (!c) {
      this.highlighted.set(a, c = new Set());
    }
    c.add(b);
    for (const e of this.getElementsForDependency(a)) {
      e.classList.add(b);
    }
  }
  unhighlight(a, b = this.overCls) {
    let c;
    const e = (c = this.highlighted) == null ? void 0 : c.get(a);
    if (e) {
      e.delete(b);
      if (!e.size) {
        this.highlighted.delete(a);
      }
    }
    for (const f of this.getElementsForDependency(a)) {
      f.classList.remove(b);
    }
  }
  highlightEventDependencies(a, b) {
    a.dependencies.forEach(c => this.highlight(c, b));
  }
  unhighlightEventDependencies(a, b) {
    a.dependencies.forEach(c => this.unhighlight(c, b));
  }
  getDependenciesToConsider(a, b, c, e) {
    let f, g;
    const {eventStore: i} = this.project;
    return (g = (f = super.getDependenciesToConsider) == null ? void 0 : f.call(this, a, b, c, e)) != null ? g : this.project.dependencyStore.records.filter(j => j.isValid && !i.isFiltered || i.isAvailable(j.fromEvent) && i.isAvailable(j.toEvent));
  }
  getDependencyKey(a, b, c) {
    return 'dep:' + a.id + ';from:' + b.id + ';to:' + c.id + ';gen:' + this.generation;
  }
  drawDependency(a, b = false, c = null) {
    let e, f, g, i;
    const j = this, {
        domConfigs: k,
        client: l,
        rowStore: n,
        topIndex: o,
        bottomIndex: p
      } = j, {useInitialAnimation: q} = l, {idMap: r} = n, {
        startMS: u,
        endMS: v
      } = j.visibleDateRange, {
        fromEvent: w,
        toEvent: z
      } = a;
    let A = w.assigned, B = z.assigned;
    if (w.isScheduled && z.isScheduled && (A == null ? void 0 : A.size) && (B == null ? void 0 : B.size)) {
      if (j.usingLinks) {
        A = [...A].flatMap(collectLinkedAssignments);
        B = [...B].flatMap(collectLinkedAssignments);
      }
      for (const C of A) {
        for (const E of B) {
          const F = (f = r[(e = C.resource) == null ? void 0 : e.id]) == null ? void 0 : f.index, G = (i = r[(g = E.resource) == null ? void 0 : g.id]) == null ? void 0 : i.index, H = Math.min(w.startDateMS, z.startDateMS), I = Math.max(w.endDateMS, z.endDateMS);
          if (l.isExporting || F != null && G != null && (C.drawDependencies !== false && E.drawDependencies !== false) && (n.isAvailable(C.resource) && n.isAvailable(E.resource)) && !(F < o && G < o || F > p && G > p || H < u && I < u || H > v && I > v)) {
            const J = j.getDependencyKey(a, C, E), K = j.getDomConfigs(a, C, E, c);
            if (K) {
              if (q) {
                K[0].style = { 'animationDelay': Math.max(F, G) / 20 * 1000 + 'ms' };
              }
              k.set(J, K);
            } else {
              k.delete(J);
            }
          }
          j.afterDrawDependency(a, F, G, H, I);
        }
      }
    }
    if (!b) {
      j.domSync();
    }
  }
  afterDrawDependency(a, b, c, e, f) {
    let g;
    (g = super.afterDrawDependency) == null ? void 0 : g.call(this, a, b, c, e, f);
  }
  beforeDraw() {
    let a;
    (a = super.beforeDraw) == null ? void 0 : a.call(this);
  }
  afterDraw() {
    let a;
    (a = super.afterDraw) == null ? void 0 : a.call(this);
  }
  domSync(a = this.client.svgCanvas, b = false) {
    DomSync.sync({
      'targetElement': a,
      'domConfig': {
        'onlyChildren': true,
        'children': Array.from(this.domConfigs.values()).flat()
      },
      'syncIdField': 'syncId',
      'releaseThreshold': 0,
      'strict': true,
      'callback'() {
      }
    });
    if (b) {
      this.clearDomConfigs();
    }
  }
  fillDrawingCache() {
    const a = this, {client: b} = a;
    a.relativeTo = Rectangle.from(b.svgCanvas);
    a.visibleResources = b.visibleResources;
    a.visibleDateRange = b.visibleDateRange;
    a.topIndex = a.rowStore.indexOf(a.visibleResources.first);
    a.bottomIndex = a.rowStore.indexOf(a.visibleResources.last);
    if (a.usingLinks == null) {
      a.usingLinks = b.resourceStore.some(c => c.hasLinks);
    }
  }
  clearDomConfigs() {
    this.domConfigs.clear();
  }
  draw() {
    const a = this, {client: b} = a, {visibleDateRange: c} = b;
    if (b.refreshSuspended || !b.foregroundCanvas || !c || !b.isEngineReady || a.disabled && !a._isDisabling || b.isExporting) {
      return;
    }
    a.fillDrawingCache();
    a.clearDomConfigs();
    if (b.firstVisibleRow && b.lastVisibleRow && b.timeAxis.count && !a.disabled && c.endMS - c.startMS > 0) {
      const {
          topIndex: e,
          bottomIndex: f
        } = a, g = a.getDependenciesToConsider(c.startMS, c.endMS, e, f);
      a.beforeDraw();
      for (const i of g) {
        a.drawDependency(i, true);
      }
      a.afterDraw();
    }
    a.domSync();
    b.trigger('dependenciesDrawn');
  }
  doRefresh() {
    let a, b, c, e, f;
    const g = this, {client: i} = g, {
        scheduledEventName: j,
        features: k
      } = i;
    g.draw();
    g.drawingLive = i.dependencyStore.count && (i.isAnimating || i.useInitialAnimation && i.eventStore.count || ((a = k[j + 'Drag']) == null ? void 0 : a.isActivelyDragging) || ((b = k[j + 'Drag']) == null ? void 0 : b.isAborting) || ((c = k[j + 'Resize']) == null ? void 0 : c.isResizing) || ((e = k[j + 'SegmentDrag']) == null ? void 0 : e.isActivelyDragging) || ((f = k[j + 'SegmentResize']) == null ? void 0 : f.isResizing));
    g.drawingLive && g.refresh(false, true);
  }
  rafRefresh() {
    this.doRefresh.now();
  }
  refresh(a = this.immediateRefresh, b = false) {
    const c = this, {client: e} = c;
    if (!e.refreshSuspended && !c.disabled && e.isPainted && !e.timeAxisSubGrid.collapsed) {
      if (a) {
        c.doRefresh.now();
      } else if (b) {
        c.rafRefresh();
      } else {
        c.doRefresh();
      }
    }
  }
  reset(a = {}, b = false) {
    let c;
    (c = super.reset) == null ? void 0 : c.call(this);
    this.refresh(b);
  }
  immediateReset() {
    this.reset({}, true);
  }
  render() {
    this.client.getConfig('svgCanvas');
  }
};
Dependencies._$name = 'Dependencies';
GridFeatureManager.registerFeature(Dependencies, false, [
  'Scheduler',
  'ResourceHistogram'
]);
const DependencyEditor = class extends Popup {
  static get $name() {
    return 'DependencyEditor';
  }
  static get defaultConfig() {
    return {
      'items': [],
      'draggable': { 'handleSelector': ':not(button,.b-field-inner)' },
      'axisLock': 'flexible'
    };
  }
  processWidgetConfig(a) {
    const {dependencyEditFeature: b} = this;
    if (a.ref === 'lagField' && !b.showLagField) {
      return false;
    }
    if (a.ref === 'deleteButton' && !b.showDeleteButton) {
      return false;
    }
    return super.processWidgetConfig(a);
  }
  afterShow(...a) {
    const {deleteButton: b} = this.widgetMap;
    if (b) {
      b.hidden = !this.record.isPartOfStore();
    }
    super.afterShow(...a);
  }
  onInternalKeyDown(a) {
    this.trigger('keyDown', { 'event': a });
    super.onInternalKeyDown(a);
  }
};
DependencyEditor._$name = 'DependencyEditor';
const _DependencyEdit = class _DependencyEdit extends InstancePlugin {
  static configurable = {
    'autoClose': true,
    'saveAndCloseOnEnter': true,
    'showDeleteButton': true,
    'triggerEvent': 'dependencydblclick',
    'showLagField': false,
    'dependencyRecord': null,
    'editorConfig': {
      'title': 'L{Edit dependency}',
      'localeClass': _DependencyEdit,
      'closable': true,
      'defaults': { 'localeClass': _DependencyEdit },
      'items': {
        'fromNameField': {
          'type': 'display',
          'weight': 100,
          'label': 'L{From}'
        },
        'toNameField': {
          'type': 'display',
          'weight': 200,
          'label': 'L{To}'
        },
        'typeField': {
          'type': 'combo',
          'weight': 300,
          'label': 'L{Type}',
          'name': 'type',
          'editable': false,
          'valueField': 'id',
          'displayField': 'name',
          'localizeDisplayFields': true,
          'buildItems': function () {
            const a = this.parent;
            return Object.keys(DependencyModel.Type).map(b => ({
              'id': DependencyModel.Type[b],
              'name': a.L(b),
              'localeKey': b
            }));
          }
        },
        'lagField': {
          'type': 'duration',
          'weight': 400,
          'label': 'L{Lag}',
          'name': 'lag',
          'allowNegative': true,
          'highlightExternalChange': false
        }
      },
      'bbar': {
        'defaults': { 'localeClass': _DependencyEdit },
        'items': {
          'foo': {
            'type': 'widget',
            'cls': 'b-label-filler'
          },
          'saveButton': {
            'color': 'b-green',
            'text': 'L{Save}'
          },
          'deleteButton': {
            'color': 'b-gray',
            'text': 'L{Delete}'
          },
          'cancelButton': {
            'color': 'b-gray',
            'text': 'L{Object.Cancel}'
          }
        }
      }
    }
  };
  static $name = 'DependencyEdit';
  construct(a, b) {
    const c = this;
    a.dependencyEdit = c;
    super.construct(a, b);
    if (!a.features.dependencies) {
      throw new Error('Dependencies feature required when using DependencyEdit');
    }
  }
  doDestroy() {
    let a;
    this.clientListenersDetacher();
    (a = this.editor) == null ? void 0 : a.destroy();
    super.doDestroy();
  }
  changeEditorConfig(a) {
    const b = this, {
        autoClose: c,
        cls: e,
        client: f
      } = b;
    return ObjectHelper.assign({
      'owner': f,
      'align': 'b-t',
      'id': f.id + '-dependency-editor',
      'autoShow': false,
      'anchor': true,
      'scrollAction': 'realign',
      'constrainTo': globalThis,
      'autoClose': c,
      'cls': e
    }, a);
  }
  get isValid() {
    return Object.values(this.editor.widgetMap).every(a => {
      if (!a.name || a.hidden) {
        return true;
      }
      return a.isValid !== false;
    });
  }
  get values() {
    const a = {};
    this.editor.eachWidget(b => {
      if (!b.name || b.hidden)
        return;
      a[b.name] = b.value;
    }, true);
    return a;
  }
  onBeforeSave(a) {
  }
  onAfterSave(a) {
  }
  updateRecord(a) {
    const {values: b} = this;
    if (b.lag) {
      b.lagUnit = b.lag.unit;
      b.lag = b.lag.magnitude;
    }
    if ('type' in b) {
      a.fromSide != null && (b.fromSide = null);
      a.toSide != null && (b.toSide = null);
    }
    ObjectHelper.cleanupProperties(b, true);
    a.set(b);
  }
  onPopupKeyDown({event: a}) {
    if (a.key === 'Enter' && this.saveAndCloseOnEnter && a.target.tagName.toLowerCase() === 'input') {
      a.preventDefault();
      this.onSaveClick();
    }
  }
  onHide() {
    if (!this.isApplyingChanges) {
      this.afterCancel();
    }
  }
  async onSaveClick() {
    const a = this;
    a.isApplyingChanges = true;
    if (await a.save()) {
      a.afterSave();
      await a.editor.hide();
      a.isApplyingChanges = false;
    } else {
      a.isApplyingChanges = false;
    }
  }
  async onDeleteClick() {
    const a = this;
    a.isApplyingChanges = true;
    if (await a.deleteDependency()) {
      a.afterDelete();
    }
    await a.editor.hide();
    a.isApplyingChanges = false;
  }
  onCancelClick() {
    this.editor.hide();
  }
  afterSave() {
  }
  afterDelete() {
  }
  afterCancel() {
  }
  internalShowEditor(a) {
    const b = this, {
        client: c,
        lastPointerDown: e
      } = b, {dependencies: f} = c.features, g = b.getEditor(a), i = (f == null ? void 0 : f.getElementForDependency(a)) || (e == null ? void 0 : e.target) || c.timeAxisSubGridElement, j = Rectangle.from(i);
    b.loadRecord(a);
    c.trigger('beforeDependencyEditShow', {
      'dependencyEdit': b,
      'dependencyRecord': a,
      'editor': g
    });
    let k;
    if (e) {
      Object.defineProperties(e, {
        'offsetX': {
          'configurable': true,
          'value': e.clientX - j.x
        },
        'offsetY': {
          'configurable': true,
          'value': e.clientY - j.y
        }
      });
      k = {
        'target': e,
        'align': 't50-b0'
      };
    } else {
      k = {
        'target': i,
        'align': 'c-c'
      };
    }
    const l = g.showBy(k), n = [];
    let o = 0;
    g.eachWidget(p => {
      const {
        labelElement: q,
        element: r
      } = p;
      if (q) {
        if (q.getBoundingClientRect().top < r.getBoundingClientRect().top) {
          return false;
        }
        p.labelWidth = null;
        o = Math.max(o, q.offsetWidth);
        n.push(p);
      }
    });
    n.forEach(p => p.labelWidth = o);
    return l;
  }
  async editDependency(a) {
    const b = this, {client: c} = b;
    if (c.readOnly || a.readOnly || await c.trigger('beforeDependencyEdit', {
        'dependencyEdit': b,
        'dependencyRecord': a
      }) === false) {
      return false;
    }
    await this.internalShowEditor(a);
    return true;
  }
  getEditor() {
    let a, b, c;
    const e = this;
    let {editor: f} = e;
    if (f) {
      return f;
    }
    f = e.editor = DependencyEditor.new({
      'dependencyEditFeature': e,
      'autoShow': false,
      'anchor': true,
      'scrollAction': 'realign',
      'constrainTo': globalThis,
      'autoClose': e.autoClose,
      'cls': e.cls,
      'rootElement': e.client.rootElement,
      'internalListeners': {
        'keydown': e.onPopupKeyDown,
        'hide': e.onHide,
        'thisObj': e
      }
    }, e.editorConfig);
    if (f.items.length === 0) {
      console.warn('Editor configured without any `items`');
    }
    f.eachWidget(g => {
      const i = g.ref || g.id;
      if (i && !e[i]) {
        e[i] = g;
      }
    });
    (a = e.saveButton) == null ? void 0 : a.ion({
      'click': 'onSaveClick',
      'thisObj': e
    });
    (b = e.deleteButton) == null ? void 0 : b.ion({
      'click': 'onDeleteClick',
      'thisObj': e
    });
    (c = e.cancelButton) == null ? void 0 : c.ion({
      'click': 'onCancelClick',
      'thisObj': e
    });
    return e.editor;
  }
  loadRecord(a) {
    const b = this;
    b.fromNameField.value = a.fromEvent.name;
    b.toNameField.value = a.toEvent.name;
    if (b.lagField) {
      b.lagField.value = new Duration(a.lag, a.lagUnit);
    }
    b.editor.record = b.dependencyRecord = a;
  }
  async save() {
    let a;
    const b = this, {
        client: c,
        dependencyRecord: e
      } = b;
    if (!e || !b.isValid) {
      return;
    }
    const {
      dependencyStore: f,
      values: g
    } = b;
    if (c.trigger('beforeDependencySave', {
        'dependencyRecord': e,
        'values': g
      }) !== false) {
      b.onBeforeSave(e);
      b.updateRecord(e);
      if (f && !e.stores.length) {
        if (c.trigger('beforeDependencyAdd', {
            'dependencyRecord': e,
            'dependencyEdit': b
          }) === false) {
          return;
        }
        f.add(e);
      }
      await ((a = c.project) == null ? void 0 : a.commitAsync());
      c.trigger('afterDependencySave', { 'dependencyRecord': e });
      b.onAfterSave(e);
    }
    return e;
  }
  async deleteDependency() {
    let a;
    const {
      client: b,
      editor: c,
      dependencyRecord: e
    } = this;
    if (b.trigger('beforeDependencyDelete', { 'dependencyRecord': e }) !== false) {
      if (c.containsFocus) {
        c.revertFocus();
      }
      b.dependencyStore.remove(e);
      await ((a = b.project) == null ? void 0 : a.commitAsync());
      return true;
    }
    return false;
  }
  get dependencyStore() {
    return this.client.dependencyStore;
  }
  onActivateEditor({
    dependency: a,
    event: b
  }) {
    if (!this.disabled) {
      this.lastPointerDown = b;
      this.editDependency(a);
    }
  }
  updateTriggerEvent(a) {
    let b;
    (b = this.clientListenersDetacher) == null ? void 0 : b.call(this);
    if (a) {
      this.clientListenersDetacher = this.client.ion({
        [a]: this.onActivateEditor,
        'thisObj': this
      });
    }
  }
};
const DependencyEdit = _DependencyEdit;
DependencyEdit._$name = 'DependencyEdit';
GridFeatureManager.registerFeature(DependencyEdit, false);
const ScheduleContext = class extends InstancePlugin.mixin(Delayable_default) {
  static configurable = {
    'triggerEvent': 'click',
    'renderer': null,
    'widget': {
      '$config': [
        'lazy',
        'nullify'
      ],
      'value': null
    },
    'context': {
      '$config': {
        'equal'(a, b) {
          return (a == null ? void 0 : a.index) === (b == null ? void 0 : b.index) && (a == null ? void 0 : a.tickParentIndex) === (b == null ? void 0 : b.tickParentIndex) && !(((a == null ? void 0 : a.tickStartDate) || 0) - ((b == null ? void 0 : b.tickStartDate) || 0)) && (a == null ? void 0 : a.eventRecord) === (b == null ? void 0 : b.eventRecord);
        }
      }
    },
    'activateOnEvent': null,
    'shareWithEvent': null
  };
  cursorTrackingSuspended = 0;
  constructor() {
    super(...arguments);
  }
  static get $name() {
    return 'ScheduleContext';
  }
  construct(a, b) {
    super.construct(a, b);
    const {triggerEvent: c} = this, e = {
        'datachange': 'syncContextElement',
        'timeaxisviewmodelupdate': 'onTimeAxisViewModelUpdate',
        'presetchange': 'clear',
        'thisObj': this
      };
    if (c === 'mouseover') {
      e.timelineContextChange = 'onTimelineContextChange';
    } else {
      if (c === 'click' || c === 'mousedown') {
        e.schedulecontextmenu = 'onScheduleContextGesture';
      }
      e['schedule' + c] = 'onScheduleContextGesture';
    }
    a.ion(e);
    a.rowManager.ion({
      'rowheight': 'syncContextElement',
      'thisObj': this
    });
  }
  changeTriggerEvent(a) {
    if (a === 'hover' || a === 'mousemove') {
      a = 'mouseover';
    }
    return a;
  }
  changeWidget(a, b) {
    if (a) {
      return Widget.reconfigure(b, {
        ...a,
        'owner': this.client,
        'positioned': true,
        'appendTo': this.client.timeAxisSubGridElement,
        'hidden': true,
        'showAnimation': false
      }, this);
    }
    b.destroy();
  }
  get element() {
    let a;
    if (!this._element) {
      this._element = ((a = this.widget) == null ? void 0 : a.element) || DomHelper.createElement({ 'parent': this.client.timeAxisSubGridElement });
      this.element.classList.add('b-schedule-selected-tick');
    }
    return this._element;
  }
  onTimelineContextChange({context: a}) {
    this.context = a;
  }
  onScheduleContextGesture(a) {
    this.context = a;
  }
  onTimeAxisViewModelUpdate({source: a}) {
    let b;
    if (a.timeAxis.includes((b = this.context) == null ? void 0 : b.tick)) {
      this.syncContextElement();
    } else {
      this.clear();
    }
  }
  clear() {
    this.context = null;
  }
  changeContext(a) {
    let b;
    if (!((b = this.widget) == null ? void 0 : b.query(c => c.floating && c.isVisible))) {
      if (!this.shareWithEvent && this.hasEvent(a)) {
        return null;
      }
      return !this.activateOnEvent && (a == null ? void 0 : a.eventElement) ? null : a || null;
    }
  }
  updateActivateOnEvent(a) {
    const b = this;
    if (a) {
      const {
        triggerEvent: c,
        client: e
      } = b;
      b.shareWithEvent = true;
      if (c !== 'mouseover') {
        e.ion({
          'name': 'scheduleContextTrigger',
          ['' + e.scheduledEventName + c]: 'onScheduleContextGesture',
          'thisObj': b
        });
      }
    } else {
      b.shareWithEvent = b.config.shareWithEvent;
      b.detachListeners('scheduleContextTrigger');
    }
  }
  hasEvent(a) {
    let b;
    return Boolean(a && ((b = this.client.eventStore.getEvents({
      'resourceRecord': a.resourceRecord,
      'startDate': a.tickStartDate,
      'endDate': a.tickEndDate
    })) == null ? void 0 : b.length));
  }
  updateContext(a, b) {
    this.syncContextElement();
  }
  syncContextElement() {
    const a = this, {
        client: b,
        element: c,
        widget: e,
        context: f
      } = a, {isVertical: g} = b, i = f && (g ? b.rowManager.rows[0] : b.getRowFor(f.resourceRecord));
    if (f && i && this.enabled && !f.domEvent.buttons) {
      const j = this, {renderer: k} = j, {style: l} = c, {
          tickStartDate: n,
          tickEndDate: o,
          resourceRecord: p
        } = f, q = b.currentOrientation.getTimeSpanRenderData({
          'startDate': n,
          'endDate': o,
          'startDateMS': n.getTime(),
          'endDateMS': o.getTime()
        }, p);
      let r, u, v;
      if (g) {
        r = q.top;
        u = q.resourceWidth;
        v = q.height;
      } else {
        r = i.top;
        u = q.width;
        v = i.height;
      }
      f.index = i.index;
      c.classList.add('b-schedule-selected-tick');
      c.classList.toggle('b-contains-widget', Boolean(c.querySelector('.b-widget')));
      if (e) {
        e.hide(false);
        if (!f.eventRecord) {
          e.setConfig({
            'width': u,
            'height': v,
            'timelineContext': f,
            'x': q.left,
            'y': r
          });
          e.show({ 'animate': false });
        }
      } else if (c) {
        l.width = u + 'px';
        l.height = v + 'px';
        DomHelper.setTranslateXY(c, q.left, r);
        c.innerHTML = '';
        k && j.callback(k, j, [
          f,
          c
        ]);
        l.display = '';
      }
    } else {
      (e == null ? void 0 : e.hide(false)) || (c.style.display = 'none');
    }
  }
};
ScheduleContext.featureClass = 'b-scheduler-context';
ScheduleContext._$name = 'ScheduleContext';
GridFeatureManager.registerFeature(ScheduleContext, false, ['Scheduler']);
const actions6 = {
  'cut': 1,
  'copy': 1,
  'paste': 1
};
const EventCopyPaste = class extends CopyPasteBase.mixin(AttachToProjectMixin_default) {
  static configurable = {
    'nameField': 'name',
    'copyPasteAction': 'clone',
    'eventToStringFields': [
      'name',
      'startDate',
      'endDate',
      'duration',
      'durationUnit',
      'allDay'
    ],
    'allowedDataSources': [
      'EventCopyPaste',
      'RowCopyPaste',
      'TaskCopyPaste'
    ],
    'allowNoAssignments': false
  };
  static $name = 'EventCopyPaste';
  entityName = 'event';
  constructor() {
    super(...arguments);
  }
  static get pluginConfig() {
    return {
      'assign': [
        'copyEvents',
        'pasteEvents'
      ],
      'chain': [
        'populateEventMenu',
        'populateScheduleMenu',
        'onEventDataGenerated'
      ]
    };
  }
  construct(a, b) {
    super.construct(a, b);
    a.ion({
      'eventClick': 'onEventClick',
      'scheduleClick': 'onScheduleClick',
      'projectChange': () => {
        this.clearClipboard();
        this._cellClickedContext = null;
      },
      'thisObj': this
    });
  }
  get scheduler() {
    return this.client;
  }
  attachToEventStore(a) {
    super.attachToEventStore(a);
    delete this._eventClickedContext;
  }
  onEventDataGenerated(a) {
    const {assignmentRecord: b} = a;
    if (b) {
      a.cls['b-cut-item'] = b.meta.isCut;
    }
  }
  onEventClick(a) {
    this._cellClickedContext = null;
    this._eventClickedContext = a;
  }
  onScheduleClick(a) {
    this._cellClickedContext = a;
    this._eventClickedContext = null;
  }
  isActionAvailable({
    event: a,
    actionName: b
  }) {
    let c, e;
    if (actions6[b]) {
      return !this.disabled && globalThis.getSelection().toString().length === 0 && !((c = this.client.features.cellEdit) == null ? void 0 : c.isEditing) && Boolean(a.target.closest('.b-timeaxissubgrid')) && !((e = this.client.focusedCell) == null ? void 0 : e.isSpecialRow);
    }
  }
  async copy() {
    await this.copyEvents();
  }
  async cut() {
    await this.copyEvents(void 0, true);
  }
  async paste() {
    await this.pasteEvents();
  }
  async copyEvents(a = this.scheduler.selectedAssignments, b = false) {
    let c;
    const e = this, {scheduler: f} = e;
    if (f.splitFrom) {
      return f.splitFrom.features.eventCopyPaste.copyEvents(a, b);
    }
    if (!(a == null ? void 0 : a.length)) {
      return;
    }
    const {isEventModel: g} = a[0];
    if (b) {
      a = a.filter(l => g ? !l.readOnly : !l.event.readOnly);
    } else {
      a = a.slice();
    }
    const i = g ? a.flatMap(l => l.assignments) : a, j = i.length ? i.map(l => l.event) : a;
    if (!i.length && (!e.allowNoAssignments || !j.length)) {
      return;
    }
    const k = (c = await e.writeToClipboard({
      'assignmentRecords': i,
      'eventRecords': j
    }, b)) == null ? void 0 : c.data;
    if (!k) {
      return;
    }
    f.trigger('copy', {
      'originalAssignmentRecords': i,
      'originalEventRecords': j,
      'assignmentRecords': k.assignmentRecords,
      'eventRecords': k.eventRecords,
      'isCut': b,
      'entityName': e.entityName
    });
    e.refreshClient();
    e._focusedEventOnCopy = e._eventClickedContext;
  }
  async beforeCopy({
    data: {
      assignmentRecords: a,
      eventRecords: b
    },
    isCut: c
  }) {
    return await this.scheduler.trigger('beforeCopy', {
      'assignmentRecords': a,
      'eventRecords': b,
      'isCut': c,
      'entityName': this.entityName
    });
  }
  handleCutData({source: a}) {
    let b;
    const c = this;
    if (a !== c && ((b = c.cutData) == null ? void 0 : b.length)) {
      const {
        assignmentRecords: e,
        eventRecords: f
      } = c.cutData[0];
      if (e == null ? void 0 : e.length) {
        c.scheduler.assignmentStore.remove(e);
      }
      if (f == null ? void 0 : f.length) {
        c.scheduler.eventStore.remove(f);
      }
    }
  }
  stringConverter({eventRecords: a}) {
    const b = [];
    for (const c of a) {
      b.push(this.eventToStringFields.map(e => {
        const f = c[e];
        if (f instanceof Date) {
          return DateHelper.format(f, this.dateFormat);
        }
        return f;
      }).join('\t'));
    }
    return b.join('\n');
  }
  setIsCut({assignmentRecords: a}, b) {
    a.forEach(c => {
      c.meta.isCut = b;
    });
    this.refreshClient();
  }
  cloneData({
    data: a,
    isPaste: b
  }) {
    const c = this, {eventStore: e} = c, f = e.modelClass, g = new Set(), i = [], j = [], k = e.usesSingleAssignment || c.copyPasteAction === 'clone', l = p => {
        const q = p.copy();
        if (b) {
          q.name = c.generateNewName(q, p, e);
        }
        q.meta.originalResourceId = b ? p.meta.originalResourceId : p.resourceId;
        return q;
      };
    let {
      assignmentRecords: n,
      eventRecords: o
    } = a;
    if (b && !n && !o) {
      if ((a == null ? void 0 : a.length) && a[0].isModel) {
        o = a.map(p => new f({
          ...p.toJSON(),
          [f.idField]: void 0
        }));
        return {
          'eventRecords': o,
          'assignmentRecords': []
        };
      } else {
        return;
      }
    }
    if (b && k) {
      i.push(...o.map(p => l(p)));
    } else {
      let p = n;
      if (!(p == null ? void 0 : p.length) && c.allowNoAssignments) {
        p = o;
      }
      for (const q of p) {
        let r = q.isEventModel ? q : q.event;
        if (g.has(r)) {
          continue;
        }
        g.add(r);
        if (k || q.isEventModel) {
          r = l(r);
          if (r.constraintDate) {
            r.constraintDate = null;
          }
          i.push(r);
        } else {
          j.push(q.copy());
        }
      }
    }
    return {
      'assignmentRecords': j,
      'eventRecords': i
    };
  }
  async pasteEvents(a, b) {
    let c, e, f, g, i, j, k;
    const l = this, {
        scheduler: n,
        copyPasteAction: o
      } = l, {
        entityName: p,
        _cellClickedContext: q,
        _eventClickedContext: r
      } = l, {
        eventStore: u,
        assignmentStore: v
      } = n;
    if (arguments.length === 0) {
      if (q) {
        a = q.date;
        b = q.resourceRecord;
      } else if (l._focusedEventOnCopy !== r) {
        a = r.eventRecord.startDate;
        b = r.resourceRecord;
      }
    }
    if (b) {
      b = b.$original;
    }
    if (n.splitFrom) {
      return n.splitFrom.features.eventCopyPaste.pasteEvents(a, b);
    }
    const w = await l.readFromClipboard({
        'resourceRecord': b,
        'date': a
      }), z = w == null ? void 0 : w.isCut, {
        assignmentRecords: A,
        eventRecords: B
      } = (c = w == null ? void 0 : w.data) != null ? c : {}, C = A == null ? void 0 : A.length;
    let E = null;
    if (!C && !(B == null ? void 0 : B.length)) {
      return;
    }
    if (C && (z || l.copyPasteAction !== 'clone')) {
      if (z) {
        B.length = 0;
      }
      for (const G of A) {
        const H = (e = G.event) != null ? e : u.getById(G.eventId);
        if (B.includes(H)) {
          G.remove();
          continue;
        }
        if (b && !H.resources.includes(b)) {
          G.resource = b;
        }
        if (a) {
          H.startDate = a;
        }
        if (H.constraintDate) {
          H.constraintDate = null;
        }
        G.meta.isCut = false;
        if (z) {
          B.push(H);
          E = G;
        } else {
          [E] = v.add(G);
        }
      }
    }
    for (const I of B) {
      if (a) {
        I.startDate = a;
      }
      if (!z) {
        u.add(I);
      }
      const J = A == null ? void 0 : A.filter(K => K.eventId === I.id && !I.isAssignedTo(K.resourceId));
      if (!z && o === 'clone' && (J == null ? void 0 : J.length) && !arguments[1]) {
        v.add(J);
      } else if (b && !I.isAssignedTo(b)) {
        I.assign(b, true);
      } else if (!z) {
        I.resourceId = I.meta.originalResourceId;
      }
      E = v.last;
    }
    if (!b) {
      b = (i = (f = A == null ? void 0 : A[0]) == null ? void 0 : f.resource) != null ? i : (g = B == null ? void 0 : B[0]) == null ? void 0 : g.resource;
    }
    n.trigger('paste', {
      'eventRecords': B,
      'assignmentRecords': A,
      'pastedEventRecords': B,
      'originalEventRecords': (j = w.originalData) == null ? void 0 : j.eventRecords,
      'originalAssignmentRecords': (k = w.originalData) == null ? void 0 : k.assignmentRecords,
      'resourceRecord': b,
      'date': a,
      'isCut': z,
      'entityName': p
    });
    const F = n.ion({
      'renderEvent'({assignmentRecord: K}) {
        if (K === E) {
          n.navigateTo(K, { 'scrollIntoView': false });
          F();
        }
      }
    });
    if (z) {
      await l.clearClipboard();
    }
  }
  async beforePaste({
    originalData: a,
    data: {
      assignmentRecords: b,
      eventRecords: c
    },
    resourceRecord: e,
    isCut: f,
    date: g
  }) {
    let i;
    const {scheduler: j} = this, k = a == null ? void 0 : a.assignmentRecords;
    if (!e) {
      e = (i = k == null ? void 0 : k[0]) == null ? void 0 : i.resource;
    }
    const l = {
      'assignmentRecords': b,
      'eventRecords': c,
      'resourceRecord': e,
      'originalAssignmentRecords': k,
      'originalEventRecords': a == null ? void 0 : a.eventRecords,
      'date': g,
      'isCut': f,
      'entityName': this.entityName
    };
    let n;
    if (e == null ? void 0 : e.readOnly) {
      n = 'resourceReadOnly';
    }
    if (!n && !j.allowOverlap) {
      const o = (b == null ? void 0 : b.length) ? b : c;
      for (const p of o) {
        const q = p.isAssignmentModel ? p.event : p;
        if (!j.isDateRangeAvailable(g != null ? g : q.startDate, g ? DateHelper.add(g, q.duration, q.durationUnit) : q.endDate, f ? q : null, e != null ? e : p.resource)) {
          n = 'overlappingEvents';
          break;
        }
      }
    }
    if (n) {
      j.trigger('pasteNotAllowed', {
        ...l,
        'reason': n
      });
      return false;
    }
    return await this.scheduler.trigger('beforePaste', l);
  }
  stringParser(a) {
    const {
        eventStore: b,
        assignmentStore: c
      } = this.scheduler, {modifiedRecords: e} = this.setFromStringData(a, true, b, this.eventToStringFields), f = [];
    for (const g of e) {
      const i = new c.modelClass({ 'eventId': g.id });
      i.event = g;
      f.push(i);
    }
    return {
      'eventRecords': e,
      'assignmentRecords': f
    };
  }
  populateEventMenu({
    assignmentRecord: a,
    items: b
  }) {
    let c;
    const e = this, {scheduler: f} = e;
    if (e.disabled) {
      return;
    }
    b.copyEvent = {
      'text': 'L{copyEvent}',
      'localeClass': e,
      'icon': 'b-icon b-icon-copy',
      'cls': 'b-separator',
      'weight': 110,
      'onItem': () => {
        const g = f.isAssignmentSelected(a) ? f.selectedAssignments : [a];
        e.copyEvents(g);
      }
    };
    if (!f.readOnly) {
      b.cutEvent = {
        'text': 'L{cutEvent}',
        'localeClass': e,
        'icon': 'b-icon b-icon-cut',
        'weight': 120,
        'disabled': a.readOnly || ((c = a.event) == null ? void 0 : c.readOnly),
        'onItem': () => {
          const g = f.isAssignmentSelected(a) ? f.selectedAssignments : [a];
          e.copyEvents(g, true);
        }
      };
    }
  }
  populateScheduleMenu({
    items: a,
    resourceRecord: b
  }) {
    const c = this, {scheduler: e} = c;
    if (!c.disabled && !e.readOnly && c.hasClipboardData() !== false) {
      a.pasteEvent = {
        'text': 'L{pasteEvent}',
        'localeClass': c,
        'icon': 'b-icon b-icon-paste',
        'disabled': e.resourceStore.count === 0 && !c.allowNoAssignments || (b == null ? void 0 : b.readOnly),
        'weight': 110,
        'internalListeners': {
          'item': c.onScheduleMenuPasteClick,
          'thisObj': c
        }
      };
    }
  }
  onScheduleMenuPasteClick({
    date: a,
    resourceRecord: b
  }) {
    this.pasteEvents(a, b);
  }
  refreshClient() {
    this.scheduler.refreshWithTransition();
  }
};
EventCopyPaste.featureClass = 'b-event-copypaste';
EventCopyPaste._$name = 'EventCopyPaste';
GridFeatureManager.registerFeature(EventCopyPaste, true, 'Scheduler');
const EventDrag = class extends DragBase {
  static get $name() {
    return 'EventDrag';
  }
  static get configurable() {
    return {
      'singleDirection': null,
      'constrainDragToResource': false,
      'constrainDragToTimeSlot': false,
      'externalDropTargetSelector': null,
      'validatorFn': (a, b) => {
      },
      'validatorFnThisObj': null,
      'unifiedDrag': null,
      'snapToPosition': null,
      'copyKey': 'CTRL',
      'copyMode': 'auto',
      'mode': 'move',
      'capitalizedEventName': null
    };
  }
  afterConstruct() {
    this.capitalizedEventName = this.capitalizedEventName || this.client.capitalizedEventName;
    super.afterConstruct(...arguments);
  }
  changeMode(a) {
    const {
      dragData: b,
      copyMode: c
    } = this;
    if ((c === 'event' || c === 'auto' || c === 'assignment' && !this.scheduler.eventStore.usesSingleAssignment) && (!b || b.eventRecords.every(e => !e.isRecurring))) {
      return a;
    }
  }
  updateMode(a) {
    if (this.dragData) {
      if (a === 'copy') {
        this.setCopying();
      } else {
        this.setMoving();
      }
      this.client.trigger('eventDragModeChange', { 'mode': a });
    }
  }
  setCopying() {
    const {dragData: a} = this;
    if (!a) {
      return;
    }
    if (!a.eventBarCopies.some(b => b.isConnected)) {
      a.eventBarCopies.forEach(b => {
        b.classList.add('b-drag-proxy-copy');
        b.classList.remove('b-hidden');
        a.context.grabbedParent.appendChild(b);
        b.retainElement = true;
      });
    } else {
      a.eventBarCopies.forEach(b => {
        b.classList.remove('b-hidden');
      });
    }
  }
  setMoving() {
    const {dragData: a} = this;
    if (!a) {
      return;
    }
    a.eventBarCopies.forEach(b => {
      b.classList.add('b-hidden');
    });
  }
  get scheduler() {
    return this.client;
  }
  onAfterDragStart(a) {
    const b = this, {drag: c} = b, {
        context: {element: e}
      } = a;
    if (b.singleDirection) {
      c.lockY = a.initialDirection === 'horizontal';
      c.lockX = !c.lockY;
    }
    super.onAfterDragStart(a);
    b.handleKeyDownOrMove(a.event);
    b.keyEventDetacher = EventHelper.on({
      'element': DomHelper.getRootElement(e),
      'keydown': b.handleKeyDownOrMove,
      'keyup': b.handleKeyUp,
      'thisObj': b
    });
  }
  onDragReset(a) {
    let b;
    super.onDragReset(a);
    (b = this.keyEventDetacher) == null ? void 0 : b.call(this);
    this.mode = 'move';
  }
  onDrop(a) {
    let b;
    (b = this.dragData.eventBarCopies) == null ? void 0 : b.forEach(c => c.remove());
    return super.onDrop(a);
  }
  getDraggableElement(a) {
    return a == null ? void 0 : a.closest(this.drag.targetSelector);
  }
  resolveEventRecord(a, b = this.client) {
    return b.resolveEventRecord(a);
  }
  isElementDraggable(a, b) {
    let c;
    const e = this, {client: f} = e, g = e.getDraggableElement(a);
    if (!g || e.disabled || f.readOnly) {
      return false;
    }
    if (a.matches('[class$="-handle"]')) {
      return false;
    }
    const i = e.resolveEventRecord(g, f);
    if (!i || !i.isDraggable || i.readOnly) {
      return false;
    }
    const j = ((c = f['is' + e.capitalizedEventName + 'ElementDraggable']) == null ? void 0 : c.call(f, g, i, a, b)) === false;
    return !j;
  }
  getTriggerParams(a) {
    const {
      assignmentRecords: b,
      eventRecords: c,
      resourceRecord: e,
      browserEvent: f
    } = a;
    return {
      'context': a,
      'eventRecords': c,
      'resourceRecord': e,
      'assignmentRecords': b,
      'domEvent': f
    };
  }
  getGroupedToStoreResources(a) {
    if (a.resourcesInStore) {
      return a.resourcesInStore;
    }
    const b = this.client, c = b.isVertical ? b.resourceStore : b.store;
    return a.resourcesInStore = [...new Set(c.getAllDataRecords().map(e => e.$original))].filter(e => e.isLeaf);
  }
  getIndexDiff(a) {
    const b = this, c = b.client, e = b.currentOverClient, f = c !== e, {isVertical: g} = e, i = c.isVertical ? c.resourceStore : c.store, j = g ? e.resourceStore : e.store, {
        resourceRecord: k,
        newResource: l
      } = a;
    let n;
    if (f) {
      n = j.indexOf(l) - i.indexOf(k.$original);
    } else if (b.constainDragToResource) {
      n = 0;
    } else if (g && j.isGrouped) {
      const o = b.getGroupedToStoreResources(a);
      n = o.indexOf(k.$original) - o.indexOf(l);
    } else {
      n = i.indexOf(k.$original) - i.indexOf(l);
    }
    return n;
  }
  getNewResource(a, b, c) {
    const e = this, f = e.client, g = e.currentOverClient, i = f !== g, {isVertical: j} = g, k = f.isVertical ? f.resourceStore : f.store, l = j ? g.resourceStore : g.store;
    let {newResource: n} = a;
    if (!i) {
      if (c !== 0) {
        let o;
        if (j && l.isGrouped) {
          const p = e.getGroupedToStoreResources(a);
          o = Math.max(Math.min(p.indexOf(b) - c, p.length - 1), 0);
          n = p[o];
        } else {
          o = Math.max(Math.min(k.indexOf(b) - c, k.count - 1), 0);
          n = k.getAt(o);
          if (n.isSpecialRow) {
            n = k.getNext(n, false, true) || k.getPrevious(n, false, true);
          }
        }
        n = n == null ? void 0 : n.$original;
      } else {
        n = b;
      }
    } else {
      const q = k.indexOf(b);
      n = l.getAt(q + c) || n;
    }
    return n;
  }
  getDropData(a) {
    const b = this.getIndexDiff(a);
    return {
      'events': a.eventRecords.map(c => {
        return {
          'eventRecord': c,
          ...this.getEventNewStartEndDates(c, a.timeDiff)
        };
      }),
      'assignments': a.assignmentRecords.map(c => {
        return {
          'assignmentRecord': c,
          'resourceRecord': this.getNewResource(a, c.resource, b)
        };
      })
    };
  }
  triggerBeforeEventDropFinalize(a, b, c) {
    b.context.dropData = this.getDropData(b.context);
    super.triggerBeforeEventDropFinalize(a, b, c);
  }
  triggerBeforeEventDrag(a, b) {
    return this.client.trigger(a, b);
  }
  triggerEventDrag(a, b) {
    this.client.trigger('eventDrag', Object.assign(this.getTriggerParams(a), {
      'startDate': a.startDate,
      'endDate': a.endDate,
      'newResource': a.newResource
    }));
  }
  triggerDragStart(a) {
    this.client.navigator.skipNextClick = true;
    this.client.trigger('eventDragStart', this.getTriggerParams(a));
  }
  triggerDragAbort(a) {
    this.client.trigger('eventDragAbort', this.getTriggerParams(a));
  }
  triggerDragAbortFinalized(a) {
    this.client.trigger('eventDragAbortFinalized', this.getTriggerParams(a));
  }
  triggerAfterDrop(a, b) {
    const c = this;
    c.currentOverClient.trigger('afterEventDrop', Object.assign(c.getTriggerParams(a), { 'valid': b }));
    if (!b) {
      const {
          assignmentStore: e,
          eventStore: f
        } = c.client, g = c.dragData.initialAssignmentsState.find(({
          resource: j,
          assignment: k
        }, l) => {
          let n;
          return !e.includes(k) || !f.includes(k.event) || j.id !== ((n = c.dragData.assignmentRecords[l]) == null ? void 0 : n.resourceId);
        });
      if (g) {
        c.client.refresh();
      }
    }
    c.client.setTimeout(() => c.client.navigator.skipNextClick = false, 10);
  }
  handleKeyDownOrMove(a) {
    let b, c;
    if (this.mode !== 'copy' && (this.isCtrlKeyPress(a) || a.key && EventHelper.specialKeyFromEventKey(a.key) === ((b = this.copyKey) == null ? void 0 : b.toLowerCase()) || a[((c = this.copyKey) == null ? void 0 : c.toLowerCase()) + 'Key'])) {
      this.mode = 'copy';
    }
  }
  handleKeyUp(a) {
    if (this.isCtrlKeyPress(a) || EventHelper.specialKeyFromEventKey(a.key) === this.copyKey.toLowerCase()) {
      this.mode = 'move';
    }
  }
  isCtrlKeyPress(a) {
    return this.copyKey === 'CTRL' && a.key === (BrowserHelper.ctrlKey === 'ctrlKey' ? 'Ctrl' : 'Meta');
  }
  isValidDrop(a) {
    const {
        newResource: b,
        resourceRecord: c,
        browserEvent: e
      } = a, f = a.draggedEntities[0], {target: g} = e;
    if (!b) {
      return !this.constrainDragToTimeline && this.externalDropTargetSelector ? Boolean(g.closest(this.externalDropTargetSelector)) : false;
    }
    if (b.isSpecialRow || b.readOnly) {
      return false;
    }
    if (c.$original !== b) {
      return !f.event.resources.includes(b);
    }
    return true;
  }
  checkDragValidity(a, b) {
    let c, e;
    const f = this, g = f.currentOverClient, {newResource: i} = a;
    let j;
    if (i == null ? void 0 : i.readOnly) {
      return false;
    }
    if (i && f.hasOverlaps()) {
      j = {
        'valid': false,
        'message': f.L('L{eventOverlapsExisting}')
      };
    } else {
      j = f.validatorFn.call(f.validatorFnThisObj || f, a, b);
    }
    if (!j || j.valid) {
      j = (e = (c = g.checkEventDragValidity) == null ? void 0 : c.call(g, a, b)) != null ? e : j;
    }
    return j;
  }
  hasOverlaps() {
    const a = this, {dragData: b} = a, {
        startDate: c,
        origStart: e
      } = b, f = c - e, g = a.getIndexDiff(a.dragData);
    return b.assignmentRecords.some(i => {
      const j = a.getNewResource(b, i.resource, g), k = i.event, l = DateHelper.add(k.startDate, f, 'ms'), n = DateHelper.add(k.endDate, f, 'ms');
      return !a.currentOverClient.isDateRangeAvailable(l, n, b.assignmentRecords, j);
    });
  }
  async updateRecords(a) {
    const b = this, c = b.client, e = b.currentOverClient, f = b.mode === 'copy', {
        draggedEntities: g,
        timeDiff: i,
        initialAssignmentsState: j
      } = a, k = j[0].startDate, l = b.adjustStartDate(k, i);
    let n;
    if (!a.externalDropTarget) {
      if (!e.timeAxis.timeSpanInAxis(l, DateHelper.add(l, g[0].event.durationMS, 'ms'))) {
        a.valid = false;
      }
      if (a.valid) {
        c.eventStore.suspendAutoCommit();
        e.eventStore.suspendAutoCommit();
        n = await b.updateAssignments(c, e, a, f);
        c.eventStore.resumeAutoCommit();
        e.eventStore.resumeAutoCommit();
      }
    }
    if (a.valid) {
      e.trigger('eventDrop', Object.assign(b.getTriggerParams(a), {
        'isCopy': f,
        'copyMode': b.copyMode,
        'domEvent': a.browserEvent,
        'targetEventRecord': a.targetEventRecord,
        'targetResourceRecord': a.newResource,
        'externalDropTarget': a.externalDropTarget
      }));
    }
    return n;
  }
  async updateAssignments(a, b, c, e) {
    let f, g, j;
    const k = this, {copyMode: l} = k, n = a !== b, {isVertical: o} = b, {
        assignmentStore: p,
        eventStore: q
      } = a, {
        assignmentStore: r,
        eventStore: u
      } = b, v = a.isVertical ? a.resourceStore : a.store, {
        eventRecords: w,
        assignmentRecords: z,
        timeDiff: A,
        initialAssignmentsState: B,
        newResource: C
      } = c, {unifiedDrag: E} = k, F = u.usesSingleAssignment || u.usesSingleAssignment !== false && q.usesSingleAssignment, G = l === 'event' ? 'event' : l === 'assignment' ? 'assignment' : F ? 'event' : 'assignment', H = k.adjustStartDate(z[0].event.startDate, A), I = [], J = [], K = [], L = [], N = [], O = new Set();
    a.suspendRefresh();
    b.suspendRefresh();
    let P = false, Q = false;
    const R = k.getIndexDiff(c);
    if (o) {
      w.forEach((V, W) => {
        const X = c.eventBarEls[W];
        delete V.instanceMeta(a).hasTemporaryDragElement;
        if (X.dataset.transient) {
          X.remove();
        }
      });
    }
    const S = c.eventBarEls.slice(), T = [], U = {};
    for (let V = 0; V < z.length; V++) {
      const W = z[V];
      let X = W.event, Y;
      if (e) {
        Y = W.copy();
        U[W.id] = Y;
      } else {
        Y = W;
      }
      if (!Y.isOccurrenceAssignment && (!p.includes(W) || !q.includes(X))) {
        S[V].remove();
        S.splice(V, 1);
        z.splice(V, 1);
        V--;
        continue;
      }
      const Z = B[V], a0 = X, a1 = Z.startDate, a2 = Z.resource, a3 = this.constrainDragToTimeSlot ? a1 : E ? H : k.adjustStartDate(a1, A);
      if (!p.equals(r)) {
        const a7 = a0.assignments.length > 1 || e;
        let a8;
        if (e) {
          a8 = Y;
        } else {
          a8 = Y.copy();
          U[Y.id] = a8;
        }
        if (a8.event && !F) {
          a8.event = a8.event.id;
          a8.resource = a8.resource.id;
        }
        if (!e) {
          L.push(Y);
        }
        if (!a7) {
          J.push(a0);
        }
        if (e && (l === 'event' || l === 'auto' && u.usesSingleAssignment) || !u.getById(a0.id)) {
          X = u.createRecord({
            ...a0.data,
            'children': (f = a0.children) == null ? void 0 : f.map(a9 => a9.copy()),
            'id': e && (l === 'event' || l === 'auto') ? void 0 : a0.id,
            'calendar': null
          });
          a8.set({
            'eventId': X.id,
            'event': X
          });
          I.push(X);
        }
        if (!F) {
          K.push(a8);
        }
        Y = a8;
      }
      let a4 = C, a5 = null;
      if (!E) {
        a4 = k.getNewResource(c, a2, R) || C;
      }
      a4 = a4.$original;
      const a6 = Y.resourceId !== a4.id;
      if (a6) {
        a5 = v.getById(Y.resourceId);
        if (e && p.equals(r)) {
          Y.setData({
            'resource': null,
            'resourceId': null
          });
          Y.resource = a4;
          Y.event = u.getById(Y.eventId);
          const a9 = l === 'event' || q.usesSingleAssignment && l === 'auto';
          if (a9) {
            X = X.copy();
            X.meta.endDateCached = k.adjustStartDate(X.endDate, A);
            X.endDate = null;
            Y.event = X;
            if (u.usesSingleAssignment) {
              X.resource = a4;
              X.resourceId = a4.id;
            }
          }
          if (!r.find(aa => aa.eventId === Y.eventId && aa.resourceId === Y.resourceId) && !K.find(aa => aa.eventId === Y.eventId && aa.resourceId === Y.resourceId)) {
            a9 && I.push(X);
            K.push(Y);
          }
        } else {
          Y.resource = a4;
        }
        X.isEvent && O.add(X);
        P = true;
        if (X.isOccurrence) {
          X.set('newResource', a4);
        }
        if (n && F) {
          X.resourceId = a4.id;
        }
      } else {
        if (e && (l === 'event' || l === 'auto' && q.usesSingleAssignment) && !I.includes(X)) {
          X = X.copy();
          X.meta.endDateCached = k.adjustStartDate(X.endDate, A);
          X.endDate = null;
          I.push(X);
          Y.event = X;
          if (u.usesSingleAssignment) {
            X.set({
              'resource': a4,
              'resourceId': a4.id
            });
          }
          K.push(Y);
        }
      }
      if (!N.find(aa => aa.draggedEvent === X) && !DateHelper.isEqual(X.startDate, a3)) {
        while (!X.isOccurrence && X.isBatchUpdating) {
          X.endBatch(true);
        }
        const aa = e && !n && !F && G === 'assignment' && a6;
        if (!aa) {
          X.startDate = a3;
          N.push({
            'draggedEvent': X,
            'originalStartDate': a1
          });
        }
        X.isEvent && O.add(X);
        Q = true;
      }
      b.processEventDrop({
        'eventRecord': X,
        'resourceRecord': a4,
        'element': V === 0 ? c.context.element : c.context.relatedElements[V - 1],
        'context': c,
        'toScheduler': b,
        'reassignedFrom': a5,
        'eventsToAdd': I,
        'addedEvents': T,
        'draggedAssignment': Y
      });
      b.trigger('processEventDrop', {
        'originalAssignment': W,
        'draggedAssignment': Y,
        'context': c,
        'copyMode': l,
        'isCopy': e
      });
    }
    p.remove(L);
    q.remove(J);
    r.add(K);
    if (e && p.equals(r)) {
      const {syncIdMap: ab} = a.foregroundCanvas;
      Object.entries(U).forEach(([ac, ad]) => {
        const ae = ab[ac];
        delete ab[ac];
        ab[ad.id] = ae;
      });
    }
    I.length && T.push(...u.add(I));
    T == null ? void 0 : T.forEach(ac => O.add(ac));
    if (L.length || J.length || K.length || I.length) {
      P = true;
    }
    if (P || Q) {
      if (!k.constrainDragToTimeline) {
        for (let ae = 0; ae < z.length; ae++) {
          const af = U[z[ae].id] || z[ae], ag = af.event, ah = (T == null ? void 0 : T.find(al => al.id === ag.id)) || ag, ai = c.eventBarEls[ae], aj = ae === 0 ? c.context.element : c.context.relatedElements[ae - 1], ak = b.isInTimeAxis(ah);
          delete ah.meta.endDateCached;
          if (!e) {
            DomSync.removeChild(ai.parentElement, ai);
          }
          if (ah.resource && (o || b.rowManager.getRowFor(ah.resource)) && ak) {
            if (!ah.parent || ah.parent.isRoot) {
              const al = Rectangle.from(aj, b.foregroundCanvas, true), am = b.rtl ? b.timeAxisViewModel.totalSize - al.right : al.x;
              DomHelper.setTopInsetInlineStart(aj, al.y, am);
              DomSync.addChild(b.foregroundCanvas, aj, ah.assignments[0].id);
              n && b.processCrossSchedulerEventDrop({
                'eventRecord': ah,
                'toScheduler': b
              });
            }
            aj.classList.remove('b-sch-event-hover', 'b-active', 'b-drag-proxy', 'b-dragging');
            aj.retainElement = false;
          }
        }
      }
      F && O.forEach(an => an.beginBatch());
      const ac = (g = u.$master.project) != null ? g : b.project, ad = (j = q.$master.project) != null ? j : a.project;
      await Promise.all([
        ac !== ad ? ac.commitAsync() : null,
        ad.commitAsync()
      ]);
      F && O.forEach(an => an.endBatch(false, true));
    }
    if (!P) {
      P = N.some(({
        draggedEvent: an,
        originalStartDate: ao
      }) => !DateHelper.isEqual(an.startDate, ao));
    }
    b.resumeRefresh(false);
    a.resumeRefresh(false);
    if (z.length > 0) {
      if (!P) {
        c.valid = false;
      } else {
        S.forEach(an => delete an.lastDomConfig);
        b.refreshWithTransition();
        if (n) {
          a.refreshWithTransition();
          b.selectedEvents = T;
        }
      }
    }
  }
  getProductDragContext(a) {
    const b = this, {currentOverClient: c} = b, e = a.browserEvent.target, f = a.newResource || a.resourceRecord, g = a.targetEventRecord;
    let i = c ? b.resolveEventRecord(e, c) : null, j, k;
    if (a.eventRecords.includes(i)) {
      i = null;
    }
    if (b.constrainDragToResource) {
      j = a.resourceRecord;
    } else if (!b.constrainDragToTimeline) {
      j = b.resolveResource();
    } else if (c) {
      j = b.resolveResource() || a.newResource || a.resourceRecord;
    }
    const {
        assignmentRecords: l,
        eventRecords: n
      } = a, o = f !== j;
    let p = Boolean(j && !j.isSpecialRow);
    if (!j && b.externalDropTargetSelector) {
      k = e.closest(b.externalDropTargetSelector);
      p = Boolean(k);
    }
    return {
      'valid': p,
      'externalDropTarget': k,
      'eventRecords': n,
      'assignmentRecords': l,
      'newResource': j,
      'targetEventRecord': i,
      'dirty': o || i !== g,
      'proxyElements': [
        a.context.element,
        ...a.context.relatedElements || []
      ]
    };
  }
  getMinimalDragData(a) {
    const b = this, {scheduler: c} = b, e = b.getElementFromContext(a), f = b.resolveEventRecord(e, c), g = c.resolveResourceRecord(e), i = c.resolveAssignmentRecord(e);
    let j = i ? [i] : [];
    if (i && (c.isAssignmentSelected(j[0]) || b.drag.startEvent.ctrlKey && c.multiEventSelect)) {
      j.push.apply(j, b.getRelatedRecords(i));
    }
    j = j.filter(l => c.resourceStore.isAvailable(l.resource));
    const k = [...new Set(j.map(l => l.event))];
    return {
      'eventRecord': f,
      'resourceRecord': g,
      'assignmentRecord': i,
      'eventRecords': k,
      'assignmentRecords': j
    };
  }
  setupProductDragData(a) {
    let b;
    const c = this, {scheduler: e} = c, f = c.getElementFromContext(a), {
        eventRecord: g,
        resourceRecord: i,
        assignmentRecord: j,
        assignmentRecords: k
      } = c.getMinimalDragData(a), l = [];
    if (c.constrainDragToResource && !i) {
      throw new Error('Resource could not be resolved for event: ' + g.id);
    }
    let n;
    if (c.constrainDragToTimeline) {
      n = (b = c.getDateConstraints) == null ? void 0 : b.call(c, i, g);
      const o = c.constrainRectangle = c.getConstrainingRectangle(n, i, g), p = Rectangle.from(f, e.timeAxisSubGridElement), q = Boolean(n.start);
      super.setupConstraints(o, p, e.timeAxisViewModel.snapPixelAmount, q && e.isHorizontal, q && e.isVertical);
    }
    k.forEach(r => {
      let u = e.getElementFromAssignmentRecord(r, true);
      if (!u) {
        u = e.currentOrientation.addTemporaryDragElement(r.event, r.resource);
      }
      l.push(u);
    });
    return {
      'record': j,
      'draggedEntities': k,
      'dateConstraints': (n == null ? void 0 : n.start) ? n : null,
      'eventBarCopies': l.map(r => c.createProxy(r)),
      'eventBarEls': l
    };
  }
  getDateConstraints(a, b) {
    let c;
    const {scheduler: e} = this, f = (c = e.getDateConstraints) == null ? void 0 : c.call(e, a, b);
    let g, i;
    if (this.constrainDragToTimeSlot) {
      g = b.startDate;
      i = b.endDate;
    } else if (f) {
      g = f.start;
      i = f.end;
    }
    return {
      'start': g,
      'end': i
    };
  }
  getConstrainingRectangle(a, b, c) {
    return this.scheduler.getScheduleRegion(this.constrainDragToResource && b, c, true, a && {
      'start': a.start,
      'end': a.end
    });
  }
  getDragData(a) {
    const b = this.getMinimalDragData(a) || {};
    return {
      ...super.getDragData(a),
      ...b,
      'initialAssignmentsState': b.assignmentRecords.map(c => ({
        'startDate': c.event.startDate,
        'resource': c.resource,
        'assignment': c
      }))
    };
  }
  getRelatedRecords(a) {
    return this.scheduler.selectedAssignments.filter(b => b !== a && !b.resource.readOnly && b.event.isDraggable);
  }
  getCoordinate(a, b, c) {
    const e = this.currentOverClient;
    if (e.isHorizontal) {
      let f = c[0];
      if (e.milestoneLayoutMode !== 'default' && a.isMilestone) {
        switch (e.milestoneAlign) {
        case 'center':
          f += b.offsetWidth / 2;
          break;
        case 'end':
          f += b.offsetWidth;
          break;
        }
      }
      return f;
    } else {
      let g = c[1];
      if (e.milestoneLayoutMode !== 'default' && a.isMilestone) {
        switch (e.milestoneAlign) {
        case 'center':
          g += b.offsetHeight / 2;
          break;
        case 'end':
          g += b.offsetHeight;
          break;
        }
      }
      return g;
    }
  }
  resolveResource() {
    const a = this, b = a.currentOverClient, {isHorizontal: c} = b, {
        context: e,
        browserEvent: f,
        dragProxy: g
      } = a.dragData, i = g || e.element, j = Rectangle.from(i, null, true), k = b.isVertical || a.unifiedDrag ? e.clientY : j.center.y, l = Rectangle.from(i, b.timeAxisSubGridElement, true), {
        x: n,
        y: o
      } = l.center;
    let p = null, q;
    if (f.touches) {
      q = Rectangle.from(b.element).contains(Point.from(f, true));
    } else {
      q = b.element.contains(a.getMouseMoveEventTarget(f));
    }
    if (q) {
      if (c) {
        const r = b.rowManager.getRowAt(k);
        p = r && b.store.storage.getAt(r.dataIndex);
      } else {
        p = b.resolveResourceRecord(b.timeAxisSubGridElement.querySelector('.b-sch-timeaxis-cell'), [
          n,
          o
        ]);
      }
    }
    return p == null ? void 0 : p.$original;
  }
  getRecordElement(a) {
    return this.client.getElementFromAssignmentRecord(a, true);
  }
  getProxyElement(a) {
    let b;
    const {dragData: c} = this;
    if (this.isDragging && ((b = c == null ? void 0 : c.proxyElements) == null ? void 0 : b.length)) {
      const e = c.assignmentRecords.indexOf(a);
      if (e >= 0) {
        return c.proxyElements[e];
      }
    }
    return null;
  }
  getMouseMoveEventTarget(a) {
    return a.target;
  }
};
EventDrag._$name = 'EventDrag';
GridFeatureManager.registerFeature(EventDrag, true, 'Scheduler');
GridFeatureManager.registerFeature(EventDrag, false, 'ResourceHistogram');
const EventDragCreate = class extends DragCreateBase {
  static configurable = { 'validatorFn': () => true };
  static $name = 'EventDragCreate';
  get scheduler() {
    return this.client;
  }
  get store() {
    return this.client.eventStore;
  }
  get project() {
    return this.client.project;
  }
  updateLockLayout(a) {
    this.dragActiveCls = 'b-dragcreating' + (a ? ' b-dragcreate-lock' : '');
  }
  handleBeforeDragCreate(a, b, c) {
    let e;
    const f = this, {scheduler: g} = f, {resourceRecord: i} = a;
    if (f.disabled || i.readOnly || !f.scheduler.resourceStore.isAvailable(i)) {
      return false;
    }
    if (!g.isDateRangeAvailable(a.mousedownDate, a.mousedownDate, null, i)) {
      return false;
    }
    const j = !g.isSchedulerPro || b.ignoreResourceCalendar || i.isWorkingTime(a.mousedownDate), k = j && g.trigger('beforeDragCreate', {
        'resourceRecord': i,
        'date': a.mousedownDate,
        'event': c
      });
    f.dateConstraints = (e = g.getDateConstraints) == null ? void 0 : e.call(g, i, b);
    return k;
  }
  dragStart(a) {
    let b;
    const c = this, {client: e} = c, {
        eventStore: f,
        assignmentStore: g,
        enableEventAnimations: i,
        enableTransactionalFeatures: j
      } = e, {resourceRecord: k} = a, l = c.createEventRecord(a), n = [k];
    l.set('duration', DateHelper.diff(l.startDate, l.endDate, l.durationUnit, true));
    l.isCreating = true;
    l.meta.isDragCreating = true;
    e.features.taskEdit && e.features.taskEdit.doCancel();
    if (c.handleBeforeDragCreate(a, l, a.event) === false) {
      return false;
    }
    c.captureStm(true);
    let o = [];
    if (k) {
      if (f.usesSingleAssignment || !j) {
        o = g.assignEventToResource(l, k);
      } else {
        o = [g.createRecord({
            'event': l,
            'resource': k
          })];
      }
    }
    if (e.trigger('beforeEventAdd', {
        'eventRecord': l,
        'resourceRecords': n,
        'assignmentRecords': o
      }) === false) {
      if (f.usesSingleAssignment || !j) {
        g.remove(o);
      }
      return false;
    }
    if (c.lockLayout) {
      l.meta.excludeFromLayout = true;
    }
    (b = e.onEventCreated) == null ? void 0 : b.call(e, l);
    e.enableEventAnimations = false;
    const p = f.add(l);
    this.project.commitAsync().then(() => e.enableEventAnimations = i);
    if (!p) {
      if (f.usesSingleAssignment || !j) {
        g.remove(o);
      }
      return false;
    }
    if (!f.usesSingleAssignment && j) {
      g.add(o[0]);
    }
    e.isCreating = true;
    if (e.isHorizontal) {
      const q = e.store.getById(k);
      e.rowManager.renderRows([e.getRowFor(q)]);
    } else {
      e.refreshRows();
    }
    e.isCreating = false;
    a.itemElement = a.element = e.getElementFromEventRecord(l);
    if (!DomHelper.isInView(a.itemElement)) {
      e.scrollable.scrollIntoView(a.itemElement, {
        'animate': true,
        'edgeOffset': e.barMargin
      });
    }
    return super.dragStart(a);
  }
  checkValidity(a, b) {
    const c = this, {client: e} = c, f = a.resourceRecord = c.dragging.resourceRecord;
    return e.isDateRangeAvailable(a.startDate, a.endDate, a.eventRecord, f) && c.createValidatorFn.call(c.validatorFnThisObj || c, a, b);
  }
  isRowEmpty(a) {
    const b = this.store.getEventsForResource(a);
    return !b || !b.length;
  }
  createEventRecord(a) {
    const b = this, {client: c} = b, e = c.isHorizontal ? 'X' : 'Y', {
        timeAxis: f,
        eventStore: g,
        weekStartDay: i
      } = c, {
        event: j,
        mousedownDate: k
      } = a, l = b.draggingEnd = j['page' + e] > a.startEvent['page' + e], n = {
        'name': g.modelClass.fieldMap.name.defaultValue || b.L('L{Object.newEvent}'),
        'startDate': l ? DateHelper.floor(k, f.resolution, null, i) : k,
        'endDate': l ? k : DateHelper.ceil(k, f.resolution, null, i)
      };
    if (c.project.isGanttProjectMixin) {
      ObjectHelper.assign(n, {
        'constraintDate': n.startDate,
        'constraintType': 'startnoearlierthan'
      });
    }
    return g.createRecord(n);
  }
  async internalUpdateRecord(a, b) {
    await super.internalUpdateRecord(a, b);
    if (!this.client.hasEventEditor) {
      a.eventRecord.isCreating = false;
    }
  }
  async finalizeDragCreate(a) {
    const {meta: b} = a.eventRecord;
    b.excludeFromLayout = false;
    b.isDragCreating = false;
    const c = await super.finalizeDragCreate(a);
    if (!c) {
      await this.freeStm(true);
    } else {
      this.hasStmCapture = false;
    }
    return c;
  }
  async cancelDragCreate(a) {
    await super.cancelDragCreate(a);
    await this.freeStm(false);
  }
  getTipHtml(...a) {
    const b = super.getTipHtml(...a), {element: c} = this.tip;
    c.classList.add('b-sch-dragcreate-tooltip');
    c.classList.toggle('b-too-narrow', this.dragging.context.tooNarrow);
    return b;
  }
  onAborted(a) {
    let b, c;
    const {
      eventRecord: e,
      resourceRecord: f
    } = a.eventContexts[0];
    (c = (b = this.store).unassignEventFromResource) == null ? void 0 : c.call(b, e, f);
    this.store.remove(e);
  }
};
EventDragCreate._$name = 'EventDragCreate';
GridFeatureManager.registerFeature(EventDragCreate, true, 'Scheduler');
GridFeatureManager.registerFeature(EventDragCreate, false, 'ResourceHistogram');
const EventDragSelect = class extends Delayable_default(InstancePlugin) {
  static $name = 'EventDragSelect';
  targetSelector = '.b-sch-timeaxis-cell, .b-timeaxissubgrid';
  constructor() {
    super(...arguments);
  }
  construct(a, b) {
    a.multiEventSelect = true;
    super.construct(a, b);
  }
  static get pluginConfig() {
    return {
      'chain': [
        'onElementMouseDown',
        'onElementMouseMove'
      ]
    };
  }
  onElementMouseDown(a) {
    const b = this, {client: c} = b, {foregroundCanvas: e} = c, {
        target: f,
        button: g,
        touches: i,
        clientX: j,
        clientY: k
      } = a, l = Rectangle.from(e, true);
    if (i || g !== 0 || b.disabled || !f.matches(b.targetSelector) || c.trigger('beforeEventDragSelect', { 'event': a }) === false) {
      return;
    }
    c.preventDragSelect = true;
    b.startX = j - l.x;
    b.startY = k - l.y;
    b.element = DomHelper.createElement({
      'tag': 'div',
      'className': 'b-dragselect-rect',
      'parent': c.foregroundCanvas,
      'retainElement': true,
      'style': { 'transform': 'translate(' + b.startX + 'px, ' + b.startY + 'px)' }
    });
    c.navigator.disabled = true;
    b.mouseUpDetacher = EventHelper.on({
      'element': DomHelper.getRootElement(c.element),
      'mouseup': 'onDocumentMouseUp',
      'thisObj': b
    });
  }
  get eventRectangles() {
    const {client: a} = this, b = Array.from(a.foregroundCanvas.children).filter(c => c.matches(a.eventSelector + ', .b-nested-events-container'));
    return b.map(c => {
      const e = a.resolveAssignmentRecord(c);
      return {
        'rectangle': Rectangle.from(c, true),
        'record': e,
        'selected': a.selectedAssignments.includes(e)
      };
    });
  }
  onElementMouseMove(a) {
    const b = this, {client: c} = b;
    if (typeof b.startX === 'number') {
      const e = b.rectangle = Rectangle.from(b.client.foregroundCanvas, true), f = Math.min(Math.max(a.clientX - e.x, 0), e.width + 1), g = Math.min(Math.max(a.clientY - e.y, 0), e.height + 1), i = new Rectangle(b.startX, b.startY, f - b.startX, g - b.startY);
      DomHelper.setTranslateXY(b.element, i.x, i.y);
      b.element.style.width = i.width + 'px';
      b.element.style.height = i.height + 'px';
      if (!b.dragging) {
        c.element.classList.add('b-dragselecting');
        if (!a.ctrlKey && !a.metaKey) {
          c.clearEventSelection();
        }
        b.originalSelection = c.selectedAssignments.slice();
        b.subGridElementRect = Rectangle.from(c.timeAxisSubGridElement, true);
        c.navigator.disabled = true;
        c.enableScrollingCloseToEdges(c.timeAxisSubGrid);
        b.dragging = true;
      }
      b.updateSelection();
    }
  }
  onDocumentMouseUp(a) {
    let b, c;
    const e = this, {client: f} = e, {
        selectedAssignments: g,
        navigator: i
      } = f, j = Rectangle.from(f.timeAxisSubGridElement);
    f.disableScrollingCloseToEdges(f.timeAxisSubGrid);
    if (!e.dragging && f.deselectAllOnScheduleClick) {
      f.clearEventSelection();
    }
    (b = e.element) == null ? void 0 : b.remove();
    f.element.classList.remove('b-dragselecting');
    e.startX = e.startY = null;
    i.skipNextClick = j.contains(Point.from(a, true));
    i.disabled = false;
    e.dragging = false;
    if (g.length) {
      i.skipScrollIntoView = true;
      f.activeAssignment = g[g.length - 1];
      (c = i.activeItem) == null ? void 0 : c.focus();
      i.skipScrollIntoView = false;
    }
    e.mouseUpDetacher();
  }
  updateSelection() {
    const a = this, b = a.rectangle = Rectangle.from(a.element, true), {
        eventRectangles: c,
        client: e
      } = a, f = e.selectedAssignments.reduce((g, i) => {
        if (!e.getElementFromAssignmentRecord(i)) {
          g.push(i);
        }
        return g;
      }, []);
    for (const g of c) {
      if (b.intersect(g.rectangle, true, true)) {
        g.selected = true;
        f.push(g.record);
      }
    }
    f.push(...a.originalSelection);
    e.selectedAssignments = f;
  }
};
EventDragSelect._$name = 'EventDragSelect';
GridFeatureManager.registerFeature(EventDragSelect, false, 'Scheduler');
const EventEditor = class extends Popup {
  static configurable = {
    'items': [],
    'draggable': { 'handleSelector': ':not(button,.b-field-inner)' },
    'axisLock': 'flexible',
    'scrollable': { 'overflowY': true },
    'readOnly': null,
    'titleRenderer': null,
    'maximizeOnMobile': true,
    'internalListeners': { 'beforeShow': 'onInternalBeforeShow' }
  };
  static $name = 'EventEditor';
  static type = 'eventeditor';
  revertFocus() {
    let a, b, c;
    const {owner: e} = this, f = (a = this.focusInEvent) == null ? void 0 : a.relatedTarget;
    if (f && !DomHelper.isFocusable(f) && e.isTimelineBase) {
      (c = (b = e.focusedCell) == null ? void 0 : b.cell) == null ? void 0 : c.focus();
    }
    super.revertFocus();
  }
  updateLocalization() {
    super.updateLocalization(...arguments);
    this.initialTitle = this.title || '';
  }
  chainResourceStore() {
    return this.eventEditFeature.resourceStore.chain(a => !a.isSpecialRow, null, {
      'storeClass': Store,
      'excludeCollapsedRecords': false
    });
  }
  processWidgetConfig(a) {
    let b;
    if (((b = a.type) == null ? void 0 : b.includes('date')) && a.weekStartDay == null) {
      a.weekStartDay = this.weekStartDay;
    }
    if (a.type === 'extraItems') {
      return false;
    }
    const {eventEditFeature: c} = this, e = {};
    if (a.ref === 'resourceField') {
      const {store: f} = a;
      a.store = this.chainResourceStore();
      if (f) {
        a.store.setConfig(f);
      }
      if (!('multiSelect' in a)) {
        a.multiSelect = !c.eventStore.usesSingleAssignment;
      }
    }
    if ((a.name === 'startDate' || a.name === 'endDate') && a.type === 'date') {
      e.format = c.dateFormat;
    }
    if ((a.name === 'startDate' || a.name === 'endDate') && a.type === 'time') {
      e.format = c.timeFormat;
    }
    Object.assign(a, e);
    return super.processWidgetConfig(a);
  }
  setupEditorButtons() {
    const {record: a} = this, {deleteButton: b} = this.widgetMap;
    if (b) {
      b.hidden = this.readOnly || a.isCreating;
    }
  }
  onInternalBeforeShow(...a) {
    this.setupUIForEditing();
  }
  onBeforeToggleReveal({reveal: a}) {
    if (a) {
      this.setupUIForEditing();
    }
  }
  setupUIForEditing() {
    const a = this, {
        record: b,
        titleRenderer: c
      } = a;
    a.setupEditorButtons();
    if (c) {
      a.title = a.callback(c, a, [b]);
    } else {
      a.title = a.initialTitle;
    }
  }
  onInternalKeyDown(a) {
    this.trigger('keyDown', { 'event': a });
    if (a.key === 'Escape') {
      a.stopImmediatePropagation();
      this.eventEditFeature.onCancelClick();
    } else {
      super.onInternalKeyDown(a);
    }
  }
  close(a, b, c) {
    if (c == null ? void 0 : c.isTool) {
      this.eventEditFeature.onCancelClick();
      return;
    }
    return super.close(...arguments);
  }
  updateReadOnly(a) {
    const {
      deleteButton: b,
      saveButton: c,
      cancelButton: e
    } = this.widgetMap;
    super.updateReadOnly(a);
    if (b) {
      b.hidden = a;
    }
    if (c) {
      c.hidden = a;
    }
    if (e) {
      e.hidden = a;
    }
  }
};
EventEditor.initClass();
EventEditor._$name = 'EventEditor';
const RecurrenceFrequencyCombo = class extends Combo {
  static configurable = {
    'editable': false,
    'displayField': 'text',
    'valueField': 'value',
    'localizeDisplayFields': true,
    'addNone': false,
    'picker': { 'align': { 'matchSize': 'min' } }
  };
  static type = 'recurrencefrequencycombo';
  static $name = 'RecurrenceFrequencyCombo';
  buildItems() {
    return [
      ...this.addNone ? [{
          'text': 'L{None}',
          'value': 'NONE'
        }] : [],
      {
        'value': 'DAILY',
        'text': 'L{Daily}'
      },
      {
        'value': 'WEEKLY',
        'text': 'L{Weekly}'
      },
      {
        'value': 'MONTHLY',
        'text': 'L{Monthly}'
      },
      {
        'value': 'YEARLY',
        'text': 'L{Yearly}'
      }
    ];
  }
};
RecurrenceFrequencyCombo.initClass();
RecurrenceFrequencyCombo._$name = 'RecurrenceFrequencyCombo';
const RecurrenceCombo = class extends RecurrenceFrequencyCombo {
  static get $name() {
    return 'RecurrenceCombo';
  }
  static get type() {
    return 'recurrencecombo';
  }
  static get defaultConfig() {
    return {
      'customValue': 'custom',
      'placeholder': 'None',
      'splitCls': 'b-recurrencecombo-split',
      'items': true,
      'highlightExternalChange': false
    };
  }
  buildItems() {
    const a = this;
    return [
      {
        'value': 'none',
        'text': 'L{None}'
      },
      ...super.buildItems(),
      {
        'value': a.customValue,
        'text': 'L{Custom}',
        'cls': a.splitCls
      }
    ];
  }
  set value(a) {
    a = a || 'none';
    super.value = a;
  }
  get value() {
    return super.value;
  }
  set recurrence(a) {
    const b = this;
    if (a) {
      b.value = b.isCustomRecurrence(a) ? b.customValue : a.frequency;
    } else {
      b.value = null;
    }
  }
  isCustomRecurrence(a) {
    const {
      interval: b,
      days: c,
      monthDays: e,
      months: f
    } = a;
    return Boolean(b > 1 || c && c.length || e && e.length || f && f.length);
  }
};
RecurrenceCombo.initClass();
RecurrenceCombo._$name = 'RecurrenceCombo';
const RecurrenceLegendButton = class extends Button {
  static get $name() {
    return 'RecurrenceLegendButton';
  }
  static get type() {
    return 'recurrencelegendbutton';
  }
  static get defaultConfig() {
    return {
      'localizableProperties': [],
      'recurrence': null
    };
  }
  set recurrence(a) {
    this._recurrence = a;
    this.updateLegend();
  }
  get recurrence() {
    return this._recurrence;
  }
  set eventStartDate(a) {
    this._eventStartDate = a;
    this.updateLegend();
  }
  get eventStartDate() {
    return this._eventStartDate;
  }
  updateLegend() {
    const {recurrence: a} = this;
    this.text = a ? RecurrenceLegend.getLegend(a, this.eventStartDate) : '';
  }
  onLocaleChange() {
    this.updateLegend();
  }
  updateLocalization() {
    this.onLocaleChange();
    super.updateLocalization();
  }
};
RecurrenceLegendButton.initClass();
RecurrenceLegendButton._$name = 'RecurrenceLegendButton';
const RecurrenceDaysCombo = class extends Combo {
  static type = 'recurrencedayscombo';
  static $name = 'RecurrenceDaysCombo';
  static get defaultConfig() {
    const a = [
        'SU',
        'MO',
        'TU',
        'WE',
        'TH',
        'FR',
        'SA'
      ], b = a.join(',');
    return {
      'allDaysValue': b,
      'editable': false,
      'defaultValue': b,
      'workingDaysValue': a.filter((c, e) => !DateHelper.nonWorkingDays[e]).join(','),
      'nonWorkingDaysValue': a.filter((c, e) => DateHelper.nonWorkingDays[e]).join(','),
      'splitCls': 'b-recurrencedays-split',
      'displayField': 'text',
      'valueField': 'value'
    };
  }
  buildItems() {
    const a = this;
    a._weekDays = null;
    return a.weekDays.concat([
      {
        'value': a.allDaysValue,
        'text': a.L('L{day}'),
        'cls': a.splitCls
      },
      {
        'value': a.workingDaysValue,
        'text': a.L('L{weekday}')
      },
      {
        'value': a.nonWorkingDaysValue,
        'text': a.L('L{weekend day}')
      }
    ]);
  }
  get weekDays() {
    const a = this;
    if (!a._weekDays) {
      const b = DateHelper.weekStartDay;
      const c = DateHelper.getDayNames().map((e, f) => ({
        'text': e,
        'value': RecurrenceDayRuleEncoder.encodeDay(f)
      }));
      a._weekDays = c.slice(b).concat(c.slice(0, b));
    }
    return a._weekDays;
  }
  set value(a) {
    const b = this;
    if (a && Array.isArray(a)) {
      a = a.join(',');
    }
    if (!a || !b.store.findRecord('value', a)) {
      a = b.defaultValue;
    }
    super.value = a;
  }
  get value() {
    let a = super.value;
    if (a && Array.isArray(a)) {
      a = a.join(',');
    }
    return a;
  }
};
RecurrenceDaysCombo.initClass();
RecurrenceDaysCombo._$name = 'RecurrenceDaysCombo';
const DayButtons = class extends ButtonGroup {
  static configurable = {
    'pressedCls': 'b-raised b-blue',
    'dayNameLength': 1,
    'separator': ',',
    'defaults': { 'toggleable': true }
  };
  static defaultConfig = {
    'value': null,
    'items': true
  };
  static type = 'daybuttons';
  static $name = 'DayButtons';
  updateItemText(a) {
    const b = RecurrenceDayRuleEncoder.decodeDay(a.value)[0], c = DateHelper.getDayName(b);
    a.text = c.substring(0, this.dayNameLength);
    a.tooltip = c;
  }
  changeItems(a) {
    if (a === true) {
      const b = DateHelper.weekStartDay, c = DateHelper.getDayNames().map((e, f) => ({
          'text': e.substring(0, this.dayNameLength),
          'tooltip': e,
          'value': RecurrenceDayRuleEncoder.encodeDay(f),
          'pressedCls': this.pressedCls,
          'dataset': { 'dayIndex': f }
        }));
      a = c.slice(b).concat(c.slice(0, b));
      a = super.changeItems(a);
    }
    return a;
  }
  set value(a) {
    if ((a == null ? void 0 : a.length) > 0 && typeof a[0] === 'number') {
      a = a.map(b => RecurrenceDayRuleEncoder.encodeDay(b));
    }
    if (a && Array.isArray(a)) {
      a = a.join(this.separator);
    }
    super.value = a;
  }
  get value() {
    let a = super.value;
    if (a && Array.isArray(a)) {
      a = a.join(this.separator);
    }
    return a;
  }
  get valueAsDayNumbers() {
    return this.value ? this.value.split(this.separator).map(a => RecurrenceDayRuleEncoder.decodeDay(a)[0]) : [];
  }
  onLocaleChange() {
    this.items.forEach(this.updateItemText, this);
  }
  updateLocalization() {
    this.onLocaleChange();
    super.updateLocalization();
  }
};
DayButtons.initClass();
DayButtons._$name = 'DayButtons';
const RecurrenceDaysButtonGroup = class extends DayButtons {
  static type = 'recurrencedaysbuttongroup';
  static $name = 'RecurrenceDaysButtonGroup';
};
RecurrenceDaysButtonGroup.initClass();
RecurrenceDaysButtonGroup._$name = 'RecurrenceDaysButtonGroup';
const RecurrenceMonthDaysButtonGroup = class extends ButtonGroup {
  static configurable = { 'defaults': { 'toggleable': true } };
  static type = 'recurrencemonthdaysbuttongroup';
  static $name = 'RecurrenceMonthDaysButtonGroup';
  get minValue() {
    return 1;
  }
  get maxValue() {
    return 31;
  }
  construct(a = {}) {
    a.items = this.buildItems();
    super.construct(a);
  }
  buildItems() {
    const a = [];
    for (let b = this.minValue; b <= this.maxValue; b++) {
      a.push({
        'text': b + '',
        'value': b
      });
    }
    return a;
  }
};
RecurrenceMonthDaysButtonGroup.initClass();
RecurrenceMonthDaysButtonGroup._$name = 'RecurrenceMonthDaysButtonGroup';
const RecurrenceMonthsButtonGroup = class extends ButtonGroup.mixin(Validatable_default) {
  static configurable = { 'defaults': { 'toggleable': true } };
  static type = 'recurrencemonthsbuttongroup';
  static $name = 'RecurrenceMonthsButtonGroup';
  construct(a = {}) {
    a.items = this.buildItems();
    super.construct(a);
  }
  buildItems() {
    return DateHelper.getMonthNames().map((a, b) => ({
      'text': a.substring(0, 3),
      'value': b + 1
    }));
  }
  updateItemText(a) {
    a.text = DateHelper.getMonthName(a.value - 1).substring(0, 3);
  }
  onLocaleChange() {
    this.items.forEach(this.updateItemText, this);
  }
  updateLocalization() {
    this.onLocaleChange();
    super.updateLocalization();
  }
};
RecurrenceMonthsButtonGroup.initClass();
RecurrenceMonthsButtonGroup._$name = 'RecurrenceMonthsButtonGroup';
const RecurrenceStopConditionCombo = class extends Combo {
  static configurable = {
    'editable': false,
    'placeholder': 'Never',
    'displayField': 'text',
    'valueField': 'value'
  };
  static type = 'recurrencestopconditioncombo';
  static $name = 'RecurrenceStopConditionCombo';
  buildItems() {
    return [
      {
        'value': 'never',
        'text': this.L('L{Never}')
      },
      {
        'value': 'count',
        'text': this.L('L{After}')
      },
      {
        'value': 'date',
        'text': this.L('L{On date}')
      }
    ];
  }
  set value(a) {
    a = a || 'never';
    super.value = a;
  }
  get value() {
    return super.value;
  }
  get recurrence() {
    return this._recurrence;
  }
  set recurrence(a) {
    let b = null;
    if (a.endDate) {
      b = 'date';
    } else if (a.count) {
      b = 'count';
    }
    this._recurrence = a;
    this.value = b;
  }
};
RecurrenceStopConditionCombo.initClass();
RecurrenceStopConditionCombo._$name = 'RecurrenceStopConditionCombo';
const RecurrencePositionsCombo = class extends Combo {
  static get $name() {
    return 'RecurrencePositionsCombo';
  }
  static get type() {
    return 'recurrencepositionscombo';
  }
  static get defaultConfig() {
    return {
      'editable': false,
      'splitCls': 'b-recurrencepositions-split',
      'displayField': 'text',
      'valueField': 'value',
      'defaultValue': 1,
      'maxPosition': 5
    };
  }
  buildItems() {
    return Store.new({
      'fields': [
        'value',
        'text',
        'cls'
      ],
      'data': this.buildDayNumbers().concat([{
          'value': '-1',
          'text': this.L('L{position-1}'),
          'cls': this.splitCls
        }])
    });
  }
  buildDayNumbers() {
    return ArrayHelper.populate(this.maxPosition, a => ({
      'value': a + 1,
      'text': this.L('position' + (a + 1))
    }));
  }
  set value(a) {
    if (a && Array.isArray(a)) {
      a = a.join(',');
    }
    if (!a || !this.store.findRecord('value', a)) {
      a = this.defaultValue;
    }
    super.value = a;
  }
  get value() {
    const a = super.value;
    return a ? ('' + a).split(',').map(b => parseInt(b, 10)) : [];
  }
};
RecurrencePositionsCombo.initClass();
RecurrencePositionsCombo._$name = 'RecurrencePositionsCombo';
const RecurrenceEditorPanel = class extends Panel {
  static configurable = {
    'focusable': false,
    'cls': 'b-recurrenceeditor',
    'record': false,
    'addNone': false,
    'strictRecordMapping': true,
    'items': {
      'frequencyField': {
        'type': 'recurrencefrequencycombo',
        'name': 'frequency',
        'label': 'L{RecurrenceEditor.Frequency}',
        'weight': 10,
        'onChange': 'up.onFrequencyFieldChange',
        'addNone': 'up.addNone'
      },
      'intervalField': {
        'type': 'numberfield',
        'weight': 15,
        'name': 'interval',
        'label': 'L{RecurrenceEditor.Every}',
        'min': 1,
        'required': true
      },
      'daysLabel': {
        'type': 'label',
        'text': 'L{RecurrenceEditor.Days}',
        'weight': 18,
        'forFrequency': 'WEEKLY',
        'cls': 'b-button-group-label'
      },
      'daysButtonField': {
        'type': 'daybuttons',
        'cls': 'b-recurrencedaysbuttongroup',
        'weight': 20,
        'name': 'days',
        'forFrequency': 'WEEKLY',
        'useGap': true,
        'required': true
      },
      'emptyMonthsHiddenField': {
        'type': 'field',
        'inputType': 'hidden',
        'name': 'monthDays',
        'forFrequency': 'MONTHLY',
        'internalListeners': {
          'beforeUpdateDisabled': 'up.onFieldBeforeUpdateDisabled',
          'change'() {
            this._value = null;
          }
        }
      },
      'monthDaysRadioField': {
        'type': 'checkbox',
        'weight': 30,
        'toggleGroup': 'radio',
        'toggleGroupRootElement': '.b-recurrenceeditorpanel',
        'forFrequency': 'MONTHLY',
        'label': 'L{RecurrenceEditor.Each}',
        'checked': true,
        'onChange': 'up.onMonthDaysRadioFieldChange',
        'cls': 'b-label-with-checkbox'
      },
      'monthDaysButtonField': {
        'type': 'recurrencemonthdaysbuttongroup',
        'weight': 40,
        'name': 'monthDays',
        'forFrequency': 'MONTHLY',
        'cls': 'b-space-above',
        'required': true,
        'internalListeners': { 'beforeUpdateDisabled': 'up.onFieldBeforeUpdateDisabled' }
      },
      'monthDaysNumberRadioField': {
        'type': 'checkbox',
        'weight': 53,
        'cls': 'b-label-with-checkbox',
        'toggleGroup': 'radio',
        'toggleGroupRootElement': '.b-recurrenceeditorpanel',
        'forFrequency': 'YEARLY',
        'label': 'L{RecurrenceEditor.on}',
        'style': 'display:inline-flex;',
        'onChange': 'up.onMonthDaysNumberRadioFieldChange'
      },
      'monthDaysNumberField': {
        'type': 'numberfield',
        'weight': 56,
        'flex': 1,
        'name': 'monthDays',
        'forFrequency': 'YEARLY',
        'min': 1,
        'max': 31,
        'required': true,
        'internalListeners': { 'beforeUpdateDisabled': 'up.onFieldBeforeUpdateDisabled' }
      },
      'monthsLabel': {
        'type': 'label',
        'text': 'L{RecurrenceEditor.Months}',
        'weight': 57,
        'forFrequency': 'YEARLY',
        'cls': 'b-button-group-label'
      },
      'monthsButtonField': {
        'type': 'recurrencemonthsbuttongroup',
        'weight': 58,
        'cls': 'b-space-above b-space-below',
        'name': 'months',
        'forFrequency': 'YEARLY',
        'onChange': 'up.onMonthsButtonFieldChange',
        'required': true
      },
      'positionAndDayRadioField': {
        'type': 'checkbox',
        'weight': 60,
        'cls': 'b-label-with-checkbox b-space-below',
        'toggleGroup': 'radio',
        'toggleGroupRootElement': '.b-recurrenceeditorpanel',
        'forFrequency': 'MONTHLY|YEARLY',
        'label': 'L{RecurrenceEditor.On the}',
        'onChange': 'up.onPositionAndDayRadioFieldChange'
      },
      'positionsCombo': {
        'type': 'recurrencepositionscombo',
        'weight': 80,
        'name': 'positions',
        'forFrequency': 'MONTHLY|YEARLY',
        'cls': 'b-space-below b-no-span',
        'internalListeners': { 'beforeUpdateDisabled': 'up.onFieldBeforeUpdateDisabled' }
      },
      'daysCombo': {
        'type': 'recurrencedayscombo',
        'weight': 90,
        'name': 'days',
        'forFrequency': 'MONTHLY|YEARLY',
        'cls': 'b-space-below b-no-span',
        'internalListeners': { 'beforeUpdateDisabled': 'up.onFieldBeforeUpdateDisabled' }
      },
      'stopRecurrenceField': {
        'type': 'recurrencestopconditioncombo',
        'weight': 100,
        'label': 'L{RecurrenceEditor.End repeat}',
        'onChange': 'up.onStopRecurrenceFieldChange',
        'cls': 'b-no-inner-span'
      },
      'countField': {
        'type': 'numberfield',
        'weight': 110,
        'name': 'count',
        'min': 2,
        'required': true,
        'disabled': true,
        'label': ' ',
        'cls': 'b-no-span',
        'internalListeners': { 'beforeUpdateDisabled': 'up.onFieldBeforeUpdateDisabled' }
      },
      'endDateField': {
        'type': 'datefield',
        'weight': 120,
        'name': 'endDate',
        'hidden': true,
        'disabled': true,
        'label': ' ',
        'required': true,
        'cls': 'b-no-span',
        'internalListeners': { 'beforeUpdateDisabled': 'up.onFieldBeforeUpdateDisabled' }
      }
    }
  };
  static type = 'recurrenceeditorpanel';
  static $name = 'RecurrenceEditorPanel';
  changeItems(a, b) {
    const c = super.changeItems(a, b), {
        positionAndDayRadioField: e,
        monthDaysNumberRadioField: f,
        monthDaysNumberField: g
      } = this.widgetMap;
    if (f) {
      e.label = this.L('L{RecurrenceEditor.the}');
    } else {
      e.label = this.L('L{RecurrenceEditor.On the}');
      g && c.remove(g);
    }
    return c;
  }
  setupWidgetConfig(a) {
    if (BrowserHelper.isMobile && !('editable' in a)) {
      a.editable = false;
    }
    return super.setupWidgetConfig(...arguments);
  }
  updateRecord(a) {
    let b, c, e;
    super.updateRecord(a);
    const f = this, {
        frequencyField: g,
        daysButtonField: i,
        monthDaysButtonField: j,
        monthDaysNumberField: k,
        monthsButtonField: l,
        monthDaysRadioField: n,
        monthDaysNumberRadioField: o,
        positionAndDayRadioField: p,
        stopRecurrenceField: q
      } = f.widgetMap;
    if (a) {
      const r = a.getDateValues();
      if (r) {
        if (!((b = a.days) == null ? void 0 : b.length)) {
          i.value = r.days;
        }
        if (!((c = a.monthDays) == null ? void 0 : c.length)) {
          if (j) {
            j.value = r.monthDays;
          }
          if (k) {
            k.value = r.monthDays[0];
          }
        }
        if (!((e = a.months) == null ? void 0 : e.length) && l) {
          l.value = r.months[0];
        }
      }
      if (i.value && a.positions) {
        p.check();
        if (!f.isPainted) {
          n.uncheck();
        }
      } else if (j == null ? void 0 : j.value) {
        if (o && !o.hidden) {
          o.check();
        } else {
          n.check();
        }
        if (!f.isPainted || !f.element.isConnected) {
          p.uncheck();
        }
      }
      if (q) {
        q.recurrence = a;
      }
    } else {
      g.value = 'NONE';
    }
  }
  get valuesCleanState() {
    return {
      'frequency': null,
      'interval': null,
      'days': null,
      'months': null,
      'monthDays': null,
      'positions': null,
      'endDate': null,
      'count': null
    };
  }
  processValues(a) {
    if ('monthDays' in a) {
      if (typeof a.monthDays === 'string') {
        a.monthDays = a.monthDays.split(',');
      } else {
        a.monthDays = ArrayHelper.asArray(a.monthDays);
      }
    }
  }
  get values() {
    const a = {
      ...this.valuesCleanState,
      ...super.values
    };
    this.processValues(a);
    return a;
  }
  set values(a) {
    super.values = a;
  }
  getValues() {
    const a = {
      ...this.valuesCleanState,
      ...super.getValues(...arguments)
    };
    this.processValues(a);
    return a;
  }
  syncEventRecord(a) {
    a = a || this.record;
    if (a) {
      const b = this.getValues(c => c.name in a && !c.disabled);
      a.set(b);
    }
  }
  toggleStopFields(a) {
    let b;
    const c = this, e = (b = c.widgetMap.stopRecurrenceField) == null ? void 0 : b.value, {
        countField: f,
        endDateField: g
      } = c.widgetMap, i = e === 'count', j = !i && e === 'date';
    f._userAction = g._userAction = a;
    f.disabled = f.hidden = !i;
    g.disabled = g.hidden = !j;
    f._userAction = g._userAction = false;
  }
  isWidgetAvailableForFrequency(a, b = this.widgetMap.frequencyField.value) {
    return !a.forFrequency || a.forFrequency.includes(b);
  }
  onFieldChange({
    source: a,
    userAction: b
  }) {
    const {record: c} = this;
    if (b && this.autoUpdateRecord && c) {
      c.isSanitizingSuspended = true;
    }
    super.onFieldChange(...arguments);
    if (b && this.autoUpdateRecord && c) {
      c.isSanitizingSuspended = false;
      this.setTimeout({
        'fn': () => c.sanitize(),
        'delay': 50,
        'cancelOutstanding': true
      });
    }
  }
  onFieldBeforeUpdateDisabled({
    source: a,
    disabled: b
  }) {
    if (!b && a._userAction) {
      a.triggerFieldChange({ 'userAction': true });
    }
  }
  onMonthDaysRadioFieldChange({
    source: a,
    checked: b,
    userAction: c,
    propagatingUserActionFrom: e
  }) {
    const {
      monthDaysButtonField: f,
      emptyMonthsHiddenField: g
    } = this.widgetMap;
    if (f) {
      f._userAction = c;
      f.disabled = !b || !this.isWidgetAvailableForFrequency(f);
      f._userAction = false;
    }
    if (g) {
      g._userAction = c || e;
      g.disabled = b || !this.isWidgetAvailableForFrequency(g);
      g._userAction = false;
    }
  }
  onMonthsButtonFieldChange({value: a}) {
    const b = DateHelper.getMonthNames(), {monthDaysNumberField: c} = this.widgetMap;
    if (a && c) {
      c.hint = a.split(',').map(e => b[e - 1]).join(', ');
    }
  }
  onMonthDaysNumberRadioFieldChange({
    checked: a,
    userAction: b
  }) {
    const {monthDaysNumberField: c} = this.widgetMap;
    c._userAction = b;
    c.disabled = !a || !this.isWidgetAvailableForFrequency(c);
    c._userAction = false;
  }
  onPositionAndDayRadioFieldChange({
    source: a,
    checked: b,
    userAction: c
  }) {
    const {
      daysCombo: e,
      positionsCombo: f
    } = this.widgetMap;
    e._userAction = f._userAction = c;
    e.disabled = f.disabled = !b || !this.isWidgetAvailableForFrequency(e);
    e._userAction = f._userAction = false;
  }
  onStopRecurrenceFieldChange({userAction: a}) {
    this.toggleStopFields(a);
  }
  onFrequencyFieldChange({
    value: a,
    oldValue: b,
    valid: c,
    userAction: e
  }) {
    const f = this, g = f.queryAll(l => 'forFrequency' in l), {
        intervalField: i,
        stopRecurrenceField: j
      } = f.widgetMap, k = a === 'NONE';
    if (c && a) {
      for (const l of g) {
        l._userAction = e;
        l.disabled = l.hidden = !f.isWidgetAvailableForFrequency(l, a);
        l._userAction = false;
      }
      if (j) {
        j.hidden = k;
      }
      if (i) {
        i.hidden = k;
        if (!k) {
          i.hint = f.L('L{RecurrenceEditor.' + a + 'intervalUnit}');
        }
        if (b === 'NONE' && i.value == null) {
          i.value = 1;
        }
      }
      f.toggleFieldsState();
    }
  }
  toggleFieldsState(a) {
    const b = this, {widgetMap: c} = b, {monthDaysRadioField: e} = c;
    if (e) {
      b.onMonthDaysRadioFieldChange({
        'checked': e.checked,
        'userAction': a
      });
    }
    b.onPositionAndDayRadioFieldChange({
      'checked': c.positionAndDayRadioField.checked,
      'userAction': a
    });
    b.onStopRecurrenceFieldChange({ 'userAction': a });
  }
  updateLocalization() {
    const {
      countField: a,
      intervalField: b,
      frequencyField: c
    } = this.widgetMap;
    a.hint = this.L('L{RecurrenceEditor.time(s)}');
    if (c.value && c.value !== 'NONE') {
      b.hint = this.L('L{RecurrenceEditor.' + c.value + 'intervalUnit}');
    }
    super.updateLocalization();
  }
};
RecurrenceEditorPanel.initClass();
RecurrenceEditorPanel._$name = 'RecurrenceEditorPanel';
const RecurrenceEditor = class extends Popup {
  static get $name() {
    return 'RecurrenceEditor';
  }
  static get type() {
    return 'recurrenceeditor';
  }
  static get configurable() {
    return {
      'draggable': true,
      'closable': true,
      'floating': true,
      'cls': 'b-recurrenceeditor',
      'title': 'L{Repeat event}',
      'autoClose': true,
      'width': 470,
      'items': {
        'recurrenceEditorPanel': {
          'type': 'recurrenceeditorpanel',
          'items': { 'monthDaysNumberRadioField': false },
          'title': null
        }
      },
      'bbar': {
        'defaults': { 'localeClass': this },
        'items': {
          'foo': {
            'type': 'widget',
            'cls': 'b-label-filler',
            'weight': 100
          },
          'saveButton': {
            'color': 'b-green',
            'text': 'L{Save}',
            'onClick': 'up.onSaveClick',
            'weight': 200
          },
          'cancelButton': {
            'color': 'b-gray',
            'text': 'L{Object.Cancel}',
            'onClick': 'up.onCancelClick',
            'weight': 300
          }
        }
      },
      'scrollable': { 'overflowY': true }
    };
  }
  updateReadOnly(a) {
    super.updateReadOnly(a);
    this.bbar.hidden = a;
  }
  get recurrenceEditorPanel() {
    return this.widgetMap.recurrenceEditorPanel;
  }
  updateRecord(a) {
    this.recurrenceEditorPanel.record = a;
  }
  onSaveClick() {
    const a = this;
    if (a.saveHandler) {
      a.saveHandler.call(a.thisObj || a, a, a.record);
    } else {
      a.recurrenceEditorPanel.syncEventRecord();
      a.close();
    }
  }
  onCancelClick() {
    const a = this;
    if (a.cancelHandler) {
      a.cancelHandler.call(a.thisObj || a, a, a.record);
    } else {
      a.close();
    }
  }
};
RecurrenceEditor.initClass();
RecurrenceEditor._$name = 'RecurrenceEditor';
const RecurringEventEdit_default = a => {
  let b;
  return b = class extends (a || Base) {
    changeEditorConfig(c) {
      c.items = {
        ...c.items,
        ...this.recurringEventsItems
      };
      c = super.changeEditorConfig(c);
      return c;
    }
    doDestroy() {
      let c, e;
      (c = this._recurrenceConfirmation) == null ? void 0 : c.destroy();
      (e = this._recurrenceEditor) == null ? void 0 : e.destroy();
      super.doDestroy();
    }
    onEditorConstructed(c) {
      let e;
      const f = this;
      c.ion({
        'hide': f.onRecurringEventEditorHide,
        'show': f.onRecurringEventEditorShow,
        'reveal': f.onRecurringEventEditorShow,
        'unreveal': f.onRecurringEventEditorHide,
        'thisObj': f
      });
      if (f.editRecurrenceButton) {
        f.editRecurrenceButton.menu = f.recurrenceEditor;
      }
      (e = f.recurrenceCombo) == null ? void 0 : e.ion({
        'change': f.onRecurrenceComboChange,
        'thisObj': f
      });
    }
    updateReadOnly(c) {
      if (this._recurrenceEditor) {
        this._recurrenceEditor.readOnly = c;
      }
    }
    internalShowEditor() {
      this.toggleRecurringFieldsVisibility(this.client.enableRecurringEvents && this.showRecurringUI !== false);
    }
    toggleRecurringFieldsVisibility(c = true) {
      let e, f, g, i;
      const j = c ? 'show' : 'hide';
      (f = (e = this.editRecurrenceButton) == null ? void 0 : e[j]) == null ? void 0 : f.call(e);
      (i = (g = this.recurrenceCombo) == null ? void 0 : g[j]) == null ? void 0 : i.call(g);
    }
    onRecurringEventEditorShow({source: c}) {
      let e;
      if (this.recurrenceCombo && ((e = c.record) == null ? void 0 : e.startDate) && !c.record.isRecurring && this.useContextualRecurrenceRules) {
        const f = this, {recurrenceCombo: g} = f, {store: i} = g;
        if (!i.modelClass.fieldMap.isCustom) {
          i.modelClass.addField({
            'name': 'isCustom',
            'defaultValue': false
          });
        }
        const j = c.record.startDate, k = j.getDate(), l = j.getDay(), n = RecurrenceDayRuleEncoder.encodeDay(l), o = j.getMonth(), p = Math.ceil(k / 7), q = f.L('L{RecurrenceLegend.position' + p + '}'), r = [
            {
              'isCustom': true,
              'text': f.L('L{RecurrenceLegend.Weekly on {1}}', { 'days': DateHelper.getDayName(l) }),
              'value': 'WEEKLY;BYDAY=' + n
            },
            {
              'isCustom': true,
              'text': f.L('L{RecurrenceLegend.Monthly on {1}}', { 'days': DateHelper.format(j, 'Do') }),
              'value': 'MONTHLY;BYMONTHDAY=' + k
            },
            {
              'isCustom': true,
              'text': f.L('L{RecurrenceLegend.Monthly on {1}}', { 'days': q + ' ' + DateHelper.getDayName(l) }),
              'value': 'MONTHLY;BYDAY=' + n + ';BYSETPOS=' + p
            },
            {
              'isCustom': true,
              'text': f.L('L{RecurrenceLegend.Yearly on {1} of {2}}', {
                'days': DateHelper.format(j, 'Do'),
                'months': DateHelper.getMonthName(o)
              }),
              'value': 'YEARLY;BYMONTH=' + (o + 1) + ';BYMONTHDAY=' + k
            },
            {
              'isCustom': true,
              'text': f.L('L{RecurrenceLegend.Yearly on {1} of {2}}', {
                'days': q + ' ' + DateHelper.getDayName(l),
                'months': DateHelper.getMonthName(o)
              }),
              'value': 'YEARLY;BYDAY=' + n + ';BYMONTH=' + (o + 1) + ';BYSETPOS=' + p
            }
          ];
        i.add(r);
        i.filter(u => u.isCustom || u.value === 'none' || u.value === 'DAILY');
      }
    }
    onRecurringEventEditorHide() {
      const {store: c} = this.recurrenceCombo;
      if (c) {
        c.clearFilters();
        c.remove(c.query(e => e.isCustom));
      }
    }
    makeRecurrence(c) {
      const e = this.eventRecord, f = e.copy();
      let g = e.recurrence;
      if (!c && g) {
        g = g.copy();
      } else {
        g = new e.recurrenceModel(c ? { 'rule': c } : {});
      }
      g.timeSpan = f;
      f.setStartDate(this.values.startDate);
      g.suspendTimeSpanNotifying();
      return g;
    }
    onRecurrableEventBeforeSave({
      eventRecord: c,
      context: e
    }) {
      const f = this;
      if (f.isEditing && !c.isCreating && c.supportsRecurring && (c.isRecurring || c.isOccurrence)) {
        f.recurrenceConfirmation.confirm({
          'actionType': 'update',
          'eventRecord': c,
          'changerFn'() {
            e.finalize(true);
          },
          'cancelFn'() {
            e.finalize(false);
          }
        });
        e.async = true;
      }
    }
    set recurrenceConfirmation(c) {
      this._recurrenceConfirmation = c;
    }
    get recurrenceConfirmation() {
      const c = this;
      let e = c._recurrenceConfirmation;
      if (!e || !e.$$name) {
        e = Widget.create({
          'type': 'recurrenceconfirmation',
          'owner': c.editor,
          ...e
        });
        c._recurrenceConfirmation = e;
      }
      return e;
    }
    set recurrenceEditor(c) {
      this._recurrenceEditor = c;
    }
    get recurrenceEditor() {
      const c = this;
      let e = c._recurrenceEditor;
      if (!e || !e.$$name) {
        c._recurrenceEditor = e = Widget.create({
          'type': 'recurrenceeditor',
          'autoShow': false,
          'centered': true,
          'modal': true,
          'minWidth': 'auto',
          'constrainTo': globalThis,
          'anchor': false,
          'rootElement': c.rootElement,
          'saveHandler': c.recurrenceEditorSaveHandler,
          'thisObj': c,
          'internalListeners': {
            'beforeShow': c.onBeforeShowRecurrenceEditor,
            'thisObj': c
          },
          ...e
        });
        e.readOnly = c._readOnly;
      }
      return e;
    }
    onBeforeShowRecurrenceEditor() {
      const c = this, {
          recurrenceEditor: e,
          eventRecord: f
        } = c;
      if (e && (f == null ? void 0 : f.supportsRecurring)) {
        if (!c.recurrence) {
          c.recurrence = c.makeRecurrence();
        }
        c.recurrence.timeSpan.setStartDate(c.values.startDate);
        e.record = c.recurrence;
        e.centered = true;
      }
    }
    loadRecurrenceData(c) {
      this.recurrence = c;
      this.updateRecurrenceFields(c);
    }
    updateRecurrenceFields(c) {
      const e = this, {editRecurrenceButton: f} = e;
      if (e.recurrenceCombo && !e.processingRecurrenceComboChange) {
        e.recurrenceCombo.recurrence = c;
      }
      if (f) {
        f.recurrence = c;
        f.value = c ? c.rule : null;
        if (c && e.client.enableRecurringEvents && e.showRecurringUI !== false) {
          f.show();
        } else {
          f.hide();
        }
      }
    }
    onRecurrenceComboChange({
      source: c,
      value: e,
      userAction: f
    }) {
      if (f) {
        const g = this, {recurrenceEditor: i} = g;
        if (e === c.customValue) {
          g.recurrenceCombo.recurrence = g.makeRecurrence();
          if (i.centered) {
            i.show();
          } else {
            i.show((g.editRecurrenceButton || c).element);
          }
        } else {
          g.processingRecurrenceComboChange = true;
          g.loadRecurrenceData(e && e !== 'none' ? g.makeRecurrence('FREQ=' + e) : null);
          g.processingRecurrenceComboChange = false;
        }
      }
    }
    recurrenceEditorSaveHandler(c, e) {
      c.recurrenceEditorPanel.syncEventRecord(e);
      const {store: f} = this.recurrenceCombo;
      f.clearFilters();
      f.remove(f.query(g => g.isCustom));
      this.updateRecurrenceFields(e);
      c.close();
    }
    onDatesChange(...c) {
      super.onDatesChange(...c);
      if (!this.loadingRecord && this.editRecurrenceButton) {
        const {startDate: e} = this.values;
        if (e) {
          this.editRecurrenceButton.eventStartDate = e;
        }
      }
    }
    internalLoadRecord(c) {
      if (c == null ? void 0 : c.supportsRecurring) {
        this.loadRecurrenceData(c.recurrence ? this.makeRecurrence() : null);
      }
    }
    updateRecord(c) {
      if (c.recurrenceRule && !this.recurrence) {
        c.recurrenceRule = null;
      }
      return super.updateRecord(c);
    }
  }, __publicField(b, '$name', 'RecurringEventEdit'), __publicField(b, 'configurable', {
    'recurringEventsItems': {
      'recurrenceCombo': {
        'type': 'recurrencecombo',
        'label': 'L{EventEdit.Repeat}',
        'ref': 'recurrenceCombo',
        'weight': 700
      },
      'editRecurrenceButton': {
        'type': 'recurrencelegendbutton',
        'ref': 'editRecurrenceButton',
        'name': 'recurrenceRule',
        'color': 'b-gray',
        'menuIcon': null,
        'flex': 1,
        'weight': 800,
        'ignoreParentReadOnly': true
      }
    },
    'showRecurringUI': null,
    'useContextualRecurrenceRules': true
  }), b;
};
const ResourceCombo = class extends Combo {
  static get $name() {
    return 'ResourceCombo';
  }
  static get type() {
    return 'resourcecombo';
  }
  static get configurable() {
    return {
      'showEventColor': null,
      'displayField': 'name',
      'valueField': 'id',
      'picker': {
        'cls': 'b-resourcecombo-picker',
        'itemIconTpl'(a) {
          if (this.owner.showEventColor) {
            const {eventColor: b} = a, c = !DomHelper.isNamedColor(b), e = b ? c ? ' style="color:' + b + '"' : '' : ' style="display:none"', f = !b || c ? '' : ' b-sch-foreground-' + b;
            return '<div class="b-icon b-icon-square' + f + '"' + e + '></div>';
          }
          if (this.multiSelect) {
            return '<div class="b-icon b-icon-square"></div>';
          }
          return '';
        }
      }
    };
  }
  changeShowEventColor(a) {
    return Boolean(a);
  }
  updateShowEventColor(a) {
    let b;
    this.element.classList.toggle('b-show-event-color', Boolean(a));
    (b = this._picker) == null ? void 0 : b.element.classList.toggle('b-show-event-color', Boolean(a));
  }
  changePicker(a, b) {
    a = super.changePicker(a, b);
    a == null ? void 0 : a.element.classList.toggle('b-show-event-color', Boolean(this.showEventColor));
    return a;
  }
  get innerElements() {
    return [
      {
        'class': 'b-icon b-resource-icon b-icon-square b-hide-display',
        'reference': 'resourceIcon'
      },
      this.inputElement
    ];
  }
  syncInputFieldValue() {
    let a, b;
    const c = this, {
        resourceIcon: e,
        lastResourceIconCls: f
      } = c, {classList: g} = e, i = (b = (a = c.selected) == null ? void 0 : a.eventColor) != null ? b : '';
    super.syncInputFieldValue();
    e.style.color = '';
    f && g.remove(f);
    c.lastResourceIconCls = null;
    if (i) {
      if (DomHelper.isNamedColor(i)) {
        c.lastResourceIconCls = 'b-sch-foreground-' + i;
        g.add(c.lastResourceIconCls);
      } else {
        e.style.color = i;
      }
      g.remove('b-hide-display');
    } else {
      g.add('b-hide-display');
    }
  }
};
ResourceCombo.initClass();
ResourceCombo._$name = 'ResourceCombo';
const EventColorField = class extends ColorField {
  static configurable = {
    'picker': { 'type': 'eventcolorpicker' },
    'name': 'eventColor'
  };
  static type = 'eventcolorfield';
  static $name = 'EventColorField';
};
EventColorField.initClass();
EventColorField._$name = 'EventColorField';
const punctuation = /[^\w\d]/g;
const _EventEdit = class _EventEdit extends EditBase.mixin(TaskEditStm_default, RecurringEventEdit_default, Delayable_default) {
  static configurable = {
    'triggerEvent': 'eventdblclick',
    'ignoreSelector': null,
    'typeField': 'eventType',
    'eventRecord': null,
    'readOnly': null,
    'editorConfig': {
      'type': 'eventeditor',
      'title': 'L{EventEdit.Edit event}',
      'closable': true,
      'localeClass': _EventEdit,
      'defaults': { 'localeClass': _EventEdit },
      'items': {
        'nameField': {
          'type': 'text',
          'label': 'L{Name}',
          'clearable': true,
          'name': 'name',
          'weight': 100
        },
        'resourceField': {
          'type': 'resourcecombo',
          'label': 'L{Resource}',
          'name': 'resource',
          'editable': true,
          'valueField': 'id',
          'displayField': 'name',
          'highlightExternalChange': false,
          'destroyStore': true,
          'weight': 200
        },
        'startDateField': {
          'type': 'date',
          'clearable': false,
          'required': true,
          'label': 'L{Start}',
          'name': 'startDate',
          'validateDateOnly': true,
          'weight': 300
        },
        'startTimeField': {
          'type': 'time',
          'clearable': false,
          'required': true,
          'keepDate': false,
          'name': 'startDate',
          'cls': 'b-match-label',
          'weight': 400
        },
        'endDateField': {
          'type': 'date',
          'clearable': false,
          'required': true,
          'label': 'L{End}',
          'name': 'endDate',
          'validateDateOnly': true,
          'weight': 500
        },
        'endTimeField': {
          'type': 'time',
          'clearable': false,
          'required': true,
          'keepDate': false,
          'name': 'endDate',
          'cls': 'b-match-label',
          'weight': 600
        },
        'colorField': {
          'label': 'L{SchedulerBase.color}',
          'type': 'eventColorField',
          'name': 'eventColor',
          'weight': 700
        }
      },
      'bbar': {
        'hideWhenEmpty': true,
        'defaults': { 'localeClass': _EventEdit },
        'items': {
          'saveButton': {
            'color': 'b-blue',
            'cls': 'b-raised',
            'text': 'L{Save}',
            'weight': 100
          },
          'deleteButton': {
            'text': 'L{Delete}',
            'weight': 200
          },
          'cancelButton': {
            'text': 'L{Object.Cancel}',
            'weight': 300
          }
        }
      }
    },
    'targetEventElement': null,
    'minEditSize': 100
  };
  static $name = 'EventEdit';
  static get pluginConfig() {
    return {
      'chain': [
        'populateEventMenu',
        'onEventEnterKey'
      ],
      'assign': ['editEvent']
    };
  }
  construct(a, b) {
    this.readOnly = a.readOnly;
    super.construct(a, b);
    a.ion({
      'projectChange': 'onChangeProject',
      'readOnly': 'onClientReadOnlyToggle',
      'thisObj': this
    });
  }
  get scheduler() {
    return this.client;
  }
  get project() {
    return this.client.project;
  }
  get readOnly() {
    return this._editor ? this.editor.readOnly : this._readOnly;
  }
  updateReadOnly(a) {
    super.updateReadOnly(a);
    if (this._editor) {
      this.editor.readOnly = a;
    }
  }
  onClientReadOnlyToggle({readOnly: a}) {
    this.readOnly = a;
  }
  get editor() {
    let a, b, c, e, f;
    const g = this, i = {
        'beforehide': 'resetEditingContext',
        'beforeshow': 'onBeforeEditorShow',
        'keydown': 'onPopupKeyDown',
        'thisObj': g
      };
    let {_editor: j} = g;
    if (j) {
      return j;
    }
    j = g._editor = Widget.create(g.getEditorConfig());
    const {
      startDateField: k,
      startTimeField: l,
      endDateField: n,
      endTimeField: o
    } = j.widgetMap;
    if (!k && l) {
      l.label = g.L('Start');
      l.flex = '1 0 100%';
    }
    if (!n && o) {
      o.label = g.L('End');
      o.flex = '1 0 100%';
    }
    if (!j.floating && !j.positioned) {
      if (!j.element.parentNode) {
        g.client.add(j);
      }
      delete i.beforehide;
      delete i.beforeshow;
      i.beforeToggleReveal = 'onBeforeEditorToggleReveal';
    }
    j.readOnly = g._readOnly;
    if (j.items.length === 0) {
      console.warn('Event Editor configured without any `items`');
    }
    j.ion(i);
    g.scheduler.relayEvents(j, ['beforeSetRecord'], 'eventEdit');
    Object.values(j.widgetMap).forEach(p => {
      const q = p.ref || p.id;
      if (q && !g[q]) {
        g[q] = p;
        switch (p.name) {
        case 'startDate':
        case 'endDate':
          p.ion({
            'change': 'onDatesChange',
            'thisObj': g
          });
          break;
        }
      }
    });
    (a = g.onEditorConstructed) == null ? void 0 : a.call(g, j);
    (b = g.eventTypeField) == null ? void 0 : b.ion({
      'change': 'onEventTypeChange',
      'thisObj': g
    });
    (c = g.saveButton) == null ? void 0 : c.ion({
      'click': 'onSaveClick',
      'thisObj': g
    });
    (e = g.deleteButton) == null ? void 0 : e.ion({
      'click': 'onDeleteClick',
      'thisObj': g
    });
    (f = g.cancelButton) == null ? void 0 : f.ion({
      'click': 'onCancelClick',
      'thisObj': g
    });
    return j;
  }
  getEditorConfig() {
    const a = this, {
        cls: b,
        scheduler: c
      } = a, e = ObjectHelper.assign({
        'owner': c,
        'eventEditFeature': a,
        'weekStartDay': a.weekStartDay,
        'align': 'b-t',
        'id': c.id + '-event-editor',
        'autoShow': false,
        'anchor': true,
        'scrollAction': 'realign',
        'constrainTo': globalThis,
        'cls': b
      }, a.editorConfig);
    if (Widget.prototype.getRenderContext(e)[0]) {
      e.floating = false;
    }
    if (e.floating === false && !e.positioned) {
      e.collapsible = {
        'type': 'overlay',
        'direction': 'right',
        'autoClose': false,
        'tool': null,
        'recollapseTool': null
      };
      e.collapsed = true;
      e.hidden = e.anchor = false;
      e.hide = function () {
        this.collapsible.toggleReveal(false);
      };
    }
    if (!c.showEventColorPickers && e.items.colorField) {
      e.items.colorField.hidden = true;
    }
    e.onElementCreated = a.updateCSSVars.bind(this);
    return e;
  }
  updateCSSVars({element: a}) {
    const b = new Date(2000, 12, 31, 23, 55, 55), c = DateHelper.format(b, this.dateFormat).replace(punctuation, '').length, e = DateHelper.format(b, this.timeFormat).replace(punctuation, '').length, f = c + e;
    a.style.setProperty('--date-time-length', f + 'em');
    a.style.setProperty('--date-width-difference', (c - e) / 2 + 'em');
  }
  async internalShowEditor(a, b, c = null) {
    let e, f;
    const g = this, {scheduler: i} = g, {useInitialAnimation: j} = i, k = ((e = c == null ? void 0 : c.target) == null ? void 0 : e.nodeType) === Element.ELEMENT_NODE ? c.target : i.getElementFromEventRecord(a, b), l = a.isPartOfStore(i.eventStore);
    c = c != null ? c : {
      'target': k,
      'anchor': true
    };
    if (c.target || (!l || a.resources.length === 0) || a.isCreating) {
      i.element.classList.add('b-eventeditor-editing');
      g.resourceRecord = b;
      const {editor: n} = g;
      g.editingContext = {
        'eventRecord': a,
        'resourceRecord': b,
        'eventElement': k,
        'editor': n,
        'isPartOfStore': l
      };
      (f = super.internalShowEditor) == null ? void 0 : f.call(this, a, b, c);
      if (g.typeField) {
        g.toggleEventType(a.getValue(g.typeField));
      }
      g.loadRecord(a, b);
      if (n.collapsed) {
        await AsyncHelper.sleep(100);
        await n.collapsible.toggleReveal(true);
        n.focus();
      } else if (n.centered || !n.anchor || !n.floating) {
        n.show();
      } else if (k && (!a.isCreating || !j || j === true || j === 'fade-in')) {
        g.targetEventElement = k;
        n.showBy(c);
      } else {
        n.show();
        n.updateCentered(true);
      }
      const o = i.timeAxisViewModel.timeResolution;
      if (o.unit === 'hour' || o.unit === 'minute') {
        const p = '' + o.increment + o.unit;
        if (g.startTimeField) {
          g.startTimeField.step = p;
        }
        if (g.endTimeField) {
          g.endTimeField.step = p;
        }
      }
      g.detachListeners('changesWhileEditing');
      i.eventStore.ion({
        'change': g.onChangeWhileEditing,
        'refresh': g.onChangeWhileEditing,
        'thisObj': g,
        'name': 'changesWhileEditing'
      });
    }
  }
  onChangeWhileEditing() {
    const a = this;
    if (!a.editor.autoUpdateRecord && !a.isFinalizingEventSave && a.isEditing && a.editingContext.isPartOfStore && !a.eventRecord.isPartOfStore(a.scheduler.eventStore)) {
      a.onCancelClick();
    }
  }
  onBeforeEditorShow() {
    super.onBeforeEditorShow(...arguments);
    this.scheduler.trigger('beforeEventEditShow', {
      'eventEdit': this,
      ...this.editingContext
    });
  }
  updateTargetEventElement(a, b) {
    a == null ? void 0 : a.classList.add('b-editing');
    b == null ? void 0 : b.classList.remove('b-editing');
  }
  editEvent(a, b, c = null, e = null) {
    let f, g, i;
    const j = this, {client: k} = j, {
        eventStore: l,
        assignmentStore: n,
        enableEventAnimations: o
      } = k, {simpleEventEdit: p} = k.features;
    if (!a.isModel) {
      a = l.createRecord(a);
    }
    const q = !a.isOccurrence && !l.$master.includes(a);
    if (q) {
      const r = [];
      a.isCreating = true;
      let u = [];
      if (b && !n.includesAssignment(a.id, b.id)) {
        r.push(b);
        u = n.assignEventToResource(a, b);
      }
      if (k.trigger('beforeEventAdd', {
          'eventRecord': a,
          'resourceRecords': r,
          'assignmentRecords': u
        }) === false) {
        n == null ? void 0 : n.remove(u);
        a.isCreating = false;
        return false;
      }
    }
    if (j.isEditing) {
      j.resetEditingContext();
    }
    if (j.disabled || a.readOnly || a.isCreating && (p == null ? void 0 : p.enabled)) {
      return;
    }
    if (k.trigger('beforeEventEdit', {
        'eventEdit': j,
        'eventRecord': a,
        'resourceRecord': b,
        'eventElement': ((f = k.getElementFromEventRecord) == null ? void 0 : f.call(k, a, b)) || c
      }) === false) {
      k.element.classList.remove('b-eventeditor-editing');
      if (k.isCalendar) {
        a.isCreating = false;
      }
      return false;
    }
    if (e) {
      j.applyStmCapture(e);
      j.hasStmCapture = true;
      e.transferred = true;
    } else if (e !== false && !k.isCalendar && !j.hasStmCapture) {
      j.captureStm(true);
    }
    if (k.refreshRows) {
      if (q) {
        j.enableEventAnimations = false;
        l.$master.add(a);
        k.project.commitAsync().then(() => k.enableEventAnimations = o);
        k.refreshRows();
      }
      if (a.isCreating) {
        (i = (g = k.getElementFromEventRecord) == null ? void 0 : g.call(k, a, b, true)) == null ? void 0 : i.focus({ 'preventScroll': true });
      }
    }
    return j.doEditEvent(a, b, c, e).then(v => {
      if (!j.isDestroying) {
        if (!j.isEditing && !k.isCalendar && !j.rejectingStmTransaction) {
          if (v !== false && j.hasStmCapture) {
            return j.freeStm(false);
          } else {
            return j.freeStm();
          }
        }
      }
    });
  }
  get isEditing() {
    const {_editor: a} = this;
    return Boolean((a == null ? void 0 : a.isVisible) && !(a.collapsed && !a.revealed));
  }
  async doEditEvent(a, b, c = null) {
    const e = this, {scheduler: f} = e, g = a.isCreating;
    if (!b) {
      b = a.resource || e.resourceStore.getById(a.resourceId);
    }
    if (g) {
      TimeSpan.prototype.normalize.call(a);
    }
    if (c || g || a.resources.length === 0) {
      return e.internalShowEditor(a, b, c ? { 'target': c } : null);
    } else {
      const i = f.isVertical ? 'maxHeight' : 'maxWidth';
      await f.scrollResourceEventIntoView(b, a, {
        [i]: e.minEditSize,
        'animate': true,
        'edgeOffset': 0,
        'extendTimeAxis': false
      });
      if (!f.isDestroyed) {
        await e.internalShowEditor(a, b);
        if (!f.isDestroyed) {
          f.element.classList.remove('b-eventeditor-editing');
        }
      }
    }
  }
  loadRecord(a, b) {
    this.loadingRecord = true;
    this.internalLoadRecord(a, b);
    this.loadingRecord = false;
  }
  get eventRecord() {
    let a;
    return (a = this._editor) == null ? void 0 : a.record;
  }
  internalLoadRecord(a, b) {
    let c;
    const e = this, {eventStore: f} = e.client, {
        editor: g,
        resourceField: i
      } = e, {endTimeField: j} = g.widgetMap;
    e.resourceRecord = b;
    if (i && ((c = i.store) == null ? void 0 : c.masterStore) !== e.resourceStore) {
      i.store = g.chainResourceStore();
    }
    if (j) {
      j.min = null;
    }
    g.record = a;
    if (i) {
      const k = f.assignmentStore.getResourcesForEvent(a);
      g.assigningValues = true;
      if (!a.isOccurrence && !f.storage.includes(a, true) && b) {
        e.resourceField.value = b.getValue(e.resourceField.valueField);
      } else if (e.assignmentStore) {
        e.resourceField.value = k.map(l => l.getValue(e.resourceField.valueField));
      }
      g.assigningValues = false;
    }
    super.internalLoadRecord(a, b);
  }
  toggleEventType(a) {
    this.editor.element.dataset.eventType = a || '';
    this.editor.eachWidget(b => {
      let c;
      ((c = b.dataset) == null ? void 0 : c.eventType) && (b.hidden = b.dataset.eventType !== a);
    });
  }
  async finalizeEventSave(a, b, c, e) {
    const f = this, {
        scheduler: g,
        assignmentStore: i
      } = f;
    const j = false;
    i.suspendAutoCommit();
    g.suspendRefresh();
    f.onBeforeSave(a);
    a.beginBatch();
    f.updateRecord(a);
    a.endBatch();
    if (!a.isOccurrence) {
      if (f.resourceField) {
        i.assignEventToResource(a, b, null, true);
      }
    } else if (b) {
      a.set('resourceRecords', b);
    }
    a.isCreating = false;
    if (!j) {
      await g.project.commitAsync();
    }
    i.resumeAutoCommit();
    g.resumeRefresh(true);
    if (!j) {
      g.trigger('afterEventSave', { 'eventRecord': a });
      f.onAfterSave(a);
    }
    c(j ? false : a);
  }
  save() {
    return new Promise((a, b) => {
      let c;
      const e = this, {
          scheduler: f,
          eventRecord: g
        } = e;
      if (!g || !e.editor.isValid) {
        a(false);
        return;
      }
      const {
          eventStore: i,
          values: j
        } = e, k = ((c = e.resourceField) == null ? void 0 : c.records) || (e.resourceRecord ? [e.resourceRecord] : []);
      if (i && f.isDateRangeAvailable) {
        let {
          startDate: p,
          endDate: q
        } = j;
        if (!q) {
          if ('duration' in j) {
            q = DateHelper.add(p, j.duration, j.durationUnit || g.durationUnit);
          } else if ('fullDuration' in j) {
            q = DateHelper.add(p, j.fullDuration);
          } else {
            q = g.endDate;
          }
        }
        const r = f.isDateRangeAvailable(p, q, g, k);
        if (!r) {
          const {startDateField: u} = this.editor.widgetMap;
          if (u) {
            u.setError(e.L('L{EventDrag.eventOverlapsExisting}'), false, true);
            u.focus();
          }
          a(false);
          return;
        }
      }
      const l = {
        'finalize'(v) {
          try {
            if (v !== false) {
              e.finalizeEventSave(g, k, a, b);
            } else {
              a(false);
            }
          } catch (w) {
            b(w);
          }
        }
      };
      const n = f.trigger('beforeEventSave', {
        'eventRecord': g,
        'resourceRecords': k,
        'values': j,
        'context': l
      });
      function o(v, w, z) {
        if (v === false) {
          a(false);
        } else {
          e.onRecurrableEventBeforeSave({
            'eventRecord': w,
            'context': z
          });
          if (!z.async) {
            z.finalize();
          }
        }
      }
      if (ObjectHelper.isPromise(n)) {
        n.then(v => o(v, g, l));
      } else {
        o(n, g, l);
      }
    });
  }
  deleteEvent() {
    this.detachListeners('changesWhileEditing');
    return new Promise((a, b) => {
      const c = this, {
          eventRecord: e,
          editor: f
        } = c;
      c.scheduler.removeEvents([e], g => {
        if (g && f.containsFocus) {
          f.revertFocus();
        }
        a(g);
      }, f);
    });
  }
  onChangeProject() {
    if (this.resourceField) {
      this.resourceField.store = {};
    }
  }
  get eventStore() {
    return this.scheduler.project.eventStore;
  }
  get resourceStore() {
    return this.scheduler.project.resourceStore;
  }
  get assignmentStore() {
    return this.scheduler.project.assignmentStore;
  }
  onActivateEditor({
    eventRecord: a,
    resourceRecord: b,
    eventElement: c,
    domEvent: e
  }) {
    let f;
    const {ignoreSelector: g} = this;
    if (!g || !((f = e.target) == null ? void 0 : f.closest(g))) {
      this.editEvent(a, b, c);
    }
  }
  onDragCreateEnd({
    eventRecord: a,
    resourceRecord: b,
    proxyElement: c,
    stmCapture: e
  }) {
    this.editEvent(a, b, c, e);
  }
  onEventEnterKey({
    assignmentRecord: a,
    eventRecord: b,
    target: c
  }) {
    const {client: e} = this, f = c[c.matches(e.eventSelector) ? 'querySelector' : 'closest'](e.eventInnerSelector);
    if (a) {
      this.editEvent(b, a.resource, f);
    } else if (b) {
      this.editEvent(b, b.resource, f);
    }
  }
  onEventTypeChange({value: a}) {
    this.toggleEventType(a);
  }
  populateEventMenu({
    eventRecord: a,
    resourceRecord: b,
    items: c
  }) {
    if (!this.scheduler.readOnly && !this.disabled) {
      c.editEvent = {
        'text': 'L{EventEdit.Edit event}',
        'localeClass': this,
        'icon': 'b-icon b-icon-edit',
        'weight': 100,
        'disabled': a.readOnly,
        'onItem': () => {
          this.editEvent(a, b);
        }
      };
    }
  }
  onBeforeEditorToggleReveal({reveal: a}) {
    if (a) {
      this.editor.setupEditorButtons();
    }
    this[a ? 'onBeforeEditorShow' : 'resetEditingContext']();
  }
  async resetEditingContext() {
    const a = this;
    a.detachListeners('changesWhileEditing');
    super.resetEditingContext();
    if (a.hasStmCapture && !a.isDeletingEvent && !a.isCancelingEdit) {
      await a.freeStm(false);
    }
    a.resourceRecord = null;
  }
  finalizeStmCapture(a) {
    return this.freeStm(!a);
  }
  updateLocalization() {
    if (this._editor) {
      this.updateCSSVars({ 'element': this._editor.element });
    }
    super.updateLocalization(...arguments);
  }
};
const EventEdit = _EventEdit;
EventEdit._$name = 'EventEdit';
GridFeatureManager.registerFeature(EventEdit, true, 'Scheduler');
GridFeatureManager.registerFeature(EventEdit, false, [
  'SchedulerPro',
  'ResourceHistogram'
]);
EventEdit.initClass();
const EventFilter = class extends InstancePlugin {
  static $name = 'EventFilter';
  static get pluginConfig() {
    return { 'chain': ['populateTimeAxisHeaderMenu'] };
  }
  populateTimeAxisHeaderMenu({items: a}) {
    const b = this;
    a.eventsFilter = {
      'text': 'L{filterEvents}',
      'icon': 'b-fw-icon b-icon-filter',
      'disabled': b.disabled,
      'localeClass': b,
      'weight': 100,
      'menu': {
        'localeClass': b,
        'cls': 'b-eventfilter-menu',
        'items': {
          'nameFilter': {
            'weight': 110,
            'type': 'textfield',
            'cls': 'b-eventfilter b-last-row',
            'clearable': true,
            'keyStrokeChangeDelay': 300,
            'label': 'L{byName}',
            'localeClass': b,
            'width': 200,
            'internalListeners': {
              'change': b.onEventFilterChange,
              'thisObj': b
            }
          }
        },
        'onBeforeShow'({source: c}) {
          const [e] = c.items, f = b.store.filters.getBy('property', 'name');
          e.value = (f == null ? void 0 : f.value) || '';
        }
      }
    };
  }
  onEventFilterChange({value: a}) {
    if (a !== '') {
      this.store.filter('name', a);
    } else {
      this.store.removeFilter('name');
    }
  }
  get store() {
    const {client: a} = this;
    return a.isGanttBase ? a.store : a.eventStore;
  }
};
EventFilter.featureClass = 'b-event-filter';
EventFilter._$name = 'EventFilter';
GridFeatureManager.registerFeature(EventFilter, true, [
  'Scheduler',
  'Gantt'
]);
GridFeatureManager.registerFeature(EventFilter, false, 'ResourceHistogram');
const EventMenu = class extends TimeSpanMenuBase {
  static get $name() {
    return 'EventMenu';
  }
  static get configurable() {
    return {
      'processItems': null,
      'type': 'event'
    };
  }
  static get pluginConfig() {
    const a = super.pluginConfig;
    a.chain.push('populateEventMenu');
    return a;
  }
  get resourceStore() {
    return this.client.isHorizontal ? this.client.store : this.client.resourceStore;
  }
  getDataFromEvent(a) {
    let b;
    const c = super.getDataFromEvent(a), e = c.targetElement, {client: f} = this, g = f.resolveEventRecord(e), i = g && ((b = f.resolveResourceRecord(e) || this.resourceStore.last) == null ? void 0 : b.$original), j = g && f.resolveAssignmentRecord(e);
    return Object.assign(c, {
      'date': f.getDateFromXY([
        a.pageX,
        a.pageY
      ], null, false),
      'eventElement': e,
      'eventRecord': g,
      'resourceRecord': i,
      'assignmentRecord': j
    });
  }
  getTargetElementFromEvent({target: a}) {
    return a.closest(this.client.eventSelector) || a;
  }
  shouldShowMenu(a) {
    return a.eventRecord;
  }
  showContextMenuFor(a, {targetElement: b} = {}) {
    if (this.disabled) {
      return;
    }
    if (!b) {
      b = this.getElementFromRecord(a);
      if (!b) {
        return;
      }
    }
    DomHelper.triggerMouseEvent(b, this.triggerEvent);
  }
  getElementFromRecord(a) {
    return this.client.getElementsFromEventRecord(a)[0];
  }
  populateEventMenu({
    items: a,
    eventRecord: b,
    assignmentRecord: c
  }) {
    const {client: e} = this;
    a.deleteEvent = {
      'disabled': b.readOnly || (c == null ? void 0 : c.readOnly),
      'hidden': e.readOnly
    };
    a.unassignEvent = {
      'disabled': b.readOnly || (c == null ? void 0 : c.readOnly),
      'hidden': e.readOnly || e.eventStore.usesSingleAssignment
    };
    if (e.showEventColorPickers || e.showTaskColorPickers) {
      a.eventColor = {
        'disabled': b.readOnly,
        'hidden': e.readOnly
      };
    } else {
      a.eventColor = { 'hidden': true };
    }
  }
  populateItemsWithData({
    items: a,
    eventRecord: b
  }) {
    let c;
    super.populateItemsWithData(...arguments);
    const {client: e} = this;
    if ((e.showEventColorPickers || e.isSchedulerPro && e.showTaskColorPickers) && ((c = a.eventColor) == null ? void 0 : c.menu)) {
      Objects.merge(a.eventColor.menu.colorMenu, {
        'value': b.eventColor,
        'record': b
      });
    }
  }
  changeItems(a) {
    const {client: b} = this;
    return Objects.merge({
      'deleteEvent': {
        'text': 'L{SchedulerBase.Delete event}',
        'icon': 'b-icon b-icon-trash',
        'cls': 'b-separator',
        'weight': 500,
        'onItem'({
          menu: c,
          eventRecord: e
        }) {
          let f;
          const g = (f = c.focusInEvent) == null ? void 0 : f.relatedTarget;
          if (g) {
            g.focus();
            b.navigator.activeItem = g;
          }
          b.removeEvents(b.isEventSelected(e) ? b.selectedEvents : [e]);
        }
      },
      'unassignEvent': {
        'text': 'L{SchedulerBase.Unassign event}',
        'icon': 'b-icon b-icon-unassign',
        'weight': 300,
        'onItem'({
          menu: c,
          eventRecord: e,
          resourceRecord: f
        }) {
          let g;
          const i = (g = c.focusInEvent) == null ? void 0 : g.relatedTarget;
          if (i) {
            i.focus();
            b.navigator.activeItem = i;
          }
          if (b.isEventSelected(e)) {
            b.assignmentStore.remove(b.selectedAssignments);
          } else {
            e.unassign(f);
          }
        }
      },
      'eventColor': {
        'text': 'L{SchedulerBase.color}',
        'icon': 'b-icon b-icon-palette',
        'separator': true,
        'menu': { 'colorMenu': { 'type': 'eventcolorpicker' } }
      }
    }, a);
  }
};
EventMenu.featureClass = '';
EventMenu._$name = 'EventMenu';
GridFeatureManager.registerFeature(EventMenu, true, 'Scheduler');
GridFeatureManager.registerFeature(EventMenu, false, 'ResourceHistogram');
const NonWorkingTimeMixin_default = a => {
  let b;
  return b = class extends a {
    getNonWorkingTimeRanges(c, e, f) {
      const g = f.getTime() - e.getTime() + 24 * 60 * 60 * 1000;
      if (!c.getNonWorkingTimeRanges) {
        const i = [];
        c.forEachAvailabilityInterval({
          'startDate': e,
          'endDate': f,
          'isForward': true,
          'maxRange': g
        }, (j, k, l) => {
          for (const [n, o] of l.intervalGroups) {
            if (!o.getIsWorking()) {
              i.push({
                'name': n.name,
                'iconCls': n.iconCls,
                'cls': n.cls,
                'type': n.type,
                'startDate': j,
                'endDate': k
              });
            }
          }
        });
        return i;
      }
      return c.getNonWorkingTimeRanges(e, f, g);
    }
    getCalendarTimeRanges(c, e = false) {
      const f = this, {
          timeAxis: g,
          fillTicks: i
        } = f.client, {
          unit: j,
          increment: k
        } = g, l = !f.maxTimeAxisUnit || DateHelper.compareUnits(j, f.maxTimeAxisUnit) <= 0;
      if (c && l && g.count) {
        const n = f.getNonWorkingTimeRanges(c, g.startDate, g.endDate), o = n.map(r => new TimeSpan({
            'name': r.type !== 'Week' ? r.name : '',
            'cls': 'b-nonworkingtime ' + (r.cls || ''),
            'startDate': r.startDate,
            'endDate': r.endDate
          }));
        let p = [];
        let q = null;
        for (const r of o) {
          if (q && r.startDate <= q.endDate && (e || r.name === q.name) && r.duration > 0) {
            q.endDate = r.endDate;
          } else {
            p.push(r);
            r.setData('id', 'nonworking-' + p.length);
            q = r;
          }
        }
        if (i) {
          p = p.filter(u => {
            return !DateHelper.isSameDate(u.startDate, u.endDate) && DateHelper.ceil(u.startDate, {
              'magnitude': k,
              'unit': j
            }) < DateHelper.floor(u.endDate, {
              'magnitude': k,
              'unit': j
            });
          });
          p.forEach(u => u.setStartEndDate(DateHelper.ceil(u.startDate, {
            'magnitude': k,
            'unit': j
          }), DateHelper.floor(u.endDate, {
            'magnitude': k,
            'unit': j
          })));
        }
        return p;
      } else {
        return [];
      }
    }
    setupDefaultCalendar() {
      const {
        client: c,
        project: e
      } = this;
      if (!this.autoGeneratedWeekends && !c.isSchedulerPro && !c.isGantt && e.effectiveCalendar === e.defaultCalendar && !e.defaultCalendar.intervalStore.count) {
        this.autoGeneratedWeekends = true;
        this.updateDefaultCalendar();
      }
    }
    updateDefaultCalendar() {
      if (this.autoGeneratedWeekends) {
        const c = this.client.project.effectiveCalendar, e = this.defaultNonWorkingIntervals, f = Boolean(e.length);
        c.ignoreTimeZone = true;
        c.clearIntervals(f);
        if (f) {
          c.addIntervals(e);
        }
      }
    }
    updateLocalization() {
      let c;
      (c = super.updateLocalization) == null ? void 0 : c.call(this);
      this.autoGeneratedWeekends && this.updateDefaultCalendar();
    }
    get defaultNonWorkingIntervals() {
      const c = [
        'Sun',
        'Mon',
        'Tue',
        'Wed',
        'Thu',
        'Fri',
        'Sat'
      ];
      return DateHelper.nonWorkingDaysAsArray.map(e => ({
        'recurrentStartDate': 'on ' + c[e] + ' at 0:00',
        'recurrentEndDate': 'on ' + c[(e + 1) % 7] + ' at 0:00',
        'isWorking': false
      }));
    }
  }, __publicField(b, '$name', 'NonWorkingTimeMixin'), __publicField(b, 'configurable', { 'maxTimeAxisUnit': 'week' }), b;
};
const EventNonWorkingTime = class extends InstancePlugin.mixin(AttachToProjectMixin_default, NonWorkingTimeMixin_default) {
  static $name = 'EventNonWorkingTime';
  static get pluginConfig() {
    return {
      'chain': [
        'onTimeAxisViewModelUpdate',
        'updateLocalization',
        'onEventDataGenerated'
      ]
    };
  }
  static get properties() {
    return { 'calendarRangeCache': new Map() };
  }
  doDisable(a) {
    this.client.refresh();
    super.doDisable(a);
  }
  attachToCalendarManagerStore(a) {
    super.attachToCalendarManagerStore(a);
    const b = this;
    a.ion({
      'name': 'calendarManagerStore',
      'change': 'onCalendarChange',
      'thisObj': b
    });
    if (b.client.isScheduler) {
      b.project.effectiveCalendar.intervals.ion({
        'name': 'calendarManagerStore',
        'change': 'onCalendarChange',
        'thisObj': b
      });
    }
    b.setupDefaultCalendar();
  }
  onCalendarChange() {
    this.calendarRangeCache.clear();
  }
  onTimeAxisViewModelUpdate() {
    this.calendarRangeCache.clear();
  }
  getRangeDomConfig(a, b, c, e) {
    const f = this, {client: g} = f, i = g.getVirtualCoordFromDate(DateHelper.max(a.startDate, b)) - e, j = a.endDate ? g.getVirtualCoordFromDate(DateHelper.min(a.endDate, c)) - e : i, k = Math.abs(j - i), l = g.isVertical ? 'insetBlockStart' : 'insetInlineStart', n = g.isVertical ? 'height' : 'width';
    return {
      'className': {
        'b-sch-timerange': 1,
        'b-sch-range': 1,
        'b-sch-nonworkingtime': 1,
        [a.cls]: a.cls,
        'b-rtl': g.rtl
      },
      'dataset': { 'id': a.id },
      'elementData': { 'timeRange': a },
      'style': {
        [l]: i,
        [n]: k > 0 ? k : null
      }
    };
  }
  getCalendarTimeRanges(a) {
    let b = this.calendarRangeCache.get(a);
    if (!b) {
      b = super.getCalendarTimeRanges(a, true);
      this.calendarRangeCache.set(a, b);
    }
    return b;
  }
  onEventDataGenerated({
    eventRecord: a,
    endMS: b,
    startMS: c,
    start: e,
    end: f,
    left: g,
    top: i,
    children: j
  }) {
    let k;
    if (this.enabled) {
      const {isVertical: l} = this.client, n = (k = a.effectiveCalendarsCombination) != null ? k : this.client.project.calendar;
      for (const o of this.getCalendarTimeRanges(n)) {
        if (o.startDateMS < b && o.endDateMS > c) {
          const p = this.getRangeDomConfig(o, e, f, l ? i : g);
          j.push(p);
        }
      }
    }
  }
};
EventNonWorkingTime._$name = 'EventNonWorkingTime';
GridFeatureManager.registerFeature(EventNonWorkingTime, false, 'Scheduler');
const zeroOffset = [
  0,
  0
];
const depOffset = [
  null,
  [
    0,
    10
  ],
  [
    10,
    0
  ]
];
const EventTooltip = class extends TooltipBase {
  static get $name() {
    return 'EventTooltip';
  }
  static get defaultConfig() {
    return {
      'template': a => '\n                ' + (a.eventRecord.name ? StringHelper.xss`<div class="b-sch-event-title">${ a.eventRecord.name }</div>` : '') + '\n                ' + a.startClockHtml + '\n                ' + a.endClockHtml,
      'cls': 'b-sch-event-tooltip',
      'monitorRecordUpdate': true,
      'scrollAction': 'hide'
    };
  }
  construct(a, b) {
    super.construct(a, b);
    if (typeof this.align === 'string') {
      this.align = { 'align': this.align };
    }
  }
  onInternalPaint({firstPaint: a}) {
    super.onInternalPaint(...arguments);
    if (a) {
      const {dependencies: b} = this.client.features;
      if (b) {
        this.tooltip.ion({
          'beforeAlign'({
            source: c,
            offset: offset = zeroOffset
          }) {
            const {edgeAligned: e} = parseAlign(c.align.align), f = b.disabled || !b.allowCreate ? zeroOffset : depOffset[e];
            arguments[0].offset = [
              offset[0] + f[0],
              offset[1] + f[1]
            ];
          }
        });
      }
    }
  }
};
EventTooltip._$name = 'EventTooltip';
GridFeatureManager.registerFeature(EventTooltip, true, 'Scheduler');
GridFeatureManager.registerFeature(EventTooltip, false, 'ResourceHistogram');
const eventStoreRefreshActions = {
  'update': 1,
  'filter': 1
};
const assignmentStoreRefreshActions = {
  'add': 1,
  'remove': 1,
  'update': 1,
  'removeAll': 1,
  'filter': 1
};
const GroupSummary2 = class extends GroupSummary {
  static configurable = {
    'showTooltip': true,
    'summaries': null,
    'renderer': null
  };
  static $name = 'GroupSummary';
  static get properties() {
    return { 'footersToUpdate': new Set() };
  }
  static get pluginConfig() {
    return { 'chain': ['render'] };
  }
  construct(a, b) {
    const c = this;
    if (a.isVertical) {
      throw new Error('GroupSummary feature is not supported in vertical mode');
    }
    c.scheduler = a;
    super.construct(a, b);
    if (!c.summaries && c.renderer) {
      c.summaries = [{ 'renderer': c.renderer }];
    }
    if (a.isSchedulerBase) {
      a.eventStore.ion({
        'changePreCommit': c.onEventStoreChange,
        'thisObj': c
      });
      a.assignmentStore.ion({
        'changePreCommit': c.onAssignmentStoreChange,
        'thisObj': c
      });
      a.ion({
        'timeAxisViewModelUpdate': c.onTimeAxisChange,
        'thisObj': c
      });
      a.project.ion({
        'dataReady': c.onProjectDataReady,
        'thisObj': c
      });
    }
  }
  doDestroy() {
    let a;
    (a = this._tip) == null ? void 0 : a.destroy();
    super.doDestroy();
  }
  onTimeAxisChange() {
    this.scheduler.rowManager.forEach(a => {
      if (a.isGroupFooter) {
        a.render();
      }
    });
  }
  onEventStoreChange({
    action: a,
    records: b,
    changes: c
  }) {
    if (eventStoreRefreshActions[a]) {
      const e = new Set();
      b.forEach(f => f.resources.forEach(g => e.add(g)));
      this.afterChange(e);
    }
  }
  onAssignmentStoreChange({
    action: a,
    records: b,
    changes: c
  }) {
    let e;
    if (assignmentStoreRefreshActions[a]) {
      const f = new Set();
      b.forEach(g => g.resource && f.add(g.resource));
      if (((e = c == null ? void 0 : c.resourceId) == null ? void 0 : e.oldValue) != null) {
        const g = this.scheduler.resourceStore.getById(c.resourceId.oldValue);
        g && f.add(g);
      }
      this.afterChange(f);
    }
  }
  afterChange(a) {
    const {resourceStore: b} = this.scheduler;
    for (const c of a) {
      let e = c;
      if (this.target === 'header') {
        while (e && !e.isGroupHeader) {
          e = b.getPrev(e);
        }
      } else {
        while (e && !e.isGroupFooter) {
          e = b.getNext(e);
        }
      }
      const f = this.scheduler.rowManager.getRowFor(e);
      if (e && f) {
        this.footersToUpdate.add(f);
      }
    }
  }
  onProjectDataReady() {
    let a;
    const {
      footersToUpdate: b,
      client: c
    } = this;
    if (c.isVisible) {
      if (b.size) {
        for (const e of b) {
          (a = e.render) == null ? void 0 : a.call(e);
        }
        b.clear();
      }
    } else {
      c.whenVisible('refresh', c, [true]);
    }
  }
  onBeforeRenderRow({
    row: a,
    record: b
  }) {
    if (a.isGroupFooter && !b.meta.hasOwnProperty('groupFooterFor')) {
      const c = a.elements.normal.querySelector('.b-sch-timeaxis-cell');
      if (c) {
        c.innerHTML = '';
      }
    }
    super.onBeforeRenderRow(...arguments);
  }
  generateHtml(a, b, c, e, f, g) {
    if (a.type === 'timeAxis') {
      const i = this, {scheduler: j} = i, {eventStore: k} = j, l = j.timeAxisViewModel.columnConfig;
      let n = '';
      const o = j.getResourcesEventsPerTick(b, ({event: p}) => {
        return p.resources.some(q => b.includes(q)) && (!k.isFiltered || k.records.includes(p));
      });
      j.timeAxis.forEach((p, q) => {
        const r = o[q] || [], u = i.summaries.map(v => {
            let w, z;
            const A = (z = (w = v.renderer) == null ? void 0 : w.call(v, {
              'startDate': p.startDate,
              'endDate': p.endDate,
              'resourceStore': j.resourceStore,
              'events': r,
              'resources': b,
              'eventStore': k,
              'groupRecord': e,
              'groupField': f,
              'groupValue': g
            })) != null ? z : '';
            return '<div class="b-timeaxis-summary-value">' + A + '</div>';
          }).join('');
        n += '<div class="b-timeaxis-tick" style="width: ' + l[l.length - 1][q].width + 'px">' + u + '</div>';
      });
      return '<div class="b-timeaxis-group-summary">' + n + '</div>';
    }
    return super.generateHtml(a, b, c, e, f, g);
  }
  updateSummaryHtml(a, b, c) {
    const e = super.updateSummaryHtml(a, b, c);
    if (b.type === 'timeAxis') {
      const f = {
        'count': 0,
        'height': 0
      };
      this.summaries.forEach(g => {
        if (g.height) {
          f.height += g.height;
        } else {
          f.count++;
        }
      });
      return f;
    }
    return e;
  }
  getTipHtml({activeTarget: a}) {
    const b = this, {client: c} = b, e = [], f = Array.from(a.parentElement.children).indexOf(a), g = c.timeAxis.getAt(f), i = c.getRecordFromElement(a), j = i.isGroupFooter ? i.meta.groupRecord : i, {eventStore: k} = c, l = j.groupChildren, n = c.getResourcesEventsPerTick(l, ({event: q}) => {
        return q.resources.some(r => l.includes(r)) && (!k.isFiltered || k.includes(q));
      });
    let o = '', p = false;
    DomHelper.forEachSelector(a, '.b-timeaxis-summary-value', (q, r) => {
      const u = b._labels[r], v = q.innerText.trim();
      e.push({
        'label': u,
        'text': v
      });
      if (q.innerHTML) {
        p = true;
      }
    });
    o = b.tooltipTemplate({
      'groupSummaryValues': e,
      'startDate': g.startDate,
      'endDate': g.endDate,
      'resourceStore': c.resourceStore,
      'events': n[f] || [],
      'resources': l,
      'eventStore': k,
      'groupRecord': j,
      'groupField': j.meta.groupField,
      'groupValue': j.meta.groupRowFor
    });
    return p ? o : null;
  }
  tooltipTemplate({
    groupSummaryValues: a,
    startDate: b,
    endDate: c,
    events: e,
    resources: f,
    eventStore: g,
    resourceStore: i,
    groupRecord: j,
    groupField: k,
    groupValue: l
  }) {
    const {client: n} = this;
    let o = '<header>' + this.L('L{Summary.Summary for}', n.getFormattedDate(b)) + '</header>';
    a.forEach(({
      label: label = '',
      text: p
    }) => {
      o += '<label>' + label + '</label><div class="b-timeaxis-summary-value">' + p + '</div>';
    });
    return o;
  }
  render() {
    let a;
    const b = this, {scheduler: c} = b;
    if (c.isSchedulerBase) {
      if (((a = b.summaries) == null ? void 0 : a.some(e => e.label)) && b.showTooltip && !b._tip) {
        b._labels = b.summaries.map(e => e.label || '');
        b._tip = new Tooltip({
          'id': c.id + '-groupsummary-tip',
          'cls': 'b-timeaxis-summary-tip',
          'hoverDelay': 0,
          'hideDelay': 0,
          'forElement': c.timeAxisSubGridElement,
          'anchorToTarget': true,
          'forSelector': '.b-timeaxis-group-summary .b-timeaxis-tick',
          'getHtml': b.getTipHtml.bind(b)
        });
      }
    }
  }
  removeSummaryElements(a) {
    const b = a.querySelector('.b-timeaxis-group-summary');
    b == null ? void 0 : b.remove();
    super.removeSummaryElements();
  }
  hasSummary(a) {
    return super.hasSummary(a) || a.isTimeAxisColumn;
  }
};
GroupSummary2._$name = 'GroupSummary';
GridFeatureManager.registerFeature(GroupSummary2, false, 'Scheduler');
const HeaderDragSelect_default = a => {
  let b;
  return b = class extends (a || Base) {
    _dragging = false;
    moveThreshold = 3;
    constructor() {
      super(...arguments);
    }
    construct() {
      super.construct(...arguments);
      this.chain(this.client, 'onElementMouseDown', 'onElementMouseDown');
      this.chain(this.client, 'onElementMouseMove', 'onElementMouseMove');
    }
    onElementMouseDown(c) {
      const e = this;
      if (c.touches || c.button !== 0 || e.disabled || !e.enableDragSelect) {
        return;
      }
      if (c.target.closest('.b-sch-header-timeaxis-cell')) {
        e.startX = c.clientX;
        EventHelper.on({
          'element': DomHelper.getRootElement(c.target),
          'mouseup': 'onMouseUp',
          'thisObj': e,
          'once': true
        });
      }
    }
    onElementMouseMove(c) {
      const e = this;
      if (typeof e.startX === 'number') {
        if (!e._dragging && Math.abs(e.startX - c.clientX) >= e.moveThreshold) {
          const f = e.client.subGrids.normal.header.headersElement;
          e.element = DomHelper.createElement({
            'parent': f,
            'tag': 'div',
            'className': 'b-header-drag-selection-rect b-headerzoom-rect'
          });
          e.headerElementRect = Rectangle.from(f);
          e._dragging = true;
        }
        if (e.isDragging) {
          const g = Math.max(c.clientX, e.headerElementRect.left), i = Math.min(e.startX, g), j = Math.abs(e.startX - g), k = new Rectangle(i - e.headerElementRect.x + e.client.scrollLeft, 0, j, e.headerElementRect.height);
          DomHelper.setTranslateX(e.element, k.left);
          e.element.style.width = k.width + 'px';
        }
      }
    }
    onMouseUp() {
      let c;
      const e = this;
      e.startX = null;
      if (e.isDragging) {
        const {client: f} = e, g = Rectangle.from(e.element), i = f.getDateFromCoordinate(g.left, 'round', false), j = f.getDateFromCoordinate(g.right, 'round', false);
        e.element.remove();
        EventHelper.on({
          'element': DomHelper.getRootElement(f.element),
          'thisObj': e,
          'click': k => k.stopPropagation(),
          'capture': true,
          'expires': 50,
          'once': true
        });
        e._dragging = null;
        (c = e.onTimeSpanDragSelected) == null ? void 0 : c.call(e, {
          'startDate': i,
          'endDate': j
        });
      }
    }
    get isDragging() {
      return this._dragging;
    }
  }, __publicField(b, 'configurable', { 'enableDragSelect': true }), b;
};
const HeaderZoom = class extends InstancePlugin.mixin(HeaderDragSelect_default) {
  static $name = 'HeaderZoom';
  onTimeSpanDragSelected({
    startDate: a,
    endDate: b
  }) {
    this.client.zoomToSpan({
      'startDate': a,
      'endDate': b
    });
  }
};
HeaderZoom._$name = 'HeaderZoom';
GridFeatureManager.registerFeature(HeaderZoom, false, 'Scheduler');
const sides = [
  'top',
  'before',
  'after',
  'bottom'
];
const editorAlign = (a, b) => {
  switch (a) {
  case 'top':
    return 'b-b';
  case 'after':
    return b.rtl ? 'r-r' : 'l-l';
  case 'right':
    return 'l-l';
  case 'bottom':
    return 't-t';
  case 'before':
    return b.rtl ? 'l-l' : 'r-r';
  case 'left':
    return 'r-r';
  }
};
const topBottom = {
  'top': 1,
  'bottom': 1
};
const layoutModes = {
  'estimate': 1,
  'measure': 1
};
const layoutSides = {
  'before': 1,
  'after': 1
};
const Labels = class extends InstancePlugin {
  static configurable = {
    'labelCls': 'b-sch-label',
    'top': null,
    'after': null,
    'right': null,
    'bottom': null,
    'before': null,
    'left': null,
    'thisObj': null,
    'blurAction': 'cancel',
    'labelLayoutMode': 'default',
    'labelCharWidth': 7
  };
  static $name = 'Labels';
  static get pluginConfig() {
    return { 'chain': ['onEventDataGenerated'] };
  }
  construct(a, b) {
    const c = this;
    if (a.isVertical) {
      throw new Error('Labels feature is not supported in vertical mode');
    }
    c.scheduler = a;
    super.construct(a, b);
    if (c.top || c.bottom || c.before || c.after) {
      c.updateHostClasslist();
    }
  }
  updateHostClasslist() {
    const {
        top: a,
        bottom: b
      } = this, {classList: c} = this.scheduler.element;
    c.remove('b-labels-topbottom');
    c.remove('b-labels-top');
    c.remove('b-labels-bottom');
    if (a || b) {
      c.add('b-labels-topbottom');
      if (a) {
        c.add('b-labels-top');
      }
      if (b) {
        c.add('b-labels-bottom');
      }
    }
  }
  onLabelDblClick(a) {
    const b = this, c = a.target;
    if (c && !b.scheduler.readOnly) {
      const {side: e} = c.dataset, f = b[e], {
          editor: g,
          field: i
        } = f;
      if (g) {
        const j = this.scheduler.resolveEventRecord(a.target);
        if (j.readOnly) {
          return;
        }
        if (!(g instanceof Editor)) {
          f.editor = new Editor({
            'blurAction': b.blurAction,
            'inputField': g,
            'scrollAction': 'realign'
          });
        }
        f.editor.render(b.scheduler.element);
        f.editor.startEdit({
          'target': c,
          'align': editorAlign(e, b.client),
          'matchSize': false,
          'record': j,
          'field': i
        });
        a.stopImmediatePropagation();
        return false;
      }
    }
  }
  changeTop(a) {
    return this.processLabelSpec(a, 'top');
  }
  updateTop() {
    this.updateHostClasslist();
  }
  changeAfter(a) {
    return this.processLabelSpec(a, 'after');
  }
  updateAfter() {
    this.updateHostClasslist();
  }
  changeRight(a) {
    this[this.client.rtl ? 'before' : 'after'] = a;
  }
  changeBottom(a) {
    return this.processLabelSpec(a, 'bottom');
  }
  updateBottom() {
    this.updateHostClasslist();
  }
  changeBefore(a) {
    return this.processLabelSpec(a, 'before');
  }
  updateBefore() {
    this.updateHostClasslist();
  }
  changeLeft(a) {
    this[this.client.rtl ? 'after' : 'before'] = a;
  }
  processLabelSpec(a, b) {
    if (typeof a === 'function') {
      a = { 'renderer': a };
    } else if (typeof a === 'string') {
      a = { 'field': a };
    } else if (a) {
      a = Object.setPrototypeOf({}, a);
    } else {
      return null;
    }
    const {scheduler: c} = this, {
        eventStore: e,
        resourceStore: f,
        taskStore: g,
        id: i
      } = c, {
        field: j,
        editor: k
      } = a;
    if (topBottom[b]) {
      c.milestoneWidth = null;
    }
    if (e && !g) {
      a.recordType = 'event';
    } else {
      a.recordType = 'task';
    }
    if (j) {
      let l, n = false;
      if (e && !g) {
        l = e.modelClass.fieldMap[j];
        if (l) {
          a.fieldDef = l;
          a.recordType = 'event';
          n = true;
        } else if (Reflect.has(e.modelClass.prototype, j)) {
          a.recordType = 'event';
          n = true;
        }
      }
      if (!l && g) {
        l = g.modelClass.fieldMap[j];
        if (l) {
          a.fieldDef = l;
          a.recordType = 'task';
          n = true;
        } else if (Reflect.has(f.modelClass.prototype, j)) {
          a.recordType = 'task';
          n = true;
        }
      }
      if (!l && f) {
        l = f.modelClass.fieldMap[j];
        if (l) {
          a.fieldDef = l;
          a.recordType = 'resource';
          n = true;
        } else if (Reflect.has(f.modelClass.prototype, j)) {
          a.recordType = 'resource';
          n = true;
        }
      }
      if (k && !this.editListenerAdded) {
        EventHelper.on({
          'element': c.timeAxisSubGrid.element,
          'delegate': '.b-sch-label',
          'dblclick': 'onLabelDblClick',
          'thisObj': this
        });
        this.editListenerAdded = true;
      }
    }
    return a;
  }
  doDisable(a) {
    super.doDisable(a);
    if (this.client.isPainted) {
      this.client.refresh();
    }
  }
  generateLabelConfigs(a) {
    let b;
    const c = this, e = [];
    for (const f of sides) {
      if (c[f]) {
        const {
            field: g,
            fieldDef: i,
            recordType: j,
            renderer: k,
            thisObj: l
          } = c[f], n = {
            'tag': 'label',
            'className': {
              [c.labelCls]: 1,
              [c.labelCls + '-' + f]: 1
            },
            'dataset': {
              'side': f,
              'taskFeature': 'label-' + f
            }
          };
        let o;
        const p = j + 'Record', q = a[p];
        if (k) {
          o = k.call(l || c.thisObj || c, {
            [p]: q,
            'resourceRecord': a.resourceRecord,
            'assignmentRecord': a.assignmentRecord,
            'domConfig': n
          });
          if (typeof o === 'object') {
            Object.assign(n, o);
            o = null;
          }
        } else {
          o = q.getValue(g);
          if ((i == null ? void 0 : i.type) === 'date' && !k) {
            o = DateHelper.format(o, c.client.displayDateFormat);
          } else {
            o = StringHelper.encodeHtml(o);
          }
        }
        if (o != null && o !== false) {
          n.html = o;
        }
        if (n.html != null || ((b = n.children) == null ? void 0 : b.length) || n.text != null) {
          e.push(n);
        }
      }
    }
    return e;
  }
  measureLabels(a, b) {
    const c = this, e = c.client.timeAxisViewModel.getSingleUnitInPixels('millisecond');
    for (const f of a) {
      if (layoutSides[f.dataset.side]) {
        let {html: g} = f;
        let i = 0;
        if (c.labelLayoutMode === 'estimate') {
          if (g.includes('<')) {
            g = DomHelper.stripTags(g);
          }
          i = g.length * c.labelCharWidth + 18;
        } else {
          const k = c.labelMeasureElement || (c.labelMeasureElement = DomHelper.createElement({
            'className': 'b-sch-event-wrap b-measure-label',
            'parent': c.client.foregroundCanvas
          }));
          k.retainElement = true;
          DomSync.sync({
            'targetElement': k,
            'domConfig': {
              'onlyChildren': true,
              'children': [f]
            }
          });
          i = k.firstElementChild.offsetWidth;
        }
        const j = i / e;
        switch (f.dataset.side) {
        case 'before':
          b.startMS -= j;
          break;
        case 'after':
          b.endMS += j;
          break;
        }
      }
    }
  }
  onEventDataGenerated(a) {
    let b;
    if (!this.disabled && !((b = a.eventRecord) == null ? void 0 : b.isResourceTimeRange)) {
      const c = this.generateLabelConfigs(a);
      if (layoutModes[this.labelLayoutMode]) {
        this.measureLabels(c, a);
      }
      a.wrapperChildren.push(...c);
    }
  }
  updateLabelLayoutMode() {
    if (!this.isConfiguring) {
      this.client.refreshWithTransition();
    }
  }
  updateLabelCharWidth() {
    if (!this.isConfiguring) {
      this.client.refreshWithTransition();
    }
  }
};
Labels.featureClass = 'b-sch-labels';
Labels._$name = 'Labels';
GridFeatureManager.registerFeature(Labels, false, 'Scheduler');
const NonWorkingTime = class extends AbstractTimeRanges.mixin(AttachToProjectMixin_default, NonWorkingTimeMixin_default) {
  static $name = 'NonWorkingTime';
  static get defaultConfig() {
    return {
      'hideRangesOnZooming': true,
      'showHeaderElements': true,
      'showLabelInBody': true,
      'autoGeneratedWeekends': false
    };
  }
  static get pluginConfig() {
    const a = super.pluginConfig;
    a.chain.push('attachToProject', 'onConfigChange');
    return a;
  }
  doDestroy() {
    this.attachToCalendar(null);
    super.doDestroy();
  }
  onConfigChange({name: a}) {
    if (!this.isConfiguring && a === 'fillTicks') {
      this.refresh();
    }
  }
  attachToProject(a) {
    super.attachToProject(a);
    this.attachToCalendar(a.effectiveCalendar);
    if (!a.graph && !this.client.isScheduler) {
      a.ion({
        'name': 'project',
        'dataReady': {
          'fn': () => this.attachToCalendar(a.effectiveCalendar),
          'once': true
        },
        'thisObj': this
      });
    }
    a.ion({
      'name': 'project',
      'calendarChange': () => this.attachToCalendar(a.effectiveCalendar),
      'thisObj': this
    });
  }
  onTimeAxisViewModelUpdate(...a) {
    this._timeAxisUnitDurationMs = null;
    return super.onTimeAxisViewModelUpdate(...a);
  }
  attachToCalendar(a) {
    const b = this, {
        project: c,
        client: e
      } = b;
    b.detachListeners('calendar');
    b.autoGeneratedWeekends = false;
    if (a) {
      b.setupDefaultCalendar();
      a.intervalStore.ion({
        'name': 'calendar',
        'change': () => b.setTimeout(() => b.refresh(), 1),
        'thisObj': b
      });
    }
    if (e.isEngineReady && !e.project.isDelayingCalculation && !e.isDestroying) {
      b.refresh();
    } else if (!c.isDestroyed) {
      b.detachListeners('initialProjectListener');
      c.ion({
        'name': 'initialProjectListener',
        'refresh'({isCalculated: f}) {
          if (f !== false) {
            b.refresh();
            b.detachListeners('initialProjectListener');
          }
        },
        'thisObj': b
      });
    }
  }
  get calendar() {
    let a;
    return (a = this.project) == null ? void 0 : a.effectiveCalendar;
  }
  get timeAxisUnitDurationMs() {
    if (!this._timeAxisUnitDurationMs) {
      this._timeAxisUnitDurationMs = DateHelper.as('ms', 1, this.client.timeAxis.unit);
    }
    return this._timeAxisUnitDurationMs;
  }
  shouldRenderRange(a, b, c) {
    return super.shouldRenderRange(a, b, c) && (!this.hideRangesOnZooming || a.durationMS >= this.timeAxisUnitDurationMs);
  }
  get timeRanges() {
    const a = this;
    if (!a._timeRanges) {
      a._timeRanges = a.getCalendarTimeRanges(a.calendar);
    }
    return a._timeRanges;
  }
};
NonWorkingTime._$name = 'NonWorkingTime';
GridFeatureManager.registerFeature(NonWorkingTime, false, 'Scheduler');
GridFeatureManager.registerFeature(NonWorkingTime, true, [
  'SchedulerPro',
  'Gantt',
  'ResourceHistogram'
]);
const Pan = class extends InstancePlugin {
  static configurable = {
    'horizontal': true,
    'vertical': true,
    'enableInHeader': true
  };
  static $name = 'Pan';
  get targetSelector() {
    return '.b-sch-timeaxis-cell,.b-tasknonworkingtime,.b-sch-resourcetimerange,.b-timeline-subgrid' + (this.enableInHeader ? ',.b-sch-header-timeaxis-cell,.b-sch-header-text' : '');
  }
  static get pluginConfig() {
    return { 'chain': ['onElementMouseDown'] };
  }
  onElementMouseDown(a) {
    let b;
    const c = this, {client: e} = c, {
        button: f,
        touches: g,
        target: i,
        clientX: j,
        clientY: k
      } = a, l = e.features.taskDrag || e.features.eventDrag, n = e.readOnly || !(l == null ? void 0 : l.enabled);
    if (g || f !== 0 || c.disabled || e.trigger('beforePan', { 'event': a }) === false) {
      return;
    }
    if (i.matches(c.targetSelector) || n && i.closest(e.eventSelector)) {
      c.mouseX = j;
      c.mouseY = k;
      c.onHeader = c.enableInHeader && i.closest('.b-sch-header-timeaxis-cell');
      (b = c.mouseDetacher) == null ? void 0 : b.call(c);
      c.mouseDetacher = EventHelper.on({
        'element': DomHelper.getRootElement(e.element),
        'mousemove': 'onMouseMove',
        'mouseup': 'onMouseUp',
        'thisObj': c
      });
    }
  }
  onMouseMove(a) {
    const b = this, {client: c} = b, e = c.timeAxisSubGrid.scrollable, f = c.scrollable, g = a.clientX, i = a.clientY;
    if (a.buttons === 0) {
      b.onMouseUp();
      return;
    }
    a.preventDefault();
    if (b.vertical && (c.isVertical || !b.onHeader)) {
      f.scrollBy(0, b.mouseY - i);
    }
    if (b.horizontal && (c.isHorizontal || !b.onHeader)) {
      e.scrollBy(b.mouseX - g);
    }
    if (!c.eventSelectionDisabled) {
      c.eventSelectionDisabled = true;
      c.panControllingEventSelection = true;
    }
    b.mouseX = g;
    b.mouseY = i;
  }
  onMouseUp() {
    let a;
    const {client: b} = this;
    (a = this.mouseDetacher) == null ? void 0 : a.call(this);
    this.mouseDetacher = null;
    b.setTimeout(() => {
      if (this.panControllingEventSelection) {
        b.eventSelectionDisabled = false;
        b.panControllingEventSelection = false;
      }
    }, 50);
  }
  get isActive() {
    return Boolean(this.mouseDetacher);
  }
};
Pan._$name = 'Pan';
GridFeatureManager.registerFeature(Pan, false, [
  'Scheduler',
  'Gantt'
]);
const ResourceMenu = class extends ContextMenuBase {
  static configurable = {
    'processItems': null,
    'items': null,
    'type': 'resource'
  };
  static $name = 'ResourceMenu';
  static get pluginConfig() {
    const a = super.pluginConfig;
    a.chain.push('populateResourceMenu');
    return a;
  }
  getDataFromEvent(a) {
    const b = super.getDataFromEvent(...arguments), c = b.cellElement = a.target.closest('.b-resourceheader-cell'), e = c == null ? void 0 : c.dataset.resourceId;
    if (e) {
      b.resourceRecord = this.client.resourceStore.getById(e);
      return b;
    }
  }
  populateResourceMenu({
    items: a,
    resourceRecord: b
  }) {
    const {client: c} = this;
    if (!c.readOnly) {
      a.remove = {
        'text': 'L{CellMenu.removeRow}',
        'localeClass': this,
        'icon': 'b-fw-icon b-icon-trash',
        'cls': 'b-separator',
        'weight': 100,
        'disabled': b.readOnly,
        'onItem': () => c.resourceStore.remove(b)
      };
    }
  }
  get showMenu() {
    return true;
  }
};
ResourceMenu.featureClass = '';
ResourceMenu._$name = 'ResourceMenu';
GridFeatureManager.registerFeature(ResourceMenu, false, ['Scheduler']);
const ResourceTimeRanges = class extends ResourceTimeRangesBase {
  static configurable = {
    'rangeCls': 'b-sch-resourcetimerange',
    'enableMouseEvents': false,
    'tabIndex': 0,
    'entityName': 'resourceTimeRange'
  };
  static $name = 'ResourceTimeRanges';
  static get pluginConfig() {
    const a = super.pluginConfig;
    return {
      ...a,
      'assign': [
        'resolveResourceTimeRangeRecord',
        'getElementFromResourceTimeRangeRecord'
      ]
    };
  }
  attachToProject(a) {
    const b = this, {client: c} = b;
    super.attachToProject(a);
    if (!a.resourceTimeRangeStore) {
      a.resourceTimeRangeStore = c.resourceTimeRangeStore || new ResourceTimeRangeStore({ 'owner': b });
      const {crudManager: f} = c;
      if (f && !f.resourceTimeRangeStore) {
        f.resourceTimeRangeStore = a.resourceTimeRangeStore;
      }
    }
    const e = a.resourceTimeRangeStore;
    if (!b.exposedOnScheduler) {
      if (c.resourceTimeRanges) {
        e.add(c.resourceTimeRanges);
        delete c.resourceTimeRanges;
      }
      Object.defineProperty(c, 'resourceTimeRanges', {
        'get': () => e.records,
        'set': g => e.data = g
      });
      b.exposedOnScheduler = true;
    }
    if (!e.resourceStore) {
      e.resourceStore = a.resourceStore;
    }
    b.detachListeners('store');
    e.ion({
      'name': 'store',
      'change': b.onStoreChange,
      'thisObj': b
    });
  }
  attachToResourceTimeRangeStore(a) {
    this.attachToProject(this.project);
    this.client.refresh();
  }
  get store() {
    return this.project.resourceTimeRangeStore;
  }
  getEventsToRender(a, b) {
    let c;
    if (!this.disabled) {
      const {timeRanges: e} = a.$original, {
          startDate: f,
          endDate: g,
          visibleDateRange: i
        } = this.client;
      (c = this.store.lazyLoad) == null ? void 0 : c.bufferedLazyLoad({
        'startDate': f,
        'endDate': g,
        'resourceRecord': a,
        'visibleStart': i.startDate,
        'visibleEnd': i.endDate
      });
      if (e == null ? void 0 : e.length) {
        e.forEach(j => {
          if (j.isRecurring) {
            b.push(...j.getOccurrencesForDateRange(f, g));
          } else {
            b.push(j);
          }
        });
      }
    }
    return b;
  }
  shouldInclude(a) {
    return a.isResourceTimeRange && !a.isNonWorking && !a.isCalendarHighlightModel;
  }
  renderContent(a, b) {
    const {client: c} = this, {eventContent: e} = b;
    if (c.resourceTimeRangeRenderer) {
      const f = c.resourceTimeRangeRenderer({
        'resourceTimeRangeRecord': a,
        'resourceRecord': b.resourceRecord,
        'renderData': b
      });
      if (Array.isArray(f)) {
        e.children = f;
      } else if (StringHelper.isHtml(f)) {
        e.children = null;
        e.html = f;
      } else if (typeof f === 'string' || typeof f === 'object') {
        e.children = [f];
      } else if (f != null) {
        e.text = f;
      }
    } else {
      super.renderContent(...arguments);
    }
  }
  doDestroy() {
    let a;
    if (((a = this.store) == null ? void 0 : a.owner) === this) {
      this.store.destroy();
    }
    super.doDestroy();
  }
};
ResourceTimeRanges.featureClass = '';
ResourceTimeRanges._$name = 'ResourceTimeRanges';
GridFeatureManager.registerFeature(ResourceTimeRanges, false, 'Scheduler');
const RowReorder2 = class extends TransactionalFeature_default(RowReorder) {
  static $name = 'RowReorder';
  onDragStart(...a) {
    super.onDragStart(...a);
    if (this.client.transactionalFeaturesEnabled) {
      return this.startFeatureTransaction();
    }
  }
  onDrop(...a) {
    this.rejectFeatureTransaction();
    return super.onDrop(...a);
  }
  onAbort(...a) {
    this.rejectFeatureTransaction();
    return super.onAbort(...a);
  }
};
RowReorder2._$name = 'RowReorder';
GridFeatureManager.registerFeature(RowReorder2, false, 'Scheduler');
GridFeatureManager.registerFeature(RowReorder2, true, 'Gantt');
const RowResize2 = class extends RowResize {
  static $name = 'RowResize';
  onResizeStart({context: a}) {
    let b, c, e;
    super.onResizeStart(...arguments);
    const {client: f} = this, {record: g} = a;
    if (f.isTimelineBase) {
      f.suspendAnimations();
      (c = (b = f.crudManager) == null ? void 0 : b.suspendAutoSync) == null ? void 0 : c.call(b);
      if (f.features.eventDragCreate) {
        this._oldDragCreateDisabled = f.features.eventDragCreate.disabled;
        f.features.eventDragCreate.disabled = true;
      }
    }
    if (f.isSchedulerBase) {
      if (((e = f.getEventLayout) == null ? void 0 : e.call(f, g).type) === 'stack') {
        a.oldEventLayout = g.eventLayout;
        g.eventLayout = 'pack';
        g.rowHeight = a.elementHeight;
      }
      a.originalBarMargin = f.getBarMargin(g);
      a.originalResourceMarginObject = f.getResourceMarginObject(g);
      a.originalRecordResourceMargin = g.resourceMargin;
    }
  }
  onResizing({context: a}) {
    const {
      record: b,
      newHeight: c,
      elementHeight: e,
      originalBarMargin: f,
      originalResourceMarginObject: g
    } = a;
    super.onResizing(...arguments);
    if (!this.applyToAllRows && !this.client.fixedRowHeight && a.valid) {
      b.beginBatch();
      b.rowHeight = c;
      if (c < e) {
        const i = c / e;
        b.barMargin = f * i;
        if (g.start === g.end) {
          b.resourceMargin = g.start * i;
        }
      }
      b.endBatch();
    }
  }
  onCancel({context: a}) {
    a.record.eventLayout = a.oldEventLayout;
    a.record.resourceMargin = a.originalRecordResourceMargin;
    a.record.barMargin = a.originalBarMargin;
    super.onCancel(...arguments);
  }
  cleanup() {
    let a, b;
    const {client: c} = this;
    if (c.isTimelineBase) {
      c.resumeAnimations();
      (b = (a = c.crudManager) == null ? void 0 : a.resumeAutoSync) == null ? void 0 : b.call(a);
      if (c.features.eventDragCreate) {
        c.features.eventDragCreate.disabled = this._oldDragCreateDisabled;
      }
    }
    super.cleanup();
  }
};
RowResize2._$name = 'RowResize';
GridFeatureManager.registerFeature(RowResize2, false);
const ScheduleMenu = class extends TimeSpanMenuBase {
  static get $name() {
    return 'ScheduleMenu';
  }
  static get defaultConfig() {
    return {
      'type': 'schedule',
      'items': null,
      'processItems': null
    };
  }
  static get pluginConfig() {
    const a = super.pluginConfig;
    a.chain.push('populateScheduleMenu');
    return a;
  }
  shouldShowMenu(a) {
    const {client: b} = this, {
        targetElement: c,
        resourceRecord: e
      } = a, f = b.timeAxisSubGridElement.contains(c);
    return !c.closest(b.eventSelector) && f && !(e && e.isSpecialRow);
  }
  getDataFromEvent(a) {
    let b, c;
    if (DomHelper.isDOMEvent(a)) {
      const {client: e} = this, f = (b = e.getCellDataFromEvent) == null ? void 0 : b.call(e, a), g = (c = e.getDateFromDomEvent) == null ? void 0 : c.call(e, a, 'floor'), i = e.resolveResourceRecord(a) || e.isVertical && e.resourceStore.last;
      return ObjectHelper.assign(super.getDataFromEvent(a), f, {
        'date': g,
        'resourceRecord': i
      });
    }
    return a;
  }
  populateScheduleMenu({
    items: a,
    resourceRecord: b,
    date: c
  }) {
    const {client: e} = this;
    if (!e.readOnly && e.eventStore) {
      a.addEvent = {
        'text': 'L{SchedulerBase.Add event}',
        'icon': 'b-icon b-icon-add',
        'disabled': this.disableAddEvent,
        'weight': 100,
        'onItem'() {
          e.createEvent(c, b, e.getRowFor(b));
        }
      };
    }
  }
  get disableAddEvent() {
    const {
      resourceRecord: a,
      date: b
    } = this.menuContext;
    return !a || a.readOnly || !a.isWorkingTime(b);
  }
};
ScheduleMenu.featureClass = '';
ScheduleMenu._$name = 'ScheduleMenu';
GridFeatureManager.registerFeature(ScheduleMenu, true, 'Scheduler');
const ScrollButtons = class extends InstancePlugin {
  static configurable = {
    'backwardIconCls': 'b-icon-before',
    'forwardIconCls': 'b-icon-after',
    'labelRenderer': null,
    'scrollOptions': {
      'animate': {
        'duration': 600,
        'easing': 'easeTo'
      },
      'y': false,
      'edgeOffset': 50
    },
    'testConfig': {
      'scrollOptions': {
        'animate': {
          'duration': 20,
          'easing': 'easeTo'
        },
        'y': false,
        'edgeOffset': 50
      }
    }
  };
  static $name = 'ScrollButtons';
  static get pluginConfig() {
    return {
      'chain': [
        'onPaint',
        'onElementClick'
      ]
    };
  }
  doDisable(a) {
    this.client.refresh();
    super.doDisable(a);
  }
  onPaint({firstPaint: a}) {
    if (a) {
      const {
          client: b,
          renderer: c
        } = this, {timeAxisColumn: e} = b, {defaultRenderer: f} = e;
      b.timeAxisSubGrid.scrollable.ion({
        'scrollEnd'() {
          b.refreshColumn(e);
        }
      });
      e.defaultRenderer = (...g) => {
        f.apply(e, g);
        return c.apply(this, g);
      };
    }
  }
  renderer({
    record: a,
    grid: b
  }) {
    const {visibleDateRange: c} = b, {
        labelRenderer: e,
        forwardIconCls: f,
        backwardIconCls: g
      } = this;
    let i = 0, j = 0;
    a.events.forEach(({
      isScheduled: k,
      startDate: l,
      endDate: n
    }) => {
      if (!k) {
        return;
      }
      if (n <= c.startDate) {
        j++;
      } else if (l >= c.endDate) {
        i++;
      }
    });
    return {
      'class': 'b-scroll-buttons-container',
      'children': [
        {
          'class': 'b-scroll-button-wrap b-scroll-backward ' + (j > 0 ? '' : 'b-scroll-button-hidden'),
          'children': [
            {
              'tag': 'i',
              'class': {
                'b-icon': 1,
                'b-scroll-button': 1,
                [g]: 1
              }
            },
            e && {
              'tag': 'span',
              'class': 'b-scroll-button-content',
              'text': e({
                'resourceRecord': a,
                'isBefore': true,
                'nbrEvents': j
              })
            }
          ]
        },
        i > 0 ? {
          'class': 'b-scroll-button-wrap b-scroll-forward',
          'children': [
            e && {
              'tag': 'span',
              'class': 'b-scroll-button-content',
              'text': e({
                'resourceRecord': a,
                'isBefore': false,
                'nbrEvents': i
              })
            },
            {
              'tag': 'i',
              'class': {
                'b-icon': 1,
                'b-scroll-button': 1,
                [f]: 1
              }
            }
          ]
        } : null
      ]
    };
  }
  onElementClick(a) {
    const {client: b} = this, {target: c} = a;
    if (c.closest('.b-scroll-button-wrap') && b.trigger('scrollButtonClick', { 'domEvent': a }) !== false) {
      const {visibleDateRange: e} = b, f = b.getRecordFromElement(c), g = c.closest('.b-scroll-forward');
      if (g) {
        b.scrollResourceEventIntoView(f, f.getEventStartingAfter(e.endDate), ObjectHelper.assign({ 'block': 'end' }, this.scrollOptions));
      } else {
        b.scrollResourceEventIntoView(f, f.getEventEndingBefore(e.startDate), ObjectHelper.assign({ 'block': 'start' }, this.scrollOptions));
      }
    }
  }
};
ScrollButtons._$name = 'ScrollButtons';
GridFeatureManager.registerFeature(ScrollButtons, false, 'Scheduler');
const ScheduleTooltip = class extends InstancePlugin {
  static get $name() {
    return 'ScheduleTooltip';
  }
  static get configurable() {
    return {
      'messageTemplate': a => '<div class="b-sch-hovertip-msg">' + a.message + '</div>',
      'hideForNonWorkingTime': null
    };
  }
  static get pluginConfig() {
    return { 'chain': ['onInternalPaint'] };
  }
  onInternalPaint({firstPaint: a}) {
    if (!a) {
      return;
    }
    const b = this, {client: c} = b;
    if (c.isSchedulerPro && b.hideForNonWorkingTime === void 0) {
      b.hideForNonWorkingTime = true;
    }
    let e;
    const f = b.hoverTip = new Tooltip({
      'id': c.id + '-schedule-tip',
      'cls': 'b-sch-scheduletip',
      'allowOver': true,
      'hoverDelay': 0,
      'hideDelay': 100,
      'showOnHover': true,
      'forElement': c.timeAxisSubGridElement,
      'anchorToTarget': false,
      'trackMouse': true,
      'updateContentOnMouseMove': true,
      'monitorResize': false,
      'textContent': false,
      'forSelector': '.b-schedulerbase:not(.b-dragging-event,.b-dragcreating,.b-creating-dependency) .b-grid-body-container:not(.b-scrolling) .b-timeline-subgrid:not(.b-scrolling) > :not(.b-sch-foreground-canvas,.b-group-footer,.b-group-row) *:not(.b-scroll-button-wrap *,.b-sch-dependency)',
      'getHtml': b.getHoverTipHtml.bind(b),
      'onDocumentMouseDown'(g) {
        if (f.forElement.contains(g.event.target)) {
          e = EventHelper.on({
            'thisObj': b,
            'element': c.timeAxisSubGridElement,
            'mousemove': j => f.internalOnPointerOver(j),
            'capture': true
          });
        }
        const i = f.hideAnimation;
        f.hideAnimation = false;
        f.constructor.prototype.onDocumentMouseDown.call(f, g);
        f.hideAnimation = i;
      },
      ...b.config,
      'internalListeners': b.configuredListeners
    });
    f.ion({
      'pointerover'({event: g}) {
        const i = 'buttons' in g ? g.buttons > 0 : g.which > 0;
        if (!i && e) {
          e();
        }
        if (b.disabled || i) {
          f.hide();
          return false;
        }
        ;
      },
      'innerhtmlupdate'({source: g}) {
        b.clockTemplate.updateDateIndicator(g.element, b.lastTime);
      }
    });
    c.ion({
      'timeAxisViewModelUpdate': 'updateTip',
      'thisObj': b
    });
    b.clockTemplate = new ClockTemplate({ 'scheduler': c });
  }
  processConfiguredListeners() {
  }
  updateTip() {
    if (this.hoverTip.isVisible) {
      this.hoverTip.updateContent();
    }
  }
  doDestroy() {
    this.destroyProperties('clockTemplate', 'hoverTip');
    super.doDestroy();
  }
  getHoverTipHtml({
    tip: a,
    event: b
  }) {
    const c = this, e = c.client, f = b && e.getDateFromDomEvent(b, 'floor', true);
    let g = c.lastHtml;
    if (f && b.target) {
      const i = e.resolveResourceRecord(b);
      if (i && (f - c.lastTime !== 0 || i.id !== c.lastResourceId)) {
        if (c.hideForNonWorkingTime) {
          const j = i.isWorkingTime(f);
          a.element.classList.toggle('b-nonworking-time', !j);
        }
        c.lastResourceId = i.id;
        g = c.lastHtml = c.generateTipContent({
          'date': f,
          'event': b,
          'resourceRecord': i
        });
      }
    } else {
      a.hide();
      c.lastTime = null;
      c.lastResourceId = null;
    }
    return g;
  }
  generateTipContent({
    date: a,
    event: b,
    resourceRecord: c
  }) {
    const e = this, f = e.clockTemplate.generateContent({
        'date': a,
        'text': e.client.getFormattedDate(a)
      }), g = e.messageTemplate({ 'message': e.getText(a, b, c) || '' });
    e.lastTime = a;
    return f + g;
  }
  getText(a, b, c) {
  }
};
ScheduleTooltip.featureClass = 'b-scheduletip';
ScheduleTooltip._$name = 'ScheduleTooltip';
GridFeatureManager.registerFeature(ScheduleTooltip, true, 'Scheduler');
GridFeatureManager.registerFeature(ScheduleTooltip, false, 'ResourceUtilization');
const SimpleEventEdit = class extends InstancePlugin.mixin(TaskEditStm_default) {
  static get $name() {
    return 'SimpleEventEdit';
  }
  static get defaultConfig() {
    return {
      'triggerEvent': 'eventdblclick',
      'eventRecord': null,
      'field': 'name',
      'editorConfig': null
    };
  }
  static get pluginConfig() {
    return {
      'chain': [
        'onEventEnterKey',
        'editEvent'
      ]
    };
  }
  construct(a, b) {
    const c = this;
    c.scheduler = a;
    a.eventEdit = c;
    super.construct(a, b);
    c.clientListenersDetacher = a.ion({
      [c.triggerEvent]: ({
        eventRecord: e,
        eventElement: f
      }) => c.editEvent(e, e.resource, f),
      'dragcreateend': c.onDragCreateEnd,
      'thisObj': c
    });
  }
  doDestroy() {
    let a;
    this.clientListenersDetacher();
    (a = this.editor) == null ? void 0 : a.destroy();
    super.doDestroy();
  }
  get eventStore() {
    return this.scheduler.eventStore;
  }
  get project() {
    return this.client.project;
  }
  async editEvent(a, b, c, e) {
    const f = this, {scheduler: g} = f, {eventEdit: i} = f.client.features;
    if (!b) {
      b = a.resource;
    }
    if (!c) {
      c = g.getElementFromEventRecord(a);
    }
    if (g.readOnly || f.disabled || a.readOnly || b.readOnly || i && !i.disabled && !a.isCreating) {
      return;
    }
    let {editor: j} = f;
    c = DomHelper.down(c, g.eventInnerSelector) || c;
    a = a.isAssignment ? a.event : a;
    f.resource = b;
    f.eventRecord = a;
    f.element = c;
    g.element.classList.add('b-eventeditor-editing');
    if (j) {
      j.render(g.timeAxisSubGridElement);
    } else {
      f.editor = j = Editor.new({
        'owner': g.timeAxisSubGrid,
        'appendTo': g.timeAxisSubGridElement,
        'scrollAction': 'realign',
        'maxHeight': 40,
        'align': { 'align': g.isHorizontal ? 'c-c' : 't-t' },
        'cls': 'b-simpleeventeditor',
        'internalListeners': {
          'complete': 'onEditorComplete',
          'cancel': 'onEditorCancel',
          'thisObj': f
        },
        'onInternalKeyDown': k => k.stopPropagation()
      }, f.editorConfig);
      j.revertFocus = () => g.getElementFromEventRecord(f.eventRecord, f.resourceRecord, true).focus({ 'scrollIntoView': false });
      f.relayEvents(f.editor, [
        'beforestart',
        'start',
        'beforecomplete',
        'complete',
        'beforecancel',
        'cancel'
      ]);
    }
    if (e) {
      f.stmInitiallyAutoRecord = e.stmInitiallyAutoRecord;
      f.stmInitiallyDisabled = e.stmInitiallyDisabled;
      f.hasStmCapture = true;
      e.transferred = true;
    } else if (e !== false && !f.hasStmCapture) {
      f.captureStm(true);
    }
    if (a.isCreating) {
      a.name = '';
    }
    await j.startEdit({
      'target': c,
      'record': a,
      'field': f.field
    });
    if (g.isVertical && a.startDate < g.visibleDateRange.startDate) {
      j.element.scrollIntoView();
    }
    g.navigator.disabled = true;
  }
  onEditorComplete() {
    this.eventRecord.isCreating = false;
    this.reset();
    this.freeStm(true);
  }
  onEditorCancel() {
    const {eventRecord: a} = this;
    this.reset();
    this.freeStm(false);
    if (a.isCreating) {
      a.remove();
    }
  }
  reset() {
    this.scheduler.element.classList.remove('b-eventeditor-editing');
    this.scheduler.navigator.disabled = false;
    this.eventRecord = null;
    this.resource = null;
  }
  onEventEnterKey({
    assignmentRecord: a,
    eventRecord: b
  }) {
    const c = a ? this.scheduler.getElementFromAssignmentRecord(a) : this.scheduler.getElementFromEventRecord(b), e = (a || b).resource;
    this.editEvent(b, e, c);
  }
  onDragCreateEnd({
    eventRecord: a,
    resourceRecord: b,
    eventElement: c,
    stmCapture: e
  }) {
    this.element = c;
    this.editEvent(a, b, c, e);
  }
};
SimpleEventEdit._$name = 'SimpleEventEdit';
GridFeatureManager.registerFeature(SimpleEventEdit, false, 'Scheduler');
const startScrollOptions2 = Object.freeze({
  'animate': false,
  'block': 'start'
});
const endScrollOptions2 = Object.freeze({
  'animate': false,
  'block': 'end'
});
const Split2 = class extends Split {
  static configurable = {
    'relayProperties': {
      'value': {
        'barMargin': 1,
        'eventColor': 1,
        'eventStyle': 1,
        'eventLayout': 1,
        'fillTicks': 1,
        'resourceMargin': 1,
        'snap': 1,
        'tickSize': 1
      },
      '$config': { 'merge': 'merge' }
    }
  };
  static $name = 'Split';
  static get pluginConfig() {
    const a = super.pluginConfig;
    a.chain.push('populateScheduleMenu');
    return a;
  }
  getClientConfig(a, b, c, e) {
    let f;
    const {client: g} = this, i = super.getClientConfig(a, b, c, e);
    if (!g.isSchedulerBase && !g.isSchedulerProBase && !g.isGanttBase) {
      return i;
    }
    delete i.store;
    delete i.events;
    delete i.resources;
    delete i.assignments;
    delete i.dependencies;
    delete i.resourceTimeRanges;
    delete i.timeRanges;
    i.project = g.project;
    i.assignmentStore = i.project.assignmentStore;
    i.crudManager = g.crudManager;
    i.viewPreset = g.viewPreset;
    i.startDate = g.startDate;
    i.endDate = g.endDate;
    i.selectedCollection = g.selectedCollection;
    i.useInitialAnimation = false;
    if (((f = i.features) == null ? void 0 : f.eventDrag) !== false) {
      ObjectHelper.setPath(i, 'features.eventDrag.constrainDragToTimeline', false);
      g.features.eventDrag.constrainDragToTimeline = false;
    }
    return i;
  }
  unsplitCleanup(a) {
    this.client.timeAxisSubGrid.onElementResize();
  }
  processOptions(a) {
    const {client: b} = this;
    let {
      atDate: c,
      atRecord: e
    } = a;
    if (a.direction !== 'horizontal' && !a.atColumn && !a.atRecord && !c) {
      const f = Rectangle.from(b.element), g = b.getDateFromCoordinate(f.center.x, 'round', false);
      if (g) {
        a.atDate = c = g;
        a.atColumn = b.timeAxisColumn;
      }
    }
    if (c) {
      if (!a.direction) {
        a.direction = e ? 'both' : 'vertical';
      }
      a.splitX = b.getVirtualCoordFromDate(c) - b.timeAxisSubGrid.scrollable.x;
      if (b.rtl) {
        a.splitX += b._bodyRectangle.width - Rectangle.from(b.timeAxisSubGridElement).right + DomHelper.scrollBarWidth;
      } else {
        a.splitX += Rectangle.from(b.timeAxisSubGridElement, b.bodyContainer).left;
      }
      a.remainingWidth = Rectangle.from(b.element).width - a.splitX;
    }
    return super.processOptions(a);
  }
  async split(a = {}) {
    const {client: b} = this;
    if (b.isVertical) {
      throw new Error('Splitting is not supported in vertical mode');
    }
    const c = await super.split(a);
    if (c && a.atDate) {
      await Promise.all([
        c[0].scrollToDate(DateHelper.add(a.atDate, -1, b.timeAxis.unit), endScrollOptions2),
        c[1].scrollToDate(a.atDate, startScrollOptions2)
      ]);
    }
    b.timeAxisSubGrid.scrollable.x += 0.5;
    return c;
  }
  populateCellMenu() {
  }
  populateScheduleMenu({
    items: a,
    date: b,
    record: c
  }) {
    const e = this, {isSplit: f} = e, {splitFrom: g} = e.client;
    if (!e.disabled && e.client.isHorizontal) {
      a.splitSchedule = {
        'text': 'L{split}',
        'localeClass': e,
        'icon': 'b-icon b-icon-split-vertical',
        'weight': 500,
        'separator': true,
        'hidden': f || g,
        'menu': {
          'splitHorizontally': {
            'text': 'L{horizontally}',
            'icon': 'b-icon b-icon-split-horizontal',
            'localeClass': e,
            'weight': 100,
            'onItem'() {
              e.split({
                'atRecord': c,
                'direction': 'horizontal'
              }).then();
            }
          },
          'splitVertically': {
            'text': 'L{vertically}',
            'icon': 'b-icon b-icon-split-vertical',
            'localeClass': e,
            'weight': 200,
            'onItem'() {
              e.split({ 'atDate': b }).then();
            }
          },
          'splitBoth': {
            'text': 'L{both}',
            'icon': 'b-icon b-icon-split-both',
            'localeClass': e,
            'weight': 300,
            'onItem'() {
              e.split({
                'atDate': b,
                'atRecord': c,
                'direction': 'both'
              }).then();
            }
          }
        }
      };
      a.unsplitSchedule = {
        'text': 'L{unsplit}',
        'localeClass': e,
        'icon': 'b-icon b-icon-clear',
        'hidden': !(f || g),
        'weight': 550,
        'onItem'() {
          (g || e).unsplit();
        }
      };
    }
  }
};
Split2._$name = 'Split';
GridFeatureManager.registerFeature(Split2, false, 'Scheduler');
const zeroMargins = {
  'width': 0,
  'height': 0
};
const StickyEvents = class extends InstancePlugin {
  static type = 'stickyEvents';
  static $name = 'StickyEvents';
  static get pluginConfig() {
    return { 'chain': ['onEventDataGenerated'] };
  }
  construct(a, b) {
    super.construct(a, b);
    if (a.isVertical) {
      this.toUpdate = new Set();
      a.ion({
        'scroll': 'onSchedulerScroll',
        'horizontalScroll': 'onHorizontalScroll',
        'thisObj': this,
        'prio': 10000
      });
    }
  }
  onEventDataGenerated(a) {
    if (this.client.isHorizontal) {
      a.wrapperCls['b-disable-sticky'] = a.eventRecord.stickyContents === false;
    } else {
      this.syncEventContentPosition(a, void 0, true);
      this.updateStyles();
    }
  }
  onSchedulerScroll() {
    if (!this.disabled) {
      this.verticalSyncAllEventsContentPosition(this.client);
    }
  }
  onHorizontalScroll({subGrid: a}) {
    if (a === this.client.timeAxisSubGrid) {
      this.verticalSyncAllEventsContentPosition(this.client);
    }
  }
  updateStyles() {
    for (const {
          contentEl: a,
          style: b
        } of this.toUpdate) {
      DomHelper.applyStyle(a, b);
    }
    this.toUpdate.clear();
  }
  verticalSyncAllEventsContentPosition(a) {
    const {resourceMap: b} = a.currentOrientation;
    for (const c of b.values()) {
      for (const {
            renderData: e,
            elementConfig: f
          } of Object.values(c)) {
        const g = [e];
        if (f && e.eventRecord.isResourceTimeRange) {
          g.push(f.children[0]);
        }
        this.syncEventContentPosition.apply(this, g);
      }
    }
    this.toUpdate.size && this.updateStyles();
  }
  syncEventContentPosition(a, b = a.eventContent, e = false) {
    if (this.disabled || a.eventRecord.stickyContents === false) {
      return;
    }
    const {client: f} = this, {
        eventRecord: g,
        resourceRecord: i,
        useEventBuffer: j,
        bufferAfterWidth: k,
        bufferBeforeWidth: l,
        top: n,
        height: o
      } = a, p = f.scrollable.y, q = e ? null : f.getElementFromEventRecord(g, i, true), r = q && DomSync.getChild(q, 'event.content'), u = g.instanceMeta(f), v = typeof b.style === 'string' ? b.style = DomHelper.parseStyle(b.style) : b.style || (b.style = {});
    if (q == null ? void 0 : q.classList.contains('b-dragging')) {
      return;
    }
    let w = n, z = o, A = w + z;
    if (j) {
      w += l;
      z = z - l - k;
      A = w + z;
    }
    if (w < p && A >= p && !g.isMilestone) {
      const B = r == null ? void 0 : r.offsetWidth, C = (r == null ? void 0 : r.parentNode) && DomHelper.getStyleValue(r.parentNode, 'justifyContent'), E = C === 'center' ? (a.width - B) / 2 : 0, F = w, G = F + z - 1;
      if ((!r || B) && F < p && G >= p) {
        const H = this.getEventContentMargins(r), I = r ? z - r.offsetHeight - H.height - E : Number.MAX_SAFE_INTEGER, J = Math.min(p - F, I - 2);
        v.transform = J > 0 ? 'translateY(' + J + 'px)' : '';
        u.stuck = true;
      } else {
        v.transform = '';
        u.stuck = false;
      }
      if (r) {
        this.toUpdate.add({
          'contentEl': r,
          'style': v
        });
      }
    } else if (r && u.stuck) {
      v.transform = '';
      u.stuck = false;
      this.toUpdate.add({
        'contentEl': r,
        'style': v
      });
    }
  }
  getEventContentMargins(a) {
    if (a == null ? void 0 : a.classList.contains('b-sch-event-content')) {
      return DomHelper.getEdgeSize(a, 'margin');
    }
    return zeroMargins;
  }
  doDisable() {
    super.doDisable(...arguments);
    if (!this.isConfiguring) {
      this.client.refreshWithTransition();
    }
  }
};
StickyEvents._$name = 'StickyEvents';
GridFeatureManager.registerFeature(StickyEvents, true, 'Scheduler');
GridFeatureManager.registerFeature(StickyEvents, false, 'ResourceHistogram');
const LockRows2 = class extends LockRows {
  static $name = 'LockRows';
  onInternalPaint({firstPaint: a}) {
    const b = this, {project: c} = b.client, {resourceStore: e} = c;
    if (a) {
      if (b.refreshSuspended) {
        b.client.resumeRefresh();
      }
      if (e.count) {
        b.lockRows();
      } else {
        const f = c.ion({
          'refresh': () => {
            if (e.count) {
              f();
              b.lockRows();
            }
          }
        });
      }
    }
  }
  getClientConfig(a, b, c, e) {
    const f = this.superGetClientConfig(a, b, c, e);
    f.partner = this.client;
    delete f.crudManager;
    return f;
  }
};
const myProto = LockRows2.prototype;
const superProto = Split2.prototype;
myProto.superGetClientConfig = superProto.getClientConfig;
LockRows2.configurable.relayProperties = Split2.configurable.relayProperties;
LockRows2._$name = 'LockRows';
GridFeatureManager.registerFeature(LockRows2, false, 'Scheduler');
const TimelineSummary = class extends Summary {
  static get $name() {
    return 'TimelineSummary';
  }
  static get configurable() {
    return { 'showTooltip': true };
  }
  static get pluginConfig() {
    return {
      'chain': [
        'renderRows',
        'updateProject'
      ]
    };
  }
  construct(a, b) {
    const c = this;
    super.construct(a, b);
    if (!c.summaries) {
      c.summaries = [{ 'renderer': c.renderer }];
    }
    if (a.isTimelineBase) {
      c.updateProject(a.project);
      a.ion({
        'timeAxisViewModelUpdate': c.renderRows,
        'thisObj': c
      });
    }
  }
  updateProject(a) {
    this.detachListeners('summaryProject');
    a.ion({
      'name': 'summaryProject',
      'dataReady': 'updateTimelineSummaries',
      'thisObj': this
    });
  }
  renderRows() {
    let a;
    if (this.client.isHorizontal) {
      (a = this.client.timeAxisSubGrid.footer.element.querySelector('.b-grid-footer')) == null ? void 0 : a.classList.add('b-sch-summarybar');
    }
    super.renderRows();
    if (!this.disabled) {
      this.render();
    }
  }
  get summaryBarElement() {
    return this.client.element.querySelector('.b-sch-summarybar');
  }
  render() {
    const a = this, {client: b} = a, c = b.isHorizontal ? 'width' : 'height', e = b.timeAxisViewModel.columnConfig, f = a.summaryBarElement;
    if (f) {
      if (!a._tip && a.showTooltip && a.summaries.some(g => g.label)) {
        a._tip = new Tooltip({
          'id': b.id + '-summary-tip',
          'cls': 'b-timeaxis-summary-tip',
          'hoverDelay': 0,
          'hideDelay': 100,
          'forElement': f,
          'anchorToTarget': true,
          'trackMouse': false,
          'forSelector': '.b-timeaxis-tick',
          'getHtml': ({activeTarget: g}) => g._tipHtml
        });
      }
      f.innerHTML = e[e.length - 1].map(g => '<div class="b-timeaxis-tick" style="' + c + ': ' + g.width + 'px"></div>').join('');
      a.updateTimelineSummaries();
    }
  }
  refresh() {
    super.refresh();
    this.updateTimelineSummaries();
  }
  doDisable(a) {
    let b;
    const {isConfiguring: c} = this.client;
    super.doDisable(a);
    (b = this.summaryColumn) == null ? void 0 : b.toggle(!a);
    if (!c && !a) {
      this.render();
    }
  }
  doDestroy() {
    let a;
    (a = this._tip) == null ? void 0 : a.destroy();
    super.doDestroy();
  }
};
TimelineSummary._$name = 'TimelineSummary';
const Summary2 = class extends TimelineSummary {
  static get $name() {
    return 'Summary';
  }
  static get configurable() {
    return {
      'summaries': null,
      'renderer': null,
      'verticalSummaryColumnConfig': null
    };
  }
  static get pluginConfig() {
    const a = super.pluginConfig;
    a.chain.push('bindStore', 'updateEventStore', 'updateResourceStore');
    return a;
  }
  construct(a, b) {
    const c = this;
    c.scheduler = a;
    if (a.isVertical) {
      a.timeAxisSubGrid.resizable = false;
      b.hideFooters = true;
      a.add(a.createSubGrid('right'));
      c.summaryColumn = a.columns.add(ObjectHelper.assign({
        'filterable': null,
        'region': 'right',
        'cellCls': 'b-grid-footer b-sch-summarybar',
        'align': 'center',
        'sortable': false,
        'editor': false,
        'groupable': false,
        'htmlEncode': false,
        'cellMenuItems': false,
        'enableHeaderContextMenu': false,
        'hidden': c.disabled
      }, c.verticalSummaryColumnConfig))[0];
    }
    super.construct(a, b);
    if (a.isSchedulerBase) {
      c.updateEventStore(a.eventStore);
      c.updateResourceStore(a.resourceStore);
    }
  }
  updateEventStore(a) {
    this.detachListeners('summaryEventStore');
    a.ion({
      'name': 'summaryEventStore',
      'filter': 'updateTimelineSummaries',
      'thisObj': this
    });
  }
  updateResourceStore(a) {
    this.detachListeners('summaryResourceStore');
    a.ion({
      'name': 'summaryResourceStore',
      'filter': 'updateTimelineSummaries',
      'thisObj': this
    });
  }
  updateTimelineSummaries() {
    const a = this, {client: b} = a, {
        eventStore: c,
        timeAxis: e
      } = b, f = a.summaryBarElement, g = a.selectedOnly && b.selectedRecords.length ? b.selectedRecords : b.resourceStore.records;
    if (f && b.isEngineReady) {
      const i = b.getResourcesEventsPerTick(g, ({event: j}) => {
        return !c.isFiltered || c.records.includes(j);
      });
      Array.from(f.children).forEach((j, k) => {
        const l = e.getAt(k), n = i[k] || [];
        let o = '', p = '<header>' + a.L('L{Summary for}', b.getFormattedDate(l.startDate)) + '</header>';
        a.summaries.forEach(q => {
          const r = q.renderer({
              'startDate': l.startDate,
              'endDate': l.endDate,
              'resourceStore': b.resourceStore,
              'eventStore': c,
              'events': n,
              'element': j
            }), u = r ? '<div class="b-timeaxis-summary-value">' + r + '</div>' : '';
          if (a.summaries.length > 1 || r) {
            o += u;
          }
          p += u ? '<label>' + (q.label || '') + '</label>' + u : '';
        });
        j.innerHTML = o;
        j._tipHtml = o ? p : '';
      });
    }
  }
  updateSummaries() {
    if (!this.isConfiguring) {
      this.refreshSummaries();
    }
  }
};
Summary2._$name = 'Summary';
GridFeatureManager.registerFeature(Summary2, false, 'Scheduler');
const setTimeSpanOptions = { 'maintainVisibleStart': true };
const TimeAxisHeaderMenu = class extends HeaderMenu {
  static configurable = {
    'menu': {
      '$config': [
        'lazy',
        'nullify'
      ],
      'value': {
        'type': 'menu',
        'autoShow': false,
        'closeAction': 'hide',
        'align': { 'monitorResize': false },
        'scrollAction': null,
        'constrainTo': globalThis
      }
    },
    'processItems': null,
    'items': null,
    'type': 'timeAxisHeader'
  };
  static $name = 'TimeAxisHeaderMenu';
  static get pluginConfig() {
    const a = super.pluginConfig;
    a.chain.push('populateTimeAxisHeaderMenu');
    return a;
  }
  construct() {
    super.construct(...arguments);
    if (this.triggerEvent.includes('click') && this.client.zoomOnTimeAxisDoubleClick) {
      this.client.zoomOnTimeAxisDoubleClick = false;
    }
  }
  shouldShowMenu(a) {
    const {
        column: b,
        targetElement: c
      } = a, {client: e} = this;
    if (e.isHorizontal) {
      return (b == null ? void 0 : b.enableHeaderContextMenu) !== false && (b == null ? void 0 : b.isTimeAxisColumn);
    }
    return c.closest('.b-sch-header-timeaxis-cell');
  }
  populateTimeAxisHeaderMenu({items: a}) {
    const b = this, {client: c} = b, e = {
        'magnitude': c.timeAxis.shiftIncrement,
        'unit': c.timeAxis.shiftUnit
      };
    Object.assign(a, {
      'zoomLevel': {
        'text': 'L{pickZoomLevel}',
        'localeClass': b,
        'icon': 'b-fw-icon b-icon-search-plus',
        'disabled': !c.presets.count || b.disabled,
        'weight': 200,
        'menu': {
          'items': {
            'zoomSlider': {
              'type': 'slider',
              'weight': 210,
              'minWidth': 130,
              'showValue': false,
              'triggerChangeOnInput': true
            }
          },
          'onBeforeShow'({source: f}) {
            const [g] = f.items;
            g.min = c.minZoomLevel;
            g.max = c.maxZoomLevel;
            g.value = c.zoomLevel;
            b.zoomDetacher = g.ion({
              'change': 'onZoomSliderChange',
              'thisObj': b
            });
          },
          'onHide'() {
            let f;
            (f = b.zoomDetacher) == null ? void 0 : f.call(b);
            b.zoomDetacher = null;
          }
        }
      },
      'dateRange': {
        'text': 'L{activeDateRange}',
        'localeClass': b,
        'icon': 'b-fw-icon b-icon-calendar',
        'weight': 300,
        'menu': {
          'type': 'popup',
          'cls': 'b-sch-timeaxis-menu-daterange-popup',
          'defaults': { 'localeClass': b },
          'items': {
            'startDateField': {
              'type': 'datefield',
              'label': 'L{startText}',
              'weight': 310,
              'labelWidth': '6em',
              'required': true,
              'step': e,
              'highlightExternalChange': false,
              'internalListeners': {
                'change': b.onRangeDateFieldChange,
                'thisObj': b
              }
            },
            'endDateField': {
              'type': 'datefield',
              'label': 'L{endText}',
              'weight': 320,
              'labelWidth': '6em',
              'required': true,
              'step': e,
              'highlightExternalChange': false,
              'internalListeners': {
                'change': b.onRangeDateFieldChange,
                'thisObj': b
              }
            },
            'leftShiftBtn': {
              'type': 'button',
              'weight': 330,
              'cls': 'b-left-nav-btn',
              'icon': 'b-icon b-icon-previous',
              'color': 'b-blue b-raised',
              'flex': 1,
              'margin': 0,
              'internalListeners': {
                'click': b.onLeftShiftBtnClick,
                'thisObj': b
              }
            },
            'todayBtn': {
              'type': 'button',
              'weight': 340,
              'cls': 'b-today-nav-btn',
              'color': 'b-blue b-raised',
              'text': 'L{todayText}',
              'flex': 4,
              'internalListeners': {
                'click': b.onTodayBtnClick,
                'thisObj': b
              }
            },
            'rightShiftBtn': {
              'type': 'button',
              'weight': 350,
              'cls': 'b-right-nav-btn',
              'icon': 'b-icon b-icon-next',
              'color': 'b-blue b-raised',
              'flex': 1,
              'internalListeners': {
                'click': b.onRightShiftBtnClick,
                'thisObj': b
              }
            }
          },
          'internalListeners': {
            'paint': b.initDateRangeFields,
            'thisObj': b
          }
        }
      }
    });
  }
  onZoomSliderChange({value: a}) {
    this.client.zoomLevel = a;
  }
  initDateRangeFields({
    source: a,
    firstPaint: b
  }) {
    if (b) {
      const {widgetMap: c} = a;
      this.startDateField = c.startDateField;
      this.endDateField = c.endDateField;
    }
    this.initDates();
  }
  initDates() {
    const a = this;
    a.startDateField.suspendEvents();
    a.endDateField.suspendEvents();
    a.startDateField.value = a.startDateFieldInitialValue = a.client.startDate;
    a.endDateField.value = a.endDateFieldInitialValue = a.client.endDate;
    a.startDateField.resumeEvents();
    a.endDateField.resumeEvents();
  }
  onRangeDateFieldChange({source: a}) {
    const b = this, c = a === b.startDateField, {client: e} = b, {timeAxis: f} = e, g = b.startDateFieldInitialValue && !c ? b.startDateFieldInitialValue : b.startDateField.value;
    let i = b.endDateFieldInitialValue && c ? b.endDateFieldInitialValue : b.endDateField.value;
    if (c) {
      b.startDateFieldInitialValue = null;
    } else {
      b.endDateFieldInitialValue = null;
    }
    if (!(i - g)) {
      i = DateHelper.add(i, f.shiftIncrement, f.shiftUnit);
    } else if (i < g) {
      i = DateHelper.add(g, f.shiftIncrement, f.shiftUnit);
    }
    e.setTimeSpan(g, i, setTimeSpanOptions);
    b.initDates();
  }
  onLeftShiftBtnClick() {
    this.client.timeAxis.shiftPrevious();
    this.initDates();
  }
  onTodayBtnClick() {
    const a = DateHelper.clearTime(new Date());
    this.client.setTimeSpan(a, DateHelper.add(a, 1, 'day'));
    this.initDates();
  }
  onRightShiftBtnClick() {
    this.client.timeAxis.shiftNext();
    this.initDates();
  }
};
TimeAxisHeaderMenu._$name = 'TimeAxisHeaderMenu';
GridFeatureManager.registerFeature(TimeAxisHeaderMenu, true, [
  'Scheduler',
  'TimelineHistogram',
  'Gantt'
]);
const TimeRanges = class extends AbstractTimeRanges.mixin(AttachToProjectMixin_default) {
  static configurable = {
    'currentTimeLineUpdateInterval': 10000,
    'currentDateFormat': 'HH:mm',
    'showCurrentTimeLine': {
      '$config': 'lazy',
      'value': null
    }
  };
  static stateProperties = ['showCurrentTimeLine'];
  static get $name() {
    return 'TimeRanges';
  }
  get timeRanges() {
    let a;
    const b = this, {store: c} = b;
    if (!b._timeRanges) {
      let e = [];
      if (c) {
        e = c.records;
        if ((a = c.recurringEvents) == null ? void 0 : a.size) {
          const {
            startDate: f,
            endDate: g
          } = b.client.timeAxis;
          e = e.flatMap(i => {
            if (i.isRecurring) {
              return i.getOccurrencesForDateRange(f, g);
            }
            return i;
          });
        }
      }
      if (b.showCurrentTimeLine && b.currentTimeLine) {
        e = [
          b.currentTimeLine,
          ...e
        ];
      }
      b._timeRanges = e;
    }
    return b._timeRanges;
  }
  attachToProject(a) {
    let b, c;
    const e = this;
    e.attachToTimeRangeStore(a.timeRangeStore);
    super.attachToProject(a);
    (b = e.projectTimeZoneChangeDetacher) == null ? void 0 : b.call(e);
    if (e.showCurrentTimeLine) {
      e.projectTimeZoneChangeDetacher = (c = e.client.project) == null ? void 0 : c.ion({
        'timeZoneChange': 'updateCurrentTimeLine',
        'thisObj': e
      });
      if (e.currentTimeLine) {
        e.updateCurrentTimeLine();
      }
    }
  }
  initCurrentTimeLine() {
    let a;
    const b = this;
    if (b.currentTimeLine || !b.showCurrentTimeLine) {
      return;
    }
    const c = typeof b.showCurrentTimeLine === 'object' ? b.showCurrentTimeLine : {};
    b.currentTimeLine = (((a = b.store) == null ? void 0 : a.modelClass) || TimeSpan).new({
      'id': 'currentTime',
      'cls': 'b-sch-current-time'
    }, c);
    b.currentTimeInterval = b.setInterval(() => b.updateCurrentTimeLine(), b.currentTimeLineUpdateInterval);
    b._timeRanges = null;
    b.updateCurrentTimeLine();
  }
  updateCurrentTimeLine() {
    let a;
    const b = this, {
        currentTimeLine: c,
        client: e
      } = b;
    c.timeZone = (a = b.project) == null ? void 0 : a.timeZone;
    c.setLocalDate('startDate', new Date());
    c.endDate = c.startDate;
    if (!c.originalData.name) {
      c.name = DateHelper.format(c.startDate, b.currentDateFormat);
    }
    b.renderRanges();
    e.trigger('currentTimelineUpdate', { 'date': c.startDate });
  }
  hideCurrentTimeLine() {
    const a = this;
    if (!a.currentTimeLine) {
      return;
    }
    a.clearInterval(a.currentTimeInterval);
    a.currentTimeLine = null;
    a.refresh();
  }
  updateShowCurrentTimeLine(a) {
    if (a) {
      this.initCurrentTimeLine();
    } else {
      this.hideCurrentTimeLine();
    }
    this.client.trigger('stateChange');
  }
  populateTimeAxisHeaderMenu({items: a}) {
    a.currentTimeLine = {
      'weight': 400,
      'text': this.L('L{showCurrentTimeLine}'),
      'checked': this.currentTimeLine,
      'onToggle': ({checked: b}) => {
        if (!this.showCurrentTimeLine) {
          this.showCurrentTimeLine = b;
        } else {
          this.updateShowCurrentTimeLine(b);
        }
      }
    };
  }
  attachToStore(a) {
    const b = this;
    if (b.storeDetacher) {
      b.storeDetacher();
    }
    b.storeDetacher = a.ion({
      'change': 'onStoreChange',
      'refresh': 'onStoreChange',
      'thisObj': b
    });
    b._timeRanges = null;
    b.renderRanges();
  }
  get store() {
    let a;
    return (a = this.client.project) == null ? void 0 : a.timeRangeStore;
  }
  attachToTimeRangeStore(a) {
    const {client: b} = this;
    this.attachToStore(a);
    if (b.timeRanges && !b._timeRangesExposed) {
      a.add(b.timeRanges);
      delete b.timeRanges;
    }
  }
  resolveTimeRangeRecord(a) {
    const b = a.closest(this.baseSelector).dataset.id;
    if (b === 'currentTime') {
      return this.currentTimeLine;
    }
    return this.store.getById(b);
  }
  onStoreChange({
    type: a,
    action: b
  }) {
    const c = this;
    c._timeRanges = null;
    if (c.disabled || !c.client.isVisible || c.isConfiguring || a === 'refresh' && b !== 'batch') {
      return;
    }
    c.client.runWithTransition(() => c.renderRanges(), !c.client.refreshSuspended);
  }
  onDragStart(a) {
    const b = this, {context: c} = a, e = b.resolveTimeRangeRecord(c.element.closest(b.baseSelector)), f = b.getBodyElementByRecord(e);
    c.relatedElements = [f];
    Object.assign(c, {
      'record': e,
      'rangeBodyEl': f,
      'originRangeX': a.source.getX(f),
      'originRangeY': a.source.getY(f)
    });
    super.onDragStart(a);
    b.showTip(c);
  }
  onDrop(a) {
    const {context: b} = a;
    if (!b.valid) {
      return this.onInvalidDrop({ 'context': b });
    }
    const c = this, {client: e} = c, {record: f} = b, g = e.isHorizontal ? b.newX : b.newY, i = e.getDateFromVirtualCoord({
        'coord': g,
        'roundingMethod': 'round'
      }), j = f.startDate - i !== 0;
    if (j) {
      f.setStartDate(i);
    } else {
      c.drag.abort();
    }
    c.destroyTip();
    super.onDrop(a);
  }
  onResizeStart({context: a}) {
    const b = this, c = b.resolveTimeRangeRecord(a.element.closest(b.baseSelector)), e = b.getBodyElementByRecord(c);
    Object.assign(a, {
      'record': c,
      'rangeBodyEl': e
    });
    b.showTip(a);
    super.onResizeStart(...arguments);
  }
  onResizeDrag({
    source: a,
    context: b
  }) {
    const c = this, {rangeBodyEl: e} = b, {client: f} = c, {isHorizontal: g} = f, i = g ? b.newX : b.newY, j = g ? b.newX + b.newWidth : b.newY + b.newHeight, k = f.getDateFromVirtualCoord({
        'coord': i,
        'roundingMethod': 'round'
      }), l = f.getDateFromVirtualCoord({
        'coord': j,
        'roundingMethod': 'round'
      });
    if (!g) {
      if (b.edge === 'top') {
        a.setY(e, b.newY);
      }
      e.style.height = b.newHeight + 'px';
    } else {
      if (b.edge === 'left') {
        a.setX(e, b.newX);
      }
      e.style.width = b.newWidth + 'px';
    }
    c.updateDateIndicator({
      'startDate': k,
      'endDate': l
    });
  }
  onResize({context: a}) {
    if (!a.valid) {
      return this.onInvalidDrop({ 'context': a });
    }
    const b = this, {client: c} = b, {record: e} = a, f = c.isHorizontal ? a.newX : a.newY, g = c.isHorizontal ? a.newX + a.newWidth : a.newY + a.newHeight, i = c.getDateFromVirtualCoord({
        'coord': f,
        'roundingMethod': 'round'
      }), j = c.getDateFromVirtualCoord({
        'coord': g,
        'roundingMethod': 'round'
      }), k = a.edge === 'left' || a.edge === 'top', l = k && e.startDate - i !== 0 || j && e.endDate - j !== 0;
    if (l && j > i) {
      if (k) {
        e.setStartDate(i, false);
      } else {
        e.setEndDate(j, false);
      }
    } else {
      b.onInvalidResize({ 'context': a });
    }
    b.destroyTip();
  }
  onInvalidResize({context: a}) {
    const b = this;
    b.resize.reset();
    a.rangeBodyEl.parentElement.lastDomConfig = a.rangeBodyEl.lastDomConfig = a.element.lastDomConfig = null;
    b.renderRanges();
    b.destroyTip();
  }
  get state() {
    return { 'showCurrentTimeLine': this.showCurrentTimeLine };
  }
};
TimeRanges._$name = 'TimeRanges';
GridFeatureManager.registerFeature(TimeRanges, false, ['TimelineBase']);
const TimeSelection = class extends AbstractTimeRanges.mixin(HeaderDragSelect_default) {
  static $name = 'TimeSelection';
  static get configurable() {
    return {
      'enableResizing': true,
      'showTooltip': false,
      'dragTipTemplate': null,
      'cls': 'b-selected-time-span',
      'dragHelperConfig': { 'skipUpdatingElement': true },
      'resizeHelperConfig': { 'skipUpdatingElement': true },
      'enableDragSelect': false,
      'headerRenderer'({timeRange: a}) {
        const {dateFormat: b} = this.client.timeAxisViewModel.bottomHeader;
        return '<span class="b-selection-start">' + DateHelper.format(a.startDate, b) + '</span>\n                        <span class="b-selection-end">' + DateHelper.format(a.endDate, b) + '</span>\n                        <i class=\'b-icon b-icon-close\' data-ref="closeButton" data-btip="L{Popup.close}"></i>';
      },
      'selectedTimeSpan': {
        '$config': null,
        'value': null,
        'default': null
      }
    };
  }
  onUIReady() {
    const a = this;
    super.onUIReady();
    if (a.hoverTooltip) {
      a.hoverTooltip.getHtml = () => a.tooltipTemplate({ ...a.selectedTimeSpan });
    }
    a.client.ion({
      'timeAxisHeaderMouseDown': 'onTimeAxisHeaderMouseDown',
      'timeAxisHeaderClick': 'onTimeAxisHeaderClick',
      'thisObj': a
    });
  }
  onTimeAxisHeaderMouseDown({
    startDate: a,
    endDate: b
  }) {
    if (!this.disabled && this.enableDragSelect) {
      this.selectedTimeSpan = null;
    }
  }
  onTimeAxisHeaderClick({
    startDate: a,
    endDate: b
  }) {
    if (!this.disabled) {
      this.selectedTimeSpan = {
        'startDate': a,
        'endDate': b
      };
    }
  }
  onTimeRangeClick(a) {
    super.onTimeRangeClick(a);
    const b = this;
    if (!b.disabled && a.target.closest('.b-selected-time-span')) {
      if (a.target.matches('[data-ref="closeButton"]')) {
        b.selectedTimeSpan = null;
      } else {
        b.client.trigger('timeSelectionElementClick', {
          ...b.selectedTimeSpan,
          'domEvent': a
        });
      }
    }
  }
  updateFromCoords(a, b, c, e) {
    const {client: f} = this, g = f.isHorizontal ? c : e, i = f.isHorizontal ? a : b, j = f.getDateFromVirtualCoord({
        'coord': i,
        'roundingMethod': 'round'
      }) || f.startDate, k = f.getDateFromVirtualCoord({
        'coord': i + g,
        'roundingMethod': 'round'
      }) || f.endDate;
    this.selectedTimeSpan = {
      'startDate': j,
      'endDate': k
    };
  }
  onResizeStart({source: a}) {
    const {
        timeAxisViewModel: b,
        timeResolution: c
      } = this.client, e = DateHelper.asMilliseconds(c.increment, c.unit);
    this.resize.minWidth = b.getDistanceForDuration(e);
    super.onResizeStart(...arguments);
  }
  onResizeDrag({context: a}) {
    let b, c;
    this.updateFromCoords(a.newX, a.newY, (b = a.newWidth) != null ? b : a.elementWidth, (c = a.newHeight) != null ? c : a.elementHeight);
  }
  onDragStart(a) {
    super.onDragStart(a);
    const {context: b} = a;
    b.elementWidth = b.element.offsetWidth;
    b.elementHeight = b.element.offsetHeight;
  }
  onDrag({context: a}) {
    this.updateFromCoords(a.newX, a.newY, a.elementWidth, a.elementHeight);
  }
  onDrop() {
    this.client.element.classList.remove('b-dragging-timerange');
  }
  onDragReset() {
    this.refresh();
  }
  get timeRanges() {
    return this.selectedTimeSpan ? [this.selectedTimeSpan] : [];
  }
  shouldRenderRange() {
    return true;
  }
  changeSelectedTimeSpan(a) {
    if (a) {
      a.id = 'time-selection';
    }
    return a;
  }
  updateSelectedTimeSpan(a) {
    if (!this.isConfiguring) {
      this.refresh();
    }
    this.client.trigger('timeSelectionChange', { ...a != null ? a : {} });
  }
  onTimeSpanDragSelected({
    startDate: a,
    endDate: b
  }) {
    this.selectedTimeSpan = {
      'startDate': a,
      'endDate': b
    };
  }
};
TimeSelection._$name = 'TimeSelection';
GridFeatureManager.registerFeature(TimeSelection, false, 'Scheduler');
const ScheduleRange = {
  'completeview': 'completeview',
  'currentview': 'currentview',
  'daterange': 'daterange'
};
const SchedulerExporterMixin_default = a => class b extends a {
  cloneElement(c, e, f) {
    super.cloneElement(c, e, f);
    const g = this.element.querySelector('.b-schedulerbase');
    g == null ? void 0 : g.classList.remove(...[
      'fade-in',
      'slide-from-left',
      'slide-from-top',
      'zoom-in'
    ].map(i => 'b-initial-' + i));
  }
  async prepareComponent(c) {
    const e = this, {client: f} = c, {isVertical: g} = f, i = f.timeAxisSubGrid.width > 0;
    switch (c.scheduleRange) {
    case ScheduleRange.completeview:
      c.rangeStart = f.startDate;
      c.rangeEnd = f.endDate;
      break;
    case ScheduleRange.currentview: {
        const {
          startDate: v,
          endDate: w
        } = f.visibleDateRange;
        c.rangeStart = v;
        c.rangeEnd = w;
        break;
      }
    }
    await f.waitForAnimations();
    c.infiniteScroll = f.infiniteScroll;
    f.infiniteScroll = false;
    if (i) {
      if (c.scheduleRange !== ScheduleRange.currentview) {
        f.setTimeSpan(c.rangeStart, c.rangeEnd);
      }
      if (c.scheduleRange === ScheduleRange.daterange) {
        c.rangeStart = f.startDate;
        c.rangeEnd = f.endDate;
      }
      f.svgCanvas;
    }
    e._oldEnableEventAnimations = f.enableEventAnimations;
    f.enableEventAnimations = false;
    await super.prepareComponent(c);
    const {
        exportMeta: j,
        element: k
      } = e, l = k.querySelector('.b-sch-foreground-canvas'), n = k.querySelector('.b-resource-time-range-canvas'), o = k.querySelector('.b-' + (g ? 'vertical' : 'horizontal') + 'timeaxis'), p = j.subGrids.normal;
    DomHelper.forEachSelector(k, '.b-sch-canvas', z => {
      let A = '';
      for (const C of z.classList.values()) {
        A += '.' + C;
      }
      const B = f.element.querySelector(A);
      if (B && B.clientWidth) {
        z.style.width = B.clientWidth + 'px';
      }
    });
    j.includeTimeline = i;
    j.timeAxisHeaders = [];
    j.timeAxisPlaceholders = [];
    j.headersCollected = false;
    j.eventsBoxes = new Map();
    j.resourceTimeRangesMap = new Map();
    j.eventsMap = new Map();
    j.verticalTimeAxisMargin = 0;
    j.isHorizontal = !g;
    j.client = f;
    if (i) {
      if (c.scheduleRange !== ScheduleRange.completeview) {
        if (g) {
          j.totalHeight = f.headerHeight + f.getTimeSpanDistance(c.rangeStart, c.rangeEnd) + f.footerHeight;
          j.verticalTimeAxisMargin = -1 * f.getCoordinateFromDate(c.rangeStart);
        } else {
          j.totalWidth -= p.width;
          j.totalWidth += p.width = f.timeAxisViewModel.getDistanceBetweenDates(c.rangeStart, c.rangeEnd);
          if (e.isMultiPageExporter) {
            const z = Math.ceil(j.totalWidth / j.pageWidth), A = z * j.verticalPages;
            j.horizontalPages = z;
            j.totalPages = A;
          }
          p.scrollLeft = f.getCoordinateFromDate(c.rangeStart);
        }
      } else if (g) {
        j.totalHeight = f.headerHeight + f.timeAxisViewModel.totalSize + f.footerHeight;
      }
    }
    if (!i) {
      return;
    }
    DomHelper.forEachSelector(o, '.b-sch-header-row', B => {
      j.timeAxisPlaceholders.push(e.createPlaceholder(B));
      j.timeAxisHeaders.push(new Map());
    });
    j.eventsPlaceholder = e.createPlaceholder(l, false);
    DomHelper.removeEachSelector(l, '.b-sch-event-wrap');
    DomHelper.removeEachSelector(e.element, '.b-released');
    if (n) {
      j.resourceTimeRangesPlaceholder = e.createPlaceholder(n, false);
      DomHelper.removeEachSelector(n, '.b-sch-resourcetimerange');
    }
    const q = k.querySelector('.b-column-lines-canvas'), r = k.querySelector('.b-timeranges-header-canvas'), u = k.querySelector('.b-timeranges-body-canvas');
    if (f.hasActiveFeature('columnLines') && q) {
      j.columnLinesPlaceholder = e.createPlaceholder(q);
      j.columnLines = '';
    }
    if (u && Object.values(f.features).some(B => B instanceof AbstractTimeRanges)) {
      j.timeRanges = {};
      if (r) {
        j.timeRanges.header = '';
        j.timeRangesHeaderPlaceholder = e.createPlaceholder(r);
      }
      j.timeRanges.body = '';
      j.timeRangesBodyPlaceholder = e.createPlaceholder(u);
    }
    if (f.hasActiveFeature('dependencies')) {
      f.features.dependencies.fillDrawingCache();
      const B = k.querySelector('[id="' + f.svgCanvas.getAttribute('id') + '"]');
      if (B) {
        j.dependencyCanvasEl = B;
        j.dependenciesPlaceholder = e.createPlaceholder(B, false, {
          'ns': 'http://www.w3.org/2000/svg',
          'tag': 'path'
        });
        DomHelper.removeEachSelector(B, '.b-sch-dependency');
      }
    }
  }
  async restoreComponent(c) {
    const {client: e} = c;
    e.infiniteScroll = c.infiniteScroll;
    e.enableEventAnimations = this._oldEnableEventAnimations;
    await super.restoreComponent(c);
  }
  async onRowsCollected(c, e) {
    const f = this;
    await super.onRowsCollected(c, e);
    if (f.exportMeta.includeTimeline) {
      const {
        pageRangeStart: g,
        pageRangeEnd: i
      } = f.getCurrentPageDateRange(e);
      if (g && i) {
        f.renderTimeAxisHeaders(e, g, i);
        f.renderResourceHeaders(e);
        f.renderLines(e, g, i);
        f.renderRanges(e, g, i);
        f.renderEvents(e, c, g, i);
      }
    }
  }
  async stateNextPage(c) {
    let e;
    await ((e = super.stateNextPage) == null ? void 0 : e.call(this, c));
    const f = this;
    f.exportMeta.eventsBoxes.clear();
    f.exportMeta.eventsMap.clear();
    f.exportMeta.resourceTimeRangesMap.clear();
    f.exportMeta.timeAxisHeaders.forEach(g => g.clear());
    f.exportMeta.columnLines = '';
    f.exportMeta.timeRanges = {};
  }
  getCurrentPageDateRange({
    rangeStart: c,
    rangeEnd: e,
    client: f
  }) {
    const g = this, {exportMeta: i} = g, {
        horizontalPages: j,
        horizontalPosition: k,
        pageWidth: l,
        subGrids: n
      } = i;
    let o, p;
    if (j > 1) {
      const q = k * l, r = (k + 1) * l, u = n.locked.width + n.locked.splitterWidth;
      if (r <= u) {
        p = o = null;
      } else {
        const {
          scrollLeft: scrollLeft = 0
        } = n.normal;
        o = f.getDateFromCoordinate(Math.max(q - u + scrollLeft, 0));
        p = f.getDateFromCoordinate(r - u + scrollLeft) || e;
      }
    } else {
      o = c;
      p = e;
    }
    return {
      'pageRangeStart': o,
      'pageRangeEnd': p
    };
  }
  prepareExportElement() {
    const {
        element: c,
        exportMeta: e
      } = this, {
        id: f,
        headerId: g,
        footerId: i,
        scrollLeft: j
      } = e.subGrids.normal, k = c.querySelector('[id="' + f + '"]');
    k.querySelectorAll('.b-sch-canvas').forEach(l => {
      l.style.height = '';
      if (j) {
        l.style.marginInlineStart = '-' + j + 'px';
      }
    });
    if (j) {
      [
        g,
        i
      ].forEach(l => {
        const n = c.querySelector('[id="' + l + '"] .b-widget-scroller');
        if (n) {
          n.style.marginInlineStart = '-' + j + 'px';
        }
      });
    }
    return super.prepareExportElement();
  }
  renderTimeAxisHeaders(c, e, f) {
    const g = this, {exportMeta: i} = g, {client: j} = c, {
        isHorizontal: k,
        timeAxisHeaders: l
      } = i, n = k ? j.timeAxisColumn.timeAxisView : j.verticalTimeAxisColumn.view, o = n.buildCells(e, f), p = document.createElement('div');
    DomSync.sync({
      'targetElement': p,
      'domConfig': o
    });
    DomHelper.forEachSelector(p, '.b-sch-header-row', (q, r) => {
      const u = l[r];
      DomHelper.forEachSelector(q, '.b-sch-header-timeaxis-cell', v => {
        if (!u.has(v.dataset.tickIndex)) {
          u.set(v.dataset.tickIndex, v.outerHTML);
        }
      });
    });
  }
  renderResourceHeaders(c) {
    const e = this, {client: f} = c;
    if (f.isHorizontal) {
      return;
    }
    const {exportMeta: g} = e, i = f.timeAxisColumn.resourceColumns, j = i.renderResourceHeaders(0, f.resourceStore.allResourceRecords.length - 1), k = document.createElement('div');
    DomSync.sync({
      'targetElement': k,
      'domConfig': {
        'onlyChildren': true,
        'children': j
      }
    });
    g.resourceHeaders = k.innerHTML;
  }
  renderEvents(c, e, f, g) {
    const i = this, {client: j} = c, {
        isHorizontal: k,
        eventsMap: l,
        eventsBoxes: n,
        resourceTimeRangesMap: o
      } = i.exportMeta, p = document.createElement('div');
    if (k) {
      e.forEach(q => {
        const r = j.store.getAt(q.dataIndex), u = j.currentOrientation.getResourceLayout(r), v = j.getCoordinateFromDate(f), w = j.getCoordinateFromDate(g), z = j.currentOrientation.getEventDOMConfigForCurrentView(u, q, j.rtl ? w : v, j.rtl ? v : w);
        z.forEach(A => {
          const {eventId: B} = A.dataset, {
              insetInlineStart: C,
              insetBlockStart: E,
              width: F,
              height: G
            } = A.style;
          DomSync.sync({
            'targetElement': p,
            'domConfig': A,
            'callback': ({
              action: I,
              domConfig: J,
              lastDomConfig: K,
              targetElement: L,
              jsx: N
            }) => horizontalRenderingDomSyncCallback(j, false, {
              'action': I,
              'domConfig': J,
              'lastDomConfig': K,
              'targetElement': L,
              'jsx': N
            })
          });
          const H = String(B);
          if (A.elementData.entityName === 'resourceTimeRange') {
            o.set(H, p.outerHTML);
          } else {
            n.set(H, new Rectangle(C, E, F, G));
            l.set(H, p.outerHTML);
          }
        });
      });
    } else {
      j.resources.forEach(q => {
        const r = j.vertical.renderResource(q, f, g);
        for (const u of r) {
          const {eventId: v} = u.dataset, {
              left: w,
              width: z,
              height: A,
              fontSize: B
            } = u.style, C = u.style.top;
          DomSync.sync({
            'targetElement': p,
            'domConfig': u,
            'callback': ({
              action: E,
              domConfig: F,
              lastDomConfig: G,
              targetElement: H,
              jsx: I
            }) => verticalRenderingDomSyncCallback(j, false, {
              'action': E,
              'domConfig': F,
              'lastDomConfig': G,
              'targetElement': H,
              'jsx': I
            })
          });
          if (u.elementData.renderData.entityName === 'resourceTimeRange') {
            o.set(String(v), p.outerHTML);
          } else {
            l.set(v, p.outerHTML);
            n.set(String(v), new Rectangle(w, C, z, typeof A === 'string' ? B : A));
          }
        }
      });
    }
  }
  renderLines(c, e, f) {
    const g = this, {client: i} = c, {exportMeta: j} = g, {columnLinesPlaceholder: k} = j;
    if (k) {
      const l = i.features.columnLines.getColumnLinesDOMConfig(e, f, 0, i.resourceStore.allResourceRecords.length - 1), n = document.createElement('div');
      DomSync.sync({
        'targetElement': n,
        'domConfig': {
          'onlyChildren': true,
          'children': l
        }
      });
      j.columnLines = n.innerHTML;
    }
  }
  renderRanges(c, e, f) {
    const g = this, {client: i} = c, {exportMeta: j} = g, {timeRanges: k} = j;
    if (k) {
      k.body = k.header = '';
      for (const l of Object.values(i.features).filter(n => n instanceof AbstractTimeRanges)) {
        const n = l.getDOMConfig(e, f), o = document.createElement('div');
        n.forEach((p, q) => {
          DomSync.sync({
            'targetElement': o,
            'domConfig': {
              'children': p,
              'onlyChildren': true
            }
          });
          if (q === 0) {
            k.body += o.innerHTML;
          } else {
            k.header += o.innerHTML;
          }
        });
      }
    }
  }
  buildPageHtml() {
    const c = this, {
        subGrids: e,
        timeAxisHeaders: f,
        timeAxisPlaceholders: g,
        eventsMap: i,
        eventsPlaceholder: j,
        resourceTimeRangesMap: k,
        resourceHeaders: l,
        resourceHeaderPlaceholder: n,
        columnLines: o,
        columnLinesPlaceholder: p,
        timeRanges: q,
        timeRangesHeaderPlaceholder: r,
        timeRangesBodyPlaceholder: u,
        resourceTimeRangesPlaceholder: v,
        isHorizontal: w
      } = c.exportMeta;
    let z = c.prepareExportElement();
    if (w) {
      Object.values(e).forEach(({
        placeHolder: A,
        rows: B,
        mergedCellsHtml: C
      }) => {
        const E = A.outerHTML;
        let F = B.join('');
        if (C == null ? void 0 : C.length) {
          F += '<div class="b-grid-merged-cells-container">' + C.join('') + '</div>';
        }
        z = z.replace(E, F);
      });
    }
    if (j) {
      z = z.replace(j.outerHTML, Array.from(i.values()).join(''));
    }
    if (v) {
      z = z.replace(v.outerHTML, Array.from(k.values()).join(''));
    }
    if (n) {
      z = z.replace(n.outerHTML, l);
    }
    f.forEach((A, B) => {
      z = z.replace(g[B].outerHTML, Array.from(A.values()).join(''));
    });
    if (o) {
      z = z.replace(p.outerHTML, o);
    }
    if (q && u) {
      z = z.replace(u.outerHTML, q.body);
      if (r) {
        z = z.replace(r.outerHTML, q.header);
      }
    }
    z = c.buildDependenciesHtml(z);
    return z;
  }
  getEventBox(c, e) {
    const f = this, {
        client: g,
        eventsBoxes: i,
        currentPageFirstRowIndex: j,
        exactGridHeight: k,
        totalHeight: l
      } = f.exportMeta, {
        resourceColumnWidth: n,
        rowHeight: o,
        isHorizontal: p,
        resourceStore: q
      } = g;
    let r = c && i.get(String(c.id));
    if (r && c.isMilestone) {
      r.translate(-r.width / 2, 0);
    }
    if (!r) {
      const u = c.assignments.reduce((v, {resource: w}) => {
        if (v === null) {
          v = q.indexOf(w);
        } else {
          v = Math.max(v, q.indexOf(w));
        }
        return v;
      }, null);
      if (p) {
        if (u < j) {
          r = new Rectangle(0, -o * 2, 10, o);
        } else {
          r = new Rectangle(0, k + o, 10, o);
        }
      } else {
        r = new Rectangle(u * n, l + o, n, o);
      }
    }
    return r;
  }
  renderDependencies() {
    const c = this, {
        client: e,
        eventsBoxes: f
      } = c.exportMeta, {dependencies: g} = e, i = e.features.dependencies, j = DomHelper.createElement();
    let k = false;
    g.forEach(l => {
      let n, o;
      if (!f.has(String((n = l.fromEvent) == null ? void 0 : n.id)) && !f.has(String((o = l.toEvent) == null ? void 0 : o.id)) || !i.isDependencyVisible(l)) {
        return;
      }
      const p = c.getEventBox(l.fromEvent, l), q = c.getEventBox(l.toEvent, l);
      i.drawDependency(l, true, {
        'from': p == null ? void 0 : p.clone(),
        'to': q == null ? void 0 : q.clone()
      });
      k = true;
    });
    if (k) {
      i.domSync(j, true);
    }
    return j.innerHTML;
  }
  buildDependenciesHtml(c) {
    const {
      dependenciesPlaceholder: e,
      includeTimeline: f
    } = this.exportMeta;
    if (e && f) {
      const g = e.outerHTML;
      c = c.replace(g, this.renderDependencies());
    }
    return c;
  }
};
const MultiPageExporter2 = class extends SchedulerExporterMixin_default(MultiPageExporter) {
  static type = 'multipage';
  static $name = 'MultiPageExporter';
  static isHorizontal = true;
  async stateNextPage(a) {
    await super.stateNextPage(a);
    this.exportMeta.eventsBoxes.clear();
  }
};
MultiPageExporter2._$name = 'MultiPageExporter';
const MultiPageVerticalExporter2 = class extends SchedulerExporterMixin_default(MultiPageVerticalExporter) {
  static type = 'multipagevertical';
  static $name = 'MultiPageVerticalExporter';
  static isHorizontal = true;
  async prepareComponent(a) {
    await super.prepareComponent(a);
    if (a.scheduleRange !== ScheduleRange.completeview) {
      this.estimateTotalPages(a);
    }
  }
};
MultiPageVerticalExporter2._$name = 'MultiPageVerticalExporter';
const ScheduleRangeCombo = class extends Combo {
  static get $name() {
    return 'ScheduleRangeCombo';
  }
  static get type() {
    return 'schedulerangecombo';
  }
  static get defaultConfig() {
    return {
      'editable': false,
      'localizeDisplayFields': true,
      'displayField': 'text',
      'buildItems'() {
        return Object.entries(ScheduleRange).map(([a, b]) => ({
          'value': a,
          'text': 'L{' + b + '}'
        }));
      }
    };
  }
};
ScheduleRangeCombo.initClass();
ScheduleRangeCombo._$name = 'ScheduleRangeCombo';
const SchedulerExportDialog = class extends ExportDialog {
  static get $name() {
    return 'SchedulerExportDialog';
  }
  static get type() {
    return 'schedulerexportdialog';
  }
  static get configurable() {
    return {
      'defaults': { 'localeClass': this },
      'items': {
        'scheduleRangeField': {
          'type': 'schedulerangecombo',
          'label': 'L{Schedule range}',
          'name': 'scheduleRange',
          'value': 'completeview',
          'weight': 150,
          'onChange'({value: a}) {
            this.parent.widgetMap.rangesContainer.hidden = a !== ScheduleRange.daterange;
          }
        },
        'rangesContainer': {
          'type': 'container',
          'flex': '1 0 100%',
          'weight': 151,
          'hidden': true,
          'defaults': { 'localeClass': this },
          'items': {
            'filler': {
              'weight': 0,
              'type': 'widget',
              'style': 'margin-inline-end: -1em;'
            },
            'rangeStartField': {
              'type': 'datefield',
              'label': 'L{Export from}',
              'name': 'rangeStart',
              'labelWidth': '3em',
              'flex': '1 0 25%',
              'weight': 10,
              'onChange'({value: a}) {
                this.parent.widgetMap.rangeEndField.min = DateHelper.add(a, 1, 'd');
              }
            },
            'rangeEndField': {
              'type': 'datefield',
              'label': 'L{Export to}',
              'name': 'rangeEnd',
              'labelWidth': '1em',
              'flex': '1 0 25%',
              'weight': 30,
              'onChange'({value: a}) {
                this.parent.widgetMap.rangeStartField.max = DateHelper.add(a, -1, 'd');
              }
            }
          }
        }
      }
    };
  }
  onLocaleChange() {
    const a = this.L('labelWidth');
    this.width = this.L('L{width}');
    this.items.forEach(b => {
      if (b instanceof Field) {
        b.labelWidth = a;
      } else if (b.ref === 'rangesContainer') {
        b.items[0].width = a;
      }
    });
  }
  applyInitialValues(a) {
    super.applyInitialValues(a);
    const b = this, {
        client: c,
        scheduleRange: e
      } = a, f = a.items = a.items || {}, g = f.scheduleRangeField = f.scheduleRangeField || {}, i = f.rangesContainer = f.rangesContainer || {}, j = i.items = i.items || {}, k = j.filler = j.filler || {}, l = j.rangeStartField = j.rangeStartField || {}, n = j.rangeEndField = j.rangeEndField || {};
    k.width = b.L('labelWidth');
    g.value = g.value || e;
    if (g.value === ScheduleRange.daterange) {
      i.hidden = false;
    }
    const o = l.value = l.value || c.startDate;
    l.max = DateHelper.max(c.startDate, DateHelper.add(c.endDate, -1, 'd'));
    let p = n.value || c.endDate;
    if (p <= o) {
      p = DateHelper.add(o, 1, 'd');
    }
    n.value = p;
    n.min = DateHelper.min(c.endDate, DateHelper.add(c.startDate, 1, 'd'));
  }
};
SchedulerExportDialog._$name = 'SchedulerExportDialog';
const SinglePageExporter2 = class extends SchedulerExporterMixin_default(SinglePageExporter) {
  static type = 'singlepage';
  static $name = 'SinglePageExporter';
  static isHorizontal = true;
};
SinglePageExporter2._$name = 'SinglePageExporter';
const VerticalExporter = class extends ExporterBase {
  static isHorizontal = false;
  cloneElement(a, b = this.element, c = true) {
    super.cloneElement(a, b, c);
  }
  async prepareComponent(a) {
    await super.prepareComponent(a);
    const {
        exportMeta: b,
        element: c
      } = this, {resourceColumns: e} = a.client.timeAxisColumn;
    b.resourceHeaderPlaceholder = this.createPlaceholder(c.querySelector('#' + e.id));
    b.resourceHeaders = '';
  }
  async onRowsCollected() {
  }
};
VerticalExporter._$name = 'VerticalExporter';
const SinglePageExporterVertical = class extends SchedulerExporterMixin_default(VerticalExporter) {
  static configurable = { 'centerContentHorizontally': false };
  static type = 'verticalsinglepage';
  static $name = 'SinglePageExporterVertical';
  static get title() {
    return this.localize('L{SinglePageExporter.singlepage}');
  }
  async prepareComponent(a) {
    await super.prepareComponent(a);
    Object.assign(this.exportMeta, {
      'verticalPages': 1,
      'horizontalPages': 1,
      'totalPages': 1,
      'currentPage': 0,
      'verticalPosition': 0,
      'horizontalPosition': 0
    });
  }
  async renderPage(a) {
    const {totalHeight: b} = this.exportMeta;
    await this.onRowsCollected([], a);
    this.exportMeta.exactGridHeight = b;
    return b;
  }
  buildPageHtml(a) {
    const {subGrids: b} = this.exportMeta;
    let c = super.buildPageHtml(a);
    Object.values(b).forEach(({placeHolder: e}) => {
      const f = e.outerHTML, g = '';
      c = c.replace(f, g);
    });
    return c;
  }
};
SinglePageExporterVertical.prototype.pagesExtractor = async function* pagesExtractor5(a) {
  const b = this, {client: c} = a, {
      totalWidth: e,
      verticalTimeAxisMargin: f
    } = b.exportMeta, g = b.stylesheets, i = a.orientation === Orientation.portrait, j = PaperFormat[a.paperFormat], k = a.useBrowserPrint, l = b.getPaperWidth(j, i), n = b.getPaperHeight(j, i);
  let o, p, q;
  o = await b.renderPage(a);
  if (a.headerTpl) {
    p = b.prepareHTML(a.headerTpl({ 'totalWidth': e }));
    const A = b.measureElement(p, 'b-export-header');
    o += A;
  }
  if (a.footerTpl) {
    q = b.prepareHTML(a.footerTpl({ 'totalWidth': e }));
    const B = b.measureElement(q, 'b-export-footer');
    o += B;
  }
  const r = b.buildPageHtml(a), u = o, v = Math.min(1, b.getScaleValue(b.inchToPx(l), e)), w = Math.min(1, b.getScaleValue(b.inchToPx(n), o)), z = Math.min(v, w);
  g.push('<style>\n                #' + c.id + ' {\n                    height: ' + u + 'px !important;\n                    width: ' + e + 'px !important;\n                }\n                .b-verticaltimeaxiscolumn {\n                    margin-top: ' + f + 'px;\n                }\n                ' + (k ? '\n                            html, body {\n                                overflow: hidden;\n                            }\n                            .b-page-wrap {\n                                width: ' + l + 'in;\n                                height: ' + n + 'in;\n                            }\n                            .b-print:not(.b-firefox) .b-export-content {\n                                zoom: ' + z + ';\n                                height: 100%;\n                            }\n                            .b-print.b-firefox .b-export-content {\n                                transform: scale(' + z + ') ' + (b.centerContentHorizontally ? 'translateX(-50%)' : '') + ';\n                                transform-origin: top left;\n                                height: ' + (z === 1 ? 'inherit' : 'auto !important') + ';\n                            }\n                            .b-export-content {\n                                ' + (b.centerContentHorizontally ? 'left: 50%;' : '') + '\n                            }\n                        ' : '\n                            .b-export-content {\n                                ' + (b.centerContentHorizontally ? 'left: 50%;' : '') + '\n                                transform: scale(' + z + ') ' + (b.centerContentHorizontally ? 'translateX(-50%)' : '') + ';\n                                transform-origin: top left;\n                                height: ' + (z === 1 ? 'inherit' : 'auto !important') + ';\n                            }\n                        ') + '\n            </style>');
  yield {
    'html': b.pageTpl({
      'html': r,
      'header': p,
      'footer': q,
      'styles': g,
      'paperWidth': l,
      'paperHeight': n,
      'currentPage': 0,
      'isPrint': a.useBrowserPrint
    })
  };
};
SinglePageExporterVertical._$name = 'SinglePageExporterVertical';
const MultiPageVerticalExporterVertical = class extends SchedulerExporterMixin_default(VerticalExporter) {
  static type = 'verticalmultipagevertical';
  static $name = 'MultiPageVerticalExporterVertical';
  static get title() {
    return this.localize('L{MultiPageVerticalExporter.multipagevertical}');
  }
  calculateScale() {
    const {exportMeta: a} = this;
    return a.scale = this.getScaleValue(a.pageWidth, a.totalWidth);
  }
  estimateTotalPages(a) {
    let b;
    const c = this, {exportMeta: e} = c, {
        client: f,
        headerTpl: g,
        footerTpl: i,
        repeatHeader: j,
        rangeStart: k,
        rangeEnd: l
      } = a, {pageHeight: n} = e, {timeAxisViewModel: o} = f, p = o.getDistanceBetweenDates(k, l), q = (l - k) / p;
    const r = e.totalWidth = f.timeAxisColumn.resourceColumns.totalWidth + f.subGrids.locked.width + ((b = f.subGrids.locked.splitterElement) == null ? void 0 : b.offsetWidth) || 0, u = c.calculateScale();
    let v = e.totalHeight = p + f.headerHeight + f.footerHeight, w = n / u;
    if (g) {
      w -= c.measureElement(g({
        'totalWidth': r,
        'totalPages': -1,
        'currentPage': -1
      }), 'b-export-header');
    }
    if (i) {
      w -= c.measureElement(i({
        'totalWidth': r,
        'totalPages': -1,
        'currentPage': -1
      }), 'b-export-footer');
    }
    if (j) {
      v -= f.headerHeight;
    }
    let z = Math.ceil(v / w);
    if (j) {
      v += f.headerHeight * z;
      z = Math.ceil(v / w);
    }
    Object.assign(e, {
      'scale': u,
      'contentHeight': w,
      'totalWidth': r,
      'totalHeight': v,
      'verticalPages': z,
      'msPerPixel': q,
      'initialScroll': 0,
      'horizontalPages': 1,
      'totalPages': z,
      'currentPage': 0,
      'firstTickTop': 0
    });
  }
  async stateNextPage(a) {
    ++this.exportMeta.currentPage;
    ++this.exportMeta.verticalPosition;
    await super.stateNextPage(a);
  }
  async prepareComponent(a) {
    await super.prepareComponent(a);
    this.estimateTotalPages(a);
    this.exportMeta.pageRangeStart = a.rangeStart;
    this.exportMeta.exactGridHeight = this.exportMeta.totalHeight;
  }
  async renderRows(a) {
    const b = this, {exportMeta: c} = b, {client: e} = a, {
        rangeEnd: f,
        repeatHeader: g
      } = a, {
        currentPage: i,
        contentHeight: j,
        msPerPixel: k,
        pageRangeStart: l
      } = c;
    let n = j;
    if (i === 0 || g) {
      n -= e.headerHeight;
    }
    const o = n * k;
    c.firstTickTop = e.timeAxisViewModel.getPositionFromDate(l);
    c.pageRangeEnd = DateHelper.min(new Date(l.getTime() + o), f);
    await b.onRowsCollected([], a);
    if (c.pageRangeEnd.getTime() < f.getTime()) {
      const p = Math.floor(e.timeAxis.getTickFromDate(c.pageRangeEnd, true));
      c.pageRangeStart = e.timeAxis.getAt(p).startDate;
    }
  }
  getCurrentPageDateRange() {
    return {
      'pageRangeStart': this.exportMeta.pageRangeStart,
      'pageRangeEnd': this.exportMeta.pageRangeEnd
    };
  }
  async buildPage(a) {
    const b = this, {exportMeta: c} = b, {
        headerTpl: e,
        footerTpl: f
      } = a, {
        totalWidth: g,
        totalPages: i,
        currentPage: j,
        subGrids: k
      } = c;
    Object.values(k).forEach(p => p.rows = []);
    let l, n;
    if (e) {
      l = b.prepareHTML(e({
        'totalWidth': g,
        'totalPages': i,
        'currentPage': j
      }));
    }
    if (f) {
      n = b.prepareHTML(f({
        'totalWidth': g,
        'totalPages': i,
        'currentPage': j
      }));
    }
    await b.renderRows(a);
    const o = b.buildPageHtml(a);
    return {
      'html': o,
      'header': l,
      'footer': n
    };
  }
};
MultiPageVerticalExporterVertical.prototype.pagesExtractor = async function* pagesExtractor6(a) {
  const b = this, {
      exportMeta: c,
      stylesheets: e
    } = b, {
      totalPages: f,
      totalWidth: g,
      paperWidth: i,
      paperHeight: j,
      realPaperWidth: k,
      realPaperHeight: l,
      title: n
    } = c, o = a.useBrowserPrint;
  let p, q;
  while ((p = c.currentPage) < f) {
    b.trigger('exportStep', {
      'text': MultiPageVerticalExporter2.L(MultiPageVerticalExporter2.exportingPageText, {
        'currentPage': p,
        'totalPages': f
      }),
      'progress': Math.round((p + 1) / f * 90)
    });
    const {
        html: r,
        header: u,
        footer: v
      } = await b.buildPage(a), {
        scale: w,
        firstTickTop: z
      } = b.exportMeta;
    q = '\n            .b-page-' + p + ' .b-grid-vertical-scroller {\n                margin-top: -' + z + 'px\n            }\n            .b-page-' + p + ' #' + a.client.id + ' {\n                height: ' + c.exactGridHeight + 'px !important;\n                width: ' + g + 'px !important;\n            }\n            ' + (o ? '\n                        .b-page-wrap {\n                            width: ' + k + 'in;\n                            height: ' + l + 'in;\n                        }\n                        .b-print:not(.b-firefox) .b-export-content {\n                            zoom: ' + w + ';\n                            height: 100%;\n                        }\n                        .b-print.b-firefox .b-export-content {\n                            transform: scale(' + w + ');\n                            transform-origin: top left;\n                            height: ' + 100 / w + '%;\n                            width: ' + 100 / w + '%;\n                        }\n                    ' : '\n                        .b-export .b-page-' + p + '.b-export-content {\n                            transform: scale(' + w + ');\n                            transform-origin: top left;\n                            height: ' + 100 / w + '%;\n                            width: ' + 100 / w + '%;\n                        }\n                    ') + '\n        ';
    if (a.repeatHeader) {
      q = '\n                ' + q + '\n                .b-export-body {\n                    height: 100%;\n                    display: flex;\n                }\n                .b-export-viewport {\n                    height: 100%;\n                }\n            ';
    } else {
      q = '\n                ' + q + '\n                ' + (p === 0 ? '' : '.b-page-' + p + ' header.b-grid-header-container {\n                    display: none;\n                }') + '\n                .b-export-body {\n                    overflow: hidden;\n                }\n            ';
    }
    const A = [
      ...e,
      '<style>' + q + '</style>'
    ];
    await b.stateNextPage(a);
    yield {
      'html': b.pageTpl({
        'html': r,
        'title': n,
        'header': u,
        'footer': v,
        'styles': A,
        'paperWidth': i,
        'paperHeight': j,
        'realPaperWidth': k,
        'realPaperHeight': l,
        'currentPage': p,
        'isPrint': o
      })
    };
  }
};
MultiPageVerticalExporterVertical._$name = 'MultiPageVerticalExporterVertical';
const PdfExport2 = class extends PdfExport {
  static get $name() {
    return 'PdfExport';
  }
  static get defaultConfig() {
    return {
      'exporters': [
        SinglePageExporter2,
        MultiPageExporter2,
        MultiPageVerticalExporter2,
        SinglePageExporterVertical,
        MultiPageVerticalExporterVertical
      ],
      'dialogClass': SchedulerExportDialog,
      'scheduleRange': 'completeview',
      'rangeStart': null,
      'rangeEnd': null
    };
  }
  get defaultExportDialogConfig() {
    const a = ObjectHelper.copyProperties(super.defaultExportDialogConfig, this, ['scheduleRange']), {isHorizontal: b} = this.client;
    if (!b) {
      a.items.exporterTypeField.value = 'verticalsinglepage';
    }
    ObjectHelper.merge(a, {
      'items': {
        'columnsField': { 'hidden': !b },
        'rowsRangeField': { 'hidden': !b },
        'alignRowsField': { 'hidden': !b }
      }
    });
    a.exporters = a.exporters.filter(c => c.isHorizontal === b);
    return a;
  }
  buildExportConfig(a) {
    a = super.buildExportConfig(a);
    const {
      scheduleRange: b,
      rangeStart: c,
      rangeEnd: e
    } = this;
    if (a.columns && !a.columns.find(f => f.type === 'timeAxis')) {
      a.columns.push(a.client.timeAxisColumn.id);
    }
    return ObjectHelper.assign({
      'scheduleRange': b,
      'rangeStart': c,
      'rangeEnd': e
    }, a);
  }
};
PdfExport2._$name = 'PdfExport';
GridFeatureManager.registerFeature(PdfExport2, false, 'Scheduler');
const Print2 = class extends PrintMixin_default(PdfExport2) {
  static $name = 'Print';
};
Print2._$name = 'Print';
GridFeatureManager.registerFeature(Print2, false, 'Scheduler');
const ScheduleTableExporter = class extends Localizable_default(TableExporter) {
  static configurable = {
    'includeUnassigned': true,
    'includeEventsOutsideTimeAxis': null,
    'eventColumns': [
      {
        'text': 'Task',
        'field': 'name'
      },
      {
        'text': 'Starts',
        'field': 'startDate',
        'width': 140
      },
      {
        'text': 'Ends',
        'field': 'endDate',
        'width': 140
      }
    ],
    'resourceColumns': null,
    'eventSortFn': null
  };
  construct(a = {}, ...b) {
    super.construct(a, ...b);
    if (!this.eventSortFn) {
      this.eventSortFn = (c, e) => c.internalId - e.internalId;
    }
  }
  normalizeColumns(a) {
    a.columns = a.resourceColumns || a.columns;
    super.normalizeColumns(a);
    a.columns = a.columns.filter(b => !b.isVerticalTimeAxisColumn);
    a.eventColumns = a.eventColumns.map(b => {
      if (typeof b === 'string') {
        return { 'field': b };
      }
      return b;
    });
  }
  generateExportData(a) {
    const b = this, c = b.generateResourceColumns(a), e = b.target.eventStore ? b.generateEventColumns(a) : [], f = c.concat(e), g = b.generateRows(a);
    return {
      'columns': f,
      'rows': g
    };
  }
  generateEventColumns(a) {
    return a.eventColumns.map(b => this.processEventColumn(b, a));
  }
  generateResourceColumns(a) {
    const b = this.target;
    if (b instanceof SchedulerBase && b.isHorizontal) {
      return super.generateColumns(a);
    }
    return a.columns.map(c => this.processResourceColumn(c, a));
  }
  processEventColumn(a, b) {
    const {
        width: c,
        minWidth: e
      } = a, {defaultColumnWidth: f} = b;
    return {
      'field': a.field,
      'value': a.text,
      'width': Math.max(c || f, e || f),
      'eventColumn': true,
      'type': this.getColumnType(a, this.target.eventStore)
    };
  }
  processResourceColumn(a, b) {
    const {
        width: c,
        minWidth: e
      } = a, {defaultColumnWidth: f} = b;
    return {
      'field': a.field,
      'value': a.text,
      'width': Math.max(c || f, e || f),
      'type': this.getColumnType(a, this.target.resourceStore)
    };
  }
  generateRows(a) {
    const b = this, {target: c} = b;
    let e;
    if (!c.resourceStore || c.isGanttBase) {
      e = super.generateRows(a);
    } else {
      e = [];
      c.resourceStore.map(f => {
        const g = f.events || [];
        g.sort(b.eventSortFn);
        if (c.isSchedulerBase && !g.length) {
          g.push('');
        }
        g.forEach(i => {
          if (i === '' || c.eventStore.isAvailable(i) && (a.includeEventsOutsideTimeAxis || i.isScheduled && c.intersectsVisibleTimeSpan(i))) {
            e.push(b.getRowData(a, f, i));
          }
        });
      });
      if (a.includeUnassigned && a.eventColumns.length) {
        const f = c.eventStore.query(g => !g.resources.length);
        if (f.length) {
          const g = new Array(a.columns.length).fill('');
          g.push(b.L('L{ExcelExporter.No resource assigned}'));
          e.push(g);
          f.forEach(i => e.push(b.getRowData(a, null, i)));
        }
      }
      e = e.filter(i => i.length);
    }
    return e;
  }
  getRowData(a, b, c) {
    const {
        columns: e,
        eventColumns: f
      } = a, g = [], i = this.processRecord(b, e, a);
    i && g.push(...i);
    if (!b || !b.isSpecialRow) {
      const j = this.processRecord(c, f, a);
      j && g.push(...j);
    }
    return g;
  }
};
ScheduleTableExporter._$name = 'ScheduleTableExporter';
const ExcelExporter2 = class extends ExcelExporter {
  static configurable = {
    'exporterClass': ScheduleTableExporter,
    'exporterConfig': null
  };
  static $name = 'ExcelExporter';
};
ExcelExporter2._$name = 'ExcelExporter';
GridFeatureManager.registerFeature(ExcelExporter2, false, ['Scheduler']);
const Scheduler = class extends SchedulerBase {
  static get $name() {
    return 'Scheduler';
  }
  static get type() {
    return 'scheduler';
  }
};
Scheduler.initClass();
Scheduler._$name = 'Scheduler';
const DelayedRecordsRendering_default = a => {
  let b;
  return b = class extends (a || Base) {
    static get properties() {
      return { 'recordsToRefresh': new Set() };
    }
    beforeRenderRow({record: c}) {
      let e;
      if ((e = this.recordIsReadyForRendering) == null ? void 0 : e.call(this, c)) {
        this.unscheduleRecordRefresh(c);
      }
      return super.beforeRenderRow(...arguments);
    }
    cleanupScheduledRecord() {
      const {
        rowManager: c,
        store: e
      } = this;
      for (const f of [...this.recordsToRefresh]) {
        if (!f.stores.includes(e) || !c.getRowById(f)) {
          this.recordsToRefresh.delete(f);
        }
      }
    }
    renderScheduledRecords() {
      const c = this;
      if (!c.refreshSuspended) {
        c.cleanupScheduledRecord();
        const {rowManager: e} = c, f = [...c.recordsToRefresh], g = f.map(i => e.getRowById(i));
        if (g.length) {
          e.renderRows(g);
          c.trigger('scheduledRecordsRender', {
            'records': f,
            'rows': g
          });
        }
        if (c.recordsToRefresh.size) {
          c.scheduleRecordRefresh();
        }
      } else {
        c.scheduleRecordRefresh();
      }
    }
    unscheduleRecordRefresh(c = true) {
      const e = this;
      if (c === true) {
        e.recordsToRefresh.clear();
      } else if (c) {
        ArrayHelper.asArray(c).forEach(f => e.recordsToRefresh.delete(f));
      }
      if (e.scheduledRecordsRefreshTimer && !e.recordsToRefresh.size) {
        e.clearTimeout(e.scheduledRecordsRefreshTimer);
      }
    }
    scheduleRecordRefresh(c) {
      const e = this;
      if (c) {
        ArrayHelper.asArray(c).forEach(f => e.recordsToRefresh.add(f));
      }
      e.scheduledRecordsRefreshTimer = e.setTimeout({
        'fn': 'renderScheduledRecords',
        'delay': e.scheduledRecordsRefreshTimeout,
        'cancelOutstanding': true
      });
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'DelayedRecordsRendering'), __publicField(b, 'configurable', { 'scheduledRecordsRefreshTimeout': 15 }), b;
};
const TimelineHistogramRendering = class extends Base {
  static configurable = { 'scrollBuffer': 0 };
  construct(a) {
    super.construct();
    this.client = a;
  }
  init() {
  }
  onTimeAxisViewModelUpdate() {
    const {scrollable: a} = this.client.timeAxisSubGrid;
    this.updateFromHorizontalScroll(a.x);
  }
  updateFromHorizontalScroll(a) {
    const b = this, {
        client: c,
        scrollBuffer: e
      } = b, {
        timeAxisSubGrid: f,
        timeAxis: g,
        rtl: i
      } = c, {width: j} = f, {totalSize: k} = c.timeAxisViewModel, l = a, n = f.scrollable.maxX !== 0 && Math.abs(f.scrollable.maxX) <= Math.round(l) + 5, o = c.getDateFromVirtualCoord({ 'coord': Math.max(0, l - e) }), p = n ? g.endDate : c.getDateFromVirtualCoord({ 'coord': l + j + e }) || g.endDate;
    if (o && !c._viewPresetChanging) {
      b._visibleDateRange = {
        'startDate': o,
        'endDate': p,
        'startMS': o.getTime(),
        'endMS': p.getTime()
      };
      b.viewportCoords = i ? {
        'left': k - a - j + e,
        'right': k - a - e
      } : {
        'left': a - e,
        'right': a + j + e
      };
      const q = c.timeView.range = {
        'startDate': o,
        'endDate': p
      };
      c.internalOnVisibleDateRangeChange(q);
      if (!c.refreshSuspended && c.rowManager.rows.length) {
        if (c.rowManager.rows[0].id === null) {
          return;
        }
        if (b._timeAxisStartDate - g.startDate || b._timeAxisEndDate - g.endDate) {
          b._timeAxisStartDate = g.startDate;
          b._timeAxisEndDate = g.endDate;
          c.rowManager.renderRows(c.rowManager.rows);
        }
      }
    }
  }
  onViewportResize() {
  }
  refreshRows() {
  }
  get visibleDateRange() {
    return this._visibleDateRange;
  }
  translateToPageCoordinate(a) {
    const {client: b} = this, {scrollable: c} = b.timeAxisSubGrid;
    let e = a + b.timeAxisSubGridElement.getBoundingClientRect().left;
    if (b.rtl) {
      e -= c.maxX - Math.abs(b.scrollLeft);
    } else {
      e -= b.scrollLeft;
    }
    return e;
  }
  translateToScheduleCoordinate(a) {
    const {client: b} = this, {scrollable: c} = b.timeAxisSubGrid;
    let e = a - b.timeAxisSubGridElement.getBoundingClientRect().left - globalThis.scrollX;
    if (b.rtl) {
      e += c.maxX - Math.abs(b.scrollLeft);
    } else {
      e += b.scrollLeft;
    }
    return e;
  }
  getDateFromXY(a, b, c, e = false) {
    const {client: f} = this;
    let g = a[0];
    if (!c) {
      g = this.translateToScheduleCoordinate(g);
    }
    g = f.getRtlX(g);
    return f.timeAxisViewModel.getDateFromPosition(g, b, e);
  }
};
TimelineHistogramRendering._$name = 'TimelineHistogramRendering';
const histogramWidgetCleanState = {
  'series': null,
  'topValue': null
};
const emptyFn4 = () => {
};
const TimelineHistogramBase = class extends TimelineBase.mixin(DelayedRecordsRendering_default) {
  static configurable = {
    'timeCellCls': 'b-sch-timeaxis-cell b-timelinehistogram-cell',
    'mode': 'horizontal',
    'rowHeight': 50,
    'showBarTip': false,
    'barTooltip': null,
    'barTooltipClass': Tooltip,
    'series': null,
    'dataModelField': 'histogramData',
    'getRecordData': null,
    'hardRefreshOnTimeAxisReconfigure': true,
    'getBarClass': null,
    'getOutlineClass'(a) {
      return '';
    },
    'getOutlineDOMConfig'(a, b, c) {
      return b;
    },
    'readOnly': true,
    'barTooltipTemplate': null,
    'getBarText': null,
    'getBarDOMConfig': null,
    'getBarTextDOMConfig': void 0,
    'histogramWidgetClass': Histogram,
    'histogramWidget': {
      'cls': 'b-hide-offscreen b-timelinehistogram-histogram',
      'omitZeroHeightBars': true,
      'data': []
    },
    'fixedRowHeight': true,
    'timeAxisColumnConfig': {
      'type': 'timeAxis',
      'renderer': 'up.histogramRenderer'
    }
  };
  static type = 'timelinehistogrambase';
  static $name = 'TimelineHistogramBase';
  static get properties() {
    return {
      'histogramDataByRecord': new Map(),
      'collectingDataFor': new Map()
    };
  }
  updateGetRecordData(a) {
    this._getRecordData = a ? this.resolveCallback(a) : null;
  }
  updateHardRefreshOnTimeAxisReconfigure(a) {
    const b = 'hardRefreshOnTimeAxisReconfigure';
    if (a) {
      this.timeAxis.ion({
        'name': b,
        'endReconfigure': 'onTimeAxisEndReconfigure',
        'thisObj': this
      });
    } else {
      this.detachListeners(b);
    }
  }
  construct(a) {
    super.construct(a);
    const b = this;
    b.scheduleRefreshRows = b.createOnFrame(b.refreshRows, [], b, true);
    b.rowManager.ion({
      'beforeRowHeight': 'onBeforeRowHeight',
      'thisObj': b
    });
  }
  onDestroy() {
    let a;
    this.clearHistogramDataCache();
    (a = this._histogramWidget) == null ? void 0 : a.destroy();
    this.barTooltip = null;
  }
  resolveTimeSpanRecord(a) {
  }
  getScheduleMouseEventParams(a, b) {
    const c = this.store.getById(a.id);
    return { 'record': c };
  }
  get currentOrientation() {
    if (!this._currentOrientation) {
      this._currentOrientation = new TimelineHistogramRendering(this);
    }
    return this._currentOrientation;
  }
  updateSeries(a) {
    const b = this;
    b.histogramWidget.series = a;
    b._series = b.histogramWidget.series;
    if (b.isPainted && !b.isConfiguring) {
      b.scheduleRefreshRows();
    }
  }
  getAsyncEventSuffixForStore(a) {
    return a.isAbstractPartOfProjectStoreMixin ? 'PreCommit' : '';
  }
  scheduleRefreshRows() {
  }
  getRowHeight() {
    return this.rowHeight;
  }
  onInternalPaint({firstPaint: a}) {
    super.onInternalPaint({ 'firstPaint': a });
    if (a && this.showBarTip) {
      this.barTooltip = {};
    }
  }
  changeBarTooltip(a, b) {
    b == null ? void 0 : b.destroy();
    if (a) {
      return a.isTooltip ? a : this.barTooltipClass.new({
        'forElement': this.timeAxisSubGridElement,
        'forSelector': '.b-histogram rect',
        'hoverDelay': 0,
        'trackMouse': false,
        'cls': 'b-celltooltip-tip',
        'getHtml': this.getTipHtml.bind(this)
      }, this.showBarTip, a);
    }
    return null;
  }
  async getTipHtml(a) {
    if (this.showBarTip && this.barTooltipTemplate) {
      const {activeTarget: b} = a, c = parseInt(b.dataset.index, 10), e = this.getRecordFromElement(b), f = await this.getRecordHistogramData(e);
      return this.barTooltipTemplate({
        ...a,
        'datum': this.extractHistogramDataArray(f, e)[c],
        'record': e,
        'index': c
      });
    }
  }
  collectTicksWidth() {
    const {ticks: a} = this.timeAxis, b = a[0].endDate - a[0].startDate, c = { 0: b };
    let e = b, f = true;
    for (let g = 1, {length: j} = a; g < j; g++) {
      const k = a[g], l = k.endDate - k.startDate;
      if (b !== l) {
        f = false;
      }
      e += l;
      c[g] = l;
    }
    if (!f) {
      const n = {};
      for (let o = 0, {length: p} = a; o < p; o++) {
        n[o] = c[o] / e;
      }
      this.ticksWidth = n;
    } else {
      this.ticksWidth = null;
    }
  }
  changeHistogramWidget(a) {
    let b;
    const c = this;
    if (a && !a.isHistogram) {
      if (c.getBarTextDOMConfig && !a.getBarTextDOMConfig) {
        a.getBarTextDOMConfig = c.getBarTextDOMConfig;
      }
      a = c.histogramWidgetClass.new({
        'owner': c,
        'appendTo': c.element,
        'height': c.rowHeight,
        'width': ((b = c.timeAxisColumn) == null ? void 0 : b.width) || 0,
        'getBarTip': emptyFn4,
        'getBarClass': c.getBarClass || c.getBarClassDefault,
        'getBarText': c.getBarText || c.getBarTextDefault,
        'getBarDOMConfig': c.getBarDOMConfig,
        'getOutlineClass': c.getOutlineClass,
        'getOutlineDOMConfig': c.getOutlineDOMConfig
      }, a);
      a.suspendRefresh();
      c.getBarTextDefault = c.getBarTextDefault.bind(a);
    }
    return a;
  }
  getBarClassDefault(a, b, c) {
  }
  getBarTextDefault(a, b) {
  }
  updateShowBarTip(a) {
    this.barTooltip = a;
  }
  onHistogramDataCacheSet({
    record: a,
    data: b
  }) {
    this.scheduleRecordRefresh(a);
  }
  onTimeAxisEndReconfigure() {
    if (this.hardRefreshOnTimeAxisReconfigure) {
      this.clearHistogramDataCache();
      this.scheduleRefreshRows();
    }
  }
  onStoreUpdateRecord({
    record: a,
    changes: b
  }) {
    const c = this;
    if (!c.getRecordData && c.dataModelField && b[c.dataModelField]) {
      c.clearHistogramDataCache(a);
    }
    return super.onStoreUpdateRecord(...arguments);
  }
  onStoreRemove({records: a}) {
    super.onStoreRemove(...arguments);
    for (const b of a) {
      this.clearHistogramDataCache(b);
    }
  }
  onBeforeRowHeight({height: a}) {
    if (this._timeAxisColumn) {
      const b = this._histogramWidget;
      if (b) {
        b.height = a;
        b.onElementResize(b.element);
      }
    }
  }
  onTimeAxisViewModelUpdate() {
    super.onTimeAxisViewModelUpdate(...arguments);
    const a = this._histogramWidget;
    if (a) {
      a.width = this.timeAxisViewModel.totalSize;
      a.onElementResize(a.element);
    }
    this.collectTicksWidth();
  }
  extractHistogramDataArray(a, b) {
    return a;
  }
  processRecordRenderData(a) {
    return a;
  }
  clearHistogramDataCache(a) {
    const b = ArrayHelper.asArray(a);
    if (b) {
      for (const c of b) {
        this.histogramDataByRecord.delete(c);
      }
    } else {
      this.histogramDataByRecord.clear();
    }
  }
  setHistogramDataCache(a, b) {
    const c = {
      'record': a,
      'data': b
    };
    this.trigger('beforeHistogramDataCacheSet', c);
    this.histogramDataByRecord.set(c.record, c.data);
    this.trigger('histogramDataCacheSet', c);
  }
  getHistogramDataCache(a) {
    return a ? this.histogramDataByRecord.get(a) : this.histogramDataByRecord;
  }
  hasHistogramDataCache(a) {
    return this.histogramDataByRecord.has(a);
  }
  finalizeDataRetrievingInternal(a, b) {
    this.collectingDataFor.delete(a);
    this.setHistogramDataCache(a, b);
    return b;
  }
  finalizeDataRetrieving(a, b) {
    if (Objects.isPromise(b)) {
      this.collectingDataFor.set(a, b);
      return b.then(c => {
        let e;
        return (e = this.finalizeDataRetrievingInternal) == null ? void 0 : e.call(this, a, c);
      });
    }
    return this.finalizeDataRetrievingInternal(a, b);
  }
  getRecordHistogramData(a) {
    const b = this, {getRecordData: c} = b;
    let e = b.collectingDataFor.get(a) || b.getHistogramDataCache(a);
    if (!e && !b.hasHistogramDataCache(a)) {
      if (c) {
        e = c.handler.call(c.thisObj, ...arguments);
      } else {
        e = a.get(b.dataModelField);
      }
      e = b.finalizeDataRetrieving(a, e);
    }
    return e;
  }
  recordIsReadyForRendering(a) {
    return !this.collectingDataFor.has(a);
  }
  beforeRenderRow(a) {
    const b = this, c = b.getRecordHistogramData(a.record);
    if (!Objects.isPromise(c)) {
      const e = c ? b.extractHistogramDataArray(c, a.record) : [];
      if (!e) {
        return;
      }
      if (b.ticksWidth) {
        for (let g = 0, {length: j} = e; g < j; g++) {
          e[g].width = b.ticksWidth[g];
        }
      }
      const f = Objects.merge({ 'topValue': null }, b.initialConfig.histogramWidget, {
        'data': e,
        'series': { ...b.series }
      });
      a = {
        ...a,
        'histogramConfig': f,
        'histogramData': c,
        'histogramWidget': b.histogramWidget
      };
      b.trigger('beforeRenderHistogramRow', a);
      delete a.eventName;
      delete a.source;
      delete a.type;
      delete a.oldId;
      delete a.row;
      delete a.recordIndex;
      b._recordRenderData = b.processRecordRenderData(a);
    }
    super.beforeRenderRow(...arguments);
  }
  applyHistogramWidgetConfig(a = this.histogramWidget, b) {
    Object.assign(a, histogramWidgetCleanState, b);
  }
  renderRecordHistogram(a) {
    const b = this, {
        histogramData: c,
        cellElement: e
      } = a;
    if (!c) {
      e.innerHTML = '';
      return;
    }
    b.trigger('beforeRenderRecordHistogram', a);
    delete a.eventName;
    delete a.type;
    delete a.source;
    const f = a.histogramWidget || b.histogramWidget;
    b.applyHistogramWidgetConfig(f, a.histogramConfig);
    f.refresh({ 'args': [a] });
    const g = f.element.cloneNode(true);
    g.removeAttribute('id');
    g.classList.remove('b-hide-offscreen');
    e.innerHTML = '';
    e.appendChild(g);
  }
  histogramRenderer(a) {
    const b = this, c = a.histogramData || b.getRecordHistogramData(a.record);
    if (!Objects.isPromise(c)) {
      Object.assign(a, b._recordRenderData);
      return b.renderRecordHistogram(...arguments);
    }
    return '';
  }
  buildGroupHeader(a) {
    if (a.column === this.timeAxisColumn) {
      return this.histogramRenderer(a);
    }
    return this.features.group.buildGroupHeader(a);
  }
  get widgetClass() {
  }
};
TimelineHistogramBase.initClass();
TimelineHistogramBase._$name = 'TimelineHistogramBase';
const TimelineHistogramGrouping_default = a => {
  let b;
  return b = class extends (a || TimelineHistogramBase) {
    afterConfigure() {
      const c = this;
      c.internalAggregateDataEntry = c.internalAggregateDataEntry.bind(this);
      c.internalInitAggregatedDataEntry = c.internalInitAggregatedDataEntry.bind(this);
      super.afterConfigure();
      if (c.features.treeGroup) {
        c.features.treeGroup.ion({
          'beforeDataLoad': c.onTreeGroupBeforeDataLoad,
          'thisObj': c
        });
      }
    }
    updateAggregateFunctions(c) {
      for (const [e, f] of Object.entries(c)) {
        f.id = e;
        if (f.aliases) {
          for (const g of f.aliases) {
            c[g] = f;
          }
        }
      }
    }
    updateStore(c) {
      super.updateStore(...arguments);
      this.detachListeners('store');
      if (c) {
        c.ion({
          'name': 'store',
          'group': this.onStoreGroup,
          'sort': this.onStoreSort,
          'thisObj': this
        });
      }
    }
    changeAggregateDataEntry(c) {
      return this.bindCallback(c);
    }
    changeGetDataEntryForAggregating(c) {
      return this.bindCallback(c);
    }
    changeInitAggregatedDataEntry(c) {
      return this.bindCallback(c);
    }
    onHistogramDataCacheSet({
      record: c,
      data: e
    }) {
      super.onHistogramDataCacheSet(...arguments);
      if (this.aggregateHistogramDataForGroups) {
        this.scheduleRecordParentsRefresh(c);
      }
    }
    onTreeGroupBeforeDataLoad() {
      if (this.aggregateHistogramDataForGroups) {
        this.resetGeneratedRecordsHistogramDataCache();
      }
    }
    onStoreGroup() {
      if (this.aggregateHistogramDataForGroups) {
        this.resetGeneratedRecordsHistogramDataCache();
      }
    }
    onStoreSort({source: c}) {
      if (this.aggregateHistogramDataForGroups && c.isGrouped) {
        this.resetGeneratedRecordsHistogramDataCache();
      }
    }
    getRecordHistogramData(c, e) {
      const f = this;
      let g;
      if (f.aggregateHistogramDataForGroups && f.isGroupRecord(c)) {
        g = f.collectingDataFor.get(c) || f.getHistogramDataCache(c);
        if (!g && !f.hasHistogramDataCache(c)) {
          g = f.getGroupRecordHistogramData(c, e);
          g = f.finalizeDataRetrieving(c, g);
        }
      } else {
        g = super.getRecordHistogramData(...arguments);
      }
      return g;
    }
    internalAggregateDataEntry(c, ...e) {
      const {aggregateFunctions: f} = this;
      for (const {
            id: g,
            aggregate: aggregate = 'sum'
          } of Object.values(this.series)) {
        let i;
        if (aggregate !== false && (i = f[aggregate].entry)) {
          c = i(g, c, ...e);
        }
      }
      return this.aggregateDataEntry ? this.aggregateDataEntry(c, ...e) : c;
    }
    internalInitAggregatedDataEntry() {
      const c = this.initAggregatedDataEntry ? this.initAggregatedDataEntry(...arguments) : {}, {aggregateFunctions: e} = this;
      for (const {
            id: f,
            aggregate: aggregate = 'sum'
          } of Object.values(this.series)) {
        const g = e[aggregate].init;
        if (g && aggregate !== false) {
          g(f, c, ...arguments);
        }
      }
      return c;
    }
    resetGeneratedRecordsHistogramDataCache() {
      const {store: c} = this;
      for (const e of this.getHistogramDataCache().keys()) {
        if (e.isGroupHeader || e.generatedParent || e.isLinked && !c.includes(e)) {
          this.clearHistogramDataCache(e);
        }
      }
    }
    setHistogramDataCache(c, e) {
      super.setHistogramDataCache(c, e);
      if (c.isLinked) {
        super.setHistogramDataCache(c.$original, e);
      } else if (c.$links) {
        const {store: f} = this;
        for (const g of c.$links) {
          if (f.includes(g)) {
            super.setHistogramDataCache(g, e);
          }
        }
      }
    }
    getHistogramDataCache(c) {
      let e = super.getHistogramDataCache(c);
      if (!e && c.isLinked) {
        e = super.getHistogramDataCache(c.$original);
      }
      return e;
    }
    getGroupRecordHistogramData(c, e = {}) {
      e.parentRecord = c;
      const f = this.aggregateRecordsHistogramData(this.getGroupChildren(c), e);
      return Objects.isPromise(f) ? f.then(g => g) : f;
    }
    aggregateRecordsHistogramData(c, e = {}) {
      const f = this, g = [], {parentRecord: i} = e;
      let j = false;
      for (const k of c) {
        const l = f.getRecordHistogramData(k, e);
        j = j || Objects.isPromise(l);
        l && g.push(l);
      }
      if (j) {
        return Promise.all(g).then(n => {
          e.parentRecord = i;
          n = n.filter(o => o);
          return c.length > 0 ? f.aggregateHistogramData(n, c, e) : [];
        });
      }
      return f.aggregateHistogramData(g, c, e);
    }
    isGroupRecord(c) {
      return c.isGroupHeader || this.isTreeGrouped && c.generatedParent;
    }
    getGroupChildren(c) {
      return c.groupChildren || c.children;
    }
    getRecordParent(c) {
      const {groupParent: e} = c.instanceMeta(this.store);
      return e || this.isTreeGrouped && c.parent;
    }
    scheduleRecordParentsRefresh(c, e = true) {
      const f = this;
      let g;
      while (g = f.getRecordParent(c)) {
        e && f.clearHistogramDataCache(g);
        f.scheduleRecordRefresh(g);
        c = g;
      }
    }
    aggregateHistogramData(c, e, f = {}) {
      const g = this, {aggregateFunctions: i} = g;
      f.recordsData = c;
      f.records = e;
      const j = c.map((l, n) => {
        return g.extractHistogramDataArray(l, e[n]);
      });
      const k = ArrayHelper.aggregate(j, g.getDataEntryForAggregating || (l => l), g.internalAggregateDataEntry, g.internalInitAggregatedDataEntry, f);
      for (const {
            id: l,
            aggregate: aggregate = 'sum'
          } of Object.values(g.series)) {
        const n = i[aggregate].finalize;
        if (n && aggregate !== false) {
          n(l, k, ...arguments);
        }
      }
      return k;
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'TimelineHistogramGrouping'), __publicField(b, 'configurable', {
    'aggregateHistogramDataForGroups': true,
    'aggregateDataEntry': null,
    'getDataEntryForAggregating': null,
    'initAggregatedDataEntry': null,
    'aggregateFunctions': {
      'sum': {
        'aliases': ['add'],
        'entry'(c, e, f) {
          e[c] = (e[c] || 0) + f[c];
          return e;
        }
      },
      'min': {
        'entry'(c, e, f) {
          const g = f[c];
          if (g < (e[c] || Number.MAX_VALUE))
            e[c] = g;
          return e;
        }
      },
      'max': {
        'entry'(c, e, f) {
          const g = f[c];
          if (g > (e[c] || Number.MIN_VALUE))
            e[c] = g;
          return e;
        }
      },
      'count': {
        'init'(c, e, f, g) {
          e[c] = g.arrays.length;
        }
      },
      'avg': {
        'entry'(c, e, f) {
          e[c] = (e[c] || 0) + f[c];
          return e;
        },
        'finalize'(c, e, f, g, i) {
          const j = i.arrays.length;
          e.forEach(k => k[c] /= j);
        }
      }
    }
  }), b;
};
const TimelineHistogramScaleColumn_default = a => {
  let b;
  return b = class extends a {
    changeScalePoints(c) {
      c == null ? void 0 : c.sort((e, f) => e.value - f.value);
      return c;
    }
    updateScalePoints(c) {
      const e = this, f = c[c.length - 1];
      if (f) {
        e.scaleUnit = f.unit;
        if (e.calculateTopValueByScalePoints) {
          e.histogramWidget.topValue = e.getTopValueByScalePoints(c);
        }
      }
      if (e.scaleColumn) {
        e.scaleColumn.scalePoints = c;
      }
    }
    changeColumns(c, e) {
      const f = this, g = f.getConfig('scaleColumn');
      if (c && g) {
        const i = Array.isArray(c);
        let j = c;
        if (!i) {
          j = c.data;
        }
        let k = j == null ? void 0 : j.length, l = g;
        j.some((n, o) => {
          if (n.type === 'scale') {
            k = o;
            l = ObjectHelper.assign(n, l);
            return true;
          }
        });
        j = j.slice();
        j[k] = {
          'type': 'scale',
          ...l
        };
        if (i) {
          c = j;
        } else {
          c.data = j;
        }
      }
      return super.changeColumns(c, e);
    }
    updateColumns(c, e) {
      super.updateColumns(c, e);
      if (c) {
        this._scaleColumn = this.columns.find(f => f.isScaleColumn);
      }
    }
    onColumnsChanged({
      action: c,
      changes: e,
      record: f,
      records: g
    }) {
      const {
        scaleColumn: i,
        columns: j
      } = this;
      if (i && (c === 'dataset' || c === 'batch') && !j.includes(i)) {
        j.add(i, true);
      }
      super.onColumnsChanged(...arguments);
    }
    convertUnitsToHistogramValue(c, e) {
      return c;
    }
    convertHistogramValueToUnits(c, e) {
      return c;
    }
    extractHistogramDataArray(c, e) {
      return c;
    }
    getTopValueByScalePoints(c) {
      const e = this, {scaleColumn: f} = e, g = c[c.length - 1], {
          value: i,
          unit: j
        } = g;
      let k = i;
      if (f) {
        k *= 1 + (f.scaleWidget.scaleMaxPadding || 0);
      }
      return e.convertUnitsToHistogramValue(k, j || e.scaleUnit);
    }
    processRecordRenderData(c) {
      let e;
      c = super.processRecordRenderData(...arguments);
      const {scaleColumn: f} = this;
      if (f) {
        const g = this, {
            record: i,
            histogramData: j,
            histogramConfig: histogramConfig = {}
          } = c;
        let k = (e = g.initialConfig.histogramWidget) == null ? void 0 : e.topValue, l = g.scalePoints || i.get(f.field);
        if (!k) {
          if (l && g.calculateTopValueByScalePoints) {
            k = g.getTopValueByScalePoints(l);
          }
          if (!k && j) {
            const n = c.histogramWidget || g.histogramWidget;
            ObjectHelper.assign(n, histogramConfig);
            k = n.getDataTopValue(j);
            l = [{
                'value': g.convertHistogramValueToUnits(k, g.scaleUnit),
                'text': g.convertHistogramValueToUnits(k, g.scaleUnit)
              }];
            k += g.scaleColumn.scaleWidget.scaleMaxPadding * k;
          }
          c.scaleWidgetConfig = { 'scalePoints': l };
          c.histogramConfig = {
            ...histogramConfig,
            'topValue': k
          };
        }
      }
      return c;
    }
    buildGroupHeader(c) {
      if (c.column === this.scaleColumn) {
        return this.scaleColumn.renderer(c);
      }
      return super.buildGroupHeader(...arguments);
    }
    beforeRenderCell(c) {
      if (this.scaleColumn && c.column === this.scaleColumn) {
        c.histogramData = this.getRecordHistogramData(c.record);
        if (!ObjectHelper.isPromise(c.histogramData)) {
          Object.assign(c, this._recordRenderData);
        }
      }
      return super.beforeRenderCell(...arguments);
    }
    renderRecordScale(c, e) {
      if (this.scaleColumn) {
        const f = this.getRowFor(c), g = f == null ? void 0 : f.getCell(this.scaleColumn.id);
        if (g) {
          f.renderCell(g);
        }
      }
    }
    get widgetClass() {
    }
  }, __publicField(b, '$name', 'TimelineHistogramScaleColumn'), __publicField(b, 'configurable', {
    'scaleColumn': {},
    'scalePoints': null,
    'calculateTopValueByScalePoints': true
  }), b;
};
const TimelineHistogram = class extends TimelineHistogramBase.mixin(TimelineHistogramGrouping_default, TimelineHistogramScaleColumn_default) {
  static type = 'timelinehistogram';
  static $name = 'TimelineHistogram';
};
TimelineHistogram.initClass();
TimelineHistogram._$name = 'TimelineHistogram';
const ProjectCombo = class extends Combo {
  static get $name() {
    return 'ProjectCombo';
  }
  static get type() {
    return 'projectcombo';
  }
  static get configurable() {
    return {
      'project': null,
      'displayField': 'title',
      'valueField': 'url',
      'highlightExternalChange': false,
      'editable': false,
      'internalListeners': { 'change': 'onInternalChange' }
    };
  }
  updateProject(a) {
    let b;
    if ((b = a.transport.load) == null ? void 0 : b.url) {
      this.value = a.transport.load.url;
    }
  }
  onInternalChange({
    value: a,
    userAction: b
  }) {
    if (b && this.project) {
      this.project.transport.load.url = a;
      this.project.load();
    }
  }
};
ProjectCombo.initClass();
ProjectCombo._$name = 'ProjectCombo';
const ResourceFilter = class extends List {
  static get $name() {
    return 'ResourceFilter';
  }
  static get type() {
    return 'resourcefilter';
  }
  static get delayable() {
    return { 'applyFilters': 'raf' };
  }
  static get configurable() {
    return {
      'eventStore': null,
      'multiSelect': true,
      'toggleAllIfCtrlPressed': true,
      'collapsibleGroups': true,
      'itemTpl': a => StringHelper.encodeHtml(a.name || ''),
      'masterFilter': a => true,
      'filterResources': null,
      'allowGroupSelect': false
    };
  }
  itemIconTpl(a, b) {
    const {eventColor: c} = a, e = DomHelper.isNamedColor(c) ? ' b-sch-foreground-' + c : '', f = !e && c ? ' style="color:' + c + '"' : '';
    return this.multiSelect && this.callback(this.isSelectable, this, [a]) ? '<div class="b-selected-icon b-icon' + e + '"' + f + '></div>' : '';
  }
  changeStore(a) {
    if (this.eventStore) {
      return super.changeStore(...arguments);
    } else {
      this._storeConfig = a;
    }
  }
  changeEventStore(a) {
    this.getConfig('store');
    return a;
  }
  updateEventStore(a) {
    const b = this, c = b._storeConfig || {}, {resourceStore: e} = a, f = {
        ...c,
        'syncSort': true,
        'excludeCollapsedRecords': false,
        'tree': e.tree
      }, g = {
        'change': 'onStoreChange',
        'thisObj': b
      };
    if (e.tree) {
      f.sorters = [];
    }
    const i = b.store = e.chain(b.masterFilter, null, f);
    i.un(g);
    e.ion(g);
    if (!e.count) {
      b.detachListeners('project');
      e.project.ion({
        'name': 'project',
        'refresh': 'initFilter',
        'thisObj': b
      });
    } else {
      b.initFilter();
    }
  }
  changeMasterFilter(a) {
    const b = this;
    if (!b.filterResources) {
      return function (c) {
        return b.callback(a, b, [c]);
      };
    }
  }
  initFilter() {
    let a;
    const b = this;
    if (b.eventStore.resourceStore.count) {
      const {selected: c} = b;
      if (!c.count) {
        if (((a = b.initialConfig.selected) == null ? void 0 : a.length) === 0) {
          b.onInternalSelectionChange({
            'source': c,
            'added': [],
            'removed': []
          });
        } else {
          c.add(b.store.allRecords.filter(e => !e.isGroupHeader));
        }
      }
      b.detachListeners('project');
    }
  }
  onStoreRefresh({
    source: a,
    action: b
  }) {
    if (b === 'filter' && this.eventStoreFilter) {
      const {eventStoreFilter: c} = this, {disabled: e} = c, f = !a.isFiltered && this.allSelected;
      if (f !== e) {
        c.disabled = f;
        this.applyFilters();
      }
    }
    super.onStoreRefresh(...arguments);
  }
  onInternalSelectionChange({
    source: a,
    added: added = [],
    removed: b
  }) {
    const c = this, e = !c.store.isFiltered && c.allSelected;
    super.onInternalSelectionChange(...arguments);
    let f = false;
    if (!c.eventStoreFilter) {
      c.eventStoreFilter = c.eventStore.addFilter({
        'id': c.id + '-filter-instance',
        'filterBy': g => !g.resource || c.selected.includes(g.resources),
        'disabled': e
      }, (added == null ? void 0 : added.length) === c.store.count);
      f = true;
    }
    if (c.filterResources && !c.resourceStoreFilter) {
      c.resourceStoreFilter = c.eventStore.resourceStore.addFilter({
        'id': c.id + '-filter-instance',
        'filterBy': g => c.selected.includes(g),
        'disabled': e
      }, (added == null ? void 0 : added.length) === c.store.count);
      f = true;
    }
    if (f) {
      return;
    }
    c.eventStoreFilter.disabled = e;
    c.resourceStoreFilter && (c.resourceStoreFilter.disabled = e);
    c.applyFilters();
    if (c.eventListeners.change) {
      const g = a.values, i = g.concat(b);
      ArrayHelper.remove(i, ...added);
      c.triggerFieldChange({
        'value': g,
        'oldValue': i
      });
    }
  }
  get value() {
    return this.selected.values;
  }
  applyFilters() {
    this.eventStore.filter();
    this.filterResources && this.eventStore.resourceStore.filter();
  }
  doDestroy() {
    let a;
    (a = this.store) == null ? void 0 : a.destroy();
    super.doDestroy();
  }
};
ResourceFilter.initClass();
ResourceFilter._$name = 'ResourceFilter';
const SchedulerDatePicker = class _SchedulerDatePicker extends DatePicker {
  static get $name() {
    return 'SchedulerDatePicker';
  }
  static get type() {
    return 'datepicker';
  }
  static get configurable() {
    return {
      'showEvents': null,
      'eventStore': null,
      'eventFilter': {
        '$config': 'lazy',
        'value': null
      }
    };
  }
  changeEventFilter(a) {
    if (typeof a === 'string') {
      const {
        handler: b,
        thisObj: c
      } = this.resolveCallback(a);
      a = b.bind(c);
    }
    return a;
  }
  doRefresh() {
    if (this.isVisible || !this.showEvents) {
      this.refreshEventsMap();
      return super.doRefresh(...arguments);
    } else {
      this.whenVisible('doRefresh');
    }
  }
  updateShowEvents(a, b) {
    const c = this, {classList: e} = c.contentElement;
    let {eventStore: f} = c;
    c.requestAnimationFrame(() => {
      let g;
      c.element.classList.toggle('b-datepicker-with-events', Boolean(a));
      (g = c.owner) == null ? void 0 : g.element.classList.toggle('b-datepicker-with-events', Boolean(a));
      a && e.add('b-show-events-' + a);
      e.remove('b-show-events-' + b);
    });
    if (a) {
      if (!f) {
        const g = c.up(i => i.eventStore);
        if (g) {
          f = g.eventStore;
        } else {
          throw new Error('DatePicker configured with events but no eventStore');
        }
      }
    } else {
      c.eventsMap = null;
    }
    if (!c.isConfiguring) {
      c.updateEventStore(f);
      c.doRefresh();
    }
  }
  refreshEventsMap() {
    const a = this;
    if (a.showEvents) {
      a.eventsMap = a.eventStore.getEventCounts({
        'startDate': a.startDate,
        'endDate': a.endDate,
        'dateMap': a.eventsMap,
        'filter': a.eventFilter
      });
    }
  }
  updateEventStore(a) {
    let b;
    if (a.findListener('change', 'refresh', this) === -1) {
      (b = a == null ? void 0 : a[this.showEvents ? 'on' : 'un']) == null ? void 0 : b.call(a, {
        'change': 'refresh',
        'thisObj': this
      });
    }
  }
  cellRenderer({
    cell: a,
    date: b
  }) {
    let c, e;
    const {showEvents: f} = this, g = (e = (c = this.eventCounts) == null ? void 0 : c.get) == null ? void 0 : e.call(c, DateHelper.makeKey(b)), i = f === 'count';
    delete a.dataset.btip;
    if (g) {
      if (!i && this.eventCountTip) {
        a.dataset.btip = this.L('L{ResourceInfoColumn.eventCountText}', g);
      }
      DomHelper.createElement({
        'dataset': { 'count': g },
        'class': {
          [i ? 'b-cell-events-badge' : 'b-icon b-icon-circle']: 1,
          [_SchedulerDatePicker.getEventCountClass(g)]: 1
        },
        'parent': a,
        [i ? 'text' : '']: g
      });
    }
  }
  static getEventCountClass(a) {
    if (a) {
      if (a < 4) {
        return 'b-datepicker-1-to-3-events';
      }
      if (a < 7) {
        return 'b-datepicker-4-to-6-events';
      }
      return 'b-calendar-7-or-more-events';
    }
    return '';
  }
  static setupClass(a) {
    a.replaceType = true;
    super.setupClass(a);
  }
};
SchedulerDatePicker.initClass();
SchedulerDatePicker._$name = 'SchedulerDatePicker';
const isProjectConsumer = a => a.isProjectConsumer;
const _UndoRedo = class _UndoRedo extends UndoRedoBase {
  static configurable = {
    'scheduler': null,
    'project': null
  };
  static type = 'undoredo';
  static $name = 'UndoRedo';
  construct() {
    super.construct(...arguments);
    if (!this.stm) {
      this.scheduler = this.up(isProjectConsumer);
    }
  }
  changeScheduler(a) {
    return a.isProjectConsumer ? a : _UndoRedo.getById(a);
  }
  updateScheduler(a) {
    const {crudManager: b} = a;
    a.ion({
      'projectChange': 'onProjectChanged',
      'thisObj': this
    });
    if (b) {
      this.setupLoadListener(b);
    } else {
      this.onLoad();
    }
    this.stm = a.project.stm;
  }
  setupLoadListener(a) {
    a.detachListeners('load');
    a.ion({
      'name': 'load',
      'load': 'onLoad',
      'thisObj': this
    });
  }
  async onLoad() {
    await this.scheduler.project.commitAsync();
    if (!this.isDestroyed) {
      this.stm.enable();
      this.fillUndoRedoCombo();
    }
  }
  onProjectChanged({project: a}) {
    this.project = a;
  }
  updateProject(a) {
    super.updateProject(...arguments);
    if (a && this.scheduler && !this.scheduler.isConfiguring) {
      this.setupLoadListener(a);
    }
  }
};
const UndoRedo = _UndoRedo;
UndoRedo.initClass();
UndoRedo._$name = 'UndoRedo';
const ViewPresetCombo = class extends Combo {
  static configurable = {
    'presets': [
      'hourAndDay',
      'weekAndDay',
      'dayAndMonth'
    ],
    'client': null,
    'useFixedDuration': true,
    'editable': false,
    'valueField': 'id',
    'displayField': 'name',
    'placeholder': 'Select view'
  };
  static type = 'viewpresetcombo';
  static $name = 'ViewPresetCombo';
  construct() {
    super.construct(...arguments);
    this.scheduler.ion({
      'presetchange': this.onClientPresetChange,
      'thisObj': this
    });
  }
  get scheduler() {
    return this.client || this.up(a => a.isTimelineBase) || Widget.query(a => a.isTimelineBase);
  }
  changeValue(a, b) {
    this.getConfig('presets');
    return super.changeValue(a, b);
  }
  updatePresets(a) {
    this.store = this.scheduler.presets.chain(b => a.includes(b.id));
    this.store.sort((c, e) => a.indexOf(c.id) - a.indexOf(e.id));
  }
  onClientPresetChange({preset: a}) {
    const b = this;
    if (!b._isSelecting) {
      b.isSettingValue = true;
      if (b.store.includes(a.id)) {
        b.value = a;
      } else {
        b.clear();
      }
      b.isSettingValue = false;
    }
  }
  onSelect({record: a}) {
    let b;
    if (!this.isSettingValue) {
      const {scheduler: c} = this, {
          mainUnit: e,
          start: f,
          defaultSpan: g
        } = a, i = {};
      if (e && g && this.useFixedDuration) {
        let j = e;
        if (f && typeof f === 'string') {
          j = (b = DateHelper.parseTimeUnit(f)) != null ? b : f;
        }
        i.startDate = DateHelper.startOf(c.viewportCenterDate, j);
        if (typeof f === 'number') {
          i.startDate = DateHelper.add(i.startDate, f, e);
        }
        i.endDate = DateHelper.add(i.startDate, g, e);
      }
      c.zoomToLevel(a, i);
    }
  }
  updateLocalization() {
    super.updateLocalization();
    this.syncInputFieldValue();
  }
};
ViewPresetCombo.initClass();
ViewPresetCombo._$name = 'ViewPresetCombo';
const CalendarCacheIntervalMultiple = class _CalendarCacheIntervalMultiple {
  constructor(a) {
    this.intervalGroups = [];
    a && Object.assign(this, a);
  }
  combineWith(a) {
    const b = this.intervalGroups.slice();
    b.push([
      a.calendar,
      a
    ]);
    return new _CalendarCacheIntervalMultiple({ 'intervalGroups': b });
  }
  getIsWorkingForEvery() {
    if (this.isWorkingForEvery != null)
      return this.isWorkingForEvery;
    for (const [a, b] of this.getGroups()) {
      if (!b[0].isWorking)
        return this.isWorkingForEvery = false;
    }
    return this.isWorkingForEvery = true;
  }
  getIsWorkingForSome() {
    if (this.isWorkingForSome != null)
      return this.isWorkingForSome;
    for (const [a, b] of this.getGroups()) {
      if (b[0].isWorking)
        return this.isWorkingForSome = true;
    }
    return this.isWorkingForSome = false;
  }
  getCalendars() {
    this.getGroups();
    return this.calendars;
  }
  isCalendarWorking(a) {
    return this.getCalendarsWorkStatus().get(a);
  }
  getCalendarsWorkStatus() {
    if (this.calendarsWorkStatus)
      return this.calendarsWorkStatus;
    const a = new Map();
    for (const [b, c] of this.getGroups()) {
      a.set(b, c[0].isWorking);
    }
    return this.calendarsWorkStatus = a;
  }
  getCalendarsWorking() {
    if (this.calendarsWorking)
      return this.calendarsWorking;
    const a = [];
    for (const [b, c] of this.getGroups()) {
      if (c[0].isWorking)
        a.push(b);
    }
    return this.calendarsWorking = a;
  }
  getCalendarsNonWorking() {
    if (this.calendarsNonWorking)
      return this.calendarsNonWorking;
    const a = [];
    for (const [b, c] of this.getGroups()) {
      if (!c[0].isWorking)
        a.push(b);
    }
    return this.calendarsNonWorking = a;
  }
  getGroups() {
    if (this.intervalsByCalendar)
      return this.intervalsByCalendar;
    const a = this.calendars = [];
    const b = new Map();
    this.intervalGroups.forEach(([c, e]) => {
      let f = b.get(c);
      if (!f) {
        a.push(c);
        f = [];
        b.set(c, f);
      }
      f.push.apply(f, e.intervals);
    });
    b.forEach((c, e) => {
      const f = stripDuplicates(c);
      f.sort(IntervalSorterFn);
      b.set(e, f);
    });
    return this.intervalsByCalendar = b;
  }
};
const CalendarCacheMultiple = class extends CalendarCache {
  constructor(a) {
    super(a);
    this.calendarCaches = stripDuplicates(this.calendarCaches);
    this.intervalCache = new IntervalCache({
      'emptyInterval': new CalendarCacheIntervalMultiple(),
      'combineIntervalsFn': (b, c) => {
        return b.combineWith(c);
      }
    });
  }
  fillCache(a, b) {
    this.calendarCaches.forEach(c => {
      c.fillCache(a, b);
      this.includeWrappingRangeFrom(c, a, b);
    });
  }
};
const COMBINED_CALENDARS_CACHE = new Map();
const combineCalendars = a => {
  const b = stripDuplicates(a);
  if (b.length === 0)
    throw new Error('No calendars to combine');
  b.sort((i, j) => {
    if (i.internalId < j.internalId)
      return -1;
    else
      return 1;
  });
  const c = b.map(i => i.internalId + '/').join('');
  const e = b.map(i => i.version + '/').join('');
  const f = COMBINED_CALENDARS_CACHE.get(c);
  let g;
  if (f && f.versionsHash === e)
    g = f.cache;
  else {
    g = new CalendarCacheMultiple({ 'calendarCaches': b.map(i => i.calendarCache) });
  }
  return g;
};
const MIN_SMI = -Math.pow(2, 30);
const MAX_SMI = Math.pow(2, 30) - 1;
const uppercaseFirst = a => a.slice(0, 1).toUpperCase() + a.slice(1);
const isAtomicValue = a => Object(a) !== a;
const defineProperty8 = (a, b, c) => {
  Object.defineProperty(a, b, {
    'value': c,
    'enumerable': true,
    'configurable': true
  });
  return c;
};
const prototypeValue = a => {
  return function (b, c) {
    b[c] = a;
  };
};
const copySetInto = (a, b) => {
  for (const c of a)
    b.add(c);
  return b;
};
const delay2 = a => new Promise(b => setTimeout(b, a));
const matchAll = function* (a, b) {
  let c;
  while ((c = a.exec(b)) !== null) {
    yield c;
  }
};
let isRegeneratorRuntime = null;
const isGeneratorFunction = function (a) {
  if (isRegeneratorRuntime === null)
    isRegeneratorRuntime = typeof regeneratorRuntime !== 'undefined';
  if (isRegeneratorRuntime === true) {
    return regeneratorRuntime.isGeneratorFunction(a);
  } else {
    return a.constructor.name === 'GeneratorFunction';
  }
};
const isPromise = function (a) {
  return a && typeof a.then === 'function';
};
const DEBUG = false;
const emptyFn5 = (...a) => void 0;
const DEBUG_ONLY = a => DEBUG ? a : emptyFn5;
const debug = DEBUG_ONLY(a => {
  debugger;
});
const warn = DEBUG_ONLY(a => {
  if (typeof console !== 'undefined')
    console.warn(a);
});
const StackEntry = class extends Base2 {
};
const SourceLinePoint = class _SourceLinePoint extends Base2 {
  constructor() {
    super(...arguments);
    this.stackEntries = [];
  }
  static fromError(a) {
    const b = _SourceLinePoint.new({
      'exception': a,
      'stackEntries': parseErrorStack(a.stack)
    });
    return b;
  }
  static fromThisCall() {
    const a = this.fromError(new Error());
    a.stackEntries.splice(0, 2);
    return a;
  }
};
var parseErrorStack = a => {
  return CI(matchAll(/^   +at\s*(.*?)\s*\((https?:\/\/.*?):(\d+):(\d+)/gm, a)).map(b => StackEntry.new({
    'statement': b[1],
    'sourceFile': b[2],
    'sourceLine': Number(b[3]),
    'sourceCharPos': Number(b[4])
  })).toArray();
};
const RequiredProperties = Symbol('RequiredProperties');
const required = DEBUG_ONLY((a, b) => {
  let c = a[RequiredProperties];
  if (!c)
    c = a[RequiredProperties] = [];
  c.push(b);
});
const validateRequiredProperties = DEBUG_ONLY(a => {
  const b = a[RequiredProperties];
  if (b) {
    for (let c = 0; c < b.length; c++)
      if (a[b[c]] === void 0)
        throw new Error('Required attribute [' + String(b[c]) + '] is not provided');
  }
});
let OnCycleAction;
(function (a) {
  a.Cancel = 'Cancel';
  a.Resume = 'Resume';
}(OnCycleAction || (OnCycleAction = {})));
const WalkSource = Symbol('WalkSource');
const NOT_VISITED = -1;
const VISITED_TOPOLOGICALLY = -2;
const WalkContext = class extends Base2 {
  constructor() {
    super(...arguments);
    this.visited = new Map();
    this.toVisit = [];
    this.currentEpoch = 0;
  }
  startFrom(a) {
    this.continueFrom(a);
  }
  continueFrom(a) {
    this.toVisit.push.apply(this.toVisit, a.map(b => {
      return {
        'node': b,
        'from': WalkSource,
        'label': void 0
      };
    }));
    this.walkDepth();
  }
  onNode(a, b) {
  }
  onTopologicalNode(a) {
  }
  onCycle(a, b) {
    return OnCycleAction.Cancel;
  }
  forEachNext(a, b) {
    throw new Error('Abstract method called');
  }
  collectNext(a, b, c) {
    throw new Error('Abstract method called');
  }
  getVisitedInfo(a) {
    return this.visited.get(a);
  }
  setVisitedInfo(a, b, c) {
    if (!c) {
      c = {
        'visitedAt': b,
        'visitEpoch': this.currentEpoch
      };
      this.visited.set(a, c);
    } else {
      c.visitedAt = b;
      c.visitEpoch = this.currentEpoch;
    }
    return c;
  }
  walkDepth() {
    const a = this.visited;
    const b = this.toVisit;
    let c;
    while (c = b.length) {
      const e = b[c - 1].node;
      const f = this.getVisitedInfo(e);
      if (f && f.visitedAt === VISITED_TOPOLOGICALLY && f.visitEpoch === this.currentEpoch) {
        b.pop();
        continue;
      }
      if (f && f.visitEpoch === this.currentEpoch && f.visitedAt !== NOT_VISITED) {
        if (f.visitedAt < c) {
          if (this.onCycle(e, b) !== OnCycleAction.Resume)
            break;
        } else {
          f.visitedAt = VISITED_TOPOLOGICALLY;
          this.onTopologicalNode(e);
        }
        b.pop();
      } else {
        if (this.onNode(e, b[c - 1]) === false)
          break;
        const g = this.setVisitedInfo(e, c, f);
        const i = b.length;
        this.collectNext(e, b, g);
        if (b.length === i) {
          g.visitedAt = VISITED_TOPOLOGICALLY;
          this.onTopologicalNode(e);
          b.pop();
        }
      }
    }
  }
};
function cycleInfo(a) {
  const b = a.length;
  if (b === 0)
    return [];
  const c = a[b - 1].node;
  const e = [c];
  let f = b - 1;
  let g = f;
  while (f >= 0 && a[f].from !== c) {
    while (f >= 0 && a[f].from === a[g].from)
      f--;
    if (f >= 0) {
      e.push(a[f].node);
      g = f;
    }
  }
  if (f < 0)
    return [];
  e.push(c);
  return e.reverse();
}
const __decorate = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
let FORMULA_ID = 0;
const CalculateProposed = FORMULA_ID++;
const Formula = class extends Base2 {
  constructor() {
    super(...arguments);
    this.formulaId = FORMULA_ID++;
    this.inputs = new Set();
  }
};
const VariableWalkContext = class extends WalkContext {
  collectNext(a, b) {
    if (a instanceof Formula) {
      b.push({
        'node': a.output,
        'from': a,
        'label': void 0
      });
    } else {
      const c = this.cache.formulasByInput.get(a);
      c && c.forEach(e => b.push({
        'node': e,
        'from': a,
        'label': void 0
      }));
    }
  }
};
const FormulasCache = class extends Mixin([Base2], a => class b extends a {
  constructor() {
    super(...arguments);
    this.variables = new Set();
    this.formulas = new Set();
    this.$formulasByInput = void 0;
    this.$formulasByOutput = void 0;
  }
  get formulasByInput() {
    if (this.$formulasByInput !== void 0)
      return this.$formulasByInput;
    this.fillCache();
    return this.$formulasByInput;
  }
  get formulasByOutput() {
    if (this.$formulasByOutput !== void 0)
      return this.$formulasByOutput;
    this.fillCache();
    return this.$formulasByOutput;
  }
  add(c) {
    this.$formulasByInput = void 0;
    this.$formulasByOutput = void 0;
    this.formulas.add(c);
  }
  has(c) {
    return this.formulas.has(c);
  }
  fillCache() {
    this.$formulasByInput = new Map();
    this.$formulasByOutput = new Map();
    this.formulas.forEach(c => {
      let e = this.$formulasByOutput.get(c.output);
      if (!e) {
        e = new Set();
        this.$formulasByOutput.set(c.output, e);
      }
      e.add(c);
      c.inputs.forEach(f => {
        let g = this.$formulasByInput.get(f);
        if (!g) {
          g = new Set();
          this.$formulasByInput.set(f, g);
        }
        g.add(c);
      });
    });
  }
  allInputVariables() {
    return uniqueOnly(concatIterable(map(this.formulas, c => c.inputs.values())));
  }
  isCyclic() {
    let c = false;
    const e = VariableWalkContext.new({
      'cache': this,
      'onCycle': () => {
        c = true;
        return OnCycleAction.Cancel;
      }
    });
    e.startFrom(Array.from(this.allInputVariables()));
    return c;
  }
}) {
};
const CycleDescription = class extends FormulasCache {
};
const CycleResolution = class extends Base2 {
  constructor() {
    super(...arguments);
    this.description = void 0;
    this.defaultResolutionFormulas = new Set();
    this.resolutionsByInputHash = new Map();
  }
  clear() {
    this.resolutionsByInputHash.clear();
  }
  resolve(a) {
    const b = this.resolutionsByInputHash.get(a.hash);
    if (b !== void 0)
      return b;
    const c = this.buildResolution(a);
    this.resolutionsByInputHash.set(a.hash, c);
    return c;
  }
  buildResolution(a) {
    const b = WalkState.new({
      'context': this,
      'input': a
    });
    const c = Array.from(b.next()).map(e => {
      return {
        'resolution': e.asResolution(),
        'nbrOfDefaultFormulas': Array.from(e.activatedFormulas.formulas).reduce((f, g) => e.formulaIsDefault(g) ? f + 1 : f, 0),
        'unCoveredInputWeight': e.unCoveredInputWeight()
      };
    });
    c.sort((e, f) => {
      if (e.unCoveredInputWeight < f.unCoveredInputWeight)
        return -1;
      if (e.unCoveredInputWeight > f.unCoveredInputWeight)
        return 1;
      return f.nbrOfDefaultFormulas - e.nbrOfDefaultFormulas;
    });
    if (c.length > 0)
      return c[0].resolution;
    else
      debugger;
  }
};
let VariableInputState;
(function (a) {
  a[a.NoInput = 0] = 'NoInput';
  a[a.HasPreviousValue = 1] = 'HasPreviousValue';
  a[a.HasProposedValue = 2] = 'HasProposedValue';
  a[a.KeepIfPossible = 4] = 'KeepIfPossible';
}(VariableInputState || (VariableInputState = {})));
const CycleResolutionInput = class extends Base2 {
  constructor() {
    super(...arguments);
    this.context = void 0;
    this.input = void 0;
    this.$hash = '';
  }
  get hash() {
    if (this.$hash !== '')
      return this.$hash;
    return this.$hash = this.buildHash();
  }
  get description() {
    return this.context.description;
  }
  get resolution() {
    return this.context.resolve(this);
  }
  initialize(...a) {
    super.initialize(...a);
    validateRequiredProperties(this);
    this.input = new Map(CI(this.description.variables).map(b => [
      b,
      VariableInputState.NoInput
    ]));
  }
  buildHash() {
    return String.fromCharCode(...CI(this.description.variables).inBatchesBySize(5).map(a => this.batchToCharCode(a)));
  }
  batchToCharCode(a) {
    return a.reduceRight((b, c, e) => b | this.input.get(c) << e * 3, 0);
  }
  addProposedValueFlag(a) {
    if (DEBUG) {
      if (!this.description.variables.has(a))
        throw new Error('Unknown variable');
      if (this.$hash !== '')
        throw new Error('Already hashed');
    }
    const b = this.input.get(a);
    this.input.set(a, b | VariableInputState.HasProposedValue);
  }
  hasProposedValue(a) {
    return Boolean(this.input.get(a) & VariableInputState.HasProposedValue);
  }
  hasProposedValueVars() {
    return CI(this.description.variables).filter(a => this.hasProposedValue(a));
  }
  addPreviousValueFlag(a) {
    if (DEBUG) {
      if (!this.description.variables.has(a))
        throw new Error('Unknown variable');
      if (this.$hash !== '')
        throw new Error('Already hashed');
    }
    const b = this.input.get(a);
    this.input.set(a, b | VariableInputState.HasPreviousValue);
  }
  hasPreviousValue(a) {
    return Boolean(this.input.get(a) & VariableInputState.HasPreviousValue);
  }
  hasPreviousValueVars() {
    return CI(this.description.variables).filter(a => this.hasPreviousValue(a));
  }
  addKeepIfPossibleFlag(a) {
    if (DEBUG) {
      if (!this.description.variables.has(a))
        throw new Error('Unknown variable');
      if (this.$hash !== '')
        throw new Error('Already hashed');
    }
    const b = this.input.get(a);
    this.input.set(a, b | VariableInputState.KeepIfPossible);
  }
  keepIfPossible(a) {
    return Boolean(this.input.get(a) & VariableInputState.KeepIfPossible);
  }
  keepIfPossibleVars() {
    return CI(this.description.variables).filter(a => this.keepIfPossible(a));
  }
};
__decorate([required], CycleResolutionInput.prototype, 'context', void 0);
var WalkState = class _WalkState extends Base2 {
  constructor() {
    super(...arguments);
    this.context = void 0;
    this.input = void 0;
    this.previous = void 0;
    this.activatedFormula = void 0;
    this.$activatedFormulas = void 0;
  }
  get activatedFormulas() {
    if (this.$activatedFormulas !== void 0)
      return this.$activatedFormulas;
    const a = FormulasCache.new({
      'variables': this.description.variables,
      'formulas': CI(this.thisAndPreviousStates()).map(b => b.activatedFormula).toSet()
    });
    return this.$activatedFormulas = a;
  }
  get description() {
    return this.context.description;
  }
  *thisAndPreviousStates() {
    let a = this;
    while (a && a.activatedFormula) {
      yield a;
      a = a.previous;
    }
  }
  formulaHasProposedValueInInput(a) {
    return Array.from(a.inputs).some(b => this.input.hasProposedValue(b));
  }
  unCoveredInputWeight() {
    const a = map(this.input.hasProposedValueVars(), e => {
      return {
        'variable': e,
        'isProposed': true
      };
    });
    const b = map(this.input.keepIfPossibleVars(), e => {
      return {
        'variable': e,
        'isProposed': false
      };
    });
    const c = CI([
      a,
      b
    ]).concat().uniqueOnlyBy(e => e.variable);
    return c.reduce((e, {
      variable: f,
      isProposed: g
    }) => {
      let i = 0;
      const j = this.activatedFormulas.formulasByOutput.get(f);
      if (j) {
        const l = j.size === 1 ? Array.from(j)[0] : null;
        if (l && this.formulaIsDefault(l) && this.formulaHasProposedValueInInput(l)) {
          if (g)
            i += 1000000;
          else
            i += 10000;
        } else {
          if (g)
            i += 10000000;
          else
            i += 100000;
        }
      }
      const k = this.activatedFormulas.formulasByInput.get(f);
      if (!(k && k.size > 0)) {
        if (g)
          i += 1000;
        else
          i += 100;
      }
      return e + i;
    }, 0);
  }
  preferFormula(a, b) {
    const c = this.formulaAllInputsHasProposed(a);
    const e = this.formulaAllInputsHasProposed(b);
    if (c && !e)
      return -1;
    if (e && !c)
      return 1;
    const f = this.formulaCountInputsWithProposedOrKeep(a);
    const g = this.formulaCountInputsWithProposedOrKeep(b);
    if (f > g)
      return -1;
    if (f < g)
      return 1;
    if (this.formulaIsDefault(a) && !this.formulaIsDefault(b))
      return -1;
    if (this.formulaIsDefault(b) && !this.formulaIsDefault(a))
      return 1;
    return 0;
  }
  formulaIsDefault(a) {
    return this.context.defaultResolutionFormulas.has(a);
  }
  formulaCountInputsWithProposedOrKeep(a) {
    let b = 0;
    Array.from(a.inputs).forEach(c => {
      if (this.input.hasProposedValue(c) || this.input.keepIfPossible(c))
        b++;
    });
    return b;
  }
  formulaAllInputsHasProposedOrKeep(a) {
    return Array.from(a.inputs).every(b => this.input.hasProposedValue(b) || this.input.keepIfPossible(b));
  }
  formulaAllInputsHasProposed(a) {
    return Array.from(a.inputs).every(b => this.input.hasProposedValue(b));
  }
  formulaIsApplicable(a) {
    const b = Array.from(a.inputs).every(e => this.input.hasProposedValue(e) || this.input.hasPreviousValue(e) || this.activatedFormulas.formulasByOutput.has(e));
    const c = FormulasCache.new({ 'formulas': new Set(this.activatedFormulas.formulas) });
    c.add(a);
    return b && !c.isCyclic();
  }
  formulaIsInsignificant(a) {
    const b = this.activatedFormulas.formulasByOutput.has(a.output);
    const c = this.input.hasPreviousValue(a.output);
    return b || c && Array.from(a.inputs).some(e => {
      return !this.input.hasPreviousValue(e) && !this.input.hasProposedValue(e) && !this.activatedFormulas.formulasByOutput.has(e);
    });
  }
  unvisitedFormulas() {
    return Array.from(this.description.formulas).filter(a => !this.activatedFormulas.has(a));
  }
  *next() {
    const a = this.unvisitedFormulas();
    a.sort(this.preferFormula.bind(this));
    let b = true;
    for (const c of a) {
      if (!this.formulaIsApplicable(c) || this.formulaIsInsignificant(c))
        continue;
      const e = _WalkState.new({
        'previous': this,
        'context': this.context,
        'input': this.input,
        'activatedFormula': c
      });
      yield* e.next();
      b = false;
    }
    if (b)
      yield this;
  }
  asResolution() {
    return new Map(CI(this.description.variables).map(a => {
      const b = this.activatedFormulas.formulasByOutput.get(a);
      if (b) {
        for (const c of b) {
          return [
            a,
            c.formulaId
          ];
        }
      }
      return [
        a,
        CalculateProposed
      ];
    }));
  }
};
const __decorate2 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const BreakCurrentStackExecution = Symbol('BreakCurrentStackExecution');
const Effect = class extends Base2 {
};
__decorate2([prototypeValue(true)], Effect.prototype, 'sync', void 0);
__decorate2([prototypeValue(true)], Effect.prototype, 'pure', void 0);
const ProposedOrPreviousSymbol = Symbol('ProposedOrPreviousSymbol');
const ProposedOrPrevious = Effect.new({ 'handler': ProposedOrPreviousSymbol });
const RejectSymbol = Symbol('RejectSymbol');
const RejectEffect = class extends Effect {
  constructor() {
    super(...arguments);
    this.handler = RejectSymbol;
  }
};
__decorate2([prototypeValue(false)], RejectEffect.prototype, 'pure', void 0);
const Reject = a => RejectEffect.new({ 'reason': a });
const TransactionSymbol = Symbol('TransactionSymbol');
const GetTransaction = Effect.new({ 'handler': TransactionSymbol });
const OwnQuarkSymbol = Symbol('OwnQuarkSymbol');
const OwnQuark = Effect.new({ 'handler': OwnQuarkSymbol });
const OwnIdentifierSymbol = Symbol('OwnIdentifierSymbol');
const OwnIdentifier = Effect.new({ 'handler': OwnIdentifierSymbol });
const WriteSymbol = Symbol('WriteSymbol');
const WriteEffect = class extends Effect {
  constructor() {
    super(...arguments);
    this.handler = WriteSymbol;
  }
};
__decorate2([prototypeValue(false)], WriteEffect.prototype, 'pure', void 0);
const Write = (a, b, ...c) => WriteEffect.new({
  'identifier': a,
  'proposedArgs': [
    b,
    ...c
  ]
});
const WriteSeveralSymbol = Symbol('WriteSeveralSymbol');
const WriteSeveralEffect = class extends Effect {
  constructor() {
    super(...arguments);
    this.handler = WriteSeveralSymbol;
  }
};
__decorate2([prototypeValue(false)], WriteSeveralEffect.prototype, 'pure', void 0);
const WriteSeveral = a => WriteSeveralEffect.new({ 'writes': a });
const HasPreviousValueSymbol = Symbol('HasPreviousValueSymbol');
const HasPreviousValueEffect = class extends Effect {
  constructor() {
    super(...arguments);
    this.handler = HasPreviousValueSymbol;
  }
};
const HasPreviousValue = a => HasPreviousValueEffect.new({ 'identifier': a });
const PreviousValueOfSymbol = Symbol('PreviousValueOfSymbol');
const PreviousValueOfEffect = class extends Effect {
  constructor() {
    super(...arguments);
    this.handler = PreviousValueOfSymbol;
  }
};
const PreviousValueOf = a => PreviousValueOfEffect.new({ 'identifier': a });
const ProposedValueOfSymbol = Symbol('ProposedValueOfSymbol');
const ProposedValueOfEffect = class extends Effect {
  constructor() {
    super(...arguments);
    this.handler = ProposedValueOfSymbol;
  }
};
const ProposedValueOf = a => ProposedValueOfEffect.new({ 'identifier': a });
const HasProposedValueSymbol = Symbol('HasProposedValueSymbol');
const HasProposedValueEffect = class extends Effect {
  constructor() {
    super(...arguments);
    this.handler = HasProposedValueSymbol;
  }
};
const HasProposedValue = a => HasProposedValueEffect.new({ 'identifier': a });
const HasProposedNotPreviousValueSymbol = Symbol('HasProposedNotPreviousValueSymbol');
const HasProposedNotPreviousValueEffect = class extends Effect {
  constructor() {
    super(...arguments);
    this.handler = HasProposedNotPreviousValueSymbol;
  }
};
const HasProposedNotPreviousValue = a => HasProposedNotPreviousValueEffect.new({ 'identifier': a });
const ProposedOrPreviousValueOfSymbol = Symbol('ProposedOrPreviousValueOfSymbol');
const ProposedOrPreviousValueOfEffect = class extends Effect {
  constructor() {
    super(...arguments);
    this.handler = ProposedOrPreviousValueOfSymbol;
  }
};
const ProposedOrPreviousValueOf = a => ProposedOrPreviousValueOfEffect.new({ 'identifier': a });
const ProposedArgumentsOfSymbol = Symbol('ProposedArgumentsOfSymbol');
const ProposedArgumentsOfEffect = class extends Effect {
  constructor() {
    super(...arguments);
    this.handler = ProposedArgumentsOfSymbol;
  }
};
const ProposedArgumentsOf = a => ProposedArgumentsOfEffect.new({ 'identifier': a });
const UnsafeProposedOrPreviousValueOfSymbol = Symbol('UnsafeProposedOrPreviousValueOfSymbol');
const UnsafeProposedOrPreviousValueOfEffect = class extends Effect {
  constructor() {
    super(...arguments);
    this.handler = UnsafeProposedOrPreviousValueOfSymbol;
  }
};
const UnsafeProposedOrPreviousValueOf = a => UnsafeProposedOrPreviousValueOfEffect.new({ 'identifier': a });
const UnsafePreviousValueOfSymbol = Symbol('UnsafePreviousValueOfSymbol');
const UnsafePreviousValueOfEffect = class extends Effect {
  constructor() {
    super(...arguments);
    this.handler = UnsafePreviousValueOfSymbol;
  }
};
const UnsafePreviousValueOf = a => UnsafePreviousValueOfEffect.new({ 'identifier': a });
const CycleResolutionInputChrono = class extends CycleResolutionInput {
  collectInfo(a, b, c) {
    if (a(PreviousValueOf(b)) != null)
      this.addPreviousValueFlag(c);
    if (a(HasProposedNotPreviousValue(b)))
      this.addProposedValueFlag(c);
  }
};
const ContextSync = Symbol('ContextSync');
const ContextGen = Symbol('ContextGen');
const CalculationGen = class extends Mixin([], a => class b extends a {
  constructor() {
    super(...arguments);
    this.iterator = void 0;
    this.iterationResult = void 0;
  }
  isCalculationStarted() {
    return Boolean(this.iterator || this.iterationResult);
  }
  isCalculationCompleted() {
    return Boolean(this.iterationResult && this.iterationResult.done);
  }
  get result() {
    return this.iterationResult && this.iterationResult.done ? this.iterationResult.value : void 0;
  }
  startCalculation(c, ...e) {
    const f = this.iterator = this.calculation.call(this.context || this, c, ...e);
    return this.iterationResult = f.next();
  }
  continueCalculation(c) {
    return this.iterationResult = this.iterator.next(c);
  }
  cleanupCalculation() {
    this.iterationResult = void 0;
    this.iterator = void 0;
  }
  *calculation(c, ...e) {
    throw new Error('Abstract method `calculation` called');
  }
  runSyncWithEffect(c, ...e) {
    this.startCalculation(c, ...e);
    while (!this.isCalculationCompleted()) {
      this.continueCalculation(c(this.iterationResult.value));
    }
    this.iterator = void 0;
    return this.result;
  }
  async runAsyncWithEffect(c, ...e) {
    this.startCalculation(c, ...e);
    while (!this.isCalculationCompleted()) {
      this.continueCalculation(await c(this.iterationResult.value));
    }
    this.iterator = void 0;
    return this.result;
  }
}) {
};
const SynchronousCalculationStarted = Symbol('SynchronousCalculationStarted');
const calculationStartedConstant = { 'value': SynchronousCalculationStarted };
const CalculationSync = class extends Mixin([], a => class b extends a {
  constructor() {
    super(...arguments);
    this.iterationResult = void 0;
  }
  isCalculationStarted() {
    return Boolean(this.iterationResult);
  }
  isCalculationCompleted() {
    return Boolean(this.iterationResult && this.iterationResult.done);
  }
  get result() {
    return this.iterationResult && this.iterationResult.done ? this.iterationResult.value : void 0;
  }
  startCalculation(c, ...e) {
    this.iterationResult = calculationStartedConstant;
    return this.iterationResult = {
      'done': true,
      'value': this.calculation.call(this.context || this, c, ...e)
    };
  }
  continueCalculation(c) {
    throw new Error('Can not continue synchronous calculation');
  }
  cleanupCalculation() {
    this.iterationResult = void 0;
  }
  calculation(c, ...e) {
    throw new Error('Abstract method `calculation` called');
  }
  runSyncWithEffect(c, ...e) {
    this.startCalculation(c, ...e);
    return this.result;
  }
  async runAsyncWithEffect(c, ...e) {
    throw new Error('Can not run synchronous calculation asynchronously');
  }
}) {
};
function runGeneratorSyncWithEffect(a, b, c, e) {
  const f = b.apply(e || null, c);
  let g = f.next();
  while (!g.done) {
    g = f.next(a(g.value));
  }
  return g.value;
}
async function runGeneratorAsyncWithEffect(a, b, c, f) {
  const g = b.apply(f || null, c);
  let i = g.next();
  while (!i.done) {
    let j;
    let k = false;
    do {
      k = false;
      try {
        j = a(i.value);
      } catch (l) {
        if (l instanceof Effect) {
          const n = await a(l);
          if (n === 'Cancel') {
            a(Reject(l));
            return;
          } else {
            k = true;
          }
        }
      }
    } while (k);
    if (isPromise(j))
      i = g.next(await j);
    else
      i = g.next(j);
  }
  return i.value;
}
let EdgeType;
(function (a) {
  a[a.Normal = 1] = 'Normal';
  a[a.Past = 2] = 'Past';
}(EdgeType || (EdgeType = {})));
let ORIGIN_ID = 0;
const Quark = class extends MixinAny([Map], a => class b extends a {
  constructor() {
    super(...arguments);
    this.identifier = void 0;
    this.value = void 0;
    this.proposedValue = void 0;
    this.proposedIsPrevious = false;
    this.proposedArguments = void 0;
    this.usedProposedOrPrevious = false;
    this.previous = void 0;
    this.origin = void 0;
    this.originId = MIN_SMI;
    this.needToBuildProposedValue = false;
    this.edgesFlow = 0;
    this.visitedAt = NOT_VISITED;
    this.visitEpoch = 0;
    this.promise = void 0;
    this.$outgoingPast = void 0;
  }
  static new(c) {
    const e = new this();
    c && Object.assign(e, c);
    return e;
  }
  get level() {
    return this.identifier.level;
  }
  get calculation() {
    return this.identifier.calculation;
  }
  get context() {
    return this.identifier.context || this.identifier;
  }
  forceCalculation() {
    this.edgesFlow = MAX_SMI;
  }
  cleanup() {
    this.cleanupCalculation();
  }
  isShadow() {
    return Boolean(this.origin && this.origin !== this);
  }
  resetToEpoch(c) {
    this.visitEpoch = c;
    this.visitedAt = NOT_VISITED;
    if (this.edgesFlow < 0)
      this.edgesFlow = 0;
    this.usedProposedOrPrevious = false;
    this.cleanupCalculation();
    this.clearOutgoing();
    this.promise = void 0;
    if (this.origin && this.origin === this) {
      this.proposedArguments = void 0;
      if (this.value !== void 0) {
        this.proposedValue = this.value;
      }
      this.value = void 0;
    } else {
      this.origin = void 0;
      this.value = void 0;
    }
    if (this.identifier.proposedValueIsBuilt && this.proposedValue !== TombStone) {
      this.needToBuildProposedValue = true;
      this.proposedValue = void 0;
    }
  }
  copyFrom(c) {
    this.value = c.value;
    this.proposedValue = c.proposedValue;
    this.proposedArguments = c.proposedArguments;
    this.usedProposedOrPrevious = c.usedProposedOrPrevious;
  }
  clearProperties() {
    this.value = void 0;
    this.proposedValue = void 0;
    this.proposedArguments = void 0;
  }
  mergePreviousOrigin(c) {
    const e = this.origin;
    if (e !== this.previous)
      throw new Error('Invalid state');
    this.copyFrom(e);
    const f = this.getOutgoing();
    for (const [g, i] of e.getOutgoing()) {
      const j = f.get(g);
      if (!j) {
        const k = c.get(g);
        if (!k || k.originId === i.originId)
          f.set(g, k || i);
      }
    }
    if (e.$outgoingPast !== void 0) {
      const l = this.getOutgoingPast();
      for (const [n, o] of e.getOutgoingPast()) {
        const p = l.get(n);
        if (!p) {
          const q = c.get(n);
          if (!q || q.originId === o.originId)
            l.set(n, q || o);
        }
      }
    }
    this.origin = this;
    e.clearProperties();
    e.clear();
  }
  setOrigin(c) {
    this.origin = c;
    this.originId = c.originId;
  }
  getOrigin() {
    if (this.origin)
      return this.origin;
    return this.startOrigin();
  }
  startOrigin() {
    this.originId = ORIGIN_ID++;
    return this.origin = this;
  }
  getOutgoing() {
    return this;
  }
  getOutgoingPast() {
    if (this.$outgoingPast !== void 0)
      return this.$outgoingPast;
    return this.$outgoingPast = new Map();
  }
  addOutgoingTo(c, e) {
    const f = e === EdgeType.Normal ? this : this.getOutgoingPast();
    f.set(c.identifier, c);
  }
  clearOutgoing() {
    this.clear();
    if (this.$outgoingPast !== void 0)
      this.$outgoingPast.clear();
  }
  getValue() {
    const c = this.origin;
    return c === this ? this.value : c ? c.getValue() : void 0;
  }
  setValue(c) {
    if (this.origin && this.origin !== this)
      throw new Error('Can not set value to the shadow entry');
    this.getOrigin().value = c;
  }
  hasValue() {
    return this.getValue() !== void 0;
  }
  hasProposedValue() {
    if (this.isShadow())
      return false;
    return this.hasProposedValueInner();
  }
  hasProposedValueInner() {
    return this.proposedValue !== void 0;
  }
  getProposedValue(c) {
    if (this.needToBuildProposedValue) {
      this.proposedValue = this.identifier.buildProposedValue.call(this.identifier.context || this.identifier, this.identifier, this, c);
      this.needToBuildProposedValue = false;
    }
    return this.proposedValue;
  }
  outgoingInTheFutureCb(c, e) {
    let f = this;
    while (f) {
      for (const g of f.getOutgoing().values()) {
        if (g.originId === c.getLatestEntryFor(g.identifier).originId)
          e(g);
      }
      if (f.isShadow())
        f = f.previous;
      else
        f = null;
    }
  }
  outgoingInTheFutureAndPastCb(c, e) {
    let f = this;
    while (f) {
      for (const g of f.getOutgoing().values()) {
        const i = c.getLatestEntryFor(g.identifier);
        if (i && g.originId === i.originId)
          e(g);
      }
      if (f.$outgoingPast !== void 0) {
        for (const j of f.$outgoingPast.values()) {
          const k = c.getLatestEntryFor(j.identifier);
          if (k && j.originId === k.originId)
            e(j);
        }
      }
      if (f.isShadow())
        f = f.previous;
      else
        f = null;
    }
  }
  outgoingInTheFutureAndPastTransactionCb(c, e) {
    let f = this;
    while (f) {
      for (const g of f.getOutgoing().values()) {
        const i = c.getLatestStableEntryFor(g.identifier);
        if (i && g.originId === i.originId)
          e(g);
      }
      if (f.$outgoingPast !== void 0) {
        for (const j of f.$outgoingPast.values()) {
          const k = c.getLatestStableEntryFor(j.identifier);
          if (k && j.originId === k.originId)
            e(j);
        }
      }
      if (f.isShadow())
        f = f.previous;
      else
        f = null;
    }
  }
  outgoingInTheFutureTransactionCb(c, e) {
    let f = this;
    while (f) {
      for (const g of f.getOutgoing().values()) {
        const i = c.getLatestEntryFor(g.identifier);
        if (i && g.originId === i.originId)
          e(g);
      }
      if (f.isShadow())
        f = f.previous;
      else
        f = null;
    }
  }
}) {
};
var TombStone = Symbol('Tombstone');
const __decorate3 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
let Levels;
(function (a) {
  a[a.UserInput = 0] = 'UserInput';
  a[a.DependsOnlyOnUserInput = 1] = 'DependsOnlyOnUserInput';
  a[a.DependsOnlyOnDependsOnlyOnUserInput = 2] = 'DependsOnlyOnDependsOnlyOnUserInput';
  a[a.DependsOnSelfKind = 3] = 'DependsOnSelfKind';
}(Levels || (Levels = {})));
const Meta = class extends Base2 {
  constructor() {
    super(...arguments);
    this.name = void 0;
    this.lazy = false;
    this.total = true;
    this.pure = true;
    this.proposedValueIsBuilt = false;
  }
  calculation(a) {
    throw new Error('Abstract method `calculation` called');
  }
  equality(a, b) {
    return a === b;
  }
};
__decorate3([prototypeValue(Levels.DependsOnSelfKind)], Meta.prototype, 'level', void 0);
__decorate3([prototypeValue(true)], Meta.prototype, 'sync', void 0);
const Identifier = class extends Meta {
  constructor() {
    super(...arguments);
    this.context = void 0;
    this.isWritingUndefined = false;
  }
  newQuark(a) {
    const b = this.quarkClass.new();
    b.identifier = this;
    b.needToBuildProposedValue = this.proposedValueIsBuilt;
    return b;
  }
  write(a, b, c, e, ...f) {
    c = c || b.getWriteTarget(a);
    c.proposedValue = e;
    c.proposedArguments = f.length > 0 ? f : void 0;
    c.proposedIsPrevious = b.graph.isWritingPreviousData;
  }
  writeToTransaction(a, b, ...c) {
    a.write(this, b, ...c);
  }
  writeToGraph(a, b, ...c) {
    a.write(this, b, ...c);
  }
  readFromGraphAsync(a) {
    return a.readAsync(this);
  }
  readFromGraph(a) {
    return a.read(this);
  }
  readFromTransaction(a) {
    return a.read(this);
  }
  readFromTransactionAsync(a) {
    return a.readAsync(this);
  }
  buildProposedValue(a, b, c) {
    return void 0;
  }
  enterGraph(a) {
  }
  leaveGraph(a) {
  }
};
const IdentifierC = a => Identifier.new(a);
const QuarkSync = Quark.mix(CalculationSync.mix(Map));
const QuarkGen = Quark.mix(CalculationGen.mix(Map));
const Variable = class extends Identifier {
  calculation(a) {
    throw new Error('The \'calculation\' method of the variables should never be called. Instead, the value will be set directly to quark');
  }
  write(a, b, c, e, ...f) {
    c = c || b.getWriteTarget(a);
    c.value = e;
    c.proposedArguments = f.length > 0 ? f : void 0;
  }
};
__decorate3([prototypeValue(Levels.UserInput)], Variable.prototype, 'level', void 0);
__decorate3([prototypeValue(QuarkSync)], Variable.prototype, 'quarkClass', void 0);
function VariableC(...a) {
  return Variable.new(...a);
}
const CalculatedValueSync = class extends Identifier {
  calculation(a) {
    return a(ProposedOrPrevious);
  }
};
__decorate3([prototypeValue(QuarkSync)], CalculatedValueSync.prototype, 'quarkClass', void 0);
function CalculatedValueSyncC(...a) {
  return CalculatedValueSync.new(...a);
}
const CalculatedValueGen = class extends Identifier {
  *calculation(a) {
    return yield ProposedOrPrevious;
  }
};
__decorate3([prototypeValue(QuarkGen)], CalculatedValueGen.prototype, 'quarkClass', void 0);
function CalculatedValueGenC(...a) {
  return CalculatedValueGen.new(...a);
}
const throwUnknownIdentifier = a => {
  throw new Error('Unknown identifier ' + a);
};
let CLOCK = 0;
const Revision = class extends Base2 {
  constructor() {
    super(...arguments);
    this.createdAt = CLOCK++;
    this.name = 'revision-' + this.createdAt;
    this.previous = void 0;
    this.scope = new Map();
    this.reachableCount = 0;
    this.referenceCount = 0;
    this.selfDependent = new Set();
  }
  getLatestEntryFor(a) {
    let b = this;
    while (b) {
      const c = b.scope.get(a);
      if (c)
        return c;
      b = b.previous;
    }
    return null;
  }
  hasIdentifier(a) {
    const b = this.getLatestEntryFor(a);
    return Boolean(b && b.getValue() !== TombStone);
  }
  *previousAxis() {
    let a = this;
    while (a) {
      yield a;
      a = a.previous;
    }
  }
};
const LeveledQueue = class {
  constructor() {
    this.length = 0;
    this.levels = [];
    this.lowestLevel = MAX_SMI;
  }
  getLowestLevel() {
    for (let a = this.lowestLevel !== MAX_SMI ? this.lowestLevel : 0; a < this.levels.length; a++) {
      if (this.levels[a])
        return this.lowestLevel = a;
    }
    return this.lowestLevel = MAX_SMI;
  }
  takeLowestLevel() {
    for (let a = this.lowestLevel !== MAX_SMI ? this.lowestLevel : 0; a < this.levels.length; a++) {
      const b = this.levels[a];
      if (b) {
        this.length -= b.length;
        this.levels[a] = null;
        this.lowestLevel = a + 1;
        return b;
      }
    }
  }
  pop() {
    for (let a = this.lowestLevel !== MAX_SMI ? this.lowestLevel : 0; a < this.levels.length; a++) {
      const b = this.levels[a];
      this.lowestLevel = a;
      if (b && b.length > 0) {
        this.length--;
        return b.pop();
      }
    }
    this.lowestLevel = MAX_SMI;
  }
  push(a) {
    const b = a.level;
    let c = this.levels[b];
    if (!c) {
      for (let e = this.levels.length; e < b; e++)
        this.levels[e] = null;
      c = this.levels[b] = [];
    }
    c.push(a);
    this.length++;
    if (b < this.lowestLevel)
      this.lowestLevel = b;
  }
  *[Symbol.iterator]() {
    for (let a = 0; a < this.levels.length; a++) {
      const b = this.levels[a];
      if (b)
        yield* b;
    }
  }
};
const ComputationCycle = class extends Base2 {
  toString() {
    const a = [];
    const b = [];
    this.cycle.forEach(({
      name: c,
      context: e
    }) => {
      a.push(c);
      if (b[b.length - 1] !== e)
        b.push(e);
    });
    return 'events: \n' + b.map(c => '#' + c.id).join(' => ') + '\n\nidentifiers: \n' + a.join('\n');
  }
};
const TransactionCycleDetectionWalkContext = class extends WalkContext {
  constructor() {
    super(...arguments);
    this.transaction = void 0;
  }
  onCycle(a, b) {
    return OnCycleAction.Cancel;
  }
  doCollectNext(a, b, c) {
    let e = this.visited.get(b);
    if (!e) {
      e = {
        'visitedAt': NOT_VISITED,
        'visitEpoch': this.currentEpoch
      };
      this.visited.set(b, e);
    }
    c.push({
      'node': b,
      'from': a,
      'label': void 0
    });
  }
  collectNext(a, b) {
    const c = this.transaction.getLatestEntryFor(a);
    if (c) {
      c.outgoingInTheFutureTransactionCb(this.transaction, e => {
        this.doCollectNext(a, e.identifier, b);
      });
    }
  }
};
const TransactionWalkDepth = class extends Base2 {
  constructor() {
    super(...arguments);
    this.visited = new Map();
    this.transaction = void 0;
    this.baseRevision = void 0;
    this.pushTo = void 0;
    this.toVisit = [];
    this.currentEpoch = 0;
  }
  startFrom(a) {
    this.continueFrom(a);
  }
  continueFrom(a) {
    this.toVisit.push.apply(this.toVisit, a);
    this.walkDepth();
  }
  startNewEpoch() {
    if (this.toVisit.length)
      throw new Error('Can not start new walk epoch in the middle of the walk');
    this.currentEpoch++;
  }
  onTopologicalNode(a, b) {
    if (!a.lazy && a.level !== Levels.UserInput)
      this.pushTo.push(b);
  }
  onCycle(a, b) {
    return OnCycleAction.Resume;
  }
  doCollectNext(a, b, c) {
    let e = this.visited.get(b);
    if (!e) {
      e = b.newQuark(this.baseRevision);
      e.visitEpoch = this.currentEpoch;
      this.visited.set(b, e);
    }
    c.push(b);
  }
  collectNext(a, b, c) {
    const e = this.baseRevision.getLatestEntryFor(a);
    if (e) {
      c.previous = e;
      e.outgoingInTheFutureAndPastTransactionCb(this.transaction, f => {
        this.doCollectNext(a, f.identifier, b);
      });
    }
    for (const f of c.getOutgoing().keys()) {
      this.doCollectNext(a, f, b);
    }
    if (c.$outgoingPast !== void 0)
      for (const g of c.getOutgoingPast().keys()) {
        this.doCollectNext(a, g, b);
      }
  }
  walkDepth() {
    const a = this.visited;
    const b = this.toVisit;
    let c;
    while (c = b.length) {
      const e = b[c - 1];
      let f = a.get(e);
      if (f && f.visitedAt === VISITED_TOPOLOGICALLY && f.visitEpoch === this.currentEpoch) {
        f.edgesFlow++;
        b.pop();
        continue;
      }
      if (f && f.visitEpoch === this.currentEpoch && f.visitedAt !== NOT_VISITED) {
        if (f.visitedAt < c) {
          if (this.onCycle(e, b) !== OnCycleAction.Resume)
            break;
          f.edgesFlow++;
        } else {
          f.visitedAt = VISITED_TOPOLOGICALLY;
          this.onTopologicalNode(e, f);
        }
        b.pop();
      } else {
        const g = b.length;
        if (!f) {
          f = e.newQuark(this.baseRevision);
          f.visitEpoch = this.currentEpoch;
          a.set(e, f);
        }
        this.collectNext(e, b, f);
        if (f.visitEpoch < this.currentEpoch) {
          f.resetToEpoch(this.currentEpoch);
        }
        f.visitedAt = c;
        f.edgesFlow++;
        if (b.length === g) {
          f.visitedAt = VISITED_TOPOLOGICALLY;
          this.onTopologicalNode(e, f);
          b.pop();
        }
      }
    }
  }
};
const EdgeTypeNormal = EdgeType.Normal;
const EdgeTypePast = EdgeType.Past;
const Transaction2 = class extends Base2 {
  constructor() {
    super(...arguments);
    this.baseRevision = void 0;
    this.candidateClass = Revision;
    this.candidate = void 0;
    this.graph = void 0;
    this.isClosed = false;
    this.walkContext = void 0;
    this.entries = new Map();
    this.stackGen = new LeveledQueue();
    this.activeStack = [];
    this.onEffectSync = void 0;
    this.onEffectAsync = void 0;
    this.propagationStartDate = 0;
    this.lastProgressNotificationDate = 0;
    this.startProgressNotificationsAfterMs = 500;
    this.emitProgressNotificationsEveryMs = 200;
    this.emitProgressNotificationsEveryCalculations = 100;
    this.plannedTotalIdentifiersToCalculate = 0;
    this.ongoing = Promise.resolve();
    this.selfDependedMarked = false;
    this.rejectedWith = void 0;
    this.stopped = false;
    this.hasEntryWithProposedValue = false;
    this.hasVariableEntry = false;
  }
  initialize(...a) {
    super.initialize(...a);
    this.walkContext = TransactionWalkDepth.new({
      'visited': this.entries,
      'transaction': this,
      'baseRevision': this.baseRevision,
      'pushTo': this.stackGen
    });
    if (!this.candidate)
      this.candidate = this.candidateClass.new({ 'previous': this.baseRevision });
    this.onEffectSync = this.read.bind(this);
    this.onEffectAsync = this.readAsync.bind(this);
  }
  get dirty() {
    return this.entries.size > 0;
  }
  markSelfDependent() {
    if (this.selfDependedMarked)
      return;
    this.selfDependedMarked = true;
    for (const a of this.baseRevision.selfDependent) {
      const b = this.entries.get(a);
      if (b && b.getValue() === TombStone)
        continue;
      this.touch(a);
    }
  }
  getActiveEntry() {
    return this.activeStack[this.activeStack.length - 1];
  }
  yieldAsync(a) {
    if (isPromise(a))
      return a;
    return this.graph[a.handler](a, this);
  }
  yieldSync(a) {
    if (isPromise(a)) {
      throw new Error('Can not yield a promise in the synchronous context');
    }
    return this.graph[a.handler](a, this);
  }
  readAsync(a) {
    if (!(a instanceof Identifier))
      return this.yieldAsync(a);
    let b;
    const c = this.getActiveEntry();
    if (c) {
      b = this.addEdge(a, c, EdgeTypeNormal);
    } else {
      b = this.entries.get(a);
      if (!b) {
        const e = this.baseRevision.getLatestEntryFor(a);
        if (!e)
          throwUnknownIdentifier(a);
        b = e.hasValue() ? e : this.touch(a);
      }
    }
    if (b.hasValue())
      return b.getValue();
    if (b.promise)
      return b.promise;
    if (!b.previous || !b.previous.hasValue())
      b.forceCalculation();
    return this.ongoing = b.promise = this.ongoing.then(() => {
      return (async () => {
        while (this.stackGen.lowestLevel < a.level) {
          await runGeneratorAsyncWithEffect(this.onEffectAsync, this.calculateTransitionsStackGen, [
            this.onEffectAsync,
            this.stackGen.takeLowestLevel()
          ], this);
        }
        this.markSelfDependent();
        if (b.getValue() === void 0)
          return runGeneratorAsyncWithEffect(this.onEffectAsync, this.calculateTransitionsStackGen, [
            this.onEffectAsync,
            [b]
          ], this);
      })();
    }).then(() => {
      if (this.rejectedWith)
        throw new Error('Transaction rejected: ' + String(this.rejectedWith.reason));
      if (!b.hasValue())
        throw new Error('Computation cycle. Sync');
      return b.getValue();
    });
  }
  get(a) {
    if (!(a instanceof Identifier))
      return this.yieldSync(a);
    let b;
    const c = this.getActiveEntry();
    if (c) {
      b = this.addEdge(a, c, EdgeTypeNormal);
    } else {
      b = this.entries.get(a);
      if (!b) {
        const f = this.baseRevision.getLatestEntryFor(a);
        if (!f)
          throwUnknownIdentifier(a);
        b = f.hasValue() ? f : this.touch(a);
      }
    }
    const e = b.getValue();
    if (e === TombStone)
      throwUnknownIdentifier(a);
    if (e !== void 0 && b.hasValue())
      return e;
    if (b.promise)
      return b.promise;
    if (!b.previous || !b.previous.hasValue())
      b.forceCalculation();
    while (this.stackGen.getLowestLevel() < a.level) {
      this.calculateTransitionsStackSync(this.onEffectSync, this.stackGen.takeLowestLevel());
    }
    this.markSelfDependent();
    if (a.sync) {
      this.calculateTransitionsStackSync(this.onEffectSync, [b]);
      const g = b.getValue();
      if (g === void 0)
        throw new Error('Cycle during synchronous computation');
      if (g === TombStone)
        throwUnknownIdentifier(a);
      return g;
    } else {
      const i = this.ongoing = b.promise = this.ongoing.then(() => {
        if (b.getValue() === void 0)
          return runGeneratorAsyncWithEffect(this.onEffectAsync, this.calculateTransitionsStackGen, [
            this.onEffectAsync,
            [b]
          ], this);
      }).then(() => {
        if (this.rejectedWith)
          throw new Error('Transaction rejected: ' + String(this.rejectedWith.reason));
        const j = b.getValue();
        if (j === void 0)
          throw new Error('Computation cycle. Async get');
        if (j === TombStone)
          throwUnknownIdentifier(a);
        return j;
      });
      if (DEBUG) {
        i.quark = b;
      }
      return i;
    }
  }
  read(a) {
    if (!(a instanceof Identifier))
      return this.yieldSync(a);
    let b;
    const c = this.getActiveEntry();
    if (c) {
      b = this.addEdge(a, c, EdgeTypeNormal);
    } else {
      b = this.entries.get(a);
      if (!b) {
        const g = this.baseRevision.getLatestEntryFor(a);
        if (!g)
          throwUnknownIdentifier(a);
        b = g.hasValue() ? g : this.touch(a);
      }
    }
    const e = b.getValue();
    if (e === TombStone)
      throwUnknownIdentifier(a);
    if (e !== void 0)
      return e;
    if (!b.previous || !b.previous.hasValue())
      b.forceCalculation();
    while (this.stackGen.getLowestLevel() < a.level) {
      this.calculateTransitionsStackSync(this.onEffectSync, this.stackGen.takeLowestLevel());
    }
    this.markSelfDependent();
    this.calculateTransitionsStackSync(this.onEffectSync, [b]);
    const f = b.getValue();
    if (f === void 0)
      throw new Error('Cycle during synchronous computation');
    if (f === TombStone)
      throwUnknownIdentifier(a);
    return f;
  }
  readCurrentOrProposedOrPrevious(a) {
    const b = this.entries.get(a);
    if (b) {
      const c = b.getValue();
      if (c !== void 0)
        return c;
      if (b.proposedValue !== void 0)
        return b.proposedValue;
    }
    return this.readPrevious(a);
  }
  readCurrentOrProposedOrPreviousAsync(a) {
    const b = this.entries.get(a);
    if (b) {
      const c = b.getValue();
      if (c !== void 0)
        return c;
      if (b.proposedValue !== void 0)
        return b.proposedValue;
    }
    return this.readPreviousAsync(a);
  }
  readPrevious(a) {
    const b = this.baseRevision.getLatestEntryFor(a);
    if (!b)
      return void 0;
    const c = b.getValue();
    return c !== TombStone ? c === void 0 && a.lazy ? this.read(a) : c : void 0;
  }
  readPreviousAsync(a) {
    const b = this.baseRevision.getLatestEntryFor(a);
    if (!b)
      return void 0;
    const c = b.getValue();
    return c !== TombStone ? c !== void 0 ? c : this.readAsync(a) : void 0;
  }
  readProposedOrPrevious(a) {
    const b = this.entries.get(a);
    if (b && b.proposedValue !== void 0) {
      return b.proposedValue;
    } else {
      return this.readPrevious(a);
    }
  }
  readProposedOrPreviousAsync(a) {
    const b = this.entries.get(a);
    if (b && b.proposedValue !== void 0) {
      return b.proposedValue;
    } else {
      return this.readPreviousAsync(a);
    }
  }
  write(a, b, ...c) {
    if (b === void 0)
      b = null;
    a.write.call(a.context || a, a, this, null, b, ...c);
    const e = this.entries.get(a);
    this.hasVariableEntry = this.hasVariableEntry || !e.isShadow() && a.level === Levels.UserInput;
    this.hasEntryWithProposedValue = this.hasEntryWithProposedValue || e.hasProposedValue();
  }
  getWriteTarget(a) {
    return this.touch(a).startOrigin();
  }
  acquireQuarkIfExists(a) {
    const b = this.entries.get(a);
    return b && b.origin === b ? b.origin : void 0;
  }
  touch(a) {
    const b = this.entries.get(a);
    if (!b || b.visitEpoch < this.walkContext.currentEpoch)
      this.walkContext.continueFrom([a]);
    const c = b || this.entries.get(a);
    c.forceCalculation();
    return c;
  }
  hasIdentifier(a) {
    const b = this.entries.get(a);
    if (b && b.getValue() === TombStone)
      return false;
    return Boolean(b || this.baseRevision.getLatestEntryFor(a));
  }
  addIdentifier(a, b, ...c) {
    let e = this.entries.get(a);
    const f = Boolean(e);
    const g = a.level === Levels.UserInput;
    if (!e) {
      e = a.newQuark(this.baseRevision);
      e.previous = this.baseRevision.getLatestEntryFor(a);
      e.forceCalculation();
      this.entries.set(a, e);
      if (!a.lazy && !g)
        this.stackGen.push(e);
      this.hasVariableEntry = this.hasVariableEntry || !e.isShadow() && g;
      this.hasEntryWithProposedValue = this.hasEntryWithProposedValue || e.hasProposedValue();
    }
    if (b !== void 0 || g) {
      e.startOrigin();
      const i = f && (e.proposedValue !== void 0 || e.value !== void 0);
      if (!i || e.proposedValue === TombStone || e.value === TombStone) {
        a.isWritingUndefined = b === void 0;
        a.write.call(a.context || a, a, this, e, b === void 0 && g ? null : b, ...c);
        a.isWritingUndefined = false;
      }
    }
    if (e.getValue() === TombStone)
      e.value = void 0;
    if (e.proposedValue === TombStone)
      e.proposedValue = void 0;
    a.enterGraph(this.graph);
    return e;
  }
  removeIdentifier(a) {
    a.leaveGraph(this.graph);
    const b = this.touch(a).startOrigin();
    b.setValue(TombStone);
    this.candidate.selfDependent.delete(a);
  }
  populateCandidateScopeFromTransitions(a, b) {
    if (a.scope.size === 0) {
      a.scope = b;
    } else {
      for (const [c, e] of b) {
        if (e.isShadow()) {
          const f = a.getLatestEntryFor(c);
          e.getOutgoing().forEach((g, i) => f.getOutgoing().set(i, g));
        } else {
          a.scope.set(c, e);
        }
      }
    }
  }
  preCommit(a) {
    if (this.isClosed)
      throw new Error('Can not propagate closed revision');
    this.markSelfDependent();
    this.isClosed = true;
    this.propagationStartDate = Date.now();
    this.plannedTotalIdentifiersToCalculate = this.stackGen.length;
  }
  postCommit() {
    this.populateCandidateScopeFromTransitions(this.candidate, this.entries);
    const a = this.entries;
    this.walkContext = void 0;
    return {
      'revision': this.candidate,
      'entries': a,
      'transaction': this
    };
  }
  commit(a) {
    this.preCommit(a);
    this.calculateTransitionsSync(this.onEffectSync);
    return this.postCommit();
  }
  reject(a = RejectEffect.new()) {
    this.rejectedWith = a;
    this.walkContext = void 0;
  }
  stop() {
    this.stopped = true;
  }
  clearRejected() {
    for (const a of this.entries.values()) {
      a.cleanup();
    }
    this.entries.clear();
  }
  async commitAsync(a) {
    this.preCommit(a);
    return this.ongoing = this.ongoing.then(() => {
      return runGeneratorAsyncWithEffect(this.onEffectAsync, this.calculateTransitions, [this.onEffectAsync], this);
    }).then(() => {
      return this.postCommit();
    });
  }
  getLatestEntryFor(a) {
    const b = this.entries.get(a) || this.baseRevision.getLatestEntryFor(a);
    if (b && b.getValue() === TombStone)
      return void 0;
    return b;
  }
  getLatestStableOrProposedEntryFor(a) {
    const b = this.entries.get(a);
    if (b) {
      const c = b.getValue();
      if (c === TombStone)
        return void 0;
      return c === void 0 && b.proposedValue === void 0 ? this.baseRevision.getLatestEntryFor(a) : b;
    } else {
      return this.baseRevision.getLatestEntryFor(a);
    }
  }
  getLatestStableEntryFor(a) {
    const b = this.entries.get(a);
    if (b) {
      const c = b.getValue();
      if (c === TombStone)
        return void 0;
      return c === void 0 ? this.baseRevision.getLatestEntryFor(a) : b;
    } else {
      return this.baseRevision.getLatestEntryFor(a);
    }
  }
  addEdge(a, b, c) {
    const e = b.identifier;
    if (e.level < a.level)
      throw new Error('Identifier can not read from higher level identifier');
    let f = this.entries.get(a);
    if (!f) {
      const g = this.baseRevision.getLatestEntryFor(a);
      if (!g)
        throwUnknownIdentifier(a);
      f = a.newQuark(this.baseRevision);
      f.setOrigin(g);
      f.previous = g;
      this.entries.set(a, f);
    }
    f.addOutgoingTo(b, c);
    return f;
  }
  onQuarkCalculationCompleted(a, b) {
    a.cleanup();
    const c = a.identifier;
    const e = a.previous;
    const f = Boolean(e && e.hasValue() && c.equality(b, e.getValue()));
    if (f) {
      e.outgoingInTheFutureAndPastTransactionCb(this, i => {
        const j = this.entries.get(i.identifier);
        if (j)
          j.edgesFlow--;
      });
      a.edgesFlow = MAX_SMI;
      a.setOrigin(e.origin);
    } else {
      a.startOrigin();
      a.setValue(b);
    }
    let g = false;
    if (a.usedProposedOrPrevious) {
      if (a.proposedValue !== void 0) {
        if (c.equality(b, a.proposedValue))
          g = true;
      } else {
        if (f || !e && b === null)
          g = true;
      }
      if (!g)
        this.candidate.selfDependent.add(c);
    }
  }
  onReadIdentifier(a, b, c) {
    const e = this.addEdge(a, b, EdgeTypeNormal);
    if (e.hasValue() || e.value !== void 0) {
      const f = e.getValue();
      if (f === TombStone)
        throwUnknownIdentifier(a);
      return b.continueCalculation(f);
    } else if (e.isShadow()) {
      e.startOrigin();
      e.forceCalculation();
      c.push(e);
      return void 0;
    } else {
      if (!e.isCalculationStarted()) {
        c.push(e);
        if (!e.previous || !e.previous.hasValue())
          e.forceCalculation();
        return void 0;
      } else {
        let g;
        const i = TransactionCycleDetectionWalkContext.new({
          'transaction': this,
          'onCycle'(j, k) {
            g = ComputationCycle.new({
              'cycle': cycleInfo(k),
              'requestedEntry': e,
              'activeEntry': b
            });
            return OnCycleAction.Cancel;
          }
        });
        i.startFrom([e.identifier]);
        return g;
      }
    }
  }
  *calculateTransitions(a) {
    const b = this.stackGen;
    while (b.length) {
      yield* this.calculateTransitionsStackGen(a, b.takeLowestLevel());
    }
  }
  calculateTransitionsSync(a) {
    const b = this.stackGen;
    while (b.length) {
      this.calculateTransitionsStackSync(a, b.takeLowestLevel());
    }
  }
  *calculateTransitionsStackGen(a, b) {
    if (this.rejectedWith || this.stopped)
      return;
    this.walkContext.startNewEpoch();
    const c = this.entries;
    const e = this.propagationStartDate;
    const f = this.graph ? this.graph.enableProgressNotifications : false;
    let g = 0;
    let i = this.activeStack;
    this.activeStack = b;
    while (b.length && !this.rejectedWith && !this.stopped) {
      if (f && !(g++ % this.emitProgressNotificationsEveryCalculations)) {
        const p = Date.now();
        const q = p - e;
        if (q > this.startProgressNotificationsAfterMs) {
          const r = this.lastProgressNotificationDate;
          if (!r || p - r > this.emitProgressNotificationsEveryMs) {
            this.lastProgressNotificationDate = p;
            this.graph.onPropagationProgressNotification({
              'total': this.plannedTotalIdentifiersToCalculate,
              'remaining': this.stackGen.length + b.length,
              'phase': 'propagating'
            });
            this.activeStack = i;
            yield delay2(0);
            this.activeStack = b;
          }
        }
      }
      if (this.rejectedWith || this.stopped)
        break;
      const j = b[b.length - 1];
      const k = j.identifier;
      const l = c.get(k);
      if (l !== j) {
        j.cleanup();
        b.pop();
        continue;
      }
      if (j.edgesFlow == 0) {
        j.edgesFlow--;
        const u = j.previous;
        u && u.outgoingInTheFutureAndPastTransactionCb(this, v => {
          const w = c.get(v.identifier);
          if (w)
            w.edgesFlow--;
        });
      }
      if (j.edgesFlow < 0 && j.previous && j.previous.origin) {
        j.setOrigin(j.previous.origin);
        if (j.size === 0) {
          c.delete(k);
        }
        j.cleanup();
        b.pop();
        continue;
      }
      if (j.hasValue() || j.proposedValue === TombStone) {
        j.cleanup();
        b.pop();
        continue;
      }
      const n = j.visitEpoch;
      let o = j.isCalculationStarted() ? j.iterationResult : j.startCalculation(this.onEffectSync);
      while (o && !this.rejectedWith && !this.stopped) {
        const v = o.value === void 0 ? null : o.value;
        if (j.isCalculationCompleted()) {
          if (j.visitEpoch == n) {
            this.onQuarkCalculationCompleted(j, v);
          }
          b.pop();
          break;
        } else if (v instanceof Identifier) {
          const w = this.onReadIdentifier(v, j, b);
          if (w instanceof ComputationCycle) {
            this.walkContext.startNewEpoch();
            this.activeStack = i;
            yield* this.graph.onComputationCycleHandler(w);
            i = this.activeStack;
            this.activeStack = b;
            j.cleanupCalculation();
            o = void 0;
          } else {
            o = w;
          }
        } else if (v === SynchronousCalculationStarted) {
          b.pop();
          break;
        } else {
          const z = yield v;
          if (z === BreakCurrentStackExecution)
            break;
          if (j.visitEpoch === n) {
            o = j.continueCalculation(z);
          } else {
            b.pop();
            break;
          }
        }
      }
    }
    this.activeStack = i;
  }
  calculateTransitionsStackSync(a, b) {
    if (this.rejectedWith || this.stopped)
      return;
    this.walkContext.startNewEpoch();
    const c = this.entries;
    let e = this.activeStack;
    this.activeStack = b;
    while (b.length && !this.rejectedWith && !this.stopped) {
      const f = b[b.length - 1];
      const g = f.identifier;
      const i = c.get(g);
      if (i !== f) {
        f.cleanup();
        b.pop();
        continue;
      }
      if (f.edgesFlow == 0) {
        f.edgesFlow--;
        const l = f.previous;
        l && l.outgoingInTheFutureAndPastTransactionCb(this, n => {
          const o = c.get(n.identifier);
          if (o)
            o.edgesFlow--;
        });
      }
      if (f.edgesFlow < 0 && f.previous && f.previous.origin) {
        f.setOrigin(f.previous.origin);
        if (f.size === 0) {
          c.delete(g);
        }
        f.cleanup();
        b.pop();
        continue;
      }
      if (f.hasValue() || f.proposedValue === TombStone) {
        f.cleanup();
        b.pop();
        continue;
      }
      const j = f.visitEpoch;
      let k = f.isCalculationStarted() ? f.iterationResult : f.startCalculation(this.onEffectSync);
      while (k && !this.rejectedWith && !this.stopped) {
        const n = k.value === void 0 ? null : k.value;
        if (f.isCalculationCompleted()) {
          if (f.visitEpoch == j) {
            this.onQuarkCalculationCompleted(f, n);
          }
          b.pop();
          break;
        } else if (n instanceof Identifier) {
          const o = this.onReadIdentifier(n, f, b);
          if (o instanceof ComputationCycle) {
            this.walkContext.startNewEpoch();
            this.activeStack = e;
            this.graph.onComputationCycleHandlerSync(o, this);
            e = this.activeStack;
            this.activeStack = b;
            f.cleanupCalculation();
            k = void 0;
          } else {
            k = o;
          }
        } else if (n === SynchronousCalculationStarted) {
          b.pop();
          break;
        } else {
          const p = a(n);
          if (isPromise(p))
            throw new Error('Effect resolved to promise in the synchronous context, check that you marked the asynchronous calculations accordingly');
          if (p === BreakCurrentStackExecution)
            break;
          if (f.visitEpoch === j) {
            k = f.continueCalculation(p);
          } else {
            b.pop();
            break;
          }
        }
      }
    }
    this.activeStack = e;
  }
};
const CommitZero = { 'rejectedWith': null };
const Listener = class extends Base2 {
  constructor() {
    super(...arguments);
    this.handlers = [];
  }
  trigger(a) {
    for (let b = 0; b < this.handlers.length; b++)
      this.handlers[b](a);
  }
};
const ChronoGraph = class extends Base2 {
  constructor() {
    super(...arguments);
    this.baseRevisionStable = void 0;
    this.baseRevisionTentative = void 0;
    this.baseRevision = Revision.new();
    this.topRevision = void 0;
    this.historyLimit = 0;
    this.listeners = new Map();
    this.isWritingPreviousData = false;
    this.$activeTransaction = void 0;
    this.isCommitting = false;
    this.enableProgressNotifications = false;
    this.ongoing = Promise.resolve();
    this._isInitialCommit = true;
    this.autoCommitTimeoutId = null;
    this.autoCommit = false;
    this.autoCommitMode = 'sync';
    this.autoCommitHandler = null;
    this.onWriteDuringCommit = 'throw';
    this.onComputationCycle = 'throw';
    this.transactionClass = Transaction2;
    this.isJustCleared = false;
    this.$followingRevision = void 0;
  }
  initialize(...a) {
    super.initialize(...a);
    if (!this.topRevision)
      this.topRevision = this.baseRevision;
    if (this.autoCommit) {
      this.autoCommitHandler = this.autoCommitMode === 'sync' ? b => this.commit(b) : async b => this.commitAsync(b);
    }
    this.markAndSweep();
  }
  hasPendingAutoCommit() {
    return this.autoCommitTimeoutId !== null;
  }
  get dirty() {
    return this.activeTransaction.dirty;
  }
  clear() {
    this.reject();
    this.unScheduleAutoCommit();
    this.baseRevision.scope && this.baseRevision.scope.clear();
    this.baseRevision.previous = null;
    this.listeners.clear();
    this.topRevision = this.baseRevision;
    this.$followingRevision = void 0;
    this.$activeTransaction = void 0;
    this.markAndSweep();
    this.isJustCleared = true;
  }
  clearBranch() {
    this.reject();
    this.unScheduleAutoCommit();
    for (const [a, b] of this.eachReachableRevision()) {
      if (b)
        a.reachableCount--;
      a.referenceCount--;
    }
    this.baseRevision.previous = null;
    this.listeners.clear();
    this.topRevision = this.baseRevision;
    this.$followingRevision = void 0;
    this.$activeTransaction = void 0;
  }
  *eachReachableRevision() {
    let a = true;
    let b = 0;
    for (const c of this.topRevision.previousAxis()) {
      yield [
        c,
        a || b < this.historyLimit
      ];
      if (c === this.baseRevision) {
        a = false;
      } else {
        if (!a)
          b++;
      }
    }
  }
  get isInitialCommit() {
    return this._isInitialCommit;
  }
  set isInitialCommit(a) {
    this._isInitialCommit = a;
  }
  markAndSweep() {
    let a;
    const b = [];
    for (const [c, e] of this.eachReachableRevision()) {
      if (e) {
        c.reachableCount++;
        a = c;
      } else
        b.push(c);
      c.referenceCount++;
    }
    b.unshift(a);
    for (let f = b.length - 1; f >= 1 && b[f].reachableCount === 0; f--) {
      this.compactRevisions(b[f - 1], b[f]);
    }
  }
  compactRevisions(a, b) {
    if (b.reachableCount > 0 || a.previous !== b)
      throw new Error('Invalid compact operation');
    if (b.referenceCount <= 1) {
      for (const [c, e] of a.scope) {
        if (e.getValue() === TombStone) {
          b.scope.delete(c);
        } else {
          const f = b.scope.get(c);
          if (e.origin === e) {
            if (f) {
              f.clear();
              f.clearProperties();
            }
          } else if (f && e.origin === f) {
            e.mergePreviousOrigin(a.scope);
          } else if (c.lazy && !e.origin && f && f.origin) {
            e.startOrigin().proposedValue = f.origin.value !== void 0 ? f.origin.value : f.origin.proposedValue;
          }
          e.previous = void 0;
          b.scope.set(c, e);
        }
      }
      copySetInto(a.selfDependent, b.selfDependent);
      a.scope = b.scope;
      b.scope = null;
    } else {
      a.scope = new Map(concat(b.scope, a.scope));
      a.selfDependent = new Set(concat(b.selfDependent, a.selfDependent));
      b.referenceCount--;
    }
    a.previous = null;
  }
  get followingRevision() {
    if (this.$followingRevision !== void 0)
      return this.$followingRevision;
    const a = Array.from(this.topRevision.previousAxis());
    const b = [];
    for (let c = a.length - 1; c > 0; c--)
      b.push([
        a[c],
        a[c - 1]
      ]);
    return this.$followingRevision = new Map(b);
  }
  get activeTransaction() {
    if (this.$activeTransaction)
      return this.$activeTransaction;
    return this.$activeTransaction = this.transactionClass.new({
      'baseRevision': this.baseRevisionTentative || this.baseRevision,
      'graph': this
    });
  }
  branch(a) {
    const b = this.constructor;
    return b.new(Object.assign({}, a, { 'baseRevision': this.baseRevision }));
  }
  propagate(a) {
    return this.commit(a);
  }
  reject(a) {
    this.activeTransaction.reject(RejectEffect.new({ 'reason': a }));
    this.ongoing = Promise.resolve();
    this.$activeTransaction = void 0;
    this.baseRevisionTentative = void 0;
    if (this.baseRevisionStable) {
      this.baseRevision = this.baseRevisionStable;
      this.baseRevisionStable = void 0;
    }
  }
  commit(a) {
    this.isJustCleared = false;
    this.unScheduleAutoCommit();
    this.baseRevisionStable = this.baseRevision;
    const b = this.activeTransaction;
    const c = b.commit(a);
    this.$activeTransaction = void 0;
    const e = this.finalizeCommit(c);
    this.baseRevisionStable = void 0;
    this.isInitialCommit = false;
    return e;
  }
  async propagateAsync(a) {
    return this.commitAsync(a);
  }
  async commitAsync(a) {
    if (this.isCommitting)
      return this.ongoing;
    this.isJustCleared = false;
    this.isCommitting = true;
    this.baseRevisionStable = this.baseRevision;
    let b;
    return this.ongoing = this.ongoing.then(() => {
      return this.doCommitAsync(a);
    }).then(c => {
      b = c;
      return c;
    }).finally(() => {
      this.baseRevisionStable = void 0;
      this.baseRevisionTentative = void 0;
      this.isInitialCommit = false;
      this.isCommitting = false;
    });
  }
  async doCommitAsync(a) {
    this.unScheduleAutoCommit();
    const b = this.activeTransaction;
    const c = await b.commitAsync(a);
    const e = Boolean(b.rejectedWith);
    const f = this.baseRevisionTentative;
    if (!e)
      this.baseRevisionTentative = b.candidate;
    this.$activeTransaction = void 0;
    await this.finalizeCommitAsync(c);
    if (this.isJustCleared)
      return { 'rejectedWith': RejectEffect.new({ 'reason': 'Graph cleared' }) };
    if (b.rejectedWith && !e) {
      this.baseRevisionTentative = f;
      this.$activeTransaction = void 0;
    }
    const g = this.finalizeCommit(c);
    if (b.rejectedWith)
      b.clearRejected();
    if (this.dirty && !b.rejectedWith) {
      await this.doCommitAsync(a);
    }
    return g;
  }
  finalizeCommit(a) {
    const {
      revision: b,
      entries: c,
      transaction: e
    } = a;
    if (!e.rejectedWith) {
      if (b.previous !== this.baseRevision)
        throw new Error('Invalid revisions chain');
      for (const [f, g] of this.eachReachableRevision()) {
        if (g)
          f.reachableCount--;
        f.referenceCount--;
      }
      this.baseRevision = this.topRevision = b;
      for (const [i, j] of c) {
        j.cleanup();
        if (j.isShadow() || !j.hasValue())
          continue;
        const k = this.listeners.get(i);
        if (k)
          k.trigger(j.getValue());
      }
      this.$followingRevision = void 0;
      this.markAndSweep();
    } else {
      if (this.baseRevisionStable)
        this.baseRevision = this.baseRevisionStable;
      this.baseRevisionStable = void 0;
      this.baseRevisionTentative = void 0;
    }
    return { 'rejectedWith': e.rejectedWith };
  }
  async finalizeCommitAsync(a) {
  }
  *onComputationCycleHandler(a) {
    const b = new Error('Computation cycle:\n' + a);
    b.cycle = a;
    switch (this.onComputationCycle) {
    case 'ignore':
      console.log(b.message);
      const {
        requestedEntry: c,
        activeEntry: e
      } = a;
      return e.continueCalculation(c.proposedValue !== void 0 ? c.proposedValue : c.value);
    case 'throw':
      throw b;
    case 'reject':
      this.reject(b);
      break;
    case 'warn':
      warn(b);
      break;
    }
  }
  onComputationCycleHandlerSync(a, b) {
    const c = new Error('Computation cycle:\n' + a);
    c.cycle = a;
    switch (this.onComputationCycle) {
    case 'ignore':
      console.log(c.message);
      const {
        requestedEntry: e,
        activeEntry: f
      } = a;
      return f.continueCalculation(e.proposedValue !== void 0 ? e.proposedValue : e.value);
    case 'throw':
      throw c;
    case 'reject':
      this.reject(c);
      break;
    case 'warn':
      warn(c);
      break;
    }
  }
  scheduleAutoCommit() {
    if (this.autoCommitTimeoutId === null && !this.isCommitting) {
      this.autoCommitTimeoutId = setTimeout(this.autoCommitHandler, 10);
    }
  }
  unScheduleAutoCommit() {
    if (this.autoCommitTimeoutId !== null) {
      clearTimeout(this.autoCommitTimeoutId);
      this.autoCommitTimeoutId = null;
    }
  }
  variable(a) {
    const b = VariableC();
    return this.addIdentifier(b, a === void 0 ? null : a);
  }
  variableNamed(a, b) {
    const c = VariableC({ 'name': a });
    return this.addIdentifier(c, b === void 0 ? null : b);
  }
  identifier(a, b) {
    const c = isGeneratorFunction(a) ? CalculatedValueGenC({
      'calculation': a,
      'context': b
    }) : CalculatedValueSyncC({
      'calculation': a,
      'context': b
    });
    return this.addIdentifier(c);
  }
  identifierNamed(a, b, c) {
    const e = b.constructor.name === 'GeneratorFunction' ? CalculatedValueGenC({
      'name': a,
      'calculation': b,
      'context': c
    }) : CalculatedValueSyncC({
      'name': a,
      'calculation': b,
      'context': c
    });
    return this.addIdentifier(e);
  }
  addIdentifier(a, b, ...c) {
    if (this.isCommitting) {
      if (this.onWriteDuringCommit === 'throw')
        throw new Error('Adding identifier during commit');
      else if (this.onWriteDuringCommit === 'warn')
        warn(new Error('Adding identifier during commit'));
    }
    this.activeTransaction.addIdentifier(a, b, ...c);
    if (this.autoCommit)
      this.scheduleAutoCommit();
    return a;
  }
  removeIdentifier(a) {
    if (this.isCommitting) {
      if (this.onWriteDuringCommit === 'throw')
        throw new Error('Removing identifier during commit');
      else if (this.onWriteDuringCommit === 'warn')
        warn(new Error('Removinfg identifier during commit'));
    }
    this.activeTransaction.removeIdentifier(a);
    this.listeners.delete(a);
    if (this.autoCommit)
      this.scheduleAutoCommit();
  }
  hasIdentifier(a) {
    return this.activeTransaction.hasIdentifier(a);
  }
  write(a, b, ...c) {
    if (this.isCommitting) {
      if (this.onWriteDuringCommit === 'throw')
        throw new Error('Write during commit');
      else if (this.onWriteDuringCommit === 'warn')
        warn(new Error('Write during commit'));
    }
    this.activeTransaction.write(a, b, ...c);
    if (this.autoCommit)
      this.scheduleAutoCommit();
  }
  readPrevious(a) {
    return this.activeTransaction.readPrevious(a);
  }
  readPreviousAsync(a) {
    return this.activeTransaction.readPreviousAsync(a);
  }
  read(a) {
    return this.activeTransaction.read(a);
  }
  readAsync(a) {
    return this.activeTransaction.readAsync(a);
  }
  get(a) {
    return this.activeTransaction.get(a);
  }
  observe(a, b) {
    const c = this.addIdentifier(CalculatedValueGen.new({
      'lazy': false,
      'calculation': a
    }));
    this.addListener(c, b);
    return c;
  }
  observeContext(a, b, c) {
    const e = this.addIdentifier(CalculatedValueGen.new({
      'lazy': false,
      'calculation': a,
      'context': b
    }));
    this.addListener(e, c);
    return e;
  }
  addListener(a, b) {
    let c = this.listeners.get(a);
    if (!c) {
      c = Listener.new();
      this.listeners.set(a, c);
    }
    c.handlers.push(b);
  }
  undo() {
    const a = this.baseRevision;
    const b = a.previous;
    if (!b)
      return false;
    this.baseRevision = b;
    this.$activeTransaction = void 0;
    return true;
  }
  redo() {
    const a = this.baseRevision;
    if (a === this.topRevision)
      return false;
    const b = this.followingRevision.get(a);
    this.baseRevision = b;
    this.$activeTransaction = void 0;
    return true;
  }
  onPropagationProgressNotification(a) {
  }
  [ProposedOrPreviousSymbol](a, b) {
    const c = b.getActiveEntry();
    c.usedProposedOrPrevious = true;
    const e = c.getProposedValue(b);
    if (e !== void 0)
      return e;
    if (!c.previous)
      return void 0;
    const f = c.identifier;
    if (f.lazy) {
      if (c.previous.hasValue())
        return c.previous.getValue();
      if (c.previous.hasProposedValue())
        return c.previous.getProposedValue(b);
      return null;
    }
    return b.readPrevious(c.identifier);
  }
  [RejectSymbol](a, b) {
    this.reject(a.reason);
    return BreakCurrentStackExecution;
  }
  [TransactionSymbol](a, b) {
    return b;
  }
  [OwnQuarkSymbol](a, b) {
    return b.getActiveEntry();
  }
  [OwnIdentifierSymbol](a, b) {
    const c = b.getActiveEntry();
    return c.identifier;
  }
  [WriteSymbol](a, b) {
    const c = b.getActiveEntry();
    if (c.identifier.lazy)
      throw new Error('Lazy identifiers can not use `Write` effect');
    const e = a.identifier.level > c.identifier.level;
    if (!e)
      b.walkContext.startNewEpoch();
    b.write(a.identifier, ...a.proposedArgs);
    return e ? void 0 : BreakCurrentStackExecution;
  }
  [WriteSeveralSymbol](a, b) {
    const c = b.getActiveEntry();
    if (c.identifier.lazy)
      throw new Error('Lazy identifiers can not use `Write` effect');
    let e = true;
    a.writes.forEach(f => {
      if (f.identifier.level <= c.identifier.level && e) {
        b.walkContext.startNewEpoch();
        e = false;
      }
      b.write(f.identifier, ...f.proposedArgs);
    });
    return e ? void 0 : BreakCurrentStackExecution;
  }
  [HasPreviousValueSymbol](a, b) {
    const c = b.getActiveEntry();
    const e = a.identifier;
    b.addEdge(e, c, EdgeTypePast);
    const f = b.entries.get(e);
    return f ? b.readPrevious(e) != null : false;
  }
  [PreviousValueOfSymbol](a, b) {
    const c = b.getActiveEntry();
    const e = a.identifier;
    b.addEdge(e, c, EdgeTypePast);
    return b.readPrevious(e);
  }
  [ProposedValueOfSymbol](a, b) {
    const c = b.getActiveEntry();
    const e = a.identifier;
    b.addEdge(e, c, EdgeTypePast);
    const f = b.entries.get(e);
    const g = f && !f.isShadow() ? f.getProposedValue(b) : void 0;
    return g;
  }
  [HasProposedValueSymbol](a, b) {
    const c = b.getActiveEntry();
    const e = a.identifier;
    b.addEdge(e, c, EdgeTypePast);
    const f = b.entries.get(e);
    return f ? f.hasProposedValue() : false;
  }
  [HasProposedNotPreviousValueSymbol](a, b) {
    const c = b.getActiveEntry();
    const e = a.identifier;
    b.addEdge(e, c, EdgeTypePast);
    const f = b.entries.get(e);
    return f ? f.hasProposedValue() && !f.proposedIsPrevious : false;
  }
  [ProposedOrPreviousValueOfSymbol](a, b) {
    const c = b.getActiveEntry();
    const e = a.identifier;
    b.addEdge(e, c, EdgeTypePast);
    return b.readProposedOrPrevious(e);
  }
  [UnsafeProposedOrPreviousValueOfSymbol](a, b) {
    return b.readProposedOrPrevious(a.identifier);
  }
  [UnsafePreviousValueOfSymbol](a, b) {
    return b.readPrevious(a.identifier);
  }
  [ProposedArgumentsOfSymbol](a, b) {
    const c = b.getActiveEntry();
    const e = a.identifier;
    b.addEdge(e, c, EdgeTypePast);
    const f = b.entries.get(e);
    return f && !f.isShadow() ? f.proposedArguments : void 0;
  }
};
const EntityMeta = class extends Base2 {
  constructor() {
    super(...arguments);
    this.name = void 0;
    this.ownFields = new Map();
    this.schema = void 0;
    this.$skeleton = {};
    this.$allFields = void 0;
  }
  hasField(a) {
    return this.getField(a) !== void 0;
  }
  getField(a) {
    return this.allFields.get(a);
  }
  addField(a) {
    const b = a.name;
    if (!b)
      throw new Error('Field must have a name');
    if (this.ownFields.has(b))
      throw new Error('Field with name [' + b + '] already exists');
    a.entity = this;
    this.ownFields.set(b, a);
    return a;
  }
  forEachParent(a) {
    let b = this;
    while (b) {
      a(b);
      b = b.parentEntity;
    }
  }
  get allFields() {
    if (this.$allFields !== void 0)
      return this.$allFields;
    const a = new Map();
    const b = new Set();
    this.forEachParent(c => {
      c.ownFields.forEach((e, f) => {
        if (!b.has(f)) {
          b.add(f);
          a.set(f, e);
        }
      });
    });
    return this.$allFields = a;
  }
  forEachField(a) {
    this.allFields.forEach(a);
  }
};
let ReadMode;
(function (a) {
  a[a.Current = 0] = 'Current';
  a[a.Previous = 1] = 'Previous';
  a[a.ProposedOrPrevious = 2] = 'ProposedOrPrevious';
  a[a.CurrentOrProposedOrPrevious = 3] = 'CurrentOrProposedOrPrevious';
}(ReadMode || (ReadMode = {})));
const Replica = class extends Mixin([ChronoGraph], a => class b extends a {
  constructor() {
    super(...arguments);
    this.autoCommit = true;
    this.readMode = ReadMode.Current;
  }
  addEntity(c) {
    c.enterGraph(this);
  }
  addEntities(c) {
    c.forEach(e => this.addEntity(e));
  }
  removeEntity(c) {
    c.leaveGraph(this);
  }
  removeEntities(c) {
    c.forEach(e => this.removeEntity(e));
  }
}) {
};
const FieldIdentifier = class extends Mixin([Identifier], a => class b extends a {
  constructor() {
    super(...arguments);
    this.field = void 0;
    this.self = void 0;
    this.DATA = void 0;
  }
  getFromGraph(c) {
    if (c) {
      if (c.readMode === ReadMode.Current)
        return c.get(this);
      if (c.readMode === ReadMode.Previous)
        return c.activeTransaction.readPrevious(this);
      if (c.readMode === ReadMode.ProposedOrPrevious)
        c.activeTransaction.readProposedOrPrevious(this);
      return c.activeTransaction.readCurrentOrProposedOrPrevious(this);
    } else
      return this.DATA;
  }
  readFromGraph(c) {
    if (c)
      return c.read(this);
    else
      return this.DATA;
  }
  writeToGraph(c, e, ...f) {
    if (c)
      c.write(this, e, ...f);
    else
      this.DATA = e;
  }
  leaveGraph(c) {
    const e = c.activeTransaction.getLatestStableEntryFor(this);
    if (e)
      this.DATA = e.getValue();
    super.leaveGraph(c);
  }
  toString() {
    return this.name;
  }
}) {
};
const MinimalFieldIdentifierSync = class extends FieldIdentifier.mix(CalculatedValueSync) {
};
const MinimalFieldIdentifierGen = class extends FieldIdentifier.mix(CalculatedValueGen) {
};
const MinimalFieldVariable = class extends FieldIdentifier.mix(Variable) {
};
const EntityIdentifier = class extends Mixin([Identifier], a => class b extends a {
  constructor() {
    super(...arguments);
    this.entity = void 0;
    this.self = void 0;
  }
  equality() {
    return false;
  }
  toString() {
    return 'Entity identifier [' + this.self + ']';
  }
}) {
};
const MinimalEntityIdentifier = class extends EntityIdentifier.mix(CalculatedValueGen) {
};
const Field2 = class extends Meta {
  constructor() {
    super(...arguments);
    this.persistent = true;
  }
  getIdentifierClass(a) {
    if (this.identifierCls)
      return this.identifierCls;
    if (!a)
      return MinimalFieldVariable;
    return isGeneratorFunction(a) ? MinimalFieldIdentifierGen : MinimalFieldIdentifierSync;
  }
};
const isEntityMarker = Symbol('isEntity');
const Entity = class extends Mixin([], a => {
  class b extends a {
    [isEntityMarker]() {
    }
    get $entity() {
      return createEntityOnPrototype(this.constructor.prototype);
    }
    get $() {
      const c = {};
      this.$entity.forEachField((e, f) => {
        c[f] = this.createFieldIdentifier(e);
      });
      if (DEBUG) {
        const e = new Proxy(c, {
          'get'(f, g, i) {
            if (!f[g])
              debug(new Error('Attempt to read a missing field ' + String(g) + ' on ' + f));
            f[g].SOURCE_POINT = SourceLinePoint.fromThisCall();
            return f[g];
          }
        });
        return defineProperty8(this, '$', e);
      } else {
        return defineProperty8(this, '$', c);
      }
    }
    get $$() {
      return defineProperty8(this, '$$', MinimalEntityIdentifier.new({
        'name': this.$entityName,
        'entity': this.$entity,
        'calculation': this.calculateSelf,
        'context': this,
        'self': this
      }));
    }
    get $entityName() {
      return this.constructor.name || this.$entity.name;
    }
    *calculateSelf() {
      return this;
    }
    createFieldIdentifier(c) {
      const e = c.name;
      const f = this.$entity;
      const g = this.constructor;
      const i = f.$skeleton;
      if (!i[e])
        i[e] = g.getIdentifierTemplateClass(this, c);
      const j = new i[e]();
      j.context = this;
      j.self = this;
      j.name = this.$$.name + '.$.' + c.name;
      return j;
    }
    forEachFieldIdentifier(c) {
      this.$entity.forEachField((e, f) => c(this.$[f], f, e));
    }
    enterGraph(c) {
      if (this.graph)
        throw new Error('Already entered replica');
      this.graph = c;
      c.addIdentifier(this.$$);
      this.$entity.forEachField((e, f) => {
        const g = this.$[f];
        c.addIdentifier(g, g.DATA);
        g.DATA = void 0;
      });
    }
    leaveGraph(c) {
      const e = this.graph;
      const f = c || e;
      if (!f)
        return;
      this.$entity.forEachField((g, i) => f.removeIdentifier(this.$[i]));
      f.removeIdentifier(this.$$);
      if (f === e)
        this.graph = void 0;
    }
    propagate(c) {
      return this.commit(c);
    }
    commit(c) {
      const e = this.graph;
      if (!e)
        return CommitZero;
      return e.commit(c);
    }
    async propagateAsync() {
      return this.commitAsync();
    }
    async commitAsync(c) {
      const e = this.graph;
      if (!e)
        return Promise.resolve(CommitZero);
      return e.commitAsync(c);
    }
    static get $entity() {
      return ensureEntityOnPrototype(this.prototype);
    }
    static getIdentifierTemplateClass(c, e) {
      const f = e.name;
      const g = {
        'name': c.$$.name + '.$.' + f,
        'field': e
      };
      if (e.hasOwnProperty('sync'))
        g.sync = e.sync;
      if (e.hasOwnProperty('lazy'))
        g.lazy = e.lazy;
      if (e.hasOwnProperty('equality'))
        g.equality = e.equality;
      const i = c.$calculations && c[c.$calculations[f]];
      if (i)
        g.calculation = i;
      const j = c.$writes && c[c.$writes[f]];
      if (j)
        g.write = j;
      const k = c.$buildProposed && c[c.$buildProposed[f]];
      if (k) {
        g.buildProposedValue = k;
        g.proposedValueIsBuilt = true;
      }
      const l = e.getIdentifierClass(i).new(g);
      const n = function () {
      };
      n.prototype = l;
      return n;
    }
    run(c, ...e) {
      const f = k => {
        if (k instanceof Identifier)
          return this.graph.read(k);
        throw new Error('Helper methods can not yield effects during computation');
      };
      const g = this.graph.activeTransaction;
      const i = g.activeStack;
      g.activeStack = [];
      const j = runGeneratorSyncWithEffect(f, this[c], e, this);
      g.activeStack = i;
      return j;
    }
    static createPropertyAccessorsFor(c) {
      const e = c;
      const f = this.prototype;
      Object.defineProperty(f, e, {
        'get': function () {
          return this.$[e].getFromGraph(this.graph);
        },
        'set': function (g) {
          this.$[e].writeToGraph(this.graph, g);
        }
      });
    }
    static createMethodAccessorsFor(c) {
      const e = c;
      const f = this.prototype;
      const g = 'get' + uppercaseFirst(e);
      const i = 'set' + uppercaseFirst(e);
      const j = 'put' + uppercaseFirst(e);
      if (!(g in f)) {
        f[g] = function () {
          return this.$[e].getFromGraph(this.graph);
        };
      }
      if (!(i in f)) {
        f[i] = function (k, ...l) {
          this.$[e].writeToGraph(this.graph, k, ...l);
          return this.graph ? this.graph.autoCommitMode === 'sync' ? this.graph.commit() : this.graph.commitAsync() : Promise.resolve(CommitZero);
        };
      }
      if (!(j in f)) {
        f[j] = function (k, ...l) {
          this.$[e].writeToGraph(this.graph, k, ...l);
        };
      }
    }
  }
  return b;
}) {
};
var createEntityOnPrototype = a => {
  const b = Object.getPrototypeOf(a);
  return defineProperty8(a, '$entity', EntityMeta.new({
    'parentEntity': b.hasOwnProperty(isEntityMarker) ? null : b.$entity,
    'name': a.constructor.name
  }));
};
var ensureEntityOnPrototype = a => {
  if (!a.hasOwnProperty('$entity'))
    createEntityOnPrototype(a);
  return a.$entity;
};
const generic_field = (a, b = Field2) => {
  return function (c, e) {
    const f = ensureEntityOnPrototype(c);
    const g = f.addField(b.new(Object.assign(a || {}, { 'name': e })));
    const i = c.constructor;
    i.createPropertyAccessorsFor(e);
    i.createMethodAccessorsFor(e);
  };
};
const field = generic_field;
const calculate = function (a) {
  return function (b, c, e) {
    ensureEntityOnPrototype(b);
    let f;
    if (!b.$calculations) {
      f = b.$calculations = {};
    } else {
      if (!b.hasOwnProperty('$calculations')) {
        f = b.$calculations = Object.create(b.$calculations);
      } else
        f = b.$calculations;
    }
    f[a] = c;
  };
};
const write = function (a) {
  return function (b, c, e) {
    ensureEntityOnPrototype(b);
    let f;
    if (!b.$writes) {
      f = b.$writes = {};
    } else {
      if (!b.hasOwnProperty('$writes')) {
        f = b.$writes = Object.create(b.$writes);
      } else
        f = b.$writes;
    }
    f[a] = c;
  };
};
const build_proposed = function (a) {
  return function (b, c, e) {
    ensureEntityOnPrototype(b);
    let f;
    if (!b.$buildProposed) {
      f = b.$buildProposed = {};
    } else {
      if (!b.hasOwnProperty('$buildProposed')) {
        f = b.$buildProposed = Object.create(b.$buildProposed);
      } else
        f = b.$buildProposed;
    }
    f[a] = c;
  };
};
const __decorate4 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const ReferenceField = class extends Mixin([Field2], a => class b extends a {
  constructor() {
    super(...arguments);
    this.identifierCls = MinimalReferenceIdentifier;
  }
}) {
};
const reference = (a, b = ReferenceField) => generic_field(a, b);
const ReferenceIdentifier = class extends Mixin([FieldIdentifier], a => {
  class b extends a {
    constructor() {
      super(...arguments);
      this.field = void 0;
      this.proposedValueIsBuilt = true;
    }
    hasBucket() {
      return Boolean(this.field.bucket);
    }
    getBucket(c) {
      return c.$[this.field.bucket];
    }
    buildProposedValue(c, e, f) {
      const g = e.proposedValue;
      if (g === null)
        return null;
      const i = isInstanceOf(g, Entity) ? g : c.resolve(g);
      if (i && c.hasBucket()) {
        c.getBucket(i).addToBucket(f, c.self);
      }
      return i;
    }
    resolve(c) {
      const e = this.field.resolver;
      return e ? e.call(this.self, c) : null;
    }
    enterGraph(c) {
      if (this.hasBucket()) {
        const e = c.activeTransaction.readProposedOrPrevious(this);
        if (e instanceof Entity) {
          this.getBucket(e).addToBucket(c.activeTransaction, this.self);
        }
      }
      super.enterGraph(c);
    }
    leaveGraph(c) {
      if (this.hasBucket()) {
        const e = c.activeTransaction.readProposedOrPrevious(this);
        if (e instanceof Entity) {
          this.getBucket(e).removeFromBucket(c.activeTransaction, this.self);
        }
      }
      super.leaveGraph(c);
    }
    write(c, e, f, g, ...i) {
      const j = f || e.acquireQuarkIfExists(c);
      if (c.hasBucket()) {
        if (j) {
          const k = j.getValue();
          if (k instanceof Entity) {
            c.getBucket(k).removeFromBucket(e, c.self);
          }
        } else if (e.baseRevision.hasIdentifier(c)) {
          const l = e.readPrevious(c);
          if (l instanceof Entity) {
            c.getBucket(l).removeFromBucket(e, c.self);
          }
        }
      }
      super.write(c, e, f, g);
    }
  }
  __decorate4([prototypeValue(Levels.DependsOnlyOnUserInput)], b.prototype, 'level', void 0);
  __decorate4([prototypeValue(QuarkSync)], b.prototype, 'quarkClass', void 0);
  return b;
}) {
};
var MinimalReferenceIdentifier = class extends ReferenceIdentifier.mix(FieldIdentifier.mix(CalculatedValueSync)) {
};
const __decorate5 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const ReferenceBucketField = class extends Mixin([Field2], a => class b extends a {
  constructor() {
    super(...arguments);
    this.persistent = false;
    this.identifierCls = MinimalReferenceBucketIdentifier;
  }
}) {
};
const bucket = (a, b = ReferenceBucketField) => generic_field(a, b);
let BucketMutationType;
(function (a) {
  a.Add = 'Add';
  a.Remove = 'Remove';
}(BucketMutationType || (BucketMutationType = {})));
const ReferenceBucketQuark = class extends Mixin([Quark], a => class b extends a {
  constructor() {
    super(...arguments);
    this.mutations = [];
    this.previousValue = void 0;
  }
  hasProposedValueInner() {
    return this.mutations.length > 0;
  }
}) {
};
const MinimalReferenceBucketQuark = ReferenceBucketQuark.mix(QuarkSync);
const ReferenceBucketIdentifier = class extends Mixin([FieldIdentifier], a => {
  class b extends a {
    constructor() {
      super(...arguments);
      this.proposedValueIsBuilt = true;
    }
    addToBucket(c, e) {
      const f = c.getWriteTarget(this);
      f.mutations.push({
        'type': BucketMutationType.Add,
        'entity': e
      });
      const g = c.baseRevision;
      if (!f.previousValue && g.hasIdentifier(this))
        f.previousValue = c.readPrevious(this);
    }
    removeFromBucket(c, e) {
      const f = c.entries.get(this);
      if (f && f.getValue() === TombStone)
        return;
      const g = c.getWriteTarget(this);
      g.mutations.push({
        'type': BucketMutationType.Remove,
        'entity': e
      });
      const i = c.baseRevision;
      if (!g.previousValue && i.hasIdentifier(this))
        g.previousValue = c.readPrevious(this);
    }
    buildProposedValue(c, e, f) {
      const g = e;
      const j = new Set(g.previousValue);
      for (let k = 0; k < g.mutations.length; k++) {
        const {
          type: l,
          entity: n
        } = g.mutations[k];
        if (l === BucketMutationType.Remove) {
          j.delete(n);
        } else if (l === BucketMutationType.Add) {
          j.add(n);
        }
      }
      return j;
    }
    leaveGraph(c) {
      super.leaveGraph(c);
      this.DATA = void 0;
    }
  }
  __decorate5([prototypeValue(Levels.DependsOnlyOnDependsOnlyOnUserInput)], b.prototype, 'level', void 0);
  __decorate5([prototypeValue(MinimalReferenceBucketQuark)], b.prototype, 'quarkClass', void 0);
  return b;
}) {
};
var MinimalReferenceBucketIdentifier = class extends ReferenceBucketIdentifier.mix(FieldIdentifier.mix(CalculatedValueSync)) {
};
const Schema = class extends Base2 {
  constructor() {
    super(...arguments);
    this.entities = new Map();
  }
  hasEntity(a) {
    return this.entities.has(a);
  }
  getEntity(a) {
    return this.entities.get(a);
  }
  addEntity(a) {
    const b = a.name;
    if (!b)
      throw new Error('Entity must have a name');
    if (this.hasEntity(b))
      throw new Error('Entity with name [' + String(b) + '] already exists');
    a.schema = this;
    this.entities.set(b, a);
    return a;
  }
  getEntityDecorator() {
    return a => {
      const b = entityDecoratorBody(a);
      this.addEntity(b);
      return a;
    };
  }
};
var entityDecoratorBody = a => {
  const b = a.name;
  if (!b)
    throw new Error('Can\'t add entity - the target class has no name');
  return ensureEntityOnPrototype(a.prototype);
};
const entity = () => {
  return a => {
    entityDecoratorBody(a);
    return a;
  };
};
const __decorate6 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const isSerializableEqual = function (a, b) {
  return a === b || ((b == null ? void 0 : b.isBase) ? this.serialize(b) : b) === ((a == null ? void 0 : a.isBase) ? this.serialize(a) : a);
};
const ModelField = class extends Field2 {
  constructor() {
    super(...arguments);
    this.modelFieldConfig = {};
  }
  getIdentifierClass(a) {
    if (this.identifierCls)
      return this.identifierCls;
    if (!a)
      return MinimalChronoModelFieldVariable;
    return isGeneratorFunction(a) ? MinimalChronoModelFieldIdentifierGen : MinimalChronoModelFieldIdentifierSync;
  }
};
const ModelReferenceField = class extends ReferenceField.mix(ModelField) {
  constructor() {
    super(...arguments);
    this.identifierCls = ChronoModelReferenceFieldIdentifier;
  }
};
const ModelBucketField = class extends ReferenceBucketField.mix(Field2) {
  constructor() {
    super(...arguments);
    this.identifierCls = ChronoModelReferenceBucketFieldIdentifier;
  }
};
const IsChronoModelSymbol = Symbol('IsChronoModelSymbol');
const ChronoModelFieldIdentifier = class extends Mixin([FieldIdentifier], a => {
  const b = a.prototype;
  class c extends a {
    [IsChronoModelSymbol]() {
    }
    getFromGraph(e) {
      let f, g;
      if (e) {
        if (e.readMode === ReadMode.CurrentOrProposedOrPrevious) {
          const i = e.isCommitting && this.level > ((g = (f = e.activeTransaction.getActiveEntry()) == null ? void 0 : f.level) != null ? g : Number.MAX_SAFE_INTEGER);
          if (this.sync && !this.context.project.isDelayingCalculation && !i)
            return e.get(this);
          else
            return e.activeTransaction.readCurrentOrProposedOrPrevious(this);
        }
        return b.getFromGraph.call(this, e);
      } else
        return this.DATA;
    }
    writeToGraph(e, f, ...g) {
      if (e) {
        b.writeToGraph.call(this, e, f, ...g);
      } else {
        this.DATA = f;
        if (!this.self.inSetting && !this.self.isConstructing && !this.field.ignore) {
          this.self.set(this.field.name, f, false, false, true);
        }
      }
    }
    write(e, f, g, i, ...j) {
      i = e.convert(i);
      b.write.call(this, e, f, g, i, ...j);
    }
    convert(e) {
      const f = this.field;
      const g = this.self.getFieldDefinition(f.name);
      if (g == null ? void 0 : g.convert) {
        e = g.convert(e, this.context.data, this.context);
      } else if (f.converter) {
        e = f.converter(e, f);
      }
      return e;
    }
    equality(e, f) {
      if (e instanceof Date && f instanceof Date)
        return e.getTime() === f.getTime();
      return e === f;
    }
  }
  __decorate6([prototypeValue(false)], c.prototype, 'sync', void 0);
  return c;
}) {
};
const ChronoModelReferenceFieldQuark = class extends Mixin([QuarkSync], a => {
  const b = a.prototype;
  class c extends a {
    setValue(e) {
      b.setValue.call(this, e);
      if (e !== TombStone)
        this.identifier.DATA = e;
    }
  }
  return c;
}) {
};
var ChronoModelReferenceFieldIdentifier = class extends ReferenceIdentifier.mix(ChronoModelFieldIdentifier.mix(MinimalFieldIdentifierSync)) {
  buildProposedValue(a, b, c) {
    const e = b;
    const f = e.proposedValue;
    if (f === null || f === void 0) {
      c.candidate.failedResolutionReferences.delete(e.identifier);
      return null;
    }
    if (isInstanceOf(f, Entity) && f.graph) {
      if (a.hasBucket())
        a.getBucket(f).addToBucket(c, a.self);
      c.candidate.failedResolutionReferences.delete(e.identifier);
      if (a.field.name === 'parentEvent') {
        a.self.updateParentEvent(f, a.DATA);
      }
      return f;
    }
    const g = a.resolve(f);
    if (isInstanceOf(g, Entity) && g.graph) {
      if (a.hasBucket())
        a.getBucket(g).addToBucket(c, a.self);
      c.candidate.failedResolutionReferences.delete(e.identifier);
      return g;
    } else {
      c.candidate.failedResolutionReferences.set(e.identifier, f);
      return null;
    }
  }
};
__decorate6([prototypeValue(true)], ChronoModelReferenceFieldIdentifier.prototype, 'sync', void 0);
__decorate6([prototypeValue(ChronoModelReferenceFieldQuark)], ChronoModelReferenceFieldIdentifier.prototype, 'quarkClass', void 0);
var ChronoModelReferenceBucketFieldIdentifier = class extends ReferenceBucketIdentifier.mix(ChronoModelFieldIdentifier.mix(MinimalFieldIdentifierSync)) {
};
__decorate6([prototypeValue(true)], ChronoModelReferenceBucketFieldIdentifier.prototype, 'sync', void 0);
var MinimalChronoModelFieldIdentifierSync = class extends ChronoModelFieldIdentifier.mix(MinimalFieldIdentifierSync) {
};
var MinimalChronoModelFieldIdentifierGen = class extends ChronoModelFieldIdentifier.mix(MinimalFieldIdentifierGen) {
};
var MinimalChronoModelFieldVariable = class extends ChronoModelFieldIdentifier.mix(MinimalFieldVariable) {
};
const model_field = function (a = {}, b = {}, c = ModelField) {
  return function (e, f) {
    const g = generic_field({
      'modelFieldConfig': a,
      ...b
    }, c);
    g(e, f);
    injectStaticFieldsProperty(e.constructor);
  };
};
var injectStaticFieldsProperty = a => {
  if (!a.hasOwnProperty('fields')) {
    Object.defineProperty(a, 'fields', {
      'get': function () {
        return getDecoratedModelFields(this);
      }
    });
  }
};
var getDecoratedModelFields = a => {
  const b = a.prototype;
  const c = [];
  if (b.hasOwnProperty('$entity')) {
    b.$entity.ownFields.forEach(e => {
      if (e instanceof ModelField) {
        const f = e.modelFieldConfig || {};
        if (!f.convert && e.converter && e.converter !== dateConverter) {
          f.convert = e.converter;
        }
        c.push(Object.assign(f, {
          '$chrono': e,
          'name': e.name
        }));
      }
    });
  }
  return c;
};
var dateConverter = (a, b) => {
  let c, e;
  if (a === null) {
    return null;
  }
  if (!(a instanceof Date)) {
    a = DateHelper.parse(a, ((c = b.modelFieldConfig) == null ? void 0 : c.format) || ((e = b.modelFieldConfig) == null ? void 0 : e.dateFormat) || DateHelper.defaultParseFormat);
  }
  return a || void 0;
};
const ChronoModelMixin = class extends Mixin([
  Entity,
  Model
], a => {
  const b = a.prototype;
  class c extends a {
    get isEntity() {
      return true;
    }
    construct(e, ...f) {
      this.constructor.exposeProperties();
      this.originalData = e = e || {};
      b.construct.call(this, e, ...f);
      for (const g in this.originalData) {
        if (this.$[g] && !this.getFieldDefinition(g)) {
          this[g] = e[g];
        }
      }
    }
    *userProvidedValue() {
      return yield ProposedOrPrevious;
    }
    copy(e = null, f = null) {
      const g = b.copy.call(this, e, f);
      const {creatingOccurrence: i} = f != null ? f : {};
      if (ObjectHelper.isObject(f) && !f.skipFieldIdentifiers || !ObjectHelper.isObject(f)) {
        this.forEachFieldIdentifier((j, k, l) => {
          let n;
          if (!l.lazy && ((n = this.getFieldDefinition(k)) == null ? void 0 : n.type) !== 'store' && (!i && !(j instanceof ChronoModelReferenceBucketFieldIdentifier) || i && (j instanceof ChronoModelReferenceBucketFieldIdentifier || j instanceof ChronoModelReferenceFieldIdentifier))) {
            g[k] = this[k];
          }
        });
      }
      return g;
    }
    applyValue(e, f, g, i, j) {
      let k;
      const l = this.$entity.getField((j == null ? void 0 : j.name) || f);
      if (l)
        e = true;
      if (i)
        e = false;
      b.applyValue.call(this, e, e ? (k = j == null ? void 0 : j.name) != null ? k : f : f, g, i, j);
    }
    get isInActiveTransaction() {
      let e;
      const f = (e = this.graph) == null ? void 0 : e.activeTransaction;
      return Boolean(f == null ? void 0 : f.getLatestEntryFor(this.$$));
    }
    get data() {
      return this._data;
    }
    set data(e) {
      this._data = e;
      const {
        fields: f,
        $: g,
        graph: j,
        generation: k
      } = this;
      for (let l = 0; l < f.length; l++) {
        const {
          name: n,
          dataSource: o,
          complexMapping: p
        } = f[l];
        const q = g[n];
        if (q) {
          const r = p ? ObjectHelper.getPath(e, o) : e[o];
          if ((p || o in e) && (k != null || r !== void 0)) {
            q.writeToGraph(j, r);
          }
        }
      }
    }
    get $entityName() {
      const e = this.constructor.name || this.$entity.name;
      const f = this.id;
      return '' + e + (f != null ? '-' + String(f) : '');
    }
    unlink() {
      let e;
      if (this.graph) {
        const {activeTransaction: f} = this.graph;
        const {$: g} = this;
        const j = Object.keys(g);
        for (let k = 0; k < j.length; k++) {
          const l = j[k];
          const n = g[l];
          const o = f.getLatestStableOrProposedEntryFor(n);
          if (o) {
            let p = o.getValue();
            if (p === void 0)
              p = o.proposedValue;
            if (p !== void 0) {
              n.DATA = n.field instanceof ModelReferenceField ? (e = p == null ? void 0 : p.id) != null ? e : p : p;
            }
          }
        }
        this.graph = null;
      }
    }
  }
  return c;
}) {
};
const locale5 = {
  'localeName': 'En',
  'localeDesc': 'English (US)',
  'localeCode': 'en-US',
  'RemoveDependencyCycleEffectResolution': { 'descriptionTpl': 'Remove dependency' },
  'DeactivateDependencyCycleEffectResolution': { 'descriptionTpl': 'Deactivate dependency' },
  'CycleEffectDescription': { 'descriptionTpl': 'A cycle has been found, formed by: {0}' },
  'EmptyCalendarEffectDescription': {
    'descriptionTpl': '"{2}" task cannot be performed. "{0}" calendar does not provide any working time intervals.',
    'forwardDescriptionTpl': '"{2}" task cannot be performed. "{0}" calendar has no working time after {1}.',
    'backwardDescriptionTpl': '"{2}" task cannot be performed. "{0}" calendar has no working time before {1}.',
    'noIntersectionDescriptionTpl': '"{2}" task cannot be performed. Its "{0}" calendar and its resource calendars have no common working time.'
  },
  'Use24hrsEmptyCalendarEffectResolution': { 'descriptionTpl': 'Use 24-hour calendar with non-working Saturdays and Sundays.' },
  'Use8hrsEmptyCalendarEffectResolution': { 'descriptionTpl': 'Use 8-hour calendar (08:00-12:00, 13:00-17:00) with non-working Saturdays and Sundays.' },
  'IgnoreProjectConstraintResolution': { 'descriptionTpl': 'Ignore the project border and proceed with the change.' },
  'ProjectConstraintConflictEffectDescription': {
    'startDescriptionTpl': 'You moved "{0}" task to start on {1}. This is before the project start date {2}.',
    'endDescriptionTpl': 'You moved "{0}" task to finish on {1}. This is after the project end date {2}.'
  },
  'HonorProjectConstraintResolution': { 'descriptionTpl': 'Adjust the task to honor the project border.' },
  'IgnoreResourceCalendarEmptyCalendarEffectResolution': { 'descriptionTpl': 'Ignore resource calendars and schedule the task using its own calendar only.' },
  'ConflictEffectDescription': { 'descriptionTpl': 'A scheduling conflict has been found: {0} is conflicting with {1}' },
  'ConstraintIntervalDescription': { 'dateFormat': 'LLL' },
  'ProjectConstraintIntervalDescription': {
    'startDateDescriptionTpl': 'Project start date {0}',
    'endDateDescriptionTpl': 'Project end date {0}'
  },
  'DependencyType': {
    'long': [
      'Start-to-Start',
      'Start-to-Finish',
      'Finish-to-Start',
      'Finish-to-Finish'
    ]
  },
  'ManuallyScheduledParentConstraintIntervalDescription': {
    'startDescriptionTpl': 'Manually scheduled "{2}" forces its children to start no earlier than {0}',
    'endDescriptionTpl': 'Manually scheduled "{2}" forces its children to finish no later than {1}'
  },
  'DisableManuallyScheduledConflictResolution': { 'descriptionTpl': 'Disable manual scheduling for "{0}"' },
  'DependencyConstraintIntervalDescription': { 'descriptionTpl': 'Dependency ({2}) from "{3}" to "{4}"' },
  'RemoveDependencyResolution': { 'descriptionTpl': 'Remove dependency from "{1}" to "{2}"' },
  'DeactivateDependencyResolution': { 'descriptionTpl': 'Deactivate dependency from "{1}" to "{2}"' },
  'DateConstraintIntervalDescription': {
    'startDateDescriptionTpl': 'Task "{2}" {3} {0} constraint',
    'endDateDescriptionTpl': 'Task "{2}" {3} {1} constraint',
    'constraintTypeTpl': {
      'startnoearlierthan': 'Start-No-Earlier-Than',
      'finishnoearlierthan': 'Finish-No-Earlier-Than',
      'muststarton': 'Must-Start-On',
      'mustfinishon': 'Must-Finish-On',
      'startnolaterthan': 'Start-No-Later-Than',
      'finishnolaterthan': 'Finish-No-Later-Than'
    }
  },
  'RemoveDateConstraintConflictResolution': { 'descriptionTpl': 'Remove "{1}" constraint of task "{0}"' }
};
const En_default4 = LocaleHelper.publishLocale(locale5);
const DateInterval = class extends Base2 {
  initialize(...a) {
    super.initialize(...a);
    if (!this.startDate)
      this.startDate = MIN_DATE2;
    if (!this.endDate)
      this.endDate = MAX_DATE2;
  }
  equalTo(a) {
    return this.startDate.getTime() === a.startDate.getTime() && this.endDate.getTime() === a.endDate.getTime();
  }
  isInfinite() {
    return this.startDate.getTime() === MIN_DATE2.getTime() && this.endDate.getTime() === MAX_DATE2.getTime();
  }
  startDateIsFinite() {
    return !this.isIntervalEmpty() && this.startDate.getTime() !== MIN_DATE2.getTime();
  }
  endDateIsFinite() {
    return !this.isIntervalEmpty() && this.endDate.getTime() !== MAX_DATE2.getTime();
  }
  containsDate(a, b = EdgeInclusion.Left) {
    return b === EdgeInclusion.Left && (a >= this.startDate && a < this.endDate) || b === EdgeInclusion.Right && (a > this.startDate && a <= this.endDate);
  }
  isIntervalEmpty() {
    return this.startDate > this.endDate;
  }
  intersect(a) {
    const b = a.startDate;
    const c = a.endDate;
    const e = this.startDate;
    const f = this.endDate;
    if (f < b || e > c) {
      return EMPTY_INTERVAL;
    }
    const g = new Date(Math.max(e.getTime(), b.getTime()));
    const i = new Date(Math.min(f.getTime(), c.getTime()));
    return this.constructor.new({
      'startDate': g,
      'endDate': i
    });
  }
  intersectMut(a, b = false) {
    let c;
    const e = a.startDate;
    const f = a.endDate;
    const g = this.startDate;
    const i = this.endDate;
    if (b) {
      if (!this.intersectionOf)
        this.intersectionOf = new Set();
      if (((c = a.intersectionOf) == null ? void 0 : c.size) > 0) {
        a.intersectionOf.forEach(this.intersectionOf.add, this.intersectionOf);
        this.intersectedAsEmpty = a.intersectedAsEmpty;
      } else {
        this.intersectionOf.add(a);
      }
    }
    if (!this.isIntervalEmpty()) {
      if (i < e || g > f) {
        this.startDate = MAX_DATE2;
        this.endDate = MIN_DATE2;
        if (b) {
          this.intersectedAsEmpty = a;
        }
        return this;
      }
      this.startDate = new Date(Math.max(g.getTime(), e.getTime()));
      this.endDate = new Date(Math.min(i.getTime(), f.getTime()));
    }
    return this;
  }
  getCopyProperties(a) {
    return a;
  }
  copyWith(a) {
    const b = this.getCopyProperties(a);
    return this.constructor.new(b);
  }
};
var EMPTY_INTERVAL = DateInterval.new({
  'startDate': MAX_DATE2,
  'endDate': MIN_DATE2
});
const intersectIntervals = (a, b = false) => {
  return a.reduce((c, e) => c.intersectMut(e, b), DateInterval.new());
};
const __decorate7 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
let EffectResolutionResult;
(function (a) {
  a.Cancel = 'Cancel';
  a.Resume = 'Resume';
}(EffectResolutionResult || (EffectResolutionResult = {})));
const SchedulingIssueEffectResolution = class extends Base {
  getDescription() {
    throw new Error('Abstract method');
  }
  resolve(...a) {
    throw new Error('Abstract method');
  }
};
const SchedulingIssueEffect = class extends Effect {
  getResolutions() {
    return this._resolutions;
  }
  getDescriptionBuilderClass() {
    return this._descriptionBuilderClass;
  }
  setDescriptionBuilderClass(a) {
    this._descriptionBuilderClass = a;
  }
  getDescription() {
    return this.getDescriptionBuilderClass().getDescription(this);
  }
};
__decorate7([prototypeValue('schedulingIssueEffect')], SchedulingIssueEffect.prototype, 'type', void 0);
__decorate7([prototypeValue(false)], SchedulingIssueEffect.prototype, 'sync', void 0);
const __decorate8 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const ConflictSymbol = Symbol('ConflictSymbol');
const ConflictEffectDescription = class extends Localizable_default(Base) {
  static get $name() {
    return 'ConflictEffectDescription';
  }
  static getDescription(a) {
    return format(this.L('L{descriptionTpl}'), a.intervals[0].getDescription(), a.intervals[1].getDescription());
  }
};
const ConflictEffect = class extends SchedulingIssueEffect {
  constructor() {
    super(...arguments);
    this.handler = ConflictSymbol;
  }
  initialize(a) {
    super.initialize(a);
    this.intervals = this.filterConflictingIntervals(this.intervals);
  }
  getResolutions() {
    if (!this._resolutions) {
      this._resolutions = [].concat(...this.intervals.map(a => a.getResolutions()));
    }
    return this._resolutions;
  }
  filterConflictingIntervals(a) {
    const b = [];
    const c = [...a].filter(f => !f.isInfinite());
    const e = c.find(f => f.isAffectedByTransaction());
    if (e) {
      const f = [
        e,
        ...c.filter(j => j !== e)
      ];
      const g = intersectIntervals(f, true);
      const i = g.intersectedAsEmpty;
      b.push(i, e);
    } else {
      b.push(intersectIntervals(c, true).intersectedAsEmpty, intersectIntervals(c.reverse(), true).intersectedAsEmpty);
    }
    return b;
  }
};
__decorate8([prototypeValue('schedulingConflict')], ConflictEffect.prototype, 'type', void 0);
__decorate8([prototypeValue(ConflictEffectDescription)], ConflictEffect.prototype, '_descriptionBuilderClass', void 0);
const ConflictResolution = class extends SchedulingIssueEffectResolution {
  resolve() {
    throw new Error('Abstract method');
  }
};
const ConstraintIntervalDescription = class extends Localizable_default(Base) {
  static get $name() {
    return 'ConstraintIntervalDescription';
  }
  static getDescription(a) {
    return format(this.L('L{descriptionTpl}'), ...this.getDescriptionParameters(a));
  }
  static getDescriptionParameters(a) {
    return [
      DateHelper.format(a.startDate, this.L('L{dateFormat}')),
      DateHelper.format(a.endDate, this.L('L{dateFormat}'))
    ];
  }
};
const ConstraintInterval = class extends DateInterval {
  constructor() {
    super(...arguments);
    this.owner = void 0;
    this.reflectionOf = void 0;
    this.side = void 0;
    this.resolutions = void 0;
  }
  get isConstraintInterval() {
    return true;
  }
  getDescription() {
    return this.descriptionBuilderClass.getDescription(this);
  }
  getResolutions() {
    return [];
  }
  isAffectedByTransaction(a) {
    return false;
  }
  getCopyProperties(a) {
    const {
      owner: b,
      reflectionOf: c,
      side: e
    } = this;
    return Object.assign({
      'owner': b,
      'reflectionOf': c,
      'side': e
    }, a);
  }
};
__decorate8([prototypeValue(ConstraintIntervalDescription)], ConstraintInterval.prototype, 'descriptionBuilderClass', void 0);
const ChronoPartOfProjectGenericMixin = class extends Mixin([AbstractPartOfProjectGenericMixin], a => {
  const b = a.prototype;
  class c extends a {
    getGraph() {
      const e = this.getProject();
      return e == null ? void 0 : e.getGraph();
    }
    getEventById(e) {
      let f;
      return (f = this.getEventStore()) == null ? void 0 : f.getById(e);
    }
    getDependencyById(e) {
      let f;
      return (f = this.getDependencyStore()) == null ? void 0 : f.getById(e);
    }
    getResourceById(e) {
      let f;
      return (f = this.getResourceStore()) == null ? void 0 : f.getById(e);
    }
    getAssignmentById(e) {
      let f;
      return (f = this.getAssignmentStore()) == null ? void 0 : f.getById(e);
    }
    getCalendarById(e) {
      let f;
      return (f = this.getCalendarManagerStore()) == null ? void 0 : f.getById(e);
    }
  }
  return c;
}) {
};
const ChronoStoreMixin = class extends Mixin([Store], a => class b extends a {
}) {
};
const ChronoPartOfProjectStoreMixin = class extends Mixin([
  AbstractPartOfProjectStoreMixin,
  ChronoPartOfProjectGenericMixin,
  ChronoStoreMixin
], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.removalOrder = 0;
    }
    setStoreData(e) {
      let f, g;
      (f = this.project) == null ? void 0 : f.repopulateStore(this);
      b.setStoreData.call(this, e);
      if ((g = this.project) == null ? void 0 : g.delayedCalculationPromise) {
        this.project.invalidateTemporaryIndices();
      }
    }
    register(e) {
      let f, g;
      b.register.call(this, e);
      !e.isRoot && !((f = this.project) == null ? void 0 : f.graph) && ((g = this.project) == null ? void 0 : g.scheduleDelayedCalculation());
    }
    onModelChange(e, f, g, i, j, k) {
      let l;
      if (!k && !(this.syncDataOnLoad && this.isLoadingData) && Object.keys(g).some(n => n !== 'intervals' && e.$entity.getField(n)) && !((l = this.project) == null ? void 0 : l.applyingSyncResponse)) {
        i = true;
      }
      super.onModelChange(e, f, g, i, j, k);
    }
  }
  return c;
}) {
};
const ChronoPartOfProjectModelMixin = class extends Mixin([
  AbstractPartOfProjectModelMixin,
  ChronoPartOfProjectGenericMixin,
  ChronoModelMixin
], a => {
  const b = a.prototype;
  class c extends a {
    joinProject() {
      let e;
      if (!((e = this.project) == null ? void 0 : e.delayEnteringReplica)) {
        if (this.graph && this.graph != this.getGraph()) {
          this.graph = null;
        }
        this.getGraph().addEntity(this);
      }
    }
    leaveProject(e = false) {
      b.leaveProject.call(this, e);
      const f = this.getGraph();
      f == null ? void 0 : f.removeEntity(this);
      this.graph = null;
    }
    getProject() {
      return b.getProject.call(this);
    }
    calculateProject() {
      let e;
      const f = (e = this.stores) == null ? void 0 : e.find(g => isInstanceOf(g, ChronoPartOfProjectStoreMixin) && !!g.getProject());
      return f == null ? void 0 : f.getProject();
    }
    get graph() {
      let e;
      return ((e = this.project) == null ? void 0 : e.delayEnteringReplica) ? null : this._graph;
    }
    set graph(e) {
      this._graph = e;
    }
  }
  return c;
}) {
};
const __decorate9 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const EmptyCalendarSymbol = Symbol('EmptyCalendarSymbol');
let CalendarAvailabilityIssue;
(function (a) {
  a.Empty = 'Empty';
  a.NoForward = 'NoForward';
  a.NoBackward = 'NoBackward';
}(CalendarAvailabilityIssue || (CalendarAvailabilityIssue = {})));
const BaseCalendarMixin = class extends Mixin([
  AbstractCalendarMixin,
  ChronoPartOfProjectModelMixin
], a => {
  class b extends a {
    constructor() {
      super(...arguments);
      this.version = 1;
    }
    leaveProject() {
      let c;
      (c = this.project) == null ? void 0 : c.clearCombinationsWith(this);
      super.leaveProject();
    }
  }
  __decorate9([model_field({}, { 'persistent': false })], b.prototype, 'version', void 0);
  __decorate9([model_field({
      'type': 'boolean',
      'defaultValue': true
    })], b.prototype, 'unspecifiedTimeIsWorking', void 0);
  __decorate9([model_field()], b.prototype, 'intervals', void 0);
  return b;
}) {
};
const EmptyCalendarEffectDescription = class extends Localizable_default(Base) {
  static get $name() {
    return 'EmptyCalendarEffectDescription';
  }
  static get dateFormat() {
    return 'LLL';
  }
  static getDescription(a) {
    let b = 'L{descriptionTpl}';
    let c;
    const e = a.getInvalidCalendar();
    if (e) {
      c = e[0];
      if (e[1] !== CalendarAvailabilityIssue.Empty) {
        b = a.isForward ? 'L{forwardDescriptionTpl}' : 'L{backwardDescriptionTpl}';
      }
    } else {
      b = 'L{noIntersectionDescriptionTpl}';
      c = a.calendars[0];
    }
    return format(this.L(b), c.name || c.id, DateHelper.format(a.date, this.dateFormat), a.event.name);
  }
};
const EmptyCalendarEffect = class extends SchedulingIssueEffect {
  constructor() {
    super(...arguments);
    this.handler = EmptyCalendarSymbol;
  }
  getResolutions() {
    const a = this.getCalendar();
    return this._resolutions || (this._resolutions = [
      Use24hrsEmptyCalendarEffectResolution.new({ 'calendar': a }),
      Use8hrsEmptyCalendarEffectResolution.new({ 'calendar': a })
    ]);
  }
  getCalendar() {
    const a = this.getInvalidCalendar();
    return a ? a[0] : this.calendars[0];
  }
  getInvalidCalendar() {
    const {
      calendars: a,
      date: b,
      isForward: c
    } = this;
    for (const e of a) {
      const f = e.skipNonWorkingTime(b, false);
      const g = e.skipNonWorkingTime(b, true);
      if (c) {
        if (!(g instanceof Date)) {
          return [
            e,
            f instanceof Date ? CalendarAvailabilityIssue.NoForward : CalendarAvailabilityIssue.Empty
          ];
        }
      } else {
        if (!(f instanceof Date)) {
          return [
            e,
            g instanceof Date ? CalendarAvailabilityIssue.NoBackward : CalendarAvailabilityIssue.Empty
          ];
        }
      }
    }
  }
};
__decorate9([prototypeValue('emptyCalendar')], EmptyCalendarEffect.prototype, 'type', void 0);
__decorate9([prototypeValue(EmptyCalendarEffectDescription)], EmptyCalendarEffect.prototype, '_descriptionBuilderClass', void 0);
const BaseEmptyCalendarEffectResolution = class extends Localizable_default(SchedulingIssueEffectResolution) {
  static get $name() {
    return 'BaseEmptyCalendarEffectResolution';
  }
  static get configurable() {
    return { 'calendarData': { 'intervals': [{ 'isWorking': true }] } };
  }
  getDescription() {
    const {calendar: a} = this;
    return format(this.L('L{descriptionTpl}'), a.name || a.id);
  }
  fixCalendarData(a) {
    let b;
    a.clearIntervals(true);
    Object.assign(a, this.calendarData);
    if ((b = a.intervals) == null ? void 0 : b.length) {
      a.addIntervals(a.intervals);
    }
  }
  resolve() {
    const {calendar: a} = this;
    this.fixCalendarData(a);
  }
};
var Use24hrsEmptyCalendarEffectResolution = class extends BaseEmptyCalendarEffectResolution {
  static get $name() {
    return 'Use24hrsEmptyCalendarEffectResolution';
  }
  static get configurable() {
    return {
      'calendarData': {
        'unspecifiedTimeIsWorking': false,
        'intervals': [{
            'recurrentStartDate': 'on Mon at 0:00',
            'recurrentEndDate': 'on Sat at 0:00',
            'isWorking': true
          }]
      }
    };
  }
};
var Use8hrsEmptyCalendarEffectResolution = class extends BaseEmptyCalendarEffectResolution {
  static get $name() {
    return 'Use8hrsEmptyCalendarEffectResolution';
  }
  static get configurable() {
    return {
      'calendarData': {
        'unspecifiedTimeIsWorking': false,
        'intervals': [
          {
            'recurrentStartDate': 'every weekday at 08:00',
            'recurrentEndDate': 'every weekday at 12:00',
            'isWorking': true
          },
          {
            'recurrentStartDate': 'every weekday at 13:00',
            'recurrentEndDate': 'every weekday at 17:00',
            'isWorking': true
          }
        ]
      }
    };
  }
};
const __decorate10 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const CycleSymbol = Symbol('CycleSymbol');
const EngineRevision = class extends Revision {
  constructor() {
    super(...arguments);
    this.failedResolutionReferences = new Map();
  }
};
const EngineTransaction = class extends Transaction2 {
  constructor() {
    super(...arguments);
    this.candidateClass = EngineRevision;
  }
  initialize(a) {
    let b;
    if ((b = a.graph.project) == null ? void 0 : b.delayCalculation) {
      a.startProgressNotificationsAfterMs = 0;
      a.emitProgressNotificationsEveryMs = 100;
    }
    super.initialize(a);
    this.candidate.failedResolutionReferences = new Map(this.baseRevision.failedResolutionReferences);
  }
  writeFailedResolutionInfo(a) {
    a.forEach((b, c) => {
      if (c.context.graph) {
        this.write(c, b);
      }
    });
  }
  addIdentifier(a, b, ...c) {
    const e = this.candidate.failedResolutionReferences;
    if (e.size > 0) {
      this.writeFailedResolutionInfo(e);
      e.clear();
    }
    return super.addIdentifier(a, b, ...c);
  }
};
const EngineReplica = class extends Mixin([Replica], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.baseRevision = EngineRevision.new();
      this.transactionClass = EngineTransaction;
      this.autoCommitMode = 'async';
      this.onComputationCycle = 'effect';
      this.cycleEffectClass = CycleEffect;
      this.silenceInitialCommit = true;
      this.ignoreInitialCommitComputationCycles = false;
      this.transactionChainEntries = new Set();
    }
    get dirty() {
      const e = this.activeTransaction;
      return e.entries.size > 0 && (e.hasVariableEntry || e.hasEntryWithProposedValue);
    }
    onPropagationProgressNotification(e) {
      let f, g;
      if (this.enableProgressNotifications && this.project)
        (g = (f = this.project).trigger) == null ? void 0 : g.call(f, 'progress', e);
    }
    async commitAsync(e) {
      if (!this.project || this.project.isDestroyed)
        return;
      if (this.activeTransaction.entries.size === 0 && !this.project.hasChangesInStores && !this.isInitialCommit) {
        return { 'rejectedWith': null };
      }
      this.project.trigger('beforeCommit');
      if (this.isInitialCommit && this.ignoreInitialCommitComputationCycles) {
        this._onComputationCycle = this._onComputationCycle || this.onComputationCycle;
        this.onComputationCycle = 'ignore';
      }
      const f = this.project.beforeCommitAsync();
      if (f)
        return f;
      return b.commitAsync.call(this, e);
    }
    get isInitialCommit() {
      return this.project.isInitialCommit || super.isInitialCommit;
    }
    set isInitialCommit(e) {
      super.isInitialCommit = e;
    }
    write(e, f, ...g) {
      let i, j, k;
      const l = (i = e.field) == null ? void 0 : i.name;
      const n = e.self;
      if (l && n) {
        const o = (j = n.beforeChronoFieldSet) == null ? void 0 : j.call(n, l, f);
        b.write.call(this, e, f, ...g);
        (k = n.afterChronoFieldSet) == null ? void 0 : k.call(n, l, f, o);
      } else {
        b.write.call(this, e, f, ...g);
      }
    }
    async finalizeCommitAsync(e) {
      let f, g;
      const {project: i} = this;
      if (!i || i.isDestroyed)
        return;
      i.trigger('finalizeCommit');
      const {
        entries: j,
        transaction: k
      } = e;
      const l = new Set();
      if (globalThis.DEBUG)
        console.timeEnd('Time to visible');
      const {
        isInitialCommit: n,
        silenceInitialCommit: o
      } = this;
      if (!j.size && !i.hasLoadedDataToCommit) {
        i.trigger('commitFinalized', {
          'isInitialCommit': n,
          'transactionResult': e
        });
        return;
      }
      const p = i.finalizeCommitHook(n) === true;
      if (p) {
        for (const r of j.values()) {
          this.transactionChainEntries.add(r);
        }
        return;
      }
      const q = n && o;
      if (n) {
        i.isInitialCommitPerformed = true;
        if (this.ignoreInitialCommitComputationCycles)
          this.onComputationCycle = this._onComputationCycle;
      }
      i.isWritingData = true;
      i.hasLoadedDataToCommit = false;
      if (this.enableProgressNotifications) {
        i.trigger('progress', {
          'total': j.size,
          'remaining': 0,
          'phase': 'finalizing'
        });
      }
      if (k.rejectedWith) {
        i.trigger('commitRejected', {
          'transactionResult': e,
          'isInitialCommit': n,
          'silenceCommit': q
        });
      }
      i.trigger('refresh', {
        'isInitialCommit': n,
        'isCalculated': true
      });
      await delay2(0);
      if (!i.isDestroyed) {
        if (!e.transaction.rejectedWith) {
          (f = i.suspendChangeTracking) == null ? void 0 : f.call(i);
          if (globalThis.DEBUG)
            console.time('Finalize propagation');
          const u = new Set();
          const v = this.transactionChainEntries.size ? this.transactionChainEntries : j.values();
          for (const z of v) {
            const A = z.identifier;
            const B = z.getValue();
            const {field: C} = A;
            if (z.isShadow() || !A[IsChronoModelSymbol] || B === TombStone || C instanceof ModelBucketField || C.ignore)
              continue;
            const E = A.self;
            const F = E.firstStore;
            if (!u.has(E)) {
              E.beginBatch(true);
              u.add(E);
            }
            if ((F == null ? void 0 : F.autoCommit) && !l.has(F)) {
              F.suspendAutoCommit();
              l.add(F);
            }
            E.meta.batchChanges[C.name] = B;
          }
          let w = false;
          for (const G of u) {
            if (!G.triggerBeforeUpdate({ ...G.meta.batchChanges })) {
              w = true;
              break;
            }
          }
          if (w) {
            for (const H of u) {
              H.cancelBatch();
            }
            e.transaction.reject();
            i.trigger('commitRejected', {
              'transactionResult': e,
              'isInitialCommit': n,
              'silenceCommit': q
            });
            i.trigger('refresh', {
              'isInitialCommit': n,
              'isCalculated': true
            });
          } else {
            for (const I of u) {
              I.ignoreBag = q || i.ignoreRecordChanges;
              I.generation++;
              I.endBatch(q, true, true);
              I.ignoreBag = false;
            }
          }
          i.ignoreRecordChanges = false;
          if (globalThis.DEBUG)
            console.timeEnd('Finalize propagation');
          i.isWritingData = false;
          if (!w) {
            i.trigger('dataReady', {
              'records': u,
              'isInitialCommit': n
            });
          }
          (g = i.resumeChangeTracking) == null ? void 0 : g.call(i, q);
          l.forEach(J => J.resumeAutoCommit());
          if (q) {
            i.eventStore.acceptChanges();
            i.dependencyStore.acceptChanges();
            i.resourceStore.acceptChanges();
            i.assignmentStore.acceptChanges();
            i.calendarManagerStore.acceptChanges();
            i.acceptChanges();
          }
        } else {
          i.isWritingData = false;
        }
        this.transactionChainEntries.clear();
        i.trigger('commitFinalized', {
          'isInitialCommit': n,
          'transactionResult': e
        });
      }
    }
    *onComputationCycleHandler(e) {
      if (this.onComputationCycle === 'effect') {
        const f = this.project.cycleEffectClass.new({ 'cycle': e });
        if ((yield f) === EffectResolutionResult.Cancel) {
          yield Reject(f);
        }
      } else {
        return yield* super.onComputationCycleHandler(e);
      }
    }
    onComputationCycleHandlerSync(e, f) {
      if (this.onComputationCycle === 'effect') {
        const g = this.project.cycleEffectClass.new({ 'cycle': e });
        throw g;
      } else {
        return super.onComputationCycleHandlerSync(e, f);
      }
    }
    async [CycleSymbol](e, f) {
      return this.project.onCycleSchedulingIssue(e, f);
    }
    async [EmptyCalendarSymbol](e, f) {
      f.walkContext.startNewEpoch();
      return this.project.onEmptyCalendarSchedulingIssue(e, f);
    }
    async [ConflictSymbol](e, f) {
      f.walkContext.startNewEpoch();
      return this.project.onConflictSchedulingIssue(e, f);
    }
    [RejectSymbol](e, f) {
      return super[RejectSymbol](e, f);
    }
  }
  return c;
}) {
};
const RemoveDependencyCycleEffectResolution = class extends Localizable_default(SchedulingIssueEffectResolution) {
  static get $name() {
    return 'RemoveDependencyCycleEffectResolution';
  }
  getDescription() {
    return this.L('L{descriptionTpl}');
  }
  resolve(a) {
    a.remove();
  }
};
const CycleEffectDescription = class extends Localizable_default(Base) {
  static get $name() {
    return 'CycleEffectDescription';
  }
  static getDescription(a) {
    return format(this.L('L{descriptionTpl}'), this.getShortDescription(a));
  }
  static getShortDescription(a) {
    const b = a.getEvents().slice();
    b.push(b[0]);
    return '"' + b.map(c => c.name || '#' + c.id).join('" -> "') + '"';
  }
};
var CycleEffect = class extends SchedulingIssueEffect {
  constructor() {
    super(...arguments);
    this.handler = CycleSymbol;
  }
  getEvents() {
    if (!this._events) {
      const a = new Set();
      this.cycle.cycle.forEach(({context: b}) => {
        let c;
        return a.add((c = b.event) != null ? c : b);
      });
      this._events = [...a];
    }
    return this._events;
  }
  matchDependencyBySourceAndTargetEvent(a, b, c) {
    return a.fromEvent === b && a.toEvent === c;
  }
  getDependencyForSourceAndTargetEvents(a, b) {
    const c = this.getEvents();
    const e = c[0].project;
    const f = e.getDependencyStore();
    return f.find(g => this.matchDependencyBySourceAndTargetEvent(g, a, b));
  }
  getDependencies() {
    if (!this._dependencies) {
      const a = new Set();
      const b = this.getEvents();
      const c = b.length;
      let e = b[0], f;
      if (c === 1) {
        if (f = this.getDependencyForSourceAndTargetEvents(e, e)) {
          a.add(f);
        }
      } else {
        for (const g of b) {
          for (const i of b) {
            if (f = this.getDependencyForSourceAndTargetEvents(g, i)) {
              a.add(f);
            }
            if (f = this.getDependencyForSourceAndTargetEvents(i, g)) {
              a.add(f);
            }
          }
        }
      }
      this._dependencies = [...a];
    }
    return this._dependencies;
  }
  getResolutions() {
    if (!this._resolutions) {
      this._resolutions = [this.removeDependencyCycleEffectResolutionClass.new()];
    }
    return this._resolutions;
  }
};
__decorate10([prototypeValue('cycle')], CycleEffect.prototype, 'type', void 0);
__decorate10([prototypeValue(CycleEffectDescription)], CycleEffect.prototype, '_descriptionBuilderClass', void 0);
__decorate10([prototypeValue(RemoveDependencyCycleEffectResolution)], CycleEffect.prototype, 'removeDependencyCycleEffectResolutionClass', void 0);
const __decorate11 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const BaseAssignmentMixin = class extends Mixin([ChronoPartOfProjectModelMixin], a => {
  const b = a.prototype;
  class c extends a {
  }
  __decorate11([generic_field({
      'bucket': 'assigned',
      'resolver': function (e) {
        return this.getEventById(e);
      },
      'modelFieldConfig': {
        'serialize': e => e == null ? void 0 : e.id,
        'isEqual': isSerializableEqual,
        'persist': false
      }
    }, ModelReferenceField)], c.prototype, 'event', void 0);
  __decorate11([generic_field({
      'bucket': 'assigned',
      'resolver': function (e) {
        return this.getResourceById(e);
      },
      'modelFieldConfig': {
        'serialize': e => e == null ? void 0 : e.id,
        'isEqual': isSerializableEqual,
        'persist': false
      }
    }, ModelReferenceField)], c.prototype, 'resource', void 0);
  injectStaticFieldsProperty(c);
  return c;
}) {
};
const ChronoAssignmentStoreMixin = class extends Mixin([
  AbstractAssignmentStoreMixin,
  ChronoPartOfProjectStoreMixin
], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.removalOrder = 100;
    }
    static get defaultConfig() {
      return { 'modelClass': BaseAssignmentMixin };
    }
    set data(e) {
      this.allAssignmentsForRemoval = true;
      super.data = e;
      this.allAssignmentsForRemoval = false;
    }
  }
  return c;
}) {
};
const ChronoCalendarManagerStoreMixin = class extends Mixin([
  AbstractCalendarManagerStoreMixin,
  ChronoPartOfProjectStoreMixin
], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.removalOrder = 500;
    }
    static get defaultConfig() {
      return {
        'tree': true,
        'modelClass': BaseCalendarMixin
      };
    }
  }
  return c;
}) {
};
const ChronoDependencyStoreMixin = class extends Mixin([
  AbstractDependencyStoreMixin,
  ChronoPartOfProjectStoreMixin
], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.removalOrder = 200;
    }
    set data(e) {
      this.allDependenciesForRemoval = true;
      super.data = e;
      this.allDependenciesForRemoval = false;
    }
  }
  return c;
}) {
};
let Instruction;
(function (a) {
  a.KeepDuration = 'KeepDuration';
  a.KeepStartDate = 'KeepStartDate';
  a.KeepEndDate = 'KeepEndDate';
}(Instruction || (Instruction = {})));
const StartDateVar = Symbol('StartDate');
const EndDateVar = Symbol('EndDate');
const DurationVar = Symbol('Duration');
const startDateFormula = Formula.new({
  'output': StartDateVar,
  'inputs': new Set([
    DurationVar,
    EndDateVar
  ])
});
const endDateFormula = Formula.new({
  'output': EndDateVar,
  'inputs': new Set([
    DurationVar,
    StartDateVar
  ])
});
const durationFormula = Formula.new({
  'output': DurationVar,
  'inputs': new Set([
    StartDateVar,
    EndDateVar
  ])
});
const SEDGraphDescription = CycleDescription.new({
  'variables': new Set([
    StartDateVar,
    EndDateVar,
    DurationVar
  ]),
  'formulas': new Set([
    startDateFormula,
    endDateFormula,
    durationFormula
  ])
});
const SEDForwardCycleResolutionContext = CycleResolution.new({
  'description': SEDGraphDescription,
  'defaultResolutionFormulas': new Set([endDateFormula])
});
const SEDBackwardCycleResolutionContext = CycleResolution.new({
  'description': SEDGraphDescription,
  'defaultResolutionFormulas': new Set([startDateFormula])
});
const SEDDispatcher = class extends CycleResolutionInputChrono {
  addInstruction(a) {
    if (a === Instruction.KeepStartDate)
      this.addKeepIfPossibleFlag(StartDateVar);
    if (a === Instruction.KeepEndDate)
      this.addKeepIfPossibleFlag(EndDateVar);
    if (a === Instruction.KeepDuration)
      this.addKeepIfPossibleFlag(DurationVar);
  }
};
const SEDDispatcherIdentifier = class extends FieldIdentifier.mix(CalculatedValueGen) {
  equality(a, b) {
    const c = a.resolution;
    const e = b.resolution;
    const f = c.get(StartDateVar) === e.get(StartDateVar) && c.get(EndDateVar) === e.get(EndDateVar) && c.get(DurationVar) === e.get(DurationVar) || c.get(StartDateVar) === CalculateProposed && c.get(EndDateVar) === CalculateProposed && c.get(DurationVar) === CalculateProposed;
    return f;
  }
};
const __decorate12 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const HasCalendarMixin = class extends Mixin([ChronoPartOfProjectModelMixin], a => {
  class b extends a {
    writeCalendar(c, e, f, g) {
      const i = this.getCalendarManagerStore();
      const j = g;
      if (g && i && g instanceof BaseCalendarMixin && !i.includes(j)) {
        i.add(g);
      }
      c.constructor.prototype.write.call(this, c, e, f, g);
    }
    resolveCalendar(c) {
      let e;
      return (e = this.getCalendarManagerStore()) == null ? void 0 : e.getById(c);
    }
    *calculateEffectiveDataOrVisualCalendar(c) {
      let e = yield this.$.calendar;
      if (!e) {
        const f = this.getProject();
        e = yield f.$.effectiveCalendar;
      }
      yield e.$.version;
      return e;
    }
    *calculateEffectiveCalendar() {
      return yield* this.calculateEffectiveDataOrVisualCalendar('data');
    }
    *calculateEffectiveVisualCalendar() {
      return yield* this.calculateEffectiveDataOrVisualCalendar('visual');
    }
    shouldRecordFieldChange(c, e, f) {
      if (!super.shouldRecordFieldChange(c, e, f)) {
        return false;
      }
      const {project: g} = this;
      if (c === 'calendar' && g) {
        const {calendarManagerStore: i} = g;
        return i.oldIdMap[e] !== i.getById(f);
      }
      return true;
    }
  }
  __decorate12([field({ 'equality': () => false })], b.prototype, 'effectiveCalendar', void 0);
  __decorate12([field({ 'equality': () => false })], b.prototype, 'effectiveVisualCalendar', void 0);
  __decorate12([generic_field({
      'modelFieldConfig': {
        'persist': true,
        'serialize': c => c === void 0 ? void 0 : (c == null ? void 0 : c.id) || null,
        'isEqual': isSerializableEqual
      },
      'resolver': function (c) {
        return this.resolveCalendar(c);
      },
      'sync': true
    }, ModelReferenceField)], b.prototype, 'calendar', void 0);
  __decorate12([write('calendar')], b.prototype, 'writeCalendar', null);
  __decorate12([calculate('effectiveCalendar')], b.prototype, 'calculateEffectiveCalendar', null);
  __decorate12([calculate('effectiveVisualCalendar')], b.prototype, 'calculateEffectiveVisualCalendar', null);
  injectStaticFieldsProperty(b);
  return b;
}) {
};
const CanCombineCalendarsMixin = class extends Mixin([], a => {
  class b extends a {
    constructor() {
      super(...arguments);
      this.combinedCalendarsCache = new Map();
      this.combinationsByCalendar = new Map();
    }
    combineCalendars(c) {
      const e = stripDuplicates(c);
      if (e.length === 0)
        throw new Error('No calendars to combine');
      e.sort((k, l) => {
        if (k.internalId < l.internalId)
          return -1;
        else
          return 1;
      });
      const f = e.map(k => k.internalId + '/').join('');
      const g = e.map(k => k.version + '/').join('');
      const i = this.combinedCalendarsCache.get(f);
      let j;
      if (i && i.versionsHash === g)
        j = i.cache;
      else {
        j = new CalendarCacheMultiple({ 'calendarCaches': e.map(k => k.calendarCache) });
        this.combinedCalendarsCache.set(f, {
          'versionsHash': g,
          'cache': j
        });
        e.forEach(k => {
          let l = this.combinationsByCalendar.get(k);
          if (!l) {
            l = new Set();
            this.combinationsByCalendar.set(k, l);
          }
          l.add(f);
        });
      }
      return j;
    }
    clearCombinationsWith(c) {
      const e = this.combinationsByCalendar.get(c);
      if (e) {
        for (const f of e) {
          for (const g of this.combinedCalendarsCache.get(f).cache.calendarCaches) {
            const i = this.combinationsByCalendar.get(g.calendar);
            i.delete(f);
          }
          this.combinedCalendarsCache.delete(f);
        }
        this.combinationsByCalendar.delete(c);
      }
    }
  }
  return b;
}) {
};
const __decorate13 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const BaseEventMixin = class extends Mixin([HasCalendarMixin], a => {
  const b = a.prototype;
  class c extends a {
    *isManuallyScheduled() {
      return yield this.$.manuallyScheduled;
    }
    *calculateDispatcher(e) {
      const f = yield ProposedOrPrevious;
      const g = yield* this.prepareDispatcher(e);
      const i = yield ProposedArgumentsOf(this.$.startDate);
      const j = i ? i[0] ? Instruction.KeepDuration : Instruction.KeepEndDate : void 0;
      if (j)
        g.addInstruction(j);
      const k = yield ProposedArgumentsOf(this.$.endDate);
      const l = k ? k[0] ? Instruction.KeepDuration : Instruction.KeepStartDate : void 0;
      if (l)
        g.addInstruction(l);
      const n = yield this.$.effectiveDirection;
      const o = yield ProposedArgumentsOf(this.$.duration);
      let p;
      if (o) {
        switch (o[0]) {
        case true:
          p = Instruction.KeepStartDate;
          break;
        case false:
          p = Instruction.KeepEndDate;
          break;
        }
      }
      if (!p && g.hasProposedValue(DurationVar)) {
        p = n.direction === Direction.Forward || n.direction === Direction.None ? Instruction.KeepStartDate : Instruction.KeepEndDate;
      }
      if (p)
        g.addInstruction(p);
      return g;
    }
    *prepareDispatcher(e) {
      const f = this.dispatcherClass(e);
      const g = f.new({ 'context': this.cycleResolutionContext(e) });
      g.collectInfo(e, this.$.startDate, StartDateVar);
      g.collectInfo(e, this.$.endDate, EndDateVar);
      g.collectInfo(e, this.$.duration, DurationVar);
      return g;
    }
    cycleResolutionContext(e) {
      const f = e(this.$.effectiveDirection);
      return f.direction === Direction.Forward || f.direction === Direction.None ? SEDForwardCycleResolutionContext : SEDBackwardCycleResolutionContext;
    }
    dispatcherClass(e) {
      return SEDDispatcher;
    }
    buildProposedDispatcher(e, f, g) {
      const i = this.dispatcherClass(g.onEffectSync).new({ 'context': this.cycleResolutionContext(g.onEffectSync) });
      i.addPreviousValueFlag(StartDateVar);
      i.addPreviousValueFlag(EndDateVar);
      i.addPreviousValueFlag(DurationVar);
      return i;
    }
    *skipNonWorkingTime(e, f) {
      const g = yield this.$.effectiveCalendar;
      if (!e)
        return null;
      const i = g.skipNonWorkingTime(e, f);
      if (i instanceof Date) {
        return i;
      } else {
        const j = EmptyCalendarEffect.new({
          'calendars': [g],
          'event': this,
          'date': e,
          'isForward': f
        });
        if ((yield j) === EffectResolutionResult.Cancel) {
          yield Reject(j);
        } else {
          return null;
        }
      }
    }
    *skipWorkingTime(e, f, g, i) {
      const j = yield this.$.durationUnit;
      if (i && i !== j) {
        g = yield* this.getProject().$convertDuration(g, i, j);
      }
      return yield* this.calculateProjectedXDateWithDuration(e, f, g);
    }
    setStartDate(e, f = true) {
      const {
        graph: g,
        project: i
      } = this;
      if (g) {
        g.write(this.$.startDate, e, f);
        return g.commitAsync();
      } else {
        this.$.startDate.DATA = e;
        return i == null ? void 0 : i.delayedCalculationPromise;
      }
    }
    writeStartDate(e, f, g, i, j = true) {
      if (!f.baseRevision.hasIdentifier(e) && i == null)
        return;
      if (!this.getProject().isStmRestoring) {
        this.$.unscheduled.write(this.$.unscheduled, f, void 0, i == null);
      }
      e.constructor.prototype.write.call(this, e, f, g, i, j);
    }
    *calculateStartDate() {
      const e = yield this.$.dispatcher;
      const f = e.resolution.get(StartDateVar);
      if (f === CalculateProposed) {
        return yield* this.calculateStartDateProposed();
      } else if (f === startDateFormula.formulaId) {
        return yield* this.calculateStartDatePure();
      } else {
        throw new Error('Unknown formula for `startDate`');
      }
    }
    *calculateStartDatePure() {
      return yield* this.calculateProjectedXDateWithDuration(yield this.$.endDate, false, yield this.$.duration);
    }
    *calculateStartDateProposed() {
      const e = this.getProject();
      const f = yield ProposedOrPrevious;
      const g = yield* this.isManuallyScheduled();
      return !g || e.skipNonWorkingTimeWhenSchedulingManually ? yield* this.skipNonWorkingTime(f, true) : f;
    }
    *calculateProjectedXDateWithDuration(e, f, g) {
      const i = yield this.$.durationUnit;
      const j = yield this.$.effectiveCalendar;
      const k = this.getProject();
      if (!e || isNotNumber(g))
        return null;
      f = f === void 0 ? true : f;
      if (f) {
        return j.calculateEndDate(e, yield* k.$convertDuration(g, i, TimeUnit.Millisecond));
      } else {
        return j.calculateStartDate(e, yield* k.$convertDuration(g, i, TimeUnit.Millisecond));
      }
    }
    setEndDate(e, f = false) {
      const {
        graph: g,
        project: i
      } = this;
      if (g) {
        g.write(this.$.endDate, e, f);
        return g.commitAsync();
      } else {
        this.$.endDate.DATA = e;
        return i == null ? void 0 : i.delayedCalculationPromise;
      }
    }
    writeEndDate(e, f, g, i, j = false) {
      if (!f.baseRevision.hasIdentifier(e) && i == null)
        return;
      if (!this.getProject().isStmRestoring) {
        this.$.unscheduled.write(this.$.unscheduled, f, void 0, i == null);
      }
      e.constructor.prototype.write.call(this, e, f, g, i, j);
    }
    *calculateEndDate() {
      const e = yield this.$.dispatcher;
      const f = e.resolution.get(EndDateVar);
      if (f === CalculateProposed) {
        return yield* this.calculateEndDateProposed();
      } else if (f === endDateFormula.formulaId) {
        return yield* this.calculateEndDatePure();
      } else {
        throw new Error('Unknown formula for `endDate`');
      }
    }
    *calculateEndDatePure() {
      return yield* this.calculateProjectedXDateWithDuration(yield this.$.startDate, true, yield this.$.duration);
    }
    *calculateEndDateProposed() {
      const e = this.getProject();
      const f = yield ProposedOrPrevious;
      const g = yield* this.isManuallyScheduled();
      return !g || e.skipNonWorkingTimeWhenSchedulingManually ? yield* this.skipNonWorkingTime(f, false) : f;
    }
    getDuration(e) {
      const f = this.duration;
      return e !== void 0 ? this.getProject().convertDuration(f, this.durationUnit, e) : f;
    }
    setDuration(e, f, g) {
      const {
        graph: i,
        project: j
      } = this;
      if (i) {
        if (e !== void 0) {
          i.write(this.$.duration, e, f, g);
          return i.commitAsync();
        }
      } else {
        const k = { 'duration': e };
        this.$.duration.DATA = e;
        if (f != null)
          k.durationUnit = this.$.durationUnit.DATA = f;
        this.set(k);
        return j == null ? void 0 : j.delayedCalculationPromise;
      }
    }
    setDurationUnit(e) {
      throw new Error('Use `setDuration` instead');
    }
    writeDuration(e, f, g, i, j, k = void 0) {
      if (i < 0)
        i = 0;
      if (!f.baseRevision.hasIdentifier(e) && i == null)
        return;
      if (!this.getProject().isStmRestoring) {
        this.$.unscheduled.write(this.$.unscheduled, f, void 0, i == null);
      }
      e.constructor.prototype.write.call(this, e, f, g, i, k);
      if (j != null)
        f.write(this.$.durationUnit, j);
    }
    *calculateDuration() {
      const e = yield this.$.dispatcher;
      const f = e.resolution.get(DurationVar);
      if (f === CalculateProposed) {
        return yield* this.calculateDurationProposed();
      } else if (f === durationFormula.formulaId) {
        return yield* this.calculateDurationPure();
      } else {
        throw new Error('Unknown formula for `duration`');
      }
    }
    *calculateDurationPure() {
      const e = yield this.$.startDate;
      const f = yield this.$.endDate;
      if (!e || !f)
        return null;
      if (e > f) {
        yield Write(this.$.duration, 0, null);
      } else {
        return yield* this.calculateProjectedDuration(e, f);
      }
    }
    *calculateDurationProposed() {
      return yield ProposedOrPrevious;
    }
    *calculateProjectedDuration(e, f, g) {
      if (!e || !f)
        return null;
      if (!g)
        g = yield this.$.durationUnit;
      const i = yield this.$.effectiveCalendar;
      const j = this.getProject();
      return yield* j.$convertDuration(i.calculateDurationMs(e, f), TimeUnit.Millisecond, g);
    }
    *calculateEffectiveDuration() {
      const e = yield this.$.dispatcher;
      let f;
      const g = e.resolution.get(DurationVar);
      if (g === CalculateProposed) {
        f = yield this.$.duration;
      } else if (g === durationFormula.formulaId) {
        f = yield* this.calculateProjectedDuration(yield ProposedOrPreviousValueOf(this.$.startDate), yield ProposedOrPreviousValueOf(this.$.endDate));
      }
      return f;
    }
    *calculateEffectiveDirection() {
      const e = yield this.$.direction;
      return {
        'kind': 'own',
        'direction': e || Direction.Forward
      };
    }
    *calculateStartDateDirection() {
      return yield this.$.effectiveDirection;
    }
    *calculateEndDateDirection() {
      return yield this.$.effectiveDirection;
    }
    *calculateEffectiveDataOrVisualCalendar(e) {
      if (e === 'visual') {
        return yield* super.calculateEffectiveDataOrVisualCalendar('visual');
      } else {
        const f = yield* this.isManuallyScheduled();
        const g = this.getProject();
        return f && !g.skipNonWorkingTimeInDurationWhenSchedulingManually ? g.defaultCalendar : yield* super.calculateEffectiveDataOrVisualCalendar('data');
      }
    }
  }
  __decorate13([model_field({
      'type': 'date',
      'bypassEqualityOnSyncDataset': true
    }, { 'converter': dateConverter })], c.prototype, 'startDate', void 0);
  __decorate13([model_field({
      'type': 'date',
      'bypassEqualityOnSyncDataset': true
    }, { 'converter': dateConverter })], c.prototype, 'endDate', void 0);
  __decorate13([model_field({
      'type': 'number',
      'bypassEqualityOnSyncDataset': true,
      'allowNull': true
    })], c.prototype, 'duration', void 0);
  __decorate13([model_field({
      'type': 'string',
      'defaultValue': TimeUnit.Day
    }, { 'converter': e => DateHelper.normalizeUnit(e) || TimeUnit.Day })], c.prototype, 'durationUnit', void 0);
  __decorate13([model_field({ 'type': 'string' }, { 'sync': true })], c.prototype, 'direction', void 0);
  __decorate13([field({
      'sync': true,
      'equality': isEqualEffectiveDirection
    })], c.prototype, 'startDateDirection', void 0);
  __decorate13([field({
      'sync': true,
      'equality': isEqualEffectiveDirection
    })], c.prototype, 'endDateDirection', void 0);
  __decorate13([model_field({
      'persist': false,
      'isEqual': isEqualEffectiveDirection
    }, {
      'sync': true,
      'equality': isEqualEffectiveDirection
    })], c.prototype, 'effectiveDirection', void 0);
  __decorate13([field({ 'identifierCls': SEDDispatcherIdentifier })], c.prototype, 'dispatcher', void 0);
  __decorate13([model_field({
      'type': 'boolean',
      'defaultValue': false
    }, { 'sync': true })], c.prototype, 'manuallyScheduled', void 0);
  __decorate13([model_field({
      'type': 'boolean',
      'defaultValue': false
    })], c.prototype, 'unscheduled', void 0);
  __decorate13([calculate('dispatcher')], c.prototype, 'calculateDispatcher', null);
  __decorate13([build_proposed('dispatcher')], c.prototype, 'buildProposedDispatcher', null);
  __decorate13([write('startDate')], c.prototype, 'writeStartDate', null);
  __decorate13([calculate('startDate')], c.prototype, 'calculateStartDate', null);
  __decorate13([write('endDate')], c.prototype, 'writeEndDate', null);
  __decorate13([calculate('endDate')], c.prototype, 'calculateEndDate', null);
  __decorate13([write('duration')], c.prototype, 'writeDuration', null);
  __decorate13([calculate('duration')], c.prototype, 'calculateDuration', null);
  __decorate13([calculate('effectiveDirection')], c.prototype, 'calculateEffectiveDirection', null);
  __decorate13([calculate('startDateDirection')], c.prototype, 'calculateStartDateDirection', null);
  __decorate13([calculate('endDateDirection')], c.prototype, 'calculateEndDateDirection', null);
  return c;
}) {
};
const __decorate14 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const BaseHasAssignmentsMixin = class extends Mixin([
  BaseEventMixin,
  AbstractHasAssignmentsMixin
], a => {
  const b = a.prototype;
  class c extends a {
    get assignments() {
      return this.assigned ? [...this.assigned] : [];
    }
  }
  __decorate14([generic_field({}, ModelBucketField)], c.prototype, 'assigned', void 0);
  return c;
}) {
};
const __decorate15 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const HasDependenciesMixin = class extends Mixin([BaseEventMixin], a => {
  const b = a.prototype;
  class c extends a {
    leaveProject() {
      let e, f;
      if (this.isInActiveTransaction) {
        const g = this.graph.activeTransaction;
        const i = this.getDependencyStore();
        const j = [];
        for (const k of (e = g.readCurrentOrProposedOrPrevious(this.$.outgoingDeps)) != null ? e : [])
          if (i.includes(k))
            j.push(k);
        for (const l of (f = g.readCurrentOrProposedOrPrevious(this.$.incomingDeps)) != null ? f : [])
          if (i.includes(l))
            j.push(l);
        this.project.dependencyStore.remove(j);
      }
      b.leaveProject.call(this);
    }
  }
  __decorate15([generic_field({}, ModelBucketField)], c.prototype, 'outgoingDeps', void 0);
  __decorate15([generic_field({}, ModelBucketField)], c.prototype, 'incomingDeps', void 0);
  return c;
}) {
};
const SchedulerBasicEvent = class extends Mixin([
  BaseEventMixin,
  BaseHasAssignmentsMixin,
  HasDependenciesMixin
], a => {
  const b = a.prototype;
  class c extends a {
  }
  return c;
}) {
};
const ChronoEventStoreMixin = class extends Mixin([
  AbstractEventStoreMixin,
  ChronoPartOfProjectStoreMixin
], a => {
  class b extends a {
    constructor() {
      super(...arguments);
      this.removalOrder = 400;
    }
    static get defaultConfig() {
      return { 'modelClass': SchedulerBasicEvent };
    }
    buildRootNode() {
      return this.getProject() || {};
    }
    set data(c) {
      super.data = c;
      this.afterEventRemoval();
    }
  }
  return b;
}) {
};
const ChronoEventTreeStoreMixin = class extends Mixin([ChronoEventStoreMixin], a => {
  class b extends a {
    static get defaultConfig() {
      return { 'tree': true };
    }
  }
  return b;
}) {
};
const __decorate16 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const BaseResourceMixin = class extends Mixin([
  HasCalendarMixin,
  ChronoPartOfProjectModelMixin
], a => {
  const b = a.prototype;
  class c extends a {
    get assignments() {
      return [...this.assigned];
    }
    leaveProject(e = false) {
      if (!this.isStmRestoring && this.assigned && !e) {
        const f = this.getResourceStore();
        this.assigned.forEach(g => f.assignmentsForRemoval.add(g));
      }
      b.leaveProject.call(this);
    }
    static get fields() {
      return [
        { 'name': 'parentId' },
        {
          'name': 'children',
          'persist': false
        }
      ];
    }
  }
  __decorate16([generic_field({}, ModelBucketField)], c.prototype, 'assigned', void 0);
  return c;
}) {
};
const ChronoResourceStoreMixin = class extends Mixin([
  AbstractResourceStoreMixin,
  ChronoPartOfProjectStoreMixin
], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.removalOrder = 300;
    }
    static get defaultConfig() {
      return { 'modelClass': BaseResourceMixin };
    }
  }
  return c;
}) {
};
const __decorate17 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const DurationConverterMixin = class extends Mixin([ChronoModelMixin], a => {
  const b = a.prototype;
  class c extends a {
    *calculateUnitsInMs() {
      const e = yield this.$.hoursPerDay;
      const f = yield this.$.daysPerWeek;
      const g = yield this.$.daysPerMonth;
      return {
        'millisecond': 1,
        'second': 1000,
        'minute': 60 * 1000,
        'hour': 60 * 60 * 1000,
        'day': e * 60 * 60 * 1000,
        'week': f * e * 60 * 60 * 1000,
        'month': g * e * 60 * 60 * 1000,
        'quarter': 3 * g * e * 60 * 60 * 1000,
        'year': 4 * 3 * g * e * 60 * 60 * 1000
      };
    }
    convertDuration(e, f, g) {
      let i = e;
      if (f !== g) {
        i = e * this.unitsInMs[f] / this.unitsInMs[g];
      }
      return i;
    }
    *$convertDuration(e, f, g) {
      if (!f || !g)
        throw new Error('Conversion unit not provided');
      const i = yield this.$.unitsInMs;
      let j = e;
      if (f !== g) {
        j = e * i[f] / i[g];
      }
      return j;
    }
  }
  __decorate17([field()], c.prototype, 'unitsInMs', void 0);
  __decorate17([model_field({
      'type': 'number',
      'defaultValue': 24
    })], c.prototype, 'hoursPerDay', void 0);
  __decorate17([model_field({
      'type': 'number',
      'defaultValue': 7
    })], c.prototype, 'daysPerWeek', void 0);
  __decorate17([model_field({
      'type': 'number',
      'defaultValue': 30
    })], c.prototype, 'daysPerMonth', void 0);
  __decorate17([calculate('unitsInMs')], c.prototype, 'calculateUnitsInMs', null);
  return c;
}) {
};
const __decorate18 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const BaseDependencyMixin = class extends Mixin([ChronoPartOfProjectModelMixin], a => {
  const b = a.prototype;
  class c extends a {
    get isValid() {
      const {
        $: e,
        graph: f
      } = this;
      if (f && (!f.hasIdentifier(e.fromEvent) || !f.hasIdentifier(e.toEvent))) {
        return false;
      }
      return super.isValid;
    }
  }
  __decorate18([generic_field({
      'bucket': 'outgoingDeps',
      'resolver': function (e) {
        return this.getEventById(e);
      },
      'modelFieldConfig': {
        'persist': true,
        'serialize': e => e == null ? void 0 : e.id,
        'isEqual': isSerializableEqual
      }
    }, ModelReferenceField)], c.prototype, 'fromEvent', void 0);
  __decorate18([generic_field({
      'bucket': 'incomingDeps',
      'resolver': function (e) {
        return this.getEventById(e);
      },
      'modelFieldConfig': {
        'persist': true,
        'serialize': e => e == null ? void 0 : e.id,
        'isEqual': isSerializableEqual
      }
    }, ModelReferenceField)], c.prototype, 'toEvent', void 0);
  __decorate18([model_field({
      'type': 'int',
      'defaultValue': DependencyType.EndToStart
    })], c.prototype, 'type', void 0);
  __decorate18([model_field({ 'type': 'string' })], c.prototype, 'fromSide', void 0);
  __decorate18([model_field({ 'type': 'string' })], c.prototype, 'toSide', void 0);
  return c;
}) {
};
const ChronoAbstractProjectMixin = class extends Mixin([
  ChronoModelMixin,
  AbstractProjectMixin
], a => {
  const b = a.prototype;
  class c extends a {
    get isDelayingCalculation() {
      return Boolean(this.delayEnteringReplica || this.delayedCalculationPromise);
    }
    getGraph() {
      return this.replica;
    }
    beforeCommitAsync() {
      return null;
    }
    enterReplica(e) {
    }
    acceptChanges() {
    }
    finalizeCommitHook(e) {
      return false;
    }
    invalidateTemporaryIndices() {
    }
    async commitAsync() {
      let e;
      return this.delayedCalculationPromise || ((e = this.replica) == null ? void 0 : e.commitAsync());
    }
    getSchedulingIssueEventArguments(e, f, g, i) {
      const j = [
        e.type,
        {
          'continueWithResolutionResult': g,
          'schedulingIssue': e
        }
      ];
      if (e instanceof ConflictEffect)
        j[1].conflict = e;
      return j;
    }
    async onSchedulingIssueCall(e, f) {
      if (e.type && this.hasListener(e.type)) {
        return new Promise((g, i) => {
          this.trigger(...this.getSchedulingIssueEventArguments(e, f, g, i));
        });
      }
      return EffectResolutionResult.Cancel;
    }
    async onCycleSchedulingIssue(e, f) {
      return this.onSchedulingIssueCall(e, f);
    }
    async onEmptyCalendarSchedulingIssue(e, f) {
      return this.onSchedulingIssueCall(e, f);
    }
    async onConflictSchedulingIssue(e, f) {
      return this.onSchedulingIssueCall(e, f);
    }
    setModelCalculations(e, f) {
      if (!f)
        return;
      const g = {};
      for (const i in f) {
        g[i] = e.prototype.$calculations[i];
      }
      Object.assign(e.prototype.$calculations, f);
      return g;
    }
    setRecordCalculations(e, f) {
      const g = this.setModelCalculations(e.constructor, f);
      const i = e.$entity.$skeleton;
      Object.keys(f).forEach(j => {
        i[j].prototype.calculation = e[f[j]];
      });
      return g;
    }
    setStoreCalculations(e, f) {
      if (!f)
        return;
      const g = e.first;
      if (g) {
        return this.setRecordCalculations(g, f);
      } else {
        return this.setModelCalculations(e.modelClass, f);
      }
    }
    async setCalculations(e) {
      this.replica && await this.commitAsync();
      const f = {};
      const g = {
        'tasks': this.eventStore,
        'events': this.eventStore,
        'dependencies': this.dependencyStore,
        'resources': this.resourceStore,
        'assignments': this.assignmentStore,
        'calendars': this.calendarManagerStore
      };
      Object.keys(g).forEach(j => {
        if (e[j]) {
          f[j] = this.setStoreCalculations(g[j], e[j]);
        }
      });
      const i = e.project;
      if (i) {
        f.project = this.setRecordCalculations(this, i);
      }
      this.replica && this.repopulateReplica.now();
      this.replica && await this.commitAsync();
      return f;
    }
  }
  return c;
}) {
};
const HasSubEventsMixin = class extends Mixin([BaseEventMixin], a => {
  const b = a.prototype;
  class c extends a {
    static get $name() {
      return 'HasSubEventsMixin';
    }
    *hasSubEvents() {
      throw new Error('Abstract method `hasSubEvents` has been called');
    }
    *subEventsIterable() {
      throw new Error('Abstract method `subEventsIterable` has been called');
    }
    *calculateStartDatePure() {
      const e = yield* this.isManuallyScheduled();
      const f = yield* this.hasSubEvents();
      if (!e && f) {
        return yield* this.calculateMinChildrenStartDate();
      } else {
        return yield* b.calculateStartDatePure.call(this);
      }
    }
    *calculateEndDatePure() {
      const e = yield* this.isManuallyScheduled();
      const f = yield* this.hasSubEvents();
      if (!e && f) {
        return yield* this.calculateMaxChildrenEndDate();
      } else {
        return yield* b.calculateEndDatePure.call(this);
      }
    }
    *calculateStartDateProposed() {
      const e = yield* this.isManuallyScheduled();
      const f = yield* this.hasSubEvents();
      if (!e && f) {
        return yield* this.calculateStartDatePure();
      } else {
        return yield* b.calculateStartDateProposed.call(this);
      }
    }
    *calculateEndDateProposed() {
      const e = yield* this.isManuallyScheduled();
      const f = yield* this.hasSubEvents();
      if (!e && f) {
        return yield* this.calculateEndDatePure();
      } else {
        return yield* b.calculateEndDateProposed.call(this);
      }
    }
    *calculateDurationProposed() {
      const e = yield* this.isManuallyScheduled();
      const f = yield* this.hasSubEvents();
      if (!e && f) {
        return yield* this.calculateDurationPure();
      } else {
        return yield* b.calculateDurationProposed.call(this);
      }
    }
    *shouldRollupChildStartDate(e) {
      return true;
    }
    *calculateMinChildrenStartDate() {
      const e = yield* this.subEventsIterable();
      let f = MAX_DATE2.getTime();
      for (const g of e) {
        if (yield* this.shouldRollupChildStartDate(g)) {
          let i = yield g.$.startDate;
          if (!i) {
            i = yield g.$.endDate;
          }
          if ((yield* g.isManuallyScheduled()) && (yield* g.hasSubEvents())) {
            const j = yield* g.calculateMinChildrenStartDate();
            if (!i || j && j.getTime() < i.getTime())
              i = j;
          }
          if (i && i.getTime() < f) {
            f = i.getTime();
          }
        }
      }
      if (f === MIN_DATE2.getTime() || f === MAX_DATE2.getTime())
        return null;
      return new Date(f);
    }
    *shouldRollupChildEndDate(e) {
      return true;
    }
    *calculateMaxChildrenEndDate() {
      const e = yield* this.subEventsIterable();
      let f = MIN_DATE2.getTime();
      for (const g of e) {
        if (yield* this.shouldRollupChildEndDate(g)) {
          let i = yield g.$.endDate;
          if (!i) {
            i = yield g.$.startDate;
          }
          if ((yield* g.isManuallyScheduled()) && (yield* g.hasSubEvents())) {
            const j = yield* g.calculateMaxChildrenEndDate();
            if (!i || j && j.getTime() > i.getTime())
              i = j;
          }
          if (i && i.getTime() > f) {
            f = i.getTime();
          }
        }
      }
      if (f === MIN_DATE2.getTime() || f === MAX_DATE2.getTime())
        return null;
      return new Date(f);
    }
  }
  return c;
}) {
};
const __decorate19 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const HasChildrenMixin = class extends Mixin([HasSubEventsMixin], a => {
  const b = a.prototype;
  class c extends a {
    *hasSubEvents() {
      const e = yield this.$.childEvents;
      return e.size > 0;
    }
    *subEventsIterable() {
      return yield this.$.childEvents;
    }
    get parent() {
      return this._parent;
    }
    set parent(e) {
      this._parent = e;
      this.parentEvent = e;
    }
    *calculateEffectiveDirection() {
      const e = yield this.$.direction;
      if (e)
        return {
          'kind': 'own',
          'direction': e
        };
      const f = yield this.$.parentEvent;
      if (f) {
        const g = yield f.$.effectiveDirection;
        return {
          'kind': 'inherited',
          'direction': g.direction,
          'inheritedFrom': g.kind === 'own' ? f : g.kind === 'inherited' ? g.inheritedFrom : f
        };
      } else
        return yield* super.calculateEffectiveDirection();
    }
    *calculateStartDateDirection() {
      const e = yield this.getProject().$.effectiveDirection;
      let f = null;
      if (!(yield* this.isManuallyScheduled())) {
        const g = yield* this.subEventsIterable();
        for (const i of g) {
          const j = yield i.$.startDateDirection;
          if (e.direction !== j.direction) {
            f = {
              'kind': 'enforced',
              'direction': j.direction,
              'enforcedBy': j.kind === 'own' ? i : j.kind === 'enforced' ? j.enforcedBy : j.inheritedFrom
            };
            break;
          }
        }
      }
      return f != null ? f : yield* super.calculateStartDateDirection();
    }
    *calculateEndDateDirection() {
      const e = yield this.getProject().$.effectiveDirection;
      let f = null;
      if (!(yield* this.isManuallyScheduled())) {
        const g = yield* this.subEventsIterable();
        for (const i of g) {
          const j = yield i.$.endDateDirection;
          if (e.direction !== j.direction) {
            f = {
              'kind': 'enforced',
              'direction': j.direction,
              'enforcedBy': j.kind === 'own' ? i : j.kind === 'enforced' ? j.enforcedBy : j.inheritedFrom
            };
            break;
          }
        }
      }
      return f != null ? f : yield* super.calculateEndDateDirection();
    }
  }
  __decorate19([generic_field({
      'bucket': 'childEvents',
      'resolver': function (e) {
        return this.getEventById(e);
      },
      'ignore': true,
      'modelFieldConfig': { 'persist': false }
    }, ModelReferenceField)], c.prototype, 'parentEvent', void 0);
  __decorate19([bucket()], c.prototype, 'childEvents', void 0);
  return c;
}) {
};
let counter = 1;
const Queue = class {
  constructor(a = 'Queue #' + counter++) {
    this.name = '';
    this.length = 0;
    this.finishedStep = 0;
    this.stacks = [];
    this.ongoing = Promise.resolve();
    this.handleReject = () => {
    };
    this.name = a;
  }
  debug(a) {
    let b;
    if ((b = globalThis.bryntum) == null ? void 0 : b.DEBUG) {
      console.log(this.name, a);
    }
  }
  queue(a, b = this.handleReject) {
    let c;
    const e = this.length++;
    if ((c = globalThis.bryntum) == null ? void 0 : c.DEBUG) {
      const g = new Error().stack;
      this.stacks.push(g);
      this.debug('Queued fn #' + e);
      this.debug(g);
    }
    const f = this.ongoing.then(async () => {
      this.debug('Running queued fn #' + e);
      const i = await a();
      this.finishedStep++;
      this.debug('Finished queued fn #' + e);
      return i;
    });
    this.ongoing = f.catch(b);
    return f;
  }
};
Queue._$name = 'Queue';
const __decorate20 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const SchedulerBasicProjectMixin = class extends MixinAny([
  ChronoAbstractProjectMixin,
  BaseEventMixin,
  HasSubEventsMixin,
  HasCalendarMixin,
  DurationConverterMixin,
  CanCombineCalendarsMixin
], a => {
  const b = a.prototype;
  class c extends a {
    construct(e = {}) {
      this.delayCalculation = e.delayCalculation !== false;
      this.enableProgressNotifications = e.enableProgressNotifications || e.delayCalculation !== false;
      this._queue = new Queue(IdHelper.generateId('ProjectModelQueue'));
      if (!('expanded' in e)) {
        e.expanded = true;
      }
      if (this.delayCalculation) {
        this.delayEnteringReplica = true;
      }
      if (!('skipNonWorkingTimeWhenSchedulingManually' in e)) {
        e.skipNonWorkingTimeWhenSchedulingManually = false;
      }
      b.construct.call(this, e);
      this.repopulateStores = new Set();
      this.ignoreInitialCommitComputationCycles = 'ignoreInitialCommitComputationCycles' in e ? e.ignoreInitialCommitComputationCycles : false;
      if (this.ignoreInitialCommitComputationCycles) {
        console.warn('Project "ignoreInitialCommitComputationCycles" option is deprecated and will be dropped in the next major release');
      }
      if (!this.eventModelClass)
        this.eventModelClass = this.getDefaultEventModelClass();
      if (!this.eventStoreClass)
        this.eventStoreClass = this.getDefaultEventStoreClass();
      if (!this.dependencyModelClass)
        this.dependencyModelClass = this.getDefaultDependencyModelClass();
      if (!this.dependencyStoreClass)
        this.dependencyStoreClass = this.getDefaultDependencyStoreClass();
      if (!this.resourceModelClass)
        this.resourceModelClass = this.getDefaultResourceModelClass();
      if (!this.resourceStoreClass)
        this.resourceStoreClass = this.getDefaultResourceStoreClass();
      if (!this.assignmentModelClass)
        this.assignmentModelClass = this.getDefaultAssignmentModelClass();
      if (!this.assignmentStoreClass)
        this.assignmentStoreClass = this.getDefaultAssignmentStoreClass();
      if (!this.calendarModelClass)
        this.calendarModelClass = this.getDefaultCalendarModelClass();
      if (!this.calendarManagerStoreClass)
        this.calendarManagerStoreClass = this.getDefaultCalendarManagerStoreClass();
      if (!this.cycleEffectClass)
        this.cycleEffectClass = this.getDefaultCycleEffectClass();
      this.initializeStm();
      this.defaultCalendar = new this.calendarModelClass({ 'unspecifiedTimeIsWorking': this.unspecifiedTimeIsWorking });
      this.defaultCalendar.project = this;
      if (!this.delayEnteringReplica)
        this.enterReplica(false);
      this.setCalendarManagerStore(this.calendarManagerStore);
      this.setEventStore(this.eventStore);
      this.setDependencyStore(this.dependencyStore);
      this.setResourceStore(this.resourceStore);
      this.setAssignmentStore(this.assignmentStore);
      const f = Boolean(this.calendarsData || this.eventsData || this.dependenciesData || this.resourcesData || this.assignmentsData);
      if (f) {
        this.loadInlineData({
          'calendarsData': this.calendarsData,
          'eventsData': this.eventsData,
          'dependenciesData': this.dependenciesData,
          'resourcesData': this.resourcesData,
          'assignmentsData': this.assignmentsData
        });
        delete this.calendarsData;
        delete this.eventsData;
        delete this.dependenciesData;
        delete this.resourcesData;
        delete this.assignmentsData;
      } else {
        if (this.delayCalculation && this.hasDataInStores)
          this.scheduleDelayedCalculation();
      }
    }
    get dataStores() {
      return [
        this.calendarManagerStore,
        this.resourceStore,
        this.eventStore,
        this.dependencyStore,
        this.assignmentStore
      ];
    }
    get hasDataInStores() {
      return this.dataStores.some(e => e.totalCount > 0);
    }
    get hasChangesInStores() {
      return this.dataStores.some(e => e.hasChanges);
    }
    enterReplica(e) {
      const f = this;
      if (!f.replica) {
        f.replica = f.createReplica();
        f.replica.addEntity(f);
        f.replica.addEntity(f.defaultCalendar);
        f.trigger('graphReady');
      }
      if (e && !f.isRepopulatingStores) {
        f.dataStores.forEach(g => {
          g.forEach(i => {
            !i.graph && i.joinProject();
          }, void 0, { 'includeFilteredOutRecords': true });
        });
      }
    }
    resetStmQueue() {
      const e = this.stm.disabled;
      this.stm.disable();
      this.stm.resetQueue();
      if (!e) {
        this.stm.enable();
      }
    }
    doDestroy() {
      let e, f, g;
      const i = this;
      i.dataStores.forEach(j => j == null ? void 0 : j.destroy());
      (e = i.defaultCalendar) == null ? void 0 : e.destroy();
      (f = i.replica) == null ? void 0 : f.clear();
      (g = i.stm) == null ? void 0 : g.destroy();
      b.doDestroy.call(this);
    }
    getReplicaConfig() {
      return {
        'project': this,
        'schema': Schema.new(),
        'enableProgressNotifications': this.enableProgressNotifications,
        'silenceInitialCommit': this.silenceInitialCommit,
        'ignoreInitialCommitComputationCycles': this.ignoreInitialCommitComputationCycles,
        'cycleEffectClass': this.cycleEffectClass,
        'onWriteDuringCommit': 'ignore',
        'readMode': ReadMode.CurrentOrProposedOrPrevious
      };
    }
    createReplica() {
      return EngineReplica.new(this.getReplicaConfig());
    }
    *hasSubEvents() {
      return this.getEventStore().count > 0;
    }
    *subEventsIterable() {
      return this.getEventStore().getRange();
    }
    getType() {
      return ProjectType.SchedulerBasic;
    }
    get enableProgressNotifications() {
      return this._enableProgressNotifications;
    }
    set enableProgressNotifications(e) {
      this._enableProgressNotifications = e;
      if (this.replica)
        this.replica.enableProgressNotifications = e;
    }
    getDefaultCycleEffectClass() {
      return CycleEffect;
    }
    getDefaultEventModelClass() {
      return SchedulerBasicEvent;
    }
    getDefaultEventStoreClass() {
      return ChronoEventStoreMixin;
    }
    getDefaultDependencyModelClass() {
      return BaseDependencyMixin;
    }
    getDefaultDependencyStoreClass() {
      return ChronoDependencyStoreMixin;
    }
    getDefaultResourceModelClass() {
      return BaseResourceMixin;
    }
    getDefaultResourceStoreClass() {
      return ChronoResourceStoreMixin;
    }
    getDefaultAssignmentModelClass() {
      return BaseAssignmentMixin;
    }
    getDefaultAssignmentStoreClass() {
      return ChronoAssignmentStoreMixin;
    }
    getDefaultCalendarModelClass() {
      return BaseCalendarMixin;
    }
    getDefaultCalendarManagerStoreClass() {
      return ChronoCalendarManagerStoreMixin;
    }
    usingSyncDataOnLoad() {
      return [
        this.eventStore,
        this.resourceStore,
        this.dependencyStore,
        this.assignmentStore
      ].some(e => e.syncDataOnLoad);
    }
    async loadInlineData(e) {
      const {
        calendarManagerStore: f,
        eventStore: g,
        dependencyStore: i,
        assignmentStore: j,
        resourceStore: k,
        replica: l
      } = this;
      if (!this.isInitialCommitPerformed) {
        l == null ? void 0 : l.unScheduleAutoCommit();
      } else {
        while (this.replica.isCommitting) {
          await this.commitAsync();
        }
      }
      if ((l == null ? void 0 : l.enableProgressNotifications) && !this.delayCalculation) {
        await delay2(0);
        await this.commitAsync();
        l.onPropagationProgressNotification({
          'total': 0,
          'remaining': 0,
          'phase': 'storePopulation'
        });
        await delay2(50);
      }
      this.isInitialCommitPerformed = false;
      this.isLoadingInlineData = true;
      if (globalThis.DEBUG) {
        console.log('%cInitializing project', 'font-weight:bold;color:darkgreen;text-transform:uppercase;margin-top: 2em');
        console.time('Time to visible');
        console.time('Populating project');
      }
      if (this.delayCalculation && !this.delayedCalculationPromise && !this.usingSyncDataOnLoad()) {
        this.scheduleDelayedCalculation();
      }
      if (e.calendarsData) {
        this.repopulateStore(f);
        f.data = e.calendarsData;
      }
      if (e.eventsData || e.tasksData) {
        this.repopulateStore(g);
        g.data = e.eventsData || e.tasksData;
      }
      if (e.dependenciesData) {
        this.repopulateStore(i);
        i.data = e.dependenciesData;
      }
      if (e.resourcesData) {
        this.repopulateStore(k);
        k.data = e.resourcesData;
      }
      if (e.assignmentsData) {
        this.repopulateStore(j);
        j.data = e.assignmentsData;
      }
      if (e.project) {
        if (this.delayEnteringReplica && this.hasDataInStores) {
          this.ion({
            'recordsUnlinked': () => {
              this.suspendChangeTracking();
              this.applyProjectResponse(e.project);
              this.resumeChangeTracking();
            },
            'once': true
          });
        } else {
          this.applyProjectResponse(e.project);
        }
      }
      if (globalThis.DEBUG)
        console.timeEnd('Populating project');
      const n = await this.commitLoad();
      this.isLoadingInlineData = false;
      return n;
    }
    setupTemporaryIndices() {
      const {storage: e} = this.assignmentStore || {};
      if (e) {
        e.addIndex({
          'property': 'event',
          'unique': false
        });
        e.addIndex({
          'property': 'resource',
          'unique': false
        });
      }
    }
    invalidateTemporaryIndices() {
      let e, f;
      (f = (e = this.assignmentStore) == null ? void 0 : e.storage) == null ? void 0 : f.invalidateIndices();
    }
    removeTemporaryIndices() {
      const {storage: e} = this.assignmentStore;
      e.removeIndex('event');
      e.removeIndex('resource');
    }
    async internalDelayCalculation(e) {
      const f = this;
      f.delayEnteringReplica = true;
      f.setupTemporaryIndices();
      await delay2(0);
      if (f.isDestroyed) {
        e();
        return;
      }
      f.trigger('delayCalculationStart');
      f.trigger('refresh', { 'isCalculated': false });
      await delay2(0);
      if (f.isDestroyed) {
        e();
        return;
      }
      f.delayEnteringReplica = false;
      f.dataStores.forEach(i => i.suspendFilterOnUpdate());
      if (f.isRepopulatingStores) {
        f.repopulateReplica.now();
      } else {
        f.trigger('recordsUnlinked');
        f.enterReplica(true);
      }
      const g = await f.replica.commitAsync();
      if (f.isDestroyed) {
        e();
        return;
      }
      f.dataStores.forEach(i => i.resumeFilterOnUpdate());
      e(g);
      f.delayedCalculationPromise = null;
      f.trigger('delayCalculationEnd');
      f.removeTemporaryIndices();
    }
    scheduleDelayedCalculation() {
      if (this.delayedCalculationPromise) {
        return this.delayedCalculationPromise;
      }
      if (this.delayCalculation !== false) {
        return this.delayedCalculationPromise = new Promise(e => this.internalDelayCalculation(e).then());
      }
    }
    async commitLoad() {
      const e = await this.commitAsync();
      if (!this.isDestroyed)
        this.trigger('load');
      return e;
    }
    initializeStm() {
      const e = this.stmClass || StateTrackingManager;
      if (!(this.stm instanceof StateTrackingManager))
        this.setStm(e.new({ 'disabled': true }, this.stm));
      if (this.resetUndoRedoQueuesAfterLoad) {
        this.ion({
          'load': this.resetStmQueue,
          'thisObj': this
        });
      }
      this.ion({
        'beforeCommit': this.onCommitInitialization,
        'commitFinalized': this.onCommitFinalization,
        'commitRejected': this.onCommitRejection,
        'thisObj': this
      });
    }
    removeRejectedRecordsAdd({
      transactionResult: e,
      silenceCommit: f
    }) {
      let g, i, j;
      const k = new Map();
      for (const n of e.entries.values()) {
        const o = n.identifier;
        const {field: p} = o;
        if (n.isShadow() || !o[IsChronoModelSymbol] || p instanceof ModelBucketField)
          continue;
        const q = o.self;
        const r = q.firstStore;
        if (r && !n.previous && !((g = e.transaction.getLatestStableEntryFor(q.$$)) == null ? void 0 : g.previous)) {
          if (!k.has(r)) {
            k.set(r, new Set([q]));
          } else if (!k.get(r).has(q)) {
            k.get(r).add(q);
          }
        }
      }
      (i = this.suspendChangeTracking) == null ? void 0 : i.call(this);
      const l = Array.from(k.keys()).sort((u, v) => u.removalOrder - v.removalOrder);
      l.forEach(u => u.remove(k.get(u)));
      (j = this.resumeChangeTracking) == null ? void 0 : j.call(this, f);
      if (f) {
        this.dataStores.forEach(u => u.acceptChanges());
      }
    }
    onCommitRejection(e) {
      if (this._stmDisabled) {
        this.replica.isWritingPreviousData = true;
        this.isRestoringData = true;
        this.removeRejectedRecordsAdd(e);
        this.isRestoringData = false;
        this.replica.isWritingPreviousData = false;
      } else {
        this.rejectStmTransaction();
      }
    }
    onCommitInitialization() {
      const {stm: e} = this;
      this._stmDisabled = e.disabled;
      if (e.isRecording && e.autoRecord) {
        this._stmAutoRecord = true;
        e.autoRecord = false;
      }
    }
    onCommitFinalization() {
      if (this._stmAutoRecord) {
        this.stm.autoRecord = true;
        this._stmAutoRecord = false;
      }
    }
    async undo() {
      await this.stm.undo();
      if (this._isCommitingAfterStmRestore) {
        await this.await('stateRestoringDone', false);
      }
    }
    async redo() {
      await this.stm.redo();
      if (this._isCommitingAfterStmRestore) {
        await this.await('stateRestoringDone', false);
      }
    }
    onSTMRestoringStart({source: e}) {
      if (this.replica)
        this.replica.isWritingPreviousData = true;
    }
    async handleSTMRestoringStop(e) {
      if (this.replica)
        this.replica.isWritingPreviousData = false;
      e.disable();
      this._isCommitingAfterStmRestore = true;
      await this.commitAsync();
      this._isCommitingAfterStmRestore = false;
      if (!this.isDestroyed) {
        e.enable();
        this.trigger('stateRestoringDone');
      }
    }
    async onSTMRestoringStop({source: e}) {
      if (e.revisionsEnabled) {
        await this.queue(() => this.handleSTMRestoringStop(e));
      } else {
        await this.handleSTMRestoringStop(e);
      }
    }
    deferUntilRepopulationIfNeeded(e, f, g) {
      if (this.isRepopulatingStores) {
        this.detachListeners(e);
        this.ion({
          'name': e,
          'repopulateReplica': {
            'fn': async () => {
              await this.commitAsync();
              if (!this.isDestroyed) {
                f(...g);
              }
            },
            'once': true
          }
        });
      } else {
        f(...g);
      }
    }
    get isRepopulatingStores() {
      let e;
      return Boolean((e = this.repopulateStores) == null ? void 0 : e.size);
    }
    repopulateStore(e) {
      let f;
      const g = this;
      if (g.repopulateOnDataset && e.totalCount && !e.syncDataOnLoad) {
        (f = g.replica) == null ? void 0 : f.activeTransaction.stop();
        if (!g.repopulateStores)
          g.repopulateStores = new Set();
        g.repopulateStores.add(e);
        g.repopulateReplica();
      }
    }
    repopulateReplica() {
      const e = this;
      if (e.delayEnteringReplica) {
        return;
      }
      const {
        calendarManagerStore: f,
        eventStore: g,
        dependencyStore: i,
        assignmentStore: j,
        resourceStore: k,
        replica: l
      } = e;
      if (l) {
        e.unlinkStoreRecords(f, g, i, k, j);
        e.unlinkRecord(e);
        e.unlinkRecord(e.defaultCalendar);
        e.trigger('recordsUnlinked');
        l.clear();
      } else {
        e.trigger('recordsUnlinked');
      }
      const n = e.replica = e.createReplica();
      n.addEntity(e);
      n.addEntity(e.defaultCalendar);
      e.joinStoreRecords(f, true);
      e.joinStoreRecords(g, true);
      e.joinStoreRecords(i, true);
      e.joinStoreRecords(k, true);
      e.joinStoreRecords(j, true);
      if (l) {
        n.activeTransaction.writeFailedResolutionInfo(l.baseRevision.failedResolutionReferences);
      }
      e.repopulateStores.clear();
      e.trigger('repopulateReplica');
    }
    beforeCommitAsync() {
      if (this.repopulateReplica.isPending && !this.isDelayingCalculation) {
        this.repopulateReplica.now();
        return this.replica.commitAsync();
      }
      return null;
    }
    unlinkRecord(e) {
      e.unlink();
    }
    unlinkStoreRecords(...e) {
      e.forEach(f => {
        if (!this.repopulateStores.has(f) || f.syncDataOnLoad) {
          f.traverse(g => {
            this.unlinkRecord(g);
          }, false, false, {
            'includeFilteredOutRecords': true,
            'includeCollapsedGroupRecords': true
          });
        }
      });
    }
    getGraph() {
      return this.replica;
    }
    async addEvents(e) {
      this.eventStore.add(e);
      return this.commitAsync();
    }
    async addEvent(e) {
      this.eventStore.add(e);
      return this.commitAsync();
    }
    includeEvent(e) {
      this.eventStore.add(e);
    }
    async removeEvents(e) {
      this.eventStore.remove(e);
      return this.commitAsync();
    }
    excludeEvent(e) {
      this.eventStore.remove(e);
    }
    async removeEvent(e) {
      this.eventStore.remove(e);
      return this.commitAsync();
    }
    getStm() {
      return this.stm;
    }
    setStm(e) {
      this.stm = e;
      this.stm.ion({
        'restoringStart': this.onSTMRestoringStart,
        'restoringStop': this.onSTMRestoringStop,
        'thisObj': this
      });
    }
    calculateProject() {
      return this;
    }
    *calculateEffectiveDataOrVisualCalendar(e) {
      let f = yield this.$.calendar;
      if (f) {
        yield f.$.version;
      } else {
        f = this.defaultCalendar;
      }
      return f;
    }
    joinStoreRecords(e, f = false) {
      const g = i => {
        i.setProject(this);
        i.joinProject();
      };
      if (e.rootNode) {
        e.rootNode.traverse(g, f, true);
      } else {
        e.forEach(g, null, {
          'includeFilteredOutRecords': true,
          'includeCollapsedGroupRecords': true
        });
      }
    }
    unjoinStoreRecords(e) {
      const f = g => {
        g.leaveProject();
        g.setProject(this);
      };
      if (e.rootNode) {
        e.rootNode.traverse(g => {
          if (g !== e.rootNode)
            f(g);
        }, false, true);
      } else {
        e.forEach(f, null, {
          'includeFilteredOutRecords': true,
          'includeCollapsedGroupRecords': true
        });
      }
    }
    setEventStore(e) {
      const f = this.eventStore;
      if (f && this.stm.hasStore(f)) {
        this.stm.removeStore(f);
        this.unjoinStoreRecords(f);
        this.detachStore(f);
        const g = f.assignmentsForRemoval;
        g.forEach(i => {
          const j = i.event;
          if (j) {
            const k = e.getById(j.id);
            if (k) {
              i.event = k;
              g.delete(i);
            }
          }
        });
        f.afterEventRemoval();
      }
      if (!e || !(e instanceof Store)) {
        const i = (e == null ? void 0 : e.storeClass) || this.eventStoreClass;
        this.eventStore = new i(ObjectHelper.assign({
          'modelClass': this.eventModelClass,
          'project': this,
          'stm': this.stm
        }, e || {}));
      } else {
        this.eventStore = e;
        e.setProject(this);
        this.stm.addStore(e);
        if (e.tree && e.rootNode !== this) {
          this.appendChild(e.rootNode.children || []);
          e.rootNode = this;
        } else {
          this.joinStoreRecords(e);
        }
      }
      this.attachStore(this.eventStore);
      this.trigger('eventStoreChange', { 'store': this.eventStore });
    }
    setDependencyStore(e) {
      const f = this.dependencyStore;
      if (f && this.stm.hasStore(f)) {
        this.stm.removeStore(f);
        this.detachStore(f);
      }
      if (!e || !(e instanceof Store)) {
        const g = (e == null ? void 0 : e.storeClass) || this.dependencyStoreClass;
        this.dependencyStore = new g(ObjectHelper.assign({
          'modelClass': this.dependencyModelClass,
          'project': this,
          'stm': this.stm
        }, e || {}));
      } else {
        this.dependencyStore = e;
        e.setProject(this);
        this.stm.addStore(e);
        this.joinStoreRecords(e);
      }
      this.attachStore(this.dependencyStore);
      this.trigger('dependencyStoreChange', { 'store': this.dependencyStore });
    }
    setResourceStore(e) {
      const f = this.resourceStore;
      if (f && this.stm.hasStore(f)) {
        this.stm.removeStore(f);
        this.unjoinStoreRecords(f);
        this.detachStore(f);
        const g = f.assignmentsForRemoval;
        g.forEach(i => {
          const j = i.resource;
          if (j) {
            const k = e.getById(j.id);
            if (k) {
              i.resource = k;
              g.delete(i);
            }
          }
        });
        f.afterResourceRemoval();
      }
      if (!e || !(e instanceof Store)) {
        const i = (e == null ? void 0 : e.storeClass) || this.resourceStoreClass;
        this.resourceStore = new i(ObjectHelper.assign({
          'modelClass': this.resourceModelClass,
          'project': this,
          'stm': this.stm
        }, e || {}));
      } else {
        this.resourceStore = e;
        e.setProject(this);
        this.stm.addStore(e);
        this.joinStoreRecords(e);
      }
      this.attachStore(this.resourceStore);
      this.trigger('resourceStoreChange', { 'store': this.resourceStore });
    }
    setAssignmentStore(e) {
      const f = this.assignmentStore;
      if (f && this.stm.hasStore(f)) {
        this.stm.removeStore(f);
        this.unjoinStoreRecords(f);
        this.detachStore(f);
      }
      if (!e || !(e instanceof Store)) {
        const g = (e == null ? void 0 : e.storeClass) || this.assignmentStoreClass;
        this.assignmentStore = new g(ObjectHelper.assign({
          'modelClass': this.assignmentModelClass,
          'project': this,
          'stm': this.stm
        }, e || {}));
      } else {
        this.assignmentStore = e;
        e.setProject(this);
        this.stm.addStore(e);
        this.joinStoreRecords(e);
      }
      this.isDelayingCalculation && this.setupTemporaryIndices();
      this.attachStore(this.assignmentStore);
      this.trigger('assignmentStoreChange', { 'store': this.assignmentStore });
    }
    setCalendarManagerStore(e) {
      const f = this.calendarManagerStore;
      if (f && this.stm.hasStore(f)) {
        this.stm.removeStore(f);
        this.detachStore(f);
      }
      if (!e || !(e instanceof Store)) {
        const g = (e == null ? void 0 : e.storeClass) || this.calendarManagerStoreClass;
        this.calendarManagerStore = new g(ObjectHelper.assign({
          'modelClass': this.calendarModelClass,
          'project': this,
          'stm': this.stm
        }, e || {}));
      } else {
        this.calendarManagerStore = e;
        if (e) {
          e.setProject(this);
          this.stm.addStore(e);
          this.joinStoreRecords(e);
        }
      }
      this.attachStore(this.calendarManagerStore);
      this.trigger('calendarManagerStoreChange', { 'store': this.calendarManagerStore });
    }
    async isValidDependency(...e) {
      return true;
    }
    rejectStmTransaction(e) {
      e = e || this.stm;
      if (e.transaction) {
        if (e.transaction.length) {
          e.forEachStore(f => f.beginBatch());
          e.rejectTransaction();
          e.forEachStore(f => f.endBatch());
        } else {
          e.stopTransaction();
        }
      }
    }
    async tryPropagateWithChanges(f) {
      const g = this.stm, i = g.disabled, j = g.autoRecord;
      if (i) {
        g.enable();
      } else {
        if (j) {
          g.autoRecord = false;
        }
        if (g.isRecording) {
          g.stopTransaction();
        }
      }
      g.startTransaction();
      f();
      let k = true;
      try {
        const l = await this.commitAsync();
        k = !l.rejectedWith;
      } catch (n) {
        if (!/cycle/i.test(n))
          throw n;
        k = false;
      }
      if (k) {
        g.stopTransaction();
        if (i) {
          g.resetQueue();
        }
      } else {
        this.replica.reject();
        this.rejectStmTransaction(g);
      }
      g.disabled = i;
      g.autoRecord = j;
      return k;
    }
    async tryPropagateWithChangesQueued(e) {
      return this.queue(() => this.tryPropagateWithChanges(e));
    }
    queue(e, f) {
      const g = this;
      return g._queue.queue(async function i() {
        return e();
      }, f);
    }
    isEngineReady() {
      const {replica: e} = this;
      return this.delayEnteringReplica || !this.isRepopulatingStores && (e ? !(e.dirty && (e.hasPendingAutoCommit() || e.isCommitting)) : true);
    }
    getCalendarConsumers(e) {
      let f;
      const g = [];
      if (this.calendar === e)
        g.push(this);
      g.push(...this.eventStore.query(i => i.calendar === e, true));
      g.push(...this.resourceStore.query(i => i.calendar === e, true));
      if ((f = e.children) == null ? void 0 : f.length) {
        for (const i of e.children) {
          g.push(...this.getCalendarConsumers(i));
        }
      }
      return g;
    }
    unlinkCalendarConsumers(e) {
      const f = this.getCalendarConsumers(e);
      for (const g of f) {
        g.calendar = null;
      }
    }
    static get defaultConfig() {
      return {
        'assignmentsData': null,
        'calendarsData': null,
        'dependenciesData': null,
        'eventsData': null,
        'resourcesData': null,
        'eventStore': null,
        'resourceStore': null,
        'assignmentStore': null,
        'dependencyStore': null,
        'calendarManagerStore': null,
        'eventModelClass': null,
        'resourceModelClass': null,
        'assignmentModelClass': null,
        'dependencyModelClass': null,
        'calendarModelClass': null,
        'repopulateOnDataset': true
      };
    }
    static get delayable() {
      return { 'repopulateReplica': 10 };
    }
  }
  c.applyConfigs = true;
  __decorate20([model_field({
      'type': 'boolean',
      'defaultValue': true
    })], c.prototype, 'unspecifiedTimeIsWorking', void 0);
  __decorate20([model_field({
      'type': 'boolean',
      'defaultValue': false
    })], c.prototype, 'skipNonWorkingTimeWhenSchedulingManually', void 0);
  __decorate20([model_field({
      'type': 'boolean',
      'defaultValue': false
    })], c.prototype, 'skipNonWorkingTimeInDurationWhenSchedulingManually', void 0);
  __decorate20([model_field({
      'type': 'string',
      'defaultValue': Direction.Forward
    }, { 'sync': true })], c.prototype, 'direction', void 0);
  return c;
}) {
};
const __decorate21 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const calculateEffectiveStartDateConstraintInterval = function* (a, b, c, e, f) {
  if (c.isIntervalEmpty())
    return c;
  if (f && c.intersectionOf) {
    const j = new Set();
    for (const k of c.intersectionOf) {
      if (k.isInfinite()) {
        j.add(k);
      } else {
        const l = k.startDateIsFinite() ? yield* a.calculateProjectedXDateWithDuration(k.startDate, false, e) : k.startDate;
        const n = k.endDateIsFinite() ? yield* a.calculateProjectedXDateWithDuration(k.endDate, false, e) : k.endDate;
        const o = k;
        j.add(o.copyWith({
          'reflectionOf': o,
          'side': o.side === ConstraintIntervalSide.Start ? ConstraintIntervalSide.End : ConstraintIntervalSide.Start,
          'startDate': l,
          'endDate': n
        }));
      }
    }
    c.intersectionOf = j;
  }
  const g = c.startDateIsFinite() ? yield* a.calculateProjectedXDateWithDuration(c.startDate, false, e) : null;
  const i = c.endDateIsFinite() ? yield* a.calculateProjectedXDateWithDuration(c.endDate, false, e) : null;
  return intersectIntervals([
    b,
    ConstraintInterval.new({
      'intersectionOf': c.intersectionOf,
      'startDate': g,
      'endDate': i
    })
  ], f);
};
const calculateEffectiveEndDateConstraintInterval = function* (a, b, c, e, f) {
  if (b.isIntervalEmpty())
    return b;
  if (f) {
    const j = new Set();
    for (const k of b.intersectionOf) {
      if (k.isInfinite()) {
        j.add(k);
      } else {
        const l = k.startDateIsFinite() ? yield* a.calculateProjectedXDateWithDuration(k.startDate, true, e) : k.startDate;
        const n = k.endDateIsFinite() ? yield* a.calculateProjectedXDateWithDuration(k.endDate, true, e) : k.endDate;
        const o = k;
        j.add(o.copyWith({
          'reflectionOf': o,
          'side': o.side === ConstraintIntervalSide.Start ? ConstraintIntervalSide.End : ConstraintIntervalSide.Start,
          'startDate': l,
          'endDate': n
        }));
      }
    }
    b.intersectionOf = j;
  }
  const g = b.startDateIsFinite() ? yield* a.calculateProjectedXDateWithDuration(b.startDate, true, e) : null;
  const i = b.endDateIsFinite() ? yield* a.calculateProjectedXDateWithDuration(b.endDate, true, e) : null;
  return intersectIntervals([
    c,
    ConstraintInterval.new({
      'reflectionOf': g || i ? b : void 0,
      'intersectionOf': g || i ? b.intersectionOf : void 0,
      'startDate': g,
      'endDate': i
    })
  ], f);
};
const EventScheduleMixin = class extends Mixin([ChronoPartOfProjectModelMixin], a => {
  class b extends a {
    constructor() {
      super(...arguments);
      this.direction = void 0;
      this.event = void 0;
    }
    get project() {
      let c;
      return (c = this.event) == null ? void 0 : c.project;
    }
    pick(c) {
      throw new Error('Implement me');
    }
    *calculateStartDate() {
      throw new Error('Implement me');
    }
    *calculateEndDate() {
      throw new Error('Implement me');
    }
  }
  __decorate21([model_field({
      'type': 'date',
      'persist': false,
      'calculated': true
    }, {
      'lazy': true,
      'converter': dateConverter,
      'persistent': false
    })], b.prototype, 'startDate', void 0);
  __decorate21([model_field({
      'type': 'date',
      'persist': false,
      'calculated': true
    }, {
      'lazy': true,
      'converter': dateConverter,
      'persistent': false
    })], b.prototype, 'endDate', void 0);
  __decorate21([calculate('startDate')], b.prototype, 'calculateStartDate', null);
  __decorate21([calculate('endDate')], b.prototype, 'calculateEndDate', null);
  return b;
}) {
};
const ConstrainedScheduleMixin = class extends Mixin([EventScheduleMixin], a => {
  class b extends a {
    constructor() {
      super(...arguments);
      this.direction = Direction.Forward;
      this.event = void 0;
    }
    pick(c) {
      return c.earlyPreSchedule;
    }
    *maybeSkipNonWorkingTime(c, e = true) {
      if (yield* this.event.hasSubEvents())
        return c;
      const f = yield* this.event.calculateEffectiveDuration();
      return c && f > 0 ? yield* this.event.skipNonWorkingTime(c, e) : c;
    }
    *calculateEffectiveConstraintInterval(c, e, f, g = false) {
      const i = yield* this.event.calculateEffectiveDuration();
      if (i == null) {
        return null;
      }
      const j = c ? calculateEffectiveStartDateConstraintInterval : calculateEffectiveEndDateConstraintInterval;
      const k = yield* j(this.event, intersectIntervals(e, g), intersectIntervals(f, g), i, g);
      return k;
    }
    *calculateStartDateConstraintIntervals() {
      return this.direction === Direction.Forward ? yield* this.event.calculateEarlyStartDateConstraintIntervals() : yield* this.event.calculateLateStartDateConstraintIntervals();
    }
    *calculateEndDateConstraintIntervals() {
      return this.direction === Direction.Forward ? yield* this.event.calculateEarlyEndDateConstraintIntervals() : yield* this.event.calculateLateEndDateConstraintIntervals();
    }
    *doCalculateEffectiveStartDateInterval(c = false) {
      const e = yield this.$.startDateConstraintIntervals;
      const f = yield this.$.endDateConstraintIntervals;
      return yield* this.calculateEffectiveConstraintInterval(true, e.concat(yield this.event.$.startDateConstraintIntervals), f.concat(yield this.event.$.endDateConstraintIntervals), c);
    }
    *doCalculateEffectiveEndDateInterval(c = false) {
      const e = yield this.$.startDateConstraintIntervals;
      const f = yield this.$.endDateConstraintIntervals;
      return yield* this.calculateEffectiveConstraintInterval(false, e.concat(yield this.event.$.startDateConstraintIntervals), f.concat(yield this.event.$.endDateConstraintIntervals), c);
    }
    *shouldRollupChildStartDate(c) {
      return this.direction === Direction.Forward ? yield* this.event.shouldRollupChildEarlyStartDate(c) : yield* this.event.shouldRollupChildLateStartDate(c);
    }
    *calculateMinChildrenStartDate() {
      let c = MAX_DATE2;
      const e = yield* this.event.subEventsIterable();
      for (const f of e) {
        if (!(yield* this.shouldRollupChildStartDate(f)))
          continue;
        let g;
        if ((yield* f.isManuallyScheduled()) && (yield* f.hasSubEvents())) {
          g = yield this.pick(f).$.minChildrenStartDate;
        }
        g = g || (yield this.pick(f).$.startDate);
        if (g && g < c)
          c = g;
      }
      return c.getTime() - MAX_DATE2.getTime() ? c : null;
    }
    *shouldRollupChildEndDate(c) {
      return this.direction === Direction.Forward ? yield* this.event.shouldRollupChildEarlyEndDate(c) : yield* this.event.shouldRollupChildLateEndDate(c);
    }
    *calculateMaxChildrenEndDate() {
      let c = MIN_DATE2;
      const e = yield* this.event.subEventsIterable();
      for (const f of e) {
        if (!(yield* this.shouldRollupChildEndDate(f)))
          continue;
        let g;
        if ((yield* f.isManuallyScheduled()) && (yield* f.hasSubEvents())) {
          g = yield this.pick(f).$.maxChildrenEndDate;
        }
        g = g || (yield this.pick(f).$.endDate);
        if (g && g > c)
          c = g;
      }
      return c.getTime() - MIN_DATE2.getTime() ? c : null;
    }
    *calculateStartDate() {
      let c;
      if ((yield* this.event.isManuallyScheduled()) && (yield this.event.$.effectiveDirection).direction === this.direction) {
        c = yield this.event.$.startDate;
      } else if (yield* this.event.hasSubEvents()) {
        c = yield this.$.minChildrenStartDate;
      } else if (!(yield* this.isConstrained())) {
        c = yield this.event.$.startDate;
      } else {
        const e = yield* this.doCalculateEffectiveStartDateInterval();
        if (e === null) {
          return null;
        } else if (e.isIntervalEmpty()) {
          const f = yield* this.onEmptyEffectiveInterval('start');
          if (f.kind === 'return') {
            return f.value;
          }
        }
        c = this.direction === Direction.Forward ? isDateFinite(e.startDate) ? e.startDate : null : isDateFinite(e.endDate) ? e.endDate : null;
      }
      return yield* this.maybeSkipNonWorkingTime(c, true);
    }
    *onEmptyEffectiveInterval(c) {
      const e = c === 'start' ? yield* this.doCalculateEffectiveStartDateInterval(true) : yield* this.doCalculateEffectiveEndDateInterval(true);
      const f = ConflictEffect.new({ 'intervals': [...e.intersectionOf] });
      if ((yield f) === EffectResolutionResult.Cancel) {
        yield Reject(f);
        return { 'kind': 'resume' };
      } else {
        return {
          'kind': 'return',
          'value': null
        };
      }
    }
    *calculateEndDate() {
      let c;
      if ((yield* this.event.isManuallyScheduled()) && (yield this.event.$.effectiveDirection).direction === this.direction) {
        c = yield this.event.$.endDate;
      } else if (yield* this.event.hasSubEvents()) {
        c = yield this.$.maxChildrenEndDate;
      } else if (!(yield* this.isConstrained())) {
        c = yield this.event.$.endDate;
      } else {
        const e = yield* this.doCalculateEffectiveEndDateInterval();
        if (e === null) {
          return null;
        } else if (e.isIntervalEmpty()) {
          const f = yield* this.onEmptyEffectiveInterval('end');
          if (f.kind === 'return') {
            return f.value;
          }
        }
        c = this.direction === Direction.Forward ? isDateFinite(e.startDate) ? e.startDate : null : isDateFinite(e.endDate) ? e.endDate : null;
      }
      return yield* this.maybeSkipNonWorkingTime(c, false);
    }
    *isConstrained() {
      const c = yield this.event.$.startDateConstraintIntervals;
      const e = yield this.event.$.endDateConstraintIntervals;
      const f = yield this.$.startDateConstraintIntervals;
      const g = yield this.$.endDateConstraintIntervals;
      return (f == null ? void 0 : f.length) > 0 || (g == null ? void 0 : g.length) > 0 || (c == null ? void 0 : c.length) > 0 || (e == null ? void 0 : e.length) > 0;
    }
  }
  __decorate21([field({ 'lazy': true })], b.prototype, 'minChildrenStartDate', void 0);
  __decorate21([field({ 'lazy': true })], b.prototype, 'maxChildrenEndDate', void 0);
  __decorate21([field({ 'lazy': true })], b.prototype, 'startDateConstraintIntervals', void 0);
  __decorate21([field({ 'lazy': true })], b.prototype, 'endDateConstraintIntervals', void 0);
  __decorate21([calculate('startDateConstraintIntervals')], b.prototype, 'calculateStartDateConstraintIntervals', null);
  __decorate21([calculate('endDateConstraintIntervals')], b.prototype, 'calculateEndDateConstraintIntervals', null);
  __decorate21([calculate('minChildrenStartDate')], b.prototype, 'calculateMinChildrenStartDate', null);
  __decorate21([calculate('maxChildrenEndDate')], b.prototype, 'calculateMaxChildrenEndDate', null);
  __decorate21([calculate('startDate')], b.prototype, 'calculateStartDate', null);
  __decorate21([calculate('endDate')], b.prototype, 'calculateEndDate', null);
  return b;
}) {
};
const __decorate22 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const HasPercentDoneMixin = class extends Mixin([HasChildrenMixin], a => {
  const b = a.prototype;
  class c extends a {
    *isManuallyScheduled() {
      const e = this.getProject();
      let f = yield this.$.manuallyScheduled;
      if (!f) {
        const g = yield e.$.startedTaskScheduling;
        if (g === StartedTaskScheduling.Manual) {
          f = (yield this.$.percentDone) > 0;
        }
      }
      return f;
    }
    *calculatePercentDone() {
      const e = yield this.$.childEvents;
      const f = this.getProject();
      const g = yield f.$.autoCalculatePercentDoneForParentTasks;
      if (e.size && g) {
        const i = yield this.$.percentDoneSummaryData;
        if (i.totalDuration > 0) {
          const j = i.milestonesTotalPercentDone / i.milestonesNum;
          const k = i.completedDuration / i.totalDuration;
          return k < 100 ? k : i.milestonesNum > 0 && j < 100 ? 99 : k;
        } else if (i.milestonesNum > 0) {
          return i.milestonesTotalPercentDone / i.milestonesNum;
        } else {
          return null;
        }
      }
      return yield ProposedOrPrevious;
    }
    *shouldRollupChildPercentDoneSummaryData(e) {
      return true;
    }
    *calculatePercentDoneSummaryData() {
      const e = yield this.$.childEvents;
      if (e.size) {
        const f = {
          'totalDuration': 0,
          'completedDuration': 0,
          'milestonesNum': 0,
          'milestonesTotalPercentDone': 0
        };
        for (const g of e) {
          if (!(yield* this.shouldRollupChildPercentDoneSummaryData(g)))
            continue;
          const i = yield g.$.percentDoneSummaryData;
          if (i) {
            f.totalDuration += i.totalDuration;
            f.completedDuration += i.completedDuration;
            f.milestonesNum += i.milestonesNum;
            f.milestonesTotalPercentDone += i.milestonesTotalPercentDone;
          }
        }
        return f;
      } else {
        const j = yield this.$.duration;
        if (typeof j == 'number') {
          const k = yield* this.getProject().$convertDuration(j, yield this.$.durationUnit, TimeUnit.Millisecond);
          const l = yield this.$.percentDone;
          return {
            'totalDuration': k,
            'completedDuration': k * l,
            'milestonesNum': k === 0 ? 1 : 0,
            'milestonesTotalPercentDone': k === 0 ? l : 0
          };
        } else {
          return null;
        }
      }
    }
  }
  __decorate22([model_field({
      'type': 'number',
      'defaultValue': 0
    })], c.prototype, 'percentDone', void 0);
  __decorate22([field()], c.prototype, 'percentDoneSummaryData', void 0);
  __decorate22([calculate('percentDone')], c.prototype, 'calculatePercentDone', null);
  __decorate22([calculate('percentDoneSummaryData')], c.prototype, 'calculatePercentDoneSummaryData', null);
  return c;
}) {
};
const __decorate23 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const ConstrainedEarlyEventMixin = class extends Mixin([
  HasSubEventsMixin,
  HasPercentDoneMixin
], a => {
  const b = a.prototype;
  class c extends a {
    get earlyPreSchedule() {
      if (this._earlyPreSchedule !== void 0)
        return this._earlyPreSchedule;
      const e = this.constructor.scheduleMixinClass.new();
      e.event = this;
      return this._earlyPreSchedule = e;
    }
    get earlySchedule() {
      return this.earlyPreSchedule;
    }
    enterGraph(e) {
      super.enterGraph(e);
      this.earlyPreSchedule.enterGraph(e);
    }
    leaveGraph(e) {
      this.earlyPreSchedule.leaveGraph(e);
      super.leaveGraph(e);
    }
    unlink() {
      this.earlyPreSchedule.unlink();
      super.unlink();
    }
    *calculateEarlyStartDate() {
      return yield this.earlySchedule.$.startDate;
    }
    *calculateEarlyEndDate() {
      return yield this.earlySchedule.$.endDate;
    }
    *calculateStartDateConstraintIntervals() {
      return [];
    }
    *calculateEndDateConstraintIntervals() {
      return [];
    }
    *calculateEarlyStartDateConstraintIntervals() {
      return [];
    }
    *calculateEarlyEndDateConstraintIntervals() {
      return [];
    }
    *calculateLateStartDateConstraintIntervals() {
      return [];
    }
    *calculateLateEndDateConstraintIntervals() {
      return [];
    }
    *shouldRollupChildEarlyStartDate(e) {
      return true;
    }
    *shouldRollupChildEarlyEndDate(e) {
      return true;
    }
    *shouldRollupChildLateStartDate(e) {
      return true;
    }
    *shouldRollupChildLateEndDate(e) {
      return true;
    }
    *calculateStartDatePure() {
      const e = yield this.$.effectiveDirection;
      let f;
      const g = yield* this.isManuallyScheduled();
      if (!g && !(yield this.$.unscheduled) && (yield* this.isConstrainedAlongDirection(e.direction))) {
        if (yield* this.hasSubEvents()) {
          return yield* this.calculateMinChildrenStartDate();
        }
        f = yield this.pickDirectionSchedule(e.direction).$.startDate;
      }
      return f || (yield* b.calculateStartDatePure.call(this));
    }
    *calculateStartDateProposed() {
      const e = yield this.$.effectiveDirection;
      let f;
      const g = yield* this.isManuallyScheduled();
      if (!g && !(yield this.$.unscheduled) && (yield* this.isConstrainedAlongDirection(e.direction))) {
        if (yield* this.hasSubEvents()) {
          return yield* this.calculateMinChildrenStartDate();
        }
        f = yield this.pickDirectionSchedule(e.direction).$.startDate;
        if (f && !isDateFinite(f)) {
          throw new Error('Fix me');
        }
      }
      return f || (yield* b.calculateStartDateProposed.call(this));
    }
    *calculateEndDatePure() {
      const e = yield this.$.effectiveDirection;
      let f;
      const g = yield* this.isManuallyScheduled();
      if (!g && !(yield this.$.unscheduled) && (yield* this.isConstrainedAlongDirection(e.direction))) {
        if (yield* this.hasSubEvents()) {
          return yield* this.calculateMaxChildrenEndDate();
        }
        f = yield this.pickDirectionSchedule(e.direction).$.endDate;
      }
      return f || (yield* b.calculateEndDatePure.call(this));
    }
    *calculateEndDateProposed() {
      const e = yield this.$.effectiveDirection;
      let f;
      const g = yield* this.isManuallyScheduled();
      if (!g && !(yield this.$.unscheduled) && (yield* this.isConstrainedAlongDirection(e.direction))) {
        if (yield* this.hasSubEvents()) {
          return yield* this.calculateMaxChildrenEndDate();
        }
        f = yield this.pickDirectionSchedule(e.direction).$.endDate;
        if (f && !isDateFinite(f)) {
          throw new Error('Fix me');
        }
      }
      return f || (yield* b.calculateEndDateProposed.call(this));
    }
    *isConstrainedAlongDirection(e) {
      return yield* this.earlyPreSchedule.isConstrained();
    }
    pickDirectionSchedule(e) {
      return this.earlyPreSchedule;
    }
  }
  c.scheduleMixinClass = ConstrainedScheduleMixin;
  __decorate23([model_field({
      'type': 'date',
      'persist': false,
      'calculated': true
    }, {
      'lazy': true,
      'converter': dateConverter,
      'persistent': false
    })], c.prototype, 'earlyStartDate', void 0);
  __decorate23([model_field({
      'type': 'date',
      'persist': false,
      'calculated': true
    }, {
      'lazy': true,
      'converter': dateConverter,
      'persistent': false
    })], c.prototype, 'earlyEndDate', void 0);
  __decorate23([field()], c.prototype, 'startDateConstraintIntervals', void 0);
  __decorate23([field()], c.prototype, 'endDateConstraintIntervals', void 0);
  __decorate23([calculate('earlyStartDate')], c.prototype, 'calculateEarlyStartDate', null);
  __decorate23([calculate('earlyEndDate')], c.prototype, 'calculateEarlyEndDate', null);
  __decorate23([calculate('startDateConstraintIntervals')], c.prototype, 'calculateStartDateConstraintIntervals', null);
  __decorate23([calculate('endDateConstraintIntervals')], c.prototype, 'calculateEndDateConstraintIntervals', null);
  return c;
}) {
};
const __decorate24 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const HasDateConstraintMixin = class extends Mixin([
  ConstrainedEarlyEventMixin,
  HasChildrenMixin
], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.ignorePinningConstraint = false;
    }
    writeStartDate(e, f, g, i, j = true) {
      const k = this.getProject();
      if ((k == null ? void 0 : k.addConstraintOnDateSet) && f.graph.hasIdentifier(this.$.effectiveDirection) && !(k == null ? void 0 : k.eventStore.isSyncingDataOnLoad) && !this.isReverting && !(k == null ? void 0 : k.getStm().isRestoring)) {
        const l = this.getStartDatePinConstraintType();
        if (l) {
          this.constraintDate = i;
          this.constraintType = l;
        }
      }
      return b.writeStartDate.call(this, e, f, g, i, j);
    }
    writeEndDate(e, f, g, i, j = false) {
      const k = this.getProject();
      if ((k == null ? void 0 : k.addConstraintOnDateSet) && f.graph.hasIdentifier(this.$.direction) && j && !(k == null ? void 0 : k.eventStore.isSyncingDataOnLoad) && !(k == null ? void 0 : k.getStm().isRestoring)) {
        const l = this.getEndDatePinConstraintType();
        if (l) {
          this.constraintDate = i;
          this.constraintType = l;
        }
      }
      return b.writeEndDate.call(this, e, f, g, i, j);
    }
    writeConstraintType(e, f, g, i, j = false) {
      const k = this.getProject();
      if (!j && !e.isWritingUndefined && k.includeAsapAlapAsConstraints) {
        if (i !== ConstraintType.AsSoonAsPossible && i !== ConstraintType.AsLateAsPossible) {
          if (f.baseRevision.hasIdentifier(e)) {
            this.$.direction.write.call(this, this.$.direction, f, void 0, null, true);
          }
        } else {
          this.$.direction.write.call(this, this.$.direction, f, void 0, i === ConstraintType.AsSoonAsPossible ? Direction.Forward : Direction.Backward, true);
          this.$.constraintDate.write.call(this, this.$.constraintDate, f, void 0, null);
        }
      }
      e.constructor.prototype.write.call(this, e, f, g, i);
    }
    writeDirection(e, f, g, i, j = false) {
      const k = this.getProject();
      if (!j && !e.isWritingUndefined && k.includeAsapAlapAsConstraints && (i || f.baseRevision.hasIdentifier(e))) {
        this.$.constraintType.write.call(this, this.$.constraintType, f, void 0, i === Direction.Forward ? ConstraintType.AsSoonAsPossible : i === Direction.Backward ? ConstraintType.AsLateAsPossible : null, true);
      }
      e.constructor.prototype.write.call(this, e, f, g, i);
    }
    *calculateConstraintType() {
      let e = yield ProposedOrPrevious;
      if (!(yield* this.isConstraintTypeApplicable(e))) {
        e = null;
      }
      return e;
    }
    *calculateConstraintDate(e) {
      let f = yield ProposedOrPrevious;
      const g = yield this.$.constraintType;
      if (!g) {
        f = null;
      } else if (!f) {
        f = this.getConstraintTypeDefaultDate(e, g);
      }
      return f;
    }
    getStartDatePinConstraintType() {
      const {effectiveDirection: e} = this.project;
      if (!this.isTaskPinnableWithConstraint())
        return null;
      switch (e.direction) {
      case Direction.Forward:
        return ConstraintType.StartNoEarlierThan;
      case Direction.Backward:
        return ConstraintType.StartNoLaterThan;
      }
    }
    getEndDatePinConstraintType() {
      const {effectiveDirection: e} = this.project;
      if (!this.isTaskPinnableWithConstraint())
        return null;
      switch (e.direction) {
      case Direction.Forward:
        return ConstraintType.FinishNoEarlierThan;
      case Direction.Backward:
        return ConstraintType.FinishNoLaterThan;
      }
    }
    _isManuallyScheduled() {
      let e = this.manuallyScheduled;
      if (!e) {
        const f = this.project.startedTaskScheduling;
        if (f === StartedTaskScheduling.Manual) {
          e = this.percentDone > 0;
        }
      }
      return e;
    }
    isTaskPinnableWithConstraint() {
      const {
        ignorePinningConstraint: e,
        constraintType: f
      } = this;
      let g = false;
      if (!this._isManuallyScheduled() && !e) {
        if (f) {
          switch (f) {
          case ConstraintType.AsSoonAsPossible:
          case ConstraintType.AsLateAsPossible:
          case ConstraintType.StartNoEarlierThan:
          case ConstraintType.StartNoLaterThan:
          case ConstraintType.FinishNoEarlierThan:
          case ConstraintType.FinishNoLaterThan:
            g = true;
          }
        } else {
          g = true;
        }
      }
      return g;
    }
    applyChangeset(e, f, g) {
      this.ignorePinningConstraint = g;
      const i = super.applyChangeset(e, f, g);
      this.ignorePinningConstraint = false;
      return i;
    }
    getConstraintTypeDefaultDate(e, f) {
      switch (f) {
      case ConstraintType.StartNoEarlierThan:
      case ConstraintType.StartNoLaterThan:
      case ConstraintType.MustStartOn:
        return e(ProposedOrPreviousValueOf(this.$.startDate));
      case ConstraintType.FinishNoEarlierThan:
      case ConstraintType.FinishNoLaterThan:
      case ConstraintType.MustFinishOn:
        return e(ProposedOrPreviousValueOf(this.$.endDate));
      }
      return null;
    }
    *isConstraintTypeApplicable(e) {
      const f = yield* this.hasSubEvents();
      switch (e) {
      case ConstraintType.FinishNoEarlierThan:
      case ConstraintType.StartNoLaterThan:
      case ConstraintType.MustFinishOn:
      case ConstraintType.MustStartOn:
        return !f;
      }
      return true;
    }
    async setConstraint(e, f) {
      this.constraintType = e;
      if (f !== void 0) {
        this.constraintDate = f;
      }
      return this.commitAsync();
    }
    *calculateEndDateConstraintIntervals() {
      const e = yield* b.calculateEndDateConstraintIntervals.call(this);
      const f = yield* this.isManuallyScheduled();
      const g = yield this.$.constraintType;
      const i = yield this.$.constraintDate;
      const j = this.project.dateConstraintIntervalClass;
      if (!f && g && i) {
        switch (g) {
        case ConstraintType.MustFinishOn:
          e.unshift(j.new({
            'owner': this,
            'side': ConstraintIntervalSide.End,
            'startDate': i,
            'endDate': i
          }));
          break;
        case ConstraintType.FinishNoEarlierThan:
          e.unshift(j.new({
            'owner': this,
            'side': ConstraintIntervalSide.End,
            'startDate': i
          }));
          break;
        case ConstraintType.FinishNoLaterThan:
          e.unshift(j.new({
            'owner': this,
            'side': ConstraintIntervalSide.End,
            'endDate': i
          }));
          break;
        }
      }
      return e;
    }
    *calculateStartDateConstraintIntervals() {
      const e = yield* b.calculateStartDateConstraintIntervals.call(this);
      const f = yield* this.isManuallyScheduled();
      const g = yield this.$.constraintType;
      const i = yield this.$.constraintDate;
      const j = this.project.dateConstraintIntervalClass;
      if (!f && g && i) {
        switch (g) {
        case ConstraintType.MustStartOn:
          e.unshift(j.new({
            'owner': this,
            'side': ConstraintIntervalSide.Start,
            'startDate': i,
            'endDate': i
          }));
          break;
        case ConstraintType.StartNoEarlierThan:
          e.unshift(j.new({
            'owner': this,
            'side': ConstraintIntervalSide.Start,
            'startDate': i
          }));
          break;
        case ConstraintType.StartNoLaterThan:
          e.unshift(j.new({
            'owner': this,
            'side': ConstraintIntervalSide.Start,
            'endDate': i
          }));
          break;
        }
      }
      return e;
    }
  }
  __decorate24([model_field({ 'type': 'string' }, { 'sync': true })], c.prototype, 'constraintType', void 0);
  __decorate24([model_field({ 'type': 'date' }, {
      'converter': dateConverter,
      'sync': true
    })], c.prototype, 'constraintDate', void 0);
  __decorate24([write('constraintType')], c.prototype, 'writeConstraintType', null);
  __decorate24([write('direction')], c.prototype, 'writeDirection', null);
  __decorate24([calculate('constraintType')], c.prototype, 'calculateConstraintType', null);
  __decorate24([calculate('constraintDate')], c.prototype, 'calculateConstraintDate', null);
  return c;
}) {
};
const RemoveDateConstraintConflictResolution = class extends Localizable_default(ConflictResolution) {
  static get $name() {
    return 'RemoveDateConstraintConflictResolution';
  }
  construct() {
    super.construct(...arguments);
    this.event = this.interval.owner;
  }
  getDescription() {
    const {event: a} = this;
    return format(this.L('L{descriptionTpl}'), a.name || a.id, this.interval.getConstraintName(a.constraintType));
  }
  resolve() {
    this.event.constraintType = null;
    this.event.constraintDate = null;
  }
};
const DateConstraintIntervalDescription = class extends ConstraintIntervalDescription {
  static get $name() {
    return 'DateConstraintIntervalDescription';
  }
  static getDescription(a) {
    let b;
    switch (a.owner.constraintType) {
    case ConstraintType.StartNoEarlierThan:
    case ConstraintType.FinishNoEarlierThan:
    case ConstraintType.MustStartOn:
    case ConstraintType.MustFinishOn:
      b = this.L('L{startDateDescriptionTpl}');
      break;
    case ConstraintType.StartNoLaterThan:
    case ConstraintType.FinishNoLaterThan:
      b = this.L('L{endDateDescriptionTpl}');
      break;
    }
    return format(b, ...this.getDescriptionParameters(a));
  }
  static getConstraintName(a) {
    return this.L('L{constraintTypeTpl}')[a];
  }
  static getDescriptionParameters(a) {
    const b = a.owner;
    return [
      DateHelper.format(a.startDate, this.L('L{dateFormat}')),
      DateHelper.format(a.endDate, this.L('L{dateFormat}')),
      b.name || b.id,
      this.getConstraintName(b.constraintType)
    ];
  }
};
const DateConstraintInterval = class extends ConstraintInterval {
  get isDateConstraintInterval() {
    return true;
  }
  getConstraintName(a) {
    return this.descriptionBuilderClass.getConstraintName(a || this.owner.constraintType);
  }
  getDescription() {
    return this.descriptionBuilderClass.getDescription(this);
  }
  isAffectedByTransaction(a) {
    const b = this.owner;
    a = a || b.graph.activeTransaction;
    const c = a.entries.get(b.$.constraintDate), e = a.entries.get(b.$.constraintType);
    return !a.baseRevision.hasIdentifier(b.$$) || c && !c.isShadow() || e && !e.isShadow();
  }
  getResolutions() {
    return this.resolutions || (this.resolutions = [this.removeDateConstraintConflictResolutionClass.new({ 'interval': this })]);
  }
};
__decorate24([prototypeValue(RemoveDateConstraintConflictResolution)], DateConstraintInterval.prototype, 'removeDateConstraintConflictResolutionClass', void 0);
__decorate24([prototypeValue(DateConstraintIntervalDescription)], DateConstraintInterval.prototype, 'descriptionBuilderClass', void 0);
const __decorate25 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const ConstrainedByDependenciesEventScheduleMixin = class extends Mixin([ConstrainedScheduleMixin], a => {
  const b = a.prototype;
  class c extends a {
    *shouldPushingDepAffectScheduling(e) {
      if (this.direction === Direction.Forward && !(yield* this.event.shouldPredecessorAffectScheduling(e)) || this.direction === Direction.Backward && !(yield* this.event.shouldSuccessorAffectScheduling(e))) {
        return false;
      }
      const f = yield this.pickPushingEventIdentifier(e);
      return f && !isAtomicValue(f) && (yield e.$.active) && (!(yield f.$.inactive) || (yield this.event.$.inactive));
    }
    pickPushingDepsIdentifier(e) {
      if (this.direction === Direction.Forward) {
        return e.$.incomingDeps;
      } else if (this.direction === Direction.Backward) {
        return e.$.outgoingDeps;
      } else {
        throw new Error('Should not be here');
      }
    }
    pickPushingEventIdentifier(e) {
      if (this.direction === Direction.Forward) {
        return e.$.fromEvent;
      } else if (this.direction === Direction.Backward) {
        return e.$.toEvent;
      } else {
        throw new Error('Should not be here');
      }
    }
    *pickPushingStartDateIdentifier(e) {
      return this.pick(e).$.startDate;
    }
    *pickPushingEndDateIdentifier(e) {
      return this.pick(e).$.endDate;
    }
    *calculateStartDateConstraintIntervals() {
      const e = yield* b.calculateStartDateConstraintIntervals.call(this);
      const f = this.getProject();
      const g = f.dependencyConstraintIntervalClass;
      const i = yield this.pickPushingDepsIdentifier(this.event);
      for (const j of i) {
        if (!(yield* this.shouldPushingDepAffectScheduling(j)))
          continue;
        const k = yield this.pickPushingEventIdentifier(j);
        const l = yield* k.isManuallyScheduled();
        const n = (yield this.event.$.effectiveDirection).direction;
        let o;
        switch (yield j.$.type) {
        case DependencyType.StartToEnd:
          if (this.direction === Direction.Backward) {
            o = l && n === this.direction ? yield k.$.endDate : yield yield* this.pickPushingEndDateIdentifier(k);
          }
          break;
        case DependencyType.EndToStart:
          if (this.direction === Direction.Forward) {
            o = l && n === this.direction ? yield k.$.endDate : yield yield* this.pickPushingEndDateIdentifier(k);
          }
          break;
        case DependencyType.StartToStart:
          o = l && n === this.direction ? yield k.$.startDate : yield yield* this.pickPushingStartDateIdentifier(k);
          break;
        }
        if (o) {
          const p = yield j.$.lag;
          const q = yield j.$.lagUnit;
          const r = yield j.$.calendar;
          yield r.$.version;
          const u = this.direction === Direction.Forward ? g.new({
            'owner': j,
            'startDate': p !== 0 ? r.calculateEndDate(o, yield* f.$convertDuration(p, q, TimeUnit.Millisecond)) : o,
            'endDate': null
          }) : g.new({
            'owner': j,
            'startDate': null,
            'endDate': p !== 0 ? r.skipNonWorkingTime(r.calculateStartDate(o, yield* f.$convertDuration(p, q, TimeUnit.Millisecond))) : o
          });
          e.unshift(u);
        }
      }
      return e;
    }
    *calculateEndDateConstraintIntervals() {
      const e = yield* b.calculateEndDateConstraintIntervals.call(this);
      const f = this.getProject();
      const g = f.dependencyConstraintIntervalClass;
      const i = yield this.pickPushingDepsIdentifier(this.event);
      for (const j of i) {
        if (!(yield* this.shouldPushingDepAffectScheduling(j)))
          continue;
        const k = yield this.pickPushingEventIdentifier(j);
        const l = yield* k.isManuallyScheduled();
        const n = (yield this.event.$.effectiveDirection).direction;
        let o;
        switch (yield j.$.type) {
        case DependencyType.EndToEnd:
          o = l && n === this.direction ? yield k.$.endDate : yield yield* this.pickPushingEndDateIdentifier(k);
          break;
        case DependencyType.StartToEnd:
          if (this.direction === Direction.Forward) {
            o = l && n === this.direction ? yield k.$.startDate : yield yield* this.pickPushingStartDateIdentifier(k);
          }
          break;
        case DependencyType.EndToStart:
          if (this.direction === Direction.Backward) {
            o = l && n === this.direction ? yield k.$.startDate : yield yield* this.pickPushingStartDateIdentifier(k);
          }
          break;
        }
        if (o) {
          const p = yield j.$.lag;
          const q = yield j.$.lagUnit;
          const r = yield j.$.calendar;
          yield r.$.version;
          const u = this.direction === Direction.Forward ? g.new({
            'owner': j,
            'startDate': p !== 0 ? r.calculateEndDate(o, yield* f.$convertDuration(p, q, TimeUnit.Millisecond)) : o,
            'endDate': null
          }) : g.new({
            'owner': j,
            'startDate': null,
            'endDate': p !== 0 ? r.skipNonWorkingTime(r.calculateStartDate(o, yield* f.$convertDuration(p, q, TimeUnit.Millisecond))) : o
          });
          e.unshift(u);
        }
      }
      return e;
    }
  }
  __decorate25([calculate('startDateConstraintIntervals')], c.prototype, 'calculateStartDateConstraintIntervals', null);
  return c;
}) {
};
const __decorate26 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const ConstrainedByDependenciesEventMixin = class extends Mixin([
  ConstrainedEarlyEventMixin,
  HasDependenciesMixin,
  HasChildrenMixin,
  HasDateConstraintMixin
], a => {
  class b extends a {
    constructor() {
      super(...arguments);
      this.isInitialConstraintCalculation = 3;
    }
    *shouldPredecessorAffectScheduling(c) {
      return true;
    }
    *shouldSuccessorAffectScheduling(c) {
      return true;
    }
    *shouldAutoSetConstraintOnLoad() {
      return this.project.autoSetConstraints && (yield this.$.incomingDeps).size === 0 && (yield this.$.childEvents).size === 0 && !(yield this.$.manuallyScheduled);
    }
    *calculateConstraintType() {
      let c;
      if (this.isInitialConstraintCalculation & 1) {
        c = yield UnsafeProposedOrPreviousValueOf(this.$.constraintType);
        if (c == null && (yield* this.shouldAutoSetConstraintOnLoad())) {
          c = ConstraintType.StartNoEarlierThan;
        } else {
          c = yield* super.calculateConstraintType();
        }
      } else {
        c = yield* super.calculateConstraintType();
      }
      this.isInitialConstraintCalculation &= 2;
      return c;
    }
    *calculateConstraintDate(c) {
      let e;
      if (this.isInitialConstraintCalculation & 2) {
        e = yield UnsafeProposedOrPreviousValueOf(this.$.constraintDate);
        if (e == null && (yield* this.shouldAutoSetConstraintOnLoad())) {
          e = yield UnsafeProposedOrPreviousValueOf(this.$.startDate);
        } else {
          e = yield* super.calculateConstraintDate(c);
        }
      } else {
        e = yield* super.calculateConstraintDate(c);
      }
      this.isInitialConstraintCalculation &= 1;
      return e;
    }
  }
  b.scheduleMixinClass = Mixin([
    a.scheduleMixinClass,
    ConstrainedByDependenciesEventScheduleMixin
  ], c => c);
  __decorate26([model_field({ 'type': 'boolean' })], b.prototype, 'inactive', void 0);
  return b;
}) {
};
const BaseDependencyResolution = class extends Localizable_default(ConflictResolution) {
  static get $name() {
    return 'BaseDependencyResolution';
  }
  getDescription() {
    const {dependency: a} = this, {
        type: b,
        fromEvent: c,
        toEvent: e
      } = a;
    return format(this.L('L{descriptionTpl}'), this.L('L{DependencyType.long}')[b], c.name || c.id, e.name || e.id);
  }
};
const RemoveDependencyResolution = class extends BaseDependencyResolution {
  static get $name() {
    return 'RemoveDependencyResolution';
  }
  resolve() {
    this.dependency.remove();
  }
};
const DeactivateDependencyResolution = class extends BaseDependencyResolution {
  static get $name() {
    return 'DeactivateDependencyResolution';
  }
  resolve() {
    this.dependency.active = false;
  }
};
const DependencyConstraintIntervalDescription = class extends ConstraintIntervalDescription {
  static get $name() {
    return 'DependencyConstraintIntervalDescription';
  }
  static getDescriptionParameters(a) {
    const b = a.owner;
    return [
      DateHelper.format(a.startDate, this.L('L{dateFormat}')),
      DateHelper.format(a.endDate, this.L('L{dateFormat}')),
      this.L('L{DependencyType.long}')[b.type],
      b.fromEvent.name,
      b.toEvent.name
    ];
  }
};
const DependencyConstraintInterval = class extends ConstraintInterval {
  get isDependencyConstraintInterval() {
    return true;
  }
  isAffectedByTransaction(a) {
    const b = this.owner;
    a = a || b.graph.activeTransaction;
    const {entries: c} = a, {
        fromEvent: e,
        toEvent: f,
        lag: g,
        lagUnit: i,
        type: j
      } = b.$, k = c.get(e), l = c.get(f), n = c.get(g), o = c.get(i), p = c.get(j);
    return !a.baseRevision.hasIdentifier(b.$$) || k && !k.isShadow() || l && !l.isShadow() || n && !n.isShadow() || o && !o.isShadow() || p && !p.isShadow();
  }
  getResolutions() {
    return this.resolutions || (this.resolutions = [
      this.deactivateDependencyConflictResolutionClass.new({ 'dependency': this.owner }),
      this.removeDependencyConflictResolutionClass.new({ 'dependency': this.owner })
    ]);
  }
};
__decorate26([prototypeValue(RemoveDependencyResolution)], DependencyConstraintInterval.prototype, 'removeDependencyConflictResolutionClass', void 0);
__decorate26([prototypeValue(DeactivateDependencyResolution)], DependencyConstraintInterval.prototype, 'deactivateDependencyConflictResolutionClass', void 0);
__decorate26([prototypeValue(DependencyConstraintIntervalDescription)], DependencyConstraintInterval.prototype, 'descriptionBuilderClass', void 0);
const __decorate27 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const SchedulerProAssignmentMixin = class extends Mixin([BaseAssignmentMixin], a => {
  const b = a.prototype;
  class c extends a {
    *calculateUnits() {
      const e = yield this.$.event;
      if (e)
        return yield* e.calculateAssignmentUnits(this);
      return yield ProposedOrPrevious;
    }
    *calculateEffort() {
      const e = yield this.$.event;
      if (e) {
        const f = yield e.$.startDate;
        const g = yield e.$.endDate;
        const i = yield e.$.effectiveCalendar;
        if (f && g) {
          const j = new Map();
          j.set(i, [this]);
          return yield* e.calculateProjectedEffort(f, g, j);
        }
      }
      return null;
    }
    *calculateActualDate() {
      const e = yield this.$.event;
      if (e) {
        const f = yield e.$.startDate;
        const g = yield e.$.duration;
        const i = yield e.$.percentDone;
        return yield* e.calculateProjectedXDateWithDuration(f, true, g * 0.01 * i);
      }
      return null;
    }
    *calculateActualEffort() {
      const e = yield this.$.event;
      if (e) {
        const f = yield e.$.startDate;
        const g = yield e.$.effectiveCalendar;
        const i = yield this.$.actualDate;
        const j = new Map();
        j.set(g, [this]);
        return yield* e.calculateProjectedEffort(f, i, j);
      }
      return null;
    }
  }
  __decorate27([model_field({
      'type': 'number',
      'defaultValue': 100
    })], c.prototype, 'units', void 0);
  __decorate27([calculate('units')], c.prototype, 'calculateUnits', null);
  __decorate27([field({ 'lazy': true })], c.prototype, 'effort', void 0);
  __decorate27([field({ 'lazy': true })], c.prototype, 'actualDate', void 0);
  __decorate27([field({ 'lazy': true })], c.prototype, 'actualEffort', void 0);
  __decorate27([calculate('effort')], c.prototype, 'calculateEffort', null);
  __decorate27([calculate('actualDate')], c.prototype, 'calculateActualDate', null);
  __decorate27([calculate('actualEffort')], c.prototype, 'calculateActualEffort', null);
  return c;
}) {
};
const __decorate28 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const SchedulerProDependencyMixin = class extends Mixin([BaseDependencyMixin], a => {
  const b = a.prototype;
  class c extends a {
    *calculateCalendar() {
      const e = this.getProject();
      const f = yield e.$.dependenciesCalendar;
      let g;
      switch (f) {
      case DependenciesCalendar.Project:
        g = yield e.$.effectiveCalendar;
        break;
      case DependenciesCalendar.FromEvent:
        const i = yield this.$.fromEvent;
        g = i && !isAtomicValue(i) ? yield i.$.effectiveCalendar : null;
        break;
      case DependenciesCalendar.ToEvent:
        const j = yield this.$.toEvent;
        g = j && !isAtomicValue(j) ? yield j.$.effectiveCalendar : null;
        break;
      }
      if (!g)
        g = yield e.$.effectiveCalendar;
      return g;
    }
    async setLag(e, f) {
      if (this.graph) {
        this.graph.write(this.$.lag, e, f);
        return this.graph.commitAsync();
      } else {
        this.$.lag.DATA = e;
        if (f != null)
          this.$.lagUnit.DATA = f;
      }
    }
    writeLag(e, f, g, i, j = void 0) {
      e.constructor.prototype.write.call(this, e, f, g, i);
      if (j != null)
        f.write(this.$.lagUnit, j);
    }
  }
  __decorate28([model_field({
      'type': 'number',
      'defaultValue': 0
    })], c.prototype, 'lag', void 0);
  __decorate28([model_field({
      'type': 'string',
      'defaultValue': TimeUnit.Day
    }, { 'converter': DateHelper.normalizeUnit })], c.prototype, 'lagUnit', void 0);
  __decorate28([field()], c.prototype, 'calendar', void 0);
  __decorate28([model_field({
      'type': 'boolean',
      'defaultValue': true,
      'persist': true
    })], c.prototype, 'active', void 0);
  __decorate28([calculate('calendar')], c.prototype, 'calculateCalendar', null);
  __decorate28([write('lag')], c.prototype, 'writeLag', null);
  return c;
}) {
};
const __decorate29 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const SchedulerProHasAssignmentsMixin = class extends Mixin([BaseHasAssignmentsMixin], a => {
  const b = a.prototype;
  class c extends a {
    *hasProposedValueForUnits() {
      const e = yield this.$.assigned;
      for (const f of e) {
        const g = yield f.$.resource;
        if (g && (yield HasProposedValue(f.$.units)))
          return true;
      }
      return false;
    }
    async assign(e, f = 100) {
      const g = this.getProject().assignmentStore.modelClass;
      this.addAssignment(new g({
        'event': this,
        'resource': e,
        'units': f
      }));
      return this.commitAsync();
    }
    *forEachAvailabilityInterval(e, f) {
      const g = yield this.$.effectiveCalendar;
      const i = yield this.$.assignmentsByCalendar;
      const j = yield this.$.effectiveCalendarsCombination;
      const k = (yield this.$.ignoreResourceCalendar) || e.ignoreResourceCalendar || !i.size;
      const l = this.getProject().maxCalendarRange;
      if (l) {
        e = Object.assign({ 'maxRange': l }, e);
      }
      return j.forEachAvailabilityInterval(e, (n, o, p) => {
        const q = p.getCalendarsWorkStatus();
        const r = p.getCalendarsWorking();
        if (q.get(g) && (k || r.some(u => i.has(u)))) {
          return f(n, o, p);
        }
      });
    }
    *calculateEffectiveCalendarsCombination() {
      const e = yield* this.isManuallyScheduled();
      const f = this.getProject();
      const g = [yield this.$.effectiveCalendar];
      if (!e || f.skipNonWorkingTimeInDurationWhenSchedulingManually) {
        const i = yield this.$.assignmentsByCalendar;
        g.push(...i.keys());
      }
      return this.getProject().combineCalendars(g);
    }
    *calculateAssignmentsByCalendar() {
      const e = yield this.$.assigned;
      const f = yield this.$.ignoreResourceCalendar;
      const g = new Map();
      for (const i of e) {
        const j = yield i.$.resource;
        if (j) {
          const k = yield f ? this.$.effectiveCalendar : j.$.effectiveCalendar;
          let l = g.get(k);
          if (!l) {
            l = [];
            g.set(k, l);
          }
          l.push(i);
        }
      }
      return g;
    }
    *getBaseOptionsForDurationCalculations() {
      return { 'ignoreResourceCalendar': false };
    }
    *useEventAvailabilityIterator() {
      const e = yield this.$.assignmentsByCalendar;
      return e.size > 0;
    }
    *skipNonWorkingTime(e, f = true, g) {
      if (!e)
        return null;
      const i = yield this.$.assignmentsByCalendar;
      const j = yield this.$.ignoreResourceCalendar;
      if (yield* this.useEventAvailabilityIterator()) {
        const k = Object.assign(yield* this.getBaseOptionsForDurationCalculations(), f ? {
          'startDate': e,
          'isForward': f
        } : {
          'endDate': e,
          'isForward': f
        }, g);
        let l;
        const n = yield* this.forEachAvailabilityInterval(k, (o, p, q) => {
          l = f ? o : p;
          return false;
        });
        if (n === CalendarIteratorResult.MaxRangeReached || n === CalendarIteratorResult.FullRangeIterated) {
          const o = [yield this.$.effectiveCalendar];
          if (!k.ignoreResourceCalendar && !j) {
            o.push(...i.keys());
          }
          const p = SchedulerProEmptyCalendarEffect.new({
            'event': this,
            'calendars': o,
            'date': e,
            'isForward': f
          });
          if ((yield p) === EffectResolutionResult.Cancel) {
            yield Reject(p);
          } else {
            return null;
          }
        }
        return new Date(l);
      } else {
        return yield* b.skipNonWorkingTime.call(this, e, f);
      }
    }
    *calculateProjectedDuration(e, f, g, i) {
      if (!e || !f) {
        return null;
      }
      if (yield* this.useEventAvailabilityIterator()) {
        const j = Object.assign(yield* this.getBaseOptionsForDurationCalculations(), {
          'startDate': e,
          'endDate': f,
          'isForward': true
        }, i);
        const k = this.getProject().adjustDurationToDST;
        let l = 0;
        yield* this.forEachAvailabilityInterval(j, (n, o) => {
          l += o.getTime() - n.getTime();
          if (k) {
            const p = n.getTimezoneOffset() - o.getTimezoneOffset();
            l += p * 60 * 1000;
          }
        });
        if (!g)
          g = yield this.$.durationUnit;
        return yield* this.getProject().$convertDuration(l, TimeUnit.Millisecond, g);
      } else {
        return yield* b.calculateProjectedDuration.call(this, e, f, g);
      }
    }
    *calculateProjectedXDateWithDuration(e, f = true, g, i, j) {
      if (g == null || isNaN(g) || e == null)
        return null;
      if (g == 0)
        return e;
      i = i || (yield this.$.durationUnit);
      const k = yield* this.getProject().$convertDuration(g, i, TimeUnit.Millisecond);
      let l = e.getTime();
      let n = k;
      const o = yield this.$.effectiveCalendar;
      if (yield* this.useEventAvailabilityIterator()) {
        const p = Object.assign(yield* this.getBaseOptionsForDurationCalculations(), f ? {
          'startDate': e,
          'isForward': f
        } : {
          'endDate': e,
          'isForward': f
        }, j);
        const q = this.getProject().adjustDurationToDST;
        const r = yield* this.forEachAvailabilityInterval(p, (u, v, w) => {
          const z = u.getTime(), A = v.getTime(), B = A - z;
          if (B >= n) {
            if (q) {
              const C = f ? u.getTimezoneOffset() - new Date(z + n).getTimezoneOffset() : new Date(A - n).getTimezoneOffset() - v.getTimezoneOffset();
              n -= C * 60 * 1000;
            }
            l = f ? z + n : A - n;
            return false;
          } else {
            n -= B;
            if (q) {
              const E = u.getTimezoneOffset() - v.getTimezoneOffset();
              n -= E * 60 * 1000;
            }
          }
        });
        return r === CalendarIteratorResult.StoppedByIterator ? new Date(l) : null;
      } else {
        return o.accumulateWorkingTime(e, k, f).finalDate;
      }
    }
  }
  __decorate29([field()], c.prototype, 'effectiveCalendarsCombination', void 0);
  __decorate29([field()], c.prototype, 'assignmentsByCalendar', void 0);
  __decorate29([model_field({ 'type': 'boolean' })], c.prototype, 'ignoreResourceCalendar', void 0);
  __decorate29([calculate('effectiveCalendarsCombination')], c.prototype, 'calculateEffectiveCalendarsCombination', null);
  __decorate29([calculate('assignmentsByCalendar')], c.prototype, 'calculateAssignmentsByCalendar', null);
  return c;
}) {
};
var SchedulerProEmptyCalendarEffect = class extends EmptyCalendarEffect {
  getResolutions() {
    if (!this.getInvalidCalendar()) {
      return [IgnoreResourceCalendarEmptyCalendarEffectResolution.new({ 'event': this.event })];
    }
    return super.getResolutions();
  }
};
var IgnoreResourceCalendarEmptyCalendarEffectResolution = class extends Localizable_default(SchedulingIssueEffectResolution) {
  static get $name() {
    return 'IgnoreResourceCalendarEmptyCalendarEffectResolution';
  }
  getDescription() {
    const {event: a} = this;
    return format(this.L('L{descriptionTpl}'), a.name);
  }
  resolve() {
    this.event.ignoreResourceCalendar = true;
  }
};
const __decorate30 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const MasterStartDateVar = Symbol('MasterStartDateVar');
const MasterEndDateVar = Symbol('MasterEndDateVar');
const MasterDurationVar = Symbol('MasterDurationVar');
const MasterTotalDurationVar = Symbol('MasterTotalDurationVar');
const StartOffsetVar = Symbol('StartOffsetVar');
const EndOffsetVar = Symbol('EndOffsetVar');
const durationByOffsetsFormula = Formula.new({
  'output': DurationVar,
  'inputs': new Set([
    StartOffsetVar,
    EndOffsetVar
  ])
});
const startDateByMasterStartAndStartOffsetFormula = Formula.new({
  'output': StartDateVar,
  'inputs': new Set([
    StartOffsetVar,
    MasterStartDateVar
  ])
});
const endDateByMasterStartAndEndOffsetFormula = Formula.new({
  'output': EndDateVar,
  'inputs': new Set([
    EndOffsetVar,
    MasterStartDateVar
  ])
});
const startOffsetByMasterStartAndStartDateFormula = Formula.new({
  'output': StartOffsetVar,
  'inputs': new Set([
    StartDateVar,
    MasterStartDateVar
  ])
});
const endOffsetByMasterStartAndEndDateFormula = Formula.new({
  'output': EndOffsetVar,
  'inputs': new Set([
    EndDateVar,
    MasterStartDateVar
  ])
});
const startOffsetByEndOffsetAndDurationFormula = Formula.new({
  'output': StartOffsetVar,
  'inputs': new Set([
    EndOffsetVar,
    DurationVar
  ])
});
const endOffsetByStartOffsetAndDurationFormula = Formula.new({
  'output': EndOffsetVar,
  'inputs': new Set([
    StartOffsetVar,
    DurationVar
  ])
});
const endOffsetByMasterTotalDurationAndStartOffsetFormula = Formula.new({
  'output': EndOffsetVar,
  'inputs': new Set([
    StartOffsetVar,
    MasterTotalDurationVar
  ])
});
const endOffsetByMasterDurationAndStartOffsetFormula = Formula.new({
  'output': EndOffsetVar,
  'inputs': new Set([
    StartOffsetVar,
    MasterDurationVar
  ])
});
const durationByMasterEndDateFormula = Formula.new({
  'output': DurationVar,
  'inputs': new Set([
    StartOffsetVar,
    MasterEndDateVar
  ])
});
const segmentCycleDescription = CycleDescription.new({
  'variables': new Set([
    StartDateVar,
    EndDateVar,
    DurationVar,
    MasterStartDateVar,
    MasterEndDateVar,
    MasterDurationVar,
    MasterTotalDurationVar,
    StartOffsetVar,
    EndOffsetVar
  ]),
  'formulas': new Set([
    durationByOffsetsFormula,
    startDateByMasterStartAndStartOffsetFormula,
    endDateByMasterStartAndEndOffsetFormula,
    startOffsetByEndOffsetAndDurationFormula,
    startOffsetByMasterStartAndStartDateFormula,
    endOffsetByStartOffsetAndDurationFormula,
    endOffsetByMasterTotalDurationAndStartOffsetFormula,
    endOffsetByMasterDurationAndStartOffsetFormula,
    endOffsetByMasterStartAndEndDateFormula,
    durationByMasterEndDateFormula
  ])
});
const segmentCycleResolution = CycleResolution.new({
  'description': segmentCycleDescription,
  'defaultResolutionFormulas': new Set([
    endDateByMasterStartAndEndOffsetFormula,
    endOffsetByMasterStartAndEndDateFormula,
    endOffsetByStartOffsetAndDurationFormula
  ])
});
const SegmentSEDDispatcherIdentifier = class extends FieldIdentifier.mix(CalculatedValueGen) {
  equality(a, b) {
    const c = a.resolution;
    const e = b.resolution;
    return c.get(StartDateVar) === e.get(StartDateVar) && c.get(EndDateVar) === e.get(EndDateVar) && c.get(DurationVar) === e.get(DurationVar) && c.get(MasterStartDateVar) === e.get(MasterStartDateVar) && c.get(MasterEndDateVar) === e.get(MasterEndDateVar) && c.get(MasterDurationVar) === e.get(MasterDurationVar) && c.get(MasterTotalDurationVar) === e.get(MasterTotalDurationVar) && c.get(StartOffsetVar) === e.get(StartOffsetVar) && c.get(EndOffsetVar) === e.get(EndOffsetVar);
  }
};
const SchedulerProEventSegment = class extends Mixin([BaseEventMixin], a => {
  class b extends a {
    get isEventSegment() {
      return true;
    }
    get stm() {
      let c;
      return (c = this.event) == null ? void 0 : c.stm;
    }
    set stm(c) {
    }
    writeStartDate(c, e, f, g, i = true) {
      const j = this.event;
      const k = this.getProject();
      if (j && !this.previousSegment && e.baseRevision.hasIdentifier(c) && !(k && k.getStm().isRestoring)) {
        j.$.startDate.constructor.prototype.write.call(this, j.$.startDate, e, null, g, i);
      } else {
        c.constructor.prototype.write.call(this, c, e, f, g, i);
      }
      if (i && this.nextSegment) {
        const l = this.endOffset - this.nextSegment.startOffset;
        if (l > 0) {
          let n = this;
          while (n = n.nextSegment) {
            n.startOffset += l;
            n.endOffset += l;
          }
        }
      }
    }
    shouldRecordFieldChange(c, e, f) {
      return c === 'startOffset' || c === 'endOffset' || super.shouldRecordFieldChange(c, e, f);
    }
    *calculateStartOffset() {
      const c = yield this.$.dispatcher;
      const e = c.resolution.get(StartOffsetVar);
      if (!this.event) {
        return yield ProposedOrPrevious;
      }
      let f;
      if (e === CalculateProposed) {
        f = yield ProposedOrPrevious;
      } else if (e === startOffsetByEndOffsetAndDurationFormula.formulaId) {
        f = yield* this.calculateStartOffsetByEndOffsetAndDuration();
      } else if (e === startOffsetByMasterStartAndStartDateFormula.formulaId) {
        const g = yield ProposedOrPreviousValueOf(this.event.$.startDate);
        const i = yield ProposedOrPreviousValueOf(this.$.startDate);
        f = yield* this.event.calculateProjectedDuration(g, i, TimeUnit.Millisecond, { 'ignoreSegments': true });
      }
      return f;
    }
    *calculateEndOffset() {
      const c = yield this.$.dispatcher;
      const e = c.resolution.get(EndOffsetVar);
      if (!this.event) {
        return yield ProposedOrPrevious;
      }
      let f;
      if (e === CalculateProposed) {
        f = yield ProposedOrPrevious;
      } else if (e === endOffsetByStartOffsetAndDurationFormula.formulaId) {
        f = yield* this.calculateEndOffsetByStartOffsetAndDuration();
      } else if (e === endOffsetByMasterStartAndEndDateFormula.formulaId) {
        const g = yield ProposedOrPreviousValueOf(this.event.$.startDate);
        const i = yield ProposedOrPreviousValueOf(this.$.endDate);
        f = yield* this.event.calculateProjectedDuration(g, i, TimeUnit.Millisecond, { 'ignoreSegments': true });
      } else if (e === endOffsetByMasterDurationAndStartOffsetFormula.formulaId) {
        f = yield* this.calculateEndOffsetByMasterDurationAndStartOffset();
      } else if (e === endOffsetByMasterTotalDurationAndStartOffsetFormula.formulaId) {
        f = yield* this.calculateEndOffsetByMasterTotalDurationAndStartOffset();
      }
      return f;
    }
    *calculateStartDate() {
      const c = yield this.$.dispatcher;
      const e = c.resolution.get(StartDateVar);
      if (!this.event) {
        return yield ProposedOrPrevious;
      }
      let f;
      if (e === startDateByMasterStartAndStartOffsetFormula.formulaId) {
        f = yield* this.calculateStartDateByMasterStartAndStartOffset();
      } else {
        f = yield* super.calculateStartDate();
      }
      return f;
    }
    *calculateEndDate() {
      const c = yield this.$.dispatcher;
      const e = c.resolution.get(EndDateVar);
      if (!this.event) {
        return yield ProposedOrPrevious;
      }
      let f;
      if (e === endDateByMasterStartAndEndOffsetFormula.formulaId) {
        f = yield* this.calculateEndDateByMasterStartAndEndOffset();
      } else {
        f = yield* super.calculateEndDate();
      }
      return f;
    }
    *calculateDuration() {
      const c = yield this.$.dispatcher;
      const e = c.resolution.get(DurationVar);
      if (!this.event) {
        return yield ProposedOrPrevious;
      }
      let f;
      if (e === durationByOffsetsFormula.formulaId) {
        f = yield* this.calculateDurationByOffsets();
      } else if (e === durationByMasterEndDateFormula.formulaId) {
        f = yield* this.calculateDurationByOffsets();
      } else {
        f = yield* super.calculateDuration();
      }
      return f;
    }
    buildProposedDispatcher(c, e, f) {
      const g = super.buildProposedDispatcher(c, e, f);
      g.addPreviousValueFlag(MasterStartDateVar);
      g.addPreviousValueFlag(StartOffsetVar);
      g.addPreviousValueFlag(EndOffsetVar);
      return g;
    }
    *prepareDispatcher(c) {
      const e = yield* super.prepareDispatcher(c);
      if (!this.event) {
        return e;
      }
      if (c(PreviousValueOf(this.event.$.startDate)) != null)
        e.addPreviousValueFlag(MasterStartDateVar);
      if (c(HasProposedValue(this.event.$.startDate)))
        e.addProposedValueFlag(MasterStartDateVar);
      if (!c(HasProposedValue(this.event.$.segments))) {
        e.collectInfo(c, this.event.$.duration, MasterDurationVar);
        if (c(HasProposedValue(this.event.$.endDate))) {
          const g = c(ProposedArgumentsOf(this.event.$.endDate));
          if (!(g == null ? void 0 : g[0])) {
            e.addProposedValueFlag(MasterEndDateVar);
          }
        }
        const f = c(this.event.$.dispatcher);
        if (f.resolution.get(StartDateVar) === CalculateProposed && f.resolution.get(EndDateVar) === CalculateProposed) {
          e.addProposedValueFlag(MasterTotalDurationVar);
        }
      }
      e.collectInfo(c, this.$.startOffset, StartOffsetVar);
      e.collectInfo(c, this.$.endOffset, EndOffsetVar);
      return e;
    }
    cycleResolutionContext(c) {
      return segmentCycleResolution;
    }
    *calculateEndOffsetByMasterDurationAndStartOffset() {
      const c = yield ProposedOrPreviousValueOf(this.event.$.duration);
      const e = yield this.event.$.durationUnit;
      const f = yield this.$.startOffset;
      const g = this.nextSegment;
      let j;
      let k = yield* this.getProject().$convertDuration(c, e, TimeUnit.Millisecond);
      const l = [];
      let n = this;
      while (n = n.previousSegment) {
        l.push(n);
      }
      for (let o = l.length - 1; o >= 0; o--) {
        const p = l[o];
        const q = yield ProposedOrPreviousValueOf(p.$.startOffset);
        const r = yield ProposedOrPreviousValueOf(p.$.endOffset);
        const u = r - q;
        k -= u;
      }
      if (k > 0) {
        if (!g) {
          j = f + k;
        } else {
          j = f + Math.min(k, (yield ProposedOrPreviousValueOf(this.$.endOffset)) - f);
        }
      } else {
        j = f;
      }
      return j;
    }
    *calculateEndOffsetByMasterTotalDurationAndStartOffset() {
      const c = yield ProposedOrPreviousValueOf(this.event.$.startDate);
      const e = yield ProposedOrPreviousValueOf(this.event.$.endDate);
      const f = yield* this.event.calculateProjectedDuration(c, e, TimeUnit.Millisecond, { 'ignoreSegments': true });
      const g = yield ProposedOrPreviousValueOf(this.$.startOffset);
      const i = yield ProposedOrPreviousValueOf(this.$.endOffset);
      const j = this.nextSegment;
      if (g <= f) {
        if (i <= f) {
          if (!j || (yield ProposedOrPreviousValueOf(j.$.startOffset)) >= f) {
            return f;
          }
          return i;
        } else {
          return f;
        }
      }
      return yield this.$.startOffset;
    }
    *calculateStartOffsetByEndOffsetAndDuration() {
      const c = yield this.$.duration;
      const e = yield this.$.durationUnit;
      const f = yield this.$.endOffset;
      return f - (yield* this.event.getProject().$convertDuration(c, e, TimeUnit.Millisecond));
    }
    *calculateEndOffsetByStartOffsetAndDuration() {
      const c = yield this.$.duration;
      const e = yield this.$.durationUnit;
      const f = yield this.$.startOffset;
      return f + (yield* this.event.getProject().$convertDuration(c, e, TimeUnit.Millisecond));
    }
    *calculateEndDateByMasterStartAndEndOffset() {
      const c = yield this.event.$.startDate;
      const e = yield this.$.endOffset;
      const f = yield* this.event.calculateProjectedXDateWithDuration(c, true, e, TimeUnit.Millisecond, { 'ignoreSegments': true });
      const g = yield* this.isManuallyScheduled();
      return g && !this.getProject().skipNonWorkingTimeWhenSchedulingManually ? f : yield* this.event.skipNonWorkingTime(f, false);
    }
    *calculateStartDateByMasterStartAndStartOffset() {
      const c = yield this.event.$.startDate;
      const e = yield this.$.startOffset;
      const f = yield* this.event.calculateProjectedXDateWithDuration(c, true, e, TimeUnit.Millisecond, { 'ignoreSegments': true });
      const g = yield* this.isManuallyScheduled();
      return g && !this.getProject().skipNonWorkingTimeWhenSchedulingManually ? f : yield* this.event.skipNonWorkingTime(f);
    }
    *calculateDurationByOffsets() {
      const c = yield this.$.startOffset;
      const e = yield this.$.endOffset;
      const f = yield this.$.durationUnit;
      return yield* this.getProject().$convertDuration(e - c, TimeUnit.Millisecond, f);
    }
    *calculatePercentDone() {
      const c = 0;
      if (!this.event) {
        return yield ProposedOrPrevious;
      }
      const e = yield this.event.$.segments;
      if (e) {
        const f = yield this.event.$.percentDone;
        const g = yield this.event.$.duration;
        const i = yield this.event.$.durationUnit;
        const j = yield* this.getProject().$convertDuration(g, i, TimeUnit.Millisecond);
        let k = f * 0.01 * j;
        for (const l of e) {
          const n = l.startOffset;
          const o = l.endOffset;
          const p = o - n;
          if (l === this) {
            if (k >= p)
              return 100;
            else if (k > 0)
              return 100 * k / p;
            else
              return 0;
          }
          k -= p;
        }
      }
      return c;
    }
    *calculateMinPercent() {
      const c = this.previousSegment;
      if (!this.event) {
        return yield ProposedOrPrevious;
      }
      if (c) {
        return yield c.$.endPercentDone;
      }
      return 0;
    }
    *calculateMaxPercent() {
      if (!this.event) {
        return yield ProposedOrPrevious;
      }
      const c = yield this.event.$.duration;
      const e = yield this.event.$.durationUnit;
      const f = yield* this.getProject().$convertDuration(c, e, TimeUnit.Millisecond);
      const g = yield this.$.startOffset;
      const i = yield this.$.endOffset;
      const j = yield this.$.startPercentDone;
      return j + 100 * (i - g) / f;
    }
    *calculateProjectedXDateWithDuration(c, e, f, g) {
      if (!g)
        g = yield this.$.durationUnit;
      return yield* this.event.calculateProjectedXDateWithDuration(c, e, f, g, { 'ignoreSegments': true });
    }
    *calculateProjectedDuration(c, e, f) {
      if (!f)
        f = yield this.$.durationUnit;
      return yield* this.event.calculateProjectedDuration(c, e, f, { 'ignoreSegments': true });
    }
    *calculateManuallyScheduled() {
      if (this.event) {
        return yield* this.event.isManuallyScheduled();
      } else {
        return yield ProposedOrPrevious;
      }
    }
    afterChange(c, e, f, g, i) {
      let j;
      super.afterChange(c, e, f, g, i);
      const k = this.event;
      if (e && k) {
        if (!('segments' in k.meta.modified) && k.segments) {
          const p = k.segments.map(q => Object.assign(q.toJSON(), q.meta.modified));
          k.storeFieldChange('segments', p);
        }
        const l = k.getProject();
        const n = l.applyingSyncResponse;
        l.applyingSyncResponse = true;
        const o = (j = k.segments) == null ? void 0 : j.map(q => q.toJSON());
        k.afterChange({ 'segments': o }, { 'segments': o }, f, g, i);
        l.applyingSyncResponse = n;
      }
    }
  }
  __decorate30([field({ 'identifierCls': SegmentSEDDispatcherIdentifier })], b.prototype, 'dispatcher', void 0);
  __decorate30([model_field({ 'persist': false })], b.prototype, 'startOffset', void 0);
  __decorate30([model_field({ 'persist': false })], b.prototype, 'endOffset', void 0);
  __decorate30([field()], b.prototype, 'percentDone', void 0);
  __decorate30([field()], b.prototype, 'startPercentDone', void 0);
  __decorate30([field()], b.prototype, 'endPercentDone', void 0);
  __decorate30([write('startDate')], b.prototype, 'writeStartDate', null);
  __decorate30([calculate('startOffset')], b.prototype, 'calculateStartOffset', null);
  __decorate30([calculate('endOffset')], b.prototype, 'calculateEndOffset', null);
  __decorate30([calculate('percentDone')], b.prototype, 'calculatePercentDone', null);
  __decorate30([calculate('startPercentDone')], b.prototype, 'calculateMinPercent', null);
  __decorate30([calculate('endPercentDone')], b.prototype, 'calculateMaxPercent', null);
  __decorate30([calculate('manuallyScheduled')], b.prototype, 'calculateManuallyScheduled', null);
  return b;
}) {
};
const __decorate31 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const SegmentsVar = Symbol('SegmentsVar');
const segmentsConverter = (a, b, c) => c.processSegmentsValue(a);
const startDateByEndDateAndSegmentsFormula = Formula.new({
  'output': StartDateVar,
  'inputs': new Set([
    EndDateVar,
    SegmentsVar
  ])
});
const endDateByStartDateAndSegmentsFormula = Formula.new({
  'output': EndDateVar,
  'inputs': new Set([
    StartDateVar,
    SegmentsVar
  ])
});
const durationByStartDateAndEndDateAndSegmentsFormula = Formula.new({
  'output': DurationVar,
  'inputs': new Set([
    StartDateVar,
    EndDateVar,
    SegmentsVar
  ])
});
const SEDSGGraphDescription = CycleDescription.new({
  'variables': new Set([
    StartDateVar,
    EndDateVar,
    DurationVar,
    SegmentsVar
  ]),
  'formulas': new Set([
    endDateByStartDateAndSegmentsFormula,
    startDateByEndDateAndSegmentsFormula,
    startDateFormula,
    endDateFormula,
    durationFormula
  ])
});
const SEDSGForwardCycleResolution = CycleResolution.new({
  'description': SEDSGGraphDescription,
  'defaultResolutionFormulas': new Set([
    endDateFormula,
    endDateByStartDateAndSegmentsFormula
  ])
});
const SEDSGBackwardCycleResolution = CycleResolution.new({
  'description': SEDSGGraphDescription,
  'defaultResolutionFormulas': new Set([
    startDateFormula,
    startDateByEndDateAndSegmentsFormula
  ])
});
const SEDSGDispatcherIdentifier = class extends FieldIdentifier.mix(CalculatedValueGen) {
  equality(a, b) {
    const c = a.resolution;
    const e = b.resolution;
    return c.get(StartDateVar) === e.get(StartDateVar) && c.get(EndDateVar) === e.get(EndDateVar) && c.get(DurationVar) === e.get(DurationVar) && c.get(SegmentsVar) === e.get(SegmentsVar);
  }
};
function compareSegmentsArray(c, e) {
  if (!c && !e)
    return true;
  if (this._skipSegmentsIsEqual)
    return false;
  if (!c && e || c && !e)
    return false;
  if (c.length !== e.length)
    return false;
  return c.every((f, g) => compareSegments(f, e[g]));
}
var compareSegments = (c, e) => {
  let f, g, i, j;
  if (c === e)
    return true;
  const k = c.isModel ? c : e;
  const l = k.fieldMap;
  const n = c.startDate instanceof Date ? c.startDate.getTime() : (f = l.startDate.convert(c.startDate)) == null ? void 0 : f.getTime();
  const o = e.startDate instanceof Date ? e.startDate.getTime() : (g = l.startDate.convert(e.startDate)) == null ? void 0 : g.getTime();
  const p = c.endDate instanceof Date ? c.endDate.getTime() : (i = l.endDate.convert(c.endDate)) == null ? void 0 : i.getTime();
  const q = e.endDate instanceof Date ? e.endDate.getTime() : (j = l.endDate.convert(e.endDate)) == null ? void 0 : j.getTime();
  return n === o && p === q;
};
const SplitEventMixin = class extends Mixin([
  SchedulerProHasAssignmentsMixin,
  HasPercentDoneMixin
], a => {
  class b extends a {
    constructor() {
      super(...arguments);
      this._segmentGeneration = {};
    }
    static get $name() {
      return 'SplitEventMixin';
    }
    construct() {
      this.segmentModelClass = this.getDefaultSegmentModelClass();
      super.construct(...arguments);
    }
    get rawModifications() {
      let c = super.rawModifications;
      if (this.segments && (!c || !('segments' in c))) {
        for (const e of this.segments) {
          if (e.rawModifications) {
            c = c || {};
            c.segments = this.getFieldPersistentValue('segments');
            break;
          }
        }
      }
      return c;
    }
    internalClearChanges(c, e, f) {
      for (const g of this.segments || []) {
        if (g.isModel) {
          g.internalClearChanges(c, e, null);
        }
      }
      super.internalClearChanges(c, e, f);
    }
    getDefaultSegmentModelClass() {
      return SchedulerProEventSegment;
    }
    *prepareDispatcher(c) {
      const e = yield* super.prepareDispatcher(c);
      if (yield* this.hasSegmentChangesProposed()) {
        e.addProposedValueFlag(SegmentsVar);
      }
      return e;
    }
    cycleResolutionContext(c) {
      const e = c(this.$.effectiveDirection);
      return e.direction === Direction.Forward || e.direction === Direction.None ? SEDSGForwardCycleResolution : SEDSGBackwardCycleResolution;
    }
    *hasSegmentChangesProposed() {
      const c = yield ProposedValueOf(this.$.segments);
      let e = false;
      if (yield HasProposedValue(this.$.segments)) {
        e = Boolean(c);
      }
      const f = yield ProposedOrPreviousValueOf(this.$.segments);
      if (!f)
        return false;
      for (const g of f) {
        const i = yield HasProposedValue(g.$.startDate);
        const j = yield HasProposedValue(g.$.endDate);
        const k = yield HasProposedValue(g.$.duration);
        if (i || j || k)
          e = true;
      }
      return e;
    }
    writeSegments(c, e, f, g) {
      let i;
      const j = (i = e.getLatestEntryFor(c)) == null ? void 0 : i.getValue();
      const k = j !== TombStone ? j != null ? j : [] : [];
      const l = new Set(k);
      const n = (g != null ? g : []).map(r => r.isModel ? r : this.processSegmentValue(r));
      const o = new Set(n);
      this.project.ion({
        'commitFinalized': () => q.removeEntities(k.filter(r => !o.has(r))),
        'once': true
      });
      c.constructor.prototype.write.call(this, c, e, f, n.length > 0 ? n : null);
      this.$.isSegmented.write.call(this, this.$.isSegmented, e, null, Boolean(g == null ? void 0 : g.length));
      const p = this.project;
      const q = p.replica;
      for (const r of n) {
        if (!l.has(r) && r.graph !== q) {
          r.setProject(p);
          q.addEntity(r);
        }
      }
    }
    *doWriteSegments(c, e) {
      e = e || [];
      if (c.length <= 1) {
        const f = c.length ? yield* this.getProject().$convertDuration(c[0].endOffset - c[0].startOffset, TimeUnit.Millisecond, yield this.$.durationUnit) : 0;
        e.push({
          'identifier': this.$.duration,
          'proposedArgs': [
            f,
            null
          ]
        });
        c = null;
      }
      e.push({
        'identifier': this.$.segments,
        'proposedArgs': [c]
      });
      yield WriteSeveral(e);
    }
    *calculateSegments() {
      const c = yield this.$.dispatcher;
      const {
        graph: e,
        project: f
      } = this;
      const g = yield f.$.autoMergeAdjacentSegments || this.forceSegmentsMerging;
      const i = yield PreviousValueOf(this.$.segments);
      let j = yield ProposedOrPrevious;
      const k = [];
      let l = false;
      if (j) {
        const n = new Set();
        let o = null;
        let p = false;
        let q = 0;
        const {baseRevision: r} = e.$activeTransaction;
        for (const u of j) {
          const v = yield u.$.startOffset;
          const w = yield u.$.endOffset;
          const z = r.hasIdentifier(u.$.startDate) && (yield ProposedArgumentsOf(u.$.startDate));
          const A = r.hasIdentifier(u.$.endDate) && (yield ProposedArgumentsOf(u.$.endDate));
          p = p || (z == null ? void 0 : z[0]) || (A == null ? void 0 : A[0]);
          if (v === w) {
            k.push(u);
          } else if (g && o && v + q <= o.endOffset) {
            const B = o.endOffset;
            k.push(u);
            if (o.graph) {
              k.push(o);
              const C = o.startOffset;
              const E = p ? w + B - v : Math.max(w, B);
              const F = o.cls;
              o = this.segmentModelClass.new({
                'event': this,
                'cls': F,
                'startOffset': C,
                'endOffset': E
              });
            } else {
              o.endOffset = p ? w + o.endOffset - v : Math.max(w, o.endOffset);
            }
          } else if (!g && o && (q || v < o.endOffset)) {
            n.add(o);
            k.push(u);
            if (v + q < o.endOffset) {
              q += o.endOffset - v - q;
            }
            o = this.segmentModelClass.new({
              'id': u.id,
              'event': this,
              'cls': o.cls,
              'startOffset': v + q,
              'endOffset': w + q
            });
          } else {
            if (o) {
              n.add(o);
            }
            o = u;
          }
        }
        if (o) {
          n.add(o);
        }
        if (n.size === 1) {
          k.push(...n);
        }
        l = k.length > 0;
        if (l) {
          j = Array.from(n);
        }
        j.reduce((G, H, I) => {
          if (G) {
            G.nextSegment = H;
          }
          H.previousSegment = G;
          H.segmentIndex = I;
          return H;
        }, null);
        if (j.length) {
          j[j.length - 1].nextSegment = null;
        }
      } else if (i) {
        k.push(...i);
      }
      if (k.length) {
        k.forEach(G => G.event = null);
        f.ion({
          'commitFinalized': () => e.removeEntities(k),
          'once': true
        });
      }
      if (l) {
        yield* this.doWriteSegments(j);
      }
      j = (j == null ? void 0 : j.length) > 1 ? j : null;
      return j;
    }
    *calculateAdjustedSegments() {
      const c = yield this.$.dispatcher;
      let e = yield this.$.segments;
      const f = yield this.$.startDate;
      const g = yield this.$.endDate;
      const j = yield this.$.duration;
      const k = yield ProposedOrPrevious;
      if (e) {
        const l = this.project;
        const n = this.graph;
        const o = [];
        const p = [];
        let q = -1;
        for (let v = e.length - 1; v >= 0; v--) {
          const w = e[v];
          const z = yield w.$.startDate;
          const A = yield w.$.endDate;
          if (z > g) {
            o.push(w);
            q = v;
          } else {
            if (A.getTime() !== g.getTime()) {
              const B = w.endOffset + (g.getTime() - A.getTime()) - w.startOffset;
              const C = yield* l.$convertDuration(B, TimeUnit.Millisecond, yield w.$.durationUnit);
              p.push({
                'identifier': w.$.duration,
                'proposedArgs': [
                  C,
                  null
                ]
              }, {
                'identifier': w.$.endDate,
                'proposedArgs': [
                  g,
                  false
                ]
              }, {
                'identifier': w.$.endOffset,
                'proposedArgs': [w.endOffset + (g.getTime() - A.getTime())]
              });
            }
            break;
          }
        }
        let r = false;
        if (q > -1) {
          r = true;
          e.splice(q);
          if (e.length) {
            e[e.length - 1].nextSegment = null;
          }
          l.ion({
            'commitFinalized': () => n.removeEntities(o),
            'once': true
          });
        }
        let u = '';
        if (e) {
          u = this.getSegmentsSnapshot(e);
        }
        if (u !== this._lastSegmentsSnapshot) {
          r = true;
          e = e ? e.slice() : e;
          this._lastSegmentsSnapshot = u;
        }
        if (r) {
          yield* this.doWriteSegments(e, p);
        }
      }
      return k;
    }
    getSegmentsSnapshot(c) {
      c = c || this.segments;
      return c == null ? void 0 : c.map(e => {
        let f, g;
        return '' + e.startOffset + '-' + ((f = e.startDate) == null ? void 0 : f.getTime()) + '-' + e.endOffset + '-' + ((g = e.endDate) == null ? void 0 : g.getTime());
      }).join(';');
    }
    processSegmentValue(c) {
      const e = c.isModel ? c : this.segmentModelClass.new(c);
      if (!e.event)
        e.event = this;
      return e;
    }
    processSegmentsValue(c) {
      if (c == null)
        return null;
      return c.map(e => this.processSegmentValue(e));
    }
    *calculateStartDate() {
      const c = yield this.$.dispatcher;
      const e = c.resolution.get(StartDateVar);
      let f;
      if (e === startDateByEndDateAndSegmentsFormula.formulaId) {
        f = yield* this.calculateStartDateBySegments();
      } else {
        f = yield* super.calculateStartDate();
      }
      return f;
    }
    *calculateStartDateBySegments() {
      const c = yield this.$.dispatcher;
      const e = yield this.$.segments;
      const f = yield this.$.endDate;
      let g;
      if (e) {
        const i = e[e.length - 1];
        const j = yield i.$.endOffset;
        const k = yield* this.calculateProjectedXDateWithDuration(f, false, j, TimeUnit.Millisecond, { 'ignoreSegments': true });
        const l = yield* this.isManuallyScheduled();
        g = l && !this.getProject().skipNonWorkingTimeWhenSchedulingManually ? k : yield* this.skipNonWorkingTime(k, true);
      }
      return g;
    }
    *calculateEndDateBySegments() {
      const c = yield this.$.dispatcher;
      const e = yield this.$.segments;
      const f = yield this.$.startDate;
      let g;
      if (e) {
        const i = e[e.length - 1];
        const j = yield i.$.endOffset;
        const k = yield* this.calculateProjectedXDateWithDuration(f, true, j, TimeUnit.Millisecond, { 'ignoreSegments': true });
        const l = yield* this.isManuallyScheduled();
        g = l && !this.getProject().skipNonWorkingTimeWhenSchedulingManually ? k : yield* this.skipNonWorkingTime(k, false);
      }
      return g;
    }
    *calculateEndDate() {
      const c = yield this.$.dispatcher;
      const e = c.resolution.get(EndDateVar);
      let f;
      if (e === endDateByStartDateAndSegmentsFormula.formulaId) {
        f = yield* this.calculateEndDateBySegments();
      } else {
        f = yield* super.calculateEndDate();
      }
      return f;
    }
    *calculateDurationProposed() {
      let c;
      if (yield* this.hasSegmentChangesProposed()) {
        c = yield* this.calculateDurationBySegments();
      } else {
        c = yield* super.calculateDurationProposed();
      }
      return c;
    }
    *skipNonWorkingTime(c, e = true, f) {
      if (!c)
        return null;
      f = Object.assign({ 'ignoreSegments': true }, f);
      return yield* super.skipNonWorkingTime(c, e, f);
    }
    *calculateDurationBySegments() {
      let c;
      const e = yield this.$.dispatcher;
      const f = yield this.$.durationUnit;
      const g = yield this.$.segments;
      if (g) {
        let i = 0;
        for (const j of g) {
          i += j.endOffset - j.startOffset;
        }
        c = yield* this.getProject().$convertDuration(i, TimeUnit.Millisecond, f);
      }
      return c;
    }
    *forEachAvailabilityInterval(c, e) {
      const f = this.getProject();
      const g = yield this.$.effectiveCalendar;
      const i = yield this.$.assignmentsByCalendar;
      const j = yield this.$.effectiveCalendarsCombination;
      const k = c.isForward !== false;
      const l = (yield this.$.ignoreResourceCalendar) || c.ignoreResourceCalendar || !i.size;
      const n = f.maxCalendarRange;
      let o = c.ignoreSegments;
      let p = 1;
      let q, r, u, v, w;
      if (!o) {
        v = yield this.$.segments;
        o = o || !v || k === false && v[0].startDate >= c.endDate || k === true && v[v.length - 1].endDate < c.startDate;
        if (!o) {
          v = v.slice();
          if (k) {
            q = v.shift();
            r = 0;
            p = 1;
            w = q.nextSegment ? q.endOffset : MAX_DATE2.getTime();
          } else {
            q = v.pop();
            r = q.endOffset;
            p = -1;
            w = q.endOffset;
          }
          u = w - q.startOffset;
        }
      }
      const z = yield* this.isManuallyScheduled();
      const A = f.adjustDurationToDST;
      return j.forEachAvailabilityInterval(Object.assign({ 'maxRange': n }, c), (B, C, E) => {
        const F = E.getCalendarsWorkStatus();
        const G = E.getCalendarsWorking();
        if (F.get(g) && (l || G.some(H => i.has(H)) || z && !f.skipNonWorkingTimeInDurationWhenSchedulingManually)) {
          if (o) {
            return e(B, C, E);
          } else {
            const H = B.getTime();
            let I = C.getTime() - B.getTime();
            if (A) {
              const L = B.getTimezoneOffset() - C.getTimezoneOffset();
              I += L * 60 * 1000;
            }
            let J, K;
            if (k) {
              J = r;
              K = r + I;
            } else {
              J = r - I;
              K = r;
            }
            while (q && J <= w && K > q.startOffset) {
              const N = Math.max(J, q.startOffset);
              const O = Math.min(K, w);
              const P = new Date(H + N - J);
              const Q = new Date(H + O - J);
              const R = e(P, Q, E);
              if (R === false)
                return false;
              u -= Q.getTime() - P.getTime();
              if (u < 0.00001) {
                q = k ? v.shift() : v.pop();
                if (q) {
                  w = !k || q.nextSegment ? q.endOffset : MAX_DATE2.getTime();
                  u = w - q.startOffset;
                }
              } else {
                break;
              }
            }
            r += p * I;
          }
        }
      });
    }
    *useEventAvailabilityIterator() {
      const c = yield this.$.isSegmented;
      if (c)
        return true;
      const e = yield* this.isManuallyScheduled();
      return !e;
    }
    getSegmentByDate(c, e) {
      e = e || this.getSegments();
      if (e) {
        const f = this.getSegmentIndexByDate(c, e);
        return e[f];
      }
    }
    getSegmentIndexByDate(c, e) {
      e = e || this.getSegments();
      return e ? e.findIndex(f => c >= f.startDate && c < f.endDate) : -1;
    }
    get firstSegment() {
      const c = this.getSegments();
      return c ? c[0] : null;
    }
    get lastSegment() {
      const c = this.getSegments();
      return c ? c[c.length - 1] : null;
    }
    getSegment(c) {
      const e = this.getSegments();
      return e == null ? void 0 : e[c];
    }
    async splitToSegments(c, e = 1, f) {
      let g;
      const j = this.getProject();
      await j.commitAsync();
      const k = this;
      if (!c || k.isHasSubEventsMixin && ((g = k.childEvents) == null ? void 0 : g.size))
        return;
      const n = k.duration;
      const o = k.durationUnit;
      const p = k.startDate;
      const q = k.endDate;
      f = f ? DateHelper.normalizeUnit(f) : o;
      if (!p || !q || p >= c || c >= q || !n)
        return;
      const r = k.isSegmented;
      let u = k.segments || [];
      let v, w;
      if (r) {
        w = k.getSegmentIndexByDate(c, u);
        v = u[w];
        if (!v)
          return;
      }
      const z = v || k;
      const A = v ? z.startDate : p;
      const B = z.duration;
      const C = z.durationUnit;
      const E = z.name;
      const F = k.run('calculateProjectedDuration', A, c, C, { 'ignoreSegments': true });
      const G = B - F;
      const H = j.run('$convertDuration', e, f, TimeUnit.Millisecond);
      const I = H + k.run('calculateProjectedDuration', p, c, TimeUnit.Millisecond, { 'ignoreSegments': true });
      if (v) {
        v.duration = F;
        const J = this.segmentModelClass.new({
          'duration': G,
          'durationUnit': C,
          'startOffset': I,
          'name': E
        });
        u = u.slice(0);
        u.splice(w + 1, 0, J);
        k.segments = u;
        k.duration = n;
        for (let K = w + 2, L = u.length; K < L; K++) {
          const N = u[K];
          if (N) {
            N.startOffset += H;
            N.endOffset += H;
          }
        }
      } else {
        const O = this.segmentModelClass.new({
          'duration': F,
          'durationUnit': C,
          'startOffset': 0,
          'name': E
        });
        const P = this.segmentModelClass.new({
          'duration': n - F,
          'durationUnit': C,
          'startOffset': I,
          'name': E
        });
        k.duration = n;
        k.segments = [
          O,
          P
        ];
      }
      return j.commitAsync();
    }
    async mergeSegments(c, e) {
      if (!this.isSegmented)
        return;
      this.forceSegmentsMerging = true;
      c = c || this.firstSegment;
      e = e || this.lastSegment;
      if (c.startOffset > e.startOffset) {
        const g = e;
        e = c;
        c = g;
      }
      c.endDate = e.startDate;
      const f = await this.getProject().commitAsync();
      this.forceSegmentsMerging = false;
      return f;
    }
    storeFieldChange(c, e) {
      if (c === 'segments' && e) {
        const f = [];
        for (const g of e) {
          if (g instanceof SchedulerProEventSegment) {
            const i = g.toJSON();
            if (!this._segmentGeneration[g.internalId] || g.generation > this._segmentGeneration[g.internalId]) {
              Object.assign(i, g.meta.modified);
            }
            f.push(i);
            this._segmentGeneration[g.internalId] = g.generation;
          } else {
            f.push(g);
          }
        }
        e = f;
      }
      super.storeFieldChange(c, e);
    }
    leaveProject() {
      const c = this.segments;
      if (c) {
        this.graph.removeEntities(c);
      }
      super.leaveProject();
    }
    endBatch(...c) {
      this.fieldMap.segments._skipSegmentsIsEqual++;
      super.endBatch(...c);
      this.fieldMap.segments._skipSegmentsIsEqual--;
    }
    copy(c = null, e = null) {
      const f = super.copy(c, e);
      f.data.segments = void 0;
      if (f.segments) {
        f.segments = f.segments.map(g => Object.assign(g.copy(), { 'event': f }));
      }
      return f;
    }
    toJSON() {
      const c = super.toJSON(), e = this.getFieldDefinition('segments'), {
          persist: f,
          dataSource: g,
          complexMapping: i
        } = e;
      if (f) {
        let j = ObjectHelper.getPath(c, g);
        if (j) {
          j = j.map(k => k.toJSON());
          ObjectHelper.setPath(c, g, j);
        }
      }
      return c;
    }
  }
  __decorate31([field({ 'identifierCls': SEDSGDispatcherIdentifier })], b.prototype, 'dispatcher', void 0);
  __decorate31([model_field({
      'type': 'array',
      'isEqual': compareSegmentsArray,
      'convert': segmentsConverter,
      '_skipSegmentsIsEqual': 0
    })], b.prototype, 'segments', void 0);
  __decorate31([field()], b.prototype, 'adjustedSegments', void 0);
  __decorate31([field()], b.prototype, 'isSegmented', void 0);
  __decorate31([write('segments')], b.prototype, 'writeSegments', null);
  __decorate31([calculate('segments')], b.prototype, 'calculateSegments', null);
  __decorate31([calculate('adjustedSegments')], b.prototype, 'calculateAdjustedSegments', null);
  return b;
}) {
};
const __decorate32 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const HasEffortMixin = class extends Mixin([
  SchedulerProHasAssignmentsMixin,
  HasChildrenMixin
], a => {
  const b = a.prototype;
  class c extends a {
    getEffort(e) {
      const f = this.effort;
      return e ? this.getProject().convertDuration(f, this.effortUnit, e) : f;
    }
    writeEffort(e, f, g, i, j) {
      if (i < 0)
        i = 0;
      if (!f.baseRevision.hasIdentifier(e) && i == null)
        return;
      if (j != null && j !== this.effortUnit) {
        this.$.effortUnit.write.call(this, this.$.effortUnit, f, null, j);
      }
      e.constructor.prototype.write(e, f, g, i);
    }
    setEffortUnit(e) {
      throw new Error('Use `setEffort` instead');
    }
    *shouldRollupChildEffort(e) {
      return true;
    }
    *calculateTotalChildrenEffort() {
      const e = yield this.$.childEvents;
      const f = this.getProject();
      let g = 0;
      for (const i of e) {
        if (!(yield* this.shouldRollupChildEffort(i)))
          continue;
        const j = yield i.$.effortUnit;
        g += yield* f.$convertDuration(yield i.$.effort, j, TimeUnit.Millisecond);
      }
      return yield* f.$convertDuration(g, TimeUnit.Millisecond, yield this.$.effortUnit);
    }
    *calculateEffort() {
      const e = yield this.$.childEvents;
      if (e.size > 0)
        return yield* this.calculateTotalChildrenEffort();
      else {
        const f = yield ProposedOrPrevious;
        return f !== void 0 ? f : yield* this.calculateEffortPure();
      }
    }
    *calculateEffortPure() {
      const e = yield this.$.childEvents;
      if (e.size > 0)
        return yield* this.calculateTotalChildrenEffort();
      else {
        return yield* this.calculateProjectedEffort(yield this.$.startDate, yield this.$.endDate);
      }
    }
    *calculateEffortProposed() {
      return yield ProposedOrPrevious;
    }
    *calculateAssignmentUnits(e) {
      return yield* this.calculateAssignmentUnitsProposed(e);
    }
    *calculateAssignmentUnitsPure(e) {
      return yield* this.calculateUnitsByStartEndAndEffort(e);
    }
    *calculateAssignmentUnitsProposed(e) {
      return yield ProposedOrPrevious;
    }
    *getBaseOptionsForEffortCalculations() {
      return { 'ignoreResourceCalendar': false };
    }
    *calculateProjectedEffort(e, f, g) {
      if (e == null || f == null || e > f)
        return null;
      if (!g) {
        g = yield this.$.assignmentsByCalendar;
      }
      const i = new Map();
      for (const [l, n] of g) {
        let o = 0;
        for (const p of n) {
          o += yield p.$.units;
        }
        i.set(l, o);
      }
      let j = 0;
      const k = Object.assign(yield* this.getBaseOptionsForEffortCalculations(), {
        'startDate': e,
        'endDate': f
      });
      if (i.size === 0) {
        i.set(yield this.$.effectiveCalendar, 100);
        k.ignoreResourceCalendar = true;
      }
      yield* this.forEachAvailabilityInterval(k, (q, r, u) => {
        const v = u.getCalendarsWorking();
        const w = q.getTime(), z = r.getTime(), A = z - w;
        let B = 0;
        for (const C of v) {
          B += i.get(C) || 0;
        }
        j += B * A * 0.01;
      });
      return yield* this.getProject().$convertDuration(j, TimeUnit.Millisecond, yield this.$.effortUnit);
    }
    *calculateUnitsByStartEndAndEffort(e) {
      const f = yield this.$.effort, g = yield this.$.effortUnit, i = yield* this.getProject().$convertDuration(f, g, TimeUnit.Millisecond), j = yield this.$.startDate, k = yield this.$.endDate;
      if (!j && !k) {
        return yield ProposedOrPrevious;
      }
      let l = 0;
      const n = Object.assign(yield* this.getBaseOptionsForEffortCalculations(), {
        'startDate': j,
        'endDate': k
      });
      const o = yield this.$.assignmentsByCalendar;
      yield* this.forEachAvailabilityInterval(n, (p, q, r) => {
        const u = r.getCalendarsWorking();
        const v = p.getTime(), w = q.getTime(), z = w - v;
        for (const A of u) {
          l += (o.has(A) ? o.get(A).length : 0) * z;
        }
      });
      return l ? 100 * i / l : 100;
    }
    *calculateProjectedXDateByEffort(e, f = true, g, i) {
      g = g !== void 0 ? g : yield this.$.effort;
      i = i !== void 0 ? i : yield this.$.effortUnit;
      const j = yield* this.getProject().$convertDuration(g, i, TimeUnit.Millisecond);
      if (e == null || g == null)
        return null;
      let k = e.getTime();
      let l = j;
      if (l === 0)
        return new Date(k);
      const n = yield this.$.effectiveCalendar;
      const o = yield this.$.assignmentsByCalendar;
      const p = new Map();
      let q = false;
      for (const [r, u] of o) {
        let v = 0;
        for (const w of u) {
          v += yield w.$.units;
        }
        p.set(r, v);
        if (v > 0)
          q = true;
      }
      if (q && (yield* this.useEventAvailabilityIterator())) {
        const z = Object.assign(yield* this.getBaseOptionsForDurationCalculations(), f ? {
          'startDate': e,
          'isForward': f
        } : {
          'endDate': e,
          'isForward': f
        });
        yield* this.forEachAvailabilityInterval(z, (A, B, C) => {
          const E = C.getCalendarsWorking();
          const F = A.getTime(), G = B.getTime(), H = G - F;
          let I = 0;
          for (const K of E) {
            I += p.get(K) || 0;
          }
          const J = I * H * 0.01;
          if (J >= l) {
            k = f ? F + l / (0.01 * I) : G - l / (0.01 * I);
            return false;
          } else {
            l -= J;
          }
        });
        return new Date(k);
      } else {
        return n.accumulateWorkingTime(e, j, f).finalDate;
      }
    }
  }
  __decorate32([model_field({ 'type': 'number' })], c.prototype, 'effort', void 0);
  __decorate32([model_field({
      'type': 'string',
      'defaultValue': TimeUnit.Hour
    }, { 'converter': e => DateHelper.normalizeUnit(e) || TimeUnit.Hour })], c.prototype, 'effortUnit', void 0);
  __decorate32([write('effort')], c.prototype, 'writeEffort', null);
  __decorate32([calculate('effort')], c.prototype, 'calculateEffort', null);
  return c;
}) {
};
const EffortVar = Symbol('EffortVar');
const UnitsVar = Symbol('UnitsVar');
const effortFormula = Formula.new({
  'output': EffortVar,
  'inputs': new Set([
    StartDateVar,
    EndDateVar,
    UnitsVar
  ])
});
const unitsFormula = Formula.new({
  'output': UnitsVar,
  'inputs': new Set([
    StartDateVar,
    EndDateVar,
    EffortVar
  ])
});
const endDateByEffortFormula = Formula.new({
  'output': EndDateVar,
  'inputs': new Set([
    StartDateVar,
    EffortVar,
    UnitsVar
  ])
});
const startDateByEffortFormula = Formula.new({
  'output': StartDateVar,
  'inputs': new Set([
    EndDateVar,
    EffortVar,
    UnitsVar
  ])
});
const SEDWUDispatcher = class extends SEDDispatcher {
};
const SEDWUDispatcherIdentifier = class extends SEDDispatcherIdentifier {
  equality(a, b) {
    const c = a.resolution;
    const e = b.resolution;
    return (a !== TombStone || b === TombStone) && (a === TombStone || b !== TombStone) && c.get(EffortVar) === e.get(EffortVar) && c.get(UnitsVar) === e.get(UnitsVar) && super.equality(a, b);
  }
};
const __decorate33 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const HasSchedulingModeMixin = class extends Mixin([HasEffortMixin], a => {
  const b = a.prototype;
  class c extends a {
    *calculateSchedulingMode() {
      return (yield ProposedOrPrevious) || SchedulingMode.Normal;
    }
    *effectiveSchedulingMode() {
      const e = yield this.$.assignmentsByCalendar;
      return e.size > 0 ? yield this.$.schedulingMode : SchedulingMode.Normal;
    }
    effectiveSchedulingModeSync(e) {
      const f = e(this.$.assignmentsByCalendar);
      return f.size > 0 ? e(this.$.schedulingMode) : SchedulingMode.Normal;
    }
    *prepareDispatcher(e) {
      const f = yield* this.effectiveSchedulingMode();
      if (f !== SchedulingMode.Normal) {
        const g = yield* b.prepareDispatcher.call(this, e);
        g.collectInfo(e, this.$.effort, EffortVar);
        if (yield* this.hasProposedValueForUnits())
          g.addProposedValueFlag(UnitsVar);
        g.addPreviousValueFlag(UnitsVar);
        return g;
      } else {
        return yield* b.prepareDispatcher.call(this, e);
      }
    }
    dispatcherClass(e) {
      const f = this.effectiveSchedulingModeSync(e);
      if (f !== SchedulingMode.Normal) {
        return SEDWUDispatcher;
      } else {
        return b.dispatcherClass.call(this, e);
      }
    }
    buildProposedDispatcher(e, f, g) {
      const i = b.buildProposedDispatcher.call(this, e, f, g);
      i.addPreviousValueFlag(EffortVar);
      i.addPreviousValueFlag(UnitsVar);
      return i;
    }
    *calculateAssignmentUnits(e) {
      const f = yield* this.effectiveSchedulingMode();
      if (f !== SchedulingMode.Normal) {
        const g = yield this.$.dispatcher;
        const i = g.resolution.get(UnitsVar);
        if (i === CalculateProposed) {
          return yield* this.calculateAssignmentUnitsProposed(e);
        } else if (i === unitsFormula.formulaId) {
          return yield* this.calculateAssignmentUnitsPure(e);
        } else {
          throw new Error('Unknown formula for `units`');
        }
      } else {
        return yield* b.calculateAssignmentUnits.call(this, e);
      }
    }
    *calculateEffort() {
      const e = yield* this.effectiveSchedulingMode();
      if (e !== SchedulingMode.Normal) {
        const f = yield this.$.dispatcher;
        const g = f.resolution.get(EffortVar);
        if (g === CalculateProposed) {
          return yield* this.calculateEffortProposed();
        } else if (g === effortFormula.formulaId) {
          return yield* this.calculateEffortPure();
        } else {
          throw new Error('Unknown formula for `effort`');
        }
      } else {
        return yield* b.calculateEffort.call(this);
      }
    }
    *calculateStartDate() {
      const e = yield* this.effectiveSchedulingMode();
      if (e !== SchedulingMode.Normal) {
        const f = yield this.$.dispatcher;
        const g = f.resolution.get(StartDateVar);
        if (g === startDateByEffortFormula.formulaId) {
          return yield* this.calculateProjectedXDateByEffort(yield this.$.endDate, false);
        } else {
          return yield* b.calculateStartDate.call(this);
        }
      } else {
        return yield* b.calculateStartDate.call(this);
      }
    }
    *calculateEndDate() {
      const e = yield* this.effectiveSchedulingMode();
      if (e !== SchedulingMode.Normal) {
        const f = yield this.$.dispatcher;
        const g = f.resolution.get(EndDateVar);
        if (g === endDateByEffortFormula.formulaId) {
          return yield* this.calculateProjectedXDateByEffort(yield this.$.startDate, true);
        } else {
          return yield* b.calculateEndDate.call(this);
        }
      } else {
        return yield* b.calculateEndDate.call(this);
      }
    }
    *calculateEffectiveDuration() {
      const e = yield this.$.dispatcher;
      const f = yield* this.effectiveSchedulingMode();
      const g = e.resolution.get(DurationVar);
      const i = e.resolution.get(EffortVar);
      let j;
      if (g === durationFormula.formulaId && f != SchedulingMode.Normal) {
        const k = yield ProposedOrPreviousValueOf(this.$.startDate);
        const l = yield ProposedOrPreviousValueOf(this.$.endDate);
        const n = e.resolution.get(StartDateVar);
        const o = e.resolution.get(EndDateVar);
        const p = yield this.$.effortDriven;
        if (l && n === startDateByEffortFormula.formulaId) {
          j = yield* this.calculateProjectedDuration(yield* this.calculateProjectedXDateByEffort(l, false), l);
        } else if (k && o === endDateByEffortFormula.formulaId) {
          j = yield* this.calculateProjectedDuration(k, yield* this.calculateProjectedXDateByEffort(k, true));
        } else if (k && l || !k && !l) {
          j = yield* b.calculateEffectiveDuration.call(this);
        }
      } else
        j = yield* b.calculateEffectiveDuration.call(this);
      return j;
    }
  }
  __decorate33([model_field({
      'type': 'boolean',
      'defaultValue': false
    })], c.prototype, 'effortDriven', void 0);
  __decorate33([model_field({
      'type': 'string',
      'defaultValue': SchedulingMode.Normal
    }, { 'sync': true })], c.prototype, 'schedulingMode', void 0);
  __decorate33([field({ 'identifierCls': SEDWUDispatcherIdentifier })], c.prototype, 'dispatcher', void 0);
  __decorate33([calculate('schedulingMode')], c.prototype, 'calculateSchedulingMode', null);
  __decorate33([calculate('effort')], c.prototype, 'calculateEffort', null);
  __decorate33([calculate('startDate')], c.prototype, 'calculateStartDate', null);
  __decorate33([calculate('endDate')], c.prototype, 'calculateEndDate', null);
  return c;
}) {
};
const fixedDurationSEDWUGraphDescription = CycleDescription.new({
  'variables': new Set([
    StartDateVar,
    EndDateVar,
    DurationVar,
    EffortVar,
    UnitsVar
  ]),
  'formulas': new Set([
    startDateFormula,
    endDateFormula,
    durationFormula,
    unitsFormula,
    effortFormula
  ])
});
const fixedDurationAndEffortSEDWUGraphDescription = CycleDescription.new({
  'variables': new Set([
    StartDateVar,
    EndDateVar,
    DurationVar,
    EffortVar,
    UnitsVar
  ]),
  'formulas': new Set([
    startDateFormula,
    endDateFormula,
    durationFormula,
    unitsFormula
  ])
});
const fixedDurationSEDWUForwardNonEffortDriven = CycleResolution.new({
  'description': fixedDurationSEDWUGraphDescription,
  'defaultResolutionFormulas': new Set([
    endDateFormula,
    effortFormula
  ])
});
const fixedDurationSEDWUForwardEffortDriven = CycleResolution.new({
  'description': fixedDurationAndEffortSEDWUGraphDescription,
  'defaultResolutionFormulas': new Set([
    endDateFormula,
    unitsFormula
  ])
});
const fixedDurationSEDWUBackwardNonEffortDriven = CycleResolution.new({
  'description': fixedDurationSEDWUGraphDescription,
  'defaultResolutionFormulas': new Set([
    startDateFormula,
    effortFormula
  ])
});
const fixedDurationSEDWUBackwardEffortDriven = CycleResolution.new({
  'description': fixedDurationAndEffortSEDWUGraphDescription,
  'defaultResolutionFormulas': new Set([
    startDateFormula,
    unitsFormula
  ])
});
const FixedDurationMixin = class extends Mixin([HasSchedulingModeMixin], a => {
  const b = a.prototype;
  class c extends a {
    *prepareDispatcher(e) {
      const f = yield* this.effectiveSchedulingMode();
      if (f === SchedulingMode.FixedDuration) {
        const g = yield* b.prepareDispatcher.call(this, e);
        const i = yield this.$.effortDriven;
        if (i)
          g.addKeepIfPossibleFlag(EffortVar);
        if (yield HasProposedValue(this.$.assigned)) {
          if (i) {
            g.addProposedValueFlag(EffortVar);
          } else {
            g.addProposedValueFlag(UnitsVar);
          }
        }
        return g;
      } else {
        return yield* b.prepareDispatcher.call(this, e);
      }
    }
    cycleResolutionContext(e) {
      const f = this.effectiveSchedulingModeSync(e);
      if (f === SchedulingMode.FixedDuration) {
        const g = e(this.$.effectiveDirection);
        const i = e(this.$.effortDriven);
        if (g.direction === Direction.Forward || g.direction === Direction.None) {
          return i ? fixedDurationSEDWUForwardEffortDriven : fixedDurationSEDWUForwardNonEffortDriven;
        } else {
          return i ? fixedDurationSEDWUBackwardEffortDriven : fixedDurationSEDWUBackwardNonEffortDriven;
        }
      } else {
        return b.cycleResolutionContext.call(this, e);
      }
    }
    *getBaseOptionsForDurationCalculations() {
      const e = yield* this.effectiveSchedulingMode();
      if (e === SchedulingMode.FixedDuration) {
        return { 'ignoreResourceCalendar': true };
      } else {
        return yield* b.getBaseOptionsForDurationCalculations.call(this);
      }
    }
  }
  return c;
}) {
};
const __decorate34 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const DelayFromParentScheduleMixin = class extends Mixin([ConstrainedScheduleMixin], a => {
  const b = a.prototype;
  class c extends a {
    *calculateEndDateConstraintIntervals() {
      const e = yield* b.calculateEndDateConstraintIntervals.call(this);
      if (this.event.delayFromParent != null) {
        const f = yield this.event.$.parentEvent;
        const g = yield f.earlySchedule.$.endDateConstraintIntervals;
        e.push(...g);
      }
      return e;
    }
  }
  __decorate34([calculate('endDateConstraintIntervals')], c.prototype, 'calculateEndDateConstraintIntervals', null);
  return c;
}) {
};
const __decorate35 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const DelayFromParentMixin = class extends Mixin([
  HasChildrenMixin,
  HasSubEventsMixin,
  HasDateConstraintMixin
], a => {
  const b = a.prototype;
  class c extends a {
    get usesDelayFromParent() {
      return this.constructor.usesDelayFromParent;
    }
    isTaskPinnableWithConstraint() {
      if (this.usesDelayFromParent && (this.parent && !this.parent.isRoot)) {
        return false;
      }
      return b.isTaskPinnableWithConstraint.call(this);
    }
    updateDelaysAfterShrinkWrap() {
      const e = Math.min(...this.children.map(f => f.delayFromParent || 0));
      if (e !== 0) {
        for (const f of this.children) {
          this.graph.write(f.$.delayFromParent, f.delayFromParent - e);
        }
      }
    }
    updateParentEvent(e, f) {
      if (f && this.usesDelayFromParent && e.usesDelayFromParent) {
        f.usesDelayFromParent && f.updateDelaysAfterShrinkWrap();
        for (const g of e.children) {
          this.graph.write(g.$.delayFromParent, null);
        }
      }
    }
    *calculateDelayFromParent() {
      let e = yield ProposedOrPrevious;
      const f = yield this.$.parentEvent;
      if (this.usesDelayFromParent && f && !f.isRoot && e == null) {
        let g = yield ProposedOrPreviousValueOf(f.$.startDate);
        if (!g) {
          const j = f.children.flatMap(k => {
            let l, n;
            return (n = (l = k.getData('startDate')) == null ? void 0 : l.getTime()) != null ? n : [];
          });
          g = new Date(Math.min(...j));
        }
        const i = yield ProposedOrPreviousValueOf(this.$.startDate);
        if (i) {
          if (i.getTime() < g.getTime()) {
            e = -(yield* this.calculateProjectedDuration(i, g));
          } else {
            e = yield* this.calculateProjectedDuration(g, i);
          }
        }
      }
      return e;
    }
    writeStartDate(e, f, g, i, j = true) {
      const {
        parent: k,
        children: l,
        project: n
      } = this;
      if (this.usesDelayFromParent && !n.isInitialTransaction) {
        if (k && !k.isRoot && k.graph && !n.isStmRestoring) {
          const o = k.run('calculateProjectedDuration', k.startDate, i);
          this.graph.write(this.$.delayFromParent, o);
          !k.manuallyScheduled && k.updateDelaysAfterShrinkWrap();
        }
        if (!j && (l == null ? void 0 : l.length) && this.manuallyScheduled && i.getTime() < this.startDate.getTime()) {
          for (const p of l) {
            const q = n.convertDuration(p.startDate.getTime() - i.getTime(), TimeUnit.Millisecond, this.durationUnit);
            this.graph.write(p.$.delayFromParent, q);
          }
        }
      }
      return b.writeStartDate.call(this, e, f, g, i, j);
    }
    *calculateStartDateConstraintIntervals() {
      const e = yield* b.calculateStartDateConstraintIntervals.call(this);
      const f = yield this.$.delayFromParent;
      if (f != null) {
        const g = yield this.$.parentEvent;
        const i = yield* g.isManuallyScheduled();
        let j;
        if (!i) {
          const k = yield g.earlySchedule.$.startDateConstraintIntervals;
          const l = yield g.$.startDateConstraintIntervals;
          if (k.length || l.length) {
            const n = intersectIntervals([
              ...k,
              ...l
            ]);
            j = n.startDate;
          }
        }
        if (!j) {
          j = yield ProposedValueOf(g.$.startDate);
        }
        if (!j && g.parent && !g.parent.isRoot) {
          j = yield PreviousValueOf(g.$.startDate);
        }
        if (j) {
          if (!i || this.project.skipNonWorkingTimeWhenSchedulingManually) {
            j = yield* this.skipWorkingTime(j, true, f);
          } else {
            j = DateHelper.add(j, f, this.durationUnit);
          }
          e.push(DateConstraintInterval.new({
            'owner': this,
            'side': ConstraintIntervalSide.Start,
            'startDate': j,
            'endDate': null
          }));
        }
      }
      return e;
    }
    *calculateStartDate() {
      let e;
      const f = yield* b.calculateStartDate.call(this);
      if (this.usesDelayFromParent) {
        if ((e = this.children) == null ? void 0 : e.length) {
          for (const g of this.children) {
            yield g.$.startDateConstraintIntervals;
          }
        }
        if (yield* this.isManuallyScheduled()) {
          const i = yield this.$.startDateConstraintIntervals;
          if (i.length) {
            return i[i.length - 1].startDate;
          }
        }
      }
      return f;
    }
  }
  c.scheduleMixinClass = Mixin([
    a.scheduleMixinClass,
    DelayFromParentScheduleMixin
  ], e => e);
  c.usesDelayFromParent = true;
  __decorate35([model_field({ 'type': 'number' })], c.prototype, 'delayFromParent', void 0);
  __decorate35([calculate('delayFromParent')], c.prototype, 'calculateDelayFromParent', null);
  return c;
}) {
};
const SchedulerProBaseEvent = class extends Mixin([
  SchedulerBasicEvent,
  HasDateConstraintMixin,
  HasPercentDoneMixin,
  SchedulerProHasAssignmentsMixin,
  HasEffortMixin,
  HasSchedulingModeMixin,
  FixedDurationMixin,
  ConstrainedEarlyEventMixin,
  ConstrainedByDependenciesEventMixin,
  SplitEventMixin
], a => {
  class b extends a {
  }
  return b;
}) {
};
const SchedulerProEvent = class extends Mixin([
  SchedulerProBaseEvent,
  DelayFromParentMixin
], a => {
  const b = a.prototype;
  class c extends a {
  }
  return c;
}) {
};
const __decorate36 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const ResourceAllocationEventRangeCalendarIntervalMixin = class extends CalendarIntervalMixin {
  static get fields() {
    return [{
        'name': 'isWorking',
        'type': 'boolean',
        'defaultValue': true
      }];
  }
};
const ResourceAllocationEventRangeCalendarIntervalStore = class extends CalendarIntervalStore {
  static get defaultConfig() {
    return { 'modelClass': ResourceAllocationEventRangeCalendarIntervalMixin };
  }
};
const ResourceAllocationEventRangeCalendar = class extends BaseCalendarMixin {
  get intervalStoreClass() {
    return ResourceAllocationEventRangeCalendarIntervalStore;
  }
};
__decorate36([model_field({
    'type': 'boolean',
    'defaultValue': false
  })], ResourceAllocationEventRangeCalendar.prototype, 'unspecifiedTimeIsWorking', void 0);
const BaseAllocationInterval = class extends Base2 {
  constructor() {
    super(...arguments);
    this.effort = 0;
    this.units = 0;
  }
};
const AssignmentAllocationInterval = class extends BaseAllocationInterval {
  constructor() {
    super(...arguments);
    this.inEventTimeSpan = false;
  }
};
const ResourceAllocationInterval = class extends BaseAllocationInterval {
  constructor() {
    super(...arguments);
    this.maxEffort = 0;
    this.isOverallocated = false;
    this.isUnderallocated = false;
    this.inEventTimeSpan = false;
    this.assignments = null;
    this.assignmentIntervals = null;
  }
};
const BaseAllocationInfo = class extends Entity.mix(Base2) {
  getDefaultAllocationIntervalClass() {
    return BaseAllocationInterval;
  }
  initialize(a) {
    a = Object.assign({
      'includeInactiveEvents': false,
      'allocationIntervalClass': this.getDefaultAllocationIntervalClass()
    }, a);
    super.initialize(a);
  }
};
__decorate36([field()], BaseAllocationInfo.prototype, 'includeInactiveEvents', void 0);
__decorate36([field()], BaseAllocationInfo.prototype, 'allocation', void 0);
const ResourceAllocationInfo = class extends BaseAllocationInfo {
  enterGraph(a) {
    super.enterGraph(a);
  }
  leaveGraph(a) {
    let b, c;
    if (this.eventRangesCalendar) {
      (b = this.resource) == null ? void 0 : b.getProject().clearCombinationsWith(this.eventRangesCalendar);
    }
    super.leaveGraph(a);
    (c = this.resource) == null ? void 0 : c.entities.delete(this);
  }
  getDefaultAllocationIntervalClass() {
    return ResourceAllocationInterval;
  }
  *shouldIncludeAssignmentInAllocation(a) {
    const b = yield a.$.event, c = yield this.$.includeInactiveEvents, e = b && (yield b.$.inactive), f = b && (yield b.$.startDate), g = b && (yield b.$.endDate);
    return Boolean(b && f && g && (c || !e));
  }
  *calculateAllocation() {
    let a;
    const b = [], c = yield this.ticks, e = yield this.$.resource, f = yield this.$.includeInactiveEvents, g = yield e.$.assigned, i = yield e.$.effectiveCalendar, j = new Map(), k = [], l = new Map(), n = new Map();
    const o = yield e.$.maxUnits;
    let p = false, q, r, u;
    for (const F of g) {
      if (!(yield* this.shouldIncludeAssignmentInAllocation(F)))
        continue;
      yield F.$.units;
      const G = yield F.$.event;
      const H = yield G.$.ignoreResourceCalendar;
      const I = yield G.$.startDate;
      const J = yield G.$.endDate;
      const K = yield G.$.segments;
      const L = yield G.$.effectiveCalendar;
      p = p || H;
      if (K) {
        for (const O of K) {
          const P = yield O.$.startDate;
          const Q = yield O.$.endDate;
          k.push({
            'startDate': P,
            'endDate': Q,
            'assignment': F
          });
        }
      } else {
        k.push({
          'startDate': I,
          'endDate': J,
          'assignment': F
        });
      }
      let N = j.get(L);
      if (!N) {
        N = [];
        j.set(L, N);
      }
      l.set(F, new Map());
      n.set(F, []);
      N.push(F);
    }
    if (this.eventRangesCalendar) {
      (a = this.resource) == null ? void 0 : a.getProject().clearCombinationsWith(this.eventRangesCalendar);
    }
    const v = this.eventRangesCalendar = new ResourceAllocationEventRangeCalendar({ 'intervals': k });
    const w = [
      c,
      v,
      ...j.keys()
    ];
    const z = new Map();
    for (const R of c.intervalStore) {
      const S = ResourceAllocationInterval.new({
        'tick': R,
        'resource': e
      });
      z.set(R, S);
      b.push(S);
      l.forEach((T, U) => {
        const V = AssignmentAllocationInterval.new({
          'tick': R,
          'assignment': U
        });
        T.set(R, V);
        n.get(U).push(V);
      });
    }
    const A = b[0].tick.startDate, B = b[b.length - 1].tick.endDate, C = {
        'startDate': A,
        'endDate': B,
        'calendars': w,
        'includeNonWorkingIntervals': p
      }, E = B.getTime() - A.getTime();
    if (E > e.getProject().maxCalendarRange) {
      C.maxRange = E;
    }
    yield* e.forEachAvailabilityInterval(C, (T, U, V) => {
      const W = V.getCalendarsWorkStatus();
      if (W.get(c)) {
        const X = V.intervalsByCalendar.get(c)[0], Y = U.getTime() - T.getTime(), Z = z.get(X), a0 = Z.assignments || new Set(), a1 = Z.assignmentIntervals || new Map();
        if (u && u !== X) {
          const a6 = z.get(u);
          a6.isUnderallocated = a6.isUnderallocated || a6.effort && a6.effort < a6.maxEffort;
        }
        u = X;
        if (!Z.assignments) {
          q = 0;
          r = 0;
        }
        let a2 = 0, a3 = false, a4, a5 = 0;
        V.intervalsByCalendar.get(v).forEach(a7 => {
          const a8 = a7.assignment;
          const a9 = a8 == null ? void 0 : a8.event;
          if (a9 && W.get(a9.effectiveCalendar) && (a9.ignoreResourceCalendar || W.get(i))) {
            const aa = Math.max(T.getTime(), a8.event.startDate.getTime());
            const ab = Math.min(U.getTime(), a8.event.endDate.getTime());
            a3 = true;
            a4 = ab - aa;
            const ac = l.get(a8).get(X);
            const ad = a4 * a8.units / 100;
            ac.effort += ad;
            ac.units = a8.units;
            ac.inEventTimeSpan = true;
            a5 += ad;
            a2 += a8.units;
            a0.add(a8);
            a1.set(a8, ac);
          }
        });
        Z.inEventTimeSpan = Z.inEventTimeSpan || a3;
        if (W.get(i)) {
          Z.maxEffort += Y * o / 100;
        }
        if (a2) {
          if (a4) {
            q += a4 * a2;
            r += a4;
            Z.units = q / r;
          } else if (!q) {
            Z.units = a2;
          }
        }
        if (a3) {
          Z.effort += a5;
          Z.assignments = a0;
          Z.assignmentIntervals = a1;
          Z.isOverallocated = Z.isOverallocated || Z.effort > Z.maxEffort || Z.units > o;
          Z.isUnderallocated = Z.isUnderallocated || a5 < Y || Z.units < o;
        }
      }
    });
    if (u) {
      const T = z.get(u);
      const U = T.resource.maxUnits;
      T.isUnderallocated = T.isUnderallocated || T.effort && T.effort < T.maxEffort;
    }
    return {
      'owner': this,
      'resource': e,
      'total': b,
      'byAssignments': n
    };
  }
};
__decorate36([field()], ResourceAllocationInfo.prototype, 'resource', void 0);
__decorate36([calculate('allocation')], ResourceAllocationInfo.prototype, 'calculateAllocation', null);
const SchedulerProResourceMixin = class extends Mixin([BaseResourceMixin], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.observers = new Set();
      this.entities = new Set();
    }
    addObserver(e) {
      this.graph.addIdentifier(e);
      this.observers.add(e);
    }
    removeObserver(e) {
      if (this.graph) {
        this.graph.removeIdentifier(e);
      }
      this.observers.delete(e);
    }
    addEntity(e) {
      this.graph.addEntity(e);
      this.entities.add(e);
    }
    removeEntity(e) {
      if (this.graph) {
        this.graph.removeEntity(e);
      }
      this.entities.delete(e);
    }
    leaveGraph(e) {
      for (const f of this.observers) {
        this.removeObserver(f);
      }
      for (const g of this.entities) {
        this.removeEntity(g);
      }
      b.leaveGraph.call(this, e);
    }
    *forEachAvailabilityInterval(e, f) {
      const g = this.getProject();
      const i = yield this.$.effectiveCalendar;
      const j = g.combineCalendars([i].concat(e.calendars || []));
      const k = g.maxCalendarRange;
      const l = e.includeNonWorkingIntervals;
      if (k) {
        e = Object.assign({ 'maxRange': k }, e);
      }
      return j.forEachAvailabilityInterval(e, (n, o, p) => {
        const q = p.getCalendarsWorkStatus();
        if (l || q.get(i)) {
          return f(n, o, p);
        }
      });
    }
  }
  __decorate36([model_field({
      'type': 'number',
      'defaultValue': 100
    }, { 'sync': true })], c.prototype, 'maxUnits', void 0);
  return c;
}) {
};
const __decorate37 = function (a, b, e, f) {
  let g = arguments.length, j = g < 3 ? b : f === null ? f = Object.getOwnPropertyDescriptor(b, e) : f, k;
  if (typeof Reflect === 'object' && typeof Reflect.decorate === 'function')
    j = Reflect.decorate(a, b, e, f);
  else
    for (let l = a.length - 1; l >= 0; l--)
      if (k = a[l])
        j = (g < 3 ? k(j) : g > 3 ? k(b, e, j) : k(b, e)) || j;
  return g > 3 && j && Object.defineProperty(b, e, j), j;
};
const SchedulerProProjectMixin = class extends Mixin([
  SchedulerBasicProjectMixin,
  ConstrainedEarlyEventMixin,
  HasChildrenMixin
], a => {
  const b = a.prototype;
  class c extends a {
    constructor() {
      super(...arguments);
      this.isInitialTransaction = true;
    }
    construct(e = {}) {
      let f, g;
      this.eventSegmentModelClass = e.eventSegmentModelClass || this.getDefaultEventSegmentModelClass();
      this.autoSetConstraints = (f = e.autoSetConstraints) != null ? f : false;
      b.construct.call(this, e);
      this.includeAsapAlapAsConstraints = (g = e.includeAsapAlapAsConstraints) != null ? g : true;
      if (!this.resourceAllocationInfoClass)
        this.resourceAllocationInfoClass = this.getDefaultResourceAllocationInfoClass();
    }
    finalizeCommitHook(e) {
      this.isInitialTransaction = false;
      return false;
    }
    getDefaultEventStoreClass() {
      return ChronoEventStoreMixin;
    }
    getDefaultEventSegmentModelClass() {
      return SchedulerProEventSegment;
    }
    getDefaultResourceAllocationInfoClass() {
      return ResourceAllocationInfo;
    }
    afterConfigure() {
      b.afterConfigure.apply(this, arguments);
      this.dateConstraintIntervalClass = this.dateConstraintIntervalClass || DateConstraintInterval;
      this.dependencyConstraintIntervalClass = this.dependencyConstraintIntervalClass || DependencyConstraintInterval;
    }
    getType() {
      return ProjectType.SchedulerPro;
    }
    getDefaultCycleEffectClass() {
      return SchedulerProCycleEffect;
    }
    getDefaultEventModelClass() {
      return SchedulerProEvent;
    }
    getDefaultDependencyModelClass() {
      return SchedulerProDependencyMixin;
    }
    getDefaultAssignmentModelClass() {
      return SchedulerProAssignmentMixin;
    }
    getDefaultResourceModelClass() {
      return SchedulerProResourceMixin;
    }
    async validateDependency(e, f, g, i) {
      let j;
      if (i) {
        j = Array.isArray(i) ? i : [i];
      }
      const k = CI(e.outgoingDeps).some(l => l.toEvent === f && !(j == null ? void 0 : j.includes(l)));
      if (k) {
        return DependencyValidationResult.DuplicatingDependency;
      }
      if (e.contains(f) || f.contains(e)) {
        return DependencyValidationResult.CyclicDependency;
      }
      if (await this.isDependencyCyclic(e, f, g, j)) {
        return DependencyValidationResult.CyclicDependency;
      }
      return DependencyValidationResult.NoError;
    }
    *calculateDurationPure() {
      const e = yield this.$.startDate;
      const f = yield this.$.endDate;
      if (!e || !f)
        return null;
      if (e > f)
        return 0;
      return yield* super.calculateDurationPure();
    }
    async isValidDependency(e, f, g, i) {
      const j = await this.validateDependency(e, f, g, i);
      return j === DependencyValidationResult.NoError;
    }
    getDependencyCycleDetectionIdentifiers(e, f) {
      return [
        f.earlySchedule.$.startDateConstraintIntervals,
        f.earlySchedule.$.endDateConstraintIntervals
      ];
    }
    async isDependencyCyclic(f, g, i, j) {
      const k = this.getDependencyStore().modelClass;
      const l = new k({
        'fromEvent': f,
        'toEvent': g,
        'type': i
      });
      const n = this.replica.branch({
        'autoCommit': false,
        'onComputationCycle': 'throw'
      });
      if (j) {
        if (!Array.isArray(j)) {
          j = [j];
        }
        j.forEach(o => n.removeEntity(o));
      }
      n.addEntity(l);
      l.project = this;
      try {
        await Promise.all(this.getDependencyCycleDetectionIdentifiers(f, g).map(o => n.readAsync(o)));
        return false;
      } catch (o) {
        if (/cycle/i.test(o))
          return true;
        if (!/conflict/i.test(o)) {
          throw o;
        }
      } finally {
        n.clearBranch();
      }
    }
    async isValidDependencyModel(e, f) {
      return this.isValidDependency(e.fromEvent, e.toEvent, e.type, f);
    }
  }
  __decorate37([model_field({
      'type': 'string',
      'defaultValue': DependenciesCalendar.ToEvent
    })], c.prototype, 'dependenciesCalendar', void 0);
  __decorate37([model_field({
      'type': 'boolean',
      'defaultValue': true
    })], c.prototype, 'autoCalculatePercentDoneForParentTasks', void 0);
  __decorate37([model_field({
      'type': 'string',
      'defaultValue': StartedTaskScheduling.Auto
    })], c.prototype, 'startedTaskScheduling', void 0);
  __decorate37([model_field({
      'type': 'boolean',
      'defaultValue': true
    })], c.prototype, 'addConstraintOnDateSet', void 0);
  __decorate37([model_field({
      'type': 'boolean',
      'defaultValue': true
    })], c.prototype, 'autoMergeAdjacentSegments', void 0);
  return c;
}) {
};
const DeactivateDependencyCycleEffectResolution = class extends Localizable_default(SchedulingIssueEffectResolution) {
  static get $name() {
    return 'DeactivateDependencyCycleEffectResolution';
  }
  getDescription() {
    return this.L('L{descriptionTpl}');
  }
  resolve(a) {
    a.active = false;
  }
};
var SchedulerProCycleEffect = class extends CycleEffect {
  getInvalidDependencies() {
    if (!this._invalidDependencies) {
      const a = this.getDependencies();
      this._invalidDependencies = a.filter(b => b.fromEvent === b.toEvent || (b.fromEvent.contains(b.toEvent) || b.toEvent.contains(b.fromEvent)));
    }
    return this._invalidDependencies;
  }
  buildInvalidDependencyResolutions(a) {
    return [
      this.removeDependencyConflictResolutionClass.new(a),
      this.deactivateDependencyConflictResolutionClass.new(a)
    ];
  }
  matchDependencyBySourceAndTargetEvent(a, b, c) {
    return a.active && super.matchDependencyBySourceAndTargetEvent(a, b, c);
  }
  getResolutions() {
    if (!this._resolutions) {
      const a = this.getInvalidDependencies();
      const b = [];
      for (const c of a) {
        b.push(...this.buildInvalidDependencyResolutions({ 'dependency': c }));
      }
      if (!a.length) {
        b.push(this.deactivateDependencyCycleEffectResolutionClass.new(), ...super.getResolutions());
      }
      this._resolutions = b;
    }
    return this._resolutions;
  }
};
__decorate37([prototypeValue(DeactivateDependencyCycleEffectResolution)], SchedulerProCycleEffect.prototype, 'deactivateDependencyCycleEffectResolutionClass', void 0);
__decorate37([prototypeValue(RemoveDependencyResolution)], SchedulerProCycleEffect.prototype, 'removeDependencyConflictResolutionClass', void 0);
__decorate37([prototypeValue(DeactivateDependencyResolution)], SchedulerProCycleEffect.prototype, 'deactivateDependencyConflictResolutionClass', void 0);
const CalendarEditorIntervalBaseModel = class extends GridRowModel {
  static fields = [
    'type',
    'name',
    'calendar',
    {
      'name': 'startDate',
      'type': 'date'
    },
    {
      'name': 'endDate',
      'type': 'date'
    },
    'color',
    'compositeCode'
  ];
  static $name = 'CalendarEditorIntervalBaseModel';
  construct(a) {
    const {
      intervals: b,
      isTypeDefined: c
    } = a;
    this._isTypeDefined = c;
    this.intervals = b instanceof Set ? b : new Set(b);
    delete a.isTypeDefined;
    delete a.intervals;
    if (!a.compositeCode) {
      a.compositeCode = StringHelper.generateUUID();
    }
    super.construct(...arguments);
  }
  get isTypeDefined() {
    return this._isTypeDefined;
  }
  get isOverride() {
    return this.startDate || this.endDate;
  }
  toJSON() {
    const a = super.toJSON();
    if (a.calendar) {
      a.calendar = a.calendar.id;
    }
    return a;
  }
};
CalendarEditorIntervalBaseModel._$name = 'CalendarEditorIntervalBaseModel';
const AvailabilityRangeModel = class extends Model {
  static errors = {
    'errorMissingDate': 'errorMissingDate',
    'errorStartAfterEnd': 'errorStartAfterEnd',
    'errorOverlap': 'errorOverlap'
  };
  static fields = [
    {
      'name': 'startDate',
      'type': 'date',
      'format': 'HH:mm'
    },
    {
      'name': 'endDate',
      'type': 'date',
      'format': 'HH:mm',
      'convert'(a, ...b) {
        a = DateDataField.prototype.convert.call(this, a, ...b);
        if (a) {
          a = DateHelper.getTime(a);
          if (DateHelper.isMidnight(a)) {
            a = DateHelper.getStartOfNextDay(a);
          }
        }
        return a;
      }
    }
  ];
  static $name = 'AvailabilityRangeModel';
  get isValid() {
    return !this.getErrors();
  }
  getErrors(a = {}) {
    const {
        startDate: startDate = this.startDate,
        endDate: endDate = this.endDate
      } = a, {errors: b} = this.constructor, c = [];
    if (!startDate || !endDate) {
      c.push(b.errorMissingDate);
    } else {
      if (startDate > endDate) {
        c.push(b.errorStartAfterEnd);
      }
      const e = this.firstStore;
      if (e && (e == null ? void 0 : e.validateOverlap({
          'startDate': startDate,
          'endDate': endDate,
          'record': this
        })) === false) {
        c.push(b.errorOverlap);
      }
    }
    return c.length ? c : null;
  }
};
AvailabilityRangeModel._$name = 'AvailabilityRangeModel';
const AvailabilityRangeStore = class extends Store {
  static configurable = {
    'modelClass': AvailabilityRangeModel,
    'verifyNoGeneratedIds': false
  };
  static $name = 'AvailabilityRangeStore';
  invertRanges(a = this.getRange(), b = this.modelClass) {
    return this.constructor.invertRanges(a, b);
  }
  static invertRanges(a, b = AvailabilityRangeModel) {
    if (!(a == null ? void 0 : a.length)) {
      return a;
    }
    a.sort((i, j) => i.startDate - j.startDate);
    const c = DateHelper.clearTime(a[0].startDate), e = DateHelper.add(c, 1, 'day'), f = [];
    let g = [];
    g = b.new({ 'startDate': c });
    f.push(g);
    for (const {
          startDate: i,
          endDate: j
        } of a) {
      g.endDate = i;
      g = b.new({ 'startDate': j });
      f.push(g);
    }
    g.endDate = e;
    if (f[0].startDate - f[0].endDate === 0) {
      f.shift();
    }
    if (f[f.length - 1].startDate - f[f.length - 1].endDate === 0) {
      f.pop();
    }
    if (f[0] && f[0].startDate - f[f.length - 1].endDate === 0) {
      f[f.length - 1].endDate = f[0].endDate;
      f.shift();
    }
    return f;
  }
  get isValid() {
    return !this.getErrors(1);
  }
  getErrors(a = 0) {
    let b = new Set();
    for (const c of this.allRecords) {
      const e = c.getErrors();
      if (e) {
        b = new Set([
          ...b,
          ...e
        ]);
      }
      if (a && b.size > a)
        break;
    }
    return b.size ? Array.from(b) : null;
  }
  validateOverlap({
    startDate: a,
    endDate: b,
    record: c
  }) {
    return !this.allRecords.some(e => e !== c && e.startDate && e.endDate && DateHelper.intersectSpans(a, b, e.startDate, e.endDate));
  }
};
AvailabilityRangeStore._$name = 'AvailabilityRangeStore';
const CalendarEditorExceptionModel = class extends RecurringTimeSpan_default(CalendarEditorIntervalBaseModel) {
  static errors = {
    'errorMissingDate': 'errorMissingDate',
    'errorStartAfterEnd': 'errorStartAfterEnd',
    'errorInvalidAvailability': 'errorInvalidAvailability',
    'errorStartAndEndRepeatNumberMismatch': 'errorStartAndEndRepeatNumberMismatch'
  };
  static fields = [
    {
      'name': 'availability',
      'type': 'store',
      'usesId': true,
      'storeClass': AvailabilityRangeStore
    },
    {
      'name': 'isWorking',
      'type': 'boolean',
      'defaultValue': false
    },
    'rawIntervalId',
    'recurrentStartDate',
    'recurrentEndDate'
  ];
  static $name = 'CalendarEditorExceptionModel';
  get isException() {
    return true;
  }
  doDestroy() {
    let a, b;
    (a = this.startRecurrence) == null ? void 0 : a.destroy();
    (b = this.endRecurrence) == null ? void 0 : b.destroy();
    super.doDestroy();
  }
  get basedOnInterval() {
    return [...this.intervals][0];
  }
  setValue(a, b) {
    if (a === 'isWorking') {
      b = Boolean(b);
    }
    return super.setValue(a, b);
  }
  get recurrentStartDate() {
    return this.get('recurrentStartDate');
  }
  parseDateSchedule(a) {
    let b = a;
    if (a && a !== Object(a)) {
      b = later_default.parse.text(a.trim());
      if (b !== Object(b) || b.error >= 0) {
        b = StringHelper.safeJsonParse(a);
      }
    }
    return b;
  }
  set recurrentStartDate(a) {
    const b = this.basedOnInterval;
    if (a) {
      const c = (b || this).parseDateSchedule(a);
      if (c && !this.readingStarRecurrence) {
        this.startRecurrence = c.schedules[0];
      }
    }
    this.set('recurrentStartDate', a);
  }
  get recurrentEndDate() {
    return this.get('recurrentEndDate');
  }
  set recurrentEndDate(a) {
    const b = this.basedOnInterval;
    if (a) {
      const c = (b || this).parseDateSchedule(a);
      if (c && !this.readingEndRecurrence) {
        this.endRecurrence = c.schedules[0];
      }
    }
    this.set('recurrentEndDate', a);
  }
  set startRecurrence(a) {
    let b;
    (b = this._startRecurrenceAfterChangeDetacher) == null ? void 0 : b.call(this);
    if (a && !a.isRecurrenceModel) {
      a = this.buildRecurrenceModel({
        'date': this.startDate,
        'laterJsSchedule': a
      });
    }
    if (a == null ? void 0 : a.isRecurrenceModel) {
      this._startRecurrenceAfterChangeDetacher = FunctionHelper.after(a, 'afterChange', 'afterStartRecurrenceChange', this);
    }
    this._startRecurrence = a;
    this.afterStartRecurrenceChange();
  }
  get startRecurrence() {
    return this._startRecurrence;
  }
  get endRecurrence() {
    return this._endRecurrence;
  }
  set endRecurrence(a) {
    let b;
    const c = this;
    (b = c._endRecurrenceAfterChangeDetacher) == null ? void 0 : b.call(c);
    if (a && !a.isRecurrenceModel) {
      a = c.buildRecurrenceModel({
        'date': c.endDate,
        'laterJsSchedule': a
      });
    }
    if (a == null ? void 0 : a.isRecurrenceModel) {
      c._endRecurrenceAfterChangeDetacher = FunctionHelper.after(a, 'afterChange', 'afterEndRecurrenceChange', c);
    }
    c._endRecurrence = a;
    c.afterEndRecurrenceChange();
  }
  afterStartRecurrenceChange() {
    let a;
    const b = this;
    b.readingStarRecurrence = true;
    b.recurrentStartDate = ((a = b.startRecurrence) == null ? void 0 : a.laterJsSchedule) || null;
    if (b.startRecurrence && b.endRecurrence) {
      b.endRecurrence.set({
        'frequency': b.startRecurrence.frequency,
        'endDate': b.startRecurrence.endDate,
        'count': b.startRecurrence.count
      });
    }
    b.readingStarRecurrence = false;
  }
  afterEndRecurrenceChange() {
    let a;
    this.readingEndRecurrence = true;
    this.recurrentEndDate = ((a = this.endRecurrence) == null ? void 0 : a.laterJsSchedule) || null;
    this.readingEndRecurrence = false;
  }
  processRawInterval(a) {
    const b = this;
    b.rawIntervalId = a.id;
    b.recurrentStartDate = a.recurrentStartDate;
    b.recurrentEndDate = a.recurrentEndDate;
    b.availability = a.availability;
    if (b.isWorking && !b.availability.count) {
      b.availability = [{
          'startDate': '00:00',
          'endDate': '00:00'
        }];
    }
  }
  buildRawInterval() {
    let a;
    const b = this, c = ((a = b.basedOnInterval) == null ? void 0 : a.toJSON()) || {}, {
        availability: e,
        rawIntervalId: f
      } = b, g = {
        ...c,
        'type': 'Exception',
        'isWorking': b.isWorking,
        'name': b.name,
        'startDate': b.startDate,
        'endDate': b.endDate,
        'recurrentStartDate': b.recurrentStartDate,
        'recurrentEndDate': b.recurrentEndDate,
        'compositeCode': b.compositeCode
      };
    if (f) {
      g.id = f;
    }
    if (e == null ? void 0 : e.count) {
      g.isWorking = true;
      if (e.count > 1 || !DateHelper.isMidnight(e.first.startDate) || !DateHelper.isMidnight(e.first.endDate)) {
        g.availability = e.toJSON();
      }
    } else if (b.isWorking === false) {
      g.isWorking = false;
      g.availability = null;
    }
    return g;
  }
  buildRawIntervals() {
    return [this.buildRawInterval()];
  }
  buildRecurrenceModel(a = {}) {
    return RecurrenceModel.new({ ...a });
  }
  get isValid() {
    return !this.getErrors();
  }
  getErrors(a = {}) {
    let b, c, e, f;
    const g = this, {errors: i} = g.constructor, {
        startDate: startDate = g.startDate,
        endDate: endDate = g.endDate,
        availability: availability = g.availability,
        startRecurrence: startRecurrence = g.startRecurrence,
        endRecurrence: endRecurrence = g.endRecurrence
      } = a, j = [];
    if (startRecurrence) {
      if (endRecurrence) {
        if (((b = startRecurrence.monthDays) == null ? void 0 : b.length) !== ((c = endRecurrence.monthDays) == null ? void 0 : c.length) || ((e = startRecurrence.months) == null ? void 0 : e.length) !== ((f = endRecurrence.months) == null ? void 0 : f.length)) {
          j.push(i.errorStartAndEndRepeatNumberMismatch);
        }
      }
    } else if (!startDate || !endDate) {
      j.push(i.errorMissingDate);
    } else if (endDate && startDate > endDate) {
      j.push(i.errorStartAfterEnd);
    }
    if (availability && !availability.isValid) {
      j.push(i.errorInvalidAvailability);
    }
    return j.length ? j : null;
  }
};
CalendarEditorExceptionModel._$name = 'CalendarEditorExceptionModel';
const DayAvailabilityModel = class extends GridRowModel {
  static fields = [{
      'name': 'availability',
      'type': 'store',
      'storeClass': AvailabilityRangeStore,
      'usesId': true
    }];
  static $name = 'DayAvailabilityModel';
  getAvailabilityStoreValue(a) {
    const {allRecords: b} = a;
    return b.filter(c => c.startDate || c.endDate).map(c => {
      const e = c.persistableData;
      if (!a.usesId) {
        delete e.id;
      }
      return e;
    });
  }
};
DayAvailabilityModel._$name = 'DayAvailabilityModel';
const DayAvailabilityStore = class extends Localizable_default(Store) {
  static configurable = {
    'modelClass': DayAvailabilityModel,
    'sorters': [{
        'id': 'weekStartDaySorter',
        'fn': (a, b) => {
          const c = a.id + (a.id < DateHelper.weekStartDay ? 7 : 0), e = b.id + (b.id < DateHelper.weekStartDay ? 7 : 0);
          return c - e;
        }
      }]
  };
  static $name = 'CalendarEditorStore';
  onLocalized() {
    this.sort();
  }
};
DayAvailabilityStore._$name = 'DayAvailabilityStore';
const MIN_DATE3 = DateHelper.getTime(0);
const CalendarEditorWeekModel = class extends CalendarEditorIntervalBaseModel {
  static errors = {
    'errorStartAfterEnd': 'errorStartAfterEnd',
    'errorInvalidWeekAvailability': 'errorInvalidWeekAvailability',
    'errorNoWeekAvailability': 'errorNoWeekAvailability'
  };
  static fields = [{
      'name': 'availability',
      'type': 'store',
      'usesId': true,
      'storeClass': DayAvailabilityStore
    }];
  static $name = 'CalendarEditorWeekModel';
  get isWeek() {
    return true;
  }
  get isOverride() {
    return this.startDate || this.endDate;
  }
  initAvailabilityStore(a) {
    a.data = a.data || ArrayHelper.populate(7, b => ({ 'id': b }));
    a.syncDataOnLoad = true;
  }
  hasAvailability(a, b = this.availability) {
    if (a !== void 0) {
      const c = b.getById(a);
      return c.availability.count;
    }
    return b.allRecords.some(e => this.hasAvailability(e.id));
  }
  static dayName(a) {
    switch (a) {
    case 0:
      return 'Sun';
    case 1:
      return 'Mon';
    case 2:
      return 'Tue';
    case 3:
      return 'Wed';
    case 4:
      return 'Thu';
    case 5:
      return 'Fri';
    case 6:
      return 'Sat';
    default:
      throw new Error('Unknown day value passed: ' + a);
    }
  }
  static buildDaysRule(a) {
    if (a.length && a.length < 7) {
      const b = a.map(c => this.dayName(c)).join(',');
      return 'on ' + b;
    }
    return '';
  }
  static buildSolidDayIntervals(a, b = {}, c) {
    let e;
    const f = [], g = a.slice();
    g.sort((k, l) => k - l);
    if (g[0] === 0 && g[g.length - 1] === 6) {
      let k, l;
      for (const n of g) {
        if (n - l > 1) {
          k = n;
          break;
        }
        l = n;
      }
      g.sort((o, p) => {
        const q = o < k ? o + 10 : o, r = p < k ? p + 10 : p;
        return q - r;
      });
    }
    let i, j;
    for (const o of g) {
      if (!j || o - i > 1) {
        if (j) {
          j.recurrentEndDate = this.buildDaysRule([i === 6 ? 0 : ++i]);
        }
        const p = ((e = c.shift()) == null ? void 0 : e.toJSON()) || {};
        f.push(j = {
          ...p,
          'recurrentStartDate': this.buildDaysRule([o]),
          ...b
        });
      }
      i = o;
    }
    if (j) {
      j.recurrentEndDate = this.buildDaysRule([i === 6 ? 0 : ++i]);
    }
    return f;
  }
  buildRawIntervals() {
    const {
        name: a,
        startDate: b,
        endDate: c,
        availability: e,
        calendar: f,
        intervals: g,
        compositeCode: i
      } = this, {unspecifiedTimeIsWorking: j} = f, k = this.constructor, l = [], n = new Map(), o = [], p = g ? [...g] : [];
    if (e) {
      for (const {
            id: q,
            availability: r
          } of e) {
        if (r == null ? void 0 : r.count) {
          const u = j ? r.invertRanges() : r.getRange();
          for (const {
                startDate: v,
                endDate: w
              } of u) {
            const z = DateHelper.format(v, 'HH:mm') + '-' + DateHelper.format(w, 'HH:mm');
            if (!n.get(z)) {
              n.set(z, []);
            }
            n.get(z).push(q);
          }
        } else {
          l.push(q);
        }
      }
      if (l.length && j) {
        o.push(...k.buildSolidDayIntervals(l, {
          'type': 'Week',
          'isWorking': false,
          'name': a,
          'startDate': b,
          'endDate': c,
          'compositeCode': i
        }, p));
      }
      if (n.size) {
        n.forEach((A, B) => {
          let C;
          const [E, F] = B.split('-');
          if (E === '00:00' && F === '00:00') {
            if (!j) {
              o.push(...k.buildSolidDayIntervals(A, {
                'type': 'Week',
                'isWorking': true,
                'name': a,
                'startDate': b,
                'endDate': c,
                'compositeCode': i
              }, p));
            }
          } else {
            const G = k.buildDaysRule(A), H = (G + ' at ' + E).trim(), I = F === '00:00' ? null : (G + ' at ' + F).trim(), J = ((C = p.shift()) == null ? void 0 : C.toJSON()) || {};
            o.push({
              ...J,
              'type': 'Week',
              'isWorking': !j,
              'recurrentStartDate': H,
              'recurrentEndDate': I,
              'name': a,
              'startDate': b,
              'endDate': c,
              'compositeCode': i
            });
          }
        });
      }
    }
    return o;
  }
  async buildCalendarSaneCopy(a, b) {
    const {
        unspecifiedTimeIsWorking: c,
        name: e,
        parent: f
      } = a, g = new a.constructor({
        'unspecifiedTimeIsWorking': c,
        'name': e
      }), i = new b({
        'calendar': a,
        'autoPull': false,
        'autoPush': false
      });
    await i.pullFromCalendar();
    i.remove(i.query(j => j.isException), true);
    await i.pushToCalendar(g);
    i.destroy();
    if (f && !f.isRoot && !f.isDestroyed) {
      g.parent = await this.buildCalendarSaneCopy(f, b);
    }
    return g;
  }
  async buildCalendarCopy(a) {
    const {calendar: b} = this, {
        unspecifiedTimeIsWorking: c,
        name: e,
        parent: f
      } = b, g = new b.constructor({
        'unspecifiedTimeIsWorking': c,
        'name': e
      });
    if (!this.isTypeDefined && f && !f.isRoot) {
      g.parent = await this.buildCalendarSaneCopy(f, a);
    }
    return g;
  }
  async collectAvailability(a) {
    const b = [...this.intervals].map(n => n.copy()), c = {}, e = [], f = await this.buildCalendarCopy(a.constructor);
    let g = this.startDate || MIN_DATE3;
    if (!DateHelper.isMidnight(g)) {
      g = DateHelper.constrain(DateHelper.getStartOfNextDay(g, true), g, this.endDate);
    }
    let j = g, k;
    for (let n = 0; n < 7; n++) {
      const o = j.getDay();
      c[o] = {
        'id': o,
        'availability': null
      };
      k = DateHelper.clearTime(DateHelper.add(j, 1, 'day'));
      e.push({
        'startDate': j,
        'endDate': k,
        'prioritySortValue': -1
      });
      j = k;
    }
    f.addIntervals([
      ...e,
      ...b
    ]);
    let l;
    f.forEachAvailabilityInterval({
      'startDate': g,
      'endDate': e[e.length - 1].endDate
    }, (p, q, r) => {
      if (r.getIsWorking()) {
        const u = c[p.getDay()];
        u.availability = u.availability || [];
        p = DateHelper.format(p, 'HH:mm');
        q = DateHelper.format(q, 'HH:mm');
        if (u.availability.length && l.endDate === p) {
          l.endDate = q;
        } else {
          l = {
            'startDate': p,
            'endDate': q
          };
          u.availability.push(l);
        }
      }
    });
    return Object.values(c);
  }
  async afterRawIntervalsProcessed(a) {
    this.availability = await this.collectAvailability(a);
  }
  get isValid() {
    return !this.getErrors();
  }
  getErrors(a = {}) {
    const {
        availability: availability = this.availability,
        startDate: startDate = this.startDate,
        endDate: endDate = this.endDate
      } = a, {errors: b} = this.constructor, c = [];
    if (startDate && endDate && startDate > endDate) {
      c.push(b.errorStartAfterEnd);
    }
    if (availability.some(e => !e.availability.isValid)) {
      c.push(b.errorInvalidWeekAvailability);
    } else if (!this.isOverride && !this.hasAvailability(void 0, availability)) {
      c.push(b.errorNoWeekAvailability);
    }
    return c.length ? c : null;
  }
};
CalendarEditorWeekModel._$name = 'CalendarEditorWeekModel';
const defaultExceptionStartDate = new Date(1900, 0, 1);
const defaultExceptionEndDate = new Date(3000, 0, 1);
const getErrorText = a => {
  const b = LocaleManager_default.locales.En;
  return (b == null ? void 0 : b.CalendarEditorWeekGrid[a]) || (b == null ? void 0 : b.CalendarEditorExceptionPanel[a]) || a;
};
const CalendarEditorStore = class extends Store {
  static intervalColors = [
    {
      'text': 'red',
      'color': '#ff8787'
    },
    {
      'text': 'pink',
      'color': '#f783ac'
    },
    {
      'text': 'purple',
      'color': '#ea80dc'
    },
    {
      'text': 'magenta',
      'color': '#ff4dff'
    },
    {
      'text': 'violet',
      'color': '#9775fa'
    },
    {
      'text': 'indigo',
      'color': '#748ffc'
    },
    {
      'text': 'blue',
      'color': '#4dadf7'
    },
    {
      'text': 'cyan',
      'color': '#3bc9db'
    },
    {
      'text': 'teal',
      'color': '#38d9a9'
    },
    {
      'text': 'green',
      'color': '#69db7c'
    },
    {
      'text': 'gantt-green',
      'color': '#a5d8a7'
    },
    {
      'text': 'lime',
      'color': '#a9e34b'
    },
    {
      'text': 'yellow',
      'color': '#fdd835'
    },
    {
      'text': 'orange',
      'color': '#ffa94d'
    },
    {
      'text': 'deep-orange',
      'color': '#ff7043'
    },
    {
      'text': 'gray',
      'color': '#a0a0a0'
    },
    {
      'text': 'light-gray',
      'color': '#e0e0e7'
    },
    {
      'text': 'black',
      'color': '#000000'
    }
  ];
  static errors = {
    'errorMultipleDefaultWeeks': 'errorMultipleDefaultWeeks',
    'errorNoDefaultWeek': 'errorNoDefaultWeek'
  };
  static properties = {
    '_isAutoPushSuspended': 0,
    '_isAutoPullSuspended': 0,
    'intervalCounter': 0,
    'exceptionCounter': 1,
    'weekCounter': 1
  };
  static configurable = {
    'autoPush': true,
    'autoPull': true,
    'validateRecordsAfterPulling': false,
    'stopPullingOnError': false,
    'autoPushTimeout': 10,
    'autoPullTimeout': 10,
    'modelClass': CalendarEditorExceptionModel,
    'weekModelClass': CalendarEditorWeekModel,
    'exceptionModelClass': CalendarEditorExceptionModel,
    'calendar': null
  };
  static $name = 'CalendarEditorStore';
  construct() {
    this._recordByRawInterval = new Map();
    this._recordByRawKey = new Map();
    super.construct(...arguments);
  }
  internalCalendarBumpVersion() {
    this.trigger('calendarBumpVersion', { 'calendar': this.calendar });
    if (this.autoPull) {
      this.scheduleAutoPull();
    }
  }
  updateCalendar(a) {
    let b;
    const c = this;
    (b = c._calendarBumpVersionDetacher) == null ? void 0 : b.call(c);
    if (a) {
      c._calendarBumpVersionDetacher = FunctionHelper.after(a, 'bumpVersion', 'internalCalendarBumpVersion', c);
      if (c.autoPull) {
        c.scheduleAutoPull();
      }
    } else {
      c.removeAll();
    }
    c.trigger('calendarChange', { 'calendar': a });
  }
  onChange({record: a}) {
    if (this.autoPush && this.calendar) {
      this.scheduleAutoPush();
    }
  }
  addNthNameRecord(a = {}, b = null) {
    let {name: c} = a;
    b = b || new RegExp('^' + StringHelper.escapeRegExp(c) + '(?:\\s*(\\d+))?$');
    const e = [0];
    for (const i of this.query(j => {
        let k;
        return (k = j.name) == null ? void 0 : k.match(b);
      })) {
      e.push(parseInt(i.name.match(b)[1] || 1, 10));
    }
    const f = Math.max(...e);
    if (f) {
      c += ' ' + (f + 1);
    }
    const [g] = this.add({
      ...a,
      'name': c
    });
    return g;
  }
  add(a) {
    const {calendar: b} = this;
    a = ArrayHelper.asArray(a);
    if (!(a == null ? void 0 : a.length)) {
      return;
    }
    if (b) {
      for (const e of a) {
        if (!e.calendar) {
          e.calendar = b;
        }
      }
    }
    const c = super.add(...arguments);
    this.sanitizeInternalMaps();
    return c;
  }
  remove() {
    const a = super.remove(...arguments);
    this.sanitizeInternalMaps();
    return a;
  }
  sanitizeInternalMaps() {
    const {_recordByRawKey: a} = this;
    for (const [b, c] of new Map(a)) {
      const {compositeCode: e} = c;
      if (!this.getById(c) || b !== e) {
        a.delete(b);
      }
    }
    if (a.size !== this.count) {
      for (const f of this) {
        a.set(f.compositeCode, f);
      }
    }
  }
  updateAutoPull(a) {
    if (a && this._calendar) {
      this.scheduleAutoPull();
    }
  }
  get rawStore() {
    let a;
    return (a = this.calendar) == null ? void 0 : a.intervalStore;
  }
  getRandomColor() {
    const a = this.constructor.intervalColors, b = a[Math.round(Math.random() * (a.length - 1))].color;
    return b;
  }
  getUnusedColor() {
    const a = this, b = a.constructor.intervalColors, c = a.getDistinctValues('color');
    let e = a.getRandomColor();
    if (c.length < b.length) {
      while (c.includes(e)) {
        e = a.getRandomColor();
      }
      ;
    }
    return e;
  }
  createRecord(a, b = false, c = false) {
    const e = this;
    if (!a.cls) {
      a.cls = '';
    }
    if (!a.color) {
      a.color = e.intervalCounter ? e.getUnusedColor() : '#4dadf7';
    }
    e.intervalCounter++;
    let f;
    if (a.type === 'Week') {
      f = e.weekModelClass;
      e.weekCounter++;
    } else {
      f = e.exceptionModelClass;
      e.exceptionCounter++;
    }
    return new f(a, e, null, b, false, c);
  }
  afterLoadData() {
    let a;
    (a = super.afterLoadData) == null ? void 0 : a.call(this);
    if (!this.some(b => b.isDefaultWeek, true)) {
      for (const b of this.allRecords) {
        if (b.isWeek && !b.isOverride) {
          b.isDefaultWeek = true;
          break;
        }
      }
    }
  }
  processIntervalRecurrence(a) {
    const b = {
        'type': a.type || 'Exception',
        'isTypeDefined': Boolean(a.type),
        'startDate': a.startDate,
        'endDate': a.endDate
      }, c = a.parseDateSchedule(a.recurrentStartDate);
    if (c) {
      const {
        schedules: e,
        exceptions: f
      } = c;
      if (e.length !== 1 || f.length) {
        return false;
      }
      const [g] = e;
      if (!a.type) {
        const i = [
            'h',
            'm',
            's',
            't'
          ], j = [
            'dw',
            'd'
          ], k = i.reduce((o, q) => o + Number(Boolean(g[q])), 0), l = j.reduce((o, q) => o + (g[q] ? g[q].length || 1 : 0), 0), n = g.Y || g.M || g.dy || g.dc || g.D || g.wm || g.wy;
        b.type = (k || l) && !n ? 'Week' : 'Exception';
      } else {
        b.type = a.type;
      }
      if (g.fd_a && !b.startDate) {
        b.startDate = new Date(g.fd_a[0]);
      }
      if (g.fd_b && !b.endDate) {
        b.endDate = new Date(g.fd_b[0]);
      }
    }
    return b;
  }
  processRawInterval(a) {
    let b;
    const c = this, {
        name: e,
        isWorking: f,
        compositeCode: g,
        compositeIntervalCode: i
      } = a, j = a.getCalendar(), k = c.processIntervalRecurrence(a);
    if (!k) {
      return false;
    }
    if (k.type === 'Exception') {
      k.startDate = k.startDate || defaultExceptionStartDate;
      k.endDate = k.endDate || defaultExceptionEndDate;
      k.name = e || 'Exception ' + c.exceptionCounter;
    } else {
      k.name = e || (k.startDate || k.endDate ? 'Week ' + c.weekCounter : 'Default week');
    }
    const l = c._recordByRawKey.get(i) || c.createRecord({
      ...k,
      'intervals': new Set(),
      'calendar': j,
      'compositeCode': g
    });
    l.isWorking = l.isWorking || f;
    l.intervals.add(a);
    (b = l.processRawInterval) == null ? void 0 : b.call(l, a);
    c._recordByRawInterval.set(a.id, l);
    c._recordByRawKey.set(i, l);
    return l;
  }
  getRecordByRawInterval(a) {
    return this._recordByRawInterval.get(a.id);
  }
  get isAutoPullSuspended() {
    return this._isAutoPullSuspended > 0;
  }
  suspendAutoPull() {
    this._isAutoPullSuspended++;
  }
  resumeAutoPull() {
    if (this._isAutoPullSuspended) {
      this._isAutoPullSuspended--;
    }
  }
  get isAutoPushSuspended() {
    return this._isAutoPushSuspended > 0;
  }
  suspendAutoPush() {
    this._isAutoPushSuspended++;
  }
  resumeAutoPush() {
    if (this._isAutoPushSuspended) {
      this._isAutoPushSuspended--;
    }
  }
  scheduleAutoPush() {
    const a = this;
    if (!a.hasTimeout('autoPush') && !a.isAutoPushSuspended) {
      a.setTimeout({
        'name': 'autoPush',
        'fn': a.pushToCalendar,
        'delay': a.autoPushTimeout
      });
    }
  }
  scheduleAutoPull() {
    const a = this;
    if (!a.hasTimeout('autoPull') && !a.isAutoPullSuspended) {
      a.setTimeout({
        'name': 'autoPull',
        'fn': a.pullFromCalendar,
        'delay': a.autoPullTimeout
      });
    }
  }
  async pullFromCalendar(a = this.calendar, b = false) {
    let c;
    if (!a) {
      return false;
    }
    const e = this, {intervalStore: f} = a, g = new Set(), {stopPullingOnError: i} = e;
    if (a === e.calendar) {
      e.suspendAutoPush();
    }
    e.intervalCounter = 0;
    e.exceptionCounter = 1;
    e.weekCounter = 1;
    e._recordByRawInterval.clear();
    e._recordByRawKey.clear();
    const j = f.allRecords.slice().sort((l, n) => l.internalId > n.internalId ? 1 : -1);
    for (const l of j) {
      const n = e.processRawInterval(l);
      if (n === false) {
        console.warn('CalendarEditorStore: The "' + a.name + '" calendar interval cannot be handled.', 'Please copy its data and report to https://bryntum.com/forum :\n' + JSON.stringify(l.toJSON(), void 0, 2));
        if (!b) {
          e.trigger('pullFromCalendarError', {
            'calendar': a,
            'interval': l
          });
        }
        if (i) {
          if (!b) {
            e.trigger('pullFromCalendarStop', {
              'calendar': a,
              'interval': l
            });
          }
          return false;
        }
      } else {
        g.add(n);
      }
    }
    const k = [...g];
    for (const o of k) {
      await ((c = o.afterRawIntervalsProcessed) == null ? void 0 : c.call(o, e));
    }
    e.data = k;
    e.sanitizeInternalMaps();
    if (e.validateRecordsAfterPulling) {
      e.validateRecords(a);
    }
    if (!b) {
      e.trigger('pullFromCalendar', { 'calendar': a });
    }
    if (a === e.calendar) {
      e.resumeAutoPush();
    }
    return k;
  }
  async pushToCalendar(a = this.calendar, b = false) {
    if (!a || !this.isValid) {
      return false;
    }
    const c = this;
    if (a === c.calendar) {
      c.suspendAutoPull();
    }
    const e = c.allRecords.flatMap(f => f.isValid ? f.buildRawIntervals() : []);
    await a.setIntervals(e != null ? e : []);
    if (a === c.calendar) {
      for (const f of a.intervalStore) {
        c._recordByRawInterval.set(f.id, c._recordByRawKey.get(f.compositeCode));
      }
      c.resumeAutoPull();
    }
    if (!b) {
      c.trigger('pushToCalendar', { 'calendar': a });
    }
    return true;
  }
  validateRecords(a = this.calendar) {
    const b = 'CalendarEditorStore: The "' + a.name + '" calendar errors:', c = [], e = this.getErrors();
    if (e) {
      c.push(e.map(f => getErrorText(f)).join(', '));
    }
    for (const f of this.allRecords) {
      const g = f.getErrors();
      if (g) {
        c.push('record #' + f.id + ': ' + g.map(i => getErrorText(i)).join(', '));
      }
    }
    if (c.length) {
      const i = this.toJSON();
      console.warn([
        b,
        ...c
      ].join('\n \u2022 ') + (i.length ? '\n\nPlease copy the store data and report to https://bryntum.com/forum :\n' + JSON.stringify(this.toJSON(), null, 2) : ''));
    }
  }
  get isValid() {
    return !this.getErrors(1);
  }
  getErrors(a = 0) {
    const b = this;
    let c = false, e = false, f = new Set();
    for (const g of b.allRecords) {
      const i = g.getErrors();
      if (i) {
        f = new Set([
          ...f,
          ...i
        ]);
      }
      if (g.isCalendarEditorWeekModel) {
        if (!g.isOverride) {
          if (c) {
            f.add(b.constructor.errors.errorMultipleDefaultWeeks);
          }
          if (g.hasAvailability()) {
            e = true;
          }
          c = true;
        }
      }
      if (a && f.size > a)
        break;
    }
    if (b.calendar && !b.calendar.unspecifiedTimeIsWorking && !e) {
      f.add(b.constructor.errors.errorNoDefaultWeek);
    }
    return f.size ? Array.from(f) : null;
  }
};
CalendarEditorStore._$name = 'CalendarEditorStore';
const CalendarEditorDateInfo = class extends Widget {
  static configurable = {
    'defaultBindProperty': null,
    'date': null,
    'calendar': null,
    'store': null,
    'dateFormat': 'L',
    'timeFormat': 'LT',
    'refreshTimeout': 100
  };
  static type = 'calendareditordateinfo';
  static $name = 'CalendarEditorDateInfo';
  updateStore() {
    let a;
    this.detachListeners('store');
    (a = this.store) == null ? void 0 : a.ion({
      'name': 'store',
      'change': 'onCalendarStoreDataChange',
      'calendarBumpVersion': 'onCalendarBumpVersion',
      'thisObj': this
    });
  }
  compose() {
    const a = this, {
        date: b,
        calendar: c,
        store: e,
        dateFormat: f,
        timeFormat: g
      } = a, i = {
        'listeners': {
          'click': 'internalOnIntervalClick',
          'delegate': '.b-interval'
        }
      };
    if (b && c && e) {
      const j = c.getDayIntervals(b), k = [], l = [], n = new Set();
      let o, p, q;
      for (const [r, u, v] of j) {
        if (v.isWorking) {
          if (p && r <= p) {
            p = u;
          } else {
            if (p) {
              k.push([
                o,
                p
              ]);
            }
            o = r;
            p = u;
          }
          q = true;
        }
        if (v.getCalendar() !== c) {
          n.add(v.getCalendar());
        } else {
          n.add(e.getRecordByRawInterval(v.mainInterval || v));
        }
      }
      if (p) {
        k.push([
          o,
          p
        ]);
      }
      l.push(...[...n].filter(w => w));
      i.children = [
        {
          'tag': 'span',
          'class': 'b-date-text',
          'text': DateHelper.format(b, f)
        },
        ...q ? [
          a.L('L{isWorking}'),
          {
            'tag': 'ul',
            'class': 'b-time-ranges',
            'children': k.map(([w, z]) => ({
              'tag': 'li',
              'text': a.L('L{rangeTpl}')(DateHelper.format(w, g), DateHelper.format(z, g))
            }))
          }
        ] : [a.L('L{isNotWorking}')],
        ...l.length ? [
          a.L('L{basedOn}'),
          {
            'tag': 'ul',
            'class': 'b-intervals',
            'children': l.map(w => ({
              'tag': 'li',
              'class': {
                'b-icon': 1,
                'b-interval': !w.isCalendarModel,
                'b-calendar': w.isCalendarModel,
                ...w.cls ? { [w.cls]: 1 } : {}
              },
              'style': { '--interval-badge-color': w.color },
              'dataset': { 'interval': w.id },
              'text': (w.isCalendarModel ? w.name + ' (parent calendar)' : w.name) || '<Unnamed>'
            }))
          }
        ] : [' ' + a.L('L{byDefault}')]
      ];
    }
    return i;
  }
  scheduleRecompose() {
    if (!this.hasTimeout('recompose')) {
      this.setTimeout({
        'name': 'recompose',
        'fn': this.recompose,
        'delay': this.recomposeTimeout
      });
    }
  }
  onCalendarStoreDataChange() {
    this.scheduleRecompose();
  }
  onCalendarBumpVersion() {
    this.scheduleRecompose();
  }
  internalOnIntervalClick(a) {
    const b = this.store.getById(a.target.dataset.interval);
    if (b) {
      this.trigger('intervalClick', {
        ...a,
        'interval': b
      });
    }
  }
  onLocalized() {
    this.scheduleRecompose();
  }
};
CalendarEditorDateInfo.initClass();
CalendarEditorDateInfo._$name = 'CalendarEditorDateInfo';
const CalendarEditorLegend = class extends Widget {
  static configurable = {
    'defaultBindProperty': null,
    'store': null
  };
  static type = 'calendareditorlegend';
  static $name = 'CalendarEditorLegend';
  updateStore(a) {
    this.detachListeners('store');
    a.ion({
      'name': 'store',
      'change': 'onCalendarStoreChange',
      'thisObj': this
    });
  }
  compose() {
    const {store: a} = this;
    if (!a) {
      return {};
    }
    return {
      'children': [{
          'tag': 'ul',
          'children': [
            {
              'tag': 'li',
              'class': 'b-working-day',
              'text': this.L('L{workingDay}')
            },
            {
              'tag': 'li',
              'class': 'b-nonworking-day',
              'text': this.L('L{nonWorkingDay}')
            },
            ...a.getRange().map(b => ({
              'tag': 'li',
              'text': b.name || 'Default',
              'class': {
                ...b.cls ? { [b.cls]: 1 } : [],
                'b-interval-legend': 1,
                'b-icon': 1
              },
              'style': { '--interval-badge-color': b.color },
              'dataset': { 'interval': b.id }
            }))
          ]
        }]
    };
  }
  onInternalPaint({firstPaint: a}) {
    if (a) {
      EventHelper.on({
        'element': this.rootElement,
        'click': 'internalOnIntervalClick',
        'delegate': '.b-interval-legend',
        'thisObj': this
      });
    }
  }
  internalOnIntervalClick(a) {
    const b = this.store.getById(a.target.dataset.interval);
    if (b) {
      this.trigger('intervalClick', {
        ...a,
        'interval': b
      });
    }
  }
  onCalendarStoreChange() {
    this.recompose();
  }
  onLocalized() {
    this.recompose();
  }
};
CalendarEditorLegend.initClass();
CalendarEditorLegend._$name = 'CalendarEditorLegend';
const _CalendarEditorDatePicker = class _CalendarEditorDatePicker extends DatePicker {
  static configurable = {
    'menu': {
      'type': 'menu',
      'defaults': { 'localeClass': _CalendarEditorDatePicker },
      'items': {
        'addException': {
          'text': 'L{addException}',
          'onItem': 'up.triggerAddExceptionItem'
        },
        'addWeek': {
          'text': 'L{addWeek}',
          'onItem': 'up.triggerAddWeekItem'
        }
      }
    },
    'store': null,
    'multiSelect': 'range',
    'nonWorkingDays': [],
    'cellRenderer': 'datePickerCellRenderer',
    'menuHideTimeout': 3000
  };
  static type = 'calendareditordatepicker';
  static $name = 'CalendarEditorDatePicker';
  static get delayable() {
    return {
      'refresh': {
        'type': 'buffer',
        'delay': 200
      }
    };
  }
  updateStore(a) {
    this.detachListeners('store');
    a == null ? void 0 : a.ion({
      'name': 'store',
      'change': 'onCalendarStoreChange',
      'calendarChange': 'onCalendarStoreChange',
      'pushToCalendar': 'onCalendarStoreChange',
      'calendarBumpVersion': 'onCalendarStoreChange',
      'thisObj': this
    });
  }
  changeMenu(a) {
    if (!(a == null ? void 0 : a.isMenu)) {
      a = Widget.create(Widget.mergeConfigs({
        'autoShow': false,
        'owner': this
      }, a), 'menu');
    }
    return a;
  }
  updateMenu(a) {
    a == null ? void 0 : a.ion({
      'show': 'onMenuShow',
      'destroy': 'onMenuDestroy',
      'thisObj': this,
      'once': true
    });
  }
  afterConstruct() {
    super.afterConstruct(...arguments);
    this.ion({ 'selectionChange': this.internalOnSelectionChange });
  }
  doDestroy() {
    let a, b, c;
    super.doDestroy();
    (a = this._menu) == null ? void 0 : a.destroy();
    (b = this.elementDetacher) == null ? void 0 : b.call(this);
    (c = this.menuElementDetacher) == null ? void 0 : c.call(this);
  }
  onInternalPaint(...a) {
    let b;
    super.onInternalPaint(...a);
    const c = this;
    (b = c.elementDetacher) == null ? void 0 : b.call(c);
    c.elementDetacher = EventHelper.on({
      'element': c.element,
      'contextmenu': c.internalOnElementContextMenu,
      'thisObj': c
    });
  }
  onCalendarStoreChange() {
    this.refresh();
  }
  onMenuShow({source: a}) {
    const b = this;
    b.menuElementDetacher = EventHelper.on({
      'element': a.element,
      'mouseenter': b.onMenuElementMouseEnter,
      'mouseleave': b.onMenuElementMouseLeave,
      'thisObj': b
    });
  }
  onMenuDestroy() {
    let a;
    (a = this.menuElementDetacher) == null ? void 0 : a.call(this);
  }
  onMenuElementMouseEnter(a) {
    this.menu.clearTimeout('hide');
  }
  onMenuElementMouseLeave(a) {
    const {relatedTarget: b} = a, c = b && this.menu.owns(b);
    if (!c) {
      this.menu.setTimeout('hide', 3000);
    }
  }
  internalOnSelectionChange({selection: a}) {
    if (a[0]) {
      this.showMenu();
    }
  }
  internalOnElementContextMenu(a) {
    a.preventDefault();
    this.showMenu();
  }
  getSelectionTimeSpan() {
    let a;
    const {selection: b} = this, c = (a = b[0]) != null ? a : this.activeDate, e = b[0] ? DateHelper.add(b[b.length - 1], 1, 'day') : DateHelper.getStartOfNextDay(c, true);
    return {
      'startDate': c,
      'endDate': e
    };
  }
  triggerAddExceptionItem() {
    this.trigger('addExceptionItem');
  }
  triggerAddWeekItem() {
    this.trigger('addWeekItem');
  }
  async showMenu() {
    const {menu: a} = this;
    a.clearTimeout('hide');
    a.activeDate = this.activeDate;
    await a.showBy({ 'target': this.getCell(this.activeDate) });
    a.setTimeout('hide', this.menuHideTimeout);
    return a;
  }
  getDateIntervals(a) {
    let b;
    const {store: c} = this, e = (b = c == null ? void 0 : c.calendar) == null ? void 0 : b.getDayIntervals(a), f = new Set(e == null ? void 0 : e.flatMap(([g, i, j]) => c.getRecordByRawInterval(j.mainInterval || j) || []));
    return Array.from(f);
  }
  datePickerCellRenderer({
    cell: a,
    date: b
  }) {
    let c;
    if (this.isConfiguring) {
      return;
    }
    const {store: e} = this, f = this.getDateIntervals(b), g = (c = e == null ? void 0 : e.calendar) == null ? void 0 : c.isDayHoliday(b);
    a.classList.toggle('b-working-day', !g);
    a.classList.toggle('b-nonworking-day', g);
    DomHelper.append(a, {
      'class': 'b-interval-badges',
      'children': f.map(j => ({
        'class': 'b-interval-badge ' + j.cls,
        'style': { '--interval-badge-color': j.color }
      }))
    });
  }
};
const CalendarEditorDatePicker = _CalendarEditorDatePicker;
CalendarEditorDatePicker.initClass();
CalendarEditorDatePicker._$name = 'CalendarEditorDatePicker';
const _CalendarEditorBaseTab = class _CalendarEditorBaseTab extends Container {
  static configurable = {
    'store': null,
    'defaultFocus': a => a.isField,
    'items': {
      'grid': {
        'type': Grid.type,
        'columns': [
          {
            'type': ColorColumn.type,
            'field': 'color',
            'width': 45,
            'minWidth': 45,
            'colors': CalendarEditorStore.intervalColors
          },
          { 'field': 'name' }
        ],
        'readOnly': true,
        'hideHeaders': true,
        'columnLines': false,
        'selectionMode': { 'multiSelect': false },
        'internalListeners': {
          'selectionChange': 'up.onGridSelect',
          'renderRows': 'up.onGridRenderRows'
        },
        'bbar': {
          'items': {
            'addButton': {
              'type': Button.type,
              'icon': 'b-fa-add',
              'cls': { 'b-transparent': 1 },
              'tooltip': 'Add record',
              'onClick': 'up.internalOnAddRecordClick'
            },
            'removeButton': {
              'type': Button.type,
              'icon': 'b-fa-trash',
              'cls': { 'b-transparent': 1 },
              'tooltip': 'Remove record',
              'onClick': 'up.internalOnRemoveRecordClick'
            }
          }
        }
      },
      'panel': {}
    }
  };
  static type = 'calendareditorbasetab';
  static $name = 'CalendarEditorBaseTab';
  updateStore(a) {
    this.widgetMap.grid.store = a;
  }
  updateRecord(a) {
    this._settingRecord = true;
    super.updateRecord(...arguments);
    this.widgetMap.panel.record = a;
    if (a) {
      this.widgetMap.grid.selectRow(a);
    }
    this._settingRecord = false;
  }
  construct() {
    this._stores = [];
    super.construct(...arguments);
    this.widgetMap.grid.rowManager.ion({
      'renderRow': 'onRowManagerRenderRow',
      'thisObj': this
    });
  }
  doDestroy() {
    _CalendarEditorBaseTab.destroy(...this._stores);
    super.doDestroy();
  }
  onRowManagerRenderRow({
    row: a,
    record: b
  }) {
    a.assignCls({ 'b-invalid': !b.isValid });
  }
  onGridSelect({source: a}) {
    if (!this._settingRecord) {
      this.record = a.selectedRecord;
    }
    this.toggleRemoveButton();
  }
  onGridRenderRows({source: a}) {
    if (!a.selectedRecord && a.store.first) {
      a.selectedRecord = a.store.first;
    }
  }
  internalOnAddRecordClick() {
    this.trigger('addRecordClick');
  }
  internalOnRemoveRecordClick() {
    this.trigger('removeRecordClick', { 'record': this.widgetMap.grid.selectedRecord });
  }
  toggleRemoveButton(a) {
    const {grid: b} = this.widgetMap;
    return b.widgetMap.removeButton.disabled = a || !b.selectedRecord;
  }
};
const CalendarEditorBaseTab = _CalendarEditorBaseTab;
CalendarEditorBaseTab.initClass();
CalendarEditorBaseTab._$name = 'CalendarEditorBaseTab';
const CalendarEditorWeekGrid = class extends Grid.mixin(Validatable_default) {
  static configurable = {
    'errorTooltipCachePath': 'validatableGridErrorTooltip',
    'errorTooltipConfig': { 'forSelector': '.b-calendareditorweekgrid.b-invalid' }
  };
  static type = 'calendareditorweekgrid';
  static $name = 'CalendarEditorWeekGrid';
  updateStore(a, b) {
    let c;
    super.updateStore(a, b);
    const e = this;
    e.syncInvalid();
    (c = e.storeChangeListenerDetacher) == null ? void 0 : c.call(e);
    if (a) {
      e.storeChangeListenerDetacher = a.ion({
        'change': e.internalOnStoreChange,
        'thisObj': e
      });
    }
  }
  get inputWrap() {
    return this.element;
  }
  get validity() {
    return {};
  }
  get isValid() {
    const {store: a} = this;
    let b;
    if (a) {
      b = a.getErrors();
      for (const c of Object.values(a.weekModelClass.errors)) {
        this.clearError(c, true);
      }
      for (const e of Object.values(a.constructor.errors)) {
        this.clearError(e, true);
      }
      if (b) {
        for (const f of b) {
          this.setError(f, true);
        }
      }
    }
    return !b;
  }
  internalOnStoreChange() {
    this.syncInvalid();
  }
};
CalendarEditorWeekGrid.initClass();
CalendarEditorWeekGrid._$name = 'CalendarEditorWeekGrid';
const CalendarEditorAvailabilityRangeContainer = class extends Container {
  static configurable = {
    'store': null,
    'defaultAvailability': [
      {
        'startDate': '08:00',
        'endDate': '12:00'
      },
      {
        'startDate': '13:00',
        'endDate': '17:00'
      }
    ],
    'defaultBindProperty': 'store'
  };
  static type = 'calendareditoravailabilityrangecontainer';
  static $name = 'CalendarEditorAvailabilityRangeContainer';
  updateStore(a) {
    this.detachListeners('store');
    a == null ? void 0 : a.ion({
      'name': 'store',
      'change': this.onAvailabilityUpdate,
      'thisObj': this
    });
    this.renderAvailabilityFields(a);
  }
  onAvailabilityUpdate() {
    this.renderAvailabilityFields();
  }
  renderAvailabilityFields(a = this.store) {
    const b = this, c = [], e = new Set();
    b.eachWidget(f => {
      if (!(a == null ? void 0 : a.getById(f.record.id))) {
        if (f.containsFocus) {
          f.revertFocus();
        }
        c.push(f);
      } else {
        e.add(f.record);
      }
    }, false);
    if (a) {
      for (const f of a) {
        if (!e.has(f)) {
          b.addTimeRangeFields(f);
        }
      }
    }
    c.forEach(g => {
      b.remove(g);
      g.destroy();
    });
    b.trigger('renderAvailabilityFields');
    b.isValid;
  }
  addTimeRangeFields(a) {
    this.add({
      'type': 'container',
      'cls': 'b-availability-range',
      'record': a,
      'autoUpdateRecord': true,
      'autoUpdateFields': true,
      'strictRecordMapping': true,
      'isolateFields': true,
      'items': {
        'startDateField': {
          'type': 'timeField',
          'name': 'startDate',
          'flex': 'none',
          'autoExpand': true,
          'triggers': null,
          'required': true,
          'checkValidity': 'up.availabilityStartDateIsValid'
        },
        'separator': '-',
        'endDateField': {
          'type': 'timeField',
          'name': 'endDate',
          'flex': 'none',
          'autoExpand': true,
          'triggers': null,
          'required': true,
          'checkValidity': 'up.availabilityEndDateIsValid',
          'internalListeners': { 'transformTimeValue': 'up.onEndDateValueTransform' }
        },
        'removeRangeButton': {
          'type': 'button',
          'icon': 'b-icon b-fa-xmark',
          'cls': { 'b-transparent': 1 },
          'record': a,
          'onClick': 'up.onRemoveRangeClick',
          'tooltip': 'L{CalendarEditorAvailabilityRangeContainer.removeRange}'
        },
        'addRangeButton': {
          'type': 'button',
          'icon': 'b-icon b-fa-add',
          'cls': { 'b-transparent': 1 },
          'record': a,
          'onClick': 'up.onAddRangeClick',
          'tooltip': 'L{CalendarEditorAvailabilityRangeContainer.addRange}'
        }
      }
    });
  }
  checkAvailabilityValuesValidityForField(a, b = {}) {
    const {record: c} = a.up('container'), e = c.getErrors({
        [a.name]: a.value,
        ...b
      });
    for (const f of Object.values(c.constructor.errors)) {
      a.clearError('L{AvailabilityRangeError.' + f + '}', true);
    }
    if (e) {
      for (const g of e) {
        a.setError('L{AvailabilityRangeError.' + g + '}', true);
      }
    }
  }
  availabilityStartDateIsValid(a) {
    this.checkAvailabilityValuesValidityForField(a);
  }
  availabilityEndDateIsValid(a) {
    const b = a.value && DateHelper.isMidnight(a.value) ? DateHelper.getStartOfNextDay(a.value, true) : a.value;
    this.checkAvailabilityValuesValidityForField(a, { 'endDate': b });
  }
  onEndDateValueTransform(a) {
    const {value: b} = a;
    if (b) {
      let c = DateHelper.getTime(b);
      if (DateHelper.isMidnight(c)) {
        c = DateHelper.getStartOfNextDay(c);
      }
      a.value = c;
    }
  }
  addRanges(a) {
    let b, c;
    const {store: e} = this;
    if ((b = e.last) == null ? void 0 : b.endDate) {
      const f = DateHelper.add((c = e.last) == null ? void 0 : c.endDate, 1, 'h', true), g = DateHelper.add(f, 1, 'h', true);
      e.add({
        'startDate': f,
        'endDate': g
      });
    } else {
      e.add(a || this.defaultAvailability || {});
    }
  }
  onAddRangeClick() {
    this.addRanges();
  }
  onRemoveRangeClick({source: a}) {
    if (this.store.count > 1) {
      this.store.remove(a.record);
    } else {
      a.record.set({
        'startDate': '00:00',
        'endDate': '00:00'
      });
    }
  }
};
CalendarEditorAvailabilityRangeContainer.initClass();
CalendarEditorAvailabilityRangeContainer._$name = 'CalendarEditorAvailabilityRangeContainer';
const _CalendarEditorWeekPanel = class _CalendarEditorWeekPanel extends Container {
  static configurable = {
    'store': null,
    'defaultWorkingDayAvailability': [
      {
        'startDate': '08:00',
        'endDate': '12:00'
      },
      {
        'startDate': '13:00',
        'endDate': '17:00'
      }
    ],
    'copyingContext': null,
    'items': {
      'weekContainer': {
        'type': 'container',
        'defaults': { 'localeClass': _CalendarEditorWeekPanel },
        'items': {
          'weekForm': {
            'weight': 10,
            'type': 'container',
            'cls': 'b-calendareditor-form',
            'autoUpdateRecord': true,
            'strictRecordMapping': true,
            'defaults': { 'localeClass': _CalendarEditorWeekPanel },
            'items': {
              'nameField': {
                'type': 'textfield',
                'name': 'name',
                'label': 'L{name}',
                'cls': 'b-span',
                'required': true
              },
              'colorField': {
                'type': ColorField.type,
                'name': 'color',
                'addNoColorItem': false,
                'colors': CalendarEditorStore.intervalColors
              },
              'startDateField': {
                'type': DateField.type,
                'name': 'startDate',
                'label': 'L{from}',
                'required': true,
                'onChange'({value: a}) {
                  this.nextSibling.min = a;
                }
              },
              'endDateField': {
                'type': DateField.type,
                'name': 'endDate',
                'label': 'L{to}',
                'required': true,
                'onChange'({value: a}) {
                  this.previousSibling.max = a;
                }
              }
            }
          },
          'daysDivider': {
            'weight': 20,
            'html': '',
            'localizableProperties': ['dataset.text'],
            'dataset': { 'text': 'L{days}' },
            'cls': 'b-divider',
            'flex': '1 0 100%'
          },
          ...ArrayHelper.fill(7).reduce((a, b, c) => {
            const e = _CalendarEditorWeekPanel.getDayContainerConfig(c);
            e.weight = 30 + c * 10;
            a[e.ref] = e;
            return a;
          }, {})
        }
      },
      'noRecordAddButton': {
        'type': Button.type,
        'icon': 'b-icon-add',
        'text': 'L{CalendarEditorWeekTab.addWeek}',
        'hidden': true,
        'internalListeners': { 'click': 'up.onAddDefaultWeekClick' }
      }
    }
  };
  static type = 'calendareditorweekpanel';
  static $name = 'CalendarEditorWeekPanel';
  construct() {
    this._weekStartDay = 0;
    this._removedAvailability = new Map();
    return super.construct(...arguments);
  }
  static getDayContainerConfig(a) {
    return {
      'type': 'container',
      'flex': '1 0 100%',
      'cls': 'b-availability-day',
      'ref': 'day' + a + 'Container',
      'isolateFields': true,
      'items': {
        'container': {
          'type': 'container',
          'width': 150,
          'minHeight': 45,
          'items': {
            ['day' + a + 'Name']: {
              'html': '',
              'defaultBindProperty': ''
            },
            ['day' + a + 'Slide']: {
              'type': SlideToggle.type,
              'flex': '0 0 40px',
              'dayId': a,
              'internalListeners': { 'change': 'up.onDayToggle' }
            }
          }
        },
        ['day' + a + 'Ranges']: {
          'type': CalendarEditorAvailabilityRangeContainer.type,
          'dataset': { 'dayId': a },
          'cls': 'b-availability-day-ranges',
          'maxWidth': 300,
          'flex': null,
          'internalListeners': { 'renderAvailabilityFields': 'up.onRenderAvailabilityFields' }
        },
        ['day' + a + 'CopyButton']: {
          'type': Button.type,
          'cls': {
            'b-availability-day-actions': 1,
            'b-day-copy': 1,
            'b-transparent': 1
          },
          'dayId': a,
          'icon': 'b-icon-copy',
          'onClick': 'up.onCopyClick',
          'tooltip': 'L{CalendarEditorWeekPanel.copyDay}',
          'toggleable': true,
          'supportsUnpressing': true,
          'toggleGroup': 'day-copy'
        },
        ['day' + a + 'PasteButton']: {
          'type': Button.type,
          'cls': {
            'b-availability-day-actions': 1,
            'b-day-paste': 1,
            'b-transparent': 1
          },
          'dayId': a,
          'icon': 'b-icon-paste b-fade',
          'onClick': 'up.onPasteClick',
          'tooltip': 'L{CalendarEditorWeekPanel.pasteDay}'
        }
      }
    };
  }
  updateStore(a) {
    this.detachListeners('store');
    a == null ? void 0 : a.ion({
      'name': 'store',
      'pullFromCalendar': this.onPullFromCalendar,
      'thisObj': this
    });
  }
  updateRecord(a) {
    super.updateRecord(...arguments);
    const {widgetMap: b} = this;
    this._removedAvailability.clear();
    b.weekForm.record = a;
    b.startDateField.disabled = b.startDateField.hidden = a == null ? void 0 : a.isDefaultWeek;
    this.element.classList.toggle('b-default-week', Boolean(a == null ? void 0 : a.isDefaultWeek));
    b.endDateField.disabled = b.endDateField.hidden = a == null ? void 0 : a.isDefaultWeek;
    b.weekContainer.hidden = !a;
    b.noRecordAddButton.hidden = Boolean(a);
    this.renderIntervalAvailability(a == null ? void 0 : a.availability);
    this.copyingContext = null;
  }
  updateCopyingContext(a, b) {
    const {widgetMap: c} = this, e = 'day' + (a == null ? void 0 : a.id) + 'Container';
    if (a) {
      c['day' + a.id + 'CopyButton'].tooltip = 'L{CalendarEditorWeekPanel.stopCopying}';
    }
    if (b) {
      const f = c['day' + b.id + 'CopyButton'];
      f.tooltip = 'L{CalendarEditorWeekPanel.copyDay}';
      f.pressed = false;
    }
    c.weekContainer.element.classList.toggle('b-copying', Boolean(a));
    this.element.querySelectorAll('.b-availability-day').forEach(g => g.classList.toggle('b-copied', Boolean(a && g.dataset.ref === e)));
  }
  updateDefaultWorkingDayAvailability(a) {
    this.getConfig('items');
    const {widgetMap: b} = this;
    for (let c = 0; c < 7; c++) {
      b['day' + c + 'Ranges'].defaultAvailability = a;
    }
  }
  onAddDefaultWeekClick() {
    this.trigger('addAddDefaultWeekClick');
  }
  onLocalized() {
    const {weekStartDay: a} = DateHelper, {widgetMap: b} = this;
    if (this._weekStartDay !== a) {
      this._weekStartDay = a;
      let c = 7 - a;
      for (let e = 0; e < a; e++) {
        b['day' + e + 'Container'].weight = 30 + (e + c) * 10;
      }
      c = -a;
      for (let f = a; f < 7; f++) {
        b['day' + f + 'Container'].weight = 30 + (f + c) * 10;
      }
      b.weekContainer._items.sort(Widget.weightSortFn);
      b.weekContainer.layout.renderChildren();
    }
    for (let g = 0; g < 7; g++) {
      b['day' + g + 'Name'].html = DateHelper.getDayName(g);
    }
  }
  onCopyClick({source: a}) {
    const {copyingContext: b} = this, {dayId: c} = a;
    if (b && c === b.id) {
      this.copyingContext = null;
    } else {
      this.copyingContext = this.record.availability.getById(c);
    }
  }
  onPasteClick({source: a}) {
    const {data: b} = this.copyingContext, c = this.record.availability.getById(a.dayId), e = b.availability.map(f => f.copy());
    c.availability.data = e;
  }
  onDayToggle({
    source: a,
    value: b,
    userAction: c
  }) {
    const e = this;
    if (e.record) {
      const {dayId: f} = a, {availability: g} = e.record.availability.getById(f);
      if (c) {
        if (!b) {
          e._removedAvailability.set(f, g.toJSON());
          g.removeAll();
        } else {
          e.widgetMap['day' + f + 'Ranges'].addRanges(e._removedAvailability.get(f));
        }
      }
    }
  }
  onPullFromCalendar({source: a}) {
    const {record: b} = this;
    if (b && !a.includes(b)) {
      this.record = null;
    }
  }
  renderIntervalAvailability(a) {
    if (!(a == null ? void 0 : a.count)) {
      a = ArrayHelper.populate(7, c => ({ 'id': c }));
    }
    const {widgetMap: b} = this;
    for (const c of a) {
      b['day' + c.id + 'Ranges'].store = c.availability;
    }
  }
  onRenderAvailabilityFields({source: a}) {
    let b;
    const {widgetMap: c} = this, e = a.dataset.dayId, f = (b = a.store) == null ? void 0 : b.count;
    c['day' + e + 'Slide'].checked = f;
    c['day' + e + 'Container'].element.classList.toggle('b-disabled', !f);
  }
};
const CalendarEditorWeekPanel = _CalendarEditorWeekPanel;
CalendarEditorWeekPanel.initClass();
CalendarEditorWeekPanel._$name = 'CalendarEditorWeekPanel';
const CalendarEditorWeekTab = class extends CalendarEditorBaseTab {
  static configurable = {
    'defaultWorkingDayAvailability': null,
    'items': {
      'grid': {
        'type': 'calendareditorweekgrid',
        'weight': 100,
        'bbar': {
          'items': {
            'addButton': { 'tooltip': 'L{CalendarEditorWeekTab.addWeek}' },
            'removeButton': { 'tooltip': 'L{CalendarEditorWeekTab.removeWeek}' }
          }
        }
      },
      'panel': {
        'type': 'calendareditorweekpanel',
        'isolateFields': true,
        'weight': 200,
        'internalListeners': { 'addAddDefaultWeekClick': 'up.onAddAddDefaultWeekClick' }
      }
    }
  };
  static type = 'calendareditorweektab';
  static $name = 'CalendarEditorWeekTab';
  updateDefaultWorkingDayAvailability(a) {
    this.widgetMap.panel.defaultWorkingDayAvailability = a;
  }
  internalOnAddRecordClick() {
    const a = {};
    if (!this.widgetMap.grid.store.find(b => b.isOverride && b.hasAvailability())) {
      a.startDate = null;
      a.endDate = null;
    }
    this.trigger('addRecordClick', a);
  }
  onAddAddDefaultWeekClick() {
    this.trigger('addRecordClick', {
      'data': {
        'startDate': null,
        'endDate': null
      }
    });
  }
  onGridSelect({source: a}) {
    super.onGridSelect(...arguments);
  }
  onGridRenderRows({source: a}) {
    super.onGridRenderRows(...arguments);
    this.toggleRemoveButton();
  }
  toggleRemoveButton() {
    super.toggleRemoveButton(this.widgetMap.grid.store.count <= 1);
  }
};
CalendarEditorWeekTab.initClass();
CalendarEditorWeekTab._$name = 'CalendarEditorWeekTab';
const _CalendarEditorExceptionPanel = class _CalendarEditorExceptionPanel extends Container {
  static configurable = {
    'defaultWorkingDayAvailability': [{
        'startDate': '08:00',
        'endDate': '16:00'
      }],
    'autoUpdateRecord': true,
    'strictRecordMapping': true,
    'items': {
      'exceptionForm': {
        'type': 'container',
        'cls': 'b-calendareditor-form',
        'defaults': { 'localeClass': _CalendarEditorExceptionPanel },
        'items': {
          'nameField': {
            'type': 'textfield',
            'name': 'name',
            'label': 'L{name}',
            'required': true
          },
          'colorField': {
            'type': ColorField.type,
            'name': 'color',
            'addNoColorItem': false,
            'colors': CalendarEditorStore.intervalColors
          },
          'exceptionStartDateField': {
            'type': DateField.type,
            'name': 'startDate',
            'label': 'L{from}',
            'checkValidity': 'up.dateFieldIsValid'
          },
          'exceptionEndDateField': {
            'type': DateField.type,
            'name': 'endDate',
            'label': 'L{to}',
            'checkValidity': 'up.dateFieldIsValid'
          },
          'isWorkingField': {
            'type': RadioGroup.type,
            'name': 'isWorking',
            'label': 'L{is}',
            'defaults': { 'localeClass': _CalendarEditorExceptionPanel },
            'items': [
              {
                'text': 'L{nonWorking}',
                'name': 'isWorking',
                'checkedValue': false
              },
              {
                'text': 'L{working}',
                'name': 'isWorking',
                'checkedValue': true
              }
            ],
            'internalListeners': { 'change': 'up.onIsWorkingFieldChange' }
          },
          'availabilitySplitter': {
            'html': '',
            'localizableProperties': ['dataset.text'],
            'dataset': { 'text': 'L{hours}' },
            'cls': 'b-divider b-span-row',
            'hidden': true
          },
          'availabilityContainer': {
            'type': CalendarEditorAvailabilityRangeContainer.type,
            'name': 'availability',
            'cls': 'b-span-row',
            'required': true
          },
          'startRepeatToggle': {
            'type': 'slidetoggle',
            'cls': 'b-slidetoggle-splitter b-span-row',
            'label': 'L{repeat}',
            'internalListeners': { 'change': 'up.onStartRepeatToggleChange' }
          },
          'startRepeatPanel': {
            'type': RecurrenceEditorPanel.type,
            'cls': 'b-span-row',
            'name': 'startRecurrence',
            'defaultBindProperty': 'record',
            'isolateFields': true,
            'autoUpdateRecord': true,
            'items': {
              'monthDaysButtonField': {
                'checkValidity': 'up.startMonthDaysButtonFieldIsValid',
                'internalListeners': { 'change': 'up.onStartMonthDaysButtonFieldChange' }
              },
              'monthsButtonField': {
                'checkValidity': 'up.startMonthsButtonFieldIsValid',
                'internalListeners': { 'change': 'up.onStartMonthsButtonFieldChange' }
              },
              'intervalField': false,
              'stopRecurrenceField': false,
              'frequencyField': {
                'buildItems'() {
                  return this.constructor.prototype.buildItems().filter(a => a.value !== 'DAILY');
                },
                'internalListeners': { 'change': 'up.onStartFrequencyChange' }
              },
              'positionsCombo': {
                'buildItems'() {
                  return Store.new({
                    'fields': [
                      'value',
                      'text',
                      'cls'
                    ],
                    'data': this.buildDayNumbers()
                  });
                }
              },
              'daysCombo': {
                'buildItems'() {
                  return this.weekDays;
                }
              }
            }
          },
          'endRepeatToggle': {
            'type': 'slidetoggle',
            'cls': 'b-slidetoggle-splitter b-span-row',
            'label': 'L{repeatEnd}',
            'internalListeners': { 'change': 'up.onEndRepeatToggleChange' }
          },
          'endRepeatPanel': {
            'type': RecurrenceEditorPanel.type,
            'cls': 'b-span-row',
            'name': 'endRecurrence',
            'defaultBindProperty': 'record',
            'isolateFields': true,
            'autoUpdateRecord': true,
            'items': {
              'monthDaysButtonField': {
                'checkValidity': 'up.endMonthDaysButtonFieldIsValid',
                'internalListeners': { 'change': 'up.onEndMonthDaysButtonFieldChange' }
              },
              'monthsButtonField': {
                'checkValidity': 'up.endMonthsButtonFieldIsValid',
                'internalListeners': { 'change': 'up.onEndMonthsButtonFieldChange' }
              },
              'frequencyField': { 'disabled': true },
              'intervalField': false,
              'stopRecurrenceField': false,
              'positionsCombo': {
                'buildItems'() {
                  return Store.new({
                    'fields': [
                      'value',
                      'text',
                      'cls'
                    ],
                    'data': this.buildDayNumbers()
                  });
                }
              },
              'daysCombo': {
                'buildItems'() {
                  return this.weekDays;
                }
              }
            }
          }
        }
      },
      'noRecordAddButton': {
        'type': 'button',
        'icon': 'b-icon-add',
        'text': 'L{CalendarEditorExceptionTab.addException}',
        'hidden': true,
        'internalListeners': { 'click': 'up.onAddExceptionClick' }
      }
    }
  };
  static type = 'calendareditorexceptionpanel';
  static $name = 'CalendarEditorExceptionPanel';
  updateRecord(a) {
    const {widgetMap: b} = this;
    b.availabilityContainer._lastValue = null;
    b.startRepeatToggle.value = Boolean(a == null ? void 0 : a.startRecurrence);
    b.endRepeatToggle.value = Boolean((a == null ? void 0 : a.startRecurrence) && (a == null ? void 0 : a.endRecurrence));
    super.updateRecord(a);
    this.onIsWorkingFieldChange({ 'value': b.isWorkingField.value });
    this.onStartRepeatToggleChange({ 'value': b.startRepeatToggle.value });
    this.onEndRepeatToggleChange({ 'value': b.endRepeatToggle.value });
    b.exceptionForm.hidden = !a;
    b.noRecordAddButton.hidden = Boolean(a);
  }
  updateDefaultWorkingDayAvailability(a) {
    this.widgetMap.availabilityContainer.defaultAvailability = a;
  }
  validateButtonGroupWidget(a, b) {
    if (this.record) {
      const {errorStartAndEndRepeatNumberMismatch: c} = this.record.constructor.errors;
      a.clearError('L{CalendarEditorExceptionPanel.' + c + '}', true);
      if (b && a.valueAsArray.length !== b.valueAsArray.length) {
        a.setError('L{CalendarEditorExceptionPanel.' + c + '}', true);
        return false;
      }
    }
    return true;
  }
  startMonthDaysButtonFieldIsValid(a) {
    const {widgetMap: b} = this;
    return this.validateButtonGroupWidget(a, b.endRepeatToggle.checked && b.endRepeatPanel.widgetMap.monthDaysButtonField);
  }
  endMonthDaysButtonFieldIsValid(a) {
    return this.validateButtonGroupWidget(a, this.widgetMap.startRepeatPanel.widgetMap.monthDaysButtonField);
  }
  startMonthsButtonFieldIsValid(a) {
    const {widgetMap: b} = this;
    return this.validateButtonGroupWidget(a, b.endRepeatToggle.checked && b.endRepeatPanel.widgetMap.monthButtonField);
  }
  endMonthsButtonFieldIsValid(a) {
    return this.validateButtonGroupWidget(a, this.widgetMap.startRepeatPanel.widgetMap.monthButtonField);
  }
  dateFieldIsValid(a) {
    const {record: b} = this;
    if (b) {
      const {errors: c} = b.constructor;
      Object.values(c).forEach(g => a.clearError(g, true));
      const e = b.getErrors({ [a.name]: a.value }), f = e == null ? void 0 : e.filter(g => g === c.errorMissingDate || g === c.errorStartAfterEnd)[0];
      if (f) {
        a.setError(f, true);
        return false;
      }
    } else {
      a.clearError(null, true);
    }
    return true;
  }
  processValuesBeforeSet(a) {
    if (a) {
      const {
        startRecurrence: b,
        endRecurrence: c
      } = a;
      if (!(b == null ? void 0 : b.isRecurrenceModel)) {
        a.startRecurrence = null;
      }
      if (!(c == null ? void 0 : c.isRecurrenceModel)) {
        a.endRecurrence = null;
      }
    }
  }
  setValues(a, ...b) {
    this.processValuesBeforeSet(a);
    super.setValues(a, ...b);
  }
  set values(a) {
    this.processValuesBeforeSet(a);
    super.values = a;
  }
  get values() {
    return super.values;
  }
  onStartMonthDaysButtonFieldChange({userAction: a}) {
    if (!this._propagatingFieldChange) {
      this._propagatingFieldChange = true;
      this.widgetMap.endRepeatPanel.widgetMap.monthDaysButtonField.triggerFieldChange({ 'userAction': a });
      this._propagatingFieldChange = false;
    }
  }
  onEndMonthDaysButtonFieldChange({userAction: a}) {
    if (!this._propagatingFieldChange) {
      this._propagatingFieldChange = true;
      this.widgetMap.startRepeatPanel.widgetMap.monthDaysButtonField.triggerFieldChange({ 'userAction': a });
      this._propagatingFieldChange = false;
    }
  }
  onStartMonthsButtonFieldChange({userAction: a}) {
    if (!this._propagatingFieldChange) {
      this._propagatingFieldChange = true;
      this.widgetMap.endRepeatPanel.widgetMap.monthsButtonField.triggerFieldChange({ 'userAction': a });
      this._propagatingFieldChange = false;
    }
  }
  onEndMonthsButtonFieldChange({userAction: a}) {
    if (!this._propagatingFieldChange) {
      this._propagatingFieldChange = true;
      this.widgetMap.startRepeatPanel.widgetMap.monthsButtonField.triggerFieldChange({ 'userAction': a });
      this._propagatingFieldChange = false;
    }
  }
  onAddExceptionClick() {
    this.owner.trigger('addRecordClick');
  }
  onStartFrequencyChange({value: a}) {
    let b;
    const {widgetMap: c} = this, {endRepeatToggle: e} = c, f = (b = c.endRepeatPanel) == null ? void 0 : b.widgetMap.frequencyField, g = a === 'WEEKLY';
    if (f) {
      f.value = a;
    }
    if (e) {
      if (g) {
        e.value = false;
        this.onEndRepeatToggleChange({
          'value': false,
          'userAction': true
        });
      }
      e.disabled = g;
    }
  }
  onIsWorkingFieldChange({
    value: a,
    userAction: b
  }) {
    const {
      availabilityContainer: c,
      availabilitySplitter: e
    } = this.widgetMap;
    e.hidden = c.hidden = !a;
    if (b) {
      if (!this.isSettingValues && !c.store.count) {
        c.addRanges(c._lastValue);
      }
      if (a) {
        c.firstItem.focus();
      } else {
        const f = c.store.allRecords.filter(g => g.isValid).map(g => g.toJSON());
        c._lastValue = f.length && f;
        c.store.removeAll();
      }
    }
  }
  onStartRepeatToggleChange({
    userAction: a,
    value: b
  }) {
    const {
      startRepeatPanel: c,
      endRepeatPanel: e,
      endRepeatToggle: f
    } = this.widgetMap;
    if (a) {
      const {record: g} = this, i = b ? g.startRecurrence || g.buildRecurrenceModel({
          'frequency': 'MONTHLY',
          'date': g.startDate
        }) : null;
      c.record = i;
      c.triggerFieldChange({
        'value': i,
        'userAction': a
      });
      if (!b && f.value) {
        f.value = false;
        e.triggerFieldChange({
          'value': null,
          'userAction': a
        });
      }
      this.isValid;
    }
    c.disabled = c.hidden = f.disabled = !b;
  }
  onEndRepeatToggleChange({
    userAction: a,
    value: b
  }) {
    const {
      endRepeatPanel: c,
      startRepeatPanel: e
    } = this.widgetMap;
    if (a) {
      const {record: f} = this, g = e.record, i = b ? f.endRecurrence || f.buildRecurrenceModel({
          'frequency': g.frequency,
          'date': f.endDate
        }) : null;
      c.record = i;
      c.triggerFieldChange({
        'value': i,
        'userAction': a
      });
    }
    c.disabled = c.hidden = !b;
  }
};
const CalendarEditorExceptionPanel = _CalendarEditorExceptionPanel;
CalendarEditorExceptionPanel.initClass();
CalendarEditorExceptionPanel._$name = 'CalendarEditorExceptionPanel';
const _CalendarEditorExceptionTab = class _CalendarEditorExceptionTab extends CalendarEditorBaseTab {
  static configurable = {
    'defaultWorkingDayAvailability': null,
    'items': {
      'grid': {
        'localeClass': _CalendarEditorExceptionTab,
        'emptyText': 'L{noRows}',
        'weight': 100,
        'bbar': {
          'items': {
            'addButton': { 'tooltip': 'L{CalendarEditorExceptionTab.addException}' },
            'removeButton': { 'tooltip': 'L{CalendarEditorExceptionTab.removeException}' }
          }
        }
      },
      'panel': {
        'type': 'calendareditorexceptionpanel',
        'isolateFields': true,
        'weight': 200
      }
    }
  };
  static type = 'calendareditorexceptiontab';
  static $name = 'CalendarEditorExceptionTab';
  updateDefaultWorkingDayAvailability(a) {
    this.widgetMap.panel.defaultWorkingDayAvailability = a;
  }
};
const CalendarEditorExceptionTab = _CalendarEditorExceptionTab;
CalendarEditorExceptionTab.initClass();
CalendarEditorExceptionTab._$name = 'CalendarEditorExceptionTab';
const _CalendarEditor = class _CalendarEditor extends Popup {
  static configurable = {
    'calendar': null,
    'activeDate': null,
    'defaultWorkingDayAvailability': [
      {
        'startDate': '08:00',
        'endDate': '12:00'
      },
      {
        'startDate': '13:00',
        'endDate': '17:00'
      }
    ],
    'defaultWorkingDays': {
      0: false,
      1: true,
      2: true,
      3: true,
      4: true,
      5: true,
      6: false
    },
    'confirmCalendarRemoving': true,
    'store': {
      'autoPush': false,
      'autoPull': false
    },
    'destroyStore': true,
    'clearOnClose': true,
    'autoShow': false,
    'autoUpdateRecord': true,
    'timeFormat': 'LT',
    'rootElement': document.body,
    'title': 'L{workingTimeCalendar}',
    'width': 800,
    'maxHeight': 800,
    'height': '90%',
    'closable': true,
    'draggable': true,
    'items': {
      'mainTabs': {
        'type': 'tabpanel',
        'flex': '1 0 100%',
        'defaults': { 'localeClass': _CalendarEditor },
        'items': {
          'generalTab': {
            'title': 'L{general}',
            'weight': 100,
            'defaults': { 'localeClass': _CalendarEditor },
            'items': {
              'nameField': {
                'type': 'text',
                'name': 'name',
                'label': 'L{name}',
                'flex': '1 0 48%',
                'required': true,
                'weight': 10,
                'labelWidth': '4em'
              },
              'parentField': {
                'type': 'combo',
                'name': 'parentId',
                'label': 'L{parent}',
                'flex': '1 0 48%',
                'displayField': 'name',
                'valueField': 'id',
                'weight': 20
              },
              'unspecifiedTimeIsWorkingField': {
                'type': 'slidetoggle',
                'name': 'unspecifiedTimeIsWorking',
                'text': 'L{daysAreWorkingByDefault}',
                'flex': '1 0 100%',
                'weight': 30
              },
              'divider': {
                'tag': 'hr',
                'weight': 40,
                'flex': '1 0 100%'
              },
              'datePickerLegend': {
                'type': 'calendareditorlegend',
                'flex': '1 0 25%',
                'weight': 50
              },
              'datePicker': {
                'type': 'calendareditordatepicker',
                'isolateFields': true,
                'flex': '1 0 47%',
                'weight': 60
              },
              'dateInfo': {
                'type': 'calendareditordateinfo',
                'flex': '1 0 25%',
                'weight': 70
              }
            }
          },
          'exceptionTab': {
            'title': 'L{exceptions}',
            'type': 'calendareditorexceptiontab',
            'weight': 200,
            'isolateFields': true
          },
          'weekTab': {
            'title': 'L{weeks}',
            'type': 'calendareditorweektab',
            'weight': 300,
            'isolateFields': true
          }
        }
      }
    },
    'bbar': {
      'defaults': { 'localeClass': _CalendarEditor },
      'items': {
        'saveButton': {
          'text': 'L{save}',
          'onClick': 'up.onSaveClick',
          'color': 'b-blue',
          'cls': 'b-raised',
          'weight': 200
        },
        'removeButton': {
          'text': 'L{delete}',
          'onClick': 'up.onRemoveClick',
          'weight': 300
        },
        'cancelButton': {
          'text': 'L{Object.Cancel}',
          'onClick': 'up.onCancelClick',
          'weight': 400
        }
      }
    }
  };
  static type = 'calendareditor';
  static $name = 'CalendarEditor';
  changeStore(a) {
    if (a && !a.isCalendarEditorStore) {
      a = CalendarEditorStore.new(a);
    }
    return a;
  }
  updateStore(a) {
    const {widgetMap: b} = this;
    b.datePickerLegend.store = a;
    b.datePicker.store = a;
    b.dateInfo.store = a;
    b.exceptionTab.store = a.chain(c => c.type === 'Exception', void 0, {
      'calendar': a.calendar,
      'autoPush': false,
      'autoPull': false
    });
    b.weekTab.store = a.chain(c => c.type === 'Week', void 0, {
      'calendar': a.calendar,
      'autoPush': false,
      'autoPull': false
    });
  }
  updateCalendar(a) {
    let b;
    const c = this, {
        widgetMap: e,
        store: f
      } = c, {parentField: g} = e;
    if (!g.store.filters.get('no-loaded-record')) {
      c.buildParentFieldStore(a);
    }
    if (a) {
      g.store.data = a.firstStore.rootNode.copy(null, true).children;
      c._copiedCalendarStore = a.firstStore;
    }
    const i = a ? g.store.findRecord('originalInternalId', a.internalId, true) : null;
    c.record = i;
    f.calendar = i;
    f.pullFromCalendar(null, true).then(() => f.pushToCalendar(null, true)).then(() => {
      f.autoPush = true;
      f.autoPull = true;
    });
    e.exceptionTab.store.calendar = i;
    e.weekTab.store.calendar = i;
    e.dateInfo.calendar = i;
    g.store.filter();
    g.hidden = !e.parentField.store.count;
    e.removeButton.hidden = !a || ((b = a.project) == null ? void 0 : b.calendar) === a;
    if (!c.initialConfig.title) {
      c.title = (a == null ? void 0 : a.isCreating) ? c.L('L{addCalendar}') : c.L('L{workingTimeCalendar}');
    }
  }
  updateLocalization() {
    let a;
    super.updateLocalization();
    const b = this;
    if (!b.initialConfig.title) {
      b.title = ((a = b.calendar) == null ? void 0 : a.isCreating) ? b.L('L{addCalendar}') : b.L('L{workingTimeCalendar}');
    }
  }
  updateTimeFormat(a) {
    this.widgetMap.dateInfo.timeFormat = a;
  }
  updateActiveDate(a) {
    const {widgetMap: b} = this;
    b.datePicker.activeDate = a;
    b.dateInfo.date = a;
  }
  changeDefaultWorkingDayAvailability(a) {
    let b = a;
    if (a) {
      b = a.map(c => {
        let e = c;
        if (c.isModel) {
          e = { ...c.data };
          delete e.id;
        }
        return e;
      });
    }
    return b;
  }
  updateDefaultWorkingDayAvailability(a) {
    const {widgetMap: b} = this;
    b.exceptionTab.defaultWorkingDayAvailability = a;
    b.weekTab.defaultWorkingDayAvailability = a;
  }
  updateConfirmCalendarRemoving(a) {
    this.detachListeners('confirmCalendarRemoving');
    if (a) {
      this.ion({
        'name': 'confirmCalendarRemoving',
        'beforeRemove': this.internalOnBeforeRemove,
        'thisObj': this
      });
    }
  }
  construct(a) {
    super.construct(...arguments);
    const b = this, {widgetMap: c} = b;
    c.mainTabs.ion({
      'tabChange': b.onMainTabTabChange,
      'thisObj': b
    });
    c.datePicker.ion({
      'selectionChange': b.onDatePickerSelectionChange,
      'addExceptionItem': b.onAddExceptionItem,
      'addWeekItem': b.onAddWeekItem,
      'thisObj': b
    });
    c.dateInfo.ion({
      'intervalClick': b.onIntervalLegendClick,
      'thisObj': b
    });
    c.datePickerLegend.ion({
      'intervalClick': b.onIntervalLegendClick,
      'thisObj': b
    });
    c.exceptionTab.ion({
      'addRecordClick': b.onAddExceptionClick,
      'removeRecordClick': b.onRemoveExceptionClick,
      'thisObj': b
    });
    c.weekTab.ion({
      'addRecordClick': b.onAddWeekClick,
      'removeRecordClick': b.onRemoveWeekClick,
      'thisObj': b
    });
  }
  doDestroy() {
    let a, b, c, e, f, g, i;
    const j = this, {widgetMap: k} = j;
    (a = j._calendarBumpVersionDetacher) == null ? void 0 : a.call(j);
    (c = (b = k.parentField.store) == null ? void 0 : b.destroy) == null ? void 0 : c.call(b);
    (f = (e = k.exceptionTab.store) == null ? void 0 : e.destroy) == null ? void 0 : f.call(e);
    (i = (g = k.weekTab.store) == null ? void 0 : g.destroy) == null ? void 0 : i.call(g);
    if (j.destroyStore && j.store) {
      j.store.destroy();
    }
    super.doDestroy();
  }
  onMainTabTabChange({activeItem: a}) {
    a.eachWidget(b => {
      let c;
      return (c = b.syncInvalid) == null ? void 0 : c.call(b);
    }, true);
  }
  onDatePickerSelectionChange({source: a}) {
    this.widgetMap.dateInfo.date = a.activeDate;
    this._activeDate = a.activeDate;
  }
  onIntervalLegendClick({interval: a}) {
    if (a) {
      this.editInterval(a);
    }
  }
  onAddExceptionItem() {
    this.addExceptionInterval();
  }
  onAddExceptionClick({data: a}) {
    this.addExceptionInterval(a);
  }
  onRemoveExceptionClick({record: a}) {
    this.store.remove(a);
  }
  onAddWeekItem() {
    this.addWeekInterval();
  }
  onAddWeekClick({data: a}) {
    this.addWeekInterval(a);
  }
  onRemoveWeekClick({record: a}) {
    this.store.remove(a);
  }
  async onSaveClick() {
    if (await this.save()) {
      this.isSaving = true;
      this.close();
      this.isSaving = false;
    }
  }
  async onRemoveClick() {
    this.isRemoving = true;
    if (await this.remove()) {
      this.close();
    }
    this.isRemoving = false;
  }
  onCancelClick() {
    this.close();
  }
  async internalOnBeforeRemove({calendar: a}) {
    let b;
    const c = this.L('L{confirmation}'), e = this.L('L{Object.Yes}');
    if ((b = a.children) == null ? void 0 : b.length) {
      const f = this.L('L{removeCalendarWithChildren}');
      if (await MessageDialog_default.confirm({
          'title': c,
          'okButton': e,
          'message': f
        }) !== MessageDialog_default.okButton) {
        return false;
      }
    }
    if (a.project.getCalendarConsumers(a).length > 1) {
      const g = this.L('L{removeCalendarAndLinks}');
      if (await MessageDialog_default.confirm({
          'title': c,
          'okButton': e,
          'message': g
        }) !== MessageDialog_default.okButton) {
        return false;
      }
    }
  }
  buildParentFieldStore(a) {
    const b = this, {parentField: c} = b.widgetMap;
    c.store = new a.firstStore.constructor();
    c.store.addFilter({
      'id': 'no-loaded-record',
      'filterBy': e => {
        let f, g;
        return !b._record || !((g = (f = b._record).contains) == null ? void 0 : g.call(f, e, false, true));
      }
    });
  }
  addExceptionInterval(a = {}) {
    const b = this, c = b.store.addNthNameRecord({
        'name': b.L('New exception'),
        ...b.widgetMap.datePicker.getSelectionTimeSpan(),
        ...a,
        'type': 'Exception',
        'calendar': b.record
      });
    return b.editInterval(c);
  }
  addWeekInterval(a = {}) {
    const b = this, {
        defaultWorkingDayAvailability: c,
        defaultWorkingDays: e
      } = b;
    if (!a.availability) {
      const g = a.availability = [];
      for (let i = 0; i < 7; i++) {
        g.push({
          'id': i,
          'availability': e[i] ? c : []
        });
      }
    }
    const f = b.store.addNthNameRecord({
      'name': b.L('New week'),
      ...b.widgetMap.datePicker.getSelectionTimeSpan(),
      ...a,
      'type': 'Week',
      'calendar': b.record
    });
    return b.editInterval(f);
  }
  editInterval(a) {
    const b = a.type.toLowerCase(), c = this.widgetMap[b + 'Tab'];
    c.record = a;
    this.widgetMap.mainTabs.activeTab = c;
  }
  async save(a = this.calendar) {
    let b;
    const c = this;
    if (c.isValid) {
      const e = c.getValues();
      if (e.parentId) {
        const g = c.widgetMap.parentField.store.getById(e.parentId).originalInternalId;
        e.parentId = (b = a.firstStore.getByInternalId(g)) == null ? void 0 : b.id;
      }
      a.set(e);
      const f = await c.store.pushToCalendar(a);
      c.trigger('save', { 'calendar': a });
      return f;
    } else {
      await MessageDialog_default.alert({
        'title': c.L('L{error}'),
        'message': c.L('L{inputErrors}')
      });
      const {mainTabs: i} = c.widgetMap;
      for (const j of i.items) {
        if (!j.isValid) {
          i.activeTab = j;
          break;
        }
      }
    }
    return false;
  }
  async remove(a = this.calendar) {
    let b;
    if (!a || await this.trigger('beforeRemove', { 'calendar': a }) === false) {
      return false;
    }
    (b = a.project) == null ? void 0 : b.unlinkCalendarConsumers(a);
    a.remove();
    this.trigger('remove', { 'calendar': a });
    this.calendar = null;
    return true;
  }
  close() {
    const a = this;
    if (!a.isSaving && !a.isRemoving) {
      a.isCancelling = true;
      a.trigger('cancel');
    }
    if (a.clearOnClose) {
      a.calendar = null;
      a.activeDate = new Date();
      a.widgetMap.mainTabs.activeTab = 0;
      a.widgetMap.datePicker.selection = null;
    }
    const b = super.close(...arguments);
    a.isCancelling = false;
    return b;
  }
};
const CalendarEditor = _CalendarEditor;
CalendarEditor.initClass();
CalendarEditor._$name = 'CalendarEditor';
const ModelCombo = class extends Combo {
  static get $name() {
    return 'ModelCombo';
  }
  static get type() {
    return 'modelcombo';
  }
  get value() {
    const a = super.value, b = this.store.getById(a);
    return b || a;
  }
  set value(a) {
    super.value = a;
  }
};
ModelCombo.initClass();
ModelCombo._$name = 'ModelCombo';
const CalendarField = class extends ModelCombo {
  static type = 'calendarfield';
  static $name = 'CalendarField';
  static get configurable() {
    return {
      'valueField': 'id',
      'displayField': 'name',
      'editable': false,
      'clearable': true,
      'calendarConsumerRecord': null,
      'calendarEditor': {
        'type': 'calendareditor',
        'autoClose': true,
        'autoShow': false,
        'isolateFields': true,
        'centered': true,
        'modal': true
      },
      'store': null,
      'listItemTpl': a => {
        return a.name || this.L('L{Default calendar}');
      },
      'displayValueRenderer': (a, b) => {
        let c, e;
        a = a || ((e = (c = b.store) == null ? void 0 : c.project) == null ? void 0 : e.effectiveCalendar);
        return (a == null ? void 0 : a.name) || this.L('L{Default calendar}');
      }
    };
  }
  changeCalendarEditor(a) {
    const b = this;
    b.detachListeners('calendarEditor');
    const c = a ? Widget.create(a) : a;
    if (c) {
      c.owner = b;
      c.ion({
        'name': 'calendarEditor',
        'save': b.onCalendarEditorSave,
        'cancel': b.onCalendarEditorCancel,
        'remove': b.onCalendarEditorRemove,
        'thisObj': b
      });
    }
    return c;
  }
  updateProject() {
    this.syncCalendarEditorTrigger();
  }
  updateCalendarConsumerRecord(a) {
    const b = this;
    b.getConfig('project');
    if (a) {
      if (!b.project && a.project) {
        b.project = a.project;
      }
      if (!b.value && a.calendar) {
        b.value = a.calendar;
      }
    }
    b.syncCalendarEditorTrigger();
  }
  updateCalendarEditor(a) {
    this.getConfig('triggers');
    this.triggers = {
      'calendarEditor': a && {
        'weight': -100,
        'cls': 'b-icon-calendar',
        'handler': 'onCalendarEditorClick',
        'tooltip': 'L{CalendarField.editCalendar}',
        'key': 'shift++'
      } || null
    };
    this.syncCalendarEditorTrigger();
  }
  doDestroy() {
    let a, b;
    (b = (a = this.calendarEditor) == null ? void 0 : a.destroy) == null ? void 0 : b.call(a);
    super.doDestroy(...arguments);
  }
  onCalendarEditorSave({calendar: a}) {
    a.isCreating = false;
  }
  onCalendarEditorCancel({source: a}) {
    const {calendar: b} = a;
    if (b == null ? void 0 : b.isCreating) {
      this.value = null;
      this.store.remove(b);
    }
  }
  onCalendarEditorRemove() {
    this.value = null;
  }
  syncCalendarEditorTrigger() {
    const a = this, b = a.triggers.calendarEditor;
    if (b) {
      const c = a.calendarEditor && a.project && a.calendarConsumerRecord;
      if (c) {
        b.cls = a.value ? 'b-icon-calendar' : 'b-icon-calendar-plus';
        b.hidden = false;
      } else {
        b.hidden = true;
      }
    }
  }
  onCalendarEditorClick() {
    const a = this;
    if (!a.readOnly && !a.disabled) {
      let b = a.value;
      const {
          calendarEditor: c,
          project: e,
          calendarConsumerRecord: f
        } = a, g = {
          'calendarEditor': c,
          'calendar': b || {}
        };
      if (f.isEventModel || f.isProjectModel) {
        c.activeDate = f.startDate;
      }
      if (!b) {
        g.calendar.name = f.name + ' - calendar';
      }
      a.trigger('beforeEditCalendar', g);
      b = g.calendar;
      if (!(b == null ? void 0 : b.isCalendarModel) && e) {
        const i = e.effectiveCalendar;
        if (!b.name) {
          b.name = i.name ? i.name + ' - copy' : 'New calendar';
        }
        b = i.copy(b);
        b.isCreating = true;
        a.store.add(b);
        a.value = b;
      }
      if (b == null ? void 0 : b.isCalendarModel) {
        c.calendar = b;
        c.show();
      }
    }
  }
  get value() {
    return super.value;
  }
  set value(a) {
    let b;
    if ((b = a == null ? void 0 : a.isDefault) == null ? void 0 : b.call(a)) {
      a = null;
    }
    this.getConfig('calendarConsumerRecord');
    super.value = a;
    this.syncCalendarEditorTrigger();
  }
  compose() {
    this.getConfig('calendarEditor');
    return super.compose();
  }
  syncEmpty() {
    let a;
    const {
        isEmpty: b,
        element: c
      } = this, e = (a = this.triggers) == null ? void 0 : a.clear;
    if (c) {
      if (e) {
        e[b ? 'hide' : 'show']();
      }
      c.classList.toggle('b-empty', b);
    }
  }
};
CalendarField.initClass();
CalendarField._$name = 'CalendarField';
const ResourceCalendarColumn = class extends Column.mixin(AttachToProjectMixin_default) {
  static type = 'resourceCalendar';
  static $name = 'ResourceCalendarColumn';
  static get defaults() {
    return {
      'field': 'calendar',
      'text': 'L{calendar}',
      'width': 250,
      'editor': {
        'type': CalendarField.type,
        'clearable': true,
        'allowInvalid': false
      }
    };
  }
  construct() {
    super.construct(...arguments);
    this.grid.ion({
      'beforeCellEditStart': 'onBeforeCellEditStart',
      'thisObj': this
    });
  }
  attachToProject(a) {
    if (a) {
      this.defaultCalendar = a.defaultCalendar;
      this.editor.store = a.calendarManagerStore;
    }
  }
  attachToResourceStore(a) {
    super.attachToResourceStore(a);
    if (a) {
      a.ion({
        'name': 'resourceStore',
        'update': 'onResourceUpdate',
        'thisObj': this
      });
    }
  }
  onResourceUpdate({
    record: a,
    changes: b
  }) {
    let c;
    const e = b[this.field];
    if (e) {
      if (typeof e.oldValue === 'string' && ((c = e.value) == null ? void 0 : c.id) === e.oldValue) {
        return;
      }
      this.refreshCell(a);
    }
  }
  onBeforeCellEditStart({
    grid: a,
    editorContext: b
  }) {
    const {editor: c} = b;
    if (b.column === this && c.calendarEditor) {
      c.calendarConsumerRecord = b.record;
      c.calendarEditor.activeDate = a.visibleDateRange.startDate;
      c.cellEditing = true;
    }
  }
  defaultRenderer({value: a}) {
    if (a == null ? void 0 : a.id) {
      const b = this.editor.store.getById(a.id);
      return (b == null ? void 0 : b.getValue(this.editor.displayField)) || '';
    } else {
      return '';
    }
  }
};
ColumnStore.registerColumnType(ResourceCalendarColumn);
ResourceCalendarColumn._$name = 'ResourceCalendarColumn';
const throwIfNotTheSameStore = (a, b) => {
  if (a !== b) {
    throw new Error('Store set is prohibited for Scheduler Pro entity!');
  }
};
const PartOfProject_default2 = a => class b extends (a || Base) {
  static get $name() {
    return 'PartOfProject';
  }
  get taskStore() {
    return this.eventStore;
  }
  set taskStore(c) {
    this.eventStore = c;
  }
  get eventStore() {
    let c;
    return (c = this.project) == null ? void 0 : c.eventStore;
  }
  get leftProjectEventStore() {
    const c = this.leftProject;
    return (c == null ? void 0 : c.getEventStore()) || null;
  }
  set eventStore(c) {
    throwIfNotTheSameStore(this.eventStore, c);
  }
  get dependencyStore() {
    let c;
    return (c = this.project) == null ? void 0 : c.dependencyStore;
  }
  set dependencyStore(c) {
    throwIfNotTheSameStore(this.dependencyStore, c);
  }
  get assignmentStore() {
    let c;
    return (c = this.project) == null ? void 0 : c.assignmentStore;
  }
  set assignmentStore(c) {
    throwIfNotTheSameStore(this.assignmentStore, c);
  }
  get resourceStore() {
    let c;
    return (c = this.project) == null ? void 0 : c.resourceStore;
  }
  set resourceStore(c) {
    throwIfNotTheSameStore(this.resourceStore, c);
  }
  get calendarManagerStore() {
    let c;
    return (c = this.project) == null ? void 0 : c.calendarManagerStore;
  }
  set calendarManagerStore(c) {
    throwIfNotTheSameStore(this.calendarManagerStore, c);
  }
};
const AssignmentModel2 = class extends PartOfProject_default2(AssignmentModelMixin_default(SchedulerProAssignmentMixin.derive(Model))) {
  static isProAssignmentModel = true;
  static $name = 'AssignmentModel';
  get event() {
    const {project: a} = this, b = super.event;
    if (a == null ? void 0 : a.isDelayingCalculation) {
      return a.eventStore.getById(b);
    }
    return b;
  }
  set event(a) {
    super.event = a;
  }
  get resource() {
    const {project: a} = this;
    let b = super.resource;
    if (a == null ? void 0 : a.isDelayingCalculation) {
      b = a.resourceStore.getById(b);
    }
    return b == null ? void 0 : b.$original;
  }
  set resource(a) {
    super.resource = a;
  }
  get eventResourceKey() {
    return this.isInActiveTransaction ? this.buildEventResourceKey(this.event, this.resource) : this.buildEventResourceKey(this.$.event.DATA, this.$.resource.DATA);
  }
};
AssignmentModel2._$name = 'AssignmentModel';
const AssignmentStore2 = class extends PartOfProject_default2(AssignmentStoreMixin_default(ChronoAssignmentStoreMixin.derive(AjaxStore))) {
  static $name = 'AssignmentStore';
  static get defaultConfig() {
    return { 'modelClass': AssignmentModel2 };
  }
};
AssignmentStore2._$name = 'AssignmentStore';
const CalendarIntervalModel = class extends PartOfProject_default2(CalendarIntervalMixin.derive(Model)) {
  static get $name() {
    return 'CalendarIntervalModel';
  }
};
CalendarIntervalModel._$name = 'CalendarIntervalModel';
const CalendarModel = class extends PartOfProject_default2(BaseCalendarMixin.derive(Model)) {
  static get $name() {
    return 'CalendarModel';
  }
  static get fields() {
    return [
      {
        'name': 'expanded',
        'internal': true,
        'defaultValue': true
      },
      'ignoreTimeZone'
    ];
  }
  toString() {
    return this.name || '';
  }
  static get defaultConfig() {
    return { 'calendarIntervalModelClass': CalendarIntervalModel };
  }
};
CalendarModel._$name = 'CalendarModel';
const CalendarManagerStore = class extends PartOfProject_default2(ChronoCalendarManagerStoreMixin.derive(AjaxStore)) {
  static get defaultConfig() {
    return {
      'tree': true,
      'modelClass': CalendarModel,
      'loadPriority': 100,
      'syncPriority': 100,
      'id': 'calendars'
    };
  }
  afterConstruct() {
    super.afterConstruct(...arguments);
  }
  validateAllRecordsForCalendarEditor() {
    for (const a of this.allRecords) {
      this.validateRecordForCalendarEditor(a);
    }
  }
  validateRecordForCalendarEditor(a) {
    const b = this._calendarEditorStore = this._calendarEditorStore || new CalendarEditorStore();
    b.validateRecordsAfterPulling = true;
    b.autoPush = false;
    b.autoPull = false;
    b.calendar = a;
    b.pullFromCalendar();
  }
};
CalendarManagerStore._$name = 'CalendarManagerStore';
const DependencyModel2 = class extends PartOfProject_default2(SchedulerProDependencyMixin.derive(DependencyBaseModel)) {
  static get $name() {
    return 'DependencyModel';
  }
  static get isProDependencyModel() {
    return true;
  }
  get fromEvent() {
    let a;
    if ((a = this.project) == null ? void 0 : a.isDelayingCalculation) {
      return this.project.eventStore.getById(super.fromEvent);
    }
    return super.fromEvent;
  }
  set fromEvent(a) {
    super.fromEvent = a;
  }
  get toEvent() {
    let a;
    if ((a = this.project) == null ? void 0 : a.isDelayingCalculation) {
      return this.project.eventStore.getById(super.toEvent);
    }
    return super.toEvent;
  }
  set toEvent(a) {
    super.toEvent = a;
  }
};
DependencyModel2._$name = 'DependencyModel';
const DependencyStore2 = class extends PartOfProject_default2(DependencyStoreMixin_default(ChronoDependencyStoreMixin.derive(AjaxStore))) {
  static get defaultConfig() {
    return { 'modelClass': DependencyModel2 };
  }
};
DependencyStore2._$name = 'DependencyStore';
const PercentDoneMixin_default = a => class b extends a {
  static get $name() {
    return 'PercentDoneMixin';
  }
  get isStarted() {
    return this.percentDone > 0;
  }
  get isCompleted() {
    return this.percentDone >= 100;
  }
  get isInProgress() {
    return this.isStarted && !this.isCompleted;
  }
  copy() {
    const c = super.copy(...arguments);
    c.percentDone = 0;
    c.internalClearChanges();
    return c;
  }
  get renderedPercentDone() {
    const c = typeof this.percentDone === 'number' && !isNaN(this.percentDone) ? this.percentDone : 0;
    return this.getFormattedPercentDone(c);
  }
  getFormattedPercentDone(c = 0) {
    if (c <= 99) {
      return Math.round(c);
    }
    return Math.floor(c);
  }
  set renderedPercentDone(c) {
    this.percentDone = c;
  }
};
const EventSegmentModel = class extends SchedulerProEventSegment.derive(TimeSpan).mixin(EventModelMixin_default, PercentDoneMixin_default) {
  static get $name() {
    return 'EventSegmentModel';
  }
  get task() {
    return this.event;
  }
};
EventSegmentModel._$name = 'EventSegmentModel';
const EventModel2 = class extends SchedulerProEvent.derive(TimeSpan).mixin(RecurringTimeSpan_default, PartOfProject_default2, EventModelMixin_default, PercentDoneMixin_default) {
  static isProEventModel = true;
  static get $name() {
    return 'EventModel';
  }
  static get fields() {
    return [
      {
        'name': 'showInTimeline',
        'type': 'boolean',
        'defaultValue': false
      },
      'note',
      {
        'name': 'preamble',
        'convert': a => a ? new Duration(a) : null,
        'serialize': a => !(a == null ? void 0 : a.magnitude) ? null : a
      },
      {
        'name': 'postamble',
        'convert': a => a ? new Duration(a) : null,
        'serialize': a => !(a == null ? void 0 : a.magnitude) ? null : a
      }
    ];
  }
  getDefaultSegmentModelClass() {
    return EventSegmentModel;
  }
  updateWrapDate(a, b, c = true) {
    b = new Duration(b);
    if (!c) {
      b.magnitude *= -1;
    }
    return DateHelper.add(a, b);
  }
  get startDate() {
    let a;
    let b;
    if (this.isOccurrence) {
      b = this.get('startDate');
    } else {
      b = (a = this._startDate) != null ? a : super.startDate;
    }
    if (this.allDay) {
      b = this.constructor.getAllDayStartDate(b);
    }
    return b;
  }
  set startDate(a) {
    const b = this;
    if (b.batching) {
      b._startDate = a;
      b.set({ 'startDate': a });
    } else {
      super.startDate = a;
      if (b.preamble) {
        b.wrapStartDate = null;
        b.wrapEndDate = null;
      }
    }
  }
  get endDate() {
    let a;
    let b;
    if (this.isOccurrence) {
      b = this.get('endDate');
    } else {
      b = (a = this._endDate) != null ? a : super.endDate;
    }
    if (this.allDay) {
      b = this.constructor.getAllDayEndDate(b);
    }
    return b;
  }
  set endDate(a) {
    const b = this;
    if (b.batching) {
      b._endDate = a;
      b.set({ 'endDate': a });
    } else {
      super.endDate = a;
      if (b.postamble) {
        b.wrapStartDate = null;
        b.wrapEndDate = null;
      }
    }
  }
  get fullEffort() {
    return new Duration({
      'unit': this.effortUnit,
      'magnitude': this.effort
    });
  }
  set fullEffort(a) {
    this.setEffort(a.magnitude, a.unit);
  }
  get wrapStartDate() {
    const a = this, {
        preamble: b,
        startDate: c
      } = a, e = a._wrapStartDate;
    let f;
    if (e) {
      f = e;
    } else {
      if (b) {
        f = a.updateWrapDate(c, b, false);
        a._wrapStartDate = f;
      } else {
        f = c;
      }
    }
    return f;
  }
  set wrapStartDate(a) {
    this._wrapStartDate = a;
  }
  get wrapEndDate() {
    const a = this, {
        postamble: b,
        endDate: c
      } = a, e = a._wrapEndDate;
    let f;
    if (e) {
      f = e;
    } else {
      if (b) {
        f = a.updateWrapDate(c, b, true);
        a._wrapEndDate = f;
      } else {
        f = c;
      }
    }
    return f;
  }
  set wrapEndDate(a) {
    this._wrapEndDate = a;
  }
  set(a) {
    const b = typeof a === 'object';
    if (a === 'preamble' || b && 'preamble' in a) {
      this.wrapStartDate = null;
    }
    if (a === 'postamble' || b && 'postamble' in a) {
      this.wrapEndDate = null;
    }
    return super.set(...arguments);
  }
  get outerStartDate() {
    return this.wrapStartDate;
  }
  get outerEndDate() {
    return this.wrapEndDate;
  }
  isEditable(a) {
    switch (a) {
    case 'earlyStartDate':
    case 'earlyEndDate':
    case 'delayFromParent':
      return false;
    case 'percentDone':
    case 'renderedPercentDone':
      return this.isLeaf;
    case 'endDate':
    case 'duration':
    case 'fullDuration':
      return this.isLeaf || this.manuallyScheduled;
    }
    return super.isEditable(a);
  }
  getFieldPersistentValue(a) {
    let b;
    const c = typeof a === 'string' ? this.getFieldDefinition(a) : a, e = (c == null ? void 0 : c.name) || a;
    if (e === 'delayFromParent' && ((b = this.parent) == null ? void 0 : b.isRoot)) {
      return;
    }
    return super.getFieldPersistentValue(a);
  }
  get stm() {
    let a, b;
    return (b = (a = this.recurringEvent) == null ? void 0 : a.stm) != null ? b : super.stm;
  }
  set stm(a) {
    super.stm = a;
  }
  get assigned() {
    const {project: a} = this, b = super.assigned;
    if ((a == null ? void 0 : a.isDelayingCalculation) && !b) {
      return a.assignmentStore.storage.findItem('event', this);
    }
    return b;
  }
  set assigned(a) {
    super.assigned = a;
  }
  getCurrentConfig(a) {
    const {segments: b} = this, c = super.getCurrentConfig(a);
    if (c && b) {
      c.segments = b.map(e => e.getCurrentConfig(a));
    }
    return c;
  }
  endBatch(a, b, c) {
    const e = this, {
        meta: {batchChanges: f}
      } = e;
    super.endBatch(a, b, c);
    if (f) {
      if ('endDate' in f) {
        delete e._endDate;
        if (e.postamble) {
          e.wrapEndDate = null;
        }
      }
      if ('startDate' in f) {
        delete e._startDate;
        if (e.preamble) {
          e.wrapStartDate = null;
        }
      }
    }
  }
  cancelBatch(...a) {
    this.wrapEndDate = null;
    this.wrapStartDate = null;
    super.cancelBatch(...a);
  }
};
EventModel2._$name = 'EventModel';
const EngineMixin10 = PartOfProject_default2(ChronoEventStoreMixin.derive(AjaxStore));
const EventStore2 = class extends EngineMixin10.mixin(RecurringEventsMixin_default, GetEventsMixin_default, DayIndexMixin_default, EventStoreMixin_default) {
  static defaultConfig = { 'modelClass': EventModel2 };
  static $name = 'EventStore';
};
EventStore2._$name = 'EventStore';
const EventUpdateAction = class extends UpdateAction {
  get type() {
    return ACTION_TYPES.EVENT_UPDATE;
  }
  construct(a) {
    let {
      model: b,
      newData: c,
      oldData: e
    } = a;
    if (c.segments && e.segments) {
      e = { ...e };
      c = { ...c };
      const f = e.segments.slice(), g = c.segments.slice();
      let i = false;
      e.segments.forEach((j, k) => {
        const l = c.segments.indexOf(j);
        if (l > -1) {
          f[k] = g[l] = j;
          i = true;
        }
      });
      if (i) {
        e.segments = f;
        c.segments = g;
      }
    }
    return super.construct({
      'model': b,
      'newData': c,
      'oldData': e
    });
  }
};
EventUpdateAction._$name = 'EventUpdateAction';
const ResourceModel2 = class extends PartOfProject_default2(ResourceModelMixin_default(SchedulerProResourceMixin.derive(GridRowModel))) {
  static $name = 'ResourceModel';
  get assigned() {
    let a;
    const {project: b} = this;
    if (b == null ? void 0 : b.assignmentStore.storage.indices.resource) {
      return (a = b.assignmentStore.storage.findItem('resource', this)) != null ? a : new Set();
    }
    return super.assigned;
  }
  set assigned(a) {
    super.assigned = a;
  }
};
ResourceModel2._$name = 'ResourceModel';
const ResourceStore2 = class extends PartOfProject_default2(ResourceStoreMixin_default(ChronoResourceStoreMixin.derive(AjaxStore))) {
  static $name = 'ResourceStore';
  static get defaultConfig() {
    return { 'modelClass': ResourceModel2 };
  }
};
ResourceStore2._$name = 'ResourceStore';
const ResourceUtilizationModel = class extends Model {
  static fields = [
    'name',
    'iconCls'
  ];
  static $name = 'ResourceUtilizationModel';
  construct(a, ...b) {
    this._childrenIndex = new Map();
    if (a.origin) {
      Object.assign(a, this.mapOriginValues(a.origin));
    }
    const {assignmentValues: c} = a;
    delete a.assignmentValues;
    super.construct(a, ...b);
    if (this.origin && !this.generatedParent) {
      this.fillChildren(c);
    }
  }
  mapOriginValues(a) {
    let b, c;
    const e = {};
    if (a.isResourceModel) {
      e.name = a.name;
    } else if (a.isAssignmentModel) {
      e.name = (b = a.event) == null ? void 0 : b.name;
      e.iconCls = (c = a.event) == null ? void 0 : c.iconCls;
    }
    return e;
  }
  fillChildren(a = {}) {
    let b;
    const c = this;
    if ((b = c.origin) == null ? void 0 : b.isResourceModel) {
      const {
          children: e,
          _childrenIndex: f
        } = c, {assigned: g} = c.origin, i = c.constructor, j = new Set(e), k = [];
      for (const l of g) {
        if (!f.has(l)) {
          k.push(i.new({ 'origin': l }, a));
        } else {
          j.delete(f.get(l));
        }
      }
      if (j.size) {
        c.removeChild([...j]);
      }
      if (k.length) {
        c.appendChild(k);
      }
    }
  }
  afterRemoveChild(a) {
    a.forEach(b => this._childrenIndex.delete(b.origin));
  }
  insertChild(...a) {
    let b;
    let c = super.insertChild(...a);
    if (c) {
      const {stores: e} = this;
      if (!Array.isArray(c)) {
        c = [c];
      }
      if ((b = this.origin) == null ? void 0 : b.isResourceModel) {
        for (const f of c) {
          if (f.origin && !this._childrenIndex.has(f.origin)) {
            this._childrenIndex.set(f.origin, f);
          }
        }
      }
      if (e == null ? void 0 : e.length) {
        for (const g of e) {
          for (const i of c) {
            i.traverse(j => j.origin && g.setModelByOrigin(j.origin, j));
          }
        }
      }
    }
    return c;
  }
  getChildByOrigin(a) {
    return this._childrenIndex.get(a);
  }
};
ResourceUtilizationModel.convertEmptyParentToLeaf = true;
ResourceUtilizationModel.exposeProperties();
ResourceUtilizationModel._$name = 'ResourceUtilizationModel';
const ResourceUtilizationStore = class extends AbstractPartOfProjectStoreMixin.derive(AjaxStore) {
  static configurable = {
    'modelClass': ResourceUtilizationModel,
    'project': null,
    'tree': true,
    'resourceValues': {},
    'assignmentValues': {},
    'reapplyFilterOnAdd': true
  };
  static $name = 'ResourceUtilizationStore';
  static get properties() {
    return { '_modelByOrigin': new Map() };
  }
  updateProject(a) {
    this.setResourceStore(a == null ? void 0 : a.resourceStore);
    this.setAssignmentStore(a == null ? void 0 : a.assignmentStore);
    this.setEventStore(a == null ? void 0 : a.eventStore);
    this.fillStoreFromProject();
  }
  setResourceStore(a) {
    this.detachListeners('resourceStore');
    a == null ? void 0 : a.ion({
      'name': 'resourceStore',
      'change': this.onResourceStoreDataChanged,
      'thisObj': this
    });
  }
  setEventStore(a) {
    this.detachListeners('eventStore');
    a == null ? void 0 : a.ion({
      'name': 'eventStore',
      'update': this.onEventUpdate,
      'thisObj': this
    });
  }
  setAssignmentStore(a) {
    this.detachListeners('assignmentStore');
    a == null ? void 0 : a.ion({
      'name': 'assignmentStore',
      'change': this.onAssignmentsChange,
      'refresh': this.onAssignmentsRefresh,
      'add': this.onAssignmentsAdd,
      'update': this.onAssignmentUpdate,
      'remove': this.onAssignmentsRemove,
      'thisObj': this
    });
  }
  onResourceStoreDataChanged(a) {
    if (a.isMove && a.action === 'remove') {
      return;
    }
    this.fillStoreFromProject();
  }
  onAssignmentsChange() {
    const {assignmentValues: a} = this;
    this.forEach(b => b.fillChildren(a));
  }
  onAssignmentsRefresh(a) {
    if (a.action === 'batch') {
      const {assignmentValues: b} = this;
      this.forEach(c => c.fillChildren(b));
    }
  }
  onAssignmentsAdd({records: a}) {
    const {assignmentValues: b} = this;
    a.forEach(c => {
      let e;
      (e = this.getModelByOrigin(c == null ? void 0 : c.resource)) == null ? void 0 : e.fillChildren(b);
    });
  }
  onAssignmentUpdate({
    record: a,
    changes: b
  }) {
    if ('resource' in b) {
      const c = this.getModelByOrigin(a), e = this.getModelByOrigin(a == null ? void 0 : a.resource);
      if (c && e) {
        e.appendChild(c);
      }
    }
  }
  onAssignmentsRemove({records: a}) {
    this.remove(a.map(b => this.getModelByOrigin(b)));
  }
  onEventUpdate({
    record: a,
    changes: b
  }) {
    if ('name' in b) {
      for (const c of a.assigned) {
        const e = this.getModelByOrigin(c);
        e.set('name', a.name);
      }
    }
  }
  fillStoreFromProject() {
    let a;
    const b = this, {
        resourceValues: c,
        assignmentValues: e,
        modelClass: f
      } = b, g = [];
    (a = b._project) == null ? void 0 : a.resourceStore.forEach(i => {
      let j;
      if (!i.isSpecialRow) {
        g.push(f.new({
          'origin': i,
          'expanded': (j = b.getModelByOrigin(i)) == null ? void 0 : j.isExpanded(b),
          'assignmentValues': e
        }, c));
      }
    });
    b.removeAll();
    b.add(g);
    b.trigger('fillFromProject');
  }
  remove() {
    const a = super.remove(...arguments);
    a == null ? void 0 : a.forEach(b => {
      this._modelByOrigin.delete(b.origin);
    });
    return a;
  }
  removeAll() {
    super.removeAll(...arguments);
    this._modelByOrigin.clear();
  }
  getModelByOrigin(a) {
    return this._modelByOrigin.get(a);
  }
  setModelByOrigin(a, b) {
    return this._modelByOrigin.set(a, b);
  }
};
ResourceUtilizationStore._$name = 'ResourceUtilizationStore';
const makeModelUpdateAction2 = (a, b, c) => {
  if (a.isSplitEventMixin) {
    return new EventUpdateAction({
      'model': a,
      'newData': b,
      'oldData': c
    });
  }
  return new UpdateAction({
    'model': a,
    'newData': b,
    'oldData': c
  });
};
const StateTrackingManager2 = class extends StateTrackingManager {
  static get defaultConfig() {
    return { 'makeModelUpdateAction': makeModelUpdateAction2 };
  }
};
StateTrackingManager2._$name = 'StateTrackingManager';
const VersionModel = class extends Localizable_default(Model) {
  static fields = [
    {
      'name': 'name',
      'type': 'string'
    },
    {
      'name': 'isAutosave',
      'type': 'boolean'
    },
    {
      'name': 'content',
      'type': 'object'
    },
    {
      'name': 'savedAt',
      'type': 'date'
    }
  ];
  static get $name() {
    return 'VersionModel';
  }
  onBeforeSave() {
  }
  get description() {
    let a;
    return (a = this.name) != null ? a : this.defaultDescription;
  }
  get defaultDescription() {
    return (this.isAutosave ? 'Auto-saved' : 'Saved') + ' at ' + DateHelper.format(this.savedAt, this.L('L{Versions.versionDateFormat}'));
  }
};
VersionModel.exposeProperties();
VersionModel._$name = 'VersionModel';
const VersionStore = class extends Store.mixin(PartOfProject_default2) {
  static configurable = {
    'modelClass': VersionModel,
    'id': 'versions'
  };
  static $name = 'VersionStore';
};
VersionStore._$name = 'VersionStore';
const ChangeLogTransactionModel = class extends Localizable_default(Model) {
  static fields = [
    { 'name': 'versionId' },
    {
      'name': 'actions',
      'type': 'array'
    },
    {
      'name': 'occurredAt',
      'type': 'date'
    },
    {
      'name': 'description',
      'type': 'string'
    }
  ];
  static $name = 'ChangeLogTransactionModel';
  get description() {
    let a;
    return (a = this.get('description')) != null ? a : this.defaultDescription;
  }
  get actions() {
    let a;
    return (a = this.get('actions')) != null ? a : [];
  }
  get defaultDescription() {
    let a, b;
    if (this.actions.length === 0) {
      return this.L('L{Versions.noChanges}');
    }
    const c = this, e = ArrayHelper.unique(c.actions.map(({actionType: o}) => {
        if (o.startsWith('add')) {
          return 'add';
        }
        if (o.startsWith('remove')) {
          return 'remove';
        }
        if (o.startsWith('update')) {
          return 'update';
        }
        return o;
      })), f = ArrayHelper.unique(c.actions.map(({entity: o}) => o.type)), g = ArrayHelper.unique(c.actions.map(({entity: o}) => o.type + '.' + o.id)).length, i = c.L('L{Versions.transactionDescriptions}'), j = c.L('L{Versions.entityNames}'), k = c.L('L{Versions.entityNamesPlural}'), l = f[0], n = f.length > 1;
    return i[e.length > 1 ? 'mixed' : e[0]].replace('{n}', g).replace('{entities}', g > 1 ? (a = k[n ? 'other' : l]) != null ? a : k.other : (b = j[l]) != null ? b : j.other);
  }
};
ChangeLogTransactionModel.exposeProperties();
ChangeLogTransactionModel._$name = 'ChangeLogTransactionModel';
const ChangeLogStore = class extends Store.mixin(PartOfProject_default2) {
  static configurable = {
    'modelClass': ChangeLogTransactionModel,
    'id': 'changelogs'
  };
  static $name = 'ChangeLogStore';
};
ChangeLogStore._$name = 'ChangeLogStore';
const ProjectCrudManager_default2 = a => class b extends (a || Base).mixin(ProjectCrudManager_default, LazyLoadCrudManager_default) {
  static get configurable() {
    return {
      'crudLoadValidationWarningPrefix': 'Project load response error(s):',
      'crudSyncValidationWarningPrefix': 'Project sync response error(s):',
      'trackProjectModelChanges': false
    };
  }
  construct(...c) {
    const e = this;
    super.construct(...c);
    e.addPrioritizedStore(e.calendarManagerStore);
    e.getStoreDescriptor(e.calendarManagerStore).subStores = ['intervals'];
    e.addPrioritizedStore(e.assignmentStore);
    e.addPrioritizedStore(e.dependencyStore);
    e.addPrioritizedStore(e.resourceStore);
    e.addPrioritizedStore(e.eventStore);
    if (e.timeRangeStore) {
      e.addPrioritizedStore(e.timeRangeStore);
    }
    if (e.resourceTimeRangeStore) {
      e.addPrioritizedStore(e.resourceTimeRangeStore);
    }
  }
  get project() {
    return this;
  }
  set project(c) {
    super.project = c;
  }
  get crudLoadValidationMandatoryStores() {
    return [this.getStoreDescriptor(this.eventStore).storeId];
  }
  async loadCrudManagerData(...c) {
    if (this.delayCalculation && !this.isDelayingCalculation && !this.usingSyncDataOnLoad()) {
      this.scheduleDelayedCalculation();
    }
    await super.loadCrudManagerData(...c);
  }
  acceptChanges() {
    super.acceptChanges();
    this.internalClearChanges(true, false);
  }
  async revertChanges() {
    const c = this, {stm: e} = c, {disabled: f} = e;
    if (!f) {
      e.disable();
    }
    c.isRestoringData = true;
    if (c.replica) {
      c.replica.isWritingPreviousData = true;
    }
    c.revertCrudStoreChanges();
    c.set(c.meta.modified, void 0, true);
    if (c.replica) {
      c.replica.isWritingPreviousData = false;
    }
    c.isRestoringData = false;
    await c.commitAsync();
    if (!f) {
      e.enable();
    }
  }
  crudStoreHasChanges(c) {
    const e = this.getCrudStore(c);
    let f;
    if (e) {
      f = super.crudStoreHasChanges(e);
    } else {
      f = this.trackProjectModelChanges && this.hasPersistableChanges || super.crudStoreHasChanges();
    }
    return f;
  }
  get changes() {
    let c = super.changes;
    if (this.trackProjectModelChanges) {
      const e = this.modificationDataToWrite;
      if (e) {
        c = c || {};
        c.project = e;
      }
    }
    return c;
  }
  shouldClearRecordFieldChange(c, e, f) {
    if (c.isCalendarModel && e === 'intervals') {
      return !c.get('intervals').changes;
    }
    return super.shouldClearRecordFieldChange(...arguments);
  }
};
const sortFn = (c, e) => {
  if (c < e) {
    return -1;
  } else if (c > e) {
    return 1;
  } else {
    return 0;
  }
};
const ProHorizontalLayout_default = a => {
  let b;
  return b = class extends (a || Base) {
    get grouped() {
      return Boolean(this.groupBy);
    }
    getGroupValue(c) {
      let e;
      let f;
      if (c.group != null) {
        f = c.group;
      } else {
        const {
            groupBy: g,
            weights: i,
            groupByThisObj: groupByThisObj = this
          } = this, {eventRecord: j} = c;
        if (typeof g === 'function') {
          f = g.call(groupByThisObj, j);
        } else {
          f = j.getValue(g);
          if (i) {
            f = (e = i[f]) != null ? e : Infinity;
          }
        }
        c.group = f;
      }
      return f;
    }
    getEventGroups(c) {
      if (this.grouped) {
        const e = new Set();
        c.sort((f, g) => {
          const i = this.getGroupValue(f), j = this.getGroupValue(g);
          e.add(i);
          e.add(j);
          return sortFn(i, j);
        });
        return Array.from(e).sort(sortFn);
      } else {
        return [];
      }
    }
  }, __publicField(b, 'configurable', {
    'type': null,
    'weights': null,
    'groupBy': null,
    'groupByThisObj': null,
    'layoutFn': null
  }), b;
};
const ProHorizontalLayoutStack = class extends HorizontalLayoutStack.mixin(ProHorizontalLayout_default) {
  static get $name() {
    return 'ProHorizontalLayoutStack';
  }
  layoutEventsInBands(a, b = false) {
    this.getEventGroups(a);
    return super.layoutEventsInBands(a, b);
  }
};
ProHorizontalLayoutStack._$name = 'ProHorizontalLayoutStack';
const ProHorizontalLayoutPack = class extends HorizontalLayoutPack.mixin(ProHorizontalLayout_default) {
  static get $name() {
    return 'ProHorizontalLayoutPack';
  }
  layoutEventsInBands(a) {
    const b = this.getEventGroups(a), c = b.length || 1;
    const e = this.packEventsInBands(a, (f, g, i, k) => {
      const l = k / c, n = c === 1 ? 0 : b.indexOf(f.group), o = n / c;
      f.height = l;
      f.top = o + i.start / c + g * l;
      f.inBandHeight = k;
      f.inBandTop = i.start + g * k;
    });
    a.forEach(f => {
      Object.assign(f, this.bandIndexToPxConvertFn.call(this.bandIndexToPxConvertThisObj || this, f.top, f.height, f.eventRecord, f.resourceRecord));
    });
    return e;
  }
};
ProHorizontalLayoutPack._$name = 'ProHorizontalLayoutPack';
let counter2 = 0;
const CalendarHighlightModel = class extends ResourceTimeRangeModel {
  static domIdPrefix = 'calendarhighlight';
  static $name = 'CalendarHighlightModel';
  static generateId() {
    return ++counter2;
  }
};
const CalendarHighlight = class extends ResourceTimeRangesBase {
  static configurable = {
    'calendar': 'event',
    'collectAvailableResources': null,
    'inflate': 3,
    'rangeCls': 'b-sch-highlighted-calendar-range',
    'resourceTimeRangeModelClass': CalendarHighlightModel
  };
  static $name = 'CalendarHighlight';
  static get pluginConfig() {
    const a = super.pluginConfig;
    a.assign = [
      'highlightEventCalendars',
      'highlightResourceCalendars',
      'unhighlightCalendars',
      'highlightResourceCalendarsForEventRecords'
    ];
    return a;
  }
  afterConstruct() {
    super.afterConstruct();
    this.client.ion({
      'eventDragStart': 'onEventDragStart',
      'eventDragReset': 'unhighlightCalendars',
      'eventResizeStart': 'onEventResizeStart',
      'eventResizeEnd': 'unhighlightCalendars',
      'dragCreateStart': 'onDragCreateStart',
      'afterDragCreate': 'unhighlightCalendars',
      'thisObj': this
    });
  }
  highlightCalendar(a, b, c) {
    a = ArrayHelper.asArray(a);
    b = ArrayHelper.asArray(b);
    if (this.calendar === 'event') {
      this.highlightEventCalendars(a, b, c);
    } else {
      this.highlightResourceCalendars(b, c);
    }
  }
  highlightEventCalendars(a, b, c = true) {
    let e;
    const f = this, {client: g} = f, {
        startDate: i,
        endDate: j
      } = g;
    if (f.disabled) {
      return;
    }
    if (c) {
      f.unhighlightCalendars();
    }
    a = ArrayHelper.asArray(a);
    if (!b) {
      b = a.flatMap(k => k.$linkedResources);
    }
    f.highlight = new Map();
    b = ArrayHelper.asArray(b);
    a.forEach(k => {
      let l;
      if (!k.calendar) {
        return;
      }
      const n = (l = k.calendar) == null ? void 0 : l.getWorkingTimeRanges(i, j).map(o => new CalendarHighlightModel(o));
      if (n) {
        for (const o of b) {
          f.highlight.set(o, n);
          g.currentOrientation.refreshEventsForResource(o, true, false);
        }
        if (b.length > 0) {
          g.currentOrientation.onRenderDone();
        }
      }
    });
    (e = g.syncSplits) == null ? void 0 : e.call(g, k => k.highlightEventCalendars(a, b, c));
  }
  highlightResourceCalendarsForEventRecords(a, b = true) {
    let c, e;
    if (a.length > 0) {
      const {client: f} = this, g = (e = (c = this.collectAvailableResources) == null ? void 0 : c.call(this, {
          'scheduler': f,
          'eventRecords': a
        })) != null ? e : f.resourceStore.records;
      this.highlightCalendar(a, g);
    } else if (b) {
      this.unhighlightCalendars();
    }
  }
  highlightResourceCalendars(a, b = true) {
    let c, e, f;
    const g = this, {
        startDate: i,
        endDate: j,
        currentOrientation: k
      } = g.client;
    if (g.disabled) {
      return;
    }
    if (b) {
      g.unhighlightCalendars();
    }
    g.highlight = new Map();
    for (const l of a) {
      const n = (c = l.calendar) == null ? void 0 : c.getWorkingTimeRanges(i, j).map(o => new CalendarHighlightModel(o));
      if (n) {
        g.highlight.set(l, n);
        k.refreshEventsForResource(l, true, false);
      }
    }
    if (a.length > 0) {
      k.onRenderDone();
    }
    (f = (e = g.client).syncSplits) == null ? void 0 : f.call(e, o => o.highlightResourceCalendars(a, b));
  }
  unhighlightCalendars() {
    let a, b;
    const c = this;
    if (!c.highlight) {
      return;
    }
    const {currentOrientation: e} = c.client, f = c.highlight.keys();
    c.highlight = null;
    for (const g of f) {
      e.refreshEventsForResource(g, true, false);
    }
    e.onRenderDone();
    (b = (a = c.client).syncSplits) == null ? void 0 : b.call(a, i => i.unhighlightCalendars());
  }
  onEventDragStart({context: a}) {
    let b, c;
    if (this.disabled) {
      return;
    }
    const e = this, {client: f} = e, {eventRecords: g} = a, i = a.availableResources = f.features.eventDrag.constrainDragToResource ? [a.resourceRecord] : (c = (b = e.collectAvailableResources) == null ? void 0 : b.call(e, {
        'scheduler': f,
        'eventRecords': g
      })) != null ? c : f.resourceStore.records;
    e.highlightCalendar(g, i);
  }
  onEventResizeStart({
    eventRecord: a,
    resourceRecord: b
  }) {
    if (!this.disabled) {
      this.highlightCalendar(a, [b]);
    }
  }
  onDragCreateStart({
    eventRecord: a,
    resourceRecord: b
  }) {
    if (!this.disabled) {
      this.highlightCalendar(a, [b]);
    }
  }
  getEventsToRender(a, b) {
    let c;
    const e = (c = this.highlight) == null ? void 0 : c.get(a);
    e && b.push(...e);
    return b;
  }
  onEventDataGenerated(a) {
    let b, c;
    const {eventRecord: e} = a;
    if (e.isCalendarHighlightModel) {
      const {inflate: f} = this, g = (b = f.x) != null ? b : f, i = (c = f.y) != null ? c : f;
      a.fillSize = this.client.isVertical;
      a.wrapperCls['b-sch-highlighted-calendar-range'] = 1;
      a.children.push({
        'className': 'b-sch-event-content',
        'html': e.name,
        'dataset': { 'taskBarFeature': 'content' }
      });
      a.width += g * 2;
      a.height += i * 2;
      a.left -= g;
      a.top -= i;
      a.eventId = this.generateElementId(e) + '-resource-' + a.resourceRecord.id;
    }
  }
  updateDisabled(a, b) {
    super.updateDisabled(a, b);
    if (a) {
      this.unhighlightCalendars();
    }
  }
  updateInflate() {
    if (!this.isConfiguring) {
      this.client.refresh();
    }
  }
  shouldInclude(a) {
    return a.isCalendarHighlightModel;
  }
  get featureClass() {
  }
};
CalendarHighlight._$name = 'CalendarHighlight';
GridFeatureManager.registerFeature(CalendarHighlight, false, 'SchedulerPro');
const CellEdit2 = class extends TransactionalFeature_default(CellEdit) {
  static get $name() {
    return 'CellEdit';
  }
  async startEditing(a = {}) {
    const b = this;
    let c;
    if (b.client.isTransactionalFeatureActive) {
      c = false;
    } else {
      await b.startFeatureTransaction();
      c = await super.startEditing(a);
      if (!c) {
        await b.rejectFeatureTransaction();
      }
    }
    return c;
  }
  async finishEditing() {
    let a;
    const b = await super.finishEditing();
    if (b) {
      await ((a = this.finishFeatureTransaction) == null ? void 0 : a.call(this));
    }
    return b;
  }
  cancelEditing(a = false, b) {
    this.rejectFeatureTransaction();
    super.cancelEditing(a, b);
  }
};
CellEdit2._$name = 'CellEdit';
GridFeatureManager.registerFeature(CellEdit2, true, 'SchedulerPro');
const Dependencies2 = class extends Dependencies.mixin(TransactionalFeature_default) {
  static $name = 'Dependencies';
  onRequestDragCreate(a) {
    const b = super.onRequestDragCreate(a);
    if (b !== false) {
      this.startFeatureTransaction().then();
    }
    return b;
  }
  async doAfterDependencyDrop(a) {
    const {dependency: b} = a;
    if (b) {
      await this.finishFeatureTransaction();
    } else {
      this.rejectFeatureTransaction();
    }
    await super.doAfterDependencyDrop(a);
  }
  abort() {
    this.rejectFeatureTransaction();
    super.abort();
  }
};
Dependencies2._$name = 'Dependencies';
GridFeatureManager.registerFeature(Dependencies2, true, 'SchedulerPro');
const DependencyEdit2 = class extends TransactionalFeature_default(DependencyEdit) {
  static get $name() {
    return 'DependencyEdit';
  }
  static get configurable() {
    return {
      'showLagField': true,
      'editorConfig': {
        'items': {
          'activeField': {
            'type': 'checkbox',
            'name': 'active',
            'label': 'L{Active}'
          }
        }
      }
    };
  }
  async editDependency(a) {
    await this.startFeatureTransaction();
    const b = await super.editDependency(a);
    if (!b) {
      await this.rejectFeatureTransaction();
    }
    return b;
  }
  async save() {
    const a = await super.save();
    if (a) {
      await this.finishFeatureTransaction();
    }
    return a;
  }
  async deleteDependency() {
    const a = await super.deleteDependency();
    if (a) {
      await this.finishFeatureTransaction();
    }
    return a;
  }
  afterCancel() {
    this.rejectFeatureTransaction();
    super.afterCancel();
  }
};
DependencyEdit2._$name = 'DependencyEdit';
GridFeatureManager.registerFeature(DependencyEdit2, false);
const EventBuffer = class extends InstancePlugin {
  static configurable = {
    'showDuration': true,
    'renderer': null,
    'tooltipTemplate': {
      'value': null,
      '$config': 'nullify'
    }
  };
  static $name = 'EventBuffer';
  static get pluginConfig() {
    return { 'chain': ['onEventDataGenerated'] };
  }
  updateTooltipTemplate(a) {
    let b;
    const c = this;
    if (a) {
      c.tooltip = Tooltip.new({
        'forElement': c.client.timeAxisSubGridElement,
        'forSelector': '.b-sch-event-buffer-before,.b-sch-event-buffer-after',
        'align': {
          'align': 'b-t',
          'offset': [
            0,
            10
          ]
        },
        'getHtml'({activeTarget: e}) {
          const f = c.client.resolveEventRecord(e), g = e.matches('.b-sch-event-buffer-before'), i = g ? f.preamble : f.postamble;
          return c.tooltipTemplate({
            'eventRecord': f,
            'duration': i,
            'before': g
          });
        }
      });
    } else {
      (b = c.tooltip) == null ? void 0 : b.destroy();
    }
  }
  onEventDataGenerated({
    useEventBuffer: a,
    bufferBeforeWidth: b,
    bufferAfterWidth: c,
    eventRecord: e,
    wrapperChildren: f
  }) {
    let g, i, j;
    if (this.enabled && a) {
      const {isHorizontal: k} = this.client, {showDuration: l} = this, n = k ? 'width' : 'height', o = {}, p = {};
      let {
        preamble: q,
        postamble: r
      } = e;
      (g = this.renderer) == null ? void 0 : g.call(this, {
        'eventRecord': e,
        'preambleConfig': o,
        'postambleConfig': p
      });
      if (!(q == null ? void 0 : q.magnitude)) {
        q = null;
      }
      if (!(r == null ? void 0 : r.magnitude)) {
        r = null;
      }
      f.push({
        'className': {
          'b-sch-event-buffer': 1,
          'b-sch-event-buffer-before': 1,
          'b-buffer-thin': !b,
          [o.cls]: o.cls
        },
        'style': { ['' + n]: b + 'px' },
        'children': l && q ? [
          o.icon ? {
            'tag': 'i',
            'className': o.icon
          } : void 0,
          {
            'tag': 'span',
            'className': 'b-buffer-label',
            'text': (i = o.text) != null ? i : q.toString(true)
          }
        ] : void 0
      }, {
        'className': {
          'b-sch-event-buffer': 1,
          'b-sch-event-buffer-after': 1,
          'b-buffer-thin': !c,
          [p.cls]: p.cls
        },
        'style': { ['' + n]: c + 'px' },
        'children': l && r ? [
          {
            'tag': 'span',
            'className': 'b-buffer-label',
            'text': (j = p.text) != null ? j : r.toString(true)
          },
          p.icon ? {
            'tag': 'i',
            'className': p.icon
          } : void 0
        ] : void 0
      });
    }
  }
  updateShowDuration() {
    if (!this.isConfiguring) {
      this.client.refreshWithTransition();
    }
  }
  doDisable(a) {
    super.doDisable(a);
    const {client: b} = this;
    if (!b.isConfiguring && b.isPainted) {
      b.element.classList.add('b-eventbuffer-transition');
      b.refreshWithTransition();
      b.waitForAnimations().then(() => {
        b.element.classList.remove('b-eventbuffer-transition');
      });
    }
  }
};
EventBuffer._$name = 'EventBuffer';
GridFeatureManager.registerFeature(EventBuffer, false, 'SchedulerPro');
const EventResize2 = class extends EventResize {
  static $name = 'EventResize';
  render() {
    super.render(...arguments);
    this.dragSelector = this.dragItemSelector = this.client.eventSelector + ':not(.b-sch-event-segment)';
  }
};
EventResize2._$name = 'EventResize';
GridFeatureManager.registerFeature(EventResize2, true, 'SchedulerPro');
const EventSegmentDrag = class extends EventDrag {
  static configurable = {
    'constrainDragToResource': true,
    'capitalizedEventName': 'EventSegment'
  };
  static $name = 'EventSegmentDrag';
  static get pluginConfig() {
    return {
      'chain': [
        'onInternalPaint',
        'isEventElementDraggable'
      ]
    };
  }
  getTriggerParams(a) {
    const {
      assignmentRecords: b,
      eventRecords: c,
      resourceRecord: e,
      browserEvent: f
    } = a;
    return {
      'context': a,
      'eventRecords': c,
      'resourceRecord': e,
      'assignmentRecords': b,
      'event': f
    };
  }
  triggerEventDrag(a, b) {
    this.scheduler.trigger('eventSegmentDrag', Object.assign(this.getTriggerParams(a), {
      'startDate': a.startDate,
      'endDate': a.endDate
    }));
  }
  triggerBeforeEventDrag(a) {
    this.scheduler.trigger('beforeEventSegmentDrag', this.getTriggerParams(a));
  }
  triggerDragStart(a) {
    this.scheduler.navigator.skipNextClick = true;
    this.scheduler.trigger('eventSegmentDragStart', this.getTriggerParams(a));
  }
  triggerDragAbort(a) {
    this.client.currentOrientation.refreshEventsForResource(a.resourceRecord);
    this.scheduler.trigger('eventSegmentDragAbort', this.getTriggerParams(a));
  }
  triggerDragAbortFinalized(a) {
    this.scheduler.trigger('eventSegmentDragAbortFinalized', this.getTriggerParams(a));
  }
  triggerAfterDrop(a, b) {
    this.scheduler.trigger('afterEventSegmentDrop', Object.assign(this.getTriggerParams(a), { 'valid': b }));
    if (!b) {
      const {
          assignmentStore: c,
          eventStore: e
        } = this.client, f = this.dragData.initialAssignmentsState.find(({
          resource: g,
          assignment: j
        }, k) => {
          let l;
          return !c.includes(j) || !e.includes(j.event) || g.id !== ((l = this.dragData.assignmentRecords[k]) == null ? void 0 : l.resourceId);
        });
      if (f) {
        this.client.refresh();
      }
    }
  }
  async updateRecords(a) {
    const b = this, {client: c} = b, e = false;
    let f;
    if (!a.externalDropTarget) {
      c.eventStore.suspendAutoCommit();
      f = await b.updateSegment(c, a, e);
      c.eventStore.resumeAutoCommit();
    }
    c.trigger('eventSegmentDrop', Object.assign(b.getTriggerParams(a), {
      'isCopy': e,
      'event': a.browserEvent,
      'targetEventRecord': a.targetEventRecord,
      'targetResourceRecord': a.newResource,
      'externalDropTarget': a.externalDropTarget
    }));
    return f;
  }
  async updateSegment(a, b) {
    let c;
    const e = this, f = a.mode === 'vertical', {
        eventRecords: g,
        assignmentRecords: i,
        timeDiff: j
      } = b;
    a.suspendRefresh();
    let k = false;
    if (f) {
      g.forEach((p, q) => {
        const r = b.eventBarEls[q];
        delete p.instanceMeta(a).hasTemporaryDragElement;
        if (r.dataset.transient) {
          r.remove();
        }
      });
    }
    const l = b.eventBarEls.slice(), n = b.eventRecord, o = e.adjustStartDate(b.origStart, j);
    if (!DateHelper.isEqual(n.startDate, o)) {
      a.endListeningForBatchedUpdates();
      e.cancelBatchUpdate(n);
      n.startDate = o;
      k = true;
      await a.project.commitAsync();
      (c = e.endBatchUpdate) == null ? void 0 : c.call(e, n);
    }
    a.resumeRefresh(false);
    if (i.length > 0) {
      if (!k) {
        b.valid = false;
      } else {
        if (a.fillTicks) {
          l.forEach(p => delete p.lastDomConfig);
        }
        a.refreshWithTransition();
      }
    }
  }
  isEventElementDraggable(a, b, c) {
    if (this.disabled || !(b.isEventSegment || b.segments)) {
      return true;
    }
    return !c.closest(this.drag.targetSelector);
  }
  buildDragHelperConfig() {
    const a = super.buildDragHelperConfig();
    a.targetSelector = '.b-sch-event-segment:not(.b-first)';
    return a;
  }
  getMinimalDragData(a) {
    const b = this, {client: c} = b, e = b.getElementFromContext(a), f = c.resolveEventRecord(e), g = c.resolveResourceRecord(e), i = c.resolveAssignmentRecord(e), j = i ? [i] : [], k = [f];
    return {
      'eventRecord': f,
      'resourceRecord': g,
      'assignmentRecord': i,
      'eventRecords': k,
      'assignmentRecords': j
    };
  }
  beginBatchUpdate(a) {
    a.event.beginBatch();
    a.beginBatch();
  }
  endBatchUpdate(a) {
    let b;
    (b = a.event) == null ? void 0 : b.endBatch();
    a.endBatch();
  }
  cancelBatchUpdate(a) {
    let b;
    (b = a.event) == null ? void 0 : b.cancelBatch();
    a.cancelBatch();
  }
  setupProductDragData(a) {
    let b;
    const c = this, {client: e} = c, f = c.getElementFromContext(a), {
        eventRecord: g,
        resourceRecord: i
      } = c.getMinimalDragData(a), j = [], k = e.getElementsFromEventRecord(g.event, i, true)[0];
    if (c.constrainDragToResource && !i) {
      throw new Error('Resource could not be resolved for event: ' + g.id);
    }
    if (!e.listenToBatchedUpdates) {
      e.beginListeningForBatchedUpdates();
    }
    c.beginBatchUpdate(g);
    const l = (b = c.getDateConstraints) == null ? void 0 : b.call(c, i, g), n = c.constrainRectangle = c.getConstrainingRectangle(l, i, g), o = Rectangle.from(f, e.foregroundCanvas, true), p = Rectangle.from(k, e.foregroundCanvas, true);
    n.translate(-p.x);
    super.setupConstraints(n, o, e.timeAxisViewModel.snapPixelAmount, Boolean(l.start));
    j.push(f);
    return {
      'record': g,
      'draggedEntities': [g],
      'dateConstraints': (l == null ? void 0 : l.start) ? l : null,
      'eventBarEls': j,
      'mainEventElement': k
    };
  }
  suspendRecordElementRedrawing() {
  }
  suspendElementRedrawing() {
  }
  getDateConstraints(a, b) {
    let {
      minDate: c,
      maxDate: e
    } = super.getDateConstraints(a, b);
    if (b.previousSegment && (!c || c < b.previousSegment.endDate)) {
      c = b.previousSegment.endDate;
    }
    if (b.nextSegment && (!e || e < b.nextSegment.startDate)) {
      e = b.nextSegment.startDate;
    }
    return {
      'start': c,
      'end': e
    };
  }
  get tipId() {
    return this.client.id + '-segment-drag-tip';
  }
  updateDragContext(a, b) {
    let c;
    super.updateDragContext(...arguments);
    const {client: e} = this, {
        dirty: f,
        eventRecord: g,
        endDate: i
      } = this.dragData;
    if (f && !g.nextSegment) {
      const {enableEventAnimations: j} = e;
      e.enableEventAnimations = false;
      g.event.set('endDate', i);
      if ((c = e.features.eventBuffer) == null ? void 0 : c.enabled) {
        g.event.wrapEndDate = i;
      }
      e.enableEventAnimations = j;
    }
  }
};
EventSegmentDrag._$name = 'EventSegmentDrag';
GridFeatureManager.registerFeature(EventSegmentDrag, true, 'SchedulerPro');
GridFeatureManager.registerFeature(EventSegmentDrag, false, 'ResourceHistogram');
const EventSegmentResize = class extends EventResize2 {
  static $name = 'EventSegmentResize';
  static get pluginConfig() {
    return {
      'chain': [
        ...super.pluginConfig.chain,
        'isEventSegmentElementDraggable'
      ]
    };
  }
  render() {
    super.render(...arguments);
    this.dragSelector = this.dragItemSelector = '.b-sch-event-segment';
  }
  isEventSegmentElementDraggable(a, b, c, e) {
    return this.isEventElementDraggable(...arguments);
  }
  isEventElementDraggable(a, b, c, e) {
    const f = this, g = b == null ? void 0 : b.resizable;
    if (f.disabled || !g || b.isMilestone) {
      return true;
    }
    if (b.segments) {
      const i = f.client.scheduledEventName;
      return b.segments.every((j, k) => {
        const l = j.resizable, n = DomSync.getChild(a, i + '.segments.' + k);
        return !l || (l !== true && l !== 'start' || !f.isOverStartHandle(e, n)) && (l !== true && l !== 'end' || !f.isOverEndHandle(e, n));
      });
    }
    return super.isEventElementDraggable(...arguments);
  }
  get tipId() {
    return this.client.id + '-event-segment-resize-tip';
  }
  dragStart(a) {
    this._segmentsSlices = null;
    this._segmentsSlicesIndex = 0;
    return super.dragStart(...arguments);
  }
  beginEventRecordBatch(a) {
    super.beginEventRecordBatch(a);
    if (a.isEventSegment) {
      a.event.beginBatch();
    }
  }
  triggerBeforeResize(a) {
    const {client: b} = this, c = b.scheduledEventName, e = b.resolveTimeSpanRecord(a.itemElement);
    return b.trigger('before' + b.capitalizedEventName + 'SegmentResize', {
      [c + 'Record']: e,
      'event': a.event,
      ...this.getBeforeResizeParams({
        'event': a.startEvent,
        'element': a.itemElement
      })
    });
  }
  triggerEventResizeStart(a, b) {
    const {client: c} = this;
    c.trigger(c.scheduledEventName + 'SegmentResizeStart', b);
  }
  triggerEventResizeEnd(a, b) {
    const {client: c} = this;
    c.trigger(c.scheduledEventName + 'SegmentResizeEnd', b);
  }
  triggerEventPartialResize(a, b) {
    const {client: c} = this;
    c.trigger(c.scheduledEventName + 'SegmentPartialResize', b);
  }
  triggerBeforeEventResizeFinalize(a, b) {
    const {client: c} = this;
    c.trigger('before' + c.capitalizedEventName + 'SegmentResizeFinalize', b);
  }
  applyDateConstraints(a, b, c) {
    let e, f;
    let g = (e = c.dateConstraints) == null ? void 0 : e.start, i = (f = c.dateConstraints) == null ? void 0 : f.end;
    if (b.isEventSegment) {
      const {
        previousSegment: j,
        nextSegment: k
      } = b;
      if (j) {
        g = g ? DateHelper.max(j.endDate, g) : j.endDate;
      }
      if (k) {
        i = i ? DateHelper.min(k.startDate, i) : k.startDate;
      }
    }
    if (g || i) {
      a = DateHelper.constrain(a, g, i);
      c.snappedDate = DateHelper.constrain(c.snappedDate, g, i);
    }
    return a;
  }
  resizeEventPartiallyInternal(a, b) {
    const {toSet: c} = b;
    super.resizeEventPartiallyInternal(...arguments);
    if (a.isEventSegment) {
      const {event: e} = a;
      if (!a.nextSegment && c === 'endDate') {
        e.set('endDate', b[c]);
      } else if (!a.previousSegment && c === 'startDate') {
        e.set('startDate', b[c]);
      } else {
        const f = e.fieldMap.segments;
        f._skipSegmentsIsEqual++;
        e.set('segments', e.get('segments'));
        f._skipSegmentsIsEqual--;
      }
    }
  }
  cancelEventRecordBatch(a) {
    super.cancelEventRecordBatch(a);
    if (a.isEventSegment) {
      a.event.cancelBatch();
    }
  }
  highlightHandle() {
    const {overItem: a} = this;
    a.classList.add('b-resize-handle', 'b-over-resize-handle');
  }
  unHighlightHandle(a = this.overItem) {
    a == null ? void 0 : a.classList.remove('b-resize-handle', this.resizingItemInnerCls, 'b-over-resize-handle', this.draggingItemCls);
  }
};
EventSegmentResize._$name = 'EventSegmentResize';
GridFeatureManager.registerFeature(EventSegmentResize, true, 'SchedulerPro');
GridFeatureManager.registerFeature(EventSegmentResize, false, 'ResourceHistogram');
const EventSegments = class extends InstancePlugin {
  static configurable = {
    'splitDuration': 0,
    'maxSplitDuration': {
      'magnitude': 1,
      'unit': 'day'
    },
    'minSplitDuration': 0
  };
  static $name = 'EventSegments';
  static get pluginConfig() {
    return {
      'override': [
        'getElementsFromEventRecord',
        'resolveEventRecord',
        'resolveTaskRecord',
        'onElementMouseOut'
      ],
      'chain': [
        'populateTaskMenu',
        'populateEventMenu',
        'onTaskDataGenerated',
        'onEventDataGenerated'
      ]
    };
  }
  onElementMouseOut(a) {
    const b = this, {client: c} = b, {
        target: e,
        relatedTarget: f
      } = a, g = e.closest(c.eventSelector), i = c.resolveTimeSpanRecord(e);
    if ((i == null ? void 0 : i.isEventSegment) && g && c.hoveredEvents.has(g)) {
      if (f && DomHelper.isDescendant(g, f)) {
        return;
      }
    }
    this.overridden.onElementMouseOut(...arguments);
  }
  getElementsFromEventRecord(a, b) {
    if (a == null ? void 0 : a.isEventSegment) {
      const c = this.overridden.getElementsFromEventRecord(a.event, b)[0];
      return [DomSync.getChild(c, 'segments.' + a.segmentIndex)];
    }
    return this.overridden.getElementsFromEventRecord(...arguments);
  }
  resolveEventRecord(a) {
    const b = a instanceof Event ? a.target : a, c = b == null ? void 0 : b.closest('.b-sch-event-segment');
    let e = this.overridden.resolveEventRecord(a);
    if ((e == null ? void 0 : e.segments) && c) {
      e = e.segments[c.dataset.segment];
    }
    return e;
  }
  resolveTaskRecord(a) {
    const b = a == null ? void 0 : a.closest('.b-sch-event-segment');
    let c = this.overridden.resolveTaskRecord(a);
    if ((c == null ? void 0 : c.segments) && b) {
      c = c.segments[b.dataset.segment];
    }
    return c;
  }
  populateTaskMenu(a) {
    let b;
    a.eventRecord = a.taskRecord;
    if ((b = a.domEvent) == null ? void 0 : b.target.closest(a.feature.client.eventInnerSelector)) {
      this.populateEventMenu(a);
    }
  }
  populateEventMenu({
    eventRecord: a,
    taskRecord: b,
    items: c,
    domEvent: e
  }) {
    const f = this;
    if (!f.client.readOnly && !a.isParent && !a.milestone && !f.disabled) {
      c['split' + (b ? 'Task' : 'Event')] = {
        'localeClass': f,
        'text': 'L{split' + (b ? 'Task' : 'Event') + '}',
        'icon': 'b-icon b-icon-cut',
        'disabled': a.readOnly,
        'weight': 650,
        'separator': true,
        'onItem'(i) {
          f.splitEvent(i);
        }
      };
      const g = e.target.closest('.b-sch-event-segment');
      if (g) {
        const i = f.client.resolveEventRecord(g);
        c.renameSegment = {
          'localeClass': f,
          'text': 'L{renameSegment}',
          'icon': 'b-icon b-icon-rename',
          'disabled': a.readOnly || i.readOnly,
          'weight': 660,
          'onItem'() {
            f.rename(i, g);
          }
        };
      }
    }
  }
  getSplitDate(a) {
    const {
      eventRecord: b,
      date: c,
      timeAxis: e
    } = a;
    return e.roundDate(c, b.startDate);
  }
  getSplitDuration(a) {
    const {
      splitDuration: b,
      minSplitDuration: c,
      maxSplitDuration: e
    } = this;
    if (b == null ? void 0 : b.magnitude) {
      return b.magnitude;
    }
    const {
      eventRecord: f,
      tick: g
    } = a;
    if (g) {
      const i = this.getSplitDurationUnit(a), {project: j} = f;
      let k = g.endDate - g.startDate;
      if (e) {
        const l = j.run('$convertDuration', e.magnitude, e.unit || i, 'millisecond');
        k = Math.min(k, l);
      }
      if (c) {
        const n = j.run('$convertDuration', c.magnitude, c.unit || i, 'millisecond');
        k = Math.max(k, n);
      }
      return j.run('$convertDuration', k, 'millisecond', i);
    }
  }
  getSplitDurationUnit(a) {
    const {splitDuration: b} = this;
    return (b == null ? void 0 : b.unit) || a.eventRecord.durationUnit;
  }
  splitEvent(a) {
    const {client: b} = a.feature, {timeAxis: c} = b;
    a.date = b.getDateFromXY(a.point, void 0, false);
    a.tick = c.getSnappedTickFromDate(a.date);
    a.timeAxis = c;
    a.eventRecord.splitToSegments(this.getSplitDate(a), this.getSplitDuration(a), this.getSplitDurationUnit(a));
  }
  rename(a, b) {
    const {client: c} = this;
    const e = new Editor({
      'owner': c,
      'appendTo': c.timeAxisSubGridElement,
      'scrollAction': 'realign',
      'align': { 'align': 'c-c' },
      'cls': 'b-event-segment-renamer',
      'internalListeners': {
        'complete'() {
          c.refresh();
        },
        'thisObj': this
      }
    });
    e.startEdit({
      'target': b,
      'record': a,
      'field': 'name'
    });
  }
  doDisable(a) {
    if (this.client.isPainted) {
      this.client.refresh();
    }
    super.doDisable(a);
  }
  generateSegmentRenderData(a, b) {
    let c;
    const {client: e} = this;
    let f, g;
    if (!a.isModel) {
      a = b.eventRecord.processSegmentValue(a);
    }
    if (e.isGantt) {
      const i = e.currentOrientation, j = i.getTaskBox(a), k = {
          'taskRecord': a,
          'task': a,
          'row': b.row,
          'children': []
        };
      if (j) {
        Object.assign(k, {
          'isTask': true,
          'top': j.top,
          'left': j.left,
          'width': j.width,
          'height': j.height
        });
      }
      i.internalPopulateTaskRenderData(k, a);
      g = k;
    } else {
      g = e.generateRenderData(a, b.resourceRecord, true);
    }
    if (g) {
      const l = g.eventColor || b.eventColor, n = DomHelper.isNamedColor(l);
      f = {
        'segmentRecord': a,
        'eventContent': g.eventContent || g.taskContent,
        'cls': g.cls || ((c = a.cls) == null ? void 0 : c.clone()) || new DomClassList(),
        'top': b.top,
        'left': g.left - b.left,
        'width': g.width,
        'height': b.height,
        'style': ''
      };
      if (l && !n) {
        f.style = { 'background-color': l };
        if (b.eventColor && b._customColorStyle) {
          b.style = b.style.replace(b._customColorStyle, '');
        }
      }
      Object.assign(f.cls, {
        ['b-sch-color-' + l]: n,
        'b-sch-color-none': !l,
        'b-sch-event-segment': true,
        'b-first': !a.previousSegment,
        'b-last': !a.nextSegment
      }, g.cls);
      if (DomHelper.isNamedColor(l)) {
        f.cls['b-sch-color-' + l] = l;
      } else if (l) {
        f.style = 'background-color:' + l + ';' + f.style;
        f.cls['b-sch-custom-color'] = 1;
      } else {
        b.wrapperCls['b-sch-color-none'] = 1;
      }
    }
    return f;
  }
  appendDOMConfig(a) {
    const b = a.eventRecord || a.taskRecord;
    if (b.segments && !this.disabled) {
      const c = a.eventContent || a.taskContent, e = a.children.indexOf(c);
      if (e > -1) {
        a.children.splice(e, 1);
      }
      delete a.eventContent;
      delete a.taskContent;
      a.cls['b-segmented'] = true;
      a.segments = b.segments.map(f => this.generateSegmentRenderData(f, a));
      a.segmentsDOMConfig = a.segments.map(this.getSegmentDOMConfig.bind(this));
      a.children.unshift({
        'syncOptions': {
          'syncIdField': 'segment',
          'releaseThreshold': 0
        },
        'className': 'b-sch-event-segments',
        'dataset': { 'taskBarFeature': 'segments' },
        'children': a.segmentsDOMConfig
      });
    }
  }
  getSegmentDOMConfig(a, b) {
    return {
      'className': a.cls,
      'style': {
        'style': a.style,
        'insetInlineStart': a.left,
        'height': a.height,
        'width': a.width,
        ...a.style
      },
      'dataset': { 'segment': b },
      'syncOptions': { 'syncIdField': 'taskBarFeature' },
      'children': [a.eventContent]
    };
  }
  onEventDataGenerated(a) {
    this.appendDOMConfig(a);
  }
  onTaskDataGenerated(a) {
    this.appendDOMConfig(a);
  }
  get featureClass() {
  }
};
EventSegments._$name = 'EventSegments';
GridFeatureManager.registerFeature(EventSegments, true, 'SchedulerPro');
GridFeatureManager.registerFeature(EventSegments, true, 'Gantt');
const borderWidths = {
  'border': 1,
  'hollow': 2
};
const NestedEvents = class extends InstancePlugin.mixin(AttachToProjectMixin_default, Delayable_default) {
  static delayable = { 'refreshClient': 'raf' };
  static configurable = {
    'eventLayout': 'pack',
    'barMargin': 5,
    'resourceMargin': 0,
    'eventHeight': 30,
    'headerHeight': 20,
    'constrainDragToParent': false,
    'allowNestingOnDrop': true,
    'allowDeNestingOnDrop': true,
    'constrainResizeToParent': true,
    'maxNesting': 1
  };
  static $name = 'NestedEvents';
  static get pluginConfig() {
    return {
      'before': ['onEventStoreBatchedUpdate'],
      'chain': [
        'getEventsToRender',
        'processEventDrop',
        'processCrossSchedulerEventDrop',
        'beforeEventDragStart',
        'afterEventDragStart',
        'afterEventDragAbortFinalized',
        'checkEventDragValidity',
        'afterEventResizeStart',
        'afterRenderEvent'
      ],
      'override': [
        'getResourceMarginObject',
        'getBarMargin',
        'getAppliedResourceHeight',
        'getResourceWidth',
        'getEventLayout',
        'getElementFromAssignmentRecord',
        'scheduleEvent'
      ]
    };
  }
  construct(a, b) {
    super.construct(a, b);
    this.client.eventStore.tree = true;
  }
  refreshClient() {
    this.client.refreshWithTransition();
  }
  possiblyRefreshClient() {
    !this.client.isConfiguring && this.refreshClient();
  }
  doDisable() {
    this.possiblyRefreshClient();
  }
  updateEventLayout(a) {
    if (a === 'stack' && this.client.isVertical) {
      console.warn('Stacked nested events are not supported in vertical mode');
    }
    this.possiblyRefreshClient();
  }
  updateBarMargin() {
    this.possiblyRefreshClient();
  }
  updateResourceMargin() {
    this.possiblyRefreshClient();
  }
  changeEventHeight(a) {
    this._eventHeights = ArrayHelper.asArray(a);
    return a;
  }
  updateEventHeight() {
    this.possiblyRefreshClient();
  }
  updateHeaderHeight(a) {
    this.client.element.style.setProperty('--schedulerpro-nested-event-header-height', a + 'px');
    this.possiblyRefreshClient();
  }
  getEventLayout(a, b) {
    if (b) {
      return { 'type': this.eventLayout };
    }
    return this.overridden.getEventLayout(a);
  }
  getResourceMarginObject(a, b) {
    if (b && !b.isRoot) {
      const c = this.resourceMargin;
      if (typeof c === 'number') {
        return {
          'start': c,
          'end': c,
          'total': c * 2
        };
      } else {
        c.total = c.start + c.end;
        return c;
      }
    }
    return this.overridden.getResourceMarginObject(a);
  }
  getBarMargin(a, b) {
    if (b && !b.isRoot) {
      return this.barMargin;
    }
    return this.overridden.getBarMargin(a);
  }
  getAppliedResourceHeight(a, b) {
    let c;
    const e = this;
    if (b && !b.isRoot) {
      if (e.eventLayout === 'stack') {
        const f = e._eventHeights[b.childLevel], {total: g} = e.getResourceMarginObject(a, b);
        return f + g;
      } else {
        const i = (c = borderWidths[e.client.getEventStyle(b, a)]) != null ? c : 0;
        return e.currentParentsHeight - e.headerHeight - i;
      }
    }
    return e.overridden.getAppliedResourceHeight(a);
  }
  getResourceWidth(a, b) {
    if (b && !b.isRoot) {
      return this.currentParentsWidth - this.headerHeight;
    }
    return this.overridden.getResourceWidth(a);
  }
  attachToEventStore(a) {
    a == null ? void 0 : a.ion({
      'name': 'eventStore',
      'change': 'onEventStoreChange',
      'thisObj': this
    });
  }
  onEventStoreChange({records: a}) {
    let b;
    if (!((b = this.client.features) == null ? void 0 : b.eventDrag.isDragging) && (a == null ? void 0 : a.some(c => c.parent && !c.parent.isRoot))) {
      this.refreshClient();
    }
  }
  onEventStoreBatchedUpdate({records: a}) {
    if (this.client.listenToBatchedUpdates && (a == null ? void 0 : a.some(b => b.parent && !b.parent.isRoot))) {
      this.refreshClient();
      return false;
    }
  }
  beforeEventDragStart(a, b) {
    const c = this, {client: e} = c, {
        eventRecord: f,
        assignmentRecords: g
      } = b, {parentElement: i} = a.element;
    if (f.parent && i !== e.foregroundCanvas) {
      c.isDraggingNestedEvent = true;
      a.originalParentElement = i;
      a.originalBounds = [];
      for (const j of g) {
        const {event: k} = j;
        if (k.parent === f.parent) {
          const l = e.getElementFromAssignmentRecord(j, true);
          a.originalBounds.push({
            'element': l,
            'bounds': Rectangle.from(l, i)
          });
          if (!c.constrainDragToParent && e.features.eventDrag.constrainDragToTimeline) {
            const n = Rectangle.from(l, e.timeAxisSubGridElement);
            l.style.insetBlockStart = n.top + 'px';
            l.style.insetInlineStart = n.left + 'px';
            DomSync.addChild(e.foregroundCanvas, l, j.id);
          }
        }
      }
    } else {
      c.isDraggingNestedEvent = false;
    }
  }
  afterEventDragStart(a, b) {
    if (this.isDraggingNestedEvent && this.constrainDragToParent) {
      const {eventDrag: c} = this.client.features, {parent: e} = b.eventRecord, f = a.originalParentElement.getBoundingClientRect();
      c.setYConstraint(0, f.height - a.originalBounds[0].bounds.height);
      c.setXConstraint(0, f.width - a.originalBounds[0].bounds.width);
      b.dateConstraints = {
        'start': e.startDate,
        'end': e.endDate
      };
    }
  }
  checkEventDragValidity({
    targetEventRecord: a,
    eventRecord: b,
    timeDiff: c,
    newResource: e,
    resourceRecord: f
  }) {
    const g = this;
    if (g.isDraggingNestedEvent && !g.allowDeNestingOnDrop && !a && (c || e !== f)) {
      return {
        'valid': false,
        'message': g.L('L{deNestingNotAllowed}')
      };
    }
    if (!g.allowNestingOnDrop && a && a !== b.parent) {
      return {
        'valid': false,
        'message': g.L('L{nestingNotAllowed}')
      };
    }
    if (a && a !== b.parent) {
      const i = g.maxNesting, j = a.isParent ? a.childLevel : a.parent.childLevel, k = Math.max(...b.allChildren.map(n => n.childLevel)), l = k - b.childLevel;
      if (j + l >= i) {
        return {
          'valid': false,
          'message': g.L('L{nestingNotAllowed}')
        };
      }
    }
  }
  processEventDrop({
    context: a,
    toScheduler: b,
    eventRecord: c,
    resourceRecord: e,
    reassignedFrom: f,
    element: g,
    eventsToAdd: i,
    addedEvents: j,
    draggedAssignment: k
  }) {
    const {parent: l} = c, {targetEventRecord: n} = a;
    let o = l;
    if (l !== n && !this.constrainDragToParent) {
      if (n && this.allowNestingOnDrop) {
        o = n.isParent ? n : n.parent.isRoot ? n : n.parent;
        const p = this.client.resolveResourceRecord(a.browserEvent);
        if (p !== e) {
          e = k.resource = p;
        }
      } else {
        o = b.eventStore.rootNode;
      }
      if (o && o !== l) {
        j.push(o.appendChild(c));
        ArrayHelper.remove(i, c);
      }
    }
    if (c.isParent && f && f !== e) {
      for (const q of c.allChildren) {
        const r = q.assignments.find(u => u.resource === f);
        if (r) {
          r.resource = e;
        }
      }
    }
    if (o && !o.isRoot) {
      const u = this.client.getElementFromEventRecord(o, e).syncIdMap.nestedEventsContainer;
      if (u) {
        const v = Rectangle.from(u, this.client.foregroundCanvas), w = Rectangle.from(g, u);
        a.context.elementStartX -= v.x;
        a.context.elementStartY -= v.y;
        g.style.insetBlockStart = w.top + 'px';
        g.style.insetInlineStart = w.left + 'px';
        g.lastDomConfig = null;
        DomSync.addChild(u, g, g.dataset.syncId);
      }
    }
  }
  processCrossSchedulerEventDrop({eventRecord: a}) {
    if (a.isParent) {
      for (const b of a.allChildren) {
        b.resource = a.resource;
      }
    }
  }
  async afterEventDragAbortFinalized({
    originalParentElement: a,
    originalBounds: b
  }) {
    if (this.isDraggingNestedEvent) {
      for (const c of b[0].element.getAnimations()) {
        if (c.transitionProperty === 'inset') {
          await c.finished;
        }
      }
      for (const {
            element: e,
            bounds: f
          } of b) {
        e.style.insetBlockStart = f.top + 'px';
        e.style.insetInlineStart = f.left + 'px';
        a.appendChild(e);
      }
    }
  }
  afterEventResizeStart(a) {
    if (this.constrainResizeToParent) {
      a.eventContexts.forEach(b => {
        const {parent: c} = b.timespanRecord;
        if (c && !c.isRoot) {
          let {
            startDate: e,
            endDate: f
          } = c;
          if (b.dateConstraints) {
            e = DateHelper.max(e, b.dateConstraints.start);
            f = DateHelper.min(f, b.dateConstraints.end);
          }
          b.dateConstraints = {
            'start': e,
            'end': f
          };
        }
      });
    }
  }
  getElementFromAssignmentRecord(a, b) {
    let c;
    if (((c = a == null ? void 0 : a.event) == null ? void 0 : c.parent) && !a.event.parent.isRoot) {
      const e = this.client.getElementFromEventRecord(a.event.parent, a.resource);
      return e && DomSync.getChild(e, 'nestedEventsContainer.' + a.id + (b ? '' : '.event'));
    }
    return this.overridden.getElementFromAssignmentRecord(a, b);
  }
  async scheduleEvent({
    eventRecord: a,
    parentEventRecord: b,
    startDate: c,
    element: e
  }) {
    if (b) {
      a.startDate = c;
      a = b.appendChild(a);
      a.assign(b.resource);
      if (e) {
        const f = this.client.getElementFromEventRecord(b).syncIdMap.nestedEventsContainer, g = Rectangle.from(e, f);
        e.style.insetBlockStart = g.y + 'px';
        e.style.insetInlineStart = g.x + 'px';
        DomSync.addChild(f, e, a.assignments[0].id);
      }
      await this.client.project.commitAsync();
    } else {
      return this.overridden.scheduleEvent(...arguments);
    }
  }
  getEventsToRender(a, b) {
    if (!this.disabled) {
      ArrayHelper.remove(b, ...b.filter(c => c.isEventModel && !c.parent.isRoot));
    }
    return b;
  }
  afterRenderEvent({renderData: a}) {
    let b;
    const {eventRecord: c} = a, {childLevel: e} = c;
    if (c.isParent) {
      const f = this, {
          resourceRecord: g,
          width: i,
          height: j,
          left: k,
          top: l,
          wrapperCls: n
        } = a;
      n.add('b-nested-events-parent');
      f.currentParentsHeight = j;
      f.currentParentsWidth = i;
      const {
          currentOrientation: o,
          isVertical: p
        } = f.client, q = c.children.filter(B => {
          let C;
          return (C = B.$linkedResources) == null ? void 0 : C.includes(g);
        }), r = o.layoutEvents(g, q, { 'viewport': true }, c, f.overlappingEventSorter), u = [];
      let v;
      if (p) {
        v = [];
        for (const B of Object.values(r)) {
          v.push(B.renderData);
        }
      } else {
        v = r == null ? void 0 : r.eventsData;
      }
      if (v) {
        for (const C of v) {
          if (p) {
            C.left -= k;
            C.top -= l;
            C.absoluteTop = C.top;
          } else {
            if (f.eventLayout === 'none') {
              C.top = 0;
              C.height = f.getAppliedResourceHeight(g, c);
            } else if (f.eventLayout === 'stack') {
              C.height = f._eventHeights[e];
            }
            C.absoluteLeft = C.left;
            C.left -= (b = a.absoluteLeft) != null ? b : k;
            C.absoluteTop = C.top;
          }
          const E = o.renderEvent(p ? { 'renderData': C } : C, j);
          E.className['b-nested-event'] = 1;
          u.push(E);
        }
      }
      const w = {
        'className': {
          'b-nested-events-container': 1,
          ['b-nested-events-layout-' + f.eventLayout]: 1
        },
        'dataset': { 'taskBarFeature': 'nestedEventsContainer' },
        'children': u,
        'syncOptions': {
          'syncIdField': 'syncId',
          'releaseThreshold': 0
        }
      };
      const {children: z} = a, A = z.findIndex(F => F.dataset.taskBarFeature === 'nestedEventsContainer');
      if (A === -1) {
        z.push(w);
      } else {
        z.splice(A, 1, w);
      }
    }
    a.elementConfig.dataset.level = e;
  }
};
NestedEvents._$name = 'NestedEvents';
GridFeatureManager.registerFeature(NestedEvents, false, 'SchedulerPro');
function cls(a) {
  return 'b-task-percent-bar' + (a[0] ? '-' + a[0] : '');
}
const PercentBar = class extends InstancePlugin.mixin(TransactionalFeature_default, TaskEditStm_default) {
  static get $name() {
    return 'PercentBar';
  }
  static get configurable() {
    return {
      'allowResize': true,
      'showPercentage': true,
      'instantUpdate': true,
      'valueField': 'percentDone',
      'displayField': 'renderedPercentDone'
    };
  }
  static get pluginConfig() {
    return {
      'chain': [
        'onInternalPaint',
        {
          'fn': 'onTaskDataGenerated',
          'prio': -10000
        },
        {
          'fn': 'onEventDataGenerated',
          'prio': -10000
        }
      ]
    };
  }
  get project() {
    return this.client.project;
  }
  onInternalPaint({firstPaint: a}) {
    if (a) {
      const b = this, {client: c} = b;
      b.drag = new DragHelper({
        'name': 'percentBarHandle',
        'lockX': c.isVertical,
        'lockY': c.isHorizontal,
        'targetSelector': c.eventSelector + ' .b-task-percent-bar-handle',
        'dragThreshold': 1,
        'outerElement': c.timeAxisSubGridElement,
        'internalListeners': {
          'beforeDragStart': 'onBeforeDragStart',
          'dragStart': 'onDragStart',
          'drag': 'onDrag',
          'drop': 'onDrop',
          'abort': 'onDragAbort',
          'thisObj': b
        }
      });
      b.detachListeners('view');
      b.client.ion({
        'name': 'view',
        [c.scheduledEventName + 'mouseenter']: 'onTimeSpanMouseEnter',
        [c.scheduledEventName + 'mouseleave']: 'onTimeSpanMouseLeave',
        'thisObj': b
      });
    }
  }
  get sizeProp() {
    return this.client.isVertical ? 'height' : 'width';
  }
  get offsetSizeProp() {
    return this.client.isVertical ? 'offsetHeight' : 'offsetWidth';
  }
  get positionProp() {
    return this.client.isVertical ? 'top' : this.client.rtl ? 'right' : 'left';
  }
  get offsetPositionProp() {
    return this.client.isVertical ? 'offsetTop' : this.client.rtl ? 'offsetRight' : 'offsetLeft';
  }
  updateAllowResize(a) {
    this.client.element.classList.toggle(cls`drag-disabled`, !a);
  }
  updateShowPercentage(a) {
    this.client.element.classList.toggle(cls`show-percentage`, Boolean(a));
  }
  doDestroy() {
    let a;
    (a = this.drag) == null ? void 0 : a.destroy();
    super.doDestroy();
  }
  doDisable(a) {
    if (this.client.isPainted) {
      this.client.refresh();
    }
    super.doDisable(a);
  }
  reset(a) {
    const b = this, {client: c} = b, {project: e} = c;
    c.element.classList.remove(cls`resizing-task`);
    if (!b.isMouseInsideEvent) {
      b.handle.remove();
      b.handle = null;
    }
    if (c.transactionalFeaturesEnabled) {
      if (a.valid) {
        b.finishFeatureTransaction();
      } else {
        b.rejectFeatureTransaction();
      }
    }
    if (b.instantUpdate) {
      e.resumeAutoSync();
      c.eventStore.resumeAutoCommit();
    }
  }
  getPercentBarDOMConfig(a) {
    return {
      'className': cls`outer`,
      'dataset': { 'taskBarFeature': 'percentBar' },
      'children': [{
          'className': cls``,
          'dataset': { 'percent': a.getValue(this.displayField) },
          'style': { [this.sizeProp]: (a.getValue(this.valueField) || 0) + '%' }
        }]
    };
  }
  appendDOMConfig(a, b, c) {
    if ((a.isEvent || a.isTask) && !a.isMilestone && !this.disabled) {
      const {eventSegments: e} = this.client.features;
      if (a.isSegmented && (e == null ? void 0 : e.enabled)) {
        a.segments.forEach((f, g) => {
          c.segmentsDOMConfig[g].children.unshift(this.getPercentBarDOMConfig(f));
        });
      } else {
        b.unshift(this.getPercentBarDOMConfig(a));
      }
    }
  }
  onEventDataGenerated(a) {
    this.appendDOMConfig(a.eventRecord, a.children, a);
  }
  onTaskDataGenerated(a) {
    this.appendDOMConfig(a.taskRecord, a.children, a);
  }
  getHoverSegment(a) {
    const b = (a.toElement || a.target).closest('.b-sch-event-segment');
    if (b) {
      const c = Rectangle.from(b);
      if (c == null ? void 0 : c.contains(EventHelper.getPagePoint(a))) {
        return b;
      }
    }
  }
  isOverSegment(a) {
    return Boolean(this.getHoverSegment(a));
  }
  onTimeSpanMouseEnter(a) {
    const b = this, {
        client: c,
        sizeProp: e
      } = b, f = a[c.scheduledEventName + 'Record'];
    if (f.isMilestone || f.readOnly || b.disabled || !f.isEditable(b.valueField)) {
      return;
    }
    if (!b.drag.context) {
      const g = a[c.scheduledEventName + 'Element'], i = DomSync.getChild(g, c.scheduledEventName);
      if (!b.handle) {
        const j = { 'percent': f.getValue(b.valueField) };
        let k = f.getValue(b.valueField) + '%';
        const {eventSegments: l} = c.features;
        if (f.isSegmented && (l == null ? void 0 : l.enabled)) {
          j.segment = 0;
          const n = Rectangle.from(i);
          let o, p;
          f.segments.some((q, r) => {
            if (!q[b.valueField] && o !== 100) {
              return true;
            }
            o = q[b.valueField];
            j.segment = r;
            const u = DomSync.getChild(i, 'segments.' + r + '.percentBar').firstChild;
            p = Rectangle.from(u);
          });
          if (c.isVertical) {
            k = p ? p.top + p[e] - n.top : 0;
          } else if (c.rtl) {
            k = p ? p.right + p[e] - n.right : 0;
          } else {
            k = p ? p.left + p[e] - n.left : 0;
          }
        }
        b.handle = DomHelper.createElement({
          'parent': i,
          'className': cls`handle`,
          'style': { [b.positionProp]: k },
          'dataset': j
        });
      }
      b.isMouseInsideEvent = true;
    } else if (f === b.drag.context.taskRecord) {
      b.isMouseInsideEvent = true;
    }
  }
  onTimeSpanMouseLeave(a) {
    const b = this;
    if (!b.drag.context && b.handle && a.event.toElement !== b.handle) {
      b.handle.remove();
      b.handle = null;
    }
    b.isMouseInsideEvent = false;
  }
  onBeforeDragStart({
    source: a,
    context: b,
    event: c
  }) {
    const {
        client: e,
        offsetPositionProp: f,
        offsetSizeProp: g
      } = this, {eventSegments: i} = e.features, {element: j} = b, k = e.resolveEventRecord(j);
    let l, n, o, p;
    if (k.isSegmented && (i == null ? void 0 : i.enabled)) {
      const q = j.dataset.segment;
      l = DomSync.getChild(j.parentElement, 'segments.' + q + '.percentBar');
      n = l.firstElementChild;
      p = j.parentElement[g];
      o = DomSync.getChild(j.parentElement, 'segments.' + q)[f] + n[g];
    } else {
      l = DomSync.getChild(j.parentElement, 'percentBar');
      n = l.firstElementChild;
      p = l[g];
      o = n[g];
    }
    if (e.isVertical) {
      a.minY = -o;
      a.maxY = p - o;
    } else if (e.rtl) {
      a.maxX = o;
      a.minX = -(p - o);
    } else {
      a.minX = -o;
      a.maxX = p - o;
    }
    Object.assign(b, {
      'percentBar': n,
      'initialPos': o,
      'size': p,
      'taskRecord': k,
      'initialValue': k.getValue(this.valueField),
      'domEvent': c
    });
  }
  async onDragStart({
    context: a,
    event: b
  }) {
    const c = this, {client: e} = c, {project: f} = e;
    e.element.classList.add(cls`resizing-task`);
    a.element.retainElement = true;
    a.domEvent = b;
    e.trigger('percentBarDragStart', a);
    if (e.transactionalFeaturesEnabled) {
      await c.startFeatureTransaction();
    } else {
      c.captureStm(true);
    }
    if (c.instantUpdate) {
      f.suspendAutoSync();
      e.eventStore.suspendAutoCommit();
    }
  }
  onDrag({
    context: a,
    event: b
  }) {
    const c = this, {
        sizeProp: e,
        offsetSizeProp: f,
        client: g
      } = c, i = g.isHorizontal ? Math.round((a.initialPos + a.newX * (g.rtl ? -1 : 1)) / a.size * 100) : Math.round((a.initialPos + a.newY) / a.size * 100), {eventSegments: j} = g.features;
    if (c.instantUpdate) {
      a.taskRecord.set(c.valueField, i);
    }
    a.domEvent = b;
    a.percent = a.element.dataset.percent = i;
    if (a.taskRecord.isSegmented && (j == null ? void 0 : j.enabled)) {
      a.taskRecord.segments.forEach((k, l) => {
        const n = DomSync.getChild(a.element.parentElement, 'segments.' + l + '.percentBar').firstChild, o = DomSync.getChild(a.element.parentElement, 'segments.' + l);
        if (a.percent >= k.endPercentDone) {
          n.style[e] = o[f] + 'px';
        } else if (a.percent <= k.startPercentDone) {
          n.style[e] = '0';
        } else if (a.percent >= k.startPercentDone && a.percent <= k.endPercentDone) {
          const p = k.endPercentDone - k.startPercentDone;
          n.style[e] = (i - k.startPercentDone) * o[f] / p + 'px';
        }
      });
    }
    g.trigger('percentBarDrag', a);
  }
  onDragAbort({
    context: a,
    event: b
  }) {
    if (a.taskRecord.isSegmented) {
      a.percentBar.style[this.sizeProp] = a.taskRecord.segments[a.element.dataset.segment][this.valueField] + '%';
    }
    a.taskRecord.set(this.valueField, a.initialValue);
    a.domEvent = b;
    if (!this.client.transactionalFeaturesEnabled) {
      this.freeStm(false);
    }
    this.reset(a);
    this.client.trigger('percentBarDragAbort', a);
  }
  async onDrop({context: a}) {
    let b;
    const c = this, {
        client: e,
        project: f
      } = c, {
        taskRecord: g,
        percent: i,
        element: j
      } = a;
    g.set(c.valueField, i);
    if (g.isSegmented && ((b = e.features.eventSegments) == null ? void 0 : b.enabled)) {
      g.segments.some(({
        startPercentDone: k,
        endPercentDone: l
      }, n) => {
        if (i >= k && i <= l) {
          const o = l - k, p = DomSync.getChild(j.parentElement, 'segments.' + n), q = parseInt(p.style[c.sizeProp]) + (i - k) * p[c.offsetSizeProp] / o;
          j.style.cssText = c.positionProp + ': ' + q + 'px';
          j.dataset.segment = n;
          return true;
        }
      });
    } else {
      j.style.cssText = c.positionProp + ': ' + (e.rtl ? 100 - i : i) + '%';
    }
    await f.commitAsync();
    if (!e.transactionalFeaturesEnabled) {
      f.stm.mergeTransactionUpdateActions();
      this.freeStm(true);
    }
    c.reset(a);
    e.trigger('percentBarDrop', a);
  }
  get featureClass() {
  }
};
PercentBar._$name = 'PercentBar';
GridFeatureManager.registerFeature(PercentBar, false, 'SchedulerPro');
GridFeatureManager.registerFeature(PercentBar, true, 'Gantt');
const ResourceNonWorkingTime = class extends ResourceTimeRangesBase {
  static configurable = {
    'rangeCls': 'b-sch-resourcenonworkingtime',
    'maxTimeAxisUnit': 'hour',
    'enableMouseEvents': false,
    'resourceTimeRangeModelClass': ResourceTimeRangeModel,
    'entityName': 'resourceNonWorkingTime'
  };
  static $name = 'ResourceNonWorkingTime';
  static get properties() {
    return { 'resourceMap': new Map() };
  }
  construct() {
    let a;
    super.construct(...arguments);
    this.resourceTimeRangeModelClass = (a = class extends this.resourceTimeRangeModelClass {
    }, __publicField(a, '$name', 'ResourceNonWorkingTimeModel'), __publicField(a, 'domIdPrefix', 'resourcenonworkingtimemodel'), a);
    this.client.timeAxis.ion({
      'name': 'timeAxis',
      'reconfigure': 'onTimeAxisReconfigure',
      'prio': 100,
      'thisObj': this
    });
  }
  attachToResourceStore(a) {
    super.attachToResourceStore(a);
    a == null ? void 0 : a.ion({
      'name': 'resourceStore',
      'changePreCommit': 'onResourceChange',
      'thisObj': this
    });
  }
  attachToCalendarManagerStore(a) {
    super.attachToCalendarManagerStore(a);
    a == null ? void 0 : a.ion({
      'name': 'calendarManagerStore',
      'changePreCommit': 'onCalendarChange',
      'thisObj': this
    });
  }
  onTimeAxisReconfigure() {
    this.resourceMap.clear();
  }
  onResourceChange({
    action: a,
    records: b,
    record: c,
    changes: e
  }) {
    let f;
    const g = this;
    if (a === 'update') {
      const i = e.calendar;
      if (i && (typeof i.oldValue !== 'string' || ((f = i.value) == null ? void 0 : f.id) !== i.oldValue)) {
        g.resourceMap.delete(c.id);
        g.client.runWithTransition(() => {
          g.client.currentOrientation.refreshEventsForResource(c);
        });
      }
    }
    if (a === 'remove') {
      b.forEach(j => g.resourceMap.delete(j.id));
    }
    if (a === 'removeall') {
      g.resourceMap.clear();
    }
  }
  onCalendarChange({
    action: a,
    records: b,
    record: c,
    changes: e
  }) {
    this.resourceMap.clear();
    this.client.refresh();
  }
  getEventsToRender(a, b) {
    const c = this, {
        resourceMap: e,
        client: f
      } = c, {timeAxis: g} = f, i = !c.maxTimeAxisUnit || DateHelper.compareUnits(g.unit, c.maxTimeAxisUnit) <= 0;
    if (!c.disabled && i && a.effectiveCalendar) {
      if (!e.has(a.id)) {
        const j = a.effectiveCalendar, {
            cls: k,
            iconCls: l
          } = j, n = a.id, o = j.getNonWorkingTimeRanges(f.startDate, f.endDate), p = o.map((q, r) => new c.resourceTimeRangeModelClass({
            'id': 'r' + n + 'i' + r,
            'iconCls': q.iconCls || l || '',
            'cls': (k || '') + ' ' + (q.cls || ''),
            'startDate': q.startDate,
            'endDate': q.endDate,
            'name': q.name || '',
            'resourceId': n,
            'isNonWorking': true
          }));
        e.set(a.id, p);
      }
      b.push(...e.get(a.id));
    }
    return b;
  }
  shouldInclude({isNonWorking: a}) {
    return a;
  }
  resolveResourceNonWorkingTimeInterval(a) {
    let b;
    return (b = a == null ? void 0 : a.closest('.b-sch-resourcenonworkingtime')) == null ? void 0 : b.elementData.eventRecord;
  }
};
ResourceNonWorkingTime.featureClass = '';
ResourceNonWorkingTime._$name = 'ResourceNonWorkingTime';
GridFeatureManager.registerFeature(ResourceNonWorkingTime, false, 'SchedulerPro');
const ReadyStatePropagator_default = a => class b extends (a || Events_default(Base)) {
  get isReadyStatePropagator() {
    return true;
  }
  get canSave() {
    return true;
  }
  requestReadyStateChange() {
    this.trigger('readystatechange', { 'canSave': this.canSave });
  }
  get widgetClass() {
  }
};
const locale6 = {
  'localeName': 'En',
  'localeDesc': 'English (US)',
  'localeCode': 'en-US',
  'ConstraintTypePicker': {
    'none': 'None',
    'assoonaspossible': 'As soon as possible',
    'aslateaspossible': 'As late as possible',
    'muststarton': 'Must start on',
    'mustfinishon': 'Must finish on',
    'startnoearlierthan': 'Start no earlier than',
    'startnolaterthan': 'Start no later than',
    'finishnoearlierthan': 'Finish no earlier than',
    'finishnolaterthan': 'Finish no later than'
  },
  'SchedulingDirectionPicker': {
    'Forward': 'Forward',
    'Backward': 'Backward',
    'inheritedFrom': 'Inherited from',
    'enforcedBy': 'Enforced by'
  },
  'CalendarField': {
    'Default calendar': 'Default calendar',
    'editCalendar': 'Edit calendar'
  },
  'TaskEditorBase': {
    'Information': 'Information',
    'Save': 'Save',
    'Cancel': 'Cancel',
    'Delete': 'Delete',
    'calculateMask': 'Calculating...',
    'saveError': 'Can\'t save, please correct errors first',
    'repeatingInfo': 'Viewing a repeating event',
    'editRepeating': 'Edit'
  },
  'TaskEdit': {
    'editEvent': 'Edit',
    'ConfirmDeletionTitle': 'Confirm deletion',
    'ConfirmDeletionMessage': 'Are you sure you want to delete the event?'
  },
  'GanttTaskEditor': { 'editorWidth': '44em' },
  'SchedulerTaskEditor': { 'editorWidth': '34em' },
  'SchedulerGeneralTab': {
    'labelWidth': '6em',
    'General': 'General',
    'Name': 'Name',
    'Resources': 'Resources',
    '% complete': '% complete',
    'Duration': 'Duration',
    'Start': 'Start',
    'Finish': 'Finish',
    'Effort': 'Effort',
    'Preamble': 'Preamble',
    'Postamble': 'Postamble'
  },
  'GeneralTab': {
    'labelWidth': '6.5em',
    'General': 'General',
    'Name': 'Name',
    '% complete': '% complete',
    'Duration': 'Duration',
    'Start': 'Start',
    'Finish': 'Finish',
    'Effort': 'Effort',
    'Dates': 'Dates'
  },
  'SchedulerAdvancedTab': {
    'labelWidth': '13em',
    'Advanced': 'Advanced',
    'Calendar': 'Calendar',
    'Scheduling mode': 'Scheduling mode',
    'Effort driven': 'Effort driven',
    'Manually scheduled': 'Manually scheduled',
    'Constraint type': 'Constraint type',
    'Constraint date': 'Constraint date',
    'Inactive': 'Inactive',
    'Ignore resource calendar': 'Ignore resource calendar'
  },
  'CalendarEditorDatePicker': {
    'addException': 'Add exception',
    'addWeek': 'Add week'
  },
  'CalendarEditorExceptionTab': {
    'addException': 'Add exception',
    'removeException': 'Remove exception',
    'noRows': 'No exceptions added'
  },
  'CalendarEditorWeekTab': {
    'addWeek': 'Add week',
    'removeWeek': 'Remove week'
  },
  'CalendarEditor': {
    'daysAreWorkingByDefault': 'Days are working by default',
    'workingTimeCalendar': 'Working time calendar',
    'exceptions': 'Exceptions',
    'general': 'General',
    'name': 'Name',
    'parent': 'Parent',
    'save': 'Save',
    'weeks': 'Weeks',
    'error': 'Error',
    'inputErrors': 'There are input errors. Please fix them before saving your changes.',
    'delete': 'Delete',
    'addCalendar': 'Add a calendar',
    'confirmation': 'Confirmation',
    'removeCalendarWithChildren': 'The calendar has child calendars. Click "Yes" to remove the calendar with its children.',
    'removeCalendarAndLinks': 'The calendar is used by some records. Click "Yes" to unlink them and remove the calendar.'
  },
  'CalendarEditorDateInfo': {
    'isWorking': ' is working',
    'isNotWorking': ' is not working',
    'basedOn': ' based on:',
    'byDefault': 'by default',
    'rangeTpl': (a, b) => a + ' to ' + b
  },
  'CalendarEditorLegend': {
    'workingDay': 'Working day',
    'nonWorkingDay': 'Non-working day'
  },
  'AvailabilityRangeError': {
    'errorOverlap': 'There are overlapping ranges',
    'errorMissingDate': 'Start and end time are required',
    'errorStartAfterEnd': 'Start time cannot be after the end time'
  },
  'CalendarEditorExceptionPanel': {
    'name': 'Name',
    'from': 'From',
    'to': 'To',
    'is': 'Is',
    'nonWorking': 'Non-working',
    'working': 'Working',
    'hours': 'Hours',
    'repeat': 'Repeat',
    'repeatEnd': 'Repeat end',
    'errorMissingDate': 'Exception date range is required',
    'errorStartAfterEnd': '"From" date cannot be after the "To" date ',
    'errorStartAndEndRepeatNumberMismatch': 'Exception should start and end the same number of times'
  },
  'CalendarEditorWeekPanel': {
    'name': 'Name',
    'from': 'From',
    'to': 'To',
    'days': 'Days',
    'copyDay': 'Copy day',
    'pasteDay': 'Paste day',
    'stopCopying': 'Stop copying'
  },
  'CalendarEditorAvailabilityRangeContainer': {
    'addRange': 'Add range',
    'removeRange': 'Remove range'
  },
  'CalendarEditorWeekGrid': {
    'errorStartAfterEnd': '"From" date cannot be after the "To" date ',
    'errorNoDefaultWeek': 'Week configuration is required',
    'errorMultipleDefaultWeeks': 'More than one default week is specified',
    'errorNoWeekAvailability': 'Week must provide some working time',
    'errorInvalidWeekAvailability': 'Week has working time configuration errors',
    'noRows': 'No weeks added'
  },
  'AdvancedTab': {
    'labelWidth': '11.5em',
    'Advanced': 'Advanced',
    'Calendar': 'Calendar',
    'Scheduling mode': 'Scheduling mode',
    'Effort driven': 'Effort driven',
    'Manually scheduled': 'Manually scheduled',
    'Constraint type': 'Constraint type',
    'Constraint date': 'Constraint date',
    'Constraint': 'Constraint',
    'Rollup': 'Rollup',
    'Inactive': 'Inactive',
    'Ignore resource calendar': 'Ignore resource calendar',
    'Scheduling direction': 'Scheduling direction',
    'projectBorder': 'Project border',
    'ignore': 'Ignore',
    'honor': 'Honor',
    'askUser': 'Ask user'
  },
  'DependencyTab': {
    'Predecessors': 'Predecessors',
    'Successors': 'Successors',
    'ID': 'ID',
    'Name': 'Name',
    'Type': 'Type',
    'Lag': 'Lag',
    'cyclicDependency': 'Cyclic dependency',
    'invalidDependency': 'Invalid dependency'
  },
  'NotesTab': { 'Notes': 'Notes' },
  'ResourceCalendarColumn': { 'calendar': 'Calendar' },
  'ResourcesTab': {
    'unitsTpl': ({value: a}) => a + '%',
    'Resources': 'Resources',
    'Resource': 'Resource',
    'Units': 'Units'
  },
  'RecurrenceTab': { 'title': 'Repeat' },
  'SchedulingModePicker': {
    'Normal': 'Normal',
    'Fixed Duration': 'Fixed Duration',
    'Fixed Units': 'Fixed Units',
    'Fixed Effort': 'Fixed Effort'
  },
  'ResourceHistogram': {
    'barTipInRange': '<b>{resource}</b> {startDate} - {endDate}<br><span class="{cls}">{allocated} of {available}</span> allocated',
    'barTipOnDate': '<b>{resource}</b> on {startDate}<br><span class="{cls}">{allocated} of {available}</span> allocated',
    'groupBarTipAssignment': '<b>{resource}</b> - <span class="{cls}">{allocated} of {available}</span>',
    'groupBarTipInRange': '{startDate} - {endDate}<br><span class="{cls}">{allocated} of {available}</span> allocated:<br>{assignments}',
    'groupBarTipOnDate': 'On {startDate}<br><span class="{cls}">{allocated} of {available}</span> allocated:<br>{assignments}',
    'plusMore': '+{value} more'
  },
  'ResourceUtilization': {
    'barTipInRange': '<b>{event}</b> {startDate} - {endDate}<br><span class="{cls}">{allocated}</span> allocated',
    'barTipOnDate': '<b>{event}</b> on {startDate}<br><span class="{cls}">{allocated}</span> allocated',
    'groupBarTipAssignment': '<b>{event}</b> - <span class="{cls}">{allocated}</span>',
    'groupBarTipInRange': '{startDate} - {endDate}<br><span class="{cls}">{allocated} of {available}</span> allocated:<br>{assignments}',
    'groupBarTipOnDate': 'On {startDate}<br><span class="{cls}">{allocated} of {available}</span> allocated:<br>{assignments}',
    'plusMore': '+{value} more',
    'nameColumnText': 'Resource / Event'
  },
  'SchedulingIssueResolutionPopup': {
    'Cancel changes': 'Cancel the change and do nothing',
    'schedulingConflict': 'Scheduling conflict',
    'emptyCalendar': 'Calendar configuration error',
    'cycle': 'Scheduling cycle',
    'Apply': 'Apply'
  },
  'CycleResolutionPopup': {
    'dependencyLabel': 'Please select a dependency:',
    'invalidDependencyLabel': 'There are invalid dependencies involved that need to be addressed:'
  },
  'DependencyEdit': { 'Active': 'Active' },
  'SchedulerProBase': {
    'propagating': 'Calculating project',
    'storePopulation': 'Loading data',
    'finalizing': 'Finalizing results'
  },
  'EventSegments': {
    'splitEvent': 'Split',
    'renameSegment': 'Rename'
  },
  'NestedEvents': {
    'deNestingNotAllowed': 'De-nesting not allowed',
    'nestingNotAllowed': 'Nesting not allowed'
  },
  'VersionGrid': {
    'compare': 'Compare',
    'description': 'Description',
    'occurredAt': 'Occurred At',
    'rename': 'Rename',
    'restore': 'Restore',
    'stopComparing': 'Stop Comparing'
  },
  'Versions': {
    'entityNames': {
      'TaskModel': 'task',
      'AssignmentModel': 'assignment',
      'DependencyModel': 'link',
      'ProjectModel': 'project',
      'ResourceModel': 'resource',
      'other': 'object'
    },
    'entityNamesPlural': {
      'TaskModel': 'tasks',
      'AssignmentModel': 'assignments',
      'DependencyModel': 'links',
      'ProjectModel': 'projects',
      'ResourceModel': 'resources',
      'other': 'objects'
    },
    'transactionDescriptions': {
      'update': 'Changed {n} {entities}',
      'add': 'Added {n} {entities}',
      'remove': 'Removed {n} {entities}',
      'move': 'Moved {n} {entities}',
      'mixed': 'Changed {n} {entities}'
    },
    'addEntity': 'Added {type} **{name}**',
    'removeEntity': 'Removed {type} **{name}**',
    'updateEntity': 'Changed {type} **{name}**',
    'moveEntity': 'Moved {type} **{name}** from {from} to {to}',
    'addDependency': 'Added link from **{from}** to **{to}**',
    'removeDependency': 'Removed link from **{from}** to **{to}**',
    'updateDependency': 'Edited link from **{from}** to **{to}**',
    'addAssignment': 'Assigned **{resource}** to **{event}**',
    'removeAssignment': 'Removed assignment of **{resource}** from **{event}**',
    'updateAssignment': 'Edited assignment of **{resource}** to **{event}**',
    'noChanges': 'No changes',
    'nullValue': 'none',
    'versionDateFormat': 'M/D/YYYY h:mm a',
    'undid': 'Undid changes',
    'redid': 'Redid changes',
    'editedTask': 'Edited task properties',
    'deletedTask': 'Deleted a task',
    'movedTask': 'Moved a task',
    'movedTasks': 'Moved tasks'
  }
};
const En_default5 = LocaleHelper.publishLocale(locale6);
const TaskEditorBase = class extends Popup.mixin(ReadyStatePropagator_default) {
  static get $name() {
    return 'TaskEditorBase';
  }
  static get type() {
    return 'taskeditorbase';
  }
  static get configurable() {
    return {
      'localizableProperties': ['width'],
      'scrollAction': null,
      'title': 'L{Information}',
      'cls': 'b-schedulerpro-taskeditor',
      'closable': true,
      'layout': 'fit',
      'draggable': { 'handleSelector': ':not(button,.b-field-inner)' },
      'items': null,
      'bbar': {
        'hideWhenEmpty': true,
        'defaults': { 'localeClass': this },
        'items': {
          'saveButton': {
            'text': 'L{Save}',
            'color': 'b-blue',
            'cls': 'b-raised',
            'weight': 100
          },
          'deleteButton': {
            'text': 'L{Delete}',
            'weight': 200
          },
          'cancelButton': {
            'text': 'L{Object.Cancel}',
            'weight': 300
          }
        }
      },
      'strips': {
        'occurrenceInfoToolbar': {
          'cls': 'occurrence-info-toolbar',
          'hidden': true,
          'ignoreParentReadOnly': true,
          'items': [
            {
              'type': 'widget',
              'tag': 'i',
              'cls': 'b-icon b-icon-locked'
            },
            {
              'ref': 'occurrenceInfoText',
              'type': 'label',
              'localeClass': this,
              'text': 'L{repeatingInfo}'
            },
            '->',
            {
              'ref': 'editOccurrenceButton',
              'localeClass': this,
              'text': 'L{editRepeating}',
              'onClick': 'up.onEditOccurrenceClick'
            }
          ]
        }
      },
      'width': { '$config': { 'localeKey': 'L{editorWidth}' } },
      'maximizeOnMobile': true,
      'includeAsapAlapAsConstraints': true
    };
  }
  static get defaultConfig() {
    return {
      'axisLock': 'flexible',
      'autoClose': true,
      'onChange': null,
      'onCancel': null,
      'onSave': null,
      'autoShow': false,
      'blurAction': 'cancel',
      'scrollAction': null,
      'durationDisplayPrecision': 1,
      'tabPanelItems': null,
      'defaultTabs': null,
      'calculateMask': null,
      'calculateMaskDelay': 100,
      'localizableProperties': ['calculateMask'],
      'project': null,
      'dependencyIdField': null
    };
  }
  processWidgetConfig(a) {
    let b;
    if (((b = a.type) == null ? void 0 : b.includes('date')) && a.weekStartDay == null) {
      a.weekStartDay = this.weekStartDay;
    }
    if (a.ref === 'tabs' && this.extraItems) {
      const c = {};
      for (const e in this.extraItems) {
        const f = e.replace('tab', 'Tab');
        c[f] = { 'items': Array.isArray(this.extraItems[e]) ? ObjectHelper.transformArrayToNamedObject(this.extraItems[e]) : this.extraItems[e] };
      }
      ObjectHelper.merge(a.items, c);
    }
    return a;
  }
  changeItems(a) {
    let b;
    const {
        tabPanelItems: tabPanelItems = {}
      } = this, c = ObjectHelper.clone(a), e = ((b = c.find) == null ? void 0 : b.call(c, f => f.ref === 'tabs')) || c.tabs;
    this.cleanItemsConfig(tabPanelItems);
    ObjectHelper.merge(e.items, tabPanelItems);
    return super.changeItems(c);
  }
  cleanItemsConfig(a) {
    for (const b in a) {
      const c = a[b];
      if (c === true) {
        delete a[b];
      } else if (c == null ? void 0 : c.items) {
        this.cleanItemsConfig(c.items);
      }
    }
  }
  afterConfigure() {
    let a;
    const b = this, {widgetMap: c} = b, {tabs: e} = c, {
        cancelButton: f,
        deleteButton: g,
        saveButton: i
      } = ((a = b.bbar) == null ? void 0 : a.widgetMap) || {};
    if (b.maximized) {
      e.autoHeight = false;
    }
    i == null ? void 0 : i.ion({
      'click': 'onSaveClick',
      'thisObj': b
    });
    f == null ? void 0 : f.ion({
      'click': 'onCancelClick',
      'thisObj': b
    });
    g == null ? void 0 : g.ion({
      'click': 'onDeleteClick',
      'thisObj': b
    });
    Object.values(c).forEach(j => {
      if (j.isDurationField) {
        j.decimalPrecision = this.durationDisplayPrecision;
      } else if (j.ref === 'startDate' || j.ref === 'endDate') {
        j.project = this.project;
      } else if (j.ref === 'predecessorsTab' || j.ref === 'successorsTab') {
        j.grid.durationDisplayPrecision = this.durationDisplayPrecision;
        j.dependencyIdField = j.dependencyIdField || b.dependencyIdField;
      }
      if (j.isReadyStatePropagator) {
        j.ion({
          'readyStateChange': 'onReadyStateChange',
          'thisObj': b
        });
      }
    });
    e.onFieldChange = ({source: j}) => {
      const {
        name: k,
        isValid: l,
        value: n
      } = j;
      if (b.loadedRecord && k && l && !b.isLoadingEvent && !j.up('recurrenceeditorpanel')) {
        b.loadedRecord.setValue(k, n);
      }
    };
  }
  get canSave() {
    let a = true;
    Object.values(this.widgetMap).forEach(b => {
      if (b.isReadyStatePropagator) {
        a = a && b.canSave;
      }
    });
    return a;
  }
  async hide() {
    const a = this;
    a.detachListeners('project');
    a.detachListeners('eventStore');
    a._delayedAction = null;
    const b = await super.hide(this.element.matches(':focus-within'));
    if (!a.isDestroyed) {
      a.trigger(a.blurAction);
    }
    return b;
  }
  toggleFieldsDisabled(a) {
    this.eachWidget(b => {
      if (b.isField && b.name && (!b.disabled || b.$disabledByEditor) && !b.up('recurrenceeditorpanel')) {
        const c = a.isEditable(b.name);
        if (c !== void 0) {
          b.disabled = b.$disabledByEditor = !c;
        }
      }
    });
  }
  loadEvent(a, b = false) {
    let c;
    const e = this, f = (c = e.project) != null ? c : a.project;
    e.isLoadingEvent = true;
    e.includeAsapAlapAsConstraints = f.includeAsapAlapAsConstraints;
    e.widgetMap.occurrenceInfoToolbar.hidden = !a.isOccurrence && !a.isRecurring || e.editingRecurring;
    e.toggleFieldsDisabled(a);
    e.callWidgetHook('loadEvent', a, b);
    e.detachListeners('project');
    e.loadedRecord = a;
    f.ion({
      'name': 'project',
      'beforeCommit': 'onProjectBeforeCommit',
      'dataReady': 'onProjectDataReady',
      'thisObj': e
    });
    e.detachListeners('eventStore');
    f.eventStore.ion({
      'name': 'eventStore',
      'remove': 'onTaskRemove',
      'thisObj': e
    });
    e.isLoadingEvent = false;
    e.trigger('loadEvent');
  }
  callWidgetHook(a, ...b) {
    this.eachWidget(c => {
      if (typeof c[a] === 'function') {
        c[a](...b);
      }
    });
  }
  onDocumentMouseDown(a) {
    const b = this, c = Object.values(b.widgetMap).some(f => f._activeCellEdit);
    let e = null;
    if (c) {
      const {event: f} = a, {
          saveButton: g,
          cancelButton: i,
          deleteButton: j
        } = b.widgetMap, k = f.target.closest('button');
      if (k) {
        switch (k) {
        case g == null ? void 0 : g.element:
          e = b.onSaveClick;
          break;
        case i == null ? void 0 : i.element:
          e = b.onCancelClick;
          break;
        case j == null ? void 0 : j.element:
          e = b.onDeleteClick;
          break;
        }
      }
    }
    b._delayedAction = e;
    super.onDocumentMouseDown(a);
  }
  onSaveClick() {
    const a = this;
    a._delayedAction = null;
    if (a.canSave) {
      a.trigger('save');
    } else {
      Toast.show({
        'rootElement': a.rootElement,
        'html': a.L('L{saveError}')
      });
    }
  }
  onCancelClick() {
    this.close();
  }
  onDeleteClick() {
    this._delayedAction = null;
    this.trigger('delete');
  }
  onPropagationRequested() {
    this.trigger('requestPropagation');
  }
  onReadyStateChange({
    source: a,
    canSave: b
  }) {
    this.requestReadyStateChange();
    if (!a.couldSaveTitle) {
      a.couldSaveTitle = a.title;
    }
    if (a.parent === this.widgetMap.tabs) {
      if (b) {
        a.tab.element.classList.remove('b-invalid');
        a.tab.icon = null;
        a.title = a.couldSaveTitle;
        a.couldSaveTitle = null;
      } else {
        a.tab.element.classList.add('b-invalid');
        a.tab.icon = 'b-icon-warning';
        a.title = a.couldSaveTitle;
      }
    }
  }
  onTaskRemove() {
    this.afterDelete();
  }
  onProjectBeforeCommit() {
    if (this.calculateMask) {
      this.mask({
        'text': this.calculateMask,
        'showDelay': this.calculateMaskDelay
      });
    }
  }
  onProjectDataReady({records: a}) {
    let b;
    const c = this;
    if (c.calculateMask) {
      c.unmask();
    }
    if (a.has(c.loadedRecord)) {
      c.callWidgetHook('afterProjectChange');
    }
    (b = c._delayedAction) == null ? void 0 : b.call(c);
  }
  beforeSave() {
    this.callWidgetHook('beforeSave');
  }
  afterSave() {
    this.loadedRecord = void 0;
    this.callWidgetHook('afterSave');
  }
  beforeCancel() {
    this.callWidgetHook('beforeCancel');
  }
  afterCancel() {
    this.loadedRecord = void 0;
    this.callWidgetHook('afterCancel');
  }
  beforeDelete() {
    this.callWidgetHook('beforeDelete');
  }
  afterDelete() {
    this.loadedRecord = void 0;
    this.callWidgetHook('afterDelete');
  }
  onInternalKeyDown(a) {
    if (!a.handled && a.key === 'Enter' && this.saveAndCloseOnEnter && a.target.tagName.toLowerCase() === 'input') {
      if (a.target.matches('input')) {
        const b = Widget.fromElement(a.target);
        if ((b == null ? void 0 : b.internalOnChange) && !b.isCheckbox && !b.isSlider) {
          b.internalOnChange();
        }
      }
      a.preventDefault();
      this.onSaveClick();
    }
    super.onInternalKeyDown(a);
  }
  onEditOccurrenceClick() {
    const a = this, b = Widget.create({
        'type': 'recurrenceconfirmation',
        'owner': a
      });
    b.confirm({
      'actionType': 'update',
      'eventRecord': a.loadedRecord,
      'changerFn': c => {
        let e;
        const f = (e = c.recurringTimeSpan) != null ? e : c;
        a.resetRecurrenceData = {
          'recurringTimeSpan': f,
          'originalExceptionDates': { ...f.exceptionDates }
        };
        if (!c.isRecurring) {
          c.convertToRealEvent({ 'startDate': c.startDate });
          c.constraintDate = null;
        }
      },
      'finalizerFn': c => {
        if (c.isRecurring) {
          a.editingRecurring = true;
          a.readOnly = false;
        }
        a.loadEvent(c);
      }
    });
  }
  updateReadOnly(a) {
    const {
        deleteButton: b,
        saveButton: c,
        cancelButton: e,
        tabs: f
      } = this.widgetMap, {items: g} = f;
    super.updateReadOnly(a);
    if (b) {
      b.hidden = a;
    }
    if (c) {
      c.hidden = a;
    }
    if (e) {
      e.hidden = a;
    }
    for (let j = 0, {length: k} = g; j < k; j++) {
      g[j].readOnly = a;
    }
  }
  updateIncludeAsapAlapAsConstraints() {
    this.eachWidget(a => {
      a.includeAsapAlapAsConstraints = this.includeAsapAlapAsConstraints;
    });
  }
};
TaskEditorBase.initClass();
TaskEditorBase._$name = 'TaskEditorBase';
const EventLoader_default = a => class b extends (a || Container) {
  get project() {
    let c, e, f;
    return (f = (c = this.up(g => g.isTaskEditorBase)) == null ? void 0 : c.project) != null ? f : (e = this.record) == null ? void 0 : e.project;
  }
  loadEvent(c, e) {
    this.setRecord(c, e);
  }
  resetData() {
    this.record = null;
  }
  beforeSave() {
  }
  afterSave() {
    this.resetData();
  }
  beforeCancel() {
  }
  afterCancel() {
    this.resetData();
  }
  beforeDelete() {
  }
  afterDelete() {
    this.resetData();
  }
  get widgetClass() {
  }
};
const EditorTab = class extends Container.mixin(EventLoader_default, ReadyStatePropagator_default) {
  static get $name() {
    return 'EditorTab';
  }
  static get type() {
    return 'editortab';
  }
  static get configurable() {
    return {
      'title': null,
      'strictRecordMapping': true
    };
  }
};
EditorTab._$name = 'EditorTab';
const FormTab = class extends EditorTab {
  static get $name() {
    return 'FormTab';
  }
  static get type() {
    return 'formtab';
  }
  static get defaultConfig() {
    return {
      'layoutStyle': {
        'flexFlow': 'row wrap',
        'alignItems': 'flex-start',
        'alignContent': 'flex-start'
      },
      'scrollable': { 'overflowY': true },
      'autoUpdateRecord': true
    };
  }
  onFieldChange({
    source: a,
    valid: b,
    userAction: c
  }) {
    if (c) {
      b = b !== void 0 ? b : typeof a.isValid === 'function' ? a.isValid() : a.isValid;
      if (b) {
        super.onFieldChange(...arguments);
      }
    }
  }
};
FormTab.initClass();
FormTab._$name = 'FormTab';
const year2300 = new Date(2300, 0, 1);
const StartDateField = class extends DateField {
  static alias = 'startdate';
  static type = 'startdatefield';
  static $name = 'StartDateField';
  static get defaultConfig() {
    return {
      'project': null,
      'eventRecord': null,
      'strictParsing': true,
      'minDateDelta': '-10 years',
      'maxDateDelta': '200 years'
    };
  }
  get calendarProvider() {
    let a, b;
    return ((b = (a = this.eventRecord) == null ? void 0 : a.recurringEvent) != null ? b : this.eventRecord) || this.project;
  }
  getStepDate(a) {
    const b = this, c = a > 0, {_step: e} = b;
    let f = b.calendarProvider.run('skipWorkingTime', b.value, c, e.magnitude, e.unit);
    if (f && c) {
      f = b.calendarProvider.run('skipNonWorkingTime', f, true);
    }
    return f;
  }
  transformTimeValue(a) {
    const {
      calendarProvider: b,
      keepTime: c
    } = this;
    if ((b == null ? void 0 : b.graph) && c !== 'entered' && c !== true) {
      const e = DateHelper.clearTime(a), f = b.run('skipNonWorkingTime', e);
      if (DateHelper.isValidDate(f) && DateHelper.isEqual(f, e, 'day')) {
        return DateHelper.copyTimeValues(e, f);
      }
    }
    return super.transformTimeValue(a);
  }
  get min() {
    let a;
    return super.min || (((a = this.project) == null ? void 0 : a.startDate) ? DateHelper.add(this.project.startDate, this.minDateDelta) : null);
  }
  set min(a) {
    super.min = a;
  }
  get max() {
    let a;
    return super.max || (((a = this.project) == null ? void 0 : a.startDate) ? DateHelper.add(this.project.startDate, this.maxDateDelta) : year2300);
  }
  set max(a) {
    super.max = a;
  }
};
StartDateField.initClass();
StartDateField._$name = 'StartDateField';
const year23002 = new Date(2300, 0, 1);
const EndDateField = class extends DateField {
  static alias = 'enddate';
  static type = 'enddatefield';
  static $name = 'EndDateField';
  static get defaultConfig() {
    return {
      'project': null,
      'eventRecord': null,
      'strictParsing': true
    };
  }
  get min() {
    let a;
    let b = this._min;
    const c = (a = this.eventRecord) == null ? void 0 : a.startDate;
    if (c) {
      b = DateHelper.max(b || c, c);
    }
    return b;
  }
  set min(a) {
    super.min = a;
  }
  get max() {
    let a;
    return super.max || ((a = this.project) == null ? void 0 : a.startDate) ? DateHelper.add(this.project.startDate, 200, 'year') : year23002;
  }
  set max(a) {
    super.max = a;
  }
  get calendarProvider() {
    let a, b;
    return ((b = (a = this.eventRecord) == null ? void 0 : a.recurringEvent) != null ? b : this.eventRecord) || this.project;
  }
  getStepDate(a) {
    const b = this, c = a > 0, {_step: e} = b;
    let f = b.calendarProvider.run('skipWorkingTime', b.value, c, e.magnitude, e.unit);
    if (f && !c) {
      f = b.calendarProvider.run('skipNonWorkingTime', f, false);
    }
    return f;
  }
  transformTimeValue(a) {
    const {
      calendarProvider: b,
      keepTime: c
    } = this;
    if ((b == null ? void 0 : b.graph) && c !== 'entered' && c !== true) {
      const e = DateHelper.clearTime(a), f = DateHelper.add(e, 1, 'day'), g = b.run('skipNonWorkingTime', f, false);
      if (DateHelper.isValidDate(g) && DateHelper.isEqual(g, e, 'day')) {
        return DateHelper.copyTimeValues(e, g);
      }
    }
    if (!c && a && this.min && DateHelper.clearTime(a, false) < this.min) {
      return this.min;
    }
    return super.transformTimeValue(a);
  }
};
EndDateField.initClass();
EndDateField._$name = 'EndDateField';
const EffortField = class extends DurationField {
  static alias = 'effortfield';
  static type = 'effort';
  static $name = 'EffortField';
};
EffortField.initClass();
EffortField._$name = 'EffortField';
const GeneralTab = class extends FormTab {
  static get $name() {
    return 'GeneralTab';
  }
  static get type() {
    return 'generaltab';
  }
  static get defaultConfig() {
    return {
      'title': 'L{General}',
      'cls': 'b-general-tab',
      'defaults': { 'localeClass': this },
      'items': {
        'name': {
          'type': 'text',
          'weight': 100,
          'required': true,
          'label': 'L{Name}',
          'clearable': true,
          'name': 'name',
          'cls': 'b-name'
        },
        'percentDone': {
          'type': 'number',
          'weight': 200,
          'label': 'L{% complete}',
          'name': 'renderedPercentDone',
          'cls': 'b-percent-done b-half-width',
          'min': 0,
          'max': 100
        },
        'effort': {
          'type': 'effort',
          'weight': 300,
          'label': 'L{Effort}',
          'name': 'fullEffort',
          'cls': 'b-half-width'
        },
        'divider': {
          'html': '',
          'weight': 400,
          'dataset': { 'text': this.L('L{Dates}') },
          'cls': 'b-divider',
          'flex': '1 0 100%'
        },
        'startDate': {
          'type': 'startdate',
          'weight': 500,
          'label': 'L{Start}',
          'name': 'startDate',
          'cls': 'b-start-date b-half-width'
        },
        'endDate': {
          'type': 'enddate',
          'weight': 600,
          'label': 'L{Finish}',
          'name': 'endDate',
          'cls': 'b-end-date b-half-width'
        },
        'duration': {
          'type': 'durationfield',
          'weight': 700,
          'label': 'L{Duration}',
          'name': 'fullDuration',
          'cls': 'b-half-width'
        },
        'colorField': {
          'type': 'eventcolorfield',
          'weight': 800,
          'label': 'L{SchedulerBase.color}',
          'name': 'eventColor',
          'cls': 'b-half-width'
        }
      }
    };
  }
  loadEvent(a) {
    const b = {
        'unit': a.durationUnit,
        'magnitude': 1
      }, {
        startDate: c,
        endDate: e,
        effort: f
      } = this.widgetMap;
    if (c) {
      c.step = b;
      c.eventRecord = a;
    }
    if (e) {
      e.step = b;
      e.eventRecord = a;
    }
    if (f) {
      f.unit = a.effortUnit;
    }
    super.loadEvent(a);
  }
};
GeneralTab.initClass();
GeneralTab._$name = 'GeneralTab';
const buildItems = a => a.map((b, c) => [
  c,
  b
]);
const DependencyTypePicker = class extends Combo {
  static get $name() {
    return 'DependencyTypePicker';
  }
  static get type() {
    return 'dependencytypepicker';
  }
  construct(a) {
    super.construct(a);
    LocaleManager_default.ion({
      'locale': () => {
        this.items = buildItems(this.L('L{DependencyType.long}'));
      },
      'thisObj': this
    });
  }
  get store() {
    if (!this._items) {
      this.items = this._items = buildItems(this.L('L{DependencyType.long}'));
    }
    return super.store;
  }
  set store(a) {
    super.store = a;
  }
};
DependencyTypePicker.initClass();
DependencyTypePicker._$name = 'DependencyTypePicker';
const markDependencyValid = (a, b) => {
  a.instanceMeta(b).valid = true;
};
const markDependencyInvalid = (a, b) => {
  a.instanceMeta(b).valid = false;
};
const isDependencyMarkedValid = (a, b) => a.instanceMeta(b).valid !== false;
const DependencyTab = class extends EditorTab {
  static get $name() {
    return 'DependencyTab';
  }
  static get type() {
    return 'dependencytab';
  }
  static get configurable() {
    return {
      'dependencyIdField': null,
      'layoutStyle': { 'flexFlow': 'column nowrap' },
      'sortField': null,
      'taskComboSortField': 'name',
      'items': {
        'grid': {
          'type': 'grid',
          'weight': 100,
          'flex': '1 1 auto',
          'emptyText': '',
          'asyncEventSuffix': 'PreCommit',
          'disableGridRowModelWarning': true,
          'features': {
            'group': false,
            'cellEdit': {
              'validateTapOut'(a, b) {
                const {target: c} = a, {grid: e} = this, f = e.up(k => k.isTaskEditorBase), {inputField: g} = b.editor, i = g.L('L{DependencyTab.invalidDependency}'), {direction: j} = e.parent;
                if (b.column.field === j) {
                  if (g.value) {
                    g.clearError(i);
                  } else {
                    g.setError(i);
                  }
                }
                if (!g.isValid) {
                  if (e.owner.widgetMap.remove.element.contains(c) || f.widgetMap.deleteButton.element.contains(c) || f.widgetMap.cancelButton.element.contains(c)) {
                    return 'cancel';
                  }
                  if (c.closest('button')) {
                    a.preventDefault();
                    EventHelper.on({
                      'element': c,
                      'thisObj': e,
                      'click': k => k.stopImmediatePropagation(),
                      'capture': true,
                      'expires': 500,
                      'once': true
                    });
                  }
                }
              }
            }
          },
          'columns': {
            'data': {
              'id': {
                'localeClass': this,
                'text': 'L{ID}',
                'flex': 1,
                'editor': false,
                'htmlEncode': false,
                'hidden': true,
                'sortable'(a, b) {
                  let c, e;
                  const {
                      dependencyIdField: f,
                      direction: g
                    } = this.grid.parent, i = (c = a[g]) == null ? void 0 : c[f], j = (e = b[g]) == null ? void 0 : e[f];
                  if (i === j) {
                    return 0;
                  }
                  return i < j ? -1 : 1;
                },
                'renderer': ({
                  record: a,
                  row: b,
                  grid: c,
                  column: e
                }) => {
                  let f;
                  const {direction: g} = c.parent, i = a[g];
                  if (i && isDependencyMarkedValid(a, c)) {
                    const j = c.parent.dependencyIdField, k = i[j];
                    if (j === 'id') {
                      f = !i || i.hasGeneratedId ? '*' : i.id;
                    } else {
                      f = k;
                    }
                  } else {
                    b.addCls('b-invalid');
                    f = '<div class="b-icon b-icon-warning"></div>';
                  }
                  return f;
                }
              },
              'name': {
                'localeClass': this,
                'text': 'L{Name}',
                'flex': 5,
                'renderer': ({
                  value: a,
                  grid: b,
                  cellElement: c
                }) => {
                  if (a) {
                    c.classList.toggle('b-inactive', a.inactive);
                    const e = a[b.parent.dependencyIdField];
                    return a.name + (e != null && !a.hasGeneratedId ? ' (' + e + ')' : '');
                  }
                  return '';
                },
                'finalizeCellEdit': 'up.finalizeLinkedTaskCellEdit',
                'editor': {
                  'type': 'modelcombo',
                  'displayField': 'name',
                  'valueField': 'id',
                  'filterOperator': '*',
                  'allowInvalid': true,
                  'listItemTpl'(a) {
                    const b = this, c = b._dependencyTab || (b._dependencyTab = b.up('dependencytab', true)), {dependencyIdField: e} = c, f = a.hasGeneratedId && e === 'id' ? null : a[e];
                    return StringHelper.xss`${ a.name }${ f != null ? ' (' + f + ')' : '' }`;
                  }
                }
              },
              'type': {
                'localeClass': this,
                'text': 'L{Type}',
                'field': 'type',
                'flex': 3,
                'sortable': false,
                'editor': 'dependencytypepicker',
                'renderer'({value: a}) {
                  return this.L('L{DependencyType.long}')[a];
                }
              },
              'lag': {
                'localeClass': this,
                'text': 'L{Lag}',
                'type': 'duration',
                'field': 'fullLag',
                'flex': 2,
                'editor': { 'allowNegative': true }
              }
            }
          }
        },
        'toolbar': {
          'type': 'toolbar',
          'dock': 'bottom',
          'cls': 'b-compact-bbar',
          'items': {
            'add': {
              'type': 'button',
              'weight': 210,
              'cls': 'b-add-button b-green',
              'icon': 'b-icon b-icon-add',
              'onAction': 'up.onAddClick'
            },
            'remove': {
              'type': 'button',
              'weight': 220,
              'cls': 'b-remove-button b-red',
              'icon': 'b-icon b-icon-trash',
              'disabled': true,
              'onAction': 'up.onRemoveClick'
            }
          }
        }
      }
    };
  }
  async finalizeLinkedTaskCellEdit({
    grid: a,
    value: b,
    record: c
  }) {
    const {project: e} = a.store.masterStore, f = this.direction === 'toEvent', g = f ? c.fromEvent : b, i = f ? b : c.toEvent, j = await e.validateDependency(g, i, c.type, c);
    switch (j) {
    case DependencyValidationResult.NoError:
      return true;
    case DependencyValidationResult.CyclicDependency:
      return 'L{DependencyTab.cyclicDependency}';
    }
    return 'L{DependencyTab.invalidDependency}';
  }
  afterConstruct() {
    super.afterConstruct();
    const a = this.grid = this.widgetMap.grid;
    a.ion({
      'selectionChange': 'onGridSelectionChange',
      'startCellEdit': 'onGridStartCellEdit',
      'finishCellEdit': 'onGridFinishCellEdit',
      'cancelCellEdit': 'onGridCancelCellEdit',
      'thisObj': this
    });
  }
  get taskCombo() {
    const {grid: a} = this, b = a == null ? void 0 : a.columns.get(this.direction);
    return b == null ? void 0 : b.editor;
  }
  loadEvent(a) {
    const b = this, {
        grid: c,
        taskCombo: e,
        record: f
      } = b;
    super.loadEvent(a);
    const {
        dependencyStore: g,
        eventStore: i
      } = b.project, j = c.store.masterStore !== g, k = !j && a !== f;
    if (j) {
      b.depStoreGeneration = g.storage.generation;
      b.eventStoreGeneration = i.storage.generation;
      b.detachListeners('taskCombo');
      c.store = g.chain(n => n[b.negDirection] === b.record, null);
      const l = e.store = i.chain(n => n !== b.record, null, {
        'doRelayToMaster': [],
        'excludeCollapsedRecords': false
      });
      l.sort(b.taskComboSortField);
      l.filterBy(n => !c.store.find(o => {
        const p = o[b.direction], q = b._activeCellEdit, r = (q == null ? void 0 : q.record) && p === q.record[b.direction];
        return p === n && !r;
      }));
      e.ion({
        'name': 'taskCombo',
        'change': 'onGridCellEditChange',
        'thisObj': b
      });
    } else {
      c.store.resumeChain();
      if (k || g.storage.generation !== b.depStoreGeneration) {
        c.store.fillFromMaster();
        b.depStoreGeneration = g.storage.generation;
      } else {
        c.refreshRows();
      }
      if (k || i.storage.generation !== b.eventStoreGeneration) {
        e.store.fillFromMaster();
        b.eventStoreGeneration = i.storage.generation;
      }
    }
    if (k) {
      c.store.sort(b.sortField);
    }
    b.requestReadyStateChange();
  }
  async insertNewDependency() {
    const a = this, {grid: b} = a, {cellEdit: c} = b.features;
    if (c.isEditing && !c.editor.isValid) {
      c.cancelEditing();
    }
    const [e] = b.store.insert(0, { [a.negDirection]: a.record });
    a.depStoreGeneration = a.project.dependencyStore.storage.generation;
    await b.features.cellEdit.startEditing({
      'field': a.direction,
      'id': e.id
    });
    markDependencyInvalid(e, b);
    return e;
  }
  beforeSave() {
    this.grid.store.suspendChain();
  }
  onAddClick() {
    this.insertNewDependency();
  }
  onRemoveClick() {
    const a = this, {grid: b} = a, c = b.selectedRecords;
    b.features.cellEdit.cancelEditing(true);
    a.project.dependencyStore.remove(c);
    b.selectedRecord = b.store.getNext(c[0]);
    a.taskCombo.store.fillFromMaster();
  }
  onGridSelectionChange({selection: a}) {
    const b = this.widgetMap.remove;
    if (b) {
      const c = Boolean(!(a == null ? void 0 : a.length) || this.up(e => e.readOnly));
      if (b.containsFocus && c) {
        this.grid.focusCell({
          'rowIndex': -1,
          'columnIndex': 0
        });
      }
      b.disabled = c;
    }
  }
  clearActiveEditorErrors() {
    const a = this._activeCellEdit;
    if (a && a.column.field === this.direction) {
      a.editor.inputField.clearError();
    }
  }
  onGridCellEditChange() {
    this.clearActiveEditorErrors();
  }
  onGridStartCellEdit({editorContext: a}) {
    const b = this, c = b._editingDependency = a.record, {
        grid: e,
        direction: f
      } = b;
    b._activeCellEdit = a;
    if (a.column.field === f) {
      if (!isDependencyMarkedValid(c, e)) {
        if (!c[f]) {
          a.editor.inputField.setError('L{DependencyTab.invalidDependency}');
        } else {
          a.editor.inputField.setError('L{DependencyTab.cyclicDependency}');
        }
      } else {
        b.clearActiveEditorErrors();
      }
    }
  }
  async onGridFinishCellEdit({editorContext: a}) {
    const b = this, {
        record: c,
        column: e
      } = a, {
        grid: f,
        direction: g
      } = b;
    if (e.field === g) {
      markDependencyValid(c, f);
      b.taskCombo.store.fillFromMaster();
    } else {
      b.redrawDependencyRow(c);
    }
    b._activeCellEdit = b._editingDependency = null;
    b.requestReadyStateChange();
  }
  afterCancel() {
    if (this._activeCellEdit) {
      this.grid.features.cellEdit.cancelEditing(true);
    }
  }
  onGridCancelCellEdit() {
    const a = this, b = a._editingDependency;
    if (b) {
      markDependencyValid(b, a.grid);
      if (!b[a.direction]) {
        a.redrawDependencyRow(b);
      }
      a._activeCellEdit = a._editingDependency = null;
    }
    a.requestReadyStateChange();
  }
  redrawDependencyRow(a) {
    const {grid: b} = this, c = b.rowManager.getRowById(a);
    if (c) {
      const e = b.store.indexOf(a);
      if (e >= 0) {
        c.render(b.store.indexOf(a), a);
      }
    }
  }
  get canSave() {
    const {grid: a} = this;
    return a.store.every(b => isDependencyMarkedValid(b, a));
  }
  updateReadOnly(a) {
    const {
      add: b,
      remove: c
    } = this.widgetMap;
    super.updateReadOnly(...arguments);
    b.hidden = c.hidden = a;
  }
};
DependencyTab._$name = 'DependencyTab';
const SuccessorsTab = class extends DependencyTab {
  static get $name() {
    return 'SuccessorsTab';
  }
  static get type() {
    return 'successorstab';
  }
  static get configurable() {
    return {
      'cls': 'b-successors-tab',
      'direction': 'toEvent',
      'negDirection': 'fromEvent',
      'title': 'L{DependencyTab.Successors}',
      'sortField': 'toEventName',
      'items': { 'grid': { 'columns': { 'data': { 'name': { 'field': 'toEvent' } } } } }
    };
  }
};
SuccessorsTab.initClass();
SuccessorsTab._$name = 'SuccessorsTab';
const PredecessorsTab = class extends DependencyTab {
  static get $name() {
    return 'PredecessorsTab';
  }
  static get type() {
    return 'predecessorstab';
  }
  static get configurable() {
    return {
      'cls': 'b-predecessors-tab',
      'direction': 'fromEvent',
      'negDirection': 'toEvent',
      'title': 'L{DependencyTab.Predecessors}',
      'sortField': 'fromEventName',
      'items': { 'grid': { 'columns': { 'data': { 'name': { 'field': 'fromEvent' } } } } }
    };
  }
};
PredecessorsTab.initClass();
PredecessorsTab._$name = 'PredecessorsTab';
const ResourcesTab = class extends EditorTab {
  static get $name() {
    return 'ResourcesTab';
  }
  static get type() {
    return 'resourcestab';
  }
  static get configurable() {
    return {
      'title': 'L{Resources}',
      'cls': 'b-resources-tab',
      'layoutStyle': { 'flexFlow': 'column nowrap' },
      'showUnits': null,
      'items': {
        'grid': {
          'type': 'grid',
          'weight': 100,
          'flex': '1 1 auto',
          'columns': {
            'data': {
              'resource': {
                'localeClass': this,
                'text': 'L{Resource}',
                'field': 'resource',
                'flex': 7,
                'renderer': ({value: a}) => (a == null ? void 0 : a.name) || '',
                'editor': {
                  'type': 'modelcombo',
                  'displayField': 'name',
                  'valueField': 'id'
                }
              },
              'units': {
                'type': 'number',
                'localeClass': this,
                'text': 'L{Units}',
                'field': 'units',
                'flex': 3,
                'renderer': a => this.L('L{unitsTpl}', a),
                'min': 0,
                'max': 100,
                'step': 10
              }
            }
          },
          'disableGridRowModelWarning': true,
          'asyncEventSuffix': 'PreCommit'
        },
        'toolbar': {
          'type': 'toolbar',
          'dock': 'bottom',
          'cls': 'b-compact-bbar',
          'items': {
            'add': {
              'type': 'button',
              'weight': 210,
              'cls': 'b-add-button b-green',
              'icon': 'b-icon b-icon-add'
            },
            'remove': {
              'type': 'button',
              'weight': 220,
              'cls': 'b-remove-button b-red',
              'icon': 'b-icon b-icon-trash',
              'disabled': true
            }
          }
        }
      }
    };
  }
  afterConstruct() {
    super.afterConstruct();
    const a = this, b = a.addButton = a.widgetMap.add, c = a.removeButton = a.widgetMap.remove, e = a.grid = a.widgetMap.grid;
    b == null ? void 0 : b.ion({
      'click': 'onAddClick',
      'thisObj': a
    });
    c == null ? void 0 : c.ion({
      'click': 'onRemoveClick',
      'thisObj': a
    });
    e.ion({
      'selectionChange': 'onGridSelectionChange',
      'startCellEdit': 'onGridStartCellEdit',
      'finishCellEdit': 'onGridFinishCellEdit',
      'cancelCellEdit': 'onGridCancelCellEdit',
      'thisObj': a
    });
  }
  updateReadOnly(a) {
    const {
      add: b,
      remove: c
    } = this.widgetMap;
    super.updateReadOnly(...arguments);
    b.hidden = c.hidden = a;
  }
  get resourceCombo() {
    let a;
    const b = (a = this.grid) == null ? void 0 : a.columns.get('resource');
    return b == null ? void 0 : b.editor;
  }
  updateShowUnits(a) {
    const b = this.widgetMap.grid.columns.get('units');
    if (b) {
      b.hidden = !a;
    }
  }
  loadEvent(a) {
    const b = this, {
        resourceCombo: c,
        grid: e,
        record: f
      } = b;
    super.loadEvent(a);
    const {
        assignmentStore: g,
        resourceStore: i
      } = b.project, j = e.store.masterStore !== g, k = !j && a !== f;
    if (typeof this.showUnits !== 'boolean') {
      const l = e.columns.get('units');
      l && (l.hidden = !a.isTask);
    }
    if (j) {
      b.assignmentStoreGeneration = g.storage.generation;
      b.resourceStoreGeneration = i.storage.generation;
      e.store = g.chain(n => b.record && n.event === b.record, null);
      c.store = i.chain(n => {
        return !n.isSpecialRow && (b.record && !b.record.isAssignedTo(n)) || !b.activeAssignment || b.activeAssignment.resource === n;
      }, null, { 'groupers': i.groupers });
    } else {
      e.store.resumeChain();
      if (k || g.storage.generation !== b.assignmentStoreGeneration) {
        e.store.fillFromMaster();
      }
      if (k || i.storage.generation !== b.resourceStoreGeneration) {
        c.store.fillFromMaster();
      }
    }
  }
  get activeAssignment() {
    return this.grid.features.cellEdit.activeRecord;
  }
  async insertNewAssignment() {
    const a = this, {
        project: b,
        grid: c
      } = a, e = b.assignmentStore;
    const [f] = e.insert(0, {
      'event': a.record,
      'resource': null,
      'units': 100
    });
    a.assignmentStoreGeneration = e.storage.generation;
    await c.features.cellEdit.startEditing({
      'field': 'resource',
      'id': f.id
    });
    return f;
  }
  beforeSave() {
    this.pruneInvalidAssignments();
    this.grid.store.suspendChain();
  }
  onAddClick() {
    this.insertNewAssignment();
  }
  onRemoveClick() {
    const a = this, {grid: b} = a;
    b.store.remove(b.selectedRecords);
    b.selectedRecords = null;
    a.removeButton.disable();
  }
  onGridSelectionChange({selection: a}) {
    const {removeButton: b} = this;
    if (b) {
      const c = !(a == null ? void 0 : a.length) || this.up(e => e.readOnly);
      b.disabled = c;
    }
  }
  onGridStartCellEdit({editorContext: a}) {
    let b, c;
    if (a.column.field === 'resource') {
      this.resourceCombo.store.fillFromMaster();
      this._editingAssignment = a.record;
      this._activeCellEdit = a;
    }
    if (a.column.field === 'units') {
      a.editor.inputField.max = (c = (b = a.record.resource) == null ? void 0 : b.maxUnits) != null ? c : 100;
    }
  }
  onGridFinishCellEdit() {
    this._activeCellEdit = this._editingAssignment = null;
  }
  onGridCancelCellEdit() {
    this._activeCellEdit = this._editingAssignment = null;
  }
  pruneInvalidAssignments() {
    const {store: a} = this.grid;
    a.remove(a.query(b => !b.isValid));
  }
};
ResourcesTab.initClass();
ResourcesTab._$name = 'ResourcesTab';
const alapIds = {
  'assoonaspossible': 1,
  'aslateaspossible': 1
};
const _ConstraintTypePicker = class _ConstraintTypePicker extends Combo {
  static configurable = {
    'valueField': 'id',
    'store': {
      'data': [
        { 'id': 'none' },
        { 'id': 'assoonaspossible' },
        { 'id': 'aslateaspossible' },
        { 'id': 'muststarton' },
        { 'id': 'mustfinishon' },
        { 'id': 'startnoearlierthan' },
        { 'id': 'startnolaterthan' },
        { 'id': 'finishnoearlierthan' },
        { 'id': 'finishnolaterthan' }
      ]
    },
    'primaryFilter'(a) {
      return _ConstraintTypePicker.localize(a.id).toLowerCase().startsWith(this.value.toLowerCase());
    },
    'includeAsapAlapAsConstraints': true,
    'nullValue': 'none'
  };
  static type = 'constrainttypepicker';
  static $name = 'ConstraintTypePicker';
  loadEvent(a) {
    this.taskRecord = a;
    this.store.filter();
  }
  set value(a) {
    super.value = a;
  }
  get value() {
    const a = super.value;
    return a === 'none' ? null : a;
  }
  get inputValue() {
    let a;
    return this.L(((a = this.selected) == null ? void 0 : a.id) || this.nullValue);
  }
  listItemTpl(a) {
    return _ConstraintTypePicker.localize(a.id);
  }
  changeStore(a) {
    return super.changeStore({
      ...a,
      'filters': {
        'filterBy': b => {
          let c, e;
          return alapIds[b.id] ? this._includeAsapAlapAsConstraints : (e = ((c = this.taskRecord) == null ? void 0 : c.recurringEvent) || this.taskRecord) == null ? void 0 : e.run('isConstraintTypeApplicable', b.id);
        }
      }
    });
  }
  updateIncludeAsapAlapAsConstraints() {
    this.store.filter();
  }
};
const ConstraintTypePicker = _ConstraintTypePicker;
ConstraintTypePicker.initClass();
ConstraintTypePicker._$name = 'ConstraintTypePicker';
const SchedulingDirectionPicker = class _SchedulingDirectionPicker extends Combo {
  static get $name() {
    return 'SchedulingDirectionPicker';
  }
  static get type() {
    return 'schedulingdirectionpicker';
  }
  static get configurable() {
    return {
      'store': {
        'data': [
          { 'id': 'Forward' },
          { 'id': 'Backward' }
        ]
      },
      'primaryFilter'(a) {
        return _SchedulingDirectionPicker.localize(a.id).toLowerCase().startsWith(this.value.toLowerCase());
      },
      'hasEnforcedBadge': false,
      'clearable': false,
      'nullValue': 'Forward'
    };
  }
  get inputValue() {
    return this.L(this.value || '');
  }
  listItemTpl(a) {
    return _SchedulingDirectionPicker.localize(a.id);
  }
  changeHasEnforcedBadge(a) {
    this.element.classList.toggle('b-enforced-sch-direction', Boolean(a));
  }
  getEnforcedName(a) {
    return a.name || (a.isRoot ? 'Project' : 'Task #' + a.id);
  }
  assignFieldValue(a) {
    const b = this, c = a == null ? void 0 : a.effectiveDirection;
    b.taskRecord = a;
    if (c) {
      b.value = c.direction;
      if (c.kind === 'enforced') {
        b.hasEnforcedBadge = true;
        b.tooltip = b.L('L{enforcedBy}') + (' "' + this.getEnforcedName(c.enforcedBy) + '"');
        b.disable();
      } else if (c.kind === 'inherited') {
        b.hasEnforcedBadge = true;
        b.tooltip = b.L('L{inheritedFrom}') + (' "' + this.getEnforcedName(c.inheritedFrom) + '"');
        b.enable();
      } else {
        b.hasEnforcedBadge = false;
        b.tooltip = void 0;
        b.enable();
      }
    } else {
      b.clear();
      b.disable();
    }
  }
};
SchedulingDirectionPicker.initClass();
SchedulingDirectionPicker._$name = 'SchedulingDirectionPicker';
const SchedulingModePicker = class extends Combo {
  static get $name() {
    return 'SchedulingModePicker';
  }
  static get type() {
    return 'schedulingmodecombo';
  }
  static get configurable() {
    return {
      'allowedModes': null,
      'store': { 'data': this.defaultStoreData }
    };
  }
  static get defaultStoreData() {
    return [
      {
        'id': SchedulingMode.Normal,
        'text': this.L('L{Normal}')
      },
      {
        'id': SchedulingMode.FixedDuration,
        'text': this.L('L{Fixed Duration}')
      },
      {
        'id': SchedulingMode.FixedUnits,
        'text': this.L('L{Fixed Units}')
      },
      {
        'id': SchedulingMode.FixedEffort,
        'text': this.L('L{Fixed Effort}')
      }
    ];
  }
  changeAllowedModes(a) {
    if (typeof a === 'string') {
      return a.split(',');
    }
    return a;
  }
  updateAllowedModes(a) {
    this._allowedModes = a;
    if (a) {
      this.store.addFilter({
        'id': 'allowed-mode-filter',
        'filterBy': b => this._allowedModes.includes(b.id)
      });
    } else {
      this.store.removeFilter('allowed-mode-filter');
    }
  }
  updateLocalization() {
    super.updateLocalization();
    this.store.data = this.constructor.defaultStoreData;
  }
};
SchedulingModePicker.initClass();
SchedulingModePicker._$name = 'SchedulingModePicker';
const AdvancedTab = class extends FormTab {
  static get $name() {
    return 'AdvancedTab';
  }
  static get type() {
    return 'advancedtab';
  }
  static get defaultConfig() {
    const a = {
        'flex': '0 0 calc(64% - var(--autocontainer-gap) / 2)',
        'labelWidth': '32.5%'
      }, b = {
        'flex': '0 0 calc(34% - var(--autocontainer-gap) / 2)',
        'labelWidth': '80%',
        'style': 'padding-right: .5em'
      };
    return {
      'localeClass': this,
      'title': 'L{Advanced}',
      'cls': 'b-advanced-tab',
      'defaults': { 'localeClass': this },
      'items': {
        'calendarField': {
          'type': 'calendarfield',
          'weight': 100,
          'ref': '',
          'name': 'calendar',
          'label': 'L{Calendar}',
          'internalListeners': { 'beforeEditCalendar': 'up.onBeforeEditCalendar' },
          ...a
        },
        'ignoreResourceCalendarField': {
          'type': 'checkbox',
          'weight': 100,
          'name': 'ignoreResourceCalendar',
          'label': 'L{Ignore resource calendar}',
          'cls': 'b-ignore-resource-calendar',
          ...b
        },
        'schedulingModeField': {
          'type': 'schedulingmodecombo',
          'weight': 300,
          'name': 'schedulingMode',
          'label': 'L{Scheduling mode}',
          ...a
        },
        'effortDrivenField': {
          'type': 'checkbox',
          'weight': 400,
          'name': 'effortDriven',
          'label': 'L{Effort driven}',
          ...b
        },
        'divider': {
          'weight': 500,
          'html': '',
          'dataset': { 'text': this.L('L{Constraint}') },
          'cls': 'b-divider',
          'flex': '1 0 100%',
          'style': 'padding-right: .5em'
        },
        'constraintTypeField': {
          'type': 'constrainttypepicker',
          'weight': 600,
          'name': 'constraintType',
          'label': 'L{Constraint type}',
          'clearable': true,
          ...a
        },
        'rollupField': {
          'type': 'checkbox',
          'weight': 700,
          'name': 'rollup',
          'label': 'L{Rollup}',
          ...b
        },
        'constraintDateField': {
          'type': 'date',
          'weight': 800,
          'name': 'constraintDate',
          'label': 'L{Constraint date}',
          'keepTime': 'entered',
          ...a
        },
        'schedulingDirectionField': {
          'type': 'schedulingdirectionpicker',
          'weight': 850,
          'name': 'direction',
          'label': 'L{Scheduling direction}',
          ...a
        },
        'inactiveField': {
          'type': 'checkbox',
          'weight': 900,
          'name': 'inactive',
          'label': 'L{Inactive}',
          ...b
        },
        'projectConstraintResolutionField': {
          'type': 'combo',
          'weight': 1000,
          'name': 'projectConstraintResolution',
          'label': 'L{projectBorder}',
          'localizeDisplayFields': true,
          'items': [
            {
              'value': 'ignore',
              'text': 'L{ignore}'
            },
            {
              'value': 'honor',
              'text': 'L{honor}'
            },
            {
              'value': 'conflict',
              'text': 'L{askUser}'
            }
          ],
          ...a
        },
        'manuallyScheduledField': {
          'type': 'checkbox',
          'weight': 1100,
          'name': 'manuallyScheduled',
          'label': 'L{Manually scheduled}',
          'cls': 'b-last-row',
          'style': 'margin-inline-start:auto',
          ...b
        }
      }
    };
  }
  get calendarField() {
    return this.widgetMap.calendarField;
  }
  get constraintTypeField() {
    return this.widgetMap.constraintTypeField;
  }
  get constraintDateField() {
    return this.widgetMap.constraintDateField;
  }
  get effortDrivenField() {
    return this.widgetMap.effortDrivenField;
  }
  get manuallyScheduledField() {
    return this.widgetMap.manuallyScheduledField;
  }
  get rollupField() {
    return this.widgetMap.rollupField;
  }
  get schedulingModeField() {
    return this.widgetMap.schedulingModeField;
  }
  get schedulingDirectionField() {
    return this.widgetMap.schedulingDirectionField;
  }
  onBeforeEditCalendar({
    calendarEditor: a,
    calendar: b
  }) {
    const {record: c} = this;
    a.activeDate = c.startDate;
    if (!b.isCalendarModel) {
      b.name = c.name + ' (calendar)';
    }
  }
  loadEvent(a) {
    let b;
    const c = this, {
        calendarField: e,
        constraintTypeField: f,
        includeAsapAlapAsConstraints: g,
        schedulingDirectionField: i
      } = c, {calendarManagerStore: j} = a.project, k = ((b = e == null ? void 0 : e.store) == null ? void 0 : b.masterStore) !== j;
    if (e) {
      if (k) {
        e.store = j.chain(void 0, void 0, { 'excludeCollapsedRecords': false });
      } else {
        e.store.resumeChain();
      }
      e.calendarConsumerRecord = a;
    }
    if (f) {
      f.taskRecord = a;
      f.includeAsapAlapAsConstraints = g;
    }
    if (i) {
      i.hidden = g;
    }
    super.loadEvent(a);
  }
  beforeSave() {
    const {calendarField: a} = this.widgetMap;
    if (a) {
      a.store.suspendChain();
    }
  }
};
AdvancedTab.initClass();
AdvancedTab._$name = 'AdvancedTab';
const NotesTab = class extends FormTab {
  static get $name() {
    return 'NotesTab';
  }
  static get type() {
    return 'notestab';
  }
  static get configurable() {
    return {
      'cls': 'b-notes-tab',
      'title': this.L('L{Notes}'),
      'tab': {
        'icon': 'b-icon-note',
        'titleProperty': 'tooltip'
      },
      'layoutConfig': {
        'alignItems': 'flex-start',
        'alignContent': 'stretch'
      },
      'items': {
        'noteField': {
          'weight': 100,
          'type': 'textfield',
          'inputAttributes': { 'tag': 'textarea' },
          'cls': 'b-taskeditor-notes-field',
          'name': 'note'
        }
      }
    };
  }
};
NotesTab.initClass();
NotesTab._$name = 'NotesTab';
const GanttTaskEditor = class extends TaskEditorBase {
  static configurable = {
    'items': {
      'tabs': {
        'type': 'tabpanel',
        'defaultType': 'formtab',
        'flex': '1 0 100%',
        'autoHeight': true,
        'layoutConfig': {
          'alignItems': 'stretch',
          'alignContent': 'stretch'
        },
        'items': {
          'generalTab': {
            'type': 'generaltab',
            'weight': 100
          },
          'predecessorsTab': {
            'type': 'predecessorstab',
            'weight': 200
          },
          'successorsTab': {
            'type': 'successorstab',
            'weight': 300
          },
          'resourcesTab': {
            'type': 'resourcestab',
            'weight': 400
          },
          'advancedTab': {
            'type': 'advancedtab',
            'weight': 500
          },
          'notesTab': {
            'type': 'notestab',
            'weight': 600
          }
        }
      }
    }
  };
  static get type() {
    return 'gantttaskeditor';
  }
  static get $name() {
    return 'GanttTaskEditor';
  }
};
GanttTaskEditor.initClass();
GanttTaskEditor._$name = 'GanttTaskEditor';
const SchedulerGeneralTab = class extends FormTab {
  static get $name() {
    return 'SchedulerGeneralTab';
  }
  static get type() {
    return 'schedulergeneraltab';
  }
  static get defaultConfig() {
    return {
      'title': 'L{General}',
      'cls': 'b-general-tab',
      'defaults': {
        'localeClass': this,
        'weight': 10
      },
      'items': {
        'nameField': {
          'type': 'text',
          'required': true,
          'label': 'L{Name}',
          'clearable': true,
          'name': 'name',
          'cls': 'b-name',
          'weight': 100
        },
        'resourcesField': {
          'type': 'combo',
          'label': 'L{Resources}',
          'name': 'resources',
          'editable': true,
          'valueField': 'id',
          'displayField': 'name',
          'highlightExternalChange': false,
          'cls': 'b-resources',
          'weight': 200
        },
        'startDateField': {
          'type': 'datetime',
          'label': 'L{Start}',
          'name': 'startDate',
          'cls': 'b-start-date',
          'flex': '1 0 100%',
          'weight': 300,
          'dateField': { 'type': 'startdatefield' }
        },
        'endDateField': {
          'type': 'datetime',
          'label': 'L{Finish}',
          'name': 'endDate',
          'cls': 'b-end-date',
          'flex': '1 0 100%',
          'weight': 400,
          'dateField': { 'type': 'enddatefield' }
        },
        'durationField': {
          'type': 'durationfield',
          'label': 'L{Duration}',
          'name': 'fullDuration',
          'cls': 'b-half-width',
          'min': 0,
          'weight': 500
        },
        'percentDoneField': {
          'type': 'number',
          'label': 'L{% complete}',
          'name': 'renderedPercentDone',
          'cls': 'b-half-width',
          'min': 0,
          'max': 100,
          'weight': 600
        },
        'effortField': {
          'type': 'durationfield',
          'label': 'L{Effort}',
          'name': 'fullEffort',
          'weight': 620
        },
        'preambleField': {
          'type': 'durationfield',
          'hidden': true,
          'useAbbreviation': true,
          'weight': 650,
          'label': 'L{SchedulerGeneralTab.Preamble}',
          'name': 'preamble',
          'cls': 'b-half-width'
        },
        'postambleField': {
          'type': 'durationfield',
          'hidden': true,
          'useAbbreviation': true,
          'weight': 660,
          'label': 'L{SchedulerGeneralTab.Postamble}',
          'name': 'postamble',
          'cls': 'b-half-width'
        },
        'colorField': {
          'type': 'eventcolorfield',
          'weight': 700,
          'label': 'L{SchedulerBase.color}',
          'name': 'eventColor'
        }
      }
    };
  }
  loadEvent(a) {
    let b;
    const c = this, {project: e} = c, f = {
        'unit': a.durationUnit,
        'magnitude': 1
      }, {
        startDateField: g,
        endDateField: i,
        resourcesField: j
      } = c.widgetMap, k = (j == null ? void 0 : j.store.masterStore) !== e.resourceStore;
    if (g) {
      g.dateField.eventRecord = a;
      if (DateHelper.compareUnits(f.unit, 'hour') > 0) {
        g.dateField.step = f;
      } else {
        g.timeField.step = f;
      }
    }
    if (i) {
      i.dateField.eventRecord = a;
      if (DateHelper.compareUnits(f.unit, 'hour') > 0) {
        i.dateField.step = f;
      } else {
        i.timeField.step = f;
      }
    }
    if (j) {
      j.multiSelect = (b = j.config.multiSelect) != null ? b : !e.eventStore.usesSingleAssignment;
      if (k) {
        j.store = e.resourceStore.chain(l => !l.isSpecialRow);
      } else {
        j.store.resumeChain();
      }
    }
    super.loadEvent(...arguments);
  }
  onFieldChange({
    source: a,
    valid: b,
    userAction: c,
    value: e
  }) {
    if (c && b) {
      const {eventStore: f} = this.record, g = a.name === 'resources' && e.length === 0 && this.autoUpdateRecord && f.removeUnassignedEvent;
      if (g) {
        f.removeUnassignedEvent = false;
      }
      super.onFieldChange(...arguments);
      if (g) {
        f.removeUnassignedEvent = true;
      }
    }
  }
  beforeSave() {
    let a;
    if (this.record.resources.length === 0 && this.record.eventStore.removeUnassignedEvent) {
      this.record.remove();
    }
    (a = this.widgetMap.resourcesField) == null ? void 0 : a.store.suspendChain();
    super.beforeSave();
  }
};
SchedulerGeneralTab.initClass();
SchedulerGeneralTab._$name = 'SchedulerGeneralTab';
const RecurrenceTab = class extends EditorTab {
  static configurable = {
    'title': 'L{title}',
    'cls': 'b-recurrence-tab',
    'scrollable': true,
    'items': {
      'recurrenceEditor': {
        'type': 'recurrenceeditorpanel',
        'addNone': true,
        'items': { 'monthDaysNumberRadioField': false }
      }
    }
  };
  static type = 'recurrencetab';
  static $name = 'RecurrenceTab';
  get recurrenceEditor() {
    return this.widgetMap.recurrenceEditor;
  }
  loadEvent(a) {
    let b;
    if (this.recurrenceEditor) {
      this.loadingRecord = true;
      this.recurrenceEditor.record = (b = a.recurrence) != null ? b : null;
      this.loadingRecord = false;
    }
    super.loadEvent(a);
  }
  onFieldChange({
    source: a,
    value: b
  }) {
    const c = this;
    if (!c.isConfiguring && !c.recurrenceEditor.assigningValues && !c.loadingRecord) {
      const {record: e} = c;
      if (!e.recurrence) {
        e.recurrence = new e.recurrenceModel({});
      } else {
        if (a === c.recurrenceEditor.widgetMap.frequencyField && b === 'NONE') {
          e.recurrence = null;
        } else {
          c.recurrenceEditor.syncEventRecord(e.recurrence);
        }
      }
      c.up(f => f.isTaskEditorBase).editingRecurring = true;
    }
  }
};
RecurrenceTab.initClass();
RecurrenceTab._$name = 'RecurrenceTab';
const SchedulerAdvancedTab = class extends FormTab {
  static get $name() {
    return 'SchedulerAdvancedTab';
  }
  static get type() {
    return 'scheduleradvancedtab';
  }
  static get configurable() {
    return {
      'cls': 'b-advanced-tab',
      'tab': {
        'icon': 'b-icon-advanced',
        'tooltip': 'L{SchedulerAdvancedTab.Advanced}'
      },
      'defaults': { 'localeClass': this },
      'items': {
        'calendarField': {
          'type': 'calendarfield',
          'name': 'calendar',
          'label': 'L{Calendar}',
          'weight': 100
        },
        'constraintTypeField': {
          'type': 'constrainttypepicker',
          'name': 'constraintType',
          'label': 'L{Constraint type}',
          'clearable': true,
          'weight': 200,
          'includeAsapAlapAsConstraints': false
        },
        'constraintDateField': {
          'type': 'date',
          'name': 'constraintDate',
          'label': 'L{Constraint date}',
          'keepTime': 'entered',
          'weight': 300
        },
        'manuallyScheduledField': {
          'type': 'checkbox',
          'name': 'manuallyScheduled',
          'label': 'L{Manually scheduled}',
          'weight': 400
        },
        'schedulingModeField': {
          'type': 'schedulingmodecombo',
          'name': 'schedulingMode',
          'label': 'L{Scheduling mode}',
          'hidden': true,
          'weight': 450,
          'allowedModes': SchedulingMode.Normal + ',' + SchedulingMode.FixedDuration
        },
        'effortDrivenField': {
          'type': 'checkbox',
          'weight': 460,
          'name': 'effortDriven',
          'label': 'L{Effort driven}',
          'hidden': true,
          'cls': 'b-half-width'
        },
        'inactiveField': {
          'type': 'checkbox',
          'weight': 500,
          'name': 'inactive',
          'label': 'L{Inactive}'
        },
        'ignoreResourceCalendarField': {
          'type': 'checkbox',
          'weight': 600,
          'name': 'ignoreResourceCalendar',
          'label': 'L{Ignore resource calendar}',
          'cls': 'b-half-width'
        }
      }
    };
  }
  get calendarField() {
    return this.widgetMap.calendarField;
  }
  get constraintTypeField() {
    return this.widgetMap.constraintTypeField;
  }
  get constraintDateField() {
    return this.widgetMap.constraintDateField;
  }
  get effortDrivenField() {
    return this.widgetMap.effortDrivenField;
  }
  get manuallyScheduledField() {
    return this.widgetMap.manuallyScheduledField;
  }
  get schedulingModeField() {
    return this.widgetMap.schedulingModeField;
  }
  loadEvent(a) {
    const b = this, {project: c} = b, e = !b.record, {
        calendarField: f,
        constraintTypeField: g
      } = b;
    if (g) {
      g.taskRecord = a;
    }
    if (f) {
      f.store = c.calendarManagerStore;
      f.hidden = !c.calendarManagerStore.count;
      f.calendarConsumerRecord = a;
    }
    super.loadEvent(...arguments);
  }
};
SchedulerAdvancedTab.initClass();
SchedulerAdvancedTab._$name = 'SchedulerAdvancedTab';
const bufferRe = /(pre|post)amble/;
const SchedulerTaskEditor = class extends TaskEditorBase {
  static get type() {
    return 'schedulertaskeditor';
  }
  static get $name() {
    return 'SchedulerTaskEditor';
  }
  static get defaultConfig() {
    return {
      'enableEventSpanBuffer': false,
      'items': [{
          'type': 'tabpanel',
          'defaultType': 'formtab',
          'ref': 'tabs',
          'flex': '1 0 100%',
          'autoHeight': true,
          'layoutConfig': {
            'alignItems': 'stretch',
            'alignContent': 'stretch'
          },
          'defaults': { 'scrollable': { 'overflowY': true } },
          'items': {
            'generalTab': {
              'type': 'schedulergeneraltab',
              'weight': 100
            },
            'recurrenceTab': {
              'type': 'recurrencetab',
              'weight': 150
            },
            'predecessorsTab': {
              'type': 'predecessorstab',
              'weight': 200
            },
            'successorsTab': {
              'type': 'successorstab',
              'weight': 300
            },
            'advancedTab': {
              'type': 'scheduleradvancedtab',
              'weight': 500
            },
            'notesTab': {
              'type': 'notestab',
              'weight': 600
            }
          }
        }]
    };
  }
  processWidgetConfig(a) {
    let b, c;
    if ((b = a.ref) == null ? void 0 : b.match(bufferRe)) {
      a.hidden = !this.enableEventSpanBuffer;
    }
    if (a.ref === 'recurrenceTab') {
      a.hidden = !((c = this.owner) == null ? void 0 : c.enableRecurringEvents);
    }
    return super.processWidgetConfig(a);
  }
  onFocusOut({relatedTarget: a}) {
    let b;
    const c = (b = a == null ? void 0 : a.closest('.b-sch-event-wrap')) == null ? void 0 : b.elementData.eventRecord;
    if (c && c === this.loadedRecord) {
      this.setTimeout(() => this.focus(), 100);
    } else {
      return super.onFocusOut(...arguments);
    }
  }
};
SchedulerTaskEditor.initClass();
SchedulerTaskEditor._$name = 'SchedulerTaskEditor';
const TaskEdit = class extends InstancePlugin.mixin(Delayable_default, TaskEditStm_default, TransactionalFeature_default, TaskEditTransactional_default) {
  static defaultConfig = {
    'triggerEvent': 'eventdblclick',
    'editorClassMap': {
      [ProjectType.SchedulerBasic]: 'schedulertaskeditor',
      [ProjectType.SchedulerPro]: 'schedulertaskeditor',
      [ProjectType.Gantt]: 'gantttaskeditor'
    },
    'editorClass': null,
    'editorConfig': null,
    'confirmDelete': true,
    'saveAndCloseOnEnter': true,
    'blurAction': 'cancel',
    'weekStartDay': null,
    'scrollIntoView': true,
    'minEditSize': 100
  };
  static $name = 'TaskEdit';
  static get pluginConfig() {
    return {
      'chain': [
        'populateEventMenu',
        'onEventEnterKey'
      ],
      'assign': ['editEvent']
    };
  }
  static get configurable() {
    return {
      'items': null,
      'suspendHasChangesEvent': false
    };
  }
  construct(a, b) {
    let c;
    a.taskEdit = this;
    super.construct(a, ObjectHelper.assign({
      'weekStartDay': a.weekStartDay,
      'enableEventSpanBuffer': (c = a.features.eventBuffer) == null ? void 0 : c.enabled
    }, b));
    a.ion({
      [this.triggerEvent]: 'onActivateEditor',
      'readOnly': 'onClientReadOnlyToggle',
      'dragCreateEnd': 'onDragCreateEnd',
      'afterEventDrop': 'onEventDropped',
      'eventResizeEnd': 'onEventResized',
      'thisObj': this
    });
  }
  doDestroy() {
    let a;
    this.cleanupProjectListener();
    (a = this.editor) == null ? void 0 : a.destroy();
    if (this.deleteConfirmationPromise) {
      MessageDialog_default.hide();
    }
    super.doDestroy();
  }
  onClientReadOnlyToggle({readOnly: a}) {
    if (this.editor) {
      this.editor.readOnly = a;
    }
  }
  get scheduler() {
    return this.client;
  }
  getElementFromTaskRecord(a, b) {
    return this.client.getElementFromEventRecord(a, b);
  }
  scrollEventIntoView(a, b, c) {
    this.client.scrollResourceEventIntoView(b, a, c);
  }
  get isValid() {
    return this.editor.eachWidget(a => {
      if (a.isValid === true || a.hidden || a.disabled || a.isField && !a.name) {
        return true;
      }
      return a.isValid !== false;
    }, true);
  }
  get project() {
    return this.scheduler.project;
  }
  setupProjectListener() {
    this.cleanupProjectListener();
    this.project.ion({
      'name': 'project',
      'loadstart': () => this.save(),
      'dataReady': 'onDataReady',
      'thisObj': this
    });
  }
  cleanupProjectListener() {
    this.detachListeners('project');
  }
  onDataReady() {
    let {record: a} = this;
    if (a) {
      a = this.scheduler.taskStore.getById(a.id);
      if (a) {
        this.load(a, true);
      }
    }
    if (!a) {
      this.editor.close();
    }
  }
  get isEditing() {
    return !!this._editing;
  }
  onActivateEditor({
    eventRecord: a,
    resourceRecord: b,
    eventElement: c
  }) {
    if (a === this.record && this.isEditing) {
      return;
    }
    this.editEvent(a, b, c);
  }
  async editEvent(a, b = null, c = null, e = null) {
    let f, g;
    const i = this, {
        scheduler: j,
        suspendHasChangesEvent: k
      } = i, {project: l} = j, n = async () => {
        l.resumeAutoSync();
        if (!i._cancelling) {
          await i.freeStm(true);
        }
        if (i.isDestroyed) {
          return;
        }
        if (k && !l.isDestroying) {
          l.resumeChangeTracking();
        }
        i._editing = false;
      };
    a = a.isEventSegment ? a.event : a;
    const o = !i.disabled && !a.readOnly && !l.isDelayingCalculation && (a.project || !j.usesDisplayStore);
    if (o && e) {
      e.transferred = true;
    }
    if (i.isEditing) {
      await i.cancel();
    }
    if ((f = j.features.cellEdit) == null ? void 0 : f.isEditing) {
      j.cancelEditing();
    }
    await Promise.all([
      i._cancelling,
      i._hiding
    ]).catch(() => {
    });
    if (!j.isGanttBase && !b) {
      b = a.resource || ((g = a.resources) == null ? void 0 : g[0]);
    }
    if (o) {
      const {taskStore: p} = j;
      i._editing = true;
      if (e) {
        i.applyStmCapture(e);
        i.hasStmCapture = true;
        i.stmCapture = e;
      } else if (!i.hasStmCapture) {
        await i.captureStm(true);
      }
      l.suspendAutoSync();
      if (typeof a === 'function') {
        a = a();
      }
      a = a.$original;
      if (!a.isOccurrence && !p.includes(a) && !a.isCreating) {
        a.isCreating = true;
        p.add(a);
        if (b) {
          j.assignmentStore.assignEventToResource(a, b);
        }
        await l.commitAsync();
        if (i.isDestroyed) {
          return;
        }
      }
      if (i.scrollIntoView && !j.timeAxisSubGrid.collapsed && !c && p.includes(a) && (b || j.isGantt)) {
        const u = j.isVertical ? 'maxHeight' : 'maxWidth';
        await i.scrollEventIntoView(a, b, {
          [u]: i.minEditSize,
          'animate': true
        });
        if (i.isDestroyed) {
          return;
        }
      }
      const q = c || DomHelper.down(i.getElementFromTaskRecord(a, b), j.eventInnerSelector), r = i.getEditor(a);
      if (await i.triggerOnClient('beforeTaskEdit', {
          'taskEdit': i,
          'taskRecord': a,
          'resourceRecord': b,
          'taskElement': q
        }) !== false) {
        if (i.suspendHasChangesEvent) {
          j.project.suspendChangeTracking();
        }
        j.element.classList.add('b-taskeditor-editing');
        await l.commitAsync();
        if (!a.isOccurrence) {
          if (!j.eventStore.includes(a)) {
            await n();
            return;
          } else {
            a = j.eventStore.getById(a.id);
          }
        }
        if (i.isDestroyed) {
          return;
        }
        i.load(a);
        i.triggerOnClient('beforeTaskEditShow', {
          'taskEdit': i,
          'taskRecord': a,
          'taskElement': q,
          'editor': r
        });
        const {widgetMap: v} = r, w = a.parent && !a.parent.isRoot;
        if (v.deleteButton) {
          v.deleteButton.hidden = j.readOnly || a.isCreating || r.readOnly;
        }
        if (j.features.nestedEvents) {
          if (v.predecessorsTab) {
            v.predecessorsTab.disabled = w;
          }
          if (v.successorsTab) {
            v.successorsTab.disabled = w;
          }
          if (v.resourcesField) {
            v.resourcesField.disabled = w;
          }
        }
        i.setupProjectListener();
        if (r.centered) {
          await r.show();
        } else {
          if (!j.timeAxisSubGrid.collapsed && q && (!j.isGanttBase || i.scrollIntoView || Rectangle.from(q).intersect(Rectangle.from(j.timeAxisSubGridElement)))) {
            await r.showBy({
              'target': q,
              'anchor': true,
              'allowTargetOut': true,
              'offset': -5
            });
          } else {
            await r.showBy({
              'target': j.element,
              'anchor': false,
              'align': 'c-c'
            });
          }
        }
      } else {
        await n();
      }
    }
  }
  prepareEditorConfig(a) {
    return a;
  }
  getEditor(a = this.record) {
    let b, c;
    const e = this, {client: f} = e;
    let {editor: g} = e;
    if (!g) {
      const i = e.prepareEditorConfig(ObjectHelper.merge({
        'eventEditFeature': e,
        'weekStartDay': e.weekStartDay,
        'enableEventSpanBuffer': e.enableEventSpanBuffer,
        'saveAndCloseOnEnter': e.saveAndCloseOnEnter,
        'blurAction': e.blurAction,
        'owner': f,
        'dependencyIdField': ((b = e.editorConfig) == null ? void 0 : b.dependencyIdField) || f.dependencyIdField,
        'project': e.project,
        'durationDisplayPrecision': f.durationDisplayPrecision,
        'tabPanelItems': e.items,
        'internalListeners': {
          'beforeHide': {
            'fn': 'onBeforeHide',
            'prio': -1000
          },
          'cancel': 'onCancel',
          'delete': 'onDelete',
          'save': 'onSave',
          'thisObj': e
        },
        'tabsConfig': e.tabsConfig
      }, e.editorConfig));
      if (!f.showTaskColorPickers && ((c = i.tabPanelItems) == null ? void 0 : c.generalTab) !== false) {
        i.tabPanelItems = ObjectHelper.merge(i.tabPanelItems || {}, { 'generalTab': { 'items': { 'colorField': false } } });
      }
      if (e.editorClass && !i.type) {
        g = e.editor = e.editorClass.new(i);
      } else {
        const j = (a == null ? void 0 : a.project) || e.project, k = j.getType();
        g = e.editor = Widget.create(Object.assign({ 'type': e.editorClassMap[k] || 'schedulertaskeditor' }, i));
      }
    }
    g.readOnly = (f.readOnly || (a == null ? void 0 : a.isOccurrence) || (a == null ? void 0 : a.isRecurring)) && !g.editingRecurring;
    g.project = e.project;
    return g;
  }
  load(a, b) {
    const c = this, e = c.getEditor(a);
    c._loading = true;
    if (a.isEventSegment) {
      a = a.event;
    }
    c.record = a;
    e.loadEvent(a, b);
    c._loading = false;
  }
  async save() {
    const a = this, {
        scheduler: b,
        record: c
      } = a;
    if (a.isEditing) {
      const e = a.getEditor();
      if (!a.isValid || await a.triggerOnClient('beforeTaskSave', {
          'taskRecord': c,
          'editor': e
        }) === false) {
        return;
      }
      a.cleanupProjectListener();
      e.beforeSave();
      c.isCreating = false;
      e.resetRecurrenceData = e.editingRecurring = null;
      let f = true;
      const g = a.freeStm(true);
      if (ObjectHelper.isPromise(g)) {
        g.then(() => f = false);
      }
      a._editing = false;
      await e.close();
      if (ObjectHelper.isPromise(g)) {
        await g;
        if (a.isDestroying) {
          return;
        }
      }
      b.project.resumeAutoSync(true);
      b.element.classList.remove('b-taskeditor-editing');
      a.triggerOnClient('afterTaskSave', {
        'taskRecord': c,
        'editor': e
      });
      e.afterSave();
      if (a.suspendHasChangesEvent) {
        b.project.resumeChangeTracking(f);
      }
      a.triggerOnClient('afterTaskEdit', {
        'taskRecord': c,
        'editor': e
      });
    }
  }
  async doCancel() {
    let a, b, c, e;
    const f = this, {
        scheduler: g,
        record: i
      } = f;
    if (f.isEditing) {
      f._editing = false;
      f.cleanupProjectListener();
      const {project: j} = f, k = f.getEditor();
      k.editingRecurring = null;
      k.beforeCancel();
      if (f.isDestroyed) {
        (a = j.resumeAutoSync) == null ? void 0 : a.call(j, false);
        return;
      }
      await f.rejectStmTransaction();
      if (i == null ? void 0 : i.isCreating) {
        i.remove();
      }
      if (f.isDestroyed) {
        (b = j.resumeAutoSync) == null ? void 0 : b.call(j, false);
        return;
      }
      await j.commitAsync();
      if (f.isDestroyed) {
        (c = j.resumeAutoSync) == null ? void 0 : c.call(j, false);
        return;
      }
      f.freeStm();
      (e = f.afterCancel) == null ? void 0 : e.call(f);
      k.afterCancel();
      j.resumeAutoSync(false);
      g.element.classList.remove('b-taskeditor-editing');
      f.trigger('afterTaskEdit');
      f.triggerOnClient('taskEditCanceled', {
        'taskRecord': i,
        'editor': k
      });
      if (f.suspendHasChangesEvent) {
        j.resumeChangeTracking();
      }
      f.triggerOnClient('afterTaskEdit', {
        'taskRecord': i,
        'editor': k
      });
    }
  }
  async cancel() {
    const a = this, {editor: b} = a, {resetRecurrenceData: c} = b;
    if (c) {
      c.recurringTimeSpan.exceptionDates = c.originalExceptionDates;
      b.resetRecurrenceData = null;
    }
    return a._cancelling || (a._cancelling = a.doCancel().finally(() => a._cancelling = void 0));
  }
  async delete() {
    let a;
    const b = this, {
        editor: c,
        scheduler: e,
        record: f,
        project: g
      } = b;
    if (!f.isCreating && await b.triggerOnClient('beforeTaskDelete', {
        'taskRecord': f,
        'editor': c
      }) === false) {
      return;
    }
    b.cleanupProjectListener();
    c.beforeDelete();
    f.remove();
    await b.commitStmTransaction();
    if (b.isDestroyed) {
      (a = g.resumeAutoSync) == null ? void 0 : a.call(g, false);
      return;
    }
    b.freeStm();
    await g.commitAsync();
    if (b.isDestroyed) {
      return;
    }
    g.resumeAutoSync(!f.isCreating);
    b._editing = false;
    c.close();
    c.afterDelete();
    e.element.classList.remove('b-taskeditor-editing');
    if (b.suspendHasChangesEvent) {
      e.project.resumeChangeTracking();
    }
    b.triggerOnClient('afterTaskEdit', {
      'editor': c,
      'taskRecord': f
    });
  }
  onSave() {
    this.requestAnimationFrame(() => this.save());
  }
  onCancel() {
    this.cancel().then();
  }
  async onDelete() {
    const a = this;
    if (a.confirmDelete) {
      const {editor: b} = a, c = b.autoClose;
      b.autoClose = false;
      a.deleteConfirmationPromise = MessageDialog_default.confirm({
        'title': 'L{TaskEdit.ConfirmDeletionTitle}',
        'message': 'L{TaskEdit.ConfirmDeletionMessage}',
        'okButton': 'L{TaskEditorBase.Delete}',
        'rootElement': a.rootElement
      });
      const e = await a.deleteConfirmationPromise;
      b.autoClose = c;
      a.deleteConfirmationPromise = null;
      if (e === MessageDialog_default.yesButton) {
        a.delete();
      }
    } else {
      a.requestAnimationFrame(() => a.delete());
    }
  }
  onBeforeHide({
    source: a,
    animate: b
  }) {
    if (a.hideAnimation && b !== false) {
      this._hiding = new Promise(c => {
        a.ion({
          'hideAnimationEnd': c,
          'expires': {
            'delay': 500,
            'alt': c
          }
        });
      }).finally(() => this._hiding = void 0);
    }
  }
  onDragCreateEnd({
    eventRecord: a,
    resourceRecord: b,
    proxyElement: c,
    stmCapture: e
  }) {
    if (!this.isDestroyed && !this.disabled && a.isCreating) {
      this.editEvent(a, b, null, e);
    }
  }
  populateEventMenu({
    eventRecord: a,
    resourceRecord: b,
    items: c
  }) {
    if (!this.scheduler.readOnly) {
      c.editEvent = {
        'text': 'L{editEvent}',
        'localeClass': this,
        'icon': 'b-icon b-icon-edit',
        'weight': -200,
        'disabled': this.disabled || a.readOnly,
        'onItem': () => this.editEvent(a, b)
      };
    }
  }
  onEventEnterKey({
    assignmentRecord: a,
    eventRecord: b
  }) {
    if (a) {
      this.editEvent(b, a.resource);
    } else if (b) {
      this.editEvent(b, b.resource);
    }
  }
  async triggerOnClient(a, b) {
    const c = a.replace(/task/, 'event').replace(/Task/, 'Event'), e = await this.scheduler.trigger(...arguments);
    b.eventRecord = b.taskRecord;
    b.eventRecords = [b.taskRecord];
    const f = await this.scheduler.trigger(c, b);
    return e && f;
  }
  onEventDropped({eventRecords: a}) {
    let b;
    const c = ((b = this.editor) == null ? void 0 : b.loadedRecord) && a.find(e => e === this.editor.loadedRecord);
    c && this.onEventResized({ 'eventRecord': c });
  }
  onEventResized({eventRecord: a}) {
    const {editor: b} = this;
    if ((b == null ? void 0 : b.isVisible) && a === b.loadedRecord) {
      b.realign();
    }
  }
};
TaskEdit._$name = 'TaskEdit';
GridFeatureManager.registerFeature(TaskEdit, true, 'SchedulerPro');
GridFeatureManager.registerFeature(TaskEdit, false, 'ResourceHistogram');
const timespanDefaults = {
  'isHighlightConfig': true,
  'clearExisting': false
};
const emptyObject15 = {};
const TimeSpanHighlight = class extends InstancePlugin {
  configs = [];
  domConfigs = [];
  constructor() {
    super(...arguments);
  }
  static get $name() {
    return 'TimeSpanHighlight';
  }
  static get configurable() {
    return { 'padding': 0 };
  }
  static get pluginConfig() {
    return {
      'assign': [
        'highlightTimeSpan',
        'highlightTimeSpans',
        'unhighlightTimeSpans'
      ],
      'chain': ['onTimeAxisViewModelUpdate']
    };
  }
  construct() {
    super.construct(...arguments);
    this.client.rowManager.ion({
      'renderDone': this.onViewChanged,
      'thisObj': this
    });
  }
  highlightTimeSpan(a, b = true) {
    let c;
    const e = this, {
        startDate: f,
        endDate: g,
        name: i,
        surround: j,
        padding: padding = e.padding,
        clearExisting: clearExisting = true
      } = a, {client: k} = e, l = a.isTimeSpan ? a : a.taskRecord;
    let n = a.resourceRecord || a.resource;
    const {animationId: o} = a;
    if (o) {
      DomHelper.addTemporaryClass(k.element, 'b-transition-highlight', 500, k);
    }
    if (clearExisting) {
      e.domConfigs.length = e.configs.length = 0;
    }
    if (e.disabled) {
      return;
    }
    if (j) {
      e.surroundTimeSpan(a);
      return;
    }
    e.configs.push(a);
    let p;
    if (k.isGanttBase) {
      p = k.getScheduleRegion(l, true, {
        'start': f,
        'end': g
      });
    } else {
      if (n) {
        n = k.store.getById(n);
      }
      p = k.getScheduleRegion(n, null, true, {
        'start': f,
        'end': g
      }, !n);
    }
    if (!p || p.width <= 0) {
      return;
    }
    if (padding) {
      if (k.isHorizontal) {
        p.inflate(padding, 0, padding, 0);
      } else {
        p.inflate(0, padding, 0, padding);
      }
    }
    e.domConfigs.push(p.visualize({
      'children': [{
          'class': 'b-sch-highlighted-range-name',
          'text': i
        }],
      'dataset': o ? { 'syncId': o } : emptyObject15,
      'class': {
        'b-sch-highlighted-range': 1,
        [a.cls]: a.cls,
        [a.class || 'b-sch-highlighted-range-default']: 1
      }
    }, true));
    if (b) {
      e.draw();
    }
    (c = k.syncSplits) == null ? void 0 : c.call(k, q => q.highlightTimeSpan(a, b));
  }
  draw() {
    DomSync.sync({
      'targetElement': this.containerEl,
      'domConfig': {
        'onlyChildren': true,
        'children': this.domConfigs
      }
    });
  }
  surroundTimeSpan(a) {
    this.highlightTimeSpans([
      Object.assign({}, a, {
        'animationId': (a.animationId || '') + 'Before',
        'class': 'b-unavailable',
        'surround': false,
        'startDate': this.client.startDate,
        'endDate': a.startDate
      }),
      Object.assign({}, a, {
        'animationId': (a.animationId || '') + 'After',
        'class': 'b-unavailable',
        'surround': false,
        'startDate': a.endDate,
        'endDate': this.client.endDate
      })
    ], { 'clearExisting': a.clearExisting });
  }
  highlightTimeSpans(a, b = {}) {
    const c = this, {
        clearExisting: clearExisting = true
      } = b;
    if (clearExisting) {
      a = a.slice();
      c.domConfigs.length = c.configs.length = 0;
    }
    if (c.disabled) {
      return;
    }
    a.forEach(e => {
      c.highlightTimeSpan(e.isHighlightConfig ? e : Object.setPrototypeOf(e, timespanDefaults), false);
    });
    c.draw();
  }
  async unhighlightTimeSpans(a = false) {
    let b;
    const c = this, {client: e} = c;
    if (a) {
      DomHelper.addTemporaryClass(e.element, 'b-transition-highlight', 500, e);
    }
    Array.from(c.containerEl.children).forEach(f => {
      if (a) {
        f.style.opacity = 0;
        c.fadeOutDetacher = EventHelper.onTransitionEnd({
          'element': f,
          'property': 'opacity',
          'thisObj': e,
          'handler': () => {
            c.domConfigs.length = c.configs.length = 0;
            c.draw();
          }
        });
      } else {
        c.domConfigs.length = c.configs.length = 0;
        c.draw();
      }
    });
    (b = e.syncSplits) == null ? void 0 : b.call(e, f => f.unhighlightTimeSpans(a));
  }
  get containerEl() {
    if (!this._containerEl) {
      this._containerEl = DomHelper.createElement({
        'parent': this.client.foregroundCanvas,
        'retainElement': true,
        'class': 'b-sch-highlight-container'
      });
    }
    return this._containerEl;
  }
  onTimeAxisViewModelUpdate() {
    this.onViewChanged();
  }
  onViewChanged() {
    if (this.configs.length > 0) {
      this.highlightTimeSpans(this.configs);
    }
  }
  updateDisabled(a, b) {
    if (a) {
      this.unhighlightTimeSpans();
    }
    super.updateDisabled(a, b);
  }
  get featureClass() {
  }
};
TimeSpanHighlight._$name = 'TimeSpanHighlight';
GridFeatureManager.registerFeature(TimeSpanHighlight, false, [
  'SchedulerPro',
  'Gantt'
]);
const _ChangeLogEntity = class _ChangeLogEntity {
  static $name = 'ChangeLogEntity';
  constructor({
    model: a,
    type: b
  }) {
    let c, e, f;
    Object.assign(this, {
      'id': (c = a.id) != null ? c : a.$entityName,
      'type': (e = b == null ? void 0 : b.$$name) != null ? e : a.constructor.name,
      'name': (f = a.name) != null ? f : a.$entityName
    });
    if (new.target === _ChangeLogEntity) {
      Object.freeze(this);
    }
  }
};
const ChangeLogEntity = _ChangeLogEntity;
ChangeLogEntity._$name = 'ChangeLogEntity';
const ChangeLogDependencyEntity = class extends ChangeLogEntity {
  static $name = 'ChangeLogDependencyEntity';
  constructor({
    model: a,
    fromTask: b,
    toTask: c
  }) {
    super({
      'model': a,
      'type': DependencyModel2
    });
    Object.assign(this, {
      'fromTask': b,
      'toTask': c
    });
    Object.freeze(this);
  }
};
ChangeLogDependencyEntity._$name = 'ChangeLogDependencyEntity';
const ChangeLogAssignmentEntity = class extends ChangeLogEntity {
  static $name = 'ChangeLogAssignmentEntity';
  constructor({
    model: a,
    event: b,
    resource: c
  }) {
    super({
      'model': a,
      'type': AssignmentModel2
    });
    Object.assign(this, {
      'event': b,
      'resource': c
    });
    Object.freeze(this);
  }
};
ChangeLogAssignmentEntity._$name = 'ChangeLogAssignmentEntity';
const _ChangeLogAction = class _ChangeLogAction {
  static $name = 'ChangeLogAction';
  constructor({
    entity: a,
    actionType: b
  }) {
    Object.assign(this, {
      'entity': a,
      'actionType': b
    });
    if (this.constructor === _ChangeLogAction) {
      Object.freeze(this);
    }
  }
};
const ChangeLogAction = _ChangeLogAction;
ChangeLogAction._$name = 'ChangeLogAction';
const ChangeLogMoveAction = class extends ChangeLogAction {
  static get $name() {
    return 'ChangeLogMoveAction';
  }
  constructor({
    entity: a,
    from: b,
    to: c
  }) {
    super({
      'entity': a,
      'actionType': 'move'
    });
    Object.assign(this, {
      'from': b,
      'to': c
    });
    Object.freeze(this);
  }
};
ChangeLogMoveAction._$name = 'ChangeLogMoveAction';
const ChangeLogUpdateAction = class extends ChangeLogAction {
  static get $name() {
    return 'ChangeLogUpdateAction';
  }
  constructor({
    entity: a,
    propertyUpdates: b,
    isInitialUserAction: c
  }) {
    super({
      'entity': a,
      'actionType': 'update'
    });
    Object.assign(this, {
      'propertyUpdates': b,
      'isUser': c
    });
    Object.freeze(this);
  }
};
ChangeLogUpdateAction._$name = 'ChangeLogUpdateAction';
const Versions = class extends InstancePlugin.mixin(AttachToProjectMixin_default) {
  static configurable = {
    'versionModelClass': VersionModel,
    'transactionModelClass': ChangeLogTransactionModel,
    'autoSaveInterval': 'hourly',
    'knownBaseTypes': [
      AssignmentModel2,
      DependencyModel2,
      ResourceModel2
    ],
    'versionContentLoadTimeout': 60000
  };
  static $name = 'Versions';
  _comparingVersionId = undefined;
  _remoteActionRanges = [];
  constructor() {
    super(...arguments);
  }
  construct(a, b) {
    super.construct(a, b);
    const c = this;
    c.versionStore = new VersionStore({
      'modelClass': c.versionModelClass,
      'internalListeners': {
        'change': c.onStoreChangeExternal,
        'thisObj': c
      },
      'id': VersionStore.configurable.id
    });
    c.changeStore = new ChangeLogStore({
      'modelClass': c.transactionModelClass,
      'internalListeners': {
        'change': c.onStoreChangeExternal,
        'thisObj': c
      },
      'id': ChangeLogStore.configurable.id
    });
    c._currentChanges = [];
    c.client.ion({
      'beforeTaskDelete': c.onBeforeTaskDelete,
      'beforeTaskSave': c.onBeforeTaskSave,
      'gridRowBeforeDropFinalize': c.onGridRowBeforeDropFinalize,
      'thisObj': c
    });
  }
  attachToProject(a) {
    const b = this;
    super.attachToProject(a);
    a.addCrudStore([
      b.versionStore,
      b.changeStore
    ]);
    a.ion({
      'name': 'project',
      'load': b.onProjectLoad,
      'once': true,
      'thisObj': b
    });
    b.detachProjectListeners();
    b.attachProjectListeners(a);
  }
  onBeforeTaskDelete() {
    this.transactionDescription = this.L('L{Versions.deletedTask}');
  }
  onBeforeTaskSave() {
    this.transactionDescription = this.L('L{Versions.editedTask}');
  }
  onGridRowBeforeDropFinalize({
    context: {
      valid: a,
      records: b
    }
  }) {
    if (a) {
      this.transactionDescription = b.length === 1 ? this.L('L{Versions.movedTask}') : this.L('L{Versions.movedTasks}');
    }
  }
  attachProjectListeners(a) {
    let b;
    const c = this;
    (b = a == null ? void 0 : a.stm) == null ? void 0 : b.ion({
      'recordingStart': c.onStmRecordingStart,
      'recordingStop': c.handleTransactionStop,
      'restoringStop': c.onStmRestoringStop,
      'thisObj': c,
      'name': 'stmListeners'
    });
    a == null ? void 0 : a.ion({
      'wsBeforeReceiveChanges': c.onBeforeApplyRemoteChanges,
      'wsReceiveChanges': c.onAfterApplyRemoteChanges,
      'thisObj': c,
      'name': 'stmListeners'
    });
  }
  detachProjectListeners() {
    this.detachListeners('stmListeners');
  }
  updateVersionModelClass(a) {
    if (!a.isVersionModel) {
      throw new Error('versionModelClass config must be a subclass of SchedulerPro.model.VersionModel.');
    }
  }
  updateTransactionModelClass(a) {
    if (!a.isChangeLogTransactionModel) {
      throw new Error('transactionModelClass config must be a subclass of SchedulerPro.model.changelog.ChangeLogTransactionModel.');
    }
  }
  onStoreChangeExternal() {
    this.triggerVersionChange();
  }
  tryAutoSave() {
    const a = this, {project: b} = a.client;
    if (a.changeStore.find(({versionId: c}) => !c)) {
      if (b == null ? void 0 : b.wsSend) {
        b.wsSend('requestVersionAutoSave', { 'project': b.wsProjectId });
        a.detachListeners('autoSave');
        b.ion({
          'name': 'autoSave',
          'wsVersionAutoSaveOK': a.autoSave,
          'thisObj': a,
          'once': true
        });
      } else {
        a.saveVersionWithConfig({ 'isAutosave': true });
      }
    }
  }
  autoSave() {
    this.saveVersionWithConfig({ 'isAutosave': true });
  }
  autoSaveHourly() {
    if (new Date().getMinutes() === 0) {
      this.tryAutoSave();
    }
  }
  updateAutoSaveInterval(a) {
    const b = this, {client: c} = b;
    if (b._autoSaveInterval) {
      c.clearInterval(b._autosaveInterval);
    }
    if (a === 'hourly') {
      b._autoSaveInterval = c.setInterval(b.autoSaveHourly.bind(b), 10 * 1000);
    } else if (a) {
      b._autoSaveInterval = c.setInterval(b.tryAutoSave.bind(b), a * 60 * 1000);
    }
  }
  saveVersion(a = null) {
    return this.saveVersionWithConfig({ 'name': a });
  }
  saveVersionWithConfig(a) {
    const b = this, c = b.captureVersionContent(), e = new b.versionModelClass({
        'savedAt': new Date(),
        'content': c,
        ...a
      });
    e.onBeforeSave();
    b.versionStore.add(e);
    b.changeStore.query(f => f.versionId == null).forEach(f => f.versionId = e.id);
    b.triggerVersionChange();
    b.triggerTransactionChange();
    return e;
  }
  async loadVersionContent(a) {
    const b = this, {client: c} = b, {project: e} = c, f = {
        'version': a,
        'content': null
      };
    if (await b.trigger('beforeLoadVersionContent', { 'context': f })) {
      if (e == null ? void 0 : e.wsSend) {
        e.wsSend('loadVersionContent', {
          'project': e.wsProjectId,
          'versionId': a.id
        });
        const g = await new Promise((i, j) => {
          const k = Date.now();
          e.ion({
            'name': 'loadVersionContent',
            'loadVersionContent'({
              project: l,
              versionId: n,
              content: o
            }) {
              if (l === e.wsProjectId && n === a.id) {
                e.detachListeners('loadVersionContent');
                return i(o);
              }
            },
            'thisObj': b,
            'expires': {
              'delay': b.versionContentLoadTimeout - (Date.now() - k),
              'alt': () => j('Failed to receive version content within ' + this.versionContentLoadTimeout + ' ms')
            }
          });
        });
        if (g) {
          b._versionContentCache.set(a.id, g);
        }
        return g;
      }
    } else if (f.content) {
      return f.content;
    }
  }
  async getVersionContent(a) {
    let b;
    return (b = a.content) != null ? b : await this.loadVersionContent(a);
  }
  triggerTransactionChange() {
    this.client.trigger('transactionChange', { 'hasUnattachedTransactions': this.hasUnattachedTransactions });
  }
  triggerVersionChange() {
    this.client.trigger('versionChange', { 'versions': this.versionStore.records });
  }
  captureVersionContent() {
    const a = this.client.project.inlineData;
    delete a.changelogsData;
    delete a.versionsData;
    return a;
  }
  async restoreVersion(a) {
    let b, c, e, f;
    const g = await this.getVersionContent(a), {project: i} = this.client, j = (b = i.stm) == null ? void 0 : b.enabled;
    (c = i.stm) == null ? void 0 : c.disable();
    i.inlineData = g;
    await i.commitAsync();
    if (j) {
      (e = i.stm) == null ? void 0 : e.enable();
    }
    (f = i.stm) == null ? void 0 : f.resetQueue();
  }
  async compareVersion(a) {
    let b, c, e;
    const f = await this.getVersionContent(a), g = {}, {project: i} = this.client, j = (b = i.stm) == null ? void 0 : b.enabled;
    (c = i.stm) == null ? void 0 : c.disable();
    const k = f.eventsData || f.tasksData;
    for (const l of k) {
      WalkHelper.preWalk(l, n => n.children, ({
        id: n,
        startDate: o,
        endDate: p
      }) => {
        g[n] = {
          'startDate': o,
          'endDate': p
        };
      });
    }
    i.taskStore.traverse(n => {
      if (g[n.id]) {
        n.baselines.removeAll();
        n.baselines.loadData([{
            'task': n,
            ...g[n.id]
          }]);
      }
    });
    if (j) {
      (e = i.stm) == null ? void 0 : e.enable();
    }
    this._comparingVersionId = a.id;
  }
  stopComparing() {
    let a, b, c;
    if (!this._comparingVersionId) {
      return;
    }
    const {project: e} = this.client, f = (a = e.stm) == null ? void 0 : a.enabled;
    (b = e.stm) == null ? void 0 : b.disable();
    e.taskStore.traverse(g => {
      g.baselines.removeAll();
    });
    if (f) {
      (c = e.stm) == null ? void 0 : c.enable();
    }
    this._comparingVersionId = null;
  }
  doDisable(a) {
    super.doDisable(a);
    if (a) {
      this.detachProjectListeners();
    } else {
      this.attachProjectListeners(this.client.project);
    }
    this.client.refresh();
  }
  getEntityDescriptor(a) {
    let b;
    const c = (b = this.knownBaseTypes.find(e => a instanceof e)) != null ? b : a.constructor;
    if (a.isDependencyModel) {
      return new ChangeLogDependencyEntity({
        'model': a,
        'fromTask': this.getEntityDescriptor(a.fromTask),
        'toTask': this.getEntityDescriptor(a.toTask)
      });
    } else if (a.isAssignmentModel) {
      return new ChangeLogAssignmentEntity({
        'model': a,
        'event': this.getEntityDescriptor(a.event),
        'resource': this.getEntityDescriptor(a.resource)
      });
    }
    return new ChangeLogEntity({
      'model': a,
      'type': c
    });
  }
  getUpdateAction(a) {
    const b = a[0].model, c = a.flatMap(({
        newData: i,
        oldData: j
      }) => Object.entries(i).map(([k, l]) => ({
        'property': k,
        'value': l,
        'oldValue': j[k]
      })).filter(({
        property: k,
        value: l,
        oldValue: n
      }) => {
        let o;
        return !ObjectHelper.isEqual(l, n) && ((o = b.getFieldDefinition(k)) == null ? void 0 : o.persist);
      })), e = ArrayHelper.groupBy(c, 'property'), f = Object.entries(e).map(([i, j]) => ({
        'property': i,
        'before': j[0].oldValue,
        'after': j[j.length - 1].value
      })), g = a.some(({isInitialUserAction: i}) => i);
    return new ChangeLogUpdateAction({
      'actionType': 'update',
      'entity': this.getEntityDescriptor(b),
      'propertyUpdates': f,
      'isInitialUserAction': g
    });
  }
  onProjectLoad() {
    this._currentStmTransaction = null;
    this.stopTransaction();
  }
  onStmRecordingStart({transaction: a}) {
    this._transactionStart = new Date();
    this._currentStmTransaction = a;
    this.triggerTransactionChange();
  }
  stopTransaction() {
    let a, b;
    if ((a = this.client.project) == null ? void 0 : a.stm.isRecording) {
      (b = this.client.project) == null ? void 0 : b.stm.stopTransaction();
    }
    this.handleTransactionStop();
  }
  async onStmRestoringStop({
    cause: a,
    transactions: b
  }) {
    const c = this;
    if (a === 'undo') {
      c.transactionDescription = c.L('L{undid}');
    } else if (a === 'redo') {
      c.transactionDescription = c.L('L{redid}');
    }
    await c.client.project.commitAsync();
    c._transactionStart = new Date();
    c.finalizeTransaction({ 'queue': b.flatMap(e => e.queue) });
  }
  onBeforeApplyRemoteChanges() {
    let a, b;
    if (this._remoteChangesStartPos == null) {
      this._remoteChangesStartPos = (b = (a = this._currentStmTransaction) == null ? void 0 : a.queue.length) != null ? b : 0;
    }
  }
  async onAfterApplyRemoteChanges() {
    this.endCurrentRemoteActionRange();
    await this.client.project.commitAsync();
  }
  endCurrentRemoteActionRange() {
    let a, b;
    const c = this;
    if (c._remoteChangesStartPos != null) {
      const e = (b = (a = c._currentStmTransaction) == null ? void 0 : a.queue.length) != null ? b : 0;
      if (e !== c._remoteChangesStartPos) {
        c._remoteActionRanges.push({
          'start': c._remoteChangesStartPos,
          'end': e
        });
      }
      c._remoteChangesStartPos = null;
    }
  }
  handleTransactionStop() {
    const a = this;
    if (a.hasChanges) {
      a.finalizeTransaction(a._currentStmTransaction);
    }
    a._currentTransactionDescription = null;
    a._currentStmTransaction = null;
    a._remoteActionRanges = [];
    a.triggerTransactionChange();
  }
  excludeRanges(a, b) {
    let c = [], e = 0;
    for (const {
          start: f,
          end: g
        } of b) {
      c = c.concat(a.slice(e, f));
      e = g;
    }
    return c.concat(a.slice(e));
  }
  finalizeTransaction(a) {
    const b = this;
    b.endCurrentRemoteActionRange();
    const c = b.excludeRanges(a.queue, b._remoteActionRanges);
    if (c.length === 0) {
      return;
    }
    const e = ({model: j}) => j.$entityName, f = ArrayHelper.groupBy(c, 'type'), g = [];
    for (const j of [
        'UpdateAction',
        'EventUpdateAction'
      ]) {
      const k = ArrayHelper.groupBy(f[j] || [], e);
      for (const l of Object.values(k)) {
        g.push(b.getUpdateAction(l));
      }
    }
    for (const {
          modelList: n,
          isInitialUserAction: o
        } of f.RemoveAction || []) {
      for (const p of n) {
        g.push(new ChangeLogAction({
          'actionType': 'remove',
          'entity': b.getEntityDescriptor(p),
          'isInitialUserAction': o
        }));
      }
    }
    for (const {childModels: q} of f.RemoveChildAction || []) {
      for (const r of q) {
        g.push(new ChangeLogAction({
          'actionType': 'remove',
          'entity': b.getEntityDescriptor(r)
        }));
      }
    }
    for (const {modelList: u} of f.AddAction || []) {
      for (const v of u) {
        g.push(new ChangeLogAction({
          'actionType': 'add',
          'entity': b.getEntityDescriptor(v)
        }));
      }
    }
    for (const {
          childModels: w,
          parentModel: z,
          context: A,
          insertIndex: B
        } of f.InsertChildAction || []) {
      for (const C of w) {
        const E = A.get(C);
        if (E != void 0) {
          g.push(new ChangeLogMoveAction({
            'actionType': 'move',
            'entity': b.getEntityDescriptor(C),
            'from': {
              'parent': b.getEntityDescriptor(E.parent),
              'index': E.index
            },
            'to': {
              'parent': b.getEntityDescriptor(z),
              'index': B
            }
          }));
        } else {
          g.push(new ChangeLogAction({
            'actionType': 'add',
            'entity': b.getEntityDescriptor(C)
          }));
        }
      }
    }
    const i = new b.transactionModelClass({
      'description': b._currentTransactionDescription,
      'actions': g,
      'occurredAt': b._transactionStart
    });
    b.changeStore.add(i);
  }
  set transactionDescription(a) {
    this._currentTransactionDescription = a;
  }
  get hasChanges() {
    let a, b;
    return ((b = (a = this._currentStmTransaction) == null ? void 0 : a.queue) == null ? void 0 : b.length) > 0;
  }
  get hasUnattachedTransactions() {
    return this.changeStore.find(a => a.versionId == null);
  }
  get isComparing() {
    return this._comparingVersionId != null;
  }
};
Versions._$name = 'Versions';
GridFeatureManager.registerFeature(Versions, false, 'SchedulerPro');
const ChangeLogPropertyUpdate = class {
  static $name = 'ChangeLogPropertyUpdate';
  constructor({
    property: a,
    before: b,
    after: c
  }) {
    Object.assign(this, {
      'property': a,
      'before': b,
      'after': c
    });
    Object.freeze(this);
  }
};
ChangeLogPropertyUpdate._$name = 'ChangeLogPropertyUpdate';
const willChange = (a, b, c) => {
  const e = c.getFieldDefinition(a === c.constructor.idField ? 'id' : a), {dataSource: f} = e, g = c.constructor.processField(a, b[f], c);
  return f in b ? !e.isEqual(g, c.getValue(a)) : true;
};
const ProjectChangeHandlerMixin_default = a => class b extends (a || Base) {
  startConfigure(c) {
    this.getConfig('project');
    super.startConfigure(c);
  }
  beforeApplyProjectChanges() {
    const {stm: c} = this;
    let e = false, f = null;
    this.suspendChangeTracking();
    if (c.enabled && !c.isNavigatingRevisions) {
      e = true;
      if (c.isRecording) {
        f = c.stash();
      }
      if (this.shouldIgnoreRemoteChangesInSTM) {
        c.disable();
      } else {
        c.startTransaction();
      }
    }
    return {
      'shouldResume': e,
      'transactionId': f
    };
  }
  async applyProjectChanges(c) {
    const e = this, {
        shouldResume: f,
        transactionId: g
      } = e.beforeApplyProjectChanges();
    e.trigger('startApplyChangeset');
    e.applyingChangeset = true;
    if (c.project) {
      e.applyProjectResponse(c.project);
    }
    e.applyChangeset(ObjectHelper.clone(c));
    await e.commitAsync();
    e.commitRespondedChanges();
    e.afterApplyProjectChangesCleanup(c);
    e.afterApplyProjectChanges(f, g);
    e.applyingChangeset = false;
    await e.commitAsync();
    e.trigger('endApplyChangeset');
  }
  afterApplyProjectChangesCleanup(c) {
    let e, f, g, i, j;
    for (const k in c) {
      const l = this.getStoreDescriptor(k);
      if (l) {
        const {store: n} = l, o = c[k], p = [
            ...(e = o.updated) != null ? e : [],
            ...(f = o.added) != null ? f : []
          ];
        if (n) {
          for (const q of p) {
            const r = n.getById(q[n.modelClass.idField]);
            if (r) {
              for (const u in r.modifications) {
                if (!willChange(u, q, r)) {
                  delete r.meta.modified[u];
                }
              }
            } else {
              const v = (i = (g = this.phantomIdField) != null ? g : l.phantomIdField) != null ? i : '$PhantomId', w = n.getById(q[v]);
              if (w) {
                n.added.remove(w);
              }
            }
          }
          for (const z of (j = o.removed) != null ? j : []) {
            n.removed.remove(z.id);
          }
        }
      }
    }
  }
  afterApplyProjectChanges(c, e) {
    if (c) {
      const {stm: f} = this;
      if (this.shouldIgnoreRemoteChangesInSTM) {
        f.enable();
      } else {
        f.stopTransaction();
      }
      f.applyStash(e);
    }
    this.resumeChangeTracking();
  }
};
const ProjectRevisionHandlerMixin_default = a => class b extends ProjectChangeHandlerMixin_default(a) {
  get shouldIgnoreRemoteChangesInSTM() {
    return super.shouldIgnoreRemoteChangesInSTM || this.stm.revisionsEnabled;
  }
  initRevisions(c, e = 'base') {
    const f = this, {stm: g} = f;
    f.detachListeners('projectRevisionListeners');
    if (g.enabled && g.revisionsEnabled) {
      f.clientId = c;
      g.initRevision(e);
      g.asyncUndoRedo = true;
      g.mergeAddUpdateActions = true;
      g.ion({
        'name': 'projectRevisionListeners',
        'recordingStart': 'revisionMixinHandleRecordingStart',
        'recordingStop': 'revisionMixinHandleRecordingStop',
        'revisionRecordingStart': 'revisionMixinHandleRecordingStart',
        'revisionRecordingStop': 'revisionMixinHandleRecordingStop',
        'temporaryRevisionRecordingStart': 'revisionMixinHandleRecordingStart',
        'temporaryRevisionRecordingStop': 'revisionMixinHandleRecordingStop',
        'beforeRevisionAdd': 'revisionMixinHandleBeforeLocalRevision',
        'revisionAdd': 'revisionMixinHandleLocalRevision',
        'increaseRevisionAsync': 'revisionMixinHandleIncreaseRevisionAsync',
        'thisObj': f
      });
      f.pendingLocalRevisions = [];
      if (globalThis.bryntum.DEBUG_REVISION) {
        f.$debugLocalRevisions = [];
      }
    }
  }
  getConflictResolutionsMap(c) {
    const e = new Map();
    for (let f = 0; f < c.length; f++) {
      const g = c[f], {
          changes: j,
          conflictResolutionFor: k
        } = g;
      if (k && c.some(l => l.revisionId === k)) {
        if (!e.has(k)) {
          e.set(k, []);
        }
        e.get(k).push(j);
      }
    }
    return e;
  }
  async applyRevisions(c) {
    const e = this, {stm: f} = e;
    if (!Array.isArray(c)) {
      c = [c];
    }
    e._revisionsQueue = e._revisionsQueue || [];
    e._revisionsQueue.push(...c);
    return e.queue(async () => {
      let g;
      if (!((g = e._revisionsQueue) == null ? void 0 : g.length)) {
        return;
      }
      c = e._revisionsQueue;
      delete e._revisionsQueue;
      const {lastCommittedRevision: i} = f;
      if (i) {
        const j = c.findLastIndex(k => k.revisionId === i.title);
        if (j !== -1) {
          c = c.slice(j + 1);
        }
      }
      if (f.isRecording) {
        f.stopTransaction();
      }
      e.trigger('startApplyingRevision', { 'revisions': c });
      if (c.length) {
        const k = e.getConflictResolutionsMap(c);
        f.checkoutToLastCommittedRevision();
        await e.commitAsync();
        e.acceptChanges();
        for (const {
              revisionId: l,
              localRevisionId: n,
              clientId: o,
              changes: p
            } of c) {
          if (o === e.clientId) {
            await e.confirmLocalRevisionFromRemote(l, n, p, k.get(l));
          } else {
            await e.recordRevisionFromRemoteChanges(l, p, k.get(l));
          }
        }
        await e.checkoutToHeadResolvingConflicts();
        e.cleanupLocalChangesAfterCheckoutToHead();
        if (f.isRecording) {
          e.trigger('postApplyRevision');
          f.stopTransaction();
        } else if (e.changes) {
          f.createDataCorrectionTransaction();
        }
      }
      e.trigger('stopApplyingRevision', { 'revisions': c });
    });
  }
  async confirmLocalRevisionFromRemote(c, e, f, g = []) {
    const i = this, {stm: j} = i;
    j.checkoutTo(e);
    j.startTemporaryRevision();
    i.handleRevisionSchedulingConflicts(f, false);
    let k = false;
    const l = i.ion({
      'conflictAutoResolved'() {
        k = true;
      }
    });
    await i.applyProjectChanges(Objects.mergeUniformObjects({}, f, ...g));
    l();
    if (k) {
      j.createConflictResolutionRevision(c, e, f);
    }
    j.stopRevision();
    j.commitRevision(e, c);
    i.detachListeners('revisionSchedulingConflicts');
  }
  async recordRevisionFromRemoteChanges(c, e, f = []) {
    const g = this, {stm: i} = g;
    g.handleRevisionSchedulingConflicts(e, false);
    i.startRevision(c);
    let j = false;
    const k = g.ion({
      'conflictAutoResolved'() {
        j = true;
      }
    });
    await g.applyProjectChanges(Objects.mergeUniformObjects({}, e, ...f));
    k();
    if (j) {
      i.createConflictResolutionRevision(c, null, e);
    }
    i.stopRevision();
    i.commitRevision(c);
    g.detachListeners('revisionSchedulingConflicts');
  }
  async checkoutToHeadResolvingConflicts() {
    let c;
    const e = this, {stm: f} = e;
    while (f.checkoutToNext() !== false) {
      const {currentRevisionTransaction: g} = f;
      f.startTemporaryRevision('', f.currentRevisionTransaction);
      const i = (c = e.pendingLocalRevisions.find(({localRevisionId: l}) => l === g.title)) == null ? void 0 : c.changes;
      e.handleRevisionSchedulingConflicts(i);
      let j = false;
      const k = e.ion({
        'conflictAutoResolved'() {
          j = true;
        }
      });
      await e.commitAsync();
      k();
      if (j) {
        f.createTemporaryConflictResolutionRevision(g.title, i);
      }
      f.stopRevision();
      e.detachListeners('revisionSchedulingConflicts');
    }
    f.checkoutToHead();
  }
  handleRevisionSchedulingConflicts(c) {
    const e = this;
    e.ion({
      'name': 'revisionSchedulingConflicts',
      'cycle'({
        schedulingIssue: f,
        continueWithResolutionResult: g
      }) {
        let i, j, k;
        e.stm.markCurrentTransactionContentUserInput();
        const l = f.getDependencies(), n = (k = (j = (i = c[e.dependencyStore.id]) == null ? void 0 : i.$input) == null ? void 0 : j.added) != null ? k : [], o = l.filter(q => n.find(r => {
            return r.id === q.id || r.$PhantomId === q.id;
          })), p = f.deactivateDependencyCycleEffectResolutionClass.new();
        if (!o.length) {
          const q = l.sort((u, v) => u.id - v.id)[0], r = [
              'Cannot resolve dependency cycle automatically. Going to deactivate dependency ' + q.id,
              'Conflicting dependencies: ' + JSON.stringify(l),
              'Added dependencies: ' + JSON.stringify(n)
            ];
          console.warn(r.join('\n'));
          o.push(q);
        }
        o.map(u => p.resolve(u));
        g(EffectResolutionResult.Resume);
        e.trigger('conflictAutoResolved');
        return false;
      },
      'schedulingConflict'({
        schedulingIssue: f,
        continueWithResolutionResult: g
      }) {
        e.stm.markCurrentTransactionContentUserInput();
        const i = f.getResolutions(), j = i.find(k => k.isDeactivateDependencyResolution) || i.find(k => k.isRemoveDateConstraintConflictResolution) || i[0];
        j.resolve();
        g(EffectResolutionResult.Resume);
        e.trigger('conflictAutoResolved');
        return false;
      },
      'thisObj': e,
      'prio': 1000
    });
  }
  cleanupLocalChangesAfterCheckoutToHead() {
    let c;
    const e = this, {stm: f} = e, g = (c = f.localRevisions[0]) == null ? void 0 : c.title;
    if (g) {
      e.pendingLocalRevisions = e.pendingLocalRevisions.slice(e.pendingLocalRevisions.findIndex(({localRevisionId: j}) => j === g));
      const i = Objects.mergeUniformObjects({}, ...e.pendingLocalRevisions.map(({changes: j}) => j));
      e.afterApplyProjectChangesCleanup(i);
    }
  }
  inflateChangesWithUserInput(c, e) {
    let f, g, i;
    if (!e) {
      return;
    }
    for (const j in c) {
      if (j === 'project' && 'updated' in e) {
        c.project.$input = (f = e.updated.get(this)) != null ? f : {};
        delete c.project.id;
        continue;
      }
      const k = c[j];
      k.$input = {};
      if (j in e) {
        [
          'added',
          'removed'
        ].forEach(l => {
          if (l in e[j]) {
            k.$input[l] = k[l].filter(n => e[j][l].find(o => {
              let p, q;
              return ((p = n.id) != null ? p : n.$PhantomId) === ((q = o.id) != null ? q : o.$PhantomId);
            }));
          }
        });
      }
      if ('updated' in k) {
        for (const l of k.updated) {
          const n = this.getCrudStore(j).getById(l.id);
          if (n && ((g = e.updated) == null ? void 0 : g.has(n))) {
            const o = k.$input;
            o.updated = o.updated || [];
            const p = Object.fromEntries(Object.keys(e.updated.get(n)).map(q => {
              const {dataSource: r} = n.getFieldDefinition(q);
              return [
                r,
                l[r]
              ];
            }));
            o.updated.push({
              [n.constructor.idField]: n.id,
              ...p
            });
          }
        }
      }
      if (k.added && e.updated && !((i = e[j]) == null ? void 0 : i.added)) {
        const q = this.getStoreDescriptor(j), r = q.phantomIdField || this.phantomIdField, u = Array.from(e.updated.keys());
        k.$input.added = k.added.filter(v => u.find(w => w.id === v[r]));
      }
    }
  }
  inflateRevisionWithConflictResolutionDataChanges(c, e = {}) {
    for (const f in e) {
      const g = e[f];
      for (const i of g.updated || []) {
        const j = this.getCrudStore(f).getById(i.id);
        if (j) {
          const {persistableData: k} = j;
          for (const l in i) {
            if (l !== 'id' && i[l] !== k[l]) {
              if (!Objects.hasPath(c, f + '.updated')) {
                Objects.setPath(c, f + '.updated', []);
              }
              const {updated: n} = c[f], o = n.find(p => p.id === i.id);
              if (o) {
                o[l] = k[l];
              } else {
                n.push({
                  'id': i.id,
                  [l]: k[l]
                });
              }
            }
          }
        } else {
          console.warn('Trying to update record with id ' + i.id + ' which is not in the store ' + f);
        }
      }
    }
  }
  revisionMixinHandleBeforeLocalRevision() {
    this._currentRevisionChanges = this.changes;
    return this._currentRevisionChanges != null;
  }
  revisionMixinHandleLocalRevision({
    localRevisionId: c,
    conflictResolutionFor: e,
    revisionChanges: f,
    userInput: g
  }) {
    const i = this;
    let {_currentRevisionChanges: j} = i;
    j = j != null ? j : {};
    delete i._currentRevisionChanges;
    i.acceptChanges();
    i.inflateRevisionWithConflictResolutionDataChanges(j, f);
    i.inflateChangesWithUserInput(j, g);
    i.trigger('revisionNotification', {
      'localRevisionId': c,
      'conflictResolutionFor': e,
      'changes': j,
      'clientId': i.clientId
    });
    i.pendingLocalRevisions.push({
      'localRevisionId': c,
      'changes': j,
      'conflictResolutionFor': e
    });
    if (globalThis.bryntum.DEBUG_REVISION) {
      i.$debugLocalRevisions.push({
        'localRevisionId': c,
        'changes': j,
        'conflictResolutionFor': e
      });
    }
  }
  revisionMixinHandleRecordingStart() {
    this.ion({
      'name': 'userInputInvestigatorListener',
      'finalizeCommit': 'revisionMixinHandleFinalizeCommitAsync',
      'dataReady': 'revisionMixinHandleDataReady',
      'thisObj': this
    });
  }
  revisionMixinHandleRecordingStop() {
    this.detachListeners('userInputInvestigatorListener');
  }
  revisionMixinHandleFinalizeCommitAsync(...c) {
    this.stm.markCurrentTransactionContentUserInput();
  }
  revisionMixinHandleDataReady() {
    this.stm.markCurrentTransactionContentCalculated();
  }
  async revisionMixinHandleIncreaseRevisionAsync({callback: c}) {
    await this.commitAsync();
    c();
  }
};
const ProjectModel2 = class extends ProjectRevisionHandlerMixin_default(ProjectCrudManager_default2(ProjectModelMixin_default(SchedulerProProjectMixin))) {
  static get $name() {
    return 'ProjectModel';
  }
  static get defaultConfig() {
    return {
      'calendarModelClass': CalendarModel,
      'dependencyModelClass': DependencyModel2,
      'eventModelClass': EventModel2,
      'assignmentModelClass': AssignmentModel2,
      'resourceModelClass': ResourceModel2,
      'calendarManagerStoreClass': CalendarManagerStore,
      'dependencyStoreClass': DependencyStore2,
      'eventStoreClass': EventStore2,
      'assignmentStoreClass': AssignmentStore2,
      'resourceStoreClass': ResourceStore2,
      'calendarsData': null,
      'supportShortSyncResponseNote': 'Note: Please consider enabling "supportShortSyncResponse" option to allow less detailed sync responses (https://bryntum.com/products/schedulerpro/docs/api/SchedulerPro/model/ProjectModel#config-supportShortSyncResponse)',
      'delayCalculation': true,
      'calendarManagerStore': {},
      'stmClass': StateTrackingManager2
    };
  }
  static get configurable() {
    return { 'calendars': null };
  }
  get taskStore() {
    return this.eventStore;
  }
  get tasks() {
    return this.taskStore.allRecords;
  }
  updateTasks(a) {
    this.taskStore.data = a;
  }
  get calendars() {
    return this.calendarManagerStore.allRecords;
  }
  updateCalendars(a) {
    this.calendarManagerStore.data = a;
  }
};
ProjectModel2._$name = 'ProjectModel';
const ProjectProgressMixin_default = a => {
  let b;
  return b = class extends (a || Base) {
    static get $name() {
      return 'ProjectProgressMixin';
    }
    updateProject(c, e) {
      super.updateProject(c, e);
      this.setupProgressListener();
      this.detachListeners('delayedCalculation');
      if (c == null ? void 0 : c.delayCalculation) {
        c.ion({
          'name': 'delayedCalculation',
          'delayCalculationStart': 'internalOnProjectDelayCalculationStart',
          'delayCalculationEnd': 'internalOnProjectDelayCalculationEnd',
          'thisObj': this
        });
      }
    }
    setupProgressListener() {
      let c;
      const e = this;
      e.detachListeners('projectProgress');
      if (e.projectProgressReporting) {
        (c = e.project) == null ? void 0 : c.ion({
          'name': 'projectProgress',
          'progress': 'onProjectProgress',
          'thisObj': e
        });
      }
    }
    updateProjectProgressReporting() {
      if (!this.isConfiguring) {
        this.setupProgressListener();
      }
    }
    onProjectProgress({
      total: c,
      remaining: e,
      phase: phase = 'propagating'
    }) {
      const f = this;
      if (!f.isPainted) {
        return;
      }
      if (c < f.projectProgressThreshold) {
        return;
      }
      let g = f.projectProgressReporting;
      if (g === 'auto') {
        g = f.project.delayCalculation ? 'progressbar' : 'mask';
      }
      if (g === 'progressbar') {
        let i = f.calculationProgressElement;
        if (!i) {
          i = f.calculationProgressElement = DomHelper.createElement({
            'parent': f.timeAxisSubGrid.header.element,
            'retainElement': true,
            'className': 'b-calculation-progress-wrap',
            'children': [{ 'className': 'b-calculation-progress' }]
          });
        }
        i.firstElementChild.style.width = (c - e) / c * 100 + '%';
        if (c > 0 && e === 0) {
          f.calculationProgressTimeout = f.setTimeout(() => {
            i == null ? void 0 : i.remove();
            f.calculationProgressElement = null;
          }, 50);
        } else {
          f.clearTimeout(f.calculationProgressTimeout);
        }
      } else {
        const j = f.L('L{SchedulerProBase.' + phase + '}'), k = c ? j + ' ' + Math.round(100 * (c - e) / c) + '%' : j;
        if (!f.masked) {
          f.mask({
            'maxProgress': c,
            'useTransition': false,
            'text': k
          });
        }
        f.masked.text = k;
        if (c) {
          f.masked.maxProgress = c;
          f.masked.progress = c - e;
        }
        if (c > 0 && e === 0) {
          f.unmask();
        }
      }
    }
    internalOnProjectDelayCalculationStart() {
      if (!this.readOnly) {
        this.$delayCalculationReadOnly = this.readOnly = true;
      }
    }
    internalOnProjectDelayCalculationEnd() {
      if (this.$delayCalculationReadOnly) {
        this.$delayCalculationReadOnly = this.readOnly = false;
      }
    }
    get widgetClass() {
    }
  }, __publicField(b, 'configurable', {
    'projectProgressReporting': 'auto',
    'projectProgressThreshold': 5000
  }), b;
};
const SchedulerProEventRendering_default = a => class b extends (a || Base) {
  static get $name() {
    return 'SchedulerProEventRendering';
  }
  static get configurable() {
    return {
      'eventLayout': 'stack',
      'horizontalLayoutPackClass': ProHorizontalLayoutPack,
      'horizontalLayoutStackClass': ProHorizontalLayoutStack
    };
  }
  updateInternalEventLayout(c, e) {
    const f = this;
    if (!f.isConfiguring) {
      f.clearLayouts();
    }
    super.updateInternalEventLayout(c, e);
  }
  getEventLayout(c) {
    c = super.getEventLayout(c);
    if ('layoutFn' in c) {
      c.type = 'layoutFn';
    }
    return c;
  }
  clearLayouts() {
    const c = this;
    if (c.layouts) {
      for (const e in c.layouts) {
        c.layouts[e].destroy();
        delete c.layouts[e];
      }
    }
  }
  getEventLayoutHandler(c) {
    const e = this;
    if (!e.isHorizontal) {
      return null;
    }
    const {
      timeAxisViewModel: f,
      horizontal: g
    } = e;
    if (!e.layouts) {
      e.layouts = {};
    }
    const {layouts: i} = e;
    switch (c.type) {
    case 'stack': {
        if (!i.horizontalStack) {
          i.horizontalStack = e.horizontalLayoutStackClass.new({
            'scheduler': e,
            'timeAxisViewModel': f,
            'bandIndexToPxConvertFn': g.layoutEventVerticallyStack,
            'bandIndexToPxConvertThisObj': g,
            'groupByThisObj': e
          }, c);
        }
        return i.horizontalStack;
      }
    case 'pack': {
        if (!i.horizontalPack) {
          i.horizontalPack = e.horizontalLayoutPackClass.new({
            'scheduler': e,
            'timeAxisViewModel': f,
            'bandIndexToPxConvertFn': g.layoutEventVerticallyPack,
            'bandIndexToPxConvertThisObj': g,
            'groupByThisObj': e
          }, c);
        }
        return i.horizontalPack;
      }
    case 'layoutFn': {
        return {
          'type': 'layoutFn',
          'scheduler': e,
          'applyLayout': c.layoutFn,
          'layoutEventsInBands': c.layoutFn
        };
      }
    default:
      return null;
    }
  }
  get widgetClass() {
  }
};
const SchedulingIssueResolutionPopup = class extends Popup {
  static get $name() {
    return 'SchedulingIssueResolutionPopup';
  }
  static get type() {
    return 'schedulingissueresolutionpopup';
  }
  static get configurable() {
    return {
      'localizableProperties': [],
      'schedulingIssue': null,
      'align': 'b-t',
      'autoShow': false,
      'autoClose': false,
      'closeAction': 'onCloseButtonClick',
      'modal': true,
      'centered': true,
      'scrollAction': 'realign',
      'constrainTo': globalThis,
      'draggable': false,
      'closable': true,
      'floating': true,
      'cls': 'b-schedulerpro-issueresolutionpopup',
      'layout': 'vbox',
      'resolutionsGroup': {
        'type': 'radiogroup',
        'name': 'resolutions'
      },
      'items': {
        'description': {
          'type': 'widget',
          'cls': 'b-error-description',
          'weight': -100
        }
      },
      'bbar': {
        'defaults': { 'localeClass': this },
        'items': {
          'applyButton': {
            'weight': 100,
            'color': 'b-raised b-blue',
            'text': 'L{Apply}',
            'onClick': 'up.onApplyButtonClick',
            'disabled': true
          },
          'cancelButton': {
            'weight': 200,
            'color': 'b-gray',
            'text': 'L{Object.Cancel}',
            'onClick': 'up.onCancelButtonClick'
          }
        }
      }
    };
  }
  get selectedResolutions() {
    let a, b;
    const c = new Set(), e = (b = (a = this.resolutionsGroup) == null ? void 0 : a.selected) == null ? void 0 : b.resolution;
    if (e) {
      c.add(e);
    }
    return c;
  }
  changeResolutionsGroup(a, b) {
    return RadioGroup.reconfigure(b, a, this);
  }
  updateResolutionsGroup(a) {
    a && this.add(a);
  }
  getResolutionParameters(a) {
    return [];
  }
  onApplyButtonClick() {
    const a = this, {selectedResolutions: b} = a;
    if (b.size) {
      b.forEach(c => c.resolve(...a.getResolutionParameters(c)));
      a.continueWithResolutionResult(EffectResolutionResult.Resume);
      a.doResolve(b);
    } else {
      a.onCancelButtonClick();
    }
  }
  onCancelButtonClick() {
    this.continueWithResolutionResult(EffectResolutionResult.Cancel);
    this.doResolve();
  }
  onCloseButtonClick() {
    if (this.canCancel) {
      this.onCancelButtonClick();
    }
  }
  get isResolving() {
    return Boolean(this.resolving);
  }
  async resolve({
    source: a,
    schedulingIssue: b,
    continueWithResolutionResult: c
  }) {
    const e = this;
    e.project = a;
    e.schedulingIssue = b;
    e.continueWithResolutionResult = c;
    e.updatePopupContent(b, c);
    e.onResolutionChange({});
    e.show();
    e.resolving = new Promissory();
    return e.resolving.promise;
  }
  doResolve(a) {
    const b = this, {resolving: c} = b;
    if (c) {
      b.resolving.resolve(a);
      b.resolving = null;
      b.schedulingIssue = null;
      b.hide();
    }
  }
  getResolutionWidgetConfig(a) {
    return {
      'text': StringHelper.encodeHtml(a.getDescription()),
      'cls': 'b-resolution',
      'checkedValue': a.$$name,
      'localeClass': this,
      'name': 'resolutions',
      'internalListeners': { 'change': 'up.onResolutionChange' },
      'resolution': a
    };
  }
  getResolutions() {
    let a;
    return (a = this.schedulingIssue) == null ? void 0 : a.getResolutions();
  }
  updatePopupContent(a, b) {
    let c;
    const e = this, {resolutionsGroup: f} = e;
    if (b) {
      e.continueWithResolutionResult = b;
    }
    if (a) {
      e.schedulingIssue = a;
    } else {
      a = e.schedulingIssue;
    }
    e.title = (a == null ? void 0 : a.type) ? e.optionalL(a.type) : 'Unknown error';
    e.widgetMap.description.content = a && StringHelper.encodeHtml(a.getDescription());
    const g = e.getResolutions(), i = (g == null ? void 0 : g.map(k => e.getResolutionWidgetConfig(k))) || [], j = ((c = i[0]) == null ? void 0 : c.name) || 'resolutions';
    f.removeAll();
    f.name = j;
    f.add(...i, {
      'ref': 'cancelResolution',
      'text': 'L{Cancel changes}',
      'name': j,
      'checkedValue': 'cancel',
      'localeClass': this,
      'cls': 'b-resolution',
      'internalListeners': { 'change': 'up.onResolutionChange' }
    });
    e.toggleControlsState();
  }
  get canApply() {
    return this.resolutionsGroup.value != null;
  }
  get canCancel() {
    let a;
    return !((a = this.project) == null ? void 0 : a.isInitialCommit);
  }
  get cancelResolution() {
    return this.resolutionsGroup.widgetMap.cancelResolution;
  }
  onResolutionChange() {
    this.toggleControlsState();
  }
  toggleControlsState() {
    const a = this, {
        applyButton: b,
        cancelButton: c
      } = a.widgetMap;
    b.disabled = !a.canApply;
    a.cancelResolution.hidden = c.hidden = !a.canCancel;
  }
  updateLocalization() {
    this.updatePopupContent();
    super.updateLocalization();
  }
};
SchedulingIssueResolutionPopup.initClass();
SchedulingIssueResolutionPopup._$name = 'SchedulingIssueResolutionPopup';
const CycleResolutionPopup = class extends SchedulingIssueResolutionPopup {
  static get $name() {
    return 'CycleResolutionPopup';
  }
  static get type() {
    return 'cycleresolutionpopup';
  }
  getDependencyTitle(a) {
    return '"' + StringHelper.encodeHtml(a.fromEvent.name) + '" -> "' + StringHelper.encodeHtml(a.toEvent.name) + '"';
  }
  getResolutionWidgetConfig(a) {
    const {dependency: b} = a, c = this.schedulingIssue.getInvalidDependencies(), e = super.getResolutionWidgetConfig(a);
    if (c.includes(b)) {
      Object.assign(e, {
        'name': 'dependency-resolution',
        'checked': !this._dependencyResolutionsChecked++
      });
    }
    return e;
  }
  getResolutions() {
    const {schedulingIssue: a} = this, b = a == null ? void 0 : a.getInvalidDependencies();
    let c = a == null ? void 0 : a.getResolutions();
    if (c && b.length) {
      c = c.filter(e => e.dependency && b.includes(e.dependency));
    }
    return c;
  }
  updatePopupContent(a, b) {
    const c = this, {
        invalidDependenciesDescription: e,
        dependencyField: f
      } = c.widgetMap;
    c._dependencyResolutionsChecked = 0;
    super.updatePopupContent(...arguments);
    e == null ? void 0 : e.hide();
    f == null ? void 0 : f.hide();
    a = c.schedulingIssue;
    if (a) {
      const g = a.getDependencies(), i = a.getInvalidDependencies(), j = g.filter(k => !i.includes(k));
      if (i.length) {
        if (e) {
          e.show();
        } else {
          c.add({
            'type': 'widget',
            'ref': 'invalidDependenciesDescription',
            'weight': -50,
            'width': '100%',
            'cls': 'b-invalid-dependencies-description',
            'html': c.L('L{invalidDependencyLabel}')
          });
        }
      } else {
        const k = j == null ? void 0 : j.map(l => ({
          'value': l.id,
          'text': c.getDependencyTitle(l)
        }));
        if (f) {
          f.value = null;
          f.items = k;
          f.show();
        } else {
          c.add({
            'type': 'combo',
            'ref': 'dependencyField',
            'weight': 50,
            'width': '100%',
            'name': 'dependency',
            'label': c.L('L{dependencyLabel}'),
            'labelPosition': 'above',
            'cls': 'b-dependency-field',
            'items': k,
            'internalListeners': { 'change': 'up.onDependencyChange' }
          });
        }
      }
    }
  }
  get canApply() {
    const {widgetMap: a} = this;
    return super.canApply && (a.cancelResolution.checked || (!a.dependencyField || a.dependencyField.value));
  }
  onDependencyChange() {
    this.toggleControlsState();
  }
  getResolutionParameters(a) {
    if (a.isRemoveDependencyCycleEffectResolution || a.isDeactivateDependencyCycleEffectResolution) {
      const b = this.widgetMap.dependencyField.value, c = this.project.dependencyStore.getById(b);
      return [c];
    }
    return super.getResolutionParameters(a);
  }
};
CycleResolutionPopup.initClass();
CycleResolutionPopup._$name = 'CycleResolutionPopup';
const SchedulingIssueResolution_default = a => class b extends (a || Base) {
  static get $name() {
    return 'SchedulingIssueResolution';
  }
  static get configurable() {
    return {
      'schedulingIssueResolutionPopupClass': SchedulingIssueResolutionPopup,
      'cycleResolutionPopupClass': CycleResolutionPopup,
      'displaySchedulingIssueResolutionPopup': true
    };
  }
  updateProject(c, e) {
    super.updateProject(c, e);
    this.unbindSchedulingIssueResolutionFromProject(e);
    if (this.displaySchedulingIssueResolutionPopup && c) {
      this.bindSchedulingIssueResolutionToProject(c);
    }
  }
  bindSchedulingIssueResolutionToProject(c) {
    c.ion({
      'name': 'schedulingIssueResolution',
      'schedulingConflict': 'onProjectSchedulingIssueEvent',
      'emptyCalendar': 'onProjectSchedulingIssueEvent',
      'cycle': 'onProjectSchedulingIssueEvent',
      'thisObj': this
    });
  }
  get isResolving() {
    let c;
    return (c = this._lastSchedulingIssueResolutionPopup) == null ? void 0 : c.isResolving;
  }
  get activeSchedulingIssueResolutionPopup() {
    return this.isResolving && this._lastSchedulingIssueResolutionPopup;
  }
  unbindSchedulingIssueResolutionFromProject(c) {
    this.detachListeners('schedulingIssueResolution');
  }
  getSchedulingIssueResolutionPopup(c) {
    if (c.type === 'cycle') {
      return this._cycleResolutionPopup || (this._cycleResolutionPopup = new this.cycleResolutionPopupClass({ 'rootElement': this.rootElement }));
    } else {
      return this._schedulingIssueResolutionPopup || (this._schedulingIssueResolutionPopup = new this.schedulingIssueResolutionPopupClass({ 'rootElement': this.rootElement }));
    }
  }
  onProjectSchedulingIssueEvent({schedulingIssue: c}) {
    const e = this.getSchedulingIssueResolutionPopup(c);
    this._lastSchedulingIssueResolutionPopup = e;
    e.resolve(...arguments);
  }
  get widgetClass() {
  }
};
function buildReturnedValue(a) {
  return { 'allocation': { 'total': a } };
}
const ResourceHistogram = class extends TimelineHistogram.mixin(SchedulerStores_default, SchedulerResourceRendering_default, CrudManagerView_default) {
  static configurable = {
    'projectModelClass': ProjectModel2,
    'sortFeatureStore': 'store',
    'timeCellCls': 'b-sch-timeaxis-cell b-resourcehistogram-cell',
    'effortFormat': '0.#',
    'getRecordData': 'getRecordAllocationData',
    'aggregateDataEntry': 'aggregateAllocationEntry',
    'initAggregatedDataEntry': 'initAggregatedAllocationEntry',
    'hardRefreshOnTimeAxisReconfigure': false,
    'showEffortUnit': true,
    'useProjectTimeUnitsForScale': false,
    'effortUnit': TimeUnit.Hour,
    'barTextEffortUnit': null,
    'barTipEffortUnit': null,
    'showMaxEffort': true,
    'series': {
      'maxEffort': {
        'type': 'outline',
        'field': 'maxEffort'
      },
      'effort': {
        'type': 'bar',
        'field': 'effort'
      }
    },
    'barTooltipTemplate'({datum: a}) {
      let b = '';
      const {
        inEventTimeSpan: c,
        isGroup: e
      } = a;
      if (c) {
        if (e) {
          b = this.getGroupBarTip(...arguments);
        } else {
          b = this.getResourceBarTip(...arguments);
        }
      }
      return b;
    },
    'showBarText': false,
    'groupBarTipAssignmentLimit': 5,
    'includeInactiveEvents': false,
    'histogramWidget': { 'cls': 'b-hide-offscreen b-resourcehistogram-histogram' },
    'fixedRowHeight': true
  };
  static type = 'resourcehistogram';
  static $name = 'ResourceHistogram';
  get timeAxis() {
    return super.timeAxis;
  }
  set timeAxis(a) {
    const b = this._timeAxis;
    super.timeAxis = a;
    if (this.partner && !a || b && b === a) {
      return;
    }
    this._timeAxis.ion({
      'name': 'timeAxis',
      'endReconfigure': 'onTimeAxisEndReconfigure',
      'thisObj': this
    });
  }
  afterConfigure() {
    super.afterConfigure();
    const a = this;
    a.onRecordAllocationCalculated = a.onRecordAllocationCalculated.bind(a);
    a.onCommitAsyncCompletion = a.onCommitAsyncCompletion.bind(a);
  }
  onDestroy() {
    let a, b;
    super.onDestroy(...arguments);
    const c = (b = (a = this.project).getGraph) == null ? void 0 : b.call(a);
    if (c && this.ticksIdentifier) {
      c.removeIdentifier(this.ticksIdentifier);
    }
  }
  clearHistogramDataCache(a) {
    let b, c;
    if (!a) {
      for (const [e, f] of (b = this.getHistogramDataCache()) == null ? void 0 : b.entries()) {
        (c = e.removeEntity) == null ? void 0 : c.call(e, f);
      }
    }
    super.clearHistogramDataCache(...arguments);
  }
  bindProject(a) {
    this.detachListeners('resourceHistogramProject');
    a.ion({
      'name': 'resourceHistogramProject',
      'refresh': 'internalOnProjectRefresh',
      'delayCalculationStart': 'onProjectDelayCalculationStart',
      'delayCalculationEnd': 'onProjectDelayCalculationEnd',
      'repopulateReplica': 'onRepopulateReplica',
      'thisObj': this
    });
  }
  updateProject(a) {
    this.bindProject(a);
    this.crudManager = a;
    this.store = a.resourceStore;
  }
  getRowHeight() {
    return this.rowHeight;
  }
  convertEffortUnit(a, b, c) {
    return this.project.run('$convertDuration', a, b, c);
  }
  updateUseProjectTimeUnitsForScale() {
    const a = this;
    if (a.isPainted) {
      const b = { 'scalePoints': a.generateScalePoints() };
      a.trigger('generateScalePoints', b);
      a._generatedScalePoints = a.scalePoints = b.scalePoints;
      a.scheduleRefreshRows();
    }
  }
  updateShowBarText(a) {
    this.scheduleRefreshRows();
  }
  get eventStore() {
    let a;
    return (a = this.project) == null ? void 0 : a.eventStore;
  }
  set eventStore(a) {
    super.eventStore = a;
  }
  convertUnitsToHistogramValue(a, b) {
    return this.useProjectTimeUnitsForScale ? this.convertEffortUnit(a, b, TimeUnit.Millisecond) : DateHelper.asMilliseconds(a, b);
  }
  convertHistogramValueToUnits(a, b) {
    return this.useProjectTimeUnitsForScale ? this.convertEffortUnit(a, TimeUnit.Millisecond, b) : DateHelper.as(b, a);
  }
  buildScalePointText(a) {
    return '' + a.value + DateHelper.getShortNameOfUnit(a.unit);
  }
  generateScalePoints(a, b) {
    let c;
    if (!((c = this.project) == null ? void 0 : c.graph)) {
      return;
    }
    const {timeAxis: e} = this, f = [];
    a = a || e.increment;
    b = b || e.unit;
    let g;
    if (a === 1) {
      b = DateHelper.getSmallerUnit(b);
      a = Math.round(this.useProjectTimeUnitsForScale ? this.convertEffortUnit(a, e.unit, b) : DateHelper.as(b, a, e.unit));
    }
    for (const j of [
        7,
        5,
        4,
        3,
        2
      ]) {
      if (!(a % j)) {
        g = a / j;
        break;
      }
    }
    if (!g) {
      g = a;
    }
    for (let k = g; k <= a; k += g) {
      f.push({ 'value': k });
    }
    const i = f[f.length - 1];
    i.unit = b;
    i.text = this.buildScalePointText(i);
    return f;
  }
  updateViewPreset(a) {
    const b = this;
    b._updatingViewPreset = true;
    super.updateViewPreset(...arguments);
    b._updatingViewPreset = false;
    if (b.project.isInitialCommitPerformed && b.isPainted) {
      b.onTimeAxisEndReconfigure();
    }
  }
  onRepopulateReplica() {
    this.ticksIdentifier = null;
    this.clearHistogramDataCache();
  }
  buildTicksIdentifier() {
    const a = this, b = a.project.getGraph();
    if (!a.ticksIdentifier) {
      a.ticksIdentifier = b.addIdentifier(CalculatedValueGen.new());
    } else {
      const c = b.read(a.ticksIdentifier);
      a.project.clearCombinationsWith(c);
    }
    a.ticksIdentifier.writeToGraph(b, new BaseCalendarMixin({
      'unspecifiedTimeIsWorking': false,
      'intervals': a.timeAxis.ticks.map(e => {
        return {
          'startDate': e.startDate,
          'endDate': e.endDate,
          'isWorking': true
        };
      })
    }));
    a.collectTicksWidth();
    return a.ticksIdentifier;
  }
  onProjectDelayCalculationStart() {
    this.suspendRefresh();
  }
  onProjectDelayCalculationEnd() {
    this.resumeRefresh(false);
  }
  projectUnitsHasChanged() {
    const {project: a} = this;
    return a.daysPerMonth !== this._projectDaysPerMonth || a.daysPerWeek !== this._projectDaysPerWeek || a.hoursPerDay !== this._projectHoursPerDay;
  }
  internalOnProjectRefresh({
    source: a,
    isInitialCommit: b,
    isCalculated: c
  }) {
    if (c) {
      const e = this;
      if (!e.ticksIdentifier) {
        e.onTimeAxisEndReconfigure();
      }
      if (e.useProjectTimeUnitsForScale && e.projectUnitsHasChanged()) {
        e._projectDaysPerMonth = a.daysPerMonth;
        e._projectDaysPerWeek = a.daysPerWeek;
        e._projectHoursPerDay = a.hoursPerDay;
        const f = { 'scalePoints': e.generateScalePoints() };
        e.trigger('generateScalePoints', f);
        e._generatedScalePoints = e.scalePoints = f.scalePoints;
      }
      if (!e.rowManager.topRow || e.rowManager.topRow.id == null) {
        e.rowManager.reinitialize();
      } else if (b) {
        e.refreshRows();
      }
    }
  }
  relayStoreDataChange(a) {
    super.relayStoreDataChange(a);
    if (this.store.count === 0) {
      this.histogramWidget.data = [];
      this.histogramWidget.refresh();
    }
  }
  changeHistogramWidget(a, b) {
    const c = this;
    if (!b) {
      const {series: e} = c;
      if (!c.showMaxEffort && e.maxEffort) {
        e.maxEffort = false;
      }
      a = super.changeHistogramWidget(...arguments);
    }
    return a;
  }
  getBarDOMConfig(a, b, c, e) {
    if (c.inEventTimeSpan) {
      const {topValue: f} = this, g = c[a.field], i = a.stretch ? 1 : c.height;
      b.height = i || (g > f ? f : g) / f;
      b.y = 1 - b.height;
      return b;
    }
  }
  getBarClassDefault(a, b, c) {
    if (a.id === 'effort') {
      switch (true) {
      case c.isOverallocated:
        return 'b-overallocated';
      case c.isUnderallocated:
        return 'b-underallocated';
      }
    }
    return '';
  }
  get effortFormatter() {
    const a = this, b = a.effortFormat;
    let c = a._effortFormatter;
    if (!c || a._effortFormat !== b) {
      c = NumberFormat.get(a._lastFormat = b);
      a._effortFormatter = c;
    }
    return c;
  }
  getEffortText(a, b, c = this.showEffortUnit) {
    let e;
    if (!((e = this.project) == null ? void 0 : e.graph)) {
      return;
    }
    const {
      scaleUnit: f,
      effortFormatter: g
    } = this;
    b = b || f;
    const i = DateHelper.getShortNameOfUnit(b), j = this.convertHistogramValueToUnits(a, b);
    return g.format(j) + (c ? i : '');
  }
  getBarTipEffortUnit() {
    const {
        effortUnit: a,
        barTipEffortUnit: b,
        timeAxis: c
      } = this, e = b || a;
    return DateHelper.compareUnits(c.unit, e) < 0 ? c.unit : e;
  }
  getGroupBarTip({datum: a}) {
    const b = this, {timeAxis: c} = b;
    let e = '';
    if (a.inEventTimeSpan) {
      const f = b.getBarTipEffortUnit(...arguments), g = b.getEffortText(a.effort, f), i = b.getEffortText(a.maxEffort, f), j = b.L('L{groupBarTipAssignment}');
      let k = 'L', l = b.L('L{groupBarTipInRange}'), n = '';
      if (DateHelper.compareUnits(c.unit, TimeUnit.Day) === 0) {
        l = b.L('L{groupBarTipOnDate}');
      } else if (DateHelper.compareUnits(c.unit, TimeUnit.Second) <= 0) {
        k = 'HH:mm:ss A';
      } else if (DateHelper.compareUnits(c.unit, TimeUnit.Hour) <= 0) {
        k = 'LT';
      }
      let o = [...a.resourceAllocation.entries()].filter(([q, r]) => r.effort).sort(([q, r], [u, v]) => r.effort > v.effort ? -1 : 1);
      if (o.length > b.groupBarTipAssignmentLimit) {
        n = '<br>' + b.L('L{plusMore}').replace('{value}', o.length - b.groupBarTipAssignmentLimit);
        o = o.slice(0, this.groupBarTipAssignmentLimit);
      }
      const p = o.map(([q, r]) => {
        return j.replace('{resource}', StringHelper.encodeHtml(q.name)).replace('{allocated}', b.getEffortText(r.effort, f)).replace('{available}', b.getEffortText(r.maxEffort, f)).replace('{cls}', r.isOverallocated ? 'b-overallocated' : r.isUnderallocated ? 'b-underallocated' : '');
      }).join('<br>') + n;
      e = l.replace('{assignments}', p).replace('{startDate}', DateHelper.format(a.tick.startDate, k)).replace('{endDate}', DateHelper.format(a.tick.endDate, k)).replace('{allocated}', g).replace('{available}', i).replace('{cls}', a.isOverallocated ? 'b-overallocated' : a.isUnderallocated ? 'b-underallocated' : '');
      e = '<div class="b-histogram-bar-tooltip">' + e + '</div>';
    }
    return e;
  }
  getResourceBarTip({datum: a}) {
    const b = this, {timeAxis: c} = b;
    let e = '';
    if (a.inEventTimeSpan) {
      const f = b.getBarTipEffortUnit(), g = b.getEffortText(a.effort, f), i = b.getEffortText(a.maxEffort, f);
      let j = 'L', k = b.L('L{barTipInRange}');
      if (DateHelper.compareUnits(c.unit, TimeUnit.Day) === 0) {
        k = b.L('L{barTipOnDate}');
      } else if (DateHelper.compareUnits(c.unit, TimeUnit.Second) <= 0) {
        j = 'HH:mm:ss A';
      } else if (DateHelper.compareUnits(c.unit, TimeUnit.Hour) <= 0) {
        j = 'LT';
      }
      e = k.replace('{startDate}', DateHelper.format(a.tick.startDate, j)).replace('{endDate}', DateHelper.format(a.tick.endDate, j)).replace('{allocated}', g).replace('{available}', i).replace('{cls}', a.isOverallocated ? 'b-overallocated' : a.isUnderallocated ? 'b-underallocated' : '');
      if (a.resource) {
        e = e.replace('{resource}', StringHelper.encodeHtml(a.resource.name));
      }
      e = '<div class="b-histogram-bar-tooltip">' + e + '</div>';
    }
    return e;
  }
  getBarTextEffortUnit() {
    const {
        effortUnit: a,
        barTextEffortUnit: b,
        timeAxis: c
      } = this, e = b || a;
    return DateHelper.compareUnits(c.unit, e) < 0 ? c.unit : e;
  }
  getBarTextDefault(a, b) {
    const c = this.owner, {showBarText: e} = c;
    let f = '';
    if (e && a.inEventTimeSpan) {
      const g = c.getBarTextEffortUnit();
      f = c.getEffortText(a.effort, g);
    }
    return f;
  }
  updateShowMaxEffort(a) {
    const b = this;
    b._showMaxEffort = a;
    const c = b._histogramWidget;
    if (!c) {
      return;
    }
    const {series: e} = b;
    if (!a) {
      if (e.maxEffort) {
        b._seriesMaxEffort = e.maxEffort;
        e.maxEffort = false;
      }
    } else if (typeof a === 'object') {
      e.maxEffort = a;
    } else if (typeof b._seriesMaxEffort === 'object') {
      e.maxEffort = b._seriesMaxEffort;
    } else {
      e.maxEffort = {
        'id': 'maxEffort',
        'type': 'outline',
        'field': 'maxEffort'
      };
    }
    b.scheduleRefreshRows();
  }
  updateIncludeInactiveEvents(a) {
    let b;
    (b = this.getHistogramDataCache()) == null ? void 0 : b.forEach(c => c.includeInactiveEvents = a);
  }
  onTimeAxisEndReconfigureInternal() {
    const a = this;
    if (!a._updatingViewPreset) {
      const {
        unit: b,
        increment: c
      } = a.timeAxis;
      if (b !== a._lastTimeAxisUnit || c !== a._lastTimeAxisIncrement) {
        a._lastTimeAxisUnit = b;
        a._lastTimeAxisIncrement = c;
        const e = a.generateScalePoints(), f = { 'scalePoints': e };
        a.trigger('generateScalePoints', f);
        a._generatedScalePoints = a.scalePoints = f.scalePoints;
      }
      a.buildTicksIdentifier();
    }
  }
  calculateRowHeights() {
  }
  onTimeAxisEndReconfigure() {
    const a = this;
    if (!a._updatingViewPreset) {
      if (a.project.graph) {
        a.onTimeAxisEndReconfigureInternal();
      } else {
        a.project.ion({
          'graphReady'() {
            a.onTimeAxisEndReconfigureInternal();
          },
          'thisObj': a,
          'once': true
        });
      }
    }
  }
  extractHistogramDataArray(a, b) {
    return a.allocation.total;
  }
  renderRows() {
    const a = this;
    if (!a.ticksIdentifier && a.project.isInitialCommitPerformed) {
      a.onTimeAxisEndReconfigure();
      if (!a.timeView.startDate || !a.timeView.endDate) {
        a.timeView.range = {
          'startDate': a.startDate,
          'endDate': a.endDate
        };
      }
    }
    return super.renderRows(...arguments);
  }
  awaitScheduledRecordsRender(a) {
    return Promise.all(a.map(b => this.await('scheduledRecordsRender', {
      'checkLog': false,
      'args': ({records: c}) => c.includes(b)
    })));
  }
  awaitScheduledRowsRender(a = this.rowManager.rows) {
    return Promise.all(a.map(b => this.await('scheduledRecordsRender', {
      'checkLog': false,
      'args': ({rows: c}) => c.includes(b)
    })));
  }
  shiftPrevious() {
    super.shiftPrevious(...arguments);
    return Promise.all([
      this.project.commitAsync(),
      this.awaitScheduledRowsRender()
    ]);
  }
  shiftNext() {
    super.shiftNext(...arguments);
    return Promise.all([
      this.project.commitAsync(),
      this.awaitScheduledRowsRender()
    ]);
  }
  async zoomToLevel() {
    const a = super.zoomToLevel(...arguments);
    await Promise.all([
      this.project.commitAsync(),
      this.awaitScheduledRowsRender()
    ]);
    return a;
  }
  onCommitAsyncCompletion() {
    if (!this.isDestroyed) {
      this.scheduleRecordRefresh();
      this._renderOnCommitPromise = null;
    }
  }
  onRecordAllocationCalculated(a) {
    if (!this.isDestroying) {
      this.setHistogramDataCache(a.resource, a.owner);
    }
  }
  setHistogramDataCache(a, b) {
    let c;
    const e = this.histogramDataByRecord.get(a);
    if (e !== void 0 && e !== b && a.isResourceModel) {
      (c = e.graph) == null ? void 0 : c.removeEntity(e);
    }
    super.setHistogramDataCache(a, b);
  }
  buildResourceAllocationReport(a) {
    return this.project.resourceAllocationInfoClass.new({
      'includeInactiveEvents': this.includeInactiveEvents,
      'ticks': this.ticksIdentifier,
      'resource': a
    });
  }
  async getRecordAllocationData(a) {
    const b = this, {project: c} = b;
    while (!b.ticksIdentifier || !c.isInitialCommitPerformed) {
      await c.await('commitFinalized');
    }
    if (a.graph) {
      const e = c.getGraph(), f = b.buildResourceAllocationReport(a);
      a.addEntity(f);
      await e.readAsync(f.$.allocation);
      e.ongoing.then(() => {
        e.addListener(f.$.allocation, b.onRecordAllocationCalculated);
      });
      return f;
    }
  }
  scheduleOnCommitAsyncCompletionCall() {
    const a = this;
    if (!a._renderOnCommitPromise) {
      a._renderOnCommitPromise = a.project.graph.ongoing.then(a.onCommitAsyncCompletion);
    }
  }
  onHistogramDataCacheSet() {
    super.onHistogramDataCacheSet(...arguments);
    this.scheduleOnCommitAsyncCompletionCall();
  }
  aggregateRecordsHistogramData(a, b) {
    const c = super.aggregateRecordsHistogramData(a, b);
    if (Objects.isPromise(c)) {
      return c.then(buildReturnedValue);
    }
    return buildReturnedValue(c);
  }
  initAggregatedAllocationEntry() {
    return {
      'tick': null,
      'effort': 0,
      'maxEffort': 0,
      'units': 0,
      'isGroup': true,
      'inEventTimeSpan': false,
      'resourceAllocation': new Map()
    };
  }
  aggregateAllocationEntry(a, b, c, e, f) {
    const {resourceAllocation: g} = a;
    a.tick = b.tick;
    a.isOverallocated = a.isOverallocated || b.isOverallocated;
    a.isUnderallocated = a.isUnderallocated || b.isUnderallocated;
    a.inEventTimeSpan = a.inEventTimeSpan || b.inEventTimeSpan;
    if (b.assignments) {
      if (a.assignments) {
        b.assignments.forEach(i => a.assignments.add(i));
      } else {
        a.assignments = new Set(b.assignments);
      }
    }
    if (b.resource) {
      g.set(b.resource, b);
    }
    if (b.resourceAllocation) {
      for (const [i, j] of b.resourceAllocation.entries()) {
        g.set(i, j);
      }
    }
    return a;
  }
  generateGroupScalePoints(a) {
    const b = this, c = b.getGroupChildren(a), e = b.generateScalePoints(b.timeAxis.increment * c.reduce((g, i) => g + i.maxUnits / 100, 0)), f = {
        'scalePoints': e,
        'groupParent': a
      };
    b.trigger('generateScalePoints', f);
    return f.scalePoints;
  }
  processRecordRenderData(a) {
    const b = this, {record: c} = a, e = b.isGroupRecord(c), f = c.isResourceModel && c.maxUnits !== 100;
    if (e || f) {
      const g = e ? b.generateGroupScalePoints(c) : b.generateScalePoints(b.timeAxis.increment * c.maxUnits / 100), i = b.getTopValueByScalePoints(g);
      a.scaleWidgetConfig = { 'scalePoints': g };
      a.histogramConfig = {
        ...a.histogramConfig,
        'topValue': i
      };
    } else {
      a = super.processRecordRenderData(a);
    }
    return a;
  }
  updateLocalization() {
    const a = this;
    if (a._generatedScalePoints === a.scalePoints && a.scalePoints) {
      a.scalePoints.forEach(b => {
        if (b.text && b.unit) {
          b.text = a.buildScalePointText(b);
        }
      });
    }
    super.updateLocalization(...arguments);
  }
};
ResourceHistogram.initClass();
ResourceHistogram._$name = 'ResourceHistogram';
const _ResourceUtilization = class _ResourceUtilization extends ResourceHistogram {
  static configurable = {
    'scaleColumn': null,
    'timeCellCls': 'b-sch-timeaxis-cell b-resourceutilization-cell',
    'rowHeight': 30,
    'showEffortUnit': false,
    'showMaxEffort': false,
    'showBarText': true,
    'barTooltipTemplate'({datum: a}) {
      let b = '';
      const {
        inEventTimeSpan: c,
        isGroup: e,
        resource: f,
        assignment: g
      } = a;
      if (c) {
        if (e) {
          b = this.getGroupBarTip(...arguments);
        } else if (g) {
          b = this.getAssignmentBarTip(...arguments);
        } else if (f) {
          b = this.getResourceBarTip(...arguments);
        }
      }
      return b;
    },
    'series': { 'effort': { 'stretch': true } },
    'readOnly': true,
    'columns': [{
        'type': 'tree',
        'field': 'name',
        'text': 'L{nameColumnText}',
        'localeClass': _ResourceUtilization
      }],
    'histogramWidget': { 'cls': 'b-hide-offscreen b-resourceutilization-histogram' }
  };
  static type = 'resourceutilization';
  static $name = 'ResourceUtilization';
  construct() {
    super.construct(...arguments);
    this.rowManager.ion({
      'renderRow': 'onRowManagerRenderRow',
      'thisObj': this
    });
  }
  updateProject(a) {
    const b = this.store;
    super.updateProject(a);
    this.store = b || this.buildStore(a);
  }
  updateResourceStore(a) {
    this._resourceStore = a;
  }
  buildStore(a) {
    a = this.project;
    return ResourceUtilizationStore.new({ 'project': a });
  }
  async getTipHtml(a) {
    const b = a.activeTarget.dataset.index, c = this.getRecordFromElement(a.activeTarget), e = await this.getRecordHistogramData(c), f = this.extractHistogramDataArray(e, c)[parseInt(b, 10)];
    return this.barTooltipTemplate({
      ...a,
      'record': c,
      'index': b,
      'datum': f
    });
  }
  getRecordAllocationData(a, ...b) {
    a = this.resolveRecordToOrigin(a);
    if (a.isResourceModel) {
      return super.getRecordAllocationData(a, ...b);
    } else if (a.isAssignmentModel) {
      return this.getRecordHistogramData(a.resource, ...b);
    }
  }
  getRecordHistogramData(a) {
    const b = super.getRecordHistogramData(...arguments), c = this.resolveRecordToOrigin(a);
    if (c.isAssignmentModel) {
      if (Objects.isPromise(b)) {
        return b.then(e => {
          let f;
          return (f = this.getRecordHistogramDataFinalize) == null ? void 0 : f.call(this, e, a);
        });
      }
      return this.getRecordHistogramDataFinalize(b, a);
    }
    return b;
  }
  getRecordHistogramDataFinalize(a, b) {
    let c, e;
    const f = this.resolveRecordToOrigin(b), {allocation: g} = a;
    if (f.isAssignmentModel && (f.resource !== a.resource || ((e = (c = g.byAssignments) == null ? void 0 : c.get(f)) == null ? void 0 : e.length) !== g.total.length)) {
      this.clearHistogramDataCache([
        b,
        f
      ]);
      return this.project.await('commitFinalized', { 'checkLog': false }).then(() => {
        let i;
        return (i = this.getRecordHistogramData) == null ? void 0 : i.call(this, b);
      });
    }
    return a;
  }
  onDestroy() {
    let a;
    if (this.destroyStores) {
      (a = this.store) == null ? void 0 : a.destroy();
    }
    super.onDestroy();
  }
  onRecordAllocationCalculated(a) {
    const b = this;
    if (!b.isDestroying) {
      const c = a.owner, e = (a.byAssignments ? [...a.byAssignments.keys()] : []).reduce((g, i) => {
          const j = b.resolveOriginToRecord(i);
          if (j) {
            g.push(j);
          }
          return g;
        }, []), f = b.resolveOriginToRecord(a.resource);
      if (f) {
        b.setHistogramDataCache(f, c);
      }
      for (const g of e) {
        b.setHistogramDataCache(g, c);
      }
      b.scheduleOnCommitAsyncCompletionCall();
    }
  }
  onRowManagerRenderRow({
    row: a,
    record: b
  }) {
    const c = this.isGroupRecord(b);
    b = this.resolveRecordToOrigin(b);
    a.assignCls({
      'b-resource-row': b.isResourceModel || c,
      'b-assignment-row': !c && b.isAssignmentModel
    });
  }
  awaitScheduledRecordsRender(a) {
    return Promise.all(a.map(b => this.await('scheduledRecordsRender', {
      'checkLog': false,
      'args': ({records: c}) => c.includes(this.resolveOriginToRecord(b))
    })));
  }
  resolveRecordToOrigin(a) {
    return a.origin || a.$original || a;
  }
  resolveOriginToRecord(a) {
    let b = a;
    if (this.store.isResourceUtilizationStore) {
      b = this.store.getModelByOrigin(a) || b;
    } else if (a.hasLinks) {
      for (const c of a.$links) {
        if (this.store.includes(c)) {
          return c;
        }
      }
    }
    return b;
  }
  getHistogramDataCache(a) {
    if (a) {
      a = this.resolveRecordToOrigin(a);
    }
    return super.getHistogramDataCache(a);
  }
  setHistogramDataCache(a, b) {
    a = this.resolveRecordToOrigin(a);
    return super.setHistogramDataCache(a, b);
  }
  scheduleRecordRefresh(a) {
    a = this.resolveOriginToRecord(a);
    return super.scheduleRecordRefresh(a);
  }
  scheduleRecordParentsRefresh(a) {
    a = this.resolveOriginToRecord(a);
    return super.scheduleRecordParentsRefresh(a);
  }
  getCell(a) {
    let b, c;
    if (((b = a.record) == null ? void 0 : b.isResourceModel) || ((c = a.record) == null ? void 0 : c.isAssignmentModel)) {
      a.record = this.resolveOriginToRecord(a.record);
    }
    return super.getCell(a);
  }
  changeHistogramWidget(a) {
    if (a && !a.isHistogram) {
      const b = a.getBarTextDOMConfig;
      if (!this.getBarTextDOMConfig && !b) {
        a.getBarTextDOMConfig = this.getBarTextDOMConfigDefault;
      }
      a.height = this.rowHeight;
    }
    return super.changeHistogramWidget(a);
  }
  getBarTextDOMConfigDefault(a, b, c) {
    a.y = '50%';
    return a;
  }
  extractHistogramDataArray(a, b) {
    let c;
    const e = this.resolveRecordToOrigin(b);
    if (this.isGroupRecord(b)) {
      c = a.allocation.total;
    } else if (e.isResourceModel) {
      c = a.allocation.total;
    } else if (e.isAssignmentModel) {
      c = a.allocation.byAssignments.get(e) || [];
    }
    return c;
  }
  initAggregatedAllocationEntry(a, b) {
    if (!b.targetArray.$resources) {
      b.targetArray.$resources = new Set();
    }
    return {
      'tick': null,
      'effort': 0,
      'maxEffort': 0,
      'units': 0,
      'isGroup': true,
      'inEventTimeSpan': false,
      'members': new Map(),
      'resources': new Set()
    };
  }
  aggregateAllocationEntry(a, b, c, e, f) {
    const {targetArray: g} = f, i = f.arrays[c];
    a.tick = b.tick;
    a.isOverallocated = a.isOverallocated || b.isOverallocated;
    a.isUnderallocated = a.isUnderallocated || b.isUnderallocated;
    a.inEventTimeSpan = a.inEventTimeSpan || b.inEventTimeSpan;
    if (b.members) {
      a.members = new Map([
        ...a.members,
        ...b.members
      ]);
    } else {
      a.members.set(b.resource || b.assignment, b);
    }
    if (i.$resources) {
      g.$resources = new Set([
        ...g.$resources,
        ...i.$resources
      ]);
    } else if (b.assignment || b.resource) {
      const j = b.resource || b.assignment.resource;
      g.$resources.add(j);
      const k = this.getHistogramDataCache(j).allocation.total[e];
      a.isOverallocated = a.isOverallocated || k.isOverallocated;
      a.isUnderallocated = a.isUnderallocated || k.isUnderallocated;
    }
    return a;
  }
  aggregateHistogramData() {
    const a = super.aggregateHistogramData(...arguments);
    for (let b = 0, c = a.length; b < c; b++) {
      const e = a[b];
      e.maxEffort = 0;
      for (const f of a.$resources) {
        const g = this.getHistogramDataCache(f).allocation.total[b];
        e.maxEffort += g.maxEffort;
      }
    }
    return a;
  }
  getResourceBarTip({datum: a}) {
    const b = this, {
        showBarTip: c,
        timeAxis: e
      } = b;
    let f = '';
    if (c && a.inEventTimeSpan) {
      const g = b.getBarTipEffortUnit(...arguments), i = b.getEffortText(a.effort, g, true), j = b.getEffortText(a.maxEffort, g, true), k = b.L('L{groupBarTipAssignment}');
      let l = 'L', n = b.L('L{groupBarTipInRange}'), o = '';
      if (DateHelper.compareUnits(e.unit, TimeUnit.Day) === 0) {
        n = b.L('L{groupBarTipOnDate}');
      } else if (DateHelper.compareUnits(e.unit, TimeUnit.Second) <= 0) {
        l = 'HH:mm:ss A';
      } else if (DateHelper.compareUnits(e.unit, TimeUnit.Hour) <= 0) {
        l = 'LT';
      }
      let p = [...a.assignmentIntervals.entries()].filter(([r, u]) => u.effort).sort(([r, u], [v, w]) => u.effort > w.effort ? -1 : 1);
      if (p.length > b.groupBarTipAssignmentLimit) {
        o = '<br>' + b.L('L{plusMore}').replace('{value}', p.length - b.groupBarTipAssignmentLimit);
        p = p.slice(0, this.groupBarTipAssignmentLimit);
      }
      const q = p.map(([r, u]) => {
        return k.replace('{event}', StringHelper.encodeHtml(r.event.name)).replace('{allocated}', b.getEffortText(u.effort, g, true)).replace('{available}', b.getEffortText(u.maxEffort, g, true)).replace('{cls}', u.isOverallocated ? 'b-overallocated' : u.isUnderallocated ? 'b-underallocated' : '');
      }).join('<br>') + o;
      f = n.replace('{assignments}', q).replace('{startDate}', DateHelper.format(a.tick.startDate, l)).replace('{endDate}', DateHelper.format(a.tick.endDate, l)).replace('{allocated}', i).replace('{available}', j).replace('{cls}', a.isOverallocated ? 'b-overallocated' : a.isUnderallocated ? 'b-underallocated' : '');
      f = '<div class="b-histogram-bar-tooltip">' + f + '</div>';
    }
    return f;
  }
  getGroupBarTip({datum: a}) {
    const b = this, {
        showBarTip: c,
        timeAxis: e
      } = b;
    let f = '';
    if (c && a.inEventTimeSpan) {
      const g = b.getBarTipEffortUnit(...arguments), i = b.getEffortText(a.effort, g), j = b.getEffortText(a.maxEffort, g), k = b.L('L{groupBarTipAssignment}');
      let l = 'L', n = b.L('L{groupBarTipInRange}'), o = '';
      if (DateHelper.compareUnits(e.unit, TimeUnit.Day) === 0) {
        n = b.L('L{groupBarTipOnDate}');
      } else if (DateHelper.compareUnits(e.unit, TimeUnit.Second) <= 0) {
        l = 'HH:mm:ss A';
      } else if (DateHelper.compareUnits(e.unit, TimeUnit.Hour) <= 0) {
        l = 'LT';
      }
      let p = [...a.members.entries()].filter(([r, u]) => u.effort).sort(([r, u], [v, w]) => u.effort > w.effort ? -1 : 1);
      if (p.length > b.groupBarTipAssignmentLimit) {
        o = '<br>' + b.L('L{plusMore}').replace('{value}', p.length - b.groupBarTipAssignmentLimit);
        p = p.slice(0, this.groupBarTipAssignmentLimit);
      }
      const q = p.map(([r, u]) => {
        let v, w;
        return k.replace('{resource}', StringHelper.encodeHtml(((v = r.resource) == null ? void 0 : v.name) || r.name)).replace('{event}', StringHelper.encodeHtml(((w = r.event) == null ? void 0 : w.name) || r.name)).replace('{allocated}', b.getEffortText(u.effort, g)).replace('{available}', b.getEffortText(u.maxEffort, g)).replace('{cls}', u.isOverallocated ? 'b-overallocated' : u.isUnderallocated ? 'b-underallocated' : '');
      }).join('<br>') + o;
      f = n.replace('{assignments}', q).replace('{startDate}', DateHelper.format(a.tick.startDate, l)).replace('{endDate}', DateHelper.format(a.tick.endDate, l)).replace('{allocated}', i).replace('{available}', j).replace('{cls}', a.isOverallocated ? 'b-overallocated' : a.isUnderallocated ? 'b-underallocated' : '');
      f = '<div class="b-histogram-bar-tooltip">' + f + '</div>';
    }
    return f;
  }
  getAssignmentBarTip({datum: a}) {
    const b = this, {
        showBarTip: c,
        timeAxis: e
      } = b;
    let f = '';
    if (c && a.inEventTimeSpan) {
      const g = b.getBarTipEffortUnit(...arguments), i = b.getEffortText(a.effort, g, true), j = b.getEffortText(a.maxEffort, g, true);
      let k = 'L', l = b.L('L{barTipInRange}');
      if (DateHelper.compareUnits(e.unit, TimeUnit.Day) === 0) {
        l = b.L('L{barTipOnDate}');
      } else if (DateHelper.compareUnits(e.unit, TimeUnit.Second) <= 0) {
        k = 'HH:mm:ss A';
      } else if (DateHelper.compareUnits(e.unit, TimeUnit.Hour) <= 0) {
        k = 'LT';
      }
      f = l.replace('{startDate}', DateHelper.format(a.tick.startDate, k)).replace('{endDate}', DateHelper.format(a.tick.endDate, k)).replace('{allocated}', i).replace('{available}', j).replace('{cls}', a.cls || '');
      if (a.assignment) {
        f = f.replace('{event}', StringHelper.encodeHtml(a.assignment.event.name));
      }
      f = '<div class="b-histogram-bar-tooltip">' + f + '</div>';
    }
    return f;
  }
};
const ResourceUtilization = _ResourceUtilization;
ResourceUtilization.initClass();
ResourceUtilization._$name = 'ResourceUtilization';
GridFeatureManager.registerFeature(Tree, true, 'ResourceUtilization');
const ProHorizontalRendering = class extends HorizontalRendering {
  static $name = 'ProHorizontalRendering';
  fillInnerSpanHorizontalPosition(a) {
    const b = this, {eventRecord: c} = a, {
        elementStartMS: e,
        elementEndMS: f,
        elementDurationMS: g
      } = b.calculateMS(c, 'startDate', 'endDate'), i = b.calculateHorizontalPosition(a, e, f, g);
    if (i) {
      const {
          left: j,
          width: k
        } = i, l = Math.max(j - a.left, 0), n = Math.max(Math.floor(a.left + a.width - j - k), 0);
      Object.assign(a, {
        'innerStartMS': e,
        'innerEndMS': f,
        'innerDurationMS': g,
        'bufferBeforeWidth': l,
        'bufferAfterWidth': n
      });
      return true;
    }
    return false;
  }
  getTimeSpanRenderData(a, b, c = false) {
    const e = super.getTimeSpanRenderData(a, b, c);
    if ((e == null ? void 0 : e.useEventBuffer) && !this.fillInnerSpanHorizontalPosition(e)) {
      return null;
    }
    return e;
  }
};
ProHorizontalRendering._$name = 'ProHorizontalRendering';
const ProVerticalLayout = class extends VerticalLayout {
  static get configurable() {
    return {
      'type': null,
      'layoutFn': null
    };
  }
  applyLayout(a, b, c) {
    let e;
    const f = this, {scheduler: g} = f, i = g.getEventLayout(b, c);
    if (i.type === 'layoutFn') {
      const j = b.instanceMeta(g), k = i.layoutFn(a, b, g);
      if (k && k !== j.columnWidth) {
        const l = (e = j.columnWidth) != null ? e : g.getResourceWidth(b);
        j.columnWidth = k;
        g.onVisibleResourceColumnChange(k, l);
      }
    } else {
      return super.applyLayout(a, b, c);
    }
  }
};
ProVerticalLayout._$name = 'ProVerticalLayout';
const ProVerticalRendering = class extends VerticalRendering {
  static $name = 'ProVerticalRendering';
  construct(a) {
    super.construct(a);
    this.verticalLayout = new ProVerticalLayout({ 'scheduler': a });
  }
  fillInnerSpanVerticalPosition(a) {
    const b = this, {scheduler: c} = b, {eventRecord: e} = a, {isBatchUpdating: f} = e, g = f ? e.get('startDate') : e.startDate, i = f ? e.get('endDate') : e.endDate, j = c.getCoordinateFromDate(g), k = g.getTime(), l = i.getTime(), n = l - k;
    let o = c.getCoordinateFromDate(i), p = o - j;
    if (o === -1) {
      p = Math.round(n * c.timeAxisViewModel.getSingleUnitInPixels('millisecond'));
      o = j + p;
    }
    Object.assign(a, {
      'innerStartMS': k,
      'innerEndMS': l,
      'innerDurationMS': n,
      'bufferBeforeWidth': j - a.top,
      'bufferAfterWidth': a.top + a.height - j - p
    });
    return true;
  }
  getTimeSpanRenderData(a, b, c = false) {
    const e = super.getTimeSpanRenderData(a, b, c);
    if (e == null ? void 0 : e.useEventBuffer) {
      if (!this.fillInnerSpanVerticalPosition(e)) {
        return null;
      }
    }
    return e;
  }
  renderResourceCustomLayout(a) {
    const b = this, {scheduler: c} = b, {resourceColumns: e} = b.scheduler, {allResourceRecords: f} = b, {first: g} = b.resourceRange, i = g === 0 ? 0 : f[g].instanceMeta(c).insetStart, j = c.timeAxisSubGridElement.clientWidth;
    let k = g, l = 0, n = true;
    while (n) {
      const o = f[k];
      a.push.apply(a, b.renderResource(o));
      l += o.instanceMeta(c).columnWidth;
      if (l > j + f[g].instanceMeta(c).columnWidth || k === f.length - 1) {
        n = false;
      } else {
        k++;
        f[k].instanceMeta(c).insetStart = l + i;
      }
    }
    e.visibleResources = {
      'firstResource': g,
      'lastResource': k
    };
    e.updateWidthCache();
    e.refresh();
  }
};
ProVerticalRendering._$name = 'ProVerticalRendering';
const SchedulerProBase = class extends SchedulerBase.mixin(ProjectProgressMixin_default, SchedulerProEventRendering_default, SchedulingIssueResolution_default) {
  static isSchedulerPro = true;
  static configurable = {
    'projectModelClass': ProjectModel2,
    'dependencyIdField': 'id',
    'showTaskColorPickers': false
  };
  static type = 'schedulerprobase';
  static $name = 'SchedulerProBase';
  onPaintOverride() {
  }
  set calendars(a) {
    this.project.calendars = a;
  }
  get calendars() {
    return this.project.calendars;
  }
  get mode() {
    return this._mode;
  }
  set mode(a) {
    const b = this;
    b._mode = a;
    if (!b[a]) {
      b.element.classList.add('b-sch-' + a);
      if (a === 'horizontal') {
        b.horizontal = new ProHorizontalRendering(b);
        if (b.isPainted) {
          b.horizontal.init();
        }
      } else if (a === 'vertical') {
        b.vertical = new ProVerticalRendering(b);
        if (b.rendered) {
          b.vertical.init();
        }
      }
    }
  }
  toggleEmptyText() {
    DomHelper.toggleClasses(this.element, 'b-grid-empty', !(this.rowManager.rowCount || this.project.isLoadingOrSyncing));
  }
  get taskStore() {
    return this.project.eventStore;
  }
  createEvent(a, b, c) {
    if (b.isSpecialRow) {
      return;
    }
    ;
    if (this.fillTicks) {
      const e = this.timeAxis.getSnappedTickFromDate(a), f = b.effectiveCalendar.getWorkingTimeRanges(e.startDate, e.endDate);
      if (f.length === 0) {
        return;
      }
      a = f[0].startDate;
    } else if (!b.isWorkingTime(a)) {
      return;
    }
    if (this.taskEdit && this.taskEdit.isEditing) {
      this.ion({
        'aftertaskedit': () => super.createEvent(a, b, c),
        'once': true
      });
    } else {
      return super.createEvent(a, b, c);
    }
  }
};
SchedulerProBase.initClass();
VersionHelper.setVersion('schedulerpro', '6.0.4');
SchedulerProBase._$name = 'SchedulerProBase';
const SchedulerPro = class extends SchedulerProBase {
  static get $name() {
    return 'SchedulerPro';
  }
  static get type() {
    return 'schedulerpro';
  }
};
SchedulerPro.initClass();
SchedulerPro._$name = 'SchedulerPro';
const Timeline = class extends SchedulerBase {
  static delayable = { 'fillFromTaskStore': 100 };
  static configurable = {
    'height': '13em',
    'eventLayout': 'pack',
    'barMargin': 1,
    'forceFit': true,
    'timeAxis': { 'autoAdjust': false },
    'readOnly': true,
    'zoomOnMouseWheel': false,
    'zoomOnTimeAxisDoubleClick': false,
    'rowHeight': 48,
    'displayDateFormat': 'L',
    'milestoneLayoutMode': 'measure',
    'milestoneTextPosition': 'always-outside',
    'resources': [{ 'id': 1 }],
    'columns': []
  };
  static type = 'timeline';
  static $name = 'Timeline';
  construct(a = {}) {
    const b = this;
    b.startDateLabel = document.createElement('label');
    b.startDateLabel.className = 'b-timeline-startdate';
    b.endDateLabel = document.createElement('label');
    b.endDateLabel.className = 'b-timeline-enddate';
    let c = true;
    if ('project' in a) {
      if (!a.project) {
        throw new Error('You need to configure the Timeline with a Project');
      } else if (a.project instanceof SchedulerProProjectMixin) {
        b.taskStore = a.project.eventStore;
        if (!a.project.isInitialCommitPerformed) {
          c = false;
          a.project.ion({
            'name': 'initialCommit',
            'refresh'({isInitialCommit: e}) {
              if (e) {
                b.fillFromTaskStore();
                b.detachListeners('initialCommit');
              }
            },
            'thisObj': b
          });
        }
        delete a.project;
      }
    }
    a.features = ObjectHelper.assign({
      'cellEdit': false,
      'cellMenu': false,
      'columnAutoWidth': false,
      'columnLines': false,
      'columnPicker': false,
      'columnReorder': false,
      'columnResize': false,
      'contextMenu': false,
      'eventContextMenu': false,
      'eventDrag': false,
      'eventDragCreate': false,
      'eventEdit': false,
      'eventFilter': false,
      'eventMenu': false,
      'eventResize': false,
      'eventTooltip': false,
      'group': false,
      'headerMenu': false,
      'regionResize': false,
      'scheduleContextMenu': false,
      'scheduleMenu': false,
      'scheduleTooltip': false,
      'sort': false,
      'timeAxisHeaderMenu': false,
      'timeRanges': false
    }, a.features);
    super.construct(a);
    if (b.features.timeRanges) {
      b.project.timeRangeStore = new Store();
    }
    c && b.fillFromTaskStore.now();
    b.taskStore.ion({
      'refreshPreCommit': b.fillFromTaskStore,
      'changePreCommit': b.onTaskStoreChange,
      'thisObj': b
    });
    b.ion({
      'resize': b.onSizeChanged,
      'thisObj': b
    });
    b.bodyContainer.appendChild(b.startDateLabel);
    b.bodyContainer.appendChild(b.endDateLabel);
  }
  onSizeChanged({
    width: a,
    oldWidth: b
  }) {
    const c = this, e = a !== b;
    e && c.suspendRefresh();
    c.syncRowHeight();
    if (e) {
      c.resumeRefresh(false);
      c.fitTimeline();
    }
  }
  syncRowHeight() {
    if (this.bodyContainer.isConnected) {
      this.rowHeight = this.bodyContainer.offsetHeight;
    }
  }
  fitTimeline() {
    const a = this;
    if (a.eventStore.count > 0) {
      a.forceFit = false;
      a.zoomToFit({
        'leftMargin': 50,
        'rightMargin': 50
      });
      a.forceFit = true;
    }
    a.updateStartEndLabels();
  }
  updateStartEndLabels() {
    const a = this;
    a.startDateLabel.innerHTML = a.getFormattedDate(a.startDate);
    a.endDateLabel.innerHTML = a.getFormattedDate(a.endDate);
  }
  async onTaskStoreChange({
    action: a,
    record: b,
    records: c,
    changes: e,
    isCollapse: f
  }) {
    const g = this, i = g.eventStore;
    let j;
    switch (a) {
    case 'add': {
        const k = [];
        c.forEach(l => {
          if (l.showInTimeline) {
            k.push(g.cloneTask(l));
            j = true;
          }
        });
        i.add(k);
      }
      break;
    case 'remove':
      if (!f) {
        const l = [], n = c.concat(...c.map(o => o.allChildren));
        n.forEach(o => {
          if (o.showInTimeline) {
            l.push(o.id);
            j = true;
          }
        });
        i.remove(l);
      }
      break;
    case 'removeall':
      g.fillFromTaskStore.now();
      break;
    case 'update': {
        const o = b;
        if (e.showInTimeline) {
          if (o.showInTimeline) {
            i.add(g.cloneTask(o));
          } else {
            const p = i.getById(o.id);
            if (p) {
              i.remove(p);
            }
          }
          j = true;
        } else if (o.showInTimeline) {
          const q = i.getById(o.id);
          if (q) {
            q.set(g.cloneTask(o));
            j = true;
          }
        }
        break;
      }
    }
    if (j) {
      g.fitTimeline();
    }
  }
  cloneTask(a) {
    return {
      'id': a.id,
      'resourceId': 1,
      'name': a.name,
      'startDate': a.startDate,
      'endDate': a.endDate,
      'cls': a.cls
    };
  }
  render() {
    super.render(...arguments);
    this.syncRowHeight();
  }
  async fillFromTaskStore() {
    const a = this, b = [];
    a.taskStore.traverse(c => {
      if (c.showInTimeline && c.isScheduled) {
        b.push(a.cloneTask(c));
      }
    });
    a.events = b;
    await a.project.commitAsync();
    if (a.isDestroyed) {
      return;
    }
    a.fitTimeline();
  }
  onLocaleChange() {
    this.updateStartEndLabels();
    super.onLocaleChange();
  }
};
Timeline.initClass();
Timeline._$name = 'Timeline';
const EMPTY_ARRAY2 = [];
const actionTypeOrder = {
  'remove': 1,
  'add': 2,
  'update': 3
};
const entityTypeOrder = {
  'TaskModel': 1,
  'DependencyModel': 2,
  'AssignmentModel': 3,
  'ProjectModel': 4
};
const describePosition = ({
  parent: a,
  index: b
}) => a.name + '[' + b + ']';
const knownEntityTypes = {
  'AssignmentModel': 'Assignment',
  'DependencyModel': 'Dependency'
};
const VersionGridRow = class extends GridRowModel {
  static fields = [
    {
      'name': 'description',
      'type': 'string'
    },
    {
      'name': 'occurredAt',
      'type': 'date'
    },
    { 'name': 'versionModel' },
    { 'name': 'transactionModel' },
    { 'name': 'propertyUpdate' },
    { 'name': 'action' }
  ];
};
const _VersionGrid = class _VersionGrid extends TreeGrid {
  static delayable = {
    'processUpdates': {
      'type': 'raf',
      'cancelOutstanding': true
    }
  };
  static configurable = {
    'store': {
      'tree': true,
      'modelClass': VersionGridRow,
      'sorters': [
        {
          'field': 'occurredAt',
          'ascending': false
        },
        _VersionGrid.sortActionRows
      ],
      'reapplySortersOnAdd': true
    },
    'project': null,
    'showUnattachedTransactions': true,
    'showNamedVersionsOnly': false,
    'showVersions': true,
    'comparingVersionId': null,
    'flex': 0,
    'features': {
      'group': { 'field': 'id' },
      'cellEdit': {
        'continueEditingOnCellClick': false,
        'editNextOnEnterPress': false
      },
      'cellMenu': {
        'items': {
          'removeRow': false,
          'cut': false,
          'copy': false,
          'paste': false,
          'renameButton': {
            'text': 'L{VersionGrid.rename}',
            'icon': 'b-icon b-icon-edit',
            'onItem': ({
              record: a,
              source: b
            }) => {
              b.startEditing({
                'id': a.id,
                'column': b.columns.get('description')
              });
            }
          },
          'restoreButton': {
            'text': 'L{VersionGrid.restore}',
            'icon': 'b-icon b-icon-undo',
            'onItem': ({
              record: a,
              source: b
            }) => {
              b.triggerRestore(a.versionModel);
            }
          },
          'compareButton': {
            'text': 'L{VersionGrid.compare}',
            'icon': 'b-icon b-icon-compare',
            'onItem': ({
              record: a,
              source: b
            }) => {
              b.triggerCompare(a.versionModel);
            }
          },
          'stopComparingButton': {
            'text': 'L{VersionGrid.stopComparing}',
            'onItem': ({
              record: a,
              source: b
            }) => {
              b.triggerStopCompare();
            }
          }
        }
      },
      'rowCopyPaste': false
    },
    'columns': [
      {
        'type': 'tree',
        'text': 'L{VersionGrid.description}',
        'field': 'description',
        'flex': 4,
        'groupable': false,
        'renderer': ({
          grid: a,
          ...b
        }) => a.renderDescription({
          'grid': a,
          ...b
        }),
        'autoHeight': true
      },
      {
        'text': 'L{VersionGrid.occurredAt}',
        'field': 'occurredAt',
        'type': 'date',
        'flex': 1,
        'groupable': false
      }
    ],
    'dateFormat': 'M/D/YY h:mm a',
    'internalListeners': {
      'beforeCellEditStart'({
        editorContext: {
          column: a,
          record: b
        }
      }) {
        if (!(a.field === 'description' && b.versionModel)) {
          return false;
        }
      },
      'finishCellEdit'({
        editorContext: {
          record: a,
          value: b
        }
      }) {
        a.versionModel.name = b != null && b.trim() ? b : null;
      },
      'cellMenuBeforeShow'({
        source: a,
        record: b,
        items: c
      }) {
        c.stopComparingButton.disabled = !a.comparingVersionId;
        return Boolean(b.versionModel);
      },
      'toggleNode'({
        record: a,
        collapse: b
      }) {
        this._expandedById.set(a.id, !b);
      }
    }
  };
  static type = 'versiongrid';
  static $name = 'VersionGrid';
  comparingRowCls = 'b-' + _VersionGrid.type + '-comparing';
  _versionChanges = [];
  _transactionChanges = [];
  constructor() {
    super(...arguments);
  }
  static get properties() {
    return {
      '_rowsByUnderlyingRecord': new WeakMap(),
      '_expandedById': new Map()
    };
  }
  construct(a) {
    super.construct({
      ...a,
      'features': ObjectHelper.merge({}, _VersionGrid.configurable.features, a.features)
    });
  }
  afterConstruct() {
    if (!this.project) {
      throw new Error(_VersionGrid.$name + ' requires the project config.');
    }
    this.refreshGrid();
  }
  updateDateFormat(a) {
    const b = this.columns.get('occurredAt');
    if (b) {
      b.format = a;
    }
  }
  updateProject(a) {
    const b = this;
    b.detachListeners('storeChange');
    b._versionStore = a.getCrudStore('versions');
    b._transactionStore = a.getCrudStore('changelogs');
    b._versionStore.ion({
      'name': 'storeChange',
      'change': b.onVersionStoreChange,
      'thisObj': b
    });
    b._transactionStore.ion({
      'name': 'storeChange',
      'change': b.onTransactionStoreChange,
      'thisObj': b
    });
  }
  updateShowNamedVersionsOnly() {
    if (this.isPainted) {
      this.refreshGrid();
    }
  }
  updateShowUnattachedTransactions() {
    if (this.isPainted) {
      this.refreshGrid();
    }
  }
  updateShowVersions() {
    if (this.isPainted) {
      this.refreshGrid();
    }
  }
  updateComparingVersionId(a, b) {
    const [c, e] = [
      b,
      a
    ].map(f => this.store.getById('v-' + f));
    if (c) {
      c.cls = '';
      c.iconCls = 'b-icon b-icon-version';
    }
    if (e) {
      e.cls = this.comparingRowCls;
      e.iconCls = 'b-icon b-icon-compare';
    }
  }
  onVersionStoreChange({
    action: a,
    records: b
  }) {
    this._versionChanges.push({
      'action': a,
      'records': b
    });
    this.processUpdates();
  }
  onTransactionStoreChange({
    action: a,
    records: b
  }) {
    this._transactionChanges.push({
      'action': a,
      'records': b
    });
    this.processUpdates();
  }
  processUpdates() {
    const a = this, b = ArrayHelper.unique(a._versionChanges.flatMap(({records: f}) => f)), c = new Set(b.map(f => String(f.id))), e = ArrayHelper.unique(a._transactionChanges.flatMap(({records: f}) => f).concat(b.length === 0 ? [] : a._transactionStore.query(f => c.has(f.versionId))));
    for (const f of e) {
      if (f.versionId && !c.has(f.versionId)) {
        b.push(a._versionStore.getById(f.versionId));
        c.add(f.versionId);
      }
    }
    a.replaceRows(ArrayHelper.unique(b), e);
    a._transactionChanges = [];
    a._versionChanges = [];
  }
  replaceRows(a, b) {
    let c;
    const e = this, {
        showNamedVersionsOnly: f,
        showUnattachedTransactions: g,
        store: i
      } = e, j = new Set(), k = ArrayHelper.groupBy(b, 'versionId'), l = b.concat(a), n = f ? a.filter(o => o.name != null) : a;
    for (const o of l) {
      for (const p of (c = e._rowsByUnderlyingRecord.get(o)) != null ? c : EMPTY_ARRAY2) {
        j.add(p);
      }
    }
    e.suspendRefresh();
    i.remove(Array.from(j));
    for (const q of n) {
      const r = i.add(e.getGridRows(q, k[q.id]));
      e._rowsByUnderlyingRecord.set(q, r);
    }
    if (g) {
      for (const u of b.filter(v => v.versionId == null)) {
        const v = i.add(e.getGridRows(null, [u]));
        e._rowsByUnderlyingRecord.set(u, v);
      }
    }
    e.resumeRefresh(false);
    i.sort(i.sorters);
  }
  refreshGrid() {
    this.replaceRows(this._versionStore.records, this._transactionStore.records);
  }
  getGridRows(a, b) {
    const c = this, {
        showVersions: e,
        comparingVersionId: f
      } = c, g = (b == null ? void 0 : b.map(j => {
        let k;
        const l = 't-' + j.id;
        return {
          'id': l,
          'expanded': Boolean((k = c._expandedById) == null ? void 0 : k.get(l)),
          'description': j.description,
          'occurredAt': j.occurredAt,
          'transactionModel': j,
          'rootVersionModel': a,
          'children': j.actions.map((n, o) => {
            let p, q, r;
            const u = 'a-' + j.id + '-' + o;
            return {
              'id': u,
              'expanded': Boolean((p = c._expandedById) == null ? void 0 : p.get(u)),
              'action': n,
              'rootVersionModel': a,
              'children': (r = (q = n.propertyUpdates) == null ? void 0 : q.map(v => ({
                'rootVersionModel': a,
                'propertyUpdate': v
              }))) != null ? r : []
            };
          })
        };
      })) || [], i = 'v-' + (a == null ? void 0 : a.id);
    return a && e ? {
      'id': i,
      'expanded': Boolean(c._expandedById.get(i)),
      'description': a.description,
      'occurredAt': a.savedAt,
      'children': g,
      'versionModel': a,
      'iconCls': 'b-icon-version',
      'cls': a.id === f ? c.comparingRowCls : null
    } : g;
  }
  renderDescription(a) {
    const {record: b} = a;
    if (b.propertyUpdate) {
      return this.renderPropertyUpdate(b.propertyUpdate);
    } else if (b.action) {
      return this.renderActionDescription(b.action);
    }
    return b.description;
  }
  renderPropertyUpdate(a) {
    const b = _VersionGrid.type, {
        property: c,
        before: e,
        after: f
      } = a;
    return {
      'children': [{
          'tag': 'div',
          'class': 'b-' + b + '-property-update-desc',
          'children': [
            {
              'tag': 'span',
              'class': 'b-' + b + '-property-name',
              'html': '' + this.formatPropertyName(c)
            },
            this.renderPropertyValue(e, 'before'),
            {
              'tag': 'i',
              'class': 'b-icon b-icon-right'
            },
            this.renderPropertyValue(f, 'after')
          ]
        }]
    };
  }
  renderPropertyValue(a, b) {
    let c;
    return {
      'tag': 'span',
      'class': [
        'b-' + _VersionGrid.type + '-property-' + b,
        a == null && 'b-' + _VersionGrid.type + '-empty-value'
      ],
      'html': a == null ? this.L('L{Versions.nullValue}') : (c = this.formatValueString(a)) != null ? c : ''
    };
  }
  formatValueString(a) {
    if (DateHelper.isDate(a)) {
      return DateHelper.format(a, this.dateFormat);
    } else if (typeof a === 'number') {
      return a.toFixed(2);
    }
    return a;
  }
  formatPropertyName(a) {
    return StringHelper.separate(a);
  }
  getAssignmentTextTokens(a) {
    return {
      'event': a.event.name,
      'resource': a.resource.name
    };
  }
  getDependencyTextTokens(a) {
    return {
      'from': a.fromTask.name,
      'to': a.toTask.name
    };
  }
  renderActionDescription(a) {
    let b;
    const c = this, {
        actionType: e,
        entity: f
      } = a, g = c.L('L{Versions.entityNames}');
    let i, j = {
        'type': g[f.type],
        'name': f.name
      };
    if (e === 'move') {
      j.from = describePosition(a.from);
      j.to = describePosition(a.to);
    }
    i = c.L('L{Versions.' + e + ((b = knownEntityTypes[f.type]) != null ? b : 'Entity') + '}');
    if (f.type === 'DependencyModel') {
      j = c.getDependencyTextTokens(f);
    } else if (f.type === 'AssignmentModel') {
      j = c.getAssignmentTextTokens(f);
    }
    i = i.replace(/\{(\w+)\}/g, (k, l) => {
      let n;
      return (n = j[l]) != null ? n : l;
    });
    if (a.isUser) {
      i = '[!] ' + i;
    }
    return c.renderHighlightedTextElements(StringHelper.capitalize(i), j);
  }
  static sortActionRows(a, b) {
    if (a.parent === b.parent && a.action && b.action) {
      const c = Boolean(a.action.isUser), e = Boolean(b.action.isUser), {
          actionType: f,
          entity: {type: g}
        } = a.action, {
          actionType: i,
          entity: {type: j}
        } = b.action;
      if (c !== e) {
        return c ? -1 : 1;
      }
      return Math.sign(actionTypeOrder[f] - actionTypeOrder[i]) || Math.sign(entityTypeOrder[g] - entityTypeOrder[j]) || 0;
    }
    return 0;
  }
  triggerRestore(a) {
    this.trigger('restore', { 'version': a });
  }
  triggerCompare(a) {
    this.trigger('compare', { 'version': a });
  }
  triggerStopCompare(a) {
    this.trigger('stopCompare');
  }
  renderHighlightedTextElements(a) {
    const b = this.constructor.type;
    return {
      'children': [{
          'tag': 'span',
          'class': 'b-' + b + '-highlighted-text',
          'children': a.split(/\*\*/g).reduce((c, e) => {
            c.children.push({
              'tag': 'span',
              'text': e,
              'class': c.isEntity ? 'b-' + b + '-highlighted-entity' : null
            });
            c.isEntity = !c.isEntity;
            return c;
          }, {
            'children': [],
            'isEntity': false
          }).children
        }]
    };
  }
};
const VersionGrid = _VersionGrid;
VersionGrid.initClass();
VersionGrid._$name = 'VersionGrid';
export {
  AbstractAssignmentStoreMixin,
  AbstractCalendarManagerStoreMixin,
  AbstractCalendarMixin,
  AbstractCrudManager,
  AbstractCrudManagerMixin_default as AbstractCrudManagerMixin,
  AbstractCrudManagerValidation_default as AbstractCrudManagerValidation,
  AbstractDateRange_default as AbstractDateRange,
  AbstractDependencyStoreMixin,
  AbstractEventStoreMixin,
  AbstractHasAssignmentsMixin,
  AbstractPartOfProjectGenericMixin,
  AbstractPartOfProjectModelMixin,
  AbstractPartOfProjectStoreMixin,
  AbstractRecurrenceIterator,
  AbstractResourceStoreMixin,
  AbstractTimeRanges,
  ActionBase,
  ActionColumn,
  AddAction,
  AdvancedTab,
  AggregateColumn,
  AjaxHelper,
  AjaxStore,
  AjaxStoreLazyLoad as AjaxStoreLazyLoadPlugin,
  AjaxTransport_default as AjaxTransport,
  Animator,
  ArrayDataField,
  ArrayHelper,
  AssignmentAllocationInterval,
  AssignmentModel2 as AssignmentModel,
  AssignmentModelMixin_default as AssignmentModelMixin,
  AssignmentStore2 as AssignmentStore,
  AssignmentStoreMixin_default as AssignmentStoreMixin,
  AsyncHelper,
  AttachToProjectMixin_default as AttachToProjectMixin,
  AvailabilityRangeModel,
  AvailabilityRangeStore,
  AvatarRendering,
  Badge_default as Badge,
  Bag,
  Bar,
  Base,
  BaseAllocationInfo,
  BaseAllocationInterval,
  BaseAssignmentMixin,
  BaseCalendarMixin,
  BaseDependencyMixin,
  BaseDependencyResolution,
  BaseEmptyCalendarEffectResolution,
  BaseEventMixin,
  BaseHasAssignmentsMixin,
  BaseResourceMixin,
  BooleanCombo,
  BooleanDataField,
  Box,
  BreakCurrentStackExecution,
  BrowserHelper,
  Button,
  ButtonGroup,
  CI,
  CIFromSetOrArrayOrValue,
  CSSHelper,
  CalculateProposed,
  CalculatedValueGen,
  CalculatedValueGenC,
  CalculatedValueSync,
  CalculatedValueSyncC,
  CalculationGen,
  CalculationSync,
  CalendarAvailabilityIssue,
  CalendarCache,
  CalendarCacheInterval,
  CalendarCacheIntervalMultiple,
  CalendarCacheMultiple,
  CalendarCacheSingle,
  CalendarEditor,
  CalendarEditorAvailabilityRangeContainer,
  CalendarEditorBaseTab,
  CalendarEditorDateInfo,
  CalendarEditorDatePicker,
  CalendarEditorExceptionModel,
  CalendarEditorExceptionPanel,
  CalendarEditorExceptionTab,
  CalendarEditorIntervalBaseModel,
  CalendarEditorLegend,
  CalendarEditorStore,
  CalendarEditorWeekGrid,
  CalendarEditorWeekModel,
  CalendarEditorWeekPanel,
  CalendarEditorWeekTab,
  CalendarField,
  CalendarHighlight,
  CalendarIntervalMixin,
  CalendarIntervalModel,
  CalendarIntervalStore,
  CalendarIntervalType,
  CalendarIteratorResult,
  CalendarManagerStore,
  CalendarModel,
  CalendarPanel,
  CanCombineCalendarsMixin,
  Card,
  Carousel,
  CellCopyPaste,
  CellEdit2 as CellEdit,
  CellMenu,
  CellTooltip,
  ChangeLogAction,
  ChangeLogAssignmentEntity,
  ChangeLogDependencyEntity,
  ChangeLogEntity,
  ChangeLogPropertyUpdate,
  ChangeLogStore,
  ChangeLogTransactionModel,
  CheckColumn,
  Checkbox,
  ChipView,
  ChronoAbstractProjectMixin,
  ChronoAssignmentStoreMixin,
  ChronoCalendarManagerStoreMixin,
  ChronoDependencyStoreMixin,
  ChronoEventStoreMixin,
  ChronoEventTreeStoreMixin,
  Field2 as ChronoField,
  ChronoGraph,
  Base2 as ChronoMixinBase,
  ChronoModelFieldIdentifier,
  ChronoModelMixin,
  ChronoModelReferenceBucketFieldIdentifier,
  ChronoModelReferenceFieldIdentifier,
  ChronoModelReferenceFieldQuark,
  ChronoPartOfProjectGenericMixin,
  ChronoPartOfProjectModelMixin,
  ChronoPartOfProjectStoreMixin,
  ChronoResourceStoreMixin,
  ChronoStoreMixin,
  Transaction2 as ChronoTransaction,
  ClickRepeater,
  Clipboardable_default as Clipboardable,
  ClockTemplate,
  CodeEditor,
  CollapseTool,
  Collection,
  CollectionFilter,
  CollectionSorter,
  ColorColumn,
  ColorField,
  ColorPicker,
  Column,
  ColumnAutoWidth,
  ColumnDragToolbar,
  ColumnLines,
  ColumnPicker,
  ColumnRename,
  ColumnReorder,
  ColumnResize,
  ColumnStore,
  Combo,
  CommitZero,
  ComputationCycle,
  Config,
  ConfirmationBar,
  ConflictEffect,
  ConflictEffectDescription,
  ConflictResolution,
  ConflictSymbol,
  ConstrainedByDependenciesEventMixin,
  ConstrainedEarlyEventMixin,
  ConstraintInterval,
  ConstraintIntervalDescription,
  ConstraintIntervalSide,
  ConstraintType,
  ConstraintTypePicker,
  Container,
  ContextGen,
  ContextMenuBase,
  ContextSync,
  CopyPasteBase,
  CoreAssignmentMixin,
  CoreAssignmentStoreMixin,
  CoreCalendarManagerStoreMixin,
  CoreCalendarMixin,
  CoreDependencyMixin,
  CoreDependencyStoreMixin,
  CoreEventMixin,
  CoreEventStoreMixin,
  CoreHasAssignmentsMixin,
  CoreHasDependenciesMixin,
  CorePartOfProjectGenericMixin,
  CorePartOfProjectModelMixin,
  CorePartOfProjectStoreMixin,
  CoreResourceMixin,
  CoreResourceStoreMixin,
  StateTrackingManager as CoreStateTrackingManager,
  CrudManager,
  CrudManagerView_default as CrudManagerView,
  CycleDescription,
  CycleEffect,
  CycleEffectDescription,
  CycleResolution,
  CycleResolutionInput,
  CycleResolutionInputChrono,
  CycleResolutionPopup,
  CycleSymbol,
  DailyRecurrenceIterator,
  DataField,
  DataGenerator,
  DateColumn,
  DateConstraintInterval,
  DateConstraintIntervalDescription,
  DateDataField,
  DateField,
  DateHelper,
  DateInterval,
  DatePicker,
  DateRangeField,
  DateRangePicker,
  DateStoreLazyLoadPlugin,
  DateTimeField,
  DayAvailabilityModel,
  DayAvailabilityStore,
  DayButtons,
  DayTime,
  DeactivateDependencyCycleEffectResolution,
  DeactivateDependencyResolution,
  Delayable_default as Delayable,
  DelayableWrapper2 as DelayableWrapper,
  DelayedRecordsRendering_default as DelayedRecordsRendering,
  DemoBot,
  DemoCodeEditor,
  Dependencies2 as Dependencies,
  DependenciesCalendar,
  DependencyBaseModel,
  DependencyConstraintInterval,
  DependencyConstraintIntervalDescription,
  DependencyCreation_default as DependencyCreation,
  DependencyEdit2 as DependencyEdit,
  DependencyModel2 as DependencyModel,
  DependencyStore2 as DependencyStore,
  DependencyStoreMixin_default as DependencyStoreMixin,
  DependencyTab,
  DependencyTooltip_default as DependencyTooltip,
  DependencyType,
  DependencyTypePicker,
  DependencyValidationResult,
  Describable_default as Describable,
  Direction,
  DisplayField,
  DomClassList,
  DomDataStore,
  DomHelper,
  DomSync,
  DragBase,
  DragContext,
  DragCreateBase,
  DragHelper,
  DragProxy,
  DragTipProxy,
  Draggable_default as Draggable,
  Droppable_default as Droppable,
  Duration,
  DurationColumn,
  DurationConverterMixin,
  DurationField,
  DurationUnitDataField,
  DurationVar,
  DynamicObject,
  EMPTY_INTERVAL,
  EdgeInclusion,
  EdgeType,
  EdgeTypeNormal,
  EdgeTypePast,
  EditBase,
  Editor,
  EditorTab,
  Effect,
  EffectResolutionResult,
  EffortField,
  EffortVar,
  EmptyCalendarEffect,
  EmptyCalendarEffectDescription,
  EmptyCalendarSymbol,
  EndDateField,
  EndDateVar,
  EngineReplica,
  EngineRevision,
  EngineTransaction,
  Entity,
  EntityIdentifier,
  EntityMeta,
  EventBuffer,
  EventColorColumn,
  EventColorField,
  EventColorPicker,
  EventCopyPaste,
  EventDayIndex,
  EventDrag,
  EventDragCreate,
  EventDragSelect,
  EventEdit,
  EventEditor,
  EventFilter,
  EventHelper,
  EventLoader_default as EventLoader,
  EventMenu,
  EventModel2 as EventModel,
  EventModelMixin_default as EventModelMixin,
  EventNavigation_default as EventNavigation,
  EventNonWorkingTime,
  EventResize2 as EventResize,
  EventSegmentDrag,
  EventSegmentModel,
  EventSegmentResize,
  EventSegments,
  EventSelection_default as EventSelection,
  EventStore2 as EventStore,
  EventStoreMixin_default as EventStoreMixin,
  EventTooltip,
  EventUpdateAction,
  Events_default as Events,
  ExcelExporter2 as ExcelExporter,
  ExportDialog,
  ExportOrientationCombo,
  ExportRowsCombo,
  Exporter,
  ExporterBase,
  Factoryable_default as Factoryable,
  Featureable_default as Featureable,
  Fencible_default as Fencible,
  Field,
  FieldContainer,
  FieldFilterPicker,
  FieldFilterPickerGroup,
  FieldIdentifier,
  FieldSet,
  FileDrop,
  FileField,
  FileFormat,
  FilePicker,
  FillHandle,
  Filter,
  FilterBar,
  FilterField,
  Finalizable_default as Finalizable,
  Fit,
  FixedDurationMixin,
  Footer,
  FormTab,
  Formatter,
  Formula,
  FormulasCache,
  Fullscreen,
  FunctionHelper,
  GanttTaskEditor,
  GeneralTab,
  GetEventsMixin_default as GetEventsMixin,
  GetTransaction,
  GlobalEvents_default as GlobalEvents,
  Grid,
  GridBase,
  CellEdit as GridCellEdit,
  GridElementEvents_default as GridElementEvents,
  ExcelExporter as GridExcelExporter,
  GridFeatureManager,
  GridFeatures_default as GridFeatures,
  GridFieldFilterPicker,
  GridFieldFilterPickerGroup,
  GroupSummary as GridGroupSummary,
  GridLocation,
  LockRows as GridLockRows,
  MultiPageExporter as GridMultiPageExporter,
  MultiPageVerticalExporter as GridMultiPageVerticalExporter,
  PdfExport as GridPdfExport,
  Print as GridPrint,
  GridResponsive_default as GridResponsive,
  GridRowModel,
  RowReorder as GridRowReorder,
  RowResize as GridRowResize,
  GridSelection_default as GridSelection,
  SinglePageExporter as GridSinglePageExporter,
  Split as GridSplit,
  GridState_default as GridState,
  GridSubGrids_default as GridSubGrids,
  Summary as GridSummary,
  Group,
  GroupBar,
  GroupSummary2 as GroupSummary,
  HasCalendarMixin,
  HasChildrenMixin,
  HasDateConstraintMixin,
  HasDependenciesMixin,
  HasEffortMixin,
  HasPercentDoneMixin,
  HasPreviousValue,
  HasPreviousValueEffect,
  HasPreviousValueSymbol,
  HasProposedNotPreviousValue,
  HasProposedNotPreviousValueEffect,
  HasProposedNotPreviousValueSymbol,
  HasProposedValue,
  HasProposedValueEffect,
  HasProposedValueSymbol,
  HasSchedulingModeMixin,
  HasSubEventsMixin,
  Header,
  HeaderMenu,
  HeaderZoom,
  Helpers_default as Helpers,
  Histogram,
  HorizontalLayoutPack,
  HorizontalLayoutStack,
  HorizontalRendering,
  HorizontalTimeAxis,
  Hoverable_default as Hoverable,
  IdHelper,
  Identifiable_default as Identifiable,
  Identifier,
  IdentifierC,
  IgnoreResourceCalendarEmptyCalendarEffectResolution,
  IndexPosition,
  Axis as InfinityAxis,
  InfinityScrollable_default as InfinityScrollable,
  InfinityScroller,
  InsertAction,
  InsertChildAction,
  InstancePlugin,
  Instruction,
  IntegerDataField,
  IntervalCache,
  IntervalSorterFn,
  IsChronoModelSymbol,
  JsonEncoder_default as JsonEncoder,
  KeyMap_default as KeyMap,
  Label,
  Labelable_default as Labelable,
  Labels,
  Layout,
  LazyLoadCrudManager_default as LazyLoadCrudManager,
  LazyLoadView_default as LazyLoadView,
  Levels,
  List,
  Listener,
  LoadMaskable_default as LoadMaskable,
  LocaleHelper,
  LocaleManager_default as LocaleManager,
  Localizable_default as Localizable,
  LockRows2 as LockRows,
  MAX_DATE2 as MAX_DATE,
  MI,
  MIN_DATE2 as MIN_DATE,
  Mask,
  Menu,
  MenuItem,
  MergeCells,
  MessageDialog_default as MessageDialog,
  Meta,
  Minifiable_default as Minifiable,
  MinimalChronoModelFieldIdentifierGen,
  MinimalChronoModelFieldIdentifierSync,
  MinimalChronoModelFieldVariable,
  MinimalEntityIdentifier,
  MinimalFieldIdentifierGen,
  MinimalFieldIdentifierSync,
  MinimalFieldVariable,
  MinimalReferenceBucketIdentifier,
  MinimalReferenceBucketQuark,
  MinimalReferenceIdentifier,
  Model,
  ModelBucketField,
  ModelCombo,
  ModelDataField,
  ModelField,
  ModelLink_default as ModelLink,
  ModelReferenceField,
  ModelStm_default as ModelStm,
  Month,
  MonthlyRecurrenceIterator,
  MultiDatePicker,
  MultiPageExporter2 as MultiPageExporter,
  MultiPageVerticalExporter2 as MultiPageVerticalExporter,
  NOT_VISITED,
  Navigator,
  NestedEvents,
  NonWorkingTime,
  NonWorkingTimeMixin_default as NonWorkingTimeMixin,
  NotesTab,
  NumberColumn,
  NumberDataField,
  NumberField,
  NumberFormat,
  ObjectDataField,
  ObjectHelper,
  Objects,
  OnCycleAction,
  Orientation,
  Override,
  OwnIdentifier,
  OwnIdentifierSymbol,
  OwnQuark,
  OwnQuarkSymbol,
  PackMixin_default as PackMixin,
  PagingToolbar,
  Pan,
  Panel,
  PanelCollapser,
  PanelCollapserOverlay,
  PaperFormat,
  Parser_default as Parser,
  PartOfBaseProject_default as PartOfBaseProject,
  PartOfProject_default2 as PartOfProject,
  PasswordField,
  PdfExport2 as PdfExport,
  PercentBar,
  PercentColumn,
  PercentDoneMixin_default as PercentDoneMixin,
  PickerField,
  Pluggable_default as Pluggable,
  Point,
  Popup,
  PredecessorsTab,
  pm as PresetManager,
  PresetStore,
  PreviousValueOf,
  PreviousValueOfEffect,
  PreviousValueOfSymbol,
  Print2 as Print,
  PrintMixin_default as PrintMixin,
  ProHorizontalLayout_default as ProHorizontalLayout,
  ProHorizontalLayoutPack,
  ProHorizontalLayoutStack,
  ProjectChangeHandlerMixin_default as ProjectChangeHandlerMixin,
  ProjectCombo,
  ProjectConstraintResolution,
  ProjectConsumer_default as ProjectConsumer,
  ProjectCrudManager_default2 as ProjectCrudManager,
  ProjectModel2 as ProjectModel,
  ProjectModelCommon_default as ProjectModelCommon,
  ProjectModelMixin_default as ProjectModelMixin,
  ProjectModelTimeZoneMixin_default as ProjectModelTimeZoneMixin,
  ProjectProgressMixin_default as ProjectProgressMixin,
  ProjectRevisionHandlerMixin_default as ProjectRevisionHandlerMixin,
  ProjectType,
  Promissory,
  ProposedArgumentsOf,
  ProposedArgumentsOfEffect,
  ProposedArgumentsOfSymbol,
  ProposedOrPrevious,
  ProposedOrPreviousSymbol,
  ProposedOrPreviousValueOf,
  ProposedOrPreviousValueOfEffect,
  ProposedOrPreviousValueOfSymbol,
  ProposedValueOf,
  ProposedValueOfEffect,
  ProposedValueOfSymbol,
  Quark,
  QuarkGen,
  QuarkSync,
  QuickFind,
  RTL_default as RTL,
  Radio,
  RadioGroup,
  RandomGenerator,
  RatingColumn,
  ReadMode,
  ReadyStatePropagator_default as ReadyStatePropagator,
  Rectangle,
  RectangularPathFinder,
  RecurrenceCombo,
  RecurrenceConfirmationPopup,
  RecurrenceDayRuleEncoder,
  RecurrenceDaysButtonGroup,
  RecurrenceDaysCombo,
  RecurrenceEditor,
  RecurrenceEditorPanel,
  RecurrenceFrequencyCombo,
  RecurrenceLegend,
  RecurrenceLegendButton,
  RecurrenceModel,
  RecurrenceMonthDaysButtonGroup,
  RecurrenceMonthsButtonGroup,
  RecurrencePositionsCombo,
  RecurrenceStopConditionCombo,
  RecurrenceTab,
  RecurringEventEdit_default as RecurringEventEdit,
  RecurringEvents_default as RecurringEvents,
  RecurringEventsMixin_default as RecurringEventsMixin,
  RecurringTimeSpan_default as RecurringTimeSpan,
  RecurringTimeSpansMixin_default as RecurringTimeSpansMixin,
  ReferenceBucketField,
  ReferenceBucketIdentifier,
  ReferenceBucketQuark,
  ReferenceField,
  ReferenceIdentifier,
  RegionResize,
  Reject,
  RejectEffect,
  RejectSymbol,
  RemoveAction,
  RemoveAllAction,
  RemoveChildAction,
  RemoveDateConstraintConflictResolution,
  RemoveDependencyCycleEffectResolution,
  RemoveDependencyResolution,
  Replica,
  ResizeHelper,
  ResizeMonitor,
  ResourceAllocationEventRangeCalendar,
  ResourceAllocationEventRangeCalendarIntervalMixin,
  ResourceAllocationEventRangeCalendarIntervalStore,
  ResourceAllocationInfo,
  ResourceAllocationInterval,
  ResourceCalendarColumn,
  ResourceCollapseColumn,
  ResourceCombo,
  ResourceFilter,
  ResourceHeader,
  ResourceHistogram,
  ResourceInfoColumn,
  ResourceMenu,
  ResourceModel2 as ResourceModel,
  ResourceModelMixin_default as ResourceModelMixin,
  ResourceNonWorkingTime,
  ResourceStore2 as ResourceStore,
  ResourceStoreMixin_default as ResourceStoreMixin,
  ResourceTimeRangeModel,
  ResourceTimeRangeStore,
  ResourceTimeRanges,
  ResourceTimeRangesBase,
  ResourceUtilization,
  ResourceUtilizationModel,
  ResourceUtilizationStore,
  ResourcesTab,
  Responsive_default as Responsive,
  Revision,
  RevisionException,
  Ripple,
  Rotatable_default as Rotatable,
  Row,
  RowCopyPaste,
  RowExpander,
  RowManager,
  RowNumberColumn,
  RowReorder2 as RowReorder,
  RowResize2 as RowResize,
  RowsRange,
  SEDBackwardCycleResolutionContext,
  SEDDispatcher,
  SEDDispatcherIdentifier,
  SEDForwardCycleResolutionContext,
  SEDGraphDescription,
  SEDWUDispatcher,
  SEDWUDispatcherIdentifier,
  Scale,
  ScaleColumn,
  ScheduleContext,
  ScheduleMenu,
  ScheduleRange,
  ScheduleRangeCombo,
  ScheduleTableExporter,
  ScheduleTooltip,
  Scheduler,
  SchedulerAdvancedTab,
  AssignmentModel as SchedulerAssignmentModel,
  AssignmentStore as SchedulerAssignmentStore,
  SchedulerBase,
  SchedulerBasicEvent,
  SchedulerBasicProjectMixin,
  SchedulerCoreEvent,
  SchedulerCoreProjectMixin,
  SchedulerDatePicker,
  Dependencies as SchedulerDependencies,
  DependencyEdit as SchedulerDependencyEdit,
  DependencyModel as SchedulerDependencyModel,
  DependencyStore as SchedulerDependencyStore,
  SchedulerDom_default as SchedulerDom,
  SchedulerDomEvents_default as SchedulerDomEvents,
  EventModel as SchedulerEventModel,
  SchedulerEventRendering_default as SchedulerEventRendering,
  EventResize as SchedulerEventResize,
  EventStore as SchedulerEventStore,
  SchedulerExportDialog,
  SchedulerGeneralTab,
  PartOfProject_default as SchedulerPartOfProject,
  SchedulerPro,
  SchedulerProAssignmentMixin,
  SchedulerProBase,
  SchedulerProBaseEvent,
  SchedulerProCycleEffect,
  SchedulerProDependencyMixin,
  SchedulerProEmptyCalendarEffect,
  SchedulerProEvent,
  SchedulerProEventRendering_default as SchedulerProEventRendering,
  SchedulerProHasAssignmentsMixin,
  SchedulerProProjectMixin,
  SchedulerProResourceMixin,
  ProjectCrudManager_default as SchedulerProjectCrudManager,
  ProjectModel as SchedulerProjectModel,
  SchedulerRegions_default as SchedulerRegions,
  ResourceModel as SchedulerResourceModel,
  SchedulerResourceRendering_default as SchedulerResourceRendering,
  ResourceStore as SchedulerResourceStore,
  SchedulerScroll_default as SchedulerScroll,
  SchedulerState_default as SchedulerState,
  SchedulerStores_default as SchedulerStores,
  SchedulerTaskEditor,
  SchedulingDirectionPicker,
  SchedulingIssueEffectResolution,
  SchedulingIssueResolution_default as SchedulingIssueResolution,
  SchedulingIssueResolutionPopup,
  SchedulingMode,
  SchedulingModePicker,
  Schema,
  ScrollButtons,
  ScrollManager,
  Scroller,
  Search,
  SimpleEventEdit,
  SinglePageExporter2 as SinglePageExporter,
  SlideToggle,
  Slider,
  Sort,
  SortedMap,
  Split2 as Split,
  Splitter,
  StartDateField,
  StartDateVar,
  StartedTaskScheduling,
  State_default as State,
  StateBase,
  StateProvider,
  StateStorage,
  StateTrackingManager2 as StateTrackingManager,
  StickyCells,
  StickyEvents,
  Store,
  StoreCRUD_default as StoreCRUD,
  StoreChained_default as StoreChained,
  StoreChanges_default as StoreChanges,
  StoreDataField,
  StoreFilter_default as StoreFilter,
  StoreGroup_default as StoreGroup,
  StoreLazyLoadPlugin,
  StoreProxy_default as StoreProxy,
  StoreRelation_default as StoreRelation,
  StoreSearch_default as StoreSearch,
  StoreSort_default as StoreSort,
  StoreState_default as StoreState,
  StoreStm_default as StoreStm,
  StoreSum_default as StoreSum,
  StoreSync_default as StoreSync,
  StoreTimeZonePlugin,
  StoreTree_default as StoreTree,
  StringDataField,
  StringHelper,
  Stripe,
  Styleable_default as Styleable,
  SubGrid,
  SuccessorsTab,
  Summary2 as Summary,
  SynchronousCalculationStarted,
  Tab,
  TabBar,
  TabPanel,
  TableExporter,
  TaskEdit,
  TaskEditStm_default as TaskEditStm,
  TaskEditTransactional_default as TaskEditTransactional,
  TaskEditorBase,
  TemplateColumn,
  TemplateHelper,
  TextAreaField,
  TextAreaPickerField,
  TextField,
  TimeAxis,
  TimeAxisColumn,
  TimeAxisHeaderMenu,
  TimeAxisSubGrid,
  TimeAxisViewModel,
  TimeColumn,
  TimeField,
  TimePicker,
  TimeRangeModel,
  TimeRangeStore,
  TimeRanges,
  TimeSelection,
  TimeSpan,
  TimeSpanHighlight,
  TimeSpanMenuBase,
  TimeUnit,
  TimeZoneHelper,
  TimeZonedDatesMixin_default as TimeZonedDatesMixin,
  Timeline,
  TimelineBase,
  TimelineDateMapper_default as TimelineDateMapper,
  TimelineDomEvents_default as TimelineDomEvents,
  TimelineEventRendering_default as TimelineEventRendering,
  TimelineHistogram,
  TimelineHistogramBase,
  TimelineHistogramGrouping_default as TimelineHistogramGrouping,
  TimelineHistogramScaleColumn_default as TimelineHistogramScaleColumn,
  TimelineScroll_default as TimelineScroll,
  TimelineState_default as TimelineState,
  TimelineSummary,
  TimelineViewPresets_default as TimelineViewPresets,
  TimelineZoomable_default as TimelineZoomable,
  Toast,
  TombStone,
  Tool,
  Toolable_default as Toolable,
  Toolbar,
  Tooltip,
  TooltipBase,
  Transaction,
  TransactionCycleDetectionWalkContext,
  TransactionSymbol,
  TransactionWalkDepth,
  TransactionalFeature_default as TransactionalFeature,
  TransactionalFeatureMixin_default as TransactionalFeatureMixin,
  Tree,
  TreeColumn,
  TreeCombo,
  TreeGrid,
  TreeGroup,
  TreeNode_default as TreeNode,
  UndoRedo,
  UndoRedoBase,
  UnitsVar,
  UnsafePreviousValueOf,
  UnsafePreviousValueOfEffect,
  UnsafePreviousValueOfSymbol,
  UnsafeProposedOrPreviousValueOf,
  UnsafeProposedOrPreviousValueOfEffect,
  UnsafeProposedOrPreviousValueOfSymbol,
  UnspecifiedTimeIntervalModel,
  UpdateAction,
  Use24hrsEmptyCalendarEffectResolution,
  Use8hrsEmptyCalendarEffectResolution,
  VBox,
  VISITED_TOPOLOGICALLY,
  Validatable_default as Validatable,
  Variable,
  VariableC,
  VariableInputState,
  VariableWalkContext,
  VersionGrid,
  VersionHelper,
  VersionModel,
  VersionStore,
  Versions,
  VerticalRendering,
  VerticalTimeAxisColumn,
  ViewPreset,
  ViewPresetCombo,
  WalkContext,
  WalkHelper,
  WalkSource,
  WalkState,
  Wbs,
  WeeklyRecurrenceIterator,
  Widget,
  WidgetColumn,
  WidgetHelper,
  Write,
  WriteEffect,
  WriteSeveral,
  WriteSeveralEffect,
  WriteSeveralSymbol,
  WriteSymbol,
  XMLHelper,
  YearPicker,
  YearlyRecurrenceIterator,
  binarySearch,
  bucket,
  build_proposed,
  calculate,
  combineCalendars,
  createEntityOnPrototype,
  cycleInfo,
  dateConverter,
  delay,
  durationFormula,
  effortFormula,
  endDateByEffortFormula,
  endDateFormula,
  ensureEntityOnPrototype,
  entity,
  entityDecoratorBody,
  field,
  fixedDurationAndEffortSEDWUGraphDescription,
  fixedDurationSEDWUBackwardEffortDriven,
  fixedDurationSEDWUBackwardNonEffortDriven,
  fixedDurationSEDWUForwardEffortDriven,
  fixedDurationSEDWUForwardNonEffortDriven,
  fixedDurationSEDWUGraphDescription,
  format,
  generic_field,
  getDecoratedModelFields,
  injectStaticFieldsProperty,
  intersectIntervals,
  isAtomicValue,
  isDateFinite,
  isEqualEffectiveDirection,
  isNotNumber,
  isSerializableEqual,
  later,
  model_field,
  prototypeValue,
  reference,
  required,
  runGeneratorAsyncWithEffect,
  runGeneratorSyncWithEffect,
  startDateByEffortFormula,
  startDateFormula,
  stripDuplicates,
  throwUnknownIdentifier,
  unitMagnitudes,
  unitsFormula,
  validateRequiredProperties,
  write
};