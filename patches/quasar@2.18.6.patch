diff --git a/dist/quasar.client.js b/dist/quasar.client.js
index efe39d03a7204fcd5b478749465d71221077137f..16c5f63a18cb9c3fab3613f96c22fb7b06458e65 100644
--- a/dist/quasar.client.js
+++ b/dist/quasar.client.js
@@ -3428,11 +3428,16 @@ function use_model_toggle_default({
   function processHide(evt) {
     if (showing.value === false) return;
     showing.value = false;
-    emit("beforeHide", evt);
+    const event = evt ?? new Event('custom');
+    emit("beforeHide", event);
+    if (event.qPreventHide) {
+      showing.value = true;
+      return;
+    }
     if (handleHide !== void 0) {
-      handleHide(evt);
+      handleHide(event);
     } else {
-      emit("hide", evt);
+      emit("hide", event);
     }
   }
   function processModelChange(val) {
@@ -20767,7 +20772,8 @@ var QSelect_default = createComponent({
     // override of useVirtualScrollProps > virtualScrollItemSize (no default)
     virtualScrollItemSize: useVirtualScrollProps.virtualScrollItemSize.type,
     onNewValue: Function,
-    onFilter: Function
+    onFilter: Function,
+    disableDownOpenMenu: Boolean
   },
   emits: [
     ...useFieldEmits,
@@ -21158,7 +21164,7 @@ var QSelect_default = createComponent({
         return;
       }
       if (e.target === void 0 || e.target.id !== state.targetUid.value || state.editable.value !== true) return;
-      if (e.keyCode === 40 && state.innerLoading.value !== true && menu.value === false) {
+      if (e.keyCode === 40 && state.innerLoading.value !== true && menu.value === false && props4.disableDownOpenMenu === false) {
         stopAndPrevent(e);
         showPopup();
         return;
@@ -21187,7 +21193,7 @@ var QSelect_default = createComponent({
         );
         moveOptionSelection(e.keyCode === 33 ? 1 : -1, props4.multiple);
       }
-      if (e.keyCode === 38 || e.keyCode === 40) {
+      if ((e.keyCode === 38 || e.keyCode === 40) && (menu.value === true || props4.disableDownOpenMenu === false)) {
         stopAndPrevent(e);
         moveOptionSelection(e.keyCode === 38 ? -1 : 1, props4.multiple);
       }
diff --git a/dist/types/index.d.ts b/dist/types/index.d.ts
index a72729bbd0d20a9059287d31962c5fea96dfe3cf..7ba7415ad5ead1640e13231e063b6c4d67d8c122 100644
--- a/dist/types/index.d.ts
+++ b/dist/types/index.d.ts
@@ -11022,6 +11022,10 @@ export interface QSelectProps {
    * Default value: 'default'
    */
   behavior?: "default" | "menu" | "dialog" | undefined;
+  /**
+   * When menu is closed, prevent ArrowDown from opening the menu
+   */
+  disableDownOpenMenu?: boolean;
   /**
    * Emitted when the virtual scroll occurs
    * @param details Object of properties on the new scroll position
diff --git a/src/components/select/QSelect.js b/src/components/select/QSelect.js
index b0d922f75b8850275ab4d5426774e9e8f8d92437..39dba48f2e402c2eb1d2de10b4354ff1f4d16d02 100644
--- a/src/components/select/QSelect.js
+++ b/src/components/select/QSelect.js
@@ -134,7 +134,8 @@ export default createComponent({
     virtualScrollItemSize: useVirtualScrollProps.virtualScrollItemSize.type,
 
     onNewValue: Function,
-    onFilter: Function
+    onFilter: Function,
+    disableDownOpenMenu: Boolean
   },
 
   emits: [
@@ -748,6 +749,7 @@ export default createComponent({
         e.keyCode === 40
         && state.innerLoading.value !== true
         && menu.value === false
+        && props.disableDownOpenMenu === false
       ) {
         stopAndPrevent(e)
         showPopup()
@@ -801,7 +803,7 @@ export default createComponent({
       }
 
       // up, down
-      if (e.keyCode === 38 || e.keyCode === 40) {
+      if ((e.keyCode === 38 || e.keyCode === 40) && (menu.value === true || props.disableDownOpenMenu === false)) {
         stopAndPrevent(e)
         moveOptionSelection(e.keyCode === 38 ? -1 : 1, props.multiple)
       }
diff --git a/src/composables/private.use-model-toggle/use-model-toggle.js b/src/composables/private.use-model-toggle/use-model-toggle.js
index 672ff183796fda899c0565f1e20c2006adfebbed..ec124f02ba84ed7db7f5c329420ff9aee09b7040 100644
--- a/src/composables/private.use-model-toggle/use-model-toggle.js
+++ b/src/composables/private.use-model-toggle/use-model-toggle.js
@@ -103,13 +103,19 @@ export default function ({
 
     showing.value = false
 
-    emit('beforeHide', evt)
+    const event = evt ?? new Event('custom')
+    emit('beforeHide', event)
+
+    if (event.qPreventHide) {
+      showing.value = true
+      return
+    }
 
     if (handleHide !== void 0) {
-      handleHide(evt)
+      handleHide(event)
     }
     else {
-      emit('hide', evt)
+      emit('hide', event)
     }
   }
 
