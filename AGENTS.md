# Project Landmines

- Prefer Quasar over Element Plus for new UI.
- `apps/machine-status` uses kysely; all other apps use knex.
- Auth API routes: use `defineAuthEventHandler`, not `defineEventHandler`.
